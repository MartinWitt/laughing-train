# dubbo 
 
# Bad smells
I found 3398 bad smells with 439 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| ReturnNull | 531 | false |
| AssignmentToMethodParameter | 237 | false |
| EmptyMethod | 175 | false |
| ConstantValue | 148 | false |
| MissortedModifiers | 146 | false |
| SizeReplaceableByIsEmpty | 145 | true |
| ZeroLengthArrayInitialization | 116 | false |
| AbstractClassNeverImplemented | 111 | false |
| RedundantSuppression | 102 | false |
| UtilityClassWithoutPrivateConstructor | 98 | true |
| RedundantFieldInitialization | 94 | false |
| PublicFieldAccessedInSynchronizedContext | 84 | false |
| DataFlowIssue | 84 | false |
| NonProtectedConstructorInAbstractClass | 79 | true |
| BoundedWildcard | 71 | false |
| NestedAssignment | 64 | false |
| FieldAccessedSynchronizedAndUnsynchronized | 63 | false |
| UnnecessaryFullyQualifiedName | 58 | false |
| SynchronizeOnThis | 55 | false |
| CatchMayIgnoreException | 53 | false |
| CommentedOutCode | 40 | false |
| DynamicRegexReplaceableByCompiledPattern | 39 | false |
| UnusedAssignment | 38 | false |
| SystemOutErr | 38 | false |
| RegExpUnnecessaryNonCapturingGroup | 37 | false |
| UnnecessaryLocalVariable | 28 | true |
| CodeBlock2Expr | 28 | true |
| IgnoreResultOfCall | 27 | false |
| RegExpRedundantEscape | 26 | false |
| UnnecessaryQualifierForThis | 24 | false |
| InfiniteRecursion | 22 | false |
| EqualsBetweenInconvertibleTypes | 20 | false |
| AssignmentToStaticFieldFromInstanceMethod | 19 | false |
| ProtectedMemberInFinalClass | 19 | true |
| ReplaceAssignmentWithOperatorAssignment | 18 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 15 | false |
| StringConcatenationInsideStringBufferAppend | 14 | false |
| InstanceofCatchParameter | 13 | false |
| RedundantLengthCheck | 13 | false |
| UnnecessaryToStringCall | 13 | true |
| KeySetIterationMayUseEntrySet | 12 | false |
| UseOfPropertiesAsHashtable | 12 | false |
| IOResource | 12 | false |
| MissingDeprecatedAnnotation | 12 | false |
| PointlessArithmeticExpression | 11 | false |
| UnnecessaryBoxing | 11 | false |
| BusyWait | 10 | false |
| UseBulkOperation | 10 | false |
| UnnecessaryUnboxing | 9 | false |
| StringBufferReplaceableByString | 9 | false |
| DeprecatedIsStillUsed | 9 | false |
| AssignmentToForLoopParameter | 9 | false |
| RegExpSimplifiable | 8 | false |
| NonFinalFieldOfException | 8 | false |
| InnerClassMayBeStatic | 8 | true |
| CollectionContainsUrl | 7 | false |
| NonExceptionNameEndsWithException | 7 | false |
| PointlessBitwiseExpression | 6 | false |
| OptionalContainsCollection | 6 | false |
| AssignmentToLambdaParameter | 6 | false |
| RedundantArrayCreation | 6 | true |
| UnnecessarySuperQualifier | 6 | false |
| TypeParameterExtendsObject | 6 | false |
| NonSerializableFieldInSerializableClass | 6 | false |
| SuspiciousMethodCalls | 6 | false |
| ConditionCoveredByFurtherCondition | 6 | false |
| TrivialStringConcatenation | 5 | false |
| MismatchedCollectionQueryUpdate | 5 | false |
| DoubleBraceInitialization | 5 | false |
| CastConflictsWithInstanceof | 5 | false |
| ManualMinMaxCalculation | 5 | false |
| StringEqualsEmptyString | 5 | false |
| ThrowablePrintStackTrace | 5 | false |
| UnnecessaryContinue | 5 | false |
| FinalStaticMethod | 4 | false |
| SimplifyStreamApiCallChains | 4 | false |
| CaughtExceptionImmediatelyRethrown | 4 | false |
| UnnecessaryStringEscape | 4 | true |
| UnnecessaryReturn | 4 | true |
| OptionalGetWithoutIsPresent | 4 | false |
| Convert2MethodRef | 4 | false |
| Convert2Lambda | 4 | false |
| ToArrayCallWithZeroLengthArrayArgument | 3 | true |
| EmptyStatementBody | 3 | false |
| ManualArrayToCollectionCopy | 3 | false |
| UnnecessarySemicolon | 3 | false |
| UnnecessaryCallToStringValueOf | 3 | false |
| CharsetObjectCanBeUsed | 3 | false |
| RedundantClassCall | 3 | false |
| InfiniteLoopStatement | 3 | false |
| RedundantImplements | 3 | false |
| FieldMayBeStatic | 3 | false |
| AbstractMethodCallInConstructor | 3 | false |
| NestedSynchronizedStatement | 3 | false |
| SynchronizeOnNonFinalField | 3 | false |
| EqualsWhichDoesntCheckParameterClass | 3 | false |
| UnnecessaryModifier | 2 | true |
| WhileCanBeForeach | 2 | false |
| EmptyFinallyBlock | 2 | false |
| DuplicateCondition | 2 | false |
| ReturnFromFinallyBlock | 2 | false |
| TypeParameterHidesVisibleType | 2 | false |
| MethodOverloadsParentMethod | 2 | false |
| NonSynchronizedMethodOverridesSynchronizedMethod | 2 | false |
| ComparatorResultComparison | 2 | false |
| OptionalUsedAsFieldOrParameterType | 2 | false |
| NonStrictComparisonCanBeEquality | 2 | true |
| EmptyTryBlock | 2 | false |
| RefusedBequest | 2 | false |
| DuplicateExpressions | 2 | false |
| ThrowFromFinallyBlock | 2 | false |
| MethodOverridesStaticMethod | 2 | false |
| NumericOverflow | 2 | false |
| Finalize | 1 | false |
| CStyleArrayDeclaration | 1 | false |
| ObjectNotify | 1 | false |
| AccessStaticViaInstance | 1 | false |
| RedundantMethodOverride | 1 | false |
| OptionalAssignedToNull | 1 | false |
| IntegerMultiplicationImplicitCastToLong | 1 | false |
| CallToStringConcatCanBeReplacedByOperator | 1 | false |
| IndexOfReplaceableByContains | 1 | false |
| ReplaceInefficientStreamCount | 1 | false |
| StringEqualsCharSequence | 1 | false |
| SuspiciousToArrayCall | 1 | false |
| StringEquality | 1 | false |
| SuspiciousSystemArraycopy | 1 | false |
| ManualArrayCopy | 1 | false |
| ObsoleteCollection | 1 | false |
| CollectionAddedToSelf | 1 | false |
| DefaultAnnotationParam | 1 | false |
| IfStatementWithIdenticalBranches | 1 | false |
| NonStaticFinalLogger | 1 | false |
| FuseStreamOperations | 1 | false |
| RedundantStringFormatCall | 1 | false |
| NullArgumentToVariableArgMethod | 1 | false |
| ParameterCanBeLocal | 1 | false |
| Java8MapApi | 1 | false |
| CopyConstructorMissesField | 1 | false |
| ThreadStartInConstruction | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[collection.size()\]'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java

    public static String[] toStringArray(Collection<String> collection) {
        return collection == null ? null : collection.toArray(new String[collection.size()]);
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[groups.size()\]'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java

            // convert list to array
            Class<?>[] classgroups = groups.toArray(new Class[groups.size()]);

            Object parameterBean = getMethodParameterBean(clazz, method, arguments);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[groups.size()\]'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java

            // convert list to array
            Class<?>[] classgroups = groups.toArray(new Class[groups.size()]);

            Object parameterBean = getMethodParameterBean(clazz, method, arguments);
```

## RuleId[id=Finalize]
### Finalize
'finalize()' should not be overridden
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java

    @Override
    protected void finalize() throws Throwable {
        try {
            super.finalize();
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Mixin.java`
#### Snippet
```java
    abstract public Object newInstance(Object[] ds);

    public static interface MixinAware {
        void setMixinInstance(Object instance);
    }
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
    }

    public static interface DC {

    } // dynamic class tag interface.
```

## RuleId[id=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
                ((b[off + 2] & 0xFFL) << 40) +
                ((b[off + 1] & 0xFFL) << 48) +
                (((long) b[off + 0]) << 56);
        return Double.longBitsToDouble(j);
    }
```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
    public static short bytes2short(byte[] b, int off) {
        return (short) (((b[off + 1] & 0xFF) << 0) +
                ((b[off + 0]) << 8));
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
    public static void short2bytes(short v, byte[] b, int off) {
        b[off + 1] = (byte) v;
        b[off + 0] = (byte) (v >>> 8);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
                ((b[off + 2] & 0xFF) << 8) +
                ((b[off + 1] & 0xFF) << 16) +
                ((b[off + 0]) << 24);
        return Float.intBitsToFloat(i);
    }
```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 2] = (byte) (v >>> 8);
        b[off + 1] = (byte) (v >>> 16);
        b[off + 0] = (byte) (v >>> 24);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
                ((b[off + 2] & 0xFFL) << 40) +
                ((b[off + 1] & 0xFFL) << 48) +
                (((long) b[off + 0]) << 56);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
                ((b[off + 2] & 0xFF) << 8) +
                ((b[off + 1] & 0xFF) << 16) +
                ((b[off + 0]) << 24);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 2] = (byte) (i >>> 8);
        b[off + 1] = (byte) (i >>> 16);
        b[off + 0] = (byte) (i >>> 24);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 2] = (byte) (j >>> 40);
        b[off + 1] = (byte) (j >>> 48);
        b[off + 0] = (byte) (j >>> 56);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 2] = (byte) (v >>> 40);
        b[off + 1] = (byte) (v >>> 48);
        b[off + 0] = (byte) (v >>> 56);
    }

```

### PointlessArithmeticExpression
`1 * 1024` can be replaced with '1024'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Constants.java`
#### Snippet
```java
    int MAX_BUFFER_SIZE = 16 * 1024;

    int MIN_BUFFER_SIZE = 1 * 1024;

    String IDLE_TIMEOUT_KEY = "idle.timeout";
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`else` statement has empty body
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
                    f.run();
                    return f;
                } else {

                }
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
                logger.warn(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", "Url of this instance will be deleted from registry soon. " +
                    "Dubbo client will try to re-register once a new session is created.");
            } else if (state == StateListener.SUSPENDED) {

            } else if (state == StateListener.CONNECTED) {
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
            } else if (state == StateListener.SUSPENDED) {

            } else if (state == StateListener.CONNECTED) {

            }
```

## RuleId[id=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LogUtil.java`
#### Snippet
```java
        Log.info("PrintList:");
        Iterator<T> it = list.iterator();
        while (it.hasNext()) {
            Log.info(it.next().toString());
        }
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
        }
        Iterator<Map.Entry<MetadataIdentifier, Object>> iterable = metadataMap.entrySet().iterator();
        while (iterable.hasNext()) {
            Map.Entry<MetadataIdentifier, Object> item = iterable.next();
            if (PROVIDER_SIDE.equals(item.getKey().getSide())) {
```

## RuleId[id=EmptyFinallyBlock]
### EmptyFinallyBlock
Empty `finally` block
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                }
                throw e;
            } finally {

            }
```

### EmptyFinallyBlock
Empty `finally` block
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                }
                throw e;
            } finally {

            }
```

## RuleId[id=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of local variable `str`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MD5Utils.java`
#### Snippet
```java

        int j = md5.length;
        char str[] = new char[j * 2];
        int k = 0;
        for (int i = 0; i < j; i++) {
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (5 lines)
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/DemoServiceImpl.java`
#### Snippet
```java
    public CompletableFuture<String> sayHelloAsync(String name) {
        CompletableFuture<String> cf = CompletableFuture.supplyAsync(() -> {
//            try {
//                Thread.sleep(1000);
//            } catch (InterruptedException e) {
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
    @Override
    public boolean cancel(boolean mayInterruptIfRunning) {
//        Invocation invocation = invocationSoftReference.get();
//        if (invocation != null) {
//            invocation.getInvoker().invoke(cancel);
```

### CommentedOutCode
Commented out code (8 lines)
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ServiceBuilder.java`
#### Snippet
```java
    }

//    @Override
//    public ServiceBuilder<U> mock(String mock) {
//        throw new IllegalArgumentException("mock doesn't support on provider side");
```

### CommentedOutCode
Commented out code (21 lines)
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java

        // Ignore checking new module after start
//        executorRepository.getSharedExecutor().submit(() -> {
//            try {
//                while (isStarting()) {
```

### CommentedOutCode
Commented out code (10 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java

        if (ProtocolConfig.class.equals(beanClass)) {
//            for (String name : parserContext.getRegistry().getBeanDefinitionNames()) {
//                BeanDefinition definition = parserContext.getRegistry().getBeanDefinition(name);
//                PropertyValue property = definition.getPropertyValues().getPropertyValue("protocol");
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
        //https://github.com/apache/dubbo/pull/6079
        //https://github.com/apache/dubbo/issues/6035
//        Environment environment = parserContext.getReaderContext().getEnvironment();
//        return environment.resolvePlaceholders(attributeValue);
        return attributeValue;
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java
    }

//    public ReferenceBeanBuilder setRouter(String router) {
//        attributes.put(ReferenceAttributes.ROUTER, router);
//        return this;
```

### CommentedOutCode
Commented out code (10 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java
    }

//    @Deprecated
//    public ReferenceBeanBuilder setApplication(ApplicationConfig application) {
//        attributes.put(ReferenceAttributes.APPLICATION, application);
```

### CommentedOutCode
Commented out code (27 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java
    }

//    public ReferenceBeanBuilder setAuth(Boolean auth) {
//        attributes.put(ReferenceAttributes.AUTH, auth);
//        return this;
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/URL.java`
#### Snippet
```java
    }

//    public List<URL> getBackupUrls() {
//        List<org.apache.dubbo.common.URL> res = super.getBackupUrls();
//        return res.stream().map(url -> new URL(url)).collect(Collectors.toList());
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerMethodModel.java`
#### Snippet
```java
public class ConsumerMethodModel {
    private final Method method;
    //    private final boolean isCallBack;
//    private final boolean isFuture;
    private final String[] parameterTypes;
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerMethodModel.java`
#### Snippet
```java
    }

//    public ConcurrentMap<String, Object> getAttributeMap() {
//        return attributeMap;
//    }
```

### CommentedOutCode
Commented out code (8 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/PathURLAddress.java`
#### Snippet
```java
            buf.append("://");
        }
//
//        if (StringUtils.isNotEmpty(username)) {
//            buf.append(username);
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                            String group = params.get(GROUP_KEY);
                            String version = params.get(VERSION_KEY);
                            // params.remove("group");
                            // params.remove("version");
                            String name = serviceName;
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
            password = defaultPassword;
        }
        /*if (u.isAnyHost() || u.isLocalHost()) {
            changed = true;
            host = NetUtils.getLocalHost();
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                    String group = params.get(GROUP_KEY);
                    String version = params.get(VERSION_KEY);
                    //params.remove("group");
                    //params.remove("version");
                    String name = serviceName;
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                String group = params.get(GROUP_KEY);
                String version = params.get(VERSION_KEY);
                //params.remove("group");
                //params.remove("version");
                String name = serviceName;
```

### CommentedOutCode
Commented out code (7 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java

            return value;
//            if (StringUtils.isEmpty(value)) {
//                // Forward compatible, make sure key dynamic increment can work.
//                // In that case, some values which are proceed before increment will set in EXTRA_PARAMS.
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
    }

//    static List<Method> getOverriderMethods(Class<?> implementationClass, Class<?>... superTypes) {

//
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
        this.name = name;
        // FIXME, add id strategy in ConfigManager
//        if (StringUtils.isEmpty(id)) {
//            id = name;
//        }
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java

    public void setTransporter(String transporter) {
        /*if(transporter != null && transporter.length() > 0 && ! this.getExtensionLoader(Transporter.class).hasExtension(transporter)){
            throw new IllegalStateException("No such transporter type : " + transporter);
        }*/
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java

    public void setClient(String client) {
        /*if(client != null && client.length() > 0 && ! this.getExtensionLoader(Transporter.class).hasExtension(client)){
            throw new IllegalStateException("No such client type : " + client);
        }*/
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java

    public void setServer(String server) {
        /*if(server != null && server.length() > 0 && ! this.getExtensionLoader(Transporter.class).hasExtension(server)){
            throw new IllegalStateException("No such server type : " + server);
        }*/
```

### CommentedOutCode
Commented out code (5 lines)
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java
    @Override
    protected void doClose() throws Throwable {
        /*try {
            bootstrap.releaseExternalResources();
        } catch (Throwable t) {
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyServer.java`
#### Snippet
```java
                NettyCodecAdapter adapter = new NettyCodecAdapter(getCodec(), getUrl(), NettyServer.this);
                ChannelPipeline pipeline = Channels.pipeline();
                /*int idleTimeout = getIdleTimeout();
                if (idleTimeout > 10000) {
                    pipeline.addLast("timer", new IdleStateHandler(timer, idleTimeout / 1000, 0, 0));
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyPortUnificationServer.java`
#### Snippet
```java
                NettyCodecAdapter adapter = new NettyCodecAdapter(getCodec(), getUrl(), NettyPortUnificationServer.this);
                ChannelPipeline pipeline = Channels.pipeline();
                /*int idleTimeout = getIdleTimeout();
                if (idleTimeout > 10000) {
                    pipeline.addLast("timer", new IdleStateHandler(timer, idleTimeout / 1000, 0, 0));
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
    protected void postProcessAfterScopeModelChanged(ScopeModel oldScopeModel, ScopeModel newScopeModel) {
        // remove this config from old ConfigManager
//        if (oldScopeModel != null && oldScopeModel instanceof ApplicationModel) {
//           ((ApplicationModel)oldScopeModel).getApplicationConfigManager().removeConfig(this);
//        }
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java

        // binding attachments into invocation.
//        Map<String, Object> contextAttachments = RpcContext.getClientAttachment().getObjectAttachments();
//        if (contextAttachments != null && contextAttachments.size() != 0) {
//            ((RpcInvocation) invocation).addObjectAttachmentsIfAbsent(contextAttachments);
```

### CommentedOutCode
Commented out code (6 lines)
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ChannelWrappedInvoker.java`
#### Snippet
```java
    @Override
    public void destroy() {
//        super.destroy();
//        try {
//            channel.close();
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/NodeBuilder.java`
#### Snippet
```java

    public static Node build() {
//        String podName = System.getenv("metadata.name");
//        String podNamespace = System.getenv("metadata.namespace");

```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/BootstrapperImpl.java`
#### Snippet
```java
            ChannelCredentials channelCredentials =
                parseChannelCredentials(JsonUtil.checkObjectList(rawChannelCredsList), serverUri);
//            if (channelCredentials == null) {
//                throw new XdsInitializationException(
//                    "Server " + serverUri + ": no supported channel credentials found");
```

### CommentedOutCode
Commented out code (5 lines)
in `dubbo-test/dubbo-test-common/src/main/java/org/apache/dubbo/test/common/impl/DemoServiceImpl.java`
#### Snippet
```java
    public CompletableFuture<String> sayHelloAsync(String name) {
        CompletableFuture<String> cf = CompletableFuture.supplyAsync(() -> {
//            try {
//                Thread.sleep(1000);
//            } catch (InterruptedException e) {
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
        TestSource testSource = testIdentifier.getSource().orElse(null);
        if (testSource instanceof ClassSource) {
//            ClassSource source = (ClassSource) testSource;
//            log("Run test class: " + source.getClassName());
        } else if (testSource instanceof MethodSource) {
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/ShutdownTelnet.java`
#### Snippet
```java
        }
        // TODO change to ApplicationDeployer.destroy() or ApplicationModel.destroy()
//        DubboShutdownHook.getDubboShutdownHook().unregister();
//        DubboShutdownHook.getDubboShutdownHook().doDestroy();
        long end = System.currentTimeMillis();
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/MetadataReportInstance.java`
#### Snippet
```java
        url = url.addParameterIfAbsent(REGISTRY_LOCAL_FILE_CACHE_ENABLED, String.valueOf(applicationModel.getCurrentConfig().getEnableFileCache()));
        String relatedRegistryId = isEmpty(config.getRegistry()) ? (isEmpty(config.getId()) ? DEFAULT_KEY : config.getId()) : config.getRegistry();
//        RegistryConfig registryConfig = applicationModel.getConfigManager().getRegistry(relatedRegistryId)
//                .orElseThrow(() -> new IllegalStateException("Registry id " + relatedRegistryId + " does not exist."));
        MetadataReport metadataReport = metadataReportFactory.getMetadataReport(url);
```

### CommentedOutCode
Commented out code (198 lines)
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/FileSystemServiceDiscovery.java`
#### Snippet
```java
 * limitations under the License.
 */
//package org.apache.dubbo.registry.client;
//
//import com.alibaba.fastjson.JSON;
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
                if (CollectionUtils.isEmpty(mappingByUrl)) {
                    logger.info("No interface-apps mapping found in local cache, stop subscribing, will automatically wait for mapping listener callback: " + url);
//                if (check) {
//                    throw new IllegalStateException("Should has at least one way to know which services this interface belongs to, subscription url: " + url);
//                }
```

### CommentedOutCode
Commented out code (7 lines)
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
    }

//    public List<MediaType> getConsumeMediaTypes() {
//        return toMediaTypes(consumes);
//    }
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
            addAll(httpHeaders, headers);
            // Handles "Content-Type" and "Accept" headers if present
//            mediaTypes(httpHeaders, HttpHeaders.CONTENT_TYPE, this.consumes);
//            mediaTypes(httpHeaders, HttpHeaders.ACCEPT, this.produces);
            this.headers.putAll(httpHeaders);
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
                        buf.append(rowbuf.toString());
                        rowbuf = new StringBuilder();
//                        for(int m = 0;m < maxcountbefore && maxcountbefore < totalWidth ; m++){
//                            rowbuf.append(" ");
//                        }
```

## RuleId[id=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `context` from instance context
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/RestDemoServiceImpl.java`
#### Snippet
```java
    @Override
    public Integer hello(Integer a, Integer b) {
        context = RpcContext.getServerAttachment().getObjectAttachments();
        return a + b;
    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `CuratorWatcherImpl.closed` from instance context
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
        super.close();
        client.close();
        CuratorWatcherImpl.closed = true;
        synchronized (CuratorWatcherImpl.class) {
            if (CuratorWatcherImpl.CURATOR_WATCHER_EXECUTOR_SERVICE != null) {
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `CuratorWatcherImpl.CURATOR_WATCHER_EXECUTOR_SERVICE` from instance context
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
            if (CuratorWatcherImpl.CURATOR_WATCHER_EXECUTOR_SERVICE != null) {
                CuratorWatcherImpl.CURATOR_WATCHER_EXECUTOR_SERVICE.shutdown();
                CuratorWatcherImpl.CURATOR_WATCHER_EXECUTOR_SERVICE = null;
            }
        }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `CuratorWatcherImpl.closed` from instance context
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
            }

            CuratorWatcherImpl.closed = false;
        } catch (Exception e) {
            close();
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `INSTANCE` from instance context
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
    public void init() throws ServletException {
        super.init();
        INSTANCE = this;
        String config = getServletConfig().getInitParameter("pages");
        Collection<String> names;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `lastPrintTime` from instance context
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/support/AbortPolicyWithReport.java`
#### Snippet
```java
                guard.release();
            }
            lastPrintTime = System.currentTimeMillis();
        });
        //must shutdown thread pool ,if not will lead to OOM
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `urlListMapCache` from instance context
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                if ((urlListMap = urlListMapCache.get()) == null) {
                    urlListMap = new ConcurrentHashMap<>();
                    urlListMapCache = new SoftReference<>(urlListMap);
                }
            }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `CONVERT_NO_LOWER_HEADER` from instance context
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleProtocol.java`
#### Snippet
```java
        this.pathResolver = frameworkModel.getExtensionLoader(PathResolver.class)
            .getDefaultExtension();
        CONVERT_NO_LOWER_HEADER = ConfigurationUtils.getEnvConfiguration(ApplicationModel.defaultModel())
            .getBoolean(H2_SUPPORT_NO_LOWER_HEADER_KEY, true);
        IGNORE_1_0_0_VERSION = ConfigurationUtils.getEnvConfiguration(ApplicationModel.defaultModel())
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `IGNORE_1_0_0_VERSION` from instance context
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleProtocol.java`
#### Snippet
```java
        CONVERT_NO_LOWER_HEADER = ConfigurationUtils.getEnvConfiguration(ApplicationModel.defaultModel())
            .getBoolean(H2_SUPPORT_NO_LOWER_HEADER_KEY, true);
        IGNORE_1_0_0_VERSION = ConfigurationUtils.getEnvConfiguration(ApplicationModel.defaultModel())
            .getBoolean(H2_IGNORE_1_0_0_KEY, false);
        RESOLVE_FALLBACK_TO_DEFAULT = ConfigurationUtils.getEnvConfiguration(ApplicationModel.defaultModel())
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `RESOLVE_FALLBACK_TO_DEFAULT` from instance context
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleProtocol.java`
#### Snippet
```java
        IGNORE_1_0_0_VERSION = ConfigurationUtils.getEnvConfiguration(ApplicationModel.defaultModel())
            .getBoolean(H2_IGNORE_1_0_0_KEY, false);
        RESOLVE_FALLBACK_TO_DEFAULT = ConfigurationUtils.getEnvConfiguration(ApplicationModel.defaultModel())
            .getBoolean(H2_RESOLVE_FALLBACK_TO_DEFAULT_KEY, false);
        Set<String> supported = frameworkModel.getExtensionLoader(DeCompressor.class)
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `debugEnabled` from instance context
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/jetty/JettyLoggerAdapter.java`
#### Snippet
```java
    @Override
    public void setDebugEnabled(boolean enabled) {
        debugEnabled = enabled;
    }

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `DispatcherServlet.INSTANCE` from instance context
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/servlet/DispatcherServlet.java`
#### Snippet
```java

    public DispatcherServlet() {
        DispatcherServlet.INSTANCE = this;
    }

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `RDS_LISTENER` from instance context
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsRouteRuleManager.java`
#### Snippet
```java
        synchronized (RdsRouteRuleManager.class) {
            if (RDS_LISTENER == null) {
                RDS_LISTENER = rds -> {
                    if (rds == null) {
                        return;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `RDS_RESULT` from instance context
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsRouteRuleManager.java`
#### Snippet
```java
                        }
                    }
                    RDS_RESULT = rds;
                };
            }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `LDS_LISTENER` from instance context
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsRouteRuleManager.java`
#### Snippet
```java
            }
            if (LDS_LISTENER == null) {
                LDS_LISTENER = new Consumer<Map<String, ListenerResult>>() {
                    private volatile Set<String> configNames = null;

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `context` from instance context
in `dubbo-test/dubbo-test-common/src/main/java/org/apache/dubbo/test/common/impl/RestDemoServiceImpl.java`
#### Snippet
```java
    @Override
    public Integer hello(Integer a, Integer b) {
        context = RpcContext.getServerAttachment().getObjectAttachments();
        return a + b;
    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `providerContext` from instance context
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringXmlConfigTest.java`
#### Snippet
```java

    private void startProvider() {
        providerContext = new ClassPathXmlApplicationContext("/spring/dubbo-demo-provider.xml");
    }

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `context` from instance context
in `dubbo-container/dubbo-container-spring/src/main/java/org/apache/dubbo/container/spring/SpringContainer.java`
#### Snippet
```java
                context.stop();
                context.close();
                context = null;
            }
        } catch (Throwable e) {
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `context` from instance context
in `dubbo-container/dubbo-container-spring/src/main/java/org/apache/dubbo/container/spring/SpringContainer.java`
#### Snippet
```java
            configPath = DEFAULT_SPRING_CONFIG;
        }
        context = new ClassPathXmlApplicationContext(configPath.split("[,\\s]+"), false);
        context.refresh();
        context.start();
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java

    private static final Logger logger = LoggerFactory.getLogger(DubboBeanDefinitionParser.class);
    private static final Pattern GROUP_AND_VERSION = Pattern.compile("^[\\-.0-9_a-zA-Z]+(\\:[\\-.0-9_a-zA-Z]+)?$");
    private static final String ONRETURN = "onreturn";
    private static final String ONTHROW = "onthrow";
```

### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
    private static final int MAX_PORT = 65535;

    private static final Pattern ADDRESS_PATTERN = Pattern.compile("^\\d{1,3}(\\.\\d{1,3}){3}\\:\\d{1,5}$");
    private static final Pattern LOCAL_IP_PATTERN = Pattern.compile("127(\\.\\d{1,3}){3}$");
    private static final Pattern IP_PATTERN = Pattern.compile("\\d{1,3}(\\.\\d{1,3}){3,5}$");
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ConfigUtils.class);
    private static final Pattern VARIABLE_PATTERN = Pattern.compile(
        "\\$\\s*\\{?\\s*([\\._0-9a-zA-Z]+)\\s*\\}?");
    private static int PID = -1;

```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ConfigUtils.class);
    private static final Pattern VARIABLE_PATTERN = Pattern.compile(
        "\\$\\s*\\{?\\s*([\\._0-9a-zA-Z]+)\\s*\\}?");
    private static int PID = -1;

```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
    private static final byte[] HEX2B;
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
    private static final byte[] HEX2B;
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern KVP_PATTERN = Pattern.compile("([_.a-zA-Z0-9][-_.a-zA-Z0-9]*)[=](.*)"); //key value pair pattern.
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern KVP_PATTERN = Pattern.compile("([_.a-zA-Z0-9][-_.a-zA-Z0-9]*)[=](.*)"); //key value pair pattern.
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern KVP_PATTERN = Pattern.compile("([_.a-zA-Z0-9][-_.a-zA-Z0-9]*)[=](.*)"); //key value pair pattern.
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";

    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/AbstractCompiler.java`
#### Snippet
```java
public abstract class AbstractCompiler implements Compiler {

    private static final Pattern PACKAGE_PATTERN = Pattern.compile("package\\s+([$_a-zA-Z][$_a-zA-Z0-9\\.]*);");

    private static final Pattern CLASS_PATTERN = Pattern.compile("class\\s+([$_a-zA-Z][$_a-zA-Z0-9]*)\\s+");
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");

    private static final Pattern IMPLEMENTS_PATTERN = Pattern.compile("\\s+implements\\s+([\\w\\.]+)\\s*\\{\n");

    private static final Pattern METHODS_PATTERN = Pattern.compile("\n(private|public|protected)\\s+");
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
    private static final Pattern IMPORT_PATTERN = Pattern.compile("import\\s+([\\w\\.\\*]+);\n");

    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");

    private static final Pattern IMPLEMENTS_PATTERN = Pattern.compile("\\s+implements\\s+([\\w\\.]+)\\s*\\{\n");
```

### RegExpRedundantEscape
Redundant character escape `\\{` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
    private static final Pattern IMPORT_PATTERN = Pattern.compile("import\\s+([\\w\\.\\*]+);\n");

    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");

    private static final Pattern IMPLEMENTS_PATTERN = Pattern.compile("\\s+implements\\s+([\\w\\.]+)\\s*\\{\n");
```

### RegExpRedundantEscape
Redundant character escape `\\,` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
        matcher = IMPLEMENTS_PATTERN.matcher(source);
        if (matcher.find()) {
            String[] ifaces = matcher.group(1).trim().split("\\,");
            Arrays.stream(ifaces).forEach(i -> builder.addInterface(i.trim()));
        }
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
public class JavassistCompiler extends AbstractCompiler {

    private static final Pattern IMPORT_PATTERN = Pattern.compile("import\\s+([\\w\\.\\*]+);\n");

    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");
```

### RegExpRedundantEscape
Redundant character escape `\\*` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
public class JavassistCompiler extends AbstractCompiler {

    private static final Pattern IMPORT_PATTERN = Pattern.compile("import\\s+([\\w\\.\\*]+);\n");

    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/argument/ArgumentConditionMatcher.java`
#### Snippet
```java
public class ArgumentConditionMatcher extends AbstractConditionMatcher {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ArgumentConditionMatcher.class);
    private static final Pattern ARGUMENTS_PATTERN = Pattern.compile("arguments\\[([0-9]+)\\]");

    public ArgumentConditionMatcher(String key, ModuleModel model) {
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/attachment/AttachmentConditionMatcher.java`
#### Snippet
```java
public class AttachmentConditionMatcher extends AbstractConditionMatcher {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AttachmentConditionMatcher.class);
    private static final Pattern ATTACHMENTS_PATTERN = Pattern.compile("attachments\\[(.+)\\]");

    public AttachmentConditionMatcher(String key, ModuleModel model) {
```

## RuleId[id=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `numStats.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java
    public List<GaugeMetricSample> exportNumMetrics() {
        List<GaugeMetricSample> list = new ArrayList<>();
        for (RegistryEvent.Type type : numStats.keySet()) {
            Map<String, AtomicLong> stringAtomicLongMap = numStats.get(type);
            for (String applicationName : stringAtomicLongMap.keySet()) {
```

### KeySetIterationMayUseEntrySet
Iteration over `stringAtomicLongMap.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java
        for (RegistryEvent.Type type : numStats.keySet()) {
            Map<String, AtomicLong> stringAtomicLongMap = numStats.get(type);
            for (String applicationName : stringAtomicLongMap.keySet()) {
                list.add(convertToSample(applicationName, type, MetricsCategory.REGISTRY, stringAtomicLongMap.get(applicationName)));
            }
```

### KeySetIterationMayUseEntrySet
Iteration over `skStats.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java
    public List<GaugeMetricSample> exportSkMetrics() {
        List<GaugeMetricSample> list = new ArrayList<>();
        for (RegistryEvent.Type type : skStats.keySet()) {
            Map<ServiceKeyMetric, AtomicLong> stringAtomicLongMap = skStats.get(type);
            for (ServiceKeyMetric serviceKeyMetric : stringAtomicLongMap.keySet()) {
```

### KeySetIterationMayUseEntrySet
Iteration over `numStats.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/collector/stat/MetadataStatComposite.java`
#### Snippet
```java
    public List<GaugeMetricSample> exportNumMetrics() {
        List<GaugeMetricSample> list = new ArrayList<>();
        for (MetadataEvent.Type type : numStats.keySet()) {
            Map<String, AtomicLong> stringAtomicLongMap = numStats.get(type);
            for (String applicationName : stringAtomicLongMap.keySet()) {
```

### KeySetIterationMayUseEntrySet
Iteration over `stringAtomicLongMap.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/collector/stat/MetadataStatComposite.java`
#### Snippet
```java
        for (MetadataEvent.Type type : numStats.keySet()) {
            Map<String, AtomicLong> stringAtomicLongMap = numStats.get(type);
            for (String applicationName : stringAtomicLongMap.keySet()) {
                list.add(convertToSample(applicationName, type, MetricsCategory.REGISTRY, stringAtomicLongMap.get(applicationName)));
            }
```

### KeySetIterationMayUseEntrySet
Iteration over `serviceDiscoveries.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleServiceDiscovery.java`
#### Snippet
```java
        MultiServiceInstancesChangedListener multiListener = (MultiServiceInstancesChangedListener) listener;

        for (String registryKey : serviceDiscoveries.keySet()) {
            ServiceDiscovery serviceDiscovery = serviceDiscoveries.get(registryKey);
            SingleServiceInstancesChangedListener singleListener = multiListener.getAndComputeIfAbsent(registryKey, k ->
```

### KeySetIterationMayUseEntrySet
Iteration over `allHeaders.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java

        // header
        for (String headerName : allHeaders.keySet()) {
            Collection<String> headerValues = allHeaders.get(headerName);

```

### KeySetIterationMayUseEntrySet
Iteration over `queries.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

        StringBuilder queryBuilder = new StringBuilder("?");
        for (String field : queries.keySet()) {

            Collection<String> queryValues = queries.get(field);
```

### KeySetIterationMayUseEntrySet
Iteration over `allHeaders.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/HttpClientRestClient.java`
#### Snippet
```java
        allHeaders.remove("Content-Length");
        // header
        for (String headerName : allHeaders.keySet()) {
            Collection<String> headerValues = allHeaders.get(headerName);

```

### KeySetIterationMayUseEntrySet
Iteration over `RDS_LISTENERS.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsRouteRuleManager.java`
#### Snippet
```java
                    }
                    for (RouteResult routeResult : rds.values()) {
                        for (String domainToNotify : RDS_LISTENERS.keySet()) {
                            VirtualHost virtualHost = routeResult.searchVirtualHost(domainToNotify);
                            if (virtualHost != null) {
```

### KeySetIterationMayUseEntrySet
Iteration over `properties.keySet()` may be replaced with 'entrySet()' iteration
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
    private String propertiesToString(Properties properties) {
        StringBuilder builder = new StringBuilder();
        for (Object key : properties.keySet()) {
            builder.append(key);
            builder.append(": ");
```

### KeySetIterationMayUseEntrySet
Iteration over `urls` may be replaced with 'entrySet()' iteration
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
        // remove child listener
        Set<URL> urls = zkListeners.keySet();
        for (URL url : urls) {
            ConcurrentMap<NotifyListener, ChildListener> map = zkListeners.get(url);
            if (CollectionUtils.isEmptyMap(map)) {
```

## RuleId[id=ObjectNotify]
### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
                    notify(key, listener, list);
                    synchronized (listener) {
                        listener.notify();
                    }
                }
```

## RuleId[id=UnnecessaryQualifierForThis]
### UnnecessaryQualifierForThis
Qualifier `MulticastRegistry` on 'this' is unnecessary in this context
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
                            msg = msg.substring(0, i).trim();
                        }
                        MulticastRegistry.this.receive(msg, (InetSocketAddress) recv.getSocketAddress());
                        Arrays.fill(buf, (byte) 0);
                    } catch (Throwable e) {
```

### UnnecessaryQualifierForThis
Qualifier `CuratorWatcherImpl` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
                Runnable task = () -> {
                    try {
                        childListener.childChanged(path, client.getChildren().usingWatcher(CuratorWatcherImpl.this).forPath(path));
                    } catch (Exception e) {
                        logger.warn(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", "client get children error", e);
```

### UnnecessaryQualifierForThis
Qualifier `DubboBootstrap` on 'this' is unnecessary in this context
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
    private void notifyStopped(ApplicationModel applicationModel) {
        ExtensionLoader<DubboBootstrapStartStopListener> exts = applicationModel.getExtensionLoader(DubboBootstrapStartStopListener.class);
        exts.getSupportedExtensionInstances().forEach(ext -> ext.onStop(DubboBootstrap.this));
        executeMutually(() -> {
            awaited.set(true);
```

### UnnecessaryQualifierForThis
Qualifier `DubboBootstrap` on 'this' is unnecessary in this context
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
    private void notifyStarted(ApplicationModel applicationModel) {
        ExtensionLoader<DubboBootstrapStartStopListener> exts = applicationModel.getExtensionLoader(DubboBootstrapStartStopListener.class);
        exts.getSupportedExtensionInstances().forEach(ext -> ext.onStart(DubboBootstrap.this));
    }

```

### UnnecessaryQualifierForThis
Qualifier `NettyClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java
                    // Close old channel
                    // copy reference
                    Channel oldChannel = NettyClient.this.channel;
                    if (oldChannel != null) {
                        try {
```

### UnnecessaryQualifierForThis
Qualifier `NettyClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java
                    }
                } finally {
                    if (NettyClient.this.isClosed()) {
                        try {
                            if (logger.isInfoEnabled()) {
```

### UnnecessaryQualifierForThis
Qualifier `NettyClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java
                            newChannel.close();
                        } finally {
                            NettyClient.this.channel = null;
                            NettyChannel.removeChannelIfDisconnected(newChannel);
                        }
```

### UnnecessaryQualifierForThis
Qualifier `NettyClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java
                        }
                    } else {
                        NettyClient.this.channel = newChannel;
                    }
                }
```

### UnnecessaryQualifierForThis
Qualifier `NettyConnectionClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConnectionClient.java`
#### Snippet
```java
            if (LOGGER.isDebugEnabled()) {
                LOGGER.debug(String.format("%s aborted to reconnect cause connection closed. ",
                        NettyConnectionClient.this));
            }
        }
```

### UnnecessaryQualifierForThis
Qualifier `NettyClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java
                try {
                    // Close old channel
                    Channel oldChannel = NettyClient.this.channel; // copy reference
                    if (oldChannel != null) {
                        try {
```

### UnnecessaryQualifierForThis
Qualifier `NettyClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java
                    }
                } finally {
                    if (NettyClient.this.isClosed()) {
                        try {
                            if (logger.isInfoEnabled()) {
```

### UnnecessaryQualifierForThis
Qualifier `NettyClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java
                            newChannel.close();
                        } finally {
                            NettyClient.this.channel = null;
                            NettyChannel.removeChannelIfDisconnected(newChannel);
                        }
```

### UnnecessaryQualifierForThis
Qualifier `NettyClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java
                        }
                    } else {
                        NettyClient.this.channel = newChannel;
                    }
                }
```

### UnnecessaryQualifierForThis
Qualifier `ReflectionAbstractServerCall` on 'this' is unnecessary in this context
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/ReflectionAbstractServerCall.java`
#### Snippet
```java
        }
        if (methodDescriptor == null) {
            ReflectionAbstractServerCall.this.close(TriRpcStatus.UNIMPLEMENTED.withDescription(
                "Method :" + methodName + "[" + Arrays.toString(
                    paramTypes) + "] " + "not found of service:"
```

### UnnecessaryQualifierForThis
Qualifier `ReflectionAbstractServerCall` on 'this' is unnecessary in this context
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/ReflectionAbstractServerCall.java`
#### Snippet
```java
            return;
        }
        listener = ReflectionAbstractServerCall.this.startInternalCall(invocation,
            methodDescriptor, invoker);
    }
```

### UnnecessaryQualifierForThis
Qualifier `TripleClientCall` on 'this' is unnecessary in this context
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/TripleClientCall.java`
#### Snippet
```java
    @Override
    public void onStart() {
        listener.onStart(TripleClientCall.this);
    }

```

### UnnecessaryQualifierForThis
Qualifier `TripleClientStream` on 'this' is unnecessary in this context
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleClientStream.java`
#### Snippet
```java
        final CancelQueueCommand cmd = CancelQueueCommand.createCommand(streamChannelFuture, Http2Error.CANCEL);

        TripleClientStream.this.rst = true;
        return this.writeQueue.enqueue(cmd);
    }
```

### UnnecessaryQualifierForThis
Qualifier `NacosRegistry` on 'this' is unnecessary in this context
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
        }
        List<URL> aggregatedUrls = toUrlWithEmpty(url, listener.saveAndAggregateAllInstances(serviceName, enabledInstances));
        NacosRegistry.this.notify(url, listener.getNotifyListener(), aggregatedUrls);
    }

```

### UnnecessaryQualifierForThis
Qualifier `ZookeeperRegistry` on 'this' is unnecessary in this context
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
                    " Since ephemeral ZNode will not get deleted for a connection lose, " +
                    "there's no need to re-register url of this instance.");
                ZookeeperRegistry.this.fetchLatestAddresses();
            } else if (state == StateListener.NEW_SESSION_CREATED) {
                logger.warn(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", "Trying to re-register urls and re-subscribe listeners of this instance to registry...");
```

### UnnecessaryQualifierForThis
Qualifier `ZookeeperRegistry` on 'this' is unnecessary in this context
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java

                try {
                    ZookeeperRegistry.this.recover();
                } catch (Exception e) {
                    logger.error(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", e.getMessage(), e);
```

### UnnecessaryQualifierForThis
Qualifier `HeaderExchangeClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeClient.java`
#### Snippet
```java
    private void startReconnectTask(URL url) {
        if (shouldReconnect(url)) {
            AbstractTimerTask.ChannelProvider cp = () -> Collections.singletonList(HeaderExchangeClient.this);
            int idleTimeout = getIdleTimeout(url);
            long heartbeatTimeoutTick = calculateLeastDuration(idleTimeout);
```

### UnnecessaryQualifierForThis
Qualifier `HeaderExchangeClient` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeClient.java`
#### Snippet
```java
    private void startHeartBeatTask(URL url) {
        if (!client.canHandleIdle()) {
            AbstractTimerTask.ChannelProvider cp = () -> Collections.singletonList(HeaderExchangeClient.this);
            int heartbeat = getHeartbeat(url);
            long heartbeatTick = calculateLeastDuration(heartbeat);
```

### UnnecessaryQualifierForThis
Qualifier `HeaderExchangeServer` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeServer.java`
#### Snippet
```java
                sendChannelReadOnlyEvent();
            }
            while (HeaderExchangeServer.this.isRunning()
                && System.currentTimeMillis() - start < (long) timeout) {
                try {
```

### UnnecessaryQualifierForThis
Qualifier `HeaderExchangeServer` on 'this' is unnecessary in this context
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeServer.java`
#### Snippet
```java
    private void startIdleCheckTask(URL url) {
        if (!server.canHandleIdle()) {
            AbstractTimerTask.ChannelProvider cp = () -> unmodifiableCollection(HeaderExchangeServer.this.getChannels());
            int idleTimeout = getIdleTimeout(url);
            long idleTimeoutTick = calculateLeastDuration(idleTimeout);
```

## RuleId[id=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`notifiedUrls.size() > 0` can be replaced with '!notifiedUrls.isEmpty()'
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
        List<URL> urls = new ArrayList<>();
        Map<String, List<URL>> notifiedUrls = getNotified().get(url);
        if (notifiedUrls != null && notifiedUrls.size() > 0) {
            for (List<URL> values : notifiedUrls.values()) {
                urls.addAll(values);
```

### SizeReplaceableByIsEmpty
`endpointsList.size() > 0` can be replaced with '!endpointsList.isEmpty()'
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
            // get endpoints directly from informer local store
            List<Endpoints> endpointsList = endInformer.getStore().list();
            if (endpointsList.size() > 0) {
                endpoints = endpointsList.get(0);
            }
```

### SizeReplaceableByIsEmpty
`attachment.size() > 0` can be replaced with '!attachment.isEmpty()'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    public RpcContextAttachment setAttachments(Map<String, String> attachment) {
        this.attachments.clear();
        if (attachment != null && attachment.size() > 0) {
            this.attachments.putAll(attachment);
        }
```

### SizeReplaceableByIsEmpty
`attachment.size() > 0` can be replaced with '!attachment.isEmpty()'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    public RpcContext setAttachments(Map<String, String> attachment) {
        CLIENT_ATTACHMENT.get().attachments.clear();
        if (attachment != null && attachment.size() > 0) {
            CLIENT_ATTACHMENT.get().attachments.putAll(attachment);
        }
```

### SizeReplaceableByIsEmpty
`attachment.size() > 0` can be replaced with '!attachment.isEmpty()'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    public RpcContext setObjectAttachments(Map<String, Object> attachment) {
        CLIENT_ATTACHMENT.get().attachments.clear();
        if (attachment != null && attachment.size() > 0) {
            CLIENT_ATTACHMENT.get().attachments.putAll(attachment);
        }
```

### SizeReplaceableByIsEmpty
`host.length() == 0` can be replaced with 'host.isEmpty()'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
                localAddress.getAddress() == null ? localAddress.getHostName()
                        : NetUtils.filterLocalHost(localAddress.getAddress().getHostAddress());
        if (host == null || host.length() == 0) {
            return NetUtils.getLocalHost();
        }
```

### SizeReplaceableByIsEmpty
`mock.length() == 0` can be replaced with 'mock.isEmpty()'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
        mock = mock.trim();

        if (mock.length() == 0) {
            return mock;
        }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractInvoker.java`
#### Snippet
```java
        for (String key : keys) {
            String value = url.getParameter(key);
            if (value != null && value.length() > 0) {
                attachment.put(key, value);
            }
```

### SizeReplaceableByIsEmpty
`listeners.size() > 0` can be replaced with '!listeners.isEmpty()'
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java

        boolean hasInternalListener() {
            return listeners != null && listeners.size() > 0;
        }
    }
```

### SizeReplaceableByIsEmpty
`wait.trim().length() > 0` can be replaced with '!wait.trim().isEmpty()'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
        PropertiesConfiguration configuration = scopeModel.getModelEnvironment().getPropertiesConfiguration();
        String wait = configuration.getProperty(SHUTDOWN_WAIT_KEY);
        if (wait != null && wait.trim().length() > 0) {
            System.setProperty(SHUTDOWN_WAIT_KEY, wait.trim());
        } else {
```

### SizeReplaceableByIsEmpty
`wait.trim().length() > 0` can be replaced with '!wait.trim().isEmpty()'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
        } else {
            wait = configuration.getProperty(SHUTDOWN_WAIT_SECONDS_KEY);
            if (wait != null && wait.trim().length() > 0) {
                System.setProperty(SHUTDOWN_WAIT_SECONDS_KEY, wait.trim());
            }
```

### SizeReplaceableByIsEmpty
`sysAddress.length() > 0` can be replaced with '!sysAddress.isEmpty()'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
        String address = null;
        String sysAddress = System.getProperty(DUBBO_MONITOR_ADDRESS);
        if (sysAddress != null && sysAddress.length() > 0) {
            address = sysAddress;
        } else if (monitor != null) {
```

### SizeReplaceableByIsEmpty
`defaultRegistries.size() > 0` can be replaced with '!defaultRegistries.isEmpty()'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java

        List<RegistryConfig> defaultRegistries = configManager.getDefaultRegistries();
        if (defaultRegistries.size() > 0) {
            defaultRegistries
                .stream()
```

### SizeReplaceableByIsEmpty
`registeredReferenceBeanNames.size() > 0` can be replaced with '!registeredReferenceBeanNames.isEmpty()'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
        // find reference bean name by reference key
        List<String> registeredReferenceBeanNames = referenceBeanManager.getBeanNamesByKey(referenceKey);
        if (registeredReferenceBeanNames.size() > 0) {
            // found same name and reference key
            if (registeredReferenceBeanNames.contains(referenceBeanName)) {
```

### SizeReplaceableByIsEmpty
`registeredReferenceBeanNames.size() > 0` can be replaced with '!registeredReferenceBeanNames.isEmpty()'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java

        // If registered matched reference before, just register alias
        if (registeredReferenceBeanNames.size() > 0) {
            beanDefinitionRegistry.registerAlias(registeredReferenceBeanNames.get(0), referenceBeanName);
            referenceBeanManager.registerReferenceKeyAndBeanName(referenceKey, referenceBeanName);
```

### SizeReplaceableByIsEmpty
`resource.length() > 0` can be replaced with '!resource.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
    private long getLastModified(String uri) {
        for (String resource : resources) {
            if (resource != null && resource.length() > 0) {
                String path = resource + uri;
                if (isFile(path)) {
```

### SizeReplaceableByIsEmpty
`config.length() > 0` can be replaced with '!config.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
    public void init(FilterConfig filterConfig) throws ServletException {
        String config = filterConfig.getInitParameter("resources");
        if (config != null && config.length() > 0) {
            String[] configs = Constants.COMMA_SPLIT_PATTERN.split(config);
            for (String c : configs) {
```

### SizeReplaceableByIsEmpty
`c.length() > 0` can be replaced with '!c.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
            String[] configs = Constants.COMMA_SPLIT_PATTERN.split(config);
            for (String c : configs) {
                if (c != null && c.length() > 0) {
                    c = c.replace('\\', '/');
                    if (c.endsWith("/")) {
```

### SizeReplaceableByIsEmpty
`rows.size() == 0` can be replaced with 'rows.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                                    List<List<String>> rows) {
        int n = random.nextInt();
        int c = (columns == null ? (rows == null || rows.size() == 0 ? 0 : rows.get(0).size())
                : columns.size());
        int r = (rows == null ? 0 : rows.size());
```

### SizeReplaceableByIsEmpty
`columns.size() > 0` can be replaced with '!columns.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
        writer.println("        <th colspan=\"" + c + "\">" + title + "</th>");
        writer.println("    </tr>");
        if (columns != null && columns.size() > 0) {
            writer.println("    <tr>");
            for (int i = 0; i < columns.size(); i++) {
```

### SizeReplaceableByIsEmpty
`rows.size() > 0` can be replaced with '!rows.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
        }
        writer.println("</thead>");
        if (rows != null && rows.size() > 0) {
            writer.println("<tbody>");
            int i = 0;
```

### SizeReplaceableByIsEmpty
`config.length() > 0` can be replaced with '!config.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
        String config = getServletConfig().getInitParameter("pages");
        Collection<String> names;
        if (config != null && config.length() > 0) {
            names = Arrays.asList(Constants.COMMA_SPLIT_PATTERN.split(config));
        } else {
```

### SizeReplaceableByIsEmpty
`uri.length() == 0` can be replaced with 'uri.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
            String uri = request.getRequestURI();
            boolean isHtml = false;
            if (uri == null || uri.length() == 0 || "/".equals(uri)) {
                uri = "index";
                isHtml = true;
```

### SizeReplaceableByIsEmpty
`query.length() == 0` can be replaced with 'query.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                    String query = request.getQueryString();
                    page = pageHandler.handle(URL.valueOf(request.getRequestURL().toString()
                            + (query == null || query.length() == 0 ? "" : "?" + query)));
                } catch (Throwable t) {
                    logger.warn(t.getMessage(), t);
```

### SizeReplaceableByIsEmpty
`nav.length() == 0` can be replaced with 'nav.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                    if (isHtml) {
                        String nav = page.getNavigation();
                        if (nav == null || nav.length() == 0) {
                            nav = ExtensionLoader.getExtensionLoader(PageHandler.class).getExtensionName(pageHandler);
                            nav = nav.substring(0, 1).toUpperCase() + nav.substring(1);
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
        }
        String value = (String) attachments.get(key);
        if (value == null || value.length() == 0) {
            return defaultValue;
        }
```

### SizeReplaceableByIsEmpty
`providerModels.size() > 0` can be replaced with '!providerModels.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkServiceRepository.java`
#### Snippet
```java
        if (providersWithoutGroup.containsKey(key)) {
            List<ProviderModel> providerModels = providersWithoutGroup.get(key);
            return providerModels.size() > 0 ? providerModels.get(0) : null;
        } else {
            return null;
```

### SizeReplaceableByIsEmpty
`pubApplicationModels.size() == 0` can be replaced with 'pubApplicationModels.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
    void tryDestroy() {
        synchronized (instLock) {
            if (pubApplicationModels.size() == 0) {
                destroy();
            }
```

### SizeReplaceableByIsEmpty
`pubApplicationModels.size() == 0` can be replaced with 'pubApplicationModels.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
    void tryDestroyProtocols() {
        synchronized (instLock) {
            if (pubApplicationModels.size() == 0) {
                notifyProtocolDestroy();
            }
```

### SizeReplaceableByIsEmpty
`pubApplicationModels.size() > 0` can be replaced with '!pubApplicationModels.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
            }
            ApplicationModel oldDefaultAppModel = this.defaultAppModel;
            if (pubApplicationModels.size() > 0) {
                this.defaultAppModel = pubApplicationModels.get(0);
            } else {
```

### SizeReplaceableByIsEmpty
`allInstances.size() > 0` can be replaced with '!allInstances.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
            }
            FrameworkModel oldDefaultFrameworkModel = defaultInstance;
            if (allInstances.size() > 0) {
                defaultInstance = allInstances.get(0);
            } else {
```

### SizeReplaceableByIsEmpty
`applicationModels.size() > 0` can be replaced with '!applicationModels.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
    private void checkApplicationDestroy() {
        synchronized (instLock) {
            if (applicationModels.size() > 0) {
                List<String> remainApplications = applicationModels.stream()
                    .map(ScopeModel::getDesc)
```

### SizeReplaceableByIsEmpty
`serviceDescriptors.size() > 0` can be replaced with '!serviceDescriptors.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        if (interfaceName != null && services.containsKey(interfaceName)) {
            List<ServiceDescriptor> serviceDescriptors = services.get(interfaceName);
            return serviceDescriptors.size() > 0 ? serviceDescriptors.get(0) : null;
        } else {
            return null;
```

### SizeReplaceableByIsEmpty
`consumerModels.size() > 0` can be replaced with '!consumerModels.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        if (consumers.containsKey(serviceKey)) {
            List<ConsumerModel> consumerModels = consumers.get(serviceKey);
            return consumerModels.size() > 0 ? consumerModels.get(0) : null;
        } else {
            return null;
```

### SizeReplaceableByIsEmpty
`snapshot.size() == 0` can be replaced with 'snapshot.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/BatchExecutorQueue.java`
#### Snippet
```java
            boolean flushedOnce = false;
            while ((item = snapshot.poll()) != null) {
                if (snapshot.size() == 0) {
                    i = 0;
                    break;
```

### SizeReplaceableByIsEmpty
`file.length() > 0` can be replaced with '!file.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/Version.java`
#### Snippet
```java

        // remove module: "x.y.z"
        while (file.length() > 0 && !Character.isDigit(file.charAt(0))) {
            i = file.indexOf("-");
            if (i >= 0) {
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/Parameters.java`
#### Snippet
```java
    public boolean hasMethodParameter(String method, String key) {
        String value = getMethodParameter(method, key);
        return value != null && value.length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/Parameters.java`
#### Snippet
```java
    public boolean hasParameter(String key) {
        String value = getParameter(key);
        return value != null && value.length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/Parameters.java`
#### Snippet
```java
    public String getDecodedParameter(String key, String defaultValue) {
        String value = getParameter(key, defaultValue);
        if (value != null && value.length() > 0) {
            try {
                value = URLDecoder.decode(value, "UTF-8");
```

### SizeReplaceableByIsEmpty
`remoteValue.length() > 0` can be replaced with '!remoteValue.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
        String remoteValue = super.getParameter(key);
        String localValue = consumerURL.getParameter(key);
        if (remoteValue != null && remoteValue.length() > 0
                && localValue != null && localValue.length() > 0) {
            value = remoteValue + "," + localValue;
```

### SizeReplaceableByIsEmpty
`localValue.length() > 0` can be replaced with '!localValue.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
        String localValue = consumerURL.getParameter(key);
        if (remoteValue != null && remoteValue.length() > 0
                && localValue != null && localValue.length() > 0) {
            value = remoteValue + "," + localValue;
            concatenatedPrams.put(key, value);
```

### SizeReplaceableByIsEmpty
`localValue.length() > 0` can be replaced with '!localValue.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
            return value;
        }
        if (localValue != null && localValue.length() > 0) {
            value = localValue;
        } else if (remoteValue != null && remoteValue.length() > 0) {
```

### SizeReplaceableByIsEmpty
`remoteValue.length() > 0` can be replaced with '!remoteValue.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
        if (localValue != null && localValue.length() > 0) {
            value = localValue;
        } else if (remoteValue != null && remoteValue.length() > 0) {
            value = remoteValue;
        }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLBuilder.java`
#### Snippet
```java
        }
        String value = getMethodParameter(method, key);
        return value != null && value.length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`list.size() > 0` can be replaced with '!list.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java

    public static List<String> sortSimpleName(List<String> list) {
        if (list != null && list.size() > 0) {
            Collections.sort(list, SIMPLE_NAME_COMPARATOR);
        }
```

### SizeReplaceableByIsEmpty
`map.size() == 0` can be replaced with 'map.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
     */
    public static boolean isEmptyMap(Map map) {
        return map == null || map.size() == 0;
    }

```

### SizeReplaceableByIsEmpty
`map.size() == 0` can be replaced with 'map.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
        }
        List<String> list = new ArrayList<>();
        if (map.size() == 0) {
            return list;
        }
```

### SizeReplaceableByIsEmpty
`categories.length() == 0` can be replaced with 'categories.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java

    public static boolean isMatchCategory(String category, String categories) {
        if (categories == null || categories.length() == 0) {
            return DEFAULT_CATEGORY.equals(category);
        } else if (categories.contains(ANY_VALUE)) {
```

### SizeReplaceableByIsEmpty
`notify.size() > 0` can be replaced with '!notify.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java

    public static Map<String, Map<String, String>> revertNotify(Map<String, Map<String, String>> notify) {
        if (notify != null && notify.size() > 0) {
            Map<String, Map<String, String>> newNotify = new HashMap<>();
            for (Map.Entry<String, Map<String, String>> entry : notify.entrySet()) {
```

### SizeReplaceableByIsEmpty
`defaultParameters.size() > 0` can be replaced with '!defaultParameters.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
            }
        }
        if (defaultParameters != null && defaultParameters.size() > 0) {
            for (Map.Entry<String, String> entry : defaultParameters.entrySet()) {
                String key = entry.getKey();
```

### SizeReplaceableByIsEmpty
`(url = url.trim()).length() == 0` can be replaced with '(url = url.trim()).isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
     */
    public static URL valueOf(String url) {
        if (url == null || (url = url.trim()).length() == 0) {
            throw new IllegalArgumentException("url == null");
        }
```

### SizeReplaceableByIsEmpty
`part.length() > 0` can be replaced with '!part.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
            for (String part : parts) {
                part = part.trim();
                if (part.length() > 0) {
                    int j = part.indexOf('=');
                    if (j >= 0) {
```

### SizeReplaceableByIsEmpty
`url.length() > 0` can be replaced with '!url.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
            }
        }
        if (url.length() > 0) {
            host = url;
        }
```

### SizeReplaceableByIsEmpty
`host.length() == 0` can be replaced with 'host.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

    public static String filterLocalHost(String host) {
        if (host == null || host.length() == 0) {
            return host;
        }
```

### SizeReplaceableByIsEmpty
`host.length() == 0` can be replaced with 'host.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
    public static boolean isInvalidLocalHost(String host) {
        return host == null
            || host.length() == 0
            || host.equalsIgnoreCase(LOCALHOST_KEY)
            || host.equals(ANYHOST_VALUE)
```

### SizeReplaceableByIsEmpty
`hostname.length() > 0` can be replaced with '!hostname.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
            }
            String hostname = HOST_NAME_CACHE.get(address);
            if (hostname != null && hostname.length() > 0) {
                return hostname;
            }
```

### SizeReplaceableByIsEmpty
`part.length() > 0` can be replaced with '!part.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        for (String part : parts) {
            part = part.trim();
            if (part.length() > 0) {
                int j = part.indexOf('=');
                if (j >= 0) {
```

### SizeReplaceableByIsEmpty
`cfg.trim().length() == 0` can be replaced with 'cfg.trim().isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java

        // add initial values
        String[] configs = (cfg == null || cfg.trim().length() == 0) ? new String[0] : COMMA_SPLIT_PATTERN.split(cfg);
        for (String config : configs) {
            if (config != null && config.trim().length() > 0) {
```

### SizeReplaceableByIsEmpty
`config.trim().length() > 0` can be replaced with '!config.trim().isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
        String[] configs = (cfg == null || cfg.trim().length() == 0) ? new String[0] : COMMA_SPLIT_PATTERN.split(cfg);
        for (String config : configs) {
            if (config != null && config.trim().length() > 0) {
                names.add(config);
            }
```

### SizeReplaceableByIsEmpty
`str.length() > 0` can be replaced with '!str.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
                }

                if (str.length() > 0 && pos <= str.length()) {
                    result.add(deleteAny(str.substring(pos), charsToDelete));
                }
```

### SizeReplaceableByIsEmpty
`ps.size() > 0` can be replaced with '!ps.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    public static String toQueryString(Map<String, String> ps) {
        StringBuilder buf = new StringBuilder();
        if (ps != null && ps.size() > 0) {
            for (Map.Entry<String, String> entry : new TreeMap<String, String>(ps).entrySet()) {
                String key = entry.getKey();
```

### SizeReplaceableByIsEmpty
`loggerAdapter.length() > 0` can be replaced with '!loggerAdapter.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/LoggerFactory.java`
#### Snippet
```java

    public static void setLoggerAdapter(FrameworkModel frameworkModel, String loggerAdapter) {
        if (loggerAdapter != null && loggerAdapter.length() > 0) {
            setLoggerAdapter(frameworkModel.getExtensionLoader(LoggerAdapter.class).getExtension(loggerAdapter));
        }
```

### SizeReplaceableByIsEmpty
`desc.length() == 0` can be replaced with 'desc.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     */
    private static Class<?>[] desc2classArray(ClassLoader cl, String desc) throws ClassNotFoundException {
        if (desc.length() == 0) {
            return EMPTY_CLASS_ARRAY;
        }
```

### SizeReplaceableByIsEmpty
`converters.size() > 0` can be replaced with '!converters.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/ConverterUtil.java`
#### Snippet
```java
            .collect(Collectors.toList()));

        return converters.size() > 0 ? converters.get(0) : null;
    }

```

### SizeReplaceableByIsEmpty
`reporters.size() > 0` can be replaced with '!reporters.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/status/reporter/FrameworkStatusReportService.java`
#### Snippet
```java

    public boolean hasReporter() {
        return reporters.size() > 0;
    }

```

### SizeReplaceableByIsEmpty
`getListeners(configFile).size() > 0` can be replaced with '!getListeners(configFile).isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    private boolean hasListeners(File configFile) {
        return getListeners(configFile).size() > 0;
    }

```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/AdaptiveCompiler.java`
#### Snippet
```java
        ExtensionLoader<Compiler> loader = frameworkModel.getExtensionLoader(Compiler.class);
        String name = DEFAULT_COMPILER; // copy reference
        if (name != null && name.length() > 0) {
            compiler = loader.getExtension(name);
        } else {
```

### SizeReplaceableByIsEmpty
`pkg.length() > 0` can be replaced with '!pkg.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/AbstractCompiler.java`
#### Snippet
```java
            throw new IllegalArgumentException("No such class name in " + code);
        }
        String className = pkg != null && pkg.length() > 0 ? pkg + "." + cls : cls;
        Lock lock = CLASS_IN_CREATION_MAP.get(className);
        if (lock == null) {
```

### SizeReplaceableByIsEmpty
`address.length() > 0` can be replaced with '!address.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    static String appendDefaultPort(String address, int defaultPort) {
        if (address != null && address.length() > 0 && defaultPort > 0) {
            int i = address.indexOf(':');
            if (i < 0) {
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
    public boolean hasParameter(String key) {
        String value = getParameter(key);
        return value != null && value.length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
    public boolean hasServiceParameter(String service, String key) {
        String value = getServiceParameter(service, key);
        return value != null && value.length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`ondisconnect.length() > 0` can be replaced with '!ondisconnect.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
    @Override
    public void setOndisconnect(String ondisconnect) {
        if (ondisconnect != null && ondisconnect.length() > 0) {
            this.stubevent = true;
        }
```

### SizeReplaceableByIsEmpty
`onconnect.length() > 0` can be replaced with '!onconnect.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
    @Override
    public void setOnconnect(String onconnect) {
        if (onconnect != null && onconnect.length() > 0) {
            this.stubevent = true;
        }
```

### SizeReplaceableByIsEmpty
`this.getArguments().size() > 0` can be replaced with '!this.getArguments().isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    protected void processExtraRefresh(String preferredPrefix, InmemoryConfiguration subPropsConfiguration) {
        // refresh ArgumentConfigs
        if (this.getArguments() != null && this.getArguments().size() > 0) {
            for (ArgumentConfig argument : this.getArguments()) {
                refreshArgument(argument, subPropsConfiguration);
```

### SizeReplaceableByIsEmpty
`resolveFile.length() > 0` can be replaced with '!resolveFile.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
                }
            }
            if (resolveFile != null && resolveFile.length() > 0) {
                Properties properties = new RegexProperties();
                try (FileInputStream fis = new FileInputStream(resolveFile)) {
```

### SizeReplaceableByIsEmpty
`cachedActivateGroups.size() == 0` can be replaced with 'cachedActivateGroups.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        Set<String> namesSet = new HashSet<>(names);
        if (!namesSet.contains(REMOVE_VALUE_PREFIX + DEFAULT_KEY)) {
            if (cachedActivateGroups.size() == 0) {
                synchronized (cachedActivateGroups) {
                    // cache all extensions
```

### SizeReplaceableByIsEmpty
`cachedActivateGroups.size() == 0` can be replaced with 'cachedActivateGroups.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                synchronized (cachedActivateGroups) {
                    // cache all extensions
                    if (cachedActivateGroups.size() == 0) {
                        getExtensionClasses();
                        for (Map.Entry<String, Object> entry : cachedActivates.entrySet()) {
```

### SizeReplaceableByIsEmpty
`(value = value.trim()).length() > 0` can be replaced with '!(value = value.trim()).isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

        String value = defaultAnnotation.value();
        if ((value = value.trim()).length() > 0) {
            String[] names = NAME_SEPARATOR.split(value);
            if (names.length > 1) {
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
            if (StringUtils.isEmpty(name)) {
                name = findAnnotationName(clazz);
                if (name.length() == 0) {
                    throw new IllegalStateException(
                        "No such extension name for the class " + clazz.getName() + " in the config " + resourceURL);
```

### SizeReplaceableByIsEmpty
`line.length() > 0` can be replaced with '!line.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                    }
                    line = line.trim();
                    if (line.length() > 0) {
                        newContentList.add(line);
                    }
```

### SizeReplaceableByIsEmpty
`methodConfigs.size() > 0` can be replaced with '!methodConfigs.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
            // refresh MethodConfigs
            List<MethodConfig> methodConfigs = this.getMethods();
            if (methodConfigs != null && methodConfigs.size() > 0) {
                // whether ignore invalid method config
                Object ignoreInvalidMethodConfigVal = getEnvironment().getConfiguration()
```

### SizeReplaceableByIsEmpty
`methods.size() > 0` can be replaced with '!methods.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java

    protected MethodConfig getMethodByName(String name) {
        if (methods != null && methods.size() > 0) {
            for (MethodConfig methodConfig : methods) {
                if (StringUtils.isEquals(methodConfig.getName(), name)) {
```

### SizeReplaceableByIsEmpty
`classesCache.size() == 0` can be replaced with 'classesCache.isEmpty()'
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/JarScanner.java`
#### Snippet
```java

    protected Map<String, Class<?>> getClasses() {
        if (classesCache == null || classesCache.size() == 0) {
            this.classesCache = forNames(classNameCache.values());
        }
```

### SizeReplaceableByIsEmpty
`line.length() > 0` can be replaced with '!line.isEmpty()'
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ClassSourceScanner.java`
#### Snippet
```java
                }
                line = line.trim();
                if (line.length() > 0) {
                    newContentList.add(line);
                }
```

### SizeReplaceableByIsEmpty
`list.size() > 0` can be replaced with '!list.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
            .filter(c -> TRUE.equals(AbstractConfigManager.isDefaultConfig(c)))
            .collect(Collectors.toList());
        if (list.size() > 0) {
            return list;
        }
```

### SizeReplaceableByIsEmpty
`configsMap.size() > 0` can be replaced with '!configsMap.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

    protected <C extends AbstractConfig> Optional<C> checkUniqueConfig(Map<String, C> configsMap, C config) {
        if (configsMap.size() > 0 && isUniqueConfig(config)) {
            C oldOne = configsMap.values().iterator().next();
            String configName = oldOne.getClass().getSimpleName();
```

### SizeReplaceableByIsEmpty
`((Collection``) value).size() != 0` can be replaced with '!((Collection) value).isEmpty()'
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ReflectionConfigWriter.java`
#### Snippet
```java

    private void addIfNotEmpty(Map<String, Object> attributes, String name, Object value) {
        if ((value instanceof Collection<?> && ((Collection<?>) value).size() != 0)) {
            attributes.put(name, value);
        }
```

### SizeReplaceableByIsEmpty
`str.length() > 0` can be replaced with '!str.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
                    Object value = method.invoke(config);
                    String str = String.valueOf(value).trim();
                    if (value != null && str.length() > 0) {
                        if (asParameters && parameter != null && parameter.escaped()) {
                            str = URL.encode(str);
```

### SizeReplaceableByIsEmpty
`pre.length() > 0` can be replaced with '!pre.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
                        if (parameter != null && parameter.append()) {
                            String pre = parameters.get(key);
                            if (pre != null && pre.length() > 0) {
                                str = pre + "," + str;
                                //Remove duplicate values
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
                            }
                        }
                        if (prefix != null && prefix.length() > 0) {
                            key = prefix + "." + key;
                        }
```

### SizeReplaceableByIsEmpty
`userInformation.length() > 0` can be replaced with '!userInformation.isEmpty()'
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
                .sessionTimeoutMs(sessionExpireMs);
            String userInformation = url.getUserInformation();
            if (userInformation != null && userInformation.length() > 0) {
                builder = builder.authorization("digest", userInformation.getBytes());
                builder.aclProvider(new ACLProvider() {
```

### SizeReplaceableByIsEmpty
`enabledService.size() > 0` can be replaced with '!enabledService.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/RouterSnapshotSwitcher.java`
#### Snippet
```java
    public synchronized void removeEnabledService(String service) {
        enabledService.remove(service);
        enable = enabledService.size() > 0;
        recentSnapshot = new String[MAX_LENGTH];
    }
```

### SizeReplaceableByIsEmpty
`virtualServiceRuleList.size() > 0` can be replaced with '!virtualServiceRuleList.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/rule/VsDestinationGroup.java`
#### Snippet
```java

    public boolean isValid() {
        return virtualServiceRuleList.size() > 0 && destinationRuleList.size() > 0;
    }
}
```

### SizeReplaceableByIsEmpty
`destinationRuleList.size() > 0` can be replaced with '!destinationRuleList.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/rule/VsDestinationGroup.java`
#### Snippet
```java

    public boolean isValid() {
        return virtualServiceRuleList.size() > 0 && destinationRuleList.size() > 0;
    }
}
```

### SizeReplaceableByIsEmpty
`input.length() > 0` can be replaced with '!input.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/rule/virtualservice/match/StringMatch.java`
#### Snippet
```java
            return input == null || "".equals(input);
        } else if (getNoempty() != null) {
            return input != null && input.length() > 0;
        } else {
            return false;
```

### SizeReplaceableByIsEmpty
`argp.size() > 0` can be replaced with '!argp.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/rule/virtualservice/match/DubboMethodMatch.java`
#### Snippet
```java
        List<StringMatch> argp = getArgp();
        Class<?>[] parameterTypes = invocation.getParameterTypes();
        if (argp != null && argp.size() > 0) {
            if (parameterTypes == null || parameterTypes.length == 0) {
                return false;
```

### SizeReplaceableByIsEmpty
`args.size() > 0` can be replaced with '!args.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/rule/virtualservice/match/DubboMethodMatch.java`
#### Snippet
```java

        List<DubboMethodArg> args = getArgs();
        if (args != null && args.size() > 0) {
            if (arguments == null || arguments.length == 0) {
                return false;
```

### SizeReplaceableByIsEmpty
`inputMap.size() == 0` can be replaced with 'inputMap.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleCache.java`
#### Snippet
```java

    protected static boolean isLabelMatch(URL url, String protocolServiceKey, Map<String, String> inputMap) {
        if (inputMap == null || inputMap.size() == 0) {
            return true;
        }
```

### SizeReplaceableByIsEmpty
`conditionRouters.size() == 0` can be replaced with 'conditionRouters.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/ListenableStateRouter.java`
#### Snippet
```java
    public BitList<Invoker<T>> doRoute(BitList<Invoker<T>> invokers, URL url, Invocation invocation,
                                       boolean needToPrintMessage, Holder<RouterSnapshotNode<T>> nodeHolder, Holder<String> messageHolder) throws RpcException {
        if (CollectionUtils.isEmpty(invokers) || conditionRouters.size() == 0) {
            if (needToPrintMessage) {
                messageHolder.set("Directly return. Reason: Invokers from previous router is empty or conditionRouters is empty.");
```

### SizeReplaceableByIsEmpty
`rule.trim().length() == 0` can be replaced with 'rule.trim().isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/ConditionStateRouter.java`
#### Snippet
```java
    public void init(String rule) {
        try {
            if (rule == null || rule.trim().length() == 0) {
                throw new IllegalArgumentException("Illegal route rule!");
            }
```

### SizeReplaceableByIsEmpty
`ref.size() > 0` can be replaced with '!ref.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/ForkingClusterInvoker.java`
#### Snippet
```java
                URL consumerUrl = RpcContext.getServiceContext().getConsumerUrl();
                CompletableFuture.<Object>supplyAsync(() -> {
                    if (ref.size() > 0) {
                        return null;
                    }
```

### SizeReplaceableByIsEmpty
`remoteMap.size() > 0` can be replaced with '!remoteMap.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/merger/DefaultProviderURLMergeProcessor.java`
#### Snippet
```java
        Map<String, String> remoteMap = remoteUrl.getParameters();

        if (remoteMap != null && remoteMap.size() > 0) {
            map.putAll(remoteMap);

```

### SizeReplaceableByIsEmpty
`localParametersMap.size() > 0` can be replaced with '!localParametersMap.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/merger/DefaultProviderURLMergeProcessor.java`
#### Snippet
```java
        }

        if (localParametersMap != null && localParametersMap.size() > 0) {
            Map<String, String> copyOfLocalMap = new HashMap<>(localParametersMap);

```

### SizeReplaceableByIsEmpty
`remoteFilter.length() > 0` can be replaced with '!remoteFilter.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/merger/DefaultProviderURLMergeProcessor.java`
#### Snippet
```java
                String remoteFilter = remoteMap.get(REFERENCE_FILTER_KEY);
                String localFilter = copyOfLocalMap.get(REFERENCE_FILTER_KEY);
                if (remoteFilter != null && remoteFilter.length() > 0
                        && localFilter != null && localFilter.length() > 0) {
                    map.put(REFERENCE_FILTER_KEY, remoteFilter + "," + localFilter);
```

### SizeReplaceableByIsEmpty
`localFilter.length() > 0` can be replaced with '!localFilter.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/merger/DefaultProviderURLMergeProcessor.java`
#### Snippet
```java
                String localFilter = copyOfLocalMap.get(REFERENCE_FILTER_KEY);
                if (remoteFilter != null && remoteFilter.length() > 0
                        && localFilter != null && localFilter.length() > 0) {
                    map.put(REFERENCE_FILTER_KEY, remoteFilter + "," + localFilter);
                }
```

### SizeReplaceableByIsEmpty
`remoteListener.length() > 0` can be replaced with '!remoteListener.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/merger/DefaultProviderURLMergeProcessor.java`
#### Snippet
```java
                String remoteListener = remoteMap.get(INVOKER_LISTENER_KEY);
                String localListener = copyOfLocalMap.get(INVOKER_LISTENER_KEY);
                if (remoteListener != null && remoteListener.length() > 0
                        && localListener != null && localListener.length() > 0) {
                    map.put(INVOKER_LISTENER_KEY, remoteListener + "," + localListener);
```

### SizeReplaceableByIsEmpty
`localListener.length() > 0` can be replaced with '!localListener.isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/merger/DefaultProviderURLMergeProcessor.java`
#### Snippet
```java
                String localListener = copyOfLocalMap.get(INVOKER_LISTENER_KEY);
                if (remoteListener != null && remoteListener.length() > 0
                        && localListener != null && localListener.length() > 0) {
                    map.put(INVOKER_LISTENER_KEY, remoteListener + "," + localListener);
                }
```

### SizeReplaceableByIsEmpty
`contextPath.length() == 0` can be replaced with 'contextPath.isEmpty()'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

    public String getContextPath() {
        if (contextPath == null || contextPath.length() == 0) {
            return "";
        }
```

### SizeReplaceableByIsEmpty
`desc.length() > 0` can be replaced with '!desc.isEmpty()'
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DecodeableRpcInvocation.java`
#### Snippet
```java
            Object[] args = DubboCodec.EMPTY_OBJECT_ARRAY;
            Class<?>[] pts = DubboCodec.EMPTY_CLASS_ARRAY;
            if (desc.length() > 0) {
//                if (RpcUtils.isGenericCall(path, getMethodName()) || RpcUtils.isEcho(path, getMethodName())) {
//                    pts = ReflectUtils.desc2classArray(desc);
```

### SizeReplaceableByIsEmpty
`enabledInstances.size() > 0` can be replaced with '!enabledInstances.isEmpty()'
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
    private void notifySubscriber(URL url, String serviceName, NacosAggregateListener listener, Collection<Instance> instances) {
        List<Instance> enabledInstances = new LinkedList<>(instances);
        if (enabledInstances.size() > 0) {
            //  Instances
            filterEnabledInstances(enabledInstances);
```

### SizeReplaceableByIsEmpty
`urls.size() == 0` can be replaced with 'urls.isEmpty()'
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
        List<URL> urls = buildURLs(consumerURL, instances);
        // Nacos does not support configurators and routers from registry, so all notifications are of providers type.
        if (urls.size() == 0 && !getUrl().getParameter(ENABLE_EMPTY_PROTECTION_KEY, DEFAULT_ENABLE_EMPTY_PROTECTION)) {
            logger.warn(REGISTRY_NACOS_EXCEPTION, "", "", "Received empty url address list and empty protection is disabled, will clear current available addresses");
            URL empty = URLBuilder.from(consumerURL)
```

### SizeReplaceableByIsEmpty
`TRACERS.size() > 0` can be replaced with '!TRACERS.isEmpty()'
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/filter/TraceFilter.java`
#### Snippet
```java
        Result result = invoker.invoke(invocation);
        long end = System.currentTimeMillis();
        if (TRACERS.size() > 0) {
            String key = invoker.getInterface().getName() + "." + invocation.getMethodName();
            Set<Channel> channels = TRACERS.get(key);
```

### SizeReplaceableByIsEmpty
`group.length() > 0` can be replaced with '!group.isEmpty()'
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
        params.put(IS_CALLBACK_SERVICE, Boolean.TRUE.toString());
        String group = (inv == null ? null : (String) inv.getObjectAttachmentWithoutConvert(GROUP_KEY));
        if (group != null && group.length() > 0) {
            params.put(GROUP_KEY, group);
        }
```

### SizeReplaceableByIsEmpty
`stubServiceMethods.length() == 0` can be replaced with 'stubServiceMethods.isEmpty()'
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
        if (isStubSupportEvent && !isCallbackService) {
            String stubServiceMethods = url.getParameter(STUB_EVENT_METHODS_KEY);
            if (stubServiceMethods == null || stubServiceMethods.length() == 0) {
                if (logger.isWarnEnabled()) {
                    logger.warn(PROTOCOL_UNSUPPORTED, "", "", "consumer [" + url.getParameter(INTERFACE_KEY) +
```

### SizeReplaceableByIsEmpty
`method.length() == 0` can be replaced with 'method.isEmpty()'
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
            private Invocation createInvocation(Channel channel, URL url, String methodKey) {
                String method = url.getParameter(methodKey);
                if (method == null || method.length() == 0) {
                    return null;
                }
```

### SizeReplaceableByIsEmpty
`resourceName.length() == 0` can be replaced with 'resourceName.isEmpty()'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java

    public T getCacheResource(String resourceName) {
        if (resourceName == null || resourceName.length() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`unclosedThreads.size() > 0` can be replaced with '!unclosedThreads.isEmpty()'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
            .collect(Collectors.toList());
        unclosedThreads.sort(Comparator.comparing(Thread::getName));
        if (unclosedThreads.size() > 0) {
            for (Thread thread : unclosedThreads) {
                unclosedThreadMap.put(thread, threadStacks.get(thread));
```

### SizeReplaceableByIsEmpty
`threads.size() > 0` can be replaced with '!threads.isEmpty()'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java

    private void printUnclosedThreads(List<Thread> threads, String testClassName) {
        if (threads.size() > 0) {
            log("Found " + threads.size() + " unclosed threads in test: " + testClassName);
            for (Thread thread : threads) {
```

### SizeReplaceableByIsEmpty
`message.trim().length() == 0` can be replaced with 'message.trim().isEmpty()'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/LogTelnetHandler.java`
#### Snippet
```java
        File file = LoggerFactory.getFile();
        StringBuilder buf = new StringBuilder();
        if (message == null || message.trim().length() == 0) {
            buf.append("EXAMPLE: log error / log 100");
        } else {
```

### SizeReplaceableByIsEmpty
`jvalidation.length() > 0` can be replaced with '!jvalidation.isEmpty()'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        String jvalidation = url.getParameter("jvalidation");
        ValidatorFactory factory;
        if (jvalidation != null && jvalidation.length() > 0) {
            factory = Validation.byProvider((Class) ReflectUtils.forName(jvalidation)).configure().buildValidatorFactory();
        } else {
```

### SizeReplaceableByIsEmpty
`jvalidation.length() > 0` can be replaced with '!jvalidation.isEmpty()'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        String jvalidation = url.getParameter("jvalidationNew");
        ValidatorFactory factory;
        if (jvalidation != null && jvalidation.length() > 0) {
            factory = Validation.byProvider((Class) ReflectUtils.forName(jvalidation)).configure().buildValidatorFactory();
        } else {
```

### SizeReplaceableByIsEmpty
`service.length() == 0` can be replaced with 'service.isEmpty()'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/CountTelnet.java`
#### Snippet
```java
        Channel channel = commandContext.getRemote();
        String service = channel.attr(ChangeTelnet.SERVICE_KEY).get();
        if ((service == null || service.length() == 0)
            && (args == null || args.length == 0)) {
            return "Please input service name, eg: \r\ncount XxxService\r\ncount XxxService xxxMethod\r\ncount XxxService xxxMethod 10\r\nor \"cd XxxService\" firstly.";
```

### SizeReplaceableByIsEmpty
`service.length() > 0` can be replaced with '!service.isEmpty()'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/CountTelnet.java`
#### Snippet
```java
        }
        StringBuilder buf = new StringBuilder();
        if (service != null && service.length() > 0) {
            buf.append("Use default service ").append(service).append(".\r\n");
        }
```

### SizeReplaceableByIsEmpty
`service.length() == 0` can be replaced with 'service.isEmpty()'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/CountTelnet.java`
#### Snippet
```java
        String method;
        String times;
        if (service == null || service.length() == 0) {
            service = args[0];
            method = args.length > 1 ? args[1] : null;
```

### SizeReplaceableByIsEmpty
`method.length() == 0` can be replaced with 'method.isEmpty()'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/CountTelnet.java`
#### Snippet
```java
        header.add("average");
        header.add("max");
        if (method == null || method.length() == 0) {
            for (Method m : invoker.getInterface().getMethods()) {
                RpcStatus count = RpcStatus.getStatus(url, m.getName());
```

### SizeReplaceableByIsEmpty
`string.length() == 0` can be replaced with 'string.isEmpty()'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
        for (String string : strings) {

            if (string.length() == 0) {
                continue;
            }
```

### SizeReplaceableByIsEmpty
`metadataReports.size() > 0` can be replaced with '!metadataReports.isEmpty()'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/MetadataReportInstance.java`
#### Snippet
```java
    public MetadataReport getMetadataReport(String registryKey) {
        MetadataReport metadataReport = metadataReports.get(registryKey);
        if (metadataReport == null && metadataReports.size() > 0) {
            metadataReport = metadataReports.values().iterator().next();
        }
```

### SizeReplaceableByIsEmpty
`newApps.size() > 0` can be replaced with '!newApps.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
            try {
                mappingLock.lock();
                if (CollectionUtils.isEmpty(tempOldApps) && newApps.size() > 0) {
                    serviceNameMapping.putCachedMapping(ServiceNameMapping.buildMappingKey(url), newApps);
                    subscribeURLs(url, listener, newApps);
```

### SizeReplaceableByIsEmpty
`protocols.size() > 0` can be replaced with '!protocols.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/ProtocolPortsMetadataCustomizer.java`
#### Snippet
```java
        });

        if (protocols.size() > 0) {// set endpoints only for multi-protocol scenario
            setEndpoints(serviceInstance, protocols);
        }
```

### SizeReplaceableByIsEmpty
`getMetadataReports(applicationModel).size() == 0` can be replaced with 'getMetadataReports(applicationModel).isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataUtils.java`
#### Snippet
```java

    public static void publishServiceDefinition(URL url, ServiceDescriptor serviceDescriptor, ApplicationModel applicationModel) {
        if (getMetadataReports(applicationModel).size() == 0) {
            String msg = "Remote Metadata Report Server is not provided or unavailable, will stop registering service definition to remote center!";
            logger.warn(REGISTRY_FAILED_LOAD_METADATA, "", "", msg);
```

### SizeReplaceableByIsEmpty
`listeners.size() == 0` can be replaced with 'listeners.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java

        void addNotifyListener(ServiceDiscoveryRegistryDirectory<?> listener) {
            if (listeners.size() == 0) {
                this.initWith(moduleModel.getApplicationModel().getApplicationName() + CONFIGURATORS_SUFFIX);
            }
```

### SizeReplaceableByIsEmpty
`listeners.size() == 0` can be replaced with 'listeners.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        void removeNotifyListener(ServiceDiscoveryRegistryDirectory<?> listener) {
            this.listeners.remove(listener);
            if (listeners.size() == 0) {
                this.stopListen(moduleModel.getApplicationModel().getApplicationName() + CONFIGURATORS_SUFFIX);
            }
```

### SizeReplaceableByIsEmpty
`newUrlInvokerMap.size() == 0` can be replaced with 'newUrlInvokerMap.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
     */
    private void destroyUnusedInvokers(Map<ProtocolServiceKeyWithAddress, Invoker<T>> oldUrlInvokerMap, Map<ProtocolServiceKeyWithAddress, Invoker<T>> newUrlInvokerMap) {
        if (newUrlInvokerMap == null || newUrlInvokerMap.size() == 0) {
            destroyAllInvokers();
            return;
```

### SizeReplaceableByIsEmpty
`oldUrlInvokerMap.size() == 0` can be replaced with 'oldUrlInvokerMap.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        }

        if (oldUrlInvokerMap == null || oldUrlInvokerMap.size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`notifyListeners.size() == 0` can be replaced with 'notifyListeners.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java

            // ServiceKey has no listener, remove set
            if (notifyListeners.size() == 0) {
                this.listeners.remove(serviceKey);
            }
```

### SizeReplaceableByIsEmpty
`queryProtocols.length() > 0` can be replaced with '!queryProtocols.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
    private boolean checkProtocolValid(String queryProtocols, URL providerUrl) {
        // If protocol is configured at the reference side, only the matching protocol is selected
        if (queryProtocols != null && queryProtocols.length() > 0) {
            boolean accept = false;

```

### SizeReplaceableByIsEmpty
`routerType.length() > 0` can be replaced with '!routerType.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            }
            String routerType = url.getParameter(ROUTER_KEY);
            if (routerType != null && routerType.length() > 0) {
                url = url.setProtocol(routerType);
            }
```

### SizeReplaceableByIsEmpty
`result.size() == 0` can be replaced with 'result.isEmpty()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
            }
        }
        if (result.size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`message.length() > 0` can be replaced with '!message.isEmpty()'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetHandlerAdapter.java`
#### Snippet
```java
        message = message.trim();
        String command;
        if (message.length() > 0) {
            int i = message.indexOf(' ');
            if (i > 0) {
```

### SizeReplaceableByIsEmpty
`command.length() > 0` can be replaced with '!command.isEmpty()'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetHandlerAdapter.java`
#### Snippet
```java
            command = "";
        }
        if (command.length() > 0) {
            if (extensionLoader.hasExtension(command)) {
                if (commandEnabled(channel.getUrl(), command)) {
```

### SizeReplaceableByIsEmpty
`message.length() > 0` can be replaced with '!message.isEmpty()'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/ClearTelnetHandler.java`
#### Snippet
```java
    public String telnet(Channel channel, String message) {
        int lines = 100;
        if (message.length() > 0) {
            if (!StringUtils.isNumber(message)) {
                return "Illegal lines " + message + ", must be integer.";
```

### SizeReplaceableByIsEmpty
`message.length() > 0` can be replaced with '!message.isEmpty()'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/HelpTelnetHandler.java`
#### Snippet
```java
    @Override
    public String telnet(Channel channel, String message) {
        if (message.length() > 0) {
            return processedTable.computeIfAbsent(message, commandName ->  generateForOneCommand(commandName));
        } else {
```

### SizeReplaceableByIsEmpty
`message.trim().length() == 0` can be replaced with 'message.trim().isEmpty()'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
        File file = LoggerFactory.getFile();
        StringBuilder buf = new StringBuilder();
        if (message == null || message.trim().length() == 0) {
            buf.append("EXAMPLE: log error / log 100");
        } else {
```

### SizeReplaceableByIsEmpty
`message.length() > 0` can be replaced with '!message.isEmpty()'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/StatusTelnetHandler.java`
#### Snippet
```java
            table.add(row);
            return TelnetUtils.toTable(header, table);
        } else if (message.length() > 0) {
            return "Unsupported parameter " + message + " for status.";
        }
```

### SizeReplaceableByIsEmpty
`result.trim().length() > 0` can be replaced with '!result.trim().isEmpty()'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
        }
        String result = toString(message, getCharset(channel));
        if (result.trim().length() > 0) {
            if (history == null) {
                history = new LinkedList<String>();
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
    }

    protected static final <V> V computeIfAbsent(String name, Function<? super String, ? extends V> mappingFunction) {
        return (V) getCache().computeIfAbsent(name, mappingFunction);
    }
```

### FinalStaticMethod
'static' method declared `final`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
                                            ExecutableElement method, Set<String> consumes);

    protected static final void put(String name, Object value) {
        Map<String, Object> cache = getCache();
        cache.put(name, value);
```

### FinalStaticMethod
'static' method declared `final`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
    }

    protected static final <T> T get(String name) throws ClassCastException {
        Map<String, Object> cache = getCache();
        return (T) cache.get(name);
```

### FinalStaticMethod
'static' method declared `final`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/TelnetCommandDecoder.java`
#### Snippet
```java

public class TelnetCommandDecoder {
    public static final CommandContext decode(String str) {
        CommandContext commandContext = null;
        if (!StringUtils.isBlank(str)) {
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/ThreadPoolMetric.java`
#### Snippet
```java
        Map<String, String> tags = new HashMap<>();
        tags.put(TAG_IP, getLocalHost());
        tags.put(TAG_PID, ConfigUtils.getPid()+"");
        tags.put(TAG_HOSTNAME, getLocalHostName());
        tags.put(TAG_APPLICATION_NAME, applicationName);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/DefaultDubboConfigBinder.java`
#### Snippet
```java
        BindingResult bindingResult = dataBinder.getBindingResult();
        if (bindingResult.hasGlobalErrors()) {
            throw new RuntimeException("Data bind global error, please check config. config: " + bindingResult.getGlobalError() + "");
        }
        if (bindingResult.hasFieldErrors()) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java
            return parentInternalId + "." + childIndex;
        } else {
            return "" + childIndex;
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
            if (logger.isWarnEnabled()) {
                if (resolveFile != null) {
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", "Using default dubbo resolve file " + resolveFile + " replace " + interfaceName + "" + resolve + " to p2p invoke remote service.");
                } else {
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", "Using -D" + interfaceName + "=" + resolve + " to p2p invoke remote service.");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/BaseOnline.java`
#### Snippet
```java
        String servicePattern = ".*";
        if (ArrayUtils.isNotEmpty(args)) {
            servicePattern = "" + args[0];
        }

```

## RuleId[id=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
No class found which is a subtype of both 'BeanPostProcessor' and 'ReferenceAnnotationBeanPostProcessor'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
            List<BeanPostProcessor> beanPostProcessors = ((AbstractBeanFactory) beanFactory).getBeanPostProcessors();
            for (BeanPostProcessor beanPostProcessor : beanPostProcessors) {
                if (beanPostProcessor == this) {
                    // This bean has been registered as BeanPostProcessor at org.apache.dubbo.config.spring.context.DubboInfraBeanRegisterPostProcessor.postProcessBeanFactory()
                    // so destroy this bean here, prevent register it as BeanPostProcessor again, avoid cause BeanPostProcessorChecker detection error
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
        beanDefinition.setAttribute(BEAN_NAME, beanName);

        if (ProtocolConfig.class.equals(beanClass)) {
//            for (String name : parserContext.getRegistry().getBeanDefinitionNames()) {
//                BeanDefinition definition = parserContext.getRegistry().getBeanDefinition(name);
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
//                }
//            }
        } else if (ServiceBean.class.equals(beanClass)) {
            String className = resolveAttribute(element, "class", parserContext);
            if (StringUtils.isNotEmpty(className)) {
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
            beanPropTypeMap = new HashMap<>();
            beanPropsCache.put(beanClass.getName(), beanPropTypeMap);
            if (ReferenceBean.class.equals(beanClass)) {
                //extract bean props from ReferenceConfig
                getPropertyMap(ReferenceConfig.class, beanPropTypeMap);
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java

        // post-process after parse attributes
        if (ProviderConfig.class.equals(beanClass)) {
            parseNested(element, parserContext, ServiceBean.class, true, "service", "provider", beanName, beanDefinition);
        } else if (ConsumerConfig.class.equals(beanClass)) {
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
        if (ProviderConfig.class.equals(beanClass)) {
            parseNested(element, parserContext, ServiceBean.class, true, "service", "provider", beanName, beanDefinition);
        } else if (ConsumerConfig.class.equals(beanClass)) {
            parseNested(element, parserContext, ReferenceBean.class, true, "reference", "consumer", beanName, beanDefinition);
        } else if (ReferenceBean.class.equals(beanClass)) {
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
        } else if (ConsumerConfig.class.equals(beanClass)) {
            parseNested(element, parserContext, ReferenceBean.class, true, "reference", "consumer", beanName, beanDefinition);
        } else if (ReferenceBean.class.equals(beanClass)) {
            configReferenceBean(element, parserContext, beanDefinition, null);
        } else if (MetricsConfig.class.equals(beanClass)) {
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
        } else if (ReferenceBean.class.equals(beanClass)) {
            configReferenceBean(element, parserContext, beanDefinition, null);
        } else if (MetricsConfig.class.equals(beanClass)) {
            parseMetrics(element, parserContext, beanDefinition);
        }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                        subDefinition.getPropertyValues().addPropertyValue(property, new RuntimeBeanReference(ref));
                    }
                    if (ReferenceBean.class.equals(beanClass)) {
                        configReferenceBean((Element) node, parserContext, subDefinition, beanDefinition);
                    }
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/support/InstantiationStrategy.java`
#### Snippet
```java
        // get scope mode value
        if (scopeModelAccessor != null) {
            if (parameterType == ScopeModel.class) {
                return scopeModelAccessor.getScopeModel();
            } else if (parameterType == FrameworkModel.class) {
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/support/InstantiationStrategy.java`
#### Snippet
```java
            if (parameterType == ScopeModel.class) {
                return scopeModelAccessor.getScopeModel();
            } else if (parameterType == FrameworkModel.class) {
                return scopeModelAccessor.getFrameworkModel();
            } else if (parameterType == ApplicationModel.class) {
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/support/InstantiationStrategy.java`
#### Snippet
```java
            } else if (parameterType == FrameworkModel.class) {
                return scopeModelAccessor.getFrameworkModel();
            } else if (parameterType == ApplicationModel.class) {
                return scopeModelAccessor.getApplicationModel();
            } else if (parameterType == ModuleModel.class) {
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/support/InstantiationStrategy.java`
#### Snippet
```java
            } else if (parameterType == ApplicationModel.class) {
                return scopeModelAccessor.getApplicationModel();
            } else if (parameterType == ModuleModel.class) {
                return scopeModelAccessor.getModuleModel();
            }
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
                        && !Modifier.isStatic(m.getModifiers())
                        && m.getParameterTypes().length == 0
                        && m.getReturnType() == URL.class) {
                    getterReturnUrl.put(name, i);
                }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
        Class<?>[] pts = method.getParameterTypes();
        for (int i = 0; i < pts.length; ++i) {
            if (pts[i].equals(URL.class)) {
                urlTypeIndex = i;
                break;
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    public Class<?> getServiceInterfaceClass() {
        Class<?> actualInterface = interfaceClass;
        if (interfaceClass == GenericService.class) {
            try {
                if (getInterfaceClassLoader() != null) {
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

            // compatible with old ExtensionFactory
            if (this.type == ExtensionInjector.class) {
                loadDirectory(extensionClasses, strategy, ExtensionFactory.class.getName());
            }
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                // When spiXXX implements ScopeModelAware, ExtensionAccessorAware,
                // the setXXX of ScopeModelAware and ExtensionAccessorAware does not need to be injected
                if (method.getDeclaringClass() == ScopeModelAware.class) {
                    continue;
                }
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        this.extensionPostProcessors = extensionDirector.getExtensionPostProcessors();
        initInstantiationStrategy();
        this.injector = (type == ExtensionInjector.class ?
            null :
            extensionDirector.getExtensionLoader(ExtensionInjector.class).getAdaptiveExtension());
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
        url = getRegistryUrl(url);
        Registry registry = getRegistry(url);
        if (RegistryService.class.equals(type)) {
            return proxyFactory.getInvoker((T) registry, type, url);
        }
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `mask[i].equals(ipAddress[i])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

        for (int i = 0; i < mask.length; i++) {
            if ("*".equals(mask[i]) || mask[i].equals(ipAddress[i])) {
                continue;
            } else if (mask[i].contains("-")) {
```

### DuplicateCondition
Duplicate condition `mask[i].equals(ipAddress[i])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
            } else if ("0".equals(ipAddress[i]) && ("0".equals(mask[i]) || "00".equals(mask[i]) || "000".equals(mask[i]) || "0000".equals(mask[i]))) {
                continue;
            } else if (!mask[i].equals(ipAddress[i])) {
                return false;
            }
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
        ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext("spring/dubbo-provider.xml");
        context.start();
        System.in.read();
    }
}
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AccessLogFilter.java`
#### Snippet
```java
        File dir = file.getParentFile();
        if (null != dir && !dir.exists()) {
            dir.mkdirs();
        }
    }
```

### IgnoreResultOfCall
Result of `File.renameTo()` is ignored
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AccessLogFilter.java`
#### Snippet
```java
            if (!now.equals(last)) {
                File archive = new File(file.getAbsolutePath() + "." + now);
                file.renameTo(archive);
            }
        }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext(ProviderConfiguration.class);
        context.start();
        System.in.read();
    }

```

### IgnoreResultOfCall
Result of `LineNumberReader.skip()` is ignored
in `dubbo-metrics/dubbo-metrics-prometheus/src/main/java/org/apache/dubbo/metrics/prometheus/PrometheusMetricsReporterCmd.java`
#### Snippet
```java
        LineNumberReader lnr = new LineNumberReader(new CharArrayReader(content.toCharArray()));
        try {
            lnr.skip(Long.MAX_VALUE);
            lnr.close();
        } catch (IOException ignore) {
```

### IgnoreResultOfCall
Result of `InetAddress.getByName()` is ignored
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
            // if the value start with "[" and end with "]", check whether it is IPV6
            try {
                InetAddress.getByName(value);
                return;
            } catch (UnknownHostException e) {
```

### IgnoreResultOfCall
Result of `InputStream.skip()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
    public static void skipUnusedStream(InputStream is) throws IOException {
        if (is.available() > 0) {
            is.skip(is.available());
        }
    }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
                }

                is.read(b, off, len);
                mPosition += len;
                return len;
```

### IgnoreResultOfCall
Result of `InputStream.skip()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
                }

                is.skip(len);
                mPosition += len;
                return len;
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
        FileLock dirLock;
        try {
            lockFile.createNewFile();
            if (!lockFile.exists()) {
                throw new AssertionError("Failed to create lock file " + lockFile);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/NativeConfigurationWriter.java`
#### Snippet
```java
            outputDirectory = outputDirectory.resolve(this.groupId).resolve(this.artifactId);
        }
        outputDirectory.toFile().mkdirs();
        File file = outputDirectory.resolve(filename).toFile();
        file.createNewFile();
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/NativeConfigurationWriter.java`
#### Snippet
```java
        outputDirectory.toFile().mkdirs();
        File file = outputDirectory.resolve(filename).toFile();
        file.createNewFile();
        return file;
    }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-provider/src/main/java/org/apache/dubbo/demo/graalvm/provider/Application.java`
#### Snippet
```java
            startWithBootstrap();
        }
        System.in.read();
    }

```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCodec.java`
#### Snippet
```java
        if (is.available() > 0) {
            byte[] result = new byte[is.available()];
            is.read(result);
            return result;
        }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiWrapperConsumer.java`
#### Snippet
```java
        // 4MB response
        System.out.println("Reply length:" + reply.length() + " cost:" + (System.currentTimeMillis() - st));
        System.in.read();
    }
}
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiConsumer.java`
#### Snippet
```java
            t.printStackTrace();
        }
        System.in.read();
    }
}
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
                File parent = curFile.getParentFile();
                if (!parent.exists()) {
                    parent.mkdirs();
                }
                try (FileOutputStream outputStream = new FileOutputStream(curFile)) {
```

### IgnoreResultOfCall
Result of `File.renameTo()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            File sourceFile = parentPath.toFile().listFiles()[0];
            File targetFile = Paths.get(parentPath.toString(), context.getUnpackedDirectory()).toFile();
            sourceFile.renameTo(targetFile);
            if (!Files.exists(targetFile.toPath()) || !targetFile.isDirectory()) {
                throw new IllegalStateException(String.format("Failed to rename the directory. source directory: %s, target directory: %s",
```

### IgnoreResultOfCall
Result of `File.setExecutable()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            // update file permission
            for (File file : zookeeperBin.toFile().listFiles()) {
                file.setExecutable(true, false);
                file.setReadable(true, false);
                file.setWritable(false, false);
```

### IgnoreResultOfCall
Result of `File.setReadable()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            for (File file : zookeeperBin.toFile().listFiles()) {
                file.setExecutable(true, false);
                file.setReadable(true, false);
                file.setWritable(false, false);
            }
```

### IgnoreResultOfCall
Result of `File.setWritable()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
                file.setExecutable(true, false);
                file.setReadable(true, false);
                file.setWritable(false, false);
            }
        }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
            this.reportFile = new File(reportFilePath);
            if (reportFile.isDirectory()) {
                reportFile.mkdirs();
                reportFile = new File(reportFile, defaultReportFileName);
            }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
            // if this file exists, firstly delete it.
            if (!initialized.getAndSet(true) && file.exists()) {
                file.delete();
            }
        }
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
            File lockfile = new File(file.getAbsolutePath() + ".lock");
            if (!lockfile.exists()) {
                lockfile.createNewFile();
            }
            try (RandomAccessFile raf = new RandomAccessFile(lockfile, "rw");
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
                try {
                    if (!file.exists()) {
                        file.createNewFile();
                    }

```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
            lockfile = new File(file.getAbsolutePath() + ".lock");
            if (!lockfile.exists()) {
                lockfile.createNewFile();
            }

```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
                try {
                    if (!file.exists()) {
                        file.createNewFile();
                    }

```

## RuleId[id=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'org.apache.dubbo.common.resource.GlobalResourcesRepository.globalReusedDisposables' accessed via instance reference
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
            synchronized (GlobalResourcesRepository.class) {
                if (globalReusedDisposables.contains(disposable)) {
                    this.globalReusedDisposables.remove(disposable);
                }
            }
```

## RuleId[id=UnnecessaryUnboxing]
### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
        Boolean configuredValue = usedRegistryAsCenter.get();
        if (configuredValue != null) { // If configured, take its value.
            supported = configuredValue.booleanValue();
        } else {                       // Or check the extension existence
            String protocol = registryConfig.getProtocol();
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static double unboxed(Double v) {
        return v == null ? 0 : v.doubleValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static float unboxed(Float v) {
        return v == null ? 0 : v.floatValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static long unboxed(Long v) {
        return v == null ? 0 : v.longValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static boolean unboxed(Boolean v) {
        return v == null ? false : v.booleanValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static short unboxed(Short v) {
        return v == null ? 0 : v.shortValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static byte unboxed(Byte v) {
        return v == null ? 0 : v.byteValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static int unboxed(Integer v) {
        return v == null ? 0 : v.intValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static char unboxed(Character v) {
        return v == null ? '\0' : v.charValue();
    }

```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `isDone()` only delegates to its super method
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java

    @Override
    public boolean isDone() {
        return super.isDone();
    }
```

## RuleId[id=OptionalAssignedToNull]
### OptionalAssignedToNull
Null is used for 'Optional' type in declaration
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    private static Optional<WatchService> newWatchService() {
        Optional<WatchService> watchService = null;
        FileSystem fileSystem = FileSystems.getDefault();
        try {
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
timeout \* 2: integer multiplication implicitly cast to long
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AdaptiveMetrics.java`
#### Snippet
```java

        //If the time more than 2 times, mandatory selected
        if (System.currentTimeMillis() - metrics.pickTime > timeout * 2) {
            return 0;
        }
```

## RuleId[id=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`filter = filter + ","` could be simplified to 'filter += ","'
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/DubboMonitorFactory.java`
#### Snippet
```java
            filter = "";
        } else {
            filter = filter + ",";
        }
        urlBuilder.addParameters(CHECK_KEY, String.valueOf(false),
```

### ReplaceAssignmentWithOperatorAssignment
`metrics.lastLatency = metrics.lastLatency >> multiple` could be simplified to 'metrics.lastLatency \>\>= multiple'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AdaptiveMetrics.java`
#### Snippet
```java
                    metrics.lastLatency = timeout * 2L;
                } else {
                    metrics.lastLatency = metrics.lastLatency >> multiple;
                }
                metrics.ewma = metrics.beta * metrics.ewma + (1 - metrics.beta) * metrics.lastLatency;
```

### ReplaceAssignmentWithOperatorAssignment
`v = v * 100` could be simplified to 'v \*= 100'
in `dubbo-common/src/main/java/org/apache/dubbo/common/Version.java`
#### Snippet
```java
                // e.g., version number 2.6.3 will convert to 2060300
                if (version.split("\\.").length == 3) {
                    v = v * 100;
                }
            } catch (Exception e) {
```

### ReplaceAssignmentWithOperatorAssignment
`cacheName = cacheName + SUFFIX` could be simplified to 'cacheName += SUFFIX'
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
        cacheName = safeName(cacheName);
        if (!cacheName.endsWith(SUFFIX)) {
            cacheName = cacheName + SUFFIX;
        }

```

### ReplaceAssignmentWithOperatorAssignment
`i = i + 2` could be simplified to 'i += 2'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
                throw new IllegalArgumentException("pairs must be even.");
            }
            for (int i = 0; i < pairs.length; i = i + 2) {
                parameters.put(pairs[i], pairs[i + 1]);
            }
```

### ReplaceAssignmentWithOperatorAssignment
`val = val >>> 7` could be simplified to 'val \>\>\>= 7'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleCustomerProtocolWapper.java`
#### Snippet
```java
        for (int i = 0; i < data.length - 1; i++) {
            data[i] = (byte) ((val & 0x7F) | 0x80);
            val = val >>> 7;
        }
        data[data.length - 1] = (byte) (val);
```

### ReplaceAssignmentWithOperatorAssignment
`val = val << 7` could be simplified to 'val \<\<= 7'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleCustomerProtocolWapper.java`
#### Snippet
```java

        for (int index = currentPosition + varIntLength - 1; index >= currentPosition; index--) {
            val = val << 7;
            val = val | (byteBuffer.get(index) & 0x7F);
        }
```

### ReplaceAssignmentWithOperatorAssignment
`val = val | (byteBuffer.get(index) & 0x7F)` could be simplified to 'val \|= (byteBuffer.get(index) \& 0x7F)'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleCustomerProtocolWapper.java`
#### Snippet
```java
        for (int index = currentPosition + varIntLength - 1; index >= currentPosition; index--) {
            val = val << 7;
            val = val | (byteBuffer.get(index) & 0x7F);
        }
        byteBuffer.position(currentPosition + varIntLength);
```

### ReplaceAssignmentWithOperatorAssignment
`val = val >>> 7` could be simplified to 'val \>\>\>= 7'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleCustomerProtocolWapper.java`
#### Snippet
```java
        int length = 0;
        while (val != 0) {
            val = val >>> 7;
            length++;
        }
```

### ReplaceAssignmentWithOperatorAssignment
`pos2 = pos2 + 1` could be simplified to 'pos2 += 1'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/AdaptiveLoadBalance.java`
#### Snippet
```java
        int pos2 = ThreadLocalRandom.current().nextInt(length - 1);
        if (pos2 >= pos1) {
            pos2 = pos2 + 1;
        }

```

### ReplaceAssignmentWithOperatorAssignment
`i = i + 4` could be simplified to 'i += 4'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            } else if (b == 27) { // escape
                if (i < message.length - 4 && message[i + 4] == 126) {
                    i = i + 4;
                } else if (i < message.length - 3 && message[i + 3] == 126) {
                    i = i + 3;
```

### ReplaceAssignmentWithOperatorAssignment
`i = i + 3` could be simplified to 'i += 3'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                    i = i + 4;
                } else if (i < message.length - 3 && message[i + 3] == 126) {
                    i = i + 3;
                } else if (i < message.length - 2) {
                    i = i + 2;
```

### ReplaceAssignmentWithOperatorAssignment
`i = i + 2` could be simplified to 'i += 2'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                    i = i + 3;
                } else if (i < message.length - 2) {
                    i = i + 2;
                }
            } else if (b == -1 && i < message.length - 2
```

### ReplaceAssignmentWithOperatorAssignment
`i = i + 2` could be simplified to 'i += 2'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            } else if (b == -1 && i < message.length - 2
                && (message[i + 1] == -3 || message[i + 1] == -5)) { // handshake
                i = i + 2;
            } else {
                copy[index++] = message[i];
```

### ReplaceAssignmentWithOperatorAssignment
`message = message + "\r\n"` could be simplified to 'message += "\\r\\n"'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
        if (message instanceof String) {
            if (isClientSide(channel)) {
                message = message + "\r\n";
            }
            byte[] msgData = ((String) message).getBytes(getCharset(channel).name());
```

### ReplaceAssignmentWithOperatorAssignment
`index = index - 1` could be simplified to 'index -= 1'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            } else {
                if (up) {
                    index = index - 1;
                    if (index < 0) {
                        index = history.size() - 1;
```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 1` could be simplified to 'index += 1'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                    }
                } else {
                    index = index + 1;
                    if (index > history.size() - 1) {
                        index = 0;
```

### ReplaceAssignmentWithOperatorAssignment
`countbefore = countbefore + widths[j]` could be simplified to 'countbefore += widths\[j\]'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
                totalWidth = (totalWidth + widths[j]) > maxwidth ? maxwidth : (totalWidth + widths[j]);
                if (j < widths.length - 1) {
                    countbefore = countbefore + widths[j];
                }
            }
```

## RuleId[id=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/StreamUtils.java`
#### Snippet
```java

            int bytesRead;
            while ((bytesRead = reader.read(buffer)) != -1) {
                out.append(buffer, 0, bytesRead);
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/ServiceRestMetadataStorage.java`
#### Snippet
```java
                char[] buf = new char[1024];
                int len;
                while ((len = reader.read(buf)) != -1) {
                    stringBuilder.append(buf, 0, len);
                }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
        //check bean definition
        boolean isContains;
        if ((isContains = beanDefinitionRegistry.containsBeanDefinition(referenceBeanName)) || beanDefinitionRegistry.isAlias(referenceBeanName)) {
            String preReferenceBeanName = referenceBeanName;
            if (!isContains){
```

### NestedAssignment
Result of assignment expression used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
            byte[] buffer = new byte[8192];
            int n = 0;
            while (-1 != (n = input.read(buffer))) {
                output.write(buffer, 0, n);
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
            checkDestroyed();
            resetDefaultAppModel();
            if ((appModel = this.defaultAppModel) == null) {
                synchronized (instLock) {
                    if (this.defaultAppModel == null) {
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/BatchExecutorQueue.java`
#### Snippet
```java
            Queue<T> snapshot = new LinkedList<>();
            T item;
            while ((item = queue.poll()) != null) {
                snapshot.add(item);
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/BatchExecutorQueue.java`
#### Snippet
```java
            int i = 0;
            boolean flushedOnce = false;
            while ((item = snapshot.poll()) != null) {
                if (snapshot.size() == 0) {
                    i = 0;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringWriter.java`
#### Snippet
```java

    public UnsafeStringWriter() {
        lock = mBuffer = new StringBuilder();
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringWriter.java`
#### Snippet
```java
        }

        lock = mBuffer = new StringBuilder(size);
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java
    public UnsafeByteArrayInputStream(byte[] buf, int offset, int length) {
        mData = buf;
        mPosition = mMark = offset;
        mLimit = Math.min(offset + length, buf.length);
    }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringReader.java`
#### Snippet
```java
        mString = str;
        mLimit = str.length();
        mPosition = mMark = 0;
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            int read, total = 0;
            do {
                if ((read = is.read(buf, total, bs - total)) <= 0) {
                    break;
                }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
            byte[] buffer = new byte[1024];
            int n;
            while (-1 != (n = inputStream.read(buffer))) {
                byteArrayOutputStream.write(buffer, 0, n);
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
        long total = 0;
        char[] buf = new char[bufferSize];
        while ((read = reader.read(buf)) != -1) {
            writer.write(buf, 0, read);
            total += read;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
        try (BufferedReader reader = new BufferedReader(new InputStreamReader(is))) {
            String line;
            while ((line = reader.readLine()) != null) {
                lines.add(line);
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
        char[] buf = new char[1024];
        int len;
        while ((len = inputStreamReader.read(buf)) != -1) {
            stringBuilder.append(buf, 0, len);
        }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
        long count = 0;
        int n;
        while (EOF != (n = input.read(buffer))) {
            output.write(buffer, 0, n);
            count += n;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java
            HashedWheelTimeout next = head.next;
            if (next == null) {
                tail = this.head = null;
            } else {
                this.head = next;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java
            timeout.bucket = this;
            if (head == null) {
                head = tail = timeout;
            } else {
                tail.next = timeout;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
            return full;
        }
        return full = super.toFullString();
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
            return string;
        }
        return string = super.toString();
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
            return parameter;
        }
        return parameter = super.toParameterString();
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
            return identity;
        }
        return identity = super.toIdentityString();
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
     */
    public static URL valueOf(String url) {
        if (url == null || (url = url.trim()).length() == 0) {
            throw new IllegalArgumentException("url == null");
        }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java
    public static Set<URL> loadResources(String fileName, ClassLoader currentClassLoader) {
        Map<ClassLoader, Map<String, Set<URL>>> classLoaderCache;
        if (classLoaderResourcesCache == null || (classLoaderCache = classLoaderResourcesCache.get()) == null) {
            synchronized (ClassLoaderResourceLoader.class) {
                if (classLoaderResourcesCache == null || (classLoaderCache = classLoaderResourcesCache.get()) == null) {
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java
        if (classLoaderResourcesCache == null || (classLoaderCache = classLoaderResourcesCache.get()) == null) {
            synchronized (ClassLoaderResourceLoader.class) {
                if (classLoaderResourcesCache == null || (classLoaderCache = classLoaderResourcesCache.get()) == null) {
                    classLoaderCache = new ConcurrentHashMap<>();
                    classLoaderResourcesCache = new SoftReference<>(classLoaderCache);
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        String networkInterfaceDisplayName;
        if (StringUtils.isNotEmpty(ignoredInterfaces)
            && StringUtils.isNotEmpty(networkInterfaceDisplayName = networkInterface.getDisplayName())) {
            for (String ignoredInterface : ignoredInterfaces.split(",")) {
                String trimIgnoredInterface = ignoredInterface.trim();
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        //ipv6 [::FFFF:129.144.52.38]:80
        int ind;
        if ((ip.charAt(0) == '[' && (ind = ip.indexOf(']')) > 2)) {
            String nhost = ip;
            ip = nhost.substring(0, ind + 1);
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
        try (BufferedReader reader = new BufferedReader(new InputStreamReader(is))) {
            int n;
            while ((n = reader.read(buffer)) != -1) {
                if (n < 10) {
                    buffer = Arrays.copyOf(buffer, n);
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
            } else {
                int delPos;
                for (pos = 0; (delPos = str.indexOf(delimiter, pos)) != -1; pos = delPos + delimiter.length()) {
                    result.add(deleteAny(str.substring(pos, delPos), charsToDelete));
                }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    public static boolean isBlank(CharSequence cs) {
        int strLen;
        if (cs == null || (strLen = cs.length()) == 0) {
            return true;
        }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    public static String stripEnd(final String str, final String stripChars) {
        int end;
        if (str == null || (end = str.length()) == 0) {
            return str;
        }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JdkCompiler.java`
#### Snippet
```java
        @Override
        public OutputStream openOutputStream() {
            return bytecode = new ByteArrayOutputStream();
        }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
            String md = entry.getKey();
            Method method = entry.getValue();
            if ((matcher = ReflectUtils.GETTER_METHOD_DESC_PATTERN.matcher(md)).matches()) {
                String pn = propertyName(matcher.group(1));
                c2.append(" if( $2.equals(\"").append(pn).append("\") ){ return ($w)w.").append(method.getName()).append("(); }");
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
                c2.append(" if( $2.equals(\"").append(pn).append("\") ){ return ($w)w.").append(method.getName()).append("(); }");
                pts.put(pn, method.getReturnType());
            } else if ((matcher = ReflectUtils.IS_HAS_CAN_METHOD_DESC_PATTERN.matcher(md)).matches()) {
                String pn = propertyName(matcher.group(1));
                c2.append(" if( $2.equals(\"").append(pn).append("\") ){ return ($w)w.").append(method.getName()).append("(); }");
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
                c2.append(" if( $2.equals(\"").append(pn).append("\") ){ return ($w)w.").append(method.getName()).append("(); }");
                pts.put(pn, method.getReturnType());
            } else if ((matcher = ReflectUtils.SETTER_METHOD_DESC_PATTERN.matcher(md)).matches()) {
                Class<?> pt = method.getParameterTypes()[0];
                String pn = propertyName(matcher.group(1));
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/concurrent/CallableSafeInitializer.java`
#### Snippet
```java
        T result;

        while ((result = reference.get()) == null) {
            if (factory.compareAndSet(null, this)) {
                reference.set(initialize());
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/serial/SerializingExecutor.java`
#### Snippet
```java
        Runnable r;
        try {
            while ((r = runQueue.poll()) != null) {
                InternalThreadLocalMap internalThreadLocalMap = InternalThreadLocalMap.getAndRemove();
                try {
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocalMap.java`
#### Snippet
```java
        InternalThreadLocalMap threadLocalMap = thread.threadLocalMap();
        if (threadLocalMap == null) {
            thread.setThreadLocalMap(threadLocalMap = new InternalThreadLocalMap());
        }
        return threadLocalMap;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

        String value = defaultAnnotation.value();
        if ((value = value.trim()).length() > 0) {
            String[] names = NAME_SEPARATOR.split(value);
            if (names.length > 1) {
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
            return cachedAdaptiveClass;
        }
        return cachedAdaptiveClass = createAdaptiveExtensionClass();
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        if (urlListMap == null) {
            synchronized (ExtensionLoader.class) {
                if ((urlListMap = urlListMapCache.get()) == null) {
                    urlListMap = new ConcurrentHashMap<>();
                    urlListMapCache = new SoftReference<>(urlListMap);
```

### NestedAssignment
Result of assignment expression used
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                new InputStreamReader(resourceURL.openStream(), StandardCharsets.UTF_8))) {
                String line;
                while ((line = reader.readLine()) != null) {
                    final int ci = line.indexOf('#');
                    if (ci >= 0) {
```

### NestedAssignment
Result of assignment expression used
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ClassSourceScanner.java`
#### Snippet
```java
            new InputStreamReader(resourceUrl.openStream(), StandardCharsets.UTF_8))) {
            String line;
            while ((line = reader.readLine()) != null) {
                final int ci = line.indexOf('#');
                if (ci >= 0) {
```

### NestedAssignment
Result of assignment expression used
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/Netty4BatchWriteQueue.java`
#### Snippet
```java
            public void operationComplete(ChannelFuture future) throws Exception {
                ChannelPromise cp;
                while ((cp = promises.poll()) != null) {
                    if (future.isSuccess()){
                        cp.setSuccess();
```

### NestedAssignment
Result of assignment expression used
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TriHttp2RemoteFlowController.java`
#### Snippet
```java
                // Write the remainder of frames that we are allowed to
                boolean writeOccurred = false;
                while (!cancelled && (frame = peek()) != null) {
                    int maxBytes = min(allocated, writableWindow());
                    if (maxBytes <= 0 && frame.size() > 0) {
```

### NestedAssignment
Result of assignment expression used
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TriHttp2RemoteFlowController.java`
#### Snippet
```java
                for (;;) {
                    if (!streamByteDistributor.distribute(bytesToWrite, this) ||
                        (bytesToWrite = writableBytes()) <= 0 ||
                        !isChannelWritable0()) {
                        break;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/transport/WriteQueue.java`
#### Snippet
```java
            int i = 0;
            boolean flushedOnce = false;
            while ((cmd = queue.poll()) != null) {
                ch = cmd.channel();
                cmd.run(ch);
```

### NestedAssignment
Result of assignment expression used
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Bzip2.java`
#### Snippet
```java
            byte[] buffer = new byte[2048];
            int n;
            while ((n = unZip.read(buffer)) >= 0) {
                out.write(buffer, 0, n);
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Gzip.java`
#### Snippet
```java
            int readByteNum;
            byte[] bufferArr = new byte[256];
            while ((readByteNum = gzipInputStream.read(bufferArr)) >= 0) {
                byteOutStream.write(bufferArr, 0, readByteNum);
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/MergerFactory.java`
#### Snippet
```java
            for (Type it : interfaceTypes) {
                if (it instanceof ParameterizedType
                    && (mergerType = ((ParameterizedType) it)).getRawType() == Merger.class) {
                    Type typeArg = mergerType.getActualTypeArguments()[0];
                    return TypeUtils.getRawClass(typeArg);
```

### NestedAssignment
Result of assignment expression used
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/PilotExchanger.java`
#### Snippet
```java
                return GLOBAL_PILOT_EXCHANGER;
            }
            return (GLOBAL_PILOT_EXCHANGER = new PilotExchanger(url));
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/ZookeeperUnixProcessor.java`
#### Snippet
```java
        try (final BufferedReader reader = new BufferedReader(new InputStreamReader(errorStream))) {
            String line;
            while ((line = reader.readLine()) != null) {
                logger.error(TESTING_REGISTRY_FAILED_TO_START_ZOOKEEPER,"","",line);
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/ZookeeperUnixProcessor.java`
#### Snippet
```java
        try (final BufferedReader reader = new BufferedReader(new InputStreamReader(inputStream))) {
            String line;
            while ((line = reader.readLine()) != null) {
                if (this.getPattern().matcher(line).matches()) {
                    return;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            File targetFile = targetPath.toFile();
            TarArchiveEntry entry;
            while ((entry = tarArchiveInputStream.getNextTarEntry()) != null) {
                if (entry.isDirectory()) {
                    continue;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
            logger.warn(PROTOCOL_UNSUPPORTED, "", "", "Received url with EMPTY protocol, will clear all available addresses.");
            refreshRouter(BitList.emptyList(), () ->
                this.forbidden = true // Forbid to access
            );
            destroyAllInvokers(); // Close all invokers
```

### NestedAssignment
Result of assignment expression used
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            && EMPTY_PROTOCOL.equals(invokerUrls.get(0).getProtocol())) {
            refreshRouter(BitList.emptyList(), () ->
                this.forbidden = true // Forbid to access
            );
            destroyAllInvokers(); // Close all invokers
```

### NestedAssignment
Result of assignment expression used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/UrlUtils.java`
#### Snippet
```java
        List<String> preferSerials = preferSerialization(url);
        for (String preferSerial : preferSerials) {
            if ((serializationId = CodecSupport.getIDByName(preferSerial)) != null) {
                return serializationId;
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/UrlUtils.java`
#### Snippet
```java

        // Secondly, obtain the value from serialization
        if ((serializationId = CodecSupport.getIDByName(url.getParameter(SERIALIZATION_KEY))) != null) {
            return serializationId;
        }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/AbstractChannelBuffer.java`
#### Snippet
```java
    @Override
    public void clear() {
        readerIndex = writerIndex = 0;
    }

```

### NestedAssignment
Result of assignment expression used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
        byte[] buffer = getBuffer(is.available());
        int len;
        while ((len = is.read(buffer)) > -1) {
            baos.write(buffer, 0, len);
        }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
        ZookeeperClient zookeeperClient = null;
        for (String address : addressList) {
            if ((zookeeperClient = zookeeperClientMap.get(address)) != null && zookeeperClient.isConnected()) {
                break;
            }
```

### NestedAssignment
Result of assignment expression used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
        List<String> addressList = getURLBackupAddress(url);
        // The field define the zookeeper server , including protocol, host, port, username, password
        if ((zookeeperClient = fetchAndUpdateZookeeperClientCache(addressList)) != null && zookeeperClient.isConnected()) {
            logger.info("find valid zookeeper client from the cache for address: " + url);
            return zookeeperClient;
```

### NestedAssignment
Result of assignment expression used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
        // avoid creating too many connections， so add lock
        synchronized (zookeeperClientMap) {
            if ((zookeeperClient = fetchAndUpdateZookeeperClientCache(addressList)) != null && zookeeperClient.isConnected()) {
                logger.info("find valid zookeeper client from the cache for address: " + url);
                return zookeeperClient;
```

## RuleId[id=ReturnFromFinallyBlock]
### ReturnFromFinallyBlock
'return' inside 'finally' block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
                } finally {
                    if (matched) {
                        return true;
                    }
                    if (networkInterfaceDisplayName.equals(trimIgnoredInterface)) {
```

### ReturnFromFinallyBlock
'return' inside 'finally' block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
                    }
                    if (networkInterfaceDisplayName.equals(trimIgnoredInterface)) {
                        return true;
                    }
                }
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `recoverRegistered` are queried, but never updated
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
    protected void recover() throws Exception {
        // register
        Set<URL> recoverRegistered = new HashSet<>(getRegistered());
        if (!recoverRegistered.isEmpty()) {
            if (logger.isInfoEnabled()) {
```

### MismatchedCollectionQueryUpdate
Contents of collection `recoverSubscribed` are queried, but never updated
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
        }
        // subscribe
        Map<URL, Set<NotifyListener>> recoverSubscribed = new HashMap<>(getSubscribed());
        if (!recoverSubscribed.isEmpty()) {
            if (logger.isInfoEnabled()) {
```

### MismatchedCollectionQueryUpdate
Contents of collection `recoverSubscribed` are queried, but never updated
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
    private void fetchLatestAddresses() {
        // subscribe
        Map<URL, Set<NotifyListener>> recoverSubscribed = new HashMap<>(getSubscribed());
        if (!recoverSubscribed.isEmpty()) {
            if (logger.isInfoEnabled()) {
```

### MismatchedCollectionQueryUpdate
Contents of collection `exporters` are queried, but never updated
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
        }

        List<Exporter<?>> exporters = new ArrayList<>(bounds.values());
        for (Exporter<?> exporter : exporters) {
            exporter.unexport();
```

### MismatchedCollectionQueryUpdate
Contents of collection `ID_SERIALIZATIONNAME_MAP` are updated, but never queried
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(CodecSupport.class);
    private static Map<Byte, Serialization> ID_SERIALIZATION_MAP = new HashMap<Byte, Serialization>();
    private static Map<Byte, String> ID_SERIALIZATIONNAME_MAP = new HashMap<Byte, String>();
    private static Map<String, Byte> SERIALIZATIONNAME_ID_MAP = new HashMap<String, Byte>();
    // Cache null object serialize results, for heartbeat request/response serialize use.
```

## RuleId[id=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `WHITELIST` is accessed in both synchronized and unsynchronized contexts
in `dubbo-serialization/dubbo-serialization-hessian2/src/main/java/org/apache/dubbo/common/serialize/hessian2/Hessian2FactoryManager.java`
#### Snippet
```java

public class Hessian2FactoryManager {
    String WHITELIST = "dubbo.application.hessian2.whitelist";
    String ALLOW = "dubbo.application.hessian2.allow";
    String DENY = "dubbo.application.hessian2.deny";
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cancellationCause` is accessed in both synchronized and unsynchronized contexts
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/CancellationContext.java`
#### Snippet
```java

    private volatile ArrayList<ExecutableListener> listeners;
    private Throwable cancellationCause;
    private boolean cancelled;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `client` is accessed in both synchronized and unsynchronized contexts
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
    private RestClientFactory clientFactory;

    private T client;

    public ReferenceCountedClient(T client, ConcurrentMap<String, ReferenceCountedClient<? extends RestClient>> clients, RestClientFactory clientFactory, URL url) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `elements` is accessed in both synchronized and unsynchronized contexts
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
public abstract class AbstractServiceAnnotationProcessor extends AbstractProcessor {

    protected Elements elements;

    private List<? extends Element> objectMembers;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `objectMembers` is accessed in both synchronized and unsynchronized contexts
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
    protected Elements elements;

    private List<? extends Element> objectMembers;

    public synchronized void init(ProcessingEnvironment processingEnv) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serviceRestMetadataWriter` is accessed in both synchronized and unsynchronized contexts
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/ServiceRestMetadataAnnotationProcessor.java`
#### Snippet
```java
    private Set<ServiceRestMetadataResolver> metadataProcessors;

    private ServiceRestMetadataStorage serviceRestMetadataWriter;

    private Set<ServiceRestMetadata> serviceRestMetadata = new LinkedHashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metadataProcessors` is accessed in both synchronized and unsynchronized contexts
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/ServiceRestMetadataAnnotationProcessor.java`
#### Snippet
```java
public class ServiceRestMetadataAnnotationProcessor extends AbstractServiceAnnotationProcessor {

    private Set<ServiceRestMetadataResolver> metadataProcessors;

    private ServiceRestMetadataStorage serviceRestMetadataWriter;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `multipleNotifyListenerWrapper` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java
    protected static class SingleNotifyListener implements NotifyListener {

        MultipleNotifyListenerWrapper multipleNotifyListenerWrapper;
        Registry registry;
        volatile List<URL> urlList;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `registryMap` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java
    protected static class MultipleNotifyListenerWrapper implements NotifyListener {

        Map<URL, SingleNotifyListener> registryMap = new ConcurrentHashMap<URL, SingleNotifyListener>(4);
        NotifyListener sourceNotifyListener;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metadataService` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ConfigurableMetadataServiceExporter.java`
#### Snippet
```java
    private final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(getClass());

    private MetadataServiceDelegation metadataService;

    private volatile ServiceConfig<MetadataService> serviceConfig;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `background` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
    private final ApplicationDeployer applicationDeployer;
    private CompletableFuture startFuture;
    private Boolean background;
    private Boolean exportAsync;
    private Boolean referAsync;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `startFuture` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java

    private final ApplicationDeployer applicationDeployer;
    private CompletableFuture startFuture;
    private Boolean background;
    private Boolean exportAsync;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `referFuture` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
    private Boolean referAsync;
    private CompletableFuture<?> exportFuture;
    private CompletableFuture<?> referFuture;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `referAsync` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
    private Boolean background;
    private Boolean exportAsync;
    private Boolean referAsync;
    private CompletableFuture<?> exportFuture;
    private CompletableFuture<?> referFuture;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `exportFuture` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
    private Boolean exportAsync;
    private Boolean referAsync;
    private CompletableFuture<?> exportFuture;
    private CompletableFuture<?> referFuture;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `protocolSPI` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
     * layers, and eventually will get a <b>ProtocolSerializationWrapper</b> or <b>ProtocolFilterWrapper</b> or <b>ProtocolListenerWrapper</b>
     */
    private Protocol protocolSPI;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `proxyFactory` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
     * its default implementation
     */
    private ProxyFactory proxyFactory;

    private ConsumerModel consumerModel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `providerModel` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
    private ProxyFactory proxyFactory;

    private ProviderModel providerModel;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `proxyFactory` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
     * default implementation
     */
    private ProxyFactory proxyFactory;

    private ProviderModel providerModel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `protocolSPI` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
    private static final Map<String, Integer> RANDOM_PORT_MAP = new HashMap<String, Integer>();

    private Protocol protocolSPI;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `asyncMetadataFuture` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
    private volatile boolean hasPreparedInternalModule = false;

    private ScheduledFuture<?> asyncMetadataFuture;
    private volatile CompletableFuture<Boolean> startFuture;
    private final DubboShutdownHook dubboShutdownHook;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `referenceConfig` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ReferenceBean.java`
#### Snippet
```java

    //actual reference config
    private ReferenceConfig referenceConfig;

    // Registration sources of this reference, may be xml file or annotation location
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `applicationContext` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java
    private ConcurrentMap<String, ReferenceConfig> referenceConfigMap = new ConcurrentHashMap<>();

    private ApplicationContext applicationContext;
    private volatile boolean initialized = false;
    private ModuleModel moduleModel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `moduleModel` is accessed in both synchronized and unsynchronized contexts
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java
    private ApplicationContext applicationContext;
    private volatile boolean initialized = false;
    private ModuleModel moduleModel;

    public void addReference(ReferenceBean referenceBean) throws Exception {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mPosition` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
    public static InputStream limitedInputStream(final InputStream is, final int limit) throws IOException {
        return new InputStream() {
            private int mPosition = 0;
            private int mMark = 0;
            private final int mLimit = Math.min(limit, is.available());
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mInMarked` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            byte[] mMarkBuffer;

            boolean mInMarked = false;
            boolean mInReset = false;
            boolean mDry = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mPosition` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            boolean mInReset = false;
            boolean mDry = false;
            private int mPosition = 0;
            private int mCount = 0;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mCount` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            boolean mDry = false;
            private int mPosition = 0;
            private int mCount = 0;

            @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mMarkBuffer` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java

        return new InputStream() {
            byte[] mMarkBuffer;

            boolean mInMarked = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mInReset` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java

            boolean mInMarked = false;
            boolean mInReset = false;
            boolean mDry = false;
            private int mPosition = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `instantiationStrategy` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
    private final ConcurrentHashMap<Class<?>, AtomicInteger> beanNameIdCounterMap = new ConcurrentHashMap<>();
    private final List<BeanInfo> registeredBeanInfos = new CopyOnWriteArrayList<>();
    private InstantiationStrategy instantiationStrategy;
    private final AtomicBoolean destroyed = new AtomicBoolean();
    private List<Class<?>> registeredClasses = new ArrayList<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `classLoaderResourcesCache` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java
public class ClassLoaderResourceLoader {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ClassLoaderResourceLoader.class);
    private static SoftReference<Map<ClassLoader, Map<String, Set<URL>>>> classLoaderResourcesCache = null;

    static {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `orderedPropertiesConfiguration` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ModuleEnvironment.java`
#### Snippet
```java
    private final Environment applicationDelegate;

    private OrderedPropertiesConfiguration orderedPropertiesConfiguration;

    private CompositeConfiguration dynamicGlobalConfiguration;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `globalConfiguration` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
    private InmemoryConfiguration appConfiguration;

    protected CompositeConfiguration globalConfiguration;

    protected List<Map<String, String>> globalConfigurationMaps;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `globalConfigurationMaps` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
    protected CompositeConfiguration globalConfiguration;

    protected List<Map<String, String>> globalConfigurationMaps;

    private CompositeConfiguration defaultDynamicGlobalConfiguration;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `defaultDynamicGlobalConfiguration` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
    protected List<Map<String, String>> globalConfigurationMaps;

    private CompositeConfiguration defaultDynamicGlobalConfiguration;

    private DynamicConfiguration defaultDynamicConfiguration;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `propertiesConfiguration` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java

    // dubbo properties in classpath
    private PropertiesConfiguration propertiesConfiguration;

    // java system props (-D)
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cachedDefaultName` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
    private final    Holder<Object>                        cachedAdaptiveInstance = new Holder<>();
    private volatile Class<?>                              cachedAdaptiveClass    = null;
    private          String                                cachedDefaultName;
    private volatile Throwable                             createAdaptiveInstanceError;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `instantiationStrategy` is accessed in both synchronized and unsynchronized contexts
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
    private final ExtensionDirector            extensionDirector;
    private final List<ExtensionPostProcessor> extensionPostProcessors;
    private InstantiationStrategy        instantiationStrategy;
    private final ActivateComparator           activateComparator;
    private final ScopeModel                   scopeModel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectingPromise` is accessed in both synchronized and unsynchronized contexts
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConnectionClient.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(NettyConnectionClient.class);

    private AtomicReference<Promise<Object>> connectingPromise;

    private Promise<Void> closePromise;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `application` is accessed in both synchronized and unsynchronized contexts
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java

    private volatile TagRouterRule tagRouterRule;
    private String application;
    private volatile BitList<Invoker<T>> invokers = BitList.emptyList();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `meshRuleManager` is accessed in both synchronized and unsynchronized contexts
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
    private volatile Set<String> remoteAppName = Collections.emptySet();

    protected MeshRuleManager meshRuleManager;
    protected Set<TracingContextProvider> tracingContextProviders;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `scriptRule` is accessed in both synchronized and unsynchronized contexts
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/script/config/AppScriptStateRouter.java`
#### Snippet
```java
    private static final String RULE_SUFFIX = ".script-router";

    private ScriptRule scriptRule;
    private ScriptStateRouter<T> scriptRouter;
    private String application;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `scriptRouter` is accessed in both synchronized and unsynchronized contexts
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/script/config/AppScriptStateRouter.java`
#### Snippet
```java

    private ScriptRule scriptRule;
    private ScriptStateRouter<T> scriptRouter;
    private String application;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `application` is accessed in both synchronized and unsynchronized contexts
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/script/config/AppScriptStateRouter.java`
#### Snippet
```java
    private ScriptRule scriptRule;
    private ScriptStateRouter<T> scriptRouter;
    private String application;

    public AppScriptStateRouter(URL url) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `RDS_RESULT` is accessed in both synchronized and unsynchronized contexts
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsRouteRuleManager.java`
#### Snippet
```java
    private static volatile Consumer<Map<String, RouteResult>> RDS_LISTENER;

    private static Map<String, RouteResult> RDS_RESULT;

    public RdsRouteRuleManager() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subscribeApplications` is accessed in both synchronized and unsynchronized contexts
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
public class XdsRouter<T> extends AbstractStateRouter<T> implements XdsRouteRuleListener, EdsEndpointListener {

    private Set<String> subscribeApplications;

    private final ConcurrentHashMap<String, List<XdsRouteRule>> xdsRouteRuleMap;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `GLOBAL_PILOT_EXCHANGER` is accessed in both synchronized and unsynchronized contexts
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/PilotExchanger.java`
#### Snippet
```java
    private final Map<String, Consumer<RdsVirtualHostListener>> rdsObserveConsumer = new ConcurrentHashMap<>();

    private static PilotExchanger GLOBAL_PILOT_EXCHANGER = null;

    private final ApplicationModel applicationModel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `certPair` is accessed in both synchronized and unsynchronized contexts
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioCitadelCertificateSigner.java`
#### Snippet
```java
    private final org.apache.dubbo.registry.xds.istio.IstioEnv istioEnv;

    private CertPair certPair;

    public IstioCitadelCertificateSigner() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `context` is accessed in both synchronized and unsynchronized contexts
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java
     * The global context of zookeeper.
     */
    private ZookeeperContext context;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `delayTime` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryNotifier.java`
#### Snippet
```java

    private Object rawAddresses;
    private long delayTime;

    // should delay notify or not
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `applicationModel` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
    protected Set<ServiceInstancesChangedListener> instanceListeners = new ConcurrentHashSet<>();

    protected ApplicationModel applicationModel;

    public AbstractServiceDiscovery(ApplicationModel applicationModel, URL registryURL) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metadataReport` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
    protected final ConcurrentHashMap<String, MetadataInfoStat> metadataInfos = new ConcurrentHashMap<>();
    protected final ScheduledFuture<?> refreshCacheFuture;
    protected MetadataReport metadataReport;
    protected String metadataType;
    protected final MetaCacheManager metaCacheManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `app` is accessed in both synchronized and unsynchronized contexts
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory.getLogger(MetadataInfo.class);

    private String app;
    // revision that will report to registry or remote meta center, must always update together with rawMetadataInfo, check {@link this#calAndGetRevision}
    private volatile String revision;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `exportedServiceURLs` is accessed in both synchronized and unsynchronized contexts
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    protected transient volatile boolean updated = false;
    private transient ConcurrentNavigableMap<String, SortedSet<URL>> subscribedServiceURLs;
    private transient ConcurrentNavigableMap<String, SortedSet<URL>> exportedServiceURLs;
    private transient ExtensionLoader<MetadataParamsFilter> loader;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initiated` is accessed in both synchronized and unsynchronized contexts
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java

    /* used at runtime */
    private transient AtomicBoolean initiated = new AtomicBoolean(false);
    // Json formatted metadata that will report to remote meta center, must always update together with revision, check {@link this#calAndGetRevision}
    private transient volatile String rawMetadataInfo;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subscribedServiceURLs` is accessed in both synchronized and unsynchronized contexts
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    private transient final Map<String, String> instanceParams;
    protected transient volatile boolean updated = false;
    private transient ConcurrentNavigableMap<String, SortedSet<URL>> subscribedServiceURLs;
    private transient ConcurrentNavigableMap<String, SortedSet<URL>> exportedServiceURLs;
    private transient ExtensionLoader<MetadataParamsFilter> loader;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `ruleMigrationFuture` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java

    protected ScheduledFuture<?> localRuleMigrationFuture;
    protected Future<?> ruleMigrationFuture;

    private DynamicConfiguration configuration;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `allInstances` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
    protected AtomicBoolean destroyed = new AtomicBoolean(false);

    protected Map<String, List<ServiceInstance>> allInstances;
    protected Map<String, List<ProtocolServiceKeyWithUrls>> serviceUrls;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serviceUrls` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java

    protected Map<String, List<ServiceInstance>> allInstances;
    protected Map<String, List<ProtocolServiceKeyWithUrls>> serviceUrls;

    private volatile long lastRefreshTime;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `listeners` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
    protected final Set<String> serviceNames;
    protected final ServiceDiscovery serviceDiscovery;
    protected Map<String, Set<NotifyListenerWithKey>> listeners;

    protected AtomicBoolean destroyed = new AtomicBoolean(false);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `destroyed` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
    protected Map<String, Set<NotifyListenerWithKey>> listeners;

    protected AtomicBoolean destroyed = new AtomicBoolean(false);

    protected Map<String, List<ServiceInstance>> allInstances;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serviceListener` is accessed in both synchronized and unsynchronized contexts
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/DynamicDirectory.java`
#### Snippet
```java
    protected volatile List<Configurator> configurators;

    protected ServiceInstancesChangedListener serviceListener;

    /**
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`(b[off + 7] & 0xFFL) << 0` can be replaced with '(b\[off + 7\] \& 0xFFL)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static double bytes2double(byte[] b, int off) {
        long j = ((b[off + 7] & 0xFFL) << 0) +
                ((b[off + 6] & 0xFFL) << 8) +
                ((b[off + 5] & 0xFFL) << 16) +
```

### PointlessBitwiseExpression
`(b[off + 1] & 0xFF) << 0` can be replaced with '(b\[off + 1\] \& 0xFF)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static short bytes2short(byte[] b, int off) {
        return (short) (((b[off + 1] & 0xFF) << 0) +
                ((b[off + 0]) << 8));
    }
```

### PointlessBitwiseExpression
`(b[off + 3] & 0xFF) << 0` can be replaced with '(b\[off + 3\] \& 0xFF)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static float bytes2float(byte[] b, int off) {
        int i = ((b[off + 3] & 0xFF) << 0) +
                ((b[off + 2] & 0xFF) << 8) +
                ((b[off + 1] & 0xFF) << 16) +
```

### PointlessBitwiseExpression
`(b[off + 7] & 0xFFL) << 0` can be replaced with '(b\[off + 7\] \& 0xFFL)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static long bytes2long(byte[] b, int off) {
        return ((b[off + 7] & 0xFFL) << 0) +
                ((b[off + 6] & 0xFFL) << 8) +
                ((b[off + 5] & 0xFFL) << 16) +
```

### PointlessBitwiseExpression
`(b[off + 3] & 0xFF) << 0` can be replaced with '(b\[off + 3\] \& 0xFF)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static int bytes2int(byte[] b, int off) {
        return ((b[off + 3] & 0xFF) << 0) +
                ((b[off + 2] & 0xFF) << 8) +
                ((b[off + 1] & 0xFF) << 16) +
```

### PointlessBitwiseExpression
`1 << 0` can be replaced with '1'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/textui/TTable.java`
#### Snippet
```java
         * border outer top
         */
        public static final int BORDER_OUTER_TOP = 1 << 0;

        /**
```

## RuleId[id=CallToStringConcatCanBeReplacedByOperator]
### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static String getResourceName(CharSequence type) {
        return type == null ? null : type.toString().replace('.', '/').concat(".class");
    }

```

## RuleId[id=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
            return certPair;
        }
        synchronized (this) {
            if (certPair == null || certPair.isExpire()) {
                try {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-serialization/dubbo-serialization-hessian2/src/main/java/org/apache/dubbo/common/serialize/hessian2/Hessian2FactoryManager.java`
#### Snippet
```java
            // system classloader
            if (SYSTEM_SERIALIZER_FACTORY == null) {
                synchronized (this) {
                    if (SYSTEM_SERIALIZER_FACTORY == null) {
                        SYSTEM_SERIALIZER_FACTORY = createSerializerFactory();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-serialization/dubbo-serialization-hessian2/src/main/java/org/apache/dubbo/common/serialize/hessian2/Hessian2FactoryManager.java`
#### Snippet
```java

        if (!CL_2_SERIALIZER_FACTORY.containsKey(classLoader)) {
            synchronized (this) {
                if (!CL_2_SERIALIZER_FACTORY.containsKey(classLoader)) {
                    SerializerFactory serializerFactory = createSerializerFactory();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/CancellationContext.java`
#### Snippet
```java
    private void notifyAndClearListeners() {
        ArrayList<ExecutableListener> tmpListeners;
        synchronized (this) {
            if (listeners == null) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/CancellationContext.java`
#### Snippet
```java
    public boolean cancel(Throwable cause) {
        boolean triggeredCancel = false;
        synchronized (this) {
            if (!cancelled) {
                cancelled = true;
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/bytebuddy/ByteBuddyProxy.java`
#### Snippet
```java
        ByteBuddyProxy proxy = cache.get(key);
        if (proxy == null) {
            synchronized (interfaces[0]) {
                proxy = cache.get(key);
                if (proxy == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AdaptiveLoadBalanceFilter.java`
#### Snippet
```java
    private ThreadPoolExecutor getExecutor(){
        if (null == executor) {
            synchronized (this) {
                if (null == executor) {
                    executor = new ThreadPoolExecutor(1, 1, 0L,TimeUnit.MILLISECONDS, new LinkedBlockingQueue<>(1024),
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java

        // for client destroy and create right now, only  lock current client
        synchronized (this) {
            ReferenceCountedClient<? extends RestClient> referenceCountedClient = clients.get(url.getAddress());

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
            // for double check
            if (referenceCountedClient.isDestroyed()) {
                synchronized (this) {
                    referenceCountedClient = clients.get(url.getAddress());
                    if (referenceCountedClient.isDestroyed()) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
        private static void initExecutorIfNecessary() {
            if (!closed && CURATOR_WATCHER_EXECUTOR_SERVICE == null) {
                synchronized (CuratorWatcherImpl.class) {
                    if (!closed && CURATOR_WATCHER_EXECUTOR_SERVICE == null) {
                        CURATOR_WATCHER_EXECUTOR_SERVICE = Executors.newSingleThreadExecutor(new NamedThreadFactory("Dubbo-CuratorWatcher"));
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
        client.close();
        CuratorWatcherImpl.closed = true;
        synchronized (CuratorWatcherImpl.class) {
            if (CuratorWatcherImpl.CURATOR_WATCHER_EXECUTOR_SERVICE != null) {
                CuratorWatcherImpl.CURATOR_WATCHER_EXECUTOR_SERVICE.shutdown();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
        }
        // Ensure that the initialization is completed when concurrent calls
        synchronized (this) {
            if (initialized) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
        getScopeModel().getDeployer().start();

        synchronized (this) {
            if (this.exported) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorPublisher.java`
#### Snippet
```java
    @Override
    public void startRequest() {
        synchronized (this) {
            if (!canRequest) {
                canRequest = true;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorPublisher.java`
#### Snippet
```java
    @Override
    public void request(long l) {
        synchronized (this) {
            if (SUBSCRIBED.get() && canRequest) {
                subscription.request(l >= Integer.MAX_VALUE ? Integer.MAX_VALUE : (int) l);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
    public static DubboBootstrap getInstance() {
        if (instance == null) {
            synchronized (DubboBootstrap.class) {
                if (instance == null) {
                    instance = DubboBootstrap.getInstance(ApplicationModel.defaultModel());
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/param/DynamicValues.java`
#### Snippet
```java
            return index;
        } else {
            synchronized (this) {
                // thread safe
                if (!value2Index.containsKey(value)) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/GsonUtils.java`
#### Snippet
```java
    private static Gson getGson() {
        if (gsonCache == null || !(gsonCache instanceof Gson)) {
            synchronized (GsonUtils.class) {
                if (gsonCache == null || !(gsonCache instanceof Gson)) {
                    gsonCache = new Gson();
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/GsonUtils.java`
#### Snippet
```java
    private static boolean isSupportGson() {
        if (supportGson == null) {
            synchronized (GsonUtils.class) {
                if (supportGson == null) {
                    try {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/GsonImpl.java`
#### Snippet
```java
    private Gson getGson() {
        if (gsonCache == null || !(gsonCache instanceof Gson)) {
            synchronized (this) {
                if (gsonCache == null || !(gsonCache instanceof Gson)) {
                    gsonCache = new Gson();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/JacksonImpl.java`
#### Snippet
```java
    private JsonMapper getJackson() {
        if (jacksonCache == null || !(jacksonCache instanceof JsonMapper)) {
            synchronized (this) {
                if (jacksonCache == null || !(jacksonCache instanceof JsonMapper)) {
                    jacksonCache = JsonMapper.builder()
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/lang/ShutdownHookCallbacks.java`
#### Snippet
```java

    public void destroy() {
        synchronized (this) {
            callbacks.clear();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/lang/ShutdownHookCallbacks.java`
#### Snippet
```java

    public Collection<ShutdownHookCallback> getCallbacks() {
        synchronized (this) {
            sort(this.callbacks);
            return this.callbacks;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/lang/ShutdownHookCallbacks.java`
#### Snippet
```java

    public ShutdownHookCallbacks addCallback(ShutdownHookCallback callback) {
        synchronized (this) {
            if (!callbacks.contains(callback)) {
                this.callbacks.add(callback);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
        if (bean == null) {
            // lock by type
            synchronized (type) {
                bean = getBean(name, type);
                if (bean == null) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
        if (bean == null) {
            // lock by type
            synchronized (type) {
                bean = getBean(name, type);
                if (bean == null) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java
        Map<ClassLoader, Map<String, Set<URL>>> classLoaderCache;
        if (classLoaderResourcesCache == null || (classLoaderCache = classLoaderResourcesCache.get()) == null) {
            synchronized (ClassLoaderResourceLoader.class) {
                if (classLoaderResourcesCache == null || (classLoaderCache = classLoaderResourcesCache.get()) == null) {
                    classLoaderCache = new ConcurrentHashMap<>();
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java

    public static void destroy() {
        synchronized (ClassLoaderResourceLoader.class) {
            classLoaderResourcesCache = null;
        }
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JsonUtils.java`
#### Snippet
```java
    public static JSON getJson() {
        if (json == null) {
            synchronized (JsonUtils.class) {
                if (json == null) {
                    String preferJsonFrameworkName = System.getProperty(CommonConstants.PREFER_JSON_FRAMEWORK_NAME);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Proxy.java`
#### Snippet
```java
        Proxy proxy = cache.get(key);
        if (proxy == null) {
            synchronized (ics[0]) {
                proxy = cache.get(key);
                if (proxy == null) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
    public static void registerGlobalDisposable(Disposable disposable) {
        if (!globalReusedDisposables.contains(disposable)) {
            synchronized (GlobalResourcesRepository.class) {
                if (!globalReusedDisposables.contains(disposable)) {
                    globalReusedDisposables.add(disposable);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
    public void removeGlobalDisposable(Disposable disposable) {
        if (globalReusedDisposables.contains(disposable)) {
            synchronized (GlobalResourcesRepository.class) {
                if (globalReusedDisposables.contains(disposable)) {
                    this.globalReusedDisposables.remove(disposable);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
    public void removeDisposable(Disposable disposable) {
        if (oneoffDisposables.contains(disposable)) {
            synchronized (this) {
                if (oneoffDisposables.contains(disposable)) {
                    oneoffDisposables.remove(disposable);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
    public static GlobalResourcesRepository getInstance() {
        if (instance == null) {
            synchronized (GlobalResourcesRepository.class) {
                if (instance == null) {
                    instance = new GlobalResourcesRepository();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
    public void registerDisposable(Disposable disposable) {
        if (!oneoffDisposables.contains(disposable)) {
            synchronized (this) {
                if (!oneoffDisposables.contains(disposable)) {
                    oneoffDisposables.add(disposable);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
    public ExecutorService getExecutorService() {
        if (executorService == null || executorService.isShutdown()) {
            synchronized (this) {
                if (executorService == null || executorService.isShutdown()) {
                    if (logger.isInfoEnabled()) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        Map<java.net.URL, List<String>> urlListMap = urlListMapCache.get();
        if (urlListMap == null) {
            synchronized (ExtensionLoader.class) {
                if ((urlListMap = urlListMapCache.get()) == null) {
                    urlListMap = new ConcurrentHashMap<>();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConnectionClient.java`
#### Snippet
```java
        boolean ret = connectingPromise.get().awaitUninterruptibly(getConnectTimeout(), TimeUnit.MILLISECONDS);
        // destroy connectingPromise after used
        synchronized (this) {
            connectingPromise.set(null);
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConnectionClient.java`
#### Snippet
```java
        connectingPromise.get().awaitUninterruptibly(getConnectTimeout(), TimeUnit.MILLISECONDS);
        // destroy connectingPromise after used
        synchronized (this) {
            connectingPromise.set(null);
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
        }

        synchronized (this) {
            if (!providerApplication.equals(application)) {
                if (StringUtils.isNotEmpty(application)) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java

        if (!remoteAppName.equals(currentApplication)) {
            synchronized (this) {
                Set<String> current = new HashSet<>(currentApplication);
                Set<String> previous = new HashSet<>(remoteAppName);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/ProviderAppStateRouter.java`
#### Snippet
```java
        }

        synchronized (this) {
            if (!providerApplication.equals(application)) {
                if (StringUtils.isNotEmpty(application)) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/script/config/AppScriptStateRouter.java`
#### Snippet
```java
        }

        synchronized (this) {
            if (!providerApplication.equals(application)) {
                if (StringUtils.isNotEmpty(application)) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/AppStateRouterFactory.java`
#### Snippet
```java
            return router;
        }
        synchronized (this) {
            if (router == null) {
                router = createRouter(url);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailbackClusterInvoker.java`
#### Snippet
```java
    private void addFailed(LoadBalance loadbalance, Invocation invocation, List<Invoker<T>> invokers, Invoker<T> lastInvoker, URL consumerUrl) {
        if (failTimer == null) {
            synchronized (this) {
                if (failTimer == null) {
                    failTimer = new HashedWheelTimer(
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
            ProtocolServer server = serverMap.get(key);
            if (server == null) {
                synchronized (this) {
                    server = serverMap.get(key);
                    if (server == null) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsRouteRuleManager.java`
#### Snippet
```java

    private void doSubscribeRds(String domain) {
        synchronized (RdsRouteRuleManager.class) {
            if (RDS_LISTENER == null) {
                RDS_LISTENER = rds -> {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java

        if (!subscribeApplications.equals(currentApplications)) {
            synchronized (this) {
                for (String currentApplication : currentApplications) {
                    if (!subscribeApplications.contains(currentApplication)) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java

        // update subset
        synchronized (this) {
            BitList<Invoker<T>> allInvokers = currentInvokeList.clone();
            for (DestinationSubset<T> subset : destinationSubsetMap.values()) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/PilotExchanger.java`
#### Snippet
```java

    public static PilotExchanger initialize(URL url) {
        synchronized (PilotExchanger.class) {
            if (GLOBAL_PILOT_EXCHANGER != null) {
                return GLOBAL_PILOT_EXCHANGER;
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/PilotExchanger.java`
#### Snippet
```java

    public static PilotExchanger getInstance() {
        synchronized (PilotExchanger.class) {
            return GLOBAL_PILOT_EXCHANGER;
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioCitadelCertificateSigner.java`
#### Snippet
```java

    private CertPair doGenerateCert() {
        synchronized (this) {
            if (certPair == null || certPair.isExpire()) {
                try {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java
        if (!INITIALIZED.get()) {
            // global look, make sure only one thread can initialize the zookeeper instances.
            synchronized (ZookeeperRegistryCenter.class) {
                if (!INITIALIZED.get()) {
                    for (Initializer initializer : this.initializers) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
            logger.info("Destroying instance listener of  " + this.getServiceNames());
            serviceDiscovery.removeServiceInstancesChangedListener(this);
            synchronized (this) {
                allInstances.clear();
                serviceUrls.clear();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/DynamicDirectory.java`
#### Snippet
```java
        }

        synchronized (this) {
            try {
                destroyAllInvokers();
```

## RuleId[id=DoubleBraceInitialization]
### DoubleBraceInitialization
Double brace initialization
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java
        } else {
            global = new MetricName(DUBBO_CONSUMER, MetricLevel.MAJOR);
            method = new MetricName(DUBBO_CONSUMER_METHOD, new HashMap<String, String>(4) {
                {
                    put(SERVICE, serviceName);
```

### DoubleBraceInitialization
Double brace initialization
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java
        if (isProvider) {
            global = new MetricName(DUBBO_PROVIDER, MetricLevel.MAJOR);
            method = new MetricName(DUBBO_PROVIDER_METHOD, new HashMap<String, String>(4) {
                {
                    put(SERVICE, serviceName);
```

### DoubleBraceInitialization
Double brace initialization
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
    private static final String SUFFIX = ".dubbo.cache";
    private static final char ESCAPE_MARK = '%';
    private static final Set<Character> LEGAL_CHARACTERS = Collections.unmodifiableSet(new HashSet<Character>() {{
        // - $ . _ 0-9 a-z A-Z
        add('-');
```

### DoubleBraceInitialization
Double brace initialization
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
     */
    private static final Map<String, ConfigChangeType> CONFIG_CHANGE_TYPES_MAP =
        unmodifiableMap(new HashMap<String, ConfigChangeType>() {
            // Initializes the elements that is mapping ConfigChangeType
            {
```

### DoubleBraceInitialization
Double brace initialization
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/DefaultProviderFirstParams.java`
#### Snippet
```java

public class DefaultProviderFirstParams implements ProviderFirstParams {
    private static final Set<String> PARAMS = Collections.unmodifiableSet(new HashSet<String>() {{
        addAll(Arrays.asList(RELEASE_KEY, DUBBO_VERSION_KEY, METHODS_KEY, TIMESTAMP_KEY, TAG_KEY));
    }});
```

## RuleId[id=CastConflictsWithInstanceof]
### CastConflictsWithInstanceof
Cast to 'MetadataEvent' type conflicts with preceding 'instanceof MetadataEvent.SubscribeEvent' check
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/event/MetricsSubscribeListener.java`
#### Snippet
```java
    @Override
    public boolean isSupport(MetricsEvent event) {
        return event instanceof MetadataEvent.SubscribeEvent && ((MetadataEvent) event).isAvailable();
    }

```

### CastConflictsWithInstanceof
Cast to 'MetadataEvent' type conflicts with preceding 'instanceof MetadataEvent.PushEvent' check
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/event/MetricsPushListener.java`
#### Snippet
```java
    @Override
    public boolean isSupport(MetricsEvent event) {
        return event instanceof MetadataEvent.PushEvent && ((MetadataEvent) event).isAvailable();
    }

```

### CastConflictsWithInstanceof
Cast to 'AbstractInterfaceConfig' type conflicts with preceding 'instanceof ReferenceConfigBase' check
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
        // special check service and reference config by unique service name, speed up the processing of large number of instances
        if (config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase) {
            C existedConfig = (C) findDuplicatedInterfaceConfig((AbstractInterfaceConfig) config);
            if (existedConfig != null) {
                return Optional.of(existedConfig);
```

### CastConflictsWithInstanceof
Cast to 'AbstractInterfaceConfig' type conflicts with preceding 'instanceof ReferenceConfigBase' check
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
        if (super.removeIfAbsent(config, configsMap)) {
            if (config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase) {
                removeInterfaceConfig((AbstractInterfaceConfig) config);
            }
            return true;
```

### CastConflictsWithInstanceof
Cast to 'Request' type conflicts with preceding 'instanceof HeartBeatRequest' check
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeartbeatHandler.java`
#### Snippet
```java

    private boolean isHeartbeatRequest(Object message) {
        return message instanceof HeartBeatRequest && ((Request) message).isHeartbeat();
    }

```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `multiValueMapAdd` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java

    private static Class multiValueMapClass = null;
    private static Method multiValueMapAdd = null;

    static {
```

### UnusedAssignment
Variable `multiValueMapClass` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
    private final static String JAVAX_MultiValueMap = "org.jboss.resteasy.specimpl.MultivaluedMapImpl";

    private static Class multiValueMapClass = null;
    private static Method multiValueMapAdd = null;

```

### UnusedAssignment
Variable `protocol` initializer `""` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/InternalServiceConfigBuilder.java`
#### Snippet
```java
     */
    private String getRelatedOrDefaultProtocol() {
        String protocol = "";
        // <dubbo:consumer/>
        List<ModuleModel> moduleModels = applicationModel.getPubModuleModels();
```

### UnusedAssignment
Variable `referenceProps` initializer `null` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java

    public static Map<String, Object> getReferenceAttributes(BeanDefinition beanDefinition) {
        Map<String, Object> referenceProps = null;
        if (beanDefinition.hasAttribute(Constants.REFERENCE_PROPS)) {
            referenceProps = (Map<String, Object>) beanDefinition.getAttribute(Constants.REFERENCE_PROPS);
```

### UnusedAssignment
Variable `consumerConfig` initializer `null` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceCreator.java`
#### Snippet
```java

    private void configureConsumerConfig(ReferenceConfig<?> referenceBean) {
        ConsumerConfig consumerConfig = null;
        Object consumer = getAttribute(attributes, "consumer");
        if (consumer != null) {
```

### UnusedAssignment
Variable `n` initializer `0` is redundant
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
            ByteArrayOutputStream output = new ByteArrayOutputStream();
            byte[] buffer = new byte[8192];
            int n = 0;
            while (-1 != (n = input.read(buffer))) {
                output.write(buffer, 0, n);
```

### UnusedAssignment
The value `serviceConfig.getInterfaceClass()` assigned to `cla` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        if (serviceConfig != null) {
            classLoader = serviceConfig.getInterfaceClassLoader();
            cla = serviceConfig.getInterfaceClass();
        }
        ProviderModel providerModel = new ProviderModel(serviceKey, serviceInstance, serviceModel,
```

### UnusedAssignment
Variable `mMark` initializer `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java
    protected byte[] mData;

    protected int mPosition, mLimit, mMark = 0;

    public UnsafeByteArrayInputStream(byte[] buf) {
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

        if (rem == 1) {
            int b1 = bs[r++] & MASK8;
            cs[w++] = code[b1 >> 2];
            cs[w++] = code[(b1 << 4) & MASK6];
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            if (pad) {
                cs[w++] = code[64];
                cs[w++] = code[64];
            }
        } else if (rem == 2) {
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            }
        } else if (rem == 2) {
            int b1 = bs[r++] & MASK8, b2 = bs[r++] & MASK8;
            cs[w++] = code[b1 >> 2];
            cs[w++] = code[(b1 << 4) & MASK6 | (b2 >> 4)];
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            cs[w++] = code[(b2 << 2) & MASK6];
            if (pad) {
                cs[w++] = code[64];
            }
        }
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

        if (rem == 2) {
            int c1 = indexOf(code, str.charAt(r++)), c2 = indexOf(code, str.charAt(r++));

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            int c1 = indexOf(code, str.charAt(r++)), c2 = indexOf(code, str.charAt(r++));

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
        } else if (rem == 3) {
            int c1 = indexOf(code, str.charAt(r++)), c2 = indexOf(code, str.charAt(r++)), c3 = indexOf(code, str.charAt(r++));
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
        } else if (rem == 3) {
            int c1 = indexOf(code, str.charAt(r++)), c2 = indexOf(code, str.charAt(r++)), c3 = indexOf(code, str.charAt(r++));

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
            b[w++] = (byte) ((c2 << 4) | (c3 >> 2));
        }
        return b;
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

        if (rem == 2) {
            int c1 = t[str.charAt(r++)], c2 = t[str.charAt(r++)];

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            int c1 = t[str.charAt(r++)], c2 = t[str.charAt(r++)];

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
        } else if (rem == 3) {
            int c1 = t[str.charAt(r++)], c2 = t[str.charAt(r++)], c3 = t[str.charAt(r++)];
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
        } else if (rem == 3) {
            int c1 = t[str.charAt(r++)], c2 = t[str.charAt(r++)], c3 = t[str.charAt(r++)];

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
            b[w++] = (byte) ((c2 << 4) | (c3 >> 2));
        }
        return b;
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            }
            String methodName = method.getName();
            Object value = null;
            if (methodName.length() > 3 && methodName.startsWith("get")) {
                value = map.get(methodName.substring(3, 4).toLowerCase() + methodName.substring(4));
```

### UnusedAssignment
Variable `localAddress` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

    private static InetAddress getLocalAddress0() {
        InetAddress localAddress = null;

        // @since 2.7.6, choose the {@link NetworkInterface} first
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
        Class[] parameterTypes = resolveTypes(methodParameters);
        Method method = findMethod(type, methodName, parameterTypes);
        T value = null;

        if (method == null) {
```

### UnusedAssignment
The value `StringUtils.join(parameterTypes)` assigned to `signature` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        String signature = clazz.getName() + "." + methodName;
        if (parameterTypes != null && parameterTypes.length > 0) {
            signature += StringUtils.join(parameterTypes);
        }
        Method method;
```

### UnusedAssignment
Variable `beanInfo` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static <T> T getProperty(Object bean, String methodName) {
        Class<?> beanClass = bean.getClass();
        BeanInfo beanInfo = null;
        T propertyValue = null;

```

### UnusedAssignment
Variable `watchService` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    private static Optional<WatchService> newWatchService() {
        Optional<WatchService> watchService = null;
        FileSystem fileSystem = FileSystems.getDefault();
        try {
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/function/ThrowableFunction.java`
#### Snippet
```java
     */
    default R execute(T t) throws RuntimeException {
        R result = null;
        try {
            result = apply(t);
```

### UnusedAssignment
Variable `v` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocal.java`
#### Snippet
```java

    private V initialize(InternalThreadLocalMap threadLocalMap) {
        V v = null;
        try {
            v = initialValue();
```

### UnusedAssignment
Variable `beanMetadata` initializer `null` is redundant
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/metadata/DubboReferencesMetadata.java`
#### Snippet
```java
            ReferenceConfig referenceConfig = referenceBean.getReferenceConfig();

            Map<String, Object> beanMetadata = null;
            if (referenceConfig != null) {
                beanMetadata = resolveBeanMetadata(referenceConfig);
```

### UnusedAssignment
Variable `published` initializer `false` is redundant
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
    @Override
    public boolean publishConfig(String key, String group, String content) {
        boolean published = false;
        try {
            published = configService.publishConfig(key, group, content);
```

### UnusedAssignment
Variable `removed` initializer `false` is redundant
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
    @Override
    public boolean removeConfig(String key, String group) {
        boolean removed = false;
        try {
            removed = configService.removeConfig(key, group);
```

### UnusedAssignment
Variable `contextPath` initializer `""` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java
    private String protocol = "http://";
    private final Invocation invocation;
    private String contextPath = "";


```

### UnusedAssignment
Variable `values` initializer `null` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java
        }

        Collection<String> values = null;
        if (!maps.containsKey(key)) {
            values = new HashSet<>();
```

### UnusedAssignment
Variable `url` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
        // if it's a callback, create proxy on client side, callback interface on client side can be invoked through channel
        // need get URL from channel and env when decode
        URL url = null;
        try {
            url = dubboProtocol.getInvoker(channel, inv).getUrl();
```

### UnusedAssignment
Variable `serviceKey` initializer `serviceKey( 0, (String) invocation.getObjectAttachme...` is redundant
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
                    Invoker<?> invoker = getInvoker(channel, invocation);
                } catch (RemotingException e) {
                    String serviceKey = serviceKey(
                        0,
                        (String) invocation.getObjectAttachmentWithoutConvert(PATH_KEY),
                        (String) invocation.getObjectAttachmentWithoutConvert(VERSION_KEY),
                        (String) invocation.getObjectAttachmentWithoutConvert(GROUP_KEY)
                    );
                    throw new RemotingException(channel, "The stub service[" + serviceKey + "] is not found, it may not be exported yet");
                }
```

### UnusedAssignment
Variable `enabled` initializer `true` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            if (invoker == null) { // Not in the cache, refer again
                try {
                    boolean enabled = true;
                    if (url.hasParameter(DISABLED_KEY)) {
                        enabled = !url.getParameter(DISABLED_KEY, false);
```

### UnusedAssignment
Variable `size` initializer `0` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
    @Override
    public String telnet(Channel channel, String message) {
        long size = 0;
        File file = LoggerFactory.getFile();
        StringBuilder buf = new StringBuilder();
```

### UnusedAssignment
The value `0` assigned to `size` is never used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                    }
                } else {
                    size = 0;
                    buf.append("\r\nMESSAGE: log file not exists or log appender is console .");
                }
```

## RuleId[id=IndexOfReplaceableByContains]
### IndexOfReplaceableByContains
`value.indexOf(VALUE_SEPARATOR) > -1` can be replaced with 'value.contains(VALUE_SEPARATOR)'
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosServiceName.java`
#### Snippet
```java

    private boolean isRange(String value) {
        return value != null && value.indexOf(VALUE_SEPARATOR) > -1 && value.split(VALUE_SEPARATOR).length > 1;
    }

```

## RuleId[id=ReplaceInefficientStreamCount]
### ReplaceInefficientStreamCount
Can be replaced with 'stream.anyMatch()'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java
            .filter(testIdentifier -> testIdentifier.getSource().isPresent())
            .filter(testIdentifier -> supportEmbeddedZookeeper(testIdentifier))
            .count() > 0;
    }

```

## RuleId[id=StringEqualsCharSequence]
### StringEqualsCharSequence
`String.equals()` called with 'CharSequence' argument
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MemberUtils.java`
#### Snippet
```java
        for (int i = 0; i < size; i++) {
            VariableElement parameter = parameters.get(i);
            if (!Objects.equals(parameter.asType().toString(), parameterTypes[i])) {
                return false;
            }
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/MediaTypeMatcher.java`
#### Snippet
```java
        List<MediaType> mediaTypeList = getDefaultList();

        for (MediaType mediaType : mediaTypes) {

            mediaTypeList.add(mediaType);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java

        Set<T> elements = new LinkedHashSet<>(size, loadFactor);
        for (int i = 0; i < size; i++) {
            elements.add(values[i]);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
            Method[] methods = publicOnly ? classToSearch.getMethods() : classToSearch.getDeclaredMethods();
            // Add the declared methods or public methods
            for (Method method : methods) {
                allMethods.add(method);
            }
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/MetricsKey.java`
#### Snippet
```java
    METADATA_GIT_COMMITID_METRIC("git.commit.id","Git Commit Id Metrics");
    // consumer metrics key
    ;

    private String name;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/InvokeMode.java`
#### Snippet
```java
public enum InvokeMode {

    SYNC, ASYNC, FUTURE;

}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/MemberCategory.java`
#### Snippet
```java
	 * via a call to {@link Class#getDeclaredClasses}.
	 */
	DECLARED_CLASSES;

}
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
The 'filter().findFirst().isPresent()' chain can be replaced with 'anyMatch()'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/ServiceAnnotationUtils.java`
#### Snippet
```java
    static boolean isServiceAnnotationPresent(TypeElement annotatedType) {
        return SUPPORTED_ANNOTATION_TYPES.stream()
                .filter(type -> isAnnotationPresent(annotatedType, type))
                .findFirst()
                .isPresent();
    }

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java

    private void putInterfaceMethodToMap(Map<Method, Method> serviceMethodsMap, List<Method> declaredServiceMethods) {
        declaredServiceMethods.stream().forEach(method -> {

            // filter static private default
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
            if (ANY_VALUE.equals(url.getServiceInterface())) {
                String root = toRootPath();
                childListeners.stream().forEach(childListener -> zkClient.removeChildListener(root, childListener));
            } else {
                for (String path : toCategoriesPath(url)) {
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
            } else {
                for (String path : toCategoriesPath(url)) {
                    childListeners.stream().forEach(childListener -> zkClient.removeChildListener(path, childListener));
                }
            }
```

## RuleId[id=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
     * @return
     */
    private <T> BitList<Invoker<T>> filterUsingStaticTag(BitList<Invoker<T>> invokers, URL url, Invocation invocation) {
        BitList<Invoker<T>> result;
        // Dynamic param
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
    }

    private <T> BitList<Invoker<T>> filterInvoker(BitList<Invoker<T>> invokers, Predicate<Invoker<T>> predicate) {
        if (invokers.stream().allMatch(predicate)) {
            return invokers;
```

## RuleId[id=OptionalContainsCollection]
### OptionalContainsCollection
'Optional' contains collection `Collection`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ConfigManager.java`
#### Snippet
```java
    }

    public Optional<Collection<ConfigCenterConfig>> getDefaultConfigCenter() {
        Collection<ConfigCenterConfig> defaults = getDefaultConfigs(getConfigsMap(getTagName(ConfigCenterConfig.class)));
        if (CollectionUtils.isEmpty(defaults)) {
```

### OptionalContainsCollection
'Optional' contains collection `Collection`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
    }

    public Optional<Collection<ConfigCenterConfig>> getDefaultConfigCenter() {
        return applicationConfigManager.getDefaultConfigCenter();
    }
```

### OptionalContainsCollection
'Optional' contains collection `List`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Configurator.java`
#### Snippet
```java
     * @return converted configurator list
     */
    static Optional<List<Configurator>> toConfigurators(List<URL> urls) {
        if (CollectionUtils.isEmpty(urls)) {
            return Optional.empty();
```

### OptionalContainsCollection
'Optional' contains collection `ConcurrentMap`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/MetricsCountSampler.java`
#### Snippet
```java
    void addRT(S source, K metricName, Long rt);

    Optional<ConcurrentMap<M, AtomicLong>> getCount(K metricName);

    <R extends MetricSample> List<R> collectRT(MetricSampleFactory<M, R> factory);
```

### OptionalContainsCollection
'Optional' contains collection `ConcurrentMap`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/SimpleMetricsCountSampler.java`
#### Snippet
```java

    @Override
    public Optional<ConcurrentMap<M, AtomicLong>> getCount(K metricName) {
        return Optional.ofNullable(metricCounter.get(metricName) == null ?
            EMPTY_COUNT :
```

### OptionalContainsCollection
'Optional' contains collection `List`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
     * else :routers list
     */
    private Optional<List<Router>> toRouters(List<URL> urls) {
        if (urls == null || urls.isEmpty()) {
            return Optional.empty();
```

## RuleId[id=MethodOverloadsParentMethod]
### MethodOverloadsParentMethod
Method `wait()` overloads a compatible method of a superclass, when overriding might have been intended
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ProviderBuilder.java`
#### Snippet
```java
    }

    public ProviderBuilder wait(Integer wait) {
        this.wait = wait;
        return getThis();
```

### MethodOverloadsParentMethod
Method `wait()` overloads a compatible method of a superclass, when overriding might have been intended
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/RegistryBuilder.java`
#### Snippet
```java
     */
    @Deprecated
    public RegistryBuilder wait(Integer wait) {
        this.wait = wait;
        return getThis();
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
        public String methodNamePascalCase() {
            String mn = methodName.replace("_", "");
            return String.valueOf(Character.toUpperCase(mn.charAt(0))) + mn.substring(1);
        }

```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
        public String methodNameCamelCase() {
            String mn = methodName.replace("_", "");
            return String.valueOf(Character.toLowerCase(mn.charAt(0))) + mn.substring(1);
        }
    }
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/SystemPageHandler.java`
#### Snippet
```java
        row = new ArrayList<String>();
        row.add("CPU");
        row.add(System.getProperty("os.arch", "") + ", " + String.valueOf(Runtime.getRuntime().availableProcessors()) + " cores");
        rows.add(row);

```

## RuleId[id=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `mark()` overrides synchronized method
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java

    @Override
    public void mark(int readAheadLimit) {
        mMark = mPosition;
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `reset()` overrides synchronized method
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java

    @Override
    public void reset() {
        mPosition = mMark;
    }
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Set `urls` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/SerializeSecurityConfigurator.java`
#### Snippet
```java

    private void loadAllow(ClassLoader classLoader) {
        Set<URL> urls = ClassLoaderResourceLoader.loadResources(SERIALIZE_ALLOW_LIST_FILE_PATH, classLoader);
        for (URL u : urls) {
            try {
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/SerializeSecurityConfigurator.java`
#### Snippet
```java

    private void loadBlocked(ClassLoader classLoader) {
        Set<URL> urls = ClassLoaderResourceLoader.loadResources(SERIALIZE_BLOCKED_LIST_FILE_PATH, classLoader);
        for (URL u : urls) {
            try {
```

### CollectionContainsUrl
Set `set` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java
        Map<String, Set<URL>> urlCache = classLoaderCache.get(currentClassLoader);
        if (!urlCache.containsKey(fileName)) {
            Set<URL> set = new LinkedHashSet<>();
            Enumeration<URL> urls;
            try {
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
            classLoadersToLoad.add(ClassUtils.getClassLoader());
            classLoadersToLoad.addAll(classLoaders);
            for (Set<URL> urls : ClassLoaderResourceLoader.loadResources(fileName, classLoadersToLoad).values()) {
                for (URL url : urls) {
                    InputStream is = url.openStream();
```

### CollectionContainsUrl
Set `set` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
        }

        Set<java.net.URL> set = null;
        try {
            List<ClassLoader> classLoadersToLoad = new LinkedList<>();
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    private void loadFromClass(Map<String, Class<?>> extensionClasses, boolean overridden,
                               Set<java.net.URL> urls, ClassLoader classLoader,
                               String[] includedPackages, String[] excludedPackages,
                               String[] onlyExtensionClassLoaderPackages) {
```

### CollectionContainsUrl
Map `urlListMap` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    private List<String> getResourceContent(java.net.URL resourceURL) throws IOException {
        Map<java.net.URL, List<String>> urlListMap = urlListMapCache.get();
        if (urlListMap == null) {
            synchronized (ExtensionLoader.class) {
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/FutureContext.java`
#### Snippet
```java
    private static final InternalThreadLocal<FutureContext> futureTL = new InternalThreadLocal<FutureContext>() {
        @Override
        protected FutureContext initialValue() {
            return new FutureContext();
        }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    private static final InternalThreadLocal<RpcContextAttachment> SERVER_RESPONSE_LOCAL = new InternalThreadLocal<RpcContextAttachment>() {
        @Override
        protected RpcContextAttachment initialValue() {
            return new RpcContextAttachment();
        }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    private static final InternalThreadLocal<RpcContextAttachment> CLIENT_ATTACHMENT = new InternalThreadLocal<RpcContextAttachment>() {
        @Override
        protected RpcContextAttachment initialValue() {
            return new RpcContextAttachment();
        }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    private static final InternalThreadLocal<CancellationContext> CANCELLATION_CONTEXT = new InternalThreadLocal<CancellationContext>() {
        @Override
        protected CancellationContext initialValue() {
            return new CancellationContext();
        }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    private static final InternalThreadLocal<RpcContextAttachment> SERVER_ATTACHMENT = new InternalThreadLocal<RpcContextAttachment>() {
        @Override
        protected RpcContextAttachment initialValue() {
            return new RpcContextAttachment();
        }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    private static final InternalThreadLocal<RpcServiceContext> SERVICE_CONTEXT = new InternalThreadLocal<RpcServiceContext>() {
        @Override
        protected RpcServiceContext initialValue() {
            return new RpcServiceContext();
        }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    private static final InternalThreadLocal<RpcContextAttachment> CLIENT_RESPONSE_LOCAL = new InternalThreadLocal<RpcContextAttachment>() {
        @Override
        protected RpcContextAttachment initialValue() {
            return new RpcContextAttachment();
        }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java
        return new AbstractProxyInvoker<T>(proxy, type, url) {
            @Override
            protected Object doInvoke(T proxy, String methodName,
                                      Class<?>[] parameterTypes,
                                      Object[] arguments) throws Throwable {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/javassist/JavassistProxyFactory.java`
#### Snippet
```java
            return new AbstractProxyInvoker<T>(proxy, type, url) {
                @Override
                protected Object doInvoke(T proxy, String methodName,
                                          Class<?>[] parameterTypes,
                                          Object[] arguments) throws Throwable {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestProtocol.java`
#### Snippet
```java
        Invoker<T> invoker = new AbstractInvoker<T>(type, url, new String[]{INTERFACE_KEY, GROUP_KEY, TOKEN_KEY}) {
            @Override
            protected Result doInvoke(Invocation invocation) {
                try {
                    RestMethodMetadata restMethodMetadata = metadataMap.get(invocation.getMethodName()).get(ParameterTypesComparator.getInstance(invocation.getParameterTypes()));
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    }

    protected static boolean isSnakeCase(String str) {
        return str.contains("_") || str.equals(str.toLowerCase()) || str.equals(str.toUpperCase());
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java

    @Override
    protected void setUrl(URL url) {
        super.setUrl(url);
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
            this.notifier = new RegistryNotifier(getUrl(), NacosRegistry.this.getDelay()) {
                @Override
                protected void doNotify(Object rawAddresses) {
                    List<Instance> instances = (List<Instance>) rawAddresses;
                    NacosRegistry.this.notifySubscriber(consumerUrl, serviceName, listener, instances);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/DefaultMetricsCollector.java`
#### Snippet
```java

        @Override
        protected void countConfigure(
            MetricsCountSampleConfigurer<String, MetricsEvent.Type, ApplicationMetric> sampleConfigure) {
            sampleConfigure.configureMetrics(configure -> new ApplicationMetric(sampleConfigure.getSource()));
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/retry/FailedUnsubscribedTask.java`
#### Snippet
```java

    @Override
    protected void doRetry(URL url, FailbackRegistry registry, Timeout timeout) {
        registry.unsubscribe(url, listener);
        registry.removeFailedUnsubscribedTask(url, listener);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/retry/FailedUnregisteredTask.java`
#### Snippet
```java

    @Override
    protected void doRetry(URL url, FailbackRegistry registry, Timeout timeout) {
        registry.doUnregister(url);
        registry.removeFailedUnregisteredTask(url);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/retry/FailedSubscribedTask.java`
#### Snippet
```java

    @Override
    protected void doRetry(URL url, FailbackRegistry registry, Timeout timeout) {
        registry.doSubscribe(url, listener);
        registry.removeFailedSubscribedTask(url, listener);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/retry/FailedRegisteredTask.java`
#### Snippet
```java

    @Override
    protected void doRetry(URL url, FailbackRegistry registry, Timeout timeout) {
        registry.doRegister(url);
        registry.removeFailedRegisteredTask(url);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscoveryChangeWatcher.java`
#### Snippet
```java
                .getBean(FrameworkExecutorRepository.class).getServiceDiscoveryAddressNotificationExecutor()) {
            @Override
            protected void doNotify(Object rawAddresses) {
                listeners.forEach(listener -> listener.onEvent((ServiceInstancesChangedEvent) rawAddresses));
            }
```

## RuleId[id=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java

    public synchronized void connect(CertConfig certConfig) {
        if (channel != null) {
            logger.error(INTERNAL_ERROR, "", "", "Dubbo Cert Authority server is already connected.");
            return;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.certConfig` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
        connect0(certConfig);

        this.certConfig = certConfig;

        // Try to generate cert from remote
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refreshFuture` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java

    public synchronized void disConnect() {
        if (refreshFuture != null) {
            refreshFuture.cancel(true);
            refreshFuture = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refreshFuture` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
    public synchronized void disConnect() {
        if (refreshFuture != null) {
            refreshFuture.cancel(true);
            refreshFuture = null;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refreshFuture` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
        if (refreshFuture != null) {
            refreshFuture.cancel(true);
            refreshFuture = null;
        }
        if (channel != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
            refreshFuture = null;
        }
        if (channel != null) {
            channel = null;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
        }
        if (channel != null) {
            channel = null;
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `certPair` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
        }
        synchronized (this) {
            if (certPair == null || certPair.isExpire()) {
                try {
                    logger.info("Try to generate cert from Dubbo Certificate Authority.");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `certPair` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
        }
        synchronized (this) {
            if (certPair == null || certPair.isExpire()) {
                try {
                    logger.info("Try to generate cert from Dubbo Certificate Authority.");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `certPair` accessed in synchronized context
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
                    CertPair certFromRemote = refreshCert();
                    if (certFromRemote != null) {
                        certPair = certFromRemote;
                    } else {
                        logger.error(CONFIG_SSL_CERT_GENERATE_FAILED, "", "", "Generate Cert from Dubbo Certificate Authority failed.");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.elements` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
    public synchronized void init(ProcessingEnvironment processingEnv) {
        super.init(processingEnv);
        this.elements = processingEnv.getElementUtils();
        this.objectMembers = elements.getAllMembers(elements.getTypeElement(Object.class.getName()));
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `elements` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
        super.init(processingEnv);
        this.elements = processingEnv.getElementUtils();
        this.objectMembers = elements.getAllMembers(elements.getTypeElement(Object.class.getName()));
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `elements` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
        super.init(processingEnv);
        this.elements = processingEnv.getElementUtils();
        this.objectMembers = elements.getAllMembers(elements.getTypeElement(Object.class.getName()));
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.urlList` accessed in synchronized context
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java
        @Override
        public synchronized void notify(List<URL> urls) {
            this.urlList = urls;
            if (multipleNotifyListenerWrapper != null) {
                this.multipleNotifyListenerWrapper.notifySourceListener();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `multipleNotifyListenerWrapper` accessed in synchronized context
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java
        public synchronized void notify(List<URL> urls) {
            this.urlList = urls;
            if (multipleNotifyListenerWrapper != null) {
                this.multipleNotifyListenerWrapper.notifySourceListener();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.multipleNotifyListenerWrapper` accessed in synchronized context
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java
            this.urlList = urls;
            if (multipleNotifyListenerWrapper != null) {
                this.multipleNotifyListenerWrapper.notifySourceListener();
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registryMap` accessed in synchronized context
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java
            List<URL> notifyURLs = new ArrayList<>();
            URL emptyURL = null;
            for (SingleNotifyListener singleNotifyListener : registryMap.values()) {
                List<URL> tmpUrls = singleNotifyListener.getUrlList();
                if (CollectionUtils.isEmpty(tmpUrls)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceExporter` accessed in synchronized context
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java
        // start metadata service exporter
        MetadataServiceDelegation metadataService = applicationModel.getBeanFactory().getOrRegisterBean(MetadataServiceDelegation.class);
        if (metadataServiceExporter == null) {
            metadataServiceExporter = new ConfigurableMetadataServiceExporter(applicationModel, metadataService);
            // fixme, let's disable local metadata service export at this moment
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceExporter` accessed in synchronized context
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java
        MetadataServiceDelegation metadataService = applicationModel.getBeanFactory().getOrRegisterBean(MetadataServiceDelegation.class);
        if (metadataServiceExporter == null) {
            metadataServiceExporter = new ConfigurableMetadataServiceExporter(applicationModel, metadataService);
            // fixme, let's disable local metadata service export at this moment
            if (!REMOTE_METADATA_STORAGE_TYPE.equals(getMetadataType(applicationModel))) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceExporter` accessed in synchronized context
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java
            // fixme, let's disable local metadata service export at this moment
            if (!REMOTE_METADATA_STORAGE_TYPE.equals(getMetadataType(applicationModel))) {
                metadataServiceExporter.export();
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceExporter` accessed in synchronized context
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java
    @Override
    public synchronized void onStopped(ApplicationModel scopeModel) {
        if (metadataServiceExporter != null && metadataServiceExporter.isExported()) {
            try {
                metadataServiceExporter.unexport();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceExporter` accessed in synchronized context
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java
    @Override
    public synchronized void onStopped(ApplicationModel scopeModel) {
        if (metadataServiceExporter != null && metadataServiceExporter.isExported()) {
            try {
                metadataServiceExporter.unexport();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceExporter` accessed in synchronized context
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java
        if (metadataServiceExporter != null && metadataServiceExporter.isExported()) {
            try {
                metadataServiceExporter.unexport();
            } catch (Exception ignored) {
                // ignored
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `subscription` accessed in synchronized context
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorPublisher.java`
#### Snippet
```java
                canRequest = true;
                long count = requested;
                subscription.request(count >= Integer.MAX_VALUE ? Integer.MAX_VALUE : (int) count);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `subscription` accessed in synchronized context
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorPublisher.java`
#### Snippet
```java
        synchronized (this) {
            if (SUBSCRIBED.get() && canRequest) {
                subscription.request(l >= Integer.MAX_VALUE ? Integer.MAX_VALUE : (int) l);
            } else {
                requested += l;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mInMarked` accessed in synchronized context
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            @Override
            public synchronized void mark(int readlimit) {
                mInMarked = true;
                mInReset = false;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mInReset` accessed in synchronized context
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            public synchronized void mark(int readlimit) {
                mInMarked = true;
                mInReset = false;

                // mark buffer is not empty
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mMarkBuffer` accessed in synchronized context
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
                int count = mCount - mPosition;
                if (count > 0) {
                    System.arraycopy(mMarkBuffer, mPosition, mMarkBuffer, 0, count);
                    mCount = count;
                    mPosition = 0;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mMarkBuffer` accessed in synchronized context
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
                int count = mCount - mPosition;
                if (count > 0) {
                    System.arraycopy(mMarkBuffer, mPosition, mMarkBuffer, 0, count);
                    mCount = count;
                    mPosition = 0;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mInMarked` accessed in synchronized context
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            @Override
            public synchronized void reset() throws IOException {
                if (!mInMarked) {
                    throw new IOException("should mark before reset!");
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mInReset` accessed in synchronized context
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
                }

                mInReset = true;
                mPosition = 0;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.globalConfiguration` accessed in synchronized context
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
        propertiesConfiguration.refresh();
        loadMigrationRule();
        this.globalConfiguration = null;
        this.globalConfigurationMaps = null;
        this.defaultDynamicGlobalConfiguration = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.globalConfigurationMaps` accessed in synchronized context
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
        loadMigrationRule();
        this.globalConfiguration = null;
        this.globalConfigurationMaps = null;
        this.defaultDynamicGlobalConfiguration = null;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.meshRuleCache` accessed in synchronized context
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
    public synchronized void onRuleChange(String appName, List<Map<String, Object>> rules) {
        // only update specified app's rule
        Map<String, VsDestinationGroup> appToVDGroup = new ConcurrentHashMap<>(this.meshRuleCache.getAppToVDGroup());
        try {
            VsDestinationGroup vsDestinationGroup = new VsDestinationGroup();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.meshRuleCache` accessed in synchronized context
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
    @Override
    public synchronized void clearRule(String appName) {
        Map<String, VsDestinationGroup> appToVDGroup = new ConcurrentHashMap<>(this.meshRuleCache.getAppToVDGroup());
        appToVDGroup.remove(appName);
        computeSubset(appToVDGroup);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `meshRuleManager` accessed in synchronized context
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
                current.removeAll(remoteAppName);
                for (String app : current) {
                    meshRuleManager.register(app, this);
                }
                for (String app : previous) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `meshRuleManager` accessed in synchronized context
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
                }
                for (String app : previous) {
                    meshRuleManager.unregister(app, this);
                }
                remoteAppName = currentApplication;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `invokersToReconnect` accessed in synchronized context
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
    private synchronized void refreshInvokerInternal() {
        BitList<Invoker<T>> copiedInvokers = invokers.clone();
        refreshInvokers(copiedInvokers, invokersToReconnect);
        refreshInvokers(copiedInvokers, disabledInvokers);
        validInvokers = copiedInvokers;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `retryScheduledFuture` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
            if (retryScheduledFuture == null) {
                synchronized (retryCounter) {
                    if (retryScheduledFuture == null) {
                        retryScheduledFuture = retryExecutor.scheduleWithFixedDelay(() -> {
                            // Check and connect to the metadata
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `retryScheduledFuture` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
                synchronized (retryCounter) {
                    if (retryScheduledFuture == null) {
                        retryScheduledFuture = retryExecutor.scheduleWithFixedDelay(() -> {
                            // Check and connect to the metadata
                            try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `retryPeriod` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
                                logger.error(COMMON_UNEXPECTED_EXCEPTION, "", "", "Unexpected error occur at failed retry, cause: " + t.getMessage(), t);
                            }
                        }, 500, retryPeriod, TimeUnit.MILLISECONDS);
                    }
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceProxies` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ReflectionBasedServiceDiscovery.java`
#### Snippet
```java
    private synchronized void destroyMetadataServiceProxy(ServiceInstance instance) {
        String key = computeKey(instance);
        if (metadataServiceProxies.containsKey(key)) {
            Object metadataServiceProxy = metadataServiceProxies.remove(key);
            if (metadataServiceProxy instanceof Destroyable) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceProxies` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ReflectionBasedServiceDiscovery.java`
#### Snippet
```java
        String key = computeKey(instance);
        if (metadataServiceProxies.containsKey(key)) {
            Object metadataServiceProxy = metadataServiceProxies.remove(key);
            if (metadataServiceProxy instanceof Destroyable) {
                ((Destroyable) metadataServiceProxy).$destroy();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataServiceProxies` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ReflectionBasedServiceDiscovery.java`
#### Snippet
```java

    private synchronized MetadataService getMetadataServiceProxy(ServiceInstance instance) {
        return ConcurrentHashMapUtils.computeIfAbsent(metadataServiceProxies, computeKey(instance), k -> MetadataUtils.referProxy(instance).getProxy());
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        }

        if (this.serviceInstance == null) {
            this.serviceInstance = createServiceInstance(this.metadataInfo);
        } else if (!isValidInstance(this.serviceInstance)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java

        if (this.serviceInstance == null) {
            this.serviceInstance = createServiceInstance(this.metadataInfo);
        } else if (!isValidInstance(this.serviceInstance)) {
            ServiceInstanceMetadataUtils.customizeInstance(this.serviceInstance, this.applicationModel);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.metadataInfo` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java

        if (this.serviceInstance == null) {
            this.serviceInstance = createServiceInstance(this.metadataInfo);
        } else if (!isValidInstance(this.serviceInstance)) {
            ServiceInstanceMetadataUtils.customizeInstance(this.serviceInstance, this.applicationModel);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        if (this.serviceInstance == null) {
            this.serviceInstance = createServiceInstance(this.metadataInfo);
        } else if (!isValidInstance(this.serviceInstance)) {
            ServiceInstanceMetadataUtils.customizeInstance(this.serviceInstance, this.applicationModel);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
            this.serviceInstance = createServiceInstance(this.metadataInfo);
        } else if (!isValidInstance(this.serviceInstance)) {
            ServiceInstanceMetadataUtils.customizeInstance(this.serviceInstance, this.applicationModel);
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.applicationModel` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
            this.serviceInstance = createServiceInstance(this.metadataInfo);
        } else if (!isValidInstance(this.serviceInstance)) {
            ServiceInstanceMetadataUtils.customizeInstance(this.serviceInstance, this.applicationModel);
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        }

        if (!isValidInstance(this.serviceInstance)) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        }

        ServiceInstance oldServiceInstance = this.serviceInstance;
        DefaultServiceInstance newServiceInstance = new DefaultServiceInstance((DefaultServiceInstance) oldServiceInstance);
        boolean revisionUpdated = calOrUpdateInstanceRevision(newServiceInstance);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
            logger.info(String.format("Metadata of instance changed, updating instance with revision %s.", newServiceInstance.getServiceMetadata().getRevision()));
            doUpdate(oldServiceInstance, newServiceInstance);
            this.serviceInstance = newServiceInstance;
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        // fixme, this metadata info might still being shared by other instances
//        unReportMetadata(this.metadataInfo);
        if (!isValidInstance(this.serviceInstance)) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
            return;
        }
        doUnregister(this.serviceInstance);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
            return;
        }
        this.serviceInstance = createServiceInstance(this.metadataInfo);
        if (!isValidInstance(this.serviceInstance)) {
            logger.warn(REGISTRY_FAILED_FETCH_INSTANCE, "", "", "No valid instance found, stop registering instance address to registry.");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.metadataInfo` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
            return;
        }
        this.serviceInstance = createServiceInstance(this.metadataInfo);
        if (!isValidInstance(this.serviceInstance)) {
            logger.warn(REGISTRY_FAILED_FETCH_INSTANCE, "", "", "No valid instance found, stop registering instance address to registry.");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        }
        this.serviceInstance = createServiceInstance(this.metadataInfo);
        if (!isValidInstance(this.serviceInstance)) {
            logger.warn(REGISTRY_FAILED_FETCH_INSTANCE, "", "", "No valid instance found, stop registering instance address to registry.");
            return;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        }

        boolean revisionUpdated = calOrUpdateInstanceRevision(this.serviceInstance);
        if (revisionUpdated) {
            reportMetadata(this.metadataInfo);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.metadataInfo` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        boolean revisionUpdated = calOrUpdateInstanceRevision(this.serviceInstance);
        if (revisionUpdated) {
            reportMetadata(this.metadataInfo);
            doRegister(this.serviceInstance);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceInstance` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
        if (revisionUpdated) {
            reportMetadata(this.metadataInfo);
            doRegister(this.serviceInstance);
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationModel` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java

                TimePair timePair = TimePair.start();
                GlobalMetricsEventMulticaster eventMulticaster = applicationModel.getBeanFactory().getBean(GlobalMetricsEventMulticaster.class);
                eventMulticaster.publishEvent(new MetadataEvent.SubscribeEvent(applicationModel, timePair));

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationModel` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
                TimePair timePair = TimePair.start();
                GlobalMetricsEventMulticaster eventMulticaster = applicationModel.getBeanFactory().getBean(GlobalMetricsEventMulticaster.class);
                eventMulticaster.publishEvent(new MetadataEvent.SubscribeEvent(applicationModel, timePair));

                metadata = MetadataUtils.getRemoteMetadata(revision, instances, metadataReport);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metadataReport` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
                eventMulticaster.publishEvent(new MetadataEvent.SubscribeEvent(applicationModel, timePair));

                metadata = MetadataUtils.getRemoteMetadata(revision, instances, metadataReport);

                if (metadata != MetadataInfo.EMPTY) {// succeeded
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationModel` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
                if (metadata != MetadataInfo.EMPTY) {// succeeded
                    metadata.init();
                    eventMulticaster.publishFinishEvent(new MetadataEvent.SubscribeEvent(applicationModel, timePair));
                    break;
                } else {// failed
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationModel` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
                    break;
                } else {// failed
                    eventMulticaster.publishErrorEvent(new MetadataEvent.SubscribeEvent(applicationModel, timePair));
                    if (triedTimes > 0) {
                        if (logger.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `updated` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        }
        addURL(exportedServiceURLs, url);
        updated = true;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `updated` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    @Override
    public synchronized MetadataInfo clone() {
        return new MetadataInfo(app, revision, services, initiated, extendParams, instanceParams, updated, subscribedServiceURLs, exportedServiceURLs, loader);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `updated` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
     */
    public synchronized String calAndGetRevision() {
        if (revision != null && !updated) {
            return revision;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `updated` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        }

        updated = false;

        if (CollectionUtils.isEmptyMap(services)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `updated` accessed in synchronized context
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        }

        updated = true;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ruleMigrationFuture` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java

        if (executorSubmit.compareAndSet(false, true)) {
            ruleMigrationFuture = ruleManageExecutor.submit(() -> {
                while (true) {
                    String rule = "";
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java

    public synchronized void addListenerAndNotify(URL url, NotifyListener listener) {
        if (destroyed.get()) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.listeners` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
        }

        Set<NotifyListenerWithKey> notifyListeners = this.listeners.computeIfAbsent(url.getServiceKey(), _k -> new ConcurrentHashSet<>());
        String protocol = listener.getConsumerUrl().getParameter(PROTOCOL_KEY, url.getProtocol());
        ProtocolServiceKey protocolServiceKey = new ProtocolServiceKey(url.getServiceInterface(), url.getVersion(), url.getGroup(),
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `allInstances` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
            serviceDiscovery.removeServiceInstancesChangedListener(this);
            synchronized (this) {
                allInstances.clear();
                serviceUrls.clear();
                listeners.clear();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serviceUrls` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
            synchronized (this) {
                allInstances.clear();
                serviceUrls.clear();
                listeners.clear();
                if (retryFuture != null && !retryFuture.isDone()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `listeners` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
                allInstances.clear();
                serviceUrls.clear();
                listeners.clear();
                if (retryFuture != null && !retryFuture.isDone()) {
                    retryFuture.cancel(true);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java

    public synchronized void removeListener(String serviceKey, NotifyListener notifyListener) {
        if (destroyed.get()) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.listeners` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java

        // synchronized method, no need to use DCL
        Set<NotifyListenerWithKey> notifyListeners = this.listeners.get(serviceKey);
        if (notifyListeners != null) {
            notifyListeners.removeIf(listener -> listener.getNotifyListener().equals(notifyListener));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.listeners` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
            // ServiceKey has no listener, remove set
            if (notifyListeners.size() == 0) {
                this.listeners.remove(serviceKey);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
     */
    private synchronized void doOnEvent(ServiceInstancesChangedEvent event) {
        if (destroyed.get() || !accept(event) || isRetryAndExpired(event)) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `allInstances` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java

        // grouping all instances of this app(service name) by revision
        for (Map.Entry<String, List<ServiceInstance>> entry : allInstances.entrySet()) {
            List<ServiceInstance> instances = entry.getValue();
            for (ServiceInstance instance : instances) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.serviceUrls` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
        }

        this.serviceUrls = newServiceUrls;
        this.notifyAddressChanged();
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serviceListener` accessed in synchronized context
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/DynamicDirectory.java`
#### Snippet
```java
            routerChain.destroy();
            invokersChangedListener = null;
            serviceListener = null;

            super.destroy(); // must be executed after unsubscribing
```

## RuleId[id=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/RouterChain.java`
#### Snippet
```java

    @SuppressWarnings({"rawtypes", "unchecked"})
    public static <T> RouterChain<T> buildChain(Class<T> interfaceClass, URL url) {
        SingleRouterChain<T> chain1 = buildSingleChain(interfaceClass, url);
        SingleRouterChain<T> chain2 = buildSingleChain(interfaceClass, url);
```

### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/AppStateRouterFactory.java`
#### Snippet
```java

    @SuppressWarnings("rawtypes")
    private volatile StateRouter router;

    @SuppressWarnings("unchecked")
```

### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/AppStateRouterFactory.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public <T> StateRouter<T> getRouter(Class<T> interfaceClass, URL url) {
        if (router != null) {
            return router;
```

### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mock/MockInvokersSelector.java`
#### Snippet
```java

    @SuppressWarnings("rawtypes")
    private void cacheNormalInvokers(BitList<Invoker<T>> invokers) {
        BitList<Invoker<T>> clonedInvokers = invokers.clone();
        clonedInvokers.removeIf((invoker) -> invoker.getUrl().getProtocol().equals(MOCK_PROTOCOL));
```

### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailoverClusterInvoker.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"unchecked", "rawtypes"})
    public Result doInvoke(Invocation invocation, final List<Invoker<T>> invokers, LoadBalance loadbalance) throws RpcException {
        List<Invoker<T>> copyInvokers = invokers;
        checkInvokers(copyInvokers, invocation);
```

### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/ForkingClusterInvoker.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"unchecked", "rawtypes"})
    public Result doInvoke(final Invocation invocation, List<Invoker<T>> invokers, LoadBalance loadbalance) throws RpcException {
        try {
            checkInvokers(invokers, invocation);
```

### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/MergeableClusterInvoker.java`
#### Snippet
```java
 */
@SuppressWarnings("unchecked")
public class MergeableClusterInvoker<T> extends AbstractClusterInvoker<T> {

    private static final ErrorTypeAwareLogger log = LoggerFactory.getErrorTypeAwareLogger(MergeableClusterInvoker.class);
```

### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/registry/ZoneAwareClusterInvoker.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"unchecked", "rawtypes"})
    public Result doInvoke(Invocation invocation, final List<Invoker<T>> invokers, LoadBalance loadbalance) throws RpcException {
        // First, pick the invoker (XXXClusterInvoker) that comes from the local registry, distinguish by a 'preferred' key.
        for (Invoker<T> invoker : invokers) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/MockClusterInvoker.java`
#### Snippet
```java

    @SuppressWarnings({"unchecked", "rawtypes"})
    private Result doMockInvoke(Invocation invocation, RpcException e) {
        Result result;
        Invoker<T> mockInvoker;
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     */
    @SuppressWarnings("deprecation")
    public static int getServerShutdownTimeout(ScopeModel scopeModel) {
        int timeout = DEFAULT_SERVER_SHUTDOWN_TIMEOUT;
        Configuration configuration = getGlobalConfiguration(scopeModel);
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ReferenceCache.java`
#### Snippet
```java
public interface ReferenceCache {
    @SuppressWarnings("unchecked")
    <T> T get(ReferenceConfigBase<T> referenceConfig);

    @SuppressWarnings("unchecked")
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ReferenceCache.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    <T> T get(String key, Class<T> type);

    @SuppressWarnings("unchecked")
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ReferenceCache.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    <T> T get(String key);

    @SuppressWarnings("unchecked")
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ReferenceCache.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    <T> List<T> getAll(Class<T> type);

    @SuppressWarnings("unchecked")
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ReferenceCache.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    <T> T get(Class<T> type);

    void destroy(String key, Class<?> type);
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionDirector.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> ExtensionLoader<T> getExtensionLoader(Class<T> type) {
        checkDestroyed();
        if (type == null) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionDirector.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private <T> ExtensionLoader<T> createExtensionLoader0(Class<T> type) {
        checkDestroyed();
        ExtensionLoader<T> loader;
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
     */
    @SuppressWarnings("deprecation")
    public List<T> getActivateExtension(URL url, String[] values, String group) {
        checkDestroyed();
        // solve the bug of using @SPI's wrapper method to report a null pointer exception.
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public T getLoadedExtension(String name) {
        checkDestroyed();
        if (StringUtils.isEmpty(name)) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    public List<T> getLoadedExtensionInstances() {
        checkDestroyed();
        List<T> instances = new ArrayList<>();
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private T postProcessBeforeInitialization(T instance, String name) throws Exception {
        if (extensionPostProcessors != null) {
            for (ExtensionPostProcessor processor : extensionPostProcessors) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private T postProcessAfterInitialization(T instance, String name) throws Exception {
        if (instance instanceof ExtensionAccessorAware) {
            ((ExtensionAccessorAware) instance).setExtensionAccessor(extensionDirector);
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
     */
    @SuppressWarnings("deprecation")
    private Map<String, Class<?>> loadExtensionClasses() throws InterruptedException {
        checkDestroyed();
        cacheDefaultExtensionName();
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
     */
    @SuppressWarnings("deprecation")
    private void cacheActivateClass(Class<?> clazz, String name) {
        Activate activate = clazz.getAnnotation(Activate.class);
        if (activate != null) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    private String findAnnotationName(Class<?> clazz) {
        Extension extension = clazz.getAnnotation(Extension.class);
        if (extension != null) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/support/ActivateComparator.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    private ActivateInfo parseActivate(Class<?> clazz) {
        ActivateInfo info = activateInfoMap.get(clazz);
        if (info != null) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/support/WrapperComparator.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    private OrderInfo parseOrder(Class<?> clazz) {
        OrderInfo info = new OrderInfo();
        if (clazz.isAnnotationPresent(Activate.class)) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocal.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public final void remove() {
        remove(InternalThreadLocalMap.getIfSet());
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java
     */
    @SuppressWarnings({"unused", "FieldMayBeFinal"})
    private volatile int workerState;

    private final long tickDuration;
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java

        @SuppressWarnings({"unused", "FieldMayBeFinal", "RedundantFieldInitialization"})
        private volatile int state = ST_INIT;

        /**
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    public static void joinMulticastGroup(MulticastSocket multicastSocket, InetAddress multicastAddress) throws
        IOException {
        setInterface(multicastSocket, multicastAddress instanceof Inet6Address);
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    public static void setInterface(MulticastSocket multicastSocket, boolean preferIpv6) throws IOException {
        boolean interfaceSet = false;
        for (NetworkInterface networkInterface : getValidNetworkInterfaces()) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     */
    @SuppressWarnings("deprecation")  // on JDK 9
    public static void makeAccessible(Method method) {
        if ((!Modifier.isPublic(method.getModifiers()) ||
                !Modifier.isPublic(method.getDeclaringClass().getModifiers())) && !method.isAccessible()) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    public static void appendParameters(Map<String, String> parameters, Object config, String prefix) {
        appendParameters0(parameters, config, prefix, true);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public <T> T getResponse(Class<T> clazz) {
        return newRpcContext.getResponse(clazz);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public V get() throws InterruptedException, ExecutionException {
        try {
            return (V) (((Result) future.get()).recreate());
```

### RedundantSuppression
Redundant suppression
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public V get(long timeout, TimeUnit unit) throws InterruptedException, ExecutionException, TimeoutException {
        try {
            return (V) (((Result) future.get(timeout, unit)).recreate());
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java

    @SuppressWarnings({"unchecked"})
    private T createProxy(Map<String, String> referenceParameters) {
        if (shouldJvmRefer(referenceParameters)) {
            createInvokerForLocal(referenceParameters);
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
     */
    @SuppressWarnings({"unchecked", "rawtypes"})
    private void createInvokerForLocal(Map<String, String> referenceParameters) {
        URL url = new ServiceConfigURL(LOCAL_PROTOCOL, LOCALHOST_VALUE, 0, interfaceClass.getName(), referenceParameters);
        url = url.setScopeModel(getScopeModel());
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
     */
    @SuppressWarnings({"unchecked", "rawtypes"})
    private void createInvokerForRemote() {
        if (urls.size() == 1) {
            URL curUrl = urls.get(0);
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java

    @SuppressWarnings({"unchecked", "rawtypes"})
    private void doExportUrls() {
        ModuleServiceRepository repository = getScopeModel().getServiceRepository();
        ServiceDescriptor serviceDescriptor;
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java

    @SuppressWarnings({"unchecked", "rawtypes"})
    private void doExportUrl(URL url, boolean withMetaData) {
        Invoker<?> invoker = proxyFactory.getInvoker(ref, (Class) interfaceClass, url);
        if (withMetaData) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractMethodBuilder.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public void build(T instance) {
        super.build(instance);

```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/SimpleReferenceCache.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> T get(String key, Class<T> type) {
        List<ReferenceConfigBase<?>> referenceConfigs = referenceKeyMap.get(key);
        if (CollectionUtils.isNotEmpty(referenceConfigs)) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/SimpleReferenceCache.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> T get(String key) {
        List<ReferenceConfigBase<?>> referenceConfigBases = referenceKeyMap.get(key);
        if (CollectionUtils.isNotEmpty(referenceConfigBases)) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/SimpleReferenceCache.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> T get(Class<T> type) {
        List<ReferenceConfigBase<?>> referenceConfigBases = referenceTypeMap.get(type);
        if (CollectionUtils.isNotEmpty(referenceConfigBases)) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> T getInstance(Class<T> type, String name) {
        if (context == null) {
            // ignore if spring context is not bound
```

### RedundantSuppression
Redundant suppression
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/lfu/LfuCache.java`
#### Snippet
```java
     */
    @SuppressWarnings("rawtypes")
    private final LFUCache store;

    /**
```

### RedundantSuppression
Redundant suppression
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/lfu/LfuCache.java`
#### Snippet
```java
     */
    @SuppressWarnings("rawtypes")
    public LfuCache (URL url) {
        final int max = url.getParameter("cache.size", 1000);
        final float factor = url.getParameter("cache.evictionFactor", 0.75f);
```

### RedundantSuppression
Redundant suppression
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/lfu/LfuCache.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public void put(Object key, Object value) {
        store.put(key, value);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/lfu/LfuCache.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public Object get(Object key) {
        return store.get(key);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/GlobalMetricsEventMulticaster.java`
#### Snippet
```java

    @SuppressWarnings({"rawtypes"})
    public GlobalMetricsEventMulticaster(ApplicationModel applicationModel) {
        ScopeBeanFactory beanFactory = applicationModel.getBeanFactory();
        ExtensionLoader<MetricsCollector> extensionLoader = applicationModel.getExtensionLoader(MetricsCollector.class);
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"rawtypes", "unchecked"})
    public void publishEvent(MetricsEvent event) {
        if (event instanceof EmptyEvent) {
            return;
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"unchecked"})
    public void publishFinishEvent(MetricsEvent event) {
        publishTimeEvent(event, metricsLifeListener -> metricsLifeListener.onEventFinish(event));
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"unchecked"})
    public void publishErrorEvent(MetricsEvent event) {
        publishTimeEvent(event, metricsLifeListener -> metricsLifeListener.onEventError(event));
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java

    @SuppressWarnings({"rawtypes"})
    private void publishTimeEvent(MetricsEvent event, Consumer<MetricsLifeListener> consumer) {
        if (event instanceof EmptyEvent) {
            return;
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporter.java`
#### Snippet
```java
    protected final URL url;
    @SuppressWarnings("rawtypes")
    protected final List<MetricsCollector> collectors = new ArrayList<>();
    public static final CompositeMeterRegistry compositeRegistry = new CompositeMeterRegistry();

```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporter.java`
#### Snippet
```java

    @SuppressWarnings("rawtypes")
    private void initCollectors() {
        ScopeBeanFactory beanFactory = applicationModel.getBeanFactory();
        beanFactory.getOrRegisterBean(AggregateMetricsCollector.class);
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporter.java`
#### Snippet
```java

    @SuppressWarnings({"unchecked", "rawtypes"})
    public void refreshData() {
        collectors.forEach(collector -> {
            List<MetricSample> samples = collector.collect();
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/service/DefaultMetricsService.java`
#### Snippet
```java

    @SuppressWarnings("rawtypes")
    protected final List<MetricsCollector> collectors = new ArrayList<>();

    public DefaultMetricsService(ApplicationModel applicationModel) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/service/DefaultMetricsService.java`
#### Snippet
```java

    @SuppressWarnings({"unchecked", "rawtypes"})
    private MetricsEntity sampleToEntity(MetricSample sample) {
        MetricsEntity entity = new MetricsEntity();

```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/collector/stat/MetadataStatComposite.java`
#### Snippet
```java

    @SuppressWarnings({"rawtypes", "unchecked"})
    public void calcRt(String applicationName, String registryOpType, Long responseTime) {
        for (LongContainer container : rtStats.stream().filter(longContainer -> longContainer.specifyType(registryOpType)).collect(Collectors.toList())) {
            Number current = (Number) ConcurrentHashMapUtils.computeIfAbsent(container, applicationName, container.getInitFunc());
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/collector/stat/MetadataStatComposite.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("rawtypes")
    public List<GaugeMetricSample> exportNumMetrics() {
        List<GaugeMetricSample> list = new ArrayList<>();
        for (MetadataEvent.Type type : numStats.keySet()) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/collector/stat/MetadataStatComposite.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("rawtypes")
    public List<GaugeMetricSample> exportRtMetrics() {
        List<GaugeMetricSample> list = new ArrayList<>();
        for (LongContainer<? extends Number> rtContainer : rtStats) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/collector/stat/MetadataStatComposite.java`
#### Snippet
```java

    @SuppressWarnings("rawtypes")
    public GaugeMetricSample convertToSample(String applicationName, MetadataEvent.Type type, MetricsCategory category, AtomicLong targetNumber) {
        return new GaugeMetricSample<>(type.getMetricsKey(), ApplicationMetric.getTagsByName(applicationName), category, targetNumber, AtomicLong::get);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java

    @SuppressWarnings({"rawtypes", "unchecked"})
    public void calcRt(String applicationName, String registryOpType, Long responseTime) {
        for (LongContainer container : rtStats.stream().filter(longContainer -> longContainer.specifyType(registryOpType)).collect(Collectors.toList())) {
            Number current = (Number) ConcurrentHashMapUtils.computeIfAbsent(container, applicationName, container.getInitFunc());
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"rawtypes"})
    public List<GaugeMetricSample> exportNumMetrics() {
        List<GaugeMetricSample> list = new ArrayList<>();
        for (RegistryEvent.Type type : numStats.keySet()) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"rawtypes"})
    public List<GaugeMetricSample> exportRtMetrics() {
        List<GaugeMetricSample> list = new ArrayList<>();
        for (LongContainer<? extends Number> rtContainer : rtStats) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java

    @SuppressWarnings({"rawtypes"})
    public List<GaugeMetricSample> exportSkMetrics() {
        List<GaugeMetricSample> list = new ArrayList<>();
        for (RegistryEvent.Type type : skStats.keySet()) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java

    @SuppressWarnings({"rawtypes"})
    public GaugeMetricSample convertToSample(String applicationName, RegistryEvent.Type type, MetricsCategory category, AtomicLong targetNumber) {
        return new GaugeMetricSample<>(type.getMetricsKey(), ApplicationMetric.getTagsByName(applicationName), category, targetNumber, AtomicLong::get);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/handler/ManyToManyMethodHandler.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public CompletableFuture<StreamObserver<T>> invoke(Object[] arguments) {
        CallStreamObserver<R> responseObserver = (CallStreamObserver<R>) arguments[0];
        StreamObserver<T> requestObserver = ReactorServerCalls.manyToMany(responseObserver, func);
```

### RedundantSuppression
Redundant suppression
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/handler/ManyToOneMethodHandler.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public CompletableFuture<StreamObserver<T>> invoke(Object[] arguments) {
        CallStreamObserver<R> responseObserver = (CallStreamObserver<R>) arguments[0];
        StreamObserver<T> requestObserver = ReactorServerCalls.manyToOne(responseObserver, func);
```

### RedundantSuppression
Redundant suppression
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ReflectionBasedServiceDiscovery.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    public final void fillServiceInstance(DefaultServiceInstance serviceInstance) {
        String hostId = serviceInstance.getAddress();
        if (metadataMap.containsKey(hostId)) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> Invoker<T> refer(Class<T> type, URL url) throws RpcException {
        url = getRegistryUrl(url);
        Registry registry = getRegistry(url);
```

### RedundantSuppression
Redundant suppression
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/ExchangeHandlerDispatcher.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"unchecked", "rawtypes"})
    public CompletableFuture<Object> reply(ExchangeChannel channel, Object request) throws RemotingException {
        return CompletableFuture.completedFuture(((Replier) replierDispatcher).reply(channel, request));
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/ReplierDispatcher.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"unchecked", "rawtypes"})
    public Object reply(ExchangeChannel channel, Object request) throws RemotingException {
        return ((Replier) getReplier(request.getClass())).reply(channel, request);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    protected Object decode(Channel channel, ChannelBuffer buffer, int readable, byte[] message) throws IOException {
        if (isClientSide(channel)) {
            return toString(message, getCharset(channel));
```

### RedundantSuppression
Redundant suppression
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/MultiMessageHandler.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public void received(Channel channel, Object message) throws RemotingException {
        if (message instanceof MultiMessage) {
            MultiMessage list = (MultiMessage) message;
```

### RedundantSuppression
Redundant suppression
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/UrlUtils.java`
#### Snippet
```java
     */
    public static String serializationOrDefault(URL url) {
        //noinspection OptionalGetWithoutIsPresent
        Optional<String> serializations = allSerializations(url).stream().findFirst();
        return serializations.orElseGet(DefaultSerializationSelector::getDefaultRemotingSerialization);
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public <T> T getRequest(Class<T> clazz) {
        return SERVICE_CONTEXT.get().getRequest(clazz);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public <T> T getResponse(Class<T> clazz) {
        return SERVICE_CONTEXT.get().getResponse(clazz);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public <T> CompletableFuture<T> getCompletableFuture() {
        return SERVICE_CONTEXT.get().getCompletableFuture();
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public <T> Future<T> getFuture() {
        return SERVICE_CONTEXT.get().getFuture();
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    @Deprecated
    @SuppressWarnings({"unchecked", "rawtypes"})
    public List<Invoker<?>> getInvokers() {
        return SERVICE_CONTEXT.get().getInvokers();
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public <T> CompletableFuture<T> asyncCall(Callable<T> callable) {
        return SERVICE_CONTEXT.get().asyncCall(callable);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public static AsyncContext startAsync() throws IllegalStateException {
        return RpcContextAttachment.startAsync();
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public static AsyncContext startAsync() throws IllegalStateException {
        RpcContextAttachment currentContext = getServerAttachment();
        if (currentContext.asyncContext == null) {
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> CompletableFuture<T> getCompletableFuture() {
        return FutureContext.getContext().getCompletableFuture();
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> Future<T> getFuture() {
        return FutureContext.getContext().getCompletableFuture();
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> Exporter<T> export(final Invoker<T> invoker) throws RpcException {
        final String uri = serviceKey(invoker.getUrl());
        Exporter<T> exporter = (Exporter<T>) exporterMap.get(uri);
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public V get() throws InterruptedException, ExecutionException {
        try {
            return super.get();
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public V get(long timeout, TimeUnit unit) throws InterruptedException, ExecutionException, TimeoutException {
        try {
            return super.get(timeout, unit);
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/bytebuddy/ByteBuddyProxyFactory.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected <T> T doGetProxy(Invoker<T> invoker, Class<?>[] interfaces) {
        ClassLoader classLoader = invoker.getInterface().getClassLoader();
        return (T) ByteBuddyProxy.newInstance(classLoader, interfaces, new InvokerInvocationHandler(invoker));
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/javassist/JavassistProxyFactory.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> T getProxy(Invoker<T> invoker, Class<?>[] interfaces) {
        try {
            return (T) Proxy.getProxy(interfaces).newInstance(new InvokerInvocationHandler(invoker));
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/wrapper/StubProxyFactoryWrapper.java`
#### Snippet
```java
    @Override
    @SuppressWarnings({"unchecked", "rawtypes"})
    public <T> T getProxy(Invoker<T> invoker) throws RpcException {
        return getProxy(invoker, false);
    }
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     */
    @SuppressWarnings({"unchecked", "rawtypes"})
    private String exportOrUnexportCallbackService(Channel channel, RpcInvocation inv, URL url, Class clazz, Object inst, Boolean export) throws IOException {
        int instid = System.identityHashCode(inst);

```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    private Object referOrDestroyCallbackService(Channel channel, URL url, Class<?> clazz, Invocation inv, int instid, boolean isRefer) {
        Object proxy;
        String invokerCacheKey = getServerSideCallbackInvokerCacheKey(channel, clazz.getName(), instid);
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
                if (!isInstancesOverLimit(channel, referurl, clazz.getName(), instid, true)) {
                    url.getOrDefaultApplicationModel().getDefaultModule().getServiceRepository().registerService(clazz);
                    @SuppressWarnings("rawtypes")
                    Invoker<?> invoker = new ChannelWrappedInvoker(clazz, channel, referurl, String.valueOf(instid));
                    proxy = proxyFactory.getProxy(invoker);
                    channel.setAttribute(proxyCacheKey, proxy);
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ChannelWrappedInvoker.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("deprecation")
    protected Result doInvoke(Invocation invocation) throws Throwable {
        RpcInvocation inv = (RpcInvocation) invocation;
        // use interface's name as service path to export if it's not found on client side
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/LazyConnectExchangeClient.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
final class LazyConnectExchangeClient implements ExchangeClient {

    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(LazyConnectExchangeClient.class);
```

### RedundantSuppression
Redundant suppression
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ReferenceCountExchangeClient.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
final class ReferenceCountExchangeClient implements ExchangeClient {

    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ReferenceCountExchangeClient.class);
```

### RedundantSuppression
Redundant suppression
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/FastJson2ObjectInput.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> T readObject(Class<T> cls, Type type) throws IOException, ClassNotFoundException {
        updateClassLoaderIfNeed();
        int length = readLength();
```

## RuleId[id=ComparatorResultComparison]
### ComparatorResultComparison
Comparison of compare method result with specific constant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CIDRUtils.java`
#### Snippet
```java
        int te = target.compareTo(end);

        return (st == -1 || st == 0) && (te == -1 || te == 0);
    }
}
```

### ComparatorResultComparison
Comparison of compare method result with specific constant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CIDRUtils.java`
#### Snippet
```java
        int te = target.compareTo(end);

        return (st == -1 || st == 0) && (te == -1 || te == 0);
    }
}
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'watchService'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
     * @return if based, return <code>true</code>, or <code>false</code>
     */
    private static boolean detectPoolingBasedWatchService(Optional<WatchService> watchService) {
        String className = watchService.map(Object::getClass).map(Class::getName).orElse(null);
        return POLLING_WATCH_SERVICE_CLASS_NAME.equals(className);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'watchService'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
        });

    private static final Optional<WatchService> watchService;

    /**
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
                        token = token.trim();
                    }
                } while (ignoreEmptyTokens && token.length() <= 0);

                tokens.add(token);
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
        }

        if (getterReturnUrl.size() <= 0) {
            // getter method not found, throw
            throw new IllegalStateException("Failed to create adaptive class for interface " + type.getName()
```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
charset can be used instead
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            return "";
        }
        return new String(copy, 0, index, charset.name()).trim();
    }

```

### CharsetObjectCanBeUsed
getCharset(channel) can be used instead
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                message = message + "\r\n";
            }
            byte[] msgData = ((String) message).getBytes(getCharset(channel).name());
            buffer.writeBytes(msgData);
        } else {
```

### CharsetObjectCanBeUsed
getCharset(channel) can be used instead
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            try {
                boolean isDoubleChar = message.length >= 3 && message[message.length - 3] < 0; // double byte char
                channel.send(new String(isDoubleChar ? new byte[]{32, 32, 8, 8} : new byte[]{32, 8}, getCharset(channel).name()));
            } catch (RemotingException e) {
                throw new IOException(StringUtils.toString(e));
```

## RuleId[id=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/java/org/apache/dubbo/springboot/demo/consumer/ConsumerApplication.java`
#### Snippet
```java
        ConsumerApplication application = context.getBean(ConsumerApplication.class);
        String result = application.doSayHello("world");
        System.out.println("result: " + result);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/java/org/apache/dubbo/springboot/demo/provider/ProviderApplication.java`
#### Snippet
```java
    public static void main(String[] args) throws Exception {
        SpringApplication.run(ProviderApplication.class, args);
        System.out.println("dubbo service started");
        new CountDownLatch(1).await();
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/WelcomeLogoApplicationListener.java`
#### Snippet
```java
            logger.info(bannerText);
        } else {
            System.out.print(bannerText);
        }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                try {
                    String greetings = greetingService.hello();
                    System.out.println(greetings + " from separated thread.");
                } catch (Exception e) {
//                    e.printStackTrace();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                try {
                    Object restResult = restDemoService.sayHello("rest");
                    System.out.println(restResult + " from separated thread.");
                    restResult = restDemoService.testBody5(TestPO.getInstance());
                    System.out.println(restResult + " from separated thread.");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                    System.out.println(restResult + " from separated thread.");
                    restResult = restDemoService.testBody5(TestPO.getInstance());
                    System.out.println(restResult + " from separated thread.");

                    restResult = restDemoService.hello(1, 2);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java

                    restResult = restDemoService.hello(1, 2);
                    System.out.println(restResult + " from separated thread.");

                    String form1 = restDemoService.testForm1("form1");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java

                    String form1 = restDemoService.testForm1("form1");
                    System.out.println(form1);

                    MultivaluedHashMap multivaluedHashMap = new MultivaluedHashMap();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                    multivaluedHashMap.put("2", Arrays.asList("2"));
                    MultivaluedMap form2 = restDemoService.testForm2(multivaluedHashMap);
                    System.out.println(form2);
                } catch (Exception e) {
                    e.printStackTrace();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
            try {
                CompletableFuture<String> hello = demoService.sayHelloAsync("world");
                System.out.println("result: " + hello.get());

                String greetings = greetingService.hello();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java

                String greetings = greetingService.hello();
                System.out.println("result: " + greetings);
            } catch (Exception e) {
//                e.printStackTrace();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
        DemoService service = context.getBean("demoServiceComponent", DemoServiceComponent.class);
        String hello = service.sayHello("world");
        System.out.println("result :" + hello);
    }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/LoggerFactory.java`
#### Snippet
```java
                }

                System.err.println("Dubbo: Unable to find a proper configured logger to log out.");
                for (Class<? extends LoggerAdapter> clazz : candidates) {
                    try {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/LoggerFactory.java`
#### Snippet
```java
                }
                if (found) {
                    System.err.println("Dubbo: Using default logger: " + loggerAdapter.getClass().getName() + ". " +
                        "If you cannot see any log, please configure -Ddubbo.application.logger property to your preferred logging framework.");
                } else {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/LoggerFactory.java`
#### Snippet
```java
                        "If you cannot see any log, please configure -Ddubbo.application.logger property to your preferred logging framework.");
                } else {
                    System.err.println("Dubbo: Unable to find any available logger adapter to log out. Dubbo logs will be ignored. " +
                        "Please configure -Ddubbo.application.logger property and add corresponding logging library to classpath.");
                }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/jdk/JdkLoggerAdapter.java`
#### Snippet
```java
                propertiesLoaded = true;
            } else {
                System.err.println("No such logging.properties in classpath for jdk logging config!");
            }
        } catch (Exception t) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/jdk/JdkLoggerAdapter.java`
#### Snippet
```java
            }
        } catch (Exception t) {
            System.err.println("Failed to load logging.properties in classpath for jdk logging config, cause: " + t.getMessage());
        }
        try {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
    public static void checkBytecode(String name, byte[] bytecode) {
        if (bytecode.length > JIT_LIMIT) {
            System.err.println("The template bytecode too long, may be affect the JIT compiler. template class: " + name);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/JarScanner.java`
#### Snippet
```java
        if (listFiles != null) {
            for (File file : listFiles) {
                System.out.println("scanFile: " + file.getPath());
                if (file.isDirectory()) {
                    scanFile(file.getPath());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/Test.java`
#### Snippet
```java
        Set<String> set = finder.findClassSet("org.apache.dubbo", msg -> {
        });
        System.out.println(set.size());
    }
}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-provider/src/main/java/org/apache/dubbo/demo/graalvm/provider/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public String sayHello(String name) {
        System.out.println("Hello " + name + ", request from consumer: " + RpcContext.getContext().getRemoteAddress());
        return "Hello " + name ;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-provider/src/main/java/org/apache/dubbo/demo/graalvm/provider/Application.java`
#### Snippet
```java
            .await();

        System.out.println("dubbo service started");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-provider/src/main/java/org/apache/dubbo/demo/graalvm/provider/Application.java`
#### Snippet
```java
        service.export();

        System.out.println("dubbo service started");
        new CountDownLatch(1).await();
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-generic-call/src/main/java/org/apache/dubbo/demo/consumer/GenericApplication.java`
#### Snippet
```java
                Object genericInvokeResult = genericService.$invoke("sayHello", new String[]{String.class.getName()},
                    new Object[]{param});
                System.out.println(genericInvokeResult);
                Thread.sleep(1000);
            } catch (Exception e) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/logging/MessageFormatter.java`
#### Snippet
```java
            sbuf.append(oAsString);
        } catch (Throwable t) {
            System.err
                .println("SLF4J: Failed toString() invocation on an object of type ["
                    + o.getClass().getName() + ']');
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-api/dubbo-demo-api-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
        DemoService service = reference.get();
        String message = service.sayHello("dubbo");
        System.out.println(message);
    }
}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-api/dubbo-demo-api-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
        DemoService demoService = bootstrap.getCache().get(reference);
        String message = demoService.sayHello("dubbo");
        System.out.println(message);

        // generic invoke
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-api/dubbo-demo-api-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
        Object genericInvokeResult = genericService.$invoke("sayHello", new String[]{String.class.getName()},
            new Object[]{"dubbo generic invoke"});
        System.out.println(genericInvokeResult);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-api/dubbo-demo-api-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
        service.export();

        System.out.println("dubbo service started");
        new CountDownLatch(1).await();
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-consumer/src/main/java/com/apache/dubbo/demo/graalvm/consumer/Application.java`
#### Snippet
```java
        DemoService service = reference.get();
        String message = service.sayHello("dubbo");
        System.out.println(message);
    }
}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-consumer/src/main/java/com/apache/dubbo/demo/graalvm/consumer/Application.java`
#### Snippet
```java
        DemoService demoService = bootstrap.getCache().get(reference);
        String message = demoService.sayHello("Native");
        System.out.println(message);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiWrapperConsumer.java`
#### Snippet
```java

        final GreeterWrapperService greeterWrapperService = referenceConfig.get();
        System.out.println("dubbo referenceConfig started");
        long st = System.currentTimeMillis();
        String reply = greeterWrapperService.sayHello("haha");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiWrapperConsumer.java`
#### Snippet
```java
        String reply = greeterWrapperService.sayHello("haha");
        // 4MB response
        System.out.println("Reply length:" + reply.length() + " cost:" + (System.currentTimeMillis() - st));
        System.in.read();
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiConsumer.java`
#### Snippet
```java

        GreeterService greeterService = referenceConfig.get();
        System.out.println("dubbo referenceConfig started");
        try {
            final HelloReply reply = greeterService.sayHello(HelloRequest.newBuilder()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiConsumer.java`
#### Snippet
```java
                .build());
            TimeUnit.SECONDS.sleep(1);
            System.out.println("Reply: " + reply.getMessage());

            CompletableFuture<String> sayHelloAsync = greeterService.sayHelloAsync("triple");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiConsumer.java`
#### Snippet
```java

            CompletableFuture<String> sayHelloAsync = greeterService.sayHelloAsync("triple");
            System.out.println("Async Reply: "+sayHelloAsync.get());
        } catch (Throwable t) {
            t.printStackTrace();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
        // logger.info(identifier + msg);
        String s = identifier + msg;
        System.out.println(s);
        if (reportWriter != null) {
            reportWriter.println(s);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `dubbo-container/dubbo-container-api/src/main/java/org/apache/dubbo/container/Main.java`
#### Snippet
```java
                logger.info("Dubbo " + container.getClass().getSimpleName() + " started!");
            }
            System.out.println(new SimpleDateFormat("[yyyy-MM-dd HH:mm:ss]").format(new Date()) + " Dubbo service server started!");
        } catch (RuntimeException e) {
            logger.error(CONFIG_START_DUBBO_ERROR, "", "", e.getMessage(), e);
```

## RuleId[id=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java

        public String methodNamePascalCase() {
            String mn = methodName.replace("_", "");
            return String.valueOf(Character.toUpperCase(mn.charAt(0))) + mn.substring(1);
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java

        public String methodNameCamelCase() {
            String mn = methodName.replace("_", "");
            return String.valueOf(Character.toLowerCase(mn.charAt(0))) + mn.substring(1);
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
                .append(prefix).append(" * <pre>\n");
            Arrays.stream(HtmlEscapers.htmlEscaper().escape(comments).split("\n"))
                .map(line -> line.replace("*/", "&#42;&#47;").replace("*", "&#42;"))
                .forEach(line -> builder.append(prefix).append(" * ").append(line).append("\n"));
            builder
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
                .append(prefix).append(" * <pre>\n");
            Arrays.stream(HtmlEscapers.htmlEscaper().escape(comments).split("\n"))
                .map(line -> line.replace("*/", "&#42;&#47;").replace("*", "&#42;"))
                .forEach(line -> builder.append(prefix).append(" * ").append(line).append("\n"));
            builder
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
        checkMultiName(property, value);
        if (isNotEmpty(value)) {
            String[] values = value.split("\\s*[,]+\\s*");
            for (String v : values) {
                if (v.startsWith(REMOVE_VALUE_PREFIX)) {
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
                }
                bb.flip();
                content = new String(bb.array()).replace("<", "&lt;")
                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
                modified = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
                bb.flip();
                content = new String(bb.array()).replace("<", "&lt;")
                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
                modified = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
                        .format(new Date(file.lastModified()));
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
                bb.flip();
                content = new String(bb.array()).replace("<", "&lt;")
                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
                modified = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
                        .format(new Date(file.lastModified()));
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                        writer.println("    <tr>");
                        writer.println("        <td>");
                        writer.println("            " + msg.replace("<", "&lt;").replace(">", "&lt;").replace("\n", "<br/>"));
                        writer.println("        </td>");
                        writer.println("    </tr>");
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                        writer.println("    <tr>");
                        writer.println("        <td>");
                        writer.println("            " + msg.replace("<", "&lt;").replace(">", "&lt;").replace("\n", "<br/>"));
                        writer.println("        </td>");
                        writer.println("    </tr>");
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                        writer.println("    <tr>");
                        writer.println("        <td>");
                        writer.println("            " + msg.replace("<", "&lt;").replace(">", "&lt;").replace("\n", "<br/>"));
                        writer.println("        </td>");
                        writer.println("    </tr>");
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java
        // Dubbo Application[1.1](appName)
        // Dubbo Module[1.1.1](appName/moduleName)
        String type = this.getClass().getSimpleName().replace("Model", "");
        String desc = "Dubbo " + type + "[" + this.getInternalId() + "]";

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

        String pairs = matcher.group(1);
        String[] pairArr = pairs.split("\\s*,\\s*");

        Map<String, String> parameters = new HashMap<>();
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeErrorTypeAwareLogger.java`
#### Snippet
```java
        }

        String[] segments = trimmedString.split("[-]");

        int[] errorCodeSegments = new int[2];
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
                                .copy(getCtConstructor(mCopyConstructors.get(code.substring(1))), mCtc, null));
                    } else {
                        String[] sn = mCtc.getSimpleName().split("\\$+"); // inner class name include $.
                        mCtc.addConstructor(
                                CtNewConstructor.make(code.replaceFirst(SIMPLE_NAME_TAG, sn[sn.length - 1]), mCtc));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
                        String[] sn = mCtc.getSimpleName().split("\\$+"); // inner class name include $.
                        mCtc.addConstructor(
                                CtNewConstructor.make(code.replaceFirst(SIMPLE_NAME_TAG, sn[sn.length - 1]), mCtc));
                    }
                }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        loadDirectoryInternal(extensionClasses, strategy, type);
        try {
            String oldType = type.replace("org.apache", "com.alibaba");
            if (oldType.equals(type)) {
                return;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/NativeClassSourceWriter.java`
#### Snippet
```java
            String code = codeGenerator.generate(true);
            try {
                String file = generatedSources + File.separator + it.getName().replaceAll("\\.", Matcher.quoteReplacement(File.separator));
                String dir = Paths.get(file).getParent().toString();
                FileUtils.forceMkdir(new File(dir));
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosConfigServiceWrapper.java`
#### Snippet
```java
            return null;
        }
        return data.replace(INNERCLASS_SYMBOL, INNERCLASS_COMPATIBLE_SYMBOL).replace(SLASH_CHAR, HYPHEN_CHAR);
    }
}
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/CodeGenerator.java`
#### Snippet
```java
                AdaptiveClassCodeGenerator codeGenerator = new AdaptiveClassCodeGenerator(it, value);
                String code = codeGenerator.generate();
                String file = p + File.separator + it.getName().replaceAll("\\.", Matcher.quoteReplacement(File.separator));
                String dir = Paths.get(file).getParent().toString();
                FileUtils.forceMkdir(new File(dir));
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/ClassFinder.java`
#### Snippet
```java

    public Set<String> findClassSet(String packageName, Consumer<String> consumer) {
        packageName = packageName.replace(".", "/");
        try {
            ClassLoader classLoader = Thread.currentThread().getContextClassLoader();
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/ClassFinder.java`
#### Snippet
```java
                    String path = file.getPath();
                    if (path.endsWith(".class")) {
                        int packageIndex = path.indexOf(packageName.replace("/", File.separator));
                        String classPath = path.substring(packageIndex, path.length() - 6);
                        result.add(classPath.replace(File.separator, "."));
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/ClassFinder.java`
#### Snippet
```java
            }
            String className = name.substring(0, name.length() - 6);
            classes.add(className.replace("/", "."));
        }
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/ConditionStateRouter.java`
#### Snippet
```java
                throw new IllegalArgumentException("Illegal route rule!");
            }
            rule = rule.replace("consumer.", "").replace("provider.", "");
            int i = rule.indexOf("=>");
            String whenRule = i < 0 ? null : rule.substring(0, i).trim();
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/ConditionStateRouter.java`
#### Snippet
```java
                throw new IllegalArgumentException("Illegal route rule!");
            }
            rule = rule.replace("consumer.", "").replace("provider.", "");
            int i = rule.indexOf("=>");
            String whenRule = i < 0 ? null : rule.substring(0, i).trim();
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
            return null;
        }
        return serviceName.replace(INNERCLASS_SYMBOL, INNERCLASS_COMPATIBLE_SYMBOL);
    }

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/FindPidWindowsProcessor.java`
#### Snippet
```java
            logger.info(String.format("Find result: %s", result));
            if (StringUtils.isNotEmpty(result)) {
                String[] values = result.split("\\r\\n");
                // values sample:
                // Protocol Local address          Foreign address        Status          PID
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/LogTelnetHandler.java`
#### Snippet
```java
                        }
                        bb.flip();
                        String content = new String(bb.array()).replace("<", "&lt;")
                                .replace(">", "&gt;").replace("\n", "<br/><br/>");
                        buf.append("\r\ncontent:").append(content);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/LogTelnetHandler.java`
#### Snippet
```java
                        bb.flip();
                        String content = new String(bb.array()).replace("<", "&lt;")
                                .replace(">", "&gt;").replace("\n", "<br/><br/>");
                        buf.append("\r\ncontent:").append(content);

```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/LogTelnetHandler.java`
#### Snippet
```java
                        bb.flip();
                        String content = new String(bb.array()).replace("<", "&lt;")
                                .replace(">", "&gt;").replace("\n", "<br/><br/>");
                        buf.append("\r\ncontent:").append(content);

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/TraceTelnetHandler.java`
#### Snippet
```java
            return "Please input service name, eg: \r\ntrace XxxService\r\ntrace XxxService xxxMethod\r\ntrace XxxService xxxMethod 10\r\nor \"cd XxxService\" firstly.";
        }
        String[] parts = message.split("\\s+");
        String method;
        String times;
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/TelnetCommandDecoder.java`
#### Snippet
```java
        if (!StringUtils.isBlank(str)) {
            str = str.trim();
            String[] array = str.split("(?<![\\\\]) ");
            if (array.length > 0) {
                String[] targetArgs = new String[array.length - 1];
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosConfigServiceWrapper.java`
#### Snippet
```java
            return null;
        }
        return data.replace(INNERCLASS_SYMBOL, INNERCLASS_COMPATIBLE_SYMBOL).replace(SLASH_CHAR, HYPHEN_CHAR);
    }

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
            if (StringUtils.isNotEmpty(key) && key.equals(url.getServiceKey()) && (Character.isLetter(key.charAt(0))
                || key.charAt(0) == '_') && StringUtils.isNotEmpty(value)) {
                String[] arr = value.trim().split(URL_SPLIT);
                List<URL> urls = new ArrayList<>();
                for (String u : arr) {
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetHandlerAdapter.java`
#### Snippet
```java
        String prompt = channel.getUrl().getParameterAndDecoded(Constants.PROMPT_KEY, Constants.DEFAULT_PROMPT);
        boolean noprompt = message.contains("--no-prompt");
        message = message.replace("--no-prompt", "");
        StringBuilder buf = new StringBuilder();
        message = message.trim();
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                }
                                bb.flip();
                                String content = new String(bb.array()).replace("<", "&lt;")
                                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
                                buf.append("\r\ncontent:" + content);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                bb.flip();
                                String content = new String(bb.array()).replace("<", "&lt;")
                                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
                                buf.append("\r\ncontent:" + content);

```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                bb.flip();
                                String content = new String(bb.array()).replace("<", "&lt;")
                                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
                                buf.append("\r\ncontent:" + content);

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `dubbo-container/dubbo-container-spring/src/main/java/org/apache/dubbo/container/spring/SpringContainer.java`
#### Snippet
```java
            configPath = DEFAULT_SPRING_CONFIG;
        }
        context = new ClassPathXmlApplicationContext(configPath.split("[,\\s]+"), false);
        context.refresh();
        context.start();
```

## RuleId[id=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'java.lang.String\[\]' found
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java
                        .map(VariableElement::asType)
                        .map(TypeUtils::toString)
                        .toArray(String[]::new);
    }
}
```

## RuleId[id=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `MetricsEvent()` of an abstract class should not be declared 'public'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/MetricsEvent.java`
#### Snippet
```java
    protected transient Object source;

    public MetricsEvent(Object source) {
        if (source == null) {
            throw new IllegalArgumentException("null source");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMetricsReporterFactory()` of an abstract class should not be declared 'public'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporterFactory.java`
#### Snippet
```java
    private final ApplicationModel applicationModel;

    public AbstractMetricsReporterFactory(ApplicationModel applicationModel) {
        this.applicationModel = applicationModel;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `DependencyFilter()` of an abstract class should not be declared 'public'
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/DependencyFilter.java`
#### Snippet
```java
	 * @param dependencies the source dependencies
	 */
	public DependencyFilter(List<? extends FilterableDependency> dependencies) {
		this.filters = dependencies;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractProxyInvoker()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyInvoker.java`
#### Snippet
```java
    private final URL url;

    public AbstractProxyInvoker(T proxy, Class<T> type, URL url) {
        if (proxy == null) {
            throw new IllegalArgumentException("proxy == null");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractExporter()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractExporter.java`
#### Snippet
```java
    private volatile boolean unexported = false;

    public AbstractExporter(Invoker<T> invoker) {
        if (invoker == null) {
            throw new IllegalStateException("service invoker == null");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractProxyProtocol()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    protected ProxyFactory proxyFactory;

    public AbstractProxyProtocol() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractProxyProtocol()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    }

    public AbstractProxyProtocol(Class<?>... exceptions) {
        for (Class<?> exception : exceptions) {
            addRpcException(exception);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInvoker()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractInvoker.java`
#### Snippet
```java
    }

    public AbstractInvoker(Class<T> type, URL url, String[] keys) {
        this(type, url, convertAttachment(url, keys));
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInvoker()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractInvoker.java`
#### Snippet
```java
    // -- Constructor

    public AbstractInvoker(Class<T> type, URL url) {
        this(type, url, (Map<String, Object>) null);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInvoker()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractInvoker.java`
#### Snippet
```java
    }

    public AbstractInvoker(Class<T> type, URL url, Map<String, Object> attachment) {
        if (type == null) {
            throw new IllegalArgumentException("service type == null");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAnnotationBeanPostProcessor()` of an abstract class should not be declared 'public'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
     * @param annotationTypes the multiple types of {@link Annotation annotations}
     */
    public AbstractAnnotationBeanPostProcessor(Class<? extends Annotation>... annotationTypes) {
        Assert.notEmpty(annotationTypes, "The argument of annotations' types must not empty");
        this.annotationTypes = annotationTypes;
```

### NonProtectedConstructorInAbstractClass
Constructor `FailbackRegistry()` of an abstract class should not be declared 'public'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
    private CompatibleFailbackRegistry failbackRegistry;

    public FailbackRegistry(URL url) {
        failbackRegistry = new CompatibleFailbackRegistry(url.getOriginalURL(), this);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRegistry()` of an abstract class should not be declared 'public'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
    private CompatibleAbstractRegistry abstractRegistry;

    public AbstractRegistry(com.alibaba.dubbo.common.URL url) {
        abstractRegistry = new CompatibleAbstractRegistry(url.getOriginalURL());
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTripleReactorPublisher()` of an abstract class should not be declared 'public'
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorPublisher.java`
#### Snippet
```java
    }

    public AbstractTripleReactorPublisher(Consumer<CallStreamObserver<?>> onSubscribe, Runnable shutdownHook) {
        this.onSubscribe = onSubscribe;
        this.shutdownHook = shutdownHook;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTripleReactorPublisher()` of an abstract class should not be declared 'public'
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorPublisher.java`
#### Snippet
```java
    private final AtomicBoolean CALLED_SHUT_DOWN_HOOK = new AtomicBoolean();

    public AbstractTripleReactorPublisher() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractIsolationExecutorSupport()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/executor/AbstractIsolationExecutorSupport.java`
#### Snippet
```java
    private final FrameworkServiceRepository frameworkServiceRepository;

    public AbstractIsolationExecutorSupport(URL url) {
        this.url = url;
        this.executorRepository = ExecutorRepository.getInstance(url.getOrDefaultApplicationModel());
```

### NonProtectedConstructorInAbstractClass
Constructor `ServiceAddressURL()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
    }

    public ServiceAddressURL(URLAddress urlAddress, URLParam urlParam, URL consumerURL) {
        super(urlAddress, urlParam);
        this.consumerURL = consumerURL;
```

### NonProtectedConstructorInAbstractClass
Constructor `ServiceAddressURL()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
//    private transient Map<String, String> allParameters;

    public ServiceAddressURL(
            String protocol,
            String username,
```

### NonProtectedConstructorInAbstractClass
Constructor `TreePathDynamicConfiguration()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/TreePathDynamicConfiguration.java`
#### Snippet
```java
    }

    public TreePathDynamicConfiguration(String rootPath,
                                        String threadPoolPrefixName,
                                        int threadPoolSize,
```

### NonProtectedConstructorInAbstractClass
Constructor `TreePathDynamicConfiguration()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/TreePathDynamicConfiguration.java`
#### Snippet
```java
    protected final String rootPath;

    public TreePathDynamicConfiguration(URL url) {
        super(url);
        this.rootPath = getRootPath(url);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDeployer()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/AbstractDeployer.java`
#### Snippet
```java
    private E scopeModel;

    public AbstractDeployer(E scopeModel) {
        this.scopeModel = scopeModel;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `StringToIterableConverter()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToIterableConverter.java`
#### Snippet
```java
    private ConverterUtil converterUtil;

    public StringToIterableConverter(FrameworkModel frameworkModel) {
        converterUtil = frameworkModel.getBeanFactory().getBean(ConverterUtil.class);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractReferenceConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
    protected String protocol;

    public AbstractReferenceConfig() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractReferenceConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
    }

    public AbstractReferenceConfig(ModuleModel moduleModel) {
        super(moduleModel);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMethodConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractMethodConfig.java`
#### Snippet
```java
    }

    public AbstractMethodConfig(ModuleModel moduleModel) {
        super(moduleModel);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMethodConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractMethodConfig.java`
#### Snippet
```java
    protected Integer forks;

    public AbstractMethodConfig() {
        super();
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ServiceConfigBase()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java


    public ServiceConfigBase() {
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute("ORIGIN_CONFIG", this);
```

### NonProtectedConstructorInAbstractClass
Constructor `ServiceConfigBase()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
    }

    public ServiceConfigBase(Service service) {
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute("ORIGIN_CONFIG", this);
```

### NonProtectedConstructorInAbstractClass
Constructor `ServiceConfigBase()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
    }

    public ServiceConfigBase(ModuleModel moduleModel) {
        super(moduleModel);
        serviceMetadata = new ServiceMetadata();
```

### NonProtectedConstructorInAbstractClass
Constructor `ServiceConfigBase()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
    }

    public ServiceConfigBase(ModuleModel moduleModel, Service service) {
        super(moduleModel);
        serviceMetadata = new ServiceMetadata();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractServiceConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java
    }

    public AbstractServiceConfig(ModuleModel moduleModel) {
        super(moduleModel);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractServiceConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java
    private Integer payload;

    public AbstractServiceConfig() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `ReferenceConfigBase()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    }

    public ReferenceConfigBase(ModuleModel moduleModel, Reference reference) {
        super(moduleModel);
        serviceMetadata = new ServiceMetadata();
```

### NonProtectedConstructorInAbstractClass
Constructor `ReferenceConfigBase()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    }

    public ReferenceConfigBase(ModuleModel moduleModel) {
        super(moduleModel);
        serviceMetadata = new ServiceMetadata();
```

### NonProtectedConstructorInAbstractClass
Constructor `ReferenceConfigBase()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    }

    public ReferenceConfigBase(Reference reference) {
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute(ORIGIN_CONFIG, this);
```

### NonProtectedConstructorInAbstractClass
Constructor `ReferenceConfigBase()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    protected Boolean unloadClusterRelated;

    public ReferenceConfigBase() {
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute(ORIGIN_CONFIG, this);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInterfaceConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
    }

    public AbstractInterfaceConfig(ModuleModel moduleModel) {
        super(moduleModel);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInterfaceConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
    private Boolean singleton;

    public AbstractInterfaceConfig() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConfigManager()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java


    public AbstractConfigManager(ScopeModel scopeModel, Collection<Class<? extends AbstractConfig>> supportedConfigTypes) {
        this.scopeModel = scopeModel;
        this.applicationModel = ScopeModelUtil.getApplicationModel(scopeModel);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
    private transient volatile ScopeModel scopeModel;

    public AbstractConfig() {
        this(null);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConfig()` of an abstract class should not be declared 'public'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
    }

    public AbstractConfig(ScopeModel scopeModel) {
        this.setScopeModel(scopeModel);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractServerCallListener()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/AbstractServerCallListener.java`
#### Snippet
```java
    final ServerCallToObserverAdapter<Object> responseObserver;

    public AbstractServerCallListener(RpcInvocation invocation, Invoker<?> invoker,
        ServerCallToObserverAdapter<Object> responseObserver) {
        this.invocation = invocation;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractStream()` of an abstract class should not be declared 'public'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/AbstractStream.java`
#### Snippet
```java
    private static final boolean HAS_PROTOBUF = hasProtobuf();

    public AbstractStream(Executor executor, FrameworkModel frameworkModel) {
        this.executor = new SerializingExecutor(executor);
        this.frameworkModel = frameworkModel;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRouter()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/AbstractRouter.java`
#### Snippet
```java
    private GovernanceRuleRepository ruleRepository;

    public AbstractRouter(URL url) {
        this.ruleRepository = url.getOrDefaultModuleModel().getExtensionLoader(GovernanceRuleRepository.class).getDefaultExtension();
        this.url = url;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRouter()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/AbstractRouter.java`
#### Snippet
```java
    }

    public AbstractRouter() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractStateRouter()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/AbstractStateRouter.java`
#### Snippet
```java
    protected ModuleModel moduleModel;

    public AbstractStateRouter(URL url) {
        moduleModel = url.getOrDefaultModuleModel();
        this.ruleRepository = moduleModel.getExtensionLoader(GovernanceRuleRepository.class).getDefaultExtension();
```

### NonProtectedConstructorInAbstractClass
Constructor `MeshRuleRouter()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
    protected volatile MeshRuleCache<T> meshRuleCache = MeshRuleCache.emptyCache();

    public MeshRuleRouter(URL url) {
        super(url);
        sourcesLabels = Collections.unmodifiableMap(new HashMap<>(url.getParameters()));
```

### NonProtectedConstructorInAbstractClass
Constructor `ListenableStateRouter()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/ListenableStateRouter.java`
#### Snippet
```java
    private final String ruleKey;

    public ListenableStateRouter(URL url, String ruleKey) {
        super(url);
        this.setForce(false);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConditionMatcher()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/AbstractConditionMatcher.java`
#### Snippet
```java
    protected final String key;

    public AbstractConditionMatcher(String key, ModuleModel model) {
        this.key = key;
        this.model = model;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractClusterInvoker()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java
    private volatile Invoker<T> stickyInvoker = null;

    public AbstractClusterInvoker() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractClusterInvoker()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java
    }

    public AbstractClusterInvoker(Directory<T> directory, URL url) {
        if (directory == null) {
            throw new IllegalArgumentException("service directory == null");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractClusterInvoker()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java
    }

    public AbstractClusterInvoker(Directory<T> directory) {
        this(directory, directory.getUrl());
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDirectory()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
    }

    public AbstractDirectory(URL url, RouterChain<T> routerChain, boolean isUrlFromRegistry) {
        if (url == null) {
            throw new IllegalArgumentException("url == null");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDirectory()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
    private ApplicationModel applicationModel;

    public AbstractDirectory(URL url) {
        this(url, null, false);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDirectory()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
    }

    public AbstractDirectory(URL url, boolean isUrlFromRegistry) {
        this(url, null, isUrlFromRegistry);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConfigurator()` of an abstract class should not be declared 'public'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/AbstractConfigurator.java`
#### Snippet
```java
    private final URL configuratorUrl;

    public AbstractConfigurator(URL url) {
        if (url == null) {
            throw new IllegalArgumentException("configurator url == null");
```

### NonProtectedConstructorInAbstractClass
Constructor `BaseRestClient()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/BaseRestClient.java`
#### Snippet
```java
    protected HttpClientConfig clientConfig;

    public BaseRestClient(HttpClientConfig clientConfig) {
        this.clientConfig = clientConfig;
        client = createHttpClient(clientConfig);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHttpServer()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java
    private volatile boolean closed;

    public AbstractHttpServer(URL url, HttpHandler handler) {
        if (url == null) {
            throw new IllegalArgumentException("url == null");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractProtocol()` of an abstract class should not be declared 'public'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
    protected Map<String, T> resourcesMap = new ConcurrentHashMap<>();

    public AbstractProtocol(AdsObserver adsObserver, Node node, int checkInterval) {
        this.adsObserver = adsObserver;
        this.node = node;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMetricsReporterFactory()` of an abstract class should not be declared 'public'
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporterFactory.java`
#### Snippet
```java
    private final ApplicationModel applicationModel;

    public AbstractMetricsReporterFactory(ApplicationModel applicationModel) {
        this.applicationModel = applicationModel;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractServiceNameMapping()` of an abstract class should not be declared 'public'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractServiceNameMapping.java`
#### Snippet
```java
    private final ConcurrentMap<String, ReentrantLock> mappingLocks = new ConcurrentHashMap<>();

    public AbstractServiceNameMapping(ApplicationModel applicationModel) {
        this.applicationModel = applicationModel;
        boolean enableFileCache = true;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractServiceRestMetadataResolver()` of an abstract class should not be declared 'public'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
    private final Set<NoAnnotatedParameterRequestTagProcessor> noAnnotatedParameterRequestTagProcessors;

    public AbstractServiceRestMetadataResolver(ApplicationModel applicationModel) {
        this.parameterProcessorsMap = loadAnnotatedMethodParameterProcessors(applicationModel);
        this.noAnnotatedParameterRequestTagProcessors = loadNoAnnotatedMethodParameterProcessors(applicationModel);
```

### NonProtectedConstructorInAbstractClass
Constructor `RegistryNotifier()` of an abstract class should not be declared 'public'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryNotifier.java`
#### Snippet
```java
    }

    public RegistryNotifier(URL registryUrl, long delayTime, ScheduledExecutorService scheduler) {
        this.delayTime = delayTime;
        if (scheduler == null) {
```

### NonProtectedConstructorInAbstractClass
Constructor `RegistryNotifier()` of an abstract class should not be declared 'public'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryNotifier.java`
#### Snippet
```java
    private ScheduledExecutorService scheduler;

    public RegistryNotifier(URL registryUrl, long delayTime) {
        this(registryUrl, delayTime, null);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMetadataReport()` of an abstract class should not be declared 'public'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
    private final boolean reportDefinition;

    public AbstractMetadataReport(URL reportServerURL) {
        setUrl(reportServerURL);

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractServiceDiscovery()` of an abstract class should not be declared 'public'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
    }

    public AbstractServiceDiscovery(String serviceName, URL registryURL) {
        this(ApplicationModel.defaultModel(), serviceName, registryURL);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractServiceDiscovery()` of an abstract class should not be declared 'public'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
    protected ApplicationModel applicationModel;

    public AbstractServiceDiscovery(ApplicationModel applicationModel, URL registryURL) {
        this(applicationModel, applicationModel.getApplicationName(), registryURL);
        MetadataReportInstance metadataReportInstance = applicationModel.getBeanFactory().getBean(MetadataReportInstance.class);
```

### NonProtectedConstructorInAbstractClass
Constructor `FailbackRegistry()` of an abstract class should not be declared 'public'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
    private final HashedWheelTimer retryTimer;

    public FailbackRegistry(URL url) {
        super(url);
        this.retryPeriod = url.getParameter(REGISTRY_RETRY_PERIOD_KEY, DEFAULT_REGISTRY_RETRY_PERIOD);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConfiguratorListener()` of an abstract class should not be declared 'public'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/AbstractConfiguratorListener.java`
#### Snippet
```java
    protected ModuleModel moduleModel;

    public AbstractConfiguratorListener(ModuleModel moduleModel) {
        this.moduleModel = moduleModel;

```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicDirectory()` of an abstract class should not be declared 'public'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/DynamicDirectory.java`
#### Snippet
```java


    public DynamicDirectory(Class<T> serviceType, URL url) {
        super(url, true);

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWireProtocol()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/AbstractWireProtocol.java`
#### Snippet
```java
    private final ProtocolDetector detector;

    public AbstractWireProtocol(ProtocolDetector detector) {
        this.detector = detector;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPortUnificationServer()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/AbstractPortUnificationServer.java`
#### Snippet
```java
    private final Map<String, ChannelHandler> supportedHandlers = new ConcurrentHashMap<>();

    public AbstractPortUnificationServer(URL url, ChannelHandler handler) throws RemotingException {
        super(url, handler);
        this.protocols = url.getOrDefaultFrameworkModel().getExtensionLoader(WireProtocol.class).getActivateExtension(url, new String[0]);
```

### NonProtectedConstructorInAbstractClass
Constructor `ExchangeHandlerAdapter()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/ExchangeHandlerAdapter.java`
#### Snippet
```java
 */
public abstract class ExchangeHandlerAdapter extends TelnetHandlerAdapter implements ExchangeHandler {
    public ExchangeHandlerAdapter(FrameworkModel frameworkModel) {
        super(frameworkModel);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChannel()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractChannel.java`
#### Snippet
```java
public abstract class AbstractChannel extends AbstractPeer implements Channel {

    public AbstractChannel(URL url, ChannelHandler handler) {
        super(url, handler);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractEndpoint()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractEndpoint.java`
#### Snippet
```java
    private int connectTimeout;

    public AbstractEndpoint(URL url, ChannelHandler handler) {
        super(url, handler);
        this.codec = getChannelCodec(url);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPeer()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractPeer.java`
#### Snippet
```java
    private volatile boolean closed;

    public AbstractPeer(URL url, ChannelHandler handler) {
        if (url == null) {
            throw new IllegalArgumentException("url == null");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractServer()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractServer.java`
#### Snippet
```java
    private ExecutorRepository executorRepository;

    public AbstractServer(URL url, ChannelHandler handler) throws RemotingException {
        super(url, handler);
        executorRepository = ExecutorRepository.getInstance(url.getOrDefaultApplicationModel());
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractClient()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
    protected volatile ExecutorService executor;

    public AbstractClient(URL url, ChannelHandler handler) throws RemotingException {
        super(url, handler);
        // set default needReconnect true when channel is not connected
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractZookeeperClient()` of an abstract class should not be declared 'public'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperClient.java`
#### Snippet
```java
    private final Set<String> persistentExistNodePath = new ConcurrentHashSet<>();

    public AbstractZookeeperClient(URL url) {
        this.url = url;
    }
```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
                throw t;
            }
        } catch (RuntimeException e) {
            throw e;
        } catch (NotFoundException | CannotCompileException e) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ChannelWrappedInvoker.java`
#### Snippet
```java
                return new AsyncRpcResult(appResponseFuture, inv);
            }
        } catch (RpcException e) {
            throw e;
        } catch (TimeoutException e) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/filter/ValidationFilter.java`
#### Snippet
```java
                    validator.validate(invocation.getMethodName(), invocation.getParameterTypes(), invocation.getArguments());
                }
            } catch (RpcException e) {
                throw e;
            } catch (Throwable t) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
            }

        } catch (RemotingException e) {
            throw e;

```

## RuleId[id=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `stub`
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
                    .trim());

            stub = stub.withInterceptors(newAttachHeadersInterceptor(header));
            logger.info("Use oidc token from " + oidcTokenPath + " to connect to Dubbo Certificate Authority.");
        } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `keyValues`
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DefaultDubboClientObservationConvention.java`
#### Snippet
```java

    private KeyValues withRemoteHostPort(KeyValues keyValues, String remoteHostName, int remotePort) {
        keyValues = appendNonNull(keyValues, NET_PEER_NAME, remoteHostName);
        if (remotePort == 0) {
            return keyValues;
```

### AssignmentToMethodParameter
Assignment to method parameter `arguments`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/RunArguments.java`
#### Snippet
```java
		}
		try {
			arguments = arguments.replace('\n', ' ').replace('\t', ' ');
			return CommandLineUtils.translateCommandline(arguments);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `max`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java
     */
    public static boolean beginCount(URL url, String methodName, int max) {
        max = (max <= 0) ? Integer.MAX_VALUE : max;
        RpcStatus appStatus = getStatus(url);
        RpcStatus methodStatus = getStatus(url, methodName);
```

### AssignmentToMethodParameter
Assignment to method parameter `port`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    public RpcServiceContext setRemoteAddress(String host, int port) {
        if (port < 0) {
            port = 0;
        }
        this.remoteAddress = InetSocketAddress.createUnresolved(host, port);
```

### AssignmentToMethodParameter
Assignment to method parameter `port`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    public RpcServiceContext setLocalAddress(String host, int port) {
        if (port < 0) {
            port = 0;
        }
        this.localAddress = InetSocketAddress.createUnresolved(host, port);
```

### AssignmentToMethodParameter
Assignment to method parameter `interfaces`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/bytebuddy/ByteBuddyProxy.java`
#### Snippet
```java
            throw new IllegalArgumentException("interface limit exceeded");
        }
        interfaces = interfaces.clone();
        Arrays.sort(interfaces, Comparator.comparing(Class::getName));
        CacheKey key = new CacheKey(interfaces);
```

### AssignmentToMethodParameter
Assignment to method parameter `mock`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
        }

        mock = mock.trim();

        if (mock.length() == 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `mock`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java

        if (mock.startsWith(FAIL_PREFIX)) {
            mock = mock.substring(FAIL_PREFIX.length()).trim();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `mock`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java

        if (mock.startsWith(FORCE_PREFIX)) {
            mock = mock.substring(FORCE_PREFIX.length()).trim();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `mock`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java

        if (mock.startsWith(RETURN_PREFIX) || mock.startsWith(THROW_PREFIX)) {
            mock = mock.replace('`', '"');
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `mockService`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
        boolean isDefault = ConfigUtils.isDefault(mockService);
        if (isDefault) {
            mockService = serviceType.getName() + "Mock";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `result`
in `dubbo-metrics/dubbo-metrics-prometheus/src/main/java/org/apache/dubbo/metrics/prometheus/PrometheusMetricsReporterCmd.java`
#### Snippet
```java
            // Contains at least one line "text/plain; version=0.0.4; charset=utf-8"
            if (getLineNumber(current) > 1) {
                result = current;
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `specPort`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/InternalServiceConfigBuilder.java`
#### Snippet
```java
            String rawPort = getApplicationConfig().getParameters().get(key);
            if (StringUtils.isNotEmpty(rawPort)) {
                specPort = Integer.parseInt(rawPort);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `protocol`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/InternalServiceConfigBuilder.java`
#### Snippet
```java

            if (CollectionUtils.isNotEmptyMap(params)) {
                protocol = params.get(key);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `protocol`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java

    private static void putRandomPort(String protocol, Integer port) {
        protocol = protocol.toLowerCase();
        if (!RANDOM_PORT_MAP.containsKey(protocol)) {
            RANDOM_PORT_MAP.put(protocol, port);
```

### AssignmentToMethodParameter
Assignment to method parameter `protocol`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java

    private static Integer getRandomPort(String protocol) {
        protocol = protocol.toLowerCase();
        return RANDOM_PORT_MAP.getOrDefault(protocol, Integer.MIN_VALUE);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
                if (StringUtils.isNotBlank(extProtocol)) {
                    // export original url
                    url = URLBuilder.from(url).
                        addParameter(IS_PU_SERVER_KEY, Boolean.TRUE.toString()).
                        removeParameter("ext.protocol").
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
                }

                url = exportRemote(url, registryURLs);
                if (!isGeneric(generic) && !getScopeModel().isInternal()) {
                    MetadataUtils.publishServiceDefinition(url, providerModel.getServiceModel(), getApplicationModel());
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
            for (URL registryURL : registryURLs) {
                if (SERVICE_REGISTRY_PROTOCOL.equals(registryURL.getProtocol())) {
                    url = url.addParameterIfAbsent(SERVICE_NAME_MAPPING_KEY, "true");
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
                }

                url = url.addParameterIfAbsent(DYNAMIC_KEY, registryURL.getParameter(DYNAMIC_KEY));
                URL monitorUrl = ConfigValidationUtils.loadMonitor(this, registryURL);
                if (monitorUrl != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
                URL monitorUrl = ConfigValidationUtils.loadMonitor(this, registryURL);
                if (monitorUrl != null) {
                    url = url.putAttribute(MONITOR_KEY, monitorUrl);
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `parameters`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractBuilder.java`
#### Snippet
```java
    protected static Map<String, String> appendParameter(Map<String, String> parameters, String key, String value) {
        if (parameters == null) {
            parameters = new HashMap<>();
        }
        parameters.put(key, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `parameters`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractBuilder.java`
#### Snippet
```java
    protected static Map<String, String> appendParameters(Map<String, String> parameters, Map<String, String> appendParameters) {
        if (parameters == null) {
            parameters = new HashMap<>();
        }
        parameters.putAll(appendParameters);
```

### AssignmentToMethodParameter
Assignment to method parameter `apackage`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServicePackagesHolder.java`
#### Snippet
```java

    public void addScannedPackage(String apackage) {
        apackage = normalizePackage(apackage);
        synchronized (scannedPackages) {
            scannedPackages.add(apackage);
```

### AssignmentToMethodParameter
Assignment to method parameter `apackage`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServicePackagesHolder.java`
#### Snippet
```java
    private String normalizePackage(String apackage) {
        if (!apackage.endsWith(".")) {
            apackage += ".";
        }
        return apackage;
```

### AssignmentToMethodParameter
Assignment to method parameter `packageName`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServicePackagesHolder.java`
#### Snippet
```java

    public boolean isPackageScanned(String packageName) {
        packageName = normalizePackage(packageName);
        synchronized (scannedPackages) {
            if (scannedPackages.contains(packageName)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                || "reliable".equals(property) && "false".equals(value)) {
            // backward compatibility for the default value in old version's xsd
            value = null;
        }
        return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
        if (ReferenceAttributes.PARAMETERS.equals(key) && obj instanceof String[]) {
            //convert parameters array pairs to map
            obj = DubboAnnotationUtils.convertParameters((String[]) obj);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/cache/support/AbstractCacheFactory.java`
#### Snippet
```java
    @Override
    public Cache getCache(URL url, Invocation invocation) {
        url = url.addParameter(METHOD_KEY, invocation.getMethodName());
        String key = url.toFullString();
        Cache cache = caches.get(key);
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
                if (t != null) {
                    if (t instanceof CompletionException) {
                        t = t.getCause();
                    }
                    callback.caught(t);
```

### AssignmentToMethodParameter
Assignment to method parameter `uptime`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/SystemPageHandler.java`
#### Snippet
```java
            buf.append(days);
            buf.append(" Days");
            uptime = uptime % DAY;
        }
        if (uptime > HOUR) {
```

### AssignmentToMethodParameter
Assignment to method parameter `uptime`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/SystemPageHandler.java`
#### Snippet
```java
            buf.append(hours);
            buf.append(" Hours");
            uptime = uptime % HOUR;
        }
        if (uptime > MINUTE) {
```

### AssignmentToMethodParameter
Assignment to method parameter `uptime`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/SystemPageHandler.java`
#### Snippet
```java
            buf.append(minutes);
            buf.append(" Minutes");
            uptime = uptime % MINUTE;
        }
        if (uptime > SECOND) {
```

### AssignmentToMethodParameter
Assignment to method parameter `uptime`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/SystemPageHandler.java`
#### Snippet
```java
            buf.append(seconds);
            buf.append(" Seconds");
            uptime = uptime % SECOND;
        }
        if (uptime > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `serviceGroup`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/support/ProtocolUtils.java`
#### Snippet
```java

    public static String serviceKey(int port, String serviceName, String serviceVersion, String serviceGroup) {
        serviceGroup = serviceGroup == null ? "" : serviceGroup;
        GroupServiceKeyCache groupServiceKeyCache = groupServiceKeyCacheMap.get(serviceGroup);
        if (groupServiceKeyCache == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `serviceVersion`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/support/GroupServiceKeyCache.java`
#### Snippet
```java
        }

        serviceVersion = serviceVersion == null ? "" : serviceVersion;
        ConcurrentMap<Integer, String> portMap = versionMap.get(serviceVersion);
        if (portMap == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `file`
in `dubbo-common/src/main/java/org/apache/dubbo/common/Version.java`
#### Snippet
```java
    private static String getFromFile(String file) {
        // remove suffix ".jar": "path/to/group-module-x.y.z"
        file = file.substring(0, file.length() - 4);

        // remove path: "group-module-x.y.z"
```

### AssignmentToMethodParameter
Assignment to method parameter `file`
in `dubbo-common/src/main/java/org/apache/dubbo/common/Version.java`
#### Snippet
```java
        int i = file.lastIndexOf('/');
        if (i >= 0) {
            file = file.substring(i + 1);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `file`
in `dubbo-common/src/main/java/org/apache/dubbo/common/Version.java`
#### Snippet
```java
        i = file.indexOf("-");
        if (i >= 0) {
            file = file.substring(i + 1);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `file`
in `dubbo-common/src/main/java/org/apache/dubbo/common/Version.java`
#### Snippet
```java
            i = file.indexOf("-");
            if (i >= 0) {
                file = file.substring(i + 1);
            } else {
                break;
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java
    public long skip(long len) {
        if (mPosition + len > mLimit) {
            len = mLimit - mPosition;
        }
        if (len <= 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java
        }
        if (mPosition + len > mLimit) {
            len = mLimit - mPosition;
        }
        if (len <= 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java

                if (mPosition + len > mLimit) {
                    len = mLimit - mPosition;
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            public long skip(long len) throws IOException {
                if (mPosition + len > mLimit) {
                    len = mLimit - mPosition;
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `decodeStr`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLAddress.java`
#### Snippet
```java
            }
            protocol = decodeStr.substring(0, i);
            decodeStr = decodeStr.substring(i + 3);
        } else {
            // case: file:/path/to/file.txt
```

### AssignmentToMethodParameter
Assignment to method parameter `decodeStr`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLAddress.java`
#### Snippet
```java
                }
                protocol = decodeStr.substring(0, i);
                decodeStr = decodeStr.substring(i + 1);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `decodeStr`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLAddress.java`
#### Snippet
```java
        if (i >= 0) {
            path = decodeStr.substring(i + 1);
            decodeStr = decodeStr.substring(0, i);
        }
        i = decodeStr.lastIndexOf('@');
```

### AssignmentToMethodParameter
Assignment to method parameter `decodeStr`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLAddress.java`
#### Snippet
```java
                username = username.substring(0, j);
            }
            decodeStr = decodeStr.substring(i + 1);
        }
        i = decodeStr.lastIndexOf(':');
```

### AssignmentToMethodParameter
Assignment to method parameter `port`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLAddress.java`
#### Snippet
```java
    public URLAddress(String host, int port, String rawAddress) {
        this.host = host;
        port = Math.max(port, 0);
        this.port = port;

```

### AssignmentToMethodParameter
Assignment to method parameter `from`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java

    private static int indexOf(String str, char ch, int from, int toExclude) {
        from = Math.max(from, 0);
        toExclude = Math.min(toExclude, str.length());
        if (from > toExclude) {
```

### AssignmentToMethodParameter
Assignment to method parameter `toExclude`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
    private static int indexOf(String str, char ch, int from, int toExclude) {
        from = Math.max(from, 0);
        toExclude = Math.min(toExclude, str.length());
        if (from > toExclude) {
            return -1;
```

### AssignmentToMethodParameter
Assignment to method parameter `valueStart`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java

        if (valueStart <= nameStart) {
            valueStart = valueEnd + 1;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `pathEndIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
        int paramStartIdx = pathEndIdx + 3;//skip ENCODED_QUESTION_MARK
        if (pathEndIdx == -1) {
            pathEndIdx = rawURLStr.indexOf('?');
            if (pathEndIdx == -1) {
                // url with no params, decode anyway
```

### AssignmentToMethodParameter
Assignment to method parameter `rawURLStr`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
            if (pathEndIdx == -1) {
                // url with no params, decode anyway
                rawURLStr = URL.decode(rawURLStr);
            } else {
                paramStartIdx = pathEndIdx + 1;
```

### AssignmentToMethodParameter
Assignment to method parameter `charBufIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
            char c = str.charAt(i);
            if (c != '%') {
                charBuf[charBufIdx++] = c != '+' || isPath ? c : SPACE;
                continue;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `charBufIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
            i--;

            charBufIdx += decodeUtf8(buf, 0, bufIdx, charBuf, charBufIdx);
        }
        return new String(charBuf, 0, charBufIdx);
```

### AssignmentToMethodParameter
Assignment to method parameter `from`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java

    private static int lastIndexOf(String str, char ch, int from, int toExclude) {
        from = Math.max(from, 0);
        toExclude = Math.min(toExclude, str.length() - 1);
        if (from > toExclude) {
```

### AssignmentToMethodParameter
Assignment to method parameter `toExclude`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
    private static int lastIndexOf(String str, char ch, int from, int toExclude) {
        from = Math.max(from, 0);
        toExclude = Math.min(toExclude, str.length() - 1);
        if (from > toExclude) {
            return -1;
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/PathURLAddress.java`
#### Snippet
```java
        // trim the beginning "/"
        while (path != null && path.startsWith("/")) {
            path = path.substring(1);
        }
        if (path != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/PathURLAddress.java`
#### Snippet
```java
        }
        if (path != null) {
            path = path.intern();
        }
        this.path = path;
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLItemCache.java`
#### Snippet
```java
            return;
        }
        key = key.intern();
        value = value.intern();
        params.put(key, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLItemCache.java`
#### Snippet
```java
        }
        key = key.intern();
        value = value.intern();
        params.put(key, value);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `basePath`
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
        if (basePath == null) {
            // default case: ~/.dubbo
            basePath = System.getProperty("user.home") + File.separator + ".dubbo";
        }
        if (basePath.endsWith(File.separator)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `basePath`
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
        }
        if (basePath.endsWith(File.separator)) {
            basePath = basePath.substring(0, basePath.length() - 1);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `cacheName`
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
        }

        cacheName = safeName(cacheName);
        if (!cacheName.endsWith(SUFFIX)) {
            cacheName = cacheName + SUFFIX;
```

### AssignmentToMethodParameter
Assignment to method parameter `cacheName`
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
        cacheName = safeName(cacheName);
        if (!cacheName.endsWith(SUFFIX)) {
            cacheName = cacheName + SUFFIX;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `destIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Utf8Utils.java`
#### Snippet
```java
            }
            offset++;
            DecodeUtil.handleOneByteSafe(b, destChars, destIdx++);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `destIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Utf8Utils.java`
#### Snippet
```java
            byte byte1 = srcBytes[offset++];
            if (DecodeUtil.isOneByte(byte1)) {
                DecodeUtil.handleOneByteSafe(byte1, destChars, destIdx++);
                // It's common for there to be multiple ASCII characters in a run mixed in, so add an
                // extra optimized loop to take care of these runs.
```

### AssignmentToMethodParameter
Assignment to method parameter `destIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Utf8Utils.java`
#### Snippet
```java
                    }
                    offset++;
                    DecodeUtil.handleOneByteSafe(b, destChars, destIdx++);
                }
            } else if (DecodeUtil.isTwoBytes(byte1)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `destIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Utf8Utils.java`
#### Snippet
```java
                    throw new IllegalArgumentException("invalid UTF-8.");
                }
                DecodeUtil.handleTwoBytesSafe(byte1, /* byte2 */ srcBytes[offset++], destChars, destIdx++);
            } else if (DecodeUtil.isThreeBytes(byte1)) {
                if (offset >= limit - 1) {
```

### AssignmentToMethodParameter
Assignment to method parameter `destIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Utf8Utils.java`
#### Snippet
```java
                        /* byte3 */ srcBytes[offset++],
                        destChars,
                        destIdx++);
            } else {
                if (offset >= limit - 2) {
```

### AssignmentToMethodParameter
Assignment to method parameter `destIdx`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Utf8Utils.java`
#### Snippet
```java
                        destChars,
                        destIdx);
                destIdx += 2;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
        Class<?> beanClass = bean.getClass();
        if (name == null) {
            name = beanClass.getName() + "#" + getNextId(beanClass);
        }
        initializeBean(name, bean);
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ExecutorUtil.java`
#### Snippet
```java
        String name = url.getParameter(THREAD_NAME_KEY, defaultName);
        name = name + "-" + url.getAddress();
        url = url.addParameter(THREAD_NAME_KEY, name);
        return url;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `ticksPerWheel`
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java
        }

        ticksPerWheel = normalizeTicksPerWheel(ticksPerWheel);
        HashedWheelBucket[] wheel = new HashedWheelBucket[ticksPerWheel];
        for (int i = 0; i < wheel.length; i++) {
```

### AssignmentToMethodParameter
Assignment to method parameter `set`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
        }
        if (!(set instanceof TreeSet)) {
            set = new TreeSet<>(set);
        }
        return set;
```

### AssignmentToMethodParameter
Assignment to method parameter `serviceKey`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
        if (i > 0) {
            arr[0] = serviceKey.substring(0, i);
            serviceKey = serviceKey.substring(i + 1);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `serviceKey`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
        if (j > 0) {
            arr[2] = serviceKey.substring(j + 1);
            serviceKey = serviceKey.substring(0, j);
        }
        arr[1] = serviceKey;
```

### AssignmentToMethodParameter
Assignment to method parameter `pattern`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
    public static boolean isMatchGlobPattern(String pattern, String value, URL param) {
        if (param != null && pattern.startsWith("$")) {
            pattern = param.getRawParameter(pattern.substring(1));
        }
        return isMatchGlobPattern(pattern, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
     */
    public static URL valueOf(String url) {
        if (url == null || (url = url.trim()).length() == 0) {
            throw new IllegalArgumentException("url == null");
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                }
            }
            url = url.substring(0, i);
        }
        i = url.indexOf("://");
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
            }
            protocol = url.substring(0, i);
            url = url.substring(i + 3);
        } else {
            // case: file:/path/to/file.txt
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                }
                protocol = url.substring(0, i);
                url = url.substring(i + 1);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
        if (i >= 0) {
            path = url.substring(i + 1);
            url = url.substring(0, i);
        }
        i = url.lastIndexOf('@');
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                username = username.substring(0, j);
            }
            url = url.substring(i + 1);
        }
        i = url.lastIndexOf(':');
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
            } else {
                port = Integer.parseInt(url.substring(i + 1));
                url = url.substring(0, i);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `service`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
        if (i > 0) {
            group = service.substring(0, i);
            service = service.substring(i + 1);
        }
        i = service.lastIndexOf(':');
```

### AssignmentToMethodParameter
Assignment to method parameter `service`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
        if (i > 0) {
            version = service.substring(i + 1);
            service = service.substring(0, i);
        }
        return URL.valueOf(EMPTY_PROTOCOL + "://0.0.0.0/" + service + URL_PARAM_STARTING_SYMBOL
```

### AssignmentToMethodParameter
Assignment to method parameter `startIndex`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ArrayUtils.java`
#### Snippet
```java
        } else {
            if (startIndex < 0) {
                startIndex = 0;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                if (!CLASS_NOT_FOUND_CACHE.containsKey(className)) {
                    try {
                        type = DefaultSerializeClassChecker.getInstance().loadClass(ClassUtils.getClassLoader(), (String) className);
                    } catch (ClassNotFoundException e) {
                        CLASS_NOT_FOUND_CACHE.put((String) className, NOT_FOUND_VALUE);
```

### AssignmentToMethodParameter
Assignment to method parameter `port`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
            try (ServerSocket ignored = new ServerSocket(i)) {
                USED_PORT.set(i);
                port = i;
                break;
            } catch (IOException e) {
```

### AssignmentToMethodParameter
Assignment to method parameter `pattern`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
            throw new IllegalArgumentException("Illegal Argument pattern or hostName. Pattern:" + pattern + ", Host:" + host);
        }
        pattern = pattern.trim();
        if ("*.*.*.*".equals(pattern) || "*".equals(pattern)) {
            return true;
```

### AssignmentToMethodParameter
Assignment to method parameter `pattern`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
            return false;
        }
        pattern = hostAndPort[0];

        String splitCharacter = SPLIT_IPV4_CHARACTER;
```

### AssignmentToMethodParameter
Assignment to method parameter `host`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        checkHostPattern(pattern, mask, isIpv4);

        host = inetAddress.getHostAddress();
        if (pattern.equals(host)) {
            return true;
```

### AssignmentToMethodParameter
Assignment to method parameter `ip`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        if ((ip.charAt(0) == '[' && (ind = ip.indexOf(']')) > 2)) {
            String nhost = ip;
            ip = nhost.substring(0, ind + 1);
            ip = ip.substring(1, ind);
            return ip;
```

### AssignmentToMethodParameter
Assignment to method parameter `ip`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
            String nhost = ip;
            ip = nhost.substring(0, ind + 1);
            ip = ip.substring(1, ind);
            return ip;
        } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `address`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
            int i = address.indexOf(':');
            if (i > -1) {
                address = address.substring(0, i);
            }
            String hostname = HOST_NAME_CACHE.get(address);
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    public static String toOSStyleKey(String key) {
        key = key.toUpperCase().replaceAll(DOT_REGEX, UNDERLINE_SEPARATOR);
        if (!key.startsWith("DUBBO_")) {
            key = "DUBBO_" + key;
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
        key = key.toUpperCase().replaceAll(DOT_REGEX, UNDERLINE_SEPARATOR);
        if (!key.startsWith("DUBBO_")) {
            key = "DUBBO_" + key;
        }
        return key;
```

### AssignmentToMethodParameter
Assignment to method parameter `max`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
            buf.append(text, start, end).append(replacement);
            start = end + replLength;
            if (--max == 0) {
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        if (index > 0) {
            c = (name.length() - index) / 2;
            name = name.substring(0, index);
        }
        while (c-- > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `cls`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static boolean isPrimitives(Class<?> cls) {
        while (cls.isArray()) {
            cls = cls.getComponentType();
        }
        return isPrimitive(cls);
```

### AssignmentToMethodParameter
Assignment to method parameter `cl`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static Map<String, Method> getBeanPropertyReadMethods(Class cl) {
        Map<String, Method> properties = new HashMap<String, Method>();
        for (; cl != null; cl = cl.getSuperclass()) {
            Method[] methods = cl.getDeclaredMethods();
            for (Method method : methods) {
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            do {
                sb.append("[]");
                c = c.getComponentType();
            }
            while (c.isArray());
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java

        if (pt) {
            c = getBoxedClass(c);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        while (c.isArray()) {
            ret.append('[');
            c = c.getComponentType();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `cl`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static Map<String, Field> getBeanPropertyFields(Class cl) {
        Map<String, Field> properties = new HashMap<String, Field>();
        for (; cl != null; cl = cl.getSuperclass()) {
            Field[] fields = cl.getDeclaredFields();
            for (Field field : fields) {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        if (index > 0) {
            c = (name.length() - index) / 2;
            name = name.substring(0, index);
        }
        if (c > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                sb.append('L').append(name).append(';');
            }
            name = sb.toString();
        } else {
            if ("void".equals(name)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `cl`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java

        if (cl == null) {
            cl = ClassUtils.getClassLoader();
        }
        return Class.forName(name, true, cl);
```

### AssignmentToMethodParameter
Assignment to method parameter `desc`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            case 'L':
                // "Ljava/lang/Object;" ==> "java.lang.Object"
                desc = desc.substring(1, desc.length() - 1).replace('/', '.');
                break;
            case '[':
```

### AssignmentToMethodParameter
Assignment to method parameter `desc`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            case '[':
                // "[[Ljava/lang/Object;" ==> "[[Ljava.lang.Object;"
                desc = desc.replace('/', '.');
                break;
            default:
```

### AssignmentToMethodParameter
Assignment to method parameter `cl`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java

        if (cl == null) {
            cl = ClassUtils.getClassLoader();
        }
        return Class.forName(desc, true, cl);
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static Class<?> getBoxedClass(Class<?> c) {
        if (c == int.class) {
            c = Integer.class;
        } else if (c == boolean.class) {
            c = Boolean.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            c = Integer.class;
        } else if (c == boolean.class) {
            c = Boolean.class;
        } else if (c == long.class) {
            c = Long.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            c = Boolean.class;
        } else if (c == long.class) {
            c = Long.class;
        } else if (c == float.class) {
            c = Float.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            c = Long.class;
        } else if (c == float.class) {
            c = Float.class;
        } else if (c == double.class) {
            c = Double.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            c = Float.class;
        } else if (c == double.class) {
            c = Double.class;
        } else if (c == char.class) {
            c = Character.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            c = Double.class;
        } else if (c == char.class) {
            c = Character.class;
        } else if (c == byte.class) {
            c = Byte.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            c = Character.class;
        } else if (c == byte.class) {
            c = Byte.class;
        } else if (c == short.class) {
            c = Short.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            c = Byte.class;
        } else if (c == short.class) {
            c = Short.class;
        }
        return c;
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    public static <V extends Object> Set<String> getSubIds(Collection<Map<String, V>> configMaps, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
        }
        Set<String> ids = new LinkedHashSet<>();
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    public static <V extends Object> boolean hasSubProperties(Collection<Map<String, V>> configMaps, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
        }
        for (Map<String, V> configMap : configMaps) {
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    public static <V extends Object> boolean hasSubProperties(Map<String, V> configMap, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
        }
        Map<String, V> copy;
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    private static <V extends Object> Map<String, V> getSubProperties(Map<String, V> configMap, String prefix, Map<String, V> resultMap) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `resultMap`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java

        if (null == resultMap) {
            resultMap = new LinkedHashMap<>();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `code`
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/AbstractCompiler.java`
#### Snippet
```java
    @Override
    public Class<?> compile(Class<?> neighbor, String code, ClassLoader classLoader) {
        code = code.trim();
        Matcher matcher = PACKAGE_PATTERN.matcher(code);
        String pkg;
```

### AssignmentToMethodParameter
Assignment to method parameter `classLoader`
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
        ClassPool cp = cls.getClassPool();
        if (classLoader == null) {
            classLoader = cp.getClassLoader();
        }
        cp.insertClassPath(new LoaderClassPath(classLoader));
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
        while (ClassGenerator.isDynamicClass(c)) // can not wrapper on dynamic class.
        {
            c = c.getSuperclass();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
            while (isArray(name)) {
                ++dimension;
                name = name.substring(1);
            }
            Class type = name2Class(loader, name);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
        }
        if (isReferenceType(name)) {
            name = name.substring(1, name.length() - 1);
        }
        return DefaultSerializeClassChecker.getInstance().loadClass(loader, name);
```

### AssignmentToMethodParameter
Assignment to method parameter `runnable`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/ThreadlessExecutor.java`
#### Snippet
```java
    @Override
    public void execute(Runnable runnable) {
        runnable = new RunnableWrapper(runnable);
        synchronized (lock) {
            if (!isWaiting()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/IsolationExecutorRepository.java`
#### Snippet
```java
    protected URL setThreadNameIfAbsent(URL url, String executorCacheKey) {
        if (url.getParameter(THREAD_NAME_KEY) == null) {
            url = url.putAttribute(THREAD_NAME_KEY, "isolation-" + executorCacheKey);
        }
        return url;
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
        String executorCacheKey = getExecutorSecondKey(url);

        url = setThreadNameIfAbsent(url, executorCacheKey);

        URL finalUrl = url;
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                protocol = DEFAULT_PROTOCOL;
            }
            url = url.putAttribute(THREAD_NAME_KEY, protocol + "-protocol-" + executorCacheKey);
        }
        return url;
```

### AssignmentToMethodParameter
Assignment to method parameter `instance`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        if (extensionPostProcessors != null) {
            for (ExtensionPostProcessor processor : extensionPostProcessors) {
                instance = (T) processor.postProcessBeforeInitialization(instance, name);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `instance`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        if (extensionPostProcessors != null) {
            for (ExtensionPostProcessor processor : extensionPostProcessors) {
                instance = (T) processor.postProcessAfterInitialization(instance, name);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        } else {
            if (StringUtils.isEmpty(name)) {
                name = findAnnotationName(clazz);
                if (name.length() == 0) {
                    throw new IllegalStateException(
```

### AssignmentToMethodParameter
Assignment to method parameter `packageName`
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/ClassFinder.java`
#### Snippet
```java

    public Set<String> findClassSet(String packageName, Consumer<String> consumer) {
        packageName = packageName.replace(".", "/");
        try {
            ClassLoader classLoader = Thread.currentThread().getContextClassLoader();
```

### AssignmentToMethodParameter
Assignment to method parameter `group`
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
            long nacosTimeout = timeout < 0 ? getDefaultTimeout() : timeout;
            if (StringUtils.isEmpty(group)) {
                group = DEFAULT_GROUP;
            }
            return configService.getConfig(key, group, nacosTimeout);
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/ServerStreamServerCallListener.java`
#### Snippet
```java
    public void onMessage(Object message) {
        if (message instanceof Object[]) {
            message = ((Object[]) message)[0];
        }
        invocation.setArguments(new Object[]{message, responseObserver});
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/BiStreamServerCallListener.java`
#### Snippet
```java
    public void onMessage(Object message) {
        if (message instanceof Object[]) {
            message = ((Object[]) message)[0];
        }
        requestObserver.onNext(message);
```

### AssignmentToMethodParameter
Assignment to method parameter `val`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleCustomerProtocolWapper.java`
#### Snippet
```java
        for (int i = 0; i < data.length - 1; i++) {
            data[i] = (byte) ((val & 0x7F) | 0x80);
            val = val >>> 7;
        }
        data[data.length - 1] = (byte) (val);
```

### AssignmentToMethodParameter
Assignment to method parameter `val`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleCustomerProtocolWapper.java`
#### Snippet
```java
        int length = 0;
        while (val != 0) {
            val = val >>> 7;
            length++;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `allocated`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TriHttp2RemoteFlowController.java`
#### Snippet
```java
                    } finally {
                        // Decrement allocated by how much was actually written.
                        allocated -= initialFrameSize - frame.size();
                    }
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `clazz`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ReflectionPackableMethod.java`
#### Snippet
```java
                }
            }
            clazz = clazz.getSuperclass();
        }
        return false;
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ReflectionPackableMethod.java`
#### Snippet
```java
        public byte[] pack(Object obj) throws IOException {
            if (!singleArgument) {
                obj = ((Object[]) obj)[0];
            }
            return PB_PACK.pack(obj);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Cluster.java`
#### Snippet
```java
    static Cluster getCluster(ScopeModel scopeModel, String name, boolean wrap) {
        if (StringUtils.isEmpty(name)) {
            name = Cluster.DEFAULT;
        }
        return ScopeModelUtil.getApplicationModel(scopeModel).getExtensionLoader(Cluster.class).getExtension(name, wrap);
```

### AssignmentToMethodParameter
Assignment to method parameter `needToPrintMessage`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/AbstractStateRouter.java`
#### Snippet
```java
    public final BitList<Invoker<T>> route(BitList<Invoker<T>> invokers, URL url, Invocation invocation, boolean needToPrintMessage, Holder<RouterSnapshotNode<T>> nodeHolder) throws RpcException {
        if (needToPrintMessage && (nodeHolder == null || nodeHolder.get() == null)) {
            needToPrintMessage = false;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `dubboDestination`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
                break;
            }
            dubboDestination = dubboRouteDestination.getDestination();

            if (dubboDestination == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `invokers`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/script/config/AppScriptStateRouter.java`
#### Snippet
```java
        }

        invokers = scriptRouter.route(invokers, url, invocation, needToPrintMessage, routerSnapshotNodeHolder);

        if (needToPrintMessage) {
```

### AssignmentToMethodParameter
Assignment to method parameter `invokers`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/ListenableStateRouter.java`
#### Snippet
```java
        }
        for (AbstractStateRouter<T> router : conditionRouters) {
            invokers = router.route(invokers, url, invocation, needToPrintMessage, nodeHolder);
            if (needToPrintMessage) {
                resultMessage.append(messageHolder.get());
```

### AssignmentToMethodParameter
Assignment to method parameter `rule`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/ConditionStateRouter.java`
#### Snippet
```java
                throw new IllegalArgumentException("Illegal route rule!");
            }
            rule = rule.replace("consumer.", "").replace("provider.", "");
            int i = rule.indexOf("=>");
            String whenRule = i < 0 ? null : rule.substring(0, i).trim();
```

### AssignmentToMethodParameter
Assignment to method parameter `last`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/AbstractCluster.java`
#### Snippet
```java
                final ClusterInterceptor interceptor = interceptors.get(i);
                final AbstractClusterInvoker<T> next = last;
                last = new InterceptorInvokerNode<>(clusterInvoker, interceptor, next);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `routers`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java

    protected void addRouters(List<Router> routers) {
        routers = routers == null ? Collections.emptyList() : routers;
        routerChain.addRouters(routers);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/AbstractConfigurator.java`
#### Snippet
```java
            String configuratorSide = configuratorUrl.getSide();
            if (currentSide.equals(configuratorSide) && CONSUMER.equals(configuratorSide)) {
                url = configureIfMatch(NetUtils.getLocalHost(), url);
            } else if (currentSide.equals(configuratorSide) && PROVIDER.equals(configuratorSide)) {
                url = configureIfMatch(url.getHost(), url);
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/AbstractConfigurator.java`
#### Snippet
```java
                url = configureIfMatch(NetUtils.getLocalHost(), url);
            } else if (currentSide.equals(configuratorSide) && PROVIDER.equals(configuratorSide)) {
                url = configureIfMatch(url.getHost(), url);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/AbstractConfigurator.java`
#### Snippet
```java
         */
        else {
            url = configureDeprecated(url);
        }
        return url;
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistryFactory.java`
#### Snippet
```java
    protected String createRegistryCacheKey(URL url) {
        String namespace = url.getParameter(CONFIG_NAMESPACE_KEY);
        url = URL.valueOf(url.toServiceStringWithoutResolving());
        if (StringUtils.isNotEmpty(namespace)) {
            url = url.addParameter(CONFIG_NAMESPACE_KEY, namespace);
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistryFactory.java`
#### Snippet
```java
        url = URL.valueOf(url.toServiceStringWithoutResolving());
        if (StringUtils.isNotEmpty(namespace)) {
            url = url.addParameter(CONFIG_NAMESPACE_KEY, namespace);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/parser/ConfigParser.java`
#### Snippet
```java
    private static URL appendMatchCondition(URL url, ConfigItem item) {
        if (item.getMatch() != null) {
            url = url.putAttribute(MATCH_CONDITION, item.getMatch());
        }
        return url;
```

### AssignmentToMethodParameter
Assignment to method parameter `msg`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/jetty/JettyLoggerAdapter.java`
#### Snippet
```java

    private String format(String msg, Object... args) {
        msg = String.valueOf(msg); // Avoids NPE
        String braces = "{}";
        StringBuilder builder = new StringBuilder();
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java

    private ProtocolServer createServer(URL url) {
        url = URLBuilder.from(url)
            // send readonly event when server closes, it's enabled by default
            .addParameterIfAbsent(CHANNEL_READONLYEVENT_SENT_KEY, Boolean.TRUE.toString())
```

### AssignmentToMethodParameter
Assignment to method parameter `connectNum`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
        try {
            // connectNum must be greater than or equal to 1
            connectNum = Math.max(connectNum, 1);

            // If the clients is empty, then the first initialization is
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
        try {
            // Replace InstanceAddressURL with ServiceConfigURL.
            url = new ServiceConfigURL(DubboCodec.NAME, url.getUsername(), url.getPassword(), url.getHost(), url.getPort(), url.getPath(), url.getAllParameters());
            url = url.addParameter(CODEC_KEY, DubboCodec.NAME);
            // enable heartbeat by default
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
            // Replace InstanceAddressURL with ServiceConfigURL.
            url = new ServiceConfigURL(DubboCodec.NAME, url.getUsername(), url.getPassword(), url.getHost(), url.getPort(), url.getPath(), url.getAllParameters());
            url = url.addParameter(CODEC_KEY, DubboCodec.NAME);
            // enable heartbeat by default
            url = url.addParameterIfAbsent(HEARTBEAT_KEY, String.valueOf(DEFAULT_HEARTBEAT));
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
            url = url.addParameter(CODEC_KEY, DubboCodec.NAME);
            // enable heartbeat by default
            url = url.addParameterIfAbsent(HEARTBEAT_KEY, String.valueOf(DEFAULT_HEARTBEAT));

            // connection should be lazy
```

### AssignmentToMethodParameter
Assignment to method parameter `resourceNames`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
    @Override
    public Map<String, T> getResource(Set<String> resourceNames) {
        resourceNames = resourceNames == null ? Collections.emptySet() : resourceNames;

        if (!resourceNames.isEmpty() && isCacheExistResource(resourceNames)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `properties`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
    public void init(Properties properties) throws IOException {
        if (properties == null) {
            properties = new Properties();
        }
        // log prefix
```

### AssignmentToMethodParameter
Assignment to method parameter `stackTrace`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
        sb.append('\n');
        if (stackTrace == null) {
            stackTrace = thread.getStackTrace();
        }
        for (StackTraceElement ste : stackTrace) {
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/AbstractCacheFactory.java`
#### Snippet
```java
    @Override
    public Cache getCache(URL url, Invocation invocation) {
        url = url.addParameter(METHOD_KEY, invocation.getMethodName());
        String key = url.getServiceKey() + invocation.getMethodName();
        Cache cache = caches.get(key);
```

### AssignmentToMethodParameter
Assignment to method parameter `str`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/TelnetCommandDecoder.java`
#### Snippet
```java
        CommandContext commandContext = null;
        if (!StringUtils.isBlank(str)) {
            str = str.trim();
            String[] array = str.split("(?<![\\\\]) ");
            if (array.length > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/AbstractMonitorFactory.java`
#### Snippet
```java
    @Override
    public Monitor getMonitor(URL url) {
        url = url.setPath(MonitorService.class.getName()).addParameter(INTERFACE_KEY, MonitorService.class.getName());
        String key = url.toServiceStringWithoutResolving();
        Monitor monitor = MONITORS.get(key);
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReportFactory.java`
#### Snippet
```java
    @Override
    public MetadataReport getMetadataReport(URL url) {
        url = url.setPath(MetadataReport.class.getName())
            .removeParameters(EXPORT_KEY, REFER_KEY);
        String key = url.toServiceString();
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryFactory.java`
#### Snippet
```java
        if (UrlUtils.hasServiceDiscoveryRegistryProtocol(url)) {
            String protocol = url.getParameter(REGISTRY_KEY, DEFAULT_REGISTRY);
            url = url.setProtocol(protocol).removeParameter(REGISTRY_KEY);
        }
        return new ServiceDiscoveryRegistry(url, applicationModel);
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
    public void doUnregister(URL url) {
        // fixme, add registry-cluster is not necessary anymore
        url = addRegistryClusterKey(url);
        serviceDiscovery.unregister(url);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
            return;
        }
        url = addRegistryClusterKey(url);
        doUnsubscribe(url, listener);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
        String registryCluster = serviceDiscovery.getUrl().getParameter(REGISTRY_CLUSTER_KEY);
        if (registryCluster != null && url.getParameter(REGISTRY_CLUSTER_KEY) == null) {
            url = url.addParameter(REGISTRY_CLUSTER_KEY, registryCluster);
        }
        return url;
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
    @Override
    public void doSubscribe(URL url, NotifyListener listener) {
        url = addRegistryClusterKey(url);

        serviceDiscovery.subscribe(url, listener);
```

### AssignmentToMethodParameter
Assignment to method parameter `serviceNames`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java

    protected void subscribeURLs(URL url, NotifyListener listener, Set<String> serviceNames) {
        serviceNames = toTreeSet(serviceNames);
        String serviceNamesKey = toStringKeys(serviceNames);
        String serviceKey = url.getServiceKey();
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
    public void doRegister(URL url) {
        // fixme, add registry-cluster is not necessary anymore
        url = addRegistryClusterKey(url);
        serviceDiscovery.register(url);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/ServiceInstanceMetadataUtils.java`
#### Snippet
```java
            return "";
        }
        url = url.removeParameters(APPLICATION_KEY, GROUP_KEY, DEPRECATED_KEY, TIMESTAMP_KEY);
        Map<String, String> params = getParams(url);

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistryFactory.java`
#### Snippet
```java
        }

        url = URLBuilder.from(url)
            .setPath(RegistryService.class.getName())
            .addParameter(INTERFACE_KEY, RegistryService.class.getName())
```

### AssignmentToMethodParameter
Assignment to method parameter `instanceUrls`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        if (supportedListeners != null && !supportedListeners.isEmpty()) {
            for (AddressListener addressListener : supportedListeners) {
                instanceUrls = addressListener.notify(instanceUrls, getConsumerUrl(), this);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `registry`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/InterfaceCompatibleRegistryProtocol.java`
#### Snippet
```java
    @Override
    public <T> ClusterInvoker<T> getServiceDiscoveryInvoker(Cluster cluster, Registry registry, Class<T> type, URL url) {
        registry = getRegistry(super.getRegistryUrl(url));
        DynamicDirectory<T> directory = new ServiceDiscoveryRegistryDirectory<>(type, url);
        return doCreateInvoker(directory, cluster, registry, type);
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
    protected InstanceAddressURL overrideWithConfigurator(InstanceAddressURL providerUrl) {
        // override url with configurator from "app-name.configurators"
        providerUrl = overrideWithConfigurators(getConsumerConfigurationListener(moduleModel).getConfigurators(), providerUrl);

        // override url with configurator from configurators from "service-name.configurators"
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        // override url with configurator from configurators from "service-name.configurators"
        if (referenceConfigurationListener != null) {
            providerUrl = overrideWithConfigurators(referenceConfigurationListener.getConfigurators(), providerUrl);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
                    overriddenURL = configurator.configure(overriddenURL);
                }
                url = new DubboServiceAddressURL(
                    interfaceAddressURL.getUrlAddress(),
                    interfaceAddressURL.getUrlParam(),
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
            } else {
                for (Configurator configurator : configurators) {
                    url = configurator.configure(url);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
    protected URL overrideDirectoryWithConfigurator(URL url) {
        // override url with configurator from "app-name.configurators"
        url = overrideWithConfigurators(getConsumerConfigurationListener(moduleModel).getConfigurators(), url);

        // override url with configurator from configurators from "service-name.configurators"
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        // override url with configurator from configurators from "service-name.configurators"
        if (referenceConfigurationListener != null) {
            url = overrideWithConfigurators(referenceConfigurationListener.getConfigurators(), url);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `urls`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
        boolean emptyProtectionEnabled = serviceDiscovery.getUrl().getParameter(ENABLE_EMPTY_PROTECTION_KEY, DEFAULT_ENABLE_EMPTY_PROTECTION);
        if (!emptyProtectionEnabled && urls == null) {
            urls = new ArrayList<>();
        } else if (emptyProtectionEnabled && urls == null) {
            urls = Collections.emptyList();
```

### AssignmentToMethodParameter
Assignment to method parameter `urls`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
            urls = new ArrayList<>();
        } else if (emptyProtectionEnabled && urls == null) {
            urls = Collections.emptyList();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `rawProvider`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
            String part1 = rawProvider.substring(0, idxStart);
            if (idxEnd == -1) {
                rawProvider = part1;
            } else {
                String part2 = rawProvider.substring(idxEnd + ENCODED_AND_MARK.length());
```

### AssignmentToMethodParameter
Assignment to method parameter `rawProvider`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
            } else {
                String part2 = rawProvider.substring(idxEnd + ENCODED_AND_MARK.length());
                rawProvider = part1 + part2;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `rawProvider`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java

        if (rawProvider.endsWith(ENCODED_AND_MARK)) {
            rawProvider = rawProvider.substring(0, rawProvider.length() - ENCODED_AND_MARK.length());
        }
        if (rawProvider.endsWith(ENCODED_QUESTION_MARK)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `rawProvider`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
        }
        if (rawProvider.endsWith(ENCODED_QUESTION_MARK)) {
            rawProvider = rawProvider.substring(0, rawProvider.length() - ENCODED_QUESTION_MARK.length());
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `consumer`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java

        for (ProviderFirstParams paramsFilter : providerFirstParams) {
            consumer = consumer.removeParameters(paramsFilter.params());
        }
        return consumer;
```

### AssignmentToMethodParameter
Assignment to method parameter `invokerUrls`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java

            if (invokerUrls == Collections.<URL>emptyList()) {
                invokerUrls = new ArrayList<>();
            }
            // use local reference to avoid NPE as this.cachedInvokerUrls will be set null by destroyAllInvokers().
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
    private URL mergeUrl(URL providerUrl) {
        if (providerUrl instanceof ServiceAddressURL) {
            providerUrl = overrideWithConfigurator(providerUrl);
        } else {
            providerUrl = moduleModel.getApplicationModel().getBeanFactory().getBean(ClusterUtils.class).mergeUrl(providerUrl, queryMap); // Merge the consumer side parameters
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            providerUrl = overrideWithConfigurator(providerUrl);
        } else {
            providerUrl = moduleModel.getApplicationModel().getBeanFactory().getBean(ClusterUtils.class).mergeUrl(providerUrl, queryMap); // Merge the consumer side parameters
            providerUrl = overrideWithConfigurator(providerUrl);
            providerUrl = providerUrl.addParameter(Constants.CHECK_KEY, String.valueOf(false)); // Do not check whether the connection is successful or not, always create Invoker!
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
        } else {
            providerUrl = moduleModel.getApplicationModel().getBeanFactory().getBean(ClusterUtils.class).mergeUrl(providerUrl, queryMap); // Merge the consumer side parameters
            providerUrl = overrideWithConfigurator(providerUrl);
            providerUrl = providerUrl.addParameter(Constants.CHECK_KEY, String.valueOf(false)); // Do not check whether the connection is successful or not, always create Invoker!
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            providerUrl = moduleModel.getApplicationModel().getBeanFactory().getBean(ClusterUtils.class).mergeUrl(providerUrl, queryMap); // Merge the consumer side parameters
            providerUrl = overrideWithConfigurator(providerUrl);
            providerUrl = providerUrl.addParameter(Constants.CHECK_KEY, String.valueOf(false)); // Do not check whether the connection is successful or not, always create Invoker!
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
        // FIXME, kept for mock
        if (providerUrl.hasParameter(MOCK_KEY) || providerUrl.getAnyMethodParameter(MOCK_KEY) != null) {
            providerUrl = providerUrl.removeParameter(TAG_KEY);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
                    path = path.substring(0, i);
                }
                providerUrl = providerUrl.setPath(path);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
                    overriddenURL = configurator.configure(overriddenURL);
                }
                url = new DubboServiceAddressURL(
                    interfaceAddressURL.getUrlAddress(),
                    interfaceAddressURL.getUrlParam(),
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            } else {
                for (Configurator configurator : configurators) {
                    url = configurator.configure(url);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
    protected URL overrideWithConfigurator(URL providerUrl) {
        // override url with configurator from "override://" URL for dubbo 2.6 and before
        providerUrl = overrideWithConfigurators(this.configurators, providerUrl);

        // override url with configurator from "app-name.configurators"
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java

        // override url with configurator from "app-name.configurators"
        providerUrl = overrideWithConfigurators(consumerConfigurationListener.getConfigurators(), providerUrl);

        // override url with configurator from configurators from "service-name.configurators"
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
        // override url with configurator from configurators from "service-name.configurators"
        if (referenceConfigurationListener != null) {
            providerUrl = overrideWithConfigurators(referenceConfigurationListener.getConfigurators(), providerUrl);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ChannelBufferInputStream.java`
#### Snippet
```java
        }

        len = Math.min(available, len);
        buffer.readBytes(b, off, len);
        return len;
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/HeapChannelBuffer.java`
#### Snippet
```java
            }
            readBytes += localReadBytes;
            index += localReadBytes;
            length -= localReadBytes;
        } while (length > 0);
```

### AssignmentToMethodParameter
Assignment to method parameter `length`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/HeapChannelBuffer.java`
#### Snippet
```java
            readBytes += localReadBytes;
            index += localReadBytes;
            length -= localReadBytes;
        } while (length > 0);

```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetHandlerAdapter.java`
#### Snippet
```java
        String prompt = channel.getUrl().getParameterAndDecoded(Constants.PROMPT_KEY, Constants.DEFAULT_PROMPT);
        boolean noprompt = message.contains("--no-prompt");
        message = message.replace("--no-prompt", "");
        StringBuilder buf = new StringBuilder();
        message = message.trim();
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetHandlerAdapter.java`
#### Snippet
```java
        message = message.replace("--no-prompt", "");
        StringBuilder buf = new StringBuilder();
        message = message.trim();
        String command;
        if (message.length() > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetHandlerAdapter.java`
#### Snippet
```java
            if (i > 0) {
                command = message.substring(0, i).trim();
                message = message.substring(i + 1).trim();
            } else {
                command = message;
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetHandlerAdapter.java`
#### Snippet
```java
            } else {
                command = message;
                message = "";
            }
        } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ByteBufferBackedChannelBuffer.java`
#### Snippet
```java

        if (buffer.hasArray()) {
            index += buffer.arrayOffset();
            do {
                int localReadBytes = in.read(buffer.array(), index, length);
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ByteBufferBackedChannelBuffer.java`
#### Snippet
```java
                }
                readBytes += localReadBytes;
                index += localReadBytes;
                length -= localReadBytes;
            } while (length > 0);
```

### AssignmentToMethodParameter
Assignment to method parameter `length`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ByteBufferBackedChannelBuffer.java`
#### Snippet
```java
                readBytes += localReadBytes;
                index += localReadBytes;
                length -= localReadBytes;
            } while (length > 0);
        } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Exchangers.java`
#### Snippet
```java
            throw new IllegalArgumentException("handler == null");
        }
        url = url.addParameterIfAbsent(Constants.CODEC_KEY, "exchange");
        return getExchanger(url).bind(url, handler);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
        if (CollectionUtils.isNotEmpty(configurators)) {
            for (Configurator configurator : configurators) {
                url = configurator.configure(url);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public <T> Invoker<T> refer(Class<T> type, URL url) throws RpcException {
        url = getRegistryUrl(url);
        Registry registry = getRegistry(url);
        if (RegistryService.class.equals(type)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `providerUrl`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    private URL overrideUrlWithConfig(URL providerUrl, OverrideListener listener) {
        ProviderConfigurationListener providerConfigurationListener = getProviderConfigurationListener(providerUrl);
        providerUrl = providerConfigurationListener.overrideUrl(providerUrl);

        ServiceConfigurationListener serviceConfigurationListener = new ServiceConfigurationListener(providerUrl.getOrDefaultModuleModel(), providerUrl, listener);
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
            consumerAttribute
        );
        url = url.putAttribute(CONSUMER_URL_KEY, consumerUrl);
        ClusterInvoker<T> migrationInvoker = getMigrationInvoker(this, cluster, registry, type, url, consumerUrl);
        return interceptInvoker(migrationInvoker, url, consumerUrl);
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
        if (message instanceof String) {
            if (isClientSide(channel)) {
                message = message + "\r\n";
            }
            byte[] msgData = ((String) message).getBytes(getCharset(channel).name());
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                System.arraycopy(b1, 0, b2, 0, b1.length);
                System.arraycopy(message, 0, b2, b1.length, message.length);
                message = b2;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `payload`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractCodec.java`
#### Snippet
```java
    protected static void checkPayload(Channel channel, int payload, long size) throws IOException {
        if (payload <= 0) {
            payload = getPayload(channel);
        }
        boolean overPayload = isOverPayload(payload, size);
```

### AssignmentToMethodParameter
Assignment to method parameter `header`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
            int length = header.length;
            if (header.length < readable) {
                header = Bytes.copyOf(header, readable);
                buffer.readBytes(header, length, readable - length);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `header`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
                if (header[i] == MAGIC_HIGH && header[i + 1] == MAGIC_LOW) {
                    buffer.readerIndex(buffer.readerIndex() - header.length + i);
                    header = Bytes.copyOf(header, i);
                    break;
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
         * which means params are shared among different services. Since client is shared among services this is currently not a problem.
         */
        url = url.addParameter(THREAD_NAME_KEY, CLIENT_THREAD_POOL_NAME)
            .addParameterIfAbsent(THREADPOOL_KEY, DEFAULT_CLIENT_THREADPOOL);
        executor = executorRepository.createExecutorIfAbsent(url);
```

### AssignmentToMethodParameter
Assignment to method parameter `args`
in `dubbo-container/dubbo-container-api/src/main/java/org/apache/dubbo/container/Main.java`
#### Snippet
```java
            if (ArrayUtils.isEmpty(args)) {
                String config = System.getProperty(CONTAINER_KEY, LOADER.getDefaultExtensionName());
                args = COMMA_SPLIT_PATTERN.split(config);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `serializeType`
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DefaultMultipleSerialization.java`
#### Snippet
```java
    @Override
    public void serialize(URL url, String serializeType, Class<?> clz, Object obj, OutputStream os) throws IOException {
        serializeType = convertHessian(serializeType);
        final Serialization serialization = url.getOrDefaultFrameworkModel().getExtensionLoader(Serialization.class).getExtension(serializeType);
        final ObjectOutput serialize = serialization.serialize(null, os);
```

### AssignmentToMethodParameter
Assignment to method parameter `serializeType`
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DefaultMultipleSerialization.java`
#### Snippet
```java
    @Override
    public Object deserialize(URL url, String serializeType, Class<?> clz, InputStream os) throws IOException, ClassNotFoundException {
        serializeType = convertHessian(serializeType);
        final Serialization serialization = url.getOrDefaultFrameworkModel().getExtensionLoader(Serialization.class).getExtension(serializeType);
        final ObjectInput in = serialization.deserialize(null, os);
```

### AssignmentToMethodParameter
Assignment to method parameter `classLoader`
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2CreatorManager.java`
#### Snippet
```java
    public void setCreator(ClassLoader classLoader) {
        if (classLoader == null) {
            classLoader = SYSTEM_CLASSLOADER_KEY;
        }
        JSONFactory.setContextReaderCreator(readerMap.computeIfAbsent(classLoader, ObjectReaderCreatorASM::new));
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/FastJson2ObjectOutput.java`
#### Snippet
```java
        for (int i = 0; i < length; i++) {
            bytes[length - i - 1] = (byte) (value & 0xFF);
            value >>= 8;
        }
        os.write(bytes);
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `listener`
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
        }
        multicast(SUBSCRIBE + " " + url.toFullString());
        synchronized (listener) {
            try {
                listener.wait(url.getParameter(TIMEOUT_KEY, DEFAULT_TIMEOUT));
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `listener`
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
                for (final NotifyListener listener : entry.getValue()) {
                    notify(key, listener, list);
                    synchronized (listener) {
                        listener.notify();
                    }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `rpcStatus`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ActiveLimitFilter.java`
#### Snippet
```java
    private void notifyFinish(final RpcStatus rpcStatus, int max) {
        if (max > 0) {
            synchronized (rpcStatus) {
                rpcStatus.notifyAll();
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `rpcStatus`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ActiveLimitFilter.java`
#### Snippet
```java
            long start = System.currentTimeMillis();
            long remain = timeout;
            synchronized (rpcStatus) {
                while (!RpcStatus.beginCount(url, methodName, max)) {
                    try {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `serviceDescriptors`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
            List<ServiceDescriptor> serviceDescriptors = ConcurrentHashMapUtils.computeIfAbsent(services, path,
                _k -> new CopyOnWriteArrayList<>());
            synchronized (serviceDescriptors) {
                Optional<ServiceDescriptor> previous = serviceDescriptors.stream()
                    .filter(s -> s.getServiceInterfaceClass().equals(serviceDescriptor.getServiceInterfaceClass())).findFirst();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `serviceDescriptors`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        List<ServiceDescriptor> serviceDescriptors = ConcurrentHashMapUtils.computeIfAbsent(services, interfaceClazz.getName(),
            k -> new CopyOnWriteArrayList<>());
        synchronized (serviceDescriptors) {
            Optional<ServiceDescriptor> previous = serviceDescriptors.stream()
                .filter(s -> s.getServiceInterfaceClass().equals(interfaceClazz)).findFirst();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `type`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
        if (bean == null) {
            // lock by type
            synchronized (type) {
                bean = getBean(name, type);
                if (bean == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `type`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
        if (bean == null) {
            // lock by type
            synchronized (type) {
                bean = getBean(name, type);
                if (bean == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `configMap`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
        for (Map<String, V> configMap : configMaps) {
            Map<String, V> copy;
            synchronized (configMap) {
                copy = new HashMap<>(configMap);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `configMap`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
        }
        Map<String, V> copy;
        synchronized (configMap) {
            copy = new HashMap<>(configMap);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `configMap`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
        if (CollectionUtils.isNotEmptyMap(configMap)) {
            Map<String, V> copy;
            synchronized (configMap) {
                copy = new HashMap<>(configMap);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `mutex`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
    private <V> V executeMutually(final Object mutex, Callable<V> callable) {
        V value = null;
        synchronized (mutex) {
            try {
                value = callable.call();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `holder`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        Object instance = holder.get();
        if (instance == null) {
            synchronized (holder) {
                instance = holder.get();
                if (instance == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `configsMap`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

        // lock by config type
        synchronized (configsMap) {
            return (T) addIfAbsent(config, configsMap);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `configs`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
        if (CollectionUtils.isNotEmptyMap(configs)) {
            // lock by config type
            synchronized (configs) {
                return removeIfAbsent(config, configs);
            }
```

## RuleId[id=ReturnNull]
### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastServiceDiscovery.java`
#### Snippet
```java
    @Override
    public List<ServiceInstance> getInstances(String serviceName) throws NullPointerException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/ObservationHandlerGrouping.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertProvider.java`
#### Snippet
```java
        CertPair certPair = dubboCertManager.generateCert();
        if (certPair == null) {
            return null;
        }
        return new ProviderCert(certPair.getCertificate().getBytes(StandardCharsets.UTF_8),
```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertProvider.java`
#### Snippet
```java
        CertPair certPair = dubboCertManager.generateCert();
        if (certPair == null) {
            return null;
        }
        return new Cert(certPair.getCertificate().getBytes(StandardCharsets.UTF_8),
```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
        if (keyPair == null) {
            logger.error(CONFIG_SSL_CERT_GENERATE_FAILED, "", "", "Generate Key failed. Please check if your system support.");
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
            logger.error(CONFIG_SSL_CERT_GENERATE_FAILED, "", "", "Failed to generate cert from Dubbo Certificate Authority. " +
                "Message: " + (certificateResponse == null ? "null" : certificateResponse.getMessage()));
            return null;
        }
        logger.info("Successfully generate cert from Dubbo Certificate Authority. Cert expire time: " + certificateResponse.getExpireTime());
```

### ReturnNull
Return of `null`
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java
    private MetricObject value2MetricObject(String metric, Integer value, MetricLevel level) {
        if (metric == null || value == null || level == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesConfigUtils.java`
#### Snippet
```java
        return StringUtils.isNotEmpty(str) ?
            new String(Base64.getDecoder().decode(str)) :
            null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/aggregate/SlidingWindow.java`
#### Snippet
```java
    public T getPaneValue(long timeMillis) {
        if (timeMillis < 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/aggregate/SlidingWindow.java`
#### Snippet
```java

        if (pane == null || !pane.isTimeInWindow(timeMillis)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/aggregate/SlidingWindow.java`
#### Snippet
```java
    public Pane<T> currentPane(long timeMillis) {
        if (timeMillis < 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
            return builder.toString();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
        Service service = kubernetesClient.services().inNamespace(namespace).withName(serviceName).get();
        if (service == null) {
            return null;
        }
        return service.getSpec().getSelector();
```

### ReturnNull
Return of `null`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/JavaCompilerPluginConfiguration.java`
#### Snippet
```java
			return this.project.getProperties().get(propertyName).toString();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/JavaCompilerPluginConfiguration.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/JavaCompilerPluginConfiguration.java`
#### Snippet
```java

		if (childNode == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AttachmentsAdapter.java`
#### Snippet
```java
                return (String) obj;
            }
            return null; // or JSON.toString(obj);
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ProtocolServer.java`
#### Snippet
```java

    default RemotingServer getRemotingServer() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ProtocolServer.java`
#### Snippet
```java

    default URL getUrl() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AppResponse.java`
#### Snippet
```java
            return (String) value;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
            return (String) value;
        }
        return null; // or JSON.toString(value);
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    public RpcContextAttachment copyOf(boolean needCopy) {
        if (!isValid()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServerContextAttachment.java`
#### Snippet
```java
                return adapter.getObjectAttachment((String) key);
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServerContextAttachment.java`
#### Snippet
```java
            return (String) attachment;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServerContextAttachment.java`
#### Snippet
```java
                return adapter.removeAttachment((String) key);
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/InvokerInvocationHandler.java`
#### Snippet
```java
            } else if ("$destroy".equals(methodName)) {
                invoker.destroy();
                return null;
            } else if ("hashCode".equals(methodName)) {
                return invoker.hashCode();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    public String getInterfaceName() {
        if (consumerUrl == null) {
            return null;
        }
        return consumerUrl.getServiceInterface();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    public String getRemoteHost() {
        return remoteAddress == null ? null :
                remoteAddress.getAddress() == null ? remoteAddress.getHostName()
                        : NetUtils.filterLocalHost(remoteAddress.getAddress().getHostAddress());
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    public String getRemoteHostName() {
        return remoteAddress == null ? null : remoteAddress.getHostName();
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    public String getProtocolServiceKey() {
        if (consumerUrl == null) {
            return null;
        }
        return consumerUrl.getProtocolServiceKey();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    public String getProtocol() {
        if (consumerUrl == null) {
            return null;
        }
        return consumerUrl.getProtocol();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    public String getVersion() {
        if (consumerUrl == null) {
            return null;
        }
        return consumerUrl.getVersion();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    public String getServiceKey() {
        if (consumerUrl == null) {
            return null;
        }
        return consumerUrl.getServiceKey();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    public String getGroup() {
        if (consumerUrl == null) {
            return null;
        }
        return consumerUrl.getGroup();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public <T> T getRequest(Class<T> clazz) {
        return (request != null && clazz.isAssignableFrom(request.getClass())) ? (T) request : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public <T> T getResponse(Class<T> clazz) {
        return (response != null && clazz.isAssignableFrom(response.getClass())) ? (T) response : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
            logger.warn(COMMON_REFLECTIVE_OPERATION_FAILED, "", "", t.getMessage(), t);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
        Method method = cls.getMethod(invocation.getMethodName(), invocation.getParameterTypes());
        if (method.getReturnType() == void.class) {
            return null;
        }
        return method;
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
                if (StringUtils.isNotEmpty(service)) {
                    Method method = getMethodByService(invocation, service);
                    return method == null ? null : method.getReturnType();
                }
            }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
            logger.warn(COMMON_REFLECTIVE_OPERATION_FAILED, "", "", t.getMessage(), t);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    public static Long getInvocationId(Invocation inv) {
        String id = inv.getAttachment(ID_KEY);
        return id == null ? null : new Long(id);
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
        @Override
        public Collection<Channel> getChannels() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
        @Override
        public URL getUrl() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
        @Override
        public InetSocketAddress getLocalAddress() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
        @Override
        public Channel getChannel(InetSocketAddress remoteAddress) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
        @Override
        public ChannelHandler getChannelHandler() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-provider/src/main/java/org/apache/dubbo/demo/provider/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<String> sayHelloAsync(String name) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractInvoker.java`
#### Snippet
```java
    private static Map<String, Object> convertAttachment(URL url, String[] keys) {
        if (ArrayUtils.isEmpty(keys)) {
            return null;
        }
        Map<String, Object> attachment = new HashMap<>(keys.length);
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/DubboHttpProtocolServer.java`
#### Snippet
```java
                @Override
                public String nextElement() {
                    return null;
                }
            };
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/DubboHttpProtocolServer.java`
#### Snippet
```java
        @Override
        public String getInitParameter(String s) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
                                                 boolean ignoreEmptyTokens) {
        if (str == null) {
            return null;
        } else {
            StringTokenizer st = new StringTokenizer(str, delimiters);
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java

    public static String[] toStringArray(Collection<String> collection) {
        return collection == null ? null : collection.toArray(new String[collection.size()]);
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
            return client.getChildren().usingWatcher(listener).forPath(path);
        } catch (NoNodeException e) {
            return null;
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
            return client.getChildren().forPath(path);
        } catch (NoNodeException e) {
            return null;
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
        try {
            byte[] dataBytes = client.getData().forPath(path);
            return (dataBytes == null || dataBytes.length == 0) ? null : new String(dataBytes, CHARSET);
        } catch (NoNodeException e) {
            // ignore NoNode Exception.
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
            throw new IllegalStateException(e.getMessage(), e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
            return findClass(name);
        } catch (Exception e) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        }

        return null;

    }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java

        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java

        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java

        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
            return ReflectUtils.getValueByFields(obj, ReflectUtils.getFieldAndTryCatch(obj.getClass(), field));
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
            return method.invoke(object, EMPTY_OBJECT_ARRAY);
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleServiceDiscovery.java`
#### Snippet
```java
    @Override
    public ServiceInstance getLocalInstance() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/ClassPathMetadataStorage.java`
#### Snippet
```java
            } catch (IOException e) {
            }
            return null;
        }).orElse(null);
    }
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static VariableElement findField(Element element, String fieldName) {
        return element == null ? null : findField(element.asType(), fieldName);
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static VariableElement getDeclaredField(Element element, String fieldName) {
        return element == null ? null : getDeclaredField(element.asType(), fieldName);
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
        }
        // or, try to judge the declared method
        return supports(processingEnv, serviceType, serviceInterfaceType, serviceMethod) ? serviceMethod : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java

        if (httpMethod == null || getLength(httpMethod) < 1) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static String getReturnType(ExecutableElement method) {
        return method == null ? null : TypeUtils.toString(method.getReturnType());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static String getMethodName(ExecutableElement method) {
        return method == null ? null : method.getSimpleName().toString();
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static ExecutableElement findMethod(TypeElement type, String methodName, Type oneParameterType, Type... otherParameterTypes) {
        return type == null ? null : findMethod(type.asType(), methodName, oneParameterType, otherParameterTypes);
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static ExecutableElement findMethod(TypeElement type, String methodName, CharSequence... parameterTypes) {
        return type == null ? null : findMethod(type.asType(), methodName, parameterTypes);
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
    static AnnotationMirror findMetaAnnotation(Element annotatedConstruct, CharSequence metaAnnotationClassName) {
        return annotatedConstruct == null ?
                null :
                getAnnotations(annotatedConstruct)
                        .stream()
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static <T> T getAttribute(AnnotationMirror annotation, String attributeName) {
        return annotation == null ? null : getAttribute(annotation.getElementValues(), attributeName);
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
                                          Class<? extends Annotation> annotationClass) {
        return annotationClass == null ?
                null :
                getAnnotation(annotatedConstruct, annotationClass.getTypeName());
    }
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static AnnotationMirror findAnnotation(TypeMirror type, Class<? extends Annotation> annotationClass) {
        return annotationClass == null ? null : findAnnotation(type, annotationClass.getTypeName());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
    static AnnotationMirror getAnnotation(AnnotatedConstruct annotatedConstruct, CharSequence annotationClassName) {
        List<AnnotationMirror> annotations = getAnnotations(annotatedConstruct, annotationClassName);
        return annotations.isEmpty() ? null : annotations.get(0);
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static AnnotationMirror findAnnotation(Element element, Class<? extends Annotation> annotationClass) {
        return annotationClass == null ? null : findAnnotation(element, annotationClass.getTypeName());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static String getResourceName(CharSequence type) {
        return type == null ? null : type.toString().replace('.', '/').concat(".class");
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static TypeElement getType(ProcessingEnvironment processingEnv, TypeMirror type) {
        return type == null ? null : getType(processingEnv, type.toString());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static TypeElement ofTypeElement(Element element) {
        return isTypeElement(element) ? (TypeElement) element : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java
    static DeclaredType getSuperType(TypeMirror type) {
        TypeElement superType = getSuperType(ofTypeElement(type));
        return superType == null ? null : ofDeclaredType(superType.asType());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static TypeElement getSuperType(TypeElement type) {
        return type == null ? null : ofTypeElement(type.getSuperclass());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static URL getResource(ProcessingEnvironment processingEnv, TypeMirror type) {
        return type == null ? null : getResource(processingEnv, type.toString());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java
    static TypeElement getType(ProcessingEnvironment processingEnv, CharSequence typeName) {
        if (processingEnv == null || typeName == null) {
            return null;
        }
        Elements elements = processingEnv.getElementUtils();
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static DeclaredType ofDeclaredType(Element element) {
        return element == null ? null : ofDeclaredType(element.asType());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static TypeElement getType(ProcessingEnvironment processingEnv, Type type) {
        return type == null ? null : getType(processingEnv, type.getTypeName());
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java
            return ofTypeElement(declaredType.asElement());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static DeclaredType ofDeclaredType(TypeMirror type) {
        return isDeclaredType(type) ? (DeclaredType) type : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/CompositeReferenceCache.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/CompositeReferenceCache.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/CompositeReferenceCache.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/SimpleReferenceCache.java`
#### Snippet
```java
            return (T) referenceConfigBases.get(0).get();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/SimpleReferenceCache.java`
#### Snippet
```java
            return (T) referenceConfigBases.get(0).get();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/SimpleReferenceCache.java`
#### Snippet
```java
            return (T) referenceConfigs.get(0).get();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/jackson/ObjectMapperCodec.java`
#### Snippet
```java

            if (bytes == null || bytes.length == 0) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/jackson/ObjectMapperCodec.java`
#### Snippet
```java
    public <T> T deserialize(String content, Class<T> clazz) {
        if (StringUtils.isBlank(content)) {
            return null;
        }
        return deserialize(content.getBytes(), clazz);
```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/jackson/ObjectMapperCodec.java`
#### Snippet
```java

            if (object == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
            return UrlUtils.parseURL(address, map);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
        if (configCenter.isValid()) {
            if (!configCenter.checkOrUpdateInitialized(true)) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
                    logger.warn(CONFIG_FAILED_INIT_CONFIG_CENTER, "", "", "The configuration center failed to initialize", e);
                    configCenter.setInitialized(false);
                    return null;
                } else {
                    throw new IllegalStateException(e);
```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
            return dynamicConfiguration;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
                return (MethodMetadata) source;
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
            if (isFactoryMethodMetadataEnabled()) {
                MethodMetadata factoryMethodMetadata = annotatedBeanDefinition.getFactoryMethodMetadata();
                return factoryMethodMetadata != null ? factoryMethodMetadata.getReturnTypeName() : null;
            } else {
                Object source = annotatedBeanDefinition.getSource();
```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
            return beanFactory.getBean(beanName, ApplicationModel.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
            return beanFactory.getBean(beanName, ModuleModel.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
            return beanFactory.getBean(beanName, DubboSpringInitContext.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitContext.java`
#### Snippet
```java

    public ApplicationModel getApplicationModel() {
        return (moduleModel == null) ? null : moduleModel.getApplicationModel();
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitializer.java`
#### Snippet
```java

    private static String safeGetModelDesc(ScopeModel scopeModel) {
        return scopeModel != null ? scopeModel.getDesc() : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitializer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitializer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
    private String[] resolveStringArray(String[] strs) {
        if (strs == null) {
            return null;
        }
        for (int i = 0; i < strs.length; i++) {
```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java
            return beanFactory.getBean(name, type);
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java
        if (context == null) {
            // ignore if spring context is not bound
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java
        //check @SPI annotation
        if (type.isInterface() && type.isAnnotationPresent(SPI.class)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java

        //logger.warn("No spring extension (bean) named:" + name + ", try to find an extension (bean) of type " + type.getName());
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java
        String[] beanNamesForType = beanFactory.getBeanNamesForType(type, true, false);
        if (beanNamesForType == null) {
            return null;
        }
        if (beanNamesForType.length > 1) {
```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
    private static ManagedMap parseParameters(NodeList nodeList, RootBeanDefinition beanDefinition, ParserContext parserContext) {
        if (nodeList == null) {
            return null;
        }
        ManagedMap parameters = null;
```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default Map<Object, Object> getAttributes() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default Object put(Object key, Object value) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java

    default org.apache.dubbo.rpc.Invocation getOriginal() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default String getAttachment(String key, String defaultValue) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
        @Override
        public String getServiceName() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default String getServiceName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default Object getObjectAttachment(String key, Object defaultValue) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default Object getObjectAttachment(String key) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default ServiceModel getServiceModel() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default String getTargetServiceUniqueName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    @Override
    default Object get(Object key) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
    private static String convertToString(String key, Object obj) {
        if (obj == null) {
            return null;
        }
        if (ReferenceAttributes.PARAMETERS.equals(key) && obj instanceof String[]) {
```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invoker.java`
#### Snippet
```java
    @Override
    default org.apache.dubbo.rpc.Result invoke(org.apache.dubbo.rpc.Invocation invocation) throws org.apache.dubbo.rpc.RpcException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invoker.java`
#### Snippet
```java

    default org.apache.dubbo.rpc.Invoker<T> getOriginal() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java
    @Override
    default Object getObjectAttachment(String key) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java
        @Override
        public org.apache.dubbo.rpc.Result get(long timeout, TimeUnit unit) throws InterruptedException, ExecutionException, TimeoutException {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java
        @Override
        public org.apache.dubbo.rpc.Result whenCompleteWithContext(BiConsumer<org.apache.dubbo.rpc.Result, Throwable> fn) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java
        @Override
        public org.apache.dubbo.rpc.Result get() throws InterruptedException, ExecutionException {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java
    @Override
    default Object getObjectAttachment(String key, Object defaultValue) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java
        @Override
        public <U> CompletableFuture<U> thenApply(Function<org.apache.dubbo.rpc.Result, ? extends U> fn) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
        CompletableFuture completableFuture = FutureContext.getContext().getCompatibleCompletableFuture();
        if (completableFuture == null) {
            return null;
        }
        return new FutureAdapter(completableFuture);
```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/cluster/Cluster.java`
#### Snippet
```java
    @Override
    default <T> Invoker<T> join(Directory<T> directory, boolean buildFilterChain) throws RpcException {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/remoting/Dispatcher.java`
#### Snippet
```java
    @Override
    default ChannelHandler dispatch(ChannelHandler handler, URL url) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/remoting/telnet/TelnetHandler.java`
#### Snippet
```java
    @Override
    default String telnet(Channel channel, String message) throws RemotingException {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    public String getAttachment(String key) {
        if (attachments == null) {
            return null;
        }
        return (String) attachments.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    @Override
    public String getProtocolServiceKey() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-consumer/src/main/java/org/apache/dubbo/demo/consumer/comp/DemoServiceComponent.java`
#### Snippet
```java
    @Override
    public CompletableFuture<String> sayHelloAsync(String name) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkServiceRepository.java`
#### Snippet
```java
        if (providersWithoutGroup.containsKey(key)) {
            List<ProviderModel> providerModels = providersWithoutGroup.get(key);
            return providerModels.size() > 0 ? providerModels.get(0) : null;
        } else {
            return null;
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkServiceRepository.java`
#### Snippet
```java
            return providerModels.size() > 0 ? providerModels.get(0) : null;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java

    private static String safeGetModelDesc(ScopeModel scopeModel) {
        return scopeModel != null ? scopeModel.getDesc() : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
            return methods.get(params);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        if (interfaceName != null && services.containsKey(interfaceName)) {
            List<ServiceDescriptor> serviceDescriptors = services.get(interfaceName);
            return serviceDescriptors.size() > 0 ? serviceDescriptors.get(0) : null;
        } else {
            return null;
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
            return serviceDescriptors.size() > 0 ? serviceDescriptors.get(0) : null;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        if (consumers.containsKey(serviceKey)) {
            List<ConsumerModel> consumerModels = consumers.get(serviceKey);
            return consumerModels.size() > 0 ? consumerModels.get(0) : null;
        } else {
            return null;
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
            return consumerModels.size() > 0 ? consumerModels.get(0) : null;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        ServiceDescriptor serviceDescriptor = lookupService(interfaceName);
        if (serviceDescriptor == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        List<MethodDescriptor> methods = serviceDescriptor.getMethods(methodName);
        if (CollectionUtils.isEmpty(methods)) {
            return null;
        }
        return methods.iterator().next();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        List<ServiceDescriptor> serviceDescriptors = services.get(serviceName);
        if (CollectionUtils.isEmpty(serviceDescriptors)) {
            return null;
        }
        return serviceDescriptors.get(0);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubServiceDescriptor.java`
#### Snippet
```java
            return methods.get(params);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubServiceDescriptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ApplicationModel.java`
#### Snippet
```java
    public String tryGetApplicationName() {
        Optional<ApplicationConfig> appCfgOptional = getApplicationConfigManager().getApplication();
        return appCfgOptional.isPresent() ? appCfgOptional.get().getName() : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ApplicationModel.java`
#### Snippet
```java
                }
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubMethodDescriptor.java`
#### Snippet
```java
    @Override
    public Method getMethod() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/executor/AbstractIsolationExecutorSupport.java`
#### Snippet
```java
        ServiceKey serviceKey = getServiceKey(data);
        if (serviceKey == null) {
            return null;
        }
        return frameworkServiceRepository.lookupExportedService(serviceKey.toString());
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelUtil.java`
#### Snippet
```java
    public static ApplicationModel getOrNullApplicationModel(ScopeModel scopeModel) {
        if (scopeModel == null) {
            return null;
        }
        if (scopeModel instanceof ApplicationModel) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/BaseServiceMetadata.java`
#### Snippet
```java
        int index = serviceKey.indexOf("/");
        if (index == -1) {
            return null;
        }
        return serviceKey.substring(0, index);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceModel.java`
#### Snippet
```java
    public ReferenceConfigBase<?> getReferenceConfig() {
        if (config == null) {
            return null;
        }
        if (config instanceof ReferenceConfigBase) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceModel.java`
#### Snippet
```java
    public ServiceConfigBase<?> getServiceConfig() {
        if (config == null) {
            return null;
        }
        if (config instanceof ServiceConfigBase) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/ssl/CertManager.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/ssl/CertManager.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/ssl/Cert.java`
#### Snippet
```java

    public InputStream getTrustCertInputStream() {
        return trustCert != null ? new ByteArrayInputStream(trustCert) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/ssl/Cert.java`
#### Snippet
```java

    public InputStream getKeyCertChainInputStream() {
        return keyCertChain != null ? new ByteArrayInputStream(keyCertChain) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/ssl/Cert.java`
#### Snippet
```java

    public InputStream getPrivateKeyInputStream() {
        return privateKey != null ? new ByteArrayInputStream(privateKey) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/param/DynamicValues.java`
#### Snippet
```java
    public String getN(int n) {
        if (n == -1) {
            return null;
        }
        return index2Value[n];
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/ScopeBeanExtensionInjector.java`
#### Snippet
```java
    @Override
    public <T> T getInstance(final Class<T> type, final String name) {
        return beanFactory == null ? null : beanFactory.getBean(name, type);
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/support/InstantiationStrategy.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/AbstractJSONImpl.java`
#### Snippet
```java
        assert key != null;
        if (!obj.containsKey(key)) {
            return null;
        }
        Object value = obj.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/AbstractJSONImpl.java`
#### Snippet
```java
        List<?> list = getList(obj, key);
        if (list == null) {
            return null;
        }
        return checkStringList(list);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/AbstractJSONImpl.java`
#### Snippet
```java
        assert key != null;
        if (!obj.containsKey(key)) {
            return null;
        }
        Object value = obj.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/AbstractJSONImpl.java`
#### Snippet
```java
        assert key != null;
        if (!obj.containsKey(key)) {
            return null;
        }
        Object value = obj.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/AbstractJSONImpl.java`
#### Snippet
```java
        assert key != null;
        if (!obj.containsKey(key)) {
            return null;
        }
        Object value = obj.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/AbstractJSONImpl.java`
#### Snippet
```java
        assert key != null;
        if (!obj.containsKey(key)) {
            return null;
        }
        Object value = obj.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/AbstractJSONImpl.java`
#### Snippet
```java
        assert key != null;
        if (!obj.containsKey(key)) {
            return null;
        }
        Object value = obj.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/AbstractJSONImpl.java`
#### Snippet
```java
        List<?> list = getList(obj, key);
        if (list == null) {
            return null;
        }
        return checkObjectList(list);
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            attachmentLock.lock();
            if (attachments == null) {
                return null;
            }
            return attachments.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            attachmentLock.lock();
            if (attachments == null) {
                return null;
            }
            final Object val = attachments.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            attachmentLock.lock();
            if (attachments == null) {
                return null;
            }
            Object value = attachments.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
                return (String) value;
            }
            return null;
        } finally {
            attachmentLock.unlock();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/store/support/SimpleDataStore.java`
#### Snippet
```java
    public Object get(String componentName, String key) {
        if (!data.containsKey(componentName)) {
            return null;
        }
        return data.get(componentName).get(key);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
        // All classes are derived from java.lang.Object, cannot filter bean by it
        if (type == Object.class) {
            return null;
        }
        List<BeanInfo> candidates = null;
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
            return (T) firstCandidate.instance;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java
            HashedWheelTimeout head = this.head;
            if (head == null) {
                return null;
            }
            HashedWheelTimeout next = head.next;
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
    public static Object convertPrimitive(FrameworkModel frameworkModel, Class<?> type, String value) {
        if (isEmpty(value)) {
            return null;
        }
        Class<?> wrapperType = WRAPPER_PRIMITIVE_TYPE_MAP.getOrDefault(type, type);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
            return keyNumber.get(key);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
        String value = getParameterAndDecoded(key);
        if (StringUtils.isEmpty(value)) {
            return null;
        }
        u = URL.valueOf(value);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
    public static <T> T first(Collection<T> values) {
        if (isEmpty(values)) {
            return null;
        }
        if (values instanceof List) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
    public static Map<String, List<String>> joinAll(Map<String, Map<String, String>> map, String separator) {
        if (map == null) {
            return null;
        }
        Map<String, List<String>> result = new HashMap<>();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
    public static Map<String, String> split(List<String> list, String separator) {
        if (list == null) {
            return null;
        }
        Map<String, String> map = new HashMap<>();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
    public static List<String> join(Map<String, String> map, String separator) {
        if (map == null) {
            return null;
        }
        List<String> list = new ArrayList<>();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
    public static Map<String, Map<String, String>> splitAll(Map<String, List<String>> list, String separator) {
        if (list == null) {
            return null;
        }
        Map<String, Map<String, String>> result = new HashMap<>();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LFUCache.java`
#### Snippet
```java
            lock.unlock();
        }
        return (node != null) ? node.value : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LFUCache.java`
#### Snippet
```java
            lock.unlock();
        }
        return (node != null) ? node.value : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/RegexProperties.java`
#### Snippet
```java
                .orElse(null);

        return keyPattern == null ? null : super.getProperty(keyPattern);
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TypeUtils.java`
#### Snippet
```java
            return (Class) rawType;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
    private static Object realize0(Object pojo, Class<?> type, Type genericType, final Map<Object, Object> history) {
        if (pojo == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
        Type rawType = type.getRawType();
        if (!isAssignableFrom((Class<?>) rawType, mapObject.getClass())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
        } else {
            // ignore other type currently
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
        } else if (fieldType instanceof GenericArrayType || fieldType instanceof TypeVariable<?> || fieldType instanceof WildcardType) {
            // ignore these type currently
            return null;
        } else {
            throw new IllegalArgumentException("Unrecognized Type: " + fieldType.toString());
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            // type didn't match and mapObject is not another Map struct.
            // we just ignore this situation.
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
    private static Object generalize(Object pojo, Map<Object, Object> history) {
        if (pojo == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            return (short) 0;
        }
        return parameterType.isPrimitive() ? 0 : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        //avoid to search network interface card many times
        if("".equals(HOST_ADDRESS_V6)){
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        }
        HOST_ADDRESS_V6 = "";
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
                return previous;
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        if (StringUtils.isNotEmpty(methodsString)) {
            if (!methodsString.contains(method)) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
            return methodMap.get(method);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
//            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
                return urlParam.getParameter((String) key);
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/FieldUtils.java`
#### Snippet
```java
                }
            }
            return null;
        } catch (Exception exception) {
            throw new RuntimeException(exception);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
            if (type == java.time.LocalDateTime.class) {
                if (StringUtils.isEmpty(string)) {
                    return null;
                }
                return LocalDateTime.parse(string);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
            if (type == java.time.LocalDate.class) {
                if (StringUtils.isEmpty(string)) {
                    return null;
                }
                return LocalDate.parse(string);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
            if (type == java.time.LocalTime.class) {
                if (StringUtils.isEmpty(string)) {
                    return null;
                }
                
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

        if (str == null) {
            return null;
        }
        if (repeat <= 0) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    public static String encodeParameters(Map<String, String> params) {
        if (params == null || params.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    public static String trim(String str) {
        return str == null ? null : str.trim();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Assert.java`
#### Snippet
```java

    private static String nullSafeGet(Supplier<String> messageSupplier) {
        return (messageSupplier != null ? messageSupplier.get() : null);
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/CompositeConfiguration.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/PrefixedConfiguration.java`
#### Snippet
```java
            return value;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
    static Method findOverriddenMethod(Method overrider, Class<?> declaringClass) {
        List<Method> matchedMethods = getAllMethods(declaringClass, method -> overrides(overrider, method));
        return matchedMethods.isEmpty() ? null : matchedMethods.get(0);
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/nop/NopDynamicConfiguration.java`
#### Snippet
```java
    public String getConfig(String key, String group, long timeout) throws IllegalStateException {
        // no-op
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/nop/NopDynamicConfiguration.java`
#### Snippet
```java
    @Override
    public Object getInternalProperty(String key) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/AbstractDynamicConfiguration.java`
#### Snippet
```java
        execute(() -> {
            task.run();
            return null;
        }, timeout);
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/AbstractDynamicConfiguration.java`
#### Snippet
```java
    @Override
    public Object getInternalProperty(String key) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    private static Object getEmptyObject(Class<?> returnType, Map<Class<?>, Object> emptyInstances, int level) {
        if (level > 2) {
            return null;
        }
        if (returnType == null) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        }
        if (returnType == null) {
            return null;
        }
        if (returnType == boolean.class || returnType == Boolean.class) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        }
        if (returnType.isInterface()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            return value;
        } catch (Throwable e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                    + method.getName().substring(4);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            return primitiveDefaults.get(classType);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static String getCodeBase(Class<?> cls) {
        if (cls == null) {
            return null;
        }
        ProtectionDomain domain = cls.getProtectionDomain();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        ProtectionDomain domain = cls.getProtectionDomain();
        if (domain == null) {
            return null;
        }
        CodeSource source = domain.getCodeSource();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        CodeSource source = domain.getCodeSource();
        if (source == null) {
            return null;
        }
        URL location = source.getLocation();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        URL location = source.getLocation();
        if (location == null) {
            return null;
        }
        return location.getFile();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            return primitiveDefaults.get(m.getReturnType());
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                            //ignore
                        }
                        return null;
                    }).get();
        } catch (Exception e) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/log4j2/Log4j2LoggerAdapter.java`
#### Snippet
```java
    @Override
    public File getFile() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
        Class<? extends Annotation> annotationType = resolveAnnotationType(annotatedElement, annotationClassName);
        if (annotationType == null) {
            return null;
        }
        return (A) annotatedElement.getAnnotation(annotationType);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
     */
    static <T> T getAttribute(Annotation annotation, String attributeName) throws IllegalArgumentException {
        return annotation == null ? null : invokeMethod(annotation, attributeName);
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
    static <T> T getDefaultValue(Class<? extends Annotation> annotationType, String attributeName) {
        Method method = findMethod(annotationType, attributeName);
        return (T) (method == null ? null : method.getDefaultValue());
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
        Class<?> annotationType = resolveClass(annotationClassName, classLoader);
        if (annotationType == null || !Annotation.class.isAssignableFrom(annotationType)) {
            return null;
        }
        return (Class<A>) annotationType;
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToCharacterConverter.java`
#### Snippet
```java
        int length = length(source);
        if (length == 0) {
            return null;
        }
        if (length > 1) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToCharArrayConverter.java`
#### Snippet
```java
    @Override
    public char[] convert(String source) {
        return isNotEmpty(source) ? source.toCharArray() : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToDoubleConverter.java`
#### Snippet
```java
    @Override
    public Double convert(String source) {
        return isNotEmpty(source) ? valueOf(source) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToFloatConverter.java`
#### Snippet
```java
    @Override
    public Float convert(String source) {
        return isNotEmpty(source) ? valueOf(source) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToBooleanConverter.java`
#### Snippet
```java
    @Override
    public Boolean convert(String source) {
        return isNotEmpty(source) ? valueOf(source) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToLongConverter.java`
#### Snippet
```java
    @Override
    public Long convert(String source) {
        return isNotEmpty(source) ? valueOf(source) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/system/OperatingSystemBeanManager.java`
#### Snippet
```java
    private static Method deduceMethod(String name) {
        if (Objects.isNull(OPERATING_SYSTEM_BEAN_CLASS)) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/system/OperatingSystemBeanManager.java`
#### Snippet
```java
            return OPERATING_SYSTEM_BEAN_CLASS.getDeclaredMethod(name);
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/system/OperatingSystemBeanManager.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToIntegerConverter.java`
#### Snippet
```java
    @Override
    public Integer convert(String source) {
        return isNotEmpty(source) ? valueOf(source) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToShortConverter.java`
#### Snippet
```java
    @Override
    public Short convert(String source) {
        return isNotEmpty(source) ? valueOf(source) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/ConverterUtil.java`
#### Snippet
```java
            return (T) converter.convert(source);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/ConverterUtil.java`
#### Snippet
```java
            .collect(Collectors.toList()));

        return converters.size() > 0 ? converters.get(0) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToByteConverter.java`
#### Snippet
```java
    @Override
    public Byte convert(String source) {
        return StringUtils.isNotEmpty(source) ? Byte.valueOf(source) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToMultiValueConverter.java`
#### Snippet
```java

        if (isEmpty(source)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/MultiValueConverter.java`
#### Snippet
```java
            return (T) converter.convert(source, multiValueType, elementType);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
                }
                addProcessing(configDirectory);
                return null;
            });
        }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
            return 2;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
                            fireConfigChangeEvent(configDirectory, configFilePath.toFile(), configChangeType);
                            signalConfigDirectory(configDirectory);
                            return null;
                        });
                    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

                // Nothing to return
                return null;
            });
        });
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
    @Override
    public Object getInternalProperty(String key) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/DubboLoaderClassPath.java`
#### Snippet
```java
    public InputStream openClassfile(String classname) throws NotFoundException {
        if (!classname.startsWith("org.apache.dubbo") && !classname.startsWith("grpc.health") && !classname.startsWith("com.google")) {
            return null;
        }
        return super.openClassfile(classname);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/DubboLoaderClassPath.java`
#### Snippet
```java
    public URL find(String classname) {
        if (!classname.startsWith("org.apache.dubbo")) {
            return null;
        }
        return super.find(classname);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanDescriptor.java`
#### Snippet
```java
        if (isEnumType()) {
            Object result = getProperty(ENUM_PROPERTY_NAME);
            return result == null ? null : result.toString();
        }
        throw new IllegalStateException("The instance is not a enum wrapper");
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanDescriptor.java`
#### Snippet
```java
        if (isEnumType()) {
            Object result = setProperty(ENUM_PROPERTY_NAME, name);
            return result == null ? null : result.toString();
        }
        throw new IllegalStateException("The instance is not a enum wrapper");
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanDescriptor.java`
#### Snippet
```java
        if (isClassType()) {
            Object result = setProperty(CLASS_PROPERTY_NAME, name);
            return result == null ? null : result.toString();
        }
        throw new IllegalStateException("The instance is not a class wrapper");
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanDescriptor.java`
#### Snippet
```java
        if (isClassType()) {
            Object result = getProperty(CLASS_PROPERTY_NAME);
            return result == null ? null : result.toString();
        }
        throw new IllegalStateException("The instance is not a class wrapper");
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/AbstractCompiler.java`
#### Snippet
```java

    protected Class<?> doCompile(ClassLoader classLoader, String name, String source) throws Throwable {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
                        return cls.getMethod("getLength", new Class<?>[0]).getName() + "()";
                    } catch (NoSuchMethodException e4) {
                        return null;
                    }
                }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
    public static String getSimpleClassName(String qualifiedName) {
        if (null == qualifiedName) {
            return null;
        }
        int i = qualifiedName.lastIndexOf('.');
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public String getPath() {
        return urlAddress == null ? null : urlAddress.getPath();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public String getAddress() {
        return urlAddress == null ? null : urlAddress.getAddress();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        String inf = getServiceInterface();
        if (inf == null) {
            return null;
        }
        serviceKey = buildKey(inf, getGroup(), getVersion());
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public String getProtocol() {
        return urlAddress == null ? null : urlAddress.getProtocol();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public Object getAttribute(String key) {
        return attributes == null ? null : attributes.get(key);
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public String getIp() {
        return urlAddress == null ? null : urlAddress.getIp();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public String getHost() {
        return urlAddress == null ? null : urlAddress.getHost();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        }

        return ret.length() == 0 ? null : ret.toString();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        String inf = StringUtils.isNotEmpty(getPath()) ? getPath() : getServiceInterface();
        if (inf == null) {
            return null;
        }
        return buildKey(inf, getGroup(), getVersion());
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        String value = getParameterAndDecoded(key);
        if (StringUtils.isEmpty(value)) {
            return null;
        }
        return URL.valueOf(value);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        }

        return ret.length() == 0 ? null : ret.toString();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public String getPassword() {
        return urlAddress == null ? null : urlAddress.getPassword();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public String getUsername() {
        return urlAddress == null ? null : urlAddress.getUsername();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
        @Override
        public Class<?> getPropertyType(String pn) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
            return (char) 0;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
    public static Object deserialize(JavaBeanDescriptor beanDescriptor, ClassLoader loader) {
        if (beanDescriptor == null) {
            return null;
        }
        IdentityHashMap<JavaBeanDescriptor, Object> cache = new IdentityHashMap<JavaBeanDescriptor, Object>();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
    public static JavaBeanDescriptor serialize(Object obj, JavaBeanAccessor accessor) {
        if (obj == null) {
            return null;
        }
        if (obj instanceof JavaBeanDescriptor) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionAccessor.java`
#### Snippet
```java
    default <T> T getExtension(Class<T> type, String name) {
        ExtensionLoader<T> extensionLoader = getExtensionLoader(type);
        return extensionLoader != null ? extensionLoader.getExtension(name) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionAccessor.java`
#### Snippet
```java
    default <T> T getDefaultExtension(Class<T> type) {
        ExtensionLoader<T> extensionLoader = getExtensionLoader(type);
        return extensionLoader != null ? extensionLoader.getDefaultExtension() : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionAccessor.java`
#### Snippet
```java
    default <T> T getAdaptiveExtension(Class<T> type) {
        ExtensionLoader<T> extensionLoader = getExtensionLoader(type);
        return extensionLoader != null ? extensionLoader.getAdaptiveExtension() : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/LoadingStrategy.java`
#### Snippet
```java
    default String[] includedPackagesInCompatibleType() {
        // default match all
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/LoadingStrategy.java`
#### Snippet
```java

    default String[] excludedPackages() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/LoadingStrategy.java`
#### Snippet
```java
    default String[] includedPackages() {
        // default match all
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/inject/SpiExtensionInjector.java`
#### Snippet
```java
    public <T> T getInstance(final Class<T> type, final String name) {
        if (!type.isInterface() || !type.isAnnotationPresent(SPI.class)) {
            return null;
        }
        ExtensionLoader<T> loader = extensionAccessor.getExtensionLoader(type);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/inject/SpiExtensionInjector.java`
#### Snippet
```java
        ExtensionLoader<T> loader = extensionAccessor.getExtensionLoader(type);
        if (loader == null) {
            return null;
        }
        if (!loader.getSupportedExtensions().isEmpty()) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/inject/SpiExtensionInjector.java`
#### Snippet
```java
            return loader.getAdaptiveExtension();
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/support/ActivateComparator.java`
#### Snippet
```java
    private Class<?> findSpi(Class<?> clazz) {
        if (clazz.getInterfaces().length == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/support/ActivateComparator.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/Ring.java`
#### Snippet
```java
    public T peekItem() {
        if (itemList.isEmpty()) {
            return null;
        }
        if (itemList.size() == 1) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/Ring.java`
#### Snippet
```java
    public T pollItem() {
        if (itemList.isEmpty()) {
            return null;
        }
        if (itemList.size() == 1) {
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocal.java`
#### Snippet
```java
    @Override
    protected V initialValue() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocal.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
        ModuleConfig moduleConfig = moduleModel.getConfigManager().getModule().orElse(null);
        if (moduleConfig == null) {
            return null;
        }
        Integer threadNum = moduleConfig.getReferThreadNum();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                "before coming to here.");

            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
    @Parameter(excluded = true, attribute = false)
    public Boolean isGeneric() {
        return this.generic != null ? ProtocolUtils.isGeneric(generic) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
        ModuleConfig moduleConfig = moduleModel.getConfigManager().getModule().orElse(null);
        if (moduleConfig == null) {
            return null;
        }
        Integer threadNum = moduleConfig.getExportThreadNum();
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ModuleConfig.java`
#### Snippet
```java

    public RegistryConfig getRegistry() {
        return CollectionUtils.isEmpty(registries) ? null : registries.get(0);
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    public AsyncMethodInfo convertMethodConfig2AsyncInfo() {
        if ((getOninvoke() == null && getOnreturn() == null && getOnthrow() == null)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ApplicationConfig.java`
#### Snippet
```java

    public RegistryConfig getRegistry() {
        return CollectionUtils.isEmpty(registries) ? null : registries.get(0);
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
    @Parameter(excluded = true, attribute = false)
    public String getUniqueServiceName() {
        return interfaceName != null ? URL.buildKey(interfaceName, getGroup(), getVersion()) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java

    public ProtocolConfig getProtocol() {
        return CollectionUtils.isEmpty(protocols) ? null : protocols.get(0);
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    @Parameter(excluded = true, attribute = false)
    public String getUniqueServiceName() {
        return interfaceName != null ? URL.buildKey(interfaceName, getGroup(), getVersion()) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
            throw new IllegalStateException(t.getMessage(), t);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
                }
            } catch (ClassNotFoundException e) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        getExtensionClasses();
        if (StringUtils.isBlank(cachedDefaultName) || "true".equals(cachedDefaultName)) {
            return null;
        }
        return getExtension(cachedDefaultName);
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/TypeDefinitionBuilder.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
        Map<String, AbstractInterfaceConfig> configCache;
        if (config instanceof ReferenceConfigBase) {
            return null;
        } else if (config instanceof ServiceConfigBase) {
            ServiceConfigBase serviceConfig = (ServiceConfigBase) config;
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/util/ClassUtils.java`
#### Snippet
```java
        ProtectionDomain protectionDomain = clazz.getProtectionDomain();
        if (protectionDomain == null || protectionDomain.getCodeSource() == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/util/ClassUtils.java`
#### Snippet
```java
        URL location = codeSource.getLocation();
        if (location == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
            return metadataReportConfigs.iterator().next();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
            return configCenterConfigs.iterator().next();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java

    public RegistryConfig getRegistry() {
        return CollectionUtils.isEmpty(registries) ? null : registries.get(0);
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java

    public URL toUrl() {
        return urls.isEmpty() ? null : urls.iterator().next();
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
        if (size < 1) {
//                throw new IllegalStateException("No such " + configType.getName() + " is found");
            return null;
        } else if (size > 1) {
            throw new IllegalStateException("Expected single instance of " + configType + ", but found " + size +
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
            return ReflectUtils.getProperty(config, CONFIG_NAME_READ_METHOD);
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
    public final <T extends AbstractConfig> T addConfig(AbstractConfig config) {
        if (config == null) {
            return null;
        }
        // ignore MethodConfig
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
        Map<String, ? extends C> configsMap = getConfigsMap(cls);
        if (configsMap.isEmpty()) {
            return null;
        }
        // try to find config by name
```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
    static NettyChannel getOrAddChannel(Channel ch, URL url, ChannelHandler handler) {
        if (ch == null) {
            return null;
        }
        NettyChannel ret = CHANNEL_MAP.get(ch);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettySslContextOperator.java`
#### Snippet
```java
    @Override
    public SslContext buildContext() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java
        Channel c = channel;
        if (c == null) {
            return null;
        }
        return NettyChannel.getOrAddChannel(c, getUrl(), this);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConnectionClient.java`
#### Snippet
```java
        io.netty.channel.Channel c = getNettyChannel();
        if (c == null) {
            return null;
        }
        return NettyChannel.getOrAddChannel(c, getUrl(), this);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyBackedChannelBuffer.java`
#### Snippet
```java
    @Override
    public ChannelBufferFactory factory() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/ssl/SslContexts.java`
#### Snippet
```java
        Cert consumerConnectionConfig = certManager.getConsumerConnectionConfig(url);
        if (consumerConnectionConfig == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java
        Channel c = channel;
        if (c == null || !c.isConnected()) {
            return null;
        }
        return NettyChannel.getOrAddChannel(c, getUrl(), this);
```

### ReturnNull
Return of `null`
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosConfigServiceWrapper.java`
#### Snippet
```java
    private String handleInnerSymbol(String data) {
        if (data == null) {
            return null;
        }
        return data.replace(INNERCLASS_SYMBOL, INNERCLASS_COMPATIBLE_SYMBOL).replace(SLASH_CHAR, HYPHEN_CHAR);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
    static NettyChannel getOrAddChannel(org.jboss.netty.channel.Channel ch, URL url, ChannelHandler handler) {
        if (ch == null) {
            return null;
        }
        NettyChannel ret = CHANNEL_MAP.get(ch);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/logging/MessageFormatter.java`
#### Snippet
```java
    static Throwable getThrowableCandidate(Object[] argArray) {
        if (ArrayUtils.isEmpty(argArray)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/logging/MessageFormatter.java`
#### Snippet
```java
            return (Throwable) lastEntry;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/metadata/DubboServicesMetadata.java`
#### Snippet
```java
        }

        return null;

    }
```

### ReturnNull
Return of `null`
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
            logger.error(CONFIG_ERROR_NACOS, "", "", e.getMessage(), e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
        @Override
        public Executor getExecutor() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
            logger.error(CONFIG_ERROR_NACOS, "", "", e.getMessage(), e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleProtocol.java`
#### Snippet
```java
    @Override
    protected <T> Invoker<T> protocolBindingRefer(Class<T> type, URL url) throws RpcException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/ReflectionAbstractServerCall.java`
#### Snippet
```java
        trySetListener();
        if (isClosed()) {
            return null;
        }
        ClassLoadUtil.switchContextLoader(
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/ReflectionAbstractServerCall.java`
#### Snippet
```java
        ProviderModel providerModel = (ProviderModel) url.getServiceModel();
        if (providerModel == null || providerModel.getServiceModel() == null) {
            return null;
        }
        return providerModel.getServiceModel();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/AbstractServerCall.java`
#### Snippet
```java
                .withCause(e));
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/AbstractServerCall.java`
#### Snippet
```java
    protected Long parseTimeoutToMills(String timeoutVal) {
        if (StringUtils.isEmpty(timeoutVal) || StringUtils.isContains(timeoutVal, "null")) {
            return null;
        }
        long value = Long.parseLong(timeoutVal.substring(0, timeoutVal.length() - 1));
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/AbstractServerCall.java`
#### Snippet
```java
            default:
                // invalid timeout config
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/SchemaDescriptorRegistry.java`
#### Snippet
```java
        Map<Integer, FileDescriptor> ret = EXTENSIONS.get(extension);
        if (ret == null) {
            return null;
        } else {
            return ret.keySet();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/transport/TripleIsolationExecutorSupport.java`
#### Snippet
```java
    protected ServiceKey getServiceKey(Object data) {
        if (!(data instanceof Http2Headers)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleClientStream.java`
#### Snippet
```java
        private TriRpcStatus getStatusFromTrailers(Map<String, String> metadata) {
            if (null == metadata) {
                return null;
            }
            if (!getGrpcStatusDetailEnabled()){
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleClientStream.java`
#### Snippet
```java
            }
            if (!getGrpcStatusDetailEnabled()){
                return null;
            }
            // second get status detail
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleClientStream.java`
#### Snippet
```java
            // second get status detail
            if (!metadata.containsKey(TripleHeaderEnum.STATUS_DETAIL_KEY.getHeader())) {
                return null;
            }
            final String raw = (metadata.remove(TripleHeaderEnum.STATUS_DETAIL_KEY.getHeader()));
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleClientStream.java`
#### Snippet
```java
                return status;
            } catch (IOException ioException) {
                return null;
            } finally {
                ClassLoadUtil.switchContextLoader(tccl);
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleClientStream.java`
#### Snippet
```java
                    .withDescription("invalid content-type: " + contentType);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/DeCompressor.java`
#### Snippet
```java
    static DeCompressor getCompressor(FrameworkModel frameworkModel, String compressorStr) {
        if (null == compressorStr) {
            return null;
        }
        if (compressorStr.equals(Identity.MESSAGE_ENCODING)) {
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Compressor.java`
#### Snippet
```java
    static Compressor getCompressor(FrameworkModel frameworkModel, String compressorStr) {
        if (null == compressorStr) {
            return null;
        }
        if (compressorStr.equals(Identity.MESSAGE_ENCODING)) {
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Router.java`
#### Snippet
```java
    @Deprecated
    default <T> List<Invoker<T>> route(List<Invoker<T>> invokers, URL url, Invocation invocation) throws RpcException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
            return client.getChildren().forPath(path);
        } catch (NoNodeException e) {
            return null;
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        try {
            byte[] dataBytes = client.getData().forPath(path);
            return (dataBytes == null || dataBytes.length == 0) ? null : new String(dataBytes, CHARSET);
        } catch (NoNodeException e) {
            // ignore NoNode Exception.
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
            throw new IllegalStateException(e.getMessage(), e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
            return client.getChildren().usingWatcher(listener).forPath(path);
        } catch (NoNodeException e) {
            return null;
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/MergerFactory.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/model/Tag.java`
#### Snippet
```java
                        logger.error(CLUSTER_FAILED_RULE_PARSING, " Failed to parse tag rule ", String.valueOf(objectMap), "Error occurred when parsing rule component.", e);
                    }
                    return null;
                }).collect(Collectors.toList()));
            } else {
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshAppRuleListener.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/TailStateRouter.java`
#### Snippet
```java
    @Override
    public URL getUrl() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
        } while (true);

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/ForkingClusterInvoker.java`
#### Snippet
```java
                CompletableFuture.<Object>supplyAsync(() -> {
                    if (ref.size() > 0) {
                        return null;
                    }
                    return invokeWithContextAsync(invoker, invocation, consumerUrl);
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java

        if (CollectionUtils.isEmpty(invokers)) {
            return null;
        }
        if (invokers.size() == 1) {
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java

        if (CollectionUtils.isEmpty(invokers)) {
            return null;
        }
        String methodName = invocation == null ? StringUtils.EMPTY_STRING : invocation.getMethodName();
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java

        // 5. No invoker match, return null.
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/governance/DefaultGovernanceRuleRepositoryImpl.java`
#### Snippet
```java
            return dynamicConfiguration.getConfig(key, group, timeout);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/AbstractLoadBalance.java`
#### Snippet
```java
    public <T> Invoker<T> select(List<Invoker<T>> invokers, URL url, Invocation invocation) {
        if (CollectionUtils.isEmpty(invokers)) {
            return null;
        }
        if (invokers.size() == 1) {
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/AbstractCluster.java`
#### Snippet
```java
        protected Result doInvoke(Invocation invocation, List<Invoker<T>> invokers, LoadBalance loadbalance) throws RpcException {
            // The only purpose is to build an interceptor chain, so the cluster related logic doesn't matter.
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/AbstractCluster.java`
#### Snippet
```java
        @Override
        protected Result doInvoke(Invocation invocation, List<Invoker<T>> invokers, LoadBalance loadbalance) throws RpcException {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/AbstractCluster.java`
#### Snippet
```java
        @Override
        protected Result doInvoke(Invocation invocation, List<Invoker<T>> invokers, LoadBalance loadbalance) throws RpcException {
           return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/RoundRobinLoadBalance.java`
#### Snippet
```java
            return map.keySet();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/parser/ConfigParser.java`
#### Snippet
```java
            }
        } catch (Throwable t) {
            return null;
        }
        return urls;
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java
    @Override
    public Collection<Channel> getChannels() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java
    @Override
    public Channel getChannel(InetSocketAddress remoteAddress) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java
    @Override
    public ChannelHandler getChannelHandler() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
    private String handleInnerSymbol(String serviceName) {
        if (StringUtils.isEmpty(serviceName)) {
            return null;
        }
        return serviceName.replace(INNERCLASS_SYMBOL, INNERCLASS_COMPATIBLE_SYMBOL);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java
                    public byte[] getBody() throws IOException {
                        ResponseBody body = response.body();
                        return body == null ? null : body.bytes();
                    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

        if (!getAllHeaders().containsKey(CONTENT_LENGTH)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

            if (headers.isEmpty()) {
                return null;
            }
            String[] strings = headers.toArray(new String[0]);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboIsolationExecutorSupport.java`
#### Snippet
```java
    protected ServiceKey getServiceKey(Object data) {
        if (!(data instanceof Request)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboIsolationExecutorSupport.java`
#### Snippet
```java
        Request request = (Request) data;
        if (!(request.getData() instanceof Invocation)) {
            return null;
        }
        Invocation inv = (Invocation) request.getData();
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/filter/FutureFilter.java`
#### Snippet
```java
        ServiceModel serviceModel = invocation.getServiceModel();
        if (!(serviceModel instanceof ConsumerModel)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/LazyConnectExchangeClient.java`
#### Snippet
```java
    public Object getAttribute(String key) {
        if (client == null) {
            return null;
        } else {
            return client.getAttribute(key);
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-injvm/src/main/java/org/apache/dubbo/rpc/protocol/injvm/DefaultParamDeepCopyUtil.java`
#### Snippet
```java
            return (T) src;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
            case CallbackServiceCodec.CALLBACK_CREATE:
                inv.setAttachment(INV_ATT_CALLBACK_KEY + paraIndex, exportOrUnexportCallbackService(channel, inv, url, pts[paraIndex], args[paraIndex], true));
                return null;
            case CallbackServiceCodec.CALLBACK_DESTROY:
                inv.setAttachment(INV_ATT_CALLBACK_KEY + paraIndex, exportOrUnexportCallbackService(channel, inv, url, pts[paraIndex], args[paraIndex], false));
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
            case CallbackServiceCodec.CALLBACK_DESTROY:
                inv.setAttachment(INV_ATT_CALLBACK_KEY + paraIndex, exportOrUnexportCallbackService(channel, inv, url, pts[paraIndex], args[paraIndex], false));
                return null;
            default:
                return args[paraIndex];
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-injvm/src/main/java/org/apache/dubbo/rpc/protocol/injvm/InjvmProtocol.java`
#### Snippet
```java

        if (result == null) {
            return null;
        } else if (ProtocolUtils.isGeneric(
                result.getInvoker().getUrl().getParameter(GENERIC_KEY))) {
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-injvm/src/main/java/org/apache/dubbo/rpc/protocol/injvm/InjvmProtocol.java`
#### Snippet
```java
        } else if (ProtocolUtils.isGeneric(
                result.getInvoker().getUrl().getParameter(GENERIC_KEY))) {
            return null;
        } else {
            return result;
```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-injvm/src/main/java/org/apache/dubbo/rpc/protocol/injvm/InjvmInvoker.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
                String method = url.getParameter(methodKey);
                if (method == null || method.length() == 0) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
                            + " please update the api interface. url is:"
                            + invoker.getUrl()) + " ,invocation is :" + inv);
                        return null;
                    }
                }
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
            List<XdsRouteRule> xdsRouteRules = virtualHost.getRoutesList().stream().map(route -> {
                if (route.getMatch().getQueryParametersCount() != 0) {
                    return null;
                }
                HttpRequestMatch match = parseMatch(route.getMatch());
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
                return pathMatcher;
            case PATHSPECIFIER_NOT_SET:
                return null;
            default:
                throw new IllegalArgumentException("Path specifier is not expect");
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
        HttpRequestMatch requestMatch = rule.getMatch();
        if (requestMatch.getPathMatcher() == null && CollectionUtils.isEmpty(requestMatch.getHeaderMatcherList())) {
            return null;
        }
        PathMatcher pathMatcher = requestMatch.getPathMatcher();
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
            String path = "/" + invocation.getInvoker().getUrl().getPath() + "/" + invocation.getMethodName();
            if (!pathMatcher.isMatch(path)) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
            // not support byte
            if (headerName.endsWith(BINARY_HEADER_SUFFIX)) {
                return null;
            }
            String headValue = invocation.getAttachment(headerName);
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
            String headValue = invocation.getAttachment(headerName);
            if (!headerMatcher.match(headValue)) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/impl/EdsProtocol.java`
#### Snippet
```java
        } catch (InvalidProtocolBufferException e) {
            logger.error(REGISTRY_ERROR_RESPONSE_XDS, "", "", "Error occur when decode xDS response.", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/impl/LdsProtocol.java`
#### Snippet
```java
        try {
            if (!any.is(HttpConnectionManager.class)) {
                return null;
            }
            return any.unpack(HttpConnectionManager.class);
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/impl/LdsProtocol.java`
#### Snippet
```java
        } catch (InvalidProtocolBufferException e) {
            logger.error(REGISTRY_ERROR_RESPONSE_XDS, "", "", "Error occur when decode xDS response.", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/impl/LdsProtocol.java`
#### Snippet
```java
        } catch (InvalidProtocolBufferException e) {
            logger.error(REGISTRY_ERROR_RESPONSE_XDS, "", "", "Error occur when decode xDS response.", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/impl/RdsProtocol.java`
#### Snippet
```java
        } catch (InvalidProtocolBufferException e) {
            logger.error(REGISTRY_ERROR_RESPONSE_XDS, "", "", "Error occur when decode xDS response.", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-demo/dubbo-demo-api/dubbo-demo-api-provider/src/main/java/org/apache/dubbo/demo/provider/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<String> sayHelloAsync(String name) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    @Override
    public String getCluster() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
            caFile = new File(IstioConstant.ISTIO_CA_PATH);
        } else {
            return null;
        }
        if (caFile.canRead()) {
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
    public T getCacheResource(String resourceName) {
        if (resourceName == null || resourceName.length() == 0) {
            return null;
        }
        return resourcesMap.get(resourceName);
```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/TraceTelnetHandler.java`
#### Snippet
```java
            return "No such service " + service;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
        ExpiryObject answer = delegateMap.remove(key);
        if (answer == null) {
            return null;
        }
        return answer.getValue();
```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
            if (timeToLive > 0 && timeIdle >= timeToLive * 1000L) {
                delegateMap.remove(object.getKey());
                return null;
            }
            object.setLastAccessTime(System.currentTimeMillis());
```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
            return object.getValue();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        Class<?> cached = methodClassMap.get(methodClassName);
        if (cached != null) {
            return cached == clazz ? null : cached;
        }
        try {
```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
    private static Object getMethodParameterBean(Class<?> clazz, Method method, Object[] args) {
        if (!hasConstraintParameter(method)) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } catch (Throwable e) {
            logger.warn(CONFIG_FILTER_VALIDATION_EXCEPTION, "", "", e.getMessage(), e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        Class<?> cached = methodClassMap.get(methodClassName);
        if (cached != null) {
            return cached == clazz ? null : cached;
        }
        try {
```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
    private static Object getMethodParameterBean(Class<?> clazz, Method method, Object[] args) {
        if (!hasConstraintParameter(method)) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } catch (Throwable e) {
            logger.warn(CONFIG_FILTER_VALIDATION_EXCEPTION, "", "", e.getMessage(), e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/util/CommandHelper.java`
#### Snippet
```java
            return frameworkModel.getExtensionLoader(BaseCommand.class).getExtension(commandName).getClass();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/nop/NopMetricsReporter.java`
#### Snippet
```java
    @Override
    public String getResponse() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/AbstractMonitorFactory.java`
#### Snippet
```java
                } catch (Throwable e) {
                    logger.warn(COMMON_MONITOR_EXCEPTION, "", "", "Create monitor failed, monitor data will not be collected until you fix this problem. monitorUrl: " + monitorUrl, e);
                    return null;
                }
            });
```

### ReturnNull
Return of `null`
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/AbstractMonitorFactory.java`
#### Snippet
```java
            });
            FUTURES.put(key, future);
            return null;
        } finally {
            // unlock
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/ServiceNameMapping.java`
#### Snippet
```java
        String providedBy = consumerURL.getParameter(RegistryConstants.PROVIDED_BY);
        if(StringUtils.isBlank(providedBy)) {
            return null;
        }
        return AbstractServiceNameMapping.parseServices(providedBy);
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/DefaultServiceRestMetadataResolver.java`
#### Snippet
```java
    @Override
    protected String resolveRequestMethod(Method serviceMethod, Class<?> serviceType, Class<?> serviceInterfaceClass) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/DefaultServiceRestMetadataResolver.java`
#### Snippet
```java
    @Override
    protected String resolveRequestPath(Method serviceMethod, Class<?> serviceType, Class<?> serviceInterfaceClass) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/RequestBodyParameterProcessor.java`
#### Snippet
```java
    @Override
    protected String getDefaultValue(Annotation annotation, Parameter parameter, int parameterIndex) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/RequestBodyParameterProcessor.java`
#### Snippet
```java
    @Override
    protected String getAnnotationValue(Annotation annotation, Parameter parameter, int parameterIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/PathVariableParameterProcessor.java`
#### Snippet
```java
    @Override
    protected String getDefaultValue(Annotation annotation, Parameter parameter, int parameterIndex) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java
    @Override
    public List<String> getSubscribedURLs(SubscriberMetadataIdentifier subscriberMetadataIdentifier) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java
    @Override
    public String getServiceDefinition(MetadataIdentifier metadataIdentifier) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java
    @Override
    public List<String> getExportedURLs(ServiceMetadataIdentifier metadataIdentifier) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/MetadataReport.java`
#### Snippet
```java

    default MetadataInfo getAppMetadata(SubscriberMetadataIdentifier identifier, Map<String, String> instanceMetadata) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosConfigServiceWrapper.java`
#### Snippet
```java
    private String handleInnerSymbol(String data) {
        if (data == null) {
            return null;
        }
        return data.replace(INNERCLASS_SYMBOL, INNERCLASS_COMPATIBLE_SYMBOL).replace(SLASH_CHAR, HYPHEN_CHAR);
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/NotifyListener.java`
#### Snippet
```java

    default URL getConsumerUrl() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/NotifyListener.java`
#### Snippet
```java

    default ServiceInstancesChangedListener getServiceListener() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java

        if (httpMethod == null || getLength(httpMethod) < 1) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
        @Override
        public Executor getExecutor() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
            logger.error(REGISTRY_NACOS_EXCEPTION, "", "", e.getMessage());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/NopServiceDiscovery.java`
#### Snippet
```java
    @Override
    public List<ServiceInstance> getInstances(String serviceName) throws NullPointerException {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/NopServiceDiscovery.java`
#### Snippet
```java
    @Override
    public Set<String> getServices() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
        String protocolServiceKey = getProtocolServiceKey();
        if (isEmpty(protocolServiceKey)) {
            return null;
        }
        return getServiceMethodParameter(protocolServiceKey, method, key);
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
            MetadataInfo.ServiceInfo serviceInfo = getServiceInfo(protocolServiceKey);
            if (null == serviceInfo) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
            return metadataInfoStat.getMetadataInfo();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
    public String removeExtendParam(String key) {
        if (extendParams == null) {
            return null;
        }
        return extendParams.remove(key);
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
    public String getExtendParam(String key) {
        if (extendParams == null) {
            return null;
        }
        return extendParams.get(key);
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscovery.java`
#### Snippet
```java

    default URL getUrl() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
    private static String getFirst(Map<String, List<String>> multiValueMap, String key) {
        List<String> values = get(multiValueMap, key);
        return CollectionUtils.isNotEmpty(values) ? values.get(0) : null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/ServiceInstanceMetadataUtils.java`
#### Snippet
```java

        if (params.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/ServiceInstanceMetadataUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataServiceDelegation.java`
#### Snippet
```java
    public MetadataInfo getMetadataInfo(String revision) {
        if (StringUtils.isEmpty(revision)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataServiceDelegation.java`
#### Snippet
```java
            logger.warn(REGISTRY_FAILED_LOAD_METADATA, "", "", "metadata not found for revision: " + revision);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    public String getParameter(String key, String serviceKey) {
        ServiceInfo serviceInfo = getValidServiceInfo(serviceKey);
        if (serviceInfo == null) return null;
        return serviceInfo.getParameter(key);
    }
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
            serviceInfo = getNoProtocolServiceInfo(serviceKey);
            if (serviceInfo == null) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    public ServiceInfo getNoProtocolServiceInfo(String serviceKeyWithoutProtocol) {
        if (CollectionUtils.isEmptyMap(subscribedServices)) {
            return null;
        }
        Set<ServiceInfo> subServices = subscribedServices.get(serviceKeyWithoutProtocol);
```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
           return subServices.iterator().next();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    public String getServiceString(String protocolServiceKey) {
        if (protocolServiceKey == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        ServiceInfo serviceInfo = getValidServiceInfo(protocolServiceKey);
        if (serviceInfo == null) {
            return null;
        }
        return serviceInfo.toFullString();
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/SkipFailbackWrapperException.java`
#### Snippet
```java
    public synchronized Throwable fillInStackTrace() {
        // do nothing
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java
        @Override
        public List<URL> lookup(URL url) {
            return null;
        }
    };
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java
        @Override
        public URL getUrl() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java
            return DEFAULT_NOP_REGISTRY;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationInvoker.java`
#### Snippet
```java
            return serviceDiscoveryInvoker.getDirectory();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/model/MigrationRule.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/DefaultCodec.java`
#### Snippet
```java
    @Override
    public Object decode(Channel channel, ChannelBuffer buffer) throws IOException {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetHandlerAdapter.java`
#### Snippet
```java
                        String result = extensionLoader.getExtension(command).telnet(channel, message);
                        if (result == null) {
                            return null;
                        }
                        buf.append(result);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/ExitTelnetHandler.java`
#### Snippet
```java
    public String telnet(Channel channel, String message) {
        channel.close();
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Request.java`
#### Snippet
```java
    private static String safeToString(Object data) {
        if (data == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/ExchangeHandlerAdapter.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Object> reply(ExchangeChannel channel, Object msg) throws RemotingException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                }
                channel.close();
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                }
                channel.close();
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
        DefaultFuture future = DefaultFuture.getFuture(id);
        if (future == null) {
            return null;
        }
        Request req = future.getRequest();
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
        Request req = future.getRequest();
        if (req == null) {
            return null;
        }
        return req.getData();
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeChannel.java`
#### Snippet
```java
    static HeaderExchangeChannel getOrAddChannel(Channel ch) {
        if (ch == null) {
            return null;
        }
        HeaderExchangeChannel ret = (HeaderExchangeChannel) ch.getAttribute(CHANNEL_KEY);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
        Channel channel = getChannel();
        if (channel == null) {
            return null;
        }
        return channel.getAttribute(key);
```

### ReturnNull
Return of `null`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperClient.java`
#### Snippet
```java
    public String getContent(String path) {
        if (!checkExists(path)) {
            return null;
        }
        return doGetContent(path);
```

### ReturnNull
Return of `null`
in `dubbo-serialization/dubbo-serialization-jdk/src/main/java/org/apache/dubbo/common/serialize/java/JavaObjectInput.java`
#### Snippet
```java
        int len = getObjectInputStream().readInt();
        if (len < 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-serialization/dubbo-serialization-jdk/src/main/java/org/apache/dubbo/common/serialize/java/JavaObjectInput.java`
#### Snippet
```java
        int len = getObjectInputStream().readInt();
        if (len < 0) {
            return null;
        }
        if (len == 0) {
```

### ReturnNull
Return of `null`
in `dubbo-serialization/dubbo-serialization-jdk/src/main/java/org/apache/dubbo/common/serialize/java/JavaObjectInput.java`
#### Snippet
```java
        byte b = getObjectInputStream().readByte();
        if (b == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `dubbo-serialization/dubbo-serialization-jdk/src/main/java/org/apache/dubbo/common/serialize/nativejava/NativeJavaObjectInput.java`
#### Snippet
```java
        int len = inputStream.readInt();
        if (len < 0) {
            return null;
        } else if (len == 0) {
            return new byte[]{};
```

### ReturnNull
Return of `null`
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2SecurityManager.java`
#### Snippet
```java

            if (clazz == DenyClass.class) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2SecurityManager.java`
#### Snippet
```java
                }

                return null;
            }

```

### ReturnNull
Return of `null`
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2SecurityManager.java`
#### Snippet
```java

            // 4. class not found
            return null;
        }

```

## RuleId[id=AssignmentToLambdaParameter]
### AssignmentToLambdaParameter
Assignment to lambda parameter `t`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
            if (t != null) {
                if (t instanceof CompletionException) {
                    t = t.getCause();
                }
                this.completeExceptionally(t);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `s1`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
        int i1 = s1.lastIndexOf('.');
        if (i1 >= 0) {
            s1 = s1.substring(i1 + 1);
        }
        int i2 = s2.lastIndexOf('.');
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `s2`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
        int i2 = s2.lastIndexOf('.');
        if (i2 >= 0) {
            s2 = s2.substring(i2 + 1);
        }
        return s1.compareToIgnoreCase(s2);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `key`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ApplicationConfig.java`
#### Snippet
```java
                            prefix += ".";
                            if (key.startsWith(prefix)) {
                                key = key.substring(prefix.length());
                            }
                            parameters.put(key, value);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `v`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/PilotExchanger.java`
#### Snippet
```java
        domainObserveConsumer.compute(domain, (k, v) -> {
            if (v == null) {
                v = new ConcurrentHashSet<>();
            }
            // support multi-consumer
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `v`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
                consumerObserveMap.compute(resourceNames, (k, v) -> {
                    if (v == null) {
                        v = new ArrayList<>();
                    }
                    // support multi-consumer
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `metricsInvoker` is redundant
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java

    private Invoker<MetricsService> initMetricsInvoker() {
        Invoker<MetricsService> metricsInvoker = new Invoker<MetricsService>() {
            @Override
            public Class<MetricsService> getInterface() {
```

### UnnecessaryLocalVariable
Local variable `cf` is redundant
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<String> sayHelloAsync(String name) {
        CompletableFuture<String> cf = CompletableFuture.supplyAsync(() -> {
//            try {
//                Thread.sleep(1000);
```

### UnnecessaryLocalVariable
Local variable `finalAccessLogKey` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AccessLogFilter.java`
#### Snippet
```java
            return invoker.invoke(inv);
        } finally {
            String finalAccessLogKey = accessLogKey;
            optionalAccessLogData.ifPresent(logData -> {
                logData.setOutTime(new Date());
```

### UnnecessaryLocalVariable
Local variable `refClient` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestProtocol.java`
#### Snippet
```java
        RestClient restClient = clientFactory.createRestClient(url);

        ReferenceCountedClient refClient = new ReferenceCountedClient(restClient, clients, clientFactory, url);

        return refClient;
```

### UnnecessaryLocalVariable
Local variable `names` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
    public static Class findClass(ClassLoader classLoader, String... name) throws ClassNotFoundException {

        String[] names = name;

        Class tmp;
```

### UnnecessaryLocalVariable
Local variable `typeDefinition` is redundant
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/builder/PrimitiveTypeDefinitionBuilder.java`
#### Snippet
```java
    @Override
    public TypeDefinition build(ProcessingEnvironment processingEnv, PrimitiveType type, Map<String, TypeDefinition> typeCache) {
        TypeDefinition typeDefinition = new TypeDefinition(type.toString());
        return typeDefinition;
    }
```

### UnnecessaryLocalVariable
Local variable `typeDefinition` is redundant
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/builder/TypeDefinitionBuilder.java`
#### Snippet
```java
     */
    static TypeDefinition build(ProcessingEnvironment processingEnv, Element element, Map<String, TypeDefinition> typeCache) {
        TypeDefinition typeDefinition = build(processingEnv, element.asType(), typeCache);
        // Comment this code for the compatibility
        // typeDefinition.set$ref(element.toString());
```

### UnnecessaryLocalVariable
Local variable `td` is redundant
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/builder/SimpleTypeDefinitionBuilder.java`
#### Snippet
```java
    @Override
    public TypeDefinition build(ProcessingEnvironment processingEnv, DeclaredType type, Map<String, TypeDefinition> typeCache) {
        TypeDefinition td = new TypeDefinition(type.toString());
        return td;
    }
```

### UnnecessaryLocalVariable
Local variable `originalEventSource` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java
    private boolean isOriginalEventSource(ApplicationEvent event) {

        boolean originalEventSource = nullSafeEquals(getApplicationContext(), event.getSource());
        return originalEventSource;
    }
```

### UnnecessaryLocalVariable
Local variable `interfaceClass` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
        }

        Class interfaceClass = injectedType;

        // TODO Only register one reference bean for same (group, interface, version)
```

### UnnecessaryLocalVariable
Local variable `interfaceClass` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
            }

            Class interfaceClass = beanClass;

            // set attribute instead of property values
```

### UnnecessaryLocalVariable
Local variable `attributeValue` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java

    private static String resolveAttribute(Element element, String attributeName, ParserContext parserContext) {
        String attributeValue = element.getAttribute(attributeName);
        // Early resolve place holder may be wrong ( Before PropertySourcesPlaceholderConfigurer/PropertyPlaceholderConfigurer )
        //https://github.com/apache/dubbo/pull/6079
```

### UnnecessaryLocalVariable
Local variable `serviceConfig` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
        ServiceBuilder builder = createServiceBuilder(id);
        consumerBuilder.accept(builder);
        ServiceConfig serviceConfig = builder.build();
        return serviceConfig;
    }
```

### UnnecessaryLocalVariable
Local variable `consumerConfig` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
        ConsumerBuilder builder = createConsumerBuilder(id);
        builderConsumer.accept(builder);
        ConsumerConfig consumerConfig = builder.build();
        return consumerConfig;
    }
```

### UnnecessaryLocalVariable
Local variable `referenceConfig` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
        ReferenceBuilder builder = createReferenceBuilder(id);
        consumerBuilder.accept(builder);
        ReferenceConfig referenceConfig = builder.build();
        return referenceConfig;
    }
```

### UnnecessaryLocalVariable
Local variable `providerConfig` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
        ProviderBuilder builder = createProviderBuilder(id);
        builderConsumer.accept(builder);
        ProviderConfig providerConfig = builder.build();
        return providerConfig;
    }
```

### UnnecessaryLocalVariable
Local variable `future` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ExecutorUtil.java`
#### Snippet
```java

    public static void cancelScheduledFuture(ScheduledFuture<?> scheduledFuture) {
        ScheduledFuture<?> future = scheduledFuture;
        if (future != null && !future.isCancelled()) {
            future.cancel(true);
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     */
    public static Class<?>[] desc2classArray(String desc) throws ClassNotFoundException {
        Class<?>[] ret = desc2classArray(ClassUtils.getClassLoader(), desc);
        return ret;
    }
```

### UnnecessaryLocalVariable
Local variable `contentList` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        }

        List<String> contentList = urlListMap.computeIfAbsent(resourceURL, key -> {
            List<String> newContentList = new ArrayList<>();

```

### UnnecessaryLocalVariable
Local variable `finalInterfaceClass` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
                boolean ignoreInvalidMethodConfig = Boolean.parseBoolean(ignoreInvalidMethodConfigVal.toString());

                Class<?> finalInterfaceClass = interfaceClass;
                List<MethodConfig> validMethodConfigs = methodConfigs.stream().filter(methodConfig -> {
                    methodConfig.setParentPrefix(preferredPrefix);
```

### UnnecessaryLocalVariable
Local variable `spiClasses` is redundant
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ClassSourceScanner.java`
#### Snippet
```java
    public List<Class<?>> spiClassesWithAdaptive() {
        Map<String, Class<?>> allClasses = getClasses();
        List<Class<?>> spiClasses = new ArrayList<>(allClasses.values()).stream().filter(it -> {
            if (null == it) {
                return false;
```

### UnnecessaryLocalVariable
Local variable `client` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java

    public OkHttpClient createHttpClient(HttpClientConfig httpClientConfig) {
        OkHttpClient client = new OkHttpClient.Builder().
            readTimeout(httpClientConfig.getReadTimeout(), TimeUnit.SECONDS).
            writeTimeout(httpClientConfig.getWriteTimeout(), TimeUnit.SECONDS).
```

### UnnecessaryLocalVariable
Local variable `cf` is redundant
in `dubbo-test/dubbo-test-common/src/main/java/org/apache/dubbo/test/common/impl/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<String> sayHelloAsync(String name) {
        CompletableFuture<String> cf = CompletableFuture.supplyAsync(() -> {
//            try {
//                Thread.sleep(1000);
```

### UnnecessaryLocalVariable
Local variable `side` is redundant
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/filter/MethodMetricsInterceptor.java`
#### Snippet
```java
    private String getSide(Invocation invocation) {
        Optional<? extends Invoker<?>> invoker = Optional.ofNullable(invocation.getInvoker());
        String side = invoker.isPresent() ? invoker.get().getUrl().getSide() : PROVIDER_SIDE;
        return side;
    }
```

### UnnecessaryLocalVariable
Local variable `side` is redundant
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/filter/MetricsClusterFilter.java`
#### Snippet
```java
    private String getSide(Invocation invocation) {
        Optional<? extends Invoker<?>> invoker = Optional.ofNullable(invocation.getInvoker());
        String side = invoker.isPresent() ? invoker.get().getUrl().getSide() : PROVIDER_SIDE;
        return side;
    }
```

### UnnecessaryLocalVariable
Local variable `supportedExtensionInstances` is redundant
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java

    private static Set<NoAnnotatedParameterRequestTagProcessor> loadNoAnnotatedMethodParameterProcessors(ApplicationModel applicationModel) {
        Set<NoAnnotatedParameterRequestTagProcessor> supportedExtensionInstances = applicationModel.getExtensionLoader(NoAnnotatedParameterRequestTagProcessor.class)
            .getSupportedExtensionInstances();

```

### UnnecessaryLocalVariable
Local variable `urls` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
        Map<String, List<URL>> categoryNotified = notified.get(url);
        if (CollectionUtils.isNotEmptyMap(categoryNotified)) {
            List<URL> urls = categoryNotified.values().stream().flatMap(Collection::stream).collect(Collectors.toList());
            return urls;
        }
```

### UnnecessaryLocalVariable
Local variable `key` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    private String getCacheKey(final Invoker<?> originInvoker) {
        URL providerUrl = getProviderUrl(originInvoker);
        String key = providerUrl.removeParameters(DYNAMIC_KEY, ENABLED_KEY).toFullString();
        return key;
    }
```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/RunProcess.java`
#### Snippet
```java
			}
			try {
				Thread.sleep(500);
			}
			catch (InterruptedException ex) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
            }

            Thread.sleep(5000);
        }
    }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java

                try {
                    Thread.sleep(sleepTimeMs);
                } catch (InterruptedException ignored) {
                    if (WORKER_STATE_UPDATER.get(HashedWheelTimer.this) == WORKER_STATE_SHUTDOWN) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-demo/dubbo-demo-generic-call/src/main/java/org/apache/dubbo/demo/consumer/GenericApplication.java`
#### Snippet
```java
                    new Object[]{param});
                System.out.println(genericInvokeResult);
                Thread.sleep(1000);
            } catch (Exception e) {
                e.printStackTrace();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
                processExpires();
                try {
                    Thread.sleep(expirationIntervalMillis);
                } catch (InterruptedException e) {
                    running = false;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java
                        rule = ruleQueue.take();
                        if (StringUtils.isEmpty(rule)) {
                            Thread.sleep(1000);
                        }
                    } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeServer.java`
#### Snippet
```java
                && System.currentTimeMillis() - start < (long) timeout) {
                try {
                    Thread.sleep(10);
                } catch (InterruptedException e) {
                    logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeChannel.java`
#### Snippet
```java
                && System.currentTimeMillis() - start < timeout) {
                try {
                    Thread.sleep(10);
                } catch (InterruptedException e) {
                    logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/MediaTypeMatcher.java`
#### Snippet
```java
        for (MediaType mediaType : mediaTypes) {

            mediaTypeList.add(mediaType);
        }
        return mediaTypeList;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/jaxrs/JAXRSServiceRestMetadataResolver.java`
#### Snippet
```java
        String[] value = getValue(annotation);
        if (value != null) {
            Stream.of(value).forEach(result::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java

        if (isNotEmpty(mediaTypes)) {
            of(mediaTypes).forEach(mediaTypesSet::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
            // Add the declared methods or public methods
            for (Method method : methods) {
                allMethods.add(method);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/jaxrs/JAXRSServiceRestMetadataResolver.java`
#### Snippet
```java
        String[] value = getValue(annotation);
        if (value != null) {
            Stream.of(value).forEach(result::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/jaxrs/JAXRSServiceRestMetadataResolver.java`
#### Snippet
```java
        String[] value = getValue(annotation);
        if (value != null) {
            Stream.of(value).forEach(result::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java

        if (isNotEmpty(mediaTypes)) {
            of(mediaTypes).forEach(mediaTypesSet::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java

        if (isNotEmpty(mediaTypes)) {
            of(mediaTypes).forEach(mediaTypesSet::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
                // the initial capacity should be set greater than the maximum number of entries divided by the load factor to avoid resizing.
                oldUrlInvokerMap = new LinkedHashMap<>(Math.round(1 + localUrlInvokerMap.size() / DEFAULT_HASHMAP_LOAD_FACTOR));
                localUrlInvokerMap.forEach(oldUrlInvokerMap::put);
            }
            Map<ProtocolServiceKeyWithAddress, Invoker<T>> newUrlInvokerMap = toInvokers(oldUrlInvokerMap, invokerUrls);// Translate url list to Invoker map
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
                // the initial capacity should be set greater than the maximum number of entries divided by the load factor to avoid resizing.
                oldUrlInvokerMap = new LinkedHashMap<>(Math.round(1 + localUrlInvokerMap.size() / DEFAULT_HASHMAP_LOAD_FACTOR));
                localUrlInvokerMap.forEach(oldUrlInvokerMap::put);
            }
            Map<URL, Invoker<T>> newUrlInvokerMap = toInvokers(oldUrlInvokerMap, invokerUrls);// Translate url list to Invoker map
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
            }
            if (entry.getValue() == null) {
                equals = equals && (entry.getValue() == that.getMetadata().get(entry.getKey()));
            } else {
                equals = equals && entry.getValue().equals(that.getMetadata().get(entry.getKey()));
```

## RuleId[id=EmptyTryBlock]
### EmptyTryBlock
Empty `try` block
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            return false;
        }
        try (Socket socket = new Socket(url.getHost(), url.getPort())) {
        } catch (Throwable e) {
            try {
```

### EmptyTryBlock
Empty `try` block
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            } catch (Throwable e2) {
            }
            try (Socket socket2 = new Socket(url.getHost(), url.getPort())) {
            } catch (Throwable e2) {
                return true;
```

## RuleId[id=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
`result` is not of an array type
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/ArrayMerger.java`
#### Snippet
```java
        for (Object[] array : items) {
            if (array != null) {
                System.arraycopy(array, 0, result, index, array.length);
                index += array.length;
            }
```

## RuleId[id=RedundantClassCall]
### RedundantClassCall
Redundant call to `cast()`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return Short.toString(number.shortValue()).getBytes();
        } else if (number instanceof BigDecimal) {
            return BigDecimal.class.cast(number).toString().getBytes();
        }

```

### RedundantClassCall
Redundant call to `cast()`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java

        if (registry instanceof SingletonBeanRegistry) {
            SingletonBeanRegistry singletonBeanRegistry = SingletonBeanRegistry.class.cast(registry);
            beanNameGenerator = (BeanNameGenerator) singletonBeanRegistry.getSingleton(CONFIGURATION_BEAN_NAME_GENERATOR);
        }
```

### RedundantClassCall
Redundant call to `cast()`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/condition/CompatibleOnEnabledEndpointCondition.java`
#### Snippet
```java
            Class<?> cls = ClassUtils.resolveClassName(CONDITION_CLASS_NAME_OLD, classLoader);
            if (Condition.class.isAssignableFrom(cls)) {
                Condition condition = Condition.class.cast(BeanUtils.instantiateClass(cls));
                return condition.matches(context, metadata);
            }
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
            value = false;
        } else if (mock.length() >= 2 && (mock.startsWith("\"") && mock.endsWith("\"")
            || mock.startsWith("\'") && mock.endsWith("\'"))) {
            value = mock.subSequence(1, mock.length() - 1);
        } else if (returnTypes != null && returnTypes.length > 0 && returnTypes[0] == String.class) {
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
            value = false;
        } else if (mock.length() >= 2 && (mock.startsWith("\"") && mock.endsWith("\"")
            || mock.startsWith("\'") && mock.endsWith("\'"))) {
            value = mock.subSequence(1, mock.length() - 1);
        } else if (returnTypes != null && returnTypes.length > 0 && returnTypes[0] == String.class) {
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
                    def = "false";
                } else if (type == char.class) {
                    def = "\'\\0\'";
                } else if (type == byte.class
                        || type == short.class
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
                    def = "false";
                } else if (type == char.class) {
                    def = "\'\\0\'";
                } else if (type == byte.class
                        || type == short.class
```

## RuleId[id=ManualArrayCopy]
### ManualArrayCopy
Manual array copy
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/RouterSnapshotSwitcher.java`
#### Snippet
```java
    public String[] cloneSnapshot() {
        String[] clonedSnapshot = new String[MAX_LENGTH];
        for (int i = 0; i < MAX_LENGTH; i++) {
            clonedSnapshot[i] = recentSnapshot[i];
        }
```

## RuleId[id=ObsoleteCollection]
### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            result = new HashMap();
        } else if (Hashtable.class == cl) {
            result = new Hashtable();
        } else if (IdentityHashMap.class == cl) {
            result = new IdentityHashMap();
```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java

    @Override
    public BitList<E> clone() {
        return new BitList<>(originList, (BitSet) rootSet.clone(), tailList == null ? null : new LinkedList<>(tailList));
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java

    @Override
    public synchronized MetadataInfo clone() {
        return new MetadataInfo(app, revision, services, initiated, extendParams, instanceParams, updated, subscribedServiceURLs, exportedServiceURLs, loader);
    }
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java

    public static Method getMethod(Class clazz, String method) throws NoSuchMethodException {
        Method declaredMethod = clazz.getDeclaredMethod(method, new Class[]{});
        declaredMethod.setAccessible(true);
        return declaredMethod;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/status/SpringStatusChecker.java`
#### Snippet
```java
            while (cls != null && method == null) {
                try {
                    method = cls.getDeclaredMethod("getConfigLocations", new Class<?>[0]);
                } catch (NoSuchMethodException t) {
                    cls = cls.getSuperclass();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                Method getter = null;
                try {
                    getter = beanClass.getMethod("get" + name.substring(3), new Class<?>[0]);
                } catch (NoSuchMethodException e) {
                    try {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                } catch (NoSuchMethodException e) {
                    try {
                        getter = beanClass.getMethod("is" + name.substring(3), new Class<?>[0]);
                    } catch (NoSuchMethodException e2) {
                        // ignore, there is no need any log here since some class implement the interface: EnvironmentAware,
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/Page.java`
#### Snippet
```java
    public Page(String navigation, String title,
                String column, String row) {
        this(navigation, title, column == null ? null : Arrays.asList(new String[]{column}), row == null ? null : stringToList(row));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        Constructor<?> targetConstructor;
        try {
            targetConstructor = clazz.getConstructor(new Class<?>[]{paramType});
        } catch (NoSuchMethodException e) {
            targetConstructor = null;
```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`[,]` can be simplified to ','
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
        checkMultiName(property, value);
        if (isNotEmpty(value)) {
            String[] values = value.split("\\s*[,]+\\s*");
            for (String v : values) {
                if (v.startsWith(REMOVE_VALUE_PREFIX)) {
```

### RegExpSimplifiable
`[=]` can be simplified to '='
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(StringUtils.class);
    private static final Pattern KVP_PATTERN = Pattern.compile("([_.a-zA-Z0-9][-_.a-zA-Z0-9]*)[=](.*)"); //key value pair pattern.
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
```

### RegExpSimplifiable
`[-]` can be simplified to '-'
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeErrorTypeAwareLogger.java`
#### Snippet
```java
        }

        String[] segments = trimmedString.split("[-]");

        int[] errorCodeSegments = new int[2];
```

### RegExpSimplifiable
`[;]` can be simplified to ';'
in `dubbo-common/src/main/java/org/apache/dubbo/common/constants/CommonConstants.java`
#### Snippet
```java
    String SEMICOLON_SEPARATOR = ";";

    Pattern SEMICOLON_SPLIT_PATTERN = Pattern.compile("\\s*[;]+\\s*");

    Pattern EQUAL_SPLIT_PATTERN = Pattern.compile("\\s*[=]+\\s*");
```

### RegExpSimplifiable
`[=]` can be simplified to '='
in `dubbo-common/src/main/java/org/apache/dubbo/common/constants/CommonConstants.java`
#### Snippet
```java
    Pattern SEMICOLON_SPLIT_PATTERN = Pattern.compile("\\s*[;]+\\s*");

    Pattern EQUAL_SPLIT_PATTERN = Pattern.compile("\\s*[=]+\\s*");

    String DEFAULT_PROXY = "javassist";
```

### RegExpSimplifiable
`[,]` can be simplified to ','
in `dubbo-common/src/main/java/org/apache/dubbo/common/constants/CommonConstants.java`
#### Snippet
```java
    String DOT_SEPARATOR = ".";

    Pattern COMMA_SPLIT_PATTERN = Pattern.compile("\\s*[,]+\\s*");

    String PATH_SEPARATOR = "/";
```

### RegExpSimplifiable
`[,]` can be simplified to ','
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        ExtensionLoader.class);

    private static final Pattern NAME_SEPARATOR         = Pattern.compile("\\s*[,]+\\s*");
    private static final String  SPECIAL_SPI_PROPERTIES = "special_spi.properties";

```

### RegExpSimplifiable
`[\\\\]` can be simplified to '\\\\'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/TelnetCommandDecoder.java`
#### Snippet
```java
        if (!StringUtils.isBlank(str)) {
            str = str.trim();
            String[] array = str.split("(?<![\\\\]) ");
            if (array.length > 0) {
                String[] targetArgs = new String[array.length - 1];
```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `Paths.get(zookeeperConf.toString(), "log4j.properties")`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
        }

        File log4j = Paths.get(zookeeperConf.toString(), "log4j.properties").toFile();
        try {
            // use Files.newInputStream instead of new FileInputStream
```

### DuplicateExpressions
Multiple occurrences of `Paths.get(zookeeperConf.toString(), "log4j.properties")`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
            FileOutputStream oFile = null;
            try {
                oFile = new FileOutputStream(Paths.get(zookeeperConf.toString(), "log4j.properties").toFile());
                properties.store(oFile, "");
            } finally {
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/TimeoutCountDown.java`
#### Snippet
```java
        long remainingMillis = timeRemaining(TimeUnit.MILLISECONDS);

        StringBuilder buf = new StringBuilder();
        buf.append("Total timeout value - ");
        buf.append(timeoutMillis);
```

### StringBufferReplaceableByString
`StringBuilder serviceNameBuilder` can be replaced with 'String'
in `dubbo-common/src/main/java/org/apache/dubbo/common/BaseServiceMetadata.java`
#### Snippet
```java
     */
    public String getDisplayServiceKey() {
        StringBuilder serviceNameBuilder = new StringBuilder();
        serviceNameBuilder.append(serviceInterfaceName);
        serviceNameBuilder.append(COLON_SEPARATOR).append(version);
```

### StringBufferReplaceableByString
`StringBuilder code` can be replaced with 'String'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
    private String generateGetUrlNullCheck(int index, Class<?> type, String method) {
        // Null point check
        StringBuilder code = new StringBuilder();
        code.append(String.format("if (arg%d == null) throw new IllegalArgumentException(\"%s argument == null\");\n",
                index, type.getName()));
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
     */
    private String generateImports() {
        StringBuilder builder = new StringBuilder();
        builder.append(String.format(CODE_IMPORTS, ScopeModel.class.getName()));
        builder.append(String.format(CODE_IMPORTS, ScopeModelUtil.class.getName()));
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosServiceName.java`
#### Snippet
```java

    private String toValue() {
        return new StringBuilder(category)
                .append(NAME_SEPARATOR).append(serviceInterface)
                .append(NAME_SEPARATOR).append(version)
```

### StringBufferReplaceableByString
`StringBuilder stringBuilder` can be replaced with 'String'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

    public String getURL() {
        StringBuilder stringBuilder = new StringBuilder(getProtocol() + address);

        stringBuilder.append(getUri());
```

### StringBufferReplaceableByString
`StringBuilder stringBuilder` can be replaced with 'String'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

    public String getUri() {
        StringBuilder stringBuilder = new StringBuilder(getContextPath() + path);
        return stringBuilder.append(getQueryString()).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder result` can be replaced with 'String'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/Ls.java`
#### Snippet
```java
    @Override
    public String execute(CommandContext commandContext, String[] args) {
        StringBuilder result = new StringBuilder();
        result.append(listProvider());
        result.append(listConsumer());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ServiceRestMetadata.java`
#### Snippet
```java
    @Override
    public String toString() {
        final StringBuilder sb = new StringBuilder("ServiceRestMetadata{");
        sb.append("serviceInterface='").append(serviceInterface).append('\'');
        sb.append(", version='").append(version).append('\'');
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
                }
            } else if (ProtocolUtils.isGenericReturnRawResult(generic)) {
                return;
            } else {
                appResponse.setValue(PojoUtils.generalize(appResponse.getValue()));
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
    public static void validateMetricsConfig(MetricsConfig metricsConfig) {
        if (metricsConfig == null) {
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
    public static void validateSslConfig(SslConfig sslConfig) {
        if (sslConfig == null) {
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
    public static void validateConsumerConfig(ConsumerConfig config) {
        if (config == null) {
            return;
        }
    }
```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
            // add default extension
            int i = names.indexOf(DEFAULT_KEY);
            if (i > 0) {
                names.addAll(i, defaults);
            } else {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
        final int replLength = searchString.length();
        int increase = replacement.length() - replLength;
        increase = increase < 0 ? 0 : increase;
        increase *= max < 0 ? 16 : max > 64 ? 64 : max;
        final StringBuilder buf = new StringBuilder(text.length() + increase);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
        int increase = replacement.length() - replLength;
        increase = increase < 0 ? 0 : increase;
        increase *= max < 0 ? 16 : max > 64 ? 64 : max;
        final StringBuilder buf = new StringBuilder(text.length() + increase);
        while (end != INDEX_NOT_FOUND) {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyCodecAdapter.java`
#### Snippet
```java
                    int size = buffer.readableBytes() + input.readableBytes();
                    message = org.apache.dubbo.remoting.buffer.ChannelBuffers.dynamicBuffer(
                            size > bufferSize ? size : bufferSize);
                    message.writeBytes(buffer, buffer.readableBytes());
                    message.writeBytes(input.toByteBuffer());
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
            for (int j = 0; j < widths.length; j++) {
                widths[j] = Math.max(widths[j], row.get(j).length());
                totalWidth = (totalWidth + widths[j]) > maxwidth ? maxwidth : (totalWidth + widths[j]);
                if (j < widths.length - 1) {
                    countbefore = countbefore + widths[j];
```

## RuleId[id=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `MetricsEvent` has no concrete subclass
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/MetricsEvent.java`
#### Snippet
```java
 * BaseMetricsEvent.
 */
public abstract class MetricsEvent {

    /**
```

### AbstractClassNeverImplemented
Abstract class `AbstractMetricsReporterFactory` has no concrete subclass
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporterFactory.java`
#### Snippet
```java
 * AbstractMetricsReporterFactory.
 */
public abstract class AbstractMetricsReporterFactory implements MetricsReporterFactory {

    private final ApplicationModel applicationModel;
```

### AbstractClassNeverImplemented
Abstract class `SlidingWindow` has no concrete subclass
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/aggregate/SlidingWindow.java`
#### Snippet
```java
 * @param <T> Value type for window statistics.
 */
public abstract class SlidingWindow<T> {

    /**
```

### AbstractClassNeverImplemented
Abstract class `AbstractGenerator` has no concrete subclass
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
import java.util.stream.Collectors;

public abstract class AbstractGenerator extends Generator {

    private static final int SERVICE_NUMBER_OF_PATHS = 2;
```

### AbstractClassNeverImplemented
Abstract class `FilterableDependency` has no concrete subclass
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/FilterableDependency.java`
#### Snippet
```java
 * @author David Turanski
 */
abstract class FilterableDependency {

	/**
```

### AbstractClassNeverImplemented
Abstract class `DependencyFilter` has no concrete subclass
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/DependencyFilter.java`
#### Snippet
```java
 * @author David Turanski
 */
public abstract class DependencyFilter extends AbstractArtifactsFilter {

	private final List<? extends FilterableDependency> filters;
```

### AbstractClassNeverImplemented
Abstract class `AbstractDependencyFilterMojo` has no concrete subclass
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/AbstractDependencyFilterMojo.java`
#### Snippet
```java
 * @author David Turanski
 */
public abstract class AbstractDependencyFilterMojo extends AbstractMojo {

    /**
```

### AbstractClassNeverImplemented
Abstract class `AbstractAotMojo` has no concrete subclass
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/AbstractAotMojo.java`
#### Snippet
```java
 * @author Scott Frederick
 */
public abstract class AbstractAotMojo extends AbstractDependencyFilterMojo {

    /**
```

### AbstractClassNeverImplemented
Abstract class `ListenableFilter` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ListenableFilter.java`
#### Snippet
```java
 */
@Deprecated
public abstract class ListenableFilter implements Filter {

    protected Listener listener = null;
```

### AbstractClassNeverImplemented
Abstract class `AbstractFallbackJdkProxyFactory` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractFallbackJdkProxyFactory.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.PROXY_FAILED;

public abstract class AbstractFallbackJdkProxyFactory extends AbstractProxyFactory {

    protected final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(this.getClass());
```

### AbstractClassNeverImplemented
Abstract class `AbstractProxyFactory` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyFactory.java`
#### Snippet
```java
 * AbstractProxyFactory
 */
public abstract class AbstractProxyFactory implements ProxyFactory {
    private static final Class<?>[] INTERNAL_INTERFACES = new Class<?>[]{
        EchoService.class, Destroyable.class
```

### AbstractClassNeverImplemented
Abstract class `AbstractProxyInvoker` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyInvoker.java`
#### Snippet
```java
 * This Invoker works on provider side, delegates RPC to interface implementation.
 */
public abstract class AbstractProxyInvoker<T> implements Invoker<T> {
    ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractProxyInvoker.class);

```

### AbstractClassNeverImplemented
Abstract class `InvokerListenerAdapter` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/listener/InvokerListenerAdapter.java`
#### Snippet
```java
 * InvokerListenerAdapter
 */
public abstract class InvokerListenerAdapter implements InvokerListener {

    @Override
```

### AbstractClassNeverImplemented
Abstract class `ExporterListenerAdapter` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/listener/ExporterListenerAdapter.java`
#### Snippet
```java
 * ExporterListenerAdapter
 */
public abstract class ExporterListenerAdapter implements ExporterListener {

    @Override
```

### AbstractClassNeverImplemented
Abstract class `AbstractExporter` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractExporter.java`
#### Snippet
```java
 * AbstractExporter.
 */
public abstract class AbstractExporter<T> implements Exporter<T> {

    protected final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(getClass());
```

### AbstractClassNeverImplemented
Abstract class `RemotingServerAdapter` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    }

    protected abstract class RemotingServerAdapter implements RemotingServer {

        public abstract Object getDelegateServer();
```

### AbstractClassNeverImplemented
Abstract class `AbstractProxyProtocol` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
 * AbstractProxyProtocol
 */
public abstract class AbstractProxyProtocol extends AbstractProtocol {

    protected final List<Class<?>> rpcExceptions = new CopyOnWriteArrayList<Class<?>>();
```

### AbstractClassNeverImplemented
Abstract class `DubboUtils` has no concrete subclass
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/util/DubboUtils.java`
#### Snippet
```java
 * @since 2.7.0
 */
public abstract class DubboUtils {

    /**
```

### AbstractClassNeverImplemented
Abstract class `AbstractProtocol` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProtocol.java`
#### Snippet
```java
 * abstract ProtocolSupport.
 */
public abstract class AbstractProtocol implements Protocol, ScopeModelAware {

    protected final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(getClass());
```

### AbstractClassNeverImplemented
Abstract class `BaseRestProtocolServer` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/BaseRestProtocolServer.java`
#### Snippet
```java
import static org.apache.dubbo.rpc.protocol.rest.Constants.EXTENSION_KEY;

public abstract class BaseRestProtocolServer implements RestProtocolServer {

    private String address;
```

### AbstractClassNeverImplemented
Abstract class `AbstractAnnotatedMethodParameterProcessor` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractAnnotatedMethodParameterProcessor.java`
#### Snippet
```java
 * @since 2.7.6
 */
public abstract class AbstractAnnotatedMethodParameterProcessor implements AnnotatedMethodParameterProcessor {

    @Override
```

### AbstractClassNeverImplemented
Abstract class `AbstractServiceAnnotationProcessor` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
 * @since 2.7.6
 */
public abstract class AbstractServiceAnnotationProcessor extends AbstractProcessor {

    protected Elements elements;
```

### AbstractClassNeverImplemented
Abstract class `ParamAnnotationParameterProcessor` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/jaxrs/ParamAnnotationParameterProcessor.java`
#### Snippet
```java
 * The abstract {@link AnnotatedMethodParameterProcessor} implementation for JAX-RS's @*Param
 */
public abstract class ParamAnnotationParameterProcessor extends AbstractAnnotatedMethodParameterProcessor {

    protected void process(String name, String defaultValue, AnnotationMirror annotation, VariableElement parameter, int parameterIndex,
```

### AbstractClassNeverImplemented
Abstract class `AbstractRequestAnnotationParameterProcessor` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/springmvc/AbstractRequestAnnotationParameterProcessor.java`
#### Snippet
```java
 * The abstract {@link AnnotatedMethodParameterProcessor} implementation for Spring Web MVC's @Request*
 */
public abstract class AbstractRequestAnnotationParameterProcessor extends AbstractAnnotatedMethodParameterProcessor {


```

### AbstractClassNeverImplemented
Abstract class `AbstractServiceRestMetadataResolver` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
 * @since 2.7.6
 */
public abstract class AbstractServiceRestMetadataResolver implements ServiceRestMetadataResolver {

    private final static ThreadLocal<Map<String, Object>> threadLocalCache = withInitial(HashMap::new);
```

### AbstractClassNeverImplemented
Abstract class `AbstractServiceBuilder` has no concrete subclass
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractServiceBuilder.java`
#### Snippet
```java
 * @since 2.7
 */
public abstract class AbstractServiceBuilder<T extends AbstractServiceConfig, B extends AbstractServiceBuilder<T, B>>
    extends AbstractInterfaceBuilder<T, B> {

```

### AbstractClassNeverImplemented
Abstract class `AbstractMethodBuilder` has no concrete subclass
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractMethodBuilder.java`
#### Snippet
```java
 * @since 2.7
 */
public abstract class AbstractMethodBuilder<T extends AbstractMethodConfig, B extends AbstractMethodBuilder<T, B>>
        extends AbstractBuilder<T, B> {
    /**
```

### AbstractClassNeverImplemented
Abstract class `AbstractReferenceBuilder` has no concrete subclass
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractReferenceBuilder.java`
#### Snippet
```java
 * @since 2.7
 */
public abstract class AbstractReferenceBuilder<T extends AbstractReferenceConfig, B extends AbstractReferenceBuilder<T, B>>
        extends AbstractInterfaceBuilder<T, B> {

```

### AbstractClassNeverImplemented
Abstract class `AbstractBuilder` has no concrete subclass
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractBuilder.java`
#### Snippet
```java
 * @since 2.7
 */
public abstract class AbstractBuilder<C extends AbstractConfig, B extends AbstractBuilder> {
    /**
     * The config id
```

### AbstractClassNeverImplemented
Abstract class `AbstractInterfaceBuilder` has no concrete subclass
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java
 * @since 2.7
 */
public abstract class AbstractInterfaceBuilder<T extends AbstractInterfaceConfig, B extends AbstractInterfaceBuilder<T, B>>
        extends AbstractMethodBuilder<T, B> {
    /**
```

### AbstractClassNeverImplemented
Abstract class `EnvironmentUtils` has no concrete subclass
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java
 * @since 2.7.0
 */
public abstract class EnvironmentUtils {

    /**
```

### AbstractClassNeverImplemented
Abstract class `AbstractAnnotationBeanPostProcessor` has no concrete subclass
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
 */
@SuppressWarnings("unchecked")
public abstract class AbstractAnnotationBeanPostProcessor implements
        InstantiationAwareBeanPostProcessor, MergedBeanDefinitionPostProcessor,
    BeanFactoryAware, BeanClassLoaderAware, EnvironmentAware, DisposableBean {
```

### AbstractClassNeverImplemented
Abstract class `AbstractDubboConfigBinder` has no concrete subclass
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/AbstractDubboConfigBinder.java`
#### Snippet
```java
 * Abstract {@link DubboConfigBinder} implementation
 */
public abstract class AbstractDubboConfigBinder implements DubboConfigBinder {

    private Iterable<PropertySource<?>> propertySources;
```

### AbstractClassNeverImplemented
Abstract class `AbstractResult` has no concrete subclass
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java
    }

    abstract class AbstractResult implements Result {

        @Override
```

### AbstractClassNeverImplemented
Abstract class `AbstractLoadBalance` has no concrete subclass
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/cluster/loadbalance/AbstractLoadBalance.java`
#### Snippet
```java

@Deprecated
public abstract class AbstractLoadBalance extends org.apache.dubbo.rpc.cluster.loadbalance.AbstractLoadBalance {
}

```

### AbstractClassNeverImplemented
Abstract class `AbstractCacheFactory` has no concrete subclass
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/cache/support/AbstractCacheFactory.java`
#### Snippet
```java

@Deprecated
public abstract class AbstractCacheFactory implements CacheFactory {

    private final ConcurrentMap<String, Cache> caches = new ConcurrentHashMap<String, Cache>();
```

### AbstractClassNeverImplemented
Abstract class `AbstractRegistryFactory` has no concrete subclass
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/AbstractRegistryFactory.java`
#### Snippet
```java
 */
@Deprecated
public abstract class AbstractRegistryFactory extends org.apache.dubbo.registry.support.AbstractRegistryFactory implements RegistryFactory {

    @Override
```

### AbstractClassNeverImplemented
Abstract class `FailbackRegistry` has no concrete subclass
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
 */
@Deprecated
public abstract class FailbackRegistry implements org.apache.dubbo.registry.Registry, Registry {

    private CompatibleFailbackRegistry failbackRegistry;
```

### AbstractClassNeverImplemented
Abstract class `AbstractRegistry` has no concrete subclass
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
 */
@Deprecated
public abstract class AbstractRegistry implements Registry {

    private CompatibleAbstractRegistry abstractRegistry;
```

### AbstractClassNeverImplemented
Abstract class `AbstractTripleReactorSubscriber` has no concrete subclass
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorSubscriber.java`
#### Snippet
```java
 * Passing the data from Reactive producer to CallStreamObserver.
 */
public abstract class AbstractTripleReactorSubscriber<T> implements Subscriber<T>, CoreSubscriber<T> {

    private volatile boolean isCancelled;
```

### AbstractClassNeverImplemented
Abstract class `AbstractTripleReactorPublisher` has no concrete subclass
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorPublisher.java`
#### Snippet
```java
 * 2. passing the request of Reactive API to CallStreamObserver
 */
public abstract class AbstractTripleReactorPublisher<T> extends CancelableStreamObserver<T> implements Publisher<T>, Subscription {

    private boolean canRequest;
```

### AbstractClassNeverImplemented
Abstract class `NativeUtils` has no concrete subclass
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NativeUtils.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.CommonConstants.NATIVE;

public abstract class NativeUtils {

    public static boolean isNative() {
```

### AbstractClassNeverImplemented
Abstract class `Assert` has no concrete subclass
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Assert.java`
#### Snippet
```java
import java.util.function.Supplier;

public abstract class Assert {

    protected Assert() {
```

### AbstractClassNeverImplemented
Abstract class `Mixin` has no concrete subclass
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Mixin.java`
#### Snippet
```java
 * Mixin
 */
public abstract class Mixin {
    private static final String PACKAGE_NAME = Mixin.class.getPackage().getName();
    private static final AtomicLong MIXIN_CLASS_COUNTER = new AtomicLong(0);
```

### AbstractClassNeverImplemented
Abstract class `CancelableStreamObserver` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/CancelableStreamObserver.java`
#### Snippet
```java
import org.apache.dubbo.rpc.protocol.tri.observer.ClientCallToObserverAdapter;

public abstract class CancelableStreamObserver<T> implements StreamObserver<T> {

    private CancellationContext cancellationContext;
```

### AbstractClassNeverImplemented
Abstract class `AbstractConfig` has no concrete subclass
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
 * @export
 */
public abstract class AbstractConfig implements Serializable {

    protected static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractConfig.class);
```

### AbstractClassNeverImplemented
Abstract class `AbstractServerCallListener` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/AbstractServerCallListener.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.CommonConstants.REMOTE_APPLICATION_KEY;

public abstract class AbstractServerCallListener implements AbstractServerCall.Listener {

    private static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(AbstractServerCallListener.class);
```

### AbstractClassNeverImplemented
Abstract class `AbstractStream` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/AbstractStream.java`
#### Snippet
```java
 * An abstract stream implementation.
 */
public abstract class AbstractStream implements Stream {

    protected Executor executor;
```

### AbstractClassNeverImplemented
Abstract class `StreamQueueCommand` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/command/StreamQueueCommand.java`
#### Snippet
```java
import org.apache.dubbo.rpc.protocol.tri.stream.TripleStreamChannelFuture;

public abstract class StreamQueueCommand extends QueuedCommand {

    protected final TripleStreamChannelFuture streamChannelFuture;
```

### AbstractClassNeverImplemented
Abstract class `QueuedCommand` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/command/QueuedCommand.java`
#### Snippet
```java
import io.netty.channel.ChannelPromise;

public abstract class QueuedCommand {

    protected Channel channel;
```

### AbstractClassNeverImplemented
Abstract class `AbstractServerCall` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/AbstractServerCall.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.PROTOCOL_FAILED_REQUEST;

public abstract class AbstractServerCall implements ServerCall, ServerStream.Listener {

    public static final String REMOTE_ADDRESS_KEY = "tri.remote.address";
```

### AbstractClassNeverImplemented
Abstract class `AbstractH2TransportListener` has no concrete subclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/transport/AbstractH2TransportListener.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.PROTOCOL_FAILED_PARSE;

public abstract class AbstractH2TransportListener implements H2TransportListener {

    private static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(AbstractH2TransportListener.class);
```

### AbstractClassNeverImplemented
Abstract class `CacheableRouterFactory` has no concrete subclass
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/CacheableRouterFactory.java`
#### Snippet
```java
 * For 2.6.x style router, please implement and use RouterFactory directly.
 */
public abstract class CacheableRouterFactory implements RouterFactory {
    private ConcurrentMap<String, Router> routerMap = new ConcurrentHashMap<>();

```

### AbstractClassNeverImplemented
Abstract class `AbstractRouter` has no concrete subclass
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/AbstractRouter.java`
#### Snippet
```java
import org.apache.dubbo.rpc.cluster.governance.GovernanceRuleRepository;

public abstract class AbstractRouter implements Router {
    private int priority = DEFAULT_PRIORITY;
    private boolean force = false;
```

### AbstractClassNeverImplemented
Abstract class `AbstractLoadBalance` has no concrete subclass
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/AbstractLoadBalance.java`
#### Snippet
```java
 * AbstractLoadBalance
 */
public abstract class AbstractLoadBalance implements LoadBalance {
    /**
     * Calculate the weight according to the uptime proportion of warmup time
```

### AbstractClassNeverImplemented
Abstract class `BaseRestClient` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/BaseRestClient.java`
#### Snippet
```java
import org.apache.dubbo.remoting.http.config.HttpClientConfig;

public abstract class BaseRestClient<CLIENT> implements RestClient {

    protected CLIENT client;
```

### AbstractClassNeverImplemented
Abstract class `AbstractHttpClientFactory` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/factory/AbstractHttpClientFactory.java`
#### Snippet
```java
 * AbstractHttpClientFactory
 */
public abstract class AbstractHttpClientFactory implements RestClientFactory {

    protected final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(getClass());
```

### AbstractClassNeverImplemented
Abstract class `AbstractHttpServer` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java
 * AbstractHttpServer
 */
public abstract class AbstractHttpServer implements HttpServer {

    private final URL url;
```

### AbstractClassNeverImplemented
Abstract class `CertificateProviderInfo` has no concrete subclass
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/Bootstrapper.java`
#### Snippet
```java
    }

    public abstract static class CertificateProviderInfo {
        public abstract String pluginName();

```

### AbstractClassNeverImplemented
Abstract class `ServerInfo` has no concrete subclass
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/Bootstrapper.java`
#### Snippet
```java
    }

    public abstract static class ServerInfo {
        public abstract String target();

```

### AbstractClassNeverImplemented
Abstract class `BootstrapInfo` has no concrete subclass
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/Bootstrapper.java`
#### Snippet
```java
    }

    public abstract static class BootstrapInfo {
        public abstract List<ServerInfo> servers();

```

### AbstractClassNeverImplemented
Abstract class `Bootstrapper` has no concrete subclass
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/Bootstrapper.java`
#### Snippet
```java
import java.util.Map;

public abstract class Bootstrapper {

    public abstract BootstrapInfo bootstrap() throws XdsInitializationException;
```

### AbstractClassNeverImplemented
Abstract class `Builder` has no concrete subclass
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/Bootstrapper.java`
#### Snippet
```java
        public abstract String serverListenerResourceNameTemplate();

        abstract static class Builder {

            abstract Builder servers(List<ServerInfo> servers);
```

### AbstractClassNeverImplemented
Abstract class `AbstractRegistryCenterTestExecutionListener` has no concrete subclass
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java
 * {@link #needRegistryCenter(TestPlan)}: checks if current {@link TestPlan} need registry center.
 */
public abstract class AbstractRegistryCenterTestExecutionListener implements TestExecutionListener {

    /**
```

### AbstractClassNeverImplemented
Abstract class `ZookeeperUnixProcessor` has no concrete subclass
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/ZookeeperUnixProcessor.java`
#### Snippet
```java
 * The abstract implementation of {@link Processor} is to provide some common methods on Unix OS.
 */
public abstract class ZookeeperUnixProcessor implements Processor {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ZookeeperUnixProcessor.class);
```

### AbstractClassNeverImplemented
Abstract class `ZookeeperWindowsProcessor` has no concrete subclass
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/ZookeeperWindowsProcessor.java`
#### Snippet
```java
 * The abstract implementation of {@link Processor} is to provide some common methods on Windows OS.
 */
public abstract class ZookeeperWindowsProcessor implements Processor {

    @Override
```

### AbstractClassNeverImplemented
Abstract class `ZookeeperInitializer` has no concrete subclass
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ZookeeperInitializer.java`
#### Snippet
```java
 * The implementation of {@link Initializer} to initialize zookeeper.
 */
public abstract class ZookeeperInitializer implements Initializer {

    /**
```

### AbstractClassNeverImplemented
Abstract class `AbstractProtocol` has no concrete subclass
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
import org.apache.dubbo.common.utils.ConcurrentHashMapUtils;

public abstract class AbstractProtocol<T, S extends DeltaResource<T>> implements XdsProtocol<T>, XdsListener {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractProtocol.class);
```

### AbstractClassNeverImplemented
Abstract class `AbstractCacheFactory` has no concrete subclass
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/AbstractCacheFactory.java`
#### Snippet
```java
 * @see org.apache.dubbo.cache.support.expiring.ExpiringCacheFactory
 */
public abstract class AbstractCacheFactory implements CacheFactory {

    /**
```

### AbstractClassNeverImplemented
Abstract class `AbstractValidation` has no concrete subclass
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/AbstractValidation.java`
#### Snippet
```java
 * @see Validator
 */
public abstract class AbstractValidation implements Validation {

    private final ConcurrentMap<String, Validator> validators = new ConcurrentHashMap<>();
```

### AbstractClassNeverImplemented
Abstract class `AbstractMetricsReporterFactory` has no concrete subclass
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporterFactory.java`
#### Snippet
```java
 * AbstractMetricsReporterFactory.
 */
public abstract class AbstractMetricsReporterFactory implements MetricsReporterFactory {

    private final ApplicationModel applicationModel;
```

### AbstractClassNeverImplemented
Abstract class `AbstractMetricsReporter` has no concrete subclass
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporter.java`
#### Snippet
```java
 * AbstractMetricsReporter.
 */
public abstract class AbstractMetricsReporter implements MetricsReporter {

    private final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractMetricsReporter.class);
```

### AbstractClassNeverImplemented
Abstract class `AbstractMonitorFactory` has no concrete subclass
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/AbstractMonitorFactory.java`
#### Snippet
```java
 * AbstractMonitorFactory. (SPI, Singleton, ThreadSafe)
 */
public abstract class AbstractMonitorFactory implements MonitorFactory {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractMonitorFactory.class);

```

### AbstractClassNeverImplemented
Abstract class `SimpleMetricsCountSampler` has no concrete subclass
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/SimpleMetricsCountSampler.java`
#### Snippet
```java
 */

public abstract class SimpleMetricsCountSampler<S, K, M extends Metric>
    implements MetricsCountSampler<S, K, M> {

```

### AbstractClassNeverImplemented
Abstract class `AbstractCacheManager` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractCacheManager.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.COMMON_FAILED_LOAD_MAPPING_CACHE;

public abstract class AbstractCacheManager<V> implements Disposable {
    protected final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(getClass());

```

### AbstractClassNeverImplemented
Abstract class `AbstractServiceNameMapping` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractServiceNameMapping.java`
#### Snippet
```java
import static org.apache.dubbo.common.utils.StringUtils.isBlank;

public abstract class AbstractServiceNameMapping implements ServiceNameMapping {
    protected final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(getClass());
    protected ApplicationModel applicationModel;
```

### AbstractClassNeverImplemented
Abstract class `AbstractNoAnnotatedParameterProcessor` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractNoAnnotatedParameterProcessor.java`
#### Snippet
```java
import static org.apache.dubbo.common.utils.ClassUtils.resolveClass;

public abstract class AbstractNoAnnotatedParameterProcessor implements NoAnnotatedParameterRequestTagProcessor {

    public void process(Parameter parameter, int parameterIndex, RestMethodMetadata restMethodMetadata) {
```

### AbstractClassNeverImplemented
Abstract class `AbstractAnnotatedMethodParameterProcessor` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractAnnotatedMethodParameterProcessor.java`
#### Snippet
```java
 * @since 2.7.6
 */
public abstract class AbstractAnnotatedMethodParameterProcessor implements AnnotatedMethodParameterProcessor {

    @Override
```

### AbstractClassNeverImplemented
Abstract class `ParamAnnotationParameterProcessor` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/jaxrs/ParamAnnotationParameterProcessor.java`
#### Snippet
```java
 * The abstract {@link AnnotatedMethodParameterProcessor} implementation for JAX-RS's @*Param
 */
public abstract class ParamAnnotationParameterProcessor extends AbstractAnnotatedMethodParameterProcessor {

    @Override
```

### AbstractClassNeverImplemented
Abstract class `AbstractServiceRestMetadataResolver` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
 * @since 2.7.6
 */
public abstract class AbstractServiceRestMetadataResolver implements ServiceRestMetadataResolver {

    private final Map<String, List<AnnotatedMethodParameterProcessor>> parameterProcessorsMap;
```

### AbstractClassNeverImplemented
Abstract class `AbstractRequestAnnotationParameterProcessor` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/AbstractRequestAnnotationParameterProcessor.java`
#### Snippet
```java
 * The abstract {@link AnnotatedMethodParameterProcessor} implementation for Spring Web MVC's @Request*
 */
public abstract class AbstractRequestAnnotationParameterProcessor extends AbstractAnnotatedMethodParameterProcessor {

    @Override
```

### AbstractClassNeverImplemented
Abstract class `AbstractMetadataReportFactory` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReportFactory.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.PROXY_FAILED_EXPORT_SERVICE;

public abstract class AbstractMetadataReportFactory implements MetadataReportFactory {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractMetadataReportFactory.class);
```

### AbstractClassNeverImplemented
Abstract class `KeyTypeEnum` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/identifier/KeyTypeEnum.java`
#### Snippet
```java
 * 2019-08-15
 */
public enum KeyTypeEnum {

    PATH(PATH_SEPARATOR) {
```

### AbstractClassNeverImplemented
Abstract class `RegistryNotifier` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryNotifier.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.REGISTRY_FAILED_NOTIFY_EVENT;

public abstract class RegistryNotifier {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(RegistryNotifier.class);
```

### AbstractClassNeverImplemented
Abstract class `AbstractRetryTask` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/retry/AbstractRetryTask.java`
#### Snippet
```java
 * AbstractRetryTask
 */
public abstract class AbstractRetryTask implements TimerTask {

    protected final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(getClass());
```

### AbstractClassNeverImplemented
Abstract class `AbstractMetadataReport` has no concrete subclass
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
import static org.apache.dubbo.metadata.report.support.Constants.USER_HOME;

public abstract class AbstractMetadataReport implements MetadataReport {

    protected final static String DEFAULT_ROOT = "dubbo";
```

### AbstractClassNeverImplemented
Abstract class `AbstractServiceDiscoveryFactory` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscoveryFactory.java`
#### Snippet
```java
 * @since 2.7.5
 */
public abstract class AbstractServiceDiscoveryFactory implements ServiceDiscoveryFactory, ScopeModelAware {

    protected ApplicationModel applicationModel;
```

### AbstractClassNeverImplemented
Abstract class `AbstractServiceDiscovery` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
 * Each service discovery is bond to one application.
 */
public abstract class AbstractServiceDiscovery implements ServiceDiscovery {
    private final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractServiceDiscovery.class);
    private volatile boolean isDestroy;
```

### AbstractClassNeverImplemented
Abstract class `AbstractRegistryFactory` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistryFactory.java`
#### Snippet
```java
 * @see org.apache.dubbo.registry.RegistryFactory
 */
public abstract class AbstractRegistryFactory implements RegistryFactory, ScopeModelAware {

    private static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(AbstractRegistryFactory.class);
```

### AbstractClassNeverImplemented
Abstract class `FailbackRegistry` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
 * (SPI, Prototype, ThreadSafe)
 */
public abstract class FailbackRegistry extends AbstractRegistry {

    /*  retry task map */
```

### AbstractClassNeverImplemented
Abstract class `AbstractConfiguratorListener` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/AbstractConfiguratorListener.java`
#### Snippet
```java
 * AbstractConfiguratorListener
 */
public abstract class AbstractConfiguratorListener implements ConfigurationListener {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractConfiguratorListener.class);

```

### AbstractClassNeverImplemented
Abstract class `DynamicDirectory` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/DynamicDirectory.java`
#### Snippet
```java
 * DynamicDirectory
 */
public abstract class DynamicDirectory<T> extends AbstractDirectory<T> implements NotifyListener {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(DynamicDirectory.class);
```

### AbstractClassNeverImplemented
Abstract class `CacheableFailbackRegistry` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
 * @see org.apache.dubbo.registry.support.AbstractRegistry
 */
public abstract class CacheableFailbackRegistry extends FailbackRegistry {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(CacheableFailbackRegistry.class);

```

### AbstractClassNeverImplemented
Abstract class `AbstractWireProtocol` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/AbstractWireProtocol.java`
#### Snippet
```java
import org.apache.dubbo.remoting.api.ssl.ContextOperator;

public abstract class AbstractWireProtocol implements WireProtocol {

    private final ProtocolDetector detector;
```

### AbstractClassNeverImplemented
Abstract class `CuratorFrameworkUtils` has no concrete subclass
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/util/CuratorFrameworkUtils.java`
#### Snippet
```java
 * @since 2.7.5
 */
public abstract class CuratorFrameworkUtils {

    public static ServiceDiscovery<ZookeeperInstance> buildServiceDiscovery(CuratorFramework curatorFramework,
```

### AbstractClassNeverImplemented
Abstract class `AbstractPortUnificationServer` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/AbstractPortUnificationServer.java`
#### Snippet
```java
import java.util.concurrent.ConcurrentHashMap;

public abstract class AbstractPortUnificationServer extends AbstractServer {
    private final List<WireProtocol> protocols;

```

### AbstractClassNeverImplemented
Abstract class `AbstractRegistry` has no concrete subclass
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
 * (SPI, Prototype, ThreadSafe)
 */
public abstract class AbstractRegistry implements Registry {

    // URL address separator, used in file cache, service provider URL separation
```

### AbstractClassNeverImplemented
Abstract class `AbstractConnectionClient` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/connection/AbstractConnectionClient.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.PROTOCOL_ERROR_CLOSE_CLIENT;

public abstract class AbstractConnectionClient extends AbstractClient {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractConnectionClient.class);
```

### AbstractClassNeverImplemented
Abstract class `AbstractChannelBuffer` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/AbstractChannelBuffer.java`
#### Snippet
```java
import java.nio.ByteBuffer;

public abstract class AbstractChannelBuffer implements ChannelBuffer {

    private int readerIndex;
```

### AbstractClassNeverImplemented
Abstract class `ExchangeHandlerAdapter` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/ExchangeHandlerAdapter.java`
#### Snippet
```java
 * ExchangeHandlerAdapter
 */
public abstract class ExchangeHandlerAdapter extends TelnetHandlerAdapter implements ExchangeHandler {
    public ExchangeHandlerAdapter(FrameworkModel frameworkModel) {
        super(frameworkModel);
```

### AbstractClassNeverImplemented
Abstract class `AbstractTimerTask` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/AbstractTimerTask.java`
#### Snippet
```java
 * AbstractTimerTask
 */
public abstract class AbstractTimerTask implements TimerTask {

    private final ChannelProvider channelProvider;
```

### AbstractClassNeverImplemented
Abstract class `AbstractChannel` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractChannel.java`
#### Snippet
```java
 * AbstractChannel
 */
public abstract class AbstractChannel extends AbstractPeer implements Channel {

    public AbstractChannel(URL url, ChannelHandler handler) {
```

### AbstractClassNeverImplemented
Abstract class `AbstractCodec` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractCodec.java`
#### Snippet
```java
 * AbstractCodec
 */
public abstract class AbstractCodec implements Codec2, ScopeModelAware {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractCodec.class);
```

### AbstractClassNeverImplemented
Abstract class `AbstractEndpoint` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractEndpoint.java`
#### Snippet
```java
 * AbstractEndpoint
 */
public abstract class AbstractEndpoint extends AbstractPeer implements Resetable {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractEndpoint.class);
```

### AbstractClassNeverImplemented
Abstract class `AbstractPeer` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractPeer.java`
#### Snippet
```java
 * AbstractPeer
 */
public abstract class AbstractPeer implements Endpoint, ChannelHandler {

    private final ChannelHandler handler;
```

### AbstractClassNeverImplemented
Abstract class `AbstractChannelHandlerDelegate` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractChannelHandlerDelegate.java`
#### Snippet
```java
import org.apache.dubbo.remoting.RemotingException;

public abstract class AbstractChannelHandlerDelegate implements ChannelHandlerDelegate {

    protected ChannelHandler handler;
```

### AbstractClassNeverImplemented
Abstract class `AbstractServer` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractServer.java`
#### Snippet
```java
 * AbstractServer
 */
public abstract class AbstractServer extends AbstractEndpoint implements RemotingServer {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractServer.class);
    private Set<ExecutorService> executors = new ConcurrentHashSet<>();
```

### AbstractClassNeverImplemented
Abstract class `AbstractClient` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
 * AbstractClient
 */
public abstract class AbstractClient extends AbstractEndpoint implements Client {

    protected static final String CLIENT_THREAD_POOL_NAME = "DubboClientHandler";
```

### AbstractClassNeverImplemented
Abstract class `AbstractZookeeperTransporter` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
 * If you want to extend this, implements createZookeeperClient.
 */
public abstract class AbstractZookeeperTransporter implements ZookeeperTransporter {
    private static final Logger logger = LoggerFactory.getLogger(ZookeeperTransporter.class);
    private final Map<String, ZookeeperClient> zookeeperClientMap = new ConcurrentHashMap<>();
```

### AbstractClassNeverImplemented
Abstract class `AbstractZookeeperClient` has no concrete subclass
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperClient.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.REGISTRY_ZOOKEEPER_EXCEPTION;

public abstract class AbstractZookeeperClient<TargetDataListener, TargetChildListener> implements ZookeeperClient {

    protected static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractZookeeperClient.class);
```

### AbstractClassNeverImplemented
Abstract class `SerializableClassRegistry` has no concrete subclass
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/support/SerializableClassRegistry.java`
#### Snippet
```java
 * and {@code dubbo-serialization-kryo}, it will register some classes at startup time (for example {@link AbstractKryoFactory#create})
 */
public abstract class SerializableClassRegistry {


```

## RuleId[id=BoundedWildcard]
### BoundedWildcard
Can generalize to `? extends ObservationHandler`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/ObservationHandlerGrouping.java`
#### Snippet
```java
    }

    void apply(List<ObservationHandler<?>> handlers, ObservationRegistry.ObservationConfig config) {
        MultiValueMap<Class<? extends ObservationHandler>, ObservationHandler<?>> groupings = new LinkedMultiValueMap<>();
        for (ObservationHandler<?> handler : handlers) {
```

### BoundedWildcard
Can generalize to `? extends BaggagePropagationCustomizer`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
        @ConditionalOnProperty(prefix = "dubbo.tracing.propagation", value = "type", havingValue = "B3")
        BaggagePropagation.FactoryBuilder b3PropagationFactoryBuilder(
            ObjectProvider<BaggagePropagationCustomizer> baggagePropagationCustomizers) {
            Propagation.Factory delegate =
                B3Propagation.newFactoryBuilder().injectFormat(B3Propagation.Format.SINGLE_NO_PARENT).build();
```

### BoundedWildcard
Can generalize to `? extends SpanExportingPredicate`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
    @ConditionalOnMissingBean
    @Order(Ordered.HIGHEST_PRECEDENCE)
    CompositeSpanHandler compositeSpanHandler(ObjectProvider<SpanExportingPredicate> predicates,
                                              ObjectProvider<SpanReporter> reporters, ObjectProvider<SpanFilter> filters) {
        return new CompositeSpanHandler(predicates.orderedStream().collect(Collectors.toList()),
```

### BoundedWildcard
Can generalize to `? extends SpanReporter`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
    @Order(Ordered.HIGHEST_PRECEDENCE)
    CompositeSpanHandler compositeSpanHandler(ObjectProvider<SpanExportingPredicate> predicates,
                                              ObjectProvider<SpanReporter> reporters, ObjectProvider<SpanFilter> filters) {
        return new CompositeSpanHandler(predicates.orderedStream().collect(Collectors.toList()),
            reporters.orderedStream().collect(Collectors.toList()),
```

### BoundedWildcard
Can generalize to `? extends SpanFilter`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
    @Order(Ordered.HIGHEST_PRECEDENCE)
    CompositeSpanHandler compositeSpanHandler(ObjectProvider<SpanExportingPredicate> predicates,
                                              ObjectProvider<SpanReporter> reporters, ObjectProvider<SpanFilter> filters) {
        return new CompositeSpanHandler(predicates.orderedStream().collect(Collectors.toList()),
            reporters.orderedStream().collect(Collectors.toList()),
```

### BoundedWildcard
Can generalize to `? extends CurrentTraceContext.ScopeDecorator`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
    @Bean
    @ConditionalOnMissingBean
    public CurrentTraceContext braveCurrentTraceContext(List<CurrentTraceContext.ScopeDecorator> scopeDecorators,
                                                        List<CurrentTraceContextCustomizer> currentTraceContextCustomizers) {
        ThreadLocalCurrentTraceContext.Builder builder = ThreadLocalCurrentTraceContext.newBuilder();
```

### BoundedWildcard
Can generalize to `? extends CurrentTraceContextCustomizer`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
    @ConditionalOnMissingBean
    public CurrentTraceContext braveCurrentTraceContext(List<CurrentTraceContext.ScopeDecorator> scopeDecorators,
                                                        List<CurrentTraceContextCustomizer> currentTraceContextCustomizers) {
        ThreadLocalCurrentTraceContext.Builder builder = ThreadLocalCurrentTraceContext.newBuilder();
        scopeDecorators.forEach(builder::addScopeDecorator);
```

### BoundedWildcard
Can generalize to `? extends SpanHandler`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
    @Bean
    @ConditionalOnMissingBean
    public Tracing braveTracing(Environment environment, List<SpanHandler> spanHandlers,
                                List<TracingCustomizer> tracingCustomizers, CurrentTraceContext currentTraceContext,
                                Propagation.Factory propagationFactory, Sampler sampler) {
```

### BoundedWildcard
Can generalize to `? extends TracingCustomizer`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
    @ConditionalOnMissingBean
    public Tracing braveTracing(Environment environment, List<SpanHandler> spanHandlers,
                                List<TracingCustomizer> tracingCustomizers, CurrentTraceContext currentTraceContext,
                                Propagation.Factory propagationFactory, Sampler sampler) {
        String applicationName = environment.getProperty("spring.application.name", DEFAULT_APPLICATION_NAME);
```

### BoundedWildcard
Can generalize to `? extends CorrelationScopeCustomizer`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
        @ConditionalOnMissingBean
        CorrelationScopeDecorator.Builder mdcCorrelationScopeDecoratorBuilder(
            ObjectProvider<CorrelationScopeCustomizer> correlationScopeCustomizers) {
            CorrelationScopeDecorator.Builder builder = MDCScopeDecorator.newBuilder();
            correlationScopeCustomizers.orderedStream().forEach((customizer) -> customizer.customize(builder));
```

### BoundedWildcard
Can generalize to `? extends BaggagePropagationCustomizer`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java
        @ConditionalOnProperty(prefix = "dubbo.tracing.propagation", value = "type", havingValue = "W3C", matchIfMissing = true)
        BaggagePropagation.FactoryBuilder w3cPropagationFactoryBuilder(
            ObjectProvider<BaggagePropagationCustomizer> baggagePropagationCustomizers) {
            Propagation.Factory delegate = new W3CPropagation(BRAVE_BAGGAGE_MANAGER, Collections.emptyList());

```

### BoundedWildcard
Can generalize to `? extends EventListener`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/otel/OpenTelemetryAutoConfiguration.java`
#### Snippet
```java
        private final List<EventListener> listeners;

        OTelEventPublisher(List<EventListener> listeners) {
            this.listeners = listeners;
        }
```

### BoundedWildcard
Can generalize to `? extends TextMapPropagator`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/otel/OpenTelemetryAutoConfiguration.java`
#### Snippet
```java
    @Bean
    @ConditionalOnMissingBean
    ContextPropagators otelContextPropagators(ObjectProvider<TextMapPropagator> textMapPropagators) {
        return ContextPropagators.create(TextMapPropagator.composite(textMapPropagators.orderedStream().collect(Collectors.toList())));
    }
```

### BoundedWildcard
Can generalize to `? extends SpanProcessor`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/otel/OpenTelemetryAutoConfiguration.java`
#### Snippet
```java
    @Bean
    @ConditionalOnMissingBean
    SdkTracerProvider otelSdkTracerProvider(Environment environment, ObjectProvider<SpanProcessor> spanProcessors,
                                            Sampler sampler) {
        String applicationName = environment.getProperty("spring.application.name", DEFAULT_APPLICATION_NAME);
```

### BoundedWildcard
Can generalize to `? extends SpanExporter`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/otel/OpenTelemetryAutoConfiguration.java`
#### Snippet
```java
    @Bean
    @ConditionalOnMissingBean
    SpanProcessor otelSpanProcessor(ObjectProvider<SpanExporter> spanExporters,
                                    ObjectProvider<SpanExportingPredicate> spanExportingPredicates, ObjectProvider<SpanReporter> spanReporters,
                                    ObjectProvider<SpanFilter> spanFilters) {
```

### BoundedWildcard
Can generalize to `? extends SpanExportingPredicate`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/otel/OpenTelemetryAutoConfiguration.java`
#### Snippet
```java
    @ConditionalOnMissingBean
    SpanProcessor otelSpanProcessor(ObjectProvider<SpanExporter> spanExporters,
                                    ObjectProvider<SpanExportingPredicate> spanExportingPredicates, ObjectProvider<SpanReporter> spanReporters,
                                    ObjectProvider<SpanFilter> spanFilters) {
        return BatchSpanProcessor.builder(new CompositeSpanExporter(spanExporters.orderedStream().collect(Collectors.toList()),
```

### BoundedWildcard
Can generalize to `? extends SpanReporter`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/otel/OpenTelemetryAutoConfiguration.java`
#### Snippet
```java
    @ConditionalOnMissingBean
    SpanProcessor otelSpanProcessor(ObjectProvider<SpanExporter> spanExporters,
                                    ObjectProvider<SpanExportingPredicate> spanExportingPredicates, ObjectProvider<SpanReporter> spanReporters,
                                    ObjectProvider<SpanFilter> spanFilters) {
        return BatchSpanProcessor.builder(new CompositeSpanExporter(spanExporters.orderedStream().collect(Collectors.toList()),
```

### BoundedWildcard
Can generalize to `? extends SpanFilter`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/otel/OpenTelemetryAutoConfiguration.java`
#### Snippet
```java
    SpanProcessor otelSpanProcessor(ObjectProvider<SpanExporter> spanExporters,
                                    ObjectProvider<SpanExportingPredicate> spanExportingPredicates, ObjectProvider<SpanReporter> spanReporters,
                                    ObjectProvider<SpanFilter> spanFilters) {
        return BatchSpanProcessor.builder(new CompositeSpanExporter(spanExporters.orderedStream().collect(Collectors.toList()),
            spanExportingPredicates.orderedStream().collect(Collectors.toList()), spanReporters.orderedStream().collect(Collectors.toList()),
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AttachmentsAdapter.java`
#### Snippet
```java
        private final Map<String, Object> attachments;

        public ObjectToStringMap(Map<String, Object> attachments) {
            for (Entry<String, Object> entry : attachments.entrySet()) {
                String convertResult = convert(entry.getValue());
```

### BoundedWildcard
Can generalize to `? super T`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/stub/FutureToObserverAdaptor.java`
#### Snippet
```java
    private final CompletableFuture<T> future;

    public FutureToObserverAdaptor(CompletableFuture<T> future) {
        this.future = future;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java
     * @since 2.7.1
     */
    private void setAllowBeanDefinitionOverriding(Map<String, Object> defaultProperties) {
        defaultProperties.put(ALLOW_BEAN_DEFINITION_OVERRIDING_PROPERTY, Boolean.TRUE.toString());
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/DefaultServiceRestMetadataResolver.java`
#### Snippet
```java
    @Override
    protected void processConsumes(ProcessingEnvironment processingEnv, TypeElement serviceType,
                                   ExecutableElement method, Set<String> consumes) {
        if (hasComplexParameterType(method)) {
            consumes.addAll(MEDIA_TYPES);
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/DefaultServiceRestMetadataResolver.java`
#### Snippet
```java
    @Override
    protected void processProduces(ProcessingEnvironment processingEnv, TypeElement serviceType,
                                   ExecutableElement method, Set<String> produces) {
        TypeMirror returnType = method.getReturnType();
        if (isComplexType(returnType)) {
```

### BoundedWildcard
Can generalize to `? super Reader`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/ClassPathMetadataStorage.java`
#### Snippet
```java
    }

    public <T> Optional<T> read(String resourceName, Function<Reader, T> consumer) {
        if (exists(resourceName)) {
            try (Reader reader = getReader(resourceName)) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/ClassPathMetadataStorage.java`
#### Snippet
```java
    }

    public <T> Optional<T> read(String resourceName, Function<Reader, T> consumer) {
        if (exists(resourceName)) {
            try (Reader reader = getReader(resourceName)) {
```

### BoundedWildcard
Can generalize to `? super ObjectMapper`
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/jackson/ObjectMapperCodec.java`
#### Snippet
```java
    }

    public ObjectMapperCodec configureMapper(Consumer<ObjectMapper> objectMapperConfigure) {
        objectMapperConfigure.accept(this.mapper);
        return this;
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java
    }

    private static void extract(String root, Map<String, PropertySource<?>> map,
                                PropertySource<?> source) {
        if (source instanceof CompositePropertySource) {
```

### BoundedWildcard
Can generalize to `? super PropertySource`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java
    }

    private static void extract(String root, Map<String, PropertySource<?>> map,
                                PropertySource<?> source) {
        if (source instanceof CompositePropertySource) {
```

### BoundedWildcard
Can generalize to `? extends FieldError`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/DefaultDubboConfigBinder.java`
#### Snippet
```java
    }

    private String buildErrorMsg(List<FieldError> errors, String prefix, String config) {
        StringBuilder builder = new StringBuilder("Data bind error, please check config. config: " + config + ", prefix: " + prefix
                + " , error fields: [" + errors.get(0).getField());
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
    }

    private static void getPropertyMap(Class<?> beanClass, Map<String, Class> beanPropsMap) {
        for (Method setter : beanClass.getMethods()) {
            String name = setter.getName();
```

### BoundedWildcard
Can generalize to `? super Class`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
    }

    private static void getPropertyMap(Class<?> beanClass, Map<String, Class> beanPropsMap) {
        for (Method setter : beanClass.getMethods()) {
            String name = setter.getName();
```

### BoundedWildcard
Can generalize to `? super Map.Entry`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java

    @Override
    default void foreachAttachment(Consumer<Map.Entry<String, Object>> consumer) {
        getObjectAttachments().entrySet().forEach(consumer);
    }
```

### BoundedWildcard
Can generalize to `? extends List`
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java

    protected final void writeTable(PrintWriter writer, String title, List<String> columns,
                                    List<List<String>> rows) {
        int n = random.nextInt();
        int c = (columns == null ? (rows == null || rows.size() == 0 ? 0 : rows.get(0).size())
```

### BoundedWildcard
Can generalize to `? super Map.Entry`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java

    @Override
    public void foreachAttachment(Consumer<Map.Entry<String, Object>> consumer) {
        try {
            attachmentLock.lock();
```

### BoundedWildcard
Can generalize to `? extends T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    public <T> List<T> getBeansOfType(Class<T> type) {
        List<T> currentBeans = (List<T>) registeredBeanInfos.stream().filter(beanInfo -> type.isInstance(beanInfo.instance)).map(beanInfo -> beanInfo.instance).collect(Collectors.toList());
        if (parent != null) {
```

### BoundedWildcard
Can generalize to `? super T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
     * @since 2.7.6
     */
    public static <T> int addAll(Collection<T> collection, T... values) {

        int size = values == null ? 0 : values.length;
```

### BoundedWildcard
Can generalize to `? extends K`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public static <K, V> Map<V, K> flip(Map<K, V> map) {
        if (isEmptyMap(map)) {
            return (Map<V, K>) map;
```

### BoundedWildcard
Can generalize to `? extends V`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public static <K, V> Map<V, K> flip(Map<K, V> map) {
        if (isEmptyMap(map)) {
            return (Map<V, K>) map;
```

### BoundedWildcard
Can generalize to `? extends T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CacheableSupplier.java`
#### Snippet
```java
    private final Supplier<T> supplier;

    public CacheableSupplier(Supplier<T> supplier) {
        this.supplier = supplier;
    }
```

### BoundedWildcard
Can generalize to `? super T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
     * @since 2.7.8
     */
    public static <T> void updatePropertyIfAbsent(Supplier<T> getterMethod, Consumer<T> setterMethod, T newValue) {
        if (newValue != null && getterMethod.get() == null) {
            setterMethod.accept(newValue);
```

### BoundedWildcard
Can generalize to `? extends T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Configuration.java`
#### Snippet
```java


    default <T> T convert(Class<T> cls, String key, T defaultValue) {
        // we only process String properties for now
        String value = (String) getProperty(key);
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationCache.java`
#### Snippet
```java
     * @return value
     */
    public String computeIfAbsent(String key, Function<String, String> function) {
        String value = cache.get(key);
        // value might be empty here!
```

### BoundedWildcard
Can generalize to `? super Class`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    }

    private static Object getEmptyObject(Class<?> returnType, Map<Class<?>, Object> emptyInstances, int level) {
        if (level > 2) {
            return null;
```

### BoundedWildcard
Can generalize to `? extends Map`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * @return
     */
    public static <V extends Object> Map<String, V> getSubProperties(Collection<Map<String, V>> configMaps, String prefix) {
        Map<String, V> map = new LinkedHashMap<>();
        for (Map<String, V> configMap : configMaps) {
```

### BoundedWildcard
Can generalize to `? extends Map`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    public static <V extends Object> boolean hasSubProperties(Collection<Map<String, V>> configMaps, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
     * @since 2.7.8
     */
    public Map<String, String> getParameters(Predicate<String> nameToSelect) {
        Map<String, String> selectedParameters = new LinkedHashMap<>();
        for (Map.Entry<String, String> entry : getParameters().entrySet()) {
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
    }

    public static void putMethodParameter(String method, String key, String value, Map<String, Map<String, String>> methodParameters) {
        Map<String, String> subParameter = methodParameters.computeIfAbsent(method, k -> new HashMap<>());
        subParameter.put(key, value);
```

### BoundedWildcard
Can generalize to `? super T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/function/Streams.java`
#### Snippet
```java
public interface Streams {

    static <T, S extends Iterable<T>> Stream<T> filterStream(S values, Predicate<T> predicate) {
        return stream(values.spliterator(), false).filter(predicate);
    }
```

### BoundedWildcard
Can generalize to `? super E`
in `dubbo-common/src/main/java/org/apache/dubbo/common/concurrent/DiscardOldestPolicy.java`
#### Snippet
```java

    @Override
    public void reject(final E e, final Queue<E> queue) {
        queue.poll();
        queue.offer(e);
```

### BoundedWildcard
Can generalize to `? extends T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/concurrent/CallableSafeInitializer.java`
#### Snippet
```java
    private final Callable<T> callable;

    public CallableSafeInitializer(Callable<T> callable) {
        this.callable = callable;
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/concurrent/ScheduledCompletableFuture.java`
#### Snippet
```java
    public static <T> CompletableFuture<T> submit(
            ScheduledExecutorService executor,
            Supplier<T> task
    ) {
        CompletableFuture<T> completableFuture = new CompletableFuture<>();
```

### BoundedWildcard
Can generalize to `? extends T`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/concurrent/ScheduledCompletableFuture.java`
#### Snippet
```java
    public static <T> CompletableFuture<T> schedule(
            ScheduledExecutorService executor,
            Supplier<T> task,
            long delay,
            TimeUnit unit
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ResourceConfigWriter.java`
#### Snippet
```java
    }

    private void addIfNotEmpty(Map<String, Object> attributes, String name, Object value) {
        if (value instanceof Collection<?>) {
            if (!((Collection<?>) value).isEmpty()) {
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ReflectionConfigWriter.java`
#### Snippet
```java
    }

    private void addIfNotEmpty(Map<String, Object> attributes, String name, Object value) {
        if ((value instanceof Collection<?> && ((Collection<?>) value).size() != 0)) {
            attributes.put(name, value);
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/ClassFinder.java`
#### Snippet
```java
public class ClassFinder {

    public Set<String> findClassSet(String packageName, Consumer<String> consumer) {
        packageName = packageName.replace(".", "/");
        try {
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/ClassFinder.java`
#### Snippet
```java
    }

    private void findClassesByFile(String packageName, String resource, Set<String> result) {
        File directory = new File(resource);
        File[] listFiles = directory.listFiles();
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/ClassFinder.java`
#### Snippet
```java
    }

    private static void findClassesByJar(String packageName, JarFile jar, Set<String> classes) {
        Enumeration<JarEntry> entry = jar.entries();
        JarEntry jarEntry;
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/metadata/DubboConfigsMetadata.java`
#### Snippet
```java

    private void addDubboConfigBeans(Class<? extends AbstractConfig> dubboConfigClass,
                                     Map<String, Map<String, Map<String, Object>>> configsMap) {

        Map<String, ? extends AbstractConfig> dubboConfigBeans = beansOfTypeIncludingAncestors(applicationContext, dubboConfigClass);
```

### BoundedWildcard
Can generalize to `? super Map`>
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/metadata/DubboConfigsMetadata.java`
#### Snippet
```java

    private void addDubboConfigBeans(Class<? extends AbstractConfig> dubboConfigClass,
                                     Map<String, Map<String, Map<String, Object>>> configsMap) {

        Map<String, ? extends AbstractConfig> dubboConfigBeans = beansOfTypeIncludingAncestors(applicationContext, dubboConfigClass);
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java


    public void addValueByKey(String key, String value, Map<String, Collection<String>> maps) {

        if (value == null) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
    }

    private <T> void filterData(Collection<T> collection, NacosDataFilter<T> filter) {
        // remove if not accept
        collection.removeIf(data -> !filter.accept(data));
```

### BoundedWildcard
Can generalize to `? extends Instance`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
    }

    private void filterEnabledInstances(Collection<Instance> instances) {
        filterData(instances, Instance::isEnabled);
    }
```

### BoundedWildcard
Can generalize to `? extends Thread`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
    }

    private void printUnclosedThreads(List<Thread> threads, String testClassName) {
        if (threads.size() > 0) {
            log("Found " + threads.size() + " unclosed threads in test: " + testClassName);
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/GetConfig.java`
#### Snippet
```java
    }

    private static void appendConfig(String type, String id, Object config, StringBuilder plainOutput, Map<String, Object> map, String[] args) {
        if (!isMatch(type, id, args)) {
            return;
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/GetConfig.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private static void appendConfigs(String type, String id, Object config, StringBuilder plainOutput, Map<String, Object> map, String[] args) {
        if (!isMatch(type, id, args)) {
            return;
```

### BoundedWildcard
Can generalize to `? extends List`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
    }

    public void setParams(Map<String, List<String>> params) {
        params(params);
    }
```

### BoundedWildcard
Can generalize to `? extends List`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
    }

    private <T extends Collection<String>> RequestMetadata headers(Map<String, List<String>> headers) {
        if (headers != null && !headers.isEmpty()) {
            Map<String, List<String>> httpHeaders = new LinkedHashMap<>();
```

### BoundedWildcard
Can generalize to `? super String`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
    }

    private static List<String> get(Map<String, List<String>> multiValueMap, String key, boolean createIfAbsent) {
        return createIfAbsent ? multiValueMap.computeIfAbsent(key, k -> new LinkedList<>()) : multiValueMap.get(key);
    }
```

### BoundedWildcard
Can generalize to `? extends Map`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        }

        private String getMethodParameter(String method, String key, Map<String, Map<String, String>> map) {
            String value = null;
            if (map == null) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
    }

    public static String toTable(List<String> header, List<List<String>> table) {
        int totalWidth = 0;
        int[] widths = new int[header.size()];
```

### BoundedWildcard
Can generalize to `? extends List`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
public class TelnetUtils {

    public static String toList(List<List<String>> table) {
        int[] widths = new int[table.get(0).size()];
        for (int j = 0; j < widths.length; j++) {
```

## RuleId[id=MissortedModifiers]
### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String HTTPS_PROXY = "httpsProxy";

    public final static String PROXY_USERNAME = "proxyUsername";

    public final static String PROXY_PASSWORD = "proxyPassword";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String NAMESPACE = "namespace";

    public final static String API_VERSION = "apiVersion";

    public final static String CA_CERT_FILE = "caCertFile";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String PROXY_PASSWORD = "proxyPassword";

    public final static String NO_PROXY = "noProxy";
}

```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String WATCH_RECONNECT_LIMIT = "watchReconnectLimit";

    public final static String CONNECTION_TIMEOUT = "connectionTimeout";

    public final static String REQUEST_TIMEOUT = "requestTimeout";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String REQUEST_TIMEOUT = "requestTimeout";

    public final static String ROLLING_TIMEOUT = "rollingTimeout";

    public final static String LOGGING_INTERVAL = "loggingInterval";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String HTTP_PROXY = "httpProxy";

    public final static String HTTPS_PROXY = "httpsProxy";

    public final static String PROXY_USERNAME = "proxyUsername";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String OAUTH_TOKEN = "oauthToken";

    public final static String USERNAME = "username";

    public final static String PASSWORD = "password";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CLIENT_CERT_DATA = "clientCertData";

    public final static String CLIENT_KEY_FILE = "clientKeyFile";

    public final static String CLIENT_KEY_DATA = "clientKeyData";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CLIENT_KEY_ALGO = "clientKeyAlgo";

    public final static String CLIENT_KEY_PASSPHRASE = "clientKeyPassphrase";

    public final static String OAUTH_TOKEN = "oauthToken";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CLIENT_KEY_DATA = "clientKeyData";

    public final static String CLIENT_KEY_ALGO = "clientKeyAlgo";

    public final static String CLIENT_KEY_PASSPHRASE = "clientKeyPassphrase";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CA_CERT_DATA = "caCertData";

    public final static String CLIENT_CERT_FILE = "clientCertFile";

    public final static String CLIENT_CERT_DATA = "clientCertData";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String LOGGING_INTERVAL = "loggingInterval";

    public final static String HTTP_PROXY = "httpProxy";

    public final static String HTTPS_PROXY = "httpsProxy";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String ROLLING_TIMEOUT = "rollingTimeout";

    public final static String LOGGING_INTERVAL = "loggingInterval";

    public final static String HTTP_PROXY = "httpProxy";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CA_CERT_FILE = "caCertFile";

    public final static String CA_CERT_DATA = "caCertData";

    public final static String CLIENT_CERT_FILE = "clientCertFile";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CLIENT_KEY_FILE = "clientKeyFile";

    public final static String CLIENT_KEY_DATA = "clientKeyData";

    public final static String CLIENT_KEY_ALGO = "clientKeyAlgo";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String PROXY_USERNAME = "proxyUsername";

    public final static String PROXY_PASSWORD = "proxyPassword";

    public final static String NO_PROXY = "noProxy";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public static final String DEFAULT_MASTER_URL = "https://kubernetes.default.svc";

    public final static String ENABLE_REGISTER = "enableRegister";

    public final static String TRUST_CERTS = "trustCerts";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String PASSWORD = "password";

    public final static String WATCH_RECONNECT_INTERVAL = "watchReconnectInterval";

    public final static String WATCH_RECONNECT_LIMIT = "watchReconnectLimit";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public static final String HTTP2_DISABLE = "http2Disable";

    public final static String NAMESPACE = "namespace";

    public final static String API_VERSION = "apiVersion";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String API_VERSION = "apiVersion";

    public final static String CA_CERT_FILE = "caCertFile";

    public final static String CA_CERT_DATA = "caCertData";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String TRUST_CERTS = "trustCerts";

    public final static String USE_HTTPS = "useHttps";

    public static final String HTTP2_DISABLE = "http2Disable";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CLIENT_KEY_PASSPHRASE = "clientKeyPassphrase";

    public final static String OAUTH_TOKEN = "oauthToken";

    public final static String USERNAME = "username";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CLIENT_CERT_FILE = "clientCertFile";

    public final static String CLIENT_CERT_DATA = "clientCertData";

    public final static String CLIENT_KEY_FILE = "clientKeyFile";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String CONNECTION_TIMEOUT = "connectionTimeout";

    public final static String REQUEST_TIMEOUT = "requestTimeout";

    public final static String ROLLING_TIMEOUT = "rollingTimeout";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String ENABLE_REGISTER = "enableRegister";

    public final static String TRUST_CERTS = "trustCerts";

    public final static String USE_HTTPS = "useHttps";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String WATCH_RECONNECT_INTERVAL = "watchReconnectInterval";

    public final static String WATCH_RECONNECT_LIMIT = "watchReconnectLimit";

    public final static String CONNECTION_TIMEOUT = "connectionTimeout";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
    public final static String USERNAME = "username";

    public final static String PASSWORD = "password";

    public final static String WATCH_RECONNECT_INTERVAL = "watchReconnectInterval";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
    private final static ConcurrentHashMap<String, SharedIndexInformer<Service>> SERVICE_INFORMER = new ConcurrentHashMap<>(64);

    private final static ConcurrentHashMap<String, SharedIndexInformer<Pod>> PODS_INFORMER = new ConcurrentHashMap<>(64);

    private final static ConcurrentHashMap<String, SharedIndexInformer<Endpoints>> ENDPOINTS_INFORMER = new ConcurrentHashMap<>(64);
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
    private final boolean enableRegister;

    public final static String KUBERNETES_PROPERTIES_KEY = "io.dubbo/metadata";

    private final static ConcurrentHashMap<String, AtomicLong> SERVICE_UPDATE_TIME = new ConcurrentHashMap<>(64);
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
    public final static String KUBERNETES_PROPERTIES_KEY = "io.dubbo/metadata";

    private final static ConcurrentHashMap<String, AtomicLong> SERVICE_UPDATE_TIME = new ConcurrentHashMap<>(64);

    private final static ConcurrentHashMap<String, SharedIndexInformer<Service>> SERVICE_INFORMER = new ConcurrentHashMap<>(64);
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
    private final static ConcurrentHashMap<String, AtomicLong> SERVICE_UPDATE_TIME = new ConcurrentHashMap<>(64);

    private final static ConcurrentHashMap<String, SharedIndexInformer<Service>> SERVICE_INFORMER = new ConcurrentHashMap<>(64);

    private final static ConcurrentHashMap<String, SharedIndexInformer<Pod>> PODS_INFORMER = new ConcurrentHashMap<>(64);
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
    private final static ConcurrentHashMap<String, SharedIndexInformer<Pod>> PODS_INFORMER = new ConcurrentHashMap<>(64);

    private final static ConcurrentHashMap<String, SharedIndexInformer<Endpoints>> ENDPOINTS_INFORMER = new ConcurrentHashMap<>(64);

    public KubernetesServiceDiscovery(ApplicationModel applicationModel, URL registryURL) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/FutureContext.java`
#### Snippet
```java
     * Whether clear future once get
     */
    private final static boolean clearFutureAfterGet = Boolean.parseBoolean(System.getProperty(CommonConstants.CLEAR_FUTURE_AFTER_GET, "false"));

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ProfilerServerFilter.java`
#### Snippet
```java
public class ProfilerServerFilter implements Filter, BaseFilter.Listener {
    private final static String CLIENT_IP_KEY = "client_ip";
    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ProfilerServerFilter.class);

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ProfilerServerFilter.java`
#### Snippet
```java
@Activate(group = PROVIDER, order = Integer.MIN_VALUE)
public class ProfilerServerFilter implements Filter, BaseFilter.Listener {
    private final static String CLIENT_IP_KEY = "client_ip";
    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ProfilerServerFilter.class);

```

### MissortedModifiers
Missorted modifiers `final public`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockProtocol.java`
#### Snippet
```java
 * MockProtocol is used for generating a mock invoker by URL and type on consumer side
 */
final public class MockProtocol extends AbstractProtocol {

    @Override
```

### MissortedModifiers
Missorted modifiers `final public`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractExporter.java`
#### Snippet
```java

    @Override
    final public void unexport() {
        if (unexported) {
            return;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
final public class MockInvoker<T> implements Invoker<T> {
    private final ProxyFactory proxyFactory;
    private final static Map<String, Invoker<?>> MOCK_MAP = new ConcurrentHashMap<String, Invoker<?>>();
    private final static Map<String, Throwable> THROWABLE_MAP = new ConcurrentHashMap<String, Throwable>();

```

### MissortedModifiers
Missorted modifiers `final public`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
import static org.apache.dubbo.rpc.Constants.THROW_PREFIX;

final public class MockInvoker<T> implements Invoker<T> {
    private final ProxyFactory proxyFactory;
    private final static Map<String, Invoker<?>> MOCK_MAP = new ConcurrentHashMap<String, Invoker<?>>();
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
    private final ProxyFactory proxyFactory;
    private final static Map<String, Invoker<?>> MOCK_MAP = new ConcurrentHashMap<String, Invoker<?>>();
    private final static Map<String, Throwable> THROWABLE_MAP = new ConcurrentHashMap<String, Throwable>();

    private final URL url;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java

public class MultiValueCreator {
    private final static String SPRING_MultiValueMap = "org.springframework.util.LinkedMultiValueMap";
    private final static String JAVAX_MultiValueMap = "org.jboss.resteasy.specimpl.MultivaluedMapImpl";

```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
public class MultiValueCreator {
    private final static String SPRING_MultiValueMap = "org.springframework.util.LinkedMultiValueMap";
    private final static String JAVAX_MultiValueMap = "org.jboss.resteasy.specimpl.MultivaluedMapImpl";

    private static Class multiValueMapClass = null;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java

public class ReflectUtils {
    private final static Class[] EMPTY_CLASS_ARRAY = new Class[0];
    private final static Object[] EMPTY_OBJECT_ARRAY = new Object[0];

```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
public class ReflectUtils {
    private final static Class[] EMPTY_CLASS_ARRAY = new Class[0];
    private final static Object[] EMPTY_OBJECT_ARRAY = new Object[0];

    public static Field getField(Class clazz, String field) throws IllegalAccessException {
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/ExecutableElementComparator.java`
#### Snippet
```java
public class ExecutableElementComparator implements Comparator<ExecutableElement> {

    public final static ExecutableElementComparator INSTANCE = new ExecutableElementComparator();

    private ExecutableElementComparator() {
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
public abstract class AbstractServiceRestMetadataResolver implements ServiceRestMetadataResolver {

    private final static ThreadLocal<Map<String, Object>> threadLocalCache = withInitial(HashMap::new);

    private final static Map<String, List<AnnotatedMethodParameterProcessor>> parameterProcessorsMap = loadAnnotatedMethodParameterProcessors();
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
    private final static ThreadLocal<Map<String, Object>> threadLocalCache = withInitial(HashMap::new);

    private final static Map<String, List<AnnotatedMethodParameterProcessor>> parameterProcessorsMap = loadAnnotatedMethodParameterProcessors();

    private final String processorName = getClass().getSimpleName();
```

### MissortedModifiers
Missorted modifiers `final public`
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/utils/SecurityNames.java`
#### Snippet
```java
package org.apache.dubbo.spring.security.utils;

final public class SecurityNames {

    public static final String SECURITY_AUTHENTICATION_CONTEXT_KEY = "security_authentication_context";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/DubboConfigAliasPostProcessor.java`
#### Snippet
```java
     * The bean name of {@link DubboConfigConfigurationRegistrar}
     */
    public final static String BEAN_NAME = "dubboConfigAliasPostProcessor";

    private BeanDefinitionRegistry registry;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboConfigApplicationListener.java`
#### Snippet
```java
public class DubboConfigApplicationListener implements ApplicationListener<DubboConfigInitEvent>, ApplicationContextAware {

    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(DubboConfigApplicationListener.class);

    private ApplicationContext applicationContext;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
    BeanFactoryAware, BeanClassLoaderAware, EnvironmentAware, DisposableBean {

    private final static int CACHE_SIZE = Integer.getInteger("", 32);

    private final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(getClass());
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
    public static final String BEAN_NAME = "dubboServiceAnnotationPostProcessor";

    private final static List<Class<? extends Annotation>> serviceAnnotationTypes = asList(
            // @since 2.7.7 Add the @DubboService , the issue : https://github.com/apache/dubbo/issues/6007
            DubboService.class,
```

### MissortedModifiers
Missorted modifiers `volatile static`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
    private static final Object globalLock = new Object();

    private volatile static FrameworkModel defaultInstance;

    private static final List<FrameworkModel> allInstances = new CopyOnWriteArrayList<>();
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodComparator.java`
#### Snippet
```java
public class MethodComparator implements Comparator<Method> {

    public final static MethodComparator INSTANCE = new MethodComparator();

    private MethodComparator() {
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CharSequenceComparator.java`
#### Snippet
```java
public class CharSequenceComparator implements Comparator<CharSequence> {

    public final static CharSequenceComparator INSTANCE = new CharSequenceComparator();

    private CharSequenceComparator() {
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
    private volatile transient String string;
    private volatile transient String identity;
    private volatile transient String parameter;

    public ServiceConfigURL() {
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java


    private volatile transient ConcurrentMap<String, URL> urls;
    private volatile transient ConcurrentMap<String, Number> numbers;
    private volatile transient ConcurrentMap<String, Map<String, Number>> methodNumbers;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
    private volatile transient ConcurrentMap<String, Map<String, Number>> methodNumbers;
    private volatile transient String full;
    private volatile transient String string;
    private volatile transient String identity;
    private volatile transient String parameter;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
    private volatile transient String full;
    private volatile transient String string;
    private volatile transient String identity;
    private volatile transient String parameter;

```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java

    private volatile transient ConcurrentMap<String, URL> urls;
    private volatile transient ConcurrentMap<String, Number> numbers;
    private volatile transient ConcurrentMap<String, Map<String, Number>> methodNumbers;
    private volatile transient String full;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
    private volatile transient ConcurrentMap<String, URL> urls;
    private volatile transient ConcurrentMap<String, Number> numbers;
    private volatile transient ConcurrentMap<String, Map<String, Number>> methodNumbers;
    private volatile transient String full;
    private volatile transient String string;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
    private volatile transient ConcurrentMap<String, Number> numbers;
    private volatile transient ConcurrentMap<String, Map<String, Number>> methodNumbers;
    private volatile transient String full;
    private volatile transient String string;
    private volatile transient String identity;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
    protected boolean enableCompressed;

    private final static URLParam EMPTY_PARAM = new URLParam(new BitSet(0), Collections.emptyMap(), Collections.emptyMap(), Collections.emptyMap(), "");

    protected URLParam() {
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Mixin.java`
#### Snippet
```java
     * @return instance.
     */
    abstract public Object newInstance(Object[] ds);

    public static interface MixinAware {
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-common/src/main/java/org/apache/dubbo/common/profiler/ProfilerSwitch.java`
#### Snippet
```java
 */
public class ProfilerSwitch {
    private final static AtomicBoolean enableDetailProfiler = new AtomicBoolean(false);

    private final static AtomicBoolean enableSimpleProfiler = new AtomicBoolean(true);
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-common/src/main/java/org/apache/dubbo/common/profiler/ProfilerSwitch.java`
#### Snippet
```java
    private final static AtomicBoolean enableDetailProfiler = new AtomicBoolean(false);

    private final static AtomicBoolean enableSimpleProfiler = new AtomicBoolean(true);

    private final static AtomicReference<Double> warnPercent = new AtomicReference<>(0.75);
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-common/src/main/java/org/apache/dubbo/common/profiler/ProfilerSwitch.java`
#### Snippet
```java
    private final static AtomicBoolean enableSimpleProfiler = new AtomicBoolean(true);

    private final static AtomicReference<Double> warnPercent = new AtomicReference<>(0.75);

    public static void enableSimpleProfiler() {
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @return property name array.
     */
    abstract public String[] getPropertyNames();

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @return return value.
     */
    abstract public Object invokeMethod(Object instance, String mn, Class<?>[] types, Object[] args) throws NoSuchMethodException, InvocationTargetException;
}

```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @return method name array.
     */
    abstract public String[] getMethodNames();

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @return value.
     */
    abstract public Object getPropertyValue(Object instance, String pn) throws NoSuchPropertyException, IllegalArgumentException;

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @return has or has not.
     */
    abstract public boolean hasProperty(String name);

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @return method name array.
     */
    abstract public String[] getDeclaredMethodNames();

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @param pv       property value.
     */
    abstract public void setPropertyValue(Object instance, String pn, Object pv) throws NoSuchPropertyException, IllegalArgumentException;

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @return Property type or nul.
     */
    abstract public Class<?> getPropertyType(String pn);

    /**
```

### MissortedModifiers
Missorted modifiers `volatile static`
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(GlobalResourcesRepository.class);

    private volatile static GlobalResourcesRepository instance;
    private volatile ExecutorService executorService;
    private final List<Disposable> oneoffDisposables = new CopyOnWriteArrayList<>();
```

### MissortedModifiers
Missorted modifiers `transient final`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     * The url of the reference service
     */
    protected transient final List<URL> urls = new ArrayList<URL>();

    @Transient
```

### MissortedModifiers
Missorted modifiers `final public`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyCodecAdapter.java`
#### Snippet
```java
 * NettyCodecAdapter.
 */
final public class NettyCodecAdapter {

    private final ChannelHandler encoder = new InternalEncoder();
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/RouterSnapshotFilter.java`
#### Snippet
```java

    private final RouterSnapshotSwitcher switcher;
    private final static Logger logger = LoggerFactory.getLogger(RouterSnapshotFilter.class);

    public RouterSnapshotFilter(FrameworkModel frameworkModel) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/RouterSnapshotSwitcher.java`
#### Snippet
```java
    private final Set<String> enabledService = new ConcurrentHashSet<>();

    private final static int MAX_LENGTH = 1 << 5; // 2 ^ 5 = 31

    private final AtomicInteger offset = new AtomicInteger(0);
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailbackCluster.java`
#### Snippet
```java
public class FailbackCluster extends AbstractCluster {

    public final static String NAME = "failback";

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailfastCluster.java`
#### Snippet
```java
public class FailfastCluster extends AbstractCluster {

    public final static String NAME = "failfast";

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/ForkingCluster.java`
#### Snippet
```java
public class ForkingCluster extends AbstractCluster {

    public final static String NAME = "forking";

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailsafeCluster.java`
#### Snippet
```java
public class FailsafeCluster extends AbstractCluster {

    public final static String NAME = "failsafe";

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailoverCluster.java`
#### Snippet
```java
public class FailoverCluster extends AbstractCluster {

    public final static String NAME = "failover";

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/registry/ZoneAwareCluster.java`
#### Snippet
```java
public class ZoneAwareCluster extends AbstractCluster {

    public final static String NAME = "zone-aware";

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java
    private final BitSet rootSet;
    private volatile List<E> originList;
    private final static BitList emptyList = new BitList(Collections.emptyList());
    private volatile List<E> tailList = null;

```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ReferenceCountExchangeClient.java`
#### Snippet
```java
final class ReferenceCountExchangeClient implements ExchangeClient {

    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ReferenceCountExchangeClient.class);
    private final URL url;
    private final AtomicInteger referenceCount = new AtomicInteger(0);
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/LazyConnectExchangeClient.java`
#### Snippet
```java
final class LazyConnectExchangeClient implements ExchangeClient {

    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(LazyConnectExchangeClient.class);
    private final boolean requestWithWarning;
    private final URL url;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-rpc/dubbo-rpc-injvm/src/main/java/org/apache/dubbo/rpc/protocol/injvm/DefaultParamDeepCopyUtil.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(DefaultParamDeepCopyUtil.class);

    public final static String NAME = "default";

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/NodeBuilder.java`
#### Snippet
```java
public class NodeBuilder {

    private final static String SVC_CLUSTER_LOCAL = ".svc.cluster.local";

    public static Node build() {
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String ECC_SIG_ALG_KEY = "ECC_SIGNATURE_ALGORITHM";

    public final static String DEFAULT_ECC_SIG_ALG = "ECDSA";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String KUBERNETES_NAMESPACE_PATH = "/var/run/secrets/kubernetes.io/serviceaccount/namespace";

    public final static String RSA_KEY_SIZE_KEY = "RSA_KEY_SIZE";

    public final static String DEFAULT_RSA_KEY_SIZE = "2048";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * The grace period ratio for the cert rotation
     */
    public final static String SECRET_GRACE_PERIOD_RATIO_KEY = "SECRET_GRACE_PERIOD_RATIO";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String DEFAULT_JWT_POLICY = "first-party-jwt";

    public final static String FIRST_PARTY_JWT = "first-party-jwt";

    public final static String THIRD_PARTY_JWT = "third-party-jwt";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String SA = "/sa/";

    public final static String JWT_POLICY = "JWT_POLICY";

    public final static String DEFAULT_JWT_POLICY = "first-party-jwt";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String ISTIO_META_CLUSTER_ID_KEY = "ISTIO_META_CLUSTER_ID";

    public final static String PILOT_CERT_PROVIDER_KEY = "PILOT_CERT_PROVIDER";

    public final static String ISTIO_PILOT_CERT_PROVIDER = "istiod";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String ISTIO_SA_PATH = "/var/run/secrets/tokens/istio-token";

    public final static String ISTIO_CA_PATH = "/var/run/secrets/istio/root-cert.pem";

    public final static String KUBERNETES_NAMESPACE_PATH = "/var/run/secrets/kubernetes.io/serviceaccount/namespace";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String PILOT_CERT_PROVIDER_KEY = "PILOT_CERT_PROVIDER";

    public final static String ISTIO_PILOT_CERT_PROVIDER = "istiod";

    public final static String DEFAULT_ISTIO_META_CLUSTER_ID = "Kubernetes";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String KUBERNETES_SA_PATH = "/var/run/secrets/kubernetes.io/serviceaccount/token";

    public final static String KUBERNETES_CA_PATH = "/var/run/secrets/kubernetes.io/serviceaccount/ca.crt";

    public final static String ISTIO_SA_PATH = "/var/run/secrets/tokens/istio-token";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * k8s jwt token
     */
    public final static String KUBERNETES_SA_PATH = "/var/run/secrets/kubernetes.io/serviceaccount/token";

    public final static String KUBERNETES_CA_PATH = "/var/run/secrets/kubernetes.io/serviceaccount/ca.crt";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String FIRST_PARTY_JWT = "first-party-jwt";

    public final static String THIRD_PARTY_JWT = "third-party-jwt";

}
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String KUBERNETES_CA_PATH = "/var/run/secrets/kubernetes.io/serviceaccount/ca.crt";

    public final static String ISTIO_SA_PATH = "/var/run/secrets/tokens/istio-token";

    public final static String ISTIO_CA_PATH = "/var/run/secrets/istio/root-cert.pem";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String NS = "/ns/";

    public final static String SA = "/sa/";

    public final static String JWT_POLICY = "JWT_POLICY";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String DEFAULT_TRUST_DOMAIN = "cluster.local";

    public final static String WORKLOAD_NAMESPACE_KEY = "WORKLOAD_NAMESPACE";

    public final static String DEFAULT_WORKLOAD_NAMESPACE = "default";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String ISTIO_CA_PATH = "/var/run/secrets/istio/root-cert.pem";

    public final static String KUBERNETES_NAMESPACE_PATH = "/var/run/secrets/kubernetes.io/serviceaccount/namespace";

    public final static String RSA_KEY_SIZE_KEY = "RSA_KEY_SIZE";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * The grace period ratio for the cert rotation, by default 0.5
     */
    public final static String DEFAULT_SECRET_GRACE_PERIOD_RATIO = "0.5";

    public final static String ISTIO_META_CLUSTER_ID_KEY = "ISTIO_META_CLUSTER_ID";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * The cert lifetime requested by istio agent
     */
    public final static String SECRET_TTL_KEY = "SECRET_TTL";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String WORKLOAD_NAMESPACE_KEY = "WORKLOAD_NAMESPACE";

    public final static String DEFAULT_WORKLOAD_NAMESPACE = "default";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String ISTIO_PILOT_CERT_PROVIDER = "istiod";

    public final static String DEFAULT_ISTIO_META_CLUSTER_ID = "Kubernetes";

    public final static String SPIFFE = "spiffe://";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String RSA_KEY_SIZE_KEY = "RSA_KEY_SIZE";

    public final static String DEFAULT_RSA_KEY_SIZE = "2048";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * Address of the spiffe certificate provider. Defaults to discoveryAddress
     */
    public final static String CA_ADDR_KEY = "CA_ADDR";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * The trust domain for spiffe certificates
     */
    public final static String TRUST_DOMAIN_KEY = "TRUST_DOMAIN";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * The cert lifetime default value 24h0m0s
     */
    public final static String DEFAULT_SECRET_TTL = "86400"; //24 * 60 * 60

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String DEFAULT_ISTIO_META_CLUSTER_ID = "Kubernetes";

    public final static String SPIFFE = "spiffe://";

    public final static String NS = "/ns/";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * The type of ECC signature algorithm to use when generating private keys
     */
    public final static String ECC_SIG_ALG_KEY = "ECC_SIGNATURE_ALGORITHM";

    public final static String DEFAULT_ECC_SIG_ALG = "ECDSA";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String JWT_POLICY = "JWT_POLICY";

    public final static String DEFAULT_JWT_POLICY = "first-party-jwt";

    public final static String FIRST_PARTY_JWT = "first-party-jwt";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * The trust domain for spiffe certificates default value
     */
    public final static String DEFAULT_TRUST_DOMAIN = "cluster.local";

    public final static String WORKLOAD_NAMESPACE_KEY = "WORKLOAD_NAMESPACE";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
     * CA and xDS services
     */
    public final static String DEFAULT_CA_ADDR = "istiod.istio-system.svc:15012";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String SPIFFE = "spiffe://";

    public final static String NS = "/ns/";

    public final static String SA = "/sa/";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
    public final static String DEFAULT_SECRET_GRACE_PERIOD_RATIO = "0.5";

    public final static String ISTIO_META_CLUSTER_ID_KEY = "ISTIO_META_CLUSTER_ID";

    public final static String PILOT_CERT_PROVIDER_KEY = "PILOT_CERT_PROVIDER";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/SetProfilerWarnPercent.java`
#### Snippet
```java
@Cmd(name = "setProfilerWarnPercent", example = "setProfilerWarnPercent 0.75", summary = "Disable Dubbo Invocation Profiler.")
public class SetProfilerWarnPercent implements BaseCommand {
    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(SetProfilerWarnPercent.class);

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/DefaultCommandExecutor.java`
#### Snippet
```java

public class DefaultCommandExecutor implements CommandExecutor {
    private final static Logger logger = LoggerFactory.getLogger(DefaultCommandExecutor.class);
    private final FrameworkModel frameworkModel;

```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/DisableSimpleProfiler.java`
#### Snippet
```java
@Cmd(name = "disableSimpleProfiler", summary = "Disable Dubbo Invocation Profiler.")
public class DisableSimpleProfiler implements BaseCommand {
    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(DisableSimpleProfiler.class);

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/DisableDetailProfiler.java`
#### Snippet
```java
@Cmd(name = "disableDetailProfiler", summary = "Disable Dubbo Invocation Profiler.")
public class DisableDetailProfiler implements BaseCommand {
    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(DisableDetailProfiler.class);

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/EnableDetailProfiler.java`
#### Snippet
```java
@Cmd(name = "enableDetailProfiler", summary = "Enable Dubbo Invocation Profiler.")
public class EnableDetailProfiler implements BaseCommand {
    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(EnableDetailProfiler.class);

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/EnableSimpleProfiler.java`
#### Snippet
```java
@Cmd(name = "enableSimpleProfiler", summary = "Enable Dubbo Invocation Profiler.")
public class EnableSimpleProfiler implements BaseCommand {
    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(EnableSimpleProfiler.class);

    @Override
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractServiceNameMapping.java`
#### Snippet
```java
     * @return
     */
    abstract public Set<String> getAndListen(URL url, MappingListener mappingListener);

    abstract protected void removeListener(URL url, MappingListener mappingListener);
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractServiceNameMapping.java`
#### Snippet
```java
    abstract public Set<String> getAndListen(URL url, MappingListener mappingListener);

    abstract protected void removeListener(URL url, MappingListener mappingListener);

    @Override
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractServiceNameMapping.java`
#### Snippet
```java
     * @return
     */
    abstract public Set<String> get(URL url);

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-metadata/dubbo-metadata-report-zookeeper/src/main/java/org/apache/dubbo/metadata/store/zookeeper/ZookeeperMetadataReport.java`
#### Snippet
```java
public class ZookeeperMetadataReport extends AbstractMetadataReport {

    private final static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ZookeeperMetadataReport.class);

    private final String root;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
public abstract class AbstractMetadataReport implements MetadataReport {

    protected final static String DEFAULT_ROOT = "dubbo";

    private static final int ONE_DAY_IN_MILLISECONDS = 60 * 24 * 60 * 1000;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java

    // cached numbers
    private volatile transient Set<String> providerFirstParams;
    // one instance address url serves only one protocol.
    private final transient String protocol;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        // cached method params
        private volatile transient Map<String, Map<String, String>> methodParams;
        private volatile transient Map<String, Map<String, String>> consumerMethodParams;
        // cached numbers
        private volatile transient Map<String, Number> numbers;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        private volatile transient Map<String, Map<String, Number>> methodNumbers;
        // service + group + version
        private volatile transient String serviceKey;
        // service + group + version + protocol
        private volatile transient String matchKey;
```

### MissortedModifiers
Missorted modifiers `transient final`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    private transient Map<String, Set<ServiceInfo>> subscribedServices;
    private transient final Map<String, String> extendParams;
    private transient final Map<String, String> instanceParams;
    protected transient volatile boolean updated = false;
    private transient ConcurrentNavigableMap<String, SortedSet<URL>> subscribedServiceURLs;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        private volatile transient String serviceKey;
        // service + group + version + protocol
        private volatile transient String matchKey;

        private volatile transient ProtocolServiceKey protocolServiceKey;
```

### MissortedModifiers
Missorted modifiers `transient final`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    // key format is '{group}/{interface name}:{version}'
    private transient Map<String, Set<ServiceInfo>> subscribedServices;
    private transient final Map<String, String> extendParams;
    private transient final Map<String, String> instanceParams;
    protected transient volatile boolean updated = false;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        private volatile transient String matchKey;

        private volatile transient ProtocolServiceKey protocolServiceKey;

        private transient URL url;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        private volatile transient Map<String, String> consumerParams;
        // cached method params
        private volatile transient Map<String, Map<String, String>> methodParams;
        private volatile transient Map<String, Map<String, String>> consumerMethodParams;
        // cached numbers
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java

        // params configured on consumer side,
        private volatile transient Map<String, String> consumerParams;
        // cached method params
        private volatile transient Map<String, Map<String, String>> methodParams;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        // cached numbers
        private volatile transient Map<String, Number> numbers;
        private volatile transient Map<String, Map<String, Number>> methodNumbers;
        // service + group + version
        private volatile transient String serviceKey;
```

### MissortedModifiers
Missorted modifiers `volatile transient`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        private volatile transient Map<String, Map<String, String>> consumerMethodParams;
        // cached numbers
        private volatile transient Map<String, Number> numbers;
        private volatile transient Map<String, Map<String, Number>> methodNumbers;
        // service + group + version
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/support/DefaultSerializationSelector.java`
#### Snippet
```java
    private final static String DEFAULT_REMOTING_SERIALIZATION_PROPERTY_KEY = "DUBBO_DEFAULT_SERIALIZATION";

    private final static String DEFAULT_REMOTING_SERIALIZATION_PROPERTY = "hessian2";

    private final static String DEFAULT_REMOTING_SERIALIZATION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/support/DefaultSerializationSelector.java`
#### Snippet
```java
public class DefaultSerializationSelector {

    private final static String DEFAULT_REMOTING_SERIALIZATION_PROPERTY_KEY = "DUBBO_DEFAULT_SERIALIZATION";

    private final static String DEFAULT_REMOTING_SERIALIZATION_PROPERTY = "hessian2";
```

### MissortedModifiers
Missorted modifiers `final static`
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/support/DefaultSerializationSelector.java`
#### Snippet
```java
    private final static String DEFAULT_REMOTING_SERIALIZATION_PROPERTY = "hessian2";

    private final static String DEFAULT_REMOTING_SERIALIZATION;

    static {
```

## RuleId[id=CollectionAddedToSelf]
### CollectionAddedToSelf
'putAll()' called on collection `attributes` with itself as argument
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
    public URL addAttributes(Map<String, Object> attributes) {
        if (attributes != null) {
            attributes.putAll(attributes);
        }
        return this;
```

## RuleId[id=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/AbstractDependencyFilterMojo.java`
#### Snippet
```java
     * Comma separated list of groupId names to exclude (exact match).
     */
    @Parameter(property = "dubbo.excludeGroupIds", defaultValue = "")
    private String excludeGroupIds;

```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
                    oFile.close();
                } catch (IOException e) {
                    throw new DubboTestException("Failed to close file", e);
                }
            }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
                    oFile.close();
                } catch (IOException e) {
                    throw new DubboTestException("Failed to close file", e);
                }
            }
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ArrayUtils.java`
#### Snippet
```java

    public static int indexOf(String[] array, String valueToFind, int startIndex) {
        if (isEmpty(array) || valueToFind == null) {
            return -1;
        } else {
```

## RuleId[id=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStatedEvent.java`
#### Snippet
```java
     */
    public DubboBootstrap getDubboBootstrap() {
        return (DubboBootstrap) super.getSource();
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/ServiceBeanExportedEvent.java`
#### Snippet
```java
     */
    public ServiceBean getServiceBean() {
        return (ServiceBean) super.getSource();
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStopedEvent.java`
#### Snippet
```java
     */
    public DubboBootstrap getDubboBootstrap() {
        return (DubboBootstrap) super.getSource();
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/support/eager/TaskQueue.java`
#### Snippet
```java
            throw new RejectedExecutionException("Executor is shutdown!");
        }
        return super.offer(o, timeout, unit);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/support/eager/EagerThreadPoolExecutor.java`
#### Snippet
```java
        } catch (RejectedExecutionException rx) {
            // retry to offer the task into queue.
            final TaskQueue queue = (TaskQueue) super.getQueue();
            try {
                if (!queue.retryOffer(command, 0, TimeUnit.MILLISECONDS)) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/transport/TripleHttp2ClientResponseHandler.java`
#### Snippet
```java
            transportListener.onData(data.content(), data.isEndStream());
        } else {
            super.channelRead(ctx, msg);
        }
    }
```

## RuleId[id=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
        }).start();

        while (true) {
            try {
                CompletableFuture<String> hello = demoService.sayHelloAsync("world");
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `dubbo-demo/dubbo-demo-generic-call/src/main/java/org/apache/dubbo/demo/consumer/GenericApplication.java`
#### Snippet
```java
        // generic invoke
        GenericService genericService = bootstrap.getCache().get(reference);
        while (true) {
            try {
                Object genericInvokeResult = genericService.$invoke("sayHello", new String[]{String.class.getName()},
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java
        if (executorSubmit.compareAndSet(false, true)) {
            ruleMigrationFuture = ruleManageExecutor.submit(() -> {
                while (true) {
                    String rule = "";
                    try {
```

## RuleId[id=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/ObservationRegistryPostProcessor.java`
#### Snippet
```java
            List<ObservationHandler<?>> observationHandlerList =
                observationHandlers.orderedStream().collect(Collectors.toList());
            observationHandlerGrouping.ifAvailable(grouping->{
                grouping.apply(observationHandlerList,
                    observationRegistry.observationConfig());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/InvocationUtil.java`
#### Snippet
```java
                if ((usage / (1000_000L * ProfilerSwitch.getWarnPercent())) > timeout) {
                    StringBuilder attachment = new StringBuilder();
                    rpcInvocation.foreachAttachment((entry) -> {
                        attachment.append(entry.getKey()).append("=").append(entry.getValue()).append(";\n");
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ProfilerServerFilter.java`
#### Snippet
```java

            StringBuilder attachment = new StringBuilder();
            invocation.foreachAttachment((entry) -> {
                attachment.append(entry.getKey()).append("=").append(entry.getValue()).append(";\n");
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AdaptiveLoadBalanceFilter.java`
#### Snippet
```java
                }

                getExecutor().execute(() -> {
                    adaptiveMetrics.setProviderMetrics(getServiceKey(invocation), metricsMap);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AdaptiveLoadBalanceFilter.java`
#### Snippet
```java
        if (StringUtils.isNotEmpty(loadBalance)
            && LoadbalanceRules.ADAPTIVE.equals(loadBalance)) {
            getExecutor().execute(() -> {
                adaptiveMetrics.addErrorReq(getServiceKey(invocation));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
    protected Map<String, ExecutableElement> getActualMethodsMap(TypeElement type) {
        Map<String, ExecutableElement> methodsMap = new HashMap<>();
        getActualMethods(type).forEach(method -> {
            methodsMap.put(method.toString(), method);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
        StringBuilder methodSignatureBuilder = new StringBuilder();

        method.getModifiers().forEach(member -> {
            methodSignatureBuilder.append(member).append(' ');
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
            String annotationType = annotation.getAnnotationType().toString();
            parameterProcessorsMap.getOrDefault(annotationType, emptyList())
                    .forEach(parameterProcessor -> {
                        parameterProcessor.process(annotation, parameter, parameterIndex, method, metadata);
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
            sort(serviceMethods, ExecutableElementComparator.INSTANCE);

            serviceMethods.forEach(serviceMethod -> {
                resolveRestMethodMetadata(processingEnv, serviceType, serviceInterfaceType, serviceMethod)
                        .ifPresent(serviceRestMetadata.getMeta()::add);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
                .filter(this::isUsedRegistryAsMetadataCenter)
                .map(registryConfig -> registryAsMetadataCenter(registryConfig, metadataConfigToOverride))
                .forEach(metadataReportConfig -> {
                    overrideMetadataReportConfig(metadataConfigToOverride, metadataReportConfig);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ConfigCenterBean.java`
#### Snippet
```java
                    Object source = propertySource.getSource();
                    if (source instanceof Map) {
                        ((Map<String, Object>) source).forEach((k, v) -> {
                            externalProperties.put(k, (String) v);
                        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/DefaultDubboConfigBinder.java`
#### Snippet
```java
                + " , error fields: [" + errors.get(0).getField());
        if (errors.size() > 1) {
            IntStream.range(1, errors.size()).forEach(i -> {
                builder.append(", " + errors.get(i).getField());
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
                .collect(Collectors.toMap(entry -> new com.alibaba.dubbo.common.URL(entry.getKey()),
                        entry -> {
                            return entry.getValue().entrySet()
                                    .stream()
                                    .collect(Collectors.toMap(e -> e.getKey(), e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
                                    .stream()
                                    .collect(Collectors.toMap(e -> e.getKey(), e -> {
                                        return e.getValue().stream().map(url -> new com.alibaba.dubbo.common.URL(url)).collect(Collectors.toList());
                                    }));
                        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/calls/ReactorServerCalls.java`
#### Snippet
```java
                                       StreamObserver<R> responseObserver,
                                       Function<Mono<T>, Mono<R>> func) {
        func.apply(Mono.just(request)).subscribe(res -> {
            CompletableFuture.completedFuture(res)
                .whenComplete((r, t) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/ThreadlessExecutor.java`
#### Snippet
```java
    public void notifyReturn(Throwable t) {
        // an empty runnable task.
        execute(() -> {
            waitingFuture.completeExceptionally(t);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
            Map<ClassLoader, Set<java.net.URL>> resources = ClassLoaderResourceLoader.loadResources(
                fileName, classLoadersToLoad);
            resources.forEach(((classLoader, urls) -> {
                loadFromClass(extensionClasses, loadingStrategy.overridden(), urls, classLoader,
                    loadingStrategy.includedPackages(), loadingStrategy.excludedPackages(),
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/CodeGenerator.java`
#### Snippet
```java
    public static void execute(String p, Log log) {
        log.info("Start generating code:" + p);
        List<Class<?>> classes = new ClassFinder().findClassSet("org.apache.dubbo", msg -> {
            log.info(msg);
        }).stream().map(it -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleClientStream.java`
#### Snippet
```java
            final Map<String, String> reserved = filterReservedHeaders(trailers);
            final Map<String, Object> attachments = headersToMap(trailers, () -> {
                return reserved.get(TripleHeaderEnum.TRI_HEADER_CONVERT.getHeader());
            });
            final TriRpcStatus detailStatus;
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleServerStream.java`
#### Snippet
```java
                return;
            }
            executor.execute(() -> {
                listener.onCancelByRemote(TriRpcStatus.CANCELLED
                    .withDescription("Canceled by client ,errorCode=" + errorCode));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleServerStream.java`
#### Snippet
```java

            Map<String, Object> requestMetadata = headersToMap(headers, () -> {
                return Optional.ofNullable(headers.get(TripleHeaderEnum.TRI_HEADER_CONVERT.getHeader()))
                    .map(CharSequence::toString)
                    .orElse(null);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/PilotExchanger.java`
#### Snippet
```java
            // check if observed domain update ( will update endpoint observation )
            List<String> domainsToUpdate = new LinkedList<>();
            domainObserveConsumer.forEach((domain, consumer) -> {
                newResult.values().forEach(o -> {
                    Set<String> newRoute = o.searchDomain(domain);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
        log("Total found " + unclosedThreadMap.size() + " unclosed threads in " + unclosedThreadsOfTestMap.size() + " tests.");
        log("");
        unclosedThreadsOfTestMap.forEach((testClassName, threads) -> {
            printUnclosedThreads(threads, testClassName);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/ThreadPoolMetricsSampler.java`
#### Snippet
```java
        List<MetricSample> metricSamples = new ArrayList<>();

        sampleThreadPoolExecutor.forEach((name, executor) -> {
            metricSamples.addAll(createMetricsSample(name, executor));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
            String annotationType = annotation.annotationType().getName();
            parameterProcessorsMap.getOrDefault(annotationType, emptyList())
                .forEach(processor -> {
                    processor.process(annotation, parameter, parameterIndex, serviceMethod, serviceType,
                        serviceInterfaceClass, metadata);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/MetadataReportInstance.java`
#### Snippet
```java
    @Override
    public void destroy() {
        metadataReports.forEach((_k, reporter) -> {
            reporter.destroy();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/model/MigrationRule.java`
#### Snippet
```java
        if (applications != null) {
            this.applicationRules = new HashMap<>();
            applications.forEach(rule -> {
                applicationRules.put(rule.getServiceKey(), rule);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/model/MigrationRule.java`
#### Snippet
```java
        if (interfaces != null) {
            this.interfaceRules = new HashMap<>();
            interfaces.forEach(rule -> {
                interfaceRules.put(rule.getServiceKey(), rule);
            });
```

## RuleId[id=UseOfPropertiesAsHashtable]
### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/JavaCompilerPluginConfiguration.java`
#### Snippet
```java
	private String getPropertyValue(String propertyName) {
		if (this.project.getProperties().containsKey(propertyName)) {
			return this.project.getProperties().get(propertyName).toString();
		}
		return null;
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.putAll()` on properties object
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/OrderedPropertiesConfiguration.java`
#### Snippet
```java
        //override the properties.
        for (OrderedPropertiesProvider orderedPropertiesProvider : orderedPropertiesProviders) {
            properties.putAll(orderedPropertiesProvider.initProperties());
        }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                            continue;
                        }
                        properties.put(key, entry.getValue().toString());
                    }
                }
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/util/JaketConfigurationUtils.java`
#### Snippet
```java
        try {
            props.load(inStream);
            String value = (String) props.get("included_interface_packages");
            if (StringUtils.isNotEmpty(value)) {
                includedInterfacePackages = value.split(",");
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
        }
        String serverAddr = serverAddrBuilder.toString();
        properties.put(SERVER_ADDR, serverAddr);
    }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
        properties.putAll(parameters);
        if (StringUtils.isNotEmpty(url.getUsername())) {
            properties.put(USERNAME, url.getUsername());
        }
        if (StringUtils.isNotEmpty(url.getPassword())) {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
        }
        if (StringUtils.isNotEmpty(url.getPassword())) {
            properties.put(PASSWORD, url.getPassword());
        }
    }
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosConnectionManager.java`
#### Snippet
```java
        properties.putAll(parameters);
        if (StringUtils.isNotEmpty(url.getUsername())) {
            properties.put(USERNAME, url.getUsername());
        }
        if (StringUtils.isNotEmpty(url.getPassword())) {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosConnectionManager.java`
#### Snippet
```java
        }
        if (StringUtils.isNotEmpty(url.getPassword())) {
            properties.put(PASSWORD, url.getPassword());
        }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosConnectionManager.java`
#### Snippet
```java

        String serverAddr = serverAddrBuilder.toString();
        properties.put(SERVER_ADDR, serverAddr);
    }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
            builder.append(key);
            builder.append(": ");
            builder.append(properties.get(key));
            builder.append("\n");
        }
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
        }
        String serverAddr = serverAddrBuilder.toString();
        properties.put(SERVER_ADDR, serverAddr);
    }

```

## RuleId[id=EmptyMethod]
### EmptyMethod
The method is empty
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java

    @Override
    public void discordAddresses() {
        // do nothing by default
    }
```

### EmptyMethod
The method is empty
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/AbstractStateRouter.java`
#### Snippet
```java

    @Override
    public void notify(BitList<Invoker<T>> invokers) {
        // default empty implement
    }
```

### EmptyMethod
The method is empty
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/TailStateRouter.java`
#### Snippet
```java

    @Override
    public void setNextRouter(StateRouter<T> nextRouter) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/TailStateRouter.java`
#### Snippet
```java

    @Override
    public void notify(BitList<Invoker<T>> invokers) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/BatchExecutorQueue.java`
#### Snippet
```java
    }

    protected void prepare(T item) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/BatchExecutorQueue.java`
#### Snippet
```java
    }

    protected void flush(T item) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStore.java`
#### Snippet
```java

        @Override
        public synchronized void refreshCache(Map<String, String> properties, String comment, long maxFileSize) {
            // No-op.
        }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/concurrent/DiscardPolicy.java`
#### Snippet
```java

    @Override
    public void reject(final E e, final Queue<E> queue) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/AbstractDynamicConfiguration.java`
#### Snippet
```java

    @Override
    public void addListener(String key, String group, ConfigurationListener listener) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/AbstractDynamicConfiguration.java`
#### Snippet
```java

    @Override
    public void removeListener(String key, String group, ConfigurationListener listener) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    @Override
    protected void doClose() throws Exception {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/nop/NopDynamicConfiguration.java`
#### Snippet
```java

    @Override
    public void addListener(String key, String group, ConfigurationListener listener) {
        // no-op
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/nop/NopDynamicConfiguration.java`
#### Snippet
```java

    @Override
    public void removeListener(String key, String group, ConfigurationListener listener) {
        // no-op
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/nop/NopDynamicConfiguration.java`
#### Snippet
```java

    @Override
    public void close() throws Exception {
        // no-op
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/context/LifecycleAdapter.java`
#### Snippet
```java

    @Override
    public void initialize() throws IllegalStateException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/context/LifecycleAdapter.java`
#### Snippet
```java

    @Override
    public void start() throws IllegalStateException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/context/LifecycleAdapter.java`
#### Snippet
```java

    @Override
    public void destroy() throws IllegalStateException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/DeployListenerAdapter.java`
#### Snippet
```java
public class DeployListenerAdapter<E extends ScopeModel> implements DeployListener<E>{
    @Override
    public void onStarting(E scopeModel) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/DeployListenerAdapter.java`
#### Snippet
```java

    @Override
    public void onStarted(E scopeModel) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/DeployListenerAdapter.java`
#### Snippet
```java

    @Override
    public void onStopping(E scopeModel) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/DeployListenerAdapter.java`
#### Snippet
```java

    @Override
    public void onStopped(E scopeModel) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/DeployListenerAdapter.java`
#### Snippet
```java

    @Override
    public void onFailure(E scopeModel, Throwable cause) {
    }
}
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionDirector.java`
#### Snippet
```java
    }

    public void removeAllCachedLoader() {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    @Deprecated
    public static void resetExtensionLoader(Class type) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/inject/AdaptiveExtensionInjector.java`
#### Snippet
```java

    @Override
    public void start() throws IllegalStateException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/inject/AdaptiveExtensionInjector.java`
#### Snippet
```java

    @Override
    public void destroy() throws IllegalStateException {
    }
}
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/jdk/JdkLoggerAdapter.java`
#### Snippet
```java

    @Override
    public void setFile(File file) {
        // ignore
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/log4j/Log4jLoggerAdapter.java`
#### Snippet
```java

    @Override
    public void setFile(File file) {
        // ignore
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/log4j2/Log4j2LoggerAdapter.java`
#### Snippet
```java

    @Override
    public void setFile(File file) {
        // ignore
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocal.java`
#### Snippet
```java
     * Invoked when this thread local variable is removed by {@link #remove()}.
     */
    protected void onRemoval(@SuppressWarnings("unused") V value) throws Exception {
    }
}
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/SerializeSecurityConfigurator.java`
#### Snippet
```java

    @Override
    public void onRemoveClassLoader(ModuleModel scopeModel, ClassLoader classLoader) {
        // ignore
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     * @param newScopeModel
     */
    protected void postProcessAfterScopeModelChanged(ScopeModel oldScopeModel, ScopeModel newScopeModel) {
        // remove this config from old ConfigManager
//        if (oldScopeModel != null && oldScopeModel instanceof ApplicationModel) {
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
    }

    protected void processExtraRefresh(String preferredPrefix, InmemoryConfiguration subPropsConfiguration) {
        // process extra refresh
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
    }

    protected void preProcessRefresh() {
        // pre-process refresh
    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     * @see AbstractConfig#appendAttributes(Map, Object)
     */
    protected void checkDefault() {
    }

```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java


    public void refreshAll() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceRepository.java`
#### Snippet
```java
    }

    public void destroy() {
        //TODO destroy application service repository
    }
```

### EmptyMethod
The method is empty
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java

        @Override
        public void setValue(Object value) {

        }
```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java

    @Override
    public void onStarting(ApplicationModel scopeModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java

    @Override
    public synchronized void onStarted(ApplicationModel applicationModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java

    @Override
    public synchronized void onStopping(ApplicationModel scopeModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ExporterDeployListener.java`
#### Snippet
```java

    @Override
    public void onFailure(ApplicationModel scopeModel, Throwable cause) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/SpringScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeFrameworkModel(FrameworkModel frameworkModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/SpringScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeApplicationModel(ApplicationModel applicationModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/SpringScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
     * @throws Exception
     */
    protected void prepareInjection(AnnotatedInjectionMetadata metadata) throws Exception {
    }

```

### EmptyMethod
The method is empty
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java

    @Deprecated
    public static void addApplicationContext(final ApplicationContext context) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesRegistry.java`
#### Snippet
```java

    @Override
    public void doRegister(URL url) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesRegistry.java`
#### Snippet
```java

    @Override
    public void doUnregister(URL url) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesRegistry.java`
#### Snippet
```java

    @Override
    public void doSubscribe(URL url, NotifyListener listener) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesRegistry.java`
#### Snippet
```java

    @Override
    public void doUnsubscribe(URL url, NotifyListener listener) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/NopKubernetesMeshEnvListener.java`
#### Snippet
```java

    @Override
    public void onSubscribe(String appName, MeshAppRuleListener listener) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/NopKubernetesMeshEnvListener.java`
#### Snippet
```java

    @Override
    public void onUnSubscribe(String appName) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/MetadataScopeModelInitializer.java`
#### Snippet
```java
public class MetadataScopeModelInitializer implements ScopeModelInitializer {
    @Override
    public void initializeFrameworkModel(FrameworkModel frameworkModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/MetadataScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java

    @Override
    public void storeProviderMetadata(MetadataIdentifier providerMetadataIdentifier, ServiceDefinition serviceDefinition) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java

    @Override
    public void storeConsumerMetadata(MetadataIdentifier consumerMetadataIdentifier, Map<String, String> serviceParameterMap) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java

    @Override
    public void destroy() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java

    @Override
    public void saveServiceMetadata(ServiceMetadataIdentifier metadataIdentifier, URL url) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java

    @Override
    public void removeServiceMetadata(ServiceMetadataIdentifier metadataIdentifier) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/NopMetadataReport.java`
#### Snippet
```java

    @Override
    public void saveSubscribedData(SubscriberMetadataIdentifier subscriberMetadataIdentifier, Set<String> urls) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractAnnotatedMethodParameterProcessor.java`
#### Snippet
```java


    protected void process(String annotationValue, String defaultValue, Annotation annotation, Parameter parameter,
                           int parameterIndex, Method method, RestMethodMetadata restMethodMetadata) {

```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/DefaultServiceRestMetadataResolver.java`
#### Snippet
```java

    @Override
    protected void processProduces(Method serviceMethod, Class<?> serviceType, Class<?> serviceInterfaceClass, Set<String> produces) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/DefaultServiceRestMetadataResolver.java`
#### Snippet
```java

    @Override
    protected void processConsumes(Method serviceMethod, Class<?> serviceType, Class<?> serviceInterfaceClass, Set<String> consumes) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
     * @param metadata      {@link RestMethodMetadata} maybe updated
     */
    protected void postProcessRestMethodMetadata(ProcessingEnvironment processingEnv, TypeElement serviceType,
                                                 ExecutableElement method, RestMethodMetadata metadata) {
    }
```

### EmptyMethod
The method is empty
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/DubboMetrics.java`
#### Snippet
```java
    }

    public void destroy() {
    }
}
```

### EmptyMethod
The method is empty
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/MetricsScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeFrameworkModel(FrameworkModel frameworkModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/MetricsScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/SimpleMetricsCountSampler.java`
#### Snippet
```java
    }

    protected void rtConfigure(MetricsCountSampleConfigurer<S, K, M> configure) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/nop/NopMetricsReporter.java`
#### Snippet
```java

    @Override
    public void init() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/nop/NopMetricsReporter.java`
#### Snippet
```java

    @Override
    public void refreshData() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/event/MetricsNotifyListener.java`
#### Snippet
```java

    @Override
    public void onEventError(RegistryEvent.MetricsNotifyEvent event) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java

            @Override
            public void destroy() {

            }
```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/QosScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeApplicationModel(ApplicationModel applicationModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/QosScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/calls/ReactorClientCalls.java`
#### Snippet
```java

                        @Override
                        public void onCompleted() {
                            // Do nothing
                        }
```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/CertDeployerListener.java`
#### Snippet
```java

    @Override
    public void onStarted(ApplicationModel scopeModel) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/CertDeployerListener.java`
#### Snippet
```java

    @Override
    public void onStopped(ApplicationModel scopeModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/CertScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeApplicationModel(ApplicationModel applicationModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/CertScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/filter/AuthenticationExceptionTranslatorFilter.java`
#### Snippet
```java

    @Override
    public void onError(Throwable t, Invoker<?> invoker, Invocation invocation) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/model/SecurityScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeApplicationModel(ApplicationModel applicationModel) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/model/SecurityScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {


```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryScopeModelInitializer.java`
#### Snippet
```java
public class RegistryScopeModelInitializer implements ScopeModelInitializer {
    @Override
    public void initializeFrameworkModel(FrameworkModel frameworkModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/NopServiceDiscovery.java`
#### Snippet
```java

    @Override
    public void doRegister(ServiceInstance serviceInstance) throws RuntimeException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/NopServiceDiscovery.java`
#### Snippet
```java

    @Override
    public void doUnregister(ServiceInstance serviceInstance) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/NopServiceDiscovery.java`
#### Snippet
```java

    @Override
    public void doDestroy() throws Exception {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ReflectionBasedServiceDiscovery.java`
#### Snippet
```java
    }

    public void doInitialize(URL registryURL) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataServiceDelegation.java`
#### Snippet
```java

    @Override
    public void destroy() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java

    @Override
    public void onExport(RegistryProtocol registryProtocol, Exporter<?> exporter) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java

        @Override
        public void destroy() {

        }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java

        @Override
        public void register(URL url) {

        }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java

        @Override
        public void unregister(URL url) {

        }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java

        @Override
        public void subscribe(URL url, NotifyListener listener) {

        }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java

        @Override
        public void unsubscribe(URL url, NotifyListener listener) {

        }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastServiceDiscovery.java`
#### Snippet
```java

    @Override
    public void doDestroy() throws Exception {

    }
```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastServiceDiscovery.java`
#### Snippet
```java

    @Override
    public void doRegister(ServiceInstance serviceInstance) throws RuntimeException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastServiceDiscovery.java`
#### Snippet
```java

    @Override
    public void doUpdate(ServiceInstance oldServiceInstance, ServiceInstance newServiceInstance) throws RuntimeException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java
    }

    protected void init() {
    }

```

### EmptyMethod
The method is empty
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java


    protected void checkApplicationName(String applicationName) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeFrameworkModel(FrameworkModel frameworkModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/AbstractWireProtocol.java`
#### Snippet
```java

    @Override
    public void configClientPipeline(URL url, ChannelOperator operator, ContextOperator contextOperator) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/AbstractWireProtocol.java`
#### Snippet
```java

    @Override
    public void close() {
    }
}
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/DefaultCodec.java`
#### Snippet
```java
public class DefaultCodec implements Codec2 {
    @Override
    public void encode(Channel channel, ChannelBuffer buffer, Object message) throws IOException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/DefaultPuHandler.java`
#### Snippet
```java
public class DefaultPuHandler implements ChannelHandler {
    @Override
    public void connected(Channel channel) throws RemotingException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/DefaultPuHandler.java`
#### Snippet
```java

    @Override
    public void disconnected(Channel channel) throws RemotingException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/DefaultPuHandler.java`
#### Snippet
```java

    @Override
    public void sent(Channel channel, Object message) throws RemotingException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/DefaultPuHandler.java`
#### Snippet
```java

    @Override
    public void received(Channel channel, Object message) throws RemotingException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/DefaultPuHandler.java`
#### Snippet
```java

    @Override
    public void caught(Channel channel, Throwable exception) throws RemotingException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/ChannelHandlerAdapter.java`
#### Snippet
```java

    @Override
    public void connected(Channel channel) throws RemotingException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/ChannelHandlerAdapter.java`
#### Snippet
```java

    @Override
    public void disconnected(Channel channel) throws RemotingException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/ChannelHandlerAdapter.java`
#### Snippet
```java

    @Override
    public void sent(Channel channel, Object message) throws RemotingException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/ChannelHandlerAdapter.java`
#### Snippet
```java

    @Override
    public void received(Channel channel, Object message) throws RemotingException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/ChannelHandlerAdapter.java`
#### Snippet
```java

    @Override
    public void caught(Channel channel, Throwable exception) throws RemotingException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/WrappedChannelHandler.java`
#### Snippet
```java
    }

    public void close() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/factory/AbstractHttpClientFactory.java`
#### Snippet
```java
    ////////////////////////////////////////   inner methods  ///////////////////////////////////////////////

    protected void beforeCreated(URL url) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/factory/AbstractHttpClientFactory.java`
#### Snippet
```java
    protected abstract RestClient doCreateRestClient(URL url) throws RpcException;

    protected void afterCreated(RestClient client) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/HttpClientRestClient.java`
#### Snippet
```java

    @Override
    public void close(int timeout) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java

    @Override
    public void close(int timeout) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/URLConnectionRestClient.java`
#### Snippet
```java

    @Override
    public void close() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/URLConnectionRestClient.java`
#### Snippet
```java

    @Override
    public void close(int timeout) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java

    @Override
    public void reset(URL url) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java

    @Override
    public void reset(Parameters parameters) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java

    @Override
    public void send(Object message) throws RemotingException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java

    @Override
    public void send(Object message, boolean sent) throws RemotingException {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java

    @Override
    public void startClose() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java

    @Override
    protected void doClose() throws Throwable {
        /*try {
            bootstrap.releaseExternalResources();
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java

    @Override
    protected void doClose() throws Throwable {
        // can't shut down nioEventLoopGroup because the method will be invoked when closing one channel but not a client,
        // but when and how to close the nioEventLoopGroup ?
```

### EmptyMethod
The method is empty
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConnectionManager.java`
#### Snippet
```java

    @Override
    public void forEachConnection(Consumer<AbstractConnectionClient> connectionConsumer) {
        // Do nothing.
    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/CompatibleFilter.java`
#### Snippet
```java

    @Override
    public void onError(Throwable t, Invoker<?> invoker, Invocation invocation) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java

    @Override
    public void onError(Throwable t, Invoker<?> invoker, Invocation invocation) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java

    @Override
    public void onError(Throwable t, Invoker<?> invoker, Invocation invocation) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/TimeoutFilter.java`
#### Snippet
```java

    @Override
    public void onError(Throwable t, Invoker<?> invoker, Invocation invocation) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/listener/ExporterListenerAdapter.java`
#### Snippet
```java

    @Override
    public void exported(Exporter<?> exporter) throws RpcException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/listener/ExporterListenerAdapter.java`
#### Snippet
```java

    @Override
    public void unexported(Exporter<?> exporter) throws RpcException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/listener/InvokerListenerAdapter.java`
#### Snippet
```java

    @Override
    public void referred(Invoker<?> invoker) throws RpcException {
    }

```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/listener/InvokerListenerAdapter.java`
#### Snippet
```java

    @Override
    public void destroyed(Invoker<?> invoker) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractExporter.java`
#### Snippet
```java
     * subclasses need to override this method to destroy resources.
     */
    public void afterUnExport() {
    }

```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

    // used to destroy unused clients and other resource
    protected void destroyInternal(URL url) {
        // subclass override
    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        @Override
        public void reset(Parameters parameters) {

        }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        @Override
        public void reset(URL url) {

        }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        @Override
        public void send(Object message) throws RemotingException {

        }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        @Override
        public void send(Object message, boolean sent) throws RemotingException {

        }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        @Override
        public void close() {

        }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        @Override
        public void close(int timeout) {

        }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        @Override
        public void startClose() {

        }
```

### EmptyMethod
Method only calls its super
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java

    @Override
    public boolean isDone() {
        return super.isDone();
    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyInvoker.java`
#### Snippet
```java

    @Override
    public void destroy() {
    }

```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java

    @Override
    public void destroy() {
        //do nothing
    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ChannelWrappedInvoker.java`
#### Snippet
```java

    @Override
    public void destroy() {
//        super.destroy();
//        try {
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ChannelWrappedInvoker.java`
#### Snippet
```java

        @Override
        public void reconnect() throws RemotingException {

        }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DecodeableRpcInvocation.java`
#### Snippet
```java
    }

    private void checkSerializationTypeFromRemote() {

    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/CancelableStreamObserver.java`
#### Snippet
```java
    }

    public void beforeStart(final ClientCallToObserverAdapter<T> clientCallToObserverAdapter) {
        // do nothing
    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/CancelableStreamObserver.java`
#### Snippet
```java
    }

    public void startRequest() {
        // do nothing
    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TriHttp2RemoteFlowController.java`
#### Snippet
```java
         * @throws Http2Exception If a write occurs and an exception happens in the write operation.
         */
        void channelWritabilityChange() throws Http2Exception { }

        /**
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TriHttp2RemoteFlowController.java`
#### Snippet
```java
         * @param state the state that was cancelled.
         */
        void stateCancelled(FlowState state) { }

        /**
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/ServerStreamServerCallListener.java`
#### Snippet
```java

    @Override
    public void onReturn(Object value) {
    }

```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/UnaryServerCallListener.java`
#### Snippet
```java

    @Override
    public void onCancel(TriRpcStatus status) {
        // ignored
    }
```

### EmptyMethod
The method is empty
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/command/CreateStreamQueueCommand.java`
#### Snippet
```java

    @Override
    public void doSend(ChannelHandlerContext ctx, ChannelPromise promise) {
        //NOOP
    }
```

### EmptyMethod
The method is empty
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2CreatorManager.java`
#### Snippet
```java

    @Override
    public void onAddClassLoader(FrameworkModel scopeModel, ClassLoader classLoader) {
        // nop
    }
```

### EmptyMethod
The method is empty
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2ScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeApplicationModel(ApplicationModel applicationModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2ScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-serialization/dubbo-serialization-hessian2/src/main/java/org/apache/dubbo/common/serialize/hessian2/Hessian2ClassLoaderListener.java`
#### Snippet
```java
public class Hessian2ClassLoaderListener implements ScopeClassLoaderListener<FrameworkModel> {
    @Override
    public void onAddClassLoader(FrameworkModel scopeModel, ClassLoader classLoader) {
        // noop
    }
```

### EmptyMethod
The method is empty
in `dubbo-serialization/dubbo-serialization-hessian2/src/main/java/org/apache/dubbo/common/serialize/hessian2/Hessian2ScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeApplicationModel(ApplicationModel applicationModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-serialization/dubbo-serialization-hessian2/src/main/java/org/apache/dubbo/common/serialize/hessian2/Hessian2ScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/DubboApplicationContextInitializer.java`
#### Snippet
```java
    }

    private void overrideBeanDefinitions(ConfigurableApplicationContext applicationContext) {
        // @since 2.7.8 OverrideBeanDefinitionRegistryPostProcessor has been removed
        // applicationContext.addBeanFactoryPostProcessor(new OverrideBeanDefinitionRegistryPostProcessor());
```

### EmptyMethod
The method is empty
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/XdsRegistry.java`
#### Snippet
```java

    @Override
    public void doRegister(URL url) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/XdsRegistry.java`
#### Snippet
```java

    @Override
    public void doUnregister(URL url) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/XdsRegistry.java`
#### Snippet
```java

    @Override
    public void doSubscribe(URL url, NotifyListener listener) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/XdsRegistry.java`
#### Snippet
```java

    @Override
    public void doUnsubscribe(URL url, NotifyListener listener) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
    }

    public void unobserveResource(Set<String> resourceNames, Consumer<Map<String, T>> consumer) {
        // TODO
    }
```

### EmptyMethod
The method is empty
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeFrameworkModel(FrameworkModel frameworkModel) {

    }
```

### EmptyMethod
The method is empty
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsScopeModelInitializer.java`
#### Snippet
```java

    @Override
    public void initializeModuleModel(ModuleModel moduleModel) {
    }
}
```

## RuleId[id=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
    private final int cleanPeriod;

    private volatile boolean admin = false;

    public MulticastRegistry(URL url, ApplicationModel applicationModel) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/RegistryMetricsCollector.java`
#### Snippet
```java
public class RegistryMetricsCollector implements ApplicationMetricsCollector<RegistryEvent.Type, RegistryEvent> {

    private Boolean collectEnabled = null;
    private final RegistryStatComposite stats;
    private final MetricsEventMulticaster registryMulticaster;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/collector/MetadataMetricsCollector.java`
#### Snippet
```java
public class MetadataMetricsCollector implements ApplicationMetricsCollector<MetadataEvent.Type, MetadataEvent> {

    private Boolean collectEnabled = null;
    private final MetadataStatComposite stats;
    private final MetricsEventMulticaster metadataEventMulticaster;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesMeshEnvListenerFactory.java`
#### Snippet
```java
    public static final Logger logger = LoggerFactory.getLogger(KubernetesMeshEnvListenerFactory.class);
    private final AtomicBoolean initialized = new AtomicBoolean(false);
    private MeshEnvListener listener = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java
    private final List<MetricsListener<?>> listeners = Collections.synchronizedList(new ArrayList<>());

    private boolean available = false;

    public void setAvailable() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesMeshEnvListener.java`
#### Snippet
```java
public class KubernetesMeshEnvListener implements MeshEnvListener {
    public static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(KubernetesMeshEnvListener.class);
    private static volatile boolean usingApiServer = false;
    private static volatile KubernetesClient kubernetesClient;
    private static volatile String namespace;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AdaptiveMetrics.java`
#### Snippet
```java
    private final AtomicLong consumerSuccess = new AtomicLong();
    private final AtomicLong errorReq = new AtomicLong();
    private double ewma = 0;

    public double getLoad(String idKey, int weight, int timeout) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AdaptiveMetrics.java`
#### Snippet
```java
    private final ConcurrentMap<String, AdaptiveMetrics> metricsStatistics = new ConcurrentHashMap<>();

    private long currentProviderTime = 0;
    private double providerCPULoad = 0;
    private long lastLatency = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AdaptiveMetrics.java`
#### Snippet
```java
    private long currentProviderTime = 0;
    private double providerCPULoad = 0;
    private long lastLatency = 0;
    private long currentTime = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AdaptiveMetrics.java`
#### Snippet
```java

    private long currentProviderTime = 0;
    private double providerCPULoad = 0;
    private long lastLatency = 0;
    private long currentTime = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AdaptiveMetrics.java`
#### Snippet
```java
    private double providerCPULoad = 0;
    private long lastLatency = 0;
    private long currentTime = 0;

    //Allow some time disorder
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ListenableFilter.java`
#### Snippet
```java
public abstract class ListenableFilter implements Filter {

    protected Listener listener = null;
    protected final ConcurrentMap<Invocation, Listener> listeners = new ConcurrentHashMap<>();

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AdaptiveLoadBalanceFilter.java`
#### Snippet
```java
     * uses a single worker thread operating off an bounded queue
     */
    private volatile ThreadPoolExecutor executor = null;

    private final AdaptiveMetrics adaptiveMetrics;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractExporter.java`
#### Snippet
```java
    private final Invoker<T> invoker;

    private volatile boolean unexported = false;

    public AbstractExporter(Invoker<T> invoker) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractInvoker.java`
#### Snippet
```java
     * {@link Node} destroy
     */
    private boolean destroyed = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java

    private static Class multiValueMapClass = null;
    private static Method multiValueMapAdd = null;

    static {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
    private final static String JAVAX_MultiValueMap = "org.jboss.resteasy.specimpl.MultivaluedMapImpl";

    private static Class multiValueMapClass = null;
    private static Method multiValueMapAdd = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
        private static volatile ExecutorService CURATOR_WATCHER_EXECUTOR_SERVICE;

        private static volatile boolean closed = false;

        private CuratorFramework client;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-metrics/dubbo-metrics-prometheus/src/main/java/org/apache/dubbo/metrics/prometheus/PrometheusMetricsReporter.java`
#### Snippet
```java

    private final PrometheusMeterRegistry prometheusRegistry = new PrometheusMeterRegistry(PrometheusConfig.DEFAULT);
    private ScheduledExecutorService pushJobExecutor = null;

    public PrometheusMetricsReporter(URL url, ApplicationModel applicationModel) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java

    private final AtomicBoolean hasPreparedApplicationInstance = new AtomicBoolean(false);
    private volatile boolean hasPreparedInternalModule = false;

    private ScheduledFuture<?> asyncMetadataFuture;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
public class SpringCompatUtils {

    private static volatile Boolean factoryMethodMetadataEnabled = null;

    private static final Log logger = LogFactory.getLog(SpringCompatUtils.class);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/AbstractDubboConfigBinder.java`
#### Snippet
```java
    private boolean ignoreUnknownFields = true;

    private boolean ignoreInvalidFields = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
    private ServicePackagesHolder servicePackagesHolder;

    private volatile boolean scanned = false;

    public ServiceAnnotationPostProcessor(String... packagesToScan) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java

    private ApplicationContext applicationContext;
    private volatile boolean initialized = false;
    private ModuleModel moduleModel;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleModel.java`
#### Snippet
```java
    private volatile ModuleConfigManager moduleConfigManager;
    private volatile ModuleDeployer deployer;
    private boolean lifeCycleManagedExternally = false;

    protected ModuleModel(ApplicationModel applicationModel) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java
    protected byte[] mData;

    protected int mPosition, mLimit, mMark = 0;

    public UnsafeByteArrayInputStream(byte[] buf) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
        return new InputStream() {
            private int mPosition = 0;
            private int mMark = 0;
            private final int mLimit = Math.min(limit, is.available());

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
    public static InputStream limitedInputStream(final InputStream is, final int limit) throws IOException {
        return new InputStream() {
            private int mPosition = 0;
            private int mMark = 0;
            private final int mLimit = Math.min(limit, is.available());
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            byte[] mMarkBuffer;

            boolean mInMarked = false;
            boolean mInReset = false;
            boolean mDry = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            boolean mInReset = false;
            boolean mDry = false;
            private int mPosition = 0;
            private int mCount = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            boolean mDry = false;
            private int mPosition = 0;
            private int mCount = 0;

            @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
            boolean mInMarked = false;
            boolean mInReset = false;
            boolean mDry = false;
            private int mPosition = 0;
            private int mCount = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java

            boolean mInMarked = false;
            boolean mInReset = false;
            boolean mDry = false;
            private int mPosition = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/param/DynamicValues.java`
#### Snippet
```java
    private volatile String[] index2Value = new String[1];
    private final Map<String, Integer> value2Index = new ConcurrentHashMap<>();
    private int indexSeq = 0;

    public DynamicValues(String defaultVal) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/GsonUtils.java`
#### Snippet
```java
public class GsonUtils {
    // weak reference of com.google.gson.Gson, prevent throw exception when init
    private static volatile Object gsonCache = null;

    private static volatile Boolean supportGson;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/GsonImpl.java`
#### Snippet
```java
public class GsonImpl extends AbstractJSONImpl {
    // weak reference of com.google.gson.Gson, prevent throw exception when init
    private volatile Object gsonCache = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/JacksonImpl.java`
#### Snippet
```java
    private ObjectMapper objectMapper = new ObjectMapper();

    private volatile Object jacksonCache = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Stack.java`
#### Snippet
```java

public class Stack<E> {
    private int mSize = 0;

    private final List<E> mElements = new ArrayList<E>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TimeUtils.java`
#### Snippet
```java
    private static volatile boolean isTickerAlive = false;

    private static volatile boolean isFallback = false;

    private TimeUtils() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TimeUtils.java`
#### Snippet
```java
    private static volatile long currentTimeMillis;

    private static volatile boolean isTickerAlive = false;

    private static volatile boolean isFallback = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LFUCache.java`
#### Snippet
```java
    private final int capacity;
    private final int evictionCount;
    private int curSize = 0;

    private final ReentrantLock lock = new ReentrantLock();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AtomicPositiveInteger.java`
#### Snippet
```java

    @SuppressWarnings("unused")
    private volatile int index = 0;

    public AtomicPositiveInteger() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/SerializeSecurityManager.java`
#### Snippet
```java
    private volatile SerializeCheckStatus checkStatus = null;

    private volatile Boolean checkSerializable = null;

    public void addToAlwaysAllowed(String className) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/SerializeSecurityManager.java`
#### Snippet
```java
    private final Set<String> warnedClasses = new ConcurrentHashSet<>(1);

    private volatile SerializeCheckStatus checkStatus = null;

    private volatile Boolean checkSerializable = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java
public class ClassLoaderResourceLoader {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ClassLoaderResourceLoader.class);
    private static SoftReference<Map<ClassLoader, Map<String, Set<URL>>>> classLoaderResourcesCache = null;

    static {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
    private static final Map<String, String> HOST_NAME_CACHE = new LRUCache<>(1000);
    private static volatile InetAddress LOCAL_ADDRESS = null;
    private static volatile Inet6Address LOCAL_ADDRESS_V6 = null;

    private static final String SPLIT_IPV4_CHARACTER = "\\.";
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

    private static final Map<String, String> HOST_NAME_CACHE = new LRUCache<>(1000);
    private static volatile InetAddress LOCAL_ADDRESS = null;
    private static volatile Inet6Address LOCAL_ADDRESS_V6 = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/DubboAppender.java`
#### Snippet
```java
    }

    public static boolean available = false;

    public static List<Log> logList = new ArrayList<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/AbstractDeployer.java`
#### Snippet
```java
    private volatile Throwable lastError;

    protected volatile boolean initialized = false;

    protected List<DeployListener<E>> listeners = new CopyOnWriteArrayList<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/jdk/JdkLoggerAdapter.java`
#### Snippet
```java
    private File file;

    private boolean propertiesLoaded = false;

    public JdkLoggerAdapter() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
    private Logger logger;

    private static boolean disabled = false;

    public FailsafeLogger(Logger logger) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
    private Map<String, Method> mCopyMethods; // <method desc,method instance>
    private Map<String, Constructor<?>> mCopyConstructors; // <constructor desc,constructor instance>
    private boolean mDefaultConstructor = false;

    private ClassGenerator() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/ThreadlessExecutor.java`
#### Snippet
```java
    private CompletableFuture<?> waitingFuture;

    private boolean finished = false;

    private volatile boolean waiting = true;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/support/AbortPolicyWithReport.java`
#### Snippet
```java
    private final URL url;

    protected static volatile long lastPrintTime = 0;

    private static final long TEN_MINUTES_MILLS = 10 * 60 * 1000;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
    private final    ConcurrentMap<String, Holder<Object>> cachedInstances        = new ConcurrentHashMap<>();
    private final    Holder<Object>                        cachedAdaptiveInstance = new Holder<>();
    private volatile Class<?>                              cachedAdaptiveClass    = null;
    private          String                                cachedDefaultName;
    private volatile Throwable                             createAdaptiveInstanceError;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
    private final AtomicBoolean initialized = new AtomicBoolean(false);
    protected ConfigMode configMode = ConfigMode.STRICT;
    protected boolean ignoreDuplicatedInterface = false;

    static {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/BasicJsonWriter.java`
#### Snippet
```java
        private final String singleIndent;

        private int level = 0;

        private String currentIndent = "";
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/BasicJsonWriter.java`
#### Snippet
```java
        private String currentIndent = "";

        private boolean prependIndent = false;

        IndentingWriter(Writer out, String singleIndent) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleProtocol.java`
#### Snippet
```java
    public static boolean IGNORE_1_0_0_VERSION = false;

    public static boolean RESOLVE_FALLBACK_TO_DEFAULT = false;

    public TripleProtocol(FrameworkModel frameworkModel) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleProtocol.java`
#### Snippet
```java
    public static boolean CONVERT_NO_LOWER_HEADER = false;

    public static boolean IGNORE_1_0_0_VERSION = false;

    public static boolean RESOLVE_FALLBACK_TO_DEFAULT = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleProtocol.java`
#### Snippet
```java
     * There is only one
     */
    public static boolean CONVERT_NO_LOWER_HEADER = false;

    public static boolean IGNORE_1_0_0_VERSION = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/frame/TriDecoder.java`
#### Snippet
```java
    private boolean compressedFlag;
    private long pendingDeliveries;
    private boolean inDelivery = false;
    private boolean closing;
    private boolean closed;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/observer/ServerCallToObserverAdapter.java`
#### Snippet
```java
    private final AbstractServerCall call;
    private Map<String, Object> attachments;
    private boolean terminated = false;

    public ServerCallToObserverAdapter(AbstractServerCall call,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleServerStream.java`
#### Snippet
```java
    private final InetSocketAddress remoteAddress;
    private Deframer deframer;
    private boolean rst = false;
    private final Http2StreamChannel http2StreamChannel;
    private final TripleStreamChannelFuture tripleStreamChannelFuture;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/AbstractRouter.java`
#### Snippet
```java
public abstract class AbstractRouter implements Router {
    private int priority = DEFAULT_PRIORITY;
    private boolean force = false;
    private URL url;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/AbstractRouterRule.java`
#### Snippet
```java
    private boolean enabled = true;
    private int priority;
    private boolean dynamic = false;

    private String version;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/AbstractRouterRule.java`
#### Snippet
```java
    private String rawRule;
    private boolean runtime = true;
    private boolean force = false;
    private boolean valid = true;
    private boolean enabled = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/AbstractStateRouter.java`
#### Snippet
```java
 */
public abstract class AbstractStateRouter<T> implements StateRouter<T> {
    private volatile boolean force = false;
    private volatile URL url;
    private volatile StateRouter<T> nextRouter = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/AbstractStateRouter.java`
#### Snippet
```java
    private volatile boolean force = false;
    private volatile URL url;
    private volatile StateRouter<T> nextRouter = null;

    private final GovernanceRuleRepository ruleRepository;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java
    private final AtomicBoolean destroyed = new AtomicBoolean(false);

    private volatile Invoker<T> stickyInvoker = null;

    public AbstractClusterInvoker() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailbackClusterInvoker.java`
#### Snippet
```java
         * Number of retried.
         */
        private int retriedTimes = 0;

        RetryTimerTask(LoadBalance loadbalance, Invocation invocation, List<Invoker<T>> invokers, Invoker<T> lastInvoker,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java
        private ListIterator<E> tailListIterator;
        private int curBitIndex = -1;
        private boolean isInTailList = false;
        private int lastReturnedIndex = -1;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java
    private volatile List<E> originList;
    private final static BitList emptyList = new BitList(Collections.emptyList());
    private volatile List<E> tailList = null;

    public BitList(List<E> originList) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
    private final URL url;

    private volatile boolean destroyed = false;

    protected volatile URL consumerUrl;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
     * Invokers initialized flag.
     */
    private volatile boolean invokersInitialized = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/jetty/JettyLoggerAdapter.java`
#### Snippet
```java
    private final ErrorTypeAwareLogger logger;

    private static boolean debugEnabled = false;

    public JettyLoggerAdapter() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
        private final Lock lock = new ReentrantLock();
        private final List<InstanceInfo> instances = new ArrayList<>();
        private volatile boolean batchRegistered = false;
        private volatile boolean valid = true;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsRouteRuleManager.java`
#### Snippet
```java
            if (LDS_LISTENER == null) {
                LDS_LISTENER = new Consumer<Map<String, ListenerResult>>() {
                    private volatile Set<String> configNames = null;

                    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/PilotExchanger.java`
#### Snippet
```java
    private final Map<String, Consumer<RdsVirtualHostListener>> rdsObserveConsumer = new ConcurrentHashMap<>();

    private static PilotExchanger GLOBAL_PILOT_EXCHANGER = null;

    private final ApplicationModel applicationModel;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
        private long timeToLiveMillis;
        private long expirationIntervalMillis;
        private volatile boolean running = false;
        private final Thread expirerThread;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/DubboMetrics.java`
#### Snippet
```java
public class DubboMetrics implements MeterBinder {

    private MeterRegistry globalRegistry = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/filter/MetricsFilter.java`
#### Snippet
```java
public class MetricsFilter implements Filter, BaseFilter.Listener, ScopeModelAware {

    private DefaultMetricsCollector collector = null;
    private MethodMetricsInterceptor metricsInterceptor;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/DefaultMetricsCollector.java`
#### Snippet
```java
public class DefaultMetricsCollector implements MetricsCollector {

    private boolean collectEnabled = false;
    private final SimpleMetricsEventMulticaster eventMulticaster;
    private final MethodMetricsSampler methodSampler = new MethodMetricsSampler(this);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporter.java`
#### Snippet
```java
    private final ApplicationModel applicationModel;

    private ScheduledExecutorService collectorSyncJobExecutor = null;

    private static final int DEFAULT_SCHEDULE_INITIAL_DELAY = 5;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
    private transient final Map<String, String> extendParams;
    private transient final Map<String, String> instanceParams;
    protected transient volatile boolean updated = false;
    private transient ConcurrentNavigableMap<String, SortedSet<URL>> subscribedServiceURLs;
    private transient ConcurrentNavigableMap<String, SortedSet<URL>> exportedServiceURLs;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
    private volatile ReferenceConfigurationListener referenceConfigurationListener;
    private volatile boolean enableConfigurationListen = true;
    private volatile List<URL> originalUrls = null;
    private volatile Map<String, String> overrideQueryMap;
    private final Set<String> providerFirstParams;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/DynamicDirectory.java`
#### Snippet
```java
     */
    protected Registry registry;
    protected volatile boolean forbidden = false;
    protected boolean shouldRegister;
    protected boolean shouldSimplified;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Request.java`
#### Snippet
```java
    private boolean mTwoWay = true;

    private boolean mEvent = false;

    private boolean mBroken = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Request.java`
#### Snippet
```java
    private boolean mEvent = false;

    private boolean mBroken = false;

    private int mPayload;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Response.java`
#### Snippet
```java
    private byte mStatus = OK;

    private boolean mEvent = false;

    private String mErrorMsg;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Response.java`
#### Snippet
```java
    public static final byte SERVER_THREADPOOL_EXHAUSTED_ERROR = 100;

    private long mId = 0;

    private String mVersion;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/AbstractTimerTask.java`
#### Snippet
```java
    private final Long tick;

    protected volatile boolean cancel = false;

    AbstractTimerTask(ChannelProvider channelProvider, Long tick) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeChannel.java`
#### Snippet
```java
    private final Channel channel;

    private volatile boolean closed = false;

    HeaderExchangeChannel(Channel channel) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperClient.java`
#### Snippet
```java
    private final ConcurrentMap<String, ConcurrentMap<DataListener, TargetDataListener>> listeners = new ConcurrentHashMap<>();

    private volatile boolean closed = false;

    private final Set<String> persistentExistNodePath = new ConcurrentHashSet<>();
```

## RuleId[id=RedundantImplements]
### RedundantImplements
Redundant interface declaration `Ordered`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/config/DubboConfigBeanCustomizer.java`
#### Snippet
```java
 * @since 2.6.6
 */
public interface DubboConfigBeanCustomizer extends ConfigurationBeanCustomizer, Ordered {

    /**
```

### RedundantImplements
Redundant interface declaration `Subscriber`
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/AbstractTripleReactorSubscriber.java`
#### Snippet
```java
 * Passing the data from Reactive producer to CallStreamObserver.
 */
public abstract class AbstractTripleReactorSubscriber<T> implements Subscriber<T>, CoreSubscriber<T> {

    private volatile boolean isCancelled;
```

### RedundantImplements
Redundant interface declaration `Set`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConcurrentHashSet.java`
#### Snippet
```java
import java.util.concurrent.ConcurrentMap;

public class ConcurrentHashSet<E> extends AbstractSet<E> implements Set<E>, java.io.Serializable {

    private static final long serialVersionUID = -8672117787651310382L;
```

## RuleId[id=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ExecuteLimitFilter.java`
#### Snippet
```java
            return invoker.invoke(invocation);
        } catch (Throwable t) {
            if (t instanceof RuntimeException) {
                throw (RuntimeException) t;
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
            return cp.toClass(cls, neighbor, classLoader, JavassistCompiler.class.getProtectionDomain());
        } catch (Throwable t) {
            if (!(t instanceof CannotCompileException)) {
                return cp.toClass(cls, classLoader, JavassistCompiler.class.getProtectionDomain());
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
                return mPool.toClass(mCtc, neighborClass, loader, pd);
            } catch (Throwable t) {
                if (!(t instanceof CannotCompileException)) {
                    return mPool.toClass(mCtc, loader, pd);
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/stub/StubInvocationUtil.java`
#### Snippet
```java
            return InvocationUtil.invoke(invoker, rpcInvocation);
        } catch (Throwable e) {
            if (e instanceof RuntimeException) {
                throw (RuntimeException) e;
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java

            if (savePropertiesRetryTimes.get() >= MAX_RETRY_TIMES_SAVE_PROPERTIES) {
                if (e instanceof OverlappingFileLockException) {
                    // fix #9341, ignore OverlappingFileLockException
                    logger.info("Failed to save registry cache file for file overlapping lock exception, file name " + file.getName());
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
            }

            if (!(e instanceof OverlappingFileLockException)) {
                logger.warn(REGISTRY_FAILED_READ_WRITE_CACHE_FILE, CAUSE_MULTI_DUBBO_USING_SAME_FILE,
                    "However, the retrying count limit is not exceeded. Dubbo will still try.",
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java

            // Rethrow exception
            if (t instanceof IOException) {
                throw (IOException) t;
            } else if (t instanceof RuntimeException) {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
            if (t instanceof IOException) {
                throw (IOException) t;
            } else if (t instanceof RuntimeException) {
                throw (RuntimeException) t;
            } else if (t instanceof Error) {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
            } else if (t instanceof RuntimeException) {
                throw (RuntimeException) t;
            } else if (t instanceof Error) {
                throw (Error) t;
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/all/AllChannelHandler.java`
#### Snippet
```java
            executor.execute(new ChannelEventRunnable(channel, handler, ChannelState.RECEIVED, message));
        } catch (Throwable t) {
            if(message instanceof Request && t instanceof RejectedExecutionException){
                sendFeedback(channel, (Request) message, t);
                return;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/message/MessageOnlyChannelHandler.java`
#### Snippet
```java
            executor.execute(new ChannelEventRunnable(channel, handler, ChannelState.RECEIVED, message));
        } catch (Throwable t) {
            if(message instanceof Request && t instanceof RejectedExecutionException){
                sendFeedback(channel, (Request) message, t);
                return;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/execution/ExecutionChannelHandler.java`
#### Snippet
```java
                // therefore the consumer side has to wait until gets timeout. This is a temporary solution to prevent
                // this scenario from happening, but a better solution should be considered later.
                if (t instanceof RejectedExecutionException) {
                    sendFeedback(channel, (Request) message, t);
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/connection/ConnectionOrderedChannelHandler.java`
#### Snippet
```java
            executor.execute(new ChannelEventRunnable(channel, handler, ChannelState.RECEIVED, message));
        } catch (Throwable t) {
            if (message instanceof Request && t instanceof RejectedExecutionException) {
                sendFeedback(channel, (Request) message, t);
                return;
```

## RuleId[id=NonStaticFinalLogger]
### NonStaticFinalLogger
Non-constant logger field `logger`
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/jdk/JdkLogger.java`
#### Snippet
```java
public class JdkLogger implements Logger {

    private final java.util.logging.Logger logger;

    public JdkLogger(java.util.logging.Logger logger) {
```

## RuleId[id=FuseStreamOperations]
### FuseStreamOperations
Stream may be extended replacing 'sort'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
            ))
            .filter(thread -> !unclosedThreadMap.containsKey(thread))
            .collect(Collectors.toList());
        unclosedThreads.sort(Comparator.comparing(Thread::getName));
        if (unclosedThreads.size() > 0) {
```

## RuleId[id=RedundantStringFormatCall]
### RedundantStringFormatCall
Redundant call to `format()`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            }
        } catch (IOException e) {
            throw new DubboTestException(String.format("Failed to unpack the zookeeper binary file"), e);
        }
    }
```

## RuleId[id=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `exceptionClass` of exception class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/com/alibaba/dubbo/rpc/service/GenericException.java`
#### Snippet
```java
    private static final long serialVersionUID = -1182299763306599962L;

    private String exceptionClass;

    private String exceptionMessage;
```

### NonFinalFieldOfException
Non-final field `exceptionMessage` of exception class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/com/alibaba/dubbo/rpc/service/GenericException.java`
#### Snippet
```java
    private String exceptionClass;

    private String exceptionMessage;

    public GenericException() {
```

### NonFinalFieldOfException
Non-final field `code` of exception class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcException.java`
#### Snippet
```java
     * RpcException cannot be extended, use error code for exception type to keep compatibility
     */
    private int code;

    public RpcException() {
```

### NonFinalFieldOfException
Non-final field `useCause` of exception class
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/GenericException.java`
#### Snippet
```java
    private static final long serialVersionUID = -1182299763306599962L;

    private boolean useCause;

    private String exceptionClass;
```

### NonFinalFieldOfException
Non-final field `exceptionClass` of exception class
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/GenericException.java`
#### Snippet
```java
    private boolean useCause;

    private String exceptionClass;

    private String exceptionMessage;
```

### NonFinalFieldOfException
Non-final field `exceptionMessage` of exception class
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/GenericException.java`
#### Snippet
```java
    private String exceptionClass;

    private String exceptionMessage;

    private final GenericExceptionInfo genericExceptionInfo;
```

### NonFinalFieldOfException
Non-final field `remoteAddress` of exception class
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingException.java`
#### Snippet
```java
    private InetSocketAddress localAddress;

    private InetSocketAddress remoteAddress;

    public RemotingException(Channel channel, String msg) {
```

### NonFinalFieldOfException
Non-final field `localAddress` of exception class
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingException.java`
#### Snippet
```java
    private static final long serialVersionUID = -3160452149606778709L;

    private InetSocketAddress localAddress;

    private InetSocketAddress remoteAddress;
```

## RuleId[id=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/DubboProcessAotMojo.java`
#### Snippet
```java
        aotArguments.add(this.project.getArtifactId());
        aotArguments.addAll(new RunArguments(this.arguments).getArgs());
        return aotArguments.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/DubboProcessAotMojo.java`
#### Snippet
```java
    @Override
    protected void executeAot() throws Exception {
        URL[] classPath = getClassPath().toArray(new URL[0]);
        generateAotAssets(classPath, AOT_PROCESSOR_CLASS_NAME, getAotArguments(mainClass));
        compileSourceFiles(classPath, this.generatedSources, this.classesDirectory);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/RunArguments.java`
#### Snippet
```java

	String[] asArray() {
		return this.args.toArray(new String[0]);
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AppResponse.java`
#### Snippet
```java
                Object stackTrace = exception.getStackTrace();
                if (stackTrace == null) {
                    exception.setStackTrace(new StackTraceElement[0]);
                }
            } catch (Exception e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyFactory.java`
#### Snippet
```java

        try {
            return getProxy(invoker, interfaces.toArray(new Class<?>[0]));
        } catch (Throwable t) {
            if (generic) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyFactory.java`
#### Snippet
```java

                logger.error(PROXY_UNSUPPORTED_INVOKER, "", "", "Error occur when creating proxy. Invoker is in generic mode. Trying to create proxy without real interface class.", t);
                return getProxy(invoker, interfaces.toArray(new Class<?>[0]));
            } else {
                throw t;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
            String[] types = (String[]) invocation.getArguments()[1];
            if (types == null) {
                return new Class<?>[0];
            }
            Class<?>[] parameterTypes = new Class<?>[types.length];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java

        sn.append('(');
        Class<?>[] types = get(TYPES) != null ? (Class<?>[]) get(TYPES) : new Class[0];
        boolean first = true;
        for (Class<?> type : types) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java

    public static Method getMethod(Class clazz, String method) throws NoSuchMethodException {
        Method declaredMethod = clazz.getDeclaredMethod(method, new Class[]{});
        declaredMethod.setAccessible(true);
        return declaredMethod;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/consumer/inercept/SerializeBodyIntercept.java`
#### Snippet
```java
            ByteArrayOutputStream outputStream = new ByteArrayOutputStream();
            Collection<String> headers = requestTemplate.getHeaders(RestConstant.CONTENT_TYPE);
            MediaType mediaType = MediaTypeUtil.convertMediaType(headers.toArray(new String[0]));
            HttpMessageCodecManager.httpMessageEncode(outputStream, unSerializedBody, url, mediaType);
            requestTemplate.serializeBody(outputStream.toByteArray());
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java
    static String[] getMethodParameterTypes(ExecutableElement method) {
        return method == null ?
                new String[0] :
                method.getParameters()
                        .stream()
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
        try {
            logger.info(getIdentifier() + " waiting services exporting ...");
            exportFuture = CompletableFuture.allOf(asyncExportingFutures.toArray(new CompletableFuture[0]));
            exportFuture.get();
        } catch (Throwable e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
        try {
            logger.info(getIdentifier() + " waiting services referring ...");
            referFuture = CompletableFuture.allOf(asyncReferringFutures.toArray(new CompletableFuture[0]));
            referFuture.get();
        } catch (Throwable e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboAnnotationUtils.java`
#### Snippet
```java
                , (a, b) -> a);

        return CollectionUtils.toStringMap(compatibleParameterArray.toArray(new String[0]));
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/status/SpringStatusChecker.java`
#### Snippet
```java
            while (cls != null && method == null) {
                try {
                    method = cls.getDeclaredMethod("getConfigLocations", new Class<?>[0]);
                } catch (NoSuchMethodException t) {
                    cls = cls.getSuperclass();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/status/SpringStatusChecker.java`
#### Snippet
```java
                    method.setAccessible(true);
                }
                String[] configs = (String[]) method.invoke(applicationContext, new Object[0]);
                if (configs != null && configs.length > 0) {
                    for (String config : configs) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                Method getter = null;
                try {
                    getter = beanClass.getMethod("get" + name.substring(3), new Class<?>[0]);
                } catch (NoSuchMethodException e) {
                    try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                } catch (NoSuchMethodException e) {
                    try {
                        getter = beanClass.getMethod("is" + name.substring(3), new Class<?>[0]);
                    } catch (NoSuchMethodException e2) {
                        // ignore, there is no need any log here since some class implement the interface: EnvironmentAware,
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
                    elementList.add(element);
                }
                value = elementList.toArray(new Object[0]);
            } else if (ReferenceAttributes.PARAMETERS.equals(propertyName)) {
                value = createParameterMap((ManagedMap) value);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java

    public void setParameterTypes(Class<?>[] parameterTypes) {
        this.parameterTypes = parameterTypes == null ? new Class<?>[0] : parameterTypes;
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    public RpcInvocation(String methodName, Class<?>[] parameterTypes, Object[] arguments, Map<String, String> attachments, Invoker<?> invoker) {
        this.methodName = methodName;
        this.parameterTypes = parameterTypes == null ? new Class<?>[0] : parameterTypes;
        this.arguments = arguments == null ? new Object[0] : arguments;
        this.attachments = attachments == null ? new HashMap<String, String>() : attachments;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
        this.methodName = methodName;
        this.parameterTypes = parameterTypes == null ? new Class<?>[0] : parameterTypes;
        this.arguments = arguments == null ? new Object[0] : arguments;
        this.attachments = attachments == null ? new HashMap<String, String>() : attachments;
        this.invoker = invoker;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java

    public void setArguments(Object[] arguments) {
        this.arguments = arguments == null ? new Object[0] : arguments;
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerMethodModel.java`
#### Snippet
```java
    private String[] createParamSignature(Class<?>[] args) {
        if (args == null || args.length == 0) {
            return new String[]{};
        }
        String[] paramSig = new String[args.length];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
                interfaceList = new Class[]{serviceInterfaceClass};
            } else {
                interfaceList = new Class[0];
            }
        } else {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderMethodModel.java`
#### Snippet
```java

    private static String[] getArgTypes(Method method) {
        String[] methodArgTypes = new String[0];
        Class<?>[] parameterTypes = method.getParameterTypes();
        if (parameterTypes.length > 0) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLPlainParam.java`
#### Snippet
```java
    public static URLPlainParam toURLPlainParam(URLParam urlParam) {
        Map<String, String> params = Collections.unmodifiableMap(new HashMap<>(urlParam.getParameters()));
        return new URLPlainParam(new BitSet(), new int[0], params, urlParam.getMethodParameters(), urlParam.getRawParam());
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        }
        if (len == 0) {
            return new byte[0];
        }
        if (off + len > str.length()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        }
        if (len == 0) {
            return new byte[0];
        }
        if (off + len > str.length()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/param/DynamicParamTable.java`
#### Snippet
```java
            .toArray();

        ORIGIN_KEYS = resultMap.keySet().toArray(new String[0]);

        VALUES = resultMap.values().toArray(new ParamValue[0]);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/param/DynamicParamTable.java`
#### Snippet
```java
        ORIGIN_KEYS = resultMap.keySet().toArray(new String[0]);

        VALUES = resultMap.values().toArray(new ParamValue[0]);

        for (int i = 0; i < ORIGIN_KEYS.length; i++) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java

    public void setArguments(Object[] arguments) {
        this.arguments = arguments == null ? new Object[0] : arguments;
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
        this.interfaceName = interfaceName;
        this.protocolServiceKey = protocolServiceKey;
        this.parameterTypes = parameterTypes == null ? new Class<?>[0] : parameterTypes;
        this.arguments = arguments == null ? new Object[0] : arguments;
        this.attachments = attachments == null ? new HashMap<>() : attachments;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
        this.protocolServiceKey = protocolServiceKey;
        this.parameterTypes = parameterTypes == null ? new Class<?>[0] : parameterTypes;
        this.arguments = arguments == null ? new Object[0] : arguments;
        this.attachments = attachments == null ? new HashMap<>() : attachments;
        this.attributes = attributes == null ? Collections.synchronizedMap(new HashMap<>()) : attributes;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java

    public void setParameterTypes(Class<?>[] parameterTypes) {
        this.parameterTypes = parameterTypes == null ? new Class<?>[0] : parameterTypes;
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
    public static String[] readLines(File file) throws IOException {
        if (file == null || !file.exists() || !file.canRead()) {
            return new String[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
                lines.add(line);
            }
            return lines.toArray(new String[0]);
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
        Method[] methods = Arrays.stream(tClass.getMethods())
            .collect(Collectors.toList())
            .toArray(new Method[] {});
        List<String> dmns = new ArrayList<>(); // method names.
        boolean hasMethod = hasMethods(methods);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
        }
        dmns.sort(Comparator.naturalOrder());
        return dmns.toArray(new String[0]);
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
        Method[] methods = Arrays.stream(tClass.getMethods())
            .collect(Collectors.toList())
            .toArray(new Method[] {});
        List<String> mns = new ArrayList<>(); // method names.
        boolean hasMethod = hasMethods(methods);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
            }
        }
        return mns.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLBuilder.java`
#### Snippet
```java
            return this;
        }
        return removeParameters(keys.toArray(new String[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/DefaultSerializeClassChecker.java`
#### Snippet
```java
    private volatile long[] allowPrefixes = new long[0];

    private volatile long[] disAllowPrefixes = new long[0];

    public DefaultSerializeClassChecker(FrameworkModel frameworkModel) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/DefaultSerializeClassChecker.java`
#### Snippet
```java

    private final SerializeSecurityManager serializeSecurityManager;
    private volatile long[] allowPrefixes = new long[0];

    private volatile long[] disAllowPrefixes = new long[0];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        }
        if (total == 0) {
            return new int[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java

        // add initial values
        String[] configs = (cfg == null || cfg.trim().length() == 0) ? new String[0] : COMMA_SPLIT_PATTERN.split(cfg);
        for (String config : configs) {
            if (config != null && config.trim().length() > 0) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    public static String[] delimitedListToStringArray(String str, String delimiter, String charsToDelete) {
        if (str == null) {
            return new String[0];
        } else if (delimiter == null) {
            return new String[]{str};
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    public static String[] toStringArray(Collection<String> collection) {
        return (String[]) collection.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Proxy.java`
#### Snippet
```java
            ccp.addField("public static java.lang.reflect.Method[] methods;");
            ccp.addField("private " + InvocationHandler.class.getName() + " handler;");
            ccp.addConstructor(Modifier.PUBLIC, new Class<?>[]{InvocationHandler.class}, new Class<?>[0], "handler=$1;");
            ccp.addDefaultConstructor();
            Class<?> clazz = ccp.toClass(neighbor, cl, domain);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Proxy.java`
#### Snippet
```java
            ccp.addDefaultConstructor();
            Class<?> clazz = ccp.toClass(neighbor, cl, domain);
            clazz.getField("methods").set(null, methods.toArray(new Method[0]));
            return clazz;
        } catch (RuntimeException e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
    public static String getSizeMethod(Class<?> cls) {
        try {
            return cls.getMethod("size", new Class<?>[0]).getName() + "()";
        } catch (NoSuchMethodException e) {
            try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
        } catch (NoSuchMethodException e) {
            try {
                return cls.getMethod("length", new Class<?>[0]).getName() + "()";
            } catch (NoSuchMethodException e2) {
                try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
            } catch (NoSuchMethodException e2) {
                try {
                    return cls.getMethod("getSize", new Class<?>[0]).getName() + "()";
                } catch (NoSuchMethodException e3) {
                    try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
                } catch (NoSuchMethodException e3) {
                    try {
                        return cls.getMethod("getLength", new Class<?>[0]).getName() + "()";
                    } catch (NoSuchMethodException e4) {
                        return null;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/CtClassBuilder.java`
#### Snippet
```java
        }

        return ClassUtils.forName(imports.toArray(new String[0]), className).getName();
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    public String toParameterString() {
        return toParameterString(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
            return this;
        }
        return removeParameters(keys.toArray(new String[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
    public String getBackupAddress(int defaultPort) {
        StringBuilder address = new StringBuilder(appendDefaultPort(getAddress(), defaultPort));
        String[] backups = getParameter(RemotingConstants.BACKUP_KEY, new String[0]);
        if (ArrayUtils.isNotEmpty(backups)) {
            for (String backup : backups) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        List<URL> urls = new ArrayList<>();
        urls.add(this);
        String[] backups = getParameter(RemotingConstants.BACKUP_KEY, new String[0]);
        if (backups != null && backups.length > 0) {
            for (String backup : backups) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
            .filter(method -> allMethod.contains(ReflectUtils.getDesc(method)))
            .collect(Collectors.toList())
            .toArray(new Method[]{});
        // get all public method.
        boolean hasMethod = ClassUtils.hasMethods(methods);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
            // setup static field.
            wc.getField("pts").set(null, pts);
            wc.getField("pns").set(null, pts.keySet().toArray(new String[0]));
            wc.getField("mns").set(null, mns.toArray(new String[0]));
            wc.getField("dmns").set(null, dmns.toArray(new String[0]));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
            wc.getField("pts").set(null, pts);
            wc.getField("pns").set(null, pts.keySet().toArray(new String[0]));
            wc.getField("mns").set(null, mns.toArray(new String[0]));
            wc.getField("dmns").set(null, dmns.toArray(new String[0]));
            int ix = 0;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
            wc.getField("pns").set(null, pts.keySet().toArray(new String[0]));
            wc.getField("mns").set(null, mns.toArray(new String[0]));
            wc.getField("dmns").set(null, dmns.toArray(new String[0]));
            int ix = 0;
            for (Method m : ms.values()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/LoadingStrategy.java`
#### Snippet
```java
     */
    default String[] onlyExtensionClassLoaderPackages() {
        return new String[]{};
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocal.java`
#### Snippet
```java
                Set<InternalThreadLocal<?>> variablesToRemove = (Set<InternalThreadLocal<?>>) v;
                InternalThreadLocal<?>[] variablesToRemoveArray =
                        variablesToRemove.toArray(new InternalThreadLocal[0]);
                for (InternalThreadLocal<?> tlv : variablesToRemoveArray) {
                    tlv.remove(threadLocalMap);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/builder/EnumTypeBuilder.java`
#### Snippet
```java
            Method methodValues = clazz.getDeclaredMethod("values");
            methodValues.setAccessible(true);
            Object[] values = (Object[]) methodValues.invoke(clazz, new Object[0]);
            int length = values.length;
            for (int i = 0; i < length; i++) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/AotProcessor.java`
#### Snippet
```java

        ResourceConfigMetadataRepository resourceRepository = new ResourceConfigMetadataRepository();
        resourceRepository.registerIncludesPatterns(ResourceScanner.INSTANCE.distinctSpiResource().toArray(new String[]{}));
        resourceRepository.registerIncludesPatterns(ResourceScanner.INSTANCE.distinctSecurityResource().toArray(new String[]{}));
        writer.writeResourceConfig(resourceRepository);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/AotProcessor.java`
#### Snippet
```java
        ResourceConfigMetadataRepository resourceRepository = new ResourceConfigMetadataRepository();
        resourceRepository.registerIncludesPatterns(ResourceScanner.INSTANCE.distinctSpiResource().toArray(new String[]{}));
        resourceRepository.registerIncludesPatterns(ResourceScanner.INSTANCE.distinctSecurityResource().toArray(new String[]{}));
        writer.writeResourceConfig(resourceRepository);

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ExceptionUtils.java`
#### Snippet
```java
    public static String[] getStackFrames(final Throwable throwable) {
        if (throwable == null) {
            return new String[0];
        }
        return getStackFrames(getStackTrace(throwable));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ExceptionUtils.java`
#### Snippet
```java
            list.add(frames.nextToken());
        }
        return list.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/AbstractServerCall.java`
#### Snippet
```java
            serviceDescriptor.getInterfaceName(), url.getProtocolServiceKey(),
            methodDescriptor.getParameterClasses(),
            new Object[0]);
        inv.setTargetServiceUniqueName(url.getServiceKey());
        inv.setReturnTypes(methodDescriptor.getReturnTypes());
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Snappy.java`
#### Snippet
```java
    public byte[] compress(byte[] payloadByteArr) throws RpcException {
        if (null == payloadByteArr || 0 == payloadByteArr.length) {
            return new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Snappy.java`
#### Snippet
```java
    public byte[] decompress(byte[] payloadByteArr) {
        if (null == payloadByteArr || 0 == payloadByteArr.length) {
            return new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Bzip2.java`
#### Snippet
```java
    public byte[] decompress(byte[] payloadByteArr) {
        if (null == payloadByteArr || 0 == payloadByteArr.length) {
            return new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Bzip2.java`
#### Snippet
```java
    public byte[] compress(byte[] payloadByteArr) throws RpcException {
        if (null == payloadByteArr || 0 == payloadByteArr.length) {
            return new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Gzip.java`
#### Snippet
```java
    public byte[] decompress(byte[] payloadByteArr) throws RpcException {
        if (null == payloadByteArr || 0 == payloadByteArr.length) {
            return new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Gzip.java`
#### Snippet
```java
    public byte[] compress(byte[] payloadByteArr) throws RpcException {
        if (null == payloadByteArr || 0 == payloadByteArr.length) {
            return new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/BooleanArrayMerger.java`
#### Snippet
```java
    public boolean[] merge(boolean[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new boolean[0];
        }
        int totalLen = 0;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/ByteArrayMerger.java`
#### Snippet
```java
    public byte[] merge(byte[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new byte[0];
        }
        int total = 0;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/ShortArrayMerger.java`
#### Snippet
```java
    public short[] merge(short[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new short[0];
        }
        int total = 0;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/IntArrayMerger.java`
#### Snippet
```java
    public int[] merge(int[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new int[0];
        }
        return Arrays.stream(items).filter(Objects::nonNull)
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/DoubleArrayMerger.java`
#### Snippet
```java
    public double[] merge(double[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new double[0];
        }
        return Arrays.stream(items).filter(Objects::nonNull)
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/LongArrayMerger.java`
#### Snippet
```java
    public long[] merge(long[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new long[0];
        }
        return Arrays.stream(items).filter(Objects::nonNull)
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/ArrayMerger.java`
#### Snippet
```java
    public Object[] merge(Object[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new Object[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/ArrayMerger.java`
#### Snippet
```java

        if (i == items.length) {
            return new Object[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/ArrayMerger.java`
#### Snippet
```java

        if (totalLen == 0) {
            return new Object[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/CharArrayMerger.java`
#### Snippet
```java
    public char[] merge(char[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new char[0];
        }
        int total = 0;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/FloatArrayMerger.java`
#### Snippet
```java
    public float[] merge(float[]... items) {
        if (ArrayUtils.isEmpty(items)) {
            return new float[0];
        }
        int total = 0;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java
    private String address;
    private Object body;
    private byte[] byteBody = new byte[0];
    private String protocol = "http://";
    private final Invocation invocation;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java
                return null;
            }
            String[] strings = headers.toArray(new String[0]);
            return strings[0];

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCodec.java`
#### Snippet
```java
            return result;
        }
        return new byte[]{};
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
                }

                RpcInvocation invocation = new RpcInvocation(url.getServiceModel(), method, url.getParameter(INTERFACE_KEY), "", new Class<?>[0], new Object[0]);
                invocation.setAttachment(PATH_KEY, url.getPath());
                invocation.setAttachment(GROUP_KEY, url.getGroup());
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
                }

                RpcInvocation invocation = new RpcInvocation(url.getServiceModel(), method, url.getParameter(INTERFACE_KEY), "", new Class<?>[0], new Object[0]);
                invocation.setAttachment(PATH_KEY, url.getPath());
                invocation.setAttachment(GROUP_KEY, url.getGroup());
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/LoggerInfo.java`
#### Snippet
```java
    @Override
    public String execute(CommandContext commandContext, String[] args) {
        String availableAdapters = String.join(", ", LoggerFactory.getAvailableAdapter().toArray(new String[0]));
        String currentAdapter = LoggerFactory.getCurrentAdapter();
        Level level = LoggerFactory.getLevel();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/textui/TTable.java`
#### Snippet
```java
    public TTable(ColumnDefine[] columnDefineArray) {
        this.columnDefineArray = null == columnDefineArray
                ? new ColumnDefine[0]
                : columnDefineArray;
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/util/ServiceCheckUtils.java`
#### Snippet
```java
        }
        // e.g. zookeeper-A(Y)/zookeeper-I(Y)
        return String.join("/", statuses.toArray(new String[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/HttpCommandDecoder.java`
#### Snippet
```java
                            valueList.addAll(values);
                        }
                        commandContext = CommandContextFactory.newInstance(name, valueList.toArray(new String[]{}), true);
                    }
                } else if (request.method() == HttpMethod.POST) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/HttpCommandDecoder.java`
#### Snippet
```java
                        commandContext.setHttp(true);
                    } else {
                        commandContext = CommandContextFactory.newInstance(name, valueList.toArray(new String[]{}), true);
                    }
                }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/util/ServiceCheckUtils.java`
#### Snippet
```java
        }
        // zookeeper-AF(I-10,A-0)/nacos-I(10)
        return String.join("/", nums.toArray(new String[0]));
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataParamsFilter.java`
#### Snippet
```java
     */
    default String[] instanceParamsIncluded() {
        return new String[0];
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataParamsFilter.java`
#### Snippet
```java
     */
    default String[] serviceParamsExcluded() {
        return new String[0];
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataParamsFilter.java`
#### Snippet
```java
    */
   default String[] serviceParamsIncluded() {
       return new String[0];
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataParamsFilter.java`
#### Snippet
```java
     */
    default String[] instanceParamsExcluded() {
        return new String[0];
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
                    }
                }
                included = includedList.toArray(new String[0]);
                excluded = excludedList.toArray(new String[0]);
            }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
                }
                included = includedList.toArray(new String[0]);
                excluded = excludedList.toArray(new String[0]);
            }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
                }
            });
            included = includedList.toArray(new String[0]);
            excluded = excludedList.toArray(new String[0]);
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
            });
            included = includedList.toArray(new String[0]);
            excluded = excludedList.toArray(new String[0]);
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/pu/AbstractPortUnificationServer.java`
#### Snippet
```java
    public AbstractPortUnificationServer(URL url, ChannelHandler handler) throws RemotingException {
        super(url, handler);
        this.protocols = url.getOrDefaultFrameworkModel().getExtensionLoader(WireProtocol.class).getActivateExtension(url, new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
                .toArray(String[]::new);
        } else {
            return new String[0];
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
            //Pre-generated Null object bytes
            ByteArrayOutputStream baos = new ByteArrayOutputStream();
            byte[] nullBytes = new byte[0];
            try {
                ObjectOutput out = s.serialize(null, baos);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-serialization/dubbo-serialization-jdk/src/main/java/org/apache/dubbo/common/serialize/java/JavaObjectInput.java`
#### Snippet
```java
        }
        if (len == 0) {
            return new byte[0];
        }
        if (len > MAX_BYTE_ARRAY_LENGTH) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-serialization/dubbo-serialization-jdk/src/main/java/org/apache/dubbo/common/serialize/nativejava/NativeJavaObjectInput.java`
#### Snippet
```java
            return null;
        } else if (len == 0) {
            return new byte[]{};
        } else {
            byte[] result = new byte[len];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2SecurityManager.java`
#### Snippet
```java
        this.allowedList = allowedList;
        this.disAllowedList = disAllowedList;
        this.securityFilter = new Handler(this.status, this.securityManager, this.checkSerializable, this.allowedList.toArray(new String[0]), this.disAllowedList);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2SecurityManager.java`
#### Snippet
```java
        securityManager = frameworkModel.getBeanFactory().getOrRegisterBean(SerializeSecurityManager.class);
        securityManager.registerListener(this);
        securityFilter = new Handler(AllowClassNotifyListener.DEFAULT_STATUS, securityManager, true, new String[0], new ConcurrentHashSet<>());
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2SecurityManager.java`
#### Snippet
```java
    public synchronized void notifyCheckSerializable(boolean checkSerializable) {
        this.checkSerializable = checkSerializable;
        this.securityFilter = new Handler(this.status, this.securityManager, this.checkSerializable, this.allowedList.toArray(new String[0]), this.disAllowedList);

    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/Fastjson2SecurityManager.java`
#### Snippet
```java
    public synchronized void notifyCheckStatus(SerializeCheckStatus status) {
        this.status = status;
        this.securityFilter = new Handler(this.status, this.securityManager, this.checkSerializable, this.allowedList.toArray(new String[0]), this.disAllowedList);
    }

```

## RuleId[id=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
    private GeneratedMessageV3.Builder getMessageBuilder(Class<?> requestType) throws Exception {
        Method method = requestType.getMethod("newBuilder");
        return (GeneratedMessageV3.Builder) method.invoke(null, null);
    }

```

## RuleId[id=InfiniteRecursion]
### InfiniteRecursion
Method `getClassPath()` recurses infinitely, and can only end by throwing an exception
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/DubboProcessAotMojo.java`
#### Snippet
```java
    }

    private List<URL> getClassPath() throws Exception {
        File[] directories = new File[]{this.classesDirectory, this.generatedClasses};
        return getClassPath(directories, new ExcludeTestScopeArtifactFilter());
```

### InfiniteRecursion
Method `appendParameter()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ApplicationBuilder.java`
#### Snippet
```java
    }

    public ApplicationBuilder appendParameter(String key, String value) {
        this.parameters = appendParameter(parameters, key, value);
        return getThis();
```

### InfiniteRecursion
Method `appendParameters()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ApplicationBuilder.java`
#### Snippet
```java
    }

    public ApplicationBuilder appendParameters(Map<String, String> appendParameters) {
        this.parameters = appendParameters(parameters, appendParameters);
        return getThis();
```

### InfiniteRecursion
Method `appendParameters()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractMethodBuilder.java`
#### Snippet
```java
    }

    public B appendParameters(Map<String, String> appendParameters) {
        this.parameters = appendParameters(parameters, appendParameters);
        return getThis();
```

### InfiniteRecursion
Method `appendParameter()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractMethodBuilder.java`
#### Snippet
```java
    }

    public B appendParameter(String key, String value) {
        this.parameters = appendParameter(parameters, key, value);
        return getThis();
```

### InfiniteRecursion
Method `appendParameter()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/MetadataReportBuilder.java`
#### Snippet
```java
    }

    public MetadataReportBuilder appendParameter(String key, String value) {
        this.parameters = appendParameter(this.parameters, key, value);
        return getThis();
```

### InfiniteRecursion
Method `appendParameters()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/MetadataReportBuilder.java`
#### Snippet
```java
    }

    public MetadataReportBuilder appendParameters(Map<String, String> appendParameters) {
        this.parameters = appendParameters(this.parameters, appendParameters);
        return getThis();
```

### InfiniteRecursion
Method `appendParameters()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ConfigCenterBuilder.java`
#### Snippet
```java
    }

    public ConfigCenterBuilder appendParameters(Map<String, String> appendParameters) {
        this.parameters = appendParameters(this.parameters, appendParameters);
        return getThis();
```

### InfiniteRecursion
Method `appendParameter()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ConfigCenterBuilder.java`
#### Snippet
```java
    }

    public ConfigCenterBuilder appendParameter(String key, String value) {
        this.parameters = appendParameter(this.parameters, key, value);
        return getThis();
```

### InfiniteRecursion
Method `appendParameter()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/MonitorBuilder.java`
#### Snippet
```java
    }

    public MonitorBuilder appendParameter(String key, String value) {
        this.parameters = appendParameter(parameters, key, value);
        return getThis();
```

### InfiniteRecursion
Method `appendParameters()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/MonitorBuilder.java`
#### Snippet
```java
    }

    public MonitorBuilder appendParameters(Map<String, String> appendParameters) {
        this.parameters = appendParameters(parameters, appendParameters);
        return getThis();
```

### InfiniteRecursion
Method `appendParameters()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ProtocolBuilder.java`
#### Snippet
```java
    }

    public ProtocolBuilder appendParameters(Map<String, String> appendParameters) {
        this.parameters = appendParameters(parameters, appendParameters);
        return getThis();
```

### InfiniteRecursion
Method `appendParameter()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ProtocolBuilder.java`
#### Snippet
```java
    }

    public ProtocolBuilder appendParameter(String key, String value) {
        this.parameters = appendParameter(parameters, key, value);
        return getThis();
```

### InfiniteRecursion
Method `appendParameter()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/RegistryBuilder.java`
#### Snippet
```java
    }

    public RegistryBuilder appendParameter(String key, String value) {
        this.parameters = appendParameter(parameters, key, value);
        return getThis();
```

### InfiniteRecursion
Method `appendParameters()` recurses infinitely, and can only end by throwing an exception
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/RegistryBuilder.java`
#### Snippet
```java
    }

    public RegistryBuilder appendParameters(Map<String, String> appendParameters) {
        this.parameters = appendParameters(parameters, appendParameters);
        return getThis();
```

### InfiniteRecursion
Method `createRegistry()` recurses infinitely, and can only end by throwing an exception
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/AbstractRegistryFactory.java`
#### Snippet
```java

    @Override
    protected Registry createRegistry(URL url) {
        return createRegistry(new com.alibaba.dubbo.common.URL(url));
    }
```

### InfiniteRecursion
Method `setInterface()` recurses infinitely, and can only end by throwing an exception
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java


    public void setInterface(Class<?> interfaceClass) {
        if (interfaceClass != null && !interfaceClass.isInterface()) {
            throw new IllegalStateException("The interface class " + interfaceClass + " is not a interface!");
```

### InfiniteRecursion
Method `setInterface()` recurses infinitely, and can only end by throwing an exception
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    }

    public void setInterface(Class<?> interfaceClass) {
        if (interfaceClass != null && !interfaceClass.isInterface()) {
            throw new IllegalStateException("The interface class " + interfaceClass + " is not a interface!");
```

### InfiniteRecursion
Method `getDefaultConfigs()` recurses infinitely, and can only end by throwing an exception
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ConfigManager.java`
#### Snippet
```java

    @Override
    public <C extends AbstractConfig> List<C> getDefaultConfigs(Class<C> cls) {
        return getDefaultConfigs(getConfigsMap(getTagName(cls)));
    }
```

### InfiniteRecursion
Method `toByteBuffer()` recurses infinitely, and can only end by throwing an exception
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/AbstractChannelBuffer.java`
#### Snippet
```java

    @Override
    public ByteBuffer toByteBuffer() {
        return toByteBuffer(readerIndex, readableBytes());
    }
```

### InfiniteRecursion
Method `copy()` recurses infinitely, and can only end by throwing an exception
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/AbstractChannelBuffer.java`
#### Snippet
```java

    @Override
    public ChannelBuffer copy() {
        return copy(readerIndex, readableBytes());
    }
```

### InfiniteRecursion
Method `send()` recurses infinitely, and can only end by throwing an exception
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractPeer.java`
#### Snippet
```java

    @Override
    public void send(Object message) throws RemotingException {
        send(message, url.getParameter(Constants.SENT_KEY, false));
    }
```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * @return
     */
    public static <V extends Object> Map<String, V> getSubProperties(Collection<Map<String, V>> configMaps, String prefix) {
        Map<String, V> map = new LinkedHashMap<>();
        for (Map<String, V> configMap : configMaps) {
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * @return
     */
    public static <V extends Object> Set<String> getSubIds(Collection<Map<String, V>> configMaps, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    public static <V extends Object> boolean hasSubProperties(Collection<Map<String, V>> configMaps, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    public static <V extends Object> Map<String, V> getSubProperties(Map<String, V> configMap, String prefix) {
        return getSubProperties(configMap, prefix, null);
    }
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    public static <V extends Object> boolean hasSubProperties(Map<String, V> configMap, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    private static <V extends Object> Map<String, V> getSubProperties(Map<String, V> configMap, String prefix, Map<String, V> resultMap) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

## RuleId[id=NonExceptionNameEndsWithException]
### NonExceptionNameEndsWithException
Non-exception class name `RpcException` ends with 'Exception'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcException.java`
#### Snippet
```java

@Deprecated
public class RpcException extends org.apache.dubbo.rpc.RpcException {
    
    public RpcException() {
```

### NonExceptionNameEndsWithException
Non-exception class name `RemotingException` ends with 'Exception'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/remoting/RemotingException.java`
#### Snippet
```java

@Deprecated
public class RemotingException extends org.apache.dubbo.remoting.RemotingException {

    public RemotingException(Channel channel, String msg) {
```

### NonExceptionNameEndsWithException
Non-exception class name `StatusRpcException` ends with 'Exception'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/StatusRpcException.java`
#### Snippet
```java
package org.apache.dubbo.rpc;

public class StatusRpcException extends RpcException {

    public TriRpcStatus getStatus() {
```

### NonExceptionNameEndsWithException
Non-exception class name `NoSuchCommandException` ends with 'Exception'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/exception/NoSuchCommandException.java`
#### Snippet
```java
package org.apache.dubbo.qos.command.exception;

public class NoSuchCommandException extends CommandException {
    public NoSuchCommandException(String msg) {
        super("NoSuchCommandException:" + msg);
```

### NonExceptionNameEndsWithException
Non-exception class name `PermissionDenyException` ends with 'Exception'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/exception/PermissionDenyException.java`
#### Snippet
```java
package org.apache.dubbo.qos.command.exception;

public class PermissionDenyException extends CommandException {
    public PermissionDenyException(String msg) {
        super("Permission Deny On Operation: " + msg);
```

### NonExceptionNameEndsWithException
Non-exception class name `TimeoutException` ends with 'Exception'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/TimeoutException.java`
#### Snippet
```java
 * @see org.apache.dubbo.remoting.exchange.support.DefaultFuture#get()
 */
public class TimeoutException extends RemotingException {

    public static final int CLIENT_SIDE = 0;
```

### NonExceptionNameEndsWithException
Non-exception class name `ExecutionException` ends with 'Exception'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/ExecutionException.java`
#### Snippet
```java
 * @export
 */
public class ExecutionException extends RemotingException {

    private static final long serialVersionUID = -2531085236111056860L;
```

## RuleId[id=ConstantValue]
### ConstantValue
Value `event` is always 'null'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java
        }
        for (MetricsListener listener : listeners) {
            if (listener.isSupport(event)) {
                listener.onEvent(event);
            }
```

### ConstantValue
Value `event` is always 'null'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java
        for (MetricsListener listener : listeners) {
            if (listener.isSupport(event)) {
                listener.onEvent(event);
            }
        }
```

### ConstantValue
Value `event` is always 'null'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java
            return;
        }
        if (event instanceof TimeCounter) {
            ((TimeCounter) event).getTimePair().end();
        }
```

### ConstantValue
Value `event` is always 'null'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/event/SimpleMetricsEventMulticaster.java`
#### Snippet
```java
        }
        for (MetricsListener listener : listeners) {
            if (listener instanceof MetricsLifeListener && listener.isSupport(event)) {
                consumer.accept(((MetricsLifeListener) listener));
            }
```

### ConstantValue
Condition `context instanceof DubboClientContext` is always `true`
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DubboClientObservationConvention.java`
#### Snippet
```java
    @Override
    default boolean supportsContext(Observation.Context context) {
        return context instanceof DubboClientContext;
    }
}
```

### ConstantValue
Condition `context instanceof DubboServerContext` is always `true`
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DubboServerObservationConvention.java`
#### Snippet
```java
    @Override
    default boolean supportsContext(Observation.Context context) {
        return context instanceof DubboServerContext;
    }
}
```

### ConstantValue
Condition `originEntry != null` is always `false`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyInvoker.java`
#### Snippet
```java
            }
            Profiler.removeBizProfiler();
            if (originEntry != null) {
                Profiler.setToBizProfiler(originEntry);
            }
```

### ConstantValue
Condition `executor instanceof ThreadlessExecutor` is always `true` when reached
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java
    @Override
    public Result get(long timeout, TimeUnit unit) throws InterruptedException, ExecutionException, TimeoutException {
        if (executor != null && executor instanceof ThreadlessExecutor) {
            ThreadlessExecutor threadlessExecutor = (ThreadlessExecutor) executor;
            threadlessExecutor.waitAndDrain();
```

### ConstantValue
Condition `executor instanceof ThreadlessExecutor` is always `true` when reached
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java
    @Override
    public Result get() throws InterruptedException, ExecutionException {
        if (executor != null && executor instanceof ThreadlessExecutor) {
            ThreadlessExecutor threadlessExecutor = (ThreadlessExecutor) executor;
            threadlessExecutor.waitAndDrain();
```

### ConstantValue
Condition `exception instanceof RpcException` is always `true`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ExceptionFilter.java`
#### Snippet
```java
                }
                // directly throw if it's dubbo exception
                if (exception instanceof RpcException) {
                    return;
                }
```

### ConstantValue
Value `value` is always 'null'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java
                    if (ProtocolUtils.isBeanGenericSerialization(generic)) {
                        if (value == null) {
                            appResponse.setValue(value);
                        } else if (value instanceof JavaBeanDescriptor) {
                            appResponse.setValue(JavaBeanSerializeUtil.deserialize((JavaBeanDescriptor) value));
```

### ConstantValue
Condition `lastException != null` is always `true`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java
                        }
                        appResponse.setException(targetException);
                    } else if (lastException != null) {
                        throw lastException;
                    }
```

### ConstantValue
Value `appException` is always 'null'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
                }
                if (!(appException instanceof com.alibaba.dubbo.rpc.service.GenericException)) {
                    appException = new com.alibaba.dubbo.rpc.service.GenericException(appException);
                }
                appResponse.setException(appException);
```

### ConstantValue
Value `inv` is always 'null'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
            clazz = ((RpcInvocation) inv).getReturnType();
        } else {
            clazz = getReturnType(inv);
        }
        return (clazz != null && CompletableFuture.class.isAssignableFrom(clazz)) || isGenericAsync(inv);
```

### ConstantValue
Value `mock` is always 'null'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
    public static String normalizeMock(String mock) {
        if (mock == null) {
            return mock;
        }

```

### ConstantValue
Condition `te instanceof RpcException` is always `true`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractInvoker.java`
#### Snippet
```java
            if (te != null) {
                // if biz exception
                if (te instanceof RpcException) {
                    ((RpcException) te).setCode(RpcException.BIZ_EXCEPTION);
                }
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        if (config instanceof ProtocolConfig) {
            ConfigValidationUtils.validateProtocolConfig((ProtocolConfig) config);
        } else if (config instanceof RegistryConfig) {
            ConfigValidationUtils.validateRegistryConfig((RegistryConfig) config);
        } else if (config instanceof MetadataReportConfig) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        } else if (config instanceof RegistryConfig) {
            ConfigValidationUtils.validateRegistryConfig((RegistryConfig) config);
        } else if (config instanceof MetadataReportConfig) {
            ConfigValidationUtils.validateMetadataConfig((MetadataReportConfig) config);
        } else if (config instanceof ProviderConfig) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        } else if (config instanceof MetadataReportConfig) {
            ConfigValidationUtils.validateMetadataConfig((MetadataReportConfig) config);
        } else if (config instanceof ProviderConfig) {
            ConfigValidationUtils.validateProviderConfig((ProviderConfig) config);
        } else if (config instanceof ConsumerConfig) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        } else if (config instanceof ProviderConfig) {
            ConfigValidationUtils.validateProviderConfig((ProviderConfig) config);
        } else if (config instanceof ConsumerConfig) {
            ConfigValidationUtils.validateConsumerConfig((ConsumerConfig) config);
        } else if (config instanceof ApplicationConfig) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        } else if (config instanceof ConsumerConfig) {
            ConfigValidationUtils.validateConsumerConfig((ConsumerConfig) config);
        } else if (config instanceof ApplicationConfig) {
            ConfigValidationUtils.validateApplicationConfig((ApplicationConfig) config);
        } else if (config instanceof MonitorConfig) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        } else if (config instanceof ApplicationConfig) {
            ConfigValidationUtils.validateApplicationConfig((ApplicationConfig) config);
        } else if (config instanceof MonitorConfig) {
            ConfigValidationUtils.validateMonitorConfig((MonitorConfig) config);
        } else if (config instanceof ModuleConfig) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        } else if (config instanceof MonitorConfig) {
            ConfigValidationUtils.validateMonitorConfig((MonitorConfig) config);
        } else if (config instanceof ModuleConfig) {
            ConfigValidationUtils.validateModuleConfig((ModuleConfig) config);
        } else if (config instanceof MetricsConfig) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        } else if (config instanceof ModuleConfig) {
            ConfigValidationUtils.validateModuleConfig((ModuleConfig) config);
        } else if (config instanceof MetricsConfig) {
            ConfigValidationUtils.validateMetricsConfig((MetricsConfig) config);
        } else if (config instanceof SslConfig) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/DefaultConfigValidator.java`
#### Snippet
```java
        } else if (config instanceof MetricsConfig) {
            ConfigValidationUtils.validateMetricsConfig((MetricsConfig) config);
        } else if (config instanceof SslConfig) {
            ConfigValidationUtils.validateSslConfig((SslConfig) config);
        }
```

### ConstantValue
Condition `introspectedMethod != null` is always `true`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
                    StandardMethodMetadata methodMetadata = (StandardMethodMetadata) source;
                    Method introspectedMethod = methodMetadata.getIntrospectedMethod();
                    if (introspectedMethod != null) {
                        return introspectedMethod.getReturnType().getName();
                    }
```

### ConstantValue
Condition `beanNames != null` is always `true`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
    static boolean checkBeanExists(ConfigurableListableBeanFactory beanFactory, Class<?> targetClass) {
        String[] beanNames = beanFactory.getBeanNamesForType(targetClass, true, false);
        return (beanNames != null && beanNames.length > 0);
    }

```

### ConstantValue
Condition `event instanceof DubboConfigInitEvent` is always `true`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java
    public void onApplicationEvent(ApplicationEvent event) {
        if (isOriginalEventSource(event)) {
            if (event instanceof DubboConfigInitEvent) {
                // This event will be notified at AbstractApplicationContext.registerListeners(),
                // init dubbo config beans before spring singleton beans
```

### ConstantValue
Condition `beanNamesForType == null` is always `false`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java
    private <T> T getOptionalBeanByType(final ListableBeanFactory beanFactory, final Class<T> type) {
        String[] beanNamesForType = beanFactory.getBeanNamesForType(type, true, false);
        if (beanNamesForType == null) {
            return null;
        }
```

### ConstantValue
Condition `nodeList == null` is always `false`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
    private static void parseNested(Element element, ParserContext parserContext, Class<?> beanClass, boolean registered, String tag, String property, String ref, BeanDefinition beanDefinition) {
        NodeList nodeList = element.getChildNodes();
        if (nodeList == null) {
            return;
        }
```

### ConstantValue
Condition `subDefinition != null` is always `true`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                }
                RootBeanDefinition subDefinition = parse((Element) node, parserContext, beanClass, registered);
                if (subDefinition != null) {
                    if (StringUtils.isNotEmpty(ref)) {
                        subDefinition.getPropertyValues().addPropertyValue(property, new RuntimeBeanReference(ref));
```

### ConstantValue
Condition `consumer instanceof ConsumerConfig` is always `false`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceCreator.java`
#### Snippet
```java
            if (consumer instanceof String) {
                consumerConfig = getConfig((String) consumer, ConsumerConfig.class);
            } else if (consumer instanceof ConsumerConfig) {
                consumerConfig = (ConsumerConfig) consumer;
            } else {
```

### ConstantValue
Condition `parameterTypes != null` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerMethodModel.java`
#### Snippet
```java
        this.parameterTypes = this.createParamSignature(parameterClasses);
        this.methodName = method.getName();
        this.generic = methodName.equals($INVOKE) && parameterTypes != null && parameterTypes.length == 3;
    }

```

### ConstantValue
Value `scopeModel` is always 'null'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelAwareExtensionProcessor.java`
#### Snippet
```java
        if (scopeModel instanceof FrameworkModel) {
            frameworkModel = (FrameworkModel) scopeModel;
        } else if (scopeModel instanceof ApplicationModel) {
            applicationModel = (ApplicationModel) scopeModel;
            frameworkModel = applicationModel.getFrameworkModel();
```

### ConstantValue
Value `scopeModel` is always 'null'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelAwareExtensionProcessor.java`
#### Snippet
```java
            applicationModel = (ApplicationModel) scopeModel;
            frameworkModel = applicationModel.getFrameworkModel();
        } else if (scopeModel instanceof ModuleModel) {
            moduleModel = (ModuleModel) scopeModel;
            applicationModel = moduleModel.getApplicationModel();
```

### ConstantValue
Condition `this instanceof ApplicationModel` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java

    private String getModelNamePath() {
        if (this instanceof ApplicationModel) {
            return safeGetAppName((ApplicationModel) this);
        } else if (this instanceof ModuleModel) {
```

### ConstantValue
Condition `b2` is always `true` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/common/lang/Prioritized.java`
#### Snippet
```java
        } else if (b2 && !b1) { // two is Prioritized, one is not
            return 1;
        } else if (b1 && b2) {  //  one and two both are Prioritized
            return ((Prioritized) one).compareTo((Prioritized) two);
        } else {                // no different
```

### ConstantValue
Condition `Float.isNaN(evictionFactor)` is always `false` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LFUCache.java`
#### Snippet
```java
        }
        boolean factorInRange = evictionFactor <= 1 && evictionFactor > 0;
        if (!factorInRange || Float.isNaN(evictionFactor)) {
            throw new IllegalArgumentException("Illegal eviction factor value:"
                    + evictionFactor);
```

### ConstantValue
Condition `type != null` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                return dest;
            } else {
                Class<?> ctype = (type != null && type.isArray() ? type.getComponentType() : pojo.getClass().getComponentType());
                int len = Array.getLength(pojo);
                Object dest = Array.newInstance(ctype, len);
```

### ConstantValue
Condition `dynamicConfiguration == null` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ModuleEnvironment.java`
#### Snippet
```java
        }
        if (dynamicGlobalConfiguration == null) {
            if (dynamicConfiguration == null) {
                if (logger.isWarnEnabled()) {
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", "dynamicConfiguration is null , return globalConfiguration.");
```

### ConstantValue
Condition `field != null` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
                    try {
                        Field field = result.getClass().getField(property);
                        if (field != null) {
                            field.set(result, value);
                        }
```

### ConstantValue
Value `defaultExtName` is always 'null'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
                    if (!CommonConstants.PROTOCOL_KEY.equals(value[i])) {
                        if (hasInvocation) {
                            getNameCode = String.format("url.getMethodParameter(methodName, \"%s\", \"%s\")", value[i], defaultExtName);
                        } else {
                            getNameCode = String.format("url.getParameter(\"%s\")", value[i]);
```

### ConstantValue
Condition `thread instanceof InternalThread` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocalMap.java`
#### Snippet
```java
        try {
            Thread thread = Thread.currentThread();
            if (thread instanceof InternalThread) {
                return ((InternalThread) thread).threadLocalMap();
            }
```

### ConstantValue
Condition `thread instanceof InternalThread` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocalMap.java`
#### Snippet
```java
    public static void remove() {
        Thread thread = Thread.currentThread();
        if (thread instanceof InternalThread) {
            ((InternalThread) thread).setThreadLocalMap(null);
        } else {
```

### ConstantValue
Condition `thread instanceof InternalThread` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocalMap.java`
#### Snippet
```java
    public static InternalThreadLocalMap get() {
        Thread thread = Thread.currentThread();
        if (thread instanceof InternalThread) {
            return fastGet((InternalThread) thread);
        }
```

### ConstantValue
Condition `thread instanceof InternalThread` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocalMap.java`
#### Snippet
```java
    public static void set(InternalThreadLocalMap internalThreadLocalMap) {
        Thread thread = Thread.currentThread();
        if (thread instanceof InternalThread) {
            ((InternalThread) thread).setThreadLocalMap(internalThreadLocalMap);
        }
```

### ConstantValue
Condition `thread instanceof InternalThread` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocalMap.java`
#### Snippet
```java
    public static InternalThreadLocalMap getIfSet() {
        Thread thread = Thread.currentThread();
        if (thread instanceof InternalThread) {
            return ((InternalThread) thread).threadLocalMap();
        }
```

### ConstantValue
Condition `k != null && k > 0` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                .stream()
                .map(ConsumerConfig::getReferThreadNum)
                .filter(k -> k != null && k > 0)
                .findAny().orElse(null);
        }
```

### ConstantValue
Condition `k != null && k > 0` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                .stream()
                .map(ProviderConfig::getExportThreadNum)
                .filter(k -> k != null && k > 0)
                .findAny().orElse(null);
        }
```

### ConstantValue
Condition `!(scopeModel instanceof ModuleModel)` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractMethodConfig.java`
#### Snippet
```java
            throw new IllegalArgumentException("scopeModel cannot be null");
        }
        if (!(scopeModel instanceof ModuleModel)) {
            throw new IllegalArgumentException("Invalid scope model, expect to be a ModuleModel but got: " + scopeModel);
        }
```

### ConstantValue
Condition `!(scopeModel instanceof ModuleModel)` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ModuleConfig.java`
#### Snippet
```java
            throw new IllegalArgumentException("scopeModel cannot be null");
        }
        if (!(scopeModel instanceof ModuleModel)) {
            throw new IllegalArgumentException("Invalid scope model, expect to be a ModuleModel but got: " + scopeModel);
        }
```

### ConstantValue
Condition `activate instanceof com.alibaba.dubbo.common.extension.Activate` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                                activateGroup = ((Activate) activate).group();
                                activateValue = ((Activate) activate).value();
                            } else if (activate instanceof com.alibaba.dubbo.common.extension.Activate) {
                                activateGroup = ((com.alibaba.dubbo.common.extension.Activate) activate).group();
                                activateValue = ((com.alibaba.dubbo.common.extension.Activate) activate).value();
```

### ConstantValue
Condition `instance instanceof ExtensionAccessorAware` is always `false` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                    continue;
                }
                if (instance instanceof ScopeModelAware || instance instanceof ExtensionAccessorAware) {
                    if (ignoredInjectMethodsDesc.contains(ReflectUtils.getDesc(method))) {
                        continue;
```

### ConstantValue
Condition `config instanceof ServiceConfigBase` is always `false` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
        // check duplicated configs
        // special check service and reference config by unique service name, speed up the processing of large number of instances
        if (config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase) {
            C existedConfig = (C) findDuplicatedInterfaceConfig((AbstractInterfaceConfig) config);
            if (existedConfig != null) {
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
        if (config instanceof ReferenceConfigBase) {
            return null;
        } else if (config instanceof ServiceConfigBase) {
            ServiceConfigBase serviceConfig = (ServiceConfigBase) config;
            uniqueServiceName = serviceConfig.getUniqueServiceName();
```

### ConstantValue
Value `config` is always 'null'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
        if (config instanceof ReferenceConfigBase) {
            return;
        } else if (config instanceof ServiceConfigBase) {
            ServiceConfigBase serviceConfig = (ServiceConfigBase) config;
            uniqueServiceName = serviceConfig.getUniqueServiceName();
```

### ConstantValue
Condition `config instanceof ServiceConfigBase` is always `false` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
    protected <C extends AbstractConfig> boolean removeIfAbsent(C config, Map<String, C> configsMap) {
        if (super.removeIfAbsent(config, configsMap)) {
            if (config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase) {
                removeInterfaceConfig((AbstractInterfaceConfig) config);
            }
```

### ConstantValue
Condition `key != null` is always `true` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
                    throw new IllegalStateException("load config failed, id: " + id + ", type:" + cls.getSimpleName());
                } finally {
                    if (addDefaultNameConfig && key != null) {
                        properties.remove(key);
                    }
```

### ConstantValue
Condition `!(config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase)` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

        // fast check duplicated equivalent config before write lock
        if (!(config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase)) {
            for (AbstractConfig value : configsMap.values()) {
                if (value.equals(config)) {
```

### ConstantValue
Condition `config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

        // fast check duplicated equivalent config before write lock
        if (!(config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase)) {
            for (AbstractConfig value : configsMap.values()) {
                if (value.equals(config)) {
```

### ConstantValue
Value `codec2` is always 'null'
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConfigOperator.java`
#### Snippet
```java

        if (!(codec2 instanceof DefaultCodec)){
            NettyCodecAdapter codec = new NettyCodecAdapter(codec2, channel.getUrl(), handler);
            ((NettyChannel) channel).getNioChannel().pipeline().addLast(
                codec.getDecoder()
```

### ConstantValue
Condition `getClass() != obj.getClass()` is always `true`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
        }

        if (getClass() != obj.getClass()) {
            return false;
        }
```

### ConstantValue
Condition `msg instanceof MultiMessage` is always `false`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyCodecAdapter.java`
#### Snippet
```java
                out.writeBytes(((ByteBuf) msg));
                encoded = true;
            } else if (msg instanceof MultiMessage) {
                for (Object singleMessage : ((MultiMessage) msg)) {
                    if (singleMessage instanceof ByteBuf) {
```

### ConstantValue
Condition `getClass() != obj.getClass()` is always `true`
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
        }

        if (getClass() != obj.getClass()) {
            return false;
        }
```

### ConstantValue
Condition `l < argArray.length - 1` is always `false`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/logging/MessageFormatter.java`
#### Snippet
```java
        // append the characters following the last {} pair.
        sbuf.append(messagePattern.substring(i));
        if (l < argArray.length - 1) {
            return new FormattingTuple(sbuf.toString(), argArray, throwableCandidate);
        } else {
```

### ConstantValue
Condition `throwable instanceof RpcException` is always `false`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/TriRpcStatus.java`
#### Snippet
```java
            return ((StatusRpcException) throwable).getStatus();
        }
        if (throwable instanceof RpcException) {
            RpcException rpcException = (RpcException) throwable;
            Code code = dubboCodeToTriCode(rpcException.getCode());
```

### ConstantValue
Condition `!(scopeModel instanceof ApplicationModel)` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
            throw new IllegalArgumentException("scopeModel cannot be null");
        }
        if (!(scopeModel instanceof ApplicationModel)) {
            throw new IllegalArgumentException("Invalid scope model, expect to be a ApplicationModel but got: " + scopeModel);
        }
```

### ConstantValue
Value `throwable` is always 'null'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/TriRpcStatus.java`
#### Snippet
```java
            return ((StatusRpcException) throwable).getStatus();
        }
        if (throwable instanceof RpcException) {
            RpcException rpcException = (RpcException) throwable;
            Code code = dubboCodeToTriCode(rpcException.getCode());
```

### ConstantValue
Condition `throwable instanceof TimeoutException` is always `false`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/TriRpcStatus.java`
#### Snippet
```java
            return new TriRpcStatus(code, throwable, description);
        }
        if (throwable instanceof TimeoutException) {
            return new TriRpcStatus(Code.DEADLINE_EXCEEDED, throwable, description);
        }
```

### ConstantValue
Value `throwable` is always 'null'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/TriRpcStatus.java`
#### Snippet
```java
            return new TriRpcStatus(code, throwable, description);
        }
        if (throwable instanceof TimeoutException) {
            return new TriRpcStatus(Code.DEADLINE_EXCEEDED, throwable, description);
        }
```

### ConstantValue
Value `throwable` is always 'null'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/TriRpcStatus.java`
#### Snippet
```java
            return new TriRpcStatus(Code.DEADLINE_EXCEEDED, throwable, description);
        }
        return new TriRpcStatus(Code.UNKNOWN, throwable, description);
    }

```

### ConstantValue
Condition `executor instanceof ThreadlessExecutor` is always `true` when reached
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/DeadlineFuture.java`
#### Snippet
```java
        // the result is returning, but the caller thread may still waiting
        // to avoid endless waiting for whatever reason, notify caller thread to return.
        if (executor != null && executor instanceof ThreadlessExecutor) {
            ThreadlessExecutor threadlessExecutor = (ThreadlessExecutor) executor;
            if (threadlessExecutor.isWaiting()) {
```

### ConstantValue
Value `invocation` is always 'null'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
            if (CollectionUtils.isNotEmpty(supportedSelectors)) {
                for (PenetrateAttachmentSelector supportedSelector : supportedSelectors) {
                    Map<String, Object> selected = supportedSelector.select(invocation, RpcContext.getClientAttachment(), RpcContext.getServerAttachment());
                    if (CollectionUtils.isNotEmptyMap(selected)) {
                        ((RpcInvocation) invocation).addObjectAttachments(selected);
```

### ConstantValue
Value `invocation` is always 'null'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                    Map<String, Object> selected = supportedSelector.select(invocation, RpcContext.getClientAttachment(), RpcContext.getServerAttachment());
                    if (CollectionUtils.isNotEmptyMap(selected)) {
                        ((RpcInvocation) invocation).addObjectAttachments(selected);
                    }
                }
```

### ConstantValue
Value `invocation` is always 'null'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                }
            } else {
                ((RpcInvocation) invocation).addObjectAttachments(RpcContext.getServerAttachment().getObjectAttachments());
            }
            Map<String, Object> contextAttachments = RpcContext.getClientAttachment().getObjectAttachments();
```

### ConstantValue
Value `invocation` is always 'null'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                 * a mistake in most cases (for example, through Filter to RpcContext output traceId and spanId and other information).
                 */
                ((RpcInvocation) invocation).addObjectAttachments(contextAttachments);
            }

```

### ConstantValue
Value `invocation` is always 'null'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
            Object countDown = RpcContext.getServerAttachment().getObjectAttachment(TIME_COUNTDOWN_KEY);
            if (countDown != null) {
                String methodName = RpcUtils.getMethodName(invocation);
                // When the client has enabled the timeout-countdown function,
                // the subsequent calls launched by the Server side will be enabled by default,
```

### ConstantValue
Value `invocation` is always 'null'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
            }
            RpcContext.removeClientResponseContext();
            return invoker.invoke(invocation);
        } finally {
            RpcContext.restoreServiceContext(originServiceContext);
```

### ConstantValue
Value `scopeModel` is always 'null'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/DefaultFilterChainBuilder.java`
#### Snippet
```java
        if (scopeModel instanceof ApplicationModel) {
            moduleModels = ((ApplicationModel) scopeModel).getPubModuleModels();
        } else if (scopeModel instanceof ModuleModel) {
            moduleModels = new ArrayList<>();
            moduleModels.add((ModuleModel) scopeModel);
```

### ConstantValue
Condition `filter instanceof FILTER.Listener` is always `false`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                        listenableFilter.removeListener(invocation);
                    }
                } else if (filter instanceof FILTER.Listener) {
                    FILTER.Listener listener = (FILTER.Listener) filter;
                    listener.onError(e, originalInvoker, invocation);
```

### ConstantValue
Condition `filter instanceof FILTER.Listener` is always `false`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                        listenableFilter.removeListener(invocation);
                    }
                } else if (filter instanceof FILTER.Listener) {
                    FILTER.Listener listener = (FILTER.Listener) filter;
                    listener.onError(e, originalInvoker, invocation);
```

### ConstantValue
Condition `filter instanceof FILTER.Listener` is always `false`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                        listenableFilter.removeListener(invocation);
                    }
                } else if (filter instanceof FILTER.Listener) {
                    FILTER.Listener listener = (FILTER.Listener) filter;
                    if (t == null) {
```

### ConstantValue
Condition `filter instanceof FILTER.Listener` is always `false`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                                listenableFilter.removeListener(invocation);
                            }
                        } else if (filter instanceof FILTER.Listener) {
                            FILTER.Listener listener = (FILTER.Listener) filter;
                            if (t == null) {
```

### ConstantValue
Condition `inputInvokers instanceof BitList` is always `true`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/RouterSnapshotNode.java`
#### Snippet
```java
        this.name = name;
        this.beforeSize = inputInvokers.size();
        if (inputInvokers instanceof BitList) {
            this.inputInvokers = inputInvokers;
        } else {
```

### ConstantValue
Condition `!mismatches.isEmpty()` is always `true` when reached
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/AbstractConditionMatcher.java`
#### Snippet
```java
        }

        if (!matches.isEmpty() && !mismatches.isEmpty()) {
            //when both mismatches and matches contain the same value, then using mismatches first
            for (String mismatch : mismatches) {
```

### ConstantValue
Result of `mismatches.isEmpty()` is always 'false'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/AbstractConditionMatcher.java`
#### Snippet
```java
        }

        if (!matches.isEmpty() && !mismatches.isEmpty()) {
            //when both mismatches and matches contain the same value, then using mismatches first
            for (String mismatch : mismatches) {
```

### ConstantValue
Condition `e instanceof RpcException` is always `true`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailfastClusterInvoker.java`
#### Snippet
```java
            return invokeWithContext(invoker, invocation);
        } catch (Throwable e) {
            if (e instanceof RpcException && ((RpcException) e).isBiz()) { // biz exception.
                throw (RpcException) e;
            }
```

### ConstantValue
Condition `e instanceof RpcException` is always `true`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailfastClusterInvoker.java`
#### Snippet
```java
                throw (RpcException) e;
            }
            throw new RpcException(e instanceof RpcException ? ((RpcException) e).getCode() : 0,
                "Failfast invoke providers " + invoker.getUrl() + " " + loadbalance.getClass().getSimpleName()
                    + " for service " + getInterface().getName()
```

### ConstantValue
Value `result` is always 'null'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/BroadcastClusterInvoker.java`
#### Snippet
```java
        }

        return result;
    }

```

### ConstantValue
Condition `le != null` is always `false`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
            }
        }
        if (le != null) {
            throw le;
        }
```

### ConstantValue
Condition `times > 1` is always `false`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
            throw le;
        }
        if (times > 1) {
            logger.info("Failed to request nacos naming server for " + (times - 1) + " times and finally success. " +
                "This may caused by high stress of nacos server.");
```

### ConstantValue
Condition `le != null` is always `false`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
            }
        }
        if (le != null) {
            throw le;
        }
```

### ConstantValue
Condition `times > 1` is always `false`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
            throw le;
        }
        if (times > 1) {
            logger.info("Failed to request nacos naming server for " + (times - 1) + " times and finally success. " +
                "This may caused by high stress of nacos server.");
```

### ConstantValue
Condition `result instanceof Response` is always `false`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCountCodec.java`
#### Snippet
```java
                /* ignore */
            }
        } else if (result instanceof Response) {
            try {
                ((AppResponse) ((Response) result).getResult()).setAttachment(OUTPUT_KEY, String.valueOf(bytes));
```

### ConstantValue
Value `invocation` is always 'null'
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
            return rpcInvocation;
        }
        return invocation;
    }
}
```

### ConstantValue
Condition `values != null` is always `true`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/FindPidWindowsProcessor.java`
#### Snippet
```java
                //   TCP    127.0.0.1:56723        127.0.0.1:2182         ESTABLISHED     1980
                //   TCP    [::]:2182              [::]:0                 LISTENING       4020
                if (values != null && values.length > 0) {
                    for (int i = 0; i < values.length; i++) {
                        List<String> segments = Arrays.stream(values[i].trim().split(" "))
```

### ConstantValue
Condition `segments != null` is always `true`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/FindPidWindowsProcessor.java`
#### Snippet
```java
                        // ESTABLISHED
                        // 4020
                        if (segments != null && segments.size() == 5) {
                            if (this.check(segments.get(1), clientPort)) {
                                int pid = Integer.valueOf(segments.get(segments.size() - 1).trim());
```

### ConstantValue
Condition `targetPath.toFile() != null` is always `true`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            String.valueOf(clientPort));
        // check if it's unpacked.
        if (targetPath.toFile() != null && targetPath.toFile().isDirectory()) {
            logger.info(String.format("The file has been unpacked, target path:%s", targetPath.toString()));
            return;
```

### ConstantValue
Condition `parameterAnnotations != null` is always `true`
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
    private static boolean hasConstraintParameter(Method method) {
        Annotation[][] parameterAnnotations = method.getParameterAnnotations();
        if (parameterAnnotations != null && parameterAnnotations.length > 0) {
            for (Annotation[] annotations : parameterAnnotations) {
                for (Annotation annotation : annotations) {
```

### ConstantValue
Condition `throwable instanceof RpcException` is always `true`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/filter/MethodMetricsInterceptor.java`
#### Snippet
```java
        }
        String side = getSide(invocation);
        if (throwable instanceof RpcException) {
            RpcException e = (RpcException) throwable;

```

### ConstantValue
Condition `throwable instanceof RpcException` is always `true`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/filter/MethodMetricsInterceptor.java`
#### Snippet
```java
        }

        if (throwable instanceof RpcException && ((RpcException) throwable).isBiz()) {
            onCompleted(invocation);
        }else{
```

### ConstantValue
Condition `t instanceof RpcException` is always `true` when reached
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/filter/MetricsClusterFilter.java`
#### Snippet
```java
            return;
        }
        if (t != null && t instanceof RpcException) {
            RpcException e = (RpcException) t;
            if (e.isForbidden()) {
```

### ConstantValue
Value `event` is always 'null'
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/AggregateMetricsCollector.java`
#### Snippet
```java
        if (event instanceof RTEvent) {
            onRTEvent((RTEvent) event);
        } else if (event instanceof MethodEvent) {
            onRequestEvent((MethodEvent) event);
        }
```

### ConstantValue
Method reference result is always 'true'
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/ThreadPoolMetricsSampler.java`
#### Snippet
```java

    public void addExecutors(String name, ExecutorService executorService) {
        Optional.ofNullable(executorService).filter(Objects::nonNull).filter(e -> e instanceof ThreadPoolExecutor)
            .map(e -> (ThreadPoolExecutor) e)
            .ifPresent(threadPoolExecutor -> sampleThreadPoolExecutor.put(name, threadPoolExecutor));
```

### ConstantValue
Condition `reportCacheExecutor != null` is always `true`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
    @Override
    public void destroy() {
        if (reportCacheExecutor != null) {
            reportCacheExecutor.shutdown();
        }
```

### ConstantValue
Value `other.extendParams` is always 'null'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
        this.metadata = new HashMap<>(other.metadata);
        this.applicationModel = other.applicationModel;
        this.extendParams = other.extendParams != null ? new HashMap<>(other.extendParams) : other.extendParams;
        this.endpoints = other.endpoints != null ? new ArrayList<>(other.endpoints) : other.endpoints;
    }
```

### ConstantValue
Value `other.endpoints` is always 'null'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
        this.applicationModel = other.applicationModel;
        this.extendParams = other.extendParams != null ? new HashMap<>(other.extendParams) : other.extendParams;
        this.endpoints = other.endpoints != null ? new ArrayList<>(other.endpoints) : other.endpoints;
    }

```

### ConstantValue
Result of `entry.getValue()` is always 'null'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
            }
            if (entry.getValue() == null) {
                equals = equals && (entry.getValue() == that.getMetadata().get(entry.getKey()));
            } else {
                equals = equals && entry.getValue().equals(that.getMetadata().get(entry.getKey()));
```

### ConstantValue
Value `value` is always 'null'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
            String value = null;
            if (map == null) {
                return value;
            }

```

### ConstantValue
Condition `oldURL instanceof OverrideInstanceAddressURL || newURL instanceof OverrideInstanceAddressURL` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        }

        if (oldURL instanceof OverrideInstanceAddressURL || newURL instanceof OverrideInstanceAddressURL) {
            if (!(oldURL instanceof OverrideInstanceAddressURL && newURL instanceof OverrideInstanceAddressURL)) {
                // sub-class changed
```

### ConstantValue
Condition `!(oldURL instanceof OverrideInstanceAddressURL && newURL instanceof OverrideInstanceAddressURL)` is always `false`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java

        if (oldURL instanceof OverrideInstanceAddressURL || newURL instanceof OverrideInstanceAddressURL) {
            if (!(oldURL instanceof OverrideInstanceAddressURL && newURL instanceof OverrideInstanceAddressURL)) {
                // sub-class changed
                return true;
```

### ConstantValue
Condition `oldURL instanceof OverrideInstanceAddressURL && newURL instanceof OverrideInstanceAddressURL` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java

        if (oldURL instanceof OverrideInstanceAddressURL || newURL instanceof OverrideInstanceAddressURL) {
            if (!(oldURL instanceof OverrideInstanceAddressURL && newURL instanceof OverrideInstanceAddressURL)) {
                // sub-class changed
                return true;
```

### ConstantValue
Condition `!(e instanceof SkipFailbackWrapperException)` is always `false`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            doUnregister(url);
        } catch (Exception e) {
            if (!(e instanceof SkipFailbackWrapperException)) {
                throw new IllegalStateException("Failed to unregister(re-export) " + url + " to registry " + getUrl().getAddress() + ", cause: " + e.getMessage(), e);
            }
```

### ConstantValue
Condition `e instanceof SkipFailbackWrapperException` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            doUnregister(url);
        } catch (Exception e) {
            if (!(e instanceof SkipFailbackWrapperException)) {
                throw new IllegalStateException("Failed to unregister(re-export) " + url + " to registry " + getUrl().getAddress() + ", cause: " + e.getMessage(), e);
            }
```

### ConstantValue
Condition `t instanceof SkipFailbackWrapperException` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                && url.getParameter(Constants.CHECK_KEY, true)
                && (url.getPort() != 0);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
```

### ConstantValue
Condition `check || skipFailback` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                && (url.getPort() != 0);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
```

### ConstantValue
Condition `skipFailback` is always `true` when reached
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                && (url.getPort() != 0);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
```

### ConstantValue
Condition `skipFailback` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
                }
```

### ConstantValue
Condition `t instanceof SkipFailbackWrapperException` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                boolean check = getUrl().getParameter(Constants.CHECK_KEY, true)
                    && url.getParameter(Constants.CHECK_KEY, true);
                boolean skipFailback = t instanceof SkipFailbackWrapperException;
                if (check || skipFailback) {
                    if (skipFailback) {
```

### ConstantValue
Condition `check || skipFailback` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                    && url.getParameter(Constants.CHECK_KEY, true);
                boolean skipFailback = t instanceof SkipFailbackWrapperException;
                if (check || skipFailback) {
                    if (skipFailback) {
                        t = t.getCause();
```

### ConstantValue
Condition `skipFailback` is always `true` when reached
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                    && url.getParameter(Constants.CHECK_KEY, true);
                boolean skipFailback = t instanceof SkipFailbackWrapperException;
                if (check || skipFailback) {
                    if (skipFailback) {
                        t = t.getCause();
```

### ConstantValue
Condition `skipFailback` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                boolean skipFailback = t instanceof SkipFailbackWrapperException;
                if (check || skipFailback) {
                    if (skipFailback) {
                        t = t.getCause();
                    }
```

### ConstantValue
Condition `t instanceof SkipFailbackWrapperException` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                && url.getParameter(Constants.CHECK_KEY, true)
                && (url.getPort() != 0);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
```

### ConstantValue
Condition `check || skipFailback` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                && (url.getPort() != 0);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
```

### ConstantValue
Condition `skipFailback` is always `true` when reached
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                && (url.getPort() != 0);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
```

### ConstantValue
Condition `skipFailback` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
                }
```

### ConstantValue
Condition `!(e instanceof SkipFailbackWrapperException)` is always `false`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            doRegister(url);
        } catch (Exception e) {
            if (!(e instanceof SkipFailbackWrapperException)) {
                throw new IllegalStateException("Failed to register (re-export) " + url + " to registry " + getUrl().getAddress() + ", cause: " + e.getMessage(), e);
            }
```

### ConstantValue
Condition `e instanceof SkipFailbackWrapperException` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            doRegister(url);
        } catch (Exception e) {
            if (!(e instanceof SkipFailbackWrapperException)) {
                throw new IllegalStateException("Failed to register (re-export) " + url + " to registry " + getUrl().getAddress() + ", cause: " + e.getMessage(), e);
            }
```

### ConstantValue
Condition `t instanceof SkipFailbackWrapperException` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            boolean check = getUrl().getParameter(Constants.CHECK_KEY, true)
                && url.getParameter(Constants.CHECK_KEY, true);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
```

### ConstantValue
Condition `check || skipFailback` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                && url.getParameter(Constants.CHECK_KEY, true);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
```

### ConstantValue
Condition `skipFailback` is always `true` when reached
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
                && url.getParameter(Constants.CHECK_KEY, true);
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
```

### ConstantValue
Condition `skipFailback` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            boolean skipFailback = t instanceof SkipFailbackWrapperException;
            if (check || skipFailback) {
                if (skipFailback) {
                    t = t.getCause();
                }
```

### ConstantValue
Value `zkListener` is always 'null'
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java

                        // Add children (i.e. service items).
                        List<String> children = zkClient.addChildListener(path, zkListener);
                        if (children != null) {
                            // The invocation point that may cause 1-1.
```

### ConstantValue
Condition `t instanceof RpcException` is always `true`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java

                    // Thrown by AbstractProtocol.optimizeSerialization()
                    if (t instanceof RpcException && t.getMessage().contains("serialization optimizer")) {
                        // 4-2 - serialization optimizer class initialization failed.
                        logger.error(PROTOCOL_FAILED_INIT_SERIALIZATION_OPTIMIZER, "typo in optimizer class", "",
```

### ConstantValue
Value `providerUrl` is always 'null'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            providerUrl = overrideWithConfigurator(providerUrl);
        } else {
            providerUrl = moduleModel.getApplicationModel().getBeanFactory().getBean(ClusterUtils.class).mergeUrl(providerUrl, queryMap); // Merge the consumer side parameters
            providerUrl = overrideWithConfigurator(providerUrl);
            providerUrl = providerUrl.addParameter(Constants.CHECK_KEY, String.valueOf(false)); // Do not check whether the connection is successful or not, always create Invoker!
```

### ConstantValue
Condition `message instanceof Response` is always `false`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/PayloadDropper.java`
#### Snippet
```java
            request.setData(null);
            return request;
        } else if (message instanceof Response) {
            Response response = (Response) message;
            response.setResult(null);
```

### ConstantValue
Value `originInvoker` is always 'null'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
                invoker = ((InvokerDelegate<?>) originInvoker).getInvoker();
            } else {
                invoker = originInvoker;
            }
            //The origin invoker
```

### ConstantValue
Condition `exception instanceof RemotingException` is always `true`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeHandler.java`
#### Snippet
```java
            if (exception instanceof RuntimeException) {
                throw (RuntimeException) exception;
            } else if (exception instanceof RemotingException) {
                throw (RemotingException) exception;
            } else {
```

### ConstantValue
Condition `message instanceof Response` is always `false`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeHandler.java`
#### Snippet
```java
                }
            }
        } else if (message instanceof Response) {
            handleResponse(channel, (Response) message);
        } else if (message instanceof String) {
```

### ConstantValue
Condition `message instanceof String` is always `false`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeHandler.java`
#### Snippet
```java
        } else if (message instanceof Response) {
            handleResponse(channel, (Response) message);
        } else if (message instanceof String) {
            if (isClientSide(channel)) {
                Exception e = new Exception("Dubbo client can not supported string message: " + message + " in channel: " + channel + ", url: " + channel.getUrl());
```

### ConstantValue
Condition `executor instanceof ThreadlessExecutor` is always `true` when reached
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/DefaultFuture.java`
#### Snippet
```java
        // the result is returning, but the caller thread may still wait
        // to avoid endless waiting for whatever reason, notify caller thread to return.
        if (executor != null && executor instanceof ThreadlessExecutor) {
            ThreadlessExecutor threadlessExecutor = (ThreadlessExecutor) executor;
            if (threadlessExecutor.isWaiting()) {
```

### ConstantValue
Condition `t instanceof ExceedPayloadLimitException` is always `true`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
                r.setStatus(Response.BAD_RESPONSE);

                if (t instanceof ExceedPayloadLimitException) {
                    logger.warn(TRANSPORT_EXCEED_PAYLOAD_LIMIT, "", "", t.getMessage(), t);
                    try {
```

### ConstantValue
Condition `t instanceof IOException` is always `true`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java

            // Rethrow exception
            if (t instanceof IOException) {
                throw (IOException) t;
            } else if (t instanceof RuntimeException) {
```

### ConstantValue
Condition `msg instanceof Response` is always `false`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
        if (msg instanceof Request) {
            encodeRequest(channel, buffer, (Request) msg);
        } else if (msg instanceof Response) {
            encodeResponse(channel, buffer, (Response) msg);
        } else {
```

### ConstantValue
Condition `message instanceof Response` is always `false`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeChannel.java`
#### Snippet
```java
        }
        if (message instanceof Request
            || message instanceof Response
            || message instanceof String) {
            channel.send(message, sent);
```

### ConstantValue
Condition `message instanceof String` is always `false`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeChannel.java`
#### Snippet
```java
        if (message instanceof Request
            || message instanceof Response
            || message instanceof String) {
            channel.send(message, sent);
        } else {
```

### ConstantValue
Value `handler` is always 'null'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractChannelHandlerDelegate.java`
#### Snippet
```java
            return ((ChannelHandlerDelegate) handler).getHandler();
        }
        return handler;
    }

```

### ConstantValue
Value `handler` is always 'null'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/WrappedChannelHandler.java`
#### Snippet
```java
            return ((ChannelHandlerDelegate) handler).getHandler();
        } else {
            return handler;
        }
    }
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/collector/stat/RegistryStatComposite.java`
#### Snippet
```java
        AtomicLongContainer avgContainer = new AtomicLongContainer(new MetricsKeyWrapper(registryOpType, MetricsKey.METRIC_RT_AVG), (k, v) -> v.incrementAndGet());
        avgContainer.setValueSupplier(applicationName -> {
            LongContainer<? extends Number> totalContainer = rtStats.stream().filter(longContainer -> longContainer.isKeyWrapper(MetricsKey.METRIC_RT_SUM, registryOpType)).findFirst().get();
            AtomicLong totalRtTimes = avgContainer.get(applicationName);
            AtomicLong totalRtSum = (AtomicLong) totalContainer.get(applicationName);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-metrics/dubbo-metrics-metadata/src/main/java/org/apache/dubbo/metrics/metadata/collector/stat/MetadataStatComposite.java`
#### Snippet
```java
        AtomicLongContainer avgContainer = new AtomicLongContainer(new MetricsKeyWrapper(registryOpType, MetricsKey.METRIC_RT_AVG), (k, v) -> v.incrementAndGet());
        avgContainer.setValueSupplier(applicationName -> {
            LongContainer<? extends Number> totalContainer = rtStats.stream().filter(longContainer -> longContainer.isKeyWrapper(MetricsKey.METRIC_RT_SUM, registryOpType)).findFirst().get();
            AtomicLong totalRtTimes = avgContainer.get(applicationName);
            AtomicLong totalRtSum = (AtomicLong) totalContainer.get(applicationName);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                        }
                        return null;
                    }).get();
        } catch (Exception e) {

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
                    if (configDirectory != null) {
                        // Register the configDirectory
                        configDirectory.toPath().register(watchService.get(), INTEREST_PATH_KINDS, MODIFIERS);
                    }
                });
```

## RuleId[id=MethodOverridesStaticMethod]
### MethodOverridesStaticMethod
Method `forServer()` tries to override a static method of a superclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleHttp2FrameCodecBuilder.java`
#### Snippet
```java
    }

    public static TripleHttp2FrameCodecBuilder forServer() {
        return forServer(Http2CodecUtil.SMALLEST_MAX_CONCURRENT_STREAMS);
    }
```

### MethodOverridesStaticMethod
Method `forClient()` tries to override a static method of a superclass
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleHttp2FrameCodecBuilder.java`
#### Snippet
```java
    }

    public static TripleHttp2FrameCodecBuilder forClient() {
        return forClient(Http2CodecUtil.SMALLEST_MAX_CONCURRENT_STREAMS);
    }
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java

        for (int i = 0; i < argTypes.length; i++) {
            method.append((i == 0 ? "" : ", ") + argTypes[i].getSimpleName());
        }
        method.append(')');
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/DefaultDubboConfigBinder.java`
#### Snippet
```java
        if (errors.size() > 1) {
            IntStream.range(1, errors.size()).forEach(i -> {
                builder.append(", " + errors.get(i).getField());
            });
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
                threadInfo.getThreadState());
        if (threadInfo.getLockName() != null) {
            sb.append(" on " + threadInfo.getLockName());
        }
        if (threadInfo.getLockOwnerName() != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
        }
        if (threadInfo.getLockOwnerName() != null) {
            sb.append(" owned by \"" + threadInfo.getLockOwnerName() +
                    "\" Id=" + threadInfo.getLockOwnerId());
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
        LockInfo[] locks = threadInfo.getLockedSynchronizers();
        if (locks.length > 0) {
            sb.append("\n\tNumber of locked synchronizers = " + locks.length);
            sb.append('\n');
            for (LockInfo li : locks) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
            sb.append('\n');
            for (LockInfo li : locks) {
                sb.append("\t- " + li);
                sb.append('\n');
            }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/Ls.java`
#### Snippet
```java
    public String listConsumer() {
        StringBuilder stringBuilder = new StringBuilder();
        stringBuilder.append("As Consumer side:" + System.lineSeparator());
        Collection<ConsumerModel> consumerModelList = frameworkModel.getServiceRepository().allConsumerModels();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/Ls.java`
#### Snippet
```java
    public String listProvider() {
        StringBuilder stringBuilder = new StringBuilder();
        stringBuilder.append("As Provider side:" + System.lineSeparator());
        Collection<ProviderModel> providerModelList = frameworkModel.getServiceRepository().allProviderModels();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/media/MediaType.java`
#### Snippet
```java

        for (MediaType mediaType : values) {
            stringBuilder.append(mediaType.value + " ");
        }
        return stringBuilder.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/HelpTelnetHandler.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder();
        buf.append("Command:\r\n    ");
        buf.append(message + " " + help.parameter().replace("\r\n", " ").replace("\n", " "));
        buf.append("\r\nSummary:\r\n    ");
        buf.append(help.summary().replace("\r\n", " ").replace("\n", " "));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                String content = new String(bb.array()).replace("<", "&lt;")
                                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
                                buf.append("\r\ncontent:" + content);

                                buf.append("\r\nmodified:" + (new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                buf.append("\r\ncontent:" + content);

                                buf.append("\r\nmodified:" + (new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
                                        .format(new Date(file.lastModified()))));
                                buf.append("\r\nsize:" + size + "\r\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                buf.append("\r\nmodified:" + (new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
                                        .format(new Date(file.lastModified()))));
                                buf.append("\r\nsize:" + size + "\r\n");
                            }
                        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
        }
        buf.append("\r\nCURRENT LOG LEVEL:" + LoggerFactory.getLevel())
                .append("\r\nCURRENT LOG APPENDER:" + (file == null ? "console" : file.getAbsolutePath()));
        return buf.toString();
    }
```

## RuleId[id=IOResource]
### IOResource
'ClassPathXmlApplicationContext' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
public class Application {
    public static void main(String[] args) throws Exception {
        ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext("spring/dubbo-provider.xml");
        context.start();
        System.in.read();
```

### IOResource
'InputStreamReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/StreamUtils.java`
#### Snippet
```java
        } else {
            StringBuilder out = new StringBuilder();
            InputStreamReader reader = new InputStreamReader(in, charset);
            char[] buffer = new char[4096];

```

### IOResource
'AnnotationConfigApplicationContext' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
public class Application {
    public static void main(String[] args) throws Exception {
        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext(ProviderConfiguration.class);
        context.start();
        System.in.read();
```

### IOResource
'ClassPathXmlApplicationContext' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
     */
    public static void main(String[] args) throws Exception {
        ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext("spring/dubbo-consumer.xml");
        context.start();
        DemoService demoService = context.getBean("demoService", DemoService.class);
```

### IOResource
'FileInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
        if (file != null && file.exists()) {
            try {
                FileInputStream fis = new FileInputStream(file);
                FileChannel channel = fis.getChannel();
                size = channel.size();
```

### IOResource
'AnnotationConfigApplicationContext' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
     */
    public static void main(String[] args) {
        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext(ConsumerConfiguration.class);
        context.start();
        DemoService service = context.getBean("demoServiceComponent", DemoServiceComponent.class);
```

### IOResource
'RandomAccessFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
                throw new AssertionError("Failed to create lock file " + lockFile);
            }
            FileChannel lockFileChannel = new RandomAccessFile(lockFile, "rw").getChannel();
            dirLock = lockFileChannel.tryLock();
        } catch (OverlappingFileLockException ofle) {
```

### IOResource
'InputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/DubboNativeCodeGeneratorMojo.java`
#### Snippet
```java

        Arrays.stream(nativeFiles).forEach(nativeFile -> {
            InputStream is = DubboNativeCodeGeneratorMojo.class.getClassLoader().getResourceAsStream(nativeFile);
            project.getResources().stream().findFirst().ifPresent(resource -> {
                String directory = resource.getDirectory();
```

### IOResource
'ByteArrayOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Bzip2.java`
#### Snippet
```java
        }

        ByteArrayOutputStream out = new ByteArrayOutputStream();
        ByteArrayInputStream in = new ByteArrayInputStream(payloadByteArr);
        try {
```

### IOResource
'BZip2CompressorInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Bzip2.java`
#### Snippet
```java
        ByteArrayInputStream in = new ByteArrayInputStream(payloadByteArr);
        try {
            BZip2CompressorInputStream unZip = new BZip2CompressorInputStream(in);
            byte[] buffer = new byte[2048];
            int n;
```

### IOResource
'DefaultAsyncHttpClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/DownloadZookeeperInitializer.java`
#### Snippet
```java
     */
    private void download(String url, Path targetPath) throws ExecutionException, InterruptedException, IOException, TimeoutException {
        AsyncHttpClient asyncHttpClient = new DefaultAsyncHttpClient(
            new DefaultAsyncHttpClientConfig.Builder()
                .setConnectTimeout(CONNECT_TIMEOUT)
```

### IOResource
'Scanner' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/textui/TTable.java`
#### Snippet
```java

                    if (null == scannerArray[colIndex]) {
                        scannerArray[colIndex] = new Scanner(
                                new StringReader(wrap(getData(rowIndex, columnDefineArray[colIndex]), width)));
                    }
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/status/SpringStatusChecker.java`
#### Snippet
```java
                }
                String[] configs = (String[]) method.invoke(applicationContext, new Object[0]);
                if (configs != null && configs.length > 0) {
                    for (String config : configs) {
                        if (buf.length() > 0) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java

    public static boolean hasMethods(Method[] methods) {
        if (methods == null || methods.length == 0) {
            return false;
        }
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLBuilder.java`
#### Snippet
```java
    @Override
    public URLBuilder removeParameters(String... keys) {
        if (keys == null || keys.length == 0) {
            return this;
        }
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
    public static <K, V> Map<K, V> toMap(Map.Entry<K, V>[] entries) {
        Map<K, V> map = new HashMap<K, V>();
        if (entries != null && entries.length > 0) {
            for (Map.Entry<K, V> entry : entries) {
                map.put(entry.getKey(), entry.getValue());
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
            return classForName(className);
        } catch (ClassNotFoundException e) {
            if (packages != null && packages.length > 0) {
                for (String pkg : packages) {
                    try {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        urls.add(this);
        String[] backups = getParameter(RemotingConstants.BACKUP_KEY, new String[0]);
        if (backups != null && backups.length > 0) {
            for (String backup : backups) {
                urls.add(this.setAddress(backup));
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/support/ActivateComparator.java`
#### Snippet
```java

    private Class<?> findSpi(Class<?> clazz) {
        if (clazz.getInterfaces().length == 0) {
            return null;
        }
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/util/JaketConfigurationUtils.java`
#### Snippet
```java
        String canonicalName = clazz.getCanonicalName();

        if (closedTypes != null && closedTypes.length > 0) {
            for (String type : closedTypes) {
                if (canonicalName.startsWith(type)) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ReflectionPackableMethod.java`
#### Snippet
```java
        while (clazz != Object.class && clazz != null) {
            Class<?>[] interfaces = clazz.getInterfaces();
            if (interfaces.length > 0) {
                for (Class<?> clazzInterface : interfaces) {
                    if (PROTOBUF_MESSAGE_CLASS_NAME.equalsIgnoreCase(clazzInterface.getName())) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
    private static boolean hasConstraintParameter(Method method) {
        Annotation[][] parameterAnnotations = method.getParameterAnnotations();
        if (parameterAnnotations != null && parameterAnnotations.length > 0) {
            for (Annotation[] annotations : parameterAnnotations) {
                for (Annotation annotation : annotations) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
    private static boolean hasConstraintParameter(Method method) {
        Annotation[][] parameterAnnotations = method.getParameterAnnotations();
        if (parameterAnnotations.length > 0) {
            for (Annotation[] annotations : parameterAnnotations) {
                for (Annotation annotation : annotations) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/PortTelnet.java`
#### Snippet
```java
        String port = null;
        boolean detail = false;
        if (args.length > 0) {
            for (String part : args) {
                if ("-l".equals(part)) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataServiceNameMapping.java`
#### Snippet
```java
                    if (StringUtils.isNotEmpty(oldConfigContent)) {
                        String[] oldAppNames = oldConfigContent.split(",");
                        if (oldAppNames.length > 0) {
                            for (String oldAppName : oldAppNames) {
                                if (oldAppName.equals(appName)) {
```

## RuleId[id=FieldMayBeStatic]
### FieldMayBeStatic
Field `UNKNOWN_SESSION_ID` may be 'static'
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java

    private class CuratorConnectionStateListener implements ConnectionStateListener {
        private final long UNKNOWN_SESSION_ID = -1L;
        private long lastSessionId;
        private int timeout;
```

### FieldMayBeStatic
Field `UNKNOWN_SESSION_ID` may be 'static'
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java

    private class CuratorConnectionStateListener implements ConnectionStateListener {
        private final long UNKNOWN_SESSION_ID = -1L;

        private long lastSessionId;
```

### FieldMayBeStatic
Field `MaxSize` may be 'static'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/pu/TelnetDetector.java`
#### Snippet
```java

    private final FrameworkModel frameworkModel;
    private final int MaxSize = 2048;
    private final ChannelBuffer AytPreface = new HeapChannelBuffer(new byte[]{(byte) 0xff, (byte) 0xf6});

```

## RuleId[id=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `MeshConstant` has only 'static' members, and lacks a 'private' constructor
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/MeshConstant.java`
#### Snippet
```java
import io.fabric8.kubernetes.client.dsl.base.CustomResourceDefinitionContext;

public class MeshConstant {
    public static CustomResourceDefinitionContext getVsDefinition() {
        // TODO cache
```

### UtilityClassWithoutPrivateConstructor
Class `KubernetesClientConst` has only 'static' members, and lacks a 'private' constructor
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesClientConst.java`
#### Snippet
```java
package org.apache.dubbo.registry.kubernetes.util;

public class KubernetesClientConst {
    public static final String DEFAULT_MASTER_PLACEHOLDER = "DEFAULT_MASTER_HOST";
    public static final String DEFAULT_MASTER_URL = "https://kubernetes.default.svc";
```

### UtilityClassWithoutPrivateConstructor
Class `KubernetesConfigUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/util/KubernetesConfigUtils.java`
#### Snippet
```java
import static org.apache.dubbo.registry.kubernetes.util.KubernetesClientConst.WATCH_RECONNECT_LIMIT;

public class KubernetesConfigUtils {

    public static Config createKubernetesConfig(URL url) {
```

### UtilityClassWithoutPrivateConstructor
Class `Application` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
import org.springframework.context.support.ClassPathXmlApplicationContext;

public class Application {
    public static void main(String[] args) throws Exception {
        ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext("spring/dubbo-provider.xml");
```

### UtilityClassWithoutPrivateConstructor
Class `ClasspathBuilder` has only 'static' members, and lacks a 'private' constructor
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/CommandLineBuilder.java`
#### Snippet
```java
	}

	static class ClasspathBuilder {

		static String build(List<URL> classpathElements) {
```

### UtilityClassWithoutPrivateConstructor
Class `InvocationProfilerUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/InvocationProfilerUtils.java`
#### Snippet
```java
import java.util.concurrent.Callable;

public class InvocationProfilerUtils {

    public static void enterSimpleProfiler(Invocation invocation, Callable<String> messageCallable) {
```

### UtilityClassWithoutPrivateConstructor
Class `ProviderApplication` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/java/org/apache/dubbo/springboot/demo/provider/ProviderApplication.java`
#### Snippet
```java
@SpringBootApplication
@EnableDubbo(scanBasePackages = {"org.apache.dubbo.springboot.demo.provider"})
public class ProviderApplication {
    public static void main(String[] args) throws Exception {
        SpringApplication.run(ProviderApplication.class, args);
```

### UtilityClassWithoutPrivateConstructor
Class `InvocationUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/InvocationUtil.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.PROXY_TIMEOUT_REQUEST;

public class InvocationUtil {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(InvokerInvocationHandler.class);

```

### UtilityClassWithoutPrivateConstructor
Class `StubSuppliers` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/stub/StubSuppliers.java`
#### Snippet
```java
import java.util.function.Function;

public class StubSuppliers {

    private static final Map<String, Function<Invoker<?>, Object>> STUB_SUPPLIERS = new ConcurrentHashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `RpcUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
 * RpcUtils
 */
public class RpcUtils {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(RpcUtils.class);
```

### UtilityClassWithoutPrivateConstructor
Class `TypeUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/TypeUtil.java`
#### Snippet
```java
package org.apache.dubbo.rpc.protocol.rest.util;

public class TypeUtil {

    public static boolean isNumber(Class clazz) {
```

### UtilityClassWithoutPrivateConstructor
Class `ContentType` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/support/ContentType.java`
#### Snippet
```java
import javax.ws.rs.core.MediaType;

public class ContentType {

    public static final String APPLICATION_JSON_UTF_8 = MediaType.APPLICATION_JSON + "; " + MediaType.CHARSET_PARAMETER + "=UTF-8";
```

### UtilityClassWithoutPrivateConstructor
Class `MediaTypeUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MediaTypeUtil.java`
#### Snippet
```java
import java.util.List;

public class MediaTypeUtil {
    private static final List<MediaType> mediaTypes = Arrays.asList(MediaType.values());

```

### UtilityClassWithoutPrivateConstructor
Class `StreamUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/StreamUtils.java`
#### Snippet
```java
import java.nio.charset.Charset;

public class StreamUtils {
    public static String copyToString(InputStream in, Charset charset) throws IOException {
        if (in == null) {
```

### UtilityClassWithoutPrivateConstructor
Class `Application` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
import org.springframework.context.annotation.PropertySource;

public class Application {
    public static void main(String[] args) throws Exception {
        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext(ProviderConfiguration.class);
```

### UtilityClassWithoutPrivateConstructor
Class `DataParseUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
import java.util.StringTokenizer;

public class DataParseUtils {

    public static Object stringTypeConvert(Class targetType, String value) {
```

### UtilityClassWithoutPrivateConstructor
Class `MultiValueCreator` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java


public class MultiValueCreator {
    private final static String SPRING_MultiValueMap = "org.springframework.util.LinkedMultiValueMap";
    private final static String JAVAX_MultiValueMap = "org.jboss.resteasy.specimpl.MultivaluedMapImpl";
```

### UtilityClassWithoutPrivateConstructor
Class `HttpMessageCodecManager` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
import java.util.Set;

public class HttpMessageCodecManager {
    private static final Set<HttpMessageCodec> httpMessageCodecs =
        FrameworkModel.defaultModel().getExtensionLoader(HttpMessageCodec.class).getSupportedExtensionInstances();
```

### UtilityClassWithoutPrivateConstructor
Class `NumberUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
import java.util.Set;

public class NumberUtils {


```

### UtilityClassWithoutPrivateConstructor
Class `ReflectUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
import java.util.List;

public class ReflectUtils {
    private final static Class[] EMPTY_CLASS_ARRAY = new Class[0];
    private final static Object[] EMPTY_OBJECT_ARRAY = new Object[0];
```

### UtilityClassWithoutPrivateConstructor
Class `Application` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
import java.util.concurrent.CompletableFuture;

public class Application {
    /**
     * In order to make sure multicast registry works, need to specify '-Djava.net.preferIPv4Stack=true' before
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigValidationUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
import static org.apache.dubbo.rpc.cluster.Constants.REFER_KEY;

public class ConfigValidationUtils {
    private static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ConfigValidationUtils.class);
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `SpringCompatUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
 * Spring Compatibility Utils for spring 3.x/4.x/5.x
 */
public class SpringCompatUtils {

    private static volatile Boolean factoryMethodMetadataEnabled = null;
```

### UtilityClassWithoutPrivateConstructor
Class `DubboAnnotationUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboAnnotationUtils.java`
#### Snippet
```java
 * @since 2.5.11
 */
public class DubboAnnotationUtils {


```

### UtilityClassWithoutPrivateConstructor
Class `ReferenceBeanSupport` has only 'static' members, and lacks a 'private' constructor
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
import static org.apache.dubbo.common.utils.StringUtils.join;

public class ReferenceBeanSupport {

    private static List<String> IGNORED_ATTRS = Arrays.asList(ReferenceAttributes.ID, ReferenceAttributes.GROUP,
```

### UtilityClassWithoutPrivateConstructor
Class `UrlUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
 */
@Deprecated
public class UrlUtils {

    public static URL parseURL(String address, Map<String, String> defaults) {
```

### UtilityClassWithoutPrivateConstructor
Class `Application` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
import org.springframework.context.annotation.PropertySource;

public class Application {
    /**
     * In order to make sure multicast registry works, need to specify '-Djava.net.preferIPv4Stack=true' before
```

### UtilityClassWithoutPrivateConstructor
Class `ServiceDescriptorInternalCache` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/ServiceDescriptorInternalCache.java`
#### Snippet
```java
import org.apache.dubbo.rpc.model.ServiceDescriptor;

public class ServiceDescriptorInternalCache {
    private static final ServiceDescriptor genericServiceDescriptor = new ReflectionServiceDescriptor(GenericService.class);
    private static final ServiceDescriptor echoServiceDescriptor = new ReflectionServiceDescriptor(EchoService.class);
```

### UtilityClassWithoutPrivateConstructor
Class `ScopeModelUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelUtil.java`
#### Snippet
```java
import org.apache.dubbo.common.extension.SPI;

public class ScopeModelUtil {

    public static <T> ScopeModel getOrDefault(ScopeModel scopeModel, Class<T> type) {
```

### UtilityClassWithoutPrivateConstructor
Class `Matcher` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/ProtocolServiceKey.java`
#### Snippet
```java
    }

    public static class Matcher {
        public static boolean isMatch(ProtocolServiceKey rule, ProtocolServiceKey target) {
            // 1. 2. 3. match interface / version / group
```

### UtilityClassWithoutPrivateConstructor
Class `Matcher` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/ServiceKey.java`
#### Snippet
```java


    public static class Matcher {
        public static boolean isMatch(ServiceKey rule, ServiceKey target) {
            // 1. match interface (accurate match)
```

### UtilityClassWithoutPrivateConstructor
Class `IgnoredParam` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/param/IgnoredParam.java`
#### Snippet
```java
import java.util.HashSet;

public class IgnoredParam {
    private static final HashSet<String> IGNORED = new HashSet<>();

```

### UtilityClassWithoutPrivateConstructor
Class `URLItemCache` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLItemCache.java`
#### Snippet
```java
import java.util.Map;

public class URLItemCache {
    // thread safe with limited size, by default 1000
    private static final Map<String, String> PARAM_KEY_CACHE = new LRUCache<>(10000);
```

### UtilityClassWithoutPrivateConstructor
Class `GsonUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/GsonUtils.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.CommonConstants.GENERIC_SERIALIZATION_GSON;

public class GsonUtils {
    // weak reference of com.google.gson.Gson, prevent throw exception when init
    private static volatile Object gsonCache = null;
```

### UtilityClassWithoutPrivateConstructor
Class `ConcurrentHashMapUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConcurrentHashMapUtils.java`
#### Snippet
```java
 */

public class ConcurrentHashMapUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ExecutorUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ExecutorUtil.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.COMMON_UNEXPECTED_EXECUTORS_SHUTDOWN;

public class ExecutorUtil {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ExecutorUtil.class);
    private static final ThreadPoolExecutor SHUTDOWN_EXECUTOR = new ThreadPoolExecutor(0, 1,
```

### UtilityClassWithoutPrivateConstructor
Class `ClassUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
import static org.apache.dubbo.common.utils.StringUtils.isEmpty;

public class ClassUtils {
    /**
     * Suffix for array class names: "[]"
```

### UtilityClassWithoutPrivateConstructor
Class `ClassHelper` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
 * @deprecated Replace to <code>ClassUtils</code>
 */
public class ClassHelper {
    public static Class<?> forNameWithThreadContextClassLoader(String name) throws ClassNotFoundException {
        return ClassUtils.forName(name, Thread.currentThread().getContextClassLoader());
```

### UtilityClassWithoutPrivateConstructor
Class `ClassLoaderResourceLoader` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.COMMON_IO_EXCEPTION;

public class ClassLoaderResourceLoader {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ClassLoaderResourceLoader.class);
    private static SoftReference<Map<ClassLoader, Map<String, Set<URL>>>> classLoaderResourcesCache = null;
```

### UtilityClassWithoutPrivateConstructor
Class `LogUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LogUtil.java`
#### Snippet
```java
import java.util.List;

public class LogUtil {

    private static final Logger Log = LoggerFactory.getLogger(LogUtil.class);
```

### UtilityClassWithoutPrivateConstructor
Class `PojoUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
 * TODO: exact PojoUtils to scope bean
 */
public class PojoUtils {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(PojoUtils.class);
```

### UtilityClassWithoutPrivateConstructor
Class `JVMUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
import static java.lang.Thread.State.WAITING;

public class JVMUtil {
    public static void jstack(OutputStream stream) throws Exception {
        ThreadMXBean threadMxBean = ManagementFactory.getThreadMXBean();
```

### UtilityClassWithoutPrivateConstructor
Class `JsonUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JsonUtils.java`
#### Snippet
```java
import java.util.List;

public class JsonUtils {
    private static volatile JSON json;

```

### UtilityClassWithoutPrivateConstructor
Class `StatusUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/status/support/StatusUtils.java`
#### Snippet
```java
 * StatusManager
 */
public class StatusUtils {

    public static Status getSummaryStatus(Map<String, Status> statuses) {
```

### UtilityClassWithoutPrivateConstructor
Class `ProfilerSwitch` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/profiler/ProfilerSwitch.java`
#### Snippet
```java
 * TODO
 */
public class ProfilerSwitch {
    private final static AtomicBoolean enableDetailProfiler = new AtomicBoolean(false);

```

### UtilityClassWithoutPrivateConstructor
Class `Profiler` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/profiler/Profiler.java`
#### Snippet
```java
import java.util.List;

public class Profiler {
    public static final String PROFILER_KEY = "DUBBO_INVOKE_PROFILER";
    public static final int MAX_ENTRY_SIZE = 1000;
```

### UtilityClassWithoutPrivateConstructor
Class `MemoryLimitCalculator` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/MemoryLimitCalculator.java`
#### Snippet
```java
 * @see <a href="https://github.com/apache/incubator-shenyu/blob/master/shenyu-common/src/main/java/org/apache/shenyu/common/concurrent/MemoryLimitCalculator.java">MemoryLimitCalculator</a>
 */
public class MemoryLimitCalculator {

    private static volatile long maxAvailable;
```

### UtilityClassWithoutPrivateConstructor
Class `ScheduledCompletableFuture` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/concurrent/ScheduledCompletableFuture.java`
#### Snippet
```java
import java.util.function.Supplier;

public class ScheduledCompletableFuture {

    public static <T> CompletableFuture<T> schedule(
```

### UtilityClassWithoutPrivateConstructor
Class `JaketConfigurationUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/util/JaketConfigurationUtils.java`
#### Snippet
```java
 * 2015/1/27.
 */
public class JaketConfigurationUtils {

    private static final String CONFIGURATION_FILE = "jaket.properties";
```

### UtilityClassWithoutPrivateConstructor
Class `AotProcessor` has only 'static' members, and lacks a 'private' constructor
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/AotProcessor.java`
#### Snippet
```java
 * generate related self-adaptive code (native image does not support dynamic code generation. Therefore, code needs to be generated before compilation)
 */
public class AotProcessor {

    public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `NettyEventLoopFactory` has only 'static' members, and lacks a 'private' constructor
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyEventLoopFactory.java`
#### Snippet
```java
import static org.apache.dubbo.remoting.Constants.NETTY_EPOLL_ENABLE_KEY;

public class NettyEventLoopFactory {
    /**
     * netty client bootstrap
```

### UtilityClassWithoutPrivateConstructor
Class `SslContexts` has only 'static' members, and lacks a 'private' constructor
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/ssl/SslContexts.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.TRANSPORT_FAILED_CLOSE_STREAM;

public class SslContexts {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(SslContexts.class);
```

### UtilityClassWithoutPrivateConstructor
Class `Test` has only 'static' members, and lacks a 'private' constructor
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/Test.java`
#### Snippet
```java
import java.util.Set;

public class Test {

    public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `NettyHelper` has only 'static' members, and lacks a 'private' constructor
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyHelper.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.INTERNAL_ERROR;

final class NettyHelper {

    public static void setNettyLoggerFactory() {
```

### UtilityClassWithoutPrivateConstructor
Class `Application` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-provider/src/main/java/org/apache/dubbo/demo/graalvm/provider/Application.java`
#### Snippet
```java
import java.util.concurrent.CountDownLatch;

public class Application {

    public static void main(String[] args) throws Exception {
```

### UtilityClassWithoutPrivateConstructor
Class `GenericApplication` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-generic-call/src/main/java/org/apache/dubbo/demo/consumer/GenericApplication.java`
#### Snippet
```java
import org.apache.dubbo.rpc.service.GenericService;

public class GenericApplication {

    public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `CodeGenerator` has only 'static' members, and lacks a 'private' constructor
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/CodeGenerator.java`
#### Snippet
```java
 * generate related self-adaptive code (native image does not support dynamic code generation. Therefore, code needs to be generated before compilation)
 */
public class CodeGenerator {


```

### UtilityClassWithoutPrivateConstructor
Class `StubInvocationUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/stub/StubInvocationUtil.java`
#### Snippet
```java
import org.apache.dubbo.rpc.proxy.InvocationUtil;

public class StubInvocationUtil {

    public static <T, R> R unaryCall(Invoker<?> invoker, MethodDescriptor methodDescriptor,
```

### UtilityClassWithoutPrivateConstructor
Class `ClassLoadUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ClassLoadUtil.java`
#### Snippet
```java
package org.apache.dubbo.rpc.protocol.tri;

public class ClassLoadUtil {

    public static void switchContextLoader(ClassLoader loader) {
```

### UtilityClassWithoutPrivateConstructor
Class `TripleConstant` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleConstant.java`
#### Snippet
```java
import io.netty.util.AsciiString;

public class TripleConstant {

    public static final String CONTENT_PROTO = "application/grpc+proto";
```

### UtilityClassWithoutPrivateConstructor
Class `ExceptionUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ExceptionUtils.java`
#### Snippet
```java
import java.util.StringTokenizer;

public class ExceptionUtils {

    private static final int NOT_FOUND = -1;
```

### UtilityClassWithoutPrivateConstructor
Class `SingleProtobufUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/SingleProtobufUtils.java`
#### Snippet
```java
import java.util.concurrent.ConcurrentMap;

public class SingleProtobufUtils {
    private static final ConcurrentHashMap<Class<?>, Message> INST_CACHE = new ConcurrentHashMap<>();
    private static final ExtensionRegistryLite GLOBAL_REGISTRY = ExtensionRegistryLite.getEmptyRegistry();
```

### UtilityClassWithoutPrivateConstructor
Class `TripleCustomerProtocolWapper` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleCustomerProtocolWapper.java`
#### Snippet
```java
import java.util.Objects;

public class TripleCustomerProtocolWapper {

    static int makeTag(int fieldNumber, int wireType) {
```

### UtilityClassWithoutPrivateConstructor
Class `SchemaDescriptorRegistry` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/SchemaDescriptorRegistry.java`
#### Snippet
```java
import java.util.concurrent.ConcurrentHashMap;

public class SchemaDescriptorRegistry {

    private static final Map<String, FileDescriptor> DESCRIPTORS_BY_SYMBOL = new ConcurrentHashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `StreamUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/StreamUtils.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.PROTOCOL_UNSUPPORTED;

public class StreamUtils {

    protected static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(StreamUtils.class);
```

### UtilityClassWithoutPrivateConstructor
Class `TagRuleParser` has only 'static' members, and lacks a 'private' constructor
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/model/TagRuleParser.java`
#### Snippet
```java
 * Parse raw rule into structured tag rule
 */
public class TagRuleParser {

    public static TagRouterRule parse(String rawRule) {
```

### UtilityClassWithoutPrivateConstructor
Class `MeshRuleConstants` has only 'static' members, and lacks a 'private' constructor
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleConstants.java`
#### Snippet
```java
package org.apache.dubbo.rpc.cluster.router.mesh.route;

public class MeshRuleConstants {
    public static final String INVALID_APP_NAME = "unknown";

```

### UtilityClassWithoutPrivateConstructor
Class `ConditionRuleParser` has only 'static' members, and lacks a 'private' constructor
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/model/ConditionRuleParser.java`
#### Snippet
```java
 *     1.1.1.1
 */
public class ConditionRuleParser {

    public static ConditionRouterRule parse(String rawRule) {
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigParser` has only 'static' members, and lacks a 'private' constructor
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/parser/ConfigParser.java`
#### Snippet
```java
 * Config parser
 */
public class ConfigParser {

    public static List<URL> parseConfigurators(String rawConfig) {
```

### UtilityClassWithoutPrivateConstructor
Class `DubboCodecSupport` has only 'static' members, and lacks a 'private' constructor
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCodecSupport.java`
#### Snippet
```java
import static org.apache.dubbo.rpc.Constants.SERIALIZATION_ID_KEY;

public class DubboCodecSupport {

    public static Serialization getRequestSerialization(URL url, Invocation invocation) {
```

### UtilityClassWithoutPrivateConstructor
Class `NodeBuilder` has only 'static' members, and lacks a 'private' constructor
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/NodeBuilder.java`
#### Snippet
```java
import io.envoyproxy.envoy.config.core.v3.Node;

public class NodeBuilder {

    private final static String SVC_CLUSTER_LOCAL = ".svc.cluster.local";
```

### UtilityClassWithoutPrivateConstructor
Class `IstioConstant` has only 'static' members, and lacks a 'private' constructor
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioConstant.java`
#### Snippet
```java
package org.apache.dubbo.registry.xds.istio;

public class IstioConstant {
    /**
     * Address of the spiffe certificate provider. Defaults to discoveryAddress
```

### UtilityClassWithoutPrivateConstructor
Class `Application` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-api/dubbo-demo-api-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
import org.apache.dubbo.rpc.service.GenericService;

public class Application {
    public static void main(String[] args) {
        if (isClassic(args)) {
```

### UtilityClassWithoutPrivateConstructor
Class `Application` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-api/dubbo-demo-api-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
import java.util.concurrent.CountDownLatch;

public class Application {
    public static void main(String[] args) throws Exception {
        if (isClassic(args)) {
```

### UtilityClassWithoutPrivateConstructor
Class `Application` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-consumer/src/main/java/com/apache/dubbo/demo/graalvm/consumer/Application.java`
#### Snippet
```java
import java.util.Map;

public class Application {

    public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `ApiWrapperConsumer` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiWrapperConsumer.java`
#### Snippet
```java
import java.io.IOException;

public class ApiWrapperConsumer {
    public static void main(String[] args) throws IOException {
        ReferenceConfig<GreeterWrapperService> referenceConfig = new ReferenceConfig<>();
```

### UtilityClassWithoutPrivateConstructor
Class `ApiProvider` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/provider/ApiProvider.java`
#### Snippet
```java
import org.apache.dubbo.demo.GreeterServiceImpl;

public class ApiProvider {
    public static void main(String[] args) throws InterruptedException {
        ServiceConfig<GreeterService> serviceConfig = new ServiceConfig<>();
```

### UtilityClassWithoutPrivateConstructor
Class `ApiConsumer` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiConsumer.java`
#### Snippet
```java
import java.util.concurrent.TimeUnit;

public class ApiConsumer {
    public static void main(String[] args) throws InterruptedException, IOException {
        ReferenceConfig<GreeterService> referenceConfig = new ReferenceConfig<>();
```

### UtilityClassWithoutPrivateConstructor
Class `ApiWrapperProvider` has only 'static' members, and lacks a 'private' constructor
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/provider/ApiWrapperProvider.java`
#### Snippet
```java
import java.io.IOException;

public class ApiWrapperProvider {
    public static void main(String[] args) throws IOException {
        ServiceConfig<GreeterWrapperService> serviceConfig = new ServiceConfig<>();
```

### UtilityClassWithoutPrivateConstructor
Class `GlobalRegistryCenter` has only 'static' members, and lacks a 'private' constructor
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/GlobalRegistryCenter.java`
#### Snippet
```java
 * The global registry center.
 */
public class GlobalRegistryCenter {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ZookeeperRegistryCenterConfig` has only 'static' members, and lacks a 'private' constructor
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/config/ZookeeperRegistryCenterConfig.java`
#### Snippet
```java
 * Define the zookeeper global config for registry center.
 */
public class ZookeeperRegistryCenterConfig {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `SysProps` has only 'static' members, and lacks a 'private' constructor
in `dubbo-test/dubbo-test-common/src/main/java/org/apache/dubbo/test/common/SysProps.java`
#### Snippet
```java
 * Use to set and clear System property
 */
public class SysProps {

    private static Map<String, String> map = new LinkedHashMap<String, String>();
```

### UtilityClassWithoutPrivateConstructor
Class `DubboLogo` has only 'static' members, and lacks a 'private' constructor
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/server/DubboLogo.java`
#### Snippet
```java
package org.apache.dubbo.qos.server;

public class DubboLogo {
    public static final String DUBBO =
                    "   ___   __  __ ___   ___   ____     " + System.lineSeparator() +
```

### UtilityClassWithoutPrivateConstructor
Class `CommandContextFactory` has only 'static' members, and lacks a 'private' constructor
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/CommandContextFactory.java`
#### Snippet
```java
package org.apache.dubbo.qos.command;

public class CommandContextFactory {
    public static CommandContext newInstance(String commandName) {
        return new CommandContext(commandName);
```

### UtilityClassWithoutPrivateConstructor
Class `HttpCommandDecoder` has only 'static' members, and lacks a 'private' constructor
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/HttpCommandDecoder.java`
#### Snippet
```java
import java.util.List;

public class HttpCommandDecoder {
    public static CommandContext decode(HttpRequest request) {
        CommandContext commandContext = null;
```

### UtilityClassWithoutPrivateConstructor
Class `ServiceCheckUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/util/ServiceCheckUtils.java`
#### Snippet
```java
import java.util.Optional;

public class ServiceCheckUtils {

    public static String getRegisterStatus(ProviderModel providerModel) {
```

### UtilityClassWithoutPrivateConstructor
Class `TelnetCommandDecoder` has only 'static' members, and lacks a 'private' constructor
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/TelnetCommandDecoder.java`
#### Snippet
```java


public class TelnetCommandDecoder {
    public static final CommandContext decode(String str) {
        CommandContext commandContext = null;
```

### UtilityClassWithoutPrivateConstructor
Class `SignatureUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-plugin/dubbo-auth/src/main/java/org/apache/dubbo/auth/utils/SignatureUtils.java`
#### Snippet
```java
import java.util.Base64;

public class SignatureUtils {
    private static final String HMAC_SHA256_ALGORITHM = "HmacSHA256";

```

### UtilityClassWithoutPrivateConstructor
Class `RevisionResolver` has only 'static' members, and lacks a 'private' constructor
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/RevisionResolver.java`
#### Snippet
```java
import org.apache.dubbo.common.utils.MD5Utils;

public class RevisionResolver {

    public static final String EMPTY_REVISION = "0";
```

### UtilityClassWithoutPrivateConstructor
Class `PathUtil` has only 'static' members, and lacks a 'private' constructor
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
 * strings.set(UrlSplitIndex, (String) args.get(argIndex));
 */
public class PathUtil {
    private static final String SEPARATOR = MetadataConstants.PATH_SEPARATOR;

```

### UtilityClassWithoutPrivateConstructor
Class `ServiceInstanceMetadataUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/ServiceInstanceMetadataUtils.java`
#### Snippet
```java
 * @since 2.7.5
 */
public class ServiceInstanceMetadataUtils {
    private static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(ServiceInstanceMetadataUtils.class);

```

### UtilityClassWithoutPrivateConstructor
Class `MetadataUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataUtils.java`
#### Snippet
```java
import static org.apache.dubbo.registry.client.metadata.ServiceInstanceMetadataUtils.METADATA_SERVICE_URLS_PROPERTY_NAME;

public class MetadataUtils {
    public static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(MetadataUtils.class);

```

### UtilityClassWithoutPrivateConstructor
Class `PayloadDropper` has only 'static' members, and lacks a 'private' constructor
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/PayloadDropper.java`
#### Snippet
```java
import org.apache.dubbo.remoting.exchange.Response;

public class PayloadDropper {
    private static Logger logger = LoggerFactory.getLogger(PayloadDropper.class);

```

### UtilityClassWithoutPrivateConstructor
Class `UrlUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/UrlUtils.java`
#### Snippet
```java
import static org.apache.dubbo.remoting.Constants.SERIALIZATION_KEY;

public class UrlUtils {
    public static int getIdleTimeout(URL url) {
        int heartBeat = getHeartbeat(url);
```

### UtilityClassWithoutPrivateConstructor
Class `PortUnificationExchanger` has only 'static' members, and lacks a 'private' constructor
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/PortUnificationExchanger.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.PROTOCOL_ERROR_CLOSE_SERVER;

public class PortUnificationExchanger {

    private static final ErrorTypeAwareLogger log = LoggerFactory.getErrorTypeAwareLogger(PortUnificationExchanger.class);
```

### UtilityClassWithoutPrivateConstructor
Class `Main` has only 'static' members, and lacks a 'private' constructor
in `dubbo-container/dubbo-container-api/src/main/java/org/apache/dubbo/container/Main.java`
#### Snippet
```java
 * This class is entry point loading containers.
 */
public class Main {

    public static final String CONTAINER_KEY = "dubbo.container";
```

### UtilityClassWithoutPrivateConstructor
Class `DefaultSerializationSelector` has only 'static' members, and lacks a 'private' constructor
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/support/DefaultSerializationSelector.java`
#### Snippet
```java


public class DefaultSerializationSelector {

    private final static String DEFAULT_REMOTING_SERIALIZATION_PROPERTY_KEY = "DUBBO_DEFAULT_SERIALIZATION";
```

### UtilityClassWithoutPrivateConstructor
Class `TelnetUtils` has only 'static' members, and lacks a 'private' constructor
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
 * TelnetUtils
 */
public class TelnetUtils {

    public static String toList(List<List<String>> table) {
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Condition `t instanceof RpcException` is redundant and can be replaced with a null check
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ExecuteLimitFilter.java`
#### Snippet
```java
    @Override
    public void onError(Throwable t, Invoker<?> invoker, Invocation invocation) {
        if (t instanceof RpcException) {
            RpcException rpcException = (RpcException) t;
            if (rpcException.isLimitExceed()) {
```

### DataFlowIssue
Condition `t instanceof RpcException` is redundant and can be replaced with a null check
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ActiveLimitFilter.java`
#### Snippet
```java
        int max = invoker.getUrl().getMethodParameter(methodName, ACTIVES_KEY, 0);

        if (t instanceof RpcException) {
            RpcException rpcException = (RpcException) t;
            if (rpcException.isLimitExceed()) {
```

### DataFlowIssue
Condition `appException instanceof GenericException` is redundant and can be replaced with a null check
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
            if (appResponse.hasException()) {
                Throwable appException = appResponse.getException();
                if (appException instanceof GenericException) {
                    GenericException tmp = (GenericException) appException;
                    appException = new com.alibaba.dubbo.rpc.service.GenericException(tmp.getExceptionClass(), tmp.getExceptionMessage());
```

### DataFlowIssue
Condition `appException instanceof com.alibaba.dubbo.rpc.service.GenericException` is redundant and can be replaced with a null check
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
                    appException = new com.alibaba.dubbo.rpc.service.GenericException(tmp.getExceptionClass(), tmp.getExceptionMessage());
                }
                if (!(appException instanceof com.alibaba.dubbo.rpc.service.GenericException)) {
                    appException = new com.alibaba.dubbo.rpc.service.GenericException(appException);
                }
```

### DataFlowIssue
Array access `returnTypes[0]` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
        }
        if (ArrayUtils.isNotEmpty(returnTypes)) {
            value = PojoUtils.realize(value, (Class<?>) returnTypes[0], returnTypes.length > 1 ? returnTypes[1] : null);
        }
        return value;
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
        }
        if (mockClass == null || !serviceType.isAssignableFrom(mockClass)) {
            throw new IllegalStateException("The mock class " + mockClass.getName() +
                " not implement interface " + serviceType.getName());
        }
```

### DataFlowIssue
Method invocation `getInterfaces` may produce `NullPointerException`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static List<TypeMirror> getInterfaces(TypeElement type, Predicate<TypeMirror>... interfaceFilters) {
        return type == null ? emptyList() : filterAll((List<TypeMirror>) ofTypeElement(type).getInterfaces(), interfaceFilters);
    }

```

### DataFlowIssue
Method invocation `getDesc` may produce `NullPointerException`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
                        }
                    }
                    onFailed(getIdentifier() + " found failed module: " + errorModule.getDesc(), error);
                    break;
                case PENDING:
```

### DataFlowIssue
Condition `beanPostProcessor instanceof ReferenceAnnotationBeanPostProcessor` is redundant and can be replaced with a null check
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
    static ReferenceAnnotationBeanPostProcessor getReferenceAnnotationBeanPostProcessor(AbstractBeanFactory beanFactory) {
        for (BeanPostProcessor beanPostProcessor : beanFactory.getBeanPostProcessors()) {
            if (beanPostProcessor instanceof ReferenceAnnotationBeanPostProcessor) {
                return (ReferenceAnnotationBeanPostProcessor) beanPostProcessor;
            }
```

### DataFlowIssue
Argument `beanClassName` might be null
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
        String beanClassName = beanDefinition.getBeanClassName();

        return resolveClassName(beanClassName, classLoader);

    }
```

### DataFlowIssue
Dereference of `managedList` may produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
                ManagedList managedList = (ManagedList) value;
                List<Map<String, Object>> elementList = new ArrayList<>();
                for (Object el : managedList) {
                    Map<String, Object> element = convertPropertyValues(((BeanDefinitionHolder) el).getBeanDefinition().getPropertyValues());
                    element.remove(ReferenceAttributes.ID);
```

### DataFlowIssue
Argument `value` might be null
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
                value = elementList.toArray(new Object[0]);
            } else if (ReferenceAttributes.PARAMETERS.equals(propertyName)) {
                value = createParameterMap((ManagedMap) value);
            }
            //convert ref
```

### DataFlowIssue
Method invocation `getSuperclass` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TypeUtils.java`
#### Snippet
```java
                        }
                    }
                    Class<?> superClass = rawClass.getSuperclass();
                    if (superClass != null) {
                        actualTypeArguments.addAll(findActualTypeArguments(superClass, interfaceClass));
```

### DataFlowIssue
Argument `type` might be null
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java

        if (pojo.getClass().isArray()) {
            if (Collection.class.isAssignableFrom(type)) {
                Class<?> ctype = pojo.getClass().getComponentType();
                int len = Array.getLength(pojo);
```

### DataFlowIssue
Method invocation `isArray` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java

        if (pojo instanceof Collection<?>) {
            if (type.isArray()) {
                Class<?> ctype = type.getComponentType();
                Collection<Object> src = (Collection<Object>) pojo;
```

### DataFlowIssue
Method invocation `cardinality` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Set<Entry<String, String>> entrySet() {
            Set<Entry<String, String>> set = new LinkedHashSet<>((int) ((urlParam.KEY.cardinality() + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Set<Entry<String, String>> entrySet() {
            Set<Entry<String, String>> set = new LinkedHashSet<>((int) ((urlParam.KEY.cardinality() + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
```

### DataFlowIssue
Dereference of `urlParam.VALUE` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Set<String> keySet() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                set.add(DynamicParamTable.getKey(i));
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Set<String> keySet() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                set.add(DynamicParamTable.getKey(i));
```

### DataFlowIssue
Method invocation `nextSetBit` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        public Set<String> keySet() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                set.add(DynamicParamTable.getKey(i));
            }
```

### DataFlowIssue
Dereference of `urlParam.VALUE` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Collection<String> values() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Collection<String> values() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
```

### DataFlowIssue
Method invocation `nextSetBit` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        public Collection<String> values() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
                int offset = urlParam.keyIndexToOffset(i);
```

### DataFlowIssue
Method invocation `cardinality` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public int size() {
            return urlParam.KEY.cardinality() + urlParam.EXTRA_PARAMS.size();
        }

```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public int size() {
            return urlParam.KEY.cardinality() + urlParam.EXTRA_PARAMS.size();
        }

```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java

        if (!allowMultiFile) {
            if (set.size() > 1) {
                String errMsg = String.format("only 1 %s file is expected, but %d dubbo.properties files found on class path: %s",
                    fileName, set.size(), set);
```

### DataFlowIssue
Dereference of `set` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
        logger.info("load " + fileName + " properties file from " + set);

        for (java.net.URL url : set) {
            try {
                Properties p = new Properties();
```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ModuleEnvironment.java`
#### Snippet
```java
            return applicationDelegate.getDynamicConfiguration();
        }
        return Optional.ofNullable(dynamicConfiguration);
    }

```

### DataFlowIssue
Method invocation `exists` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
        }

        if (directoryPath == null || !rootDirectory.exists()) { // If the directory does not exist
            rootDirectory = new File(DEFAULT_CONFIG_CENTER_DIR_PATH);
        }
```

### DataFlowIssue
Argument `getRootDirectory().listFiles()` might be null
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    public Set<String> getConfigGroups() {
        return Stream.of(getRootDirectory().listFiles())
            .filter(File::isDirectory)
            .map(File::getName)
```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
            configCache = serviceConfigCache;
        } else {
            throw new IllegalArgumentException("Illegal type of parameter 'config' : " + config.getClass().getName());
        }

```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
            configCache = serviceConfigCache;
        } else {
            throw new IllegalArgumentException("Illegal type of parameter 'config' : " + config.getClass().getName());
        }
        configCache.remove(uniqueServiceName, config);
```

### DataFlowIssue
Method invocation `getRemoteAddress` may produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyServerHandler.java`
#### Snippet
```java

        if (logger.isInfoEnabled()) {
            logger.info("The connection of " + channel.getRemoteAddress() + " -> " + channel.getLocalAddress() + " is established.");
        }
    }
```

### DataFlowIssue
Method invocation `readableBytes` will produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyCodecAdapter.java`
#### Snippet
```java
                    message = buffer;
                } else {
                    int size = buffer.readableBytes() + input.readableBytes();
                    message = org.apache.dubbo.remoting.buffer.ChannelBuffers.dynamicBuffer(
                            size > bufferSize ? size : bufferSize);
```

### DataFlowIssue
Method invocation `getRemoteAddress` may produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyHandler.java`
#### Snippet
```java

        if (logger.isInfoEnabled()) {
            logger.info("The connection between " + channel.getRemoteAddress() + " and " + channel.getLocalAddress() + " is established");
        }
    }
```

### DataFlowIssue
Argument `is` might be null
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/DubboNativeCodeGeneratorMojo.java`
#### Snippet
```java
                    File file = new File(directory + File.separator + nativeFile);
                    if (!file.exists()) {
                        FileUtils.copyInputStreamToFile(is, file);
                        log.info("Copy native config file:" + file);
                    } else {
```

### DataFlowIssue
Method invocation `getFile` may produce `NullPointerException`
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/CodeGenerator.java`
#### Snippet
```java
    public static void main(String[] args) {
        URL r = Thread.currentThread().getContextClassLoader().getResource("");
        String targetClassPath = new File(r.getFile()).getAbsolutePath();
        String p = Paths.get(targetClassPath).getParent().getParent().toString() + File.separator + "src" + File.separator + "main" + File.separator + "java";
        execute(p, new SystemStreamLog());
```

### DataFlowIssue
Condition `throwable instanceof StatusRpcException` is redundant and can be replaced with a null check
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/TriRpcStatus.java`
#### Snippet
```java

    public static TriRpcStatus getStatus(Throwable throwable, String description) {
        if (throwable instanceof StatusRpcException) {
            return ((StatusRpcException) throwable).getStatus();
        }
```

### DataFlowIssue
Condition `executor instanceof ThreadlessExecutor` is redundant and can be replaced with a null check
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/DeadlineFuture.java`
#### Snippet
```java
        future.setExecutor(executor);
        // ThreadlessExecutor needs to hold the waiting future in case of circuit return.
        if (executor instanceof ThreadlessExecutor) {
            ((ThreadlessExecutor) executor).setWaitingFuture(future);
        }
```

### DataFlowIssue
Method invocation `addObjectAttachments` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                    Map<String, Object> selected = supportedSelector.select(invocation, RpcContext.getClientAttachment(), RpcContext.getServerAttachment());
                    if (CollectionUtils.isNotEmptyMap(selected)) {
                        ((RpcInvocation) invocation).addObjectAttachments(selected);
                    }
                }
```

### DataFlowIssue
Method invocation `addObjectAttachments` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                }
            } else {
                ((RpcInvocation) invocation).addObjectAttachments(RpcContext.getServerAttachment().getObjectAttachments());
            }
            Map<String, Object> contextAttachments = RpcContext.getClientAttachment().getObjectAttachments();
```

### DataFlowIssue
Method invocation `addObjectAttachments` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                 * a mistake in most cases (for example, through Filter to RpcContext output traceId and spanId and other information).
                 */
                ((RpcInvocation) invocation).addObjectAttachments(contextAttachments);
            }

```

### DataFlowIssue
Condition `e instanceof RpcException` is redundant and can be replaced with a null check
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/ForkingClusterInvoker.java`
#### Snippet
```java
                if (ret instanceof Throwable) {
                    Throwable e = ret instanceof CompletionException ? ((CompletionException) ret).getCause() : (Throwable) ret;
                    throw new RpcException(e instanceof RpcException ? ((RpcException) e).getCode() : RpcException.UNKNOWN_EXCEPTION,
                        "Failed to forking invoke provider " + selected + ", but no luck to perform the invocation. " +
                            "Last error is: " + e.getMessage(), e.getCause() != null ? e.getCause() : e);
```

### DataFlowIssue
Method invocation `getMessage` will produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/ForkingClusterInvoker.java`
#### Snippet
```java
                    throw new RpcException(e instanceof RpcException ? ((RpcException) e).getCode() : RpcException.UNKNOWN_EXCEPTION,
                        "Failed to forking invoke provider " + selected + ", but no luck to perform the invocation. " +
                            "Last error is: " + e.getMessage(), e.getCause() != null ? e.getCause() : e);
                }
                return (Result) ret;
```

### DataFlowIssue
Condition `throwable instanceof RpcException` is redundant and can be replaced with a null check
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/BroadcastClusterInvoker.java`
#### Snippet
```java
    private RpcException getRpcException(Throwable throwable) {
        RpcException rpcException;
        if (throwable instanceof RpcException) {
            rpcException = (RpcException) throwable;
        } else {
```

### DataFlowIssue
Method invocation `getMessage` will produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/BroadcastClusterInvoker.java`
#### Snippet
```java
            rpcException = (RpcException) throwable;
        } else {
            rpcException = new RpcException(throwable.getMessage(), throwable);
        }
        return rpcException;
```

### DataFlowIssue
Method invocation `getCode` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailoverClusterInvoker.java`
#### Snippet
```java
            }
        }
        throw new RpcException(le.getCode(), "Failed to invoke the method "
                + methodName + " in the service " + getInterface().getName()
                + ". Tried " + len + " times of the providers " + providers
```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
            try {
                result = command.apply();
                le = null;
                break;
            } catch (NacosException e) {
```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
            try {
                command.accept();
                le = null;
                break;
            } catch (NacosException e) {
```

### DataFlowIssue
Method invocation `addHeader` may produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/HttpClientRestClient.java`
#### Snippet
```java

            for (String headerValue : headerValues) {
                httpRequest.addHeader(headerName, headerValue);
            }
        }
```

### DataFlowIssue
Method invocation `setConfig` may produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/HttpClientRestClient.java`
#### Snippet
```java
        }

        httpRequest.setConfig(getRequestConfig(httpClientConfig));

        CompletableFuture<RestResult> future = new CompletableFuture<>();
```

### DataFlowIssue
Method invocation `getResult` will produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCodec.java`
#### Snippet
```java
            return CodecSupport.getSerializationById(((HeartBeatResponse) res).getProto());
        }
        if (!(res.getResult() instanceof AppResponse)) {
            return super.getSerialization(channel, res);
        }
```

### DataFlowIssue
Method invocation `getExchangeHandler` will produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ReferenceCountExchangeClient.java`
#### Snippet
```java
        // the order of judgment in the if statement cannot be changed.
        if (!(client instanceof LazyConnectExchangeClient)) {
            client = new LazyConnectExchangeClient(url, client.getExchangeHandler());
        }
    }
```

### DataFlowIssue
Argument `url` might be null
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
            // one channel can have multiple callback instances, no need to re-export for different instance.
            if (!channel.hasAttribute(cacheKey)) {
                if (!isInstancesOverLimit(channel, url, clazz.getName(), instid, false)) {
                    ModuleModel moduleModel;
                    if (inv.getServiceModel() == null) {
```

### DataFlowIssue
Method invocation `getServiceModel` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
                if (!isInstancesOverLimit(channel, url, clazz.getName(), instid, false)) {
                    ModuleModel moduleModel;
                    if (inv.getServiceModel() == null) {
                        //TODO should get scope model from url?
                        moduleModel = ApplicationModel.defaultModel().getDefaultModule();
```

### DataFlowIssue
Method invocation `hasException` will produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-injvm/src/main/java/org/apache/dubbo/rpc/protocol/injvm/InjvmInvoker.java`
#### Snippet
```java
                });
            } else {
                if (result.hasException()) {
                    rpcResult.setException(result.getException());
                } else {
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
                typedClients = buildReferenceCountExchangeClientList(url, connectNum);
            } else {
                for (int i = 0; i < typedClients.size(); i++) {
                    ReferenceCountExchangeClient referenceCountExchangeClient = typedClients.get(i);
                    // If there is a client in the list that is no longer available, create a new one to replace him.
```

### DataFlowIssue
Argument `valueMap` might be null
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/BootstrapperImpl.java`
#### Snippet
```java
                Map<String, ?> valueMap = JsonUtil.getObject(certProvidersBlob, name);
                String pluginName =
                    checkForNull(JsonUtil.getString(valueMap, "plugin_name"), "plugin_name");
                Map<String, ?> config = checkForNull(JsonUtil.getObject(valueMap, "config"), "config");
                CertificateProviderInfoImpl certificateProviderInfo =
```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/config/ZookeeperConfig.java`
#### Snippet
```java
                port1 = Integer.parseInt(portConfig1);
            } catch (NumberFormatException e) {
                port1 = DEFAULT_CLIENT_PORT_1;
            }
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/config/ZookeeperConfig.java`
#### Snippet
```java
                port2 = Integer.parseInt(portConfig2);
            } catch (NumberFormatException e) {
                port2 = DEFAULT_CLIENT_PORT_2;
            }
        }
```

### DataFlowIssue
Dereference of `parentPath.toFile().listFiles()` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            if (!Files.exists(parentPath) ||
                !parentPath.toFile().isDirectory() ||
                parentPath.toFile().listFiles().length != 1) {
                throw new IllegalStateException("There is something wrong in unpacked file!");
            }
```

### DataFlowIssue
Array access `parentPath.toFile().listFiles()[0]` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            }
            // rename directory
            File sourceFile = parentPath.toFile().listFiles()[0];
            File targetFile = Paths.get(parentPath.toString(), context.getUnpackedDirectory()).toFile();
            sourceFile.renameTo(targetFile);
```

### DataFlowIssue
Dereference of `zookeeperBin.toFile().listFiles()` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            Path zookeeperBin = Paths.get(targetFile.toString(), "bin");
            // update file permission
            for (File file : zookeeperBin.toFile().listFiles()) {
                file.setExecutable(true, false);
                file.setReadable(true, false);
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
            } finally {
                try {
                    oFile.close();
                } catch (IOException e) {
                    throw new DubboTestException("Failed to close file", e);
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
            } finally {
                try {
                    oFile.close();
                } catch (IOException e) {
                    throw new DubboTestException("Failed to close file", e);
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/TraceTelnetHandler.java`
#### Snippet
```java
        Invoker<?> invoker = null;
        for (Exporter<?> exporter : DubboProtocol.getDubboProtocol().getExporters()) {
            if (service.equals(exporter.getInvoker().getInterface().getSimpleName())
                    || service.equals(exporter.getInvoker().getInterface().getName())
                    || service.equals(exporter.getInvoker().getUrl().getPath())) {
```

### DataFlowIssue
Argument `port` might be null
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/PortTelnet.java`
#### Snippet
```java
            }
        } else {
            int p = Integer.parseInt(port);
            ProtocolServer protocolServer = null;
            for (ProtocolServer s : dubboProtocol.getServers()) {
```

### DataFlowIssue
Argument `times` might be null
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/CountTelnet.java`
#### Snippet
```java
            return "Illegal times " + times + ", must be integer.";
        }
        final int t = Integer.parseInt(times);
        Invoker<?> invoker = null;
        for (Exporter<?> exporter : dubboProtocol.getExporters()) {
```

### DataFlowIssue
Condition `metadataServiceProxy instanceof Destroyable` is redundant and can be replaced with a null check
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ReflectionBasedServiceDiscovery.java`
#### Snippet
```java
        if (metadataServiceProxies.containsKey(key)) {
            Object metadataServiceProxy = metadataServiceProxies.remove(key);
            if (metadataServiceProxy instanceof Destroyable) {
                ((Destroyable) metadataServiceProxy).$destroy();
            }
```

### DataFlowIssue
Method invocation `isDestroy` will produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
            return true;
        }
        return !serviceDiscovery.isDestroy() && !serviceDiscovery.getServices().isEmpty();
    }

```

### DataFlowIssue
Method invocation `getServiceName` will produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
            return;
        }
        String appName = event.getServiceName();
        List<ServiceInstance> appInstances = event.getServiceInstances();
        logger.info("Received instance notification, serviceName: " + appName + ", instances: " + appInstances.size());
```

### DataFlowIssue
Method invocation `getUrl` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/DynamicDirectory.java`
#### Snippet
```java
            // 1-8: Failed to unregister / unsubscribe url on destroy.
            logger.warn(REGISTRY_FAILED_DESTROY_UNREGISTER_URL, "", "",
                "unexpected error when unsubscribe service " + serviceKey + " from registry: " + registry.getUrl(), t);
        }

```

### DataFlowIssue
Method invocation `setBytes` will produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/HeapChannelBuffer.java`
#### Snippet
```java
            getBytes(index, ((HeapChannelBuffer) dst).array, dstIndex, length);
        } else {
            dst.setBytes(dstIndex, array, index, length);
        }
    }
```

### DataFlowIssue
Method invocation `getBytes` will produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/HeapChannelBuffer.java`
#### Snippet
```java
            setBytes(index, ((HeapChannelBuffer) src).array, srcIndex, length);
        } else {
            src.getBytes(srcIndex, array, index, length);
        }
    }
```

### DataFlowIssue
Method invocation `getBytes` will produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ByteBufferBackedChannelBuffer.java`
#### Snippet
```java
            setBytes(index, data);
        } else if (buffer.hasArray()) {
            src.getBytes(srcIndex, buffer.array(), index + buffer.arrayOffset(), length);
        } else {
            src.getBytes(srcIndex, this, index, length);
```

### DataFlowIssue
Method invocation `getBytes` will produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ByteBufferBackedChannelBuffer.java`
#### Snippet
```java
            src.getBytes(srcIndex, buffer.array(), index + buffer.arrayOffset(), length);
        } else {
            src.getBytes(srcIndex, this, index, length);
        }
    }
```

### DataFlowIssue
Method invocation `setBytes` will produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ByteBufferBackedChannelBuffer.java`
#### Snippet
```java
            getBytes(index, data);
        } else if (buffer.hasArray()) {
            dst.setBytes(dstIndex, buffer.array(), index + buffer.arrayOffset(), length);
        } else {
            dst.setBytes(dstIndex, this, index, length);
```

### DataFlowIssue
Method invocation `setBytes` will produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ByteBufferBackedChannelBuffer.java`
#### Snippet
```java
            dst.setBytes(dstIndex, buffer.array(), index + buffer.arrayOffset(), length);
        } else {
            dst.setBytes(dstIndex, this, index, length);
        }
    }
```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                    }
                } else {
                    size = 0;
                    buf.append("\r\nMESSAGE: log file not exists or log appender is console .");
                }
```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    public <T> void reRefer(ClusterInvoker<?> invoker, URL newSubscribeUrl) {
        if (!(invoker instanceof MigrationClusterInvoker)) {
            logger.error(REGISTRY_UNSUPPORTED_CATEGORY, "", "", "Only invoker type of MigrationClusterInvoker supports reRefer, current invoker is " + invoker.getClass());
            return;
        }
```

### DataFlowIssue
Condition `exception instanceof ExecutionException` is redundant and can be replaced with a null check
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeHandler.java`
#### Snippet
```java
    @Override
    public void caught(Channel channel, Throwable exception) throws RemotingException {
        if (exception instanceof ExecutionException) {
            ExecutionException e = (ExecutionException) exception;
            Object msg = e.getRequest();
```

### DataFlowIssue
Condition `executor instanceof ThreadlessExecutor` is redundant and can be replaced with a null check
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/DefaultFuture.java`
#### Snippet
```java
        future.setExecutor(executor);
        // ThreadlessExecutor needs to hold the waiting future in case of circuit return.
        if (executor instanceof ThreadlessExecutor) {
            ((ThreadlessExecutor) executor).setWaitingFuture(future);
        }
```

### DataFlowIssue
Condition `executor instanceof ThreadlessExecutor` is redundant and can be replaced with a null check
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/direct/DirectChannelHandler.java`
#### Snippet
```java
    public void received(Channel channel, Object message) throws RemotingException {
        ExecutorService executor = getPreferredExecutorService(message);
        if (executor instanceof ThreadlessExecutor) {
            try {
                executor.execute(new ChannelEventRunnable(channel, handler, ChannelState.RECEIVED, message));
```

### DataFlowIssue
Condition `executor instanceof ThreadlessExecutor` is redundant and can be replaced with a null check
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/execution/ExecutionChannelHandler.java`
#### Snippet
```java
                throw new ExecutionException(message, channel, getClass() + " error when process received event.", t);
            }
        } else if (executor instanceof ThreadlessExecutor) {
            executor.execute(new ChannelEventRunnable(channel, handler, ChannelState.RECEIVED, message));
        } else {
```

## RuleId[id=ParameterCanBeLocal]
### ParameterCanBeLocal
Parameter can be converted to a local variable
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/InterfaceCompatibleRegistryProtocol.java`
#### Snippet
```java

    @Override
    public <T> ClusterInvoker<T> getServiceDiscoveryInvoker(Cluster cluster, Registry registry, Class<T> type, URL url) {
        registry = getRegistry(super.getRegistryUrl(url));
        DynamicDirectory<T> directory = new ServiceDiscoveryRegistryDirectory<>(type, url);
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'invocation' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    private Invoker<?> invoker;
    @Deprecated
    private Invocation invocation;

    // now we don't use the 'values' map to hold these objects
```

### DeprecatedIsStillUsed
Deprecated member 'invoker' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    private List<Invoker<?>> invokers;
    @Deprecated
    private Invoker<?> invoker;
    @Deprecated
    private Invocation invocation;
```

### DeprecatedIsStillUsed
Deprecated member 'invokers' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java

    @Deprecated
    private List<Invoker<?>> invokers;
    @Deprecated
    private Invoker<?> invoker;
```

### DeprecatedIsStillUsed
Deprecated member 'loadMigrationRule' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     */
    @Deprecated
    private void loadMigrationRule() {
        if (Boolean.parseBoolean(System.getProperty(CommonConstants.DUBBO_MIGRATION_FILE_ENABLE, "false"))) {
            String path = System.getProperty(CommonConstants.DUBBO_MIGRATION_KEY);
```

### DeprecatedIsStillUsed
Deprecated member 'port' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/MetricsConfig.java`
#### Snippet
```java
     */
    @Deprecated
    private String port;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'parameters' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/model/MethodDefinition.java`
#### Snippet
```java
     */
    @Deprecated
    private List<TypeDefinition> parameters;

    private List<String> annotations;
```

### DeprecatedIsStillUsed
Deprecated member 'build27xCompatibleClusterInterceptors' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/AbstractCluster.java`
#### Snippet
```java

    @Deprecated
    private <T> ClusterInvoker<T> build27xCompatibleClusterInterceptors(AbstractClusterInvoker<T> clusterInvoker, AbstractClusterInvoker<T> last) {
        List<ClusterInterceptor> interceptors = ScopeModelUtil.getApplicationModel(clusterInvoker.getUrl().getScopeModel()).getExtensionLoader(ClusterInterceptor.class).getActivateExtensions();

```

### DeprecatedIsStillUsed
Deprecated member 'configureDeprecated' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/AbstractConfigurator.java`
#### Snippet
```java

    @Deprecated
    private URL configureDeprecated(URL url) {
        // If override url has port, means it is a provider address. We want to control a specific provider with this override url, it may take effect on the specific provider instance or on consumers holding this provider instance.
        if (configuratorUrl.getPort() != 0) {
```

### DeprecatedIsStillUsed
Deprecated member 'Codec' is still used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Codec.java`
#### Snippet
```java
@Deprecated
@SPI(scope = ExtensionScope.FRAMEWORK)
public interface Codec {

    /**
```

## RuleId[id=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java
            .flatMap(testIdentifier -> testPlan.getChildren(testIdentifier).stream())
            .filter(testIdentifier -> testIdentifier.getSource().isPresent())
            .filter(testIdentifier -> supportEmbeddedZookeeper(testIdentifier))
            .count() > 0;
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
                Map<String, T> dsResultMap = entry.getKey()
                    .stream()
                    .collect(Collectors.toMap(k -> k, v -> newResult.get(v)));
                entry.getValue().forEach(o -> o.accept(dsResultMap));
            }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java
                    }
                    // add shutdown hook
                    Runtime.getRuntime().addShutdownHook(new Thread(() -> shutdown()));
                    INITIALIZED.set(true);
                }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/HelpTelnetHandler.java`
#### Snippet
```java
    public String telnet(Channel channel, String message) {
        if (message.length() > 0) {
            return processedTable.computeIfAbsent(message, commandName ->  generateForOneCommand(commandName));
        } else {
            return processedTable.computeIfAbsent(MAIN_HELP, commandName ->  generateForAllCommand(channel));
```

## RuleId[id=AbstractMethodCallInConstructor]
### AbstractMethodCallInConstructor
Call to 'abstract' method `createHttpClient()` during object construction
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/BaseRestClient.java`
#### Snippet
```java
    public BaseRestClient(HttpClientConfig clientConfig) {
        this.clientConfig = clientConfig;
        client = createHttpClient(clientConfig);
    }

```

### AbstractMethodCallInConstructor
Call to 'abstract' method `doOpen()` during object construction
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractServer.java`
#### Snippet
```java
        this.accepts = url.getParameter(ACCEPTS_KEY, DEFAULT_ACCEPTS);
        try {
            doOpen();
            if (logger.isInfoEnabled()) {
                logger.info("Start " + getClass().getSimpleName() + " bind " + getBindAddress() + ", export " + getLocalAddress());
```

### AbstractMethodCallInConstructor
Call to 'abstract' method `doOpen()` during object construction
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java

        try {
            doOpen();
        } catch (Throwable t) {
            close();
```

## RuleId[id=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'arguments' in a Serializable class
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    private Class<?>[] parameterTypes;

    private Object[] arguments;

    private Map<String, String> attachments;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'arguments' in a Serializable class
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    private String[] compatibleParamSignatures;

    private Object[] arguments;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'body' in a Serializable class
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java
    private String path;
    private String address;
    private Object body;
    private byte[] byteBody = new byte[0];
    private String protocol = "http://";
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'value' in a Serializable class
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/filter/CacheFilter.java`
#### Snippet
```java
        private static final long serialVersionUID = -1777337318019193256L;

        private final Object value;

        public ValueWrapper (Object value) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'reflectMethod' in a Serializable class
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RestMethodMetadata.java`
#### Snippet
```java
    private List<ArgInfo> argInfos;

    private Method reflectMethod;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'defaultValue' in a Serializable class
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/util/CuratorFrameworkParams.java`
#### Snippet
```java
    private final String name;

    private final Object defaultValue;

    private final Function<String, Object> converter;
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            try {
                listener.wait(url.getParameter(TIMEOUT_KEY, DEFAULT_TIMEOUT));
            } catch (InterruptedException e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            try {
                Thread.sleep(100);
            } catch (Throwable e2) {
            }
            try (Socket socket2 = new Socket(url.getHost(), url.getPort())) {
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
        try {
            return multiValueMapClass.getDeclaredConstructor().newInstance();
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
        try {
            ReflectUtils.invokeAndTryCatch(multiValueMap, multiValueMapAdd, new String[]{key, value});
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
                    return o;
                }
            } catch (Exception e) {

            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            return getField(clazz, field);
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            return invoke(object, method, params);
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            return getMethod(clazz, method, paramTypes);
        } catch (Throwable e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            return findClass(classLoader, name);
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            return findClass(name, classLoader);
        } catch (Throwable e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
            declaredMethod = clazz.getDeclaredMethod(method, EMPTY_CLASS_ARRAY);
            declaredMethod.setAccessible(true);
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
            try {
                declaredMethod = getMethodByName(clazz, method);
            } catch (Exception e) {

            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/ClassPathMetadataStorage.java`
#### Snippet
```java
            try {
                return fileObject.openReader(false);
            } catch (IOException e) {
            }
            return null;
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/jackson/ObjectMapperCodec.java`
#### Snippet
```java
                mapper.registerModule(objectMapperModule);

            } catch (Throwable ex) {
            }
        }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
            try {
                serviceDiscovery.destroy();
            } catch (Throwable ignored) {
                logger.warn(CONFIG_FAILED_EXECUTE_DESTROY, "", "", ignored.getMessage(), ignored);
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
                    return new URL(path).openStream();
                }
            } catch (IOException e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
                modified = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
                        .format(new Date(file.lastModified()));
            } catch (IOException e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
                }
            }
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
                        try {
                            input.close();
                        } catch (Throwable t) {
                        }
                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
                method = type.getDeclaredMethod(methodName, parameterTypes);
            }
        } catch (NoSuchMethodException e) {
        }
        return method;
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                    .findFirst();
            return descriptor.isPresent();
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                        return null;
                    }).get();
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.warn(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.trace(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.error(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.debug(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.error(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.error(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.warn(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.debug(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.trace(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.info(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.trace(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.debug(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.info(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.warn(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.info(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                        try {
                            ExecutorUtil.shutdownNow(executor, 100);
                        } catch (Throwable ignored) {
                            // ignored
                            logger.warn(COMMON_UNEXPECTED_EXECUTORS_SHUTDOWN, "", "", ignored.getMessage(), ignored);
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                try {
                    serviceExportExecutor.shutdown();
                } catch (Throwable ignored) {
                    // ignored
                    logger.warn(COMMON_UNEXPECTED_EXECUTORS_SHUTDOWN, "", "", ignored.getMessage(), ignored);
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                try {
                    serviceReferExecutor.shutdown();
                } catch (Throwable ignored) {
                    logger.warn(COMMON_UNEXPECTED_EXECUTORS_SHUTDOWN, "", "", ignored.getMessage(), ignored);
                }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
            ClassUtils.forName(oldType);
            loadDirectoryInternal(extensionClasses, strategy, oldType);
        } catch (ClassNotFoundException classNotFoundException) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ClassSourceScanner.java`
#### Snippet
```java
                        extensionClasses.put(clazz, getClasses().get(clazz));
                    }
                } catch (Throwable t) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ClassSourceScanner.java`
#### Snippet
```java
                }
            }
        } catch (Throwable t) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/CodeGenerator.java`
#### Snippet
```java
            try {
                return Class.forName(it);
            } catch (Throwable e) {
            }
            return null;
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/URLConnectionRestClient.java`
#### Snippet
```java
                try {
                    out.close();
                } catch (IOException suppressed) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/HttpClientRestClient.java`
#### Snippet
```java
        try {
            closeableHttpClient.close();
        } catch (IOException e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
        try {
            Thread.sleep(waitMs);
        } catch (InterruptedException e) {
        }

```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReportFactory.java`
#### Snippet
```java
                try {
                    metadataReport.destroy();
                } catch (Throwable ignored) {
                    // ignored
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", ignored.getMessage(), ignored);
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
                    try {
                        Thread.sleep(1000);
                    } catch (InterruptedException e) {
                    }
                }
```

### CatchMayIgnoreException
Unexpected VM exception like 'java.lang.NullPointerException' might be ignored in a `catch` block
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            // Sending a cancellation request to the server side
            doUnregister(url);
        } catch (Exception e) {
            if (!(e instanceof SkipFailbackWrapperException)) {
                throw new IllegalStateException("Failed to unregister(re-export) " + url + " to registry " + getUrl().getAddress() + ", cause: " + e.getMessage(), e);
```

### CatchMayIgnoreException
Unexpected VM exception like 'java.lang.NullPointerException' might be ignored in a `catch` block
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            // Sending a registration request to the server side
            doRegister(url);
        } catch (Exception e) {
            if (!(e instanceof SkipFailbackWrapperException)) {
                throw new IllegalStateException("Failed to register (re-export) " + url + " to registry " + getUrl().getAddress() + ", cause: " + e.getMessage(), e);
```

## RuleId[id=AssignmentToForLoopParameter]
### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
                }
                buf[bufIdx++] = decodeHexByte(str, i + 1);
                i += 3;
            } while (i < toExcluded && str.charAt(i) == '%');
            i--;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
                i += 3;
            } while (i < toExcluded && str.charAt(i) == '%');
            i--;

            charBufIdx += decodeUtf8(buf, 0, bufIdx, charBuf, charBufIdx);
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `offset`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        Map<Integer, Integer> map = new HashMap<>((int) (KEY.size() / 0.75) + 1);
        for (int i = KEY.nextSetBit(0), offset = 0; i >= 0; i = KEY.nextSetBit(i + 1)) {
            map.put(i, VALUE[offset++]);
        }
        return map;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `offset`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
            // skip if value if less than 0
            if (array[i] > -1) {
                result[offset++] = array[i];
            }
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `offset`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        for (int i = key.nextSetBit(0), offset = 0; i >= 0; i = key.nextSetBit(i + 1)) {
            if (value.containsKey(i)) {
                VALUE[offset++] = value.get(i);
            } else {
                throw new IllegalArgumentException();
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            } else if (b == 27) { // escape
                if (i < message.length - 4 && message[i + 4] == 126) {
                    i = i + 4;
                } else if (i < message.length - 3 && message[i + 3] == 126) {
                    i = i + 3;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                    i = i + 4;
                } else if (i < message.length - 3 && message[i + 3] == 126) {
                    i = i + 3;
                } else if (i < message.length - 2) {
                    i = i + 2;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                    i = i + 3;
                } else if (i < message.length - 2) {
                    i = i + 2;
                }
            } else if (b == -1 && i < message.length - 2
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            } else if (b == -1 && i < message.length - 2
                && (message[i + 1] == -3 || message[i + 1] == -5)) { // handshake
                i = i + 2;
            } else {
                copy[index++] = message[i];
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
        if (!matcher.matches()) {
            throw new IllegalArgumentException("Map protobuf property " + fieldName + "of Type " +
                    typeName + " can't be parsed.The type name should mathch[" + MAP_PATTERN.toString() + "].");
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
        methodSignatureBuilder.append(method.getReturnType())
                .append(' ')
                .append(method.toString());

        return methodSignatureBuilder.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
            exportFuture.get();
        } catch (Throwable e) {
            logger.warn(CONFIG_FAILED_EXPORT_SERVICE, "","",getIdentifier() + " export services occurred an exception: " + e.toString());
        } finally {
            logger.info(getIdentifier() + " export services finished.");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
            referFuture.get();
        } catch (Throwable e) {
            logger.warn(CONFIG_FAILED_REFER_SERVICE, "", "", getIdentifier() + " refer services occurred an exception: " + e.toString());
        } finally {
            logger.info(getIdentifier() + " refer services finished.");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            while (c.isArray());

            return c.getName() + sb.toString();
        }
        return c.getName();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                        createAdaptiveInstanceError = t;
                        throw new IllegalStateException(
                            "Failed to create adaptive instance: " + t.toString(), t);
                    }
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
        // check if it's unpacked.
        if (targetPath.toFile() != null && targetPath.toFile().isDirectory()) {
            logger.info(String.format("The file has been unpacked, target path:%s", targetPath.toString()));
            return;
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            if (!Files.exists(targetFile.toPath()) || !targetFile.isDirectory()) {
                throw new IllegalStateException(String.format("Failed to rename the directory. source directory: %s, target directory: %s",
                    sourceFile.toPath().toString(),
                    targetFile.toPath().toString()));
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
                throw new IllegalStateException(String.format("Failed to rename the directory. source directory: %s, target directory: %s",
                    sourceFile.toPath().toString(),
                    targetFile.toPath().toString()));
            }
            // get the bin path
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/DownloadZookeeperInitializer.java`
#### Snippet
```java
            logger.info("It is beginning to download the zookeeper binary archive, it will take several minutes..." +
                "\nThe zookeeper binary archive file will be download from " + zookeeperBinaryUrl + "," +
                "\nwhich will be saved in " + temporaryFilePath.toString() + "," +
                "\nalso it will be renamed to 'apache-zookeeper-bin.tar.gz' and moved into " + context.getSourceFile() + ".\n");
            this.download(zookeeperBinaryUrl, temporaryFilePath);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
        String protocolServiceKey = getProtocolServiceKey();
        if (StringUtils.isNotEmpty(protocolServiceKey)) {
            return instance.toString() + ", " + metadataInfo.getServiceString(protocolServiceKey);
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
                    new IllegalStateException(
                        "urls to invokers error. invokerUrls.size :" +
                            invokerUrls.size() + ", invoker.size :0. urls :" + invokerUrls.toString()));

                return;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java

                    if (rowbuf.length() >= totalWidth) {
                        buf.append(rowbuf.toString());
                        rowbuf = new StringBuilder();
//                        for(int m = 0;m < maxcountbefore && maxcountbefore < totalWidth ; m++){
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
'List' may not contain objects of type ''
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/CompositeConfiguration.java`
#### Snippet
```java
    public CompositeConfiguration(Configuration... configurations) {
        if (ArrayUtils.isNotEmpty(configurations)) {
            Arrays.stream(configurations).filter(config -> !configList.contains(config)).forEach(configList::add);
        }
    }
```

### SuspiciousMethodCalls
'Set\>' may not contain objects of type 'TripleInvoker'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleInvoker.java`
#### Snippet
```java
                super.destroy();
                if (invokers != null) {
                    invokers.remove(this);
                }
                try {
```

### SuspiciousMethodCalls
'Map\>' may not contain values of type 'DubboExporter'
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboExporter.java`
#### Snippet
```java
    @Override
    public void afterUnExport() {
        exporterMap.remove(key, this);
    }

```

### SuspiciousMethodCalls
'Set\>' may not contain objects of type 'DubboInvoker'
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboInvoker.java`
#### Snippet
```java
                super.destroy();
                if (invokers != null) {
                    invokers.remove(this);
                }
                for (ExchangeClient client : clients) {
```

### SuspiciousMethodCalls
'Map' may not contain keys of type ''
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscovery.java`
#### Snippet
```java
        }
        listener.getServiceNames().forEach(serviceName -> {
            ZookeeperServiceDiscoveryChangeWatcher watcher = watcherCaches.get(serviceName);
            if (watcher != null) {
                watcher.getListeners().remove(listener);
```

### SuspiciousMethodCalls
'Map' may not contain keys of type ''
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscovery.java`
#### Snippet
```java
                watcher.getListeners().remove(listener);
                if (watcher.getListeners().isEmpty()) {
                    watcherCaches.remove(serviceName);
                    try {
                        watcher.getCacheInstance().close();
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `ServiceContext` may be 'static'
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
     * Template class for proto Service objects.
     */
    private class ServiceContext {

        // CHECKSTYLE DISABLE VisibilityModifier FOR 8 LINES
```

### InnerClassMayBeStatic
Inner class `RemotingServerAdapter` may be 'static'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    }

    protected abstract class RemotingServerAdapter implements RemotingServer {

        public abstract Object getDelegateServer();
```

### InnerClassMayBeStatic
Inner class `ProxyProtocolServer` may be 'static'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    protected abstract <T> Runnable doExport(T impl, Class<T> type, URL url) throws RpcException;

    protected class ProxyProtocolServer implements ProtocolServer {

        private RemotingServer server;
```

### InnerClassMayBeStatic
Inner class `ApolloListener` may be 'static'
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java
    }

    public class ApolloListener implements ConfigChangeListener {

        private Set<ConfigurationListener> listeners = new CopyOnWriteArraySet<>();
```

### InnerClassMayBeStatic
Inner class `NacosConfigListener` may be 'static'
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
    }

    public class NacosConfigListener extends AbstractSharedListener {

        private Set<ConfigurationListener> listeners = new CopyOnWriteArraySet<>();
```

### InnerClassMayBeStatic
Inner class `Border` may be 'static'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/textui/TTable.java`
#### Snippet
```java
     * border style
     */
    public class Border {

        private int borders = BORDER_OUTER | BORDER_INNER;
```

### InnerClassMayBeStatic
Inner class `NacosConfigListener` may be 'static'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
    }

    public class NacosConfigListener extends AbstractSharedListener {

        private Set<ConfigurationListener> listeners = new CopyOnWriteArraySet<>();
```

### InnerClassMayBeStatic
Inner class `ReferenceConfigurationListener` may be 'static'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
    }

    private class ReferenceConfigurationListener extends AbstractConfiguratorListener {
        private final ServiceDiscoveryRegistryDirectory<?> directory;
        private final URL url;
```

## RuleId[id=NestedSynchronizedStatement]
### NestedSynchronizedStatement
Nested `synchronized` statement
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
            // for double check
            if (referenceCountedClient.isDestroyed()) {
                synchronized (this) {
                    referenceCountedClient = clients.get(url.getAddress());
                    if (referenceCountedClient.isDestroyed()) {
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java

            // remove from allInstances and reset default FrameworkModel
            synchronized (globalLock) {
                allInstances.remove(this);
                resetDefaultFrameworkModel();
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
        super(null, ExtensionScope.FRAMEWORK, false);
        synchronized (globalLock) {
            synchronized (instLock) {
                this.setInternalId(String.valueOf(index.getAndIncrement()));
                // register FrameworkModel instance early
```

## RuleId[id=StringEqualsEmptyString]
### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
            List<String> result = new ArrayList();
            int pos;
            if ("".equals(delimiter)) {
                for (pos = 0; pos < str.length(); ++pos) {
                    result.add(deleteAny(str.substring(pos, pos + 1), charsToDelete));
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanDescriptor.java`
#### Snippet
```java

    private boolean isEmpty(String string) {
        return string == null || "".equals(string.trim());
    }
}
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/rule/virtualservice/match/StringMatch.java`
#### Snippet
```java
            return input.equals(getWildcard()) || ANY_VALUE.equals(getWildcard());
        } else if (getEmpty() != null) {
            return input == null || "".equals(input);
        } else if (getNoempty() != null) {
            return input != null && input.length() > 0;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
    public boolean isCacheExistResource(Set<String> resourceNames) {
        for (String resourceName : resourceNames) {
            if ("".equals(resourceName)) {
                continue;
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/FindPidWindowsProcessor.java`
#### Snippet
```java
                    for (int i = 0; i < values.length; i++) {
                        List<String> segments = Arrays.stream(values[i].trim().split(" "))
                            .filter(str -> !"".equals(str))
                            .collect(Collectors.toList());
                        // segments sample:
```

## RuleId[id=UnnecessaryBoxing]
### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/AwaitingNonWebApplicationListener.java`
#### Snippet
```java
    private final AtomicBoolean awaited = new AtomicBoolean(false);

    private static final Integer UNDEFINED_ID = Integer.valueOf(-1);

    /**
```

### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static Double boxed(double v) {
        return Double.valueOf(v);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static Character boxed(char v) {
        return Character.valueOf(v);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static Float boxed(float v) {
        return Float.valueOf(v);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static Short boxed(short v) {
        return Short.valueOf(v);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static Integer boxed(int v) {
        return Integer.valueOf(v);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static Boolean boxed(boolean v) {
        return Boolean.valueOf(v);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static Long boxed(long v) {
        return Long.valueOf(v);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static Byte boxed(byte v) {
        return Byte.valueOf(v);
    }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java
        PACKAGE_NAME.add("org.apache.dubbo.metadata.store.zookeeper");

        enableEmbeddedZookeeper = Boolean.valueOf(System.getProperty(CONFIG_ENABLE_EMBEDDED_ZOOKEEPER, "true"));
    }

```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/FindPidWindowsProcessor.java`
#### Snippet
```java
                        if (segments != null && segments.size() == 5) {
                            if (this.check(segments.get(1), clientPort)) {
                                int pid = Integer.valueOf(segments.get(segments.size() - 1).trim());
                                context.register(clientPort, pid);
                                return;
```

## RuleId[id=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `mdInst`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MD5Utils.java`
#### Snippet
```java
        byte[] md5;
        // MessageDigest instance is NOT thread-safe
        synchronized (mdInst) {
            mdInst.update(input.getBytes(UTF_8));
            md5 = mdInst.digest();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `this.validInvokers`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
    private boolean addValidInvoker(Invoker<T> invoker) {
        publishMetricsEvent(new RegistryEvent.MetricsDirectoryEvent(applicationModel, RegistryEvent.Type.D_VALID));
        synchronized (this.validInvokers) {
            return this.validInvokers.add(invoker);
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `this.validInvokers`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
    private boolean removeValidInvoker(Invoker<T> invoker) {
        publishMetricsEvent(new RegistryEvent.MetricsDirectoryEvent(applicationModel, RegistryEvent.Type.D_UN_VALID));
        synchronized (this.validInvokers) {
            return this.validInvokers.remove(invoker);
        }
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'instance instanceof ScopeModelAware' covered by subsequent condition 'instance instanceof ExtensionAccessorAware'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                    continue;
                }
                if (instance instanceof ScopeModelAware || instance instanceof ExtensionAccessorAware) {
                    if (ignoredInjectMethodsDesc.contains(ReflectUtils.getDesc(method))) {
                        continue;
```

### ConditionCoveredByFurtherCondition
Condition 'config instanceof ReferenceConfigBase' covered by subsequent condition 'config instanceof ServiceConfigBase'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
        // check duplicated configs
        // special check service and reference config by unique service name, speed up the processing of large number of instances
        if (config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase) {
            C existedConfig = (C) findDuplicatedInterfaceConfig((AbstractInterfaceConfig) config);
            if (existedConfig != null) {
```

### ConditionCoveredByFurtherCondition
Condition 'config instanceof ReferenceConfigBase' covered by subsequent condition 'config instanceof ServiceConfigBase'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
    protected <C extends AbstractConfig> boolean removeIfAbsent(C config, Map<String, C> configsMap) {
        if (super.removeIfAbsent(config, configsMap)) {
            if (config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase) {
                removeInterfaceConfig((AbstractInterfaceConfig) config);
            }
```

### ConditionCoveredByFurtherCondition
Condition 'config instanceof ReferenceConfigBase' covered by subsequent condition 'config instanceof ServiceConfigBase'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

        // fast check duplicated equivalent config before write lock
        if (!(config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase)) {
            for (AbstractConfig value : configsMap.values()) {
                if (value.equals(config)) {
```

### ConditionCoveredByFurtherCondition
Condition 't != null' covered by subsequent condition 't instanceof RpcException'
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/filter/MetricsClusterFilter.java`
#### Snippet
```java
            return;
        }
        if (t != null && t instanceof RpcException) {
            RpcException e = (RpcException) t;
            if (e.isForbidden()) {
```

### ConditionCoveredByFurtherCondition
Condition 'message instanceof Request' covered by subsequent condition 'message instanceof Response'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeChannel.java`
#### Snippet
```java
            throw new RemotingException(this.getLocalAddress(), null, "Failed to send message " + message + ", cause: The channel " + this + " is closed!");
        }
        if (message instanceof Request
            || message instanceof Response
            || message instanceof String) {
```

## RuleId[id=MissingDeprecatedAnnotation]
### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
 * @deprecated
 */
public class ServiceBeanIdConflictProcessor implements MergedBeanDefinitionPostProcessor, DisposableBean, PriorityOrdered {

    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
     * @deprecated as 2.7.6, use {@link Class#isPrimitive()} plus {@link #isSimpleType(Class)} instead
     */
    public static boolean isPrimitive(Class<?> type) {
        return type != null && (type.isPrimitive() || isSimpleType(type));
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
 * @deprecated Replace to <code>ClassUtils</code>
 */
public class ClassHelper {
    public static Class<?> forNameWithThreadContextClassLoader(String name) throws ClassNotFoundException {
        return ClassUtils.forName(name, Thread.currentThread().getContextClassLoader());
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
     * @deprecated Replace to <code>MethodUtils#isSetter(Method)</code>
     */
    public static boolean isSetter(Method method) {
        return MethodUtils.isSetter(method);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
     * @deprecated Replace to <code>MethodUtils#isGetter(Method)</code>
     */
    public static boolean isGetter(Method method) {
        return MethodUtils.isGetter(method);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConsumerConfig.java`
#### Snippet
```java
     * @see ModuleConfig#setBackground(Boolean)
     */
    private Boolean referBackground;

    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConfigCenterConfig.java`
#### Snippet
```java
     * @deprecated no longer used
     */
    private Boolean highestPriority;

    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
     * @deprecated
     */
    public void setInterfaceClass(Class<?> interfaceClass) {
        setInterface(interfaceClass);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/config/ProviderConfig.java`
#### Snippet
```java
     * @see ModuleConfig#setBackground(Boolean)
     */
    private Boolean exportBackground;

    public ProviderConfig() {
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
     * @deprecated
     */
    String local() default "";

    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/Service.java`
#### Snippet
```java
     * @deprecated
     */
    String local() default "";

    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
     * @deprecated only for uts
     */
    protected XdsRouter(URL url, RdsRouteRuleManager rdsRouteRuleManager, EdsEndpointManager edsEndpointManager, boolean isEnable) {
        super(url);
        this.isEnable = isEnable;
```

## RuleId[id=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `io.micrometer.tracing` is unnecessary and can be removed
in `dubbo-spring-boot/dubbo-spring-boot-observability-starter/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/brave/BraveAutoConfiguration.java`
#### Snippet
```java

    @Bean
    @ConditionalOnMissingBean(io.micrometer.tracing.Tracer.class)
    BraveTracer braveTracerBridge(brave.Tracer tracer, CurrentTraceContext currentTraceContext) {
        return new BraveTracer(tracer, new BraveCurrentTraceContext(currentTraceContext), BRAVE_BAGGAGE_MANAGER);
```

### UnnecessaryFullyQualifiedName
Qualifier `com.ctrip.framework.apollo.model` is unnecessary, and can be replaced with an import
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java

        @Override
        public void onChange(com.ctrip.framework.apollo.model.ConfigChangeEvent changeEvent) {
            for (String key : changeEvent.changedKeys()) {
                ConfigChange change = changeEvent.getChange(key);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.beans.factory.config` is unnecessary and can be removed
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
     * @param registry
     * @see DubboInfraBeanRegisterPostProcessor
     * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
     */
    static void registerPlaceholderConfigurerBeanIfNotExists(ConfigurableListableBeanFactory beanFactory, BeanDefinitionRegistry registry) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.beans.factory.config` is unnecessary and can be removed
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboInfraBeanRegisterPostProcessor.java`
#### Snippet
```java
 *
 * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(
 *org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
 */
public class DubboInfraBeanRegisterPostProcessor implements BeanDefinitionRegistryPostProcessor, ApplicationContextAware {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.context` is unnecessary and can be removed
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStatedEvent.java`
#### Snippet
```java

/**
 * A {@link org.springframework.context.ApplicationEvent} after {@link org.apache.dubbo.config.bootstrap.DubboBootstrap#start()} success
 *
 * @see org.springframework.context.ApplicationEvent
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.context` is unnecessary and can be removed
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStatedEvent.java`
#### Snippet
```java
 * A {@link org.springframework.context.ApplicationEvent} after {@link org.apache.dubbo.config.bootstrap.DubboBootstrap#start()} success
 *
 * @see org.springframework.context.ApplicationEvent
 * @see org.springframework.context.ApplicationListener
 * @see org.apache.dubbo.config.bootstrap.DubboBootstrap
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.context` is unnecessary and can be removed
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStopedEvent.java`
#### Snippet
```java

/**
 * A {@link org.springframework.context.ApplicationEvent} after {@link org.apache.dubbo.config.bootstrap.DubboBootstrap#stop()} success
 *
 * @see org.springframework.context.ApplicationEvent
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.context` is unnecessary and can be removed
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStopedEvent.java`
#### Snippet
```java
 * A {@link org.springframework.context.ApplicationEvent} after {@link org.apache.dubbo.config.bootstrap.DubboBootstrap#stop()} success
 *
 * @see org.springframework.context.ApplicationEvent
 * @see org.springframework.context.ApplicationListener
 * @see org.apache.dubbo.config.bootstrap.DubboBootstrap
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.core.annotation` is unnecessary, and can be replaced with an import
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
                ClassUtils.isPresent("org.springframework.core.annotation.AnnotatedElementUtils",
                    Thread.currentThread().getContextClassLoader()) &&
                ReflectUtils.hasMethod(org.springframework.core.annotation.AnnotatedElementUtils.class, "findMergedAnnotation") ?
                    org.springframework.core.annotation.AnnotatedElementUtils.findMergedAnnotation(beanClass, annotationType) :
                    org.apache.dubbo.common.utils.AnnotationUtils.findAnnotation(beanClass, annotationType))
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.core.annotation` is unnecessary, and can be replaced with an import
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
                    Thread.currentThread().getContextClassLoader()) &&
                ReflectUtils.hasMethod(org.springframework.core.annotation.AnnotatedElementUtils.class, "findMergedAnnotation") ?
                    org.springframework.core.annotation.AnnotatedElementUtils.findMergedAnnotation(beanClass, annotationType) :
                    org.apache.dubbo.common.utils.AnnotationUtils.findAnnotation(beanClass, annotationType))
                .filter(Objects::nonNull)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.log4j` is unnecessary, and can be replaced with an import
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
    public LogPageHandler() {
        try {
            org.apache.log4j.Logger logger = LogManager.getRootLogger();
            if (logger != null) {
                Enumeration<Appender> appenders = logger.getAllAppenders();
```

### UnnecessaryFullyQualifiedName
Qualifier `com.alibaba.fastjson2` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/FastJson2Impl.java`
#### Snippet
```java
    @Override
    public <T> T toJavaObject(String json, Type type) {
        return com.alibaba.fastjson2.JSON.parseObject(json, type);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `com.alibaba.fastjson2` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/FastJson2Impl.java`
#### Snippet
```java
    @Override
    public <T> List<T> toJavaList(String json, Class<T> clazz) {
        return com.alibaba.fastjson2.JSON.parseArray(json, clazz);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `com.alibaba.fastjson2` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/FastJson2Impl.java`
#### Snippet
```java
    @Override
    public String toJson(Object obj) {
        return com.alibaba.fastjson2.JSON.toJSONString(obj, JSONWriter.Feature.WriteEnumsUsingName);
    }
}
```

### UnnecessaryFullyQualifiedName
Qualifier `com.fasterxml.jackson.core` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/JacksonImpl.java`
#### Snippet
```java
        try {
            return getJackson().writeValueAsString(obj);
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `com.fasterxml.jackson.core` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/JacksonImpl.java`
#### Snippet
```java
        try {
            return getJackson().readValue(json, getJackson().getTypeFactory().constructType(type));
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `com.fasterxml.jackson.core` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/JacksonImpl.java`
#### Snippet
```java
        try {
            return getJackson().readValue(json, getJackson().getTypeFactory().constructCollectionType(List.class, clazz));
        } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
            throw new IllegalArgumentException(e);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `com.alibaba.fastjson` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/FastJsonImpl.java`
#### Snippet
```java
    @Override
    public <T> List<T> toJavaList(String json, Class<T> clazz) {
        return com.alibaba.fastjson.JSON.parseArray(json, clazz);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `com.alibaba.fastjson` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/FastJsonImpl.java`
#### Snippet
```java
    @Override
    public <T> T toJavaObject(String json, Type type) {
        return com.alibaba.fastjson.JSON.parseObject(json, type);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `com.alibaba.fastjson` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/impl/FastJsonImpl.java`
#### Snippet
```java
    @Override
    public String toJson(Object obj) {
        return com.alibaba.fastjson.JSON.toJSONString(obj, SerializerFeature.DisableCircularReferenceDetect);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java
     * @param maxPendingTimeouts The maximum number of pending timeouts after which call to
     *                           {@code newTimeout} will result in
     *                           {@link java.util.concurrent.RejectedExecutionException}
     *                           being thrown. No maximum pending timeouts limit is assumed if
     *                           this value is 0 or negative.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
     *
     * @return the default ClassLoader (never <code>null</code>)
     * @see java.lang.Thread#getContextClassLoader()
     */
    public static ClassLoader getClassLoader() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LRU2Cache.java`
#### Snippet
```java

    @Override
    protected boolean removeEldestEntry(java.util.Map.Entry<K, V> eldest) {
        return size() > maxCapacity;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LRU2Cache.java`
#### Snippet
```java

        @Override
        protected boolean removeEldestEntry(java.util.Map.Entry<K, V> eldest) {
            return size() > maxCapacity;
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
     *
     * @return the default ClassLoader (never <code>null</code>)
     * @see java.lang.Thread#getContextClassLoader()
     */
    public static ClassLoader getClassLoader() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LRUCache.java`
#### Snippet
```java

    @Override
    protected boolean removeEldestEntry(java.util.Map.Entry<K, V> eldest) {
        return size() > maxCapacity;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
        }

        Set<java.net.URL> set = null;
        try {
            List<ClassLoader> classLoadersToLoad = new LinkedList<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
        logger.info("load " + fileName + " properties file from " + set);

        for (java.net.URL url : set) {
            try {
                Properties p = new Properties();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
                return Boolean.valueOf(string);
            }
            if (type == Date.class || type == java.sql.Date.class || type == java.sql.Timestamp.class
                    || type == java.sql.Time.class) {
                try {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
            }
            if (type == Date.class || type == java.sql.Date.class || type == java.sql.Timestamp.class
                    || type == java.sql.Time.class) {
                try {
                    Date date = new SimpleDateFormat(DATE_FORMAT).parse(string);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
                        return new java.sql.Date(date.getTime());
                    }
                    if (type == java.sql.Timestamp.class) {
                        return new java.sql.Timestamp(date.getTime());
                    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
                    }
                    if (type == java.sql.Timestamp.class) {
                        return new java.sql.Timestamp(date.getTime());
                    }
                    if (type == java.sql.Time.class) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
                        return new java.sql.Timestamp(date.getTime());
                    }
                    if (type == java.sql.Time.class) {
                        return new java.sql.Time(date.getTime());
                    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
                    }
                    if (type == java.sql.Time.class) {
                        return new java.sql.Time(date.getTime());
                    }
                    return date;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
                }
            }
            if (type == java.time.LocalDateTime.class) {
                if (StringUtils.isEmpty(string)) {
                    return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
                return LocalDateTime.parse(string);
            }
            if (type == java.time.LocalDate.class) {
                if (StringUtils.isEmpty(string)) {
                    return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CompatibleTypeUtils.java`
#### Snippet
```java
                return LocalDate.parse(string);
            }
            if (type == java.time.LocalTime.class) {
                if (StringUtils.isEmpty(string)) {
                    return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConcurrentHashSet.java`
#### Snippet
```java
import java.util.concurrent.ConcurrentMap;

public class ConcurrentHashSet<E> extends AbstractSet<E> implements Set<E>, java.io.Serializable {

    private static final long serialVersionUID = -8672117787651310382L;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java

    /**
     * Check if the method is a deprecated method. The standard is whether the {@link java.lang.Deprecated} annotation is declared on the class.
     * Return {@code true} if this annotation is present.
     * Otherwise, return {@code false}.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/slf4j/Slf4jLoggerAdapter.java`
#### Snippet
```java
    @Override
    public Logger getLogger(String key) {
        return new Slf4jLogger(org.slf4j.LoggerFactory.getLogger(key));
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/slf4j/Slf4jLoggerAdapter.java`
#### Snippet
```java
    @Override
    public Logger getLogger(Class<?> key) {
        return new Slf4jLogger(org.slf4j.LoggerFactory.getLogger(key));
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * SecurityManager (if active).
     * @param method the method to make accessible
     * @see java.lang.reflect.Method#setAccessible
     */
    @SuppressWarnings("deprecation")  // on JDK 9
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/MemorySafeLinkedBlockingQueue.java`
#### Snippet
```java

/**
 * Can completely solve the OOM problem caused by {@link java.util.concurrent.LinkedBlockingQueue},
 * does not depend on {@link java.lang.instrument.Instrumentation} and is easier to use than
 * {@link MemoryLimitedLinkedBlockingQueue}.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/MemoryLimitCalculator.java`
#### Snippet
```java

/**
 * {@link java.lang.Runtime#freeMemory()} technology is used to calculate the
 * memory limit by using the percentage of the current maximum available memory,
 * which can be used with {@link MemoryLimiter}.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/MemoryLimitedLinkedBlockingQueue.java`
#### Snippet
```java

/**
 * Can completely solve the OOM problem caused by {@link java.util.concurrent.LinkedBlockingQueue}.
 */
public class MemoryLimitedLinkedBlockingQueue<E> extends LinkedBlockingQueue<E> {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
     * @see FrameworkModel#getExtensionDirector()
     * @see ModuleModel#getExtensionDirector()
     * @see ExtensionDirector#getExtensionLoader(java.lang.Class)
     * @deprecated get extension loader from extension director of some module.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosConfigServiceWrapper.java`
#### Snippet
```java

    /**
     * see {@link com.alibaba.nacos.client.config.utils.ParamUtils#isValid(java.lang.String)}
     */
    private String handleInnerSymbol(String data) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jboss.netty.channel` is unnecessary, and can be replaced with an import
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
    private static final ConcurrentMap<org.jboss.netty.channel.Channel, NettyChannel> CHANNEL_MAP = new ConcurrentHashMap<org.jboss.netty.channel.Channel, NettyChannel>();

    private final org.jboss.netty.channel.Channel channel;

    private final Map<String, Object> attributes = new ConcurrentHashMap<String, Object>();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jboss.netty.channel` is unnecessary, and can be replaced with an import
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
    }

    static NettyChannel getOrAddChannel(org.jboss.netty.channel.Channel ch, URL url, ChannelHandler handler) {
        if (ch == null) {
            return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jboss.netty.channel` is unnecessary, and can be replaced with an import
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
    }

    static void removeChannelIfDisconnected(org.jboss.netty.channel.Channel ch) {
        if (ch != null && !ch.isConnected()) {
            CHANNEL_MAP.remove(ch);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jboss.netty.channel` is unnecessary, and can be replaced with an import
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
    private final Map<String, Object> attributes = new ConcurrentHashMap<String, Object>();

    private NettyChannel(org.jboss.netty.channel.Channel channel, URL url, ChannelHandler handler) {
        super(url, handler);
        if (channel == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jboss.netty.channel` is unnecessary, and can be replaced with an import
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(NettyChannel.class);

    private static final ConcurrentMap<org.jboss.netty.channel.Channel, NettyChannel> CHANNEL_MAP = new ConcurrentHashMap<org.jboss.netty.channel.Channel, NettyChannel>();

    private final org.jboss.netty.channel.Channel channel;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jboss.netty.channel` is unnecessary, and can be replaced with an import
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(NettyChannel.class);

    private static final ConcurrentMap<org.jboss.netty.channel.Channel, NettyChannel> CHANNEL_MAP = new ConcurrentHashMap<org.jboss.netty.channel.Channel, NettyChannel>();

    private final org.jboss.netty.channel.Channel channel;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.dubbo.common.status` is unnecessary, and can be replaced with an import
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/health/DubboHealthIndicator.java`
#### Snippet
```java
            StatusChecker checker = extensionLoader.getExtension(statusCheckerName);

            org.apache.dubbo.common.status.Status status = checker.check();

            org.apache.dubbo.common.status.Status.Level level = status.getLevel();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.dubbo.common.status` is unnecessary, and can be replaced with an import
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/health/DubboHealthIndicator.java`
#### Snippet
```java
            org.apache.dubbo.common.status.Status status = checker.check();

            org.apache.dubbo.common.status.Status.Level level = status.getLevel();

            if (!hasError && level.equals(org.apache.dubbo.common.status.Status.Level.ERROR)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.dubbo.common.status` is unnecessary, and can be replaced with an import
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/health/DubboHealthIndicator.java`
#### Snippet
```java
            org.apache.dubbo.common.status.Status.Level level = status.getLevel();

            if (!hasError && level.equals(org.apache.dubbo.common.status.Status.Level.ERROR)) {
                hasError = true;
                builder.down();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.dubbo.common.status` is unnecessary, and can be replaced with an import
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/health/DubboHealthIndicator.java`
#### Snippet
```java
            }

            if (!hasError && !hasUnknown && level.equals(org.apache.dubbo.common.status.Status.Level.UNKNOWN)) {
                hasUnknown = true;
                builder.unknown();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosConfigServiceWrapper.java`
#### Snippet
```java

    /**
     * see {@link com.alibaba.nacos.client.config.utils.ParamUtils#isValid(java.lang.String)}
     */
    private String handleInnerSymbol(String data) {
```

## RuleId[id=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/DemoServiceImpl.java`
#### Snippet
```java
            Thread.sleep(1000);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        return "Hello " + name + ", response from provider: " + RpcContext.getServiceContext().getLocalAddress();
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                    System.out.println(form2);
                } catch (Exception e) {
                    e.printStackTrace();
                }
                try {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-generic-call/src/main/java/org/apache/dubbo/demo/consumer/GenericApplication.java`
#### Snippet
```java
                Thread.sleep(1000);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiConsumer.java`
#### Snippet
```java
            System.out.println("Async Reply: "+sayHelloAsync.get());
        } catch (Throwable t) {
            t.printStackTrace();
        }
        System.in.read();
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
```

## RuleId[id=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java
    private void put(OS os, Command command, Processor processor) {
        Map<Command, Processor> commandProcessorMap = this.processors.get(os);
        if (commandProcessorMap == null) {
            commandProcessorMap = new HashMap<>();
            this.processors.put(os, commandProcessorMap);
```

## RuleId[id=Convert2Lambda]
### Convert2Lambda
Anonymous new Callable() can be replaced with lambda
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
                //local invoke will return directly
                if (o != null) {
                    FutureTask<T> f = new FutureTask<T>(new Callable<T>() {
                        @Override
                        public T call() throws Exception {
```

### Convert2Lambda
Anonymous new BiConsumer() can be replaced with lambda
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java

    void setCallback(ResponseCallback callback) {
        BiConsumer<Object, ? super Throwable> biConsumer = new BiConsumer<Object, Throwable>() {

            @Override
```

### Convert2Lambda
Anonymous new Comparator\>() can be replaced with lambda
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/Help.java`
#### Snippet
```java
        final List<Class<?>> classes = commandHelper.getAllCommandClass();

        Collections.sort(classes, new Comparator<Class<?>>() {

            @Override
```

### Convert2Lambda
Anonymous new Consumer\>() can be replaced with lambda
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/MetricsCountSampleConfigurer.java`
#### Snippet
```java
    public Long rt;

    public Consumer<MetricsCountSampleConfigurer<S,K,M>> fireEventHandler = new Consumer<MetricsCountSampleConfigurer<S, K,M>>() {
        @Override
        public void accept(MetricsCountSampleConfigurer<S, K,M> skMetricsCountSampleConfigure) {
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
            tmp = findClassAndTryCatch(s, classLoader);
            if (tmp == null) {
                continue;
            } else {
                return tmp;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        for (int i = 0; i < mask.length; i++) {
            if ("*".equals(mask[i]) || mask[i].equals(ipAddress[i])) {
                continue;
            } else if (mask[i].contains("-")) {
                String[] rangeNumStrs = StringUtils.split(mask[i], '-');
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
                }
            } else if ("0".equals(ipAddress[i]) && ("0".equals(mask[i]) || "00".equals(mask[i]) || "000".equals(mask[i]) || "0000".equals(mask[i]))) {
                continue;
            } else if (!mask[i].equals(ipAddress[i])) {
                return false;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathMatcher.java`
#### Snippet
```java
            boolean equals = split[i].equals(pathSplits[i]);
            if (equals) {
                continue;
            } else {
                if (placeHoldCompare(pathSplits[i])) {
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathMatcher.java`
#### Snippet
```java
            } else {
                if (placeHoldCompare(pathSplits[i])) {
                    continue;
                } else {
                    return false;
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy field 'rawAddress'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
    }

    public DefaultServiceInstance(DefaultServiceInstance other) {
        this.serviceName = other.serviceName;
        this.host = other.host;
```

## RuleId[id=RegExpUnnecessaryNonCapturingGroup]
### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:((?:[_$a-zA-Z][_$a-zA-Z0-9]*))?\\(((?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))*)\\)((?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))))?)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";

    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";

    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";

    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Class<?>[] EMPTY_CLASS_ARRAY = new Class<?>[0];

    public static final String JAVA_IDENT_REGEX = "(?:[_$a-zA-Z][_$a-zA-Z0-9]*)";

    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/Parameters.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj) {
        if (this == obj) {
            return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java

        @Override
        public boolean equals(Object obj) {
            if (this == obj) {
                return true;
```

## RuleId[id=ThreadStartInConstruction]
### ThreadStartInConstruction
Call to `start()` during object construction
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            }, "DubboMulticastRegistryReceiver");
            thread.setDaemon(true);
            thread.start();
        } catch (IOException e) {
            throw new IllegalStateException(e.getMessage(), e);
```

## RuleId[id=NumericOverflow]
### NumericOverflow
Numeric overflow in expression
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
    @Override
    public int getOrder() {
        return LOWEST_PRECEDENCE + 1;
    }

```

### NumericOverflow
Numeric overflow in expression
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/DubboConfigDefaultPropertyValueBeanPostProcessor.java`
#### Snippet
```java
    @Override
    public int getOrder() {
        return Ordered.LOWEST_PRECEDENCE + 1;
    }
}
```

