# tika 
 
# Bad smells
I found 4026 bad smells with 518 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| RedundantFieldInitialization | 511 | false |
| ReturnNull | 327 | false |
| UnusedAssignment | 296 | false |
| SizeReplaceableByIsEmpty | 271 | true |
| MissortedModifiers | 229 | false |
| AssignmentToMethodParameter | 213 | false |
| BoundedWildcard | 200 | false |
| UnnecessaryFullyQualifiedName | 171 | false |
| SystemOutErr | 149 | false |
| ConstantValue | 138 | false |
| UtilityClassWithoutPrivateConstructor | 87 | true |
| DataFlowIssue | 87 | false |
| NestedAssignment | 78 | false |
| ZeroLengthArrayInitialization | 75 | false |
| DynamicRegexReplaceableByCompiledPattern | 72 | false |
| CommentedOutCode | 48 | false |
| IOResource | 44 | false |
| UnnecessaryToStringCall | 43 | true |
| AssignmentToForLoopParameter | 38 | false |
| RegExpRedundantEscape | 34 | false |
| StringEqualsEmptyString | 31 | false |
| PointlessBooleanExpression | 30 | true |
| MissingDeprecatedAnnotation | 30 | false |
| NonSerializableFieldInSerializableClass | 29 | false |
| IntegerMultiplicationImplicitCastToLong | 27 | false |
| ObsoleteCollection | 27 | false |
| UnnecessaryModifier | 26 | true |
| FieldAccessedSynchronizedAndUnsynchronized | 25 | false |
| FieldMayBeStatic | 23 | false |
| IgnoreResultOfCall | 22 | false |
| TrivialStringConcatenation | 20 | false |
| NonProtectedConstructorInAbstractClass | 20 | true |
| BusyWait | 18 | false |
| AssignmentToStaticFieldFromInstanceMethod | 17 | false |
| FinalStaticMethod | 17 | false |
| EmptyMethod | 17 | false |
| ThrowablePrintStackTrace | 17 | false |
| LongLiteralsEndingWithLowercaseL | 16 | false |
| ReplaceAssignmentWithOperatorAssignment | 15 | false |
| StaticCallOnSubclass | 15 | false |
| RedundantImplements | 15 | false |
| DeprecatedIsStillUsed | 15 | false |
| RedundantMethodOverride | 14 | false |
| StringBufferReplaceableByString | 13 | false |
| StringBufferReplaceableByStringBuilder | 13 | false |
| PublicFieldAccessedInSynchronizedContext | 12 | false |
| KeySetIterationMayUseEntrySet | 11 | false |
| DoubleBraceInitialization | 11 | false |
| DuplicateBranchesInSwitch | 11 | false |
| PointlessArithmeticExpression | 10 | false |
| StringOperationCanBeSimplified | 10 | false |
| DuplicateExpressions | 10 | false |
| ParameterCanBeLocal | 10 | false |
| UnnecessaryContinue | 10 | false |
| SwitchStatementWithConfusingDeclaration | 9 | false |
| RedundantSuppression | 9 | false |
| UnnecessaryReturn | 9 | true |
| ConditionCoveredByFurtherCondition | 9 | false |
| OptionalUsedAsFieldOrParameterType | 8 | false |
| ImplicitArrayToString | 8 | false |
| UnnecessaryLocalVariable | 8 | true |
| UnnecessarySuperQualifier | 8 | false |
| InstanceofCatchParameter | 8 | false |
| EqualsWhichDoesntCheckParameterClass | 8 | false |
| ThrowFromFinallyBlock | 7 | false |
| InnerClassMayBeStatic | 7 | true |
| CastCanBeRemovedNarrowingVariableType | 7 | false |
| NonSynchronizedMethodOverridesSynchronizedMethod | 6 | false |
| OverwrittenKey | 6 | false |
| StringEquality | 6 | false |
| RedundantArrayCreation | 6 | true |
| SetReplaceableByEnumSet | 6 | false |
| EmptyStatementBody | 5 | false |
| FinalPrivateMethod | 5 | false |
| AccessStaticViaInstance | 5 | false |
| DuplicateThrows | 5 | false |
| UnnecessaryCallToStringValueOf | 5 | false |
| CharsetObjectCanBeUsed | 5 | false |
| CaughtExceptionImmediatelyRethrown | 5 | false |
| UseCompareMethod | 5 | false |
| ManualMinMaxCalculation | 5 | false |
| IfStatementWithIdenticalBranches | 5 | false |
| DuplicateCondition | 4 | false |
| CallToStringConcatCanBeReplacedByOperator | 4 | false |
| SynchronizeOnThis | 4 | false |
| RedundantFileCreation | 4 | false |
| UseBulkOperation | 4 | false |
| EnumSwitchStatementWhichMissesCases | 4 | false |
| UnnecessaryStringEscape | 4 | true |
| InfiniteLoopStatement | 4 | false |
| OptionalGetWithoutIsPresent | 4 | false |
| RegExpSingleCharAlternation | 4 | false |
| MagicConstant | 3 | false |
| UnnecessarySemicolon | 3 | false |
| NonStrictComparisonCanBeEquality | 3 | true |
| NegativeIntConstantInLongContext | 3 | false |
| NonFinalFieldOfException | 3 | false |
| InjectedReferences | 3 | false |
| SuspiciousMethodCalls | 3 | false |
| Java8MapApi | 3 | false |
| ThrowableNotThrown | 3 | false |
| MismatchedArrayReadWrite | 2 | false |
| EmptyFinallyBlock | 2 | false |
| UnnecessaryUnboxing | 2 | false |
| MismatchedCollectionQueryUpdate | 2 | false |
| ShiftOutOfRange | 2 | false |
| ProtectedMemberInFinalClass | 2 | true |
| UtilityClassWithPublicConstructor | 2 | false |
| ConditionalBreakInInfiniteLoop | 2 | false |
| LoopStatementsThatDontLoop | 2 | false |
| RegExpSimplifiable | 2 | false |
| AbstractClassNeverImplemented | 2 | false |
| CodeBlock2Expr | 2 | true |
| UseOfPropertiesAsHashtable | 2 | false |
| ExceptionNameDoesntEndWithException | 2 | false |
| AbstractMethodCallInConstructor | 2 | false |
| MismatchedJavadocCode | 2 | false |
| RegExpUnnecessaryNonCapturingGroup | 2 | false |
| CopyConstructorMissesField | 2 | false |
| ThreadStartInConstruction | 2 | false |
| WrapperTypeMayBePrimitive | 1 | false |
| SimplifiableBooleanExpression | 1 | false |
| PointlessBitwiseExpression | 1 | false |
| HtmlWrongAttributeValue | 1 | false |
| AssignmentUsedAsCondition | 1 | false |
| RedundantEscapeInRegexReplacement | 1 | false |
| UnnecessaryBreak | 1 | false |
| EmptyTryBlock | 1 | false |
| JavaLangInvokeHandleSignature | 1 | false |
| MalformedFormatString | 1 | false |
| NonExceptionNameEndsWithException | 1 | false |
| MethodOverridesStaticMethod | 1 | false |
| RedundantLengthCheck | 1 | false |
| Java8MapForEach | 1 | false |
| CatchMayIgnoreException | 1 | false |
| ExcessiveRangeCheck | 1 | false |
| CovariantEquals | 1 | false |
| Convert2Lambda | 1 | false |
## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `tika-pipes/tika-emitters/tika-emitter-jdbc/src/main/java/org/apache/tika/pipes/emitter/jdbc/JDBCEmitter.java`
#### Snippet
```java
            return;
        }
        Double d = Double.parseDouble(val);
        insertStatement.setDouble(i, d);
    }
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-core/src/main/java/org/apache/tika/concurrent/ConfigurableThreadPoolExecutor.java`
#### Snippet
```java
    public void setMaximumPoolSize(int threads);
    
    public void setCorePoolSize(int threads);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-core/src/main/java/org/apache/tika/concurrent/ConfigurableThreadPoolExecutor.java`
#### Snippet
```java
public interface ConfigurableThreadPoolExecutor extends ExecutorService {
    
    public void setMaximumPoolSize(int threads);
    
    public void setCorePoolSize(int threads);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/BytesRefCalculator.java`
#### Snippet
```java
public interface BytesRefCalculator<T> extends TextStatsCalculator {

    public BytesRefCalcInstance<T> getInstance();

    interface BytesRefCalcInstance<T> {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/IDBWriter.java`
#### Snippet
```java

public interface IDBWriter {
    public void writeRow(TableInfo table, Map<Cols, String> data) throws IOException;

    public void close() throws IOException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/IDBWriter.java`
#### Snippet
```java
    public void writeRow(TableInfo table, Map<Cols, String> data) throws IOException;

    public void close() throws IOException;

    public int getMimeId(String mimeString);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/IDBWriter.java`
#### Snippet
```java
    public void close() throws IOException;

    public int getMimeId(String mimeString);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/XMLLogMsgHandler.java`
#### Snippet
```java

public interface XMLLogMsgHandler {
    public void handleMsg(Level level, String xml) throws IOException, SQLException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/XSLXCellFormatter.java`
#### Snippet
```java
interface XSLXCellFormatter {

    public void reset(XSSFWorkbook workbook);

    public void applyStyleAndValue(int dbColNum, ResultSet resultSet, Cell cell)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/XSLXCellFormatter.java`
#### Snippet
```java
    public void reset(XSSFWorkbook workbook);

    public void applyStyleAndValue(int dbColNum, ResultSet resultSet, Cell cell)
            throws SQLException;
}
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESSerializer.java`
#### Snippet
```java
    private final String className;

    private CTAKESSerializer(String className) {
        this.className = className;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/FileResource.java`
#### Snippet
```java
     * @return an id for a FileResource
     */
    public String getResourceId();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/FileResource.java`
#### Snippet
```java
     * @return Metadata
     */
    public Metadata getMetadata();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/FileResource.java`
#### Snippet
```java
    //The literal lowercased extension of a file.  This may or may not
    //have any relationship to the actual type of the file.
    public static final Property FILE_EXTENSION = Property.internalText("tika:file_ext");

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `tika-batch/src/main/java/org/apache/tika/batch/FileResource.java`
#### Snippet
```java
    //The literal lowercased extension of a file.  This may or may not
    //have any relationship to the actual type of the file.
    public static final Property FILE_EXTENSION = Property.internalText("tika:file_ext");

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `tika-batch/src/main/java/org/apache/tika/batch/FileResource.java`
#### Snippet
```java
    //The literal lowercased extension of a file.  This may or may not
    //have any relationship to the actual type of the file.
    public static final Property FILE_EXTENSION = Property.internalText("tika:file_ext");

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/FileResource.java`
#### Snippet
```java
     * @throws java.io.IOException
     */
    public InputStream openInputStream() throws IOException;

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/OutputStreamFactory.java`
#### Snippet
```java
public interface OutputStreamFactory {

    public OutputStream getOutputStream(Metadata metadata) throws IOException;

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/builders/IParserFactoryBuilder.java`
#### Snippet
```java

public interface IParserFactoryBuilder {
    public ParserFactory build(Node node, Map<String, String> runtimeAttrs);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/builders/ICrawlerBuilder.java`
#### Snippet
```java
public interface ICrawlerBuilder extends ObjectFromDOMAndQueueBuilder<FileResourceCrawler> {

    public FileResourceCrawler build(Node node, Map<String, String> attributes,
                                     ArrayBlockingQueue<FileResource> queue);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/builders/ObjectFromDOMAndQueueBuilder.java`
#### Snippet
```java
public interface ObjectFromDOMAndQueueBuilder<T> {

    public T build(Node node, Map<String, String> runtimeAttributes,
                   ArrayBlockingQueue<FileResource> resourceQueue);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/builders/IContentHandlerFactoryBuilder.java`
#### Snippet
```java
public interface IContentHandlerFactoryBuilder extends ObjectFromDOMBuilder<ContentHandlerFactory> {

    public ContentHandlerFactory build(Node node, Map<String, String> attributes);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/builders/ObjectFromDOMBuilder.java`
#### Snippet
```java
public interface ObjectFromDOMBuilder<T> {

    public T build(Node node, Map<String, String> runtimeAttributes);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/builders/StatusReporterBuilder.java`
#### Snippet
```java
public interface StatusReporterBuilder {

    public StatusReporter build(FileResourceCrawler crawler, ConsumersManager consumers, Node n,
                                Map<String, String> commandlineArguments);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `tika-batch/src/main/java/org/apache/tika/batch/builders/ReporterBuilder.java`
#### Snippet
```java
 */
public interface ReporterBuilder extends ObjectFromDOMBuilder<StatusReporter> {
    public StatusReporter build(Node n, Map<String, String> runtimeAttributes);
}

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsEncoding.java`
#### Snippet
```java
    private String encoding;

    private StringsEncoding(char value, String encoding) {
        this.value = value;
        this.encoding = encoding;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageMetadataExtractor.java`
#### Snippet
```java
     * Reads one or more type of Metadata Extractor fields.
     */
    static interface DirectoryHandler {
        /**
         * @param directoryType A Metadata Extractor directory class
```

## RuleId[id=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `IS_CONTROL_BYTE` are written to, but never read
in `tika-core/src/main/java/org/apache/tika/detect/TextDetector.java`
#### Snippet
```java
     * @see <a href="https://issues.apache.org/jira/browse/TIKA-154">TIKA-154</a>
     */
    private static final boolean[] IS_CONTROL_BYTE = new boolean[0x20];

    static {
```

### MismatchedArrayReadWrite
Contents of array `dateFormats` are written to, but never read
in `tika-pipes/tika-emitters/tika-emitter-jdbc/src/main/java/org/apache/tika/pipes/emitter/jdbc/JDBCEmitter.java`
#### Snippet
```java
        insertStatement.clearParameters();
        int i = 0;
        DateFormat[] dateFormats = new DateFormat[TIKA_DATE_PATTERNS.length];
        for (int j = 0; j < TIKA_DATE_PATTERNS.length; j++) {
            dateFormats[i] = new SimpleDateFormat(TIKA_DATE_PATTERNS[j], Locale.US);
```

## RuleId[id=MagicConstant]
### MagicConstant
Should be one of: Calendar.JANUARY, Calendar.FEBRUARY, Calendar.MARCH, Calendar.APRIL, Calendar.MAY, ...
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
                if (nextMetaData == TikaCoreProperties.CREATED) {
                    Calendar cal = Calendar.getInstance(TimeZone.getDefault(), Locale.ROOT);
                    cal.set(year, month - 1, day, hour, minute, 0);
                    metadata.set(nextMetaData, cal.getTime());
                } else if (nextMetaData.isMultiValuePermitted()) {
```

### MagicConstant
Should be one of: Calendar.JANUARY, Calendar.FEBRUARY, Calendar.MARCH, Calendar.APRIL, Calendar.MAY, ...
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFFileHeader.java`
#### Snippet
```java
        }
        Calendar lastModified = new GregorianCalendar(TimeZone.getTimeZone("UTC"), Locale.ROOT);
        lastModified.set(lastModYear, lastModMonth - 1, lastModDay, 0, 0, 0);
        header.lastModified = lastModified;

```

### MagicConstant
Should be one of: Calendar.JANUARY, Calendar.FEBRUARY, Calendar.MARCH, Calendar.APRIL, Calendar.MAY, ...
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFCell.java`
#### Snippet
```java
                GregorianCalendar.getInstance(TimeZone.getTimeZone("UTC"), Locale.ROOT);
//        baseCalendar.set(1899, 11, 31, 0, 0, 0);
        baseCalendar.set(-4712, 0, 1, 0, 0, 0);
        try (InputStream is = new UnsynchronizedByteArrayInputStream(getBytes())) {

```

## RuleId[id=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`i + 0` can be replaced with 'i'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                b[1] = getContent()[i + 2];
                b[2] = getContent()[i + 1];
                b[3] = getContent()[i + 0];
                long absoff = (new BigInteger(b)).longValue();
                if ((absoff >= -curpos) && (absoff < getState().getIntelFileSize())) {
```

### PointlessArithmeticExpression
`i + 0` can be replaced with 'i'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    long reloff = (absoff >= 0) ? absoff - curpos :
                            absoff + getState().getIntelFileSize();
                    getContent()[i + 0] = (byte) reloff;
                    getContent()[i + 1] = (byte) (reloff >>> 8);
                    getContent()[i + 2] = (byte) (reloff >>> 16);
```

### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/SimpleChunking.java`
#### Snippet
```java
    @Override
    public List<LeafNodeObject> chunking() throws TikaException {
        int maxChunkSize = 1 * 1024 * 1024;
        java.util.List<LeafNodeObject> list = new ArrayList<>();
        LeafNodeObject.IntermediateNodeObjectBuilder builder =
```

### PointlessArithmeticExpression
`offset + 0` can be replaced with 'offset'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java
     */
    protected static int get7BitsInt(byte[] data, int offset) {
        int b0 = data[offset + 0] & 0x7F;
        int b1 = data[offset + 1] & 0x7F;
        int b2 = data[offset + 2] & 0x7F;
```

### PointlessArithmeticExpression
`offset + 0` can be replaced with 'offset'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java

    protected static int getInt(byte[] data, int offset) {
        int b0 = data[offset + 0] & 0xFF;
        int b1 = data[offset + 1] & 0xFF;
        int b2 = data[offset + 2] & 0xFF;
```

### PointlessArithmeticExpression
`offset + 0` can be replaced with 'offset'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java

    protected static int getInt3(byte[] data, int offset) {
        int b0 = data[offset + 0] & 0xFF;
        int b1 = data[offset + 1] & 0xFF;
        int b2 = data[offset + 2] & 0xFF;
```

### PointlessArithmeticExpression
`offset + 0` can be replaced with 'offset'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java

    protected static int getInt2(byte[] data, int offset) {
        int b0 = data[offset + 0] & 0xFF;
        int b1 = data[offset + 1] & 0xFF;
        return (b0 << 8) + (b1);
```

### PointlessArithmeticExpression
`lookat + 0` can be replaced with 'lookat'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/LyricsHandler.java`
#### Snippet
```java
            lookat -= 128;
        }
        if (tagData[lookat + 0] == 'L' && tagData[lookat + 1] == 'Y' &&
                tagData[lookat + 2] == 'R' && tagData[lookat + 3] == 'I' &&
                tagData[lookat + 4] == 'C' && tagData[lookat + 5] == 'S' &&
```

### PointlessArithmeticExpression
`index + 0` can be replaced with 'index'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
        int getChar(byte[] input, int index) {
            return (input[index + 3] & 0xFF) << 24 | (input[index + 2] & 0xFF) << 16 |
                    (input[index + 1] & 0xFF) << 8 | (input[index + 0] & 0xFF);
        }

```

### PointlessArithmeticExpression
`index + 0` can be replaced with 'index'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
    static class CharsetRecog_UTF_32_BE extends CharsetRecog_UTF_32 {
        int getChar(byte[] input, int index) {
            return (input[index + 0] & 0xFF) << 24 | (input[index + 1] & 0xFF) << 16 |
                    (input[index + 2] & 0xFF) << 8 | (input[index + 3] & 0xFF);
        }
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`if` statement has empty body
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/ZipContainerDetectorBase.java`
#### Snippet
```java
            }
        }
        if (kmlFound) {
        }
        return null;
```

### EmptyStatementBody
`if` statement has empty body
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/XLSXNumFormatter.java`
#### Snippet
```java
            throws SQLException {
        double d = resultSet.getDouble(dbColNum);
        if (resultSet.wasNull()) {

        } else {
```

### EmptyStatementBody
`if` statement has empty body
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private TikaInputStream runFilters(COSBase filters, TikaInputStream is,
                                       List<COSName> usedFilters) throws IOException {
        if (filters instanceof COSNull) {
        } else if (filters instanceof COSName) {
            is = runFilter((COSName) filters, is, new COSDictionary(), 0);
```

### EmptyStatementBody
`if` statement has empty body
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java
        }

        if ("sf:menu-choices".equals(qName)) {
        }

```

### EmptyStatementBody
`for` statement has empty body
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-csv/src/test/java/TestCSVPipesIterator.java`
#### Snippet
```java
        assertThrows(RuntimeException.class, () -> {
            it.setFetchKeyColumn("fetchKeyDoesntExist");
            for (FetchEmitTuple t : it) {

            }
```

## RuleId[id=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `USE_STATIC` from instance context
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFParser.java`
#### Snippet
```java
    public void setMemoryLimitInKb(int memoryLimitInKb) {
        this.memoryLimitInKb = memoryLimitInKb;
        USE_STATIC = false;
    }
}
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `HAS_WARNED` from instance context
in `tika-core/src/main/java/org/apache/tika/detect/FileCommandDetector.java`
#### Snippet
```java
            if (!HAS_WARNED) {
                LOGGER.warn("'file' command isn't working: '" + fileCommandPath + "'");
                HAS_WARNED = true;
            }
            return MediaType.OCTET_STREAM;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `restHostUrlStr` from instance context
in `tika-langdetect/tika-langdetect-mitll-text/src/main/java/org/apache/tika/langdetect/mitll/TextLangDetector.java`
#### Snippet
```java
    public TextLangDetector() {
        super();
        restHostUrlStr = TEXT_REST_HOST;
        languages = getAllLanguages();
        writer = new CharArrayWriter();
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `agePredictor` from instance context
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniser.java`
#### Snippet
```java
    public AgePredicterLocal getAgePredictorClient() throws InvalidFormatException, IOException {
        if (agePredictor == null) {
            agePredictor = new AgePredicterLocal(config.getPathClassifyModel(),
                    config.getPathClassifyRegression());
        }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `available` from instance context
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniser.java`
#### Snippet
```java
        try {
            secondaryParser = new Tika(new TikaConfig());
            available = true;
        } catch (Exception e) {
            available = false;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `available` from instance context
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniser.java`
#### Snippet
```java
            available = true;
        } catch (Exception e) {
            available = false;
            LOG.error("Unable to initialize secondary parser", e);
        }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `available` from instance context
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/nltk/NLTKNERecogniser.java`
#### Snippet
```java
            int responseCode = response.getStatus();
            if (responseCode == 200) {
                available = true;
            } else {
                LOG.info("NLTKRest Server is not running");
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `available` from instance context
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/grobid/GrobidNERecogniser.java`
#### Snippet
```java
            int responseCode = response.getStatus();
            if (responseCode == 200) {
                available = true;
            } else {
                LOG.info("Grobid REST Server is not running");
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `HAS_WARNED` from instance context
in `tika-detectors/tika-detector-siegfried/src/main/java/org/apache/tika/detect/siegfried/SiegfriedDetector.java`
#### Snippet
```java
            if (!HAS_WARNED) {
                LOGGER.warn("'siegfried' command isn't working: '" + siegfriedPath + "'");
                HAS_WARNED = true;
            }
            return MediaType.OCTET_STREAM;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `failedFiles` from instance context
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
                public FileVisitResult visitFileFailed(Path file, IOException exc)
                        throws IOException {
                    failedFiles++;
                    return FileVisitResult.CONTINUE;
                }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `successfulFiles` from instance context
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
                    try {
                        process(file);
                        successfulFiles++;
                    } catch (Exception e) {
                        failedFiles++;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `failedFiles` from instance context
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
                        successfulFiles++;
                    } catch (Exception e) {
                        failedFiles++;
                        // ignore this file
                    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `successfulFiles` from instance context
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
                    try {
                        process(file);
                        successfulFiles++;
                    } catch (Exception e) {
                        failedFiles++;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `failedFiles` from instance context
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
                        successfulFiles++;
                    } catch (Exception e) {
                        failedFiles++;
                        // ignore this file
                    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `failedFiles` from instance context
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
                public FileVisitResult visitFileFailed(Path file, IOException exc)
                        throws IOException {
                    failedFiles++;
                    return FileVisitResult.CONTINUE;
                }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `counter` from instance context
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/PSDParser.java`
#### Snippet
```java
                throws IOException, TikaException {
            this.maxDataLengthBytes = maxDataLengthBytes;
            counter++;
            // Verify the signature
            long sig = EndianUtils.readIntBE(stream);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `HAS_WARNED` from instance context
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
                "to be applied to your files see: " +
                "https://cwiki.apache.org/confluence/display/TIKA/TikaOCR#TikaOCR-disable-ocr");
        HAS_WARNED = true;
    }

```

## RuleId[id=EmptyFinallyBlock]
### EmptyFinallyBlock
Empty `finally` block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
            throw new TikaException("POI MAPIMessage broken - didn't return null on missing chunk",
                    e);
        } finally {
            //You'd think you'd want to call msg.close().
            //Don't do that.  That closes down the file system.
```

### EmptyFinallyBlock
Empty `finally` block
in `tika-core/src/main/java/org/apache/tika/detect/TrainedModelDetector.java`
#### Snippet
```java

            return histogram;
        } finally {
            // inputChannel.close();
        }
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (3 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
            //put [Content_Types.xml] as the first physical entry in the zip file
            //so we need to read through the full file
            /*if (cnt.getCount() > 2) {
                return TIKA_OOXML;
            }*/
```

### CommentedOutCode
Commented out code (3 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmParser.java`
#### Snippet
```java
            final String entryName = entry.getName();
            if (entryName.endsWith(".html") || entryName.endsWith(".htm")) {
//                AttributesImpl attrs = new AttributesImpl();
//                attrs.addAttribute("", "name", "name", "String", entryName);
//                xhtml.startElement("", "document", "document", attrs);
```

### CommentedOutCode
Commented out code (2 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
            /* Creates and parses chm itsf header */
            setChmItsfHeader(new ChmItsfHeader());
            // getChmItsfHeader().parse(Arrays.copyOfRange(getData(), 0,
            // ChmConstants.CHM_ITSF_V3_LEN - 1), getChmItsfHeader());
            getChmItsfHeader()
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
            /* Creates and parses chm itsp header */
            setChmItspHeader(new ChmItspHeader());
            // getChmItspHeader().parse(Arrays.copyOfRange( getData(), (int)
            // getChmItsfHeader().getDirOffset(),
            // (int) getChmItsfHeader().getDirOffset() +
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
                                .getLength());
            }
            // dir_chunk = Arrays.copyOfRange(getData(), indexOfResetData,
            // indexOfResetData
            // +
```

### CommentedOutCode
Commented out code (3 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
            ChmAssert.assertCopyingDataIndex(startIndex, getData().length);

            // dir_chunk = Arrays.copyOfRange(getData(), startIndex, startIndex
            // +
            // getChmDirList().getDirectoryListingEntryList().get(indexOfResetTable).getLength());
```

### CommentedOutCode
Commented out code (2 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
                int dataOffset = (int) (getChmItsfHeader().getDataOffset() +
                        directoryListingEntry.getOffset());
                // dataSegment = Arrays.copyOfRange(getData(), dataOffset,
                // dataOffset + directoryListingEntry.getLength());
                buffer.write(ChmCommons.copyOfRange(getData(), dataOffset,
```

### CommentedOutCode
Commented out code (2 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
        /* unmarshal common fields */
        chmItspHeader.unmarshalCharArray(data, chmItspHeader, ChmConstants.CHM_SIGNATURE_LEN);
        // ChmCommons.unmarshalCharArray(data, chmItspHeader,
        // ChmConstants.CHM_SIGNATURE_LEN);
        chmItspHeader.setVersion(chmItspHeader
```

### CommentedOutCode
Commented out code (3 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java
    }

//    private void setData(byte[] data) {
//        this.data = data;
//    }
```

### CommentedOutCode
Commented out code (11 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java
    }

    /*
    public BigInteger unmarshalUlong() {
        return getBigInteger(8);
```

### CommentedOutCode
Commented out code (53 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java
            }

//                int indexWorkData = ChmCommons.indexOf(dir_chunk,
//                        "::".getBytes(UTF_8));
//                int indexUserData = ChmCommons.indexOf(dir_chunk,
```

### CommentedOutCode
Commented out code (14 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    // this.out and this.metadata
    public void extract(InputStream in) throws IOException, SAXException, TikaException {
//        in = new FilterInputStream(in) {
//            public int read() throws IOException {
//                int r = super.read();
```

### CommentedOutCode
Commented out code (3 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        FCHARSET_MAP.put(177, CP1255); // Hebrew
        FCHARSET_MAP.put(178, CP1256); // Arabic
        // FCHARSET_MAP.put( 179, "" ); // Arabic Traditional
        // FCHARSET_MAP.put( 180, "" ); // Arabic user
        // FCHARSET_MAP.put( 181, "" ); // Hebrew user
```

### CommentedOutCode
Commented out code (20 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java

    private void writeRow(List<GlyphRun> row) throws SAXException {
/*
        int rtl = 0;
        int ltr = 0;
```

### CommentedOutCode
Commented out code (3 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java
        GUID header = deserializeGUID();
        // TODO - the expected header is different per version of one note.
//    if (!header.equals(FILE_DATA_STORE_OBJ_HEADER)) {
//      throw new TikaException("Unexpected file data store object header: " + header);
//    }
```

### CommentedOutCode
Commented out code (3 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java
        GUID footer = deserializeGUID();
        // TODO - the expected footer is per version of one note.
//    if (!footer.equals(FILE_DATA_STORE_OBJ_FOOTER)) {
//      throw new TikaException("Unexpected file data store object footer: " + footer);
//    }
```

### CommentedOutCode
Commented out code (2 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java
        data.contextIDs.extendedStreamsPresent = 0;
        data.contextIDs.osidsStreamNotPresent = 0;
        //uint64_t cur_offset = offset;
        //LOG.debug("starting deserialization %lx(%lx) / %lx", offset, offset - cur_offset, end);
        data.oids = deserializeObjectSpaceObjectStreamOfOIDsOSIDsOrContextIDs();
```

### CommentedOutCode
Commented out code (5 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/RDCAnalysisChunking.java`
#### Snippet
```java
        signature.signatureData = new BinaryItem(ByteUtil.toListOfByte(temp));

//            RDCSignatureGenerator generator = new RDCSignatureGenerator();
//            signatureBytes = generator.ComputeHash(temp);
//
```

### CommentedOutCode
Commented out code (2 lines)
in `tika-core/src/main/java/org/apache/tika/sax/SafeContentHandler.java`
#### Snippet
```java
package org.apache.tika.sax;

/*
import java.util.ArrayList;
import java.util.List;
```

### CommentedOutCode
Commented out code (30 lines)
in `tika-core/src/main/java/org/apache/tika/sax/SafeContentHandler.java`
#### Snippet
```java


    /*
    private final List<String> elements = new ArrayList<String>();

```

### CommentedOutCode
Commented out code (5 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
    Property ALT_TAPE_NAME = Property.externalText("xmpDM:altTapeName");

//    /**
//     * "A timecode set by the user. When specified, it is used instead
//     * of the startTimecode."
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
    Property COMPILATION = Property.externalInteger("xmpDM:compilation");

//    /**
//     * "Additional parameters for Beat Splice stretch mode."
//     */
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
    Property COPYRIGHT = Property.externalText("xmpDM:copyright");

//    /**
//     * "An unordered list of all media used to create this media."
//     */
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
                    "G#", "A", "A#", "B");

//    /**
//     * "The duration of lead time for queuing music."
//     */
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
    Property METADATA_MOD_DATE = Property.internalDate("xmpDM:metadataModDate");

//    /**
//     * An ordered list of markers. See also {@link #TRACKS xmpDM:Tracks}.
//     */
```

### CommentedOutCode
Commented out code (8 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
                    "WWSSW", "WSSWW_24p", "SSWWW_24p", "SWWWS_24p", "WWWSS_24p", "WWSSW_24p");

//    /**
//     * "The time at which to fade out."
//     */
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
    Property RELEASE_DATE = Property.externalDate("xmpDM:releaseDate");

//    /**
//     * "The start time of the media inside the audio project."
//     */
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
            Property.internalClosedChoise("xmpDM:scaleType", "Major", "Minor", "Both", "Neither");

//    /**
//     * "Additional parameters for Resample stretch mode."
//     */
```

### CommentedOutCode
Commented out code (5 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
                    "Resample", "Beat Splice", "Hybrid");

//    /**
//     * "The timecode of the first frame of video in the file, as obtained
//     * from the device control."
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
                    "6/8", "9/8", "12/8", "other");

//    /**
//     * "Additional parameters for Time-Scale stretch mode."
//     */
```

### CommentedOutCode
Commented out code (6 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
            Property.externalClosedChoise("xmpDM:videoAlphaMode", "straight", "pre-multiplied");

//    /**
//     * "An unordered list of tracks. A track is a named set of markers,
//     * which can specify a frame rate for all markers in the set.
```

### CommentedOutCode
Commented out code (5 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
            Property.internalBoolean("xmpDM:videoAlphaUnityIsTransparent");

//    /**
//     * "A color in CMYK or RGB to be used as the pre-multiple color when
//     * alpha mode is pre-multiplied."
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
    Property VIDEO_MOD_DATE = Property.internalDate("xmpDM:videoModDate");

//    /**
//     * "The frame size. For example: w:720, h: 480, unit:pixels"
//     */
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaClient.java`
#### Snippet
```java
    }

    /*public List<Metadata> parse(InputStream is, Metadata metadata)
    throws IOException, TikaException {

```

### CommentedOutCode
Commented out code (7 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DeprecatedStreamingZipContainerDetector.java`
#### Snippet
```java
        return MediaType.APPLICATION_ZIP;
    }
/*
    private static MediaType detectIWorks(Set<String> entryNames) {
        //general iworks
```

### CommentedOutCode
Commented out code (98 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DeprecatedStreamingZipContainerDetector.java`
#### Snippet
```java
    @Override
    public MediaType detect(InputStream is, Metadata metadata) {
/*
        Set<String> fileNames = new HashSet<>();
        Set<String> directoryNames = new HashSet<>();
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp4/MP4Parser.java`
#### Snippet
```java
                }
            }
/*            for (Tag t : mp4Directory.getTags()) {
                System.out.println(mp4Directory.getClass() + " : " + t.getTagName()
                                + " : " + mp4Directory.getString(t.getTagType()));
```

### CommentedOutCode
Commented out code (2 lines)
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
        }
        //TODO -- figure out how to turn this back on
        //logFetchersAndEmitters(tikaServerConfig.isEnableUnsecureFeatures(), fetcherManager,
          //      emitterManager);

```

### CommentedOutCode
Commented out code (15 lines)
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/UnpackerResource.java`
#### Snippet
```java
        }

/*        protected void copy(DirectoryEntry sourceDir, DirectoryEntry destDir)
                throws IOException {
            for (Entry entry : sourceDir) {
```

### CommentedOutCode
Commented out code (15 lines)
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CJKBigramAwareLengthFilterFactory.java`
#### Snippet
```java
    }

    /*
    private static boolean isCJ(int codePoint) {
        if (
```

### CommentedOutCode
Commented out code (5 lines)
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenCounter.java`
#### Snippet
```java
        }

/*            Collections.sort(allTokens);
            List<TokenIntPair> topNList = new ArrayList<>(topN);
            for (int i = 0; i < topN && i < allTokens.size(); i++) {
```

### CommentedOutCode
Commented out code (10 lines)
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
    // This method was commented because it depends on org.apache.lucene.analysis.Token
    // that is not a part of the Tika
    // /**
    // * Adds ngrams from a token to this profile
    // *
```

### CommentedOutCode
Commented out code (17 lines)
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/XMLErrorLogUpdater.java`
#### Snippet
```java
                LOG.error("Records ids should be unique: {}", resourceId);
            }
/*
            if (containerId < 0) {
                System.err.println("CONTAINER ID < 0!!!");
```

### CommentedOutCode
Commented out code (3 lines)
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java

    protected Map<Class, Object> calcTextStats(ContentTags contentTags) {
/*        if (contentTags == ContentTags.EMPTY_CONTENT_TAGS) {
            return Collections.EMPTY_MAP;
        }*/
```

### CommentedOutCode
Commented out code (13 lines)
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/BasicTikaFSConsumersBuilder.java`
#### Snippet
```java

        Path outputDir = PropsUtil.getPath(attrs.get("outputDir"), null);
/*        FSUtil.HANDLE_EXISTING handleExisting = null;
        String handleExistingString = attrs.get("handleExisting");
        if (handleExistingString == null) {
```

### CommentedOutCode
Commented out code (4 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        metadata.set(TikaCoreProperties.PUBLISHER, clean(this.getFormatName()));

/*
        metadata.set(TikaCoreProperties.DATE, font.getHeader().getCreated().getTime());
        metadata.set(
```

### CommentedOutCode
Commented out code (2 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                        term = Math.min(term,
                                (tmp_line.toString().contains("=") ? tmp_line.indexOf("=") : term));
//                  term = Math.min(term, (tmp_line.indexOf("\n") > -1 ? tmp_line.indexOf("\n") :
//                  term));
                        term = (term > 0) ? term : tmp_line.length();
```

### CommentedOutCode
Commented out code (6 lines)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

                if (finished = (b == bfinish)) {
/*
               is.reset();
               long skipped = is.skip((long)read);
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/RelationshipsManager.java`
#### Snippet
```java

        String tmp = relsFileName;
        tmp = tmp.replaceFirst("\\/_rels\\/", "/");
        tmp = tmp.replaceFirst(".rels\\Z", "");
        return tmp;
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/RelationshipsManager.java`
#### Snippet
```java

        String tmp = relsFileName;
        tmp = tmp.replaceFirst("\\/_rels\\/", "/");
        tmp = tmp.replaceFirst(".rels\\Z", "");
        return tmp;
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
    // reference
    private static final String REGEX_FALLBACK = "\\(?" + "(?<mainOrganization>[A-Z]\\w{1,64}+)" +
            "\\)?((\\s?(?<separator>\\/)\\s?)(\\w{1,64}+\\s)*\\(?" + "(?<secondOrganization>[A-Z" +
            "]\\w{1,64}+)" +
            "\\)?)?" + REGEX_STANDARD_TYPE + "?" + "(-|\\s)?" + REGEX_IDENTIFIER;
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    // TIKA-350: handle charset as first element in content-type
    private static final Pattern CHARSET_FIRST_PATTERN = Pattern.compile(
            "(?is)\\s*(charset\\s*=\\s*[^\\c;\\s]+)\\s*;\\s*" + VALID_CHARS + "\\s*/\\s*" +
                    VALID_CHARS + "\\s*");

```

### RegExpRedundantEscape
Redundant character escape `\?` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    // TIKA-350: handle charset as first element in content-type
    private static final Pattern CHARSET_FIRST_PATTERN = Pattern.compile(
            "(?is)\\s*(charset\\s*=\\s*[^\\c;\\s]+)\\s*;\\s*" + VALID_CHARS + "\\s*/\\s*" +
                    VALID_CHARS + "\\s*");

```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    private static final Pattern CHARSET_FIRST_PATTERN = Pattern.compile(
            "(?is)\\s*(charset\\s*=\\s*[^\\c;\\s]+)\\s*;\\s*" + VALID_CHARS + "\\s*/\\s*" +
                    VALID_CHARS + "\\s*");

    /**
```

### RegExpRedundantEscape
Redundant character escape `\?` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    private static final Pattern CHARSET_FIRST_PATTERN = Pattern.compile(
            "(?is)\\s*(charset\\s*=\\s*[^\\c;\\s]+)\\s*;\\s*" + VALID_CHARS + "\\s*/\\s*" +
                    VALID_CHARS + "\\s*");

    /**
```

### RegExpRedundantEscape
Redundant character escape `\\)` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
     * See http://www.ietf.org/rfc/rfc2045.txt for valid mime-type characters.
     */
    private static final String VALID_CHARS = "([^\\c\\(\\)<>@,;:\\\\\"/\\[\\]\\?=\\s]+)";

    private static final Pattern TYPE_PATTERN =
```

### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
     * See http://www.ietf.org/rfc/rfc2045.txt for valid mime-type characters.
     */
    private static final String VALID_CHARS = "([^\\c\\(\\)<>@,;:\\\\\"/\\[\\]\\?=\\s]+)";

    private static final Pattern TYPE_PATTERN =
```

### RegExpRedundantEscape
Redundant character escape `\\(` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    private static final long serialVersionUID = -3831000556189036392L;

    private static final Pattern SPECIAL = Pattern.compile("[\\(\\)<>@,;:\\\\\"/\\[\\]\\?=]");

    private static final Pattern SPECIAL_OR_WHITESPACE =
```

### RegExpRedundantEscape
Redundant character escape `\\)` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    private static final long serialVersionUID = -3831000556189036392L;

    private static final Pattern SPECIAL = Pattern.compile("[\\(\\)<>@,;:\\\\\"/\\[\\]\\?=]");

    private static final Pattern SPECIAL_OR_WHITESPACE =
```

### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    private static final long serialVersionUID = -3831000556189036392L;

    private static final Pattern SPECIAL = Pattern.compile("[\\(\\)<>@,;:\\\\\"/\\[\\]\\?=]");

    private static final Pattern SPECIAL_OR_WHITESPACE =
```

### RegExpRedundantEscape
Redundant character escape `\\(` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    private static final Pattern SPECIAL_OR_WHITESPACE =
            Pattern.compile("[\\(\\)<>@,;:\\\\\"/\\[\\]\\?=\\s]");

    /**
```

### RegExpRedundantEscape
Redundant character escape `\\)` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    private static final Pattern SPECIAL_OR_WHITESPACE =
            Pattern.compile("[\\(\\)<>@,;:\\\\\"/\\[\\]\\?=\\s]");

    /**
```

### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    private static final Pattern SPECIAL_OR_WHITESPACE =
            Pattern.compile("[\\(\\)<>@,;:\\\\\"/\\[\\]\\?=\\s]");

    /**
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    private static final Pattern TYPE_PATTERN =
            Pattern.compile("(?s)\\s*" + VALID_CHARS + "\\s*/\\s*" + VALID_CHARS + "\\s*($|;.*)");

    // TIKA-350: handle charset as first element in content-type
```

### RegExpRedundantEscape
Redundant character escape `\?` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    private static final Pattern TYPE_PATTERN =
            Pattern.compile("(?s)\\s*" + VALID_CHARS + "\\s*/\\s*" + VALID_CHARS + "\\s*($|;.*)");

    // TIKA-350: handle charset as first element in content-type
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    private static final Pattern TYPE_PATTERN =
            Pattern.compile("(?s)\\s*" + VALID_CHARS + "\\s*/\\s*" + VALID_CHARS + "\\s*($|;.*)");

    // TIKA-350: handle charset as first element in content-type
```

### RegExpRedundantEscape
Redundant character escape `\?` in RegExp
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    private static final Pattern TYPE_PATTERN =
            Pattern.compile("(?s)\\s*" + VALID_CHARS + "\\s*/\\s*" + VALID_CHARS + "\\s*($|;.*)");

    // TIKA-350: handle charset as first element in content-type
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
public class CharsetUtils {

    private static final Pattern CHARSET_NAME_PATTERN = Pattern.compile("[ \\\"]*([^ >,;\\\"]+).*");

    private static final Pattern ISO_NAME_PATTERN = Pattern.compile(".*8859-(\\d+)");
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
public class CharsetUtils {

    private static final Pattern CHARSET_NAME_PATTERN = Pattern.compile("[ \\\"]*([^ >,;\\\"]+).*");

    private static final Pattern ISO_NAME_PATTERN = Pattern.compile(".*8859-(\\d+)");
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `tika-core/src/main/java/org/apache/tika/language/detect/LanguageResult.java`
#### Snippet
```java
     */
    public boolean isLanguage(String language) {
        String[] targetLanguage = language.split("\\-");
        String[] resultLanguage = this.language.split("\\-");

```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `tika-core/src/main/java/org/apache/tika/language/detect/LanguageResult.java`
#### Snippet
```java
    public boolean isLanguage(String language) {
        String[] targetLanguage = language.split("\\-");
        String[] resultLanguage = this.language.split("\\-");

        int minLength = Math.min(targetLanguage.length, resultLanguage.length);
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java

    private void addPatternWithIs(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(name + " is ([A-Za-z0-9\\.,\\s`']+)"), name);
    }

```

### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java

    private void addPatternWithColon(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(name + "\\:\\s*([A-Za-z0-9/ _\\-\\.]+)\\s*"), name);
    }

```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java

    private void addPatternWithColon(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(name + "\\:\\s*([A-Za-z0-9/ _\\-\\.]+)\\s*"), name);
    }

```

### RegExpRedundantEscape
Redundant character escape `\\>` in RegExp
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFExtractor.java`
#### Snippet
```java

    private static final Pattern openTagPattern = Pattern.compile("<(\\S*).*");
    private static final Pattern selfTagPattern = Pattern.compile("(<(\\S*))(\\s)(.*)(\\>).*");
    private static final String OPEN_TAG_MARKER = "<";
    private static final String CLOSE_TAG_MARKER = ">";
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(TikaServerConfig.class);

    private static Pattern SYS_PROPS = Pattern.compile("\\$\\{sys:([-_0-9A-Za-z]+)\\}");

    public static final int DEFAULT_PORT = 9998;
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
    private static final String META_PREFIX = "meta_";
    private static final Logger LOG = LoggerFactory.getLogger(TikaResource.class);
    private static Pattern ALLOWABLE_HEADER_CHARS = Pattern.compile("(?i)^[-/_+\\.A-Z0-9 ]+$");
    private static TikaConfig TIKA_CONFIG;
    private static TikaServerConfig TIKA_SERVER_CONFIG;
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlEncodingDetector.java`
#### Snippet
```java
    //("(?is)charset\\s*=\\s*['\\\"]?\\s*([^<>\\s'\\\";]+)")
    private static final Pattern FLEXIBLE_CHARSET_ATTR_PATTERN =
            Pattern.compile(("(?is)\\bcharset\\s*=\\s*(?:['\\\"]\\s*)?([-_:\\.a-z0-9]+)"));
    private static final Charset ASCII = Charset.forName("US-ASCII");
    /**
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlEncodingDetector.java`
#### Snippet
```java
    //("(?is)charset\\s*=\\s*['\\\"]?\\s*([^<>\\s'\\\";]+)")
    private static final Pattern FLEXIBLE_CHARSET_ATTR_PATTERN =
            Pattern.compile(("(?is)\\bcharset\\s*=\\s*(?:['\\\"]\\s*)?([-_:\\.a-z0-9]+)"));
    private static final Charset ASCII = Charset.forName("US-ASCII");
    /**
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java

    private final static Pattern FILE_NAME_PATTERN =
            Pattern.compile("\\A(.*?)(?:\\((\\d+)\\))?\\.([^\\.]+)\\Z");

    @Deprecated
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java

    private static Pattern ALLOWABLE_PAGE_SEPARATORS_PATTERN =
            Pattern.compile("(?i)^[-_/\\.A-Z0-9]+$");

    private static Pattern ALLOWABLE_OTHER_PARAMS_PATTERN =
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java

    private static Pattern ALLOWABLE_OTHER_PARAMS_PATTERN =
            Pattern.compile("(?i)^[-_/\\.A-Z0-9]+$");

    // whether or not to apply rotation calculated by the rotation.py script
```

## RuleId[id=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `jIndex.keySet()` may be replaced with 'entrySet()' iteration
in `tika-parsers/tika-parsers-ml/tika-dl/src/main/java/org/apache/tika/dl/imagerec/DL4JInceptionV3Net.java`
#### Snippet
```java
        JSONObject jIndex = (JSONObject) new JSONParser().parse(content);
        Map<Integer, String> classMap = new HashMap<>();
        for (Object key : jIndex.keySet()) {
            JSONArray names = (JSONArray) jIndex.get(key);
            classMap.put(Integer.parseInt(key.toString()), names.get(names.size() - 1).toString());
```

### KeySetIterationMayUseEntrySet
Iteration over `metadataPatterns.keySet()` may be replaced with 'entrySet()' iteration
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
            while (scanner.hasNextLine()) {
                String line = scanner.nextLine();
                for (Pattern p : metadataPatterns.keySet()) {
                    Matcher m = p.matcher(line);
                    if (m.find()) {
```

### KeySetIterationMayUseEntrySet
Iteration over `contentOrderScraper.locationMap.keySet()` may be replaced with 'entrySet()' iteration
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
        EmbeddedDocumentExtractor embeddedDocumentExtractor =
                EmbeddedDocumentUtil.getEmbeddedDocumentExtractor(context);
        for (String id : contentOrderScraper.locationMap.keySet()) {
            if (!processed.contains(id)) {
                HRefMediaPair hRefMediaPair = contentOrderScraper.locationMap.get(id);
```

### KeySetIterationMayUseEntrySet
Iteration over `HTTP_METHODS.keySet()` may be replaced with 'entrySet()' iteration
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaWelcome.java`
#### Snippet
```java

                for (Annotation a : m.getAnnotations()) {
                    for (Class<? extends Annotation> httpMethAnn : HTTP_METHODS.keySet()) {
                        if (httpMethAnn.isInstance(a)) {
                            httpMethod = HTTP_METHODS.get(httpMethAnn);
```

### KeySetIterationMayUseEntrySet
Iteration over `firstType.keySet()` may be replaced with 'entrySet()' iteration
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaMimeTypes.java`
#### Snippet
```java
        }
        h.append("<ul>");
        for (String section : firstType.keySet()) {
            h.append("<li><a href=\"#").append(firstType.get(section)).append("\">").append(section)
                    .append("</a></li>\n");
```

### KeySetIterationMayUseEntrySet
Iteration over `paragraphs.keySet()` may be replaced with 'entrySet()' iteration
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
                xhtml.endElement("p");
            }
            for (MCID mcid : paragraphs.keySet()) {
                if (!state.processedMCIDs.contains(mcid)) {
                    if (mcid.mcid > -1) {
```

### KeySetIterationMayUseEntrySet
Iteration over `resolvedGeonames.keySet()` may be replaced with 'entrySet()' iteration
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoTag.java`
#### Snippet
```java
    public void toGeoTag(Map<String, List<Location>> resolvedGeonames, String bestNER) {

        for (String key : resolvedGeonames.keySet()) {
            List<Location> cur = resolvedGeonames.get(key);
            if (key.equals(bestNER)) {
```

### KeySetIterationMayUseEntrySet
Iteration over `parameters.keySet()` may be replaced with 'entrySet()' iteration
in `tika-example/src/main/java/org/apache/tika/example/MediaTypeExample.java`
#### Snippet
```java
        Map<String, String> parameters = type.getParameters();
        System.out.println("parameters:");
        for (String name : parameters.keySet()) {
            System.out.println("  " + name + "=" + parameters.get(name));
        }
```

### KeySetIterationMayUseEntrySet
Iteration over `charsetTester.charsetText.keySet()` may be replaced with 'entrySet()' iteration
in `tika-example/src/main/java/org/apache/tika/example/PickBestTextEncodingParser.java`
#### Snippet
```java
                int numEnglish = 0;
                String bestcharset = null;
                for (String pcharset : charsetTester.charsetText.keySet()) {
                    String text = charsetTester.charsetText.get(pcharset);
                    int cEnglish = 0;
```

### KeySetIterationMayUseEntrySet
Iteration over `dict.keySet()` may be replaced with 'entrySet()' iteration
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork13PackageParser.java`
#### Snippet
```java
            if (rootObj instanceof NSDictionary) {
                NSDictionary dict = (NSDictionary)rootObj;
                for (String k : dict.keySet()) {
                    String v = dict.get(k).toString();
                    metadata.set(IWORKS_PREFIX + k, v);
```

### KeySetIterationMayUseEntrySet
Iteration over `languageProbabilities.keySet()` may be replaced with 'entrySet()' iteration
in `tika-langdetect/tika-langdetect-optimaize/src/main/java/org/apache/tika/langdetect/optimaize/OptimaizeLangDetector.java`
#### Snippet
```java
        if (languageProbabilities != null) {
            Map<LdLocale, Double> languageWeights = new HashMap<>(languageProbabilities.size());
            for (String language : languageProbabilities.keySet()) {
                Double priority = (double) languageProbabilities.get(language);
                languageWeights.put(LdLocale.fromString(language), priority);
```

## RuleId[id=FinalPrivateMethod]
### FinalPrivateMethod
'private' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
    }

    private final void writePage() throws SAXException {
        if (canvases.size() == 0) {
            return;
```

### FinalPrivateMethod
'private' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UByte.java`
#### Snippet
```java
     * @return Array of cached values for UByte.
     */
    private static final UByte[] mkValues() {
        UByte[] ret = new UByte[256];

```

### FinalPrivateMethod
'private' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
     * @return Array of cached values for UInteger
     */
    private static final UInteger[] mkValues() {
        int precacheSize = getPrecacheSize();
        UInteger[] ret;
```

### FinalPrivateMethod
'private' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
     * {@link Integer#MAX_VALUE} then Integer#MAX_VALUE will be used.
     */
    private static final int getPrecacheSize() {
        String prop = null;
        long propParsed;
```

### FinalPrivateMethod
'private' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
     * @return if the byte is a char.
     */
    private static final boolean isChar(byte c) {
        return isChar[c & 0xFF];
    }
```

## RuleId[id=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`dateOverrideFormat.trim().length() > 0` can be replaced with '!dateOverrideFormat.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/TikaExcelDataFormatter.java`
#### Snippet
```java

    public void setDateFormatOverride(String dateOverrideFormat) {
        if (dateOverrideFormat != null && dateOverrideFormat.trim().length() > 0) {
            this.dateOverrideFormatString = dateOverrideFormat;
        }
```

### SizeReplaceableByIsEmpty
`subject.length() > 0` can be replaced with '!subject.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/TNEFParser.java`
#### Snippet
```java
        // Set the message subject if known
        String subject = msg.getSubject();
        if (subject != null && subject.length() > 0) {
            metadata.set(TikaCoreProperties.TITLE, subject);
            metadata.set(TikaCoreProperties.SUBJECT, subject);
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/TNEFParser.java`
#### Snippet
```java
        for (Attachment attachment : msg.getAttachments()) {
            String name = attachment.getLongFilename();
            if (name == null || name.length() == 0) {
                name = attachment.getFilename();
            }
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/TNEFParser.java`
#### Snippet
```java
                name = attachment.getFilename();
            }
            if (name == null || name.length() == 0) {
                String ext = attachment.getExtension();
                if (ext != null) {
```

### SizeReplaceableByIsEmpty
`core.size() == 0` can be replaced with 'core.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
                pkg.getRelationshipsByType(PackageRelationshipTypes.CORE_DOCUMENT);
        // Otherwise check for some other Office core document types
        if (core.size() == 0) {
            core = pkg.getRelationshipsByType(PackageRelationshipTypes.STRICT_CORE_DOCUMENT);
        }
```

### SizeReplaceableByIsEmpty
`core.size() == 0` can be replaced with 'core.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
            core = pkg.getRelationshipsByType(PackageRelationshipTypes.STRICT_CORE_DOCUMENT);
        }
        if (core.size() == 0) {
            core = pkg.getRelationshipsByType(PackageRelationshipTypes.VISIO_CORE_DOCUMENT);
        }
```

### SizeReplaceableByIsEmpty
`core.size() == 0` can be replaced with 'core.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
            core = pkg.getRelationshipsByType(PackageRelationshipTypes.VISIO_CORE_DOCUMENT);
        }
        if (core.size() == 0) {
            core = pkg.getRelationshipsByType(XPS_DOCUMENT);
            if (core.size() == 1) {
```

### SizeReplaceableByIsEmpty
`core.size() == 0` can be replaced with 'core.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
        }

        if (core.size() == 0) {
            core = pkg.getRelationshipsByType(
                    "http://schemas.autodesk.com/dwfx/2007/relationships/documentsequence");
```

### SizeReplaceableByIsEmpty
`slide.getComments().size() == 0` can be replaced with 'slide.getComments().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java

    private void handleComments(HSLFSlide slide, XHTMLContentHandler xhtml) throws SAXException {
        if (slide.getComments() == null || slide.getComments().size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`shapes.size() > 0` can be replaced with '!shapes.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java

        List<HSLFShape> shapes = getShapes(notes);
        if (shapes != null && shapes.size() > 0) {
            // Table text
            for (HSLFShape shape : shapes) {
```

### SizeReplaceableByIsEmpty
`text.length() > 0` can be replaced with '!text.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
    private void addTextIfAny(XHTMLContentHandler xhtml, String section, String text)
            throws SAXException {
        if (text != null && text.length() > 0) {
            xhtml.startElement("div", "class", section);
            xhtml.element("p", text);
```

### SizeReplaceableByIsEmpty
`extension.length() > 0` can be replaced with '!extension.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
        // There isn't one in the file, but we need to be able to reference
        //  the picture from the img tag and the embedded resource
        String filename = "image" + pictureNumber + (extension.length() > 0 ? "." + extension : "");

        // Grab the mime type for the picture
```

### SizeReplaceableByIsEmpty
`controls.size() > 0` can be replaced with '!controls.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java

        // Do we need to do something special with this?
        if (controls.size() > 0) {
            StringBuilder text = new StringBuilder(controls.get(0).text());
            for (int j = 1; j < controls.size(); j++) {
```

### SizeReplaceableByIsEmpty
`style.getName().length() > 0` can be replaced with '!style.getName().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
            StyleDescription style =
                    document.getStyleSheet().getStyleDescription(p.getStyleIndex());
            if (style != null && style.getName() != null && style.getName().length() > 0) {
                if (p.isInList()) {
                    numbering = listManager.getFormattedNumber(p);
```

### SizeReplaceableByIsEmpty
`vals.size() > 0` can be replaced with '!vals.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java

            RECIPIENT_TYPE recipientType = RECIPIENT_TYPE.UNSPECIFIED;
            if (vals != null && vals.size() > 0) {
                Object val = vals.get(0).getValue();
                if (val instanceof Integer) {
```

### SizeReplaceableByIsEmpty
`vals.size() > 0` can be replaced with '!vals.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java

            vals = chunks.getProperties().get(MAPIProperty.ADDRTYPE);
            if (vals != null && vals.size() > 0) {
                String val = vals.get(0).toString();
                if (val != null) {
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java

    private void header(XHTMLContentHandler xhtml, String key, String value) throws SAXException {
        if (value != null && value.length() > 0) {
            xhtml.element("dt", key);
            xhtml.element("dd", value);
```

### SizeReplaceableByIsEmpty
`headers.size() > 0` can be replaced with '!headers.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
                metadata.set(TikaCoreProperties.MODIFIED, msg.getMessageDate().getTime());
            } else {
                if (headers != null && headers.size() > 0) {
                    for (Map.Entry<String, String[]> header : headers.entrySet()) {
                        String headerKey = header.getKey();
```

### SizeReplaceableByIsEmpty
`filename.length() > 0` can be replaced with '!filename.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
                    filename = attachment.getAttachFileName().getValue();
                }
                if (filename != null && filename.length() > 0) {
                    xhtml.element("h1", filename);
                }
```

### SizeReplaceableByIsEmpty
`val.size() > 0` can be replaced with '!val.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
                    MAPIProperty.INTERNET_CPID}) {
                List<PropertyValue> val = props.get(prop);
                if (val != null && val.size() > 0) {
                    int codepage = ((PropertyValue.LongPropertyValue) val.get(0)).getValue();
                    String encoding = null;
```

### SizeReplaceableByIsEmpty
`html.length() > 0` can be replaced with '!html.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
        try {
            String html = msg.getHtmlBody();
            if (html != null && html.length() > 0) {
                Charset charset = null;
                try {
```

### SizeReplaceableByIsEmpty
`messageClass.trim().length() == 0` can be replaced with 'messageClass.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
    //Still needed by PSTParser
    public static String getMessageClass(String messageClass) {
        if (messageClass == null || messageClass.trim().length() == 0) {
            return "UNSPECIFIED";
        } else if (messageClass.equalsIgnoreCase("IPM.Note")) {
```

### SizeReplaceableByIsEmpty
`senderAddresType.size() > 0` can be replaced with '!senderAddresType.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
        List<Chunk> senderAddresType = mainChunks.get(MAPIProperty.SENDER_ADDRTYPE);
        String senderAddressTypeString = "";
        if (senderAddresType != null && senderAddresType.size() > 0) {
            senderAddressTypeString = senderAddresType.get(0).toString();
        }
```

### SizeReplaceableByIsEmpty
`unicode.getExtendedRst().getPhoneticText().trim().length() > 0` can be replaced with '!unicode.getExtendedRst().getPhoneticText().trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
                        String phonetic = (unicode != null && unicode.getExtendedRst() != null &&
                                unicode.getExtendedRst().getPhoneticText() != null &&
                                unicode.getExtendedRst().getPhoneticText().trim().length() > 0) ?
                                unicode.getExtendedRst().getPhoneticText() : "";
                        cellString = unicode.getString() + " " + phonetic;
```

### SizeReplaceableByIsEmpty
`text.length() > 0` can be replaced with '!text.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
            if (text != null) {
                text = text.trim();
                if (text.length() > 0) {
                    addCell(record, new TextCell(text));
                }
```

### SizeReplaceableByIsEmpty
`extraTextCells.size() > 0` can be replaced with '!extraTextCells.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java

        private void processExtraText() throws SAXException {
            if (extraTextCells.size() > 0) {
                for (Cell cell : extraTextCells) {
                    handler.startElement("div", "class", "outside");
```

### SizeReplaceableByIsEmpty
`names.size() == 0` can be replaced with 'names.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/POIFSContainerDetector.java`
#### Snippet
```java
     */
    public static MediaType detect(Set<String> names, DirectoryEntry root) {
        if (names == null || names.size() == 0) {
            return OLE;
        }
```

### SizeReplaceableByIsEmpty
`str.length() == 0` can be replaced with 'str.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
     */
    public static boolean isEmpty(String str) {
        return str == null || str.length() == 0;
    }

```

### SizeReplaceableByIsEmpty
`filePath.length() > 0` can be replaced with '!filePath.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFEmbObjHandler.java`
#### Snippet
```java
        } else if (state == EMB_STATE.PICT) {
            String filePath = metadata.get(RTFMetadata.RTF_PICT_META_PREFIX + "wzDescription");
            if (filePath != null && filePath.length() > 0) {
                metadata.set(TikaCoreProperties.EMBEDDED_RELATIONSHIP_ID, filePath);
                metadata.set(TikaCoreProperties.RESOURCE_NAME_KEY, FilenameUtils.getName(filePath));
```

### SizeReplaceableByIsEmpty
`className.length() > 0` can be replaced with '!className.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
        String itemName = readLengthPrefixedAnsiString(is).trim();

        if (className != null && className.length() > 0) {
            metadata.add(RTFMetadata.EMB_CLASS, className);
        }
```

### SizeReplaceableByIsEmpty
`topicName.length() > 0` can be replaced with '!topicName.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
            metadata.add(RTFMetadata.EMB_CLASS, className);
        }
        if (topicName != null && topicName.length() > 0) {
            metadata.add(RTFMetadata.EMB_TOPIC, topicName);
        }
```

### SizeReplaceableByIsEmpty
`itemName.length() > 0` can be replaced with '!itemName.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
            metadata.add(RTFMetadata.EMB_TOPIC, topicName);
        }
        if (itemName != null && itemName.length() > 0) {
            metadata.add(RTFMetadata.EMB_ITEM, itemName);
        }
```

### SizeReplaceableByIsEmpty
`hf.length() > 0` can be replaced with '!hf.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSSFBExcelExtractorDecorator.java`
#### Snippet
```java
    @Override
    protected void extractHeaderFooter(String hf, XHTMLContentHandler xhtml) throws SAXException {
        if (hf.length() > 0) {
            xhtml.element("p", hf);
        }
```

### SizeReplaceableByIsEmpty
`stylesParts.size() > 0` can be replaced with '!stylesParts.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
        PackageRelationshipCollection stylesParts =
                packagePart.getRelationshipsByType(XWPFRelation.STYLES.getRelation());
        if (stylesParts.size() > 0) {
            PackageRelationship stylesRelationShip = stylesParts.getRelationship(0);
            if (stylesRelationShip == null) {
```

### SizeReplaceableByIsEmpty
`pps.size() > 0` can be replaced with '!pps.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
        pps = opcPackage.getPartsByContentType(XWPFRelation.GLOSSARY_DOCUMENT.getContentType());
        if (pps != null) {
            if (pps.size() > 0) {
                xhtml.startElement("div", "class", "glossary");

```

### SizeReplaceableByIsEmpty
`numberingParts.size() > 0` can be replaced with '!numberingParts.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
            PackageRelationshipCollection numberingParts =
                    packagePart.getRelationshipsByType(XWPFRelation.NUMBERING.getRelation());
            if (numberingParts.size() > 0) {
                PackageRelationship numberingRelationShip = numberingParts.getRelationship(0);
                if (numberingRelationShip == null) {
```

### SizeReplaceableByIsEmpty
`pps.size() > 0` can be replaced with '!pps.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
        for (String contentType : MAIN_STORY_PART_RELATIONS) {
            List<PackagePart> pps = opcPackage.getPartsByContentType(contentType);
            if (pps.size() > 0) {
                return pps;
            }
```

### SizeReplaceableByIsEmpty
`sText.length() > 0` can be replaced with '!sText.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSSFExcelExtractorDecorator.java`
#### Snippet
```java
            if (shape instanceof XSSFSimpleShape) {
                String sText = ((XSSFSimpleShape) shape).getText();
                if (sText != null && sText.length() > 0) {
                    xhtml.element("p", sText);
                }
```

### SizeReplaceableByIsEmpty
`content.length() > 0` can be replaced with '!content.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSSFExcelExtractorDecorator.java`
#### Snippet
```java
    protected void extractHeaderFooter(String hf, XHTMLContentHandler xhtml) throws SAXException {
        String content = ExcelExtractor._extractHeaderFooter(new HeaderFooterFromString(hf));
        if (content.length() > 0) {
            xhtml.element("p", content);
        }
```

### SizeReplaceableByIsEmpty
`prc.size() == 0` can be replaced with 'prc.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSExtractorDecorator.java`
#### Snippet
```java

        PackageRelationshipCollection prc = pkg.getRelationshipsByType(XPS_DOCUMENT);
        if (prc.size() == 0) {
            prc = pkg.getRelationshipsByType(OPEN_XPS_DOCUMENT);
        }
```

### SizeReplaceableByIsEmpty
`embeddedImages.size() > 0` can be replaced with '!embeddedImages.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSExtractorDecorator.java`
#### Snippet
```java

        //now handle embedded images
        if (embeddedImages.size() > 0) {
            EmbeddedDocumentUtil embeddedDocumentUtil = new EmbeddedDocumentUtil(context);
            for (Map.Entry<String, Metadata> embeddedImage : embeddedImages.entrySet()) {
```

### SizeReplaceableByIsEmpty
`prc.size() > 0` can be replaced with '!prc.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
                PackageRelationshipCollection prc =
                        pkg.getRelationshipsByType(OOXMLParser.SIGNATURE_RELATIONSHIP);
                if (prc != null && prc.size() > 0) {
                    metadata.set(TikaCoreProperties.HAS_SIGNATURE, "true");
                }
```

### SizeReplaceableByIsEmpty
`packageRelationshipCollection.size() == 0` can be replaced with 'packageRelationshipCollection.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
        PackageRelationshipCollection packageRelationshipCollection = pkg.getRelationshipsByType(
                "http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument");
        if (packageRelationshipCollection.size() == 0) {
            packageRelationshipCollection = pkg.getRelationshipsByType(
                    "http://purl.oclc.org/ooxml/officeDocument/relationships/officeDocument");
```

### SizeReplaceableByIsEmpty
`packageRelationshipCollection.size() == 0` can be replaced with 'packageRelationshipCollection.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
        }

        if (packageRelationshipCollection.size() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`packageRelationshipCollection.size() == 0` can be replaced with 'packageRelationshipCollection.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
        PackageRelationshipCollection packageRelationshipCollection = pkg.getRelationshipsByType(
                "http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument");
        if (packageRelationshipCollection.size() == 0) {
            packageRelationshipCollection = pkg.getRelationshipsByType(
                    "http://purl.oclc.org/ooxml/officeDocument/relationships/officeDocument");
```

### SizeReplaceableByIsEmpty
`packageRelationshipCollection.size() == 0` can be replaced with 'packageRelationshipCollection.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
        }

        if (packageRelationshipCollection.size() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`pps.size() > 0` can be replaced with '!pps.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
        for (String contentType : MAIN_STORY_PART_RELATIONS) {
            List<PackagePart> pps = opcPackage.getPartsByContentType(contentType);
            if (pps.size() > 0) {
                mainDocument = pps.get(0);
                break;
```

### SizeReplaceableByIsEmpty
`slidesPRC.size() > 0` can be replaced with '!slidesPRC.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
        }

        if (slidesPRC != null && slidesPRC.size() > 0) {
            for (int i = 0; i < slidesPRC.size(); i++) {
                try {
```

### SizeReplaceableByIsEmpty
`prc.size() == 0` can be replaced with 'prc.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
                    ExceptionUtils.getStackTrace(e));
        }
        if (prc == null || prc.size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`paragraphStack.size() > 0` can be replaced with '!paragraphStack.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            lazyStartParagraph();
        }
        if (inParagraph || paragraphStack.size() > 0) {
            if (groupState.italic) {
                end("i");
```

### SizeReplaceableByIsEmpty
`paragraphStack.size() > 0` can be replaced with '!paragraphStack.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            boolean badTagAlignment = false;
            if (inList()) {
                if (paragraphStack.size() > 0) {
                    String lastP = paragraphStack.pop();
                    if (lastP.equals(LI)) {
```

### SizeReplaceableByIsEmpty
`paragraphStack.size() > 0` can be replaced with '!paragraphStack.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
                }
            } else {
                if (paragraphStack.size() > 0) {
                    String lastP = paragraphStack.pop();
                    if (P.equals(lastP)) {
```

### SizeReplaceableByIsEmpty
`paragraphStack.size() > 0` can be replaced with '!paragraphStack.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            //dump all tags and start fresh.
            if (badTagAlignment) {
                while (paragraphStack.size() > 0) {
                    end(paragraphStack.pop());
                }
```

### SizeReplaceableByIsEmpty
`paragraphStack.size() > 0` can be replaced with '!paragraphStack.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java

        //close out whatever tags were left
        while (paragraphStack.size() > 0) {
            end(paragraphStack.pop());
        }
```

### SizeReplaceableByIsEmpty
`paragraphStack.size() > 0` can be replaced with '!paragraphStack.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java

        boolean localInParagraph = inParagraph;
        if (paragraphStack.size() > 0 && paragraphStack.contains(P)) {
            localInParagraph = true;
        }
```

### SizeReplaceableByIsEmpty
`groupStates.size() > 0` can be replaced with '!groupStates.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        // closing }s):
        // TODO: log a warning?
        if (groupStates.size() > 0) {
            // Restore group state:
            final GroupState outerGroupState = groupStates.removeLast();
```

### SizeReplaceableByIsEmpty
`paragraphStack.size() > 0` can be replaced with '!paragraphStack.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        if (!ignoreListMarkup) {
            String xl = isUnorderedList(listID) ? UL : OL;
            if (paragraphStack.size() > 0) {
                String p = paragraphStack.pop();
                if (xl.equals(p)) {
```

### SizeReplaceableByIsEmpty
`numberingParts.size() > 0` can be replaced with '!numberingParts.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFEventBasedWordExtractor.java`
#### Snippet
```java
            PackageRelationshipCollection numberingParts =
                    packagePart.getRelationshipsByType(XWPFRelation.NUMBERING.getRelation());
            if (numberingParts.size() > 0) {
                PackageRelationship numberingRelationShip = numberingParts.getRelationship(0);
                if (numberingRelationShip == null) {
```

### SizeReplaceableByIsEmpty
`hyperlinkRun.getAnchor().length() > 0` can be replaced with '!hyperlinkRun.getAnchor().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
                        inHyperlink = true;
                    } else if (hyperlinkRun.getAnchor() != null &&
                            hyperlinkRun.getAnchor().length() > 0) {
                        xhtml.startElement("a", "href", "#" + hyperlinkRun.getAnchor());
                        inHyperlink = true;
```

### SizeReplaceableByIsEmpty
`commentText.length() > 0` can be replaced with '!commentText.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
        XWPFCommentsDecorator comments = new XWPFCommentsDecorator(paragraph, null);
        String commentText = comments.getCommentText();
        if (commentText != null && commentText.length() > 0) {
            xhtml.characters(commentText);
        }
```

### SizeReplaceableByIsEmpty
`footnameText.length() > 0` can be replaced with '!footnameText.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java

        String footnameText = paragraph.getFootnoteText();
        if (footnameText != null && footnameText.length() > 0) {
            xhtml.characters(footnameText + "\n");
        }
```

### SizeReplaceableByIsEmpty
`relatedPartPRC.size() > 0` can be replaced with '!relatedPartPRC.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
                    ExceptionUtils.getStackTrace(e));
        }
        if (relatedPartPRC != null && relatedPartPRC.size() > 0) {
            AttributesImpl attributes = new AttributesImpl();

```

### SizeReplaceableByIsEmpty
`canvases.size() == 0` can be replaced with 'canvases.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java

    private final void writePage() throws SAXException {
        if (canvases.size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`runs.size() == 0` can be replaced with 'runs.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
            String clip = e.getKey();
            List<GlyphRun> runs = e.getValue();
            if (runs.size() == 0) {
                continue;
            }
```

### SizeReplaceableByIsEmpty
`urls.size() > 0` can be replaced with '!urls.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
        //At some point, we could link them back up to their
        //true anchor text.
        if (urls.size() > 0) {
            xhml.startElement(DIV, CLASS, URL_DIV);
            for (String u : urls) {
```

### SizeReplaceableByIsEmpty
`rows.size() == 0` can be replaced with 'rows.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
        float maxY = -1.0f;
        for (GlyphRun glyphRun : glyphRuns) {
            if (rows.size() == 0) {
                List<GlyphRun> row = new ArrayList<>();
                row.add(glyphRun);
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
            value = (value == null) ? "" : value.trim();

            if (ORIGIN_X.equals(lName) && value.length() > 0) {
                try {
                    originX = Float.parseFloat(atts.getValue(i));
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
                    throw new SAXException(e);
                }
            } else if (ORIGIN_Y.equals(lName) && value.length() > 0) {
                try {
                    originY = Float.parseFloat(atts.getValue(i));
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
            } else if (UNICODE_STRING.equals(lName)) {
                unicodeString = atts.getValue(i);
            } else if (BIDI_LEVEL.equals(lName) && value.length() > 0) {
                try {
                    bidilevel = Integer.parseInt(atts.getValue(i));
```

### SizeReplaceableByIsEmpty
`canvasStack.size() > 0` can be replaced with '!canvasStack.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
            originX = (originX == null) ? Integer.MIN_VALUE : originX;
            originY = (originY == null) ? Integer.MAX_VALUE : originY;
            String currentCanvasClip = (canvasStack.size() > 0) ? canvasStack.peek() : NULL_CLIP;
            List<GlyphRun> runs = canvases.get(currentCanvasClip);
            if (runs == null) {
```

### SizeReplaceableByIsEmpty
`dc.getCreators().size() > 0` can be replaced with '!dc.getCreators().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/JempboxExtractor.java`
#### Snippet
```java
            metadata.set(TikaCoreProperties.DESCRIPTION, dc.getDescription());
        }
        if (dc.getCreators() != null && dc.getCreators().size() > 0) {
            metadata.set(TikaCoreProperties.CREATOR, joinCreators(dc.getCreators()));
        }
```

### SizeReplaceableByIsEmpty
`dc.getSubjects().size() > 0` can be replaced with '!dc.getSubjects().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/JempboxExtractor.java`
#### Snippet
```java
            metadata.set(TikaCoreProperties.CREATOR, joinCreators(dc.getCreators()));
        }
        if (dc.getSubjects() != null && dc.getSubjects().size() > 0) {
            for (String keyword : dc.getSubjects()) {
                metadata.add(TikaCoreProperties.SUBJECT, keyword);
```

### SizeReplaceableByIsEmpty
`instanceId.trim().length() > 0` can be replaced with '!instanceId.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/JempboxExtractor.java`
#### Snippet
```java
                        //swallow
                    }
                    if (instanceId != null && instanceId.trim().length() > 0) {
                        //for absent data elements, pass in empty strings so
                        //that parallel arrays will have matching offsets
```

### SizeReplaceableByIsEmpty
`creators.size() == 0` can be replaced with 'creators.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/JempboxExtractor.java`
#### Snippet
```java

    protected static String joinCreators(List<String> creators) {
        if (creators == null || creators.size() == 0) {
            return "";
        }
```

### SizeReplaceableByIsEmpty
`metadataList.size() == 0` can be replaced with 'metadataList.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-s3/src/main/java/org/apache/tika/pipes/emitter/s3/S3Emitter.java`
#### Snippet
```java
    public void emit(String emitKey, List<Metadata> metadataList)
            throws IOException, TikaEmitterException {
        if (metadataList == null || metadataList.size() == 0) {
            throw new TikaEmitterException("metadata list must not be null or of size 0");
        }
```

### SizeReplaceableByIsEmpty
`metadataList.size() == 0` can be replaced with 'metadataList.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-gcs/src/main/java/org/apache/tika/pipes/emitter/gcs/GCSEmitter.java`
#### Snippet
```java
    public void emit(String emitKey, List<Metadata> metadataList)
            throws IOException, TikaEmitterException {
        if (metadataList == null || metadataList.size() == 0) {
            throw new TikaEmitterException("metadata list must not be null or of size 0");
        }
```

### SizeReplaceableByIsEmpty
`path.length() == 0` can be replaced with 'path.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/io/FilenameUtils.java`
#### Snippet
```java
    public static String getName(final String path) {

        if (path == null || path.length() == 0) {
            return StringUtils.EMPTY;
        }
```

### SizeReplaceableByIsEmpty
`cellIDMappings.get().size() == 0` can be replaced with 'cellIDMappings.get().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
        }

        if (cellIDMappings.get().size() == 0) {
            return false;
        }
```

### SizeReplaceableByIsEmpty
`revisionIDMappings.get().size() == 0` can be replaced with 'revisionIDMappings.get().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
        }

        if (revisionIDMappings.get().size() == 0) {
            return false;
        }
```

### SizeReplaceableByIsEmpty
`title.length() > 0` can be replaced with '!title.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/XHTMLContentHandler.java`
#### Snippet
```java
            super.startElement(XHTML, "title", "title", EMPTY_ATTRIBUTES);
            String title = metadata.get(TikaCoreProperties.TITLE);
            if (title != null && title.length() > 0) {
                char[] titleChars = title.toCharArray();
                super.characters(titleChars, 0, titleChars.length);
```

### SizeReplaceableByIsEmpty
`characters.length() > 0` can be replaced with '!characters.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/XHTMLContentHandler.java`
#### Snippet
```java

    public void characters(String characters) throws SAXException {
        if (characters != null && characters.length() > 0) {
            characters(characters.toCharArray(), 0, characters.length());
        }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/XHTMLContentHandler.java`
#### Snippet
```java
     */
    public void element(String name, String value) throws SAXException {
        if (value != null && value.length() > 0) {
            startElement(name);
            characters(value);
```

### SizeReplaceableByIsEmpty
`title.length() > 0` can be replaced with '!title.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/Link.java`
#### Snippet
```java
            builder.append("<img src=\"");
            builder.append(uri);
            if (title != null && title.length() > 0) {
                builder.append("\" title=\"");
                builder.append(title);
```

### SizeReplaceableByIsEmpty
`text.length() > 0` can be replaced with '!text.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/Link.java`
#### Snippet
```java
                builder.append(title);
            }
            if (text != null && text.length() > 0) {
                builder.append("\" alt=\"");
                builder.append(text);
```

### SizeReplaceableByIsEmpty
`title.length() > 0` can be replaced with '!title.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/Link.java`
#### Snippet
```java
            builder.append(" href=\"");
            builder.append(uri);
            if (title != null && title.length() > 0) {
                builder.append("\" title=\"");
                builder.append(title);
```

### SizeReplaceableByIsEmpty
`rel.length() > 0` can be replaced with '!rel.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/Link.java`
#### Snippet
```java
                builder.append(title);
            }
            if (rel != null && rel.length() > 0) {
                builder.append("\" rel=\"");
                builder.append(rel);
```

### SizeReplaceableByIsEmpty
`content.trim().length() > 0` can be replaced with '!content.trim().isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/RecursiveParserWrapperHandler.java`
#### Snippet
```java
        } else {
            String content = handler.toString();
            if (content != null && content.trim().length() > 0) {
                metadata.add(TikaCoreProperties.TIKA_CONTENT, content);
                metadata.add(TikaCoreProperties.TIKA_CONTENT_HANDLER,
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
            write("xmlns");
            String prefix = entry.getValue();
            if (prefix.length() > 0) {
                write(':');
                write(prefix);
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
        public String getQName(String uri, String localName) throws SAXException {
            String prefix = getPrefix(uri);
            if (prefix.length() > 0) {
                return prefix + ":" + localName;
            } else {
```

### SizeReplaceableByIsEmpty
`uri.length() == 0` can be replaced with 'uri.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
            } else if (parent != null) {
                return parent.getPrefix(uri);
            } else if (uri == null || uri.length() == 0) {
                return "";
            } else {
```

### SizeReplaceableByIsEmpty
`xpath.length() == 0` can be replaced with 'xpath.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/xpath/XPathParser.java`
#### Snippet
```java
        } else if (xpath.equals("/@*")) {
            return AttributeMatcher.INSTANCE;
        } else if (xpath.length() == 0) {
            return ElementMatcher.INSTANCE;
        } else if (xpath.startsWith("/@")) {
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/sax/ElementMappingContentHandler.java`
#### Snippet
```java
    protected static final String getQNameAsString(QName qname) {
        String prefix = qname.getPrefix();
        if (prefix.length() > 0) {
            return prefix + ":" + qname.getLocalPart();
        } else {
```

### SizeReplaceableByIsEmpty
`clauses.size() == 0` can be replaced with 'clauses.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/mime/MinShouldMatchClause.java`
#### Snippet
```java
    MinShouldMatchClause(int min, List<Clause> clauses) {

        if (clauses == null || clauses.size() == 0) {
            throw new IllegalArgumentException("clauses must be not null with size > 0");
        }
```

### SizeReplaceableByIsEmpty
`exceptions.size() > 0` can be replaced with '!exceptions.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesParser.java`
#### Snippet
```java
            }
        }
        if (exceptions.size() > 0) {
            throw exceptions.get(0);
        }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesReader.java`
#### Snippet
```java
        } else if (MAGIC_TAG.equals(qName)) {
            String value = attributes.getValue(MAGIC_PRIORITY_ATTR);
            if (value != null && value.length() > 0) {
                priority = Integer.parseInt(value);
            } else {
```

### SizeReplaceableByIsEmpty
`pipesReporters.size() == 0` can be replaced with 'pipesReporters.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/CompositePipesReporter.java`
#### Snippet
```java
            throw new TikaConfigException("must specify 'pipesReporters'");
        }
        if (pipesReporters.size() == 0) {
            throw new TikaConfigException("must specify at least one pipes reporter");
        }
```

### SizeReplaceableByIsEmpty
`emitterMap.size() == 0` can be replaced with 'emitterMap.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/emitter/EmitterManager.java`
#### Snippet
```java
     */
    public Emitter getEmitter() {
        if (emitterMap.size() == 0) {
            throw new IllegalArgumentException("emitters size must == 1 for the no arg call");
        }
```

### SizeReplaceableByIsEmpty
`includes.size() > 0` can be replaced with '!includes.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesReporterBase.java`
#### Snippet
```java
    private StatusFilter buildStatusFilter(Set<PipesResult.STATUS> includes,
                                           Set<PipesResult.STATUS> excludes) throws TikaConfigException {
        if (includes.size() > 0 && excludes.size() > 0) {
            throw new TikaConfigException("Only one of includes and excludes may have any " +
                    "contents");
```

### SizeReplaceableByIsEmpty
`excludes.size() > 0` can be replaced with '!excludes.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesReporterBase.java`
#### Snippet
```java
    private StatusFilter buildStatusFilter(Set<PipesResult.STATUS> includes,
                                           Set<PipesResult.STATUS> excludes) throws TikaConfigException {
        if (includes.size() > 0 && excludes.size() > 0) {
            throw new TikaConfigException("Only one of includes and excludes may have any " +
                    "contents");
```

### SizeReplaceableByIsEmpty
`includes.size() > 0` can be replaced with '!includes.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesReporterBase.java`
#### Snippet
```java
                    "contents");
        }
        if (includes.size() > 0) {
            return new IncludesFilter(includes);
        } else if (excludes.size() > 0) {
```

### SizeReplaceableByIsEmpty
`excludes.size() > 0` can be replaced with '!excludes.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesReporterBase.java`
#### Snippet
```java
        if (includes.size() > 0) {
            return new IncludesFilter(includes);
        } else if (excludes.size() > 0) {
            return new ExcludesFilter(excludes);
        }
```

### SizeReplaceableByIsEmpty
`fetcherMap.size() == 0` can be replaced with 'fetcherMap.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/FetcherManager.java`
#### Snippet
```java
     */
    public Fetcher getFetcher() {
        if (fetcherMap.size() == 0) {
            throw new IllegalArgumentException("fetchers size must == 1 for the no arg call");
        }
```

### SizeReplaceableByIsEmpty
`name.trim().length() == 0` can be replaced with 'name.trim().isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/FetcherManager.java`
#### Snippet
```java
        for (Fetcher fetcher : fetchers) {
            String name = fetcher.getName();
            if (name == null || name.trim().length() == 0) {
                throw new TikaConfigException("fetcher name must not be blank");
            }
```

### SizeReplaceableByIsEmpty
`basePath.toString().trim().length() == 0` can be replaced with 'basePath.toString().trim().isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/fs/FileSystemFetcher.java`
#### Snippet
```java
    public void checkInitialization(InitializableProblemHandler problemHandler)
            throws TikaConfigException {
        if (basePath == null || basePath.toString().trim().length() == 0) {
            LOG.warn("'basePath' has not been set. " +
                    "This means that client code or clients can read from any file that this " +
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
            }
        }
        return name.length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`string.length() == 0` can be replaced with 'string.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    private static Map<String, String> parseParameters(String string) {
        if (string.length() == 0) {
            return Collections.emptyMap();
        }
```

### SizeReplaceableByIsEmpty
`string.length() > 0` can be replaced with '!string.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
        //  doesn't fully handle quoted keys or values (eg containing ; or =)
        Map<String, String> parameters = new HashMap<>();
        while (string.length() > 0) {
            String key = string;
            String value = "";
```

### SizeReplaceableByIsEmpty
`key.length() > 0` can be replaced with '!key.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

            key = key.trim();
            if (key.length() > 0) {
                parameters.put(key, unquote(value.trim()));
            }
```

### SizeReplaceableByIsEmpty
`content.length() == 0` can be replaced with 'content.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/utils/RegexUtils.java`
#### Snippet
```java
     */
    public static List<String> extractLinks(String content) {
        if (content == null || content.length() == 0) {
            return Collections.emptyList();
        }
```

### SizeReplaceableByIsEmpty
`metadataList.size() == 0` can be replaced with 'metadataList.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java

    private boolean metadataIsEmpty(List<Metadata> metadataList) {
        return metadataList == null || metadataList.size() == 0;
    }

```

### SizeReplaceableByIsEmpty
`s.trim().length() == 0` can be replaced with 's.trim().isEmpty()'
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java

    public static boolean isBlank(final String s) {
        return s == null || s.trim().length() == 0;
    }

```

### SizeReplaceableByIsEmpty
`lines.size() == 0` can be replaced with 'lines.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java

    public static String joinWith(String delimiter, List<String> lines) {
        if (lines.size() == 0) {
            return EMPTY;
        }
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/detect/NameDetector.java`
#### Snippet
```java
            // Skip any leading or trailing whitespace
            name = name.trim();
            if (name.length() > 0) {
                // Match the name against the registered patterns
                for (Map.Entry<Pattern, MediaType> entry : patterns.entrySet()) {
```

### SizeReplaceableByIsEmpty
`line.length() > 0` can be replaced with '!line.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/config/ServiceLoader.java`
#### Snippet
```java
                line = COMMENT.matcher(line).replaceFirst("");
                line = WHITESPACE.matcher(line).replaceAll("");
                if (line.length() > 0) {
                    names.add(line);
                }
```

### SizeReplaceableByIsEmpty
`detectors.size() > 0` can be replaced with '!detectors.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/detect/DefaultDetector.java`
#### Snippet
```java
        if (loader != null && loader.isDynamic()) {
            List<Detector> detectors = loader.loadDynamicServiceProviders(Detector.class);
            if (detectors.size() > 0) {
                detectors.addAll(super.getDetectors());
                return detectors;
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/parser/ParsingReader.java`
#### Snippet
```java
    private static Metadata getMetadata(String name) {
        Metadata metadata = new Metadata();
        if (name != null && name.length() > 0) {
            metadata.set(TikaCoreProperties.RESOURCE_NAME_KEY, name);
        }
```

### SizeReplaceableByIsEmpty
`supportedTypes.size() == 0` can be replaced with 'supportedTypes.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/parser/external2/ExternalParser.java`
#### Snippet
```java
    public void checkInitialization(InitializableProblemHandler problemHandler)
            throws TikaConfigException {
        if (supportedTypes.size() == 0) {
            throw new TikaConfigException("supportedTypes size must be > 0");
        }
```

### SizeReplaceableByIsEmpty
`this.supportedTypes.size() > 0` can be replaced with '!this.supportedTypes.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/parser/external2/ExternalParser.java`
#### Snippet
```java
    @Field
    public void setSupportedTypes(List<String> supportedTypes) {
        if (this.supportedTypes.size() > 0) {
            throw new IllegalStateException("can't set supportedTypes after initialization");
        }
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/extractor/ParsingEmbeddedDocumentExtractor.java`
#### Snippet
```java

        String name = metadata.get(TikaCoreProperties.RESOURCE_NAME_KEY);
        if (writeFileNameToContent && name != null && name.length() > 0 && outputHtml) {
            handler.startElement(XHTML, "h1", "h1", new AttributesImpl());
            char[] chars = name.toCharArray();
```

### SizeReplaceableByIsEmpty
`jsonArray.size() != 0` can be replaced with '!jsonArray.isEmpty()'
in `tika-langdetect/tika-langdetect-mitll-text/src/main/java/org/apache/tika/langdetect/mitll/TextLangDetector.java`
#### Snippet
```java
            String json = response.readEntity(String.class);
            JsonNode jsonArray = new ObjectMapper().readTree(json).get("all_languages");
            return jsonArray.size() != 0;
        } catch (Exception e) {
            LOG.warn("Can't run", e);
```

### SizeReplaceableByIsEmpty
`initializableProblemHandler.length() == 0` can be replaced with 'initializableProblemHandler.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
    private static InitializableProblemHandler getInitializableProblemHandler(
            String initializableProblemHandler) throws TikaConfigException {
        if (initializableProblemHandler == null || initializableProblemHandler.length() == 0) {
            return InitializableProblemHandler.DEFAULT;
        }
```

### SizeReplaceableByIsEmpty
`initProbHandler.length() == 0` can be replaced with 'initProbHandler.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            String initProbHandler = element.getAttribute("initializableProblemHandler");
            InitializableProblemHandler initializableProblemHandler;
            if (initProbHandler == null || initProbHandler.length() == 0) {
                initializableProblemHandler = loader.getInitializableProblemHandler();
            } else {
```

### SizeReplaceableByIsEmpty
`excludeParsers.size() == 0` can be replaced with 'excludeParsers.isEmpty()'
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                try {
                    CompositeParser cp = null;
                    if (childParsers.size() == 1 && excludeParsers.size() == 0 &&
                            childParsers.get(0) instanceof CompositeParser) {
                        cp = (CompositeParser) childParsers.get(0);
```

### SizeReplaceableByIsEmpty
`tikaEndpoints.size() == 0` can be replaced with 'tikaEndpoints.isEmpty()'
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaServerClientConfig.java`
#### Snippet
```java
    public void checkInitialization(InitializableProblemHandler problemHandler)
            throws TikaConfigException {
        if (tikaEndpoints.size() == 0) {
            throw new TikaConfigException("tikaEndpoints must not be empty");
        }
```

### SizeReplaceableByIsEmpty
`metadataList.size() == 0` can be replaced with 'metadataList.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-opensearch/src/main/java/org/apache/tika/pipes/emitter/opensearch/OpenSearchEmitter.java`
#### Snippet
```java
    public void emit(String emitKey, List<Metadata> metadataList)
            throws IOException, TikaEmitterException {
        if (metadataList == null || metadataList.size() == 0) {
            LOG.debug("metadataList is null or empty");
            return;
```

### SizeReplaceableByIsEmpty
`emitData.size() == 0` can be replaced with 'emitData.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-opensearch/src/main/java/org/apache/tika/pipes/emitter/opensearch/OpenSearchEmitter.java`
#### Snippet
```java
    @Override
    public void emit(List<? extends EmitData> emitData) throws IOException, TikaEmitterException {
        if (emitData == null || emitData.size() == 0) {
            LOG.debug("metadataList is null or empty");
            return;
```

### SizeReplaceableByIsEmpty
`comments.size() > 0` can be replaced with '!comments.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-font-module/src/main/java/org/apache/tika/parser/font/AdobeFontMetricParser.java`
#### Snippet
```java

        // Display the comments
        if (comments.size() > 0) {
            xhtml.element("h1", "Comments");
            xhtml.startElement("div", "class", "comments");
```

### SizeReplaceableByIsEmpty
`result.getEmitData().getMetadataList().size() > 0` can be replaced with '!result.getEmitData().getMetadataList().isEmpty()'
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-opensearch/src/main/java/org/apache/tika/pipes/reporters/opensearch/OpenSearchPipesReporter.java`
#### Snippet
```java
        metadata.set(parseTimeKey, Long.toString(elapsed));
        if (result.getEmitData() != null && result.getEmitData().getMetadataList() != null &&
                result.getEmitData().getMetadataList().size() > 0) {
            Metadata m = result.getEmitData().getMetadataList().get(0);
            if (m.get(ExternalProcess.EXIT_VALUE) != null) {
```

### SizeReplaceableByIsEmpty
`includeStatus.size() > 0` can be replaced with '!includeStatus.isEmpty()'
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-opensearch/src/main/java/org/apache/tika/pipes/reporters/opensearch/OpenSearchPipesReporter.java`
#### Snippet
```java

    private boolean shouldReport(PipesResult result) {
        if (includeStatus.size() > 0) {
            if (includeStatus.contains(result.getStatus().name())) {
                return true;
```

### SizeReplaceableByIsEmpty
`excludeStatus.size() > 0` can be replaced with '!excludeStatus.isEmpty()'
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-opensearch/src/main/java/org/apache/tika/pipes/reporters/opensearch/OpenSearchPipesReporter.java`
#### Snippet
```java
            return false;
        }
        if (excludeStatus.size() > 0 && excludeStatus.contains(result.getStatus().name())) {
            return false;
        }
```

### SizeReplaceableByIsEmpty
`probabilities.size() > 0` can be replaced with '!probabilities.isEmpty()'
in `tika-server/tika-server-eval/src/main/java/org/apache/tika/server/eval/TikaEvalResource.java`
#### Snippet
```java
        List<LanguageResult> probabilities =
                (List<LanguageResult>) results.get(LanguageIDWrapper.class);
        if (probabilities.size() > 0) {
            stats.put(LANGUAGE.getName() + suffix, probabilities.get(0).getLanguage());
            stats.put(LANGUAGE_CONFIDENCE.getName() + suffix, probabilities.get(0).getRawScore());
```

### SizeReplaceableByIsEmpty
`metadataList.size() == 0` can be replaced with 'metadataList.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
    public void emit(String emitKey, List<Metadata> metadataList)
            throws IOException, TikaEmitterException {
        if (metadataList == null || metadataList.size() == 0) {
            LOG.warn("metadataList is null or empty");
            return;
```

### SizeReplaceableByIsEmpty
`batch.size() == 0` can be replaced with 'batch.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
    @Override
    public void emit(List<? extends EmitData> batch) throws IOException, TikaEmitterException {
        if (batch == null || batch.size() == 0) {
            LOG.warn("batch is null or empty");
            return;
```

### SizeReplaceableByIsEmpty
`comments.size() > 0` can be replaced with '!comments.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
        for (ID3Tags tag : tags) {
            List<ID3Comment> comments = tag.getComments();
            if (comments != null && comments.size() > 0) {
                return comments;
            }
```

### SizeReplaceableByIsEmpty
`headers.size() == 0` can be replaced with 'headers.isEmpty()'
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-jdbc/src/main/java/org/apache/tika/pipes/pipesiterator/jdbc/JDBCPipesIterator.java`
#### Snippet
```java
            try (ResultSet rs = st.executeQuery(select)) {
                while (rs.next()) {
                    if (headers.size() == 0) {
                        fetchEmitKeyIndices = loadHeaders(rs.getMetaData(), headers);
                        checkFetchEmitValidity(fetcherName, emitterName, fetchEmitKeyIndices,
```

### SizeReplaceableByIsEmpty
`lyricsText.length() > 0` can be replaced with '!lyricsText.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/LyricsHandler.java`
#### Snippet
```java

    public boolean hasLyrics() {
        return lyricsText != null && lyricsText.length() > 0;
    }
}
```

### SizeReplaceableByIsEmpty
`dynamicDetectors.size() > 0` can be replaced with '!dynamicDetectors.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
            List<ZipContainerDetector> dynamicDetectors =
                    loader.loadDynamicServiceProviders(ZipContainerDetector.class);
            if (dynamicDetectors.size() > 0) {
                List<ZipContainerDetector> zipDetectors = new ArrayList<>(staticZipDetectors);
                zipDetectors.addAll(dynamicDetectors);
```

### SizeReplaceableByIsEmpty
`parts.size() > 0` can be replaced with '!parts.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
        // if we're in a part, skip.
        // We want to gather only the metadata for the whole msg.
        if (parts.size() > 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`parts.size() > 0` can be replaced with '!parts.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java

        // TIKA-2455: flag the containing type.
        if (parts.size() > 0) {
            submd.set(Message.MULTIPART_SUBTYPE, parts.peek().getSubType());
            submd.set(Message.MULTIPART_BOUNDARY, parts.peek().getBoundary());
```

### SizeReplaceableByIsEmpty
`alternativePartBuffer.size() > 0` can be replaced with '!alternativePartBuffer.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
        //if we're in a multipart/alternative or any one of its children
        //add the bodypart to the latest that was added
        if (!extractAllAlternatives && alternativePartBuffer.size() > 0) {
            UnsynchronizedByteArrayOutputStream bos = new UnsynchronizedByteArrayOutputStream();
            IOUtils.copy(is, bos);
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
            Mailbox mailbox = (Mailbox) address;
            String name = mailbox.getName();
            if (name != null && name.length() > 0) {
                name = DecoderUtil.decodeEncodedWords(name, DecodeMonitor.SILENT);
                return name + " <" + mailbox.getAddress() + ">";
```

### SizeReplaceableByIsEmpty
`alternativePartBuffer.size() > 0` can be replaced with '!alternativePartBuffer.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
        //if we're buffering for a multipart/alternative
        //don't write </p></div>
        if (alternativePartBuffer.size() > 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`parts.size() > 0` can be replaced with '!parts.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
        //e.g. more endMultiPart than startMultipart
        //we're currently silently swallowing this
        if (parts.size() > 0) {
            parts.pop();
        }
```

### SizeReplaceableByIsEmpty
`alternativePartBuffer.size() > 0` can be replaced with '!alternativePartBuffer.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
        //if we're buffering for a multipart/alternative
        //don't write <div><p>
        if (alternativePartBuffer.size() > 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`alternativePartBuffer.size() == 0` can be replaced with 'alternativePartBuffer.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
        parts.push(descr);
        if (!extractAllAlternatives) {
            if (alternativePartBuffer.size() == 0 &&
                    MULTIPART_ALTERNATIVE.equalsIgnoreCase(descr.getMimeType())) {
                Part part = new Part(descr);
```

### SizeReplaceableByIsEmpty
`alternativePartBuffer.size() > 0` can be replaced with '!alternativePartBuffer.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
                Part part = new Part(descr);
                alternativePartBuffer.push(part);
            } else if (alternativePartBuffer.size() > 0) {
                //add the part to the stack
                Part parent = alternativePartBuffer.peek();
```

### SizeReplaceableByIsEmpty
`firstRows.size() > 0` can be replaced with '!firstRows.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFParser.java`
#### Snippet
```java

        //now write cached rows
        while (firstRows.size() > 0) {
            DBFRow cachedRow = firstRows.remove(0);
            writeRow(cachedRow, charset, xhtml);
```

### SizeReplaceableByIsEmpty
`subjects.size() > 0` can be replaced with '!subjects.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentMetaParser.java`
#### Snippet
```java
        }

        if (subjects.size() > 0) {
            metadata.set(TikaCoreProperties.SUBJECT, subjects.toArray(new String[0]));
        }
```

### SizeReplaceableByIsEmpty
`contentOrderScraper.contentItems.size() == 0` can be replaced with 'contentOrderScraper.contentItems.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
        }
        //if no content items, false
        if (contentOrderScraper.contentItems.size() == 0) {
            return false;
        }
```

### SizeReplaceableByIsEmpty
`exceptions.size() > 0` can be replaced with '!exceptions.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentParser.java`
#### Snippet
```java
        } while (entry != null);

        if (exceptions.size() > 0) {
            throw exceptions.get(0);
        }
```

### SizeReplaceableByIsEmpty
`names.size() == 0` can be replaced with 'names.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/detect/ole/MiscOLEDetector.java`
#### Snippet
```java
     */
    protected static MediaType detect(Set<String> names, DirectoryEntry root) {
        if (names == null || names.size() == 0) {
            return OLE;
        } else if (names.contains("\u0005HwpSummaryInformation")) {
```

### SizeReplaceableByIsEmpty
`e.getValue().length() > 0` can be replaced with '!e.getValue().isEmpty()'
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
        for (Map.Entry<String, String> e : jvmOpts.entrySet()) {
            fullCommand.add(e.getKey());
            if (e.getValue().length() > 0) {
                fullCommand.add(commandLineSafe(e.getValue()));
            }
```

### SizeReplaceableByIsEmpty
`e.getValue().length() > 0` can be replaced with '!e.getValue().isEmpty()'
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
        for (Map.Entry<String, String> e : processArgs.entrySet()) {
            fullCommand.add(e.getKey());
            if (e.getValue().length() > 0) {
                fullCommand.add(commandLineSafe(e.getValue()));
            }
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java
            entrydata.set(Metadata.CONTENT_LENGTH, Long.toString(size));
        }
        if (name != null && name.length() > 0) {
            name = name.replace("\\", "/");
            entrydata.set(TikaCoreProperties.RESOURCE_NAME_KEY, name);
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java
                        parentMetadata);
            }
            if (name.length() > 0) {
                xhtml.element("p", name);
            }
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/AttributeDependantMetadataHandler.java`
#### Snippet
```java

    public void addMetadata(String value) {
        if (name == null || name.length() == 0) {
            // We didn't find the attribute which holds the name
            return;
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/AttributeDependantMetadataHandler.java`
#### Snippet
```java
            return;
        }
        if (value.length() > 0) {
            String previous = metadata.get(name);
            if (previous != null && previous.length() > 0) {
```

### SizeReplaceableByIsEmpty
`previous.length() > 0` can be replaced with '!previous.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/AttributeDependantMetadataHandler.java`
#### Snippet
```java
        if (value.length() > 0) {
            String previous = metadata.get(name);
            if (previous != null && previous.length() > 0) {
                value = previous + ", " + value;
            }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/AbstractMetadataHandler.java`
#### Snippet
```java
     */
    protected void addMetadata(String value) {
        if (value != null && value.length() > 0) {
            if (metadata.isMultiValued(name)) {
                // Add the value, assuming it's not already there
```

### SizeReplaceableByIsEmpty
`previous.length() > 0` can be replaced with '!previous.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/AbstractMetadataHandler.java`
#### Snippet
```java
                // Set the value, assuming it's not already there
                String previous = metadata.get(name);
                if (previous != null && previous.length() > 0) {
                    if (!previous.equals(value)) {
                        if (property != null) {
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/CompressorParser.java`
#### Snippet
```java
                        name.endsWith(".zlib") || name.endsWith(".pack") || name.endsWith(".br")) {
                    name = name.substring(0, name.lastIndexOf("."));
                } else if (name.length() > 0) {
                    name = GzipUtils.getUncompressedFilename(name);
                }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/MetadataHandler.java`
#### Snippet
```java

    public void addMetadata(String value) {
        if (value.length() > 0) {
            String previous = metadata.get(name);
            if (previous != null && previous.length() > 0) {
```

### SizeReplaceableByIsEmpty
`previous.length() > 0` can be replaced with '!previous.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/MetadataHandler.java`
#### Snippet
```java
        if (value.length() > 0) {
            String previous = metadata.get(name);
            if (previous != null && previous.length() > 0) {
                value = previous + ", " + value;
            }
```

### SizeReplaceableByIsEmpty
`valueBagless.length() > 0` can be replaced with '!valueBagless.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/ElementMetadataHandler.java`
#### Snippet
```java
            if (matchLevel == 0 && isBagless) {
                String valueBagless = bufferBagless.toString();
                if (valueBagless.length() > 0 && !valueBagless.contains(LOCAL_NAME_RDF_BAG)) {
                    // we're in a standard element, add the bagless buffer
                    addMetadata(valueBagless.trim());
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/ElementMetadataHandler.java`
#### Snippet
```java
        LOG.trace("adding {}={}", name, value);
        if (targetProperty != null && targetProperty.isMultiValuePermitted()) {
            if ((value != null && value.length() > 0) || allowEmptyValues) {
                if (value == null || value.length() == 0 && allowEmptyValues) {
                    value = "";
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/ElementMetadataHandler.java`
#### Snippet
```java
        if (targetProperty != null && targetProperty.isMultiValuePermitted()) {
            if ((value != null && value.length() > 0) || allowEmptyValues) {
                if (value == null || value.length() == 0 && allowEmptyValues) {
                    value = "";
                }
```

### SizeReplaceableByIsEmpty
`dirPath.length() == 0` can be replaced with 'dirPath.isEmpty()'
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            //if the user accidentally doesn't include
            //a directory, set the directory to the cwd
            if (dirPath.length() == 0) {
                dirPath = ".";
            }
```

### SizeReplaceableByIsEmpty
`rv.toString().length() > 0` can be replaced with '!rv.toString().isEmpty()'
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
                    .showInputDialog(this, "Enter the URL of the resource to be parsed:",
                            "Open URL", JOptionPane.PLAIN_MESSAGE, null, null, "");
            if (rv != null && rv.toString().length() > 0) {
                try {
                    openURL(new URL(rv.toString().trim()));
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java

        String name = md.get(TikaCoreProperties.RESOURCE_NAME_KEY);
        if (name != null && name.length() > 0) {
            setTitle("Apache Tika: " + name);
        } else {
```

### SizeReplaceableByIsEmpty
`supportedFetchers.size() == 0` can be replaced with 'supportedFetchers.isEmpty()'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
            Set<String> supportedFetchers = fetcherManager.getSupported();
            sb.append("enableSecureFeatures has been selected.\n");
            if (supportedFetchers.size() == 0) {
                sb.append("There are no fetchers specified in the TikaConfig");
            } else {
```

### SizeReplaceableByIsEmpty
`supportedFetchers.size() == 0` can be replaced with 'supportedFetchers.isEmpty()'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
            }
            Set<String> emitters = emitterManager.getSupported();
            if (supportedFetchers.size() == 0) {
                sb.append("There are no emitters specified in the TikaConfig");
            } else {
```

### SizeReplaceableByIsEmpty
`emitterManager.getSupported().size() > 0` can be replaced with '!emitterManager.getSupported().isEmpty()'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
            LOG.info(sb.toString());
        } else {
            if (emitterManager.getSupported().size() > 0) {
                String warn =
                        "enableUnsecureFeatures has not been set to 'true' in the server " +
```

### SizeReplaceableByIsEmpty
`emitterManager.getSupported().size() > 0` can be replaced with '!emitterManager.getSupported().isEmpty()'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
                LOG.warn(warn);
            }
            if (emitterManager.getSupported().size() > 0) {
                String warn =
                        "enableUnsecureFeatures has not been set to 'true' in the server " +
```

### SizeReplaceableByIsEmpty
`tikaServerConfig.getEndpoints().size() == 0` can be replaced with 'tikaServerConfig.getEndpoints().isEmpty()'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
        boolean addAsyncResource = false;
        boolean addPipesResource = false;
        if (tikaServerConfig.getEndpoints().size() == 0) {
            resourceProviders.add(new SingletonResourceProvider(new MetadataResource()));
            resourceProviders.add(new SingletonResourceProvider(new RecursiveMetadataResource()));
```

### SizeReplaceableByIsEmpty
`tikaServerConfig.getSupportedFetchers().size() > 0` can be replaced with '!tikaServerConfig.getSupportedFetchers().isEmpty()'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
                //specified.  It is possible that users may only specify fetchers
                //for legacy endpoints.
                if (tikaServerConfig.getSupportedFetchers().size() > 0 &&
                        tikaServerConfig.getSupportedEmitters().size() > 0) {
                    addAsyncResource = true;
```

### SizeReplaceableByIsEmpty
`tikaServerConfig.getSupportedEmitters().size() > 0` can be replaced with '!tikaServerConfig.getSupportedEmitters().isEmpty()'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
                //for legacy endpoints.
                if (tikaServerConfig.getSupportedFetchers().size() > 0 &&
                        tikaServerConfig.getSupportedEmitters().size() > 0) {
                    addAsyncResource = true;
                    addPipesResource = true;
```

### SizeReplaceableByIsEmpty
`engineExceptions.size() > 0` can be replaced with '!engineExceptions.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDF2XHTML.java`
#### Snippet
```java
        engine.run();
        List<IOException> engineExceptions = engine.getExceptions();
        if (engineExceptions.size() > 0) {
            IOException first = engineExceptions.remove(0);
            if (config.isCatchIntermediateIOExceptions()) {
```

### SizeReplaceableByIsEmpty
`pdf2XHTML.exceptions.size() > 0` can be replaced with '!pdf2XHTML.exceptions.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDF2XHTML.java`
#### Snippet
```java
            }
        }
        if (pdf2XHTML.exceptions.size() > 0) {
            //throw the first
            throw new TikaException("Unable to extract PDF content", pdf2XHTML.exceptions.get(0));
```

### SizeReplaceableByIsEmpty
`ocr2XHTML.exceptions.size() > 0` can be replaced with '!ocr2XHTML.exceptions.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/OCR2XHTML.java`
#### Snippet
```java
            }
        }
        if (ocr2XHTML.exceptions.size() > 0) {
            //throw the first
            throw new TikaException("Unable to extract all PDF content",
```

### SizeReplaceableByIsEmpty
`value.trim().length() > 0` can be replaced with '!value.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java

    private static void setNotNull(Property property, String value, Metadata metadata) {
        if (metadata.get(property) == null && value != null && value.trim().length() > 0) {
            metadata.set(property, decode(value));
        }
```

### SizeReplaceableByIsEmpty
`pdfBoxBaseline.length() > 0` can be replaced with '!pdfBoxBaseline.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        //if schema is null, just go with pdfBoxBaseline
        if (schema == null) {
            if (pdfBoxBaseline != null && pdfBoxBaseline.length() > 0) {
                addMetadata(metadata, property, pdfBoxBaseline);
            }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
            String value = schema.getLanguageProperty(property.getName(), lang);

            if (value != null && value.length() > 0) {
                //if you're going to add it below in the baseline addition, don't add it now
                if (pdfBoxBaseline != null && value.equals(pdfBoxBaseline)) {
```

### SizeReplaceableByIsEmpty
`pdfBoxBaseline.length() > 0` can be replaced with '!pdfBoxBaseline.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        }

        if (pdfBoxBaseline != null && pdfBoxBaseline.length() > 0) {
            //if we've already added something above and multivalue is not permitted
            //return.
```

### SizeReplaceableByIsEmpty
`remainder.trim().length() > 0` can be replaced with '!remainder.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
        }
        String remainder = buffer.toString();
        if (remainder.trim().length() > 0) {
            xhtml.element("p", remainder);
        }
```

### SizeReplaceableByIsEmpty
`namedFields.size() == 0` can be replaced with 'namedFields.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
        }

        if (namedFields.size() == 0) {
            xhtml.endElement("div");
            return;
```

### SizeReplaceableByIsEmpty
`field.toolTip.trim().length() == 0` can be replaced with 'field.toolTip.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java

            String displayFieldName =
                    (field.toolTip == null || field.toolTip.trim().length() == 0) ? fieldName :
                            field.toolTip;

```

### SizeReplaceableByIsEmpty
`((COSDictionary) base).keySet().size() > 0` can be replaced with '!((COSDictionary) base).keySet().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
        //TODO: are there other checks we need to perform?
        if (base instanceof COSDictionary) {
            if (((COSDictionary) base).keySet().size() > 0) {
                return true;
            }
```

### SizeReplaceableByIsEmpty
`t.length() > 0` can be replaced with '!t.isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
                    String[] cols = line.split("\t");
                    String t = cols[0].trim();
                    if (t.length() > 0 && cols.length > 2) {
                        if (model == null) {
                            throw new IllegalArgumentException(
```

### SizeReplaceableByIsEmpty
`probabilities.size() > 0` can be replaced with '!probabilities.isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/metadata/TikaEvalMetadataFilter.java`
#### Snippet
```java
        List<LanguageResult> probabilities =
                (List<LanguageResult>) results.get(LanguageIDWrapper.class);
        if (probabilities.size() > 0) {
            metadata.set(LANGUAGE, probabilities.get(0).getLanguage());
            metadata.set(LANGUAGE_CONFIDENCE, probabilities.get(0).getRawScore());
```

### SizeReplaceableByIsEmpty
`state.uri.trim().length() > 0` can be replaced with '!state.uri.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
        //If we want to catch within doc references (GOTO, we need to cache those in state.
        //See testPDF_childAttachments.pdf for examples
        if (state.uri != null && state.uri.trim().length() > 0) {
            xhtml.startElement("a", "href", state.uri);
            xhtml.characters(state.hrefAnchorBuilder.toString());
```

### SizeReplaceableByIsEmpty
`tag.clazz.trim().length() > 0` can be replaced with '!tag.clazz.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
                }
                if (!ignoreTag) {
                    if (tag.clazz != null && tag.clazz.trim().length() > 0) {
                        xhtml.startElement(tag.tag, "class", tag.clazz);
                    } else {
```

### SizeReplaceableByIsEmpty
`pdfMarkedContent2XHTML.exceptions.size() > 0` can be replaced with '!pdfMarkedContent2XHTML.exceptions.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
            }
        }
        if (pdfMarkedContent2XHTML.exceptions.size() > 0) {
            //throw the first
            throw new TikaException("Unable to extract PDF content",
```

### SizeReplaceableByIsEmpty
`tokenCounts.getTokens().entrySet().size() == 0` can be replaced with 'tokenCounts.getTokens().entrySet().isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensBhattacharyya.java`
#### Snippet
```java
        LangModel model = pair.getValue();
        double sum = 0.0;
        if (tokenCounts.getTokens().entrySet().size() == 0) {
            return 0.0;
        }
```

### SizeReplaceableByIsEmpty
`tokenCounts.getTokens().entrySet().size() == 0` can be replaced with 'tokenCounts.getTokens().entrySet().isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensHellinger.java`
#### Snippet
```java
        LangModel model = pair.getValue();
        double sum = 0.0;
        if (tokenCounts.getTokens().entrySet().size() == 0) {
            return 0.0;
        }
```

### SizeReplaceableByIsEmpty
`tokenCounts.getTokens().entrySet().size() == 0` can be replaced with 'tokenCounts.getTokens().entrySet().isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensKLDNormed.java`
#### Snippet
```java
        LangModel model = pair.getValue();
        double kl = 0.0;
        if (tokenCounts.getTokens().entrySet().size() == 0) {
            return 1.0;
        }
```

### SizeReplaceableByIsEmpty
`tokenCounts.getTokens().entrySet().size() == 0` can be replaced with 'tokenCounts.getTokens().entrySet().isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensKLDivergence.java`
#### Snippet
```java
        LangModel model = pair.getValue();
        double kl = 0.0;
        if (tokenCounts.getTokens().entrySet().size() == 0) {
            return 1.0;
        }
```

### SizeReplaceableByIsEmpty
`fontNames.size() > 0` can be replaced with '!fontNames.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
            throw new IOException("Unable to end a document", e);
        }
        if (fontNames.size() > 0) {
            for (String fontName : fontNames) {
                metadata.add(Font.FONT_NAME, fontName);
```

### SizeReplaceableByIsEmpty
`link.trim().length() > 0` can be replaced with '!link.trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
                    if (uri != null) {
                        String link = uri.getURI();
                        if (link != null && link.trim().length() > 0) {
                            xhtml.startElement("div", "class", "annotation");
                            xhtml.startElement("a", "href", link);
```

### SizeReplaceableByIsEmpty
`supportedTypes.size() == 0` can be replaced with 'supportedTypes.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        }

        if (supportedTypes == null || supportedTypes.size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`tokenCounts.getTokens().entrySet().size() == 0` can be replaced with 'tokenCounts.getTokens().entrySet().isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensCosine.java`
#### Snippet
```java
        LangModel model = pair.getValue();
        double kl = 0.0;
        if (tokenCounts.getTokens().entrySet().size() == 0) {
            return 1.0;
        }
```

### SizeReplaceableByIsEmpty
`propName.length() > 0` can be replaced with '!propName.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGParser.java`
#### Snippet
```java
            String propName = read2004String(stream);
            String propValue = read2004String(stream);
            if (propName.length() > 0 && propValue.length() > 0) {
                metadata.add(DWG_CUSTOM_META_PREFIX + propName, propValue);
            }
```

### SizeReplaceableByIsEmpty
`propValue.length() > 0` can be replaced with '!propValue.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGParser.java`
#### Snippet
```java
            String propName = read2004String(stream);
            String propValue = read2004String(stream);
            if (propName.length() > 0 && propValue.length() > 0) {
                metadata.add(DWG_CUSTOM_META_PREFIX + propName, propValue);
            }
```

### SizeReplaceableByIsEmpty
`propName.length() > 0` can be replaced with '!propName.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGParser.java`
#### Snippet
```java
            String propName = read2007and2010String(stream);
            String propValue = read2007and2010String(stream);
            if (propName.length() > 0 && propValue.length() > 0) {
                metadata.add(DWG_CUSTOM_META_PREFIX + propName, propValue);
            }
```

### SizeReplaceableByIsEmpty
`propValue.length() > 0` can be replaced with '!propValue.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGParser.java`
#### Snippet
```java
            String propName = read2007and2010String(stream);
            String propValue = read2007and2010String(stream);
            if (propName.length() > 0 && propValue.length() > 0) {
                metadata.add(DWG_CUSTOM_META_PREFIX + propName, propValue);
            }
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGParser.java`
#### Snippet
```java
    private void handleHeader(int headerNumber, String value, Metadata metadata, XHTMLContentHandler xhtml)
            throws SAXException {
        if (value == null || value.length() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`description.length() > 0` can be replaced with '!description.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/prt/PRTParser.java`
#### Snippet
```java
        IOUtils.readFully(stream, desc);
        String description = extractText(desc, true);
        if (description.length() > 0) {
            metadata.set(TikaCoreProperties.DESCRIPTION, description);
        }
```

### SizeReplaceableByIsEmpty
`bytesRefCalculators.size() == 0` can be replaced with 'bytesRefCalculators.isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CompositeTextStatsCalculator.java`
#### Snippet
```java
        TokenCounts counts = new TokenCounts();
        TokenStream ts = analyzer.tokenStream(FIELD, txt);
        if (bytesRefCalculators.size() == 0) {
            try {
                CharTermAttribute termAtt = ts.getAttribute(CharTermAttribute.class);
```

### SizeReplaceableByIsEmpty
`tokenCountStatCalculators.size() > 0` can be replaced with '!tokenCountStatCalculators.isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CompositeTextStatsCalculator.java`
#### Snippet
```java

        TokenCounts tokenCounts = null;
        if (tokenCountStatCalculators.size() > 0 || languageAwareTokenCountStats.size() > 0 ||
                bytesRefCalculators.size() > 0) {
            try {
```

### SizeReplaceableByIsEmpty
`languageAwareTokenCountStats.size() > 0` can be replaced with '!languageAwareTokenCountStats.isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CompositeTextStatsCalculator.java`
#### Snippet
```java

        TokenCounts tokenCounts = null;
        if (tokenCountStatCalculators.size() > 0 || languageAwareTokenCountStats.size() > 0 ||
                bytesRefCalculators.size() > 0) {
            try {
```

### SizeReplaceableByIsEmpty
`bytesRefCalculators.size() > 0` can be replaced with '!bytesRefCalculators.isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CompositeTextStatsCalculator.java`
#### Snippet
```java
        TokenCounts tokenCounts = null;
        if (tokenCountStatCalculators.size() > 0 || languageAwareTokenCountStats.size() > 0 ||
                bytesRefCalculators.size() > 0) {
            try {
                tokenCounts = tokenize(txt, results);
```

### SizeReplaceableByIsEmpty
`languageAwareTokenCountStats.size() > 0` can be replaced with '!languageAwareTokenCountStats.isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CompositeTextStatsCalculator.java`
#### Snippet
```java
        }

        if (languageAwareTokenCountStats.size() > 0) {
            List<LanguageResult> langs = results.containsKey(LanguageIDWrapper.class) ?
                    (List) results.get(LanguageIDWrapper.class) : languageIDWrapper.calculate(txt);
```

### SizeReplaceableByIsEmpty
`line.length() > 0` can be replaced with '!line.isEmpty()'
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageIdentifier.java`
#### Snippet
```java
                String line = reader.readLine();
                while (line != null) {
                    if (line.length() > 0 && !line.startsWith("#")) {
                        int space = line.indexOf(' ');
                        profile.add(line.substring(0, space),
```

### SizeReplaceableByIsEmpty
`metadataList.size() == 0` can be replaced with 'metadataList.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-fs/src/main/java/org/apache/tika/pipes/emitter/fs/FileSystemEmitter.java`
#### Snippet
```java
            throws IOException, TikaEmitterException {
        Path output;
        if (metadataList == null || metadataList.size() == 0) {
            throw new TikaEmitterException("metadata list must not be null or of size 0");
        }
```

### SizeReplaceableByIsEmpty
`fileExtension.length() > 0` can be replaced with '!fileExtension.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-fs/src/main/java/org/apache/tika/pipes/emitter/fs/FileSystemEmitter.java`
#### Snippet
```java
        }

        if (fileExtension != null && fileExtension.length() > 0) {
            emitKey += "." + fileExtension;
        }
```

### SizeReplaceableByIsEmpty
`ext.length() == 0` can be replaced with 'ext.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/MimeBuffer.java`
#### Snippet
```java

            //special handling for text/html/xml
            if (ext.length() == 0) {
                ext = tryTextyTypes(mime.getType());
            }
```

### SizeReplaceableByIsEmpty
`ext.length() == 0` can be replaced with 'ext.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/MimeBuffer.java`
#### Snippet
```java
            try {
                String ext = MimeUtil.getExtension(value, config);
                if (ext == null || ext.length() == 0) {
                    st.setNull(3, Types.VARCHAR);
                } else {
```

### SizeReplaceableByIsEmpty
`driverClass.length() == 0` can be replaced with 'driverClass.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
        this.connectionString = connectionString;
        this.driverClass = driverClass;
        if (driverClass == null || driverClass.length() == 0) {
            if (System.getProperty("jdbc.drivers") != null) {
                //user has specified it on the command line
```

### SizeReplaceableByIsEmpty
`docs.size() > 0` can be replaced with '!docs.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
                    }
                }
                if (docs.size() > 0) {
                    writer.addDocuments(docs);
                }
```

### SizeReplaceableByIsEmpty
`metadataListB.size() == 0` can be replaced with 'metadataListB.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
     */
    private String findSharedDigestKey(List<Metadata> metadataListA, List<Metadata> metadataListB) {
        if (metadataListB == null || metadataListB.size() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`metadataListA.size() > 0` can be replaced with '!metadataListA.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
        }
        Set<String> digestA = new HashSet<>();
        if (metadataListA != null && metadataListA.size() > 0) {
            for (String n : metadataListA.get(0).names()) {
                if (n.startsWith(DIGEST_KEY_PREFIX)) {
```

### SizeReplaceableByIsEmpty
`metadataListB.size() == 0` can be replaced with 'metadataListB.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
                         List<Metadata> metadataListA, List<Metadata> metadataListB) {
        //TODO: could make this more robust
        if (metadataListB == null || metadataListB.size() == 0) {
            return -1;
        }
```

### SizeReplaceableByIsEmpty
`dataString.length() == 0` can be replaced with 'dataString.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURISchemeUtil.java`
#### Snippet
```java
        dataString = (dataString != null) ? dataString.replaceAll("\\\\", " ") : dataString;

        if (dataString == null || dataString.length() == 0) {
            data = new byte[0];
        } else if (isBase64 != null) {
```

### SizeReplaceableByIsEmpty
`line.length() > 0` can be replaced with '!line.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlEncodingDetector.java`
#### Snippet
```java
                }
                line = line.trim();
                if (line.length() > 0) {
                    unsupported.add(line.toLowerCase(Locale.US));
                }
```

### SizeReplaceableByIsEmpty
`script.toString().trim().length() == 0` can be replaced with 'script.toString().trim().isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java
        //don't write an attached macro if there is no content
        //we may want to revisit this behavior
        if (script.toString().trim().length() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`path.length() > 0` can be replaced with '!path.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java
            // portion of the path, which we don't want.
            String path = baseURL.getPath();
            if (url.startsWith("?") && path.length() > 0 && !path.endsWith("/")) {
                return new URL(baseURL.getProtocol(), baseURL.getHost(), baseURL.getPort(),
                        baseURL.getPath() + url).toExternalForm();
```

### SizeReplaceableByIsEmpty
`this.locationNameEntities.size() == 0` can be replaced with 'this.locationNameEntities.isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/NameEntityExtractor.java`
#### Snippet
```java
     */
    public void getBestNameEntity() {
        if (this.locationNameEntities.size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`list.size() == 0` can be replaced with 'list.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    static List<Integer> countAttachments(List<Metadata> list) {
        List<Integer> ret = new ArrayList<>();
        if (list == null || list.size() == 0) {
            return ret;
        }
```

### SizeReplaceableByIsEmpty
`content.trim().length() == 0` can be replaced with 'content.trim().isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
        Map<Cols, String> data = new HashMap<>();
        String content = truncateContent(contentTags, maxContentLength, data);
        if (content == null || content.trim().length() == 0) {
            content = "";
        }
```

### SizeReplaceableByIsEmpty
`probabilities.size() > 0` can be replaced with '!probabilities.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
                (List<LanguageResult>) stats.get(LanguageIDWrapper.class);

        if (probabilities.size() > 0) {
            data.put(Cols.LANG_ID_1, probabilities.get(0).getLanguage());
            data.put(Cols.LANG_ID_PROB_1, Double.toString(probabilities.get(0).getRawScore()));
```

### SizeReplaceableByIsEmpty
`content.trim().length() == 0` can be replaced with 'content.trim().isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java

        String content = contentTags.getContent();
        if (content == null || content.trim().length() == 0) {
            data.put(Cols.HAS_CONTENT, FALSE);
        } else {
```

### SizeReplaceableByIsEmpty
`data.keySet().size() > 0` can be replaced with '!data.keySet().isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
        Map<Cols, String> data = new HashMap<>();
        getExceptionStrings(m, data);
        if (data.keySet().size() > 0) {
            try {
                data.put(Cols.ID, fileId);
```

### SizeReplaceableByIsEmpty
`tags.size() == 0` can be replaced with 'tags.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    void writeTagData(String fileId, ContentTags contentTags, TableInfo tagsTable) {
        Map<String, Integer> tags = contentTags.getTags();
        if (tags.size() == 0 && contentTags.getParseException() == false) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`s.length() == 0` can be replaced with 's.isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    private static ContentTags parseContentAndTags(EvalFilePaths evalFilePaths, Metadata metadata) {
        String s = metadata.get(TikaCoreProperties.TIKA_CONTENT);
        if (s == null || s.length() == 0) {
            return ContentTags.EMPTY_CONTENT_TAGS;
        }
```

### SizeReplaceableByIsEmpty
`nameFinders.size() > 0` can be replaced with '!nameFinders.isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/opennlp/OpenNLPNERecogniser.java`
#### Snippet
```java
        }
        this.entityTypes = Collections.unmodifiableSet(this.entityTypes);
        this.available = nameFinders.size() > 0; //at least one finder is present
    }

```

### SizeReplaceableByIsEmpty
`person.length() > 0` can be replaced with '!person.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
        String email = extractEmail(string, sb);
        String person = clean(sb.toString());
        if (person != null && person.length() > 0) {
            metadata.set(personProperty, person);
        }
```

### SizeReplaceableByIsEmpty
`email.length() > 0` can be replaced with '!email.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
            metadata.set(personProperty, person);
        }
        if (email != null && email.length() > 0) {
            metadata.set(emailProperty, email);
        }
```

### SizeReplaceableByIsEmpty
`person.length() > 0` can be replaced with '!person.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
        String person = clean(sb.toString());

        if (person != null && person.length() > 0) {
            metadata.add(personProperty, person);
        }
```

### SizeReplaceableByIsEmpty
`email.length() > 0` can be replaced with '!email.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
            metadata.add(personProperty, person);
        }
        if (email != null && email.length() > 0) {
            metadata.add(emailProperty, email);
        }
```

### SizeReplaceableByIsEmpty
`v.length() == 0` can be replaced with 'v.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
     */
    public static Boolean getBoolean(String v, Boolean defaultMissing) {
        if (v == null || v.length() == 0) {
            return defaultMissing;
        }
```

### SizeReplaceableByIsEmpty
`v.length() == 0` can be replaced with 'v.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
     */
    public static Integer getInt(String v, Integer defaultMissing) {
        if (v == null || v.length() == 0) {
            return defaultMissing;
        }
```

### SizeReplaceableByIsEmpty
`v.length() == 0` can be replaced with 'v.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
     */
    public static Path getPath(String v, Path defaultMissing) {
        if (v == null || v.length() == 0) {
            return defaultMissing;
        }
```

### SizeReplaceableByIsEmpty
`v.length() == 0` can be replaced with 'v.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
    @Deprecated
    public static File getFile(String v, File defaultMissing) {
        if (v == null || v.length() == 0) {
            return defaultMissing;
        }
```

### SizeReplaceableByIsEmpty
`v.length() == 0` can be replaced with 'v.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
     */
    public static Long getLong(String v, Long defaultMissing) {
        if (v == null || v.length() == 0) {
            return defaultMissing;
        }
```

### SizeReplaceableByIsEmpty
`metadataList.size() == 0` can be replaced with 'metadataList.isEmpty()'
in `tika-pipes/tika-emitters/tika-emitter-az-blob/src/main/java/org/apache/tika/pipes/emitter/azblob/AZBlobEmitter.java`
#### Snippet
```java
    public void emit(String emitKey, List<Metadata> metadataList)
            throws IOException, TikaEmitterException {
        if (metadataList == null || metadataList.size() == 0) {
            throw new TikaEmitterException("metadata list must not be null or of size 0");
        }
```

### SizeReplaceableByIsEmpty
`terms.size() == 0` can be replaced with 'terms.isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
            if (keywordsNode != null) {
                List<Node> terms = getChildNodes(keywordsNode.getChildNodes(), "term");
                if (terms.size() == 0) {
                    // test AJ15.pdf
                    metadata.add("Keyword", keywordsNode.getTextContent());
```

### SizeReplaceableByIsEmpty
`timedOuts.size() == 0` can be replaced with 'timedOuts.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        @Override
        public TimeoutFutureResult call() throws Exception {
            while (timedOuts.size() == 0) {
                try {
                    Thread.sleep(timeoutCheckPulseMillis);
```

### SizeReplaceableByIsEmpty
`opt.trim().length() == 0` can be replaced with 'opt.trim().isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/builders/CommandLineParserBuilder.java`
#### Snippet
```java
        boolean isRequired = getBoolean(map, "required", false);
        boolean hasArg = getBoolean(map, "hasArg", false);
        if (opt.trim().length() == 0 || description.trim().length() == 0) {
            throw new IllegalArgumentException("Must specify at least option and description");
        }
```

### SizeReplaceableByIsEmpty
`description.trim().length() == 0` can be replaced with 'description.trim().isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/builders/CommandLineParserBuilder.java`
#### Snippet
```java
        boolean isRequired = getBoolean(map, "required", false);
        boolean hasArg = getBoolean(map, "hasArg", false);
        if (opt.trim().length() == 0 || description.trim().length() == 0) {
            throw new IllegalArgumentException("Must specify at least option and description");
        }
```

### SizeReplaceableByIsEmpty
`longOpt.trim().length() > 0` can be replaced with '!longOpt.trim().isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/builders/CommandLineParserBuilder.java`
#### Snippet
```java
        }
        Option option = new Option(opt, description);
        if (longOpt.trim().length() > 0) {
            option.setLongOpt(longOpt);
        }
```

### SizeReplaceableByIsEmpty
`files.size() == 0` can be replaced with 'files.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSDirectoryCrawler.java`
#### Snippet
```java
                    e);
        }
        if (files.size() == 0) {
            LOG.info("Empty directory: {}", directory.toAbsolutePath());
            return;
```

### SizeReplaceableByIsEmpty
`includeString.length() > 0` can be replaced with '!includeString.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
        long maxFileSize = PropsUtil.getLong(attributes.get(MAX_FILE_SIZE_BYTES_ATTR), -1L);
        long minFileSize = PropsUtil.getLong(attributes.get(MIN_FILE_SIZE_BYTES_ATTR), -1L);
        Pattern includePat = (includeString != null && includeString.length() > 0) ?
                Pattern.compile(includeString) : null;
        Pattern excludePat = (excludeString != null && excludeString.length() > 0) ?
```

### SizeReplaceableByIsEmpty
`excludeString.length() > 0` can be replaced with '!excludeString.isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
        Pattern includePat = (includeString != null && includeString.length() > 0) ?
                Pattern.compile(includeString) : null;
        Pattern excludePat = (excludeString != null && excludeString.length() > 0) ?
                Pattern.compile(excludeString) : null;

```

### SizeReplaceableByIsEmpty
`s.trim().length() == 0` can be replaced with 's.trim().isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java

    private FSDirectoryCrawler.CRAWL_ORDER getCrawlOrder(String s) {
        if (s == null || s.trim().length() == 0 || s.equals("os")) {
            return FSDirectoryCrawler.CRAWL_ORDER.OS_ORDER;
        } else if (s.toLowerCase(Locale.ROOT).contains("rand")) {
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/AbstractConverter.java`
#### Snippet
```java
                                             int arrayType) throws XMPException {
        String value = metadata.get(tikaKey);
        if (value != null && value.length() > 0) {
            XMPUtils.separateArrayItems(meta, ns, propertyName, value,
                    new PropertyOptions(arrayType), false);
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/AbstractConverter.java`
#### Snippet
```java
            throws XMPException {
        String value = metadata.get(tikaKey);
        if (value != null && value.length() > 0) {
            meta.setLocalizedText(ns, propertyName, null, XMPConst.X_DEFAULT, value);
        }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/AbstractConverter.java`
#### Snippet
```java
            throws XMPException {
        String value = metadata.get(tikaKey);
        if (value != null && value.length() > 0) {
            meta.setProperty(ns, propertyName, value);
        }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/MSOfficeXMLConverter.java`
#### Snippet
```java
        String creatorTool = "";
        String value = metadata.get(OfficeOpenXMLExtended.APPLICATION);
        if (value != null && value.length() > 0) {
            creatorTool = value;

```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/MSOfficeXMLConverter.java`
#### Snippet
```java

            value = metadata.get(OfficeOpenXMLExtended.APP_VERSION);
            if (value != null && value.length() > 0) {
                creatorTool += " " + value;
            }
```

### SizeReplaceableByIsEmpty
`creatorTool.length() > 0` can be replaced with '!creatorTool.isEmpty()'
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/MSOfficeXMLConverter.java`
#### Snippet
```java
        }

        if (creatorTool.length() > 0) {
            meta.setProperty(XMPConst.NS_XMP, "CreatorTool", creatorTool);
        }
```

### SizeReplaceableByIsEmpty
`key.length() == 0` can be replaced with 'key.isEmpty()'
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     */
    private void checkKey(String key) throws PropertyTypeException {
        if (key == null || key.length() == 0) {
            throw new PropertyTypeException("Key must not be null");
        }
```

### SizeReplaceableByIsEmpty
`results.size() == 0` can be replaced with 'results.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
        //a higher confidence if detection contradicts filename?
        List<CSVResult> results = sniff(reader);
        if (results == null || results.size() == 0) {
            return CSVResult.TEXT;
        }
```

### SizeReplaceableByIsEmpty
`xRefEntriesList.size() > 0` can be replaced with '!xRefEntriesList.isEmpty()'
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
        }
        // If no new entry is found, we need to write out the last result
        if (xRefEntriesList.size() > 0) {
            list.add(last - count + 1);
            list.add(count);
```

### SizeReplaceableByIsEmpty
`filters.size() == 0` can be replaced with 'filters.isEmpty()'
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private COSBase getFilters(COSBase existingFilters) {
        List<COSName> filters = config.getFilters(existingFilters);
        if (filters.size() == 0) {
            return COSNull.NULL;
        } else if (filters.size() == 1) {
```

### SizeReplaceableByIsEmpty
`objectsToWrite.size() > 0` can be replaced with '!objectsToWrite.isEmpty()'
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java

    private void doWriteObjects() throws IOException {
        while (objectsToWrite.size() > 0) {
            COSBase nextObject = objectsToWrite.removeFirst();
            objectsToWriteSet.remove(nextObject);
```

### SizeReplaceableByIsEmpty
`parents.length() > 0` can be replaced with '!parents.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageParser.java`
#### Snippet
```java
                                 boolean addThisNodeName) {
        if (addThisNodeName) {
            if (parents.length() > 0) {
                parents += " ";
            }
```

### SizeReplaceableByIsEmpty
`fieldInfoList.size() == 0` can be replaced with 'fieldInfoList.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/apple/AppleSingleFileParser.java`
#### Snippet
```java
            ));
        }
        if (fieldInfoList.size() == 0) {
            throw new TikaException("AppleSingleFile missing field info");
        }
```

### SizeReplaceableByIsEmpty
`text.length() > 0` can be replaced with '!text.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java

        private void text(String text) {
            if (text != null && text.length() > 0 && currentText != null) {
                currentText.append(text);
            }
```

### SizeReplaceableByIsEmpty
`invalidlangs.size() > 0` can be replaced with '!invalidlangs.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
        Set<String> validLangs = new HashSet<>();
        TesseractOCRConfig.getLangs(language, validLangs, invalidlangs);
        if (invalidlangs.size() > 0) {
            throw new TikaConfigException("Invalid language code(s): " + invalidlangs);
        }
```

### SizeReplaceableByIsEmpty
`langs.size() > 0` can be replaced with '!langs.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
            throw new TikaConfigException("Invalid language code(s): " + invalidlangs);
        }
        if (langs.size() > 0) {
            for (String lang : validLangs) {
                if (!langs.contains(lang)) {
```

### SizeReplaceableByIsEmpty
`langs.size() > 0` can be replaced with '!langs.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
            throws TikaConfigException {

        if (langs.size() > 0 && !StringUtils.isBlank(defaultConfig.getLanguage())) {
            if (!langs.contains(defaultConfig.getLanguage())) {
                throw new TikaConfigException("It doesn't look like tesseract has lang data for " +
```

### SizeReplaceableByIsEmpty
`ftr_datetime.length() > 0` can be replaced with '!ftr_datetime.isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
            }

            if (ftr_datetime.length() > 0) {
                // we want to pass this back in a more friendly format
                String format_out = "yyyy-MM-dd'T'HH:mm:ss'Z'";
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/GUID.java`
#### Snippet
```java
        for (int i = 0; i < utf16Str.length(); i += 2) {
            intGuid[i / 2] =
                    Integer.parseUnsignedInt("" + utf16Str.charAt(i) + utf16Str.charAt(i + 1), 16);
        }
        return new GUID(intGuid);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java
        if (end - offset > dif.size()) {
            throw new TikaMemoryLimitException(
                    "Exceeded memory limit when trying to dumpHex - " + "" + (end - offset) +
                            " > " + dif.size());
        }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-ml/tika-dl/src/main/java/org/apache/tika/dl/imagerec/DL4JInceptionV3Net.java`
#### Snippet
```java
            if (scores.getDouble(i) > minConfidence) {
                String label = labelMap.get(i);
                String id = i + "";
                result.add(new RecognisedObject(label, labelLang, id, scores.getDouble(i)));
                LOG.debug("Found Object {}", label);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-crypto-module/src/main/java/org/apache/tika/parser/crypto/TSDParser.java`
#### Snippet
```java

        public String getTsaStr() {
            return tsa + "";
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-crypto-module/src/main/java/org/apache/tika/parser/crypto/TSDParser.java`
#### Snippet
```java
        public String getSerialNumberFormatted() {
            String outsn = String.format(Locale.ROOT, "%12x", getSerialNumber());
            return outsn != null ? outsn.trim() : "" + getSerialNumber();
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        } catch (IOException e) {
            metadata.set(TikaCoreProperties.TIKA_META_PREFIX + "pdf:metadata-xmp-parse-failed",
                    "" + e);
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        } catch (IOException e) {
            metadata.set(TikaCoreProperties.TIKA_META_PREFIX + "pdf:metadata-xmp-parse-failed",
                    "" + e);
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        } catch (IOException e) {
            metadata.set(TikaCoreProperties.TIKA_META_PREFIX + "pdf:metadata-xmp-parse-failed",
                    "" + e);
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        } catch (IOException e) {
            metadata.set(TikaCoreProperties.TIKA_META_PREFIX + "pdf:metadata-xmp-parse-failed",
                    "" + e);
        }
        try {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        } catch (IOException e) {
            metadata.set(TikaCoreProperties.TIKA_META_PREFIX + "pdf:metadata-xmp-parse-failed",
                    "" + e);
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        } catch (IOException e) {
            metadata.set(TikaCoreProperties.TIKA_META_PREFIX + "pdf:metadata-xmp-parse-failed",
                    "" + e);
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
            if (ocrStrategy == OCR_ONLY || ocrStrategy == OCR_AND_TEXT_EXTRACTION) {
                throw new TikaException(
                        "" + "I regret that I couldn't find an OCR parser to handle " +
                                ocrImageMediaType + "." +
                                "Please set the OCR_STRATEGY to NO_OCR or configure your" +
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
            return "Author [surName=" + surName + ", middleName=" + middleName != null ?
                    middleName :
                    "" + ", firstName=" + firstName + ", affiliations=" + affiliations + "]";
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java
        Path outputParent = cand.getParent();
        while (fNameBase != null && Files.exists(cand) && ++cnt < 10000) {
            String candFileName = fNameBase + "(" + cnt + ")." + fNameExt + "" + localSuffix;
            cand = FSUtil.resolveRelative(outputParent, candFileName);
        }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java
            UUID uid = UUID.randomUUID();
            cand = FSUtil.resolveRelative(outputParent,
                    uid.toString() + fNameExt + "" + localSuffix);
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java
            if (!SystemUtils.IS_OS_WINDOWS) {
                String[] checkOpt =
                        {stringsProg, "-e", "" + defaultStringsConfig.getEncoding().get(),
                                "/dev/null"};
                int[] errorValues =
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java

            // Metadata
            metadata.set("strings:min-len", "" + stringsConfig.getMinLength());
            metadata.set("strings:encoding", stringsConfig.toString());
            metadata.set("strings:file_output", doFile(tis));
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java

            // Metadata
            metadata.set("strings:length", "" + totalBytes);
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java
        cmdList.add(stringsProg);
        cmdList.add("-n");
        cmdList.add("" + config.getMinLength());
        // Currently, encoding option is not supported by Windows (and other) versions
        if (hasEncodingOption) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java
        if (hasEncodingOption) {
            cmdList.add("-e");
            cmdList.add("" + config.getEncoding().get());
        }
        cmdList.add(input.getPath());
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmAssert.java`
#### Snippet
```java
     * @throws IOException
     */
    public static final void assertInputStreamNotNull(InputStream is) throws IOException {
        if (is == null) {
            throw new IOException("input sream is null");
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmAssert.java`
#### Snippet
```java
     * @throws ChmParsingException
     */
    public static final void assertChmAccessorParameters(byte[] data, ChmAccessor<?> chmAccessor,
                                                         int count) throws ChmParsingException {
        assertByteArrayNotNull(data);
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmAssert.java`
#### Snippet
```java
     * @throws ChmParsingException
     */
    public static final void assertChmAccessorNotNull(ChmAccessor<?> chmAccessor)
            throws ChmParsingException {
        if (chmAccessor == null) {
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmAssert.java`
#### Snippet
```java
     * @throws ChmParsingException
     */
    public static final void assertByteArrayNotNull(byte[] data) throws ChmParsingException {
        if (data == null) {
            throw new ChmParsingException("byte[] data is null");
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmAssert.java`
#### Snippet
```java
     * @throws TikaException
     */
    public static final void assertChmBlockSegment(byte[] data, ChmLzxcResetTable resetTable,
                                                   int blockNumber, int lzxcBlockOffset,
                                                   int lzxcBlockLength) throws TikaException {
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmAssert.java`
#### Snippet
```java
     * @throws ChmParsingException
     */
    public static final void assertDirectoryListingEntry(int name_length, String name,
                                                         ChmCommons.EntryType entryType, int offset,
                                                         int length) throws ChmParsingException {
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java
    }

    public static final boolean startsWith(byte[] data, String prefix) {
        for (int i = 0; i < prefix.length(); i++) {
            if (data[i] != prefix.charAt(i)) {
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
     * @throws ChmParsingException
     */
    public static final int indexOfResetTableBlock(byte[] text, byte[] pattern)
            throws ChmParsingException {
        return (indexOf(text, pattern)) - 4;
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UByte.java`
#### Snippet
```java
     * @return Array of cached values for UByte.
     */
    private static final UByte[] mkValues() {
        UByte[] ret = new UByte[256];

```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
     * @return Array of cached values for UInteger
     */
    private static final UInteger[] mkValues() {
        int precacheSize = getPrecacheSize();
        UInteger[] ret;
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
     * {@link Integer#MAX_VALUE} then Integer#MAX_VALUE will be used.
     */
    private static final int getPrecacheSize() {
        String prop = null;
        long propParsed;
```

### FinalStaticMethod
'static' method declared `final`
in `tika-core/src/main/java/org/apache/tika/sax/ElementMappingContentHandler.java`
#### Snippet
```java
    }

    protected static final String getQNameAsString(QName qname) {
        String prefix = qname.getPrefix();
        if (prefix.length() > 0) {
```

### FinalStaticMethod
'static' method declared `final`
in `tika-core/src/main/java/org/apache/tika/parser/ParserDecorator.java`
#### Snippet
```java
     * @return the decorated parser
     */
    public static final Parser withoutTypes(Parser parser, final Set<MediaType> excludeTypes) {
        return new ParserDecorator(parser) {
            private static final long serialVersionUID = 7979614774021768609L;
```

### FinalStaticMethod
'static' method declared `final`
in `tika-core/src/main/java/org/apache/tika/parser/ParserDecorator.java`
#### Snippet
```java
     * @deprecated This has been replaced by {@link FallbackParser}
     */
    public static final Parser withFallbacks(final Collection<? extends Parser> parsers,
                                             final Set<MediaType> types) {
        // Delegate to the new FallbackParser for now, until people upgrade
```

### FinalStaticMethod
'static' method declared `final`
in `tika-core/src/main/java/org/apache/tika/parser/ParserDecorator.java`
#### Snippet
```java
     * @return the decorated parser
     */
    public static final Parser withTypes(Parser parser, final Set<MediaType> types) {
        return new ParserDecorator(parser) {
            private static final long serialVersionUID = -7345051519565330731L;
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java


    static final Set<MediaType> loadPackageSpecializations() {
        Set<MediaType> zipSpecializations = new HashSet<>();
        for (String mediaTypeString : new String[]{
```

### FinalStaticMethod
'static' method declared `final`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
     * @return if the byte is a char.
     */
    private static final boolean isChar(byte c) {
        return isChar[c & 0xFF];
    }
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `abs > 1E15`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/TikaExcelGeneralFormat.java`
#### Snippet
```java
        }
        final double abs = Math.abs(value);
        if (abs > 1E15 || (abs <= 1E-15 && abs > 0)) {
            return scientificFormat.format(number, toAppendTo, pos);
        } else if (Math.floor(value) == value || abs > 1E15) {
```

### DuplicateCondition
Duplicate condition `abs > 1E15`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/TikaExcelGeneralFormat.java`
#### Snippet
```java
        if (abs > 1E15 || (abs <= 1E-15 && abs > 0)) {
            return scientificFormat.format(number, toAppendTo, pos);
        } else if (Math.floor(value) == value || abs > 1E15) {
            // integer, or integer portion uses all 15 allowed digits
            return integerFormat.format(number, toAppendTo, pos);
```

### DuplicateCondition
Duplicate condition `prelentable == null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
    private void createLengthTreeLenTable(int offset, int tablelen, short[] pretreetable,
                                          short[] prelentable) throws TikaException {
        if (prelentable == null || getChmSection() == null || pretreetable == null ||
                prelentable == null) {
            throw new ChmParsingException("is null");
```

### DuplicateCondition
Duplicate condition `prelentable == null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                                          short[] prelentable) throws TikaException {
        if (prelentable == null || getChmSection() == null || pretreetable == null ||
                prelentable == null) {
            throw new ChmParsingException("is null");
        }
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/MSOwnerFileParser.java`
#### Snippet
```java
        int unicodeCharLength = stream.read();
        if (asciiNameLength == unicodeCharLength) {
            stream.read();//zero after the char length
            //this is effectively bounds checked by asciiNameLength
            byte[] unicodeBytes = new byte[unicodeCharLength * 2];
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `tika-parsers/tika-parsers-ml/tika-dl/src/main/java/org/apache/tika/dl/imagerec/DL4JInceptionV3Net.java`
#### Snippet
```java
        }
        if (!cacheDir.exists()) {
            cacheDir.mkdirs();
        }
        String[] parts = uri.toASCIIString().split("/");
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `tika-parsers/tika-parsers-ml/tika-dl/src/main/java/org/apache/tika/dl/imagerec/DL4JInceptionV3Net.java`
#### Snippet
```java
        } else {
            if (successFlag.exists()) {
                successFlag.delete();
            }
            LOG.info("Cache doesn't exist. Going to make a copy");
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `tika-parsers/tika-parsers-ml/tika-dl/src/main/java/org/apache/tika/dl/imagerec/DL4JVGG16Net.java`
#### Snippet
```java
                } else {
                    LOG.warn("Preprocessed Model doesn't exist at {}", cacheDir);
                    cacheDir.getParentFile().mkdirs();
                    ZooModel zooModel = VGG16.builder().build();
                    model = (ComputationGraph) zooModel.initPretrained(PretrainedType.IMAGENET);
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
        } finally {
            if (!ok) {
                file.delete();
            }
        }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
        }
        if (jar != null) {
            jar.delete();
        }
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `tika-core/src/main/java/org/apache/tika/utils/RereadableInputStream.java`
#### Snippet
```java
        super.close();
        if (storeFile != null) {
            storeFile.delete();
        }
        closed = true;
```

### IgnoreResultOfCall
Result of `Reader.read()` is ignored
in `tika-core/src/main/java/org/apache/tika/parser/ParsingReader.java`
#### Snippet
```java
        // TIKA-203: Buffer first character to force metadata extraction
        reader.mark(1);
        reader.read();
        reader.reset();
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
                try {
                    // Clean up temp output files
                    tempOutputFile.delete();
                } catch (Exception e) {
                    //swallow
```

### IgnoreResultOfCall
Result of `WPInputStream.skip()` is ignored
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPPrefixAreaExtractor.java`
#### Snippet
```java
        if (prefixArea.getMajorVersion() == WPPrefixArea.WP6_MAJOR_VERSION) {
            try {
                in.skip(4); // 4 reserved bytes: skip     // 17-20
                prefixArea.setFileSize(in.readWPLong());      // 21-24
            } catch (IOException e) {
```

### IgnoreResultOfCall
Result of `String.substring()` is ignored
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
            int splitAt = embeddedName.lastIndexOf('.');
            if (splitAt > 0) {
                embeddedName.substring(splitAt);
            }

```

### IgnoreResultOfCall
Result of `Collections.unmodifiableList()` is ignored
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/TableInfo.java`
#### Snippet
```java
    public TableInfo(String name, ColInfo... cols) {
        Collections.addAll(colInfos, cols);
        Collections.unmodifiableList(colInfos);
        this.name = name;
        for (ColInfo c : colInfos) {
```

### IgnoreResultOfCall
Result of `Collections.unmodifiableList()` is ignored
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/TableInfo.java`
#### Snippet
```java
    public TableInfo(String name, List<ColInfo> cols) {
        colInfos.addAll(cols);
        Collections.unmodifiableList(colInfos);
        this.name = name;
        for (ColInfo c : colInfos) {
```

### IgnoreResultOfCall
Result of `InputStream.skip()` is ignored
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/executable/ExecutableParser.java`
#### Snippet
```java
        // Skip the rest of the MS-DOS stub (if PE), until we reach what should
        //  be the PE header (if this is a PE executable)
        stream.skip(peOffset - 0x40);

        // Read the PE header
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowImageRecParser.java`
#### Snippet
```java
        try {
            if (!modelFile.exists()) {
                modelFile.getParentFile().mkdirs();
                LOG.warn("Model doesn't exist at {}. Expecting the script to download it.",
                        modelFile);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowImageRecParser.java`
#### Snippet
```java
            }
            if (!scriptFile.exists()) {
                scriptFile.getParentFile().mkdirs();
                LOG.info("Copying script to : {}", scriptFile);
                try (InputStream sourceStream = getClass().getResourceAsStream(SCRIPT_FILE_NAME)) {
```

### IgnoreResultOfCall
Result of `CommonTokenResult.getLangCode()` is ignored
in `tika-example/src/main/java/org/apache/tika/example/TextStatsFromTikaEval.java`
#### Snippet
```java

        CommonTokenResult result = (CommonTokenResult) results.get(CommonTokens.class);
        result.getLangCode();
        return result.getOOV();
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `tika-example/src/main/java/org/apache/tika/example/ImportContextImpl.java`
#### Snippet
```java
        completed = true;
        if (inputFile != null) {
            inputFile.delete();
        }
    }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/PSDParser.java`
#### Snippet
```java
                    // The name length is padded to be even
                    if (nameLen % 2 == 1) {
                        stream.read();
                        nameLen++;
                    }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/IWorkPackageParser.java`
#### Snippet
```java
                //  spot them because they encrypt part of the zip stream
                try {
                    stream.read();
                } catch (UnsupportedZipFeatureException e) {
                    // Compression field was likely encrypted
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
        } finally {
            if (tmpTxtOutput != null) {
                tmpTxtOutput.delete();
            }
        }
```

### IgnoreResultOfCall
Result of `InputStream.skip()` is ignored
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
            if (finished) {
                // now, we want to reset the stream to be sitting right on top of the finish marker
                is.skip(read);
                value = new byte[read - start];
                System.arraycopy(buf, start, value, 0, read - start);
```

## RuleId[id=UnnecessaryUnboxing]
### UnnecessaryUnboxing
Unnecessary unboxing
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
            if (part != null) {
                partString = Integer.toString(part);
                metadata.set(PDF.PDFAID_PART, part.intValue());
            }
        } catch (NumberFormatException e) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
            Integer part = schema.getPart();
            if (schema.getPart() != null) {
                metadata.set(PDF.PDFUAID_PART, part.intValue());
            }
        } catch (NumberFormatException e) {
```

## RuleId[id=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'org.apache.tika.parser.microsoft.ooxml.OOXMLWordAndPowerPointTextHandler.EditType.INSERT' accessed via instance reference
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
            picDescription = atts.getValue(O_NS, "title");
        } else if (INS.equals(localName)) {
            startEditedSection(editType.INSERT, atts);
        } else if (DEL_TEXT.equals(localName)) {
            inDelText = true;
```

### AccessStaticViaInstance
Static member 'org.apache.tika.parser.microsoft.ooxml.OOXMLWordAndPowerPointTextHandler.EditType.DELETE' accessed via instance reference
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
            inDelText = true;
        } else if (DEL.equals(localName)) {
            startEditedSection(editType.DELETE, atts);
        } else if (MOVE_TO.equals(localName)) {
            startEditedSection(EditType.MOVE_TO, atts);
```

### AccessStaticViaInstance
Static member 'org.apache.tika.parser.microsoft.ooxml.OOXMLWordAndPowerPointTextHandler.EditType.MOVE_FROM' accessed via instance reference
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
            startEditedSection(EditType.MOVE_TO, atts);
        } else if (MOVE_FROM.equals(localName)) {
            startEditedSection(editType.MOVE_FROM, atts);
        } else if (OLE_OBJECT.equals(localName)) { //check for O_NS?
            String type = null;
```

### AccessStaticViaInstance
Static member 'org.apache.tika.eval.app.io.ExtractReader.ALTER_METADATA_LIST.CONCATENATE_CONTENT_INTO_FIRST' accessed via instance reference
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/ExtractReader.java`
#### Snippet
```java
                    }
                } else if (alterMetadataList
                        .equals(ALTER_METADATA_LIST.AS_IS.CONCATENATE_CONTENT_INTO_FIRST) &&
                        metadataList.size() > 1) {
                    StringBuilder sb = new StringBuilder();
```

### AccessStaticViaInstance
Static member 'org.apache.tika.eval.app.db.JDBCUtil.batchInsert(java.sql.PreparedStatement, org.apache.tika.eval.app.db.TableInfo, java.util.Map)' accessed via instance reference
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/DBWriter.java`
#### Snippet
```java
                        "Failed to create prepared statement for: " + table.getName());
            }
            dbUtil.batchInsert(p, table, data);
            LastInsert lastInsert = lastInsertMap.get(table.getName());
            lastInsert.rowCount++;
```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `processRecord()` only delegates to its super method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java

            @Override
            public void processRecord(Record record) {
//                System.out.println(record.getClass() + " : "+record.toString());
                super.processRecord(record);
```

### RedundantMethodOverride
Method `endElement()` only delegates to its super method
in `tika-core/src/main/java/org/apache/tika/sax/SafeContentHandler.java`
#### Snippet
```java

    @Override
    public void endElement(String uri, String localName, String name) throws SAXException {
        // TODO: enable this, but some parsers currently
        // trip it
```

### RedundantMethodOverride
Method `endDocument()` only delegates to its super method
in `tika-core/src/main/java/org/apache/tika/sax/SafeContentHandler.java`
#### Snippet
```java

    @Override
    public void endDocument() throws SAXException {
        // TODO: enable this, but some parsers currently
        // trip it
```

### RedundantMethodOverride
Method `startEmbeddedDocument()` only delegates to its super method
in `tika-core/src/main/java/org/apache/tika/sax/RecursiveParserWrapperHandler.java`
#### Snippet
```java
     */
    @Override
    public void startEmbeddedDocument(ContentHandler contentHandler, Metadata metadata)
            throws SAXException {
        super.startEmbeddedDocument(contentHandler, metadata);
```

### RedundantMethodOverride
Method `loadOne()` only delegates to its super method
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

        @Override
        ConfigurableThreadPoolExecutor loadOne(Element element, MimeTypes mimeTypes,
                                               ServiceLoader loader)
                throws TikaException, IOException {
```

### RedundantMethodOverride
Method `shouldAcceptContainer()` only delegates to its super method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp4/TikaMp4BoxHandler.java`
#### Snippet
```java

    @Override
    public boolean shouldAcceptContainer(@NotNull String box) {
        return super.shouldAcceptContainer(box);
    }
```

### RedundantMethodOverride
Method `checkInitialization()` is identical to its super method
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-jdbc/src/main/java/org/apache/tika/pipes/reporters/jdbc/JDBCPipesReporter.java`
#### Snippet
```java

    @Override
    public void checkInitialization(InitializableProblemHandler problemHandler)
            throws TikaConfigException {

```

### RedundantMethodOverride
Method `getArray()` is identical to its super method
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
        }

        public TokenDFTF[] getArray() {
            TokenDFTF[] topN = new TokenDFTF[size()];
            //now we reverse the queue
```

### RedundantMethodOverride
Method `shutdown()` is identical to its super method
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSConsumersManager.java`
#### Snippet
```java

    @Override
    public void shutdown() {
        //noop
    }
```

### RedundantMethodOverride
Method `init()` is identical to its super method
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSConsumersManager.java`
#### Snippet
```java

    @Override
    public void init() {
        //noop
    }
```

### RedundantMethodOverride
Method `set()` only delegates to its super method
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     */
    @Override
    public void set(Property property, double value) {
        super.set(property, value);
    }
```

### RedundantMethodOverride
Method `set()` only delegates to its super method
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     */
    @Override
    public void set(Property property, int value) {
        // Can reuse the checks from the base class implementation which will call
        // the set(String, String) method in the end
```

### RedundantMethodOverride
Method `set()` only delegates to its super method
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     */
    @Override
    public void set(Property property, Date date) {
        super.set(property, date);
    }
```

### RedundantMethodOverride
Method `getSupportedTypes()` is identical to its super method
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3DBParser.java`
#### Snippet
```java
     */
    @Override
    public Set<MediaType> getSupportedTypes(ParseContext context) {
        return null;
    }
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
(data\[this.getCurrentPlace() + 3\] \& 0xff) \<\< 24: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmPmglHeader.java`
#### Snippet
```java
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
                (data[this.getCurrentPlace() + 3] & 0xff) << 24;

        setDataRemained(this.getDataRemained() - 4);
```

### IntegerMultiplicationImplicitCastToLong
(data\[this.getCurrentPlace() + 3\] \& 0xff) \<\< 24: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcResetTable.java`
#### Snippet
```java
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
                (data[this.getCurrentPlace() + 3] & 0xff) << 24;

        setDataRemained(this.getDataRemained() - 4);
```

### IntegerMultiplicationImplicitCastToLong
(data\[this.getCurrentPlace() + 3\] \& 0xff) \<\< 24: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
                (data[this.getCurrentPlace() + 3] & 0xff) << 24;

        setDataRemained(this.getDataRemained() - 4);
```

### IntegerMultiplicationImplicitCastToLong
(data\[this.getCurrentPlace() + 3\] \& 0xff) \<\< 24: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmPmgiHeader.java`
#### Snippet
```java
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
                (data[this.getCurrentPlace() + 3] & 0xff) << 24;

        setDataRemained(this.getDataRemained() - 4);
```

### IntegerMultiplicationImplicitCastToLong
1 \<\< win: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxState.java`
#### Snippet
```java
            int position_slots;
            int win = ChmCommons.getWindowSize(window);
            setWindowSize(1 << win);
            /* LZX supports window sizes of 2^15 (32Kb) through 2^21 (2Mb) */
            if (win < 15 || win > 21) {
```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFEmbObjHandler.java`
#### Snippet
```java
        }
        if (len > memoryLimitInKb * 1024) {
            throw new TikaMemoryLimitException(len, (memoryLimitInKb * 1024));
        }

```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
                Entry ooxml = root.getEntry("Package");
                UnsynchronizedByteArrayOutputStream out = new UnsynchronizedByteArrayOutputStream();
                try (BoundedInputStream bis = new BoundedInputStream(memoryLimitInKb * 1024,
                        new DocumentInputStream((DocumentEntry) ooxml))) {
                    IOUtils.copy(bis, out);
```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
                    IOUtils.copy(bis, out);
                    if (bis.hasHitBound()) {
                        throw new TikaMemoryLimitException((memoryLimitInKb * 1024 + 1),
                                (memoryLimitInKb * 1024));
                    }
```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
                    if (bis.hasHitBound()) {
                        throw new TikaMemoryLimitException((memoryLimitInKb * 1024 + 1),
                                (memoryLimitInKb * 1024));
                    }
                }
```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
                    UnsynchronizedByteArrayOutputStream out = new UnsynchronizedByteArrayOutputStream();
                    is.reset();
                    BoundedInputStream bis = new BoundedInputStream(memoryLimitInKb * 1024, is);
                    IOUtils.copy(is, out);
                    if (bis.hasHitBound()) {
```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
                    IOUtils.copy(is, out);
                    if (bis.hasHitBound()) {
                        throw new TikaMemoryLimitException(memoryLimitInKb * 1024 + 1,
                                memoryLimitInKb * 1024);
                    }
```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
                    if (bis.hasHitBound()) {
                        throw new TikaMemoryLimitException(memoryLimitInKb * 1024 + 1,
                                memoryLimitInKb * 1024);
                    }
                    ret = out.toByteArray();
```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
        if (len < 0) {
            throw new IOException("Requested length for reading bytes < 0?!: " + len);
        } else if (memoryLimitInKb > -1 && len > memoryLimitInKb * 1024) {
            throw new TikaMemoryLimitException(len, memoryLimitInKb * 1024);
        } else if (len > Integer.MAX_VALUE) {
```

### IntegerMultiplicationImplicitCastToLong
memoryLimitInKb \* 1024: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
            throw new IOException("Requested length for reading bytes < 0?!: " + len);
        } else if (memoryLimitInKb > -1 && len > memoryLimitInKb * 1024) {
            throw new TikaMemoryLimitException(len, memoryLimitInKb * 1024);
        } else if (len > Integer.MAX_VALUE) {
            throw new TikaMemoryLimitException(len, Integer.MAX_VALUE);
```

### IntegerMultiplicationImplicitCastToLong
1 \<\< bits: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        int pos = 0;
        /* the current position in the decode table */
        long table_mask = (1 << bits);
        long bit_mask = (table_mask >> 1);
        long next_symbol = bit_mask;
```

### IntegerMultiplicationImplicitCastToLong
ch4 \<\< 24: integer shift implicitly cast to long
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
            throw new BufferUnderrunException();
        }
        return ((ch4 << 24) + (ch3 << 16) + (ch2 << 8) + (ch1)) & 0x00FFFFFFFFl;
    }

```

### IntegerMultiplicationImplicitCastToLong
ch1 \<\< 24: integer shift implicitly cast to long
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
            throw new BufferUnderrunException();
        }
        return ((ch1 << 24) + (ch2 << 16) + (ch3 << 8) + (ch4)) & 0x00FFFFFFFFl;
    }

```

### IntegerMultiplicationImplicitCastToLong
id.length() \* 2: integer multiplication implicitly cast to long
in `tika-core/src/main/java/org/apache/tika/pipes/emitter/EmitData.java`
#### Snippet
```java
    private static long estimateSizeInBytes(String id, List<Metadata> metadataList,
                                            String containerStackTrace) {
        long sz = 36 + id.length() * 2;
        sz += 36 + containerStackTrace.length() * 2;
        for (Metadata m : metadataList) {
```

### IntegerMultiplicationImplicitCastToLong
containerStackTrace.length() \* 2: integer multiplication implicitly cast to long
in `tika-core/src/main/java/org/apache/tika/pipes/emitter/EmitData.java`
#### Snippet
```java
                                            String containerStackTrace) {
        long sz = 36 + id.length() * 2;
        sz += 36 + containerStackTrace.length() * 2;
        for (Metadata m : metadataList) {
            for (String n : m.names()) {
```

### IntegerMultiplicationImplicitCastToLong
n.length() \* 2: integer multiplication implicitly cast to long
in `tika-core/src/main/java/org/apache/tika/pipes/emitter/EmitData.java`
#### Snippet
```java
        for (Metadata m : metadataList) {
            for (String n : m.names()) {
                sz += 36 + n.length() * 2;
                for (String v : m.getValues(n)) {
                    sz += 36 + v.length() * 2;
```

### IntegerMultiplicationImplicitCastToLong
v.length() \* 2: integer multiplication implicitly cast to long
in `tika-core/src/main/java/org/apache/tika/pipes/emitter/EmitData.java`
#### Snippet
```java
                sz += 36 + n.length() * 2;
                for (String v : m.getValues(n)) {
                    sz += 36 + v.length() * 2;
                }
            }
```

### IntegerMultiplicationImplicitCastToLong
numCols \* 32: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFFileHeader.java`
#### Snippet
```java
            throw new TikaException("Expected new line at end of header");
        }
        long totalReadSoFar = 32 + (numCols * 32) + 1;
        //there can be extra bytes in the header
        long extraHeaderBytes = header.numBytesInHeader - totalReadSoFar;
```

### IntegerMultiplicationImplicitCastToLong
ch4 \<\< 24: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPInputStream.java`
#### Snippet
```java
            throw new EOFException();
        }
        return ((ch4 << 24) + (ch3 << 16) + (ch2 << 8) + ch1);
    }

```

### IntegerMultiplicationImplicitCastToLong
ch3 \<\< 16: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPInputStream.java`
#### Snippet
```java
            throw new EOFException();
        }
        return ((ch4 << 24) + (ch3 << 16) + (ch2 << 8) + ch1);
    }

```

### IntegerMultiplicationImplicitCastToLong
ch2 \<\< 8: integer shift implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPInputStream.java`
#### Snippet
```java
            throw new EOFException();
        }
        return ((ch4 << 24) + (ch3 << 16) + (ch2 << 8) + ch1);
    }

```

### IntegerMultiplicationImplicitCastToLong
defaultStringsConfig.getTimeoutSeconds() \* 1000: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java
        fileCommandDetector = new FileCommandDetector();
        fileCommandDetector.setFilePath(filePath);
        fileCommandDetector.setTimeoutMs(defaultStringsConfig.getTimeoutSeconds() * 1000);
    }

```

### IntegerMultiplicationImplicitCastToLong
config.getTimeoutSeconds() \* 1000: integer multiplication implicitly cast to long
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
        String id = null;
        long timeoutMillis = TikaTaskTimeout.getTimeoutMillis(parseContext,
                config.getTimeoutSeconds() * 1000);
        try {
            process = pb.start();
```

## RuleId[id=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OldExcelParser.java`
#### Snippet
```java
        String line;
        BufferedReader reader = new BufferedReader(new StringReader(text));
        while ((line = reader.readLine()) != null) {
            xhtml.startElement("p");
            xhtml.characters(line);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java
        BigInteger bi = BigInteger.ZERO;
        byte[] nb = new byte[1];
        while ((ob = this.getByte()) < 0) {
            nb[0] = (byte) ((ob & 0x7f));
            bi = bi.shiftLeft(7).add(new BigInteger(nb));
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java

        if (placeHolder < data_chunk.length) {
            while ((ob = data_chunk[placeHolder]) < 0) {
                nb[0] = (byte) ((ob & 0x7f));
                bi = bi.shiftLeft(7).add(new BigInteger(nb));
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java
                int strlen = 0;// = getEncint(data);
                byte temp;
                while ((temp = dir_chunk[placeHolder++]) >= 0x80) {
                    strlen <<= 7;
                    strlen += temp & 0x7f;
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java

        /* Reinitializes local variables */
        i = j = 0;

        /* Matching */
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    leaf = pos;

                    if ((pos += bit_mask) > table_mask) {
                        /* table overflow */
                        throw new ChmParsingException("Table overflow");
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                        tmp[leaf] = sym;

                        if ((pos += bit_mask) > table_mask) {
                            /* table overflow */
                            throw new ChmParsingException("Table overflow");
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    s <<= 1;
                    s += getChmSection().checkBit(x);
                } while ((s = getState().mainTreeTable[s]) >= getState().getMainTreeElements());
            }
            //System.out.printf("%d,", s);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                            matchfooter <<= 1;
                            matchfooter += getChmSection().checkBit(x);
                        } while ((matchfooter = getState().lengthTreeTable[matchfooter]) >=
                                ChmConstants.LZX_NUM_SECONDARY_LENGTHS);
                    }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                                t <<= 1;
                                t += getChmSection().checkBit(x);
                            } while ((t = getState().getAlignedTreeTable()[t]) >=
                                    getState().getMainTreeElements());
                        }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                                t <<= 1;
                                t += getChmSection().checkBit(x);
                            } while ((t = getState().getAlignedTreeTable()[t]) >=
                                    getState().getMainTreeElements());
                        }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    z <<= 1;
                    z += getChmSection().checkBit(x);
                } while ((z = pretreetable[z]) >= ChmConstants.LZX_PRETREE_MAXSYMBOLS);
            }
            getChmSection().getSyncBits(prelentable[z]);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                        z <<= 1;
                        z += getChmSection().checkBit(x);
                    } while ((z = pretreetable[z]) >= ChmConstants.LZX_PRETREE_MAXSYMBOLS);
                }
                getChmSection().getSyncBits(prelentable[z]);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    s <<= 1;
                    s += getChmSection().checkBit(x);
                } while ((s = getState().getMainTreeTable()[s]) >=
                        ChmConstants.LZX_MAIN_MAXSYMBOLS);
            }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                            matchfooter <<= 1;
                            matchfooter += getChmSection().checkBit(x);
                        } while ((matchfooter = getState().getLengthTreeTable()[matchfooter]) >=
                                ChmConstants.LZX_NUM_SECONDARY_LENGTHS);
                    }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    z <<= 1;
                    z += getChmSection().checkBit(x);
                } while ((z = pretreetable[z]) >= ChmConstants.LZX_PRETREE_NUM_ELEMENTS);
            }
            getChmSection().getSyncBits(prelentable[z]);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                        z <<= 1;
                        z += getChmSection().checkBit(x);
                    } while ((z = pretreetable[z]) >=
                            ChmConstants.LZX_PRETREE_NUM_ELEMENTS);//LZX_MAINTREE_TABLEBITS);
                }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
            //swallow
        }
        b = OVERRIDE_AVAILABLE = false;

    }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteLegacyDumpStrings.java`
#### Snippet
```java
            long sz = oneNoteDirectFileResource.size();
            long pos;
            while ((pos = oneNoteDirectFileResource.position()) != sz) {
                long nextBufferSize = BUFFER_SIZE;
                if (sz - pos < BUFFER_SIZE) {
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteLegacyDumpStrings.java`
#### Snippet
```java

            long pos;
            while ((pos = oneNoteDirectFileResource.position()) != sz) {
                long nextBufferSize = bufSize;
                if (sz - pos < bufSize) {
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
        UInteger cached;

        if ((cached = getCached(value)) != null) {
            return cached;
        }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
        // the value read could be invalid so check it
        rangeCheck(value);
        if ((cached = getCached(value)) != null) {
            return cached;
        }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/RevisionManifestDataElementData.java`
#### Snippet
```java
        AtomicReference<StreamObjectHeaderStart> header = new AtomicReference<>();
        int headerLength = 0;
        while ((headerLength = StreamObjectHeaderStart.tryParse(byteArray, index.get(), header)) !=
                0) {
            if (header.get().type == StreamObjectTypeHeaderStart.RevisionManifestRootDeclare) {
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupMetadataDeclarations.java`
#### Snippet
```java
        this.objectGroupMetadataList = new ArrayList<>();

        while ((headerLength = StreamObjectHeaderStart.tryParse(byteArray, index.get(), header)) !=
                0) {
            index.addAndGet(headerLength);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageIndexDataElementData.java`
#### Snippet
```java
        AtomicReference<StreamObjectHeaderStart> header = new AtomicReference<>();
        boolean isStorageIndexManifestMappingExist = false;
        while ((headerLength = StreamObjectHeaderStart.tryParse(byteArray, index.get(), header)) !=
                0) {
            index.addAndGet(headerLength);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupDeclarations.java`
#### Snippet
```java
        this.objectDeclarationList = new ArrayList<>();
        this.objectGroupObjectBLOBDataDeclarationList = new ArrayList<>();
        while ((headerLength = StreamObjectHeaderStart.tryParse(byteArray, index.get(), header)) !=
                0) {
            if (header.get().type == StreamObjectTypeHeaderStart.ObjectGroupObjectDeclare) {
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObject.java`
#### Snippet
```java
        int length = 0;
        AtomicReference<StreamObjectHeaderStart> streamObjectHeader = new AtomicReference<>();
        if ((length =
                StreamObjectHeaderStart.tryParse(byteArray, tmpIndex.get(), streamObjectHeader)) ==
                0) {
            return false;
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObject.java`
#### Snippet
```java
        int length;
        AtomicReference<StreamObjectHeaderStart> streamObjectHeader = new AtomicReference<>();
        if ((length =
                StreamObjectHeaderStart.tryParse(byteArray, tmpIndex.get(), streamObjectHeader)) ==
                0) {
            throw new StreamObjectParseErrorException(tmpIndex.get(), clazz.getName(),
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupData.java`
#### Snippet
```java
        this.objectGroupObjectDataBLOBReferenceList = new ArrayList<>();

        while ((headerLength = StreamObjectHeaderStart.tryParse(byteArray, index.get(), header)) !=
                0) {
            StreamObjectTypeHeaderStart type = header.get().type;
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageManifestDataElementData.java`
#### Snippet
```java
        AtomicReference<StreamObjectHeaderStart> header = new AtomicReference<>();
        int headerLength = 0;
        while ((headerLength = StreamObjectHeaderStart.tryParse(byteArray, index.get(), header)) !=
                0) {
            if (header.get().type == StreamObjectTypeHeaderStart.StorageManifestRootDeclare) {
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/ZipFilesChunking.java`
#### Snippet
```java
                        Arrays.copyOfRange(this.fileContent, index, headerLength + compressedSize),
                        this.getSingleChunkSignature(header, dataFileSignatureBytes.get())));
                index += headerLength += compressedSize;
            } else {
                list.add(builder.Build(header, this.getSHA1Signature(header)));
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/XMPPacketScanner.java`
#### Snippet
```java
        int len = match.length;
        int b;
        while ((b = in.read()) >= 0) {
            if (b == match[found]) {
                found++;
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
        int max = 6;
        int read = 0;
        while ((i = stream.read()) >= 0 && read++ < max) {
            v = v << 7;
            if ((i & 128) == 128) {
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
        while (pos < to) {
            if (ch[pos] == '<') {
                from = pos = writeCharsAndEntity(ch, from, pos, "lt");
            } else if (ch[pos] == '>') {
                from = pos = writeCharsAndEntity(ch, from, pos, "gt");
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
                from = pos = writeCharsAndEntity(ch, from, pos, "lt");
            } else if (ch[pos] == '>') {
                from = pos = writeCharsAndEntity(ch, from, pos, "gt");
            } else if (ch[pos] == '&') {
                from = pos = writeCharsAndEntity(ch, from, pos, "amp");
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
                from = pos = writeCharsAndEntity(ch, from, pos, "gt");
            } else if (ch[pos] == '&') {
                from = pos = writeCharsAndEntity(ch, from, pos, "amp");
            } else if (attribute && ch[pos] == '"') {
                from = pos = writeCharsAndEntity(ch, from, pos, "quot");
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
                from = pos = writeCharsAndEntity(ch, from, pos, "amp");
            } else if (attribute && ch[pos] == '"') {
                from = pos = writeCharsAndEntity(ch, from, pos, "quot");
            } else {
                pos++;
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderResource.java`
#### Snippet
```java
            byte[] buffer = new byte[0x10000 - 1];
            int n;
            while ((n = stream.read(buffer)) != -1) {
                output.writeShort(n);
                output.write(buffer, 0, n);
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderProxy.java`
#### Snippet
```java
            byte[] buffer = new byte[0xffff];
            int n;
            while ((n = input.readUnsignedShort()) > 0) {
                input.readFully(buffer, 0, n);
                stream.write(buffer, 0, n);
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/detect/NNExampleModelDetector.java`
#### Snippet
```java
        String line;
        try {
            while ((line = bReader.readLine()) != null) {
                line = line.trim();
                if (line.startsWith("#")) {
```

### NestedAssignment
Result of assignment expression used
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
            try {
                String line;
                while ((line = reader.readLine()) != null) {
                    boolean consumed = false;
                    for (Map.Entry<Pattern, String> entry : metadataPatterns.entrySet()) {
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java
        // text contents of the xhtml
        String line;
        while ((line = reader.readLine()) != null) {
            if (line.contains("{") && !line.endsWith("}") || line.startsWith(" ")) {
                String completeField = parseMultiLineFieldValue(line);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/Mp3Parser.java`
#### Snippet
```java
        // So, keep going until we don't find any more
        MP3Frame f;
        while ((f = ID3v2Frame.createFrameIfPresent(mpegStream)) != null) {
            if (f instanceof ID3v2Frame) {
                ID3v2Frame id3F = (ID3v2Frame) f;
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFExtractor.java`
#### Snippet
```java
        Tag currentTag = new Tag();
        Stack<Tag> parents = new Stack<>();
        while ((line = reader.readLine()) != null) {
            if (line.contains(OPEN_TAG_MARKER) && !line.contains(CLOSE_TAG_MARKER)) {
                Matcher matcher = openTagPattern.matcher(line.trim());
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPDocumentAreaExtractor.java`
#### Snippet
```java
        int count = 0;
        int c;
        while ((c = in.read()) != -1) {
            count++;
            if (c == targetChar) {
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPDocumentAreaExtractor.java`
#### Snippet
```java
        StringBuilder out = new StringBuilder(chunk);
        int c;
        while ((c = in.read()) != -1) {
            extract(c, in, out, xhtml);
            if (out.length() >= chunk) {
```

### NestedAssignment
Result of assignment expression used
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                        new BufferedReader(new InputStreamReader(new FileInputStream(mf), UTF_8));
                String line;
                while ((line = r.readLine()) != null) {
                    if (line.startsWith("!:mime") || line.startsWith("#!:mime")) {
                        String mime = line.substring(7).trim();
```

### NestedAssignment
Result of assignment expression used
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MosesTranslator.java`
#### Snippet
```java
                        Charset.defaultCharset()))) {
            String line;
            while ((line = reader.readLine()) != null) stringBuilder.append(line);
        }

```

### NestedAssignment
Result of assignment expression used
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/Lingo24Translator.java`
#### Snippet
```java
                BufferedReader reader = new BufferedReader(inputStreamReader)) {
            String line;
            while ((line = reader.readLine()) != null) {
                responseText.append(line);
            }
```

### NestedAssignment
Result of assignment expression used
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/GoogleTranslator.java`
#### Snippet
```java
                BufferedReader reader = new BufferedReader(inputStreamReader)) {
            String line;
            while ((line = reader.readLine()) != null) {
                responseText.append(line);
            }
```

### NestedAssignment
Result of assignment expression used
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/YandexTranslator.java`
#### Snippet
```java
                BufferedReader reader = new BufferedReader(inputStreamReader)) {
            String line;
            while ((line = reader.readLine()) != null) {
                responseText.append(line);
            }
```

### NestedAssignment
Result of assignment expression used
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/JoshuaNetworkTranslator.java`
#### Snippet
```java
        StringBuilder sb = new StringBuilder(text);
        int i = 0;
        while ((i = sb.indexOf(".", i + 1)) != -1) {
            sb.replace(i, i + 1, "\n");
        }
```

### NestedAssignment
Result of assignment expression used
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/JoshuaNetworkTranslator.java`
#### Snippet
```java
                BufferedReader reader = new BufferedReader(inputStreamReader)) {
            String line;
            while ((line = reader.readLine()) != null) {
                responseText.append(line);
            }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/prt/PRTParser.java`
#### Snippet
```java

        // Now look for text
        while ((read = stream.read()) > -1) {
            if (read == 0xe0 || read == 0xe3 || read == 0xf0) {
                int nread = stream.read();
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java

                        String sCurrentLine;
                        while ((sCurrentLine = br.readLine()) != null) 
                        {
                            sCurrentLine = sCurrentLine
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
            DWGReadFormatRemover dwgReadFormatRemover = new DWGReadFormatRemover();
            JsonToken nextToken = jParser.nextToken();
            while ((nextToken = jParser.nextToken()) != JsonToken.END_OBJECT) {
                if (nextToken == JsonToken.FIELD_NAME) {
                    String nextFieldName = jParser.currentName();
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
    private void parseSummaryInfo(JsonParser jsonParser, Metadata metadata) throws IOException {
        JsonToken nextToken;
        while ((nextToken = jsonParser.nextToken()) != JsonToken.END_OBJECT) {
            if (nextToken == JsonToken.FIELD_NAME) {
                String nextFieldName = jsonParser.currentName();
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
    private void parseDwgObject(JsonParser jsonParser, Consumer<String> textConsumer) throws IOException {
        JsonToken nextToken;
        while ((nextToken = jsonParser.nextToken()) != JsonToken.END_OBJECT) {
            if (nextToken == JsonToken.FIELD_NAME) {
                String nextFieldName = jsonParser.currentName();
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
    private void parseHeader(JsonParser jsonParser, Metadata metadata) throws IOException {
        JsonToken nextToken;
        while ((nextToken = jsonParser.nextToken()) != JsonToken.END_OBJECT) {
            if (nextToken == JsonToken.FIELD_NAME) {
                String nextFieldName = jsonParser.currentName();
```

### NestedAssignment
Result of assignment expression used
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java

        try {
            while ((len = bis.read(buffer)) != -1) {
                text.append(new String(buffer, 0, len, encoding));
            }
```

### NestedAssignment
Result of assignment expression used
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
        String line = null;

        while ((line = reader.readLine()) != null) {

            // # starts a comment line
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/code/SourceCodeParser.java`
#### Snippet
```java
                String line;
                int nbLines = 0;
                while ((line = reader.readLine()) != null) {
                    out.append(line).append(System.getProperty("line.separator"));
                    String author = parserAuthor(line);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/sas/SAS7BDATParser.java`
#### Snippet
```java
        // Process each row in turn
        Object[] row = null;
        while ((row = sas.readNext()) != null) {
            xhtml.startElement("tr");
            for (String val : DataWriterUtil.getRowValues(sas.getColumns(), row, formatMap)) {
```

### NestedAssignment
Result of assignment expression used
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
            try {
                LOG.trace("watcher starting to read");
                while ((line = reader.readLine()) != null && this.running) {
                    if (line.startsWith(
                            BatchProcess.BATCH_CONSTANTS.BATCH_PROCESS_FATAL_MUST_RESTART
```

### NestedAssignment
Result of assignment expression used
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
            try {
                LOG.trace("gobbler starting to read");
                while ((line = reader.readLine()) != null && this.running) {
                    if (redirectForkedProcessToStdOut) {
                        System.out.println("BatchProcess:" + line);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/pot/PooledTimeSeriesParser.java`
#### Snippet
```java
            xhtml.endElement("h3");
            xhtml.startElement("table", attributes);
            while ((line = reader.readLine()) != null) {
                xhtml.startElement("tr");
                for (String val : line.split(" ")) {
```

### NestedAssignment
Result of assignment expression used
in `tika-langdetect/tika-langdetect-test-commons/src/main/java/org/apache/tika/langdetect/LanguageDetectorTest.java`
#### Snippet
```java
        int n = 0;

        while ((-1 != (n = input.read(buffer))) && (count < limit)) {
            int bytesToCopy = Math.min(limit - count, n);
            output.write(buffer, 0, bytesToCopy);
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
            int bytesRead = 0;

            while ((bytesRead = reader.read(buffer, 0, Math.min(max, 1024))) > 0) {
                sb.append(buffer, 0, bytesRead);
                max -= bytesRead;
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
        do {
            inSize = 0;
            while ((i = stream.read(input, inSize, BUF_SIZE - inSize)) > 0) {
                inSize += i;
            }
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            buildChar:
            {
                firstByte = it.charValue = it.nextByte(det);
                if (firstByte < 0) {
                    // Ran off the end of the input data
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            it.error = false;
            int firstByte;
            firstByte = it.charValue = it.nextByte(det);
            if (firstByte < 0) {
                return false;
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            it.error = false;
            int firstByte;
            firstByte = it.charValue = it.nextByte(det);
            if (firstByte < 0) {
                return false;
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            buildChar:
            {
                firstByte = it.charValue = it.nextByte(det);

                if (firstByte < 0) {
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
            boolean ignoreSpace = false;

            while ((b = nextByte(det)) >= 0) {
                byte mb = byteMap[b];

```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
            boolean ignoreSpace = false;

            while ((b = nextByte(det)) >= 0) {
                byte mb = byteMap[b];

```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
            ngram = 0;

            ngramCount = hitCount = 0;
        }

```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                        maxsize);    // read in at least the full data
                if (msgsize == -1) {
                    remainder = msgsize = 0;
                }
                remainder -= msgsize;
```

### NestedAssignment
Result of assignment expression used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                }

                if (finished = (b == bfinish)) {
/*
               is.reset();
```

## RuleId[id=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`z = z + 17` could be simplified to 'z += 17'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                z = getState().getMainTreeLengtsTable()[i] - z;
                if (z < 0) {
                    z = z + 17;
                }
                getState().mainTreeLengtsTable[i] = (short) z;
```

### ReplaceAssignmentWithOperatorAssignment
`z = z + 17` could be simplified to 'z += 17'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                z = getState().mainTreeLengtsTable[i] - z;
                if (z < 0) {
                    z = z + 17;
                }
                for (int j = 0; j < y; j++)
```

### ReplaceAssignmentWithOperatorAssignment
`z = z + 17` could be simplified to 'z += 17'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                z = getState().getLengthTreeLengtsTable()[i] - z;
                if (z < 0) {
                    z = z + 17;
                }
                getState().getLengthTreeLengtsTable()[i] = (short) z;
```

### ReplaceAssignmentWithOperatorAssignment
`z = z + 17` could be simplified to 'z += 17'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                z = getState().getLengthTreeLengtsTable()[i] - z;
                if (z < 0) {
                    z = z + 17;
                }
                for (int j = 0; j < y; j++)
```

### ReplaceAssignmentWithOperatorAssignment
`value = value >> 8` could be simplified to 'value \>\>= 8'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/LittleEndianBitConverter.java`
#### Snippet
```java
        for (int i = 0; i < buffer.length; i++) {
            buffer[i] = (byte) (value & 0xff);
            value = value >> 8;
        }
    }
```

### ReplaceAssignmentWithOperatorAssignment
`i = i + Character.charCount(c)` could be simplified to 'i += Character.charCount(c)'
in `tika-core/src/main/java/org/apache/tika/sax/SafeContentHandler.java`
#### Snippet
```java
                return true;
            }
            i = i + Character.charCount(c);
        }

```

### ReplaceAssignmentWithOperatorAssignment
`v = v << 7` could be simplified to 'v \<\<= 7'
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
        int read = 0;
        while ((i = stream.read()) >= 0 && read++ < max) {
            v = v << 7;
            if ((i & 128) == 128) {
                // Continues
```

### ReplaceAssignmentWithOperatorAssignment
`title = title + value` could be simplified to 'title += value'
in `tika-core/src/main/java/org/apache/tika/sax/DIFContentHandler.java`
#### Snippet
```java
            this.delegate.startElement("", "h3", "h3", EMPTY_ATTRIBUTES);
            String title = "Title: ";
            title = title + value;
            this.delegate.characters(title.toCharArray(), 0, title.length());
            this.delegate.endElement("", "h3", "h3");
```

### ReplaceAssignmentWithOperatorAssignment
`mag_trust = mag_trust * (1 - changeRate)` could be simplified to 'mag_trust \*= (1 - changeRate)'
in `tika-core/src/main/java/org/apache/tika/mime/ProbabilisticMimeDetectionSelector.java`
#### Snippet
```java
                     * magic
                     */
                    mag_trust = mag_trust * (1 - changeRate);
                    /*
                     * grow as our trust goes down
```

### ReplaceAssignmentWithOperatorAssignment
`mag_neg = mag_neg * (1 + changeRate)` could be simplified to 'mag_neg \*= (1 + changeRate)'
in `tika-core/src/main/java/org/apache/tika/mime/ProbabilisticMimeDetectionSelector.java`
#### Snippet
```java
                     * grow as our trust goes down
                     */
                    mag_neg = mag_neg * (1 + changeRate);

                }
```

### ReplaceAssignmentWithOperatorAssignment
`title = title + value` could be simplified to 'title += value'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dif/DIFContentHandler.java`
#### Snippet
```java
            this.delegate.startElement("", "h3", "h3", EMPTY_ATTRIBUTES);
            String title = "Title: ";
            title = title + value;
            this.delegate.characters(title.toCharArray(), 0, title.length());
            this.delegate.endElement("", "h3", "h3");
```

### ReplaceAssignmentWithOperatorAssignment
`unmappedUnicodeCharsPerPage = unmappedUnicodeCharsPerPage / 100f` could be simplified to 'unmappedUnicodeCharsPerPage /= 100f'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
                        ("Error parsing OCRStrategyAuto - Percent cannot exceed 100%");
                    }
                    unmappedUnicodeCharsPerPage = unmappedUnicodeCharsPerPage / 100f;
                }
                // The 2nd number is optional.  Default to 10 chars per page
```

### ReplaceAssignmentWithOperatorAssignment
`tmpPos = tmpPos - outPos` could be simplified to 'tmpPos -= outPos'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
            System.arraycopy(output, outPos, output, 0, tmpPos - outPos);
        }
        tmpPos = tmpPos - outPos;
        outPos = 0;
    }
```

### ReplaceAssignmentWithOperatorAssignment
`dataLen = dataLen + 1` could be simplified to 'dataLen += 1'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/PSDParser.java`
#### Snippet
```java
            if (dataLen % 2 == 1) {
                // Data Length is even padded
                dataLen = dataLen + 1;
            }
            //protect against overflow
```

### ReplaceAssignmentWithOperatorAssignment
`offset = offset + icon_length` could be simplified to 'offset += icon_length'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSParser.java`
#### Snippet
```java
                break;
            }
            offset = offset + icon_length;
        }
        StringBuilder icon_details = new StringBuilder();
```

## RuleId[id=SimplifiableBooleanExpression]
### SimplifiableBooleanExpression
`(apt && !isComposite) || !apt` can be simplified to '!apt\|\|!isComposite'
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            name += decorated;
        }
        if ((apt && !isComposite) ||
                !apt) {    // Don't display Composite parsers in the apt output.
            System.out.println(indent(i) + ((apt) ? "* " : "") + name +
                    (isComposite ? " (Composite Parser):" : ""));
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `valueMap` are updated, but never queried
in `tika-core/src/main/java/org/apache/tika/config/Param.java`
#### Snippet
```java
    //one of these two is used for serialization
    private final List<String> valueStrings = new ArrayList<>();
    private final Map<String, String> valueMap = new LinkedHashMap<>();

    private Class<T> type;
```

### MismatchedCollectionQueryUpdate
Contents of collection `colNames` are updated, but never queried
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/Report.java`
#### Snippet
```java
            int rowCount = 0;
            ResultSetMetaData meta = rs.getMetaData();
            Set<String> colNames = new HashSet<>();

            Row xssfRow = sheet.createRow(rowCount++);
```

## RuleId[id=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `counts` is accessed in both synchronized and unsynchronized contexts
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java

    Thread reporterThread;
    private ConcurrentHashMap<PipesResult.STATUS, LongAdder> counts = new ConcurrentHashMap<>();
    private AsyncStatus asyncStatus = new AsyncStatus();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `statusFile` is accessed in both synchronized and unsynchronized contexts
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java

    ObjectMapper objectMapper;
    private Path statusFile;

    private long reportUpdateMillis = 1000;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `objectMapper` is accessed in both synchronized and unsynchronized contexts
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(FileSystemStatusReporter.class);

    ObjectMapper objectMapper;
    private Path statusFile;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `position` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/io/LookaheadInputStream.java`
#### Snippet
```java
    private int buffered = 0;

    private int position = 0;

    private int mark = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `output` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderProxy.java`
#### Snippet
```java
    private transient DataInputStream input;

    private transient DataOutputStream output;

    public ClassLoaderProxy(int resource) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `input` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderProxy.java`
#### Snippet
```java
    private final int resource;

    private transient DataInputStream input;

    private transient DataOutputStream output;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxFilesProcessedPerClient` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/fork/ForkParser.java`
#### Snippet
```java

    @Field
    private int maxFilesProcessedPerClient = -1;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverParseTimeoutMillis` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/fork/ForkParser.java`
#### Snippet
```java

    @Field
    private long serverParseTimeoutMillis = 60000;

    @Field
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `java` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/fork/ForkParser.java`
#### Snippet
```java
     * Java command line
     */
    private List<String> java = Arrays.asList("java", "-Xmx32m", "-Djava.awt.headless=true");
    /**
     * Process pool size
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverWaitTimeoutMillis` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/fork/ForkParser.java`
#### Snippet
```java

    @Field
    private long serverWaitTimeoutMillis = 60000;

    @Field
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverPulseMillis` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/fork/ForkParser.java`
#### Snippet
```java
    private int currentlyInUse = 0;
    @Field
    private long serverPulseMillis = 1000;

    @Field
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `executorService` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/pipes/PipesClient.java`
#### Snippet
```java
    private final int pipesClientId;
    private volatile boolean closed = false;
    private ExecutorService executorService = Executors.newFixedThreadPool(1);
    private Process process;
    private DataOutputStream output;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `statusCounts` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncStatus.java`
#### Snippet
```java
    private Instant lastUpdate;
    private TotalCountResult totalCountResult = new TotalCountResult(0, TotalCountResult.STATUS.NOT_COMPLETED);
    private Map<PipesResult.STATUS, Long> statusCounts = new HashMap<>();
    private ASYNC_STATUS asyncStatus = ASYNC_STATUS.STARTED;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastUpdate` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncStatus.java`
#### Snippet
```java
    private final Instant started;

    private Instant lastUpdate;
    private TotalCountResult totalCountResult = new TotalCountResult(0, TotalCountResult.STATUS.NOT_COMPLETED);
    private Map<PipesResult.STATUS, Long> statusCounts = new HashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `totalCountResult` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncStatus.java`
#### Snippet
```java

    private Instant lastUpdate;
    private TotalCountResult totalCountResult = new TotalCountResult(0, TotalCountResult.STATUS.NOT_COMPLETED);
    private Map<PipesResult.STATUS, Long> statusCounts = new HashMap<>();
    private ASYNC_STATUS asyncStatus = ASYNC_STATUS.STARTED;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `asyncStatus` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncStatus.java`
#### Snippet
```java
    private TotalCountResult totalCountResult = new TotalCountResult(0, TotalCountResult.STATUS.NOT_COMPLETED);
    private Map<PipesResult.STATUS, Long> statusCounts = new HashMap<>();
    private ASYNC_STATUS asyncStatus = ASYNC_STATUS.STARTED;

    private String crashMessage = StringUtils.EMPTY;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `MAX_OFFER_WAIT_MS` is accessed in both synchronized and unsynchronized contexts
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
    private final AsyncConfig asyncConfig;
    private final AtomicLong totalProcessed = new AtomicLong(0);
    private static long MAX_OFFER_WAIT_MS = 120000;
    private volatile int numParserThreadsFinished = 0;
    private volatile int numEmitterThreadsFinished = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `commonTokenMap` is accessed in both synchronized and unsynchronized contexts
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
    //make this configurable
    private final String defaultLangCode;
    Map<String, LangModel> commonTokenMap = new ConcurrentHashMap<>();
    Set<String> alreadyTriedToLoad = new HashSet<>();
    private Matcher digitsMatcher = Pattern.compile("(\\d+)").matcher("");
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `secondaryParser` is accessed in both synchronized and unsynchronized contexts
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/NamedEntityParser.java`
#### Snippet
```java
    }

    public Tika secondaryParser;
    private List<NERecogniser> nerChain;
    private volatile boolean initialized = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nerChain` is accessed in both synchronized and unsynchronized contexts
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/NamedEntityParser.java`
#### Snippet
```java

    public Tika secondaryParser;
    private List<NERecogniser> nerChain;
    private volatile boolean initialized = false;
    private volatile boolean available = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentFile` is accessed in both synchronized and unsynchronized contexts
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
    //being processed.  It is null if no file is currently being processed.
    //no need for volatile because of lock for checkForStales
    private FileStarted currentFile = null;
    private final AtomicInteger numResourcesConsumed = new AtomicInteger(0);
    private final AtomicInteger numHandledExceptions = new AtomicInteger(0);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `endpointConfigurationService` is accessed in both synchronized and unsynchronized contexts
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
    private String accessKey;
    private String secretKey;
    private String endpointConfigurationService;
    private String prefix;
    private String credentialsProvider;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `region` is accessed in both synchronized and unsynchronized contexts
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
    private static final String PREFIX = "s3";
    private final Object[] clientLock = new Object[0];
    private String region;
    private String bucket;
    private String profile;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pathStyleAccessEnabled` is accessed in both synchronized and unsynchronized contexts
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
    private long sleepBeforeRetryMillis = 30000;
    private long maxLength = -1;
    private boolean pathStyleAccessEnabled = false;

    @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `recogniser` is accessed in both synchronized and unsynchronized contexts
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/ObjectRecognitionParser.java`
#### Snippet
```java
            ObjectRecognitionParser.class.getPackage().getName() + ".object.rec.impl";
    private static final Logger LOG = LoggerFactory.getLogger(ObjectRecognitionParser.class);
    private ObjectRecogniser recogniser;

    @Field(name = "class")
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`(0xff & bytes[index + 1]) << 0` can be replaced with '(0xff \& bytes\[index + 1\])'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/BitConverter.java`
#### Snippet
```java
                    "The length of the byte array must be at least 2 bytes long.");
        }
        return (char) ((0xff & bytes[index]) << 8 | (0xff & bytes[index + 1]) << 0);
    }

```

## RuleId[id=HtmlWrongAttributeValue]
### HtmlWrongAttributeValue
Wrong attribute value
in `log/indexing-diagnostic/project.15375f63/diagnostic-2023-03-17-08-17-02.509.html`
#### Snippet
```java
              <td>0</td>
              <td>0</td>
              <td><textarea rows="10" cols="75" readonly="true" placeholder="empty" style="white-space: pre; border: none">Not collected for refresh</textarea></td>
            </tr>
          </tbody>
```

## RuleId[id=CallToStringConcatCanBeReplacedByOperator]
### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java
            return leftPad(str, size, String.valueOf(padChar));
        }
        return repeat(padChar, pads).concat(str);
    }

```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java

        if (pads == padLen) {
            return padStr.concat(str);
        } else if (pads < padLen) {
            return padStr.substring(0, pads).concat(str);
```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java
            return padStr.concat(str);
        } else if (pads < padLen) {
            return padStr.substring(0, pads).concat(str);
        } else {
            final char[] padding = new char[pads];
```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java
                padding[i] = padChars[i % padLen];
            }
            return new String(padding).concat(str);
        }
    }
```

## RuleId[id=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `tika-core/src/main/java/org/apache/tika/fork/ForkParser.java`
#### Snippet
```java
            } else if (currentlyInUse >= poolSize) {
                try {
                    wait();
                } catch (InterruptedException e) {
                    throw new TikaException("Interrupted while waiting for a fork parser", e);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `tika-core/src/main/java/org/apache/tika/fork/ForkParser.java`
#### Snippet
```java
                pool.offer(client);
            }
            notifyAll();
        } else {
            client.close();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypes.java`
#### Snippet
```java

            if (mime == null) {
                synchronized (this) {
                    // Double check it didn't already get added while
                    //  we were waiting for the lock
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `tika-core/src/main/java/org/apache/tika/utils/AnnotationUtils.java`
#### Snippet
```java
        Class<?> beanClass = bean.getClass();
        if (!PARAM_INFO.containsKey(beanClass)) {
            synchronized (TikaConfig.class) {
                if (!PARAM_INFO.containsKey(beanClass)) {
                    List<AccessibleObject> aObjs =
```

## RuleId[id=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'org.apache.tika.exception.TikaException', in the throws list already.
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java
     * @param dir_chunk
     */
    private void enumerateOneSegment(byte[] dir_chunk) throws ChmParsingException, TikaException {
//        try {
        if (dir_chunk != null) {
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadataDeserializer.java`
#### Snippet
```java
    public Metadata deserialize(JsonParser jsonParser,
                                DeserializationContext deserializationContext)
            throws IOException, JsonProcessingException {
        return readMetadataObject(jsonParser);
    }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniser.java`
#### Snippet
```java
    }

    public AgePredicterLocal getAgePredictorClient() throws InvalidFormatException, IOException {
        if (agePredictor == null) {
            agePredictor = new AgePredicterLocal(config.getPathClassifyModel(),
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `tika-example/src/main/java/org/apache/tika/example/TrecDocumentGenerator.java`
#### Snippet
```java
public class TrecDocumentGenerator {
    public TrecDocument summarize(File file)
            throws FileNotFoundException, IOException, TikaException {
        Tika tika = new Tika();
        Metadata met = new Metadata();
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `tika-example/src/main/java/org/apache/tika/example/RecentFiles.java`
#### Snippet
```java
            new SimpleDateFormat("E, dd MMM yyyy HH:mm:ss z", Locale.getDefault());

    public String generateRSS(Path indexFile) throws CorruptIndexException, IOException {
        StringBuffer output = new StringBuffer();
        output.append(getRSSHeaders());
```

## RuleId[id=DoubleBraceInitialization]
### DoubleBraceInitialization
Double brace initialization
in `tika-core/src/main/java/org/apache/tika/config/ParamField.java`
#### Snippet
```java
    // Example : short.class.isAssignableFrom(Short.class) ? false
    private static final Map<Class<?>, Class<?>> PRIMITIVE_MAP =
            new HashMap<Class<?>, Class<?>>() {{
                    put(int.class, Integer.class);
                    put(short.class, Short.class);
```

### DoubleBraceInitialization
Double brace initialization
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
                    solrInputDocument.setField(n, vals[0]);
                } else {
                    solrInputDocument.setField(n, new HashMap<String, String>() {
                        {
                            put("set", vals[0]);
```

### DoubleBraceInitialization
Double brace initialization
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
                    solrInputDocument.setField(n, vals);
                } else {
                    solrInputDocument.setField(n, new HashMap<String, String[]>() {
                        {
                            put("set", vals);
```

### DoubleBraceInitialization
Double brace initialization
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DefaultHtmlMapper.java`
#### Snippet
```java
    public static final HtmlMapper INSTANCE = new DefaultHtmlMapper();
    // Based on http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd
    private static final Map<String, String> SAFE_ELEMENTS = new HashMap<String, String>() {{
            put("H1", "h1");
            put("H2", "h2");
```

### DoubleBraceInitialization
Double brace initialization
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DefaultHtmlMapper.java`
#### Snippet
```java
    // http://www.w3schools.com/TAGS/
    private static final Map<String, Set<String>> SAFE_ATTRIBUTES =
            new HashMap<String, Set<String>>() {{
                    put("a", attrSet("charset", "type", "name", "href", "hreflang", "rel", "rev",
                            "shape", "coords"));
```

### DoubleBraceInitialization
Double brace initialization
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DefaultHtmlMapper.java`
#### Snippet
```java
            put("DEL", "del");
        }};
    private static final Set<String> DISCARDABLE_ELEMENTS = new HashSet<String>() {{
            add("STYLE");
            add("SCRIPT");
```

### DoubleBraceInitialization
Double brace initialization
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/nltk/NLTKNERecogniser.java`
#### Snippet
```java
     * some common entities identified by NLTK
     */
    public static final Set<String> ENTITY_TYPES = new HashSet<String>() {
        {
            add("NAMES");
```

### DoubleBraceInitialization
Double brace initialization
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/mitie/MITIENERecogniser.java`
#### Snippet
```java
public class MITIENERecogniser implements NERecogniser {
    public static final String MODEL_PROP_NAME = "ner.mitie.model";
    public static final Set<String> ENTITY_TYPES = new HashSet<String>() {
        {
            add(PERSON);
```

### DoubleBraceInitialization
Double brace initialization
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/corenlp/CoreNLPNERecogniser.java`
#### Snippet
```java
    public static final String DEFAULT_MODEL_PATH = NER_7CLASS_MODEL;
    public static final String MODEL_PROP_NAME = "ner.corenlp.model";
    public static final Set<String> ENTITY_TYPES = new HashSet<String>() {
        {
            add(PERSON);
```

### DoubleBraceInitialization
Double brace initialization
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/grobid/GrobidNERecogniser.java`
#### Snippet
```java
     * Useful Entities from Grobid NER
     */
    public static final Set<String> ENTITY_TYPES = new HashSet<String>() {
        {
            add("MEASUREMENT_NUMBERS");
```

### DoubleBraceInitialization
Double brace initialization
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/opennlp/OpenNLPNERecogniser.java`
#### Snippet
```java
    public static final String NER_MONEY_MODEL = MODELS_DIR + "/" + MONEY_FILE;

    public static final Map<String, String> DEFAULT_MODELS = new HashMap<String, String>() {
        {
            put(PERSON, NER_PERSON_MODEL);
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `picture` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WMFParser.java`
#### Snippet
```java
        xhtml.startDocument();
        try {
            HwmfPicture picture = null;
            try {
                picture = new HwmfPicture(stream);
```

### UnusedAssignment
Variable `pkg` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
        //Unfortunately, POI can throw a RuntimeException...so we
        //have to catch that.
        OPCPackage pkg = null;
        try {
            pkg = OPCPackage.open(zipEntrySource);
```

### UnusedAssignment
Variable `inp` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractPOIFSExtractor.java`
#### Snippet
```java
        }
        int length = contentsEntry.getSize();
        DocumentInputStream inp = null;
        try {
            inp = new DocumentInputStream(contentsEntry);
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractPOIFSExtractor.java`
#### Snippet
```java

                Detector detector = new DefaultZipContainerDetector();
                MediaType type = null;
                try {
                    type = detector.detect(stream, metadata);
```

### UnusedAssignment
Variable `entry` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/SummaryExtractor.java`
#### Snippet
```java
                return;
            }
            DocumentEntry entry = null;

            try {
```

### UnusedAssignment
Variable `isLegal` initializer `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java

    private LevelTuple buildTuple(int i, ListLevel listLevel) {
        boolean isLegal = false;
        int start = 1;
        int restart = -1;
```

### UnusedAssignment
Variable `start` initializer `1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
    private LevelTuple buildTuple(int i, ListLevel listLevel) {
        boolean isLegal = false;
        int start = 1;
        int restart = -1;
        String lvlText = "%" + i + ".";
```

### UnusedAssignment
Variable `restart` initializer `-1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
        boolean isLegal = false;
        int start = 1;
        int restart = -1;
        String lvlText = "%" + i + ".";
        String numFmt = "decimal";
```

### UnusedAssignment
Variable `lvlText` initializer `"%" + i + "."` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
        int start = 1;
        int restart = -1;
        String lvlText = "%" + i + ".";
        String numFmt = "decimal";

```

### UnusedAssignment
Variable `numFmt` initializer `"decimal"` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
        int restart = -1;
        String lvlText = "%" + i + ".";
        String numFmt = "decimal";

        start = listLevel.getStartAt();
```

### UnusedAssignment
Variable `currAbNumId` initializer `-1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
        //lsid is equivalent to docx's abnum
        //ilfo is equivalent to docx's num
        int currAbNumId = -1;
        try {
            currAbNumId = paragraph.getList().getLsid();
```

### UnusedAssignment
Variable `shouldProcess` initializer `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
                    }
                } else {
                    boolean shouldProcess = false;
                    try {
                        tis.mark(1);
```

### UnusedAssignment
Variable `mediaType` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
        //TODO -- inject progId into the metadata of the embedded file
        try (TikaInputStream stream = TikaInputStream.get(dataStream)) {
            String mediaType = null;
            if ("Excel.Chart.8".equals(progId)) {
                mediaType = "application/vnd.ms-excel";
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
                HSLFObjectShape oleShape = (HSLFObjectShape) shape;

                HSLFObjectData data = null;
                try {
                    data = oleShape.getObjectData();
```

### UnusedAssignment
Variable `dataStream` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
                    xhtml.startElement("div", attributes);
                    xhtml.endElement("div");
                    InputStream dataStream = null;
                    try {
                        dataStream = data.getInputStream();
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
                    break;
            }
            byte[] data = null;
            try {
                data = pic.getData();
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParser.java`
#### Snippet
```java
            throws IOException, SAXException {

        VBAMacroReader reader = null;
        Map<String, String> macros = null;
        try {
```

### UnusedAssignment
Variable `macros` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParser.java`
#### Snippet
```java

        VBAMacroReader reader = null;
        Map<String, String> macros = null;
        try {
            reader = new VBAMacroReader(fs);
```

### UnusedAssignment
Variable `fs` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParser.java`
#### Snippet
```java
                    root = (DirectoryNode) container;
                } else {
                    POIFSFileSystem fs = null;
                    if (tstream.hasFile()) {
                        fs = new POIFSFileSystem(tstream.getFile(), true);
```

### UnusedAssignment
Variable `p` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
         */
        private Picture nextUnclaimed() {
            Picture p = null;
            while (pn < nonU1based.size()) {
                p = nonU1based.get(pn);
```

### UnusedAssignment
Variable `numString` initializer `""` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractListManager.java`
#### Snippet
```java
                    //swallow
                }
                String numString = "";
                //need to subtract 1 because, e.g. %1 is the format
                //for the number at array offset 0
```

### UnusedAssignment
Variable `tis` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
                //and see if getFilePath or getLocalFilePath is meaningful
                //for TikaCoreProperties.ORIGINAL_RESOURCE_NAME
                TikaInputStream tis = null;
                try {
                    tis = TikaInputStream.get(spc.getStream());
```

### UnusedAssignment
The value `senderAddresType.get(0).toString()` assigned to `senderAddressTypeString` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
        String senderAddressTypeString = "";
        if (senderAddresType != null && senderAddresType.size() > 0) {
            senderAddressTypeString = senderAddresType.get(0).toString();
        }

```

### UnusedAssignment
Variable `stream` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmParser.java`
#### Snippet
```java
    private void parsePage(byte[] byteObject, Parser htmlParser, ContentHandler xhtml,
                           ParseContext context) throws TikaException, SAXException { // throws IOException
        InputStream stream = null;
        Metadata metadata = new Metadata();
        ContentHandler handler = new EmbeddedContentHandler(new BodyContentHandler(xhtml));// -1
```

### UnusedAssignment
Variable `cellString` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
                    LabelSSTRecord sst = (LabelSSTRecord) record;
                    UnicodeString unicode = sstRecord.getString(sst.getSSTIndex());
                    String cellString = null;
                    if (officeParserConfig.isConcatenatePhoneticRuns()) {
                        String phonetic = (unicode != null && unicode.getExtendedRst() != null &&
```

### UnusedAssignment
Variable `lzxBlock` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
            throws TikaException {
        UnsynchronizedByteArrayOutputStream buffer = new UnsynchronizedByteArrayOutputStream();
        ChmLzxBlock lzxBlock = null;
        try {
            /* UNCOMPRESSED type is easiest one */
```

### UnusedAssignment
Variable `i` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
                        chmBlockInfo);

                int i = 0, start = 0, hit_cache = 0;

                if ((getLzxBlockLength() < Integer.MAX_VALUE) &&
```

### UnusedAssignment
Variable `start` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
                        chmBlockInfo);

                int i = 0, start = 0, hit_cache = 0;

                if ((getLzxBlockLength() < Integer.MAX_VALUE) &&
```

### UnusedAssignment
Variable `index` initializer `-1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmPmgiHeader.java`
#### Snippet
```java
    private void unmarshalCharArray(byte[] data, ChmPmgiHeader chmPmgiHeader, int count)
            throws ChmParsingException {
        int index = -1;
        ChmAssert.assertByteArrayNotNull(data);
        ChmAssert.assertChmAccessorNotNull(chmPmgiHeader);
```

### UnusedAssignment
The value `42` assigned to `position_slots` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxState.java`
#### Snippet
```java
            /* Calculates required position slots */
            if (win == 20) {
                position_slots = 42;
            } else if (win == 21) {
                position_slots = 50;
```

### UnusedAssignment
The value `50` assigned to `position_slots` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxState.java`
#### Snippet
```java
                position_slots = 42;
            } else if (win == 21) {
                position_slots = 50;
            } else {
                position_slots = win << 1;
```

### UnusedAssignment
The value `win << 1` assigned to `position_slots` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxState.java`
#### Snippet
```java
                position_slots = 50;
            } else {
                position_slots = win << 1;
            }
            //TODO: position_slots is not used ?
```

### UnusedAssignment
Variable `dir_chunk` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java

            /* loops over all pmgls */
            byte[] dir_chunk = null;
            Set<Integer> processed = new HashSet<>();
            for (int i = startPmgl; i >= 0; ) {
```

### UnusedAssignment
The value `null` assigned to `dir_chunk` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java
                }
                i = nextBlock;
                dir_chunk = null;
            }

```

### UnusedAssignment
The value `ChmConstants.CHM_PMGI_LEN` assigned to `header_len` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java
            int header_len;
            if (startsWith(dir_chunk, ChmConstants.CHM_PMGI_MARKER)) {
                header_len = ChmConstants.CHM_PMGI_LEN;
                return; //skip PMGI
            } else if (startsWith(dir_chunk, ChmConstants.PMGL)) {
```

### UnusedAssignment
Variable `blockLength` initializer `-1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
        ChmAssert.assertChmBlockSegment(data, resetTable, blockNumber, lzxcBlockOffset,
                lzxcBlockLength);
        int blockLength = -1;
        // TODO add int_max_value checking
        if (blockNumber < (resetTable.getBlockAddress().length - 1)) {
```

### UnusedAssignment
Variable `next` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
     */
    public static int indexOf(byte[] text, byte[] pattern) throws ChmParsingException {
        int[] next = null;
        int i = 0, j = -1;

```

### UnusedAssignment
Variable `fileNameToUse` initializer `""` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
            unicodeFilePath.setLength(0);
        }
        String fileNameToUse = "";
        String pathToUse = "";
        if (unicodeFilePath.length() > 0) {
```

### UnusedAssignment
Variable `pathToUse` initializer `""` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
        }
        String fileNameToUse = "";
        String pathToUse = "";
        if (unicodeFilePath.length() > 0) {
            String p = unicodeFilePath.toString();
```

### UnusedAssignment
Variable `hasPoifs` initializer `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
        } else {
            UnsynchronizedByteArrayInputStream embIs = new UnsynchronizedByteArrayInputStream(embObjBytes);
            boolean hasPoifs = false;
            try {
                hasPoifs = hasPOIFSHeader(embIs);
```

### UnusedAssignment
Variable `tis` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/pst/OutlookPSTParser.java`
#### Snippet
```java
                xhtml.startElement("div", attributes);
                if (embeddedExtractor.shouldParseEmbedded(attachMeta)) {
                    TikaInputStream tis = null;
                    try {
                        tis = TikaInputStream.get(attach.getFileInputStream());
```

### UnusedAssignment
Variable `prc` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
    private void addRelatedParts(PackagePart documentPart, List<PackagePart> relatedParts) {
        for (String relation : MAIN_PART_RELATIONS) {
            PackageRelationshipCollection prc = null;
            try {
                prc = documentPart.getRelationshipsByType(relation);
```

### UnusedAssignment
Variable `matchlen` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        short s;
        int x, i, border;
        int matchlen = 0, matchfooter = 0, extra, rundest, runsrc;
        int matchoffset = 0;
        for (i = getContentLength(); i < len; i++) {
```

### UnusedAssignment
Variable `matchfooter` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        short s;
        int x, i, border;
        int matchlen = 0, matchfooter = 0, extra, rundest, runsrc;
        int matchoffset = 0;
        for (i = getContentLength(); i < len; i++) {
```

### UnusedAssignment
Variable `matchoffset` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        int x, i, border;
        int matchlen = 0, matchfooter = 0, extra, rundest, runsrc;
        int matchoffset = 0;
        for (i = getContentLength(); i < len; i++) {
            /* new code */
```

### UnusedAssignment
Variable `matchlen` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        short s;
        int x, i;
        int matchlen = 0, matchfooter = 0, extra, rundest, runsrc;
        int matchoffset = 0;
        for (i = getContentLength(); i < len; i++) {
```

### UnusedAssignment
Variable `matchfooter` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        short s;
        int x, i;
        int matchlen = 0, matchfooter = 0, extra, rundest, runsrc;
        int matchoffset = 0;
        for (i = getContentLength(); i < len; i++) {
```

### UnusedAssignment
Variable `matchoffset` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        int x, i;
        int matchlen = 0, matchfooter = 0, extra, rundest, runsrc;
        int matchoffset = 0;
        for (i = getContentLength(); i < len; i++) {
            int f = getChmSection().peekBits(ChmConstants.LZX_MAINTREE_TABLEBITS);
```

### UnusedAssignment
Variable `start` initializer `1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
    private LevelTuple buildTuple(int level, CTLvl ctLvl) {
        boolean isLegal = false;
        int start = 1;
        int restart = -1;
        String lvlText = "%" + level + ".";
```

### UnusedAssignment
The value `true` assigned to `b` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
        try {
            Class.forName("org.openxmlformats.schemas.wordprocessingml.x2006.main.CTNumLvl");
            b = true;
        } catch (ClassNotFoundException e) {
            //swallow
```

### UnusedAssignment
The value `OVERRIDE_AVAILABLE = false` assigned to `b` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
            //swallow
        }
        b = OVERRIDE_AVAILABLE = false;

    }
```

### UnusedAssignment
Variable `document` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
    protected List<PackagePart> getMainDocumentParts() throws TikaException {
        List<PackagePart> parts = new ArrayList<>();
        XSLFSlideShow document = null;
        try {
            document = new XSLFSlideShow(extractor.getPackage());
```

### UnusedAssignment
Variable `sheetPart` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSSFExcelExtractorDecorator.java`
#### Snippet
```java
        while (iter.hasNext()) {
            SheetTextAsHTML sheetExtractor = new SheetTextAsHTML(config, xhtml);
            PackagePart sheetPart = null;
            try (InputStream stream = iter.next()) {
                sheetPart = iter.getSheetPart();
```

### UnusedAssignment
Variable `stream` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSExtractorDecorator.java`
#### Snippet
```java
                                     EmbeddedDocumentUtil embeddedDocumentUtil,
                                     XHTMLContentHandler xhtml) throws SAXException, IOException {
        InputStream stream = null;
        try {
            stream = getZipStream(zipPath, pkg);
```

### UnusedAssignment
Variable `relativeRoot` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSExtractorDecorator.java`
#### Snippet
```java

            //relative root is /Documents/1 ..need this Pages...
            String relativeRoot = null;
            int i = docRef.lastIndexOf("/");
            if (i > 0) {
```

### UnusedAssignment
Variable `extractor` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
        OPCPackage pkg = null;
        try {
            OOXMLExtractor extractor = null;

            // Locate or Open the OPCPackage for the file
```

### UnusedAssignment
Variable `prc` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
    private void addRelatedParts(PackagePart documentPart, List<PackagePart> relatedParts) {
        for (String relation : MAIN_PART_RELATIONS) {
            PackageRelationshipCollection prc = null;
            try {
                prc = documentPart.getRelationshipsByType(relation);
```

### UnusedAssignment
Variable `fs` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
        InputStream is = part.getInputStream();
        // Open the POIFS (OLE2) structure and process
        POIFSFileSystem fs = null;
        try {
            fs = new POIFSFileSystem(part.getInputStream());
```

### UnusedAssignment
Variable `hyperlink` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
        } else if (HYPERLINK.equals(localName)) { //docx hyperlink
            String hyperlinkId = atts.getValue(OFFICE_DOC_RELATIONSHIP_NS, "id");
            String hyperlink = null;
            if (hyperlinkId != null) {
                hyperlink = linkedRelationships.get(hyperlinkId);
```

### UnusedAssignment
Variable `hyperlink` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
        } else if (HLINK_CLICK.equals(localName)) { //pptx hyperlink
            String hyperlinkId = atts.getValue(OFFICE_DOC_RELATIONSHIP_NS, "id");
            String hyperlink = null;
            if (hyperlinkId != null) {
                hyperlink = linkedRelationships.get(hyperlinkId);
```

### UnusedAssignment
The value `0` assigned to `data.rawData.cb` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java

                    data.rawData.stp = offset;
                    data.rawData.cb = 0;
                    if (offset + val32 > end) {
                        data.rawData.cb = end - offset;
```

### UnusedAssignment
Variable `prop` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
     */
    private static final int getPrecacheSize() {
        String prop = null;
        long propParsed;

```

### UnusedAssignment
Variable `headerLength` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/RevisionManifestDataElementData.java`
#### Snippet
```java
        this.revisionManifestObjectGroupReferences = new ArrayList<>();
        AtomicReference<StreamObjectHeaderStart> header = new AtomicReference<>();
        int headerLength = 0;
        while ((headerLength = StreamObjectHeaderStart.tryParse(byteArray, index.get(), header)) !=
                0) {
```

### UnusedAssignment
Variable `revisionObject` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/RevisionStoreObjectGroup.java`
#### Snippet
```java
        Map<ExGuid, RevisionStoreObject> objectDict = new HashMap<>();
        if (!isEncryption) {
            RevisionStoreObject revisionObject = null;
            for (int i = 0; i < dataObject.objectGroupDeclarations.objectDeclarationList.size();
                    i++) {
```

### UnusedAssignment
Variable `headerLength` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageIndexDataElementData.java`
#### Snippet
```java
            throws TikaException, IOException {
        AtomicInteger index = new AtomicInteger(startIndex);
        int headerLength = 0;
        AtomicReference<StreamObjectHeaderStart> header = new AtomicReference<>();
        boolean isStorageIndexManifestMappingExist = false;
```

### UnusedAssignment
Variable `headerLength` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupDeclarations.java`
#### Snippet
```java

        AtomicInteger index = new AtomicInteger(currentIndex.get());
        int headerLength = 0;
        AtomicReference<StreamObjectHeaderStart> header = new AtomicReference<>();
        this.objectDeclarationList = new ArrayList<>();
```

### UnusedAssignment
Variable `buf` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteTreeWalker.java`
#### Snippet
```java
    private void handleEmbedded(int length) throws TikaException, IOException, SAXException {
        TikaInputStream stream = null;
        ByteBuffer buf = null;
        try {
            buf = ByteBuffer.allocate(length);
```

### UnusedAssignment
Variable `length` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObject.java`
#### Snippet
```java
        AtomicInteger tmpIndex = new AtomicInteger(index.get());

        int length = 0;
        AtomicReference<StreamObjectHeaderStart> streamObjectHeader = new AtomicReference<>();
        if ((length =
```

### UnusedAssignment
Variable `headerLength` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupData.java`
#### Snippet
```java

        AtomicInteger index = new AtomicInteger(currentIndex.get());
        int headerLength = 0;
        AtomicReference<StreamObjectHeaderStart> header = new AtomicReference<>();

```

### UnusedAssignment
Variable `headerLength` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageManifestDataElementData.java`
#### Snippet
```java

        AtomicReference<StreamObjectHeaderStart> header = new AtomicReference<>();
        int headerLength = 0;
        while ((headerLength = StreamObjectHeaderStart.tryParse(byteArray, index.get(), header)) !=
                0) {
```

### UnusedAssignment
Variable `bitWriter` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/SerialNumber.java`
#### Snippet
```java
    @Override
    public List<Byte> serializeToByteList() throws IOException {
        BitWriter bitWriter = null;
        if (this.guid.equals(GuidUtil.emptyGuid())) {
            bitWriter = new BitWriter(1);
```

### UnusedAssignment
The value `compressedSize` assigned to `headerLength` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/ZipFilesChunking.java`
#### Snippet
```java
                        Arrays.copyOfRange(this.fileContent, index, headerLength + compressedSize),
                        this.getSingleChunkSignature(header, dataFileSignatureBytes.get())));
                index += headerLength += compressedSize;
            } else {
                list.add(builder.Build(header, this.getSHA1Signature(header)));
```

### UnusedAssignment
Variable `m` initializer `null` is redundant
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadata.java`
#### Snippet
```java
     */
    public static Metadata fromJson(Reader reader) throws IOException {
        Metadata m = null;
        try (JsonParser jParser = new JsonFactory().createParser(new CloseShieldReader(reader))) {
            m = readMetadataObject(jParser);
```

### UnusedAssignment
Variable `mmSchema` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/JempboxExtractor.java`
#### Snippet
```java
            return;
        }
        XMPSchemaMediaManagement mmSchema = null;
        try {
            mmSchema = xmp.getMediaManagementSchema();
```

### UnusedAssignment
Variable `suffix` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
    //suffix of the file if known. This is used to create temp files
    //with the right suffixes. This should include the initial . as in ".doc"
    private String suffix = null;

    /**
```

### UnusedAssignment
Variable `obj` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-transcribe-aws/src/main/java/org/apache/tika/parser/transcribe/aws/AmazonTranscribe.java`
#### Snippet
```java
        }
        JSONParser parser = new JSONParser();
        JSONObject obj = null;
        try {
            obj = (JSONObject) parser.parse(text);
```

### UnusedAssignment
The value changed at `i++` is never used
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
        int b1 = data[i++] & 0xFF;
        int b2 = data[i++] & 0xFF;
        int b3 = data[i++] & 0xFF;
        return (b3 << 24) + (b2 << 16) + (b1 << 8) + (b0);
    }
```

### UnusedAssignment
The value changed at `i++` is never used
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
        int b1 = data[i++] & 0xFF;
        int b2 = data[i++] & 0xFF;
        int b3 = data[i++] & 0xFF;
        return (b0 << 24) + (b1 << 16) + (b2 << 8) + (b3);
    }
```

### UnusedAssignment
Variable `score` initializer `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
                                                              double threshold) {
        ArrayList<StandardReference> standards = new ArrayList<>();
        double score = 0;

        Pattern pattern = Pattern.compile(REGEX_FALLBACK);
```

### UnusedAssignment
Variable `parsing` initializer `false` is redundant
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
    private Object parser;
    private ClassLoader classLoader;
    private boolean parsing = false;
    private long since;
    /**
```

### UnusedAssignment
Variable `serverWaitTimeoutMillis` initializer `60000` is redundant
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
    private long serverPulseMillis = 5000;
    private long serverParserTimeoutMillis = 60000;
    private long serverWaitTimeoutMillis = 60000;
    //can't be class Parser because then you'd
    //have to include that in bootstrap jar (legacy mode)
```

### UnusedAssignment
Variable `serverPulseMillis` initializer `5000` is redundant
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
    //milliseconds to sleep before checking to see if there has been any reading/writing
    //If no reading or writing in this time, shutdown the server.
    private long serverPulseMillis = 5000;
    private long serverParserTimeoutMillis = 60000;
    private long serverWaitTimeoutMillis = 60000;
```

### UnusedAssignment
Variable `serverParserTimeoutMillis` initializer `60000` is redundant
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
    //If no reading or writing in this time, shutdown the server.
    private long serverPulseMillis = 5000;
    private long serverParserTimeoutMillis = 60000;
    private long serverWaitTimeoutMillis = 60000;
    //can't be class Parser because then you'd
```

### UnusedAssignment
Variable `namespaceURI` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
        private MimeType type = null;

        private String namespaceURI = null;

        private String localName = null;
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
        private static final long serialVersionUID = 5140496601491000730L;

        private MimeType type = null;

        private String namespaceURI = null;
```

### UnusedAssignment
Variable `localName` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
        private String namespaceURI = null;

        private String localName = null;

        RootXML(MimeType type, String namespaceURI, String localName) {
```

### UnusedAssignment
Variable `type` initializer `BasicContentHandlerFactory.HANDLER_TYPE.TEXT` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/HandlerConfig.java`
#### Snippet
```java

    private BasicContentHandlerFactory.HANDLER_TYPE type =
            BasicContentHandlerFactory.HANDLER_TYPE.TEXT;

    int writeLimit = -1;
```

### UnusedAssignment
Variable `parseMode` initializer `PARSE_MODE.RMETA` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/HandlerConfig.java`
#### Snippet
```java

    boolean throwOnWriteLimitReached = true;
    PARSE_MODE parseMode = PARSE_MODE.RMETA;


```

### UnusedAssignment
Variable `writeLimit` initializer `-1` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/HandlerConfig.java`
#### Snippet
```java
            BasicContentHandlerFactory.HANDLER_TYPE.TEXT;

    int writeLimit = -1;
    int maxEmbeddedResources = -1;

```

### UnusedAssignment
Variable `throwOnWriteLimitReached` initializer `true` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/HandlerConfig.java`
#### Snippet
```java
    int maxEmbeddedResources = -1;

    boolean throwOnWriteLimitReached = true;
    PARSE_MODE parseMode = PARSE_MODE.RMETA;

```

### UnusedAssignment
Variable `maxEmbeddedResources` initializer `-1` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/HandlerConfig.java`
#### Snippet
```java

    int writeLimit = -1;
    int maxEmbeddedResources = -1;

    boolean throwOnWriteLimitReached = true;
```

### UnusedAssignment
Variable `parser` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesReader.java`
#### Snippet
```java
    private static SAXParser acquireSAXParser() throws TikaException {
        while (true) {
            SAXParser parser = null;
            try {
                READ_WRITE_LOCK.readLock().lock();
```

### UnusedAssignment
Variable `p` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/fs/FileSystemFetcher.java`
#### Snippet
```java
                    "a file name with this character in it.");
        }
        Path p = null;
        if (basePath != null) {
            p = basePath.resolve(fetchKey);
```

### UnusedAssignment
Variable `future` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/ConcurrentUtils.java`
#### Snippet
```java
    public static Future execute(ParseContext context, Runnable runnable) {

        Future future = null;
        ExecutorService executorService = context.get(ExecutorService.class);
        if (executorService == null) {
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
                        return PARSER_FUTURE_CODE;
                    } else {
                        PipesResult result = null;
                        long start = System.currentTimeMillis();
                        try {
```

### UnusedAssignment
Variable `emitter` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java

    private void emit(String taskId, EmitData emitData, String parseExceptionStack) {
        Emitter emitter = null;

        try {
```

### UnusedAssignment
Variable `canReset` initializer `false` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/XMLReaderUtils.java`
#### Snippet
```java

    private static PoolSAXParser buildPoolParser(int generation, SAXParser parser) {
        boolean canReset = false;
        try {
            parser.reset();
```

### UnusedAssignment
Variable `builder` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/XMLReaderUtils.java`
#### Snippet
```java
        long lastWarn = -1;
        while (true) {
            PoolDOMBuilder builder = null;
            DOM_READ_WRITE_LOCK.readLock().lock();
            try {
```

### UnusedAssignment
Variable `parser` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/XMLReaderUtils.java`
#### Snippet
```java
        long lastWarn = -1;
        while (true) {
            PoolSAXParser parser = null;
            SAX_READ_WRITE_LOCK.readLock().lock();
            try {
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
                continue;
            }
            Object value = null;
            try {
                value = getterTuple.method.invoke(object);
```

### UnusedAssignment
Variable `item` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
                                         MethodTuple getter, Object object) {

        Object item = null;
        try {
            item = getter.method.invoke(object);
```

### UnusedAssignment
Variable `key` initializer `""` is redundant
in `tika-core/src/main/java/org/apache/tika/config/Param.java`
#### Snippet
```java
        while (child != null) {
            if (child.getNodeType() == Node.ELEMENT_NODE) {
                String key = "";
                String value = "";
                if (child.getAttributes().getNamedItem("key") != null) {
```

### UnusedAssignment
Variable `value` initializer `""` is redundant
in `tika-core/src/main/java/org/apache/tika/config/Param.java`
#### Snippet
```java
            if (child.getNodeType() == Node.ELEMENT_NODE) {
                String key = "";
                String value = "";
                if (child.getAttributes().getNamedItem("key") != null) {
                    key = child.getAttributes().getNamedItem("key").getNodeValue();
```

### UnusedAssignment
Variable `method` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/Param.java`
#### Snippet
```java
                        Param param = load(params.item(j));

                        Method method = null;
                        String methodName = "set" +
                                param.getName().substring(0,1).toUpperCase(Locale.US) +
```

### UnusedAssignment
Variable `tmpVal` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java

        byte[] decoded = null;
        String tmpVal = null;
        int radix = 8;

```

### UnusedAssignment
Variable `radix` initializer `8` is redundant
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
        byte[] decoded = null;
        String tmpVal = null;
        int radix = 8;

        // hex
```

### UnusedAssignment
Variable `match` initializer `false` is redundant
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
                Matcher m = p.matcher(result);

                boolean match = false;
                // Loop until we've covered the entire offset range
                for (int i = 0; i <= offsetRangeEnd - offsetRangeBegin; i++) {
```

### UnusedAssignment
Variable `len` initializer `-1l` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParser.java`
#### Snippet
```java
        }
        if (metadata.get(Metadata.CONTENT_LENGTH) != null) {
            long len = -1l;
            try {
                len = Long.parseLong(metadata.get(Metadata.CONTENT_LENGTH));
```

### UnusedAssignment
Variable `fallback` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParser.java`
#### Snippet
```java

    private static Parser buildFallbackParser(TikaConfig config) {
        Parser fallback = null;
        Parser p = config.getParser();
        if (p instanceof DefaultParser) {
```

### UnusedAssignment
Variable `embeddedIdPath` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
        private String location = null;

        private String embeddedIdPath = null;


```

### UnusedAssignment
Variable `location` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
        private static final long serialVersionUID = 207648200464263337L;
        private final ParserState parserState;
        private String location = null;

        private String embeddedIdPath = null;
```

### UnusedAssignment
Variable `objectName` initializer `""` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java

    private String getResourceName(Metadata metadata, ParserState state) {
        String objectName = "";
        if (metadata.get(TikaCoreProperties.RESOURCE_NAME_KEY) != null) {
            objectName = metadata.get(TikaCoreProperties.RESOURCE_NAME_KEY);
```

### UnusedAssignment
Variable `finishedStream` initializer `false` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/digest/InputStreamDigester.java`
#### Snippet
```java
        //then stop and spool to file via TikaInputStream
        BoundedInputStream bis = new BoundedInputStream(markLimit, is);
        boolean finishedStream = false;
        bis.mark(markLimit + 1);
        finishedStream = digestStream(bis, metadata);
```

### UnusedAssignment
Variable `constructor` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/ConfigBase.java`
#### Snippet
```java
            if (compositeElementName.equals(child.getLocalName())) {
                List<T> components = loadComposite(child, itemName, itemClass);
                Constructor constructor = null;
                try {
                    constructor = compositeClass.getConstructor(List.class);
```

### UnusedAssignment
Variable `properties` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/ConfigBase.java`
#### Snippet
```java
        Set<String> settings = new HashSet<>();

        Node properties = null;
        try {
            properties = XMLReaderUtils.buildDOM(is).getDocumentElement();
```

### UnusedAssignment
Variable `properties` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/ConfigBase.java`
#### Snippet
```java
    protected static <T> T buildSingle(String itemName, Class<T> itemClass, InputStream is)
            throws TikaConfigException, IOException {
        Element properties = null;
        try {
            properties = XMLReaderUtils.buildDOM(is).getDocumentElement();
```

### UnusedAssignment
Variable `properties` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/ConfigBase.java`
#### Snippet
```java
                                             String itemName, Class<T> itemClass, InputStream is)
            throws TikaConfigException, IOException {
        Element properties = null;
        try {
            properties = XMLReaderUtils.buildDOM(is).getDocumentElement();
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/external2/ExternalParser.java`
#### Snippet
```java
                }
            }
            FileProcessResult result = null;
            long localTimeoutMillis = TikaTaskTimeout.getTimeoutMillis(context, timeoutMs);
            if (outputFileInCommandline) {
```

### UnusedAssignment
Variable `d` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/metadata/filter/DateNormalizingMetadataFilter.java`
#### Snippet
```java
                        utcFormatter.setTimeZone(UTC);
                    }
                    Date d = null;
                    try {
                        d = dateFormatter.parse(dateString);
```

### UnusedAssignment
Variable `rendererMap` initializer `new HashMap<>()` is redundant
in `tika-core/src/main/java/org/apache/tika/renderer/CompositeRenderer.java`
#### Snippet
```java
public class CompositeRenderer implements Renderer, Initializable {

    private Map<MediaType, Renderer> rendererMap = new HashMap<>();

    public CompositeRenderer(ServiceLoader serviceLoader) {
```

### UnusedAssignment
Variable `location` initializer `null` is redundant
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/isatab/ISArchiveParser.java`
#### Snippet
```java
    private final Set<MediaType> SUPPORTED_TYPES =
            Collections.singleton(MediaType.application("x-isatab"));
    private String location = null;

    private String studyFileName = null;
```

### UnusedAssignment
Variable `metadata` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
     * A map of all metadata attributes.
     */
    private Map<String, String[]> metadata = null;


```

### UnusedAssignment
Variable `other` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
        }

        Metadata other = null;
        try {
            other = (Metadata) o;
```

### UnusedAssignment
Variable `parser` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                throws IllegalAccessException, InstantiationException, NoSuchMethodException,
                InvocationTargetException {
            Parser parser = null;
            if (AbstractEncodingDetectorParser.class.isAssignableFrom(loadedClass)) {
                Constructor ctor = loadedClass.getConstructor(EncodingDetector.class);
```

### UnusedAssignment
Variable `loaded` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            }

            T loaded = null;

            try {
```

### UnusedAssignment
Variable `params` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

                // Get any parameters / settings for the parser
                Map<String, Param> params = null;
                try {
                    params = getParams(element);
```

### UnusedAssignment
Variable `c` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                throws InvocationTargetException, IllegalAccessException, InstantiationException {
            Parser parser = null;
            Constructor<? extends Parser> c = null;
            MediaTypeRegistry registry = mimeTypes.getMediaTypeRegistry();

```

### UnusedAssignment
Variable `cp` initializer `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            if (parser == null && ParserDecorator.class.isAssignableFrom(parserClass)) {
                try {
                    CompositeParser cp = null;
                    if (childParsers.size() == 1 && excludeParsers.size() == 0 &&
                            childParsers.get(0) instanceof CompositeParser) {
```

### UnusedAssignment
Variable `future` initializer `null` is redundant
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaClientCLI.java`
#### Snippet
```java
        int finished = 0;
        while (finished < clientConfig.getNumThreads() + 1) {
            Future<Long> future = null;
            try {
                future = completionService.poll(30, TimeUnit.SECONDS);
```

### UnusedAssignment
Variable `response` initializer `null` is redundant
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaPipesHttpClient.java`
#### Snippet
```java
        try {
            while (tries++ < maxRetries) {
                HttpResponse response = null;
                try {
                    response = httpClient.execute(post);
```

### UnusedAssignment
Variable `dataStore` initializer `null` is redundant
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/geoinfo/GeographicInformationParser.java`
#### Snippet
```java
                      ParseContext parseContext) throws IOException, SAXException, TikaException {
        metadata.set(Metadata.CONTENT_TYPE, geoInfoType);
        DataStore dataStore = null;
        DefaultMetadata defaultMetadata = null;
        XHTMLContentHandler xhtmlContentHandler = new XHTMLContentHandler(contentHandler, metadata);
```

### UnusedAssignment
Variable `defaultMetadata` initializer `null` is redundant
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/geoinfo/GeographicInformationParser.java`
#### Snippet
```java
        metadata.set(Metadata.CONTENT_TYPE, geoInfoType);
        DataStore dataStore = null;
        DefaultMetadata defaultMetadata = null;
        XHTMLContentHandler xhtmlContentHandler = new XHTMLContentHandler(contentHandler, metadata);

```

### UnusedAssignment
Variable `encoder` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-digest-commons/src/main/java/org/apache/tika/parser/digestutils/BouncyCastleDigester.java`
#### Snippet
```java
        for (String digest : digests) {
            String[] parts = digest.split(":");
            DigestingParser.Encoder encoder = null;
            if (parts.length > 1) {
                if (parts[1].equals("16")) {
```

### UnusedAssignment
Variable `output` initializer `null` is redundant
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
        // Execute
        Process process;
        String output = null;
        if (cmd.length == 1) {
            process = Runtime.getRuntime().exec(cmd[0]);
```

### UnusedAssignment
Variable `encoder` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-digest-commons/src/main/java/org/apache/tika/parser/digestutils/CommonsDigester.java`
#### Snippet
```java
        for (String digest : digests) {
            String[] parts = digest.split(":");
            DigestingParser.Encoder encoder = null;
            if (parts.length > 1) {
                if (parts[1].equals("16")) {
```

### UnusedAssignment
Variable `mt` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/FrictionlessPackageDetector.java`
#### Snippet
```java

        Enumeration<ZipArchiveEntry> entries = zip.getEntries();
        MediaType mt = null;
        Counter counter = new Counter();
        while (entries.hasMoreElements()) {
```

### UnusedAssignment
Variable `node` initializer `null` is redundant
in `tika-server/tika-server-eval/src/main/java/org/apache/tika/server/eval/TikaEvalResource.java`
#### Snippet
```java
    @Path("compare")
    public Map<String, Object> compare(InputStream is) throws Exception {
        JsonNode node = null;
        try (BufferedReader reader = new BufferedReader(
                new InputStreamReader(is, StandardCharsets.UTF_8))) {
```

### UnusedAssignment
Variable `node` initializer `null` is redundant
in `tika-server/tika-server-eval/src/main/java/org/apache/tika/server/eval/TikaEvalResource.java`
#### Snippet
```java
    @Path("profile")
    public Map<String, Object> profile(InputStream is) throws Exception {
        JsonNode node = null;
        try (BufferedReader reader = new BufferedReader(
                new InputStreamReader(is, StandardCharsets.UTF_8))) {
```

### UnusedAssignment
The value `null` assigned to `message` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mbox/MboxParser.java`
#### Snippet
```java

                    InputStream messageStream = message.toInputStream();
                    message = null;

                    if (extractor.shouldParseEmbedded(mailMetadata)) {
```

### UnusedAssignment
Variable `text` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java
        int textStart = -1;
        String description = null;
        String text = null;

        // Find where the description ends
```

### UnusedAssignment
Variable `length` initializer `-1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/StreamingZipContainerDetector.java`
#### Snippet
```java
        byte[] prefix = new byte[1024]; // enough for all known archive formats
        input.mark(1024);
        int length = -1;
        try {
            length = IOUtils.read(input, prefix, 0, 1024);
```

### UnusedAssignment
Variable `length` initializer `-1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
        byte[] prefix = new byte[1024]; // enough for all known archive formats
        input.mark(1024);
        int length = -1;
        try {
            length = IOUtils.read(input, prefix, 0, 1024);
```

### UnusedAssignment
Variable `strictParsing` initializer `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
    private final EmbeddedDocumentExtractor extractor;
    private final Detector detector;
    private boolean strictParsing = false;
    //this is used to buffer a multipart body that
    //keeps track of multipart/alternative and its children
```

### UnusedAssignment
Variable `date` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
            } else if (fieldname.equalsIgnoreCase("Date")) {
                String dateBody = parsedField.getBody();
                Date date = null;
                try {
                    date = MailDateParser.parseDateLenient(dateBody);
```

### UnusedAssignment
Variable `v1` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/Mp3Parser.java`
#### Snippet
```java
        ID3v23Handler v23 = null;
        ID3v22Handler v22 = null;
        ID3v1Handler v1 = null;
        LyricsHandler lyrics = null;
        AudioFrame firstAudio = null;
```

### UnusedAssignment
Variable `lyrics` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/Mp3Parser.java`
#### Snippet
```java
        ID3v22Handler v22 = null;
        ID3v1Handler v1 = null;
        LyricsHandler lyrics = null;
        AudioFrame firstAudio = null;

```

### UnusedAssignment
Variable `currRow` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFReader.java`
#### Snippet
```java
    private final DBFFileHeader header;
    private final InputStream is;
    private DBFRow currRow = null;
    private Charset charset = StandardCharsets.US_ASCII;
    private DBFReader(InputStream is) throws IOException, TikaException {
```

### UnusedAssignment
Variable `p` initializer `null` is redundant
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-jdbc/src/main/java/org/apache/tika/pipes/reporters/jdbc/JDBCPipesReporter.java`
#### Snippet
```java
            while (true) {
                //blocking
                KeyStatusPair p = null;
                try {
                    p = queue.take();
```

### UnusedAssignment
Variable `style` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java

    private void startList(String name) throws SAXException {
        ListStyle style = null;
        if (name == null || ! listStyleMap.containsKey(name)) {
            style = NULL_LIST_STYLE;
```

### UnusedAssignment
Variable `cipher` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/hwp/HwpTextExtractorV5.java`
#### Snippet
```java
    public InputStream createDecryptStream(InputStream input, Key key)
            throws NoSuchAlgorithmException, NoSuchPaddingException, InvalidKeyException {
        Cipher cipher = null;

        cipher = Cipher.getInstance("AES/ECB/NoPadding");
```

### UnusedAssignment
Variable `stream` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
        }

        TikaInputStream stream = null;
        try {
            stream = TikaInputStream.get(zipFile.getInputStream(ze));
```

### UnusedAssignment
Variable `success` initializer `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
                    resources.createTempFile(FilenameUtils.getSuffixFromPath(brokenZip.getFileName().toString()));
            ZipSalvager.salvageCopy(brokenZip.toFile(), salvaged.toFile());
            boolean success = false;
            try (ZipFile zipFile = new ZipFile(salvaged.toFile())) {
                success =
```

### UnusedAssignment
Variable `zipFile` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
            tis = TikaInputStream.get(new CloseShieldInputStream(stream), temporaryResources, metadata);
        }
        ZipFile zipFile = null;
        try {
            zipFile = new ZipFile(tis.getPath().toFile());
```

### UnusedAssignment
Variable `includeDeletedContent` initializer `true` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WP6DocumentAreaExtractor.java`
#### Snippet
```java
                            254, 8,  // (Reserved)
                    });
    private boolean includeDeletedContent = true;
    private boolean inUndo = false;

```

### UnusedAssignment
Variable `blobs` initializer `null` is redundant
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-gcs/src/main/java/org/apache/tika/pipes/pipesiterator/gcs/GCSPipesIterator.java`
#### Snippet
```java
        HandlerConfig handlerConfig = getHandlerConfig();

        Page<Blob> blobs = null;
        if (StringUtils.isBlank(prefix)) {
            blobs = storage.list(bucket);
```

### UnusedAssignment
Variable `readLimit` initializer `-1` is redundant
in `tika-app/src/main/java/org/apache/tika/batch/builders/AppParserFactoryBuilder.java`
#### Snippet
```java
                    "the DigestingAutoDetectParserFactory");
        }
        int readLimit = -1;

        try {
```

### UnusedAssignment
Variable `ais` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java

        TemporaryResources tmp = new TemporaryResources();
        ArchiveInputStream ais = null;
        String encoding = null;
        try {
```

### UnusedAssignment
Variable `outputFile` initializer `null` is redundant
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java

            String name = metadata.get(TikaCoreProperties.RESOURCE_NAME_KEY);
            File outputFile = null;
            if (name == null) {
                name = "file" + count++;
```

### UnusedAssignment
Variable `connection` initializer `null` is redundant
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/JoshuaNetworkTranslator.java`
#### Snippet
```java
                        mue);
            }
            HttpURLConnection connection = null;
            try {
                if (url != null) {
```

### UnusedAssignment
Variable `isAvailable` initializer `false` is redundant
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/RTGTranslator.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(RTGTranslator.class);
    private WebClient client;
    private boolean isAvailable = false;

    public RTGTranslator() {
```

### UnusedAssignment
Variable `fetcherManager` initializer `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java

        //TODO -- clean this up -- only load as necessary
        FetcherManager fetcherManager = null;
        InputStreamFactory inputStreamFactory = null;
        if (tikaServerConfig.isEnableUnsecureFeatures()) {
```

### UnusedAssignment
Variable `inputStreamFactory` initializer `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
        //TODO -- clean this up -- only load as necessary
        FetcherManager fetcherManager = null;
        InputStreamFactory inputStreamFactory = null;
        if (tikaServerConfig.isEnableUnsecureFeatures()) {
            fetcherManager = FetcherManager.load(tikaServerConfig.getConfigPath());
```

### UnusedAssignment
Variable `logFilter` initializer `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
        writers.add(new JSONObjWriter());

        TikaLoggingFilter logFilter = null;
        if (!StringUtils.isBlank(tikaServerConfig.getLogLevel())) {
            String logLevel = tikaServerConfig.getLogLevel();
```

### UnusedAssignment
Variable `corsFilter` initializer `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
        }

        CrossOriginResourceSharingFilter corsFilter = null;
        if (!StringUtils.isBlank(tikaServerConfig.getCors())) {
            corsFilter = new CrossOriginResourceSharingFilter();
```

### UnusedAssignment
Variable `t` initializer `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/PipesResource.java`
#### Snippet
```java
    public Map<String, String> postRmeta(InputStream is, @Context HttpHeaders httpHeaders,
                                         @Context UriInfo info) throws Exception {
        FetchEmitTuple t = null;
        try (Reader reader = new InputStreamReader(is, StandardCharsets.UTF_8)) {
            t = JsonFetchEmitTuple.fromJson(reader);
```

### UnusedAssignment
Variable `properties` initializer `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
        //this is an abomination... clean up this double read
        try (InputStream is = Files.newInputStream(tikaServerConfig.getConfigPath())) {
            Node properties = null;
            try {
                properties = XMLReaderUtils.buildDOM(is).getDocumentElement();
```

### UnusedAssignment
Variable `config` initializer `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
            throws IOException, TikaException {

        TikaServerConfig config = null;
        Set<String> settings = new HashSet<>();
        if (commandLine.hasOption("c")) {
```

### UnusedAssignment
Variable `pdf2XHTML` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDF2XHTML.java`
#### Snippet
```java
                               Metadata metadata, PDFParserConfig config)
            throws SAXException, TikaException {
        PDF2XHTML pdf2XHTML = null;
        try {
            // Extract text using a dummy Writer as we override the
```

### UnusedAssignment
Variable `ocr2XHTML` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/OCR2XHTML.java`
#### Snippet
```java
                               PDFParserConfig config)
            throws SAXException, TikaException {
        OCR2XHTML ocr2XHTML = null;

        try {
```

### UnusedAssignment
Variable `unmappedString` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
            //TODO -- figure out if this is actual BEST or whatever
            //and return that instead of the literal values
            String unmappedString = null;
            if (unmappedUnicodeCharsPerPage < 1.0) {
                unmappedString = String.format(Locale.US, "%.03f",
```

### UnusedAssignment
Variable `basic` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        }

        XMPSchemaBasic basic = null;
        try {
            basic = xmp.getBasicSchema();
```

### UnusedAssignment
Variable `pdf` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        }

        XMPSchemaPDF pdf = null;
        try {
            pdf = xmp.getPDFSchema();
```

### UnusedAssignment
Variable `bufferedImage` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/image/ImageGraphicsEngine.java`
#### Snippet
```java
                        .extract(((PDImageXObject) pdImage).getMetadata(), metadata, parseContext);
            }
            BufferedImage bufferedImage = null;
            try {
                bufferedImage = writeToBuffer(pdImage, suffix, useDirectJPEG, buffer);
```

### UnusedAssignment
Variable `renderResults` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
            return;
        }
        RenderResults renderResults = null;
        try {
            renderResults = renderPDF(tstream, context, config);
```

### UnusedAssignment
Variable `page` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
    private void checkIllustrator(final PDDocument pdfDocument, Metadata metadata) {

        PDPage page = null;
        try {
            page = pdfDocument.getPage(0);
```

### UnusedAssignment
Variable `password` initializer `""` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
        PDDocument pdfDocument = null;

        String password = "";
        PDFRenderingState incomingRenderingState = context.get(PDFRenderingState.class);
        TikaInputStream tstream = null;
```

### UnusedAssignment
Variable `p` initializer `0.0d` is redundant
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenCounter.java`
#### Snippet
```java

        double ent = 0.0d;
        double p = 0.0d;
        double base = 2.0;

```

### UnusedAssignment
Variable `p` initializer `0.0d` is redundant
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/TokenEntropy.java`
#### Snippet
```java
    public Double calculate(TokenCounts tokenCounts) {
        double ent = 0.0d;
        double p = 0.0d;
        double base = 2.0;
        double totalTokens = (double) tokenCounts.getTotalTokens();
```

### UnusedAssignment
Variable `pdfMarkedContent2XHTML` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
            throws SAXException, TikaException {

        PDFMarkedContent2XHTML pdfMarkedContent2XHTML = null;
        try {
            pdfMarkedContent2XHTML =
```

### UnusedAssignment
Variable `stream` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
            return;
        }
        TikaInputStream stream = null;
        try {
            stream = TikaInputStream.get(file.createInputStream());
```

### UnusedAssignment
Variable `tmpFile` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java

        int dpi = config.getOcrDPI();
        Path tmpFile = null;

        RenderingTracker renderingTracker = parseContext.get(RenderingTracker.class);
```

### UnusedAssignment
Variable `fs` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dgn/DGN8Parser.java`
#### Snippet
```java
                    root = (DirectoryNode) container;
                } else {
                    POIFSFileSystem fs = null;
                    if (tstream.hasFile()) {
                        fs = new POIFSFileSystem(tstream.getFile(), true);
```

### UnusedAssignment
Variable `nextToken` initializer `jParser.nextToken()` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
            // with
            DWGReadFormatRemover dwgReadFormatRemover = new DWGReadFormatRemover();
            JsonToken nextToken = jParser.nextToken();
            while ((nextToken = jParser.nextToken()) != JsonToken.END_OBJECT) {
                if (nextToken == JsonToken.FIELD_NAME) {
```

### UnusedAssignment
Variable `blobs` initializer `null` is redundant
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-az-blob/src/main/java/org/apache/tika/pipes/pipesiterator/azblob/AZBlobPipesIterator.java`
#### Snippet
```java
        HandlerConfig handlerConfig = getHandlerConfig();

        PagedIterable<BlobItem> blobs = null;
        if (StringUtils.isBlank(prefix)) {
            ListBlobsOptions options = new ListBlobsOptions()
```

### UnusedAssignment
Variable `connection` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
        String connectionString = getConnectionString(stream, metadata, context);

        Connection connection = null;
        try {
            Class.forName(getJDBCClassName());
```

### UnusedAssignment
Variable `xHandler` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
                      ParseContext context) throws IOException, SAXException, TikaException {
        connection = getConnection(stream, metadata, context);
        XHTMLContentHandler xHandler = null;
        List<String> tableNames = null;
        EmbeddedDocumentUtil embeddedDocumentUtil = new EmbeddedDocumentUtil(context);
```

### UnusedAssignment
Variable `tableNames` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
        connection = getConnection(stream, metadata, context);
        XHTMLContentHandler xHandler = null;
        List<String> tableNames = null;
        EmbeddedDocumentUtil embeddedDocumentUtil = new EmbeddedDocumentUtil(context);
        try {
```

### UnusedAssignment
Variable `e` initializer `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     */
    protected void normalize() {
        NGramEntry e = null;
        Iterator<NGramEntry> i = ngrams.values().iterator();

```

### UnusedAssignment
Variable `ngrams` initializer `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * An index of the ngrams of the profile
     */
    private Map<CharSequence, NGramEntry> ngrams = null;

    /**
```

### UnusedAssignment
Variable `seq` initializer `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
         * The sequence of characters of the ngram
         */
        CharSequence seq = null;
        /**
         * The NGRamProfile this NGram is related to
```

### UnusedAssignment
Variable `ngram` initializer `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
        public boolean equals(Object obj) {

            NGramEntry ngram = null;
            try {
                ngram = (NGramEntry) obj;
```

### UnusedAssignment
Variable `name` initializer `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The profile's name
     */
    private String name = null;
    /**
     * The NGrams of this profile sorted on the number of occurrences
```

### UnusedAssignment
Variable `maxLength` initializer `DEFAULT_MAX_NGRAM_LENGTH` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The max length of ngram
     */
    private int maxLength = DEFAULT_MAX_NGRAM_LENGTH;
    /**
     * The total number of ngrams occurences
```

### UnusedAssignment
Variable `minLength` initializer `DEFAULT_MIN_NGRAM_LENGTH` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The min length of ngram
     */
    private int minLength = DEFAULT_MIN_NGRAM_LENGTH;
    /**
     * The max length of ngram
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
        ngramcounts = new int[maxLength + 1];
        BufferedReader reader = new BufferedReader(new InputStreamReader(is, UTF_8));
        String line = null;

        while ((line = reader.readLine()) != null) {
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/XMLErrorLogUpdater.java`
#### Snippet
```java
                return;
            }
            XMLStreamReader reader = null;
            try {
                reader = XMLInputFactory.newInstance().createXMLStreamReader(new StringReader(xml));
```

### UnusedAssignment
Variable `fps` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractProfiler.java`
#### Snippet
```java
    public boolean processFileResource(FileResource fileResource) {
        Metadata metadata = fileResource.getMetadata();
        EvalFilePaths fps = null;

        if (inputDir != null && inputDir.equals(extracts)) {
```

### UnusedAssignment
Variable `length` initializer `-1L` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/ExtractReader.java`
#### Snippet
```java
        }

        long length = -1L;
        try {
            length = Files.size(extractFile);
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/ExtractReader.java`
#### Snippet
```java
        }

        Reader reader = null;
        InputStream is = null;
        try {
```

### UnusedAssignment
Variable `is` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/ExtractReader.java`
#### Snippet
```java

        Reader reader = null;
        InputStream is = null;
        try {
            is = Files.newInputStream(extractFile);
```

### UnusedAssignment
Variable `denom` initializer `0` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/CommonTokenOverlapCounter.java`
#### Snippet
```java
    private void compare(String langA, String langB, CommonTokenCountManager mgr) {
        int overlap = 0;
        int denom = 0;
        Set<String> setA = mgr.getTokens(langA);
        Set<String> setB = mgr.getTokens(langB);
```

### UnusedAssignment
Variable `jdbcUtil` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/EvalConsumersBuilder.java`
#### Snippet
```java
        }

        JDBCUtil jdbcUtil = null;
        if (db != null) {
            jdbcUtil = new H2Util(db);
```

### UnusedAssignment
Variable `mimeBuffer` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/EvalConsumersBuilder.java`
#### Snippet
```java

        boolean forceDrop = PropsUtil.getBoolean(localAttrs.get("drop"), false);
        MimeBuffer mimeBuffer = null;
        try {
            mimeBuffer = consumerBuilder.init(queue, localAttrs, jdbcUtil, forceDrop);
```

### UnusedAssignment
Variable `map` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/SlowCompositeReaderWrapper.java`
#### Snippet
```java
    public SortedDocValues getSortedDocValues(String field) throws IOException {
        ensureOpen();
        OrdinalMap map = null;
        synchronized (cachedOrdMaps) {
            map = cachedOrdMaps.get(field);
```

### UnusedAssignment
Variable `map` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/SlowCompositeReaderWrapper.java`
#### Snippet
```java
    public SortedSetDocValues getSortedSetDocValues(String field) throws IOException {
        ensureOpen();
        OrdinalMap map = null;
        synchronized (cachedOrdMaps) {
            map = cachedOrdMaps.get(field);
```

### UnusedAssignment
Variable `totalDocs` initializer `-1` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
        Path luceneDir = Files.createTempDirectory("tika-eval-lucene-");
        AbstractTokenTFDFPriorityQueue queue = new TokenDFPriorityQueue(TOP_N);
        long totalDocs = -1;
        long sumDocFreqs = -1;
        long sumTotalTermFreqs = -1;
```

### UnusedAssignment
Variable `sumDocFreqs` initializer `-1` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
        AbstractTokenTFDFPriorityQueue queue = new TokenDFPriorityQueue(TOP_N);
        long totalDocs = -1;
        long sumDocFreqs = -1;
        long sumTotalTermFreqs = -1;
        long uniqueTerms = -1;
```

### UnusedAssignment
Variable `sumTotalTermFreqs` initializer `-1` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
        long totalDocs = -1;
        long sumDocFreqs = -1;
        long sumTotalTermFreqs = -1;
        long uniqueTerms = -1;
        try (Directory directory = FSDirectory.open(luceneDir)) {
```

### UnusedAssignment
Variable `tokenStatsA` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java

        String sharedDigestKey = findSharedDigestKey(metadataListA, metadataListB);
        Map<Class, Object> tokenStatsA = null;
        Map<Class, Object> tokenStatsB = null;
        //now get that metadata
```

### UnusedAssignment
Variable `tokenStatsB` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
        String sharedDigestKey = findSharedDigestKey(metadataListA, metadataListB);
        Map<Class, Object> tokenStatsA = null;
        Map<Class, Object> tokenStatsB = null;
        //now get that metadata
        if (metadataListA != null) {
```

### UnusedAssignment
Variable `fpsA` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
    public boolean processFileResource(FileResource fileResource) {
        Metadata metadata = fileResource.getMetadata();
        EvalFilePaths fpsA = null;
        EvalFilePaths fpsB = null;

```

### UnusedAssignment
Variable `fpsB` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
        Metadata metadata = fileResource.getMetadata();
        EvalFilePaths fpsA = null;
        EvalFilePaths fpsB = null;

        if (inputDir != null && (inputDir.equals(extractsA) || inputDir.equals(extractsB))) {
```

### UnusedAssignment
Variable `commandLine` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/ResultsReporter.java`
#### Snippet
```java

        DefaultParser defaultCLIParser = new DefaultParser();
        CommandLine commandLine = null;
        try {
            commandLine = defaultCLIParser.parse(OPTIONS, args);
```

### UnusedAssignment
Variable `dbUtil` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/ResultsReporter.java`
#### Snippet
```java
            return;
        }
        JDBCUtil dbUtil = null;
        if (commandLine.hasOption("db")) {
            String dbString = commandLine.getOptionValue("db");
```

### UnusedAssignment
Variable `resultsReporter` initializer `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/ResultsReporter.java`
#### Snippet
```java
            Path tmpReportsFile = null;
            try {
                ResultsReporter resultsReporter = null;
                String reportsFile = commandLine.getOptionValue("rf");
                if (reportsFile == null) {
```

### UnusedAssignment
Variable `row` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/sas/SAS7BDATParser.java`
#### Snippet
```java

        // Process each row in turn
        Object[] row = null;
        while ((row = sas.readNext()) != null) {
            xhtml.startElement("tr");
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURISchemeUtil.java`
#### Snippet
```java

    private DataURIScheme build(String mediaTypeString, String isBase64, String dataString) {
        byte[] data = null;
        //strip out back slashes as you might have in css
        dataString = (dataString != null) ? dataString.replaceAll("\\\\", " ") : dataString;
```

### UnusedAssignment
Variable `nerModelUrl` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoParserConfig.java`
#### Snippet
```java
    private static final long serialVersionUID = -3167692634278575818L;
    private static final Logger LOG = LoggerFactory.getLogger(GeoParserConfig.class);
    private URL nerModelUrl = null;
    private String gazetteerRestEndpoint = null;

```

### UnusedAssignment
Variable `dataURIScheme` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java

    private void handleDataURIScheme(String string) throws SAXException {
        DataURIScheme dataURIScheme = null;
        try {
            dataURIScheme = dataURISchemeUtil.parse(string);
```

### UnusedAssignment
Variable `available` initializer `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/regex/RegexNERecogniser.java`
#### Snippet
```java
    public Set<String> entityTypes = new HashSet<>();
    public Map<String, Pattern> patterns;
    private boolean available = false;

    public RegexNERecogniser() {
```

### UnusedAssignment
Variable `extractor` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoParser.java`
#### Snippet
```java
            return;
        }
        NameEntityExtractor extractor = null;

        try {
```

### UnusedAssignment
Variable `entityMentionObject` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/mitie/MITIENERecogniser.java`
#### Snippet
```java
                    Class.forName("edu.mit.ll.mitie.EntityMentionVector");
            Class<?> entityMentionClass = Class.forName("edu.mit.ll.mitie.EntityMention");
            Object entityMentionObject = null;
            Class<?> globalClass = Class.forName("edu.mit.ll.mitie.global");
            Object stringVectorObject = extractorInstance.getClass().getMethod("getPossibleNerTags")
```

### UnusedAssignment
The value `null` assigned to `jcas` is never used
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
        // JCas
        resetCAS(jcas);
        jcas = null;
    }

```

### UnusedAssignment
The value `null` assigned to `ae` is never used
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
        if (ae != null) {
            ae.destroy();
            ae = null;
        }
    }
```

### UnusedAssignment
Variable `metadata` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

    // Metadata object used for cTAKES annotations
    private Metadata metadata = null;

    // UIMA Analysis Engine
```

### UnusedAssignment
Variable `config` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

    // Configuration object for CTAKESContentHandler
    private CTAKESConfig config = null;

    // StringBuilder object used to build the clinical free-text for cTAKES
```

### UnusedAssignment
Variable `sb` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

    // StringBuilder object used to build the clinical free-text for cTAKES
    private StringBuilder sb = null;

    // Metadata object used for cTAKES annotations
```

### UnusedAssignment
Variable `response` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/GrobidRESTParser.java`
#### Snippet
```java

    protected static boolean canRun() {
        Response response = null;
        try {
            response = WebClient.create(readRestUrl() + GROBID_ISALIVE_PATH).get();
```

### UnusedAssignment
Variable `resolvedUrl` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/sentiment/SentimentAnalysisParser.java`
#### Snippet
```java
        }
        try {
            URL resolvedUrl = null;
            if (modelPath.startsWith("http://") || modelPath.startsWith("https://")) {
                resolvedUrl = new URL(modelPath);
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `tika-pipes/tika-emitters/tika-emitter-az-blob/src/main/java/org/apache/tika/pipes/emitter/azblob/AZBlobEmitter.java`
#### Snippet
```java

    private String getActualPath(final String path) {
        String ret = null;
        if (!StringUtils.isBlank(prefix)) {
            ret = prefix + "/" + path;
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
        @Override
        public void run() {
            String line = null;
            try {
                LOG.trace("watcher starting to read");
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
        @Override
        public void run() {
            String line = null;
            try {
                LOG.trace("gobbler starting to read");
```

### UnusedAssignment
Variable `is` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSBatchProcessCLI.java`
#### Snippet
```java
    private TikaInputStream getConfigInputStream(String[] args, boolean logDefault)
            throws IOException {
        TikaInputStream is = null;
        Path batchConfigFile = getConfigFile(args);
        if (batchConfigFile != null) {
```

### UnusedAssignment
Variable `process` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSBatchProcessCLI.java`
#### Snippet
```java
        BatchProcessBuilder b = new BatchProcessBuilder();
        TikaInputStream is = null;
        BatchProcess process = null;
        try {
            is = getConfigInputStream(args, false);
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        System.setErr(System.out);

        ParallelFileProcessingResult result = null;
        try {
            int numConsumers = consumersManager.getConsumers().size();
```

### UnusedAssignment
Variable `causeForTermination` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java

        state.numConsumers = consumersManager.getConsumers().size();
        CAUSE_FOR_TERMINATION causeForTermination = null;
        //main processing loop
        while (true) {
```

### UnusedAssignment
Variable `future` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java

        for (int t = 0; t < end; t++) {
            Future<IFileProcessorFutureResult> future = null;
            try {
                future = completionService.poll(10, TimeUnit.MILLISECONDS);
```

### UnusedAssignment
Variable `metadataList` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/fs/RecursiveParserWrapperFSConsumer.java`
#### Snippet
```java

        Throwable thrown = null;
        List<Metadata> metadataList = null;
        Metadata containerMetadata = fileResource.getMetadata();
        RecursiveParserWrapperHandler handler =
```

### UnusedAssignment
Variable `consumed` initializer `false` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
    private boolean _processFileResource(FileResource fileResource) {
        currentFile = new FileStarted(fileResource.getResourceId());
        boolean consumed = false;
        try {
            consumed = processFileResource(fileResource);
```

### UnusedAssignment
Variable `os` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/fs/AbstractFSConsumer.java`
#### Snippet
```java
    protected OutputStream getOutputStream(OutputStreamFactory fsOSFactory,
                                           FileResource fileResource) {
        OutputStream os = null;
        try {
            os = fsOSFactory.getOutputStream(fileResource.getMetadata());
```

### UnusedAssignment
Variable `doc` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/builders/CommandLineParserBuilder.java`
#### Snippet
```java

    public Options build(InputStream is) throws IOException {
        Document doc = null;
        try {
            DocumentBuilder docBuilder = XMLReaderUtils.getDocumentBuilder();
```

### UnusedAssignment
Variable `sslContext` initializer `null` is redundant
in `tika-pipes/tika-httpclient-commons/src/main/java/org/apache/tika/client/HttpClientFactory.java`
#### Snippet
```java
                "If you need that, please open a ticket.");
        TrustStrategy acceptingTrustStrategy = (cert, authType) -> true;
        SSLContext sslContext = null;
        try {
            sslContext =
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSListCrawler.java`
#### Snippet
```java

    private String nextLine() {
        String line = null;
        try {
            line = reader.readLine();
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/builders/DefaultContentHandlerFactoryBuilder.java`
#### Snippet
```java
    public ContentHandlerFactory build(Node node, Map<String, String> runtimeAttributes) {
        Map<String, String> attributes = XMLDOMUtil.mapifyAttrs(node, runtimeAttributes);
        BasicContentHandlerFactory.HANDLER_TYPE type = null;
        String handlerTypeString = attributes.get("basicHandlerType");
        type = BasicContentHandlerFactory
```

### UnusedAssignment
Variable `crawler` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
        int numConsumers = BatchProcessBuilder.getNumConsumers(runtimeAttributes);
        Path inputDir = PropsUtil.getPath(attributes.get(INPUT_DIR_ATTR), Paths.get("input"));
        FileResourceCrawler crawler = null;
        if (attributes.containsKey("fileList")) {
            String randomCrawlString = attributes.get(CRAWL_ORDER);
```

### UnusedAssignment
Variable `ext` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowRESTVideoRecogniser.java`
#### Snippet
```java
    protected URI getApiUri(Metadata metadata) {
        TikaConfig config = TikaConfig.getDefaultConfig();
        String ext = null;
        //Find extension for video. It's required for OpenCV in InceptionAPI to decode video
        try {
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/pot/PooledTimeSeriesParser.java`
#### Snippet
```java
                           String frames, String vecSize) throws SAXException, IOException {
        try (BufferedReader reader = new BufferedReader(new InputStreamReader(stream, UTF_8))) {
            String line = null;
            AttributesImpl attributes = new AttributesImpl();
            attributes.addAttribute("", "", "rows", "CDATA", frames);
```

### UnusedAssignment
Variable `s3Object` initializer `null` is redundant
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
                objectRequest.withRange(startRange, endRange);
            }
            S3Object s3Object = null;
            synchronized (clientLock) {
                s3Object = s3Client.getObject(objectRequest);
```

### UnusedAssignment
Variable `converter` initializer `null` is redundant
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/TikaToXMP.java`
#### Snippet
```java
        }

        ITikaToXMPConverter converter = null;

        if (isConverterAvailable(mimetype)) {
```

### UnusedAssignment
Variable `xmp` initializer `null` is redundant
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/TikaToXMP.java`
#### Snippet
```java
        }

        XMPMeta xmp = null;

        if (converter != null) {
```

### UnusedAssignment
Variable `text` initializer `null` is redundant
in `tika-example/src/main/java/org/apache/tika/example/TranscribeTranslateExample.java`
#### Snippet
```java
     */
    public static void main (String[] args) throws Exception {
        String text = null;
        if (args.length > 1) {
            if ("transcribe-translate".equals(args[1])) {
```

### UnusedAssignment
Variable `config` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/BasicTikaFSConsumersBuilder.java`
#### Snippet
```java
        }

        TikaConfig config = null;
        String tikaConfigPath = runtimeAttributes.get("c");

```

### UnusedAssignment
Variable `c` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/BasicTikaFSConsumersBuilder.java`
#### Snippet
```java

            for (int i = 0; i < numConsumers; i++) {
                FileResourceConsumer c = null;
                if (streamOut) {
                    c = new StreamOutRPWFSConsumer(queue, parser, contentHandlerFactory,
```

### UnusedAssignment
Variable `writer` initializer `null` is redundant
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
        }

        Writer writer = null;
        if (filename != null) {
            writer = new OutputStreamWriter(new FileOutputStream(filename), encoding);
```

### UnusedAssignment
Variable `n` initializer `0` is redundant
in `tika-langdetect/tika-langdetect-test-commons/src/main/java/org/apache/tika/langdetect/LanguageDetectorTest.java`
#### Snippet
```java
        char[] buffer = new char[4096];
        int count = 0;
        int n = 0;

        while ((-1 != (n = input.read(buffer))) && (count < limit)) {
```

### UnusedAssignment
Variable `searcher` initializer `null` is redundant
in `tika-example/src/main/java/org/apache/tika/example/RecentFiles.java`
#### Snippet
```java
        StringBuffer output = new StringBuffer();
        output.append(getRSSHeaders());
        IndexSearcher searcher = null;
        try {
            reader = DirectoryReader.open(FSDirectory.open(indexFile));
```

### UnusedAssignment
Variable `connection` initializer `null` is redundant
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3DBParser.java`
#### Snippet
```java
        String connectionString = getConnectionString(stream, metadata, context);

        Connection connection = null;
        try {
            Class.forName(getJDBCClassName());
```

### UnusedAssignment
Variable `trailBytes` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_UTF8.java`
#### Snippet
```java
        byte[] input = det.fRawInput;
        int i;
        int trailBytes = 0;
        int confidence;

```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
     */
    public String getString(int maxLength) throws java.io.IOException {
        String result = null;
        if (fInputStream != null) {
            StringBuilder sb = new StringBuilder();
```

### UnusedAssignment
Variable `bytesRead` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
            Reader reader = getReader();
            int max = maxLength < 0 ? Integer.MAX_VALUE : maxLength;
            int bytesRead = 0;

            while ((bytesRead = reader.read(buffer, 0, Math.min(max, 1024))) > 0) {
```

### UnusedAssignment
Variable `fInputStream` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
    //  If user gave us a byte array, this is it.
    private int fRawLength;           // Length of data in fRawInput array.
    private InputStream fInputStream = null;  // User's input stream, or null if the user
    private String fCharsetName;         // The name of the charset this CharsetMatch
    //   represents.  Filled in by the recognizer.
```

### UnusedAssignment
Variable `future` initializer `null` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/cli/FuzzingCLI.java`
#### Snippet
```java
            int finished = 0;
            while (finished < totalThreads) {
                Future<Integer> future = null;
                try {
                    future = executorCompletionService.poll(1, TimeUnit.SECONDS);
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/TextAndCSVParser.java`
#### Snippet
```java
            }
        }
        Reader reader = null;
        if (params.getCharset() == null) {
            reader = new AutoDetectReader(new CloseShieldInputStream(stream), metadata,
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/TextAndCSVParser.java`
#### Snippet
```java

        CSVParams params = getOverride(metadata);
        Reader reader = null;
        Charset charset = null;
        if (!params.isComplete()) {
```

### UnusedAssignment
Variable `charset` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/TextAndCSVParser.java`
#### Snippet
```java
        CSVParams params = getOverride(metadata);
        Reader reader = null;
        Charset charset = null;
        if (!params.isComplete()) {
            reader = detect(params, stream, metadata, context);
```

### UnusedAssignment
Variable `srci` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java
     */
    private void MungeInput() {
        int srci = 0;
        int dsti = 0;
        byte b;
```

### UnusedAssignment
Variable `bytesRead` initializer `-1` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java
        //   previous one may have come from the caller,
        //   in which case we can't touch it.
        long bytesRead = -1;
        try {
            bytesRead = IOUtils.read(fInputStream, inputBytes);
```

### UnusedAssignment
Variable `i` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
        xhtml.startDocument();

        int i = 0;
        do {
            inSize = 0;
```

### UnusedAssignment
Variable `firstByte` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
        boolean nextChar(iteratedChar it, CharsetDetector det) {
            it.error = false;
            int firstByte = 0;
            int secondByte = 0;
            int thirdByte = 0;
```

### UnusedAssignment
Variable `secondByte` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            it.error = false;
            int firstByte = 0;
            int secondByte = 0;
            int thirdByte = 0;
            //int fourthByte = 0;
```

### UnusedAssignment
Variable `thirdByte` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            int firstByte = 0;
            int secondByte = 0;
            int thirdByte = 0;
            //int fourthByte = 0;

```

### UnusedAssignment
Variable `firstByte` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
        boolean nextChar(iteratedChar it, CharsetDetector det) {
            it.error = false;
            int firstByte = 0;
            int secondByte = 0;
            int thirdByte = 0;
```

### UnusedAssignment
Variable `secondByte` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            it.error = false;
            int firstByte = 0;
            int secondByte = 0;
            int thirdByte = 0;
            int fourthByte = 0;
```

### UnusedAssignment
Variable `thirdByte` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            int firstByte = 0;
            int secondByte = 0;
            int thirdByte = 0;
            int fourthByte = 0;

```

### UnusedAssignment
Variable `fourthByte` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            int secondByte = 0;
            int thirdByte = 0;
            int fourthByte = 0;

            buildChar:
```

### UnusedAssignment
Variable `length` initializer `-1` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
            } else {
                List<COSName> usedFilters = new ArrayList<>();
                long length = -1;
                try (TikaInputStream rawBytes = TikaInputStream.get(bytes)) {
                    try (TikaInputStream filtered = runFilters(filters, rawBytes, usedFilters)) {
```

### UnusedAssignment
Variable `totalBytes` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java
            metadata.set("strings:file_output", doFile(tis));

            int totalBytes = 0;

            // Content
```

### UnusedAssignment
The value `EndianUtils.readUE7(stream)` assigned to `alphaDataLength` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/BPGParser.java`
#### Snippet
```java
        long alphaDataLength = 0;
        if (hasAlphaPlane1 || hasAlphaPlane2) {
            alphaDataLength = EndianUtils.readUE7(stream);
        }

```

### UnusedAssignment
The value changed at `i++` is never used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/detect/apple/BPListDetector.java`
#### Snippet
```java
        int i = 0;
        if (bytes[i++] != 'b' || bytes[i++] != 'p' || bytes[i++] != 'l' || bytes[i++] != 'i' ||
                bytes[i++] != 's' || bytes[i++] != 't') {
            return MediaType.OCTET_STREAM;
        }
```

### UnusedAssignment
Variable `rootObj` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/detect/apple/BPListDetector.java`
#### Snippet
```java
        }
        //TODO: extract the version with the next two bytes if they were read
        NSObject rootObj = null;
        try {
            if (input instanceof TikaInputStream && ((TikaInputStream) input).hasFile()) {
```

### UnusedAssignment
Variable `buffer` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/apple/AppleSingleFileParser.java`
#### Snippet
```java
                                     Metadata embeddedMetadata, long bytesRead)
            throws IOException, TikaException {
        byte[] buffer = null;
        for (FieldInfo f : fieldInfoList) {
            long diff = f.offset - bytesRead;
```

### UnusedAssignment
Variable `original` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageMetadataExtractor.java`
#### Snippet
```java
            //if it exists?
            // Date/Time Original overrides value from ExifDirectory.TAG_DATETIME
            Date original = null;
            if (directory.containsTag(ExifSubIFDDirectory.TAG_DATETIME_ORIGINAL)) {
                original = directory.getDate(ExifSubIFDDirectory.TAG_DATETIME_ORIGINAL);
```

### UnusedAssignment
Variable `webPMetadata` initializer `new com.drew.metadata.Metadata()` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageMetadataExtractor.java`
#### Snippet
```java

        try {
            com.drew.metadata.Metadata webPMetadata = new com.drew.metadata.Metadata();
            webPMetadata = WebpMetadataReader.readMetadata(file);
            handle(webPMetadata);
```

### UnusedAssignment
Variable `ngram` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        protected byte[] byteMap;
        protected byte spaceChar;
        private int ngram = 0;
        private int[] ngramList;
        private int ngramCount;
```

### UnusedAssignment
Variable `text` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java

        private void output(String what) throws SAXException {
            String text = null;
            if (pageCount == 1 && defaultFirst != null) {
                text = defaultFirst;
```

### UnusedAssignment
Variable `doc` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/builders/BatchProcessBuilder.java`
#### Snippet
```java
    public BatchProcess build(InputStream is, Map<String, String> runtimeAttributes)
            throws IOException {
        Document doc = null;
        try {
            DocumentBuilder docBuilder = XMLReaderUtils.getDocumentBuilder();
```

### UnusedAssignment
Variable `crawler` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/builders/BatchProcessBuilder.java`
#### Snippet
```java
                XMLDOMUtil.getInt("maxAliveTimeSeconds", incomingRuntimeAttributes, docElement);

        FileResourceCrawler crawler = null;
        ConsumersManager consumersManager = null;
        StatusReporter reporter = null;
```

### UnusedAssignment
Variable `consumersManager` initializer `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/builders/BatchProcessBuilder.java`
#### Snippet
```java

        FileResourceCrawler crawler = null;
        ConsumersManager consumersManager = null;
        StatusReporter reporter = null;
        Interrupter interrupter = null;
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork18PackageParser.java`
#### Snippet
```java
         */
        public static MediaType detect(ZipFile zipFile) {
            MediaType type = null;
            Enumeration<? extends ZipEntry> entries = zipFile.getEntries();
            while (entries.hasMoreElements()) {
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork13PackageParser.java`
#### Snippet
```java

        public static MediaType detect(ZipFile zipFile) {
            MediaType type = null;
            Enumeration<? extends ZipEntry> entries = zipFile.getEntries();
            while (entries.hasMoreElements()) {
```

### UnusedAssignment
Variable `exitValue` initializer `Integer.MIN_VALUE` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
        errThread.start();

        int exitValue = Integer.MIN_VALUE;
        try {
            boolean finished = process.waitFor(timeoutSeconds, TimeUnit.SECONDS);
```

### UnusedAssignment
Variable `exitValue` initializer `Integer.MIN_VALUE` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
        errThread.start();

        int exitValue = Integer.MIN_VALUE;
        try {
            boolean finished = process.waitFor(timeoutMillis, TimeUnit.MILLISECONDS);
```

### UnusedAssignment
Variable `added` initializer `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

    private boolean parseHeader(byte[] value, HashMap<String, String> properties) {
        boolean added = false;

        String env_serviceid = "";
```

### UnusedAssignment
Variable `added` initializer `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

    private boolean parseBody(byte[] value, HashMap<String, String> properties) {
        boolean added = false;

        StringBuilder bdy_heading = new StringBuilder();
```

### UnusedAssignment
Variable `added` initializer `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

    private boolean parseFooter(byte[] value, HashMap<String, String> properties) {
        boolean added = false;

        StringBuilder ftr_source = new StringBuilder();
```

### UnusedAssignment
Variable `read` initializer `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
            boolean started = false;                   // check if we have found the start flag
            boolean finished = false;                  // check if we have found the finish flag
            int read = 0;                              // the number of bytes we read
            int start = 0;                             // the position after the start flag

```

## RuleId[id=AssignmentUsedAsCondition]
### AssignmentUsedAsCondition
Assignment `finished = (b == bfinish)` used as condition
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                }

                if (finished = (b == bfinish)) {
/*
               is.reset();
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesResult.java`
#### Snippet
```java
        NO_EMITTER_FOUND,
        EMIT_SUCCESS, EMIT_SUCCESS_PARSE_EXCEPTION, EMIT_EXCEPTION,
        INTERRUPTED_EXCEPTION, NO_FETCHER_FOUND;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/video/FLVParser.java`
#### Snippet
```java
                try (
                        UnsynchronizedByteArrayInputStream is = new UnsynchronizedByteArrayInputStream(metaBytes);
                        DataInputStream dis = new DataInputStream(is);
                ) {
                    Object data = null;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
    public boolean isExtractAcroFormContent() {
        return defaultConfig.isExtractAcroFormContent();
    };

    @Field
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Unnecessary empty string argument
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        out.println("    --dump-static-config   Print static config");
        out.println("    --dump-static-full-config  Print static explicit config");
        out.println("");
        out.println("    -x  or --xml           Output XHTML content (default)");
        out.println("    -h  or --html          Output HTML content");
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/YandexTranslator.java`
#### Snippet
```java
            } else {
                throw new TikaException("Return message not recognized: " +
                        responseText.toString().substring(0, Math.min(responseText.length(), 100)));
            }
        } catch (JsonParseException e) {
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFEncodedStringDecoder.java`
#### Snippet
```java
    String decode(String value) {
        try {
            byte[] bytes = new String("(" + value + ")").getBytes(ISO_8859_1);
            InputStream is = new UnsynchronizedByteArrayInputStream(bytes);
            COSStringParser p = new COSStringParser(new RandomAccessBuffer(is));
```

### StringOperationCanBeSimplified
Can be replaced with 'String.valueOf()'
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The String form of the separator char
     */
    private final static String SEP_CHARSEQ = new String(new char[]{SEPARATOR});
    /**
     * A StringBuffer used during analysis
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
    private static String buildLocalizedOffset(Matcher matcher, String text) {
        StringBuilder sb = new StringBuilder();
        sb.append(text.substring(0, matcher.start()));
        sb.append(matcher.group(1));// +/-
        sb.append(StringUtils.leftPad(matcher.group(2), 2, '0'));//HH
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
                    System.out.println(
                            "  DumpTikaConfig [--dump-minimal] [--dump-current] [--dump-static] [filename] [encoding]");
                    System.out.println("");
                    System.out.println("--dump-minimal    Produce the minimal config file");
                    System.out.println("--dump-current    The current (with defaults) config file");
```

### StringOperationCanBeSimplified
Can be replaced with 'String.valueOf()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll("``", "`");
        value = value.replaceAll("''", "'");
        value = value.replaceAll(new String(new char[]{SL}), "'");
        value = value.replaceAll(new String(new char[]{SR}), "'");
        value = value.replaceAll(new String(new char[]{DL}), "\"");
```

### StringOperationCanBeSimplified
Can be replaced with 'String.valueOf()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll("''", "'");
        value = value.replaceAll(new String(new char[]{SL}), "'");
        value = value.replaceAll(new String(new char[]{SR}), "'");
        value = value.replaceAll(new String(new char[]{DL}), "\"");
        value = value.replaceAll(new String(new char[]{DR}), "\"");
```

### StringOperationCanBeSimplified
Can be replaced with 'String.valueOf()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll(new String(new char[]{SL}), "'");
        value = value.replaceAll(new String(new char[]{SR}), "'");
        value = value.replaceAll(new String(new char[]{DL}), "\"");
        value = value.replaceAll(new String(new char[]{DR}), "\"");
        value = value.trim();
```

### StringOperationCanBeSimplified
Can be replaced with 'String.valueOf()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll(new String(new char[]{SR}), "'");
        value = value.replaceAll(new String(new char[]{DL}), "\"");
        value = value.replaceAll(new String(new char[]{DR}), "\"");
        value = value.trim();

```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/grib/GribParser.java`
#### Snippet
```java
                for (Dimension dim : ncFile.getDimensions()) {
                    xhtml.element("li",
                            dim.getFullName() + "=" + String.valueOf(dim.getLength()) + ";");
                    xhtml.newline();
                }
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/grib/GribParser.java`
#### Snippet
```java
                for (Variable var : ncFile.getVariables()) {
                    xhtml.element("p",
                            String.valueOf(var.getDataType()) + var.getNameAndDimensions() + ";");
                    for (Attribute element : var.getAttributes()) {
                        xhtml.element("li", " :" + element + ";");
```

### UnnecessaryCallToStringValueOf
Unnecessary `Float.toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
        metadata.set(PDF.PDF_VERSION, Float.toString(document.getDocument().getVersion()));
        metadata.add(TikaCoreProperties.FORMAT.getName(), MEDIA_TYPE.toString() + "; version=" +
                Float.toString(document.getDocument().getVersion()));


```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/mat/MatParser.java`
#### Snippet
```java
                MLArray varData = entry.getValue();

                xhtml.element("p", varName + ":" + String.valueOf(varData));

                // If the variable is a structure, extract variable info from structure
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
            String polarity_pref = "POLARITY";
            value = new StringBuilder(polarity_pref).append("=").
                    append(Integer.toString(annotation.getPolarity())).toString();
        }
        return value;
```

## RuleId[id=ShiftOutOfRange]
### ShiftOutOfRange
Shift operation `>>>` by overly large constant value 32
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenStatistics.java`
#### Snippet
```java
        int result;
        long temp;
        result = (int) (totalTokens ^ (totalTokens >>> 32));
        result = 31 * result + (int) (totalUniqueTokens ^ (totalUniqueTokens >>> 32));
        result = 31 * result + Arrays.hashCode(topN);
```

### ShiftOutOfRange
Shift operation `>>>` by overly large constant value 32
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenStatistics.java`
#### Snippet
```java
        long temp;
        result = (int) (totalTokens ^ (totalTokens >>> 32));
        result = 31 * result + (int) (totalUniqueTokens ^ (totalUniqueTokens >>> 32));
        result = 31 * result + Arrays.hashCode(topN);
        temp = Double.doubleToLongBits(entropy);
```

## RuleId[id=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `reset()` overrides synchronized method
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java

    @Override
    public void reset() throws IOException {
        super.reset();
        position = mark;
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `mark()` overrides synchronized method
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java

    @Override
    public void mark(int readlimit) {
        super.mark(readlimit);
        mark = position;
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `reset()` overrides synchronized method
in `tika-core/src/main/java/org/apache/tika/io/BoundedInputStream.java`
#### Snippet
```java

    @Override
    public void reset() throws IOException {
        in.reset();
        pos = 0;
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `mark()` overrides synchronized method
in `tika-core/src/main/java/org/apache/tika/io/BoundedInputStream.java`
#### Snippet
```java

    @Override
    public void mark(int readLimit) {
        in.mark(readLimit);
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `reset()` overrides synchronized method
in `tika-core/src/main/java/org/apache/tika/io/TailStream.java`
#### Snippet
```java
     */
    @Override
    public void reset() {
        if (markBuffer != null) {
            System.arraycopy(markBuffer, 0, tailBuffer, 0, tailSize);
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `mark()` overrides synchronized method
in `tika-core/src/main/java/org/apache/tika/io/TailStream.java`
#### Snippet
```java
     */
    @Override
    public void mark(int limit) {
        markBuffer = new byte[tailSize];
        System.arraycopy(tailBuffer, 0, markBuffer, 0, tailSize);
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    }

    protected static boolean isHexChar(int ch) {
        return (ch >= '0' && ch <= '9') || (ch >= 'a' && ch <= 'f') || (ch >= 'A' && ch <= 'F');
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    }

    protected static int hexValue(int ch) {
        if (ch >= '0' && ch <= '9') {
            return ch - '0';
```

## RuleId[id=RedundantEscapeInRegexReplacement]
### RedundantEscapeInRegexReplacement
Redundant escape of '.'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
                                                    dwgc.getCleanDwgReadReplaceWith())
                                            .replaceAll("\\bnan\\b", " 0,")
                                            .replaceAll("\\.,", " \\. ,") + "\n";
                            out.write(sCurrentLine);
                        }                            
```

## RuleId[id=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `start` declared in one 'switch' branch and used in another
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
                    break;
                case XMLStreamConstants.CHARACTERS:
                    int start = reader.getTextStart();
                    int length = reader.getTextLength();
                    buffer.append(reader.getTextCharacters(), start, length);
```

### SwitchStatementWithConfusingDeclaration
Local variable `length` declared in one 'switch' branch and used in another
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
                case XMLStreamConstants.CHARACTERS:
                    int start = reader.getTextStart();
                    int length = reader.getTextLength();
                    buffer.append(reader.getTextCharacters(), start, length);
                    break;
```

### SwitchStatementWithConfusingDeclaration
Local variable `start` declared in one 'switch' branch and used in another
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
                    break;
                case XMLStreamConstants.CHARACTERS:
                    int start = reader.getTextStart();
                    int length = reader.getTextLength();
                    buffer.append(reader.getTextCharacters(), start, length);
```

### SwitchStatementWithConfusingDeclaration
Local variable `length` declared in one 'switch' branch and used in another
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
                case XMLStreamConstants.CHARACTERS:
                    int start = reader.getTextStart();
                    int length = reader.getTextLength();
                    buffer.append(reader.getTextCharacters(), start, length);
                    break;
```

### SwitchStatementWithConfusingDeclaration
Local variable `start` declared in one 'switch' branch and used in another
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
                    break;
                case XMLStreamConstants.CHARACTERS:
                    int start = reader.getTextStart();
                    int length = reader.getTextLength();
                    buffer.append(reader.getTextCharacters(), start, length);
```

### SwitchStatementWithConfusingDeclaration
Local variable `length` declared in one 'switch' branch and used in another
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
                case XMLStreamConstants.CHARACTERS:
                    int start = reader.getTextStart();
                    int length = reader.getTextLength();
                    buffer.append(reader.getTextCharacters(), start, length);
                    break;
```

### SwitchStatementWithConfusingDeclaration
Local variable `f` declared in one 'switch' branch and used in another
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                case CREATE:

                    File f = new File(filename);
                    FileInputStream fis = new FileInputStream(f);
                    LanguageProfilerBuilder newProfile =
```

### SwitchStatementWithConfusingDeclaration
Local variable `fis` declared in one 'switch' branch and used in another
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java

                    File f = new File(filename);
                    FileInputStream fis = new FileInputStream(f);
                    LanguageProfilerBuilder newProfile =
                            LanguageProfilerBuilder.create(profilename, fis, encoding);
```

### SwitchStatementWithConfusingDeclaration
Local variable `newProfile` declared in one 'switch' branch and used in another
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                    File f = new File(filename);
                    FileInputStream fis = new FileInputStream(f);
                    LanguageProfilerBuilder newProfile =
                            LanguageProfilerBuilder.create(profilename, fis, encoding);
                    fis.close();
```

## RuleId[id=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `objectMapper` accessed in synchronized context
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java
        asyncStatus.update(localCounts, totalCountResult, status);
        try (Writer writer = Files.newBufferedWriter(statusFile, StandardCharsets.UTF_8)) {
            objectMapper.writeValue(writer, asyncStatus);
        } catch (IOException e) {
            LOG.warn("couldn't write report", e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `objectMapper` accessed in synchronized context
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java
        asyncStatus.updateCrash(crashMessage);
        try (Writer writer = Files.newBufferedWriter(statusFile, StandardCharsets.UTF_8)) {
            objectMapper.writeValue(writer, asyncStatus);
        } catch (IOException e) {
            LOG.warn("couldn't write report", e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isShuttingDown` accessed in synchronized context
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
        if (fetchEmitTuples == null) {
            throw new IllegalStateException("queue hasn't been initialized yet.");
        } else if (isShuttingDown) {
            throw new IllegalStateException(
                    "Can't call offer after calling close() or " + "shutdownNow()");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isShuttingDown` accessed in synchronized context
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
    public synchronized boolean offer(List<FetchEmitTuple> newFetchEmitTuples, long offerMs)
            throws PipesException, InterruptedException {
        if (isShuttingDown) {
            throw new IllegalStateException(
                    "Can't call offer after calling close() or " + "shutdownNow()");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `alreadyTriedToLoad` accessed in synchronized context
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java

    private synchronized void tryToLoad(String langCode) {
        if (alreadyTriedToLoad.contains(langCode)) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `commonTokenMap` accessed in synchronized context
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
        //check once more now that we're in a
        //synchronized block
        if (commonTokenMap.get(langCode) != null) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `alreadyTriedToLoad` accessed in synchronized context
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
                LOG.warn("Couldn't find common tokens file for: '" + langCode + "' tried here: " +
                        path);
                alreadyTriedToLoad.add(langCode);
                return;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `alreadyTriedToLoad` accessed in synchronized context
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
            try (BufferedReader reader = new BufferedReader(
                    new InputStreamReader(is, COMMON_TOKENS_CHARSET))) {
                alreadyTriedToLoad.add(langCode);
                String line = reader.readLine();
                while (line != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `commonTokenMap` accessed in synchronized context
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
                }
            }
            commonTokenMap.put(langCode, model);
        } catch (IOException e) {
            LOG.warn("IOException trying to read: '" + langCode + "'");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.secondaryParser` accessed in synchronized context
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/NamedEntityParser.java`
#### Snippet
```java
        try {
            TikaConfig config = new TikaConfig();
            this.secondaryParser = new Tika(config);
            this.available = !nerChain.isEmpty();
            LOG.info("Number of NERecognisers in chain {}", nerChain.size());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `TIMED_OUT` accessed in synchronized context
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
            if (tmp.getElapsedMillis() > staleThresholdMillis) {
                setEndedState(STATE.TIMED_OUT);
                LOG.error("{}", getXMLifiedLogMsg(TIMED_OUT, tmp.getResourceId(), ELAPSED_MILLIS,
                        Long.toString(tmp.getElapsedMillis())));
                return tmp;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ELAPSED_MILLIS` accessed in synchronized context
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
            if (tmp.getElapsedMillis() > staleThresholdMillis) {
                setEndedState(STATE.TIMED_OUT);
                LOG.error("{}", getXMLifiedLogMsg(TIMED_OUT, tmp.getResourceId(), ELAPSED_MILLIS,
                        Long.toString(tmp.getElapsedMillis())));
                return tmp;
```

## RuleId[id=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `tika-example/src/main/java/org/apache/tika/example/LuceneIndexerExtended.java`
#### Snippet
```java

@SuppressWarnings("deprecation")
public class LuceneIndexerExtended {
    private final Tika tika;

```

### RedundantSuppression
Redundant suppression
in `tika-example/src/main/java/org/apache/tika/example/MetadataAwareLuceneIndexer.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
public class MetadataAwareLuceneIndexer {
    private Tika tika;

```

### RedundantSuppression
Redundant suppression
in `tika-example/src/main/java/org/apache/tika/example/RecentFiles.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
public class RecentFiles {
    private IndexReader reader;

```

### RedundantSuppression
Redundant suppression
in `tika-example/src/main/java/org/apache/tika/example/TrecDocumentGenerator.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
public class TrecDocumentGenerator {
    public TrecDocument summarize(File file)
            throws FileNotFoundException, IOException, TikaException {
```

### RedundantSuppression
Redundant suppression
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/QPWTextExtractor.java`
#### Snippet
```java

    @SuppressWarnings("resource")
    public void extract(InputStream input, XHTMLContentHandler xhtml, Metadata metadata)
            throws IOException, SAXException, TikaException {

```

### RedundantSuppression
Redundant suppression
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java

    @SuppressWarnings("serial")
    public static Parser createParser() {
        final Parser parser = new AutoDetectParser(TIKA_CONFIG);

```

### RedundantSuppression
Redundant suppression
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java

    @SuppressWarnings("serial")
    public static void fillMetadata(Parser parser, Metadata metadata,
                                    MultivaluedMap<String, String> httpHeaders) {
        String fileName = detectFilename(httpHeaders);
```

### RedundantSuppression
Redundant suppression
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/writer/CSVMessageBodyWriter.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("resource")
    public void writeTo(Metadata metadata, Class<?> type, Type genericType,
                        Annotation[] annotations, MediaType mediaType,
                        MultivaluedMap<String, Object> httpHeaders, OutputStream entityStream)
```

### RedundantSuppression
Redundant suppression
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/writer/TextMessageBodyWriter.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("resource")
    public void writeTo(Metadata metadata, Class<?> type, Type genericType,
                        Annotation[] annotations, MediaType mediaType,
                        MultivaluedMap<String, Object> httpHeaders, OutputStream entityStream)
```

## RuleId[id=UtilityClassWithPublicConstructor]
### UtilityClassWithPublicConstructor
Class `TikaToXMP` has only 'static' members, and a 'public' constructor
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/TikaToXMP.java`
#### Snippet
```java
import org.apache.tika.parser.odf.OpenDocumentParser;

public class TikaToXMP {
    /**
     * Map from mimetype to converter class Must only be accessed through
```

### UtilityClassWithPublicConstructor
Class `ImageUtil` has only 'static' members, and a 'public' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/tess4j/ImageUtil.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class ImageUtil {
    private static final Logger LOG = LoggerFactory.getLogger(ImageUtil.class);

```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'optionalValue'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/MetadataExtractor.java`
#### Snippet
```java
    }

    private <T> void setProperty(Metadata metadata, Property property, Optional<T> optionalValue) {
        if (!optionalValue.isPresent()) {
            return;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'value'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/MetadataExtractor.java`
#### Snippet
```java
    }

    private void addMultiProperty(Metadata metadata, Property property, Optional<String> value) {
        if (!value.isPresent()) {
            return;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'optionalValue'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/MetadataExtractor.java`
#### Snippet
```java
    }

    private <T> void addProperty(Metadata metadata, Property property, Optional<T> optionalValue) {
        if (!optionalValue.isPresent()) {
            return;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'postConnectionString'
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-jdbc/src/main/java/org/apache/tika/pipes/reporters/jdbc/JDBCPipesReporter.java`
#### Snippet
```java
    private String connectionString;

    private Optional<String> postConnectionString = Optional.empty();
    private final ArrayBlockingQueue<KeyStatusPair> queue =
            new ArrayBlockingQueue(ARRAY_BLOCKING_QUEUE_SIZE);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'postConnectionString'
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-jdbc/src/main/java/org/apache/tika/pipes/reporters/jdbc/JDBCPipesReporter.java`
#### Snippet
```java
        private static final int MAX_TRIES = 3;
        private final String connectionString;
        private final Optional<String> postConnectionString;
        private final ArrayBlockingQueue<KeyStatusPair> queue;
        List<KeyStatusPair> cache = new ArrayList<>();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'postConnectionString'
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-jdbc/src/main/java/org/apache/tika/pipes/reporters/jdbc/JDBCPipesReporter.java`
#### Snippet
```java

        public ReportWorker(String connectionString,
                            Optional<String> postConnectionString,
                            ArrayBlockingQueue<KeyStatusPair> queue) {
            this.connectionString = connectionString;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'fileName'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TaskStatus.java`
#### Snippet
```java
    final ServerStatus.TASK task;
    final Instant started;
    final Optional<String> fileName;
    final long timeoutMillis;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'postConnectionString'
in `tika-pipes/tika-emitters/tika-emitter-jdbc/src/main/java/org/apache/tika/pipes/emitter/jdbc/JDBCEmitter.java`
#### Snippet
```java
    private String connectionString;

    private Optional<String> postConnectionString = Optional.empty();
    private String insert;
    private String createTable;
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmAssert.java`
#### Snippet
```java
        }

        if ((data.length <= 0)) {
            throw new TikaException("data[] length should be greater than zero");
        }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
        // empty value
        // FIXME: should we log this somewhere?
        if (prop.length() <= 0) {
            return DEFAULT_PRECACHE_SIZE;
        }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/RDCAnalysisChunking.java`
#### Snippet
```java
        int inputLength = fileContent.length;

        if (inputLength <= 0) {
            throw new IOException("Cannot support the length less than 0");
        } else if (inputLength <= horizon) {
```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    private static final String UL = "ul";

    private static final Charset ASCII = Charset.forName("US-ASCII");
    private static final Charset WINDOWS_1252 = getCharset("WINDOWS-1252");
    private static final Charset MAC_ROMAN = getCharset("MacRoman");
```

### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGParser.java`
#### Snippet
```java
            byte[] header = new byte[128];
            IOUtils.readFully(stream, header);
            String version = new String(header, 0, 6, "US-ASCII");

            XHTMLContentHandler xhtml = new XHTMLContentHandler(handler, metadata);
```

### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlEncodingDetector.java`
#### Snippet
```java
    private static final Pattern FLEXIBLE_CHARSET_ATTR_PATTERN =
            Pattern.compile(("(?is)\\bcharset\\s*=\\s*(?:['\\\"]\\s*)?([-_:\\.a-z0-9]+)"));
    private static final Charset ASCII = Charset.forName("US-ASCII");
    /**
     * HTML can include non-iana supported charsets that Java
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/sentiment/SentimentAnalysisParser.java`
#### Snippet
```java
            return;
        }
        String inputString = IOUtils.toString(stream, "UTF-8");
        String sentiment = classifier.predict(inputString);
        metadata.add("Sentiment", sentiment);
```

### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSType.java`
#### Snippet
```java
        byte[] bytes = null;
        try {
            bytes = type.getBytes("US-ASCII");
        } catch (UnsupportedEncodingException cannotHappen) {
            //swallow
```

## RuleId[id=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java
        byte[] array = {4, 78, -67, 90, 1, -33};
        ChmSection chmSection = new ChmSection(array);
        System.out.println("before " + Arrays.toString(array));
        System.out.println("after " + Arrays.toString(chmSection.reverseByteOrder(array)));
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java
        ChmSection chmSection = new ChmSection(array);
        System.out.println("before " + Arrays.toString(array));
        System.out.println("after " + Arrays.toString(chmSection.reverseByteOrder(array)));
    }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
        } catch (Throwable t) {
            t.printStackTrace();
            System.err.flush();
            try {
                output.writeByte(FAILED_TO_START);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
            } catch (IOException e) {
                e.printStackTrace();
                System.err.flush();
            }
            return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
            t.printStackTrace();
        }
        System.err.flush();
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java

        ForkServer server =
                new ForkServer(System.in, System.out, serverPulseMillis, serverParseTimeoutMillis,
                        serverWaitTimeoutMillis);
        System.setIn(new ByteArrayInputStream(new byte[0]));
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
                        serverWaitTimeoutMillis);
        System.setIn(new ByteArrayInputStream(new byte[0]));
        System.setOut(System.err);

        Thread watchdog = new Thread(server, "Tika Watchdog");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java

            PipesServer server =
                    new PipesServer(tikaConfig, System.in, System.out, maxForEmitBatchBytes,
                            serverParseTimeoutMillis, serverWaitTimeoutMillis);
            System.setIn(new UnsynchronizedByteArrayInputStream(new byte[0]));
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
                            serverParseTimeoutMillis, serverWaitTimeoutMillis);
            System.setIn(new UnsynchronizedByteArrayInputStream(new byte[0]));
            System.setOut(System.err);
            Thread watchdog = new Thread(server, "Tika Watchdog");
            watchdog.setDaemon(true);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
            exit(1);
        }
        System.err.flush();
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
        for (Method method : object.getClass().getMethods()) {
            if (object.getClass().getName().contains("PDF")) {
                System.out.println(method.getName());
            }
            if (m.reset(method.getName()).find()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
            if (m.reset(method.getName()).find()) {
                if (object.getClass().getName().contains("PDF")) {
                    System.out.println("2: " + method.getName());
                }
                String paramName = m.group(1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
            IOUtils.copy(process.getInputStream(), outputStream);
        } catch (IOException e) {
            System.out.println("ERROR: " + e.getMessage());
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
                IOUtils.copy(inputStream, outputStream);
            } catch (IOException e) {
                System.out.println("ERROR: " + e.getMessage());
            }
        }).start();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        boolean isComposite = (d instanceof CompositeDetector);
        String name = d.getClass().getName();
        System.out.println(indent(i) + name + (isComposite ? " (Composite Detector):" : ""));
        if (isComposite) {
            List<Detector> subDetectors = ((CompositeDetector) d).getDetectors();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                try (InputStream stream = TikaInputStream
                        .get(new CloseShieldInputStream(System.in))) {
                    type.process(stream, System.out, new Metadata());
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                }
                if (recursiveJSON) {
                    handleRecursiveJson(url, System.out);
                } else {
                    Metadata metadata = new Metadata();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                    Metadata metadata = new Metadata();
                    try (InputStream input = TikaInputStream.get(url, metadata)) {
                        type.process(input, System.out, metadata);
                    } finally {
                        System.out.flush();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                        type.process(input, System.out, metadata);
                    } finally {
                        System.out.flush();
                    }
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java

    private void usage() {
        PrintStream out = System.out;
        out.println("usage: java -jar tika-app.jar [option...] [file...]");
        out.println();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            // if we do, then we can take this conditional out
            if (!modelClass.getSimpleName().contains("Tika")) {
                System.out.println(modelClass.getSimpleName());
                Field[] keyFields = modelClass.getFields();
                Arrays.sort(keyFields, Comparator.comparing(Field::getName));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                Arrays.sort(keyFields, Comparator.comparing(Field::getName));
                for (Field keyField : keyFields) {
                    System.out.println(" " + keyField.getName());
                }
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java

    private void version() {
        System.out.println(new Tika().toString());
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java

        TikaConfigSerializer
                .serialize(localConfig, mode, new OutputStreamWriter(System.out, UTF_8), UTF_8);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        if ((apt && !isComposite) ||
                !apt) {    // Don't display Composite parsers in the apt output.
            System.out.println(indent(i) + ((apt) ? "* " : "") + name +
                    (isComposite ? " (Composite Parser):" : ""));
            if (apt) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                    (isComposite ? " (Composite Parser):" : ""));
            if (apt) {
                System.out.println();
            }
            if (includeMimeTypes && !isComposite) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            if (includeMimeTypes && !isComposite) {
                for (MediaType mt : p.getSupportedTypes(context)) {
                    System.out.println(indent(i + 3) + ((apt) ? "* " : "") + mt);
                    if (apt) {
                        System.out.println();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                    System.out.println(indent(i + 3) + ((apt) ? "* " : "") + mt);
                    if (apt) {
                        System.out.println();
                    }
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java

        for (MediaType type : registry.getTypes()) {
            System.out.println(type);
            for (MediaType alias : registry.getAliases(type)) {
                System.out.println("  alias:     " + alias);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            System.out.println(type);
            for (MediaType alias : registry.getAliases(type)) {
                System.out.println("  alias:     " + alias);
            }
            MediaType supertype = registry.getSupertype(type);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            MediaType supertype = registry.getSupertype(type);
            if (supertype != null) {
                System.out.println("  supertype: " + supertype);
            }
            Parser p = parsers.get(type);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                    p = ((CompositeParser) p).getParsers().get(type);
                }
                System.out.println("  parser:    " + p.getClass().getName());
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                }
            }
            System.out.println("Extracting '" + name + "' (" + contentType + ") to " + outputFile);

            try (FileOutputStream os = new FileOutputStream(outputFile)) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java

        // Report
        System.out.println("Tika knows about " + tikaTypes + " unique mime types");
        System.out.println(
                "Tika knows about " + (tikaTypes + tikaAliases) + " mime types including aliases");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        // Report
        System.out.println("Tika knows about " + tikaTypes + " unique mime types");
        System.out.println(
                "Tika knows about " + (tikaTypes + tikaAliases) + " mime types including aliases");
        System.out.println(
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        System.out.println(
                "Tika knows about " + (tikaTypes + tikaAliases) + " mime types including aliases");
        System.out.println(
                "The File Magic directory knows about " + fileMimes.size() + " unique mime types");
        System.out.println();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        System.out.println(
                "The File Magic directory knows about " + fileMimes.size() + " unique mime types");
        System.out.println();
        System.out.println("The following mime types are known to File but not Tika:");
        for (String mime : tikaLacking) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                "The File Magic directory knows about " + fileMimes.size() + " unique mime types");
        System.out.println();
        System.out.println("The following mime types are known to File but not Tika:");
        for (String mime : tikaLacking) {
            System.out.println("  " + mime);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        System.out.println("The following mime types are known to File but not Tika:");
        for (String mime : tikaLacking) {
            System.out.println("  " + mime);
        }
        System.out.println();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            System.out.println("  " + mime);
        }
        System.out.println();
        System.out.println(
                "The following mime types from File have no Tika magic (but their children might):");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        }
        System.out.println();
        System.out.println(
                "The following mime types from File have no Tika magic (but their children might):");
        for (String mime : tikaNoMagic) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                "The following mime types from File have no Tika magic (but their children might):");
        for (String mime : tikaNoMagic) {
            System.out.println("  " + mime);
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/ExternalTranslator.java`
#### Snippet
```java
        } catch (IOException e) {
            // Some problem, command is there or is broken
            System.err.println("Broken pipe");
            return false;
        } catch (InterruptedException ie) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/ExternalTranslator.java`
#### Snippet
```java
        } catch (InterruptedException ie) {
            // Some problem, command is there or is broken
            System.err.println("Interrupted");
            return false;
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MosesTranslator.java`
#### Snippet
```java
        this.smtPath = smtPath;
        this.scriptPath = scriptPath;
        System.out.println(buildCommand(smtPath, scriptPath));
    }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
                                        newAttrs.setValue(i, newSrc);
                                    } catch (IOException e) {
                                        System.err.println(
                                                "Error creating temp image file " + filename);
                                        // The html viewer will show a broken image too to alert them
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
        } else {
            serverStatus = new ServerStatus(serverId, tikaServerConfig.getNumRestarts(), false);
            System.setOut(System.err);
        }
        TikaResource.init(tika, tikaServerConfig, digester, inputStreamFactory, serverStatus);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
            //from maven during build: Corrupted STDOUT by directly writing to
            // native stream in forked
            redirectIO(process.getInputStream(), System.err);
            redirectIO(process.getErrorStream(), System.err);
            return process;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
            // native stream in forked
            redirectIO(process.getInputStream(), System.err);
            redirectIO(process.getErrorStream(), System.err);
            return process;
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java

        if (args.length == 0) {
            System.err.println(usage);
            System.exit(-1);
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                    FileOutputStream fos = new FileOutputStream(f);
                    newProfile.save(fos);
                    System.out.println(
                            "new profile " + profilename + "." + FILE_EXTENSION + " was created.");
                    break;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                            LanguageProfilerBuilder.create(filename2, fis, encoding);
                    newProfile2.normalize();
                    System.out.println("Similarity is " + newProfile.getSimilarity(newProfile2));
                    break;

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                                    DEFAULT_MAX_NGRAM_LENGTH);
                    compare.load(fis);
                    System.out.println("Score is " + compare.getSimilarity(newProfile));
                    break;

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
        TikaEvalCLI cli = new TikaEvalCLI();
        if (args.length == 0) {
            System.err.println(specifyTools());
            return;
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                break;
            default:
                System.out.println(specifyTools());
                break;
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                case "-inputDir":
                    if (i + 1 >= argList.size()) {
                        System.err.println("Must specify directory after -inputDir");
                        ExtractProfiler.USAGE();
                        return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                case "-extracts":
                    if (i + 1 >= argList.size()) {
                        System.err.println("Must specify directory after -extracts");
                        ExtractProfiler.USAGE();
                        return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                case "-alterExtract":
                    if (i + 1 >= argList.size()) {
                        System.err.println("Must specify type 'as_is', 'first_only' or " +
                                           "'concatenate_content' after -alterExtract");
                        ExtractComparer.USAGE();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
        if (alterExtract != null && !alterExtract.equals("as_is") &&
                !alterExtract.equals("concatenate_content") && !alterExtract.equals("first_only")) {
            System.out.println("Sorry, I don't understand:" + alterExtract +
                    ". The values must be one of: as_is, first_only, concatenate_content");
            ExtractProfiler.USAGE();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                        defaultCLIParser.parse(ExtractProfiler.OPTIONS, updatedArgs);
                if (commandLine.hasOption("db") && commandLine.hasOption("jdbc")) {
                    System.out.println(
                            "Please specify either the default -db or the full -jdbc, not both");
                    ExtractProfiler.USAGE();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                }
            } catch (ParseException e) {
                System.out.println(e.getMessage() + "\n");
                ExtractProfiler.USAGE();
                return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                case "-inputDir":
                    if (i + 1 >= argList.size()) {
                        System.err.println("Must specify directory after -inputDir");
                        ExtractComparer.USAGE();
                        return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                case "-extractsA":
                    if (i + 1 >= argList.size()) {
                        System.err.println("Must specify directory after -extractsA");
                        ExtractComparer.USAGE();
                        return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                case "-alterExtract":
                    if (i + 1 >= argList.size()) {
                        System.err.println("Must specify type 'as_is', 'first_only' or " +
                                           "'concatenate_content' after -alterExtract");
                        ExtractComparer.USAGE();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
        if (alterExtract != null && !alterExtract.equals("as_is") &&
                !alterExtract.equals("concatenate_content") && !alterExtract.equals("first_only")) {
            System.out.println("Sorry, I don't understand:" + alterExtract +
                    ". The values must be one of: as_is, first_only, concatenate_content");
            ExtractComparer.USAGE();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                        defaultCLIParser.parse(ExtractComparer.OPTIONS, updatedArgs);
                if (commandLine.hasOption("db") && commandLine.hasOption("jdbc")) {
                    System.out.println(
                            "Please specify either the default -db or the full -jdbc, not both");
                    ExtractComparer.USAGE();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                }
            } catch (ParseException e) {
                System.out.println(e.getMessage() + "\n");
                ExtractComparer.USAGE();
                return;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                CommandLine commandLine = defaultCLIParser.parse(FileProfiler.OPTIONS, updatedArgs);
                if (commandLine.hasOption("db") && commandLine.hasOption("jdbc")) {
                    System.out.println(
                            "Please specify either the default -db or the full -jdbc, not both");
                    ExtractProfiler.USAGE();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                }
            } catch (ParseException e) {
                System.out.println(e.getMessage() + "\n");
                FileProfiler.USAGE();
                return;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/CommonTokenOverlapCounter.java`
#### Snippet
```java
        double percent = (double) overlap / (double) denom;
        if (percent > 0.01) {
            System.out.printf(Locale.US, "%s %s %.2f%n", langA, langB, percent);
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TrainTestSplit.java`
#### Snippet
```java
    private void processFile(File f, Path outputDir) throws Exception {
        Map<String, BufferedWriter> writers = getWriters(outputDir, f);
        System.err.println("working on " + f);
        try (BufferedReader reader = Files.newBufferedReader(f.toPath(), StandardCharsets.UTF_8)) {
            String line = reader.readLine();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/LeipzigHelper.java`
#### Snippet
```java
        Map<String, List<Path>> m = new TreeMap<>();
        for (File f : leipzigDir.toFile().listFiles()) {
            System.err.println(f);
            String lang = null;
            if (tableMatcher.reset(f.getName()).find()) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/LeipzigHelper.java`
#### Snippet
```java
            }
            if (lang == null) {
                System.err.println("couldn't find a lang: " + f);
                continue;
            }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
        TopCommonTokenCounter counter = new TopCommonTokenCounter();
        if (Files.exists(commonTokensFile)) {
            System.err.println(
                    commonTokensFile.getFileName().toString() + " exists. I'm skipping this.");
            return;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
                            line = reader.readLine();
                            if (++lines % 100000 == 0) {
                                System.out.println(
                                        "processed " + lines + " for " + inputFile.getFileName() +
                                                " :: " + commonTokensFile.toAbsolutePath());
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
                                  AbstractTokenTFDFPriorityQueue queue) throws IOException {
        if (Files.isRegularFile(path)) {
            System.err.println("File " + path.getFileName() + " already exists. Skipping.");
            return;
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/ResultsReporter.java`
#### Snippet
```java
            commandLine = defaultCLIParser.parse(OPTIONS, args);
        } catch (ParseException e) {
            System.out.println(e.getMessage());
            USAGE();
            return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/ResultsReporter.java`
#### Snippet
```java
            dbUtil = new JDBCUtil(commandLine.getOptionValue("jdbc"), driverClass);
        } else {
            System.err.println("Must specify either -db for the default in-memory h2 database\n" +
                    "or -jdbc for a full jdbc connection string");
            USAGE();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/corenlp/CoreNLPNERecogniser.java`
#### Snippet
```java
    public static void main(String[] args) throws IOException, JSONException {
        if (args.length != 1) {
            System.err.println("Error: Invalid Args");
            System.err.println("This tool finds names inside text");
            System.err.println("Usage: <path/to/text/file>");
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/corenlp/CoreNLPNERecogniser.java`
#### Snippet
```java
        if (args.length != 1) {
            System.err.println("Error: Invalid Args");
            System.err.println("This tool finds names inside text");
            System.err.println("Usage: <path/to/text/file>");
            return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/corenlp/CoreNLPNERecogniser.java`
#### Snippet
```java
            System.err.println("Error: Invalid Args");
            System.err.println("This tool finds names inside text");
            System.err.println("Usage: <path/to/text/file>");
            return;
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/corenlp/CoreNLPNERecogniser.java`
#### Snippet
```java
            Map<String, Set<String>> names = ner.recognise(text);
            JSONObject jNames = new JSONObject(names);
            System.out.println(jNames.toString(2));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-pipes/tika-httpclient-commons/src/main/java/org/apache/tika/client/HttpClientUtil.java`
#### Snippet
```java
        } else {
            String msg = EntityUtils.toString(response.getEntity());
            System.out.println("httputil: " + msg);
        }
        return true;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-pipes/tika-httpclient-commons/src/main/java/org/apache/tika/client/HttpClientUtil.java`
#### Snippet
```java
        } else {
            String msg = EntityUtils.toString(response.getEntity());
            System.out.println("httputil: " + msg);
        }
        return true;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java

        runner.execute();
        System.out.println("FSBatchProcessDriver has gracefully completed");
        System.exit(0);
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
                while ((line = reader.readLine()) != null && this.running) {
                    if (redirectForkedProcessToStdOut) {
                        System.out.println("BatchProcess:" + line);
                    }
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSBatchProcessCLI.java`
#### Snippet
```java

        ParallelFileProcessingResult result = futureResult.get();
        System.out.println(FINISHED_STRING);
        System.out.println("\n");
        System.out.println(result.toString());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSBatchProcessCLI.java`
#### Snippet
```java
        ParallelFileProcessingResult result = futureResult.get();
        System.out.println(FINISHED_STRING);
        System.out.println("\n");
        System.out.println(result.toString());
        System.exit(result.getExitStatus());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSBatchProcessCLI.java`
#### Snippet
```java
        System.out.println(FINISHED_STRING);
        System.out.println("\n");
        System.out.println(result.toString());
        System.exit(result.getExitStatus());
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        //redirect streams; all organic warnings should go to System.err;
        //System.err should be redirected to System.out
        PrintStream sysErr = System.err;
        try {
            outputStreamWriter = new PrintStream(sysErr, true, UTF_8.toString());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
            throw new RuntimeException("Can't redirect streams");
        }
        System.setErr(System.out);

        ParallelFileProcessingResult result = null;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/fs/strawman/StrawManTikaAppDriver.java`
#### Snippet
```java
        long start = System.currentTimeMillis();
        if (args.length < 6) {
            System.err.println(StrawManTikaAppDriver.usage());
        }
        Path inputDir = Paths.get(args[0]);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
            if (randomCrawlString != null) {
                //TODO: change to logger warn or throw RuntimeException?
                System.err.println("randomCrawl attribute is ignored by FSListCrawler");
            }
            Path fileList = PropsUtil.getPath(attributes.get("fileList"), null);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/Language.java`
#### Snippet
```java

        LanguageResult result = handler.getLanguage();
        System.out.println(result.getLanguage());
    }
}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/Language.java`
#### Snippet
```java

            LanguageResult result = writer.getLanguage();
            System.out.println(result.getLanguage());
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/Language.java`
#### Snippet
```java
                detector.detect("Alla människor är födda fria och lika i värde och rättigheter.");

        System.out.println(result.getLanguage());
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MediaTypeExample.java`
#### Snippet
```java
        MediaType type = MediaType.parse("image/svg+xml");
        while (type != null) {
            System.out.println(type);
            type = registry.getSupertype(type);
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MediaTypeExample.java`
#### Snippet
```java
        for (MediaType type : registry.getTypes()) {
            Set<MediaType> aliases = registry.getAliases(type);
            System.out.println(type + ", also known as " + aliases);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MediaTypeExample.java`
#### Snippet
```java
        MediaType type = MediaType.parse("text/plain; charset=UTF-8");

        System.out.println("type:    " + type.getType());
        System.out.println("subtype: " + type.getSubtype());

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MediaTypeExample.java`
#### Snippet
```java

        System.out.println("type:    " + type.getType());
        System.out.println("subtype: " + type.getSubtype());

        Map<String, String> parameters = type.getParameters();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MediaTypeExample.java`
#### Snippet
```java

        Map<String, String> parameters = type.getParameters();
        System.out.println("parameters:");
        for (String name : parameters.keySet()) {
            System.out.println("  " + name + "=" + parameters.get(name));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MediaTypeExample.java`
#### Snippet
```java
        System.out.println("parameters:");
        for (String name : parameters.keySet()) {
            System.out.println("  " + name + "=" + parameters.get(name));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DisplayMetInstance.java`
#### Snippet
```java
    public static void main(String[] args) throws Exception {
        Metadata met = DisplayMetInstance.getMet(new URL(args[0]));
        System.out.println(met);
    }
}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/TranscribeTranslateExample.java`
#### Snippet
```java
                text = googleTranslateToEnglish(amazonTranscribe(Paths.get(args[0]),
                        Paths.get(args[1])));
                System.out.print("Transcription and translation successful!\nEXTRACTED TEXT: " + text);
            } else if ("transcribe".equals(args[1])) {
                text = amazonTranscribe(Paths.get(args[0]), Paths.get(args[1]));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/TranscribeTranslateExample.java`
#### Snippet
```java
            } else if ("transcribe".equals(args[1])) {
                text = amazonTranscribe(Paths.get(args[0]), Paths.get(args[1]));
                System.out.print("Transcription successful!\nEXTRACTED TEXT: " + text);
            } else {
                System.out.print("Incorrect invocation, see Javadoc.");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/TranscribeTranslateExample.java`
#### Snippet
```java
                System.out.print("Transcription successful!\nEXTRACTED TEXT: " + text);
            } else {
                System.out.print("Incorrect invocation, see Javadoc.");
            }
        } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/TranscribeTranslateExample.java`
#### Snippet
```java
            }
        } else {
            System.out.print("Incorrect invocation, see Javadoc.");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/ZipListFiles.java`
#### Snippet
```java
        try (ZipFile zip = new ZipFile(path)) {
            for (ZipEntry entry : Collections.list(zip.entries())) {
                System.out.println(entry.getName());
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/ZipListFiles.java`
#### Snippet
```java
        if (args.length > 0) {
            for (String file : args) {
                System.out.println("Files in " + file + " file:");
                listZipEntries(file);
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        Metadata metadata = new Metadata();
        String text = parseUsingComponents(filename, tikaConfig, metadata);
        System.out.println("Parsed Metadata: ");
        System.out.println(metadata);
        System.out.println("Parsed Text: ");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        String text = parseUsingComponents(filename, tikaConfig, metadata);
        System.out.println("Parsed Metadata: ");
        System.out.println(metadata);
        System.out.println("Parsed Text: ");
        System.out.println(text);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        System.out.println("Parsed Metadata: ");
        System.out.println(metadata);
        System.out.println("Parsed Text: ");
        System.out.println(text);

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        System.out.println(metadata);
        System.out.println("Parsed Text: ");
        System.out.println(text);

        System.out.println("-------------------------");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        System.out.println(text);

        System.out.println("-------------------------");

        metadata = new Metadata();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        metadata = new Metadata();
        text = parseUsingAutoDetect(filename, tikaConfig, metadata);
        System.out.println("Parsed Metadata: ");
        System.out.println(metadata);
        System.out.println("Parsed Text: ");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        text = parseUsingAutoDetect(filename, tikaConfig, metadata);
        System.out.println("Parsed Metadata: ");
        System.out.println(metadata);
        System.out.println("Parsed Text: ");
        System.out.println(text);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        System.out.println("Parsed Metadata: ");
        System.out.println(metadata);
        System.out.println("Parsed Text: ");
        System.out.println(text);
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        System.out.println(metadata);
        System.out.println("Parsed Text: ");
        System.out.println(text);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        MimeTypes mimeRegistry = tikaConfig.getMimeRepository();

        System.out.println("Examining: [" + filename + "]");

        metadata.set(TikaCoreProperties.RESOURCE_NAME_KEY, filename);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java

        metadata.set(TikaCoreProperties.RESOURCE_NAME_KEY, filename);
        System.out.println(
                "The MIME type (based on filename) is: [" + mimeRegistry.detect(null, metadata) +
                        "]");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java

        InputStream stream = TikaInputStream.get(new File(filename));
        System.out.println(
                "The MIME type (based on MAGIC) is: [" + mimeRegistry.detect(stream, metadata) +
                        "]");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
        stream = TikaInputStream.get(new File(filename));
        Detector detector = tikaConfig.getDetector();
        System.out.println("The MIME type (based on the Detector interface) is: [" +
                detector.detect(stream, metadata) + "]");

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
                langDetector.detect(FileUtils.readFileToString(new File(filename), UTF_8));

        System.out.println("The language of this content is: [" + lang.getLanguage() + "]");

        // Get a non-detecting parser that handles all the types it can
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
    public static String parseUsingAutoDetect(String filename, TikaConfig tikaConfig,
                                              Metadata metadata) throws Exception {
        System.out.println("Handling using AutoDetectParser: [" + filename + "]");

        AutoDetectParser parser = new AutoDetectParser(tikaConfig);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
    public static void main(String[] args) {
        if (args.length != 1) {
            System.err.println("Usage `java GrabPhoneNumbers [corpus]");
            return;
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
        }
        Path folder = Paths.get(args[0]);
        System.out.println("Searching " + folder.toAbsolutePath() + "...");
        processFolder(folder);
        System.out.println(phoneNumbers.toString());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
        System.out.println("Searching " + folder.toAbsolutePath() + "...");
        processFolder(folder);
        System.out.println(phoneNumbers.toString());
        System.out.println("Parsed " + successfulFiles + "/" + (successfulFiles + failedFiles));
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
        processFolder(folder);
        System.out.println(phoneNumbers.toString());
        System.out.println("Parsed " + successfulFiles + "/" + (successfulFiles + failedFiles));
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/SpringExample.java`
#### Snippet
```java
        Parser parser = context.getBean("tika", Parser.class);
        parser.parse(new ByteArrayInputStream("Hello, World!".getBytes(UTF_8)),
                new WriteOutContentHandler(System.out), new Metadata(), new ParseContext());
    }
}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
    public static void main(String[] args) {
        if (args.length < 1) {
            System.err.println(
                    "Usage: " + StandardsExtractionExample.class.getName() + " /path/to/input");
            System.exit(1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java

        Path folder = Paths.get(pathname);
        System.out.println("Searching " + folder.toAbsolutePath() + "...");
        processFolder(folder);
        System.out.println(standardReferences.toString());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
        System.out.println("Searching " + folder.toAbsolutePath() + "...");
        processFolder(folder);
        System.out.println(standardReferences.toString());
        System.out.println("Parsed " + successfulFiles + "/" + (successfulFiles + failedFiles));
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
        processFolder(folder);
        System.out.println(standardReferences.toString());
        System.out.println("Parsed " + successfulFiles + "/" + (successfulFiles + failedFiles));
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/RollbackSoftware.java`
#### Snippet
```java

    private void updateVersion(String version) {
        System.out.println("Rolling back to version: [" + version + "]");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java
            int n = reader.read(buffer);
            while (n != -1) {
                System.out.append(CharBuffer.wrap(buffer, 0, n));
                n = reader.read(buffer);
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java
        File document = new File("example.doc");
        String content = new Tika().parseToString(document);
        System.out.print(content);
        return content;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/SimpleTypeDetector.java`
#### Snippet
```java
        for (String file : args) {
            String type = tika.detect(new File(file));
            System.out.println(file + ": " + type);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/CustomMimeInfo.java`
#### Snippet
```java

    public static void main(String[] args) throws Exception {
        System.out.println("customMimeInfo=" + customMimeInfo());
        System.out.println("customCompositeDetector=" + customCompositeDetector());
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/CustomMimeInfo.java`
#### Snippet
```java
    public static void main(String[] args) throws Exception {
        System.out.println("customMimeInfo=" + customMimeInfo());
        System.out.println("customCompositeDetector=" + customCompositeDetector());
    }
}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/SimpleTextExtractor.java`
#### Snippet
```java
        for (String file : args) {
            String text = tika.parseToString(new File(file));
            System.out.print(text);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
                    mode = TikaConfigSerializer.Mode.STATIC;
                } else {
                    System.out.println("Use:");
                    System.out.println(
                            "  DumpTikaConfig [--dump-minimal] [--dump-current] [--dump-static] [filename] [encoding]");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
                } else {
                    System.out.println("Use:");
                    System.out.println(
                            "  DumpTikaConfig [--dump-minimal] [--dump-current] [--dump-static] [filename] [encoding]");
                    System.out.println("");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
                    System.out.println(
                            "  DumpTikaConfig [--dump-minimal] [--dump-current] [--dump-static] [filename] [encoding]");
                    System.out.println("");
                    System.out.println("--dump-minimal    Produce the minimal config file");
                    System.out.println("--dump-current    The current (with defaults) config file");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
                            "  DumpTikaConfig [--dump-minimal] [--dump-current] [--dump-static] [filename] [encoding]");
                    System.out.println("");
                    System.out.println("--dump-minimal    Produce the minimal config file");
                    System.out.println("--dump-current    The current (with defaults) config file");
                    System.out.println("--dump-static     Convert dynamic parts to static");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
                    System.out.println("");
                    System.out.println("--dump-minimal    Produce the minimal config file");
                    System.out.println("--dump-current    The current (with defaults) config file");
                    System.out.println("--dump-static     Convert dynamic parts to static");
                    return;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
                    System.out.println("--dump-minimal    Produce the minimal config file");
                    System.out.println("--dump-current    The current (with defaults) config file");
                    System.out.println("--dump-static     Convert dynamic parts to static");
                    return;
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java

        if (writer instanceof StringWriter) {
            System.out.println(writer.toString());
        }
        writer.close();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DirListParser.java`
#### Snippet
```java
        parser.parse(System.in, new BodyContentHandler(), met);

        System.out.println("Num files: " + met.getValues("Filename").length);
        System.out.println("Num executables: " + met.get("NumExecutables"));
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/DirListParser.java`
#### Snippet
```java

        System.out.println("Num files: " + met.getValues("Filename").length);
        System.out.println("Num executables: " + met.get("NumExecutables"));
    }

```

## RuleId[id=ImplicitArrayToString]
### ImplicitArrayToString
Implicit call to 'toString()' on array returned by call to `getSystem_uuid()`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
                .append(System.getProperty("line.separator"));
        sb.append("system_uuid:=")
                .append(getSystem_uuid())
                .append(System.getProperty("line.separator"));
        sb.append("unknown_0044:=")
```

### ImplicitArrayToString
Implicit call to 'toString()' on array returned by call to `getUnknown_0044()`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
                .append(System.getProperty("line.separator"));
        sb.append("unknown_0044:=")
                .append(getUnknown_0044())
                .append(" ]");
        return sb.toString();
```

### ImplicitArrayToString
Implicit call to 'toString()' on array returned by call to `getDir_uuid()`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
        sb.append(getLastModified()).append(" ");
        sb.append(getLangId()).append(" ");
        sb.append(getDir_uuid()).append(" ");
        sb.append(getStream_uuid()).append(" ");
        sb.append(getUnknownOffset()).append(" ");
```

### ImplicitArrayToString
Implicit call to 'toString()' on array returned by call to `getStream_uuid()`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
        sb.append(getLangId()).append(" ");
        sb.append(getDir_uuid()).append(" ");
        sb.append(getStream_uuid()).append(" ");
        sb.append(getUnknownOffset()).append(" ");
        sb.append(getUnknownLen()).append(" ");
```

### ImplicitArrayToString
Call to 'toString()' on array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/GUID.java`
#### Snippet
```java

    public String getGuidString() {
        return guid.toString();
    }

```

### ImplicitArrayToString
Implicit call to 'toString()' on array `this.pattern`
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
        // Needs to be unique, as these get compared.
        return "Magic Detection for " + type + " looking for " + pattern.length + " bytes = " +
                this.pattern + " mask = " + this.mask;
    }
}
```

### ImplicitArrayToString
Implicit call to 'toString()' on array `this.mask`
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
        // Needs to be unique, as these get compared.
        return "Magic Detection for " + type + " looking for " + pattern.length + " bytes = " +
                this.pattern + " mask = " + this.mask;
    }
}
```

### ImplicitArrayToString
Implicit call to 'toString()' on array `name`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFColumnHeader.java`
#### Snippet
```java
    @Override
    public String toString() {
        return "DBFColumnHeader{" + "name='" + name + '\'' + ", colType=" + colType +
                ", fieldLength=" + fieldLength + ", decimalCount=" + decimalCount + '}';
    }
```

## RuleId[id=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
                    if (line != null) {
                        boolean isfirst = true;
                        for (String fragment : line.split("\\u000b")) {
                            if (!isfirst) {
                                xhtml.startElement("br");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
        // the last text run of a text paragraph contains the paragraph break (\r)
        // line breaks (\\u000b) can happen more often
        return fragment.replaceFirst("\\r$", "");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java

        // Control characters as line break
        text = text.replaceAll("[\u0000-\u001f]", "\n");
        xhtml.characters(text);
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java

        String text = p.text();
        if (text.replaceAll("[\\r\\n\\s]+", "").isEmpty()) {
            // Skip empty paragraphs
            return 0;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
                    pictName = attrs.getValue(WORD_ML_URL, NAME_ATTR);
                    if (pictName != null) {
                        pictName = pictName.replaceFirst("wordml://", "");
                    }
                }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/RelationshipsManager.java`
#### Snippet
```java

        String tmp = relsFileName;
        tmp = tmp.replaceFirst("\\/_rels\\/", "/");
        tmp = tmp.replaceFirst(".rels\\Z", "");
        return tmp;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/RelationshipsManager.java`
#### Snippet
```java
        String tmp = relsFileName;
        tmp = tmp.replaceFirst("\\/_rels\\/", "/");
        tmp = tmp.replaceFirst(".rels\\Z", "");
        return tmp;
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/GUID.java`
#### Snippet
```java
    public static GUID fromCurlyBraceUTF16Bytes(byte[] guid) {
        int[] intGuid = new int[16];
        String utf16Str = new String(guid, StandardCharsets.UTF_16LE).replaceAll("\\{", "")
                .replaceAll("-", "").replaceAll("}", "");
        for (int i = 0; i < utf16Str.length(); i += 2) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/GUID.java`
#### Snippet
```java
        int[] intGuid = new int[16];
        String utf16Str = new String(guid, StandardCharsets.UTF_16LE).replaceAll("\\{", "")
                .replaceAll("-", "").replaceAll("}", "");
        for (int i = 0; i < utf16Str.length(); i += 2) {
            intGuid[i / 2] =
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/GUID.java`
#### Snippet
```java
        int[] intGuid = new int[16];
        String utf16Str = new String(guid, StandardCharsets.UTF_16LE).replaceAll("\\{", "")
                .replaceAll("-", "").replaceAll("}", "");
        for (int i = 0; i < utf16Str.length(); i += 2) {
            intGuid[i / 2] =
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
            // increases by 0.25 the score of references which include the word "Publication" or
            // "Standard"
            if (matcher.group().matches(".*" + REGEX_STANDARD_TYPE + ".*")) {
                score += 0.25;
            }
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
            // increases by 0.25 the score of references which have been found within "Applicable
            // Documents" and equivalent sections
            if (header != null && headers.get(startHeader).matches(REGEX_APPLICABLE_DOCUMENTS)) {
                score += 0.25;
            }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/sax/LinkBuilder.java`
#### Snippet
```java

        if (collapseWhitespace) {
            anchor = anchor.replaceAll("\\s+", " ").trim();
        }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/sax/CleanPhoneText.java`
#### Snippet
```java
        }
        // Delete all non-digits and white space.
        text = text.replaceAll("[\\D+\\s]", "");
        return text;
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/pipes/PipesClient.java`
#### Snippet
```java
            if (arg.startsWith("-Xloggc:")) {
                origGCString = arg;
                newGCLogString = arg.replace("${pipesClientId}", "id-" + pipesClientId);
            }
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
        for (int i = 0; i < cmd.length; i++) {
            if (cmd[i].contains(INPUT_FILE_TOKEN)) {
                cmd[i] = cmd[i].replace(INPUT_FILE_TOKEN, stream.getFile().getPath());
                inputToStdIn = false;
            }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
                output = tmp.createTemporaryFile();
                outputFromStdOut = false;
                cmd[i] = cmd[i].replace(OUTPUT_FILE_TOKEN, output.getPath());
            }
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/parser/external2/ExternalParser.java`
#### Snippet
```java
            for (String c : commandLine) {
                if (inputMatcher.reset(c).find()) {
                    String updated = c.replace(INPUT_FILE_TOKEN,
                            ProcessUtils.escapeCommandLine(p.toAbsolutePath().toString()));
                    thisCommandLine.add(updated);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/parser/external2/ExternalParser.java`
#### Snippet
```java
                } else if (outputMatcher.reset(c).find()) {
                    outFile = Files.createTempFile("tika-external2-", "");
                    String updated = c.replace(OUTPUT_FILE_TOKEN,
                            ProcessUtils.escapeCommandLine(outFile.toAbsolutePath().toString()));
                    thisCommandLine.add(updated);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
        for (String commandSegment : origCmd) {
            if (commandSegment.contains(ExternalParser.INPUT_FILE_TOKEN)) {
                commandSegment = commandSegment.replace(ExternalParser.INPUT_FILE_TOKEN,
                        tikaInputStream.getFile().toString());
                inputToStdIn = false;
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
                tempOutputFile = tmp.createTemporaryFile();
                commandSegment = commandSegment
                        .replace(ExternalParser.OUTPUT_FILE_TOKEN, tempOutputFile.toString());
                outputFromStdOut = false;
            }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
                    if (commandSegment.contains(METADATA_COMMAND_ARGUMENTS_SERIALIZED_TOKEN)) {
                        commandSegment = commandSegment
                                .replace(METADATA_COMMAND_ARGUMENTS_SERIALIZED_TOKEN,
                                        serializeMetadata(commandMetadataSegments));
                        cmd.set(i, commandSegment);
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/isatab/ISArchiveParser.java`
#### Snippet
```java

            File locationFile = new File(location);
            String[] investigationList = locationFile.list((dir, name) -> name.matches("i_.+\\.txt"));

            XHTMLContentHandler xhtml = new XHTMLContentHandler(handler, metadata);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java
                    String[] mapInfoValues = parseMapInfoContents(keyValue[1]);
                    if (mapInfoValues[0].equals("UTM")) {
                        metadata.set("envi." + keyValue[0].trim().replace(" ", "."),
                                keyValue[1].trim());
                        String[] latLonStringArray =
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java
                        xhtml.endElement("p");
                    } else {
                        metadata.set("envi." + keyValue[0].trim().replace(" ", "."),
                                keyValue[1].trim());
                    }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java
                    }
                } else {
                    metadata.set("envi." + keyValue[0].trim().replace(" ", "."),
                            keyValue[1].trim());
                }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
        try {
            if (this.command.contains(INPUT_FILE_TOKEN)) {
                pCommand = this.command.replace(INPUT_FILE_TOKEN, tis.getFile().getPath());
            }
        } catch (Exception e) {
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-font-module/src/main/java/org/apache/tika/parser/font/AdobeFontMetricParser.java`
#### Snippet
```java
        for (String value : comments) {
            // Look for the creation date
            if (value.matches(".*Creation\\sDate.*")) {
                date = value.substring(value.indexOf(":") + 2);
                comments.remove(value);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java
        }
        if (name != null && name.length() > 0) {
            name = name.replace("\\", "/");
            entrydata.set(TikaCoreProperties.RESOURCE_NAME_KEY, name);
            AttributesImpl attributes = new AttributesImpl();
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        if (apt) {
            name = name.substring(0, name.lastIndexOf(".") + 1) + "{{{./api/" +
                    name.replace(".", "/") + "}" + name.substring(name.lastIndexOf(".") + 1) + "}}";
        } else if (decorated != null) {
            name += decorated;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            //defensively do this so that we don't get an exception
            //from FilenameUtils.normalize
            name = name.replaceAll("\u0000", " ");
            String normalizedName = FilenameUtils.normalize(name);

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/RTGTranslator.java`
#### Snippet
```java
        }
        Map<String, List<Object>> input = new HashMap<>();
        input.put("source", Arrays.asList(text.split("(?<=(?<![A-Z])\\. )|\\n")));
        Response response = client.path("translate").type(MediaType.APPLICATION_JSON)
                .accept(MediaType.APPLICATION_JSON).post(input);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/HTMLHelper.java`
#### Snippet
```java
     */
    public void generateHeader(StringBuffer html, String title) {
        html.append(PRE_BODY.replace(TITLE_VAR, title));
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-pipes/tika-emitters/tika-emitter-jdbc/src/main/java/org/apache/tika/pipes/emitter/jdbc/JDBCEmitter.java`
#### Snippet
```java
        @Override
        String normalize(String emitKey, String columnName, String s, int maxLength) {
            s = s.replaceAll("\u0000", " ");
            return super.normalize(emitKey, columnName, s, maxLength);
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/UnpackerResource.java`
#### Snippet
```java

        private String getFinalName(String name, Map<String, byte[]> zout) {
            name = name.replaceAll("\u0000", " ");
            String normalizedName = FilenameUtils.normalize(name);

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/util/EvalExceptionUtils.java`
#### Snippet
```java
        Matcher matcher = CAUSED_BY_SNIPPER.matcher(sortTrace);
        sortTrace = matcher.replaceAll("$1");
        return sortTrace.replaceAll("org.apache.tika.", "o.a.t.");
    }
}
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
        String factoryName = factoryEl.asText();
        factoryName = factoryName.startsWith("oala.") ?
                factoryName.replaceFirst("oala.", "org.apache.lucene.analysis.") : factoryName;

        JsonNode paramsEl = map.get(PARAMS);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
            }
            String factoryName = factoryEl.asText();
            factoryName = factoryName.replaceAll("oala.", "org.apache.lucene.analysis.");

            JsonNode paramsEl = filterMap.get(PARAMS);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
            String factoryName = factoryEl.asText();
            factoryName = factoryName.startsWith("oala.") ?
                    factoryName.replaceFirst("oala.", "org.apache.lucene.analysis.") : factoryName;
            JsonNode paramsEl = filterMap.get(PARAMS);
            Map<String, String> params = mapify(paramsEl);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadFormatRemover.java`
#### Snippet
```java
        cleanString = sb.toString();
            //now get rid of escape characters
        cleanString = cleanString.replaceAll(escapeChars, "");
        //now unescape backslash
        cleanString = cleanString.replaceAll("(\\\\\\\\)", "\\\\");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadFormatRemover.java`
#### Snippet
```java
        cleanString = cleanString.replaceAll(escapeChars, "");
        //now unescape backslash
        cleanString = cleanString.replaceAll("(\\\\\\\\)", "\\\\");
        return cleanString;
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/prt/PRTParser.java`
#### Snippet
```java

        // Fix up the known character issues
        text = text.replace("\u03C6", "\u00D8");

        // All done, as best as we can!
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
                                            .replaceAll( dwgc.getCleanDwgReadRegexToReplace(), 
                                                    dwgc.getCleanDwgReadReplaceWith())
                                            .replaceAll("\\bnan\\b", " 0,")
                                            .replaceAll("\\.,", " \\. ,") + "\n";
                            out.write(sCurrentLine);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
                                                    dwgc.getCleanDwgReadReplaceWith())
                                            .replaceAll("\\bnan\\b", " 0,")
                                            .replaceAll("\\.,", " \\. ,") + "\n";
                            out.write(sCurrentLine);
                        }                            
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
                    }
                    //postgres doesn't allow \0000
                    value = value.replaceAll("\u0000", " ");
                    st.setString(dbColOffset, value);
                    break;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
                case Types.CHAR:
                    //postgres doesn't allow \0000
                    value = value.replaceAll("\u0000", " ");
                    st.setString(dbColOffset, value);
                    break;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/LeipzigSampler.java`
#### Snippet
```java

    private String row(String lang, String s) {
        s = s.replaceAll("\\s+", " ");
        return lang + "\t" + s + "\n";
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
            return "";
        }
        return s.replaceAll("\\s+", " ").trim();
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURISchemeUtil.java`
#### Snippet
```java
        byte[] data = null;
        //strip out back slashes as you might have in css
        dataString = (dataString != null) ? dataString.replaceAll("\\\\", " ") : dataString;

        if (dataString == null || dataString.length() == 0) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlEncodingDetector.java`
#### Snippet
```java
        String head = ASCII.decode(ByteBuffer.wrap(buffer, 0, n)).toString();
        //strip out comments
        String headNoComments = head.replaceAll("<!--.*?(-->|$)", " ");
        //try to find the encoding in head without comments
        Charset charset = findCharset(headNoComments);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/opennlp/OpenNLPNameFinder.java`
#### Snippet
```java
        //NOTE: replace this with a NLP tokenizer tool
        //clean + split
        return text.trim().replaceAll("(\\s\\s+)", " ").split("\\s");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/opennlp/OpenNLPNameFinder.java`
#### Snippet
```java
        //NOTE: replace this with a NLP tokenizer tool
        //clean + split
        return text.trim().replaceAll("(\\s\\s+)", " ").split("\\s");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
            LOG.warn("{} in {}", e.getMessage(), path);
        }
        path = path.replaceAll("\u0000", " ");
        try {
            return FilenameUtils.getName(path);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/corenlp/CoreNLPNERecogniser.java`
#### Snippet
```java
                String name = text.substring(start, end);
                //Clean repeating spaces, replace line breaks and tabs with single space
                name = name.trim().replaceAll("(\\s\\s+)|\n|\t", " ");
                if (!name.isEmpty()) {
                    names.get(entityType).add(name);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/opennlp/OpenNLPNERecogniser.java`
#### Snippet
```java

    public static final String MODELS_DIR =
            OpenNLPNERecogniser.class.getPackage().getName().replace(".", "/");
    public static final String PERSON_FILE = "ner-person.bin";
    public static final String LOCATION_FILE = "ner-location.bin";
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java

    private static String clean(String s) {
        s = s.replaceAll("[<>\"]", " ");
        s = s.trim();
        return s;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java

        //strip out commas
        text = text.replaceAll(",", "");

        //1) strip off extra stuff after +0800, e.g. "Mon, 9 May 2016 7:32:00 UTC+0600 (BST)",
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        }
        //16 May 2016 at 09:30:32  GMT+1
        text = text.replaceAll("(?i) at ", " ");
        //just cause
        text = text.replaceAll("\\s+", " ").trim();
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        text = text.replaceAll("(?i) at ", " ");
        //just cause
        text = text.replaceAll("\\s+", " ").trim();
        return text;
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-example/src/main/java/org/apache/tika/example/ExtractEmbeddedFiles.java`
#### Snippet
```java
                //that might be included in the name) and make sure
                //to normalize the name
                name = name.replaceAll("\u0000", " ");
                int prefix = FilenameUtils.getPrefixLength(name);
                if (prefix > -1) {
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-example/src/main/java/org/apache/tika/example/DirListParser.java`
#### Snippet
```java
        List<String> lines = FileUtils.readLines(TikaInputStream.get(is).getFile(), UTF_8);
        for (String line : lines) {
            String[] fileToks = line.split("\\s+");
            if (fileToks.length < 8) {
                continue;
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/feed/FeedParser.java`
#### Snippet
```java
        }

        String[] parts = value.split("<[^>]*>");
        StringBuilder buf = new StringBuilder();

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageMetadataExtractor.java`
#### Snippet
```java
                        // because that is less surprising to users
                        directory.getString(IptcDirectory.TAG_CAPTION)
                                .replaceAll("\r\n?", "\n"));
            }
        }
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
    public void setOtherTesseractSettings(List<String> settings) throws TikaConfigException {
        for (String s : settings) {
            String[] bits = s.trim().split("\\s+");
            if (bits.length != 2) {
                throw new TikaConfigException(
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
                            errBuilder.toString());
        }
        for (String line : outBuilder.toString().split("[\r\n]+")) {
            if (line.startsWith("List of available")) {
                continue;
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
            throw new IllegalArgumentException("Colorspace value cannot be null.");
        }
        if (!colorspace.matches("(?i)^[-_A-Z0-9]+$")) {
            throw new IllegalArgumentException(
                    "colorspace must match this pattern: (?i)^[-_A-Z0-9]+$");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
        }
        // Get rid of embedded spaces
        language = language.replaceAll("\\s", "");
        // Test for leading or trailing +
        if (language.matches("\\+.*|.*\\+")) {
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
        language = language.replaceAll("\\s", "");
        // Test for leading or trailing +
        if (language.matches("\\+.*|.*\\+")) {
            throw new IllegalArgumentException(
                    "Invalid syntax - Can't start or end with +" + language);
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
        for (String lang : langs) {
            // First, make sure it conforms to the correct syntax
            if (!lang.matches(
                    "([a-zA-Z]{3}(_[a-zA-Z]{3,4}){0,2})|script(/|\\\\)[A-Z][a-zA-Z_]+")) {
                invalidLangs.add(lang + " (invalid syntax)");
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
     */
    public void setPageSegMode(String pageSegMode) {
        if (!pageSegMode.matches("[0-9]|10|11|12|13")) {
            throw new IllegalArgumentException("Invalid page segmentation mode");
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        }

        value = value.replaceAll("``", "`");
        value = value.replaceAll("''", "'");
        value = value.replaceAll(new String(new char[]{SL}), "'");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

        value = value.replaceAll("``", "`");
        value = value.replaceAll("''", "'");
        value = value.replaceAll(new String(new char[]{SL}), "'");
        value = value.replaceAll(new String(new char[]{SR}), "'");
```

## RuleId[id=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `AbstractOOXMLExtractor()` of an abstract class should not be declared 'public'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
    protected POIXMLTextExtractor extractor;

    public AbstractOOXMLExtractor(ParseContext context, POIXMLTextExtractor extractor) {
        this.context = context;
        this.extractor = extractor;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRecursiveParserWrapperHandler()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/sax/AbstractRecursiveParserWrapperHandler.java`
#### Snippet
```java
    private int embeddedDepth = 0;

    public AbstractRecursiveParserWrapperHandler(ContentHandlerFactory contentHandlerFactory) {
        this(contentHandlerFactory, -1);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRecursiveParserWrapperHandler()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/sax/AbstractRecursiveParserWrapperHandler.java`
#### Snippet
```java
    }

    public AbstractRecursiveParserWrapperHandler(ContentHandlerFactory contentHandlerFactory,
                                                 int maxEmbeddedResources) {
        this.contentHandlerFactory = contentHandlerFactory;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractFetcher()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/AbstractFetcher.java`
#### Snippet
```java
    }

    public AbstractFetcher(String name) {
        this.name = name;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractFetcher()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/AbstractFetcher.java`
#### Snippet
```java
    private String name;

    public AbstractFetcher() {

    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TrainedModelDetector()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/detect/TrainedModelDetector.java`
#### Snippet
```java
    private final Map<MediaType, TrainedModel> MODEL_MAP = new HashMap<>();

    public TrainedModelDetector() {
        loadDefaultModels(getClass().getClassLoader());
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractEncodingDetectorParser()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/parser/AbstractEncodingDetectorParser.java`
#### Snippet
```java
    }

    public AbstractEncodingDetectorParser(EncodingDetector encodingDetector) {
        this.encodingDetector = encodingDetector;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractEncodingDetectorParser()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/parser/AbstractEncodingDetectorParser.java`
#### Snippet
```java
    private EncodingDetector encodingDetector;

    public AbstractEncodingDetectorParser() {
        encodingDetector = new DefaultEncodingDetector();
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ParserFactory()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/parser/ParserFactory.java`
#### Snippet
```java
    final Map<String, String> args;

    public ParserFactory(Map<String, String> args) {
        this.args = args;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `CryptoParser()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/parser/CryptoParser.java`
#### Snippet
```java
    private final Set<MediaType> types;

    public CryptoParser(String transformation, Provider provider, Set<MediaType> types) {
        this.transformation = transformation;
        this.provider = provider;
```

### NonProtectedConstructorInAbstractClass
Constructor `CryptoParser()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/parser/CryptoParser.java`
#### Snippet
```java
    }

    public CryptoParser(String transformation, Set<MediaType> types) {
        this(transformation, null, types);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMultipleParser()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java
    }

    public AbstractMultipleParser(MediaTypeRegistry registry, MetadataPolicy policy,
                                  Collection<? extends Parser> parsers) {
        this.policy = policy;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMultipleParser()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java

    @SuppressWarnings("rawtypes")
    public AbstractMultipleParser(MediaTypeRegistry registry, Collection<? extends Parser> parsers,
                                  Map<String, Param> params) {
        this(registry, getMetadataPolicy(params), parsers);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMultipleParser()` of an abstract class should not be declared 'public'
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java
    }

    public AbstractMultipleParser(MediaTypeRegistry registry, MetadataPolicy policy,
                                  Parser... parsers) {
        this(registry, policy, Arrays.asList(parsers));
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractProfiler()` of an abstract class should not be declared 'public'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    private String lastExtractExtension = null;

    public AbstractProfiler(ArrayBlockingQueue<FileResource> fileQueue, IDBWriter writer) {
        super(fileQueue);
        this.writer = writer;
```

### NonProtectedConstructorInAbstractClass
Constructor `ConsumersManager()` of an abstract class should not be declared 'public'
in `tika-batch/src/main/java/org/apache/tika/batch/ConsumersManager.java`
#### Snippet
```java
    private long consumersManagerMaxMillis = 60000;

    public ConsumersManager(List<FileResourceConsumer> consumers) {
        this.consumers = Collections.unmodifiableList(consumers);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FileResourceCrawler()` of an abstract class should not be declared 'public'
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
     * @param numConsumers number of consumers (needs to know how many poisons to add when done)
     */
    public FileResourceCrawler(ArrayBlockingQueue<FileResource> queue, int numConsumers) {
        this.queue = queue;
        this.numConsumers = numConsumers;
```

### NonProtectedConstructorInAbstractClass
Constructor `FileResourceConsumer()` of an abstract class should not be declared 'public'
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
    private volatile STATE currentState = STATE.NOT_YET_STARTED;

    public FileResourceConsumer(ArrayBlockingQueue<FileResource> fileQueue) {
        this.fileQueue = fileQueue;
        consumerId = numConsumers.incrementAndGet();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractFSConsumer()` of an abstract class should not be declared 'public'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/AbstractFSConsumer.java`
#### Snippet
```java
public abstract class AbstractFSConsumer extends FileResourceConsumer {

    public AbstractFSConsumer(ArrayBlockingQueue<FileResource> fileQueue) {
        super(fileQueue);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConverter()` of an abstract class should not be declared 'public'
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/AbstractConverter.java`
#### Snippet
```java
    private Metadata metadata;

    public AbstractConverter() throws TikaException {
        meta = XMPMetaFactory.create();
        metadata = new Metadata();
```

## RuleId[id=UnnecessaryBreak]
### UnnecessaryBreak
`break` statement is unnecessary
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java

                    it.error = true;
                    break buildChar;
                }
            }
```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                throw new TikaException("Check your chm lzx block parameters");
            }
        } catch (TikaException e) {
            throw e;
        }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `se` is immediately rethrown
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
            // Some problem, command is there or is broken
            return false;
        } catch (SecurityException se) {
            // External process execution is banned by the security manager
            throw se;
```

### CaughtExceptionImmediatelyRethrown
Caught exception `ioe` is immediately rethrown
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/isatab/ISATabUtils.java`
#### Snippet
```java
                }
            }
        } catch (IOException ioe) {
            throw ioe;
        }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
                return detectStreaming(input, metadata, true);
            }
        } catch (SecurityException e) {
            throw e;
        } catch (EOFException e) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageMetadataExtractor.java`
#### Snippet
```java
            webPMetadata = WebpMetadataReader.readMetadata(file);
            handle(webPMetadata);
        } catch (IOException e) {
            throw e;
        } catch (RiffProcessingException | MetadataException e) {
```

## RuleId[id=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
    public static void addEvenIfNull(Property property, String value, Metadata metadata) {
        if (value == null) {
            value = "";
        }
        metadata.add(property, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
        private void addTextCell(Record record, String text) throws SAXException {
            if (text != null) {
                text = text.trim();
                if (text.length() > 0) {
                    addCell(record, new TextCell(text));
```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcControlData.java`
#### Snippet
```java
            throw new ChmParsingException("4 > dataLenght");
        }
        dest = data[this.getCurrentPlace()] | data[this.getCurrentPlace() + 1] << 8 |
                data[this.getCurrentPlace() + 2] << 16 | data[this.getCurrentPlace() + 3] << 24;

```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcResetTable.java`
#### Snippet
```java
    private long unmarshalUInt32(byte[] data, long dest) throws TikaException {
        ChmAssert.assertByteArrayNotNull(data);
        dest = (data[this.getCurrentPlace()] & 0xff) |
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcResetTable.java`
#### Snippet
```java
            }
        }
        dest = new BigInteger(temp).longValue();
        this.setDataRemained(this.getDataRemained() - 8);
        return dest;
```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
            throw new TikaException("4 > dataLenght");
        }
        dest = (data[this.getCurrentPlace()] & 0xff) |
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
            throw new TikaException("4 > dataLenght");
        }
        dest = (data[this.getCurrentPlace()] & 0xff) |
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmPmgiHeader.java`
#### Snippet
```java
            throw new ChmParsingException("4 > dataLenght");
        }
        dest = (data[this.getCurrentPlace()] & 0xff) |
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java
        }
        if (getData().length - getSwath() < i) {
            i = getData().length - getSwath();
        }
        byte[] tmp = new byte[i];
```

### AssignmentToMethodParameter
Assignment to method parameter `chmBlockInfo`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmBlockInfo.java`
#### Snippet
```java
            throws ChmParsingException {
        if (chmBlockInfo == null) {
            chmBlockInfo = new ChmBlockInfo();
        }
        if (!validateParameters(dle, bytesPerBlock, clcd, chmBlockInfo)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `window`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
        int win = 0;
        while (window > 1) {
            window >>>= 1;
            win++;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
        }

        dest = new BigInteger(temp).longValue();
        this.setDataRemained(this.getDataRemained() - 8);
        return dest;
```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
            throw new TikaException("4 > dataLenght");
        }
        dest = data[this.getCurrentPlace()] | data[this.getCurrentPlace() + 1] << 8 |
                data[this.getCurrentPlace() + 2] << 16 | data[this.getCurrentPlace() + 3] << 24;

```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
            throw new TikaException("4 > dataLenght");
        }
        dest = (data[this.getCurrentPlace()] & 0xff) |
                (data[this.getCurrentPlace() + 1] & 0xff) << 8 |
                (data[this.getCurrentPlace() + 2] & 0xff) << 16 |
```

### AssignmentToMethodParameter
Assignment to method parameter `localName`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
        @Override
        public void endElement(String uri, String localName, String qName) throws SAXException {
            localName = localName.toLowerCase(Locale.US);
            if (MS_SPREADSHEET_URN.equals(uri)) {
                switch (localName) {
```

### AssignmentToMethodParameter
Assignment to method parameter `localName`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
        public void startElement(String uri, String localName, String qName, Attributes attrs)
                throws SAXException {
            localName = localName.toLowerCase(Locale.US);

            if (MS_SPREADSHEET_URN.equals(uri)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `localName`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
                //for now, don't bother checking for end of body...if there's any text
                //after the close of body, we should extract it
                localName = localName.toLowerCase(Locale.US);
                String html = WORDML_TO_XHTML.get(localName);
                if (html != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `localName`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
        public void startElement(String uri, String localName, String qName, Attributes attrs)
                throws SAXException {
            localName = localName.toLowerCase(Locale.US);
            if (WORD_ML_URL.equals(uri)) {
                if (BODY.equals(localName)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/CorePropertiesHandler.java`
#### Snippet
```java
    private Property getProperty(String uri, String localName) {
        if (uri.endsWith("/")) {
            uri = uri.substring(0, uri.length() - 1);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `desc`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
        int idx = desc.lastIndexOf('/');
        if (idx != -1) {
            desc = desc.substring(idx + 1);
        }
        idx = desc.lastIndexOf('.');
```

### AssignmentToMethodParameter
Assignment to method parameter `desc`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
        idx = desc.lastIndexOf('.');
        if (idx != -1) {
            desc = desc.substring(0, idx);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/LittleEndianBitConverter.java`
#### Snippet
```java
        for (int i = 0; i < buffer.length; i++) {
            buffer[i] = (byte) (value & 0xff);
            value = value >> 8;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `ptr`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java
                }
                localPtr.reposition(next);
                ptr = localPtr;
            }
            return ptr;
```

### AssignmentToMethodParameter
Assignment to method parameter `x`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/ULong.java`
#### Snippet
```java

    public static int compare(long x, long y) {
        x += Long.MIN_VALUE;
        y += Long.MIN_VALUE;
        return (x < y) ? -1 : ((x == y) ? 0 : 1);
```

### AssignmentToMethodParameter
Assignment to method parameter `y`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/ULong.java`
#### Snippet
```java
    public static int compare(long x, long y) {
        x += Long.MIN_VALUE;
        y += Long.MIN_VALUE;
        return (x < y) ? -1 : ((x == y) ? 0 : 1);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `in`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/XMPPacketScanner.java`
#### Snippet
```java
    public boolean parse(InputStream in, OutputStream xmlOut) throws IOException {
        if (!in.markSupported()) {
            in = new java.io.BufferedInputStream(in);
        }
        boolean foundXMP = skipAfter(in, PACKET_HEADER);
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `tika-pipes/tika-emitters/tika-emitter-s3/src/main/java/org/apache/tika/pipes/emitter/s3/S3Emitter.java`
#### Snippet
```java

        if (!StringUtils.isBlank(prefix)) {
            path = prefix + "/" + path;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `tika-pipes/tika-emitters/tika-emitter-s3/src/main/java/org/apache/tika/pipes/emitter/s3/S3Emitter.java`
#### Snippet
```java

        if (!StringUtils.isBlank(fileExtension)) {
            path += "." + fileExtension;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `tika-pipes/tika-emitters/tika-emitter-gcs/src/main/java/org/apache/tika/pipes/emitter/gcs/GCSEmitter.java`
#### Snippet
```java
    private void write(String path, Metadata userMetadata, byte[] bytes) {
        if (!StringUtils.isBlank(prefix)) {
            path = prefix + "/" + path;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `tika-pipes/tika-emitters/tika-emitter-gcs/src/main/java/org/apache/tika/pipes/emitter/gcs/GCSEmitter.java`
#### Snippet
```java

        if (!StringUtils.isBlank(fileExtension)) {
            path += "." + fileExtension;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
            // (properly) supports the mark feature
            if (!(stream.markSupported())) {
                stream = new BufferedInputStream(stream);
            }
            return new TikaInputStream(stream, tmp, -1, getExtension(metadata));
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `tika-core/src/main/java/org/apache/tika/io/LookaheadInputStream.java`
#### Snippet
```java
        fill();
        if (buffered > position) {
            len = Math.min(len, buffered - position);
            System.arraycopy(buffer, position, b, off, len);
            position += len;
```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `tika-core/src/main/java/org/apache/tika/io/LookaheadInputStream.java`
#### Snippet
```java
    public long skip(long n) throws IOException {
        fill();
        n = Math.min(n, available());
        position += n;
        return n;
```

### AssignmentToMethodParameter
Assignment to method parameter `length`
in `tika-core/src/main/java/org/apache/tika/sax/StandardsExtractingContentHandler.java`
#### Snippet
```java
            if (maxBufferLength > -1) {
                int remaining = maxBufferLength - stringBuilder.length();
                length = remaining > length ? length : remaining;
            }
            String text = new String(Arrays.copyOfRange(ch, start, start + length));
```

### AssignmentToMethodParameter
Assignment to method parameter `rootExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
            List<DataElement> dataElements, RevisionManifestDataElementData revisionData,
            AtomicReference<ExGuid> rootExGuid) throws TikaException {
        rootExGuid = null;

        for (RevisionManifestRootDeclare kv : revisionData.revisionManifestRootDeclareList) {
```

### AssignmentToMethodParameter
Assignment to method parameter `atts`
in `tika-core/src/main/java/org/apache/tika/sax/SafeContentHandler.java`
#### Snippet
```java
                            atts.getType(j), value);
                }
                atts = filtered;
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `start`
in `tika-core/src/main/java/org/apache/tika/sax/SafeContentHandler.java`
#### Snippet
```java

                // Continue with the rest of the array
                start = j;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-core/src/main/java/org/apache/tika/sax/CleanPhoneText.java`
#### Snippet
```java

    public static String clean(String text) {
        text = text.toLowerCase(Locale.ROOT);
        for (String[][] group : cleanSubstitutions) {
            for (String[] sub : group) {
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-core/src/main/java/org/apache/tika/sax/CleanPhoneText.java`
#### Snippet
```java
        for (String[][] group : cleanSubstitutions) {
            for (String[] sub : group) {
                text = text.replaceAll(sub[0], sub[1]);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-core/src/main/java/org/apache/tika/sax/CleanPhoneText.java`
#### Snippet
```java
        }
        // Delete all non-digits and white space.
        text = text.replaceAll("[\\D+\\s]", "");
        return text;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-core/src/main/java/org/apache/tika/sax/CleanPhoneText.java`
#### Snippet
```java

    public static ArrayList<String> extractPhoneNumbers(String text) {
        text = clean(text);
        int idx = 0;
        Pattern p = Pattern.compile(cleanPhoneRegex);
```

### AssignmentToMethodParameter
Assignment to method parameter `from`
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
        while (pos < to) {
            if (ch[pos] == '<') {
                from = pos = writeCharsAndEntity(ch, from, pos, "lt");
            } else if (ch[pos] == '>') {
                from = pos = writeCharsAndEntity(ch, from, pos, "gt");
```

### AssignmentToMethodParameter
Assignment to method parameter `from`
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
                from = pos = writeCharsAndEntity(ch, from, pos, "lt");
            } else if (ch[pos] == '>') {
                from = pos = writeCharsAndEntity(ch, from, pos, "gt");
            } else if (ch[pos] == '&') {
                from = pos = writeCharsAndEntity(ch, from, pos, "amp");
```

### AssignmentToMethodParameter
Assignment to method parameter `from`
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
                from = pos = writeCharsAndEntity(ch, from, pos, "gt");
            } else if (ch[pos] == '&') {
                from = pos = writeCharsAndEntity(ch, from, pos, "amp");
            } else if (attribute && ch[pos] == '"') {
                from = pos = writeCharsAndEntity(ch, from, pos, "quot");
```

### AssignmentToMethodParameter
Assignment to method parameter `from`
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
                from = pos = writeCharsAndEntity(ch, from, pos, "amp");
            } else if (attribute && ch[pos] == '"') {
                from = pos = writeCharsAndEntity(ch, from, pos, "quot");
            } else {
                pos++;
```

### AssignmentToMethodParameter
Assignment to method parameter `startIndex`
in `tika-core/src/main/java/org/apache/tika/mime/HexCoDec.java`
#### Snippet
```java
        byte[] result = new byte[length / 2];
        for (int j = 0; j < result.length; j++) {
            result[j] = (byte) (hexCharToNibble(hexChars[startIndex++]) * 16 +
                    hexCharToNibble(hexChars[startIndex++]));
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `startIndex`
in `tika-core/src/main/java/org/apache/tika/mime/HexCoDec.java`
#### Snippet
```java
        for (int j = 0; j < result.length; j++) {
            result[j] = (byte) (hexCharToNibble(hexChars[startIndex++]) * 16 +
                    hexCharToNibble(hexChars[startIndex++]));
        }
        return result;
```

### AssignmentToMethodParameter
Assignment to method parameter `startIndex`
in `tika-core/src/main/java/org/apache/tika/mime/HexCoDec.java`
#### Snippet
```java
        char[] result = new char[length * 2];
        for (int i = 0, j = 0; i < length; i++) {
            int bite = bites[startIndex++] & 0xff;
            result[j++] = HEX_CHARS[bite >> 4];
            result[j++] = HEX_CHARS[bite & 0xf];
```

### AssignmentToMethodParameter
Assignment to method parameter `classLoader`
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesFactory.java`
#### Snippet
```java
        // If no specific classloader was requested, use our own class's one
        if (classLoader == null) {
            classLoader = MimeTypesReader.class.getClassLoader();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `object`
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
        if (object instanceof InputStream) {
            resources.add(new InputStreamResource((InputStream) object));
            object = new InputStreamProxy(n);
        } else if (object instanceof RecursiveParserWrapperHandler) {
            resources.add(new RecursiveMetadataContentHandlerResource(
```

### AssignmentToMethodParameter
Assignment to method parameter `object`
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
            resources.add(new RecursiveMetadataContentHandlerResource(
                    (RecursiveParserWrapperHandler) object));
            object = new RecursiveMetadataContentHandlerProxy(n,
                    ((RecursiveParserWrapperHandler) object).getContentHandlerFactory());
        } else if (object instanceof ContentHandler &&
```

### AssignmentToMethodParameter
Assignment to method parameter `object`
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
                !(object instanceof AbstractRecursiveParserWrapperHandler)) {
            resources.add(new ContentHandlerResource((ContentHandler) object));
            object = new ContentHandlerProxy(n);
        } else if (object instanceof ClassLoader) {
            resources.add(new ClassLoaderResource((ClassLoader) object));
```

### AssignmentToMethodParameter
Assignment to method parameter `object`
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
        } else if (object instanceof ClassLoader) {
            resources.add(new ClassLoaderResource((ClassLoader) object));
            object = new ClassLoaderProxy(n);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    private static String unquote(String s) {
        while (s.startsWith("\"") || s.startsWith("'")) {
            s = s.substring(1);
        }
        while (s.endsWith("\"") || s.endsWith("'")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
        }
        while (s.endsWith("\"") || s.endsWith("'")) {
            s = s.substring(0, s.length() - 1);
        }
        return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java

    public MediaType(String type, String subtype, Map<String, String> parameters) {
        type = type.trim().toLowerCase(Locale.ENGLISH);
        subtype = subtype.trim().toLowerCase(Locale.ENGLISH);

```

### AssignmentToMethodParameter
Assignment to method parameter `subtype`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    public MediaType(String type, String subtype, Map<String, String> parameters) {
        type = type.trim().toLowerCase(Locale.ENGLISH);
        subtype = subtype.trim().toLowerCase(Locale.ENGLISH);

        this.slash = type.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `string`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
            if (semicolon != -1) {
                key = string.substring(0, semicolon);
                string = string.substring(semicolon + 1);
            } else {
                string = "";
```

### AssignmentToMethodParameter
Assignment to method parameter `string`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
                string = string.substring(semicolon + 1);
            } else {
                string = "";
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `dateString`
in `tika-core/src/main/java/org/apache/tika/utils/DateUtils.java`
#### Snippet
```java
        if (dateString.charAt(n - 3) == ':' &&
                (dateString.charAt(n - 6) == '+' || dateString.charAt(n - 6) == '-')) {
            dateString = dateString.substring(0, n - 3) + dateString.substring(n - 2);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
            throw new IllegalCharsetNameException(name);
        }
        name = m.group(1);

        String lower = name.toLowerCase(Locale.ENGLISH);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
            if (iso.matches()) {
                // Handle "iso 8859-x" error
                name = "iso-8859-" + iso.group(1);
                charset = COMMON_CHARSETS.get(name);
            } else if (cp.matches()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
            } else if (cp.matches()) {
                // Handle "cp-xxx" error
                name = "cp" + cp.group(1);
                charset = COMMON_CHARSETS.get(name);
            } else if (win.matches()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
            } else if (win.matches()) {
                // Handle "winxxx" and "win-xxx" errors
                name = "windows-" + win.group(1);
                charset = COMMON_CHARSETS.get(name);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `padStr`
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java
        }
        if (isEmpty(padStr)) {
            padStr = SPACE;
        }
        final int padLen = padStr.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `arg`
in `tika-core/src/main/java/org/apache/tika/utils/ProcessUtils.java`
#### Snippet
```java
        if (arg.contains(" ") && SystemUtils.IS_OS_WINDOWS &&
                (arg.startsWith("\"") && arg.endsWith("\""))) {
            arg = arg.substring(1, arg.length() - 1);
        }
        return arg;
```

### AssignmentToMethodParameter
Assignment to method parameter `arg`
in `tika-core/src/main/java/org/apache/tika/utils/ProcessUtils.java`
#### Snippet
```java
        if (arg.contains(" ") && SystemUtils.IS_OS_WINDOWS &&
                (!arg.startsWith("\"") && !arg.endsWith("\""))) {
            arg = "\"" + arg + "\"";
        }
        return arg;
```

### AssignmentToMethodParameter
Assignment to method parameter `classLoader`
in `tika-core/src/main/java/org/apache/tika/detect/NNExampleModelDetector.java`
#### Snippet
```java
    public void loadDefaultModels(ClassLoader classLoader) {
        if (classLoader == null) {
            classLoader = TrainedModelDetector.class.getClassLoader();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `mode`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
            return;
        } else if (mode == Mode.MINIMAL) {
            mode = Mode.CURRENT;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `parser`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
            if (parser.getClass().getName().startsWith(ParserDecorator.class.getName() + "$")) {
                decoration = ((ParserDecorator) parser);
                parser = decoration.getWrappedParser();
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `rootElement`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java

        if (outputParser) {
            rootElement = addParser(mode, rootElement, doc, parser, decoration);
        }
        for (Parser childParser : children) {
```

### AssignmentToMethodParameter
Assignment to method parameter `data`
in `tika-core/src/main/java/org/apache/tika/detect/XmlRootExtractor.java`
#### Snippet
```java
                }
                if (newLen > 0) {
                    data = Arrays.copyOf(data, newLen);
                } else {
                    break;
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParser.java`
#### Snippet
```java
                }
            }
            handler = decorateHandler(handler, metadata, context, autoDetectParserConfig);
            // TIKA-216: Zip bomb prevention
            SecureContentHandler sch = handler != null ?
```

### AssignmentToMethodParameter
Assignment to method parameter `errorValue`
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
    public static boolean check(String[] checkCmd, int... errorValue) {
        if (errorValue.length == 0) {
            errorValue = new int[]{127};
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java
                //  mushed together onto the one solitary handler...
                if (handlerFactory != null) {
                    handler = handlerFactory.getNewContentHandler();
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `errorValue`
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
    public static boolean check(String[] checkCmd, int... errorValue) {
        if (errorValue.length == 0) {
            errorValue = new int[]{127};
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `group`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/hdf/HDFParser.java`
#### Snippet
```java
    protected void unravelStringMet(NetcdfFile ncFile, Group group, Metadata met) {
        if (group == null) {
            group = ncFile.getRootGroup();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `location`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/isatab/ISArchiveParser.java`
#### Snippet
```java
    public ISArchiveParser(String location) {
        if (location != null && !location.endsWith(File.separator)) {
            location += File.separator;
        }
        this.location = location;
```

### AssignmentToMethodParameter
Assignment to method parameter `loader`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

            if (loader == null) {
                loader = ServiceLoader.getContextClassLoader();
            }
            serviceLoader = new ServiceLoader(loader, loadErrorHandler, initializableProblemHandler,
```

### AssignmentToMethodParameter
Assignment to method parameter `baseUrl`
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaPipesHttpClient.java`
#### Snippet
```java
            throws TikaConfigException {
        if (!baseUrl.endsWith("/")) {
            baseUrl += "/";
        }
        this.endPointUrl = baseUrl + getEndpoint();
```

### AssignmentToMethodParameter
Assignment to method parameter `brokenZip`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/zip/utils/ZipSalvager.java`
#### Snippet
```java
        try {
            if (!(brokenZip instanceof RereadableInputStream)) {
                brokenZip = new RereadableInputStream(brokenZip, 50000, true);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `zae`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/zip/utils/ZipSalvager.java`
#### Snippet
```java
                }
            }
            zae = zipArchiveInputStream.getNextZipEntry();
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `end`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v1Handler.java`
#### Snippet
```java

        // Skip trailing whitespace
        end = zero;
        while (start < end && buffer[end - 1] <= ' ') {
            end--;
```

### AssignmentToMethodParameter
Assignment to method parameter `end`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v1Handler.java`
#### Snippet
```java
        end = zero;
        while (start < end && buffer[end - 1] <= ' ') {
            end--;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `start`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v1Handler.java`
#### Snippet
```java
        // Skip leading whitespace
        while (start < end && buffer[start] <= ' ') {
            start++;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java
        byte maybeEncodingFlag = data[offset];
        if (maybeEncodingFlag >= 0 && maybeEncodingFlag < encodings.length) {
            offset++;
            actualLength--;
            encoding = encodings[maybeEncodingFlag];
```

### AssignmentToMethodParameter
Assignment to method parameter `length`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/LyricsHandler.java`
#### Snippet
```java

        if (bytesInBuffer < length) {
            length = bytesInBuffer;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `h1`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/AudioFrame.java`
#### Snippet
```java
    public AudioFrame(int h1, int h2, int h3, int h4, InputStream in) throws IOException {
        if (h1 == -2 && h2 == -2 && h3 == -2 && h4 == -2) {
            h1 = in.read();
            h2 = in.read();
            h3 = in.read();
```

### AssignmentToMethodParameter
Assignment to method parameter `h2`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/AudioFrame.java`
#### Snippet
```java
        if (h1 == -2 && h2 == -2 && h3 == -2 && h4 == -2) {
            h1 = in.read();
            h2 = in.read();
            h3 = in.read();
            h4 = in.read();
```

### AssignmentToMethodParameter
Assignment to method parameter `h3`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/AudioFrame.java`
#### Snippet
```java
            h1 = in.read();
            h2 = in.read();
            h3 = in.read();
            h4 = in.read();
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `h4`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/AudioFrame.java`
#### Snippet
```java
            h2 = in.read();
            h3 = in.read();
            h4 = in.read();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/audio/MidiParser.java`
#### Snippet
```java
        // MidiSystem expects the stream to support the mark feature
        if (!stream.markSupported()) {
            stream = new BufferedInputStream(stream);
        }
        try {
```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/audio/AudioParser.java`
#### Snippet
```java
        // AudioSystem expects the stream to support the mark feature
        if (!stream.markSupported()) {
            stream = new BufferedInputStream(stream);
        }
        stream = new SkipFullyInputStream(stream);
```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/audio/AudioParser.java`
#### Snippet
```java
            stream = new BufferedInputStream(stream);
        }
        stream = new SkipFullyInputStream(stream);
        try {
            AudioFileFormat fileFormat = AudioSystem.getAudioFileFormat(stream);
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/video/FLVParser.java`
#### Snippet
```java
    private Object readAMFData(DataInputStream input, int type) throws IOException {
        if (type == -1) {
            type = input.readUnsignedByte();
        }
        switch (type) {
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentParser.java`
#### Snippet
```java
        embeddedMetadata.set(TikaCoreProperties.EMBEDDED_RESOURCE_TYPE,
                TikaCoreProperties.EmbeddedResourceType.MACRO.toString());
        handler = new OpenDocumentMacroHandler(handler, context);
        XMLReaderUtils.parseSAX(new CloseShieldInputStream(is),
                new EmbeddedContentHandler(handler), context);
```

### AssignmentToMethodParameter
Assignment to method parameter `in`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPInputStream.java`
#### Snippet
```java
    public WPInputStream(InputStream in) {
        if (!in.markSupported()) {
            in = new BufferedInputStream(in);
        }
        this.in = new DataInputStream(in);
```

### AssignmentToMethodParameter
Assignment to method parameter `arg`
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
        //across platforms.
        if (arg.contains(" ") && SystemUtils.IS_OS_WINDOWS) {
            arg = "\"" + arg + "\"";
        }
        return arg;
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java
        }
        if (name != null && name.length() > 0) {
            name = name.replace("\\", "/");
            entrydata.set(TikaCoreProperties.RESOURCE_NAME_KEY, name);
            AttributesImpl attributes = new AttributesImpl();
```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java
        // Ensure that the stream supports the mark feature
        if (!stream.markSupported()) {
            stream = new BufferedInputStream(stream);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/AttributeDependantMetadataHandler.java`
#### Snippet
```java
            String previous = metadata.get(name);
            if (previous != null && previous.length() > 0) {
                value = previous + ", " + value;
            }
            metadata.set(name, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/MetadataHandler.java`
#### Snippet
```java
            String previous = metadata.get(name);
            if (previous != null && previous.length() > 0) {
                value = previous + ", " + value;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/CompressorParser.java`
#### Snippet
```java
        // should not be closed
        if (stream.markSupported()) {
            stream = new CloseShieldInputStream(stream);
        } else {
            // Ensure that the stream supports the mark feature
```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/CompressorParser.java`
#### Snippet
```java
        } else {
            // Ensure that the stream supports the mark feature
            stream = new BufferedInputStream(new CloseShieldInputStream(stream));
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/ElementMetadataHandler.java`
#### Snippet
```java
            if ((value != null && value.length() > 0) || allowEmptyValues) {
                if (value == null || value.length() == 0 && allowEmptyValues) {
                    value = "";
                }
                String[] previous = metadata.getValues(name);
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            ParserDecorator pd = (ParserDecorator) p;
            decorated = " (Wrapped by " + pd.getDecorationName() + ")";
            p = pd.getWrappedParser();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            String ext = getExtension(contentType);
            if (name.indexOf('.') == -1 && contentType != null) {
                name += ext;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            String relID = metadata.get(TikaCoreProperties.EMBEDDED_RELATIONSHIP_ID);
            if (relID != null && !name.startsWith(relID)) {
                name = relID + "_" + name;
            }
            //defensively do this so that we don't get an exception
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            //defensively do this so that we don't get an exception
            //from FilenameUtils.normalize
            name = name.replaceAll("\u0000", " ");
            String normalizedName = FilenameUtils.normalize(name);

```

### AssignmentToMethodParameter
Assignment to method parameter `inputStream`
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java

            if (!inputStream.markSupported()) {
                inputStream = TikaInputStream.get(inputStream);
            }
            MediaType contentType = detector.detect(inputStream, metadata);
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
                    throws SAXException {
                if (XHTMLContentHandler.XHTML.equals(uri)) {
                    uri = null;
                }
                if (!"head".equals(localName)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
            public void endElement(String uri, String localName, String name) throws SAXException {
                if (XHTMLContentHandler.XHTML.equals(uri)) {
                    uri = null;
                }
                if (!"head".equals(localName)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `input`
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
        context.set(DocumentSelector.class, new ImageDocumentSelector());

        input = TikaInputStream.get(input);

        if (input.markSupported()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `sourceLanguage`
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/JoshuaNetworkTranslator.java`
#### Snippet
```java
        //make an attempt to guess language if one is not provided.
        if (sourceLanguage == null) {
            sourceLanguage = detectLanguage(text).getLanguage();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `process`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
            throws InterruptedException {

        process = process.destroyForcibly();
        try {
            boolean destroyed = process.waitFor(60, TimeUnit.SECONDS);
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaParsers.java`
#### Snippet
```java
                isDecorated = true;
                decoratedBy = ((ParserDecorator) p).getDecorationName();
                p = ((ParserDecorator) p).getWrappedParser();
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `tika-pipes/tika-emitters/tika-emitter-jdbc/src/main/java/org/apache/tika/pipes/emitter/jdbc/JDBCEmitter.java`
#### Snippet
```java
        @Override
        String normalize(String emitKey, String columnName, String s, int maxLength) {
            s = s.replaceAll("\u0000", " ");
            return super.normalize(emitKey, columnName, s, maxLength);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `host`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    public void setHost(String host) {
        if ("*".equals(host)) {
            host = "0.0.0.0";
        }
        this.host = host;
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/UnpackerResource.java`
#### Snippet
```java

        private String getFinalName(String name, Map<String, byte[]> zout) {
            name = name.replaceAll("\u0000", " ");
            String normalizedName = FilenameUtils.normalize(name);

```

### AssignmentToMethodParameter
Assignment to method parameter `defaultLangCode`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
    public CommonTokenCountManager(Path commonTokensDir, String defaultLangCode) {
        if (defaultLangCode == null) {
            defaultLangCode = "";
        }
        this.defaultLangCode = defaultLangCode;
```

### AssignmentToMethodParameter
Assignment to method parameter `currentPageRef`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java

            if (pageBase != null && pageBase instanceof COSObject) {
                currentPageRef = new ObjectRef(((COSObject) pageBase).getObjectNumber(),
                        ((COSObject) pageBase).getGenerationNumber());
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `itemCount`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
                extractBookmarkText(current, seen, itemCount + 1);
                current = current.getNextSibling();
                itemCount++;
            }
            xhtml.endElement("ul");
```

### AssignmentToMethodParameter
Assignment to method parameter `fileName`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        }

        fileName = (fileName == null || "".equals(fileName.trim())) ? unicodeFileName : fileName;
        fileName = (fileName == null || "".equals(fileName.trim())) ? displayName : fileName;

```

### AssignmentToMethodParameter
Assignment to method parameter `fileName`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java

        fileName = (fileName == null || "".equals(fileName.trim())) ? unicodeFileName : fileName;
        fileName = (fileName == null || "".equals(fileName.trim())) ? displayName : fileName;

        // TODO: other metadata?
```

### AssignmentToMethodParameter
Assignment to method parameter `dwgReadExecutable`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGParserConfig.java`
#### Snippet
```java
        if (!Paths.get(dwgReadExecutable).isAbsolute())
            try {
                dwgReadExecutable =   new File(dwgReadExecutable).getCanonicalFile().toString();
            } catch (IOException e) {
                //do nothing as the error will be picked up by the DWG Parser
```

### AssignmentToMethodParameter
Assignment to method parameter `str`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
        QuickStringBuffer append(String str) {
            if (str == null) {
                str = String.valueOf(str);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `emitKey`
in `tika-pipes/tika-emitters/tika-emitter-fs/src/main/java/org/apache/tika/pipes/emitter/fs/FileSystemEmitter.java`
#### Snippet
```java

        if (fileExtension != null && fileExtension.length() > 0) {
            emitKey += "." + fileExtension;
        }
        if (basePath != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `tableName`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
     */
    String normalizeTableName(String tableName) {
        tableName = tableName.toLowerCase(Locale.US);
        return tableName;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
                case Types.VARCHAR:
                    if (value != null && value.length() > colInfo.getPrecision()) {
                        value = value.substring(0, colInfo.getPrecision());
                        LOG.warn("truncated varchar value in {} : {}", colInfo.getName(), value);
                    }
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
                    }
                    //postgres doesn't allow \0000
                    value = value.replaceAll("\u0000", " ");
                    st.setString(dbColOffset, value);
                    break;
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
                case Types.CHAR:
                    //postgres doesn't allow \0000
                    value = value.replaceAll("\u0000", " ");
                    st.setString(dbColOffset, value);
                    break;
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/LeipzigSampler.java`
#### Snippet
```java

    private String row(String lang, String s) {
        s = s.replaceAll("\\s+", " ");
        return lang + "\t" + s + "\n";
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java
            if (!TikaInputStream.isTikaInputStream(stream)) {
                tmp = new TemporaryResources();
                stream = TikaInputStream.get(stream, tmp, metadata);
            }
            //AutoDetectReader can throw exceptions during
```

### AssignmentToMethodParameter
Assignment to method parameter `charsetName`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/CharsetDetectionResult.java`
#### Snippet
```java
    public void find(String charsetName) {
        this.found = true;
        charsetName = charsetName.trim();
        if ("x-user-defined".equals(charsetName)) {
            charsetName = "windows-1252";
```

### AssignmentToMethodParameter
Assignment to method parameter `charsetName`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/CharsetDetectionResult.java`
#### Snippet
```java
        charsetName = charsetName.trim();
        if ("x-user-defined".equals(charsetName)) {
            charsetName = "windows-1252";
        }
        this.charset = CharsetAliases.getCharsetByLabel(charsetName);
```

### AssignmentToMethodParameter
Assignment to method parameter `dataString`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURISchemeUtil.java`
#### Snippet
```java
        byte[] data = null;
        //strip out back slashes as you might have in css
        dataString = (dataString != null) ? dataString.replaceAll("\\\\", " ") : dataString;

        if (dataString == null || dataString.length() == 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `label`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/CharsetAliases.java`
#### Snippet
```java
            }
        }
        label = label.trim().toLowerCase(Locale.US);
        return charsetsByLabel.get(label);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java

    private String resolve(String base, String url) {
        url = url.trim();

        // Return the URL as-is if no base URL is available or if the URL
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
            LOG.warn("{} in {}", e.getMessage(), path);
        }
        path = path.replaceAll("\u0000", " ");
        try {
            return FilenameUtils.getName(path);
```

### AssignmentToMethodParameter
Assignment to method parameter `jcas`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
        // JCas
        resetCAS(jcas);
        jcas = null;
    }

```

### AssignmentToMethodParameter
Assignment to method parameter `ae`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
        if (ae != null) {
            ae.destroy();
            ae = null;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java

    private static String clean(String s) {
        s = s.replaceAll("[<>\"]", " ");
        s = s.trim();
        return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
    private static String clean(String s) {
        s = s.replaceAll("[<>\"]", " ");
        s = s.trim();
        return s;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `duration`
in `tika-batch/src/main/java/org/apache/tika/util/DurationFormatUtils.java`
#### Snippet
```java

    public static String formatMillis(long duration) {
        duration = Math.abs(duration);
        StringBuilder sb = new StringBuilder();
        int secs = (int) (duration / 1000) % 60;
```

### AssignmentToMethodParameter
Assignment to method parameter `v`
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
        //trim initial and final " if they exist
        if (v.startsWith("\"")) {
            v = v.substring(1);
        }
        if (v.endsWith("\"")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `v`
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
        }
        if (v.endsWith("\"")) {
            v = v.substring(0, v.length() - 1);
        }
        return Paths.get(v);
```

### AssignmentToMethodParameter
Assignment to method parameter `v`
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
        //trim initial and final " if they exist
        if (v.startsWith("\"")) {
            v = v.substring(1);
        }
        if (v.endsWith("\"")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `v`
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
        }
        if (v.endsWith("\"")) {
            v = v.substring(0, v.length() - 1);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
    protected static String normalize(String text) {

        text = text.toUpperCase(Locale.US);

        //strip out commas
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java

        //strip out commas
        text = text.replaceAll(",", "");

        //1) strip off extra stuff after +0800, e.g. "Mon, 9 May 2016 7:32:00 UTC+0600 (BST)",
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        while (matcher.find()) {
            if (matcher.group(1) != null) {
                text = text.substring(0, matcher.start());
                text += matcher.group(1) + StringUtils.leftPad(matcher.group(2), 2, '0') + ":" +
                        matcher.group(3);
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
            if (matcher.group(1) != null) {
                text = text.substring(0, matcher.start());
                text += matcher.group(1) + StringUtils.leftPad(matcher.group(2), 2, '0') + ":" +
                        matcher.group(3);
                break;
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        matcher = LOCALIZED_OFFSET_PATTERN.matcher(text);
        if (matcher.find()) {
            text = buildLocalizedOffset(matcher, text);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        matcher = AM_PM.matcher(text);
        if (matcher.find()) {
            text = matcher.replaceFirst("$1 $2");
        }
        //The rfc_lenient parser had a problem parsing dates
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        matcher = DAYS_OF_WEEK.matcher(text);
        if (matcher.find()) {
            text = matcher.replaceAll(" ");
        }
        //16 May 2016 at 09:30:32  GMT+1
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        }
        //16 May 2016 at 09:30:32  GMT+1
        text = text.replaceAll("(?i) at ", " ");
        //just cause
        text = text.replaceAll("\\s+", " ").trim();
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        text = text.replaceAll("(?i) at ", " ");
        //just cause
        text = text.replaceAll("\\s+", " ").trim();
        return text;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `string`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        // the +0200?!
        if (string != null) {
            string = string.trim();
            string = string.toUpperCase(Locale.US);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `string`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
        if (string != null) {
            string = string.trim();
            string = string.toUpperCase(Locale.US);
        }
        return Date.from(Instant.from(RFC_5322.parse(string, new ParsePosition(0))));
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/pot/PooledTimeSeriesParser.java`
#### Snippet
```java

            if (prefix == null) {
                prefix = "";
            }
            metadata.add(prefix + "_frames", frames);
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
        //guarantee that the prefix ends with /
        if (!prefix.endsWith("/")) {
            prefix += "/";
        }
        this.prefix = prefix;
```

### AssignmentToMethodParameter
Assignment to method parameter `stream`
in `tika-example/src/main/java/org/apache/tika/example/ImportContextImpl.java`
#### Snippet
```java
        }
        if (stream != null && !stream.markSupported()) {
            stream = new BufferedInputStream(stream);
        }
        type = detector.detect(stream, metadata);
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/AutoDetectTransformer.java`
#### Snippet
```java
        }
        // We always work on the normalised, canonical form
        type = registry.normalize(type);

        while (type != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/AutoDetectTransformer.java`
#### Snippet
```java

            // Failing that, try for the parent of the type
            type = registry.getSupertype(type);
        }
        return fallback;
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/UniversalEncodingListener.java`
#### Snippet
```java
            if (hint != null) {
                // Use the encoding hint when available
                name = hint;
            } else if (hasNonexistentHexInCharsetWindows1252() || statistics.count('\r') == 0) {
                // If it has nonexistent hex value in charset windows-1252 or
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/UniversalEncodingListener.java`
#### Snippet
```java
                    // with ISO-8859-15, where the character is used for
                    // the euro symbol, which is more commonly used.
                    name = CHARSET_ISO_8859_15;
                } else {
                    name = CHARSET_ISO_8859_1;
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/UniversalEncodingListener.java`
#### Snippet
```java
                    name = CHARSET_ISO_8859_15;
                } else {
                    name = CHARSET_ISO_8859_1;
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `confidence`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
    static int adjustConfidence(int codeUnit, int confidence) {
        if (codeUnit == 0) {
            confidence -= 10;
        } else if ((codeUnit >= 0x20 && codeUnit <= 0xff) || codeUnit == 0x0a) {
            confidence += 10;
```

### AssignmentToMethodParameter
Assignment to method parameter `confidence`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
            confidence -= 10;
        } else if ((codeUnit >= 0x20 && codeUnit <= 0xff) || codeUnit == 0x0a) {
            confidence += 10;
        }
        if (confidence < 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `confidence`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
        }
        if (confidence < 0) {
            confidence = 0;
        } else if (confidence > 100) {
            confidence = 100;
```

### AssignmentToMethodParameter
Assignment to method parameter `confidence`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
            confidence = 0;
        } else if (confidence > 100) {
            confidence = 100;
        }
        return confidence;
```

### AssignmentToMethodParameter
Assignment to method parameter `reader`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
    List<CSVResult> sniff(Reader reader) throws IOException {
        if (!reader.markSupported()) {
            reader = new BufferedReader(reader);
        }
        List<CSVResult> ret = new ArrayList<>();
```

### AssignmentToMethodParameter
Assignment to method parameter `is`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
        if (filters instanceof COSNull) {
        } else if (filters instanceof COSName) {
            is = runFilter((COSName) filters, is, new COSDictionary(), 0);
            usedFilters.add((COSName) filters);
            LOG.debug("filter:" + filters + " " + 0 + " : " + is.getLength());
```

### AssignmentToMethodParameter
Assignment to method parameter `is`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
            for (int i = filterArray.size() - 1; i >= 0; i--) {
                COSName filter = (COSName) filterArray.get(i);
                is = runFilter(filter, is, new COSDictionary(), 0);
                if (random.nextFloat() > 0.1 && transformed == false) {
                    is = transformRawStream(is);
```

### AssignmentToMethodParameter
Assignment to method parameter `is`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
                is = runFilter(filter, is, new COSDictionary(), 0);
                if (random.nextFloat() > 0.1 && transformed == false) {
                    is = transformRawStream(is);
                    transformed = true;
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java
    public void setStringsPath(String path) {
        if (!path.isEmpty() && !path.endsWith(File.separator)) {
            path += File.separatorChar;
        }
        this.stringsPath = path;
```

### AssignmentToMethodParameter
Assignment to method parameter `parents`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageParser.java`
#### Snippet
```java
        if (addThisNodeName) {
            if (parents.length() > 0) {
                parents += " ";
            }
            parents += node.getNodeName();
```

### AssignmentToMethodParameter
Assignment to method parameter `parents`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageParser.java`
#### Snippet
```java
                parents += " ";
            }
            parents += node.getNodeName();
        }
        NamedNodeMap map = node.getAttributes();
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageParser.java`
#### Snippet
```java
    private static String normalize(String value) {
        if (value != null) {
            value = value.trim();
        } else {
            value = "";
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageParser.java`
#### Snippet
```java
            value = value.trim();
        } else {
            value = "";
        }
        if (Boolean.TRUE.toString().equalsIgnoreCase(value)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bytesRead`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/apple/AppleSingleFileParser.java`
#### Snippet
```java
            //just in case
            IOUtils.skipFully(stream, diff);
            bytesRead += diff;
            if (f.entryId == REAL_NAME) {
                if (f.length > MAX_FIELD_LENGTH) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bytesRead`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/apple/AppleSingleFileParser.java`
#### Snippet
```java
                buffer = new byte[(int) f.length];
                IOUtils.readFully(stream, buffer);
                bytesRead += f.length;
                String originalFileName =
                        new String(buffer, 0, buffer.length, StandardCharsets.US_ASCII);
```

### AssignmentToMethodParameter
Assignment to method parameter `bytesRead`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/apple/AppleSingleFileParser.java`
#### Snippet
```java
            } else if (f.entryId != DATA_FORK) {
                IOUtils.skipFully(stream, f.length);
                bytesRead += f.length;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageMetadataExtractor.java`
#### Snippet
```java
        if (s != null) {
            int i = s.lastIndexOf(" pixels");
            s = s.substring(0, i);
        }
        return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java
        StringBuffer sbuff = new StringBuffer();

        i = i2r(sbuff, i, "M", 1000, "CM", 900, "D", 500, "CD", 400);
        i = i2r(sbuff, i, "C", 100, "XC", 90, "L", 50, "XL", 40);
        i = i2r(sbuff, i, "X", 10, "IX", 9, "V", 5, "IV", 4);
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java

        i = i2r(sbuff, i, "M", 1000, "CM", 900, "D", 500, "CD", 400);
        i = i2r(sbuff, i, "C", 100, "XC", 90, "L", 50, "XL", 40);
        i = i2r(sbuff, i, "X", 10, "IX", 9, "V", 5, "IV", 4);

```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java
        i = i2r(sbuff, i, "M", 1000, "CM", 900, "D", 500, "CD", 400);
        i = i2r(sbuff, i, "C", 100, "XC", 90, "L", 50, "XL", 40);
        i = i2r(sbuff, i, "X", 10, "IX", 9, "V", 5, "IV", 4);

        while (i >= 1) {
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java
        while (i >= 1) {
            sbuff.append("I");
            i -= 1;
        }
        return sbuff.toString();
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java
        while (i >= iTens) {
            sbuff.append(tens);
            i -= iTens;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java
        if (i >= iNines) {
            sbuff.append(nines);
            i -= iNines;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java
        if (i >= iFives) {
            sbuff.append(fives);
            i -= iFives;
        }
        if (i >= iFours) {
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java
        if (i >= iFours) {
            sbuff.append(fours);
            i -= iFours;
        }
        return i;
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
            if (lastSeenMark != null) {
                if (footnotes.containsKey(lastSeenMark)) {
                    text = footnotes.get(lastSeenMark) + text;
                }
                footnotes.put(lastSeenMark, text);
```

### AssignmentToMethodParameter
Assignment to method parameter `var3`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/tess4j/ImageUtil.java`
#### Snippet
```java
        }

        var3 -= var7;
        var4 -= var8;
        BufferedImage var17 = new BufferedImage(var9 - var7, var10 - var8, var0.getType());
```

### AssignmentToMethodParameter
Assignment to method parameter `var4`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/tess4j/ImageUtil.java`
#### Snippet
```java

        var3 -= var7;
        var4 -= var8;
        BufferedImage var17 = new BufferedImage(var9 - var7, var10 - var8, var0.getType());
        Graphics2D var18 = var17.createGraphics();
```

### AssignmentToMethodParameter
Assignment to method parameter `tesseractPath`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
    @Field
    public void setTesseractPath(String tesseractPath) {
        tesseractPath = FilenameUtils.normalize(tesseractPath);
        if (!tesseractPath.isEmpty() && !tesseractPath.endsWith(File.separator)) {
            tesseractPath += File.separator;
```

### AssignmentToMethodParameter
Assignment to method parameter `tesseractPath`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
        tesseractPath = FilenameUtils.normalize(tesseractPath);
        if (!tesseractPath.isEmpty() && !tesseractPath.endsWith(File.separator)) {
            tesseractPath += File.separator;
        }
        this.tesseractPath = tesseractPath;
```

### AssignmentToMethodParameter
Assignment to method parameter `tessdataPath`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
    @Field
    public void setTessdataPath(String tessdataPath) {
        tessdataPath = FilenameUtils.normalize(tessdataPath);
        if (!tessdataPath.isEmpty() && !tessdataPath.endsWith(File.separator)) {
            tessdataPath += File.separator;
```

### AssignmentToMethodParameter
Assignment to method parameter `tessdataPath`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
        tessdataPath = FilenameUtils.normalize(tessdataPath);
        if (!tessdataPath.isEmpty() && !tessdataPath.endsWith(File.separator)) {
            tessdataPath += File.separator;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `parseContext`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
            throws TikaException, IOException, SAXException {
        if (parseContext == null) {
            parseContext = new ParseContext();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `imageMagickPath`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
    @Field
    public void setImageMagickPath(String imageMagickPath) {
        imageMagickPath = FilenameUtils.normalize(imageMagickPath);
        if (!imageMagickPath.isEmpty() && !imageMagickPath.endsWith(File.separator)) {
            imageMagickPath += File.separator;
```

### AssignmentToMethodParameter
Assignment to method parameter `imageMagickPath`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
        imageMagickPath = FilenameUtils.normalize(imageMagickPath);
        if (!imageMagickPath.isEmpty() && !imageMagickPath.endsWith(File.separator)) {
            imageMagickPath += File.separator;
        }
        this.imageMagickPath = imageMagickPath;
```

### AssignmentToMethodParameter
Assignment to method parameter `language`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
        }
        // Get rid of embedded spaces
        language = language.replaceAll("\\s", "");
        // Test for leading or trailing +
        if (language.matches("\\+.*|.*\\+")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                                    byte[] resize = new byte[value.length + 1];
                                    System.arraycopy(value, 0, resize, 1, value.length);
                                    value = resize;
                                }
                            }
```

### AssignmentToMethodParameter
Assignment to method parameter `maxsize`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
            //       more nefariously, it caps at a total 8K, through all sections
            int streammax = is.available();
            maxsize = Math.min(maxsize, streammax);

            is.mark(maxsize);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private String clean(String value) {
        if (value == null) {
            value = "";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        }

        value = value.replaceAll("``", "`");
        value = value.replaceAll("''", "'");
        value = value.replaceAll(new String(new char[]{SL}), "'");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

        value = value.replaceAll("``", "`");
        value = value.replaceAll("''", "'");
        value = value.replaceAll(new String(new char[]{SL}), "'");
        value = value.replaceAll(new String(new char[]{SR}), "'");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll("``", "`");
        value = value.replaceAll("''", "'");
        value = value.replaceAll(new String(new char[]{SL}), "'");
        value = value.replaceAll(new String(new char[]{SR}), "'");
        value = value.replaceAll(new String(new char[]{DL}), "\"");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll("''", "'");
        value = value.replaceAll(new String(new char[]{SL}), "'");
        value = value.replaceAll(new String(new char[]{SR}), "'");
        value = value.replaceAll(new String(new char[]{DL}), "\"");
        value = value.replaceAll(new String(new char[]{DR}), "\"");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll(new String(new char[]{SL}), "'");
        value = value.replaceAll(new String(new char[]{SR}), "'");
        value = value.replaceAll(new String(new char[]{DL}), "\"");
        value = value.replaceAll(new String(new char[]{DR}), "\"");
        value = value.trim();
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll(new String(new char[]{SR}), "'");
        value = value.replaceAll(new String(new char[]{DL}), "\"");
        value = value.replaceAll(new String(new char[]{DR}), "\"");
        value = value.trim();

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        value = value.replaceAll(new String(new char[]{DL}), "\"");
        value = value.replaceAll(new String(new char[]{DR}), "\"");
        value = value.trim();

        return (value);
```

## RuleId[id=ReturnNull]
### ReturnNull
Return of `null`
in `tika-java7/src/main/java/org/apache/tika/filetypedetector/TikaFileTypeDetector.java`
#### Snippet
```java
        // Specification says to return null if we could not 
        // conclusively determine the file type
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/EMFParser.java`
#### Snippet
```java
    private String tryToReadAsString(byte[] bytes) {
        if (bytes.length < 2) {
            return null;
        }
        //act like this is a null terminated unicode le
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/EMFParser.java`
#### Snippet
```java
            //didn't work out...oh, well
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
            //only closes the underlying zipFile, not any other resources
            //as of this writing.... :'(
            return null;
        }
        //this will now be closed eventually when the wrapper closes
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
            }*/
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
            return TIKA_OOXML;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
        if (core.size() != 1) {
            // Invalid OOXML Package received
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractPOIFSExtractor.java`
#### Snippet
```java
            return passwordProvider.getPassword(parentMetadata);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/SummaryExtractor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
        // find the override for this level
        if (listTables.getLfoData(par.getIlfo()).getRgLfoLvl().length == 0) {
            return null;
        }
        overrideLevel = listTables.getLfoData(par.getIlfo()).getRgLfoLvl()[0];
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
        overrideLevel = listTables.getLfoData(par.getIlfo()).getRgLfoLvl()[0];
        if (overrideLevel == null) {
            return null;
        }
        LevelTuple[] levelTuples = new LevelTuple[length];
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
        ListLevel listLevel = overrideLevel.getLevel();
        if (listLevel == null) {
            return null;
        }
        for (int i = 0; i < length; i++) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
            // Please open POI bugs for any you come across!
            EmbeddedDocumentUtil.recordEmbeddedStreamException(e, parentMetadata);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
                }
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxState.java`
#### Snippet
```java
            return clone;
        } catch (CloneNotSupportedException ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxState.java`
#### Snippet
```java

    private static short[] arrayClone(short[] a) {
        return a == null ? null : (short[]) a.clone();
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
        //this only handles embedded objects
        if (type != 3) {
            return null;
        }
        //should we really be ignoring this filePathLen?
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
        //2 is an embedded object. 1 is a link.
        if (formatId != 2L) {
            return null;
        }
        String className = readLengthPrefixedAnsiString(is).trim();
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
            PackageRelationship stylesRelationShip = stylesParts.getRelationship(0);
            if (stylesRelationShip == null) {
                return null;
            }
            PackagePart stylesPart = packagePart.getRelatedPart(stylesRelationShip);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
            PackagePart stylesPart = packagePart.getRelatedPart(stylesRelationShip);
            if (stylesPart == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
            return new XWPFStylesShim(stylesPart, context);
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
                PackageRelationship numberingRelationShip = numberingParts.getRelationship(0);
                if (numberingRelationShip == null) {
                    return null;
                }
                PackagePart numberingPart = packagePart.getRelatedPart(numberingRelationShip);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
                PackagePart numberingPart = packagePart.getRelatedPart(numberingRelationShip);
                if (numberingPart == null) {
                    return null;
                }
                return new XWPFNumberingShim(numberingPart);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
            //swallow
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
        int overrideLength = ctNum.sizeOfLvlOverrideArray();
        if (overrideLength == 0) {
            return null;
        }
        for (int i = 0; i < length; i++) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSTextExtractor.java`
#### Snippet
```java
    @Override
    public POIXMLDocument getDocument() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSTextExtractor.java`
#### Snippet
```java
    @Override
    public String getText() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSTextExtractor.java`
#### Snippet
```java
    @Override
    public Closeable getFilesystem() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSExtractorDecorator.java`
#### Snippet
```java
    @Override
    public POIXMLDocument getDocument() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java

        if (packageRelationshipCollection.size() == 0) {
            return null;
        }
        PackagePart corePart = pkg.getPart(packageRelationshipCollection.getRelationship(0));
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java

        if (packageRelationshipCollection.size() == 0) {
            return null;
        }
        PackagePart corePart = pkg.getPart(packageRelationshipCollection.getRelationship(0));
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xslf/XSLFEventBasedPowerPointExtractor.java`
#### Snippet
```java
    @Override
    public POIXMLDocument getDocument() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xslf/XSLFEventBasedPowerPointExtractor.java`
#### Snippet
```java
    @Override
    public Closeable getFilesystem() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
        String getName(String id) {
            if (id == null) {
                return null;
            }
            return nameMap.get(id);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
        String getInitials(String id) {
            if (id == null) {
                return null;
            }
            return initialMap.get(id);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFStylesShim.java`
#### Snippet
```java
        @Override
        public String getStyleName(String styleId) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFStylesShim.java`
#### Snippet
```java
    public String getStyleName(String styleId) {
        if (styleId == null) {
            return null;
        }
        return styles.get(styleId);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/CorePropertiesHandler.java`
#### Snippet
```java
            return m.get(localName);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFEventBasedWordExtractor.java`
#### Snippet
```java
    @Override
    public POIXMLDocument getDocument() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFEventBasedWordExtractor.java`
#### Snippet
```java
    @Override
    public Closeable getFilesystem() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFEventBasedWordExtractor.java`
#### Snippet
```java
                PackageRelationship numberingRelationShip = numberingParts.getRelationship(0);
                if (numberingRelationShip == null) {
                    return null;
                }
                PackagePart numberingPart = container.getPart(numberingRelationShip);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFEventBasedWordExtractor.java`
#### Snippet
```java
                PackagePart numberingPart = container.getPart(numberingRelationShip);
                if (numberingPart == null) {
                    return null;
                }
                return new XWPFNumbering(numberingPart);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFEventBasedWordExtractor.java`
#### Snippet
```java
            LOG.warn("Couldn't load numbering", e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/RelationshipsManager.java`
#### Snippet
```java
            return thisPackageRels.get(id);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
        if (part == null || part.getPackagePart() == null
                || part.getPackagePart().getContentType() == null) {
            return null;
        }
        PackagePart packagePart = part.getPackagePart();
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/FileNodePtr.java`
#### Snippet
```java
    public FileNode dereference(OneNoteDocument document) throws TikaException {
        if (nodeListPositions.isEmpty()) {
            return null;
        }
        if (nodeListPositions.get(0) >= document.root.children.size()) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
            return "package";
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/Unsigned.java`
#### Snippet
```java
     */
    public static UByte ubyte(String value) throws NumberFormatException {
        return value == null ? null : UByte.valueOf(value);
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/Unsigned.java`
#### Snippet
```java
     */
    public static ULong ulong(String value) throws NumberFormatException {
        return value == null ? null : ULong.valueOf(value);
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/Unsigned.java`
#### Snippet
```java
     */
    public static UShort ushort(String value) throws NumberFormatException {
        return value == null ? null : UShort.valueOf(value);
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/Unsigned.java`
#### Snippet
```java
     */
    public static UInteger uint(String value) throws NumberFormatException {
        return value == null ? null : UInteger.valueOf(value);
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java

        if (precacheSize <= 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/ZipFilesChunking.java`
#### Snippet
```java

        if (0 == index) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadataList.java`
#### Snippet
```java
        }
        if (ms == null) {
            return null;
        }
        //if the last object is the main document,
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
                        if (boundedInputStream.hasHitBound()) {
                            //tmpFile will be cleaned up when this TikaInputStream is closed
                            return null;
                        }
                    } finally {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
            return (TikaInputStream) stream;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/io/TemporaryResources.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
                .orElse(null);
        if (storageManifestDataElement == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/io/MappedBufferCleaner.java`
#### Snippet
```java
                        try {
                            unmapper.invokeExact(buffer);
                            return null;
                        } catch (Throwable t) {
                            return t;
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/RecursiveMetadataContentHandlerResource.java`
#### Snippet
```java
        try {
            internalProcess(input);
            return null;
        } catch (SAXException e) {
            return e;
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderResource.java`
#### Snippet
```java
        }
        output.flush();
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/MemoryURLStreamHandlerFactory.java`
#### Snippet
```java
            return new MemoryURLStreamHandler();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/InputStreamResource.java`
#### Snippet
```java
        }
        output.flush();
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ContentHandlerResource.java`
#### Snippet
```java
        try {
            internalProcess(input);
            return null;
        } catch (SAXException e) {
            return e;
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ContentHandlerResource.java`
#### Snippet
```java
            return readStringUTF(input);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderProxy.java`
#### Snippet
```java
    protected synchronized URL findResource(String name) {
        if (notFound.contains(name)) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderProxy.java`
#### Snippet
```java
            } else {
                notFound.add(name);
                return null;
            }
        } catch (IOException e) {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderProxy.java`
#### Snippet
```java
            }
        } catch (IOException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ClassLoaderProxy.java`
#### Snippet
```java
            return className.substring(0, packageEndIndex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
            klass = klass.getSuperclass();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/mime/MediaTypeRegistry.java`
#### Snippet
```java
    public MediaType getSupertype(MediaType type) {
        if (type == null) {
            return null;
        } else if (inheritance.containsKey(type)) {
            return inheritance.get(type);
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/mime/MediaTypeRegistry.java`
#### Snippet
```java
            return MediaType.OCTET_STREAM;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/mime/MediaTypeRegistry.java`
#### Snippet
```java
    public MediaType normalize(MediaType type) {
        if (type == null) {
            return null;
        }
        MediaType canonical = registry.get(type.getBaseType());
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/mime/Patterns.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypes.java`
#### Snippet
```java
                return types.get(normalisedType.getBaseType());
            }
            return null;
        } else {
            throw new MimeTypeException("Invalid media type name: " + name);
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
                }
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/EmptyFetcher.java`
#### Snippet
```java
    @Override
    public InputStream fetch(String fetchKey, Metadata metadata) throws TikaException, IOException {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
    public static MediaType parse(String string) {
        if (string == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
                int slash = string.indexOf('/');
                if (slash == -1) {
                    return null;
                } else if (SIMPLE_TYPES.size() < 10000 &&
                        isSimpleName(string.substring(0, slash)) &&
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/mime/MediaType.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/utils/SystemUtils.java`
#### Snippet
```java
            return System.getProperty(property);
        } catch (SecurityException var2) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/utils/DateUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
            return forName(charsetName).name();
        } catch (IllegalArgumentException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
            LOG.warn(noFetcherMsg);
            write(STATUS.FETCHER_NOT_FOUND, noFetcherMsg);
            return null;
        } catch (IOException | TikaException e) {
            LOG.warn("Couldn't initialize fetcher for fetch id '" +
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
            write(STATUS.FETCHER_INITIALIZATION_EXCEPTION,
                    ExceptionUtils.getStackTrace(e));
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
        }
        //unreachable, no?!
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java

        if (str == null) {
            return null;
        }
        if (repeat <= 0) {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java
    public static String leftPad(final String str, final int size, final char padChar) {
        if (str == null) {
            return null;
        }
        final int pads = size - str.length();
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java
    public static String leftPad(final String str, final int size, String padStr) {
        if (str == null) {
            return null;
        }
        if (isEmpty(padStr)) {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/utils/XMLReaderUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/detect/TrainedModelDetector.java`
#### Snippet
```java
            return maxType;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
            return t;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/detect/XmlRootExtractor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/Param.java`
#### Snippet
```java
    public String getTypeString() {
        if (type == null) {
            return null;
        }
        if (List.class.isAssignableFrom(type)) {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/detect/CompositeEncodingDetector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
        // Preliminary check
        if ((value == null) || (type == null)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/ServiceLoader.java`
#### Snippet
```java
            return loader.getResourceAsStream(name);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/detect/CompositeDetector.java`
#### Snippet
```java
            }
        }
        return null;
    }
    /**
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/parser/ParserDecorator.java`
#### Snippet
```java
     */
    public String getDecorationName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/parser/digest/InputStreamDigester.java`
#### Snippet
```java
     */
    protected Provider getProvider() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParsersConfigReader.java`
#### Snippet
```java
                        boolean present = readCheckTagAndCheck(child);
                        if (!present) {
                            return null;
                        }
                        break;
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/language/translate/EmptyTranslator.java`
#### Snippet
```java
public class EmptyTranslator implements Translator {
    public String translate(String text, String sourceLanguage, String targetLanguage) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/language/translate/EmptyTranslator.java`
#### Snippet
```java

    public String translate(String text, String targetLanguage) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/language/translate/DefaultTranslator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/extractor/EmbeddedDocumentUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/extractor/EmbeddedDocumentUtil.java`
#### Snippet
```java
        Parser p = context.get(Parser.class);
        if (p == null) {
            return null;
        }
        if (p instanceof StatefulParser) {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/extractor/EmbeddedDocumentUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java
            //do nothing
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
    public Date getDate(Property property) {
        if (property.getPrimaryProperty().getPropertyType() != Property.PropertyType.SIMPLE) {
            return null;
        }
        if (property.getPrimaryProperty().getValueType() != Property.ValueType.DATE) {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
        }
        if (property.getPrimaryProperty().getValueType() != Property.ValueType.DATE) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
            return parseDate(v);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
        String[] values = metadata.get(name);
        if (values == null) {
            return null;
        } else {
            return values[0];
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
    public Integer getInt(Property property) {
        if (property.getPrimaryProperty().getPropertyType() != Property.PropertyType.SIMPLE) {
            return null;
        }
        if (property.getPrimaryProperty().getValueType() != Property.ValueType.INTEGER) {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
        }
        if (property.getPrimaryProperty().getValueType() != Property.ValueType.INTEGER) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
        String v = get(property);
        if (v == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
            return Integer.valueOf(v);
        } catch (NumberFormatException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-langdetect/tika-langdetect-mitll-text/src/main/java/org/apache/tika/langdetect/mitll/TextLangDetector.java`
#### Snippet
```java
    @Override
    public LanguageDetector loadModels(Set<String> set) throws IOException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-langdetect/tika-langdetect-mitll-text/src/main/java/org/apache/tika/langdetect/mitll/TextLangDetector.java`
#### Snippet
```java
    @Override
    public LanguageDetector loadModels() throws IOException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-langdetect/tika-langdetect-mitll-text/src/main/java/org/apache/tika/langdetect/mitll/TextLangDetector.java`
#### Snippet
```java
    @Override
    public LanguageDetector setPriors(Map<String, Float> languageProbabilities) throws IOException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                              MimeTypes mimeTypes) throws TikaException {
            // Continue with normal loading
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            // Check for classes which can't be set in config
            // Continue with normal loading
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                Class<? extends ConfigurableThreadPoolExecutor> loadedClass, String classname,
                MimeTypes mimeTypes) throws TikaException {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                // Report the problem
                loader.getLoadErrorHandler().handleLoadError(name, e);
                return null;
            } catch (IllegalAccessException e) {
                throw new TikaException(
```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            // Check for classes which can't be set in config
            // Continue with normal loading
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            child = child.getNextSibling();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

        String getParentTagName() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
        @Override
        String getParentTagName() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            }
            // Continue with normal loading
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            }
            // Continue with normal loading
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/ZipContainerDetectorBase.java`
#### Snippet
```java
                kmlFound = true;
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/ZipContainerDetectorBase.java`
#### Snippet
```java
        if (kmlFound) {
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/ZipContainerDetectorBase.java`
#### Snippet
```java
            }

            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/ZipContainerDetectorBase.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/JarDetector.java`
#### Snippet
```java
            }
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/JarDetector.java`
#### Snippet
```java
            }

            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/JarDetector.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/FrictionlessPackageDetector.java`
#### Snippet
```java
            return WACZ;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/FrictionlessPackageDetector.java`
#### Snippet
```java
    MediaType getMediaType(Counter counter) {
        if (counter == null) {
            return null;
        }
        if (counter.parts.contains(PARTS.PACKAGE)) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/FrictionlessPackageDetector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/StarOfficeDetector.java`
#### Snippet
```java
        String name = zae.getName();
        if (!"META-INF/manifest.xml".equals(name)) {
            return null;
        }
        //for an unknown reason, passing in the zipArchiveInputStream
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/StarOfficeDetector.java`
#### Snippet
```java
    @Override
    public MediaType streamingDetectFinal(StreamingDetectContext detectContext) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/StarOfficeDetector.java`
#### Snippet
```java

        if (zae == null) {
            return null;
        }
        return detectStarOfficeX(zip.getInputStream(zae));
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/OpenDocumentDetector.java`
#### Snippet
```java
                }
            } else {
                return null;
            }
        } catch (IOException e) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/OpenDocumentDetector.java`
#### Snippet
```java
            }
        } catch (IOException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/OpenDocumentDetector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/OpenDocumentDetector.java`
#### Snippet
```java
    @Override
    public MediaType streamingDetectFinal(StreamingDetectContext detectContext) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/KMZDetector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/KMZDetector.java`
#### Snippet
```java
                    kmlFound = true;
                } else {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/KMZDetector.java`
#### Snippet
```java
            return MediaType.application("vnd.google-earth.kmz");
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/KMZDetector.java`
#### Snippet
```java

        if (name.indexOf('/') != -1 || name.indexOf('\\') != -1) {
            return null;
        }
        if (name.endsWith(".kml")) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/KMZDetector.java`
#### Snippet
```java
            counter.increment();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/IPADetector.java`
#### Snippet
```java
            return IPA;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/IPADetector.java`
#### Snippet
```java
        TmpPatterns tmp = detectContext.get(TmpPatterns.class);
        if (tmp == null) {
            return null;
        }
        if (tmp.patterns.isEmpty()) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/IPADetector.java`
#### Snippet
```java
        }
        detectContext.remove(TmpPatterns.class);
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/IPADetector.java`
#### Snippet
```java

        // If we get here, not all required entries were found
        return null;

    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/CompositeTagHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v1Handler.java`
#### Snippet
```java
     */
    public String getComposer() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v1Handler.java`
#### Snippet
```java
     */
    public String getAlbumArtist() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v1Handler.java`
#### Snippet
```java
     */
    public String getCompilation() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v1Handler.java`
#### Snippet
```java
     */
    public String getDisc() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java
            if (majorVersion == -1 || minorVersion == -1) {
                pushBack(inp, h1, h2, h3, majorVersion, minorVersion);
                return null;
            }
            return new ID3v2Frame(majorVersion, minorVersion, inp);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java
        // Not a frame header
        pushBack(inp, h1, h2, h3);
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java
        } else {
            // Invalid string
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-jdbc/src/main/java/org/apache/tika/pipes/pipesiterator/jdbc/JDBCPipesIterator.java`
#### Snippet
```java
        String val = rs.getString(i);
        if (rs.wasNull()) {
            return null;
        }
        return val;
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/MpegStream.java`
#### Snippet
```java
    private AudioFrame createHeader(HeaderBitField bits) {
        if (bits.get(21, 23) != 7) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v22Handler.java`
#### Snippet
```java
     */
    public String getCompilation() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/MpegStream.java`
#### Snippet
```java
                sampleRateCode == 3) {
            // invalid header values
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v22Handler.java`
#### Snippet
```java
            }
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/video/FLVParser.java`
#### Snippet
```java
                return "UNDEFINED";
            default:
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFReader.java`
#### Snippet
```java
            return currRow;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/hwp/HwpTextExtractorV5.java`
#### Snippet
```java
            if (read != 256 || !Arrays.equals(HWP_V5_SIGNATURE,
                    Arrays.copyOfRange(header, 0, HWP_V5_SIGNATURE.length))) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
                }
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/CompressorParser.java`
#### Snippet
```java
        String mimeString = metadata.get(Metadata.CONTENT_TYPE);
        if (mimeString == null) {
            return null;
        }
        return MIMES_TO_NAME.get(mimeString);
```

### ReturnNull
Return of `null`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TranslateResource.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
        }
        if (shutDown) {
            return null;
        }
        throw new RuntimeException("Couldn't start forked process");
```

### ReturnNull
Return of `null`
in `tika-pipes/tika-emitters/tika-emitter-jdbc/src/main/java/org/apache/tika/pipes/emitter/jdbc/JDBCEmitter.java`
#### Snippet
```java
        String[] vals = metadata.getValues(columnDefinition.getColumnName());
        if (vals.length == 0) {
            return null;
        } else if (vals.length == 1) {
            return vals[0];
```

### ReturnNull
Return of `null`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFEncodedStringDecoder.java`
#### Snippet
```java
                //swallow
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
                                    ParseContext context) {
        if (pdMetadata == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
            } catch (IOException e) {
                EmbeddedDocumentUtil.recordEmbeddedStreamException(e, metadata);
                return null;
            }
            return XMLReaderUtils.buildDOM(is, context);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
            IOUtils.closeQuietly(is);
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/image/ImageGraphicsEngine.java`
#### Snippet
```java
                    IOUtils.closeQuietly(data);
                }
                return null;
            } else {
                BufferedImage image = pdImage.getImage();
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/image/ImageGraphicsEngine.java`
#### Snippet
```java
                    IOUtils.closeQuietly(data);
                }
                return null;
            } else {
                // for CMYK and other "unusual" colorspaces, the image will be converted
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/image/ImageGraphicsEngine.java`
#### Snippet
```java
            //TODO: log or otherwise report
            if (image == null) {
                return null;
            }
            // CCITT compressed images can have a different colorspace, but this one is B/W
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/image/ImageGraphicsEngine.java`
#### Snippet
```java
            BufferedImage image = pdImage.getImage();
            if (image == null) {
                return null;
            }
            ImageIOUtil.writeImage(image, suffix, out);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/image/ImageGraphicsEngine.java`
#### Snippet
```java

        out.flush();
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
    private MetadataMap<String, String> preparePostHeaderMap(Attachment att,
                                                             HttpHeaders httpHeaders) {
        if (att == null && httpHeaders == null) return null;
        MetadataMap<String, String> finalHeaders = new MetadataMap<>(false,
                true);
```

### ReturnNull
Return of `null`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
            return object.getClass().getMethod(method, clazz);
        } catch (NoSuchMethodException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
            //swallow
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
            return renderer;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/TikaLanguageDetector.java`
#### Snippet
```java
    @Override
    public LanguageDetector setPriors(Map<String, Float> languageProbabilities) throws IOException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-kafka/src/main/java/org/apache/tika/pipes/pipesiterator/kafka/KafkaPipesIterator.java`
#### Snippet
```java
        } catch (ClassNotFoundException e) {
            LOGGER.error("Could not find key serializer class: {}", className);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
    @Override
    public Set<MediaType> getSupportedTypes(ParseContext context) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/JDBCTableReader.java`
#### Snippet
```java
            return blob;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/EvalConsumersBuilder.java`
#### Snippet
```java
        String filePath = attrs.get(key);
        if (filePath == null) {
            return null;
        }
        return Paths.get(filePath);
```

### ReturnNull
Return of `null`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/Report.java`
#### Snippet
```java
                return defaultDoubleFormatter;
            default:
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/SlowCompositeReaderWrapper.java`
#### Snippet
```java
            final FieldInfo fieldInfo = reader.getFieldInfos().fieldInfo(field);
            if (fieldInfo != null && fieldInfo.getDocValuesType() != DocValuesType.SORTED) {
                return null;
            }
            SortedDocValues v = reader.getSortedDocValues(field);
```

### ReturnNull
Return of `null`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/SlowCompositeReaderWrapper.java`
#### Snippet
```java
            final FieldInfo fieldInfo = reader.getFieldInfos().fieldInfo(field);
            if (fieldInfo != null && fieldInfo.getDocValuesType() != DocValuesType.SORTED_SET) {
                return null;
            }
            SortedSetDocValues v = reader.getSortedSetDocValues(field);
```

### ReturnNull
Return of `null`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/SlowCompositeReaderWrapper.java`
#### Snippet
```java
    public PointValues getPointValues(String field) {
        ensureOpen();
        return null; // because not supported.  Throw UOE?
    }

```

### ReturnNull
Return of `null`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
    private String findSharedDigestKey(List<Metadata> metadataListA, List<Metadata> metadataListB) {
        if (metadataListB == null || metadataListB.size() == 0) {
            return null;
        }
        Set<String> digestA = new HashSet<>();
```

### ReturnNull
Return of `null`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/code/SourceCodeParser.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/asm/XHTMLClassVisitor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/asm/XHTMLClassVisitor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURIScheme.java`
#### Snippet
```java
            return MediaType.parse(rawMediaTypeString);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/asm/XHTMLClassVisitor.java`
#### Snippet
```java
     */
    public AnnotationVisitor visitAnnotation(String desc, boolean visible) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/StandardHtmlEncodingDetector.java`
#### Snippet
```java
        MediaType mediatype = MediaType.parse(contentType);
        if (mediatype == null) {
            return null;
        }
        String charsetLabel = mediatype.getParameters().get("charset");
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlEncodingDetector.java`
#### Snippet
```java
    public Charset detect(InputStream input, Metadata metadata) throws IOException {
        if (input == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlEncodingDetector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DefaultHtmlMapper.java`
#### Snippet
```java
            return attributeName;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/CharsetAliases.java`
#### Snippet
```java
    static Charset getCharsetByLabel(String label) {
        if (label == null) {
            return null;
        }
        synchronized (charsetsByLabel) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/PreScanner.java`
#### Snippet
```java
            }
        } catch (IOException e) { /* stream could not be read, also return null */ }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/PreScanner.java`
#### Snippet
```java
        skipAll(SPACE_OR_SLASH);
        if (expect(TAG_END)) {
            return null;
        }
        StringBuilder name = new StringBuilder();
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/PreScanner.java`
#### Snippet
```java
        String name = getAttributeName();
        if (name == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/PreScanner.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/PreScanner.java`
#### Snippet
```java
        Matcher matcher = CHARSET_PATTERN.matcher(attributeValue);
        if (!matcher.find()) {
            return null;
        }
        return matcher.group(2);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/gazetteer/GeoGazetteerClient.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/PoisonFileResource.java`
#### Snippet
```java
    @Override
    public InputStream openInputStream() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/PoisonFileResource.java`
#### Snippet
```java
    @Override
    public Metadata getMetadata() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/PoisonFileResource.java`
#### Snippet
```java
    @Override
    public String getResourceId() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-pipes/tika-emitters/tika-emitter-kafka/src/main/java/org/apache/tika/pipes/emitter/kafka/KafkaEmitter.java`
#### Snippet
```java
        } catch (ClassNotFoundException e) {
            LOGGER.error("Could not find key serializer class: {}", className);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
    public static Date parseDateLenient(String text) {
        if (text == null) {
            return null;
        }
        String normalized = normalize(text);
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java
                return cand;
            } else if (handleExisting.equals(HANDLE_EXISTING.SKIP)) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
        //if there isn't a current file, don't bother obtaining lock
        if (currentFile == null) {
            return null;
        }
        //if threshold is < 0, don't even look.
```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
        //if threshold is < 0, don't even look.
        if (staleThresholdMillis < 0) {
            return null;
        }
        synchronized (lock) {
```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
            if (currentState != STATE.ACTIVELY_CONSUMING &&
                    currentState != STATE.ASKED_TO_SHUTDOWN) {
                return null;
            }
            FileStarted tmp = currentFile;
```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
            FileStarted tmp = currentFile;
            if (tmp == null) {
                return null;
            }
            if (tmp.getElapsedMillis() > staleThresholdMillis) {
```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSOutputStreamFactory.java`
#### Snippet
```java
                FSUtil.getOutputPath(outputRoot, initialRelativePath, handleExisting, suffix);
        if (outputPath == null) {
            return null;
        }
        if (!Files.isDirectory(outputPath.getParent())) {
```

### ReturnNull
Return of `null`
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
        }

        return null;
    } // checkKeyPrefix

```

### ReturnNull
Return of `null`
in `tika-example/src/main/java/org/apache/tika/example/ImportContextImpl.java`
#### Snippet
```java
     */
    public String getContentLanguage() {
        return (inputCtx != null) ? inputCtx.getContentLanguage() : null;
    }

```

### ReturnNull
Return of `null`
in `tika-example/src/main/java/org/apache/tika/example/ImportContextImpl.java`
#### Snippet
```java
     */
    public Object getProperty(Object propertyName) {
        return (inputCtx != null) ? inputCtx.getProperty(propertyName.toString()) : null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3TableReader.java`
#### Snippet
```java
            return new SerialBlob(bytes);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3DBParser.java`
#### Snippet
```java
    @Override
    public Set<MediaType> getSupportedTypes(ParseContext context) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecognizer.java`
#### Snippet
```java
     */
    public String getLanguage() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_UTF8.java`
#### Snippet
```java
            confidence = 25;
        }
        return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
            return new InputStreamReader(inputStream, getName());
        } catch (IOException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/UniversalEncodingDetector.java`
#### Snippet
```java
    public Charset detect(InputStream input, Metadata metadata) throws IOException {
        if (input == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/UniversalEncodingDetector.java`
#### Snippet
```java
            return listener.dataEnd();
        } catch (LinkageError e) {
            return null; // juniversalchardet is not available
        } finally {
            input.reset();
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java

            if (limit == 0) {
                return null;
            }
            if (getChar(input, 0) == 0x0000FEFF) {
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
            }

            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
                return new CharsetMatch(det, this, confidence);
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
                return new CharsetMatch(det, this, confidence);
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/Icu4jEncodingDetector.java`
#### Snippet
```java
    public Charset detect(InputStream input, Metadata metadata) throws IOException {
        if (input == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/Icu4jEncodingDetector.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_2022.java`
#### Snippet
```java
        CharsetMatch match(CharsetDetector det) {
            int confidence = match(det.fInputBytes, det.fInputLen, escapeSequences);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_2022.java`
#### Snippet
```java
        CharsetMatch match(CharsetDetector det) {
            int confidence = match(det.fInputBytes, det.fInputLen, escapeSequences);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_2022.java`
#### Snippet
```java
        CharsetMatch match(CharsetDetector det) {
            int confidence = match(det.fInputBytes, det.fInputLen, escapeSequences);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java

            if (match == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java
            return match.getReader();
        } catch (IOException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java

            if (match == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java
            return match.getString(-1);
        } catch (IOException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java

        if (matches == null || matches.length == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            CharsetMatch match(CharsetDetector det) {
                int confidence = match(det, commonChars);
                return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
            }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
        CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, commonChars);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            CharsetMatch match(CharsetDetector det) {
                int confidence = match(det, commonChars);
                return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
            }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
        CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, commonChars);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
        CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, commonChars);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    public Object visitFromNull(COSNull obj) throws IOException {
        obj.writePDF(getStandardOutput());
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    public Object visitFromFloat(COSFloat obj) throws IOException {
        obj.writePDF(getStandardOutput());
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    public Object visitFromBoolean(COSBoolean obj) throws IOException {
        obj.writePDF(getStandardOutput());
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
            getStandardOutput().write(ENDSTREAM);
            getStandardOutput().writeEOL();
            return null;
        } finally {
            if (input != null) {
```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    public Object visitFromName(COSName obj) throws IOException {
        obj.writePDF(getStandardOutput());
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    public Object visitFromInt(COSInteger obj) throws IOException {
        obj.writePDF(getStandardOutput());
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
        getStandardOutput().write(ARRAY_CLOSE);
        getStandardOutput().writeEOL();
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
        getStandardOutput().write(DICT_CLOSE);
        getStandardOutput().writeEOL();
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
        }
        COSWriter.writeString(obj, getStandardOutput());
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageParser.java`
#### Snippet
```java

        if (mediaType == null) {
            return null;
        }
        // If the old (pre-RFC7903) BMP mime type is given,
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/detect/apple/IWorkDetector.java`
#### Snippet
```java
        EntryNames entryNames = detectContext.get(EntryNames.class);
        if (entryNames == null) {
            return null;
        }
        //general iworks
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/detect/apple/IWorkDetector.java`
#### Snippet
```java
            return MediaType.application("vnd.apple.iwork");
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/detect/apple/IWorkDetector.java`
#### Snippet
```java
            return mt;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/detect/apple/IWorkDetector.java`
#### Snippet
```java
            return IWork13PackageParser.IWork13DocumentType.detect(zip);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/detect/apple/IWorkDetector.java`
#### Snippet
```java
            return MediaType.application("vnd.apple.iwork");
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSType.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/AbstractImageParser.java`
#### Snippet
```java
    static MediaType convertToOCRMediaType(MediaType mediaType) {
        if (mediaType == null) {
            return null;
        }
        return new MediaType(mediaType.getType(), OCR_MEDIATYPE_PREFIX + mediaType.getSubtype());
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/apple/AppleSingleFileParser.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/IWorkPackageParser.java`
#### Snippet
```java
            try {
                if (entry == null) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/IWorkPackageParser.java`
#### Snippet
```java
                }
            } catch (IOException e) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/IWorkPackageParser.java`
#### Snippet
```java
                                                   ZipArchiveInputStream zip) {
            if (entry == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/IWorkPackageParser.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
            int confidence =
                    match(det, CharsetRecog_8859_1.ngrams_8859_1[getLangIndex()].fNGrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
            String name = det.fC1Bytes ? "windows-1254" : "ISO-8859-9";
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence, name, "tr");
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = matchIBM420(det, ngrams, byteMap, (byte) 0x40);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
            String name = det.fC1Bytes ? "windows-1255" : "ISO-8859-8-I";
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence, name, "he");
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null :
                    new CharsetMatch(det, this, confidence, getName(), "ru");
        }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, ngrams, byteMap, (byte) 0x40);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
            String name = det.fC1Bytes ? "windows-1255" : "ISO-8859-8";
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence, name, "he");

        }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
                }
            }
            return bestConfidenceSoFar <= 0 ? null :
                    new CharsetMatch(det, this, bestConfidenceSoFar, name, lang);
        }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = matchIBM420(det, ngrams, byteMap, (byte) 0x40);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, ngrams, byteMap, (byte) 0x40);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        public CharsetMatch match(CharsetDetector det) {
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence);
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
                }
            }
            return bestConfidenceSoFar <= 0 ? null :
                    new CharsetMatch(det, this, bestConfidenceSoFar, name, lang);
        }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
            String name = det.fC1Bytes ? "windows-1253" : "ISO-8859-7";
            int confidence = match(det, ngrams, byteMap);
            return confidence == 0 ? null : new CharsetMatch(det, this, confidence, name, "el");
        }
    }
```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
            }

            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork18PackageParser.java`
#### Snippet
```java

            // If we get here, we don't know what it is
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork18PackageParser.java`
#### Snippet
```java
            }
            // Unknown
            return null;
        }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork13PackageParser.java`
#### Snippet
```java
            String name = entry.getName();
            if (!name.endsWith(".iwa")) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork13PackageParser.java`
#### Snippet
```java

            // Unknown
            return null;
        }

```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `bstart` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                int maxsize = 8192;     //  8K

                byte bstart =
                        SYN;     // check for SYN [0x16 : ctrl-v] (may have leftover residue from

```

### UnnecessaryLocalVariable
Local variable `bfinish` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

                // preceding message)
                byte bfinish =
                        SOH;     // check for SOH [0x01 : ctrl-a] (typically follows a pair of SYN

```

### UnnecessaryLocalVariable
Local variable `bstart` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                int maxsize = 8192;     //  8K

                byte bstart =
                        SOH;     // check for SOH [0x01 : ctrl-a] (typically follows a pair of SYN

```

### UnnecessaryLocalVariable
Local variable `bfinish` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

                // [0x16 : ctrl-v])
                byte bfinish =
                        STX;     // check for STX [0x02 : ctrl-b] (marks end of header, beginning of

```

### UnnecessaryLocalVariable
Local variable `bstart` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                int maxsize = 524288;     //  512K

                byte bstart =
                        STX;     // check for STX [0x02 : ctrl-b] (marks end of header, beginning of

```

### UnnecessaryLocalVariable
Local variable `bfinish` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

                // message)
                byte bfinish =
                        ETX;     // check for ETX [0x03 : ctrl-c] (marks end of message, beginning of

```

### UnnecessaryLocalVariable
Local variable `bstart` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                int maxsize = 8192;     //  8K

                byte bstart =
                        ETX;     // check for ETX [0x03 : ctrl-c] (marks end of message, beginning of

```

### UnnecessaryLocalVariable
Local variable `bfinish` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

                // footer)
                byte bfinish = EOT;     // check for EOT [0x04 : ctrl-d] (marks end of transmission)

                value = getSection(is, maxsize, bstart, bfinish, true);
```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java
                try {
                    while (true) {
                        Thread.sleep(reportUpdateMillis);
                        report(AsyncStatus.ASYNC_STATUS.STARTED);
                    }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
                    }
                }
                Thread.sleep(serverPulseMillis);
            }
            System.exit(0);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
                while (true) {
                    try {
                        Thread.sleep(500);
                        checkActive();
                    } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
                    }
                }
                Thread.sleep(checkForTimeoutMs);
            }
        } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MarianTranslator.java`
#### Snippet
```java
            while (!clientEndpoint.receivedResponse && elapsed < maxWaitForMarianServerResponse) {
                try {
                    Thread.sleep(pulseCheckMarianServerResponse);
                } catch (InterruptedException e) {
                    // Swallow
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ServerStatusWatcher.java`
#### Snippet
```java

                try {
                    Thread.sleep(100);
                } catch (InterruptedException e) {
                    LOG.warn("status watcher sees interrupted exception");
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ServerStatusWatcher.java`
#### Snippet
```java
                    LOG.warn("Problem writing to status file", e);
                }
                Thread.sleep(100);
                elapsed = Duration.between(started, Instant.now()).toMillis();
            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
                        //swallow
                    }
                    Thread.sleep(100);
                    elapsed = Duration.between(started, Instant.now()).toMillis();
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
                while (process.isAlive() && Files.size(forkedStatusFile) < 12 &&
                        elapsed < tikaServerConfig.getMaxForkedStartupMillis()) {
                    Thread.sleep(50);
                    elapsed = Duration.between(start, Instant.now()).toMillis();
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
                        "Will retry {} times.", consecutiveRestarts, maxBind);
                consecutiveRestarts++;
                Thread.sleep(1000);
                if (consecutiveRestarts > maxBind) {
                    throw e;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
        while (true) {
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-pipes/tika-async-cli/src/main/java/org/apache/tika/async/cli/TikaAsyncCLI.java`
#### Snippet
```java
            while (true) {
                if (processor.checkActive()) {
                    Thread.sleep(500);
                } else {
                    break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-batch/src/main/java/org/apache/tika/batch/StatusReporter.java`
#### Snippet
```java
        try {
            while (true) {
                Thread.sleep(sleepMillis);
                int cnt = getRoughCountConsumed();
                int exceptions = getRoughCountExceptions();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
            //mustRestart hasn't been set to true
            try {
                Thread.sleep(pulseMillis);
            } catch (InterruptedException e) {
                LOG.trace("interrupted exception during sleep");
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
            try {
                while (true) {
                    Thread.sleep(500);
                    if (userInterrupted) {
                        writer.write(String.format(Locale.ENGLISH, "Ave atque vale!%n"));
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        while (countActiveConsumers() > 0) {
            try {
                Thread.sleep(500);
            } catch (InterruptedException e) {
                LOG.warn("Thread interrupted while trying to politelyAwaitTermination");
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
            while (timedOuts.size() == 0) {
                try {
                    Thread.sleep(timeoutCheckPulseMillis);
                } catch (InterruptedException e) {
                    LOG.debug("Thread interrupted exception in TimeoutChecker");
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
                        sleepBeforeRetryMillis);
                try {
                    Thread.sleep(sleepBeforeRetryMillis);
                } catch (InterruptedException e) {
                    throw new RuntimeException("interrupted");
```

## RuleId[id=UseCompareMethod]
### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UShort.java`
#### Snippet
```java
    @Override
    public int compareTo(UShort o) {
        return (value < o.value ? -1 : (value == o.value ? 0 : 1));
    }

```

### UseCompareMethod
Expression can be replaced with 'Short.compare'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UByte.java`
#### Snippet
```java
    @Override
    public int compareTo(UByte o) {
        return (value < o.value ? -1 : (value == o.value ? 0 : 1));
    }

```

### UseCompareMethod
Expression can be replaced with 'Long.compare'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/UInteger.java`
#### Snippet
```java
    @Override
    public int compareTo(UInteger o) {
        return (value < o.value ? -1 : (value == o.value ? 0 : 1));
    }

```

### UseCompareMethod
Expression can be replaced with 'Long.compare'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/unsigned/ULong.java`
#### Snippet
```java
        x += Long.MIN_VALUE;
        y += Long.MIN_VALUE;
        return (x < y) ? -1 : ((x == y) ? 0 : 1);
    }

```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ProduceTypeResourceComparator.java`
#### Snippet
```java

        // final calculation
        return oper1Priority == oper2Priority ? 0 : (oper1Priority > oper2Priority ? -1 : 1);
    }

```

## RuleId[id=RedundantFileCreation]
### RedundantFileCreation
`new File` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/pot/PooledTimeSeriesParser.java`
#### Snippet
```java
            String cmdOutput = computePoT(input);
            try (InputStream ofStream = new FileInputStream(
                    new File(input.getAbsoluteFile() + ".of.txt"))) {
                try (InputStream ogStream = new FileInputStream(
                        new File(input.getAbsoluteFile() + ".hog.txt"))) {
```

### RedundantFileCreation
`new File` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/pot/PooledTimeSeriesParser.java`
#### Snippet
```java
                    new File(input.getAbsoluteFile() + ".of.txt"))) {
                try (InputStream ogStream = new FileInputStream(
                        new File(input.getAbsoluteFile() + ".hog.txt"))) {

                    extractHeaderOutput(ofStream, metadata, "of");
```

### RedundantFileCreation
`new File` is redundant
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java
        Parser parser = new AutoDetectParser();
        LinkContentHandler linkCollector = new LinkContentHandler();
        try (OutputStream output = new FileOutputStream(new File(filename))) {
            ContentHandler handler =
                    new TeeContentHandler(new BodyContentHandler(output), linkCollector);
```

### RedundantFileCreation
`new File` is redundant
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java
        Metadata metadata = new Metadata();
        ParseContext context = new ParseContext();
        try (InputStream stream = new FileInputStream(new File(filename))) {
            parser.parse(stream, handler, metadata, context);
        }
```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`parseState.hitIconOnly == false` can be simplified to '!parseState.hitIconOnly'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/EMFParser.java`
#### Snippet
```java
            String val =
                    tryToReadAsString((((HemfComment.EmfCommentDataGeneric) commentData).getPrivateData()));
            if (ICON_ONLY.equals(val) && parseState.hitIconOnly == false) {
                parseState.hitIconOnly = true;
                parseState.isIconOnly = true;
```

### PointlessBooleanExpression
`inHyperlink == true` can be simplified to 'inHyperlink'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
                        }
                        xhtml.characters(run.getRawText());
                        if (inHyperlink == true) {
                            xhtml.endElement("a");
                        }
```

### PointlessBooleanExpression
`groupState.objdata == true` can be simplified to 'groupState.objdata'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
                    break;
                }
            } else if (groupState.objdata == true || groupState.pictDepth == 1) {
                embObjHandler.writeHexChar(b);
            } else if (b != '\r' && b != '\n' &&
```

### PointlessBooleanExpression
`groupState.sn == true` can be simplified to 'groupState.sn'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
                embObjHandler.writeHexChar(b);
            } else if (b != '\r' && b != '\n' &&
                    (!groupState.ignore || nextMetaData != null || groupState.sn == true ||
                            groupState.sv == true)) {
                // Linefeed and carriage return are not
```

### PointlessBooleanExpression
`groupState.sv == true` can be simplified to 'groupState.sv'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            } else if (b != '\r' && b != '\n' &&
                    (!groupState.ignore || nextMetaData != null || groupState.sn == true ||
                            groupState.sv == true)) {
                // Linefeed and carriage return are not
                // significant
```

### PointlessBooleanExpression
`groupState.objdata == true` can be simplified to 'groupState.objdata'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        ansiSkip = 0;

        if (groupState.objdata == true) {
            try {
                embObjHandler.handleCompletedObject();
```

### PointlessBooleanExpression
`groupState.sn == true` can be simplified to 'groupState.sn'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            groupState.objdata = false;
        } else if (groupState.pictDepth > 0) {
            if (groupState.sn == true) {
                embObjHandler.endSN();
            } else if (groupState.sv == true) {
```

### PointlessBooleanExpression
`groupState.sv == true` can be simplified to 'groupState.sv'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            if (groupState.sn == true) {
                embObjHandler.endSN();
            } else if (groupState.sv == true) {
                embObjHandler.endSV();
            } else if (groupState.sp == true) {
```

### PointlessBooleanExpression
`groupState.sp == true` can be simplified to 'groupState.sp'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            } else if (groupState.sv == true) {
                embObjHandler.endSV();
            } else if (groupState.sp == true) {
                embObjHandler.endSP();
            } else if (groupState.pictDepth == 1) {
```

### PointlessBooleanExpression
`groupState.annotation == true` can be simplified to 'groupState.annotation'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            }
        }
        if (groupState.annotation == true) {
            addOutputChar(SPACE);
        }
```

### PointlessBooleanExpression
`groupState.object == true` can be simplified to 'groupState.object'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
            addOutputChar(SPACE);
        }
        if (groupState.object == true) {
            embObjHandler.setInObject(false);
        }
```

### PointlessBooleanExpression
`groupState.sn == true` can be simplified to 'groupState.sn'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        if (inHeader || fieldState == 1) {
            pendingBuffer.append(ch);
        } else if (groupState.sn == true || groupState.sv == true) {
            embObjHandler.writeMetadataChar(ch);
        } else {
```

### PointlessBooleanExpression
`groupState.sv == true` can be simplified to 'groupState.sv'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        if (inHeader || fieldState == 1) {
            pendingBuffer.append(ch);
        } else if (groupState.sn == true || groupState.sv == true) {
            embObjHandler.writeMetadataChar(ch);
        } else {
```

### PointlessBooleanExpression
`throwOnWriteLimitReached == false` can be simplified to '!throwOnWriteLimitReached'
in `tika-core/src/main/java/org/apache/tika/sax/BasicContentHandlerFactory.java`
#### Snippet
```java
        this.throwOnWriteLimitReached = throwOnWriteLimitReached;
        this.parseContext = parseContext;
        if (throwOnWriteLimitReached == false && parseContext == null) {
            throw new IllegalArgumentException("parse context must not be null if " +
                    "throwOnWriteLimitReached is false");
```

### PointlessBooleanExpression
`HAS_WARNED_STAX.getAndSet(true) == false` can be simplified to '!HAS_WARNED_STAX.getAndSet(true)'
in `tika-core/src/main/java/org/apache/tika/utils/XMLReaderUtils.java`
#### Snippet
```java
                inputFactory.setProperty("com.ctc.wstx.maxEntityCount", MAX_ENTITY_EXPANSIONS);
            } catch (IllegalArgumentException e2) {
                if (HAS_WARNED_STAX.getAndSet(true) == false) {
                    LOG.warn("Could not set limit on maximum entity expansions for: " + inputFactory.getClass());
                }
```

### PointlessBooleanExpression
`allowStoredEntries == false` can be simplified to '!allowStoredEntries'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/zip/utils/ZipSalvager.java`
#### Snippet
```java
            } catch (UnsupportedZipFeatureException e) {
                //now retry
                if (allowStoredEntries == false &&
                        e.getFeature() == UnsupportedZipFeatureException.Feature.DATA_DESCRIPTOR) {
                    ((RereadableInputStream) brokenZip).rewind();
```

### PointlessBooleanExpression
`id3v1.found == false` can be simplified to '!id3v1.found'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/LyricsHandler.java`
#### Snippet
```java

    public boolean hasID3v1() {
        if (id3v1 == null || id3v1.found == false) {
            return false;
        }
```

### PointlessBooleanExpression
`allowStoredEntries == false` can be simplified to '!allowStoredEntries'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
            }
        } catch (UnsupportedZipFeatureException zfe) {
            if (allowStoredEntries == false &&
                    zfe.getFeature() == UnsupportedZipFeatureException.Feature.DATA_DESCRIPTOR) {
                input.reset();
```

### PointlessBooleanExpression
`this.shuttingDown == true` can be simplified to 'this.shuttingDown'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ServerStatusWatcher.java`
#### Snippet
```java

    private synchronized void writeStatus(boolean shuttingDown) throws InterruptedException {
        if (this.shuttingDown == true) {
            return;
        }
```

### PointlessBooleanExpression
`shuttingDown == true` can be simplified to 'shuttingDown'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ServerStatusWatcher.java`
#### Snippet
```java
            return;
        }
        if (shuttingDown == true) {
            this.shuttingDown = true;
        }
```

### PointlessBooleanExpression
`success == false` can be simplified to '!success'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/MetadataResource.java`
#### Snippet
```java
        }

        if (success == false || metadata.get(field) == null) {
            return Response.status(defaultErrorResponse)
                    .entity("Failed to get metadata field " + field).build();
```

### PointlessBooleanExpression
`config.isExtractInlineImages() == false` can be simplified to '!config.isExtractInlineImages()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDF2XHTML.java`
#### Snippet
```java

    void extractImages(PDPage page) throws SAXException, IOException {
        if (config.isExtractInlineImages() == false &&
                config.isExtractInlineImageMetadataOnly() == false) {
            return;
```

### PointlessBooleanExpression
`config.isExtractInlineImageMetadataOnly() == false` can be simplified to '!config.isExtractInlineImageMetadataOnly()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDF2XHTML.java`
#### Snippet
```java
    void extractImages(PDPage page) throws SAXException, IOException {
        if (config.isExtractInlineImages() == false &&
                config.isExtractInlineImageMetadataOnly() == false) {
            return;
        }
```

### PointlessBooleanExpression
`config.isExtractAcroFormContent() == true` can be simplified to 'config.isExtractAcroFormContent()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java

            //extract acroform data at end of doc
            if (config.isExtractAcroFormContent() == true) {
                try {
                    extractAcroForm(pdf);
```

### PointlessBooleanExpression
`contentTags.getParseException() == false` can be simplified to '!contentTags.getParseException()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    void writeTagData(String fileId, ContentTags contentTags, TableInfo tagsTable) {
        Map<String, Integer> tags = contentTags.getTags();
        if (tags.size() == 0 && contentTags.getParseException() == false) {
            return;
        }
```

### PointlessBooleanExpression
`queue.offer(fileResource, PAUSE_INCREMENT_MILLIS, TimeUnit.MILLISECONDS) == fals...` can be simplified to '!queue.offer(fileResource, PAUSE_INCREMENT_MILLIS, TimeUnit.MILLISECONDS)'
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
        if (select(fileResource.getMetadata())) {
            long start = System.currentTimeMillis();
            while (queue.offer(fileResource, PAUSE_INCREMENT_MILLIS, TimeUnit.MILLISECONDS) ==
                    false) {
                long elapsed = System.currentTimeMillis() - start;
                LOG.info("FileResourceCrawler is pausing. Queue is full: {} after {} ms",
```

### PointlessBooleanExpression
`it.done == false` can be simplified to '!it.done'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            }

            return (it.done == false);
        }

```

### PointlessBooleanExpression
`it.done == false` can be simplified to '!it.done'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            }

            return (it.done == false);
        }

```

### PointlessBooleanExpression
`transformed == false` can be simplified to '!transformed'
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
                COSName filter = (COSName) filterArray.get(i);
                is = runFilter(filter, is, new COSDictionary(), 0);
                if (random.nextFloat() > 0.1 && transformed == false) {
                    is = transformRawStream(is);
                    transformed = true;
```

### PointlessBooleanExpression
`icnstype.hasMask() == true` can be simplified to 'icnstype.hasMask()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSParser.java`
#### Snippet
```java
                //No more icons left
                break;
            } else if (icnstype.hasMask() == true) {
                icon_masks.add(findIconType(tempByteArray));
            } else {
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `tika-core/src/main/java/org/apache/tika/metadata/filter/FieldNameMappingFilter.java`
#### Snippet
```java
    public void setMappings(Map<String, String> mappings) {
        for (Map.Entry<String, String> e : mappings.entrySet()) {
            this.mappings.put(e.getKey(), e.getValue());
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        if (names != null) {
            for (Map.Entry<String, PDComplexFileSpecification> e : names.entrySet()) {
                embeddedFileNames.put(e.getKey(), e.getValue());
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `tika-batch/src/main/java/org/apache/tika/util/XMLDOMUtil.java`
#### Snippet
```java
        if (overwrite != null) {
            for (Map.Entry<String, String> e : overwrite.entrySet()) {
                map.put(e.getKey(), e.getValue());
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `tika-batch/src/main/java/org/apache/tika/batch/builders/BatchProcessBuilder.java`
#### Snippet
```java

        for (Map.Entry<String, String> e : incomingRuntimeAttributes.entrySet()) {
            runtimeAttributes.put(e.getKey(), e.getValue());
        }

```

## RuleId[id=OverwrittenKey]
### OverwrittenKey
Duplicate Map key
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java

        ANSICPG_MAP.put(709, WINDOWS_709);  // Arabic (ASMO 449+, BCON V4)
        ANSICPG_MAP.put(710, WINDOWS_710);  // Arabic (transparent Arabic)
        ANSICPG_MAP.put(710, WINDOWS_711);  // Arabic (Nafitha Enhanced)
        ANSICPG_MAP.put(710, WINDOWS_720);  // Arabic (transparent ASMO)
```

### OverwrittenKey
Duplicate Map key
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        ANSICPG_MAP.put(709, WINDOWS_709);  // Arabic (ASMO 449+, BCON V4)
        ANSICPG_MAP.put(710, WINDOWS_710);  // Arabic (transparent Arabic)
        ANSICPG_MAP.put(710, WINDOWS_711);  // Arabic (Nafitha Enhanced)
        ANSICPG_MAP.put(710, WINDOWS_720);  // Arabic (transparent ASMO)
        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)
```

### OverwrittenKey
Duplicate Map key
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        ANSICPG_MAP.put(710, WINDOWS_710);  // Arabic (transparent Arabic)
        ANSICPG_MAP.put(710, WINDOWS_711);  // Arabic (Nafitha Enhanced)
        ANSICPG_MAP.put(710, WINDOWS_720);  // Arabic (transparent ASMO)
        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)
        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)
```

### OverwrittenKey
Duplicate Map key
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        ANSICPG_MAP.put(710, WINDOWS_711);  // Arabic (Nafitha Enhanced)
        ANSICPG_MAP.put(710, WINDOWS_720);  // Arabic (transparent ASMO)
        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)
        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)

```

### OverwrittenKey
Duplicate Map key
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        ANSICPG_MAP.put(710, WINDOWS_720);  // Arabic (transparent ASMO)
        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)
        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)

        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)
```

### OverwrittenKey
Duplicate Map key
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)

        ANSICPG_MAP.put(819, CP819);  // Windows 3.1 (US & Western Europe)
        ANSICPG_MAP.put(850, CP8502);  // IBM Multilingual
        ANSICPG_MAP.put(852, CP852);  // Eastern European
```

## RuleId[id=EnumSwitchStatementWhichMissesCases]
### EnumSwitchStatementWhichMissesCases
`switch (content.getType()) { case LINK: xhtml.characters(((OleBlob.LinkC...` statement on enum type 'com.healthmarketscience.jackcess.util.OleBlob.ContentType' misses case 'UNKNOWN'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
switch (content.getType()) {
            case LINK:
                xhtml.characters(((OleBlob.LinkContent) content).getLinkPath());
                break;
            case SIMPLE_PACKAGE:
                OleBlob.SimplePackageContent spc = (OleBlob.SimplePackageContent) content;
                //TODO: find test file that has this kind of attachment
                //and see if getFilePath or getLocalFilePath is meaningful
                //for TikaCoreProperties.ORIGINAL_RESOURCE_NAME
                TikaInputStream tis = null;
                try {
                    tis = TikaInputStream.get(spc.getStream());
                } catch (IOException e) {
                    EmbeddedDocumentUtil.recordEmbeddedStreamException(e, parentMetadata);
                    break;
                }
                if (tis != null) {
                    try {
                        handleEmbeddedResource(tis, spc.getFileName(),//filename
                                null,//relationshipId
                                spc.getTypeName(),//mediatype
                                xhtml, false);
                    } finally {
                        IOUtils.closeQuietly(tis);
                    }
                }
                break;
            case OTHER:
                OleBlob.OtherContent oc = (OleBlob.OtherContent) content;
                TikaInputStream ocStream = null;
                try {
                    ocStream = TikaInputStream.get(oc.getStream());
                } catch (IOException e) {
                    EmbeddedDocumentUtil.recordException(e, parentMetadata);
                }
                try {
                    handleEmbeddedResource(ocStream, null,//filename
                            null,//relationshipId
                            oc.getTypeName(),//mediatype
                            xhtml, false);
                } finally {
                    IOUtils.closeQuietly(ocStream);
                }
                break;
            case COMPOUND_STORAGE:
                OleBlob.CompoundContent cc = (OleBlob.CompoundContent) content;
                handleCompoundContent(cc, xhtml);
                break;
        }
```

### EnumSwitchStatementWhichMissesCases
`switch (compression) { case BZIP2: os = new BZip2CompressorOutputStream(...` statement on enum type 'org.apache.tika.batch.fs.FSOutputStreamFactory.COMPRESSION' misses case 'NONE'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSOutputStreamFactory.java`
#### Snippet
```java

        OutputStream os = Files.newOutputStream(outputPath);
        switch (compression) {
            case BZIP2:
                os = new BZip2CompressorOutputStream(os);
                break;
            case GZIP:
                os = new GZIPOutputStream(os);
                break;
            case ZIP:
                os = new ZipArchiveOutputStream(os);
                break;
        }
        return new BufferedOutputStream(os);
    }
```

### EnumSwitchStatementWhichMissesCases
`switch (type) { case SIMPLE: cre...` statement on enum type 'org.apache.tika.metadata.Property.PropertyType' misses cases: 'STRUCTURE', and 'COMPOSITE'
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/GenericConverter.java`
#### Snippet
```java
                        PropertyType type = Property.getPropertyType(key);
                        if (type != null) {
                            switch (type) {
                                case SIMPLE:
                                    createProperty(key, uri, keyParts[1]);
                                    break;
                                case BAG:
                                    createArrayProperty(key, uri, keyParts[1],
                                            PropertyOptions.ARRAY);
                                    break;
                                case SEQ:
                                    createArrayProperty(key, uri, keyParts[1],
                                            PropertyOptions.ARRAY_ORDERED);
                                    break;
                                case ALT:
                                    createArrayProperty(key, uri, keyParts[1],
                                            PropertyOptions.ARRAY_ALTERNATE);
                                    break;
                                // TODO Add support for structs and lang-alts, but those types are
                                // currently not used in Tika
                            }
                        }
                    }
```

### EnumSwitchStatementWhichMissesCases
`switch (type) { case BAG: result = PropertyOptions.ARRAY; ...` statement on enum type 'org.apache.tika.metadata.Property.PropertyType' misses cases: 'SIMPLE', 'STRUCTURE', and 'COMPOSITE'
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
    private int tikaToXMPArrayType(PropertyType type) {
        int result = 0;
        switch (type) {
            case BAG:
                result = PropertyOptions.ARRAY;
                break;
            case SEQ:
                result = PropertyOptions.ARRAY_ORDERED;
                break;
            case ALT:
                result = PropertyOptions.ARRAY_ALTERNATE;
                break;
        }
        return result;
    }
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CJKBigramAwareLengthFilterFactory.java`
#### Snippet
```java
            if (termAtt.length() < min) {
                String type = typeAtt.type();
                if (type == CJKBigramFilter.DOUBLE_TYPE || type == CJKBigramFilter.SINGLE_TYPE) {
                    return true;
                }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CJKBigramAwareLengthFilterFactory.java`
#### Snippet
```java
            if (termAtt.length() < min) {
                String type = typeAtt.type();
                if (type == CJKBigramFilter.DOUBLE_TYPE || type == CJKBigramFilter.SINGLE_TYPE) {
                    return true;
                }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/URLEmailNormalizingFilterFactory.java`
#### Snippet
```java
            }
            //== is actually substantially faster than .equals(String)
            if (typeAtt.type() == UAX29URLEmailTokenizer.TOKEN_TYPES[UAX29URLEmailTokenizer.URL]) {
                termAtt.copyBuffer(URL_CHARS, 0, URL_CHARS.length);
                termAtt.setLength(URL_CHARS.length);
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/URLEmailNormalizingFilterFactory.java`
#### Snippet
```java
                termAtt.copyBuffer(URL_CHARS, 0, URL_CHARS.length);
                termAtt.setLength(URL_CHARS.length);
            } else if (typeAtt.type() ==
                    UAX29URLEmailTokenizer.TOKEN_TYPES[UAX29URLEmailTokenizer.EMAIL]) {
                termAtt.copyBuffer(EMAIL_CHARS, 0, EMAIL_CHARS.length);
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AlphaIdeographFilterFactory.java`
#### Snippet
```java
        protected boolean accept() throws IOException {
            String type = typeAtt.type();
            if (type == UAX29URLEmailTokenizer.TOKEN_TYPES[UAX29URLEmailTokenizer.EMOJI] ||
                    type == UAX29URLEmailTokenizer.TOKEN_TYPES[UAX29URLEmailTokenizer.NUM]) {
                return false;
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AlphaIdeographFilterFactory.java`
#### Snippet
```java
            String type = typeAtt.type();
            if (type == UAX29URLEmailTokenizer.TOKEN_TYPES[UAX29URLEmailTokenizer.EMOJI] ||
                    type == UAX29URLEmailTokenizer.TOKEN_TYPES[UAX29URLEmailTokenizer.NUM]) {
                return false;
            }
```

## RuleId[id=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFFileHeader.java`
#### Snippet
```java
        List<DBFColumnHeader> headers = new LinkedList<>();
        int bytesAccountedFor = 0;
        while (true) {
            DBFColumnHeader colHeader = readCol(is);
            bytesAccountedFor += colHeader.fieldLength;
```

### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/hwp/HwpTextExtractorV5.java`
#### Snippet
```java
        TagInfo tag = new TagInfo();

        while (true) {
            if (!readTag(reader, tag)) {
                break;
```

## RuleId[id=EmptyTryBlock]
### EmptyTryBlock
Empty `try` block
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/H2Util.java`
#### Snippet
```java
    public static boolean databaseExists(Path db) {
        String connString = getConnectionString(db, false);
        try (Connection conn = DriverManager.getConnection(connString)) {
        } catch (SQLException e) {
            return false;
```

## RuleId[id=LoopStatementsThatDontLoop]
### LoopStatementsThatDontLoop
`while` statement does not loop
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
            output.writeByte(ForkServer.PING);
            output.flush();
            while (true) {
                int type = input.read();
                return type == ForkServer.PING;
```

### LoopStatementsThatDontLoop
`while` statement does not loop
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        int read = 0;

        while (read < value.length) {

            // pull apart the envelope, getting the service id  (....\x1f)
```

## RuleId[id=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `buildComposite()` declared in class 'org.apache.tika.config.ConfigBase' but referenced via subclass 'org.apache.tika.pipes.emitter.EmitterManager'
in `tika-core/src/main/java/org/apache/tika/pipes/emitter/EmitterManager.java`
#### Snippet
```java
    public static EmitterManager load(Path tikaConfigPath) throws IOException, TikaConfigException {
        try (InputStream is = Files.newInputStream(tikaConfigPath) ) {
            return EmitterManager.buildComposite(
                    "emitters", EmitterManager.class,
                    "emitter",
```

### StaticCallOnSubclass
Static method `buildComposite()` declared in class 'org.apache.tika.config.ConfigBase' but referenced via subclass 'org.apache.tika.pipes.fetcher.FetcherManager'
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/FetcherManager.java`
#### Snippet
```java
        try (InputStream is =
                     Files.newInputStream(p)) {
            return FetcherManager.buildComposite("fetchers", FetcherManager.class,
                    "fetcher", Fetcher.class, is);
        }
```

### StaticCallOnSubclass
Static method `getInstance()` declared in class 'java.util.Calendar' but referenced via subclass 'java.util.GregorianCalendar'
in `tika-core/src/main/java/org/apache/tika/utils/DateUtils.java`
#### Snippet
```java
     */
    public static String formatDate(Date date) {
        Calendar calendar = GregorianCalendar.getInstance(UTC, Locale.US);
        calendar.setTime(date);
        return doFormatDate(calendar);
```

### StaticCallOnSubclass
Static method `getInstance()` declared in class 'java.util.Calendar' but referenced via subclass 'java.util.GregorianCalendar'
in `tika-core/src/main/java/org/apache/tika/utils/DateUtils.java`
#### Snippet
```java
    public static String formatDateUnknownTimezone(Date date) {
        // Create the Calendar object in the system timezone
        Calendar calendar = GregorianCalendar.getInstance(TimeZone.getDefault(), Locale.US);
        calendar.setTime(date);
        // Have it formatted
```

### StaticCallOnSubclass
Static method `buildSingle()` declared in class 'org.apache.tika.config.ConfigBase' but referenced via subclass 'org.apache.tika.parser.AutoDetectParserConfig'
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserConfig.java`
#### Snippet
```java
    public static AutoDetectParserConfig load(Element element)
            throws TikaConfigException, IOException {
        return AutoDetectParserConfig.buildSingle("autoDetectParserConfig",
                AutoDetectParserConfig.class, element, AutoDetectParserConfig.DEFAULT);
    }
```

### StaticCallOnSubclass
Static method `getInstance()` declared in class 'java.util.Calendar' but referenced via subclass 'java.util.GregorianCalendar'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFFileHeader.java`
#### Snippet
```java
        int lastModMonth = is.read();
        int lastModDay = is.read();
        Calendar now = GregorianCalendar.getInstance(TimeZone.getTimeZone("UTC"), Locale.ROOT);

        //if this was last modified after the current year, assume
```

### StaticCallOnSubclass
Static method `getInstance()` declared in class 'java.util.Calendar' but referenced via subclass 'java.util.GregorianCalendar'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFCell.java`
#### Snippet
```java
        //TODO: find example of file with time != 0
        Calendar baseCalendar =
                GregorianCalendar.getInstance(TimeZone.getTimeZone("UTC"), Locale.ROOT);
//        baseCalendar.set(1899, 11, 31, 0, 0, 0);
        baseCalendar.set(-4712, 0, 1, 0, 0, 0);
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'javax.xml.transform.TransformerFactory' but referenced via subclass 'javax.xml.transform.sax.SAXTransformerFactory'
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                                                            String encoding, boolean prettyPrint)
            throws TransformerConfigurationException {
        SAXTransformerFactory factory = (SAXTransformerFactory) SAXTransformerFactory.newInstance();
        TransformerHandler handler = factory.newTransformerHandler();
        handler.getTransformer().setOutputProperty(OutputKeys.METHOD, method);
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'javax.xml.transform.TransformerFactory' but referenced via subclass 'javax.xml.transform.sax.SAXTransformerFactory'
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
    private ContentHandler getXmlContentHandler(Writer writer)
            throws TransformerConfigurationException {
        SAXTransformerFactory factory = (SAXTransformerFactory) SAXTransformerFactory.newInstance();
        TransformerHandler handler = factory.newTransformerHandler();
        handler.getTransformer().setOutputProperty(OutputKeys.METHOD, "xml");
```

### StaticCallOnSubclass
Static method `setClientId()` declared in class 'com.memetix.mst.MicrosoftTranslatorAPI' but referenced via subclass 'com.memetix.mst.translate.Translate'
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MicrosoftTranslator.java`
#### Snippet
```java
        }
        Language target = Language.fromString(targetLanguage);
        Translate.setClientId(clientId);
        Translate.setClientSecret(clientSecret);
        try {
```

### StaticCallOnSubclass
Static method `setClientSecret()` declared in class 'com.memetix.mst.MicrosoftTranslatorAPI' but referenced via subclass 'com.memetix.mst.translate.Translate'
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MicrosoftTranslator.java`
#### Snippet
```java
        Language target = Language.fromString(targetLanguage);
        Translate.setClientId(clientId);
        Translate.setClientSecret(clientSecret);
        try {
            return Translate.execute(text, target);
```

### StaticCallOnSubclass
Static method `setClientId()` declared in class 'com.memetix.mst.MicrosoftTranslatorAPI' but referenced via subclass 'com.memetix.mst.translate.Translate'
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MicrosoftTranslator.java`
#### Snippet
```java
        Language source = Language.fromString(sourceLanguage);
        Language target = Language.fromString(targetLanguage);
        Translate.setClientId(clientId);
        Translate.setClientSecret(clientSecret);
        try {
```

### StaticCallOnSubclass
Static method `setClientSecret()` declared in class 'com.memetix.mst.MicrosoftTranslatorAPI' but referenced via subclass 'com.memetix.mst.translate.Translate'
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MicrosoftTranslator.java`
#### Snippet
```java
        Language target = Language.fromString(targetLanguage);
        Translate.setClientId(clientId);
        Translate.setClientSecret(clientSecret);
        try {
            return Translate.execute(text, source, target);
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'javax.xml.transform.TransformerFactory' but referenced via subclass 'javax.xml.transform.sax.SAXTransformerFactory'
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
     */
    private ContentHandler getHtmlHandler(Writer writer) throws TransformerConfigurationException {
        SAXTransformerFactory factory = (SAXTransformerFactory) SAXTransformerFactory.newInstance();
        TransformerHandler handler = factory.newTransformerHandler();
        handler.getTransformer().setOutputProperty(OutputKeys.METHOD, "html");
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'javax.xml.transform.TransformerFactory' but referenced via subclass 'javax.xml.transform.sax.SAXTransformerFactory'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
            try {
                SAXTransformerFactory factory =
                        (SAXTransformerFactory) SAXTransformerFactory.newInstance();
                TransformerHandler handler = factory.newTransformerHandler();
                handler.getTransformer().setOutputProperty(OutputKeys.METHOD, format);
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURISchemeUtil.java`
#### Snippet
```java
    private static Pattern PARSE_PATTERN = Pattern.compile("(?s)data:([^,]*?)(base64)?,(.*)$");
    private static Pattern EXTRACT_PATTERN =
            Pattern.compile("(?s)data:([^,]*?)(base64)?,([^\"\']*)[\"\']");
    private final Matcher parseMatcher = PARSE_PATTERN.matcher("");
    private final Matcher extractMatcher = EXTRACT_PATTERN.matcher("");
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURISchemeUtil.java`
#### Snippet
```java
    private static Pattern PARSE_PATTERN = Pattern.compile("(?s)data:([^,]*?)(base64)?,(.*)$");
    private static Pattern EXTRACT_PATTERN =
            Pattern.compile("(?s)data:([^,]*?)(base64)?,([^\"\']*)[\"\']");
    private final Matcher parseMatcher = PARSE_PATTERN.matcher("");
    private final Matcher extractMatcher = EXTRACT_PATTERN.matcher("");
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/captioning/CaptionObject.java`
#### Snippet
```java
    @Override
    public String toString() {
        return "Caption{" + "sentence='" + label + "\' (" + labelLang + ')' + ", confidence=" +
                confidence + '}';
    }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/RecognisedObject.java`
#### Snippet
```java
    @Override
    public String toString() {
        return "RecognisedObject{" + "label='" + label + "\' (" + labelLang + ')' + ", id='" + id +
                '\'' + ", confidence=" + confidence + '}';
    }
```

## RuleId[id=JavaLangInvokeHandleSignature]
### JavaLangInvokeHandleSignature
Cannot resolve method 'invokeCleaner'
in `tika-core/src/main/java/org/apache/tika/io/MappedBufferCleaner.java`
#### Snippet
```java
                // first check if Unsafe has the right method, otherwise we can give up
                // without doing any security critical stuff:
                final MethodHandle unmapper = lookup.findVirtual(unsafeClass, "invokeCleaner",
                        methodType(void.class, ByteBuffer.class));
                // fetch the unsafe instance and bind it to the virtual MH:
```

## RuleId[id=ObsoleteCollection]
### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    //    if its match doesn't pop off the stack...or should
    //    we pop all at the first failure.
    private Stack<String> paragraphStack = new Stack<>();
    //this is an arbitrary limit on the size of the stack
    //to defend against DoS with memory consumption
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    //    if its match doesn't pop off the stack...or should
    //    we pop all at the first failure.
    private Stack<String> paragraphStack = new Stack<>();
    //this is an arbitrary limit on the size of the stack
    //to defend against DoS with memory consumption
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
    private Map<String, List<GlyphRun>> canvases = new LinkedHashMap<>();
    private Set<String> urls = new LinkedHashSet();
    private Stack<String> canvasStack = new Stack<>();

    public XPSPageContentHandler(XHTMLContentHandler xhtml, Map<String, Metadata> embeddedInfos) {
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
    private Map<String, List<GlyphRun>> canvases = new LinkedHashMap<>();
    private Set<String> urls = new LinkedHashSet();
    private Stack<String> canvasStack = new Stack<>();

    public XPSPageContentHandler(XHTMLContentHandler xhtml, Map<String, Metadata> embeddedInfos) {
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-core/src/main/java/org/apache/tika/sax/DIFContentHandler.java`
#### Snippet
```java
    private static final Attributes EMPTY_ATTRIBUTES = new AttributesImpl();

    private final Stack<String> treeStack;
    private final Stack<String> dataStack;
    private final ContentHandler delegate;
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-core/src/main/java/org/apache/tika/sax/DIFContentHandler.java`
#### Snippet
```java
        this.isLeaf = false;
        this.metadata = metadata;
        this.treeStack = new Stack<>();
        this.dataStack = new Stack<>();
    }
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-core/src/main/java/org/apache/tika/sax/DIFContentHandler.java`
#### Snippet
```java
        this.metadata = metadata;
        this.treeStack = new Stack<>();
        this.dataStack = new Stack<>();
    }

```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-core/src/main/java/org/apache/tika/sax/DIFContentHandler.java`
#### Snippet
```java

    private final Stack<String> treeStack;
    private final Stack<String> dataStack;
    private final ContentHandler delegate;
    private final Metadata metadata;
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-core/src/main/java/org/apache/tika/sax/DIFContentHandler.java`
#### Snippet
```java
        }
        if (this.isLeaf) {
            Stack<String> tempStack = (Stack<String>) this.treeStack.clone();
            StringBuilder key = new StringBuilder();
            while (!tempStack.isEmpty()) {
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
    //keeps track of multipart/alternative and its children
    private Stack<Part> alternativePartBuffer = new Stack<>();
    private Stack<BodyDescriptor> parts = new Stack<>();
    MailContentHandler(XHTMLContentHandler xhtml, Detector detector, Metadata metadata,
                       ParseContext context, boolean strictParsing,
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
    //keeps track of multipart/alternative and its children
    private Stack<Part> alternativePartBuffer = new Stack<>();
    private Stack<BodyDescriptor> parts = new Stack<>();
    MailContentHandler(XHTMLContentHandler xhtml, Detector detector, Metadata metadata,
                       ParseContext context, boolean strictParsing,
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
    //this is used to buffer a multipart body that
    //keeps track of multipart/alternative and its children
    private Stack<Part> alternativePartBuffer = new Stack<>();
    private Stack<BodyDescriptor> parts = new Stack<>();
    MailContentHandler(XHTMLContentHandler xhtml, Detector detector, Metadata metadata,
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
    //this is used to buffer a multipart body that
    //keeps track of multipart/alternative and its children
    private Stack<Part> alternativePartBuffer = new Stack<>();
    private Stack<BodyDescriptor> parts = new Stack<>();
    MailContentHandler(XHTMLContentHandler xhtml, Detector detector, Metadata metadata,
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dif/DIFContentHandler.java`
#### Snippet
```java
    private static final Attributes EMPTY_ATTRIBUTES = new AttributesImpl();
    private final ContentHandler delegate;
    private Stack<String> treeStack;
    private Stack<String> dataStack;
    private boolean isLeaf;
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dif/DIFContentHandler.java`
#### Snippet
```java
    private final ContentHandler delegate;
    private Stack<String> treeStack;
    private Stack<String> dataStack;
    private boolean isLeaf;
    private Metadata metadata;
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dif/DIFContentHandler.java`
#### Snippet
```java
        }
        if (this.isLeaf) {
            Stack<String> tempStack = (Stack<String>) this.treeStack.clone();
            StringBuilder key = new StringBuilder();
            while (!tempStack.isEmpty()) {
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dif/DIFContentHandler.java`
#### Snippet
```java
        this.isLeaf = false;
        this.metadata = metadata;
        this.treeStack = new Stack<>();
        this.dataStack = new Stack<>();
    }
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dif/DIFContentHandler.java`
#### Snippet
```java
        this.metadata = metadata;
        this.treeStack = new Stack<>();
        this.dataStack = new Stack<>();
    }

```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFExtractor.java`
#### Snippet
```java
        String line;
        Tag currentTag = new Tag();
        Stack<Tag> parents = new Stack<>();
        while ((line = reader.readLine()) != null) {
            if (line.contains(OPEN_TAG_MARKER) && !line.contains(CLOSE_TAG_MARKER)) {
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFExtractor.java`
#### Snippet
```java
        String line;
        Tag currentTag = new Tag();
        Stack<Tag> parents = new Stack<>();
        while ((line = reader.readLine()) != null) {
            if (line.contains(OPEN_TAG_MARKER) && !line.contains(CLOSE_TAG_MARKER)) {
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    private TextStyle currTextStyle; //this is the text style for particular spans/paragraphs
    private String currTextStyleName;
    private Stack<ListStyle> listStyleStack = new Stack<>();
    private ListStyle listStyle;
    // True if we are currently in the named style:
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    private TextStyle currTextStyle; //this is the text style for particular spans/paragraphs
    private String currTextStyleName;
    private Stack<ListStyle> listStyleStack = new Stack<>();
    private ListStyle listStyle;
    // True if we are currently in the named style:
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    private int nodeDepth = 0;
    private int completelyFiltered = 0;
    private Stack<String> headingStack = new Stack<>();
    private Map<String, TextStyle> paragraphTextStyleMap = new HashMap<>();
    private Map<String, TextStyle> textStyleMap = new HashMap<>();
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    private int nodeDepth = 0;
    private int completelyFiltered = 0;
    private Stack<String> headingStack = new Stack<>();
    private Map<String, TextStyle> paragraphTextStyleMap = new HashMap<>();
    private Map<String, TextStyle> textStyleMap = new HashMap<>();
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-package/src/main/java/org/apache/tika/parser/internal/Activator.java`
#### Snippet
```java
    public void start(BundleContext context) throws Exception {
        detectorService = context.registerService(Detector.class.getName(),
                new DefaultDetector(Activator.class.getClassLoader()), new Hashtable<>());
        Parser parser = new DefaultParser(Activator.class.getClassLoader());
        parserService = context.registerService(Parser.class.getName(), parser, new Hashtable<>());
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-package/src/main/java/org/apache/tika/parser/internal/Activator.java`
#### Snippet
```java
                new DefaultDetector(Activator.class.getClassLoader()), new Hashtable<>());
        Parser parser = new DefaultParser(Activator.class.getClassLoader());
        parserService = context.registerService(Parser.class.getName(), parser, new Hashtable<>());
    }

```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    //so that null entries will not get added.
    @SuppressWarnings({"squid:S1149"})
    private final Map<COSBase, COSObjectKey> objectKeys = new Hashtable<>();
    private final Map<COSObjectKey, COSBase> keyObject = new HashMap<>();
    // the list of x ref entries to be made so far
```

## RuleId[id=LongLiteralsEndingWithLowercaseL]
### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonFetchEmitTuple.java`
#### Snippet
```java
        String emitterName = null;
        String emitKey = null;
        long fetchRangeStart = -1l;
        long fetchRangeEnd = -1l;

```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonFetchEmitTuple.java`
#### Snippet
```java
        String emitKey = null;
        long fetchRangeStart = -1l;
        long fetchRangeEnd = -1l;

        FetchEmitTuple.ON_PARSE_EXCEPTION onParseException =
```

### LongLiteralsEndingWithLowercaseL
'long' literal `0x00FFFFFFFFl` ends with lowercase 'l'
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
    public static long getUIntLE(byte[] data, int offset) {
        long retNum = getIntLE(data, offset);
        return retNum & 0x00FFFFFFFFl;
    }

```

### LongLiteralsEndingWithLowercaseL
'long' literal `0x00FFFFFFFFl` ends with lowercase 'l'
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
            throw new BufferUnderrunException();
        }
        return ((ch4 << 24) + (ch3 << 16) + (ch2 << 8) + (ch1)) & 0x00FFFFFFFFl;
    }

```

### LongLiteralsEndingWithLowercaseL
'long' literal `0x00FFFFFFFFl` ends with lowercase 'l'
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
    public static long getUIntBE(byte[] data, int offset) {
        long retNum = getIntBE(data, offset);
        return retNum & 0x00FFFFFFFFl;
    }

```

### LongLiteralsEndingWithLowercaseL
'long' literal `0x00FFFFFFFFl` ends with lowercase 'l'
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
            throw new BufferUnderrunException();
        }
        return ((ch1 << 24) + (ch2 << 16) + (ch3 << 8) + (ch4)) & 0x00FFFFFFFFl;
    }

```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParser.java`
#### Snippet
```java
        }
        if (metadata.get(Metadata.CONTENT_LENGTH) != null) {
            long len = -1l;
            try {
                len = Long.parseLong(metadata.get(Metadata.CONTENT_LENGTH));
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaClientCLI.java`
#### Snippet
```java
                    //potentially blocks forever
                    queue.put(PipesIterator.COMPLETED_SEMAPHORE);
                    return 1l;
                }
                try {
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-jdbc/src/main/java/org/apache/tika/pipes/pipesiterator/jdbc/JDBCPipesIterator.java`
#### Snippet
```java
        Metadata metadata = new Metadata();
        String fetchKey = "";
        long fetchStartRange = -1l;
        long fetchEndRange = -1l;
        String emitKey = "";
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-jdbc/src/main/java/org/apache/tika/pipes/pipesiterator/jdbc/JDBCPipesIterator.java`
#### Snippet
```java
        String fetchKey = "";
        long fetchStartRange = -1l;
        long fetchEndRange = -1l;
        String emitKey = "";
        String id = "";
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-jdbc/src/main/java/org/apache/tika/pipes/pipesiterator/jdbc/JDBCPipesIterator.java`
#### Snippet
```java
        long val = rs.getLong(i);
        if (rs.wasNull()) {
            return -1l;
        }
        return val;
```

### LongLiteralsEndingWithLowercaseL
'long' literal `0xa00000l` ends with lowercase 'l'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGParser.java`
#### Snippet
```java
        // and the offset isn't available at 0x20. Until we can work out how
        // to find the offset in those files, skip them if detected
        if (offsetToSection > 0xa00000l) {
            // Header should never be more than 10mb into the file, something is wrong
            offsetToSection = 0;
```

### LongLiteralsEndingWithLowercaseL
'long' literal `32128791892482l` ends with lowercase 'l'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/executable/ExecutableParser.java`
#### Snippet
```java
     * Serial version UID
     */
    private static final long serialVersionUID = 32128791892482l;

    private static final MediaType PE_EXE = MediaType.application("x-msdownload");
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1000l` ends with lowercase 'l'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/executable/ExecutableParser.java`
#### Snippet
```java

        // Turn this into helpful metadata
        Date createdAtD = new Date(createdAt * 1000l);
        metadata.set(TikaCoreProperties.CREATED, createdAtD);

```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    public static final String FALSE = Boolean.toString(false);
    protected static final AtomicInteger ID = new AtomicInteger();
    static final long NON_EXISTENT_FILE_LENGTH = -1l;
    final static int FILE_PATH_MAX_LEN = 1024;//max len for varchar for file_path
    private static final Logger LOG = LoggerFactory.getLogger(AbstractProfiler.class);
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
        Path extractFile = findFile(extracts, relativeSourceFilePath);
        Path inputFile = srcDir.resolve(relativeSourceFilePath);
        long srcLen = -1l;
        //try to get the length of the source file in case there was an error
        //in both extracts
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
                    "Please make sure you know what you are doing.";
    private static final List<String> ONLY_IN_FORK_MODE = Arrays.asList(
            new String[]{"taskTimeoutMillis", "taskPulseMillis",
                    "maxFiles", "javaPath", "maxRestarts", "numRestarts",
                    "forkedStatusFile", "maxForkedStartupMillis", "tmpFilePrefix"});

        /*
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
    //these should exist in every list
    static Set<String> INCLUDE_LIST = new HashSet<>(Arrays.asList(
            new String[]{URLEmailNormalizingFilterFactory.URL,
                    URLEmailNormalizingFilterFactory.EMAIL}));
    //words to ignore
    //these are common 4 letter html markup words that we do
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/captioning/tf/TensorflowRESTCaptioner.java`
#### Snippet
```java

    private static final Set<MediaType> SUPPORTED_MIMES = Collections.unmodifiableSet(new HashSet<>(
            Arrays.asList(new MediaType[]{MediaType.image("jpeg"), MediaType.image("png"),
                    MediaType.image("gif")})));

    private static final String LABEL_LANG = "eng";
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/pot/PooledTimeSeriesParser.java`
#### Snippet
```java
    private static final Set<MediaType> SUPPORTED_TYPES = isAvailable ? Collections.unmodifiableSet(
            new HashSet<>(Arrays.asList(
                    new MediaType[]{MediaType.video("avi"), MediaType.video("mp4")}))) :
            Collections.<MediaType>emptySet();
    // TODO: Add all supported video types
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowRESTRecogniser.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(TensorflowRESTRecogniser.class);
    private static final Set<MediaType> SUPPORTED_MIMES = Collections.unmodifiableSet(new HashSet<>(
            Arrays.asList(new MediaType[]{MediaType.image("jpeg"), MediaType.image("png"),
                    MediaType.image("gif")})));
    @Field
    protected URI apiBaseUri = URI.create("http://localhost:8764/inception/v4");
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
    private static final Set<MediaType> SUPPORTED_TYPES = Collections.unmodifiableSet(new HashSet<>(
            Arrays.asList(
                    new MediaType[]{MediaType.image(OCR + "png"), MediaType.image(OCR + "jpeg"),
                            MediaType.image(OCR + "tiff"), MediaType.image(OCR + "bmp"),
                            MediaType.image(OCR + "gif"),
                            //these are not currently covered by other parsers
                            MediaType.image("jp2"), MediaType.image("jpx"),
                            MediaType.image("x-portable-pixmap"),
                            //add the ocr- versions as well
                            MediaType.image(OCR + "jp2"), MediaType.image(OCR + "jpx"),
                            MediaType.image(OCR + "x-portable-pixmap"),

                    })));
    private static volatile boolean HAS_WARNED = false;
    //if a user specifies a custom tess path or tessdata path
```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ListManager.java`
#### Snippet
```java
                return "bullet";
            case 47:
                return "none";
            default:
                //do we really want to silently swallow these uncovered cases?
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
                return Boolean.toString((Boolean) value);
            case MEMO:
                return (String) value;
            case INT:
                return Short.toString((Short) value);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
                return Byte.toString((Byte) value);
            case GUID:
                return value.toString();
            case COMPLEX_TYPE: //skip all these
            case UNKNOWN_0D:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `tika-core/src/main/java/org/apache/tika/sax/BasicContentHandlerFactory.java`
#### Snippet
```java
        switch (type) {
            case TEXT:
                return new ToTextContentHandler();
            case HTML:
                return new ToHTMLContentHandler();
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `tika-core/src/main/java/org/apache/tika/sax/BasicContentHandlerFactory.java`
#### Snippet
```java
                return HANDLER_TYPE.TEXT;
            case "txt":
                return HANDLER_TYPE.TEXT;
            case "html":
                return HANDLER_TYPE.HTML;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `tika-core/src/main/java/org/apache/tika/sax/BasicContentHandlerFactory.java`
#### Snippet
```java
                                writeLimit);
                    case TEXT:
                        return new WriteOutContentHandler(
                                new ToTextContentHandler(os, charset.name()), writeLimit);
                    case HTML:
                        return new WriteOutContentHandler(
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `tika-core/src/main/java/org/apache/tika/sax/BasicContentHandlerFactory.java`
#### Snippet
```java
                        return new BodyContentHandler(new OutputStreamWriter(os, charset));
                    case TEXT:
                        return new ToTextContentHandler(os, charset.name());
                    case HTML:
                        return new ToHTMLContentHandler(os, charset.name());
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFCell.java`
#### Snippet
```java
                return getFormattedDate();
            case N:
                return new String(getBytes(), StandardCharsets.US_ASCII).trim();
            case L:
                return new String(getBytes(), StandardCharsets.US_ASCII).trim();
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFCell.java`
#### Snippet
```java
                return new String(getBytes(), StandardCharsets.US_ASCII).trim();
            case L:
                return new String(getBytes(), StandardCharsets.US_ASCII).trim();
            case T:
                return getFormattedDateTime();
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/executable/ExecutableParser.java`
#### Snippet
```java

                case 12:
                    metadata.set(PLATFORM, PLATFORM_FREEBSD);
                    break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/executable/ExecutableParser.java`
#### Snippet
```java
                break;
            case 0x1a8:
                metadata.set(MACHINE_TYPE, MACHINE_SH3);
                metadata.set(ENDIAN, Endian.BIG.getName());
                metadata.set(ARCHITECTURE_BITS, "32");
                break;

```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`\\p{Blank}` can be simplified to '\[ \\t\]'
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
    // Regular expression to match uppercase headers
    private static final String REGEX_HEADER =
            "(\\d{1,10}+\\.(\\d{1,10}+\\.?){0,10}+)\\p{Blank}+([A-Z]{1,64}+(\\s[A-Z]{1,64}+){0," +
                    "256}+){5,10}+";

```

### RegExpSimplifiable
`{3,3}` can be simplified to '{3}'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/LeipzigHelper.java`
#### Snippet
```java
        Matcher tableMatcher = Pattern.compile("([a-z]+)_table(\\.txt)?(\\.gz)?$").matcher("");
        Matcher leipzigMatcher =
                Pattern.compile("([a-z]{3,3}(-(simp|trad|rom|zaw))?)[-_].*$").matcher("");

        Map<String, List<Path>> m = new TreeMap<>();
```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `Paths.get(tikaConfigPath)`
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserFactory.java`
#### Snippet
```java
        TikaConfig tikaConfig = null;
        if (tikaConfigPath != null) {
            if (Files.isReadable(Paths.get(tikaConfigPath))) {
                tikaConfig = new TikaConfig(Paths.get(tikaConfigPath));
            } else if (this.getClass().getResource(tikaConfigPath) != null) {
```

### DuplicateExpressions
Multiple occurrences of `Paths.get(tikaConfigPath)`
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserFactory.java`
#### Snippet
```java
        if (tikaConfigPath != null) {
            if (Files.isReadable(Paths.get(tikaConfigPath))) {
                tikaConfig = new TikaConfig(Paths.get(tikaConfigPath));
            } else if (this.getClass().getResource(tikaConfigPath) != null) {
                try (InputStream is = this.getClass().getResourceAsStream(tikaConfigPath)) {
```

### DuplicateExpressions
Multiple occurrences of `Math.pow((1.0 - e * e), (1.0 / 2.0))`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java
                5.0 * Math.pow(e, 6.0) / 256.0));

        double ei = (1.0 - Math.pow((1.0 - e * e), (1.0 / 2.0))) /
                (1.0 + Math.pow((1.0 - e * e), (1.0 / 2.0)));

```

### DuplicateExpressions
Multiple occurrences of `Math.pow((1.0 - e * e), (1.0 / 2.0))`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java

        double ei = (1.0 - Math.pow((1.0 - e * e), (1.0 / 2.0))) /
                (1.0 + Math.pow((1.0 - e * e), (1.0 / 2.0)));

        double ca = 3.0 * ei / 2.0 - 27.0 * Math.pow(ei, 3.0) / 32.0;
```

### DuplicateExpressions
Multiple occurrences of `1.0 - Math.pow((e * Math.sin(phi1)), 2.0)`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java
                        cd * Math.sin(8.0 * mu);

        double n0 = a / Math.pow((1.0 - Math.pow((e * Math.sin(phi1)), 2.0)), (1.0 / 2.0));

        double r0 = a * (1.0 - e * e) /
```

### DuplicateExpressions
Multiple occurrences of `Math.pow((e * Math.sin(phi1)), 2.0)`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java
                        cd * Math.sin(8.0 * mu);

        double n0 = a / Math.pow((1.0 - Math.pow((e * Math.sin(phi1)), 2.0)), (1.0 / 2.0));

        double r0 = a * (1.0 - e * e) /
```

### DuplicateExpressions
Multiple occurrences of `1.0 - Math.pow((e * Math.sin(phi1)), 2.0)`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java

        double r0 = a * (1.0 - e * e) /
                Math.pow((1.0 - Math.pow((e * Math.sin(phi1)), 2.0)), (3.0 / 2.0));
        double fact1 = n0 * Math.tan(phi1) / r0;

```

### DuplicateExpressions
Multiple occurrences of `Math.pow((e * Math.sin(phi1)), 2.0)`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/envi/EnviHeaderParser.java`
#### Snippet
```java

        double r0 = a * (1.0 - e * e) /
                Math.pow((1.0 - Math.pow((e * Math.sin(phi1)), 2.0)), (3.0 / 2.0));
        double fact1 = n0 * Math.tan(phi1) / r0;

```

### DuplicateExpressions
Multiple occurrences of `tmp_line.toString().toLowerCase(Locale.ROOT).trim()`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                                continue;
                            }
                        } else if (tmp_line.toString().toLowerCase(Locale.ROOT).trim().startsWith("(") &&
                                   tmp_line.toString().toLowerCase(Locale.ROOT).trim().endsWith(")")) {
                            // the author line may have one or more comment lines between the
```

### DuplicateExpressions
Multiple occurrences of `tmp_line.toString().toLowerCase(Locale.ROOT).trim()`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                            }
                        } else if (tmp_line.toString().toLowerCase(Locale.ROOT).trim().startsWith("(") &&
                                   tmp_line.toString().toLowerCase(Locale.ROOT).trim().endsWith(")")) {
                            // the author line may have one or more comment lines between the
                            // copyright
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/DirectoryListingEntry.java`
#### Snippet
```java

    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("name_length:=").append(getNameLength()).append(System.getProperty("line.separator"));
        sb.append("name:=").append(getName()).append(System.getProperty("line.separator"));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcResetTable.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("version:=").append(getVersion()).append(System.getProperty("line.separator"));
        sb.append("block_count:=").append(getBlockCount()).append(System.getProperty("line.separator"));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxState.java`
#### Snippet
```java
     */
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("actual decoding window:=")
                .append(getWindow())
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmBlockInfo.java`
#### Snippet
```java
     */
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("iniBlock:=").append(getIniBlock()).append(", ");
        sb.append("startBlock:=").append(getStartBlock()).append(", ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java

    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("list:=")
                .append(getDirectoryListingEntryList().toString())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/CompactID.java`
#### Snippet
```java

    public String getCompactIDString() {
        return new StringBuilder().append(guid).append(", index=").append(guidIndex).append(", n=")
                .append((int) n).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/FileNode.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder().append("FileNodeID=0x").append(Long.toHexString(id))
                .append(", gosid=").append(gosid).append(", baseType=0x")
                .append(Long.toHexString(baseType)).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
        } else if (property == CTAKESAnnotationProperty.POLARITY) {
            String polarity_pref = "POLARITY";
            value = new StringBuilder(polarity_pref).append("=").
                    append(Integer.toString(annotation.getPolarity())).toString();
        }
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `tika-batch/src/main/java/org/apache/tika/batch/FileStarted.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder builder = new StringBuilder();
        builder.append("FileStarted [resourceId=");
        builder.append(resourceId);
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
        @Override
        public String toString() {
            StringBuilder builder = new StringBuilder();
            builder.append(settlment);
            builder.append(", ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/strawman/StrawManTikaAppDriver.java`
#### Snippet
```java

    public static String usage() {
        StringBuilder sb = new StringBuilder();
        sb.append("Example usage:\n");
        sb.append("java -cp <CP> org.apache.batch.fs.strawman.StrawManTikaAppDriver ");
```

### StringBufferReplaceableByString
`StringBuilder lastModDate` can be replaced with 'String'
in `tika-example/src/main/java/org/apache/tika/example/DirListParser.java`
#### Snippet
```java
            String fileOwnerGroup = fileToks[3];
            String fileSize = fileToks[4];
            StringBuilder lastModDate = new StringBuilder();
            lastModDate.append(fileToks[5]);
            lastModDate.append(" ");
```

### StringBufferReplaceableByString
`StringBuilder output` can be replaced with 'String'
in `tika-example/src/main/java/org/apache/tika/example/RecentFiles.java`
#### Snippet
```java

    public String getRSSHeaders() {
        StringBuilder output = new StringBuilder();
        output.append("<?xml version=\"1.0\" encoding=\"utf-8\">");
        output.append("<rss version=\"2.0\">");
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
            if (match != null && match.getConfidence() > 35 &&
                    tryToSet7BitEncoding(msg, match.getName())) {
                return;
            }
        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
            handlePict();
            inPic = false;
            return;
        } else if (RPR.equals(localName)) {
            inRPr = false;
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-core/src/main/java/org/apache/tika/utils/StreamGobbler.java`
#### Snippet
```java
            }
        } catch (IOException e) {
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp4/MP4Parser.java`
#### Snippet
```java
        } catch (NumberFormatException e) {
            //log
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp4/MP4Parser.java`
#### Snippet
```java
        } catch (MetadataException e) {
            //log
            return;
        }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniser.java`
#### Snippet
```java
            LOG.error("Age Predictor is not available. Please check wiki for detailed instructions",
                    e);
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFDOMUtil.java`
#### Snippet
```java
                    || value.equals(COSName.PARENT))) {
                //don't descend page, p, or parent
                return;
            } else {
                for (Map.Entry<COSName, COSBase> e : dict.entrySet()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        List<PDNameTreeNode<PDComplexFileSpecification>> kids = efTree.getKids();
        if (kids == null) {
            return;
        } else {
            for (PDNameTreeNode<PDComplexFileSpecification> node : kids) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
            }
            unread(reader, c);
            return;
        }

```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                            int intelSizeTemp = (getChmSection().getSyncBits(16) << 16) +
                                    getChmSection().getSyncBits(16);
                            if (intelSizeTemp >= 0) {
                                getState().setIntelFileSize(intelSizeTemp);
                            } else {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteLegacyDumpStrings.java`
#### Snippet
```java
            while ((pos = oneNoteDirectFileResource.position()) != sz) {
                long nextBufferSize = bufSize;
                if (sz - pos < bufSize) {
                    nextBufferSize = sz - pos;
                }
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteTreeWalker.java`
#### Snippet
```java
        for (int i = 0; i < arr.length - 1; i += 2) {
            if (arr[i] == 0 && arr[i + 1] == 0) {
                firstNull = (i > 0) ? i : 0;
                break;
            }
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/ZipFilesChunking.java`
#### Snippet
```java
        int index = 0;
        while (index < chunkData.length) {
            int length = chunkData.length - index < 1048576 ? chunkData.length - index : 1048576;
            byte[] temp = Arrays.copyOfRange(chunkData, index, length);
            subChunkList.add(new LeafNodeObject.IntermediateNodeObjectBuilder().Build(temp,
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `tika-core/src/main/java/org/apache/tika/sax/StandardsExtractingContentHandler.java`
#### Snippet
```java
            if (maxBufferLength > -1) {
                int remaining = maxBufferLength - stringBuilder.length();
                length = remaining > length ? length : remaining;
            }
            String text = new String(Arrays.copyOfRange(ch, start, start + length));
```

## RuleId[id=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `CryptoParser` has no concrete subclass
in `tika-core/src/main/java/org/apache/tika/parser/CryptoParser.java`
#### Snippet
```java
 * @since Apache Tika 0.10
 */
public abstract class CryptoParser extends DelegatingParser {

    /**
```

### AbstractClassNeverImplemented
Abstract class `MetadataFields` has no concrete subclass
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/MetadataFields.java`
#### Snippet
```java
 * ImageMetadataExtractor, but it can be generalized.
 */
public abstract class MetadataFields {

    private static final Logger LOG = LoggerFactory.getLogger(MetadataFields.class);
```

## RuleId[id=BoundedWildcard]
### BoundedWildcard
Can generalize to `? extends List`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java

    private void textRunsToText(XHTMLContentHandler xhtml,
                                List<List<HSLFTextParagraph>> paragraphsList) throws SAXException {
        if (paragraphsList == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends EscherRecord`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
        }

        private void findPictures(List<EscherRecord> records)
                throws IOException, SAXException, TikaException {
            for (EscherRecord escherRecord : records) {
```

### BoundedWildcard
Can generalize to `? extends DirectoryListingEntry`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
     * @return an index, if nothing found returns -1
     */
    public static int indexOf(List<DirectoryListingEntry> list, String pattern) {
        int place = 0;
        for (DirectoryListingEntry directoryListingEntry : list) {
```

### BoundedWildcard
Can generalize to `? super PackagePart`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
    }

    private void addRelatedParts(PackagePart documentPart, List<PackagePart> relatedParts) {
        for (String relation : MAIN_PART_RELATIONS) {
            PackageRelationshipCollection prc = null;
```

### BoundedWildcard
Can generalize to `? super PackagePart`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSLFPowerPointExtractorDecorator.java`
#### Snippet
```java


    private void addSlideParts(PackagePart slidePart, List<PackagePart> parts) {

        for (String relation : new String[]{XSLFRelation.VML_DRAWING.getRelation(),
```

### BoundedWildcard
Can generalize to `? extends XSSFShape`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSSFExcelExtractorDecorator.java`
#### Snippet
```java
    }

    protected void processShapes(List<XSSFShape> shapes, XHTMLContentHandler xhtml)
            throws SAXException {
        if (shapes == null) {
```

### BoundedWildcard
Can generalize to `? super PackagePart`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
    }

    private void addSlideParts(PackagePart slidePart, List<PackagePart> parts) {

        for (String relation : new String[]{XSLFRelation.VML_DRAWING.getRelation(),
```

### BoundedWildcard
Can generalize to `? super PackagePart`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
    }

    private void addRelatedParts(PackagePart documentPart, List<PackagePart> relatedParts) {
        for (String relation : MAIN_PART_RELATIONS) {
            PackageRelationshipCollection prc = null;
```

### BoundedWildcard
Can generalize to `? extends XWPFRun`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
    }

    private void processEmbeddedObjects(List<XWPFRun> runs, XHTMLContentHandler xhtml)
            throws SAXException {
        // TODO: replace w/ XPath/XQuery:
```

### BoundedWildcard
Can generalize to `? extends EmbeddedPartMetadata`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
    private void handleEmbeddedPart(PackagePart source, PackageRelationship rel,
                                    XHTMLContentHandler xhtml, Metadata parentMetadata,
                                    Map<String, EmbeddedPartMetadata> embeddedPartMetadataMap,
                                    Set<String> handledTarget)
            throws IOException, SAXException, TikaException, InvalidFormatException {
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
                                    XHTMLContentHandler xhtml, Metadata parentMetadata,
                                    Map<String, EmbeddedPartMetadata> embeddedPartMetadataMap,
                                    Set<String> handledTarget)
            throws IOException, SAXException, TikaException, InvalidFormatException {
        URI targetURI = rel.getTargetURI();
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
    private Stack<String> canvasStack = new Stack<>();

    public XPSPageContentHandler(XHTMLContentHandler xhtml, Map<String, Metadata> embeddedInfos) {
        this.xhml = xhtml;
        this.embeddedInfos = embeddedInfos;
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/ByteUtil.java`
#### Snippet
```java
    }

    public static void appendByteArrayToListOfByte(List<Byte> byteList, byte[] byteArrayToAdd) {
        for (byte b : byteArrayToAdd) {
            byteList.add(b);
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupObjectDataBLOBReference.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.objectExtendedGUIDArray.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/IntermediateNodeObject.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws TikaException, IOException {
        byteList.addAll(this.signature.serializeToByteList());
        byteList.addAll(this.dataSize.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupMetadataDeclarations.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws TikaException, IOException {
        if (this.objectGroupMetadataList != null) {
            for (ObjectGroupMetadata objectGroupMetadata : this.objectGroupMetadataList) {
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupObjectBLOBDataDeclaration.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.objectExGUID.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/SignatureObject.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int length = byteList.size();
        byteList.addAll(this.signatureData.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageManifestRootDeclare.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.rootExGUID.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/RevisionManifestObjectGroupReferences.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        List<Byte> tmpList = this.objectGroupExtendedGUID.serializeToByteList();
        byteList.addAll(tmpList);
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupDeclarations.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws TikaException, IOException {
        if (this.objectDeclarationList != null) {
            for (ObjectGroupObjectDeclare objectGroupObjectDeclare : this.objectDeclarationList) {
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageManifestSchemaGUID.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) {
        byteList.addAll(
                ByteUtil.toListOfByte(this.guid.toString().getBytes(StandardCharsets.UTF_8)));
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupMetadata.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        List<Byte> tmpList = this.ObjectChangeFrequency.serializeToByteList();
        byteList.addAll(tmpList);
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/CellManifestCurrentRevision.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        List<Byte> tmpList = this.cellManifestCurrentRevisionExGuid.serializeToByteList();
        byteList.addAll(tmpList);
```

### BoundedWildcard
Can generalize to `? super T`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObject.java`
#### Snippet
```java
    public static <T extends StreamObject> boolean tryGetCurrent(byte[] byteArray,
                                                                 AtomicInteger index,
                                                                 AtomicReference<T> streamObject,
                                                                 Class<T> clazz)
            throws TikaException, IOException {
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/DataElementPackage.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws TikaException, IOException {
        // Add the reserved byte
        byteList.add((byte) 0);
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupObjectDeclare.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.objectExtendedGUID.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/DataHashObject.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int length = byteList.size();
        byteList.addAll(this.data.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/DataElementHash.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int startPoint = byteList.size();
        byteList.addAll(this.dataElementHashScheme.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/RevisionManifest.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.revisionID.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/RevisionManifestRootDeclare.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.rootExGuid.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/DataElement.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException, TikaException {
        int startIndex = byteList.size();
        byteList.addAll(this.dataElementExGuid.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupData.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws TikaException, IOException {
        if (this.objectGroupObjectDataList != null) {
            for (ObjectGroupObjectData objectGroupObjectData : this.objectGroupObjectDataList) {
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageIndexCellMapping.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.cellID.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageIndexRevisionMapping.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.revisionExGuid.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupObjectData.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.objectExGUIDArray.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageIndexManifestMapping.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws IOException {
        int itemsIndex = byteList.size();
        byteList.addAll(this.manifestMappingExGuid.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? extends ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/ExGUIDArray.java`
#### Snippet
```java
     * @param content Specify the list of ExGuid contents.
     */
    public ExGUIDArray(List<ExGuid> content) {
        this();
        this.content = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? super Byte`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/LeafNodeObject.java`
#### Snippet
```java
     */
    @Override
    protected int serializeItemsToByteList(List<Byte> byteList) throws TikaException, IOException {
        byteList.addAll(this.signature.serializeToByteList());
        byteList.addAll(this.dataSize.serializeToByteList());
```

### BoundedWildcard
Can generalize to `? extends ObjectGroupDataElementData`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/LeafNodeObject.java`
#### Snippet
```java
         */

        private ObjectGroupObjectData FindByExGuid(List<ObjectGroupDataElementData> objectGroupList,
                                                   ExGuid extendedGuid,
                                                   AtomicReference<ObjectGroupObjectDeclare> declare)
```

### BoundedWildcard
Can generalize to `? super ObjectGroupObjectDeclare`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/LeafNodeObject.java`
#### Snippet
```java
        private ObjectGroupObjectData FindByExGuid(List<ObjectGroupDataElementData> objectGroupList,
                                                   ExGuid extendedGuid,
                                                   AtomicReference<ObjectGroupObjectDeclare> declare)
                throws TikaException {
            for (ObjectGroupDataElementData objectGroup : objectGroupList) {
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadataList.java`
#### Snippet
```java
     * @throws org.apache.tika.exception.TikaException if there is an IOException during writing
     */
    public static void toJson(List<Metadata> metadataList, Writer writer) throws IOException {
        if (metadataList == null) {
            writer.write("null");
```

### BoundedWildcard
Can generalize to `? extends FetchEmitTuple`
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonFetchEmitTupleList.java`
#### Snippet
```java
    }

    public static void toJson(List<FetchEmitTuple> list, Writer writer) throws IOException {

        try (JsonGenerator jsonGenerator = new JsonFactory().createGenerator(writer)) {
```

### BoundedWildcard
Can generalize to `? extends DataElement`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     */
    public static RevisionManifestDataElementData getRevisionManifestDataElementData(
            List<DataElement> dataElements, CellManifestDataElementData cellData,
            HashMap<ExGuid, ExGuid> revisionIDMappings) throws TikaException {
        ExGuid currentRevisionExGuid =
```

### BoundedWildcard
Can generalize to `? extends ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
    public static RevisionManifestDataElementData getRevisionManifestDataElementData(
            List<DataElement> dataElements, CellManifestDataElementData cellData,
            HashMap<ExGuid, ExGuid> revisionIDMappings) throws TikaException {
        ExGuid currentRevisionExGuid =
                cellData.cellManifestCurrentRevision.cellManifestCurrentRevisionExGuid;
```

### BoundedWildcard
Can generalize to `? extends DataElement`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     * @return If the data elements confirms to the schema defined in the MS-FSSHTTPD returns true, otherwise false.
     */
    public static boolean tryAnalyzeWhetherConfirmSchema(List<DataElement> dataElements,
                                                         ExGuid storageIndexExGuid)
            throws TikaException {
```

### BoundedWildcard
Can generalize to `? extends DataElement`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     */
    public static StorageManifestDataElementData getStorageManifestDataElementData(
            List<DataElement> dataElements, ExGuid manifestMapping) throws TikaException {
        DataElement storageManifestDataElement = dataElements.stream()
                .filter(element -> element.dataElementExGuid.equals(manifestMapping)).findAny()
```

### BoundedWildcard
Can generalize to `? super CellID`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     */
    public static DataElement createCellMainifestDataElement(ExGuid revisionId,
                                                             Map<CellID, ExGuid> cellIDMapping) {
        CellManifestDataElementData data = new CellManifestDataElementData();
        data.cellManifestCurrentRevision = new CellManifestCurrentRevision();
```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     */
    public static DataElement createCellMainifestDataElement(ExGuid revisionId,
                                                             Map<CellID, ExGuid> cellIDMapping) {
        CellManifestDataElementData data = new CellManifestDataElementData();
        data.cellManifestCurrentRevision = new CellManifestCurrentRevision();
```

### BoundedWildcard
Can generalize to `? extends DataElement`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     */
    public static List<ObjectGroupDataElementData> getDataObjectDataElementData(
            List<DataElement> dataElements, RevisionManifestDataElementData revisionData,
            AtomicReference<ExGuid> rootExGuid) throws TikaException {
        rootExGuid = null;
```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
    public static List<ObjectGroupDataElementData> getDataObjectDataElementData(
            List<DataElement> dataElements, RevisionManifestDataElementData revisionData,
            AtomicReference<ExGuid> rootExGuid) throws TikaException {
        rootExGuid = null;

```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     */
    public static List<DataElement> buildDataElements(byte[] fileContent,
                                                      AtomicReference<ExGuid> storageIndexExGuid)
            throws TikaException, IOException {
        List<ExGuid> objectDataExGuidList = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends DataElement`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     * @return Return true if analyze the storage index succeeds, otherwise return false.
     */
    public static boolean analyzeStorageIndexDataElement(List<DataElement> dataElements,
                                                         ExGuid storageIndexExGuid,
                                                         AtomicReference<ExGuid> manifestMappingGuid,
```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
    public static boolean analyzeStorageIndexDataElement(List<DataElement> dataElements,
                                                         ExGuid storageIndexExGuid,
                                                         AtomicReference<ExGuid> manifestMappingGuid,
                                                         AtomicReference<HashMap<CellID, ExGuid>> cellIDMappings,
                                                         AtomicReference<HashMap<ExGuid, ExGuid>> revisionIDMappings)
```

### BoundedWildcard
Can generalize to `? extends DataElement`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     */
    public static CellManifestDataElementData getCellManifestDataElementData(
            List<DataElement> dataElements, StorageManifestDataElementData manifestDataElementData,
            HashMap<CellID, ExGuid> cellIDMappings) throws TikaException {
        CellID cellID = new CellID(new ExGuid(1, RootExGuid), new ExGuid(1, CellSecondExGuid));
```

### BoundedWildcard
Can generalize to `? extends ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
    public static CellManifestDataElementData getCellManifestDataElementData(
            List<DataElement> dataElements, StorageManifestDataElementData manifestDataElementData,
            HashMap<CellID, ExGuid> cellIDMappings) throws TikaException {
        CellID cellID = new CellID(new ExGuid(1, RootExGuid), new ExGuid(1, CellSecondExGuid));

```

### BoundedWildcard
Can generalize to `? extends ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
    public static DataElement createRevisionManifestDataElement(ExGuid rootObjectExGuid,
                                                                ExGuid baseRevisionID,
                                                                List<ExGuid> refferenceObjectDataExGuidList,
                                                                Map<ExGuid, ExGuid> revisionMapping,
                                                                AtomicReference<ExGuid> currentRevisionID) {
```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
                                                                ExGuid baseRevisionID,
                                                                List<ExGuid> refferenceObjectDataExGuidList,
                                                                Map<ExGuid, ExGuid> revisionMapping,
                                                                AtomicReference<ExGuid> currentRevisionID) {
        RevisionManifestDataElementData data = new RevisionManifestDataElementData();
```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
                                                                ExGuid baseRevisionID,
                                                                List<ExGuid> refferenceObjectDataExGuidList,
                                                                Map<ExGuid, ExGuid> revisionMapping,
                                                                AtomicReference<ExGuid> currentRevisionID) {
        RevisionManifestDataElementData data = new RevisionManifestDataElementData();
```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
                                                                List<ExGuid> refferenceObjectDataExGuidList,
                                                                Map<ExGuid, ExGuid> revisionMapping,
                                                                AtomicReference<ExGuid> currentRevisionID) {
        RevisionManifestDataElementData data = new RevisionManifestDataElementData();
        data.revisionManifest.revisionID = new ExGuid(1, UUID.randomUUID());
```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
     */
    public static List<DataElement> createObjectGroupDataElement(byte[] fileContent,
                                                                 AtomicReference<ExGuid> rootNodeExGuid,
                                                                 List<ExGuid> objectDataExGuidList)
            throws TikaException, IOException {
```

### BoundedWildcard
Can generalize to `? super ExGuid`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
    public static List<DataElement> createObjectGroupDataElement(byte[] fileContent,
                                                                 AtomicReference<ExGuid> rootNodeExGuid,
                                                                 List<ExGuid> objectDataExGuidList)
            throws TikaException, IOException {
        NodeObject rootNode = new IntermediateNodeObject.RootNodeObjectBuilder().Build(fileContent);
```

### BoundedWildcard
Can generalize to `? extends TargetElement`
in `tika-core/src/main/java/org/apache/tika/sax/ElementMappingContentHandler.java`
#### Snippet
```java

    public ElementMappingContentHandler(ContentHandler handler,
                                        Map<QName, TargetElement> mappings) {
        super(handler);
        this.mappings = mappings;
```

### BoundedWildcard
Can generalize to `? extends Clause`
in `tika-core/src/main/java/org/apache/tika/mime/OrClause.java`
#### Snippet
```java
    private final List<Clause> clauses;

    OrClause(List<Clause> clauses) {
        this.clauses = clauses;
    }
```

### BoundedWildcard
Can generalize to `? extends Clause`
in `tika-core/src/main/java/org/apache/tika/mime/MinShouldMatchClause.java`
#### Snippet
```java
     * @param clauses
     */
    MinShouldMatchClause(int min, List<Clause> clauses) {

        if (clauses == null || clauses.size() == 0) {
```

### BoundedWildcard
Can generalize to `? extends EmitData`
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncEmitter.java`
#### Snippet
```java
        }

        private void tryToEmit(Emitter emitter, List<EmitData> cachedEmitData) {

            try {
```

### BoundedWildcard
Can generalize to `? extends EmitData`
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncEmitter.java`
#### Snippet
```java
    Instant lastEmitted = Instant.now();

    public AsyncEmitter(AsyncConfig asyncConfig, ArrayBlockingQueue<EmitData> emitData,
                        EmitterManager emitterManager) {
        this.asyncConfig = asyncConfig;
```

### BoundedWildcard
Can generalize to `? extends ForkResource`
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
    }

    private Throwable waitForResponse(List<ForkResource> resources) throws IOException {
        output.flush();
        while (!Thread.currentThread().isInterrupted()) {
```

### BoundedWildcard
Can generalize to `? extends Emitter`
in `tika-core/src/main/java/org/apache/tika/pipes/emitter/EmitterManager.java`
#### Snippet
```java
    }

    public EmitterManager(List<Emitter> emitters) {
        for (Emitter emitter : emitters) {
            if (emitterMap.containsKey(emitter.getName())) {
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-core/src/main/java/org/apache/tika/pipes/emitter/EmitData.java`
#### Snippet
```java
    }

    private static long estimateSizeInBytes(String id, List<Metadata> metadataList,
                                            String containerStackTrace) {
        long sz = 36 + id.length() * 2;
```

### BoundedWildcard
Can generalize to `? extends Fetcher`
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/FetcherManager.java`
#### Snippet
```java
    private final Map<String, Fetcher> fetcherMap = new ConcurrentHashMap<>();

    public FetcherManager(List<Fetcher> fetchers) throws TikaConfigException {
        for (Fetcher fetcher : fetchers) {
            String name = fetcher.getName();
```

### BoundedWildcard
Can generalize to `? super FetchEmitTuple`
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/CallablePipesIterator.java`
#### Snippet
```java
     */
    public CallablePipesIterator(PipesIterator pipesIterator,
                                 ArrayBlockingQueue<FetchEmitTuple> queue, long timeoutMillis,
                                 int numConsumers) {
        this.pipesIterator = pipesIterator;
```

### BoundedWildcard
Can generalize to `? extends FetchEmitTuple`
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
    }

    public synchronized boolean offer(List<FetchEmitTuple> newFetchEmitTuples, long offerMs)
            throws PipesException, InterruptedException {
        if (isShuttingDown) {
```

### BoundedWildcard
Can generalize to `? extends FetchEmitTuple`
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java

        private FetchEmitWorker(AsyncConfig asyncConfig,
                                ArrayBlockingQueue<FetchEmitTuple> fetchEmitTuples,
                                ArrayBlockingQueue<EmitData> emitDataQueue) {
            this.asyncConfig = asyncConfig;
```

### BoundedWildcard
Can generalize to `? super EmitData`
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
        private FetchEmitWorker(AsyncConfig asyncConfig,
                                ArrayBlockingQueue<FetchEmitTuple> fetchEmitTuples,
                                ArrayBlockingQueue<EmitData> emitDataQueue) {
            this.asyncConfig = asyncConfig;
            this.fetchEmitTuples = fetchEmitTuples;
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
    }

    private void injectUserMetadata(Metadata userMetadata, List<Metadata> metadataList) {
        for (String n : userMetadata.names()) {
            //overwrite whatever was there
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
     * @return
     */
    private String getContainerStacktrace(FetchEmitTuple t, List<Metadata> metadataList) {
        if (metadataList == null || metadataList.size() < 1) {
            return StringUtils.EMPTY;
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
    }

    private void filterMetadata(List<Metadata> metadataList) {
        for (Metadata m : metadataList) {
            try {
```

### BoundedWildcard
Can generalize to `? extends Param`
in `tika-core/src/main/java/org/apache/tika/utils/AnnotationUtils.java`
#### Snippet
```java
     * @throws TikaConfigException when an error occurs while assigning params
     */
    public static void assignFieldParams(Object bean, Map<String, Param> params)
            throws TikaConfigException {
        Class<?> beanClass = bean.getClass();
```

### BoundedWildcard
Can generalize to `? extends MethodTuple`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
    }

    private static void processNonPrimitive(String name, Set<MethodTuple> setters,
                                            Set<MethodTuple> getters, Document doc, Element element,
                                            Object object) {
```

### BoundedWildcard
Can generalize to `? extends MethodTuple`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java

    private static void processNonPrimitive(String name, Set<MethodTuple> setters,
                                            Set<MethodTuple> getters, Document doc, Element element,
                                            Object object) {
        for (MethodTuple setter : setters) {
```

### BoundedWildcard
Can generalize to `? extends MethodTuple`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
    }

    private static MethodTuple pickBestSetter(Set<MethodTuple> tuples) {
        //TODO -- if both string and integer, which one do we pick?
        //stub for now -- just pick the first
```

### BoundedWildcard
Can generalize to `? extends EncodingDetector`
in `tika-core/src/main/java/org/apache/tika/detect/AutoDetectReader.java`
#### Snippet
```java

    private static Charset detect(InputStream input, Metadata metadata,
                                  List<EncodingDetector> detectors, LoadErrorHandler handler)
            throws IOException, TikaException {
        // Ask all given detectors for the character encoding
```

### BoundedWildcard
Can generalize to `? extends T`
in `tika-core/src/main/java/org/apache/tika/config/ServiceLoader.java`
#### Snippet
```java
     * @return available service providers
     */
    public <T> List<T> loadServiceProviders(Class<T> iface) {
        List<T> tmp = new ArrayList<>();
        tmp.addAll(loadDynamicServiceProviders(iface));
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-core/src/main/java/org/apache/tika/config/ServiceLoader.java`
#### Snippet
```java
    }

    private void collectServiceClassNames(URL resource, Collection<String> names)
            throws IOException {
        try (InputStream stream = resource.openStream();
```

### BoundedWildcard
Can generalize to `? extends Detector`
in `tika-core/src/main/java/org/apache/tika/detect/CompositeDetector.java`
#### Snippet
```java
    private final List<Detector> detectors;

    public CompositeDetector(MediaTypeRegistry registry, List<Detector> detectors,
                             Collection<Class<? extends Detector>> excludeDetectors) {
        if (excludeDetectors == null || excludeDetectors.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `tika-core/src/main/java/org/apache/tika/parser/ParseContext.java`
#### Snippet
```java
     * or the given default value if not found
     */
    public <T> T get(Class<T> key, T defaultValue) {
        T value = get(key);
        if (value != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-core/src/main/java/org/apache/tika/config/ConfigBase.java`
#### Snippet
```java
    }

    private static void setParams(Object object, Node targetNode, Set<String> settings,
                                  String exceptNodeName) throws TikaConfigException {
        NodeList children = targetNode.getChildNodes();
```

### BoundedWildcard
Can generalize to `? extends Param`
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java

    @SuppressWarnings("rawtypes")
    protected static MetadataPolicy getMetadataPolicy(Map<String, Param> params) {
        if (params.containsKey(METADATA_POLICY_CONFIG_KEY)) {
            return (MetadataPolicy) params.get(METADATA_POLICY_CONFIG_KEY).getValue();
```

### BoundedWildcard
Can generalize to `? extends MetadataFilter`
in `tika-core/src/main/java/org/apache/tika/metadata/filter/CompositeMetadataFilter.java`
#### Snippet
```java
    private final List<MetadataFilter> filters;

    public CompositeMetadataFilter(List<MetadataFilter> filters) {
        this.filters = filters;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-core/src/main/java/org/apache/tika/metadata/filter/IncludeFieldMetadataFilter.java`
#### Snippet
```java
    }

    public IncludeFieldMetadataFilter(Set<String> fields) {
        this.includeSet = fields;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-core/src/main/java/org/apache/tika/metadata/filter/ClearByMimeMetadataFilter.java`
#### Snippet
```java
    }

    public ClearByMimeMetadataFilter(Set<String> mimes) {
        this.mimes = mimes;
    }
```

### BoundedWildcard
Can generalize to `? extends Renderer`
in `tika-core/src/main/java/org/apache/tika/renderer/CompositeRenderer.java`
#### Snippet
```java
    }

    public CompositeRenderer(List<Renderer> renderers) {
        Map<MediaType, Renderer> tmp = new ConcurrentHashMap<>();
        ParseContext empty = new ParseContext();
```

### BoundedWildcard
Can generalize to `? extends EmbeddedStreamTranslator`
in `tika-core/src/main/java/org/apache/tika/extractor/DefaultEmbeddedStreamTranslator.java`
#### Snippet
```java
    }

    private DefaultEmbeddedStreamTranslator(List<EmbeddedStreamTranslator> translators) {
        this.translators = translators;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
        //legacy behavior -- remove the field if value is null
        @Override
        public void set(String field, String value, Map<String, String[]> data) {
            if (value != null) {
                data.put(field, new String[]{ value });
```

### BoundedWildcard
Can generalize to `? extends Translator`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

        @Override
        Translator createComposite(List<Translator> loaded, MimeTypes mimeTypes,
                                   ServiceLoader loader) {
            return loaded.get(0);
```

### BoundedWildcard
Can generalize to `? extends ConfigurableThreadPoolExecutor`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

        @Override
        ConfigurableThreadPoolExecutor createComposite(List<ConfigurableThreadPoolExecutor> loaded,
                                                       MimeTypes mimeTypes, ServiceLoader loader) {
            return loaded.get(0);
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-core/src/main/java/org/apache/tika/metadata/writefilter/StandardWriteFilter.java`
#### Snippet
```java
    }

    private void setTruncated(Map<String, String[]> data) {
        data.put(METADATA_TRUNCATED_KEY, TRUE);
    }
```

### BoundedWildcard
Can generalize to `? extends TikaPipesHttpClient`
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaClient.java`
#### Snippet
```java


    private TikaClient(List<TikaPipesHttpClient> clients) {
        this.clients = clients;
    }
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-pipes/tika-emitters/tika-emitter-opensearch/src/main/java/org/apache/tika/pipes/emitter/opensearch/OpenSearchClient.java`
#### Snippet
```java
    }

    private void appendDoc(String emitKey, List<Metadata> metadataList, StringBuilder json)
            throws IOException {
        int i = 0;
```

### BoundedWildcard
Can generalize to `? super Pattern`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
    }

    private void addPatternWithIs(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(name + " is ([A-Za-z0-9\\.,\\s`']+)"), name);
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
    }

    private void addPatternWithIs(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(name + " is ([A-Za-z0-9\\.,\\s`']+)"), name);
    }
```

### BoundedWildcard
Can generalize to `? super Pattern`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
    }

    private void addPatternWithColon(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(name + "\\:\\s*([A-Za-z0-9/ _\\-\\.]+)\\s*"), name);
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
    }

    private void addPatternWithColon(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(name + "\\:\\s*([A-Za-z0-9/ _\\-\\.]+)\\s*"), name);
    }
```

### BoundedWildcard
Can generalize to `? super Pattern`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
    }

    private void addBoundingBoxPattern(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(
                name + "\\s*\\(\\s*([0-9]+\\.[0-9]+\\s*,\\s*[0-9]+\\.[0-9]+\\s*)\\)\\s*"), name);
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
    }

    private void addBoundingBoxPattern(String name, Map<Pattern, String> patterns) {
        patterns.put(Pattern.compile(
                name + "\\s*\\(\\s*([0-9]+\\.[0-9]+\\s*,\\s*[0-9]+\\.[0-9]+\\s*)\\)\\s*"), name);
```

### BoundedWildcard
Can generalize to `? extends TSDMetas`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-crypto-module/src/main/java/org/apache/tika/parser/crypto/TSDParser.java`
#### Snippet
```java
    }

    private void buildMetas(List<TSDMetas> tsdMetasList, Metadata metadata) {
        int count = 1;

```

### BoundedWildcard
Can generalize to `? extends T`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/StreamingDetectContext.java`
#### Snippet
```java
     * or the given default value if not found
     */
    public <T> T get(Class<T> key, T defaultValue) {
        T value = get(key);
        if (value != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-eval/src/main/java/org/apache/tika/server/eval/TikaEvalResource.java`
#### Snippet
```java

    private void reportContrastStats(ContrastStatistics contrastStatistics,
                                     Map<String, Object> stats) {
        stats.put(DICE.getName(), contrastStatistics.getDiceCoefficient());
        stats.put(OVERLAP.getName(), contrastStatistics.getOverlap());
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-eval/src/main/java/org/apache/tika/server/eval/TikaEvalResource.java`
#### Snippet
```java
    }

    private TokenCounts profile(String suffix, String content, Map<String, Object> stats) {
        Map<Class, Object> results = TEXT_STATS_CALCULATOR.calculate(content);

```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
    }

    private void addMetadataAsSolrInputDocuments(String emitKey, List<Metadata> metadataList,
                                                 List<SolrInputDocument> docsToUpdate)
            throws IOException, TikaEmitterException {
```

### BoundedWildcard
Can generalize to `? super SolrInputDocument`
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java

    private void addMetadataAsSolrInputDocuments(String emitKey, List<Metadata> metadataList,
                                                 List<SolrInputDocument> docsToUpdate)
            throws IOException, TikaEmitterException {
        SolrInputDocument solrInputDocument = new SolrInputDocument();
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-jdbc/src/main/java/org/apache/tika/pipes/pipesiterator/jdbc/JDBCPipesIterator.java`
#### Snippet
```java


    private FetchEmitKeyIndices loadHeaders(ResultSetMetaData metaData, List<String> headers)
            throws SQLException {
        int idIndex = -1;
```

### BoundedWildcard
Can generalize to `? extends Mp4Directory`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp4/MP4Parser.java`
#### Snippet
```java
    }

    private Set<String> processMp4Directories(Collection<Mp4Directory> mp4Directories,
                                         Metadata metadata) {
        Set<String> errorMsgs = new HashSet<>();
```

### BoundedWildcard
Can generalize to `? extends DBFRow`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFParser.java`
#### Snippet
```java
    }

    private Charset getCharset(List<DBFRow> firstRows, DBFFileHeader header)
            throws IOException, TikaException {
        //TODO: potentially use codepage info in the header
```

### BoundedWildcard
Can generalize to `? extends KeyStatusPair`
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-jdbc/src/main/java/org/apache/tika/pipes/reporters/jdbc/JDBCPipesReporter.java`
#### Snippet
```java
        public ReportWorker(String connectionString,
                            Optional<String> postConnectionString,
                            ArrayBlockingQueue<KeyStatusPair> queue) {
            this.connectionString = connectionString;
            this.postConnectionString = postConnectionString;
```

### BoundedWildcard
Can generalize to `? extends File`
in `tika-app/src/main/java/org/apache/tika/gui/ParsingTransferHandler.java`
#### Snippet
```java
    }

    private void importFiles(List<File> files) {
        for (File file : files) {
            tika.openFile(file);
```

### BoundedWildcard
Can generalize to `? extends Param`
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniserConfig.java`
#### Snippet
```java
    private String pathClassifyRegression = null;

    public AgeRecogniserConfig(Map<String, Param> params) {

        URL classifyUrl = AgeRecogniserConfig.class
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
     * @param jvmArgs     args that belong as jvm arguments for the forked process
     */
    private static void mapifyArgs(final String[] args, final Map<String, String> commandLine,
                                   final Map<String, String> jvmArgs) {

```

### BoundedWildcard
Can generalize to `? super String`
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
     * @param jvmArgs     args that belong as jvm arguments for the forked process
     */
    private static void mapifyArgs(final String[] args, final Map<String, String> commandLine,
                                   final Map<String, String> jvmArgs) {

```

### BoundedWildcard
Can generalize to `? super String`
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
     */
    private static void mapifyArgs(final String[] args, final Map<String, String> commandLine,
                                   final Map<String, String> jvmArgs) {

        if (args.length == 0) {
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
     */
    private static void mapifyArgs(final String[] args, final Map<String, String> commandLine,
                                   final Map<String, String> jvmArgs) {

        if (args.length == 0) {
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
    }

    private static void translateCommandLine(String[] args, Map<String, String> map)
            throws IOException {
        //if there are only two args and they are both directories, treat the first
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/config/PasswordProviderConfig.java`
#### Snippet
```java

    @Override
    public void configure(MultivaluedMap<String, String> httpHeaders, Metadata metadata,
                          ParseContext context) {
        String tmpPassword = httpHeaders.getFirst(PASSWORD_BASE64_UTF8);
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/config/TimeoutConfig.java`
#### Snippet
```java

    @Override
    public void configure(MultivaluedMap<String, String> httpHeaders, Metadata metadata,
                          ParseContext context) {
        if (httpHeaders.containsKey(X_TIKA_TIMEOUT_MILLIS)) {
```

### BoundedWildcard
Can generalize to `? super ResourceProvider`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
    private static void loadAllProviders(TikaServerConfig tikaServerConfig,
                                         ServerStatus serverStatus,
                                         List<ResourceProvider> resourceProviders,
                                         List<Object> writers)
            throws TikaException, SAXException, IOException {
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaParsers.java`
#### Snippet
```java
    }

    private void parserAsMap(ParserDetails p, boolean withMimeTypes, Map<String, Object> details) {
        details.put("name", p.className);
        details.put("composite", p.isComposite);
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaDetectors.java`
#### Snippet
```java
    }

    private void detectorAsMap(Detector d, Map<String, Object> details) {
        details.put("name", d.getClass().getName());

```

### BoundedWildcard
Can generalize to `? extends ResourceProvider`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaWelcome.java`
#### Snippet
```java
    private List<Class<?>> endpoints = new LinkedList<>();

    public TikaWelcome(List<ResourceProvider> rCoreProviders) {
        this.tika = new Tika(TikaResource.getConfig());
        this.html = new HTMLHelper();
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-pipes/tika-emitters/tika-emitter-jdbc/src/main/java/org/apache/tika/pipes/emitter/jdbc/JDBCEmitter.java`
#### Snippet
```java
    private void updateValue(String emitKey, PreparedStatement insertStatement, int i,
                             ColumnDefinition columnDefinition, int metadataListIndex,
                             List<Metadata> metadataList)
            throws SQLException {
        Metadata metadata = metadataList.get(metadataListIndex);
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    private static void loadSupported(Node compound,
                                      String itemName,
                                      Set<String> supported) {
        NodeList children = compound.getChildNodes();
        for (int i = 0; i < children.getLength(); i++) {
```

### BoundedWildcard
Can generalize to `? super COSBase`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFDOMUtil.java`
#### Snippet
```java

    private static void find(COSBase cosBase, Set<COSName> types, int depth, int maxDepth,
                             Set<COSBase> seen, List<COSDictionary> found) {
        if (seen.contains(cosBase)) {
            return;
```

### BoundedWildcard
Can generalize to `? super COSDictionary`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFDOMUtil.java`
#### Snippet
```java

    private static void find(COSBase cosBase, Set<COSName> types, int depth, int maxDepth,
                             Set<COSBase> seen, List<COSDictionary> found) {
        if (seen.contains(cosBase)) {
            return;
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
    }

    private void handleField(XMLStreamReader reader, Map<String, XFAField> fields)
            throws XMLStreamException {
        //reader is set to the field element
```

### BoundedWildcard
Can generalize to `? super XFAField`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
    }

    private void handleField(XMLStreamReader reader, Map<String, XFAField> fields)
            throws XMLStreamException {
        //reader is set to the field element
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
    }

    private void loadData(XMLStreamReader reader, Map<String, String> pdfObjRToValues)
            throws XMLStreamException {
        //reader is at the "xfa:data" element
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/XFAExtractor.java`
#### Snippet
```java
    }

    private void loadData(XMLStreamReader reader, Map<String, String> pdfObjRToValues)
            throws XMLStreamException {
        //reader is at the "xfa:data" element
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
    }

    public static boolean getThrowOnWriteLimitReached(MultivaluedMap<String, String> httpHeaders) {
        if (httpHeaders.containsKey("throwOnWriteLimitReached")) {
            String val = httpHeaders.getFirst("throwOnWriteLimitReached");
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
    }

    public static String detectFilename(MultivaluedMap<String, String> httpHeaders) {

        String disposition = httpHeaders.getFirst("Content-Disposition");
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/util/ContentTagParser.java`
#### Snippet
```java

        public XHTMLContentTagHandler(Set<String> uppercaseTagsOfInterest,
                                      Map<String, Integer> tags) {
            this.uppercaseTagsOfInterest = uppercaseTagsOfInterest;
            this.tags = tags;
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
    }

    private HtmlTag getTag(String name, Map<String, HtmlTag> roleMap) {
        if (roleMap.containsKey(name)) {
            return roleMap.get(name);
```

### BoundedWildcard
Can generalize to `? extends ObjectRef`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
    }

    private Map<MCID, String> loadTextByMCID(List<ObjectRef> pageRefs) throws IOException {
        int pageCount = 1;
        Map<MCID, String> paragraphs = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? super ObjectRef`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
    }

    private static void findPages(COSBase kidsObj, List<ObjectRef> pageRefs) {
        if (kidsObj == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends LanguageResult`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensBhattacharyya.java`
#### Snippet
```java

    @Override
    public Double calculate(List<LanguageResult> languages, TokenCounts tokenCounts) {
        Pair<String, LangModel> pair =
                commonTokenCountManager.getLangTokens(languages.get(0).getLanguage());
```

### BoundedWildcard
Can generalize to `? extends LanguageResult`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokens.java`
#### Snippet
```java

    @Override
    public CommonTokenResult calculate(List<LanguageResult> languages, TokenCounts tokenCounts) {
        Pair<String, LangModel> pair =
                commonTokenCountManager.getLangTokens(languages.get(0).getLanguage());
```

### BoundedWildcard
Can generalize to `? extends LanguageResult`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensHellinger.java`
#### Snippet
```java

    @Override
    public Double calculate(List<LanguageResult> languages, TokenCounts tokenCounts) {
        Pair<String, LangModel> pair =
                commonTokenCountManager.getLangTokens(languages.get(0).getLanguage());
```

### BoundedWildcard
Can generalize to `? extends LanguageResult`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensKLDNormed.java`
#### Snippet
```java

    @Override
    public Double calculate(List<LanguageResult> languages, TokenCounts tokenCounts) {
        Pair<String, LangModel> pair =
                commonTokenCountManager.getLangTokens(languages.get(0).getLanguage());
```

### BoundedWildcard
Can generalize to `? extends LanguageResult`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensKLDivergence.java`
#### Snippet
```java

    @Override
    public Double calculate(List<LanguageResult> languages, TokenCounts tokenCounts) {
        Pair<String, LangModel> pair =
                commonTokenCountManager.getLangTokens(languages.get(0).getLanguage());
```

### BoundedWildcard
Can generalize to `? super COSObjectable`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
    }

    void extractBookmarkText(PDOutlineNode bookmark, Set<COSObjectable> seen, int itemCount)
            throws SAXException, IOException, TikaException {
        PDOutlineItem current = bookmark.getFirstChild();
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java

    private void extractFilesfromEFTree(PDNameTreeNode efTree,
                                        Map<String, PDComplexFileSpecification> embeddedFileNames,
                                        int depth) throws IOException {
        if (depth > MAX_RECURSION_DEPTH) {
```

### BoundedWildcard
Can generalize to `? super PDComplexFileSpecification`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java

    private void extractFilesfromEFTree(PDNameTreeNode efTree,
                                        Map<String, PDComplexFileSpecification> embeddedFileNames,
                                        int depth) throws IOException {
        if (depth > MAX_RECURSION_DEPTH) {
```

### BoundedWildcard
Can generalize to `? extends LanguageResult`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CommonTokensCosine.java`
#### Snippet
```java

    @Override
    public Double calculate(List<LanguageResult> languages, TokenCounts tokenCounts) {
        Pair<String, LangModel> pair =
                commonTokenCountManager.getLangTokens(languages.get(0).getLanguage());
```

### BoundedWildcard
Can generalize to `? super Class`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/CompositeTextStatsCalculator.java`
#### Snippet
```java
    }

    private TokenCounts tokenize(String txt, Map<Class, Object> results) throws IOException {
        TokenCounts counts = new TokenCounts();
        TokenStream ts = analyzer.tokenStream(FIELD, txt);
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
    }

    private void parseDwgObject(JsonParser jsonParser, Consumer<String> textConsumer) throws IOException {
        JsonToken nextToken;
        while ((nextToken = jsonParser.nextToken()) != JsonToken.END_OBJECT) {
```

### BoundedWildcard
Can generalize to `? extends TableInfo`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
    }

    public void createTables(List<TableInfo> tableInfos, CREATE_TABLE createTable)
            throws SQLException, IOException {

```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/LeipzigSampler.java`
#### Snippet
```java
    }

    private void addSentences(Path p, List<String> sentences) throws IOException {
        try (BufferedReader reader = Files.newBufferedReader(p, StandardCharsets.UTF_8)) {
            String line = reader.readLine();
```

### BoundedWildcard
Can generalize to `? extends Path`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TopCommonTokenCounter.java`
#### Snippet
```java
    }

    private void execute(Path commonTokensFile, List<Path> inputFiles) throws Exception {
        Path luceneDir = Files.createTempDirectory("tika-eval-lucene-");
        AbstractTokenTFDFPriorityQueue queue = new TokenDFPriorityQueue(TOP_N);
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
     * @return
     */
    private String findSharedDigestKey(List<Metadata> metadataListA, List<Metadata> metadataListB) {
        if (metadataListB == null || metadataListB.size() == 0) {
            return null;
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
     * @return
     */
    private String findSharedDigestKey(List<Metadata> metadataListA, List<Metadata> metadataListB) {
        if (metadataListB == null || metadataListB.size() == 0) {
            return null;
```

### BoundedWildcard
Can generalize to `? super Cols`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
    }

    private void writeContrastString(Map<Cols, String> data, Cols col,
                                     TokenIntPair[] tokenIntPairs) {

```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
    }

    private void writeContrastString(Map<Cols, String> data, Cols col,
                                     TokenIntPair[] tokenIntPairs) {

```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java

    private int findMatchingDigests(String sharedDigestKey, Set<Integer> handledB,
                                    Metadata metadata, List<Metadata> metadataListB) {
        String digestA = metadata.get(sharedDigestKey);
        if (digestA == null) {
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
     */
    private int getMatch(int aIndex, String sharedDigestKey, Set<Integer> handledB,
                         List<Metadata> metadataListA, List<Metadata> metadataListB) {
        //TODO: could make this more robust
        if (metadataListB == null || metadataListB.size() == 0) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoTag.java`
#### Snippet
```java
     * input stream
     */
    public void toGeoTag(Map<String, List<Location>> resolvedGeonames, String bestNER) {

        for (String key : resolvedGeonames.keySet()) {
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
     * @return empty list if input list is empty or null
     */
    static List<Integer> countAttachments(List<Metadata> list) {
        List<Integer> ret = new ArrayList<>();
        if (list == null || list.size() == 0) {
```

### BoundedWildcard
Can generalize to `? super Cols`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void getFileTypes(Metadata metadata, Map<Cols, String> output) {
        if (metadata == null) {
            return;
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void getFileTypes(Metadata metadata, Map<Cols, String> output) {
        if (metadata == null) {
            return;
```

### BoundedWildcard
Can generalize to `? super Cols`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void writeTokenCounts(Map<Class, Object> textStats, Map<Cols, String> data) {
        TokenIntPair[] tokenIntPairs = (TokenIntPair[]) textStats.get(TopNTokens.class);
        int i = 0;
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void writeTokenCounts(Map<Class, Object> textStats, Map<Cols, String> data) {
        TokenIntPair[] tokenIntPairs = (TokenIntPair[]) textStats.get(TopNTokens.class);
        int i = 0;
```

### BoundedWildcard
Can generalize to `? super Cols`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void langid(Map<Class, Object> stats, Map<Cols, String> data) {
        List<LanguageResult> probabilities =
                (List<LanguageResult>) stats.get(LanguageIDWrapper.class);
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void langid(Map<Class, Object> stats, Map<Cols, String> data) {
        List<LanguageResult> probabilities =
                (List<LanguageResult>) stats.get(LanguageIDWrapper.class);
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    long getSourceFileLength(List<Metadata> metadataList) {
        if (metadataList == null || metadataList.size() < 1) {
            return NON_EXISTENT_FILE_LENGTH;
```

### BoundedWildcard
Can generalize to `? super Cols`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void getExceptionStrings(Metadata metadata, Map<Cols, String> data) {

        String fullTrace = metadata.get(TikaCoreProperties.CONTAINER_EXCEPTION);
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void getExceptionStrings(Metadata metadata, Map<Cols, String> data) {

        String fullTrace = metadata.get(TikaCoreProperties.CONTAINER_EXCEPTION);
```

### BoundedWildcard
Can generalize to `? super Cols`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void unicodeBlocks(Map<Class, Object> tokenStats, Map<Cols, String> data) {

        Map<String, MutableInt> blocks =
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    }

    void unicodeBlocks(Map<Class, Object> tokenStats, Map<Cols, String> data) {

        Map<String, MutableInt> blocks =
```

### BoundedWildcard
Can generalize to `? super Cols`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
     */
    protected static String truncateContent(ContentTags contentTags, int maxLength,
                                            Map<Cols, String> data) {
        data.put(Cols.CONTENT_TRUNCATED_AT_MAX_LEN, "FALSE");
        if (contentTags == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
     */
    protected static String truncateContent(ContentTags contentTags, int maxLength,
                                            Map<Cols, String> data) {
        data.put(Cols.CONTENT_TRUNCATED_AT_MAX_LEN, "FALSE");
        if (contentTags == null) {
```

### BoundedWildcard
Can generalize to `? extends Metadata`
in `tika-pipes/tika-emitters/tika-emitter-kafka/src/main/java/org/apache/tika/pipes/emitter/kafka/KafkaEmitter.java`
#### Snippet
```java

    @Override
    public void emit(String emitKey, List<Metadata> metadataList)
            throws IOException, TikaEmitterException {
        if (metadataList == null || metadataList.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends Author`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
    }

    private String getMetadataAuthors(List<Author> authorList) {
        // generates Chris A. Mattmann 1, 2 Daniel J. Crichton 1 Nenad Medvidovic 2
        // Steve Hughes 1
```

### BoundedWildcard
Can generalize to `? super Author`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
    }

    private void parseAuthor(Node authorNode, List<Author> authorList) {
        Author author = new Author();
        Node persName = getFirstChild(authorNode.getChildNodes(), "persName");
```

### BoundedWildcard
Can generalize to `? extends Author`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
    }

    private String getMetadataAddresses(List<Author> authorList) {
        // generates: "Pasadena, CA 91109, USA Los Angeles, CA 90089, USA",
        List<Address> unique = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends Author`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
    }

    private String getMetadataAffiliations(List<Author> authorList) {
        // generates 1 Jet Propulsion Laboratory California Institute of Technology
        // ; 2 Computer Science Department University of Southern California
```

### BoundedWildcard
Can generalize to `? extends Author`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
    }

    private String getMetadataFullAffiliations(List<Author> authorList) {
        List<Affiliation> unique = new ArrayList<>();
        StringBuilder metAffils = new StringBuilder();
```

### BoundedWildcard
Can generalize to `? extends FileResource`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
    private volatile STATE currentState = STATE.NOT_YET_STARTED;

    public FileResourceConsumer(ArrayBlockingQueue<FileResource> fileQueue) {
        this.fileQueue = fileQueue;
        consumerId = numConsumers.incrementAndGet();
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/AbstractConverter.java`
#### Snippet
```java
     * @throws TikaException in case a namespace oculd not be registered
     */
    protected void registerNamespaces(Set<Namespace> namespaces) throws TikaException {
        XMPSchemaRegistry registry = XMPMetaFactory.getSchemaRegistry();

```

### BoundedWildcard
Can generalize to `? extends Transformer`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/AutoDetectTransformer.java`
#### Snippet
```java
    }

    public AutoDetectTransformer(List<Transformer> transformers) {
        for (Transformer t : transformers) {
            for (MediaType mediaType : t.getSupportedTypes()) {
```

### BoundedWildcard
Can generalize to `? super FetchEmitTuple`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/cli/FuzzingCLI.java`
#### Snippet
```java
        private int added = 0;

        public FileAdder(PipesIterator pipesIterator, ArrayBlockingQueue<FetchEmitTuple> queue) {
            this.pipesIterator = pipesIterator;
            this.queue = queue;
```

### BoundedWildcard
Can generalize to `? super COSName`
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java

    private TikaInputStream runFilters(COSBase filters, TikaInputStream is,
                                       List<COSName> usedFilters) throws IOException {
        if (filters instanceof COSNull) {
        } else if (filters instanceof COSName) {
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-csv/src/main/java/org/apache/tika/pipes/pipesiterator/csv/CSVPipesIterator.java`
#### Snippet
```java


    private FetchEmitKeyIndices loadHeaders(CSVRecord record, List<String> headers)
            throws IOException {
        int fetchKeyColumnIndex = -1;
```

### BoundedWildcard
Can generalize to `? extends FieldInfo`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/apple/AppleSingleFileParser.java`
#### Snippet
```java
    }

    private long processFieldEntries(InputStream stream, List<FieldInfo> fieldInfoList,
                                     Metadata embeddedMetadata, long bytesRead)
            throws IOException, TikaException {
```

### BoundedWildcard
Can generalize to `? extends FieldInfo`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/apple/AppleSingleFileParser.java`
#### Snippet
```java
    }

    private FieldInfo getContentFieldInfo(List<FieldInfo> fieldInfoList) {
        for (FieldInfo fieldInfo : fieldInfoList) {
            if (fieldInfo.entryId == 1) {
```

### BoundedWildcard
Can generalize to `? extends Directory`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageMetadataExtractor.java`
#### Snippet
```java
     * @throws MetadataException This method does not handle exceptions from Metadata Extractor
     */
    protected void handle(Iterator<Directory> directories) throws MetadataException {
        while (directories.hasNext()) {
            Directory directory = directories.next();
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
     * @param invalidLangs
     */
    public static void getLangs(String language, Set<String> validLangs, Set<String> invalidLangs) {
        if (StringUtils.isBlank(language)) {
            return;
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
     * @param invalidLangs
     */
    public static void getLangs(String language, Set<String> validLangs, Set<String> invalidLangs) {
        if (StringUtils.isBlank(language)) {
            return;
```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    }

    private boolean parseBody(byte[] value, HashMap<String, String> properties) {
        boolean added = false;

```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    }

    private boolean parseBody(byte[] value, HashMap<String, String> properties) {
        boolean added = false;

```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java


    private boolean parseFooter(byte[] value, HashMap<String, String> properties) {
        boolean added = false;

```

### BoundedWildcard
Can generalize to `? super String`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java


    private boolean parseFooter(byte[] value, HashMap<String, String> properties) {
        boolean added = false;

```

### BoundedWildcard
Can generalize to `? extends FetchEmitTuple`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-csv/src/test/java/TestCSVPipesIterator.java`
#### Snippet
```java
        private final List<FetchEmitTuple> pairs = new ArrayList<>();

        private MockFetcher(ArrayBlockingQueue<FetchEmitTuple> queue) {
            this.queue = queue;
        }
```

## RuleId[id=MissortedModifiers]
### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessParser.java`
#### Snippet
```java
    public static final String SUMMARY_PROPERTY_PREFIX =
            "MDB_SUMMARY_PROP" + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER;
    private final static LinkResolver IGNORE_LINK_RESOLVER = new IgnoreLinkResolver();
    private static final long serialVersionUID = -752276948656079347L;
    private static final MediaType MEDIA_TYPE = MediaType.application("x-msaccess");
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractListManager.java`
#### Snippet
```java

public abstract class AbstractListManager {
    private final static String BULLET = "\u00b7";

    protected Map<Integer, ParagraphLevelCounter> listLevelMap =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
    final static String COMPANY_PROP_KEY = "Company";

    final static String TEXT_FORMAT_KEY = "TextFormat";
    final static String CURRENCY_FORMAT_KEY = "Format";
    final static byte TEXT_FORMAT = 0;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
    final static String TITLE_PROP_KEY = "Title";
    final static String AUTHOR_PROP_KEY = "Author";
    final static String COMPANY_PROP_KEY = "Company";

    final static String TEXT_FORMAT_KEY = "TextFormat";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
    final static String CURRENCY_FORMAT_KEY = "Format";
    final static byte TEXT_FORMAT = 0;
    final static byte RICH_TEXT_FORMAT = 1;

    final NumberFormat currencyFormatter;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
    final static String TEXT_FORMAT_KEY = "TextFormat";
    final static String CURRENCY_FORMAT_KEY = "Format";
    final static byte TEXT_FORMAT = 0;
    final static byte RICH_TEXT_FORMAT = 1;

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java

    final static String TEXT_FORMAT_KEY = "TextFormat";
    final static String CURRENCY_FORMAT_KEY = "Format";
    final static byte TEXT_FORMAT = 0;
    final static byte RICH_TEXT_FORMAT = 1;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
class JackcessExtractor extends AbstractPOIFSExtractor {

    final static String TITLE_PROP_KEY = "Title";
    final static String AUTHOR_PROP_KEY = "Author";
    final static String COMPANY_PROP_KEY = "Company";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java

    final static String TITLE_PROP_KEY = "Title";
    final static String AUTHOR_PROP_KEY = "Author";
    final static String COMPANY_PROP_KEY = "Company";

```

### MissortedModifiers
Missorted modifiers `final private`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java
public class ChmSection {
    final private byte[] data;
    final private byte[] prevcontent;
    private int swath;// kiks
    private int total;// remains
```

### MissortedModifiers
Missorted modifiers `final private`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java

public class ChmSection {
    final private byte[] data;
    final private byte[] prevcontent;
    private int swath;// kiks
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
     * Represents lzx block types in order to decompress differently
     */
    public final static int UNDEFINED = 0;
    public final static int VERBATIM = 1;
    public final static int ALIGNED_OFFSET = 2;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
    public final static int VERBATIM = 1;
    public final static int ALIGNED_OFFSET = 2;
    public final static int UNCOMPRESSED = 3;
    private static final Logger LOG = LoggerFactory.getLogger(ChmCommons.class);

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
     */
    public final static int UNDEFINED = 0;
    public final static int VERBATIM = 1;
    public final static int ALIGNED_OFFSET = 2;
    public final static int UNCOMPRESSED = 3;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
    public final static int UNDEFINED = 0;
    public final static int VERBATIM = 1;
    public final static int ALIGNED_OFFSET = 2;
    public final static int UNCOMPRESSED = 3;
    private static final Logger LOG = LoggerFactory.getLogger(ChmCommons.class);
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/ListDescriptor.java`
#### Snippet
```java
 */
public class ListDescriptor {
    public final static int NUMBER_TYPE_BULLET = 23;

    public int id;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String A = "a";
    final static String BODY = "body";
    final static String BR = "br";
    final static String CDATA = "cdata";
    final static String DIV = "div";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String IMG = "img";
    final static String P = "p";
    final static String TD = "td";
    final static String TR = "tr";
    final static String TABLE = "table";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java

    final static String MS_OFFICE_PROPERTIES_URN = "urn:schemas-microsoft-com:office:office";
    final static String MS_DOC_PROPERTIES_URN = "urn:schemas-microsoft-com:office:office";
    final static String MS_SPREADSHEET_URN = "urn:schemas-microsoft-com:office:spreadsheet";
    final static String MS_VML_URN = "urn:schemas-microsoft-com:vml";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String HREF = "href";
    final static String IMG = "img";
    final static String P = "p";
    final static String TD = "td";
    final static String TR = "tr";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java

    final static String HLINK = "hlink";
    final static String HLINK_DEST = "dest";
    final static String NAME_ATTR = "name";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String MS_DOC_PROPERTIES_URN = "urn:schemas-microsoft-com:office:office";
    final static String MS_SPREADSHEET_URN = "urn:schemas-microsoft-com:office:spreadsheet";
    final static String MS_VML_URN = "urn:schemas-microsoft-com:vml";
    final static String WORD_ML_URL = "http://schemas.microsoft.com/office/word/2003/wordml";
    final static Attributes EMPTY_ATTRS = new AttributesImpl();
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String DIV = "div";
    final static String HREF = "href";
    final static String IMG = "img";
    final static String P = "p";
    final static String TD = "td";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String TD = "td";
    final static String TR = "tr";
    final static String TABLE = "table";
    final static String TBODY = "tbody";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String MS_OFFICE_PROPERTIES_URN = "urn:schemas-microsoft-com:office:office";
    final static String MS_DOC_PROPERTIES_URN = "urn:schemas-microsoft-com:office:office";
    final static String MS_SPREADSHEET_URN = "urn:schemas-microsoft-com:office:spreadsheet";
    final static String MS_VML_URN = "urn:schemas-microsoft-com:vml";
    final static String WORD_ML_URL = "http://schemas.microsoft.com/office/word/2003/wordml";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String BIN_DATA = "binData";

    final static String A = "a";
    final static String BODY = "body";
    final static String BR = "br";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String P = "p";
    final static String TD = "td";
    final static String TR = "tr";
    final static String TABLE = "table";
    final static String TBODY = "tbody";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String BODY = "body";
    final static String BR = "br";
    final static String CDATA = "cdata";
    final static String DIV = "div";
    final static String HREF = "href";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String TBODY = "tbody";

    final static String HLINK = "hlink";
    final static String HLINK_DEST = "dest";
    final static String NAME_ATTR = "name";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String BR = "br";
    final static String CDATA = "cdata";
    final static String DIV = "div";
    final static String HREF = "href";
    final static String IMG = "img";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String NAME_ATTR = "name";

    final static char[] NEWLINE = new char[]{'\n'};


```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static Attributes EMPTY_ATTRS = new AttributesImpl();

    final static String DOCUMENT_PROPERTIES = "DocumentProperties";
    final static String PICT = "pict";
    final static String BIN_DATA = "binData";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String MS_SPREADSHEET_URN = "urn:schemas-microsoft-com:office:spreadsheet";
    final static String MS_VML_URN = "urn:schemas-microsoft-com:vml";
    final static String WORD_ML_URL = "http://schemas.microsoft.com/office/word/2003/wordml";
    final static Attributes EMPTY_ATTRS = new AttributesImpl();

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String TR = "tr";
    final static String TABLE = "table";
    final static String TBODY = "tbody";

    final static String HLINK = "hlink";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String CDATA = "cdata";
    final static String DIV = "div";
    final static String HREF = "href";
    final static String IMG = "img";
    final static String P = "p";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String DOCUMENT_PROPERTIES = "DocumentProperties";
    final static String PICT = "pict";
    final static String BIN_DATA = "binData";

    final static String A = "a";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
public abstract class AbstractXML2003Parser extends AbstractParser {

    final static String MS_OFFICE_PROPERTIES_URN = "urn:schemas-microsoft-com:office:office";
    final static String MS_DOC_PROPERTIES_URN = "urn:schemas-microsoft-com:office:office";
    final static String MS_SPREADSHEET_URN = "urn:schemas-microsoft-com:office:spreadsheet";
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    }

    abstract protected void setContentType(Metadata contentType);
}

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java

    final static String A = "a";
    final static String BODY = "body";
    final static String BR = "br";
    final static String CDATA = "cdata";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java

    final static String DOCUMENT_PROPERTIES = "DocumentProperties";
    final static String PICT = "pict";
    final static String BIN_DATA = "binData";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String HLINK = "hlink";
    final static String HLINK_DEST = "dest";
    final static String NAME_ATTR = "name";

    final static char[] NEWLINE = new char[]{'\n'};
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/AbstractXML2003Parser.java`
#### Snippet
```java
    final static String MS_VML_URN = "urn:schemas-microsoft-com:vml";
    final static String WORD_ML_URL = "http://schemas.microsoft.com/office/word/2003/wordml";
    final static Attributes EMPTY_ATTRS = new AttributesImpl();

    final static String DOCUMENT_PROPERTIES = "DocumentProperties";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
class RTFObjDataParser {

    private final static String WIN_ASCII = "WINDOWS-1252";
    private final int memoryLimitInKb;

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
public class SpreadsheetMLParser extends AbstractXML2003Parser {

    final static String CELL = "cell";
    final static String DATA = "data";
    final static String ROW = "row";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java

    final static String CELL = "cell";
    final static String DATA = "data";
    final static String ROW = "row";
    final static String WORKSHEET = "worksheet";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
    final static String DATA = "data";
    final static String ROW = "row";
    final static String WORKSHEET = "worksheet";

    private static final MediaType MEDIA_TYPE = MediaType.application("vnd.ms-spreadsheetml");
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
    final static String CELL = "cell";
    final static String DATA = "data";
    final static String ROW = "row";
    final static String WORKSHEET = "worksheet";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java

    //include all parts that might have embedded objects
    private final static String[] MAIN_PART_RELATIONS =
            new String[]{XWPFRelation.HEADER.getRelation(), XWPFRelation.FOOTER.getRelation(),
                    XWPFRelation.FOOTNOTE.getRelation(),
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java

    //a docx file should have one of these "main story" parts
    private final static String[] MAIN_STORY_PART_RELATIONS =
            new String[]{XWPFRelation.DOCUMENT.getContentType(),
                    XWPFRelation.MACRO_DOCUMENT.getContentType(),
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
    public final static XWPFListManager EMPTY_LIST = new EmptyListManager();
    private final static boolean OVERRIDE_AVAILABLE;
    private final static String SKIP_FORMAT = Character.toString((char) 61623);
//if this shows up as the lvlText, don't show a number

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
     * Always returns empty string.
     */
    public final static XWPFListManager EMPTY_LIST = new EmptyListManager();
    private final static boolean OVERRIDE_AVAILABLE;
    private final static String SKIP_FORMAT = Character.toString((char) 61623);
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
     */
    public final static XWPFListManager EMPTY_LIST = new EmptyListManager();
    private final static boolean OVERRIDE_AVAILABLE;
    private final static String SKIP_FORMAT = Character.toString((char) 61623);
//if this shows up as the lvlText, don't show a number
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
public class XSLFPowerPointExtractorDecorator extends AbstractOOXMLExtractor {

    private final static String HANDOUT_MASTER =
            "http://schemas.openxmlformats.org/officeDocument/2006/relationships/handoutMaster";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSExtractorDecorator.java`
#### Snippet
```java
    private class FixedDocSeqHandler extends DefaultHandler {
        private final static String DOCUMENT_REFERENCE = "DocumentReference";
        private final static String SOURCE = "Source";

        private final XHTMLContentHandler xhtml;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSExtractorDecorator.java`
#### Snippet
```java

    private class FixedDocSeqHandler extends DefaultHandler {
        private final static String DOCUMENT_REFERENCE = "DocumentReference";
        private final static String SOURCE = "Source";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
public class SXSLFPowerPointExtractorDecorator extends AbstractOOXMLExtractor {

    private final static String HANDOUT_MASTER =
            "http://schemas.openxmlformats.org/officeDocument/2006/relationships/handoutMaster";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java

    //a pptx file should have one of these "main story" parts
    private final static String[] MAIN_STORY_PART_RELATIONS =
            new String[]{XSLFRelation.MAIN.getContentType(),
                    XSLFRelation.PRESENTATION_MACRO.getContentType(),
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/RelationshipsHandler.java`
#### Snippet
```java
class RelationshipsHandler extends AbstractPartHandler {

    final static String REL_NS = "http://schemas.openxmlformats.org/package/2006/relationships";

    private final RelationshipsManager relationshipsManager;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/ExtendedPropertiesHandler.java`
#### Snippet
```java
class ExtendedPropertiesHandler extends CorePropertiesHandler {

    final static String EP_NS =
            "http://schemas.openxmlformats.org/officeDocument/2006/extended-properties";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/CorePropertiesHandler.java`
#### Snippet
```java

    final static String DC_NS = "http://purl.org/dc/elements/1.1";
    final static String DC_TERMS_NS = "http://purl.org/dc/terms";
    final static String CP_NS =
            "http://schemas.openxmlformats.org/package/2006/metadata/core-properties";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/CorePropertiesHandler.java`
#### Snippet
```java
class CorePropertiesHandler extends AbstractPartHandler {

    final static String DC_NS = "http://purl.org/dc/elements/1.1";
    final static String DC_TERMS_NS = "http://purl.org/dc/terms";
    final static String CP_NS =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/CorePropertiesHandler.java`
#### Snippet
```java
    final static String DC_NS = "http://purl.org/dc/elements/1.1";
    final static String DC_TERMS_NS = "http://purl.org/dc/terms";
    final static String CP_NS =
            "http://schemas.openxmlformats.org/package/2006/metadata/core-properties";
    final StringBuilder buffer = new StringBuilder();
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java

    //include all parts that might have embedded objects
    private final static String[] MAIN_PART_RELATIONS =
            new String[]{XWPFRelation.HEADER.getRelation(), XWPFRelation.FOOTER.getRelation(),
                    XWPFRelation.FOOTNOTE.getRelation(),
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/Word2006MLDocHandler.java`
#### Snippet
```java
class Word2006MLDocHandler extends DefaultHandler {

    final static String PKG_NS = "http://schemas.microsoft.com/office/2006/xmlPackage";


```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
    }

    final static class GlyphRun {

        //TODO: use name in conjunction with Frag information
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String OFFICE_DOC_RELATIONSHIP_NS =
            "http://schemas.openxmlformats.org/officeDocument/2006/relationships";
    private final static char[] TAB_CHAR = new char[]{'\t'};
    private final static char NEWLINE = '\n';
    private final static String BOOKMARK_START = "bookmarkStart";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String B = "b";
    private final static String ILVL = "ilvl";
    private final static String NUM_ID = "numId";
    private final static String TC = "tc";
    private final static String TR = "tr";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String BOOKMARK_END = "bookmarkEnd";
    private final static String FOOTNOTE_REFERENCE = "footnoteReference";
    private final static String INS = "ins";
    private final static String DEL = "del";
    private final static String DEL_TEXT = "delText";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String O_NS = "urn:schemas-microsoft-com:office:office";
    private final static String PIC_NS = "http://schemas.openxmlformats.org/drawingml/2006/picture";
    private final static String DRAWING_MAIN_NS =
            "http://schemas.openxmlformats.org/drawingml/2006/main";
    private final static String V_NS = "urn:schemas-microsoft-com:vml";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String HYPERLINK = "hyperlink";
    private final static String HLINK_CLICK = "hlinkClick"; //pptx hlink
    private final static String TBL = "tbl";
    private final static String PIC = "pic";
    private final static String PICT = "pict";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String CHOICE = "Choice";
    private final static String FALLBACK = "Fallback";
    private final static String OLE_OBJECT = "OLEObject";
    private final static String CR = "cr";
    private final static String V = "v";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String TAB = "tab";
    private final static String B = "b";
    private final static String ILVL = "ilvl";
    private final static String NUM_ID = "numId";
    private final static String TC = "tc";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java


    public final static String W_NS =
            "http://schemas.openxmlformats.org/wordprocessingml/2006/main";
    private final static String R = "r";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String BOOKMARK_START = "bookmarkStart";
    private final static String BOOKMARK_END = "bookmarkEnd";
    private final static String FOOTNOTE_REFERENCE = "footnoteReference";
    private final static String INS = "ins";
    private final static String DEL = "del";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String IMAGEDATA = "imagedata";
    private final static String BLIP = "blip";
    private final static String CHOICE = "Choice";
    private final static String FALLBACK = "Fallback";
    private final static String OLE_OBJECT = "OLEObject";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String P = "p";
    private final static String P_STYLE = "pStyle";
    private final static String PPR = "pPr";
    private final static String T = "t";
    private final static String TAB = "tab";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
            "http://schemas.openxmlformats.org/markup-compatibility/2006";
    private final static String O_NS = "urn:schemas-microsoft-com:office:office";
    private final static String PIC_NS = "http://schemas.openxmlformats.org/drawingml/2006/picture";
    private final static String DRAWING_MAIN_NS =
            "http://schemas.openxmlformats.org/drawingml/2006/main";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String U = "u";
    private final static String STRIKE = "strike";
    private final static String NUM_PR = "numPr";
    private final static String BR = "br";
    private final static String HYPERLINK = "hyperlink";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String V = "v";
    private final static String RUBY = "ruby"; //phonetic section
    private final static String RT = "rt"; //phonetic run
    private static final String VAL = "val";
    private final static String MC_NS =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String P_STYLE = "pStyle";
    private final static String PPR = "pPr";
    private final static String T = "t";
    private final static String TAB = "tab";
    private final static String B = "b";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String BLIP = "blip";
    private final static String CHOICE = "Choice";
    private final static String FALLBACK = "Fallback";
    private final static String OLE_OBJECT = "OLEObject";
    private final static String CR = "cr";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String RPR = "rPr";
    private final static String P = "p";
    private final static String P_STYLE = "pStyle";
    private final static String PPR = "pPr";
    private final static String T = "t";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String MOVE_FROM = "moveFrom";
    private final static String MOVE_TO = "moveTo";
    private final static String ENDNOTE_REFERENCE = "endnoteReference";
    private static final String TEXTBOX = "textbox";
    private final XWPFBodyContentsHandler bodyContentsHandler;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String MC_NS =
            "http://schemas.openxmlformats.org/markup-compatibility/2006";
    private final static String O_NS = "urn:schemas-microsoft-com:office:office";
    private final static String PIC_NS = "http://schemas.openxmlformats.org/drawingml/2006/picture";
    private final static String DRAWING_MAIN_NS =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String OLE_OBJECT = "OLEObject";
    private final static String CR = "cr";
    private final static String V = "v";
    private final static String RUBY = "ruby"; //phonetic section
    private final static String RT = "rt"; //phonetic run
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String BR = "br";
    private final static String HYPERLINK = "hyperlink";
    private final static String HLINK_CLICK = "hlinkClick"; //pptx hlink
    private final static String TBL = "tbl";
    private final static String PIC = "pic";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static char NEWLINE = '\n';
    private final static String BOOKMARK_START = "bookmarkStart";
    private final static String BOOKMARK_END = "bookmarkEnd";
    private final static String FOOTNOTE_REFERENCE = "footnoteReference";
    private final static String INS = "ins";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String ILVL = "ilvl";
    private final static String NUM_ID = "numId";
    private final static String TC = "tc";
    private final static String TR = "tr";
    private final static String I = "i";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    public final static String W_NS =
            "http://schemas.openxmlformats.org/wordprocessingml/2006/main";
    private final static String R = "r";
    private final static String FLD = "fld";
    private final static String RPR = "rPr";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
            "http://schemas.openxmlformats.org/officeDocument/2006/relationships";
    private final static char[] TAB_CHAR = new char[]{'\t'};
    private final static char NEWLINE = '\n';
    private final static String BOOKMARK_START = "bookmarkStart";
    private final static String BOOKMARK_END = "bookmarkEnd";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String T = "t";
    private final static String TAB = "tab";
    private final static String B = "b";
    private final static String ILVL = "ilvl";
    private final static String NUM_ID = "numId";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String R = "r";
    private final static String FLD = "fld";
    private final static String RPR = "rPr";
    private final static String P = "p";
    private final static String P_STYLE = "pStyle";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String FALLBACK = "Fallback";
    private final static String OLE_OBJECT = "OLEObject";
    private final static String CR = "cr";
    private final static String V = "v";
    private final static String RUBY = "ruby"; //phonetic section
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String NUM_ID = "numId";
    private final static String TC = "tc";
    private final static String TR = "tr";
    private final static String I = "i";
    private final static String U = "u";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String TBL = "tbl";
    private final static String PIC = "pic";
    private final static String PICT = "pict";
    private final static String IMAGEDATA = "imagedata";
    private final static String BLIP = "blip";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static char[] TAB_CHAR = new char[]{'\t'};
    private final static char NEWLINE = '\n';
    private final static String BOOKMARK_START = "bookmarkStart";
    private final static String BOOKMARK_END = "bookmarkEnd";
    private final static String FOOTNOTE_REFERENCE = "footnoteReference";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String PICT = "pict";
    private final static String IMAGEDATA = "imagedata";
    private final static String BLIP = "blip";
    private final static String CHOICE = "Choice";
    private final static String FALLBACK = "Fallback";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String DEL = "del";
    private final static String DEL_TEXT = "delText";
    private final static String MOVE_FROM = "moveFrom";
    private final static String MOVE_TO = "moveTo";
    private final static String ENDNOTE_REFERENCE = "endnoteReference";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String PPR = "pPr";
    private final static String T = "t";
    private final static String TAB = "tab";
    private final static String B = "b";
    private final static String ILVL = "ilvl";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String RT = "rt"; //phonetic run
    private static final String VAL = "val";
    private final static String MC_NS =
            "http://schemas.openxmlformats.org/markup-compatibility/2006";
    private final static String O_NS = "urn:schemas-microsoft-com:office:office";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String V_NS = "urn:schemas-microsoft-com:vml";
    private final static String C_NS = "http://schemas.openxmlformats.org/drawingml/2006/chart";
    private final static String OFFICE_DOC_RELATIONSHIP_NS =
            "http://schemas.openxmlformats.org/officeDocument/2006/relationships";
    private final static char[] TAB_CHAR = new char[]{'\t'};
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String DRAWING_MAIN_NS =
            "http://schemas.openxmlformats.org/drawingml/2006/main";
    private final static String V_NS = "urn:schemas-microsoft-com:vml";
    private final static String C_NS = "http://schemas.openxmlformats.org/drawingml/2006/chart";
    private final static String OFFICE_DOC_RELATIONSHIP_NS =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String TR = "tr";
    private final static String I = "i";
    private final static String U = "u";
    private final static String STRIKE = "strike";
    private final static String NUM_PR = "numPr";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String FLD = "fld";
    private final static String RPR = "rPr";
    private final static String P = "p";
    private final static String P_STYLE = "pStyle";
    private final static String PPR = "pPr";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String TC = "tc";
    private final static String TR = "tr";
    private final static String I = "i";
    private final static String U = "u";
    private final static String STRIKE = "strike";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
            "http://schemas.openxmlformats.org/drawingml/2006/main";
    private final static String V_NS = "urn:schemas-microsoft-com:vml";
    private final static String C_NS = "http://schemas.openxmlformats.org/drawingml/2006/chart";
    private final static String OFFICE_DOC_RELATIONSHIP_NS =
            "http://schemas.openxmlformats.org/officeDocument/2006/relationships";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String STRIKE = "strike";
    private final static String NUM_PR = "numPr";
    private final static String BR = "br";
    private final static String HYPERLINK = "hyperlink";
    private final static String HLINK_CLICK = "hlinkClick"; //pptx hlink
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String I = "i";
    private final static String U = "u";
    private final static String STRIKE = "strike";
    private final static String NUM_PR = "numPr";
    private final static String BR = "br";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
            "http://schemas.openxmlformats.org/wordprocessingml/2006/main";
    private final static String R = "r";
    private final static String FLD = "fld";
    private final static String RPR = "rPr";
    private final static String P = "p";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String FOOTNOTE_REFERENCE = "footnoteReference";
    private final static String INS = "ins";
    private final static String DEL = "del";
    private final static String DEL_TEXT = "delText";
    private final static String MOVE_FROM = "moveFrom";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String INS = "ins";
    private final static String DEL = "del";
    private final static String DEL_TEXT = "delText";
    private final static String MOVE_FROM = "moveFrom";
    private final static String MOVE_TO = "moveTo";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String PIC = "pic";
    private final static String PICT = "pict";
    private final static String IMAGEDATA = "imagedata";
    private final static String BLIP = "blip";
    private final static String CHOICE = "Choice";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String NUM_PR = "numPr";
    private final static String BR = "br";
    private final static String HYPERLINK = "hyperlink";
    private final static String HLINK_CLICK = "hlinkClick"; //pptx hlink
    private final static String TBL = "tbl";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String CR = "cr";
    private final static String V = "v";
    private final static String RUBY = "ruby"; //phonetic section
    private final static String RT = "rt"; //phonetic run
    private static final String VAL = "val";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String HLINK_CLICK = "hlinkClick"; //pptx hlink
    private final static String TBL = "tbl";
    private final static String PIC = "pic";
    private final static String PICT = "pict";
    private final static String IMAGEDATA = "imagedata";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final static String DEL_TEXT = "delText";
    private final static String MOVE_FROM = "moveFrom";
    private final static String MOVE_TO = "moveTo";
    private final static String ENDNOTE_REFERENCE = "endnoteReference";
    private static final String TEXTBOX = "textbox";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-core/src/main/java/org/apache/tika/io/FilenameUtils.java`
#### Snippet
```java
                    0x1B, 0x1C, 0x1D, 0x1E, 0x1F, '?', ':', '*', '<', '>', '|'};

    private final static HashSet<Character> RESERVED = new HashSet<>(38);


```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-core/src/main/java/org/apache/tika/io/FilenameUtils.java`
#### Snippet
```java
     * Reserved characters
     */
    public final static char[] RESERVED_FILENAME_CHARACTERS =
            {0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08, 0x09, 0x0A, 0x0B, 0x0C, 0x0D,
                    0x0E, 0x0F, 0x10, 0x11, 0x12, 0x13, 0x14, 0x15, 0x16, 0x17, 0x18, 0x19, 0x1A,
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-core/src/main/java/org/apache/tika/io/BoundedInputStream.java`
#### Snippet
```java


    private final static int EOF = -1;
    private final long max;
    private final InputStream in;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-core/src/main/java/org/apache/tika/sax/AbstractRecursiveParserWrapperHandler.java`
#### Snippet
```java
        implements Serializable {

    public final static Property EMBEDDED_RESOURCE_LIMIT_REACHED = Property.internalBoolean(
            TikaCoreProperties.TIKA_META_EXCEPTION_PREFIX + "embedded_resource_limit_reached");
    private static final int MAX_DEPTH = 100;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-core/src/main/java/org/apache/tika/utils/ParserUtils.java`
#### Snippet
```java
public class ParserUtils {

    public final static Property EMBEDDED_PARSER = Property.internalText(
            TikaCoreProperties.TIKA_META_EXCEPTION_PREFIX + "embedded_parser");

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-core/src/main/java/org/apache/tika/utils/ExceptionUtils.java`
#### Snippet
```java
public class ExceptionUtils {

    private final static Pattern MSG_PATTERN = Pattern.compile(":[^\r\n]+");

    /**
```

### MissortedModifiers
Missorted modifiers `transient final`
in `tika-core/src/main/java/org/apache/tika/detect/DefaultProbDetector.java`
#### Snippet
```java
public class DefaultProbDetector extends CompositeDetector {
    private static final long serialVersionUID = -8836240060532323352L;
    private transient final ServiceLoader loader;

    public DefaultProbDetector(ProbabilisticMimeDetectionSelector sel, ServiceLoader loader) {
```

### MissortedModifiers
Missorted modifiers `transient final`
in `tika-core/src/main/java/org/apache/tika/detect/DefaultDetector.java`
#### Snippet
```java
     */
    private static final long serialVersionUID = -8170114575326908027L;
    private transient final ServiceLoader loader;

    public DefaultDetector(MimeTypes types, ServiceLoader loader,
```

### MissortedModifiers
Missorted modifiers `transient final`
in `tika-core/src/main/java/org/apache/tika/parser/DefaultParser.java`
#### Snippet
```java
     */
    private static final long serialVersionUID = 3612324825403757520L;
    private transient final ServiceLoader loader;

    public DefaultParser(MediaTypeRegistry registry, ServiceLoader loader,
```

### MissortedModifiers
Missorted modifiers `transient final`
in `tika-core/src/main/java/org/apache/tika/language/translate/DefaultTranslator.java`
#### Snippet
```java
 */
public class DefaultTranslator implements Translator {
    private transient final ServiceLoader loader;

    public DefaultTranslator(ServiceLoader loader) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-core/src/main/java/org/apache/tika/exception/WriteLimitReachedException.java`
#### Snippet
```java

    //in case of (hopefully impossible) cyclic exception
    private final static int MAX_DEPTH = 100;

    private final int writeLimit;
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
    }

    private static abstract class XmlLoader<CT, T> {
        protected static final String PARAMS_TAG_NAME = "params";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
    //we can remove all of this. See TIKA-2591.
    final static MediaType TIFF = MediaType.image("tiff");
    final static byte[][] TIFF_SIGNATURES = new byte[3][];
    /**
     * Serial version UID
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
    //If commons-compress ever chooses to take over TIFF detection
    //we can remove all of this. See TIKA-2591.
    final static MediaType TIFF = MediaType.image("tiff");
    final static byte[][] TIFF_SIGNATURES = new byte[3][];
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFColumnHeader.java`
#### Snippet
```java
class DBFColumnHeader {

    private final static Map<Integer, ColType> COL_TYPE_MAP = new ConcurrentHashMap<>();

    static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFContentHandler.java`
#### Snippet
```java
public class MIFContentHandler extends DefaultHandler {

    private final static Attributes EMPTY_ATTRIBUTES = new AttributesImpl();

    private final ContentHandler handler;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/indesign/ContentAndMetadataExtractor.java`
#### Snippet
```java
class ContentAndMetadataExtractor {

    private final static Attributes EMPTY_ATTRIBUTES = new AttributesImpl();

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/QuattroProParser.java`
#### Snippet
```java
    private static final long serialVersionUID = 8941810225917012232L;

    private final static MediaType QP_BASE = MediaType.application("x-quattro-pro");
    public final static MediaType QP_9 = new MediaType(QP_BASE, "version", "9");
    public static final MediaType QP_7_8 = new MediaType(QP_BASE, "version", "7-8");
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/QuattroProParser.java`
#### Snippet
```java

    private final static MediaType QP_BASE = MediaType.application("x-quattro-pro");
    public final static MediaType QP_9 = new MediaType(QP_BASE, "version", "9");
    public static final MediaType QP_7_8 = new MediaType(QP_BASE, "version", "7-8");

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WordPerfectParser.java`
#### Snippet
```java

    final static MediaType WP_BASE = MediaType.application("vnd.wordperfect");
    final static MediaType WP_UNK = new MediaType(WP_BASE, "version", "unknown");
    final static MediaType WP_5_0 = new MediaType(WP_BASE, "version", "5.0");
    final static MediaType WP_5_1 = new MediaType(WP_BASE, "version", "5.1");
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WordPerfectParser.java`
#### Snippet
```java
public class WordPerfectParser extends AbstractParser {

    final static MediaType WP_BASE = MediaType.application("vnd.wordperfect");
    final static MediaType WP_UNK = new MediaType(WP_BASE, "version", "unknown");
    final static MediaType WP_5_0 = new MediaType(WP_BASE, "version", "5.0");
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WordPerfectParser.java`
#### Snippet
```java
    final static MediaType WP_UNK = new MediaType(WP_BASE, "version", "unknown");
    final static MediaType WP_5_0 = new MediaType(WP_BASE, "version", "5.0");
    final static MediaType WP_5_1 = new MediaType(WP_BASE, "version", "5.1");
    final static MediaType WP_6_x = new MediaType(WP_BASE, "version", "6.x");
    private static final long serialVersionUID = 8941810225917012232L;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WordPerfectParser.java`
#### Snippet
```java
    final static MediaType WP_5_0 = new MediaType(WP_BASE, "version", "5.0");
    final static MediaType WP_5_1 = new MediaType(WP_BASE, "version", "5.1");
    final static MediaType WP_6_x = new MediaType(WP_BASE, "version", "6.x");
    private static final long serialVersionUID = 8941810225917012232L;
    private static final Set<MediaType> SUPPORTED_TYPES =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WordPerfectParser.java`
#### Snippet
```java
    final static MediaType WP_BASE = MediaType.application("vnd.wordperfect");
    final static MediaType WP_UNK = new MediaType(WP_BASE, "version", "unknown");
    final static MediaType WP_5_0 = new MediaType(WP_BASE, "version", "5.0");
    final static MediaType WP_5_1 = new MediaType(WP_BASE, "version", "5.1");
    final static MediaType WP_6_x = new MediaType(WP_BASE, "version", "6.x");
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/util/EvalExceptionUtils.java`
#### Snippet
```java
    //these remove runtime info from the stacktraces so
    //that actual causes can be counted.
    private final static Pattern CAUSED_BY_SNIPPER =
            Pattern.compile("(Caused by: [^:]+):[^\\r\\n]+");

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerManager.java`
#### Snippet
```java
public class AnalyzerManager {

    private final static String GENERAL = "general";
    private static final String ALPHA_IDEOGRAPH = "alpha";
    private static final String COMMON_TOKENS = "common_tokens";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
     * the embedded document tree.
     */
    private final static int MAX_RECURSION_DEPTH = 100;
    private final static int MAX_BOOKMARK_ITEMS = 10000;

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
    //This is used for both types and subtypes.
    //These can be unbounded.  We need to limit the number we store.
    private final static int MAX_ANNOTATION_TYPES = 100;
    private static final String THREE_D = "3D";
    private static final COSName THREE_DD = COSName.getPDFName("3DD");
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
     */
    private final static int MAX_RECURSION_DEPTH = 100;
    private final static int MAX_BOOKMARK_ITEMS = 10000;

    //This is used for both types and subtypes.
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * @throws java.io.IOException
     */
    abstract protected String getConnectionString(InputStream stream, Metadata metadata,
                                                  ParseContext parseContext) throws IOException;

```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     */
    @Deprecated
    abstract protected JDBCTableReader getTableReader(Connection connection, String tableName,
                                                      ParseContext parseContext);

```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * @return
     */
    abstract protected JDBCTableReader getTableReader(Connection connection, String tableName,
                                                      EmbeddedDocumentUtil embeddedDocumentUtil);

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
public abstract class AbstractDBParser extends AbstractParser {

    private final static byte[] EMPTY_BYTE_ARR = new byte[0];

    private Connection connection;
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * @throws java.sql.SQLException
     */
    abstract protected List<String> getTableNames(Connection connection, Metadata metadata,
                                                  ParseContext context) throws SQLException;

```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * @return jdbc class name
     */
    abstract protected String getJDBCClassName();

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/JDBCTableReader.java`
#### Snippet
```java
public class JDBCTableReader {

    private final static Attributes EMPTY_ATTRIBUTES = new AttributesImpl();
    private final Connection connection;
    private final String tableName;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The maximum length allowed for a ngram.
     */
    final static int ABSOLUTE_MAX_NGRAM_LENGTH = 3; /* was 4 */
    /**
     * The default min length of ngram
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The profile max size (number of ngrams of the same size)
     */
    final static int MAX_SIZE = 1000;
    /**
     * separator char
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The ngram profile file extension
     */
    final static String FILE_EXTENSION = "ngp";
    /**
     * The profile max size (number of ngrams of the same size)
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The String form of the separator char
     */
    private final static String SEP_CHARSEQ = new String(new char[]{SEPARATOR});
    /**
     * A StringBuffer used during analysis
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The minimum length allowed for a ngram.
     */
    final static int ABSOLUTE_MIN_NGRAM_LENGTH = 3; /* was 1 */
    /**
     * The maximum length allowed for a ngram.
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * separator char
     */
    final static char SEPARATOR = '_';
    private static final Logger LOG = LoggerFactory.getLogger(LanguageProfilerBuilder.class);
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The default min length of ngram
     */
    final static int DEFAULT_MIN_NGRAM_LENGTH = 3;
    /**
     * The default max length of ngram
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The default max length of ngram
     */
    final static int DEFAULT_MAX_NGRAM_LENGTH = 3;
    /**
     * The ngram profile file extension
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/H2Util.java`
#### Snippet
```java
public class H2Util extends JDBCUtil {

    private final static String DRIVER_CLASS = "org.h2.Driver";

    private final Path db;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/XMLLogReader.java`
#### Snippet
```java
    static class LogXMLWrappingInputStream extends InputStream {
        //plagiarized from log4j's chainsaw
        private final static String HEADER =
                "<?xml version=\"1.0\" encoding=\"UTF-8\" ?>" + "<log4j:eventSet version=\"1.2\" " +
                        "xmlns:log4j=\"http://jakarta.apache.org/log4j/\">";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractProfiler.java`
#### Snippet
```java
public class ExtractProfiler extends AbstractProfiler {

    private final static String FIELD = "f";
    public static TableInfo EXTRACT_EXCEPTION_TABLE =
            new TableInfo("extract_exceptions", new ColInfo(Cols.CONTAINER_ID, Types.INTEGER),
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/ExtractProfilerBuilder.java`
#### Snippet
```java
public class ExtractProfilerBuilder extends EvalConsumerBuilder {

    public final static String TABLE_PREFIX_KEY = "tablePrefix";

    private final List<TableInfo> tableInfos;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/ExtractComparerBuilder.java`
#### Snippet
```java
public class ExtractComparerBuilder extends EvalConsumerBuilder {
    public final static String TABLE_PREFIX_A_KEY = "tablePrefixA";
    public final static String TABLE_PREFIX_B_KEY = "tablePrefixB";

    private final List<TableInfo> tableInfosA;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/ExtractComparerBuilder.java`
#### Snippet
```java

public class ExtractComparerBuilder extends EvalConsumerBuilder {
    public final static String TABLE_PREFIX_A_KEY = "tablePrefixA";
    public final static String TABLE_PREFIX_B_KEY = "tablePrefixB";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/FileProfilerBuilder.java`
#### Snippet
```java
public class FileProfilerBuilder extends EvalConsumerBuilder {

    public final static String TABLE_PREFIX_KEY = "tablePrefix";

    private final List<TableInfo> tableInfos;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
    private static final String DIGEST_KEY_PREFIX = TikaCoreProperties.TIKA_META_PREFIX + "digest" +
            TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER;
    private final static String FIELD_A = "fa";
    private final static String FIELD_B = "fb";
    public static TableInfo REF_PAIR_NAMES =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
            TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER;
    private final static String FIELD_A = "fa";
    private final static String FIELD_B = "fb";
    public static TableInfo REF_PAIR_NAMES =
            new TableInfo("pair_names", new ColInfo(Cols.DIR_NAME_A, Types.VARCHAR, 128),
```

### MissortedModifiers
Missorted modifiers `synchronized static`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/regex/RegexNERecogniser.java`
#### Snippet
```java
    }

    public synchronized static RegexNERecogniser getInstance() {
        if (INSTANCE == null) {
            INSTANCE = new RegexNERecogniser();
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    private static final String DIGEST_KEY = "X-TIKA:digest:MD5";
    private static final Map<String, Cols> UC_TAGS_OF_INTEREST = initTags();
    private final static Pattern ACCESS_PERMISSION_EXCEPTION =
            Pattern.compile("org\\.apache\\.tika\\.exception\\.AccessPermissionException");
    private final static Pattern ENCRYPTION_EXCEPTION =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    private final static Pattern ACCESS_PERMISSION_EXCEPTION =
            Pattern.compile("org\\.apache\\.tika\\.exception\\.AccessPermissionException");
    private final static Pattern ENCRYPTION_EXCEPTION =
            Pattern.compile("org\\.apache\\.tika.exception\\.EncryptedDocumentException");
    public static TableInfo REF_EXTRACT_EXCEPTION_TYPES =
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
    protected static final AtomicInteger ID = new AtomicInteger();
    static final long NON_EXISTENT_FILE_LENGTH = -1l;
    final static int FILE_PATH_MAX_LEN = 1024;//max len for varchar for file_path
    private static final Logger LOG = LoggerFactory.getLogger(AbstractProfiler.class);
    private static final String[] EXTRACT_EXTENSIONS = {".json", ".txt", ""};
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java

    // UMLS password property
    private final static String CTAKES_UMLS_PASS = "ctakes.umlspw";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
public class CTAKESUtils {
    // UMLS username property
    private final static String CTAKES_UMLS_USER = "ctakes.umlsuser";

    // UMLS password property
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
public class MailUtil {

    private final static Pattern EMAIL = Pattern.compile("(?i)([^<@\\s]+@[^@> ]+)");

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java

    protected final static int SKIPPED = 0;
    protected final static int ADDED = 1;
    protected final static int STOP_NOW = 2;
    //how long to pause if can't add to queue
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
    protected final static int SKIPPED = 0;
    protected final static int ADDED = 1;
    protected final static int STOP_NOW = 2;
    //how long to pause if can't add to queue
    private static final long PAUSE_INCREMENT_MILLIS = 1000;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
            LoggerFactory.getLogger(FileResourceCrawler.class.toString());

    protected final static int SKIPPED = 0;
    protected final static int ADDED = 1;
    protected final static int STOP_NOW = 2;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSProperties.java`
#### Snippet
```java
     * File's relative path (including file name) from a given source root
     */
    public final static Property FS_REL_PATH =
            Property.internalText(TIKA_BATCH_FS_NAMESPACE + ":relative_path");
}
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSProperties.java`
#### Snippet
```java

public class FSProperties {
    private final static String TIKA_BATCH_FS_NAMESPACE = "tika_batch_fs";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java
public class FSUtil {

    private final static Pattern FILE_NAME_PATTERN =
            Pattern.compile("\\A(.*?)(?:\\((\\d+)\\))?\\.([^\\.]+)\\Z");

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java

    private final static String INCLUDE_FILE_PAT_ATTR = "includeFilePat";
    private final static String EXCLUDE_FILE_PAT_ATTR = "excludeFilePat";

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java

    private final static String CRAWL_ORDER = "crawlOrder";
    private final static String INPUT_DIR_ATTR = "inputDir";
    private final static String INPUT_START_DIR_ATTR = "startDir";
    private final static String MAX_FILE_SIZE_BYTES_ATTR = "maxFileSizeBytes";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java


    private final static String INCLUDE_FILE_PAT_ATTR = "includeFilePat";
    private final static String EXCLUDE_FILE_PAT_ATTR = "excludeFilePat";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
    private final static String INPUT_START_DIR_ATTR = "startDir";
    private final static String MAX_FILE_SIZE_BYTES_ATTR = "maxFileSizeBytes";
    private final static String MIN_FILE_SIZE_BYTES_ATTR = "minFileSizeBytes";


```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java


    private final static String CRAWL_ORDER = "crawlOrder";
    private final static String INPUT_DIR_ATTR = "inputDir";
    private final static String INPUT_START_DIR_ATTR = "startDir";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
    private final static String MAX_CONSEC_WAIT_MILLIS = "maxConsecWaitMillis";
    private final static String MAX_FILES_TO_ADD_ATTR = "maxFilesToAdd";
    private final static String MAX_FILES_TO_CONSIDER_ATTR = "maxFilesToConsider";


```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
public class FSCrawlerBuilder implements ICrawlerBuilder {

    private final static String MAX_CONSEC_WAIT_MILLIS = "maxConsecWaitMillis";
    private final static String MAX_FILES_TO_ADD_ATTR = "maxFilesToAdd";
    private final static String MAX_FILES_TO_CONSIDER_ATTR = "maxFilesToConsider";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
    private final static String INPUT_DIR_ATTR = "inputDir";
    private final static String INPUT_START_DIR_ATTR = "startDir";
    private final static String MAX_FILE_SIZE_BYTES_ATTR = "maxFileSizeBytes";
    private final static String MIN_FILE_SIZE_BYTES_ATTR = "minFileSizeBytes";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java
    private final static String CRAWL_ORDER = "crawlOrder";
    private final static String INPUT_DIR_ATTR = "inputDir";
    private final static String INPUT_START_DIR_ATTR = "startDir";
    private final static String MAX_FILE_SIZE_BYTES_ATTR = "maxFileSizeBytes";
    private final static String MIN_FILE_SIZE_BYTES_ATTR = "minFileSizeBytes";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java

    private final static String MAX_CONSEC_WAIT_MILLIS = "maxConsecWaitMillis";
    private final static String MAX_FILES_TO_ADD_ATTR = "maxFilesToAdd";
    private final static String MAX_FILES_TO_CONSIDER_ATTR = "maxFilesToConsider";

```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/AbstractConverter.java`
#### Snippet
```java
     * @return the additional namespace information
     */
    abstract protected Set<Namespace> getAdditionalNamespaces();

    public void setMetadata(Metadata metadata) {
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/AbstractConverter.java`
#### Snippet
```java
    }

    abstract public XMPMeta process(Metadata metadata) throws XMPException;

    /**
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_Unicode.java`
#### Snippet
```java
    }

    static abstract class CharsetRecog_UTF_32 extends CharsetRecog_Unicode {
        abstract int getChar(byte[] input, int index);

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java

    public final static String PRESENTATION_WIDTH = "slides-width";
    public final static String PRESENTATION_HEIGHT = "slides-height";

    private final XHTMLContentHandler xhtml;
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
class KeynoteContentHandler extends DefaultHandler {

    public final static String PRESENTATION_WIDTH = "slides-width";
    public final static String PRESENTATION_HEIGHT = "slides-height";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/IWorkPackageParser.java`
#### Snippet
```java
     * This parser handles all iWorks formats.
     */
    private final static Set<MediaType> supportedTypes = Collections.unmodifiableSet(
            new HashSet<>(Arrays.asList(MediaType.application("vnd.apple.iwork"),
                    IWORKDocumentType.KEYNOTE.getType(), IWORKDocumentType.NUMBERS.getType(),
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/IWorkPackageParser.java`
#### Snippet
```java
     * All iWork files contain one of these, so we can detect based on it
     */
    public final static String IWORK_COMMON_ENTRY = "buildVersionHistory.plist";
    /**
     * Serial version UID
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/IWorkPackageParser.java`
#### Snippet
```java
     * Which files within an iWork file contain the actual content?
     */
    public final static Set<String> IWORK_CONTENT_ENTRIES = Collections.unmodifiableSet(
            new HashSet<>(Arrays.asList("index.apxl", "index.xml", "presentation.apxl")));
    /**
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
    }

    static abstract class CharsetRecog_EBCDIC_500 extends CharsetRecog_sbcs {

        // This maps EBCDIC 500 codepoints onto either space (not of interest), or a lower
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/builders/BatchProcessBuilder.java`
#### Snippet
```java
    public final static int DEFAULT_MAX_QUEUE_SIZE = 1000;
    public final static String MAX_QUEUE_SIZE_KEY = "maxQueueSize";
    public final static String NUM_CONSUMERS_KEY = "numConsumers";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/builders/BatchProcessBuilder.java`
#### Snippet
```java

    public final static int DEFAULT_MAX_QUEUE_SIZE = 1000;
    public final static String MAX_QUEUE_SIZE_KEY = "maxQueueSize";
    public final static String NUM_CONSUMERS_KEY = "numConsumers";

```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-batch/src/main/java/org/apache/tika/batch/builders/BatchProcessBuilder.java`
#### Snippet
```java
public class BatchProcessBuilder {

    public final static int DEFAULT_MAX_QUEUE_SIZE = 1000;
    public final static String MAX_QUEUE_SIZE_KEY = "maxQueueSize";
    public final static String NUM_CONSUMERS_KEY = "numConsumers";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork18PackageParser.java`
#### Snippet
```java
public class IWork18PackageParser extends AbstractParser {

    private final static Set<MediaType> supportedTypes = Collections.unmodifiableSet(
            new HashSet<>(Arrays.asList(IWork18DocumentType.KEYNOTE18.getType(),
                    IWork18DocumentType.NUMBERS18.getType(),
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork13PackageParser.java`
#### Snippet
```java


    private final static Set<MediaType> supportedTypes = Collections.unmodifiableSet(
            new HashSet<>(Arrays.asList(IWork13DocumentType.KEYNOTE13.getType(),
                    IWork13DocumentType.NUMBERS13.getType(),
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork13PackageParser.java`
#### Snippet
```java
     * All iWork 13 files contain this, so we can detect based on it
     */
    public final static String IWORK13_COMMON_ENTRY = "Metadata/BuildVersionHistory.plist";

    public static final String IWORKS_PREFIX = "iworks:";
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char FS = 0x1F;    // a field delimiter
    private final static char HY = 0x2D;    // hyphen
    private final static char SP = 0x20;    // the blank space
    private final static char LT = 0x3C;    // less than
    private final static char EQ = 0x3D;    // less than
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char SYN = 0x16;    // synchronous idle (ctrl-v)
    private final static char BS = 0x08;    // the backspace character (used for diacriticals)
    private final static char TB = 0x09;    // the tab character
    private final static char LF = 0x0A;    // line feed
    private final static char FF = 0x0C;    // form feed
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char EOT = 0x04;    // the tab character (ctrl-d)
    private final static char SYN = 0x16;    // synchronous idle (ctrl-v)
    private final static char BS = 0x08;    // the backspace character (used for diacriticals)
    private final static char TB = 0x09;    // the tab character
    private final static char LF = 0x0A;    // line feed
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char STX = 0x02;    // start of text (ctrl-b)
    private final static char ETX = 0x03;    // end of text (ctrl-c)
    private final static char EOT = 0x04;    // the tab character (ctrl-d)
    private final static char SYN = 0x16;    // synchronous idle (ctrl-v)
    private final static char BS = 0x08;    // the backspace character (used for diacriticals)
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private static final Set<MediaType> SUPPORTED_TYPES = Collections.singleton(TYPE);
    private final static char SOH = 0x01;    // start of header (ctrl-a)
    private final static char STX = 0x02;    // start of text (ctrl-b)
    private final static char ETX = 0x03;    // end of text (ctrl-c)
    private final static char EOT = 0x04;    // the tab character (ctrl-d)
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char LF = 0x0A;    // line feed
    private final static char FF = 0x0C;    // form feed
    private final static char CR = 0x0D;    // carriage return
    private final static char XQ = 0x11;    // device control (ctrl-q)
    private final static char XS = 0x13;    // device control (ctrl-s)
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char ETX = 0x03;    // end of text (ctrl-c)
    private final static char EOT = 0x04;    // the tab character (ctrl-d)
    private final static char SYN = 0x16;    // synchronous idle (ctrl-v)
    private final static char BS = 0x08;    // the backspace character (used for diacriticals)
    private final static char TB = 0x09;    // the tab character
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char FF = 0x0C;    // form feed
    private final static char CR = 0x0D;    // carriage return
    private final static char XQ = 0x11;    // device control (ctrl-q)
    private final static char XS = 0x13;    // device control (ctrl-s)
    private final static char FS = 0x1F;    // a field delimiter
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char SL = 0x91;    // single-quote left
    private final static char SR = 0x92;    // single-quote right
    private final static char DL = 0x93;    // double-quote left
    private final static char DR = 0x94;    // double-quote right
    private int FMT_ANPA_1312 = 0x00;   // "NAA 89-3 (ANPA 1312)"
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char SP = 0x20;    // the blank space
    private final static char LT = 0x3C;    // less than
    private final static char EQ = 0x3D;    // less than
    private final static char CT = 0x5E;    // carat
    private final static char SL = 0x91;    // single-quote left
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char TB = 0x09;    // the tab character
    private final static char LF = 0x0A;    // line feed
    private final static char FF = 0x0C;    // form feed
    private final static char CR = 0x0D;    // carriage return
    private final static char XQ = 0x11;    // device control (ctrl-q)
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char SOH = 0x01;    // start of header (ctrl-a)
    private final static char STX = 0x02;    // start of text (ctrl-b)
    private final static char ETX = 0x03;    // end of text (ctrl-c)
    private final static char EOT = 0x04;    // the tab character (ctrl-d)
    private final static char SYN = 0x16;    // synchronous idle (ctrl-v)
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char LT = 0x3C;    // less than
    private final static char EQ = 0x3D;    // less than
    private final static char CT = 0x5E;    // carat
    private final static char SL = 0x91;    // single-quote left
    private final static char SR = 0x92;    // single-quote right
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char XQ = 0x11;    // device control (ctrl-q)
    private final static char XS = 0x13;    // device control (ctrl-s)
    private final static char FS = 0x1F;    // a field delimiter
    private final static char HY = 0x2D;    // hyphen
    private final static char SP = 0x20;    // the blank space
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char EQ = 0x3D;    // less than
    private final static char CT = 0x5E;    // carat
    private final static char SL = 0x91;    // single-quote left
    private final static char SR = 0x92;    // single-quote right
    private final static char DL = 0x93;    // double-quote left
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

    private static final Set<MediaType> SUPPORTED_TYPES = Collections.singleton(TYPE);
    private final static char SOH = 0x01;    // start of header (ctrl-a)
    private final static char STX = 0x02;    // start of text (ctrl-b)
    private final static char ETX = 0x03;    // end of text (ctrl-c)
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char SR = 0x92;    // single-quote right
    private final static char DL = 0x93;    // double-quote left
    private final static char DR = 0x94;    // double-quote right
    private int FMT_ANPA_1312 = 0x00;   // "NAA 89-3 (ANPA 1312)"
    private int FMT_ANPA_UPI = 0x01;   // "United Press International ANPA 1312 variant"
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char XS = 0x13;    // device control (ctrl-s)
    private final static char FS = 0x1F;    // a field delimiter
    private final static char HY = 0x2D;    // hyphen
    private final static char SP = 0x20;    // the blank space
    private final static char LT = 0x3C;    // less than
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char CR = 0x0D;    // carriage return
    private final static char XQ = 0x11;    // device control (ctrl-q)
    private final static char XS = 0x13;    // device control (ctrl-s)
    private final static char FS = 0x1F;    // a field delimiter
    private final static char HY = 0x2D;    // hyphen
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char HY = 0x2D;    // hyphen
    private final static char SP = 0x20;    // the blank space
    private final static char LT = 0x3C;    // less than
    private final static char EQ = 0x3D;    // less than
    private final static char CT = 0x5E;    // carat
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char BS = 0x08;    // the backspace character (used for diacriticals)
    private final static char TB = 0x09;    // the tab character
    private final static char LF = 0x0A;    // line feed
    private final static char FF = 0x0C;    // form feed
    private final static char CR = 0x0D;    // carriage return
```

### MissortedModifiers
Missorted modifiers `final static`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char CT = 0x5E;    // carat
    private final static char SL = 0x91;    // single-quote left
    private final static char SR = 0x92;    // single-quote right
    private final static char DL = 0x93;    // double-quote left
    private final static char DR = 0x94;    // double-quote right
```

## RuleId[id=NegativeIntConstantInLongContext]
### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/SequenceNumberGenerator.java`
#### Snippet
```java

        // The token value cannot be exceed the value 0xFFFFFFFF.
        if (currentID > MAXSUBREQUESTID) {
            throw new TikaException("The token exceeds the max value 4294967295");
        }
```

### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
                long i = Long.parseLong(tmpVal, radix);
                decoded = new byte[]{(byte) ((i & 0x000000FF)), (byte) ((i & 0x0000FF00) >> 8),
                        (byte) ((i & 0x00FF0000) >> 16), (byte) ((i & 0xFF000000) >> 24)};
                break;
            }
```

### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
            case "big32": {
                long i = Long.parseLong(tmpVal, radix);
                decoded = new byte[]{(byte) ((i & 0xFF000000) >> 24), (byte) ((i & 0x00FF0000) >> 16),
                        (byte) ((i & 0x0000FF00) >> 8), (byte) ((i & 0x000000FF))};
                break;
```

## RuleId[id=MalformedFormatString]
### MalformedFormatString
Too many arguments for format string (found: 1, expected: 0)
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObjectHeaderEnd16bit.java`
#### Snippet
```java
        this.type = StreamObjectTypeHeaderEnd.fromIntVal(type);
        if (this.type == null) {
            throw new TikaException(String.format(Locale.US,
                    "The type value RuntimeException is not defined for the stream object end 16-bit header",
                    type));
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `tika-core/src/main/java/org/apache/tika/parser/NetworkParser.java`
#### Snippet
```java
                    thread.join(1000);
                } catch (InterruptedException e) {
                    throw new TikaException("Network parser interrupted", e);
                }

```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `tika-core/src/main/java/org/apache/tika/parser/NetworkParser.java`
#### Snippet
```java
                if (exception != null) {
                    input.throwIfCauseOf(exception);
                    throw new TikaException("Unexpected network parser error", exception);
                }
            }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `tika-core/src/main/java/org/apache/tika/parser/digest/CompositeDigester.java`
#### Snippet
```java
                tmp.dispose();
            } catch (TikaException e) {
                throw new IOException(e);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `tika-core/src/main/java/org/apache/tika/parser/digest/InputStreamDigester.java`
#### Snippet
```java
                    tmp.dispose();
                } catch (TikaException e) {
                    throw new IOException(e);
                }
            }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
            IOUtils.closeQuietly(stdErrOutputStream);
            if (process.exitValue() != 0) {
                throw new TikaException("There was an error executing the command line" +
                        "\nExecutable Command:\n\n" + cmd + "\nExecutable Error:\n\n" +
                        stdErrOutputStream.toString(UTF_8.name()));
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/XMLErrorLogUpdater.java`
#### Snippet
```java
                statement.close();
            } catch (SQLException e) {
                throw new RuntimeException("Failed to close db connection!", e);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `tika-batch/src/main/java/org/apache/tika/batch/fs/StreamOutRPWFSConsumer.java`
#### Snippet
```java
                //this is a stop the world kind of thing
                LOG.error("{}", getXMLifiedLogMsg(IO_OS + "json", fileResource.getResourceId(), e));
                throw new RuntimeException(e);
            } finally {
                IOUtils.closeQuietly(is);
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java

        /* is it full table? */
        if (pos == table_mask) {
            return tmp;
        }
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `tika-core/src/main/java/org/apache/tika/detect/TrainedModelDetector.java`
#### Snippet
```java
                    int idx = byt;
                    idx++;
                    if (byt < 0) {
                        idx = 256 + idx;
                        histogram[idx]++;
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `tika-core/src/main/java/org/apache/tika/parser/multiple/SupplementingParser.java`
#### Snippet
```java
                                      ParseContext context, Exception exception) {
        // If there was no exception, just carry on to the next
        if (exception == null) {
            return true;
        }
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java

    private boolean hasHeadings(String line, String[] headings) {
        if (headings != null && headings.length > 0) {
            for (String heading : headings) {
                if (line.contains(heading)) {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
                pos++;
            }
            if (pos >= buffer.length - 1) {
                next = null;
                return ret;
```

## RuleId[id=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
            skipBuffer = new byte[4096];
        }
        long n = IOUtils.skip(super.in, ln, skipBuffer);
        position += n;
        return n;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `tika-core/src/main/java/org/apache/tika/detect/NNExampleModelDetector.java`
#### Snippet
```java
                    readNNParams(nnBuilder, line);
                    // add this model into map of trained models.
                    super.registerModels(nnBuilder.getType(), nnBuilder.build());
                }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/OpenDocumentConverter.java`
#### Snippet
```java
    @Override
    public XMPMeta process(Metadata metadata) throws XMPException {
        super.setMetadata(metadata);

        createProperty(HttpHeaders.CONTENT_TYPE, XMPConst.NS_DC, "format");
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/OpenDocumentConverter.java`
#### Snippet
```java
        // nbWord | 5

        return super.getXMPMeta();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/MSOfficeBinaryConverter.java`
#### Snippet
```java
     */
    public XMPMeta process(Metadata metadata) throws XMPException {
        super.setMetadata(metadata);

        // For all formats, Tika uses the same keys
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/MSOfficeBinaryConverter.java`
#### Snippet
```java
        // not mapped: (MSOffice) Security ???

        return super.getXMPMeta();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/MSOfficeXMLConverter.java`
#### Snippet
```java
    @Override
    public XMPMeta process(Metadata metadata) throws XMPException {
        super.setMetadata(metadata);

        createProperty(HttpHeaders.CONTENT_TYPE, XMPConst.NS_DC, "format");
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/MSOfficeXMLConverter.java`
#### Snippet
```java
        createProperty(Office.WORD_COUNT, OfficeOpenXMLExtended.NAMESPACE_URI, "Words");

        return super.getXMPMeta();
    }

```

## RuleId[id=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
    public void run() {
        try {
            while (true) {
                synchronized (lock) {
                    long elapsed = System.currentTimeMillis() - since;
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
            write(STATUS.READY);
            long start = System.currentTimeMillis();
            while (true) {
                int request = input.read();
                if (request == -1) {
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `tika-batch/src/main/java/org/apache/tika/batch/StatusReporter.java`
#### Snippet
```java
        NumberFormat numberFormat = NumberFormat.getNumberInstance(Locale.ROOT);
        try {
            while (true) {
                Thread.sleep(sleepMillis);
                int cnt = getRoughCountConsumed();
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
        public void run() {
            try {
                while (true) {
                    Thread.sleep(500);
                    if (userInterrupted) {
```

## RuleId[id=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `tika-core/src/main/java/org/apache/tika/utils/ProcessUtils.java`
#### Snippet
```java

    static {
        Runtime.getRuntime().addShutdownHook(new Thread(() -> {
            PROCESS_MAP.forEachValue(1, Process::destroyForcibly);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `tika-core/src/main/java/org/apache/tika/parser/AbstractExternalProcessParser.java`
#### Snippet
```java

    static {
        Runtime.getRuntime().addShutdownHook(new Thread(() -> {
            PROCESS_MAP.forEachValue(1, Process::destroyForcibly);
        }));
```

## RuleId[id=UseOfPropertiesAsHashtable]
### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-kafka/src/main/java/org/apache/tika/pipes/pipesiterator/kafka/KafkaPipesIterator.java`
#### Snippet
```java
    private void safePut(Properties props, String key, Object val) {
        if (val != null) {
            props.put(key, val);
        }
    }
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.put()` on properties object
in `tika-pipes/tika-emitters/tika-emitter-kafka/src/main/java/org/apache/tika/pipes/emitter/kafka/KafkaEmitter.java`
#### Snippet
```java
    private void safePut(Properties props, String key, Object val) {
        if (val != null) {
            props.put(key, val);
        }
    }
```

## RuleId[id=EmptyMethod]
### EmptyMethod
Empty method overrides empty method
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSConsumersManager.java`
#### Snippet
```java

    @Override
    public void init() {
        //noop
    }
```

### EmptyMethod
Empty method overrides empty method
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSConsumersManager.java`
#### Snippet
```java

    @Override
    public void shutdown() {
        //noop
    }
```

### EmptyMethod
The method is empty
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSDirectoryCrawler.java`
#### Snippet
```java
     * @param f file to handle
     */
    public void handleFirstFileInDirectory(Path f) {
        //no-op
    }
```

### EmptyMethod
The method is empty
in `tika-core/src/main/java/org/apache/tika/config/ConfigBase.java`
#### Snippet
```java
     * @param settings
     */
    protected void handleSettings(Set<String> settings) {
        //no-op
    }
```

### EmptyMethod
The method and all its derivables are empty
in `tika-core/src/main/java/org/apache/tika/pipes/PipesReporterBase.java`
#### Snippet
```java

    @Override
    public void checkInitialization(InitializableProblemHandler problemHandler)
            throws TikaConfigException {

```

### EmptyMethod
Method only calls its super
in `tika-core/src/main/java/org/apache/tika/sax/SafeContentHandler.java`
#### Snippet
```java

    @Override
    public void endDocument() throws SAXException {
        // TODO: enable this, but some parsers currently
        // trip it
```

### EmptyMethod
The method is empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmBlockInfo.java`
#### Snippet
```java
    }

    public static void main(String[] args) {
    }

```

### EmptyMethod
The method is empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
    }

    public static void main(String[] args) {
    }

```

### EmptyMethod
The method is empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcControlData.java`
#### Snippet
```java
     * @param args
     */
    public static void main(String[] args) {
    }

```

### EmptyMethod
All implementations of this method are empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
        void endSDT() throws SAXException;

        void startEditedSection(String editor, Date date, EditType editType) throws SAXException;

        void endEditedSection() throws SAXException;
```

### EmptyMethod
All implementations of this method are empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
        void startEditedSection(String editor, Date date, EditType editType) throws SAXException;

        void endEditedSection() throws SAXException;

        boolean isIncludeDeletedText() throws SAXException;
```

### EmptyMethod
All implementations of this method are empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
        void startBookmark(String id, String name) throws SAXException;

        void endBookmark(String id) throws SAXException;
    }
}
```

### EmptyMethod
The method is empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
        }

        void unquoted(String string) {
            //TODO -- do some analysis to make sure you don't have
            //large tokens like 2,3,2,3,2,3,
```

### EmptyMethod
The method is empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-webarchive-module/src/main/java/org/apache/tika/parser/http/HttpParser.java`
#### Snippet
```java
    }

    private void updateMetadata(MessageHeaders messageHeaders, Metadata metadata) {
        //TODO
        //metadata.set(HttpHeaders.CONTENT_LENGTH, messageHeaders.)
```

### EmptyMethod
The method is empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-webarchive-module/src/main/java/org/apache/tika/parser/wacz/WACZParser.java`
#### Snippet
```java
    }

    private void processDataPackage(InputStream is, ZipArchiveEntry zae,
                                    XHTMLContentHandler xhtml, Metadata metadata)
            throws IOException {
```

### EmptyMethod
The method is empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-webarchive-module/src/main/java/org/apache/tika/parser/warc/WARCParser.java`
#### Snippet
```java
    }

    private void processWarcInfo(WarcRecord record, XHTMLContentHandler xhtml,
                                 ParseContext context) {
        //NO-OP for now
```

### EmptyMethod
The method is empty
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-webarchive-module/src/main/java/org/apache/tika/parser/warc/WARCParser.java`
#### Snippet
```java
    }

    private void processResponseMetadata(HttpResponse http, Metadata metadata) {
        //TODO -- no-op for now
    }
```

## RuleId[id=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/EMFParser.java`
#### Snippet
```java
        boolean hitIconOnly = false;
        boolean lastWasIconOnly = false;
        boolean isIconOnly = false;
        String iconOnlyString = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/EMFParser.java`
#### Snippet
```java
        boolean lastWasIconOnly = false;
        boolean isIconOnly = false;
        String iconOnlyString = null;

        EmbeddedDocumentExtractor extractor;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/EMFParser.java`
#### Snippet
```java
        double lastX = -1;
        boolean hitIconOnly = false;
        boolean lastWasIconOnly = false;
        boolean isIconOnly = false;
        String iconOnlyString = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/EMFParser.java`
#### Snippet
```java
        double lastY = -1;
        double lastX = -1;
        boolean hitIconOnly = false;
        boolean lastWasIconOnly = false;
        boolean isIconOnly = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java

    private static class OOXMLHintCounter {
        private int cnt = 0;

        private void increment() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
    private static class RelsHandler extends DefaultHandler {
        Set<String> rels = new HashSet<>();
        private MediaType mediaType = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java

    private static class CloseShieldZipFileZipEntrySource extends ZipFileZipEntrySource {
        private boolean allowClose = false;
        public CloseShieldZipFileZipEntrySource(ZipFile zipFile) {
            super(zipFile);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
    private static class ContentTypeHandler extends DefaultHandler {

        private MediaType mediaType = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParserConfig.java`
#### Snippet
```java

    private boolean useSAXDocxExtractor = false;
    private boolean useSAXPptxExtractor = false;
    private boolean extractAllAlternativesFromMSG;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParserConfig.java`
#### Snippet
```java

    private String dateOverrideFormat = null;
    private int maxOverride = 0;//ignore

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParserConfig.java`
#### Snippet
```java
    private boolean extractAllAlternativesFromMSG;

    private String dateOverrideFormat = null;
    private int maxOverride = 0;//ignore

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParserConfig.java`
#### Snippet
```java
    private boolean concatenatePhoneticRuns = true;

    private boolean useSAXDocxExtractor = false;
    private boolean useSAXPptxExtractor = false;
    private boolean extractAllAlternativesFromMSG;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParserConfig.java`
#### Snippet
```java

    private boolean includeDeletedContent = false;
    private boolean includeMoveFromContent = false;
    private boolean includeShapeBasedContent = true;
    private boolean includeHeadersAndFooters = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParserConfig.java`
#### Snippet
```java
    private boolean extractMacros = false;

    private boolean includeDeletedContent = false;
    private boolean includeMoveFromContent = false;
    private boolean includeShapeBasedContent = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParserConfig.java`
#### Snippet
```java
    private boolean includeShapeBasedContent = true;
    private boolean includeHeadersAndFooters = true;
    private boolean includeMissingRows = false;
    private boolean includeSlideNotes = true;
    private boolean includeSlideMasterContent = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParserConfig.java`
#### Snippet
```java
public class OfficeParserConfig implements Serializable {

    private boolean extractMacros = false;

    private boolean includeDeletedContent = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
        private List<Picture> nonU1based;
        private List<Picture> all;
        private int pn = 0;

        private PicturesSource(HWPFDocument doc) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmWrapper.java`
#### Snippet
```java
    private List<ChmLzxBlock> lzxBlocksCache = null;
    private ChmDirectoryListingSet chmDirList = null;
    private ChmItsfHeader chmItsfHeader = null;
    private ChmItspHeader chmItspHeader = null;
    private ChmLzxcResetTable chmLzxcResetTable = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmWrapper.java`
#### Snippet
```java
public class ChmWrapper {
    private List<ChmLzxBlock> lzxBlocksCache = null;
    private ChmDirectoryListingSet chmDirList = null;
    private ChmItsfHeader chmItsfHeader = null;
    private ChmItspHeader chmItspHeader = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmWrapper.java`
#### Snippet
```java
    private ChmDirectoryListingSet chmDirList = null;
    private ChmItsfHeader chmItsfHeader = null;
    private ChmItspHeader chmItspHeader = null;
    private ChmLzxcResetTable chmLzxcResetTable = null;
    private ChmLzxcControlData chmLzxcControlData = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmWrapper.java`
#### Snippet
```java

public class ChmWrapper {
    private List<ChmLzxBlock> lzxBlocksCache = null;
    private ChmDirectoryListingSet chmDirList = null;
    private ChmItsfHeader chmItsfHeader = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmWrapper.java`
#### Snippet
```java
    private ChmLzxcResetTable chmLzxcResetTable = null;
    private ChmLzxcControlData chmLzxcControlData = null;
    private byte[] data = null;
    private int indexOfContent;
    private long lzxBlockOffset;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmWrapper.java`
#### Snippet
```java
    private ChmItsfHeader chmItsfHeader = null;
    private ChmItspHeader chmItspHeader = null;
    private ChmLzxcResetTable chmLzxcResetTable = null;
    private ChmLzxcControlData chmLzxcControlData = null;
    private byte[] data = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmWrapper.java`
#### Snippet
```java
    private ChmItspHeader chmItspHeader = null;
    private ChmLzxcResetTable chmLzxcResetTable = null;
    private ChmLzxcControlData chmLzxcControlData = null;
    private byte[] data = null;
    private int indexOfContent;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
     * records.
     */
    private boolean listenForAllRecords = false;

    public ExcelExtractor(ParseContext context, Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
         * {@link #throwStoredException()} is invoked.
         */
        private Exception exception = null;
        private SSTRecord sstRecord;
        private FormulaRecord stringFormulaRecord;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
         * worksheet is currently active.
         */
        private SortedMap<Point, Cell> currentSheet = null;
        /**
         * Extra text or cells that crops up, typically as part of a
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java


    private List<ChmLzxBlock> lzxBlocksCache = null;
    private ChmDirectoryListingSet chmDirList = null;
    private ChmItsfHeader chmItsfHeader = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
    private ChmDirectoryListingSet chmDirList = null;
    private ChmItsfHeader chmItsfHeader = null;
    private ChmItspHeader chmItspHeader = null;
    private ChmLzxcResetTable chmLzxcResetTable = null;
    private ChmLzxcControlData chmLzxcControlData = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
    private ChmItsfHeader chmItsfHeader = null;
    private ChmItspHeader chmItspHeader = null;
    private ChmLzxcResetTable chmLzxcResetTable = null;
    private ChmLzxcControlData chmLzxcControlData = null;
    private byte[] data = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
    private List<ChmLzxBlock> lzxBlocksCache = null;
    private ChmDirectoryListingSet chmDirList = null;
    private ChmItsfHeader chmItsfHeader = null;
    private ChmItspHeader chmItspHeader = null;
    private ChmLzxcResetTable chmLzxcResetTable = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java

    private List<ChmLzxBlock> lzxBlocksCache = null;
    private ChmDirectoryListingSet chmDirList = null;
    private ChmItsfHeader chmItsfHeader = null;
    private ChmItspHeader chmItspHeader = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
    private long lzxBlockOffset;
    private long lzxBlockLength;
    private ChmBlockInfo chmBlockInfo = null;//this will be instantiated at first call of

    public ChmExtractor(InputStream is) throws TikaException, IOException {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
    private ChmLzxcResetTable chmLzxcResetTable = null;
    private ChmLzxcControlData chmLzxcControlData = null;
    private byte[] data = null;
    private int indexOfContent;
    private long lzxBlockOffset;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
    private ChmItspHeader chmItspHeader = null;
    private ChmLzxcResetTable chmLzxcResetTable = null;
    private ChmLzxcControlData chmLzxcControlData = null;
    private byte[] data = null;
    private int indexOfContent;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmPmglHeader.java`
#### Snippet
```java
    /* local usage */
    private int dataRemained;
    private int currentPlace = 0;

    public ChmPmglHeader() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcControlData.java`
#### Snippet
```java
    /* local usage */
    private int dataRemained;
    private int currentPlace = 0;

    public ChmLzxcControlData() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcResetTable.java`
#### Snippet
```java
    /* local usage */
    private int dataRemained;
    private int currentPlace = 0;

    private int getDataRemained() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
    /* local usage */
    private int dataRemained;
    private int currentPlace = 0;

    public ChmItspHeader() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmPmgiHeader.java`
#### Snippet
```java
    /* local usage */
    private int dataRemained;
    private int currentPlace = 0;

    public ChmPmgiHeader() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFParser.java`
#### Snippet
```java
    private int memoryLimitInKb = EMB_OBJ_MAX_BYTES / 1024;
    @Field
    private boolean ignoreListMarkup = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFParser.java`
#### Snippet
```java
    private static int EMB_OBJ_MAX_BYTES = 20 * 1024 * 1024; //20MB
    //get rid of this once we get rid of the other static maxbytes...
    private static volatile boolean USE_STATIC = false;
    @Field
    private int memoryLimitInKb = EMB_OBJ_MAX_BYTES / 1024;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
    /* local usage */
    private int dataRemained;
    private int currentPlace = 0;

    public ChmItsfHeader() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/HyperlinkHandler.java`
#### Snippet
```java
    private final ContentHandler handler;
    private final String namespace;
    boolean inLink = false;
    StringBuilder linkCache = new StringBuilder();
    String href = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/HyperlinkHandler.java`
#### Snippet
```java
    boolean inLink = false;
    StringBuilder linkCache = new StringBuilder();
    String href = null;

    public HyperlinkHandler(ContentHandler handler, String namespace) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFEmbObjHandler.java`
#### Snippet
```java
    private final int memoryLimitInKb;

    private boolean isPictBitmap = false;
    //high hex cached for writing hexpair chars (data)
    private int hi = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFEmbObjHandler.java`
#### Snippet
```java
    //high hex cached for writing hexpair chars (data)
    private int hi = -1;
    private int thumbCount = 0;
    //don't need atomic, do need mutable
    private AtomicInteger unknownFilenameCount = new AtomicInteger();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFEmbObjHandler.java`
#### Snippet
```java
    //don't need atomic, do need mutable
    private AtomicInteger unknownFilenameCount = new AtomicInteger();
    private boolean inObject = false;
    private String sv = EMPTY_STRING;
    private String sn = EMPTY_STRING;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
        StringBuilder buffer = new StringBuilder();
        String href = null;
        boolean inData = false;
        private boolean inBody = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
        final ContentHandler handler;
        StringBuilder buffer = new StringBuilder();
        String href = null;
        boolean inData = false;
        private boolean inBody = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
        String href = null;
        boolean inData = false;
        private boolean inBody = false;

        public ExcelMLHandler(ContentHandler handler) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
        boolean inBin = false;
        String pictName = null;
        String pictSource = null;

        public PictHandler(ContentHandler handler, Metadata metadata,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
        EmbeddedDocumentExtractor embeddedDocumentExtractor;
        boolean inPict = false;
        boolean inBin = false;
        String pictName = null;
        String pictSource = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
        final ContentHandler handler;
        final Base64 base64 = new Base64();
        byte[] rawBytes = null;
        EmbeddedDocumentExtractor embeddedDocumentExtractor;
        boolean inPict = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
        byte[] rawBytes = null;
        EmbeddedDocumentExtractor embeddedDocumentExtractor;
        boolean inPict = false;
        boolean inBin = false;
        String pictName = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
        private final ContentHandler handler;
        private boolean ignoreCharacters;
        private boolean inBody = false;

        //use inP to keep track of whether the handler is
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
        boolean inPict = false;
        boolean inBin = false;
        String pictName = null;
        String pictSource = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/RunProperties.java`
#### Snippet
```java
public class RunProperties {
    boolean italics = false;
    boolean bold = false;
    boolean strikeThrough = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/RunProperties.java`
#### Snippet
```java
    boolean italics = false;
    boolean bold = false;
    boolean strikeThrough = false;

    UnderlinePatterns underline = UnderlinePatterns.NONE;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/RunProperties.java`
#### Snippet
```java

public class RunProperties {
    boolean italics = false;
    boolean bold = false;
    boolean strikeThrough = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
    private byte[] content = null;
    private ChmSection chmSection = null;
    private int contentLength = 0;

    // trying to find solution for bad blocks ...
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
    private ChmLzxState state;
    private byte[] content = null;
    private ChmSection chmSection = null;
    private int contentLength = 0;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
    private long block_length;
    private ChmLzxState state;
    private byte[] content = null;
    private ChmSection chmSection = null;
    private int contentLength = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    //<tc><p/><p/><table><tr><tc></p></p></tc></tr></table>...
    private int tableCellDepth = 0;
    private int pWithinCell = 0;

    //will need to replace this with a stack
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    private boolean isItalics = false;
    private boolean isBold = false;
    private boolean isUnderline = false;
    private boolean isStrikeThrough = false;
    private boolean wroteHyperlinkStart = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    private int tableDepth = 0;//table depth
    private int sdtDepth = 0;//
    private boolean isItalics = false;
    private boolean isBold = false;
    private boolean isUnderline = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    private final XWPFStylesShim styles;

    private int pDepth = 0; //paragraph depth
    private int tableDepth = 0;//table depth
    private int sdtDepth = 0;//
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java

    private int pDepth = 0; //paragraph depth
    private int tableDepth = 0;//table depth
    private int sdtDepth = 0;//
    private boolean isItalics = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    //you know what your paragraph count was in the parent cell.
    //<tc><p/><p/><table><tr><tc></p></p></tc></tr></table>...
    private int tableCellDepth = 0;
    private int pWithinCell = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    private boolean isUnderline = false;
    private boolean isStrikeThrough = false;
    private boolean wroteHyperlinkStart = false;

    //TODO: fix this
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    private boolean isBold = false;
    private boolean isUnderline = false;
    private boolean isStrikeThrough = false;
    private boolean wroteHyperlinkStart = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    //will need to replace this with a stack
    //if we're marking more that the first level <p/> element
    private String paragraphTag = null;

    public OOXMLTikaBodyPartHandler(XHTMLContentHandler xhtml) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    private int sdtDepth = 0;//
    private boolean isItalics = false;
    private boolean isBold = false;
    private boolean isUnderline = false;
    private boolean isStrikeThrough = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLTikaBodyPartHandler.java`
#### Snippet
```java
    private int pDepth = 0; //paragraph depth
    private int tableDepth = 0;//table depth
    private int sdtDepth = 0;//
    private boolean isItalics = false;
    private boolean isBold = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSSFExcelExtractorDecorator.java`
#### Snippet
```java
    protected static class XSSFSheetInterestingPartsCapturer extends DefaultHandler {
        private ContentHandler delegate;
        private boolean hasProtection = false;

        protected XSSFSheetInterestingPartsCapturer(ContentHandler delegate) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java

    private class XSLFCommentAuthorHandler extends DefaultHandler {
        String id = null;
        String name = null;
        String initials = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
    private final Metadata metadata;
    private final CommentAuthors commentAuthors = new CommentAuthors();
    private PackagePart mainDocument = null;

    public SXSLFPowerPointExtractorDecorator(Metadata metadata, ParseContext context,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
    private class XSLFCommentAuthorHandler extends DefaultHandler {
        String id = null;
        String name = null;
        String initials = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
    private class XSLFCommentsHandler extends DefaultHandler {

        private String commentAuthorId = null;
        private StringBuilder commentBuffer = new StringBuilder();
        private XHTMLContentHandler xhtml;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
        String id = null;
        String name = null;
        String initials = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXSLFPowerPointExtractorDecorator.java`
#### Snippet
```java

        private final ContentHandler wrappedHandler;
        boolean inPH = false;

        PlaceHolderSkipper(ContentHandler wrappedHandler) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    // shadow" after seeing a unicode escape, at which
    // point it's set to the last ucN skip we had seen:
    int ansiSkip = 0;
    private int written = 0;
    // Hold pending bytes (encoded in the current charset)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    private boolean inHeader = true;
    //0 not yet in font table, 1 in font table, 2 have processed font table
    private int fontTableState = 0;
    //depth at which the font table started
    private int fontTableDepth;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java
    // point it's set to the last ucN skip we had seen:
    int ansiSkip = 0;
    private int written = 0;
    // Hold pending bytes (encoded in the current charset)
    // for text output:
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/XWPFStylesShim.java`
#### Snippet
```java
    private class StylesStripper extends DefaultHandler {

        String currentStyleId = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/BinaryDataHandler.java`
#### Snippet
```java
    private final Metadata metadata;
    private final ParseContext parseContext;
    private boolean inBinaryData = false;
    private StringBuilder buffer = new StringBuilder();

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xwpf/ml2006/Word2006MLDocHandler.java`
#### Snippet
```java
    private final BinaryDataHandler binaryDataHandler;
    private final RelationshipsManager relationshipsManager = new RelationshipsManager();
    private PartHandler currentPartHandler = null;

    public Word2006MLDocHandler(XHTMLContentHandler xhtml, Metadata metadata,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
    private String imageSourcePathInZip = null;
    //embedded images sometimes include full path info of original image
    private String originalLocationOnDrive = null;
    //buffer for the glyph runs within a given canvas
    //in insertion order
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
    private final Map<String, Metadata> embeddedInfos;
    //path in zip file for an image rendered on this page
    private String imageSourcePathInZip = null;
    //embedded images sometimes include full path info of original image
    private String originalLocationOnDrive = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inPict = false;
    private String picDescription = null;
    private String picRId = null;
    private String picFilename = null;
    //mechanism used to determine when to
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    //buffers rt in ruby sections (see 17.3.3.25)
    private boolean inHlinkClick = false;
    private boolean inTextBox = false;
    private boolean inV = false; //in c:v in chart file
    private OOXMLWordAndPowerPointTextHandler.EditType editType =
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private final StringBuilder runBuffer = new StringBuilder();
    private final StringBuilder rubyBuffer = new StringBuilder();
    private boolean inR = false;
    //in run or in field. TODO: convert this to an integer because you can have a run within a run
    private boolean inT = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inRt = false;
    private boolean inPic = false;
    private boolean inPict = false;
    private String picDescription = null;
    private String picRId = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inDelText = false;
    //buffers rt in ruby sections (see 17.3.3.25)
    private boolean inHlinkClick = false;
    private boolean inTextBox = false;
    private boolean inV = false; //in c:v in chart file
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    //in run or in field. TODO: convert this to an integer because you can have a run within a run
    private boolean inT = false;
    private boolean inRPr = false;
    private boolean inNumPr = false;
    private boolean inRt = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    //signal the start of the p, and still
    //handle p with pPr and those without
    private boolean lastStartElementWasP = false;
    //have we signaled the start of a p?
    //pPr can happen multiple times within a p
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    //if in alternate, choose fallback, maybe make this configurable?
    private int inACChoiceDepth = 0;
    private int inACFallbackDepth = 0;
    private boolean inDelText = false;
    //buffers rt in ruby sections (see 17.3.3.25)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inRPr = false;
    private boolean inNumPr = false;
    private boolean inRt = false;
    private boolean inPic = false;
    private boolean inPict = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    //need to track depth.
    //if in alternate, choose fallback, maybe make this configurable?
    private int inACChoiceDepth = 0;
    private int inACFallbackDepth = 0;
    private boolean inDelText = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inNumPr = false;
    private boolean inRt = false;
    private boolean inPic = false;
    private boolean inPict = false;
    private String picDescription = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    //pPr can happen multiple times within a p
    //<p><pPr/><r><t>text</t></r><pPr></p>
    private boolean pStarted = false;
    //alternate content can be embedded in itself.
    //need to track depth.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private String picDescription = null;
    private String picRId = null;
    private String picFilename = null;
    //mechanism used to determine when to
    //signal the start of the p, and still
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private int inACChoiceDepth = 0;
    private int inACFallbackDepth = 0;
    private boolean inDelText = false;
    //buffers rt in ruby sections (see 17.3.3.25)
    private boolean inHlinkClick = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inR = false;
    //in run or in field. TODO: convert this to an integer because you can have a run within a run
    private boolean inT = false;
    private boolean inRPr = false;
    private boolean inNumPr = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inT = false;
    private boolean inRPr = false;
    private boolean inNumPr = false;
    private boolean inRt = false;
    private boolean inPic = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inHlinkClick = false;
    private boolean inTextBox = false;
    private boolean inV = false; //in c:v in chart file
    private OOXMLWordAndPowerPointTextHandler.EditType editType =
            OOXMLWordAndPowerPointTextHandler.EditType.NONE;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
    private boolean inPic = false;
    private boolean inPict = false;
    private String picDescription = null;
    private String picRId = null;
    private String picFilename = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/MSOneStorePackage.java`
#### Snippet
```java
    public List<RevisionStoreObjectGroup> OtherFileNodeList;
    private boolean mostRecentAuthorProp = false;
    private boolean originalAuthorProp = false;
    private Instant lastModifiedTimestamp = Instant.MIN;
    private long creationTimestamp = Long.MAX_VALUE;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/MSOneStorePackage.java`
#### Snippet
```java
    public List<RevisionStoreObjectGroup> dataRoot;
    public List<RevisionStoreObjectGroup> OtherFileNodeList;
    private boolean mostRecentAuthorProp = false;
    private boolean originalAuthorProp = false;
    private Instant lastModifiedTimestamp = Instant.MIN;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java
            "{638DE92F-A6D4-4BC1-9A36-B3FC2511A5B7}";

    int indentLevel = 0;

    long offset;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteTreeWalker.java`
#### Snippet
```java
    private long lastModified = Long.MIN_VALUE;
    private boolean mostRecentAuthorProp = false;
    private boolean originalAuthorProp = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteTreeWalker.java`
#### Snippet
```java
    private long creationTimestamp = Long.MAX_VALUE;
    private long lastModified = Long.MIN_VALUE;
    private boolean mostRecentAuthorProp = false;
    private boolean originalAuthorProp = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadataList.java`
#### Snippet
```java

public class JsonMetadataList {
    static volatile boolean PRETTY_PRINT = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonStreamingSerializer.java`
#### Snippet
```java

    private final Writer writer;
    boolean hasStartedArray = false;
    private JsonGenerator jsonGenerator;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadata.java`
#### Snippet
```java
public class JsonMetadata {

    static volatile boolean PRETTY_PRINT = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-emitters/tika-emitter-s3/src/main/java/org/apache/tika/pipes/emitter/s3/S3Emitter.java`
#### Snippet
```java
    private String prefix = null;
    private int maxConnections = ClientConfiguration.DEFAULT_MAX_CONNECTIONS;
    private boolean pathStyleAccessEnabled = false;
    private AmazonS3 s3Client;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-pipes/tika-emitters/tika-emitter-s3/src/main/java/org/apache/tika/pipes/emitter/s3/S3Emitter.java`
#### Snippet
```java
    private String fileExtension = "json";
    private boolean spoolToTemp = true;
    private String prefix = null;
    private int maxConnections = ClientConfiguration.DEFAULT_MAX_CONNECTIONS;
    private boolean pathStyleAccessEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-pipes/tika-emitters/tika-emitter-gcs/src/main/java/org/apache/tika/pipes/emitter/gcs/GCSEmitter.java`
#### Snippet
```java
    private String bucket;
    private String fileExtension = "json";
    private String prefix = null;
    private Storage storage;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
     * Current read position within this stream.
     */
    private long position = 0;
    /**
     * Marked position, or -1 if there is no current mark.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
    //suffix of the file if known. This is used to create temp files
    //with the right suffixes. This should include the initial . as in ".doc"
    private String suffix = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
     */
    private Object openContainer;
    private int consecutiveEOFs = 0;
    private byte[] skipBuffer;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java
    private long reportUpdateMillis = 1000;

    private volatile boolean crashed = false;

    Thread reporterThread;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-dl/src/main/java/org/apache/tika/dl/imagerec/DL4JVGG16Net.java`
#### Snippet
```java
    private NativeImageLoader imageLoader = new NativeImageLoader(224, 224, 3);
    private DataNormalization preProcessor = new VGG16ImagePreProcessor();
    private boolean available = false;
    private ComputationGraph model;
    private ImageNetLabels imageNetLabels;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/io/LookaheadInputStream.java`
#### Snippet
```java
    private int position = 0;

    private int mark = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/io/LookaheadInputStream.java`
#### Snippet
```java
    private final byte[] buffer;
    private InputStream stream;
    private int buffered = 0;

    private int position = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/io/LookaheadInputStream.java`
#### Snippet
```java
    private int buffered = 0;

    private int position = 0;

    private int mark = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/io/TemporaryResources.java`
#### Snippet
```java
     * Directory for temporary files, <code>null</code> for the system default.
     */
    private Path tempFileDir = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/WriteOutContentHandler.java`
#### Snippet
```java
     * Number of characters written so far.
     */
    private int writeCount = 0;

    private boolean throwOnWriteLimitReached = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/WriteOutContentHandler.java`
#### Snippet
```java
    private boolean throwOnWriteLimitReached = true;

    private ParseContext parseContext = null;

    private boolean writeLimitReached;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/StandardsExtractingContentHandler.java`
#### Snippet
```java

    private int maxBufferLength = 100000;
    private double threshold = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/XMPContentHandler.java`
#### Snippet
```java

    private static final Attributes EMPTY_ATTRIBUTES = new AttributesImpl();
    private String prefix = null;
    private String uri = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/XMPContentHandler.java`
#### Snippet
```java
    private static final Attributes EMPTY_ATTRIBUTES = new AttributesImpl();
    private String prefix = null;
    private String uri = null;

    public XMPContentHandler(ContentHandler handler) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/ToTextContentHandler.java`
#### Snippet
```java
     */
    private final Writer writer;
    private int styleDepth = 0;
    private int scriptDepth = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/ToTextContentHandler.java`
#### Snippet
```java
    private final Writer writer;
    private int styleDepth = 0;
    private int scriptDepth = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/XHTMLContentHandler.java`
#### Snippet
```java
    private boolean headStarted = false;
    private boolean headEnded = false;
    private boolean useFrameset = false;
    public XHTMLContentHandler(ContentHandler handler, Metadata metadata) {
        super(handler);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/XHTMLContentHandler.java`
#### Snippet
```java
     */
    private boolean headStarted = false;
    private boolean headEnded = false;
    private boolean useFrameset = false;
    public XHTMLContentHandler(ContentHandler handler, Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/XHTMLContentHandler.java`
#### Snippet
```java
     * Flag to indicate whether the document has been started.
     */
    private boolean documentStarted = false;
    /**
     * Flags to indicate whether the document head element has been started/ended.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/XHTMLContentHandler.java`
#### Snippet
```java
     * Flags to indicate whether the document head element has been started/ended.
     */
    private boolean headStarted = false;
    private boolean headEnded = false;
    private boolean useFrameset = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/AbstractRecursiveParserWrapperHandler.java`
#### Snippet
```java
    private final ContentHandlerFactory contentHandlerFactory;
    private final int maxEmbeddedResources;
    private int embeddedResources = 0;
    private int embeddedDepth = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/AbstractRecursiveParserWrapperHandler.java`
#### Snippet
```java
    private final int maxEmbeddedResources;
    private int embeddedResources = 0;
    private int embeddedDepth = 0;

    public AbstractRecursiveParserWrapperHandler(ContentHandlerFactory contentHandlerFactory) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/ToXMLContentHandler.java`
#### Snippet
```java
    private final String encoding;

    protected boolean inStartElement = false;
    private ElementInfo currentElement;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/SecureContentHandler.java`
#### Snippet
```java
     * Number of output characters that Tika has produced so far.
     */
    private long characterCount = 0;
    /**
     * The current XML element depth.
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/sax/SecureContentHandler.java`
#### Snippet
```java
     * The current XML element depth.
     */
    private int currentDepth = 0;
    /**
     * Output threshold.
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/fork/ForkParser.java`
#### Snippet
```java
    @Field
    private int poolSize = 5;
    private int currentlyInUse = 0;
    @Field
    private long serverPulseMillis = 1000;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
    private Object parser;
    private ClassLoader classLoader;
    private boolean parsing = false;
    private long since;
    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MagicMatch.java`
#### Snippet
```java
    private final String mask;

    private MagicDetector detector = null;

    MagicMatch(MediaType mediaType, String type, String offset, String value, String mask) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
     * The root-XML associated to this Mime-Type
     */
    private List<RootXML> rootXML = null;
    /**
     * All known file extensions of this type, in order of preference
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
        private MimeType type = null;

        private String namespaceURI = null;

        private String localName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
        private static final long serialVersionUID = 5140496601491000730L;

        private MimeType type = null;

        private String namespaceURI = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
     * The magics associated to this Mime-Type
     */
    private List<Magic> magics = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
     * (best first).
     */
    private List<String> extensions = null;
    /**
     * Whether this mime-type is used for server-side scripts,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
        private String namespaceURI = null;

        private String localName = null;

        RootXML(MimeType type, String namespaceURI, String localName) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
     * and thus cannot reliably be used for filename-based type detection
     */
    private boolean isInterpreted = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/Patterns.java`
#### Snippet
```java
            new TreeMap<>(new LengthComparator());
    private int minExtensionLength = Integer.MAX_VALUE;
    private int maxExtensionLength = 0;

    public Patterns(MediaTypeRegistry registry) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypes.java`
#### Snippet
```java
    private static final Map<ClassLoader, MimeTypes> CLASSLOADER_SPECIFIC_DEFAULT_TYPES =
            new HashMap<>();
    private static MimeTypes DEFAULT_TYPES = null;
    /**
     * Root type, application/octet-stream.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesReader.java`
#### Snippet
```java
    protected int priority;

    protected StringBuilder characters = null;
    private ClauseRecord current = new ClauseRecord(null);

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesReader.java`
#### Snippet
```java
        private Clause clause;

        private List<Clause> subclauses = null;

        public ClauseRecord(Clause clause) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesReader.java`
#### Snippet
```java
     * Current type
     */
    protected MimeType type = null;

    protected int priority;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/PipesClient.java`
#### Snippet
```java
    private final PipesConfigBase pipesConfig;
    private final int pipesClientId;
    private volatile boolean closed = false;
    private ExecutorService executorService = Executors.newFixedThreadPool(1);
    private Process process;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/PipesClient.java`
#### Snippet
```java
    private DataOutputStream output;
    private DataInputStream input;
    private int filesProcessed = 0;

    public PipesClient(PipesConfigBase pipesConfig) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncEmitter.java`
#### Snippet
```java
        private final long maxBytes;

        long estimatedSize = 0;
        int size = 0;
        Map<String, List<EmitData>> map = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncEmitter.java`
#### Snippet
```java

        long estimatedSize = 0;
        int size = 0;
        Map<String, List<EmitData>> map = new HashMap<>();

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
    private final int id = CLIENT_COUNTER.incrementAndGet();

    private volatile int filesProcessed = 0;

    public ForkClient(Path tikaDir, ParserFactoryFactory parserFactoryFactory, List<String> java,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/fetcher/fs/FileSystemFetcher.java`
#### Snippet
```java

    //Warning! basePath can be null!
    private Path basePath = null;

    static boolean isDescendant(Path root, Path descendant) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/filelist/FileListPipesIterator.java`
#### Snippet
```java

    @Field
    private boolean hasHeader = false;

    private Path fileListPath;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/fs/FileSystemPipesIterator.java`
#### Snippet
```java

    private Path basePath;
    private boolean countTotal = false;

    private FileCountWorker fileCountWorker;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/PipesIterator.java`
#### Snippet
```java

    private long maxWaitMs = DEFAULT_MAX_WAIT_MS;
    private ArrayBlockingQueue<FetchEmitTuple> queue = null;
    private int queueSize = DEFAULT_QUEUE_SIZE;
    private String fetcherName;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/PipesIterator.java`
#### Snippet
```java
    private int maxEmbeddedResources = -1;

    private int added = 0;
    private FutureTask<Integer> futureTask;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/PipesIterator.java`
#### Snippet
```java

    private class TupleIterator implements Iterator<FetchEmitTuple> {
        FetchEmitTuple next = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
    private static long MAX_OFFER_WAIT_MS = 120000;
    private volatile int numParserThreadsFinished = 0;
    private volatile int numEmitterThreadsFinished = 0;
    private boolean addedEmitterSemaphores = false;
    boolean isShuttingDown = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
    private volatile int numParserThreadsFinished = 0;
    private volatile int numEmitterThreadsFinished = 0;
    private boolean addedEmitterSemaphores = false;
    boolean isShuttingDown = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
    private volatile int numEmitterThreadsFinished = 0;
    private boolean addedEmitterSemaphores = false;
    boolean isShuttingDown = false;

    public AsyncProcessor(Path tikaConfigPath) throws TikaException, IOException {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
    private final AtomicLong totalProcessed = new AtomicLong(0);
    private static long MAX_OFFER_WAIT_MS = 120000;
    private volatile int numParserThreadsFinished = 0;
    private volatile int numEmitterThreadsFinished = 0;
    private boolean addedEmitterSemaphores = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/StreamGobbler.java`
#### Snippet
```java
    List<String> lines = new ArrayList<>();
    long streamLength = 0;
    boolean isTruncated = false;

    public StreamGobbler(InputStream is, int maxBufferLength) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/StreamGobbler.java`
#### Snippet
```java
    private final int maxBufferLength;
    List<String> lines = new ArrayList<>();
    long streamLength = 0;
    boolean isTruncated = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
    private static final Map<String, Charset> COMMON_CHARSETS = new HashMap<>();

    private static Method getCharsetICU = null;
    private static Method isSupportedICU = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java

    private static Method getCharsetICU = null;
    private static Method isSupportedICU = null;

    static {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/FileProcessResult.java`
#### Snippet
```java
    long stderrLength = -1;
    boolean stderrTruncated = false;
    boolean stdoutTruncated = false;

    public String getStderr() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/FileProcessResult.java`
#### Snippet
```java
    long stdoutLength = -1;
    long stderrLength = -1;
    boolean stderrTruncated = false;
    boolean stdoutTruncated = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/utils/FileProcessResult.java`
#### Snippet
```java
    int exitValue = -1;
    long processTimeMillis = -1;
    boolean isTimeout = false;
    long stdoutLength = -1;
    long stderrLength = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
        parseContext.set(DocumentSelector.class, new DocumentSelector() {
            final int maxEmbedded = handlerConfig.maxEmbeddedResources;
            int embedded = 0;
            @Override
            public boolean select(Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/detect/FileCommandDetector.java`
#### Snippet
```java
    private long timeoutMs = DEFAULT_TIMEOUT_MS;

    private boolean useMime = false;

    public static boolean checkHasFile() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/detect/FileCommandDetector.java`
#### Snippet
```java
    private static final String DEFAULT_FILE_COMMAND_PATH = "file";
    private static boolean HAS_WARNED = false;
    private Boolean hasFileCommand = null;
    private String fileCommandPath = DEFAULT_FILE_COMMAND_PATH;
    private int maxBytes = 1_000_000;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/detect/FileCommandDetector.java`
#### Snippet
```java
    private static final long DEFAULT_TIMEOUT_MS = 6000;
    private static final String DEFAULT_FILE_COMMAND_PATH = "file";
    private static boolean HAS_WARNED = false;
    private Boolean hasFileCommand = null;
    private String fileCommandPath = DEFAULT_FILE_COMMAND_PATH;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/detect/XmlRootExtractor.java`
#### Snippet
```java
    private static class ExtractorHandler extends DefaultHandler {

        private QName rootElement = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/detect/TextStatistics.java`
#### Snippet
```java
    private final int[] counts = new int[256];

    private int total = 0;

    public void addData(byte[] buffer, int offset, int length) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/config/ServiceLoader.java`
#### Snippet
```java
     * <code>null</code> to automatically select the context class loader.
     */
    private static volatile ClassLoader CONTEXT_CLASS_LOADER = null;
    private final ClassLoader loader;
    private final LoadErrorHandler handler;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/ParseRecord.java`
#### Snippet
```java
    private static final int MAX_METADATA_LIST_SIZE = 100;

    private int depth = 0;
    private final Set<String> parsers = new LinkedHashSet<>();

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/ParseRecord.java`
#### Snippet
```java
    private final List<Metadata> metadataList = new ArrayList<>();

    private boolean writeLimitReached = false;

    void beforeParse() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/NetworkParser.java`
#### Snippet
```java
        private final OutputStream output;

        private volatile Exception exception = null;

        public ParsingTask(TikaInputStream input, OutputStream output) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserConfig.java`
#### Snippet
```java
     * SecureContentHandler -- Desired maximum package entry nesting level.
     */
    private Integer maximumPackageEntryDepth = null;

    private MetadataWriteFilterFactory metadataWriteFilterFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserConfig.java`
#### Snippet
```java
    private Integer maximumPackageEntryDepth = null;

    private MetadataWriteFilterFactory metadataWriteFilterFactory = null;

    private EmbeddedDocumentExtractorFactory embeddedDocumentExtractorFactory =
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserConfig.java`
#### Snippet
```java
     * SecureContentHandler -- Desired maximum XML nesting level.
     */
    private Integer maximumDepth = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserConfig.java`
#### Snippet
```java
            NOOP_CONTENT_HANDLER_DECORATOR_FACTORY;

    private DigestingParser.DigesterFactory digesterFactory = null;

    private boolean throwOnZeroBytes = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserConfig.java`
#### Snippet
```java
     * SecureContentHandler -- Desired output threshold in characters.
     */
    private Long outputThreshold = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserConfig.java`
#### Snippet
```java
     * ahead of time.
     */
    private Long spoolToDisk = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParserConfig.java`
#### Snippet
```java
     * SecureContentHandler -- Desired maximum compression ratio.
     */
    private Long maximumCompressionRatio = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RegexCaptureParser.java`
#### Snippet
```java
    }

    private boolean writeContent = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
        private final ParseContext parseContext;

        private boolean writeLimitReached = false;

        //total chars written to all handlers
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
        private String location = null;

        private String embeddedIdPath = null;


```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
        private static final long serialVersionUID = 207648200464263337L;
        private final ParserState parserState;
        private String location = null;

        private String embeddedIdPath = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
    private static class ParserState {
        private final AbstractRecursiveParserWrapperHandler recursiveParserWrapperHandler;
        private int unknownCount = 0;
        private int embeddedCount = 0;//this is effectively 1-indexed
        private ParserState(AbstractRecursiveParserWrapperHandler handler) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
        private final AbstractRecursiveParserWrapperHandler recursiveParserWrapperHandler;
        private int unknownCount = 0;
        private int embeddedCount = 0;//this is effectively 1-indexed
        private ParserState(AbstractRecursiveParserWrapperHandler handler) {
            this.recursiveParserWrapperHandler = handler;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java

        //total chars written to all handlers
        private int totalChars = 0;
        public RecursivelySecureContentHandler(ContentHandler handler, TikaInputStream stream,
                                               int totalWriteLimit,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
     * extract Metadata.
     */
    private Map<Pattern, String> metadataPatterns = null;
    /**
     * The external command to invoke.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/language/detect/LanguageDetector.java`
#### Snippet
```java
    // True if text is expected to be a mix of languages, and thus higher-resolution
    // detection must be done to avoid under-sampling the text.
    protected boolean mixedLanguages = false;

    // True if the text is expected to be 'short' (typically less than 100 chars), and
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/language/detect/LanguageDetector.java`
#### Snippet
```java
    // True if the text is expected to be 'short' (typically less than 100 chars), and
    // thus a different algorithm and/or set of profiles should be used.
    protected boolean shortText = false;

    public static LanguageDetector getDefaultLanguageDetector() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/parser/external2/ExternalParser.java`
#### Snippet
```java
    private Parser outputParser = EmptyParser.INSTANCE;

    private boolean returnStdout = false;

    private boolean returnStderr = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
     * Mapping of Tika metadata to command line parameters.
     */
    private Map<Property, String[]> metadataCommandArguments = null;
    /**
     * The external command to invoke.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
    private String commandAssignmentDelimeter = ", ";
    private String commandAppendOperator = "=";
    private boolean quoteAssignmentValues = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-core/src/main/java/org/apache/tika/metadata/writefilter/StandardWriteFilterFactory.java`
#### Snippet
```java
    private int maxTotalEstimatedBytes = DEFAULT_TOTAL_ESTIMATED_BYTES;
    private int maxValuesPerField = DEFAULT_MAX_VALUES_PER_FIELD;
    private boolean includeEmpty = false;

    public MetadataWriteFilter newInstance() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/metadata/writefilter/StandardWriteFilterFactory.java`
#### Snippet
```java
    public static int DEFAULT_MAX_VALUES_PER_FIELD = 10;

    private Set<String> includeFields = null;
    private int maxKeySize = DEFAULT_MAX_KEY_SIZE;
    private int maxFieldSize = DEFAULT_MAX_FIELD_SIZE;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/renderer/RenderingTracker.java`
#### Snippet
```java
public class RenderingTracker {

    private int id = 0;

    public synchronized int getNextId() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/isatab/ISArchiveParser.java`
#### Snippet
```java
    private String location = null;

    private String studyFileName = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/isatab/ISArchiveParser.java`
#### Snippet
```java
    private final Set<MediaType> SUPPORTED_TYPES =
            Collections.singleton(MediaType.application("x-isatab"));
    private String location = null;

    private String studyFileName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
     * A map of all metadata attributes.
     */
    private Map<String, String[]> metadata = null;


```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-core/src/main/java/org/apache/tika/metadata/writefilter/StandardWriteFilter.java`
#### Snippet
```java

    //tracks the estimated size in utf16 bytes. Can be > maxEstimated size
    int estimatedSize = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-pipes/tika-emitters/tika-emitter-opensearch/src/main/java/org/apache/tika/pipes/emitter/opensearch/OpenSearchEmitter.java`
#### Snippet
```java

    private UpdateStrategy updateStrategy = UpdateStrategy.OVERWRITE;
    private String openSearchUrl = null;
    private String idField = "_id";
    private int commitWithin = 1000;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-opensearch/src/main/java/org/apache/tika/pipes/reporters/opensearch/OpenSearchPipesReporter.java`
#### Snippet
```java
    private String exitValueKey = DEFAULT_EXIT_VALUE_KEY;

    private boolean includeRouting = false;


```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-crypto-module/src/main/java/org/apache/tika/parser/crypto/TSDParser.java`
#### Snippet
```java
        private String policyId = "";
        private BigInteger serialNumber = null;
        private GeneralName tsa = null;
        private String algorithm = "";
        private Date parsedDate = new Date();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-crypto-module/src/main/java/org/apache/tika/parser/crypto/TSDParser.java`
#### Snippet
```java
        private Date emitDate = new Date();
        private String policyId = "";
        private BigInteger serialNumber = null;
        private GeneralName tsa = null;
        private String algorithm = "";
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/StarOfficeDetector.java`
#### Snippet
```java
    private static class StarOfficeXHandler extends DefaultHandler {

        private MediaType mediaType = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/KMZDetector.java`
#### Snippet
```java

    private static class KMLCounter {
        private int cnt = 0;

        int getCount() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/RFC822Parser.java`
#### Snippet
```java

    @Field
    private boolean extractAllAlternatives = false;

    public Set<MediaType> getSupportedTypes(ParseContext context) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mbox/MboxParser.java`
#### Snippet
```java
    private static final String EMAIL_FROMLINE_METADATA = EMAIL_HEADER_METADATA_PREFIX + "from";
    private final Map<Integer, Metadata> trackingMetadata = new HashMap<>();
    private boolean tracking = false;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v1Handler.java`
#### Snippet
```java
 */
public class ID3v1Handler implements ID3Tags {
    boolean found = false;
    private String title;
    private String artist;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java
        private int flagLength;

        private int offset = 0;

        protected RawTagIterator(int nameLength, int sizeLength, int sizeMultiplier,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/LyricsHandler.java`
#### Snippet
```java
 */
public class LyricsHandler {
    boolean foundLyrics = false;
    String lyricsText = null;
    ID3v1Handler id3v1 = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/LyricsHandler.java`
#### Snippet
```java
public class LyricsHandler {
    boolean foundLyrics = false;
    String lyricsText = null;
    ID3v1Handler id3v1 = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/LyricsHandler.java`
#### Snippet
```java
    boolean foundLyrics = false;
    String lyricsText = null;
    ID3v1Handler id3v1 = null;

    public LyricsHandler(InputStream stream, ContentHandler handler)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mail/MailContentHandler.java`
#### Snippet
```java
    private final EmbeddedDocumentExtractor extractor;
    private final Detector detector;
    private boolean strictParsing = false;
    //this is used to buffer a multipart body that
    //keeps track of multipart/alternative and its children
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp4/boxes/TikaUserDataBox.java`
#### Snippet
```java
    private String coordinateString;

    private boolean isQuickTime = false;
    private final Metadata metadata;
    private final XHTMLContentHandler xhtml;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFRow.java`
#### Snippet
```java

    DBFCell[] cells;
    private boolean isDeleted = false;

    DBFRow(DBFFileHeader header) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFColumnHeader.java`
#### Snippet
```java
    int fieldLength = -1;
    int decimalCount = -1;
    private ColType colType = null;

    public void setType(int type) throws TikaException {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFCell.java`
#### Snippet
```java
    private final byte[] bytes;
    private final int decimalCount;
    int bytesReadLast = 0;

    DBFCell(DBFColumnHeader.ColType colType, int fieldLength, int decimalCount) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFReader.java`
#### Snippet
```java
    public static final int MAX_FIELD_LENGTH = 66000;
    private static final Map<Integer, Version> VERSION_MAP = new ConcurrentHashMap<>();
    public static boolean STRICT = false;

    static {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFReader.java`
#### Snippet
```java
    private final DBFFileHeader header;
    private final InputStream is;
    private DBFRow currRow = null;
    private Charset charset = StandardCharsets.US_ASCII;
    private DBFReader(InputStream is) throws IOException, TikaException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFContentHandler.java`
#### Snippet
```java
    private final ContentHandler handler;
    private final Metadata metadata;
    private boolean inContent = false;
    private boolean inPage = false;
    private int bodyPageCount = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFContentHandler.java`
#### Snippet
```java
    private boolean inContent = false;
    private boolean inPage = false;
    private int bodyPageCount = 0;
    private int masterPageCount = 0;
    private int referencePageCount = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFContentHandler.java`
#### Snippet
```java
    private boolean inPage = false;
    private int bodyPageCount = 0;
    private int masterPageCount = 0;
    private int referencePageCount = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFContentHandler.java`
#### Snippet
```java
    private final Metadata metadata;
    private boolean inContent = false;
    private boolean inPage = false;
    private int bodyPageCount = 0;
    private int masterPageCount = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFContentHandler.java`
#### Snippet
```java
    private int bodyPageCount = 0;
    private int masterPageCount = 0;
    private int referencePageCount = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/FlatOpenDocumentMacroHandler.java`
#### Snippet
```java
    private final StringBuilder macroBuffer = new StringBuilder();
    String macroName = null;
    boolean inMacro = false;
    private EmbeddedDocumentExtractor embeddedDocumentExtractor;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/FlatOpenDocumentMacroHandler.java`
#### Snippet
```java
    private final ParseContext parseContext;
    private final StringBuilder macroBuffer = new StringBuilder();
    String macroName = null;
    boolean inMacro = false;
    private EmbeddedDocumentExtractor embeddedDocumentExtractor;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/FlatOpenDocumentParser.java`
#### Snippet
```java
        private final boolean extractMacros;
        private ContentHandler currentHandler = defaultHandler;
        private MediaType detectedType = null;

        private FlatOpenDocumentParserHandler(ContentHandler baseHandler, Metadata metadata,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/FlatOpenDocumentParser.java`
#### Snippet
```java
            .unmodifiableSet(new HashSet<>(Arrays.asList(FLAT_OD, FLAT_ODT, FLAT_ODP, FLAT_ODS)));

    private boolean extractMacros = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    private StringBuilder base64BinaryDataBuffer = new StringBuilder();
    private int nodeDepth = 0;
    private int completelyFiltered = 0;
    private Stack<String> headingStack = new Stack<>();
    private Map<String, TextStyle> paragraphTextStyleMap = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    private boolean curBold;
    private boolean curItalic;
    private int pDepth = 0;
    OpenDocumentBodyHandler(ContentHandler handler, ParseContext parseContext) {
        super(handler, MAPPINGS);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    private EmbeddedDocumentExtractor embeddedDocumentExtractor;
    private StringBuilder base64BinaryDataBuffer = new StringBuilder();
    private int nodeDepth = 0;
    private int completelyFiltered = 0;
    private Stack<String> headingStack = new Stack<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    //have we written the start style tags
    //yet for the current text style
    boolean hasWrittenStartStyleTags = false;
    //if we're in a binary-data tag
    boolean inBinaryData = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentBodyHandler.java`
#### Snippet
```java
    boolean hasWrittenStartStyleTags = false;
    //if we're in a binary-data tag
    boolean inBinaryData = false;
    private EmbeddedDocumentExtractor embeddedDocumentExtractor;
    private StringBuilder base64BinaryDataBuffer = new StringBuilder();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/indesign/ContentAndMetadataExtractor.java`
#### Snippet
```java
        private final ContentHandler handler;
        private final Metadata metadata;
        private boolean inContent = false;

        ContentAndMetadataHandler(ContentHandler handler, Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/indesign/IDMLParser.java`
#### Snippet
```java
     * Internal page count.
     */
    private int pageCount = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/indesign/IDMLParser.java`
#### Snippet
```java
     * Internal master spread count.
     */
    private int masterSpreadCount = 0;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
                    MediaType.application("x-ibooks+zip"))));
    @Field
    boolean streaming = false;
    private Parser meta = new DcXMLParser();
    private Parser content = new EpubContentParser();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
        Map<String, HRefMediaPair> locationMap = new HashMap<>();
        List<String> contentItems = new ArrayList<>();
        boolean inManifest = false;
        boolean inSpine = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java

    private static class RootFinder extends DefaultHandler {
        String root = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/epub/EpubParser.java`
#### Snippet
```java
        List<String> contentItems = new ArrayList<>();
        boolean inManifest = false;
        boolean inSpine = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentParser.java`
#### Snippet
```java

    private Parser content = new OpenDocumentContentParser();
    private boolean extractMacros = false;

    public Parser getMetaParser() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPDocumentAreaExtractor.java`
#### Snippet
```java
abstract class WPDocumentAreaExtractor {

    boolean startedP = false;

    public void extract(WPInputStream in, XHTMLContentHandler xhtml)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WP6DocumentAreaExtractor.java`
#### Snippet
```java
                    });
    private boolean includeDeletedContent = true;
    private boolean inUndo = false;

    public WP6DocumentAreaExtractor(boolean includeDeletedContent) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-app/src/main/java/org/apache/tika/batch/DigestingAutoDetectParserFactory.java`
#### Snippet
```java
public class DigestingAutoDetectParserFactory extends ParserFactory {

    private DigestingParser.Digester digester = null;


```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
        private final int maxGram;
        private String next;
        private int pos = 0;
        private int[] buffer;
        private int currGram;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniserConfig.java`
#### Snippet
```java

    private String pathClassifyModel = null;
    private String pathClassifyRegression = null;

    public AgeRecogniserConfig(Map<String, Param> params) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniserConfig.java`
#### Snippet
```java
public class AgeRecogniserConfig {

    private String pathClassifyModel = null;
    private String pathClassifyRegression = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniser.java`
#### Snippet
```java
    private static final Set<MediaType> SUPPORTED_TYPES = Collections.singleton(MEDIA_TYPE);
    private static AgePredicterLocal agePredictor;
    private static volatile boolean available = false;
    public Tika secondaryParser;
    private AgeRecogniserConfig config;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/tmx/TMXContentHandler.java`
#### Snippet
```java
    private String sourceLang;
    private final Set<String> targetLanguages = new HashSet<>();
    private int numberOfTUs = 0;
    private int numberOfTUVs = 0;
    private boolean inSegment = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/tmx/TMXContentHandler.java`
#### Snippet
```java
    private final Set<String> targetLanguages = new HashSet<>();
    private int numberOfTUs = 0;
    private int numberOfTUVs = 0;
    private boolean inSegment = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/tmx/TMXContentHandler.java`
#### Snippet
```java
    private int numberOfTUs = 0;
    private int numberOfTUVs = 0;
    private boolean inSegment = false;

    TMXContentHandler(XHTMLContentHandler xhtml, Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/FictionBookParser.java`
#### Snippet
```java
        private final ContentHandler handler;
        private final StringBuilder binaryData = new StringBuilder();
        private boolean binaryMode = false;
        private Metadata metadata;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/XMLProfiler.java`
#### Snippet
```java
        private final Metadata metadata;

        int starts = 0;

        Map<String, Set> entities = new TreeMap<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/ElementMetadataHandler.java`
#### Snippet
```java

    private int matchLevel = 0;
    private int parentMatchLevel = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/ElementMetadataHandler.java`
#### Snippet
```java
    private boolean isBagless = true;

    private int matchLevel = 0;
    private int parentMatchLevel = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
    private boolean asyncMode = false;
    private boolean pipeMode = true;
    private boolean fork = false;
    private boolean prettyPrint;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
     * Output character encoding, or <code>null</code> for platform default
     */
    private String encoding = null;
    private final OutputType TEXT = new OutputType() {
        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
    private OutputType type = XML;
    private boolean recursiveJSON = false;
    private URI networkURI = null;
    /**
     * Output character encoding, or <code>null</code> for platform default
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        private final EmbeddedStreamTranslator embeddedStreamTranslator =
                new DefaultEmbeddedStreamTranslator();
        private int count = 0;

        public boolean shouldParseEmbedded(Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
    private String password = System.getenv("TIKA_PASSWORD");
    private DigestingParser.Digester digester = null;
    private boolean asyncMode = false;
    private boolean pipeMode = true;
    private boolean fork = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
     */
    private String password = System.getenv("TIKA_PASSWORD");
    private DigestingParser.Digester digester = null;
    private boolean asyncMode = false;
    private boolean pipeMode = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
    private String configFilePath;
    private OutputType type = XML;
    private boolean recursiveJSON = false;
    private URI networkURI = null;
    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xliff/XLIFF12ContentHandler.java`
#### Snippet
```java
    private final Metadata metadata;
    private int numberOfFiles = 0;
    private int numberOfTUs = 0;
    private boolean inTransUnit = false;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xliff/XLIFF12ContentHandler.java`
#### Snippet
```java
    private final XHTMLContentHandler xhtml;
    private final Metadata metadata;
    private int numberOfFiles = 0;
    private int numberOfTUs = 0;
    private boolean inTransUnit = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xliff/XLIFF12ContentHandler.java`
#### Snippet
```java
    private int numberOfFiles = 0;
    private int numberOfTUs = 0;
    private boolean inTransUnit = false;

    XLIFF12ContentHandler(XHTMLContentHandler xhtml, Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-s3/src/main/java/org/apache/tika/pipes/pipesiterator/s3/S3PipesIterator.java`
#### Snippet
```java
    private String profile;
    private String bucket;
    private Pattern fileNamePattern = null;
    private int maxConnections = ClientConfiguration.DEFAULT_MAX_CONNECTIONS;
    private boolean pathStyleAccessEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-s3/src/main/java/org/apache/tika/pipes/pipesiterator/s3/S3PipesIterator.java`
#### Snippet
```java
    private Pattern fileNamePattern = null;
    private int maxConnections = ClientConfiguration.DEFAULT_MAX_CONNECTIONS;
    private boolean pathStyleAccessEnabled = false;

    private AmazonS3 s3Client;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MarianTranslator.java`
#### Snippet
```java
        Session session;
        File translationResult;
        volatile boolean receivedResponse = false;

        /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/RTGTranslator.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(RTGTranslator.class);
    private WebClient client;
    private boolean isAvailable = false;

    public RTGTranslator() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ServerStatusWatcher.java`
#### Snippet
```java
    private final Path forkedStatusPath;
    private final ByteBuffer statusBuffer = ByteBuffer.allocate(16);
    private volatile boolean shuttingDown = false;

    public ServerStatusWatcher(ServerStatus serverStatus, InputStream inputStream,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    private String trustStoreType = null;
    private String trustStorePassword = null;
    private String trustStoreFile = null;

    private boolean clientAuthenticationWanted = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    private String keyStoreType = null;
    private String keyStorePassword = null;
    private String keyStoreFile = null;
    private String trustStoreType = null;
    private String trustStorePassword = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    private String trustStoreFile = null;

    private boolean clientAuthenticationWanted = false;

    private boolean clientAuthenticationRequired = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    private final boolean passwordsAESEncrypted = false;
    private String keyStoreType = null;
    private String keyStorePassword = null;
    private String keyStoreFile = null;
    private String trustStoreType = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
public class TlsConfig implements Initializable {

    private boolean active = false;
    //TODO make this configurable
    private final boolean passwordsAESEncrypted = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    private boolean clientAuthenticationWanted = false;

    private boolean clientAuthenticationRequired = false;

    public boolean isActive() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    private String keyStoreFile = null;
    private String trustStoreType = null;
    private String trustStorePassword = null;
    private String trustStoreFile = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    //TODO make this configurable
    private final boolean passwordsAESEncrypted = false;
    private String keyStoreType = null;
    private String keyStorePassword = null;
    private String keyStoreFile = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    private String keyStorePassword = null;
    private String keyStoreFile = null;
    private String trustStoreType = null;
    private String trustStorePassword = null;
    private String trustStoreFile = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerCli.java`
#### Snippet
```java
    private static List<TikaServerWatchDog> WATCHERS = new ArrayList<>();

    private static boolean PREVENT_STOP = false;

    private static Options getOptions() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
    private volatile FORKED_STATUS forkedStatus = FORKED_STATUS.INITIALIZING;
    private volatile Instant lastPing = null;
    private ForkedProcess forkedProcess = null;
    private int restarts = 0;
    private volatile boolean shutDown = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
    private volatile Instant lastPing = null;
    private ForkedProcess forkedProcess = null;
    private int restarts = 0;
    private volatile boolean shutDown = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
    private final Object[] forkedStatusLock = new Object[0];
    private volatile FORKED_STATUS forkedStatus = FORKED_STATUS.INITIALIZING;
    private volatile Instant lastPing = null;
    private ForkedProcess forkedProcess = null;
    private int restarts = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
    private ForkedProcess forkedProcess = null;
    private int restarts = 0;
    private volatile boolean shutDown = false;


```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-pipes/tika-emitters/tika-emitter-jdbc/src/main/java/org/apache/tika/pipes/emitter/jdbc/JDBCEmitter.java`
#### Snippet
```java
    private String alterTable;

    private int maxRetries = 0;

    //used only for specification of column name/string definition of
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    private long taskPulseMillis = DEFAULT_TASK_PULSE_MILLIS;
    private long maxforkedStartupMillis = DEFAULT_FORKED_STARTUP_MILLIS;
    private boolean enableUnsecureFeatures = false;
    private String cors = "";
    private boolean returnStackTrace = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    private boolean enableUnsecureFeatures = false;
    private String cors = "";
    private boolean returnStackTrace = false;
    private boolean noFork = false;
    //TODO: make parameterizable for debugging
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    private String cors = "";
    private boolean returnStackTrace = false;
    private boolean noFork = false;
    //TODO: make parameterizable for debugging
    private String tempFilePrefix = "apache-tika-server-forked-tmp-";
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    //and they are automatically set by the forking process
    private String forkedStatusFile;
    private int numRestarts = 0;

    private boolean preventStopMethod = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    private int numRestarts = 0;

    private boolean preventStopMethod = false;

    private TlsConfig tlsConfig = new TlsConfig();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
    private boolean setKCMS = false;

    private boolean detectAngles = false;

    private Renderer renderer;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
    //True if inline images should only have their metadata
    //extracted.
    private boolean extractInlineImageMetadataOnly = false;

    private ImageGraphicsEngineFactory imageGraphicsEngineFactory =
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java

    // True if we let PDFBox remove duplicate overlapping text:
    private boolean suppressDuplicateOverlappingText = false;

    // True if we extract annotation text ourselves
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
    private boolean extractActions = false;

    private boolean extractFontNames = false;

    private long maxMainMemoryBytes = 512 * 1024 * 1024;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
    //If the PDF has an XFA element, process only that and skip extracting
    //content from elsewhere in the document.
    private boolean ifXFAExtractOnlyXFA = false;

    private OCR_STRATEGY ocrStrategy = OCR_STRATEGY.AUTO;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java

    //True if inline PDXImage objects should be extracted
    private boolean extractInlineImages = false;

    //True if inline images should only have their metadata
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
    private long maxMainMemoryBytes = 512 * 1024 * 1024;

    private boolean setKCMS = false;

    private boolean detectAngles = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
    //Should the PDFParser _try_ to extract marked content/structure tags (backoff to regular
    //text extraction if the given PDF doesn't have marked content)
    private boolean extractMarkedContent = false;

    //The character width-based tolerance value used to estimate where spaces in text should be
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
    // True if we should sort text tokens by position
    // (necessary for some PDFs, but messes up other PDFs):
    private boolean sortByPosition = false;

    //True if acroform content should be extracted
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParserConfig.java`
#### Snippet
```java
    private boolean catchIntermediateIOExceptions = true;

    private boolean extractActions = false;

    private boolean extractFontNames = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/image/ImageGraphicsEngine.java`
#### Snippet
```java
    protected final boolean extractInlineImageMetadataOnly;
    //TODO: parameterize this ?
    protected boolean useDirectJPEG = false;

    //TODO: this is an embarrassment of an initializer...fix
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
    private static TikaConfig TIKA_CONFIG;
    private static TikaServerConfig TIKA_SERVER_CONFIG;
    private static DigestingParser.Digester DIGESTER = null;
    private static InputStreamFactory INPUTSTREAM_FACTORY = null;
    private static ServerStatus SERVER_STATUS = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
    private static DigestingParser.Digester DIGESTER = null;
    private static InputStreamFactory INPUTSTREAM_FACTORY = null;
    private static ServerStatus SERVER_STATUS = null;

    private static ParseContextConfig PARSE_CONTEXT_CONFIG = new CompositeParseContextConfig();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
    private static TikaServerConfig TIKA_SERVER_CONFIG;
    private static DigestingParser.Digester DIGESTER = null;
    private static InputStreamFactory INPUTSTREAM_FACTORY = null;
    private static ServerStatus SERVER_STATUS = null;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenCounts.java`
#### Snippet
```java
public class TokenCounts {

    private int totalTokens = 0;
    private Map<String, MutableInt> tokens = new HashMap<>();

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenContraster.java`
#### Snippet
```java

    private int diceCoefficientNum = 0;
    private int overlapNum = 0;

    private double diceCoefficient = 0.0d;
```

### RedundantFieldInitialization
Field initialization to `0.0` is redundant
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenContraster.java`
#### Snippet
```java

    private double diceCoefficient = 0.0d;
    private double overlap = 0.0;


```

### RedundantFieldInitialization
Field initialization to `0.0d` is redundant
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenContraster.java`
#### Snippet
```java
    private int overlapNum = 0;

    private double diceCoefficient = 0.0d;
    private double overlap = 0.0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenContraster.java`
#### Snippet
```java
    private int topN = 10;

    private int diceCoefficientNum = 0;
    private int overlapNum = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
        private StringBuilder hrefAnchorBuilder = new StringBuilder();
        private String uri = null;
        private int tdDepth = 0;
    }

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
        Set<MCID> processedMCIDs = new HashSet<>();
        boolean inLink = false;
        int tableDepth = 0;
        private StringBuilder hrefAnchorBuilder = new StringBuilder();
        private String uri = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
        int tableDepth = 0;
        private StringBuilder hrefAnchorBuilder = new StringBuilder();
        private String uri = null;
        private int tdDepth = 0;
    }
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
    private static class State {
        Set<MCID> processedMCIDs = new HashSet<>();
        boolean inLink = false;
        int tableDepth = 0;
        private StringBuilder hrefAnchorBuilder = new StringBuilder();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java

    //contains at least one font that is not embedded
    boolean containsNonEmbeddedFont = false;

    //contains at least one broken font
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
    int startPage = -1;
    //private in PDFTextStripper...must have own copy because we override processpages
    int unmappedUnicodeCharsPerPage = 0;
    int totalCharsPerPage = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java

    //contains at least one broken font
    boolean containsDamagedFont = false;

    int num3DAnnotations = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
    private final Set<String> actionTypes = new TreeSet<>();
    //zero-based pageIndex
    int pageIndex = 0;
    int startPage = -1;
    //private in PDFTextStripper...must have own copy because we override processpages
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
    //private in PDFTextStripper...must have own copy because we override processpages
    int unmappedUnicodeCharsPerPage = 0;
    int totalCharsPerPage = 0;

    int totalUnmappedUnicodeCharacters;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
    boolean containsDamagedFont = false;

    int num3DAnnotations = 0;

    AbstractPDF2XHTML(PDDocument pdDocument, ContentHandler handler, ParseContext context,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/prt/PRTParser.java`
#### Snippet
```java
    private static class Last5 {
        byte[] data = new byte[5];
        int pos = 0;

        private void record(int b) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-pipes/tika-fetchers/tika-fetcher-http/src/main/java/org/apache/tika/pipes/fetcher/http/HttpFetcher.java`
#### Snippet
```java
    //When making the request, what User-Agent is sent.
    //By default httpclient adds e.g. "Apache-HttpClient/4.5.13 (Java/x.y.z)"
    private String userAgent = null;


```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/JDBCTableReader.java`
#### Snippet
```java
    int maxClobLength = 1000000;
    ResultSet results = null;
    int rows = 0;

    public JDBCTableReader(Connection connection, String tableName,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/JDBCTableReader.java`
#### Snippet
```java
    private final EmbeddedDocumentUtil embeddedDocumentUtil;
    int maxClobLength = 1000000;
    ResultSet results = null;
    int rows = 0;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-pipes/tika-emitters/tika-emitter-fs/src/main/java/org/apache/tika/pipes/emitter/fs/FileSystemEmitter.java`
#### Snippet
```java
public class FileSystemEmitter extends AbstractEmitter implements StreamEmitter {

    private Path basePath = null;
    private String fileExtension = "json";
    private ON_EXISTS onExists = ON_EXISTS.EXCEPTION;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * An index of the ngrams of the profile
     */
    private Map<CharSequence, NGramEntry> ngrams = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
         * The sequence of characters of the ngram
         */
        CharSequence seq = null;
        /**
         * The NGRamProfile this NGram is related to
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The profile's name
     */
    private String name = null;
    /**
     * The NGrams of this profile sorted on the number of occurrences
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
         * The number of occurences of this ngram in its profile
         */
        private int count = 0;

        /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The total number of ngrams occurences
     */
    private int[] ngramcounts = null;
    /**
     * An index of the ngrams of the profile
```

### RedundantFieldInitialization
Field initialization to `0.0F` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
         * The frequency of this ngram in its profile
         */
        private float frequency = 0.0F;

        /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
         * The NGRamProfile this NGram is related to
         */
        private LanguageProfilerBuilder profile = null;
        /**
         * The number of occurences of this ngram in its profile
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
     * The NGrams of this profile sorted on the number of occurrences
     */
    private List<NGramEntry> sorted = null;
    /**
     * The min length of ngram
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfile.java`
#### Snippet
```java
    private class Interleaved {

        private char[] entries = null; // <ngram(length chars)><count(2 chars)>*
        private int size = 0; // Number of entries (one entry = length+2 chars)
        private long entriesGeneratedAtCount = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfile.java`
#### Snippet
```java
        private class Entry implements Comparable<Entry> {
            char[] ngram = new char[length];
            int count = 0;
            int pos = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfile.java`
#### Snippet
```java
     * Used to calculate relative ngram frequency.
     */
    private long count = 0;

    private static final Logger LOG = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfile.java`
#### Snippet
```java
            char[] ngram = new char[length];
            int count = 0;
            int pos = 0;

            private void update(int pos) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfile.java`
#### Snippet
```java

        private char[] entries = null; // <ngram(length chars)><count(2 chars)>*
        private int size = 0; // Number of entries (one entry = length+2 chars)
        private long entriesGeneratedAtCount = -1;
        // Keeps track of when the sequential structure was current
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfile.java`
#### Snippet
```java

    private static class Counter {
        private long count = 0;

        public String toString() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/AbstractDBBuffer.java`
#### Snippet
```java
    private final Lock w = rwl.writeLock();

    private int numWrites = 0;

    public int getId(String key) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/XMLLogReader.java`
#### Snippet
```java
                        "xmlns:log4j=\"http://jakarta.apache.org/log4j/\">";
        private static final String FOOTER = "</log4j:eventSet>";
        int currentStreamIndex = 0;
        private InputStream[] streams;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
    private final String connectionString;
    private String driverClass;
    private Connection connection = null;
    public JDBCUtil(String connectionString, String driverClass) {
        this.connectionString = connectionString;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/DBWriter.java`
#### Snippet
```java
    private static class LastInsert {
        private long lastInsert = System.currentTimeMillis();
        private long rowCount = 0;
    }
}
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/XLSXHREFFormatter.java`
#### Snippet
```java
    private XSSFWorkbook workbook;
    private CellStyle style;
    private int links = 0;

    public XLSXHREFFormatter(String urlBase, HyperlinkType hyperlinkType) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java

    @Field
    private boolean extractScripts = false;

    public HtmlParser() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/CharsetDetectionResult.java`
#### Snippet
```java
class CharsetDetectionResult {
    private boolean found = false;
    private Charset charset = null;

    private CharsetDetectionResult() { /* default result: not found */}
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/CharsetDetectionResult.java`
#### Snippet
```java
 */
class CharsetDetectionResult {
    private boolean found = false;
    private Charset charset = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/charsets/ReplacementCharset.java`
#### Snippet
```java
    public CharsetDecoder newDecoder() {
        return new CharsetDecoder(this, Float.MIN_VALUE, 1) {
            private boolean replacementErrorReturned = false;

            @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/MetaProcessor.java`
#### Snippet
```java
    private Set<String> attributeNames = new HashSet<>();
    private boolean gotPragma = false;
    private Boolean needPragma = null; // needPragma can be null, true, or false
    private CharsetDetectionResult detectionResult = CharsetDetectionResult.notFound();

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/charsetdetector/MetaProcessor.java`
#### Snippet
```java
class MetaProcessor {
    private Set<String> attributeNames = new HashSet<>();
    private boolean gotPragma = false;
    private Boolean needPragma = null; // needPragma can be null, true, or false
    private CharsetDetectionResult detectionResult = CharsetDetectionResult.notFound();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoParserConfig.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(GeoParserConfig.class);
    private URL nerModelUrl = null;
    private String gazetteerRestEndpoint = null;

    public GeoParserConfig() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoParserConfig.java`
#### Snippet
```java
    private static final long serialVersionUID = -3167692634278575818L;
    private static final Logger LOG = LoggerFactory.getLogger(GeoParserConfig.class);
    private URL nerModelUrl = null;
    private String gazetteerRestEndpoint = null;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java
    private int discardLevel = 0;
    private int titleLevel = 0;
    private int scriptLevel = 0;
    private Attributes scriptAtts = EMPTY_ATTS;//attributes from outermost script element
    private boolean isTitleSetToMetadata = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java
    private final StringBuilder script = new StringBuilder();
    private int bodyLevel = 0;
    private int discardLevel = 0;
    private int titleLevel = 0;
    private int scriptLevel = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java
    private final DataURISchemeUtil dataURISchemeUtil = new DataURISchemeUtil();
    private final StringBuilder script = new StringBuilder();
    private int bodyLevel = 0;
    private int discardLevel = 0;
    private int titleLevel = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java
    private int bodyLevel = 0;
    private int discardLevel = 0;
    private int titleLevel = 0;
    private int scriptLevel = 0;
    private Attributes scriptAtts = EMPTY_ATTS;//attributes from outermost script element
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java
    private int scriptLevel = 0;
    private Attributes scriptAtts = EMPTY_ATTS;//attributes from outermost script element
    private boolean isTitleSetToMetadata = false;

    private HtmlHandler(HtmlMapper mapper, XHTMLContentHandler xhtml, Metadata metadata,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/nltk/NLTKNERecogniser.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(NLTKNERecogniser.class);
    private static final String NLTK_REST_HOST = "http://localhost:8881";
    private static boolean available = false;
    private String restHostUrlStr;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/NamedEntityParser.java`
#### Snippet
```java
    public Tika secondaryParser;
    private List<NERecogniser> nerChain;
    private volatile boolean initialized = false;
    private volatile boolean available = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/NamedEntityParser.java`
#### Snippet
```java
    private List<NERecogniser> nerChain;
    private volatile boolean initialized = false;
    private volatile boolean available = false;

    private synchronized void initialize(ParseContext context) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/regex/RegexNERecogniser.java`
#### Snippet
```java
    public Set<String> entityTypes = new HashSet<>();
    public Map<String, Pattern> patterns;
    private boolean available = false;

    public RegexNERecogniser() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java

    CompositeTextStatsCalculator compositeTextStatsCalculator;
    private String lastExtractExtension = null;

    public AbstractProfiler(ArrayBlockingQueue<FileResource> fileQueue, IDBWriter writer) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/mitie/MITIENERecogniser.java`
#### Snippet
```java
    private static final String NamedEntityExtractor_Class =
            "edu.mit.ll.mitie.NamedEntityExtractor";
    private boolean available = false;
    private Object extractorInstance;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/corenlp/CoreNLPNERecogniser.java`
#### Snippet
```java
    private static final String CLASSIFIER_CLASS_NAME = "edu.stanford.nlp.ie.crf.CRFClassifier";

    private boolean available = false;
    private Field firstField;
    private Field secondField;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/grobid/GrobidNERecogniser.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(GrobidNERecogniser.class);
    private static final String GROBID_REST_HOST = "http://localhost:8080";
    private static boolean available = false;
    private String restHostUrlStr;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESConfig.java`
#### Snippet
```java

    // List of metadata to analyze using cTAKES
    private String[] metadata = null;

    // List of annotation properties to add to metadata in addition to text covered by an annotation
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESConfig.java`
#### Snippet
```java

    // Enables CAS serialization
    private boolean serialize = false;

    // Enables text analysis using cTAKES
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESConfig.java`
#### Snippet
```java

    // List of annotation properties to add to metadata in addition to text covered by an annotation
    private CTAKESAnnotationProperty[] annotationProps = null;

    // Character used to separate the annotation properties into metadata
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

    // Metadata object used for cTAKES annotations
    private Metadata metadata = null;

    // UIMA Analysis Engine
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

    // Configuration object for CTAKESContentHandler
    private CTAKESConfig config = null;

    // StringBuilder object used to build the clinical free-text for cTAKES
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

    // UIMA Analysis Engine
    private AnalysisEngine ae = null;

    // JCas object for working with the CAS (Common Analysis System)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

    // StringBuilder object used to build the clinical free-text for cTAKES
    private StringBuilder sb = null;

    // Metadata object used for cTAKES annotations
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

    // JCas object for working with the CAS (Common Analysis System)
    private JCas jcas = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/GrobidRESTParser.java`
#### Snippet
```java
    private static final String GROBID_LEGACY_ISALIVE_PATH = "/grobid";
    private static final String GROBID_LEGACY_PROCESSHEADER_PATH = "/processHeaderDocument";
    private Boolean legacyMode = null;
    private String restHostUrlStr;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-emitters/tika-emitter-kafka/src/main/java/org/apache/tika/pipes/emitter/kafka/KafkaEmitter.java`
#### Snippet
```java
    int connectionsMaxIdleMs = 9 * 60 * 1000;
    int deliveryTimeoutMs = 120 * 1000;
    boolean enableIdempotence = false;
    String interceptorClasses;
    int maxBlockMs = 60 * 1000;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-emitters/tika-emitter-az-blob/src/main/java/org/apache/tika/pipes/emitter/azblob/AZBlobEmitter.java`
#### Snippet
```java
    private BlobServiceClient blobServiceClient;
    private BlobContainerClient blobContainerClient;
    private boolean overwriteExisting = false;
    /**
     * Requires the src-bucket/path/to/my/file.txt in the {@link TikaCoreProperties#SOURCE_PATH}.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
    private int maxFilesToConsider = -1;
    private long maxConsecWaitInMillis = 300000;//300,000ms = 5 minutes
    private DocumentSelector documentSelector = null;

    //number of files added to queue
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
    private final int numConsumers;
    private volatile boolean hasCompletedCrawling = false;
    private volatile boolean shutDownNoPoison = false;
    private volatile boolean isActive = true;
    private volatile boolean timedOut = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
    private int added = 0;
    //number of files considered including those that were rejected by documentSelector
    private int considered = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
    private volatile boolean shutDownNoPoison = false;
    private volatile boolean isActive = true;
    private volatile boolean timedOut = false;
    private int maxFilesToAdd = -1;
    private int maxFilesToConsider = -1;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java
    private final ArrayBlockingQueue<FileResource> queue;
    private final int numConsumers;
    private volatile boolean hasCompletedCrawling = false;
    private volatile boolean shutDownNoPoison = false;
    private volatile boolean isActive = true;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceCrawler.java`
#### Snippet
```java

    //number of files added to queue
    private int added = 0;
    //number of files considered including those that were rejected by documentSelector
    private int considered = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/StatusReporter.java`
#### Snippet
```java
    private long staleThresholdMillis = 100000;

    private volatile boolean isShuttingDown = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    //forked process has not yet exited
    private int waitNumLoopsAfterRestartMessage = 60;
    private int loopsAfterRestartMessageReceived = 0;
    private volatile boolean userInterrupted = false;
    private boolean receivedRestartMsg = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private volatile boolean userInterrupted = false;
    private boolean receivedRestartMsg = false;
    private Process process = null;
    private StreamGobbler errorWatcher = null;
    private StreamGobbler outGobbler = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private Process process = null;
    private StreamGobbler errorWatcher = null;
    private StreamGobbler outGobbler = null;
    private InterruptWriter interruptWriter = null;
    private Thread errorWatcherThread = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private Thread outGobblerThread = null;
    private Thread interruptWriterThread = null;
    private int numRestarts = 0;
    private boolean redirectForkedProcessToStdOut = true;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private boolean receivedRestartMsg = false;
    private Process process = null;
    private StreamGobbler errorWatcher = null;
    private StreamGobbler outGobbler = null;
    private InterruptWriter interruptWriter = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private InterruptWriter interruptWriter = null;
    private Thread errorWatcherThread = null;
    private Thread outGobblerThread = null;
    private Thread interruptWriterThread = null;
    private int numRestarts = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private int loopsAfterRestartMessageReceived = 0;
    private volatile boolean userInterrupted = false;
    private boolean receivedRestartMsg = false;
    private Process process = null;
    private StreamGobbler errorWatcher = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private Thread errorWatcherThread = null;
    private Thread outGobblerThread = null;
    private Thread interruptWriterThread = null;
    private int numRestarts = 0;
    private boolean redirectForkedProcessToStdOut = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private StreamGobbler outGobbler = null;
    private InterruptWriter interruptWriter = null;
    private Thread errorWatcherThread = null;
    private Thread outGobblerThread = null;
    private Thread interruptWriterThread = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private StreamGobbler errorWatcher = null;
    private StreamGobbler outGobbler = null;
    private InterruptWriter interruptWriter = null;
    private Thread errorWatcherThread = null;
    private Thread outGobblerThread = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    private int waitNumLoopsAfterRestartMessage = 60;
    private int loopsAfterRestartMessageReceived = 0;
    private volatile boolean userInterrupted = false;
    private boolean receivedRestartMsg = false;
    private Process process = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        int numNonConsumers = 0;
        int removed = 0;
        int consumersRemoved = 0;
        int crawlersRemoved = 0;
        CAUSE_FOR_TERMINATION causeForTermination = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        int numConsumers = 0;
        int numNonConsumers = 0;
        int removed = 0;
        int consumersRemoved = 0;
        int crawlersRemoved = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
    private static class State {
        long start = -1;
        int numConsumers = 0;
        int numNonConsumers = 0;
        int removed = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        int consumersRemoved = 0;
        int crawlersRemoved = 0;
        CAUSE_FOR_TERMINATION causeForTermination = null;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
    //every hour or so.
    private int maxAliveTimeSeconds = -1;
    private boolean alreadyExecuted = false;

    public BatchProcess(FileResourceCrawler fileResourceCrawler, ConsumersManager consumersManager,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        long start = -1;
        int numConsumers = 0;
        int numNonConsumers = 0;
        int removed = 0;
        int consumersRemoved = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
        int removed = 0;
        int consumersRemoved = 0;
        int crawlersRemoved = 0;
        CAUSE_FOR_TERMINATION causeForTermination = null;
    }
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
    //being processed.  It is null if no file is currently being processed.
    //no need for volatile because of lock for checkForStales
    private FileStarted currentFile = null;
    private final AtomicInteger numResourcesConsumed = new AtomicInteger(0);
    private final AtomicInteger numHandledExceptions = new AtomicInteger(0);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-pipes/tika-httpclient-commons/src/main/java/org/apache/tika/client/HttpClientFactory.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(HttpClientFactory.class);

    private AES aes = null;

    private String proxyHost;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-httpclient-commons/src/main/java/org/apache/tika/client/HttpClientFactory.java`
#### Snippet
```java
    private String authScheme = "basic"; //ntlm or basic
    private boolean credentialsAESEncrypted = false;
    private boolean disableContentCompression = false;

    public String getProxyHost() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-httpclient-commons/src/main/java/org/apache/tika/client/HttpClientFactory.java`
#### Snippet
```java
    private String ntDomain;//if using nt credentials
    private String authScheme = "basic"; //ntlm or basic
    private boolean credentialsAESEncrypted = false;
    private boolean disableContentCompression = false;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
    private AmazonS3 s3Client;
    private boolean spoolToTemp = true;
    private int retries = 0;
    private long sleepBeforeRetryMillis = 30000;
    private long maxLength = -1;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
    private long sleepBeforeRetryMillis = 30000;
    private long maxLength = -1;
    private boolean pathStyleAccessEnabled = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowImageRecParser.java`
#### Snippet
```java
    private String availabilityTestArgs = ""; //when no args are given, the script will test itself!

    private boolean available = false;

    public Set<MediaType> getSupportedMimes() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-detectors/tika-detector-siegfried/src/main/java/org/apache/tika/detect/siegfried/SiegfriedDetector.java`
#### Snippet
```java

    private static ObjectMapper OBJECT_MAPPER = new ObjectMapper();
    private static boolean HAS_WARNED = false;
    private Boolean hasSiegfriedCommand = null;
    private String siegfriedPath = DEFAULT_SIEGFRIED_PATH;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-detectors/tika-detector-siegfried/src/main/java/org/apache/tika/detect/siegfried/SiegfriedDetector.java`
#### Snippet
```java
    private static ObjectMapper OBJECT_MAPPER = new ObjectMapper();
    private static boolean HAS_WARNED = false;
    private Boolean hasSiegfriedCommand = null;
    private String siegfriedPath = DEFAULT_SIEGFRIED_PATH;
    private int maxBytes = 1_000_000;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-detectors/tika-detector-siegfried/src/main/java/org/apache/tika/detect/siegfried/SiegfriedDetector.java`
#### Snippet
```java
    private long timeoutMs = DEFAULT_TIMEOUT_MS;

    private boolean useMime = false;

    public static boolean checkHasSiegfried(String siegfriedCommandPath) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
public class GrabPhoneNumbersExample {
    private static HashSet<String> phoneNumbers = new HashSet<>();
    private static int failedFiles, successfulFiles = 0;

    public static void main(String[] args) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
public class StandardsExtractionExample {
    private static HashSet<String> standardReferences = new HashSet<>();
    private static int failedFiles = 0;
    private static int successfulFiles = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
    private static HashSet<String> standardReferences = new HashSet<>();
    private static int failedFiles = 0;
    private static int successfulFiles = 0;

    public static void main(String[] args) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-example/src/main/java/org/apache/tika/example/Pharmacy.java`
#### Snippet
```java

public class Pharmacy {
    private static Key key = null;

    public static Key getKey() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-example/src/main/java/org/apache/tika/example/ExtractEmbeddedFiles.java`
#### Snippet
```java
    private class MyEmbeddedDocumentExtractor extends ParsingEmbeddedDocumentExtractor {
        private final Path outputDir;
        private int fileCount = 0;

        private MyEmbeddedDocumentExtractor(Path outputDir, ParseContext context) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3DBParser.java`
#### Snippet
```java
    protected static final String SQLITE_CLASS_NAME = "org.sqlite.JDBC";
    //If the InputStream wasn't a TikaInputStream, copy to this tmp file
    Path tmpFile = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVParams.java`
#### Snippet
```java

    private MediaType mediaType = null;
    private Character delimiter = null;
    private Charset charset = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVParams.java`
#### Snippet
```java
public class CSVParams {

    private MediaType mediaType = null;
    private Character delimiter = null;
    private Charset charset = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVParams.java`
#### Snippet
```java
    private MediaType mediaType = null;
    private Character delimiter = null;
    private Charset charset = null;

    CSVParams() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
    //
    private int fConfidence;
    private byte[] fRawInput = null;     // Original, untouched input bytes.
    //  If user gave us a byte array, this is it.
    private int fRawLength;           // Length of data in fRawInput array.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
    //  If user gave us a byte array, this is it.
    private int fRawLength;           // Length of data in fRawInput array.
    private InputStream fInputStream = null;  // User's input stream, or null if the user
    private String fCharsetName;         // The name of the charset this CharsetMatch
    //   represents.  Filled in by the recognizer.
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/cli/FuzzingCLI.java`
#### Snippet
```java
        private final PipesIterator pipesIterator;
        private final ArrayBlockingQueue<FetchEmitTuple> queue;
        private int added = 0;

        public FileAdder(PipesIterator pipesIterator, ArrayBlockingQueue<FetchEmitTuple> queue) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/UniversalEncodingListener.java`
#### Snippet
```java
    private String hint = null;

    private Charset charset = null;

    public UniversalEncodingListener(Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/UniversalEncodingListener.java`
#### Snippet
```java
    private final UniversalDetector detector = new UniversalDetector(this);

    private String hint = null;

    private Charset charset = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java

        Map<Integer, MutableInt> rowLengthCounts = new HashMap<>();
        int charsRead = 0;
        int colCount = 0;
        int encapsulated = 0; //number of cells that are encapsulated in dquotes (for now)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
        int charsRead = 0;
        int colCount = 0;
        int encapsulated = 0; //number of cells that are encapsulated in dquotes (for now)
        boolean parseException = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
        int colCount = 0;
        int encapsulated = 0; //number of cells that are encapsulated in dquotes (for now)
        boolean parseException = false;

        public Snifflet(char delimiter) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
        Map<Integer, MutableInt> rowLengthCounts = new HashMap<>();
        int charsRead = 0;
        int colCount = 0;
        int encapsulated = 0; //number of cells that are encapsulated in dquotes (for now)
        boolean parseException = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/Icu4jEncodingDetector.java`
#### Snippet
```java

    @Field
    private boolean stripMarkup = false;

    @Field
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java
    //
    private boolean fStripTags =   // If true, setText() will strip tags from input text.
            false;
    private boolean[] fEnabledRecognizers;   // If not null, active set of charset recognizers had

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java
            new short[256];  //   Value is percent, not absolute.
    boolean fC1Bytes =          // True if any bytes in the range 0x80 - 0x9F are in the input;
            false;
    String fDeclaredEncoding;
    byte[] fRawInput;     // Original, untouched input bytes.
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
     * The number of bytes into the input buffer.
     */
    private int inSize = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
     * The temporary position into the output buffer.
     */
    private int tmpPos = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
     * The current position into the output buffer.
     */
    private int outPos = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
     * The position into the input buffer.
     */
    private int inPos = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
        int charValue = 0;             // 1-4 bytes from the raw input data
        int nextIndex = 0;
        boolean error = false;
        boolean done = false;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
    static class iteratedChar {
        int charValue = 0;             // 1-4 bytes from the raw input data
        int nextIndex = 0;
        boolean error = false;
        boolean done = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
        int nextIndex = 0;
        boolean error = false;
        boolean done = false;

        void reset() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
    //
    static class iteratedChar {
        int charValue = 0;             // 1-4 bytes from the raw input data
        int nextIndex = 0;
        boolean error = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private PDDocument pdDocument = null;
    private FDFDocument fdfDocument = null;
    private boolean willEncrypt = false;
    // signing
    private final boolean incrementalUpdate = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private long number = 0;
    private int roughNumberOfObjects = 0;
    private COSObjectKey currentObjectKey = null;
    private PDDocument pdDocument = null;
    private FDFDocument fdfDocument = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private int roughNumberOfObjects = 0;
    private COSObjectKey currentObjectKey = null;
    private PDDocument pdDocument = null;
    private FDFDocument fdfDocument = null;
    private boolean willEncrypt = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    // signing
    private final boolean incrementalUpdate = false;
    private boolean reachedSignature = false;
    private long signatureOffset;
    private long signatureLength;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    // the current object number
    private long number = 0;
    private int roughNumberOfObjects = 0;
    private COSObjectKey currentObjectKey = null;
    private PDDocument pdDocument = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private long startxref = 0;
    // the current object number
    private long number = 0;
    private int roughNumberOfObjects = 0;
    private COSObjectKey currentObjectKey = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private COSStandardOutputStream standardOutput;
    // the start position of the x ref section
    private long startxref = 0;
    // the current object number
    private long number = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private COSObjectKey currentObjectKey = null;
    private PDDocument pdDocument = null;
    private FDFDocument fdfDocument = null;
    private boolean willEncrypt = false;
    // signing
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java

    private boolean stringsPresent = false;
    private boolean hasEncodingOption = false;//whether or not the strings app allows -e

    private String stringsPath = "";
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java
    private FileCommandDetector fileCommandDetector;

    private boolean stringsPresent = false;
    private boolean hasEncodingOption = false;//whether or not the strings app allows -e

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/PSDParser.java`
#### Snippet
```java
        private static final int ID_AUTO_SAVE_FILE_PATH = 0x043E;
        private static final int ID_THUMBNAIL_RESOURCE = 0x040C;
        static int counter = 0;
        private final int maxDataLengthBytes;
        private int id;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java

    private boolean inMetadata = false;
    private boolean inMetaDataTitle = false;
    private boolean inMetaDataAuthors = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private boolean inMetadata = false;
    private boolean inMetaDataTitle = false;
    private boolean inMetaDataAuthors = false;

    private boolean inParsableText = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private String tableId;
    private Integer numberOfColumns = null;
    private Integer currentColumn = null;

    private boolean inMetadata = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private boolean inBody = false;
    private String tableId;
    private Integer numberOfColumns = null;
    private Integer currentColumn = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private Integer currentColumn = null;

    private boolean inMetadata = false;
    private boolean inMetaDataTitle = false;
    private boolean inMetaDataAuthors = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private boolean inTheme = false;
    private boolean inTitle = false;
    private boolean inBody = false;
    private String tableId;
    private Integer numberOfColumns = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private final Metadata metadata;

    private boolean inSlide = false;
    private boolean inTheme = false;
    private boolean inTitle = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private boolean inSlide = false;
    private boolean inTheme = false;
    private boolean inTitle = false;
    private boolean inBody = false;
    private String tableId;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private boolean inMetaDataAuthors = false;

    private boolean inParsableText = false;

    private int numberOfSlides = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java

    private boolean inSlide = false;
    private boolean inTheme = false;
    private boolean inTitle = false;
    private boolean inBody = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/KeynoteContentHandler.java`
#### Snippet
```java
    private boolean inParsableText = false;

    private int numberOfSlides = 0;

    KeynoteContentHandler(XHTMLContentHandler xhtml, Metadata metadata) {
```

### RedundantFieldInitialization
Field initialization to `0x00` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
                (byte) 0xF6, (byte) 0xF7, (byte) 0xF8, (byte) 0xF9, (byte) 0xFA, (byte) 0xFB,
                (byte) 0xFC, (byte) 0xFD, (byte) 0xFE, (byte) 0xFF,};
        private byte alef = 0x00;


```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        private static final int N_GRAM_MASK = 0xFFFFFF;

        protected int byteIndex = 0;
        protected byte[] byteMap;
        protected byte spaceChar;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_sbcs.java`
#### Snippet
```java
        protected byte[] byteMap;
        protected byte spaceChar;
        private int ngram = 0;
        private int[] ngramList;
        private int ngramCount;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
         */
        Map<String, String> annotations = new HashMap<>();
        String currentID = null;
        StringBuffer currentText = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private HeaderFooter footers = null;
    private Footnotes footnotes = null;
    private Annotations annotations = null;
    private Map<String, List<List<String>>> tableData = new HashMap<>();
    private String activeTableId;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private int pageCount = 0;
    private int slPageCount = 0;
    private HeaderFooter headers = null;
    private HeaderFooter footers = null;
    private Footnotes footnotes = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private boolean parseProperty = false;
    private int pageCount = 0;
    private int slPageCount = 0;
    private HeaderFooter headers = null;
    private HeaderFooter footers = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
         */
        Map<String, String> footnotes = new HashMap<>();
        String lastSeenMark = null;

        /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java

    private boolean inText = false;
    private boolean parseText = false;

    private boolean inMetadata = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java
    private String metadataPropertyQName;

    private boolean inTable = false;
    private int numberOfSheets = 0;
    private int numberOfColumns = -1;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private HeaderFooter headers = null;
    private HeaderFooter footers = null;
    private Footnotes footnotes = null;
    private Annotations annotations = null;
    private Map<String, List<List<String>>> tableData = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private final XHTMLContentHandler xhtml;
    private final Metadata metadata;
    private DocumentPart inPart = null;
    private boolean ghostText;
    private boolean parseProperty = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private Map<String, List<List<String>>> tableData = new HashMap<>();
    private String activeTableId;
    private int numberOfColumns = 0;
    private List<String> activeRow = new ArrayList<>();
    private String metaDataLocalName;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private DocumentPart inPart = null;
    private boolean ghostText;
    private boolean parseProperty = false;
    private int pageCount = 0;
    private int slPageCount = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private boolean ghostText;
    private boolean parseProperty = false;
    private int pageCount = 0;
    private int slPageCount = 0;
    private HeaderFooter headers = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
        Map<String, String> annotations = new HashMap<>();
        String currentID = null;
        StringBuffer currentText = null;

        private void start(String id) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/PagesContentHandler.java`
#### Snippet
```java
    private int slPageCount = 0;
    private HeaderFooter headers = null;
    private HeaderFooter footers = null;
    private Footnotes footnotes = null;
    private Annotations annotations = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java
    private boolean parseText = false;

    private boolean inMetadata = false;
    private Property metadataKey;
    private String metadataPropertyQName;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java
    private boolean inSheet = false;

    private boolean inText = false;
    private boolean parseText = false;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java
    private int numberOfSheets = 0;
    private int numberOfColumns = -1;
    private int currentColumn = 0;

    private Map<String, String> menuItems = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java

    private boolean inTable = false;
    private int numberOfSheets = 0;
    private int numberOfColumns = -1;
    private int currentColumn = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java
    private final Metadata metadata;

    private boolean inSheet = false;

    private boolean inText = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/tess4j/ImageDeskew.java`
#### Snippet
```java
    public static class HoughLine {
        public int count = 0;
        public int index = 0;
        public double alpha;
        public double d;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/tess4j/ImageDeskew.java`
#### Snippet
```java

    public static class HoughLine {
        public int count = 0;
        public int index = 0;
        public double alpha;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java

                    })));
    private static volatile boolean HAS_WARNED = false;
    //if a user specifies a custom tess path or tessdata path
    //load the available languages at initialization time
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
    //at initialization and then check langs
    //at parse time
    private boolean preloadLangs = false;
    private boolean hasTesseract;
    private boolean hasImageMagick;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
    private int resize = 200;
    // runtime switch to turn off OCR
    private boolean skipOcr = false;
    // Maximum time (seconds) to wait for the ocring process termination
    private int timeoutSeconds = 120;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
    private String pageSeparator = "";
    // whether or not to preserve interword spacing
    private boolean preserveInterwordSpacing = false;
    // factor by which image is to be scaled.
    // TODO: we should make this dynamic depending on the size of the image
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
    private long maxFileSizeToOcr = Integer.MAX_VALUE;
    // Minimum file size to submit file to ocr.
    private long minFileSizeToOcr = 0;
    // The format of the ocr'ed output to be returned, txt or hocr.
    private OUTPUT_TYPE outputType = OUTPUT_TYPE.TXT;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
    private int depth = 4;
    // enable image preprocessing with ImageMagick (optional)
    private boolean enableImagePreprocessing = false;
    // filter to be applied to the processed image.
    private String filter = "triangle";
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java

    // whether or not to apply rotation calculated by the rotation.py script
    private boolean applyRotation = false;
    // colorspace of processed image.
    private String colorspace = "gray";
```

### RedundantFieldInitialization
Field initialization to `0x00` is redundant
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private final static char DL = 0x93;    // double-quote left
    private final static char DR = 0x94;    // double-quote right
    private int FMT_ANPA_1312 = 0x00;   // "NAA 89-3 (ANPA 1312)"
    private int FMT_ANPA_UPI = 0x01;   // "United Press International ANPA 1312 variant"
    private int FMT_ANPA_UPI_DL = 0x02;   // "United Press International Down-Load Message"
```

## RuleId[id=RedundantImplements]
### RedundantImplements
Redundant interface declaration `Serializable`
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypes.java`
#### Snippet
```java
 * detection if it wants to process the stream based on the detected type.
 */
public final class MimeTypes implements Detector, Serializable {

    /**
```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/filelist/FileListPipesIterator.java`
#### Snippet
```java
 *
 */
public class FileListPipesIterator extends PipesIterator implements Initializable {

    @Field
```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/fs/FileSystemPipesIterator.java`
#### Snippet
```java

public class FileSystemPipesIterator extends PipesIterator
        implements TotalCounter, Initializable, Closeable {

    private static final Logger LOG = LoggerFactory.getLogger(AsyncProcessor.class);
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `tika-core/src/main/java/org/apache/tika/detect/CompositeEncodingDetector.java`
#### Snippet
```java
import org.apache.tika.metadata.Metadata;

public class CompositeEncodingDetector implements EncodingDetector, Serializable {

    /**
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `tika-core/src/main/java/org/apache/tika/metadata/writefilter/StandardWriteFilter.java`
#### Snippet
```java
 * <b>NOTE:</b> as with {@link Metadata}, this object is not thread safe.
 */
public class StandardWriteFilter implements MetadataWriteFilter, Serializable {

    public static final Set<String> ALWAYS_SET_FIELDS = new HashSet<>();
```

### RedundantImplements
Redundant interface declaration `TikaServerResource`
in `tika-server/tika-server-eval/src/main/java/org/apache/tika/server/eval/TikaEvalResource.java`
#### Snippet
```java

@Path("/eval")
public class TikaEvalResource implements TikaServerResource, ServerStatusResource {

    public static final String TEXT = "text";
```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-jdbc/src/main/java/org/apache/tika/pipes/pipesiterator/jdbc/JDBCPipesIterator.java`
#### Snippet
```java
 *  </ul>
 */
public class JDBCPipesIterator extends PipesIterator implements Initializable {


```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-jdbc/src/main/java/org/apache/tika/pipes/reporters/jdbc/JDBCPipesReporter.java`
#### Snippet
```java
 * please open a ticket on our JIRA!
 */
public class JDBCPipesReporter extends PipesReporterBase implements Initializable {

    private static final Logger LOG = LoggerFactory.getLogger(JDBCPipesReporter.class);
```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-gcs/src/main/java/org/apache/tika/pipes/pipesiterator/gcs/GCSPipesIterator.java`
#### Snippet
```java
import org.apache.tika.utils.StringUtils;

public class GCSPipesIterator extends PipesIterator implements Initializable {

    private static final Logger LOGGER = LoggerFactory.getLogger(GCSPipesIterator.class);
```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-solr/src/main/java/org/apache/tika/pipes/pipesiterator/solr/SolrPipesIterator.java`
#### Snippet
```java
 * Iterates through results from a Solr query.
 */
public class SolrPipesIterator extends PipesIterator implements Initializable {

    private static final Logger LOGGER = LoggerFactory.getLogger(SolrPipesIterator.class);
```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-s3/src/main/java/org/apache/tika/pipes/pipesiterator/s3/S3PipesIterator.java`
#### Snippet
```java
import org.apache.tika.utils.StringUtils;

public class S3PipesIterator extends PipesIterator implements Initializable {

    private static final Logger LOGGER = LoggerFactory.getLogger(S3PipesIterator.class);
```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-az-blob/src/main/java/org/apache/tika/pipes/pipesiterator/azblob/AZBlobPipesIterator.java`
#### Snippet
```java
import org.apache.tika.utils.StringUtils;

public class AZBlobPipesIterator extends PipesIterator implements Initializable {

    private static final Logger LOGGER = LoggerFactory.getLogger(AZBlobPipesIterator.class);
```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-kafka/src/main/java/org/apache/tika/pipes/pipesiterator/kafka/KafkaPipesIterator.java`
#### Snippet
```java
import org.apache.tika.pipes.pipesiterator.PipesIterator;

public class KafkaPipesIterator extends PipesIterator implements Initializable {

    private static final Logger LOGGER = LoggerFactory.getLogger(KafkaPipesIterator.class);
```

### RedundantImplements
Redundant interface declaration `Runnable`
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
    }

    private class StreamWatcher extends StreamGobbler implements Runnable {
        //plagiarized from org.apache.oodt's StreamGobbler

```

### RedundantImplements
Redundant interface declaration `Initializable`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-csv/src/main/java/org/apache/tika/pipes/pipesiterator/csv/CSVPipesIterator.java`
#### Snippet
```java
 *  </ul>
 */
public class CSVPipesIterator extends PipesIterator implements Initializable {

    private static final Logger LOGGER = LoggerFactory.getLogger(CSVPipesIterator.class);
```

## RuleId[id=ExceptionNameDoesntEndWithException]
### ExceptionNameDoesntEndWithException
Exception class name `OfferLargerThanQueueSize` does not end with 'Exception'
in `tika-core/src/main/java/org/apache/tika/pipes/async/OfferLargerThanQueueSize.java`
#### Snippet
```java
package org.apache.tika.pipes.async;

public class OfferLargerThanQueueSize extends IllegalArgumentException {
    private final int sizeOffered;
    private final int queueSize;
```

### ExceptionNameDoesntEndWithException
Exception class name `UnsurprisingEOF` does not end with 'Exception'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
    }

    private static class UnsurprisingEOF extends EOFException {

    }
```

## RuleId[id=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
                        pkg = OPCPackage.open(tmpRepairedCopy, PackageAccess.READ);
                    } catch (IOException e) {
                        if (e instanceof EOFException) {
                            //keep going
                        } else if (e instanceof IOException && e.getMessage() != null &&
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
                        if (e instanceof EOFException) {
                            //keep going
                        } else if (e instanceof IOException && e.getMessage() != null &&
                                e.getMessage().contains("Truncated")) {
                            //keep going
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `tika-core/src/main/java/org/apache/tika/detect/XmlRootExtractor.java`
#### Snippet
```java
            throw e;
        } catch (Exception e) {
            if (throwMalformed && (e instanceof CharConversionException
                    || e.getCause() instanceof CharConversionException)) {
                throw new MalformedCharException(e);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
                throw e;
            } catch (TikaException e) {
                if (e instanceof EncryptedDocumentException) {
                    metadata.set(TikaCoreProperties.IS_ENCRYPTED, true);
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
                }
                if (context.get(ZeroByteFileException.IgnoreZeroByteFileException.class) != null &&
                        e instanceof ZeroByteFileException) {
                    //do nothing
                } else if (catchEmbeddedExceptions) {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
            getWrappedParser().parse(tis, secureContentHandler, metadata, context);
        } catch (Throwable e) {
            if (e instanceof EncryptedDocumentException) {
                metadata.set(TikaCoreProperties.IS_ENCRYPTED, "true");
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
            parser.parse(is, handler, m, parseContext);
        } catch (Throwable t) {
            if (t instanceof OutOfMemoryError) {
                LOG.error(getXMLifiedLogMsg(OOM, resourceId, t));
            } else if (t instanceof Error) {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
            if (t instanceof OutOfMemoryError) {
                LOG.error(getXMLifiedLogMsg(OOM, resourceId, t));
            } else if (t instanceof Error) {
                LOG.error(getXMLifiedLogMsg(PARSE_ERR, resourceId, t));
            } else {
```

## RuleId[id=StringBufferReplaceableByStringBuilder]
### StringBufferReplaceableByStringBuilder
`StringBuffer c` may be declared as 'StringBuilder'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/JempboxExtractor.java`
#### Snippet
```java
            return creators.get(0);
        }
        StringBuffer c = new StringBuffer();
        for (String s : creators) {
            c.append(", ").append(s);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer s` may be declared as 'StringBuilder'
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParsersConfigReader.java`
#### Snippet
```java

    private static String getString(Element element) {
        StringBuffer s = new StringBuffer();

        NodeList children = element.getChildNodes();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buf` may be declared as 'StringBuilder'
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java

    public String toString() {
        StringBuffer buf = new StringBuffer();
        String[] names = names();
        for (String name : names) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer cmt` may be declared as 'StringBuilder'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/Mp3Parser.java`
#### Snippet
```java

            for (ID3Comment comment : tag.getComments()) {
                StringBuffer cmt = new StringBuffer();
                if (comment.getLanguage() != null) {
                    cmt.append(comment.getLanguage());
```

### StringBufferReplaceableByStringBuilder
`StringBuffer text` may be declared as 'StringBuilder'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaWelcome.java`
#### Snippet
```java
    public String getWelcomePlain() {
        TikaResource.checkIsOperating();
        StringBuffer text = new StringBuffer();

        text.append(tika.toString());
```

### StringBufferReplaceableByStringBuilder
`StringBuffer text` may be declared as 'StringBuilder'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaMimeTypes.java`
#### Snippet
```java
    @Produces("text/plain")
    public String getMimeTypesPlain() {
        StringBuffer text = new StringBuffer();

        for (MediaTypeDetails type : getMediaTypes()) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer newText` may be declared as 'StringBuilder'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/textstats/TextProfileSignature.java`
#### Snippet
```java
        }
        profile.sort(new TokenComparator());
        StringBuffer newText = new StringBuffer();
        int i = 0;
        for (Token t : profile) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer s` may be declared as 'StringBuilder'
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
    public String toString() {

        StringBuffer s = new StringBuffer().append("NGramProfile: ").append(name).append("\n");

        for (NGramEntry entry : getSorted()) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer measurementString` may be declared as 'StringBuilder'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/grobid/GrobidNERecogniser.java`
#### Snippet
```java
                for (Object measurement : measurements) {

                    StringBuffer measurementString = new StringBuffer();
                    StringBuffer normalizedMeasurementString = new StringBuffer();

```

### StringBufferReplaceableByStringBuilder
`StringBuffer normalizedMeasurementString` may be declared as 'StringBuilder'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/grobid/GrobidNERecogniser.java`
#### Snippet
```java

                    StringBuffer measurementString = new StringBuffer();
                    StringBuffer normalizedMeasurementString = new StringBuffer();

                    JSONObject quantity = (JSONObject) convertToJSONObject(measurement.toString())
```

### StringBufferReplaceableByStringBuilder
`StringBuffer output` may be declared as 'StringBuilder'
in `tika-example/src/main/java/org/apache/tika/example/RecentFiles.java`
#### Snippet
```java

    public String generateRSS(Path indexFile) throws CorruptIndexException, IOException {
        StringBuffer output = new StringBuffer();
        output.append(getRSSHeaders());
        IndexSearcher searcher = null;
```

### StringBufferReplaceableByStringBuilder
`StringBuffer nameB` may be declared as 'StringBuilder'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/PSDParser.java`
#### Snippet
```java
            id = EndianUtils.readUShortBE(stream);

            StringBuffer nameB = new StringBuffer();
            int nameLen = 0;
            while (true) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer sbuff` may be declared as 'StringBuilder'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java

    public static String asAlphaNumeric(int i) {
        StringBuffer sbuff = new StringBuffer();
        int index = i % MAX;
        int ratio = i / MAX;
```

## RuleId[id=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractPOIFSExtractor.java`
#### Snippet
```java
            return;
        }
        try (TikaInputStream tis = TikaInputStream.get(new byte[0])) {
            tis.setOpenContainer(dir);
            if (dir.getStorageClsid() != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
        //convert to array
        for (Map.Entry<String, List<String>> e : headers.entrySet()) {
            ret.put(e.getKey(), e.getValue().toArray(new String[0]));
        }
        return ret;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteParser.java`
#### Snippet
```java
                if (!oneNoteTreeWalker.getAuthors().isEmpty()) {
                    metadata.set(TikaCoreProperties.CREATOR,
                            oneNoteTreeWalker.getAuthors().toArray(new String[]{}));
                }
                if (!oneNoteTreeWalker.getMostRecentAuthors().isEmpty()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteParser.java`
#### Snippet
```java
                if (!oneNoteTreeWalker.getMostRecentAuthors().isEmpty()) {
                    metadata.set(Property.externalTextBag(ONE_NOTE_PREFIX + "mostRecentAuthors"),
                            oneNoteTreeWalker.getMostRecentAuthors().toArray(new String[]{}));
                }
                if (!oneNoteTreeWalker.getOriginalAuthors().isEmpty()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteParser.java`
#### Snippet
```java
                if (!oneNoteTreeWalker.getOriginalAuthors().isEmpty()) {
                    metadata.set(Property.externalTextBag(ONE_NOTE_PREFIX + "originalAuthors"),
                            oneNoteTreeWalker.getOriginalAuthors().toArray(new String[]{}));
                }
                if (!Instant.MAX.equals(
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/MSOneStorePackage.java`
#### Snippet
```java
        }
        if (!authors.isEmpty()) {
            metadata.set(TikaCoreProperties.CREATOR, authors.toArray(new String[]{}));
        }
        if (!mostRecentAuthors.isEmpty()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/MSOneStorePackage.java`
#### Snippet
```java
        if (!mostRecentAuthors.isEmpty()) {
            metadata.set(Property.externalTextBag(ONE_NOTE_PREFIX + "mostRecentAuthors"),
                    mostRecentAuthors.toArray(new String[]{}));
        }
        if (!originalAuthors.isEmpty()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/MSOneStorePackage.java`
#### Snippet
```java
        if (!originalAuthors.isEmpty()) {
            metadata.set(Property.externalTextBag(ONE_NOTE_PREFIX + "originalAuthors"),
                    originalAuthors.toArray(new String[]{}));
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/sax/XHTMLContentHandler.java`
#### Snippet
```java
            } else {
                // TIKA-725: Prefer <title></title> over <title/>
                super.characters(new char[0], 0, 0);
            }
            super.endElement(XHTML, "title", "title");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/sax/ExpandedTitleContentHandler.java`
#### Snippet
```java
            // Hack to close the title tag
            try {
                super.characters(new char[0], 0, 1);
            } catch (ArrayIndexOutOfBoundsException e) {
                // Expected, just wanted to close the title tag
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
    public static final byte INIT_LOADER_PARSER = 7;
    public static final byte INIT_PARSER_FACTORY_FACTORY_LOADER = 8;
    private final Object[] lock = new Object[0];
    /**
     * Input stream for reading from the parent process
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
                new ForkServer(System.in, System.out, serverPulseMillis, serverParseTimeoutMillis,
                        serverWaitTimeoutMillis);
        System.setIn(new ByteArrayInputStream(new byte[0]));
        System.setOut(System.err);

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesFactory.java`
#### Snippet
```java
        }

        return create(urls.toArray(new URL[0]));
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesReader.java`
#### Snippet
```java
    @Override
    public InputSource resolveEntity(String publicId, String systemId) {
        return new InputSource(new UnsynchronizedByteArrayInputStream(new byte[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/pipes/PipesClient.java`
#### Snippet
```java
    //there are a number of assumptions throughout that PipesClient is run
    //single threaded
    private final Object[] executorServiceLock = new Object[0];
    private final PipesConfigBase pipesConfig;
    private final int pipesClientId;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/pipes/PipesClient.java`
#### Snippet
```java
        commandLine.add(Long.toString(pipesConfig.getShutdownClientAfterMillis()));
        LOG.debug("pipesClientId={}: commandline: {}", pipesClientId, commandLine);
        return commandLine.toArray(new String[0]);
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
    }

    private final Object[] lock = new Object[0];
    private long checkForTimeoutMs = 1000;
    private final Path tikaConfigPath;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
                    new PipesServer(tikaConfig, System.in, System.out, maxForEmitBatchBytes,
                            serverParseTimeoutMillis, serverWaitTimeoutMillis);
            System.setIn(new UnsynchronizedByteArrayInputStream(new byte[0]));
            System.setOut(System.err);
            Thread watchdog = new Thread(server, "Tika Watchdog");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/parser/ParseRecord.java`
#### Snippet
```java

    public String[] getParsers() {
        return parsers.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParsersFactory.java`
#### Snippet
```java
        Enumeration<URL> files = loader.findServiceResources(filepath);
        ArrayList<URL> list = Collections.list(files);
        URL[] urls = list.toArray(new URL[0]);
        return create(urls);
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
        Process process;
        if (cmd.toArray().length == 1) {
            process = Runtime.getRuntime().exec(cmd.toArray(new String[]{})[0]);
        } else {
            process = Runtime.getRuntime().exec(cmd.toArray(new String[]{}));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
            process = Runtime.getRuntime().exec(cmd.toArray(new String[]{})[0]);
        } else {
            process = Runtime.getRuntime().exec(cmd.toArray(new String[]{}));
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/metadata/Property.java`
#### Snippet
```java
        String[] choices = null;
        if (primaryProperty.getChoices() != null) {
            choices = primaryProperty.getChoices().toArray(new String[0]);
        }
        return new Property(primaryProperty.getName(), primaryProperty.isInternal(),
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/renderer/RenderResult.java`
#### Snippet
```java
            return TikaInputStream.get((Path)result, metadata);
        } else {
            TikaInputStream tis = TikaInputStream.get(new byte[0]);
            tis.setOpenContainer(result);
            return tis;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
        String[] values = metadata.get(name);
        if (values == null) {
            values = new String[0];
        }
        return values;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
     */
    public String[] names() {
        return metadata.keySet().toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-digest-commons/src/main/java/org/apache/tika/parser/digestutils/CommonsDigester.java`
#### Snippet
```java
            ret.add(getDigestAlgorithm(algoString));
        }
        return ret.toArray(new DigestAlgorithm[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
            solrClient = new LBHttpSolrClient.Builder().withConnectionTimeout(connectionTimeout)
                    .withSocketTimeout(socketTimeout).withHttpClient(httpClientFactory.build())
                    .withBaseSolrUrls(solrUrls.toArray(new String[]{})).build();
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/Mp3Parser.java`
#### Snippet
```java
        ret.audio = firstAudio;
        ret.lyrics = lyrics;
        ret.tags = tags.toArray(new ID3Tags[0]);
        ret.duration = duration;
        return ret;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFFileHeader.java`
#### Snippet
```java
        }

        header.cols = headers.toArray(new DBFColumnHeader[0]);

        int endOfHeader = is.read();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentMetaParser.java`
#### Snippet
```java

        if (subjects.size() > 0) {
            metadata.set(TikaCoreProperties.SUBJECT, subjects.toArray(new String[0]));
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-solr/src/main/java/org/apache/tika/pipes/pipesiterator/solr/SolrPipesIterator.java`
#### Snippet
```java
        return new LBHttpSolrClient.Builder().withConnectionTimeout(connectionTimeout)
                .withSocketTimeout(socketTimeout).withHttpClient(httpClientFactory.build())
                .withBaseSolrUrls(solrUrls.toArray(new String[]{})).build();
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-solr/src/main/java/org/apache/tika/pipes/pipesiterator/solr/SolrPipesIterator.java`
#### Snippet
```java
            allFields.addAll(additionalFields);

            query.setFields(allFields.toArray(new String[]{}));
            query.setSort(SolrQuery.SortClause.asc(parsingIdField));
            query.addSort(SolrQuery.SortClause.asc("id"));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-solr/src/main/java/org/apache/tika/pipes/pipesiterator/solr/SolrPipesIterator.java`
#### Snippet
```java
            query.setSort(SolrQuery.SortClause.asc(parsingIdField));
            query.addSort(SolrQuery.SortClause.asc("id"));
            query.setFilterQueries(filters.toArray(new String[]{}));

            HandlerConfig handlerConfig = getHandlerConfig();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
            }
        }
        return fullCommand.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
    private Parser[] sortParsers(Map<Parser, Set<MediaType>> parsers) {
        // Get a nicely sorted list of the parsers
        Parser[] sortedParsers = parsers.keySet().toArray(new Parser[0]);
        Arrays.sort(sortedParsers, (p1, p2) -> {
            String name1 = p1.getClass().getName();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                TikaGUI.main(new String[]{configFilePath});
            } else {
                TikaGUI.main(new String[0]);
            }
        } else if (arg.equals("--list-parser") || arg.equals("--list-parsers")) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MosesTranslator.java`
#### Snippet
```java
        }

        Runtime.getRuntime().exec(buildCommand(smtPath, scriptPath), new String[]{},
                buildWorkingDirectory(scriptPath));

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerCli.java`
#### Snippet
```java
            }
        }
        return ret.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerCli.java`
#### Snippet
```java
                .getForkedProcessArgs(tikaServerConfig.getPort(), tikaServerConfig.getIdBase());
        args.add("--noFork");
        TikaServerProcess.main(args.toArray(new String[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
            //redirect
            InputStream in = System.in;
            System.setIn(new ByteArrayInputStream(new byte[0]));

            String forkedStatusFile = tikaServerConfig.getForkedStatusFile();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
    private final String id;
    private final TikaServerConfig tikaServerConfig;
    private final Object[] forkedStatusLock = new Object[0];
    private volatile FORKED_STATUS forkedStatus = FORKED_STATUS.INITIALIZING;
    private volatile Instant lastPing = null;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaWelcome.java`
#### Snippet
```java
                    }
                    if (produces == null) {
                        produces = new String[0];
                    }
                    found.add(new Endpoint(endpoint, m, mPath, httpMethod, produces));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaMimeTypes.java`
#### Snippet
```java
            MediaTypeDetails details = new MediaTypeDetails();
            details.type = type;
            details.aliases = registry.getAliases(type).toArray(new MediaType[0]);

            try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/renderer/pdf/mutool/MuPDFRenderer.java`
#### Snippet
```java
            args.add(sb.toString());
        }
        return args.toArray(new String[0]);
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/image/ImageGraphicsEngine.java`
#### Snippet
```java
            parseContext.set(ZeroByteFileException.IgnoreZeroByteFileException.class,
                    ZeroByteFileException.IGNORE_ZERO_BYTE_FILE_EXCEPTION);
            embeddedDocumentExtractor.parseEmbedded(TikaInputStream.get(new byte[0]),
                    new EmbeddedContentHandler(xhtml), metadata, false);
        } finally {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenCounter.java`
#### Snippet
```java

    private final TokenStatistics NULL_TOKEN_STAT =
            new TokenStatistics(0, 0, new TokenIntPair[0], 0.0d, new SummaryStatistics());
    private final Analyzer generalAnalyzer;
    Map<String, Map<String, MutableInt>> map = new HashMap<>(); //Map<field, Map<token, count>>
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
            if (thisRenderer instanceof PDDocumentRenderer) {
                //do not do autocloseable.  We need to leave the pdDocument open!
                TikaInputStream tis = TikaInputStream.get(new byte[0]);
                tis.setOpenContainer(pdDocument);
                return thisRenderer.render(tis, pageMetadata, parseContext, pageRangeRequest)
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
        List<NGramEntry> list = new ArrayList<>();
        List<NGramEntry> sublist = new ArrayList<>();
        NGramEntry[] entries = ngrams.values().toArray(new NGramEntry[0]);
        for (int i = minLength; i <= maxLength; i++) {
            for (NGramEntry entry : entries) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
        List<String> argList = new ArrayList<>();
        argList.add("-web");
        Console.main(argList.toArray(new String[0]));
        while (true) {
            try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
            }

            String[] updatedArgs = argList.toArray(new String[0]);
            DefaultParser defaultCLIParser = new DefaultParser();
            try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java

            }
            String[] updatedArgs = argList.toArray(new String[0]);
            DefaultParser defaultCLIParser = new DefaultParser();
            try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
            }

            String[] updatedArgs = argList.toArray(new String[0]);
            DefaultParser defaultCLIParser = new DefaultParser();
            try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURISchemeUtil.java`
#### Snippet
```java

        if (dataString == null || dataString.length() == 0) {
            data = new byte[0];
        } else if (isBase64 != null) {
            data = base64.decode(dataString);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
            }
        }
        return args.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-batch/src/main/java/org/apache/tika/batch/fs/strawman/StrawManTikaAppDriver.java`
#### Snippet
```java
            StrawManTikaAppDriver driver =
                    new StrawManTikaAppDriver(inputDir, outputDir, totalThreads, fileList,
                            commandLine.toArray(new String[0]));
            completionService.submit(driver);
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(S3Fetcher.class);
    private static final String PREFIX = "s3";
    private final Object[] clientLock = new Object[0];
    private String region;
    private String bucket;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java

    public static void testTeeContentHandler(String filename) throws Exception {
        InputStream stream = new ByteArrayInputStream(new byte[0]);
        Metadata metadata = new Metadata();
        ParseContext context = new ParseContext();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java

    public static void testCompositeDocument() throws Exception {
        InputStream stream = new ByteArrayInputStream(new byte[0]);
        ContentHandler handler = new DefaultHandler();
        Metadata metadata = new Metadata();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java

    public static void testLocale() throws Exception {
        InputStream stream = new ByteArrayInputStream(new byte[0]);
        ContentHandler handler = new DefaultHandler();
        Metadata metadata = new Metadata();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java

    public static void useAutoDetectParser() throws Exception {
        InputStream stream = new ByteArrayInputStream(new byte[0]);
        ContentHandler handler = new DefaultHandler();
        Metadata metadata = new Metadata();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java

    public static void useHtmlParser() throws Exception {
        InputStream stream = new ByteArrayInputStream(new byte[0]);
        ContentHandler handler = new DefaultHandler();
        Metadata metadata = new Metadata();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java

    public static void useCompositeParser() throws Exception {
        InputStream stream = new ByteArrayInputStream(new byte[0]);
        ContentHandler handler = new DefaultHandler();
        ParseContext context = new ParseContext();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java

    public static void testHtmlMapper() throws Exception {
        InputStream stream = new ByteArrayInputStream(new byte[0]);
        ContentHandler handler = new DefaultHandler();
        Metadata metadata = new Metadata();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-langdetect/tika-langdetect-test-commons/src/main/java/org/apache/tika/langdetect/LanguageDetectorTest.java`
#### Snippet
```java
                }
            }
            return result.toArray(new String[0]);
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/general/SpanSwapper.java`
#### Snippet
```java
    private byte[] swap(byte[] ret) {
        if (ret.length == 0) {
            return new byte[0];
        }
        int srcStart = random.nextInt(ret.length);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/cli/FuzzOne.java`
#### Snippet
```java
        try {
            Path err = target.getParent().resolve(target.getFileName().toString() + ext);
            Files.write(err, new byte[0]);
        } catch (IOException e) {
            LOG.warn("things aren't going right today.", e);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java

        if (bytesRead < 1) {
            return setText(new byte[0]);
        } else if (kBufSize > bytesRead) {
            return setText(inputBytes, (int) bytesRead);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java
        Collections.sort(matches);      // CharsetMatch compares on confidence
        Collections.reverse(matches);   //  Put best match first.
        return matches.toArray(new CharsetMatch[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java
            }
        }
        return csnames.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
            list.add(count);
        }
        return list.toArray(new Long[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/StringsParser.java`
#### Snippet
```java
        cmdList.add(input.getPath());

        String[] cmd = cmdList.toArray(new String[0]);

        ProcessBuilder pb = new ProcessBuilder(cmd);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/PSDParser.java`
#### Snippet
```java
                IOUtils.readFully(stream, data);
            } else {
                data = new byte[0];
                IOUtils.skipFully(stream, dataLen);
            }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private byte[] getSection(InputStream is, int maxsize, byte bstart, byte bfinish,
                              boolean ifincomplete) {
        byte[] value = new byte[0];

        try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
    private byte[] getSection(InputStream is, String name) {

        byte[] value = new byte[0];

        switch (name) {
```

## RuleId[id=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `index` of exception class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObjectParseErrorException.java`
#### Snippet
```java
public class StreamObjectParseErrorException extends RuntimeException {

    public int index;

    public String streamObjectTypeName;
```

### NonFinalFieldOfException
Non-final field `streamObjectTypeName` of exception class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObjectParseErrorException.java`
#### Snippet
```java
    public int index;

    public String streamObjectTypeName;

    /**
```

### NonFinalFieldOfException
Non-final field `IGNORE_ZERO_BYTE_FILE_EXCEPTION` of exception class
in `tika-core/src/main/java/org/apache/tika/exception/ZeroByteFileException.java`
#### Snippet
```java
     * ignore embedded files with zero-byte length inputstreams
     */
    public static IgnoreZeroByteFileException IGNORE_ZERO_BYTE_FILE_EXCEPTION =
            new IgnoreZeroByteFileException();

```

## RuleId[id=NonExceptionNameEndsWithException]
### NonExceptionNameEndsWithException
Non-exception class name `IgnoreZeroByteFileException` ends with 'Exception'
in `tika-core/src/main/java/org/apache/tika/exception/ZeroByteFileException.java`
#### Snippet
```java
    }

    public static class IgnoreZeroByteFileException {
    }
}
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/MSOneStoreParser.java`
#### Snippet
```java
                    (ObjectGroupDataElementData) objectGroupDataElements.stream()
                            .filter(d -> d.dataElementExGuid.equals(objRef.objectGroupExtendedGUID))
                            .findFirst().get().data;

            RevisionStoreObjectGroup objectGroup =
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFParser.java`
#### Snippet
```java
            metadata.set(Metadata.CONTENT_ENCODING, charset.name());
            Optional<MediaType> firstElement = SUPPORTED_TYPES.stream().findFirst();
            metadata.set(Metadata.CONTENT_TYPE, firstElement.get().toString());

            XHTMLContentHandler xhtml = new XHTMLContentHandler(handler, metadata);
```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ProduceTypeResourceComparator.java`
#### Snippet
```java
        int oper1Priority = op1Matched.stream()
                .mapToInt(PRIORITIZED_MEDIA_LIST::indexOf)
                .max().getAsInt();
        int oper2Priority = op2Matched.stream()
                .mapToInt(PRIORITIZED_MEDIA_LIST::indexOf)
```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ProduceTypeResourceComparator.java`
#### Snippet
```java
        int oper2Priority = op2Matched.stream()
                .mapToInt(PRIORITIZED_MEDIA_LIST::indexOf)
                .max().getAsInt();

        // final calculation
```

## RuleId[id=ConstantValue]
### ConstantValue
Value `type` is always 'null'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
        //to needless logging. Do a preliminary test for OPC now.
        if (zipEntrySource.getEntry("[Content_Types].xml") == null) {
            return type;
        }
        // Use POI to open and investigate it for us
```

### ConstantValue
Condition `poifs != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
                    handleEmbeddedOfficeDoc(poifs.getRoot(), objID, xhtml, false);
                } finally {
                    if (poifs != null) {
                        poifs.close();
                    }
```

### ConstantValue
Condition `abs > 1E15` is always `false` when reached
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/TikaExcelGeneralFormat.java`
#### Snippet
```java
        if (abs > 1E15 || (abs <= 1E-15 && abs > 0)) {
            return scientificFormat.format(number, toAppendTo, pos);
        } else if (Math.floor(value) == value || abs > 1E15) {
            // integer, or integer portion uses all 15 allowed digits
            return integerFormat.format(number, toAppendTo, pos);
```

### ConstantValue
Condition `start >= 0` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
                int end = findHyperlinkEnd(text.toString(), start);
                String url = "";
                if (start >= 0 && start < end && end <= text.length()) {
                    url = text.substring(start, end);
                }
```

### ConstantValue
Condition `tis != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/JackcessExtractor.java`
#### Snippet
```java
                    break;
                }
                if (tis != null) {
                    try {
                        handleEmbeddedResource(tis, spc.getFileName(),//filename
```

### ConstantValue
Condition `extractAllAlternatives` is always `false` when reached
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
            }
        }
        if (rtfChunk != null && (extractAllAlternatives || !doneBody)) {
            ByteChunk chunk = (ByteChunk) rtfChunk;
            //avoid buffer underflow TIKA-2530
```

### ConstantValue
Condition `extractAllAlternatives` is always `false` when reached
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
            }
        }
        if (textChunk != null && (extractAllAlternatives || !doneBody)) {
            xhtml.element("p", ((StringChunk) textChunk).getValue());
        }
```

### ConstantValue
Condition `headers != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OutlookExtractor.java`
#### Snippet
```java
                metadata.set(TikaCoreProperties.MODIFIED, msg.getMessageDate().getTime());
            } else {
                if (headers != null && headers.size() > 0) {
                    for (Map.Entry<String, String[]> header : headers.entrySet()) {
                        String headerKey = header.getKey();
```

### ConstantValue
Condition `goodParameter == 2` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcResetTable.java`
#### Snippet
```java
        ChmAssert.assertChmAccessorNotNull(chmLzxcResetTable);
        ++goodParameter;
        return (goodParameter == 2);
    }

```

### ConstantValue
Condition `(temp = dir_chunk[placeHolder++]) >= 0x80` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java
                int strlen = 0;// = getEncint(data);
                byte temp;
                while ((temp = dir_chunk[placeHolder++]) >= 0x80) {
                    strlen <<= 7;
                    strlen += temp & 0x7f;
```

### ConstantValue
Result of `strlen << 7` is always '0'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmDirectoryListingSet.java`
#### Snippet
```java
                }

                strlen = (strlen << 7) + temp & 0x7f;

                if (strlen > dir_chunk.length) {
```

### ConstantValue
Condition `t != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmCommons.java`
#### Snippet
```java
                (int) (lzxcBlockOffset + resetTable.getBlockAddress()[blockNumber]),
                (int) (lzxcBlockOffset + resetTable.getBlockAddress()[blockNumber] + blockLength));
        return (t != null) ? t : new byte[1];
    }

```

### ConstantValue
Condition `linkString != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/HyperlinkHandler.java`
#### Snippet
```java
            String linkString = linkCache.toString();
            //can't be null I don't think
            if (linkString != null) {
                char[] chars = linkString.trim().toCharArray();
                handler.characters(chars, 0, chars.length);
```

### ConstantValue
Condition `sum > Integer.MAX_VALUE` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFEmbObjHandler.java`
#### Snippet
```java
            } else {
                long sum = hi + TextExtractor.hexValue(b);
                if (sum > Integer.MAX_VALUE || sum < 0) {
                    throw new IOException("hex char to byte overflow");
                }
```

### ConstantValue
Value `ret` is always 'null'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java

            if (root == null) {
                return ret;
            }

```

### ConstantValue
Condition `displayName == null` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
            pathToUse = p;
        } else {
            fileNameToUse = displayName == null ? "" : displayName;
            pathToUse = ansiFilePath == null ? "" : ansiFilePath;
        }
```

### ConstantValue
Condition `ansiFilePath == null` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
        } else {
            fileNameToUse = displayName == null ? "" : displayName;
            pathToUse = ansiFilePath == null ? "" : ansiFilePath;
        }
        metadata.set(TikaCoreProperties.ORIGINAL_RESOURCE_NAME, fileNameToUse);
```

### ConstantValue
Condition `className != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
        String itemName = readLengthPrefixedAnsiString(is).trim();

        if (className != null && className.length() > 0) {
            metadata.add(RTFMetadata.EMB_CLASS, className);
        }
```

### ConstantValue
Condition `topicName != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
            metadata.add(RTFMetadata.EMB_CLASS, className);
        }
        if (topicName != null && topicName.length() > 0) {
            metadata.add(RTFMetadata.EMB_TOPIC, topicName);
        }
```

### ConstantValue
Condition `itemName != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
            metadata.add(RTFMetadata.EMB_TOPIC, topicName);
        }
        if (itemName != null && itemName.length() > 0) {
            metadata.add(RTFMetadata.EMB_ITEM, itemName);
        }
```

### ConstantValue
Condition `b == null` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/SpreadsheetMLParser.java`
#### Snippet
```java
                        }
                        String b = buffer.toString();
                        if (b == null) {
                            b = "";
                        }
```

### ConstantValue
Condition `pps != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/SXWPFWordExtractorDecorator.java`
#### Snippet
```java
        //handle main document
        List<PackagePart> pps = getStoryDocumentParts();
        if (pps != null) {
            for (PackagePart pp : pps) {
                //likely only one, but why not...
```

### ConstantValue
Condition `prelentable == null` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        short[] prelentable = createPreLenTable();

        if (prelentable == null) {
            throw new ChmParsingException("pretreetable is null");
        }
```

### ConstantValue
Condition `pretreetable == null` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                ChmConstants.LZX_PRETREE_TABLEBITS, ChmConstants.LZX_PRETREE_MAXSYMBOLS);

        if (pretreetable == null) {
            throw new ChmParsingException("pretreetable is null");
        }
```

### ConstantValue
Condition `goodParameter == 3` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
            throw new ChmParsingException("block length should be more than zero");
        }
        return (goodParameter == 3);
    }

```

### ConstantValue
Condition `content[i] != 0xe8` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
            int i = 0;
            while (i < getBlockLength() - 10) {
                if (content[i] != 0xe8) {
                    i++;
                    continue;
```

### ConstantValue
Condition `(runsrc < 0) && (matchlen-- > 0)` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                } else {
                    /* copies any wrappes around source data */
                    while ((runsrc < 0) && (matchlen-- > 0)) {
                        content[rundest++] = content[(int) (runsrc + getBlockLength())];
                        runsrc++;
```

### ConstantValue
Condition `runsrc < 0` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                } else {
                    /* copies any wrappes around source data */
                    while ((runsrc < 0) && (matchlen-- > 0)) {
                        content[rundest++] = content[(int) (runsrc + getBlockLength())];
                        runsrc++;
```

### ConstantValue
Condition `z == 19` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    getState().mainTreeLengtsTable[i++] = 0;
                }
            } else if (z == 19) {
                y = getChmSection().getSyncBits(1);
                y += 4;
```

### ConstantValue
Condition `prevcontent != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    if (matchlen + runsrc <= 0) {
                        runsrc = prevcontent.length + runsrc;
                        while ((matchlen-- > 0) && (prevcontent != null) && ((runsrc + 1) > 0))
                            if ((rundest < content.length) && (runsrc < content.length)) {
                                content[rundest++] = prevcontent[runsrc++];
```

### ConstantValue
Condition `(runsrc < 0) && (matchlen-- > 0)` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                } else {
                    /* copies any wrapped source data */
                    while ((runsrc < 0) && (matchlen-- > 0)) {
                        content[rundest++] = content[(int) (runsrc + getBlockLength())];
                        runsrc++;
```

### ConstantValue
Condition `runsrc < 0` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                } else {
                    /* copies any wrapped source data */
                    while ((runsrc < 0) && (matchlen-- > 0)) {
                        content[rundest++] = content[(int) (runsrc + getBlockLength())];
                        runsrc++;
```

### ConstantValue
Condition `prelentable == null` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                                          short[] prelentable) throws TikaException {
        if (prelentable == null || getChmSection() == null || pretreetable == null ||
                prelentable == null) {
            throw new ChmParsingException("is null");
        }
```

### ConstantValue
Condition `z == 19` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
                    //no tolerate //if (i < getState().getLengthTreeLengtsTable().length)
                    getState().getLengthTreeLengtsTable()[i++] = 0;
            } else if (z == 19) {
                y = getChmSection().getSyncBits(1);
                y += 4;
```

### ConstantValue
Condition `e instanceof IOException` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
                        if (e instanceof EOFException) {
                            //keep going
                        } else if (e instanceof IOException && e.getMessage() != null &&
                                e.getMessage().contains("Truncated")) {
                            //keep going
```

### ConstantValue
Condition `fs != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
            EmbeddedDocumentUtil.recordEmbeddedStreamException(e, parentMetadata);
        } finally {
            if (fs != null) {
                fs.close();
            }
```

### ConstantValue
Condition `isRawText` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/PropertyValue.java`
#### Snippet
```java
        long type = propertyId.type;

        if (isRawText) {
            LOG.debug("{}<{}", IndentUtil.getIndent(indentLevel + 1), propertyId);
        }
```

### ConstantValue
Condition `isRawText` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/PropertyValue.java`
#### Snippet
```java
        }
        if (type > 0 && type <= 6) {
            if (isRawText) {
                LOG.debug("(%d)", scalar);
            }
```

### ConstantValue
Condition `isRawText` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/PropertyValue.java`
#### Snippet
```java
            OneNotePtr content = new OneNotePtr(pointer);
            content.reposition(rawData);
            if (isRawText) {
                LOG.debug(" [");
                content.dumpHex();
```

### ConstantValue
Condition `isRawText` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/PropertyValue.java`
#### Snippet
```java
                xtype = "OSIDS";
            }
            if (isRawText) {
                if (!compactIDs.isEmpty()) {
                    LOG.debug("");
```

### ConstantValue
Condition `isRawText` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/PropertyValue.java`
#### Snippet
```java
            }
        } else if (type == 0x10 || type == 0x11) {
            if (isRawText) {
                LOG.debug("SubProperty");
            }
```

### ConstantValue
Condition `isRawText` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/PropertyValue.java`
#### Snippet
```java
            propertySet.print(document, pointer, indentLevel + 1);
        }
        if (isRawText) {
            LOG.debug(">");
        }
```

### ConstantValue
Condition `data.id == FndStructureConstants.ObjectGroupStartFND` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java

            // the ref populates the FileNodeList children
        } else if (data.id == FndStructureConstants.ObjectGroupStartFND) {
            data.idDesc = "object_group_id";
            data.gosid = deserializeExtendedGUID(); // the oid
```

### ConstantValue
Condition `data.id == FndStructureConstants.ObjectGroupEndFND` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNotePtr.java`
#### Snippet
```java
            data.gosid = deserializeExtendedGUID(); // the oid

        } else if (data.id == FndStructureConstants.ObjectGroupEndFND) {
            // nothing to see here
        } else if (data.id == FndStructureConstants.DataSignatureGroupDefinitionFND) {
```

### ConstantValue
Condition `intermediateNode.dataNodeObjectData == null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupDataElementData.java`
#### Snippet
```java
                }

                if (intermediateNode.dataNodeObjectData == null &&
                        intermediateNode.intermediateNodeObjectList != null) {
                    dataElements.add(
```

### ConstantValue
Condition `i > 0` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/RDCAnalysisChunking.java`
#### Snippet
```java
        int i = 32;

        while (i > 0 && hashWindowSize % i != 0) {
            shiftAmount *= 2;
            i /= 2;
```

### ConstantValue
Value `ms` is always 'null'
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadataList.java`
#### Snippet
```java
        List<Metadata> ms = null;
        if (reader == null) {
            return ms;
        }
        ms = new ArrayList<>();
```

### ConstantValue
Condition `ms == null` is always `false`
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadataList.java`
#### Snippet
```java

        }
        if (ms == null) {
            return null;
        }
```

### ConstantValue
Condition `vals.length > 1` is always `true`
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadata.java`
#### Snippet
```java
            } else if (vals.length == 1) {
                jsonGenerator.writeStringField(n, vals[0]);
            } else if (vals.length > 1) {
                jsonGenerator.writeArrayFieldStart(n);
                for (String val : vals) {
```

### ConstantValue
Value `language` is always 'null'
in `tika-langdetect/tika-langdetect-lingo24/src/main/java/org/apache/tika/langdetect/lingo24/Lingo24LangDetector.java`
#### Snippet
```java

        if (!isAvailable) {
            return language;
        }

```

### ConstantValue
Value `language` is always 'null'
in `tika-langdetect/tika-langdetect-lingo24/src/main/java/org/apache/tika/langdetect/lingo24/Lingo24LangDetector.java`
#### Snippet
```java
            result.add(new LanguageResult(language, LanguageConfidence.MEDIUM, 1));
        } else {
            result.add(new LanguageResult(language, LanguageConfidence.NONE, 0));
        }
        return result;
```

### ConstantValue
Value `status` is always 'null'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesClient.java`
#### Snippet
```java
            status = lookup(statusByte);
        } catch (IllegalArgumentException e) {
            throw new IOException("problem reading response from server " + status);
        }

```

### ConstantValue
Value `arg` is always 'null'
in `tika-core/src/main/java/org/apache/tika/utils/ProcessUtils.java`
#### Snippet
```java
    public static String escapeCommandLine(String arg) {
        if (arg == null) {
            return arg;
        }
        //need to test for " " on windows, can't just add double quotes
```

### ConstantValue
Condition `!hasSecurityManager` is always `true`
in `tika-core/src/main/java/org/apache/tika/utils/XMLReaderUtils.java`
#### Snippet
```java
        } else if (canReset && hasSecurityManager) {
            return new Xerces2PoolSAXParser(generation, parser);
        } else if (canReset && !hasSecurityManager && canSetJaxPEntity) {
            return new BuiltInPoolSAXParser(generation, parser);
        } else {
```

### ConstantValue
Value `hasSecurityManager` is always 'false'
in `tika-core/src/main/java/org/apache/tika/utils/XMLReaderUtils.java`
#### Snippet
```java
        } else if (canReset && hasSecurityManager) {
            return new Xerces2PoolSAXParser(generation, parser);
        } else if (canReset && !hasSecurityManager && canSetJaxPEntity) {
            return new BuiltInPoolSAXParser(generation, parser);
        } else {
```

### ConstantValue
Condition `recursiveParserWrapperHandler instanceof AbstractRecursiveParserWrapperHandler` is always `true`
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java
        boolean throwOnWriteLimitReached = true;

        if (recursiveParserWrapperHandler instanceof AbstractRecursiveParserWrapperHandler) {
            ContentHandlerFactory factory =
                    ((AbstractRecursiveParserWrapperHandler)recursiveParserWrapperHandler).getContentHandlerFactory();
```

### ConstantValue
Condition `itemClass == null` is always `true` when reached
in `tika-core/src/main/java/org/apache/tika/config/ConfigBase.java`
#### Snippet
```java
            }
        }
        if (setterMethod == null && itemClass == null) {
            throw new TikaConfigException(
                    "Couldn't find setter '" + setter + "' or adder '" + adder + "' for " + itemName +
```

### ConstantValue
Condition `!serializeMetadataCommandArgumentsToken` is always `true` when reached
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
                }
            } else if (!replacedMetadataCommandArgumentsToken &&
                    !serializeMetadataCommandArgumentsToken) {
                // Tack metadata onto the end of the cmd as arguments
                cmd.addAll(commandMetadataSegments);
```

### ConstantValue
Value `serializeMetadataCommandArgumentsToken` is always 'false'
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
                }
            } else if (!replacedMetadataCommandArgumentsToken &&
                    !serializeMetadataCommandArgumentsToken) {
                // Tack metadata onto the end of the cmd as arguments
                cmd.addAll(commandMetadataSegments);
```

### ConstantValue
Value `language` is always 'null'
in `tika-langdetect/tika-langdetect-mitll-text/src/main/java/org/apache/tika/langdetect/mitll/TextLangDetector.java`
#### Snippet
```java
            result.add(new LanguageResult(language, LanguageConfidence.MEDIUM, 1));
        } else {
            result.add(new LanguageResult(language, LanguageConfidence.NONE, 0));
        }
        return result;
```

### ConstantValue
Condition `detector == null` is always `true`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

            // Try the possible default and composite detector constructors
            if (detector == null) {
                try {
                    c = detectorClass
```

### ConstantValue
Condition `encodingDetector == null` is always `true`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

            // Try the possible default and composite detector constructors
            if (encodingDetector == null) {
                try {
                    c = encodingDetectorClass.getConstructor(ServiceLoader.class, Collection.class);
```

### ConstantValue
Condition `name == null` is always `false`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                throws TikaException, IOException {
            String name = element.getAttribute("class");
            if (name == null) {
                throw new TikaConfigException("class attribute must not be null: " + element);
            }
```

### ConstantValue
Condition `initProbHandler == null` is always `false`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            String initProbHandler = element.getAttribute("initializableProblemHandler");
            InitializableProblemHandler initializableProblemHandler;
            if (initProbHandler == null || initProbHandler.length() == 0) {
                initializableProblemHandler = loader.getInitializableProblemHandler();
            } else {
```

### ConstantValue
Condition `renderer == null` is always `true`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

            // Try the possible default and composite detector constructors
            if (renderer == null) {
                try {
                    c = rendererClass.getConstructor(ServiceLoader.class, Collection.class);
```

### ConstantValue
Value `renderer` is always 'null'
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                } catch (NoSuchMethodException me) {
                    LOG.debug("couldn't find constructor for service loader + collection for {}",
                            renderer);
                }
            }
```

### ConstantValue
Condition `parser == null` is always `true`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java

            // Try the possible default and composite parser constructors
            if (parser == null) {
                try {
                    c = parserClass.getConstructor(MediaTypeRegistry.class, ServiceLoader.class,
```

### ConstantValue
Condition `loaded.size() == 1` is always `false`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
            } else if (!supportsComposite()) {
                // No composite support, just return the first one
                if (loaded.size() == 1) {
                    return (CT) loaded.get(0);
                } else if (loaded.size() > 1) {
```

### ConstantValue
Condition `loaded.size() > 1` is always `true`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                if (loaded.size() == 1) {
                    return (CT) loaded.get(0);
                } else if (loaded.size() > 1) {
                    throw new TikaConfigException(
                            "Composite not supported for " + getParentTagName() +
```

### ConstantValue
Condition `defaultMetadata != null` is always `true`
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/geoinfo/GeographicInformationParser.java`
#### Snippet
```java
            dataStore = DataStores.open(file);
            defaultMetadata = new DefaultMetadata(dataStore.getMetadata());
            if (defaultMetadata != null) {
                extract(xhtmlContentHandler, metadata, defaultMetadata);
            }
```

### ConstantValue
Condition `outsn != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-crypto-module/src/main/java/org/apache/tika/parser/crypto/TSDParser.java`
#### Snippet
```java
        public String getSerialNumberFormatted() {
            String outsn = String.format(Locale.ROOT, "%12x", getSerialNumber());
            return outsn != null ? outsn.trim() : "" + getSerialNumber();
        }

```

### ConstantValue
Condition `name.equals("AndroidManifest.xml")` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/JarDetector.java`
#### Snippet
```java

        if (seenManifest != null) {
            if (name.equals("AndroidManifest.xml")) {
                // Is it an Android APK?
                return MediaType.application("vnd.android.package-archive");
```

### ConstantValue
Condition `vals.length > 1` is always `true`
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
                    });
                }
            } else if (vals.length > 1) {
                if (updateStrategy == UpdateStrategy.ADD) {
                    solrInputDocument.setField(n, vals);
```

### ConstantValue
Condition `zip == null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
            LOG.debug("zip file failed to open; attempting streaming detect");
        }
        if (zip == null) {
            //problem opening zip file (truncated?)
            try (InputStream is = new BufferedInputStream(Files.newInputStream(tis.getPath()))) {
```

### ConstantValue
Condition `ILST.equals(subType)` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp4/boxes/TikaUserDataBox.java`
#### Snippet
```java
            subType = reader.getString(4, StandardCharsets.ISO_8859_1);
        }
        if (ILST.equals(subType)) {
            processIList(reader, len);
        }
```

### ConstantValue
Condition `colType < 0` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFFileHeader.java`
#### Snippet
```java

        int colType = fieldRecord[11] & 0xFF;
        if (colType < 0) {
            throw new IOException("File truncated before coltype in header");
        }
```

### ConstantValue
Condition `col.fieldLength < 0` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFFileHeader.java`
#### Snippet
```java
        col.setType(colType);
        col.fieldLength = fieldRecord[16] & 0xFF;
        if (col.fieldLength < 0) {
            throw new TikaException(
                    "Field length for column " + col.getName(StandardCharsets.US_ASCII) +
```

### ConstantValue
Condition `col.fieldLength > DBFReader.MAX_FIELD_LENGTH` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFFileHeader.java`
#### Snippet
```java
                    "Field length for column " + col.getName(StandardCharsets.US_ASCII) +
                            " is < 0");
        } else if (col.fieldLength > DBFReader.MAX_FIELD_LENGTH) {
            throw new TikaException("Field length (" + col.fieldLength +
                    ") is greater than DBReader.MAX_FIELD_LENGTH (" + DBFReader.MAX_FIELD_LENGTH +
```

### ConstantValue
Condition `C == type` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/hwp/HwpTextExtractorV5.java`
#### Snippet
```java
                    } else if (X == type) { // EXTENDED
                        index += 7;
                    } else if (C == type) { // CONTROL
                        buf.append(' ');
                    }
```

### ConstantValue
Value `arg` is always 'null'
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
    protected static String commandLineSafe(String arg) {
        if (arg == null) {
            return arg;
        }
        //need to test for " " on windows, can't just add double quotes
```

### ConstantValue
Condition `allowEmptyValues` is always `true` when reached
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/ElementMetadataHandler.java`
#### Snippet
```java
        if (targetProperty != null && targetProperty.isMultiValuePermitted()) {
            if ((value != null && value.length() > 0) || allowEmptyValues) {
                if (value == null || value.length() == 0 && allowEmptyValues) {
                    value = "";
                }
```

### ConstantValue
Condition `contentType != null` is always `true` when reached
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        private File getOutputFile(String name, Metadata metadata, MediaType contentType) {
            String ext = getExtension(contentType);
            if (name.indexOf('.') == -1 && contentType != null) {
                name += ext;
            }
```

### ConstantValue
Condition `normalizedName == null` is always `false`
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            }

            if (normalizedName == null) {
                normalizedName = "file" + count++ + ext;
            }
```

### ConstantValue
Condition `input instanceof TikaInputStream` is always `true`
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
        if (input.markSupported()) {
            int mark = -1;
            if (input instanceof TikaInputStream) {
                if (((TikaInputStream) input).hasFile()) {
                    mark = (int) ((TikaInputStream) input).getLength();
```

### ConstantValue
Condition `cause instanceof UnsupportedFormatException` is always `false`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerParseExceptionMapper.java`
#### Snippet
```java
            } else if (cause instanceof IllegalStateException) {
                return buildResponse(cause, 422);
            } else if (cause instanceof UnsupportedFormatException) {
                return buildResponse(cause, 422);
            } else if (cause instanceof WebApplicationException) {
```

### ConstantValue
Condition `fetchRangeEnd > -1` is always `true` when reached
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/FetcherStreamFactory.java`
#### Snippet
```java
                LOG.debug("going to fetch '{}' from fetcher: {}", fetchKey, fetcherName);
                Fetcher fetcher = fetcherManager.getFetcher(fetcherName);
                if (fetchRangeStart > -1 && fetchRangeEnd > -1) {
                    if (!(fetcher instanceof RangeFetcher)) {
                        throw new IllegalArgumentException(
```

### ConstantValue
Value `fetchKey` is always 'null'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/FetcherStreamFactory.java`
#### Snippet
```java
    private String urlDecode(String fetchKey) {
        if (fetchKey == null) {
            return fetchKey;
        }
        try {
```

### ConstantValue
Condition `consecutiveRestarts > maxBind` is always `false`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
                consecutiveRestarts++;
                Thread.sleep(1000);
                if (consecutiveRestarts > maxBind) {
                    throw e;
                }
```

### ConstantValue
Condition `normalizedName == null` is always `false`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/UnpackerResource.java`
#### Snippet
```java
            }

            if (normalizedName == null) {
                normalizedName = count.toString();
            }
```

### ConstantValue
Condition `clazz == Integer.class` is always `false` when reached
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
                    checkTrustWorthy(setter, val);
                    m.invoke(object, val);
                } else if (clazz == int.class || clazz == Integer.class) {
                    m.invoke(object, Integer.parseInt(val));
                } else if (clazz == double.class || clazz == Double.class) {
```

### ConstantValue
Condition `clazz == long.class || clazz == Long.class` is always `true`
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
                } else if (clazz == float.class || clazz == Float.class) {
                    m.invoke(object, Float.parseFloat(val));
                } else if (clazz == long.class || clazz == Long.class) {
                    m.invoke(object, Long.parseLong(val));
                } else {
```

### ConstantValue
Condition `clazz == Long.class` is always `true` when reached
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
                } else if (clazz == float.class || clazz == Float.class) {
                    m.invoke(object, Float.parseFloat(val));
                } else if (clazz == long.class || clazz == Long.class) {
                    m.invoke(object, Long.parseLong(val));
                } else {
```

### ConstantValue
Value `hasSignature` is always 'true'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
        }
        if (hasSignature) {
            metadata.set(TikaCoreProperties.HAS_SIGNATURE, hasSignature);
        }
    }
```

### ConstantValue
Value `renderer` is always 'null'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
    private Renderer getPDFRenderer(Renderer renderer) {
        if (renderer == null) {
            return renderer;
        }
        if (renderer instanceof CompositeRenderer) {
```

### ConstantValue
Condition `itr == null` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        @SuppressWarnings("rawtypes") ListIterator itr = fields.listIterator();

        if (itr == null) {
            return;
        }
```

### ConstantValue
Condition `xHandler != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
                //swallow
            }
            if (xHandler != null) {
                xHandler.endDocument();
            }
```

### ConstantValue
Value `str` is always 'null'
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
        QuickStringBuffer append(String str) {
            if (str == null) {
                str = String.valueOf(str);
            }

```

### ConstantValue
Condition `type == null` is always `true`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/XMLErrorLogUpdater.java`
#### Snippet
```java
            String resourceId = null;
            try {
                while (reader.hasNext() && type == null && resourceId == null) {
                    reader.next();
                    switch (reader.getEventType()) {
```

### ConstantValue
Condition `extracts == null` is always `false`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/ExtractProfilerBuilder.java`
#### Snippet
```java
            throw new RuntimeException("Must specify -inputDir");
        }
        if (extracts == null && inputDir != null) {
            extracts = inputDir;
        }
```

### ConstantValue
Condition `extracts == null && inputDir != null` is always `false`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/ExtractProfilerBuilder.java`
#### Snippet
```java
            throw new RuntimeException("Must specify -inputDir");
        }
        if (extracts == null && inputDir != null) {
            extracts = inputDir;
        }
```

### ConstantValue
Value `metadataList` is always 'null'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/ExtractReader.java`
#### Snippet
```java
                    default:
                        LOG.warn("Can't yet process compression of type: {}", fileSuffixes.compression);
                        return metadataList;
                }
            }
```

### ConstantValue
Condition `value != null` is always `true`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
            switch (colInfo.getType()) {
                case Types.VARCHAR:
                    if (value != null && value.length() > colInfo.getPrecision()) {
                        value = value.substring(0, colInfo.getPrecision());
                        LOG.warn("truncated varchar value in {} : {}", colInfo.getName(), value);
```

### ConstantValue
Condition `consumerBuilder == null` is always `false`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/batch/EvalConsumersBuilder.java`
#### Snippet
```java
        EvalConsumerBuilder consumerBuilder = ClassLoaderUtil.buildClass(EvalConsumerBuilder.class,
                PropsUtil.getString(localAttrs.get("consumerBuilderClass"), null));
        if (consumerBuilder == null) {
            throw new RuntimeException("Must specify consumerBuilderClass in config file");
        }
```

### ConstantValue
Condition `map != null` is always `true`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/SlowCompositeReaderWrapper.java`
#### Snippet
```java
        }

        assert map != null;
        int size = in.leaves().size();
        final SortedSetDocValues[] values = new SortedSetDocValues[size];
```

### ConstantValue
Condition `extractExceptionA == null` is always `true`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java

        List<Metadata> metadataListA = null;
        if (extractExceptionA == null) {
            try {
                metadataListA = extractReader.loadExtract(fpsA.getExtractFile());
```

### ConstantValue
Value `dataString` is always 'null'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/DataURISchemeUtil.java`
#### Snippet
```java
        byte[] data = null;
        //strip out back slashes as you might have in css
        dataString = (dataString != null) ? dataString.replaceAll("\\\\", " ") : dataString;

        if (dataString == null || dataString.length() == 0) {
```

### ConstantValue
Condition `stream != null` is always `true`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoParserConfig.java`
#### Snippet
```java
            LOG.warn("GeoTopicConfig.properties not found in class path");
        } finally {
            if (stream != null) {
                try {
                    stream.close();
```

### ConstantValue
Condition `names != null` is always `true`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/opennlp/OpenNLPNameFinder.java`
#### Snippet
```java
        String[] names = Span.spansToStrings(nameSpans, tokens);
        Map<String, Set<String>> result = new HashMap<>();
        if (names != null && names.length > 0) {
            result.put(nameType, new HashSet<>(Arrays.asList(names)));
        }
```

### ConstantValue
Condition `stream != null` is always `true`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESConfig.java`
#### Snippet
```java
            // TODO warning
        } finally {
            if (stream != null) {
                try {
                    stream.close();
```

### ConstantValue
Condition `person != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
        String email = extractEmail(string, sb);
        String person = clean(sb.toString());
        if (person != null && person.length() > 0) {
            metadata.set(personProperty, person);
        }
```

### ConstantValue
Condition `person != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
        String person = clean(sb.toString());

        if (person != null && person.length() > 0) {
            metadata.add(personProperty, person);
        }
```

### ConstantValue
Condition `exit == PROCESS_COMPLETED_SUCCESSFULLY || exit == BatchProcessDriverCLI.PROCESS_...` is always `true` when reached
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
                    break;
                }
            } else if (exit != null && (exit == PROCESS_COMPLETED_SUCCESSFULLY ||
                    exit == BatchProcessDriverCLI.PROCESS_NO_RESTART_EXIT_CODE)) {
                LOG.trace("Will not restart: {}", exit);
                break;
```

### ConstantValue
Condition `"Author [surName=" + surName + ", middleName=" + middleName != null` is always `true`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
        @Override
        public String toString() {
            return "Author [surName=" + surName + ", middleName=" + middleName != null ?
                    middleName :
                    "" + ", firstName=" + firstName + ", affiliations=" + affiliations + "]";
```

### ConstantValue
Condition `exit == BatchProcessDriverCLI.PROCESS_NO_RESTART_EXIT_CODE` is always `true` when reached
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
                }
            } else if (exit != null && (exit == PROCESS_COMPLETED_SUCCESSFULLY ||
                    exit == BatchProcessDriverCLI.PROCESS_NO_RESTART_EXIT_CODE)) {
                LOG.trace("Will not restart: {}", exit);
                break;
```

### ConstantValue
Condition `fileResource == null` is always `true`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
        FileResource fileResource = null;
        long start = System.currentTimeMillis();
        while (fileResource == null) {
            //check to see if thread is interrupted before polling
            if (Thread.currentThread().isInterrupted()) {
```

### ConstantValue
Condition `MAX_CONSEC_WAIT_IN_MILLIS > 0` is always `true`
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java

            long elapsed = System.currentTimeMillis() - start;
            if (MAX_CONSEC_WAIT_IN_MILLIS > 0 && elapsed > MAX_CONSEC_WAIT_IN_MILLIS) {
                setEndedState(STATE.EXCEEDED_MAX_CONSEC_WAIT_MILLIS);
                break;
```

### ConstantValue
Value `is` is always 'null'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/AbstractFSConsumer.java`
#### Snippet
```java
        } catch (IOException e) {
            LOG.warn("{}", getXMLifiedLogMsg(IO_IS, fileResource.getResourceId(), e));
            flushAndClose(is);
        }
        return is;
```

### ConstantValue
Condition `opt != null` is always `true`
in `tika-batch/src/main/java/org/apache/tika/batch/builders/CommandLineParserBuilder.java`
#### Snippet
```java
            }
            Option opt = buildOption(optionNode);
            if (opt != null) {
                options.addOption(opt);
            }
```

### ConstantValue
Condition `credentialsAESEncrypted` is always `true`
in `tika-pipes/tika-httpclient-commons/src/main/java/org/apache/tika/client/HttpClientFactory.java`
#### Snippet
```java
                        "must specify aes key in the environment variable: " + AES_ENV_VAR);
            }
            if (credentialsAESEncrypted) {
                aes = new AES();
            }
```

### ConstantValue
Condition `selector != null` is always `true`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/FSCrawlerBuilder.java`
#### Snippet
```java

        DocumentSelector selector = buildSelector(attributes);
        if (selector != null) {
            crawler.setDocumentSelector(selector);
        }
```

### ConstantValue
Value `recursiveParserWrapper` is always 'false'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/BasicTikaFSConsumersBuilder.java`
#### Snippet
```java
        if (recursiveParserWrapperString != null) {
            recursiveParserWrapper =
                    PropsUtil.getBoolean(recursiveParserWrapperString, recursiveParserWrapper);
        } else {
            Node recursiveParserWrapperNode =
```

### ConstantValue
Value `recursiveParserWrapper` is always 'false'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/BasicTikaFSConsumersBuilder.java`
#### Snippet
```java
                recursiveParserWrapper = PropsUtil
                        .getBoolean(recursiveParserWrapperNode.getNodeValue(),
                                recursiveParserWrapper);
            }
        }
```

### ConstantValue
Value `streamOut` is always 'false'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/BasicTikaFSConsumersBuilder.java`
#### Snippet
```java
        String streamOutString = runtimeAttributes.get("streamOut");
        if (streamOutString != null) {
            streamOut = PropsUtil.getBoolean(streamOutString, streamOut);
        } else {
            Node streamOutNode = node.getAttributes().getNamedItem("streamout");
```

### ConstantValue
Value `streamOut` is always 'false'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/BasicTikaFSConsumersBuilder.java`
#### Snippet
```java
            Node streamOutNode = node.getAttributes().getNamedItem("streamout");
            if (streamOutNode != null) {
                streamOut = PropsUtil.getBoolean(streamOutNode.getNodeValue(), streamOut);
            }
        }
```

### ConstantValue
Condition `consumersManagerMaxMillis == null` is always `true`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/builders/BasicTikaFSConsumersBuilder.java`
#### Snippet
```java
            Node consumersManagerMaxMillisNode =
                    node.getAttributes().getNamedItem("consumersManagerMaxMillis");
            if (consumersManagerMaxMillis == null && consumersManagerMaxMillisNode != null) {
                consumersManagerMaxMillis =
                        PropsUtil.getLong(consumersManagerMaxMillisNode.getNodeValue(), null);
```

### ConstantValue
Condition `cs != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetDetector.java`
#### Snippet
```java

        Charset cs = Charset.forName(encoding);
        if (cs != null) {
            fDeclaredEncoding = cs.name();
        }
```

### ConstantValue
Condition `c_ >= (byte) 0x80` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
                     * Test if the next byte is in the valid UTF8 range
                     */
                    if (c_ >= (byte) 0x80 && c_ <= (byte) 0xBF) {
                        utf8 = true;
                        output[tmpPos++] = (byte) (c_ + 0x40);
```

### ConstantValue
Condition `firstByte >= 0x81` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
                it.charValue = (it.charValue << 8) | secondByte;

                if (firstByte >= 0x81 && firstByte <= 0xFE) {
                    // Two byte Char
                    if ((secondByte >= 0x40 && secondByte <= 0x7E) ||
```

### ConstantValue
Condition `streamName == null` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork13PackageParser.java`
#### Snippet
```java
            throws TikaException, IOException, SAXException {
        String streamName = entry.getName();
        if (streamName == null) {
            return;
        }
```

### ConstantValue
Condition `zipStream != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork13PackageParser.java`
#### Snippet
```java
        if (zipFile != null) {
            type = processZipFile(zipFile, metadata, xhtml, context);
        } else if (zipStream != null) {
            type = processZipStream(zipStream, metadata, xhtml, context);
        }
```

### ConstantValue
Condition `filter.equals(null)` is always `false`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
     */
    public void setFilter(String filter) {
        if (filter.equals(null)) {
            throw new IllegalArgumentException(
                    "Filter value cannot be null. Valid values are point, hermite, " +
```

### ConstantValue
Condition `body != null` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
        xhtml.startElement("p");
        String body = clean(properties.get("body"));
        if (body != null) {
            xhtml.characters(body);
        }
```

### ConstantValue
Condition `read < value.length` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

            // pull apart the body, getting the heading (^....\x0d\x0a)
            while (read < value.length) {
                byte val_next = value[read++];
                if (val_next == CT) {      //  start of a new section , first is the heading
```

### ConstantValue
Condition `val_next != CT` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                    // into the stream
                    if (FORMAT == this.FMT_IPTC_RTR) {
                        if (val_next != CT) {
                            // for any non-whitespace, we need to go back an additional step to
                            // non destroy the data
```

### ConstantValue
Condition `val_next != CT` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                    // the stream
                    if (FORMAT == this.FMT_IPTC_RTR) {
                        if (val_next != CT) {
                            // for any non-whitespace, we need to go back an additional step to
                            // non destroy the data
```

### ConstantValue
Condition `!longline.equals(longkey)` is always `true` when reached
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                                    byline.length(), term)).append(" ");
                            metastarted = true;
                            longline = ((tmp_line.toString().contains("=")) && (!longline.equals(longkey)) ?
                                    longkey : "");
                        } else if (tmp_line.toString().toLowerCase(Locale.ROOT).startsWith("c.")) {
```

### ConstantValue
Result of `longline.equals(longkey)` is always 'false'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                                    byline.length(), term)).append(" ");
                            metastarted = true;
                            longline = ((tmp_line.toString().contains("=")) && (!longline.equals(longkey)) ?
                                    longkey : "");
                        } else if (tmp_line.toString().toLowerCase(Locale.ROOT).startsWith("c.")) {
```

### ConstantValue
Condition `read < value.length` is always `true`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                        // read until the train runs out of tracks
                        bdy_body.append((char) (val_next & 0xff));  // convert the byte to an unsigned int
                        val_next = (read < value.length) ? value[read++] : 0x00;
                        if (read >= value.length) {
                            break;
```

## RuleId[id=MethodOverridesStaticMethod]
### MethodOverridesStaticMethod
Method `process()` tries to override a static method of a superclass
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
     * @throws TikaException if there was an exception outside of per page processing
     */
    public static void process(PDDocument pdDocument, ContentHandler handler,
                               ParseContext context,
                               Metadata metadata, PDFParserConfig config)
```

## RuleId[id=IOResource]
### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OldExcelParser.java`
#### Snippet
```java
        // Split and output
        String line;
        BufferedReader reader = new BufferedReader(new StringReader(text));
        while ((line = reader.readLine()) != null) {
            xhtml.startElement("p");
```

### IOResource
'VBAMacroReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParser.java`
#### Snippet
```java
        Map<String, String> macros = null;
        try {
            reader = new VBAMacroReader(fs);
            macros = reader.readMacros();
        } catch (SecurityException e) {
```

### IOResource
'PublisherTextExtractor' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParser.java`
#### Snippet
```java
                break;
            case PUBLISHER:
                PublisherTextExtractor publisherTextExtractor = new PublisherTextExtractor(root);
                xhtml.element("p", publisherTextExtractor.getText());
                break;
```

### IOResource
'VisioTextExtractor' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/OfficeParser.java`
#### Snippet
```java
                break;
            case VISIO:
                VisioTextExtractor visioTextExtractor = new VisioTextExtractor(root);
                for (String text : visioTextExtractor.getAllText()) {
                    xhtml.element("p", text);
```

### IOResource
'WordExtractor' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java

        org.apache.poi.hwpf.extractor.WordExtractor wordExtractor =
                new org.apache.poi.hwpf.extractor.WordExtractor(document);

        // Grab the list of pictures. As far as we can tell,
```

### IOResource
'Word6Extractor' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
        //documents in the root.
        HWPFOldDocument doc = new HWPFOldDocument(root);
        extractor = new Word6Extractor(doc);

        for (String p : extractor.getParagraphText()) {
```

### IOResource
'UnsynchronizedByteArrayOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmExtractor.java`
#### Snippet
```java
    public byte[] extractChmEntry(DirectoryListingEntry directoryListingEntry)
            throws TikaException {
        UnsynchronizedByteArrayOutputStream buffer = new UnsynchronizedByteArrayOutputStream();
        ChmLzxBlock lzxBlock = null;
        try {
```

### IOResource
'TaggedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFParser.java`
#### Snippet
```java
                      ParseContext context) throws IOException, SAXException, TikaException {
        metadata.set(Metadata.CONTENT_TYPE, "application/rtf");
        TaggedInputStream tagged = new TaggedInputStream(stream);
        try {
            XHTMLContentHandler xhtmlHandler = new XHTMLContentHandler(handler, metadata);
```

### IOResource
'BoundedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/RTFObjDataParser.java`
#### Snippet
```java
                    UnsynchronizedByteArrayOutputStream out = new UnsynchronizedByteArrayOutputStream();
                    is.reset();
                    BoundedInputStream bis = new BoundedInputStream(memoryLimitInKb * 1024, is);
                    IOUtils.copy(is, out);
                    if (bis.hasHitBound()) {
```

### IOResource
'XSLFSlideShow' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSLFPowerPointExtractorDecorator.java`
#### Snippet
```java
        XSLFSlideShow document = null;
        try {
            document = new XSLFSlideShow(extractor.getPackage());
        } catch (Exception e) {
            throw new TikaException(e.getMessage()); // Shouldn't happen
```

### IOResource
'POIFSFileSystem' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/extractor/microsoft/MSEmbeddedStreamTranslator.java`
#### Snippet
```java
            if (tin.getOpenContainer() != null &&
                    tin.getOpenContainer() instanceof DirectoryEntry) {
                POIFSFileSystem fs = new POIFSFileSystem();
                copy((DirectoryEntry) tin.getOpenContainer(), fs.getRoot());
                try (UnsynchronizedByteArrayOutputStream bos2 = new UnsynchronizedByteArrayOutputStream()) {
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/XMPPacketScanner.java`
#### Snippet
```java
    public boolean parse(InputStream in, OutputStream xmlOut) throws IOException {
        if (!in.markSupported()) {
            in = new java.io.BufferedInputStream(in);
        }
        boolean foundXMP = skipAfter(in, PACKET_HEADER);
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
            // (properly) supports the mark feature
            if (!(stream.markSupported())) {
                stream = new BufferedInputStream(stream);
            }
            return new TikaInputStream(stream, tmp, -1, getExtension(metadata));
```

### IOResource
'ForkObjectInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-core/src/main/java/org/apache/tika/fork/ForkObjectInputStream.java`
#### Snippet
```java

        ObjectInputStream deserializer =
                new ForkObjectInputStream(new ByteArrayInputStream(data), loader);
        return deserializer.readObject();
    }
```

### IOResource
'UnsynchronizedByteArrayOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-core/src/main/java/org/apache/tika/pipes/PipesClient.java`
#### Snippet
```java

        //wait for ready signal
        final UnsynchronizedByteArrayOutputStream bos = new UnsynchronizedByteArrayOutputStream();
        FutureTask<Integer> futureTask = new FutureTask<>(() -> {
            int b = input.read();
```

### IOResource
'InputStreamProxy' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
        if (object instanceof InputStream) {
            resources.add(new InputStreamResource((InputStream) object));
            object = new InputStreamProxy(n);
        } else if (object instanceof RecursiveParserWrapperHandler) {
            resources.add(new RecursiveMetadataContentHandlerResource(
```

### IOResource
'TemporaryResources' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-core/src/main/java/org/apache/tika/detect/TrainedModelDetector.java`
#### Snippet
```java
     */
    private void writeHisto(final float[] histogram) throws IOException {
        Path histPath = new TemporaryResources().createTempFile();
        try (Writer writer = Files.newBufferedWriter(histPath, UTF_8)) {
            for (float bin : histogram) {
```

### IOResource
'RereadableInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/zip/utils/ZipSalvager.java`
#### Snippet
```java
        try {
            if (!(brokenZip instanceof RereadableInputStream)) {
                brokenZip = new RereadableInputStream(brokenZip, 50000, true);
            }

```

### IOResource
'UnsynchronizedByteArrayOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-module/src/main/java/org/apache/tika/parser/mbox/MboxParser.java`
#### Snippet
```java
                        break;
                    }
                    UnsynchronizedByteArrayOutputStream message = new UnsynchronizedByteArrayOutputStream(100000);
                    do {
                        if (curLine.startsWith(" ") || curLine.startsWith("\t")) {
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/audio/MidiParser.java`
#### Snippet
```java
        // MidiSystem expects the stream to support the mark feature
        if (!stream.markSupported()) {
            stream = new BufferedInputStream(stream);
        }
        try {
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/audio/AudioParser.java`
#### Snippet
```java
        // AudioSystem expects the stream to support the mark feature
        if (!stream.markSupported()) {
            stream = new BufferedInputStream(stream);
        }
        stream = new SkipFullyInputStream(stream);
```

### IOResource
'SkipFullyInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/audio/AudioParser.java`
#### Snippet
```java
            stream = new BufferedInputStream(stream);
        }
        stream = new SkipFullyInputStream(stream);
        try {
            AudioFileFormat fileFormat = AudioSystem.getAudioFileFormat(stream);
```

### IOResource
'POIFSFileSystem' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/QPWTextExtractor.java`
#### Snippet
```java
            throws IOException, SAXException, TikaException {

        POIFSFileSystem pfs = new POIFSFileSystem(input);
        DirectoryNode rootNode = pfs.getRoot();
        if (rootNode == null || !rootNode.hasEntry(OLE_DOCUMENT_NAME)) {
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/wordperfect/WPInputStream.java`
#### Snippet
```java
    public WPInputStream(InputStream in) {
        if (!in.markSupported()) {
            in = new BufferedInputStream(in);
        }
        this.in = new DataInputStream(in);
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java
        // Ensure that the stream supports the mark feature
        if (!stream.markSupported()) {
            stream = new BufferedInputStream(stream);
        }

```

### IOResource
'CloseShieldInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/CompressorParser.java`
#### Snippet
```java
        // should not be closed
        if (stream.markSupported()) {
            stream = new CloseShieldInputStream(stream);
        } else {
            // Ensure that the stream supports the mark feature
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        public void process(InputStream stream, OutputStream output, Metadata metadata)
                throws Exception {
            PrintWriter writer = new PrintWriter(getOutputWriter(output, encoding));
            writer.println(detector.detect(stream, metadata).toString());
            writer.flush();
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        protected ContentHandler getContentHandler(OutputStream output, Metadata metadata)
                throws Exception {
            final PrintWriter writer = new PrintWriter(getOutputWriter(output, encoding));
            return new LanguageHandler() {
                public void endDocument() {
```

### IOResource
'OutputStreamWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/writer/TextMessageBodyWriter.java`
#### Snippet
```java
            throw new WebApplicationException("Metadata object must only have one entry!");
        }
        Writer writer = new OutputStreamWriter(entityStream, UTF_8);

        for (String name : metadata.names()) {
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                new LanguageProfilerBuilder(name, ABSOLUTE_MIN_NGRAM_LENGTH,
                        ABSOLUTE_MAX_NGRAM_LENGTH);
        BufferedInputStream bis = new BufferedInputStream(is);

        byte[] buffer = new byte[4096];
```

### IOResource
'DefaultHttpClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/gazetteer/GeoGazetteerClient.java`
#### Snippet
```java
     */
    public boolean checkAvail() {
        HttpClient httpClient = new DefaultHttpClient();

        try {
```

### IOResource
'DefaultHttpClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/gazetteer/GeoGazetteerClient.java`
#### Snippet
```java
     */
    public Map<String, List<Location>> getLocations(List<String> locations) {
        HttpClient httpClient = new DefaultHttpClient();

        try {
```

### IOResource
'OutputStreamWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-server/tika-server-standard/src/main/java/org/apache/tika/server/standard/writer/XMPMessageBodyWriter.java`
#### Snippet
```java
            throws IOException, WebApplicationException {
        try {
            Writer writer = new OutputStreamWriter(entityStream, UTF_8);
            XMPMetadata xmp = new XMPMetadata(metadata);
            writer.write(xmp.toString());
```

### IOResource
'DefaultHttpClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/captioning/tf/TensorflowRESTCaptioner.java`
#### Snippet
```java
                    maxCaptionLength));

            DefaultHttpClient client = new DefaultHttpClient();
            HttpResponse response = client.execute(new HttpGet(healthUri));
            available = response.getStatusLine().getStatusCode() == 200;
```

### IOResource
'DefaultHttpClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/captioning/tf/TensorflowRESTCaptioner.java`
#### Snippet
```java
        List<CaptionObject> capObjs = new ArrayList<>();
        try {
            DefaultHttpClient client = new DefaultHttpClient();

            HttpPost request = new HttpPost(getApiUri(metadata));
```

### IOResource
'DefaultHttpClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowRESTVideoRecogniser.java`
#### Snippet
```java
                    mode));

            DefaultHttpClient client = new DefaultHttpClient();
            HttpResponse response = client.execute(new HttpGet(healthUri));
            available = response.getStatusLine().getStatusCode() == 200;
```

### IOResource
'DefaultHttpClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowRESTRecogniser.java`
#### Snippet
```java
                    "/classify/image?topn=%1$d&min_confidence=%2$f", topN, minConfidence));

            DefaultHttpClient client = new DefaultHttpClient();
            HttpResponse response = client.execute(new HttpGet(healthUri));
            available = response.getStatusLine().getStatusCode() == 200;
```

### IOResource
'DefaultHttpClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowRESTRecogniser.java`
#### Snippet
```java
        List<RecognisedObject> recObjs = new ArrayList<>();
        try {
            DefaultHttpClient client = new DefaultHttpClient();

            HttpPost request = new HttpPost(getApiUri(metadata));
```

### IOResource
'ClassPathXmlApplicationContext' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-example/src/main/java/org/apache/tika/example/SpringExample.java`
#### Snippet
```java
public class SpringExample {
    public static void main(String[] args) throws Exception {
        ApplicationContext context = new ClassPathXmlApplicationContext(
                new String[]{"org/apache/tika/example/spring.xml"});
        Parser parser = context.getBean("tika", Parser.class);
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-example/src/main/java/org/apache/tika/example/ImportContextImpl.java`
#### Snippet
```java
        }
        if (stream != null && !stream.markSupported()) {
            stream = new BufferedInputStream(stream);
        }
        type = detector.detect(stream, metadata);
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java
    List<CSVResult> sniff(Reader reader) throws IOException {
        if (!reader.markSupported()) {
            reader = new BufferedReader(reader);
        }
        List<CSVResult> ret = new ArrayList<>();
```

### IOResource
'ZipFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork18PackageParser.java`
#### Snippet
```java
                zipFile = (ZipFile) container;
            } else if (tis.hasFile()) {
                zipFile = new ZipFile(tis.getFile());
            } else {
                zipStream = new ZipInputStream(stream);
```

### IOResource
'ZipInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork18PackageParser.java`
#### Snippet
```java
                zipFile = new ZipFile(tis.getFile());
            } else {
                zipStream = new ZipInputStream(stream);
            }
        } else {
```

### IOResource
'ZipInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/iwana/IWork18PackageParser.java`
#### Snippet
```java
            }
        } else {
            zipStream = new ZipInputStream(stream);
        }

```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `tika-example/src/main/java/org/apache/tika/example/ZipListFiles.java`
#### Snippet
```java
public class ZipListFiles {
    public static void main(String[] args) throws Exception {
        if (args.length > 0) {
            for (String file : args) {
                System.out.println("Files in " + file + " file:");
```

## RuleId[id=InjectedReferences]
### InjectedReferences
Unknown encoding: 'iso-8859-'
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
            if (iso.matches()) {
                // Handle "iso 8859-x" error
                name = "iso-8859-" + iso.group(1);
                charset = COMMON_CHARSETS.get(name);
            } else if (cp.matches()) {
```

### InjectedReferences
Unknown encoding: 'cp'
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
            } else if (cp.matches()) {
                // Handle "cp-xxx" error
                name = "cp" + cp.group(1);
                charset = COMMON_CHARSETS.get(name);
            } else if (win.matches()) {
```

### InjectedReferences
Unknown encoding: 'windows-'
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
            } else if (win.matches()) {
                // Handle "winxxx" and "win-xxx" errors
                name = "windows-" + win.group(1);
                charset = COMMON_CHARSETS.get(name);
            }
```

## RuleId[id=Java8MapForEach]
### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java
    private synchronized void report(AsyncStatus.ASYNC_STATUS status) {
        Map<PipesResult.STATUS, Long> localCounts = new HashMap<>();
        counts.entrySet().forEach( e -> localCounts.put(e.getKey(), e.getValue().longValue()));
        asyncStatus.update(localCounts, totalCountResult, status);
        try (Writer writer = Files.newBufferedWriter(statusFile, StandardCharsets.UTF_8)) {
```

## RuleId[id=FieldMayBeStatic]
### FieldMayBeStatic
Field `NOT_SEEN_YET` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractListManager.java`
#### Snippet
```java
        //counts can == 0 if the format is decimal, make sure
        //that flag values are < 0
        private final Integer NOT_SEEN_YET = -1;
        private final Integer FIRST_SKIPPED = -2;
        private final LevelTuple[] levelTuples;
```

### FieldMayBeStatic
Field `FIRST_SKIPPED` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractListManager.java`
#### Snippet
```java
        //that flag values are < 0
        private final Integer NOT_SEEN_YET = -1;
        private final Integer FIRST_SKIPPED = -2;
        private final LevelTuple[] levelTuples;
        Pattern LEVEL_INTERPOLATOR = Pattern.compile("%(\\d+)");
```

### FieldMayBeStatic
Field `maxChunkSize` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/RDCAnalysisChunking.java`
#### Snippet
```java
     * The max chunk size in RDC analysis chunking.
     */
    private final int maxChunkSize = 65535;

    /**
```

### FieldMayBeStatic
Field `active` may be 'static'
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
     */
    private final DataOutputStream output;
    private final boolean active = true;
    //milliseconds to sleep before checking to see if there has been any reading/writing
    //If no reading or writing in this time, shutdown the server.
```

### FieldMayBeStatic
Field `minLength` may be 'static'
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
     * The minimum length of data to provides for magic analyzis
     */
    private final int minLength = 0;
    /**
     * The MimeType acronym
```

### FieldMayBeStatic
Field `timeoutMs` may be 'static'
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
    private static final long serialVersionUID = -1079128990650687037L;
    //make this parameterizable
    private final long timeoutMs = 60000;
    /**
     * Media types supported by the external program.
```

### FieldMayBeStatic
Field `minimumMaxFieldSizeInAlwaysFields` may be 'static'
in `tika-core/src/main/java/org/apache/tika/metadata/writefilter/StandardWriteFilter.java`
#### Snippet
```java
    //As of 2022-03, the longest mime is 146.  Doubling that gives
    //us some leeway.  If a mime is truncated, bad things will happen.
    private final int minimumMaxFieldSizeInAlwaysFields = 300;


```

### FieldMayBeStatic
Field `endPoint` may be 'static'
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaAsyncHttpClient.java`
#### Snippet
```java
    }

    private final String endPoint = "async";

    String getEndpoint() {
```

### FieldMayBeStatic
Field `maxWaitForTikaMs` may be 'static'
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaPipesHttpClient.java`
#### Snippet
```java
    private final int maxRetries = 3;
    //if can't make contact with Tika server, max wait time in ms
    private final long maxWaitForTikaMs = 120000;
    //how often to ping /tika (in ms) to see if the server is up and running
    private final long pulseWaitForTikaMs = 1000;
```

### FieldMayBeStatic
Field `maxRetries` may be 'static'
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaPipesHttpClient.java`
#### Snippet
```java
    private final String endPointUrl;
    private final String tikaUrl;
    private final int maxRetries = 3;
    //if can't make contact with Tika server, max wait time in ms
    private final long maxWaitForTikaMs = 120000;
```

### FieldMayBeStatic
Field `pulseWaitForTikaMs` may be 'static'
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaPipesHttpClient.java`
#### Snippet
```java
    private final long maxWaitForTikaMs = 120000;
    //how often to ping /tika (in ms) to see if the server is up and running
    private final long pulseWaitForTikaMs = 1000;

    /**
```

### FieldMayBeStatic
Field `endPoint` may be 'static'
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaPipesHttpClient.java`
#### Snippet
```java
    private static final String TIKA_ENDPOINT = "tika";
    private static final Logger LOGGER = LoggerFactory.getLogger(TikaPipesHttpClient.class);
    private final String endPoint = "pipes";

    private final HttpClientFactory httpClientFactory;
```

### FieldMayBeStatic
Field `DATE_FORMAT` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-crypto-module/src/main/java/org/apache/tika/parser/crypto/TSDParser.java`
#### Snippet
```java

    private static class TSDMetas {
        private final String DATE_FORMAT = "dd/MM/yyyy HH:mm:ss";

        private Boolean parseBuilt = false;
```

### FieldMayBeStatic
Field `MAX_MARK` may be 'static'
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(TikaCLI.class);

    private final int MAX_MARK = 20 * 1024 * 1024;//20MB

    private final OutputType NO_OUTPUT = new OutputType() {
```

### FieldMayBeStatic
Field `passwordsAESEncrypted` may be 'static'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TlsConfig.java`
#### Snippet
```java
    private boolean active = false;
    //TODO make this configurable
    private final boolean passwordsAESEncrypted = false;
    private String keyStoreType = null;
    private String keyStorePassword = null;
```

### FieldMayBeStatic
Field `commitEveryXRows` may be 'static'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/DBWriter.java`
#### Snippet
```java

    private static final AtomicInteger WRITER_ID = new AtomicInteger();
    private final Long commitEveryXRows = 10000L;
    //private final Long commitEveryXMS = 60000L;

```

### FieldMayBeStatic
Field `NULL_VALUE` may be 'static'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/Report.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(Report.class);

    final String NULL_VALUE = "";//TODO: make this configurable!!!
    Map<String, XSLXCellFormatter> cellFormatters = new HashMap<>();
    String sql;
```

### FieldMayBeStatic
Field `MAX_CONSEC_WAIT_IN_MILLIS` may be 'static'
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
    //used to lock checks on state to prevent
    private final Object lock = new Object();
    private final long MAX_CONSEC_WAIT_IN_MILLIS = 10 * 60 * 1000;// 10 minutes
    //this records the file that is currently
    //being processed.  It is null if no file is currently being processed.
```

### FieldMayBeStatic
Field `MAXIMUM_TEXT_CHUNK_SIZE` may be 'static'
in `tika-example/src/main/java/org/apache/tika/example/ContentHandlerExample.java`
#### Snippet
```java
 */
public class ContentHandlerExample {
    protected final int MAXIMUM_TEXT_CHUNK_SIZE = 40;

    /**
```

### FieldMayBeStatic
Field `swapProbability` may be 'static'
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/general/SpanSwapper.java`
#### Snippet
```java
    Random random = new Random();
    int maxSpanLength = 10000;
    private final float swapProbability = 0.01f;

    @Override
```

### FieldMayBeStatic
Field `quoteCharacter` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/CSVSniffer.java`
#### Snippet
```java

        //hardcode this for now
        private final char quoteCharacter = '"';

        Map<Integer, MutableInt> rowLengthCounts = new HashMap<>();
```

### FieldMayBeStatic
Field `incrementalUpdate` may be 'static'
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
    private boolean willEncrypt = false;
    // signing
    private final boolean incrementalUpdate = false;
    private boolean reachedSignature = false;
    private long signatureOffset;
```

### FieldMayBeStatic
Field `cSteps` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/tess4j/ImageDeskew.java`
#### Snippet
```java

    private final BufferedImage cImage;
    private final int cSteps = 200;
    private double[] cSinA;
    private double[] cCosA;
```

## RuleId[id=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `ChmAssert` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmAssert.java`
#### Snippet
```java
 * Contains chm extractor assertions
 */
public class ChmAssert {
    /**
     * Checks a validity of the chmBlockSegment parameters
```

### UtilityClassWithoutPrivateConstructor
Class `OOXMLExtractorFactory` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
 * returns it.
 */
public class OOXMLExtractorFactory {

    private static final Logger LOG = LoggerFactory.getLogger(OOXMLExtractorFactory.class);
```

### UtilityClassWithoutPrivateConstructor
Class `IndentUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/IndentUtil.java`
#### Snippet
```java
package org.apache.tika.parser.microsoft.onenote;

class IndentUtil {
    public static String getIndent(int indentLevel) {
        StringBuilder retval = new StringBuilder();
```

### UtilityClassWithoutPrivateConstructor
Class `Bit` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/Bit.java`
#### Snippet
```java
 * The class is used to read/set bit value for a byte array
 */
public class Bit {
    /**
     * Read a bit value from a byte array with the specified bit position.
```

### UtilityClassWithoutPrivateConstructor
Class `GuidUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/GuidUtil.java`
#### Snippet
```java
import java.util.UUID;

public class GuidUtil {
    public static UUID emptyGuid() {
        return UUID.fromString("00000000-0000-0000-0000-000000000000");
```

### UtilityClassWithoutPrivateConstructor
Class `ByteUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/ByteUtil.java`
#### Snippet
```java
import java.util.List;

public final class ByteUtil {
    public static byte[] toByteArray(List<Byte> bytes) {
        byte[] res = new byte[bytes.size()];
```

### UtilityClassWithoutPrivateConstructor
Class `UuidUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/UuidUtils.java`
#### Snippet
```java
import java.util.UUID;

public class UuidUtils {
    public static UUID asUuid(byte[] bytes) {
        ByteBuffer bb = ByteBuffer.wrap(bytes);
```

### UtilityClassWithoutPrivateConstructor
Class `SequenceNumberGenerator` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/SequenceNumberGenerator.java`
#### Snippet
```java
import org.apache.tika.exception.TikaException;

public class SequenceNumberGenerator {
    /**
     * Specify the max token value.
```

### UtilityClassWithoutPrivateConstructor
Class `AdapterHelper` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/AdapterHelper.java`
#### Snippet
```java
import org.apache.tika.parser.microsoft.onenote.fsshttpb.util.ByteUtil;

public class AdapterHelper {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `JsonEmitData` has only 'static' members, and lacks a 'private' constructor
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonEmitData.java`
#### Snippet
```java
import org.apache.tika.pipes.emitter.EmitKey;

public class JsonEmitData {

    public static void toJson(EmitData emitData, Writer writer) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `JsonMetadataList` has only 'static' members, and lacks a 'private' constructor
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadataList.java`
#### Snippet
```java
import org.apache.tika.metadata.TikaCoreProperties;

public class JsonMetadataList {
    static volatile boolean PRETTY_PRINT = false;

```

### UtilityClassWithoutPrivateConstructor
Class `JsonMetadata` has only 'static' members, and lacks a 'private' constructor
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadata.java`
#### Snippet
```java
import org.apache.tika.metadata.Metadata;

public class JsonMetadata {

    static volatile boolean PRETTY_PRINT = false;
```

### UtilityClassWithoutPrivateConstructor
Class `XMPMetadataExtractor` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/XMPMetadataExtractor.java`
#### Snippet
```java
 * XMP Metadata Extractor based on Apache XmpBox.
 */
public class XMPMetadataExtractor {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `JsonFetchEmitTupleList` has only 'static' members, and lacks a 'private' constructor
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonFetchEmitTupleList.java`
#### Snippet
```java
import org.apache.tika.pipes.FetchEmitTuple;

public class JsonFetchEmitTupleList {

    public static List<FetchEmitTuple> fromJson(Reader reader) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `JsonFetchEmitTuple` has only 'static' members, and lacks a 'private' constructor
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonFetchEmitTuple.java`
#### Snippet
```java
import org.apache.tika.utils.StringUtils;

public class JsonFetchEmitTuple {

    public static final String ID = "id";
```

### UtilityClassWithoutPrivateConstructor
Class `FilenameUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/io/FilenameUtils.java`
#### Snippet
```java


public class FilenameUtils {


```

### UtilityClassWithoutPrivateConstructor
Class `IOUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/io/IOUtils.java`
#### Snippet
```java
import java.io.InputStream;

public class IOUtils {

    public static long skip(final InputStream input, final long toSkip, byte[] buffer)
```

### UtilityClassWithoutPrivateConstructor
Class `DataElementUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
import org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj.basic.SerialNumber;

public class DataElementUtils {
    public static final UUID RootExGuid = UUID.fromString("84DEFAB9-AAA3-4A0D-A3A8-520C77AC7073");
    public static final UUID CellSecondExGuid =
```

### UtilityClassWithoutPrivateConstructor
Class `EndianUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/io/EndianUtils.java`
#### Snippet
```java
 * Origin of code: Based on the version in POI
 */
public class EndianUtils {
    private static final int LONG_SIZE = 8;

```

### UtilityClassWithoutPrivateConstructor
Class `StandardsText` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
 * </p>
 */
public class StandardsText {
    // Regular expression to match uppercase headers
    private static final String REGEX_HEADER =
```

### UtilityClassWithoutPrivateConstructor
Class `MappedBufferCleaner` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/io/MappedBufferCleaner.java`
#### Snippet
```java
 * Copied/pasted from the Apache Lucene/Solr project.
 */
public class MappedBufferCleaner {
    /**
     * <code>true</code>, if this platform supports unmapping mmapped files.
```

### UtilityClassWithoutPrivateConstructor
Class `CleanPhoneText` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/sax/CleanPhoneText.java`
#### Snippet
```java
 * Class to help de-obfuscate phone numbers in text.
 */
public class CleanPhoneText {
    public static final String[][][] cleanSubstitutions =
            new String[][][]{{{"&#\\d{1,3};", ""}},         // first simply remove numeric entities
```

### UtilityClassWithoutPrivateConstructor
Class `StandardOrganizations` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/sax/StandardOrganizations.java`
#### Snippet
```java
 * East), and British and American standard organizations among the national-based ones.
 */
public class StandardOrganizations {

    private static final Map<String, String> organizations;
```

### UtilityClassWithoutPrivateConstructor
Class `HexCoDec` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/mime/HexCoDec.java`
#### Snippet
```java
 * A set of Hex encoding and decoding utility methods.
 */
public class HexCoDec {

    private static final char[] HEX_CHARS =
```

### UtilityClassWithoutPrivateConstructor
Class `MimeTypesFactory` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypesFactory.java`
#### Snippet
```java
 * Creates instances of MimeTypes.
 */
public class MimeTypesFactory {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `SystemUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/SystemUtils.java`
#### Snippet
```java
 * Copied from commons-lang to avoid requiring the dependency
 */
public class SystemUtils {

    public static final String OS_NAME = getSystemProperty("os.name");
```

### UtilityClassWithoutPrivateConstructor
Class `ParserUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/ParserUtils.java`
#### Snippet
```java
 * Helper util methods for Parsers themselves.
 */
public class ParserUtils {

    public final static Property EMBEDDED_PARSER = Property.internalText(
```

### UtilityClassWithoutPrivateConstructor
Class `RegexUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/RegexUtils.java`
#### Snippet
```java
 * content
 */
public class RegexUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ExceptionUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/ExceptionUtils.java`
#### Snippet
```java
import org.apache.tika.exception.TikaException;

public class ExceptionUtils {

    private final static Pattern MSG_PATTERN = Pattern.compile(":[^\r\n]+");
```

### UtilityClassWithoutPrivateConstructor
Class `ConcurrentUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/ConcurrentUtils.java`
#### Snippet
```java
 * @since Apache Tika 1.11
 */
public class ConcurrentUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ServiceLoaderUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/ServiceLoaderUtils.java`
#### Snippet
```java
 * Service Loading and Ordering related utils
 */
public class ServiceLoaderUtils {
    /**
     * Sorts a list of loaded classes, so that non-Tika ones come
```

### UtilityClassWithoutPrivateConstructor
Class `CompareUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/CompareUtils.java`
#### Snippet
```java
package org.apache.tika.utils;

public class CompareUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `CharsetUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/CharsetUtils.java`
#### Snippet
```java
import java.util.regex.Pattern;

public class CharsetUtils {

    private static final Pattern CHARSET_NAME_PATTERN = Pattern.compile("[ \\\"]*([^ >,;\\\"]+).*");
```

### UtilityClassWithoutPrivateConstructor
Class `StringUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/StringUtils.java`
#### Snippet
```java
import java.util.List;

public class StringUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ProcessUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/ProcessUtils.java`
#### Snippet
```java
import java.util.concurrent.TimeUnit;

public class ProcessUtils {


```

### UtilityClassWithoutPrivateConstructor
Class `AnnotationUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/utils/AnnotationUtils.java`
#### Snippet
```java
 * @since Apache Tika 1.14
 */
public class AnnotationUtils {
    private static final Logger LOG = LoggerFactory.getLogger(AnnotationUtils.class);

```

### UtilityClassWithoutPrivateConstructor
Class `TikaConfigSerializer` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
import org.apache.tika.utils.XMLReaderUtils;

public class TikaConfigSerializer {

    private static final Logger LOG = LoggerFactory.getLogger(TikaConfigSerializer.class);
```

### UtilityClassWithoutPrivateConstructor
Class `ExternalParsersFactory` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParsersFactory.java`
#### Snippet
```java
 * @see ExternalParsersConfigReader
 */
public class ExternalParsersFactory {

    public static List<ExternalParser> create() throws IOException, TikaException {
```

### UtilityClassWithoutPrivateConstructor
Class `LanguageNames` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/language/detect/LanguageNames.java`
#### Snippet
```java
 * language detector API?
 */
public class LanguageNames {

    public static String makeName(String language, String script, String region) {
```

### UtilityClassWithoutPrivateConstructor
Class `ChannelTypePropertyConverter` has only 'static' members, and lacks a 'private' constructor
in `tika-core/src/main/java/org/apache/tika/metadata/XMPDM.java`
#### Snippet
```java
     */
    @Deprecated
    class ChannelTypePropertyConverter {
        private static final Property property = AUDIO_CHANNEL_TYPE;

```

### UtilityClassWithoutPrivateConstructor
Class `ISATabUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/isatab/ISATabUtils.java`
#### Snippet
```java
import org.apache.tika.sax.XHTMLContentHandler;

public class ISATabUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ZipSalvager` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/zip/utils/ZipSalvager.java`
#### Snippet
```java
import org.apache.tika.utils.RereadableInputStream;

public class ZipSalvager {

    private static final Logger LOG = LoggerFactory.getLogger(ZipSalvager.class);
```

### UtilityClassWithoutPrivateConstructor
Class `PackageConstants` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/PackageConstants.java`
#### Snippet
```java
import org.apache.tika.mime.MediaType;

public class PackageConstants {


```

### UtilityClassWithoutPrivateConstructor
Class `CompressorConstants` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/CompressorConstants.java`
#### Snippet
```java
import org.apache.tika.mime.MediaType;

public class CompressorConstants {

    public static final MediaType BROTLI = MediaType.application("x-brotli");
```

### UtilityClassWithoutPrivateConstructor
Class `MIFExtractor` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/mif/MIFExtractor.java`
#### Snippet
```java
 * Helper Class to Parse and Extract Adobe MIF Files.
 */
public class MIFExtractor {

    private static final Pattern openTagPattern = Pattern.compile("<(\\S*).*");
```

### UtilityClassWithoutPrivateConstructor
Class `ContentAndMetadataExtractor` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/indesign/ContentAndMetadataExtractor.java`
#### Snippet
```java
 * Extractor for InDesign Content and Metadata.
 */
class ContentAndMetadataExtractor {

    private final static Attributes EMPTY_ATTRIBUTES = new AttributesImpl();
```

### UtilityClassWithoutPrivateConstructor
Class `BatchCommandLineBuilder` has only 'static' members, and lacks a 'private' constructor
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
 * is: tika-batch/src/main/resources/.../default-tika-batch-config.xml
 */
class BatchCommandLineBuilder {

    static Pattern JVM_OPTS_PATTERN = Pattern.compile("^(--?)J(.+)");
```

### UtilityClassWithoutPrivateConstructor
Class `TikaServerCli` has only 'static' members, and lacks a 'private' constructor
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerCli.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class TikaServerCli {

    private static final Logger LOG = LoggerFactory.getLogger(TikaServerCli.class);
```

### UtilityClassWithoutPrivateConstructor
Class `TikaServerProcess` has only 'static' members, and lacks a 'private' constructor
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
import org.apache.tika.utils.StringUtils;

public class TikaServerProcess {


```

### UtilityClassWithoutPrivateConstructor
Class `PDFDOMUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFDOMUtil.java`
#### Snippet
```java
import org.apache.pdfbox.cos.COSObject;

class PDFDOMUtil {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `PDMetadataExtractor` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
import org.apache.tika.utils.XMLReaderUtils;

public class PDMetadataExtractor {

    public static void extract(PDMetadata pdMetadata, Metadata metadata, ParseContext context) {
```

### UtilityClassWithoutPrivateConstructor
Class `EvalExceptionUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/util/EvalExceptionUtils.java`
#### Snippet
```java
import org.apache.tika.utils.ExceptionUtils;

public class EvalExceptionUtils {

    //these remove runtime info from the stacktraces so
```

### UtilityClassWithoutPrivateConstructor
Class `ContentTagParser` has only 'static' members, and lacks a 'private' constructor
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/util/ContentTagParser.java`
#### Snippet
```java
import org.apache.tika.utils.XMLReaderUtils;

public class ContentTagParser {

    private static final ParseContext EMPTY_PARSE_CONTEXT = new ParseContext();
```

### UtilityClassWithoutPrivateConstructor
Class `AnalyzerDeserializer` has only 'static' members, and lacks a 'private' constructor
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
import org.apache.lucene.analysis.util.TokenFilterFactory;

class AnalyzerDeserializer {


```

### UtilityClassWithoutPrivateConstructor
Class `BatchTopCommonTokenCounter` has only 'static' members, and lacks a 'private' constructor
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/BatchTopCommonTokenCounter.java`
#### Snippet
```java
 * and outputs common tokens files for each input table file in the output directory.
 */
public class BatchTopCommonTokenCounter {

    public static void main(String[] args) throws Exception {
```

### UtilityClassWithoutPrivateConstructor
Class `LeipzigHelper` has only 'static' members, and lacks a 'private' constructor
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/LeipzigHelper.java`
#### Snippet
```java
import java.util.regex.Pattern;

public class LeipzigHelper {

    static Map<String, List<Path>> getFiles(Path leipzigDir) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `TikaAsyncCLI` has only 'static' members, and lacks a 'private' constructor
in `tika-pipes/tika-async-cli/src/main/java/org/apache/tika/async/cli/TikaAsyncCLI.java`
#### Snippet
```java
import org.apache.tika.pipes.pipesiterator.PipesIterator;

public class TikaAsyncCLI {

    private static final long TIMEOUT_MS = 600_000;
```

### UtilityClassWithoutPrivateConstructor
Class `CTAKESUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
 * </p>
 */
public class CTAKESUtils {
    // UMLS username property
    private final static String CTAKES_UMLS_USER = "ctakes.umlsuser";
```

### UtilityClassWithoutPrivateConstructor
Class `MailUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailUtil.java`
#### Snippet
```java
import org.apache.tika.metadata.Property;

public class MailUtil {

    private final static Pattern EMAIL = Pattern.compile("(?i)([^<@\\s]+@[^@> ]+)");
```

### UtilityClassWithoutPrivateConstructor
Class `HttpClientUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-pipes/tika-httpclient-commons/src/main/java/org/apache/tika/client/HttpClientUtil.java`
#### Snippet
```java
import org.apache.http.util.EntityUtils;

public class HttpClientUtil {

    public static boolean postJson(HttpClient client, String url, String json)
```

### UtilityClassWithoutPrivateConstructor
Class `DurationFormatUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-batch/src/main/java/org/apache/tika/util/DurationFormatUtils.java`
#### Snippet
```java
 * so that we didn't have to add another dependency.
 */
public class DurationFormatUtils {

    public static String formatMillis(long duration) {
```

### UtilityClassWithoutPrivateConstructor
Class `PropsUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-batch/src/main/java/org/apache/tika/util/PropsUtil.java`
#### Snippet
```java
 * or if there is a parser error, the defaultMissing value will be returned.
 */
public class PropsUtil {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ClassLoaderUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-batch/src/main/java/org/apache/tika/util/ClassLoaderUtil.java`
#### Snippet
```java
package org.apache.tika.util;

public class ClassLoaderUtil {

    @SuppressWarnings("unchecked")
```

### UtilityClassWithoutPrivateConstructor
Class `MailDateParser` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
 * At some point, we should probably try joda or, heaven forfend, a pile of regexes.
 */
public class MailDateParser {

    //TIKA-1970 Mac Mail's format is GMT+1 so we need to check for hour only
```

### UtilityClassWithoutPrivateConstructor
Class `FSProperties` has only 'static' members, and lacks a 'private' constructor
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSProperties.java`
#### Snippet
```java
import org.apache.tika.metadata.Property;

public class FSProperties {
    private final static String TIKA_BATCH_FS_NAMESPACE = "tika_batch_fs";

```

### UtilityClassWithoutPrivateConstructor
Class `XMLDOMUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-batch/src/main/java/org/apache/tika/util/XMLDOMUtil.java`
#### Snippet
```java
import org.w3c.dom.Node;

public class XMLDOMUtil {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `FSUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java
 * reading from and writing to a file system (FS).
 */
public class FSUtil {

    private final static Pattern FILE_NAME_PATTERN =
```

### UtilityClassWithoutPrivateConstructor
Class `TikaToXMP` has only 'static' members, and lacks a 'private' constructor
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/TikaToXMP.java`
#### Snippet
```java
import org.apache.tika.parser.odf.OpenDocumentParser;

public class TikaToXMP {
    /**
     * Map from mimetype to converter class Must only be accessed through
```

### UtilityClassWithoutPrivateConstructor
Class `Language` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/Language.java`
#### Snippet
```java
import org.apache.tika.parser.ParseContext;

public class Language {
    public static void languageDetection() throws IOException {
        LanguageDetector detector = new OptimaizeLangDetector().loadModels();
```

### UtilityClassWithoutPrivateConstructor
Class `MediaTypeExample` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/MediaTypeExample.java`
#### Snippet
```java
import org.apache.tika.mime.MediaTypeRegistry;

public class MediaTypeExample {
    public static void describeMediaType() {
        MediaType type = MediaType.parse("text/plain; charset=UTF-8");
```

### UtilityClassWithoutPrivateConstructor
Class `DisplayMetInstance` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/DisplayMetInstance.java`
#### Snippet
```java
 * Grabs a PDF file from a URL and prints its {@link Metadata}
 */
public class DisplayMetInstance {
    public static Metadata getMet(URL url) throws IOException, SAXException, TikaException {
        Metadata met = new Metadata();
```

### UtilityClassWithoutPrivateConstructor
Class `TranscribeTranslateExample` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/TranscribeTranslateExample.java`
#### Snippet
```java
 *
 */
public class TranscribeTranslateExample {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ZipListFiles` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/ZipListFiles.java`
#### Snippet
```java
 * standard APIs.
 */
public class ZipListFiles {
    public static void main(String[] args) throws Exception {
        if (args.length > 0) {
```

### UtilityClassWithoutPrivateConstructor
Class `AdvancedTypeDetector` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/AdvancedTypeDetector.java`
#### Snippet
```java
import org.apache.tika.mime.MimeTypesFactory;

public class AdvancedTypeDetector {
    public static String detectWithCustomConfig(String name) throws Exception {
        String config = "/org/apache/tika/mime/tika-mimetypes.xml";
```

### UtilityClassWithoutPrivateConstructor
Class `MyFirstTika` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/MyFirstTika.java`
#### Snippet
```java
 * It also shows the "easy way" via {@link AutoDetectParser}
 */
public class MyFirstTika {
    public static void main(String[] args) throws Exception {
        String filename = args[0];
```

### UtilityClassWithoutPrivateConstructor
Class `DescribeMetadata` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/DescribeMetadata.java`
#### Snippet
```java
 * Print the supported Tika Metadata models and their fields.
 */
public class DescribeMetadata {
    public static void main(String[] args) throws Exception {
        TikaCLI.main(new String[]{"--list-met-models"});
```

### UtilityClassWithoutPrivateConstructor
Class `GrabPhoneNumbersExample` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java
 * from the tika-example directory.
 */
public class GrabPhoneNumbersExample {
    private static HashSet<String> phoneNumbers = new HashSet<>();
    private static int failedFiles, successfulFiles = 0;
```

### UtilityClassWithoutPrivateConstructor
Class `SpringExample` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/SpringExample.java`
#### Snippet
```java
import org.apache.tika.sax.WriteOutContentHandler;

public class SpringExample {
    public static void main(String[] args) throws Exception {
        ApplicationContext context = new ClassPathXmlApplicationContext(
```

### UtilityClassWithoutPrivateConstructor
Class `StandardsExtractionExample` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/StandardsExtractionExample.java`
#### Snippet
```java
 * </p>
 */
public class StandardsExtractionExample {
    private static HashSet<String> standardReferences = new HashSet<>();
    private static int failedFiles = 0;
```

### UtilityClassWithoutPrivateConstructor
Class `Pharmacy` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/Pharmacy.java`
#### Snippet
```java
import java.security.Key;

public class Pharmacy {
    private static Key key = null;

```

### UtilityClassWithoutPrivateConstructor
Class `SimpleTypeDetector` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/SimpleTypeDetector.java`
#### Snippet
```java
import org.apache.tika.Tika;

public class SimpleTypeDetector {
    public static void main(String[] args) throws Exception {
        Tika tika = new Tika();
```

### UtilityClassWithoutPrivateConstructor
Class `TIAParsingExample` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/TIAParsingExample.java`
#### Snippet
```java
import org.apache.tika.sax.TeeContentHandler;

public class TIAParsingExample {
    public static String parseToStringExample() throws Exception {
        File document = new File("example.doc");
```

### UtilityClassWithoutPrivateConstructor
Class `CustomMimeInfo` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/CustomMimeInfo.java`
#### Snippet
```java
import org.apache.tika.mime.MimeTypesFactory;

public class CustomMimeInfo {
    public static String customMimeInfo() throws Exception {
        String path = "file:///path/to/prescription-type.xml";
```

### UtilityClassWithoutPrivateConstructor
Class `SimpleTextExtractor` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/SimpleTextExtractor.java`
#### Snippet
```java
import org.apache.tika.Tika;

public class SimpleTextExtractor {
    public static void main(String[] args) throws Exception {
        // Create a Tika instance with the default configuration
```

### UtilityClassWithoutPrivateConstructor
Class `DumpTikaConfigExample` has only 'static' members, and lacks a 'private' constructor
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java
 * for your custom mime types.
 */
public class DumpTikaConfigExample {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `AutoPageNumberUtils` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/AutoPageNumberUtils.java`
#### Snippet
```java
 * or alpha-numeric symbols in line with Pages auto numbering formats.
 */
class AutoPageNumberUtils {

    private static final String[] ALPHABET =
```

### UtilityClassWithoutPrivateConstructor
Class `ImageUtil` has only 'static' members, and lacks a 'private' constructor
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/tess4j/ImageUtil.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class ImageUtil {
    private static final Logger LOG = LoggerFactory.getLogger(ImageUtil.class);

```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Casting `record` to `HwmfText.WmfExtTextOut` may produce `ClassCastException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WMFParser.java`
#### Snippet
```java
                }
                if (record.getWmfRecordType().equals(HwmfRecordType.extTextOut)) {
                    HwmfText.WmfExtTextOut textOut = (HwmfText.WmfExtTextOut) record;
                    xhtml.startElement("p");
                    xhtml.characters(textOut.getText(charset));
```

### DataFlowIssue
Casting `record` to `HwmfText.WmfTextOut` may produce `ClassCastException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WMFParser.java`
#### Snippet
```java
                    xhtml.endElement("p");
                } else if (record.getWmfRecordType().equals(HwmfRecordType.textOut)) {
                    HwmfText.WmfTextOut textOut = (HwmfText.WmfTextOut) record;
                    xhtml.startElement("p");
                    xhtml.characters(textOut.getText(charset));
```

### DataFlowIssue
Method invocation `getFirstFooterSubrange` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
        if (officeParserConfig.isIncludeHeadersAndFooters()) {
            // Do any footers, if present
            Range[] footers = new Range[]{headerFooter.getFirstFooterSubrange(),
                    headerFooter.getEvenFooterSubrange(), headerFooter.getOddFooterSubrange()};
            handleHeaderFooter(footers, "footer", document, pictures, pictureTable, xhtml);
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractListManager.java`
#### Snippet
```java
                numFmtStyle = 2;
            } else if ("decimal".equals(numFmt)) {
                numFmtStyle = 0;
            } else if ("upperLetter".equals(numFmt)) {
                numFmtStyle = 3;
```

### DataFlowIssue
Method invocation `getString` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
                                unicode.getExtendedRst().getPhoneticText().trim().length() > 0) ?
                                unicode.getExtendedRst().getPhoneticText() : "";
                        cellString = unicode.getString() + " " + phonetic;
                    } else {
                        cellString = unicode.getString();
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmSection.java`
#### Snippet
```java
            j++;
        }
        i = ba.length;
        if (i == 1) {
            return (char) ba[0];
```

### DataFlowIssue
Method invocation `getState` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxBlock.java`
#### Snippet
```java
        //use clone to avoid changing a cached or to be cached block
        {
            setState(chmPrevLzxBlock.getState().clone());
        }
    }
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFListManager.java`
#### Snippet
```java
                start = 0;
            } else {
                start = 1;
            }
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/MetadataExtractor.java`
#### Snippet
```java
                BigDecimal d = property.getDecimal();
                if (d == null) {
                    val = null;
                } else {
                    val = d.toPlainString();
```

### DataFlowIssue
Condition `pkg instanceof ZipPackage` is redundant and can be replaced with a null check
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSExtractorDecorator.java`
#### Snippet
```java
            }
            String zipPath = (docRef.startsWith("/") ? docRef.substring(1) : docRef);
            if (pkg instanceof ZipPackage) {
                try (InputStream stream = getZipStream(zipPath, pkg)) {
                    XMLReaderUtils.parseSAX(new CloseShieldInputStream(stream),
```

### DataFlowIssue
Argument `pkg` might be null
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
            if (type == null || !OOXMLParser.SUPPORTED_TYPES.contains(type)) {
                // Get the type, and ensure it's one we handle
                type = OPCPackageDetector.detectOfficeOpenXML(pkg);
            }

```

### DataFlowIssue
Argument `pkg` might be null
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
            OfficeParserConfig config = context.get(OfficeParserConfig.class);
            if (config.isUseSAXDocxExtractor()) {
                poiExtractor = trySXWPF(pkg);
            }
            if (poiExtractor == null) {
```

### DataFlowIssue
Argument `pkg` might be null
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
            }
            if (poiExtractor == null) {
                poiExtractor = tryXSLF(pkg, config.isUseSAXPptxExtractor());
            }
            if (type.equals(OOXMLParser.XPS)) {
```

### DataFlowIssue
Method invocation `setProgId` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
        }
        if (! StringUtils.isBlank(progId)) {
            embeddedPartMetadata.setProgId(progId);
        }

```

### DataFlowIssue
Method invocation `getFullName` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java
        attributes.addAttribute("", "class", "class", "CDATA", "embedded");
        attributes.addAttribute("", "id", "id", "CDATA", objectRelId);
        if (!StringUtils.isBlank(embeddedPartMetadata.getFullName())) {
            attributes.addAttribute("", "name", "name", "CDATA",
                    embeddedPartMetadata.getFullName());
```

### DataFlowIssue
Result of 'max' is the same as the first argument making the call meaningless
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/MSOneStorePackage.java`
#### Snippet
```java
        for (int i = 0; i < arr.length - 1; i += 2) {
            if (arr[i] == 0 && arr[i + 1] == 0) {
                firstNull = Math.max(i, 0);
                break;
            }
```

### DataFlowIssue
Dereference of `end` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObject.java`
#### Snippet
```java
            }

            if (end.type.getIntVal() != this.streamObjectType.getIntVal()) {
                throw new StreamObjectParseErrorException(index.get(), null,
                        "Unexpected the stream header end value " +
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/CellIDArray.java`
#### Snippet
```java
        if (cellIdArray.content != null) {
            for (CellID cellId : cellIdArray.content) {
                this.content.add(new CellID(cellId));
            }
        }
```

### DataFlowIssue
Method invocation `has` may produce `NullPointerException`
in `tika-langdetect/tika-langdetect-lingo24/src/main/java/org/apache/tika/langdetect/lingo24/Lingo24LangDetector.java`
#### Snippet
```java
            LOG.warn("problem detecting ", e);
        }
        if (element.has("success") && element.get("success").asText().equals("true")) {
            language = element.get("lang").asText();
        }
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `tika-pipes/tika-emitters/tika-emitter-gcs/src/main/java/org/apache/tika/pipes/emitter/gcs/GCSEmitter.java`
#### Snippet
```java
                        vals.length);
            }
            blobInfo.getMetadata().put(n, vals[0]);
        }
        storage.create(blobInfo, bytes);
```

### DataFlowIssue
Method invocation `getData` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java

        StorageIndexDataElementData storageIndexData =
                storageIndexDataElement.getData(StorageIndexDataElementData.class);
        ExGuid manifestMappingGuid =
                storageIndexData.storageIndexManifestMapping.manifestMappingExGuid;
```

### DataFlowIssue
Method invocation `set` will produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
        for (RevisionManifestRootDeclare kv : revisionData.revisionManifestRootDeclareList) {
            if (kv.rootExGuid.equals(new ExGuid(2, RootExGuid))) {
                rootExGuid.set(kv.objectExGuid);
                break;
            }
```

### DataFlowIssue
Method invocation `getData` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
                .orElse(null);
        StorageIndexDataElementData storageIndexData =
                storageIndexDataElement.getData(StorageIndexDataElementData.class);
        manifestMappingGuid.set(storageIndexData.storageIndexManifestMapping.manifestMappingExGuid);

```

### DataFlowIssue
Method invocation `getParameterTypes` may produce `NullPointerException`
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
        try {
            Method method = getMethod(object, input.readUTF());
            Object[] args = new Object[method.getParameterTypes().length];
            for (int i = 0; i < args.length; i++) {
                args[i] = readObject(loader);
```

### DataFlowIssue
Immutable object is modified
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
        }
        if (!extensions.contains(extension)) {
            extensions.add(extension);
        }
    }
```

### DataFlowIssue
Argument `input` might be null
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
                try (InputStream input = loader.getResourceAsStream(path)) {
                    jar.putNextEntry(new JarEntry(path));
                    IOUtils.copy(input, jar);
                }
            }
```

### DataFlowIssue
Method invocation `getId` may produce `NullPointerException`
in `tika-core/src/main/java/org/apache/tika/pipes/PipesServer.java`
#### Snippet
```java
            }
        } catch (OutOfMemoryError e) {
            handleOOM(t.getId(), e);
        } finally {
            synchronized (lock) {
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-core/src/main/java/org/apache/tika/utils/XMLReaderUtils.java`
#### Snippet
```java
            canReset = true;
        } catch (UnsupportedOperationException e) {
            canReset = false;
        }
        boolean hasSecurityManager = false;
```

### DataFlowIssue
Argument `value` might be null
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
            if (List.class.isAssignableFrom(getterTuple.singleParam)) {
                //this outputs even empty list elements, which I think is good.
                addList(param, doc, getterTuple, (List<String>) value);
            } else if (Map.class.isAssignableFrom(getterTuple.singleParam)) {
                //this outputs even empty lists, which I think is good.
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
        }
        Element entry = doc.createElement(name);
        entry.setAttribute("class", item.getClass().getCanonicalName());
        element.appendChild(entry);
        serializeParams(doc, element, item);
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
        } else {
            tmpVal = value;
            radix = 8;
        }

```

### DataFlowIssue
Method invocation `throwIfCauseOf` may produce `NullPointerException`
in `tika-core/src/main/java/org/apache/tika/parser/AutoDetectParser.java`
#### Snippet
```java
            } catch (SAXException e) {
                // Convert zip bomb exceptions to TikaExceptions
                sch.throwIfCauseOf(e);
                throw e;
            }
```

### DataFlowIssue
Method invocation `getOutputStream` may produce `NullPointerException`
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
                sendInput(process, stream);
            } else {
                process.getOutputStream().close();
            }

```

### DataFlowIssue
Method invocation `getInputStream` may produce `NullPointerException`
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
            }

            InputStream out = process.getInputStream();
            InputStream err = process.getErrorStream();

```

### DataFlowIssue
Method invocation `waitFor` may produce `NullPointerException`
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
        } finally {
            try {
                process.waitFor();
            } catch (InterruptedException ignore) {
            }
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java

                // Handle metadata merging / clashes
                metadata = mergeMetadata(metadata, lastMetadata, policy);

                // Abort if requested, with the exception if there was one
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
                loadErrorHandler = LoadErrorHandler.WARN;
            } else if (LoadErrorHandler.THROW.toString().equalsIgnoreCase(loadErrorHandleConfig)) {
                loadErrorHandler = LoadErrorHandler.THROW;
            } else if (LoadErrorHandler.IGNORE.toString().equalsIgnoreCase(loadErrorHandleConfig)) {
                loadErrorHandler = LoadErrorHandler.IGNORE;
```

### DataFlowIssue
Array access `arr[code]` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/MpegStream.java`
#### Snippet
```java
            }
        }
        return arr[code];
    }

```

### DataFlowIssue
Argument `is` might be null
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/OpenNLPDetector.java`
#### Snippet
```java
        try (InputStream is = OpenNLPDetector.class
                .getResourceAsStream("/opennlp-langdetect-20210413.bin")) {
            LANG_MODEL = new LanguageDetectorModel(is);
        }
    }
```

### DataFlowIssue
Dereference of `path.toFile().listFiles()` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/UnrarParser.java`
#### Snippet
```java
                               EmbeddedDocumentExtractor extractor, ParseContext context)
            throws IOException, SAXException {
        for (File f : path.toFile().listFiles()) {
            if (f.isDirectory()) {
                processDirectory(baseDir, f.toPath(), xhtml, extractor,
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            handlerType = BasicContentHandlerFactory.HANDLER_TYPE.BODY;
        } else if (type.equals(METADATA)) {
            handlerType = BasicContentHandlerFactory.HANDLER_TYPE.IGNORE;
        }
        return new BasicContentHandlerFactory(handlerType, -1);
```

### DataFlowIssue
Dereference of `dir.listFiles()` may produce `NullPointerException`
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
        // Find all the mimetypes in the directory
        Set<String> fileMimes = new HashSet<>();
        for (File mf : dir.listFiles()) {
            if (mf.isFile()) {
                BufferedReader r =
```

### DataFlowIssue
Dereference of `dir.toFile().listFiles()` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/renderer/pdf/mutool/MuPDFRenderer.java`
#### Snippet
```java
        //TODO -- fix this
        Matcher m = Pattern.compile("tika-mutool-render-(\\d+)\\.png").matcher("");
        for (File f : dir.toFile().listFiles()) {
            String n = f.getName();
            if (m.reset(n).find()) {
```

### DataFlowIssue
Argument `is` might be null
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerManager.java`
#### Snippet
```java
        try (InputStream is = AnalyzerManager.class.getClassLoader()
                .getResourceAsStream("lucene-analyzers.json")) {
            try (Reader reader = new InputStreamReader(is, StandardCharsets.UTF_8)) {
                map = AnalyzerDeserializer.buildAnalyzers(reader, maxTokens);
            }
```

### DataFlowIssue
Method invocation `getRenderResults` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        //the user wants to render twice (say, full color to display to users, but
        //grayscale for (notionally?) better OCR).
        PageBasedRenderResults results = (PageBasedRenderResults) renderingState.getRenderResults();
        if (results != null) {
            List<RenderResult> pageResults = results.getPage(getCurrentPageNo());
```

### DataFlowIssue
Argument `stream` might be null
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageIdentifier.java`
#### Snippet
```java
            try (InputStream stream = LanguageIdentifier.class
                    .getResourceAsStream(language + PROFILE_SUFFIX)) {
                BufferedReader reader = new BufferedReader(new InputStreamReader(stream, UTF_8));
                String line = reader.readLine();
                while (line != null) {
```

### DataFlowIssue
Argument `is` might be null
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                try (InputStream is = this.getClass()
                        .getResourceAsStream("/tika-eval-profiler-config.xml")) {
                    Files.copy(is, tmpBCConfig, StandardCopyOption.REPLACE_EXISTING);
                }
                argList.add("-bc");
```

### DataFlowIssue
Argument `is` might be null
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                try (InputStream is = this.getClass()
                        .getResourceAsStream("/tika-eval-comparison-config.xml")) {
                    Files.copy(is, tmpBCConfig, StandardCopyOption.REPLACE_EXISTING);
                }
                argList.add("-bc");
```

### DataFlowIssue
Argument `is` might be null
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                try (InputStream is = this.getClass()
                        .getResourceAsStream("/tika-eval-file-profiler-config.xml")) {
                    Files.copy(is, tmpBCConfig, StandardCopyOption.REPLACE_EXISTING);
                }
                argList.add("-bc");
```

### DataFlowIssue
Argument `inputDir` might be null
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractProfiler.java`
#### Snippet
```java
            fps = getPathsFromExtractCrawl(metadata, extracts);
        } else {
            fps = getPathsFromSrcCrawl(metadata, inputDir, extracts);
        }
        int containerId = ID.incrementAndGet();
```

### DataFlowIssue
Dereference of `metadataList` may produce `NullPointerException`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractProfiler.java`
#### Snippet
```java
        List<Integer> numAttachments = countAttachments(metadataList);
        int i = 0;
        for (Metadata m : metadataList) {
            ContentTags contentTags = getContent(fps, m);
            //the first file should have the same id as the container id
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/ExtractReader.java`
#### Snippet
```java
                metadataList = JsonMetadataList.fromJson(reader);
                if (alterMetadataList.equals(ALTER_METADATA_LIST.FIRST_ONLY) &&
                        metadataList.size() > 1) {
                    while (metadataList.size() > 1) {
                        metadataList.remove(metadataList.size() - 1);
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/io/ExtractReader.java`
#### Snippet
```java
                } else if (alterMetadataList
                        .equals(ALTER_METADATA_LIST.AS_IS.CONCATENATE_CONTENT_INTO_FIRST) &&
                        metadataList.size() > 1) {
                    StringBuilder sb = new StringBuilder();
                    Metadata containerMetadata = metadataList.get(0);
```

### DataFlowIssue
Dereference of `commonTokensDir.toFile().listFiles()` may produce `NullPointerException`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/CommonTokenOverlapCounter.java`
#### Snippet
```java
    private void execute(Path commonTokensDir) throws IOException {
        List<String> langs = new ArrayList<>();
        for (File f : commonTokensDir.toFile().listFiles()) {
            langs.add(f.getName());
        }
```

### DataFlowIssue
Dereference of `leipzigDir.toFile().listFiles()` may produce `NullPointerException`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/TrainTestSplit.java`
#### Snippet
```java
    private void execute(Path leipzigDir, Path outputDir) throws Exception {
        initOutDirs(outputDir);
        for (File f : leipzigDir.toFile().listFiles()) {
            if (f.isDirectory()) {
                continue;
```

### DataFlowIssue
Dereference of `leipzigDir.toFile().listFiles()` may produce `NullPointerException`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/tools/LeipzigHelper.java`
#### Snippet
```java

        Map<String, List<Path>> m = new TreeMap<>();
        for (File f : leipzigDir.toFile().listFiles()) {
            System.err.println(f);
            String lang = null;
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java

                if (matchIndex > -1 && !handledB.contains(matchIndex)) {
                    metadataB = metadataListB.get(matchIndex);
                    handledB.add(matchIndex);
                }
```

### DataFlowIssue
Argument `inputDir` might be null
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java

        } else {
            fpsA = getPathsFromSrcCrawl(metadata, inputDir, extractsA);
            fpsB = getPathsFromSrcCrawl(metadata, inputDir, extractsB);
        }
```

### DataFlowIssue
Argument `ResultsReporter.class.getResourceAsStream(internalPath)` might be null
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/reports/ResultsReporter.java`
#### Snippet
```java
        }
        Path tmp = Files.createTempFile("tmp-tika-reports", ".xml");
        Files.copy(ResultsReporter.class.getResourceAsStream(internalPath), tmp,
                StandardCopyOption.REPLACE_EXISTING);
        return tmp;
```

### DataFlowIssue
Argument `HtmlEncodingDetector.class .getResourceAsStream("StandardCharsets_unsupporte...` might be null
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlEncodingDetector.java`
#### Snippet
```java
        Set<String> unsupported = new HashSet<>();
        try (BufferedReader reader = new BufferedReader(new InputStreamReader(
                HtmlEncodingDetector.class
                        .getResourceAsStream("StandardCharsets_unsupported_by_IANA.txt"),
                StandardCharsets.UTF_8))) {
            String line = reader.readLine();
```

### DataFlowIssue
Method invocation `toURI` may produce `NullPointerException`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESUtils.java`
#### Snippet
```java
            URISyntaxException {
        // UMLS user ID and password.
        String aeDescriptorPath = CTAKESUtils.class.getResource(aeDescriptor).toURI().getPath();

        // get Resource Specifier from XML
```

### DataFlowIssue
Argument `string` might be null
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
            string = string.toUpperCase(Locale.US);
        }
        return Date.from(Instant.from(RFC_5322.parse(string, new ParsePosition(0))));
    }

```

### DataFlowIssue
Method invocation `getChildNodes` may produce `NullPointerException`
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
            }
            Node teiHeader = getFirstChild(root.getChildNodes(), "teiHeader");
            Node fileDesc = getFirstChild(teiHeader.getChildNodes(), "fileDesc");
            if (fileDesc != null) {
                parseFileDesc(fileDesc, metadata);
```

### DataFlowIssue
Method invocation `toFile` may produce `NullPointerException`
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java
            throws IOException {
        return getOutputPath(Paths.get(outputRoot.toURI()), initialRelativePath, handleExisting,
                suffix).toFile();
    }

```

### DataFlowIssue
Dereference of `ex` may produce `NullPointerException`
in `tika-pipes/tika-fetchers/tika-fetcher-s3/src/main/java/org/apache/tika/pipes/fetcher/s3/S3Fetcher.java`
#### Snippet
```java
            tries++;
        }
        throw ex;
    }

```

### DataFlowIssue
Argument `sourceStream` might be null
in `tika-parsers/tika-parsers-ml/tika-parser-advancedmedia-module/src/main/java/org/apache/tika/parser/recognition/tf/TensorflowImageRecParser.java`
#### Snippet
```java
                try (InputStream sourceStream = getClass().getResourceAsStream(SCRIPT_FILE_NAME)) {
                    try (OutputStream destStream = new FileOutputStream(scriptFile)) {
                        IOUtils.copy(sourceStream, destStream);
                    }
                }
```

### DataFlowIssue
Array access `keyParts[0]` may produce `NullPointerException`
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
        String[] keyParts = splitKey(name);

        String ns = registry.getNamespaceURI(keyParts[0]);
        if (ns != null) {
            try {
```

### DataFlowIssue
Array access `keyParts[0]` may produce `NullPointerException`
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
        String[] keyParts = splitKey(name);

        String ns = registry.getNamespaceURI(keyParts[0]);
        if (ns != null) {
            xmpData.deleteProperty(ns, keyParts[1]);
```

### DataFlowIssue
Array access `keyParts[0]` may produce `NullPointerException`
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
        String[] keyParts = splitKey(name);

        String ns = registry.getNamespaceURI(keyParts[0]);
        if (ns != null) {
            try {
```

### DataFlowIssue
Array access `keyParts[0]` may produce `NullPointerException`
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
        String[] keyParts = splitKey(name);

        String ns = registry.getNamespaceURI(keyParts[0]);
        if (ns != null) {
            try {
```

### DataFlowIssue
Array access `keyParts[0]` may produce `NullPointerException`
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
        String[] keyParts = splitKey(property.getName());

        String ns = registry.getNamespaceURI(keyParts[0]);
        if (ns != null) {
            try {
```

### DataFlowIssue
Array access `keyParts[0]` may produce `NullPointerException`
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
        String[] keyParts = splitKey(name);

        String ns = registry.getNamespaceURI(keyParts[0]);
        if (ns != null) {
            try {
```

### DataFlowIssue
Dereference of `versions` may produce `NullPointerException`
in `tika-example/src/main/java/org/apache/tika/example/RollbackSoftware.java`
#### Snippet
```java
            XHTMLContentHandler xhtml = new XHTMLContentHandler(handler, metadata);
            xhtml.startDocument();
            for (File v : versions) {
                if (isSymlink(v)) {
                    continue;
```

### DataFlowIssue
Argument `stream` might be null
in `tika-langdetect/tika-langdetect-test-commons/src/main/java/org/apache/tika/langdetect/LanguageDetectorTest.java`
#### Snippet
```java
        try (InputStream stream = LanguageDetectorTest.class
                .getResourceAsStream("/language-tests/" + language + ".test")) {
            copyAtMost(new InputStreamReader(stream, UTF_8), writer, limit);
        }
    }
```

### DataFlowIssue
Argument `is` might be null
in `tika-langdetect/tika-langdetect-test-commons/src/main/java/org/apache/tika/langdetect/LanguageDetectorTest.java`
#### Snippet
```java

        try (InputStream is = this.getClass().getResourceAsStream("language-codes.txt")) {
            List<String> lines = IOUtils.readLines(is, UTF_8);
            for (String line : lines) {
                line = line.trim();
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/TextAndCSVParser.java`
#### Snippet
```java
            metadata.set(Metadata.CONTENT_ENCODING, params.getCharset().name());
        }
        if (!mediaType.equals(MediaType.TEXT_PLAIN) && params.getDelimiter() != null) {
            if (CHAR_TO_STRING_DELIMITER_MAP.containsKey(params.getDelimiter())) {
                attrs.put(DELIMITER, CHAR_TO_STRING_DELIMITER_MAP.get(params.getDelimiter()));
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
                    // Statistical analysis of single byte non-ASCII charcters
                    // would probably help here.
                    confidence = 0;
                } else {
                    //   ASCII or ISO file?  It's probably not our encoding,
```

### DataFlowIssue
Variable is already assigned to this value
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetRecog_mbcs.java`
#### Snippet
```java
            //
            if (doubleByteCharCount < 20 * badCharCount) {
                confidence = 0;
                break detectBlock;
            }
```

### DataFlowIssue
Switch label `2` is unreachable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/BPGParser.java`
#### Snippet
```java
                metadata.set(Photoshop.COLOR_MODE, "RGB Colour");
                break;
            case 2:
                metadata.set(Photoshop.COLOR_MODE, "YCgCo Colour");
                break;
```

### DataFlowIssue
Switch label `3` is unreachable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/BPGParser.java`
#### Snippet
```java
                metadata.set(Photoshop.COLOR_MODE, "YCgCo Colour");
                break;
            case 3:
                metadata.set(Photoshop.COLOR_MODE, "YCbCrK Colour");
                break;
```

### DataFlowIssue
Dereference of `fetchEmitKeyIndices` may produce `NullPointerException`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-csv/src/main/java/org/apache/tika/pipes/pipesiterator/csv/CSVPipesIterator.java`
#### Snippet
```java
            HandlerConfig handlerConfig = getHandlerConfig();
            for (CSVRecord record : records) {
                String id = record.get(fetchEmitKeyIndices.idIndex);
                String fetchKey = record.get(fetchEmitKeyIndices.fetchKeyIndex);
                String emitKey = record.get(fetchEmitKeyIndices.emitKeyIndex);
```

### DataFlowIssue
Argument `bytes` might be null
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSType.java`
#### Snippet
```java
            //swallow
        }
        this.type = converttoInt(bytes);
        this.width = width;
        this.height = height;
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java

        if (currentMenuItemId != null && "sf:ct".equals(qName)) {
            menuItems.put(currentMenuItemId, attributes.getValue("sfa:s"));
        }

```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-apple-module/src/main/java/org/apache/tika/parser/iwork/NumbersContentHandler.java`
#### Snippet
```java
            }

            xhtml.element("td", menuItems.get(attributes.getValue("sfa:IDREF")));
            currentColumn++;
        }
```

### DataFlowIssue
Argument `config` might be null
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
            XHTMLContentHandler xhtml = new XHTMLContentHandler(handler, metadata);
            xhtml.startDocument();
            parse(tikaStream, tmpOCROutputFile, xhtml, metadata, parseContext, config);
            xhtml.endDocument();
        }
```

### DataFlowIssue
Method invocation `toURI` may produce `NullPointerException`
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-csv/src/test/java/TestCSVPipesIterator.java`
#### Snippet
```java

    private Path get(String testFileName) throws Exception {
        return Paths.get(TestCSVPipesIterator.class.getResource("/" + testFileName).toURI());
    }

```

### DataFlowIssue
Method invocation `toURI` may produce `NullPointerException`
in `tika-fuzzing/src/test/java/TestFuzzingCLI.java`
#### Snippet
```java
    public void testMock() throws Exception {
        //convert to actual unit test
        Path inputDir = Paths.get(getClass().getResource("/test-documents").toURI());
        Path outputDir = Files.createTempDirectory("tika-fuzzing-");
        String[] args = new String[]{"-i",
```

## RuleId[id=ParameterCanBeLocal]
### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcControlData.java`
#### Snippet
```java
    }

    private long unmarshalUInt32(byte[] data, long dest) throws ChmParsingException {
        assert (data != null && data.length > 0);
        if (4 > getDataRemained()) {
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcResetTable.java`
#### Snippet
```java
    }

    private long unmarshalUInt32(byte[] data, long dest) throws TikaException {
        ChmAssert.assertByteArrayNotNull(data);
        dest = (data[this.getCurrentPlace()] & 0xff) |
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmLzxcResetTable.java`
#### Snippet
```java
    }

    private long unmarshalUint64(byte[] data, long dest) throws TikaException {
        ChmAssert.assertByteArrayNotNull(data);
        byte[] temp = new byte[8];
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
    }

    private long unmarshalUInt32(byte[] data, int dataLenght, long dest) throws TikaException {
        ChmAssert.assertByteArrayNotNull(data);
        if (4 > dataLenght) {
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItspHeader.java`
#### Snippet
```java
    }

    private int unmarshalInt32(byte[] data, int dataLenght, int dest) throws TikaException {
        ChmAssert.assertByteArrayNotNull(data);
        if (4 > this.getDataRemained()) {
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmPmgiHeader.java`
#### Snippet
```java
    }

    private long unmarshalUInt32(byte[] data, long dest) throws ChmParsingException {
        ChmAssert.assertByteArrayNotNull(data);

```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
     * @throws TikaException
     */
    private long unmarshalUint64(byte[] data, long dest) throws TikaException {
        byte[] temp = new byte[8];
        int i, j;
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
    }

    private long unmarshalUInt32(byte[] data, long dest) throws TikaException {
        ChmAssert.assertByteArrayNotNull(data);
        if (4 > getDataRemained()) {
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmItsfHeader.java`
#### Snippet
```java
    }

    private int unmarshalInt32(byte[] data, int dest) throws TikaException {
        ChmAssert.assertByteArrayNotNull(data);

```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/util/DataElementUtils.java`
#### Snippet
```java
    public static List<ObjectGroupDataElementData> getDataObjectDataElementData(
            List<DataElement> dataElements, RevisionManifestDataElementData revisionData,
            AtomicReference<ExGuid> rootExGuid) throws TikaException {
        rootExGuid = null;

```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'getMimeTypes' is still used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractPOIFSExtractor.java`
#### Snippet
```java
     * @deprecated use {@link #embeddedDocumentUtil}
     */
    protected MimeTypes getMimeTypes() {
        return embeddedDocumentUtil.getMimeTypes();
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getChmBlockInfoInstance' is still used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/chm/ChmBlockInfo.java`
#### Snippet
```java

    @Deprecated
    public static ChmBlockInfo getChmBlockInfoInstance(DirectoryListingEntry dle, int bytesPerBlock,
                                                       ChmLzxcControlData clcd)
            throws ChmParsingException {
```

### DeprecatedIsStillUsed
Deprecated member 'getMimeType' is still used
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypes.java`
#### Snippet
```java
     * @deprecated Use {@link Tika#detect(String)} instead
     */
    public MimeType getMimeType(String name) {
        MimeType type = patterns.matches(name);
        if (type != null) {
```

### DeprecatedIsStillUsed
Deprecated member 'OverrideDetector' is still used
in `tika-core/src/main/java/org/apache/tika/detect/OverrideDetector.java`
#### Snippet
```java
 */
@Deprecated
public class OverrideDetector implements Detector {

    @Override
```

### DeprecatedIsStillUsed
Deprecated member 'withFallbacks' is still used
in `tika-core/src/main/java/org/apache/tika/parser/ParserDecorator.java`
#### Snippet
```java
     * @deprecated This has been replaced by {@link FallbackParser}
     */
    public static final Parser withFallbacks(final Collection<? extends Parser> parsers,
                                             final Set<MediaType> types) {
        // Delegate to the new FallbackParser for now, until people upgrade
```

### DeprecatedIsStillUsed
Deprecated member 'IMAGE_CREATOR_ID_WRONG_CASE' is still used
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated use {@link IPTC#IMAGE_CREATOR_ID}
     */
    String IMAGE_CREATOR_ID_WRONG_CASE =
            PREFIX_PLUS + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + "ImageCreatorId";
    /**
```

### DeprecatedIsStillUsed
Deprecated member 'COPYRIGHT_OWNER_ID_WRONG_CASE' is still used
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated use {@link IPTC#COPYRIGHT_OWNER_ID}
     */
    String COPYRIGHT_OWNER_ID_WRONG_CASE =
            PREFIX_PLUS + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + "CopyrightOwnerId";
    /**
```

### DeprecatedIsStillUsed
Deprecated member 'LICENSOR_ID_WRONG_CASE' is still used
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated use {@link IPTC#LICENSOR_ID}
     */
    String LICENSOR_ID_WRONG_CASE =
            PREFIX_PLUS + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + "LicensorId";
    /**
```

### DeprecatedIsStillUsed
Deprecated member 'IMAGE_SUPPLIER_ID_WRONG_CASE' is still used
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated use {@link IPTC#IMAGE_SUPPLIER_ID}
     */
    String IMAGE_SUPPLIER_ID_WRONG_CASE =
            PREFIX_PLUS + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + "ImageSupplierId";
    /**
```

### DeprecatedIsStillUsed
Deprecated member 'CommonsDigester' is still used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-digest-commons/src/main/java/org/apache/tika/parser/digestutils/CommonsDigester.java`
#### Snippet
```java
     * @deprecated use {@link #CommonsDigester(int, String)}
     */
    public CommonsDigester(int markLimit, DigestAlgorithm... algorithms) {
        super(buildDigesters(markLimit, algorithms));
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getMediaType' is still used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pkg-module/src/main/java/org/apache/tika/parser/pkg/PackageParser.java`
#### Snippet
```java

    @Deprecated
    static MediaType getMediaType(ArchiveInputStream stream) {
        if (stream instanceof JarArchiveInputStream) {
            return JAR;
```

### DeprecatedIsStillUsed
Deprecated member 'MetadataHandler' is still used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/MetadataHandler.java`
#### Snippet
```java
 * {@link ElementMetadataHandler} classes instead
 */
public class MetadataHandler extends DefaultHandler {

    private final Metadata metadata;
```

### DeprecatedIsStillUsed
Deprecated member 'PASSWORD' is still used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
     * @deprecated Supply a {@link PasswordProvider} on the {@link ParseContext} instead
     */
    public static final String PASSWORD = "org.apache.tika.parser.pdf.password";
    public static final MediaType MEDIA_TYPE = MediaType.application("pdf");
    /**
```

### DeprecatedIsStillUsed
Deprecated member 'TokenCounter' is still used
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/TokenCounter.java`
#### Snippet
```java
 */
@Deprecated
public class TokenCounter {


```

### DeprecatedIsStillUsed
Deprecated member 'HtmlParserMapper' is still used
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java
     * the HTML mapping. This class will be removed in Tika 1.0.
     */
    private class HtmlParserMapper implements HtmlMapper {
        public String mapSafeElement(String name) {
            return HtmlParser.this.mapSafeElement(name);
```

## RuleId[id=AbstractMethodCallInConstructor]
### AbstractMethodCallInConstructor
Call to 'abstract' method `loadDefaultModels()` during object construction
in `tika-core/src/main/java/org/apache/tika/detect/TrainedModelDetector.java`
#### Snippet
```java

    public TrainedModelDetector() {
        loadDefaultModels(getClass().getClassLoader());
    }

```

### AbstractMethodCallInConstructor
Call to 'abstract' method `getAdditionalNamespaces()` during object construction
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/AbstractConverter.java`
#### Snippet
```java
        meta = XMPMetaFactory.create();
        metadata = new Metadata();
        registerNamespaces(getAdditionalNamespaces());
    }

```

## RuleId[id=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrTransactionLog' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    long crcName;
    FileChunkReference fcrHashedChunkList;
    FileChunkReference fcrTransactionLog;
    FileChunkReference fcrFileNodeListRoot;
    FileChunkReference fcrFreeChunkList;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrDebugLogA' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    GUID guidDenyReadFileVersion;
    long grfDebugLogFlags;
    FileChunkReference fcrDebugLogA;
    FileChunkReference fcrDebugLogB;
    long buildNumberCreated;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'guidAncestor' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    long ignoredZeroC;
    long ignoredZeroD;
    GUID guidAncestor;
    long crcName;
    FileChunkReference fcrHashedChunkList;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'guidLegacyFileVersion' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    GUID guidFileType;
    GUID guidFile;
    GUID guidLegacyFileVersion;
    GUID guidFileFormat;
    long ffvLastCodeThatWroteToThisFile;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'guidFile' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java

    GUID guidFileType;
    GUID guidFile;
    GUID guidLegacyFileVersion;
    GUID guidFileFormat;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrLegacyFreeChunkList' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    long ffvOldestCodeThatMayReadThisFile;

    FileChunkReference fcrLegacyFreeChunkList;
    FileChunkReference fcrLegacyTransactionLog;
    long cTransactionsInLog;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrLegacyTransactionLog' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java

    FileChunkReference fcrLegacyFreeChunkList;
    FileChunkReference fcrLegacyTransactionLog;
    long cTransactionsInLog;
    long cbLegacyExpectedFileLength;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrFreeChunkList' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    FileChunkReference fcrTransactionLog;
    FileChunkReference fcrFileNodeListRoot;
    FileChunkReference fcrFreeChunkList;
    long cbExpectedFileLength;
    long cbFreeSpaceInFreeChunkList;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrDebugLogB' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    long grfDebugLogFlags;
    FileChunkReference fcrDebugLogA;
    FileChunkReference fcrDebugLogB;
    long buildNumberCreated;
    long buildNumberLastWroteToFile;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'guidFileType' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
class OneNoteHeader implements Serializable {

    GUID guidFileType;
    GUID guidFile;
    GUID guidLegacyFileVersion;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrFileNodeListRoot' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    FileChunkReference fcrHashedChunkList;
    FileChunkReference fcrTransactionLog;
    FileChunkReference fcrFileNodeListRoot;
    FileChunkReference fcrFreeChunkList;
    long cbExpectedFileLength;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'guidDenyReadFileVersion' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    GUID guidFileVersion;
    long nFileVersionGeneration;
    GUID guidDenyReadFileVersion;
    long grfDebugLogFlags;
    FileChunkReference fcrDebugLogA;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'guidFileFormat' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    GUID guidFile;
    GUID guidLegacyFileVersion;
    GUID guidFileFormat;
    long ffvLastCodeThatWroteToThisFile;
    long ffvOldestCodeThatHasWrittenToThisFile;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrHashedChunkList' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    GUID guidAncestor;
    long crcName;
    FileChunkReference fcrHashedChunkList;
    FileChunkReference fcrTransactionLog;
    FileChunkReference fcrFileNodeListRoot;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'guidFileVersion' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    long cbExpectedFileLength;
    long cbFreeSpaceInFreeChunkList;
    GUID guidFileVersion;
    long nFileVersionGeneration;
    GUID guidDenyReadFileVersion;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fcrLegacyFileNodeListRoot' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteHeader.java`
#### Snippet
```java
    long cbLegacyExpectedFileLength;
    long rgbPlaceholder;
    FileChunkReference fcrLegacyFileNodeListRoot;
    long cbLegacyFreeSpaceInFreeChunkList;
    long ignoredZeroA;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'credsProvider' in a Serializable class
in `tika-parsers/tika-parsers-ml/tika-transcribe-aws/src/main/java/org/apache/tika/parser/transcribe/aws/AmazonTranscribe.java`
#### Snippet
```java
    private String clientId;
    private String clientSecret; // Keys used for the API calls.
    private AWSStaticCredentialsProvider credsProvider;

    //https://docs.aws.amazon.com/transcribe/latest/dg/input.html
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'tag' in a Serializable class
in `tika-core/src/main/java/org/apache/tika/sax/TaggedSAXException.java`
#### Snippet
```java
     * The object reference used to tag the exception.
     */
    private final Object tag;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'parserState' in a Serializable class
in `tika-core/src/main/java/org/apache/tika/parser/RecursiveParserWrapper.java`
#### Snippet
```java

        private static final long serialVersionUID = 207648200464263337L;
        private final ParserState parserState;
        private String location = null;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'tmp' in a Serializable class
in `tika-core/src/main/java/org/apache/tika/embedder/ExternalEmbedder.java`
#### Snippet
```java
            "${METADATA_SERIALIZED}";
    private static final long serialVersionUID = -2828829275642475697L;
    private final TemporaryResources tmp = new TemporaryResources();
    /**
     * Media types supported by the external program.
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'tikaConfig' in a Serializable class
in `tika-core/src/main/java/org/apache/tika/extractor/EmbeddedDocumentUtil.java`
#### Snippet
```java
    private final EmbeddedDocumentExtractor embeddedDocumentExtractor;
    //these are lazily initialized and can be null
    private TikaConfig tikaConfig;
    private MimeTypes mimeTypes;
    private Detector detector;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'secondaryParser' in a Serializable class
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniser.java`
#### Snippet
```java
    private static AgePredicterLocal agePredictor;
    private static volatile boolean available = false;
    public Tika secondaryParser;
    private AgeRecogniserConfig config;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'config' in a Serializable class
in `tika-parsers/tika-parsers-ml/tika-age-recogniser/src/main/java/org/apache/tika/parser/recognition/AgeRecogniser.java`
#### Snippet
```java
    private static volatile boolean available = false;
    public Tika secondaryParser;
    private AgeRecogniserConfig config;

    public AgeRecogniser() {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'secondaryParser' in a Serializable class
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/NamedEntityParser.java`
#### Snippet
```java
    }

    public Tika secondaryParser;
    private List<NERecogniser> nerChain;
    private volatile boolean initialized = false;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'gazetteerClient' in a Serializable class
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoParser.java`
#### Snippet
```java

    private GeoParserConfig defaultConfig = new GeoParserConfig();
    private GeoGazetteerClient gazetteerClient;

    private boolean initialized;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'nameFinder' in a Serializable class
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/geo/GeoParser.java`
#### Snippet
```java
    private boolean initialized;
    private URL modelUrl;
    private NameFinderME nameFinder;
    private boolean available;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'classifier' in a Serializable class
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/sentiment/SentimentAnalysisParser.java`
#### Snippet
```java
            Collections.singleton(MediaType.application("sentiment"));
    private static final Logger LOG = LoggerFactory.getLogger(SentimentAnalysisParser.class);
    private SentimentME classifier;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'jsonWriter' in a Serializable class
in `tika-batch/src/main/java/org/apache/tika/batch/fs/StreamOutRPWFSConsumer.java`
#### Snippet
```java
    //be written straight to disk.
    private static class WriteoutRPWHandler extends AbstractRecursiveParserWrapperHandler {
        private final JsonStreamingSerializer jsonWriter;
        private final MetadataFilter metadataFilter;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'xhtml' in a Serializable class
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
     * The output content handler.
     */
    private XHTMLContentHandler xhtml;

    /**
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
Unexpected VM exception like 'java.lang.NullPointerException' might be ignored in a `catch` block
in `tika-core/src/main/java/org/apache/tika/detect/XmlRootExtractor.java`
#### Snippet
```java
        } catch (SecurityException e) {
            throw e;
        } catch (Exception e) {
            if (throwMalformed && (e instanceof CharConversionException
                    || e.getCause() instanceof CharConversionException)) {
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
     *
     * @param text the text from which the headers are extracted.
     * @return the list of headers found within the given text.
     */
    private static Map<Integer, String> findHeaders(String text) {
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
     * Get a list of links to help document this mime type
     *
     * @return an array of links (will never be null)
     */
    public List<URI> getLinks() {
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLWordAndPowerPointTextHandler.java`
#### Snippet
```java
        if (rubyBuffer.length() > 0) {
            if (concatenatePhoneticRuns) {
                bodyContentsHandler.run(currRunProperties, " (" + rubyBuffer.toString() + ")");
            }
            rubyBuffer.setLength(0);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-opensearch/src/main/java/org/apache/tika/pipes/reporters/opensearch/OpenSearchPipesReporter.java`
#### Snippet
```java
                throw new TikaConfigException("I regret I don't recognize '" +
                        include + "' in the include list. " +
                        "I recognize: " + sb.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-opensearch/src/main/java/org/apache/tika/pipes/reporters/opensearch/OpenSearchPipesReporter.java`
#### Snippet
```java
                throw new TikaConfigException("I regret I don't recognize '" +
                        exclude + "' in the exclude list. " +
                        "I recognize: " + sb.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-digest-commons/src/main/java/org/apache/tika/parser/digestutils/CommonsDigester.java`
#### Snippet
```java
            }
            throw new IllegalArgumentException(
                    "Couldn't match " + algoString + " with any of: " + sb.toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-digest-commons/src/main/java/org/apache/tika/parser/digestutils/CommonsDigester.java`
#### Snippet
```java
        String getMetadataKey() {
            return TikaCoreProperties.TIKA_META_PREFIX + "digest" +
                    TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + this.toString();
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
                Metadata m = metadataList.get(i);
                childSolrInputDocument
                        .setField(idField, emitKey + "-" + UUID.randomUUID().toString());
                addMetadataToSolrInputDocument(m, childSolrInputDocument, updateStrategy);
                children.add(childSolrInputDocument);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
                Metadata m = metadataList.get(i);
                childSolrInputDocument.setField(idField,
                        solrInputDocument.get(idField).getValue() + "-" + UUID.randomUUID().toString());
                addMetadataToSolrInputDocument(m, childSolrInputDocument, updateStrategy);
                docsToUpdate.add(childSolrInputDocument);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/DefaultZipContainerDetector.java`
#### Snippet
```java
                    if (LOG.isDebugEnabled()) {
                        LOG.debug("{} detected {}", zipDetector.getClass(),
                                type.toString());
                    }
                    //e.g. if OPCPackage has already been set
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java

    private void version() {
        System.out.println(new Tika().toString());
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            if (outputFile.exists()) {
                String fileName = FilenameUtils.getName(normalizedName);
                outputFile = new File(extractDir, UUID.randomUUID().toString() + "-" + fileName);
            }
            return outputFile;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/YandexTranslator.java`
#### Snippet
```java
                            targetLanguage +
                            "', JSON response from Lingo24 is not well formatted: " +
                            responseText.toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/JoshuaNetworkTranslator.java`
#### Snippet
```java
                            targetLanguage + "', JSON response " +
                            "from Joshua REST Server is not well formatted: " +
                            responseText.toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerWatchDog.java`
#### Snippet
```java
            argList.add("-numRestarts");
            argList.add(Integer.toString(numRestarts));
            LOG.debug("forked process commandline: " + argList.toString());
            builder.command(argList);
            //now overwrite with the specific server id
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaMimeTypes.java`
#### Snippet
```java
            }
            if (type.supertype != null) {
                text.append("  supertype: ").append(type.supertype.toString()).append("\n");
            }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/UnpackerResource.java`
#### Snippet
```java
            }
            if (zout.containsKey(normalizedName)) {
                return UUID.randomUUID().toString() + "-" + normalizedName;
            }
            return normalizedName;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/renderer/pdf/mutool/MuPDFRenderer.java`
#### Snippet
```java
        args.add(
                ProcessUtils.escapeCommandLine(
                        dir.toAbsolutePath().toString() + "/" + "tika-mutool-render-%d.png"));
        args.add(ProcessUtils.escapeCommandLine(pdf.toAbsolutePath().toString()));
        if (request != PageRangeRequest.RENDER_ALL) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java

    public static final String GREETING =
            "This is Tika Server (" + new Tika().toString() + "). Please PUT\n";
    private static final String META_PREFIX = "meta_";
    private static final Logger LOG = LoggerFactory.getLogger(TikaResource.class);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
        //    pdf:PDFVersion, pdfa:PDFVersion, pdf:PDFExtensionVersion
        metadata.set(PDF.PDF_VERSION, Float.toString(document.getDocument().getVersion()));
        metadata.add(TikaCoreProperties.FORMAT.getName(), MEDIA_TYPE.toString() + "; version=" +
                Float.toString(document.getDocument().getVersion()));

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
                                    baseVersion + " Adobe Extension Level " + el);
                            metadata.add(TikaCoreProperties.FORMAT.getName(),
                                    MEDIA_TYPE.toString() + "; version=\"" + baseVersion +
                                            " Adobe Extension Level " + el + "\"");
                        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
        if (!map.isObject()) {
            throw new IllegalArgumentException("Expecting a map with \"factory\" string and " +
                    "\"params\" map in tokenizer factory;" + " not: " + map.toString() + " in " +
                    analyzerName);
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
        if (!el.isArray()) {
            throw new IllegalArgumentException(
                    "Expecting array for charfilters, but got:" + el.toString() + " for " +
                            analyzerName);
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
                throw new IllegalArgumentException(
                        "Expecting a map with \"factory\" string and \"params\" map in char filter factory;" +
                                " not: " + filterMap.toString() + " in " + analyzerName);
            }
            JsonNode factoryEl = filterMap.get(FACTORY);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
        }
        if (!paramsEl.isObject()) {
            throw new IllegalArgumentException("Expecting map, not: " + paramsEl.toString());
        }
        Map<String, String> params = new HashMap<>();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
            if (value.isObject() || value.isArray() || value.isNull()) {
                throw new IllegalArgumentException(
                        "Expecting parameter to have primitive value: " + value.toString());
            }
            String v = e.getValue().asText();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
        if (!el.isArray()) {
            throw new IllegalArgumentException(
                    "Expecting array for tokenfilters, but got:" + el.toString() + " in " +
                            analyzerName);
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AnalyzerDeserializer.java`
#### Snippet
```java
                throw new IllegalArgumentException(
                        "Expecting a map with \"factory\" string and \"params\" map in token filter factory;" +
                                " not: " + filterMap.toString() + " in " + analyzerName);
            }
            JsonNode factoryEl = filterMap.get(FACTORY);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-cad-module/src/main/java/org/apache/tika/parser/dwg/DWGReadParser.java`
#### Snippet
```java
                    tmpFileOut.getCanonicalPath(), tmpFileIn.getCanonicalPath());
            ProcessBuilder pb = new ProcessBuilder().command(command);
            LOG.info("About to call DWGRead: " + command.toString());
            FileProcessResult fpr = ProcessUtils.execute(pb, dwgc.getDwgReadTimeout(), 10000, 10000);
            LOG.info("DWGRead Exit code is: " + fpr.getExitValue());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/XMLErrorLogUpdater.java`
#### Snippet
```java
        } catch (IOException e) {
            throw new RuntimeException(
                    "Problem reading: " + xmlLogFile.toAbsolutePath().toString());
        } finally {
            try {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
            } catch (IOException | SAXException e) {
                LOG.warn("Problem parsing html in {}; backing off to treat string as text",
                        evalFilePaths.getExtractFile().toAbsolutePath().toString(), e);

                return new ContentTags(s, true);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
            } catch (TikaException | IOException | SAXException e) {
                LOG.warn("Problem parsing xhtml in {}; backing off to html parser",
                        evalFilePaths.getExtractFile().toAbsolutePath().toString(), e);
                try {
                    ContentTags contentTags =
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/AbstractProfiler.java`
#### Snippet
```java
                } catch (IOException | SAXException e2) {
                    LOG.warn("Problem parsing html in {}; backing off to treat string as text",
                            evalFilePaths.getExtractFile().toAbsolutePath().toString(), e2);
                }
                return new ContentTags(s, true);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ctakes/CTAKESContentHandler.java`
#### Snippet
```java

            // analyze text
            jcas.setDocumentText(metaText.toString() + sb.toString());
            ae.process(jcas);

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
            }

            metAuthors.append(affilBuilder.toString());
            metAuthors.append(" ");
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java
            UUID uid = UUID.randomUUID();
            cand = FSUtil.resolveRelative(outputParent,
                    uid.toString() + fNameExt + "" + localSuffix);
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcess.java`
#### Snippet
```java
            //send over stdout wrapped in outputStreamWriter
            outputStreamWriter.println(
                    BATCH_CONSTANTS.BATCH_PROCESS_FATAL_MUST_RESTART.toString() + " >> " +
                            restartMsg);
            outputStreamWriter.flush();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-batch/src/main/java/org/apache/tika/batch/fs/strawman/StrawManTikaAppDriver.java`
#### Snippet
```java

            Path outputFile =
                    Paths.get(outputRoot.toAbsolutePath().toString(), relPath.toString() + suffix);
            try {
                Files.createDirectories(outputFile.getParent());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-xmp/src/main/java/org/apache/tika/xmp/convert/TikaToXMP.java`
#### Snippet
```java
                    throw new TikaException(
                            "TikaToXMP converter class cannot be instantiated for mimetype: " +
                                    type.toString(), e);
                }
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-example/src/main/java/org/apache/tika/example/ExtractEmbeddedFiles.java`
#### Snippet
```java
            Path outputFile = outputDir.resolve(name);
            if (Files.exists(outputFile)) {
                outputFile = outputDir.resolve(UUID.randomUUID().toString() + "-" + name);
            }
            Files.createDirectories(outputFile.getParent());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-example/src/main/java/org/apache/tika/example/DumpTikaConfigExample.java`
#### Snippet
```java

        if (writer instanceof StringWriter) {
            System.out.println(writer.toString());
        }
        writer.close();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3DBParser.java`
#### Snippet
```java
        if (tis != null) {
            Path dbFile = tis.getPath();
            return "jdbc:sqlite:" + dbFile.toAbsolutePath().toString();
        } else {
            //if not TikaInputStream, create own tmpResources.
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3DBParser.java`
#### Snippet
```java
            tmpFile = Files.createTempFile("tika-sqlite-tmp", "");
            Files.copy(is, tmpFile, StandardCopyOption.REPLACE_EXISTING);
            return "jdbc:sqlite:" + tmpFile.toAbsolutePath().toString();
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
            throw new TikaException(
                    "TesseractOCRParser bad exit value " + exitValue + " err msg: " +
                            errBuilder.toString());
        }
        for (String line : outBuilder.toString().split("[\r\n]+")) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRParser.java`
#### Snippet
```java
            throw new TikaException(
                    "TesseractOCRParser bad exit value " + exitValue + " err msg: " +
                            errBuilder.toString());
        }

```

## RuleId[id=AssignmentToForLoopParameter]
### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
                            Paragraph p = r.getParagraph(i);

                            i += handleParagraph(p, 0, r, document, FieldsDocumentPart.HEADER, pictures,
                                pictureTable, listManager, xhtml);
                        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java
        for (int i = 0; i < r.numParagraphs(); i++) {
            Paragraph p = r.getParagraph(i);
            i += handleParagraph(p, 0, r, document, FieldsDocumentPart.MAIN, pictures, pictureTable,
                    listManager, xhtml);
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/WordExtractor.java`
#### Snippet
```java

            if (cr.text().equals("\u0013")) {
                j += handleSpecialCharacterRuns(p, j, tas.isHeading(), pictures, xhtml);
            } else if (cr.text().startsWith("\b")) { //\u0008"
                // Floating Picture(s)
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteLegacyDumpStrings.java`
#### Snippet
```java

                    if (c2 == 0x00 && c1 >= 0x20) { // add this back? && c1 < 0x7F) {
                        ++i;
                        os.write(c1);
                    } else {
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `n`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/RDCAnalysisChunking.java`
#### Snippet
```java

                if (n == chunkStart) {
                    n = chunkStart + horizon;
                }

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `n`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/RDCAnalysisChunking.java`
#### Snippet
```java

                if (n + horizon > inputLength) {
                    n = chunkEndMax;
                    continue;
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `tika-core/src/main/java/org/apache/tika/mime/HexCoDec.java`
#### Snippet
```java
        for (int i = 0, j = 0; i < length; i++) {
            int bite = bites[startIndex++] & 0xff;
            result[j++] = HEX_CHARS[bite >> 4];
            result[j++] = HEX_CHARS[bite & 0xf];
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `tika-core/src/main/java/org/apache/tika/mime/HexCoDec.java`
#### Snippet
```java
            int bite = bites[startIndex++] & 0xff;
            result[j++] = HEX_CHARS[bite >> 4];
            result[j++] = HEX_CHARS[bite & 0xf];
        }
        return result;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
                if (value.charAt(i + 1) == '\\') {
                    decoded.write('\\');
                    i++;
                } else if (value.charAt(i + 1) == 'x') {
                    decoded.write(Integer.parseInt(value.substring(i + 2, i + 4), 16));
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
                } else if (value.charAt(i + 1) == 'x') {
                    decoded.write(Integer.parseInt(value.substring(i + 2, i + 4), 16));
                    i += 3;
                } else if (value.charAt(i + 1) == 'r') {
                    decoded.write('\r');
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
                } else if (value.charAt(i + 1) == 'r') {
                    decoded.write('\r');
                    i++;
                } else if (value.charAt(i + 1) == 'n') {
                    decoded.write('\n');
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
                } else if (value.charAt(i + 1) == 'n') {
                    decoded.write('\n');
                    i++;
                } else {
                    int j = i + 1;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-core/src/main/java/org/apache/tika/detect/MagicDetector.java`
#### Snippet
```java
                    }
                    decoded.write(Short.decode("0" + value.substring(i + 1, j)).byteValue());
                    i = j - 1;
                }
            } else {
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-audiovideo-module/src/main/java/org/apache/tika/parser/mp3/ID3v2Frame.java`
#### Snippet
```java
                    // Handle LE vs BE on low byte text
                    if (i + 2 < offset + length && data[i + 1] == 0 && data[i + 2] == 0) {
                        i++;
                    }
                    textStart = i + 2;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `n`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/hwp/HwpTextExtractorV5.java`
#### Snippet
```java
            if (n == 0) {
                xor = (byte) (srand.rand() & 0xFF);
                n = (int) ((srand.rand() & 0xF) + 1);
            }
            if (i >= 4) {
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `index`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/hwp/HwpTextExtractorV5.java`
#### Snippet
```java
                if (ch == 9) { // tab, INLINE
                    buf.append('\t');
                    index += 7;
                } else {
                    int type = HWP_CHAR_TYPE[ch];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `index`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/hwp/HwpTextExtractorV5.java`
#### Snippet
```java
                    int type = HWP_CHAR_TYPE[ch];
                    if (I == type) { // INLINE
                        index += 7;
                    } else if (X == type) { // EXTENDED
                        index += 7;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `index`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/hwp/HwpTextExtractorV5.java`
#### Snippet
```java
                        index += 7;
                    } else if (X == type) { // EXTENDED
                        index += 7;
                    } else if (C == type) { // CONTROL
                        buf.append(' ');
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
                if (i < args.length - 1 && !args[i + 1].startsWith("-")) {
                    v = args[i + 1];
                    i++;
                }
                jvmArgs.put(jvmArg, v);
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-app/src/main/java/org/apache/tika/cli/BatchCommandLineBuilder.java`
#### Snippet
```java
                if (i < args.length - 1 && !args[i + 1].startsWith("-")) {
                    v = args[i + 1];
                    i++;
                }
                commandLine.put(k, v);
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/AlphaIdeographFilterFactory.java`
#### Snippet
```java
                if (i < length - 1) {
                    cp = Character.toCodePoint(token[i], token[i + 1]);
                    i++;
                }
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
            if (args[i].equals("-create")) { // found -create option
                command = CREATE;
                profilename = args[++i];
                filename = args[++i];
                encoding = args[++i];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                command = CREATE;
                profilename = args[++i];
                filename = args[++i];
                encoding = args[++i];
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                profilename = args[++i];
                filename = args[++i];
                encoding = args[++i];
            }

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
            if (args[i].equals("-similarity")) { // found -similarity option
                command = SIMILARITY;
                filename = args[++i];
                filename2 = args[++i];
                encoding = args[++i];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                command = SIMILARITY;
                filename = args[++i];
                filename2 = args[++i];
                encoding = args[++i];
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                filename = args[++i];
                filename2 = args[++i];
                encoding = args[++i];
            }

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
            if (args[i].equals("-score")) { // found -Score option
                command = SCORE;
                profilename = args[++i];
                filename = args[++i];
                encoding = args[++i];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                command = SCORE;
                profilename = args[++i];
                filename = args[++i];
                encoding = args[++i];
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java
                profilename = args[++i];
                filename = args[++i];
                encoding = args[++i];
            }
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                    }
                    inputDir = argList.get(i + 1);
                    i++;
                    break;
                case "-extracts":
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                    }
                    extracts = argList.get(i + 1);
                    i++;
                    break;
                case "-alterExtract":
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                    }
                    alterExtract = argList.get(i + 1);
                    i++;
                    break;
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                    }
                    inputDir = argList.get(i + 1);
                    i++;
                    break;
                case "-extractsA":
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                    }
                    extractsA = argList.get(i + 1);
                    i++;
                    break;
                case "-alterExtract":
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/TikaEvalCLI.java`
#### Snippet
```java
                    }
                    alterExtract = argList.get(i + 1);
                    i++;
                    break;
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `att`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlHandler.java`
#### Snippet
```java
            if (normAttrName == null) {
                newAttributes.removeAttribute(att);
                att--;
            } else {
                // We have a remapped attribute name, so set it as it might have changed.
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `tika-batch/src/main/java/org/apache/tika/batch/BatchProcessDriverCLI.java`
#### Snippet
```java
                            "Must specify an integer after \"-maxRestarts\" arg.");
                }
                i++;
            } else {
                args.add(arg);
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `FSFileCounter` may be 'static'
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/fs/FileSystemPipesIterator.java`
#### Snippet
```java
        }

        private class FSFileCounter implements FileVisitor<Path> {

            private final AtomicLong count;
```

### InnerClassMayBeStatic
Inner class `FetchWorker` may be 'static'
in `tika-server/tika-server-client/src/main/java/org/apache/tika/server/client/TikaClientCLI.java`
#### Snippet
```java
    }

    private class FetchWorker implements Callable<Long> {
        private final ArrayBlockingQueue<FetchEmitTuple> queue;
        private final TikaClient client;
```

### InnerClassMayBeStatic
Inner class `TextOnlyPageDrawer` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/renderer/pdf/pdfbox/TextOnlyPDFRenderer.java`
#### Snippet
```java
    }

    private class TextOnlyPageDrawer extends PageDrawer {
        public TextOnlyPageDrawer(PageDrawerParameters parameters) throws IOException {
            super(parameters);
```

### InnerClassMayBeStatic
Inner class `VectorGraphicsOnlyDrawer` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/renderer/pdf/pdfbox/VectorGraphicsOnlyPDFRenderer.java`
#### Snippet
```java
    }

    private class VectorGraphicsOnlyDrawer extends PageDrawer {
        public VectorGraphicsOnlyDrawer(PageDrawerParameters parameters) throws IOException {
            super(parameters);
```

### InnerClassMayBeStatic
Inner class `NoTextPageDrawer` may be 'static'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/renderer/pdf/pdfbox/NoTextPDFRenderer.java`
#### Snippet
```java
    }

    private class NoTextPageDrawer extends PageDrawer {
        public NoTextPageDrawer(PageDrawerParameters parameters) throws IOException {
            super(parameters);
```

### InnerClassMayBeStatic
Inner class `Author` may be 'static'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
    }

    class Author {

        private String surName;
```

### InnerClassMayBeStatic
Inner class `FileAdder` may be 'static'
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/cli/FuzzingCLI.java`
#### Snippet
```java
    }

    private class FileAdder implements Callable<Integer> {
        private final PipesIterator pipesIterator;
        private final ArrayBlockingQueue<FetchEmitTuple> queue;
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
'Set' may not contain objects of type 'OneNotePropertyId'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteTreeWalker.java`
#### Snippet
```java
                dif.read(buf);
                propMap.put("dataUnicode16LE", new String(buf.array(), StandardCharsets.UTF_16LE));
                if (options.getUtf16PropertiesToPrint().contains(propertyValue.propertyId)) {
                    xhtml.startElement(P);
                    xhtml.characters((String) propMap.get("dataUnicode16LE"));
```

### SuspiciousMethodCalls
'Set' may not contain objects of type 'OneNotePropertyId'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteTreeWalker.java`
#### Snippet
```java
                dif.read(buf);
                propMap.put("dataUnicode16LE", new String(buf.array(), StandardCharsets.UTF_16LE));
                if (options.getUtf16PropertiesToPrint().contains(propertyValue.propertyId)) {
                    xhtml.startElement(P);
                    xhtml.characters((String) propMap.get("dataUnicode16LE"));
```

### SuspiciousMethodCalls
'Map' may not contain keys of type 'Class'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/StreamingDetectContext.java`
#### Snippet
```java

    public void remove(Class key) {
        context.remove(key);
    }
}
```

## RuleId[id=SetReplaceableByEnumSet]
### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/OneNoteTreeWalkerOptions.java`
#### Snippet
```java
    private boolean crawlAllFileNodesFromRoot = true;
    private boolean onlyLatestRevision = true;
    private Set<OneNotePropertyEnum> utf16PropertiesToPrint = new HashSet<>(
            Arrays.asList(OneNotePropertyEnum.ImageFilename, OneNotePropertyEnum.Author,
                    OneNotePropertyEnum.CachedTitleString));
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StreamObject.java`
#### Snippet
```java
     * Hash set contains the StreamObjectTypeHeaderStart type.
     */
    private static final Set<StreamObjectTypeHeaderStart> compoundTypes = new HashSet<>(
            Arrays.asList(StreamObjectTypeHeaderStart.DataElement,
                    StreamObjectTypeHeaderStart.Knowledge,
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesReporterBase.java`
#### Snippet
```java

    private final Set<PipesResult.STATUS> includes = new HashSet<>();
    private final Set<PipesResult.STATUS> excludes = new HashSet<>();

    private StatusFilter statusFilter;
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `tika-core/src/main/java/org/apache/tika/pipes/PipesReporterBase.java`
#### Snippet
```java
public abstract class PipesReporterBase extends PipesReporter implements Initializable {

    private final Set<PipesResult.STATUS> includes = new HashSet<>();
    private final Set<PipesResult.STATUS> excludes = new HashSet<>();

```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/FrictionlessPackageDetector.java`
#### Snippet
```java

    private static class Counter {
        private Set<PARTS> parts = new HashSet<>();
        void update(PARTS val) {
            parts.add(val);
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/TableInfo.java`
#### Snippet
```java
    private final String name;
    private final List<ColInfo> colInfos = new ArrayList<>();
    private final Set<Cols> colNames = new HashSet<>();

    private String prefix;
```

## RuleId[id=StringEqualsEmptyString]
### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/HSLFExtractor.java`
#### Snippet
```java
                List<HSLFTextRun> textRuns = htp.getTextRuns();
                String firstLine = removePBreak(textRuns.get(0).getRawText());
                boolean showBullet = (isBullet && (textRuns.size() > 1 || !"".equals(firstLine)));
                String paraTag = showBullet ? "li" : "p";

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/xml/WordMLParser.java`
#### Snippet
```java
                    //title appears to be the original file name
                    String title = attrs.getValue(MS_OFFICE_PROPERTIES_URN, "title");
                    if (title != null && !title.equals("")) {
                        if (src != null) {
                            //take the extention from the src and append it to the title
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/RunProperties.java`
#### Snippet
```java

    public void setUnderline(String underlineString) {
        if (underlineString == null || underlineString.equals("")) {
            underline = UnderlinePatterns.SINGLE;
        } else if (UnderlinePatterns.NONE.name().equals(underlineString)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-core/src/main/java/org/apache/tika/mime/MimeType.java`
#### Snippet
```java
         */
        private boolean isEmpty(String str) {
            return (str == null) || (str.equals(""));
        }

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-core/src/main/java/org/apache/tika/parser/external/ExternalParser.java`
#### Snippet
```java
                            consumed = true;
                            if (entry.getValue() != null &&
                                    !entry.getValue().equals("")) {
                                metadata.add(entry.getValue(), m.group(1));
                            } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
    public static void mustNotBeEmpty(String paramName, String paramValue)
            throws TikaConfigException {
        if (paramValue == null || paramValue.trim().equals("")) {
            throw new IllegalArgumentException(
                    "parameter '" + paramName + "' must be set in the config file");
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
        }

        if (config == null || config.trim().equals("")) {
            config = System.getenv("TIKA_CONFIG");
            if (!StringUtils.isBlank(config)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
        }

        if (config == null || config.trim().equals("")) {
            LOG.debug("loading tika config from defaults; no config file specified");
            this.serviceLoader = new ServiceLoader();
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-extended/tika-parser-scientific-module/src/main/java/org/apache/tika/parser/gdal/GDALParser.java`
#### Snippet
```java
                    Matcher m = p.matcher(line);
                    if (m.find()) {
                        if (metadataPatterns.get(p) != null && !metadataPatterns.get(p).equals("")) {
                            metadata.add(metadataPatterns.get(p), m.group(1));
                        } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java/org/apache/tika/detect/zip/StarOfficeDetector.java`
#### Snippet
```java
                }
            }
            if ("".equals(mediaTypeString) && "/".equals(fullPath)) {
                mediaType = BAU;
                throw StoppingEarlyException.INSTANCE;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dbf/DBFReader.java`
#### Snippet
```java
            StringBuilder sb = new StringBuilder();
            sb.append("application/x-dbf; ").append("format=").append(getFormat());
            if (!"".equals(type)) {
                sb.append("; type=").append(getType());
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentMetaParser.java`
#### Snippet
```java
        // Copy subject to description for OO2
        String odfSubject = metadata.get(OfficeOpenXMLCore.SUBJECT);
        if (odfSubject != null && !odfSubject.equals("") &&
                (metadata.get(TikaCoreProperties.DESCRIPTION) == null ||
                        metadata.get(TikaCoreProperties.DESCRIPTION).equals(""))) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/odf/OpenDocumentMetaParser.java`
#### Snippet
```java
        if (odfSubject != null && !odfSubject.equals("") &&
                (metadata.get(TikaCoreProperties.DESCRIPTION) == null ||
                        metadata.get(TikaCoreProperties.DESCRIPTION).equals(""))) {
            metadata.set(TikaCoreProperties.DESCRIPTION, odfSubject);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
    //lazily load the appropriate model
    private String getActualLangCode(String langCode) {
        if (langCode == null || "".equals(langCode)) {
            return defaultLangCode;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-eval/tika-eval-core/src/main/java/org/apache/tika/eval/core/tokens/CommonTokenCountManager.java`
#### Snippet
```java
        this.defaultLangCode = defaultLangCode;
        this.commonTokensDir = commonTokensDir;
        if (!"".equals(defaultLangCode)) {
            tryToLoad(defaultLangCode);
            //if you couldn't load it, make sure to add an empty
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        int nonNull = 0;
        for (String val : vals.keySet()) {
            if (val != null && !val.equals("")) {
                nonNull++;
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
            xhtml.startElement("ol", attrs);
            for (Map.Entry<String, String> e : vals.entrySet()) {
                if (e.getValue() == null || e.getValue().equals("")) {
                    continue;
                }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        }

        fileName = (fileName == null || "".equals(fileName.trim())) ? unicodeFileName : fileName;
        fileName = (fileName == null || "".equals(fileName.trim())) ? displayName : fileName;

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java

        fileName = (fileName == null || "".equals(fileName.trim())) ? unicodeFileName : fileName;
        fileName = (fileName == null || "".equals(fileName.trim())) ? displayName : fileName;

        // TODO: other metadata?
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageIdentifier.java`
#### Snippet
```java
     */
    public static boolean hasErrors() {
        return !errors.equals("");
    }

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/db/JDBCUtil.java`
#### Snippet
```java
            }
        } catch (NumberFormatException e) {
            if (!"".equals(value)) {
                LOG.warn("number format exception: {} : {}", colInfo.getName(), value);
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/nltk/NLTKNERecogniser.java`
#### Snippet
```java
            }

            if (restHostUrlStr == null || restHostUrlStr.equals("")) {
                this.restHostUrlStr = NLTK_REST_HOST;
            } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/grobid/GrobidNERecogniser.java`
#### Snippet
```java
            }

            if (restHostUrlStr == null || restHostUrlStr.equals("")) {
                this.restHostUrlStr = GROBID_REST_HOST;
            } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/grobid/GrobidNERecogniser.java`
#### Snippet
```java
                        }

                        if (!measurementString.toString().equals("")) {
                            measurementSet.add(measurementString.toString());
                        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/ner/grobid/GrobidNERecogniser.java`
#### Snippet
```java
                        }

                        if (!normalizedMeasurementString.toString().equals("")) {
                            normalizedMeasurementSet.add(normalizedMeasurementString.toString());
                        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/GrobidRESTParser.java`
#### Snippet
```java
        }

        if (restHostUrlStr == null || restHostUrlStr.equals("")) {
            this.restHostUrlStr = GROBID_REST_HOST;
        } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/GrobidRESTParser.java`
#### Snippet
```java
            response = WebClient.create(readRestUrl() + GROBID_ISALIVE_PATH).get();
            String resp = response.readEntity(String.class);
            return resp != null && !resp.equals("") && resp.startsWith("true");
        } catch (Exception e) {
            //swallow...can't run
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java

    private String printOrBlank(String val) {
        if (val != null && !val.equals("")) {
            return val + " ";
        } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSBatchProcessCLI.java`
#### Snippet
```java
        for (Option option : line.getOptions()) {
            String v = option.getValue();
            if (v == null || v.equals("")) {
                v = "true";
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSParser.java`
#### Snippet
```java

        metadata.set(Metadata.CONTENT_TYPE, ICNS_MIME_TYPE);
        if (!icon_details.toString().equals("")) {
            metadata.set("Icon count", String.valueOf(icons.size()));
            icon_details = new StringBuilder(icon_details.substring(2));
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSParser.java`
#### Snippet
```java
            metadata.set("Icon details", icon_details.toString());
        }
        if (!iconmask_details.toString().equals("")) {
            metadata.set("Masked icon count", String.valueOf(icon_masks.size()));
            iconmask_details = new StringBuilder(iconmask_details.substring(2));
```

## RuleId[id=ExcessiveRangeCheck]
### ExcessiveRangeCheck
Can be replaced with 'ports.length != 1'
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerConfig.java`
#### Snippet
```java
    public List<String> getForkedProcessArgs(String portString, String id) {
        int[] ports = getPorts(portString);
        if (ports.length > 1 || ports.length == 0) {
            throw new IllegalArgumentException(
                    "must specify one and only one port here:" + portString);
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'attr != null' covered by subsequent condition 'attr instanceof MAPIRtfAttribute'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/TNEFParser.java`
#### Snippet
```java
        // Recurse into the message body RTF
        MAPIAttribute attr = msg.getMessageMAPIAttribute(MAPIProperty.RTF_COMPRESSED);
        if (attr != null && attr instanceof MAPIRtfAttribute) {
            MAPIRtfAttribute rtf = (MAPIRtfAttribute) attr;
            handleEmbedded("message.rtf", "application/rtf", rtf.getData(), embeddedExtractor,
```

### ConditionCoveredByFurtherCondition
Condition 'cause != null' covered by subsequent condition 'cause instanceof TimeoutException'
in `tika-core/src/main/java/org/apache/tika/pipes/pipesiterator/fs/FileSystemPipesIterator.java`
#### Snippet
```java
        } catch (IOException e) {
            Throwable cause = e.getCause();
            if (cause != null && cause instanceof TimeoutException) {
                throw (TimeoutException) cause;
            }
```

### ConditionCoveredByFurtherCondition
Condition 'pipesIterator != null' covered by subsequent condition 'pipesIterator instanceof TotalCounter'
in `tika-core/src/main/java/org/apache/tika/pipes/async/AsyncProcessor.java`
#### Snippet
```java
            });
            //this is run in a daemon thread
            if (pipesIterator != null && (pipesIterator instanceof TotalCounter)) {
                LOG.debug("going to total counts");
                startCounter((TotalCounter) pipesIterator);
```

### ConditionCoveredByFurtherCondition
Condition 'response != null' covered by subsequent condition 'response instanceof CloseableHttpResponse'
in `tika-pipes/tika-emitters/tika-emitter-opensearch/src/main/java/org/apache/tika/pipes/emitter/opensearch/OpenSearchClient.java`
#### Snippet
```java
            }
        } finally {
            if (response != null && response instanceof CloseableHttpResponse) {
                ((CloseableHttpResponse)response).close();
            }
```

### ConditionCoveredByFurtherCondition
Condition 'response != null' covered by subsequent condition 'response instanceof CloseableHttpResponse'
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-opensearch/src/main/java/org/apache/tika/pipes/reporters/opensearch/OpenSearchClient.java`
#### Snippet
```java
            }
        } finally {
            if (response != null && response instanceof CloseableHttpResponse) {
                ((CloseableHttpResponse) response).close();
            }
```

### ConditionCoveredByFurtherCondition
Condition 'cosType != null' covered by subsequent condition 'cosType instanceof COSName'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
        } else if (kids instanceof COSObject) {
            COSBase cosType = ((COSObject) kids).getItem(COSName.TYPE);
            if (cosType != null && cosType instanceof COSName) {
                if ("OBJR".equals(((COSName) cosType).getName())) {
                    recurse(((COSObject) kids).getDictionaryObject(COSName.OBJ), currentPageRef,
```

### ConditionCoveredByFurtherCondition
Condition 'pageBase != null' covered by subsequent condition 'pageBase instanceof COSObject'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFMarkedContent2XHTML.java`
#### Snippet
```java
            COSBase pageBase = ((COSObject) kids).getItem(COSName.PG);

            if (pageBase != null && pageBase instanceof COSObject) {
                currentPageRef = new ObjectRef(((COSObject) pageBase).getObjectNumber(),
                        ((COSObject) pageBase).getGenerationNumber());
```

### ConditionCoveredByFurtherCondition
Condition 'obj != null' covered by subsequent condition 'obj instanceof PDField'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/AbstractPDF2XHTML.java`
#### Snippet
```java
        while (itr.hasNext()) {
            Object obj = itr.next();
            if (obj != null && obj instanceof PDField) {
                processAcroField((PDField) obj, 0);
            }
```

### ConditionCoveredByFurtherCondition
Condition '!resp.equals(...)' covered by subsequent condition 'resp.startsWith(...)'
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/GrobidRESTParser.java`
#### Snippet
```java
            response = WebClient.create(readRestUrl() + GROBID_ISALIVE_PATH).get();
            String resp = response.readEntity(String.class);
            return resp != null && !resp.equals("") && resp.startsWith("true");
        } catch (Exception e) {
            //swallow...can't run
```

## RuleId[id=MissingDeprecatedAnnotation]
### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/AbstractPOIFSExtractor.java`
#### Snippet
```java
     * @deprecated use {@link #embeddedDocumentUtil}
     */
    protected MimeTypes getMimeTypes() {
        return embeddedDocumentUtil.getMimeTypes();
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/POIFSContainerDetector.java`
#### Snippet
```java
     * second argument.
     */
    public static MediaType detect(Set<String> names) {
        return detect(names, null);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/sax/ToTextContentHandler.java`
#### Snippet
```java
     * @deprecated use {@link ToTextContentHandler#ToTextContentHandler(Writer)}
     */
    public ToTextContentHandler(OutputStream stream) {
        this(new OutputStreamWriter(stream, Charset.defaultCharset()));
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypes.java`
#### Snippet
```java
     * @deprecated Use {@link Tika#detect(String)} instead
     */
    public MimeType getMimeType(String name) {
        MimeType type = patterns.matches(name);
        if (type != null) {
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/mime/MimeTypes.java`
#### Snippet
```java
     * @deprecated Use {@link Tika#detect(File)} instead
     */
    public MimeType getMimeType(File file) throws MimeTypeException, IOException {
        return forName(new Tika(this).detect(file));
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/parser/AbstractParser.java`
#### Snippet
```java
     * Metadata, ParseContext)} method instead
     */
    public void parse(InputStream stream, ContentHandler handler, Metadata metadata)
            throws IOException, SAXException, TikaException {
        parse(stream, handler, metadata, new ParseContext());
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/parser/ParserDecorator.java`
#### Snippet
```java
     * @deprecated This has been replaced by {@link FallbackParser}
     */
    public static final Parser withFallbacks(final Collection<? extends Parser> parsers,
                                             final Set<MediaType> types) {
        // Delegate to the new FallbackParser for now, until people upgrade
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated use {@link IPTC#IMAGE_CREATOR_ID}
     */
    String IMAGE_CREATOR_ID_WRONG_CASE =
            PREFIX_PLUS + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + "ImageCreatorId";
    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated use {@link IPTC#COPYRIGHT_OWNER_ID}
     */
    String COPYRIGHT_OWNER_ID_WRONG_CASE =
            PREFIX_PLUS + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + "CopyrightOwnerId";
    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated
     */
    Property URGENCY = Photoshop.URGENCY;

    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated
     */
    Property SUPPLEMENTAL_CATEGORIES = Photoshop.SUPPLEMENTAL_CATEGORIES;

    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated use {@link IPTC#LICENSOR_ID}
     */
    String LICENSOR_ID_WRONG_CASE =
            PREFIX_PLUS + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + "LicensorId";
    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated
     */
    Property DIGITAL_SOURCE_FILE_TYPE = Property.internalText(
            PREFIX_IPTC_EXT + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER +
                    "DigitalSourcefileType");
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated
     */
    Property CATEGORY = Photoshop.CATEGORY;

    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/metadata/IPTC.java`
#### Snippet
```java
     * @deprecated use {@link IPTC#IMAGE_SUPPLIER_ID}
     */
    String IMAGE_SUPPLIER_ID_WRONG_CASE =
            PREFIX_PLUS + TikaCoreProperties.NAMESPACE_PREFIX_DELIMITER + "ImageSupplierId";
    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java
     * and the method signature is subject to change before Tika 2.0
     */
    public void parse(InputStream stream, ContentHandlerFactory handlers, Metadata metadata,
                      ParseContext context) throws IOException, SAXException, TikaException {
        parse(stream, null, handlers, metadata, context);
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-core/src/main/java/org/apache/tika/config/TikaConfig.java`
#### Snippet
```java
     * @deprecated Use the {@link #getParser()} method instead
     */
    public Parser getParser(MediaType mimeType) {
        return parser.getParsers().get(mimeType);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-digest-commons/src/main/java/org/apache/tika/parser/digestutils/CommonsDigester.java`
#### Snippet
```java
     * @deprecated use {@link #CommonsDigester(int, String)}
     */
    public CommonsDigester(int markLimit, DigestAlgorithm... algorithms) {
        super(buildDigesters(markLimit, algorithms));
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/detect/ole/MiscOLEDetector.java`
#### Snippet
```java
     * second argument.
     */
    protected static MediaType detect(Set<String> names) {
        return detect(names, null);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java/org/apache/tika/parser/xml/MetadataHandler.java`
#### Snippet
```java
 * {@link ElementMetadataHandler} classes instead
 */
public class MetadataHandler extends DefaultHandler {

    private final Metadata metadata;
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
     * @deprecated use {@link #getPDFParserConfig()}
     */
    public boolean isExtractAnnotationText() {
        return defaultConfig.isExtractAnnotationText();
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
     * @deprecated use {@link #getPDFParserConfig()}
     */
    public boolean isSortByPosition() {
        return defaultConfig.isSortByPosition();
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
     * @deprecated use {@link #getPDFParserConfig()}
     */
    public boolean isSuppressDuplicateOverlappingText() {
        return defaultConfig.isSuppressDuplicateOverlappingText();
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDFParser.java`
#### Snippet
```java
     * @deprecated Supply a {@link PasswordProvider} on the {@link ParseContext} instead
     */
    public static final String PASSWORD = "org.apache.tika.parser.pdf.password";
    public static final MediaType MEDIA_TYPE = MediaType.application("pdf");
    /**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java
     * the HTML mapping. This method will be removed in Tika 1.0.
     */
    protected boolean isDiscardElement(String name) {
        return DefaultHtmlMapper.INSTANCE.isDiscardElement(name);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java
     * the HTML mapping. This method will be removed in Tika 1.0.
     */
    protected String mapSafeElement(String name) {
        return DefaultHtmlMapper.INSTANCE.mapSafeElement(name);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java
     * the HTML mapping. This method will be removed in Tika 1.0.
     */
    public String mapSafeAttribute(String elementName, String attributeName) {
        return DefaultHtmlMapper.INSTANCE.mapSafeAttribute(elementName, attributeName);
    }
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java
     * the HTML mapping. This class will be removed in Tika 1.0.
     */
    private class HtmlParserMapper implements HtmlMapper {
        public String mapSafeElement(String name) {
            return HtmlParser.this.mapSafeElement(name);
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-example/src/main/java/org/apache/tika/example/PickBestTextEncodingParser.java`
#### Snippet
```java
 * @deprecated Currently not suitable for real use, more a demo / prototype!
 */
public class PickBestTextEncodingParser extends AbstractMultipleParser {
    /**
     * Serial version UID.
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
     * @deprecated This method will be removed in Apache Tika 1.0.
     */
    public void parse(InputStream stream, ContentHandler handler, Metadata metadata)
            throws IOException, SAXException, TikaException {
        parse(stream, handler, metadata, new ParseContext());
```

## RuleId[id=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-java7/src/main/java/org/apache/tika/filetypedetector/package-info.java`
#### Snippet
```java
 * Tika Java-7 FileTypeDetector implementations.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.filetypedetector;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.poi.hssf.eventusermodel` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ExcelExtractor.java`
#### Snippet
```java
 * is being processed.
 *
 * @see org.apache.poi.hssf.eventusermodel.HSSFListener
 * @see <a href="http://poi.apache.org/hssf/how-to.html#event_api">
 * POI Event API How To</a>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.poi.xssf.extractor` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XSSFBExcelExtractorDecorator.java`
#### Snippet
```java

    /**
     * @see org.apache.poi.xssf.extractor.XSSFBEventBasedExcelExtractor#getText()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.poi.xslf.extractor` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/OOXMLExtractorFactory.java`
#### Snippet
```java
            } else if (document instanceof XMLSlideShow) {
                extractor = new XSLFPowerPointExtractorDecorator(metadata, context,
                        (org.apache.poi.xslf.extractor.XSLFExtractor) poiExtractor);
            } else if (document instanceof XWPFDocument) {
                extractor = new XWPFWordExtractorDecorator(metadata, context,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.poi.xwpf.extractor` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/XWPFWordExtractorDecorator.java`
#### Snippet
```java

    /**
     * @see org.apache.poi.xwpf.extractor.XWPFWordExtractor#getText()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser.microsoft.ooxml` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java
    /**
     * @see
     * org.apache.tika.parser.microsoft.ooxml.OOXMLExtractor#getXHTML(ContentHandler, Metadata,
     * ParseContext)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser.microsoft.ooxml` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.parser.microsoft.ooxml.OOXMLExtractor#getDocument()
     */
    public POIXMLDocument getDocument() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser.microsoft.ooxml` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/AbstractOOXMLExtractor.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.parser.microsoft.ooxml.OOXMLExtractor#getMetadataExtractor()
     */
    public MetadataExtractor getMetadataExtractor() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj.basic` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/NodeObject.java`
#### Snippet
```java

public abstract class NodeObject extends StreamObject {
    public org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj.basic.ExGuid exGuid;
    public List<LeafNodeObject> intermediateNodeObjectList;
    public SignatureObject signature;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/RevisionStoreObject.java`
#### Snippet
```java
    public JCIDObject jcid;
    public PropertySetObject propertySet;
    public org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj.FileDataObject
            fileDataObject;
    public ExGUIDArray referencedObjectID;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj.basic` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/StorageIndexCellMapping.java`
#### Snippet
```java
 */
public class StorageIndexCellMapping extends StreamObject {
    public org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj.basic.CellID cellID;
    public ExGuid cellMappingExGuid;
    public SerialNumber cellMappingSerialNumber;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/ObjectGroupDataElementData.java`
#### Snippet
```java

public class ObjectGroupDataElementData extends DataElementData {
    public org.apache.tika.parser.microsoft.onenote.fsshttpb.streamobj.DataElementHash
            dataElementHash;
    public ObjectGroupDeclarations objectGroupDeclarations;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/CellIDArray.java`
#### Snippet
```java
     * @param content Specify the list of CellID.
     */
    public CellIDArray(long count, java.util.List<CellID> content) {
        this.count = count;
        this.content = content;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/CellID.java`
#### Snippet
```java
    @Override
    public List<Byte> serializeToByteList() throws IOException {
        java.util.List<Byte> byteList = new ArrayList<>();
        byteList.addAll(this.extendGUID1.serializeToByteList());
        byteList.addAll(this.extendGUID2.serializeToByteList());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/ExGUIDArray.java`
#### Snippet
```java
    }

    public java.util.List<ExGuid> getContent() {
        return content;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/ExGUIDArray.java`
#### Snippet
```java
    }

    public void setContent(java.util.List<ExGuid> content) {
        this.content = content;
        this.count.setDecodedValue(this.content.size());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/SimpleChunking.java`
#### Snippet
```java
    public List<LeafNodeObject> chunking() throws TikaException {
        int maxChunkSize = 1 * 1024 * 1024;
        java.util.List<LeafNodeObject> list = new ArrayList<>();
        LeafNodeObject.IntermediateNodeObjectBuilder builder =
                new LeafNodeObject.IntermediateNodeObjectBuilder();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/PrettyMetadataKeyComparator.java`
#### Snippet
```java
import org.apache.tika.metadata.TikaCoreProperties;

public class PrettyMetadataKeyComparator implements java.util.Comparator<String> {
    @Override
    public int compare(String s1, String s2) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/chunking/ZipFilesChunking.java`
#### Snippet
```java
    @Override
    public List<LeafNodeObject> chunking() throws TikaException, IOException {
        java.util.List<LeafNodeObject> list = new ArrayList<>();
        LeafNodeObject.IntermediateNodeObjectBuilder builder =
                new LeafNodeObject.IntermediateNodeObjectBuilder();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/extractor/microsoft/MSEmbeddedStreamTranslator.java`
#### Snippet
```java
    @Override
    public InputStream translate(InputStream inputStream, Metadata metadata) throws IOException {
        String contentType = metadata.get(org.apache.tika.metadata.HttpHeaders.CONTENT_TYPE);
        if ("application/vnd.openxmlformats-officedocument.oleObject".equals(contentType)) {
            UnsynchronizedByteArrayOutputStream bos = new UnsynchronizedByteArrayOutputStream();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/extractor/microsoft/MSEmbeddedStreamTranslator.java`
#### Snippet
```java
    @Override
    public boolean shouldTranslate(InputStream inputStream, Metadata metadata) throws IOException {
        String contentType = metadata.get(org.apache.tika.metadata.HttpHeaders.CONTENT_TYPE);
        if ("application/vnd.openxmlformats-officedocument.oleObject".equals(contentType)) {
            return true;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xmp-commons/src/main/java/org/apache/tika/parser/xmp/XMPPacketScanner.java`
#### Snippet
```java
    public boolean parse(InputStream in, OutputStream xmlOut) throws IOException {
        if (!in.markSupported()) {
            in = new java.io.BufferedInputStream(in);
        }
        boolean foundXMP = skipAfter(in, PACKET_HEADER);
```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/package-info.java`
#### Snippet
```java
 * Apache Tika.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika;

```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/io/package-info.java`
#### Snippet
```java
 * IO utilities.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.io;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/io/TikaInputStream.java`
#### Snippet
```java
 * to allow files and other resources and information to be associated with
 * the {@link InputStream} instance passed through the
 * {@link org.apache.tika.parser.Parser} interface and other similar APIs.
 * <p>
 * TikaInputStream instances can be created using the various static
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.language.translate` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/Tika.java`
#### Snippet
```java
     * @return The translated text. If translation is unavailable (client keys not set),  returns
     * the same text back.
     * @see org.apache.tika.language.translate.Translator
     */
    public String translate(String text, String sourceLanguage, String targetLanguage) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.language.translate` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/Tika.java`
#### Snippet
```java
     * @return The translated text. If translation is unavailable (client keys not set), returns
     * the same text back.
     * @see org.apache.tika.language.translate.Translator
     */
    public String translate(String text, String targetLanguage) {
```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/sax/package-info.java`
#### Snippet
```java
 * SAX utilities.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.sax;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.xml.sax` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/sax/TextContentHandler.java`
#### Snippet
```java

    @Override
    public void setDocumentLocator(org.xml.sax.Locator locator) {
        delegate.setDocumentLocator(locator);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/sax/xpath/package-info.java`
#### Snippet
```java
 * XPath utilities
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.sax.xpath;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.xml.sax` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/sax/DIFContentHandler.java`
#### Snippet
```java

    @Override
    public void setDocumentLocator(org.xml.sax.Locator locator) {
        delegate.setDocumentLocator(locator);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/fork/package-info.java`
#### Snippet
```java
 * Forked parser.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.fork;

```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/mime/package-info.java`
#### Snippet
```java
 * Media type information.
 */
@aQute.bnd.annotation.Version("1.2.0")
package org.apache.tika.mime;

```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/utils/package-info.java`
#### Snippet
```java
 * Utilities.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.utils;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.text` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/utils/DateUtils.java`
#### Snippet
```java
            try {
                return df.parse(dateString);
            } catch (java.text.ParseException e) {
                //swallow
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/config/package-info.java`
#### Snippet
```java
 * Tika configuration tools.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.config;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.config` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/utils/AnnotationUtils.java`
#### Snippet
```java
                if (!PARAM_INFO.containsKey(beanClass)) {
                    List<AccessibleObject> aObjs =
                            collectInfo(beanClass, org.apache.tika.config.Field.class);
                    List<ParamField> fields = new ArrayList<>(aObjs.size());

```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/detect/package-info.java`
#### Snippet
```java
 * Media type detection.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.detect;

```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/parser/package-info.java`
#### Snippet
```java
 * Tika parsers.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.parser;

```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/parser/external/package-info.java`
#### Snippet
```java
 * External parser process.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.parser.external;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/language/translate/Translator.java`
#### Snippet
```java
     * @return The translation result. If translation is unavailable, returns the same text back.
     * @throws TikaException       When there is an error translating.
     * @throws java.io.IOException
     * @since Tika 1.6
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/language/translate/Translator.java`
#### Snippet
```java
     * @return The translation result. If translation is unavailable, returns the same text back.
     * @throws TikaException       When there is an error translating.
     * @throws java.io.IOException
     * @since Tika 1.6
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/metadata/package-info.java`
#### Snippet
```java
 * Multi-valued metadata container, and set of constant metadata fields.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.metadata;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/parser/external2/ExternalParser.java`
#### Snippet
```java
    /**
     * If set to true, this will return the stderr in the metadata
     * via {@link org.apache.tika.metadata.ExternalProcess#STD_ERR}.
     * Default is <code>true</code>
     * @param returnStderr
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/parser/external2/ExternalParser.java`
#### Snippet
```java
    /**
     * If set to true, this will return the stdout in the metadata
     * via {@link org.apache.tika.metadata.ExternalProcess#STD_OUT}.
     * Default is <code>false</code> because this should normally
     * be handled by the outputParser
```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/exception/package-info.java`
#### Snippet
```java
 * Tika exception.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.exception;

```

### UnnecessaryFullyQualifiedName
Qualifier `aQute.bnd.annotation` is unnecessary, and can be replaced with an import
in `tika-core/src/main/java/org/apache/tika/extractor/package-info.java`
#### Snippet
```java
 * Extraction of component documents.
 */
@aQute.bnd.annotation.Version("1.0.0")
package org.apache.tika.extractor;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.xml.sax` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/extractor/EmbeddedDocumentExtractor.java`
#### Snippet
```java
     * @param metadata The metadata for the embedded resource
     * @param outputHtml Should we output HTML for this resource, or has the parser already done so?
     * @throws org.xml.sax.SAXException
     * @throws java.io.IOException
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-core/src/main/java/org/apache/tika/extractor/EmbeddedDocumentExtractor.java`
#### Snippet
```java
     * @param outputHtml Should we output HTML for this resource, or has the parser already done so?
     * @throws org.xml.sax.SAXException
     * @throws java.io.IOException
     */
    void parseEmbedded(
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-digest-commons/src/main/java/org/apache/tika/parser/digestutils/CommonsDigester.java`
#### Snippet
```java

/**
 * Implementation of {@link org.apache.tika.parser.DigestingParser.Digester}
 * that relies on commons.codec.digest.DigestUtils to calculate digest hashes.
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.xml.sax` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-miscoffice-module/src/main/java/org/apache/tika/parser/dif/DIFContentHandler.java`
#### Snippet
```java

    @Override
    public void setDocumentLocator(org.xml.sax.Locator locator) {
        delegate.setDocumentLocator(locator);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
    }

    private opennlp.tools.langdetect.Language[] predict(Map<String, MutableInt> ngramCounts) {
        String[] allGrams = new String[ngramCounts.size()];
        float[] counts = new float[ngramCounts.size()];
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
        }
        double[] eval = model.getMaxentModel().eval(allGrams, counts);
        opennlp.tools.langdetect.Language[] arr =
                new opennlp.tools.langdetect.Language[eval.length];
        for (int j = 0; j < eval.length; j++) {
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
        double[] eval = model.getMaxentModel().eval(allGrams, counts);
        opennlp.tools.langdetect.Language[] arr =
                new opennlp.tools.langdetect.Language[eval.length];
        for (int j = 0; j < eval.length; j++) {
            arr[j] = new opennlp.tools.langdetect.Language(model.getMaxentModel().getOutcome(j),
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
                new opennlp.tools.langdetect.Language[eval.length];
        for (int j = 0; j < eval.length; j++) {
            arr[j] = new opennlp.tools.langdetect.Language(model.getMaxentModel().getOutcome(j),
                    eval[j]);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java

    @Override
    public opennlp.tools.langdetect.Language[] predictLanguages(CharSequence content) {
        //list of the languages that received the highest
        //confidence over the last n chunk detections
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
        //list of the languages that received the highest
        //confidence over the last n chunk detections
        LinkedList<opennlp.tools.langdetect.Language[]> predictions = new LinkedList();
        int start = 0;//where to start the next chunk in codepoints
        opennlp.tools.langdetect.Language[] currPredictions = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
        LinkedList<opennlp.tools.langdetect.Language[]> predictions = new LinkedList();
        int start = 0;//where to start the next chunk in codepoints
        opennlp.tools.langdetect.Language[] currPredictions = null;
        //cache ngram counts across chunks
        Map<String, MutableInt> ngramCounts = new HashMap<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
     * @return
     */
    boolean seenEnough(LinkedList<opennlp.tools.langdetect.Language[]> predictionsQueue,
                       opennlp.tools.langdetect.Language[] newPredictions,
                       Map<String, MutableInt> ngramCounts) {
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
     */
    boolean seenEnough(LinkedList<opennlp.tools.langdetect.Language[]> predictionsQueue,
                       opennlp.tools.langdetect.Language[] newPredictions,
                       Map<String, MutableInt> ngramCounts) {

```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java
        //hasn't changed, and that the confidence in it
        //hasn't decreased
        for (opennlp.tools.langdetect.Language[] predictions : predictionsQueue) {
            if (lastLang == null) {
                lastLang = predictions[0].getLang();
```

### UnnecessaryFullyQualifiedName
Qualifier `opennlp.tools.langdetect` is unnecessary, and can be replaced with an import
in `tika-langdetect/tika-langdetect-opennlp/src/main/java/org/apache/tika/langdetect/opennlp/ProbingLanguageDetector.java`
#### Snippet
```java

    @Override
    public opennlp.tools.langdetect.Language predictLanguage(CharSequence content) {
        return predictLanguages(content)[0];
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.logging.log4j.core.config` is unnecessary, and can be replaced with an import
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
            version();
        } else if (arg.equals("-v") || arg.equals("--verbose")) {
            org.apache.logging.log4j.core.config.Configurator.setRootLevel(Level.DEBUG);
        } else if (arg.equals("-g") || arg.equals("--gui")) {
            pipeMode = false;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MosesTranslator.java`
#### Snippet
```java
     * translator.moses.properties file.
     *
     * @throws java.lang.AssertionError When the properties file is unreadable.
     */
    public MosesTranslator() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.language.translate` is unnecessary and can be removed
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MicrosoftTranslator.java`
#### Snippet
```java
     * @return The translated text. If translation is unavailable, returns the unchanged text.
     * @throws Exception
     * @see org.apache.tika.language.translate.Translator
     * @since Tika 1.6
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.language.translate` is unnecessary and can be removed
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/MicrosoftTranslator.java`
#### Snippet
```java
     * @return The translated text. If translation is unavailable, returns the unchanged text.
     * @throws Exception
     * @see org.apache.tika.language.translate.Translator
     * @since Tika 1.6
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/JoshuaNetworkTranslator.java`
#### Snippet
```java
     * {@link JoshuaNetworkTranslator#translate(String, String, String)}
     *
     * @see org.apache.tika.language.translate.Translator#translate(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-translate/src/main/java/org/apache/tika/language/translate/impl/JoshuaNetworkTranslator.java`
#### Snippet
```java
     * {@link JoshuaNetworkTranslator#translate(String, String, String)}
     *
     * @see org.apache.tika.language.translate.Translator#translate(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.commons.cli` is unnecessary, and can be replaced with an import
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerCli.java`
#### Snippet
```java
                return;
            }
        } catch (org.apache.commons.cli.ParseException e) {
            LOG.error("Can't parse stop arguments: ", e);
            System.exit(-1);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.server.core.writer` is unnecessary, and can be replaced with an import
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/TikaServerProcess.java`
#### Snippet
```java
    private static Collection<?> loadWriterServices() {
        return new ServiceLoader(TikaServerProcess.class.getClassLoader())
                .loadServiceProviders(org.apache.tika.server.core.writer.TikaServerWriter.class);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary and can be removed
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/MetadataResource.java`
#### Snippet
```java
     * value was found for the given metadata field, then the value of the field
     * is returned as part of a 200 OK response; otherwise a
     * {@link javax.ws.rs.core.Response.Status#BAD_REQUEST} is generated. If the stream
     * was successfully parsed but the specific metadata field was not found, then a
     * {@link javax.ws.rs.core.Response.Status#NOT_FOUND} is returned.
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary and can be removed
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/MetadataResource.java`
#### Snippet
```java
     * {@link javax.ws.rs.core.Response.Status#BAD_REQUEST} is generated. If the stream
     * was successfully parsed but the specific metadata field was not found, then a
     * {@link javax.ws.rs.core.Response.Status#NOT_FOUND} is returned.
     * <p/>
     * Note that this method handles multivalue fields and returns possibly more
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary and can be removed
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/MetadataResource.java`
#### Snippet
```java
     * @param info        info
     * @param field       the tika metadata field name
     * @return one of {@link javax.ws.rs.core.Response.Status#OK},
     * {@link javax.ws.rs.core.Response.Status#NOT_FOUND}, or
     * {@link javax.ws.rs.core.Response.Status#BAD_REQUEST}
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary and can be removed
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/MetadataResource.java`
#### Snippet
```java
     * @param field       the tika metadata field name
     * @return one of {@link javax.ws.rs.core.Response.Status#OK},
     * {@link javax.ws.rs.core.Response.Status#NOT_FOUND}, or
     * {@link javax.ws.rs.core.Response.Status#BAD_REQUEST}
     * @throws Exception
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary and can be removed
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/MetadataResource.java`
#### Snippet
```java
     * @return one of {@link javax.ws.rs.core.Response.Status#OK},
     * {@link javax.ws.rs.core.Response.Status#NOT_FOUND}, or
     * {@link javax.ws.rs.core.Response.Status#BAD_REQUEST}
     * @throws Exception
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary, and can be replaced with an import
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaDetectors.java`
#### Snippet
```java

    @GET
    @Produces(javax.ws.rs.core.MediaType.APPLICATION_JSON)
    public String getDetectorsJSON() throws IOException {
        Map<String, Object> details = new HashMap<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary, and can be replaced with an import
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java

        String contentTypeHeader = httpHeaders.getFirst(HttpHeaders.CONTENT_TYPE);
        javax.ws.rs.core.MediaType mediaType =
                (contentTypeHeader == null || "*/*".equals(contentTypeHeader)) ? null :
                        javax.ws.rs.core.MediaType.valueOf(contentTypeHeader);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary, and can be replaced with an import
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java
        javax.ws.rs.core.MediaType mediaType =
                (contentTypeHeader == null || "*/*".equals(contentTypeHeader)) ? null :
                        javax.ws.rs.core.MediaType.valueOf(contentTypeHeader);
        if (mediaType != null && "xml".equals(mediaType.getSubtype())) {
            mediaType = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.ws.rs.core` is unnecessary, and can be replaced with an import
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/resource/TikaResource.java`
#### Snippet
```java

        if (mediaType != null &&
                mediaType.equals(javax.ws.rs.core.MediaType.APPLICATION_OCTET_STREAM_TYPE)) {
            mediaType = null;
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * @param parseContext context to use to help create connectionString
     * @return connection string to be used by {@link #getConnection}.
     * @throws java.io.IOException
     */
    abstract protected String getConnectionString(InputStream stream, Metadata metadata,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * @param context  parsecontext that could be used in parameterizing the connection
     * @return connection
     * @throws java.io.IOException
     * @throws org.apache.tika.exception.TikaException
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.exception` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * @return connection
     * @throws java.io.IOException
     * @throws org.apache.tika.exception.TikaException
     */
    protected Connection getConnection(InputStream stream, Metadata metadata, ParseContext context)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * @param context    ParseContext to use (potentially) in decision about which tables to extract
     * @return
     * @throws java.sql.SQLException
     */
    abstract protected List<String> getTableNames(Connection connection, Metadata metadata,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     * Override this for any special handling of closing the connection.
     *
     * @throws java.sql.SQLException
     * @throws java.io.IOException
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/AbstractDBParser.java`
#### Snippet
```java
     *
     * @throws java.sql.SQLException
     * @throws java.io.IOException
     */
    protected void close() throws SQLException, IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.ccil.cowan.tagsoup` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/code/SourceCodeParser.java`
#### Snippet
```java
                Schema schema = context.get(Schema.class, HTML_SCHEMA);

                org.ccil.cowan.tagsoup.Parser parser = new org.ccil.cowan.tagsoup.Parser();
                parser.setProperty(org.ccil.cowan.tagsoup.Parser.schemaProperty, schema);
                parser.setContentHandler(handler);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.ccil.cowan.tagsoup` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/code/SourceCodeParser.java`
#### Snippet
```java
                Schema schema = context.get(Schema.class, HTML_SCHEMA);

                org.ccil.cowan.tagsoup.Parser parser = new org.ccil.cowan.tagsoup.Parser();
                parser.setProperty(org.ccil.cowan.tagsoup.Parser.schemaProperty, schema);
                parser.setContentHandler(handler);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.ccil.cowan.tagsoup` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-code-module/src/main/java/org/apache/tika/parser/code/SourceCodeParser.java`
#### Snippet
```java

                org.ccil.cowan.tagsoup.Parser parser = new org.ccil.cowan.tagsoup.Parser();
                parser.setProperty(org.ccil.cowan.tagsoup.Parser.schemaProperty, schema);
                parser.setContentHandler(handler);
                parser.parse(new InputSource(new StringReader(codeAsHtml)));
```

### UnnecessaryFullyQualifiedName
Qualifier `org.ccil.cowan.tagsoup` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java

            // Parse the HTML document
            org.ccil.cowan.tagsoup.Parser parser = new org.ccil.cowan.tagsoup.Parser();

            // Use schema from context or default
```

### UnnecessaryFullyQualifiedName
Qualifier `org.ccil.cowan.tagsoup` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java

            // Parse the HTML document
            org.ccil.cowan.tagsoup.Parser parser = new org.ccil.cowan.tagsoup.Parser();

            // Use schema from context or default
```

### UnnecessaryFullyQualifiedName
Qualifier `org.ccil.cowan.tagsoup` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java

            // TIKA-528: Reuse share schema to avoid heavy instantiation
            parser.setProperty(org.ccil.cowan.tagsoup.Parser.schemaProperty, schema);
            // TIKA-599: Shared schema is thread-safe only if bogons are ignored
            parser.setFeature(org.ccil.cowan.tagsoup.Parser.ignoreBogonsFeature, true);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.ccil.cowan.tagsoup` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-html-module/src/main/java/org/apache/tika/parser/html/HtmlParser.java`
#### Snippet
```java
            parser.setProperty(org.ccil.cowan.tagsoup.Parser.schemaProperty, schema);
            // TIKA-599: Shared schema is thread-safe only if bogons are ignored
            parser.setFeature(org.ccil.cowan.tagsoup.Parser.ignoreBogonsFeature, true);

            parser.setContentHandler(new XHTMLDowngradeHandler(
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-batch/src/main/java/org/apache/tika/batch/FileResource.java`
#### Snippet
```java
    /**
     * @return an InputStream for the FileResource
     * @throws java.io.IOException
     */
    public InputStream openInputStream() throws IOException;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.batch` is unnecessary and can be removed
in `tika-batch/src/main/java/org/apache/tika/batch/ConsumersManager.java`
#### Snippet
```java

    /**
     * {@link org.apache.tika.batch.BatchProcess} will throw an exception
     * if the ConsumersManager doesn't complete init() or shutdown()
     * within this amount of time.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSUtil.java`
#### Snippet
```java
     * @param suffix              suffix to add to files, can be null
     * @return output file or null if no output file should be created
     * @throws java.io.IOException
     * @see #getOutputPath(Path, String, HANDLE_EXISTING, String)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.batch` is unnecessary and can be removed
in `tika-batch/src/main/java/org/apache/tika/batch/FileResourceConsumer.java`
#### Snippet
```java
     * This offers another method for politely requesting
     * that a FileResourceConsumer stop processing
     * besides passing it {@link org.apache.tika.batch.PoisonFileResource}.
     */
    public void pleaseShutdown() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.batch.fs` is unnecessary and can be removed
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSOutputStreamFactory.java`
#### Snippet
```java

    /**
     * This tries to create a file based on the {@link org.apache.tika.batch.fs.FSUtil.HANDLE_EXISTING}
     * value that was passed in during initialization.
     * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSOutputStreamFactory.java`
#### Snippet
```java
     *                 else NullPointerException will be thrown!
     * @return OutputStream
     * @throws java.io.IOException, NullPointerException
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.batch.builders` is unnecessary and can be removed
in `tika-batch/src/main/java/org/apache/tika/batch/builders/ObjectFromDOMAndQueueBuilder.java`
#### Snippet
```java

/**
 * Same as {@link org.apache.tika.batch.builders.ObjectFromDOMAndQueueBuilder},
 * but this is for objects that require access to the shared queue.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#get(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#get(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * is removed
     *
     * @see org.apache.tika.metadata.Metadata#remove(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * is removed
     *
     * @see org.apache.tika.metadata.Metadata#remove(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * properties that use a registered prefix are stored in the XMP.
     *
     * @see org.apache.tika.metadata.Metadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * properties that use a registered prefix are stored in the XMP.
     *
     * @see org.apache.tika.metadata.Metadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * properties that use a registered prefix are stored in the XMP.
     *
     * @see org.apache.tika.metadata.Metadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * method, which replaces any existing value
     *
     * @see org.apache.tika.metadata.Metadata#add(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * method, which replaces any existing value
     *
     * @see org.apache.tika.metadata.Metadata#add(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * method, which replaces any existing value
     *
     * @see org.apache.tika.metadata.Metadata#add(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#get(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#get(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#isMultiValued(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#isMultiValued(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#process(org.apache.tika.metadata.Metadata,
     * java.lang.String)
     * But the mimetype is retrieved from the metadata map.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#process(org.apache.tika.metadata.Metadata,
     * java.lang.String)
     * But the mimetype is retrieved from the metadata map.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
    /**
     * @see org.apache.tika.xmp.XMPMetadata#process(org.apache.tika.metadata.Metadata,
     * java.lang.String)
     * But the mimetype is retrieved from the metadata map.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * Serializes the XMP data in compact form without packet wrapper
     *
     * @see org.apache.tika.metadata.Metadata#toString()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#get(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#get(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * contain a namespace prefix of a registered namespace.
     *
     * @see org.apache.tika.metadata.Metadata#get(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * contain a namespace prefix of a registered namespace.
     *
     * @see org.apache.tika.metadata.Metadata#get(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * Checks if the named property is an array.
     *
     * @see org.apache.tika.metadata.Metadata#isMultiValued(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * Checks if the named property is an array.
     *
     * @see org.apache.tika.metadata.Metadata#isMultiValued(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * properties that use a registered prefix are stored in the XMP.
     *
     * @see org.apache.tika.metadata.Metadata#set(org.apache.tika.metadata.Property,
     * java.lang.String[])
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * properties that use a registered prefix are stored in the XMP.
     *
     * @see org.apache.tika.metadata.Metadata#set(org.apache.tika.metadata.Property,
     * java.lang.String[])
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     *
     * @see org.apache.tika.metadata.Metadata#set(org.apache.tika.metadata.Property,
     * java.lang.String[])
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * It will set all simple and array properties that have QName keys in registered namespaces.
     *
     * @see org.apache.tika.metadata.Metadata#setAll(java.util.Properties)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * It will set all simple and array properties that have QName keys in registered namespaces.
     *
     * @see org.apache.tika.metadata.Metadata#setAll(java.util.Properties)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#set(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * are of simple type. The given name must contain a namespace prefix of a registered namespace.
     *
     * @see org.apache.tika.metadata.Metadata#getValues(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java
     * are of simple type. The given name must contain a namespace prefix of a registered namespace.
     *
     * @see org.apache.tika.metadata.Metadata#getValues(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#getValues(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#getValues(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.xmp` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#remove(java.lang.String)
     */
    public void remove(Property property) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `tika-xmp/src/main/java/org/apache/tika/xmp/XMPMetadata.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.xmp.XMPMetadata#remove(java.lang.String)
     */
    public void remove(Property property) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.sax` is unnecessary and can be removed
in `tika-example/src/main/java/org/apache/tika/example/GrabPhoneNumbersExample.java`
#### Snippet
```java

/**
 * Class to demonstrate how to use the {@link org.apache.tika.sax.PhoneExtractingContentHandler}
 * to get a list of all of the phone numbers from every file in a directory.
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser` is unnecessary and can be removed
in `tika-example/src/main/java/org/apache/tika/example/ParsingExample.java`
#### Snippet
```java
     * <p>
     * The "content" format is determined by the ContentHandlerFactory, and
     * the content is stored in {@link org.apache.tika.parser.RecursiveParserWrapper#TIKA_CONTENT}
     * <p>
     * The drawback to the RecursiveParserWrapper is that it caches metadata and contents
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser` is unnecessary and can be removed
in `tika-example/src/main/java/org/apache/tika/example/ParsingExample.java`
#### Snippet
```java
    /**
     * If you don't want content from embedded documents, send in
     * a {@link org.apache.tika.parser.ParseContext} that does contains a
     * {@link EmptyParser}.
     *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata.serialization` is unnecessary and can be removed
in `tika-example/src/main/java/org/apache/tika/example/ParsingExample.java`
#### Snippet
```java
    /**
     * We include a simple JSON serializer for a list of metadata with
     * {@link org.apache.tika.metadata.serialization.JsonMetadataList}.
     * That class also includes a deserializer to convert from JSON
     * back to a List<Metadata>.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary and can be removed
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3TableReader.java`
#### Snippet
```java
     * @param handler
     * @param context
     * @throws java.sql.SQLException
     * @throws java.io.IOException
     * @throws org.xml.sax.SAXException
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3TableReader.java`
#### Snippet
```java
     * @param context
     * @throws java.sql.SQLException
     * @throws java.io.IOException
     * @throws org.xml.sax.SAXException
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.xml.sax` is unnecessary and can be removed
in `tika-parsers/tika-parsers-extended/tika-parser-sqlite3-module/src/main/java/org/apache/tika/parser/sqlite3/SQLite3TableReader.java`
#### Snippet
```java
     * @throws java.sql.SQLException
     * @throws java.io.IOException
     * @throws org.xml.sax.SAXException
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-example/src/main/java/org/apache/tika/example/RecentFiles.java`
#### Snippet
```java
            searcher = new IndexSearcher(reader);
            GregorianCalendar gc =
                    new java.util.GregorianCalendar(TimeZone.getDefault(), Locale.getDefault());
            gc.setTime(new Date());
            String nowDateTime = ISO8601.format(gc);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-example/src/main/java/org/apache/tika/example/RecentFiles.java`
#### Snippet
```java
            gc.setTime(new Date());
            String nowDateTime = ISO8601.format(gc);
            gc.add(java.util.GregorianCalendar.MINUTE, -5);
            String fiveMinsAgo = ISO8601.format(gc);
            TermRangeQuery query = new TermRangeQuery(TikaCoreProperties.CREATED.getName(),
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.fuzzing` is unnecessary and can be removed
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/AutoDetectTransformer.java`
#### Snippet
```java

    public AutoDetectTransformer() {
        this(DEFAULT_LOADER.loadServiceProviders(org.apache.tika.fuzzing.Transformer.class));
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
     * @stable ICU 3.4
     * @see java.nio.charset.Charset
     * @see java.io.InputStreamReader
     */
    public String getName() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
     * @stable ICU 3.4
     */
    public String getString(int maxLength) throws java.io.IOException {
        String result = null;
        if (fInputStream != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/txt/CharsetMatch.java`
#### Snippet
```java
     * @stable ICU 3.4
     */
    public String getString() throws java.io.IOException {
        return getString(-1);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.commons.csv` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/TextAndCSVParser.java`
#### Snippet
```java
        XHTMLContentHandler xhtmlContentHandler = new XHTMLContentHandler(handler, metadata);
        int totalRows = 0;
        try (org.apache.commons.csv.CSVParser commonsParser = new org.apache.commons.csv.CSVParser(
                reader, csvFormat)) {
            xhtmlContentHandler.startDocument();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.commons.csv` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/csv/TextAndCSVParser.java`
#### Snippet
```java
        XHTMLContentHandler xhtmlContentHandler = new XHTMLContentHandler(handler, metadata);
        int totalRows = 0;
        try (org.apache.commons.csv.CSVParser commonsParser = new org.apache.commons.csv.CSVParser(
                reader, csvFormat)) {
            xhtmlContentHandler.startDocument();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java

    /**
     * @see org.apache.tika.parser.Parser#parse(java.io.InputStream,
     * org.xml.sax.ContentHandler, org.apache.tika.metadata.Metadata,
     * org.apache.tika.parser.ParseContext)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.xml.sax` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
    /**
     * @see org.apache.tika.parser.Parser#parse(java.io.InputStream,
     * org.xml.sax.ContentHandler, org.apache.tika.metadata.Metadata,
     * org.apache.tika.parser.ParseContext)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.metadata` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
    /**
     * @see org.apache.tika.parser.Parser#parse(java.io.InputStream,
     * org.xml.sax.ContentHandler, org.apache.tika.metadata.Metadata,
     * org.apache.tika.parser.ParseContext)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.tika.parser` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-text-module/src/main/java/org/apache/tika/parser/strings/Latin1StringsParser.java`
#### Snippet
```java
     * @see org.apache.tika.parser.Parser#parse(java.io.InputStream,
     * org.xml.sax.ContentHandler, org.apache.tika.metadata.Metadata,
     * org.apache.tika.parser.ParseContext)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
     * @return The output stream.
     */
    protected java.io.OutputStream getOutput() {
        return output;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSParser.java`
#### Snippet
```java
        }
        IOUtils.read(stream, header, 0, 4); //Extract image size/length of bytes in file
        int image_length = java.nio.ByteBuffer.wrap(header).getInt();
        image_length -= 8;//for the bytes read so far
        if (image_length > MAX_IMAGE_LENGTH_BYTES) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio` is unnecessary, and can be replaced with an import
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ICNSParser.java`
#### Snippet
```java
            tempByteArray[2] = full_file[offset + 6];
            tempByteArray[3] = full_file[offset + 7];
            int icon_length = java.nio.ByteBuffer.wrap(tempByteArray).getInt();
            if (icon_length <= 0) {
                break;
```

### UnnecessaryFullyQualifiedName
Qualifier `com.drew.metadata` is unnecessary and can be removed
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/main/java/org/apache/tika/parser/image/ImageMetadataExtractor.java`
#### Snippet
```java
     * Copies extracted tags to tika metadata using registered handlers.
     *
     * @param directories Metadata Extractor {@link com.drew.metadata.Directory} instances.
     * @throws MetadataException This method does not handle exceptions from Metadata Extractor
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `tika-batch/src/main/java/org/apache/tika/batch/builders/BatchProcessBuilder.java`
#### Snippet
```java
     * @param runtimeAttributes incoming runtime attributes
     * @return batch process
     * @throws java.io.IOException
     */
    public BatchProcess build(InputStream is, Map<String, String> runtimeAttributes)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `tika-langdetect/tika-langdetect-optimaize/src/main/java/org/apache/tika/langdetect/optimaize/OptimaizeLangDetector.java`
#### Snippet
```java
     * @return the detected list of languages
     * @throws IllegalStateException if no models have been loaded with
     *                               {@link #loadModels() } or {@link #loadModels(java.util.Set) }
     */
    @Override
```

## RuleId[id=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/InputStreamResource.java`
#### Snippet
```java
            // returning exception causes deadlock
            // return e;
            e.printStackTrace();
            m = -1;
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
            initializeParserAndLoader();
        } catch (Throwable t) {
            t.printStackTrace();
            System.err.flush();
            try {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
                output.flush();
            } catch (IOException e) {
                e.printStackTrace();
                System.err.flush();
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkServer.java`
#### Snippet
```java
            }
        } catch (Throwable t) {
            t.printStackTrace();
        }
        System.err.flush();
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
            ok = true;
        } catch (Throwable t) {
            t.printStackTrace();
            throw t;
        } finally {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/cli/TikaCLI.java`
#### Snippet
```java
                }
            } catch (MimeTypeException e) {
                e.printStackTrace();
            }
            return ".bin";
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
            dialog.setVisible(true);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
            panel.add(new JScrollPane(editor), name);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-app/src/main/java/org/apache/tika/gui/TikaGUI.java`
#### Snippet
```java
                }
            } catch (IOException exception) {
                exception.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-eval/tika-eval-app/src/main/java/org/apache/tika/eval/app/ExtractComparer.java`
#### Snippet
```java
                metadataListA = extractReader.loadExtract(fpsA.getExtractFile());
            } catch (ExtractReaderException e) {
                e.printStackTrace();
                extractExceptionA = e.getType();
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-batch/src/main/java/org/apache/tika/batch/fs/FSBatchProcessCLI.java`
#### Snippet
```java
            cli.execute(args);
        } catch (Throwable t) {
            t.printStackTrace();
            LOG.error(MarkerFactory.getMarker("FATAL"),
                    "Fatal exception from FSBatchProcessCLI: {}", t.getMessage(), t);
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/TranscribeTranslateExample.java`
#### Snippet
```java
                result = translator.translate(text, "en-US");
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/InterruptableParsingExample.java`
#### Snippet
```java
        } catch (SAXException | TikaException | IOException e) {
            // something went wrong with parsing...
            e.printStackTrace();
        }
        return false;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-example/src/main/java/org/apache/tika/example/ExtractEmbeddedFiles.java`
#### Snippet
```java
                            .getExtension();
                } catch (MimeTypeException e) {
                    e.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/cli/FuzzOne.java`
#### Snippet
```java
                return;
            } catch (TikaException e) {
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/cli/FuzzingCLI.java`
#### Snippet
```java
                    FileUtils.deleteDirectory(cwd.toFile());
                } catch (IOException e) {
                    e.printStackTrace();
                    LOG.warn("Couldn't delete " + cwd.toAbsolutePath(), e);
                }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/cli/FuzzingCLI.java`
#### Snippet
```java
                    LOG.info("Finished thread {} threads of {}", finished, totalThreads);
                } catch (InterruptedException | ExecutionException e) {
                    e.printStackTrace();
                    break;
                }
```

## RuleId[id=RegExpSingleCharAlternation]
### RegExpSingleCharAlternation
Single character alternation in RegExp
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
            "\\)?((\\s?(?<separator>\\/)\\s?)(\\w{1,64}+\\s)*\\(?" + "(?<secondOrganization>[A-Z" +
            "]\\w{1,64}+)" +
            "\\)?)?" + REGEX_STANDARD_TYPE + "?" + "(-|\\s)?" + REGEX_IDENTIFIER;

    // Regular expression to match the standard organization within a string
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
    // Regular expression to match the alphanumeric identifier of the standard
    private static final String REGEX_IDENTIFIER =
            "(?<identifier>([0-9]{3,64}+|([A-Z]{1,64}+(-|_|\\.)?[0-9]{2,64}+))((-|_|\\.)" +
                    "?[A-Z0-9]{1,64}+){0,64}+)";

```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `tika-core/src/main/java/org/apache/tika/sax/StandardsText.java`
#### Snippet
```java
    // Regular expression to match the alphanumeric identifier of the standard
    private static final String REGEX_IDENTIFIER =
            "(?<identifier>([0-9]{3,64}+|([A-Z]{1,64}+(-|_|\\.)?[0-9]{2,64}+))((-|_|\\.)" +
                    "?[A-Z0-9]{1,64}+){0,64}+)";

```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-ocr-module/src/main/java/org/apache/tika/parser/ocr/TesseractOCRConfig.java`
#### Snippet
```java
            // First, make sure it conforms to the correct syntax
            if (!lang.matches(
                    "([a-zA-Z]{3}(_[a-zA-Z]{3,4}){0,2})|script(/|\\\\)[A-Z][a-zA-Z_]+")) {
                invalidLangs.add(lang + " (invalid syntax)");
            } else {
```

## RuleId[id=CovariantEquals]
### CovariantEquals
`equals()` should take 'Object' as its argument
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/TextExtractor.java`
#### Snippet
```java

    // NOTE: s must be ascii alpha only
    private boolean equals(String s) {
        if (pendingControlCount != s.length()) {
            return false;
```

## RuleId[id=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
        public void add(MethodTuple tuple) {
            Set<MethodTuple> set = tuples.get(tuple.name);
            if (set == null) {
                set = new HashSet<>();
                tuples.put(tuple.name, set);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `tika-core/src/main/java/org/apache/tika/parser/RegexCaptureParser.java`
#### Snippet
```java
                        String val = m.group(1);
                        Set<String> vals = keyVals.get(e.getKey());
                        if (vals == null) {
                            vals = new LinkedHashSet<>();
                            keyVals.put(e.getKey(), vals);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `tika-core/src/main/java/org/apache/tika/renderer/PageBasedRenderResults.java`
#### Snippet
```java
        if (page != null) {
            List<RenderResult> pageResults = results.get(page);
            if (pageResults == null) {
                pageResults = new ArrayList<>();
                results.put(page, pageResults);
```

## RuleId[id=Convert2Lambda]
### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `tika-pipes/tika-pipes-reporters/tika-pipes-reporter-fs-status/src/main/java/org/apache/tika/pipes/reporters/fs/FileSystemStatusReporter.java`
#### Snippet
```java
                .disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS)
                .build();
        reporterThread = new Thread(new Runnable() {
            @Override
            public void run() {
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/ooxml/xps/XPSPageContentHandler.java`
#### Snippet
```java
                row.add(glyphRun);
                rows.add(row);
                continue;
            } else {
                boolean addedNewRow = false;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-serialization/src/main/java/org/apache/tika/metadata/serialization/JsonMetadata.java`
#### Snippet
```java
            String[] vals = metadata.getValues(n);
            if (vals.length == 0) {
                continue;
            } else if (vals.length == 1) {
                jsonGenerator.writeStringField(n, vals[0]);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-core/src/main/java/org/apache/tika/config/TikaConfigSerializer.java`
#### Snippet
```java
                LOG.warn("no getter for setter non-primitive: {} in {}", e.getKey(),
                        object.getClass());
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java
            } else if (Arrays.deepEquals(oldVals, newVals)) {
                // Metadata is the same, nothing to do
                continue;
            } else {
                switch (policy) {
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-core/src/main/java/org/apache/tika/parser/multiple/AbstractMultipleParser.java`
#### Snippet
```java
                        }

                        continue;
                }
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-pipes/tika-emitters/tika-emitter-solr/src/main/java/org/apache/tika/pipes/emitter/solr/SolrEmitter.java`
#### Snippet
```java
            String[] vals = metadata.getValues(n);
            if (vals.length == 0) {
                continue;
            } else if (vals.length == 1) {
                if (updateStrategy == UpdateStrategy.ADD) {
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                        val_next = (read < value.length) ? value[read++] : 0x00;
                    }
                    continue;
                } else {
                    while (((val_next >= (byte) 0x30) && (val_next <= (byte) 0x39))
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                            if (val_next == TB) {
                                value[--read] = CT;
                                continue;
                            }
                        } else if (tmp_line.toString().toLowerCase(Locale.ROOT).trim().startsWith("(") &&
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java
                            if (val_next == TB) {
                                value[--read] = CT;
                                continue;
                            }
                        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/iptc/IptcAnpaParser.java`
#### Snippet
```java

                // load from the stream until we run out of characters, or hit the termination byte
                continue;
            }

```

## RuleId[id=RegExpUnnecessaryNonCapturingGroup]
### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:-\\d\\d-\\d{4})|([-+])\\s*(\\d?\\d):?(\\d\\d))`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
    //the while loop.
    private static final Pattern OFFSET_PATTERN =
            Pattern.compile("(?:(?:-\\d\\d-\\d{4})|([-+])\\s*(\\d?\\d):?(\\d\\d))");

    private static final Pattern DAYS_OF_WEEK =
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:-\\d\\d-\\d{4})`
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-mail-commons/src/main/java/org/apache/tika/parser/mailcommons/MailDateParser.java`
#### Snippet
```java
    //the while loop.
    private static final Pattern OFFSET_PATTERN =
            Pattern.compile("(?:(?:-\\d\\d-\\d{4})|([-+])\\s*(\\d?\\d):?(\\d\\d))");

    private static final Pattern DAYS_OF_WEEK =
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy 6 fields
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/rtf/GroupState.java`
#### Snippet
```java

    // Create new GroupState, inheriting all properties from current one, adding 1 to the depth
    public GroupState(GroupState other) {
        bold = other.bold;
        italic = other.italic;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'type'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/SerialNumber.java`
#### Snippet
```java
     * @param sn Specify the serial number where copy from.
     */
    public SerialNumber(SerialNumber sn) {
        this.guid = sn.guid;
        this.value = sn.value;
```

## RuleId[id=ThrowableNotThrown]
### ThrowableNotThrown
Result of `waitForResponse()` not thrown
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
        }

        waitForResponse(resources);
    }

```

### ThrowableNotThrown
Result of `process()` not thrown
in `tika-core/src/main/java/org/apache/tika/fork/ForkClient.java`
#### Snippet
```java
            } else if (type == ForkServer.RESOURCE) {
                ForkResource resource = resources.get(input.readUnsignedByte());
                resource.process(input, output);
            } else if ((byte) type == ForkServer.ERROR) {
                try {
```

### ThrowableNotThrown
Checked exception instance `TikaConfigException` is not thrown
in `tika-pipes/tika-pipes-iterators/tika-pipes-iterator-csv/src/main/java/org/apache/tika/pipes/pipesiterator/csv/CSVPipesIterator.java`
#### Snippet
```java

        if (StringUtils.isBlank(fetcherName) && !StringUtils.isBlank(fetchKeyColumn)) {
            new TikaConfigException("If specifying a 'fetchKeyColumn', " +
                    "you must also specify a 'fetcherName'");
        }
```

## RuleId[id=ThreadStartInConstruction]
### ThreadStartInConstruction
Call to `start()` during object construction
in `tika-core/src/main/java/org/apache/tika/parser/ParsingReader.java`
#### Snippet
```java
            Thread thread = new Thread(command, name);
            thread.setDaemon(true);
            thread.start();
        });
    }
```

### ThreadStartInConstruction
Call to `start()` during object construction
in `tika-server/tika-server-core/src/main/java/org/apache/tika/server/core/ServerStatusWatcher.java`
#### Snippet
```java
        Thread statusWatcher = new Thread(new StatusWatcher());
        statusWatcher.setDaemon(true);
        statusWatcher.start();
        writeStatus(false);
    }
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/parser/microsoft/onenote/fsshttpb/streamobj/basic/CellID.java`
#### Snippet
```java
     */
    @Override
    public boolean equals(Object obj) {
        CellID another = (CellID) obj;

```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `tika-core/src/main/java/org/apache/tika/metadata/Metadata.java`
#### Snippet
```java
    }

    public boolean equals(Object o) {

        if (o == null) {
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `tika-langdetect/tika-langdetect-tika/src/main/java/org/apache/tika/langdetect/tika/LanguageProfilerBuilder.java`
#### Snippet
```java

        // Inherited JavaDoc
        public boolean equals(Object obj) {

            NGramEntry ngram = null;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
         */
        @Override
        public boolean equals(Object obj) {
            Address otherA = (Address) obj;
            if (this.settlment == null) {
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
         */
        @Override
        public boolean equals(Object obj) {
            OrgName otherA = (OrgName) obj;

```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
         */
        @Override
        public boolean equals(Object obj) {
            Affiliation otherA = (Affiliation) obj;
            return this.getAddress().equals(otherA.getAddress()) &&
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
         */
        @Override
        public boolean equals(Object obj) {
            Country otherC = (Country) obj;

```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `tika-parsers/tika-parsers-ml/tika-parser-nlp-module/src/main/java/org/apache/tika/parser/journal/TEIDOMParser.java`
#### Snippet
```java
         */
        @Override
        public boolean equals(Object obj) {
            OrgTypeName otherOrgName = (OrgTypeName) obj;
            return this.type.equals(otherOrgName.getType()) &&
```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'zipEntrySource' to 'CloseShieldZipFileZipEntrySource'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-microsoft-module/src/main/java/org/apache/tika/detect/microsoft/ooxml/OPCPackageDetector.java`
#### Snippet
```java
        //this will now be closed eventually when the wrapper closes
        //the pkg which will close this
        ((CloseShieldZipFileZipEntrySource)zipEntrySource).allowClose();
        //only set the open container if we made it here
        stream.setOpenContainer(new OPCPackageWrapper(pkg));
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'v' to 'COSBase'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-pdf-module/src/main/java/org/apache/tika/parser/pdf/PDMetadataExtractor.java`
#### Snippet
```java
        if (value instanceof COSArray) {
            for (Object v : ((COSArray) value).toList()) {
                addMetadata(metadata, name, ((COSBase) v));
            }
        } else if (value instanceof COSString) {
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'attrs' to 'AttributesImpl'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/JDBCTableReader.java`
#### Snippet
```java
            is = TikaInputStream.get(blob, m);
            Attributes attrs = new AttributesImpl();
            ((AttributesImpl) attrs).addAttribute("", "type", "type", "CDATA", "blob");
            ((AttributesImpl) attrs)
                    .addAttribute("", "column_name", "column_name", "CDATA", columnName);
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'attrs' to 'AttributesImpl'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/JDBCTableReader.java`
#### Snippet
```java
            Attributes attrs = new AttributesImpl();
            ((AttributesImpl) attrs).addAttribute("", "type", "type", "CDATA", "blob");
            ((AttributesImpl) attrs)
                    .addAttribute("", "column_name", "column_name", "CDATA", columnName);
            ((AttributesImpl) attrs).addAttribute("", "row_number", "row_number", "CDATA",
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'attrs' to 'AttributesImpl'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-jdbc-commons/src/main/java/org/apache/tika/parser/jdbc/JDBCTableReader.java`
#### Snippet
```java
            ((AttributesImpl) attrs)
                    .addAttribute("", "column_name", "column_name", "CDATA", columnName);
            ((AttributesImpl) attrs).addAttribute("", "row_number", "row_number", "CDATA",
                    Integer.toString(rowNum));
            handler.startElement("", "span", "span", attrs);
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'e' to 'SyndEntry'
in `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-news-module/src/main/java/org/apache/tika/parser/feed/FeedParser.java`
#### Snippet
```java
            xhtml.startElement("ul");
            for (Object e : feed.getEntries()) {
                SyndEntry entry = (SyndEntry) e;
                String link = entry.getLink();
                if (link != null) {
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'object' to 'COSWriterXRefEntry'
in `tika-fuzzing/src/main/java/org/apache/tika/fuzzing/pdf/EvilCOSWriter.java`
#### Snippet
```java
        List<Long> list = new ArrayList<>();
        for (Object object : xRefEntriesList) {
            long nr = (int) ((COSWriterXRefEntry) object).getKey().getNumber();
            if (nr == last + 1) {
                ++count;
```

