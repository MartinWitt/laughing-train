# calcite 
 
# Bad smells
I found 4644 bad smells with 98 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| DataFlowIssue | 1128 | false |
| ConstantValue | 840 | false |
| NullableProblems | 649 | false |
| AutoCloseableResource | 374 | false |
| SwitchStatementWithTooFewBranches | 196 | false |
| UNCHECKED_WARNING | 153 | false |
| DuplicatedCode | 147 | false |
| UnstableApiUsage | 105 | false |
| NotNullFieldNotInitialized | 96 | false |
| FieldMayBeFinal | 81 | false |
| TrivialIf | 74 | false |
| TypeParameterExtendsObject | 53 | false |
| DefaultAnnotationParam | 48 | false |
| UnnecessaryToStringCall | 47 | true |
| RedundantCast | 44 | false |
| DeprecatedIsStillUsed | 42 | false |
| EmptyStatementBody | 37 | false |
| FieldCanBeLocal | 35 | false |
| UnnecessaryLocalVariable | 34 | true |
| RedundantMethodOverride | 30 | false |
| DuplicateBranchesInSwitch | 26 | false |
| UnnecessaryUnicodeEscape | 24 | false |
| UnusedAssignment | 22 | false |
| CommentedOutCode | 19 | false |
| TrivialStringConcatenation | 19 | false |
| MagicConstant | 14 | false |
| MismatchedJavadocCode | 14 | false |
| JavadocReference | 13 | false |
| DuplicateExpressions | 12 | false |
| FunctionalExpressionCanBeFolded | 12 | false |
| JavadocLinkAsPlainText | 10 | false |
| Deprecation | 9 | false |
| ManualMinMaxCalculation | 9 | false |
| StaticInitializerReferencesSubClass | 8 | false |
| IgnoreResultOfCall | 8 | false |
| SuspiciousNameCombination | 8 | false |
| UnnecessarySemicolon | 8 | false |
| IfStatementWithIdenticalBranches | 8 | false |
| ToArrayCallWithZeroLengthArrayArgument | 6 | true |
| HasPlatformType | 6 | false |
| UnnecessaryCallToStringValueOf | 6 | false |
| UseBulkOperation | 6 | false |
| UnnecessaryEmptyArrayUsage | 6 | false |
| RegExpSimplifiable | 6 | false |
| OptionalGetWithoutIsPresent | 6 | false |
| SuspiciousMethodCalls | 6 | false |
| RedundantTypeArguments | 5 | false |
| DanglingJavadoc | 5 | false |
| UnpredictableBigDecimalConstructorCall | 5 | false |
| ArraysAsListWithZeroOrOneArgument | 5 | false |
| SlowListContainsAll | 5 | false |
| SlowAbstractSetRemoveAll | 5 | false |
| WrapperTypeMayBePrimitive | 4 | false |
| DuplicateCondition | 4 | false |
| AccessStaticViaInstance | 4 | false |
| PointlessBooleanExpression | 4 | true |
| IOStreamConstructor | 4 | false |
| CollectionAddAllCanBeReplacedWithConstructor | 4 | false |
| ConditionCoveredByFurtherCondition | 4 | false |
| BigDecimalMethodWithoutRoundingCalled | 4 | false |
| SuspiciousInvocationHandlerImplementation | 3 | false |
| ExplicitArrayFilling | 3 | false |
| IntegerMultiplicationImplicitCastToLong | 3 | false |
| UNUSED_IMPORT | 3 | false |
| MismatchedCollectionQueryUpdate | 3 | false |
| SimplifyStreamApiCallChains | 3 | false |
| SwitchStatementWithConfusingDeclaration | 3 | false |
| BusyWait | 3 | false |
| StringConcatenationInsideStringBufferAppend | 3 | false |
| CatchMayIgnoreException | 3 | false |
| CloneableImplementsClone | 2 | false |
| ManualArrayToCollectionCopy | 2 | false |
| ProtectedMemberInFinalClass | 2 | true |
| ListIndexOfReplaceableByContains | 2 | false |
| NonStrictComparisonCanBeEquality | 2 | true |
| ConditionalBreakInInfiniteLoop | 2 | false |
| RefusedBequest | 2 | false |
| UnnecessaryReturn | 2 | true |
| RedundantOperationOnEmptyContainer | 2 | false |
| RegExpDuplicateAlternationBranch | 2 | false |
| RemoveEmptyParenthesesFromLambdaCall | 2 | false |
| UnnecessaryModifier | 1 | true |
| KDocUnresolvedReference | 1 | false |
| ForLoopReplaceableByWhile | 1 | false |
| ListRemoveInLoop | 1 | false |
| IteratorHasNextCallsIteratorNext | 1 | false |
| StringOperationCanBeSimplified | 1 | false |
| JavaReflectionMemberAccess | 1 | false |
| ShiftOutOfRange | 1 | false |
| UnnecessaryInitCause | 1 | false |
| OptionalUsedAsFieldOrParameterType | 1 | false |
| SuspiciousToArrayCall | 1 | false |
| IntegerDivisionInFloatingPointContext | 1 | false |
| StringEquality | 1 | false |
| SuspiciousSystemArraycopy | 1 | false |
| RedundantClassCall | 1 | false |
| NonFinalFieldInEnum | 1 | false |
| NegativeIntConstantInLongContext | 1 | false |
| ConditionalExpressionWithIdenticalBranches | 1 | false |
| RedundantLengthCheck | 1 | false |
| RedundantCollectionOperation | 1 | false |
| RegExpSingleCharAlternation | 1 | false |
| ReplaceWithOperatorAssignment | 1 | false |
| CloneDeclaresCloneNotSupported | 1 | false |
| UnnecessaryContinue | 1 | false |
| ThrowableNotThrown | 1 | false |
| EqualsWhichDoesntCheckParameterClass | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Coordinate\[list.size()\]'
in `core/src/main/java/org/apache/calcite/runtime/RemoveRepeatedPointsTransformer.java`
#### Snippet
```java
      list.set(list.size() - 1, last);
    }
    Coordinate[] array = list.toArray(new Coordinate[list.size()]);
    return new CoordinateArraySequence(array);
  }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Geometry\[accumulator.size()\]'
in `core/src/main/java/org/apache/calcite/runtime/CollectOperation.java`
#### Snippet
```java

  public Geometry result(List<Geometry> accumulator) {
    Geometry[] array = accumulator.toArray(new Geometry[accumulator.size()]);
    return GEOMETRY_FACTORY.createGeometryCollection(array);
  }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Geometry\[acc.size()\]'
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
    List<Geometry> acc = new ArrayList<>();
    extractGeometryHoles(geom, acc);
    Geometry[] array = acc.toArray(new Geometry[acc.size()]);
    return GEOMETRY_FACTORY.createGeometryCollection(array);
  }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LineString\[lines.size()\]'
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
      return factory.createMultiLineString();
    } else {
      return factory.createMultiLineString(lines.toArray(new LineString[lines.size()]));
    }
  }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LineString\[lines.size()\]'
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
      return factory.createMultiLineString();
    } else {
      return factory.createMultiLineString(lines.toArray(new LineString[lines.size()]));
    }
  }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new HttpHost\[hosts.size()\]'
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSchemaFactory.java`
#### Snippet
```java
      return REST_CLIENTS.get(cacheKey, new Callable<RestClient>() {
        @Override public RestClient call() throws NoSuchAlgorithmException, KeyManagementException {
          RestClientBuilder builder = RestClient.builder(hosts.toArray(new HttpHost[hosts.size()]));

          if (!Strings.isNullOrEmpty(username) && !Strings.isNullOrEmpty(password)) {
```

## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      c = (Calendar) c.clone();
      if (!down) {
        final Integer code = calendarUnitFor(timeUnit);
        final int v = c.get(code);
        c.set(code, v + 1);
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableBatchNestedLoopJoin.java`
#### Snippet
```java
    }

    Double restartCount = mq.getRowCount(getLeft()) / variablesSet.size();

    RelOptCost rightCost = planner.getCost(getRight(), mq);
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `core/src/main/java/org/apache/calcite/runtime/ResultSetEnumerable.java`
#### Snippet
```java
        return new ResultSetEnumerator<>(resultSet, rowBuilderFactory);
      } else {
        Integer updateCount = statement.getUpdateCount();
        //noinspection unchecked
        return Linq4j.singletonEnumerator((T) updateCount);
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `core/src/main/java/org/apache/calcite/runtime/ResultSetEnumerable.java`
#### Snippet
```java
        return new ResultSetEnumerator<>(resultSet, rowBuilderFactory);
      } else {
        Integer updateCount = preparedStatement.getUpdateCount();
        //noinspection unchecked
        return Linq4j.singletonEnumerator((T) updateCount);
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidator.java`
#### Snippet
```java
   */
  @Value.Immutable(singleton = false)
  public interface Config {
    /** Default configuration. */
    SqlValidator.Config DEFAULT = ImmutableSqlValidator.Config.builder()
```

## RuleId[id=MagicConstant]
### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
    private static Calendar increment(Calendar c, TimeUnitRange timeUnit) {
      c = (Calendar) c.clone();
      c.add(calendarUnitFor(timeUnit), 1);
      return c;
    }
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      if (!down) {
        final Integer code = calendarUnitFor(timeUnit);
        final int v = c.get(code);
        c.set(code, v + 1);
      }
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        final Integer code = calendarUnitFor(timeUnit);
        final int v = c.get(code);
        c.set(code, v + 1);
      }
      return c;
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      switch (timeUnit) {
      case YEAR:
        c.set(calendarUnitFor(TimeUnitRange.MONTH), Calendar.JANUARY);
        // fall through; need to zero out lower time units
      case MONTH:
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        // fall through; need to zero out lower time units
      case MONTH:
        c.set(calendarUnitFor(TimeUnitRange.DAY), 1);
        // fall through; need to zero out lower time units
      case DAY:
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        // fall through; need to zero out lower time units
      case DAY:
        c.set(calendarUnitFor(TimeUnitRange.HOUR), 0);
        // fall through; need to zero out lower time units
      case HOUR:
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        // fall through; need to zero out lower time units
      case HOUR:
        c.set(calendarUnitFor(TimeUnitRange.MINUTE), 0);
        // fall through; need to zero out lower time units
      case MINUTE:
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        // fall through; need to zero out lower time units
      case MINUTE:
        c.set(calendarUnitFor(TimeUnitRange.SECOND), 0);
        // fall through; need to zero out lower time units
      case SECOND:
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        // fall through; need to zero out lower time units
      case SECOND:
        c.set(calendarUnitFor(TimeUnitRange.MILLISECOND), 0);
        break;
      default:
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      final int code = calendarUnitFor(timeUnit);
      for (;;) {
        c.set(code, v);
        int v2 = c.get(code);
        if (v2 < v) {
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      for (;;) {
        c.set(code, v);
        int v2 = c.get(code);
        if (v2 < v) {
          // E.g. when we set DAY=30 on 2014-02-01, we get 2014-02-30 because
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        if (strict && original.compareTo(c) == 0) {
          c.add(
              calendarUnitFor(
              requireNonNull(TIME_UNIT_PARENTS.get(timeUnit),
                  () -> "TIME_UNIT_PARENTS.get(timeUnit) is null for " + timeUnit)), 1);
          continue;
        }
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
    private static Calendar decrement(Calendar c, TimeUnitRange timeUnit) {
      c = (Calendar) c.clone();
      c.add(calendarUnitFor(timeUnit), -1);
      return c;
    }
```

### MagicConstant
Should be one of: Pattern.UNIX_LINES, Pattern.CASE_INSENSITIVE, Pattern.COMMENTS, Pattern.MULTILINE, ... or their combination
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

    final int flags = makeRegexpFlags(matchType);
    final Pattern pattern = Pattern.compile(regex, flags);

    return Unsafe.regexpReplace(s, pattern, replacement, pos, occurrence);
```

## RuleId[id=KDocUnresolvedReference]
### KDocUnresolvedReference
Cannot resolve symbol 'WithLex'
in `testkit/src/main/kotlin/org/apache/calcite/testlib/WithLocaleExtension.kt`
#### Snippet
```java

/**
 * Enables to set a locale per test class or test method by adding [WithLex] annotation.
 *
 * It might be useful in case the test depends on decimal separators (1,000 vs 1 000),
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/rel/core/Aggregate.java`
#### Snippet
```java
      ImmutableBitSet g = null;
      for (ImmutableBitSet bitSet : groupSets) {
        if (g == null) {
          // First item must equal groupSet
        } else {
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/rel/core/Window.java`
#### Snippet
```java
        buf.append(orderKeys);
      }
      if (orderKeys.getFieldCollations().isEmpty()
          && lowerBound.isUnbounded()
          && lowerBound.isPreceding()
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/rel/core/Window.java`
#### Snippet
```java
        // which is equivalent to
        // "ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING"
      } else if (!orderKeys.getFieldCollations().isEmpty()
          && lowerBound.isUnbounded()
          && lowerBound.isPreceding()
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/rel/rules/ReduceExpressionsRule.java`
#### Snippet
```java
          final RexNode newConditionExp =
              expandedExprList.get(conditionIndex);
          if (newConditionExp.isAlwaysTrue()) {
            // condition is always TRUE - drop it.
          } else if (newConditionExp instanceof RexLiteral
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/rex/RexCall.java`
#### Snippet
```java
  protected String computeDigest(boolean withType) {
    final StringBuilder sb = new StringBuilder(op.getName());
    if ((operands.size() == 0)
        && (op.getSyntax() == SqlSyntax.FUNCTION_ID)) {
      // Don't print params for empty arg list. For example, we want
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/rex/RexWindow.java`
#### Snippet
```java
    // 3. If the ROWS/RANGE clause is the default, "RANGE BETWEEN UNBOUNDED
    //    PRECEDING AND CURRENT ROW"
    if (!allowFraming // 1
        || orderKeys.isEmpty() // 2
        || (lowerBound.isUnbounded() // 3
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java
      map.put("order", toJson(window.orderKeys));
    }
    if (window.getLowerBound() == null) {
      // No ROWS or RANGE clause
    } else if (window.getUpperBound() == null) {
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
      SqlNode lowerBound = window.lowerBound;
      SqlNode upperBound = window.upperBound;
      if (lowerBound == null) {
        // No ROWS or RANGE clause
      } else if (upperBound == null) {
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

      // ALLOW PARTIAL/DISALLOW PARTIAL
      if (window.allowPartial == null) {
        // do nothing
      } else if (window.isAllowPartial()) {
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
      if (window.allowPartial == null) {
        // do nothing
      } else if (window.isAllowPartial()) {
        // We could output "ALLOW PARTIAL", but this syntax is
        // non-standard. Omitting the clause has the same effect.
```

### EmptyStatementBody
`else` statement has empty body
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
                RESOURCE.rowMustBeNonNegativeIntegral());
          }
        } else {
          // Allow expressions in ROWS clause
        }
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
        terms.add(RexUtil.andNot(rexBuilder, branchTerm, notTerms));
      }
      if (booleanBranches && useBranch) {
        // we are safe to ignore this branch because for boolean true branches:
        // a || (b && !a) === a || b
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeName.java`
#### Snippet
```java

      // Apply scale.
      if (scale == 0) {
        // do nothing
      } else if (scale > 0) {
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      } else if (operand instanceof TargetOperand) {
        ++targetCount;
      } else if (operand instanceof AnyOperand) {
        // nothing
      } else {
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
              // (which is more specific than method)
              continue;
            } else if (c2.isAssignableFrom(c1)) {
              // c1 inherits from c2 (method is more specific
              // than candidate method), so fall through
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
        Method method = map.get(key);
        if (method == null) {
          if (map.containsKey(key)) {
            // We already looked for the method and found nothing.
          } else {
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
    RelDigest digest = rel.getRelDigest();
    RelNode equivExp = mapDigestToRel.get(digest);
    if (equivExp == null) {
      // do nothing
    } else if (equivExp == rel) {
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
    RelTraitSet traitSet = super.emptyTraitSet();
    for (RelTraitDef traitDef : traitDefs) {
      if (traitDef.multiple()) {
        // TODO: restructure RelTraitSet to allow a list of entries
        //  for any given trait
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/util/graph/Graphs.java`
#### Snippet
```java
            list.add(ImmutableList.copyOf(prefix));
            prefix.remove(prefix.size() - 1);
          } else if (excludedNodes.contains(target)) {
            // ignore it
          } else {
```

### EmptyStatementBody
`else` statement has empty body
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
        registerSubQueries(parentScope, listNode);
      }
    } else {
      // atomic node -- can be ignored
    }
```

### EmptyStatementBody
`for` statement has empty body
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java

        if (start != -1) {
          for (int j = actualStart; j <= end; j++) {
            // builder7
            // implementAdd
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
          void sample() {
            if (curentPosition > startIndex) {
              if (rows[curentPosition - 1].compareTo(rows[curentPosition])
                  > 0) {
                // update rank
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
        // else {} whitespace
      } else {  // Inside of a word or white space after end of word.
        if (c > 47 && c < 58) { // 0-9
          // noop
        } else if (c > 64 && c < 91) {  // A-Z
```

### EmptyStatementBody
`if` statement has empty body
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
        } else if (c > 64 && c < 91) {  // A-Z
          curCh = (char) (c + 32); // Lowercase this character
        } else if (c > 96 && c < 123) {  // a-z
          // noop
        } else { // whitespace
```

### EmptyStatementBody
`if` statement has empty body
in `plus/src/main/java/org/apache/calcite/adapter/os/SqlShell.java`
#### Snippet
```java

      private void value(StringBuilder b, String s) {
        if (s == null) {
          // do nothing - unfortunately same as empty string
        } else if (s.contains("\"")) {
```

### EmptyStatementBody
`while` statement has empty body
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
    }
    expect(parser, JsonToken.START_OBJECT);
    while (parser.nextToken() != JsonToken.END_OBJECT) {
      // empty
    }
```

### EmptyStatementBody
`while` statement has empty body
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
                  || parser.getCurrentName().equals("metrics")) {
                expect(parser, JsonToken.START_ARRAY);
                while (parser.nextToken() != JsonToken.END_ARRAY) {
                  // empty
                }
```

### EmptyStatementBody
`while` statement has empty body
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
            if (parser.getCurrentName().equals("columns")) {
              expect(parser, JsonToken.START_ARRAY);
              while (parser.nextToken() != JsonToken.END_ARRAY) {
                // Skip the columns list
              }
```

### EmptyStatementBody
`if` statement has empty body
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java
        if (primitive.assignableFrom(bestPrimitive)) {
          bestPrimitive = primitive;
        } else if (bestPrimitive.assignableFrom(primitive)) {
          // ok
        } else if (bestPrimitive == Primitive.CHAR
```

### EmptyStatementBody
`if` statement has empty body
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
        clazz = requireNonNull(primitive.boxClass, "boxClass");
      }
      if ((clazz == Float.class || clazz == Double.class)
          && value instanceof BigDecimal) {
        // Don't try to convert the value of float and double literals.
```

### EmptyStatementBody
`if` statement has empty body
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockBuilder.java`
#### Snippet
```java
                  requireNonNull(gotoStatement.expression, "expression"),
                  optimize);
          if (isSimpleExpression(result)) {
            // already simple; no need to declare a variable or
            // even to evaluate the expression
```

### EmptyStatementBody
`else` statement has empty body
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockBuilder.java`
#### Snippet
```java
            result = declare.parameter;
          }
        } else {
          // not an expression -- result remains null
        }
```

### EmptyStatementBody
`if` statement has empty body
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
      try {
        fieldNames = csvReader.readNext();
        if (fieldNames == null
            || fieldNames.length == 0
            || fieldNames.length == 1 && fieldNames[0].isEmpty()) {
```

### EmptyStatementBody
`if` statement has empty body
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
      final int de = d.getDeletedEnd() + 1;
      if (ae == 0) {
        if (de == 0) {
          // no change
        } else {
```

### EmptyStatementBody
`if` statement has empty body
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
        try {
          Expression expr = call.getOperands().get(i).accept(this);
          if (expr instanceof NamedFieldExpression) {
            // nop currently
          } else {
```

### EmptyStatementBody
`if` statement has empty body
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
          f.checkFails("CAST(^" + literalString + "^ AS " + type + ")",
              "Numeric literal '.*' out of range", false);
        } else if ((type.getSqlTypeName() == SqlTypeName.CHAR)
            || (type.getSqlTypeName() == SqlTypeName.VARCHAR)
            || (type.getSqlTypeName() == SqlTypeName.BINARY)
```

### EmptyStatementBody
`if` statement has empty body
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
          f.checkType(expr, type.getFullTypeString());

          if (type.getSqlTypeName() == SqlTypeName.BINARY) {
            // Casting a string/binary values may change the value.
            // For example, CAST(X'AB' AS BINARY(2)) yields
```

## RuleId[id=StaticInitializerReferencesSubClass]
### StaticInitializerReferencesSubClass
Referencing subclass QuotedParserPos from superclass SqlParserPos initializer might lead to class loading deadlock
in `core/src/main/java/org/apache/calcite/sql/parser/SqlParserPos.java`
#### Snippet
```java

  /** Same as {@link #ZERO} but always quoted. */
  public static final SqlParserPos QUOTED_ZERO = new QuotedParserPos(0, 0, 0, 0);

  private static final long serialVersionUID = 1L;
```

### StaticInitializerReferencesSubClass
Referencing subclass SpecialSarg from superclass Sarg initializer might lead to class loading deadlock
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, NOT_EQUAL)} is equivalent to {@code x <> x}. */
  private static final SpecialSarg NOT_EQUAL =
      new SpecialSarg(ImmutableRangeSet.of(), RexUnknownAs.UNKNOWN,
          "Sarg[<>]", 6);

```

### StaticInitializerReferencesSubClass
Referencing subclass SpecialSarg from superclass Sarg initializer might lead to class loading deadlock
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, TRUE)} is equivalent to {@code TRUE}. */
  private static final SpecialSarg TRUE =
      new SpecialSarg(ImmutableRangeSet.of().complement(), RexUnknownAs.TRUE,
          "Sarg[TRUE]", 5);

```

### StaticInitializerReferencesSubClass
Referencing subclass SpecialSarg from superclass Sarg initializer might lead to class loading deadlock
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, FALSE)} is equivalent to {@code FALSE}. */
  private static final SpecialSarg FALSE =
      new SpecialSarg(ImmutableRangeSet.of(), RexUnknownAs.FALSE,
          "Sarg[FALSE]", 2);

```

### StaticInitializerReferencesSubClass
Referencing subclass SpecialSarg from superclass Sarg initializer might lead to class loading deadlock
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * {@code x IS NOT NULL}. */
  private static final SpecialSarg IS_NOT_NULL =
      new SpecialSarg(ImmutableRangeSet.of().complement(), RexUnknownAs.FALSE,
          "Sarg[IS NOT NULL]", 3);

```

### StaticInitializerReferencesSubClass
Referencing subclass SpecialSarg from superclass Sarg initializer might lead to class loading deadlock
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, IS_NULL)} is equivalent to {@code x IS NULL}. */
  private static final SpecialSarg IS_NULL =
      new SpecialSarg(ImmutableRangeSet.of(), RexUnknownAs.TRUE,
          "Sarg[IS NULL]", 4);

```

### StaticInitializerReferencesSubClass
Referencing subclass SpecialSarg from superclass Sarg initializer might lead to class loading deadlock
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, EQUAL)} is equivalent to {@code x = x}. */
  private static final SpecialSarg EQUAL =
      new SpecialSarg(ImmutableRangeSet.of().complement(), RexUnknownAs.UNKNOWN,
          "Sarg[=]", 7);

```

### StaticInitializerReferencesSubClass
Referencing subclass DeterministicCodeOptimizer from superclass ClassDeclarationFinder initializer might lead to class loading deadlock
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ClassDeclarationFinder.java`
#### Snippet
```java
  private static final Function1<ClassDeclarationFinder,
      ClassDeclarationFinder> DEFAULT_CHILD_FACTORY =
      DeterministicCodeOptimizer::new;

  /**
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (3 lines)
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigRel.java`
#### Snippet
```java
  void implement(Implementor implementor);

  // String getPigRelationAlias();
  //
  // String getFieldName(int index);
```

### CommentedOutCode
Commented out code (3 lines)
in `core/src/main/java/org/apache/calcite/rel/rules/ReduceExpressionsRule.java`
#### Snippet
```java
      // environment. Caller should execute something like the following before
      // invoking the planner:
      //
      // final RexExecutorImpl executor =
      //   new RexExecutorImpl(Schemas.createDataContext(null));
```

### CommentedOutCode
Commented out code (4 lines)
in `core/src/main/java/org/apache/calcite/util/graph/Graphs.java`
#### Snippet
```java
        return;
      }
//      final E edge = graph.getEdge(from, to);
//      if (edge != null) {
//        list.add(ImmutableList.of(from, to));
```

### CommentedOutCode
Commented out code (5 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableValues.java`
#### Snippet
```java

  @Override public Result implement(EnumerableRelImplementor implementor, Prefer pref) {
/*
          return Linq4j.asEnumerable(
              new Object[][] {
```

### CommentedOutCode
Commented out code (14 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableAggregate.java`
#### Snippet
```java
    //
    // final Enumerable<Employee> child = <<child adapter>>;
    // Function1<Employee, Integer> keySelector =
    //     new Function1<Employee, Integer>() {
    //         public Integer apply(Employee a0) {
```

### CommentedOutCode
Commented out code (6 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableAggregate.java`
#### Snippet
```java
    }

    // Function0<Object[]> accumulatorInitializer =
    //     new Function0<Object[]>() {
    //         public Object[] apply() {
```

### CommentedOutCode
Commented out code (8 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableAggregate.java`
#### Snippet
```java
                initBlock.toBlock()));

    // Function2<Object[], Employee, Object[]> accumulatorAdder =
    //     new Function2<Object[], Employee, Object[]>() {
    //         public Object[] apply(Object[] acc, Employee in) {
```

### CommentedOutCode
Commented out code (6 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableAggregate.java`
#### Snippet
```java
    implementLambdaFactory(builder, inputPhysType, aggs, accumulatorInitializer,
        hasOrderedCall(aggs), lambdaFactory);
    // Function2<Integer, Object[], Object[]> resultSelector =
    //     new Function2<Integer, Object[], Object[]>() {
    //         public Object[] apply(Integer key, Object[] acc) {
```

### CommentedOutCode
Commented out code (6 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableSortedAggregate.java`
#### Snippet
```java
    }

    // Function0<Object[]> accumulatorInitializer =
    //     new Function0<Object[]>() {
    //         public Object[] apply() {
```

### CommentedOutCode
Commented out code (8 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableSortedAggregate.java`
#### Snippet
```java
                initBlock.toBlock()));

    // Function2<Object[], Employee, Object[]> accumulatorAdder =
    //     new Function2<Object[], Employee, Object[]>() {
    //         public Object[] apply(Object[] acc, Employee in) {
```

### CommentedOutCode
Commented out code (6 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java
    }

    // Utilities.generateCollator(
    //      new Locale(
    //          collation.getLocale().getLanguage(),
```

### CommentedOutCode
Commented out code (8 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
    for (int windowIdx = 0; windowIdx < groups.size(); windowIdx++) {
      Group group = groups.get(windowIdx);
      // Comparator:
      // final Comparator<JdbcTest.Employee> comparator =
      //    new Comparator<JdbcTest.Employee>() {
```

### CommentedOutCode
Commented out code (12 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      Expression comparator_) {
    // Populate map of lists, one per partition
    //   final Map<Integer, List<Employee>> multiMap =
    //     new SortedMultiMap<Integer, List<Employee>>();
    //    source.foreach(
```

### CommentedOutCode
Commented out code (5 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
    if (group.keys.isEmpty()) {
      // If partition key is empty, no need to partition.
      //
      //   final List<Employee> tempList =
      //       source.into(new ArrayList<Employee>());
```

### CommentedOutCode
Commented out code (2 lines)
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      }
    }
    // multiMap.clear(); // allows gc
    // source = Linq4j.asEnumerable(list);
  }
```

### CommentedOutCode
Commented out code (40 lines)
in `core/src/main/java/org/apache/calcite/runtime/Enumerables.java`
#### Snippet
```java
                  matches -> emitter.emit(matches.rows, null, matches.symbols,
                      matchCounter.getAndIncrement(), emitRows::add));
/*
              recentRows.add(e);
              int earliestRetainedRow = Integer.MAX_VALUE;
```

### CommentedOutCode
Commented out code (5 lines)
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkRules.java`
#### Snippet
```java

    @Override public Result implementSpark(Implementor implementor) {
/*
            return Linq4j.asSpark(
                new Object[][] {
```

### CommentedOutCode
Commented out code (3 lines)
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockBuilder.java`
#### Snippet
```java
          // Don't inline variables whose name begins with "_". This
          // is a hacky way to prevent inlining. E.g.
          //   final int _count = collection.size();
          //   foo(collection);
          //   return collection.size() - _count;
```

### CommentedOutCode
Commented out code (19 lines)
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
      LogicalProject bottomProj) {
    assert false;
/*
    RelNode rel =
        new EnumerableTableScan(
```

## RuleId[id=CloneableImplementsClone]
### CloneableImplementsClone
`CorrelationId` is 'Cloneable' but does not define 'clone()' method
in `core/src/main/java/org/apache/calcite/rel/core/CorrelationId.java`
#### Snippet
```java
 * identify and set dynamic variables.
 */
public class CorrelationId implements Cloneable, Comparable<CorrelationId> {
  /**
   * Prefix to the name of correlating variables.
```

### CloneableImplementsClone
`AbstractSqlType` is 'Cloneable' but does not define 'clone()' method
in `core/src/main/java/org/apache/calcite/sql/type/AbstractSqlType.java`
#### Snippet
```java
 * Abstract base class for SQL implementations of {@link RelDataType}.
 */
public abstract class AbstractSqlType
    extends RelDataTypeImpl
    implements Cloneable, Serializable {
```

## RuleId[id=NotNullFieldNotInitialized]
### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `core/src/main/java/org/apache/calcite/plan/RelOptRuleOperand.java`
#### Snippet
```java

  private @Nullable RelOptRuleOperand parent;
  private @NotOnlyInitialized RelOptRule rule;
  private final Predicate<RelNode> predicate;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `core/src/main/java/org/apache/calcite/plan/RelOptCluster.java`
#### Snippet
```java
  private RexNode originalExpression;
  private final RexBuilder rexBuilder;
  private RelMetadataProvider metadataProvider;
  @Deprecated // to be removed before 2.0
  private MetadataFactory metadataFactory;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `core/src/main/java/org/apache/calcite/plan/RelOptCluster.java`
#### Snippet
```java
  private RelMetadataProvider metadataProvider;
  @Deprecated // to be removed before 2.0
  private MetadataFactory metadataFactory;
  private @Nullable HintStrategyTable hintStrategies;
  private final RelTraitSet emptyTraitSet;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `core/src/main/java/org/apache/calcite/plan/RelOptCluster.java`
#### Snippet
```java
  private final RelTraitSet emptyTraitSet;
  private @Nullable RelMetadataQuery mq;
  private Supplier<RelMetadataQuery> mqSupplier;

  //~ Constructors -----------------------------------------------------------
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSubset.java`
#### Snippet
```java

  /** Cost of best known plan (it may have improved since). */
  RelOptCost bestCost;

  /** The set this subset belongs to. */
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
   * A driver to manage rule and rule matches.
   */
  RuleDriver ruleDriver;

  /**
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileEnumerator.java`
#### Snippet
```java
  private final FileRowConverter converter;
  private final int[] fields;
  private Object current;

  FileEnumerator(Iterator<Elements> iterator, FileRowConverter converter) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/CsvTable.java`
#### Snippet
```java
  protected final Source source;
  protected final RelProtoDataType protoRowType;
  private RelDataType rowType;
  private List<RelDataType> fieldTypes;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/CsvTable.java`
#### Snippet
```java
  protected final RelProtoDataType protoRowType;
  private RelDataType rowType;
  private List<RelDataType> fieldTypes;

  /** Creates a CsvTable. */
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java
  private final Charset charset = StandardCharsets.UTF_8;
  private Element tableElement;
  private Elements headings;

  public FileReader(Source source, String selector, Integer index)
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java
  private final Integer index;
  private final Charset charset = StandardCharsets.UTF_8;
  private Element tableElement;
  private Elements headings;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    private String type;
    private String selector;
    private Integer selectedElement;
    private String replaceText;
    private Pattern replacePattern;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    private String replaceWith;
    private String matchText;
    private Pattern matchPattern;
    private Integer matchSeq;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    private String selector;
    private Integer selectedElement;
    private String replaceText;
    private Pattern replacePattern;
    private String replaceWith;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
  private static class CellReader {
    @SuppressWarnings("unused")
    private String type;
    private String selector;
    private Integer selectedElement;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    private Pattern replacePattern;
    private String replaceWith;
    private String matchText;
    private Pattern matchPattern;
    private Integer matchSeq;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    private String replaceText;
    private Pattern replacePattern;
    private String replaceWith;
    private String matchText;
    private Pattern matchPattern;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    private Integer selectedElement;
    private String replaceText;
    private Pattern replacePattern;
    private String replaceWith;
    private String matchText;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    @SuppressWarnings("unused")
    private String type;
    private String selector;
    private Integer selectedElement;
    private String replaceText;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    private String matchText;
    private Pattern matchPattern;
    private Integer matchSeq;

    CellReader(Map<String, Object> config) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `plus/src/main/java/org/apache/calcite/adapter/os/Processes.java`
#### Snippet
```java
      return new Enumerator<String>() {
        private final StringBuilder b = new StringBuilder();
        private String line;

        @Override public String current() {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `plus/src/main/java/org/apache/calcite/adapter/os/Processes.java`
#### Snippet
```java
      final BufferedReader br = new BufferedReader(isr);
      return new Enumerator<String>() {
        private String line;

        @Override public String current() {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/VirtualColumn.java`
#### Snippet
```java
    private String expression;

    private DruidType type;

    public Builder withName(String name) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/VirtualColumn.java`
#### Snippet
```java
    private String name;

    private String expression;

    private DruidType type;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/VirtualColumn.java`
#### Snippet
```java
   */
  public static class Builder {
    private String name;

    private String expression;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRel.java`
#### Snippet
```java
    final Map<String, String> oqlAggregateFunctions = new LinkedHashMap<>();

    Long limitValue;

    RelOptTable table;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRel.java`
#### Snippet
```java
    RelOptTable table;

    GeodeTable geodeTable;

    /**
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRel.java`
#### Snippet
```java
    Long limitValue;

    RelOptTable table;

    GeodeTable geodeTable;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSchema.java`
#### Snippet
```java
  final GemFireCache cache;
  private final List<String> regionNames;
  private ImmutableMap<String, Table> tableMap;

  public GeodeSchema(final GemFireCache gemFireCache, final Iterable<String> regionNames) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java
  @SuppressWarnings("unused")
  private ClientCache clientCache;
  private ImmutableMap<String, Table> tableMap;

  public GeodeSimpleSchema(
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
  @SuppressWarnings({ "WeakerAccess", "unused" })
  private static class JsonColumn {
    public String type;
    public boolean hasMultipleValues;
    public int size;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
    public long size;
    public long numRows;
    public Map<String, JsonAggregator> aggregators;
  }

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
  @SuppressWarnings({ "WeakerAccess", "unused" })
  private static class JsonSegmentMetadata {
    public String id;
    public List<String> intervals;
    public Map<String, JsonColumn> columns;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
    public int size;
    public Integer cardinality;
    public String errorMessage;
  }

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
  private static class JsonSegmentMetadata {
    public String id;
    public List<String> intervals;
    public Map<String, JsonColumn> columns;
    public long size;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
    public boolean hasMultipleValues;
    public int size;
    public Integer cardinality;
    public String errorMessage;
  }
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
    public String type;
    public String name;
    public String fieldName;

    DruidType druidType() {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
  private static class JsonAggregator {
    public String type;
    public String name;
    public String fieldName;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
    public String id;
    public List<String> intervals;
    public Map<String, JsonColumn> columns;
    public long size;
    public long numRows;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
  @SuppressWarnings({ "WeakerAccess", "unused" })
  private static class JsonAggregator {
    public String type;
    public String name;
    public String fieldName;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaTableOptions.java`
#### Snippet
```java
  private String bootstrapServers;
  private String topicName;
  private KafkaRowConverter rowConverter;
  private Map<String, String> consumerParams;
  //added to inject MockConsumer for testing.
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaTableOptions.java`
#### Snippet
```java
public final class KafkaTableOptions {
  private String bootstrapServers;
  private String topicName;
  private KafkaRowConverter rowConverter;
  private Map<String, String> consumerParams;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaTableOptions.java`
#### Snippet
```java
  private String topicName;
  private KafkaRowConverter rowConverter;
  private Map<String, String> consumerParams;
  //added to inject MockConsumer for testing.
  private Consumer consumer;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaTableOptions.java`
#### Snippet
```java
 */
public final class KafkaTableOptions {
  private String bootstrapServers;
  private String topicName;
  private KafkaRowConverter rowConverter;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaTableOptions.java`
#### Snippet
```java
  private Map<String, String> consumerParams;
  //added to inject MockConsumer for testing.
  private Consumer consumer;

  public String getBootstrapServers() {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTableFieldInfo.java`
#### Snippet
```java
 */
public class RedisTableFieldInfo {
  private String tableName;
  private String dataFormat;
  private List<LinkedHashMap<String, Object>> fields;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTableFieldInfo.java`
#### Snippet
```java
  private String tableName;
  private String dataFormat;
  private List<LinkedHashMap<String, Object>> fields;
  private String keyDelimiter = ":";

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTableFieldInfo.java`
#### Snippet
```java
public class RedisTableFieldInfo {
  private String tableName;
  private String dataFormat;
  private List<LinkedHashMap<String, Object>> fields;
  private String keyDelimiter = ":";
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTable.java`
#### Snippet
```java
  final String dataFormat;
  final RedisConfig redisConfig;
  RedisEnumerator redisEnumerator;

  public RedisTable(
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java
 */
class HttpServer {
  private static String localIpAddress;

  private final File resourceBase;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java
  }

  private Server server;
  private int port = -1;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbRel.java`
#### Snippet
```java

    RelOptTable table;
    InnodbTable innodbTable;

    public void addSelectFields(Map<String, String> fields) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbRel.java`
#### Snippet
```java
    boolean ascOrder = true;

    RelOptTable table;
    InnodbTable innodbTable;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
          .add(new SubstringOperatorConversion())
          .build();
  protected QuerySpec querySpec;

  final RelOptTable table;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilterTranslator.java`
#### Snippet
```java
  private static class InternalRexNode {
    /** Relation expression node. */
    RexNode node;
    /** Field ordinal in indexes. */
    int ordinalInKey;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilterTranslator.java`
#### Snippet
```java
    String op;
    /** Binary operation right literal value. */
    Object right;
  }

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilterTranslator.java`
#### Snippet
```java
    int ordinalInKey;
    /** Field name. */
    String fieldName;
    /** Binary operation like =, >=, <=, > or <.*/
    String op;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilterTranslator.java`
#### Snippet
```java
    String fieldName;
    /** Binary operation like =, >=, <=, > or <.*/
    String op;
    /** Binary operation right literal value. */
    Object right;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvSchema.java`
#### Snippet
```java
  private final File directoryFile;
  private final CsvTable.Flavor flavor;
  private Map<String, Table> tableMap;

  /**
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
    private final CSVReader csvReader;
    private String[] fieldNames;
    private int[] sources;
    private Object current;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
    private String[] fieldNames;
    private int[] sources;
    private Object current;

    /**
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
  public static class SplunkResultEnumerator implements Enumerator<Object> {
    private final CSVReader csvReader;
    private String[] fieldNames;
    private int[] sources;
    private Object current;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
  final String username;
  final String password;
  String sessionKey;
  final Map<String, String> requestHeaders = new HashMap<>();

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoRel.java`
#### Snippet
```java
    final RexBuilder rexBuilder;
    RelOptTable table;
    MongoTable mongoTable;

    public Implementor(RexBuilder rexBuilder) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoRel.java`
#### Snippet
```java
    final List<Pair<String, String>> list = new ArrayList<>();
    final RexBuilder rexBuilder;
    RelOptTable table;
    MongoTable mongoTable;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoEnumerator.java`
#### Snippet
```java
  private final Iterator<Document> cursor;
  private final Function1<Document, Object> getter;
  private Object current;

  /** Creates a MongoEnumerator.
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
  private RelOptRule rule;

  private RelNode transformationResult;

  private long metadataTimestamp = 0L;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private RelNode root;

  private RelOptRule rule;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
  private RelNode root;

  private RelOptRule rule;

  private RelNode transformationResult;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
  Matcher compiledDiffMatcher;
  private String ignorePatterns;
  Pattern compiledIgnorePattern;
  Matcher compiledIgnoreMatcher;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
   * Name of current .log file.
   */
  protected File logFile;

  /**
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
  /** Diff masks defined so far. */
  private String diffMasks;
  Pattern compiledDiffPattern;
  Matcher compiledDiffMatcher;
  private String ignorePatterns;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
   * OutputStream for current test log.
   */
  protected OutputStream logOutputStream;

  /** Diff masks defined so far. */
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
   * Name of current .ref file.
   */
  protected File refFile;

  /**
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
        new ArrayList<>();
    protected RelDataType rowType;
    protected List<RelCollation> collationList;
    protected final List<String> names;
    protected final Double maxRowCount;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
    protected final List<RelReferentialConstraint> referentialConstraints =
        new ArrayList<>();
    protected RelDataType rowType;
    protected List<RelCollation> collationList;
    protected final List<String> names;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
    return new Consumer<ResultSet>() {
      int executeCount = 0;
      Collection expected;

      @Override public void accept(ResultSet resultSet) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
    private final @Nullable PreparedStatementConsumer consumer;

    private String plan;

    private AssertQuery(ConnectionFactory connectionFactory, String sql,
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
                  int i = 0;
                  int curI;
                  String curS;

                  @Override public IntString current() {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    private Enumerator<Object[]> enumeratorImpl(final int[] fields) {
      return new Enumerator<Object[]>() {
        private Object[] current;
        private final Iterator<Object[]> iterator = Arrays.asList(rows)
            .iterator();
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `ubenchmark/src/jmh/java/org/apache/calcite/adapter/enumerable/CodeGenerationBenchmark.java`
#### Snippet
```java
    ClassDeclaration classExpr;
    EnumerableRel plan;
    String javaCode;
  }

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `ubenchmark/src/jmh/java/org/apache/calcite/adapter/enumerable/CodeGenerationBenchmark.java`
#### Snippet
```java
    PlanInfo[] planInfos;

    ICompilerFactory compilerFactory;

    private int currentPlan = 0;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `ubenchmark/src/jmh/java/org/apache/calcite/adapter/enumerable/CodeGenerationBenchmark.java`
#### Snippet
```java
    int cacheSize;

    Cache<String, Bindable> cache;

    @Setup(Level.Iteration)
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `ubenchmark/src/jmh/java/org/apache/calcite/adapter/enumerable/CodeGenerationBenchmark.java`
#### Snippet
```java
     * The necessary plan information for every generated query.
     */
    PlanInfo[] planInfos;

    ICompilerFactory compilerFactory;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `ubenchmark/src/jmh/java/org/apache/calcite/adapter/enumerable/CodeGenerationBenchmark.java`
#### Snippet
```java
  private static class PlanInfo {
    ClassDeclaration classExpr;
    EnumerableRel plan;
    String javaCode;
  }
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `ubenchmark/src/jmh/java/org/apache/calcite/adapter/enumerable/CodeGenerationBenchmark.java`
#### Snippet
```java
  /** Plan information. */
  private static class PlanInfo {
    ClassDeclaration classExpr;
    EnumerableRel plan;
    String javaCode;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
    private Object lt;
    private boolean lte;
    private Object gt;
    private boolean gte;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
    private boolean gte;

    private String format;

    private RangeQueryBuilder(final String fieldName) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
    private final String fieldName;

    private Object lt;
    private boolean lte;
    private Object gt;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/Scrolling.java`
#### Snippet
```java
    /** Keeps last value of {@code scrollId} in memory so scroll can be released
     * upon termination. */
    private String scrollId;

    private AutoClosingIterator(
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRel.java`
#### Snippet
```java

    RelOptTable table;
    ElasticsearchTable elasticsearchTable;

    void add(String findOp) {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRel.java`
#### Snippet
```java
     * @see <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/search-request-from-size.html">From/Size</a>
     */
    Long offset;

    /**
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRel.java`
#### Snippet
```java
     * @see <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/search-request-from-size.html">From/Size</a>
     */
    Long fetch;

    RelOptTable table;
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRel.java`
#### Snippet
```java
    Long fetch;

    RelOptTable table;
    ElasticsearchTable elasticsearchTable;

```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    private final NamedFieldExpression rel;
    private QueryBuilder builder;

    private String getFieldReference() {
```

### NotNullFieldNotInitialized
@DefaultQualifier fields must be initialized
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java

    private final List<? extends Aggregation> aggregations;
    private Map<String, Aggregation> aggregationsAsMap;

    Aggregations(List<? extends Aggregation> aggregations) {
```

## RuleId[id=ForLoopReplaceableByWhile]
### ForLoopReplaceableByWhile
`for` loop statement may be replace by 'while' loop
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/ParserBenchmark.java`
#### Snippet
```java
    Random rnd = new Random();
    rnd.setSeed(424242);
    for (; sb.length() < length;) {
      for (int i = 0; i < 7 && sb.length() < length; i++) {
        sb.append(", ");
```

## RuleId[id=Deprecation]
### Deprecation
'ROUND_HALF_UP' is deprecated
in `core/src/main/java/org/apache/calcite/rel/rules/ReduceDecimalsRule.java`
#### Snippet
```java
     * Scales down a decimal value, and returns the scaled value as an exact
     * numeric. with the rounding convention
     * {@link BigDecimal#ROUND_HALF_UP BigDecimal.ROUND_HALF_UP}. (Values midway
     * between two points are rounded away from zero.)
     *
```

### Deprecation
'metadata(java.lang.Class, org.apache.calcite.rel.metadata.RelMetadataQuery)' is deprecated
in `core/src/main/java/org/apache/calcite/rel/metadata/Metadata.java`
#### Snippet
```java
 * <p>User code (typically in a planner rule or an implementation of
 * {@link RelNode#computeSelfCost(org.apache.calcite.plan.RelOptPlanner, RelMetadataQuery)})
 * acquires a {@code Metadata} instance by calling {@link RelNode#metadata}.
 *
 * <p>A {@code Metadata} instance already knows which particular {@code RelNode}
```

### Deprecation
'deprecated(T, boolean)' is deprecated
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

  // CHECKSTYLE: IGNORE 1
  /** @see Util#deprecated(Object, boolean) */
  static void checkSpecialLiterals(SqlWindow window, SqlValidator validator) {
    final SqlNode lowerBound = window.getLowerBound();
```

### Deprecation
Overrides deprecated method in 'org.apache.calcite.plan.AbstractRelOptPlanner'
in `core/src/main/java/org/apache/calcite/plan/hep/HepPlanner.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  @Override public void registerMetadataProviders(List<RelMetadataProvider> list) {
    list.add(0, new HepRelMetadataProvider());
  }
```

### Deprecation
Overrides deprecated method in 'org.apache.calcite.plan.AbstractRelOptPlanner'
in `core/src/main/java/org/apache/calcite/plan/hep/HepPlanner.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  @Override public long getRelMetadataTimestamp(RelNode rel) {
    // TODO jvs 20-Apr-2006: This is overly conservative.  Better would be
    // to keep a timestamp per HepRelVertex, and update only affected
```

### Deprecation
Overrides deprecated method in 'org.apache.calcite.plan.AbstractRelOptPlanner'
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
  // implement RelOptPlanner
  @Deprecated // to be removed before 2.0
  @Override public long getRelMetadataTimestamp(RelNode rel) {
    RelSubset subset = getSubset(rel);
    if (subset == null) {
```

### Deprecation
Overrides deprecated method in 'org.apache.calcite.plan.AbstractRelOptPlanner'
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
  // implement RelOptPlanner
  @Deprecated // to be removed before 2.0
  @Override public void registerMetadataProviders(List<RelMetadataProvider> list) {
    list.add(0, new VolcanoRelMetadataProvider());
  }
```

### Deprecation
Overrides deprecated method in 'org.apache.calcite.plan.AbstractRelOptPlanner'
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  @Override public long getRelMetadataTimestamp(RelNode rel) {
    return metadataTimestamp;
  }
```

### Deprecation
'timeUnitCodes()' is deprecated
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
  /** Tests that EXTRACT, FLOOR, CEIL, DATE_TRUNC functions accept abbreviations for
   * time units (such as "Y" for "YEAR") when configured via
   * {@link Config#timeUnitCodes()}. */
  @Test protected void testTimeUnitCodes() {
    // YEAR is a built-in time frame. When unparsed, it looks like a keyword.
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `core/src/main/java/org/apache/calcite/rel/AbstractRelNode.java`
#### Snippet
```java
        // We can't call hashCode and equals on Array, so
        // convert it to String to keep the same behaviour.
        value = "" + value;
      }
      attrs.add(Pair.of(term, value));
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/src/main/java/org/apache/calcite/rel/externalize/RelXmlWriter.java`
#### Snippet
```java
    String tagName = rel.getRelTypeName();
    xmlOutput.beginBeginTag(tagName);
    xmlOutput.attribute("id", rel.getId() + "");

    for (Pair<String, @Nullable Object> value : values) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/src/main/java/org/apache/calcite/sql/SqlCollation.java`
#### Snippet
```java
      throw RESOURCE.invalidCompare(
          col1.collationName,
          "" + col1.coercibility,
          col2.collationName,
          "" + col2.coercibility).ex();
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/src/main/java/org/apache/calcite/sql/SqlCollation.java`
#### Snippet
```java
          "" + col1.coercibility,
          col2.collationName,
          "" + col2.coercibility).ex();
    }
    return ret;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/src/main/java/org/apache/calcite/plan/visualizer/RuleMatchVisualizer.java`
#### Snippet
```java

  private static String key(final RelNode rel) {
    return "" + rel.getId();
  }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/src/main/java/org/apache/calcite/util/Template.java`
#### Snippet
```java
    }
    // Next by string ordinal.
    return argMap.get(ordinal + "");
  }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java
    if (this.index == null) {
      if (list.size() != 1) {
        throw new FileReaderException("" + list.size()
            + " HTML element(s) selected");
      }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `plus/src/main/java/org/apache/calcite/chinook/CodesFunction.java`
#### Snippet
```java
        }
        return Linq4j.asEnumerable(new String[][]{
            new String[]{"HASHCODE", "" + name.hashCode()},
            new String[]{"BASE64",
                Base64.getEncoder().encodeToString(name.getBytes(StandardCharsets.UTF_8))}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `plus/src/main/java/org/apache/calcite/adapter/os/FilesTableFunction.java`
#### Snippet
```java
      private Enumerable<String> sourceLinux() {
        final String[] args = {
            "find", path, "-printf", ""
              + "%A@\\0" // access_time
              + "%b\\0" // block_count
```

### TrivialStringConcatenation
Empty string used in concatenation
in `example/function/src/main/java/org/apache/calcite/example/maze/Maze.java`
#### Snippet
```java
    } else {
      cellContent = c -> {
        String s = region(c) + "";
        return s.length() == 1 ? " " + s : s;
      };
```

### TrivialStringConcatenation
Empty string used in concatenation
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/MetadataBenchmark.java`
#### Snippet
```java
          rel.getCluster().invalidateMetadataQuery();
        })
        .explainContains(""
            + "EnumerableAggregate(group=[{1, 6, 10}], m0=[COUNT()])\n"
            + "  EnumerableMergeJoin(condition=[=($2, $8)], joinType=[inner])\n"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java

  @Test void testPivotThroughShuttle() {
    final String sql = ""
        + "SELECT *\n"
        + "FROM (SELECT job, deptno FROM \"EMP\")\n"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
        + "FROM (SELECT job, deptno FROM \"EMP\")\n"
        + "PIVOT (COUNT(*) AS \"COUNT\" FOR deptno IN (10, 50, 20))";
    final String expected = ""
        + "SELECT *\n"
        + "FROM (SELECT `JOB`, `DEPTNO`\n"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
    // Does not generate extra parentheses around UNNEST because UNNEST is
    // a table expression.
    final String sql1 = ""
        + "SELECT\n"
        + "  item.name,\n"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
    // Currently DESCRIBE STATEMENT becomes EXPLAIN.
    // See [CALCITE-1221] Implement DESCRIBE DATABASE, CATALOG, STATEMENT
    final String expected0 = ""
        + "EXPLAIN PLAN INCLUDING ATTRIBUTES WITH IMPLEMENTATION FOR\n"
        + "SELECT *\n"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
        + "FROM `EMPS`";
    sql("describe statement select * from emps").ok(expected0);
    final String expected1 = ""
        + "EXPLAIN PLAN INCLUDING ATTRIBUTES WITH IMPLEMENTATION FOR\n"
        + "(SELECT *\n"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
    sql("describe (select * from emps)").ok(expected0);
    sql("describe statement (select * from emps)").ok(expected0);
    final String expected2 = ""
        + "EXPLAIN PLAN INCLUDING ATTRIBUTES WITH IMPLEMENTATION FOR\n"
        + "SELECT `DEPTNO`\n"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
        + "FROM `DEPTS`";
    sql("describe select deptno from emps union select deptno from depts").ok(expected2);
    final String expected3 = ""
        + "EXPLAIN PLAN INCLUDING ATTRIBUTES WITH IMPLEMENTATION FOR\n"
        + "INSERT INTO `EMPS`\n"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java

  @Test void testStmtListWithSemiColonInComment() {
    final String sql = ""
        + "select * from emp, dept; // comment with semicolon ; values 1\n"
        + "values 2";
```

## RuleId[id=RedundantTypeArguments]
### RedundantTypeArguments
Explicit type arguments can be inferred
in `core/src/main/java/org/apache/calcite/jdbc/CalciteMetaImpl.java`
#### Snippet
```java
    return schemas(catalog)
        .selectMany(schema ->
            tables(schema, Functions.<String>truePredicate1()));
  }

```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
            .map(pair ->
                Pair.<List<RexLiteral>, List<RexNode>>of(
                    ImmutableList.<RexLiteral>copyOf(pair.getKey()),
                    ImmutableList.<RexNode>copyOf(pair.getValue())))
            .collect(Util.toImmutableList());
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
                Pair.<List<RexLiteral>, List<RexNode>>of(
                    ImmutableList.<RexLiteral>copyOf(pair.getKey()),
                    ImmutableList.<RexNode>copyOf(pair.getValue())))
            .collect(Util.toImmutableList());

```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    try (Enumerator<TSource> os = enumerable.enumerator()) {
      if (os.moveNext()) {
        return Linq4j.<TSource>asEnumerable(() -> new Iterator<TSource>() {

          private boolean nonFirst;
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/StatementTest.java`
#### Snippet
```java
    public final Department[] depts = {
        new Department(10, "Sales", Arrays.asList(emps[0], emps[2])),
        new Department(30, "Marketing", Collections.<Employee>emptyList()),
        new Department(40, "HR", Collections.singletonList(emps[1])),
    };
```

## RuleId[id=SuspiciousInvocationHandlerImplementation]
### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `core/src/main/java/org/apache/calcite/rel/metadata/ChainedRelMetadataProvider.java`
#### Snippet
```java
        }
      }
      return null;
    }
  }
```

### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `core/src/main/java/org/apache/calcite/util/Compatible.java`
#### Snippet
```java
              }
            }
            return null;
          });
    }
```

### SuspiciousInvocationHandlerImplementation
Method is never used in 'invoke': it's unlikely that 'hashCode', 'equals' and 'toString' are implemented correctly
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java
          (F) Proxy.newProxyInstance(classLoader,
              new Class[]{Types.toClass(type)},
              (proxy, method, args) -> x.dynamicInvoke(args));
    }
    return dynamicFunction;
```

## RuleId[id=NullableProblems]
### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigAggregate.java`
#### Snippet
```java

  @Override public Aggregate copy(RelTraitSet traitSet, RelNode input,
      ImmutableBitSet groupSet, List<ImmutableBitSet> groupSets,
      List<AggregateCall> aggCalls) {
    return new PigAggregate(input.getCluster(), traitSet, input, groupSet,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/DataContexts.java`
#### Snippet
```java
  private static class DataContextImpl extends MapDataContext {
    private CalciteConnection connection;
    private @Nullable SchemaPlus rootSchema;

    DataContextImpl(CalciteConnection connection,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/RelVisitor.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private @Nullable RelNode root;

  //~ Methods ----------------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/AbstractRelNode.java`
#### Snippet
```java
    private final List<Pair<String, @Nullable Object>> attrs = new ArrayList<>();

    @Nullable String digest = null;

    @Override public void explain(final RelNode rel,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/Match.java`
#### Snippet
```java
  protected final ImmutableBitSet partitionKeys;
  protected final RelCollation orderKeys;
  protected final @Nullable RexNode interval;

  //~ Constructors -----------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/Sort.java`
#### Snippet
```java
  public final RelCollation collation;
  public final @Nullable RexNode offset;
  public final @Nullable RexNode fetch;
  protected final ImmutableList<RelHint> hints;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/Sort.java`
#### Snippet
```java

  public final RelCollation collation;
  public final @Nullable RexNode offset;
  public final @Nullable RexNode fetch;
  protected final ImmutableList<RelHint> hints;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/RepeatUnion.java`
#### Snippet
```java
   * Transient table where repeat union's intermediate results will be stored (optional).
   */
  protected final @Nullable RelOptTable transientTable;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/TableModify.java`
#### Snippet
```java
  protected final RelOptTable table;
  private final Operation operation;
  private final @Nullable List<String> updateColumnList;
  private final @Nullable List<RexNode> sourceExpressionList;
  private @MonotonicNonNull RelDataType inputRowType;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/TableModify.java`
#### Snippet
```java
  private final Operation operation;
  private final @Nullable List<String> updateColumnList;
  private final @Nullable List<RexNode> sourceExpressionList;
  private @MonotonicNonNull RelDataType inputRowType;
  private final boolean flattened;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/AggregateCall.java`
#### Snippet
```java
  private final ImmutableList<Integer> argList;
  public final int filterArg;
  public final @Nullable ImmutableBitSet distinctKeys;
  public final RelCollation collation;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/AggregateCall.java`
#### Snippet
```java
  private final boolean ignoreNulls;
  public final RelDataType type;
  public final @Nullable String name;
  public final List<RexNode> rexList;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/TableFunctionScan.java`
#### Snippet
```java
  private final RexNode rexCall;

  private final @Nullable Type elementType;

  private ImmutableList<RelNode> inputs;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/core/TableFunctionScan.java`
#### Snippet
```java
  private ImmutableList<RelNode> inputs;

  protected final @Nullable ImmutableSet<RelColumnMapping> columnMappings;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/hint/HintStrategy.java`
#### Snippet
```java
  public static class Builder {
    private final HintPredicate predicate;
    private @Nullable HintOptionChecker optionChecker;
    private ImmutableSet<RelOptRule> excludedRules;
    private ImmutableSet<ConverterRule> converterRules;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/hint/HintStrategy.java`
#### Snippet
```java

  public final HintPredicate predicate;
  public final @Nullable HintOptionChecker hintOptionChecker;
  public final ImmutableSet<RelOptRule> excludedRules;
  public final ImmutableSet<ConverterRule> converterRules;
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rel/type/RelDataType.java`
#### Snippet
```java
   * @return named field, or null if not found
   */
  @Nullable RelDataTypeField getField(String fieldName, boolean caseSensitive,
      boolean elideRecord);

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  protected final @Nullable List<RelDataTypeField> fieldList;
  protected @Nullable String digest;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java
  private static class Slot {
    int count;
    @Nullable RelDataTypeField field;
  }
}
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java
  //~ Methods ----------------------------------------------------------------

  @Override public @Nullable RelDataTypeField getField(String fieldName,
      boolean caseSensitive, boolean elideRecord) {
    if (fieldList == null) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java

  protected final @Nullable List<RelDataTypeField> fieldList;
  protected @Nullable String digest;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rel/type/DynamicRecordTypeImpl.java`
#### Snippet
```java
  }

  @Override public @Nullable RelDataTypeField getField(String fieldName,
      boolean caseSensitive, boolean elideRecord) {
    final Pair<RelDataTypeField, Boolean> pair =
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/type/RelRecordType.java`
#### Snippet
```java
  private final StructKind kind;
  private final boolean nullable;
  private final @Nullable Map<String, RelDataTypeField> fieldNameMap;

  /** Minimum number of fields where it is worth populating {@link #fieldNameMap}
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/ValuesReduceRule.java`
#### Snippet
```java
  /** Shuttle that converts inputs to literals. */
  private static class MyRexShuttle extends RexShuttle {
    private @Nullable List<RexLiteral> literalList;

    @Override public RexNode visitInputRef(RexInputRef inputRef) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactoryImpl.java`
#### Snippet
```java
    private final boolean nullable;
    private @Nullable SqlCollation collation;
    private @Nullable Charset charset;

    public JavaType(Class clazz) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactoryImpl.java`
#### Snippet
```java
    private final Class clazz;
    private final boolean nullable;
    private @Nullable SqlCollation collation;
    private @Nullable Charset charset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java
   * we can only preserve them if the expressions if it is null when these fields are null.
   */
  final @Nullable ImmutableBitSet strongBitmap;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java
    private final BitSet rexRefs;
    private final ImmutableBitSet leftFields;
    private final @Nullable ImmutableBitSet rightFields;
    private final ImmutableBitSet strongFields;
    private final ExprCondition preserveExprCondition;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private final @Nullable Project origProj;
  private final @Nullable RexNode origFilter;
  private final RelNode childRel;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java

  private final @Nullable Project origProj;
  private final @Nullable RexNode origFilter;
  private final RelNode childRel;
  private final ExprCondition preserveExprCondition;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java
   * otherwise.
   */
  final @Nullable ImmutableBitSet rightBitmap;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoinOptimizeBushyRule.java`
#### Snippet
```java
    implements TransformationRule {

  private final @Nullable PrintWriter pw = CalciteSystemProperty.DEBUG.value()
      ? Util.printWriter(System.out)
      : null;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoin.java`
#### Snippet
```java
  private final List<@Nullable ImmutableBitSet> projFields;
  public final ImmutableMap<Integer, ImmutableIntList> joinFieldRefCountsMap;
  private final @Nullable RexNode postJoinFilter;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateJoinTransposeRule.java`
#### Snippet
```java
  private static class Side {
    final Map<Integer, Integer> split = new HashMap<>();
    @Nullable RelNode newInput;
    boolean aggregate;
  }
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/convert/ConverterImpl.java`
#### Snippet
```java

  protected RelTraitSet inTraits;
  protected final @Nullable RelTraitDef traitDef;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableSort.java`
#### Snippet
```java
public class MutableSort extends MutableSingleRel {
  public final RelCollation collation;
  public final @Nullable RexNode offset;
  public final @Nullable RexNode fetch;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableSort.java`
#### Snippet
```java
  public final RelCollation collation;
  public final @Nullable RexNode offset;
  public final @Nullable RexNode fetch;

  private MutableSort(MutableRel input, RelCollation collation,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableMatch.java`
#### Snippet
```java
  public final ImmutableBitSet partitionKeys;
  public final RelCollation orderKeys;
  public final @Nullable RexNode interval;

  private MutableMatch(RelDataType rowType, MutableRel input,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableTableFunctionScan.java`
#### Snippet
```java
public class MutableTableFunctionScan extends MutableMultiRel {
  public final RexNode rexCall;
  public final @Nullable Type elementType;
  public final @Nullable Set<RelColumnMapping> columnMappings;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableTableFunctionScan.java`
#### Snippet
```java
  public final RexNode rexCall;
  public final @Nullable Type elementType;
  public final @Nullable Set<RelColumnMapping> columnMappings;

  private MutableTableFunctionScan(RelOptCluster cluster,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableTableModify.java`
#### Snippet
```java
  public final Operation operation;
  public final @Nullable List<String> updateColumnList;
  public final @Nullable List<RexNode> sourceExpressionList;
  public final boolean flattened;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableTableModify.java`
#### Snippet
```java
  public final RelOptTable table;
  public final Operation operation;
  public final @Nullable List<String> updateColumnList;
  public final @Nullable List<RexNode> sourceExpressionList;
  public final boolean flattened;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableRel.java`
#### Snippet
```java
  protected final MutableRelType type;

  protected @Nullable MutableRel parent;
  protected int ordinalInParent;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
  protected static class ViewPartialRewriting {
    private final RelNode newView;
    private final @Nullable Project newTopViewProject;
    private final RelNode newViewNode;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
    private final Map<RexTableInputRef, Set<RexTableInputRef>> nodeToEquivalenceClass;
    private @Nullable Map<RexTableInputRef, Set<RexTableInputRef>> cacheEquivalenceClassesMap;
    private @Nullable List<Set<RexTableInputRef>> cacheEquivalenceClasses;

    protected EquivalenceClasses() {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java

    private final Map<RexTableInputRef, Set<RexTableInputRef>> nodeToEquivalenceClass;
    private @Nullable Map<RexTableInputRef, Set<RexTableInputRef>> cacheEquivalenceClassesMap;
    private @Nullable List<Set<RexTableInputRef>> cacheEquivalenceClasses;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQueryBase.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  public final @Nullable JaninoRelMetadataProvider metadataProvider;

  //~ Static fields/initializers ---------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQueryBase.java`
#### Snippet
```java
  public final Table<RelNode, Object, Object> map = HashBasedTable.create();

  private final @Nullable MetadataHandlerProvider metadataHandlerProvider;

  @Deprecated // to be removed before 2.0
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java

  /** Visits an Aggregate; called by {@link #dispatch} via reflection. */
  public Result visit(Aggregate e) {
    final Builder builder =
        visitAggregate(e, e.getGroupSet().toList(), Clause.GROUP_BY);
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java

  /** Visits a Window; called by {@link #dispatch} via reflection. */
  public Result visit(Window e) {
    final Result x = visitInput(e, 0);
    final Builder builder = x.builder(e);
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPredicates.java`
#### Snippet
```java
    final Set<RexNode> equalityPredicates;
    final @Nullable RexNode leftChildPredicates;
    final @Nullable RexNode rightChildPredicates;
    final RexSimplify simplify;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPredicates.java`
#### Snippet
```java
      final BitSet[] columnSets;
      final int[] iterationIdx;
      @Nullable Mapping nextMapping;
      boolean firstCall;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPredicates.java`
#### Snippet
```java
    final Set<RexNode> allExprs;
    final Set<RexNode> equalityPredicates;
    final @Nullable RexNode leftChildPredicates;
    final @Nullable RexNode rightChildPredicates;
    final RexSimplify simplify;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJsonWriter.java`
#### Snippet
```java
  protected final List<@Nullable Object> relList;
  private final List<Pair<String, @Nullable Object>> values = new ArrayList<>();
  private @Nullable String previousId;

  //~ Constructors -------------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
    public final Context context;
    final boolean anon;
    private final @Nullable Map<String, RelDataType> aliases;

    public Builder(RelNode rel, List<Clause> clauses, SqlSelect select,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
     * expression. */
    private final ImmutableSet<Clause> expectedClauses;
    private final @Nullable RelNode expectedRel;
    private final boolean needNew;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
    final SqlNode node;
    final @Nullable String neededAlias;
    private final @Nullable RelDataType neededType;
    private final Map<String, RelDataType> aliases;
    final List<Clause> clauses;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
  public class Result {
    final SqlNode node;
    final @Nullable String neededAlias;
    private final @Nullable RelDataType neededType;
    private final Map<String, RelDataType> aliases;
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rex/RexVisitorImpl.java`
#### Snippet
```java
  //~ Methods ----------------------------------------------------------------

  @Override public R visitInputRef(RexInputRef inputRef) {
    return null;
  }
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rex/RexVisitorImpl.java`
#### Snippet
```java
  }

  @Override public R visitCall(RexCall call) {
    if (!deep) {
      return null;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rex/RexChecker.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  protected final RelNode.@Nullable Context context;
  protected final Litmus litmus;
  protected final List<RelDataType> inputTypeList;
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rex/RexVisitor.java`
#### Snippet
```java
  //~ Methods ----------------------------------------------------------------

  R visitInputRef(RexInputRef inputRef);

  R visitLocalRef(RexLocalRef localRef);
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rex/RexVisitor.java`
#### Snippet
```java
  R visitLiteral(RexLiteral literal);

  R visitCall(RexCall call);

  R visitOver(RexOver over);
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
  private final List<@Nullable String> projectNameList = new ArrayList<>();
  @SuppressWarnings("unused")
  private final @Nullable RexSimplify simplify;
  private @Nullable RexLocalRef conditionRef = null;
  private boolean validating;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
  @SuppressWarnings("unused")
  private final @Nullable RexSimplify simplify;
  private @Nullable RexLocalRef conditionRef = null;
  private boolean validating;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJsonReader.java`
#### Snippet
```java
  private final RelJson relJson;
  private final Map<String, RelNode> relMap = new LinkedHashMap<>();
  private @Nullable RelNode lastRel;

  public RelJsonReader(RelOptCluster cluster, RelOptSchema relOptSchema,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rex/RexExecutable.java`
#### Snippet
```java
  private final Function1<DataContext, @Nullable Object @Nullable []> compiledFunction;
  private final String code;
  private @Nullable DataContext dataContext;

  public RexExecutable(String code, Object reason) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rex/RexCall.java`
#### Snippet
```java
   * Cache of normalized variables used for #equals and #hashCode.
   */
  private @Nullable Pair<SqlOperator, List<RexNode>> normalized;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
   * doesn't really matter how the values are stored.
   */
  private final @Nullable Comparable value;

  /**
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
  }

  @Override public RexNode visitCall(final RexCall call) {
    boolean[] update = {false};
    List<RexNode> clonedOperands = visitList(call.operands, update);
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
  }

  @Override public RexNode visitInputRef(RexInputRef inputRef) {
    return inputRef;
  }
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java

  private final Map<String, Constructor> constructorMap = new HashMap<>();
  private final @Nullable JsonBuilder jsonBuilder;
  private final InputTranslator inputTranslator;
  private final SqlOperatorTable operatorTable;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rel/metadata/CachingRelMetadataProvider.java`
#### Snippet
```java
    long timestamp;

    @Nullable Object result;
  }

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
   * The optional condition. If null, the calculator does not filter rows.
   */
  private final @Nullable RexLocalRef condition;

  private final RelDataType inputRowType;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
  SqlNode condition;
  SqlNode source;
  @Nullable SqlUpdate updateCall;
  @Nullable SqlInsert insertCall;
  @Nullable SqlSelect sourceSelect;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
  SqlNode source;
  @Nullable SqlUpdate updateCall;
  @Nullable SqlInsert insertCall;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
  @Nullable SqlInsert insertCall;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
  @Nullable SqlUpdate updateCall;
  @Nullable SqlInsert insertCall;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlJdbcDataTypeName.java`
#### Snippet
```java
  SQL_INTERVAL_SECOND(TimeUnitRange.SECOND);

  private final @Nullable TimeUnitRange range;
  private final @Nullable SqlTypeName typeName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlJdbcDataTypeName.java`
#### Snippet
```java

  private final @Nullable TimeUnitRange range;
  private final @Nullable SqlTypeName typeName;

  SqlJdbcDataTypeName(SqlTypeName typeName) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlCallBinding.java`
#### Snippet
```java

  private final SqlValidator validator;
  private final @Nullable SqlValidatorScope scope;
  private final SqlCall call;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlWithItem.java`
#### Snippet
```java
public class SqlWithItem extends SqlCall {
  public SqlIdentifier name;
  public @Nullable SqlNodeList columnList; // may be null
  public SqlNode query;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlAlter.java`
#### Snippet
```java

  /** Scope of the operation. Values "SYSTEM" and "SESSION" are typical. */
  @Nullable String scope;

  protected SqlAlter(SqlParserPos pos) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlIdentifier.java`
#### Snippet
```java
   * A list of the positions of the components of compound identifiers.
   */
  protected @Nullable ImmutableList<SqlParserPos> componentPositions;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlIdentifier.java`
#### Snippet
```java
   * This identifier's collation (if any).
   */
  final @Nullable SqlCollation collation;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalQualifier.java`
#### Snippet
```java

  private final int startPrecision;
  public final @Nullable String timeFrameName;
  public final TimeUnitRange timeUnitRange;
  private final int fractionalSecondPrecision;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlBasicCall.java`
#### Snippet
```java
  private SqlOperator operator;
  private List<@Nullable SqlNode> operandList;
  private final @Nullable SqlLiteral functionQuantifier;

  @Deprecated // to be removed before 2.0
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ExplicitOperatorBinding.java`
#### Snippet
```java

  private final List<RelDataType> types;
  private final @Nullable SqlOperatorBinding delegate;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlNumericLiteral.java`
#### Snippet
```java

  private @Nullable Integer prec;
  private @Nullable Integer scale;
  private boolean isExact;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlNumericLiteral.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private @Nullable Integer prec;
  private @Nullable Integer scale;
  private boolean isExact;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDelete.java`
#### Snippet
```java

  SqlNode targetTable;
  @Nullable SqlNode condition;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDelete.java`
#### Snippet
```java
  SqlNode targetTable;
  @Nullable SqlNode condition;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDelete.java`
#### Snippet
```java
  @Nullable SqlNode condition;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
  private static class SearchExpandingShuttle extends RexShuttle {
    private final RexBuilder rexBuilder;
    private final @Nullable RexProgram program;
    private final int maxComplexity;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlFunction.java`
#### Snippet
```java
  private final SqlFunctionCategory category;

  private final @Nullable SqlIdentifier sqlIdentifier;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

  /** The name of the window being declared. */
  @Nullable SqlIdentifier declName;

  /** The name of the window being referenced, or null. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

  /** Whether to allow partial results. It may be null. */
  @Nullable SqlLiteral allowPartial;

  private @Nullable SqlCall windowCall = null;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

  /** The upper bound of the window. */
  @Nullable SqlNode upperBound;

  /** Whether to allow partial results. It may be null. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
  @Nullable SqlLiteral allowPartial;

  private @Nullable SqlCall windowCall = null;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

  /** The lower bound of the window. */
  @Nullable SqlNode lowerBound;

  /** The upper bound of the window. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

  /** The name of the window being referenced, or null. */
  @Nullable SqlIdentifier refName;

  /** The list of partitioning columns. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlBasicTypeNameSpec.java`
#### Snippet
```java
  private int scale;

  private @Nullable String charSetName;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
  protected final @Nullable String identifierQuoteString;
  protected final @Nullable String identifierEndQuoteString;
  protected final @Nullable String identifierEscapedQuote;
  protected final String literalQuoteString;
  protected final String literalEndQuoteString;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  protected final @Nullable String identifierQuoteString;
  protected final @Nullable String identifierEndQuoteString;
  protected final @Nullable String identifierEscapedQuote;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
    private final String literalQuoteString;
    private final String literalEscapedQuoteString;
    private final @Nullable String identifierQuoteString;
    private final @Nullable String identifierEscapedQuoteString;
    private final Casing unquotedCasing;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
    private final DatabaseProduct databaseProduct;
    private final @Nullable String databaseProductName;
    private final @Nullable String databaseVersion;
    private final int databaseMajorVersion;
    private final int databaseMinorVersion;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java

  protected final @Nullable String identifierQuoteString;
  protected final @Nullable String identifierEndQuoteString;
  protected final @Nullable String identifierEscapedQuote;
  protected final String literalQuoteString;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
  private static class ContextImpl implements Context {
    private final DatabaseProduct databaseProduct;
    private final @Nullable String databaseProductName;
    private final @Nullable String databaseVersion;
    private final int databaseMajorVersion;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
    private final String literalEscapedQuoteString;
    private final @Nullable String identifierQuoteString;
    private final @Nullable String identifierEscapedQuoteString;
    private final Casing unquotedCasing;
    private final Casing quotedCasing;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDataTypeSpec.java`
#### Snippet
```java

  private final SqlTypeNameSpec typeNameSpec;
  private final @Nullable TimeZone timeZone;

  /** Whether data type allows nulls.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDataTypeSpec.java`
#### Snippet
```java
   * {@code CAST(x AS INTEGER)} preserves the same nullability as {@code x}.
   */
  private final @Nullable Boolean nullable;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlDescribeTable.java`
#### Snippet
```java

  SqlIdentifier table;
  @Nullable SqlIdentifier column;

  /** Creates a SqlDescribeTable. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlOrderBy.java`
#### Snippet
```java
  public final SqlNode query;
  public final SqlNodeList orderList;
  public final @Nullable SqlNode offset;
  public final @Nullable SqlNode fetch;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlOrderBy.java`
#### Snippet
```java
  public final SqlNodeList orderList;
  public final @Nullable SqlNode offset;
  public final @Nullable SqlNode fetch;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  @Nullable SqlNode from;
  @Nullable SqlNode where;
  @Nullable SqlNodeList groupBy;
  @Nullable SqlNode having;
  SqlNodeList windowDecls;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  @Nullable SqlNode qualify;
  @Nullable SqlNodeList orderBy;
  @Nullable SqlNode offset;
  @Nullable SqlNode fetch;
  @Nullable SqlNodeList hints;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  SqlNodeList windowDecls;
  @Nullable SqlNode qualify;
  @Nullable SqlNodeList orderBy;
  @Nullable SqlNode offset;
  @Nullable SqlNode fetch;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  SqlNodeList selectList;
  @Nullable SqlNode from;
  @Nullable SqlNode where;
  @Nullable SqlNodeList groupBy;
  @Nullable SqlNode having;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  @Nullable SqlNode offset;
  @Nullable SqlNode fetch;
  @Nullable SqlNodeList hints;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  SqlNodeList keywordList;
  SqlNodeList selectList;
  @Nullable SqlNode from;
  @Nullable SqlNode where;
  @Nullable SqlNodeList groupBy;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  @Nullable SqlNodeList orderBy;
  @Nullable SqlNode offset;
  @Nullable SqlNode fetch;
  @Nullable SqlNodeList hints;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  @Nullable SqlNode having;
  SqlNodeList windowDecls;
  @Nullable SqlNode qualify;
  @Nullable SqlNodeList orderBy;
  @Nullable SqlNode offset;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSelect.java`
#### Snippet
```java
  @Nullable SqlNode where;
  @Nullable SqlNodeList groupBy;
  @Nullable SqlNode having;
  SqlNodeList windowDecls;
  @Nullable SqlNode qualify;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlLiteral.java`
#### Snippet
```java
   * the typeName, as defined by the {@link #valueMatchesType} method.
   */
  protected final @Nullable Object value;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlInsert.java`
#### Snippet
```java
  SqlNode targetTable;
  SqlNode source;
  @Nullable SqlNodeList columnList;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlGroupedWindowFunction.java`
#### Snippet
```java
public class SqlGroupedWindowFunction extends SqlFunction {
  /** The grouped function, if this an auxiliary function; null otherwise. */
  public final @Nullable SqlGroupedWindowFunction groupFunction;

  /** Creates a SqlGroupedWindowFunction.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
   */
  SqlLiteral conditionType;
  @Nullable SqlNode condition;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
  SqlNodeList sourceExpressionList;
  @Nullable SqlNode condition;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
  @Nullable SqlNode condition;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
  SqlNodeList targetColumnList;
  SqlNodeList sourceExpressionList;
  @Nullable SqlNode condition;
  @Nullable SqlSelect sourceSelect;
  @Nullable SqlIdentifier alias;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlJdbcFunctionCall.java`
#### Snippet
```java
  private @Nullable SqlCall lookupCall;

  private @Nullable SqlNode @Nullable [] thisOperands;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlJdbcFunctionCall.java`
#### Snippet
```java

  private final String jdbcName;
  private final @Nullable MakeCall lookupMakeCallObj;
  private @Nullable SqlCall lookupCall;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlJdbcFunctionCall.java`
#### Snippet
```java
  private final String jdbcName;
  private final @Nullable MakeCall lookupMakeCallObj;
  private @Nullable SqlCall lookupCall;

  private @Nullable SqlNode @Nullable [] thisOperands;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlSetOption.java`
#### Snippet
```java
   * part. Reserved words (currently just 'ON') are converted to
   * identifiers by the parser. */
  @Nullable SqlNode value;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateType.java`
#### Snippet
```java
public class SqlCreateType extends SqlCreate {
  public final SqlIdentifier name;
  public final @Nullable SqlNodeList attributeDefs;
  public final @Nullable SqlDataTypeSpec dataType;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateType.java`
#### Snippet
```java
  public final SqlIdentifier name;
  public final @Nullable SqlNodeList attributeDefs;
  public final @Nullable SqlDataTypeSpec dataType;

  private static final SqlOperator OPERATOR =
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateForeignSchema.java`
#### Snippet
```java
  public final SqlIdentifier name;
  public final @Nullable SqlNode type;
  public final @Nullable SqlNode library;
  private final @Nullable SqlNodeList optionList;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateForeignSchema.java`
#### Snippet
```java
  public final @Nullable SqlNode type;
  public final @Nullable SqlNode library;
  private final @Nullable SqlNodeList optionList;

  private static final SqlOperator OPERATOR =
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateForeignSchema.java`
#### Snippet
```java
public class SqlCreateForeignSchema extends SqlCreate {
  public final SqlIdentifier name;
  public final @Nullable SqlNode type;
  public final @Nullable SqlNode library;
  private final @Nullable SqlNodeList optionList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlKeyConstraint.java`
#### Snippet
```java
      new SqlSpecialOperator("PRIMARY KEY", SqlKind.PRIMARY_KEY);

  private final @Nullable SqlIdentifier name;
  private final SqlNodeList columnList;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCheckConstraint.java`
#### Snippet
```java
      new SqlSpecialOperator("CHECK", SqlKind.CHECK);

  private final @Nullable SqlIdentifier name;
  private final SqlNode expression;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateTable.java`
#### Snippet
```java
public class SqlCreateTable extends SqlCreate {
  public final SqlIdentifier name;
  public final @Nullable SqlNodeList columnList;
  public final @Nullable SqlNode query;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateTable.java`
#### Snippet
```java
  public final SqlIdentifier name;
  public final @Nullable SqlNodeList columnList;
  public final @Nullable SqlNode query;

  private static final SqlOperator OPERATOR =
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateMaterializedView.java`
#### Snippet
```java
public class SqlCreateMaterializedView extends SqlCreate {
  public final SqlIdentifier name;
  public final @Nullable SqlNodeList columnList;
  public final SqlNode query;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlColumnDeclaration.java`
#### Snippet
```java
  public final SqlIdentifier name;
  public final SqlDataTypeSpec dataType;
  public final @Nullable SqlNode expression;
  public final ColumnStrategy strategy;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateView.java`
#### Snippet
```java
public class SqlCreateView extends SqlCreate {
  public final SqlIdentifier name;
  public final @Nullable SqlNodeList columnList;
  public final SqlNode query;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlAttributeDefinition.java`
#### Snippet
```java
  public final SqlIdentifier name;
  public final SqlDataTypeSpec dataType;
  final @Nullable SqlNode expression;
  final @Nullable SqlCollation collation;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlAttributeDefinition.java`
#### Snippet
```java
  public final SqlDataTypeSpec dataType;
  final @Nullable SqlNode expression;
  final @Nullable SqlCollation collation;

  /** Creates a SqlAttributeDefinition; use {@link SqlDdlNodes#attribute}. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
  private @Nullable SqlNode after;
  private SqlNodeList subsetList;
  private @Nullable SqlLiteral rowsPerMatch;
  private SqlNodeList partitionList;
  private SqlNodeList orderList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
  private SqlNodeList partitionList;
  private SqlNodeList orderList;
  private @Nullable SqlLiteral interval;

  /** Creates a SqlMatchRecognize. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
  private SqlNodeList patternDefList;
  private SqlNodeList measureList;
  private @Nullable SqlNode after;
  private SqlNodeList subsetList;
  private @Nullable SqlLiteral rowsPerMatch;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlOperator.java`
#### Snippet
```java

  /** Used to infer the return type of a call to this operator. */
  private final @Nullable SqlReturnTypeInference returnTypeInference;

  /** Used to infer types of unknown operands. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlOperator.java`
#### Snippet
```java

  /** Used to infer types of unknown operands. */
  private final @Nullable SqlOperandTypeInference operandTypeInference;

  /** Used to validate operand types. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/SqlOperator.java`
#### Snippet
```java

  /** Used to validate operand types. */
  private final @Nullable SqlOperandTypeChecker operandTypeChecker;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCase.java`
#### Snippet
```java
 */
public class SqlCase extends SqlCall {
  @Nullable SqlNode value;
  SqlNodeList whenList;
  SqlNodeList thenList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCase.java`
#### Snippet
```java
  SqlNodeList whenList;
  SqlNodeList thenList;
  @Nullable SqlNode elseExpr;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlBasicAggFunction.java`
#### Snippet
```java
 */
public final class SqlBasicAggFunction extends SqlAggFunction {
  private final @Nullable SqlStaticAggFunction staticFun;
  private final Optionality distinctOptionality;
  private final SqlSyntax syntax;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/fun/OracleSqlOperatorTable.java`
#### Snippet
```java
   * The table of contains Oracle-specific operators.
   */
  private static @Nullable OracleSqlOperatorTable instance;

  @Deprecated // to be removed before 2.0
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/sql/type/SqlOperandMetadata.java`
#### Snippet
```java

  /** Returns the types of the parameters. */
  List<RelDataType> paramTypes(RelDataTypeFactory typeFactory);

  /** Returns the names of the parameters. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/BasicSqlType.java`
#### Snippet
```java
  protected final RelDataTypeSystem typeSystem;
  private final @Nullable SqlCollation collation;
  private final @Nullable SerializableCharset wrappedCharset;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/BasicSqlType.java`
#### Snippet
```java
  private final int scale;
  protected final RelDataTypeSystem typeSystem;
  private final @Nullable SqlCollation collation;
  private final @Nullable SerializableCharset wrappedCharset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeName.java`
#### Snippet
```java
  private final boolean special;
  private final int jdbcOrdinal;
  private final @Nullable SqlTypeFamily family;

  SqlTypeName(int signatures, boolean special, int jdbcType,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/AssignableOperandTypeChecker.java`
#### Snippet
```java

  private final List<RelDataType> paramTypes;
  private final @Nullable ImmutableList<String> paramNames;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/sql/type/SqlOperandTypeChecker.java`
#### Snippet
```java
   * @return generated string
   */
  String getAllowedSignatures(SqlOperator op, String opName);

  /** Returns the strategy for making the arguments have consistency types. */
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/sql/type/SqlOperandTypeChecker.java`
#### Snippet
```java
   * @return generated string
   */
  String getAllowedSignatures(SqlOperator op, String opName);

  /** Returns the strategy for making the arguments have consistency types. */
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/sql/type/SqlOperandTypeChecker.java`
#### Snippet
```java
   */
  boolean checkOperandTypes(
      SqlCallBinding callBinding,
      boolean throwOnFailure);

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/ObjectSqlType.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private final @Nullable SqlIdentifier sqlIdentifier;

  private final RelDataTypeComparability comparability;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/ObjectSqlType.java`
#### Snippet
```java
  private final RelDataTypeComparability comparability;

  private @Nullable RelDataTypeFamily family;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/CompositeOperandTypeChecker.java`
#### Snippet
```java
  protected final ImmutableList<@UnknownKeyFor ? extends SqlOperandTypeChecker> allowedRules;
  protected final Composition composition;
  private final @Nullable String allowedSignatures;
  private final @Nullable BiFunction<SqlOperator, String, String> signatureGenerator;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/CompositeOperandTypeChecker.java`
#### Snippet
```java
 */
public class CompositeOperandTypeChecker implements SqlOperandTypeChecker {
  private final @Nullable SqlOperandCountRange range;
  //~ Enums ------------------------------------------------------------------

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/type/CompositeOperandTypeChecker.java`
#### Snippet
```java
  protected final Composition composition;
  private final @Nullable String allowedSignatures;
  private final @Nullable BiFunction<SqlOperator, String, String> signatureGenerator;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/util/SqlString.java`
#### Snippet
```java
  private final String sql;
  private SqlDialect dialect;
  private @Nullable ImmutableList<Integer> dynamicParameters;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisorHint2.java`
#### Snippet
```java
public class SqlAdvisorHint2 extends SqlAdvisorHint {
  /** Replacement string. */
  public final @Nullable String replacement;

  public SqlAdvisorHint2(String id, String @Nullable [] names, String type,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisorHint.java`
#### Snippet
```java
public class SqlAdvisorHint {
  /** Fully qualified object name as string. */
  public final @Nullable String id;
  /** Fully qualified object name as array of names. */
  public final String @Nullable [] names;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisorHint.java`
#### Snippet
```java
  public final @Nullable String id;
  /** Fully qualified object name as array of names. */
  public final String @Nullable [] names;
  /** One of {@link org.apache.calcite.sql.validate.SqlMonikerType}. */
  public final String type;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/parser/StringAndPos.java`
#### Snippet
```java
  public final String sql;
  public final int cursor;
  public final @Nullable SqlParserPos pos;

  private StringAndPos(String sql, int cursor, @Nullable SqlParserPos pos) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
  // Cache for getPreferredCasing
  private @Nullable String prevWord;
  private @Nullable Casing prevPreferredCasing;

  // Reserved words cache
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
    private int endLineNum;
    private int endColumnNum;
    private @Nullable String errorMsg;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
  // Reserved words cache
  private @Nullable Set<String> reservedWordsSet;
  private @Nullable List<String> reservedWordsList;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java

  // Reserved words cache
  private @Nullable Set<String> reservedWordsSet;
  private @Nullable List<String> reservedWordsList;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java

  // Cache for getPreferredCasing
  private @Nullable String prevWord;
  private @Nullable Casing prevPreferredCasing;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/parser/SqlAbstractParserImpl.java`
#### Snippet
```java
  protected int nDynamicParams;

  protected @Nullable String originalSql;

  protected final List<CalciteContextException> warnings = new ArrayList<>();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlSimpleParser.java`
#### Snippet
```java
  public static class Token {
    private final TokenType type;
    private final @Nullable String s;

    Token(TokenType tokenType) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SelectScope.java`
#### Snippet
```java
  protected final List<String> windowNames = new ArrayList<>();

  private @Nullable List<SqlNode> expandedSelectList = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SelectScope.java`
#### Snippet
```java

  /** Scope to use to resolve windows. */
  private final @Nullable SqlValidatorScope windowParent;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/JoinScope.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private final @Nullable SqlValidatorScope usingScope;
  private final SqlJoin join;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlNameMatchers.java`
#### Snippet
```java
  /** Matcher that remembers the requests that were made of it. */
  private static class LiberalNameMatcher extends BaseMatcher {
    @Nullable List<String> matchedNames;

    LiberalNameMatcher() {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/CollectScope.java`
#### Snippet
```java

  @SuppressWarnings("unused")
  private final @Nullable SqlValidatorScope usingScope;
  private final SqlCall child;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/AbstractNamespace.java`
#### Snippet
```java
  protected @Nullable RelDataType type;

  protected final @Nullable SqlNode enclosingNode;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/AbstractNamespace.java`
#### Snippet
```java
   * column. Set on validate.
   */
  protected @Nullable RelDataType rowType;

  /** As {@link #rowType}, but not necessarily a struct. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/AbstractNamespace.java`
#### Snippet
```java

  /** As {@link #rowType}, but not necessarily a struct. */
  protected @Nullable RelDataType type;

  protected final @Nullable SqlNode enclosingNode;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
  protected class FrameImpl implements Frame {
    final FrameType frameType;
    final @Nullable String keyword;
    final String open;
    final String close;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
  private final StringBuilder buf;
  private final Deque<FrameImpl> listStack = new ArrayDeque<>();
  private ImmutableList.@Nullable Builder<Integer> dynamicParameters;
  protected @Nullable FrameImpl frame;
  private boolean needWhitespace;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
  protected @Nullable String nextWhitespace;
  private SqlWriterConfig config;
  private @Nullable Bean bean;
  private int currentIndent;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
  protected @Nullable FrameImpl frame;
  private boolean needWhitespace;
  protected @Nullable String nextWhitespace;
  private SqlWriterConfig config;
  private @Nullable Bean bean;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
  private final Deque<FrameImpl> listStack = new ArrayDeque<>();
  private ImmutableList.@Nullable Builder<Integer> dynamicParameters;
  protected @Nullable FrameImpl frame;
  private boolean needWhitespace;
  protected @Nullable String nextWhitespace;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlQualified.java`
#### Snippet
```java
public class SqlQualified {
  public final int prefixLength;
  public final @Nullable SqlValidatorNamespace namespace;
  public final SqlIdentifier identifier;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/AggVisitor.java`
#### Snippet
```java
  /** Whether to find windowed aggregates. */
  protected final boolean over;
  protected final @Nullable AggFinder delegate;
  /** Whether to find regular (non-windowed) aggregates. */
  protected final boolean aggregate;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
    public final @Nullable SqlCall distinctCall;
    public final @Nullable SqlNodeList distinctList;
    public final @Nullable SqlCall orderCall;
    public final @Nullable SqlNodeList orderList;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
  public static class FlatAggregate {
    public final SqlCall aggregateCall;
    public final @Nullable SqlCall filterCall;
    public final @Nullable SqlNode filter;
    public final @Nullable SqlCall distinctCall;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
    public final @Nullable SqlNodeList distinctList;
    public final @Nullable SqlCall orderCall;
    public final @Nullable SqlNodeList orderList;

    FlatAggregate(SqlCall aggregateCall, @Nullable SqlCall filterCall,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
    public final SqlCall aggregateCall;
    public final @Nullable SqlCall filterCall;
    public final @Nullable SqlNode filter;
    public final @Nullable SqlCall distinctCall;
    public final @Nullable SqlNodeList distinctList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
    public final @Nullable SqlNode filter;
    public final @Nullable SqlCall distinctCall;
    public final @Nullable SqlNodeList distinctList;
    public final @Nullable SqlCall orderCall;
    public final @Nullable SqlNodeList orderList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
    public final @Nullable SqlCall filterCall;
    public final @Nullable SqlNode filter;
    public final @Nullable SqlCall distinctCall;
    public final @Nullable SqlNodeList distinctList;
    public final @Nullable SqlCall orderCall;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/IdentifierNamespace.java`
#### Snippet
```java
   * List of monotonic expressions. Set on validate.
   */
  private @Nullable List<Pair<SqlNode, SqlMonotonicity>> monotonicExprs;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/IdentifierNamespace.java`
#### Snippet
```java
  private final SqlIdentifier id;
  private final SqlValidatorScope parentScope;
  public final @Nullable SqlNodeList extendList;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingScope.java`
#### Snippet
```java

  /** Use while resolving. */
  SqlValidatorUtil.@Nullable GroupAnalyzer groupAnalyzer;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/Driver.java`
#### Snippet
```java
  public static final String CONNECT_STRING_PREFIX = "jdbc:calcite:";

  protected final @Nullable Supplier<CalcitePrepare> prepareFactory;

  static {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CachingCalciteSchema.java`
#### Snippet
```java
   * @param <T> element type */
  private abstract class AbstractCached<T> implements Cached<T> {
    @Nullable T t;
    boolean built = false;

```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/jdbc/JavaTypeFactoryImpl.java`
#### Snippet
```java
  }

  @Override public RelDataType toSql(RelDataType type) {
    return toSql(this, type);
  }
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/JavaTypeFactoryImpl.java`
#### Snippet
```java
  public static class SyntheticRecordType implements Types.RecordType {
    final List<Types.RecordField> fields = new ArrayList<>();
    final @Nullable RelDataType relType;
    private final String name;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
  class CalciteSignature<T> extends Meta.Signature {
    @JsonIgnore public final @Nullable RelDataType rowType;
    @JsonIgnore public final @Nullable CalciteSchema rootSchema;
    @JsonIgnore private final List<RelCollation> collationList;
    private final long maxRowCount;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
    public final @Nullable ImmutableList<String> tablePath;
    public final @Nullable RexNode constraint;
    public final @Nullable ImmutableIntList columnMapping;
    public final boolean modifiable;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
   * interface. */
  class Dummy {
    private static @Nullable SparkHandler sparkHandler;

    private Dummy() {}
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
  class AnalyzeViewResult extends ConvertResult {
    /** Not null if and only if the view is modifiable. */
    public final @Nullable Table table;
    public final @Nullable ImmutableList<String> tablePath;
    public final @Nullable RexNode constraint;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
  class Query<T> {
    public final @Nullable String sql;
    public final @Nullable Queryable<T> queryable;
    public final @Nullable RelNode rel;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
   * @param <T> element type */
  class Query<T> {
    public final @Nullable String sql;
    public final @Nullable Queryable<T> queryable;
    public final @Nullable RelNode rel;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
    @JsonIgnore private final List<RelCollation> collationList;
    private final long maxRowCount;
    private final @Nullable Bindable<T> bindable;

    @Deprecated // to be removed before 2.0
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
    public final @Nullable Table table;
    public final @Nullable ImmutableList<String> tablePath;
    public final @Nullable RexNode constraint;
    public final @Nullable ImmutableIntList columnMapping;
    public final boolean modifiable;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
    /** Not null if and only if the view is modifiable. */
    public final @Nullable Table table;
    public final @Nullable ImmutableList<String> tablePath;
    public final @Nullable RexNode constraint;
    public final @Nullable ImmutableIntList columnMapping;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
   * @param <T> element type */
  class CalciteSignature<T> extends Meta.Signature {
    @JsonIgnore public final @Nullable RelDataType rowType;
    @JsonIgnore public final @Nullable CalciteSchema rootSchema;
    @JsonIgnore private final List<RelCollation> collationList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
    public final @Nullable String sql;
    public final @Nullable Queryable<T> queryable;
    public final @Nullable RelNode rel;

    private Query(@Nullable String sql, @Nullable Queryable<T> queryable, @Nullable RelNode rel) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalciteSchema.java`
#### Snippet
```java
  protected final NameMap<FunctionEntry> nullaryFunctionMap;
  protected final NameMap<CalciteSchema> subSchemaMap;
  private @Nullable List<? extends List<String>> path;

  protected CalciteSchema(@Nullable CalciteSchema parent, Schema schema,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalciteSchema.java`
#### Snippet
```java
public abstract class CalciteSchema {

  private final @Nullable CalciteSchema parent;
  public final Schema schema;
  public final String name;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalciteConnectionImpl.java`
#### Snippet
```java
      implements CalciteServerStatement {
    private final CalciteConnectionImpl connection;
    private @Nullable Iterator<Object> iterator;
    private Meta.@Nullable Signature signature;
    private final AtomicBoolean cancelFlag = new AtomicBoolean();
```

### NullableProblems
Non-annotated parameter 'schema' in method 'setSchema' from 'AvaticaConnection' should not override non-null parameter from 'CalciteConnection'
in `core/src/main/java/org/apache/calcite/jdbc/CalciteConnectionImpl.java`
#### Snippet
```java
abstract class CalciteConnectionImpl
    extends AvaticaConnection
    implements CalciteConnection, QueryProvider {
  public final JavaTypeFactory typeFactory;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalciteConnectionImpl.java`
#### Snippet
```java
    private final CalciteConnectionImpl connection;
    private @Nullable Iterator<Object> iterator;
    private Meta.@Nullable Signature signature;
    private final AtomicBoolean cancelFlag = new AtomicBoolean();

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalciteConnectionImpl.java`
#### Snippet
```java
  static class DataContextImpl implements DataContext {
    private final ImmutableMap<Object, Object> map;
    private final @Nullable CalciteSchema rootSchema;
    private final QueryProvider queryProvider;
    private final JavaTypeFactory typeFactory;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptListener.java`
#### Snippet
```java
   */
  abstract class RelEvent extends EventObject {
    private final @Nullable RelNode rel;

    protected RelEvent(Object eventSource, @Nullable RelNode rel) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
  private @MonotonicNonNull RuleAttemptsListener ruleAttemptsListener;

  private @Nullable Pattern ruleDescExclusionFilter;

  protected final AtomicBoolean cancelFlag;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
  protected final Context context;

  private @Nullable RexExecutor executor;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelTraitSet.java`
#### Snippet
```java
  private final Cache cache;
  private final RelTrait[] traits;
  private @Nullable String string;
  /** Caches the hash code for the traits. */
  private int hash; // Default to 0
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptRuleOperand.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private @Nullable RelOptRuleOperand parent;
  private @NotOnlyInitialized RelOptRule rule;
  private final Predicate<RelNode> predicate;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptRuleOperand.java`
#### Snippet
```java
  public int ordinalInParent;
  public int ordinalInRule;
  public final @Nullable RelTrait trait;
  private final Class<? extends RelNode> clazz;
  private final ImmutableList<RelOptRuleOperand> children;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/jdbc/CalciteMetaImpl.java`
#### Snippet
```java
        new Meta.PrepareCallback() {
          long updateCount;
          @Nullable Signature signature;

          @Override public Object getMonitor() {
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/plan/RelOptRule.java`
#### Snippet
```java
   * @see #matches(RelOptRuleCall)
   */
  public abstract void onMatch(RelOptRuleCall call);

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptRuleCall.java`
#### Snippet
```java
  public final RelNode[] rels;
  private final RelOptPlanner planner;
  private final @Nullable List<RelNode> parents;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterialization.java`
#### Snippet
```java
  /** A table scan and optional project mapping and filter condition. */
  private static class ProjectFilterTable {
    final @Nullable RexNode condition;
    final Mappings.@Nullable TargetMapping mapping;
    final TableScan scan;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterialization.java`
#### Snippet
```java
  private static class ProjectFilterTable {
    final @Nullable RexNode condition;
    final Mappings.@Nullable TargetMapping mapping;
    final TableScan scan;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterialization.java`
#### Snippet
```java
  public final RelNode tableRel;
  public final @Nullable RelOptTable starRelOptTable;
  public final @Nullable StarTable starTable;
  public final List<String> qualifiedTableName;
  public final RelNode queryRel;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterialization.java`
#### Snippet
```java
public class RelOptMaterialization {
  public final RelNode tableRel;
  public final @Nullable RelOptTable starRelOptTable;
  public final @Nullable StarTable starTable;
  public final List<String> qualifiedTableName;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptCluster.java`
#### Snippet
```java
  private @Nullable HintStrategyTable hintStrategies;
  private final RelTraitSet emptyTraitSet;
  private @Nullable RelMetadataQuery mq;
  private Supplier<RelMetadataQuery> mqSupplier;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptCluster.java`
#### Snippet
```java
  @Deprecated // to be removed before 2.0
  private MetadataFactory metadataFactory;
  private @Nullable HintStrategyTable hintStrategies;
  private final RelTraitSet emptyTraitSet;
  private @Nullable RelMetadataQuery mq;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/hep/HepInstruction.java`
#### Snippet
```java
    class State extends HepState {
      /** Rule looked up by planner from description. */
      @Nullable RelOptRule rule;

      State(PrepareContext px) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/hep/HepInstruction.java`
#### Snippet
```java
    /** State for a {@link CommonRelSubExprRules} instruction. */
    class State extends HepState {
      @Nullable Set<RelOptRule> ruleSet;

      State(PrepareContext px) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/hep/HepInstruction.java`
#### Snippet
```java
      /** Actual rule set instantiated during planning by filtering all the
       * planner's rules through {@link #ruleClass}. */
      @Nullable Set<RelOptRule> ruleSet;

      State(PrepareContext px) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/hep/HepProgram.java`
#### Snippet
```java
    int matchLimit = MATCH_UNTIL_FIXPOINT;
    HepMatchOrder matchOrder = HepMatchOrder.DEPTH_FIRST;
    HepInstruction.EndGroup.@Nullable State group;

    State(PrepareContext px, List<HepInstruction> instructions) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/hep/HepPlanner.java`
#### Snippet
```java
  private @Nullable HepRelVertex root;

  private @Nullable RelTraitSet requestedRootTraits;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/hep/HepPlanner.java`
#### Snippet
```java
  private final HepProgram mainProgram;

  private @Nullable HepRelVertex root;

  private @Nullable RelTraitSet requestedRootTraits;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSubset.java`
#### Snippet
```java
   * so as to avoid duplicate invocation.
   */
  @Nullable Set<RelNode> passThroughCache;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSubset.java`
#### Snippet
```java

  /** Best known plan. */
  @Nullable RelNode best;

  /** Timestamp for metadata validity. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSubset.java`
#### Snippet
```java

  /** Optimization task state. */
  @Nullable OptimizeState taskState;

  /** Cost of best known plan (it may have improved since). */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/TopDownRuleDriver.java`
#### Snippet
```java
    private RelOptCost upperForInput;
    private int processingChild;
    private @Nullable List<RelOptCost> lowerBounds;
    private @Nullable RelOptCost lowerBoundSum;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/TopDownRuleDriver.java`
#### Snippet
```java
  private class CheckInput implements Task {

    private final @Nullable OptimizeInputs context;
    private final RelOptCost upper;
    private final RelNode parent;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/TopDownRuleDriver.java`
#### Snippet
```java
   * are registered during task performing.
   */
  private @Nullable GeneratorTask applying = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/TopDownRuleDriver.java`
#### Snippet
```java
    private int processingChild;
    private @Nullable List<RelOptCost> lowerBounds;
    private @Nullable RelOptCost lowerBoundSum;

    OptimizeInputs(RelNode rel, RelSubset group) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/visualizer/NodeUpdateHelper.java`
#### Snippet
```java

  private final String key;
  private final @Nullable RelNode rel;
  private final NodeUpdateInfo state;
  private @Nullable NodeUpdateInfo update = null;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/visualizer/NodeUpdateHelper.java`
#### Snippet
```java
  private final @Nullable RelNode rel;
  private final NodeUpdateInfo state;
  private @Nullable NodeUpdateInfo update = null;

  NodeUpdateHelper(String key, @Nullable RelNode rel) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoRuleCall.java`
#### Snippet
```java
   * List of {@link RelNode} generated by this call. For debugging purposes.
   */
  private @Nullable List<RelNode> generatedRelList;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSet.java`
#### Snippet
```java
   * Exploring state of current RelSet.
   */
  @Nullable ExploringState exploringState;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelRule.java`
#### Snippet
```java
    private final Class<R> relClass;
    final OperandBuilderImpl inputBuilder = new OperandBuilderImpl();
    private @Nullable RelTrait trait;
    private Predicate<? super R> predicate = r -> true;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
  private @MonotonicNonNull RelNode originalRoot;

  private @Nullable Convention rootConvention;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/visualizer/RuleMatchVisualizer.java`
#### Snippet
```java
  // core/src/main/resources/org/apache/calcite/plan/visualizer/viz-template.html
  private final String templateDirectory = "org/apache/calcite/plan/visualizer";
  private final @Nullable String outputDirectory;
  private final @Nullable String outputSuffix;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/visualizer/RuleMatchVisualizer.java`
#### Snippet
```java
  private boolean initialized = false;

  private @Nullable RelOptPlanner planner = null;

  private boolean includeTransitiveEdges = false;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/visualizer/RuleMatchVisualizer.java`
#### Snippet
```java
  private final String templateDirectory = "org/apache/calcite/plan/visualizer";
  private final @Nullable String outputDirectory;
  private final @Nullable String outputSuffix;

  private String latestRuleID = "";
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
  public static class InputFinder extends RexVisitorImpl<Void> {
    private final ImmutableBitSet.Builder bitBuilder;
    private final @Nullable Set<RelDataTypeField> extraFields;

    private InputFinder(@Nullable Set<RelDataTypeField> extraFields,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    private final @Nullable List<RelDataTypeField> srcFields;
    protected final @Nullable List<RelDataTypeField> destFields;
    private final @Nullable List<RelDataTypeField> leftDestFields;
    private final @Nullable List<RelDataTypeField> rightDestFields;
    private final int nLeftDestFields;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
        LinkedHashMultimap.create();
    @NotOnlyInitialized
    private final @Nullable RelShuttle relShuttle;

    public VariableUsedVisitor(@UnknownInitialization @Nullable RelShuttle relShuttle) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    protected final RexBuilder rexBuilder;
    private final @Nullable List<RelDataTypeField> srcFields;
    protected final @Nullable List<RelDataTypeField> destFields;
    private final @Nullable List<RelDataTypeField> leftDestFields;
    private final @Nullable List<RelDataTypeField> rightDestFields;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    protected final @Nullable List<RelDataTypeField> destFields;
    private final @Nullable List<RelDataTypeField> leftDestFields;
    private final @Nullable List<RelDataTypeField> rightDestFields;
    private final int nLeftDestFields;
    private final int[] adjustments;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
  public static class RexInputConverter extends RexShuttle {
    protected final RexBuilder rexBuilder;
    private final @Nullable List<RelDataTypeField> srcFields;
    protected final @Nullable List<RelDataTypeField> destFields;
    private final @Nullable List<RelDataTypeField> leftDestFields;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Filterator.java`
#### Snippet
```java
  Class<E> includeFilter;
  Iterator<?> iterator;
  @Nullable E lookAhead;
  boolean ready;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorScope.java`
#### Snippet
```java
  class Step extends Path {
    final Path parent;
    final @Nullable RelDataType rowType;
    public final int i;
    public final String name;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorScope.java`
#### Snippet
```java
    public final SqlValidatorNamespace namespace;
    private final boolean nullable;
    public final @Nullable SqlValidatorScope scope; // may be null
    public final Path path;
    /** Names not matched; empty if it was a full match. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/ChunkList.java`
#### Snippet
```java
  /** Iterator over a {@link ChunkList}. */
  private class ChunkListIterator implements ListIterator<E> {
    private E @Nullable [] chunk;
    /** Offset in the list of the first element of this chunk. */
    private int start;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/ChunkList.java`
#### Snippet
```java
  private int size;
  private E @Nullable [] first;
  private E @Nullable [] last;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/ChunkList.java`
#### Snippet
```java

  private int size;
  private E @Nullable [] first;
  private E @Nullable [] last;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/NlsString.java`
#### Snippet
```java
  private final @Nullable String charsetName;
  private final @Nullable Charset charset;
  private final @Nullable SqlCollation collation;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/NlsString.java`
#### Snippet
```java
  private final @Nullable String stringValue;
  @JsonProperty("valueBytes")
  private final @Nullable ByteString bytesValue;
  private final @Nullable String charsetName;
  private final @Nullable Charset charset;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/NlsString.java`
#### Snippet
```java
  private final @Nullable ByteString bytesValue;
  private final @Nullable String charsetName;
  private final @Nullable Charset charset;
  private final @Nullable SqlCollation collation;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/NlsString.java`
#### Snippet
```java
              });

  private final @Nullable String stringValue;
  @JsonProperty("valueBytes")
  private final @Nullable ByteString bytesValue;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/NlsString.java`
#### Snippet
```java
  @JsonProperty("valueBytes")
  private final @Nullable ByteString bytesValue;
  private final @Nullable String charsetName;
  private final @Nullable Charset charset;
  private final @Nullable SqlCollation collation;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Sources.java`
#### Snippet
```java
   * {@link URL}. */
  private static class FileSource implements Source {
    private final @Nullable File file;
    private final URL url;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/MonotonicSupplier.java`
#### Snippet
```java
 */
public class MonotonicSupplier<E> implements Consumer<E>, Supplier<E> {
  private @Nullable E e;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/PartiallyOrderedSet.java`
#### Snippet
```java
  private final @Nullable Function<E, Iterable<E>> parentFunction;
  @SuppressWarnings("unused")
  private final @Nullable Function<E, Iterable<E>> childFunction;
  private final Ordering<E> ordering;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/PartiallyOrderedSet.java`
#### Snippet
```java

  private final Map<E, Node<E>> map;
  private final @Nullable Function<E, Iterable<E>> parentFunction;
  @SuppressWarnings("unused")
  private final @Nullable Function<E, Iterable<E>> childFunction;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/ImmutableBitSet.java`
#### Snippet
```java
  /** Builder. */
  public static class Builder {
    private long @Nullable [] words;

    private Builder(long[] words) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/PrecedenceClimbingParser.java`
#### Snippet
```java
public class PrecedenceClimbingParser {
  private @Nullable Token first;
  private @Nullable Token last;

  private PrecedenceClimbingParser(List<Token> tokens) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/PrecedenceClimbingParser.java`
#### Snippet
```java
 */
public class PrecedenceClimbingParser {
  private @Nullable Token first;
  private @Nullable Token last;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/PrecedenceClimbingParser.java`
#### Snippet
```java
    @Nullable Token next;
    public final Type type;
    public final @Nullable Object o;
    final int left;
    final int right;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/PrecedenceClimbingParser.java`
#### Snippet
```java
  public static class Token {
    @Nullable Token previous;
    @Nullable Token next;
    public final Type type;
    public final @Nullable Object o;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/PrecedenceClimbingParser.java`
#### Snippet
```java
   * or an unmatched operator. */
  public static class Token {
    @Nullable Token previous;
    @Nullable Token next;
    public final Type type;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * (See <a href="http://c2.com/cgi/wiki?BuilderPattern">GoF</a>.)
   */
  @Nullable Glossary BUILDER_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?MediatorPattern">GoF</a>.)
   */
  @Nullable Glossary MEDIATOR_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * GoF</a>.)
   */
  @Nullable Glossary INTERPRETER_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * (See <a href="http://c2.com/cgi/wiki?MementoPattern">GoF</a>.)
   */
  @Nullable Glossary MEMENTO_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?AbstractFactoryPattern">GoF</a>.)
   */
  @Nullable Glossary ABSTRACT_FACTORY_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * to it. (See <a href="http://c2.com/cgi/wiki?ProxyPattern">GoF</a>.)
   */
  @Nullable Glossary PROXY_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?PrototypePattern">GoF</a>.)
   */
  @Nullable Glossary PROTOTYPE_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?TemplateMethodPattern">GoF</a>.)
   */
  @Nullable Glossary TEMPLATE_METHOD_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?CommandPattern">GoF</a>.)
   */
  @Nullable Glossary COMMAND_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * (See <a href="http://c2.com/cgi/wiki?ObserverPattern">GoF</a>.)
   */
  @Nullable Glossary OBSERVER_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * it!
   */
  @Nullable Glossary SINGLETON_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?VisitorPattern">GoF</a>.)
   */
  @Nullable Glossary VISITOR_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * GoF</a>.)
   */
  @Nullable Glossary FACTORY_METHOD_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?DecoratorPattern">GoF</a>.)
   */
  @Nullable Glossary DECORATOR_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   */
  // CHECKSTYLE: ON
  @Nullable Glossary PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * GoF</a>.)
   */
  @Nullable Glossary BRIDGE_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?IteratorPattern">GoF</a>.)
   */
  @Nullable Glossary ITERATOR_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * inline.
   */
  @Nullable Glossary SQL92 = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * GoF</a>.)
   */
  @Nullable Glossary CHAIN_OF_RESPONSIBILITY_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * (See <a href="http://c2.com/cgi/wiki?FacadePattern">GoF</a>.)
   */
  @Nullable Glossary FACADE_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * GoF</a>.)
   */
  @Nullable Glossary COMPOSITE_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * inline.
   */
  @Nullable Glossary SQL2003 = null;
}

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?AdapterPattern">GoF</a>.)
   */
  @Nullable Glossary ADAPTER_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * use it. (See <a href="http://c2.com/cgi/wiki?StrategyPattern">GoF</a>.)
   */
  @Nullable Glossary STRATEGY_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * (See <a href="http://c2.com/cgi/wiki?FlyweightPattern">GoF</a>.)
   */
  @Nullable Glossary FLYWEIGHT_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * inline.
   */
  @Nullable Glossary SQL99 = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Glossary.java`
#### Snippet
```java
   * href="http://c2.com/cgi/wiki?StatePattern">GoF</a>.)
   */
  @Nullable Glossary STATE_PATTERN = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
  static class StringEscaper implements Cloneable {
    private @Nullable List<@Nullable String> translationVector;
    private String @Nullable [] translationTable;

    public static final StringEscaper XML_ESCAPER;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
   */
  static class StringEscaper implements Cloneable {
    private @Nullable List<@Nullable String> translationVector;
    private String @Nullable [] translationTable;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Pair.java`
#### Snippet
```java

  public final T1 left;
  public final T2 right;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Pair.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  public final T1 left;
  public final T2 right;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/trace/CalciteTimingTracer.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private final @Nullable Logger logger;

  private long lastNanoTime;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/javac/JaninoCompiler.java`
#### Snippet
```java
    @Nullable String destdir;
    @Nullable String fullClassName;
    @Nullable String source;

    public JaninoCompilerArgs() {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/javac/JaninoCompiler.java`
#### Snippet
```java
  public static class JaninoCompilerArgs extends JavaCompilerArgs {
    @Nullable String destdir;
    @Nullable String fullClassName;
    @Nullable String source;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/javac/JaninoCompiler.java`
#### Snippet
```java

  // REVIEW jvs 28-June-2004:  pool this instance?  Is it thread-safe?
  private @Nullable AccountingClassLoader classLoader;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/javac/JaninoCompiler.java`
#### Snippet
```java
   */
  private static class AccountingClassLoader extends JavaSourceClassLoader {
    private final @Nullable File destDir;
    private int nBytes;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/javac/JaninoCompiler.java`
#### Snippet
```java
   */
  public static class JaninoCompilerArgs extends JavaCompilerArgs {
    @Nullable String destdir;
    @Nullable String fullClassName;
    @Nullable String source;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonCustomTable.java`
#### Snippet
```java
   * <p>May be a JSON object (represented as Map) or null.
   */
  public final @Nullable Map<String, Object> operand;

  @JsonCreator
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonCustomSchema.java`
#### Snippet
```java
   * <p>May be a JSON object (represented as Map) or null.
   */
  public final @Nullable Map<String, Object> operand;

  @JsonCreator
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonMaterialization.java`
#### Snippet
```java
public class JsonMaterialization {
  public final @Nullable String view;
  public final @Nullable String table;

  /** SQL query that defines the materialization.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonMaterialization.java`
#### Snippet
```java
 */
public class JsonMaterialization {
  public final @Nullable String view;
  public final @Nullable String table;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonMaterialization.java`
#### Snippet
```java
  public final Object sql;

  public final @Nullable List<String> viewSchemaPath;

  @JsonCreator
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonMeasure.java`
#### Snippet
```java
   * the lattice (using "{@code AS alias}" if necessary).
   */
  public final @Nullable Object args;

  @JsonCreator
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonJdbcSchema.java`
#### Snippet
```java
   * {@link java.sql.DriverManager} chooses.
   */
  public final @Nullable String sqlDialectFactory;

  /** JDBC connect string, for example "jdbc:mysql://localhost/foodmart".
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonJdbcSchema.java`
#### Snippet
```java
   * <p>Optional.
   */
  public final @Nullable String jdbcCatalog;

  /** Name of the initial schema in the JDBC data source.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonJdbcSchema.java`
#### Snippet
```java
   * {@link java.sql.DriverManager} chooses.
   */
  public final @Nullable String jdbcDriver;

  /** The FQN of the {@link org.apache.calcite.sql.SqlDialectFactory} implementation.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonJdbcSchema.java`
#### Snippet
```java
   * <p>Optional.
   */
  public final @Nullable String jdbcSchema;

  @JsonCreator
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonJdbcSchema.java`
#### Snippet
```java
   * <p>Optional.
   */
  public final @Nullable String jdbcPassword;

  /** Name of the initial catalog in the JDBC data source.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonJdbcSchema.java`
#### Snippet
```java
   * <p>Optional.
   */
  public final @Nullable String jdbcUser;

  /** JDBC connect string, for example "jdbc:mysql://localhost/foodmart".
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonSchema.java`
#### Snippet
```java
   * immediately, and are never flushed.
   */
  public final @Nullable Boolean cache;

  /** Whether to create lattices in this schema based on queries occurring in
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonSchema.java`
#### Snippet
```java
   * string.
   */
  public final @Nullable List<Object> path;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonSchema.java`
#### Snippet
```java
  /** Whether to create lattices in this schema based on queries occurring in
   * other schemas. Default value is {@code false}. */
  public final @Nullable Boolean autoLattice;

  protected JsonSchema(String name, @Nullable List<Object> path, @Nullable Boolean cache,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonFunction.java`
#### Snippet
```java
   * <p>Optional.
   */
  public final @Nullable List<String> path;

  @JsonCreator
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonFunction.java`
#### Snippet
```java
   * if found, creates an aggregate function.
   */
  public final @Nullable String methodName;

  /** Path for resolving this function.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonTable.java`
#### Snippet
```java
  /** Information about whether the table can be streamed, and if so, whether
   * the history of the table is also available. */
  public final @Nullable JsonStream stream;

  protected JsonTable(String name, @Nullable JsonStream stream) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonRoot.java`
#### Snippet
```java
   * model with this name.
   */
  public final @Nullable String defaultSchema;

  /** List of schema elements.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
  private static class NavigationExpander extends NavigationModifier {
    final @Nullable SqlOperator op;
    final @Nullable SqlNode offset;

    NavigationExpander() {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
  private final Map<SqlNode, SqlNode> originalExprs = new HashMap<>();

  private @Nullable SqlNode top;

  // TODO jvs 11-Dec-2008:  make this local to performUnconditionalRewrites
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
   * The name-resolution scope of a LATERAL TABLE clause.
   */
  private @Nullable TableScope tableScope = null;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
   */
  private static class NavigationExpander extends NavigationModifier {
    final @Nullable SqlOperator op;
    final @Nullable SqlNode offset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonType.java`
#### Snippet
```java
  /** Type if this is not a struct.
   */
  public final @Nullable String type;

  /** Definition of the attributes of this type.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonView.java`
#### Snippet
```java
   * <p>The default value is {@code null}.
   */
  public final @Nullable Boolean modifiable;

  @JsonCreator
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonView.java`
#### Snippet
```java
   * <p>If not specified, defaults to current schema.
   */
  public final @Nullable List<String> path;

  /** Whether this view should allow INSERT requests.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonLattice.java`
#### Snippet
```java
   *
   * <p>If null, Calcite will a query to find the real value. */
  public final @Nullable Double rowCountEstimate;

  /** Name of a class that provides estimates of the number of distinct values
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/JsonLattice.java`
#### Snippet
```java
   * <p>If not set, Calcite will generate and execute a SQL query to find the
   * real value, and cache the results. */
  public final @Nullable String statisticProvider;

  /** List of materialized aggregates to create up front. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/PigRelBuilder.java`
#### Snippet
```java
 */
public class PigRelBuilder extends RelBuilder {
  private @Nullable String lastAlias;
  protected PigRelBuilder(Context context,
      RelOptCluster cluster,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/config/CalciteConnectionProperty.java`
#### Snippet
```java
  private final @Nullable Object defaultValue;
  private final boolean required;
  private final @Nullable Class valueClass;

  private static final Map<String, CalciteConnectionProperty> NAME_TO_PROPS;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/config/CalciteConnectionProperty.java`
#### Snippet
```java
  private final Type type;
  @SuppressWarnings("ImmutableEnumChecker")
  private final @Nullable Object defaultValue;
  private final boolean required;
  private final @Nullable Class valueClass;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
   */
  public static class FoundOne extends ControlFlowException {
    private final @Nullable Object node;

    /** Singleton instance. Can be used if you don't care about node. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/ModelHandler.java`
#### Snippet
```java
  private final String modelUri;
  Lattice.@Nullable Builder latticeBuilder;
  Lattice.@Nullable TileBuilder tileBuilder;

  @SuppressWarnings("method.invocation.invalid")
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/model/ModelHandler.java`
#### Snippet
```java
      new ArrayDeque<>();
  private final String modelUri;
  Lattice.@Nullable Builder latticeBuilder;
  Lattice.@Nullable TileBuilder tileBuilder;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private final SqlOperatorTable operatorTable;
    private final ImmutableList<Program> programs;
    private final @Nullable ImmutableList<RelTraitDef> traitDefs;
    private final SqlParser.Config parserConfig;
    private final SqlValidator.Config sqlValidatorConfig;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private SqlValidator.Config sqlValidatorConfig;
    private SqlToRelConverter.Config sqlToRelConverterConfig;
    private @Nullable SchemaPlus defaultSchema;
    private @Nullable RexExecutor executor;
    private @Nullable RelOptCostFactory costFactory;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private final SqlToRelConverter.Config sqlToRelConverterConfig;
    private final @Nullable SchemaPlus defaultSchema;
    private final @Nullable RelOptCostFactory costFactory;
    private final RelDataTypeSystem typeSystem;
    private final @Nullable RexExecutor executor;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private boolean evolveLattice;
    private SqlStatisticProvider statisticProvider;
    private RelOptTable.@Nullable ViewExpander viewExpander;

    /** Creates a ConfigBuilder, initializing to defaults. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private ImmutableList<Program> programs;
    private Context context;
    private @Nullable ImmutableList<RelTraitDef> traitDefs;
    private SqlParser.Config parserConfig;
    private SqlValidator.Config sqlValidatorConfig;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private @Nullable SchemaPlus defaultSchema;
    private @Nullable RexExecutor executor;
    private @Nullable RelOptCostFactory costFactory;
    private RelDataTypeSystem typeSystem;
    private boolean evolveLattice;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private SqlToRelConverter.Config sqlToRelConverterConfig;
    private @Nullable SchemaPlus defaultSchema;
    private @Nullable RexExecutor executor;
    private @Nullable RelOptCostFactory costFactory;
    private RelDataTypeSystem typeSystem;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private final SqlValidator.Config sqlValidatorConfig;
    private final SqlToRelConverter.Config sqlToRelConverterConfig;
    private final @Nullable SchemaPlus defaultSchema;
    private final @Nullable RelOptCostFactory costFactory;
    private final RelDataTypeSystem typeSystem;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private final boolean evolveLattice;
    private final SqlStatisticProvider statisticProvider;
    private final RelOptTable.@Nullable ViewExpander viewExpander;

    StdFrameworkConfig(Context context,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
    private final @Nullable RelOptCostFactory costFactory;
    private final RelDataTypeSystem typeSystem;
    private final @Nullable RexExecutor executor;
    private final boolean evolveLattice;
    private final SqlStatisticProvider statisticProvider;
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/schema/TableFactory.java`
#### Snippet
```java
   */
  T create(
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/schema/TableFactory.java`
#### Snippet
```java
  T create(
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
      @Nullable RelDataType rowType);
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/schema/TableFactory.java`
#### Snippet
```java
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
      @Nullable RelDataType rowType);
}
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/schema/ScannableTable.java`
#### Snippet
```java
  /** Returns an enumerator over the rows in this Table. Each row is represented
   * as an array of its column values. */
  Enumerable<@Nullable Object[]> scan(DataContext root);
}

```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/schema/Table.java`
#### Snippet
```java
   * @return Row type
   */
  RelDataType getRowType(RelDataTypeFactory typeFactory);

  /** Returns a provider of statistics about this table. */
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/schema/QueryableTable.java`
#### Snippet
```java
public interface QueryableTable extends Table {
  /** Converts this table into a {@link Queryable}. */
  <T> Queryable<T> asQueryable(QueryProvider queryProvider, SchemaPlus schema,
      String tableName);

```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/schema/QueryableTable.java`
#### Snippet
```java
public interface QueryableTable extends Table {
  /** Converts this table into a {@link Queryable}. */
  <T> Queryable<T> asQueryable(QueryProvider queryProvider, SchemaPlus schema,
      String tableName);

```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/schema/QueryableTable.java`
#### Snippet
```java
  /** Converts this table into a {@link Queryable}. */
  <T> Queryable<T> asQueryable(QueryProvider queryProvider, SchemaPlus schema,
      String tableName);

  /** Returns the element type of the collection that will implement this
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/schema/impl/ViewTableMacro.java`
#### Snippet
```java
  protected final String viewSql;
  protected final CalciteSchema schema;
  private final @Nullable Boolean modifiable;
  /** Typically null. If specified, overrides the path of the schema as the
   * context for validating {@code viewSql}. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/schema/impl/ViewTableMacro.java`
#### Snippet
```java
  /** Typically null. If specified, overrides the path of the schema as the
   * context for validating {@code viewSql}. */
  protected final @Nullable List<String> schemaPath;
  protected final @Nullable List<String> viewPath;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/schema/impl/ViewTableMacro.java`
#### Snippet
```java
   * context for validating {@code viewSql}. */
  protected final @Nullable List<String> schemaPath;
  protected final @Nullable List<String> viewPath;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/schema/impl/ViewTable.java`
#### Snippet
```java
  private final List<String> schemaPath;
  private final RelProtoDataType protoRowType;
  private final @Nullable List<String> viewPath;

  public ViewTable(Type elementType, RelProtoDataType rowType, String viewSql,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/schema/impl/AggregateFunctionImpl.java`
#### Snippet
```java
  public final Method addMethod;
  public final @Nullable Method mergeMethod;
  public final @Nullable Method resultMethod; // may be null
  public final ImmutableList<Class<?>> valueTypes;
  private final List<FunctionParameter> parameters;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/schema/impl/AggregateFunctionImpl.java`
#### Snippet
```java
  public final Method initMethod;
  public final Method addMethod;
  public final @Nullable Method mergeMethod;
  public final @Nullable Method resultMethod; // may be null
  public final ImmutableList<Class<?>> valueTypes;
```

### NullableProblems
Overridden method parameters are not annotated
in `core/src/main/java/org/apache/calcite/adapter/java/JavaTypeFactory.java`
#### Snippet
```java

  /** Converts a type in Java format to a SQL-oriented type. */
  RelDataType toSql(RelDataType type);
}

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcSchema.java`
#### Snippet
```java
  public final SqlDialect dialect;
  final JdbcConvention convention;
  private @Nullable ImmutableMap<String, JdbcTable> tableMap;
  private final boolean snapshot;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcSchema.java`
#### Snippet
```java

  final DataSource dataSource;
  final @Nullable String catalog;
  final @Nullable String schema;
  public final SqlDialect dialect;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcSchema.java`
#### Snippet
```java
  final DataSource dataSource;
  final @Nullable String catalog;
  final @Nullable String schema;
  public final SqlDialect dialect;
  final JdbcConvention convention;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
    final List<@Nullable Comparable> values = new ArrayList<>();
    @Nullable Comparable min;
    @Nullable Comparable max;
    boolean containsNull;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
    final Map<Comparable, Comparable> map = new HashMap<>();
    final List<@Nullable Comparable> values = new ArrayList<>();
    @Nullable Comparable min;
    @Nullable Comparable max;
    boolean containsNull;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableMatch.java`
#### Snippet
```java
   */
  static class PassedRowsInputGetter implements RexToLixTranslator.InputGetter {
    private @Nullable Expression index;
    private final ParameterExpression row;
    private final ParameterExpression passedRows;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableMatch.java`
#### Snippet
```java
   */
  static class PrevInputGetter implements RexToLixTranslator.InputGetter {
    private @Nullable Expression offset;
    private final ParameterExpression row;
    private final Function<Expression, RexToLixTranslator.InputGetter> generator;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableLimit.java`
#### Snippet
```java
/** Relational expression that applies a limit and/or offset to its input. */
public class EnumerableLimit extends SingleRel implements EnumerableRel {
  public final @Nullable RexNode offset;
  public final @Nullable RexNode fetch;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableLimit.java`
#### Snippet
```java
public class EnumerableLimit extends SingleRel implements EnumerableRel {
  public final @Nullable RexNode offset;
  public final @Nullable RexNode fetch;

  /** Creates an EnumerableLimit.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/impl/AggResultContextImpl.java`
#### Snippet
```java
public class AggResultContextImpl extends AggResetContextImpl
    implements AggResultContext {
  private final @Nullable AggregateCall call;
  private final @Nullable ParameterExpression key;
  private final @Nullable PhysType keyPhysType;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/impl/AggResultContextImpl.java`
#### Snippet
```java
    implements AggResultContext {
  private final @Nullable AggregateCall call;
  private final @Nullable ParameterExpression key;
  private final @Nullable PhysType keyPhysType;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/impl/AggResultContextImpl.java`
#### Snippet
```java
  private final @Nullable AggregateCall call;
  private final @Nullable ParameterExpression key;
  private final @Nullable PhysType keyPhysType;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
  final SqlConformance conformance;
  private final Expression root;
  final RexToLixTranslator.@Nullable InputGetter inputGetter;
  private final BlockBuilder list;
  private final @Nullable BlockBuilder staticList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
  final RexToLixTranslator.@Nullable InputGetter inputGetter;
  private final BlockBuilder list;
  private final @Nullable BlockBuilder staticList;
  private final @Nullable Function1<String, InputGetter> correlates;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
  private final Map<RexNode, Result> rexResultMap = new HashMap<>();

  private @Nullable Type currentStorageType;

  private RexToLixTranslator(@Nullable RexProgram program,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
  final JavaTypeFactory typeFactory;
  final RexBuilder builder;
  private final @Nullable RexProgram program;
  final SqlConformance conformance;
  private final Expression root;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
  private final BlockBuilder list;
  private final @Nullable BlockBuilder staticList;
  private final @Nullable Function1<String, InputGetter> correlates;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
    private final ImmutableList<RexNode> operands;
    private final boolean ignoreNulls;
    private final @Nullable String alias;
    private final boolean nullWhenCountZero;
    private final boolean allowPartial;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
    private final boolean approximate;
    private final boolean ignoreNulls;
    private final @Nullable RexNode filter;
    private final @Nullable String alias;
    private final ImmutableList<RexNode> preOperands; // may be empty
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
  static class GroupKeyImpl implements GroupKey {
    final ImmutableList<RexNode> nodes;
    final @Nullable ImmutableList<ImmutableList<RexNode>> nodeLists;
    final @Nullable String alias;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
    private final boolean ignoreNulls;
    private final @Nullable RexNode filter;
    private final @Nullable String alias;
    private final ImmutableList<RexNode> preOperands; // may be empty
    private final ImmutableList<RexNode> operands; // may be empty
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
    final ImmutableList<RexNode> nodes;
    final @Nullable ImmutableList<ImmutableList<RexNode>> nodeLists;
    final @Nullable String alias;

    GroupKeyImpl(ImmutableList<RexNode> nodes,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
    private final ImmutableList<RexNode> preOperands; // may be empty
    private final ImmutableList<RexNode> operands; // may be empty
    private final @Nullable ImmutableList<RexNode> distinctKeys; // may be empty or null
    private final ImmutableList<RexNode> orderKeys; // may be empty

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
public class RelBuilder {
  protected final RelOptCluster cluster;
  protected final @Nullable RelOptSchema relOptSchema;
  private final Deque<Frame> stack = new ArrayDeque<>();
  private RexSimplify simplifier;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
    /** Relational expression for the table. Usually a
     * {@link org.apache.calcite.rel.logical.LogicalTableScan}. */
    @Nullable RelNode tableRel;
    /** Relational expression for the query to populate the table. */
    @Nullable RelNode queryRel;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
   */
  protected final Convention resultConvention;
  protected @Nullable CalciteTimingTracer timingTracer;
  protected @MonotonicNonNull List<@Nullable List<String>> fieldOrigins;
  protected @MonotonicNonNull RelDataType parameterRowType;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
    @Nullable RelNode tableRel;
    /** Relational expression for the query to populate the table. */
    @Nullable RelNode queryRel;
    /** Star table identified. */
    private @Nullable RelOptTable starRelOptTable;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
    private final @Nullable RelDataType rowType;
    private final RelDataType parameterRowType;
    private final @Nullable RelRoot root;
    private final SqlExplainFormat format;
    private final SqlExplainLevel detailLevel;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
    @Nullable RelNode queryRel;
    /** Star table identified. */
    private @Nullable RelOptTable starRelOptTable;

    public Materialization(CalciteSchema.TableEntry materializedTable,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
    protected final RelDataType rowType;
    protected final boolean isDml;
    protected final TableModify.@Nullable Operation tableModOp;
    protected final List<? extends @Nullable List<String>> fieldOrigins;
    protected final List<RelCollation> collations;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
  public abstract static class PreparedExplain
      implements PreparedResult {
    private final @Nullable RelDataType rowType;
    private final RelDataType parameterRowType;
    private final @Nullable RelRoot root;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
 */
public class RelOptTableImpl extends Prepare.AbstractPreparingTable {
  private final @Nullable RelOptSchema schema;
  private final RelDataType rowType;
  private final @Nullable Table table;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
  private final @Nullable RelOptSchema schema;
  private final RelDataType rowType;
  private final @Nullable Table table;
  private final @Nullable TableExpressionFactory tableExpressionFactory;
  private final ImmutableList<String> names;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
  private final RelDataType rowType;
  private final @Nullable Table table;
  private final @Nullable TableExpressionFactory tableExpressionFactory;
  private final ImmutableList<String> names;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
   * allows table expressions to be generated. */
  private static class MySchemaPlus implements SchemaPlus {
    private final @Nullable SchemaPlus parent;
    private final String name;
    private final Schema schema;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
   * used to replace a query expression that wildly underestimates the row
   * count. Now the materialized table can tell the same lie. */
  private final @Nullable Double rowCount;

  private RelOptTableImpl(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/profile/Profiler.java`
#### Snippet
```java

    final NavigableSet<Column> columns;
    final @Nullable NavigableSet<Comparable> values;
    final double cardinality;
    final int nullCount;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java

  // set in STATE_2_READY
  private @Nullable SchemaPlus defaultSchema;
  private @Nullable JavaTypeFactory typeFactory;
  private @Nullable RelOptPlanner planner;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java

  /** Holds the trait definitions to be registered with planner. May be null. */
  private final @Nullable ImmutableList<RelTraitDef> traitDefs;

  private final SqlParser.Config parserConfig;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java
  private @Nullable JavaTypeFactory typeFactory;
  private @Nullable RelOptPlanner planner;
  private @Nullable RexExecutor executor;

  // set in STATE_4_VALIDATE
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java
  // set in STATE_4_VALIDATE
  private @Nullable SqlValidator validator;
  private @Nullable SqlNode validatedSqlNode;

  /** Creates a planner. Not a public API; call
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java
  // set in STATE_2_READY
  private @Nullable SchemaPlus defaultSchema;
  private @Nullable JavaTypeFactory typeFactory;
  private @Nullable RelOptPlanner planner;
  private @Nullable RexExecutor executor;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java
  private final SqlOperatorTable operatorTable;
  private final ImmutableList<Program> programs;
  private final @Nullable RelOptCostFactory costFactory;
  private final Context context;
  private final CalciteConnectionConfig connectionConfig;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java
  private @Nullable SchemaPlus defaultSchema;
  private @Nullable JavaTypeFactory typeFactory;
  private @Nullable RelOptPlanner planner;
  private @Nullable RexExecutor executor;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java

  // set in STATE_4_VALIDATE
  private @Nullable SqlValidator validator;
  private @Nullable SqlNode validatedSqlNode;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/QueryableRelBuilder.java`
#### Snippet
```java
class QueryableRelBuilder<T> implements QueryableFactory<T> {
  private final LixToRelTranslator translator;
  private @Nullable RelNode rel;

  QueryableRelBuilder(LixToRelTranslator translator) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/CalcitePrepareImpl.java`
#### Snippet
```java
    protected final RelDataTypeFactory typeFactory;
    protected final SqlRexConvertletTable convertletTable;
    private final EnumerableRel.@Nullable Prefer prefer;
    private final RelOptCluster cluster;
    private final Map<String, Object> internalParameters =
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/prepare/CalcitePrepareImpl.java`
#### Snippet
```java
    @SuppressWarnings("unused")
    private int expansionDepth;
    private @Nullable SqlValidator sqlValidator;

    /** Constructor.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
    final Set<ImmutableBitSet> dependents = new HashSet<>();
    double expectedCardinality;
    @Nullable Collector collector;
    /** Assigned by {@link Collector#finish()}. */
    int nullCount;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
    int cardinality;
    /** Assigned by {@link Collector#finish()}. */
    @Nullable SortedSet<Comparable> valueSet;

    Space(Run run, ImmutableBitSet columnOrdinals, Iterable<Column> columns) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/CalciteContextException.java`
#### Snippet
```java
  private int endPosColumn;

  private @Nullable String originalStatement;

  //~ Constructors -----------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/Enumerables.java`
#### Snippet
```java

          /** Current result row. Null if no row is ready. */
          @Nullable TResult resultRow;

          /** Match counter is 1-based in Oracle. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/ResultSetEnumerable.java`
#### Snippet
```java
  private static class ResultSetEnumerator<T> implements Enumerator<T> {
    private final Function0<T> rowBuilder;
    private @Nullable ResultSet resultSet;

    ResultSetEnumerator(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/ResultSetEnumerable.java`
#### Snippet
```java
      LoggerFactory.getLogger(ResultSetEnumerable.class);

  private @Nullable Long queryStart;
  private long timeout;
  private boolean timeoutSetFailed;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/ResultSetEnumerable.java`
#### Snippet
```java
  private final String sql;
  private final Function1<ResultSet, Function0<T>> rowBuilderFactory;
  private final @Nullable PreparedStatementEnricher preparedStatementEnricher;

  private static final Logger LOGGER =
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
  public static class JsonPathContext {
    public final PathMode mode;
    public final @Nullable Object obj;
    public final @Nullable Exception exc;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
    @JsonValue
    public final @Nullable Object obj;
    public final @Nullable Exception exc;

    private JsonValueContext(@Nullable Object obj, @Nullable Exception exc) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
    public final PathMode mode;
    public final @Nullable Object obj;
    public final @Nullable Exception exc;

    private JsonPathContext(@Nullable Object obj, @Nullable Exception exc) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
  public static class JsonValueContext {
    @JsonValue
    public final @Nullable Object obj;
    public final @Nullable Exception exc;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/runtime/Resources.java`
#### Snippet
```java
  /** String property instance. */
  public static class StringProp extends Prop {
    private final @Nullable String defaultValue;

    public StringProp(PropertyAccessor accessor, Method method) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/RelStructuredTypeFlattener.java`
#### Snippet
```java
  private int iRestructureInput;
  @SuppressWarnings("unused")
  private @Nullable RelDataType flattenedRootType;
  boolean restructured;
  private final RelOptTable.ToRelContext toRelContext;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/RelStructuredTypeFlattener.java`
#### Snippet
```java

  private final Map<RelNode, RelNode> oldToNewRelMap = new HashMap<>();
  private @Nullable RelNode currentRel;
  private int iRestructureInput;
  @SuppressWarnings("unused")
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
    final String variableName;

    final @Nullable NullPolicy nullPolicy;
    private final boolean harmonize;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
  private static class UnaryImplementor extends AbstractRexCallImplementor {
    final ExpressionType expressionType;
    final @Nullable String backupMethodName;

    UnaryImplementor(ExpressionType expressionType, NullPolicy nullPolicy,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
          int curentPosition; // position in for-win-agg-loop
          int startIndex;     // index of start of window
          Comparable @Nullable [] rows;  // accessed via WinAggAddContext.compareRows
          @SuppressWarnings("nullness")
          void sample() {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/InterpretableRel.java`
#### Snippet
```java
    public final Map<String, Object> internalParameters =
        new LinkedHashMap<>();
    public final CalcitePrepare.@Nullable SparkHandler spark;
    public final DataContext dataContext;
    public final Map<RelNode, List<Sink>> relSinks = new HashMap<>();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/AggregateNode.java`
#### Snippet
```java
    final AggregateFunctionImpl aggFunction;
    final int argOrdinal;
    public final @Nullable Object instance;
    public final boolean nullIfEmpty;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/AggregateNode.java`
#### Snippet
```java
  private static class UdaAccumulator implements Accumulator {
    private final UdaAccumulatorFactory factory;
    private @Nullable Object value;
    private boolean empty;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/MutableNode.java`
#### Snippet
```java
  int startCol;
  int endCol;
  @Nullable String alias;
  final List<MutableNode> children = new ArrayList<>();

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/MutableNode.java`
#### Snippet
```java
  final LatticeTable table;
  final @Nullable MutableNode parent;
  final @Nullable Step step;
  int startCol;
  int endCol;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/MutableNode.java`
#### Snippet
```java
class MutableNode {
  final LatticeTable table;
  final @Nullable MutableNode parent;
  final @Nullable Step step;
  int startCol;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
    protected final Interpreter interpreter;
    protected @Nullable RelNode rootRel;
    protected @Nullable RelNode rel;
    protected @Nullable Node node;
    final Map<RelNode, NodeInfo> nodes = new LinkedHashMap<>();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
    final Map<Edge, ListSink> sinks = new LinkedHashMap<>();
    final @Nullable Enumerable<Row> rowEnumerable;
    @Nullable Node node;

    NodeInfo(RelNode rel, @Nullable Enumerable<Row> rowEnumerable) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
    @NotOnlyInitialized
    protected final Interpreter interpreter;
    protected @Nullable RelNode rootRel;
    protected @Nullable RelNode rel;
    protected @Nullable Node node;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
    protected @Nullable RelNode rootRel;
    protected @Nullable RelNode rel;
    protected @Nullable Node node;
    final Map<RelNode, NodeInfo> nodes = new LinkedHashMap<>();
    final Map<RelNode, List<RelNode>> relInputs = new HashMap<>();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
  private static class ListSource implements Source {
    private final ArrayDeque<Row> list;
    private @Nullable Iterator<Row> iterator;

    ListSource(ArrayDeque<Row> list) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
    final RelNode rel;
    final Map<Edge, ListSink> sinks = new LinkedHashMap<>();
    final @Nullable Enumerable<Row> rowEnumerable;
    @Nullable Node node;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/LatticeNode.java`
#### Snippet
```java
  final int startCol;
  final int endCol;
  public final @Nullable String alias;
  private final ImmutableList<LatticeChildNode> children;
  public final String digest;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/MaterializationActor.java`
#### Snippet
```java
    final MaterializationKey key;
    final CalciteSchema rootSchema;
    CalciteSchema.@Nullable TableEntry materializedTable;
    final String sql;
    final RelDataType rowType;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/MaterializationActor.java`
#### Snippet
```java
    final String sql;
    final CalciteSchema schema;
    final @Nullable List<String> path;

    QueryKey(String sql, CalciteSchema schema, @Nullable List<String> path) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/MaterializationActor.java`
#### Snippet
```java
    final String sql;
    final RelDataType rowType;
    final @Nullable List<String> viewSchemaPath;

    /** Creates a materialization.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java

  // The rel which is being visited
  protected @Nullable RelNode currentRel;

  protected final Context context;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
    final RelDataTypeFactory typeFactory;
    final boolean projectPulledAboveLeftCorrelator;
    final @Nullable RexInputRef nullIndicator;
    final ImmutableSet<Integer> isCount;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `file/src/main/java/org/apache/calcite/adapter/file/JsonTable.java`
#### Snippet
```java
public class JsonTable extends AbstractTable {
  private final Source source;
  private @Nullable RelDataType rowType;
  protected @Nullable List<Object> dataList;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `file/src/main/java/org/apache/calcite/adapter/file/JsonTable.java`
#### Snippet
```java
  private final Source source;
  private @Nullable RelDataType rowType;
  protected @Nullable List<Object> dataList;

  public JsonTable(Source source) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/LatticeSuggester.java`
#### Snippet
```java
    final boolean distinct;
    final List<? extends @Nullable ColRef> arguments;
    final @Nullable String name;

    private MutableMeasure(SqlAggFunction aggregate, boolean distinct,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
    private boolean auto = true;
    private @MonotonicNonNull Double rowCountEstimate;
    private @Nullable String statisticProvider;
    private final Map<String, DerivedColumn> derivedColumnsByName =
        new LinkedHashMap<>();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
    public final SqlAggFunction agg;
    public final boolean distinct;
    public final @Nullable String name;
    public final ImmutableList<Column> args;
    public final String digest;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
  private static class Vertex {
    final LatticeTable table;
    final @Nullable String alias;

    private Vertex(LatticeTable table, @Nullable String alias) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `file/src/main/java/org/apache/calcite/adapter/file/CsvEnumerator.java`
#### Snippet
```java
  private final AtomicBoolean cancelFlag;
  private final RowConverter<E> rowConverter;
  private @Nullable E current;

  private static final FastDateFormat TIME_FORMAT_DATE;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `file/src/main/java/org/apache/calcite/adapter/file/CsvEnumerator.java`
#### Snippet
```java

  private final CSVReader reader;
  private final @Nullable List<@Nullable String> filterValues;
  private final AtomicBoolean cancelFlag;
  private final RowConverter<E> rowConverter;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `plus/src/main/java/org/apache/calcite/adapter/os/StdinTableFunction.java`
#### Snippet
```java
          @Override public Enumerator<Object[]> enumerator() {
            return new Enumerator<Object[]>() {
              @Nullable String line;
              int i;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `plus/src/main/java/org/apache/calcite/adapter/os/GitCommitsTableFunction.java`
#### Snippet
```java
            final Enumerator<String> e = enumerable.enumerator();
            return new Enumerator<@Nullable Object[]>() {
              private @Nullable Object @Nullable [] objects;
              private final StringBuilder b = new StringBuilder();

```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `plus/src/main/java/org/apache/calcite/chinook/CodesFunction.java`
#### Snippet
```java

    return new AbstractQueryableTable(Object[].class) {
      @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
        return typeFactory.builder()
            .add("TYPE", SqlTypeName.VARCHAR)
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/CodesFunction.java`
#### Snippet
```java

    return new AbstractQueryableTable(Object[].class) {
      @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
        return typeFactory.builder()
            .add("TYPE", SqlTypeName.VARCHAR)
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `plus/src/main/java/org/apache/calcite/chinook/CodesFunction.java`
#### Snippet
```java
      }

      @Override public Queryable<String[]> asQueryable(QueryProvider queryProvider,
                                                       SchemaPlus schema,
                                                       String tableName) {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/CodesFunction.java`
#### Snippet
```java
      }

      @Override public Queryable<String[]> asQueryable(QueryProvider queryProvider,
                                                       SchemaPlus schema,
                                                       String tableName) {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/CodesFunction.java`
#### Snippet
```java

      @Override public Queryable<String[]> asQueryable(QueryProvider queryProvider,
                                                       SchemaPlus schema,
                                                       String tableName) {
        if (name == null) {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/CodesFunction.java`
#### Snippet
```java
      @Override public Queryable<String[]> asQueryable(QueryProvider queryProvider,
                                                       SchemaPlus schema,
                                                       String tableName) {
        if (name == null) {
          return Linq4j.<String[]>emptyEnumerable().asQueryable();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `plus/src/main/java/org/apache/calcite/adapter/os/FilesTableFunction.java`
#### Snippet
```java
            final Enumerator<String> e = enumerable.enumerator();
            return new Enumerator<@Nullable Object[]>() {
              @Nullable Object @Nullable [] current;

              @Override public Object[] current() {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java

  @Override public AbstractQueryableTable create(
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java
  @Override public AbstractQueryableTable create(
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
      @Nullable RelDataType rowType) {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
      @Nullable RelDataType rowType) {
    return new AbstractQueryableTable(Integer.class) {
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java
      @Nullable RelDataType rowType) {
    return new AbstractQueryableTable(Integer.class) {
      @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
        return typeFactory.builder().add("ID", SqlTypeName.INTEGER).build();
      }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java
      @Nullable RelDataType rowType) {
    return new AbstractQueryableTable(Integer.class) {
      @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
        return typeFactory.builder().add("ID", SqlTypeName.INTEGER).build();
      }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java
      }

      @Override public Queryable<Integer> asQueryable(
          QueryProvider qp,
          SchemaPlus sp,
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java

      @Override public Queryable<Integer> asQueryable(
          QueryProvider qp,
          SchemaPlus sp,
          String string) {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java
      @Override public Queryable<Integer> asQueryable(
          QueryProvider qp,
          SchemaPlus sp,
          String string) {
        return fetchPreferredGenres();
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java
          QueryProvider qp,
          SchemaPlus sp,
          String string) {
        return fetchPreferredGenres();
      }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java
      }

      @Override public Queryable<Integer> asQueryable(
          QueryProvider qp,
          SchemaPlus sp,
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java

      @Override public Queryable<Integer> asQueryable(
          QueryProvider qp,
          SchemaPlus sp,
          String string) {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java
      @Override public Queryable<Integer> asQueryable(
          QueryProvider qp,
          SchemaPlus sp,
          String string) {
        return fetchPreferredAlbums();
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java
          QueryProvider qp,
          SchemaPlus sp,
          String string) {
        return fetchPreferredAlbums();
      }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java
      @Nullable RelDataType rowType) {
    return new AbstractQueryableTable(Integer.class) {
      @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
        return typeFactory.builder().add("ID", SqlTypeName.INTEGER).build();
      }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java
      @Nullable RelDataType rowType) {
    return new AbstractQueryableTable(Integer.class) {
      @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
        return typeFactory.builder().add("ID", SqlTypeName.INTEGER).build();
      }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java

  @Override public AbstractQueryableTable create(
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java
  @Override public AbstractQueryableTable create(
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
      @Nullable RelDataType rowType) {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java
      SchemaPlus schema,
      String name,
      Map<String, Object> operand,
      @Nullable RelDataType rowType) {
    return new AbstractQueryableTable(Integer.class) {
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSort.java`
#### Snippet
```java

  @Override public Sort copy(RelTraitSet traitSet, RelNode input,
      RelCollation newCollation, RexNode offset, RexNode fetch) {
    return new GeodeSort(getCluster(), traitSet, input, collation, fetch);
  }
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSort.java`
#### Snippet
```java

  @Override public Sort copy(RelTraitSet traitSet, RelNode input,
      RelCollation newCollation, RexNode offset, RexNode fetch) {
    return new GeodeSort(getCluster(), traitSet, input, collation, fetch);
  }
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeAggregate.java`
#### Snippet
```java

  @Override public Aggregate copy(RelTraitSet traitSet, RelNode input,
      ImmutableBitSet groupSet, List<ImmutableBitSet> groupSets,
      List<AggregateCall> aggCalls) {
    return new GeodeAggregate(getCluster(), traitSet, input, groupSet,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaMessageEnumerator.java`
#### Snippet
```java
  //runtime
  private final Deque<ConsumerRecord<K, V>> bufferedRecords = new ArrayDeque<>();
  private @Nullable ConsumerRecord<K, V> curRecord;

  KafkaMessageEnumerator(final Consumer consumer,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
     * guaranteed to be non-empty.
     */
    @Nullable SqlWindow window;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
     * statement.
     */
    public final @Nullable SqlValidatorScope scope;
    private final @Nullable Map<String, RexNode> nameToNodeMap;
    public @Nullable RelNode root;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    private final @Nullable Map<String, RexNode> nameToNodeMap;
    public @Nullable RelNode root;
    private @Nullable List<RelNode> inputs;
    private final Map<CorrelationId, RexFieldAccess> mapCorrelateToRex =
        new HashMap<>();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  public final @Nullable SqlValidator validator;
  protected final RexBuilder rexBuilder;
  protected final Prepare.CatalogReader catalogReader;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
     * Workspace for building aggregates.
     */
    @Nullable AggConverter agg;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    public final @Nullable SqlValidatorScope scope;
    private final @Nullable Map<String, RexNode> nameToNodeMap;
    public @Nullable RelNode root;
    private @Nullable List<RelNode> inputs;
    private final Map<CorrelationId, RexFieldAccess> mapCorrelateToRex =
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    final RelNode rel;
    final JoinRelType joinType;
    final @Nullable List<RexNode> leftKeys;

    RegisterArgs(RelNode rel, JoinRelType joinType, @Nullable List<RexNode> leftKeys) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    final RelOptUtil.Logic logic;
    @Nullable RexNode expr;
    final SqlImplementor.@Nullable Clause clause;

    private SubQuery(SqlNode node, RelOptUtil.Logic logic,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
  protected class AggConverter implements SqlVisitor<Void> {
    private final Blackboard bb;
    public final @Nullable AggregatingSelectScope aggregatingSelectScope;

    private final Map<String, String> nameMap = new HashMap<>();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
     */
    public final @Nullable SqlValidatorScope scope;
    private final @Nullable Map<String, RexNode> nameToNodeMap;
    public @Nullable RelNode root;
    private @Nullable List<RelNode> inputs;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    final SqlNode node;
    final RelOptUtil.Logic logic;
    @Nullable RexNode expr;
    final SqlImplementor.@Nullable Clause clause;

```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbSort.java`
#### Snippet
```java

  @Override public Sort copy(RelTraitSet traitSet, RelNode input,
      RelCollation newCollation, RexNode offset, RexNode fetch) {
    return new InnodbSort(getCluster(), traitSet, input, collation);
  }
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbSort.java`
#### Snippet
```java

  @Override public Sort copy(RelTraitSet traitSet, RelNode input,
      RelCollation newCollation, RexNode offset, RexNode fetch) {
    return new InnodbSort(getCluster(), traitSet, input, collation);
  }
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilter.java`
#### Snippet
```java
  private final TableDef tableDef;
  public final IndexCondition indexCondition;
  private final @Nullable String forceIndexName;

  /** Creates an InnodbFilter; but use {@link #create} if possible. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbTableScan.java`
#### Snippet
```java
  final RelDataType projectRowType;
  /** Force to use one specific index from hint. */
  private final @Nullable String forceIndexName;
  /** This contains index to scan table and optional condition. */
  private final IndexCondition indexCondition;
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
  }

  @Override public boolean isValid(Litmus litmus, Context context) {
    if (!super.isValid(litmus, context)) {
      return false;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilterTranslator.java`
#### Snippet
```java
  private final List<KeyMeta> skMetaList;
  /** If not null, force to use one specific index from hint. */
  private final @Nullable String forceIndexName;

  InnodbFilterTranslator(RexBuilder rexBuilder, RelDataType rowType,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/MergeUnionEnumerator.java`
#### Snippet
```java
  private final @Nullable Set<EnumerableDefaults.Wrapped<TSource>> processed;
  private final @Nullable Function1<TSource, EnumerableDefaults.Wrapped<TSource>> wrapper;
  private @Nullable TKey currentKeyInProcessedSet;

  private static final Object NOT_INIT = new Object();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/MergeUnionEnumerator.java`
#### Snippet
```java
  // Set to control duplicates, only used if "all" is false
  private final @Nullable Set<EnumerableDefaults.Wrapped<TSource>> processed;
  private final @Nullable Function1<TSource, EnumerableDefaults.Wrapped<TSource>> wrapper;
  private @Nullable TKey currentKeyInProcessedSet;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/MergeUnionEnumerator.java`
#### Snippet
```java

  // Set to control duplicates, only used if "all" is false
  private final @Nullable Set<EnumerableDefaults.Wrapped<TSource>> processed;
  private final @Nullable Function1<TSource, EnumerableDefaults.Wrapped<TSource>> wrapper;
  private @Nullable TKey currentKeyInProcessedSet;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/Linq4j.java`
#### Snippet
```java
  static class IterableEnumerator<T> implements Enumerator<T> {
    private final Iterable<? extends T> iterable;
    @Nullable Iterator<? extends T> iterator;
    T current;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/BaseQueryable.java`
#### Snippet
```java
  protected final QueryProvider provider;
  protected final Type elementType;
  protected final @Nullable Expression expression;

  protected BaseQueryable(QueryProvider provider, Type elementType,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableQueryable.java`
#### Snippet
```java
  private final Class<T> elementType;
  private final Enumerable<T> enumerable;
  private final @Nullable Expression expression;

  EnumerableQueryable(QueryProvider provider, Class<T> elementType,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/DeclarationStatement.java`
#### Snippet
```java
  public final int modifiers;
  public final ParameterExpression parameter;
  public final @Nullable Expression initializer;

  public DeclarationStatement(int modifiers, ParameterExpression parameter,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TryStatement.java`
#### Snippet
```java
  public final Statement body;
  public final List<CatchBlock> catchBlocks;
  public final @Nullable Statement fynally;

  public TryStatement(Statement body, List<CatchBlock> catchBlocks,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodCallExpression.java`
#### Snippet
```java
public class MethodCallExpression extends Expression {
  public final Method method;
  public final @Nullable Expression targetExpression; // null for call to static method
  public final List<Expression> expressions;
  /** Cached hash code for the expression. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ClassDeclaration.java`
#### Snippet
```java
  public final String name;
  public final List<MemberDeclaration> memberDeclarations;
  public final @Nullable Type extended;
  public final List<Type> implemented;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ExpressionType.java`
#### Snippet
```java
  While;

  final @Nullable String op;
  final @Nullable String op2;
  final boolean postfix;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ExpressionType.java`
#### Snippet
```java

  final @Nullable String op;
  final @Nullable String op2;
  final boolean postfix;
  final int lprec;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/GotoStatement.java`
#### Snippet
```java
  public final GotoExpressionKind kind;
  public final @Nullable LabelTarget labelTarget;
  public final @Nullable Expression expression;

  GotoStatement(GotoExpressionKind kind, @Nullable LabelTarget labelTarget,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/GotoStatement.java`
#### Snippet
```java
public class GotoStatement extends Statement {
  public final GotoExpressionKind kind;
  public final @Nullable LabelTarget labelTarget;
  public final @Nullable Expression expression;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MemberExpression.java`
#### Snippet
```java
 */
public class MemberExpression extends Expression {
  public final @Nullable Expression expression;
  public final PseudoField field;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ClassDeclarationFinder.java`
#### Snippet
```java
 */
public class ClassDeclarationFinder extends Shuttle {
  protected final @Nullable ClassDeclarationFinder parent;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FieldDeclaration.java`
#### Snippet
```java
  public final int modifier;
  public final ParameterExpression parameter;
  public final @Nullable Expression initializer;

  public FieldDeclaration(int modifier, ParameterExpression parameter,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BinaryExpression.java`
#### Snippet
```java
  public final Expression expression0;
  public final Expression expression1;
  private final @Nullable Primitive primitive;

  BinaryExpression(ExpressionType nodeType, Type type, Expression expression0,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstantExpression.java`
#### Snippet
```java
 */
public class ConstantExpression extends Expression {
  public final @Nullable Object value;

  public ConstantExpression(Type type, @Nullable Object value) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java
    private final Type rawType;
    private final List<Type> typeArguments;
    private final @Nullable Type ownerType;

    ParameterizedTypeImpl(Type rawType, List<Type> typeArguments,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java
  public final @Nullable BlockStatement body;
  public final List<ParameterExpression> parameterList;
  private @Nullable F dynamicFunction;
  /** Cached hash code for the expression. */
  private int hash;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java
    extends LambdaExpression {
  public final @Nullable F function;
  public final @Nullable BlockStatement body;
  public final List<ParameterExpression> parameterList;
  private @Nullable F dynamicFunction;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java
public final class FunctionExpression<F extends Function<?>>
    extends LambdaExpression {
  public final @Nullable F function;
  public final @Nullable BlockStatement body;
  public final List<ParameterExpression> parameterList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ForStatement.java`
#### Snippet
```java
  public final List<DeclarationStatement> declarations;
  public final @Nullable Expression condition;
  public final @Nullable Expression post;
  public final Statement body;
  /** Cached hash code for the expression. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ForStatement.java`
#### Snippet
```java
public class ForStatement extends Statement {
  public final List<DeclarationStatement> declarations;
  public final @Nullable Expression condition;
  public final @Nullable Expression post;
  public final Statement body;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/NewArrayExpression.java`
#### Snippet
```java
public class NewArrayExpression extends Expression {
  public final int dimension;
  public final @Nullable Expression bound;
  public final @Nullable List<Expression> expressions;
  /** Cached hash code for the expression. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/NewArrayExpression.java`
#### Snippet
```java
  public final int dimension;
  public final @Nullable Expression bound;
  public final @Nullable List<Expression> expressions;
  /** Cached hash code for the expression. */
  private int hash;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/NewExpression.java`
#### Snippet
```java
  public final Type type;
  public final List<Expression> arguments;
  public final @Nullable List<MemberDeclaration> memberDeclarations;
  /** Cached hash code for the expression. */
  private int hash;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockBuilder.java`
#### Snippet
```java

  private final boolean optimizing;
  private final @Nullable BlockBuilder parent;
  private final boolean removeUnused;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java
   * this type. */
  @SuppressWarnings("ImmutableEnumChecker")
  public final @Nullable Object minPositive;

  /** The maximum value of this primitive class. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java

  public final @Nullable Class primitiveClass;
  public final @Nullable Class boxClass;
  public final @Nullable String primitiveName; // e.g. "int"
  public final @Nullable String boxName;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java
  /** The maximum value of this primitive class. */
  @SuppressWarnings("ImmutableEnumChecker")
  public final @Nullable Object max;

  /** The size of a value of this type, in bits. Null if not applicable for this
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java
   * type. */
  @SuppressWarnings("ImmutableEnumChecker")
  public final @Nullable Object maxNegative;

  /** The smallest value that is greater than zero. Null if not applicable for
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java
  public final @Nullable Class primitiveClass;
  public final @Nullable Class boxClass;
  public final @Nullable String primitiveName; // e.g. "int"
  public final @Nullable String boxName;
  private final int family;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java
  OTHER(null, null, 4, null, null, null, null, null, -1);

  public final @Nullable Class primitiveClass;
  public final @Nullable Class boxClass;
  public final @Nullable String primitiveName; // e.g. "int"
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java
  /** The minimum value of this primitive class. */
  @SuppressWarnings("ImmutableEnumChecker")
  public final @Nullable Object min;

  /** The largest value that is less than zero. Null if not applicable for this
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java
   * {@code boolean}, etc. */
  @SuppressWarnings("ImmutableEnumChecker")
  public final @Nullable Object defaultValue;

  /** The minimum value of this primitive class. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Primitive.java`
#### Snippet
```java
  public final @Nullable Class boxClass;
  public final @Nullable String primitiveName; // e.g. "int"
  public final @Nullable String boxName;
  private final int family;

```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/DynamicTupleRecordType.java`
#### Snippet
```java
  }

  @Override public RelDataTypeField getField(String fieldName,
      boolean caseSensitive, boolean elideRecord) {
    final int index = nameToIndex(fieldName);
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigToSqlAggregateRule.java`
#### Snippet
```java
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    @Override default PigToSqlAggregateRule toRule() {
      return new PigToSqlAggregateRule(this);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigToSqlAggregateRule.java`
#### Snippet
```java
    }

    @Override public RexNode visitInputRef(RexInputRef inputRef) {
      if (inputRef.getIndex() == oldProjectCol
          && newProjectCol != null
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigToSqlAggregateRule.java`
#### Snippet
```java
    }

    @Override public RexNode visitCall(RexCall call) {
      if (replacementMap.containsKey(call)) {
        return replacementMap.get(call);
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigToSqlAggregateRule.java`
#### Snippet
```java
    }

    @Override public RexNode visitCall(RexCall call) {
      if (replacementMap.containsKey(call)) {
        return replacementMap.get(call);
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigToSqlAggregateRule.java`
#### Snippet
```java
    }

    @Override public Void visitCall(RexCall call) {
      if (PigRelUdfConverter.getSqlAggFuncForPigUdf(call) != null) {
        pigAggCalls.add(call);
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigTable.java`
#### Snippet
```java
  }

  @Override public Enumerable<@Nullable Object[]> scan(final DataContext root) {
    return null;
  }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigTable.java`
#### Snippet
```java
  }

  @Override public Enumerable<@Nullable Object[]> scan(final DataContext root) {
    return null;
  }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigTable.java`
#### Snippet
```java
  }

  @Override public RelDataType getRowType(final RelDataTypeFactory typeFactory) {
    return rowType;
  }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigTable.java`
#### Snippet
```java
  }

  @Override public RelDataType getRowType(final RelDataTypeFactory typeFactory) {
    return rowType;
  }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigTable.java`
#### Snippet
```java
  }

  @Override public Statistic getStatistic() {
    return DUMMY_STATISTICS;
  }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigTypes.java`
#### Snippet
```java
    }

    @Override public RelDataType toSql(RelDataType type) {
      if (type instanceof JavaType
          && ((JavaType) type).getJavaClass() == DataBag.class) {
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigTypes.java`
#### Snippet
```java
    }

    @Override public RelDataType toSql(RelDataType type) {
      if (type instanceof JavaType
          && ((JavaType) type).getJavaClass() == DataBag.class) {
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelBuilder.java`
#### Snippet
```java

  /** Creates a PigRelBuilder. */
  public static PigRelBuilder create(FrameworkConfig config) {
    final RelBuilder relBuilder = RelBuilder.create(config);
    return new PigRelBuilder(
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelBuilder.java`
#### Snippet
```java

  /** Creates a PigRelBuilder. */
  public static PigRelBuilder create(FrameworkConfig config) {
    final RelBuilder relBuilder = RelBuilder.create(config);
    return new PigRelBuilder(
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelToSqlConverter.java`
#### Snippet
```java
  }

  @Override public Result visit(Aggregate e) {
    final boolean isProjectOutput = e.getInput() instanceof Project
        || (e.getInput() instanceof EnumerableInterpreter
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelToSqlConverter.java`
#### Snippet
```java
  // CHECKSTYLE: IGNORE 1
  /** @see #dispatch */
  @Override public Result visit(Window e) {
    final Result x = visitInput(e, 0, Clause.SELECT);
    final Builder builder = x.builder(e);
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelToSqlConverter.java`
#### Snippet
```java
  // CHECKSTYLE: IGNORE 1
  /** @see #dispatch */
  @Override public Result visit(Window e) {
    final Result x = visitInput(e, 0, Clause.SELECT);
    final Builder builder = x.builder(e);
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `server/src/main/java/org/apache/calcite/server/MaterializedViewTable.java`
#### Snippet
```java
  /** The key with which this was stored in the materialization service,
   * or null if not (yet) materialized. */
  @Nullable MaterializationKey key;

  MaterializedViewTable(String name, RelProtoDataType protoRowType) {
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      }

      @Override public SqlOperandCountRange getOperandCountRange() {
        return SqlOperandCountRanges.from(2);
      }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
    return new SqlOperandMetadata() {
      @Override public boolean checkOperandTypes(
          SqlCallBinding callBinding, boolean throwOnFailure) {
        // Need at least two arguments
        if (callBinding.getOperandCount() < 2) {
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      }

      @Override public List<RelDataType> paramTypes(
          RelDataTypeFactory typeFactory) {
        return Functions.generate(paramCount,
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java

      @Override public List<RelDataType> paramTypes(
          RelDataTypeFactory typeFactory) {
        return Functions.generate(paramCount,
            i -> typeFactory.createSqlType(SqlTypeName.ANY));
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      }

      @Override public List<String> paramNames() {
        return Functions.generate(paramCount,  i -> "arg" + i);
      }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      }

      @Override public String getAllowedSignatures(SqlOperator op, String opName) {
        return opName + "(...)";
      }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      }

      @Override public String getAllowedSignatures(SqlOperator op, String opName) {
        return opName + "(...)";
      }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      }

      @Override public String getAllowedSignatures(SqlOperator op, String opName) {
        return opName + "(...)";
      }
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `server/src/main/java/org/apache/calcite/server/AbstractModifiableTable.java`
#### Snippet
```java
      RelNode child,
      TableModify.Operation operation,
      List<String> updateColumnList,
      List<RexNode> sourceExpressionList,
      boolean flattened) {
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `server/src/main/java/org/apache/calcite/server/AbstractModifiableTable.java`
#### Snippet
```java
      TableModify.Operation operation,
      List<String> updateColumnList,
      List<RexNode> sourceExpressionList,
      boolean flattened) {
    return LogicalTableModify.create(table, catalogReader, child, operation,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          @Nullable TInner innerValue;
          @Nullable Enumerable<TInner> innerEnumerable;
          @Nullable Enumerator<TInner> innerEnumerator;
          boolean innerEnumHasNext = false;
          boolean atLeastOneResult = false;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    private boolean isInitialized;
    private boolean isLastMoveNextFalse;
    private @Nullable TAccumulate curAccumulator;
    private Enumerator<TSource> enumerator;
    private @Nullable TResult curResult;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          final List<TSource> outerValues = new ArrayList<>(batchSize);
          final List<TInner> innerValues = new ArrayList<>();
          @Nullable TSource outerValue;
          @Nullable TInner innerValue;
          @Nullable Enumerable<TInner> innerEnumerable;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    private final Enumerable<TInner> rightEnumerable;
    private @Nullable Enumerator<TSource> leftEnumerator = null;
    private @Nullable Enumerator<TInner> rightEnumerator = null;
    private final Function1<TSource, TKey> outerKeySelector;
    private final Function1<TInner, TKey> innerKeySelector;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          private boolean outerMatch = false; // whether the outerValue has matched an innerValue
          private @Nullable TSource outerValue;
          private @Nullable TInner innerValue;
          private int state = 0; // 0 moving outer, 1 moving inner

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          Enumerator<TSource> outers = outer.enumerator();
          Enumerator<TInner> inners = Linq4j.emptyEnumerator();
          @Nullable Set<TKey> unmatchedKeys =
              generateNullsOnLeft
                  ? new HashSet<>(innerLookup.keySet())
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    private final Enumerable<TSource> leftEnumerable;
    private final Enumerable<TInner> rightEnumerable;
    private @Nullable Enumerator<TSource> leftEnumerator = null;
    private @Nullable Enumerator<TInner> rightEnumerator = null;
    private final Function1<TSource, TKey> outerKeySelector;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          @Nullable TSource outerValue;
          @Nullable TInner innerValue;
          @Nullable Enumerable<TInner> innerEnumerable;
          @Nullable Enumerator<TInner> innerEnumerator;
          boolean innerEnumHasNext = false;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          private boolean nonFirst;

          private @Nullable Iterator<TSource> rest;

          @Override public boolean hasNext() {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          Enumerator<TSource> outers = outer.enumerator();
          Enumerator<TInner> inners = Linq4j.emptyEnumerator();
          @Nullable List<TInner> innersUnmatched =
              generateNullsOnLeft
                  ? new ArrayList<>(innerToLookUp.toList())
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          private @Nullable Enumerator<TInner> innerEnumerator = null;
          private boolean outerMatch = false; // whether the outerValue has matched an innerValue
          private @Nullable TSource outerValue;
          private @Nullable TInner innerValue;
          private int state = 0; // 0 moving outer, 1 moving inner
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    private final Function1<TInner, TKey> innerKeySelector;
    // extra predicate in case of non equi-join, in case of equi-join it will be null
    private final @Nullable Predicate2<TSource, TInner> extraPredicate;
    private final Function2<TSource, @Nullable TInner, TResult> resultSelector;
    private final JoinType joinType;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          private int currentIteration = 0;
          private final Enumerator<TSource> seedEnumerator = seed.enumerator();
          private @Nullable Enumerator<TSource> iterativeEnumerator = null;

          // Set to control duplicates, only used if "all" is false
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    private final JoinType joinType;
    // key comparator, possibly null (Comparable#compareTo to be used in that case)
    private final @Nullable Comparator<TKey> comparator;
    private boolean done;
    private @Nullable Enumerator<TResult> results = null;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          final List<TInner> innerValues = new ArrayList<>();
          @Nullable TSource outerValue;
          @Nullable TInner innerValue;
          @Nullable Enumerable<TInner> innerEnumerable;
          @Nullable Enumerator<TInner> innerEnumerator;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    private final @Nullable Comparator<TKey> comparator;
    private boolean done;
    private @Nullable Enumerator<TResult> results = null;
    // used for LEFT/ANTI join: if right input is over, all remaining elements from left are results
    private boolean remainingLeft;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    private @Nullable TAccumulate curAccumulator;
    private Enumerator<TSource> enumerator;
    private @Nullable TResult curResult;

    SortedAggregateEnumerator(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
        return new Enumerator<TResult>() {
          private Enumerator<TSource> outerEnumerator = outer.enumerator();
          private @Nullable Enumerator<TInner> innerEnumerator = null;
          private boolean outerMatch = false; // whether the outerValue has matched an innerValue
          private @Nullable TSource outerValue;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          private final Enumerator<TSource> outerEnumerator = outer.enumerator();
          private @Nullable Enumerator<TInner> innerEnumerator;
          @Nullable TSource outerValue;
          @Nullable TInner innerValue;
          int state = 0; // 0 -- moving outer, 1 moving inner;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
        return new Enumerator<TResult>() {
          private final Enumerator<TSource> outerEnumerator = outer.enumerator();
          private @Nullable Enumerator<TInner> innerEnumerator;
          @Nullable TSource outerValue;
          @Nullable TInner innerValue;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
          private @Nullable Enumerator<TInner> innerEnumerator;
          @Nullable TSource outerValue;
          @Nullable TInner innerValue;
          int state = 0; // 0 -- moving outer, 1 moving inner;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTable.java`
#### Snippet
```java
public abstract class CsvTable extends AbstractTable {
  protected final Source source;
  protected final @Nullable RelProtoDataType protoRowType;
  private @Nullable RelDataType rowType;
  private @Nullable List<RelDataType> fieldTypes;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTable.java`
#### Snippet
```java
  protected final Source source;
  protected final @Nullable RelProtoDataType protoRowType;
  private @Nullable RelDataType rowType;
  private @Nullable List<RelDataType> fieldTypes;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTable.java`
#### Snippet
```java
  protected final @Nullable RelProtoDataType protoRowType;
  private @Nullable RelDataType rowType;
  private @Nullable List<RelDataType> fieldTypes;

  /** Creates a CsvTable. */
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `example/function/src/main/java/org/apache/calcite/example/maze/MazeTable.java`
#### Snippet
```java
  }

  @Override public Enumerable<@Nullable Object[]> scan(DataContext root) {
    final Random random = seed >= 0 ? new Random(seed) : new Random();
    final Maze maze = new Maze(width, height);
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `example/function/src/main/java/org/apache/calcite/example/maze/MazeTable.java`
#### Snippet
```java
  }

  @Override public Enumerable<@Nullable Object[]> scan(DataContext root) {
    final Random random = seed >= 0 ? new Random(seed) : new Random();
    final Maze maze = new Maze(width, height);
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `example/function/src/main/java/org/apache/calcite/example/maze/MazeTable.java`
#### Snippet
```java
  }

  @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
    return typeFactory.builder()
        .add("S", SqlTypeName.VARCHAR, width * 3 + 1)
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `example/function/src/main/java/org/apache/calcite/example/maze/MazeTable.java`
#### Snippet
```java
    }
    return new AbstractEnumerable<@Nullable Object[]>() {
      @Override public Enumerator<@Nullable Object[]> enumerator() {
        final Set<Integer> solutionSet;
        if (solution) {
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java

  @Override public Sort copy(RelTraitSet traitSet, RelNode input,
      RelCollation newCollation, RexNode offset, RexNode fetch) {
    return new MongoSort(getCluster(), traitSet, input, collation, offset,
        fetch);
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java

  @Override public Sort copy(RelTraitSet traitSet, RelNode input,
      RelCollation newCollation, RexNode offset, RexNode fetch) {
    return new MongoSort(getCluster(), traitSet, input, collation, offset,
        fetch);
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoAggregate.java`
#### Snippet
```java

  @Override public Aggregate copy(RelTraitSet traitSet, RelNode input,
      ImmutableBitSet groupSet, List<ImmutableBitSet> groupSets,
      List<AggregateCall> aggCalls) {
    try {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserListFixture.java`
#### Snippet
```java
  final SqlTestFactory factory;
  final SqlParserTest.Tester tester;
  final @Nullable SqlDialect dialect;
  final boolean convertToLinux;
  final StringAndPos sap;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserFixture.java`
#### Snippet
```java
  public final SqlParserTest.Tester tester;
  public final boolean convertToLinux;
  public final @Nullable SqlDialect dialect;
  public final Consumer<SqlParser> parserChecker;

```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `testkit/src/main/java/org/apache/calcite/test/AbstractModifiableTable.java`
#### Snippet
```java
      RelNode child,
      TableModify.Operation operation,
      List<String> updateColumnList,
      List<RexNode> sourceExpressionList,
      boolean flattened) {
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `testkit/src/main/java/org/apache/calcite/test/AbstractModifiableTable.java`
#### Snippet
```java
      TableModify.Operation operation,
      List<String> updateColumnList,
      List<RexNode> sourceExpressionList,
      boolean flattened) {
    return LogicalTableModify.create(table, catalogReader, child, operation,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `testkit/src/main/java/org/apache/calcite/test/SqlToRelFixture.java`
#### Snippet
```java

  private final String sql;
  private final @Nullable DiffRepository diffRepos;
  private final boolean decorrelate;
  private final SqlTester tester;
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
  }

  @Override public RelNode ensureRegistered(RelNode rel, RelNode equivRel) {
    return rel;
  }
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `testkit/src/main/java/org/apache/calcite/test/RelOptFixture.java`
#### Snippet
```java
  final RelSupplier relSupplier;
  final SqlTestFactory factory;
  final @Nullable DiffRepository diffRepos;
  final @Nullable HepProgram preProgram;
  final RelOptPlanner planner;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `testkit/src/main/java/org/apache/calcite/test/RelOptFixture.java`
#### Snippet
```java
  final SqlTestFactory factory;
  final @Nullable DiffRepository diffRepos;
  final @Nullable HepProgram preProgram;
  final RelOptPlanner planner;
  final ImmutableMap<Hook, Consumer<Object>> hooks;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraRel.java`
#### Snippet
```java
    final List<String> order = new ArrayList<>();

    @Nullable RelOptTable table;
    @Nullable CassandraTable cassandraTable;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraRel.java`
#### Snippet
```java

    @Nullable RelOptTable table;
    @Nullable CassandraTable cassandraTable;

    /** Adds newly projected fields and restricted predicates.
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
      @Override protected ModifiableViewTable modifiableViewTable(
          CalcitePrepare.AnalyzeViewResult parsed, String viewSql,
          List<String> schemaPath, List<String> viewPath, CalciteSchema schema) {
        final JavaTypeFactory typeFactory = (JavaTypeFactory) parsed.typeFactory;
        final Type elementType = typeFactory.getJavaClass(parsed.rowType);
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraLimit.java`
#### Snippet
```java
public class CassandraLimit extends SingleRel implements CassandraRel {
  public final @Nullable RexNode offset;
  public final @Nullable RexNode fetch;

  public CassandraLimit(RelOptCluster cluster, RelTraitSet traitSet,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraLimit.java`
#### Snippet
```java
 */
public class CassandraLimit extends SingleRel implements CassandraRel {
  public final @Nullable RexNode offset;
  public final @Nullable RexNode fetch;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
    private final boolean materializationsEnabled;
    private final ImmutableList<Pair<Hook, Consumer>> hooks;
    private final @Nullable PreparedStatementConsumer consumer;

    private String plan;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraTableScan.java`
#### Snippet
```java
public class CassandraTableScan extends TableScan implements CassandraRel {
  final CassandraTable cassandraTable;
  final @Nullable RelDataType projectRowType;

  /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@org.checkerframework.checker.nullness.qual.Nullable'
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraEnumerator.java`
#### Snippet
```java
  private final Iterator<Row> iterator;
  private final List<RelDataTypeField> fieldTypes;
  @Nullable private Row current;

  /** Creates a CassandraEnumerator.
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java
        final Enumerable<Integer> enumerable = Linq4j.asEnumerable(items);

        @Override public <E> Queryable<E> asQueryable(
            QueryProvider queryProvider, SchemaPlus schema, String tableName) {
          return (Queryable<E>) enumerable.asQueryable();
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java

        @Override public <E> Queryable<E> asQueryable(
            QueryProvider queryProvider, SchemaPlus schema, String tableName) {
          return (Queryable<E>) enumerable.asQueryable();
        }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java

        @Override public <E> Queryable<E> asQueryable(
            QueryProvider queryProvider, SchemaPlus schema, String tableName) {
          return (Queryable<E>) enumerable.asQueryable();
        }
```

### NullableProblems
Not annotated parameter overrides @DefaultQualifier parameter
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java

        @Override public <E> Queryable<E> asQueryable(
            QueryProvider queryProvider, SchemaPlus schema, String tableName) {
          return (Queryable<E>) enumerable.asQueryable();
        }
```

### NullableProblems
Not annotated method overrides method annotated with @DefaultQualifier
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java
        }

        @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
          RelDataTypeFactory.Builder builder = typeFactory.builder();
          for (int i = 0; i < columnLength; i++) {
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchAggregate.java`
#### Snippet
```java

  @Override public Aggregate copy(RelTraitSet traitSet, RelNode input,
      ImmutableBitSet groupSet, List<ImmutableBitSet> groupSets,
      List<AggregateCall> aggCalls) {
    try {
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSort.java`
#### Snippet
```java

  @Override public Sort copy(RelTraitSet traitSet, RelNode relNode, RelCollation relCollation,
      RexNode offset, RexNode fetch) {
    return new ElasticsearchSort(getCluster(), traitSet, relNode, collation, offset, fetch);
  }
```

### NullableProblems
Parameter annotated @DefaultQualifier must not override @Nullable parameter
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSort.java`
#### Snippet
```java

  @Override public Sort copy(RelTraitSet traitSet, RelNode relNode, RelCollation relCollation,
      RexNode offset, RexNode fetch) {
    return new ElasticsearchSort(getCluster(), traitSet, relNode, collation, offset, fetch);
  }
```

## RuleId[id=ExplicitArrayFilling]
### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `core/src/main/java/org/apache/calcite/rel/rules/JoinToMultiJoinRule.java`
#### Snippet
```java
    int nFieldsOnRight = right.getRowType().getFieldList().size();
    int[] adjustments = new int[nFieldsOnRight];
    for (int i = 0; i < nFieldsOnRight; i++) {
      adjustments[i] = nFieldsOnLeft;
    }
```

### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `core/src/main/java/org/apache/calcite/rel/rules/JoinToMultiJoinRule.java`
#### Snippet
```java
      int nFields = srcFields.size();
      int[] adjustments = new int[nFields];
      for (int idx = 0; idx < nFields; idx++) {
        adjustments[idx] = adjustmentAmount;
      }
```

### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `core/src/main/java/org/apache/calcite/rel/rules/JoinProjectTransposeRule.java`
#### Snippet
```java
      int nChildFields = childFields.size();
      int[] adjustments = new int[nChildFields];
      for (int i = 0; i < nChildFields; i++) {
        adjustments[i] = adjustmentAmount;
      }
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `aggCall.isDistinct()`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    switch (aggCall.getAggregation().getKind()) {
    case COUNT:
      if (aggCall.isDistinct()) {
        if (aggCall.isApproximate() || config.approximateDistinctCount()) {
          if (complexMetric == null) {
```

### DuplicateCondition
Duplicate condition `aggCall.isDistinct()`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
            new JsonFilteredAggregation(DruidJsonFilter.toNotDruidFilter(matchNulls),
                new JsonAggregation("count", name, fieldName, aggExpression));
      } else if (!aggCall.isDistinct()) {
        aggregation = new JsonAggregation("count", name, fieldName, aggExpression);
      } else {
```

### DuplicateCondition
Duplicate condition `false`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      // whether the java or fennel calc are used.
      // Try to make them the same
      if (false /* fennel calc*/) { // Treated as FLOAT or DOUBLE
        f.checkCastToString(numeric.maxNumericString, type,
            // Treated as DOUBLE
```

### DuplicateCondition
Duplicate condition `false`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        f.checkCastToString(numeric.minNumericString, type,
            isFloat ? "1.401299E-45" : "4.940656458412465E-324", castType);
      } else if (false /* JavaCalc */) {
        // Treated as FLOAT or DOUBLE
        f.checkCastToString(numeric.maxNumericString, type,
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `Collections.replaceAll()` is ignored
in `core/src/main/java/org/apache/calcite/rex/LogicVisitor.java`
#### Snippet
```java
    // Convert FALSE (which can only exist within LogicVisitor) to
    // UNKNOWN_AS_TRUE.
    Collections.replaceAll(logicList, Logic.FALSE, Logic.UNKNOWN_AS_TRUE);
  }

```

### IgnoreResultOfCall
Result of `Bug.upgrade()` is ignored
in `core/src/main/java/org/apache/calcite/util/Unsafe.java`
#### Snippet
```java
  public static String regexpReplace(String s, Pattern pattern,
      String replacement, int pos, int occurrence) {
    Bug.upgrade("when we drop JDK 8, replace StringBuffer with StringBuilder");
    final StringBuffer sb = new StringBuffer();
    final String input;
```

### IgnoreResultOfCall
Result of `Bug.upgrade()` is ignored
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
   * but we don't require Guava version 20 yet. */
  public static void throwIfUnchecked(Throwable throwable) {
    Bug.upgrade("Remove when minimum Guava version is 20");
    requireNonNull(throwable, "throwable");
    if (throwable instanceof RuntimeException) {
```

### IgnoreResultOfCall
Result of `Bug.upgrade()` is ignored
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
  @CanIgnoreReturnValue
  public static <T> T verifyNotNull(@Nullable T reference) {
    Bug.upgrade("Remove when minimum Guava version is 17");
    return requireNonNull(reference, "expected a non-null reference");
  }
```

### IgnoreResultOfCall
Result of `Bug.upgrade()` is ignored
in `plus/src/main/java/org/apache/calcite/adapter/tpcds/TpcdsSchema.java`
#### Snippet
```java

    @Override public Statistic getStatistic() {
      Bug.upgrade("add row count estimate to TpcdsTable, and use it");
      Integer rowCount = TABLE_ROW_COUNTS.get(tpcdsTable.name());
      assert rowCount != null : tpcdsTable;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
            getTestlogRoot(),
            ReflectUtil.getUnqualifiedClassName(getClass()));
    testClassDir.mkdirs();
    File testLogFile =
        new File(
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java

    // no diffs detected, so delete redundant .log file
    logFile.delete();
  }

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java

    logFile = new File(testFileSansExt.toString() + ".log");
    logFile.delete();

    refFile = new File(testFileSansExt.toString() + ".ref");
```

## RuleId[id=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'org.apache.calcite.plan.volcano.VolcanoPlanner.equivRoot(org.apache.calcite.plan.volcano.RelSet)' accessed via instance reference
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSet.java`
#### Snippet
```java
      }
      for (RelNode child : node.getInputs()) {
        RelSet childSet = planner.equivRoot(((RelSubset) child).getSet());
        if (childSet.id != this.id) {
          childSets.add(childSet);
```

### AccessStaticViaInstance
Static member 'java.lang.ThreadLocal.withInitial(java.util.function.Supplier)' accessed via instance reference
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
  public static class MyPlusFunction {
    public static final ThreadLocal<AtomicInteger> INSTANCE_COUNT =
        new ThreadLocal<>().withInitial(() -> new AtomicInteger(0));

    // Note: Not marked @Deterministic
```

### AccessStaticViaInstance
Static member 'java.lang.ThreadLocal.withInitial(java.util.function.Supplier)' accessed via instance reference
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
  public static class MyDeterministicPlusFunction {
    public static final ThreadLocal<AtomicInteger> INSTANCE_COUNT =
        new ThreadLocal<>().withInitial(() -> new AtomicInteger(0));

    @Deterministic public MyDeterministicPlusFunction() {
```

### AccessStaticViaInstance
Static member 'java.lang.ThreadLocal.withInitial(java.util.function.Supplier)' accessed via instance reference
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
  public static class MyPlusInitFunction {
    public static final ThreadLocal<AtomicInteger> INSTANCE_COUNT =
        new ThreadLocal<>().withInitial(() -> new AtomicInteger(0));
    public static final ThreadLocal<String> THREAD_DIGEST =
        new ThreadLocal<>();
```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `isEnforcer()` is identical to its super method
in `core/src/main/java/org/apache/calcite/rel/AbstractRelNode.java`
#### Snippet
```java
  }

  @Override public boolean isEnforcer() {
    return false;
  }
```

### RedundantMethodOverride
Method `canExpand()` is identical to its super method
in `core/src/main/java/org/apache/calcite/rel/rules/ReduceDecimalsRule.java`
#### Snippet
```java
    }

    @Override public boolean canExpand(RexCall call) {
      return RexUtil.requiresDecimalExpansion(call, false);
    }
```

### RedundantMethodOverride
Method `visitOver()` is identical to its super method
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
    }

    @Override public Boolean visitOver(RexOver over) {
      return false;
    }
```

### RedundantMethodOverride
Method `fullyQualify()` is identical to its super method
in `core/src/main/java/org/apache/calcite/sql/validate/ParameterScope.java`
#### Snippet
```java
  //~ Methods ----------------------------------------------------------------

  @Override public SqlQualified fullyQualify(SqlIdentifier identifier) {
    return SqlQualified.create(this, 1, null, identifier);
  }
```

### RedundantMethodOverride
Method `getOperandScope()` is identical to its super method
in `core/src/main/java/org/apache/calcite/sql/validate/ParameterScope.java`
#### Snippet
```java
  }

  @Override public SqlValidatorScope getOperandScope(SqlCall call) {
    return this;
  }
```

### RedundantMethodOverride
Method `canConvertConvention()` is identical to its super method
in `core/src/main/java/org/apache/calcite/plan/Convention.java`
#### Snippet
```java
    }

    @Override public boolean canConvertConvention(Convention toConvention) {
      return false;
    }
```

### RedundantMethodOverride
Method `useAbstractConvertersForConversion()` is identical to its super method
in `core/src/main/java/org/apache/calcite/plan/Convention.java`
#### Snippet
```java
    }

    @Override public boolean useAbstractConvertersForConversion(RelTraitSet fromTraits,
        RelTraitSet toTraits) {
      return false;
```

### RedundantMethodOverride
Method `nest()` is identical to its super method
in `core/src/main/java/org/apache/calcite/plan/visualizer/InputExcludedRelWriter.java`
#### Snippet
```java
  }

  @Override public boolean nest() {
    return false;
  }
```

### RedundantMethodOverride
Method `chooseDelegate()` is identical to its super method
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
  }

  @Override public RelOptPlanner chooseDelegate() {
    return this;
  }
```

### RedundantMethodOverride
Method `set()` is identical to its super method
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
    }

    @Override public void set(int source, int target) {
      throw new UnsupportedOperationException();
    }
```

### RedundantMethodOverride
Method `getExpression()` is identical to its super method
in `core/src/main/java/org/apache/calcite/schema/impl/ListTransientTable.java`
#### Snippet
```java
  }

  @Override public Expression getExpression(SchemaPlus schema, String tableName,
      @SuppressWarnings("rawtypes") Class clazz) {
    return Schemas.tableExpression(schema, elementType, tableName, clazz);
```

### RedundantMethodOverride
Method `getStatistic()` is identical to its super method
in `core/src/main/java/org/apache/calcite/adapter/java/ReflectiveSchema.java`
#### Snippet
```java
    }

    @Override public Statistic getStatistic() {
      return Statistics.UNKNOWN;
    }
```

### RedundantMethodOverride
Method `getDeriveMode()` is identical to its super method
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableCorrelate.java`
#### Snippet
```java
  }

  @Override public DeriveMode getDeriveMode() {
    return DeriveMode.LEFT_FIRST;
  }
```

### RedundantMethodOverride
Method `getDeriveMode()` is identical to its super method
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableRel.java`
#### Snippet
```java
  }

  @Override default DeriveMode getDeriveMode() {
    return DeriveMode.LEFT_FIRST;
  }
```

### RedundantMethodOverride
Method `getProgram()` is identical to its super method
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableCalc.java`
#### Snippet
```java
  }

  @Override public RexProgram getProgram() {
    return program;
  }
```

### RedundantMethodOverride
Method `canConvertConvention()` is identical to its super method
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableConvention.java`
#### Snippet
```java
  @Override public void register(RelOptPlanner planner) {}

  @Override public boolean canConvertConvention(Convention toConvention) {
    return false;
  }
```

### RedundantMethodOverride
Method `toAutomaton()` is identical to its super method
in `core/src/main/java/org/apache/calcite/runtime/Pattern.java`
#### Snippet
```java
    }

    @Override public Automaton toAutomaton() {
      return new AutomatonBuilder().add(this).build();
    }
```

### RedundantMethodOverride
Method `useAbstractConvertersForConversion()` is identical to its super method
in `core/src/main/java/org/apache/calcite/interpreter/InterpretableConvention.java`
#### Snippet
```java
  }

  @Override public boolean useAbstractConvertersForConversion(RelTraitSet fromTraits,
      RelTraitSet toTraits) {
    return false;
```

### RedundantMethodOverride
Method `canConvertConvention()` is identical to its super method
in `core/src/main/java/org/apache/calcite/interpreter/InterpretableConvention.java`
#### Snippet
```java
  @Override public void register(RelOptPlanner planner) {}

  @Override public boolean canConvertConvention(Convention toConvention) {
    return false;
  }
```

### RedundantMethodOverride
Method `canConvertConvention()` is identical to its super method
in `core/src/main/java/org/apache/calcite/interpreter/BindableConvention.java`
#### Snippet
```java
  @Override public void register(RelOptPlanner planner) {}

  @Override public boolean canConvertConvention(Convention toConvention) {
    return false;
  }
```

### RedundantMethodOverride
Method `useAbstractConvertersForConversion()` is identical to its super method
in `core/src/main/java/org/apache/calcite/interpreter/BindableConvention.java`
#### Snippet
```java
  }

  @Override public boolean useAbstractConvertersForConversion(RelTraitSet fromTraits,
      RelTraitSet toTraits) {
    return false;
```

### RedundantMethodOverride
Method `implementNotNullResult()` is identical to its super method
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
    }

    @Override protected Expression implementNotNullResult(AggContext info,
        AggResultContext result) {
      return result.accumulator().get(0);
```

### RedundantMethodOverride
Method `getStatistic()` is identical to its super method
in `file/src/main/java/org/apache/calcite/adapter/file/FileTable.java`
#### Snippet
```java
  }

  @Override public Statistic getStatistic() {
    return Statistics.UNKNOWN;
  }
```

### RedundantMethodOverride
Method `getStatistic()` is identical to its super method
in `file/src/main/java/org/apache/calcite/adapter/file/JsonTable.java`
#### Snippet
```java
  }

  @Override public Statistic getStatistic() {
    return Statistics.UNKNOWN;
  }
```

### RedundantMethodOverride
Method `accept()` is identical to its super method
in `testkit/src/main/java/org/apache/calcite/test/SqlToRelTestBase.java`
#### Snippet
```java
    }

    @Override public RelNode accept(RelShuttle shuttle) {
      return shuttle.visit(this);
    }
```

### RedundantMethodOverride
Method `getDef()` is identical to its super method
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderExtended.java`
#### Snippet
```java
          }

          @Override public MetadataDef<BuiltInMetadata.AllPredicates> getDef() {
            return BuiltInMetadata.AllPredicates.DEF;
          }
```

### RedundantMethodOverride
Method `rolledUpColumnValidInsideAgg()` is identical to its super method
in `testkit/src/main/java/org/apache/calcite/test/schemata/orderstream/OrdersTable.java`
#### Snippet
```java
  }

  @Override public boolean rolledUpColumnValidInsideAgg(String column,
      SqlCall call, @Nullable SqlNode parent, @Nullable CalciteConnectionConfig config) {
    return false;
```

### RedundantMethodOverride
Method `isRolledUp()` is identical to its super method
in `testkit/src/main/java/org/apache/calcite/test/schemata/orderstream/OrdersTable.java`
#### Snippet
```java
  }

  @Override public boolean isRolledUp(String column) {
    return false;
  }
```

### RedundantMethodOverride
Method `nameMatcher()` is identical to its super method
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
  }

  @Override public SqlNameMatcher nameMatcher() {
    return nameMatcher;
  }
```

### RedundantMethodOverride
Method `isCaseSensitive()` is identical to its super method
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
  }

  @Override public boolean isCaseSensitive() {
    return nameMatcher.isCaseSensitive();
  }
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
1 \<\< (bitCount \* j): integer shift implicitly cast to long
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
          long v = 0;
          for (int j = 0; j < chunksPerWord; j++) {
            v |= booleans.get(k++) ? (1 << (bitCount * j)) : 0;
          }
          longs[i] = v;
```

### IntegerMultiplicationImplicitCastToLong
1 \<\< (bitCount \* j): integer shift implicitly cast to long
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
          long v = 0;
          for (int j = 0; j < remainingChunkCount; j++) {
            v |= booleans.get(k++) ? (1 << (bitCount * j)) : 0;
          }
          longs[i] = v;
```

### IntegerMultiplicationImplicitCastToLong
seed \<\< 16: integer shift implicitly cast to long
in `core/src/main/java/org/apache/calcite/runtime/RandomFunction.java`
#### Snippet
```java
  public double randSeed(@Parameter(name = "seed") int seed) {
    if (random == null) {
      random = new Random(seed ^ (seed << 16));
    }
    return random.nextDouble();
```

## RuleId[id=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import org.apache.calcite.rel.type.TimeFrameSet;`
in `core/src/main/java/org/apache/calcite/sql/parser/SqlAbstractParserImpl.java`
#### Snippet
```java
import org.apache.calcite.avatica.util.TimeUnit;
import org.apache.calcite.config.CharLiteralStyle;
import org.apache.calcite.rel.type.TimeFrameSet;
import org.apache.calcite.runtime.CalciteContextException;
import org.apache.calcite.runtime.CalciteException;
```

### UNUSED_IMPORT
Unused import `import org.apache.calcite.runtime.CalciteException;`
in `core/src/main/java/org/apache/calcite/sql/parser/SqlAbstractParserImpl.java`
#### Snippet
```java
import org.apache.calcite.rel.type.TimeFrameSet;
import org.apache.calcite.runtime.CalciteContextException;
import org.apache.calcite.runtime.CalciteException;
import org.apache.calcite.runtime.Resources;
import org.apache.calcite.sql.SqlCall;
```

### UNUSED_IMPORT
Unused import `import org.apache.calcite.runtime.Resources;`
in `core/src/main/java/org/apache/calcite/sql/parser/SqlAbstractParserImpl.java`
#### Snippet
```java
import org.apache.calcite.runtime.CalciteContextException;
import org.apache.calcite.runtime.CalciteException;
import org.apache.calcite.runtime.Resources;
import org.apache.calcite.sql.SqlCall;
import org.apache.calcite.sql.SqlFunctionCategory;
```

## RuleId[id=ListRemoveInLoop]
### ListRemoveInLoop
Can be replaced with 'List.subList().clear()'
in `core/src/main/java/org/apache/calcite/sql/parser/SqlParserUtil.java`
#### Snippet
```java
    requireNonNull(list, "list");
    Preconditions.checkArgument(start < end);
    for (int i = end - 1; i > start; --i) {
      list.remove(i);
    }
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `distinctList` are updated, but never queried
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    final SqlNodeList list = new SqlNodeList(SqlParserPos.ZERO);
    final SqlNodeList filterList = new SqlNodeList(SqlParserPos.ZERO);
    final SqlNodeList distinctList = new SqlNodeList(SqlParserPos.ZERO);
    final SqlNodeList orderList = new SqlNodeList(SqlParserPos.ZERO);

```

### MismatchedCollectionQueryUpdate
Contents of collection `tableNames` are updated, but never queried
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
  /** Mock schema. */
  public static class MockSchema {
    private final List<String> tableNames = new ArrayList<>();
    private String name;

```

### MismatchedCollectionQueryUpdate
Contents of collection `fieldMap` are updated, but never queried
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      String queryString = ((LiteralExpression) query).stringValue();
      @SuppressWarnings("ModifiedButNotUsed")
      Map<String, String> fieldMap = new LinkedHashMap<>();
      for (Expression expr : fields) {
        if (expr instanceof NamedFieldExpression) {
```

## RuleId[id=IteratorHasNextCallsIteratorNext]
### IteratorHasNextCallsIteratorNext
`Iterator.hasNext()` contains call to 'next()'
in `core/src/main/java/org/apache/calcite/util/Filterator.java`
#### Snippet
```java
    // look ahead to see if there are any additional elements
    try {
      lookAhead = next();
      ready = true;
      return true;
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
The value `leftKeys` assigned to `truncatedLeftKeys` is never used
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
    final List<Integer> truncatedRightKeys;
    if (actualLeftKeys.size() == bestKeyOrder.size()) {
      truncatedLeftKeys = leftKeys;
      truncatedRightKeys = rightKeys;
    } else {
```

### UnusedAssignment
The value `rightKeys` assigned to `truncatedRightKeys` is never used
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
    if (actualLeftKeys.size() == bestKeyOrder.size()) {
      truncatedLeftKeys = leftKeys;
      truncatedRightKeys = rightKeys;
    } else {
      truncatedLeftKeys = new ArrayList<>();
```

### UnusedAssignment
The value `null` assigned to `directlyUnderIs` is never used
in `core/src/main/java/org/apache/calcite/rex/RexTransformer.java`
#### Snippet
```java
      if (null != directlyUnderIs) {
        isParentsCount--;
        directlyUnderIs = null;
        return operands.get(0);
      }
```

### UnusedAssignment
The value `registerInput(expr)` assigned to `conditionRef` is never used
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
    RexLocalRef conditionRef = this.conditionRef;
    if (conditionRef == null) {
      this.conditionRef = conditionRef = registerInput(expr);
    } else {
      // AND the new condition with the existing condition.
```

### UnusedAssignment
Variable `refNameNew` initializer `refName` is redundant
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

    SqlIdentifier declNameNew = declName;
    SqlIdentifier refNameNew = refName;
    SqlNodeList partitionListNew = partitionList;
    SqlNodeList orderListNew = orderList;
```

### UnusedAssignment
The value `calciteConnection.server.getStatement(h)` assigned to `stmt` is never used
in `core/src/main/java/org/apache/calcite/jdbc/CalciteMetaImpl.java`
#### Snippet
```java
    final CalciteServerStatement stmt;
    try {
      stmt = calciteConnection.server.getStatement(h);
    } catch (NoSuchStatementException e) {
      // statement is not valid; nothing to do
```

### UnusedAssignment
The value `true` assigned to `cdataEnd` is never used
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
      if (data.contains("]]>")) {
        // TODO: support string that contains cdataEnd literal values
        cdataEnd = true;
      }
    }
```

### UnusedAssignment
Variable `actualStart` initializer `0` is redundant
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      if (start != prevStart || end != prevEnd) {
        // builder5
        int actualStart = 0;
        if (start != prevStart || end < prevEnd) {
          // builder6
```

### UnusedAssignment
Variable `row` initializer `null` is redundant
in `core/src/main/java/org/apache/calcite/interpreter/UncollectNode.java`
#### Snippet
```java

  @Override public void run() throws InterruptedException {
    Row row = null;
    while ((row = source.receive()) != null) {
      for (Object value: row.getValues()) {
```

### UnusedAssignment
Variable `outerRow` initializer `null` is redundant
in `core/src/main/java/org/apache/calcite/interpreter/JoinNode.java`
#### Snippet
```java

    // row from outer source
    Row outerRow = null;
    // rows from inner source
    List<Row> innerRows = null;
```

### UnusedAssignment
Variable `innerRow` initializer `null` is redundant
in `core/src/main/java/org/apache/calcite/interpreter/JoinNode.java`
#### Snippet
```java
      if (innerRows == null) {
        innerRows = new ArrayList<Row>();
        Row innerRow = null;
        while ((innerRow = innerSource.receive()) != null) {
          innerRows.add(innerRow);
```

### UnusedAssignment
Variable `dataType` initializer `null` is redundant
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisDataProcess.java`
#### Snippet
```java
  String dataFormat;
  String keyDelimiter;
  RedisDataType dataType = null;
  RedisDataFormat redisDataFormat = null;
  List<LinkedHashMap<String, Object>> fields;
```

### UnusedAssignment
Variable `redisDataFormat` initializer `null` is redundant
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisDataProcess.java`
#### Snippet
```java
  String keyDelimiter;
  RedisDataType dataType = null;
  RedisDataFormat redisDataFormat = null;
  List<LinkedHashMap<String, Object>> fields;
  private Jedis jedis;
```

### UnusedAssignment
The value `methodCallExpression.targetExpression.accept(this)` assigned to `r` is never used
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/VisitorImpl.java`
#### Snippet
```java
    R r = null;
    if (methodCallExpression.targetExpression != null) {
      r = methodCallExpression.targetExpression.accept(this);
    }
    return Expressions.acceptNodes(methodCallExpression.expressions, this);
```

### UnusedAssignment
The value `newArrayExpression.bound.accept(this)` assigned to `r` is never used
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/VisitorImpl.java`
#### Snippet
```java
    R r = null;
    if (newArrayExpression.bound != null) {
      r = newArrayExpression.bound.accept(this);
    }
    return Expressions.acceptNodes(newArrayExpression.expressions, this);
```

### UnusedAssignment
The value `catchBlock.parameter.accept(this)` assigned to `r` is never used
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/VisitorImpl.java`
#### Snippet
```java
    R r = tryStatement.body.accept(this);
    for (CatchBlock catchBlock : tryStatement.catchBlocks) {
      r = catchBlock.parameter.accept(this);
      r = catchBlock.body.accept(this);
    }
```

### UnusedAssignment
Variable `p` initializer `'\0'` is redundant
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/util/StringUtils.java`
#### Snippet
```java
    int end = 0;
    boolean hasEscapedDelim = false;
    char p = '\0';
    char c = '\0';
    for (int i = 0; i < len; i++, ++end) {
```

### UnusedAssignment
Variable `b` initializer `'\0'` is redundant
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/util/StringUtils.java`
#### Snippet
```java
            StringBuilder sb = new StringBuilder(end - start);
            char a = '\0';
            char b = '\0';
            for (int j = start; j < end; ++j) {
              b = a;
```

### UnusedAssignment
Variable `b` initializer `'\0'` is redundant
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/util/StringUtils.java`
#### Snippet
```java
      StringBuilder sb = new StringBuilder(end - start);
      char a = '\0';
      char b = '\0';
      for (int j = start; j < end; ++j) {
        b = a;
```

### UnusedAssignment
Variable `ename` initializer `null` is redundant
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/StatementTest.java`
#### Snippet
```java
    PreparedStatement st = state.ps;
    ResultSet rs = null;
    String ename = null;
    try {
      st.setInt(1, state.id);
```

### UnusedAssignment
Variable `ename` initializer `null` is redundant
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/StatementTest.java`
#### Snippet
```java
    Statement st = null;
    ResultSet rs = null;
    String ename = null;
    try {
      final PreparedStatement ps =
```

### UnusedAssignment
Variable `ename` initializer `null` is redundant
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/StatementTest.java`
#### Snippet
```java
    Statement st = null;
    ResultSet rs = null;
    String ename = null;
    try {
      st = con.createStatement();
```

## RuleId[id=SuspiciousNameCombination]
### SuspiciousNameCombination
'topViewProject' should probably not be passed as parameter 'left'
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewJoinRule.java`
#### Snippet
```java
      @Nullable RelNode topViewProject, RelNode viewNode, RexNode cond) {
    // Nothing to do
    return Pair.of(topViewProject, viewNode);
  }
}
```

### SuspiciousNameCombination
'topViewProject' should probably not be passed as parameter 'left'
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewAggregateRule.java`
#### Snippet
```java
        if (resultTopViewProject != null) {
          // Both projects could not be merged, we will bail out
          return Pair.of(topViewProject, viewNode);
        }
        resultTopViewProject = topNode;
```

### SuspiciousNameCombination
'resultTopViewProject' should probably not be passed as parameter 'left'
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewAggregateRule.java`
#### Snippet
```java
      }
    }
    return Pair.of(resultTopViewProject, requireNonNull(resultViewNode, "resultViewNode"));
  }

```

### SuspiciousNameCombination
'y' should probably not be assigned to 'x'
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    if (x.length() < y.length()) {
      String c = x;
      x = y;
      y = c;
    }
```

### SuspiciousNameCombination
'right' should probably not be assigned to 'yField'
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
          : Pair.zip(xFields, yFields)) {
        RelDataTypeField xField = pair.left;
        RelDataTypeField yField = pair.right;
        RexNode newX =
            rexBuilder.makeFieldAccess(
```

### SuspiciousNameCombination
'topHints' should probably not be passed as parameter 'left'
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      final boolean hasQueryHints = hasHints && !(node instanceof TableScan);
      if (hasQueryHints) {
        inheritPaths.push(Pair.of(topHints, new ArrayDeque<>()));
      }
      final RelNode node1 = visitChildren(node);
```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `core/src/main/java/org/apache/calcite/runtime/FlipCoordinatesTransformer.java`
#### Snippet
```java
    Coordinate[] coordinateArray =
        Stream.of(coordinateSequence.toCoordinateArray())
            .map(c -> new Coordinate(c.y, c.x))
            .toArray(Coordinate[]::new);
    return new CoordinateArraySequence(coordinateArray);
```

### SuspiciousNameCombination
'x' should probably not be passed as parameter 'y'
in `core/src/main/java/org/apache/calcite/runtime/FlipCoordinatesTransformer.java`
#### Snippet
```java
    Coordinate[] coordinateArray =
        Stream.of(coordinateSequence.toCoordinateArray())
            .map(c -> new Coordinate(c.y, c.x))
            .toArray(Coordinate[]::new);
    return new CoordinateArraySequence(coordinateArray);
```

## RuleId[id=JavadocReference]
### JavadocReference
Symbol `performUnconditionalRewrites(SqlNode, boolean)` is inaccessible from here
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
   * Gets the source SELECT expression for the data to be updated/inserted.
   * Returns null before the statement has been expanded by
   * {@link SqlValidatorImpl#performUnconditionalRewrites(SqlNode, boolean)}.
   *
   * @return the source SELECT for the data to be updated
```

### JavadocReference
Symbol `performUnconditionalRewrites(SqlNode, boolean)` is inaccessible from here
in `core/src/main/java/org/apache/calcite/sql/SqlDelete.java`
#### Snippet
```java
   * Gets the source SELECT expression for the data to be deleted. This
   * returns null before the condition has been expanded by
   * {@link SqlValidatorImpl#performUnconditionalRewrites(SqlNode, boolean)}.
   *
   * @return the source SELECT for the data to be inserted
```

### JavadocReference
Symbol `performUnconditionalRewrites` is inaccessible from here
in `core/src/main/java/org/apache/calcite/sql/SqlOrderBy.java`
#### Snippet
```java
 *
 * <p>It is a purely syntactic operator, and is eliminated by
 * {@link org.apache.calcite.sql.validate.SqlValidatorImpl#performUnconditionalRewrites}
 * and replaced with the ORDER_OPERAND of SqlSelect.
 */
```

### JavadocReference
Symbol `performUnconditionalRewrites(SqlNode, boolean)` is inaccessible from here
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
   * Gets the source SELECT expression for the data to be updated. Returns
   * null before the statement has been expanded by
   * {@link SqlValidatorImpl#performUnconditionalRewrites(SqlNode, boolean)}.
   *
   * @return the source SELECT for the data to be updated
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.sql.parser.impl.SqlParserImpl`
in `core/src/main/java/org/apache/calcite/sql/parser/SqlParserUtil.java`
#### Snippet
```java
  /**
   * Parses string to array literal
   * using {@link org.apache.calcite.sql.parser.impl.SqlParserImpl} parser.
   * String format description can be found at the
   * <a href="https://www.postgresql.org/docs/current/arrays.html#ARRAYS-INPUT">link</a>
```

### JavadocReference
Symbol `clone()` is inaccessible from here
in `core/src/main/java/org/apache/calcite/plan/volcano/package-info.java`
#### Snippet
```java
 *
 * <p>The second trait extension mechanism requires that implementations of
 * {@link org.apache.calcite.rel.AbstractRelNode#clone()} must not assume the
 * type and quantity of traits in their trait set. In either case, the new
 * <code>RelTraitDef</code> implementation must be
```

### JavadocReference
Symbol `lookupVisitMethod` is inaccessible from here
in `core/src/main/java/org/apache/calcite/util/ReflectiveVisitDispatcher.java`
#### Snippet
```java
 * is the base class of visiteE class.
 *
 * <p>TODO: obsolete {@link ReflectUtil#lookupVisitMethod}, and use caching in
 * implementing that method.
 *
```

### JavadocReference
Symbol `SqlLiteral` is inaccessible from here
in `core/src/main/java/org/apache/calcite/util/SaffronProperties.java`
#### Snippet
```java
   * default collation. The default is "ISO-8859-1$en_US". Used in
   * {@link org.apache.calcite.sql.SqlCollation} and
   * {@link org.apache.calcite.sql.SqlLiteral#SqlLiteral}
   */
  @Resource("saffron.default.collation.name")
```

### JavadocReference
Symbol `SqlLiteral` is inaccessible from here
in `core/src/main/java/org/apache/calcite/util/SaffronProperties.java`
#### Snippet
```java
   * which may or may not be different from the {@link #defaultCharset}. The
   * default is "ISO-8859-1". It is used in
   * {@link org.apache.calcite.sql.SqlLiteral#SqlLiteral}
   */
  @Resource("saffron.default.nationalcharset")
```

### JavadocReference
Symbol `SqlLiteral` is inaccessible from here
in `core/src/main/java/org/apache/calcite/util/SaffronProperties.java`
#### Snippet
```java
   * of the default collation. The default is "primary". Used in
   * {@link org.apache.calcite.sql.SqlCollation} and
   * {@link org.apache.calcite.sql.SqlLiteral#SqlLiteral}
   */
  @Resource("saffron.default.collation.strength")
```

### JavadocReference
Symbol `SqlLiteral` is inaccessible from here
in `core/src/main/java/org/apache/calcite/config/CalciteSystemProperty.java`
#### Snippet
```java
   *
   * <p>It is used in {@link org.apache.calcite.sql.SqlCollation} and
   * {@link org.apache.calcite.sql.SqlLiteral#SqlLiteral}.
   */
  // TODO review zabetak:
```

### JavadocReference
Symbol `SqlLiteral` is inaccessible from here
in `core/src/main/java/org/apache/calcite/config/CalciteSystemProperty.java`
#### Snippet
```java
   *
   * <p>It is used with the N'string' construct in
   * {@link org.apache.calcite.sql.SqlLiteral#SqlLiteral}
   * and may be different from the {@link #DEFAULT_CHARSET}.
   */
```

### JavadocReference
Symbol `SqlLiteral` is inaccessible from here
in `core/src/main/java/org/apache/calcite/config/CalciteSystemProperty.java`
#### Snippet
```java
   *
   * <p>It is used in {@link org.apache.calcite.sql.SqlCollation} and
   * {@link org.apache.calcite.sql.SqlLiteral#SqlLiteral}.
   */
  // TODO review zabetak:
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
  public static SqlNodeList toNodeList(SqlNode[] operands) {
    SqlNodeList ret = new SqlNodeList(SqlParserPos.ZERO);
    for (SqlNode node : operands) {
      ret.add(node);
    }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
    if (elements != null) {
      // Add the remaining elements from the input
      for (int i = 1; i < elements.length; i++) {
        finalTuple.add(elements[i]);
      }
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/calcite/rex/RexDigestIncludeType.java`
#### Snippet
```java
  ALWAYS,
  OPTIONAL,
  NO_TYPE;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
    LOCAL,
    DIRECT,
    SHIFT;
  }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/calcite/sql/SqlAccessEnum.java`
#### Snippet
```java
 */
public enum SqlAccessEnum {
  SELECT, UPDATE, INSERT, DELETE;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidator.java`
#### Snippet
```java
      boolean populateBounds) {
    return resolveWindow(windowOrRef, scope);
  };

  /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `testkit/src/main/java/org/apache/calcite/test/MockSqlOperatorTable.java`
#### Snippet
```java
      @Override public String getAllowedSignatures(SqlOperator op, String opName) {
        return "Score(TABLE table_name)";
      };
    }
  }
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `testkit/src/main/java/org/apache/calcite/test/schemata/countries/StatesTableFunction.java`
#### Snippet
```java
  public static ScannableTable states(boolean b) {
    return eval(STATE_ROWS);
  };

  public static ScannableTable parks(boolean b) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `testkit/src/main/java/org/apache/calcite/test/schemata/countries/StatesTableFunction.java`
#### Snippet
```java
  public static ScannableTable parks(boolean b) {
    return eval(PARK_ROWS);
  };

  private static ScannableTable eval(final Object[][] rows) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `testkit/src/main/java/org/apache/calcite/test/schemata/countries/CountriesTableFunction.java`
#### Snippet
```java
      @Override public Enumerable<@Nullable Object[]> scan(DataContext root) {
        return Linq4j.asEnumerable(ROWS);
      };

      @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
Can be replaced with 'containsAll'
in `core/src/main/java/org/apache/calcite/rel/RelCollations.java`
#### Snippet
```java
      return false;
    } else {
      return colKeys.stream().allMatch(i -> distinctKeys.contains(i));
    }
  }
```

### SimplifyStreamApiCallChains
'filter()' and 'map()' can be swapped
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
    return Arrays.stream(geom.getCoordinates())
        .filter(c -> !Double.isNaN(c.getZ()))
        .map(c -> c.getZ())
        .min(Double::compareTo)
        .orElse(Double.NaN);
```

### SimplifyStreamApiCallChains
'filter()' and 'map()' can be swapped
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
    return Arrays.stream(geom.getCoordinates())
        .filter(c -> !Double.isNaN(c.getZ()))
        .map(c -> c.getZ())
        .max(Double::compareTo)
        .orElse(Double.NaN);
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
`substring()` call can be replaced with 'charAt()'
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
      return s.charAt(0) + "." + s.substring(1, 3);
    case 1:
      return s.substring(0, 2) + "." + s.substring(2, 3);
    default:
      return s.substring(0, 3);
```

## RuleId[id=HasPlatformType]
### HasPlatformType
Declaration has type inferred from a platform call, which can lead to unchecked nullability issues. Specify type explicitly as nullable or non-nullable.
in `core/build.gradle.kts`
#### Snippet
```java
// dependencies using the default 'testRuntimeOnly' configuration to simplify the build but at the
// moment they can remain as is.
val testH2 by configurations.creating(integrationTestConfig)
val testOracle by configurations.creating(integrationTestConfig)
val testPostgresql by configurations.creating(integrationTestConfig)
```

### HasPlatformType
Declaration has type inferred from a platform call, which can lead to unchecked nullability issues. Specify type explicitly as nullable or non-nullable.
in `core/build.gradle.kts`
#### Snippet
```java
// moment they can remain as is.
val testH2 by configurations.creating(integrationTestConfig)
val testOracle by configurations.creating(integrationTestConfig)
val testPostgresql by configurations.creating(integrationTestConfig)
val testMysql by configurations.creating(integrationTestConfig)
```

### HasPlatformType
Declaration has type inferred from a platform call, which can lead to unchecked nullability issues. Specify type explicitly as nullable or non-nullable.
in `core/build.gradle.kts`
#### Snippet
```java
val testH2 by configurations.creating(integrationTestConfig)
val testOracle by configurations.creating(integrationTestConfig)
val testPostgresql by configurations.creating(integrationTestConfig)
val testMysql by configurations.creating(integrationTestConfig)

```

### HasPlatformType
Declaration has type inferred from a platform call, which can lead to unchecked nullability issues. Specify type explicitly as nullable or non-nullable.
in `core/build.gradle.kts`
#### Snippet
```java
val testOracle by configurations.creating(integrationTestConfig)
val testPostgresql by configurations.creating(integrationTestConfig)
val testMysql by configurations.creating(integrationTestConfig)

dependencies {
```

### HasPlatformType
Declaration has type inferred from a platform call, which can lead to unchecked nullability issues. Specify type explicitly as nullable or non-nullable.
in `build.gradle.kts`
#### Snippet
```java
)

val sqllineClasspath by configurations.creating {
    isCanBeConsumed = false
    attributes {
```

### HasPlatformType
Declaration has type inferred from a platform call, which can lead to unchecked nullability issues. Specify type explicitly as nullable or non-nullable.
in `example/csv/build.gradle.kts`
#### Snippet
```java
}

val sqllineClasspath by configurations.creating {
    isCanBeConsumed = false
    extendsFrom(configurations.testRuntimeClasspath.get())
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
      assert value instanceof BigDecimal;
      long narrowLong = ((BigDecimal) value).longValue();
      sb.append(String.valueOf(narrowLong));
      sb.append('L');
      break;
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `core/src/main/java/org/apache/calcite/sql/SqlCollation.java`
#### Snippet
```java
      @UnderInitialization SqlCollation this,
      Charset charset) {
    return charset.name().toUpperCase(Locale.ROOT) + "$" + String.valueOf(locale) + "$" + strength;
  }

```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java
        sb.append(
            String.format(Locale.ROOT, "c%s / CASE WHEN c%s > %d THEN c%s ELSE c%s END ",
                String.valueOf(rnd.nextInt(columnLength)), String.valueOf(i % columnLength),
                rnd.nextInt(columnLength), String.valueOf(rnd.nextInt(columnLength)),
                String.valueOf(rnd.nextInt(columnLength))));
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java
        sb.append(
            String.format(Locale.ROOT, "c%s / CASE WHEN c%s > %d THEN c%s ELSE c%s END ",
                String.valueOf(rnd.nextInt(columnLength)), String.valueOf(i % columnLength),
                rnd.nextInt(columnLength), String.valueOf(rnd.nextInt(columnLength)),
                String.valueOf(rnd.nextInt(columnLength))));
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java
            String.format(Locale.ROOT, "c%s / CASE WHEN c%s > %d THEN c%s ELSE c%s END ",
                String.valueOf(rnd.nextInt(columnLength)), String.valueOf(i % columnLength),
                rnd.nextInt(columnLength), String.valueOf(rnd.nextInt(columnLength)),
                String.valueOf(rnd.nextInt(columnLength))));
      }
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java
                String.valueOf(rnd.nextInt(columnLength)), String.valueOf(i % columnLength),
                rnd.nextInt(columnLength), String.valueOf(rnd.nextInt(columnLength)),
                String.valueOf(rnd.nextInt(columnLength))));
      }
      sb.append(" FROM test1");
```

## RuleId[id=JavaReflectionMemberAccess]
### JavaReflectionMemberAccess
Cannot resolve constructor with specified argument types
in `core/src/main/java/org/apache/calcite/util/Compatible.java`
#### Snippet
```java
      try {
        final Constructor<MethodHandles.Lookup> constructor =
            MethodHandles.Lookup.class.getDeclaredConstructor(Class.class,
                int.class);
        if (!constructor.isAccessible()) {
          constructor.setAccessible(true);
```

## RuleId[id=ShiftOutOfRange]
### ShiftOutOfRange
Shift operation `>>>` by out-of-bounds value {Integer.MIN_VALUE..-1}
in `core/src/main/java/org/apache/calcite/util/ImmutableBitSet.java`
#### Snippet
```java
      return fromIndex;
    }
    long word = ~words[u] & (WORD_MASK >>> -(fromIndex + 1));

    while (true) {
```

## RuleId[id=DuplicatedCode]
### DuplicatedCode
Duplicated code
in `babel/src/main/java/org/apache/calcite/sql/babel/SqlBabelCreateTable.java`
#### Snippet
```java
    writer.keyword("TABLE");
    if (ifNotExists) {
      writer.keyword("IF NOT EXISTS");
    }
    name.unparse(writer, leftPrec, rightPrec);
    if (columnList != null) {
      SqlWriter.Frame frame = writer.startList("(", ")");
      for (SqlNode c : columnList) {
        writer.sep(",");
        c.unparse(writer, 0, 0);
      }
      writer.endList(frame);
    }
    if (query != null) {
      writer.keyword("AS");
      writer.newlineAndIndent();
      query.unparse(writer, 0, 0);
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateMaterializedView.java`
#### Snippet
```java
    if (ifNotExists) {
      writer.keyword("IF NOT EXISTS");
    }
    name.unparse(writer, leftPrec, rightPrec);
    if (columnList != null) {
      SqlWriter.Frame frame = writer.startList("(", ")");
      for (SqlNode c : columnList) {
        writer.sep(",");
        c.unparse(writer, 0, 0);
      }
      writer.endList(frame);
    }
```

### DuplicatedCode
Duplicated code
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraFilter.java`
#### Snippet
```java
      final RexNode left = call.operands.get(0);
      final RexNode right = call.operands.get(1);
      String expression = translateBinary2(op, left, right);
      if (expression != null) {
        return expression;
      }
      expression = translateBinary2(rop, right, left);
      if (expression != null) {
        return expression;
      }
      throw new AssertionError("cannot translate op " + op + " call " + call);
```

### DuplicatedCode
Duplicated code
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraRules.java`
#### Snippet
```java
      boolean reversed = sortFieldCollations.get(0).getDirection().reverse().lax()
          == implicitFieldCollations.get(0).getDirection();

      for (int i = 0; i < sortFieldCollations.size(); i++) {
        RelFieldCollation sorted = sortFieldCollations.get(i);
        RelFieldCollation implied = implicitFieldCollations.get(i);

        // Check that the fields being sorted match
        if (sorted.getFieldIndex() != implied.getFieldIndex()) {
          return false;
        }

        // Either all fields must be sorted in the same direction
        // or the opposite direction based on whether we decided
        // if the sort direction should be reversed above
        RelFieldCollation.Direction sortDirection = sorted.getDirection();
        RelFieldCollation.Direction implicitDirection = implied.getDirection();
        if ((!reversed && sortDirection != implicitDirection)
            || (reversed && sortDirection.reverse().lax() != implicitDirection)) {
          return false;
        }
      }

      return true;
```

### DuplicatedCode
Duplicated code
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraToEnumerableConverter.java`
#### Snippet
```java
    List<Map.Entry<String, String>> selectList = new ArrayList<>();
    for (Map.Entry<String, String> entry
            : Pair.zip(cassandraImplementor.selectFields.keySet(),
                cassandraImplementor.selectFields.values())) {
      selectList.add(entry);
    }
    final Expression selectFields =
        list.append("selectFields", constantArrayList(selectList, Pair.class));
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
      final long[] longs = (long[]) dataSet;
      final int chunksPerWord = 64 / bitCount;
      final int word = ordinal / chunksPerWord;
      final long v = longs[word];
      final int chunk = ordinal % chunksPerWord;
      final int mask = (1 << bitCount) - 1;
      final int signMask = 1 << (bitCount - 1);
      final int shift = chunk * bitCount;
      final long w = v >> shift;
      long x = w & mask;
      if (signed && (x & signMask) != 0) {
        x = -x;
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
    v--;
    v |= v >>> 1;
    v |= v >>> 2;
    v |= v >>> 4;
    v |= v >>> 8;
    v |= v >>> 16;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableAggregate.java`
#### Snippet
```java
final JavaTypeFactory typeFactory = implementor.getTypeFactory();
    final BlockBuilder builder = new BlockBuilder();
    final EnumerableRel child = (EnumerableRel) getInput();
    final Result result = implementor.visitChild(this, 0, child, pref);
    Expression childExp =
        builder.append(
            "child",
            result.block);

    final PhysType physType =
        PhysTypeImpl.of(
            typeFactory, getRowType(), pref.preferCustom());

    // final Enumerable<Employee> child = <<child adapter>>;
    // Function1<Employee, Integer> keySelector =
    //     new Function1<Employee, Integer>() {
    //         public Integer apply(Employee a0) {
    //             return a0.deptno;
    //         }
    //     };
    // Function1<Employee, Object[]> accumulatorInitializer =
    //     new Function1<Employee, Object[]>() {
    //         public Object[] apply(Employee a0) {
    //             return new Object[] {0, 0};
    //         }
    //     };
    // Function2<Object[], Employee, Object[]> accumulatorAdder =
    //     new Function2<Object[], Employee, Object[]>() {
    //         public Object[] apply(Object[] a1, Employee a0) {
    //              a1[0] = ((Integer) a1[0]) + 1;
    //              a1[1] = ((Integer) a1[1]) + a0.salary;
    //             return a1;
    //         }
    //     };
    // Function2<Integer, Object[], Object[]> resultSelector =
    //     new Function2<Integer, Object[], Object[]>() {
    //         public Object[] apply(Integer a0, Object[] a1) {
    //             return new Object[] { a0, a1[0], a1[1] };
    //         }
    //     };
    // return childEnumerable
    //     .groupBy(
    //        keySelector, accumulatorInitializer, accumulatorAdder,
    //        resultSelector);
    //
    // or, if key has 0 columns,
    //
    // return childEnumerable
    //     .aggregate(
    //       accumulatorInitializer.apply(),
    //       accumulatorAdder,
    //       resultSelector);
    //
    // with a slightly different resultSelector; or if there are no aggregate
    // functions
    //
    // final Enumerable<Employee> child = <<child adapter>>;
    // Function1<Employee, Integer> keySelector =
    //     new Function1<Employee, Integer>() {
    //         public Integer apply(Employee a0) {
    //             return a0.deptno;
    //         }
    //     };
    // EqualityComparer<Employee> equalityComparer =
    //     new EqualityComparer<Employee>() {
    //         boolean equal(Employee a0, Employee a1) {
    //             return a0.deptno;
    //         }
    //     };
    // return child
    //     .distinct(equalityComparer);

    final PhysType inputPhysType = result.physType;

    ParameterExpression parameter =
        Expressions.parameter(inputPhysType.getJavaRowType(), "a0");

    final PhysType keyPhysType =
        inputPhysType.project(groupSet.asList(), getGroupType() != Group.SIMPLE,
            JavaRowFormat.LIST);
    final int groupCount = getGroupCount();

    final List<AggImpState> aggs = new ArrayList<>(aggCalls.size());
    for (Ord<AggregateCall> call : Ord.zip(aggCalls)) {
      aggs.add(new AggImpState(call.i, call.e, false));
    }

    // Function0<Object[]> accumulatorInitializer =
    //     new Function0<Object[]>() {
    //         public Object[] apply() {
    //             return new Object[] {0, 0};
    //         }
    //     };
    final List<Expression> initExpressions = new ArrayList<>();
    final BlockBuilder initBlock = new BlockBuilder();

    final List<Type> aggStateTypes =
        createAggStateTypes(initExpressions, initBlock, aggs, typeFactory);

    final PhysType accPhysType =
        PhysTypeImpl.of(typeFactory,
            typeFactory.createSyntheticType(aggStateTypes));

    declareParentAccumulator(initExpressions, initBlock, accPhysType);

    final Expression accumulatorInitializer =
        builder.append("accumulatorInitializer",
            Expressions.lambda(
                Function0.class,
                initBlock.toBlock()));

    // Function2<Object[], Employee, Object[]> accumulatorAdder =
    //     new Function2<Object[], Employee, Object[]>() {
    //         public Object[] apply(Object[] acc, Employee in) {
    //              acc[0] = ((Integer) acc[0]) + 1;
    //              acc[1] = ((Integer) acc[1]) + in.salary;
    //             return acc;
    //         }
    //     };
    final ParameterExpression inParameter =
        Expressions.parameter(inputPhysType.getJavaRowType(), "in");
    final ParameterExpression acc_ =
        Expressions.parameter(accPhysType.getJavaRowType(), "acc");

    createAccumulatorAdders(
        inParameter, aggs, accPhysType, acc_, inputPhysType, builder, implementor, typeFactory);

    final ParameterExpression lambdaFactory =
        Expressions.parameter(AggregateLambdaFactory.class,
            builder.newName("lambdaFactory"));
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableAggregateBase.java`
#### Snippet
```java
      List<Types.RecordField> fieldList = synType.getRecordFields();
      for (int i = 0; i < initExpressions.size(); i++) {
        Expression right = initExpressions.get(i);
        initBlock.add(
            Expressions.statement(
                Expressions.assign(
                    Expressions.field(record0_, fieldList.get(i)), right)));
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableBatchNestedLoopJoinRule.java`
#### Snippet
```java
    final ImmutableBitSet.Builder requiredColumns = ImmutableBitSet.builder();

    // Generate first condition
    final RexNode condition = join.getCondition().accept(new RexShuttle() {
      @Override public RexNode visitInputRef(RexInputRef input) {
        int field = input.getIndex();
        if (field >= leftFieldCount) {
          return rexBuilder.makeInputRef(input.getType(),
              input.getIndex() - leftFieldCount);
        }
        requiredColumns.set(field);
        return rexBuilder.makeFieldAccess(corrVar0, field);
      }
    });
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableFilterToCalcRule.java`
#### Snippet
```java
    final RelNode input = filter.getInput();

    // Create a program containing a filter.
    final RexBuilder rexBuilder = filter.getCluster().getRexBuilder();
    final RelDataType inputRowType = input.getRowType();
    final RexProgramBuilder programBuilder =
        new RexProgramBuilder(inputRowType, rexBuilder);
    programBuilder.addIdentity();
    programBuilder.addCondition(filter.getCondition());
    final RexProgram program = programBuilder.getProgram();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableHashJoin.java`
#### Snippet
```java
    double rowCount = mq.getRowCount(this);

    // Joins can be flipped, and for many algorithms, both versions are viable
    // and have the same cost. To make the results stable between versions of
    // the planner, make one of the versions slightly more expensive.
    switch (joinType) {
    case SEMI:
    case ANTI:
      // SEMI and ANTI join cannot be flipped
      break;
    case RIGHT:
      rowCount = RelMdUtil.addEpsilon(rowCount);
      break;
    default:
      if (RelNodes.COMPARATOR.compare(left, right) > 0) {
        rowCount = RelMdUtil.addEpsilon(rowCount);
      }
    }

    // Cheaper if the smaller number of rows is coming from the LHS.
    // Model this by adding L log L to the cost.
    final double rightRowCount = right.estimateRowCount(mq);
    final double leftRowCount = left.estimateRowCount(mq);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableHashJoin.java`
#### Snippet
```java
    BlockBuilder builder = new BlockBuilder();
    final Result leftResult =
        implementor.visitChild(this, 0, (EnumerableRel) left, pref);
    Expression leftExpression =
        builder.append(
            "left", leftResult.block);
    final Result rightResult =
        implementor.visitChild(this, 1, (EnumerableRel) right, pref);
    Expression rightExpression =
        builder.append(
            "right", rightResult.block);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableHashJoin.java`
#### Snippet
```java
    final PhysType keyPhysType =
        leftResult.physType.project(
            joinInfo.leftKeys, JavaRowFormat.LIST);
    Expression predicate = Expressions.constant(null);
    if (!joinInfo.nonEquiConditions.isEmpty()) {
      RexNode nonEquiCondition =
          RexUtil.composeConjunction(getCluster().getRexBuilder(),
              joinInfo.nonEquiConditions, true);
      if (nonEquiCondition != null) {
        predicate =
            EnumUtils.generatePredicate(implementor,
                getCluster().getRexBuilder(), left, right, leftResult.physType,
                rightResult.physType, nonEquiCondition);
      }
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableJoinRule.java`
#### Snippet
```java
      final RexNode equi = info.getEquiCondition(left, right, rexBuilder);
      final RexNode condition;
      if (info.isEqui()) {
        condition = equi;
      } else {
        final RexNode nonEqui = RexUtil.composeConjunction(rexBuilder, info.nonEquiConditions);
        condition = RexUtil.composeConjunction(rexBuilder, Arrays.asList(equi, nonEqui));
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableLimit.java`
#### Snippet
```java
    final BlockBuilder builder = new BlockBuilder();
    final EnumerableRel child = (EnumerableRel) getInput();
    final Result result = implementor.visitChild(this, 0, child, pref);
    final PhysType physType =
        PhysTypeImpl.of(implementor.getTypeFactory(), getRowType(),
            result.format);

    Expression v = builder.append("child", result.block);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableValues.java`
#### Snippet
```java
    for (List<RexLiteral> tuple : tuples) {
      final List<Expression> literals = new ArrayList<>();
      for (Pair<RelDataTypeField, RexLiteral> pair
          : Pair.zip(fields, tuple)) {
        literals.add(
            RexToLixTranslator.translateLiteral(
                pair.right,
                pair.left.getType(),
                typeFactory,
                RexImpTable.NullAs.NULL));
      }
      expressions.add(physType.record(literals));
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/PhysTypeImpl.java`
#### Snippet
```java
    switch (fields.size()) {
    case 0:
      targetFormat = JavaRowFormat.LIST;
      break;
    case 1:
      targetFormat = JavaRowFormat.SCALAR;
      break;
    default:
      break;
    }
    final PhysType targetPhysType =
        project(fields, targetFormat);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/PhysTypeImpl.java`
#### Snippet
```java
      final int index = collation.getFieldIndex();
      final RelDataType fieldType = rowType.getFieldList().get(index).getType();
      final Expression fieldComparator = generateCollatorExpression(fieldType.getCollation());
      Expression arg0 = fieldReference(parameterV0, index);
      Expression arg1 = fieldReference(parameterV1, index);
      switch (Primitive.flavor(fieldClass(index))) {
      case OBJECT:
        arg0 = EnumUtils.convert(arg0, Comparable.class);
        arg1 = EnumUtils.convert(arg1, Comparable.class);
        break;
      default:
        break;
      }
      final boolean nullsFirst =
          collation.nullDirection
              == RelFieldCollation.NullDirection.FIRST;
      final boolean descending =
          collation.getDirection()
              == RelFieldCollation.Direction.DESCENDING;
      body.add(
          Expressions.statement(
              Expressions.assign(
                  parameterC,
                  Expressions.call(
                      Utilities.class,
                      fieldNullable(index)
                          ? (nullsFirst != descending
                          ? "compareNullsFirst"
                          : "compareNullsLast")
                          : "compare",
                      Expressions.list(
                          arg0,
                          arg1)
                          .appendIfNotNull(fieldComparator)))));
      body.add(
          Expressions.ifThen(
              Expressions.notEqual(
                  parameterC, Expressions.constant(0)),
              Expressions.return_(
                  null,
                  descending
                      ? Expressions.negate(parameterC)
                      : parameterC)));
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/adapter/enumerable/PhysTypeImpl.java`
#### Snippet
```java
    body.add(
        Expressions.return_(null, Expressions.constant(0)));

    final List<MemberDeclaration> memberDeclarations =
        Expressions.list(
            Expressions.methodDecl(
                Modifier.PUBLIC,
                int.class,
                "compare",
                ImmutableList.of(
                    parameterV0, parameterV1),
                body.toBlock()));

    if (EnumerableRules.BRIDGE_METHODS) {
      final ParameterExpression parameterO0 =
          Expressions.parameter(Object.class, "o0");
      final ParameterExpression parameterO1 =
          Expressions.parameter(Object.class, "o1");
      BlockBuilder bridgeBody = new BlockBuilder();
      bridgeBody.add(
          Expressions.return_(
              null,
              Expressions.call(
                  Expressions.parameter(
                      Comparable.class, "this"),
                  BuiltInMethod.COMPARATOR_COMPARE.method,
                  Expressions.convert_(
                      parameterO0,
                      javaRowClass),
                  Expressions.convert_(
                      parameterO1,
                      javaRowClass))));
      memberDeclarations.add(
          overridingMethodDecl(
              BuiltInMethod.COMPARATOR_COMPARE.method,
              ImmutableList.of(parameterO0, parameterO1),
              bridgeBody.toBlock()));
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/jdbc/CachingCalciteSchema.java`
#### Snippet
```java
      if (explicitTables.containsKey(s)) {
        continue;
      }
      for (Function function : schema.getFunctions(s)) {
        if (function instanceof TableMacro
            && function.getParameters().isEmpty()) {
          final Table table = ((TableMacro) function).apply(ImmutableList.of());
          builder.put(s, table);
        }
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      final Pair<RexNode, List<RexNode>> qInput0Explained = explainCalc(qInput0);
      final RexNode qInput0Cond = qInput0Explained.left;
      final List<RexNode> qInput0Projs = qInput0Explained.right;

      final MutableJoin target = (MutableJoin) call.target;

      final RexBuilder rexBuilder = call.getCluster().getRexBuilder();

      // Check whether is same join type.
      final JoinRelType joinRelType = sameJoinType(query.joinType, target.joinType);
      if (joinRelType == null) {
        return null;
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      final List<MutableRel> queryInputs = new ArrayList<>(query.getInputs());
      final List<MutableRel> targetInputs = new ArrayList<>(target.getInputs());
      if (query.isAll() == target.isAll()
          && sameRelCollectionNoOrderConsidered(queryInputs, targetInputs)) {
        return call.result(target);
      }
      return null;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
            final ImmutableBitSet dependents =
                space.columnOrdinals.except(s.columnOrdinals);
            for (int i : s.columnOrdinals) {
              final Space s1 = singletonSpaces.get(i);
              final ImmutableBitSet rest = s.columnOrdinals.clear(i);
              for (ImmutableBitSet dependent : requireNonNull(s1, "s1").dependents) {
                if (rest.contains(dependent)) {
                  // The "key" of this functional dependency is not minimal.
                  // For instance, if we know that
                  //   (a) -> x
                  // then
                  //   (a, b, x) -> y
                  // is not minimal; we could say the same with a smaller key:
                  //   (a, b) -> y
                  ++nonMinimal;
                  continue dependents;
                }
              }
            }
            for (int dependent : dependents) {
              final Space s1 = singletonSpaces.get(dependent);
              for (ImmutableBitSet d : requireNonNull(s1, "s1").dependents) {
                if (s.columnOrdinals.contains(d)) {
                  ++nonMinimal;
                  continue dependents;
                }
              }
            }
            space.dependencies.or(dependents.toBitSet());
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/RelFieldCollation.java`
#### Snippet
```java
      switch (this) {
      case ASCENDING:
        return DESCENDING;
      case STRICTLY_ASCENDING:
        return STRICTLY_DESCENDING;
      case DESCENDING:
        return ASCENDING;
      case STRICTLY_DESCENDING:
        return STRICTLY_ASCENDING;
      default:
        return this;
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdAllPredicates.java`
#### Snippet
```java
    final Set<RelDataTypeField> inputExtraFields = new LinkedHashSet<>();
    final RelOptUtil.InputFinder inputFinder = new RelOptUtil.InputFinder(inputExtraFields);
    pred.accept(inputFinder);
    final ImmutableBitSet inputFieldsUsed = inputFinder.build();

    // Infer column origin expressions for given references
    final Map<RexInputRef, Set<RexNode>> mapping = new LinkedHashMap<>();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdAllPredicates.java`
#### Snippet
```java
      final RelOptPredicateList inputPreds = mq.getAllPredicates(input);
      if (inputPreds == null) {
        // Bail out
        return null;
      }
      // Gather table references
      final Set<RelTableRef> tableRefs = mq.getTableReferences(input);
      if (tableRefs == null) {
        return null;
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdAllPredicates.java`
#### Snippet
```java
        final Map<RelTableRef, RelTableRef> currentTablesMapping = new HashMap<>();
        for (RelTableRef rightRef : tableRefs) {
          int shift = 0;
          Collection<RelTableRef> lRefs =
              qualifiedNamesToRefs.get(rightRef.getQualifiedName());
          if (lRefs != null) {
            shift = lRefs.size();
          }
          currentTablesMapping.put(rightRef,
              RelTableRef.of(rightRef.getTable(), shift + rightRef.getEntityNumber()));
        }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdExpressionLineage.java`
#### Snippet
```java
    for (RelTableRef rightRef : rightTableRefs) {
      int shift = 0;
      Collection<RelTableRef> lRefs =
          qualifiedNamesToRefs.get(rightRef.getQualifiedName());
      if (lRefs != null) {
        shift = lRefs.size();
      }
      currentTablesMapping.put(rightRef,
          RelTableRef.of(rightRef.getTable(), shift + rightRef.getEntityNumber()));
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdDistinctRowCount.java`
#### Snippet
```java
    final List<RexNode> notPushable = new ArrayList<>();
    final List<RexNode> pushable = new ArrayList<>();
    RelOptUtil.splitFilters(
        rel.getGroupSet(),
        predicate,
        pushable,
        notPushable);
    final RexBuilder rexBuilder = rel.getCluster().getRexBuilder();
    RexNode childPreds =
        RexUtil.composeConjunction(rexBuilder, pushable, true);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdDistinctRowCount.java`
#### Snippet
```java
    final List<RexNode> notPushable = new ArrayList<>();
    final List<RexNode> pushable = new ArrayList<>();
    RelOptUtil.splitFilters(
        ImmutableBitSet.range(rel.getRowType().getFieldCount()),
        predicate,
        pushable,
        notPushable);
    final RexBuilder rexBuilder = rel.getCluster().getRexBuilder();

    // get the distinct row count of the child input, passing in the
    // columns and filters that only reference the child; convert the
    // filter to reference the children projection expressions
    RexNode childPred =
        RexUtil.composeConjunction(rexBuilder, pushable, true);
    RexNode modifiedPred;
    if (childPred == null) {
      modifiedPred = null;
    } else {
      modifiedPred = RelOptUtil.pushPastProject(childPred, rel);
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdDistinctRowCount.java`
#### Snippet
```java
    if (projCols.cardinality() == 0) {
      return distinctRowCount;
    }

    // multiply by the cardinality of the non-child projection expressions
    for (int bit : projCols.build()) {
      Double subRowCount =
          RelMdUtil.cardOfProjExpr(mq, rel, projExprs.get(bit));
      if (subRowCount == null) {
        return null;
      }
      distinctRowCount *= subRowCount;
    }

    return RelMdUtil.numDistinctVals(distinctRowCount, mq.getRowCount(rel));
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdMaxRowCount.java`
#### Snippet
```java
    Double rowCount = mq.getMaxRowCount(rel.getInput());
    if (rowCount == null) {
      rowCount = Double.POSITIVE_INFINITY;
    }

    final int offset = rel.offset instanceof RexLiteral ? RexLiteral.intValue(rel.offset) : 0;
    rowCount = Math.max(rowCount - offset, 0D);

    final double limit =
        rel.fetch instanceof RexLiteral ? RexLiteral.intValue(rel.fetch) : rowCount;
    return limit < rowCount ? limit : rowCount;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdMaxRowCount.java`
#### Snippet
```java
    Util.discard(Bug.CALCITE_1048_FIXED);
    for (RelNode node : rel.getRels()) {
      if (node instanceof Sort) {
        Sort sort = (Sort) node;
        if (sort.fetch instanceof RexLiteral) {
          return (double) RexLiteral.intValue(sort.fetch);
        }
      }
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdMinRowCount.java`
#### Snippet
```java
    Double rowCount = mq.getMinRowCount(rel.getInput());
    if (rowCount == null) {
      rowCount = 0D;
    }

    final int offset = rel.offset instanceof RexLiteral ? RexLiteral.intValue(rel.offset) : 0;
    rowCount = Math.max(rowCount - offset, 0D);

    final double limit =
        rel.fetch instanceof RexLiteral ? RexLiteral.intValue(rel.fetch) : rowCount;
    return limit < rowCount ? limit : rowCount;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdRowCount.java`
#### Snippet
```java
    Double rowCount = mq.getRowCount(rel.getInput());
    if (rowCount == null) {
      return null;
    }

    final int offset = rel.offset instanceof RexLiteral ? RexLiteral.intValue(rel.offset) : 0;
    rowCount = Math.max(rowCount - offset, 0D);

    final double limit =
        rel.fetch instanceof RexLiteral ? RexLiteral.intValue(rel.fetch) : rowCount;
    return limit < rowCount ? limit : rowCount;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdTableReferences.java`
#### Snippet
```java
      int shift = 0;
      Collection<RelTableRef> lRefs =
          leftQualifiedNamesToRefs.get(rightRef.getQualifiedName());
      if (lRefs != null) {
        shift = lRefs.size();
      }
      RelTableRef shiftTableRef =
          RelTableRef.of(rightRef.getTable(), shift + rightRef.getEntityNumber());
      assert !result.contains(shiftTableRef);
      result.add(shiftTableRef);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java
    String name = rowType.getFieldNames().get(selectList.size());
    @Nullable String alias = SqlValidatorUtil.alias(node);
    if (alias == null || !alias.equals(name)) {
      node = as(node, name);
    }
    selectList.add(node);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
      if (o0.getKind() == SqlKind.CAST
          && o1.getKind() != SqlKind.CAST) {
        if (!dialect.supportsImplicitTypeCoercion((RexCall) o0)) {
          // If the dialect does not support implicit type coercion,
          // we definitely can not strip the cast.
          return node;
        }
        final RexNode o0b = ((RexCall) o0).getOperands().get(0);
        return call.clone(call.getType(), ImmutableList.of(o0b, o1));
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateJoinJoinRemoveRule.java`
#### Snippet
```java
    final List<Integer> leftKeys = new ArrayList<>();
    RelOptUtil.splitJoinCondition(topJoin.getLeft(), topJoin.getRight(),
        topJoin.getCondition(), leftKeys, new ArrayList<>(),
        new ArrayList<>());
    if (leftKeys.stream().anyMatch(s -> s >= leftBottomChildSize)) {
      return;
    }

    // Check whether left join keys in top join and bottom join are equal.
    final List<Integer> leftChildKeys = new ArrayList<>();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateJoinJoinRemoveRule.java`
#### Snippet
```java
    final ImmutableBitSet groupSet = aggregate.getGroupSet().permute(map);

    final ImmutableList.Builder<AggregateCall> aggCalls =
        ImmutableList.builder();
    final int sourceCount = aggregate.getInput().getRowType().getFieldCount();
    final Mappings.TargetMapping targetMapping =
        Mappings.target(map, sourceCount, sourceCount);
    aggregate.getAggCallList().forEach(
        aggregateCall ->
            aggCalls.add(aggregateCall.transform(targetMapping)));
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/ExchangeRemoveConstantKeysRule.java`
#### Snippet
```java
    final RelMetadataQuery mq = call.getMetadataQuery();
    final RelNode input = exchange.getInput();
    final RelOptPredicateList predicates = mq.getPulledUpPredicates(input);
    if (RelOptPredicateList.isEmpty(predicates)) {
      return;
    }

    final Set<Integer> constants = new HashSet<>();
    predicates.constantMap.keySet().forEach(key -> {
      if (key instanceof RexInputRef) {
        constants.add(((RexInputRef) key).getIndex());
      }
    });
    if (constants.isEmpty()) {
      return;
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/JoinAssociateRule.java`
#### Snippet
```java
    if (topJoin.getJoinType() != JoinRelType.INNER
        || bottomJoin.getJoinType() != JoinRelType.INNER) {
      return;
    }

    // Goal is to transform to
    //
    //       newTopJoin
    //        /     \
    //       A   newBottomJoin
    //               /    \
    //              B      C

    // Split the condition of topJoin and bottomJoin into a conjunctions. A
    // condition can be pushed down if it does not use columns from A.
    final List<RexNode> top = new ArrayList<>();
    final List<RexNode> bottom = new ArrayList<>();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/JoinPushThroughJoinRule.java`
#### Snippet
```java
    final Join topJoin = call.rel(0);
    final Join bottomJoin = call.rel(1);
    final RelNode relC = call.rel(2);
    final RelNode relA = bottomJoin.getLeft();
    final RelNode relB = bottomJoin.getRight();
    final RelOptCluster cluster = topJoin.getCluster();

    //        topJoin
    //        /     \
    //   bottomJoin  C
    //    /    \
    //   A      B

    final int aCount = relA.getRowType().getFieldCount();
    final int bCount = relB.getRowType().getFieldCount();
    final int cCount = relC.getRowType().getFieldCount();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/JoinPushThroughJoinRule.java`
#### Snippet
```java
    final List<RexNode> newBottomList = new ArrayList<>();
    new RexPermuteInputsShuttle(bottomMapping, relA, relC)
        .visitList(nonIntersecting, newBottomList);
    new RexPermuteInputsShuttle(bottomMapping, relA, relC)
        .visitList(bottomNonIntersecting, newBottomList);
    final RexBuilder rexBuilder = cluster.getRexBuilder();
    RexNode newBottomCondition =
        RexUtil.composeConjunction(rexBuilder, newBottomList);
    final Join newBottomJoin =
        bottomJoin.copy(bottomJoin.getTraitSet(), newBottomCondition, relA,
            relC, bottomJoin.getJoinType(), bottomJoin.isSemiJoinDone());
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/JoinPushThroughJoinRule.java`
#### Snippet
```java
    final List<RexNode> newTopList = new ArrayList<>();
    new RexPermuteInputsShuttle(topMapping, newBottomJoin, relB)
        .visitList(intersecting, newTopList);
    new RexPermuteInputsShuttle(topMapping, newBottomJoin, relB)
        .visitList(bottomIntersecting, newTopList);
    RexNode newTopCondition =
        RexUtil.composeConjunction(rexBuilder, newTopList);
    @SuppressWarnings("SuspiciousNameCombination")
    final Join newTopJoin =
        topJoin.copy(topJoin.getTraitSet(), newTopCondition, newBottomJoin,
            relB, topJoin.getJoinType(), topJoin.isSemiJoinDone());
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/JoinToMultiJoinRule.java`
#### Snippet
```java
      final MultiJoin leftMultiJoin = (MultiJoin) left;
      for (int i = 0; i < left.getInputs().size(); i++) {
        newInputs.add(leftMultiJoin.getInput(i));
        projFieldsList.add(leftMultiJoin.getProjFields().get(i));
        joinFieldRefCountsList.add(
            requireNonNull(leftMultiJoin.getJoinFieldRefCountsMap().get(i)).toIntArray());
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewAggregateRule.java`
#### Snippet
```java
    final Set<RelTableRef> extraTableRefs = new HashSet<>();
    for (RelTableRef tRef : queryTableRefs) {
      if (!viewTableRefs.contains(tRef)) {
        // Add to extra tables if table is not part of the view
        extraTableRefs.add(tRef);
      }
    }
    Multimap<Class<? extends RelNode>, RelNode> nodeTypes = mq.getNodeTypes(node);
    if (nodeTypes == null) {
      return null;
    }
    Collection<RelNode> tableScanNodes = nodeTypes.get(TableScan.class);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewAggregateRule.java`
#### Snippet
```java
    List<RelNode> newRels = new ArrayList<>();
    for (RelTableRef tRef : extraTableRefs) {
      int i = 0;
      for (RelNode relNode : tableScanNodes) {
        TableScan scan = (TableScan) relNode;
        if (tRef.getQualifiedName().equals(scan.getTable().getQualifiedName())) {
          if (tRef.getEntityNumber() == i++) {
            newRels.add(relNode);
            break;
          }
        }
      }
    }
    assert extraTableRefs.size() == newRels.size();

    relBuilder.push(input);
    for (RelNode newRel : newRels) {
      // Add to the view
      relBuilder.push(newRel);
      relBuilder.join(JoinRelType.INNER, rexBuilder.makeLiteral(true));
    }
    final RelNode newView = relBuilder.build();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewAggregateRule.java`
#### Snippet
```java
    HepProgram unionRewritingPullProgram = config.unionRewritingPullProgram();
    if (unionRewritingPullProgram != null) {
      final HepPlanner tmpPlanner = new HepPlanner(unionRewritingPullProgram);
      tmpPlanner.setRoot(newAggregateInput);
      newAggregateInput = tmpPlanner.findBestExp();
      target = newAggregateInput.getInput(0);
    }

    // We need to check that all columns required by compensating predicates
    // are contained in the query.
    List<RexNode> queryExprs = extractReferences(rexBuilder, target);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewAggregateRule.java`
#### Snippet
```java
    relBuilder.push(unionInputQuery);
    relBuilder.push(unionInputView);
    relBuilder.union(true);
    List<RexNode> exprList = new ArrayList<>(relBuilder.peek().getRowType().getFieldCount());
    List<String> nameList = new ArrayList<>(relBuilder.peek().getRowType().getFieldCount());
    for (int i = 0; i < relBuilder.peek().getRowType().getFieldCount(); i++) {
      // We can take unionInputQuery as it is query based.
      RelDataTypeField field = unionInputQuery.getRowType().getFieldList().get(i);
      exprList.add(
          rexBuilder.ensureType(
              field.getType(),
              rexBuilder.makeInputRef(relBuilder.peek(), i),
              true));
      nameList.add(field.getName());
    }
    relBuilder.project(exprList, nameList);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
      final EquivalenceClasses qEC = new EquivalenceClasses();
      for (RexNode conj : RelOptUtil.conjunctions(queryPreds.left)) {
        assert conj.isA(SqlKind.EQUALS);
        RexCall equiCond = (RexCall) conj;
        qEC.addEquivalenceClass(
            (RexTableInputRef) equiCond.getOperands().get(0),
            (RexTableInputRef) equiCond.getOperands().get(1));
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
      final RexNode expr = nodeExprs.get(i);
      final Set<RexNode> lineages = mq.getExpressionLineage(node, expr);
      if (lineages == null) {
        // Next expression
        continue;
      }
      if (lineages.size() != 1) {
        throw new IllegalStateException("We only support project - filter - join, "
            + "thus expression lineage should map to a single expression, got: '"
            + lineages + "' for expr '" + expr + "' in node '" + node + "'");
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
              Collection<Integer> c = exprsLineage.get(ref);
              if (c.isEmpty()) {
                // Cannot map expression
                throw Util.FoundOne.NULL;
              }
              int pos = c.iterator().next();
              if (rewritingMapping != null) {
                if (!rewritingMapping.containsKey(pos)) {
                  // Cannot map expression
                  throw Util.FoundOne.NULL;
                }
                pos = rewritingMapping.get(pos).iterator().next();
              }
              if (node != null) {
                return rexBuilder.makeInputRef(node, pos);
              }
              return rexBuilder.makeInputRef(ref.getType(), pos);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeSystem.java`
#### Snippet
```java
        type1 = RelDataTypeFactoryImpl.isJavaType(type1)
            ? typeFactory.decimalOf(type1)
            : type1;
        type2 = RelDataTypeFactoryImpl.isJavaType(type2)
            ? typeFactory.decimalOf(type2)
            : type2;
        int p1 = type1.getPrecision();
        int p2 = type2.getPrecision();
        int s1 = type1.getScale();
        int s2 = type2.getScale();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rel/type/TimeFrameSet.java`
#### Snippet
```java
      final int y2 =
          (int) DateTimeUtils.unixDateExtract(TimeUnitRange.YEAR, date);
      final int m2 =
          (int) DateTimeUtils.unixDateExtract(TimeUnitRange.MONTH, date);
      final int fullMonth = TimeFrames.fullMonth(y2, m2);

      final int m = perMonth.getDenominator().intValueExact(); // e.g. 12 for YEAR
      final int mod = floorMod(fullMonth - frame.monthEpoch(), m);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
      switch (toType.getSqlTypeName()) {
      case CHAR:
        return SqlTypeUtil.comparePrecision(toType.getPrecision(), length) == 0;
      case VARCHAR:
        return SqlTypeUtil.comparePrecision(toType.getPrecision(), length) >= 0;
      default:
        throw new AssertionError(toType);
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/runtime/AutomatonBuilder.java`
#### Snippet
```java
    final State beforeState = createState();
    final State afterState = createState();
    transitionList.add(new EpsilonTransition(fromState, beforeState));
    add(pattern, beforeState, afterState);
    transitionList.add(new EpsilonTransition(afterState, beforeState));
    transitionList.add(new EpsilonTransition(afterState, toState));
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        if (t0 == null) {
          return 0;
        }
        if (t1 == null) {
          return 1;
        }
        if (t2 == null) {
          return 2;
        }
        if (t3 == null) {
          return 3;
        }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        if (o.equals(t0)) {
          return 0;
        }
        if (o.equals(t1)) {
          return 1;
        }
        if (o.equals(t2)) {
          return 2;
        }
        if (o.equals(t3)) {
          return 3;
        }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        if (t3 == null) {
          return 3;
        }
        if (t2 == null) {
          return 2;
        }
        if (t1 == null) {
          return 1;
        }
        if (t0 == null) {
          return 0;
        }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        if (o.equals(t3)) {
          return 3;
        }
        if (o.equals(t2)) {
          return 2;
        }
        if (o.equals(t1)) {
          return 1;
        }
        if (o.equals(t0)) {
          return 0;
        }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        if (t4 == null) {
          return 4;
        }
        if (t3 == null) {
          return 3;
        }
        if (t2 == null) {
          return 2;
        }
        if (t1 == null) {
          return 1;
        }
        if (t0 == null) {
          return 0;
        }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        if (o.equals(t4)) {
          return 4;
        }
        if (o.equals(t3)) {
          return 3;
        }
        if (o.equals(t2)) {
          return 2;
        }
        if (o.equals(t1)) {
          return 1;
        }
        if (o.equals(t0)) {
          return 0;
        }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
    for (Coordinate coordinate : geom.getCoordinates()) {
      double distance = point.getCoordinate().distance(coordinate);
      if (distance < minDistance) {
        minDistance = distance;
        closestCoordinates.clear();
        closestCoordinates.add(coordinate);
      } else if (distance == minDistance && !closestCoordinates.contains(coordinate)) {
        closestCoordinates.add(coordinate);
      }
    }
    if (closestCoordinates.size() == 1) {
      return GEOMETRY_FACTORY.createPoint(closestCoordinates.get(0));
    } else {
      Coordinate[] coordinates = closestCoordinates.toArray(new Coordinate[0]);
      return GEOMETRY_FACTORY.createMultiPointFromCoords(coordinates);
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/SqlDelete.java`
#### Snippet
```java
    final int opLeft = getOperator().getLeftPrec();
    final int opRight = getOperator().getRightPrec();
    targetTable.unparse(writer, opLeft, opRight);
    SqlIdentifier alias = this.alias;
    if (alias != null) {
      writer.keyword("AS");
      alias.unparse(writer, opLeft, opRight);
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
      int found = s.indexOf(find);
      if (found == -1) {
        return s;
      }
      StringBuilder sb = new StringBuilder(s.length());
      int start = 0;
      for (;;) {
        for (; start < found; start++) {
          sb.append(s.charAt(start));
        }
        if (found == s.length()) {
          break;
        }
        sb.append(replace);
        start += find.length();
        found = s.indexOf(find, start);
        if (found == -1) {
          found = s.length();
        }
      }
      return sb.toString();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/SqlHopTableFunction.java`
#### Snippet
```java
      if (!checkTableAndDescriptorOperands(callBinding, 1)) {
        return throwValidationSignatureErrorOrReturnFalse(callBinding, throwOnFailure);
      }
      if (!checkTimeColumnDescriptorOperand(callBinding, 1)) {
        return throwValidationSignatureErrorOrReturnFalse(callBinding, throwOnFailure);
      }
      if (!checkIntervalOperands(callBinding, 2)) {
        return throwValidationSignatureErrorOrReturnFalse(callBinding, throwOnFailure);
      }
      return true;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/SqlLiteral.java`
#### Snippet
```java
      } else if (clazz == BigDecimal.class) {
        return clazz.cast(BigDecimal.valueOf(getValueAs(Long.class)));
      } else if (clazz == TimeUnitRange.class) {
        return clazz.cast(qualifier.timeUnitRange);
      } else if (clazz == TimeUnit.class) {
        return clazz.cast(qualifier.timeUnitRange.startUnit);
      } else if (clazz == SqlIntervalQualifier.class) {
        return clazz.cast(qualifier);
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/SqlLiteral.java`
#### Snippet
```java
    BigDecimal value;
    int prec;
    int scale;

    int i = s.indexOf('.');
    if ((i >= 0) && ((s.length() - 1) != i)) {
      value = SqlParserUtil.parseDecimal(s);
      scale = s.length() - i - 1;
      assert scale == value.scale() : s;
      prec = s.length() - 1;
    } else if ((i >= 0) && ((s.length() - 1) == i)) {
      value = SqlParserUtil.parseInteger(s.substring(0, i));
      scale = 0;
      prec = s.length() - 1;
    } else {
      value = SqlParserUtil.parseInteger(s);
      scale = 0;
      prec = s.length();
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/SqlPivot.java`
#### Snippet
```java
    final Set<String> columnNames = new HashSet<>();
    final SqlVisitor<Void> nameCollector = new SqlBasicVisitor<Void>() {
      @Override public Void visit(SqlIdentifier id) {
        columnNames.add(Util.last(id.names));
        return super.visit(id);
      }
    };
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateView.java`
#### Snippet
```java
    name.unparse(writer, leftPrec, rightPrec);
    if (columnList != null) {
      SqlWriter.Frame frame = writer.startList("(", ")");
      for (SqlNode c : columnList) {
        writer.sep(",");
        c.unparse(writer, 0, 0);
      }
      writer.endList(frame);
    }
    writer.keyword("AS");
    writer.newlineAndIndent();
    query.unparse(writer, 0, 0);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/dialect/BigQuerySqlDialect.java`
#### Snippet
```java
    switch (trimFlag.getValueAs(SqlTrimFunction.Flag.class)) {
    case LEADING:
      operatorName = "LTRIM";
      break;
    case TRAILING:
      operatorName = "RTRIM";
      break;
    default:
      operatorName = call.getOperator().getName();
      break;
    }
    final SqlWriter.Frame trimFrame = writer.startFunCall(operatorName);
    call.operand(2).unparse(writer, leftPrec, rightPrec);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/dialect/FireboltSqlDialect.java`
#### Snippet
```java
    switch (kind) {
    case ANY_VALUE:
    case AVG:
    case COUNT:
    case MAX:
    case MIN:
    case STDDEV_SAMP:
    case SUM:
      return true;
    default:
      break;
    }
    return false;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/dialect/FireboltSqlDialect.java`
#### Snippet
```java
      switch (call.getKind()) {
      case FLOOR:
        if (call.operandCount() != 2) {
          super.unparseCall(writer, call, leftPrec, rightPrec);
          return;
        }

        final SqlLiteral timeUnitNode = call.operand(1);
        final TimeUnitRange timeUnit = timeUnitNode.getValueAs(TimeUnitRange.class);

        SqlCall call2 =
            SqlFloorFunction.replaceTimeUnitOperand(call, timeUnit.name(),
                timeUnitNode.getParserPosition());
        SqlFloorFunction.unparseDatetimeFunction(writer, call2, "DATE_TRUNC", false);
        break;

      default:
        super.unparseCall(writer, call, leftPrec, rightPrec);
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/fun/SqlDotOperator.java`
#### Snippet
```java
    SqlNode left = list.node(ordinal - 1);
    SqlNode right = list.node(ordinal + 1);
    return new ReduceResult(ordinal - 1,
        ordinal + 2,
        createCall(
            SqlParserPos.sum(
                Arrays.asList(requireNonNull(left, "left").getParserPosition(),
                    requireNonNull(right, "right").getParserPosition(),
                    list.pos(ordinal))),
            left,
            right));
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/fun/SqlJsonModifyFunction.java`
#### Snippet
```java
    assert operandsCount % 2 == 1;
    StringBuilder sb = new StringBuilder();
    sb.append("{0}(");
    for (int i = 1; i < operandsCount; i++) {
      sb.append(String.format(Locale.ROOT, "{%d} ", i + 1));
    }
    sb.append("{1})");
    return sb.toString();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/fun/SqlMonotonicBinaryOperator.java`
#### Snippet
```java
      switch (value == null ? 1 : value.signum()) {
      case -1:
        // mono0 * negative constant --> reverse mono0
        return mono0.reverse();

      case 0:
        // mono0 * 0 --> constant (zero)
        return SqlMonotonicity.CONSTANT;

      default:
        // mono0 * positive constant --> mono0
        return mono0;
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/fun/SqlStdOperatorTable.java`
#### Snippet
```java
    switch (comparisonKind) {
    case EQUALS:
      return SOME_EQ;
    case NOT_EQUALS:
      return SOME_NE;
    case LESS_THAN:
      return SOME_LT;
    case LESS_THAN_OR_EQUAL:
      return SOME_LE;
    case GREATER_THAN:
      return SOME_GT;
    case GREATER_THAN_OR_EQUAL:
      return SOME_GE;
    default:
      throw new AssertionError(comparisonKind);
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
          if (itemCount == 0) {
            if (newlineAfterOpen) {
              newlineAndIndent(currentIndent);
            }
          } else {
            if (newlineBeforeSep) {
              newlineAndIndent(currentIndent + sepIndent);
            }
          }
          if ((itemCount > 0) || printFirst) {
            keyword(sep);
            nextWhitespace = newlineAfterSep ? NL : " ";
          }
          ++itemCount;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/type/ArraySqlType.java`
#### Snippet
```java
    return new RelDataTypePrecedenceList() {
      @Override public boolean containsType(RelDataType type) {
        if (type.getSqlTypeName() != getSqlTypeName()) {
          return false;
        }
        RelDataType otherComponentType = type.getComponentType();
        return otherComponentType != null
            && getComponentType().getPrecedenceList().containsType(otherComponentType);
      }

      @Override public int compareTypePrecedence(RelDataType type1, RelDataType type2) {
        if (!containsType(type1)) {
          throw new IllegalArgumentException("must contain type: " + type1);
        }
        if (!containsType(type2)) {
          throw new IllegalArgumentException("must contain type: " + type2);
        }
        return getComponentType().getPrecedenceList()
            .compareTypePrecedence(getComponentTypeOrThrow(type1), getComponentTypeOrThrow(type2));
      }
    };
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/type/OperandTypes.java`
#### Snippet
```java
          if (!LITERAL.checkSingleOperandType(
              callBinding,
              operand,
              iFormalOperand,
              throwOnFailure)) {
            return false;
          }

          if (!super.checkSingleOperandType(
              callBinding,
              operand,
              iFormalOperand,
              throwOnFailure)) {
            return false;
          }

          final SqlLiteral arg = (SqlLiteral) operand;
          final BigDecimal value = arg.getValueAs(BigDecimal.class);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/type/SameOperandTypeChecker.java`
#### Snippet
```java
    if (throwOnFailure && callBinding == null) {
      throw new IllegalArgumentException(
          "callBinding must be non-null in case throwOnFailure=true");
    }
    int nOperandsActual = nOperands;
    if (nOperandsActual == -1) {
      nOperandsActual = operatorBinding.getOperandCount();
    }
    RelDataType[] types = new RelDataType[nOperandsActual];
    final List<Integer> operandList =
        getOperandList(operatorBinding.getOperandCount());
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
    switch (typeName) {
    case VARCHAR:
    case VARBINARY:

      // TODO angel 8-June-2005: Multiset should be LOB
    case MULTISET:
      return true;
    default:
      return false;
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      if (!t1.isStruct() || !t2.isStruct()) {
        return false;
      }
      if (t1.getFieldCount() != t2.getFieldCount()) {
        return false;
      }
      List<RelDataTypeField> fields1 = t1.getFieldList();
      List<RelDataTypeField> fields2 = t2.getFieldList();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/validate/AliasNamespace.java`
#### Snippet
```java
    int i = 0;
    for (RelDataTypeField field : getRowType().getFieldList()) {
      if (field.getName().equals(name)) {
        return underlyingRowType.getFieldList().get(i).getName();
      }
      ++i;
    }
    throw new AssertionError("unknown field '" + name
        + "' in rowtype " + underlyingRowType);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql/validate/SqlConformanceEnum.java`
#### Snippet
```java
    switch (this) {
    case BABEL:
    case BIG_QUERY:
    case LENIENT:
    case MYSQL_5:
    case SQL_SERVER_2008:
      return true;
    default:
      return false;
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql2rel/ReflectiveConvertletTable.java`
#### Snippet
```java
    if (!Modifier.isPublic(method.getModifiers())) {
      return;
    }
    if (!method.getName().startsWith("convert")) {
      return;
    }
    if (!RexNode.class.isAssignableFrom(method.getReturnType())) {
      return;
    }
    final Class[] parameterTypes = method.getParameterTypes();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    ImmutableBitSet inputFieldsUsed = inputFinder.build();

    // Create input with trimmed columns.
    TrimResult trimResult =
        trimChild(calc, input, inputFieldsUsed, inputExtraFields);
    RelNode newInput = trimResult.left;
    final Mapping inputMapping = trimResult.right;

    // If the input is unchanged, and we need to project all columns,
    // there's nothing we can do.
    if (newInput == input
        && fieldsUsed.cardinality() == fieldCount) {
      return result(calc, Mappings.createIdentity(fieldCount));
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    TrimResult trimResult =
        trimChild(filter, input, inputFieldsUsed, inputExtraFields);
    RelNode newInput = trimResult.left;
    final Mapping inputMapping = trimResult.right;

    // If the input is unchanged, and we need to project all columns,
    // there's nothing we can do.
    if (newInput == input
        && fieldsUsed.cardinality() == fieldCount) {
      return result(filter, Mappings.createIdentity(fieldCount));
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    final Set<RelDataTypeField> inputExtraFields = Collections.emptySet();
    TrimResult trimResult =
        trimChild(sort, input, inputFieldsUsed.build(), inputExtraFields);
    RelNode newInput = trimResult.left;
    final Mapping inputMapping = trimResult.right;

    // If the input is unchanged, and we need to project all columns,
    // there's nothing we can do.
    if (newInput == input
        && inputMapping.isIdentity()
        && fieldsUsed.cardinality() == fieldCount) {
      return result(sort, Mappings.createIdentity(fieldCount));
    }

    relBuilder.push(newInput);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    TrimResult trimResult =
        trimChild(sortExchange, input, inputFieldsUsed.build(), inputExtraFields);
    RelNode newInput = trimResult.left;
    final Mapping inputMapping = trimResult.right;

    // If the input is unchanged, and we need to project all columns,
    // there's nothing we can do.
    if (newInput == input
        && inputMapping.isIdentity()
        && fieldsUsed.cardinality() == fieldCount) {
      return result(sortExchange, Mappings.createIdentity(fieldCount));
    }

    relBuilder.push(newInput);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
    final RexBuilder rexBuilder = cx.getRexBuilder();
    final List<RexNode> operands =
        convertOperands(cx, call, SqlOperandTypeChecker.Consistency.NONE);
    final RelDataType type =
        cx.getValidator().getValidatedNodeType(call);
    final List<RexNode> exprs = new ArrayList<>();
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
    final RelDataType originalType =
        cx.getValidator().getValidatedNodeType(call);
    RexRangeRef rr = cx.getSubQueryExpr(call);
    assert rr != null;
    RelDataType msType = rr.getType().getFieldList().get(0).getType();
    RexNode expr =
        cx.getRexBuilder().makeInputRef(
            msType,
            rr.getOffset());
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
      switch (unit) {
      case MICROSECOND:
      case NANOSECOND:
        interval2Add =
            divide(rexBuilder,
                multiply(rexBuilder,
                    rexBuilder.makeIntervalLiteral(BigDecimal.ONE, qualifier), op1),
                BigDecimal.ONE.divide(unit.multiplier,
                    RoundingMode.UNNECESSARY));
        break;
      default:
        interval2Add =
            multiply(rexBuilder,
                rexBuilder.makeIntervalLiteral(unit.multiplier, qualifier), op1);
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/EquivalenceSet.java`
#### Snippet
```java
    final E eParent = add(e);
    if (!eParent.equals(e)) {
      assert Objects.equals(parents.get(eParent), eParent);
      final E root = equiv(eParent, f);
      parents.put(e, root);
      return root;
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/ImmutableBitSet.java`
#### Snippet
```java
    int max = -1;
    for (int bit : bits) {
      if (bit < 0) {
        throw new IndexOutOfBoundsException("bit < 0: " + bit);
      }
      max = Math.max(bit, max);
    }
    if (max == -1) {
      return EMPTY;
    }
    long[] words = new long[wordIndex(max) + 1];
    for (int bit : bits) {
      int wordIndex = wordIndex(bit);
      words[wordIndex] |= 1L << bit;
    }
    return new ImmutableBitSet(words);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/ImmutableBitSet.java`
#### Snippet
```java
    long firstWordMask = WORD_MASK << fromIndex;
    long lastWordMask  = WORD_MASK >>> -toIndex;
    if (startWordIndex == endWordIndex) {
      // One word
      words[startWordIndex] |= firstWordMask & lastWordMask;
    } else {
      // First word, middle words, last word
      words[startWordIndex] |= firstWordMask;
      for (int i = startWordIndex + 1; i < endWordIndex; i++) {
        words[i] = WORD_MASK;
      }
      words[endWordIndex] |= lastWordMask;
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/ImmutableNullableMap.java`
#### Snippet
```java
      final SortedMap<K, V> sortedMap = (SortedMap) map;
      try {
        Comparator<? super K> comparator = sortedMap.comparator();
        if (comparator == null) {
          return ImmutableSortedMap.copyOf(sortedMap);
        } else {
          return ImmutableSortedMap.copyOf(sortedMap, comparator);
        }
      } catch (NullPointerException e) {
        // Make an effectively immutable map by creating a mutable copy
        // and wrapping it to prevent modification. Unfortunately, if we see
        // it again we will not recognize that it is immutable and we will make
        // another copy.
        return Collections.unmodifiableNavigableMap(new TreeMap<>(sortedMap));
      }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/PartiallyOrderedSet.java`
#### Snippet
```java
        if (lt12 && !lt21) {
          if (!get(nodeAncestors, node1, "nodeAncestors").contains(node2.e)) {
            assert !fail
                : node1.e + " is less than " + node2.e + " but "
                + node2.e + " is not in the ancestor set of "
                + node1.e;
            return false;
          }
          if (!get(nodeDescendants, node2, "nodeDescendants").contains(node1.e)) {
            assert !fail
                : node1.e + " is less than " + node2.e + " but "
                + node1.e + " is not in the descendant set of "
                + node2.e;
            return false;
          }
        }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/TimeString.java`
#### Snippet
```java
    String v = this.v;
    int i = v.indexOf('.');
    if (i >= 0) {
      v = v.substring(0, i);
    }
    while (fraction.endsWith("0")) {
      fraction = fraction.substring(0, fraction.length() - 1);
    }
    if (fraction.length() > 0) {
      v = v + "." + fraction;
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/TimeString.java`
#### Snippet
```java
    final int p = precision();
    if (precision < p) {
      return round(precision).toString(precision);
    }
    if (precision > p) {
      String s = v;
      if (p == 0) {
        s += ".";
      }
      return s + Strings.repeat("0", precision - p);
    }
    return v;
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/TimeWithTimeZoneString.java`
#### Snippet
```java
    if (this.timeZone.equals(timeZone)) {
      return this;
    }
    String localTimeString = localTime.toString();
    String v;
    String fraction;
    int i = localTimeString.indexOf('.');
    if (i >= 0) {
      v = localTimeString.substring(0, i);
      fraction = localTimeString.substring(i + 1);
    } else {
      v = localTimeString;
      fraction = null;
    }
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
    int startMode = groupAsInt(matcher, ++j);
    int startMonth = groupAsInt(matcher, ++j);
    int startDay = groupAsInt(matcher, ++j);
    int startDayOfWeek = groupAsInt(matcher, ++j);
    int startTime = groupAsInt(matcher, ++j);
    int startTimeMode = groupAsInt(matcher, ++j);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
    if (inTag) {
      // complete the parent's start tag
      if (compact) {
        out.print(">");
      } else {
        out.println(">");
      }
      inTag = false;
    }
    displayIndent(out, indent);
    out.print("<");
    out.print(tagName);
```

### DuplicatedCode
Duplicated code
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
    final int sourceCount = targets.size();
    final PartialFunctionImpl mapping =
        new PartialFunctionImpl(sourceCount, targetCount, MappingType.FUNCTION);
    for (int source = 0; source < sourceCount; source++) {
      int target = targets.get(source);
      mapping.set(source, target);
    }
    return mapping;
```

### DuplicatedCode
Duplicated code
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    if (literalValue == null) {
      // cannot translate literal; better bail out
      return null;
    }
    final boolean isNumeric = refNode.getType().getFamily() == SqlTypeFamily.NUMERIC
        || rexLiteral.getType().getFamily() == SqlTypeFamily.NUMERIC;
    final Pair<String, ExtractionFunction> druidColumn =
        DruidQuery.toDruidColumn(refNode, rowType, druidQuery);
    final String columnName = druidColumn.left;
    final ExtractionFunction extractionFunction = druidColumn.right;
    if (columnName == null) {
      // no column name better bail out.
      return null;
    }
```

### DuplicatedCode
Duplicated code
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    final boolean isNumeric = lhs.getType().getFamily() == SqlTypeFamily.NUMERIC
        || rhs.getType().getFamily() == SqlTypeFamily.NUMERIC;
    final Pair<String, ExtractionFunction> druidColumn = DruidQuery
        .toDruidColumn(refNode, rowType, query);
    final String columnName = druidColumn.left;
    final ExtractionFunction extractionFunction = druidColumn.right;

    if (columnName == null) {
      return null;
    }
```

### DuplicatedCode
Duplicated code
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    Filter filterRel = null;
    if (i < rels.size() && rels.get(i) instanceof Filter) {
      filterRel = (Filter) rels.get(i++);
    }

    Project project = null;
    if (i < rels.size() && rels.get(i) instanceof Project) {
      project = (Project) rels.get(i++);
    }
```

### DuplicatedCode
Duplicated code
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java
      if (aggregate.getGroupSets().size() != 1) {
        return;
      }
      if (DruidQuery
          .computeProjectGroupSet(project, aggregate.getGroupSet(), query.table.getRowType(), query)
          == null) {
        return;
      }
      final List<String> aggNames = Util
          .skip(aggregate.getRowType().getFieldNames(), aggregate.getGroupSet().cardinality());
      if (DruidQuery.computeDruidJsonAgg(aggregate.getAggCallList(), aggNames, project, query)
          == null) {
        return;
      }
```

### DuplicatedCode
Duplicated code
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchEnumerators.java`
#### Snippet
```java
    if (o == null) {
      return null;
    }
    Primitive primitive = Primitive.of(clazz);
    if (primitive != null) {
      clazz = primitive.boxClass;
    } else {
      primitive = Primitive.ofBox(clazz);
    }
    if (clazz.isInstance(o)) {
      return o;
    }
```

### DuplicatedCode
Duplicated code
in `geode/src/main/java/org/apache/calcite/adapter/geode/util/GeodeUtils.java`
#### Snippet
```java
    if (clazz.isInstance(o)) {
      return o;
    }
    if (o instanceof Date && primitive != null) {
      o = ((Date) o).getTime() / DateTimeUtils.MILLIS_PER_DAY;
    }
    if (o instanceof Number && primitive != null) {
      return primitive.number((Number) o);
    }
    return o;
```

### DuplicatedCode
Duplicated code
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchEnumerators.java`
#### Snippet
```java
    if (o instanceof Date && primitive != null) {
      o = ((Date) o).getTime() / DateTimeUtils.MILLIS_PER_DAY;
    }
    if (o instanceof Number && primitive != null) {
      return primitive.number((Number) o);
    }
    return o;
```

### DuplicatedCode
Duplicated code
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
      this.id = Objects.requireNonNull(id, "id");

      // both can't be null
      if (source == null && fields == null) {
        final String message =
            String.format(Locale.ROOT,
                "Both '_source' and 'fields' are missing for %s", id);
        throw new IllegalArgumentException(message);
      }

      // both can't be non-null
      if (source != null && fields != null) {
        final String message =
            String.format(Locale.ROOT,
                "Both '_source' and 'fields' are populated (non-null) for %s", id);
        throw new IllegalArgumentException(message);
      }

      this.source = source;
      this.fields = fields;
```

### DuplicatedCode
Duplicated code
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
        Object field = fields.get(name);
        if (field instanceof Iterable) {
          // return first element (or null)
          Iterator<?> iter = ((Iterable<?>) field).iterator();
          return iter.hasNext() ? iter.next() : null;
        }

        return field;
```

### DuplicatedCode
Duplicated code
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
    if (call.getOperator() != SqlStdOperatorTable.ITEM) {
      return null;
    }
    final RexNode op0 = call.getOperands().get(0);
    final RexNode op1 = call.getOperands().get(1);

    if (op0 instanceof RexInputRef
        && ((RexInputRef) op0).getIndex() == 0
        && op1 instanceof RexLiteral
        && ((RexLiteral) op1).getValue2() instanceof String) {
      return (String) ((RexLiteral) op1).getValue2();
    }
    return null;
```

### DuplicatedCode
Duplicated code
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
    return SqlValidatorUtil.uniquify(
        new AbstractList<String>() {
          @Override public String get(int index) {
            final String name = rowType.getFieldList().get(index).getName();
            return name.startsWith("$") ? "_" + name.substring(2) : name;
          }

          @Override public int size() {
            return rowType.getFieldCount();
          }
        },
        SqlValidatorUtil.EXPR_SUGGESTER, true);
```

### DuplicatedCode
Duplicated code
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
      generator.writeStartObject();
      generator.writeFieldName(fieldName);
      generator.writeStartArray();
      for (Object value: values) {
        writeObject(generator, value);
      }
      generator.writeEndArray();
      generator.writeEndObject();
      generator.writeEndObject();
```

### DuplicatedCode
Duplicated code
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvProjectTableScanRule.java`
#### Snippet
```java
    final LogicalProject project = call.rel(0);
    final CsvTableScan scan = call.rel(1);
    int[] fields = getProjectFields(project.getProjects());
    if (fields == null) {
      // Project contains expressions more complex than just field references.
      return;
    }
    call.transformTo(
        new CsvTableScan(
            scan.getCluster(),
            scan.getTable(),
            scan.csvTable,
            fields));
```

### DuplicatedCode
Duplicated code
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvProjectTableScanRule.java`
#### Snippet
```java
    final int[] fields = new int[exps.size()];
    for (int i = 0; i < exps.size(); i++) {
      final RexNode exp = exps.get(i);
      if (exp instanceof RexInputRef) {
        fields[i] = ((RexInputRef) exp).getIndex();
      } else {
        return null; // not a simple projection
      }
    }
    return fields;
```

### DuplicatedCode
Duplicated code
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvSchema.java`
#### Snippet
```java
    final Source baseSource = Sources.of(directoryFile);
    File[] files = directoryFile.listFiles((dir, name) -> {
      final String nameSansGz = trim(name, ".gz");
      return nameSansGz.endsWith(".csv")
          || nameSansGz.endsWith(".json");
    });
    if (files == null) {
      System.out.println("directory " + directoryFile + " not found");
      files = new File[0];
    }
```

### DuplicatedCode
Duplicated code
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvStreamTableFactory.java`
#### Snippet
```java
    String fileName = (String) operand.get("file");
    File file = new File(fileName);
    final File base =
        (File) operand.get(ModelHandler.ExtraOperand.BASE_DIRECTORY.camelName);
    if (base != null && !file.isAbsolute()) {
      file = new File(base, fileName);
    }
```

### DuplicatedCode
Duplicated code
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTableFactory.java`
#### Snippet
```java
    String fileName = (String) operand.get("file");
    final File base =
        (File) operand.get(ModelHandler.ExtraOperand.BASE_DIRECTORY.camelName);
    final Source source = Sources.file(base, fileName);
    final RelProtoDataType protoRowType =
        rowType != null ? RelDataTypeImpl.proto(rowType) : null;
```

### DuplicatedCode
Duplicated code
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTranslatableTable.java`
#### Snippet
```java
    final AtomicBoolean cancelFlag = DataContext.Variable.CANCEL_FLAG.get(root);
    return new AbstractEnumerable<Object>() {
      @Override public Enumerator<Object> enumerator() {
        JavaTypeFactory typeFactory = root.getTypeFactory();
        return new CsvEnumerator<>(
            source,
            cancelFlag,
            getFieldTypes(typeFactory),
            ImmutableIntList.of(fields));
      }
    };
```

### DuplicatedCode
Duplicated code
in `file/src/main/java/org/apache/calcite/adapter/file/JsonEnumerator.java`
#### Snippet
```java
    final List<RelDataType> types = new ArrayList<RelDataType>(jsonFieldMap.size());
    final List<String> names = new ArrayList<String>(jsonFieldMap.size());

    for (Object key : jsonFieldMap.keySet()) {
      final RelDataType type = typeFactory.createJavaType(jsonFieldMap.get(key).getClass());
      names.add(key.toString());
      types.add(type);
    }
```

### DuplicatedCode
Duplicated code
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeProject.java`
#### Snippet
```java
    final Map<String, String> fields = new LinkedHashMap<>();
    for (Pair<RexNode, String> pair : getNamedProjects()) {
      final String name = pair.right;
      final String originalName = pair.left.accept(translator);
      fields.put(originalName, name);
    }
    geodeImplementContext.addSelectFields(fields);
```

### DuplicatedCode
Duplicated code
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSchemaFactory.java`
#### Snippet
```java
    Map map = (Map) operand;
    String locatorHost = (String) map.get(LOCATOR_HOST);
    int locatorPort = Integer.valueOf((String) map.get(LOCATOR_PORT));
    String[] regionNames = ((String) map.get(REGIONS)).split(COMMA_DELIMITER);
    String pbxSerializablePackagePath = (String) map.get(PDX_SERIALIZABLE_PACKAGE_PATH);
```

### DuplicatedCode
Duplicated code
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BinaryExpression.java`
#### Snippet
```java
      switch (primitive) {
      case INT:
        return evaluateInt(expression0, evaluator) > evaluateInt(expression1, evaluator);
      case SHORT:
        return evaluateShort(expression0, evaluator) > evaluateShort(expression1, evaluator);
      case BYTE:
        return evaluateByte(expression0, evaluator) > evaluateByte(expression1, evaluator);
      case FLOAT:
        return evaluateFloat(expression0, evaluator) > evaluateFloat(expression1, evaluator);
      case DOUBLE:
        return evaluateDouble(expression0, evaluator) > evaluateDouble(expression1, evaluator);
      case LONG:
        return evaluateLong(expression0, evaluator) > evaluateLong(expression1, evaluator);
      default:
        throw cannotEvaluate();
      }
```

### DuplicatedCode
Duplicated code
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BinaryExpression.java`
#### Snippet
```java
      switch (primitive) {
      case INT:
        return evaluateInt(expression0, evaluator) >= evaluateInt(expression1, evaluator);
      case SHORT:
        return evaluateShort(expression0, evaluator) >= evaluateShort(expression1, evaluator);
      case BYTE:
        return evaluateByte(expression0, evaluator) >= evaluateByte(expression1, evaluator);
      case FLOAT:
        return evaluateFloat(expression0, evaluator) >= evaluateFloat(expression1, evaluator);
      case DOUBLE:
        return evaluateDouble(expression0, evaluator) >= evaluateDouble(expression1, evaluator);
      case LONG:
        return evaluateLong(expression0, evaluator) >= evaluateLong(expression1, evaluator);
      default:
        throw cannotEvaluate();
      }
```

### DuplicatedCode
Duplicated code
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstantExpression.java`
#### Snippet
```java
    buf.append('"');
    int n = s.length();
    char lastChar = 0;
    for (int i = 0; i < n; ++i) {
      char c = s.charAt(i);
      switch (c) {
      case '\\':
        buf.append("\\\\");
        break;
      case '"':
        buf.append("\\\"");
        break;
      case '\n':
        buf.append("\\n");
        break;
      case '\r':
        if (lastChar != '\n') {
          buf.append("\\r");
        }
        break;
      default:
        buf.append(c);
        break;
      }
      lastChar = c;
    }
```

### DuplicatedCode
Duplicated code
in `plus/src/main/java/org/apache/calcite/adapter/os/Processes.java`
#### Snippet
```java
      final Process process = processSupplier.get();
      final InputStream is = process.getInputStream();
      final BufferedInputStream bis =
          new BufferedInputStream(is);
      final InputStreamReader isr =
          new InputStreamReader(bis, StandardCharsets.UTF_8);
      final BufferedReader br = new BufferedReader(isr);
```

### DuplicatedCode
Duplicated code
in `plus/src/main/java/org/apache/calcite/adapter/os/PsTableFunction.java`
#### Snippet
```java
        JavaTypeFactory typeFactory = root.getTypeFactory();
        final RelDataType rowType = getRowType(typeFactory);
        final List<String> fieldNames =
            ImmutableList.copyOf(rowType.getFieldNames());
        final String[] args;
        final String osName = System.getProperty("os.name");
        final String osVersion = System.getProperty("os.version");
        Util.discard(osVersion);
```

### DuplicatedCode
Duplicated code
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisDataProcess.java`
#### Snippet
```java
    List<Object[]> objs = new ArrayList<>();
    for (String key : keys) {
      if (dataType == RedisDataType.STRING) {
        key = jedis.get(key);
      }
      switch (redisDataFormat) {
      case RAW:
        objs.add(new Object[]{key});
        break;
      case JSON:
        objs.add(parseJson(key));
        break;
      case CSV:
        objs.add(parseCsv(key));
        break;
      default:
        break;
      }
    }
    return objs;
```

### DuplicatedCode
Duplicated code
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final String name;
    final List<String> path;
    if (id.isSimple()) {
      path = context.getDefaultSchemaPath();
      name = id.getSimple();
    } else {
      path = Util.skipLast(id.names);
      name = Util.last(id.names);
    }
    CalciteSchema schema = mutable ? context.getMutableRootSchema()
        : context.getRootSchema();
```

### DuplicatedCode
Duplicated code
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    if (columnList == null) {
      return query;
    }
    final SqlParserPos p = query.getParserPosition();
    final SqlNodeList selectList = SqlNodeList.SINGLETON_STAR;
    final SqlCall from =
        SqlStdOperatorTable.AS.createCall(p,
            ImmutableList.<SqlNode>builder()
                .add(query)
                .add(new SqlIdentifier("_", p))
                .addAll(columnList)
                .build());
    return new SqlSelect(p, null, selectList, from, null, null, null, null,
        null, null, null, null, null);
```

### DuplicatedCode
Duplicated code
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
      query.unparse(w, 0, 0);
      final String sql = buf.toString();
      final SqlNode query1 = planner.parse(sql);
      final SqlNode query2 = planner.validate(query1);
      final RelRoot r = planner.rel(query2);
      final PreparedStatement prepare =
          context.getRelRunner().prepareStatement(r.rel);
      int rowCount = prepare.executeUpdate();
      Util.discard(rowCount);
      prepare.close();
```

### DuplicatedCode
Duplicated code
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final Pair<CalciteSchema, String> pair =
        schema(context, true, create.name);
    final SchemaPlus schemaPlus = pair.left.plus();
    for (Function function : schemaPlus.getFunctions(pair.right)) {
      if (function.getParameters().isEmpty()) {
        if (!create.getReplace()) {
          throw SqlUtil.newContextException(create.name.getParserPosition(),
              RESOURCE.viewExists(pair.right));
        }
        pair.left.removeFunction(pair.right);
      }
    }
    final SqlNode q = renameColumns(create.columnList, create.query);
    final String sql = q.toSqlString(CalciteSqlDialect.DEFAULT).getSql();
    final ViewTableMacro viewTableMacro =
        ViewTable.viewMacro(schemaPlus, sql, pair.left.path(null),
            context.getObjectPath(), false);
    final TranslatableTable x = viewTableMacro.apply(ImmutableList.of());
    Util.discard(x);
    schemaPlus.add(pair.right, viewTableMacro);
```

### DuplicatedCode
Duplicated code
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/util/StringUtils.java`
#### Snippet
```java
          if (!hasEscapedDelim) {
            list.add(encoded.subSequence(start, end).toString());
          } else {
            StringBuilder sb = new StringBuilder(end - start);
            char a = '\0';
            char b = '\0';
            for (int j = start; j < end; ++j) {
              b = a;
              a = encoded.charAt(j);
              if (b == '\\' && a != delim) {
                sb.append(b);
              }
              if (a != '\\') {
                sb.append(a);
              }
            }
            list.add(sb.toString());
          }
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/sql/test/AbstractSqlTester.java`
#### Snippet
```java
      final RelDataType rowType =
          validator.getValidatedNodeType(validatedNode);
      final List<RelDataTypeField> fields = rowType.getFieldList();
      assertEquals(1, fields.size(), "expected query to return 1 field");
      final RelDataType actualType = fields.get(0).getType();
      String actual = SqlTests.getTypeString(actualType);
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlTests.java`
#### Snippet
```java
      for (int i = 0; i < inputValues.length; i++) {
        if (i > 0) {
          buf.append(" UNION ALL ");
        }
        buf.append("SELECT ");
        String inputValue = inputValues[i];
        buf.append(inputValue).append(" AS x FROM (VALUES (1))");
      }
      buf.append(")");
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      if (connection.isWrapperFor(CalciteConnection.class)) {
        final CalciteConnection calciteConnection =
            connection.unwrap(CalciteConnection.class);
        final Properties properties = calciteConnection.getProperties();
        properties.setProperty(
            CalciteConnectionProperty.MATERIALIZATIONS_ENABLED.camelName(),
            Boolean.toString(materializationsEnabled));
        properties.setProperty(
            CalciteConnectionProperty.CREATE_MATERIALIZATIONS.camelName(),
            Boolean.toString(materializationsEnabled));
        if (!properties
            .containsKey(CalciteConnectionProperty.TIME_ZONE.camelName())) {
          // Do not override id some test has already set this property.
          properties.setProperty(
              CalciteConnectionProperty.TIME_ZONE.camelName(),
              DateTimeUtils.UTC_ZONE.getID());
        }
      }
      for (Pair<Hook, Consumer> hook : hooks) {
        //noinspection unchecked
        closer.add(hook.left.addThread(hook.right));
      }
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      if (resultChecker != null) {
        resultChecker.accept(resultSet);
      }
      if (updateChecker != null) {
        updateChecker.accept(updateCount);
      }
      if (resultSet != null) {
        resultSet.close();
      }
      statement.close();
      connection.close();
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderExtended.java`
#### Snippet
```java
    empmTable.addColumn("EMPNO", f.intType, true);
    empmTable.addColumn("ENAME", f.varchar20Type);
    empmTable.addColumn("JOB", f.varchar10Type);
    empmTable.addColumn("MGR", f.intTypeNull);
    empmTable.addColumn("HIREDATE", f.timestampType);
    empmTable.addColumn("SAL", f.intType);
    empmTable.addColumn("COMM", f.intType);
    empmTable.addColumn("DEPTNO", f.intType);
    empmTable.addColumn("SLACKER", f.booleanType);
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
    emp20View.addColumn("ENAME", fixture.varchar20Type);
    emp20View.addColumn("JOB", fixture.varchar10Type);
    emp20View.addColumn("MGR", fixture.intTypeNull);
    emp20View.addColumn("HIREDATE", fixture.timestampType);
    emp20View.addColumn("SAL", fixture.intType);
    emp20View.addColumn("COMM", fixture.intType);
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
    empNullablesTable.addColumn("ENAME", fixture.varchar20TypeNull);
    empNullablesTable.addColumn("JOB", fixture.varchar10TypeNull);
    empNullablesTable.addColumn("MGR", fixture.intTypeNull);
    empNullablesTable.addColumn("HIREDATE", fixture.timestampTypeNull);
    empNullablesTable.addColumn("SAL", fixture.intTypeNull);
    empNullablesTable.addColumn("COMM", fixture.intTypeNull);
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
    empDefaultsTable.addColumn("JOB", fixture.varchar10TypeNull);
    empDefaultsTable.addColumn("MGR", fixture.intTypeNull);
    empDefaultsTable.addColumn("HIREDATE", fixture.timestampTypeNull);
    empDefaultsTable.addColumn("SAL", fixture.intTypeNull);
    empDefaultsTable.addColumn("COMM", fixture.intTypeNull);
    empDefaultsTable.addColumn("DEPTNO", fixture.intTypeNull);
    empDefaultsTable.addColumn("SLACKER", fixture.booleanTypeNull);
    registerTable(empDefaultsTable);
```

### DuplicatedCode
Duplicated code
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
          @Override public RexNode getConstraint(RexBuilder rexBuilder,
              RelDataType tableRowType) {
            final RelDataTypeField deptnoField =
                tableRowType.getFieldList().get(7);
            final RelDataTypeField salField =
                tableRowType.getFieldList().get(5);
            final List<RexNode> nodes =
                Arrays.asList(
                    rexBuilder.makeCall(SqlStdOperatorTable.EQUALS,
                        rexBuilder.makeInputRef(deptnoField.getType(),
                            deptnoField.getIndex()),
                        rexBuilder.makeExactLiteral(BigDecimal.valueOf(20L),
                            deptnoField.getType())),
                    rexBuilder.makeCall(SqlStdOperatorTable.GREATER_THAN,
                        rexBuilder.makeInputRef(salField.getType(),
                            salField.getIndex()),
                        rexBuilder.makeExactLiteral(BigDecimal.valueOf(1000L),
                            salField.getType())));
            return RexUtil.composeConjunction(rexBuilder, nodes);
          }
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
        return new FrameImpl(frameType, keyword, open, close, left, 0,
            chopColumn, lineFolding, false, true, 0, false, false, false) {
          protected void _before() {
            nextWhitespace = NL;
          }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
            left, 0, chopColumn, lineFolding, false, true, indentation,
            false, false, false) {
          protected void _before() {
            newlineAndIndent();
          }
```

## RuleId[id=DanglingJavadoc]
### DanglingJavadoc
Dangling Javadoc comment
in `core/src/main/java/org/apache/calcite/rel/metadata/MetadataHandlerProvider.java`
#### Snippet
```java
  <MH extends MetadataHandler<?>> MH handler(Class<MH> handlerClass);

  /** Re-generates the handler for a given kind of metadata.  */
  /**
   * Revise the handler for a given kind of metadata.
```

### DanglingJavadoc
Dangling Javadoc comment
in `core/src/main/java/org/apache/calcite/rex/RexTransformer.java`
#### Snippet
```java
    transformableOperators.add(SqlStdOperatorTable.AND);

    /** NOTE the OR operator is NOT missing.
     * see {@link org.apache.calcite.test.RexTransformerTest} */
    transformableOperators.add(SqlStdOperatorTable.EQUALS);
```

### DanglingJavadoc
Dangling Javadoc comment
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
   * tree. */
  public static int countJoins(RelNode rootRel) {
    /** Visitor that counts join nodes. */
    class JoinCounter extends RelVisitor {
      int joinCount;
```

### DanglingJavadoc
Dangling Javadoc comment
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    return new AbstractEnumerable<TResult>() {
      @Override public Enumerator<TResult> enumerator() {
        /**
         * the innerToLookUp will refer the inner , if current join
         * is a right join, we should figure out the right list first, if
```

### DanglingJavadoc
Dangling Javadoc comment
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  /** Allow tests to tweak the timestamp. */
  public void setRelMetadataTimestamp(long metadataTimestamp) {
    this.metadataTimestamp = metadataTimestamp;
```

## RuleId[id=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `pulledUpPredicates` declared in one 'switch' branch and used in another
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPredicates.java`
#### Snippet
```java
      switch (joinType) {
      case SEMI:
        Iterable<RexNode> pulledUpPredicates;
        pulledUpPredicates =
            Iterables.concat(RelOptUtil.conjunctions(leftChildPredicates),
```

### SwitchStatementWithConfusingDeclaration
Local variable `call` declared in one 'switch' branch and used in another
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    switch (groupByItem.getKind()) {
    case GROUP_BY_DISTINCT:
      SqlCall call = (SqlCall) groupByItem;
      for (SqlNode operand : call.getOperandList()) {
        validateGroupByExpr(operand, groupByScope);
```

### SwitchStatementWithConfusingDeclaration
Local variable `operands2` declared in one 'switch' branch and used in another
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
        // as if it were "LISTAGG(s, sep) WITHIN GROUP (ORDER BY x, y)";
        // and "STRING_AGG(s, sep)" as "LISTAGG(s, sep)".
        final List<SqlNode> operands2;
        if (!operands.isEmpty()
            && Util.last(operands) instanceof SqlNodeList) {
```

## RuleId[id=UnnecessaryInitCause]
### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
      String message = "Internal error: \u0000" + s;
      AssertionError ae = new AssertionError(message);
      ae.initCause(e);
      return ae;
    }
```

## RuleId[id=TrivialIf]
### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/core/Values.java`
#### Snippet
```java
        // overflow, rounding, and padding/truncation must already have
        // been dealt with.
        if (!RexLiteral.isNullLiteral(literal)) {
          assert SqlTypeUtil.canAssignFrom(fieldType, literal.getType())
              : "to " + fieldType + " from " + literal;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/type/TimeFrames.java`
#### Snippet
```java
        final TimeFrameImpl isoWeek =
            requireNonNull(frameSet.map.get("ISOWEEK"));
        if (canDirectlyRollUp(this, isoWeek)
            && canDirectlyRollUp(isoYear, toFrame1)) {
          return true;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/rules/SortJoinTransposeRule.java`
#### Snippet
```java
        }
      }
      if (sort.offset != null
          && !RelMdUtil.areColumnsDefinitelyUnique(
              mq, join.getRight(), joinInfo.rightSet())) {
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/rules/SortJoinTransposeRule.java`
#### Snippet
```java
        }
      }
      if (sort.offset != null
          && !RelMdUtil.areColumnsDefinitelyUnique(
              mq, join.getLeft(), joinInfo.leftSet())) {
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/rules/ReduceDecimalsRule.java`
#### Snippet
```java
        return false;
      }
      if (innerCheck || outerCheck) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java
    // project cases
    boolean allFieldsReferenced = IntStream.range(0, nChildFields).allMatch(i -> projRefs.get(i));
    if (allFieldsReferenced
        && childPreserveExprs.size() == 0
        && rightPreserveExprs.size() == 0) {
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/rules/FilterJoinRule.java`
#### Snippet
```java
    // generating side.
    boolean filterPushed = false;
    if (RelOptUtil.classifyFilters(
        join,
        aboveFilters,
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/rules/UnionMergeRule.java`
#### Snippet
```java
    }

    if (topOp.all && !bottomOp.all) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
          continue;
        }
        if (!r.contains(c)) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
    // After removing them, we check whether all the remaining tables in the graph
    // are tables present in the query: if they are, we can try to rewrite
    if (!Collections.disjoint(graph.vertexSet(), extraTableRefs)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
        // The expression should already be valid. Check that its
        // index is within bounds.
        if ((index < 0) || (index >= inputRowType.getFieldCount())) {
          assert false
              : "RexInputRef index " + index + " out of range 0.."
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/sql/SqlCall.java`
#### Snippet
```java
      if (parm instanceof SqlIdentifier) {
        SqlIdentifier id = (SqlIdentifier) parm;
        if (id.isStar() && id.names.size() == 1) {
          return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
      if (node.isA(SqlKind.CAST)) {
        RexCall call = (RexCall) node;
        if (isLiteral(call.operands.get(0), false)) {
          // node is "CAST(literal as type)"
          return true;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
      if (node.isA(SqlKind.CAST)) {
        RexCall call = (RexCall) node;
        if (isNullLiteral(call.operands.get(0), false)) {
          // node is "CAST(NULL as type)"
          return true;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
      if (node.getKind() == SqlKind.CAST) {
        SqlCall call = (SqlCall) node;
        if (isNullLiteral(call.operand(0), false)) {
          // node is "CAST(NULL as type)"
          return true;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java

    // We can implicitly convert from character to date
    if (family1 == SqlTypeFamily.CHARACTER
        && canConvertStringInCompare(family2)
        || family2 == SqlTypeFamily.CHARACTER
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    Charset cs2 = t2.getCharset();
    if ((cs1 != null) && (cs2 != null)) {
      if (!cs1.equals(cs2)) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingScope.java`
#### Snippet
```java
        }
      }
      if (count > 1) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/plan/RelTraitDef.java`
#### Snippet
```java
  @SuppressWarnings("BetaApi")
  public final T canonize(T trait) {
    if (!(trait instanceof RelCompositeTrait)) {
      assert getTraitClass().isInstance(trait)
          : getClass().getName()
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/plan/volcano/IterativeRuleQueue.java`
#### Snippet
```java
  @Override public boolean clear() {
    boolean empty = true;
    if (!matchList.queue.isEmpty() || !matchList.preQueue.isEmpty()) {
      empty = false;
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      // Args of agg-call is distinct, we can build a new agg-call.
      isAllowBuild = true;
    } else if (aggregation.getDistinctOptionality() == Optionality.IGNORED) {
      // If attribute of agg-call's distinct could be ignore, we can build a new agg-call.
      isAllowBuild = true;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      return true;
    }
    if (joinType == JoinRelType.FULL
        && ((rightFilterRexNode == null || rightFilterRexNode.isAlwaysTrue())
        && (leftFilterRexNode == null || leftFilterRexNode.isAlwaysTrue()))) {
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
    final Set<String> targetDisjunctions =
        new HashSet<>(RexUtil.strings(RelOptUtil.conjunctions(target)));
    if (conditionDisjunctions.equals(targetDisjunctions)) {
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java

    // make sure final converted traitset subsumes what was required
    if (converted != null) {
      assert converted.getTraitSet().satisfies(toTraits);
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
    // Checking if tree is valid considerably slows down planning
    // Only doing it if logger level is debug or finer
    if (LOGGER.isDebugEnabled()) {
      assert isValid(Litmus.THROW);
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/util/DateTimeStringUtils.java`
#### Snippet
```java
    } else {
      String id = TimeZone.getTimeZone(timeZone).getID();
      if (!id.equals("GMT")) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/util/PartiallyOrderedSet.java`
#### Snippet
```java
        final boolean lt21 = ordering.lessThan(node2.e, node1.e);
        if (node1 == node2) {
          if (!(lt12 && lt21)) {
            assert !fail
                : "self should be less than self: " + node1;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/util/RangeSets.java`
#### Snippet
```java
      }
      c = r0.upperBoundType().compareTo(r1.upperBoundType());
      if (c != 0) {
        return c;
      }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableTableFunctionScan.java`
#### Snippet
```java

  private static boolean isImplementorDefined(RexCall call) {
    if (call.getOperator() instanceof SqlWindowTableFunction
        && RexImpTable.INSTANCE.get((SqlWindowTableFunction) call.getOperator()) != null) {
      return true;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableMergeUnionRule.java`
#### Snippet
```java

    final Union union = call.rel(1);
    if (union.getInputs().size() < 2) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableRelImplementor.java`
#### Snippet
```java
      EnumerableRel child,
      EnumerableRel.Prefer prefer) {
    if (parent != null) {
      assert child == parent.getInputs().get(ordinal);
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableTraitsUtils.java`
#### Snippet
```java
          RexCallBinding.create(typeFactory, cast,
              ImmutableList.of(RelCollations.of(newFieldCollation)));
      if (cast.getOperator().getMonotonicity(binding)
          == SqlMonotonicity.NOT_MONOTONIC) {
        return false;
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
      // (boxed or not) when hint was provided.
      // It is favourable to get the type matching desired type
      if (desiredType == null && !isNullable(rex)) {
        assert !Primitive.isBox(translate.getType())
            : "Not-null boxed primitive should come back as primitive: "
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
      }
      assert deque.size() == priorityQueue.size();
      if (count > size) {
        assert deque.size() == size;
      }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
      return false;
    }
    if (obj instanceof Map) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
        + " + " + extraFields.size()
        + " != " + newFieldCount;
    if (Bug.TODO_FIXED) {
      assert newFieldCount > 0 : "rel has no fields after trim: " + rel;
    }
```

### TrivialIf
`if` statement can be simplified
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRules.java`
#### Snippet
```java

        return (leftName != null) && (rightName != null);
      } else if (left.isA(SqlKind.ITEM) && right.isA(SqlKind.LITERAL)) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java

        String name = getLeftNodeFieldName(left);
        if (name == null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
        if (call.getOperator() == SqlStdOperatorTable.CAST) {
          RexNode operand = call.getOperands().get(0);
          if (operand instanceof RexLiteral) {
            return true;
          }
```

### TrivialIf
`if` statement can be simplified
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkToEnumerableConverter.java`
#### Snippet
```java

    @Override SparkRel.Result visitInput(SparkRel parent, int ordinal, SparkRel input) {
      if (parent != null) {
        assert input == parent.getInputs().get(ordinal);
      }
```

### TrivialIf
`if` statement can be simplified
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
          || SqlTypeFamily.INTEGER.getTypeNames().contains(sqlTypeName)) {
        isNotAcceptedType = false;
      } else if (SqlTypeFamily.EXACT_NUMERIC.getTypeNames().contains(sqlTypeName)
          && (type.getScale() == 0
              || druidQuery.getConnectionConfig().approximateDecimal())) {
```

### TrivialIf
`if` statement can be simplified
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      return true;
    }
    if (toTypeName.getFamily().contains(input.getType())) {
      // same type it is okay to push it
      return true;
```

### TrivialIf
`if` statement can be simplified
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      // Decimal
      assert sqlTypeName == SqlTypeName.DECIMAL;
      if (type.getScale() == 0) {
        fractional = false;
      } else {
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/WhileStatement.java`
#### Snippet
```java
      return false;
    }
    if (!condition.equals(that.condition)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/LabelTarget.java`
#### Snippet
```java
    LabelTarget that = (LabelTarget) o;

    if (name != null ? !name.equals(that.name) : that.name != null) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TernaryExpression.java`
#### Snippet
```java
      return false;
    }
    if (!expression2.equals(that.expression2)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/AbstractNode.java`
#### Snippet
```java
      return false;
    }
    if (type != null ? !type.equals(that.type) : that.type != null) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodDeclaration.java`
#### Snippet
```java
      return false;
    }
    if (!resultType.equals(that.resultType)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConditionalExpression.java`
#### Snippet
```java
    ConditionalExpression that = (ConditionalExpression) o;

    if (!expressionList.equals(that.expressionList)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/DeclarationStatement.java`
#### Snippet
```java
      return false;
    }
    if (!parameter.equals(that.parameter)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockStatement.java`
#### Snippet
```java
    BlockStatement that = (BlockStatement) o;

    if (!statements.equals(that.statements)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ArrayLengthRecordField.java`
#### Snippet
```java
      return false;
    }
    if (!fieldName.equals(that.fieldName)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TryStatement.java`
#### Snippet
```java
      return false;
    }
    if (fynally != null ? !fynally.equals(that.fynally) : that.fynally
        != null) {
      return false;
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodCallExpression.java`
#### Snippet
```java
      return false;
    }
    if (targetExpression != null ? !targetExpression.equals(that
        .targetExpression) : that.targetExpression != null) {
      return false;
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ClassDeclaration.java`
#### Snippet
```java
      return false;
    }
    if (!name.equals(that.name)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TypeBinaryExpression.java`
#### Snippet
```java
      return false;
    }
    if (type != null ? !type.equals(that.type) : that.type != null) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/UnaryExpression.java`
#### Snippet
```java
    UnaryExpression that = (UnaryExpression) o;

    if (!expression.equals(that.expression)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/GotoStatement.java`
#### Snippet
```java
      return false;
    }
    if (labelTarget != null ? !labelTarget.equals(that.labelTarget) : that
        .labelTarget != null) {
      return false;
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MemberExpression.java`
#### Snippet
```java
      return false;
    }
    if (!field.equals(that.field)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FieldDeclaration.java`
#### Snippet
```java
      return false;
    }
    if (!parameter.equals(that.parameter)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BinaryExpression.java`
#### Snippet
```java
      return false;
    }
    if (primitive != that.primitive) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstantExpression.java`
#### Snippet
```java
    ConstantExpression that = (ConstantExpression) o;

    if (value != null ? !value.equals(that.value) : that.value != null) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java
      return false;
    }
    if (!parameterList.equals(that.parameterList)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/CatchBlock.java`
#### Snippet
```java
      return false;
    }
    if (parameter != null ? !parameter.equals(that.parameter) : that
        .parameter != null) {
      return false;
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ForStatement.java`
#### Snippet
```java
      return false;
    }
    if (post != null ? !post.equals(that.post) : that.post != null) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstructorDeclaration.java`
#### Snippet
```java
      return false;
    }
    if (!resultType.equals(that.resultType)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/NewArrayExpression.java`
#### Snippet
```java
      return false;
    }
    if (expressions != null ? !expressions.equals(that.expressions) : that
        .expressions != null) {
      return false;
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConditionalStatement.java`
#### Snippet
```java
    ConditionalStatement that = (ConditionalStatement) o;

    if (!expressionList.equals(that.expressionList)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ReflectedPseudoField.java`
#### Snippet
```java
    ReflectedPseudoField that = (ReflectedPseudoField) o;

    if (!field.equals(that.field)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/LabelStatement.java`
#### Snippet
```java
    LabelStatement that = (LabelStatement) o;

    if (defaultValue != null ? !defaultValue.equals(that.defaultValue) : that
        .defaultValue != null) {
      return false;
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ThrowStatement.java`
#### Snippet
```java
    ThrowStatement that = (ThrowStatement) o;

    if (expression != null ? !expression.equals(that.expression) : that
        .expression != null) {
      return false;
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/NewExpression.java`
#### Snippet
```java
      return false;
    }
    if (!type.equals(that.type)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/IndexExpression.java`
#### Snippet
```java
      return false;
    }
    if (!indexExpressions.equals(that.indexExpressions)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java

    // Application launched from IntelliJ 6.0
    if (lastStackTraceElement.getClassName().equals(
        "com.intellij.rt.execution.application.AppMain")
        && lastStackTraceElement.getMethodName().equals("main")) {
```

## RuleId[id=ListIndexOfReplaceableByContains]
### ListIndexOfReplaceableByContains
`Util.skip(columnMapping, x + 1).indexOf(field.getIndex()) < 0` can be replaced with '!Util.skip(columnMapping, x + 1).contains(field.getIndex())'
in `core/src/main/java/org/apache/calcite/prepare/CalcitePrepareImpl.java`
#### Snippet
```java
      final int x = columnMapping.indexOf(field.getIndex());
      if (x >= 0) {
        assert Util.skip(columnMapping, x + 1).indexOf(field.getIndex()) < 0
            : "column projected more than once; should have checked above";
        continue; // target column is projected
```

### ListIndexOfReplaceableByContains
`flattenCols.indexOf(i) >= 0` can be replaced with 'flattenCols.contains(i)'
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelBuilder.java`
#### Snippet
```java
    int flattenCount = 0;
    for (int i = 0; i < colCount; i++) {
      if (flattenCols.indexOf(i) >= 0) {
        // The original multiset columns to be flattened, select new flattened columns instead
        RelDataType componentType = inputFields.get(i).getType().getComponentType();
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'keyspace'
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraTable.java`
#### Snippet
```java
  List<String> clusteringKeys;
  List<RelFieldCollation> clusteringOrder;
  private final Optional<String> keyspace;
  private final String columnFamily;

```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `core/src/main/java/org/apache/calcite/sql/fun/SqlLibraryOperatorTableFactory.java`
#### Snippet
```java
    }
    SqlLibrary[] librarySet = libraryOperator.libraries();
    if (librarySet.length <= 0) {
      throw new AssertionError("Operator must belong to at least one library: "
          + operatorName);
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `core/src/main/java/org/apache/calcite/sql/dialect/MysqlSqlDialect.java`
#### Snippet
```java
    case WITHIN_GROUP:
      final List<SqlNode> operands = call.getOperandList();
      if (operands.size() <= 0 || operands.get(0).getKind() != SqlKind.LISTAGG) {
        super.unparseCall(writer, call, leftPrec, rightPrec);
        return;
```

## RuleId[id=AutoCloseableResource]
### AutoCloseableResource
'AvaticaStatement' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/jdbc/CalciteConnectionImpl.java`
#### Snippet
```java
  @Override public <T> Enumerator<T> executeQuery(Queryable<T> queryable) {
    try {
      CalciteStatement statement = (CalciteStatement) createStatement();
      CalcitePrepare.CalciteSignature<T> signature =
          statement.prepare(queryable);
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/util/IntegerIntervalSet.java`
#### Snippet
```java
  @Override public int size() {
    int n = 0;
    Enumerator<Integer> e = enumerator();
    while (e.moveNext()) {
      ++n;
```

### AutoCloseableResource
'Connection' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
      // auto-loading can get broken by shading and jar-repacking.
      Connection connection =
          DRIVER_SUPPLIER.get().connect("jdbc:calcite:", info);
      final CalciteServerStatement statement =
          connection.createStatement()
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableInterpretable.java`
#### Snippet
```java

    @Override public void run() throws InterruptedException {
      final Enumerator<@Nullable Object[]> enumerator = enumerable.enumerator();
      while (enumerator.moveNext()) {
        @Nullable Object[] values = enumerator.current();
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableInterpretable.java`
#### Snippet
```java
        return new AbstractEnumerable<@Nullable Object[]>() {
          @Override public Enumerator<@Nullable Object[]> enumerator() {
            final Enumerator<?> enumerator = enumerable.enumerator();
            return new Enumerator<@Nullable Object[]>() {
              @Override public @Nullable Object[] current() {
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableBindable.java`
#### Snippet
```java
              () -> "relSinks.get is null for " + EnumerableBindable.this).get(0);
      final Enumerable<@Nullable Object[]> enumerable = bind(implementor.dataContext);
      final Enumerator<@Nullable Object[]> enumerator = enumerable.enumerator();
      while (enumerator.moveNext()) {
        sink.send(Row.asCopy(enumerator.current()));
```

### AutoCloseableResource
'ResultSet' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/runtime/ResultSetEnumerable.java`
#### Snippet
```java
    @Override public void reset() {
      try {
        resultSet().beforeFirst();
      } catch (SQLException e) {
        throw new RuntimeException(e);
```

### AutoCloseableResource
'ResultSet' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/runtime/ResultSetEnumerable.java`
#### Snippet
```java
    @Override public boolean moveNext() {
      try {
        return resultSet().next();
      } catch (SQLException e) {
        throw new RuntimeException(e);
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/interpreter/TableFunctionScanNode.java`
#### Snippet
```java
      for (@SuppressWarnings({"unchecked", "rawtypes"})
           final Enumerator<Row> enumerator =
           ((Enumerable) o).select(mapFn).enumerator();
           enumerator.moveNext();) {
        sink.send(enumerator.current());
```

### AutoCloseableResource
'Enumerator\>' used without 'try'-with-resources statement
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
          enumerators.add(Linq4j.enumerator((List) list));
        }
        final Enumerator<List<@Nullable Object>> product = Linq4j.product(enumerators);
        return new AbstractEnumerable<@Nullable Object[]>() {
          @Override public Enumerator<@Nullable Object[]> enumerator() {
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `plus/src/main/java/org/apache/calcite/adapter/os/GitCommitsTableFunction.java`
#### Snippet
```java
        return new AbstractEnumerable<@Nullable Object[]>() {
          @Override public Enumerator<@Nullable Object[]> enumerator() {
            final Enumerator<String> e = enumerable.enumerator();
            return new Enumerator<@Nullable Object[]>() {
              private @Nullable Object @Nullable [] objects;
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `plus/src/main/java/org/apache/calcite/adapter/os/FilesTableFunction.java`
#### Snippet
```java
        return new AbstractEnumerable<@Nullable Object[]>() {
          @Override public Enumerator<@Nullable Object[]> enumerator() {
            final Enumerator<String> e = enumerable.enumerator();
            return new Enumerator<@Nullable Object[]>() {
              @Nullable Object @Nullable [] current;
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `plus/src/main/java/org/apache/calcite/adapter/tpch/TpchSchema.java`
#### Snippet
```java
        @Override public Enumerator<Object[]> enumerator() {
          final Enumerator<E> iterator =
              Linq4j.iterableEnumerator(
                  tpchTable.createGenerator(scaleFactor, part, partCount));
          return new Enumerator<Object[]>() {
```

### AutoCloseableResource
'RedisJedisManager' used without 'try'-with-resources statement
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisEnumerator.java`
#### Snippet
```java

    RedisJedisManager redisManager =
        new RedisJedisManager(redisConfig.getHost(), redisConfig.getPort(),
            redisConfig.getDatabase(), redisConfig.getPassword());

```

### AutoCloseableResource
'ServerConnector' used without 'try'-with-resources statement
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java
      server.manage(threadPool);

      final ServerConnector connector = new ServerConnector(server);
      connector.setIdleTimeout(60 * 1000);
      connector.setPort(0);
```

### AutoCloseableResource
'JavaSparkContext' used without 'try'-with-resources statement
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkRules.java`
#### Snippet
```java

  public static void main(String[] args) {
    final JavaSparkContext sc = new JavaSparkContext("local[1]", "calcite");
    final JavaRDD<String> file = sc.textFile("/usr/share/dict/words");
    System.out.println(
```

### AutoCloseableResource
'TableReader' used without 'try'-with-resources statement
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbTable.java`
#### Snippet
```java
    final RelProtoDataType resultRowType = RelDataTypeImpl.proto(fieldInfo.build());

    TableReader tableReader = tableReaderFactory.createTableReader(tableName);
    tableReader.open();
    return new AbstractEnumerable<Object>() {
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/MergeUnionEnumerator.java`
#### Snippet
```java
    int i = 0;
    for (Enumerable<TSource> source : sources) {
      this.inputs[i++] = source.enumerator();
    }

```

### AutoCloseableResource
'Enumerator\>' used without 'try'-with-resources statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/LookupImpl.java`
#### Snippet
```java
      @Override public Enumerator<V> enumerator() {
        final Enumerator<Enumerable<V>> listEnumerator =
            Linq4j.iterableEnumerator(values());
        return new Enumerator<V>() {
          Enumerator<V> enumerator = Linq4j.emptyEnumerator();
```

### AutoCloseableResource
'Enumerator\>' used without 'try'-with-resources statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/LookupImpl.java`
#### Snippet
```java
      @Override public Enumerator<TResult> enumerator() {
        final Enumerator<Grouping<K, V>> groupingEnumerator =
            LookupImpl.this.enumerator();
        return new Enumerator<TResult>() {
          @Override public TResult current() {
```

### AutoCloseableResource
'Enumerator' used without 'try'-with-resources statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
   */
  public static boolean any(Enumerable enumerable) {
    return enumerable.enumerator().moveNext();
  }

```

### AutoCloseableResource
'MongoClient' used without 'try'-with-resources statement
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSchema.java`
#### Snippet
```java
    try {
      final MongoClient mongo = credential == null
          ? new MongoClient(new ServerAddress(host), options)
          : new MongoClient(new ServerAddress(host), credential, options);
      this.mongoDb = mongo.getDatabase(database);
```

### AutoCloseableResource
'MongoClient' used without 'try'-with-resources statement
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSchema.java`
#### Snippet
```java
      final MongoClient mongo = credential == null
          ? new MongoClient(new ServerAddress(host), options)
          : new MongoClient(new ServerAddress(host), credential, options);
      this.mongoDb = mongo.getDatabase(database);
    } catch (Exception e) {
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlOperatorFixture.java`
#### Snippet
```java

  default SqlOperatorFixture withLibrary(SqlLibrary library) {
    return withOperatorTable(
        SqlLibraryOperatorTableFactory.INSTANCE
            .getOperatorTable(SqlLibrary.STANDARD, library))
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlOperatorFixture.java`
#### Snippet
```java

  default SqlOperatorFixture forOracle(SqlConformance conformance) {
    return withConformance(conformance)
        .withOperatorTable(
            SqlLibraryOperatorTableFactory.INSTANCE
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlOperatorFixture.java`
#### Snippet
```java
   * version. */
  default SqlOperatorFixture withConformance(SqlConformance conformance) {
    return withParserConfig(c -> c.withConformance(conformance))
        .withValidatorConfig(c -> c.withConformance(conformance))
        .withConnectionFactory(cf -> cf.with("conformance", conformance));
```

### AutoCloseableResource
'CqlSession' used without 'try'-with-resources statement
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraSchemaFactory.java`
#### Snippet
```java
    final Map<String, Object> sessionMap = projectMapOverKeys(operand, SESSION_DEFINING_KEYS);

    INFO_TO_SESSION.computeIfAbsent(sessionMap, m -> {
      String host = (String) m.get("host");
      String username = (String) m.get("username");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDatetimeTrunc() {
    SqlOperatorFixture nonBigQuery = fixture()
        .setFor(SqlLibraryOperators.DATETIME_TRUNC);
    nonBigQuery.checkFails("^datetime_trunc(timestamp '2012-05-02 15:30:00', hour)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        false);

    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.DATETIME_TRUNC);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testOrOperatorLazy() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.OR, VmName.EXPAND);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractInterval() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXTRACT, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDescendingOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.DESC, VM_EXPAND);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSechFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.SECH);
    f0.checkFails("^sech(1)^",
        "No match found for function signature SECH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testConcatOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CONCAT, VmName.EXPAND);
    f.checkString(" 'a'||'b' ", "ab", "CHAR(2) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * the operator fixture does not currently support type aliases. */
  @Test void testDatetimeDiff() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.DATETIME_DIFF);
    f0.checkFails("^datetime_diff(timestamp '2008-12-25 15:30:00', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
            + "DATETIME_DIFF\\(<TIMESTAMP>, <TIMESTAMP>, <INTERVAL_DAY_TIME>\\)", false);

    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.DATETIME_DIFF);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDayOfWeek() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.DAYOFWEEK, VM_FENNEL, VM_JAVA);
    f.checkScalar("dayofweek(date '2008-1-23')", "4", "BIGINT NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testToBase64() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.MYSQL);
    f.setFor(SqlLibraryOperators.TO_BASE64);
    f.checkString("to_base64(x'546869732069732061207465737420537472696e672e')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsNotEmptyOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_NOT_EMPTY, VM_EXPAND);
    f.checkBoolean("multiset[1] is not empty", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testArrayValueConstructor() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ARRAY_VALUE_CONSTRUCTOR, VmName.EXPAND);
    f.checkScalar("Array['foo']",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  @Test void testGreatestFunc() {
    final SqlOperatorFixture f0 =
        fixture().setFor(SqlLibraryOperators.GREATEST, VmName.EXPAND);
    f0.checkFails("^greatest('on', 'earth')^",
        "No match found for function signature GREATEST\\(<CHARACTER>, <CHARACTER>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testParseDate() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.PARSE_DATE);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonValueExpressionOperator() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("'{}' format json", "{}", "ANY NOT NULL");
    f.checkScalar("'[1, 2, 3]' format json", "[1,2,3]", "ANY NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAndOperatorLazy() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.AND, VmName.EXPAND);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNotOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.NOT, VmName.EXPAND);
    f.checkBoolean("not true", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLogFunc() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.LOG, VmName.EXPAND);
    f0.checkFails("^log(100, 10)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testEqualsOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EQUALS, VmName.EXPAND);
    f.checkBoolean("1=1", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testGreaterThanOrEqualOperatorIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("interval '2' day >= interval '1' day", true);
    f.checkBoolean("interval '2' day >= interval '5' day", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testEndsWithFunction() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.BIG_QUERY);
    f.setFor(SqlLibraryOperators.ENDS_WITH);
    f.checkBoolean("ends_with('12345', '345')", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * while for {@code TIMESTAMP_DIFF} is is (t1 - t2). */
  @Test void testTimestampDiff3() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.TIMESTAMP_DIFF3);
    f0.checkFails("^timestamp_diff(timestamp '2008-12-25 15:30:00', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
            + "TIMESTAMP_DIFF\\(<TIMESTAMP>, <TIMESTAMP>, <INTERVAL_DAY_TIME>\\)", false);

    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.TIMESTAMP_DIFF3);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIfFunc() {
    final SqlOperatorFixture f = fixture();
    checkIf(f.withLibrary(SqlLibrary.BIG_QUERY));
    checkIf(f.withLibrary(SqlLibrary.HIVE));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testStddevSampFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.STDDEV_SAMP, VM_EXPAND);
    f.checkFails("stddev_samp(^*^)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testGreaterThanOperatorIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("interval '2' day > interval '1' day", true);
    f.checkBoolean("interval '2' day > interval '5' day", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTrimFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.TRIM, VmName.EXPAND);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCschFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.CSCH);
    f0.checkFails("^csch(1)^",
        "No match found for function signature CSCH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testOctetLengthFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.OCTET_LENGTH, VmName.EXPAND);
    f.checkScalarExact("OCTET_LENGTH(x'aabbcc')", 3);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLessThanOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LESS_THAN, VmName.EXPAND);
    f.checkBoolean("1<2", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testBitOrFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.BIT_OR, VM_FENNEL, VM_JAVA);
    f.checkFails("bit_or(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRepeatFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.REPEAT);
    f0.checkFails("^repeat('a', -100)^",
        "No match found for function signature REPEAT\\(<CHARACTER>, <NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSumFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SUM, VM_EXPAND);
    f.checkFails("sum(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsEmptyOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_EMPTY, VM_EXPAND);
    f.checkBoolean("multiset[1] is empty", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testEveryFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EVERY, VM_EXPAND);
    f.checkFails("every(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSqrtFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SQRT, VmName.EXPAND);
    f.checkType("sqrt(2)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCardinalityFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CARDINALITY, VM_FENNEL, VM_JAVA);
    f.checkScalarExact("cardinality(multiset[cast(null as integer),2])", 2);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRightFunc() {
    final SqlOperatorFixture f0 = fixture();
    final Consumer<SqlOperatorFixture> consumer = f -> {
      f.setFor(SqlLibraryOperators.RIGHT);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNullifFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.NULLIF, VM_EXPAND);
    f.checkNull("nullif(1,1)");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFloorFuncDateTime() {
    final SqlOperatorFixture f = fixture();
    f.enableTypeCoercion(false)
        .checkFails("^floor('12:34:56')^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonDepth() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlLibraryOperators.JSON_DEPTH, VmName.EXPAND);
    f.checkString("json_depth('1')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   */
  @Test void testChainedCast() {
    final SqlOperatorFixture f = fixture();
    f.checkFails("CAST(CAST(CAST(123456 AS TINYINT) AS INT) AS BIGINT)",
        "Value out of range. Value:\"123456\"", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNotEqualsOperatorIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("interval '2' day <> interval '1' day", true);
    f.checkBoolean("interval '2' day <> interval '2' day", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testOverlayFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.OVERLAY, VmName.EXPAND);
    f.checkString("overlay('ABCdef' placing 'abc' from 1)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      return;
    }
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("interval '2' day < interval '1' day", false);
    f.checkBoolean("interval '2' day < interval '5' day", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCothFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.COTH);
    f0.checkFails("^coth(1)^",
        "No match found for function signature COTH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonStorageSize() {
    final SqlOperatorFixture f = fixture();
    f.checkString("json_storage_size('[100, \"sakila\", [1, 3, 5], 425.05]')",
        "29", "INTEGER");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTruncateFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.TRUNCATE, VmName.EXPAND);
    f.checkType("truncate(42, -1)", "INTEGER NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAsinhFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.ASINH);
    f0.checkFails("^asinh(1)^",
        "No match found for function signature ASINH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCollectFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.COLLECT, VM_FENNEL, VM_JAVA);
    f.checkFails("collect(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPowFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.POW)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testEscapeOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ESCAPE, VM_EXPAND);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  @Disabled("[CALCITE-525] Exception-handling in built-in functions")
  @Test void testLikeEscape2() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("'x' not like 'x' escape 'x'", true);
    f.checkBoolean("'xyz' not like 'xyz' escape 'xyz'", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testArrayConcatAggFunc() {
    final SqlOperatorFixture f = fixture();
    checkArrayConcatAggFunc(f.withLibrary(SqlLibrary.POSTGRESQL));
    checkArrayConcatAggFunc(f.withLibrary(SqlLibrary.BIG_QUERY));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSystemUserFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SYSTEM_USER, VM_FENNEL);
    String user = System.getProperty("user.name"); // e.g. "jhyde"
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCovarPopFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.COVAR_POP, VM_EXPAND);
    f.checkFails("covar_pop(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSignFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SIGN, VmName.EXPAND);
    f.checkType("sign(1)", "INTEGER NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIlikeOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlLibraryOperators.ILIKE, VmName.EXPAND);
    final String noLike = "No match found for function signature ILIKE";
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRoundFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ROUND, VmName.EXPAND);
    f.checkType("round(42, -1)", "INTEGER NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testHour() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.HOUR, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAsinFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ASIN, VmName.EXPAND);
    f.checkType("asin(0)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRandIntegerFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.RAND_INTEGER, VmName.EXPAND);
    for (int i = 0; i < 100; i++) {
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testQuarter() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.QUARTER, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSimilarToOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SIMILAR_TO, VmName.EXPAND);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testStringAggFunc() {
    final SqlOperatorFixture f = fixture();
    checkStringAggFunc(f.withLibrary(SqlLibrary.POSTGRESQL));
    checkStringAggFunc(f.withLibrary(SqlLibrary.BIG_QUERY));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCoalesceFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.COALESCE, VM_EXPAND);
    f.checkString("coalesce('a','b')", "a", "CHAR(1) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSecond() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SECOND, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCurrentRoleFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CURRENT_ROLE, VM_FENNEL);
    // By default, the CURRENT_ROLE function returns
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test public void testJsonInsert() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.MYSQL);
    f.checkString("json_insert('10', '$.a', 10, '$.c', '[true]')",
        "10", "VARCHAR(2000)");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCaseType() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CASE, VmName.EXPAND);
    f.checkType("case 1 when 1 then current_timestamp else null end",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTimestampTrunc() {
    SqlOperatorFixture nonBigQuery = fixture()
        .setFor(SqlLibraryOperators.TIMESTAMP_TRUNC);
    nonBigQuery.checkFails("^timestamp_trunc(timestamp '2012-05-02 15:30:00', hour)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        false);

    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.TIMESTAMP_TRUNC);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTimeSub() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.TIME_SUB);
    f0.checkFails("^time_sub(time '15:30:00', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsDistinctFromOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_DISTINCT_FROM, VM_EXPAND);
    f.checkBoolean("1 is distinct from 1", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDayOfMonth() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.DAYOFMONTH, VM_FENNEL, VM_JAVA);
    f.checkScalar("dayofmonth(date '2008-1-23')", "23", "BIGINT NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testComplexLiteral() {
    final SqlOperatorFixture f = fixture();
    f.check("select 2 * 2 * x from (select 2 as x)",
        SqlTests.INTEGER_TYPE_CHECKER, 8);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAtanFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ATAN, VmName.EXPAND);
    f.checkType("atan(2)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testStrposFunction() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.STRPOS);
    f0.checkFails("^strpos('abc', 'a')^",
        "No match found for function signature STRPOS\\(<CHARACTER>, <CHARACTER>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPrefixPlusOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.UNARY_PLUS, VM_EXPAND);
    f.checkScalarExact("+1", 1);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRegrSyyFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.REGR_SYY, VM_EXPAND);
    f.checkFails("regr_syy(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDayOfYear() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.DAYOFYEAR, VM_FENNEL, VM_JAVA);
    f.checkScalar("dayofyear(date '2008-01-23')", "23", "BIGINT NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   */
  @Test void testLiteralBeyondLimit() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CAST, VmName.EXPAND);
    final List<RelDataType> types =
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMonth() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MONTH, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractIntervalDayTime() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXTRACT, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsNotNullOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_NOT_NULL, VmName.EXPAND);
    f.checkBoolean("true is not null", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * 3-argument {@code TIMESTAMPADD} function. */
  @Test void testTimestampAdd2() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.TIMESTAMP_ADD2);
    f0.checkFails("^timestamp_add(timestamp '2008-12-25 15:30:00', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAcoshFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.ACOSH);
    f0.checkFails("^acosh(1)^",
        "No match found for function signature ACOSH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMssqlConvert() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlLibraryOperators.MSSQL_CONVERT, VmName.EXPAND);
    // happy-paths (no need to test all, proper functionality is tested by CAST already
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNotInOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.NOT_IN, VM_EXPAND);
    f.checkBoolean("1 not in (0, 1, 2)", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMultisetUnionOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MULTISET_UNION_DISTINCT,
        VM_FENNEL, VM_JAVA);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code ARRAY_REPEAT} function from Spark. */
  @Test void testArrayRepeatFunc() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.ARRAY_REPEAT);
    f0.checkFails("^array_repeat(1, 2)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLog10Func() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LOG10, VmName.EXPAND);
    f.checkScalarApprox("log10(10)", "DOUBLE NOT NULL",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testListAggFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LISTAGG, VM_FENNEL, VM_JAVA);
    f.checkFails("listagg(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsNotUnknownOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_NOT_UNKNOWN, VM_EXPAND);
    f.checkBoolean("false is not unknown", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExistsNode() {
    SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.ORACLE);

    f.checkFails("EXISTSNODE('', '<','a')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testBitXorFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.BIT_XOR, VM_FENNEL, VM_JAVA);
    f.checkFails("bit_xor(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFusionFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.FUSION, VM_FENNEL, VM_JAVA);
    f.checkFails("fusion(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonObjectAgg() {
    final SqlOperatorFixture f = fixture();
    f.checkAggType("json_objectagg('foo': 'bar')", "VARCHAR(2000) NOT NULL");
    f.checkAggType("json_objectagg('foo': null)", "VARCHAR(2000) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * {@code TIMESTAMPDIFF} functions with custom time frames. */
  @Test void testCustomTimeFrame() {
    final SqlOperatorFixture f = fixture()
        .withFactory(tf ->
            tf.withTypeSystem(typeSystem ->
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMinusOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MINUS, VmName.EXPAND);
    f.checkScalarExact("-2-1", -3);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testGreaterThanOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.GREATER_THAN, VmName.EXPAND);
    f.checkBoolean("1>2", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testGroupConcatFunc() {
    final SqlOperatorFixture f = fixture();
    checkGroupConcatFunc(f.withLibrary(SqlLibrary.MYSQL));
    checkGroupConcatFuncFails(f.withLibrary(SqlLibrary.BIG_QUERY));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    // SAFE_CAST is available in BigQuery library but not by default.
    // TRY_CAST is available in MSSQL library but not by default.
    final SqlOperatorFixture f0 = fixture();
    if (castType != CastType.CAST) {
      f0.checkFails("^" + castType.name() + "(12 + 3 as varchar(10))^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFormatTime() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.FORMAT_TIME);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  @ParameterizedTest(name = "CoercionEnabled: {0}")
  void testTimestampDiff(boolean coercionEnabled) {
    final SqlOperatorFixture f = fixture()
        .withValidatorConfig(c -> c.withTypeCoercionEnabled(coercionEnabled));
    f.setFor(SqlStdOperatorTable.TIMESTAMP_DIFF, VmName.EXPAND);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractDate() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXTRACT, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testUserFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.USER, VM_FENNEL);
    f.checkString("USER", "sa", "VARCHAR(2000) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAcosFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ACOS, VmName.EXPAND);
    f.checkType("acos(0)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  SubFunChecker substringChecker(SqlConformanceEnum conformance,
      SqlLibrary library) {
    final SqlOperatorFixture f = fixture();
    return new SubFunChecker(
        f.withConnectionFactory(cf ->
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSinhFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.SINH);
    f0.checkFails("^sinh(1)^",
        "No match found for function signature SINH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testApproxCountDistinctFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.COUNT, VM_EXPAND);
    f.checkFails("approx_count_distinct(^*^)", "Unknown identifier '\\*'",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLikeEscape() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LIKE, VmName.EXPAND);
    f.checkBoolean("'a_c' like 'a#_c' escape '#'", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExpFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXP, VM_FENNEL);
    f.checkScalarApprox("exp(2)", "DOUBLE NOT NULL",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLogicalAndFunc() {
    final SqlOperatorFixture f = fixture();
    // not in standard dialect
    final String[] values = {"true", "true", "null"};
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  @Test void testModOperator() {
    // "%" is allowed under BIG_QUERY, MYSQL_5 SQL conformance levels
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlStdOperatorTable.PERCENT_REMAINDER);
    final List<SqlConformanceEnum> conformances =
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * LIKE must match '.' (period) literally</a>. */
  @Test void testLikeDot() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("'abc' like 'a.c'", false);
    f.checkBoolean("'abcde' like '%c.e'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCountifFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.COUNTIF, VM_FENNEL, VM_JAVA)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCoshFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.COSH);
    f0.checkFails("^cosh(1)^",
        "No match found for function signature COSH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPercentRankFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.PERCENT_RANK, VM_FENNEL, VM_JAVA);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAndOperator2() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("case when false then unknown else true end and true",
        true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsTrueOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_TRUE, VmName.EXPAND);
    f.checkBoolean("true is true", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * the Core parser, and therefore gives validation errors. */
  @Test void testDateDiff() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.DATEDIFF);
    f.checkFails("datediff(^\"MONTH\"^, '2019-09-14',  '2019-09-15')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLnFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LN, VmName.EXPAND);
    f.checkScalarApprox("ln(2.71828)", "DOUBLE NOT NULL",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonKeys() {
    final SqlOperatorFixture f = fixture();
    // no path context
    f.checkString("json_keys('{}')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testModFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MOD, VmName.EXPAND);
    f.checkScalarExact("mod(4,2)", 0);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFromBase64() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.FROM_BASE64);
    f0.checkFails("^from_base64('2fjoeiwjfoj==')^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSafeOffsetOperator() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.SAFE_OFFSET);
    f0.checkFails("^ARRAY[2,4,6][SAFE_OFFSET(2)]^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMultiplyOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MULTIPLY, VmName.EXPAND);
    f.checkScalarExact("2*3", 6);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCeilFuncDateTime() {
    final SqlOperatorFixture f = fixture();
    f.enableTypeCoercion(false)
        .checkFails("^ceil('12:34:56')^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAndOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.AND, VmName.EXPAND);
    f.checkBoolean("true and false", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * operator fixture does not currently support type aliases. */
  @Test void testDatetimeAdd() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.DATETIME_ADD);
    f0.checkFails("^datetime_add(timestamp '2008-12-25 15:30:00', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTanFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.TAN, VmName.EXPAND);
    f.checkType("tan(1)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFormatTimestamp() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.FORMAT_TIMESTAMP);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSha1() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.SHA1);
    f0.checkFails("^sha1(x'')^",
        "No match found for function signature SHA1\\(<BINARY>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code ARRAY_DISTINCT} function from Spark. */
  @Test void testArrayDistinctFunc() {
    SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.ARRAY_DISTINCT)
        .withLibrary(SqlLibrary.SPARK);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCaseNull() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CASE, VmName.EXPAND);
    f.checkScalarExact("case when 1 = 1 then 10 else null end", 10);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPowerFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.POWER, VmName.EXPAND);
    f.checkScalarApprox("power(2,-2)", "DOUBLE NOT NULL", isExactly("0.25"));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testConvertFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CONVERT, VM_FENNEL, VM_JAVA);
    f.checkFails("convert('a', utf8, utf10)", "UTF10", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFloorFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.FLOOR, VM_FENNEL);
    f.checkScalarApprox("floor(2.5e0)", "DOUBLE NOT NULL", isExactly(2));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  private void testCurrentDateFunc(Pair<String, Hook.Closeable> pair) {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CURRENT_DATE, VM_FENNEL);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testStddevPopFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.STDDEV_POP, VM_EXPAND);
    f.checkFails("stddev_pop(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonValue() {
    final SqlOperatorFixture f = fixture();
    if (false) {
      f.checkFails("json_value('{\"foo\":100}', 'lax $.foo1' error on empty)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCurrentCatalogFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CURRENT_CATALOG, VM_FENNEL);
    // By default, the CURRENT_CATALOG function returns
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * first argument. */
  @Test void testDatetimeSub() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.DATETIME_SUB);
    f0.checkFails("^datetime_sub(timestamp '2008-12-25 15:30:00', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testParseTimestamp() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.PARSE_TIMESTAMP);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonLength() {
    final SqlOperatorFixture f = fixture();
    // no path context
    f.checkString("json_length('{}')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRankFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.RANK, VM_FENNEL, VM_JAVA);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testArrayAggFunc() {
    final SqlOperatorFixture f = fixture();
    checkArrayAggFunc(f.withLibrary(SqlLibrary.POSTGRESQL));
    checkArrayAggFunc(f.withLibrary(SqlLibrary.BIG_QUERY));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTanhFunc() {
    SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.TANH);
    f0.checkFails("^tanh(1)^",
        "No match found for function signature TANH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code MAP_KEYS} function from Spark. */
  @Test void testMapKeysFunc() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.MAP_KEYS);
    f0.checkFails("^map_keys(map['foo', 1, 'bar', 2])^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  SubFunChecker substrChecker(SqlLibrary library, SqlFunction function) {
    return new SubFunChecker(fixture().withLibrary(library), library, function);
  }

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNotBetween() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.NOT_BETWEEN, VM_EXPAND);
    f.checkBoolean("2 not between 1 and 3", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRtrimFunc() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.RTRIM, VmName.EXPAND);
    f0.checkFails("^rtrim(' aaa')^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAtan2Func() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ATAN2, VmName.EXPAND);
    f.checkType("atan2(2, -2)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSelect() {
    final SqlOperatorFixture f = fixture();
    f.check("select * from (values(1))", SqlTests.INTEGER_TYPE_CHECKER, 1);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSessionUserFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SESSION_USER, VM_FENNEL);
    f.checkString("SESSION_USER", "sa", "VARCHAR(2000) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  @Tag("slow")
  @Test void testArgumentBounds() {
    final SqlOperatorFixture f = fixture();
    final SqlValidatorImpl validator =
        (SqlValidatorImpl) f.getFactory().createValidator();
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLogicalOrFunc() {
    final SqlOperatorFixture f = fixture();
    // not in standard dialect
    final String[] values = {"true", "true", "null"};
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAnyValueFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ANY_VALUE, VM_EXPAND);
    f.checkFails("any_value(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code ARRAY_REVERSE} function from BigQuery. */
  @Test void testArrayReverseFunc() {
    SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.ARRAY_REVERSE)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code UNIX_SECONDS} and other datetime functions from BigQuery. */
  @Test void testUnixSecondsFunc() {
    SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.UNIX_SECONDS)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testValuesOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.VALUES, VM_EXPAND);
    f.check("select 'abc' from (values(true))",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonArrayAgg() {
    final SqlOperatorFixture f = fixture();
    f.checkAggType("json_arrayagg('foo')", "VARCHAR(2000) NOT NULL");
    f.checkAggType("json_arrayagg(null)", "VARCHAR(2000) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRpadFunction() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.BIG_QUERY);
    f.setFor(SqlLibraryOperators.RPAD);
    f.check("select rpad('12345', 8, 'a')", "VARCHAR(5) NOT NULL", "12345aaa");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDegreesFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.DEGREES, VmName.EXPAND);
    f.checkType("degrees(1)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLessThanOrEqualOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LESS_THAN_OR_EQUAL,
        VmName.EXPAND);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCumeDistFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CUME_DIST, VM_FENNEL, VM_JAVA);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonQuery() {
    final SqlOperatorFixture f = fixture();
    // default pathmode the default is: strict mode
    f.checkString("json_query('{\"foo\":100}', '$' null on empty)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMd5() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.MD5);
    f0.checkFails("^md5(x'')^",
        "No match found for function signature MD5\\(<BINARY>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAvgFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.AVG, VM_EXPAND);
    f.checkFails("avg(^*^)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractTimestamp() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXTRACT, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testBoolOrFunc() {
    final SqlOperatorFixture f = fixture();
    // not in standard dialect
    final String[] values = {"true", "true", "null"};
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPlusIntervalOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.PLUS, VmName.EXPAND);
    f.checkScalar("interval '2' day + interval '1' day",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLastValueFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LAST_VALUE, VM_EXPAND);
    final String[] values = {"0", "CAST(null AS INTEGER)", "3", "3"};
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTimestampAdd() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.TIMESTAMP_ADD, VmName.EXPAND);
    MICROSECOND_VARIANTS.forEach(s ->
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCeilFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CEIL, VM_FENNEL);
    f.checkScalarApprox("ceil(10.1e0)", "DOUBLE NOT NULL", isExactly(11));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractXml() {
    SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.ORACLE);

    f.checkFails("\"EXTRACT\"('', '<','a')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDivideOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.DIVIDE, VmName.EXPAND);
    f.checkScalarExact("10 / 5", "INTEGER NOT NULL", "2");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLpadFunction() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.BIG_QUERY);
    f.setFor(SqlLibraryOperators.LPAD);
    f.check("select lpad('12345', 8, 'a')", "VARCHAR(5) NOT NULL", "aaa12345");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSafeOrdinalOperator() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.SAFE_ORDINAL);
    f0.checkFails("^ARRAY[2,4,6][SAFE_ORDINAL(2)]^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test public void testJsonReplace() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.MYSQL);
    f.checkString("json_replace('10', '$.a', 10, '$.c', '[true]')",
        "10", "VARCHAR(2000)");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLeastFunc() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.LEAST, VmName.EXPAND);
    f0.checkFails("^least('on', 'earth')^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testStartsWithFunction() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.BIG_QUERY);
    f.setFor(SqlLibraryOperators.STARTS_WITH);
    f.checkBoolean("starts_with('12345', '123')", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testChar() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.CHR, VM_FENNEL, VM_JAVA);
    f0.checkFails("^char(97)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testOrOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.OR, VmName.EXPAND);
    f.checkBoolean("true or false", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testOffsetOperator() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.OFFSET);
    f0.checkFails("^ARRAY[2,4,6][OFFSET(2)]^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testEqualsOperatorInterval() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("interval '2' day = interval '1' day", false);
    f.checkBoolean("interval '2' day = interval '2' day", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testModeFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MODE, VM_EXPAND);
    f.checkFails("mode(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code ARRAY_CONCAT} function from BigQuery. */
  @Test void testArrayConcat() {
    SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.ARRAY_CONCAT)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRegexpReplaceFunc() {
    final SqlOperatorFixture f0 = fixture();
    final Consumer<SqlOperatorFixture> consumer = f -> {
      f.setFor(SqlLibraryOperators.REGEXP_REPLACE);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRandSeedFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.RAND, VmName.EXPAND);
    f.checkScalarApprox("rand(1)", "DOUBLE NOT NULL", isWithin(0.6016, 0.0001));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractFuncFromDateTime() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXTRACT, VM_FENNEL, VM_JAVA);
    f.checkScalar("extract(year from date '2008-2-23')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPrefixPlusOperatorIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("+interval '-6:2:8' hour to second",
        "-6:02:08.000000", "INTERVAL HOUR TO SECOND NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSoundexFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.SOUNDEX);
    f0.checkFails("^soundex('tech on the net')^",
        "No match found for function signature SOUNDEX\\(<CHARACTER>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMultisetUnionAllOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MULTISET_UNION, VM_FENNEL, VM_JAVA);
    f.checkScalar("cardinality(multiset[1, 2, 3, 4, 2] "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRandIntegerSeedFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.RAND_INTEGER, VmName.EXPAND);
    f.checkScalar("rand_integer(1, 11)", 4, "INTEGER NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMapValueConstructor() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MAP_VALUE_CONSTRUCTOR, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   */
  @Test void testLiteralAtLimit() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CAST, VmName.EXPAND);
    if (!f.brokenTestsEnabled()) {
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsUnknownOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_UNKNOWN, VM_EXPAND);
    f.checkBoolean("false is unknown", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSinFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SIN, VmName.EXPAND);
    f.checkType("sin(1)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLastDayFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LAST_DAY, VmName.EXPAND);
    f.checkScalar("last_day(DATE '2019-02-10')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTimestampSub() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.TIMESTAMP_SUB);
    f0.checkFails("^timestamp_sub(timestamp '2008-12-25 15:30:00', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDateTrunc() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.DATE_TRUNC);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsNotASetOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_NOT_A_SET, VM_EXPAND);
    f.checkBoolean("multiset[1] is not a set", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSha512() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.SHA1);
    f0.checkFails("^sha512(x'')^",
        "No match found for function signature SHA512\\(<BINARY>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsNotDistinctFromOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_NOT_DISTINCT_FROM, VM_EXPAND);
    f.checkBoolean("1 is not distinct from 1", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNotSubMultisetOfOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.NOT_SUBMULTISET_OF, VM_FENNEL, VM_JAVA);
    f.checkBoolean("multiset[2] not submultiset of multiset[1]", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonType() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlLibraryOperators.JSON_TYPE, VmName.EXPAND);
    f.checkString("json_type('\"1\"')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCharacterLengthFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CHARACTER_LENGTH, VmName.EXPAND);
    f.checkScalarExact("CHARACTER_LENGTH('abc')", 3);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIntersectOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MULTISET_INTERSECT, VM_EXPAND);
    f.checkScalar("multiset[1] multiset intersect multiset[1]",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIlikeDot() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.ILIKE, VmName.EXPAND)
        .withLibrary(SqlLibrary.POSTGRESQL);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testConcatFunc() {
    final SqlOperatorFixture f = fixture();
    checkConcatFunc(f.withLibrary(SqlLibrary.MYSQL));
    checkConcatFunc(f.withLibrary(SqlLibrary.POSTGRESQL));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractIntervalYearMonth() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXTRACT, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPiFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.PI, VmName.EXPAND);
    f.checkScalarApprox("PI", "DOUBLE NOT NULL", isWithin(3.1415d, 0.0001d));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAsciiFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ASCII, VmName.EXPAND);
    f.checkScalarExact("ASCII('')", 0);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCompress() {
    SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.MYSQL);
    f.checkNull("COMPRESS(NULL)");
    f.checkString("COMPRESS('')", "",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonRemove() {
    final SqlOperatorFixture f = fixture();
    f.checkString("json_remove('{\"foo\":100}', '$.foo')",
        "{}", "VARCHAR(2000)");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testStddevFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.STDDEV, VM_EXPAND);
    f.checkFails("stddev(^*^)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSha256() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.SHA1);
    f0.checkFails("^sha256(x'')^",
        "No match found for function signature SHA256\\(<BINARY>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testOverlapsOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.OVERLAPS, VM_EXPAND);
    f.checkBoolean("(date '1-2-3', date '1-2-3') "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Test case for {@link SqlLibraryOperators#ARRAY} (Spark). */
  @Test void testArrayFunction() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlLibraryOperators.ARRAY, VmName.EXPAND);
    f.checkFails("^array()^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testBoolAndFunc() {
    final SqlOperatorFixture f = fixture();
    // not in standard dialect
    final String[] values = {"true", "true", "null"};
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * {@code COALESCE} but requires precisely two arguments. */
  @Test void testIfnullFunc() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.IFNULL, VM_EXPAND);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRow() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ROW, VM_FENNEL);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAbsFuncIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("abs(interval '-2' day)", "+2", "INTERVAL DAY NOT NULL");
    f.checkScalar("abs(interval '-5-03' year to month)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExistsOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXISTS, VM_EXPAND);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testElementFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ELEMENT, VM_FENNEL, VM_JAVA);
    f.checkString("element(multiset['abc'])", "abc", "CHAR(3) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code ARRAY_SIZE} function from Spark. */
  @Test void testArraySizeFunc() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.ARRAY_SIZE);
    f0.checkFails("^array_size(array[1])^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testToChar() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.POSTGRESQL);
    f.setFor(SqlLibraryOperators.TO_CHAR);
    f.checkString("to_char(timestamp '2022-06-03 12:15:48.678', 'YYYY-MM-DD HH24:MI:SS.MS TZ')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testBetween() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.BETWEEN, VmName.EXPAND);
    f.checkBoolean("2 between 1 and 3", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testVarPopFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.VAR_POP, VM_EXPAND);
    f.checkFails("var_pop(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRowNumberFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ROW_NUMBER, VM_FENNEL, VM_JAVA);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCbrtFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CBRT, VmName.EXPAND);
    f.checkType("cbrt(1)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMultiplyIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("interval '2:2' hour to minute * 3",
        "+6:06", "INTERVAL HOUR TO MINUTE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTranslateFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.TRANSLATE, VM_FENNEL, VM_JAVA);
    f.checkFails("translate('a' using utf10)", "UTF10", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testInOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IN, VM_EXPAND);
    f.checkBoolean("1 in (0, 1, 2)", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractValue() {
    SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.MYSQL);
    f.checkNull("ExtractValue(NULL, '//b')");
    f.checkNull("ExtractValue('', NULL)");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testModFuncNull() {
    final SqlOperatorFixture f = fixture();
    f.checkNull("mod(cast(null as integer),2)");
    f.checkNull("mod(4,cast(null as tinyint))");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPercentileDiscBigQueryFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.PERCENTILE_DISC2, SqlOperatorFixture.VmName.EXPAND)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIntersectionFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.INTERSECTION, VM_FENNEL, VM_JAVA);
    f.checkFails("intersection(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPositionFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.POSITION, VmName.EXPAND);
    f.checkScalarExact("position('b' in 'abc')", 2);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMaxFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MAX, VM_EXPAND);
    f.checkFails("max(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCosFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.COS, VmName.EXPAND);
    f.checkType("cos(1)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonPredicate() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("'{}' is json value", true);
    f.checkBoolean("'{]' is json value", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPlusOperatorAny() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.PLUS, VmName.EXPAND);
    f.checkScalar("1+CAST(2 AS ANY)", "3", "ANY NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testStrcmpFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.STRCMP)
        .withLibrary(SqlLibrary.MYSQL);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPrefixMinusOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.UNARY_MINUS, VmName.EXPAND);
    f.enableTypeCoercion(false)
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCovarSampFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.COVAR_SAMP, VM_EXPAND);
    f.checkFails("covar_samp(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTimeDiff() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.TIME_DIFF);
    f0.checkFails("^time_diff(time '15:30:00', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCscFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.CSC);
    f0.checkFails("^csc(1)^",
        "No match found for function signature CSC\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code ARRAY_COMPACT} function from Spark. */
  @Test void testArrayCompactFunc() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.ARRAY_COMPACT);
    f0.checkFails("^array_compact(array[null, 1, null, 2])^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLowerFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LOWER, VmName.EXPAND);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNvlFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.NVL, VmName.EXPAND)
        .withLibrary(SqlLibrary.ORACLE);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonObject() {
    final SqlOperatorFixture f = fixture();
    f.checkString("json_object()", "{}", "VARCHAR(2000) NOT NULL");
    f.checkString("json_object('foo': 'bar')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testUpperFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.UPPER, VmName.EXPAND);
    f.checkString("upper('a')", "A", "CHAR(1) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPlusOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.PLUS, VmName.EXPAND);
    f.checkScalarExact("1+2", 3);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code MAP_VALUES} function from Spark. */
  @Test void testMapValuesFunc() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.MAP_VALUES);
    f0.checkFails("^map_values(map['foo', 1, 'bar', 2])^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSubMultisetOfOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SUBMULTISET_OF, VM_FENNEL, VM_JAVA);
    f.checkBoolean("multiset[2] submultiset of multiset[1]", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCharLengthFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CHAR_LENGTH, VmName.EXPAND);
    f.checkScalarExact("char_length('abc')", 3);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLessThanOrEqualOperatorInterval() {
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("interval '2' day <= interval '1' day", false);
    f.checkBoolean("interval '2' day <= interval '5' day", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDecodeFunc() {
    checkDecodeFunc(fixture().withLibrary(SqlLibrary.ORACLE));
  }

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRadiansFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.RADIANS, VmName.EXPAND);
    f.checkType("radians(42)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsNotTrueOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_NOT_TRUE, VmName.EXPAND);
    f.checkBoolean("true is not true", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test public void testJsonSet() {
    final SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.MYSQL);
    f.checkString("json_set('10', '$.a', 10, '$.c', '[true]')",
        "10", "VARCHAR(2000)");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   */
  @Test void testArrayQueryConstructor() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ARRAY_QUERY, SqlOperatorFixture.VmName.EXPAND);
    f.checkScalar("array(select 1)", "[1]",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTimeTrunc() {
    SqlOperatorFixture nonBigQuery = fixture()
        .setFor(SqlLibraryOperators.TIME_TRUNC);
    nonBigQuery.checkFails("^time_trunc(time '15:30:00', hour)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        false);

    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.TIME_TRUNC);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testOrdinalOperator() {
    final SqlOperatorFixture f0 = fixture();
    f0.setFor(SqlLibraryOperators.ORDINAL);
    f0.checkFails("^ARRAY[2,4,6][ORDINAL(2)]^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCurrentUserFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CURRENT_USER, VM_FENNEL);
    f.checkString("CURRENT_USER", "sa", "VARCHAR(2000) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFormatDate() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.FORMAT_DATE);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPercentileDiscFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.PERCENTILE_DISC, VM_FENNEL, VM_JAVA);
    f.checkType("percentile_disc(0.25) within group (order by 1)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCotFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.COT, VmName.EXPAND);
    f.checkType("cot(1)", "DOUBLE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPercentileContFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.PERCENTILE_CONT, VM_FENNEL, VM_JAVA);
    f.checkType("percentile_cont(0.25) within group (order by 1)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExplicitTableOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.EXPLICIT_TABLE, VM_EXPAND);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testXmlTransform() {
    SqlOperatorFixture f = fixture().withLibrary(SqlLibrary.ORACLE);
    f.checkNull("XMLTRANSFORM('', NULL)");
    f.checkNull("XMLTRANSFORM(NULL,'')");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testWeek() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.WEEK, VM_FENNEL, VM_JAVA);
    f.checkScalar("week(date '2008-1-23')", "4", "BIGINT NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRandFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.RAND, VmName.EXPAND);
    f.checkFails("^rand^", "Column 'RAND' not found in any table", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testParseTime() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.PARSE_TIME);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDifferenceFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.DIFFERENCE)
        .withLibrary(SqlLibrary.POSTGRESQL);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    // defined in the SQL:2003 standard
    // todo: implement in fennel
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.INITCAP, VM_FENNEL);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * expression. */
  @Test void testTimeAdd() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.TIME_ADD);
    f0.checkFails("^time_add(time '15:30:00', interval 5 minute)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMssqlConvertWithStyle() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlLibraryOperators.MSSQL_CONVERT, VmName.EXPAND);
    // ensure 'style' argument is ignored
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMinute() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MINUTE, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLiteralChain() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LITERAL_CHAIN, VM_EXPAND);
    f.checkString("'buttered'\n"
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testVarSampFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.VAR_SAMP, VM_EXPAND);
    f.checkFails("var_samp(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDateSub() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.DATE_SUB);
    f0.checkFails("^date_sub(date '2008-12-25', "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLengthFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.LENGTH);
    f0.checkFails("^length('hello')^",
        "No match found for function signature LENGTH\\(<CHARACTER>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSpaceFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.SPACE)
        .withLibrary(SqlLibrary.MYSQL);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMinusIntervalOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MINUS, VmName.EXPAND);
    f.checkScalar("interval '2' day - interval '1' day",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   * ANY/SOME, ALL operators should support collection expressions</a>. */
  @Test void testQuantifyCollectionOperators() {
    final SqlOperatorFixture f = fixture();
    QUANTIFY_OPERATORS.forEach(operator -> f.setFor(operator, SqlOperatorFixture.VmName.EXPAND));

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  @Test void testJsonExists() {
    // default pathmode the default is: strict mode
    final SqlOperatorFixture f = fixture();
    f.checkBoolean("json_exists('{\"foo\":\"bar\"}', "
        + "'$.foo')", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMinFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MIN, VM_EXPAND);
    f.checkFails("min(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExtractWithDatesBeforeUnixEpoch() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("extract(millisecond from"
            + " TIMESTAMP '1969-12-31 21:13:17.357')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testReplaceFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.REPLACE, VmName.EXPAND);
    f.checkString("REPLACE('ciao', 'ciao', '')", "",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMinusDateOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MINUS_DATE, VmName.EXPAND);
    f.checkScalar("(time '12:03:34' - time '11:57:23') minute to second",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testChr() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.CHR, VM_FENNEL, VM_JAVA);
    f0.checkFails("^chr(97.1)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAbsFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ABS, VmName.EXPAND);
    f.checkScalarExact("abs(-1)", 1);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFirstValueFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.FIRST_VALUE, VM_EXPAND);
    final String[] values = {"0", "CAST(null AS INTEGER)", "3", "3"};
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonPretty() {
    final SqlOperatorFixture f = fixture();
    f.checkString("json_pretty('{\"foo\":100}')",
        "{\n  \"foo\" : 100\n}", "VARCHAR(2000)");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNotSimilarToOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.NOT_SIMILAR_TO, VM_EXPAND);
    f.checkBoolean("'ab' not similar to 'a_'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCeilFuncInterval() {
    final SqlOperatorFixture f = fixture();
    if (!f.brokenTestsEnabled()) {
      return;
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   */
  @Test void testJdbcFn() {
    final SqlOperatorFixture f = fixture();
    f.setFor(new SqlJdbcFunctionCall("dummy"), VmName.EXPAND);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testFloorFuncInterval() {
    final SqlOperatorFixture f = fixture();
    if (!f.brokenTestsEnabled()) {
      return;
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDivideOperatorIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("interval '-2:2' hour to minute / 3",
        "-0:41", "INTERVAL HOUR TO MINUTE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTruncFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.TRUNC)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRlikeOperator() {
    SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.RLIKE, VM_EXPAND);
    checkRlike(f.withLibrary(SqlLibrary.SPARK));
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testYear() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.YEAR, VM_FENNEL, VM_JAVA);

```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNotEqualsOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.NOT_EQUALS, VmName.EXPAND);
    f.checkBoolean("1<>1", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSomeAggFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.SOME, VM_EXPAND);
    f.checkFails("some(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDenseRankFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.DENSE_RANK, VM_FENNEL, VM_JAVA);
  }
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPrefixMinusOperatorIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("-interval '-6:2:8' hour to second",
        "+6:02:08.000000", "INTERVAL HOUR TO SECOND NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testItemOp() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.ITEM, VmName.EXPAND);
    f.checkScalar("ARRAY ['foo', 'bar'][1]", "foo", "CHAR(3)");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTranslate3Func() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.TRANSLATE3);
    f0.checkFails("^translate('aabbcc', 'ab', '+-')^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNotLikeOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.NOT_LIKE, VM_EXPAND);
    f.checkBoolean("'abc' not like '_b_'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLeftFunc() {
    final SqlOperatorFixture f0 = fixture();
    final Consumer<SqlOperatorFixture> consumer = f -> {
      f.setFor(SqlLibraryOperators.LEFT);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testReverseFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.REVERSE);
    f0.checkFails("^reverse('abc')^",
        "No match found for function signature REVERSE\\(<CHARACTER>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  @Test void testIlikeEscape() {
    final SqlOperatorFixture f =
        fixture().setFor(SqlLibraryOperators.ILIKE, VmName.EXPAND)
            .withLibrary(SqlLibrary.POSTGRESQL);
    f.checkBoolean("'a_c' ilike 'a#_C' escape '#'", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testBitAndFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.BIT_AND, VM_FENNEL, VM_JAVA);
    f.checkFails("bit_and(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testRegrSxxFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.REGR_SXX, VM_EXPAND);
    f.checkFails("regr_sxx(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLikeOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.LIKE, VmName.EXPAND);
    f.checkBoolean("''  like ''", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCase() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CASE, VmName.EXPAND);
    f.checkScalarExact("case when 'a'='a' then 1 end", 1);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCountFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.COUNT, VM_EXPAND);
    f.checkType("count(*)", "BIGINT NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testSecFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.SEC);
    f0.checkFails("^sec(1)^",
        "No match found for function signature SEC\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testParseDatetime() {
    final SqlOperatorFixture f = fixture()
        .withLibrary(SqlLibrary.BIG_QUERY)
        .setFor(SqlLibraryOperators.PARSE_DATETIME);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testVarFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.VARIANCE, VM_EXPAND);
    f.checkFails("variance(^*^)", "Unknown identifier '\\*'", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testTimestampAddFractionalSeconds() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.TIMESTAMP_ADD, VmName.EXPAND);
    f.checkType("timestampadd(SQL_TSI_FRAC_SECOND, 2, "
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testExceptOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MULTISET_EXCEPT, VM_EXPAND);
    f.checkScalar("multiset[1] multiset except multiset[1]",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests {@code ARRAY_LENGTH} function from BigQuery. */
  @Test void testArrayLengthFunc() {
    SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.ARRAY_LENGTH)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  /** Tests the {@code SPLIT} operator. */
  @Test void testSplitFunction() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.SPLIT);
    f0.checkFails("^split('hello')^",
        "No match found for function signature SPLIT\\(<CHARACTER>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsFalseOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_FALSE, VmName.EXPAND);
    f.checkBoolean("false is false", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsNullOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_NULL, VmName.EXPAND);
    f.checkBoolean("true is null", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testNullIfOperatorIntervals() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("nullif(interval '2' month, interval '3' year)", "+2",
        "INTERVAL MONTH");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testInstrFunction() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.CHR, VM_FENNEL, VM_JAVA);
    f0.checkFails("^INSTR('abc', 'a', 1, 1)^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsNotFalseOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_NOT_FALSE, VmName.EXPAND);
    f.checkBoolean("false is not false", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testGreaterThanOrEqualOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.GREATER_THAN_OR_EQUAL, VmName.EXPAND);
    f.checkBoolean("1>=2", false);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testWindow() {
    final SqlOperatorFixture f = fixture();
    f.check("select sum(1) over (order by x)\n"
            + "from (select 1 as x, 2 as y\n"
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testJsonArray() {
    final SqlOperatorFixture f = fixture();
    f.checkString("json_array()", "[]", "VARCHAR(2000) NOT NULL");
    f.checkString("json_array('foo')",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testLtrimFunc() {
    final SqlOperatorFixture f0 = fixture()
        .setFor(SqlLibraryOperators.LTRIM, VmName.EXPAND);
    f0.checkFails("^ltrim('  aa')^",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testDatePlusInterval() {
    final SqlOperatorFixture f = fixture();
    f.checkScalar("date '2014-02-11' + interval '2' day",
        "2014-02-13", "DATE NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    // submit as non-runtime because the janino exception does not have
    // error position information and the framework is unhappy with that.
    final SqlOperatorFixture f = fixture();
    f.checkFails("mod(3,case 'a' when 'a' then 0 end)",
        DIVISION_BY_ZERO_MESSAGE, true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testCurrentPathFunc() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.CURRENT_PATH, VM_FENNEL);
    f.checkString("CURRENT_PATH", "", "VARCHAR(2000) NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testIsASetOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.IS_A_SET, VM_EXPAND);
    f.checkBoolean("multiset[1] is a set", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testAtanhFunc() {
    final SqlOperatorFixture f0 = fixture().setFor(SqlLibraryOperators.ATANH);
    f0.checkFails("^atanh(1)^",
        "No match found for function signature ATANH\\(<NUMERIC>\\)",
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testMemberOfOperator() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MEMBER_OF, VM_FENNEL, VM_JAVA);
    f.checkBoolean("1 member of multiset[1]", true);
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
   */
  @Test void testMultisetQueryConstructor() {
    final SqlOperatorFixture f = fixture();
    f.setFor(SqlStdOperatorTable.MULTISET_QUERY, SqlOperatorFixture.VmName.EXPAND);
    f.checkScalar("multiset(select 1)", "[1]", "INTEGER NOT NULL MULTISET NOT NULL");
```

### AutoCloseableResource
'SqlOperatorFixture' used without 'try'-with-resources statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

  @Test void testPercentileContBigQueryFunc() {
    final SqlOperatorFixture f = fixture()
        .setFor(SqlLibraryOperators.PERCENTILE_CONT2, SqlOperatorFixture.VmName.EXPAND)
        .withLibrary(SqlLibrary.BIG_QUERY);
```

## RuleId[id=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'java.util.Map\[\]' expected, 'org.apache.calcite.model.JsonCustomTable\[\]' found
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisSchema.java`
#### Snippet
```java
  @Override protected Map<String, Table> getTableMap() {
    JsonCustomTable[] jsonCustomTables = new JsonCustomTable[tables.size()];
    Set<String> tableNames = Arrays.stream(tables.toArray(jsonCustomTables))
        .map(e -> e.name).collect(Collectors.toSet());
    tableMap =
```

## RuleId[id=RedundantCast]
### RedundantCast
Casting `getLeft()` to `PigRel` is redundant
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigJoin.java`
#### Snippet
```java
        filterNulls);

    String leftRelAlias = implementor.getPigRelationAlias((PigRel) getLeft());
    String rightRelAlias = implementor.getPigRelationAlias((PigRel) getRight());
    String leftJoinFieldName = implementor.getFieldName((PigRel) getLeft(), leftKeys.get(0));
```

### RedundantCast
Casting `getRight()` to `PigRel` is redundant
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigJoin.java`
#### Snippet
```java

    String leftRelAlias = implementor.getPigRelationAlias((PigRel) getLeft());
    String rightRelAlias = implementor.getPigRelationAlias((PigRel) getRight());
    String leftJoinFieldName = implementor.getFieldName((PigRel) getLeft(), leftKeys.get(0));
    String rightJoinFieldName = implementor.getFieldName((PigRel) getRight(), rightKeys.get(0));
```

### RedundantCast
Casting `getLeft()` to `PigRel` is redundant
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigJoin.java`
#### Snippet
```java
    String leftRelAlias = implementor.getPigRelationAlias((PigRel) getLeft());
    String rightRelAlias = implementor.getPigRelationAlias((PigRel) getRight());
    String leftJoinFieldName = implementor.getFieldName((PigRel) getLeft(), leftKeys.get(0));
    String rightJoinFieldName = implementor.getFieldName((PigRel) getRight(), rightKeys.get(0));

```

### RedundantCast
Casting `getRight()` to `PigRel` is redundant
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigJoin.java`
#### Snippet
```java
    String rightRelAlias = implementor.getPigRelationAlias((PigRel) getRight());
    String leftJoinFieldName = implementor.getFieldName((PigRel) getLeft(), leftKeys.get(0));
    String rightJoinFieldName = implementor.getFieldName((PigRel) getRight(), rightKeys.get(0));

    return implementor.getPigRelationAlias((PigRel) getLeft()) + " = JOIN " + leftRelAlias + " BY "
```

### RedundantCast
Casting `getLeft()` to `PigRel` is redundant
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigJoin.java`
#### Snippet
```java
    String rightJoinFieldName = implementor.getFieldName((PigRel) getRight(), rightKeys.get(0));

    return implementor.getPigRelationAlias((PigRel) getLeft()) + " = JOIN " + leftRelAlias + " BY "
        + leftJoinFieldName + ' ' + getPigJoinType() + ", " + rightRelAlias + " BY "
        + rightJoinFieldName + ';';
```

### RedundantCast
Casting `null` to `@Nullable RexNode` is redundant
in `core/src/main/java/org/apache/calcite/rel/rules/JoinToMultiJoinRule.java`
#### Snippet
```java
            null);
      } else {
        joinSpecs.add(Pair.of(JoinRelType.INNER, (@Nullable RexNode) null));
      }
      joinSpecs.add(Pair.of(joinType, joinRel.getCondition()));
```

### RedundantCast
Casting `null` to `RexNode` is redundant
in `core/src/main/java/org/apache/calcite/rel/rules/JoinToMultiJoinRule.java`
#### Snippet
```java
            joinRel.getRowType().getFieldList());
      } else {
        joinSpecs.add(Pair.of(JoinRelType.INNER, (RexNode) null));
      }
      break;
```

### RedundantCast
Casting `null` to `RexNode` is redundant
in `core/src/main/java/org/apache/calcite/rel/rules/JoinToMultiJoinRule.java`
#### Snippet
```java
            null);
      } else {
        joinSpecs.add(Pair.of(JoinRelType.INNER, (RexNode) null));
      }
      if (rightCombined) {
```

### RedundantCast
Casting `null` to `RexNode` is redundant
in `core/src/main/java/org/apache/calcite/rel/rules/JoinToMultiJoinRule.java`
#### Snippet
```java
            joinRel.getRowType().getFieldList());
      } else {
        joinSpecs.add(Pair.of(JoinRelType.INNER, (RexNode) null));
      }
    }
```

### RedundantCast
Casting `ex.getCause()` to `CyclicMetadataException` is redundant
in `core/src/main/java/org/apache/calcite/rel/metadata/ProxyingMetadataHandlerProvider.java`
#### Snippet
```java
      } catch (InvocationTargetException ex) {
        if (ex.getCause() instanceof CyclicMetadataException) {
          throw (CyclicMetadataException) ex.getCause();
        }

```

### RedundantCast
Casting `type.getPrecision()` to `double` is redundant
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdSize.java`
#### Snippet
```java
      return (double) type.getPrecision();
    case VARBINARY:
      return Math.min((double) type.getPrecision(), 100d);
    case CHAR:
      return (double) type.getPrecision() * BYTES_PER_CHARACTER;
```

### RedundantCast
Casting `null` to `RexProgram` is redundant
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
    public SqlNode toSql(AggregateCall aggCall) {
      return toSql(aggCall.getAggregation(), aggCall.isDistinct(),
          Util.transform(aggCall.rexList, e -> toSql((RexProgram) null, e)),
          Util.transform(aggCall.getArgList(), this::field),
          aggCall.filterArg, aggCall.collation, aggCall.isApproximate());
```

### RedundantCast
Casting `map` to `Map` is redundant
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java
            physical = false;
          }
          final boolean distinct = get((Map<String, Object>) map, "distinct");
          return rexBuilder.makeOver(type, operator, rexOperands, partitionKeys,
              ImmutableList.copyOf(orderKeys),
```

### RedundantCast
Casting `call.getOperandList()` to `List<@Nullable SqlNode>` is redundant
in `core/src/main/java/org/apache/calcite/sql/util/SqlShuttle.java`
#### Snippet
```java
      this.call = call;
      this.update = false;
      final List<@Nullable SqlNode> operands = (List<@Nullable SqlNode>) call.getOperandList();
      this.clonedOperands = operands.toArray(new SqlNode[0]);
      this.alwaysCopy = alwaysCopy;
```

### RedundantCast
Casting `validatorTable` to `Prepare.PreparingTable` is redundant
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingScope.java`
#### Snippet
```java
      SqlValidatorTable validatorTable = ns.getTable();
      if (validatorTable instanceof Prepare.PreparingTable) {
        Table t = ((Prepare.PreparingTable) validatorTable).unwrap(Table.class);
        if (t instanceof CustomColumnResolvingTable) {
          final List<Pair<RelDataTypeField, List<String>>> entries =
```

### RedundantCast
Casting `getTraits(...)` to `List` is redundant
in `core/src/main/java/org/apache/calcite/plan/RelTraitSet.java`
#### Snippet
```java
    if (index >= 0) {
      //noinspection unchecked
      return (List<T>) getTraits(index);
    }

```

### RedundantCast
Casting `metaSchema -> {...}` to `Function1` is redundant
in `core/src/main/java/org/apache/calcite/jdbc/CalciteMetaImpl.java`
#### Snippet
```java
            new CalciteMetaSchema(calciteSchema, catalog,
                calciteSchema.getName()))
        .orderBy((Function1<MetaSchema, Comparable>) metaSchema ->
            (Comparable) FlatLists.of(Util.first(metaSchema.tableCatalog, ""),
                metaSchema.tableSchem));
```

### RedundantCast
Casting `enumerable` to `Iterable` is redundant
in `core/src/main/java/org/apache/calcite/jdbc/CalciteMetaImpl.java`
#### Snippet
```java
    }
    //noinspection unchecked
    final Iterable<Object> iterable = (Iterable<Object>) (Iterable) enumerable;
    return createResultSet(Collections.emptyMap(),
        columns, CursorFactory.record(clazz, fields, fieldNames),
```

### RedundantCast
Casting `element(...)` to `E` is redundant
in `core/src/main/java/org/apache/calcite/util/ChunkList.java`
#### Snippet
```java
      }
      //noinspection unchecked
      return (E) element(currentChunk(), cursor - start);
    }

```

### RedundantCast
Casting `element(...)` to `E` is redundant
in `core/src/main/java/org/apache/calcite/util/ChunkList.java`
#### Snippet
```java
      @SuppressWarnings("unchecked")
      final E element =
          (E) element(currentChunk(), HEADER_SIZE + (lastRet = cursor++) - start);
      return element;
    }
```

### RedundantCast
Casting `a.getClass()` to `Class` is redundant
in `core/src/main/java/org/apache/calcite/util/ImmutableIntList.java`
#### Snippet
```java
              requireNonNull(a.getClass().getComponentType()), size);
    }
    if ((Class) a.getClass() == Integer[].class) {
      final Integer[] integers = (Integer[]) a;
      for (int i = 0; i < size; i++) {
```

### RedundantCast
Casting `from` to `int` is redundant
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
     */
    public void defineEscape(char from, String to) {
      int i = (int) from;
      List<@Nullable String> translationVector =
          requireNonNull(this.translationVector, "translationVector");
```

### RedundantCast
Casting `vertexMap.keySet()` to `Set` is redundant
in `core/src/main/java/org/apache/calcite/util/graph/DefaultDirectedGraph.java`
#### Snippet
```java
      Ordering<E> edgeOrdering) {
    return "graph("
        + "vertices: " + vertexOrdering.sortedCopy((Set<V>) vertexMap.keySet())
        + ", edges: " + edgeOrdering.sortedCopy(edges) + ")";
  }
```

### RedundantCast
Casting `source -> {...}` to `IntFunction<@Nullable Integer>` is redundant
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
    }
    return target(
        (IntFunction<@Nullable Integer>) source -> {
          int source2 = source - offset;
          return source2 < 0 || source2 >= mapping.getSourceCount()
```

### RedundantCast
Casting `parent.inverse()` to `TargetMapping` is redundant
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
    @Override public Mapping inverse() {
      return new OverridingTargetMapping(
          (TargetMapping) parent.inverse(),
          target,
          source);
```

### RedundantCast
Casting `source -> {...}` to `IntFunction<@Nullable Integer>` is redundant
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
    }
    return target(
        (IntFunction<@Nullable Integer>) source -> {
          final int source2 = source - offset;
          if (source2 < 0 || source2 >= mapping.getSourceCount()) {
```

### RedundantCast
Casting `source -> {...}` to `IntFunction<@Nullable Integer>` is redundant
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
    }
    return target(
        (IntFunction<@Nullable Integer>) source -> {
          int target = mapping.getTargetOpt(source);
          return target < 0 ? null : target + offset;
```

### RedundantCast
Casting `null` to `Space` is redundant
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
      }
      this.singletonSpaces =
          new ArrayList<>(Collections.nCopies(columns.size(), (Space) null));
      if (combinationsPerPass > Math.pow(2D, columns.size())) {
        // There are not many columns. We can compute all combinations in the
```

### RedundantCast
Casting `curCh` to `int` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    for (int i = 0; i < len; i++) {
      char curCh = s.charAt(i);
      final int c = (int) curCh;
      if (start) {  // curCh is whitespace or first character of word.
        if (c > 47 && c < 58) { // 0-9
```

### RedundantCast
Casting `elements` to `Object[]` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    @Override public FlatLists.ComparableList<E> current() {
      int i = 0;
      for (Object element : (Object[]) elements) {
        Object[] a;
        if (element.getClass().isArray()) {
```

### RedundantCast
Casting `b0` to `int` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>FLOOR</code> operator applied to byte values. */
  public static byte floor(byte b0, byte b1) {
    return (byte) floor((int) b0, (int) b1);
  }

```

### RedundantCast
Casting `b0` to `int` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>FLOOR</code> operator applied to short values. */
  public static short floor(short b0, short b1) {
    return (short) floor((int) b0, (int) b1);
  }

```

### RedundantCast
Casting `operand` to `Map` is redundant
in `plus/src/main/java/org/apache/calcite/adapter/tpch/TpchSchemaFactory.java`
#### Snippet
```java
  @Override public Schema create(SchemaPlus parentSchema, String name,
      Map<String, Object> operand) {
    Map map = (Map) operand;
    double scale = Util.first((Double) map.get("scale"), 1D);
    int part = Util.first((Integer) map.get("part"), 1);
```

### RedundantCast
Casting `operand` to `Map` is redundant
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSchemaFactory.java`
#### Snippet
```java
  @Override public synchronized Schema create(SchemaPlus parentSchema, String name,
      Map<String, Object> operand) {
    Map map = (Map) operand;
    String locatorHost = (String) map.get(LOCATOR_HOST);
    int locatorPort = Integer.valueOf((String) map.get(LOCATOR_PORT));
```

### RedundantCast
Casting `operand` to `Map` is redundant
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchemaFactory.java`
#### Snippet
```java
  @Override public Schema create(SchemaPlus parentSchema,
      String name, Map<String, Object> operand) {
    Map map = (Map) operand;

    String locatorHost = (String) map.get(LOCATOR_HOST);
```

### RedundantCast
Casting `requireNonNull(...)` to `RexFieldAccess` is redundant
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java

    public RexFieldAccess getFieldAccess(CorrelationId name) {
      return (RexFieldAccess) requireNonNull(
          bb.mapCorrelateToRex.get(name),
          () -> "Correlation " + name + " is not found");
```

### RedundantCast
Casting `requireNonNull(...)` to `T` is redundant
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
  private <T extends SqlValidatorNamespace> T getNamespace(SqlNode node) {
    //noinspection unchecked
    return (T) requireNonNull(
        getNamespaceOrNull(node),
        () -> "Namespace is not found for " + node);
```

### RedundantCast
Casting `this` to `Queryable` is redundant
in `linq4j/src/main/java/org/apache/calcite/linq4j/QueryableDefaults.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    <U> U castSingle() {
      return ((Queryable<U>) (Queryable) this).single();
    }

```

### RedundantCast
Casting `this` to `Queryable` is redundant
in `linq4j/src/main/java/org/apache/calcite/linq4j/QueryableDefaults.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public <U> Queryable<U> castQueryable() {
      return (Queryable<U>) (Queryable) this;
    }
  }
```

### RedundantCast
Casting `null` to `Type` is redundant
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
  public static BlockStatement block(
      Iterable<? extends Statement> statements) {
    return block((Type) null, statements);
  }

```

### RedundantCast
Casting `Extensions.COMPARABLE_MIN` to `Function2` is redundant
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  private static <TSource extends Comparable<TSource>> Function2<TSource, TSource, TSource>
      minFunction() {
    return (Function2<TSource, TSource, TSource>) (Function2) Extensions.COMPARABLE_MIN;
  }

```

### RedundantCast
Casting `Extensions.COMPARABLE_MAX` to `Function2` is redundant
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  private static <TSource extends Comparable<TSource>> Function2<TSource, TSource, TSource>
      maxFunction() {
    return (Function2<TSource, TSource, TSource>) (Function2) Extensions.COMPARABLE_MAX;
  }

```

### RedundantCast
Casting `listGetter(...)` to `Function1` is redundant
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoEnumerator.java`
#### Snippet
```java
        : fields.size() == 1
            ? singletonGetter(fields.get(0).getKey(), fields.get(0).getValue())
            : (Function1) listGetter(fields);
  }

```

### RedundantCast
Casting `operand` to `Map` is redundant
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSchemaFactory.java`
#### Snippet
```java
      Map<String, Object> operand) {

    final Map map = (Map) operand;

    final ObjectMapper mapper = new ObjectMapper();
```

## RuleId[id=FieldMayBeFinal]
### FieldMayBeFinal
Field `connection` may be 'final'
in `core/src/main/java/org/apache/calcite/DataContexts.java`
#### Snippet
```java
  /** Implementation of {@link DataContext} backed by a Map. */
  private static class DataContextImpl extends MapDataContext {
    private CalciteConnection connection;
    private @Nullable SchemaPlus rootSchema;

```

### FieldMayBeFinal
Field `rootSchema` may be 'final'
in `core/src/main/java/org/apache/calcite/DataContexts.java`
#### Snippet
```java
  private static class DataContextImpl extends MapDataContext {
    private CalciteConnection connection;
    private @Nullable SchemaPlus rootSchema;

    DataContextImpl(CalciteConnection connection,
```

### FieldMayBeFinal
Field `relClazz` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/hint/NodeTypeHintPredicate.java`
#### Snippet
```java
    /** Relational expression clazz that the hint can apply to. */
    @SuppressWarnings("ImmutableEnumChecker")
    private Class<?> relClazz;

    NodeType(Class<?> relClazz) {
```

### FieldMayBeFinal
Field `nodeType` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/hint/NodeTypeHintPredicate.java`
#### Snippet
```java
  }

  private NodeType nodeType;

  public NodeTypeHintPredicate(NodeType nodeType) {
```

### FieldMayBeFinal
Field `composition` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/hint/CompositeHintPredicate.java`
#### Snippet
```java

  private ImmutableList<HintPredicate> predicates;
  private Composition composition;

  /**
```

### FieldMayBeFinal
Field `predicates` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/hint/CompositeHintPredicate.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private ImmutableList<HintPredicate> predicates;
  private Composition composition;

```

### FieldMayBeFinal
Field `hintName` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/hint/RelHint.java`
#### Snippet
```java
  /** Builder for {@link RelHint}. */
  public static class Builder {
    private String hintName;
    private List<Integer> inheritPath;

```

### FieldMayBeFinal
Field `fields` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/type/RelRecordType.java`
#### Snippet
```java
   */
  private static class SerializableRelRecordType implements Serializable {
    private List<RelDataTypeField> fields;

    private SerializableRelRecordType(List<RelDataTypeField> fields) {
```

### FieldMayBeFinal
Field `chosenSemiJoins` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
   * filtered by semijoins. Otherwise, the entry is the original join factor.
   */
  private RelNode [] chosenSemiJoins;

  /**
```

### FieldMayBeFinal
Field `charset` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactoryImpl.java`
#### Snippet
```java
    private final boolean nullable;
    private @Nullable SqlCollation collation;
    private @Nullable Charset charset;

    public JavaType(Class clazz) {
```

### FieldMayBeFinal
Field `collation` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactoryImpl.java`
#### Snippet
```java
    private final Class clazz;
    private final boolean nullable;
    private @Nullable SqlCollation collation;
    private @Nullable Charset charset;

```

### FieldMayBeFinal
Field `joinFilters` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/rules/LoptMultiJoin.java`
#### Snippet
```java
   * Excludes left/right outer join filters.
   */
  private List<RexNode> joinFilters;

  /**
```

### FieldMayBeFinal
Field `projFields` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/rules/LoptMultiJoin.java`
#### Snippet
```java
   * join conditions, unless the field appears in the final projection list.
   */
  private List<@Nullable ImmutableBitSet> projFields;

  /**
```

### FieldMayBeFinal
Field `joinFieldRefCountsMap` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/rules/LoptMultiJoin.java`
#### Snippet
```java
   * the factor number.
   */
  private Map<Integer, int[]> joinFieldRefCountsMap;

  /**
```

### FieldMayBeFinal
Field `allJoinFilters` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/rules/LoptMultiJoin.java`
#### Snippet
```java
   * list. Includes left/right outer join filters.
   */
  private List<RexNode> allJoinFilters;

  /** Number of factors into the MultiJoin. */
```

### FieldMayBeFinal
Field `nTotalFields` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/rules/LoptMultiJoin.java`
#### Snippet
```java

  /** Total number of fields in the MultiJoin. */
  private int nTotalFields;

  /** Original inputs into the MultiJoin. */
```

### FieldMayBeFinal
Field `defaultExpander` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/rules/ReduceDecimalsRule.java`
#### Snippet
```java
  private static class ExpanderMap {
    private final Map<SqlOperator, RexExpander> map;
    private RexExpander defaultExpander;

    private ExpanderMap(RexBuilder rexBuilder) {
```

### FieldMayBeFinal
Field `rel` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
  private static class CardOfProjExpr extends RexVisitorImpl<@Nullable Double> {
    private final RelMetadataQuery mq;
    private Project rel;

    CardOfProjExpr(RelMetadataQuery mq, Project rel) {
```

### FieldMayBeFinal
Field `metadataClass0` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/metadata/ReflectiveRelMetadataProvider.java`
#### Snippet
```java
  @Deprecated // to be removed before 2.0
  static class Space2 extends Space {
    private Class<Metadata> metadataClass0;

    Space2(Class<Metadata> metadataClass0,
```

### FieldMayBeFinal
Field `nodeLabels` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/externalize/RelDotWriter.java`
#### Snippet
```java
  private final Map<RelNode, List<RelNode>> outArcTable = new LinkedHashMap<>();

  private Map<RelNode, String> nodeLabels = new HashMap<>();

  private Multimap<RelNode, String> nodeStyles = HashMultimap.create();
```

### FieldMayBeFinal
Field `nodeStyles` may be 'final'
in `core/src/main/java/org/apache/calcite/rel/externalize/RelDotWriter.java`
#### Snippet
```java
  private Map<RelNode, String> nodeLabels = new HashMap<>();

  private Multimap<RelNode, String> nodeStyles = HashMultimap.create();

  private final WriteOption option;
```

### FieldMayBeFinal
Field `newRefs` may be 'final'
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
   */
  private static class UpdateRefShuttle extends RexShuttle {
    private List<RexLocalRef> newRefs;

    private UpdateRefShuttle(List<RexLocalRef> newRefs) {
```

### FieldMayBeFinal
Field `validating` may be 'final'
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
  private final @Nullable RexSimplify simplify;
  private @Nullable RexLocalRef conditionRef = null;
  private boolean validating;

  //~ Constructors -----------------------------------------------------------
```

### FieldMayBeFinal
Field `scale` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/SqlNumericLiteral.java`
#### Snippet
```java

  private @Nullable Integer prec;
  private @Nullable Integer scale;
  private boolean isExact;

```

### FieldMayBeFinal
Field `isExact` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/SqlNumericLiteral.java`
#### Snippet
```java
  private @Nullable Integer prec;
  private @Nullable Integer scale;
  private boolean isExact;

  //~ Constructors -----------------------------------------------------------
```

### FieldMayBeFinal
Field `prec` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/SqlNumericLiteral.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private @Nullable Integer prec;
  private @Nullable Integer scale;
  private boolean isExact;
```

### FieldMayBeFinal
Field `precision` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/SqlBasicTypeNameSpec.java`
#### Snippet
```java
  private final SqlTypeName sqlTypeName;

  private int precision;
  private int scale;

```

### FieldMayBeFinal
Field `charSetName` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/SqlBasicTypeNameSpec.java`
#### Snippet
```java
  private int scale;

  private @Nullable String charSetName;

  /**
```

### FieldMayBeFinal
Field `scale` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/SqlBasicTypeNameSpec.java`
#### Snippet
```java

  private int precision;
  private int scale;

  private @Nullable String charSetName;
```

### FieldMayBeFinal
Field `type` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/SqlTypeConstructorFunction.java`
#### Snippet
```java
public class SqlTypeConstructorFunction extends SqlFunction {

  private RelDataType type;

  /**
```

### FieldMayBeFinal
Field `point` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSpatialTypeFunctions.java`
#### Snippet
```java
    private final BigDecimal deltaX;
    private final BigDecimal deltaY;
    private boolean point;

    GridTable(Geometry geom, BigDecimal deltaX, BigDecimal deltaY,
```

### FieldMayBeFinal
Field `dynamicParameters` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/util/SqlString.java`
#### Snippet
```java
  private final String sql;
  private SqlDialect dialect;
  private @Nullable ImmutableList<Integer> dynamicParameters;

  /**
```

### FieldMayBeFinal
Field `dialect` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/util/SqlString.java`
#### Snippet
```java
public class SqlString {
  private final String sql;
  private SqlDialect dialect;
  private @Nullable ImmutableList<Integer> dynamicParameters;

```

### FieldMayBeFinal
Field `endColumnNum` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
    private int startColumnNum;
    private int endLineNum;
    private int endColumnNum;
    private @Nullable String errorMsg;

```

### FieldMayBeFinal
Field `endLineNum` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
    private int startLineNum;
    private int startColumnNum;
    private int endLineNum;
    private int endColumnNum;
    private @Nullable String errorMsg;
```

### FieldMayBeFinal
Field `startColumnNum` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
  public static class ValidateErrorInfo {
    private int startLineNum;
    private int startColumnNum;
    private int endLineNum;
    private int endColumnNum;
```

### FieldMayBeFinal
Field `errorMsg` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
    private int endLineNum;
    private int endColumnNum;
    private @Nullable String errorMsg;

    /**
```

### FieldMayBeFinal
Field `startLineNum` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
  /** Text and position info of a validator or parser exception. */
  public static class ValidateErrorInfo {
    private int startLineNum;
    private int startColumnNum;
    private int endLineNum;
```

### FieldMayBeFinal
Field `ruleAttempts` may be 'final'
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
  private static class RuleAttemptsListener implements RelOptListener {
    private long beforeTimestamp;
    private Map<String, Pair<Long, Long>> ruleAttempts;

    RuleAttemptsListener() {
```

### FieldMayBeFinal
Field `results` may be 'final'
in `core/src/main/java/org/apache/calcite/plan/hep/HepRuleCall.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------

  private List<RelNode> results;

  //~ Constructors -----------------------------------------------------------
```

### FieldMayBeFinal
Field `upperBound` may be 'final'
in `core/src/main/java/org/apache/calcite/plan/volcano/TopDownRuleDriver.java`
#### Snippet
```java
  private class OptimizeGroup implements Task {
    private final RelSubset group;
    private RelOptCost upperBound;

    OptimizeGroup(RelSubset group, RelOptCost upperBound) {
```

### FieldMayBeFinal
Field `builder` may be 'final'
in `core/src/main/java/org/apache/calcite/plan/volcano/TopDownRuleDriver.java`
#### Snippet
```java
  private static class TaskDescriptor {
    private boolean first = true;
    private StringBuilder builder = new StringBuilder();

    void log(Task task) {
```

### FieldMayBeFinal
Field `primitiveToBoxingMap` may be 'final'
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
  //~ Static fields/initializers ---------------------------------------------

  private static Map<Class, Class> primitiveToBoxingMap;
  private static Map<Class, Method> primitiveToByteBufferReadMethod;
  private static Map<Class, Method> primitiveToByteBufferWriteMethod;
```

### FieldMayBeFinal
Field `primitiveToByteBufferReadMethod` may be 'final'
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java

  private static Map<Class, Class> primitiveToBoxingMap;
  private static Map<Class, Method> primitiveToByteBufferReadMethod;
  private static Map<Class, Method> primitiveToByteBufferWriteMethod;

```

### FieldMayBeFinal
Field `primitiveToByteBufferWriteMethod` may be 'final'
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
  private static Map<Class, Class> primitiveToBoxingMap;
  private static Map<Class, Method> primitiveToByteBufferReadMethod;
  private static Map<Class, Method> primitiveToByteBufferWriteMethod;

  static {
```

### FieldMayBeFinal
Field `equivalence` may be 'final'
in `core/src/main/java/org/apache/calcite/util/BitSets.java`
#### Snippet
```java
   */
  private static class Closure {
    private SortedMap<Integer, BitSet> equivalence;
    private final NavigableMap<Integer, BitSet> closure = new TreeMap<>();

```

### FieldMayBeFinal
Field `indentation` may be 'final'
in `core/src/main/java/org/apache/calcite/util/StackWriter.java`
#### Snippet
```java

  private int indentationDepth;
  private String indentation;
  private boolean needIndent;
  private final Deque<Character> quoteStack = new ArrayDeque<>();
```

### FieldMayBeFinal
Field `scope` may be 'final'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
  /** Visitor that retrieves pattern variables defined. */
  private static class PatternVarVisitor implements SqlVisitor<Void> {
    private MatchRecognizeScope scope;
    PatternVarVisitor(MatchRecognizeScope scope) {
      this.scope = scope;
```

### FieldMayBeFinal
Field `defaultSchema` may be 'final'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java

  // set in STATE_2_READY
  private @Nullable SchemaPlus defaultSchema;
  private @Nullable JavaTypeFactory typeFactory;
  private @Nullable RelOptPlanner planner;
```

### FieldMayBeFinal
Field `executor` may be 'final'
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java
  private @Nullable JavaTypeFactory typeFactory;
  private @Nullable RelOptPlanner planner;
  private @Nullable RexExecutor executor;

  // set in STATE_4_VALIDATE
```

### FieldMayBeFinal
Field `queues` may be 'final'
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
  /** Implementation of {@link Sink} using a {@link java.util.ArrayDeque}. */
  private static class DuplicatingSink implements Sink {
    private List<ArrayDeque<Row>> queues;

    private DuplicatingSink(List<ArrayDeque<Row>> queues) {
```

### FieldMayBeFinal
Field `converter` may be 'final'
in `file/src/main/java/org/apache/calcite/adapter/file/FileTable.java`
#### Snippet
```java
  private final RelProtoDataType protoRowType;
  private FileReader reader;
  private FileRowConverter converter;

  /** Creates a FileTable. */
```

### FieldMayBeFinal
Field `reader` may be 'final'
in `file/src/main/java/org/apache/calcite/adapter/file/FileTable.java`
#### Snippet
```java

  private final RelProtoDataType protoRowType;
  private FileReader reader;
  private FileRowConverter converter;

```

### FieldMayBeFinal
Field `processSupplier` may be 'final'
in `plus/src/main/java/org/apache/calcite/adapter/os/Processes.java`
#### Snippet
```java
  private static class ProcessLinesEnumerator
      extends AbstractEnumerable<String> {
    private Supplier<Process> processSupplier;

    ProcessLinesEnumerator(Supplier<Process> processSupplier) {
```

### FieldMayBeFinal
Field `fieldTypes` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeEnumerator.java`
#### Snippet
```java
  private Iterator iterator;
  private Object current;
  private List<RelDataTypeField> fieldTypes;

  /**
```

### FieldMayBeFinal
Field `iterator` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeEnumerator.java`
#### Snippet
```java
  protected static final Logger LOGGER = LoggerFactory.getLogger(GeodeEnumerator.class.getName());

  private Iterator iterator;
  private Object current;
  private List<RelDataTypeField> fieldTypes;
```

### FieldMayBeFinal
Field `clientCache` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleScannableTable.java`
#### Snippet
```java
  private final RelDataType relDataType;
  private String regionName;
  private ClientCache clientCache;

  public GeodeSimpleScannableTable(String regionName, RelDataType relDataType,
```

### FieldMayBeFinal
Field `regionName` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleScannableTable.java`
#### Snippet
```java

  private final RelDataType relDataType;
  private String regionName;
  private ClientCache clientCache;

```

### FieldMayBeFinal
Field `locatorPort` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java
  private String locatorHost;
  @SuppressWarnings("unused")
  private int locatorPort;
  @SuppressWarnings("unused")
  private String[] regionNames;
```

### FieldMayBeFinal
Field `pdxAutoSerializerPackageExp` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java
  private String[] regionNames;
  @SuppressWarnings("unused")
  private String pdxAutoSerializerPackageExp;
  @SuppressWarnings("unused")
  private ClientCache clientCache;
```

### FieldMayBeFinal
Field `clientCache` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java
  private String pdxAutoSerializerPackageExp;
  @SuppressWarnings("unused")
  private ClientCache clientCache;
  private ImmutableMap<String, Table> tableMap;

```

### FieldMayBeFinal
Field `regionNames` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java
  private int locatorPort;
  @SuppressWarnings("unused")
  private String[] regionNames;
  @SuppressWarnings("unused")
  private String pdxAutoSerializerPackageExp;
```

### FieldMayBeFinal
Field `locatorHost` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java

  @SuppressWarnings("unused")
  private String locatorHost;
  @SuppressWarnings("unused")
  private int locatorPort;
```

### FieldMayBeFinal
Field `clientCache` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleEnumerator.java`
#### Snippet
```java
  private E current;
  @SuppressWarnings("unused")
  private ClientCache clientCache;

  protected GeodeSimpleEnumerator(ClientCache clientCache, String regionName) {
```

### FieldMayBeFinal
Field `rexBuilder` may be 'final'
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java

    @SuppressWarnings("unused")
    private RexBuilder rexBuilder;

    Translator(RelDataType rowType, RexBuilder rexBuilder) {
```

### FieldMayBeFinal
Field `jedis` may be 'final'
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisDataProcess.java`
#### Snippet
```java
  RedisDataFormat redisDataFormat = null;
  List<LinkedHashMap<String, Object>> fields;
  private Jedis jedis;
  private final ObjectMapper objectMapper = new ObjectMapper();

```

### FieldMayBeFinal
Field `intervals` may be 'final'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    private String dataSource;

    private List<Interval> intervals;

    private DruidJsonFilter jsonFilter;
```

### FieldMayBeFinal
Field `jsonFilter` may be 'final'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    private List<Interval> intervals;

    private DruidJsonFilter jsonFilter;

    private List<VirtualColumn> virtualColumnList;
```

### FieldMayBeFinal
Field `columns` may be 'final'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    private List<VirtualColumn> virtualColumnList;

    private List<String> columns;

    private Integer fetchLimit;
```

### FieldMayBeFinal
Field `dataSource` may be 'final'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
  private static class ScanQuery {

    private String dataSource;

    private List<Interval> intervals;
```

### FieldMayBeFinal
Field `virtualColumnList` may be 'final'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    private DruidJsonFilter jsonFilter;

    private List<VirtualColumn> virtualColumnList;

    private List<String> columns;
```

### FieldMayBeFinal
Field `fetchLimit` may be 'final'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    private List<String> columns;

    private Integer fetchLimit;

    ScanQuery(String dataSource, List<Interval> intervals,
```

### FieldMayBeFinal
Field `enumerator` may be 'final'
in `linq4j/src/main/java/org/apache/calcite/linq4j/LookupImpl.java`
#### Snippet
```java
  @Override public Enumerator<Grouping<K, V>> enumerator() {
    return new Enumerator<Grouping<K, V>>() {
      Enumerator<Entry<K, List<V>>> enumerator =
          Linq4j.enumerator(map.entrySet());

```

### FieldMayBeFinal
Field `sourceEnumerator` may be 'final'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
      @Override public Enumerator<TResult> enumerator() {
        return new Enumerator<TResult>() {
          Enumerator<TSource> sourceEnumerator = source.enumerator();
          Enumerator<TCollection> collectionEnumerator = Linq4j.emptyEnumerator();
          Enumerator<TResult> resultEnumerator = Linq4j.emptyEnumerator();
```

### FieldMayBeFinal
Field `enumerator` may be 'final'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    private boolean isLastMoveNextFalse;
    private @Nullable TAccumulate curAccumulator;
    private Enumerator<TSource> enumerator;
    private @Nullable TResult curResult;

```

### FieldMayBeFinal
Field `sourceEnumerator` may be 'final'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
      @Override public Enumerator<TResult> enumerator() {
        return new Enumerator<TResult>() {
          Enumerator<TSource> sourceEnumerator = source.enumerator();
          Enumerator<TResult> resultEnumerator = Linq4j.emptyEnumerator();

```

### FieldMayBeFinal
Field `sourceEnumerator` may be 'final'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
        return new Enumerator<TResult>() {
          int index = -1;
          Enumerator<TSource> sourceEnumerator = source.enumerator();
          Enumerator<TResult> resultEnumerator = Linq4j.emptyEnumerator();

```

### FieldMayBeFinal
Field `sourceEnumerator` may be 'final'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
        return new Enumerator<TResult>() {
          int index = -1;
          Enumerator<TSource> sourceEnumerator = source.enumerator();
          Enumerator<TCollection> collectionEnumerator = Linq4j.emptyEnumerator();
          Enumerator<TResult> resultEnumerator = Linq4j.emptyEnumerator();
```

### FieldMayBeFinal
Field `outerEnumerator` may be 'final'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
      @Override public Enumerator<TResult> enumerator() {
        return new Enumerator<TResult>() {
          private Enumerator<TSource> outerEnumerator = outer.enumerator();
          private @Nullable Enumerator<TInner> innerEnumerator = null;
          private boolean outerMatch = false; // whether the outerValue has matched an innerValue
```

### FieldMayBeFinal
Field `name` may be 'final'
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
  public static class MockSchema {
    private final List<String> tableNames = new ArrayList<>();
    private String name;

    public MockSchema(String name) {
```

### FieldMayBeFinal
Field `items` may be 'final'
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java

      AbstractTable t = new AbstractQueryableTable(Integer.class) {
        List<Integer> items = ImmutableList.of();
        final Enumerable<Integer> enumerable = Linq4j.asEnumerable(items);

```

## RuleId[id=UnnecessaryUnicodeEscape]
### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03B1` can be replaced with 'α'
in `core/src/main/java/org/apache/calcite/util/ConversionUtil.java`
#### Snippet
```java
   */
  public static final String TEST_UNICODE_STRING =
      "\u03B1\u03BD\u03B8\u03C1\u03C9\u03C0\u03BF\u03C2";

  /**
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03BD` can be replaced with 'ν'
in `core/src/main/java/org/apache/calcite/util/ConversionUtil.java`
#### Snippet
```java
   */
  public static final String TEST_UNICODE_STRING =
      "\u03B1\u03BD\u03B8\u03C1\u03C9\u03C0\u03BF\u03C2";

  /**
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03B8` can be replaced with 'θ'
in `core/src/main/java/org/apache/calcite/util/ConversionUtil.java`
#### Snippet
```java
   */
  public static final String TEST_UNICODE_STRING =
      "\u03B1\u03BD\u03B8\u03C1\u03C9\u03C0\u03BF\u03C2";

  /**
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03C1` can be replaced with 'ρ'
in `core/src/main/java/org/apache/calcite/util/ConversionUtil.java`
#### Snippet
```java
   */
  public static final String TEST_UNICODE_STRING =
      "\u03B1\u03BD\u03B8\u03C1\u03C9\u03C0\u03BF\u03C2";

  /**
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03C9` can be replaced with 'ω'
in `core/src/main/java/org/apache/calcite/util/ConversionUtil.java`
#### Snippet
```java
   */
  public static final String TEST_UNICODE_STRING =
      "\u03B1\u03BD\u03B8\u03C1\u03C9\u03C0\u03BF\u03C2";

  /**
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03C0` can be replaced with 'π'
in `core/src/main/java/org/apache/calcite/util/ConversionUtil.java`
#### Snippet
```java
   */
  public static final String TEST_UNICODE_STRING =
      "\u03B1\u03BD\u03B8\u03C1\u03C9\u03C0\u03BF\u03C2";

  /**
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03BF` can be replaced with 'ο'
in `core/src/main/java/org/apache/calcite/util/ConversionUtil.java`
#### Snippet
```java
   */
  public static final String TEST_UNICODE_STRING =
      "\u03B1\u03BD\u03B8\u03C1\u03C9\u03C0\u03BF\u03C2";

  /**
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03C2` can be replaced with 'ς'
in `core/src/main/java/org/apache/calcite/util/ConversionUtil.java`
#### Snippet
```java
   */
  public static final String TEST_UNICODE_STRING =
      "\u03B1\u03BD\u03B8\u03C1\u03C9\u03C0\u03BF\u03C2";

  /**
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u221e` can be replaced with '∞'
in `core/src/main/java/org/apache/calcite/util/RangeSets.java`
#### Snippet
```java
      sb.append('[');
      valuePrinter.accept(sb, lower);
      sb.append("..+\u221e)");
    }

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u221e` can be replaced with '∞'
in `core/src/main/java/org/apache/calcite/util/RangeSets.java`
#### Snippet
```java

    @Override public void all() {
      sb.append("(-\u221e..+\u221e)");
    }

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u221e` can be replaced with '∞'
in `core/src/main/java/org/apache/calcite/util/RangeSets.java`
#### Snippet
```java

    @Override public void all() {
      sb.append("(-\u221e..+\u221e)");
    }

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u221e` can be replaced with '∞'
in `core/src/main/java/org/apache/calcite/util/RangeSets.java`
#### Snippet
```java
      sb.append('(');
      valuePrinter.accept(sb, lower);
      sb.append("..+\u221e)");
    }

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u221e` can be replaced with '∞'
in `core/src/main/java/org/apache/calcite/util/RangeSets.java`
#### Snippet
```java

    @Override public void lessThan(C upper) {
      sb.append("(-\u221e..");
      valuePrinter.accept(sb, upper);
      sb.append(")");
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u221e` can be replaced with '∞'
in `core/src/main/java/org/apache/calcite/util/RangeSets.java`
#### Snippet
```java

    @Override public void atMost(C upper) {
      sb.append("(-\u221e..");
      valuePrinter.accept(sb, upper);
      sb.append("]");
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u2025` can be replaced with '‥'
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java
  public static Matcher<RangeSet> isRangeSet(final String value) {
    return compose(Is.is(value), input -> {
      // Change all '\u2025' (a unicode symbol denoting a range) to '..',
      // consistent with Guava 29+.
      return input.toString().replace("\u2025", "..");
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u2025` can be replaced with '‥'
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java
      // Change all '\u2025' (a unicode symbol denoting a range) to '..',
      // consistent with Guava 29+.
      return input.toString().replace("\u2025", "..");
    });
  }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5B57` can be replaced with '字'
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.checkScalarExact("ASCII('ABC')", 65);
    f.checkScalarExact("ASCII(_UTF8'\u0082')", 130);
    f.checkScalarExact("ASCII(_UTF8'\u5B57')", 23383);
    f.checkScalarExact("ASCII(_UTF8'\u03a9')", 937); // omega
    f.checkNull("ASCII(cast(null as varchar(1)))");
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u03a9` can be replaced with 'Ω'
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.checkScalarExact("ASCII(_UTF8'\u0082')", 130);
    f.checkScalarExact("ASCII(_UTF8'\u5B57')", 23383);
    f.checkScalarExact("ASCII(_UTF8'\u03a9')", 937); // omega
    f.checkNull("ASCII(cast(null as varchar(1)))");
  }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u4F60` can be replaced with '你'
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      f.checkString("reverse('Hello World')", "dlroW olleH",
          "VARCHAR(11) NOT NULL");
      f.checkString("reverse(_UTF8'\u4F60\u597D')", "\u597D\u4F60",
          "VARCHAR(2) NOT NULL");
      f.checkNull("reverse(cast(null as varchar(1)))");
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u597D` can be replaced with '好'
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      f.checkString("reverse('Hello World')", "dlroW olleH",
          "VARCHAR(11) NOT NULL");
      f.checkString("reverse(_UTF8'\u4F60\u597D')", "\u597D\u4F60",
          "VARCHAR(2) NOT NULL");
      f.checkNull("reverse(cast(null as varchar(1)))");
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u597D` can be replaced with '好'
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      f.checkString("reverse('Hello World')", "dlroW olleH",
          "VARCHAR(11) NOT NULL");
      f.checkString("reverse(_UTF8'\u4F60\u597D')", "\u597D\u4F60",
          "VARCHAR(2) NOT NULL");
      f.checkNull("reverse(cast(null as varchar(1)))");
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u4F60` can be replaced with '你'
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      f.checkString("reverse('Hello World')", "dlroW olleH",
          "VARCHAR(11) NOT NULL");
      f.checkString("reverse(_UTF8'\u4F60\u597D')", "\u597D\u4F60",
          "VARCHAR(2) NOT NULL");
      f.checkNull("reverse(cast(null as varchar(1)))");
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5B57` can be replaced with '字'
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      f.checkString("SOUNDEX('mile')", "M400", "VARCHAR(4) NOT NULL");
      f.checkNull("SOUNDEX(cast(null as varchar(1)))");
      f.checkFails("SOUNDEX(_UTF8'\u5B57\u5B57')", "The character is not mapped.*", true);
    };
    f0.forEachLibrary(libraries, consumer);
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5B57` can be replaced with '字'
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      f.checkString("SOUNDEX('mile')", "M400", "VARCHAR(4) NOT NULL");
      f.checkNull("SOUNDEX(cast(null as varchar(1)))");
      f.checkFails("SOUNDEX(_UTF8'\u5B57\u5B57')", "The character is not mapped.*", true);
    };
    f0.forEachLibrary(libraries, consumer);
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `nameIterable` is redundant
in `core/src/main/java/org/apache/calcite/Demo.java`
#### Snippet
```java
    names.add("Ringo");

    Iterable<String> nameIterable = names;

    for (String name : nameIterable) {
```

### UnnecessaryLocalVariable
Local variable `reducedNode` is redundant
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateReduceFunctionsRule.java`
#### Snippet
```java
      // replace original MAX/MIN/AVG/ANY_VALUE/FIRST_VALUE/LAST_VALUE(x) with
      // target field of x, when x exists in group
      final RexNode reducedNode = reduceAggCallByGrouping(oldAggRel, oldCall);
      return reducedNode;
    } else if (canReduce(oldCall)) {
```

### UnnecessaryLocalVariable
Local variable `program` is redundant
in `core/src/main/java/org/apache/calcite/rel/rules/CalcRelSplitter.java`
#### Snippet
```java
          RexUtil.createStructType(typeFactory, projectRefs, fieldNames, null);
    }
    final RexProgram program =
        new RexProgram(
            inputRowType, exprs, projectRefs, conditionRef, outputRowType);
```

### UnnecessaryLocalVariable
Local variable `dCpu` is redundant
in `core/src/main/java/org/apache/calcite/rel/convert/ConverterImpl.java`
#### Snippet
```java
      RelMetadataQuery mq) {
    double dRows = mq.getRowCount(getInput());
    double dCpu = dRows;
    double dIo = 0;
    return planner.getCostFactory().makeCost(dRows, dCpu, dIo);
```

### UnnecessaryLocalVariable
Local variable `origin` is redundant
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQuery.java`
#### Snippet
```java
      return null;
    }
    final RelColumnOrigin origin = Iterables.getOnlyElement(origins);
    return origin;
  }
```

### UnnecessaryLocalVariable
Local variable `andBoolNode` is redundant
in `core/src/main/java/org/apache/calcite/rex/RexTransformer.java`
#### Snippet
```java
                  operand,
                  boolNode);
          RexNode andBoolNode =
              rexBuilder.makeCall(
                  SqlStdOperatorTable.AND,
```

### UnnecessaryLocalVariable
Local variable `andBoolNode` is redundant
in `core/src/main/java/org/apache/calcite/rex/RexTransformer.java`
#### Snippet
```java
              rexBuilder.makeLiteral(
                  directlyUnderIs.booleanValue());
          RexNode andBoolNode =
              rexBuilder.makeCall(
                  SqlStdOperatorTable.EQUALS,
```

### UnnecessaryLocalVariable
Local variable `andNullAndCheckNode` is redundant
in `core/src/main/java/org/apache/calcite/rex/RexTransformer.java`
#### Snippet
```java

        if (null != intoFinalAnd) {
          RexNode andNullAndCheckNode =
              rexBuilder.makeCall(
                  SqlStdOperatorTable.AND,
```

### UnnecessaryLocalVariable
Local variable `ref` is redundant
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
      // Return a reference to the N'th expression, which should be
      // equivalent.
      final RexLocalRef ref = localRefList.get(index);
      return ref;
    }
```

### UnnecessaryLocalVariable
Local variable `over` is redundant
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
            upperBound,
            rows);
    final RexOver over =
        new RexOver(type, operator, exprs, window, distinct, ignoreNulls);
    RexNode result = over;
```

### UnnecessaryLocalVariable
Local variable `c` is redundant
in `core/src/main/java/org/apache/calcite/sql/SqlDialects.java`
#### Snippet
```java
    final Casing quotedCasing = getCasing(databaseMetaData, true);
    final boolean caseSensitive = isCaseSensitive(databaseMetaData);
    final SqlDialect.Context c = SqlDialect.EMPTY_CONTEXT
        .withDatabaseProductName(databaseProductName)
        .withDatabaseMajorVersion(databaseMajorVersion)
```

### UnnecessaryLocalVariable
Local variable `operandScope` is redundant
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
  @Override public void validate(SqlValidator validator,
      SqlValidatorScope scope) {
    SqlValidatorScope operandScope = scope; // REVIEW

    @SuppressWarnings("unused")
```

### UnnecessaryLocalVariable
Local variable `currentOffset` is redundant
in `core/src/main/java/org/apache/calcite/jdbc/CalciteConnectionImpl.java`
#### Snippet
```java
              .deriveTimeFrameSet(TimeFrames.CORE);
      final long localOffset = timeZone.getOffset(time);
      final long currentOffset = localOffset;
      final String user = "sa";
      final String systemUser = System.getProperty("user.name");
```

### UnnecessaryLocalVariable
Local variable `node` is redundant
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
    }
    final MutableRel node0 = unifyResult.result;
    MutableRel node = node0; // replaceAncestors(node0);
    if (DEBUG) {
      System.out.println("Convert: query:\n"
```

### UnnecessaryLocalVariable
Local variable `compenCond` is redundant
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      // MutableJoin matches only when the conditions are analyzed to be same.
      if (splitted != null && splitted.isAlwaysTrue()) {
        final RexNode compenCond = qInput0Cond;
        final List<RexNode> compenProjs = new ArrayList<>();
        for (int i = 0; i < fieldCnt(query); i++) {
```

### UnnecessaryLocalVariable
Local variable `i` is redundant
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
     */
    public void defineEscape(char from, String to) {
      int i = (int) from;
      List<@Nullable String> translationVector =
          requireNonNull(this.translationVector, "translationVector");
```

### UnnecessaryLocalVariable
Local variable `finalNode` is redundant
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

        assert qualifiedNode.size() == 2;
        final SqlNode finalNode =
            SqlStdOperatorTable.AS.createCall(SqlParserPos.ZERO,
                SqlStdOperatorTable.COALESCE.createCall(SqlParserPos.ZERO,
```

### UnnecessaryLocalVariable
Local variable `s1` is redundant
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
    String s3 = Util.replace(s4, "\\n", "\n");
    String s2 = Util.replace(s3, "\\\"", "\"");
    String s1 = Util.replace(s2, "\\\\", "\\");
    return s1;
  }
```

### UnnecessaryLocalVariable
Local variable `longs` is redundant
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
      case OBJECT:
      case JAVA_SQL_TIMESTAMP:
        final List<@Nullable Long> longs =
            Util.transform((List<@Nullable Timestamp>) list,
                (Timestamp t) -> t == null ? null : t.getTime());
```

### UnnecessaryLocalVariable
Local variable `mapping` is redundant
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableMergeJoin.java`
#### Snippet
```java
    }

    Mappings.TargetMapping mapping =
        Mappings.target(keyMap,
            (left2Right ? left : right).getRowType().getFieldCount(),
```

### UnnecessaryLocalVariable
Local variable `minified` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeUtils.java`
#### Snippet
```java
    GMLWriter gmlWriter = new GMLWriter();
    // remove line breaks and indentation
    String minified = gmlWriter.write(geometry)
        .replace("\n", "")
        .replace("  ", "");
```

### UnnecessaryLocalVariable
Local variable `g` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  @Hints({"SqlKind:ST_POINT3"})
  public static Geometry ST_Point(BigDecimal x, BigDecimal y, BigDecimal z) {
    final Geometry g =
        GEOMETRY_FACTORY.createPoint(
            new Coordinate(x.doubleValue(), y.doubleValue(), z.doubleValue()));
```

### UnnecessaryLocalVariable
Local variable `translated` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
    AffineTransformation transformation = new AffineTransformation();
    transformation.translate(x.doubleValue(), y.doubleValue());
    Geometry translated = transformation.transform(geom);
    return translated;
  }
```

### UnnecessaryLocalVariable
Local variable `polygon` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java

            LinearRing linearRing = GEOMETRY_FACTORY.createLinearRing(coordinates);
            Polygon polygon = GEOMETRY_FACTORY.createPolygon(linearRing);

            geom = polygon;
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
    BufferParameters params = style.asBufferParameters();
    double sidedDistance = style.asSidedDistance(distance);
    Geometry result = new BufferOp(geom, params).getResultGeometry(sidedDistance);
    return result;
  }
```

### UnnecessaryLocalVariable
Local variable `newRef` is redundant
in `core/src/main/java/org/apache/calcite/sql2rel/RelStructuredTypeFlattener.java`
#### Snippet
```java
          int newInnerOrdinal = getNewInnerOrdinal(firstOp, literalString);
          Ord<RelDataType> newField = getNewFieldForOldInput(oldOrdinal, newInnerOrdinal);
          RexInputRef newRef = rexBuilder.makeInputRef(newField.e, newField.i);
          return newRef;
        } else {
```

### UnnecessaryLocalVariable
Local variable `newRef` is redundant
in `core/src/main/java/org/apache/calcite/sql2rel/RelStructuredTypeFlattener.java`
#### Snippet
```java
                + getNewInnerOrdinal(firstOp, literalString);
            RelDataTypeField newField = getNewInputFieldByNewOrdinal(newRefOrdinal);
            RexInputRef newRef = rexBuilder.makeInputRef(newField.getType(), newRefOrdinal);
            return newRef;
          }
```

### UnnecessaryLocalVariable
Local variable `cardinality` is redundant
in `core/src/main/java/org/apache/calcite/materialize/ProfilerLatticeStatisticProvider.java`
#### Snippet
```java
  @Override public double cardinality(List<Lattice.Column> columns) {
    final ImmutableBitSet build = Lattice.Column.toBitSet(columns);
    final double cardinality = profile.get().cardinality(build);
//    System.out.println(columns + ": " + cardinality);
    return cardinality;
```

### UnnecessaryLocalVariable
Local variable `groupCount` is redundant
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
      nullIndicatorPos = joinOutputProjExprCount - 1;

      final int groupCount = leftInputFieldCount;

      List<AggregateCall> newAggCalls = new ArrayList<>();
```

### UnnecessaryLocalVariable
Local variable `map` is redundant
in `plus/src/main/java/org/apache/calcite/adapter/tpch/TpchSchemaFactory.java`
#### Snippet
```java
  @Override public Schema create(SchemaPlus parentSchema, String name,
      Map<String, Object> operand) {
    Map map = (Map) operand;
    double scale = Util.first((Double) map.get("scale"), 1D);
    int part = Util.first((Integer) map.get("part"), 1);
```

### UnnecessaryLocalVariable
Local variable `map` is redundant
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSchemaFactory.java`
#### Snippet
```java
  @Override public synchronized Schema create(SchemaPlus parentSchema, String name,
      Map<String, Object> operand) {
    Map map = (Map) operand;
    String locatorHost = (String) map.get(LOCATOR_HOST);
    int locatorPort = Integer.valueOf((String) map.get(LOCATOR_PORT));
```

### UnnecessaryLocalVariable
Local variable `map` is redundant
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchemaFactory.java`
#### Snippet
```java
  @Override public Schema create(SchemaPlus parentSchema,
      String name, Map<String, Object> operand) {
    Map map = (Map) operand;

    String locatorHost = (String) map.get(LOCATOR_HOST);
```

### UnnecessaryLocalVariable
Local variable `r1` is redundant
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/VisitorImpl.java`
#### Snippet
```java
  @Override public R visit(BinaryExpression binaryExpression) {
    R r0 = binaryExpression.expression0.accept(this);
    R r1 = binaryExpression.expression1.accept(this);
    return r1;
  }
```

### UnnecessaryLocalVariable
Local variable `map` is redundant
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSchemaFactory.java`
#### Snippet
```java
      Map<String, Object> operand) {

    final Map map = (Map) operand;

    final ObjectMapper mapper = new ObjectMapper();
```

## RuleId[id=UnpredictableBigDecimalConstructorCall]
### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
        return o;
      }
      return new BigDecimal(((Number) o).doubleValue(), MathContext.DECIMAL32)
          .stripTrailingZeros();
    case REAL:
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
        return o;
      }
      return new BigDecimal(((Number) o).doubleValue(), MathContext.DECIMAL64)
          .stripTrailingZeros();
    case CHAR:
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `core/src/main/java/org/apache/calcite/rex/RexInterpreter.java`
#### Snippet
```java
                || comparable instanceof Short
        ? new BigDecimal(((Number) comparable).longValue())
        : new BigDecimal(((Number) comparable).doubleValue());
  }

```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `core/src/main/java/org/apache/calcite/profile/Profiler.java`
#### Snippet
```java
      map.put("expectedCardinality",
          new BigDecimal(expectedCardinality, ROUND5));
      map.put("surprise", new BigDecimal(surprise(), ROUND3));
      return map;
    }
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
        : number instanceof BigInteger ? new BigDecimal((BigInteger) number)
        : number instanceof Long ? new BigDecimal(number.longValue())
        : new BigDecimal(number.doubleValue());
  }

```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `file/src/main/java/org/apache/calcite/adapter/file/CsvEnumerator.java`
#### Snippet
```java
          if (reader instanceof CsvStreamReader) {
            try {
              Thread.sleep(CsvStreamReader.DEFAULT_MONITOR_DELAY);
            } catch (InterruptedException e) {
              throw new RuntimeException(e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
          if ((cal.get(Calendar.HOUR_OF_DAY) == 23)
              && (cal.get(Calendar.MINUTE) >= 58)) {
            Thread.sleep(10 * 1000);
            continue;
          }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
          // increments until calendar moves into the next next day.
          if (cal.get(Calendar.MINUTE) >= 58) {
            Thread.sleep(10 * 1000);
            continue;
          }
```

## RuleId[id=ArraysAsListWithZeroOrOneArgument]
### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
        // For instance, row_number does not use for loop to compute the value
        Statement forAggLoop =
            Expressions.for_(Arrays.asList(jDecl),
                Expressions.lessThanOrEqual(jDecl.parameter, endX),
                Expressions.preIncrementAssign(jDecl.parameter),
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
          : Collections.singletonList(elements[0]);
      for (Object row : bag) {
        tupleList.add(tupleFactory.newTuple(Arrays.asList(row)));
      }
    }
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
        typeFactory.builder().add("c", SqlTypeName.VARCHAR, 100).build(),
        "values (" + CalciteSqlDialect.DEFAULT.quoteStringLiteral(s) + ")",
        ImmutableList.of(), Arrays.asList("view"));
  }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
        "values " + CalciteSqlDialect.DEFAULT.quoteStringLiteral(o.toString())
            + ", " + CalciteSqlDialect.DEFAULT.quoteStringLiteral(p.toString()),
        ImmutableList.of(), Arrays.asList("view"));
  }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    return new ViewTable(Object.class, typeFactory ->
        typeFactory.builder().add("c", SqlTypeName.INTEGER).build(),
        "values (1), (3), " + s, ImmutableList.of(), Arrays.asList("view"));
  }

```

## RuleId[id=IntegerDivisionInFloatingPointContext]
### IntegerDivisionInFloatingPointContext
`x * y / 100`: integer division in floating-point context
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
  public static class MyIncrement {
    public float eval(int x, int y) {
      return x + x * y / 100;
    }
  }
```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`false && !Util.isDistinct(Util.transform(exps, RexNode::toString))` can be simplified to 'false'
in `core/src/main/java/org/apache/calcite/rel/core/Project.java`
#### Snippet
```java
    }
    //CHECKSTYLE: IGNORE 1
    if (false && !Util.isDistinct(Util.transform(exps, RexNode::toString))) {
      // Projecting the same expression twice is usually a bad idea,
      // because it may create expressions downstream which are equivalent
```

### PointlessBooleanExpression
`false && RexUtil.containNoCommonExprs(exprs, litmus)` can be simplified to 'false'
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
    // todo: enable
    // CHECKSTYLE: IGNORE 1
    if (false && RexUtil.containNoCommonExprs(exprs, litmus)) {
      return litmus.fail(null);
    }
```

### PointlessBooleanExpression
`false && type != null && !type.isAssignableFrom(Schema.class)` can be simplified to 'false'
in `core/src/main/java/org/apache/calcite/schema/Schemas.java`
#### Snippet
```java
    //CHECKSTYLE: IGNORE 2
    //noinspection unchecked
    if (false && type != null && !type.isAssignableFrom(Schema.class)) {
      return unwrap(call, type);
    }
```

### PointlessBooleanExpression
`containsUsedDefinedFunction |= true` can be simplified to 'containsUsedDefinedFunction=true'
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcRules.java`
#### Snippet
```java
      if (operator instanceof SqlFunction
          && ((SqlFunction) operator).getFunctionType().isUserDefined()) {
        containsUsedDefinedFunction |= true;
      }
      return super.visitCall(call);
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnOrigins.java`
#### Snippet
```java
    final List<RexNode> projects = new ArrayList<>();
    for (RexNode rex: rexShuttle.apply(rel.getProgram().getProjectList())) {
      projects.add(rex);
    }
    final RexNode rexNode = projects.get(iOutputColumn);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
    SqlNodeList ret = new SqlNodeList(SqlParserPos.ZERO);
    for (SqlNode node : operands) {
      ret.add(node);
    }
    return ret;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
    final List<RexNode> projects = new ArrayList<>();
    for (RexNode rex: shuttle.apply(calc.program.getProjectList())) {
      projects.add(rex);
    }
    if (condition == null) {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
        if (obj instanceof Map) {
          for (Object value : ((LinkedHashMap) obj).values()) {
            q.add(value);
          }
        } else if (obj instanceof Collection) {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
        } else if (obj instanceof Collection) {
          for (Object value : (Collection) obj) {
            q.add(value);
          }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
            (SqlNodeList) call.getOperandList().get(1);
        list.add(aggCall);
        distinctList.getList().forEach(this.distinctList::add);
        return null;
      }
```

## RuleId[id=UnstableApiUsage]
### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
    private final RexBuilder rexBuilder;
    private final TimeUnitRange timeUnit;
    private final Map<RexNode, RangeSet<Calendar>> operandRanges;
    private final Deque<RexCall> calls = new ArrayDeque<>();
    private final ImmutableSortedSet<TimeUnitRange> timeUnitRanges;
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
    RexNode compareExtract(SqlKind comparison, RexNode operand,
        RexLiteral literal) {
      RangeSet<Calendar> rangeSet = operandRanges.get(operand);
      if (rangeSet == null) {
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      RangeSet<Calendar> rangeSet = operandRanges.get(operand);
      if (rangeSet == null) {
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
```

### UnstableApiUsage
'of()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      RangeSet<Calendar> rangeSet = operandRanges.get(operand);
      if (rangeSet == null) {
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
```

### UnstableApiUsage
'complement()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      RangeSet<Calendar> rangeSet = operandRanges.get(operand);
      if (rangeSet == null) {
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
      // Calendar.MONTH is 0-based
      final int v = RexLiteral.intValue(literal)
```

### UnstableApiUsage
'com.google.common.collect.TreeRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
      // Calendar.MONTH is 0-based
      final int v = RexLiteral.intValue(literal)
```

### UnstableApiUsage
'create()' is declared in unstable class 'com.google.common.collect.TreeRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
      // Calendar.MONTH is 0-based
      final int v = RexLiteral.intValue(literal)
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      }

      for (Range<Calendar> r : rangeSet.asRanges()) {
        final Calendar c;
        switch (timeUnit) {
```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
          c.clear();
          c.set(v, Calendar.JANUARY, 1);
          s2.add(extractRange(timeUnit, comparison, c));
          break;
        case MONTH:
```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
            int i = 0;
            while (next(c, timeUnit, v, r, i++ > 0)) {
              s2.add(extractRange(timeUnit, comparison, c));
            }
          }
```

### UnstableApiUsage
'removeAll(com.google.common.collect.RangeSet)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      }
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      final List<RexNode> nodes = new ArrayList<>();
```

### UnstableApiUsage
'complement()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      }
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      final List<RexNode> nodes = new ArrayList<>();
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      final List<RexNode> nodes = new ArrayList<>();
      for (Range<Calendar> r : s2.asRanges()) {
```

### UnstableApiUsage
'copyOf(com.google.common.collect.RangeSet)' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      final List<RexNode> nodes = new ArrayList<>();
      for (Range<Calendar> r : s2.asRanges()) {
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      final List<RexNode> nodes = new ArrayList<>();
      for (Range<Calendar> r : s2.asRanges()) {
        nodes.add(toRex(operand, r));
      }
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
    private RexNode compareFloorCeil(SqlKind comparison, RexNode operand,
        RexLiteral timeLiteral, TimeUnitRange timeUnit, boolean floor) {
      RangeSet<Calendar> rangeSet = operandRanges.get(operand);
      if (rangeSet == null) {
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      RangeSet<Calendar> rangeSet = operandRanges.get(operand);
      if (rangeSet == null) {
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
```

### UnstableApiUsage
'of()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      RangeSet<Calendar> rangeSet = operandRanges.get(operand);
      if (rangeSet == null) {
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
```

### UnstableApiUsage
'complement()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      RangeSet<Calendar> rangeSet = operandRanges.get(operand);
      if (rangeSet == null) {
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
      final Calendar c = timestampValue(timeLiteral);
      final Range<Calendar> range = floor
```

### UnstableApiUsage
'com.google.common.collect.TreeRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
      final Calendar c = timestampValue(timeLiteral);
      final Range<Calendar> range = floor
```

### UnstableApiUsage
'create()' is declared in unstable class 'com.google.common.collect.TreeRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        rangeSet = ImmutableRangeSet.<Calendar>of().complement();
      }
      final RangeSet<Calendar> s2 = TreeRangeSet.create();
      final Calendar c = timestampValue(timeLiteral);
      final Range<Calendar> range = floor
```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
          ? floorRange(timeUnit, comparison, c)
          : ceilRange(timeUnit, comparison, c);
      s2.add(range);
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
```

### UnstableApiUsage
'removeAll(com.google.common.collect.RangeSet)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      s2.add(range);
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      if (range.isEmpty()) {
```

### UnstableApiUsage
'complement()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      s2.add(range);
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      if (range.isEmpty()) {
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      if (range.isEmpty()) {
        return rexBuilder.makeLiteral(false);
```

### UnstableApiUsage
'copyOf(com.google.common.collect.RangeSet)' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      // Intersect old range set with new.
      s2.removeAll(rangeSet.complement());
      operandRanges.put(operand, ImmutableRangeSet.copyOf(s2));
      if (range.isEmpty()) {
        return rexBuilder.makeLiteral(false);
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
    @VisibleForTesting
    ExtractShuttle(RexBuilder rexBuilder, TimeUnitRange timeUnit,
        Map<RexNode, RangeSet<Calendar>> operandRanges,
        ImmutableSortedSet<TimeUnitRange> timeUnitRanges, String timeZone) {
      this.rexBuilder = requireNonNull(rexBuilder, "rexBuilder");
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
          .addAll(timeUnits).add(TimeUnitRange.YEAR).build();
    }
    final Map<RexNode, RangeSet<Calendar>> operandRanges = new HashMap<>();
    for (TimeUnitRange timeUnit : timeUnits) {
      e =
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        return true;
      }
      final RangeSet<Calendar> calendarRangeSet = operandRanges.get(operand);
      if (calendarRangeSet == null || calendarRangeSet.isEmpty()) {
        return false;
```

### UnstableApiUsage
'isEmpty()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
      }
      final RangeSet<Calendar> calendarRangeSet = operandRanges.get(operand);
      if (calendarRangeSet == null || calendarRangeSet.isEmpty()) {
        return false;
      }
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        return false;
      }
      for (Range<Calendar> range : calendarRangeSet.asRanges()) {
        // Cannot reWrite if range does not have an upper or lower bound
        if (!range.hasUpperBound() || !range.hasLowerBound()) {
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
          return (List<RexNode>) exprs;
        }
        final Map<RexNode, RangeSet<Calendar>> save =
            ImmutableMap.copyOf(operandRanges);
        final ImmutableList.Builder<RexNode> clonedOperands =
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
    private <C extends Comparable<C>> SqlNode toIn(SqlNode operandSql,
        SqlBinaryOperator eqOp, SqlBinaryOperator inOp,
        @Nullable RexProgram program, RelDataType type, RangeSet<C> rangeSet) {
      final SqlNodeList list = rangeSet.asRanges().stream()
          .map(range ->
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
        SqlBinaryOperator eqOp, SqlBinaryOperator inOp,
        @Nullable RexProgram program, RelDataType type, RangeSet<C> rangeSet) {
      final SqlNodeList list = rangeSet.asRanges().stream()
          .map(range ->
              toSql(program,
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
      final Sarg sarg =
          Sarg.of(RexUnknownAs.UNKNOWN,
              ImmutableRangeSet.<Comparable>of(
                  Range.closed(lowerValue, upperValue)));
      List<RelDataType> types = ImmutableList.of(lower.getType(), upper.getType());
```

### UnstableApiUsage
'of(com.google.common.collect.Range)' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
      final Sarg sarg =
          Sarg.of(RexUnknownAs.UNKNOWN,
              ImmutableRangeSet.<Comparable>of(
                  Range.closed(lowerValue, upperValue)));
      List<RelDataType> types = ImmutableList.of(lower.getType(), upper.getType());
```

### UnstableApiUsage
'org.gradle.testing.jacoco.plugins.JacocoCoverageReport' is marked unstable with @Incubating
in `build.gradle.kts`
#### Snippet
```java
    reports {
        if (enableJacoco) {
            val jacocoAggregateTestReport by creating(JacocoCoverageReport::class) {
                testType.set(TestSuiteType.UNIT_TEST)
            }
```

### UnstableApiUsage
'getTestType()' is declared in unstable interface 'org.gradle.testing.jacoco.plugins.JacocoCoverageReport' marked with @Incubating
in `build.gradle.kts`
#### Snippet
```java
        if (enableJacoco) {
            val jacocoAggregateTestReport by creating(JacocoCoverageReport::class) {
                testType.set(TestSuiteType.UNIT_TEST)
            }
        }
```

### UnstableApiUsage
'org.gradle.api.attributes.TestSuiteType' is marked unstable with @Incubating
in `build.gradle.kts`
#### Snippet
```java
        if (enableJacoco) {
            val jacocoAggregateTestReport by creating(JacocoCoverageReport::class) {
                testType.set(TestSuiteType.UNIT_TEST)
            }
        }
```

### UnstableApiUsage
'UNIT_TEST' is declared in unstable interface 'org.gradle.api.attributes.TestSuiteType' marked with @Incubating
in `build.gradle.kts`
#### Snippet
```java
        if (enableJacoco) {
            val jacocoAggregateTestReport by creating(JacocoCoverageReport::class) {
                testType.set(TestSuiteType.UNIT_TEST)
            }
        }
```

### UnstableApiUsage
'consistentResolution(org.gradle.api.Action)' is marked unstable with @Incubating
in `build.gradle.kts`
#### Snippet
```java
        }
        configure<JavaPluginExtension> {
            consistentResolution {
                useCompileClasspathVersions()
            }
```

### UnstableApiUsage
'useCompileClasspathVersions()' is declared in unstable interface 'org.gradle.api.plugins.JavaResolutionConsistency' marked with @Incubating
in `build.gradle.kts`
#### Snippet
```java
        configure<JavaPluginExtension> {
            consistentResolution {
                useCompileClasspathVersions()
            }
        }
```

### UnstableApiUsage
'getXml()' is unstable because its signature references unstable interface 'com.github.spotbugs.SpotBugsXmlReport' marked with @Incubating
in `build.gradle.kts`
#### Snippet
```java
                reports {
                    html.isEnabled = reportsForHumans()
                    xml.isEnabled = !reportsForHumans()
                }
                enabled = enableSpotBugs
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
    if (sarg.isPoints()) {
      // Generate 'ref = value1 OR ... OR ref = valueN'
      sarg.rangeSet.asRanges().forEach(range ->
          orList.add(
              rexBuilder.makeCall(SqlStdOperatorTable.EQUALS, ref,
```

### UnstableApiUsage
'complement()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
    } else if (sarg.isComplementedPoints()) {
      // Generate 'ref <> value1 AND ... AND ref <> valueN'
      final List<RexNode> list = sarg.rangeSet.complement().asRanges().stream()
          .map(range ->
              rexBuilder.makeCall(SqlStdOperatorTable.NOT_EQUALS, ref,
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
    } else if (sarg.isComplementedPoints()) {
      // Generate 'ref <> value1 AND ... AND ref <> valueN'
      final List<RexNode> list = sarg.rangeSet.complement().asRanges().stream()
          .map(range ->
              rexBuilder.makeCall(SqlStdOperatorTable.NOT_EQUALS, ref,
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
   */
  @SuppressWarnings("BetaApi")
  private static <C extends Comparable<C>> RangeSet<C> residue(RexNode ref,
      RangeSet<C> r0, List<RexNode> predicates, Class<C> clazz) {
    RangeSet<C> result = r0;
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
  @SuppressWarnings("BetaApi")
  private static <C extends Comparable<C>> RangeSet<C> residue(RexNode ref,
      RangeSet<C> r0, List<RexNode> predicates, Class<C> clazz) {
    RangeSet<C> result = r0;
    for (RexNode predicate : predicates) {
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
  private static <C extends Comparable<C>> RangeSet<C> residue(RexNode ref,
      RangeSet<C> r0, List<RexNode> predicates, Class<C> clazz) {
    RangeSet<C> result = r0;
    for (RexNode predicate : predicates) {
      switch (predicate.getKind()) {
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
            // c1. We subtract the point c1 from result, which is equivalent.
            final Range<C> pointRange = range(SqlKind.EQUALS, c1);
            final RangeSet<C> notEqualsRangeSet =
                ImmutableRangeSet.of(pointRange).complement();
            if (result.enclosesAll(notEqualsRangeSet)) {
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
            final Range<C> pointRange = range(SqlKind.EQUALS, c1);
            final RangeSet<C> notEqualsRangeSet =
                ImmutableRangeSet.of(pointRange).complement();
            if (result.enclosesAll(notEqualsRangeSet)) {
              result = RangeSets.rangeSetAll();
```

### UnstableApiUsage
'of(com.google.common.collect.Range)' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
            final Range<C> pointRange = range(SqlKind.EQUALS, c1);
            final RangeSet<C> notEqualsRangeSet =
                ImmutableRangeSet.of(pointRange).complement();
            if (result.enclosesAll(notEqualsRangeSet)) {
              result = RangeSets.rangeSetAll();
```

### UnstableApiUsage
'complement()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
            final Range<C> pointRange = range(SqlKind.EQUALS, c1);
            final RangeSet<C> notEqualsRangeSet =
                ImmutableRangeSet.of(pointRange).complement();
            if (result.enclosesAll(notEqualsRangeSet)) {
              result = RangeSets.rangeSetAll();
```

### UnstableApiUsage
'enclosesAll(com.google.common.collect.RangeSet)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
            final RangeSet<C> notEqualsRangeSet =
                ImmutableRangeSet.of(pointRange).complement();
            if (result.enclosesAll(notEqualsRangeSet)) {
              result = RangeSets.rangeSetAll();
              continue;
```

### UnstableApiUsage
'encloses(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
          default:
            final Range<C> r1 = range(predicate.getKind(), c1);
            if (result.encloses(r1)) {
              // Given these predicates, term is always satisfied.
              // e.g. r0 is "$0 < 10", r1 is "$0 < 5"
```

### UnstableApiUsage
'subRangeSet(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
              continue;
            }
            result = result.subRangeSet(r1);
          }
          if (result.isEmpty()) {
```

### UnstableApiUsage
'isEmpty()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
            result = result.subRangeSet(r1);
          }
          if (result.isEmpty()) {
            break; // short-cut
          }
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java

  @SuppressWarnings("BetaApi")
  private static <C extends Comparable<C>> RangeSet<C> rangeSet(SqlKind comparison,
      C c) {
    switch (comparison) {
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    case GREATER_THAN:
    case GREATER_THAN_OR_EQUAL:
      return ImmutableRangeSet.of(range(comparison, c));
    case NOT_EQUALS:
      return ImmutableRangeSet.<C>builder()
```

### UnstableApiUsage
'of(com.google.common.collect.Range)' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    case GREATER_THAN:
    case GREATER_THAN_OR_EQUAL:
      return ImmutableRangeSet.of(range(comparison, c));
    case NOT_EQUALS:
      return ImmutableRangeSet.<C>builder()
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      return ImmutableRangeSet.of(range(comparison, c));
    case NOT_EQUALS:
      return ImmutableRangeSet.<C>builder()
          .add(range(SqlKind.LESS_THAN, c))
          .add(range(SqlKind.GREATER_THAN, c))
```

### UnstableApiUsage
'builder()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      return ImmutableRangeSet.of(range(comparison, c));
    case NOT_EQUALS:
      return ImmutableRangeSet.<C>builder()
          .add(range(SqlKind.LESS_THAN, c))
          .add(range(SqlKind.GREATER_THAN, c))
```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    case NOT_EQUALS:
      return ImmutableRangeSet.<C>builder()
          .add(range(SqlKind.LESS_THAN, c))
          .add(range(SqlKind.GREATER_THAN, c))
          .build();
```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      return ImmutableRangeSet.<C>builder()
          .add(range(SqlKind.LESS_THAN, c))
          .add(range(SqlKind.GREATER_THAN, c))
          .build();
    default:
```

### UnstableApiUsage
'build()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
          .add(range(SqlKind.LESS_THAN, c))
          .add(range(SqlKind.GREATER_THAN, c))
          .build();
    default:
      throw new AssertionError();
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      return e;
    }
    final RangeSet<C> rangeSet = rangeSet(comparison.kind, v0);
    final RangeSet<C> rangeSet2 =
        residue(comparison.ref, rangeSet, predicates.pulledUpPredicates,
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    }
    final RangeSet<C> rangeSet = rangeSet(comparison.kind, v0);
    final RangeSet<C> rangeSet2 =
        residue(comparison.ref, rangeSet, predicates.pulledUpPredicates,
            clazz);
```

### UnstableApiUsage
'isEmpty()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
        residue(comparison.ref, rangeSet, predicates.pulledUpPredicates,
            clazz);
    if (rangeSet2.isEmpty()) {
      // Term is impossible to satisfy given these predicates
      return rexBuilder.makeLiteral(false);
```

### UnstableApiUsage
'equals(java.lang.Object)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      // Term is impossible to satisfy given these predicates
      return rexBuilder.makeLiteral(false);
    } else if (rangeSet2.equals(rangeSet)) {
      // no change
      return e;
```

### UnstableApiUsage
'equals(java.lang.Object)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      // no change
      return e;
    } else if (rangeSet2.equals(RangeSets.rangeSetAll())) {
      // Range is always satisfied given these predicates; but nullability might
      // be problematic
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
          rexBuilder.makeCall(SqlStdOperatorTable.IS_NOT_NULL, comparison.ref),
          RexUnknownAs.UNKNOWN);
    } else if (rangeSet2.asRanges().size() == 1
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasLowerBound()
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasUpperBound()
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
          RexUnknownAs.UNKNOWN);
    } else if (rangeSet2.asRanges().size() == 1
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasLowerBound()
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasUpperBound()
        && Iterables.getOnlyElement(rangeSet2.asRanges()).lowerEndpoint()
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    } else if (rangeSet2.asRanges().size() == 1
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasLowerBound()
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasUpperBound()
        && Iterables.getOnlyElement(rangeSet2.asRanges()).lowerEndpoint()
        .equals(Iterables.getOnlyElement(rangeSet2.asRanges()).upperEndpoint())) {
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasLowerBound()
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasUpperBound()
        && Iterables.getOnlyElement(rangeSet2.asRanges()).lowerEndpoint()
        .equals(Iterables.getOnlyElement(rangeSet2.asRanges()).upperEndpoint())) {
      final Range<C> r = Iterables.getOnlyElement(rangeSet2.asRanges());
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
        && Iterables.getOnlyElement(rangeSet2.asRanges()).hasUpperBound()
        && Iterables.getOnlyElement(rangeSet2.asRanges()).lowerEndpoint()
        .equals(Iterables.getOnlyElement(rangeSet2.asRanges()).upperEndpoint())) {
      final Range<C> r = Iterables.getOnlyElement(rangeSet2.asRanges());
      // range is now a point; it's worth simplifying
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
        && Iterables.getOnlyElement(rangeSet2.asRanges()).lowerEndpoint()
        .equals(Iterables.getOnlyElement(rangeSet2.asRanges()).upperEndpoint())) {
      final Range<C> r = Iterables.getOnlyElement(rangeSet2.asRanges());
      // range is now a point; it's worth simplifying
      return rexBuilder.makeCall(SqlStdOperatorTable.EQUALS, comparison.ref,
```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    void addRange(Range<Comparable> range, RelDataType type) {
      types.add(type);
      rangeSet.add(range);
      mergedSarg |= hasSarg;
      nullAs = nullAs.or(UNKNOWN);
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    final boolean negate;
    final List<RelDataType> types = new ArrayList<>();
    final RangeSet<Comparable> rangeSet = TreeRangeSet.create();
    boolean hasSarg;
    boolean mergedSarg;
```

### UnstableApiUsage
'com.google.common.collect.TreeRangeSet' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    final boolean negate;
    final List<RelDataType> types = new ArrayList<>();
    final RangeSet<Comparable> rangeSet = TreeRangeSet.create();
    boolean hasSarg;
    boolean mergedSarg;
```

### UnstableApiUsage
'create()' is declared in unstable class 'com.google.common.collect.TreeRangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    final boolean negate;
    final List<RelDataType> types = new ArrayList<>();
    final RangeSet<Comparable> rangeSet = TreeRangeSet.create();
    boolean hasSarg;
    boolean mergedSarg;
```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java

    void addAll() {
      rangeSet.add(Range.all());
    }

```

### UnstableApiUsage
'com.google.common.base.Utf8' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
    if (charset == StandardCharsets.UTF_8) {
      final byte[] bytes = value.getBytes();
      if (!Utf8.isWellFormed(bytes)) {
        //CHECKSTYLE: IGNORE 1
        final String string = new String(bytes, charset);
```

### UnstableApiUsage
'isWellFormed(byte\[\])' is declared in unstable class 'com.google.common.base.Utf8' marked with @Beta
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
    if (charset == StandardCharsets.UTF_8) {
      final byte[] bytes = value.getBytes();
      if (!Utf8.isWellFormed(bytes)) {
        //CHECKSTYLE: IGNORE 1
        final String string = new String(bytes, charset);
```

### UnstableApiUsage
'copyOf(java.lang.Iterable\>)' is marked unstable with @Beta
in `core/src/main/java/org/apache/calcite/util/ImmutableNullableMap.java`
#### Snippet
```java
   * Returns an immutable map containing the given elements.
   *
   * <p>Behavior is as {@link ImmutableMap#copyOf(Iterable)}
   * except that this map allows nulls.
   */
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      final Sarg<?> sarg = right.getValueAs(Sarg.class);
      ranges = ImmutableList.builder();
      for (Range range : sarg.rangeSet.asRanges()) {
        Range<Long> range2 = RangeSets.copy(range, DruidDateTimeUtils::toLong);
        if (withNot) {
```

### UnstableApiUsage
'com.google.common.collect.ImmutableRangeSet' is marked unstable with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        Range<Long> range2 = RangeSets.copy(range, DruidDateTimeUtils::toLong);
        if (withNot) {
          ranges.addAll(ImmutableRangeSet.of(range2).complement().asRanges());
        } else {
          ranges.add(range2);
```

### UnstableApiUsage
'of(com.google.common.collect.Range)' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        Range<Long> range2 = RangeSets.copy(range, DruidDateTimeUtils::toLong);
        if (withNot) {
          ranges.addAll(ImmutableRangeSet.of(range2).complement().asRanges());
        } else {
          ranges.add(range2);
```

### UnstableApiUsage
'complement()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        Range<Long> range2 = RangeSets.copy(range, DruidDateTimeUtils::toLong);
        if (withNot) {
          ranges.addAll(ImmutableRangeSet.of(range2).complement().asRanges());
        } else {
          ranges.add(range2);
```

### UnstableApiUsage
'asRanges()' is declared in unstable class 'com.google.common.collect.ImmutableRangeSet' marked with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        Range<Long> range2 = RangeSets.copy(range, DruidDateTimeUtils::toLong);
        if (withNot) {
          ranges.addAll(ImmutableRangeSet.of(range2).complement().asRanges());
        } else {
          ranges.add(range2);
```

### UnstableApiUsage
'com.google.common.collect.TreeRangeSet' is marked unstable with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      return null;
    }
    final TreeRangeSet condensedRanges = TreeRangeSet.create();
    for (Range r : ranges) {
      condensedRanges.add(r);
```

### UnstableApiUsage
'com.google.common.collect.TreeRangeSet' is marked unstable with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      return null;
    }
    final TreeRangeSet condensedRanges = TreeRangeSet.create();
    for (Range r : ranges) {
      condensedRanges.add(r);
```

### UnstableApiUsage
'create()' is declared in unstable class 'com.google.common.collect.TreeRangeSet' marked with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      return null;
    }
    final TreeRangeSet condensedRanges = TreeRangeSet.create();
    for (Range r : ranges) {
      condensedRanges.add(r);
```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable class 'com.google.common.collect.TreeRangeSet' marked with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
    final TreeRangeSet condensedRanges = TreeRangeSet.create();
    for (Range r : ranges) {
      condensedRanges.add(r);
    }
    LOGGER.debug("Inferred ranges on interval : {}", condensedRanges);
```

### UnstableApiUsage
'asRanges()' is declared in unstable class 'com.google.common.collect.TreeRangeSet' marked with @Beta
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
    }
    LOGGER.debug("Inferred ranges on interval : {}", condensedRanges);
    return toInterval(ImmutableList.<Range<Long>>copyOf(condensedRanges.asRanges()));
  }

```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java

  /**
   * Creates a Matcher that matches a {@link RangeSet} if its string
   * representation, after changing "&#2025;" to "..",
   * is equal to the given {@code value}.
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java
   * is equal to the given {@code value}.
   *
   * <p>This method is necessary because {@link RangeSet#toString()} changed
   * behavior. Guava 19 - 28 used a unicode symbol; Guava 29 onwards uses "..".
   */
```

### UnstableApiUsage
'toString()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java
   * is equal to the given {@code value}.
   *
   * <p>This method is necessary because {@link RangeSet#toString()} changed
   * behavior. Guava 19 - 28 used a unicode symbol; Guava 29 onwards uses "..".
   */
```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java
   */
  @SuppressWarnings("BetaApi")
  public static Matcher<RangeSet> isRangeSet(final String value) {
    return compose(Is.is(value), input -> {
      // Change all '\u2025' (a unicode symbol denoting a range) to '..',
```

### UnstableApiUsage
'toString()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java
      // Change all '\u2025' (a unicode symbol denoting a range) to '..',
      // consistent with Guava 29+.
      return input.toString().replace("\u2025", "..");
    });
  }
```

### UnstableApiUsage
'PatternFilenameFilter(java.lang.String)' is declared in unstable class 'com.google.common.io.PatternFilenameFilter' marked with @Beta
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
    final File dir = firstFile.getParentFile();
    final List<String> paths = new ArrayList<>();
    final FilenameFilter filter = new PatternFilenameFilter(".*\\.iq$");
    for (File f : Util.first(dir.listFiles(filter), new File[0])) {
      paths.add(f.getAbsolutePath().substring(commonPrefixLength));
```

### UnstableApiUsage
'com.google.common.io.PatternFilenameFilter' is marked unstable with @Beta
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
    final File dir = firstFile.getParentFile();
    final List<String> paths = new ArrayList<>();
    final FilenameFilter filter = new PatternFilenameFilter(".*\\.iq$");
    for (File f : Util.first(dir.listFiles(filter), new File[0])) {
      paths.add(f.getAbsolutePath().substring(commonPrefixLength));
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      final SqlTypeName sqlTypeName = type.getSqlTypeName();
      if (sarg.isPoints()) {
        Set<Range> ranges = sarg.rangeSet.asRanges();
        ranges.forEach(range ->
            values.add(sargPointValue(range.lowerEndpoint(), sqlTypeName)));
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
            values.add(sargPointValue(range.lowerEndpoint(), sqlTypeName)));
      } else if (sarg.isComplementedPoints()) {
        Set<Range> ranges = sarg.negate().rangeSet.asRanges();
        ranges.forEach(range ->
            values.add(sargPointValue(range.lowerEndpoint(), sqlTypeName)));
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
   */
  private Casing getPreferredCasing(String word) {
    if (word == prevWord) {
      return castNonNull(prevPreferredCasing);
    }
```

## RuleId[id=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
    final StringBuilder b = new StringBuilder();
    int i = 0;
    for (;;) {
      char c = s.charAt(i);
      if (c != '"') {
```

### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
      } else {
        try (Enumerator<TSource> os = enumerable.enumerator()) {
          while (true) {
            if (!os.moveNext()) {
              break;
```

## RuleId[id=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
Source parameter type 'java.lang.Object\[\]' is not assignable to destination parameter `a` of type 'T\[\]'
in `core/src/main/java/org/apache/calcite/util/ImmutableIntList.java`
#### Snippet
```java
      }
    } else {
      System.arraycopy(toArray(), 0, a, 0, size);
    }
    if (a.length > size) {
```

## RuleId[id=RedundantClassCall]
### RedundantClassCall
Redundant call to `isInstance()`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
      // than BigDecimal.equals(BigDecimal). So if both of types are BigDecimal,
      // we just use SqlFunctions.eq(BigDecimal, BigDecimal).
      if (BigDecimal.class.isInstance(b0)) {
        return eq((BigDecimal) b0, (BigDecimal) b1);
      } else {
```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/src/main/java/org/apache/calcite/util/Permutation.java`
#### Snippet
```java
  //~ Methods ----------------------------------------------------------------

  @Override public Object clone() {
    return new Permutation(
        targets.clone(),
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
    }

    @Override protected StringEscaper clone() {
      StringEscaper clone = new StringEscaper();
      if (translationVector != null) {
```

## RuleId[id=UnnecessaryEmptyArrayUsage]
### UnnecessaryEmptyArrayUsage
Zero length array can be changed to constant
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
          SqlCall sqlCall = (SqlCall) node;
          @SuppressWarnings("assignment.type.incompatible")
          SqlNode[] operands = sqlCall.getOperandList().toArray(new SqlNode[0]);
          operands[1] = new SqlIdentifier(neededAlias, POS);
          return SqlStdOperatorTable.AS.createCall(POS, operands);
```

### UnnecessaryEmptyArrayUsage
Zero length array can be changed to constant
in `core/src/main/java/org/apache/calcite/sql/SqlOperator.java`
#### Snippet
```java
        null,
        nodeList.getParserPosition(),
        nodeList.toArray(new SqlNode[0]));
  }

```

### UnnecessaryEmptyArrayUsage
Zero length array can be changed to constant
in `core/src/main/java/org/apache/calcite/sql/SqlOperator.java`
#### Snippet
```java
        null,
        pos,
        operandList.toArray(new SqlNode[0]));
  }

```

### UnnecessaryEmptyArrayUsage
Zero length array can be changed to constant
in `core/src/main/java/org/apache/calcite/sql/util/SqlShuttle.java`
#### Snippet
```java
      this.update = false;
      final List<@Nullable SqlNode> operands = (List<@Nullable SqlNode>) call.getOperandList();
      this.clonedOperands = operands.toArray(new SqlNode[0]);
      this.alwaysCopy = alwaysCopy;
    }
```

### UnnecessaryEmptyArrayUsage
Zero length array can be changed to constant
in `core/src/main/java/org/apache/calcite/sql/parser/SqlParserUtil.java`
#### Snippet
```java

  public static SqlNode[] toNodeArray(SqlNodeList list) {
    return list.toArray(new SqlNode[0]);
  }

```

### UnnecessaryEmptyArrayUsage
Zero length array can be changed to constant
in `core/src/main/java/org/apache/calcite/sql/parser/SqlParserUtil.java`
#### Snippet
```java

  public static SqlNode[] toNodeArray(List<SqlNode> list) {
    return list.toArray(new SqlNode[0]);
  }

```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeSystemImpl.java`
#### Snippet
```java
    case VARBINARY:
    case BINARY:
      return 65536;
    case TIME:
    case TIME_WITH_LOCAL_TIME_ZONE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeSystemImpl.java`
#### Snippet
```java
      return SqlTypeName.DEFAULT_INTERVAL_START_PRECISION;
    case BOOLEAN:
      return 1;
    case TINYINT:
      return 3;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactoryImpl.java`
#### Snippet
```java
      return createSqlType(SqlTypeName.DECIMAL, 14, 7);
    case FLOAT:
      return createSqlType(SqlTypeName.DECIMAL, 14, 7);
    case DOUBLE:
      // the default max precision is 19, so this is actually DECIMAL(19, 15)
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/rex/LogicVisitor.java`
#### Snippet
```java
      break;
    case CASE:
      logic = Logic.TRUE_FALSE_UNKNOWN;
      break;
    default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case INTERVAL_MINUTE_SECOND:
    case INTERVAL_SECOND:
      assert value instanceof BigDecimal;
      sb.append(value.toString());
      break;
    case MULTISET:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
      return value instanceof TimeString;
    case TIME_WITH_LOCAL_TIME_ZONE:
      return value instanceof TimeString;
    case TIMESTAMP:
      return value instanceof TimestampString;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
      return value instanceof TimestampString;
    case TIMESTAMP_WITH_LOCAL_TIME_ZONE:
      return value instanceof TimestampString;
    case INTERVAL_YEAR:
    case INTERVAL_YEAR_MONTH:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/SqlCollation.java`
#### Snippet
```java
        return col2;
      case IMPLICIT:
        return col2;
      case NONE:
        return null;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/SqlCollation.java`
#### Snippet
```java
        return null;
      case EXPLICIT:
        return col2;
      default:
        throw Util.unexpected(coercibility2);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
      return DatabaseProduct.CLICKHOUSE;
    case "DBMS:CLOUDSCAPE":
      return DatabaseProduct.DERBY;
    case "EXASOL":
      return DatabaseProduct.EXASOL;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
      break;
    case 4:
      sourceExpressionList = (SqlNodeList) operand;
      break;
    case 5:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/SqlDialectFactoryImpl.java`
#### Snippet
```java
      return new ClickHouseSqlDialect(c);
    case "DBMS:CLOUDSCAPE":
      return new DerbySqlDialect(c);
    case "EXASOL":
      return new ExasolSqlDialect(c);
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeName.java`
#### Snippet
```java
      switch (limit) {
      case ZERO:
        break;
      case UNDERFLOW:
        if (beyond) {
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeFamily.java`
#### Snippet
```java
      return factory.createSqlType(SqlTypeName.BOOLEAN);
    case STRING:
      return factory.createSqlType(SqlTypeName.VARCHAR);
    case APPROXIMATE_NUMERIC:
      return factory.createSqlType(SqlTypeName.DOUBLE);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeFamily.java`
#### Snippet
```java
      return factory.createSqlType(SqlTypeName.DOUBLE);
    case EXACT_NUMERIC:
      return SqlTypeUtil.getMaxPrecisionScaleDecimal(factory);
    case INTEGER:
      return factory.createSqlType(SqlTypeName.BIGINT);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeFamily.java`
#### Snippet
```java
      return factory.createSqlType(SqlTypeName.DECIMAL);
    case DATETIME:
      return factory.createSqlType(SqlTypeName.TIMESTAMP);
    case INTERVAL_DAY_TIME:
      return factory.createSqlIntervalType(
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/sql/advise/SqlSimpleParser.java`
#### Snippet
```java
        case GROUP:
        case HAVING:
          purgeSelect();
          purgeWhere();
          purgeOrderBy();
          break;
        case ORDER:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/plan/VisitorDataContext.java`
#### Snippet
```java
      case DATE:
      case TIME:
        return Pair.of(index, rexLiteral.getValueAs(Integer.class));
      case TIMESTAMP:
        return Pair.of(index, rexLiteral.getValueAs(Long.class));
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/plan/VisitorDataContext.java`
#### Snippet
```java
        return Pair.of(index, rexLiteral.getValueAs(Integer.class));
      case TIMESTAMP:
        return Pair.of(index, rexLiteral.getValueAs(Long.class));
      case CHAR:
        return Pair.of(index, rexLiteral.getValueAs(Character.class));
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
      return TimeUnit.YEAR.multiplier.longValue();
    case QUARTER:
      return TimeUnit.YEAR.multiplier.longValue();
    case YEAR:
    case DECADE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
    case CENTURY:
    case MILLENNIUM:
      return 1L;
    default:
      throw Util.unexpected(unit);
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      return;
    case IN:
      break;
    case NOT_IN:
    case NOT:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java
          switch (fieldCollation.nullDirection) {
          case FIRST:
            break;
          case LAST:
            break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java
            break;
          case LAST:
            break;
          default:
            break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
        return prefix(call);
      case INTERNAL:
        return binary(call);
      case SPECIAL:
        switch (call.getKind()) {
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
          return binary(call);
        case CONTAINS:
          return binary(call);
        default:
          // manually process ITEM($0, 'foo') which in our case will be named attribute
```

## RuleId[id=NonFinalFieldInEnum]
### NonFinalFieldInEnum
Non-final field `relClazz` in enum 'NodeType'
in `core/src/main/java/org/apache/calcite/rel/hint/NodeTypeHintPredicate.java`
#### Snippet
```java
    /** Relational expression clazz that the hint can apply to. */
    @SuppressWarnings("ImmutableEnumChecker")
    private Class<?> relClazz;

    NodeType(Class<?> relClazz) {
```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`{1,` can be simplified to '+'
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalQualifier.java`
#### Snippet
```java
        getFractionalSecondPrecision(typeSystem);
    String intervalPatternWithFracSec =
        "(\\d+) (\\d{1,2}):(\\d{1,2}):(\\d{1,2})\\.(\\d{1,"
        + fractionalSecondPrecision + "})";
    String intervalPatternWithoutFracSec =
```

### RegExpSimplifiable
`{1,` can be simplified to '+'
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalQualifier.java`
#### Snippet
```java
        getFractionalSecondPrecision(typeSystem);
    String intervalPatternWithFracSec =
        "(\\d+):(\\d{1,2})\\.(\\d{1," + fractionalSecondPrecision + "})";
    String intervalPatternWithoutFracSec =
        "(\\d+):(\\d{1,2})";
```

### RegExpSimplifiable
`{1,` can be simplified to '+'
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalQualifier.java`
#### Snippet
```java
        getFractionalSecondPrecision(typeSystem);
    String intervalPatternWithFracSec =
        "(\\d+)\\.(\\d{1," + fractionalSecondPrecision + "})";
    String intervalPatternWithoutFracSec =
        "(\\d+)";
```

### RegExpSimplifiable
`{1,` can be simplified to '+'
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalQualifier.java`
#### Snippet
```java
        getFractionalSecondPrecision(typeSystem);
    String intervalPatternWithFracSec =
        "(\\d+):(\\d{1,2}):(\\d{1,2})\\.(\\d{1,"
        + fractionalSecondPrecision + "})";
    String intervalPatternWithoutFracSec =
```

### RegExpSimplifiable
`99*` can be simplified to '9+'
in `testkit/src/main/java/org/apache/calcite/util/TestUtil.java`
#### Snippet
```java
  /** Matches a number with at least four nines after the point. */
  private static final Pattern TRAILING_NINE_PATTERN =
      Pattern.compile("-?[0-9]+\\.([0-9]*[0-8])?(99999*[0-9][0-9]?)");

  /** This is to be used by {@link #rethrow(Throwable, String)} to add extra information via
```

### RegExpSimplifiable
`00*` can be simplified to '0+'
in `testkit/src/main/java/org/apache/calcite/util/TestUtil.java`
#### Snippet
```java
  /** Matches a number with at least four zeros after the point. */
  private static final Pattern TRAILING_ZERO_PATTERN =
      Pattern.compile("-?[0-9]+\\.([0-9]*[1-9])?(00000*[0-9][0-9]?)");

  /** Matches a number with at least four nines after the point. */
```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `(precision >= 3) ? 999 : ((precision == 2) ? 990 : ((precision == 1) ? 900 : 0))`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeName.java`
#### Snippet
```java
        calendar.set(Calendar.SECOND, 59);
        int millis =
            (precision >= 3) ? 999
                : ((precision == 2) ? 990 : ((precision == 1) ? 900 : 0));
        calendar.set(Calendar.MILLISECOND, millis);
        break;
```

### DuplicateExpressions
Multiple occurrences of `(precision >= 3) ? 999 : ((precision == 2) ? 990 : ((precisi...`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeName.java`
#### Snippet
```java
          calendar.set(Calendar.SECOND, 59);
          int millis =
              (precision >= 3) ? 999
                  : ((precision == 2) ? 990
                      : ((precision == 1) ? 900 : 0));
          calendar.set(Calendar.MILLISECOND, millis);
        } else {
```

### DuplicateExpressions
Multiple occurrences of `Objects.equals(dependent, arg0Input) ? arg1 : dependent`
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
        sum0 =
            SqlStdOperatorTable.SUM.createCall(pos, arg0,
                Objects.equals(dependent, arg0Input) ? arg1 : dependent);
        sum1 =
            SqlStdOperatorTable.SUM.createCall(pos, arg1,
```

### DuplicateExpressions
Multiple occurrences of `Objects.equals(dependent, arg0Input) ? arg1 : dependent`
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
        count =
            SqlStdOperatorTable.REGR_COUNT.createCall(pos, arg0,
                Objects.equals(dependent, arg0Input) ? arg1 : dependent);
      }

```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong( line.substring(line.length() - TS_OFF.length(), ...`
in `plus/src/main/java/org/apache/calcite/adapter/os/GitCommitsTableFunction.java`
#### Snippet
```java
                            line.length() - TS_OFF.length() - 1);
                    objects[5] =
                        Long.parseLong(
                            line.substring(line.length() - TS_OFF.length(),
                            line.length() - OFF.length() - 1)) * 1000;
                  } else if (line.startsWith("committer ")) {
                    objects[6] =
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong( line.substring(line.length() - TS_OFF.length(), ...`
in `plus/src/main/java/org/apache/calcite/adapter/os/GitCommitsTableFunction.java`
#### Snippet
```java
                            line.length() - TS_OFF.length() - 1);
                    objects[5] =
                        Long.parseLong(
                            line.substring(line.length() - TS_OFF.length(),
                            line.length() - OFF.length() - 1)) * 1000;
                  } else if (line.startsWith("committer ")) {
                    objects[6] =
```

### DuplicateExpressions
Multiple occurrences of `line.substring(line.length() - TS_OFF.length(), line.length() - OFF.leng...`
in `plus/src/main/java/org/apache/calcite/adapter/os/GitCommitsTableFunction.java`
#### Snippet
```java
                    objects[5] =
                        Long.parseLong(
                            line.substring(line.length() - TS_OFF.length(),
                            line.length() - OFF.length() - 1)) * 1000;
                  } else if (line.startsWith("committer ")) {
                    objects[6] =
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong( line.substring(line.length() - TS_OFF.length(), ...`
in `plus/src/main/java/org/apache/calcite/adapter/os/GitCommitsTableFunction.java`
#### Snippet
```java
                            line.length() - TS_OFF.length() - 1);
                    objects[7] =
                        Long.parseLong(
                            line.substring(line.length() - TS_OFF.length(),
                            line.length() - OFF.length() - 1)) * 1000;
                  }
                  if (!e.moveNext()) {
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong( line.substring(line.length() - TS_OFF.length(), ...`
in `plus/src/main/java/org/apache/calcite/adapter/os/GitCommitsTableFunction.java`
#### Snippet
```java
                            line.length() - TS_OFF.length() - 1);
                    objects[7] =
                        Long.parseLong(
                            line.substring(line.length() - TS_OFF.length(),
                            line.length() - OFF.length() - 1)) * 1000;
                  }
                  if (!e.moveNext()) {
```

### DuplicateExpressions
Multiple occurrences of `line.substring(line.length() - TS_OFF.length(), line.length() - OFF.leng...`
in `plus/src/main/java/org/apache/calcite/adapter/os/GitCommitsTableFunction.java`
#### Snippet
```java
                    objects[7] =
                        Long.parseLong(
                            line.substring(line.length() - TS_OFF.length(),
                            line.length() - OFF.length() - 1)) * 1000;
                  }
                  if (!e.moveNext()) {
```

### DuplicateExpressions
Multiple occurrences of `kind == SqlKind.IN || kind == SqlKind.NOT_IN || kind == SqlKind.SOME || kind == Sq...`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
          // sub-queries so we can convert them to constants
          findSubQueries(bb, operand, logic,
              kind == SqlKind.IN || kind == SqlKind.NOT_IN
                  || kind == SqlKind.SOME || kind == SqlKind.ALL
                  || registerOnlyScalarSubQueries, clause);
        }
      }
```

### DuplicateExpressions
Multiple occurrences of `kind == SqlKind.IN || kind == SqlKind.NOT_IN || kind == SqlKind.SOME || kind == SqlK...`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      for (SqlNode child : (SqlNodeList) node) {
        findSubQueries(bb, child, logic,
            kind == SqlKind.IN || kind == SqlKind.NOT_IN
                || kind == SqlKind.SOME || kind == SqlKind.ALL
                || registerOnlyScalarSubQueries, clause);
      }
    }
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
    default:
      list.add(node);
      return;
    }
  }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
      builder.add(
          analyzeGroupExpr(scope, groupAnalyzer, groupExpr));
      return;
    }
  }
```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/calcite/rel/externalize/RelDotWriter.java`
#### Snippet
```java
    List<String> descParts = new ArrayList<>();
    for (int idx = 0; idx < label.length(); idx += option.maxNodeLabelPerLine()) {
      int endIdx = idx + option.maxNodeLabelPerLine() > label.length() ? label.length()
          : idx + option.maxNodeLabelPerLine();
      descParts.add(label.substring(idx, endIdx));
    }
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static int lesser(int b0, int b1) {
    return b0 > b1 ? b1 : b0;
  }

```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static double lesser(double b0, double b1) {
    return b0 > b1 ? b1 : b0;
  }

```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static float lesser(float b0, float b1) {
    return b0 > b1 ? b1 : b0;
  }

```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static long lesser(long b0, long b1) {
    return b0 > b1 ? b1 : b0;
  }

```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static double greater(double b0, double b1) {
    return b0 > b1 ? b0 : b1;
  }

```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static long greater(long b0, long b1) {
    return b0 > b1 ? b0 : b1;
  }

```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static float greater(float b0, float b1) {
    return b0 > b1 ? b0 : b1;
  }

```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static int greater(int b0, int b1) {
    return b0 > b1 ? b0 : b1;
  }

```

## RuleId[id=NegativeIntConstantInLongContext]
### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
  static final int[] INT_S = {1, 2, 4, 8, 16};
  static final long[] LONG_B = {
      0x2, 0xC, 0xF0, 0xFF00, 0xFFFF0000, 0xFFFFFFFF00000000L};
  static final int[] LONG_S = {1, 2, 4, 8, 16, 32};

```

## RuleId[id=JavadocLinkAsPlainText]
### JavadocLinkAsPlainText
Link specified as plain text
in `core/src/main/java/org/apache/calcite/config/CalciteSystemProperty.java`
#### Snippet
```java

  /**
   * Whether to use Docker containers (https://www.testcontainers.org/) in tests.
   *
   * If the property is set to <code>true</code>, affected tests will attempt to start Docker
```

### JavadocLinkAsPlainText
Link specified as plain text
in `core/src/main/java/org/apache/calcite/model/ModelHandler.java`
#### Snippet
```java
  public enum ExtraOperand {
    /** URI of model, e.g. "target/test-classes/model.json",
     * "http://localhost/foo/bar.json", "inline:{...}",
     * "target/test-classes/model.yaml",
     * "http://localhost/foo/bar.yaml", "inline:..."
```

### JavadocLinkAsPlainText
Link specified as plain text
in `core/src/main/java/org/apache/calcite/model/ModelHandler.java`
#### Snippet
```java
     * "http://localhost/foo/bar.json", "inline:{...}",
     * "target/test-classes/model.yaml",
     * "http://localhost/foo/bar.yaml", "inline:..."
     * */
    MODEL_URI("modelUri"),
```

### JavadocLinkAsPlainText
Link specified as plain text
in `core/src/main/java/org/apache/calcite/runtime/BinarySearch.java`
#### Snippet
```java

  /**
   * Taken from http://en.wikipedia.org/wiki/Binary_search_algorithm
   * #Deferred_detection_of_equality
   */
```

### JavadocLinkAsPlainText
Link specified as plain text
in `core/src/main/java/org/apache/calcite/runtime/BinarySearch.java`
#### Snippet
```java

  /**
   * Taken from http://en.wikipedia.org/wiki/Binary_search_algorithm
   * #Deferred_detection_of_equality
   * Adapted to find upper bound.
```

### JavadocLinkAsPlainText
Link specified as plain text
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSchema.java`
#### Snippet
```java
   * Creates a Druid schema.
   *
   * @param url URL of query REST service, e.g. "http://localhost:8082"
   * @param coordinatorUrl URL of coordinator REST service,
   *                       e.g. "http://localhost:8081"
```

### JavadocLinkAsPlainText
Link specified as plain text
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSchema.java`
#### Snippet
```java
   * @param url URL of query REST service, e.g. "http://localhost:8082"
   * @param coordinatorUrl URL of coordinator REST service,
   *                       e.g. "http://localhost:8081"
   * @param discoverTables If true, ask Druid what tables exist;
   *                       if false, only create tables explicitly in the model
```

### JavadocLinkAsPlainText
Link specified as plain text
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSchemaFactory.java`
#### Snippet
```java
 *     <td>url</td>
 *     <td>URL of Druid's query node.
 *     The default is "http://localhost:8082".</td>
 *     <td>No</td>
 *   </tr>
```

### JavadocLinkAsPlainText
Link specified as plain text
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSchemaFactory.java`
#### Snippet
```java
 *     <td>URL of Druid's coordinator node.
 *     The default is <code>url</code>, replacing "8082" with "8081",
 *     for example "http://localhost:8081".</td>
 *     <td>No</td>
 *   </tr>
```

### JavadocLinkAsPlainText
Link specified as plain text
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java

  /**
   * Returns the URI of this HTTP server ("http://host:port").
   */
  String uri() {
```

## RuleId[id=FieldCanBeLocal]
### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java
    private final RelNode parent;
    @SuppressWarnings("unused")
    private final int ordinalInParent;
    private final RelNode r;
    private final boolean anon;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
  private final List<@Nullable String> projectNameList = new ArrayList<>();
  @SuppressWarnings("unused")
  private final @Nullable RexSimplify simplify;
  private @Nullable RexLocalRef conditionRef = null;
  private boolean validating;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/sql/SqlStateCodes.java`
#### Snippet
```java

  @SuppressWarnings("unused")
  private final String msg;
  private final String stateClass;
  private final String stateSubClass;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/sql/fun/SqlMultisetSetOperator.java`
#### Snippet
```java

  @SuppressWarnings("unused")
  private final boolean all;

  //~ Constructors -----------------------------------------------------------
```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/sql/validate/CollectScope.java`
#### Snippet
```java

  @SuppressWarnings("unused")
  private final @Nullable SqlValidatorScope usingScope;
  private final SqlCall child;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/plan/visualizer/RuleMatchVisualizer.java`
#### Snippet
```java
  // default HTML template can be edited at
  // core/src/main/resources/org/apache/calcite/plan/visualizer/viz-template.html
  private final String templateDirectory = "org/apache/calcite/plan/visualizer";
  private final @Nullable String outputDirectory;
  private final @Nullable String outputSuffix;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/util/PartiallyOrderedSet.java`
#### Snippet
```java
  private final @Nullable Function<E, Iterable<E>> parentFunction;
  @SuppressWarnings("unused")
  private final @Nullable Function<E, Iterable<E>> childFunction;
  private final Ordering<E> ordering;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/util/Pair.java`
#### Snippet
```java
   * @param <E> Element type */
  private static class AdjacentIterator<E> implements Iterator<Pair<E, E>> {
    private final E first;
    private final Iterator<? extends E> iterator;
    E previous;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/runtime/Matcher.java`
#### Snippet
```java

  @SuppressWarnings("unused")
  private final ImmutableBitSet startSet;

  /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   */
  public static class GridEnumerable extends AbstractEnumerable<Object[]> {
    private final Envelope envelope;
    private final boolean point;
    private final double deltaX;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
    private final int baseY;
    private final int spanX;
    private final int spanY;
    private final int area;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/interpreter/ValuesNode.java`
#### Snippet
```java
public class ValuesNode implements Node {
  private final Sink sink;
  private final int fieldCount;
  private final ImmutableList<Row> rows;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `core/src/main/java/org/apache/calcite/sql2rel/RelStructuredTypeFlattener.java`
#### Snippet
```java
  private int iRestructureInput;
  @SuppressWarnings("unused")
  private @Nullable RelDataType flattenedRootType;
  boolean restructured;
  private final RelOptTable.ToRelContext toRelContext;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
  private static class CellReader {
    @SuppressWarnings("unused")
    private String type;
    private String selector;
    private Integer selectedElement;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `plus/src/main/java/org/apache/calcite/adapter/os/SqlShell.java`
#### Snippet
```java
  private final List<String> args;
  @SuppressWarnings("unused")
  private final InputStreamReader in;
  private final PrintWriter out;
  @SuppressWarnings("unused")
```

### FieldCanBeLocal
Field can be converted to a local variable
in `plus/src/main/java/org/apache/calcite/adapter/os/SqlShell.java`
#### Snippet
```java
  private final PrintWriter out;
  @SuppressWarnings("unused")
  private final PrintWriter err;

  SqlShell(InputStreamReader in, PrintWriter out,
```

### FieldCanBeLocal
Field can be converted to a local variable
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java
  private String locatorHost;
  @SuppressWarnings("unused")
  private int locatorPort;
  @SuppressWarnings("unused")
  private String[] regionNames;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java
  private String[] regionNames;
  @SuppressWarnings("unused")
  private String pdxAutoSerializerPackageExp;
  @SuppressWarnings("unused")
  private ClientCache clientCache;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java

  @SuppressWarnings("unused")
  private String locatorHost;
  @SuppressWarnings("unused")
  private int locatorPort;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleEnumerator.java`
#### Snippet
```java
  private E current;
  @SuppressWarnings("unused")
  private ClientCache clientCache;

  protected GeodeSimpleEnumerator(ClientCache clientCache, String regionName) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
  static class Translator {
    @SuppressWarnings("unused")
    private final RelDataType rowType;

    private final List<String> fieldNames;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisSchema.java`
#### Snippet
```java
  public final String password;
  public final List<Map<String, Object>> tables;
  private Map<String, Table> tableMap = null;

  RedisSchema(String host,
```

### FieldCanBeLocal
Field can be converted to a local variable
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisJedisManager.java`
#### Snippet
```java
  private final int maxTotal = GenericObjectPoolConfig.DEFAULT_MAX_TOTAL;
  private final int maxIdle = GenericObjectPoolConfig.DEFAULT_MAX_IDLE;
  private final int minIdle = GenericObjectPoolConfig.DEFAULT_MIN_IDLE;
  private final int timeout = Protocol.DEFAULT_TIMEOUT;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisJedisManager.java`
#### Snippet
```java
  private final JedisPoolConfig jedisPoolConfig;

  private final int maxTotal = GenericObjectPoolConfig.DEFAULT_MAX_TOTAL;
  private final int maxIdle = GenericObjectPoolConfig.DEFAULT_MAX_IDLE;
  private final int minIdle = GenericObjectPoolConfig.DEFAULT_MIN_IDLE;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisJedisManager.java`
#### Snippet
```java
  private final int maxIdle = GenericObjectPoolConfig.DEFAULT_MAX_IDLE;
  private final int minIdle = GenericObjectPoolConfig.DEFAULT_MIN_IDLE;
  private final int timeout = Protocol.DEFAULT_TIMEOUT;

  private final String host;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisJedisManager.java`
#### Snippet
```java

  private final int maxTotal = GenericObjectPoolConfig.DEFAULT_MAX_TOTAL;
  private final int maxIdle = GenericObjectPoolConfig.DEFAULT_MAX_IDLE;
  private final int minIdle = GenericObjectPoolConfig.DEFAULT_MIN_IDLE;
  private final int timeout = Protocol.DEFAULT_TIMEOUT;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkHandlerImpl.java`
#### Snippet
```java
 */
public class SparkHandlerImpl implements CalcitePrepare.SparkHandler {
  private final HttpServer classServer;
  private final AtomicInteger classId;
  private final SparkConf sparkConf =
```

### FieldCanBeLocal
Field can be converted to a local variable
in `server/src/main/java/org/apache/calcite/server/MutableArrayTable.java`
#### Snippet
```java
  final List rows = new ArrayList();
  @SuppressWarnings("unused")
  private final RelProtoDataType protoStoredRowType;
  private final RelProtoDataType protoRowType;
  private final InitializerExpressionFactory initializerExpressionFactory;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkDriver.java`
#### Snippet
```java
  private static class WrappingSplunkConnection implements SplunkConnection {
    @SuppressWarnings("unused")
    private final SplunkConnection connection;

    WrappingSplunkConnection(SplunkConnection connection) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
      implements Enumerator<TResult> {
    @SuppressWarnings("unused")
    private final Enumerable<TSource> enumerable;
    private final Function1<TSource, TKey> keySelector;
    private final Function0<TAccumulate> accumulatorInitializer;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `example/function/src/main/java/org/apache/calcite/example/maze/Maze.java`
#### Snippet
```java

  static final boolean DEBUG = false;
  private final boolean horizontal = false;
  private final boolean spiral = false;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `example/function/src/main/java/org/apache/calcite/example/maze/Maze.java`
#### Snippet
```java
  static final boolean DEBUG = false;
  private final boolean horizontal = false;
  private final boolean spiral = false;

  Maze(int width, int height) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
    private final String fieldName;
    @SuppressWarnings("unused")
    private final String value;

    RegexpQueryBuilder(final String fieldName, final String value) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
  static class RegexpQueryBuilder extends QueryBuilder {
    @SuppressWarnings("unused")
    private final String fieldName;
    @SuppressWarnings("unused")
    private final String value;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
  static final class CastExpression implements TerminalExpression {
    @SuppressWarnings("unused")
    private final RelDataType type;
    private final TerminalExpression argument;

```

## RuleId[id=FunctionalExpressionCanBeFolded]
### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/rel/rules/MaterializedViewFilterScanRule.java`
#### Snippet
```java
          .addRuleInstance(CoreRules.FILTER_PROJECT_TRANSPOSE)
          .addRuleInstance(CoreRules.PROJECT_MERGE)
          .build())::get;

  //~ Constructors -----------------------------------------------------------
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
            .withIdentifierQuoteString(quoteString)
            .withNullCollation(nullCollation));
      })::get;
    }

```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterializations.java`
#### Snippet
```java
    // Remember leaf-join form of root so we convert at most once.
    final Supplier<RelNode> leafJoinRoot =
        Suppliers.memoize(() -> RelOptMaterialization.toLeafJoinForm(rel))::get;
    for (RelOptLattice lattice : lattices) {
      if (queryTableNames.contains(lattice.rootTable().getQualifiedName())) {
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
      final Collection<V> values,
      final com.google.common.base.Function<V, K> function) {
    return asIndexMapJ(values, function::apply);
  }

```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/tools/Frameworks.java`
#### Snippet
```java
  /** Caches an instance of the JDBC driver. */
  private static final Supplier<Driver> DRIVER_SUPPLIER =
      Suppliers.memoize(Driver::new)::get;

  private Frameworks() {
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcTable.java`
#### Snippet
```java
  @SuppressWarnings("methodref.receiver.bound.invalid")
  private final Supplier<RelProtoDataType> protoRowTypeSupplier =
      Suppliers.memoize(this::supplyProto)::get;
  public final JdbcSchema jdbcSchema;
  public final String jdbcCatalogName;
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/adapter/enumerable/impl/WinAggAddContextImpl.java`
#### Snippet
```java
  protected WinAggAddContextImpl(BlockBuilder block, List<Expression> accumulator,
      com.google.common.base.Function<BlockBuilder, WinAggFrameResultContext> frame) {
    this(block, accumulator, (Function<BlockBuilder, WinAggFrameResultContext>) frame::apply);
  }

```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/adapter/enumerable/impl/WinAggResultContextImpl.java`
#### Snippet
```java
      com.google.common.base.Function<BlockBuilder, WinAggFrameResultContext> frameContextBuilder) {
    this(block, accumulator,
        (Function<BlockBuilder, WinAggFrameResultContext>) frameContextBuilder::apply);
  }

```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `core/src/main/java/org/apache/calcite/materialize/ProfilerLatticeStatisticProvider.java`
#### Snippet
```java
              });
      return profiler.profile(rows, columns, initialGroups);
    })::get;
  }

```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlTestFactory.java`
#### Snippet
```java
    this.typeFactorySupplier = Suppliers.memoize(() ->
        typeFactoryFactory.create(validatorConfig.conformance(),
            typeSystemTransform.apply(RelDataTypeSystem.DEFAULT)))::get;
    this.catalogReaderSupplier = Suppliers.memoize(() ->
        catalogReaderFactory.create(this.typeFactorySupplier.get(),
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlTestFactory.java`
#### Snippet
```java
    this.catalogReaderSupplier = Suppliers.memoize(() ->
        catalogReaderFactory.create(this.typeFactorySupplier.get(),
            parserConfig.caseSensitive()))::get;
    this.parserConfig = parserConfig;
    this.validatorConfig = validatorConfig;
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `testkit/src/main/java/org/apache/calcite/util/TestUtil.java`
#### Snippet
```java

  private static final Supplier<Integer> GUAVA_MAJOR_VERSION =
      Suppliers.memoize(TestUtil::computeGuavaMajorVersion)::get;

  /** Matches a number with at least four zeros after the point. */
```

## RuleId[id=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/CalciteImmutable.java`
#### Snippet
```java
@Value.Style(
    visibility = Value.Style.ImplementationVisibility.PACKAGE,
    defaults = @Value.Immutable(builder = true, singleton = true),
    get = {"is*", "get*"},
    init = "with*",
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectJoinTransposeRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config DEFAULT = ImmutableProjectJoinTransposeRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/ValuesReduceRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config FILTER = ImmutableValuesReduceRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectCorrelateTransposeRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config DEFAULT = ImmutableProjectCorrelateTransposeRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/CoerceInputsRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config DEFAULT = ImmutableCoerceInputsRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectSetOpTransposeRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config DEFAULT = ImmutableProjectSetOpTransposeRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/ExchangeRemoveConstantKeysRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config DEFAULT = ImmutableExchangeRemoveConstantKeysRule.Config
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/FilterJoinRule.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface FilterIntoJoinRuleConfig extends FilterJoinRule.Config {
      FilterIntoJoinRuleConfig DEFAULT =
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/FilterJoinRule.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface JoinConditionPushRuleConfig extends FilterJoinRule.Config {
      JoinConditionPushRuleConfig DEFAULT = ImmutableJoinConditionPushRuleConfig
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewOnlyAggregateRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends MaterializedViewAggregateRule.Config {
    Config DEFAULT = create(RelFactories.LOGICAL_BUILDER);
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewProjectJoinRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends MaterializedViewRule.Config {
    Config DEFAULT = ImmutableMaterializedViewProjectJoinRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewProjectFilterRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends MaterializedViewRule.Config {
    Config DEFAULT = ImmutableMaterializedViewProjectFilterRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewOnlyJoinRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends MaterializedViewRule.Config {
    Config DEFAULT = ImmutableMaterializedViewOnlyJoinRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewProjectAggregateRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends MaterializedViewAggregateRule.Config {
    Config DEFAULT = create(RelFactories.LOGICAL_BUILDER);
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewOnlyFilterRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends MaterializedViewRule.Config {
    Config DEFAULT = ImmutableMaterializedViewOnlyFilterRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/convert/TraitMatchingRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    @Override default TraitMatchingRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/convert/ConverterRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config INSTANCE = ImmutableConverterRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/rules/SubQueryRemoveRule.java`
#### Snippet
```java
  }
  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config PROJECT = ImmutableSubQueryRemoveRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/rel/metadata/janino/RelMetadataHandlerGeneratorUtil.java`
#### Snippet
```java

  /** Contains Name and code that been generated for {@link MetadataHandler}.*/
  @Value.Immutable(singleton = false)
  public interface HandlerNameAndGeneratedCode {
    String getHandlerName();
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidator.java`
#### Snippet
```java
   * Provides methods to set each configuration option.
   */
  @Value.Immutable(singleton = false)
  public interface Config {
    /** Default configuration. */
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/tools/Hoist.java`
#### Snippet
```java

  /** Configuration. */
  @Value.Immutable(singleton = false)
  public interface Config {
    /** Returns the configuration for the SQL parser. */
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
     * <p>Extends {@link RelDecorrelator.Config} because rule needs a
     * decorrelator instance. */
    @Value.Immutable(singleton = false)
    public interface RemoveCorrelationForScalarAggregateRuleConfig extends RelDecorrelator.Config {
      @Override default RemoveCorrelationForScalarAggregateRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface AdjustProjectForCountAggregateRuleConfig extends RelDecorrelator.Config {
      @Override default AdjustProjectForCountAggregateRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
     * <p>Extends {@link RelDecorrelator.Config} because rule needs a
     * decorrelator instance. */
    @Value.Immutable(singleton = false)
    public interface RemoveCorrelationForScalarProjectRuleConfig extends RelDecorrelator.Config {
      @Override default RemoveCorrelationForScalarProjectRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface RemoveSingleAggregateRuleConfig extends RelRule.Config {
      @Override default RemoveSingleAggregateRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `file/src/main/java/org/apache/calcite/adapter/file/CsvProjectTableScanRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config DEFAULT = ImmutableCsvProjectTableScanRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface DruidFilterRuleConfig extends RelRule.Config {
      DruidFilterRuleConfig DEFAULT = ImmutableDruidFilterRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface DruidSortRuleConfig extends RelRule.Config {
      DruidSortRuleConfig DEFAULT = ImmutableDruidSortRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface DruidHavingFilterRuleConfig extends RelRule.Config {
      DruidHavingFilterRuleConfig DEFAULT = ImmutableDruidHavingFilterRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface DruidProjectRuleConfig extends RelRule.Config {
      DruidProjectRuleConfig DEFAULT = ImmutableDruidProjectRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface DruidAggregateProjectRuleConfig extends RelRule.Config {
      DruidAggregateProjectRuleConfig DEFAULT = ImmutableDruidAggregateProjectRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface DruidAggregateRuleConfig extends RelRule.Config {
      DruidAggregateRuleConfig DEFAULT = ImmutableDruidAggregateRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface DruidPostAggregationProjectRuleConfig extends RelRule.Config {
      DruidPostAggregationProjectRuleConfig DEFAULT =
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface GeodeSortLimitRuleConfig extends RelRule.Config {
      @Override default GeodeSortLimitRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface GeodeFilterRuleConfig extends RelRule.Config {
      @Override default GeodeFilterRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
   * @see SqlToRelConverter#CONFIG
   */
  @Value.Immutable(singleton = false)
  public interface Config {
    /** Returns the {@code decorrelationEnabled} option. Controls whether to
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface InnodbSortTableScanRuleConfig extends RelRule.Config {
      InnodbSortTableScanRuleConfig DEFAULT = ImmutableInnodbSortTableScanRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface InnodbSortFilterRuleConfig extends RelRule.Config {
      InnodbSortFilterRuleConfig DEFAULT = ImmutableInnodbSortFilterRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbRules.java`
#### Snippet
```java

    /** Rule configuration. */
    @Value.Immutable(singleton = false)
    public interface InnodbFilterRuleConfig extends RelRule.Config {
      InnodbFilterRuleConfig DEFAULT = ImmutableInnodbFilterRuleConfig.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `piglet/src/main/java/org/apache/calcite/piglet/PigToSqlAggregateRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    @Override default PigToSqlAggregateRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    @Override default SplunkPushDownRule toRule() {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvProjectTableScanRule.java`
#### Snippet
```java

  /** Rule configuration. */
  @Value.Immutable(singleton = false)
  public interface Config extends RelRule.Config {
    Config DEFAULT = ImmutableCsvProjectTableScanRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
  /** Example of a UDF with named parameters, some of them optional. */
  public static class MyAbcdeFunction {
    public String eval(@Parameter(name = "A", optional = false) Integer a,
        @Parameter(name = "B", optional = true) Integer b,
        @Parameter(name = "C", optional = false) Integer c,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    public String eval(@Parameter(name = "A", optional = false) Integer a,
        @Parameter(name = "B", optional = true) Integer b,
        @Parameter(name = "C", optional = false) Integer c,
        @Parameter(name = "D", optional = true) Integer d,
        @Parameter(name = "E", optional = true) Integer e) {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/AbstractRelNodeGetRelTypeNameBenchmark.java`
#### Snippet
```java
 */
@Fork(value = 1, jvmArgsPrepend = "-Xmx1024m")
@Measurement(iterations = 10, time = 1, timeUnit = TimeUnit.SECONDS)
@Warmup(iterations = 10, time = 1, timeUnit = TimeUnit.SECONDS)
@Threads(1)
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/AbstractRelNodeGetRelTypeNameBenchmark.java`
#### Snippet
```java
@Fork(value = 1, jvmArgsPrepend = "-Xmx1024m")
@Measurement(iterations = 10, time = 1, timeUnit = TimeUnit.SECONDS)
@Warmup(iterations = 10, time = 1, timeUnit = TimeUnit.SECONDS)
@Threads(1)
@OutputTimeUnit(TimeUnit.NANOSECONDS)
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/ParserBenchmark.java`
#### Snippet
```java
 */
@Fork(value = 1, jvmArgsPrepend = "-Xmx128m")
@Measurement(iterations = 7, time = 1, timeUnit = TimeUnit.SECONDS)
@Warmup(iterations = 7, time = 1, timeUnit = TimeUnit.SECONDS)
@State(Scope.Thread)
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/ParserBenchmark.java`
#### Snippet
```java
@Fork(value = 1, jvmArgsPrepend = "-Xmx128m")
@Measurement(iterations = 7, time = 1, timeUnit = TimeUnit.SECONDS)
@Warmup(iterations = 7, time = 1, timeUnit = TimeUnit.SECONDS)
@State(Scope.Thread)
@Threads(1)
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
    assert operands.get(1) instanceof RexInputRef;
    int idx = ((RexInputRef) operands.get(0)).getIndex();
    if (idx < nFields) {
      if (!keys.contains(idx)) {
        return null;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `core/src/main/java/org/apache/calcite/rel/rules/SemiJoinJoinTransposeRule.java`
#### Snippet
```java
    final RexNode newSemiJoinFilter;
    int[] adjustments = new int[nTotalFields];
    if (nKeysFromX > 0) {
      // (X, Y, Z) --> (X, Z, Y)
      // semiJoin(X, Z)
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/TypeCoercionImpl.java`
#### Snippet
```java
      if (lNode.getKind() == SqlKind.LITERAL) {
        BigDecimal val = ((SqlLiteral) lNode).getValueAs(BigDecimal.class);
        if (val.compareTo(BigDecimal.ONE) == 0) {
          SqlNode lNode1 = SqlLiteral.createBoolean(true, SqlParserPos.ZERO);
          binding.getCall().setOperand(0, lNode1);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/TypeCoercionImpl.java`
#### Snippet
```java
      if (rNode.getKind() == SqlKind.LITERAL) {
        BigDecimal val = ((SqlLiteral) rNode).getValueAs(BigDecimal.class);
        if (val.compareTo(BigDecimal.ONE) == 0) {
          SqlNode rNode1 = SqlLiteral.createBoolean(true, SqlParserPos.ZERO);
          binding.getCall().setOperand(1, rNode1);
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java

    @Override public @Nullable Object[] current() {
      if (list.size() > 0) {
        return takeOne();
      } else {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
        final List<Edge> edges = graph.getInwardEdges(vertex);
        MutableNode node;
        if (root == null) {
          if (!edges.isEmpty()) {
            throw new RuntimeException("root node must not have relationships: "
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `plus/src/main/java/org/apache/calcite/adapter/os/VmstatTableFunction.java`
#### Snippet
```java
                      return 0;
                    }
                    if (value.endsWith(".")) {
                      return Long.parseLong(value);
                    }
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
    if (isTimestampColumn || ColumnMetaData.Rep.JAVA_SQL_TIMESTAMP == type) {
      final int fieldPos = posTimestampField != -1 ? posTimestampField : i;
      if (token == JsonToken.VALUE_NUMBER_INT) {
        rowBuilder.set(posTimestampField, parser.getLongValue());
        return;
```

## RuleId[id=SlowListContainsAll]
### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `core/src/main/java/org/apache/calcite/sql/SqlNodeList.java`
#### Snippet
```java

  @Override public boolean containsAll(Collection<?> c) {
    return list.containsAll(c);
  }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    for (RexNode notDisjunction : notTerms) {
      final List<RexNode> terms2 = RelOptUtil.conjunctions(notDisjunction);
      if (!terms.containsAll(terms2)) {
        // may be satisfiable ==> check other terms
        continue;
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      return;
    }
    if (!foo0.variables.containsAll(foo1.variables)) {
      throw new AssertionError("variable mismatch: "
          + before + " has " + foo0.variables + ", "
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      final List<RexNode> disjunctions2 =
          RelOptUtil.conjunctions(notDisjunction);
      if (disjunctions.containsAll(disjunctions2)) {
        return false;
      }
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTable.java`
#### Snippet
```java
    final List<String> fieldNames = rowType.getFieldNames();
    Preconditions.checkArgument(fieldNames.contains(timestampFieldName));
    Preconditions.checkArgument(fieldNames.containsAll(metricFieldNames));
    return rowType;
  }
```

## RuleId[id=SlowAbstractSetRemoveAll]
### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectFilterTransposeRule.java`
#### Snippet
```java
        }
      }
      set.removeAll(list);
      list.addAll(set);
      builder.project(list);
```

### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSubset.java`
#### Snippet
```java
        visit(oldInput);
      }
      activeNodes.removeAll(p.getInputs());
      return false;
    }
```

### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
                originalRoot, materializations));
    for (Pair<RelNode, List<RelOptMaterialization>> use : materializationUses) {
      applicableMaterializations.removeAll(use.right);
    }
    for (RelOptMaterialization materialization : applicableMaterializations) {
```

### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `core/src/main/java/org/apache/calcite/util/graph/TopologicalOrderIterator.java`
#### Snippet
```java
      }
    }
    countMap.keySet().removeAll(empties);
  }

```

### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `core/src/main/java/org/apache/calcite/runtime/Matcher.java`
#### Snippet
```java
      final Set<String> predicateSymbolsNotInGraph =
          Sets.newTreeSet(symbolPredicates.keySet());
      predicateSymbolsNotInGraph.removeAll(automaton.symbolNames);
      if (!predicateSymbolsNotInGraph.isEmpty()) {
        throw new IllegalArgumentException("not all predicate symbols ["
```

## RuleId[id=ConditionalExpressionWithIdenticalBranches]
### ConditionalExpressionWithIdenticalBranches
Conditional expression `isFloat ? null : null` with identical branches
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
            isFloat ? "3.402823E38" : "1.797693134862316E308", castType);
        f.checkCastToString(numeric.minNumericString, null,
            isFloat ? null : null, castType); // Treated as FLOAT or DOUBLE
        f.checkCastToString(numeric.minNumericString, type,
            isFloat ? "1.401298E-45" : null, castType);
```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/rel/metadata/JaninoRelMetadataProvider.java`
#### Snippet
```java
  static  <MH extends MetadataHandler<?>> MH compile(String className,
      String generatedCode, Class<MH> handlerClass,
      List<? extends Object> argList) throws CompileException, IOException {
    final ICompilerFactory compilerFactory;
    ClassLoader classLoader =
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java

  @SuppressWarnings("unchecked")
  private static <T extends Object> T get(Map<String, ? extends @Nullable Object> map,
      String key) {
    return (T) requireNonNull(map.get(key), () -> "entry for key " + key);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/SqlCallBinding.java`
#### Snippet
```java
  }

  private <T extends Object> @Nullable T valueAs(SqlNode node, Class<T> clazz) {
    final SqlLiteral literal;
    switch (node.getKind()) {
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/SqlCallBinding.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T getOperandLiteralValue(int ordinal,
      Class<T> clazz) {
    final SqlNode node = operand(ordinal);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/SqlOperatorBinding.java`
#### Snippet
```java
   * @return value of operand
   */
  public <T extends Object> @Nullable T getOperandLiteralValue(int ordinal, Class<T> clazz) {
    throw new UnsupportedOperationException();
  }
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/SqlLiteral.java`
#### Snippet
```java
   * @throws AssertionError if the value type is not supported
   */
  public <T extends Object> T getValueAs(Class<T> clazz) {
    Object value = this.value;
    if (clazz.isInstance(value)) {
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/SqlAggFunction.java`
#### Snippet
```java
  //~ Methods ----------------------------------------------------------------

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    return clazz.isInstance(this) ? clazz.cast(this) : null;
  }
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlAnyValueAggFunction.java`
#### Snippet
```java
  //~ Methods ----------------------------------------------------------------

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(SqlSplittableAggFunction.SelfSplitter.INSTANCE)) {
      return clazz.cast(SqlSplittableAggFunction.SelfSplitter.INSTANCE);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCountAggFunction.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(SqlSplittableAggFunction.CountSplitter.INSTANCE)) {
      return clazz.cast(SqlSplittableAggFunction.CountSplitter.INSTANCE);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSumAggFunction.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(SqlSplittableAggFunction.SumSplitter.INSTANCE)) {
      return clazz.cast(SqlSplittableAggFunction.SumSplitter.INSTANCE);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSingleValueAggFunction.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(SqlSplittableAggFunction.SelfSplitter.INSTANCE)) {
      return clazz.cast(SqlSplittableAggFunction.SelfSplitter.INSTANCE);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSumEmptyIsZeroAggFunction.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(SqlSplittableAggFunction.Sum0Splitter.INSTANCE)) {
      return clazz.cast(SqlSplittableAggFunction.Sum0Splitter.INSTANCE);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlBitOpAggFunction.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(SqlSplittableAggFunction.SelfSplitter.INSTANCE)) {
      return clazz.cast(SqlSplittableAggFunction.SelfSplitter.INSTANCE);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlBasicAggFunction.java`
#### Snippet
```java
  //~ Methods ----------------------------------------------------------------

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(staticFun)) {
      return clazz.cast(staticFun);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlGroupingFunction.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(STATIC)) {
      return clazz.cast(STATIC);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlMinMaxAggFunction.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(SqlSplittableAggFunction.SelfSplitter.INSTANCE)) {
      return clazz.cast(SqlSplittableAggFunction.SelfSplitter.INSTANCE);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingNamespace.java`
#### Snippet
```java
  }

  @Override public <T extends Object> T unwrap(Class<T> clazz) {
    if (clazz.isInstance(this)) {
      return clazz.cast(this);
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingSqlValidatorCatalogReader.java`
#### Snippet
```java
  }

  @Override public <C extends Object> @Nullable C unwrap(Class<C> aClass) {
    return catalogReader.unwrap(aClass);
  }
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/validate/AbstractNamespace.java`
#### Snippet
```java
  }

  @Override public <T extends Object> T unwrap(Class<T> clazz) {
    return clazz.cast(this);
  }
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlNonNullableAccessors.java`
#### Snippet
```java

  @API(since = "1.27", status = API.Status.EXPERIMENTAL)
  public static <T extends Object> T getOperandLiteralValueOrThrow(SqlOperatorBinding opBinding,
      int ordinal, Class<T> clazz) {
    return requireNonNull(opBinding.getOperandLiteralValue(ordinal, clazz),
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorNamespace.java`
#### Snippet
```java
   * @throws ClassCastException if no such interface is available
   */
  <T extends Object> T unwrap(Class<T> clazz);

  /**
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/jdbc/CalciteSchema.java`
#### Snippet
```java
    }

    @Override public <T extends Object> T unwrap(Class<T> clazz) {
      if (clazz.isInstance(this)) {
        return clazz.cast(this);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/plan/Contexts.java`
#### Snippet
```java
    }

    @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
      if (clazz.isInstance(target)) {
        return clazz.cast(target);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/plan/Contexts.java`
#### Snippet
```java
  /** Empty context. */
  static class EmptyContext implements Context {
    @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
      return null;
    }
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/plan/Contexts.java`
#### Snippet
```java
    }

    @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
      for (Context context : contexts) {
        final T t = context.unwrap(clazz);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/plan/RelOptAbstractTable.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    return clazz.isInstance(this)
        ? clazz.cast(this)
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/plan/RelRule.java`
#### Snippet
```java

    /** Casts this configuration to another type, usually a sub-class. */
    default <T extends Object> T as(Class<T> class_) {
      if (class_.isAssignableFrom(this.getClass())) {
        return class_.cast(this);
```

### TypeParameterExtendsObject
Type parameter `E` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
   */
  public static <R extends ReflectiveVisitor,
      E extends Object> ReflectiveVisitDispatcher<R, E> createDispatcher(
      final Class<R> visitorBaseClazz,
      final Class<E> visiteeBaseClazz) {
```

### TypeParameterExtendsObject
Type parameter `E` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
   * @param otherArgClasses Types of remaining arguments
   */
  public static <E extends Object, T> MethodDispatcher<T> createMethodDispatcher(
      final Class<T> returnClazz,
      final ReflectiveVisitor visitor,
```

### TypeParameterExtendsObject
Type parameter `E` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/util/Filterator.java`
#### Snippet
```java
 * @param <E> Element type
 */
public class Filterator<E extends Object> implements Iterator<E> {
  //~ Instance fields --------------------------------------------------------

```

### TypeParameterExtendsObject
Type parameter `E` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/util/ReflectiveVisitDispatcher.java`
#### Snippet
```java
 */
public interface ReflectiveVisitDispatcher<R extends ReflectiveVisitor,
    E extends Object> {
  //~ Methods ----------------------------------------------------------------

```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/util/graph/DefaultEdge.java`
#### Snippet
```java
  }

  public static <V extends Object> DirectedGraph.EdgeFactory<V, DefaultEdge> factory() {
    // see https://github.com/typetools/checker-framework/issues/3637
    //noinspection Convert2MethodRef
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/util/graph/Graphs.java`
#### Snippet
```java
   * @param <E> Edge type
   */
  public static class FrozenGraph<V extends Object, E extends DefaultEdge> {
    private final DefaultDirectedGraph<V, E> graph;
    private final Map<Pair<V, V>, int[]> shortestDistances;
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
   * <p>Equivalent to the Elvis operator ({@code ?:}) of languages such as
   * Groovy or PHP. */
  public static <T extends Object> @PolyNull T first(@Nullable T v0, @PolyNull T v1) {
    return v0 != null ? v0 : v1;
  }
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/schema/SchemaPlus.java`
#### Snippet
```java

  /** Returns an underlying object. */
  <T extends Object> @Nullable T unwrap(Class<T> clazz);

  void setPath(ImmutableList<ImmutableList<String>> path);
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/schema/Wrapper.java`
#### Snippet
```java
   * that interface. */
  @API(since = "1.27", status = API.Status.INTERNAL)
  default <C extends Object> C unwrapOrThrow(Class<C> aClass) {
    return requireNonNull(unwrap(aClass),
        () -> "Can't unwrap " + aClass + " from " + this);
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/schema/Wrapper.java`
#### Snippet
```java
  /** Finds an instance of an interface implemented by this object,
   * or returns null if this object does not support that interface. */
  <C extends Object> @Nullable C unwrap(Class<C> aClass);

  /** Finds an instance of an interface implemented by this object,
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/schema/Wrapper.java`
#### Snippet
```java
   * that interface. */
  @API(since = "1.27", status = API.Status.INTERNAL)
  default <C extends Object> Optional<C> maybeUnwrap(Class<C> aClass) {
    return Optional.ofNullable(unwrap(aClass));
  }
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/schema/impl/ModifiableViewTable.java`
#### Snippet
```java
  }

  @Override public <C extends Object> @Nullable C unwrap(Class<C> aClass) {
    if (aClass.isInstance(initializerExpressionFactory)) {
      return aClass.cast(initializerExpressionFactory);
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/schema/impl/AbstractTable.java`
#### Snippet
```java
  }

  @Override public <C extends Object> @Nullable C unwrap(Class<C> aClass) {
    if (aClass.isInstance(this)) {
      return aClass.cast(this);
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcTable.java`
#### Snippet
```java
  }

  @Override public <C extends Object> @Nullable C unwrap(Class<C> aClass) {
    if (aClass.isInstance(jdbcSchema.getDataSource())) {
      return aClass.cast(jdbcSchema.getDataSource());
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
    }

    @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
      return null;
    }
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
  }

  @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
    if (clazz.isInstance(this)) {
      return clazz.cast(this);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
          new RelOptTableImpl(this.schema, b.build(), this.names, this.table,
              this.tableExpressionFactory, this.rowCount) {
            @Override public <T extends Object> @Nullable T unwrap(Class<T> clazz) {
              if (clazz.isAssignableFrom(InitializerExpressionFactory.class)) {
                return clazz.cast(NullInitializerExpressionFactory.INSTANCE);
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/prepare/CalciteCatalogReader.java`
#### Snippet
```java
  }

  @Override public <C extends Object> @Nullable C unwrap(Class<C> aClass) {
    if (aClass.isInstance(this)) {
      return aClass.cast(this);
```

### TypeParameterExtendsObject
Type parameter `E` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
  /** Returns a list that consists of a given list plus an element, guaranteed
   * to be an {@link ImmutableList}. */
  public static <E extends Object> ImmutableList<E> append(ImmutableList<E> list, E e) {
    return ImmutableList.<E>builder().addAll(list).add(e).build();
  }
```

### TypeParameterExtendsObject
Type parameter `K` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
  /** Returns a map that consists of a given map plus an (key, value),
   * guaranteed to be an {@link ImmutableMap}. */
  public static <K extends Object, V extends Object> ImmutableMap<K, V> append(
      Map<K, V> map, K k, V v) {
    final ImmutableMap.Builder<K, V> builder = ImmutableMap.builder();
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
  /** Returns a map that consists of a given map plus an (key, value),
   * guaranteed to be an {@link ImmutableMap}. */
  public static <K extends Object, V extends Object> ImmutableMap<K, V> append(
      Map<K, V> map, K k, V v) {
    final ImmutableMap.Builder<K, V> builder = ImmutableMap.builder();
```

### TypeParameterExtendsObject
Type parameter `E` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

  /** Returns a list that consists of a given list plus an element. */
  public static <E extends Object> List<E> append(List<E> list, E e) {
    if (list instanceof AbstractFlatList) {
      //noinspection unchecked
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
  }

  private static <T extends Object> @Nullable T unwrap(@Nullable Object o, Class<T> clazz) {
    if (o instanceof Wrapper) {
      return ((Wrapper) o).unwrap(clazz);
```

### TypeParameterExtendsObject
Type parameter `K` explicitly extends 'java.lang.Object'
in `linq4j/src/main/java/org/apache/calcite/linq4j/GroupingImpl.java`
#### Snippet
```java
 */
@SuppressWarnings("type.argument.type.incompatible")
class GroupingImpl<K extends Object, V> extends AbstractEnumerable<V>
    implements Grouping<K, V>, Map.Entry<K, Enumerable<V>> {
  private final K key;
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/calcite/server/MaterializedViewTable.java`
#### Snippet
```java
  }

  @Override public <C extends Object> @Nullable C unwrap(Class<C> aClass) {
    if (MaterializationKey.class.isAssignableFrom(aClass)
        && aClass.isInstance(key)) {
```

### TypeParameterExtendsObject
Type parameter `C` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/calcite/server/MutableArrayTable.java`
#### Snippet
```java
  }

  @Override public <C extends Object> @Nullable C unwrap(Class<C> aClass) {
    if (aClass.isInstance(initializerExpressionFactory)) {
      return aClass.cast(initializerExpressionFactory);
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/src/main/java/org/apache/calcite/rex/RexInterpreter.java`
#### Snippet
```java
    case AND:
      return values.stream().map(Truthy::of).min(Comparator.naturalOrder())
          .get().toComparable();
    case OR:
      return values.stream().map(Truthy::of).max(Comparator.naturalOrder())
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/src/main/java/org/apache/calcite/rex/RexInterpreter.java`
#### Snippet
```java
    case OR:
      return values.stream().map(Truthy::of).max(Comparator.naturalOrder())
          .get().toComparable();
    case NOT:
      return not(values.get(0));
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `plus/src/main/java/org/apache/calcite/adapter/tpcds/TpcdsSchema.java`
#### Snippet
```java
      case DECIMAL:
        return typeFactory.createSqlType(SqlTypeName.DECIMAL,
            type.getPrecision().get(), type.getScale().get());
      case VARCHAR:
        return typeFactory.createSqlType(SqlTypeName.VARCHAR,
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `plus/src/main/java/org/apache/calcite/adapter/tpcds/TpcdsSchema.java`
#### Snippet
```java
      case DECIMAL:
        return typeFactory.createSqlType(SqlTypeName.DECIMAL,
            type.getPrecision().get(), type.getScale().get());
      case VARCHAR:
        return typeFactory.createSqlType(SqlTypeName.VARCHAR,
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `plus/src/main/java/org/apache/calcite/adapter/tpcds/TpcdsSchema.java`
#### Snippet
```java
      case VARCHAR:
        return typeFactory.createSqlType(SqlTypeName.VARCHAR,
            type.getPrecision().get());
      case CHAR:
        return typeFactory.createSqlType(SqlTypeName.CHAR,
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `plus/src/main/java/org/apache/calcite/adapter/tpcds/TpcdsSchema.java`
#### Snippet
```java
      case CHAR:
        return typeFactory.createSqlType(SqlTypeName.CHAR,
            type.getPrecision().get());
      default:
        throw new AssertionError(type.getBase() + ": " + column);
```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `condition != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/core/Join.java`
#### Snippet
```java
      return litmus.fail("field count mismatch");
    }
    if (condition != null) {
      if (condition.getType().getSqlTypeName() != SqlTypeName.BOOLEAN) {
        return litmus.fail("condition must be boolean: {}",
```

### ConstantValue
Condition `cluster != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/AbstractRelNode.java`
#### Snippet
```java
  protected AbstractRelNode(RelOptCluster cluster, RelTraitSet traitSet) {
    super();
    assert cluster != null;
    this.cluster = cluster;
    this.traitSet = traitSet;
```

### ConstantValue
Condition `rowType != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/AbstractRelNode.java`
#### Snippet
```java
    if (rowType == null) {
      rowType = deriveRowType();
      assert rowType != null : this;
    }
    return rowType;
```

### ConstantValue
Condition `traitSet == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/AbstractRelNode.java`
#### Snippet
```java
  @Override public final @Nullable Convention getConvention(
      @UnknownInitialization AbstractRelNode this) {
    return traitSet == null ? null : traitSet.getTrait(ConventionTraitDef.INSTANCE);
  }

```

### ConstantValue
Condition `repeatableSeed != null` is always `true` when reached
in `core/src/main/java/org/apache/calcite/rel/core/Sample.java`
#### Snippet
```java
    return new RelOptSamplingParameters(
        "bernoulli".equals(mode), percentage, repeatable,
        repeatable && repeatableSeed != null ? ((Number) repeatableSeed).intValue() : 0);
  }

```

### ConstantValue
Condition `rowType != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/core/Window.java`
#### Snippet
```java
    super(cluster, traitSet, input);
    this.constants = ImmutableList.copyOf(constants);
    assert rowType != null;
    this.rowType = rowType;
    this.groups = ImmutableList.copyOf(groups);
```

### ConstantValue
Condition `d != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/core/TableFunctionScan.java`
#### Snippet
```java
    for (RelNode input : inputs) {
      Double d = mq.getRowCount(input);
      if (d != null) {
        nRows += d;
      }
```

### ConstantValue
Condition `rowCount == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/core/Intersect.java`
#### Snippet
```java
    for (RelNode input : inputs) {
      Double rowCount = mq.getRowCount(input);
      if (rowCount == null) {
        // Assume this input does not reduce row count
        continue;
```

### ConstantValue
Condition `deriveRowType() != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/core/Uncollect.java`
#### Snippet
```java
    this.withOrdinality = withOrdinality;
    this.itemAliases = ImmutableList.copyOf(itemAliases);
    assert deriveRowType() != null : "invalid child rowtype";
  }

```

### ConstantValue
Condition `restartCount == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/core/Correlate.java`
#### Snippet
```java

    Double restartCount = mq.getRowCount(getLeft());
    if (restartCount == null) {
      return planner.getCostFactory().makeInfiniteCost();
    }
```

### ConstantValue
Condition `rowType != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/core/Project.java`
#### Snippet
```java
      Set<CorrelationId> variableSet) {
    super(cluster, traits, input);
    assert rowType != null;
    this.exps = ImmutableList.copyOf(projects);
    this.hints = ImmutableList.copyOf(hints);
```

### ConstantValue
Condition `fieldName == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/core/Project.java`
#### Snippet
```java
      for (Ord<RelDataTypeField> field : Ord.zip(getRowType().getFieldList())) {
        String fieldName = field.e.getName();
        if (fieldName == null) {
          fieldName = "field#" + field.i;
        }
```

### ConstantValue
Condition `predicates != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/hint/CompositeHintPredicate.java`
#### Snippet
```java
   */
  CompositeHintPredicate(Composition composition, HintPredicate... predicates) {
    assert predicates != null;
    assert predicates.length > 1;
    for (HintPredicate predicate : predicates) {
```

### ConstantValue
Condition `protoType != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java
   */
  public static RelProtoDataType proto(final RelDataType protoType) {
    assert protoType != null;
    return typeFactory -> typeFactory.copyType(protoType);
  }
```

### ConstantValue
Condition `typeName != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java
  public static RelProtoDataType proto(final SqlTypeName typeName,
      final int precision, final boolean nullable) {
    assert typeName != null;
    return typeFactory -> {
      final RelDataType type = typeFactory.createSqlType(typeName, precision);
```

### ConstantValue
Condition `typeName != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java
  public static RelProtoDataType proto(final SqlTypeName typeName,
      final boolean nullable) {
    assert typeName != null;
    return typeFactory -> {
      final RelDataType type = typeFactory.createSqlType(typeName);
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java
  @Override public @Nullable SqlIdentifier getSqlIdentifier() {
    SqlTypeName typeName = getSqlTypeName();
    if (typeName == null) {
      return null;
    }
```

### ConstantValue
Condition `values != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rules/ValuesReduceRule.java`
#### Snippet
```java
  protected void apply(RelOptRuleCall call, @Nullable LogicalProject project,
      @Nullable LogicalFilter filter, LogicalValues values) {
    assert values != null;
    assert filter != null || project != null;
    final RexNode conditionExpr =
```

### ConstantValue
Result of `bestKeyOrder.size()` is always '0'
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
    final List<Integer> truncatedLeftKeys;
    final List<Integer> truncatedRightKeys;
    if (actualLeftKeys.size() == bestKeyOrder.size()) {
      truncatedLeftKeys = leftKeys;
      truncatedRightKeys = rightKeys;
```

### ConstantValue
Condition `dimSortCost == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
    final Double dupRemCost = uniq ? 0 : dimSortCost;
    final RelOptCost dimCost = mq.getCumulativeCost(dimRel);
    if ((dimSortCost == null)
        || (dupRemCost == null)
        || (dimCost == null)) {
```

### ConstantValue
Condition `dupRemCost == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
    final RelOptCost dimCost = mq.getCumulativeCost(dimRel);
    if ((dimSortCost == null)
        || (dupRemCost == null)
        || (dimCost == null)) {
      return 0;
```

### ConstantValue
Condition `typeName != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactoryImpl.java`
#### Snippet
```java
    assert SqlTypeUtil.isNumeric(type) || SqlTypeUtil.isNull(type);
    SqlTypeName typeName = type.getSqlTypeName();
    assert typeName != null;
    switch (typeName) {
    case DECIMAL:
```

### ConstantValue
Condition `types != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactoryImpl.java`
#### Snippet
```java
  // implement RelDataTypeFactory
  @Override public RelDataType createJoinType(RelDataType... types) {
    assert types != null;
    assert types.length >= 1;
    final List<RelDataType> flattenedTypes = new ArrayList<>();
```

### ConstantValue
Condition `rexList2 != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java
      }

      if (rexList2 != null) {
        match = rexList2.indexOf(rex);
        if (match >= 0) {
```

### ConstantValue
Condition `preserveRight != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java
          } else if (requireNonNull(rightFields, "rightFields").contains(exprArgs)
              && isStrong(exprArgs, call)) {
            assert preserveRight != null;
            if (!preserveRight.contains(call)) {
              preserveRight.add(call);
```

### ConstantValue
Condition `inputs != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rules/PruneEmptyRules.java`
#### Snippet
```java
          final Union union = call.rel(0);
          final List<RelNode> inputs = union.getInputs();
          assert inputs != null;
          final RelBuilder relBuilder = call.builder();
          int nonEmptyInputs = 0;
```

### ConstantValue
Condition `inputs != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rules/PruneEmptyRules.java`
#### Snippet
```java
          final Minus minus = call.rel(0);
          final List<RelNode> inputs = minus.getInputs();
          assert inputs != null;
          int nonEmptyInputs = 0;
          final RelBuilder relBuilder = call.builder();
```

### ConstantValue
Result of `aggregateCall.isDistinct()` is always 'false'
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateFilterTransposeRule.java`
#### Snippet
```java
        }
        topAggCallList.add(
            AggregateCall.create(rollup, aggregateCall.isDistinct(),
                aggregateCall.isApproximate(), aggregateCall.ignoreNulls(),
                aggregateCall.rexList, ImmutableList.of(i++), -1,
```

### ConstantValue
Result of `origCall.isDistinct()` is always 'false'
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateUnionTransposeRule.java`
#### Snippet
```java
      }
      AggregateCall newCall =
          AggregateCall.create(aggFun, origCall.isDistinct(),
              origCall.isApproximate(), origCall.ignoreNulls(),
              origCall.rexList, ImmutableList.of(groupCount + ord.i), -1,
```

### ConstantValue
Value `newGroupingSets` is always 'null'
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
    relBuilder.push(
        aggregate.copy(aggregate.getTraitSet(), relBuilder.build(),
            newGroupSet, newGroupingSets, aggCallList));

    // If there's no left child yet, no need to create the join
```

### ConstantValue
Condition `leftRowCount != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rules/LoptOptimizeJoinRule.java`
#### Snippet
```java
    // or if it has the same number of rows as the right (excluding
    // roundoff), but fewer columns.
    if ((leftRowCount != null)
        && (rightRowCount != null)
        && ((leftRowCount < rightRowCount)
```

### ConstantValue
Condition `rightRowCount != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rules/LoptOptimizeJoinRule.java`
#### Snippet
```java
    // roundoff), but fewer columns.
    if ((leftRowCount != null)
        && (rightRowCount != null)
        && ((leftRowCount < rightRowCount)
        || ((Math.abs(leftRowCount - rightRowCount) < RelOptUtil.EPSILON)
```

### ConstantValue
Condition `joinType == JoinRelType.RIGHT` is always `false` when reached
in `core/src/main/java/org/apache/calcite/rel/rules/LoptOptimizeJoinRule.java`
#### Snippet
```java
    // outer join condition
    RexNode condition;
    if ((joinType == JoinRelType.LEFT) || (joinType == JoinRelType.RIGHT)) {
      condition =
          requireNonNull(multiJoin.getOuterJoinCond(factorToAdd),
```

### ConstantValue
Condition `otherTree != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rules/LoptOptimizeJoinRule.java`
#### Snippet
```java
    final List<Integer> joinOrder = new ArrayList<>();
    joinTree.getTreeOrder(joinOrder);
    if (otherTree != null) {
      otherTree.getTreeOrder(joinOrder);
    }
```

### ConstantValue
Condition `filterFactory == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/rules/FilterProjectTransposeRule.java`
#### Snippet
```java
                        .anyInputs()))
        .as(Config.class)
        .withCopyFilter(filterFactory == null)
        .withCopyProject(projectFactory == null));
  }
```

### ConstantValue
Condition `projectFactory == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/rules/FilterProjectTransposeRule.java`
#### Snippet
```java
        .as(Config.class)
        .withCopyFilter(filterFactory == null)
        .withCopyProject(projectFactory == null));
  }

```

### ConstantValue
Condition `input != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableMultiRel.java`
#### Snippet
```java
  @Override public void setInput(int ordinalInParent, MutableRel input) {
    inputs.set(ordinalInParent, input);
    if (input != null) {
      input.parent = this;
      input.ordinalInParent = ordinalInParent;
```

### ConstantValue
Condition `input != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableBiRel.java`
#### Snippet
```java
      this.right = input;
    }
    if (input != null) {
      input.parent = this;
      input.ordinalInParent = ordinalInParent;
```

### ConstantValue
Condition `input != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/mutable/MutableSingleRel.java`
#### Snippet
```java
    }
    this.input = input;
    if (input != null) {
      input.parent = this;
      input.ordinalInParent = 0;
```

### ConstantValue
Condition `tableScanNodes == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewAggregateRule.java`
#### Snippet
```java
    }
    Collection<RelNode> tableScanNodes = nodeTypes.get(TableScan.class);
    if (tableScanNodes == null) {
      return null;
    }
```

### ConstantValue
Result of `aggCall.isDistinct()` is always 'false'
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewAggregateRule.java`
#### Snippet
```java
      aggregateCalls.add(
          relBuilder.aggregateCall(rollupAgg, operand)
              .distinct(aggCall.isDistinct())
              .approximate(aggCall.isApproximate())
              .as(aggCall.name));
```

### ConstantValue
Condition `lRefs != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdTableReferences.java`
#### Snippet
```java
        Collection<RelTableRef> lRefs =
            qualifiedNamesToRefs.get(tableRef.getQualifiedName());
        if (lRefs != null) {
          shift = lRefs.size();
        }
```

### ConstantValue
Condition `lRefs != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdTableReferences.java`
#### Snippet
```java
      Collection<RelTableRef> lRefs =
          leftQualifiedNamesToRefs.get(rightRef.getQualifiedName());
      if (lRefs != null) {
        shift = lRefs.size();
      }
```

### ConstantValue
Condition `rowCount == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdRowCount.java`
#### Snippet
```java
  public @Nullable Double getRowCount(Sort rel, RelMetadataQuery mq) {
    Double rowCount = mq.getRowCount(rel.getInput());
    if (rowCount == null) {
      return null;
    }
```

### ConstantValue
Condition `rowCount == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdRowCount.java`
#### Snippet
```java
  public @Nullable Double getRowCount(EnumerableLimit rel, RelMetadataQuery mq) {
    Double rowCount = mq.getRowCount(rel.getInput());
    if (rowCount == null) {
      return null;
    }
```

### ConstantValue
Condition `partialRowCount != null` is always `true` when reached
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdRowCount.java`
#### Snippet
```java
      Double partialRowCount = mq.getRowCount(input);
      if (rowCount == null
          || partialRowCount != null && partialRowCount < rowCount) {
        rowCount = partialRowCount;
      }
```

### ConstantValue
Condition `partialRowCount == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdRowCount.java`
#### Snippet
```java
    for (RelNode input : rel.getInputs()) {
      Double partialRowCount = mq.getRowCount(input);
      if (partialRowCount == null) {
        return null;
      }
```

### ConstantValue
Condition `partialRowCount != null` is always `true` when reached
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdRowCount.java`
#### Snippet
```java
      Double partialRowCount = mq.getRowCount(input);
      if (rowCount == null
          || partialRowCount != null && partialRowCount < rowCount) {
        rowCount = partialRowCount;
      }
```

### ConstantValue
Condition `(numerator == null) || (denominator == null)` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPercentageOriginalRows.java`
#### Snippet
```java
      @PolyNull Double numerator,
      @PolyNull Double denominator) {
    if ((numerator == null) || (denominator == null)) {
      return null;
    }
```

### ConstantValue
Condition `numerator == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPercentageOriginalRows.java`
#### Snippet
```java
      @PolyNull Double numerator,
      @PolyNull Double denominator) {
    if ((numerator == null) || (denominator == null)) {
      return null;
    }
```

### ConstantValue
Condition `denominator == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPercentageOriginalRows.java`
#### Snippet
```java
      @PolyNull Double numerator,
      @PolyNull Double denominator) {
    if ((numerator == null) || (denominator == null)) {
      return null;
    }
```

### ConstantValue
Condition `rowCount == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPercentageOriginalRows.java`
#### Snippet
```java
    for (RelNode input : rel.getInputs()) {
      Double rowCount = mq.getRowCount(input);
      if (rowCount == null) {
        continue;
      }
```

### ConstantValue
Condition `relPercentage == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPercentageOriginalRows.java`
#### Snippet
```java
    Double relPercentage =
        quotientForPercentage(mq.getRowCount(rel), mq.getRowCount(child));
    if (relPercentage == null) {
      return null;
    }
```

### ConstantValue
Condition `left == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
    final Double left = mq.getRowCount(join.getLeft());
    final Double right = mq.getRowCount(join.getRight());
    if (left == null || right == null) {
      return null;
    }
```

### ConstantValue
Condition `left == null || right == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
    final Double left = mq.getRowCount(join.getLeft());
    final Double right = mq.getRowCount(join.getRight());
    if (left == null || right == null) {
      return null;
    }
```

### ConstantValue
Condition `right == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
    final Double left = mq.getRowCount(join.getLeft());
    final Double right = mq.getRowCount(join.getRight());
    if (left == null || right == null) {
      return null;
    }
```

### ConstantValue
Condition `result == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
   */
  public static @PolyNull Double validateResult(@PolyNull Double result) {
    if (result == null) {
      return null;
    }
```

### ConstantValue
Condition `(domainSize == null) || (numSelected == null)` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
      @PolyNull Double domainSize,
      @PolyNull Double numSelected) {
    if ((domainSize == null) || (numSelected == null)) {
      return domainSize;
    }
```

### ConstantValue
Condition `domainSize == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
      @PolyNull Double domainSize,
      @PolyNull Double numSelected) {
    if ((domainSize == null) || (numSelected == null)) {
      return domainSize;
    }
```

### ConstantValue
Condition `numSelected == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
      @PolyNull Double domainSize,
      @PolyNull Double numSelected) {
    if ((domainSize == null) || (numSelected == null)) {
      return domainSize;
    }
```

### ConstantValue
Condition `lRefs != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdAllPredicates.java`
#### Snippet
```java
          Collection<RelTableRef> lRefs =
              qualifiedNamesToRefs.get(rightRef.getQualifiedName());
          if (lRefs != null) {
            shift = lRefs.size();
          }
```

### ConstantValue
Condition `lRefs != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdAllPredicates.java`
#### Snippet
```java
          Collection<RelTableRef> lRefs =
              qualifiedNamesToRefs.get(rightRef.getQualifiedName());
          if (lRefs != null) {
            shift = lRefs.size();
          }
```

### ConstantValue
Condition `nRows == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdSelectivity.java`
#### Snippet
```java
    for (RelNode input : rel.getInputs()) {
      Double nRows = mq.getRowCount(input);
      if (nRows == null) {
        return null;
      }
```

### ConstantValue
Condition `e.getOrderKeys() != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java

    final List<SqlNode> orderBySqlList = new ArrayList<>();
    if (e.getOrderKeys() != null) {
      for (RelFieldCollation fc : e.getOrderKeys().getFieldCollations()) {
        if (fc.nullDirection != RelFieldCollation.NullDirection.UNSPECIFIED) {
```

### ConstantValue
Condition `hint.kvOptions != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java

  private static SqlHint toSqlHint(RelHint hint, SqlParserPos pos) {
    if (hint.kvOptions != null) {
      return new SqlHint(pos, new SqlIdentifier(hint.hintName, pos),
          SqlNodeList.of(pos, hint.kvOptions.entrySet().stream()
```

### ConstantValue
Condition `inputSet == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnOrigins.java`
#### Snippet
```java
  private static @PolyNull Set<RelColumnOrigin> createDerivedColumnOrigins(
      @PolyNull Set<RelColumnOrigin> inputSet) {
    if (inputSet == null) {
      return null;
    }
```

### ConstantValue
Condition `inputSet != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnOrigins.java`
#### Snippet
```java
          mq.getColumnOrigins(rel.getInput(), iInput);
      inputSet = createDerivedColumnOrigins(inputSet);
      if (inputSet != null) {
        set.addAll(inputSet);
      }
```

### ConstantValue
Condition `result != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQuery.java`
#### Snippet
```java
      try {
        RelOptPredicateList result = predicatesHandler.getPredicates(rel, this);
        return result != null ? result : RelOptPredicateList.EMPTY;
      } catch (MetadataHandlerProvider.NoHandler e) {
        predicatesHandler = revise(BuiltInMetadata.Predicates.Handler.class);
```

### ConstantValue
Condition `b == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQuery.java`
#### Snippet
```java
        Boolean b =
            explainVisibilityHandler.isVisibleInExplain(rel, this, explainLevel);
        return b == null || b;
      } catch (MetadataHandlerProvider.NoHandler e) {
        explainVisibilityHandler = revise(BuiltInMetadata.ExplainVisibility.Handler.class);
```

### ConstantValue
Condition `inferredPredicates.size() == 0` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPredicates.java`
#### Snippet
```java
            inferredPredicates, EMPTY_LIST);
      default:
        assert inferredPredicates.size() == 0;
        return RelOptPredicateList.EMPTY;
      }
```

### ConstantValue
Condition `info == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPredicates.java`
#### Snippet
```java
    for (Ord<RelNode> input : Ord.zip(intersect.getInputs())) {
      RelOptPredicateList info = mq.getPulledUpPredicates(input.e);
      if (info == null || info.pulledUpPredicates.isEmpty()) {
        continue;
      }
```

### ConstantValue
Condition `list2 != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdPredicates.java`
#### Snippet
```java
    for (RelNode r2 : r.getRels()) {
      RelOptPredicateList list2 = mq.getPulledUpPredicates(r2);
      if (list2 != null) {
        list = list == null ? list2 : list.union(rexBuilder, list2);
      }
```

### ConstantValue
Condition `lRefs != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdExpressionLineage.java`
#### Snippet
```java
      Collection<RelTableRef> lRefs =
          qualifiedNamesToRefs.get(rightRef.getQualifiedName());
      if (lRefs != null) {
        shift = lRefs.size();
      }
```

### ConstantValue
Condition `lRefs != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdExpressionLineage.java`
#### Snippet
```java
        Collection<RelTableRef> lRefs =
            qualifiedNamesToRefs.get(tableRef.getQualifiedName());
        if (lRefs != null) {
          shift = lRefs.size();
        }
```

### ConstantValue
Condition `nodeList != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
        final SqlSelect select = (SqlSelect) node;
        final SqlNodeList nodeList = select.getSelectList();
        if (nodeList != null) {
          for (int i = 0; i < nodeList.size(); i++) {
            final SqlNode n = nodeList.get(i);
```

### ConstantValue
Condition `rexWindow.orderKeys != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java

      List<SqlNode> orderNodes = Expressions.list();
      if (rexWindow.orderKeys != null) {
        for (RexFieldCollation rfc : rexWindow.orderKeys) {
          addOrderItem(orderNodes, program, rfc);
```

### ConstantValue
Value `allowPartial` is always 'null'
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
      final SqlWindow sqlWindow =
          SqlWindow.create(null, null, partitionList,
              orderList, isRows, lowerBound, upperBound, allowPartial, POS);

      final List<SqlNode> nodeList = toSql(program, rexOver.getOperands());
```

### ConstantValue
Value `allowPartial` is always 'null'
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
                    new SqlNodeList(partitionKeys, POS),
                    new SqlNodeList(orderByKeys, POS),
                    isRows, lowerBound, upperBound, allowPartial, POS);
        if (aggFunction.allowsFraming()) {
          lowerBound = createSqlWindowBound(group.lowerBound);
```

### ConstantValue
Condition `type != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexLocalRef.java`
#### Snippet
```java
  public RexLocalRef(int index, RelDataType type) {
    super(createName(index), index, type);
    assert type != null;
    assert index >= 0;
  }
```

### ConstantValue
Condition `name != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
      this.projectRefList.add(ref);
      final String name = outFields.get(i).getName();
      assert name != null;
      projectNameList.add(name);
    }
```

### ConstantValue
Condition `expr != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
   */
  public void addCondition(RexNode expr) {
    assert expr != null;
    RexLocalRef conditionRef = this.conditionRef;
    if (conditionRef == null) {
```

### ConstantValue
Value `assertionsEnabled` is always 'true'
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
    //noinspection AssertWithSideEffects
    assert assertionsEnabled = true;
    return assertionsEnabled;
  }

```

### ConstantValue
Condition `operator.getKind() != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexCall.java`
#### Snippet
```java
    this.operands = ImmutableList.copyOf(operands);
    this.nodeCount = RexUtil.nodeCount(1, this.operands);
    assert operator.getKind() != null : operator;
    assert operator.validRexOperands(operands.size(), Litmus.THROW) : this;
    assert operator.kind != SqlKind.IN || this instanceof RexSubQuery;
```

### ConstantValue
Value `allowFraming` is always 'false'
in `core/src/main/java/org/apache/calcite/rex/RexWindow.java`
#### Snippet
```java
      return sb.append(digest);
    } else {
      return appendDigest_(sb, allowFraming);
    }
  }
```

### ConstantValue
Condition `lowerBound == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexWindow.java`
#### Snippet
```java
    return RexUtil.nodeCount(partitionKeys)
        + RexUtil.nodeCount(Pair.left(orderKeys))
        + (lowerBound == null ? 0 : lowerBound.nodeCount())
        + (upperBound == null ? 0 : upperBound.nodeCount());
  }
```

### ConstantValue
Condition `upperBound == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexWindow.java`
#### Snippet
```java
        + RexUtil.nodeCount(Pair.left(orderKeys))
        + (lowerBound == null ? 0 : lowerBound.nodeCount())
        + (upperBound == null ? 0 : upperBound.nodeCount());
  }
}
```

### ConstantValue
Condition `literal == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
      SqlTypeName typeName,
      @PolyNull String literal) {
    if (literal == null) {
      return null;
    }
```

### ConstantValue
Condition `format != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
      default:
        // Allow fractional seconds for times and timestamps
        assert format != null;
        final DateTimeUtils.PrecisionTime ts =
            DateTimeUtils.parsePrecisionDateTimeLiteral(literal,
```

### ConstantValue
Condition `expr == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
   */
  public final @PolyNull RexNode apply(@PolyNull RexNode expr) {
    return (expr == null) ? expr : expr.accept(this);
  }
}
```

### ConstantValue
Condition `exprList == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
   */
  public final <T extends @Nullable RexNode> @PolyNull List<T> apply(@PolyNull List<T> exprList) {
    if (exprList == null) {
      return exprList;
    }
```

### ConstantValue
Condition `lowerBound == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
    final RexWindowBound lowerBound = window.getLowerBound().accept(this);
    final RexWindowBound upperBound = window.getUpperBound().accept(this);
    if (lowerBound == null
        || upperBound == null
        || !update[0]
```

### ConstantValue
Condition `upperBound == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
    final RexWindowBound upperBound = window.getUpperBound().accept(this);
    if (lowerBound == null
        || upperBound == null
        || !update[0]
        && lowerBound == window.getLowerBound()
```

### ConstantValue
Value `update` is always 'null'
in `core/src/main/java/org/apache/calcite/rex/RexCopier.java`
#### Snippet
```java
    return builder.makeCall(copy(call.getType()),
        call.getOperator(),
        visitList(call.getOperands(), update));
  }

```

### ConstantValue
Value `update` is always 'null'
in `core/src/main/java/org/apache/calcite/rex/RexCopier.java`
#### Snippet
```java
    final boolean[] update = null;
    return new RexOver(copy(over.getType()), over.getAggOperator(),
        visitList(over.getOperands(), update), visitWindow(over.getWindow()),
        over.isDistinct(), over.ignoreNulls());
  }
```

### ConstantValue
Condition `o == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java
    final RelOptCluster cluster = relInput.getCluster();
    final RexBuilder rexBuilder = cluster.getRexBuilder();
    if (o == null) {
      return null;
    // Support JSON deserializing of non-default Map classes such as gson LinkedHashMap
```

### ConstantValue
Condition `window.getLowerBound() == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java
      map.put("order", toJson(window.orderKeys));
    }
    if (window.getLowerBound() == null) {
      // No ROWS or RANGE clause
    } else if (window.getUpperBound() == null) {
```

### ConstantValue
Condition `window.getUpperBound() == null` is always `false`
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java
    if (window.getLowerBound() == null) {
      // No ROWS or RANGE clause
    } else if (window.getUpperBound() == null) {
      if (window.isRows()) {
        map.put("rows-lower", toJson(window.getLowerBound()));
```

### ConstantValue
Condition `flag != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
   */
  public RexLiteral makeFlag(Enum flag) {
    assert flag != null;
    return makeLiteral(flag,
        typeFactory.createSqlType(SqlTypeName.SYMBOL),
```

### ConstantValue
Condition `checkOverflow != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
      RexNode checkOverflow) {
    List<RexNode> args;
    if ((checkOverflow != null) && checkOverflow.isAlwaysTrue()) {
      args = ImmutableList.of(exp, checkOverflow);
    } else {
```

### ConstantValue
Value `allowCast` is always 'true'
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
          typeFactory.createTypeWithNullability(type, false);
      if (allowCast) {
        RexNode literalNotNull = makeLiteral(value, typeNotNull, allowCast);
        return makeAbstractCast(type, literalNotNull, false);
      }
```

### ConstantValue
Value `nullable` is always 'true'
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
          typeFactory.createTypeWithNullability(
              type,
              nullable);
    }
    return new RexRangeRef(type, offset);
```

### ConstantValue
Condition `o == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
   * <p>Returns null if and only if {@code o} is null. */
  private @PolyNull Object clean(@PolyNull Object o, RelDataType type) {
    if (o == null) {
      return o;
    }
```

### ConstantValue
Condition `s != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
   */
  public RexLiteral makeLiteral(String s) {
    assert s != null;
    return makePreciseStringLiteral(s);
  }
```

### ConstantValue
Condition `str != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
   */
  public RexLiteral makeCharLiteral(NlsString str) {
    assert str != null;
    RelDataType type = SqlUtil.createNlsStringType(typeFactory, str);
    return makeLiteral(str, type, SqlTypeName.CHAR);
```

### ConstantValue
Condition `s != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
   */
  protected RexLiteral makePreciseStringLiteral(String s) {
    assert s != null;
    if (s.equals("")) {
      return charEmpty;
```

### ConstantValue
Condition `args != null` is always `true`
in `core/src/main/java/org/apache/calcite/rel/metadata/CachingRelMetadataProvider.java`
#### Snippet
```java
      builder.add(method);
      builder.add(metadata.rel());
      if (args != null) {
        for (Object arg : args) {
          // Replace null values because ImmutableList does not allow them.
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/rex/RexInterpreter.java`
#### Snippet
```java
  public static @Nullable Comparable evaluate(RexNode e, Map<RexNode, Comparable> map) {
    final Comparable v = e.accept(new RexInterpreter(map));
    if (false) {
      System.out.println("evaluate " + e + " on " + map + " returns " + v);
    }
```

### ConstantValue
Condition `inputRowType == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
      @UnknownInitialization RexProgram this,
      Litmus litmus, RelNode.@Nullable Context context) {
    if (inputRowType == null) {
      return litmus.fail(null);
    }
```

### ConstantValue
Condition `exprs == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
      return litmus.fail(null);
    }
    if (exprs == null) {
      return litmus.fail(null);
    }
```

### ConstantValue
Condition `projects == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
      return litmus.fail(null);
    }
    if (projects == null) {
      return litmus.fail(null);
    }
```

### ConstantValue
Condition `outputRowType == null` is always `false`
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
      return litmus.fail(null);
    }
    if (outputRowType == null) {
      return litmus.fail(null);
    }
```

### ConstantValue
Condition `tableType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlSetSemanticsTableOperator.java`
#### Snippet
```java
    final List<SqlNode> operands = call.getOperandList();
    RelDataType tableType = validator.deriveType(scope, operands.get(0));
    assert tableType != null;
    return tableType;
  }
```

### ConstantValue
Condition `this.hints != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlTableRef.java`
#### Snippet
```java
  @Override public void unparse(SqlWriter writer, int leftPrec, int rightPrec) {
    tableName.unparse(writer, leftPrec, rightPrec);
    if (this.hints != null && this.hints.size() > 0) {
      writer.getDialect().unparseTableScanHints(writer, this.hints, leftPrec, rightPrec);
    }
```

### ConstantValue
Condition `null == operandType` is always `false`
in `core/src/main/java/org/apache/calcite/sql/SqlPostfixOperator.java`
#### Snippet
```java
      RelDataType operandType =
          validator.getValidatedNodeType(call.operand(0));
      if (null == operandType) {
        throw new AssertionError("operand's type should have been derived");
      }
```

### ConstantValue
Condition `unit != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalQualifier.java`
#### Snippet
```java
    // YEAR and DAY can never be secondary units,
    // nor can unit be null.
    assert unit != null;
    switch (unit) {
    case YEAR:
```

### ConstantValue
Condition `null == argType` is always `false`
in `core/src/main/java/org/apache/calcite/sql/SqlCall.java`
#### Snippet
```java
          validator.deriveType(Objects.requireNonNull(scope, "scope"),
              operand);
      if (null == argType) {
        continue;
      }
```

### ConstantValue
Condition `node != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
   */
  public static boolean isLiteral(RexNode node, boolean allowCast) {
    assert node != null;
    if (node.isA(SqlKind.LITERAL)) {
      return true;
```

### ConstantValue
Condition `ec != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
        new RexShuttle() {
          @Override public RexNode visitTableInputRef(RexTableInputRef inputRef) {
            if (ec != null) {
              Set<RexTableInputRef> s = ec.get(inputRef);
              if (s != null) {
```

### ConstantValue
Condition `node != null` is always `true`
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
   */
  public static boolean isReferenceOrAccess(RexNode node, boolean allowCast) {
    assert node != null;
    if (node instanceof RexInputRef || node instanceof RexFieldAccess) {
      return true;
```

### ConstantValue
Condition `null != col1` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlCollation.java`
#### Snippet
```java
      SqlCollation col1,
      SqlCollation col2) {
    assert null != col1;
    assert null != col2;
    final Coercibility coercibility1 = col1.getCoercibility();
```

### ConstantValue
Condition `null != col2` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlCollation.java`
#### Snippet
```java
      SqlCollation col2) {
    assert null != col1;
    assert null != col2;
    final Coercibility coercibility1 = col1.getCoercibility();
    final Coercibility coercibility2 = col2.getCoercibility();
```

### ConstantValue
Value `onlyExpressions` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
        }
      } else {
        super.acceptCall(visitor, call, onlyExpressions, argHandler);
      }
    }
```

### ConstantValue
Condition `partitionList != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java

    assert declName == null || declName.isSimple();
    assert partitionList != null;
    assert orderList != null;
  }
```

### ConstantValue
Condition `orderList != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
    assert declName == null || declName.isSimple();
    assert partitionList != null;
    assert orderList != null;
  }

```

### ConstantValue
Value `lowerBound` is always 'null'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
    SqlNodeList orderListNew = orderList;
    SqlLiteral isRowsNew = isRows;
    SqlNode lowerBoundNew = lowerBound;
    SqlNode upperBoundNew = upperBound;
    SqlLiteral allowPartialNew = allowPartial;
```

### ConstantValue
Value `upperBound` is always 'null'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
    SqlLiteral isRowsNew = isRows;
    SqlNode lowerBoundNew = lowerBound;
    SqlNode upperBoundNew = upperBound;
    SqlLiteral allowPartialNew = allowPartial;

```

### ConstantValue
Value `lowerBoundNew` is always 'null'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
      orderListNew = that.orderList;
    }
    if (setOperand(lowerBoundNew, that.lowerBound, validator)) {
      lowerBoundNew = that.lowerBound;
    }
```

### ConstantValue
Value `upperBoundNew` is always 'null'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
      lowerBoundNew = that.lowerBound;
    }
    if (setOperand(upperBoundNew, that.upperBound, validator)) {
      upperBoundNew = that.upperBound;
    }
```

### ConstantValue
Value `refNameNew` is always 'null'
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
        SqlParserPos.ZERO,
        declNameNew,
        refNameNew,
        partitionListNew,
        orderListNew,
```

### ConstantValue
Condition `from != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
      SqlSelect select = (SqlSelect) query;
      final SqlNode from = stripAs(select.getFrom());
      if (from != null && from.getKind() == SqlKind.VALUES) {
        // They wrote "VALUES (x, y)", but the validator has
        // converted this into "SELECT * FROM VALUES (x, y)".
```

### ConstantValue
Condition `fields != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
      final SqlNodeList fields = select.getSelectList();

      assert fields != null : "fields must not be null in " + select;
      // Range check the index to avoid index out of range.  This
      // could be expanded to actually check to see if the select
```

### ConstantValue
Condition `charsetName != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
   */
  public static Charset getCharset(String charsetName) {
    assert charsetName != null;
    charsetName = charsetName.toUpperCase(Locale.ROOT);
    String javaCharsetName = translateCharacterSetName(charsetName);
```

### ConstantValue
Condition `alias != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
   */
  public static boolean isGeneratedAlias(String alias) {
    assert alias != null;
    return alias.toUpperCase(Locale.ROOT).startsWith(GENERATED_EXPR_ALIAS_PREFIX);
  }
```

### ConstantValue
Condition `node != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
   */
  public static boolean isLiteralChain(SqlNode node) {
    assert node != null;
    if (node instanceof SqlCall) {
      SqlCall call = (SqlCall) node;
```

### ConstantValue
Condition `node != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
   */
  public static boolean isLiteral(SqlNode node, boolean allowCast) {
    assert node != null;
    if (node instanceof SqlLiteral) {
      return true;
```

### ConstantValue
Condition `node != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
   * the node. Returns null if and only if the node is null. */
  public static @PolyNull SqlNode stripAs(@PolyNull SqlNode node) {
    if (node != null && node.getKind() == SqlKind.AS) {
      return ((SqlCall) node).operand(0);
    }
```

### ConstantValue
Condition `typeName != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlLiteral.java`
#### Snippet
```java
    this.value = value;
    this.typeName = typeName;
    assert typeName != null;
    assert valueMatchesType(value, typeName);
  }
```

### ConstantValue
Condition `keywords != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlInsert.java`
#### Snippet
```java
    this.source = source;
    this.columnList = columnList;
    assert keywords != null;
  }

```

### ConstantValue
Condition `accessNames != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlAccessType.java`
#### Snippet
```java

  public static SqlAccessType create(String[] accessNames) {
    assert accessNames != null;
    EnumSet<SqlAccessEnum> enumSet = EnumSet.noneOf(SqlAccessEnum.class);
    for (String accessName : accessNames) {
```

### ConstantValue
Condition `accessString != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlAccessType.java`
#### Snippet
```java

  public static SqlAccessType create(String accessString) {
    assert accessString != null;
    accessString = accessString.replace('[', ' ');
    accessString = accessString.replace(']', ' ');
```

### ConstantValue
Value `onlyExpressions` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/SqlSelectOperator.java`
#### Snippet
```java
    if (!onlyExpressions) {
      // None of the arguments to the SELECT operator are expressions.
      super.acceptCall(visitor, call, onlyExpressions, argHandler);
    }
  }
```

### ConstantValue
Condition `null == operandType` is always `false`
in `core/src/main/java/org/apache/calcite/sql/SqlPrefixOperator.java`
#### Snippet
```java
      RelDataType operandType =
          validator.getValidatedNodeType(call.operand(0));
      if (null == operandType) {
        throw new AssertionError("operand's type should have been derived");
      }
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/sql/SqlCharStringLiteral.java`
#### Snippet
```java
      int rightPrec) {
    final NlsString nlsString = getValueNonNull();
    if (false) {
      Util.discard(Bug.FRG78_FIXED);
      String stringValue = nlsString.getValue();
```

### ConstantValue
Condition `nodeType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlAsOperator.java`
#### Snippet
```java
    RelDataType nodeType =
        validator.deriveType(scope, call.operand(0));
    assert nodeType != null;
    return validateOperands(validator, scope, call);
  }
```

### ConstantValue
Value `onlyExpressions` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/SqlAsOperator.java`
#### Snippet
```java
      argHandler.visitChild(visitor, call, 0, call.operand(0));
    } else {
      super.acceptCall(visitor, call, onlyExpressions, argHandler);
    }
  }
```

### ConstantValue
Condition `intervalQualifier != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalLiteral.java`
#### Snippet
```java
        String intervalStr) {
      assert (sign == -1) || (sign == 1);
      assert intervalQualifier != null;
      assert intervalStr != null;
      this.intervalQualifier = intervalQualifier;
```

### ConstantValue
Condition `intervalStr != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalLiteral.java`
#### Snippet
```java
      assert (sign == -1) || (sign == 1);
      assert intervalQualifier != null;
      assert intervalStr != null;
      this.intervalQualifier = intervalQualifier;
      this.sign = sign;
```

### ConstantValue
Condition `name != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlSetOption.java`
#### Snippet
```java
    this.name = name;
    this.value = value;
    assert name != null;
  }

```

### ConstantValue
Value `onlyExpressions` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/SqlOverOperator.java`
#### Snippet
```java
      }
    } else {
      super.acceptCall(visitor, call, onlyExpressions, argHandler);
    }
  }
```

### ConstantValue
Value `onlyExpressions` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
        }
      } else {
        super.acceptCall(visitor, call, onlyExpressions, argHandler);
      }
    }
```

### ConstantValue
Condition `pattern.partitionList != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      final SqlWriter.Frame mrFrame = writer.startFunCall("MATCH_RECOGNIZE");

      if (pattern.partitionList != null && pattern.partitionList.size() > 0) {
        writer.newlineAndIndent();
        writer.sep("PARTITION BY");
```

### ConstantValue
Condition `pattern.orderList != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      }

      if (pattern.orderList != null && pattern.orderList.size() > 0) {
        writer.newlineAndIndent();
        writer.sep("ORDER BY");
```

### ConstantValue
Condition `pattern.measureList != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      }

      if (pattern.measureList != null && pattern.measureList.size() > 0) {
        writer.newlineAndIndent();
        writer.sep("MEASURES");
```

### ConstantValue
Condition `pattern.subsetList != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      }

      if (pattern.subsetList != null && pattern.subsetList.size() > 0) {
        writer.newlineAndIndent();
        writer.sep("SUBSET");
```

### ConstantValue
Condition `nodeType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlOperator.java`
#### Snippet
```java
    for (SqlNode operand : call.getOperandList()) {
      RelDataType nodeType = validator.deriveType(scope, operand);
      assert nodeType != null;
    }

```

### ConstantValue
Condition `kind != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/SqlOperator.java`
#### Snippet
```java
      @Nullable SqlOperandTypeInference operandTypeInference,
      @Nullable SqlOperandTypeChecker operandTypeChecker) {
    assert kind != null;
    this.name = name;
    this.kind = kind;
```

### ConstantValue
Condition `geom != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSpatialTypeFunctions.java`
#### Snippet
```java

    @Override public Enumerable<@Nullable Object[]> scan(DataContext root) {
      if (geom != null && deltaX != null && deltaY != null) {
        if (deltaX.compareTo(BigDecimal.ZERO) > 0
            && deltaY.compareTo(BigDecimal.ZERO) > 0) {
```

### ConstantValue
Condition `geom != null && deltaX != null && deltaY != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSpatialTypeFunctions.java`
#### Snippet
```java

    @Override public Enumerable<@Nullable Object[]> scan(DataContext root) {
      if (geom != null && deltaX != null && deltaY != null) {
        if (deltaX.compareTo(BigDecimal.ZERO) > 0
            && deltaY.compareTo(BigDecimal.ZERO) > 0) {
```

### ConstantValue
Condition `deltaX != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSpatialTypeFunctions.java`
#### Snippet
```java

    @Override public Enumerable<@Nullable Object[]> scan(DataContext root) {
      if (geom != null && deltaX != null && deltaY != null) {
        if (deltaX.compareTo(BigDecimal.ZERO) > 0
            && deltaY.compareTo(BigDecimal.ZERO) > 0) {
```

### ConstantValue
Condition `deltaY != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSpatialTypeFunctions.java`
#### Snippet
```java

    @Override public Enumerable<@Nullable Object[]> scan(DataContext root) {
      if (geom != null && deltaX != null && deltaY != null) {
        if (deltaX.compareTo(BigDecimal.ZERO) > 0
            && deltaY.compareTo(BigDecimal.ZERO) > 0) {
```

### ConstantValue
Condition `mono0 != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSubstringFunction.java`
#### Snippet
```java
    if (call.getOperandCount() == 3) {
      final SqlMonotonicity mono0 = call.getOperandMonotonicity(0);
      if (mono0 != null
          && mono0 != SqlMonotonicity.NOT_MONOTONIC
          && call.getOperandMonotonicity(1) == SqlMonotonicity.CONSTANT
```

### ConstantValue
Value `onlyExpressions` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlConvertFunction.java`
#### Snippet
```java
      argHandler.visitChild(visitor, call, 0, call.operand(0));
    } else {
      super.acceptCall(visitor, call, onlyExpressions, argHandler);
    }
  }
```

### ConstantValue
Value `functionQualifier` is always 'null'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlTrimFunction.java`
#### Snippet
```java
          "invalid operand count " + Arrays.toString(operands));
    }
    return super.createCall(functionQualifier, pos, operands);
  }

```

### ConstantValue
Condition `nodeType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/fun/SqlDotOperator.java`
#### Snippet
```java
    final RelDataType nodeType =
        validator.deriveType(scope, operand);
    assert nodeType != null;
    if (!nodeType.isStruct()) {
      throw SqlUtil.newContextException(operand.getParserPosition(),
```

### ConstantValue
Value `onlyExpressions` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/fun/SqlDotOperator.java`
#### Snippet
```java
      argHandler.visitChild(visitor, call, 0, call.operand(0));
    } else {
      super.acceptCall(visitor, call, onlyExpressions, argHandler);
    }
  }
```

### ConstantValue
Condition `protoType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/ExplicitReturnTypeInference.java`
#### Snippet
```java
   */
  protected ExplicitReturnTypeInference(RelProtoDataType protoType) {
    assert protoType != null;
    this.protoType = protoType;
  }
```

### ConstantValue
Condition `keyType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/MapSqlType.java`
#### Snippet
```java
      RelDataType keyType, RelDataType valueType, boolean isNullable) {
    super(SqlTypeName.MAP, isNullable, null);
    assert keyType != null;
    assert valueType != null;
    this.keyType = keyType;
```

### ConstantValue
Condition `valueType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/MapSqlType.java`
#### Snippet
```java
    super(SqlTypeName.MAP, isNullable, null);
    assert keyType != null;
    assert valueType != null;
    this.keyType = keyType;
    this.valueType = valueType;
```

### ConstantValue
Condition `typeName != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeFactoryImpl.java`
#### Snippet
```java

  private static void assertBasic(SqlTypeName typeName) {
    assert typeName != null;
    assert typeName != SqlTypeName.MULTISET
        : "use createMultisetType() instead";
```

### ConstantValue
Condition `type0.getSqlTypeName() != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeFactoryImpl.java`
#### Snippet
```java

    RelDataType type0 = types.get(0);
    if (type0.getSqlTypeName() != null) {
      RelDataType resultType = leastRestrictiveSqlType(types);
      if (resultType != null) {
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeName.java`
#### Snippet
```java
   */
  public static @Nullable SqlTypeName get(String name) {
    if (false) {
      // The following code works OK, but the spurious exceptions are
      // annoying.
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeFactoryImpl.java`
#### Snippet
```java
    for (RelDataType type : types) {
      final SqlTypeName typeName = type.getSqlTypeName();
      if (typeName == null) {
        return null;
      }
```

### ConstantValue
Condition `null == thisEnd` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/IntervalSqlType.java`
#### Snippet
```java
              this.intervalQualifier,
              that.intervalQualifier);
    } else if (null == thisEnd || thisEnd.ordinal() < thatStart.ordinal()) {
      thisEnd = thatStart;
    }
```

### ConstantValue
Condition `null != thatEnd` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/IntervalSqlType.java`
#### Snippet
```java
    }

    if (null != thatEnd) {
      if (null == thisEnd || thisEnd.ordinal() < thatEnd.ordinal()) {
        thisEnd = thatEnd;
```

### ConstantValue
Condition `null == thisEnd` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/IntervalSqlType.java`
#### Snippet
```java

    if (null != thatEnd) {
      if (null == thisEnd || thisEnd.ordinal() < thatEnd.ordinal()) {
        thisEnd = thatEnd;
      }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeExplicitPrecedenceList.java`
#### Snippet
```java
  static @Nullable RelDataTypePrecedenceList getListForType(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return null;
    }
```

### ConstantValue
Condition `typeName != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeExplicitPrecedenceList.java`
#### Snippet
```java
  @Override public boolean containsType(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    return typeName != null && typeNames.contains(typeName);
  }

```

### ConstantValue
Condition `elementType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/MultisetSqlType.java`
#### Snippet
```java
  public MultisetSqlType(RelDataType elementType, boolean isNullable) {
    super(SqlTypeName.MULTISET, isNullable, null);
    assert elementType != null;
    this.elementType = elementType;
    computeDigest();
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isNull(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isCharacter(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isMap(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `toTypeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      }
    }
    if (toTypeName == null || fromTypeName == null) {
      return false;
    }
```

### ConstantValue
Condition `toTypeName == null || fromTypeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      }
    }
    if (toTypeName == null || fromTypeName == null) {
      return false;
    }
```

### ConstantValue
Condition `fromTypeName == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      }
    }
    if (toTypeName == null || fromTypeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    }
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return null;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isDouble(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isMultiset(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isApproximateNumeric(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    SqlTypeName typeName = type.getSqlTypeName();

    if (typeName == null) {
      return 0;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isString(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    }
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return null;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isBoolean(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `argTypes != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
   */
  public static boolean isCharTypeComparable(List<RelDataType> argTypes) {
    assert argTypes != null;
    assert argTypes.size() >= 2;

```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isIntType(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isJavaPrimitive(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isBoundedVariableWidth(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isExactNumeric(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isDate(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isAtomic(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isBinary(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `type.getSqlTypeName() != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    // the Saffron type system happy.
    RelDataTypeFamily family = null;
    if (type.getSqlTypeName() != null) {
      family = type.getSqlTypeName().getFamily();
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isBigint(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isDecimal(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isRow(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static boolean isCollection(RelDataType type) {
    SqlTypeName typeName = type.getSqlTypeName();
    if (typeName == null) {
      return false;
    }
```

### ConstantValue
Condition `typeName != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    // TODO jvs 28-Dec-2004:  support row types, user-defined types,
    // interval types, multiset types, etc
    assert typeName != null;

    final SqlTypeNameSpec typeNameSpec;
```

### ConstantValue
Condition `sql != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/util/SqlString.java`
#### Snippet
```java
    this.sql = sql;
    this.dynamicParameters = dynamicParameters;
    assert sql != null : "sql must be NOT null";
    assert dialect != null : "dialect must be NOT null";
  }
```

### ConstantValue
Condition `dialect != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/util/SqlString.java`
#### Snippet
```java
    this.dynamicParameters = dynamicParameters;
    assert sql != null : "sql must be NOT null";
    assert dialect != null : "dialect must be NOT null";
  }

```

### ConstantValue
Condition `dialect != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/util/SqlBuilder.java`
#### Snippet
```java
   */
  public SqlBuilder(SqlDialect dialect) {
    assert dialect != null;
    this.dialect = dialect;
    this.buf = new StringBuilder();
```

### ConstantValue
Condition `dialect != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/util/SqlBuilder.java`
#### Snippet
```java
   */
  public SqlBuilder(SqlDialect dialect, String s) {
    assert dialect != null;
    this.dialect = dialect;
    this.buf = new StringBuilder(s);
```

### ConstantValue
Condition `timestamp == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/util/SqlBuilder.java`
#### Snippet
```java
  public SqlBuilder literal(Timestamp timestamp) {
    buf.append(
        timestamp == null
            ? "null"
            : dialect.quoteTimestampLiteral(timestamp));
```

### ConstantValue
Condition `s == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/util/SqlBuilder.java`
#### Snippet
```java
  public SqlBuilder literal(String s) {
    buf.append(
        s == null
            ? "null"
            : dialect.quoteStringLiteral(s));
```

### ConstantValue
Condition `names == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisorHint.java`
#### Snippet
```java
    this.id = id.toString();
    final List<String> names = id.getFullyQualifiedNames();
    this.names = names == null
      ? null
      : names.toArray(new String[0]);
```

### ConstantValue
Condition `expectedTokenSequences == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/parser/SqlParseException.java`
#### Snippet
```java
   */
  public Collection<String> getExpectedTokenNames() {
    if (expectedTokenSequences == null) {
      return Collections.emptyList();
    }
```

### ConstantValue
Condition `functions != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/dialect/JethroDataSqlDialect.java`
#### Snippet
```java
        info.supportedFunctions.get(operator.getName());

    if (functions != null) {
      for (JethroSupportedFunction f : functions) {
        if (f.argumentsMatch(paramTypes)) {
```

### ConstantValue
Condition `c == '-'` is always `true`
in `core/src/main/java/org/apache/calcite/sql/advise/SqlSimpleParser.java`
#### Snippet
```java
              case '-':
                // possible start of '--' comment
                if (c == '-' && pos + 1 < sql.length() && sql.charAt(pos + 1) == '-') {
                  break loop;
                }
```

### ConstantValue
Condition `m1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/validate/SetopNamespace.java`
#### Snippet
```java
      return m1;
    }
    if (m1 == null) {
      return m0;
    }
```

### ConstantValue
Condition `type != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/AbstractNamespace.java`
#### Snippet
```java
            "Namespace.rowType must be null before validate has been called");
        RelDataType type = validateImpl(targetRowType);
        Preconditions.checkArgument(type != null,
            "validateImpl() returned null");
        setType(type);
```

### ConstantValue
Condition `frameType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Override public Frame startList(FrameTypeEnum frameType) {
    assert frameType != null;
    return startList(frameType, null, "", "");
  }
```

### ConstantValue
Condition `frameType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Override public Frame startList(FrameType frameType, String open, String close) {
    assert frameType != null;
    return startList(frameType, null, open, close);
  }
```

### ConstantValue
Value `newlineAfterSep` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
      return new FrameImpl(frameType, keyword, open, close, left,
          indentation, chopColumn, lineFolding, newlineAfterOpen,
          newlineBeforeSep, sepIndent, newlineAfterSep, false, false) {
        @Override protected void sep(boolean printFirst, String sep) {
          final boolean newlineBeforeSep;
```

### ConstantValue
Condition `open != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

    protected void before() {
      if ((open != null) && !open.equals("")) {
        keyword(open);
      }
```

### ConstantValue
Condition `frameType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
      String open,
      String close) {
    assert frameType != null;
    FrameImpl frame = this.frame;
    if (frame != null) {
```

### ConstantValue
Condition `scope != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/UnnestNamespace.java`
#### Snippet
```java
      SqlNode enclosingNode) {
    super(validator, enclosingNode);
    assert scope != null;
    assert unnest.getOperator() instanceof SqlUnnestOperator;
    this.unnest = unnest;
```

### ConstantValue
Condition `dataType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/FieldNamespace.java`
#### Snippet
```java
      RelDataType dataType) {
    super(validator, null);
    assert dataType != null;
    this.rowType = dataType;
  }
```

### ConstantValue
Condition `monotonicity != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
      for (String field : ns.getRowType().getFieldNames()) {
        SqlMonotonicity monotonicity = ns.getMonotonicity(field);
        if (monotonicity != null && !monotonicity.mayRepeat()) {
          return true;
        }
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
      Charset colCharset = getCollation(type).getCharset();
      if (!strCharset.equals(colCharset)) {
        if (false) {
          // todo: enable this checking when we have a charset to
          //   collation mapping
```

### ConstantValue
Condition `systemFieldList != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
      @Nullable List<String> fieldNameList,
      List<RelDataTypeField> systemFieldList) {
    assert systemFieldList != null;
    switch (joinType) {
    case LEFT:
```

### ConstantValue
Condition `child.name != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/ListScope.java`
#### Snippet
```java
    for (ScopeChild child : children) {
      String lastName = Util.last(names);
      if (child.name != null) {
        if (!nameMatcher.matches(child.name, lastName)) {
          // Alias does not match last segment. Don't consider the
```

### ConstantValue
Condition `qualifiedNames != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/IdentifierNamespace.java`
#### Snippet
```java
        // TODO:  expand qualifiers for column references also
        List<String> qualifiedNames = table.getQualifiedName();
        if (qualifiedNames != null) {
          // Assign positions to the components of the fully-qualified
          // identifier, as best we can. We assume that qualification
```

### ConstantValue
Condition `monotonicity != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/IdentifierNamespace.java`
#### Snippet
```java
      final SqlMonotonicity monotonicity =
          resolvedNamespace.getMonotonicity(fieldName);
      if (monotonicity != null && monotonicity != SqlMonotonicity.NOT_MONOTONIC) {
        builder.add(
            Pair.of(new SqlIdentifier(fieldName, SqlParserPos.ZERO),
```

### ConstantValue
Condition `schemaType != null` is always `true`
in `core/src/main/java/org/apache/calcite/jdbc/Driver.java`
#### Snippet
```java
        if (schemaFactory == null) {
          final JsonSchema.Type schemaType = connection.config().schemaType();
          if (schemaType != null) {
            switch (schemaType) {
            case JDBC:
```

### ConstantValue
Condition `coerced` is always `false` when reached
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/TypeCoercionImpl.java`
#### Snippet
```java
      if (kind.belongsTo(SqlKind.BINARY_EQUALITY)) {
        // STRING and datetime
        coerced = dateTimeStringEquality(binding, type1, type2) || coerced;
        // BOOLEAN and NUMERIC
        // BOOLEAN and literal
```

### ConstantValue
Condition `sourceExpressionList != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/TypeCoercionImpl.java`
#### Snippet
```java
      SqlUpdate update = (SqlUpdate) query;
      final SqlNodeList sourceExpressionList = update.getSourceExpressionList();
      if (sourceExpressionList != null) {
        return coerceColumnType(sourceScope, sourceExpressionList, columnIndex, targetType);
      } else {
```

### ConstantValue
Condition `functions != null` is always `true`
in `core/src/main/java/org/apache/calcite/jdbc/CachingCalciteSchema.java`
#### Snippet
```java
    for (String name2 : set.range(name, caseSensitive)) {
      final Collection<Function> functions = schema.getFunctions(name2);
      if (functions != null) {
        builder.addAll(functions);
      }
```

### ConstantValue
Condition `fromType == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
    // This depends on the fact that type validate happens before coercion.
    // We do not have inferred type for some node, i.e. LOCALTIME.
    if (fromType == null) {
      return false;
    }
```

### ConstantValue
Condition `fromType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
      RelDataType toType) {
    RelDataType syncedType = toType;
    if (fromType != null) {
      syncedType = factory.createTypeWithNullability(syncedType, fromType.isNullable());
      if (SqlTypeUtil.inCharOrBinaryFamilies(fromType)
```

### ConstantValue
Condition `typeName1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
    SqlTypeName typeName2 = type2.getSqlTypeName();

    if (typeName1 == null || typeName2 == null) {
      return null;
    }
```

### ConstantValue
Condition `typeName1 == null || typeName2 == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
    SqlTypeName typeName2 = type2.getSqlTypeName();

    if (typeName1 == null || typeName2 == null) {
      return null;
    }
```

### ConstantValue
Condition `typeName2 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
    SqlTypeName typeName2 = type2.getSqlTypeName();

    if (typeName1 == null || typeName2 == null) {
      return null;
    }
```

### ConstantValue
Condition `functions != null` is always `true`
in `core/src/main/java/org/apache/calcite/jdbc/SimpleCalciteSchema.java`
#### Snippet
```java
      boolean caseSensitive) {
    Collection<Function> functions = schema.getFunctions(tableName);
    if (functions != null) {
      for (Function function : functions) {
        if (function instanceof TableMacro
```

### ConstantValue
Condition `functions != null` is always `true`
in `core/src/main/java/org/apache/calcite/jdbc/SimpleCalciteSchema.java`
#### Snippet
```java
      String name, boolean caseSensitive) {
    Collection<Function> functions = schema.getFunctions(name);
    if (functions != null) {
      builder.addAll(functions);
    }
```

### ConstantValue
Condition `materialization.qualifiedTableName != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterializations.java`
#### Snippet
```java
    for (RelOptMaterialization materialization : materializations) {
      // If materialization is a tile in a lattice, we will deal with it shortly.
      if (materialization.qualifiedTableName != null
          && materialization.starTable == null) {
        final List<String> qname = materialization.qualifiedTableName;
```

### ConstantValue
Condition `localeName != null` is always `true`
in `core/src/main/java/org/apache/calcite/jdbc/CalciteConnectionImpl.java`
#### Snippet
```java
      final String systemUser = System.getProperty("user.name");
      final String localeName = connection.config().locale();
      final Locale locale = localeName != null
          ? Util.parseLocale(localeName) : Locale.ROOT;

```

### ConstantValue
Condition `description != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
    // Check that there isn't a rule with the same description
    final String description = rule.toString();
    assert description != null;

    RelOptRule existingRule = mapDescToRule.put(description, rule);
```

### ConstantValue
Value `trait` is always 'null'
in `core/src/main/java/org/apache/calcite/plan/RelTraitSet.java`
#### Snippet
```java
      // Return "trait" makes the input type to be the same as the output type,
      // so checkerframework is happy
      return trait;
    }

```

### ConstantValue
Condition `clazz != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptRuleOperand.java`
#### Snippet
```java
      RelOptRuleOperandChildPolicy childPolicy,
      ImmutableList<RelOptRuleOperand> children) {
    assert clazz != null;
    switch (childPolicy) {
    case ANY:
```

### ConstantValue
Condition `children != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptRuleOperand.java`
#### Snippet
```java
      s.append('*');
    }
    if (children != null && !children.isEmpty()) {
      s.append('(');
      boolean first = true;
```

### ConstantValue
Condition `typeList == null` is always `false`
in `core/src/main/java/org/apache/calcite/jdbc/CalciteMetaImpl.java`
#### Snippet
```java
      final List<String> typeList) {
    final Predicate1<MetaTable> typeFilter;
    if (typeList == null) {
      typeFilter = Functions.truePredicate1();
    } else {
```

### ConstantValue
Condition `type.getSqlTypeName() == null` is always `false`
in `core/src/main/java/org/apache/calcite/plan/VisitorDataContext.java`
#### Snippet
```java
      final RelDataType type = inputRef.getType();

      if (type.getSqlTypeName() == null) {
        LOGGER.warn("{} returned null SqlTypeName", inputRef.toString());
        return null;
```

### ConstantValue
Result of `operandList.size()` is always '0'
in `core/src/main/java/org/apache/calcite/plan/RelOptRule.java`
#### Snippet
```java
    rootOperand.setParent(null);
    rootOperand.ordinalInParent = 0;
    rootOperand.ordinalInRule = operandList.size();
    operandList.add(rootOperand);
    flattenRecurse(operandList, rootOperand);
```

### ConstantValue
Condition `toTrait != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptRule.java`
#### Snippet
```java
    for (int i = 0; i < toTraits.size(); i++) {
      RelTrait toTrait = toTraits.getTrait(i);
      if (toTrait != null) {
        outTraits = outTraits.replace(i, toTrait);
      }
```

### ConstantValue
Condition is always true
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoCost.java`
#### Snippet
```java
  @Override public boolean isLe(RelOptCost other) {
    VolcanoCost that = (VolcanoCost) other;
    if (true) {
      return this == that
          || this.rowCount <= that.rowCount;
```

### ConstantValue
Condition is always true
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoCost.java`
#### Snippet
```java

  @Override public boolean isLt(RelOptCost other) {
    if (true) {
      VolcanoCost that = (VolcanoCost) other;
      return this.rowCount < that.rowCount;
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSubset.java`
#### Snippet
```java

    set.addInternal(rel);
    if (false) {
      Set<CorrelationId> variablesSet = RelOptUtil.getVariablesSet(rel);
      Set<CorrelationId> variablesStopped = rel.getVariablesSet();
```

### ConstantValue
Result of `aggregateCall.isDistinct()` is always 'false'
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
            if (aggFunction != null) {
              newAggCall =
                  AggregateCall.create(aggFunction, aggregateCall.isDistinct(),
                      aggregateCall.isApproximate(), aggregateCall.ignoreNulls(),
                      aggregateCall.rexList,
```

### ConstantValue
Condition `condition == null` is always `false`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      projects.add(rex);
    }
    if (condition == null) {
      return Pair.of(calc.cluster.getRexBuilder().makeLiteral(true), projects);
    } else {
```

### ConstantValue
Condition `number == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * returns null if and only if the number is null. */
  public static @PolyNull BigDecimal toBigDecimal(@PolyNull Number number) {
    if (number == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `a == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double multiply(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `a == null || b == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double multiply(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double multiply(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `bd != null` is always `true`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
  public static @PolyNull BigDecimal rescaleBigDecimal(@PolyNull BigDecimal bd,
      int scale) {
    if (bd != null) {
      bd = bd.setScale(scale, RoundingMode.HALF_UP);
    }
```

### ConstantValue
Condition `a == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if both are null. */
  public static @PolyNull Double min(@PolyNull Double a, @PolyNull Double b) {
    if (a == null) {
      return b;
    } else if (b == null) {
```

### ConstantValue
Condition `b == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
    if (a == null) {
      return b;
    } else if (b == null) {
      return a;
    } else {
```

### ConstantValue
Condition `a == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
  /** Returns the sum of two numbers, or null if either is null. */
  public static @PolyNull Double add(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return null;
    }
```

### ConstantValue
Condition `a == null || b == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
  /** Returns the sum of two numbers, or null if either is null. */
  public static @PolyNull Double add(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return null;
    }
```

### ConstantValue
Condition `b == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
  /** Returns the sum of two numbers, or null if either is null. */
  public static @PolyNull Double add(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return null;
    }
```

### ConstantValue
Condition `a == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double subtract(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `a == null || b == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double subtract(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double subtract(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `a == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double max(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `a == null || b == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double max(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java
   * or null if either is null. */
  public static @PolyNull Double max(@PolyNull Double a, @PolyNull Double b) {
    if (a == null || b == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `hierarchyRoot != null` is always `true`
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
    }

    if (hierarchyRoot != null) {
      Class paramType = method.getParameterTypes()[0];
      if (!hierarchyRoot.isAssignableFrom(paramType)) {
```

### ConstantValue
Condition `rel != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
   */
  public @Nullable RelSubset getSubset(RelNode rel) {
    assert rel != null : "pre: rel != null";
    if (rel instanceof RelSubset) {
      return (RelSubset) rel;
```

### ConstantValue
Condition `ruleDriver != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
    }

    if (ruleDriver != null) {
      ruleDriver.onProduce(rel, subset);
    }
```

### ConstantValue
Condition `toTrait == null` is always `false`
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
      RelTrait toTrait = toTraits.getTrait(i);

      if (toTrait == null) {
        continue;
      }
```

### ConstantValue
Condition `rel != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java

  @Override public @Nullable RelOptCost getCost(RelNode rel, RelMetadataQuery mq) {
    assert rel != null : "pre-condition: rel != null";
    if (rel instanceof RelSubset) {
      return ((RelSubset) rel).bestCost;
```

### ConstantValue
Condition `plan == null` is always `false`
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
   */
  public static @PolyNull String normalizePlan(@PolyNull String plan) {
    if (plan == null) {
      return null;
    }
```

### ConstantValue
Condition `traitDefs.add(relTraitDef)` is always `true` when reached
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java

  @Override public boolean addRelTraitDef(RelTraitDef relTraitDef) {
    return !traitDefs.contains(relTraitDef) && traitDefs.add(relTraitDef);
  }

```

### ConstantValue
Condition `ruleDriver != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
    }

    if (ruleDriver != null) {
      ruleDriver.onSetMerged(set1);
    }
```

### ConstantValue
Condition `rel != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
   */
  public @Nullable RelSet getSet(RelNode rel) {
    assert rel != null : "pre: rel != null";
    final RelSubset subset = getSubset(rel);
    if (subset != null) {
```

### ConstantValue
Condition `subset.set != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
    final RelSubset subset = getSubset(rel);
    if (subset != null) {
      assert subset.set != null;
      return subset.set;
    }
```

### ConstantValue
Condition `rel == null` is always `false`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      final @PolyNull RelNode rel,
      SqlExplainLevel detailLevel) {
    if (rel == null) {
      return null;
    }
```

### ConstantValue
Condition `kind != SqlKind.IS_DISTINCT_FROM` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
        if (rangeOp != null
            && kind != SqlKind.EQUALS
            && kind != SqlKind.IS_DISTINCT_FROM) {
          SqlOperator op = call.getOperator();
          if (reverse) {
```

### ConstantValue
Condition `projectFactory != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      boolean rename,
      RelFactories.ProjectFactory projectFactory) {
    assert projectFactory != null;
    RelDataType rowType = rel.getRowType();
    if (areRowTypesEqual(rowType, castRowType, rename)) {
```

### ConstantValue
Condition `fieldOrdinals != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    RelDataType rowType = rel.getRowType();
    int n;
    if (fieldOrdinals != null) {
      n = fieldOrdinals.length;
    } else {
```

### ConstantValue
Condition `fieldOrdinals != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    for (int i = 0; i < n; ++i) {
      int iField;
      if (fieldOrdinals != null) {
        iField = fieldOrdinals[i];
      } else {
```

### ConstantValue
Condition `topHints != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    private RelNode visitHintable(RelNode node) {
      final List<RelHint> topHints = ((Hintable) node).getHints();
      final boolean hasHints = topHints != null && topHints.size() > 0;
      if (hasHints) {
        // This node is definitely not created by the planner, returns early.
```

### ConstantValue
Condition `topHints != null` is always `true`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    private RelNode visitHintable(RelNode node) {
      final List<RelHint> topHints = ((Hintable) node).getHints();
      final boolean hasHints = topHints != null && topHints.size() > 0;
      final boolean hasQueryHints = hasHints && !(node instanceof TableScan);
      if (hasQueryHints) {
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/util/NlsString.java`
#### Snippet
```java

    // NOTE jvs 3-Feb-2005:  see FRG-78 for why this should go away
    if (false) {
      if (suffix && (null != collation)) {
        ret.append(" ");
```

### ConstantValue
Condition `value.method != null` is always `true`
in `core/src/main/java/org/apache/calcite/util/BuiltInMethod.java`
#### Snippet
```java
        ImmutableMap.builder();
    for (BuiltInMethod value : BuiltInMethod.values()) {
      if (value.method != null) {
        builder.put(value.method, value);
      }
```

### ConstantValue
Condition `charset != null` is always `true`
in `core/src/main/java/org/apache/calcite/util/SerializableCharset.java`
#### Snippet
```java
   */
  private SerializableCharset(Charset charset) {
    assert charset != null;
    this.charset = charset;
    this.charsetName = charset.name();
```

### ConstantValue
Condition `charset == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/SerializableCharset.java`
#### Snippet
```java
   */
  public static @PolyNull SerializableCharset forCharset(@PolyNull Charset charset) {
    if (charset == null) {
      return null;
    }
```

### ConstantValue
Condition `attributes != null` is always `true`
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
    out.print("<");
    out.print(tagName);
    if (attributes != null) {
      out.print(" ");
      attributes.display(out, indent);
```

### ConstantValue
Condition `logger != null` is always `true`
in `core/src/main/java/org/apache/calcite/util/trace/CalciteLogger.java`
#### Snippet
```java

  public CalciteLogger(Logger logger) {
    assert logger != null;
    this.logger = logger;
  }
```

### ConstantValue
Condition `destdir == null` is always `false`
in `core/src/main/java/org/apache/calcite/util/javac/JaninoCompiler.java`
#### Snippet
```java
            sourceFinder,
            null,
            destdir == null ? null : new File(destdir));
    if (CalciteSystemProperty.DEBUG.value()) {
      // Add line numbers to the generated janino class
```

### ConstantValue
Condition `type != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    type = deriveTypeImpl(scope, expr);
    Preconditions.checkArgument(
        type != null,
        "SqlValidator.deriveTypeInternal returned null");
    setValidatedNodeType(expr, type);
```

### ConstantValue
Value `natural` is always 'false'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    final RelDataType leftColType = natural
        ? checkAndDeriveDataType(id, left)
        : validateCommonInputJoinColumn(id, left, scope, natural);
    final RelDataType rightColType = validateCommonInputJoinColumn(id, right, scope, natural);
    if (!SqlTypeUtil.isComparable(leftColType, rightColType)) {
```

### ConstantValue
Condition `partitionBy != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    // parse PARTITION BY column
    SqlNodeList partitionBy = matchRecognize.getPartitionList();
    if (partitionBy != null) {
      for (SqlNode node : partitionBy) {
        SqlIdentifier identifier = (SqlIdentifier) node;
```

### ConstantValue
Condition `orderBy != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    // parse ORDER BY column
    SqlNodeList orderBy = matchRecognize.getOrderList();
    if (orderBy != null) {
      for (SqlNode node : orderBy) {
        node.validate(this, scope);
```

### ConstantValue
Condition `orderBy == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
              intervalValue != null ? intervalValue : interval.toString()));
      }
      if (orderBy == null || orderBy.size() == 0) {
        throw newValidationError(interval,
          RESOURCE.cannotUseWithinWithoutOrderBy());
```

### ConstantValue
Condition `subsets != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

    SqlNodeList subsets = matchRecognize.getSubsetList();
    if (subsets != null && subsets.size() > 0) {
      for (SqlNode node : subsets) {
        List<SqlNode> operands = ((SqlCall) node).getOperandList();
```

### ConstantValue
Condition `qualifier != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

  @Override public void validateIntervalQualifier(SqlIntervalQualifier qualifier) {
    assert qualifier != null;
    boolean startPrecisionOutOfRange = false;
    boolean fractionalSecondPrecisionOutOfRange = false;
```

### ConstantValue
Value `natural` is always 'true'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
        final SqlIdentifier id =
            new SqlIdentifier(name, join.isNaturalNode().getParserPosition());
        validateCommonJoinColumn(id, left, right, scope, natural);
      }
    }
```

### ConstantValue
Condition `node == null` is always `false`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
      @PolyNull SqlNode node,
      boolean underFrom) {
    if (node == null) {
      return null;
    }
```

### ConstantValue
Condition `newOperand != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
        SqlNode newOperand =
            performUnconditionalRewrites(operand, childUnderFrom);
        if (newOperand != null && newOperand != operand) {
          call.setOperand(i, newOperand);
        }
```

### ConstantValue
Condition `newOperand != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
                operand,
                false);
        if (newOperand != null) {
          list.set(i, newOperand);
        }
```

### ConstantValue
Condition `update.getSourceExpressionList() != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    } else if (query instanceof SqlUpdate) {
      SqlUpdate update = (SqlUpdate) query;
      if (update.getSourceExpressionList() != null) {
        return update.getSourceExpressionList().get(ordinal);
      } else {
```

### ConstantValue
Condition `info.scope == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
      Collection<SqlMoniker> hintList) {
    IdInfo info = idPositions.get(pos.toString());
    if ((info == null) || (info.scope == null)) {
      SqlNode fromNode = select.getFrom();
      final SqlValidatorScope fromScope = getFromScope(select);
```

### ConstantValue
Condition `node != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
  @Override public CalciteContextException newValidationError(SqlNode node,
      Resources.ExInst<SqlValidatorException> e) {
    assert node != null;
    final SqlParserPos pos = node.getParserPosition();
    return SqlUtil.newContextException(pos, e);
```

### ConstantValue
Condition `targetRowType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
      SqlSelect select,
      RelDataType targetRowType) {
    assert targetRowType != null;
    // Namespace is either a select namespace or a wrapper around one.
    final SelectNamespace ns =
```

### ConstantValue
Condition `rootRel2 != null` is always `true`
in `core/src/main/java/org/apache/calcite/tools/Programs.java`
#### Snippet
```java
                  ? rel
                  : planner.changeTraits(rel, requiredOutputTraits);
          assert rootRel2 != null;

          planner.setRoot(rootRel2);
```

### ConstantValue
Condition `rootRel3 != null` is always `true`
in `core/src/main/java/org/apache/calcite/tools/Programs.java`
#### Snippet
```java
          final RelOptPlanner planner2 = planner.chooseDelegate();
          final RelNode rootRel3 = planner2.findBestExp();
          assert rootRel3 != null : "could not implement exp";
          return rootRel3;
        };
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
   */
  public static void discard(int i) {
    if (false) {
      discard(i);
    }
```

### ConstantValue
Condition `clazz == Void.class` is always `false`
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
            || (clazz == Float.class)
            || (clazz == Double.class)
            || (clazz == Void.class)) {
      pw.print(o.toString());
    } else if (clazz.isArray()) {
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
   */
  public static void discard(@Nullable Object o) {
    if (false) {
      discard(o);
    }
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
   */
  public static void discard(boolean b) {
    if (false) {
      discard(b);
    }
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
   */
  public static void discard(double d) {
    if (false) {
      discard(d);
    }
```

### ConstantValue
Condition `v <= Integer.MAX_VALUE` is always `true` when reached
in `core/src/main/java/org/apache/calcite/config/CalciteSystemProperty.java`
#### Snippet
```java
  public static final CalciteSystemProperty<Integer> BINDABLE_CACHE_CONCURRENCY_LEVEL =
      intProperty("calcite.bindable.cache.concurrencyLevel", 1,
          v -> v >= 1 && v <= Integer.MAX_VALUE);

  private static CalciteSystemProperty<Boolean> booleanProperty(String key,
```

### ConstantValue
Condition `v <= Integer.MAX_VALUE` is always `true` when reached
in `core/src/main/java/org/apache/calcite/config/CalciteSystemProperty.java`
#### Snippet
```java
   */
  public static final CalciteSystemProperty<Integer> BINDABLE_CACHE_MAX_SIZE =
      intProperty("calcite.bindable.cache.maxSize", 0, v -> v >= 0 && v <= Integer.MAX_VALUE);

  /**
```

### ConstantValue
Condition `type == null` is always `false`
in `core/src/main/java/org/apache/calcite/model/ModelHandler.java`
#### Snippet
```java
                    () -> "SqlTypeName.get for " + jsonTypeAttribute.type);
            RelDataType type = typeFactory.createSqlType(typeName);
            if (type == null) {
              type =
                  requireNonNull(currentSchema().getType(jsonTypeAttribute.type),
```

### ConstantValue
Condition `s >= s1` is always `true`
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
      if (t >= 0) {
        mapping.set(s, t);
        assert s >= s1 || mapping1.getTargetOpt(s) < 0;
      } else {
        t = s < s1 ? mapping1.getTargetOpt(s) : -1;
```

### ConstantValue
Condition `s >= s1 || mapping1.getTargetOpt(s) < 0` is always `true`
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
      if (t >= 0) {
        mapping.set(s, t);
        assert s >= s1 || mapping1.getTargetOpt(s) < 0;
      } else {
        t = s < s1 ? mapping1.getTargetOpt(s) : -1;
```

### ConstantValue
Value `mergeMethod` is always 'null'
in `core/src/main/java/org/apache/calcite/schema/impl/AggregateFunctionImpl.java`
#### Snippet
```java
      return new AggregateFunctionImpl(clazz, params.build(),
          valueTypes.build(), accumulatorType, resultType, initMethod,
          addMethod, mergeMethod, resultMethod);
    }
    return null;
```

### ConstantValue
Condition `connection == null` is always `false`
in `core/src/main/java/org/apache/calcite/schema/Schemas.java`
#### Snippet
```java
      @Nullable List<String> schemaPath, @Nullable List<String> objectPath,
      final ImmutableMap<CalciteConnectionProperty, String> propValues) {
    if (connection == null) {
      final CalcitePrepare.Context context0 = CalcitePrepare.Dummy.peek();
      final CalciteConnectionConfig config =
```

### ConstantValue
Condition `exceptions == null` is always `false`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/ReflectiveCallNotNullImplementor.java`
#### Snippet
```java
  private static boolean containsCheckedException(Method method) {
    Class[] exceptions = method.getExceptionTypes();
    if (exceptions == null || exceptions.length == 0) {
      return false;
    }
```

### ConstantValue
Condition `RexImpTable.INSTANCE.get((SqlWindowTableFunction) call.getOperator()) != null` is always `true` when reached
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableTableFunctionScan.java`
#### Snippet
```java
  private static boolean isImplementorDefined(RexCall call) {
    if (call.getOperator() instanceof SqlWindowTableFunction
        && RexImpTable.INSTANCE.get((SqlWindowTableFunction) call.getOperator()) != null) {
      return true;
    }
```

### ConstantValue
Condition `sort != null` is always `true`
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
        valueSet.add((Comparable) o);
      }
      if (sort != null
          && sort[0] < 0
          && valueSet.map.keySet().size() == list.size()) {
```

### ConstantValue
Condition is always true
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
  private static List<? extends @Nullable Object> wrap(ColumnMetaData.Rep rep, List<?> list,
      RelDataType type) {
    if (true) {
      return list;
    }
```

### ConstantValue
Condition `childExp != null` is always `true`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableMinus.java`
#### Snippet
```java
              "child" + ord.i,
              result.block);
      assert childExp != null : "childExp must not be null";

      if (minusExp == null) {
```

### ConstantValue
Condition `collation == null` is always `false`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableMergeUnionRule.java`
#### Snippet
```java
    final Sort sort = call.rel(0);
    final RelCollation collation = sort.getCollation();
    if (collation == null || collation.getFieldCollations().isEmpty()) {
      return false;
    }
```

### ConstantValue
Condition `parent != null` is always `true`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableRelImplementor.java`
#### Snippet
```java
      EnumerableRel child,
      EnumerableRel.Prefer prefer) {
    if (parent != null) {
      assert child == parent.getInputs().get(ordinal);
    }
```

### ConstantValue
Condition `rowType.getFieldList() == null` is always `false`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/PhysTypeImpl.java`
#### Snippet
```java
    if (collations.size() == 1) {
      RelFieldCollation collation = collations.get(0);
      RelDataType fieldType = rowType.getFieldList() == null || rowType.getFieldList().isEmpty()
          ? rowType
          : rowType.getFieldList().get(collation.getFieldIndex()).getType();
```

### ConstantValue
Condition `javaClass == null` is always `false`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
      break;
    }
    return javaClass == null || javaClass == Void.class
        ? RexImpTable.NULL_EXPR
        : Expressions.constant(null, javaClass);
```

### ConstantValue
Condition `implementor == null` is always `false`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
    TableFunctionCallImplementor implementor =
        RexImpTable.INSTANCE.get((SqlWindowTableFunction) rexCall.getOperator());
    if (implementor == null) {
      throw Util.needToImplement("implementor of " + rexCall.getOperator().getName());
    }
```

### ConstantValue
Condition `end != prevEnd` is always `false` when reached
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      int start = 0;
      int end = 100;
      if (start != prevStart || end != prevEnd) {
        // builder5
        int actualStart = 0;
```

### ConstantValue
Condition `start != prevStart` is always `true`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
        // builder5
        int actualStart = 0;
        if (start != prevStart || end < prevEnd) {
          // builder6
          // recompute
```

### ConstantValue
Condition `start != prevStart || end < prevEnd` is always `true`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
        // builder5
        int actualStart = 0;
        if (start != prevStart || end < prevEnd) {
          // builder6
          // recompute
```

### ConstantValue
Condition `start != -1` is always `true`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
        prevEnd = end;

        if (start != -1) {
          for (int j = actualStart; j <= end; j++) {
            // builder7
```

### ConstantValue
Condition `materializedTable != null` is always `true`
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
    public Materialization(CalciteSchema.TableEntry materializedTable,
        String sql, List<String> viewSchemaPath) {
      assert materializedTable != null;
      assert sql != null;
      this.materializedTable = materializedTable;
```

### ConstantValue
Condition `sql != null` is always `true`
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
        String sql, List<String> viewSchemaPath) {
      assert materializedTable != null;
      assert sql != null;
      this.materializedTable = materializedTable;
      this.sql = sql;
```

### ConstantValue
Condition `config != null` is always `true`
in `core/src/main/java/org/apache/calcite/prepare/CalciteCatalogReader.java`
#### Snippet
```java
  public CalciteCatalogReader(CalciteSchema rootSchema,
      List<String> defaultSchema, RelDataTypeFactory typeFactory, CalciteConnectionConfig config) {
    this(rootSchema, SqlNameMatchers.withCaseSensitive(config != null && config.caseSensitive()),
        ImmutableList.of(Objects.requireNonNull(defaultSchema, "defaultSchema"),
            ImmutableList.of()),
```

### ConstantValue
Condition `v0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/Utilities.java`
#### Snippet
```java
    //noinspection unchecked
    return v0 == v1 ? 0
        : v0 == null ? 1
            : v1 == null ? -1
                : FlatLists.ComparableListImpl.compare(v0, v1);
```

### ConstantValue
Condition `v1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/Utilities.java`
#### Snippet
```java
    return v0 == v1 ? 0
        : v0 == null ? 1
            : v1 == null ? -1
                : FlatLists.ComparableListImpl.compare(v0, v1);
  }
```

### ConstantValue
Value `origin` is always 'null'
in `core/src/main/java/org/apache/calcite/prepare/CalcitePrepareImpl.java`
#### Snippet
```java
    final List<String> origin = null;
    final List<@Nullable List<String>> origins =
        Collections.nCopies(x.getFieldCount(), origin);
    final List<ColumnMetaData> columns =
        getColumnMetaDataList(typeFactory, x, x, origins);
```

### ConstantValue
Condition `geom == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/RemoveHoleTransformer.java`
#### Snippet
```java

  @Override protected Geometry transformPolygon(Polygon geom, Geometry parent) {
    if (geom == null || geom.isEmpty()) {
      return factory.createPolygon();
    }
```

### ConstantValue
Value `minimal` is always 'true'
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
          final Distribution distribution =
              new Distribution(space.columns, space.valueSet, space.cardinality,
                  space.nullCount, expectedCardinality, minimal);
          final double surprise = distribution.surprise();
          if (CalciteSystemProperty.DEBUG.value() && surprise > 0.1d) {
```

### ConstantValue
Condition `headers != null` is always `true`
in `core/src/main/java/org/apache/calcite/runtime/HttpUtils.java`
#### Snippet
```java
    conn.setConnectTimeout(cTimeout);

    if (headers != null) {
      for (Map.Entry<String, String> me : headers.entrySet()) {
        conn.setRequestProperty(me.getKey(), me.getValue());
```

### ConstantValue
Condition `data == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/HttpUtils.java`
#### Snippet
```java
      }
    }
    if (data == null) {
      return conn.getInputStream();
    }
```

### ConstantValue
Condition `data == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/HttpUtils.java`
#### Snippet
```java
      int cTimeout,
      int rTimeout) throws IOException {
    return executeMethod(data == null ? "GET" : "POST", url, data, headers,
        cTimeout, rTimeout);
  }
```

### ConstantValue
Condition `cause != null` is always `true`
in `core/src/main/java/org/apache/calcite/runtime/CalciteContextException.java`
#### Snippet
```java
      int endPosColumn) {
    super(message, cause);
    assert cause != null;
    setPosition(posLine, posColumn, endPosLine, endPosColumn);
  }
```

### ConstantValue
Condition `k == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
  public static void jsonObjectAggAdd(Map map, String k, @Nullable Object v,
      SqlJsonConstructorNullClause nullClause) {
    if (k == null) {
      throw RESOURCE.nullKeyOfJsonObjectNotAllowed().ex();
    }
```

### ConstantValue
Condition `shuttle != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/DeduplicateCorrelateVariables.java`
#### Snippet
```java

    @Override public RexNode visitSubQuery(RexSubQuery subQuery) {
      if (shuttle != null) {
        RelNode r = subQuery.rel.accept(shuttle); // look inside sub-queries
        if (r != subQuery.rel) {
```

### ConstantValue
Condition `newModifier.getClass() == modifier.getClass()` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
              Collections.singletonList(newInput));
    }
    assert newModifier.getClass() == modifier.getClass();

    // Always project all fields.
```

### ConstantValue
Value `setOp.all` is always 'true'
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    switch (setOp.kind) {
    case UNION:
      relBuilder.union(setOp.all, setOp.getInputs().size());
      break;
    case INTERSECT:
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
      Expression acc = add.accumulator().get(0);
      // This is an example of the generated code
      if (false) {
        new Object() {
          int curentPosition; // position in for-win-agg-loop
```

### ConstantValue
Value `initScalar` is always 'null'
in `core/src/main/java/org/apache/calcite/interpreter/AggregateNode.java`
#### Snippet
```java
      final Scalar addScalar = addScalarProducer.apply(dataContext);
      final Scalar endScalar = castNonNull(null);
      return new ScalarAccumulatorDef(initScalar, addScalar, endScalar,
          rel.getInput().getRowType().getFieldCount(), stateSize, dataContext);
    }
```

### ConstantValue
Value `endScalar` is always 'null'
in `core/src/main/java/org/apache/calcite/interpreter/AggregateNode.java`
#### Snippet
```java
      final Scalar addScalar = addScalarProducer.apply(dataContext);
      final Scalar endScalar = castNonNull(null);
      return new ScalarAccumulatorDef(initScalar, addScalar, endScalar,
          rel.getInput().getRowType().getFieldCount(), stateSize, dataContext);
    }
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
    // Convert "element(<expr>)" to "$element_slice(<expr>)", if the
    // expression is a multiset of scalars.
    if (false) {
      registerOp(SqlStdOperatorTable.ELEMENT,
          (cx, call) -> {
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java

    // Convert "$element_slice(<expr>)" to "element(<expr>).field#0"
    if (false) {
      registerOp(SqlStdOperatorTable.ELEMENT_SLICE,
          (cx, call) -> {
```

### ConstantValue
Condition `rr != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
        cx.getValidator().getValidatedNodeType(call);
    RexRangeRef rr = cx.getSubQueryExpr(call);
    assert rr != null;
    RelDataType msType = rr.getType().getFieldList().get(0).getType();
    RexNode expr =
```

### ConstantValue
Condition `rr != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
        cx.getValidator().getValidatedNodeType(call);
    RexRangeRef rr = cx.getSubQueryExpr(call);
    assert rr != null;
    RelDataType msType = rr.getType().getFieldList().get(0).getType();
    RexNode expr =
```

### ConstantValue
Value `sink` is always 'null'
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
      }
      throw new IllegalStateException(
          "Got a sink " + sink + " to which there is no match source type!");
    }

```

### ConstantValue
Condition `currentRel != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
  private static RexInputRef getNewForOldInputRef(RelNode currentRel,
      Map<RelNode, Frame> map, RexInputRef oldInputRef) {
    assert currentRel != null;

    int oldOrdinal = oldInputRef.getIndex();
```

### ConstantValue
Condition is always false
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
    // If this Filter has correlated reference, create value generator
    // and produce the correlated variables in the new output.
    if (false) {
      if (cm.mapRefRelToCorRef.containsKey(rel)) {
        frame = decorrelateInputWithValueGenerator(rel, frame);
```

### ConstantValue
Condition `oldInput != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java

      final RelNode oldInput = getCorRel(corVar);
      assert oldInput != null;
      final Frame frame = getOrCreateFrame(oldInput);
      assert frame != null;
```

### ConstantValue
Condition `frame != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
      assert oldInput != null;
      final Frame frame = getOrCreateFrame(oldInput);
      assert frame != null;
      final RelNode newInput = frame.r;

```

### ConstantValue
Condition `oldInput != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
    for (CorRef corVar : correlations) {
      final RelNode oldInput = getCorRel(corVar);
      assert oldInput != null;
      final RelNode newInput = getOrCreateFrame(oldInput).r;
      assert newInput != null;
```

### ConstantValue
Condition `newInput != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
      assert oldInput != null;
      final RelNode newInput = getOrCreateFrame(oldInput).r;
      assert newInput != null;

      if (!joinedInputs.contains(newInput)) {
```

### ConstantValue
Condition `oldInput != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
      // the correlated variables.
      final RelNode oldInput = getCorRel(corRef);
      assert oldInput != null;
      final Frame frame = getOrCreateFrame(oldInput);
      final RelNode newInput = frame.r;
```

### ConstantValue
Condition `newInput != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
      final Frame frame = getOrCreateFrame(oldInput);
      final RelNode newInput = frame.r;
      assert newInput != null;

      final List<Integer> newLocalOutputs =
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable long and int values. */
  public static @PolyNull Long minus(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() - b1.longValue());
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable long and int values. */
  public static @PolyNull Long minus(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() - b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable long and int values. */
  public static @PolyNull Long minus(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() - b1.longValue());
```

### ConstantValue
Condition `s == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Helper for CAST(... AS VARBINARY(maxLength)). */
  public static @PolyNull ByteString truncate(@PolyNull ByteString s, int maxLength) {
    if (s == null) {
      return s;
    } else if (s.length() > maxLength) {
```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   */
  public static java.sql.@PolyNull Date internalToDate(@PolyNull Integer v) {
    return v == null ? castNonNull(null) : internalToDate(v.intValue());
  }

```

### ConstantValue
Condition `b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * null. */
  public static @PolyNull Integer plus(int b0, @PolyNull Integer b1) {
    return b1 == null ? castNonNull(null) : (b0 + b1);
  }

```

### ConstantValue
Condition `s == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Helper for CAST(... AS BINARY(maxLength)). */
  public static @PolyNull ByteString truncateOrPad(@PolyNull ByteString s, int maxLength) {
    if (s == null) {
      return s;
    } else {
```

### ConstantValue
Condition `b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * null. */
  public static @PolyNull Integer minus(int b0, @PolyNull Integer b1) {
    return b1 == null ? castNonNull(null) : (b0 - b1);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>/</code> operator applied to nullable long and int values. */
  public static @PolyNull Long divide(Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() / b1.longValue());
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>/</code> operator applied to nullable long and int values. */
  public static @PolyNull Long divide(Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() / b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>/</code> operator applied to nullable long and int values. */
  public static @PolyNull Long divide(Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() / b1.longValue());
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * null. */
  public static @PolyNull Integer divide(@PolyNull Integer b0, int b1) {
    return b0 == null ? castNonNull(null) : (b0 / b1);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal plus(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.add(b1);
  }

```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal plus(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.add(b1);
  }

```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal plus(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.add(b1);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Integer multiply(@PolyNull Integer b0,
      @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 * b1);
  }

```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Integer multiply(@PolyNull Integer b0,
      @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 * b1);
  }

```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Integer multiply(@PolyNull Integer b0,
      @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 * b1);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object multiplyAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object multiplyAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object multiplyAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   */
  public static java.sql.@PolyNull Time internalToTime(@PolyNull Integer v) {
    return v == null ? castNonNull(null) : internalToTime(v.intValue());
  }

```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   */
  public static @PolyNull Long toLongOptional(@PolyNull Timestamp v) {
    return v == null ? castNonNull(null) : toLong(v, LOCAL_TZ);
  }

```

### ConstantValue
Condition `b == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** NULL &rarr; NULL, FALSE &rarr; TRUE, TRUE &rarr; FALSE. */
  public static @PolyNull Boolean not(@PolyNull Boolean b) {
    return b == null ? castNonNull(null) : !b;
  }

```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Integer toIntOptional(java.sql.@PolyNull Date v,
      TimeZone timeZone) {
    return v == null
        ? castNonNull(null)
        : toInt(v, timeZone);
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * has ANY type; either may be null). */
  public static @PolyNull Object minusAny(@PolyNull Object b0, @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * has ANY type; either may be null). */
  public static @PolyNull Object minusAny(@PolyNull Object b0, @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * has ANY type; either may be null). */
  public static @PolyNull Object minusAny(@PolyNull Object b0, @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `o == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static @PolyNull Integer toIntOptional(@PolyNull Object o) {
    return o == null ? castNonNull(null) : toInt(o);
  }

```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static @PolyNull Long toTimestampWithLocalTimeZone(@PolyNull String v) {
    if (v == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Long toTimestampWithLocalTimeZone(@PolyNull String v,
      TimeZone timeZone) {
    if (v == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `a == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Converts a JDBC array to a list. */
  public static @PolyNull List arrayToList(final java.sql.@PolyNull Array a) {
    if (a == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Integer toTimeWithLocalTimeZone(@PolyNull String v,
      TimeZone timeZone) {
    if (v == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   */
  public static @PolyNull Integer toIntOptional(java.sql.@PolyNull Time v) {
    return v == null ? castNonNull(null) : toInt(v);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * null. */
  public static @PolyNull Integer minus(@PolyNull Integer b0, int b1) {
    return b0 == null ? castNonNull(null) : (b0 - b1);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object divideAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object divideAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object divideAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>/</code> operator applied to nullable int and long values. */
  public static @PolyNull Long divide(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() / b1.longValue());
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>/</code> operator applied to nullable int and long values. */
  public static @PolyNull Long divide(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() / b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>/</code> operator applied to nullable int and long values. */
  public static @PolyNull Long divide(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() / b1.longValue());
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal divide(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : b0.divide(b1, MathContext.DECIMAL64);
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal divide(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : b0.divide(b1, MathContext.DECIMAL64);
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal divide(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : b0.divide(b1, MathContext.DECIMAL64);
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable int and long values. */
  public static @PolyNull Long plus(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() + b1.longValue());
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable int and long values. */
  public static @PolyNull Long plus(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() + b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable int and long values. */
  public static @PolyNull Long plus(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() + b1.longValue());
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable long and int values. */
  public static @PolyNull Long plus(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() + b1.longValue());
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable long and int values. */
  public static @PolyNull Long plus(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() + b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable long and int values. */
  public static @PolyNull Long plus(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() + b1.longValue());
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * null. */
  public static @PolyNull Integer multiply(@PolyNull Integer b0, int b1) {
    return b0 == null ? castNonNull(null) : (b0 * b1);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal minus(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.subtract(b1);
  }

```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal minus(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.subtract(b1);
  }

```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal minus(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.subtract(b1);
  }

```

### ConstantValue
Value `condition` is always 'true'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
      throw new IllegalStateException(message);
    }
    return condition;
  }

```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   */
  public static java.sql.@PolyNull Timestamp internalToTimestamp(@PolyNull Long v) {
    return v == null ? castNonNull(null) : internalToTimestamp(v.longValue());
  }

```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   */
  public static @PolyNull Integer toIntOptional(java.sql.@PolyNull Date v) {
    return v == null
        ? castNonNull(null)
        : toInt(v);
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>*</code> operator applied to nullable int and long values. */
  public static @PolyNull Long multiply(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() * b1.longValue());
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>*</code> operator applied to nullable int and long values. */
  public static @PolyNull Long multiply(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() * b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>*</code> operator applied to nullable int and long values. */
  public static @PolyNull Long multiply(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() * b1.longValue());
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * null. */
  public static @PolyNull Integer plus(@PolyNull Integer b0, int b1) {
    return b0 == null ? castNonNull(null) : (b0 + b1);
  }

```

### ConstantValue
Condition `count == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Support the ARRAY_REPEAT function. */
  public static @Nullable List<Object> repeat(Object element, Object count) {
    if (count == null) {
      return null;
    }
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object plusAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object plusAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Object plusAny(@PolyNull Object b0,
      @PolyNull Object b1) {
    if (b0 == null || b1 == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>*</code> operator applied to nullable long and int values. */
  public static @PolyNull Long multiply(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() * b1.longValue());
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>*</code> operator applied to nullable long and int values. */
  public static @PolyNull Long multiply(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() * b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>*</code> operator applied to nullable long and int values. */
  public static @PolyNull Long multiply(@PolyNull Long b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() * b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * null. */
  public static @PolyNull Integer divide(int b0, @PolyNull Integer b1) {
    return b1 == null ? castNonNull(null) : (b0 / b1);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Integer divide(@PolyNull Integer b0,
      @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 / b1);
  }

```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Integer divide(@PolyNull Integer b0,
      @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 / b1);
  }

```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Integer divide(@PolyNull Integer b0,
      @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 / b1);
  }

```

### ConstantValue
Condition `s == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Helper for CAST(... AS VARCHAR(maxLength)). */
  public static @PolyNull String truncate(@PolyNull String s, int maxLength) {
    if (s == null) {
      return s;
    } else if (s.length() > maxLength) {
```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull Long toLongOptional(@PolyNull Timestamp v,
      TimeZone timeZone) {
    if (v == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `o == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static @PolyNull Long toLongOptional(@PolyNull Object o) {
    return o == null ? castNonNull(null) : toLong(o);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal multiply(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.multiply(b1);
  }

```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal multiply(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.multiply(b1);
  }

```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static @PolyNull BigDecimal multiply(@PolyNull BigDecimal b0,
      @PolyNull BigDecimal b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : b0.multiply(b1);
  }

```

### ConstantValue
Condition `s == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Helper for CAST(... AS CHAR(maxLength)). */
  public static @PolyNull String truncateOrPad(@PolyNull String s, int maxLength) {
    if (s == null) {
      return s;
    } else {
```

### ConstantValue
Condition `v == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  public static @PolyNull Integer toTimeWithLocalTimeZone(@PolyNull String v) {
    if (v == null) {
      return castNonNull(null);
    }
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable int and long values. */
  public static @PolyNull Long minus(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() - b1.longValue());
```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable int and long values. */
  public static @PolyNull Long minus(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() - b1.longValue());
```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable int and long values. */
  public static @PolyNull Long minus(@PolyNull Integer b0, @PolyNull Long b1) {
    return (b0 == null || b1 == null)
        ? castNonNull(null)
        : (b0.longValue() - b1.longValue());
```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable int values. */
  public static @PolyNull Integer minus(@PolyNull Integer b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 - b1);
  }

```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable int values. */
  public static @PolyNull Integer minus(@PolyNull Integer b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 - b1);
  }

```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>-</code> operator applied to nullable int values. */
  public static @PolyNull Integer minus(@PolyNull Integer b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 - b1);
  }

```

### ConstantValue
Condition `b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
   * null. */
  public static @PolyNull Integer multiply(int b0, @PolyNull Integer b1) {
    return b1 == null ? castNonNull(null) : (b0 * b1);
  }

```

### ConstantValue
Condition `b0 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable int values. */
  public static @PolyNull Integer plus(@PolyNull Integer b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 + b1);
  }

```

### ConstantValue
Condition `b0 == null || b1 == null` is always `false`
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable int values. */
  public static @PolyNull Integer plus(@PolyNull Integer b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 + b1);
  }

```

### ConstantValue
Condition `b1 == null` is always `false` when reached
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** SQL <code>+</code> operator applied to nullable int values. */
  public static @PolyNull Integer plus(@PolyNull Integer b0, @PolyNull Integer b1) {
    return (b0 == null || b1 == null) ? castNonNull(null) : (b0 + b1);
  }

```

### ConstantValue
Condition `current == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileEnumerator.java`
#### Snippet
```java

  @Override public Object current() {
    if (current == null) {
      this.moveNext();
    }
```

### ConstantValue
Condition `value.primitive != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileFieldType.java`
#### Snippet
```java
      builder.put(value.clazz.getSimpleName(), value);

      if (value.primitive != null) {
        builder.put(value.primitive.primitiveName, value);
      }
```

### ConstantValue
Condition `protoRowType != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileTable.java`
#### Snippet
```java

  @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
    if (protoRowType != null) {
      return protoRowType.apply(typeFactory);
    }
```

### ConstantValue
Condition `nextLine == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/CsvStreamReader.java`
#### Snippet
```java
    do {
      String nextLine = getNextLine();
      if (nextLine == null) {
        return null;
      }
```

### ConstantValue
Condition `fields == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/CsvProjectTableScanRule.java`
#### Snippet
```java
    final CsvTableScan scan = call.rel(1);
    int[] fields = getProjectFields(project.getProjects());
    if (fields == null) {
      // Project contains expressions more complex than just field references.
      return;
```

### ConstantValue
Condition `protoRowType != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/CsvTable.java`
#### Snippet
```java

  @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
    if (protoRowType != null) {
      return protoRowType.apply(typeFactory);
    }
```

### ConstantValue
Condition `fieldTypes == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/CsvTable.java`
#### Snippet
```java
  /** Returns the field types of this CSV table. */
  public List<RelDataType> getFieldTypes(RelDataTypeFactory typeFactory) {
    if (fieldTypes == null) {
      fieldTypes = new ArrayList<>();
      CsvEnumerator.deduceRowType((JavaTypeFactory) typeFactory, source,
```

### ConstantValue
Condition `tables == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
  FileSchema(SchemaPlus parentSchema, String name, File baseDirectory,
      List<Map<String, Object>> tables) {
    this.tables = tables == null ? ImmutableList.of()
        : ImmutableList.copyOf(tables);
    this.baseDirectory = baseDirectory;
```

### ConstantValue
Condition `tableDef != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
    }

    if (tableDef != null) {
      try {
        FileTable table = FileTable.create(source, tableDef);
```

### ConstantValue
Condition `trimmed != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
  private static String trim(String s, String suffix) {
    String trimmed = trimOrNull(s, suffix);
    return trimmed != null ? trimmed : s;
  }

```

### ConstantValue
Condition `baseDirectory == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
    final Source source0 = Sources.url(url);
    final Source source;
    if (baseDirectory == null) {
      source = source0;
    } else {
```

### ConstantValue
Condition `baseDirectory != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
    // Look for files in the directory ending in ".csv", ".csv.gz", ".json",
    // ".json.gz".
    if (baseDirectory != null) {
      final Source baseSource = Sources.of(baseDirectory);
      File[] files = baseDirectory.listFiles((dir, name) -> {
```

### ConstantValue
Condition `groupSet != null` is always `true`
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
      throw new UnsupportedOperationException();
    });
    if (groupSet != null) {
      for (int i : groupSet) {
        if (k++ > 0) {
```

### ConstantValue
Condition `list == null` is always `false`
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
    private Column resolveColumnByAlias(String name) {
      final ImmutableList<Column> list = columnsByAlias.get(name);
      if (list == null || list.size() == 0) {
        throw new RuntimeException("Unknown lattice column '" + name + "'");
      } else if (list.size() == 1) {
```

### ConstantValue
Condition `csvTable != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/CsvTableScan.java`
#### Snippet
```java
    this.fields = fields;

    assert csvTable != null;
  }

```

### ConstantValue
Condition `webTable != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileTableScan.java`
#### Snippet
```java
    this.fields = fields;

    assert webTable != null;
  }

```

### ConstantValue
Condition `this.selectedElement != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
      ArrayList<String> cellText = new ArrayList<>();

      if (this.selectedElement != null) {
        cellText.add(cell.select(this.selector)
            .get(this.selectedElement).ownText());
```

### ConstantValue
Condition `this.replacePattern != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java

      // replace
      if (this.replacePattern != null) {
        Matcher m = this.replacePattern.matcher(cellString);
        cellString = m.replaceAll(this.replaceWith);
```

### ConstantValue
Condition `this.matchPattern == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java

      // match
      if (this.matchPattern == null) {
        return cellString;
      } else {
```

### ConstantValue
Condition `config != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java

    CellReader(Map<String, Object> config) {
      if (config != null) {
        this.type = (String) config.get("type");
        this.selector = (String) config.get("selector");
```

### ConstantValue
Condition `string == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    @SuppressWarnings("JavaUtilDate")
    private Object toObject(FileFieldType fieldType, String string) {
      if ((string == null) || (string.length() == 0)) {
        return null;
      }
```

### ConstantValue
Condition `fieldType == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
      }

      if (fieldType == null) {
        return string;
      }
```

### ConstantValue
Condition `this.headings == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java
  Elements getHeadings() throws FileReaderException {

    if (this.headings == null) {
      this.iterator();
    }
```

### ConstantValue
Condition `this.tableElement == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java

  @Override public FileReaderIterator iterator() {
    if (this.tableElement == null) {
      try {
        getTable();
```

### ConstantValue
Condition is always true
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java
    // TODO: this needs to be reworked to properly cache the headings
    //if (this.headings == null) {
    if (true) {
      // first row must contain headings
      Elements headings = iterator.next("th");
```

### ConstantValue
Condition `this.selector != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java
    }

    this.tableElement = (this.selector != null && !this.selector.equals(""))
        ? getSelectedTable(doc, this.selector) : getBestTable(doc);
  }
```

### ConstantValue
Condition `this.index == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java
    Element el;

    if (this.index == null) {
      if (list.size() != 1) {
        throw new FileReaderException("" + list.size()
```

### ConstantValue
Condition `fieldType == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
      RelDataType type;

      if (fieldType == null) {
        type = typeFactory.createJavaType(String.class);
      } else {
```

### ConstantValue
Condition `this.fieldConfigs != null` is always `true`
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
      final Set<String> colNames = new HashSet<>();
      final Set<String> sources = new HashSet<>();
      if (this.fieldConfigs != null) {
        try {
          for (Map<String, Object> fieldConfig : this.fieldConfigs) {
```

### ConstantValue
Condition `source == null` is always `false`
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java
  public FileReader(Source source, String selector, Integer index)
      throws FileReaderException {
    if (source == null) {
      throw new FileReaderException("source must not be null");
    }
```

### ConstantValue
Condition `value == null` is always `false`
in `plus/src/main/java/org/apache/calcite/adapter/os/SqlShell.java`
#### Snippet
```java
      private void value(StringBuilder b, String value, int length,
          boolean right) {
        if (value == null) {
          pad(b, length, ' ');
        } else {
```

### ConstantValue
Condition `s == null` is always `false`
in `plus/src/main/java/org/apache/calcite/adapter/os/SqlShell.java`
#### Snippet
```java

      private void value(StringBuilder b, String s) {
        if (s == null) {
          // do nothing - unfortunately same as empty string
        } else if (s.contains("\"")) {
```

### ConstantValue
Condition `outputType == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/ExtractionDimensionSpec.java`
#### Snippet
```java
    this.extractionFunction = Objects.requireNonNull(extractionFunction, "extractionFunction");
    this.outputName = outputName;
    this.outputType = outputType == null ? DruidType.STRING : outputType;
  }

```

### ConstantValue
Condition `connection != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTable.java`
#### Snippet
```java
      Set<String> metricNameSet, String timestampColumnName,
      DruidConnectionImpl connection, Map<String, List<ComplexMetric>> complexMetrics) {
    assert connection != null;

    connection.metadata(dataSourceName, timestampColumnName, intervals,
```

### ConstantValue
Condition `intervals != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTable.java`
#### Snippet
```java
    this.protoRowType = protoRowType;
    this.metricFieldNames = ImmutableSet.copyOf(metricFieldNames);
    this.intervals = intervals != null ? ImmutableList.copyOf(intervals)
        : ImmutableList.of(DEFAULT_INTERVAL);
    this.complexMetrics = complexMetrics == null ? ImmutableMap.of()
```

### ConstantValue
Condition `complexMetrics == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTable.java`
#### Snippet
```java
    this.intervals = intervals != null ? ImmutableList.copyOf(intervals)
        : ImmutableList.of(DEFAULT_INTERVAL);
    this.complexMetrics = complexMetrics == null ? ImmutableMap.of()
            : ImmutableMap.copyOf(complexMetrics);
    this.allFields = allFields == null ? ImmutableMap.of()
```

### ConstantValue
Condition `allFields == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTable.java`
#### Snippet
```java
    this.complexMetrics = complexMetrics == null ? ImmutableMap.of()
            : ImmutableMap.copyOf(complexMetrics);
    this.allFields = allFields == null ? ImmutableMap.of()
            : ImmutableMap.copyOf(allFields);
  }
```

### ConstantValue
Condition `tableMap == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSchema.java`
#### Snippet
```java
    }

    if (tableMap == null) {
      final DruidConnectionImpl connection =
          new DruidConnectionImpl(url, coordinatorUrl);
```

### ConstantValue
Condition `call != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/ComplexMetric.java`
#### Snippet
```java
    case HYPER_UNIQUE:
    case THETA_SKETCH:
      return call != null
            && call.getAggregation().getKind() == SqlKind.COUNT
            && call.isDistinct();
```

### ConstantValue
Value `simpleFilter` is always 'null'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
              rowType, druidQuery, rexBuilder);
      return simpleFilter != null ? new JsonCompositeFilter(Type.NOT, simpleFilter)
          : simpleFilter;
    case AND:
    case OR:
```

### ConstantValue
Condition `lower != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
      generator.writeStringField("type", type.lowercase());
      generator.writeStringField("dimension", dimension);
      if (lower != null) {
        generator.writeStringField("lower", lower);
        generator.writeBooleanField("lowerStrict", lowerStrict);
```

### ConstantValue
Condition `upper != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
        generator.writeBooleanField("lowerStrict", lowerStrict);
      }
      if (upper != null) {
        generator.writeStringField("upper", upper);
        generator.writeBooleanField("upperStrict", upperStrict);
```

### ConstantValue
Condition `druidJsonFilter == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java

  protected static @Nullable DruidJsonFilter toNotDruidFilter(DruidJsonFilter druidJsonFilter) {
    if (druidJsonFilter == null) {
      return null;
    }
```

### ConstantValue
Condition `s == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidExpressions.java`
#### Snippet
```java

  public static String stringLiteral(final String s) {
    return s == null ? nullLiteral() : "'" + escape(s) + "'";
  }

```

### ConstantValue
Condition `n == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidExpressions.java`
#### Snippet
```java

  public static String numberLiteral(final Number n) {
    return n == null ? nullLiteral() : n.toString();
  }

```

### ConstantValue
Condition `timeZoneConf == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSqlCastConverter.java`
#### Snippet
```java
    final SqlTypeName toType = rexNode.getType().getSqlTypeName();
    final String timeZoneConf = druidQuery.getConnectionConfig().timeZone();
    final TimeZone timeZone = TimeZone.getTimeZone(timeZoneConf == null ? "UTC" : timeZoneConf);
    final boolean nullEqualToEmpty = druidQuery.getConnectionConfig().nullEqualToEmpty();

```

### ConstantValue
Condition `fromTypeString == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSqlCastConverter.java`
#### Snippet
```java
    final boolean nullEqualToEmpty = druidQuery.getConnectionConfig().nullEqualToEmpty();

    if (fromTypeString == null) {
      fromTypeString = nullEqualToEmpty ? "" :  null;
    }
```

### ConstantValue
Condition `type != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidType.java`
#### Snippet
```java
  /** Returns a DruidType matching the given String type from a Druid metric. */
  static DruidType getTypeFromMetric(String type) {
    assert type != null;
    if (type.equals("hyperUnique")) {
      return HYPER_UNIQUE;
```

### ConstantValue
Condition `type != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidType.java`
#### Snippet
```java
  /** Returns a DruidType matching the String from a meta data query. */
  static DruidType getTypeFromMetaData(String type) {
    assert type != null;
    switch (type) {
    case "LONG":
```

### ConstantValue
Condition `outputType == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/VirtualColumn.java`
#### Snippet
```java
    this.name = Objects.requireNonNull(name, "name");
    this.expression = Objects.requireNonNull(expression, "expression");
    this.outputType = outputType == null ? DruidType.FLOAT : outputType;
  }

```

### ConstantValue
Condition `outputType == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DefaultDimensionSpec.java`
#### Snippet
```java
    this.dimension = Objects.requireNonNull(dimension, "dimension");
    this.outputName = Objects.requireNonNull(outputName, "outputName");
    this.outputType = outputType == null ? DruidType.STRING : outputType;
  }

```

### ConstantValue
Condition `timeZone != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java
        final String timeZone = cluster.getPlanner().getContext()
            .unwrap(CalciteConnectionConfig.class).timeZone();
        assert timeZone != null;
        intervals =
            DruidDateTimeUtils.createInterval(
```

### ConstantValue
Condition `pair == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java
      final Pair<List<RexNode>, List<RexNode>> pair =
          splitProjects(rexBuilder, query, project.getProjects());
      if (pair == null) {
        // We can't push anything useful to Druid.
        return;
```

### ConstantValue
Condition `timeZone == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
    final String castFormat = DruidSqlCastConverter
        .dateTimeFormatString(rexCall.getType().getSqlTypeName());
    final String timeZoneId = timeZone == null ? null : timeZone.getID();
    if (castFormat == null) {
      // unknown format
```

### ConstantValue
Condition `castFormat == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
        .dateTimeFormatString(rexCall.getType().getSqlTypeName());
    final String timeZoneId = timeZone == null ? null : timeZone.getID();
    if (castFormat == null) {
      // unknown format
      return null;
```

### ConstantValue
Condition `timeUnit == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
   */
  public static Granularity.@Nullable Type toDruidGranularity(TimeUnitRange timeUnit) {
    if (timeUnit == null) {
      return null;
    }
```

### ConstantValue
Condition `geodeTable != null` is always `true`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeTableScan.java`
#### Snippet
```java
    this.projectRowType = projectRowType;

    assert geodeTable != null;
    assert getConvention() == GeodeRel.CONVENTION;
  }
```

### ConstantValue
Condition `projectRowType != null` is always `true`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeTableScan.java`
#### Snippet
```java

  @Override public RelDataType deriveRowType() {
    return projectRowType != null ? projectRowType : super.deriveRowType();
  }

```

### ConstantValue
Condition `fields != null` is always `true`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRel.java`
#### Snippet
```java
     */
    public void addSelectFields(Map<String, String> fields) {
      if (fields != null) {
        selectFields.putAll(fields);
      }
```

### ConstantValue
Condition `predicates != null` is always `true`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRel.java`
#### Snippet
```java
     */
    public void addPredicates(List<String> predicates) {
      if (predicates != null) {
        whereClause.addAll(predicates);
      }
```

### ConstantValue
Condition `results == null` is always `false`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeEnumerator.java`
#### Snippet
```java
   */
  GeodeEnumerator(SelectResults results, RelProtoDataType protoRowType) {
    if (results == null) {
      LOGGER.warn("Null OQL results!");
    }
```

### ConstantValue
Condition `results == null` is always `false`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeEnumerator.java`
#### Snippet
```java
      LOGGER.warn("Null OQL results!");
    }
    this.iterator = (results == null) ? Collections.emptyIterator() : results.iterator();
    this.current = null;

```

### ConstantValue
Condition `tableMap == null` is always `false`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSchema.java`
#### Snippet
```java
  @Override protected Map<String, Table> getTableMap() {

    if (tableMap == null) {

      final ImmutableMap.Builder<String, Table> builder = ImmutableMap.builder();
```

### ConstantValue
Condition `limit != null` is always `true`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeTable.java`
#### Snippet
```java
      queryBuilder.append(Util.toString(orderByFields, " ORDER BY ", ", ", ""));
    }
    if (limit != null) {
      queryBuilder.append(" LIMIT " + limit);
    }
```

### ConstantValue
Condition `fieldType instanceof PdxInstance` is always `false`
in `geode/src/main/java/org/apache/calcite/adapter/geode/util/JavaTypeFactoryExtImpl.java`
#### Snippet
```java
      Object field = pdxInstance.getField(fieldName);
      final Type fieldType = field.getClass();
      if (fieldType instanceof PdxInstance) {
        recursiveCreatePdxType(
            (PdxInstance) field, list, fieldNamePrefix + fieldName + ".");
```

### ConstantValue
Condition `tableMap == null` is always `false`
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleSchema.java`
#### Snippet
```java
  @Override protected Map<String, Table> getTableMap() {

    if (tableMap == null) {
      final ImmutableMap.Builder<String, Table> builder = ImmutableMap.builder();

```

### ConstantValue
Condition `o.aggregators != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
          fieldBuilder.put(entry.getKey(), druidType.sqlType);
        }
        if (o.aggregators != null) {
          for (Map.Entry<String, JsonAggregator> entry
              : o.aggregators.entrySet()) {
```

### ConstantValue
Value `data` is always 'null'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
    final String url = coordinatorUrl + "/druid/coordinator/v1/metadata/datasources";
    if (CalciteSystemProperty.DEBUG.value()) {
      System.out.println("Druid: table names" + data + "; " + url);
    }
    try (InputStream in0 = post(url, data, requestHeaders, 10000, 1800000);
```

### ConstantValue
Value `data` is always 'null'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
      System.out.println("Druid: table names" + data + "; " + url);
    }
    try (InputStream in0 = post(url, data, requestHeaders, 10000, 1800000);
         InputStream in = traceResponse(in0)) {
      final ObjectMapper mapper = new ObjectMapper();
```

### ConstantValue
Condition `name == null` is always `false`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java

        String name = getLeftNodeFieldName(left);
        if (name == null) {
          return false;
        }
```

### ConstantValue
Condition `expression != null` is always `true`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
      final RexNode right = call.operands.get(1);
      String expression = translateBinary2(op, left, right);
      if (expression != null) {
        return expression;
      }
```

### ConstantValue
Condition `leftNodeFieldName != null` is always `true`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
      String leftNodeFieldName = getLeftNodeFieldNameForNode(node);

      if (leftNodeFieldName != null) {
        leftNodeDisjunctions = disjunctions.stream().filter(rexNode -> {
          RexCall rexCall = (RexCall) rexNode;
```

### ConstantValue
Condition `tableOptions.getConsumer() != null` is always `true`
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaStreamTable.java`
#### Snippet
```java
    return new AbstractEnumerable<@Nullable Object[]>() {
      @Override public Enumerator<@Nullable Object[]> enumerator() {
        if (tableOptions.getConsumer() != null) {
          return new KafkaMessageEnumerator(tableOptions.getConsumer(),
              tableOptions.getRowConverter(), cancelFlag);
```

### ConstantValue
Condition `o == null` is always `false`
in `geode/src/main/java/org/apache/calcite/adapter/geode/util/GeodeUtils.java`
#### Snippet
```java
  @SuppressWarnings("JavaUtilDate")
  private static Object convert(Object o, Class clazz) {
    if (o == null) {
      return null;
    }
```

### ConstantValue
Condition `protoRowType != null` is always `true`
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTable.java`
#### Snippet
```java

  @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
    if (protoRowType != null) {
      return protoRowType.apply(typeFactory);
    }
```

### ConstantValue
Condition `allFields == null` is always `false`
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTable.java`
#### Snippet
```java
    this.tableName = tableName;
    this.protoRowType = protoRowType;
    this.allFields = allFields == null ? ImmutableMap.of()
        : ImmutableMap.copyOf(allFields);
    this.dataFormat = dataFormat;
```

### ConstantValue
Condition `redisDataFormat != null` is always `true`
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisEnumerator.java`
#### Snippet
```java
    String dataFormat = tableFieldInfo.getDataFormat();
    RedisDataFormat redisDataFormat = RedisDataFormat.fromTypeName(dataFormat);
    assert redisDataFormat != null;
    if (redisDataFormat == RedisDataFormat.RAW) {
      fieldBuilder.put("key", "key");
```

### ConstantValue
Condition `havingExpr != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    // implement HAVING (we have already checked that it is non-trivial)
    relBuilder.push(bb.root());
    if (havingExpr != null) {
      relBuilder.filter(havingExpr);
    }
```

### ConstantValue
Condition `constExpr != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
                config.isExplain());
      }
      if (constExpr != null) {
        subQuery.expr = constExpr;
        mapConvertedNonCorrSubqs.put(call, constExpr);
```

### ConstantValue
Condition `select != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      RelFieldCollation.Direction direction,
      RelFieldCollation.NullDirection nullDirection) {
    assert select != null;
    // Handle DESC keyword, e.g. 'select a, b from t order by a desc'.
    switch (orderItem.getKind()) {
```

### ConstantValue
Condition `select != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    // TODO:  add validation rules to SqlValidator also
    assert bb.root != null : "precondition: child != null";
    assert select != null;
    if (orderList == null) {
      return;
```

### ConstantValue
Condition `joinCond != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      RexNode joinCond,
      JoinRelType joinType) {
    assert joinCond != null;

    final CorrelationUse p = getCorrelationUse(bb, rightRel);
```

### ConstantValue
Condition `scope != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      @Nullable RelDataType targetRowType) {
    final SqlValidatorScope scope = validator().getOverScope(values);
    assert scope != null;
    final Blackboard bb = createBlackboard(scope, null, false);
    convertValuesImpl(bb, values, targetRowType);
```

### ConstantValue
Condition `parentScope != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
          break;
        }
      } while (parentScope != null);
    }
    return true;
```

### ConstantValue
Condition `outerCall != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
        boolean ignoreNulls, SqlCall outerCall) {
      assert bb.agg == this;
      assert outerCall != null;
      final List<SqlNode> operands = call.getOperandList();
      final SqlParserPos pos = call.getParserPosition();
```

### ConstantValue
Value `orderList` is always 'null'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      case WITHIN_DISTINCT:
        assert orderList == null;
        translateAgg(call.operand(0), filter, call.operand(1), orderList,
            ignoreNulls, outerCall);
        return;
```

### ConstantValue
Condition `e != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
          for (SqlNode distinct : distinctList) {
            RexNode e = bb.convertExpression(distinct);
            assert e != null;
            distinctBuilder.set(lookupOrCreateGroupExpr(e));
          }
```

### ConstantValue
Condition `targetRowType != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    final RelDataType targetRowType =
        validator().getValidatedNodeType(call);
    assert targetRowType != null;
    RelNode sourceRel =
        convertQueryRecursive(call.getSource(), true, targetRowType).project();
```

### ConstantValue
Condition `node != null` is always `true`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
            new LookupContext(this, inputs, systemFieldList.size());
        final RexNode node = lookup(resolve.path.steps().get(0).i, rels);
        assert node != null;
        return Pair.of(node, (e, fieldName) -> {
          final RelDataTypeField field =
```

### ConstantValue
Value `bb.root` is always 'null'
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    }

    assert rel != null : "rel must not be null, root = " + bb.root;
    // Usual case: all expressions in the SELECT clause are different.
    final ImmutableBitSet groupSet =
```

### ConstantValue
Condition is always true
in `spark/src/main/java/org/apache/calcite/adapter/spark/EnumerableToSparkConverter.java`
#### Snippet
```java
    //   Enumerable source = ...;
    //   return SparkRuntime.createRdd(sparkContext, source);
    if (true) {
      throw new RuntimeException("EnumerableToSparkConverter is not implemented");
    }
```

### ConstantValue
Condition `localIpAddress == null` is always `false`
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java
  static synchronized String localIpAddress() {
    synchronized (HttpServer.class) {
      if (localIpAddress == null) {
        try {
          localIpAddress = findLocalIpAddress();
```

### ConstantValue
Condition `server == null` is always `false`
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java

  void stop() {
    if (server == null) {
      throw new RuntimeException("Server is already stopped");
    } else {
```

### ConstantValue
Condition `server != null` is always `true`
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java

  void start() {
    if (server != null) {
      throw new RuntimeException("Server is already started");
    } else {
```

### ConstantValue
Condition `server == null` is always `false`
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java
   */
  String uri() {
    if (server == null) {
      throw new RuntimeException("Server is not started");
    } else {
```

### ConstantValue
Condition `parent != null` is always `true`
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkToEnumerableConverter.java`
#### Snippet
```java

    @Override SparkRel.Result visitInput(SparkRel parent, int ordinal, SparkRel input) {
      if (parent != null) {
        assert input == parent.getInputs().get(ordinal);
      }
```

### ConstantValue
Condition `v == null` is always `false`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbEnumerator.java`
#### Snippet
```java

  private static Time shift(Time v) {
    if (v == null) {
      return null;
    }
```

### ConstantValue
Condition `obj == null` is always `false`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbEnumerator.java`
#### Snippet
```java
   */
  private static Object convertToEnumeratorObject(Object obj, RelDataType relDataType) {
    if (obj == null) {
      return null;
    }
```

### ConstantValue
Condition `v == null` is always `false`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbEnumerator.java`
#### Snippet
```java

  private static Timestamp shift(Timestamp v) {
    if (v == null) {
      return null;
    }
```

### ConstantValue
Condition `fields != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbRel.java`
#### Snippet
```java

    public void addSelectFields(Map<String, String> fields) {
      if (fields != null) {
        selectFields.putAll(fields);
      }
```

### ConstantValue
Condition `projectRowType != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbTableScan.java`
#### Snippet
```java

  @Override public RelDataType deriveRowType() {
    return projectRowType != null ? projectRowType : super.deriveRowType();
  }

```

### ConstantValue
Condition `innodbTable != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbTableScan.java`
#### Snippet
```java
    this.forceIndexName = getForceIndexName(hints).orElse(null);
    this.indexCondition = getIndexCondition();
    assert innodbTable != null;
    assert getConvention() == InnodbRel.CONVENTION;
  }
```

### ConstantValue
Condition `name != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java

  public boolean nameMatch(String name) {
    return name != null && name.equalsIgnoreCase(indexName);
  }

```

### ConstantValue
Condition `fieldNames != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
  private static RelCollation deduceImplicitCollation(List<String> fieldNames,
      List<String> indexColumnNames) {
    checkState(fieldNames != null, "field names cannot be null");
    List<RelFieldCollation> keyCollations = new ArrayList<>(indexColumnNames.size());
    for (String keyColumnName : indexColumnNames) {
```

### ConstantValue
Condition `implicitCollation != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
    this.indexColumnNames = indexColumnNames;
    this.implicitCollation =
        implicitCollation != null ? implicitCollation
            : deduceImplicitCollation(fieldNames, indexColumnNames);
    this.pushDownConditions =
```

### ConstantValue
Condition `pushDownConditions == null` is always `false`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
            : deduceImplicitCollation(fieldNames, indexColumnNames);
    this.pushDownConditions =
        pushDownConditions == null ? ImmutableList.of()
            : ImmutableList.copyOf(pushDownConditions);
    this.remainderConditions =
```

### ConstantValue
Condition `remainderConditions == null` is always `false`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
            : ImmutableList.copyOf(pushDownConditions);
    this.remainderConditions =
        remainderConditions == null ? ImmutableList.of()
            : ImmutableList.copyOf(remainderConditions);
    this.queryType = queryType;
```

### ConstantValue
Condition `page.pagingIdentifier != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
            querySpec.fieldNames, fieldTypes, page);
      } while (!limitQuery
          && page.pagingIdentifier != null
          && page.totalRowCount > 0);
    }
```

### ConstantValue
Condition `collationIndexes != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      List<String> queryOutputFieldNames) {
    final List<JsonCollation> collations;
    if (collationIndexes != null) {
      assert collationDirections != null;
      ImmutableList.Builder<JsonCollation> colBuilder = ImmutableList.builder();
```

### ConstantValue
Condition `collationDirections != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    final List<JsonCollation> collations;
    if (collationIndexes != null) {
      assert collationDirections != null;
      ImmutableList.Builder<JsonCollation> colBuilder = ImmutableList.builder();
      for (Pair<Integer, Direction> p : Pair.zip(collationIndexes, collationDirections)) {
```

### ConstantValue
Condition `havingFilter == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      writeField(generator, "intervals", intervals);
      writeFieldIf(generator, "having",
          havingFilter == null ? null : new DruidJsonFilter.JsonDimHavingFilter(havingFilter));
      generator.writeEndObject();
      generator.close();
```

### ConstantValue
Condition `groupSet == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    final DruidJsonFilter jsonFilter = computeFilter(filter);

    if (groupSet == null) {
      // It is Scan Query since no Grouping
      assert aggCalls == null;
```

### ConstantValue
Condition `aggCalls != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    // we need to extract the conditions to know whether the query will be executed as a
    // Timeseries, TopN, or GroupBy in Druid
    assert aggCalls != null;
    assert aggNames != null;
    assert aggCalls.size() == aggNames.size();
```

### ConstantValue
Condition `aggNames != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    // Timeseries, TopN, or GroupBy in Druid
    assert aggCalls != null;
    assert aggNames != null;
    assert aggCalls.size() == aggNames.size();

```

### ConstantValue
Condition `havingFilter != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

    final DruidJsonFilter havingJsonFilter;
    if (havingFilter != null) {
      havingJsonFilter =
          DruidJsonFilter.toDruidFilters(havingFilter.getCondition(),
```

### ConstantValue
Condition `postProject != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    // Then we handle projects after aggregates as Druid Post Aggregates
    final List<String> postAggregateStageFieldNames;
    if (postProject != null) {
      final List<String> postProjectDimListBuilder = new ArrayList<>();
      final RelDataType postAggInputRowType = getCluster().getTypeFactory()
```

### ConstantValue
Condition `postAggregateStageFieldNames == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

    // final Query output row field names.
    final List<String> queryOutputFieldNames = postAggregateStageFieldNames == null
        ? aggregateStageFieldNames
        : postAggregateStageFieldNames;
```

### ConstantValue
Result of `planAsTimeSeries(groupByKeyDims, jsonFilter, virtualColumnList, aggregations, postAggs, ...` is always 'null'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

    final String timeSeriesQueryString =
        planAsTimeSeries(groupByKeyDims, jsonFilter,
            virtualColumnList, aggregations, postAggs, limit, havingJsonFilter);
    if (timeSeriesQueryString != null) {
      final String timeExtractColumn = groupByKeyDims.isEmpty()
```

### ConstantValue
Condition `timeSeriesQueryString != null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        planAsTimeSeries(groupByKeyDims, jsonFilter,
            virtualColumnList, aggregations, postAggs, limit, havingJsonFilter);
    if (timeSeriesQueryString != null) {
      final String timeExtractColumn = groupByKeyDims.isEmpty()
          ? null
```

### ConstantValue
Result of `planAsTopN(groupByKeyDims, jsonFilter, virtualColumnList, aggregations, postAggs, limit,...` is always 'null'
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    }
    final String topNQuery =
        planAsTopN(groupByKeyDims, jsonFilter,
            virtualColumnList, aggregations, postAggs, limit, havingJsonFilter);
    if (topNQuery != null) {
      return new QuerySpec(QueryType.TOP_N, topNQuery, queryOutputFieldNames);
```

### ConstantValue
Condition `topNQuery != null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        planAsTopN(groupByKeyDims, jsonFilter,
            virtualColumnList, aggregations, postAggs, limit, havingJsonFilter);
    if (topNQuery != null) {
      return new QuerySpec(QueryType.TOP_N, topNQuery, queryOutputFieldNames);
    }
```

### ConstantValue
Condition `o != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
  protected static void writeFieldIf(JsonGenerator generator, String fieldName,
      Object o) throws IOException {
    if (o != null) {
      writeField(generator, fieldName, o);
    }
```

### ConstantValue
Condition `pagingIdentifier == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

    public String getQueryString(String pagingIdentifier, int offset) {
      if (pagingIdentifier == null) {
        return queryString;
      }
```

### ConstantValue
Condition `fetchLimit != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        writeField(generator, "columns", columns);
        generator.writeStringField("resultFormat", "compactedList");
        if (fetchLimit != null) {
          generator.writeNumberField("limit", fetchLimit);
        }
```

### ConstantValue
Condition `havingFilter != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      List<VirtualColumn> virtualColumnList, List<JsonAggregation> aggregations,
      List<JsonExpressionPostAgg> postAggregations, JsonLimit limit, DruidJsonFilter havingFilter) {
    if (havingFilter != null) {
      return null;
    }
```

### ConstantValue
Condition `havingFilter != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      List<VirtualColumn> virtualColumnList, List<JsonAggregation> aggregations,
      List<JsonExpressionPostAgg> postAggregations, JsonLimit limit, DruidJsonFilter havingFilter) {
    if (havingFilter != null) {
      return null;
    }
```

### ConstantValue
Condition `druidColumn.right != null` is always `true` when reached
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
                DruidExpressions.EXPRESSION_TYPES.get(project.getType().getSqlTypeName()));
        usedFieldNames.add(druidColumn.left);
      } else if (druidColumn.left != null && druidColumn.right != null) {
       // CASE it is an extraction Dimension
        final String columnPrefix;
```

### ConstantValue
Condition `filter != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    final RexNode filter = filterRel.getCondition();
    final RelDataType inputRowType = filterRel.getInput().getRowType();
    if (filter != null) {
      return DruidJsonFilter.toDruidFilters(filter, inputRowType, this,
          getCluster().getRexBuilder());
```

### ConstantValue
Condition `complexMetric == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      if (aggCall.isDistinct()) {
        if (aggCall.isApproximate() || config.approximateDistinctCount()) {
          if (complexMetric == null) {
            aggregation =
                new JsonCardinalityAggregation("cardinality", name,
```

### ConstantValue
Condition `!aggCall.isDistinct()` is always `true` when reached
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        }
      }
      if (aggCall.getArgList().size() == 1 && !aggCall.isDistinct()) {
        // case we have count(column) push it as count(*) where column is not null
        final DruidJsonFilter matchNulls;
```

### ConstantValue
Condition `fieldName == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        // case we have count(column) push it as count(*) where column is not null
        final DruidJsonFilter matchNulls;
        if (fieldName == null) {
          matchNulls = new DruidJsonFilter.JsonExpressionFilter(aggExpression + " == null");
        } else {
```

### ConstantValue
Condition `!aggCall.isDistinct()` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
            new JsonFilteredAggregation(DruidJsonFilter.toNotDruidFilter(matchNulls),
                new JsonAggregation("count", name, fieldName, aggExpression));
      } else if (!aggCall.isDistinct()) {
        aggregation = new JsonAggregation("count", name, fieldName, aggExpression);
      } else {
```

### ConstantValue
Condition `aggregation == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    }

    if (aggregation == null) {
      return null;
    }
```

### ConstantValue
Condition `filterNode != null` is always `true`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    }
    // translate filters
    if (filterNode != null) {
      DruidJsonFilter druidFilter =
          DruidJsonFilter.toDruidFilters(filterNode,
```

### ConstantValue
Condition `querySpec == null` is always `false`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

  public QuerySpec getQuerySpec() {
    if (querySpec == null) {
      querySpec = deriveQuerySpec();
      assert querySpec != null : this;
```

### ConstantValue
Condition `converted != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbRules.java`
#### Snippet
```java
      final Sort sort = call.rel(0);
      final RelNode converted = convert(sort);
      if (converted != null) {
        call.transformTo(converted);
      }
```

### ConstantValue
Condition `converted != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbRules.java`
#### Snippet
```java
      if (filter.getTraitSet().contains(Convention.NONE)) {
        final RelNode converted = convert(filter, scan);
        if (converted != null) {
          call.transformTo(converted);
        }
```

### ConstantValue
Condition `pkMeta != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilterTranslator.java`
#### Snippet
```java

    // try to push down filter by primary key
    if (pkMeta != null) {
      IndexCondition pkPushDownCond = findPushDownCondition(rexNodeList, pkMeta);
      indexConditions.add(pkPushDownCond);
```

### ConstantValue
Condition `rexCall != null` is always `true`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilterTranslator.java`
#### Snippet
```java

  private static boolean isSqlTypeMatch(RexCall rexCall, SqlTypeName sqlTypeName) {
    assert rexCall != null;
    return rexCall.type.getSqlTypeName() == sqlTypeName;
  }
```

### ConstantValue
Condition `condition != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/WhileStatement.java`
#### Snippet
```java
  public WhileStatement(Expression condition, Statement body) {
    super(ExpressionType.While, Void.TYPE);
    assert condition != null : "condition should not be null";
    assert body != null : "body should not be null";
    this.condition = condition;
```

### ConstantValue
Condition `body != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/WhileStatement.java`
#### Snippet
```java
    super(ExpressionType.While, Void.TYPE);
    assert condition != null : "condition should not be null";
    assert body != null : "body should not be null";
    this.condition = condition;
    this.body = body;
```

### ConstantValue
Condition `nodeType != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expression.java`
#### Snippet
```java
  protected Expression(ExpressionType nodeType, Type type) {
    super(nodeType, type);
    assert nodeType != null;
    assert type != null;
  }
```

### ConstantValue
Condition `type != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expression.java`
#### Snippet
```java
    super(nodeType, type);
    assert nodeType != null;
    assert type != null;
  }

```

### ConstantValue
Condition `name != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/LabelTarget.java`
#### Snippet
```java
    LabelTarget that = (LabelTarget) o;

    if (name != null ? !name.equals(that.name) : that.name != null) {
      return false;
    }
```

### ConstantValue
Condition `expression0 != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TernaryExpression.java`
#### Snippet
```java
      Expression expression1, Expression expression2) {
    super(nodeType, type);
    assert expression0 != null : "expression0 should not be null";
    assert expression1 != null : "expression1 should not be null";
    assert expression2 != null : "expression2 should not be null";
```

### ConstantValue
Condition `expression1 != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TernaryExpression.java`
#### Snippet
```java
    super(nodeType, type);
    assert expression0 != null : "expression0 should not be null";
    assert expression1 != null : "expression1 should not be null";
    assert expression2 != null : "expression2 should not be null";
    this.expression0 = expression0;
```

### ConstantValue
Condition `expression2 != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TernaryExpression.java`
#### Snippet
```java
    assert expression0 != null : "expression0 should not be null";
    assert expression1 != null : "expression1 should not be null";
    assert expression2 != null : "expression2 should not be null";
    this.expression0 = expression0;
    this.expression1 = expression1;
```

### ConstantValue
Condition `type != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/AbstractNode.java`
#### Snippet
```java
      return false;
    }
    if (type != null ? !type.equals(that.type) : that.type != null) {
      return false;
    }
```

### ConstantValue
Condition `name != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodDeclaration.java`
#### Snippet
```java
  public MethodDeclaration(int modifier, String name, Type resultType,
      List<ParameterExpression> parameters, BlockStatement body) {
    assert name != null : "name should not be null";
    assert resultType != null : "resultType should not be null";
    assert parameters != null : "parameters should not be null";
```

### ConstantValue
Condition `resultType != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodDeclaration.java`
#### Snippet
```java
      List<ParameterExpression> parameters, BlockStatement body) {
    assert name != null : "name should not be null";
    assert resultType != null : "resultType should not be null";
    assert parameters != null : "parameters should not be null";
    assert body != null : "body should not be null";
```

### ConstantValue
Condition `parameters != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodDeclaration.java`
#### Snippet
```java
    assert name != null : "name should not be null";
    assert resultType != null : "resultType should not be null";
    assert parameters != null : "parameters should not be null";
    assert body != null : "body should not be null";
    this.modifier = modifier;
```

### ConstantValue
Condition `body != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodDeclaration.java`
#### Snippet
```java
    assert resultType != null : "resultType should not be null";
    assert parameters != null : "parameters should not be null";
    assert body != null : "body should not be null";
    this.modifier = modifier;
    this.name = name;
```

### ConstantValue
Condition `expressionList != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConditionalExpression.java`
#### Snippet
```java
  public ConditionalExpression(List<Node> expressionList, Type type) {
    super(ExpressionType.Conditional, type);
    assert expressionList != null : "expressionList should not be null";
    this.expressionList = expressionList;
  }
```

### ConstantValue
Condition `parameter != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/DeclarationStatement.java`
#### Snippet
```java
      @Nullable Expression initializer) {
    super(ExpressionType.Declaration, Void.TYPE);
    assert parameter != null : "parameter should not be null";
    this.modifiers = modifiers;
    this.parameter = parameter;
```

### ConstantValue
Condition `statements != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockStatement.java`
#### Snippet
```java
  BlockStatement(List<Statement> statements, Type type) {
    super(ExpressionType.Block, type);
    assert statements != null : "statements should not be null";
    this.statements = statements;
    assert distinctVariables(true);
```

### ConstantValue
Condition `fieldName != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ArrayLengthRecordField.java`
#### Snippet
```java

  public ArrayLengthRecordField(String fieldName, Class clazz) {
    assert fieldName != null : "fieldName should not be null";
    assert clazz != null : "clazz should not be null";
    this.fieldName = fieldName;
```

### ConstantValue
Condition `clazz != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ArrayLengthRecordField.java`
#### Snippet
```java
  public ArrayLengthRecordField(String fieldName, Class clazz) {
    assert fieldName != null : "fieldName should not be null";
    assert clazz != null : "clazz should not be null";
    this.fieldName = fieldName;
    this.clazz = clazz;
```

### ConstantValue
Condition `expressions != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodCallExpression.java`
#### Snippet
```java
      @Nullable Expression targetExpression, List<Expression> expressions) {
    super(ExpressionType.Call, returnType);
    assert expressions != null : "expressions should not be null";
    assert method != null : "method should not be null";
    assert (targetExpression == null) == Modifier.isStatic(
```

### ConstantValue
Condition `method != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MethodCallExpression.java`
#### Snippet
```java
    super(ExpressionType.Call, returnType);
    assert expressions != null : "expressions should not be null";
    assert method != null : "method should not be null";
    assert (targetExpression == null) == Modifier.isStatic(
        method.getModifiers());
```

### ConstantValue
Condition `name != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ClassDeclaration.java`
#### Snippet
```java
  public ClassDeclaration(int modifier, String name, @Nullable Type extended,
      List<Type> implemented, List<MemberDeclaration> memberDeclarations) {
    assert name != null : "name should not be null";
    this.modifier = modifier;
    this.name = name;
```

### ConstantValue
Condition `!classClass.equals(that.classClass)` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ClassDeclaration.java`
#### Snippet
```java
      return false;
    }
    if (!classClass.equals(that.classClass)) {
      return false;
    }
```

### ConstantValue
Condition `implemented != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ClassDeclaration.java`
#### Snippet
```java
      return false;
    }
    if (implemented != null ? !implemented.equals(that.implemented) : that
        .implemented != null) {
      return false;
```

### ConstantValue
Condition `memberDeclarations != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ClassDeclaration.java`
#### Snippet
```java
      return false;
    }
    if (memberDeclarations != null ? !memberDeclarations.equals(that
        .memberDeclarations) : that.memberDeclarations != null) {
      return false;
```

### ConstantValue
Condition `type != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TypeBinaryExpression.java`
#### Snippet
```java
      return false;
    }
    if (type != null ? !type.equals(that.type) : that.type != null) {
      return false;
    }
```

### ConstantValue
Condition `expression != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/TypeBinaryExpression.java`
#### Snippet
```java
      Type type) {
    super(nodeType, Boolean.TYPE);
    assert expression != null : "expression should not be null";
    this.expression = expression;
    this.type = type;
```

### ConstantValue
Condition `expression != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/UnaryExpression.java`
#### Snippet
```java
  UnaryExpression(ExpressionType nodeType, Type type, Expression expression) {
    super(nodeType, type);
    assert expression != null : "expression should not be null";
    this.expression = expression;
  }
```

### ConstantValue
Condition `kind != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/GotoStatement.java`
#### Snippet
```java
    super(ExpressionType.Goto,
        expression == null ? Void.TYPE : expression.getType());
    assert kind != null : "kind should not be null";
    this.kind = kind;
    this.labelTarget = labelTarget;
```

### ConstantValue
Condition `field != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/MemberExpression.java`
#### Snippet
```java
  public MemberExpression(@Nullable Expression expression, PseudoField field) {
    super(ExpressionType.MemberAccess, field.getType());
    assert field != null : "field should not be null";
    assert expression != null || Modifier.isStatic(field.getModifiers())
        : "must specify expression if field is not static";
```

### ConstantValue
Condition `parameter != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FieldDeclaration.java`
#### Snippet
```java
  public FieldDeclaration(int modifier, ParameterExpression parameter,
      @Nullable Expression initializer) {
    assert parameter != null : "parameter should not be null";
    this.modifier = modifier;
    this.parameter = parameter;
```

### ConstantValue
Condition `expression0 != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BinaryExpression.java`
#### Snippet
```java
      Expression expression1) {
    super(nodeType, type);
    assert expression0 != null : "expression0 should not be null";
    assert expression1 != null : "expression1 should not be null";
    this.expression0 = expression0;
```

### ConstantValue
Condition `expression1 != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BinaryExpression.java`
#### Snippet
```java
    super(nodeType, type);
    assert expression0 != null : "expression0 should not be null";
    assert expression1 != null : "expression1 should not be null";
    this.expression0 = expression0;
    this.expression1 = expression1;
```

### ConstantValue
Condition `value == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstantExpression.java`
#### Snippet
```java
  private static ExpressionWriter write(ExpressionWriter writer,
      final Object value, @Nullable Type type) {
    if (value == null) {
      return writer.append("null");
    }
```

### ConstantValue
Condition `rawType != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java
      this.typeArguments = typeArguments;
      this.ownerType = ownerType;
      assert rawType != null;
      for (Type typeArgument : typeArguments) {
        assert typeArgument != null;
```

### ConstantValue
Condition is always false
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java

  public static void discard(Object o) {
    if (false) {
      discard(o);
    }
```

### ConstantValue
Condition `type != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java
      List<ParameterExpression> parameterList) {
    super(ExpressionType.Lambda, type);
    assert type != null : "type should not be null";
    assert function != null || body != null
        : "both function and body should not be null";
```

### ConstantValue
Condition `parameterList != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java
    assert function != null || body != null
        : "both function and body should not be null";
    assert parameterList != null : "parameterList should not be null";
    this.function = function;
    this.body = body;
```

### ConstantValue
Condition `body != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/CatchBlock.java`
#### Snippet
```java
    CatchBlock that = (CatchBlock) o;

    if (body != null ? !body.equals(that.body) : that.body != null) {
      return false;
    }
```

### ConstantValue
Condition `parameter != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/CatchBlock.java`
#### Snippet
```java
      return false;
    }
    if (parameter != null ? !parameter.equals(that.parameter) : that
        .parameter != null) {
      return false;
```

### ConstantValue
Condition `declarations != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ForStatement.java`
#### Snippet
```java
      @Nullable Expression condition, @Nullable Expression post, Statement body) {
    super(ExpressionType.For, Void.TYPE);
    assert declarations != null;
    assert body != null;
    this.declarations = declarations; // may be empty, not null
```

### ConstantValue
Condition `body != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ForStatement.java`
#### Snippet
```java
    super(ExpressionType.For, Void.TYPE);
    assert declarations != null;
    assert body != null;
    this.declarations = declarations; // may be empty, not null
    this.condition = condition; // may be null
```

### ConstantValue
Condition `parameters != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstructorDeclaration.java`
#### Snippet
```java
  public ConstructorDeclaration(int modifier, Type declaredAgainst,
      List<ParameterExpression> parameters, BlockStatement body) {
    assert parameters != null : "parameters should not be null";
    assert body != null : "body should not be null";
    assert declaredAgainst != null : "declaredAgainst should not be null";
```

### ConstantValue
Condition `body != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstructorDeclaration.java`
#### Snippet
```java
      List<ParameterExpression> parameters, BlockStatement body) {
    assert parameters != null : "parameters should not be null";
    assert body != null : "body should not be null";
    assert declaredAgainst != null : "declaredAgainst should not be null";
    this.modifier = modifier;
```

### ConstantValue
Condition `declaredAgainst != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstructorDeclaration.java`
#### Snippet
```java
    assert parameters != null : "parameters should not be null";
    assert body != null : "body should not be null";
    assert declaredAgainst != null : "declaredAgainst should not be null";
    this.modifier = modifier;
    this.resultType = declaredAgainst;
```

### ConstantValue
Condition `expressionList != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConditionalStatement.java`
#### Snippet
```java
  public ConditionalStatement(List<Node> expressionList) {
    super(ExpressionType.Conditional, Void.TYPE);
    assert expressionList != null : "expressionList should not be null";
    this.expressionList = expressionList;
  }
```

### ConstantValue
Condition `field != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ReflectedPseudoField.java`
#### Snippet
```java

  public ReflectedPseudoField(Field field) {
    assert field != null : "field should not be null";
    this.field = field;
  }
```

### ConstantValue
Condition `defaultValue != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/LabelStatement.java`
#### Snippet
```java
    LabelStatement that = (LabelStatement) o;

    if (defaultValue != null ? !defaultValue.equals(that.defaultValue) : that
        .defaultValue != null) {
      return false;
```

### ConstantValue
Condition `expression != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ThrowStatement.java`
#### Snippet
```java
    ThrowStatement that = (ThrowStatement) o;

    if (expression != null ? !expression.equals(that.expression) : that
        .expression != null) {
      return false;
```

### ConstantValue
Condition `name != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ParameterExpression.java`
#### Snippet
```java
  public ParameterExpression(int modifier, Type type, String name) {
    super(ExpressionType.Parameter, type);
    assert name != null : "name should not be null";
    assert Character.isJavaIdentifierStart(name.charAt(0))
      : "parameter name should be valid java identifier: "
```

### ConstantValue
Condition `arguments != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/NewExpression.java`
#### Snippet
```java
    NewExpression that = (NewExpression) o;

    if (arguments != null ? !arguments.equals(that.arguments)
        : that.arguments != null) {
      return false;
```

### ConstantValue
Condition `indexExpressions != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/IndexExpression.java`
#### Snippet
```java
            Types.getComponentType(array.getType()),
            () -> "component type for " + array));
    assert indexExpressions != null : "indexExpressions should not be null";
    assert !indexExpressions.isEmpty() : "indexExpressions should not be empty";
    this.array = array;
```

### ConstantValue
Condition `initializer != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
  public static DeclarationStatement declare(int modifiers, String name,
      Expression initializer) {
    assert initializer != null
        : "empty initializer for variable declaration with name '" + name + "', modifiers "
        + modifiers + ". Please use declare(int, ParameterExpression, initializer) instead";
```

### ConstantValue
Condition `type != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
  public static UnaryExpression makeUnary(ExpressionType expressionType,
      Expression expression, Type type, @Nullable Method method) {
    assert type != null;
    return new UnaryExpression(expressionType, type, expression);
  }
```

### ConstantValue
Condition `expression == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockBuilder.java`
#### Snippet
```java
   */
  public @PolyNull Expression appendIfNotNull(String name, @PolyNull Expression expression) {
    if (expression == null) {
      return null;
    }
```

### ConstantValue
Condition `oldStatement instanceof DeclarationStatement` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockBuilder.java`
#### Snippet
```java
            ++optimizeCount;
            if (count != Integer.MAX_VALUE
                && oldStatement instanceof DeclarationStatement
                && isSafeForReuse((DeclarationStatement) oldStatement)
                && isSimpleExpression(
```

### ConstantValue
Condition `oldStatement instanceof DeclarationStatement` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/BlockBuilder.java`
#### Snippet
```java
          }
          if (oldStatement != OptimizeShuttle.EMPTY_STATEMENT) {
            if (oldStatement instanceof DeclarationStatement) {
              addExpressionForReuse((DeclarationStatement) oldStatement);
            }
```

### ConstantValue
Condition `o1 == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
        return 0;
      }
      if (o1 == null) {
        return -1;
      }
```

### ConstantValue
Condition `o2 == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
        return -1;
      }
      if (o2 == null) {
        return 1;
      }
```

### ConstantValue
Condition `o1 == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
        return 0;
      }
      if (o1 == null) {
        return -1;
      }
```

### ConstantValue
Condition `o2 == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
        return -1;
      }
      if (o2 == null) {
        return 1;
      }
```

### ConstantValue
Condition `o1 == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
        return 0;
      }
      if (o1 == null) {
        return 1;
      }
```

### ConstantValue
Condition `o2 == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
        return 1;
      }
      if (o2 == null) {
        return -1;
      }
```

### ConstantValue
Condition `o1 == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
        return 0;
      }
      if (o1 == null) {
        return 1;
      }
```

### ConstantValue
Condition `o2 == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
        return 1;
      }
      if (o2 == null) {
        return -1;
      }
```

### ConstantValue
Condition `t == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java

    @Override public int hashCode(Object t) {
      return t == null ? 0x789d : t.hashCode();
    }
  }
```

### ConstantValue
Condition `udfClass == JythonFunction.class` is always `false`
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelBuilder.java`
#### Snippet
```java
    Class udfClass = pigFunc.getClass();
    String key = className;
    if (udfClass == JythonFunction.class) {
      final String[] args = pigFunc.getCtorArgs();
      assert args != null && args.length == 2;
```

### ConstantValue
Condition `splunkConnection != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkQuery.java`
#### Snippet
```java
    this.latest = latest;
    this.fieldList = fieldList;
    assert splunkConnection != null;
    assert search != null;
  }
```

### ConstantValue
Condition `search != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkQuery.java`
#### Snippet
```java
    this.fieldList = fieldList;
    assert splunkConnection != null;
    assert search != null;
  }

```

### ConstantValue
Condition `fieldList != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkQuery.java`
#### Snippet
```java
  private Map<String, String> getArgs() {
    Map<String, String> args = new HashMap<>();
    if (fieldList != null) {
      String fields =
          StringUtils.encodeList(fieldList, ',').toString();
```

### ConstantValue
Condition `earliest != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkQuery.java`
#### Snippet
```java
      args.put("field_list", fields);
    }
    if (earliest != null) {
      args.put("earliest_time", earliest);
    }
```

### ConstantValue
Condition `latest != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkQuery.java`
#### Snippet
```java
      args.put("earliest_time", earliest);
    }
    if (latest != null) {
      args.put("latest_time", latest);
    }
```

### ConstantValue
Condition `fieldList == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTableScan.java`
#### Snippet
```java
                Expressions.constant(earliest),
                Expressions.constant(latest),
                fieldList == null
                    ? Expressions.constant(null)
                    : constantStringList(fieldList))).toBlock());
```

### ConstantValue
Condition `splunkTable != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTableScan.java`
#### Snippet
```java
    this.fieldList = fieldList;

    assert splunkTable != null;
    assert search != null;
  }
```

### ConstantValue
Condition `search != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTableScan.java`
#### Snippet
```java

    assert splunkTable != null;
    assert search != null;
  }

```

### ConstantValue
Condition `str == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/util/StringUtils.java`
#### Snippet
```java
  public static boolean parseBoolean(
      String str, boolean defaultVal, boolean missingVal) {
    if (str == null || str.isEmpty()) {
      return missingVal;
    }
```

### ConstantValue
Condition `strategy == ColumnStrategy.NULLABLE || strategy == ColumnStrategy.NOT_NULLABLE ...` is always `true`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
      this.strategy = Objects.requireNonNull(strategy, "strategy");
      Preconditions.checkArgument(
          strategy == ColumnStrategy.NULLABLE
              || strategy == ColumnStrategy.NOT_NULLABLE
              || expr != null);
    }

```

### ConstantValue
Condition `expr != null` is always `true`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
          strategy == ColumnStrategy.NULLABLE
              || strategy == ColumnStrategy.NOT_NULLABLE
              || expr != null);
    }

```

### ConstantValue
Condition `c.expr != null` is always `true`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
              int iColumn, InitializerContext context) {
            final ColumnDef c = columns.get(iColumn);
            if (c.expr != null) {
              // REVIEW Danny 2019-10-09: Should we support validation for DDL nodes?
              final SqlNode validated = context.validateExpression(storedRowType, c.expr);
```

### ConstantValue
Condition `predicate != null` is always `true`
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> Enumerable<TSource> where(
      final Enumerable<TSource> source, final Predicate1<TSource> predicate) {
    assert predicate != null;
    return new AbstractEnumerable<TSource>() {
      @Override public Enumerator<TSource> enumerator() {
```

### ConstantValue
Condition `comparer == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
        final Supplier<Lookup<TKey, TInner>> innerLookup =
            Suppliers.memoize(() ->
                comparer == null
                    ? inner.toLookup(innerKeySelector)
                    : inner.toLookup(innerKeySelector, comparer));
```

### ConstantValue
Condition `nonEquiPredicate == null` is always `false`
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
      final boolean anti,
      final Predicate2<TSource, TInner> nonEquiPredicate) {
    if (nonEquiPredicate == null) {
      return semiEquiJoin_(outer, inner, outerKeySelector, innerKeySelector,
          comparer,
```

### ConstantValue
Condition `tableMap == null` is always `false`
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvSchema.java`
#### Snippet
```java

  @Override protected Map<String, Table> getTableMap() {
    if (tableMap == null) {
      tableMap = createTableMap();
    }
```

### ConstantValue
Condition `trimmed != null` is always `true`
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvSchema.java`
#### Snippet
```java
  private static String trim(String s, String suffix) {
    String trimmed = trimOrNull(s, suffix);
    return trimmed != null ? trimmed : s;
  }

```

### ConstantValue
Condition `proj == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
  @SuppressWarnings("unused")
  private static RelNode addProjectionRule(LogicalProject proj, RelNode rel) {
    if (proj == null) {
      return rel;
    }
```

### ConstantValue
Condition `bottomRow == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
    }
    List<RelDataTypeField> bottomFields =
        bottomRow == null ? null : bottomRow.getFieldList();
    List<RelDataTypeField> topFields    =
        topRow    == null ? null : topRow.getFieldList();
```

### ConstantValue
Condition `topRow == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
        bottomRow == null ? null : bottomRow.getFieldList();
    List<RelDataTypeField> topFields    =
        topRow    == null ? null : topRow.getFieldList();

    if (bottomFields == null) {
```

### ConstantValue
Condition `bottomFields == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
        topRow    == null ? null : topRow.getFieldList();

    if (bottomFields == null) {
      bottomFields = splunkRel.getRowType().getFieldList();
    }
```

### ConstantValue
Condition `bottomProj != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java

    // handle bottom projection (ie choose a subset of the table fields)
    if (bottomProj != null) {
      List<RelDataTypeField> tmp  = new ArrayList<>();
      List<RelDataTypeField> dRow = bottomProj.getRowType().getFieldList();
```

### ConstantValue
Condition `topProj != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
    // handle top projection (ie reordering and renaming)
    List<RelDataTypeField> newFields = bottomFields;
    if (topProj != null) {
      LOGGER.debug("topProj: {}", topProj.getPermutation());
      newFields = new ArrayList<>();
```

### ConstantValue
Condition `tmp == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
      } else { // RexLiteral
        String tmp = toString(like, (RexLiteral) operand);
        if (tmp == null) {
          return false;
        }
```

### ConstantValue
Condition `fields == null` is always `false`
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvProjectTableScanRule.java`
#### Snippet
```java
    final CsvTableScan scan = call.rel(1);
    int[] fields = getProjectFields(project.getProjects());
    if (fields == null) {
      // Project contains expressions more complex than just field references.
      return;
```

### ConstantValue
Condition `srl != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
  @Override public void getSearchResults(String search, Map<String, String> otherArgs,
      List<String> fieldList, SearchResultListener srl) {
    assert srl != null;
    Enumerator<Object> x = getSearchResults_(search, otherArgs, fieldList, srl);
    assert x == null;
```

### ConstantValue
Condition `x == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
    assert srl != null;
    Enumerator<Object> x = getSearchResults_(search, otherArgs, fieldList, srl);
    assert x == null;
  }

```

### ConstantValue
Condition `source >= -1` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
            // Yields -1 if wanted field does not exist.
            source = headerList.indexOf(wantedFields.get(0));
            assert source >= -1;
            sources = null;
          } else if (wantedFields.equals(headerList)) {
```

### ConstantValue
Condition `otherArgs != null` is always `true`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
    StringBuilder data = new StringBuilder();
    Map<String, String> args = new LinkedHashMap<>();
    if (otherArgs != null) {
      args.putAll(otherArgs);
    }
```

### ConstantValue
Condition `srl == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
      InputStream in =
          post(searchUrl, data, requestHeaders, 10000, 1800000);
      if (srl == null) {
        return new SplunkResultEnumerator(in, wantedFields);
      } else {
```

### ConstantValue
Condition `srl == null` is always `false`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
      e.printStackTrace(new PrintWriter(sw));
      LOGGER.warn("{}\n{}", e.getMessage(), sw);
      return srl == null ? Linq4j.emptyEnumerator() : null;
    }
  }
```

### ConstantValue
Condition `csvTable != null` is always `true`
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTableScan.java`
#### Snippet
```java
    this.fields = fields;

    assert csvTable != null;
  }

```

### ConstantValue
Condition is always false
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java
            fields.get(fieldCollation.getFieldIndex()).getName();
        keys.add(name + ": " + direction(fieldCollation));
        if (false) {
          // TODO: NULLS FIRST and NULLS LAST
          switch (fieldCollation.nullDirection) {
```

### ConstantValue
Condition `projectRowType != null` is always `true`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTableScan.java`
#### Snippet
```java

  @Override public RelDataType deriveRowType() {
    return projectRowType != null ? projectRowType : super.deriveRowType();
  }

```

### ConstantValue
Condition `projectRowType == null` is always `false`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTableScan.java`
#### Snippet
```java
      RelMetadataQuery mq) {
    // scans with a small project list are cheaper
    final float f = projectRowType == null ? 1f
        : (float) projectRowType.getFieldCount() / 100f;
    return super.computeSelfCost(planner, mq).multiplyBy(.1 * f);
```

### ConstantValue
Condition `mongoTable != null` is always `true`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTableScan.java`
#### Snippet
```java
    this.projectRowType = projectRowType;

    assert mongoTable != null;
    assert getConvention() == MongoRel.CONVENTION;
  }
```

### ConstantValue
Condition `op == null` is always `false`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java

    private void translateOp2(String op, String name, RexLiteral right) {
      if (op == null) {
        // E.g.: {deptno: 100}
        eqMap.put(name, right);
```

### ConstantValue
Condition `itemName != null` is always `true`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
      case ITEM:
        String itemName = MongoRules.isItem((RexCall) left);
        if (itemName != null) {
          translateOp2(op, itemName, rightLiteral);
          return true;
```

### ConstantValue
Condition `credential == null` is always `false`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSchema.java`
#### Snippet
```java
    super();
    try {
      final MongoClient mongo = credential == null
          ? new MongoClient(new ServerAddress(host), options)
          : new MongoClient(new ServerAddress(host), credential, options);
```

### ConstantValue
Condition `fields == null` is always `false`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoEnumerator.java`
#### Snippet
```java
      List<Map.Entry<String, Class>> fields) {
    //noinspection unchecked
    return fields == null
        ? (Function1) mapGetter()
        : fields.size() == 1
```

### ConstantValue
Condition `o == null` is always `false`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoEnumerator.java`
#### Snippet
```java
  @SuppressWarnings("JavaUtilDate")
  private static Object convert(Object o, Class clazz) {
    if (o == null) {
      return null;
    }
```

### ConstantValue
Condition `filterJson == null` is always `false`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTable.java`
#### Snippet
```java
        mongoDb.getCollection(collectionName);
    final Bson filter =
        filterJson == null ? null : BsonDocument.parse(filterJson);
    final Bson project =
        projectJson == null ? null : BsonDocument.parse(projectJson);
```

### ConstantValue
Condition `projectJson == null` is always `false`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTable.java`
#### Snippet
```java
        filterJson == null ? null : BsonDocument.parse(filterJson);
    final Bson project =
        projectJson == null ? null : BsonDocument.parse(projectJson);
    final Function1<Document, Object> getter = MongoEnumerator.getter(fields);
    return new AbstractEnumerable<Object>() {
```

### ConstantValue
Condition `name != null` is always `true`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoRules.java`
#### Snippet
```java
    @Override public String visitCall(RexCall call) {
      String name = isItem(call);
      if (name != null) {
        return "'$" + name + "'";
      }
```

### ConstantValue
Condition `((SqlParseException) x).getPos() != null` is always `true` when reached
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlTests.java`
#### Snippet
```java
    for (Throwable x = ex; x != null; x = x.getCause()) {
      if ((x instanceof SqlParseException)
          && (((SqlParseException) x).getPos() != null)) {
        spe = (SqlParseException) x;
        break;
```

### ConstantValue
Condition `spe.getCause() != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlTests.java`
#### Snippet
```java
      actualEndLine = spe.getPos().getEndLineNum();
      actualEndColumn = spe.getPos().getEndColumnNum();
      if (spe.getCause() != null) {
        actualException = spe.getCause();
        actualMessage = actualException.getMessage();
```

### ConstantValue
Condition `transformationResult != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
    }

    if (transformationResult != null) {
      if (parent == null) {
        root = transformationResult;
```

### ConstantValue
Condition `parent == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java

    if (transformationResult != null) {
      if (parent == null) {
        root = transformationResult;
      } else {
```

### ConstantValue
Condition `rule != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java

  @Override public RelNode findBestExp() {
    if (rule != null) {
      matchRecursive(root, null, -1);
    }
```

### ConstantValue
Condition `rule == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java

  @Override public List<RelOptRule> getRules() {
    return rule == null
        ? ImmutableList.of() : ImmutableList.of(rule);
  }
```

### ConstantValue
Condition `this.rule == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java

  @Override public boolean addRule(RelOptRule rule) {
    assert this.rule == null
        : "MockRelOptPlanner only supports a single rule";
    this.rule = rule;
```

### ConstantValue
Condition `compiledDiffMatcher != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java

  private String applyDiffMask(String s) {
    if (compiledDiffMatcher != null) {
      compiledDiffMatcher.reset(s);

```

### ConstantValue
Condition `compiledIgnoreMatcher != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java

  private boolean matchIgnorePatterns(String s) {
    if (compiledIgnoreMatcher != null) {
      compiledIgnoreMatcher.reset(s);
      return compiledIgnoreMatcher.matches();
```

### ConstantValue
Condition `logOutputStream != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
   */
  protected void diffTestLog() throws IOException {
    assert logOutputStream != null;
    logOutputStream.close();
    logOutputStream = null;
```

### ConstantValue
Condition `logOutputStream != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
  @AfterEach
  protected void tearDown() throws IOException {
    if (logOutputStream != null) {
      logOutputStream.close();
      logOutputStream = null;
```

### ConstantValue
Condition `logOutputStream == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
  protected OutputStream openTestLogOutputStream(File testFileSansExt)
      throws IOException {
    assert logOutputStream == null;

    logFile = new File(testFileSansExt.toString() + ".log");
```

### ConstantValue
Condition `actual == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java
    private static boolean isWithin(Object actual, Number expected,
        double epsilon) {
      if (actual == null) {
        return expected == null;
      }
```

### ConstantValue
Condition `expected != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/Matchers.java`
#### Snippet
```java
        }
        Throwable error = (Throwable) item;
        return expected != null
            && Objects.equals(error.getClass(), expected.getClass())
            && Objects.equals(error.getMessage(), expected.getMessage());
```

### ConstantValue
Condition `text == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
   */
  public String expand(String tag, String text) {
    if (text == null) {
      return null;
    } else if (text.startsWith("${")
```

### ConstantValue
Condition `tag == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      final String testCaseName = getCurrentTestCaseName(true);
      final String token = text.substring(2, text.length() - 1);
      if (tag == null) {
        tag = token;
      }
```

### ConstantValue
Condition `expanded == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
          + "' does not match tag '" + tag + "'";
      String expanded = get(testCaseName, token);
      if (expanded == null) {
        // Token is not specified. Return the original text: this will
        // cause a diff, and the actual value will be written to the
```

### ConstantValue
Condition `filter != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
        return text;
      }
      if (filter != null) {
        expanded =
            filter.filter(this, testCaseName, tag, text, expanded);
```

### ConstantValue
Condition `baseRepository == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      // resource file.
      final String testCaseName = getCurrentTestCaseName(true);
      if (baseRepository == null || baseRepository.get(testCaseName, tag) == null) {
        set(tag, text);
      }
```

### ConstantValue
Condition `baseRepository == null || baseRepository.get(testCaseName, tag) == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      // resource file.
      final String testCaseName = getCurrentTestCaseName(true);
      if (baseRepository == null || baseRepository.get(testCaseName, tag) == null) {
        set(tag, text);
      }
```

### ConstantValue
Condition `baseRepository.get(testCaseName, tag) == null` is always `false` when reached
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      // resource file.
      final String testCaseName = getCurrentTestCaseName(true);
      if (baseRepository == null || baseRepository.get(testCaseName, tag) == null) {
        set(tag, text);
      }
```

### ConstantValue
Condition `expected2 == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
    final String testCaseName = getCurrentTestCaseName(true);
    String expected2 = expand(tag, expected);
    if (expected2 == null) {
      update(testCaseName, expected, actual);
      throw new AssertionError("reference file does not contain resource '"
```

### ConstantValue
Condition `resourceName != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
   */
  public synchronized void set(String resourceName, String value) {
    assert resourceName != null;
    final String testCaseName = getCurrentTestCaseName(true);
    update(testCaseName, resourceName, value);
```

### ConstantValue
Condition `testCaseElement == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
    final List<Pair<String, Element>> map = new ArrayList<>();
    Element testCaseElement = getTestCaseElement(testCaseName, true, map);
    if (testCaseElement == null) {
      testCaseElement = doc.createElement(TEST_CASE_TAG);
      testCaseElement.setAttribute(TEST_CASE_NAME_ATTR, testCaseName);
```

### ConstantValue
Condition `resourceElement == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
    Element resourceElement =
        getResourceElement(testCaseElement, resourceName, true);
    if (resourceElement == null) {
      resourceElement = doc.createElement(RESOURCE_TAG);
      resourceElement.setAttribute(RESOURCE_NAME_ATTR, resourceName);
```

### ConstantValue
Condition `baseRepository != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
        if (testCaseName.equals(name)) {
          if (checkOverride
              && (baseRepository != null)
              && (baseRepository.getTestCaseElement(testCaseName, false, null) != null)
              && !"true".equals(
```

### ConstantValue
Condition `baseRepository.getTestCaseElement(testCaseName, false, null) != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
          if (checkOverride
              && (baseRepository != null)
              && (baseRepository.getTestCaseElement(testCaseName, false, null) != null)
              && !"true".equals(
                  testCase.getAttribute(TEST_CASE_OVERRIDES_ATTR))) {
```

### ConstantValue
Condition `elements != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
          return testCase;
        }
        if (elements != null) {
          elements.add(Pair.of(name, testCase));
        }
```

### ConstantValue
Condition `testCaseElement == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      String resourceName) {
    Element testCaseElement = getTestCaseElement(testCaseName, true, null);
    if (testCaseElement == null) {
      if (baseRepository != null) {
        return baseRepository.get(testCaseName, resourceName);
```

### ConstantValue
Condition `resourceElement != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
    final Element resourceElement =
        getResourceElement(testCaseElement, resourceName);
    if (resourceElement != null) {
      return getText(resourceElement);
    }
```

### ConstantValue
Condition `method != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
  public void test(String path) throws Exception {
    final Method method = findMethod(path);
    if (method != null) {
      try {
        method.invoke(this, path);
```

### ConstantValue
Condition `s == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/schemata/foodmart/FoodmartSchema.java`
#### Snippet
```java

  private static String q(String s) {
    return s == null ? "null" : "'" + s + "'";
  }

```

### ConstantValue
Condition `s == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/TestUtil.java`
#### Snippet
```java
   * {@code -12.37999999991} becomes {@code -12.38}. */
  public static String correctRoundedFloat(String s) {
    if (s == null) {
      return s;
    }
```

### ConstantValue
Condition `resolver == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
      }
      if (clazz.isAssignableFrom(Table.class)) {
        final Table table = resolver == null
            ? new ModifiableTable(Util.last(names))
                : new ModifiableTableWithCustomColumnResolving(Util.last(names));
```

### ConstantValue
Condition `resolver == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
    @Override public <T> T unwrap(Class<T> clazz) {
      if (clazz.isAssignableFrom(ModifiableView.class)) {
        ModifiableView view = resolver == null
            ? new ModifiableView()
                : new ModifiableViewWithCustomColumnResolving();
```

### ConstantValue
Condition `updateChecker == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      try {
        consumer.accept(statement);
        if (updateChecker == null) {
          resultSet = statement.executeQuery();
        } else {
```

### ConstantValue
Condition `exceptionChecker != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
          updateCount = statement.executeUpdate(sql);
        }
        if (exceptionChecker != null) {
          exceptionChecker.accept(null);
          return;
```

### ConstantValue
Condition `exceptionChecker != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        }
      } catch (Exception | Error e) {
        if (exceptionChecker != null) {
          exceptionChecker.accept(e);
          return;
```

### ConstantValue
Condition `s != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
              new ResultSetFormatter() {
                @Override protected String adjustValue(String s) {
                  if (s != null) {
                    if (s.contains(".")) {
                      while (s.endsWith("0")) {
```

### ConstantValue
Condition `expected != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

        assertNotNull(exception, "Expected to fail at validation, but did not");
        if (expected != null) {
          String stack = TestUtil.printStackTrace(exception);
          assertThat(stack, containsString(expected));
```

### ConstantValue
Condition `expected == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        final String resultString = resultSet.getString(1);
        assertThat(resultString,
            expected == null ? nullValue(String.class) : isLinux(expected));
      } catch (SQLException e) {
        throw TestUtil.rethrow(e);
```

### ConstantValue
Condition `convertChecker != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      final Consumer<RelNode> substitutionChecker) {
    try (Closer closer = new Closer()) {
      if (convertChecker != null) {
        closer.add(
            Hook.TRIMMED.addThread(convertChecker));
```

### ConstantValue
Condition `substitutionChecker != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
            Hook.TRIMMED.addThread(convertChecker));
      }
      if (substitutionChecker != null) {
        closer.add(
            Hook.SUB.addThread(substitutionChecker));
```

### ConstantValue
Condition `expected.sql != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        JavaSql expected) {
      ensurePlan(checkUpdate);
      if (expected.sql != null) {
        final List<String> planSqls = JavaSql.fromJava(plan).extractSql();
        final String planSql;
```

### ConstantValue
Condition `counter != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      final AtomicInteger counter) {
    return relNode -> {
      if (counter != null) {
        counter.incrementAndGet();
      }
```

### ConstantValue
Condition `updateChecker == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      Integer updateCount = null;
      try {
        if (updateChecker == null) {
          resultSet = statement.executeQuery(sql);
        } else {
```

### ConstantValue
Condition `exceptionChecker != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
          updateCount = statement.executeUpdate(sql);
        }
        if (exceptionChecker != null) {
          exceptionChecker.accept(null);
          return;
```

### ConstantValue
Condition `exceptionChecker != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        }
      } catch (Exception | Error e) {
        if (exceptionChecker != null) {
          exceptionChecker.accept(e);
          return;
```

### ConstantValue
Condition `objects == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

  private static String str(Object[] objects) {
    return objects == null
          ? null
          : Arrays.stream(objects).map(Object::toString)
```

### ConstantValue
Condition `string != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

    protected String adjustValue(String string) {
      if (string != null) {
        string = TestUtil.correctRoundedFloat(string);
      }
```

### ConstantValue
Condition `plan != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

    private void ensurePlan(Consumer<Integer> checkUpdate) {
      if (plan != null) {
        return;
      }
```

### ConstantValue
Condition `fields != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
          if (iterator.hasNext()) {
            Object[] full = iterator.next();
            current = fields != null ? convertRow(full) : full;
            return true;
          } else {
```

### ConstantValue
Condition `v == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    }
    public static long timeFun(java.sql.Time v) {
      return v == null ? -1L : SqlFunctions.toLong(v);
    }

```

### ConstantValue
Condition `var == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
     * {@code BigDecimal} arguments will go wrong. */
    public static double toDouble(BigDecimal var) {
      return var == null ? 0.0d : var.doubleValue();
    }
    public static double toDouble(Double var) {
```

### ConstantValue
Condition `v == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    }
    public static long timestampFun(java.sql.Timestamp v) {
      return v == null ? -1L : SqlFunctions.toLong(v);
    }
    public static long timeFun(java.sql.Time v) {
```

### ConstantValue
Condition `time == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    public static long toLong(Time time) {
      return time == null ? 0 : SqlFunctions.toLong(time);
    }

```

### ConstantValue
Condition `date == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    /** Overloaded functions with DATE, TIMESTAMP and TIME arguments. */
    public static long toLong(Date date) {
      return date == null ? 0 : SqlFunctions.toLong(date);
    }

```

### ConstantValue
Condition `v == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    public static long dateFun(java.sql.Date v) {
      return v == null ? -1L : SqlFunctions.toLong(v);
    }
    public static long timestampFun(java.sql.Timestamp v) {
```

### ConstantValue
Condition `s != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    private static void abc(StringBuilder sb, Object s) {
      if (s != null) {
        if (sb.length() > 0) {
          sb.append(", ");
```

### ConstantValue
Condition `offset == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
  public static QueryableTable multiplicationTable(final int ncol,
      final int nrow, Integer offset) {
    final int offs = offset == null ? 0 : offset;
    return new AbstractQueryableTable(Object[].class) {
      @Override public RelDataType getRowType(RelDataTypeFactory typeFactory) {
```

### ConstantValue
Condition `var == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    }
    public static double toDouble(Double var) {
      return var == null ? 0.0d : var;
    }
    public static double toDouble(Float var) {
```

### ConstantValue
Condition `v == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    }
    public static java.sql.Time toTimeFun(Long v) {
      return v == null ? null : SqlFunctions.internalToTime(v.intValue());
    }

```

### ConstantValue
Condition `v == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    public static java.sql.Date toDateFun(Long v) {
      return v == null ? null : SqlFunctions.internalToDate(v.intValue());
    }
    public static java.sql.Timestamp toTimestampFun(Long v) {
```

### ConstantValue
Condition `s == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
  public static class Null4Function {
    @SemiStrict public static String eval(@Parameter(name = "s") String s) {
      if (s == null || s.length() == 4) {
        return null;
      }
```

### ConstantValue
Condition `x == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    public Integer eval(@Parameter(name = "x") Integer x,
        @Parameter(name = "y") Integer y) {
      if (x == null || y == null) {
        return null;
      }
```

### ConstantValue
Condition `x == null || y == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    public Integer eval(@Parameter(name = "x") Integer x,
        @Parameter(name = "y") Integer y) {
      if (x == null || y == null) {
        return null;
      }
```

### ConstantValue
Condition `y == null` is always `false` when reached
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    public Integer eval(@Parameter(name = "x") Integer x,
        @Parameter(name = "y") Integer y) {
      if (x == null || y == null) {
        return null;
      }
```

### ConstantValue
Condition `s != null` is always `true`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    private static void abc(StringBuilder sb, Object s) {
      if (s != null) {
        if (sb.length() > 0) {
          sb.append(", ");
```

### ConstantValue
Condition `timestamp == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    public static long toLong(Timestamp timestamp) {
      return timestamp == null ? 0 : SqlFunctions.toLong(timestamp);
    }

```

### ConstantValue
Condition `var == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    }
    public static double toDouble(Float var) {
      return var == null ? 0.0d : Double.valueOf(var.toString());
    }

```

### ConstantValue
Condition `s == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    // Argument is null in case SQL contains function call with expression.
    // Then the engine calls a function with null arguments to get getRowType.
    if (s == null) {
      items = ImmutableList.of();
    } else {
```

### ConstantValue
Condition `v == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    public static List arrayAppendFun(List v, Integer i) {
      if (v == null || i == null) {
        return null;
      } else {
```

### ConstantValue
Condition `v == null || i == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    public static List arrayAppendFun(List v, Integer i) {
      if (v == null || i == null) {
        return null;
      } else {
```

### ConstantValue
Condition `i == null` is always `false` when reached
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    public static List arrayAppendFun(List v, Integer i) {
      if (v == null || i == null) {
        return null;
      } else {
```

### ConstantValue
Condition `o == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
  public static class MyToStringFunction {
    public static String eval(@Parameter(name = "o") Object o) {
      if (o == null) {
        return "<null>";
      }
```

### ConstantValue
Condition `o == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchEnumerators.java`
#### Snippet
```java
  @SuppressWarnings("JavaUtilDate")
  private static Object convert(Object o, Class clazz) {
    if (o == null) {
      return null;
    }
```

### ConstantValue
Condition `index == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSchema.java`
#### Snippet
```java
    this.fetchSize = fetchSize;

    if (index == null) {
      try {
        this.tableMap = createTables(indicesFromElastic());
```

### ConstantValue
Condition `pathPrefix != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSchemaFactory.java`
#### Snippet
```java
          }

          if (pathPrefix != null && !pathPrefix.isEmpty()) {
            builder.setPathPrefix(pathPrefix);
          }
```

### ConstantValue
Condition `offset != null` is always `true` when reached
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java
      Long offset, Long fetch) throws IOException {

    if (!groupBy.isEmpty() && offset != null) {
      String message = "Currently ES doesn't support generic pagination "
          + "with aggregations. You can still use LIMIT keyword (without OFFSET). "
```

### ConstantValue
Condition `fetch != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java
      });

      if (fetch != null) {
        terms.put("size", fetch);
      }
```

### ConstantValue
Condition `res.aggregations() != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java

    final List<Map<String, Object>> result = new ArrayList<>();
    if (res.aggregations() != null) {
      // collect values
      ElasticsearchJson.visitValueNodes(res.aggregations(), m -> {
```

### ConstantValue
Condition `offset != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java
    }

    if (offset != null) {
      query.put("from", offset);
    }
```

### ConstantValue
Condition `fetch != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java
    }

    if (fetch != null) {
      query.put("size", fetch);
    }
```

### ConstantValue
Condition `offset == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java

    Iterable<ElasticsearchJson.SearchHit> iter;
    if (offset == null) {
      // apply scrolling when there is no offsets
      iter = () -> new Scrolling(transport).query(query);
```

### ConstantValue
Condition `lt == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java

    @Override void writeJson(final JsonGenerator generator) throws IOException {
      if (lt == null && gt == null) {
        throw new IllegalStateException("Either lower or upper bound should be provided");
      }
```

### ConstantValue
Condition `lt == null && gt == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java

    @Override void writeJson(final JsonGenerator generator) throws IOException {
      if (lt == null && gt == null) {
        throw new IllegalStateException("Either lower or upper bound should be provided");
      }
```

### ConstantValue
Condition `scrollId != null` is always `true` when reached
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/Scrolling.java`
#### Snippet
```java

    @Override public void close() {
      if (!closed && scrollId != null) {
        // close once (if scrollId is present)
        closer.accept(scrollId);
```

### ConstantValue
Condition `gt != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
      generator.writeStartObject();

      if (gt != null) {
        final String op = gte ? "gte" : "gt";
        generator.writeFieldName(op);
```

### ConstantValue
Condition `lt != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
      }

      if (lt != null) {
        final String op = lte ? "lte" : "lt";
        generator.writeFieldName(op);
```

### ConstantValue
Condition `format != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/QueryBuilders.java`
#### Snippet
```java
      }

      if (format != null) {
        generator.writeStringField("format", format);
      }
```

### ConstantValue
Condition `source == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSearchResult.java`
#### Snippet
```java

      // both can't be null
      if (source == null && fields == null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `source == null && fields == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSearchResult.java`
#### Snippet
```java

      // both can't be null
      if (source == null && fields == null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `source != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSearchResult.java`
#### Snippet
```java

      // both can't be non-null
      if (source != null && fields != null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `source != null && fields != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSearchResult.java`
#### Snippet
```java

      // both can't be non-null
      if (source != null && fields != null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `fields != null` is always `true` when reached
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSearchResult.java`
#### Snippet
```java

      // both can't be non-null
      if (source != null && fields != null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `source != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSearchResult.java`
#### Snippet
```java
      }

      if (source != null) {
        return source.get(name);
      } else if (fields != null) {
```

### ConstantValue
Condition `source != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSearchResult.java`
#### Snippet
```java

    public Map<String, Object> sourceOrFields() {
      return source != null ? source : fields;
    }
  }
```

### ConstantValue
Condition `projectRowType == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTableScan.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    final float f = projectRowType == null ? 1f : (float) projectRowType.getFieldCount() / 100f;
    return super.computeSelfCost(planner, mq).multiplyBy(.1 * f);
  }
```

### ConstantValue
Condition `projectRowType != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTableScan.java`
#### Snippet
```java

  @Override public RelDataType deriveRowType() {
    return projectRowType != null ? projectRowType : super.deriveRowType();
  }

```

### ConstantValue
Condition `name != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
    @Override public String visitCall(RexCall call) {
      final String name = isItemCall(call);
      if (name != null) {
        return name;
      }
```

### ConstantValue
Condition `isItemCall(uncast(call)) != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
    final Boolean result = node.accept(new RexVisitorImpl<Boolean>(false) {
      @Override public Boolean visitCall(final RexCall call) {
        return isItemCall(uncast(call)) != null;
      }
    });
```

### ConstantValue
Condition `schemaField == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    private NamedFieldExpression(RexInputRef schemaField) {
      this.name = schemaField == null ? null : schemaField.getName();
    }

```

### ConstantValue
Condition `builder == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    @Override public QueryBuilder builder() {
      if (builder == null) {
        throw new IllegalStateException("Builder was not initialized");
      }
```

### ConstantValue
Condition `literal == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    private NamedFieldExpression(RexLiteral literal) {
      this.name = literal == null ? null : RexLiteral.stringValue(literal);
    }

```

### ConstantValue
Condition `literal != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      LiteralExpression literal = expressAsLiteral(left);
      boolean swapped = false;
      if (literal != null) {
        swapped = true;
        terminal = (TerminalExpression) right;
```

### ConstantValue
Condition `literal == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      }

      if (literal == null || terminal == null) {
        String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `literal == null || terminal == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      }

      if (literal == null || terminal == null) {
        String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `terminal == null` is always `false` when reached
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      }

      if (literal == null || terminal == null) {
        String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Value `swapped` is always 'true'
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      }

      return new SwapResult(swapped, terminal, literal);
    }

```

### ConstantValue
Condition `source == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java

      // both can't be null
      if (source == null && fields == null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `source == null && fields == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java

      // both can't be null
      if (source == null && fields == null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `source != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java

      // both can't be non-null
      if (source != null && fields != null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `source != null && fields != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java

      // both can't be non-null
      if (source != null && fields != null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `fields != null` is always `true` when reached
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java

      // both can't be non-null
      if (source != null && fields != null) {
        final String message =
            String.format(Locale.ROOT,
```

### ConstantValue
Condition `source != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java

    Map<String, Object> sourceOrFields() {
      return source != null ? source : fields;
    }
  }
```

### ConstantValue
Condition `aggregationsAsMap == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
     */
    final Map<String, Aggregation> asMap() {
      if (aggregationsAsMap == null) {
        Map<String, Aggregation> map = new LinkedHashMap<>(aggregations.size());
        for (Aggregation aggregation : aggregations) {
```

### ConstantValue
Condition `map == null` is always `false`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
     */
    private static Object valueFromPath(Map<String, Object> map, String path) {
      if (map == null) {
        return null;
      }
```

### ConstantValue
Condition `fields != null` is always `true`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
      }

      if (fields != null && fields.containsKey(name)) {
        Object field = fields.get(name);
        if (field instanceof Iterable) {
```

### ConstantValue
Condition `value == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

    private SqlNode literal(RelDataType type, Object value) {
      if (value == null) {
        return SqlStdOperatorTable.CAST.createCall(
            SqlParserPos.ZERO,
```

### ConstantValue
Condition is always false
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  @Test void testJsonValue() {
    final SqlOperatorFixture f = fixture();
    if (false) {
      f.checkFails("json_value('{\"foo\":100}', 'lax $.foo1' error on empty)",
          "(?s).*Empty result of JSON_VALUE function is not allowed.*",
```

### ConstantValue
Condition `ex instanceof CalciteContextException` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
  private Throwable findMostDescriptiveCause(Throwable ex) {
    if (ex instanceof CalciteException
        || ex instanceof CalciteContextException
        || ex instanceof SqlParseException) {
      return ex;
```

### ConstantValue
Condition `arg.value == null` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
          int nullCount = 0;
          for (ValueType arg : args) {
            if (arg.value == null) {
              ++nullCount;
            }
```

### ConstantValue
Condition `nullCount > 0` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
          final Strong.Policy policy = Strong.policy(op);
          try {
            if (nullCount > 0 && policy == Strong.Policy.ANY) {
              f.checkNull(s);
            } else {
```

### ConstantValue
Condition `nullCount > 0 && policy == Strong.Policy.ANY` is always `false`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
          final Strong.Policy policy = Strong.policy(op);
          try {
            if (nullCount > 0 && policy == Strong.Policy.ANY) {
              f.checkNull(s);
            } else {
```

### ConstantValue
Condition is always false
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      // whether the java or fennel calc are used.
      // Try to make them the same
      if (false /* fennel calc*/) { // Treated as FLOAT or DOUBLE
        f.checkCastToString(numeric.maxNumericString, type,
            // Treated as DOUBLE
```

### ConstantValue
Condition is always false
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        f.checkCastToString(numeric.minNumericString, type,
            isFloat ? "1.401299E-45" : "4.940656458412465E-324", castType);
      } else if (false /* JavaCalc */) {
        // Treated as FLOAT or DOUBLE
        f.checkCastToString(numeric.maxNumericString, type,
```

### ConstantValue
Condition is always false
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.checkScalar("{fn LCASE('foo' || 'bar')}",
        "foobar", "CHAR(6) NOT NULL");
    if (false) {
      f.checkScalar("{fn LENGTH(string)}", null, "");
    }
```

### ConstantValue
Condition is always false
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

    // IS NOT DISTINCT FROM not implemented for ROW yet
    if (false) {
      f.checkBoolean("row(1,1) is not distinct from row(1,1)", false);
      f.checkBoolean("row(1,1) is not distinct from row(1,2)", true);
```

### ConstantValue
Condition is always false
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

    // IS DISTINCT FROM not implemented for ROW yet
    if (false) {
      f.checkBoolean("row(1,1) is distinct from row(1,1)", true);
      f.checkBoolean("row(1,1) is distinct from row(1,2)", false);
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeTable.java`
#### Snippet
```java
    StringBuilder queryBuilder = new StringBuilder("SELECT ");
    queryBuilder.append(oqlSelectStatement);
    queryBuilder.append(" FROM /" + regionName);
    queryBuilder.append(whereClause);

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeTable.java`
#### Snippet
```java
    }
    if (limit != null) {
      queryBuilder.append(" LIMIT " + limit);
    }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchProject.java`
#### Snippet
```java
                + field + "\"}");
      }
      query.append("\"script_fields\": {" + String.join(", ", scriptFields) + "}");
    }

```

## RuleId[id=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Collection `bestKeyOrder` is always empty
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
      truncatedLeftKeys = new ArrayList<>();
      truncatedRightKeys = new ArrayList<>();
      for (int key : bestKeyOrder) {
        truncatedLeftKeys.add(leftKeys.get(key));
        truncatedRightKeys.add(rightKeys.get(key));
```

### RedundantOperationOnEmptyContainer
Collection `rule` is always empty
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeAssignmentRule.java`
#### Snippet
```java

    // SMALLINT is assignable from...
    rule.clear();
    rule.add(SqlTypeName.TINYINT);
    rule.add(SqlTypeName.SMALLINT);
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `core/src/main/java/org/apache/calcite/adapter/enumerable/ReflectiveCallNotNullImplementor.java`
#### Snippet
```java
  private static boolean containsCheckedException(Method method) {
    Class[] exceptions = method.getExceptionTypes();
    if (exceptions == null || exceptions.length == 0) {
      return false;
    }
```

## RuleId[id=UNCHECKED_WARNING]
### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.apache.calcite.rel.RelDistribution', required 'T'
in `core/src/main/java/org/apache/calcite/rel/RelDistribution.java`
#### Snippet
```java
   * @return distribution with mapping applied
   */
  @Override RelDistribution apply(Mappings.TargetMapping mapping);

  /** Type of distribution. */
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.apache.calcite.rel.RelCollationImpl', required 'T'
in `core/src/main/java/org/apache/calcite/rel/RelCollationImpl.java`
#### Snippet
```java
   * @return Collation with applied mapping.
   */
  @Override public RelCollationImpl apply(
      final Mappings.TargetMapping mapping) {
    return (RelCollationImpl) RexUtil.apply(mapping, this);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'MH'
in `core/src/main/java/org/apache/calcite/rel/metadata/ProxyingMetadataHandlerProvider.java`
#### Snippet
```java
    };

    return (MH) Proxy.newProxyInstance(
        handlerClass.getClassLoader(),
        new Class[]{handlerClass},
        handler);
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'capture\>' to 'H'
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQueryBase.java`
#### Snippet
```java
  protected <M extends Metadata, H extends MetadataHandler<M>> H
      revise(Class<? extends RelNode> class_, MetadataDef<M> def) {
    return (H) revise(def.handlerClass);
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class\>'
in `core/src/main/java/org/apache/calcite/rel/metadata/ReflectiveRelMetadataProvider.java`
#### Snippet
```java
  public static RelMetadataProvider reflectiveSource(MetadataHandler target,
      Method... methods) {
    return reflectiveSource(target, ImmutableList.copyOf(methods), target.getDef().handlerClass);
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class\>'
in `core/src/main/java/org/apache/calcite/rel/metadata/ReflectiveRelMetadataProvider.java`
#### Snippet
```java
  public static RelMetadataProvider reflectiveSource(Method method,
      MetadataHandler target) {
    return reflectiveSource(target, ImmutableList.of(method), target.getDef().handlerClass);
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `core/src/main/java/org/apache/calcite/rel/metadata/janino/DispatchGenerator.java`
#### Snippet
```java
        }
      }
      return (Class<? extends RelNode>) cpt[0];
    }
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List\>'
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case ROW:
      assert value instanceof List : "value must implement List: " + value;
      final List<Comparable<?>> fieldValues = (List) value;
      final List<RelDataTypeField> fields = type.getFieldList();
      final List<RexLiteral> fieldLiterals =
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List\>'
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case MULTISET:
      assert value instanceof List : "value must implement List: " + value;
      final List<Comparable<?>> elementValues = (List) value;
      final List<RexLiteral> elementLiterals =
          FlatLists.of(
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.calcite.rex.@org.checkerframework.checker.nullness.qual.PolyNull RexNode' to 'T'
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
    for (int i = 0; i < exprList.size(); i++) {
      T expr = exprList.get(i);
      T expr2 = (T) apply(expr); // Avoid NPE if expr is null
      if (expr != expr2) {
        ++changeCount;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java

    ImmutableIntList list = EMPTY;
    List<Integer> keys = (List<Integer>) map.get("keys");
    if (keys != null) {
      list = ImmutableIntList.copyOf(keys);
```

### UNCHECKED_WARNING
Unchecked method 'toSarg(Class, List, RexUnknownAs)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
  public RexNode makeIn(RexNode arg, List<? extends RexNode> ranges) {
    if (areAssignable(arg, ranges)) {
      final Sarg sarg = toSarg(Comparable.class, ranges, RexUnknownAs.UNKNOWN);
      if (sarg != null) {
        final List<RelDataType> types = ranges.stream()
```

### UNCHECKED_WARNING
Unchecked method 'toComparable(Class, RexNode)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
  @SuppressWarnings("BetaApi")
  public RexNode makeBetween(RexNode arg, RexNode lower, RexNode upper) {
    final Comparable lowerValue = toComparable(Comparable.class, lower);
    final Comparable upperValue = toComparable(Comparable.class, upper);
    if (lowerValue != null
```

### UNCHECKED_WARNING
Unchecked method 'toComparable(Class, RexNode)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
  public RexNode makeBetween(RexNode arg, RexNode lower, RexNode upper) {
    final Comparable lowerValue = toComparable(Comparable.class, lower);
    final Comparable upperValue = toComparable(Comparable.class, upper);
    if (lowerValue != null
        && upperValue != null
```

### UNCHECKED_WARNING
Unchecked method 'of(RexUnknownAs, RangeSet)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
        && areAssignable(arg, Arrays.asList(lower, upper))) {
      final Sarg sarg =
          Sarg.of(RexUnknownAs.UNKNOWN,
              ImmutableRangeSet.<Comparable>of(
                  Range.closed(lowerValue, upperValue)));
```

### UNCHECKED_WARNING
Unchecked method 'sargRef(RexBuilder, RexNode, Sarg, RelDataType, RexUnknownAs)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
        final Sarg sarg = requireNonNull(literal.getValueAs(Sarg.class), "Sarg");
        if (maxComplexity < 0 || sarg.complexity() < maxComplexity) {
          return sargRef(rexBuilder, ref, sarg, literal.getType(),
              RexUnknownAs.UNKNOWN);
        }
```

### UNCHECKED_WARNING
Unchecked method 'isOpenInterval(RangeSet)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    private static boolean simpleSarg(Sarg sarg) {
      return sarg.isPoints()
          || RangeSets.isOpenInterval(sarg.rangeSet)
          || sarg.isComplementedPoints();
    }
```

### UNCHECKED_WARNING
Unchecked method 'sargRef(RexBuilder, RexNode, Sarg, RelDataType, RexUnknownAs)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
          // Expand small sargs into comparisons in order to avoid plan changes
          // and better readability.
          return RexUtil.sargRef(rexBuilder, sargBuilder.ref, sarg,
              term.getType(), unknownAs);
        }
```

### UNCHECKED_WARNING
Unchecked method 'simpleSarg(RexBuilder, RexNode, Sarg, RexUnknownAs)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      final Sarg sarg = castNonNull(literal.getValueAs(Sarg.class));
      if (sarg.isAll() || sarg.isNone()) {
        return RexUtil.simpleSarg(rexBuilder, a, sarg, unknownAs);
      }
      // Remove null from sarg if the left-hand side is never null
```

### UNCHECKED_WARNING
Unchecked method 'of(RexUnknownAs, RangeSet)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
        if (simplified != null
            && simplified.isAlwaysFalse()) {
          final Sarg sarg2 = Sarg.of(UNKNOWN, sarg.rangeSet);
          final RexLiteral literal2 =
              rexBuilder.makeLiteral(sarg2, literal.getType(),
```

### UNCHECKED_WARNING
Unchecked method 'simplifyAnd2ForUnknownAsFalse(List, List, Class)' invocation
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    switch (unknownAs) {
    case FALSE:
      return simplifyAnd2ForUnknownAsFalse(terms, notTerms, Comparable.class);
    default:
      break;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
    }
    return changeCount == 0
        ? (List<String>) nameList
        : newNameList;
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map,org.apache.calcite.plan.TableAccessMap.Mode\>'
in `core/src/main/java/org/apache/calcite/plan/TableAccessMap.java`
#### Snippet
```java
   */
  public TableAccessMap() {
    accessMap = Collections.EMPTY_MAP;
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.calcite.plan.RelTrait' to 'T'
in `core/src/main/java/org/apache/calcite/plan/RelTrait.java`
#### Snippet
```java
   */
  default <T extends RelTrait> T apply(Mappings.TargetMapping mapping) {
    return (T) this;
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.plan.RelTraitDef' to 'org.apache.calcite.plan.RelTraitDef'
in `core/src/main/java/org/apache/calcite/plan/RelTraitSet.java`
#### Snippet
```java
  public boolean containsIfApplicable(RelTrait trait) {
    // Note that '==' is sufficient, because trait should be canonized.
    final RelTrait trait1 = getTrait(trait.getTraitDef());
    return trait1 == null || trait1 == trait;
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.plan.RelTraitDef' to 'org.apache.calcite.plan.RelTraitDef'
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSet.java`
#### Snippet
```java
      for (RelTrait fromTrait : difference) {
        RelTraitDef traitDef = fromTrait.getTraitDef();
        RelTrait toTrait = to.getTraitSet().getTrait(traitDef);

        if (toTrait == null || !traitDef.canConvert(
```

### UNCHECKED_WARNING
Unchecked call to 'canConvert(RelOptPlanner, T, T)' as a member of raw type 'org.apache.calcite.plan.RelTraitDef'
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSet.java`
#### Snippet
```java
        RelTrait toTrait = to.getTraitSet().getTrait(traitDef);

        if (toTrait == null || !traitDef.canConvert(
            cluster.getPlanner(), fromTrait, toTrait)) {
          needsConverter = false;
```

### UNCHECKED_WARNING
Unchecked call to 'canonize(T)' as a member of raw type 'org.apache.calcite.plan.RelTraitDef'
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSet.java`
#### Snippet
```java
      rels.add(rel);
      for (RelTrait trait : rel.getTraitSet()) {
        assert trait == trait.getTraitDef().canonize(trait);
      }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.rex.RexVisitor' to 'org.apache.calcite.rex.RexVisitor'
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        }
      };
      joinCondition.accept(rexVisitor);
    } catch (Util.FoundOne e) {
      return false;
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        return null;
      }
      if (!referenceByMapping(query.condition, qInput0Projs, qInput1Projs)) {
        return null;
      }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      final List<RexNode> identityProjects =
          rexBuilder.identityProjects(qInput1.rowType);
      if (!referenceByMapping(query.condition, qInput0Projs, identityProjects)) {
        return null;
      }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      final List<RexNode> identityProjects =
          rexBuilder.identityProjects(qInput0.rowType);
      if (!referenceByMapping(query.condition, identityProjects, qInput1Projs)) {
        return null;
      }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
            dispatcher.lookupVisitMethod(
                visitor.getClass(),
                (Class<? extends E>) arg0.getClass(),
                methodName,
                otherArgClassList);
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `core/src/main/java/org/apache/calcite/util/ReflectUtil.java`
#### Snippet
```java
    if (hierarchyRoot != null) {
      Class paramType = method.getParameterTypes()[0];
      if (!hierarchyRoot.isAssignableFrom(paramType)) {
        return false;
      }
```

### UNCHECKED_WARNING
Unchecked call to 'convert(RelOptPlanner, RelNode, T, boolean)' as a member of raw type 'org.apache.calcite.plan.RelTraitDef'
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java

      RelNode convertedRel =
          traitDef.convert(
              this,
              converted,
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.plan.RelTraitDef' to 'org.apache.calcite.plan.RelTraitDef'
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
              allowInfiniteCostConverters);
      if (convertedRel != null) {
        assert castNonNull(convertedRel.getTraitSet().getTrait(traitDef)).satisfies(toTrait);
        register(convertedRel, converted);
      }
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `core/src/main/java/org/apache/calcite/util/BarfingInvocationHandler.java`
#### Snippet
```java
    try {
      matchingMethod =
          clazz.getMethod(
              method.getName(),
              method.getParameterTypes());
```

### UNCHECKED_WARNING
Unchecked call to 'SpecialSarg(ImmutableRangeSet, RexUnknownAs, String, int)' as a member of raw type 'org.apache.calcite.util.Sarg.SpecialSarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, NOT_EQUAL)} is equivalent to {@code x <> x}. */
  private static final SpecialSarg NOT_EQUAL =
      new SpecialSarg(ImmutableRangeSet.of(), RexUnknownAs.UNKNOWN,
          "Sarg[<>]", 6);

```

### UNCHECKED_WARNING
Unchecked call to 'SpecialSarg(ImmutableRangeSet, RexUnknownAs, String, int)' as a member of raw type 'org.apache.calcite.util.Sarg.SpecialSarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, TRUE)} is equivalent to {@code TRUE}. */
  private static final SpecialSarg TRUE =
      new SpecialSarg(ImmutableRangeSet.of().complement(), RexUnknownAs.TRUE,
          "Sarg[TRUE]", 5);

```

### UNCHECKED_WARNING
Unchecked call to 'SpecialSarg(ImmutableRangeSet, RexUnknownAs, String, int)' as a member of raw type 'org.apache.calcite.util.Sarg.SpecialSarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, FALSE)} is equivalent to {@code FALSE}. */
  private static final SpecialSarg FALSE =
      new SpecialSarg(ImmutableRangeSet.of(), RexUnknownAs.FALSE,
          "Sarg[FALSE]", 2);

```

### UNCHECKED_WARNING
Unchecked call to 'SpecialSarg(ImmutableRangeSet, RexUnknownAs, String, int)' as a member of raw type 'org.apache.calcite.util.Sarg.SpecialSarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * {@code x IS NOT NULL}. */
  private static final SpecialSarg IS_NOT_NULL =
      new SpecialSarg(ImmutableRangeSet.of().complement(), RexUnknownAs.FALSE,
          "Sarg[IS NOT NULL]", 3);

```

### UNCHECKED_WARNING
Unchecked call to 'SpecialSarg(ImmutableRangeSet, RexUnknownAs, String, int)' as a member of raw type 'org.apache.calcite.util.Sarg.SpecialSarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, IS_NULL)} is equivalent to {@code x IS NULL}. */
  private static final SpecialSarg IS_NULL =
      new SpecialSarg(ImmutableRangeSet.of(), RexUnknownAs.TRUE,
          "Sarg[IS NULL]", 4);

```

### UNCHECKED_WARNING
Unchecked call to 'SpecialSarg(ImmutableRangeSet, RexUnknownAs, String, int)' as a member of raw type 'org.apache.calcite.util.Sarg.SpecialSarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
   * <p>{@code SEARCH(x, EQUAL)} is equivalent to {@code x = x}. */
  private static final SpecialSarg EQUAL =
      new SpecialSarg(ImmutableRangeSet.of().complement(), RexUnknownAs.UNKNOWN,
          "Sarg[=]", 7);

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.Sarg.SpecialSarg' to 'org.apache.calcite.util.Sarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
      switch (nullAs) {
      case FALSE:
        return FALSE;
      case TRUE:
        return IS_NULL;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.Sarg.SpecialSarg' to 'org.apache.calcite.util.Sarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
        return FALSE;
      case TRUE:
        return IS_NULL;
      default:
        return NOT_EQUAL;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.Sarg.SpecialSarg' to 'org.apache.calcite.util.Sarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
        return IS_NULL;
      default:
        return NOT_EQUAL;
      }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.Sarg.SpecialSarg' to 'org.apache.calcite.util.Sarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
      switch (nullAs) {
      case FALSE:
        return IS_NOT_NULL;
      case TRUE:
        return TRUE;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.Sarg.SpecialSarg' to 'org.apache.calcite.util.Sarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
        return IS_NOT_NULL;
      case TRUE:
        return TRUE;
      default:
        return EQUAL;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.Sarg.SpecialSarg' to 'org.apache.calcite.util.Sarg'
in `core/src/main/java/org/apache/calcite/util/Sarg.java`
#### Snippet
```java
        return TRUE;
      default:
        return EQUAL;
      }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'ImmutableNullableSet(ImmutableSet)' as a member of raw type 'org.apache.calcite.util.ImmutableNullableSet'
in `core/src/main/java/org/apache/calcite/util/ImmutableNullableSet.java`
#### Snippet
```java
  @SuppressWarnings("rawtypes")
  private static final Set SINGLETON_NULL =
      new ImmutableNullableSet(ImmutableSet.of(NullSentinel.INSTANCE));

  private static final Set<Integer> SINGLETON = Collections.singleton(0);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'E'
in `core/src/main/java/org/apache/calcite/util/ImmutableNullableSet.java`
#### Snippet
```java
  @Override public Iterator<E> iterator() {
    return Util.transform(elements.iterator(), e ->
        e == NullSentinel.INSTANCE ? castNonNull(null) : (E) e);
  }

```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `core/src/main/java/org/apache/calcite/util/DelegatingInvocationHandler.java`
#### Snippet
```java
    try {
      matchingMethod =
          clazz.getMethod(
              method.getName(),
              method.getParameterTypes());
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.NameMap' to 'org.apache.calcite.util.NameMap'
in `core/src/main/java/org/apache/calcite/util/NameMap.java`
#### Snippet
```java
  /** Creates a NameMap that is an immutable copy of a given map. */
  public static <V> NameMap<V> immutableCopyOf(Map<String, V> names) {
    return new NameMap<>(ImmutableSortedMap.copyOf(names, COMPARATOR));
  }

```

### UNCHECKED_WARNING
Unchecked call to 'NameMap(NavigableMap)' as a member of raw type 'org.apache.calcite.util.NameMap'
in `core/src/main/java/org/apache/calcite/util/NameMap.java`
#### Snippet
```java
  /** Creates a NameMap that is an immutable copy of a given map. */
  public static <V> NameMap<V> immutableCopyOf(Map<String, V> names) {
    return new NameMap<>(ImmutableSortedMap.copyOf(names, COMPARATOR));
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.CaseInsensitiveComparator' to 'java.util.Comparator'
in `core/src/main/java/org/apache/calcite/util/NameMap.java`
#### Snippet
```java
  /** Creates a NameMap that is an immutable copy of a given map. */
  public static <V> NameMap<V> immutableCopyOf(Map<String, V> names) {
    return new NameMap<>(ImmutableSortedMap.copyOf(names, COMPARATOR));
  }

```

### UNCHECKED_WARNING
Unchecked call to 'subMap(K, boolean, K, boolean)' as a member of raw type 'java.util.NavigableMap'
in `core/src/main/java/org/apache/calcite/util/NameMap.java`
#### Snippet
```java
      ceilingKey = COMPARATOR.ceilingKey(name);
    }
    NavigableMap subMap = ((NavigableMap) map).subMap(floorKey, true, ceilingKey, true);
    return Collections.unmodifiableNavigableMap((NavigableMap<String, V>) subMap);
  }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.NavigableMap' to 'java.util.NavigableMap'
in `core/src/main/java/org/apache/calcite/util/NameMap.java`
#### Snippet
```java
    }
    NavigableMap subMap = ((NavigableMap) map).subMap(floorKey, true, ceilingKey, true);
    return Collections.unmodifiableNavigableMap((NavigableMap<String, V>) subMap);
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.CaseInsensitiveComparator' to 'java.util.Comparator'
in `core/src/main/java/org/apache/calcite/util/NameMap.java`
#### Snippet
```java
  /** Creates a NameMap, initially empty. */
  public NameMap() {
    this(new TreeMap<String, V>(COMPARATOR));
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.util.CaseInsensitiveComparator' to 'java.util.Comparator'
in `core/src/main/java/org/apache/calcite/util/NameSet.java`
#### Snippet
```java
/** Set of names that can be accessed with and without case sensitivity. */
public class NameSet {
  public static final Comparator<String> COMPARATOR = CaseInsensitiveComparator.COMPARATOR;

  private static final Object DUMMY = new Object();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `core/src/main/java/org/apache/calcite/util/ImmutableIntList.java`
#### Snippet
```java
      // Make a new array of a's runtime type, but my contents:
      a = a.getClass() == Object[].class
          ? (T[]) new Object[size]
          : (T[]) Array.newInstance(
              requireNonNull(a.getClass().getComponentType()), size);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `core/src/main/java/org/apache/calcite/util/ImmutableIntList.java`
#### Snippet
```java
      a = a.getClass() == Object[].class
          ? (T[]) new Object[size]
          : (T[]) Array.newInstance(
              requireNonNull(a.getClass().getComponentType()), size);
    }
    if ((Class) a.getClass() == Integer[].class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `core/src/main/java/org/apache/calcite/util/graph/DefaultDirectedGraph.java`
#### Snippet
```java
    vertexMap.keySet().removeAll(vertexSet);
    for (VertexInfo<V, E> info : vertexMap.values()) {
      info.outEdges.removeIf(e -> vertexSet.contains(castNonNull((V) e.target)));
      info.inEdges.removeIf(e -> vertexSet.contains(castNonNull((V) e.source)));
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `core/src/main/java/org/apache/calcite/util/graph/DefaultDirectedGraph.java`
#### Snippet
```java
    for (VertexInfo<V, E> info : vertexMap.values()) {
      info.outEdges.removeIf(e -> vertexSet.contains(castNonNull((V) e.target)));
      info.inEdges.removeIf(e -> vertexSet.contains(castNonNull((V) e.source)));
    }
  }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
        }
      }
      return (T) DUMMY;
    }
  }
```

### UNCHECKED_WARNING
Unchecked call to 'create(SchemaPlus, String, Map, RelDataType)' as a member of raw type 'org.apache.calcite.schema.TableFactory'
in `core/src/main/java/org/apache/calcite/model/ModelHandler.java`
#### Snippet
```java
              jsonTable.factory);
      final Table table =
          tableFactory.create(schema, jsonTable.name,
              operandMap(null, jsonTable.operand), null);
      for (JsonColumn column : jsonTable.columns) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/adapter/clone/CloneSchema.java`
#### Snippet
```java
                  ? RelCollations.createSingleton(loader.sortField)
                  : collations;
          return new ArrayTable.Content(loader.representationValues,
              loader.size(), collation2);
        }));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Iterable' to 'java.lang.Iterable'
in `core/src/main/java/org/apache/calcite/adapter/java/ReflectiveSchema.java`
#### Snippet
```java
    }
    if (o instanceof Iterable) {
      return Linq4j.asEnumerable((Iterable) o);
    }
    throw new RuntimeException(
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
      case JAVA_SQL_TIMESTAMP:
        final List<@Nullable Long> longs =
            Util.transform((List<@Nullable Timestamp>) list,
                (Timestamp t) -> t == null ? null : t.getTime());
        return longs;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
      case JAVA_SQL_TIME:
        return Util.<@Nullable Time, @Nullable Integer>transform(
            (List<@Nullable Time>) list,
            (Time t) -> t == null ? null
                : (int) (t.getTime() % DateTimeUtils.MILLIS_PER_DAY));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/adapter/clone/ColumnLoader.java`
#### Snippet
```java
      case JAVA_SQL_DATE:
        return Util.<@Nullable Date, @Nullable Integer>transform(
            (List<@Nullable Date>) list, (Date d) -> d == null
                ? null
                : (int) (d.getTime() / DateTimeUtils.MILLIS_PER_DAY));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.calcite.util.Pair'
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java

    private static Pair<Object, @Nullable Comparable[]> unfreeze(Object value) {
      return (Pair<Object, @Nullable Comparable[]>) value;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.calcite.util.Pair'
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java

    private static Pair<@Nullable Object, Integer> unfreeze(Object value) {
      return (Pair<@Nullable Object, Integer>) value;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableRelImplementor.java`
#### Snippet
```java

    @Override public Void visit(FunctionExpression functionExpression) {
      final List<ParameterExpression> list = functionExpression.parameterList;
      for (ParameterExpression pe : list) {
        types.add(pe.getType());
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java
    Class[] argumentTypes = Types.toClassArray(arguments);
    try {
      Method candidate = clazz.getMethod(methodName, argumentTypes);
      return Expressions.call(targetExpression, candidate, arguments);
    } catch (NoSuchMethodException e) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
    final List<@Nullable String> fieldNameList =
        fieldNames == null ? null
          : fieldNames instanceof List ? (List<@Nullable String>) fieldNames
          : ImmutableNullableList.copyOf(fieldNames);
    final RelNode input = peek();
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.linq4j.Queryable' to 'org.apache.calcite.linq4j.Queryable'
in `core/src/main/java/org/apache/calcite/prepare/CalcitePrepareImpl.java`
#### Snippet
```java
            prepare.createCluster(planner, rexBuilder);
        return new LixToRelTranslator(cluster, CalcitePreparingStmt.this)
            .translate(queryable);
      }, resultType);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
    if (element == null) {
      if (nullClause == SqlJsonConstructorNullClause.NULL_ON_NULL) {
        list.add(null);
      }
    } else {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
      }
    } else {
      list.add(element);
    }
  }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
    if (v == null) {
      if (nullClause == SqlJsonConstructorNullClause.NULL_ON_NULL) {
        map.put(k, null);
      }
    } else {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
      }
    } else {
      map.put(k, v);
    }
  }
```

### UNCHECKED_WARNING
Unchecked call to 'compare(T, T)' as a member of raw type 'java.util.Comparator'
in `core/src/main/java/org/apache/calcite/runtime/ArrayComparator.java`
#### Snippet
```java
    for (int i = 0; i < comparators.length; i++) {
      Comparator comparator = comparators[i];
      int c = comparator.compare(o1[i], o2[i]);
      if (c != 0) {
        return c;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `core/src/main/java/org/apache/calcite/runtime/ConsList.java`
#### Snippet
```java
    final int s = size();
    if (s > castNonNull(a).length) {
      a = (T[]) Arrays.copyOf(a, s, a.getClass());
    } else if (s < a.length) {
      a[s] = castNonNull(null);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.stream.Stream' to 'java.util.stream.Stream'. Reason: 'merger.getMergedLineStrings()' has raw type, so result of stream is erased
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
    LineMerger merger = new LineMerger();
    merger.add(geom);
    LineString[] geometries = ((Stream<Object>) merger.getMergedLineStrings().stream())
        .map(LineString.class::cast)
        .toArray(size -> new LineString[size]);
```

### UNCHECKED_WARNING
Unchecked method 'compare(List, List)' invocation
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public int compareTo(List o) {
      return ComparableListImpl.compare((List) this, o);
    }

```

### UNCHECKED_WARNING
Unchecked method 'compare(List, List)' invocation
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public int compareTo(List o) {
      return ComparableListImpl.compare((List) this, o);
    }

```

### UNCHECKED_WARNING
Unchecked method 'compare(List, List)' invocation
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public int compareTo(List o) {
      return ComparableListImpl.compare((List) this, o);
    }

```

### UNCHECKED_WARNING
Unchecked method 'compare(List, List)' invocation
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public int compareTo(List o) {
      return ComparableListImpl.compare((List) this, o);
    }

```

### UNCHECKED_WARNING
Unchecked method 'compare(T, T)' invocation
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        Comparable o0 = list0.get(i);
        Comparable o1 = list1.get(i);
        int c = compare(o0, o1);
        if (c != 0) {
          return c;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public int compareTo(List o) {
      return compare(list, o);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.runtime.FlatLists.ComparableListImpl' to 'org.apache.calcite.runtime.FlatLists.ComparableList'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
      //   bit.
      if (copy) {
        return new ComparableListImpl<>(Arrays.asList(t.clone()));
      } else {
        return new ComparableListImpl<>(Arrays.asList(t));
```

### UNCHECKED_WARNING
Unchecked call to 'ComparableListImpl(List)' as a member of raw type 'org.apache.calcite.runtime.FlatLists.ComparableListImpl'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
      //   bit.
      if (copy) {
        return new ComparableListImpl<>(Arrays.asList(t.clone()));
      } else {
        return new ComparableListImpl<>(Arrays.asList(t));
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.runtime.FlatLists.ComparableListImpl' to 'org.apache.calcite.runtime.FlatLists.ComparableList'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        return new ComparableListImpl<>(Arrays.asList(t.clone()));
      } else {
        return new ComparableListImpl<>(Arrays.asList(t));
      }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'ComparableListImpl(List)' as a member of raw type 'org.apache.calcite.runtime.FlatLists.ComparableListImpl'
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java
        return new ComparableListImpl<>(Arrays.asList(t.clone()));
      } else {
        return new ComparableListImpl<>(Arrays.asList(t));
      }
    }
```

### UNCHECKED_WARNING
Unchecked method 'compare(List, List)' invocation
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public int compareTo(List o) {
      return ComparableListImpl.compare((List) this, o);
    }
  }
```

### UNCHECKED_WARNING
Unchecked method 'compare(List, List)' invocation
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public int compareTo(List o) {
      return ComparableListImpl.compare((List) this, o);
    }

```

### UNCHECKED_WARNING
Unchecked method 'compare(List, List)' invocation
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public int compareTo(List o) {
      return ComparableListImpl.compare((List) this, o);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
          void sample() {
            if (curentPosition > startIndex) {
              if (rows[curentPosition - 1].compareTo(rows[curentPosition])
                  > 0) {
                // update rank
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    List result = new ArrayList(list.size());
    for (Object e : list) {
      result.add(structAccess(e, 0, null));
    }
    return result;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
      Collection collection2) {
    List resultCollection = new ArrayList(collection1.size() + collection2.size());
    resultCollection.addAll(collection1);
    resultCollection.addAll(collection2);
    return resultCollection;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    List resultCollection = new ArrayList(collection1.size() + collection2.size());
    resultCollection.addAll(collection1);
    resultCollection.addAll(collection2);
    return resultCollection;
  }
```

### UNCHECKED_WARNING
Unchecked call to 'LinkedList(Collection)' as a member of raw type 'java.util.LinkedList'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    }
    // TODO: use Multisets?
    Collection multisetLocal = new LinkedList(multiset);
    for (Object e : possibleSubMultiset) {
      if (!multisetLocal.remove(e)) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    for (Object element : list) {
      if (element != null) {
        result.add(element);
      }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Set'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    Set resultCollection =
        new HashSet(Math.max((int) ((collection1.size() + collection2.size()) / .75f) + 1, 16));
    resultCollection.addAll(collection1);
    resultCollection.addAll(collection2);
    return new ArrayList(resultCollection);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Set'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
        new HashSet(Math.max((int) ((collection1.size() + collection2.size()) / .75f) + 1, 16));
    resultCollection.addAll(collection1);
    resultCollection.addAll(collection2);
    return new ArrayList(resultCollection);
  }
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    resultCollection.addAll(collection1);
    resultCollection.addAll(collection2);
    return new ArrayList(resultCollection);
  }

```

### UNCHECKED_WARNING
Unchecked call to 'LinkedHashSet(Collection)' as a member of raw type 'java.util.LinkedHashSet'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Support the ARRAY_DISTINCT function. */
  public static List distinct(List list) {
    Set result = new LinkedHashSet<>(list);
    return new ArrayList<>(result);
  }
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  public static List distinct(List list) {
    Set result = new LinkedHashSet<>(list);
    return new ArrayList<>(result);
  }

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Support the MAP_VALUES function. */
  public static List mapValues(Map map) {
    return new ArrayList<>(map.values());
  }

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
  /** Support the MAP_KEYS function. */
  public static List mapKeys(Map map) {
    return new ArrayList<>(map.keySet());
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.linq4j.Enumerator' to 'org.apache.calcite.linq4j.Enumerator'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
        final List<Enumerator<@Nullable Object>> enumerators = new ArrayList<>();
        for (Object list : lists) {
          enumerators.add(Linq4j.enumerator((List) list));
        }
        final Enumerator<List<@Nullable Object>> product = Linq4j.product(enumerators);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    Set set = new HashSet(Math.max((int) (collection.size() / .75f) + 1, 16));
    for (Object e : collection) {
      if (!set.add(e)) {
        return false;
      }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.apache.calcite.linq4j.Queryable', required 'org.apache.calcite.linq4j.Queryable'
in `plus/src/main/java/org/apache/calcite/chinook/CodesFunction.java`
#### Snippet
```java
      }

      @Override public Queryable<String[]> asQueryable(QueryProvider queryProvider,
                                                       SchemaPlus schema,
                                                       String tableName) {
```

### UNCHECKED_WARNING
Unchecked call to 'TpchQueryableTable(TpchTable)' as a member of raw type 'org.apache.calcite.adapter.tpch.TpchSchema.TpchQueryableTable'
in `plus/src/main/java/org/apache/calcite/adapter/tpch/TpchSchema.java`
#### Snippet
```java
    for (TpchTable<?> tpchTable : TpchTable.getTables()) {
      builder.put(tpchTable.getTableName().toUpperCase(Locale.ROOT),
          new TpchQueryableTable(tpchTable));
    }
    this.tableMap = builder.build();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.apache.calcite.linq4j.Queryable', required 'org.apache.calcite.linq4j.Queryable'
in `plus/src/main/java/org/apache/calcite/chinook/PreferredGenresTableFactory.java`
#### Snippet
```java
      }

      @Override public Queryable<Integer> asQueryable(
          QueryProvider qp,
          SchemaPlus sp,
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.apache.calcite.linq4j.Queryable', required 'org.apache.calcite.linq4j.Queryable'
in `plus/src/main/java/org/apache/calcite/chinook/PreferredAlbumsTableFactory.java`
#### Snippet
```java
      }

      @Override public Queryable<Integer> asQueryable(
          QueryProvider qp,
          SchemaPlus sp,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaTableFactory.java`
#### Snippet
```java
    if (operand.containsKey(KafkaTableConstants.SCHEMA_CONSUMER_PARAMS)) {
      tableOptionBuilder.setConsumerParams(
          (Map<String, String>) operand.get(KafkaTableConstants.SCHEMA_CONSUMER_PARAMS));
    }
    if (operand.containsKey(KafkaTableConstants.SCHEMA_CUST_CONSUMER)) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.kafka.clients.consumer.ConsumerRecords' to 'org.apache.kafka.clients.consumer.ConsumerRecords'. Reason: 'consumer' has raw type, so result of poll is erased
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaMessageEnumerator.java`
#### Snippet
```java

  private void pullRecords() {
    ConsumerRecords<K, V> records = consumer.poll(Duration.ofMillis(100));
    for (ConsumerRecord record : records) {
      bufferedRecords.add(record);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.kafka.clients.consumer.ConsumerRecord' to 'org.apache.kafka.clients.consumer.ConsumerRecord'
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaMessageEnumerator.java`
#### Snippet
```java
    ConsumerRecords<K, V> records = consumer.poll(Duration.ofMillis(100));
    for (ConsumerRecord record : records) {
      bufferedRecords.add(record);
    }
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.adapter.kafka.KafkaMessageEnumerator' to 'org.apache.calcite.linq4j.Enumerator'
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaStreamTable.java`
#### Snippet
```java
      @Override public Enumerator<@Nullable Object[]> enumerator() {
        if (tableOptions.getConsumer() != null) {
          return new KafkaMessageEnumerator(tableOptions.getConsumer(),
              tableOptions.getRowConverter(), cancelFlag);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'KafkaMessageEnumerator(Consumer, KafkaRowConverter, AtomicBoolean)' as a member of raw type 'org.apache.calcite.adapter.kafka.KafkaMessageEnumerator'
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaStreamTable.java`
#### Snippet
```java
      @Override public Enumerator<@Nullable Object[]> enumerator() {
        if (tableOptions.getConsumer() != null) {
          return new KafkaMessageEnumerator(tableOptions.getConsumer(),
              tableOptions.getRowConverter(), cancelFlag);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'subscribe(Collection)' as a member of raw type 'org.apache.kafka.clients.consumer.Consumer'
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaStreamTable.java`
#### Snippet
```java
        }
        Consumer consumer = new KafkaConsumer<>(consumerConfig);
        consumer.subscribe(Collections.singletonList(tableOptions.getTopicName()));

        return new KafkaMessageEnumerator(consumer, tableOptions.getRowConverter(), cancelFlag);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.adapter.kafka.KafkaMessageEnumerator' to 'org.apache.calcite.linq4j.Enumerator'
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaStreamTable.java`
#### Snippet
```java
        consumer.subscribe(Collections.singletonList(tableOptions.getTopicName()));

        return new KafkaMessageEnumerator(consumer, tableOptions.getRowConverter(), cancelFlag);
      }
    };
```

### UNCHECKED_WARNING
Unchecked call to 'KafkaMessageEnumerator(Consumer, KafkaRowConverter, AtomicBoolean)' as a member of raw type 'org.apache.calcite.adapter.kafka.KafkaMessageEnumerator'
in `kafka/src/main/java/org/apache/calcite/adapter/kafka/KafkaStreamTable.java`
#### Snippet
```java
        consumer.subscribe(Collections.singletonList(tableOptions.getTopicName()));

        return new KafkaMessageEnumerator(consumer, tableOptions.getRowConverter(), cancelFlag);
      }
    };
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisSchema.java`
#### Snippet
```java
        }
        dataFormat = map.get("dataFormat").toString();
        fields = (List<LinkedHashMap<String, Object>>) map.get("fields");
        if (map.get("keyDelimiter") != null) {
          keyDelimiter = map.get("keyDelimiter").toString();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbSchemaFactory.java`
#### Snippet
```java
  @Override public Schema create(SchemaPlus parentSchema, String name,
      Map<String, Object> operand) {
    final List<String> sqlFilePathList = (List<String>) operand.get("sqlFilePath");
    final String ibdDataFileBasePath = (String) operand.get("ibdDataFileBasePath");
    final String timeZone = (String) operand.get("timeZone");
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.@org.checkerframework.checker.nullness.qual.Nullable Object' to 'E'
in `linq4j/src/main/java/org/apache/calcite/linq4j/MemoryFactory.java`
#### Snippet
```java
            + " points in time, you wanted " + position);
      }
      return (E) this.values[this.offset.plus(position - 1 - future).get()];
    }
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.lang.Iterable'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Shuttle.java`
#### Snippet
```java
        : Expressions.lambda(
            requireNonNull(body, "body"),
            functionExpression.parameterList);
  }

```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java
      Class... argumentTypes) {
    try {
      return clazz.getMethod(methodName, argumentTypes);
    } catch (NoSuchMethodException e) {
      for (Method method : clazz.getMethods()) {
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java
  @SuppressWarnings("nullness")
  private static boolean assignableFrom(Class parameter, Class argument) {
    return parameter.isAssignableFrom(argument)
           || parameter.isPrimitive()
        && argument.isPrimitive()
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java

  public static boolean isAssignableFrom(Type type0, Type type) {
    return toClass(type0).isAssignableFrom(toClass(type));
  }

```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java
    if (constructors.length == 0 && argumentTypes.length == 0) {
      try {
        return clazz.getConstructor();
      } catch (NoSuchMethodException e) {
        // ignore
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java

  public FunctionExpression(F function) {
    this((Class) function.getClass(), function, null, ImmutableList.of());
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/DeterministicCodeOptimizer.java`
#### Snippet
```java
  protected boolean isConstructorDeterministic(NewExpression newExpression) {
    final Class klass = (Class) newExpression.type;
    final Constructor constructor = getConstructor(klass);
    return allMethodsDeterministic(klass)
        || constructor != null
```

### UNCHECKED_WARNING
Unchecked call to 'isAnnotationPresent(Class)' as a member of raw type 'java.lang.Class'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/DeterministicCodeOptimizer.java`
#### Snippet
```java
    return DETERMINISTIC_CLASSES.contains(klass)
        || "org.apache.calcite.avatica.util.DateTimeUtils".equals(klass.getCanonicalName())
        || klass.isAnnotationPresent(Deterministic.class);
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.List'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
  private static <T> List<T> toList(Iterable<? extends T> iterable) {
    if (iterable instanceof List) {
      return (List<T>) iterable;
    }
    final List<T> list = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
  public static List projectMultiset(Object... objects) {
    // The first input is a multiset
    final List<Object[]> inputMultiset = (List) objects[0];
    final List projectedMultiset = new ArrayList<>();

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
          newRow[j - 1] = row[(Integer) objects[j]];
        }
        projectedMultiset.add(newRow);
      } else {
        // Projecting a single column
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      } else {
        // Projecting a single column
        projectedMultiset.add(row[(Integer) objects[1]]);
      }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.@org.checkerframework.checker.nullness.qual.Nullable Object' to 'K'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java

    @Override public @Nullable V get(@Nullable Object key) {
      return map.get(wrap((K) key));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.linq4j.EnumerableDefaults.SortedAggregateEnumerator' to 'org.apache.calcite.linq4j.Enumerator'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    return new AbstractEnumerable<TResult>() {
      @Override public Enumerator<TResult> enumerator() {
        return new SortedAggregateEnumerator(
          enumerable, keySelector, accumulatorInitializer,
          accumulatorAdder, resultSelector, comparator);
```

### UNCHECKED_WARNING
Unchecked call to 'SortedAggregateEnumerator(Enumerable, Function1, Function0, Function2, Function2, Comparator)' as a member of raw type 'org.apache.calcite.linq4j.EnumerableDefaults.SortedAggregateEnumerator'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    return new AbstractEnumerable<TResult>() {
      @Override public Enumerator<TResult> enumerator() {
        return new SortedAggregateEnumerator(
          enumerable, keySelector, accumulatorInitializer,
          accumulatorAdder, resultSelector, comparator);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.@org.checkerframework.checker.nullness.qual.Nullable Object' to 'K'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
    @SuppressWarnings("contracts.conditional.postcondition.not.satisfied")
    @Override public boolean containsKey(@Nullable Object key) {
      return map.containsKey(wrap((K) key));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.@org.checkerframework.checker.nullness.qual.Nullable Object' to 'K'
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java

    @Override public @Nullable V remove(@Nullable Object key) {
      return map.remove(wrap((K) key));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Throwable' to 'E'
in `testkit/src/main/java/org/apache/calcite/util/TestUtil.java`
#### Snippet
```java
      e = e.getCause();
    }
    throw (E) e;
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Throwable' to 'E'
in `testkit/src/main/java/org/apache/calcite/util/TestUtil.java`
#### Snippet
```java
      String message) throws E {
    e.addSuppressed(new ExtraInformation(message));
    throw (E) e;
  }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.apache.calcite.linq4j.Queryable', required 'org.apache.calcite.linq4j.Queryable'
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
      }

      @Override public Queryable<Object[]> asQueryable(QueryProvider queryProvider,
          SchemaPlus schema, String tableName) {
        final List<Object[]> table = new AbstractList<Object[]>() {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
        return null;
      } else {
        v.add(i);
        return v;
      }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.calcite.linq4j.Queryable' to 'org.apache.calcite.linq4j.Queryable'
in `ubenchmark/src/jmh/java/org/apache/calcite/benchmarks/RelNodeConversionBenchmark.java`
#### Snippet
```java
        @Override public <E> Queryable<E> asQueryable(
            QueryProvider queryProvider, SchemaPlus schema, String tableName) {
          return (Queryable<E>) enumerable.asQueryable();
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.calcite.linq4j.function.Function1' to 'org.apache.calcite.linq4j.function.Function1'
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchEnumerators.java`
#### Snippet
```java
    }

    return getter;
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'sarg.rangeSet' has raw type, so result of asRanges is erased
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      final SqlTypeName sqlTypeName = type.getSqlTypeName();
      if (sarg.isPoints()) {
        Set<Range> ranges = sarg.rangeSet.asRanges();
        ranges.forEach(range ->
            values.add(sargPointValue(range.lowerEndpoint(), sqlTypeName)));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'sarg.negate().rangeSet' has raw type, so result of asRanges is erased
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
            values.add(sargPointValue(range.lowerEndpoint(), sqlTypeName)));
      } else if (sarg.isComplementedPoints()) {
        Set<Range> ranges = sarg.negate().rangeSet.asRanges();
        ranges.forEach(range ->
            values.add(sargPointValue(range.lowerEndpoint(), sqlTypeName)));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
        throws JsonProcessingException {

      return new MultiValue(name, parser.getCodec().treeToValue(node, Map.class));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
      Object maybeMap = map.get(prefix);
      if (maybeMap instanceof Map) {
        return valueFromPath((Map<String, Object>) maybeMap, suffix);
      }

```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Expression `getInput().getTable()` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigProject.java`
#### Snippet
```java
   */
  @Override public RelOptTable getTable() {
    return getInput().getTable();
  }
}
```

### DataFlowIssue
Method invocation `registerTable` may produce `NullPointerException`
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigTableFactory.java`
#### Snippet
```java
    final List<String> fieldNames = (List<String>) operand.get("columns");
    final PigTable result = new PigTable(file.getAbsolutePath(), fieldNames.toArray(new String[0]));
    schema.unwrap(PigSchema.class).registerTable(name, result);
    return result;
  }
```

### DataFlowIssue
Method invocation `getQualifiedName` may produce `NullPointerException`
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigRel.java`
#### Snippet
```java

    public String getTableName(RelNode input) {
      final List<String> qualifiedName = input.getTable().getQualifiedName();
      return qualifiedName.get(qualifiedName.size() - 1);
    }
```

### DataFlowIssue
Expression `getInput().getTable()` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigFilter.java`
#### Snippet
```java
   */
  @Override public RelOptTable getTable() {
    return getInput().getTable();
  }

```

### DataFlowIssue
Expression `getInput().getTable()` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigAggregate.java`
#### Snippet
```java
   */
  @Override public RelOptTable getTable() {
    return getInput().getTable();
  }

```

### DataFlowIssue
Expression `getLeft().getTable()` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigJoin.java`
#### Snippet
```java
   */
  @Override public RelOptTable getTable() {
    return getLeft().getTable();
  }

```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/PhysicalNode.java`
#### Snippet
```java
    }
    int size = getInputs().size();
    assert size == p.right.size();
    List<RelNode> list = new ArrayList<>(size);
    for (int i = 0; i < size; i++) {
```

### DataFlowIssue
Argument `p.left` might be null
in `core/src/main/java/org/apache/calcite/rel/PhysicalNode.java`
#### Snippet
```java
      list.add(node);
    }
    return copy(p.left, list);
  }

```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/PhysicalNode.java`
#### Snippet
```java
    }
    int size = getInputs().size();
    assert size == p.right.size();
    List<RelNode> list = new ArrayList<>(size);
    for (int i = 0; i < size; i++) {
```

### DataFlowIssue
Argument `p.left` might be null
in `core/src/main/java/org/apache/calcite/rel/PhysicalNode.java`
#### Snippet
```java
      list.add(n);
    }
    return copy(p.left, list);
  }

```

### DataFlowIssue
Unboxing of `field.left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/RelRoot.java`
#### Snippet
```java
    final RexBuilder rexBuilder = rel.getCluster().getRexBuilder();
    for (Pair<Integer, String> field : fields) {
      projects.add(rexBuilder.makeInputRef(rel, field.left));
    }
    return LogicalProject.create(rel, hints, projects, Pair.right(fields), ImmutableSet.of());
```

### DataFlowIssue
Argument `this.offset` might be null
in `core/src/main/java/org/apache/calcite/rel/core/Sort.java`
#### Snippet
```java

  @Override public RelNode accept(RexShuttle shuttle) {
    RexNode offset = shuttle.apply(this.offset);
    RexNode fetch = shuttle.apply(this.fetch);
    List<RexNode> originalSortExps = getSortExps();
```

### DataFlowIssue
Argument `this.fetch` might be null
in `core/src/main/java/org/apache/calcite/rel/core/Sort.java`
#### Snippet
```java
  @Override public RelNode accept(RexShuttle shuttle) {
    RexNode offset = shuttle.apply(this.offset);
    RexNode fetch = shuttle.apply(this.fetch);
    List<RexNode> originalSortExps = getSortExps();
    List<RexNode> sortExps = shuttle.apply(originalSortExps);
```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/core/Window.java`
#### Snippet
```java
            final RexFieldCollation collation = collations.get(index);
            return new RelFieldCollation(
                ((RexLocalRef) collation.left).getIndex(),
                collation.getDirection(),
                collation.getNullDirection());
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/core/Values.java`
#### Snippet
```java
          : Pair.zip(tuple, rowType.getFieldList())) {
        RexLiteral literal = pair.left;
        RelDataType fieldType = pair.right.getType();

        // TODO jvs 19-Feb-2006: strengthen this a bit.  For example,
```

### DataFlowIssue
Argument `literal` might be null
in `core/src/main/java/org/apache/calcite/rel/core/Values.java`
#### Snippet
```java
        // overflow, rounding, and padding/truncation must already have
        // been dealt with.
        if (!RexLiteral.isNullLiteral(literal)) {
          assert SqlTypeUtil.canAssignFrom(fieldType, literal.getType())
              : "to " + fieldType + " from " + literal;
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeImpl.java`
#### Snippet
```java
  @Override public SqlTypeName getSqlTypeName() {
    // The implementations must provide non-null value, however, we keep this for compatibility
    return castNonNull(null);
  }

```

### DataFlowIssue
Unboxing of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/type/DynamicRecordTypeImpl.java`
#### Snippet
```java
        holder.getFieldOrInsert(fieldName, caseSensitive);
    // If a new field is added, we should re-compute the digest.
    if (pair.right) {
      computeDigest();
    }
```

### DataFlowIssue
Method invocation `getKind` will produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectJoinTransposeRule.java`
#### Snippet
```java
            return false;
          }
          if (SqlKind.CAST == expr.getKind()) {
            final RelDataType relType = expr.getType();
            final RexCall castCall = (RexCall) expr;
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/SemiJoinProjectTransposeRule.java`
#### Snippet
```java
    // of the semijoin
    for (Pair<RexNode, String> pair : project.getNamedProjects()) {
      bottomProgramBuilder.addProject(pair.left, pair.right);
    }
    int nLeftFields = project.getInput().getRowType().getFieldCount();
```

### DataFlowIssue
Argument `p.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/PushProjector.java`
#### Snippet
```java
            Pair.of(
                convertRefsAndExprs(
                    p.left,
                    projChild.getRowType().getFieldList(),
                    adjustments),
```

### DataFlowIssue
Argument `field.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectCalcMergeRule.java`
#### Snippet
```java
            rexBuilder);
    for (Pair<RexNode, String> field : project.getNamedProjects()) {
      progBuilder.addProject(field.left, field.right);
    }
    RexProgram topProgram = progBuilder.getProgram();
```

### DataFlowIssue
Argument `leftMapping` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoinOptimizeBushyRule.java`
#### Snippet
```java
        final Mappings.TargetMapping rightMapping = rightPair.right;
        final Mappings.TargetMapping mapping =
            Mappings.merge(leftMapping,
                Mappings.offsetTarget(rightMapping,
                    left.getRowType().getFieldCount()));
```

### DataFlowIssue
Argument `rightMapping` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoinOptimizeBushyRule.java`
#### Snippet
```java
        final Mappings.TargetMapping mapping =
            Mappings.merge(leftMapping,
                Mappings.offsetTarget(rightMapping,
                    left.getRowType().getFieldCount()));
        if (pw != null) {
```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoinOptimizeBushyRule.java`
#### Snippet
```java
            Mappings.merge(leftMapping,
                Mappings.offsetTarget(rightMapping,
                    left.getRowType().getFieldCount()));
        if (pw != null) {
          pw.println("left: " + leftMapping);
```

### DataFlowIssue
Argument `right` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoinOptimizeBushyRule.java`
#### Snippet
```java

        final RelNode join = relBuilder.push(left)
            .push(right)
            .join(JoinRelType.INNER, condition.accept(shuttle))
            .build();
```

### DataFlowIssue
Argument `top.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoinOptimizeBushyRule.java`
#### Snippet
```java

    final Pair<RelNode, Mappings.TargetMapping> top = Util.last(relNodes);
    relBuilder.push(top.left)
        .project(relBuilder.fields(top.right));
    call.transformTo(relBuilder.build());
```

### DataFlowIssue
Argument `top.right` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoinOptimizeBushyRule.java`
#### Snippet
```java
    final Pair<RelNode, Mappings.TargetMapping> top = Util.last(relNodes);
    relBuilder.push(top.left)
        .project(relBuilder.fields(top.right));
    call.transformTo(relBuilder.build());
  }
```

### DataFlowIssue
Argument `projectFilter.right` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/CalcSplitRule.java`
#### Snippet
```java
    final RelBuilder relBuilder = call.builder();
    relBuilder.push(calc.getInput());
    relBuilder.filter(projectFilter.right);
    relBuilder.project(projectFilter.left, calc.getRowType().getFieldNames());
    call.transformTo(relBuilder.build());
```

### DataFlowIssue
Argument `projectFilter.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/CalcSplitRule.java`
#### Snippet
```java
    relBuilder.push(calc.getInput());
    relBuilder.filter(projectFilter.right);
    relBuilder.project(projectFilter.left, calc.getRowType().getFieldNames());
    call.transformTo(relBuilder.build());
  }
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectToWindowRule.java`
#### Snippet
```java
          for (Pair<RexWindow, Set<Integer>> pair : windowToIndices) {
            // Check the first condition
            if (pair.left.equals(over.getWindow())) {
              // Check the second condition
              boolean hasDependency = false;
```

### DataFlowIssue
Dereference of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectToWindowRule.java`
#### Snippet
```java
              // Check the second condition
              boolean hasDependency = false;
              for (int ordinal : pair.right) {
                if (isDependent(graph, rank, ordinal, i)) {
                  hasDependency = true;
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/JoinProjectTransposeRule.java`
#### Snippet
```java
          // shift the references by the adjustment amount
          e =
              e.accept(
                  new RelOptUtil.RexInputConverter(rexBuilder, childFields,
                      joinChildrenFields, adjustments));
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/JoinProjectTransposeRule.java`
#### Snippet
```java
    final List<RelDataType> projTypes = new ArrayList<>();
    for (int i = 0; i < nProjExprs; i++) {
      projTypes.add(projects.get(i).left.getType());
    }
    RelDataType projRowType =
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
            rexBuilder.makeCall(SqlStdOperatorTable.CASE, filterRef,
                argRef.left,
                rexBuilder.makeNullLiteral(argRef.left.getType()));
        sourceOf.put(arg, projects.size());
        projects.add(Pair.of(condition, "i$" + argRef.right));
```

### DataFlowIssue
Method invocation `isDistinct` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
    final List<AggregateCall> distinctAggCalls = new ArrayList<>();
    for (Pair<AggregateCall, String> aggCall : aggregate.getNamedAggCalls()) {
      if (!aggCall.left.isDistinct()) {
        AggregateCall newAggCall =
            aggCall.left.adaptTo(aggregate.getInput(),
```

### DataFlowIssue
Argument `entry.getKey().left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
      final RexNode nodeZ = nodes.remove(nodes.size() - 1);
      for (Map.Entry<Pair<ImmutableBitSet, Integer>, Integer> entry : filters.entrySet()) {
        final long v = groupValue(fullGroupSet.asList(), entry.getKey().left);
        int distinctFilterArg = remap(fullGroupSet, entry.getKey().right);
        RexNode expr = relBuilder.equals(nodeZ, relBuilder.literal(v));
```

### DataFlowIssue
Unboxing of `entry.getKey().right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
      for (Map.Entry<Pair<ImmutableBitSet, Integer>, Integer> entry : filters.entrySet()) {
        final long v = groupValue(fullGroupSet.asList(), entry.getKey().left);
        int distinctFilterArg = remap(fullGroupSet, entry.getKey().right);
        RexNode expr = relBuilder.equals(nodeZ, relBuilder.literal(v));
        if (distinctFilterArg > -1) {
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
          Iterables.getOnlyElement(distinctCallArgLists2);
      final RelBuilder relBuilder = call.builder();
      convertMonopole(relBuilder, aggregate, pair.left, pair.right);
      call.transformTo(relBuilder.build());
      return;
```

### DataFlowIssue
Unboxing of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
          Iterables.getOnlyElement(distinctCallArgLists2);
      final RelBuilder relBuilder = call.builder();
      convertMonopole(relBuilder, aggregate, pair.left, pair.right);
      call.transformTo(relBuilder.build());
      return;
```

### DataFlowIssue
Argument `argList.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
    // set of operands.
    for (Pair<List<Integer>, Integer> argList : distinctCallArgLists) {
      doRewrite(relBuilder, aggregate, n++, argList.left, argList.right, refs);
    }
    // It is assumed doRewrite above replaces nulls in refs
```

### DataFlowIssue
Unboxing of `argList.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateExpandDistinctAggregatesRule.java`
#### Snippet
```java
    // set of operands.
    for (Pair<List<Integer>, Integer> argList : distinctCallArgLists) {
      doRewrite(relBuilder, aggregate, n++, argList.left, argList.right, refs);
    }
    // It is assumed doRewrite above replaces nulls in refs
```

### DataFlowIssue
Method invocation `getTable` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateStarTableRule.java`
#### Snippet
```java
    final RelMetadataQuery mq = call.getMetadataQuery();
    final double rowCount = aggregate.estimateRowCount(mq);
    final Table aggregateTable = tableEntry.getTable();
    final RelDataType aggregateTableRowType =
        aggregateTable.getRowType(cluster.getTypeFactory());
```

### DataFlowIssue
Argument `this.postJoinFilter` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/MultiJoin.java`
#### Snippet
```java
    RexNode joinFilter = shuttle.apply(this.joinFilter);
    List<@Nullable RexNode> outerJoinConditions = shuttle.apply(this.outerJoinConditions);
    RexNode postJoinFilter = shuttle.apply(this.postJoinFilter);

    if (joinFilter == this.joinFilter
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/rel/convert/ConverterRule.java`
#### Snippet
```java
    // another, and for them, the "out" field is a convenient short-cut.
    this.out = outTrait instanceof Convention ? (Convention) outTrait
        : castNonNull(null);
  }

```

### DataFlowIssue
Dereference of `((LogicalFilter) obj)` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/logical/LogicalFilter.java`
#### Snippet
```java
  @Override public boolean deepEquals(@Nullable Object obj) {
    return deepEquals0(obj)
        && variablesSet.equals(((LogicalFilter) obj).variablesSet);
  }

```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/SubQueryRemoveRule.java`
#### Snippet
```java
      final List<RexNode> conditions =
          Pair.zip(expressionOperands, fields).stream()
              .map(pair -> builder.equals(pair.left, pair.right))
              .collect(Collectors.toList());
      switch (logic) {
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/SubQueryRemoveRule.java`
#### Snippet
```java
      final List<RexNode> conditions =
          Pair.zip(expressionOperands, fields).stream()
              .map(pair -> builder.equals(pair.left, pair.right))
              .collect(Collectors.toList());
      switch (logic) {
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/SubQueryRemoveRule.java`
#### Snippet
```java
    final List<RexNode> conditions =
        Pair.zip(expressionOperands, builder.fields()).stream()
            .map(pair -> builder.equals(pair.left, RexUtil.shift(pair.right, refOffset)))
            .collect(Collectors.toList());
    switch (logic) {
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/SubQueryRemoveRule.java`
#### Snippet
```java
    final List<RexNode> conditions =
        Pair.zip(expressionOperands, builder.fields()).stream()
            .map(pair -> builder.equals(pair.left, RexUtil.shift(pair.right, refOffset)))
            .collect(Collectors.toList());
    switch (logic) {
```

### DataFlowIssue
Dereference of `((LogicalJoin) obj)` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/logical/LogicalJoin.java`
#### Snippet
```java
    }
    return deepEquals0(obj)
        && semiJoinDone == ((LogicalJoin) obj).semiJoinDone
        && systemFieldList.equals(((LogicalJoin) obj).systemFieldList);
  }
```

### DataFlowIssue
Argument `sourcePreds.right` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
    // Compute compensating predicates.
    final RexNode queryPred =
        RexUtil.swapColumnReferences(rexBuilder, sourcePreds.right,
            sourceEC.getEquivalenceClassesMap());
    final RexNode viewPred =
```

### DataFlowIssue
Argument `targetPreds.right` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
            sourceEC.getEquivalenceClassesMap());
    final RexNode viewPred =
        RexUtil.swapTableColumnReferences(rexBuilder, targetPreds.right,
            sourceToTargetTableMapping.inverse(),
            sourceEC.getEquivalenceClassesMap());
```

### DataFlowIssue
Argument `viewPreds.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
          // predicates to point to query tables and compute equivalence classes.
          final RexNode viewColumnsEquiPred =
              RexUtil.swapTableReferences(rexBuilder, viewPreds.left,
                  queryToViewTableMapping.inverse());
          final EquivalenceClasses queryBasedVEC = new EquivalenceClasses();
```

### DataFlowIssue
Method invocation `isAlwaysTrue` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
            RexNode compensationColumnsEquiPred = compensationPreds.left;
            RexNode otherCompensationPred = compensationPreds.right;
            assert !compensationColumnsEquiPred.isAlwaysTrue()
                || !otherCompensationPred.isAlwaysTrue();

```

### DataFlowIssue
Method invocation `isAlwaysTrue` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
            RexNode otherCompensationPred = compensationPreds.right;
            assert !compensationColumnsEquiPred.isAlwaysTrue()
                || !otherCompensationPred.isAlwaysTrue();

            // b. Generate union branch (query).
```

### DataFlowIssue
Argument `otherCompensationPred` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
            final RelNode unionInputQuery =
                rewriteQuery(call.builder(), rexBuilder, simplify, mq,
                    compensationColumnsEquiPred, otherCompensationPred,
                    topProject, node, queryToViewTableMapping, queryBasedVEC,
                    currQEC);
```

### DataFlowIssue
Method invocation `isAlwaysTrue` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java

            // a. Compute final compensation predicate.
            if (!compensationColumnsEquiPred.isAlwaysTrue()
                || !otherCompensationPred.isAlwaysTrue()) {
              // All columns required by compensating predicates must be contained
```

### DataFlowIssue
Method invocation `isAlwaysTrue` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
            // a. Compute final compensation predicate.
            if (!compensationColumnsEquiPred.isAlwaysTrue()
                || !otherCompensationPred.isAlwaysTrue()) {
              // All columns required by compensating predicates must be contained
              // in the view output (condition 2).
```

### DataFlowIssue
Method invocation `isAlwaysTrue` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
              }
              // For the rest, we use the query equivalence classes
              if (!otherCompensationPred.isAlwaysTrue()) {
                otherCompensationPred =
                    rewriteExpression(rexBuilder, mq, view, viewNode, viewExprs,
```

### DataFlowIssue
Argument `viewNode` might be null
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
                rewriteView(builder, rexBuilder, simplify, mq, matchModality,
                    false, viewWithFilter, topProject, node, topViewProject,
                    viewNode, queryToViewTableMapping, currQEC);
            if (result == null) {
              // Skip it
```

### DataFlowIssue
Argument `key.left` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/MetadataFactoryImpl.java`
#### Snippet
```java
        UnboundMetadata<@Nullable Metadata>>from(key -> {
          final UnboundMetadata<@Nullable Metadata> function =
              provider.apply(key.left, key.right);
          // Return DUMMY, not null, so the cache knows to not ask again.
          return function != null ? function : DUMMY;
```

### DataFlowIssue
Argument `key.right` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/MetadataFactoryImpl.java`
#### Snippet
```java
        UnboundMetadata<@Nullable Metadata>>from(key -> {
          final UnboundMetadata<@Nullable Metadata> function =
              provider.apply(key.left, key.right);
          // Return DUMMY, not null, so the cache knows to not ask again.
          return function != null ? function : DUMMY;
```

### DataFlowIssue
Argument `v` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdRowCount.java`
#### Snippet
```java
    for (RelNode r : subset.getRels()) {
      try {
        v = NumberUtil.min(v, mq.getRowCount(r));
      } catch (CyclicMetadataException e) {
        // ignore this rel; there will be other, non-cyclic ones
```

### DataFlowIssue
Method invocation `getParameterTypes` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/MetadataDef.java`
#### Snippet
```java
    for (Pair<Method, Method> pair : Pair.zip(methods, handlerMethods)) {
      final List<Class<?>> leftTypes =
          Arrays.asList(pair.left.getParameterTypes());
      final List<Class<?>> rightTypes =
          Arrays.asList(pair.right.getParameterTypes());
```

### DataFlowIssue
Method invocation `getParameterTypes` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/MetadataDef.java`
#### Snippet
```java
          Arrays.asList(pair.left.getParameterTypes());
      final List<Class<?>> rightTypes =
          Arrays.asList(pair.right.getParameterTypes());
      assert leftTypes.size() + 2 == rightTypes.size();
      assert RelNode.class.isAssignableFrom(rightTypes.get(0));
```

### DataFlowIssue
Method invocation `cardinality` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnUniqueness.java`
#### Snippet
```java
    // guaranteed that the result will be unique
    if (!ignoreNulls && rel.getJoinType() == JoinRelType.FULL
        && leftColumns.cardinality() > 0 && rightColumns.cardinality() > 0) {
      return false;
    }
```

### DataFlowIssue
Method invocation `cardinality` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnUniqueness.java`
#### Snippet
```java
    // guaranteed that the result will be unique
    if (!ignoreNulls && rel.getJoinType() == JoinRelType.FULL
        && leftColumns.cardinality() > 0 && rightColumns.cardinality() > 0) {
      return false;
    }
```

### DataFlowIssue
Argument `leftColumns` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnUniqueness.java`
#### Snippet
```java
    // right hand side, then the columns are unique if and only if they're
    // unique for their respective join inputs
    Boolean leftUnique = mq.areColumnsUnique(left, leftColumns, ignoreNulls);
    Boolean rightUnique = mq.areColumnsUnique(right, rightColumns, ignoreNulls);
    if ((leftColumns.cardinality() > 0)
```

### DataFlowIssue
Argument `rightColumns` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnUniqueness.java`
#### Snippet
```java
    // unique for their respective join inputs
    Boolean leftUnique = mq.areColumnsUnique(left, leftColumns, ignoreNulls);
    Boolean rightUnique = mq.areColumnsUnique(right, rightColumns, ignoreNulls);
    if ((leftColumns.cardinality() > 0)
        && (rightColumns.cardinality() > 0)) {
```

### DataFlowIssue
Method invocation `cardinality` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnUniqueness.java`
#### Snippet
```java
      final RelNode right = rel.getRight();

      if (leftColumns.cardinality() > 0
          && rightColumns.cardinality() > 0) {
        Boolean leftUnique =
```

### DataFlowIssue
Method invocation `cardinality` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnUniqueness.java`
#### Snippet
```java

      if (leftColumns.cardinality() > 0
          && rightColumns.cardinality() > 0) {
        Boolean leftUnique =
            mq.areColumnsUnique(left, leftColumns, ignoreNulls);
```

### DataFlowIssue
Argument `p.right` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdSize.java`
#### Snippet
```java
        : Pair.zip(averageColumnSizes, fields)) {
      if (p.left == null) {
        Double fieldValueSize = averageFieldValueSize(p.right);
        if (fieldValueSize == null) {
          return null;
```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdSize.java`
#### Snippet
```java
        mq.getAverageColumnSizesNotNull(rel.getInput());
    final ImmutableNullableList.Builder<@Nullable Double> sizes = ImmutableNullableList.builder();
    rel.getProgram().split().left.forEach(
        exp -> sizes.add(averageRexSize(exp, inputColumnSizes)));
    return sizes.build();
```

### DataFlowIssue
Argument `mq.getSelectivity(rel, predicate)` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdDistinctRowCount.java`
#### Snippet
```java
    if (uniq) {
      return NumberUtil.multiply(mq.getRowCount(rel),
          mq.getSelectivity(rel, predicate));
    }
    return null;
```

### DataFlowIssue
Argument `d` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdDistinctRowCount.java`
#### Snippet
```java
      try {
        Double d2 = mq.getDistinctRowCount(r2, groupKey, predicate);
        d = NumberUtil.min(d, d2);
      } catch (CyclicMetadataException e) {
        // Ignore this relational expression; there will be non-cyclic ones
```

### DataFlowIssue
Argument `d2` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdDistinctRowCount.java`
#### Snippet
```java
      try {
        Double d2 = mq.getDistinctRowCount(r2, groupKey, predicate);
        d = NumberUtil.min(d, d2);
      } catch (CyclicMetadataException e) {
        // Ignore this relational expression; there will be non-cyclic ones
```

### DataFlowIssue
Argument `mq.getDistinctRowCount(left, leftMask.build(), leftPred)` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
      distRowCount =
          NumberUtil.max(
              mq.getDistinctRowCount(left, leftMask.build(), leftPred),
              mq.getDistinctRowCount(right, rightMask.build(), rightPred));
    } else {
```

### DataFlowIssue
Argument `mq.getDistinctRowCount(right, rightMask.build(), rightPred)` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
          NumberUtil.max(
              mq.getDistinctRowCount(left, leftMask.build(), leftPred),
              mq.getDistinctRowCount(right, rightMask.build(), rightPred));
    } else {
      distRowCount =
```

### DataFlowIssue
Argument `mq.getDistinctRowCount(left, leftMask.build(), leftPred)` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
      distRowCount =
        multiply(
            mq.getDistinctRowCount(left, leftMask.build(), leftPred),
            mq.getDistinctRowCount(right, rightMask.build(), rightPred));
    }
```

### DataFlowIssue
Argument `mq.getDistinctRowCount(right, rightMask.build(), rightPred)` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
        multiply(
            mq.getDistinctRowCount(left, leftMask.build(), leftPred),
            mq.getDistinctRowCount(right, rightMask.build(), rightPred));
    }

```

### DataFlowIssue
Argument `cardOfProjExpr(mq, rel, call.getOperands().get(0))` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
        distinctRowCount =
            multiply(
                cardOfProjExpr(mq, rel, call.getOperands().get(0)),
                cardOfProjExpr(mq, rel, call.getOperands().get(1)));

```

### DataFlowIssue
Argument `cardOfProjExpr(mq, rel, call.getOperands().get(1))` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
            multiply(
                cardOfProjExpr(mq, rel, call.getOperands().get(0)),
                cardOfProjExpr(mq, rel, call.getOperands().get(1)));

        // TODO zfong 6/21/06 - Broadbase has code to handle date
```

### DataFlowIssue
Argument `distinctRowCount` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
      }

      return numDistinctVals(distinctRowCount, rowCount);
    }
  }
```

### DataFlowIssue
Argument `mq.getPopulationSize(left, leftMask.build())` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
    Double population =
        multiply(
            mq.getPopulationSize(left, leftMask.build()),
            mq.getPopulationSize(right, rightMask.build()));

```

### DataFlowIssue
Argument `mq.getPopulationSize(right, rightMask.build())` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
        multiply(
            mq.getPopulationSize(left, leftMask.build()),
            mq.getPopulationSize(right, rightMask.build()));

    return numDistinctVals(population, mq.getRowCount(join));
```

### DataFlowIssue
Unboxing of `((RexLiteral) operand).getValueAs(Double.class)` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
    RexNode operand = call.getOperands().get(0);
    @SuppressWarnings("unboxing.of.nullable")
    double doubleValue = ((RexLiteral) operand).getValueAs(Double.class);
    return doubleValue;
  }
```

### DataFlowIssue
Argument `mq.getSelectivity(child, condition)` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdUtil.java`
#### Snippet
```java
    @SuppressWarnings("unboxing.of.nullable")
    double result =
        multiply(mq.getRowCount(child), mq.getSelectivity(child, condition));
    return result;
  }
```

### DataFlowIssue
Method invocation `compound` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdCollation.java`
#### Snippet
```java
          ordering = comparator;
        } else {
          ordering = Util.last(pairs).right.compound(comparator);
        }
        pairs.add(Pair.of(fieldCollation, ordering));
```

### DataFlowIssue
@Nullable method 'project' always returns a non-null value
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdCollation.java`
#### Snippet
```java

  /** Helper method to determine a {@link Project}'s collation. */
  public static @Nullable List<RelCollation> project(RelMetadataQuery mq,
      RelNode input, List<? extends RexNode> projects) {
    final NavigableSet<RelCollation> collations = new TreeSet<>();
```

### DataFlowIssue
Argument `value.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java
        final SqlNodeList exprList = exprList(context, tuple);
        for (Pair<SqlNode, String> value : Pair.zip(exprList, fieldNames)) {
          values2.add(as(value.left, value.right));
        }
        list.add(
```

### DataFlowIssue
Argument `value.right` might be null
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java
        final SqlNodeList exprList = exprList(context, tuple);
        for (Pair<SqlNode, String> value : Pair.zip(exprList, fieldNames)) {
          values2.add(as(value.left, value.right));
        }
        list.add(
```

### DataFlowIssue
Argument `inputSet` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnOrigins.java`
#### Snippet
```java
      Set<RelColumnOrigin> inputSet =
          mq.getColumnOrigins(rel.getInput(), iInput);
      inputSet = createDerivedColumnOrigins(inputSet);
      if (inputSet != null) {
        set.addAll(inputSet);
```

### DataFlowIssue
Argument `set` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnOrigins.java`
#### Snippet
```java
      // nulls are generated due to outer join; that counts
      // as derivation
      set = createDerivedColumnOrigins(set);
    }
    return set;
```

### DataFlowIssue
Argument `result` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQuery.java`
#### Snippet
```java
      try {
        Double result = selectivityHandler.getSelectivity(rel, this, predicate);
        return RelMdUtil.validatePercentage(result);
      } catch (MetadataHandlerProvider.NoHandler e) {
        selectivityHandler = revise(BuiltInMetadata.Selectivity.Handler.class);
```

### DataFlowIssue
Argument `result` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQuery.java`
#### Snippet
```java
        Double result =
            percentageOriginalRowsHandler.getPercentageOriginalRows(rel, this);
        return RelMdUtil.validatePercentage(result);
      } catch (MetadataHandlerProvider.NoHandler e) {
        percentageOriginalRowsHandler =
```

### DataFlowIssue
Argument `result` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQuery.java`
#### Snippet
```java
            distinctRowCountHandler.getDistinctRowCount(rel, this, groupKey,
                predicate);
        return RelMdUtil.validateResult(result);
      } catch (MetadataHandlerProvider.NoHandler e) {
        distinctRowCountHandler = revise(BuiltInMetadata.DistinctRowCount.Handler.class);
```

### DataFlowIssue
Argument `result` might be null
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQuery.java`
#### Snippet
```java
        Double result =
            populationSizeHandler.getPopulationSize(rel, this, groupKey);
        return RelMdUtil.validateResult(result);
      } catch (MetadataHandlerProvider.NoHandler e) {
        populationSizeHandler = revise(BuiltInMetadata.PopulationSize.Handler.class);
```

### DataFlowIssue
Argument `value.left` might be null
in `core/src/main/java/org/apache/calcite/rel/externalize/RelXmlWriter.java`
#### Snippet
```java
      }
      xmlOutput.attribute(
          value.left,
          Objects.toString(value.right));
    }
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/rel/externalize/RelXmlWriter.java`
#### Snippet
```java
      }
      xmlOutput.beginBeginTag("Property");
      xmlOutput.attribute("name", pair.left);
      xmlOutput.endBeginTag("Property");
      xmlOutput.cdata(pair.right.toString());
```

### DataFlowIssue
Argument `value.left` might be null
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJsonWriter.java`
#### Snippet
```java
        continue;
      }
      put(map, value.left, value.right);
    }
    // omit 'inputs: ["3"]' if "3" is the preceding rel
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdExpressionLineage.java`
#### Snippet
```java
        calc.getProgram().split();
    for (int idx : inputFieldsUsed) {
      final RexNode inputExpr = calcProjectsAndFilter.getKey().get(idx);
      final Set<RexNode> originalExprs = mq.getExpressionLineage(input, inputExpr);
      if (originalExprs == null) {
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rel/externalize/RelWriterImpl.java`
#### Snippet
```java
  private boolean checkInputsPresentInExplain(RelNode node) {
    int i = 0;
    if (values.size() > 0 && values.get(0).left.equals("subset")) {
      ++i;
    }
```

### DataFlowIssue
Argument `field.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
    private void addOrderItem(List<SqlNode> orderByList,
        @Nullable RexProgram program, RexFieldCollation field) {
      SqlNode node = toSql(program, field.left);
      SqlNode nullDirectionNode = null;
      if (field.getNullDirection() != RelFieldCollation.NullDirection.UNSPECIFIED) {
```

### DataFlowIssue
Argument `rexWindowBound.getOffset()` might be null
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
          return SqlWindow.createUnboundedPreceding(POS);
        } else {
          SqlNode literal = toSql(null, rexWindowBound.getOffset());
          return SqlWindow.createPreceding(literal, POS);
        }
```

### DataFlowIssue
Argument `rexWindowBound.getOffset()` might be null
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
          return SqlWindow.createUnboundedFollowing(POS);
        } else {
          SqlNode literal = toSql(null, rexWindowBound.getOffset());
          return SqlWindow.createFollowing(literal, POS);
        }
```

### DataFlowIssue
Argument `rfc.left` might be null
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java

    private SqlNode toSql(@Nullable RexProgram program, RexFieldCollation rfc) {
      SqlNode node = toSql(program, rfc.left);
      switch (rfc.getDirection()) {
      case DESCENDING:
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexVisitorImpl.java`
#### Snippet
```java
    final RexWindow window = over.getWindow();
    for (RexFieldCollation orderKey : window.orderKeys) {
      orderKey.left.accept(this);
    }
    visitEach(window.partitionKeys);
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
    for (Pair<RexLocalRef, String> pair
        : Pair.zip(projectRefList, outputRowType.getFieldNames(), true)) {
      progBuilder.addProject(pair.left, pair.right);
    }
    RexProgram mergedProg = progBuilder.getProgram(normalize);
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
      final RexNode project;
      if (simplify != null) {
        project = simplify.simplify(pair.left.accept(expander));
      } else {
        project = pair.left;
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
        project = pair.left;
      }
      final String name = pair.right.getName();
      final RexLocalRef ref = (RexLocalRef) project.accept(shuttle);
      addProject(ref.getIndex(), name);
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexProgramBuilder.java`
#### Snippet
```java
      }
      final String name = pair.right.getName();
      final RexLocalRef ref = (RexLocalRef) project.accept(shuttle);
      addProject(ref.getIndex(), name);
    }
```

### DataFlowIssue
Immutable object is modified
in `core/src/main/java/org/apache/calcite/rex/RexSlot.java`
#### Snippet
```java
            final int size = size();
            if (index >= size) {
              addAll(fromTo(prefix, size, Math.max(index + 1, size * 2)));
            }
          }
```

### DataFlowIssue
@Nullable method 'getExpression' always returns a non-null value
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJsonReader.java`
#### Snippet
```java
      }

      @Override public @Nullable RexNode getExpression(String tag) {
        return relJson.toRex(this, jsonRel.get(tag));
      }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexExecutable.java`
#### Snippet
```java
        for (Pair<RexNode, @Nullable Object> value : Pair.zip(constExps, valueList)) {
          reducedValues.add(
              rexBuilder.makeLiteral(value.right, value.left.getType(), true));
        }
      }
```

### DataFlowIssue
Method invocation `contains` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexFieldCollation.java`
#### Snippet
```java

  public RelFieldCollation.NullDirection getNullDirection() {
    return right.contains(SqlKind.NULLS_LAST)
        ? RelFieldCollation.NullDirection.LAST
        : right.contains(SqlKind.NULLS_FIRST)
```

### DataFlowIssue
Method invocation `contains` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexFieldCollation.java`
#### Snippet
```java

  public RelFieldCollation.Direction getDirection() {
    return right.contains(SqlKind.DESCENDING)
        ? RelFieldCollation.Direction.DESCENDING
        : RelFieldCollation.Direction.ASCENDING;
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexFieldCollation.java`
#### Snippet
```java

  @Override public String toString() {
    final String s = left.toString();
    if (right.isEmpty()) {
      return s;
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexFieldCollation.java`
#### Snippet
```java
  @Override public String toString() {
    final String s = left.toString();
    if (right.isEmpty()) {
      return s;
    }
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexCall.java`
#### Snippet
```java
    RexCall rexCall = (RexCall) o;
    Pair<SqlOperator, List<RexNode>> y = rexCall.getNormalized();
    return x.left.equals(y.left)
        && x.right.equals(y.right)
        && type.equals(rexCall.type);
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexCall.java`
#### Snippet
```java
    Pair<SqlOperator, List<RexNode>> y = rexCall.getNormalized();
    return x.left.equals(y.left)
        && x.right.equals(y.right)
        && type.equals(rexCall.type);
  }
```

### DataFlowIssue
Argument `expr` might be null
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
    for (int i = 0; i < exprList.size(); i++) {
      T expr = exprList.get(i);
      T expr2 = (T) apply(expr); // Avoid NPE if expr is null
      if (expr != expr2) {
        ++changeCount;
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
        ImmutableList.builder();
    for (RexFieldCollation collation : collations) {
      RexNode clonedOperand = collation.left.accept(this);
      if ((clonedOperand != collation.left) && (update != null)) {
        update[0] = true;
```

### DataFlowIssue
Casting `literal` to `String` may produce `ClassCastException`
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java
        }
        if (type.getSqlTypeName() == SqlTypeName.SYMBOL) {
          literal = RelEnumTypes.toEnum((String) literal);
        }
        return rexBuilder.makeLiteral(literal, type);
```

### DataFlowIssue
Argument `collation.left` might be null
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java
  private Object toJson(RexFieldCollation collation) {
    final Map<String, @Nullable Object> map = jsonBuilder().map();
    map.put("expr", toJson(collation.left));
    map.put("direction", collation.getDirection().name());
    map.put("null-direction", collation.getNullDirection().name());
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexBiVisitorImpl.java`
#### Snippet
```java
    final RexWindow window = over.getWindow();
    for (RexFieldCollation orderKey : window.orderKeys) {
      orderKey.left.accept(this, arg);
    }
    for (RexNode partitionKey : window.partitionKeys) {
```

### DataFlowIssue
Casting `value` to `BigDecimal` may produce `ClassCastException`
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
        return (RexNode) value;
      }
      return makeExactLiteral((BigDecimal) value, type);
    case FLOAT:
    case REAL:
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java
        final RexNode e = pair.right instanceof RexLiteral
            ? (RexNode) pair.right
            : makeLiteral(pair.right, pair.left.getType(), allowCast);
        operands.add(e);
      }
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/SqlRowTypeNameSpec.java`
#### Snippet
```java
    for (Pair<SqlIdentifier, SqlDataTypeSpec> p : Pair.zip(this.fieldNames, this.fieldTypes)) {
      writer.sep(",", false);
      p.left.unparse(writer, 0, 0);
      p.right.unparse(writer, leftPrec, rightPrec);
      Boolean isNullable = p.right.getNullable();
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/SqlRowTypeNameSpec.java`
#### Snippet
```java
      writer.sep(",", false);
      p.left.unparse(writer, 0, 0);
      p.right.unparse(writer, leftPrec, rightPrec);
      Boolean isNullable = p.right.getNullable();
      if (isNullable != null && isNullable) {
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlExplain.java`
#### Snippet
```java
    switch (i) {
    case 0:
      explicandum = operand;
      break;
    case 1:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlExplain.java`
#### Snippet
```java
      break;
    case 1:
      detailLevel = (SqlLiteral) operand;
      break;
    case 2:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlExplain.java`
#### Snippet
```java
      break;
    case 2:
      depth = (SqlLiteral) operand;
      break;
    case 3:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlExplain.java`
#### Snippet
```java
      break;
    case 3:
      format = (SqlLiteral) operand;
      break;
    default:
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlExplain.java`
#### Snippet
```java
        @Override public SqlCall createCall(@Nullable SqlLiteral functionQualifier,
            SqlParserPos pos, @Nullable SqlNode... operands) {
          return new SqlExplain(pos, operands[0], (SqlLiteral) operands[1],
              (SqlLiteral) operands[2], (SqlLiteral) operands[3], 0);
        }
```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlExplain.java`
#### Snippet
```java
        @Override public SqlCall createCall(@Nullable SqlLiteral functionQualifier,
            SqlParserPos pos, @Nullable SqlNode... operands) {
          return new SqlExplain(pos, operands[0], (SqlLiteral) operands[1],
              (SqlLiteral) operands[2], (SqlLiteral) operands[3], 0);
        }
```

### DataFlowIssue
Argument `operands[2]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlExplain.java`
#### Snippet
```java
            SqlParserPos pos, @Nullable SqlNode... operands) {
          return new SqlExplain(pos, operands[0], (SqlLiteral) operands[1],
              (SqlLiteral) operands[2], (SqlLiteral) operands[3], 0);
        }
      };
```

### DataFlowIssue
Argument `operands[3]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlExplain.java`
#### Snippet
```java
            SqlParserPos pos, @Nullable SqlNode... operands) {
          return new SqlExplain(pos, operands[0], (SqlLiteral) operands[1],
              (SqlLiteral) operands[2], (SqlLiteral) operands[3], 0);
        }
      };
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
        writer.sep(",");
        SqlIdentifier id = (SqlIdentifier) pair.left;
        id.unparse(writer, opLeft, opRight);
        writer.keyword("=");
        SqlNode sourceExp = pair.right;
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
        writer.keyword("=");
        SqlNode sourceExp = pair.right;
        sourceExp.unparse(writer, opLeft, opRight);
      }
      writer.endList(setFrame);
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
      break;
    case 1:
      condition = operand;
      break;
    case 2:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlMerge.java`
#### Snippet
```java
      break;
    case 2:
      source = operand;
      break;
    case 3:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlPivot.java`
#### Snippet
```java
    switch (i) {
    case 0:
      query = operand;
      break;
    default:
```

### DataFlowIssue
Method invocation `getParserPosition` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/SqlCallBinding.java`
#### Snippet
```java
        if (j == call.getOperandList().size() - 1) {
          if (args != null) {
            throw SqlUtil.newContextException(args.right.getParserPosition(),
                RESOURCE.paramNotFoundInFunctionDidYouMean(args.right.getSimple(),
                    operator.getName(), args.left));
```

### DataFlowIssue
Argument `args.left` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlCallBinding.java`
#### Snippet
```java
            throw SqlUtil.newContextException(args.right.getParserPosition(),
                RESOURCE.paramNotFoundInFunctionDidYouMean(args.right.getSimple(),
                    operator.getName(), args.left));
          }
          if (operandMetadata.isFixedParameters()) {
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlWithItem.java`
#### Snippet
```java
      assert functionQualifier == null;
      assert operands.length == 3;
      return new SqlWithItem(pos, (SqlIdentifier) operands[0],
          (SqlNodeList) operands[1], operands[2]);
    }
```

### DataFlowIssue
Argument `operands[2]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlWithItem.java`
#### Snippet
```java
      assert operands.length == 3;
      return new SqlWithItem(pos, (SqlIdentifier) operands[0],
          (SqlNodeList) operands[1], operands[2]);
    }
  }
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlWithItem.java`
#### Snippet
```java
    switch (i) {
    case 0:
      name = (SqlIdentifier) operand;
      break;
    case 1:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlWithItem.java`
#### Snippet
```java
      break;
    case 2:
      query = operand;
      break;
    default:
```

### DataFlowIssue
Argument `a` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlNodeList.java`
#### Snippet
```java
  @SuppressWarnings("return.type.incompatible")
  @Override public <T> @Nullable T[] toArray(T @Nullable [] a) {
    return list.toArray(a);
  }

```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlDelete.java`
#### Snippet
```java
    switch (i) {
    case 0:
      targetTable = operand;
      break;
    case 1:
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
          break;
        default:
          unknownAs = RexUnknownAs.FALSE;
        }
        for (RexNode operand : call.operands) {
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
        : Pair.zip(lhsFields, rhsExps, true)) {
      RelDataTypeField lhsField = pair.left;
      RelDataType lhsType = lhsField.getType();
      final RexNode rhsExp = pair.right;
      RelDataType rhsType = rhsExp.getType();
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
      RelDataType lhsType = lhsField.getType();
      final RexNode rhsExp = pair.right;
      RelDataType rhsType = rhsExp.getType();
      if (lhsType.equals(rhsType)) {
        castExps.add(rhsExp);
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
      break;
    case 2:
      this.partitionList = (SqlNodeList) operand;
      break;
    case 3:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
      break;
    case 3:
      this.orderList = (SqlNodeList) operand;
      break;
    case 4:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
      break;
    case 4:
      this.isRows = (SqlLiteral) operand;
      break;
    case 5:
```

### DataFlowIssue
Argument `operands[2]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
          (SqlIdentifier) operands[0],
          (SqlIdentifier) operands[1],
          (SqlNodeList) operands[2],
          (SqlNodeList) operands[3],
          (SqlLiteral) operands[4],
```

### DataFlowIssue
Argument `operands[3]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
          (SqlIdentifier) operands[1],
          (SqlNodeList) operands[2],
          (SqlNodeList) operands[3],
          (SqlLiteral) operands[4],
          operands[5],
```

### DataFlowIssue
Argument `operands[4]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlWindow.java`
#### Snippet
```java
          (SqlNodeList) operands[2],
          (SqlNodeList) operands[3],
          (SqlLiteral) operands[4],
          operands[5],
          operands[6],
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlUnpivot.java`
#### Snippet
```java
    switch (i) {
    case 0:
      query = operand;
      break;
    default:
```

### DataFlowIssue
Method invocation `contains` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      switch (comparison) {
      case EQUALS:
        if (!r.contains(v0)) {
          // Range is empty, not satisfiable
          return rexBuilder.makeLiteral(false);
```

### DataFlowIssue
Dereference of `p.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
                ImmutableList.of(term)));
        // remove
        for (RexNode e : p.right) {
          replaceLast(terms, e, trueLiteral);
        }
```

### DataFlowIssue
Method invocation `hasUpperBound` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      case LESS_THAN: {
        int comparisonResult = 0;
        if (r.hasUpperBound()) {
          comparisonResult = v0.compareTo(r.upperEndpoint());
        }
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlDescribeTable.java`
#### Snippet
```java
        @Override public SqlCall createCall(@Nullable SqlLiteral functionQualifier,
            SqlParserPos pos, @Nullable SqlNode... operands) {
          return new SqlDescribeTable(pos, (SqlIdentifier) operands[0],
              (@Nullable SqlIdentifier) operands[1]);
        }
```

### DataFlowIssue
Method invocation `hasUpperBound` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      case LESS_THAN_OR_EQUAL: {
        int comparisonResult = -1;
        if (r.hasUpperBound()) {
          comparisonResult = v0.compareTo(r.upperEndpoint());
        }
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlDescribeTable.java`
#### Snippet
```java
    switch (i) {
    case 0:
      table = (SqlIdentifier) operand;
      break;
    case 1:
```

### DataFlowIssue
Method invocation `hasLowerBound` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      case GREATER_THAN: {
        int comparisonResult = 0;
        if (r.hasLowerBound()) {
          comparisonResult = v0.compareTo(r.lowerEndpoint());
        }
```

### DataFlowIssue
Method invocation `hasLowerBound` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      case GREATER_THAN_OR_EQUAL: {
        int comparisonResult = 1;
        if (r.hasLowerBound()) {
          comparisonResult = v0.compareTo(r.lowerEndpoint());
        }
```

### DataFlowIssue
Dereference of `p.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      if (removeUpperBound) {
        ImmutableList.Builder<RexNode> newBounds = ImmutableList.builder();
        for (RexNode e : p.right) {
          if (isUpperBound(e)) {
            replaceLast(terms, e, trueLiteral);
```

### DataFlowIssue
Dereference of `p.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      } else if (removeLowerBound) {
        ImmutableList.Builder<RexNode> newBounds = ImmutableList.builder();
        for (RexNode e : p.right) {
          if (isLowerBound(e)) {
            replaceLast(terms, e, trueLiteral);
```

### DataFlowIssue
Argument `select.getFrom()` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
    case SELECT:
      SqlSelect select = (SqlSelect) query;
      final SqlNode from = stripAs(select.getFrom());
      if (from != null && from.getKind() == SqlKind.VALUES) {
        // They wrote "VALUES (x, y)", but the validator has
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlOrderBy.java`
#### Snippet
```java
    @Override public SqlCall createCall(@Nullable SqlLiteral functionQualifier,
        SqlParserPos pos, @Nullable SqlNode... operands) {
      return new SqlOrderBy(pos, operands[0], (SqlNodeList) operands[1],
          operands[2], operands[3]);
    }
```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlOrderBy.java`
#### Snippet
```java
    @Override public SqlCall createCall(@Nullable SqlLiteral functionQualifier,
        SqlParserPos pos, @Nullable SqlNode... operands) {
      return new SqlOrderBy(pos, operands[0], (SqlNodeList) operands[1],
          operands[2], operands[3]);
    }
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlWith.java`
#### Snippet
```java
    switch (i) {
    case 0:
      withList = (SqlNodeList) operand;
      break;
    case 1:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlWith.java`
#### Snippet
```java
      break;
    case 1:
      body = operand;
      break;
    default:
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlWith.java`
#### Snippet
```java
    @Override public SqlCall createCall(@Nullable SqlLiteral functionQualifier,
        SqlParserPos pos, @Nullable SqlNode... operands) {
      return new SqlWith(pos, (SqlNodeList) operands[0], operands[1]);
    }

```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlWith.java`
#### Snippet
```java
    @Override public SqlCall createCall(@Nullable SqlLiteral functionQualifier,
        SqlParserPos pos, @Nullable SqlNode... operands) {
      return new SqlWith(pos, (SqlNodeList) operands[0], operands[1]);
    }

```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlInsert.java`
#### Snippet
```java
          return new SqlInsert(
              pos,
              (SqlNodeList) operands[0],
              operands[1],
              operands[2],
```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlInsert.java`
#### Snippet
```java
              pos,
              (SqlNodeList) operands[0],
              operands[1],
              operands[2],
              (SqlNodeList) operands[3]);
```

### DataFlowIssue
Argument `operands[2]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlInsert.java`
#### Snippet
```java
              (SqlNodeList) operands[0],
              operands[1],
              operands[2],
              (SqlNodeList) operands[3]);
        }
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlInsert.java`
#### Snippet
```java
    switch (i) {
    case 0:
      keywords = (SqlNodeList) operand;
      break;
    case 1:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlInsert.java`
#### Snippet
```java
      break;
    case 2:
      source = operand;
      break;
    case 3:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
    switch (i) {
    case 0:
      left = operand;
      break;
    case 1:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
      break;
    case 1:
      natural = (SqlLiteral) operand;
      break;
    case 2:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
      break;
    case 2:
      joinType = (SqlLiteral) operand;
      break;
    case 3:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
      break;
    case 3:
      right = operand;
      break;
    case 4:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
      break;
    case 4:
      conditionType = (SqlLiteral) operand;
      break;
    case 5:
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
        @Nullable SqlNode... operands) {
      assert functionQualifier == null;
      return new SqlJoin(pos, operands[0], (SqlLiteral) operands[1],
          (SqlLiteral) operands[2], operands[3], (SqlLiteral) operands[4],
          operands[5]);
```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
        @Nullable SqlNode... operands) {
      assert functionQualifier == null;
      return new SqlJoin(pos, operands[0], (SqlLiteral) operands[1],
          (SqlLiteral) operands[2], operands[3], (SqlLiteral) operands[4],
          operands[5]);
```

### DataFlowIssue
Argument `operands[2]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
      assert functionQualifier == null;
      return new SqlJoin(pos, operands[0], (SqlLiteral) operands[1],
          (SqlLiteral) operands[2], operands[3], (SqlLiteral) operands[4],
          operands[5]);
    }
```

### DataFlowIssue
Argument `operands[3]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
      assert functionQualifier == null;
      return new SqlJoin(pos, operands[0], (SqlLiteral) operands[1],
          (SqlLiteral) operands[2], operands[3], (SqlLiteral) operands[4],
          operands[5]);
    }
```

### DataFlowIssue
Argument `operands[4]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlJoin.java`
#### Snippet
```java
      assert functionQualifier == null;
      return new SqlJoin(pos, operands[0], (SqlLiteral) operands[1],
          (SqlLiteral) operands[2], operands[3], (SqlLiteral) operands[4],
          operands[5]);
    }
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
      writer.sep(",");
      SqlIdentifier id = (SqlIdentifier) pair.left;
      id.unparse(writer, opLeft, opRight);
      writer.keyword("=");
      SqlNode sourceExp = pair.right;
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
      writer.keyword("=");
      SqlNode sourceExp = pair.right;
      sourceExp.unparse(writer, opLeft, opRight);
    }
    writer.endList(setFrame);
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
      break;
    case 1:
      targetColumnList = (SqlNodeList) operand;
      break;
    case 2:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
      break;
    case 2:
      sourceExpressionList = (SqlNodeList) operand;
      break;
    case 3:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlUpdate.java`
#### Snippet
```java
      break;
    case 4:
      sourceExpressionList = (SqlNodeList) operand;
      break;
    case 5:
```

### DataFlowIssue
Argument `type.getCharset()` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlBinaryOperator.java`
#### Snippet
```java
                .createTypeWithCharsetAndCollation(
                    type,
                    type.getCharset(),
                    requireNonNull(resultCol, "resultCol"));
      }
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlDescribeSchema.java`
#### Snippet
```java
        @Override public SqlCall createCall(@Nullable SqlLiteral functionQualifier,
            SqlParserPos pos, @Nullable SqlNode... operands) {
          return new SqlDescribeSchema(pos, (SqlIdentifier) operands[0]);
        }
      };
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlDescribeSchema.java`
#### Snippet
```java
    switch (i) {
    case 0:
      schema = (SqlIdentifier) operand;
      break;
    default:
```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlSetOption.java`
#### Snippet
```java
          return new SqlSetOption(pos,
              scopeNode == null ? null : scopeNode.toString(),
              (SqlIdentifier) operands[1], operands[2]);
        }
      };
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlSnapshot.java`
#### Snippet
```java
      assert functionQualifier == null;
      assert operands.length == 2;
      return new SqlSnapshot(pos, operands[0], operands[1]);
    }

```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlSnapshot.java`
#### Snippet
```java
      assert functionQualifier == null;
      assert operands.length == 2;
      return new SqlSnapshot(pos, operands[0], operands[1]);
    }

```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateForeignSchema.java`
#### Snippet
```java
          writer.sep(",");
        }
        c.left.unparse(writer, 0, 0);
        c.right.unparse(writer, 0, 0);
      }
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateForeignSchema.java`
#### Snippet
```java
        }
        c.left.unparse(writer, 0, 0);
        c.right.unparse(writer, 0, 0);
      }
      writer.endList(frame);
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateFunction.java`
#### Snippet
```java
      for (Pair<SqlLiteral, SqlLiteral> using : pairs()) {
        writer.sep(",");
        using.left.unparse(writer, 0, 0); // FILE, URL or ARCHIVE
        using.right.unparse(writer, 0, 0); // e.g. 'file:foo/bar.jar'
      }
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/ddl/SqlCreateFunction.java`
#### Snippet
```java
        writer.sep(",");
        using.left.unparse(writer, 0, 0); // FILE, URL or ARCHIVE
        using.right.unparse(writer, 0, 0); // e.g. 'file:foo/bar.jar'
      }
      writer.endList(frame);
```

### DataFlowIssue
Argument `operands[0]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      assert operands.length == 12;

      return new SqlMatchRecognize(pos, operands[0], operands[1],
          (SqlLiteral) operands[2], (SqlLiteral) operands[3],
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      assert operands.length == 12;

      return new SqlMatchRecognize(pos, operands[0], operands[1],
          (SqlLiteral) operands[2], (SqlLiteral) operands[3],
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
```

### DataFlowIssue
Argument `operands[2]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java

      return new SqlMatchRecognize(pos, operands[0], operands[1],
          (SqlLiteral) operands[2], (SqlLiteral) operands[3],
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
          (SqlNodeList) operands[7], (SqlLiteral) operands[8],
```

### DataFlowIssue
Argument `operands[3]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java

      return new SqlMatchRecognize(pos, operands[0], operands[1],
          (SqlLiteral) operands[2], (SqlLiteral) operands[3],
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
          (SqlNodeList) operands[7], (SqlLiteral) operands[8],
```

### DataFlowIssue
Argument `operands[4]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      return new SqlMatchRecognize(pos, operands[0], operands[1],
          (SqlLiteral) operands[2], (SqlLiteral) operands[3],
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
          (SqlNodeList) operands[7], (SqlLiteral) operands[8],
          (SqlNodeList) operands[9], (SqlNodeList) operands[10], (SqlLiteral) operands[11]);
```

### DataFlowIssue
Argument `operands[5]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      return new SqlMatchRecognize(pos, operands[0], operands[1],
          (SqlLiteral) operands[2], (SqlLiteral) operands[3],
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
          (SqlNodeList) operands[7], (SqlLiteral) operands[8],
          (SqlNodeList) operands[9], (SqlNodeList) operands[10], (SqlLiteral) operands[11]);
```

### DataFlowIssue
Argument `operands[7]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
          (SqlLiteral) operands[2], (SqlLiteral) operands[3],
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
          (SqlNodeList) operands[7], (SqlLiteral) operands[8],
          (SqlNodeList) operands[9], (SqlNodeList) operands[10], (SqlLiteral) operands[11]);
    }
```

### DataFlowIssue
Argument `operands[9]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
          (SqlNodeList) operands[7], (SqlLiteral) operands[8],
          (SqlNodeList) operands[9], (SqlNodeList) operands[10], (SqlLiteral) operands[11]);
    }

```

### DataFlowIssue
Argument `operands[10]` might be null
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
          (SqlNodeList) operands[4], (SqlNodeList) operands[5], operands[6],
          (SqlNodeList) operands[7], (SqlLiteral) operands[8],
          (SqlNodeList) operands[9], (SqlNodeList) operands[10], (SqlLiteral) operands[11]);
    }

```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      break;
    case OPERAND_PATTERN:
      pattern = operand;
      break;
    case OPERAND_STRICT_START:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      break;
    case OPERAND_STRICT_START:
      strictStart = (SqlLiteral) operand;
      break;
    case OPERAND_STRICT_END:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      break;
    case OPERAND_STRICT_END:
      strictEnd = (SqlLiteral) operand;
      break;
    case OPERAND_PATTERN_DEFINES:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      break;
    case OPERAND_SUBSET:
      subsetList = (SqlNodeList) operand;
      break;
    case OPERAND_ROWS_PER_MATCH:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      break;
    case OPERAND_PARTITION_BY:
      partitionList = (SqlNodeList) operand;
      break;
    case OPERAND_ORDER_BY:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/SqlMatchRecognize.java`
#### Snippet
```java
      break;
    case OPERAND_ORDER_BY:
      orderList = (SqlNodeList) operand;
      break;
    case OPERAND_INTERVAL:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCase.java`
#### Snippet
```java
      break;
    case 1:
      whenList = (SqlNodeList) operand;
      break;
    case 2:
```

### DataFlowIssue
Expression `operand` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCase.java`
#### Snippet
```java
      break;
    case 2:
      thenList = (SqlNodeList) operand;
      break;
    case 3:
```

### DataFlowIssue
Argument `castNonNull(firstType)` might be null
in `core/src/main/java/org/apache/calcite/sql/fun/SqlLiteralChainOperator.java`
#### Snippet
```java
      if (operand.i == 0) {
        firstType = type;
      } else if (!SqlTypeUtil.sameNamedType(castNonNull(firstType), type)) {
        return false;
      }
```

### DataFlowIssue
Argument `operands[1]` might be null
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCaseOperator.java`
#### Snippet
```java
    assert functionQualifier == null;
    assert operands.length == 4;
    return new SqlCase(pos, operands[0], (SqlNodeList) operands[1],
        (SqlNodeList) operands[2], operands[3]);
  }
```

### DataFlowIssue
Argument `operands[2]` might be null
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCaseOperator.java`
#### Snippet
```java
    assert operands.length == 4;
    return new SqlCase(pos, operands[0], (SqlNodeList) operands[1],
        (SqlNodeList) operands[2], operands[3]);
  }

```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCaseOperator.java`
#### Snippet
```java
    for (Pair<SqlNode, SqlNode> pair : Pair.zip(kase.whenList, kase.thenList)) {
      writer.sep("WHEN");
      pair.left.unparse(writer, 0, 0);
      writer.sep("THEN");
      pair.right.unparse(writer, 0, 0);
```

### DataFlowIssue
Method invocation `unparse` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/fun/SqlCaseOperator.java`
#### Snippet
```java
      pair.left.unparse(writer, 0, 0);
      writer.sep("THEN");
      pair.right.unparse(writer, 0, 0);
    }

```

### DataFlowIssue
Immutable object is modified
in `core/src/main/java/org/apache/calcite/sql/type/SameOperandTypeExceptLastOperandChecker.java`
#### Snippet
```java
    } else {
      List<String> types = Collections.nCopies(nOperands - 1, typeName);
      types.add(lastOperandTypeName);
      return SqlUtil.getAliasedSignature(op, opName, types);
    }
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/sql/type/AssignableOperandTypeChecker.java`
#### Snippet
```java
    final List<SqlNode> operands = callBinding.getCall().getOperandList();
    for (Pair<RelDataType, SqlNode> pair : Pair.zip(paramTypes, operands)) {
      RelDataType argType = SqlTypeUtil.deriveType(callBinding, pair.right);
      if (!SqlTypeUtil.canAssignFrom(pair.left, argType)) {
        // TODO: add in unresolved function type cast.
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/sql/type/AssignableOperandTypeChecker.java`
#### Snippet
```java
    for (Pair<RelDataType, SqlNode> pair : Pair.zip(paramTypes, operands)) {
      RelDataType argType = SqlTypeUtil.deriveType(callBinding, pair.right);
      if (!SqlTypeUtil.canAssignFrom(pair.left, argType)) {
        // TODO: add in unresolved function type cast.
        if (throwOnFailure) {
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      for (Pair<RelDataTypeField, RelDataTypeField> pair
          : Pair.zip(type1.getFieldList(), type2.getFieldList())) {
        if (!isComparable(pair.left.getType(), pair.right.getType())) {
          return false;
        }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      for (Pair<RelDataTypeField, RelDataTypeField> pair
          : Pair.zip(type1.getFieldList(), type2.getFieldList())) {
        if (!isComparable(pair.left.getType(), pair.right.getType())) {
          return false;
        }
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
        : Pair.zip(type1.getFieldList(), type2.getFieldList())) {
      if (nameMatcher != null
          && !nameMatcher.matches(pair.left.getName(), pair.right.getName())) {
        return false;
      }
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
        : Pair.zip(type1.getFieldList(), type2.getFieldList())) {
      if (nameMatcher != null
          && !nameMatcher.matches(pair.left.getName(), pair.right.getName())) {
        return false;
      }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
        return false;
      }
      if (!equalSansNullability(factory, pair.left.getType(), pair.right.getType())) {
        return false;
      }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
        return false;
      }
      if (!equalSansNullability(factory, pair.left.getType(), pair.right.getType())) {
        return false;
      }
```

### DataFlowIssue
Argument `adjacent.left` might be null
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    }
    for (Pair<RelDataType, RelDataType> adjacent : Pair.adjacents(typeList)) {
      if (!isSameFamily(adjacent.left, adjacent.right)) {
        return false;
      }
```

### DataFlowIssue
Argument `adjacent.right` might be null
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    }
    for (Pair<RelDataType, RelDataType> adjacent : Pair.adjacents(typeList)) {
      if (!isSameFamily(adjacent.left, adjacent.right)) {
        return false;
      }
```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
  public static SqlDataTypeSpec convertTypeToSpec(RelDataType type) {
    // TODO jvs 28-Dec-2004:  collation
    String charSetName = inCharFamily(type) ? type.getCharset().name() : null;
    return convertTypeToSpec(type, charSetName, -1, -1);
  }
```

### DataFlowIssue
Argument `t0` might be null
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      RelDataType t1 = pair.right;

      if (!inCharFamily(t0) || !inCharFamily(t1)) {
        return false;
      }
```

### DataFlowIssue
Argument `t1` might be null
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      RelDataType t1 = pair.right;

      if (!inCharFamily(t0) || !inCharFamily(t1)) {
        return false;
      }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      for (Pair<RelDataTypeField, RelDataTypeField> pair
          : Pair.zip(type1.getFieldList(), type2.getFieldList())) {
        if (!isSameFamily(pair.left.getType(), pair.right.getType())) {
          return false;
        }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      for (Pair<RelDataTypeField, RelDataTypeField> pair
          : Pair.zip(type1.getFieldList(), type2.getFieldList())) {
        if (!isSameFamily(pair.left.getType(), pair.right.getType())) {
          return false;
        }
```

### DataFlowIssue
Immutable object is modified
in `core/src/main/java/org/apache/calcite/sql/util/ChainedSqlOperatorTable.java`
#### Snippet
```java
  public void add(SqlOperatorTable table) {
    if (!tableList.contains(table)) {
      tableList.add(table);
    }
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/sql/fun/SqlStdOperatorTable.java`
#### Snippet
```java
   */
  public static final SqlAggFunction HISTOGRAM_AGG =
      new SqlHistogramAggFunction(castNonNull(null));

  /**
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/sql/fun/SqlStdOperatorTable.java`
#### Snippet
```java
   */
  public static final SqlAggFunction SINGLE_VALUE =
      new SqlSingleValueAggFunction(castNonNull(null));

  /**
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/sql/fun/SqlStdOperatorTable.java`
#### Snippet
```java
   * <code>SUM</code> aggregate function.
   */
  public static final SqlAggFunction SUM = new SqlSumAggFunction(castNonNull(null));

  /**
```

### DataFlowIssue
Expression `reservedWordsList` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
  public List<String> getReservedAndKeyWords() {
    ensureReservedAndKeyWords();
    return reservedWordsList;
  }

```

### DataFlowIssue
Expression `reservedWordsSet` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
  private Set<String> getReservedAndKeyWordsSet() {
    ensureReservedAndKeyWords();
    return reservedWordsSet;
  }

```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
    case SELECT:
      extraIndent = indentation;
      newlineAfterOpen = false;
      newlineBeforeSep = config.clauseStartsLine(); // newline before FROM, WHERE etc.
      newlineAfterSep = false;
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
      newlineAfterOpen = false;
      newlineBeforeSep = config.clauseStartsLine(); // newline before FROM, WHERE etc.
      newlineAfterSep = false;
      sepIndent = 0; // all clauses appear below SELECT
      break;
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

    case SETOP:
      extraIndent = 0;
      newlineAfterOpen = false;
      newlineBeforeSep = config.clauseStartsLine(); // newline before UNION, EXCEPT
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
    case SETOP:
      extraIndent = 0;
      newlineAfterOpen = false;
      newlineBeforeSep = config.clauseStartsLine(); // newline before UNION, EXCEPT
      newlineAfterSep = config.clauseStartsLine(); // newline after UNION, EXCEPT
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
          && (fold == SqlWriterConfig.LineFolding.TALL
              || fold == SqlWriterConfig.LineFolding.STEP);
      newlineBeforeSep = false;
      newlineAfterSep = newline;
      if (config.leadingComma() && newline) {
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
      newlineBeforeSep = newline;
      sepIndent = 0;
      newlineAfterSep = false;
      break;

```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
    case OFFSET:
    case FETCH:
      newlineAfterOpen = false;
      newlineBeforeSep = true;
      sepIndent = 0;
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
      newlineBeforeSep = true;
      sepIndent = 0;
      newlineAfterSep = false;
      break;

```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java
      extraIndent = indentation;
      newlineAfterOpen = newline;
      newlineBeforeSep = false;
      sepIndent = 0;
      newlineAfterSep = newline;
```

### DataFlowIssue
Expression `pair.right` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/sql/validate/OverScope.java`
#### Snippet
```java
      for (Pair<SqlNode, SqlMonotonicity> pair : monotonicExprs) {
        if (expr.equalsDeep(pair.left, Litmus.IGNORE)) {
          return pair.right;
        }
      }
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
    switch (entries.size()) {
    case 1:
      if (!entries.get(0).getValue().isEmpty()) {
        return null;
      }
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
      final SqlDataTypeSpec type = pair.right;
      extendedFields.add(
          new RelDataTypeFieldImpl(identifier.toString(),
              extendedFieldOffset++,
              type.deriveType(requireNonNull(validator, "validator"))));
```

### DataFlowIssue
Method invocation `deriveType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
          new RelDataTypeFieldImpl(identifier.toString(),
              extendedFieldOffset++,
              type.deriveType(requireNonNull(validator, "validator"))));
    }
    return extendedFields.build();
```

### DataFlowIssue
Argument `split(node).left` might be null
in `core/src/main/java/org/apache/calcite/sql/validate/IdentifierNamespace.java`
#### Snippet
```java
  IdentifierNamespace(SqlValidatorImpl validator, SqlNode node,
      @Nullable SqlNode enclosingNode, SqlValidatorScope parentScope) {
    this(validator, split(node).left, split(node).right, enclosingNode,
        parentScope);
  }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingScope.java`
#### Snippet
```java
            final List<String> remainder = entry.getValue();
            final SqlValidatorNamespace ns2 =
                new FieldNamespace(validator, field.getType());
            final Step path2 =
                path.plus(rowType, field.getIndex(), field.getName(),
```

### DataFlowIssue
Argument `remainder` might be null
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingScope.java`
#### Snippet
```java
                path.plus(rowType, field.getIndex(), field.getName(),
                    StructKind.FULLY_QUALIFIED);
            resolveInNamespace(ns2, nullable, remainder, nameMatcher, path2,
                resolved);
          }
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
    }
    for (Pair<RelDataType, SqlTypeFamily> pair : Pair.zip(types, families)) {
      RelDataType implicitType = implicitCast(pair.left, pair.right);
      if (null == implicitType) {
        return false;
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
    }
    for (Pair<RelDataType, SqlTypeFamily> pair : Pair.zip(types, families)) {
      RelDataType implicitType = implicitCast(pair.left, pair.right);
      if (null == implicitType) {
        return false;
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
        for (Pair<RelDataTypeField, RelDataTypeField> pair
            : Pair.zip(type1.getFieldList(), type2.getFieldList())) {
          RelDataType leftType = pair.left.getType();
          RelDataType rightType = pair.right.getType();
          RelDataType dataType = getTightestCommonTypeOrThrow(leftType, rightType);
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/validate/implicit/AbstractTypeCoercion.java`
#### Snippet
```java
            : Pair.zip(type1.getFieldList(), type2.getFieldList())) {
          RelDataType leftType = pair.left.getType();
          RelDataType rightType = pair.right.getType();
          RelDataType dataType = getTightestCommonTypeOrThrow(leftType, rightType);
          boolean isNullable = leftType.isNullable() || rightType.isNullable();
```

### DataFlowIssue
Expression `castNonNull(castNonNull(THREAD_CONTEXT_STACK.get()).peek())` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java

    public static Context peek() {
      return castNonNull(castNonNull(THREAD_CONTEXT_STACK.get()).peek());
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/jdbc/CalcitePrepare.java`
#### Snippet
```java
      this(sql, parameterList, internalParameters, rowType, columns,
          cursorFactory, rootSchema, collationList, maxRowCount, bindable,
          castNonNull(null));
    }

```

### DataFlowIssue
Argument `current.left` might be null
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterializations.java`
#### Snippet
```java
      for (int i = 0; i < count; i++) {
        Pair<RelNode, List<RelOptMaterialization>> current = applied.get(i);
        List<RelNode> sub = substitute(current.left, m, materializationRules);
        if (!sub.isEmpty()) {
          ImmutableList.Builder<RelOptMaterialization> builder =
```

### DataFlowIssue
Argument `current.right` might be null
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterializations.java`
#### Snippet
```java
          ImmutableList.Builder<RelOptMaterialization> builder =
              ImmutableList.builder();
          builder.addAll(current.right);
          builder.add(m);
          List<RelOptMaterialization> uses = builder.build();
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
      Collections.sort(list,
          (left, right) -> {
            int res = right.getValue().left.compareTo(left.getValue().left);
            if (res == 0) {
              res = right.getValue().right.compareTo(left.getValue().right);
```

### DataFlowIssue
Argument `left.getValue().left` might be null
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
      Collections.sort(list,
          (left, right) -> {
            int res = right.getValue().left.compareTo(left.getValue().left);
            if (res == 0) {
              res = right.getValue().right.compareTo(left.getValue().right);
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
            int res = right.getValue().left.compareTo(left.getValue().left);
            if (res == 0) {
              res = right.getValue().right.compareTo(left.getValue().right);
            }
            if (res == 0) {
```

### DataFlowIssue
Argument `left.getValue().right` might be null
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
            int res = right.getValue().left.compareTo(left.getValue().left);
            if (res == 0) {
              res = right.getValue().right.compareTo(left.getValue().right);
            }
            if (res == 0) {
```

### DataFlowIssue
Unboxing of `entry.getValue().left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
                usFormat.format(entry.getValue().left),
                usFormat.format(entry.getValue().right)));
        totalAttempts += entry.getValue().left;
        totalTime += entry.getValue().right;
      }
```

### DataFlowIssue
Unboxing of `entry.getValue().right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
                usFormat.format(entry.getValue().right)));
        totalAttempts += entry.getValue().left;
        totalTime += entry.getValue().right;
      }
      sb.append(
```

### DataFlowIssue
Unboxing of `p.left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
        if (ruleAttempts.containsKey(rule)) {
          Pair<Long, Long> p = ruleAttempts.get(rule);
          ruleAttempts.put(rule, Pair.of(p.left + 1, p.right + elapsed));
        } else {
          ruleAttempts.put(rule, Pair.of(1L,  elapsed));
```

### DataFlowIssue
Unboxing of `p.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/AbstractRelOptPlanner.java`
#### Snippet
```java
        if (ruleAttempts.containsKey(rule)) {
          Pair<Long, Long> p = ruleAttempts.get(rule);
          ruleAttempts.put(rule, Pair.of(p.left + 1, p.right + elapsed));
        } else {
          ruleAttempts.put(rule, Pair.of(1L,  elapsed));
```

### DataFlowIssue
Unboxing of `value.getKey()` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/VisitorDataContext.java`
#### Snippet
```java
        return null;
      }
      int index = value.getKey();
      values[index] = value.getValue();
    }
```

### DataFlowIssue
Unboxing of `value.getKey()` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/VisitorDataContext.java`
#### Snippet
```java
    if (value != null) {
      final @Nullable Object[] values = new Object[size];
      int index = value.getKey();
      values[index] = value.getValue();
      return new VisitorDataContext(values);
```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RexImplicationChecker.java`
#### Snippet
```java
      final int firstLen = firstUsageList.size();

      final SqlKind fKind = firstUsageList.get(0).getKey().getKind();
      final SqlKind sKind = secondUsageList.get(0).getKey().getKind();
      final SqlKind fKind2 =
```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RexImplicationChecker.java`
#### Snippet
```java

      final SqlKind fKind = firstUsageList.get(0).getKey().getKind();
      final SqlKind sKind = secondUsageList.get(0).getKey().getKind();
      final SqlKind fKind2 =
          firstLen == 2 ? firstUsageList.get(1).getKey().getKind() : null;
```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RexImplicationChecker.java`
#### Snippet
```java
      final SqlKind sKind = secondUsageList.get(0).getKey().getKind();
      final SqlKind fKind2 =
          firstLen == 2 ? firstUsageList.get(1).getKey().getKind() : null;
      final SqlKind sKind2 =
          secondLen == 2 ? secondUsageList.get(1).getKey().getKind() : null;
```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RexImplicationChecker.java`
#### Snippet
```java
          firstLen == 2 ? firstUsageList.get(1).getKey().getKind() : null;
      final SqlKind sKind2 =
          secondLen == 2 ? secondUsageList.get(1).getKey().getKind() : null;

      // Note: arguments to isEquivalentOp are never null, however checker-framework's
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterialization.java`
#### Snippet
```java
    }
    final RelNode rel2 =
        program.run(castNonNull(null), rel, castNonNull(null),
            ImmutableList.of(), ImmutableList.of());
    if (CalciteSystemProperty.DEBUG.value()) {
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterialization.java`
#### Snippet
```java
    }
    final RelNode rel2 =
        program.run(castNonNull(null), rel, castNonNull(null),
            ImmutableList.of(), ImmutableList.of());
    if (CalciteSystemProperty.DEBUG.value()) {
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterialization.java`
#### Snippet
```java
        false,
        DefaultRelMetadataProvider.INSTANCE);
    return program.run(castNonNull(null), rel2, castNonNull(null),
        ImmutableList.of(),
        ImmutableList.of());
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/plan/RelOptMaterialization.java`
#### Snippet
```java
        false,
        DefaultRelMetadataProvider.INSTANCE);
    return program.run(castNonNull(null), rel2, castNonNull(null),
        ImmutableList.of(),
        ImmutableList.of());
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/plan/hep/HepInstruction.java`
#### Snippet
```java

    static PrepareContext create(HepPlanner planner) {
      return new PrepareContext(planner, castNonNull(null), castNonNull(null));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/plan/hep/HepInstruction.java`
#### Snippet
```java

    static PrepareContext create(HepPlanner planner) {
      return new PrepareContext(planner, castNonNull(null), castNonNull(null));
    }

```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/plan/hep/HepPlanner.java`
#### Snippet
```java
        boolean match =
            matchOperands(
                pair.right,
                pair.left.getCurrentRel(),
                bindings,
```

### DataFlowIssue
Method invocation `getCurrentRel` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/hep/HepPlanner.java`
#### Snippet
```java
            matchOperands(
                pair.right,
                pair.left.getCurrentRel(),
                bindings,
                nodeChildren);
```

### DataFlowIssue
Method invocation `satisfies` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSubset.java`
#### Snippet
```java
      return Pair.zip(original, desired)
          .stream()
          .filter(p -> !p.left.satisfies(p.right))
          .map(p -> p.left.getTraitDef().getSimpleName() + ": " + p.left + " -> " + p.right)
          .collect(Collectors.joining(", ", "[", "]"));
```

### DataFlowIssue
Argument `p.right` might be null
in `core/src/main/java/org/apache/calcite/plan/volcano/RelSubset.java`
#### Snippet
```java
      return Pair.zip(original, desired)
          .stream()
          .filter(p -> !p.left.satisfies(p.right))
          .map(p -> p.left.getTraitDef().getSimpleName() + ": " + p.left + " -> " + p.right)
          .collect(Collectors.joining(", ", "[", "]"));
```

### DataFlowIssue
Method invocation `isAlwaysTrue` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      final RexBuilder rexBuilder = call.getCluster().getRexBuilder();

      if (!query.equals(targetInput) || !targetCond.isAlwaysTrue()) {
        return null;
      }
```

### DataFlowIssue
Argument `targetProjs` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        return null;
      }
      final RexShuttle shuttle = getRexShuttle(targetProjs);
      final List<RexNode> compenProjs;
      try {
```

### DataFlowIssue
Argument `qInput1Cond` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      if (splitted != null && splitted.isAlwaysTrue()) {
        final RexNode qInput1CondShifted =
            RexUtil.shift(qInput1Cond, fieldCnt(qInput0.getInput()));
        final RexNode compenCond =
            RexUtil.composeConjunction(rexBuilder,
```

### DataFlowIssue
Argument `qInput0Cond` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        final RexNode compenCond =
            RexUtil.composeConjunction(rexBuilder,
                ImmutableList.of(qInput0Cond, qInput1CondShifted));

        final List<RexNode> compenProjs = new ArrayList<>();
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        for (int i = 0; i < query.rowType.getFieldCount(); i++) {
          if (i < fieldCnt(qInput0)) {
            compenProjs.add(qInput0Projs.get(i));
          } else {
            RexNode shifted =
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
          } else {
            RexNode shifted =
                RexUtil.shift(qInput1Projs.get(i - fieldCnt(qInput0)),
                    fieldCnt(qInput0.getInput()));
            compenProjs.add(shifted);
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
            return inputRef;
          } else {
            final int newIdx = ((RexInputRef) qInput1Projs.get(idx - fieldCnt(qInput0)))
                .getIndex() + fieldCnt(qInput0);
            return new RexInputRef(newIdx, inputRef.getType());
```

### DataFlowIssue
Method invocation `matches` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      }
      for (Pair<Operand, MutableRel> pair : Pair.zip(operands, rels)) {
        if (!pair.left.matches(visitor, pair.right)) {
          return false;
        }
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      }
      for (Pair<Operand, MutableRel> pair : Pair.zip(operands, rels)) {
        if (!pair.left.matches(visitor, pair.right)) {
          return false;
        }
```

### DataFlowIssue
@Nullable method 'matchRecurse' always returns a non-null value
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
  }

  private @Nullable UnifyResult matchRecurse(MutableRel target) {
    assert false; // not called
    final List<MutableRel> targetInputs = target.getInputs();
```

### DataFlowIssue
Argument `qInputProjs` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java

      final Mappings.TargetMapping mapping =
          Project.getMapping(fieldCnt(qInput.getInput()), qInputProjs);
      if (mapping == null) {
        return null;
```

### DataFlowIssue
Method invocation `isAlwaysTrue` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      }

      if (!qInputCond.isAlwaysTrue()) {
        try {
          // Fail the matching when filtering condition references
```

### DataFlowIssue
Argument `calc.program.getCondition()` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
  public static Pair<RexNode, List<RexNode>> explainCalc(MutableCalc calc) {
    final RexShuttle shuttle = getExpandShuttle(calc.program);
    final RexNode condition = shuttle.apply(calc.program.getCondition());
    final List<RexNode> projects = new ArrayList<>();
    for (RexNode rex: shuttle.apply(calc.program.getProjectList())) {
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
          final int idx = inputRef.getIndex();
          if (idx < fieldCnt(qInput0)) {
            final int newIdx = ((RexInputRef) qInput0Projs.get(idx)).getIndex();
            return new RexInputRef(newIdx, inputRef.getType());
          } else {
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
            return new RexInputRef(newIdx, inputRef.getType());
          } else {
            final int newIdx = ((RexInputRef) qInput1Projs.get(idx - fieldCnt(qInput0)))
                .getIndex() + fieldCnt(qInput0.getInput());
            return new RexInputRef(newIdx, inputRef.getType());
```

### DataFlowIssue
Argument `queryInputExplained0.left` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        // Matching fails when filtering conditions are not equal or projects are not equal.
        RexNode residue =
            splitFilter(call.getSimplify(), queryInputExplained0.left,
                queryInputExplained.left);
        if (residue == null || !residue.isAlwaysTrue()) {
```

### DataFlowIssue
Argument `queryInputExplained.left` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        RexNode residue =
            splitFilter(call.getSimplify(), queryInputExplained0.left,
                queryInputExplained.left);
        if (residue == null || !residue.isAlwaysTrue()) {
          return null;
```

### DataFlowIssue
Argument `queryInputExplained0.right` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        }
        for (Pair<RexNode, RexNode> pair : Pair.zip(
            queryInputExplained0.right, queryInputExplained.right)) {
          if (!pair.left.equals(pair.right)) {
            return null;
```

### DataFlowIssue
Argument `queryInputExplained.right` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        }
        for (Pair<RexNode, RexNode> pair : Pair.zip(
            queryInputExplained0.right, queryInputExplained.right)) {
          if (!pair.left.equals(pair.right)) {
            return null;
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        for (Pair<RexNode, RexNode> pair : Pair.zip(
            queryInputExplained0.right, queryInputExplained.right)) {
          if (!pair.left.equals(pair.right)) {
            return null;
          }
```

### DataFlowIssue
Argument `queryInputExplained0.right` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java

      List<RexNode> projectExprs =
          MutableRels.createProjects(target, queryInputExplained0.right);
      final RexProgram compenRexProgram =
          RexProgram.create(target.rowType, projectExprs,
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
          final int idx = inputRef.getIndex();
          if (idx < fieldCnt(qInput0)) {
            final int newIdx = ((RexInputRef) qInput0Projs.get(idx)).getIndex();
            return new RexInputRef(newIdx, inputRef.getType());
          } else {
```

### DataFlowIssue
Method invocation `isWeaker` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      }
      for (Pair<Operand, MutableRel> pair : Pair.zip(operands, rels)) {
        if (!pair.left.isWeaker(visitor, pair.right)) {
          return false;
        }
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      }
      for (Pair<Operand, MutableRel> pair : Pair.zip(operands, rels)) {
        if (!pair.left.isWeaker(visitor, pair.right)) {
          return false;
        }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
    for (Pair<RelDataTypeField, RelDataTypeField> pair
        : Pair.zip(rel0.rowType.getFieldList(), rel1.rowType.getFieldList())) {
      if (!pair.left.getType().equals(pair.right.getType())) {
        return litmus.fail("Mismatch for column type: [{}]", Pair.of(rel0, rel1));
      }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
    for (Pair<RelDataTypeField, RelDataTypeField> pair
        : Pair.zip(rel0.rowType.getFieldList(), rel1.rowType.getFieldList())) {
      if (!pair.left.getType().equals(pair.right.getType())) {
        return litmus.fail("Mismatch for column type: [{}]", Pair.of(rel0, rel1));
      }
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        for (int i = 0; i < fieldCnt(query); i++) {
          if (i < fieldCnt(qInput0)) {
            compenProjs.add(qInput0Projs.get(i));
          } else {
            final int newIdx = i - fieldCnt(qInput0) + fieldCnt(qInput0.getInput());
```

### DataFlowIssue
Argument `qInput1Cond` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
      if (splitted != null && splitted.isAlwaysTrue()) {
        final RexNode compenCond =
            RexUtil.shift(qInput1Cond, qInput0.rowType.getFieldCount());
        final List<RexNode> compenProjs = new ArrayList<>();
        for (int i = 0; i < query.rowType.getFieldCount(); i++) {
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
          } else {
            final RexNode shifted =
                RexUtil.shift(qInput1Projs.get(i - fieldCnt(qInput0)),
                    qInput0.rowType.getFieldCount());
            compenProjs.add(shifted);
```

### DataFlowIssue
Argument `targetProjs` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java

      try {
        final RexShuttle shuttle = getRexShuttle(targetProjs);
        final RexNode splitted =
            splitFilter(call.getSimplify(), queryCond, targetCond);
```

### DataFlowIssue
Argument `queryCond` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        final RexShuttle shuttle = getRexShuttle(targetProjs);
        final RexNode splitted =
            splitFilter(call.getSimplify(), queryCond, targetCond);

        final RexNode compenCond;
```

### DataFlowIssue
Argument `targetCond` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        final RexShuttle shuttle = getRexShuttle(targetProjs);
        final RexNode splitted =
            splitFilter(call.getSimplify(), queryCond, targetCond);

        final RexNode compenCond;
```

### DataFlowIssue
Argument `queryProjs` might be null
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
        }

        final List<RexNode> compenProjs = shuttle.apply(queryProjs);
        if (compenCond == null
            && RexUtil.isIdentity(compenProjs, target.rowType)) {
```

### DataFlowIssue
Argument `rel` might be null
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
    for (Pair<RelNode, List<RelOptMaterialization>> use : materializationUses) {
      RelNode rel = use.left;
      Hook.SUB.run(rel);
      registerImpl(rel, root.set);
    }
```

### DataFlowIssue
Argument `use.right` might be null
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
                originalRoot, materializations));
    for (Pair<RelNode, List<RelOptMaterialization>> use : materializationUses) {
      applicableMaterializations.removeAll(use.right);
    }
    for (RelOptMaterialization materialization : applicableMaterializations) {
```

### DataFlowIssue
Argument `rel` might be null
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoPlanner.java`
#### Snippet
```java
    if (!latticeUses.isEmpty()) {
      RelNode rel = latticeUses.get(0).left;
      Hook.SUB.run(rel);
      registerImpl(rel, root.set);
    }
```

### DataFlowIssue
Argument `outputDirectory` might be null
in `core/src/main/java/org/apache/calcite/plan/visualizer/RuleMatchVisualizer.java`
#### Snippet
```java
      String dataJsContent = "var data = " + getJsonStringResult() + ";\n";

      Path outputDirPath = Paths.get(outputDirectory);
      Path htmlOutput = outputDirPath.resolve(htmlFileName);
      Path dataOutput = outputDirPath.resolve(dataFileName);
```

### DataFlowIssue
Casting `node` to `RexCall` may produce `ClassCastException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    case EQUALS:
    case IS_NOT_DISTINCT_FROM:
      final RexCall call0 = (RexCall) node;
      final RexNode leftRex = call0.getOperands().get(0);
      final RexNode rightRex = call0.getOperands().get(1);
```

### DataFlowIssue
Casting `node` to `RexCall` may produce `ClassCastException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      // fall through
    case AND:
      final RexCall call = (RexCall) node;
      final List<RexNode> list = new ArrayList<>();
      List<RexNode> operands = Lists.newArrayList(call.getOperands());
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      final RelDataTypeField inputField = pair.left;
      final RelDataTypeField outputField = pair.right;
      assert inputField.getType().equals(outputField.getType());
      final RexBuilder rexBuilder = rel.getCluster().getRexBuilder();
      renames.add(
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      final RelDataTypeField inputField = pair.left;
      final RelDataTypeField outputField = pair.right;
      assert inputField.getType().equals(outputField.getType());
      final RexBuilder rexBuilder = rel.getCluster().getRexBuilder();
      renames.add(
```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
            rexBuilder.makeFieldAccess(
                x,
                xField.getIndex());
        RexNode newY =
            rexBuilder.makeFieldAccess(
```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
            rexBuilder.makeFieldAccess(
                y,
                yField.getIndex());
        RexNode newCall =
            isDistinctFromInternal(rexBuilder, newX, newY, neg);
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    final List<RelDataTypeField> f2 = rowType2.getFieldList();
    for (Pair<RelDataTypeField, RelDataTypeField> pair : Pair.zip(f1, f2)) {
      final RelDataType type1 = pair.left.getType();
      final RelDataType type2 = pair.right.getType();
      // If one of the types is ANY comparison should succeed
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    for (Pair<RelDataTypeField, RelDataTypeField> pair : Pair.zip(f1, f2)) {
      final RelDataType type1 = pair.left.getType();
      final RelDataType type2 = pair.right.getType();
      // If one of the types is ANY comparison should succeed
      if (type1.getSqlTypeName() == SqlTypeName.ANY
```

### DataFlowIssue
Method invocation `push` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
     */
    @Override protected RelNode visitChild(RelNode parent, int i, RelNode child) {
      inheritPaths.forEach(inheritPath -> inheritPath.right.push(i));
      try {
        RelNode child2 = child.accept(this);
```

### DataFlowIssue
Method invocation `pop` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
        return parent;
      } finally {
        inheritPaths.forEach(inheritPath -> inheritPath.right.pop());
      }
    }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    final List<RelDataTypeField> f2 = targetType.getFieldList();
    for (Pair<RelDataTypeField, RelDataTypeField> pair : Pair.zip(f1, f2)) {
      final RelDataType t1 = pair.left.getType();
      final RelDataType t2 = pair.right.getType();
      // If one of the types is ANY comparison should succeed
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
    for (Pair<RelDataTypeField, RelDataTypeField> pair : Pair.zip(f1, f2)) {
      final RelDataType t1 = pair.left.getType();
      final RelDataType t2 = pair.right.getType();
      // If one of the types is ANY comparison should succeed
      if (sourceType.getSqlTypeName() == SqlTypeName.ANY
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      if (inheritPaths.size() > 0) {
        final List<RelHint> hints = inheritPaths.stream()
            .sorted(Comparator.comparingInt(o -> o.right.size()))
            .map(path -> copyWithInheritPath(path.left, path.right))
            .reduce(new ArrayList<>(), (acc, hints1) -> {
```

### DataFlowIssue
Argument `path.left` might be null
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
        final List<RelHint> hints = inheritPaths.stream()
            .sorted(Comparator.comparingInt(o -> o.right.size()))
            .map(path -> copyWithInheritPath(path.left, path.right))
            .reduce(new ArrayList<>(), (acc, hints1) -> {
              acc.addAll(hints1);
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/util/ChunkList.java`
#### Snippet
```java
        start = end;
        if (chunk == null) {
          end = start;
        } else {
          end = start + occupied(chunk);
```

### DataFlowIssue
Method invocation `newDecoder` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/util/NlsString.java`
#### Snippet
```java
                @Override public String load(Pair<ByteString, Charset> key) {
                  final Charset charset = key.right;
                  final CharsetDecoder decoder = charset.newDecoder();
                  final byte[] bytes = key.left.getBytes();
                  final ByteBuffer buffer = ByteBuffer.wrap(bytes);
```

### DataFlowIssue
Method invocation `getBytes` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/util/NlsString.java`
#### Snippet
```java
                  final Charset charset = key.right;
                  final CharsetDecoder decoder = charset.newDecoder();
                  final byte[] bytes = key.left.getBytes();
                  final ByteBuffer buffer = ByteBuffer.wrap(bytes);
                  try {
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
            : Pair.zip(rowConstructor.getOperandList(),
                targetRowType.getFieldList())) {
          if (!pair.right.getType().isNullable()
              && SqlUtil.isNullLiteral(pair.left, false)) {
            throw newValidationError(node,
```

### DataFlowIssue
Argument `columnName` might be null
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    Table table = resolveTable(identifier, scope);
    if (table != null) {
      return table.isRolledUp(columnName);
    }
    return false;
```

### DataFlowIssue
Argument `columnName` might be null
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    Table table = resolveTable(identifier, scope);
    if (table != null) {
      return table.rolledUpColumnValidInsideAgg(columnName, aggCall, parent,
          catalogReader.getConfig());
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    IdentifierNamespace ns =
        new IdentifierNamespace(this, target, null,
            castNonNull(null));
    RelDataType rowType = ns.getRowType();
    SqlNode source = updateCall.getTargetTable().clone(SqlParserPos.ZERO);
```

### DataFlowIssue
Argument `current` might be null
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
  private void checkRollUp(@Nullable SqlNode grandParent, @Nullable SqlNode parent,
      @Nullable SqlNode current, SqlValidatorScope scope, @Nullable String contextClause) {
    current = stripAs(current);
    if (current instanceof SqlCall && !(current instanceof SqlSelect)) {
      // Validate OVER separately
```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
        for (SqlNode row : values) {
          final SqlCall call = (SqlCall) row;
          final SqlNode sourceValue = call.operand(targetField.getIndex());
          final ValidationError validationError =
              new ValidationError(sourceValue,
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/tools/Programs.java`
#### Snippet
```java
  public static Program getProgram() {
    return (planner, rel, requiredOutputTraits, materializations, lattices) ->
        castNonNull(null);
  }

```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
      switch (day) {
      case 1:
        week = 1; // 1st week of month
        break;
      case 8:
```

### DataFlowIssue
Expression `nameAndSchema().right` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/model/ModelHandler.java`
#### Snippet
```java

  private SchemaPlus currentSchema() {
    return nameAndSchema().right;
  }

```

### DataFlowIssue
@Nullable method 'getKeys' always returns a non-null value
in `core/src/main/java/org/apache/calcite/schema/Statistics.java`
#### Snippet
```java
      }

      @Override public @Nullable List<ImmutableBitSet> getKeys() {
        return keysCopy;
      }
```

### DataFlowIssue
Unboxing of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/schema/impl/StarTable.java`
#### Snippet
```java
        return n;
      }
      n += pair.right;
    }
    throw new IllegalArgumentException("star table " + this
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcQueryProvider.java`
#### Snippet
```java

  @Override public <T> Enumerator<T> executeQuery(Queryable<T> queryable) {
    return castNonNull(null);
  }
}
```

### DataFlowIssue
Expression `pairs.get(index + 1).left` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/schema/Schemas.java`
#### Snippet
```java
      return new AbstractList<String>() {
        @Override public String get(int index) {
          return pairs.get(index + 1).left;
        }

```

### DataFlowIssue
Method invocation `getConnection` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcUtils.java`
#### Snippet
```java
      Connection connection = null;
      try {
        connection = dataSource.getConnection();
        DatabaseMetaData metaData = connection.getMetaData();
        SqlDialect dialect = dialectFactory.create(metaData);
```

### DataFlowIssue
Method invocation `create` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcUtils.java`
#### Snippet
```java
        connection = dataSource.getConnection();
        DatabaseMetaData metaData = connection.getMetaData();
        SqlDialect dialect = dialectFactory.create(metaData);
        connection.close();
        connection = null;
```

### DataFlowIssue
Unboxing of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
    @Override public String toString(Object dataSet) {
      Pair<@Nullable Object, Integer> pair = unfreeze(dataSet);
      return Collections.nCopies(pair.right, pair.left).toString();
    }
  }
```

### DataFlowIssue
Unboxing of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
    @Override public int size(Object dataSet) {
      Pair<@Nullable Object, Integer> pair = unfreeze(dataSet);
      return pair.right;
    }

```

### DataFlowIssue
Argument `codes` might be null
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
      Object codes = pair.left;
      @Nullable Comparable[] codeValues = pair.right;
      return Pair.of(representation.permute(codes, sources), codeValues);
    }

```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
    @Override public @Nullable Object getObject(Object dataSet, int ordinal) {
      final Pair<Object, @Nullable Comparable[]> pair = unfreeze(dataSet);
      int code = representation.getInt(pair.left, ordinal);
      return pair.right[code];
    }
```

### DataFlowIssue
Array access `pair.right[code]` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
      final Pair<Object, @Nullable Comparable[]> pair = unfreeze(dataSet);
      int code = representation.getInt(pair.left, ordinal);
      return pair.right[code];
    }

```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/adapter/clone/ArrayTable.java`
#### Snippet
```java
    @Override public int size(Object dataSet) {
      final Pair<Object, @Nullable Comparable[]> pair = unfreeze(dataSet);
      return representation.size(pair.left);
    }

```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableValues.java`
#### Snippet
```java
        literals.add(
            RexToLixTranslator.translateLiteral(
                pair.right,
                pair.left.getType(),
                typeFactory,
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableValues.java`
#### Snippet
```java
            RexToLixTranslator.translateLiteral(
                pair.right,
                pair.left.getType(),
                typeFactory,
                RexImpTable.NullAs.NULL));
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableLimitSort.java`
#### Snippet
```java
                BuiltInMethod.ORDER_BY_WITH_FETCH_AND_OFFSET.method, Expressions.list(
                    childExp,
                    builder.append("keySelector", pair.left))
                    .appendIfNotNull(builder.appendIfNotNull("comparator", pair.right))
                    .appendIfNotNull(
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableLimitSort.java`
#### Snippet
```java
                    childExp,
                    builder.append("keySelector", pair.left))
                    .appendIfNotNull(builder.appendIfNotNull("comparator", pair.right))
                    .appendIfNotNull(
                        builder.appendIfNotNull("offset",
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableSort.java`
#### Snippet
```java
                BuiltInMethod.ORDER_BY.method,
                Expressions.list(
                    builder.append("keySelector", pair.left))
                    .appendIfNotNull(
                        builder.appendIfNotNull("comparator", pair.right)))));
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableSort.java`
#### Snippet
```java
                    builder.append("keySelector", pair.left))
                    .appendIfNotNull(
                        builder.appendIfNotNull("comparator", pair.right)))));
    return implementor.result(physType, builder.toBlock());
  }
```

### DataFlowIssue
Unboxing of `a.left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java

    private static Pair<Long, Long> mergeWindows(Pair<Long, Long> a, Pair<Long, Long> b) {
      return new Pair<>(a.left <= b.left ? a.left : b.left, a.right >= b.right ? a.right : b.right);
    }

```

### DataFlowIssue
Unboxing of `b.left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java

    private static Pair<Long, Long> mergeWindows(Pair<Long, Long> a, Pair<Long, Long> b) {
      return new Pair<>(a.left <= b.left ? a.left : b.left, a.right >= b.right ? a.right : b.right);
    }

```

### DataFlowIssue
Unboxing of `a.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java

    private static Pair<Long, Long> mergeWindows(Pair<Long, Long> a, Pair<Long, Long> b) {
      return new Pair<>(a.left <= b.left ? a.left : b.left, a.right >= b.right ? a.right : b.right);
    }

```

### DataFlowIssue
Unboxing of `b.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java

    private static Pair<Long, Long> mergeWindows(Pair<Long, Long> a, Pair<Long, Long> b) {
      return new Pair<>(a.left <= b.left ? a.left : b.left, a.right >= b.right ? a.right : b.right);
    }

```

### DataFlowIssue
Unboxing of `b.left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java

    private static boolean isOverlapped(Pair<Long, Long> a, Pair<Long, Long> b) {
      return !(b.left >= a.right);
    }

```

### DataFlowIssue
Unboxing of `a.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java

    private static boolean isOverlapped(Pair<Long, Long> a, Pair<Long, Long> b) {
      return !(b.left >= a.right);
    }

```

### DataFlowIssue
@Nullable method 'enforce' always returns a non-null value
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableConvention.java`
#### Snippet
```java
  }

  @Override public @Nullable RelNode enforce(
      final RelNode input,
      final RelTraitSet required) {
```

### DataFlowIssue
Argument `e.left` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
    final List<Expression> list = new ArrayList<>();
    for (Pair<RexNode, ? extends @Nullable Type> e : Pair.zip(operandList, storageTypes)) {
      list.add(translate(e.left, nullAs, e.right));
    }
    return list;
```

### DataFlowIssue
Unboxing of `pair.left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableMergeJoin.java`
#### Snippet
```java
    final List<Expression> rightExpressions = new ArrayList<>();
    for (Pair<Integer, Integer> pair : Pair.zip(joinInfo.leftKeys, joinInfo.rightKeys)) {
      RelDataType leftType = left.getRowType().getFieldList().get(pair.left).getType();
      RelDataType rightType = right.getRowType().getFieldList().get(pair.right).getType();
      final RelDataType keyType =
```

### DataFlowIssue
Unboxing of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableMergeJoin.java`
#### Snippet
```java
    for (Pair<Integer, Integer> pair : Pair.zip(joinInfo.leftKeys, joinInfo.rightKeys)) {
      RelDataType leftType = left.getRowType().getFieldList().get(pair.left).getType();
      RelDataType rightType = right.getRowType().getFieldList().get(pair.right).getType();
      final RelDataType keyType =
          requireNonNull(
```

### DataFlowIssue
Method invocation `contains` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
          : Ord.zip(frame.fields)) {
        // If alias and field name match, reference that field.
        if (p.e.left.contains(alias)
            && p.e.right.getName().equals(fieldName)) {
          return field(inputCount, inputCount - 1 - inputOrdinal, p.i);
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
        // If alias and field name match, reference that field.
        if (p.e.left.contains(alias)
            && p.e.right.getName().equals(fieldName)) {
          return field(inputCount, inputCount - 1 - inputOrdinal, p.i);
        }
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
        fields.add(
            String.format(Locale.ROOT, "{aliases=%s,fieldName=%s}", p.e.left,
                p.e.right.getName()));
      }
    }
```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
      for (Pair<RexNode, Field> pair
          : Pair.zip(project.getProjects(), frame1.fields)) {
        switch (pair.left.getKind()) {
        case INPUT_REF:
          final int i = ((RexInputRef) pair.left).getIndex();
```

### DataFlowIssue
Dereference of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
          final int i = ((RexInputRef) pair.left).getIndex();
          final Field field = fields.get(i);
          final ImmutableSet<String> aliases = pair.right.left;
          fields.set(i, new Field(aliases, field.right));
          break;
```

### DataFlowIssue
Argument `aliases` might be null
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
          final Field field = fields.get(i);
          final ImmutableSet<String> aliases = pair.right.left;
          fields.set(i, new Field(aliases, field.right));
          break;
        default:
```

### DataFlowIssue
Argument `field.right` might be null
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
          final Field field = fields.get(i);
          final ImmutableSet<String> aliases = pair.right.left;
          fields.set(i, new Field(aliases, field.right));
          break;
        default:
```

### DataFlowIssue
Argument `frame.fields.get(index).left` might be null
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
        // preserve rel aliases for INPUT_REF fields
        final int index = ((RexInputRef) node).getIndex();
        field = new Field(frame.fields.get(index).left, fieldType);
        break;
      default:
```

### DataFlowIssue
Unboxing of `p.left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
        registrar.extraNodes, inFields);
    final List<RexNode> fields = projects.stream()
        .map(p -> aliasMaybe(field(p.left), p.right))
        .collect(Collectors.toList());
    return project(fields);
```

### DataFlowIssue
Argument `p.left` might be null
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
      for (Pair<String, RelDataTypeField> p
          : Pair.zip(newFieldNames, v.getRowType().getFieldList())) {
        b.add(p.left, p.right.getType());
      }
      return values(v.tuples, b.build());
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
      for (Pair<String, RelDataTypeField> p
          : Pair.zip(newFieldNames, v.getRowType().getFieldList())) {
        b.add(p.left, p.right.getType());
      }
      return values(v.tuples, b.build());
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
      final RexInputRef ref = (RexInputRef) expr;
      return requireNonNull(stack.peek(), "empty frame stack")
          .fields.get(ref.getIndex()).getValue().getName();
    case CAST:
      return inferAlias(exprList, ((RexCall) expr).getOperands().get(0), -1);
```

### DataFlowIssue
Method invocation `contains` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java

    Field addAlias(String alias) {
      if (left.contains(alias)) {
        return this;
      }
```

### DataFlowIssue
Argument `right` might be null
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
      final ImmutableSet<String> aliasList =
          ImmutableSet.<String>builder().addAll(left).add(alias).build();
      return new Field(aliasList, right);
    }
  }
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
              Expressions.assign(key_, Expressions.new_(keyJavaType))));
      List<Types.RecordField> fieldList = keyJavaType.getRecordFields();
      for (int i = 0; i < initExpressions.size(); i++) {
        Expression right = initExpressions.get(i);
        builder2.add(
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
    } else {
      DeclarationStatement declare =
          Expressions.declare(0, "key", selector.right.get(0));
      builder2.add(declare);
      key_ = declare.parameter;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      agg.implementor.implementReset(agg.context,
          new WinAggResetContextImpl(builder, agg.state,
              castNonNull(null), castNonNull(null), castNonNull(null), castNonNull(null),
              castNonNull(null), castNonNull(null)));
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      agg.implementor.implementReset(agg.context,
          new WinAggResetContextImpl(builder, agg.state,
              castNonNull(null), castNonNull(null), castNonNull(null), castNonNull(null),
              castNonNull(null), castNonNull(null)));
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      agg.implementor.implementReset(agg.context,
          new WinAggResetContextImpl(builder, agg.state,
              castNonNull(null), castNonNull(null), castNonNull(null), castNonNull(null),
              castNonNull(null), castNonNull(null)));
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      agg.implementor.implementReset(agg.context,
          new WinAggResetContextImpl(builder, agg.state,
              castNonNull(null), castNonNull(null), castNonNull(null), castNonNull(null),
              castNonNull(null), castNonNull(null)));
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
          new WinAggResetContextImpl(builder, agg.state,
              castNonNull(null), castNonNull(null), castNonNull(null), castNonNull(null),
              castNonNull(null), castNonNull(null)));
    }
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
          new WinAggResetContextImpl(builder, agg.state,
              castNonNull(null), castNonNull(null), castNonNull(null), castNonNull(null),
              castNonNull(null), castNonNull(null)));
    }
  }
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      // optimize=false to prevent inlining of object create into for-loops
      return Pair.of(
          builder.append("keySelector" + windowIdx, pair.left, false),
          builder.append("keyComparator" + windowIdx, pair.right, false));
    } else {
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      return Pair.of(
          builder.append("keySelector" + windowIdx, pair.left, false),
          builder.append("keyComparator" + windowIdx, pair.right, false));
    } else {
      return Pair.of(null, null);
```

### DataFlowIssue
Method invocation `next` will produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java

    // builder3
    Integer[] rows = iterator.next();

    int prevStart = -1;
```

### DataFlowIssue
Variable is already assigned to this value
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
          // builder6
          // recompute
          actualStart = start;
          // implementReset
        } else { // must be start == prevStart && end > prevEnd
```

### DataFlowIssue
Argument `node` might be null
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableWindow.java`
#### Snippet
```java
      }
      RexNode node = bound.getOffset();
      Expression offs = translator.translate(node);
      // Floating offset does not make sense since we refer to array index.
      // Nulls do not make sense as well.
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
        parent = create(path.parent());
      }
      return new MySchemaPlus(parent, pair.left, pair.right);
    }

```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
        parent = create(path.parent());
      }
      return new MySchemaPlus(parent, pair.left, pair.right);
    }

```

### DataFlowIssue
Argument `Util.last(path).left` might be null
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
    final SchemaPlus schemaPlus = MySchemaPlus.create(path);
    return new RelOptTableImpl(schema, rowType, Pair.left(path), table,
        c -> Schemas.getTableExpression(schemaPlus, Util.last(path).left, table, c),
        table.getStatistic().getRowCount());
  }
```

### DataFlowIssue
Method invocation `getValidatedNodeType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java
    final SqlNode validatedNode = this.validate(sqlNode);
    final RelDataType type =
        this.validator.getValidatedNodeType(validatedNode);
    return Pair.of(validatedNode, type);
  }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/prepare/QueryableRelBuilder.java`
#### Snippet
```java
        LogicalProject.create(child, ImmutableList.of(), nodes, (List<String>)  null,
            ImmutableSet.of()));
    return castNonNull(null);
  }

```

### DataFlowIssue
Unboxing of `castNonNull(priorityQueue.peek())` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
    boolean offer(double d) {
      boolean b;
      if (count++ < warmUpCount || d > castNonNull(priorityQueue.peek())) {
        if (priorityQueue.size() >= size) {
          priorityQueue.remove(deque.pop());
```

### DataFlowIssue
Argument `context.exc` might be null
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
    try {
      if (context.hasException()) {
        throw toUnchecked(context.exc);
      }
      value = context.obj;
```

### DataFlowIssue
Argument `exc` might be null
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
    switch (errorBehavior) {
    case ERROR:
      throw toUnchecked(exc);
    case NULL:
      return null;
```

### DataFlowIssue
Argument `context.exc` might be null
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
        return Boolean.FALSE;
      case ERROR:
        throw toUnchecked(context.exc);
      case UNKNOWN:
        return null;
```

### DataFlowIssue
Argument `input.exc` might be null
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
      case STRICT:
        if (input.hasException()) {
          return JsonPathContext.withStrictException(pathSpec, input.exc);
        }
        ctx =
```

### DataFlowIssue
Argument `exc` might be null
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
    switch (errorBehavior) {
    case ERROR:
      throw toUnchecked(exc);
    case NULL:
      return null;
```

### DataFlowIssue
Argument `context.exc` might be null
in `core/src/main/java/org/apache/calcite/runtime/JsonFunctions.java`
#### Snippet
```java
    try {
      if (context.hasException()) {
        throw toUnchecked(context.exc);
      }
      value = context.obj;
```

### DataFlowIssue
@Nullable method 'ST_Length' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   * Returns the length of the {@code geom}.
   */
  public static @Nullable Double ST_Length(Geometry geom) {
    return geom.getLength();
  }
```

### DataFlowIssue
@Nullable method 'ST_MaxDistance' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   * Computes the maximum distance between {@code geom1} and {@code geom2}.
   */
  public static @Nullable Double ST_MaxDistance(Geometry geom1, Geometry geom2) {
    double maxDistance = Double.MIN_VALUE;
    for (Coordinate coordinate1 : geom1.getCoordinates()) {
```

### DataFlowIssue
@Nullable method 'ST_XMax' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   * Returns the X maxima of a 2D or 3D bounding box or a geometry.
   */
  public static @Nullable Double ST_XMax(Geometry geom) {
    return geom.getEnvelopeInternal().getMaxX();
  }
```

### DataFlowIssue
@Nullable method 'ST_AsEWKT' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable String ST_AsEWKT(Geometry geometry) {
    return asEwkt(geometry);
  }
```

### DataFlowIssue
@Nullable method 'ST_YMax' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   * Returns the Y maxima of a 2D or 3D bounding box or a geometry.
   */
  public static @Nullable Double ST_YMax(Geometry geom) {
    return geom.getEnvelopeInternal().getMaxY();
  }
```

### DataFlowIssue
@Nullable method 'ST_GeomFromGeoJSON' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable Geometry ST_GeomFromGeoJSON(String geojson) {
    return fromGeoJson(geojson);
  }
```

### DataFlowIssue
@Nullable method 'ST_AsGeoJSON' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable String ST_AsGeoJSON(Geometry geometry) {
    return asGeoJson(geometry);
  }
```

### DataFlowIssue
@Nullable method 'ST_Perimeter' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   * Returns the length of the perimeter of *polygon* (which may be a MULTIPOLYGON).
   */
  public static @Nullable Double ST_Perimeter(Geometry geom) {
    double perimeter = 0;
    for (int i = 0; i < geom.getNumGeometries(); i++) {
```

### DataFlowIssue
@Nullable method 'ST_GeomFromWKT' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable Geometry ST_GeomFromWKT(String wkt, int srid) {
    Geometry geometry = fromWkt(wkt);
    geometry.setSRID(srid);
```

### DataFlowIssue
@Nullable method 'ST_AsWKT' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable String ST_AsWKT(Geometry geometry) {
    return asWkt(geometry);
  }
```

### DataFlowIssue
@Nullable method 'ST_AsGML' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable String ST_AsGML(Geometry geometry) {
    return asGml(geometry);
  }
```

### DataFlowIssue
@Nullable method 'ST_YMin' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   * Returns the Y minima of a 2D or 3D bounding box or a geometry.
   */
  public static @Nullable Double ST_YMin(Geometry geom) {
    return geom.getEnvelopeInternal().getMinY();
  }
```

### DataFlowIssue
@Nullable method 'ST_GeomFromWKB' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable Geometry ST_GeomFromWKB(ByteString wkb, int srid) {
    Geometry geometry = fromWkb(wkb);
    geometry.setSRID(srid);
```

### DataFlowIssue
@Nullable method 'ST_XMin' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   * Returns the X minima of a 2D or 3D bounding box or a geometry.
   */
  public static @Nullable Double ST_XMin(Geometry geom) {
    return geom.getEnvelopeInternal().getMinX();
  }
```

### DataFlowIssue
@Nullable method 'ST_Area' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
   * Returns the area of the {@code geom}.
   */
  public static @Nullable Double ST_Area(Geometry geom) {
    return geom.getArea();
  }
```

### DataFlowIssue
@Nullable method 'ST_GeomFromWKB' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable Geometry ST_GeomFromWKB(ByteString wkb) {
    return fromWkb(wkb);
  }
```

### DataFlowIssue
@Nullable method 'ST_GeomFromEWKT' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable Geometry ST_GeomFromEWKT(String ewkt) {
    return fromEwkt(ewkt);
  }
```

### DataFlowIssue
@Nullable method 'ST_AsWKB' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable ByteString ST_AsWKB(Geometry geometry) {
    return asWkb(geometry);
  }
```

### DataFlowIssue
@Nullable method 'ST_GeomFromGML' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SpatialTypeFunctions.java`
#### Snippet
```java
  }

  public static @Nullable Geometry ST_GeomFromGML(String gml, int srid) {
    Geometry geometry = fromGml(gml);
    geometry.setSRID(srid);
```

### DataFlowIssue
Argument `inputMapping` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    // Build new project expressions, and populate the mapping.
    final RexVisitor<RexNode> shuttle =
        new RexPermuteInputsShuttle(inputMapping, newInput);
    RexNode newConditionExpr =
        conditionExpr.accept(shuttle);
```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java

    // Build new filter with trimmed input and condition.
    relBuilder.push(newInput)
        .filter(filter.getVariablesSet(), newConditionExpr);

```

### DataFlowIssue
Method invocation `isIdentity` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
      ImmutableBitSet inputFieldsUsed = ImmutableBitSet.range(input.getRowType().getFieldCount());
      TrimResult trimResult = dispatchTrimFields(input, inputFieldsUsed, extraFields);
      if (!trimResult.right.isIdentity()) {
        throw new IllegalArgumentException("Expected identity mapping after processing RelNode "
            + input + "; but got " + trimResult.right);
```

### DataFlowIssue
Method invocation `getSourceCount` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    final Mapping mapping = trimResult.right;
    final int fieldCount = rel.getRowType().getFieldCount();
    assert mapping.getSourceCount() == fieldCount
        : "source: " + mapping.getSourceCount() + " != " + fieldCount;
    final int newFieldCount = newRel.getRowType().getFieldCount();
```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    assert mapping.getSourceCount() == fieldCount
        : "source: " + mapping.getSourceCount() + " != " + fieldCount;
    final int newFieldCount = newRel.getRowType().getFieldCount();
    assert mapping.getTargetCount() + extraFields.size() == newFieldCount
        || Bug.TODO_FIXED
```

### DataFlowIssue
Method invocation `isIdentity` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    final TrimResult trimResult =
        dispatchTrimFields(root, fieldsUsed, extraFields);
    if (!trimResult.right.isIdentity()) {
      throw new IllegalArgumentException();
    }
```

### DataFlowIssue
Argument `trimResult.left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
      SqlToRelConverter.SQL2REL_LOGGER.debug(
          RelOptUtil.dumpPlan("Plan after trimming unused fields",
              trimResult.left, SqlExplainFormat.TEXT,
              SqlExplainLevel.EXPPLAN_ATTRIBUTES));
    }
```

### DataFlowIssue
Expression `trimResult.left` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
              SqlExplainLevel.EXPPLAN_ATTRIBUTES));
    }
    return trimResult.left;
  }

```

### DataFlowIssue
Method invocation `isIdentity` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    // there's nothing we can do.
    if (newInput == input
        && inputMapping.isIdentity()
        && fieldsUsed.cardinality() == fieldCount) {
      return result(sort, Mappings.createIdentity(fieldCount));
```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    }

    relBuilder.push(newInput);
    final ImmutableList<RexNode> fields =
        relBuilder.fields(RexUtil.apply(inputMapping, collation));
```

### DataFlowIssue
Argument `inputMapping` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    relBuilder.push(newInput);
    final ImmutableList<RexNode> fields =
        relBuilder.fields(RexUtil.apply(inputMapping, collation));
    relBuilder.sortLimit(sort.offset, sort.fetch, fields);

```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    // pretend that one field is used.
    if (fieldsUsed.cardinality() == 0) {
      return dummyProject(fieldCount, newInput, project);
    }

```

### DataFlowIssue
Argument `inputMapping` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    final RexVisitor<RexNode> shuttle =
        new RexPermuteInputsShuttle(
            inputMapping, newInput);
    final Mapping mapping =
        Mappings.create(
```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
            mapping);

    relBuilder.push(newInput);
    relBuilder.project(newProjects, newRowType.getFieldNames());
    final RelNode newProject = relBuilder.build();
```

### DataFlowIssue
Argument `trimResult.right` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
      // and the transformed input has columns inputMapping = [d, a, b].
      // remaining will permute [b, d] to [d, a, b].
      Mapping remaining = Mappings.divide(mapping, trimResult.right);

      // Create a projection; does nothing if remaining is identity.
```

### DataFlowIssue
Argument `trimResult.left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java

      // Create a projection; does nothing if remaining is identity.
      relBuilder.push(trimResult.left);
      relBuilder.permute(remaining);

```

### DataFlowIssue
Switch label `UNION` is the only reachable in the whole switch
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java

    switch (setOp.kind) {
    case UNION:
      relBuilder.union(setOp.all, setOp.getInputs().size());
      break;
```

### DataFlowIssue
Method invocation `isIdentity` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
          trimChildRestore(
              tabFun, input, inputFieldsUsed, inputExtraFields);
      assert trimResult.right.isIdentity();
      newInputs.add(trimResult.left);
    }
```

### DataFlowIssue
Method invocation `isIdentity` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
      Set<RelDataTypeField> extraFields) {
    TrimResult trimResult = trimChild(rel, input, fieldsUsed, extraFields);
    if (trimResult.right.isIdentity()) {
      return trimResult;
    }
```

### DataFlowIssue
Argument `trimResult.left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
              : rexBuilder.makeInputRef(field.getType(), source));
    }
    relBuilder.push(trimResult.left)
        .project(exprList, nameList);
    return result(relBuilder.build(),
```

### DataFlowIssue
Method invocation `isIdentity` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    // there's nothing we can do.
    if (newInput == input
        && inputMapping.isIdentity()
        && fieldsUsed.cardinality() == fieldCount) {
      return result(sortExchange, Mappings.createIdentity(fieldCount));
```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    }

    relBuilder.push(newInput);
    RelCollation newCollation = RexUtil.apply(inputMapping, collation);
    RelDistribution newDistribution = distribution.apply(inputMapping);
```

### DataFlowIssue
Argument `inputMapping` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java

    relBuilder.push(newInput);
    RelCollation newCollation = RexUtil.apply(inputMapping, collation);
    RelDistribution newDistribution = distribution.apply(inputMapping);
    relBuilder.sortExchange(newDistribution, newCollation);
```

### DataFlowIssue
Method invocation `isIdentity` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    RelNode newInput = trimResult.left;
    final Mapping inputMapping = trimResult.right;
    if (!inputMapping.isIdentity()) {
      // We asked for all fields. Can't believe that the child decided
      // to permute them!
```

### DataFlowIssue
Argument `inputMapping` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java

    final ImmutableBitSet newGroupSet =
        Mappings.apply(inputMapping, aggregate.getGroupSet());

    final ImmutableList<ImmutableBitSet> newGroupSets =
```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java

    // Now create new agg calls, and populate mapping for them.
    relBuilder.push(newInput);
    final List<RelBuilder.AggCall> newAggCallList = new ArrayList<>();
    j = groupCount;
```

### DataFlowIssue
Method invocation `isIdentity` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    // there's nothing we can do.
    if (newInput == input
        && inputMapping.isIdentity()
        && fieldsUsed.cardinality() == fieldCount) {
      return result(exchange, Mappings.createIdentity(fieldCount));
```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    }

    relBuilder.push(newInput);
    final RelDistribution newDistribution = distribution.apply(inputMapping);
    relBuilder.exchange(newDistribution);
```

### DataFlowIssue
Argument `inputMapping` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java

    relBuilder.push(newInput);
    final RelDistribution newDistribution = distribution.apply(inputMapping);
    relBuilder.exchange(newDistribution);

```

### DataFlowIssue
Method invocation `getTargetCount` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
      offset += inputFieldCount;
      newFieldCount +=
          inputMapping.getTargetCount() + inputExtraFields.size();
    }

```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    // pretend that one field is used.
    if (fieldsUsed.cardinality() == 0 && rexProgram.getCondition() == null) {
      return dummyProject(fieldCount, newInput);
    }

```

### DataFlowIssue
Argument `inputMapping` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
    final RexVisitor<RexNode> shuttle =
        new RexPermuteInputsShuttle(
            inputMapping, newInput);
    final Mapping mapping =
        Mappings.create(
```

### DataFlowIssue
Argument `newInput` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelFieldTrimmer.java`
#### Snippet
```java
            mapping);

    final RelNode newInputRelNode = relBuilder.push(newInput).build();
    RexNode newConditionExpr = null;
    if (conditionExpr != null) {
```

### DataFlowIssue
Expression `fieldName` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/sql2rel/RelStructuredTypeFlattener.java`
#### Snippet
```java
      fieldName = prefix + "$" + fieldName;
    }
    return fieldName;
  }

```

### DataFlowIssue
Argument `flattenedExp.left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelStructuredTypeFlattener.java`
#### Snippet
```java
    // Register each of the new projections.
    for (Pair<RexNode, String> flattenedExp : flattenedExpList) {
      programBuilder.addProject(flattenedExp.left, flattenedExp.right);
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/interpreter/AggregateNode.java`
#### Snippet
```java
      final Scalar addScalar = addScalarProducer.apply(dataContext);
      final Scalar endScalar = castNonNull(null);
      return new ScalarAccumulatorDef(initScalar, addScalar, endScalar,
          rel.getInput().getRowType().getFieldCount(), stateSize, dataContext);
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/calcite/interpreter/AggregateNode.java`
#### Snippet
```java
      final Scalar addScalar = addScalarProducer.apply(dataContext);
      final Scalar endScalar = castNonNull(null);
      return new ScalarAccumulatorDef(initScalar, addScalar, endScalar,
          rel.getInput().getRowType().getFieldCount(), stateSize, dataContext);
    }
```

### DataFlowIssue
Argument `r0` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
    // Sort end points into start and end, such that (s0 <= e0) and (s1 <= e1).
    final RexBuilder rexBuilder = cx.getRexBuilder();
    RexNode leftSwap = le(rexBuilder, r0, r1);
    final RexNode s0 = case_(rexBuilder, leftSwap, r0, r1);
    final RexNode e0 = case_(rexBuilder, leftSwap, r1, r0);
```

### DataFlowIssue
Argument `r1` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
    // Sort end points into start and end, such that (s0 <= e0) and (s1 <= e1).
    final RexBuilder rexBuilder = cx.getRexBuilder();
    RexNode leftSwap = le(rexBuilder, r0, r1);
    final RexNode s0 = case_(rexBuilder, leftSwap, r0, r1);
    final RexNode e0 = case_(rexBuilder, leftSwap, r1, r0);
```

### DataFlowIssue
Argument `r2` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
    final RexNode s0 = case_(rexBuilder, leftSwap, r0, r1);
    final RexNode e0 = case_(rexBuilder, leftSwap, r1, r0);
    RexNode rightSwap = le(rexBuilder, r2, r3);
    final RexNode s1 = case_(rexBuilder, rightSwap, r2, r3);
    final RexNode e1 = case_(rexBuilder, rightSwap, r3, r2);
```

### DataFlowIssue
Argument `r3` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
    final RexNode s0 = case_(rexBuilder, leftSwap, r0, r1);
    final RexNode e0 = case_(rexBuilder, leftSwap, r1, r0);
    RexNode rightSwap = le(rexBuilder, r2, r3);
    final RexNode s1 = case_(rexBuilder, rightSwap, r2, r3);
    final RexNode e1 = case_(rexBuilder, rightSwap, r3, r2);
```

### DataFlowIssue
Expression `pair.left` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
    @SuppressWarnings("method.invocation.invalid")
    Pair<RelNode, Map<RelNode, NodeInfo>> pair = compiler.visitRoot(rel);
    this.rootRel = pair.left;
    this.nodes = ImmutableMap.copyOf(pair.right);
  }
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/interpreter/Interpreter.java`
#### Snippet
```java
    Pair<RelNode, Map<RelNode, NodeInfo>> pair = compiler.visitRoot(rel);
    this.rootRel = pair.left;
    this.nodes = ImmutableMap.copyOf(pair.right);
  }

```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
      RexNode newProjExpr =
          removeCorrelationExpr(
              pair.left,
              projectPulledAboveLeftCorrelator,
              nullIndicator);
```

### DataFlowIssue
Argument `pair.left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java
      RexNode newProjExpr =
          removeCorrelationExpr(
              pair.left,
              projectPulledAboveLeftCorrelator,
              isCount);
```

### DataFlowIssue
Method invocation `getTable` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/materialize/MaterializationService.java`
#### Snippet
```java
      (o0, o1) -> {
        // We prefer rolling up from the table with the fewest rows.
        final Table t0 = o0.left.getTable();
        final Table t1 = o1.left.getTable();
        Double rowCount0 = t0.getStatistic().getRowCount();
```

### DataFlowIssue
Method invocation `getTable` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/materialize/MaterializationService.java`
#### Snippet
```java
        // We prefer rolling up from the table with the fewest rows.
        final Table t0 = o0.left.getTable();
        final Table t1 = o1.left.getTable();
        Double rowCount0 = t0.getStatistic().getRowCount();
        Double rowCount1 = t1.getStatistic().getRowCount();
```

### DataFlowIssue
@Nullable method 'repeat' always returns a non-null value
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java

  /** Support the ARRAY_REPEAT function. */
  public static @Nullable List<Object> repeat(Object element, Object count) {
    if (count == null) {
      return null;
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileEnumerator.java`
#### Snippet
```java
        return true;
      } else {
        current = null;
        return false;
      }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileFieldType.java`
#### Snippet
```java
  FLOAT(Primitive.FLOAT),
  DOUBLE(Primitive.DOUBLE),
  DATE(null, java.sql.Date.class),
  TIME(null, java.sql.Time.class),
  TIMESTAMP(null, java.sql.Timestamp.class);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileFieldType.java`
#### Snippet
```java
 */
enum FileFieldType {
  STRING(null, String.class),
  BOOLEAN(Primitive.BOOLEAN),
  BYTE(Primitive.BYTE),
```

### DataFlowIssue
Argument `value.primitive.primitiveName` might be null
in `file/src/main/java/org/apache/calcite/adapter/file/FileFieldType.java`
#### Snippet
```java

      if (value.primitive != null) {
        builder.put(value.primitive.primitiveName, value);
      }
    }
```

### DataFlowIssue
Argument `primitive.boxClass` might be null
in `file/src/main/java/org/apache/calcite/adapter/file/FileFieldType.java`
#### Snippet
```java

  FileFieldType(Primitive primitive) {
    this(primitive, primitive.boxClass);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileFieldType.java`
#### Snippet
```java
  DATE(null, java.sql.Date.class),
  TIME(null, java.sql.Time.class),
  TIMESTAMP(null, java.sql.Timestamp.class);

  private final Primitive primitive;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileFieldType.java`
#### Snippet
```java
  DOUBLE(Primitive.DOUBLE),
  DATE(null, java.sql.Date.class),
  TIME(null, java.sql.Time.class),
  TIMESTAMP(null, java.sql.Timestamp.class);

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileTable.java`
#### Snippet
```java
    String selector = (String) tableDef.get("selector");
    Integer index = (Integer) tableDef.get("index");
    return new FileTable(source, selector, index, null, fieldConfigs);
  }

```

### DataFlowIssue
Expression `result` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/CsvStreamReader.java`
#### Snippet
```java
      }
    } while (parser.isPending());
    return result;
  }

```

### DataFlowIssue
Expression `contentQueue.poll()` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/CsvStreamReader.java`
#### Snippet
```java
   */
  private String getNextLine() throws IOException {
    return contentQueue.poll();
  }

```

### DataFlowIssue
Argument `protoRowType` might be null
in `file/src/main/java/org/apache/calcite/adapter/file/CsvTableFactory.java`
#### Snippet
```java
    final RelProtoDataType protoRowType =
        rowType != null ? RelDataTypeImpl.proto(rowType) : null;
    return new CsvTranslatableTable(source, protoRowType);
  }
}
```

### DataFlowIssue
Argument `directoryFile` might be null
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchemaFactory.java`
#### Snippet
```java
      }
    }
    return new FileSchema(parentSchema, name, directoryFile, tables);
  }
}
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/CsvProjectTableScanRule.java`
#### Snippet
```java
        fields[i] = ((RexInputRef) exp).getIndex();
      } else {
        return null; // not a simple projection
      }
    }
```

### DataFlowIssue
Argument `p.left` might be null
in `core/src/main/java/org/apache/calcite/materialize/LatticeSuggester.java`
#### Snippet
```java
          for (Pair<RexNode, String> p : project.getNamedProjects()) {
            @SuppressWarnings("method.invocation.invalid")
            ColRef colRef = toColRef(p.left, p.right);
            columnBuilder.add(colRef);
          }
```

### DataFlowIssue
Argument `p.right` might be null
in `core/src/main/java/org/apache/calcite/materialize/LatticeSuggester.java`
#### Snippet
```java
          for (Pair<RexNode, String> p : project.getNamedProjects()) {
            @SuppressWarnings("method.invocation.invalid")
            ColRef colRef = toColRef(p.left, p.right);
            columnBuilder.add(colRef);
          }
```

### DataFlowIssue
Argument `p.left` might be null
in `core/src/main/java/org/apache/calcite/materialize/LatticeSuggester.java`
#### Snippet
```java
            Lattice.BaseColumn baseColumn = (Lattice.BaseColumn) c;
            Pair<Path, Integer> p = lattice.columnToPathOffset(baseColumn);
            return builder.pathOffsetToColumn(p.left, p.right);
          } else {
            final Lattice.DerivedColumn derivedColumn = (Lattice.DerivedColumn) c;
```

### DataFlowIssue
Unboxing of `p.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/materialize/LatticeSuggester.java`
#### Snippet
```java
            Lattice.BaseColumn baseColumn = (Lattice.BaseColumn) c;
            Pair<Path, Integer> p = lattice.columnToPathOffset(baseColumn);
            return builder.pathOffsetToColumn(p.left, p.right);
          } else {
            final Lattice.DerivedColumn derivedColumn = (Lattice.DerivedColumn) c;
```

### DataFlowIssue
Argument `source` might be null
in `core/src/main/java/org/apache/calcite/materialize/LatticeSuggester.java`
#### Snippet
```java
      final TableRef target = e.getKey().right;
      final StepRef stepRef =
          q.stepRef(source, target, ImmutableList.copyOf(e.getValue()));
      g.addVertex(stepRef.source());
      g.addVertex(stepRef.target());
```

### DataFlowIssue
Argument `target` might be null
in `core/src/main/java/org/apache/calcite/materialize/LatticeSuggester.java`
#### Snippet
```java
      final TableRef target = e.getKey().right;
      final StepRef stepRef =
          q.stepRef(source, target, ImmutableList.copyOf(e.getValue()));
      g.addVertex(stepRef.source());
      g.addVertex(stepRef.target());
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
    final Source sourceSansCsv = sourceSansGz.trimOrNull(".csv");
    if (sourceSansCsv != null) {
      final Table table = new CsvTranslatableTable(source, null);
      builder.put(Util.first(tableName, sourceSansCsv.path()), table);
      return true;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
        if (sourceSansJson != null) {
          addTable(builder, source, sourceSansJson.relative(baseSource).path(),
              null);
        }
        final Source sourceSansCsv = sourceSansGz.trimOrNull(".csv");
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
        if (sourceSansCsv != null) {
          addTable(builder, source, sourceSansCsv.relative(baseSource).path(),
              null);
        }
      }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/FileSchema.java`
#### Snippet
```java
    return s.endsWith(suffix)
        ? s.substring(0, s.length() - suffix.length())
        : null;
  }

```

### DataFlowIssue
Method invocation `getTable` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
      final List<Vertex> vertices = new ArrayList<>();
      for (Pair<TableScan, @Nullable String> p : Pair.zip(relNodes, aliases)) {
        final LatticeTable table = space.register(p.left.getTable());
        final Vertex vertex = new Vertex(table, p.right);
        graph.addVertex(vertex);
```

### DataFlowIssue
Dereference of `p.left` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
    for (Pair<LatticeNode, Path> p
        : Pair.zip(rootNode.descendants, rootNode.paths)) {
      if (Objects.equals(p.left.alias, c.table)) {
        return Pair.of(p.right, c.ordinal - p.left.startCol);
      }
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `file/src/main/java/org/apache/calcite/adapter/file/CsvTableScan.java`
#### Snippet
```java
    // then factor = (1 + 2) / (3 + 2) = 0.6
    return super.computeSelfCost(planner, mq)
        .multiplyBy(((double) fields.length + 2D)
            / ((double) table.getRowType().getFieldCount() + 2D));
  }
```

### DataFlowIssue
Argument `table.getExpression(FileTable.class)` might be null
in `file/src/main/java/org/apache/calcite/adapter/file/FileTableScan.java`
#### Snippet
```java
        physType,
        Blocks.toBlock(
            Expressions.call(table.getExpression(FileTable.class), "project",
                Expressions.constant(fields))));
  }
```

### DataFlowIssue
Argument `table.getExpression(CsvTranslatableTable.class)` might be null
in `file/src/main/java/org/apache/calcite/adapter/file/CsvTableScan.java`
#### Snippet
```java
        physType,
        Blocks.toBlock(
            Expressions.call(table.getExpression(CsvTranslatableTable.class),
                "project", implementor.getRootExpression(),
                Expressions.constant(fields))));
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java

  public FileReader(Source source, String selector) throws FileReaderException {
    this(source, selector, null);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
          return allMatches.get(this.matchSeq);
        } else {
          return null;
        }
      }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java

  public FileReader(Source source) throws FileReaderException {
    this(source, null, null);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java

  public FileReader(Source source) throws FileReaderException {
    this(source, null, null);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
    private Object toObject(FileFieldType fieldType, String string) {
      if ((string == null) || (string.length() == 0)) {
        return null;
      }

```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileReader.java`
#### Snippet
```java

  void refresh() throws FileReaderException {
    this.headings = null;
    getTable();
  }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
          return integerFormat.parse(string).shortValue();
        } catch (ParseException e) {
          return null;
        }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
          return integerFormat.parse(string).intValue();
        } catch (ParseException e) {
          return null;
        }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
          return numberFormat.parse(string).longValue();
        } catch (ParseException e) {
          return null;
        }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
          return numberFormat.parse(string).floatValue();
        } catch (ParseException e) {
          return null;
        }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
          return numberFormat.parse(string).doubleValue();
        } catch (ParseException e) {
          return null;
        }

```

### DataFlowIssue
Argument `type` might be null
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
            sources.add(thName);
            if (!skip) {
              addFieldDef(name, type, fieldConfig, sourceIx);
            }
          }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
        final String name = e.getKey();
        if (!sources.contains(name) && !colNames.contains(name)) {
          addFieldDef(name, null, null, e.getValue());
        }
      }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `file/src/main/java/org/apache/calcite/adapter/file/FileRowConverter.java`
#### Snippet
```java
        final String name = e.getKey();
        if (!sources.contains(name) && !colNames.contains(name)) {
          addFieldDef(name, null, null, e.getValue());
        }
      }
```

### DataFlowIssue
Argument `stream` might be null
in `plus/src/main/java/org/apache/calcite/chinook/CalciteConnectionProvider.java`
#### Snippet
```java
    final InputStream stream =
        getClass().getResourceAsStream("/chinook/chinook.json");
    return CharStreams.toString(new InputStreamReader(stream, Charsets.UTF_8));
  }

```

### DataFlowIssue
Method invocation `startsWith` may produce `NullPointerException`
in `plus/src/main/java/org/apache/calcite/adapter/os/FilesTableFunction.java`
#### Snippet
```java
                    current[14] = path = "";
                    current[3] = 0; // depth
                  } else if (path.startsWith("./")) {
                    current[14] = path = path.substring(2);
                    current[3] = count(path, '/') + 1; // depth
```

### DataFlowIssue
Argument `operands[0]` might be null
in `babel/src/main/java/org/apache/calcite/sql/babel/postgresql/SqlRollback.java`
#### Snippet
```java
            final SqlParserPos pos,
            final @Nullable SqlNode... operands) {
          return new SqlRollback(pos, (SqlLiteral) operands[0]);
        }
      };
```

### DataFlowIssue
Argument `operands[0]` might be null
in `babel/src/main/java/org/apache/calcite/sql/babel/postgresql/SqlShow.java`
#### Snippet
```java
        final SqlParserPos pos,
        final @Nullable SqlNode... operands) {
      return new SqlShow(pos, (SqlIdentifier) operands[0]);
    }
  };
```

### DataFlowIssue
Argument `operands[0]` might be null
in `babel/src/main/java/org/apache/calcite/sql/babel/postgresql/SqlCommit.java`
#### Snippet
```java
            final SqlParserPos pos,
            final @Nullable SqlNode... operands) {
          return new SqlCommit(pos, (SqlLiteral) operands[0]);
        }
      };
```

### DataFlowIssue
Argument `operands[0]` might be null
in `babel/src/main/java/org/apache/calcite/sql/babel/postgresql/SqlBegin.java`
#### Snippet
```java
            final SqlParserPos pos,
            final @Nullable SqlNode... operands) {
          return new SqlBegin(pos, (SqlNodeList) operands[0]);
        }
      };
```

### DataFlowIssue
Argument `operands[0]` might be null
in `babel/src/main/java/org/apache/calcite/sql/babel/postgresql/SqlDiscard.java`
#### Snippet
```java
            final SqlParserPos pos,
            final @Nullable SqlNode... operands) {
          return new SqlDiscard(pos, (SqlIdentifier) operands[0]);
        }
      };
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/BinaryOperatorConversion.java`
#### Snippet
```java
            call.getOperands());
    if (druidExpressions == null) {
      return null;
    }
    if (druidExpressions.size() != 2) {
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DirectOperatorConversion.java`
#### Snippet
```java
            call.getOperands());
    if (druidExpressions == null) {
      return null;
    }
    return DruidExpressions.functionCall(druidFunctionName, druidExpressions);
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/ExtractOperatorConversion.java`
#### Snippet
```java
    final String input = DruidExpressions.toDruidExpression(arg, rowType, query);
    if (input == null) {
      return null;
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/ExtractOperatorConversion.java`
#### Snippet
```java
    final String druidUnit = EXTRACT_UNIT_MAP.get(calciteUnit);
    if (druidUnit == null) {
      return null;
    }

```

### DataFlowIssue
Dereference of `druidSchema` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTableFactory.java`
#### Snippet
```java
    if (dimensionsRaw == null || metricsRaw == null) {
      DruidConnectionImpl connection =
          new DruidConnectionImpl(druidSchema.url,
              druidSchema.url.replace(":8082", ":8081"));
      return DruidTable.create(druidSchema, dataSourceName, intervals,
```

### DataFlowIssue
Argument `intervals` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTableFactory.java`
#### Snippet
```java
          new DruidConnectionImpl(druidSchema.url,
              druidSchema.url.replace(":8082", ":8081"));
      return DruidTable.create(druidSchema, dataSourceName, intervals,
          fieldBuilder, metricNameBuilder, timestampColumnName, connection,
          complexMetrics);
```

### DataFlowIssue
Argument `druidSchema` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTableFactory.java`
#### Snippet
```java
          complexMetrics);
    } else {
      return DruidTable.create(druidSchema, dataSourceName, intervals,
          fieldBuilder, metricNameBuilder, timestampColumnName, complexMetrics);
    }
```

### DataFlowIssue
Argument `intervals` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTableFactory.java`
#### Snippet
```java
          complexMetrics);
    } else {
      return DruidTable.create(druidSchema, dataSourceName, intervals,
          fieldBuilder, metricNameBuilder, timestampColumnName, complexMetrics);
    }
```

### DataFlowIssue
Argument `parent` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTable.java`
#### Snippet
```java
            || call.getOperator() == SqlStdOperatorTable.APPROX_COUNT_DISTINCT)
        && call.getOperandList().size() == 1 // for COUNT(a_1, a_2, ... a_n). n should be 1
        && isValidParentKind(parent);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTable.java`
#### Snippet
```java
    }

    return null;
  }

```

### DataFlowIssue
Expression `complexMetrics.get(alias)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidTable.java`
#### Snippet
```java
  public List<ComplexMetric> getComplexMetricsFrom(String alias) {
    return complexMetrics.containsKey(alias)
            ? complexMetrics.get(alias)
            : new ArrayList<>();
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSchema.java`
#### Snippet
```java

    connection.metadata(tableName, DruidTable.DEFAULT_TIMESTAMP_COLUMN,
        null, fieldMap, metricNameSet, complexMetrics);

    return DruidTable.create(DruidSchema.this, tableName, null,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSchema.java`
#### Snippet
```java
        null, fieldMap, metricNameSet, complexMetrics);

    return DruidTable.create(DruidSchema.this, tableName, null,
        fieldMap, metricNameSet, DruidTable.DEFAULT_TIMESTAMP_COLUMN,
        complexMetrics);
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSchema.java`
#### Snippet
```java
  final String coordinatorUrl;
  private final boolean discoverTables;
  private Map<String, Table> tableMap = null;

  /**
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/UnarySuffixOperatorConversion.java`
#### Snippet
```java

    if (druidExpressions == null) {
      return null;
    }

```

### DataFlowIssue
Argument `timeUnit` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/CeilOperatorConversion.java`
#### Snippet
```java
      final RexLiteral flag = (RexLiteral) call.getOperands().get(1);
      final TimeUnitRange timeUnit = (TimeUnitRange) flag.getValue();
      final Granularity.Type type = DruidDateTimeUtils.toDruidGranularity(timeUnit);
      if (type == null) {
        // Unknown Granularity bail out
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    return new JsonBound(columnName, lhsLiteralValue, false, rhsLiteralValue,
        false, isNumeric,
        extractionFunction);

  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    case LESS_THAN:
      if (lhsIsRef) {
        return new JsonBound(columnName, null, false, literalValue,
            rexCall.getKind() == SqlKind.LESS_THAN, isNumeric,
            extractionFunction);
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
        return new JsonBound(columnName, null, false, literalValue,
            rexCall.getKind() == SqlKind.LESS_THAN, isNumeric,
            extractionFunction);
      } else {
        return new JsonBound(columnName, literalValue, rexCall.getKind() == SqlKind.LESS_THAN, null,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
            extractionFunction);
      } else {
        return new JsonBound(columnName, literalValue, rexCall.getKind() == SqlKind.LESS_THAN, null,
            false, isNumeric,
            extractionFunction);
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
        return new JsonBound(columnName, literalValue, rexCall.getKind() == SqlKind.LESS_THAN, null,
            false, isNumeric,
            extractionFunction);
      }
    case GREATER_THAN_OR_EQUAL:
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    case GREATER_THAN:
      if (!lhsIsRef) {
        return new JsonBound(columnName, null, false, literalValue,
            rexCall.getKind() == SqlKind.GREATER_THAN, isNumeric,
            extractionFunction);
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
        return new JsonBound(columnName, null, false, literalValue,
            rexCall.getKind() == SqlKind.GREATER_THAN, isNumeric,
            extractionFunction);
      } else {
        return new JsonBound(columnName, literalValue, rexCall.getKind() == SqlKind.GREATER_THAN,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
      } else {
        return new JsonBound(columnName, literalValue, rexCall.getKind() == SqlKind.GREATER_THAN,
            null,
            false, isNumeric,
            extractionFunction);
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
            null,
            false, isNumeric,
            extractionFunction);
      }
    default:
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
      partialFilter =
          new JsonBound(columnName, literalValue, false, literalValue,
              false, true, extractionFunction);
    } else {
      partialFilter = new JsonSelector(columnName, literalValue, extractionFunction);
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
              false, true, extractionFunction);
    } else {
      partialFilter = new JsonSelector(columnName, literalValue, extractionFunction);
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    }
    if (rexNode.getKind() == SqlKind.IS_NOT_NULL) {
      return toNotDruidFilter(new JsonSelector(columnName, null, extractionFunction));
    }
    return new JsonSelector(columnName, null, extractionFunction);
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    }
    if (rexNode.getKind() == SqlKind.IS_NOT_NULL) {
      return toNotDruidFilter(new JsonSelector(columnName, null, extractionFunction));
    }
    return new JsonSelector(columnName, null, extractionFunction);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
      return toNotDruidFilter(new JsonSelector(columnName, null, extractionFunction));
    }
    return new JsonSelector(columnName, null, extractionFunction);
  }

```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
      return toNotDruidFilter(new JsonSelector(columnName, null, extractionFunction));
    }
    return new JsonSelector(columnName, null, extractionFunction);
  }

```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    }
    if (e.getKind() != SqlKind.NOT_IN) {
      return new DruidJsonFilter.JsonInFilter(columnName, listBuilder.build(), extractionFunction);
    } else {
      return toNotDruidFilter(
```

### DataFlowIssue
Argument `extractionFunction` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
    } else {
      return toNotDruidFilter(
          new DruidJsonFilter.JsonInFilter(columnName, listBuilder.build(), extractionFunction));
    }
  }
```

### DataFlowIssue
@Nullable method 'toNotDruidFilter' always returns a non-null value
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
  }

  protected static @Nullable DruidJsonFilter toNotDruidFilter(DruidJsonFilter druidJsonFilter) {
    if (druidJsonFilter == null) {
      return null;
```

### DataFlowIssue
Argument `RexLiteral .value(rexNode)` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidExpressions.java`
#### Snippet
```java
        return null;
      } else if (SqlTypeName.NUMERIC_TYPES.contains(sqlTypeName)) {
        return DruidExpressions.numberLiteral((Number) RexLiteral
            .value(rexNode));
      } else if (SqlTypeFamily.INTERVAL_DAY_TIME == sqlTypeName.getFamily()) {
        // Calcite represents DAY-TIME intervals in milliseconds.
```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidExpressions.java`
#### Snippet
```java
      } else if (SqlTypeFamily.INTERVAL_DAY_TIME == sqlTypeName.getFamily()) {
        // Calcite represents DAY-TIME intervals in milliseconds.
        final long milliseconds = ((Number) RexLiteral.value(rexNode)).longValue();
        return DruidExpressions.numberLiteral(milliseconds);
      } else if (SqlTypeFamily.INTERVAL_YEAR_MONTH == sqlTypeName.getFamily()) {
```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidExpressions.java`
#### Snippet
```java
      } else if (SqlTypeFamily.INTERVAL_YEAR_MONTH == sqlTypeName.getFamily()) {
        // Calcite represents YEAR-MONTH intervals in months.
        final long months = ((Number) RexLiteral.value(rexNode)).longValue();
        return DruidExpressions.numberLiteral(months);
      } else if (SqlTypeName.STRING_TYPES.contains(sqlTypeName)) {
```

### DataFlowIssue
Argument `RexLiteral.stringValue(rexNode)` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidExpressions.java`
#### Snippet
```java
      } else if (SqlTypeName.STRING_TYPES.contains(sqlTypeName)) {
        return
            DruidExpressions.stringLiteral(RexLiteral.stringValue(rexNode));
      } else if (SqlTypeName.DATE == sqlTypeName
          || SqlTypeName.TIMESTAMP == sqlTypeName
```

### DataFlowIssue
Argument `DruidDateTimeUtils.literalValue(rexNode)` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidExpressions.java`
#### Snippet
```java
          || SqlTypeName.TIME_WITH_LOCAL_TIME_ZONE == sqlTypeName) {
        return DruidExpressions.numberLiteral(
            DruidDateTimeUtils.literalValue(rexNode));
      } else if (SqlTypeName.BOOLEAN == sqlTypeName) {
        return DruidExpressions.numberLiteral(RexLiteral.booleanValue(rexNode) ? 1 : 0);
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSqlCastConverter.java`
#### Snippet
```java
      return "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'";
    } else {
      return null;
    }
  }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSqlCastConverter.java`
#### Snippet
```java

    if (operandExpression == null) {
      return null;
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidSqlCastConverter.java`
#### Snippet
```java
      if (fromExprType == null || toExprType == null) {
        // Unknown types bail out.
        return null;
      }
      final String typeCastExpression;
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/UnaryPrefixOperatorConversion.java`
#### Snippet
```java

    if (druidExpressions == null) {
      return null;
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DefaultDimensionSpec.java`
#### Snippet
```java

  public DefaultDimensionSpec(String dimension) {
    this(dimension, dimension, null);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DefaultDimensionSpec.java`
#### Snippet
```java

  @Override public ExtractionFunction getExtractionFn() {
    return null;
  }

```

### DataFlowIssue
Method invocation `timeZone` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java
      if (!triple.getLeft().isEmpty()) {
        final String timeZone = cluster.getPlanner().getContext()
            .unwrap(CalciteConnectionConfig.class).timeZone();
        assert timeZone != null;
        intervals =
```

### DataFlowIssue
Argument `query.getConvention()` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java

      return DruidQuery.create(query.getCluster(),
             aggregate.getTraitSet().replace(query.getConvention()),
             query.getTable(), query.druidTable, newNodes);
    }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java
      if (visitor.inputPosReferenced.size() == input.getRowType().getFieldCount()) {
        // All inputs are referenced
        return null;
      }
      final List<RexNode> belowNodes = new ArrayList<>();
```

### DataFlowIssue
Dereference of `below` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java
          cluster.getTypeFactory().builder();
      final RelNode input = Util.last(query.rels);
      for (RexNode e : below) {
        final String name;
        if (e instanceof RexInputRef) {
```

### DataFlowIssue
Argument `name` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java
          name = null;
        }
        builder.add(name, e.getType());
      }
      final RelNode newProject =
```

### DataFlowIssue
Argument `above` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidRules.java`
#### Snippet
```java
      final DruidQuery newQuery = DruidQuery.extendQuery(query, newProject);
      final RelNode newProject2 =
              project.copy(project.getTraitSet(), newQuery, above,
                  project.getRowType());
      call.transformTo(newProject2);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
  public static TimeExtractionFunction createDefault(String timeZone) {
    return new TimeExtractionFunction(ISO_DATETIME_FRACTIONAL_SECOND_FORMAT,
        null, timeZone, null);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
  public static TimeExtractionFunction createDefault(String timeZone) {
    return new TimeExtractionFunction(ISO_DATETIME_FRACTIONAL_SECOND_FORMAT,
        null, timeZone, null);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
      // timestamp with local time zone -> granularityTZId
      return new TimeExtractionFunction(
          castFormat, null, granularityTZId, Locale.ENGLISH.toString());
    case TIMESTAMP_WITH_LOCAL_TIME_ZONE:
      return new TimeExtractionFunction(
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
    case TIMESTAMP_WITH_LOCAL_TIME_ZONE:
      return new TimeExtractionFunction(
          castFormat, null, DateTimeUtils.UTC_ZONE.getID(), Locale.ENGLISH.toString());
    default:
      return null;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
    switch (granularity.getType()) {
    case DAY:
      return new TimeExtractionFunction("d", null, timeZone, local);
    case MONTH:
      return new TimeExtractionFunction("M", null, timeZone, local);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
      return new TimeExtractionFunction("d", null, timeZone, local);
    case MONTH:
      return new TimeExtractionFunction("M", null, timeZone, local);
    case YEAR:
      return new TimeExtractionFunction("yyyy", null, timeZone, local);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
      return new TimeExtractionFunction("M", null, timeZone, local);
    case YEAR:
      return new TimeExtractionFunction("yyyy", null, timeZone, local);
    case WEEK:
      return new TimeExtractionFunction("w", null, timeZone, local);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
      return new TimeExtractionFunction("yyyy", null, timeZone, local);
    case WEEK:
      return new TimeExtractionFunction("w", null, timeZone, local);
    case HOUR:
      return new TimeExtractionFunction("H", null, timeZone, local);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
      return new TimeExtractionFunction("w", null, timeZone, local);
    case HOUR:
      return new TimeExtractionFunction("H", null, timeZone, local);
    case MINUTE:
      return new TimeExtractionFunction("m", null, timeZone, local);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
      return new TimeExtractionFunction("H", null, timeZone, local);
    case MINUTE:
      return new TimeExtractionFunction("m", null, timeZone, local);
    case SECOND:
      return new TimeExtractionFunction("s", null, timeZone, local);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/TimeExtractionFunction.java`
#### Snippet
```java
      return new TimeExtractionFunction("m", null, timeZone, local);
    case SECOND:
      return new TimeExtractionFunction("s", null, timeZone, local);
    default:
      throw new IllegalArgumentException("Granularity [" + granularity + "] is not supported");
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      switch (kind) {
      case LESS_THAN:
        return ImmutableList.of(withNot ? Range.atLeast(value) : Range.lessThan(value));
      case LESS_THAN_OR_EQUAL:
        return ImmutableList.of(withNot ? Range.greaterThan(value) : Range.atMost(value));
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      switch (kind) {
      case LESS_THAN:
        return ImmutableList.of(withNot ? Range.atLeast(value) : Range.lessThan(value));
      case LESS_THAN_OR_EQUAL:
        return ImmutableList.of(withNot ? Range.greaterThan(value) : Range.atMost(value));
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        return ImmutableList.of(withNot ? Range.atLeast(value) : Range.lessThan(value));
      case LESS_THAN_OR_EQUAL:
        return ImmutableList.of(withNot ? Range.greaterThan(value) : Range.atMost(value));
      case GREATER_THAN:
        return ImmutableList.of(withNot ? Range.atMost(value) : Range.greaterThan(value));
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        return ImmutableList.of(withNot ? Range.atLeast(value) : Range.lessThan(value));
      case LESS_THAN_OR_EQUAL:
        return ImmutableList.of(withNot ? Range.greaterThan(value) : Range.atMost(value));
      case GREATER_THAN:
        return ImmutableList.of(withNot ? Range.atMost(value) : Range.greaterThan(value));
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        return ImmutableList.of(withNot ? Range.greaterThan(value) : Range.atMost(value));
      case GREATER_THAN:
        return ImmutableList.of(withNot ? Range.atMost(value) : Range.greaterThan(value));
      case GREATER_THAN_OR_EQUAL:
        return ImmutableList.of(withNot ? Range.lessThan(value) : Range.atLeast(value));
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        return ImmutableList.of(withNot ? Range.greaterThan(value) : Range.atMost(value));
      case GREATER_THAN:
        return ImmutableList.of(withNot ? Range.atMost(value) : Range.greaterThan(value));
      case GREATER_THAN_OR_EQUAL:
        return ImmutableList.of(withNot ? Range.lessThan(value) : Range.atLeast(value));
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        return ImmutableList.of(withNot ? Range.atMost(value) : Range.greaterThan(value));
      case GREATER_THAN_OR_EQUAL:
        return ImmutableList.of(withNot ? Range.lessThan(value) : Range.atLeast(value));
      default:
        if (!withNot) {
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        return ImmutableList.of(withNot ? Range.atMost(value) : Range.greaterThan(value));
      case GREATER_THAN_OR_EQUAL:
        return ImmutableList.of(withNot ? Range.lessThan(value) : Range.atLeast(value));
      default:
        if (!withNot) {
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      default:
        if (!withNot) {
          return ImmutableList.of(Range.closed(value, value));
        }
        return ImmutableList.of(Range.lessThan(value), Range.greaterThan(value));
```

### DataFlowIssue
Argument `value` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
          return ImmutableList.of(Range.closed(value, value));
        }
        return ImmutableList.of(Range.lessThan(value), Range.greaterThan(value));
      }
    }
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      }

      boolean inverted = value1.compareTo(value2) > 0;
      if (!withNot) {
        return ImmutableList.of(
```

### DataFlowIssue
Argument `value2` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      }

      boolean inverted = value1.compareTo(value2) > 0;
      if (!withNot) {
        return ImmutableList.of(
```

### DataFlowIssue
Dereference of `sarg` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      final Sarg<?> sarg = right.getValueAs(Sarg.class);
      ranges = ImmutableList.builder();
      for (Range range : sarg.rangeSet.asRanges()) {
        Range<Long> range2 = RangeSets.copy(range, DruidDateTimeUtils::toLong);
        if (withNot) {
```

### DataFlowIssue
Argument `timeUnit` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
        || valueType.getSqlTypeName() == SqlTypeName.TIMESTAMP) {
      // We use 'UTC' for date/timestamp type as Druid needs timezone information
      return Granularities.createGranularity(timeUnit, "UTC");
    } else if (valueType.getSqlTypeName() == SqlTypeName.TIMESTAMP_WITH_LOCAL_TIME_ZONE) {
      return Granularities.createGranularity(timeUnit, timeZone);
```

### DataFlowIssue
Argument `timeUnit` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidDateTimeUtils.java`
#### Snippet
```java
      return Granularities.createGranularity(timeUnit, "UTC");
    } else if (valueType.getSqlTypeName() == SqlTypeName.TIMESTAMP_WITH_LOCAL_TIME_ZONE) {
      return Granularities.createGranularity(timeUnit, timeZone);
    }
    // Type not recognized
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeEnumerator.java`
#### Snippet
```java
    }
    this.iterator = (results == null) ? Collections.emptyIterator() : results.iterator();
    this.current = null;

    final RelDataTypeFactory typeFactory =
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSort.java`
#### Snippet
```java

    if (fetch != null) {
      return cost.multiplyBy(0.05);
    } else {
      return cost.multiplyBy(0.9);
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSort.java`
#### Snippet
```java
      return cost.multiplyBy(0.05);
    } else {
      return cost.multiplyBy(0.9);
    }
  }
```

### DataFlowIssue
Unboxing of `((RexLiteral) fetch).getValueAs(Long.class)` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeSort.java`
#### Snippet
```java

    if (fetch != null) {
      geodeImplementContext.setLimit(((RexLiteral) fetch).getValueAs(Long.class));
    }
  }
```

### DataFlowIssue
Dereference of `schema.unwrap(GeodeSchema.class)` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeTable.java`
#### Snippet
```java

    private GemFireCache getClientCache() {
      return schema.unwrap(GeodeSchema.class).cache;
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeTable.java`
#### Snippet
```java
    final RelOptCluster cluster = context.getCluster();
    return new GeodeTableScan(cluster, cluster.traitSetOf(GeodeRel.CONVENTION),
        relOptTable, this, null);
  }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeProject.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeProject.java`
#### Snippet
```java
    for (Pair<RexNode, String> pair : getNamedProjects()) {
      final String name = pair.right;
      final String originalName = pair.left.accept(translator);
      fields.put(originalName, name);
    }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRules.java`
#### Snippet
```java
  static String isItem(RexCall call) {
    if (call.getOperator() != SqlStdOperatorTable.ITEM) {
      return null;
    }
    final RexNode op0 = call.getOperands().get(0);
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRules.java`
#### Snippet
```java
      return (String) ((RexLiteral) op1).getValue2();
    }
    return null;
  }

```

### DataFlowIssue
Argument `sort.fetch` might be null
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeRules.java`
#### Snippet
```java
          new GeodeSort(sort.getCluster(), traitSet,
              convert(sort.getInput(), traitSet.replace(RelCollations.EMPTY)),
              sort.getCollation(), sort.fetch);

      call.transformTo(geodeSort);
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeToEnumerableConverter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(.1);
  }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeAggregate.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Argument `aggCall.getName()` might be null
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeAggregate.java`
#### Snippet
```java
          Util.toString(aggCallFieldNames, functionName + "(", ", ", ")");

      aggregateFunctionMap.put(aggCall.getName(), oqlAggregateCall);
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
      // ignore bad value
    }
    return null;
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
      System.out.println("Druid: table names" + data + "; " + url);
    }
    try (InputStream in0 = post(url, data, requestHeaders, 10000, 1800000);
         InputStream in = traceResponse(in0)) {
      final ObjectMapper mapper = new ObjectMapper();
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
  /** Progress through a large fetch. */
  static class Page {
    String pagingIdentifier = null;
    int offset = -1;
    int totalRowCount = 0;
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidConnectionImpl.java`
#### Snippet
```java
        if (parser.nextToken() == JsonToken.START_ARRAY
            && parser.nextToken() == JsonToken.START_OBJECT) {
          page.pagingIdentifier = null;
          page.offset = -1;
          page.totalRowCount = 0;
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `geode/src/main/java/org/apache/calcite/adapter/geode/simple/GeodeSimpleEnumerator.java`
#### Snippet
```java
    } catch (Exception e) {
      e.printStackTrace();
      results = null;
    }
  }
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
        break;
      default:
        return null;
      }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
      case ITEM:
        String item = left.accept(new GeodeRules.RexToGeodeTranslator(this.fieldNames));
        return (item == null) ? null : item + " " + op + " " + quoteCharLiteral(rightLiteral);
      default:
        return null;
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
        return (item == null) ? null : item + " " + op + " " + quoteCharLiteral(rightLiteral);
      default:
        return null;
      }
    }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
        return left.accept(new GeodeRules.RexToGeodeTranslator(this.fieldNames));
      default:
        return null;
      }
    }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `geode/src/main/java/org/apache/calcite/adapter/geode/util/GeodeUtils.java`
#### Snippet
```java
        e.printStackTrace();
      }
      return null;
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisDataType.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### DataFlowIssue
Argument `redisSchema` might be null
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTableFactory.java`
#### Snippet
```java
    final RelProtoDataType protoRowType =
        rowType != null ? RelDataTypeImpl.proto(rowType) : null;
    return RedisTable.create(redisSchema, tableName, operand, protoRowType);
  }
}
```

### DataFlowIssue
Argument `protoRowType` might be null
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTableFactory.java`
#### Snippet
```java
    final RelProtoDataType protoRowType =
        rowType != null ? RelDataTypeImpl.proto(rowType) : null;
    return RedisTable.create(redisSchema, tableName, operand, protoRowType);
  }
}
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisDataFormat.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### DataFlowIssue
Argument `password` might be null
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisSchemaFactory.java`
#### Snippet
```java
    String password = operand.get("password") == null ? null
        : operand.get("password").toString();
    return new RedisSchema(host, port, database, password, tables);
  }
}
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisSchema.java`
#### Snippet
```java
  public final String password;
  public final List<Map<String, Object>> tables;
  private Map<String, Table> tableMap = null;

  RedisSchema(String host,
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisSchema.java`
#### Snippet
```java
      if (jsonCustomTable.name.equals(tableName)) {
        map = jsonCustomTable.operand;
        if (map.get("dataFormat") == null) {
          throw new RuntimeException("dataFormat is null");
        }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisSchema.java`
#### Snippet
```java
  private Table table(String tableName) {
    RedisConfig redisConfig = new RedisConfig(host, port, database, password);
    return RedisTable.create(RedisSchema.this, tableName, redisConfig, null);
  }

```

### DataFlowIssue
Argument `convertedInputExprs.get(groupOrdinal).left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
          final int groupOrdinal = e.getValue().i;
          return converter.convert(rexBuilder,
              convertedInputExprs.get(groupOrdinal).left,
              rexBuilder.makeInputRef(castNonNull(bb.root), groupOrdinal));
        }
```

### DataFlowIssue
Argument `p.left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
        for (Pair<SqlNode, AuxiliaryConverter> p
            : SqlStdOperatorTable.convertGroupToAuxiliaryCalls(call)) {
          addAuxiliaryGroupExpr(p.left, index, p.right);
        }
      }
```

### DataFlowIssue
Argument `p.right` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
        for (Pair<SqlNode, AuxiliaryConverter> p
            : SqlStdOperatorTable.convertGroupToAuxiliaryCalls(call)) {
          addAuxiliaryGroupExpr(p.left, index, p.right);
        }
      }
```

### DataFlowIssue
Argument `e` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
        final boolean caseSensitive = true; // name already fully-qualified
        if (identifier.isStar() && bb.scope instanceof MatchRecognizeScope) {
          e = rexBuilder.makeFieldAccess(e, 0);
        } else {
          e = rexBuilder.makeFieldAccess(e, name, caseSensitive);
```

### DataFlowIssue
Argument `e` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
          e = rexBuilder.makeFieldAccess(e, 0);
        } else {
          e = rexBuilder.makeFieldAccess(e, name, caseSensitive);
        }
      }
```

### DataFlowIssue
Argument `p.left` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    final SqlNameMatcher nameMatcher = catalogReader.nameMatcher();
    for (Pair<String, RexNode> p : Pair.zip(targetColumnNames, columnExprs)) {
      RelDataTypeField field = nameMatcher.field(targetRowType, p.left);
      assert field != null : "column " + p.left + " not found";
      sourceExps.set(field.getIndex(), p.right);
```

### DataFlowIssue
Argument `pair.right` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
                        // TODO: remove requireNonNull when checkerframework issue resolved
                        ensureSqlType(requireNonNull(pair.left, "pair.left").getType(),
                            bb.convertExpression(pair.right)))));
      }
      comparisons.add(rexComparison);
```

### DataFlowIssue
Expression `SqlValidatorUtil.validateExprWithRowType( catalogReader.nameMatcher().isCaseSensitive(), o...` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java

    @Override public SqlNode validateExpression(RelDataType rowType, SqlNode expr) {
      return SqlValidatorUtil.validateExprWithRowType(
          catalogReader.nameMatcher().isCaseSensitive(), opTab,
          typeFactory, rowType, expr).left;
    }

```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
              aggCalls,
              aggCallMapping,
              i -> convertedInputExprs.get(i).left.getType().isNullable());
      aggMapping.put(outerCall, rex);
    }
```

### DataFlowIssue
Argument `value` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      BigDecimal roundedValue =
          NumberUtil.rescaleBigDecimal(
              (BigDecimal) value,
              type.getScale());
      return rexBuilder.makeExactLiteral(
```

### DataFlowIssue
Argument `rightRel` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    }
    final RelNode joinRel =
        createJoin(fromBlackboard, leftRel, rightRel, condition,
            convertJoinType(join.getJoinType()));
    relBuilder.push(joinRel);
```

### DataFlowIssue
Argument `condition` might be null
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    }
    final RelNode joinRel =
        createJoin(fromBlackboard, leftRel, rightRel, condition,
            convertJoinType(join.getJoinType()));
    relBuilder.push(joinRel);
```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      Pair<RelNode, Integer> pair = lookupContext.findRel(offset);
      return rexBuilder.makeRangeReference(
          pair.left.getRowType(),
          pair.right,
          false);
```

### DataFlowIssue
Unboxing of `pair.right` may produce `NullPointerException`
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
      return rexBuilder.makeRangeReference(
          pair.left.getRowType(),
          pair.right,
          false);
    }
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisDataProcess.java`
#### Snippet
```java
  String dataFormat;
  String keyDelimiter;
  RedisDataType dataType = null;
  RedisDataFormat redisDataFormat = null;
  List<LinkedHashMap<String, Object>> fields;
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisDataProcess.java`
#### Snippet
```java
  String keyDelimiter;
  RedisDataType dataType = null;
  RedisDataFormat redisDataFormat = null;
  List<LinkedHashMap<String, Object>> fields;
  private Jedis jedis;
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `spark/src/main/java/org/apache/calcite/adapter/spark/EnumerableToSparkConverter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(.01);
  }

```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `spark/src/main/java/org/apache/calcite/adapter/spark/HttpServer.java`
#### Snippet
```java
        final Server server1 = server;
        port = -1;
        server = null;
        server1.stop();
      } catch (Exception e) {
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkToEnumerableConverter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(.01);
  }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `spark/src/main/java/org/apache/calcite/adapter/spark/JdbcToSparkConverter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(.1);
  }

```

### DataFlowIssue
Dereference of `jdbcConvention` may produce `NullPointerException`
in `spark/src/main/java/org/apache/calcite/adapter/spark/JdbcToSparkConverter.java`
#### Snippet
```java
    final JdbcConvention jdbcConvention =
        (JdbcConvention) child.getConvention();
    String sql = generateSql(jdbcConvention.dialect);
    if (CalciteSystemProperty.DEBUG.value()) {
      System.out.println("[" + sql + "]");
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbSort.java`
#### Snippet
```java
    RelOptCost cost = super.computeSelfCost(planner, mq);
    if (!collation.getFieldCollations().isEmpty()) {
      return cost.multiplyBy(0.05);
    } else {
      return cost;
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbToEnumerableConverter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(.1);
  }

```

### DataFlowIssue
Argument `innodbImplementor.table.getExpression( InnodbTable.InnodbQueryable.class)` might be null
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbToEnumerableConverter.java`
#### Snippet
```java
    final Expression table =
        list.append("table",
            innodbImplementor.table.getExpression(
                InnodbTable.InnodbQueryable.class));
    IndexCondition condition = innodbImplementor.indexCondition;
    final Expression indexName =
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbEnumerator.java`
#### Snippet
```java
  InnodbEnumerator(Iterator<GenericRecord> resultIterator, RelDataType rowType) {
    this.iterator = resultIterator;
    this.current = null;
    this.fieldTypes = rowType.getFieldList();
  }
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbFilter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkRules.java`
#### Snippet
```java
      if (RexMultisetUtil.containsMultiset(program)
          || program.containsAggs()) {
        return null;
      }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkRules.java`
#### Snippet
```java
                builder2,
                new RexToLixTranslator.InputGetterImpl(e_, result.physType),
                null, implementor.getConformance());
        builder2.add(
            Expressions.ifThen(
```

### DataFlowIssue
Argument `pair.right` might be null
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkRules.java`
#### Snippet
```java
          literals.add(
              RexToLixTranslator.translateLiteral(
                  pair.right,
                  pair.left.getType(),
                  typeFactory,
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `spark/src/main/java/org/apache/calcite/adapter/spark/SparkRules.java`
#### Snippet
```java
              RexToLixTranslator.translateLiteral(
                  pair.right,
                  pair.left.getType(),
                  typeFactory,
                  RexImpTable.NullAs.NULL));
```

### DataFlowIssue
Expression `forceIndexName` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbTableScan.java`
#### Snippet
```java

  public String getForceIndexName() {
    return forceIndexName;
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      List<RexNode> pushDownConditions,
      List<RexNode> remainderConditions) {
    return new IndexCondition(fieldNames, indexName, indexColumnNames, null,
        pushDownConditions, remainderConditions, null, null,
        ComparisonOperator.NOP, ComparisonOperator.NOP, ImmutableList.of(),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      List<RexNode> remainderConditions) {
    return new IndexCondition(fieldNames, indexName, indexColumnNames, null,
        pushDownConditions, remainderConditions, null, null,
        ComparisonOperator.NOP, ComparisonOperator.NOP, ImmutableList.of(),
        ImmutableList.of());
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      List<RexNode> remainderConditions) {
    return new IndexCondition(fieldNames, indexName, indexColumnNames, null,
        pushDownConditions, remainderConditions, null, null,
        ComparisonOperator.NOP, ComparisonOperator.NOP, ImmutableList.of(),
        ImmutableList.of());
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      List<Object> rangeQueryUpperKey) {
    return new IndexCondition(ImmutableList.of(), indexName, ImmutableList.of(),
        null, null, null, queryType, pointQueryKey, rangeQueryLowerOp,
        rangeQueryUpperOp, rangeQueryLowerKey, rangeQueryUpperKey);
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      List<Object> rangeQueryUpperKey) {
    return new IndexCondition(ImmutableList.of(), indexName, ImmutableList.of(),
        null, null, null, queryType, pointQueryKey, rangeQueryLowerOp,
        rangeQueryUpperOp, rangeQueryLowerKey, rangeQueryUpperKey);
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      List<Object> rangeQueryUpperKey) {
    return new IndexCondition(ImmutableList.of(), indexName, ImmutableList.of(),
        null, null, null, queryType, pointQueryKey, rangeQueryLowerOp,
        rangeQueryUpperOp, rangeQueryLowerKey, rangeQueryUpperKey);
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      List<String> indexColumnNames,
      QueryType queryType) {
    return new IndexCondition(fieldNames, indexName, indexColumnNames, null,
        null, null, queryType, null, ComparisonOperator.NOP,
        ComparisonOperator.NOP, ImmutableList.of(), ImmutableList.of());
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      QueryType queryType) {
    return new IndexCondition(fieldNames, indexName, indexColumnNames, null,
        null, null, queryType, null, ComparisonOperator.NOP,
        ComparisonOperator.NOP, ImmutableList.of(), ImmutableList.of());
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      QueryType queryType) {
    return new IndexCondition(fieldNames, indexName, indexColumnNames, null,
        null, null, queryType, null, ComparisonOperator.NOP,
        ComparisonOperator.NOP, ImmutableList.of(), ImmutableList.of());
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java
      QueryType queryType) {
    return new IndexCondition(fieldNames, indexName, indexColumnNames, null,
        null, null, queryType, null, ComparisonOperator.NOP,
        ComparisonOperator.NOP, ImmutableList.of(), ImmutableList.of());
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java

  static final IndexCondition EMPTY_CONDITION =
      create(null, null, null, ComparisonOperator.NOP, ComparisonOperator.NOP,
          ImmutableList.of(), ImmutableList.of());

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java

  static final IndexCondition EMPTY_CONDITION =
      create(null, null, null, ComparisonOperator.NOP, ComparisonOperator.NOP,
          ImmutableList.of(), ImmutableList.of());

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/IndexCondition.java`
#### Snippet
```java

  static final IndexCondition EMPTY_CONDITION =
      create(null, null, null, ComparisonOperator.NOP, ComparisonOperator.NOP,
          ImmutableList.of(), ImmutableList.of());

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        return ColumnMetaData.Rep.DOUBLE;
      default:
        return null;
      }
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      writeField(generator, "dimensions", groupByKeyDims);
      writeFieldIf(generator, "virtualColumns",
          virtualColumnList.size() > 0 ? virtualColumnList : null);
      writeFieldIf(generator, "limitSpec", limit);
      writeFieldIf(generator, "filter", jsonFilter);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      writeField(generator, "aggregations", aggregations);
      writeFieldIf(generator, "postAggregations",
          postAggregations.size() > 0 ? postAggregations : null);
      writeField(generator, "intervals", intervals);
      writeFieldIf(generator, "having",
```

### DataFlowIssue
Dereference of `projectGroupSet` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        computeProjectGroupSet(project, groupSet, aggInputRowType, this);

    final List<DimensionSpec> groupByKeyDims = projectGroupSet.left;
    final List<VirtualColumn> virtualColumnList = projectGroupSet.right;
    for (DimensionSpec dim : groupByKeyDims) {
```

### DataFlowIssue
Dereference of `groupByKeyDims` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    final List<DimensionSpec> groupByKeyDims = projectGroupSet.left;
    final List<VirtualColumn> virtualColumnList = projectGroupSet.right;
    for (DimensionSpec dim : groupByKeyDims) {
      aggregateStageFieldNames.add(dim.getOutputName());
    }
```

### DataFlowIssue
Dereference of `aggregations` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    final List<JsonAggregation> aggregations =
        computeDruidJsonAgg(aggCalls, aggNames, project, this);
    for (JsonAggregation jsonAgg : aggregations) {
      aggregateStageFieldNames.add(jsonAgg.name);
    }
```

### DataFlowIssue
Argument `postProjectRexNode` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        final RexNode postProjectRexNode = pair.left;
        String expression = DruidExpressions
              .toDruidExpression(postProjectRexNode, postAggInputRowType, this);
        final String existingFieldName = existingProjects.get(expression);
        if (existingFieldName != null) {
```

### DataFlowIssue
Argument `expression` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
              SqlValidatorUtil.uniquify(pair.right,
                  existingAggFieldsNames, SqlValidatorUtil.EXPR_SUGGESTER);
          postAggs.add(new JsonExpressionPostAgg(uniquelyProjectFieldName, expression, null));
          postProjectDimListBuilder.add(uniquelyProjectFieldName);
          existingAggFieldsNames.add(uniquelyProjectFieldName);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
              SqlValidatorUtil.uniquify(pair.right,
                  existingAggFieldsNames, SqlValidatorUtil.EXPR_SUGGESTER);
          postAggs.add(new JsonExpressionPostAgg(uniquelyProjectFieldName, expression, null));
          postProjectDimListBuilder.add(uniquelyProjectFieldName);
          existingAggFieldsNames.add(uniquelyProjectFieldName);
```

### DataFlowIssue
Argument `jsonFilter` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

    final String timeSeriesQueryString =
        planAsTimeSeries(groupByKeyDims, jsonFilter,
            virtualColumnList, aggregations, postAggs, limit, havingJsonFilter);
    if (timeSeriesQueryString != null) {
```

### DataFlowIssue
Argument `virtualColumnList` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    final String timeSeriesQueryString =
        planAsTimeSeries(groupByKeyDims, jsonFilter,
            virtualColumnList, aggregations, postAggs, limit, havingJsonFilter);
    if (timeSeriesQueryString != null) {
      final String timeExtractColumn = groupByKeyDims.isEmpty()
```

### DataFlowIssue
Argument `havingJsonFilter` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    final String timeSeriesQueryString =
        planAsTimeSeries(groupByKeyDims, jsonFilter,
            virtualColumnList, aggregations, postAggs, limit, havingJsonFilter);
    if (timeSeriesQueryString != null) {
      final String timeExtractColumn = groupByKeyDims.isEmpty()
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    this.converterOperatorMap =
        requireNonNull(converterOperatorMap, "Operator map cannot be null");
    assert isValid(Litmus.THROW, null);
  }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbProject.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      final RexNode filterNode;
      // Type check First
      final RelDataType type = aggCall.getType();
      final SqlTypeName sqlTypeName = type.getSqlTypeName();
      final boolean isNotAcceptedType;
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbProject.java`
#### Snippet
```java
    for (Pair<RexNode, String> pair : getNamedProjects()) {
      final String name = pair.right;
      final String originalName = pair.left.accept(translator);
      fields.put(originalName, name);
    }
```

### DataFlowIssue
Argument `agg.right` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      }
      final JsonAggregation jsonAggregation =
          getJsonAggregation(agg.right, agg.left, filterNode, fieldName,
              expression, druidQuery);
      if (jsonAggregation == null) {
```

### DataFlowIssue
Argument `filterNode` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      }
      final JsonAggregation jsonAggregation =
          getJsonAggregation(agg.right, agg.left, filterNode, fieldName,
              expression, druidQuery);
      if (jsonAggregation == null) {
```

### DataFlowIssue
Argument `fieldName` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      }
      final JsonAggregation jsonAggregation =
          getJsonAggregation(agg.right, agg.left, filterNode, fieldName,
              expression, druidQuery);
      if (jsonAggregation == null) {
```

### DataFlowIssue
Argument `expression` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      final JsonAggregation jsonAggregation =
          getJsonAggregation(agg.right, agg.left, filterNode, fieldName,
              expression, druidQuery);
      if (jsonAggregation == null) {
        return null;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      // Filtered aggregations don't use the "name" and "fieldName" fields directly,
      // but rather use the ones defined in their "aggregation" field.
      super("filtered", aggregation.name, aggregation.fieldName, null);
      this.filter = filter;
      this.aggregation = aggregation;
```

### DataFlowIssue
Method invocation `contains` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      return true;
    }
    if (toTypeName.getFamily().contains(input.getType())) {
      // same type it is okay to push it
      return true;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        writeFieldIf(generator, "filter", jsonFilter);
        writeFieldIf(generator, "virtualColumns",
            virtualColumnList.size() > 0 ? virtualColumnList : null);
        writeField(generator, "columns", columns);
        generator.writeStringField("resultFormat", "compactedList");
```

### DataFlowIssue
Argument `query.getConvention()` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
  public static DruidQuery extendQuery(DruidQuery query, RelNode r) {
    final ImmutableList.Builder<RelNode> builder = ImmutableList.builder();
    return DruidQuery.create(query.getCluster(), r.getTraitSet().replace(query.getConvention()),
        query.getTable(), query.druidTable, query.intervals,
        builder.addAll(query.rels).add(r).build(), query.getOperatorConversionMap());
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
        // plan returning 2 columns.
        // A plan where all extra columns are pruned will be preferred.
        .multiplyBy(
            RelMdUtil.linear(querySpec.fieldNames.size(), 2, 100, 1d, 2d))
        .multiplyBy(getQueryTypeCostMultiplier())
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
            aggregation =
                new JsonAggregation(complexMetric.getMetricType(), name,
                    complexMetric.getMetricName(), null);
          }
          break;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
          matchNulls = new DruidJsonFilter.JsonExpressionFilter(aggExpression + " == null");
        } else {
          matchNulls = DruidJsonFilter.getSelectorFilter(fieldName, null, null);
        }
        aggregation =
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
          matchNulls = new DruidJsonFilter.JsonExpressionFilter(aggExpression + " == null");
        } else {
          matchNulls = DruidJsonFilter.getSelectorFilter(fieldName, null, null);
        }
        aggregation =
```

### DataFlowIssue
Argument `filterRel` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    }

    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
```

### DataFlowIssue
Argument `project` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    }

    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
```

### DataFlowIssue
Argument `groupSet` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    }

    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
```

### DataFlowIssue
Argument `aggCalls` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    }

    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
```

### DataFlowIssue
Argument `aggNames` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    }

    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
```

### DataFlowIssue
Argument `collationIndexes` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
  }
```

### DataFlowIssue
Argument `collationDirections` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
  }
```

### DataFlowIssue
Argument `fetch` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java

    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
  }
```

### DataFlowIssue
Argument `postProject` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
  }

```

### DataFlowIssue
Argument `havingFilter` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    return getQuery(rowType, filterRel, project, groupSet, aggCalls, aggNames,
        collationIndexes, collationDirections, numericCollationBitSetBuilder.build(), fetch,
        postProject, havingFilter);
  }

```

### DataFlowIssue
Unboxing of `p.left` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      ImmutableList.Builder<JsonCollation> colBuilder = ImmutableList.builder();
      for (Pair<Integer, Direction> p : Pair.zip(collationIndexes, collationDirections)) {
        final String dimensionOrder = numericCollationIndexes.get(p.left)
            ? "numeric"
            : "lexicographic";
```

### DataFlowIssue
Argument `fetch` might be null
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
      collations = null;
    }
    return new JsonLimit("default", fetch, collations);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    private JsonCardinalityAggregation(String type, String name,
        List<String> fieldNames) {
      super(type, name, null, null);
      this.fieldNames = fieldNames;
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
    private JsonCardinalityAggregation(String type, String name,
        List<String> fieldNames) {
      super(type, name, null, null);
      this.fieldNames = fieldNames;
    }
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
              EXTRACT_COLUMN_NAME_PREFIX + "_" + requireNonNull(DruidDateTimeUtils
                      .extractGranularity(project, druidQuery.getConnectionConfig().timeZone())
                      .getType().lowerName);
        } else if (project.getKind() == SqlKind.FLOOR) {
          columnPrefix =
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidQuery.java`
#### Snippet
```java
              FLOOR_COLUMN_NAME_PREFIX + "_" + requireNonNull(DruidDateTimeUtils
                      .extractGranularity(project, druidQuery.getConnectionConfig().timeZone())
                      .getType().lowerName);
        } else {
          columnPrefix = "extract";
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbTable.java`
#### Snippet
```java
    Function1<String, Void> addField = fieldName -> {
      RelDataType relDataType =
          rowType.getField(fieldName, true, false).getType();
      fieldInfo.add(fieldName, relDataType).nullable(relDataType.isNullable());
      return null;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbTable.java`
#### Snippet
```java
    final RelOptCluster cluster = context.getCluster();
    return new InnodbTableScan(cluster, cluster.traitSetOf(InnodbRel.CONVENTION),
        relOptTable, this, null, context.getTableHints());
  }

```

### DataFlowIssue
Dereference of `schema.unwrap(InnodbSchema.class)` may produce `NullPointerException`
in `innodb/src/main/java/org/apache/calcite/adapter/innodb/InnodbTable.java`
#### Snippet
```java

    private TableReaderFactory getTableReaderFactory() {
      return schema.unwrap(InnodbSchema.class).tableReaderFactory;
    }

```

### DataFlowIssue
Method invocation `apply` may produce `NullPointerException`
in `linq4j/src/main/java/org/apache/calcite/linq4j/MergeUnionEnumerator.java`
#### Snippet
```java
    // check duplicates
    @SuppressWarnings("dereference.of.nullable")
    final EnumerableDefaults.Wrapped<TSource> wrapped = wrapper.apply(value);
    if (!processed.contains(wrapped)) {
      final TKey key = sortKeySelector.apply(value);
```

### DataFlowIssue
Method invocation `assignableFrom` may produce `NullPointerException`
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java
           || parameter.isPrimitive()
        && argument.isPrimitive()
        && Primitive.of(parameter).assignableFrom(Primitive.of(argument));
  }

```

### DataFlowIssue
Argument `Primitive.of(argument)` might be null
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Types.java`
#### Snippet
```java
           || parameter.isPrimitive()
        && argument.isPrimitive()
        && Primitive.of(parameter).assignableFrom(Primitive.of(argument));
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
  public static SwitchStatement switch_(Expression switchValue,
      SwitchCase... cases) {
    return switch_(switchValue, null, null, toList(cases));
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
  public static SwitchStatement switch_(Expression switchValue,
      SwitchCase... cases) {
    return switch_(switchValue, null, null, toList(cases));
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
  public static SwitchStatement switch_(Expression switchValue,
      Expression defaultBody, SwitchCase... cases) {
    return switch_(switchValue, defaultBody, null, toList(cases));
  }

```

### DataFlowIssue
Argument `udf` might be null
in `piglet/src/main/java/org/apache/calcite/piglet/PigToSqlAggregateRule.java`
#### Snippet
```java
        final SqlAggFunction udf =
            PigRelUdfConverter.getSqlAggFuncForPigUdf(rexCall);
        aggCalls.add(relBuilder.aggregateCall(udf, aggOperands));
      }
    }
```

### DataFlowIssue
Method invocation `intValue` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/PigToSqlAggregateRule.java`
#### Snippet
```java
      final RexLiteral indexLiteral =
          (RexLiteral) multisetProjection.getOperands().get(i);
      columns.add(((BigDecimal) indexLiteral.getValue()).intValue());
    }
    return columns;
```

### DataFlowIssue
Method invocation `isStruct` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelBuilder.java`
#### Snippet
```java
        // The original multiset columns to be flattened, select new flattened columns instead
        RelDataType componentType = inputFields.get(i).getType().getComponentType();
        final int numSubFields = componentType.isStruct() ? componentType.getFieldCount() : 1;
        for (int j = 0; j < numSubFields; j++) {
          finnalCols.add(field(colCount + flattenCount));
```

### DataFlowIssue
Method invocation `getOperator` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelExVisitor.java`
#### Snippet
```java

    String className = op.getFuncSpec().getClassName();
    SqlOperator sqlOp = ((RexCall) stack.peek()).getOperator();
    if (sqlOp instanceof SqlUserDefinedFunction) {
      ScalarFunctionImpl sqlFunc =
```

### DataFlowIssue
Unboxing of `opBinding.getOperandLiteralValue(1, Integer.class)` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      // Project a multiset of single column
      if (opBinding.getOperandCount() == 2) {
        final int fieldNo = opBinding.getOperandLiteralValue(1, Integer.class);
        if (fields.size() == 1) {
          // Corner case: source with only single column, nothing to do.
```

### DataFlowIssue
Unboxing of `opBinding.getOperandLiteralValue(i, Integer.class)` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
      final List<RelDataType> destTypes = new ArrayList<>();
      for (int i = 1; i < opBinding.getOperandCount(); i++) {
        final int fieldNo = opBinding.getOperandLiteralValue(i, Integer.class);
        destNames.add(fields.get(fieldNo).getName());
        destTypes.add(fields.get(fieldNo).getType());
```

### DataFlowIssue
Unboxing of `callBinding.getOperandLiteralValue(i, Integer.class)` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelSqlUdfs.java`
#### Snippet
```java
          }
          final int fieldNo =
              callBinding.getOperandLiteralValue(i, Integer.class);
          // Field number should between 0 and maxFieldNo
          if (fieldNo < 0 || fieldNo > maxFieldNo) {
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
      input = map.get(distinct.source.value);
      builder.push(input);
      builder.distinct(null, -1);
      register(distinct.target.value);
      return this;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
      }
      groupKeys.add(builder.groupKey(keys));
      builder.group(PigRelBuilder.GroupOption.COLLECTED, null, -1, groupKeys);
      register(group.target.value);
      return this;
```

### DataFlowIssue
Method invocation `isStar` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
  private void toSortRex(List<RexNode> nodes,
      Pair<Ast.Identifier, Ast.Direction> pair) {
    if (pair.left.isStar()) {
      for (RexNode node : builder.fields()) {
        switch (pair.right) {
```

### DataFlowIssue
Dereference of `pair.right` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
    if (pair.left.isStar()) {
      for (RexNode node : builder.fields()) {
        switch (pair.right) {
        case DESC:
          node = builder.desc(node);
```

### DataFlowIssue
Dereference of `pair.right` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
    } else {
      RexNode node = toRex(pair.left);
      switch (pair.right) {
      case DESC:
        node = builder.desc(node);
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
        : Pair.zip(nodeList, rowType.getFieldList())) {
      final Ast.Node node = pair.left;
      final RelDataType type = pair.right.getType();
      listBuilder.add(item(node, type));
    }
```

### DataFlowIssue
Argument `node` might be null
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
      final Ast.Node node = pair.left;
      final RelDataType type = pair.right.getType();
      listBuilder.add(item(node, type));
    }
    return listBuilder.build();
```

### DataFlowIssue
Dereference of `splunkSchema` may produce `NullPointerException`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTable.java`
#### Snippet
```java
        String latest, List<String> fieldList) {
      final SplunkSchema splunkSchema = schema.unwrap(SplunkSchema.class);
      return new SplunkQuery<>(splunkSchema.splunkConnection, search,
          earliest, latest, fieldList);
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTable.java`
#### Snippet
```java
        this,
        "search",
        null,
        null,
        relOptTable.getRowType().getFieldNames());
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTable.java`
#### Snippet
```java
        "search",
        null,
        null,
        relOptTable.getRowType().getFieldNames());
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTable.java`
#### Snippet
```java

    @Override public Enumerator<T> enumerator() {
      final SplunkQuery<T> query = createQuery("search", null, null, null);
      return query.enumerator();
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTable.java`
#### Snippet
```java

    @Override public Enumerator<T> enumerator() {
      final SplunkQuery<T> query = createQuery("search", null, null, null);
      return query.enumerator();
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTable.java`
#### Snippet
```java

    @Override public Enumerator<T> enumerator() {
      final SplunkQuery<T> query = createQuery("search", null, null, null);
      return query.enumerator();
    }
```

### DataFlowIssue
Unboxing of `p.right` may produce `NullPointerException`
in `piglet/src/main/java/org/apache/calcite/piglet/PigRelOpVisitor.java`
#### Snippet
```java
      RexNode orderNode =
          PigRelExVisitor.translatePigEx(builder, p.left);
      final boolean ascending = p.right;
      if (!ascending) {
        orderNode = builder.desc(orderNode);
```

### DataFlowIssue
Argument `table.getRowType().getField(field, true, false)` might be null
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkTableScan.java`
#### Snippet
```java
    for (String field : fieldList) {
      // REVIEW: is case-sensitive match what we want here?
      builder.add(table.getRowType().getField(field, true, false));
    }
    return builder.build();
```

### DataFlowIssue
Argument `pair.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
      CalcitePrepare.Context context) {
    final Pair<CalciteSchema, String> pair = schema(context, false, drop.name);
    final boolean existed = pair.left != null && pair.left.removeSubSchema(pair.right);
    if (!existed && !drop.ifExists) {
      throw SqlUtil.newContextException(drop.name.getParserPosition(),
```

### DataFlowIssue
Argument `pair.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    if (!existed && !drop.ifExists) {
      throw SqlUtil.newContextException(drop.name.getParserPosition(),
          RESOURCE.schemaNotFound(pair.right));
    }
  }
```

### DataFlowIssue
Method invocation `plus` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
          create.query.toSqlString(CalciteSqlDialect.DEFAULT).getSql();
      final ViewTableMacro viewTableMacro =
          ViewTable.viewMacro(pair.left.plus(), sql, pair.left.path(null),
              context.getObjectPath(), false);
      final TranslatableTable x = viewTableMacro.apply(ImmutableList.of());
```

### DataFlowIssue
Argument `d.expression` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
          storedBuilder.add(d.name.getSimple(), type);
        }
        b.add(ColumnDef.of(d.expression, type, d.strategy));
      } else if (c.e instanceof SqlIdentifier) {
        final SqlIdentifier id = (SqlIdentifier) c.e;
```

### DataFlowIssue
Method invocation `plus` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
          }
        };
    if (pair.left.plus().getTable(pair.right) != null) {
      // Table exists.
      if (create.ifNotExists) {
```

### DataFlowIssue
Argument `pair.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
          }
        };
    if (pair.left.plus().getTable(pair.right) != null) {
      // Table exists.
      if (create.ifNotExists) {
```

### DataFlowIssue
Expression `v` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
  static Comparable value(SqlNode node) {
    final Comparable v = SqlLiteral.value(node);
    return v instanceof NlsString ? ((NlsString) v).getValue() : v;
  }

```

### DataFlowIssue
Method invocation `plus` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
      CalcitePrepare.Context context) {
    final Pair<CalciteSchema, String> pair = schema(context, true, create.name);
    final SchemaPlus subSchema0 = pair.left.plus().getSubSchema(pair.right);
    if (subSchema0 != null) {
      if (create.ifNotExists) {
```

### DataFlowIssue
Argument `pair.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
      CalcitePrepare.Context context) {
    final Pair<CalciteSchema, String> pair = schema(context, true, create.name);
    final SchemaPlus subSchema0 = pair.left.plus().getSubSchema(pair.right);
    if (subSchema0 != null) {
      if (create.ifNotExists) {
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final Pair<CalciteSchema, String> pair = schema(context, true, create.name);
    final SqlValidator validator = validator(context, false);
    pair.left.add(pair.right, typeFactory -> {
      if (create.dataType != null) {
        return create.dataType.deriveType(validator);
```

### DataFlowIssue
Argument `pair.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final Pair<CalciteSchema, String> pair = schema(context, true, create.name);
    final SqlValidator validator = validator(context, false);
    pair.left.add(pair.right, typeFactory -> {
      if (create.dataType != null) {
        return create.dataType.deriveType(validator);
```

### DataFlowIssue
Dereference of `create.attributeDefs` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
      } else {
        final RelDataTypeFactory.Builder builder = typeFactory.builder();
        for (SqlNode def : create.attributeDefs) {
          final SqlAttributeDefinition attributeDef =
              (SqlAttributeDefinition) def;
```

### DataFlowIssue
Method invocation `getSubSchema` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
        : context.getRootSchema();
    for (String p : path) {
      schema = schema.getSubSchema(p, true);
    }
    return Pair.of(schema, name);
```

### DataFlowIssue
Method invocation `plus` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final Pair<CalciteSchema, String> pair =
        schema(context, true, create.name);
    final SchemaPlus schemaPlus = pair.left.plus();
    for (Function function : schemaPlus.getFunctions(pair.right)) {
      if (function.getParameters().isEmpty()) {
```

### DataFlowIssue
Argument `pair.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
        schema(context, true, create.name);
    final SchemaPlus schemaPlus = pair.left.plus();
    for (Function function : schemaPlus.getFunctions(pair.right)) {
      if (function.getParameters().isEmpty()) {
        if (!create.getReplace()) {
```

### DataFlowIssue
Method invocation `plus` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
      CalcitePrepare.Context context) {
    final Pair<CalciteSchema, String> pair = schema(context, true, create.name);
    if (pair.left.plus().getTable(pair.right) != null) {
      // Materialized view exists.
      if (!create.ifNotExists) {
```

### DataFlowIssue
Argument `pair.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
      CalcitePrepare.Context context) {
    final Pair<CalciteSchema, String> pair = schema(context, true, create.name);
    if (pair.left.plus().getTable(pair.right) != null) {
      // Materialized view exists.
      if (!create.ifNotExists) {
```

### DataFlowIssue
Method invocation `plus` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final Pair<CalciteSchema, String> pair =
        schema(context, true, create.name);
    final SchemaPlus subSchema0 = pair.left.plus().getSubSchema(pair.right);
    if (subSchema0 != null) {
      if (!create.getReplace() && !create.ifNotExists) {
```

### DataFlowIssue
Argument `pair.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final Pair<CalciteSchema, String> pair =
        schema(context, true, create.name);
    final SchemaPlus subSchema0 = pair.left.plus().getSubSchema(pair.right);
    if (subSchema0 != null) {
      if (!create.getReplace() && !create.ifNotExists) {
```

### DataFlowIssue
Method invocation `getSimple` may produce `NullPointerException`
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final Map<String, Object> operandMap = new LinkedHashMap<>();
    for (Pair<SqlIdentifier, SqlNode> option : create.options()) {
      operandMap.put(option.left.getSimple(), value(option.right));
    }
    subSchema =
```

### DataFlowIssue
Argument `option.right` might be null
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
    final Map<String, Object> operandMap = new LinkedHashMap<>();
    for (Pair<SqlIdentifier, SqlNode> option : create.options()) {
      operandMap.put(option.left.getSimple(), value(option.right));
    }
    subSchema =
```

### DataFlowIssue
Expression `aggregate(source.select(selector), maxFunction())` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> BigDecimal max(Enumerable<TSource> source,
      NullableBigDecimalFunction1<TSource> selector) {
    return aggregate(source.select(selector), maxFunction());
  }

```

### DataFlowIssue
Expression `aggregate(source.select(selector), Extensions.DOUBLE_MIN)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> Double min(Enumerable<TSource> source,
      NullableDoubleFunction1<TSource> selector) {
    return aggregate(source.select(selector), Extensions.DOUBLE_MIN);
  }

```

### DataFlowIssue
Expression `aggregate(source.select(selector), Extensions.DOUBLE_MAX)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> Double max(Enumerable<TSource> source,
      NullableDoubleFunction1<TSource> selector) {
    return aggregate(source.select(selector), Extensions.DOUBLE_MAX);
  }

```

### DataFlowIssue
The call to 'requireNonNull' always fails, according to its method contracts
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
        ? ((OrderedQueryable<T>) source)
        : new EnumerableOrderedQueryable<>(
            source, (Class) Object.class, requireNonNull(null, "null"), null);
  }

```

### DataFlowIssue
Expression `aggregate(source.select(selector), Extensions.INTEGER_MAX)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> Integer max(Enumerable<TSource> source,
      NullableIntegerFunction1<TSource> selector) {
    return aggregate(source.select(selector), Extensions.INTEGER_MAX);
  }

```

### DataFlowIssue
Expression `aggregate(source.select(selector), maxFunction())` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> BigDecimal max(Enumerable<TSource> source,
      BigDecimalFunction1<TSource> selector) {
    return aggregate(source.select(selector), maxFunction());
  }

```

### DataFlowIssue
Expression `aggregate(source.select(selector), minFunction())` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> BigDecimal min(Enumerable<TSource> source,
      NullableBigDecimalFunction1<TSource> selector) {
    return aggregate(source.select(selector), minFunction());
  }

```

### DataFlowIssue
Argument `protoRowType` might be null
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTableFactory.java`
#### Snippet
```java
    final RelProtoDataType protoRowType =
        rowType != null ? RelDataTypeImpl.proto(rowType) : null;
    return new CsvScannableTable(source, protoRowType);
  }
}
```

### DataFlowIssue
Argument `protoRowType` might be null
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvStreamTableFactory.java`
#### Snippet
```java
    final RelProtoDataType protoRowType =
        rowType != null ? RelDataTypeImpl.proto(rowType) : null;
    return new CsvStreamScannableTable(source, protoRowType);
  }
}
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvSchema.java`
#### Snippet
```java
    return s.endsWith(suffix)
        ? s.substring(0, s.length() - suffix.length())
        : null;
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvSchema.java`
#### Snippet
```java
    switch (flavor) {
    case TRANSLATABLE:
      return new CsvTranslatableTable(source, null);
    case SCANNABLE:
      return new CsvScannableTable(source, null);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvSchema.java`
#### Snippet
```java
      return new CsvTranslatableTable(source, null);
    case SCANNABLE:
      return new CsvScannableTable(source, null);
    case FILTERABLE:
      return new CsvFilterableTable(source, null);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvSchema.java`
#### Snippet
```java
      return new CsvScannableTable(source, null);
    case FILTERABLE:
      return new CsvFilterableTable(source, null);
    default:
      throw new AssertionError("Unknown flavor " + this.flavor);
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
    SqlTypeName litSqlType = literal.getTypeName();
    if (SqlTypeName.NUMERIC_TYPES.contains(litSqlType)) {
      value = literal.getValue().toString();
    } else if (litSqlType == SqlTypeName.CHAR) {
      value = ((NlsString) literal.getValue()).getValue();
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
      value = literal.getValue().toString();
    } else if (litSqlType == SqlTypeName.CHAR) {
      value = ((NlsString) literal.getValue()).getValue();
      if (like) {
        value = value.replace("%", "*");
```

### DataFlowIssue
Expression `value` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
      value = searchEscape(value);
    }
    return value;
  }

```

### DataFlowIssue
Argument `topProj` might be null
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
    call.transformTo(
        appendSearchString(
            filterString, splunkRel, topProj, bottomProj,
            topRow, null));
  }
```

### DataFlowIssue
Argument `bottomProj` might be null
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
    call.transformTo(
        appendSearchString(
            filterString, splunkRel, topProj, bottomProj,
            topRow, null));
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/SplunkPushDownRule.java`
#### Snippet
```java
        appendSearchString(
            filterString, splunkRel, topProj, bottomProj,
            topRow, null));
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvProjectTableScanRule.java`
#### Snippet
```java
        fields[i] = ((RexInputRef) exp).getIndex();
      } else {
        return null; // not a simple projection
      }
    }
```

### DataFlowIssue
Argument `rd` might be null
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
      e.printStackTrace();
    } finally {
      close(rd);
    }
  }
```

### DataFlowIssue
Argument `field_list` might be null
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
    }

    List<String> fieldList = StringUtils.decodeList(field_list, ',');

    SplunkConnection c =
```

### DataFlowIssue
Argument `search` might be null
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
            Boolean.valueOf(argsMap.get("-print")));
    long start = System.currentTimeMillis();
    c.getSearchResults(search, searchArgs, null, dummy);

    System.out.printf(Locale.ROOT, "received %d results in %dms\n",
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
            Boolean.valueOf(argsMap.get("-print")));
    long start = System.currentTimeMillis();
    c.getSearchResults(search, searchArgs, null, dummy);

    System.out.printf(Locale.ROOT, "received %d results in %dms\n",
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
  @Override public Enumerator<Object> getSearchResultEnumerator(String search,
      Map<String, String> otherArgs, List<String> fieldList) {
    return getSearchResults_(search, otherArgs, fieldList, null);
  }

```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
  public static class CountingSearchResultListener
      implements SearchResultListener {
    String[] fieldNames = null;
    int resultCount = 0;
    final boolean print;
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
            case -1:
              // Singleton null
              this.current = null;
              break;
            default:
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
            source = headerList.indexOf(wantedFields.get(0));
            assert source >= -1;
            sources = null;
          } else if (wantedFields.equals(headerList)) {
            source = -2;
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
            in,
            srl);
        return null;
      }
    } catch (Exception e) {
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
      e.printStackTrace(new PrintWriter(sw));
      LOGGER.warn("{}\n{}", e.getMessage(), sw);
      return srl == null ? Linq4j.emptyEnumerator() : null;
    }
  }
```

### DataFlowIssue
Argument `table.getExpression(CsvTranslatableTable.class)` might be null
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTableScan.java`
#### Snippet
```java
        physType,
        Blocks.toBlock(
            Expressions.call(table.getExpression(CsvTranslatableTable.class),
                "project", implementor.getRootExpression(),
                Expressions.constant(fields))));
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvTableScan.java`
#### Snippet
```java
    // then factor = (1 + 2) / (3 + 2) = 0.6
    return super.computeSelfCost(planner, mq)
        .multiplyBy(((double) fields.length + 2D)
            / ((double) table.getRowType().getFieldCount() + 2D));
  }
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `example/csv/src/main/java/org/apache/calcite/adapter/csv/CsvFilterableTable.java`
#### Snippet
```java
        final int index = ((RexInputRef) left).getIndex();
        if (filterValues[index] == null) {
          filterValues[index] = ((RexLiteral) right).getValue2().toString();
          return true;
        }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java
        }
      }
      implementor.add(null,
          "{$sort: " + Util.toString(keys, "{", ", ", "}") + "}");
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java
    }
    if (offset != null) {
      implementor.add(null,
          "{$skip: " + ((RexLiteral) offset).getValue() + "}");
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java
    }
    if (fetch != null) {
      implementor.add(null,
          "{$limit: " + ((RexLiteral) fetch).getValue() + "}");
    }
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSort.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.05);
  }

```

### DataFlowIssue
Argument `mongoImplementor.table.getExpression( MongoTable.MongoQueryable.class)` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoToEnumerableConverter.java`
#### Snippet
```java
    final Expression table =
        list.append("table",
            mongoImplementor.table.getExpression(
                MongoTable.MongoQueryable.class));
    List<String> opList = Pair.right(mongoImplementor.list);
    final Expression ops =
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoToEnumerableConverter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(.1);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoAggregate.java`
#### Snippet
```java
          + toMongo(aggCall.getAggregation(), inNames, aggCall.getArgList()));
    }
    implementor.add(null,
        "{$group: " + Util.toString(list, "{", ", ", "}") + "}");
    final List<String> fixups;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoAggregate.java`
#### Snippet
```java
    }
    if (!groupSet.isEmpty()) {
      implementor.add(null,
          "{$project: " + Util.toString(fixups, "{", ", ", "}") + "}");
    }
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoProject.java`
#### Snippet
```java
    for (Pair<RexNode, String> pair : getNamedProjects()) {
      final String name = pair.right;
      final String expr = pair.left.accept(translator);
      items.add(expr.equals("'$" + name + "'")
          ? MongoRules.maybeQuote(name) + ": 1"
```

### DataFlowIssue
Argument `name` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoProject.java`
#### Snippet
```java
      final String expr = pair.left.accept(translator);
      items.add(expr.equals("'$" + name + "'")
          ? MongoRules.maybeQuote(name) + ": 1"
          : MongoRules.maybeQuote(name) + ": " + expr);
    }
```

### DataFlowIssue
Argument `name` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoProject.java`
#### Snippet
```java
      items.add(expr.equals("'$" + name + "'")
          ? MongoRules.maybeQuote(name) + ": 1"
          : MongoRules.maybeQuote(name) + ": " + expr);
    }
    final String findString = Util.toString(items, "{", ", ", "}");
```

### DataFlowIssue
Argument `op.left` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoProject.java`
#### Snippet
```java
    final String aggregateString = "{$project: " + findString + "}";
    final Pair<String, String> op = Pair.of(findString, aggregateString);
    implementor.add(op.left, op.right);
  }
}
```

### DataFlowIssue
Argument `op.right` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoProject.java`
#### Snippet
```java
    final String aggregateString = "{$project: " + findString + "}";
    final Pair<String, String> op = Pair.of(findString, aggregateString);
    implementor.add(op.left, op.right);
  }
}
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoProject.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Variable is already assigned to this value
in `example/function/src/main/java/org/apache/calcite/example/maze/Maze.java`
#### Snippet
```java
        break;
      case BACKTRACK:
        move = false;
        do {
          c = list.remove(list.size() - 1);
```

### DataFlowIssue
Argument `credential` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoSchemaFactory.java`
#### Snippet
```java
    }

    return new MongoSchema(host, database, credential, options.build());
  }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTableScan.java`
#### Snippet
```java
    final float f = projectRowType == null ? 1f
        : (float) projectRowType.getFieldCount() / 100f;
    return super.computeSelfCost(planner, mq).multiplyBy(.1 * f);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
            MongoRules.mongoFieldNames(getRowType()));
    String match = translator.translateMatch(condition);
    implementor.add(null, match);
  }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
      switch (node.getKind()) {
      case EQUALS:
        return translateBinary(null, null, (RexCall) node);
      case LESS_THAN:
        return translateBinary("$lt", "$gt", (RexCall) node);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
      switch (node.getKind()) {
      case EQUALS:
        return translateBinary(null, null, (RexCall) node);
      case LESS_THAN:
        return translateBinary("$lt", "$gt", (RexCall) node);
```

### DataFlowIssue
Expression `literal.getValue2()` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java

    private static Object literalValue(RexLiteral literal) {
      return literal.getValue2();
    }

```

### DataFlowIssue
Argument `s.left` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
        Map<String, Object> map2 = builder.map();
        for (Pair<String, RexLiteral> s : entry.getValue()) {
          addPredicate(map2, s.left, literalValue(s.right));
        }
        map.put(entry.getKey(), map2);
```

### DataFlowIssue
Argument `s.right` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
        Map<String, Object> map2 = builder.map();
        for (Pair<String, RexLiteral> s : entry.getValue()) {
          addPredicate(map2, s.left, literalValue(s.right));
        }
        map.put(entry.getKey(), map2);
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoEnumerator.java`
#### Snippet
```java
        return true;
      } else {
        current = null;
        return false;
      }
```

### DataFlowIssue
Method invocation `isInstance` may produce `NullPointerException`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoEnumerator.java`
#### Snippet
```java
      primitive = Primitive.ofBox(clazz);
    }
    if (clazz.isInstance(o)) {
      return o;
    }
```

### DataFlowIssue
Dereference of `schema.unwrap(MongoSchema.class)` may produce `NullPointerException`
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTable.java`
#### Snippet
```java

    private MongoDatabase getMongoDb() {
      return schema.unwrap(MongoSchema.class).mongoDb;
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTable.java`
#### Snippet
```java
    final RelOptCluster cluster = context.getCluster();
    return new MongoTableScan(cluster, cluster.traitSetOf(MongoRel.CONVENTION),
        relOptTable, this, null);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTable.java`
#### Snippet
```java
      //noinspection unchecked
      final Enumerable<T> enumerable =
          (Enumerable<T>) getTable().find(getMongoDb(), null, null, null);
      return enumerable.enumerator();
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTable.java`
#### Snippet
```java
      //noinspection unchecked
      final Enumerable<T> enumerable =
          (Enumerable<T>) getTable().find(getMongoDb(), null, null, null);
      return enumerable.enumerator();
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoTable.java`
#### Snippet
```java
      //noinspection unchecked
      final Enumerable<T> enumerable =
          (Enumerable<T>) getTable().find(getMongoDb(), null, null, null);
      return enumerable.enumerator();
    }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoRules.java`
#### Snippet
```java
      } catch (InvalidRelException e) {
        LOGGER.warn(e.toString());
        return null;
      }
    }
```

### DataFlowIssue
Argument `sort.offset` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoRules.java`
#### Snippet
```java
      return new MongoSort(rel.getCluster(), traitSet,
          convert(sort.getInput(), traitSet.replace(RelCollations.EMPTY)),
          sort.getCollation(), sort.offset, sort.fetch);
    }
  }
```

### DataFlowIssue
Argument `sort.fetch` might be null
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoRules.java`
#### Snippet
```java
      return new MongoSort(rel.getCluster(), traitSet,
          convert(sort.getInput(), traitSet.replace(RelCollations.EMPTY)),
          sort.getCollation(), sort.offset, sort.fetch);
    }
  }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoRules.java`
#### Snippet
```java
  static String isItem(RexCall call) {
    if (call.getOperator() != SqlStdOperatorTable.ITEM) {
      return null;
    }
    final RexNode op0 = call.operands.get(0);
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoRules.java`
#### Snippet
```java
      return (String) ((RexLiteral) op1).getValue2();
    }
    return null;
  }

```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlOperatorFixture.java`
#### Snippet
```java
  // "String right truncation: PC=0 Code=22001"
  String STRING_TRUNC_MESSAGE =
      Bug.FNL3_FIXED ? null : "(?s).*";

  // TODO: Change message when Fnl3Fixed to something like
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlOperatorFixture.java`
#### Snippet
```java
  // "Invalid character for cast: PC=0 Code=22018"
  String INVALID_CHAR_MESSAGE =
      Bug.FNL3_FIXED ? null : "(?s).*";

  // TODO: Change message when Fnl3Fixed to something like
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlOperatorFixture.java`
#### Snippet
```java
  // "Invalid datetime format: PC=0 Code=22007"
  String BAD_DATETIME_MESSAGE =
      Bug.FNL3_FIXED ? null : "(?s).*";

  // Error messages when an invalid time unit is given as
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlOperatorFixture.java`
#### Snippet
```java
  // "Overflow during calculation or cast: PC=0 Code=22003"
  String OUT_OF_RANGE_MESSAGE =
      Bug.FNL3_FIXED ? null : "(?s).*";

  // TODO: Change message when Fnl3Fixed to something like
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/sql/test/SqlOperatorFixture.java`
#### Snippet
```java
  // "Division by zero: PC=0 Code=22012"
  String DIVISION_BY_ZERO_MESSAGE =
      Bug.FNL3_FIXED ? null : "(?s).*";

  // TODO: Change message when Fnl3Fixed to something like
```

### DataFlowIssue
Argument `unresolvedFunction.getSqlIdentifier()` might be null
in `testkit/src/main/java/org/apache/calcite/sql/test/AbstractSqlTester.java`
#### Snippet
```java
                  SqlValidatorUtil.lookupSqlFunctionByID(
                      SqlStdOperatorTable.instance(),
                      unresolvedFunction.getSqlIdentifier(),
                      unresolvedFunction.getFunctionType());
              if (lookup != null) {
```

### DataFlowIssue
Expression `super.visit(call)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/sql/test/AbstractSqlTester.java`
#### Snippet
```java
              return call;
            } else {
              return super.visit(call);
            }
          }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
  @Override public RelNode findBestExp() {
    if (rule != null) {
      matchRecursive(root, null, -1);
    }
    return root;
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
  @Override public void clear() {
    super.clear();
    this.rule = null;
  }

```

### DataFlowIssue
Argument `pair.left` might be null
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
    for (Pair<RelOptRuleOperand, ? extends RelNode> pair
        : Pair.zip(childOperands, childRels)) {
      if (!match(pair.left, pair.right, bindings)) {
        return false;
      }
```

### DataFlowIssue
Argument `pair.right` might be null
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
    for (Pair<RelOptRuleOperand, ? extends RelNode> pair
        : Pair.zip(childOperands, childRels)) {
      if (!match(pair.left, pair.right, bindings)) {
        return false;
      }
```

### DataFlowIssue
Method invocation `plus` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/MockDdlExecutor.java`
#### Snippet
```java
    final Pair<CalciteSchema, String> pair =
        schema(context, true, create.name);
    final SchemaPlus schemaPlus = pair.left.plus();
    for (Function function : schemaPlus.getFunctions(pair.right)) {
      if (function.getParameters().isEmpty()) {
```

### DataFlowIssue
Argument `pair.right` might be null
in `testkit/src/main/java/org/apache/calcite/test/MockDdlExecutor.java`
#### Snippet
```java
        schema(context, true, create.name);
    final SchemaPlus schemaPlus = pair.left.plus();
    for (Function function : schemaPlus.getFunctions(pair.right)) {
      if (function.getParameters().isEmpty()) {
        if (!create.getReplace()) {
```

### DataFlowIssue
Method invocation `plus` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/MockDdlExecutor.java`
#### Snippet
```java
          create.query.toSqlString(CalciteSqlDialect.DEFAULT).getSql();
      final ViewTableMacro viewTableMacro =
          ViewTable.viewMacro(schema.plus(), sql, schema.path(null),
              context.getObjectPath(), false);
      final TranslatableTable x = viewTableMacro.apply(ImmutableList.of());
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/MockDdlExecutor.java`
#### Snippet
```java
    }
    final RelDataType rowType = builder.build();
    schema.add(create.name.getSimple(),
        new MutableArrayTable(create.name.getSimple(),
            RelDataTypeImpl.proto(rowType)));
```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/MockDdlExecutor.java`
#### Snippet
```java
  protected void forEachNameType(SqlCreateTable createTable,
      BiConsumer<SqlIdentifier, SqlDataTypeSpec> consumer) {
    createTable.columnList.forEach(sqlNode -> {
      if (sqlNode instanceof SqlColumnDeclaration) {
        final SqlColumnDeclaration d = (SqlColumnDeclaration) sqlNode;
```

### DataFlowIssue
Method invocation `getMonotonicity` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/SqlValidatorFixture.java`
#### Snippet
```java
          final String field0 = rowType.getFieldList().get(0).getName();
          final SqlMonotonicity monotonicity =
              selectNamespace.getMonotonicity(field0);
          assertThat(monotonicity, matcher);
        });
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/test/ConnectionSpec.java`
#### Snippet
```java
    this.driver = driver;
    this.schema = schema;
    this.catalog = null;
  }
}
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
    assert logOutputStream != null;
    logOutputStream.close();
    logOutputStream = null;

    if (!refFile.exists()) {
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
    diffMasks = "";
    ignorePatterns = "";
    compiledIgnoreMatcher = null;
    compiledDiffMatcher = null;
    String verboseVal =
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
    ignorePatterns = "";
    compiledIgnoreMatcher = null;
    compiledDiffMatcher = null;
    String verboseVal =
        System.getProperty(DiffTestCase.class.getName() + ".verbose");
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
    if (logOutputStream != null) {
      logOutputStream.close();
      logOutputStream = null;
    }
  }
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
    diffMasks = "";
    ignorePatterns = "";
    compiledIgnoreMatcher = null;
    compiledDiffMatcher = null;
  }
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
    ignorePatterns = "";
    compiledIgnoreMatcher = null;
    compiledDiffMatcher = null;
  }

```

### DataFlowIssue
Expression `rel.computeSelfCost(planner, mq)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/RelMetadataFixture.java`
#### Snippet
```java
    final RelMetadataQuery mq = rel.getCluster().getMetadataQuery();
    RelOptPlanner planner = new VolcanoPlanner();
    return rel.computeSelfCost(planner, mq);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
   */
  public static DiffRepository lookup(Class<?> clazz) {
    return lookup(clazz, null, null, 2);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
   */
  public static DiffRepository lookup(Class<?> clazz) {
    return lookup(clazz, null, null, 2);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      throw new RuntimeException("no test case on current call stack");
    } else {
      return null;
    }
  }
```

### DataFlowIssue
Expression `clazz.getResource(rest)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
    String rest = "/" + clazz.getName().replace('.', File.separatorChar)
        + suffix;
    return clazz.getResource(rest);
  }

```

### DataFlowIssue
Expression `found` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      testCaseElement.removeChild(kill);
    }
    return found;
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      List<Pair<String, Element>> map) {
    if (map.isEmpty()) {
      return null;
    }
    // Compute the position that the new element should be if the map were
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
    }
    if (i >= map.size() - 1) {
      return null;
    }
    while (i >= 0 && names.get(i).compareToIgnoreCase(testCaseName) > 0) {
```

### DataFlowIssue
Expression `map.get(i + 1).right` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      --i;
    }
    return map.get(i + 1).right;
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
          if (checkOverride
              && (baseRepository != null)
              && (baseRepository.getTestCaseElement(testCaseName, false, null) != null)
              && !"true".equals(
                  testCase.getAttribute(TEST_CASE_OVERRIDES_ATTR))) {
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
      final String testCaseName,
      String resourceName) {
    Element testCaseElement = getTestCaseElement(testCaseName, true, null);
    if (testCaseElement == null) {
      if (baseRepository != null) {
```

### DataFlowIssue
Method invocation `isEqualNode` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/DiffRepository.java`
#### Snippet
```java
    return list1.size() == list0.size()
        && Pair.zip(list1, list0).stream()
        .allMatch(p -> p.left.isEqualNode(p.right));
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
          return connection;
        }
        return null;
      }
      switch (name) {
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
        connection.unwrap(CalciteConnection.class).getRootSchema()
            .getSubSchema("s")
            .add("my_seq",
                new AbstractTable() {
                  @Override public RelDataType getRowType(
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
      };
    default:
      return null;
    }
  }
```

### DataFlowIssue
Argument `inUrl` might be null
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
      // inUrl = "file:/home/fred/calcite/core/target/test-classes/sql/outer.iq"
      final URL inUrl = QuidemTest.class.getResource("/" + n2u(path));
      inFile = Sources.of(inUrl).file();
      outFile = new File(inFile.getAbsoluteFile().getParent(), u2n("surefire/") + path);
    }
```

### DataFlowIssue
Argument `inUrl` might be null
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
    // inUrl = "file:/home/fred/calcite/core/target/test-classes/sql/agg.iq"
    final URL inUrl = QuidemTest.class.getResource("/" + n2u(first));
    final File firstFile = Sources.of(inUrl).file();
    final int commonPrefixLength = firstFile.getAbsolutePath().length() - first.length();
    final File dir = firstFile.getParentFile();
```

### DataFlowIssue
Expression `m` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/QuidemTest.java`
#### Snippet
```java
      m = null;
    }
    return m;
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/RelOptFixture.java`
#### Snippet
```java
  static final RelOptFixture DEFAULT =
      new RelOptFixture(SqlToRelFixture.TESTER, SqlTestFactory.INSTANCE,
          null, RelSupplier.NONE, null, null,
          ImmutableMap.of(), (f, r) -> r, (f, r) -> r, false, false)
          .withFactory(f ->
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderDynamic.java`
#### Snippet
```java
        MockRelViewTable.create((ViewTable) empModifiableView, this,
            custModifiableViewNames.get(0), custModifiableViewNames.get(1),
            custModifiableViewNames.get(2), false, 20, null);
    registerTable(mockCustViewTable);

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderExtended.java`
#### Snippet
```java
                empModifiableView, this,
            empModifiableViewNames.get(0), empModifiableViewNames.get(1),
            empModifiableViewNames.get(2), false, 20, null);
    registerTable(mockEmpViewTable);

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderExtended.java`
#### Snippet
```java
                empModifiableView2, this,
            empModifiableViewNames2.get(0), empModifiableViewNames2.get(1),
            empModifiableViewNames2.get(2), false, 20, null);
    registerTable(mockEmpViewTable2);

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderExtended.java`
#### Snippet
```java
                empModifiableView3, this,
            empModifiableViewNames3.get(0), empModifiableViewNames3.get(1),
            empModifiableViewNames3.get(2), false, 20, null);
    registerTable(mockEmpViewTable3);

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderExtended.java`
#### Snippet
```java
    final MockTable virtualColumnsTable1 =
        MockTable.create(this, virtualColumnsSchema, "VC_T1", false, 100,
            null, new VirtualColumnsExpressionFactory(), true);
    virtualColumnsTable1.addColumn("A", f.intTypeNull);
    virtualColumnsTable1.addColumn("B", f.bigintType);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderExtended.java`
#### Snippet
```java
    final MockTable virtualColumnsTable2 =
        MockTable.create(this, virtualColumnsSchema, "VC_T2", false, 100,
            null, new VirtualColumnsExpressionFactory(), false);
    virtualColumnsTable2.addColumn("A", f.intTypeNull);
    virtualColumnsTable2.addColumn("B", f.bigintType);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HierarchySchema.java`
#### Snippet
```java
      new Employee(1, 10, "Emp1", 10000, 1000),
      new Employee(2, 10, "Emp2", 8000, 500),
      new Employee(3, 10, "Emp3", 7000, null),
      new Employee(4, 10, "Emp4", 8000, 500),
      new Employee(5, 10, "Emp5", 7000, null),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HierarchySchema.java`
#### Snippet
```java
      new Employee(3, 10, "Emp3", 7000, null),
      new Employee(4, 10, "Emp4", 8000, 500),
      new Employee(5, 10, "Emp5", 7000, null),
  };

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchema.java`
#### Snippet
```java
      new Employee(100, 10, "Bill", 10000, 1000),
      new Employee(200, 20, "Eric", 8000, 500),
      new Employee(150, 10, "Sebastian", 7000, null),
      new Employee(110, 10, "Theodore", 11500, 250),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchema.java`
#### Snippet
```java
          new Location(-122, 38)),
      new Department(30, "Marketing", ImmutableList.of(), new Location(0, 52)),
      new Department(40, "HR", Collections.singletonList(emps[1]), null),
  };
  public final Dependent[] dependents = {
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(1, 10, "Bill", 10000, 1000),
      new Employee(2, 20, "Eric", 8000, 500),
      new Employee(3, 10, "Sebastian", 7000, null),
      new Employee(4, 10, "Theodore", 11500, 250),
      new Employee(5, 10, "Marjorie", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(5, 10, "Marjorie", 10000, 1000),
      new Employee(6, 20, "Guy", 8000, 500),
      new Employee(7, 10, "Dieudonne", 7000, null),
      new Employee(8, 10, "Haroun", 11500, 250),
      new Employee(9, 10, "Sarah", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(9, 10, "Sarah", 10000, 1000),
      new Employee(10, 20, "Gabriel", 8000, 500),
      new Employee(11, 10, "Pierre", 7000, null),
      new Employee(12, 10, "Paul", 11500, 250),
      new Employee(13, 10, "Jacques", 100, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(13, 10, "Jacques", 100, 1000),
      new Employee(14, 20, "Khawla", 8000, 500),
      new Employee(15, 10, "Brielle", 7000, null),
      new Employee(16, 10, "Hyuna", 11500, 250),
      new Employee(17, 10, "Ahmed", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(17, 10, "Ahmed", 10000, 1000),
      new Employee(18, 20, "Lara", 8000, 500),
      new Employee(19, 10, "Capucine", 7000, null),
      new Employee(20, 10, "Michelle", 11500, 250),
      new Employee(21, 10, "Cerise", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(21, 10, "Cerise", 10000, 1000),
      new Employee(22, 80, "Travis", 8000, 500),
      new Employee(23, 10, "Taylor", 7000, null),
      new Employee(24, 10, "Seohyun", 11500, 250),
      new Employee(25, 70, "Helen", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(25, 70, "Helen", 10000, 1000),
      new Employee(26, 50, "Patric", 8000, 500),
      new Employee(27, 10, "Clara", 7000, null),
      new Employee(28, 10, "Catherine", 11500, 250),
      new Employee(29, 10, "Anibal", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(29, 10, "Anibal", 10000, 1000),
      new Employee(30, 30, "Ursula", 8000, 500),
      new Employee(31, 10, "Arturito", 7000, null),
      new Employee(32, 70, "Diane", 11500, 250),
      new Employee(33, 10, "Phoebe", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(33, 10, "Phoebe", 10000, 1000),
      new Employee(34, 20, "Maria", 8000, 500),
      new Employee(35, 10, "Edouard", 7000, null),
      new Employee(36, 110, "Isabelle", 11500, 250),
      new Employee(37, 120, "Olivier", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(37, 120, "Olivier", 10000, 1000),
      new Employee(38, 20, "Yann", 8000, 500),
      new Employee(39, 60, "Ralf", 7000, null),
      new Employee(40, 60, "Emmanuel", 11500, 250),
      new Employee(41, 10, "Berenice", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(41, 10, "Berenice", 10000, 1000),
      new Employee(42, 20, "Kylie", 8000, 500),
      new Employee(43, 80, "Natacha", 7000, null),
      new Employee(44, 100, "Henri", 11500, 250),
      new Employee(45, 90, "Pascal", 10000, 1000),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
      new Employee(45, 90, "Pascal", 10000, 1000),
      new Employee(46, 90, "Sabrina", 8000, 500),
      new Employee(47, 8, "Riyad", 7000, null),
      new Employee(48, 5, "Andy", 11500, 250),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
          new Location(-122, 38)),
      new Department(20, "Marketing", ImmutableList.of(), new Location(0, 52)),
      new Department(30, "HR", Collections.singletonList(emps[1]), null),
      new Department(40, "Administration", Arrays.asList(emps[0], emps[2]),
          new Location(-122, 38)),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
          new Location(-122, 38)),
      new Department(50, "Design", ImmutableList.of(), new Location(0, 52)),
      new Department(60, "IT", Collections.singletonList(emps[1]), null),
      new Department(70, "Production", Arrays.asList(emps[0], emps[2]),
          new Location(-122, 38)),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
          new Location(-122, 38)),
      new Department(80, "Finance", ImmutableList.of(), new Location(0, 52)),
      new Department(90, "Accounting", Collections.singletonList(emps[1]), null),
      new Department(100, "Research", Arrays.asList(emps[0], emps[2]),
          new Location(-122, 38)),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/hr/HrSchemaBig.java`
#### Snippet
```java
          new Location(-122, 38)),
      new Department(110, "Maintenance", ImmutableList.of(), new Location(0, 52)),
      new Department(120, "Client Support", Collections.singletonList(emps[1]), null),
  };
}
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/bookstore/BookstoreSchema.java`
#### Snippet
```java
      new Author(3,
          "Homer",
          new Place(null,
              "Ionia",
              "Greece"),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
      new IntAndString(1, "A"), new IntAndString(2,
      "B"), new IntAndString(2, "C"),
      new IntAndString(3, null)};

  public final IntAndString[] bools = {
```

### DataFlowIssue
Method invocation `isNumeric` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
    switch (Primitive.flavor(type)) {
    case BOX:
      return Primitive.ofBox(type).isNumeric();
    case PRIMITIVE:
      return Primitive.of(type).isNumeric();
```

### DataFlowIssue
Method invocation `isNumeric` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
      return Primitive.ofBox(type).isNumeric();
    case PRIMITIVE:
      return Primitive.of(type).isNumeric();
    default:
      return Number.class.isAssignableFrom(type); // e.g. BigDecimal
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java

  public final Employee[] prefixEmps = {
      new Employee(1, 10, "A", 0f, null),
      new Employee(2, 10, "Ab", 0f, null),
      new Employee(3, 10, "Abc", 0f, null),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
  public final Employee[] prefixEmps = {
      new Employee(1, 10, "A", 0f, null),
      new Employee(2, 10, "Ab", 0f, null),
      new Employee(3, 10, "Abc", 0f, null),
      new Employee(4, 10, "Abd", 0f, null),
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
      new Employee(1, 10, "A", 0f, null),
      new Employee(2, 10, "Ab", 0f, null),
      new Employee(3, 10, "Abc", 0f, null),
      new Employee(4, 10, "Abd", 0f, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
      new Employee(2, 10, "Ab", 0f, null),
      new Employee(3, 10, "Abc", 0f, null),
      new Employee(4, 10, "Abd", 0f, null),
  };

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
  public final IntAndString[] bools = {
      new IntAndString(1, "T"), new IntAndString(2,
      "F"), new IntAndString(3, null)};

  private static boolean isNumeric(Class type) {
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Integer.MAX_VALUE, Long.MAX_VALUE, Float.MAX_VALUE,
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Integer.MAX_VALUE, Long.MAX_VALUE, Float.MAX_VALUE,
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Integer.MAX_VALUE, Long.MAX_VALUE, Float.MAX_VALUE,
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Integer.MAX_VALUE, Long.MAX_VALUE, Float.MAX_VALUE,
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Integer.MAX_VALUE, Long.MAX_VALUE, Float.MAX_VALUE,
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Integer.MAX_VALUE, Long.MAX_VALUE, Float.MAX_VALUE,
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Integer.MAX_VALUE, Long.MAX_VALUE, Float.MAX_VALUE,
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Integer.MAX_VALUE, Long.MAX_VALUE, Float.MAX_VALUE,
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/schemata/catchall/CatchallSchema.java`
#### Snippet
```java
          Double.MAX_VALUE,
          null, null, null, null, null, null, null, null,
          null, null, null, null, null, null),
  };

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
    MockTable empNullables20View =
        new MockViewTable(this, salesSchema.getCatalogName(), salesSchema.getName(),
            "EMPNULLABLES_20", false, 600, empNullablesTable, m0, null,
            NullInitializerExpressionFactory.INSTANCE) {
          @Override public RexNode getConstraint(RexBuilder rexBuilder,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
  private void registerTableEmpDefaults(MockSchema salesSchema, Fixture fixture) {
    final MockTable empDefaultsTable =
        MockTable.create(this, salesSchema, "EMPDEFAULTS", false, 14, null,
            new EmpInitializerExpressionFactory(), false);
    empDefaultsTable.addColumn("EMPNO", fixture.intType, true);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
    MockTable productsTemporalTable =
        MockTable.create(this, salesSchema, "PRODUCTS_TEMPORAL", false, 200D,
            null, NullInitializerExpressionFactory.INSTANCE, true);
    productsTemporalTable.addColumn("PRODUCTID", fixture.intType);
    productsTemporalTable.addColumn("NAME", fixture.varchar20Type);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
    MockTable emp20View =
        new MockViewTable(this, salesSchema.getCatalogName(), salesSchema.getName(),
            "EMP_20", false, 600, empTable, m0, null,
            NullInitializerExpressionFactory.INSTANCE) {
          @Override public RexNode getConstraint(RexBuilder rexBuilder,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
    // Register "EMP" table.
    final MockTable empTable =
        MockTable.create(this, salesSchema, "EMP", false, 14, null,
            countingInitializerExpressionFactory, false);
    registerTableEmp(empTable, fixture);
```

### DataFlowIssue
Expression `super.getNamedType(typeName)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReaderSimple.java`
#### Snippet
```java
      return addressType;
    } else {
      return super.getNamedType(typeName);
    }
  }
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraSort.java`
#### Snippet
```java
    RelOptCost cost = super.computeSelfCost(planner, mq);
    if (!collation.getFieldCollations().isEmpty()) {
      return cost.multiplyBy(0.05);
    } else {
      return cost;
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
      asQueryable(QueryProvider queryProvider, SchemaPlus schema,
          String tableName) {
        return null;
      }

```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
  private void registerNestedSchema(MockSchema parentSchema, MockSchema schema) {
    rootSchema.getSubSchema(parentSchema.getName(), true)
        .add(schema.name, new AbstractSchema());
  }

```

### DataFlowIssue
Argument `getViewPath()` might be null
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
          RelProtoDataType protoRowType, ImmutableIntList newColumnMapping) {
        return new MockModifiableViewTable(getElementType(), protoRowType,
            getViewSql(), getSchemaPath(), getViewPath(), extendedTable,
            getTablePath(), constraint, newColumnMapping);
      }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
        SqlNameMatchers.withCaseSensitive(caseSensitive),
        ImmutableList.of(PREFIX, ImmutableList.of()),
        typeFactory, null);
  }

```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
        SqlValidatorUtil.getSchema(rootSchema,
            schemaPath, SqlNameMatchers.withCaseSensitive(true));
    schema.add(Util.last(names), relProtoDataType);
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
    public static MockTable create(MockCatalogReader catalogReader,
        MockSchema schema, String name, boolean stream, double rowCount) {
      return create(catalogReader, schema, name, stream, rowCount, null);
    }

```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
      // For testing
      return call.getKind() != SqlKind.MAX
              && (parent.getKind() == SqlKind.SELECT || parent.getKind() == SqlKind.FILTER);
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
    public static MockTable create(MockCatalogReader catalogReader,
        MockSchema schema, String name, boolean stream, double rowCount, double maxRowCount) {
      return create(catalogReader, schema, name, stream, rowCount, null, maxRowCount);
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
    public static MockTable create(MockCatalogReader catalogReader,
        List<String> names, boolean stream, double rowCount) {
      return new MockTable(catalogReader, names, stream, false, rowCount, null,
          NullInitializerExpressionFactory.INSTANCE, ImmutableList.of(), Double.POSITIVE_INFINITY);
    }
```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
        // For testing
        return call.getKind() != SqlKind.MAX
            && (parent.getKind() == SqlKind.SELECT || parent.getKind() == SqlKind.FILTER);
      }
    }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java

      @Override public Type getElementType() {
        return null;
      }

```

### DataFlowIssue
Method invocation `removeTable` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
        SqlValidatorUtil.getSchema(rootSchema,
            schemaPath, SqlNameMatchers.withCaseSensitive(true));
    schema.removeTable(tableName);
    schema.add(tableName, table);
  }
```

### DataFlowIssue
Argument `parsed.table` might be null
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
        return new MockModifiableViewTable(elementType,
            RelDataTypeImpl.proto(parsed.rowType), viewSql, schemaPath, viewPath,
            parsed.table, Schemas.path(schema.root(), parsed.tablePath),
            parsed.constraint, parsed.columnMapping);
      }
```

### DataFlowIssue
Argument `parsed.tablePath` might be null
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
        return new MockModifiableViewTable(elementType,
            RelDataTypeImpl.proto(parsed.rowType), viewSql, schemaPath, viewPath,
            parsed.table, Schemas.path(schema.root(), parsed.tablePath),
            parsed.constraint, parsed.columnMapping);
      }
```

### DataFlowIssue
Argument `parsed.constraint` might be null
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
            RelDataTypeImpl.proto(parsed.rowType), viewSql, schemaPath, viewPath,
            parsed.table, Schemas.path(schema.root(), parsed.tablePath),
            parsed.constraint, parsed.columnMapping);
      }
    }
```

### DataFlowIssue
Argument `parsed.columnMapping` might be null
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
            RelDataTypeImpl.proto(parsed.rowType), viewSql, schemaPath, viewPath,
            parsed.table, Schemas.path(schema.root(), parsed.tablePath),
            parsed.constraint, parsed.columnMapping);
      }
    }
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
      @Override public Expression getExpression(SchemaPlus schema,
          String tableName, Class clazz) {
        return null;
      }

```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java
        SqlValidatorUtil.getSchema(rootSchema,
            schemaPath, SqlNameMatchers.withCaseSensitive(true));
    schema.add(tableName, table);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/catalog/MockCatalogReader.java`
#### Snippet
```java

      @Override public Collection getModifiableCollection() {
        return null;
      }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraToEnumerableConverter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(.1);
  }

```

### DataFlowIssue
Method invocation `getExpression` may produce `NullPointerException`
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraToEnumerableConverter.java`
#### Snippet
```java
        list.append("table",
            Objects.requireNonNull(
                cassandraImplementor.table.getExpression(
                    CassandraTable.CassandraQueryable.class)));
    final Expression predicates =
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
        assertPrepare(connection, sql, this.materializationsEnabled,
            checker, null));
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
    @Override public Collection<org.apache.calcite.schema.Function>
      getFunctions(String name) {
      return null;
    }

```

### DataFlowIssue
Argument `foodmart.unwrap(CalciteSchema.class)` might be null
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      foodmart = addSchemaIfNotExists(rootSchema, SchemaSpec.JDBC_FOODMART);
      foodmart.add(schema.schemaName,
          Lattice.create(foodmart.unwrap(CalciteSchema.class),
              "select 1 from \"foodmart\".\"sales_fact_1997\" as s\n"
                  + "join \"foodmart\".\"time_by_day\" as t using (\"time_id\")\n"
```

### DataFlowIssue
Argument `AggregateFunctionImpl.create(UnionOperation.class)` might be null
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      ModelHandler.addFunctions(rootSchema, null, emptyPath,
          SqlSpatialTypeFunctions.class.getName(), "*", true);
      rootSchema.add("ST_UNION", AggregateFunctionImpl.create(UnionOperation.class));
      rootSchema.add("ST_ACCUM", AggregateFunctionImpl.create(AccumOperation.class));
      rootSchema.add("ST_COLLECT", AggregateFunctionImpl.create(CollectOperation.class));
```

### DataFlowIssue
Argument `AggregateFunctionImpl.create(AccumOperation.class)` might be null
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
          SqlSpatialTypeFunctions.class.getName(), "*", true);
      rootSchema.add("ST_UNION", AggregateFunctionImpl.create(UnionOperation.class));
      rootSchema.add("ST_ACCUM", AggregateFunctionImpl.create(AccumOperation.class));
      rootSchema.add("ST_COLLECT", AggregateFunctionImpl.create(CollectOperation.class));
      final SchemaPlus s =
```

### DataFlowIssue
Argument `AggregateFunctionImpl.create(CollectOperation.class)` might be null
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      rootSchema.add("ST_UNION", AggregateFunctionImpl.create(UnionOperation.class));
      rootSchema.add("ST_ACCUM", AggregateFunctionImpl.create(AccumOperation.class));
      rootSchema.add("ST_COLLECT", AggregateFunctionImpl.create(CollectOperation.class));
      final SchemaPlus s =
          rootSchema.add(schema.schemaName, new AbstractSchema());
```

### DataFlowIssue
Argument `tableFunction` might be null
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      TableFunction tableFunction =
          TableFunctionImpl.create(Smalls.SimpleTableFunction.class, "eval");
      aux.add("TBLFUN", tableFunction);
      final String simpleSql = "select *\n"
          + "from (values\n"
```

### DataFlowIssue
Method invocation `unwrap` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
          if (aClass.isAssignableFrom(SqlDialect.class)
              || aClass.isAssignableFrom(DataSource.class)) {
            return salesTable.unwrap(aClass);
          }
          return super.unwrap(aClass);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        new ConnectionSpec(
            "jdbc:postgresql://localhost/foodmart?user=foodmart&password=foodmart&searchpath=foodmart",
            "foodmart", "foodmart", "org.postgresql.Driver", "foodmart"), null);

    public final ConnectionSpec foodmart;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        new ConnectionSpec("jdbc:h2:" + CalciteSystemProperty.TEST_DATASET_PATH.value()
            + "/h2/target/foodmart;user=foodmart;password=foodmart",
            "foodmart", "foodmart", "org.h2.Driver", "foodmart"), null),
    MYSQL(
        new ConnectionSpec("jdbc:mysql://localhost/foodmart", "foodmart",
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
    ORACLE(
        new ConnectionSpec("jdbc:oracle:thin:@localhost:1521:XE", "foodmart",
            "foodmart", "oracle.jdbc.OracleDriver", "FOODMART"), null),
    POSTGRESQL(
        new ConnectionSpec(
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
     * expression matching the given string. */
    public final AssertQuery convertContains(final String expected) {
      return convertMatches(checkRel(expected, null));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
     * {@link #failsAtValidation} with {@code null}. */
    public AssertQuery failsAtValidation() {
      return failsAtValidation(null);
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

    @Override public Schema getSubSchema(String name) {
      return null;
    }

```

### DataFlowIssue
Method invocation `unwrap` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
          if (aClass.isAssignableFrom(SqlDialect.class)
              || aClass.isAssignableFrom(DataSource.class)) {
            return salesTable.unwrap(aClass);
          }
          return super.unwrap(aClass);
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

      @Override public Schema.TableType getJdbcTableType() {
        return null;
      }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
          currentCause = currentCause.getCause();
        }
        return null;
      }
    };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

    static JavaSql fromJava(String java) {
      return new JavaSql(java, null);
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        if (consumer == null) {
          assertQuery(connection, sql, limit, materializationsEnabled,
              hooks, null, null, null);
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        if (consumer == null) {
          assertQuery(connection, sql, limit, materializationsEnabled,
              hooks, null, null, null);
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        if (consumer == null) {
          assertQuery(connection, sql, limit, materializationsEnabled,
              hooks, null, null, null);
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
              hooks, null, null, null, consumer);
        }
      });
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
              hooks, null, null, null, consumer);
        }
      });
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
              hooks, null, null, null, consumer);
        }
      });
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

    @Override public RelProtoDataType getType(String name) {
      return null;
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
          assertQuery(connection, sql, limit, materializationsEnabled,
              hooks, null, checkUpdateCount(count), null));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
          assertQuery(connection, sql, limit, materializationsEnabled,
              hooks, null, checkUpdateCount(count), null));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

    public AssertQuery planHasSql(String expected) {
      return planContains(null, JavaSql.fromSql(expected));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

    public AssertQuery planContains(String expected) {
      return planContains(null, JavaSql.fromJava(expected));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection -> {
        assertQuery(connection, sql, limit, materializationsEnabled,
            newHooks, null, null, null);
        predicate1.accept(list);
      });
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection -> {
        assertQuery(connection, sql, limit, materializationsEnabled,
            newHooks, null, null, null);
        predicate1.accept(list);
      });
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection -> {
        assertQuery(connection, sql, limit, materializationsEnabled,
            newHooks, null, null, null);
        predicate1.accept(list);
      });
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
        assertQuery(connection, sql, limit, materializationsEnabled,
            hooks, null, null, checkException(message)));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
        assertQuery(connection, sql, limit, materializationsEnabled,
            hooks, null, null, checkException(message)));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
        assertQuery(connection, sql, limit, materializationsEnabled,
            hooks, null, null, checkValidationException(optionalMessage)));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
        assertQuery(connection, sql, limit, materializationsEnabled,
            hooks, null, null, checkValidationException(optionalMessage)));
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java

    @Override public Schema snapshot(SchemaVersion version) {
      return null;
    }
  };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
        assertQuery(connection, sql, limit, false,
            hooks, checkResultType(expected), null, null));
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      return withConnection(connection ->
        assertQuery(connection, sql, limit, false,
            hooks, checkResultType(expected), null, null));
    }

```

### DataFlowIssue
Method invocation `addThread` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      for (Pair<Hook, Consumer> hook : hooks) {
        //noinspection unchecked
        closer.add(hook.left.addThread(hook.right));
      }
      PreparedStatement statement = connection.prepareStatement(sql);
```

### DataFlowIssue
Argument `hook.right` might be null
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      for (Pair<Hook, Consumer> hook : hooks) {
        //noinspection unchecked
        closer.add(hook.left.addThread(hook.right));
      }
      PreparedStatement statement = connection.prepareStatement(sql);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
    MYSQL(
        new ConnectionSpec("jdbc:mysql://localhost/foodmart", "foodmart",
            "foodmart", "com.mysql.jdbc.Driver", "foodmart"), null),
    ORACLE(
        new ConnectionSpec("jdbc:oracle:thin:@localhost:1521:XE", "foodmart",
```

### DataFlowIssue
Method invocation `isInstance` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
          for (Pair<Object, Class> pair
              : Pair.zip(args, (Class[]) method1.getParameterTypes())) {
            if (!pair.right.isInstance(pair.left)) {
              continue loop;
            }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        final Consumer<RelNode> checker) {
      return withConnection(connection ->
        assertPrepare(connection, sql, materializationsEnabled, null, checker));
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
    @Override public Expression getExpression(@Nullable SchemaPlus parentSchema,
        String name) {
      return null;
    }

```

### DataFlowIssue
Method invocation `addThread` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      for (Pair<Hook, Consumer> hook : hooks) {
        //noinspection unchecked
        closer.add(hook.left.addThread(hook.right));
      }
      Statement statement = connection.createStatement();
```

### DataFlowIssue
Argument `hook.right` might be null
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
      for (Pair<Hook, Consumer> hook : hooks) {
        //noinspection unchecked
        closer.add(hook.left.addThread(hook.right));
      }
      Statement statement = connection.createStatement();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        if (consumer == null) {
          assertQuery(connection, sql, limit, materializationsEnabled,
              hooks, checker, null, null);
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        if (consumer == null) {
          assertQuery(connection, sql, limit, materializationsEnabled,
              hooks, checker, null, null);
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
              hooks, checker, null, null, consumer);
        }
      });
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        } else {
          assertPrepare(connection, sql, limit, materializationsEnabled,
              hooks, checker, null, null, consumer);
        }
      });
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraProject.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraFilter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Expression `currentRowField(0)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraEnumerator.java`
#### Snippet
```java
    if (fieldTypes.size() == 1) {
      // If we just have one field, produce it directly
      return currentRowField(0);
    } else {
      // Build an array with all fields in this row
```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
            return true;
          } else {
            current = null;
            return false;
          }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
        @Override public Enumerator<T> enumerator() {
          //noinspection unchecked
          return (Enumerator<T>) enumeratorImpl(null);
        }
      };
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java

    public Enumerator<Object[]> enumerator() {
      return enumeratorImpl(null);
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    @SemiStrict public static String eval(@Parameter(name = "s") String s) {
      if (s.length() == 8) {
        return null;
      }
      return s;
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
    @SemiStrict public static String eval(@Parameter(name = "s") String s) {
      if (s == null || s.length() == 4) {
        return null;
      }
      return s;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
          SchemaPlus schema, String tableName) {
        BaseQueryable<IntString> queryable =
            new BaseQueryable<IntString>(null, IntString.class, null) {
              @Override public Enumerator<IntString> enumerator() {
                return new Enumerator<IntString>() {
```

### DataFlowIssue
Unboxing of `fx.getArgumentValueAs(1, Integer.class)` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
      THREAD_DIGEST.set(b.toString());
      this.initY = fx.isArgumentConstant(1)
          ? fx.getArgumentValueAs(1, Integer.class)
          : 100;
    }
```

### DataFlowIssue
Method invocation `isInstance` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchEnumerators.java`
#### Snippet
```java
      primitive = Primitive.ofBox(clazz);
    }
    if (clazz.isInstance(o)) {
      return o;
    }
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchAggregate.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Argument `aggCall.getName()` might be null
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchAggregate.java`
#### Snippet
```java
      }

      implementor.addAggregation(aggCall.getName(), aggregation.toString());
    }
  }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSchemaFactory.java`
#### Snippet
```java
          try {
            // Free resources allocated by this RestClient
            notice.getValue().close();
          } catch (IOException ex) {
            LOGGER.warn("Could not close RestClient {}", notice.getValue(), ex);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java
    ElasticsearchJson.SearchHits hits =
        new ElasticsearchJson.SearchHits(res.searchHits().total(), result.stream()
            .map(r -> new ElasticsearchJson.SearchHit("_id", r, null))
            .collect(Collectors.toList()));

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java
    final RelOptCluster cluster = context.getCluster();
    return new ElasticsearchTableScan(cluster, cluster.traitSetOf(ElasticsearchRel.CONVENTION),
        relOptTable, this, null);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTable.java`
#### Snippet
```java

    @Override public Enumerator<T> enumerator() {
      return null;
    }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchFilter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSort.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.05);
  }

```

### DataFlowIssue
Unboxing of `((RexLiteral) offset).getValueAs(Long.class)` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSort.java`
#### Snippet
```java

    if (offset != null) {
      implementor.offset(((RexLiteral) offset).getValueAs(Long.class));
    }

```

### DataFlowIssue
Unboxing of `((RexLiteral) fetch).getValueAs(Long.class)` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSort.java`
#### Snippet
```java

    if (fetch != null) {
      implementor.fetch(((RexLiteral) fetch).getValueAs(Long.class));
    }
  }
```

### DataFlowIssue
Argument `implementor.table.getExpression( ElasticsearchTable.ElasticsearchQueryable.class)` might be null
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchToEnumerableConverter.java`
#### Snippet
```java
    final Expression table =
        block.append("table",
            implementor.table.getExpression(
                ElasticsearchTable.ElasticsearchQueryable.class));
    final Expression ops = block.append("ops", Expressions.constant(implementor.list));
    final Expression sort = block.append("sort", constantArrayList(implementor.sort, Pair.class));
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchToEnumerableConverter.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(.1);
  }

```

### DataFlowIssue
Expression `((RexLiteral) call.getOperands().get(1)).getValueAs(String.class)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/MapProjectionFieldVisitor.java`
#### Snippet
```java
  @Override public String visitCall(RexCall call) {
    if (call.op == SqlStdOperatorTable.ITEM) {
      return ((RexLiteral) call.getOperands().get(1)).getValueAs(String.class);
    }
    return super.visitCall(call);
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchTableScan.java`
#### Snippet
```java
      RelMetadataQuery mq) {
    final float f = projectRowType == null ? 1f : (float) projectRowType.getFieldCount() / 100f;
    return super.computeSelfCost(planner, mq).multiplyBy(.1 * f);
  }

```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchProject.java`
#### Snippet
```java
    for (Pair<RexNode, String> pair: getNamedProjects()) {
      final String name = pair.right;
      final String expr = pair.left.accept(translator);

      // "select *" present?
```

### DataFlowIssue
Argument `name` might be null
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchProject.java`
#### Snippet
```java

      // "select *" present?
      hasSelectStar |= ElasticsearchConstants.isSelectAll(name);

      if (ElasticsearchRules.isItem(pair.left)) {
```

### DataFlowIssue
Method invocation `multiplyBy` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchProject.java`
#### Snippet
```java
  @Override public @Nullable RelOptCost computeSelfCost(RelOptPlanner planner,
      RelMetadataQuery mq) {
    return super.computeSelfCost(planner, mq).multiplyBy(0.1);
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
  private static String isItemCall(RexCall call) {
    if (call.getOperator() != SqlStdOperatorTable.ITEM) {
      return null;
    }
    final RexNode op0 = call.getOperands().get(0);
```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
      return (String) ((RexLiteral) op1).getValue2();
    }
    return null;
  }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
            agg.getAggCallList());
      } catch (InvalidRelException e) {
        return null;
      }
    }
```

### DataFlowIssue
Argument `sort.offset` might be null
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
      return new ElasticsearchSort(relNode.getCluster(), traitSet,
        convert(sort.getInput(), traitSet.replace(RelCollations.EMPTY)), sort.getCollation(),
        sort.offset, sort.fetch);
    }
  }
```

### DataFlowIssue
Argument `sort.fetch` might be null
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchRules.java`
#### Snippet
```java
      return new ElasticsearchSort(relNode.getCluster(), traitSet,
        convert(sort.getInput(), traitSet.replace(RelCollations.EMPTY)), sort.getCollation(),
        sort.offset, sort.fetch);
    }
  }
```

### DataFlowIssue
Expression `missingValueForType(name)` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchMapping.java`
#### Snippet
```java
    private Datatype(final String name) {
      this.name = Objects.requireNonNull(name, "name");
      this.missingValue = missingValueForType(name);
    }

```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchMapping.java`
#### Snippet
```java

    Optional<JsonNode> missingValue() {
      return Optional.ofNullable(missingValue);
    }

```

### DataFlowIssue
Expression `RexLiteral.stringValue(literal)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
        return booleanValue();
      } else if (isString()) {
        return RexLiteral.stringValue(literal);
      } else {
        return rawValue();
```

### DataFlowIssue
Method invocation `doubleValue` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    double doubleValue() {
      return ((Number) literal.getValue()).doubleValue();
    }

```

### DataFlowIssue
`null` is assigned to a variable that is annotated with @NotNull
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    private NamedFieldExpression() {
      this.name = null;
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
        throw new UnsupportedOperationException("Can't handle partial QueryExpression: " + e);
      }
      return e != null ? e.builder() : null;
    } catch (Throwable e) {
      Throwables.propagateIfPossible(e, UnsupportedOperationException.class);
```

### DataFlowIssue
Expression `RexLiteral.stringValue(literal)` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    String stringValue() {
      return RexLiteral.stringValue(literal);
    }

```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    long longValue() {
      return ((Number) literal.getValue()).longValue();
    }

```

### DataFlowIssue
Expression `literal.getValue()` might evaluate to null but is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    Object rawValue() {
      return literal.getValue();
    }
  }
```

### DataFlowIssue
Expression `RexLiteral.stringValue(literal)` might evaluate to null but is assigned to a variable that is annotated with @NotNull
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java

    private NamedFieldExpression(RexLiteral literal) {
      this.name = literal == null ? null : RexLiteral.stringValue(literal);
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
      }

      return null;
    }

```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java

    Optional<String> scrollId() {
      return Optional.ofNullable(scrollId);
    }

```

### DataFlowIssue
Argument `key` might be null
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
      }

      return new Bucket(key, name, parseAggregations(parser, node));
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
      final int index = path.indexOf('.');
      if (index == -1) {
        return null;
      }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
      }

      return null;
    }

```

### DataFlowIssue
`null` is returned by the method declared as @DefaultQualifier
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchJson.java`
#### Snippet
```java
          // return first element (or null)
          Iterator<?> iter = ((Iterable<?>) field).iterator();
          return iter.hasNext() ? iter.next() : null;
        }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        "{\"foo\":100}", "VARCHAR(2000)");
    f.checkString("json_query('{\"foo\":100}', 'lax $.foo' null on empty)",
        null, "VARCHAR(2000)");
    f.checkFails("json_query('{\"foo\":100}', 'lax $.foo' error on empty)",
        "(?s).*Empty result of JSON_QUERY function is not allowed.*", true);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    // path error test
    f.checkString("json_query('{\"foo\":100}', 'invalid $.foo' null on error)",
        null, "VARCHAR(2000)");
    f.checkFails("json_query('{\"foo\":100}', 'invalid $.foo' error on error)",
        "(?s).*Illegal jsonpath spec.*", true);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

    f.checkString("json_query('{\"foo\":100}', 'strict $.foo1' null on error)",
        null, "VARCHAR(2000)");
    f.checkFails("json_query('{\"foo\":100}', 'strict $.foo1' error on error)",
        "(?s).*No results for path: \\$\\['foo1'\\].*", true);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

    f.checkString("json_query('{\"foo\":100}', 'strict $.foo' null on error)",
        null, "VARCHAR(2000)");
    f.checkFails("json_query('{\"foo\":100}', 'strict $.foo' error on error)",
        "(?s).*Strict jsonpath mode requires array or object value, "
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    // array wrapper test
    f.checkString("json_query('{\"foo\":100}', 'strict $.foo' without wrapper)",
        null, "VARCHAR(2000)");
    f.checkString("json_query('{\"foo\":100}', 'strict $.foo' without array wrapper)",
        null, "VARCHAR(2000)");
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        null, "VARCHAR(2000)");
    f.checkString("json_query('{\"foo\":100}', 'strict $.foo' without array wrapper)",
        null, "VARCHAR(2000)");
    f.checkString("json_query('{\"foo\":100}', 'strict $.foo' with wrapper)",
        "[100]", "VARCHAR(2000)");
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.enableTypeCoercion(false).checkFails("json_query(^null^, 'lax $')",
        "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_query(null, 'lax $')", null, "VARCHAR(2000)");
    f.checkNull("json_query(cast(null as varchar), 'lax $')");
  }
```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    try (Hook.Closeable ignore = pair.right) {
      f.checkScalar("CAST(CURRENT_DATE AS VARCHAR(30))",
          dateString.substring(0, 10),
          "VARCHAR(30) NOT NULL");
      f.checkScalar("CURRENT_DATE",
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.check("values 1 < cast(null as integer) or sqrt(-4) = -2",
        SqlTests.BOOLEAN_TYPE_CHECKER, SqlTests.ANY_PARAMETER_CHECKER,
        new ValueOrExceptionResultChecker(null, INVALID_ARG_FOR_POWER,
            CODE_2201F));

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.check("values 1 < cast(null as integer) or sqrt(4) = -2",
        SqlTests.BOOLEAN_TYPE_CHECKER, SqlTests.ANY_PARAMETER_CHECKER,
        new ValueOrExceptionResultChecker(null, INVALID_ARG_FOR_POWER,
            CODE_2201F));

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        "100", "VARCHAR(2000)");
    f.checkString("json_value('{\"foo\":100}', 'lax $.foo1' null on empty)",
        null, "VARCHAR(2000)");
    f.checkFails("json_value('{\"foo\":100}', 'lax $.foo1' error on empty)",
        "(?s).*Empty result of JSON_VALUE function is not allowed.*", true);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        "empty", "VARCHAR(2000)");
    f.checkString("json_value('{\"foo\":{}}', 'lax $.foo' null on empty)",
        null, "VARCHAR(2000)");
    f.checkFails("json_value('{\"foo\":{}}', 'lax $.foo' error on empty)",
        "(?s).*Empty result of JSON_VALUE function is not allowed.*", true);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    // path error test
    f.checkString("json_value('{\"foo\":100}', 'invalid $.foo' null on error)",
        null, "VARCHAR(2000)");
    f.checkFails("json_value('{\"foo\":100}', 'invalid $.foo' error on error)",
        "(?s).*Illegal jsonpath spec.*", true);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        "100", "VARCHAR(2000)");
    f.checkString("json_value('{\"foo\":100}', 'strict $.foo1' null on error)",
        null, "VARCHAR(2000)");
    f.checkFails("json_value('{\"foo\":100}', 'strict $.foo1' error on error)",
        "(?s).*No results for path: \\$\\['foo1'\\].*", true);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        "empty", "VARCHAR(2000)");
    f.checkString("json_value('{\"foo\":{}}', 'strict $.foo' null on error)",
        null, "VARCHAR(2000)");
    f.checkFails("json_value('{\"foo\":{}}', 'strict $.foo' error on error)",
        "(?s).*Strict jsonpath mode requires scalar value, "
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        .checkFails("json_value(^null^, 'strict $')",
            "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_value(null, 'strict $')", null, "VARCHAR(2000)");
    f.checkNull("json_value(cast(null as varchar), 'strict $')");
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.enableTypeCoercion(false).checkFails("json_remove(^null^, '$')",
        "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_remove(null, '$')", null, "VARCHAR(2000)");
    f.checkNull("json_remove(cast(null as varchar), '$')");
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.enableTypeCoercion(false).checkFails("json_storage_size(^null^)",
        "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_storage_size(null)", null, "INTEGER");
    f.checkNull("json_storage_size(cast(null as varchar))");
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.enableTypeCoercion(false).checkFails("json_pretty(^null^)",
        "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_pretty(null)", null, "VARCHAR(2000)");
    f.checkNull("json_pretty(cast(null as varchar))");
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        "1", "INTEGER");
    f.checkString("json_length('{\"foo\":100}', 'lax $.foo1')",
        null, "INTEGER");

    // strict test
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.enableTypeCoercion(false).checkFails("json_length(^null^)",
        "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_length(null)", null, "INTEGER");
    f.checkNull("json_length(cast(null as varchar))");
  }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        Pattern.compile(pair.left + "[0-9][0-9]:[0-9][0-9]"),
        "TIMESTAMP(0) NOT NULL");
    pair.right.close();
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.enableTypeCoercion(false).checkFails("json_keys(^null^)",
        "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_keys(null)", null, "VARCHAR(2000)");
    f.checkNull("json_keys(cast(null as varchar))");
  }
```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

    f.checkScalar("CAST(CURRENT_TIME AS VARCHAR(30))",
        Pattern.compile(pair.left.substring(11) + "[0-9][0-9]:[0-9][0-9]"),
        "VARCHAR(30) NOT NULL");
    f.checkScalar("CURRENT_TIME",
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        Pattern.compile(pair.left.substring(11) + "[0-9][0-9]:[0-9][0-9]"),
        "TIME(0) NOT NULL");
    pair.right.close();
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        this.values.add(new ValueType(type, value));
      }
      this.values.add(new ValueType(type, null));
    }
  }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        Pattern.compile(pair.left + "[0-9][0-9]:[0-9][0-9]"),
        "TIMESTAMP(0) NOT NULL");
    pair.right.close();
  }

```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java

    f.checkScalar("CAST(LOCALTIME AS VARCHAR(30))",
        Pattern.compile(pair.left.substring(11) + "[0-9][0-9]:[0-9][0-9]"),
        "VARCHAR(30) NOT NULL");
    f.checkScalar("LOCALTIME",
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        Pattern.compile(pair.left.substring(11) + "[0-9][0-9]:[0-9][0-9]"),
        "TIME(0) NOT NULL");
    pair.right.close();
  }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        "1", "INTEGER");
    f.checkString("json_depth('null')",
        null, "INTEGER");
    f.checkString("json_depth(cast(null as varchar(1)))",
        null, "INTEGER");
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        null, "INTEGER");
    f.checkString("json_depth(cast(null as varchar(1)))",
        null, "INTEGER");
    f.checkString("json_depth('[10, true]')",
        "2", "INTEGER");
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.enableTypeCoercion(false).checkFails("json_depth(^null^)",
        "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_depth(null)", null, "INTEGER");
    f.checkNull("json_depth(cast(null as varchar))");
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
    f.enableTypeCoercion(false).checkFails("json_type(^null^)",
        "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_type(null)", null, "VARCHAR(20)");
    f.checkNull("json_type(cast(null as varchar))");
  }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
            + "'lax $' unknown on error)", "(?s).*Illegal use of 'NULL'.*", false);
    f.checkString("json_exists(null, 'lax $' unknown on error)",
        null, "BOOLEAN");
    f.checkNull("json_exists(cast(null as varchar), "
        + "'lax $.foo1' unknown on error)");
```

### DataFlowIssue
Method invocation `getMessage` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
      assertThat(throwables, hasSize(messages.size()));
      for (Pair<? extends Throwable, String> pair : Pair.zip(throwables, messages)) {
        assertThat(pair.left.getMessage(), containsString(pair.right));
      }
    };
```

### DataFlowIssue
Argument `opt.getValue()` might be null
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
    assertThat(writer.format(opt.getName()), equalTo("\"SCHEMA\""));
    writer = new SqlPrettyWriter();
    assertThat(writer.format(opt.getValue()), equalTo("TRUE"));
    writer = new SqlPrettyWriter();
    assertThat(writer.format(opt),
```

### DataFlowIssue
@Nullable method 'visit' always returns a non-null value
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java

    final SqlNode shuttled = sqlNode.accept(new SqlShuttle() {
      @Override public @Nullable SqlNode visit(final SqlCall call) {
        // Handler always creates a new copy of 'call'
        CallCopyingArgHandler argHandler = new CallCopyingArgHandler(call, true);
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
      }
    });
    assertThat(toLinux(shuttled.toString()), is(expected));
  }

```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
        + "FROM `EMP`\n"
        + "/*+ `OPTIONS`('key1' = 'val1') */";
    assertThat(toLinux(shuttled.toString()), is(expected));
  }

```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
        + "SELECT *\n"
        + "FROM `EMPS`";
    assertThat(str0, is(toLinux(sqlNodeVisited0.toString())));

    final String sql1 = "insert into emps select empno from emps";
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
        + "SELECT `EMPNO`\n"
        + "FROM `EMPS`";
    assertThat(str1, is(toLinux(sqlNodeVisited1.toString())));
  }

```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `testkit/src/main/java/org/apache/calcite/sql/parser/SqlParserTest.java`
#### Snippet
```java
    });
    assertNotSame(sqlNodeVisited, sqlNode);
    assertThat(sqlNodeVisited.getKind(), is(SqlKind.INSERT));
  }

```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'AggCallBinding' is still used
in `core/src/main/java/org/apache/calcite/rel/core/Aggregate.java`
#### Snippet
```java

    @Deprecated // to be removed before 2.0
    public AggCallBinding(RelDataTypeFactory typeFactory,
        SqlAggFunction aggFunction, List<RelDataType> operands, int groupCount,
        boolean filter) {
```

### DeprecatedIsStillUsed
Deprecated member 'FieldInfo' is still used
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactory.java`
#### Snippet
```java
   */
  @Deprecated // to be removed before 2.0
  interface FieldInfo {
    /**
     * Returns the number of fields.
```

### DeprecatedIsStillUsed
Deprecated member 'FieldInfoBuilder' is still used
in `core/src/main/java/org/apache/calcite/rel/type/RelDataTypeFactory.java`
#### Snippet
```java
  @Deprecated
  @SuppressWarnings("deprecation")
  class FieldInfoBuilder extends Builder implements FieldInfo {
    public FieldInfoBuilder(RelDataTypeFactory typeFactory) {
      super(typeFactory);
```

### DeprecatedIsStillUsed
Deprecated member 'MetadataFactoryImpl' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/MetadataFactoryImpl.java`
#### Snippet
```java
 */
@Deprecated // to be removed before 2.0
public class MetadataFactoryImpl implements MetadataFactory {
  @SuppressWarnings("unchecked")
  public static final UnboundMetadata<@Nullable Metadata> DUMMY = (rel, mq) -> null;
```

### DeprecatedIsStillUsed
Deprecated member 'NoHandler' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/JaninoRelMetadataProvider.java`
#### Snippet
```java
   * */
  @Deprecated
  public static class NoHandler extends MetadataHandlerProvider.NoHandler {
    public NoHandler(Class<? extends RelNode> relClass) {
      super(relClass);
```

### DeprecatedIsStillUsed
Deprecated member 'metadataProvider' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQueryBase.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  public final @Nullable JaninoRelMetadataProvider metadataProvider;

  //~ Static fields/initializers ---------------------------------------------
```

### DeprecatedIsStillUsed
Deprecated member 'RelMetadataQueryBase' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQueryBase.java`
#### Snippet
```java
  //~ Constructors -----------------------------------------------------------
  @Deprecated // to be removed before 2.0
  protected RelMetadataQueryBase(@Nullable JaninoRelMetadataProvider metadataProvider) {
    this((MetadataHandlerProvider) metadataProvider);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'initialHandler' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQueryBase.java`
#### Snippet
```java

  @Deprecated
  protected static <H> H initialHandler(Class<H> handlerClass) {
    return handlerClass.cast(
        Proxy.newProxyInstance(RelMetadataQuery.class.getClassLoader(),
```

### DeprecatedIsStillUsed
Deprecated member 'handlerMap' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/ReflectiveRelMetadataProvider.java`
#### Snippet
```java
  private final Class<? extends Metadata> metadataClass0;
  @Deprecated // to be removed before 2.0
  private final ImmutableMultimap<Method, MetadataHandler> handlerMap;
  private final Class<? extends MetadataHandler<?>> handlerClass;
  private final ImmutableList<MetadataHandler<?>> handlers;
```

### DeprecatedIsStillUsed
Deprecated member 'map' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/ReflectiveRelMetadataProvider.java`
#### Snippet
```java
  //~ Instance fields --------------------------------------------------------
  @Deprecated // to be removed before 2.0
  private final ConcurrentMap<Class<RelNode>, UnboundMetadata> map;
  @Deprecated // to be removed before 2.0
  private final Class<? extends Metadata> metadataClass0;
```

### DeprecatedIsStillUsed
Deprecated member 'reflectiveSource' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/ReflectiveRelMetadataProvider.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  private static RelMetadataProvider reflectiveSource(
      final MetadataHandler target, final ImmutableList<Method> methods,
      final Class<? extends MetadataHandler<?>> handlerClass) {
```

### DeprecatedIsStillUsed
Deprecated member 'metadataClass0' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/ReflectiveRelMetadataProvider.java`
#### Snippet
```java
  private final ConcurrentMap<Class<RelNode>, UnboundMetadata> map;
  @Deprecated // to be removed before 2.0
  private final Class<? extends Metadata> metadataClass0;
  @Deprecated // to be removed before 2.0
  private final ImmutableMultimap<Method, MetadataHandler> handlerMap;
```

### DeprecatedIsStillUsed
Deprecated member 'CachingRelMetadataProvider' is still used
in `core/src/main/java/org/apache/calcite/rel/metadata/CachingRelMetadataProvider.java`
#### Snippet
```java
 */
@Deprecated // to be removed before 2.0
public class CachingRelMetadataProvider implements RelMetadataProvider {
  //~ Instance fields --------------------------------------------------------

```

### DeprecatedIsStillUsed
Deprecated member 'type' is still used
in `core/src/main/java/org/apache/calcite/sql/fun/SqlHistogramAggFunction.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  private final RelDataType type;

  //~ Constructors -----------------------------------------------------------
```

### DeprecatedIsStillUsed
Deprecated member 'type' is still used
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSumAggFunction.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  private final RelDataType type;

  //~ Constructors -----------------------------------------------------------
```

### DeprecatedIsStillUsed
Deprecated member 'type' is still used
in `core/src/main/java/org/apache/calcite/sql/fun/SqlSingleValueAggFunction.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  private final RelDataType type;

  //~ Constructors -----------------------------------------------------------
```

### DeprecatedIsStillUsed
Deprecated member 'setTimeUnitCodes' is still used
in `core/src/main/java/org/apache/calcite/sql/parser/SqlAbstractParserImpl.java`
#### Snippet
```java
   */
  @Deprecated // to be removed before 2.0
  public void setTimeUnitCodes(Map<String, TimeUnit> timeUnitCodes) {
  }

```

### DeprecatedIsStillUsed
Deprecated member 'setWindowDeclListNewline' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setWindowDeclListNewline(boolean windowDeclListNewline) {
    this.config = config.withWindowDeclListNewline(windowDeclListNewline);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'isKeywordsLowerCase' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  @Override public boolean isKeywordsLowerCase() {
    return config.keywordsLowerCase();
  }
```

### DeprecatedIsStillUsed
Deprecated member 'isQuoteAllIdentifiers' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  @Override public boolean isQuoteAllIdentifiers() {
    return config.quoteAllIdentifiers();
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setCaseClausesOnNewLines' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setCaseClausesOnNewLines(boolean caseClausesOnNewLines) {
    this.config = config.withCaseClausesOnNewLines(caseClausesOnNewLines);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setQuoteAllIdentifiers' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setQuoteAllIdentifiers(boolean quoteAllIdentifiers) {
    this.config = config.withQuoteAllIdentifiers(quoteAllIdentifiers);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setWindowNewline' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setWindowNewline(boolean windowNewline) {
    this.config = config.withWindowNewline(windowNewline);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setKeywordsLowerCase' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setKeywordsLowerCase(boolean keywordsLowerCase) {
    this.config = config.withKeywordsLowerCase(keywordsLowerCase);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setIndentation' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setIndentation(int indentation) {
    this.config = config.withIndentation(indentation);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setLineLength' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setLineLength(int lineLength) {
    this.config = config.withLineLength(lineLength);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setClauseStartsLine' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setClauseStartsLine(boolean clauseStartsLine) {
    this.config = config.withClauseStartsLine(clauseStartsLine);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setWhereListItemsOnSeparateLines' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setWhereListItemsOnSeparateLines(
      boolean whereListItemsOnSeparateLines) {
    this.config =
```

### DeprecatedIsStillUsed
Deprecated member 'setAlwaysUseParentheses' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setAlwaysUseParentheses(boolean alwaysUseParentheses) {
    this.config = config.withAlwaysUseParentheses(alwaysUseParentheses);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'setSelectListItemsOnSeparateLines' is still used
in `core/src/main/java/org/apache/calcite/sql/pretty/SqlPrettyWriter.java`
#### Snippet
```java

  @Deprecated
  public void setSelectListItemsOnSeparateLines(boolean b) {
    this.config = config.withSelectListItemsOnSeparateLines(b);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'getAlias' is still used
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
   * or {@link #alias(SqlNode, int)} if {@code ordinal} is non-negative. */
  @Deprecated // to be removed before 2.0
  public static @Nullable String getAlias(SqlNode node, int ordinal) {
    return alias_(node, ordinal);
  }
```

### DeprecatedIsStillUsed
Deprecated member 'createPrepareFactory' is still used
in `core/src/main/java/org/apache/calcite/jdbc/Driver.java`
#### Snippet
```java
   * or override {@link #createPrepare()}. */
  @Deprecated // to be removed before 2.0
  protected Function0<CalcitePrepare> createPrepareFactory() {
    return CalcitePrepare.DEFAULT_FACTORY;
  }
```

### DeprecatedIsStillUsed
Deprecated member 'metadataFactory' is still used
in `core/src/main/java/org/apache/calcite/plan/RelOptCluster.java`
#### Snippet
```java
  private RelMetadataProvider metadataProvider;
  @Deprecated // to be removed before 2.0
  private MetadataFactory metadataFactory;
  private @Nullable HintStrategyTable hintStrategies;
  private final RelTraitSet emptyTraitSet;
```

### DeprecatedIsStillUsed
Deprecated member 'registerMetadataProviders' is still used
in `core/src/main/java/org/apache/calcite/plan/hep/HepPlanner.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  @Override public void registerMetadataProviders(List<RelMetadataProvider> list) {
    list.add(0, new HepRelMetadataProvider());
  }
```

### DeprecatedIsStillUsed
Deprecated member 'getTableNamespace' is still used
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorScope.java`
#### Snippet
```java
   * {@link #resolveTable(List, SqlNameMatcher, Path, Resolved)}. */
  @Deprecated // to be removed before 2.0
  @Nullable SqlValidatorNamespace getTableNamespace(List<String> names);

  /**
```

### DeprecatedIsStillUsed
Deprecated member 'findQualifyingTableName' is still used
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorScope.java`
#### Snippet
```java
   * {@link #findQualifyingTableNames(String, SqlNode, SqlNameMatcher)} */
  @Deprecated // to be removed before 2.0
  Pair<String, SqlValidatorNamespace> findQualifyingTableName(String columnName,
      SqlNode ctx);

```

### DeprecatedIsStillUsed
Deprecated member 'canHandle' is still used
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableTableScan.java`
#### Snippet
```java
   */
  @Deprecated
  public static boolean canHandle(Table table) {
    if (table instanceof TransientTable) {
      // CALCITE-3673: TransientTable can't be implemented with Enumerable
```

### DeprecatedIsStillUsed
Deprecated member 'createParserConfig' is still used
in `core/src/main/java/org/apache/calcite/prepare/CalcitePrepareImpl.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  protected SqlParser.ConfigBuilder createParserConfig() {
    return SqlParser.configBuilder();
  }
```

### DeprecatedIsStillUsed
Deprecated member 'createParser' is still used
in `core/src/main/java/org/apache/calcite/prepare/CalcitePrepareImpl.java`
#### Snippet
```java

  @Deprecated // to be removed before 2.0
  protected SqlParser createParser(String sql,
      SqlParser.ConfigBuilder parserConfig) {
    return createParser(sql, parserConfig.build());
```

### DeprecatedIsStillUsed
Deprecated member 'Config' is still used
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraRules.java`
#### Snippet
```java
    /** Deprecated in favor of CassandraLimitRuleConfig. */
    @Deprecated
    public interface Config extends CassandraLimitRuleConfig { }

    /** Rule configuration. */
```

### DeprecatedIsStillUsed
Deprecated member 'Config' is still used
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraRules.java`
#### Snippet
```java
    /** Deprecated in favor of {@link CassandraFilterRuleConfig}. */
    @Deprecated
    public interface Config extends CassandraFilterRuleConfig { }

    /** Rule configuration. */
```

### DeprecatedIsStillUsed
Deprecated member 'Config' is still used
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraRules.java`
#### Snippet
```java
    /** Deprecated in favor of CassandraSortRuleConfig. */
    @Deprecated
    public interface Config extends CassandraSortRuleConfig { }

    /** Rule configuration. */
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `core/src/main/java/org/apache/calcite/util/ImmutableIntList.java`
#### Snippet
```java

    @Override public Iterator<Integer> iterator() {
      return Collections.<Integer>emptyList().iterator();
    }

```

## RuleId[id=SwitchStatementWithTooFewBranches]
### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `pig/src/main/java/org/apache/calcite/adapter/pig/PigJoin.java`
#### Snippet
```java
   */
  private String getPigJoinType() {
    switch (getJoinType()) {
    case INNER:
      return "";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/hint/NodeTypeHintPredicate.java`
#### Snippet
```java

  @Override public boolean apply(RelHint hint, RelNode rel) {
    switch (this.nodeType) {
    // Hints of SET_VAR type never propagate.
    case SET_VAR:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/type/TimeFrames.java`
#### Snippet
```java
      assert from.core() == this;
      assert to.core() == this;
      switch (name) {
      case "MONTH":
        return BigInteger.valueOf(from.monthEpoch())
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/LoptSemiJoinOptimizer.java`
#### Snippet
```java
    // ignore non-equality filters where the operands are not
    // RexInputRefs
    switch (joinFilter.getKind()) {
    case EQUALS:
      break;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/LoptMultiJoin.java`
#### Snippet
```java
        if (leftBitmap.cardinality() == 1) {
          // give higher weight to equi-joins
          switch (joinFilter.getKind()) {
          case EQUALS:
            weight = 3;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/ProjectAggregateMergeRule.java`
#### Snippet
```java
    final RexShuttle shuttle = new RexShuttle() {
      @Override public RexNode visitCall(RexCall call) {
        switch (call.getKind()) {
        case CASE:
          // Do we have "CASE(IS NOT NULL($0), CAST($0):INTEGER NOT NULL, 0)"?
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/AggregateJoinTransposeRule.java`
#### Snippet
```java
  private static void populateEquivalences(Map<Integer, BitSet> equivalence,
      RexNode predicate) {
    switch (predicate.getKind()) {
    case EQUALS:
      RexCall call = (RexCall) predicate;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        final RexNode op0 = call.operands.get(0);
        final RexNode op1 = call.operands.get(1);
        switch (op0.getKind()) {
        case LITERAL:
          assert op0 instanceof RexLiteral;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
          break;
        }
        switch (op1.getKind()) {
        case LITERAL:
          assert op1 instanceof RexLiteral;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java

    boolean isExtractCall(RexNode e) {
      switch (e.getKind()) {
      case EXTRACT:
        final RexCall call = (RexCall) e;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/DateRangeRules.java`
#### Snippet
```java
        return ImmutableList.of(); // a bit more efficient
      }
      switch (calls.getFirst().getKind()) {
      case AND:
        return super.visitList(exprs, update);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/SubQueryRemoveRule.java`
#### Snippet
```java
    builder.push(e.rel);
    builder.project(builder.alias(builder.literal(true), "i"));
    switch (logic) {
    case TRUE:
      // Handles queries with single EXISTS in filter condition:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/SubQueryRemoveRule.java`
#### Snippet
```java
            .map(pair -> builder.equals(pair.left, RexUtil.shift(pair.right, refOffset)))
            .collect(Collectors.toList());
    switch (logic) {
    case TRUE:
      builder.join(JoinRelType.INNER, builder.and(conditions), variablesSet);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rules/materialize/MaterializedViewRule.java`
#### Snippet
```java
    List<RexNode> residualPreds = new ArrayList<>();
    for (RexNode e : RelOptUtil.conjunctions(pred)) {
      switch (e.getKind()) {
      case EQUALS:
        RexCall eqCall = (RexCall) e;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdCollation.java`
#### Snippet
```java
    final int nullComparison = fieldCollation.nullDirection.nullComparison;
    final int x = fieldCollation.getFieldIndex();
    switch (fieldCollation.direction) {
    case ASCENDING:
      return new Ordering<List<RexLiteral>>() {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rel2sql/RelToSqlConverter.java`
#### Snippet
```java
        .map(key -> groupKeys.get(wholeGroupSet.indexOf(key)))
        .collect(Collectors.toList());
    switch (nodes.size()) {
    case 1:
      return nodes.get(0);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/externalize/RelDotWriter.java`
#### Snippet
```java
    }

    switch (detailLevel) {
    case ALL_ATTRIBUTES:
      sb.append("rowcount = ")
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/externalize/RelWriterImpl.java`
#### Snippet
```java
      }
    }
    switch (detailLevel) {
    case ALL_ATTRIBUTES:
      s.append(": rowcount = ")
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexMultisetUtil.java`
#### Snippet
```java
   */
  public static boolean isMultisetCast(RexCall call) {
    switch (call.getKind()) {
    case CAST:
      return call.getType().getSqlTypeName() == SqlTypeName.MULTISET;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
        final SqlOperator inverseOperator = getInverseOperator(operand);
        if (inverseOperator != null) {
          switch (operand.getKind()) {
          case IN:
            assert operand instanceof RexSubQuery
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
    case ANY:
    case NULL:
      switch (typeName) {
      case NULL:
        return SqlLiteral.createNull(POS);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
          @Override public SqlNode field(int ordinal) {
            final SqlNode selectItem = selectList.get(ordinal);
            switch (selectItem.getKind()) {
            case AS:
              final SqlCall asCall = (SqlCall) selectItem;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
      }
      final List<SqlNode> nodeList = toSql(program, call.getOperands());
      switch (call.getKind()) {
      case CAST:
        // CURSOR is used inside CAST, like 'CAST ($0): CURSOR NOT NULL',
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/rel2sql/SqlImplementor.java`
#### Snippet
```java
                      type, true, true)))
          .collect(SqlNode.toList());
      switch (list.size()) {
      case 1:
        return eqOp.createCall(POS, operandSql, list.get(0));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/LogicVisitor.java`
#### Snippet
```java
      break;
    }
    switch (requireNonNull(logic, "logic")) {
    case TRUE:
      switch (call.getKind()) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/LogicVisitor.java`
#### Snippet
```java
    switch (requireNonNull(logic, "logic")) {
    case TRUE:
      switch (call.getKind()) {
      case AND:
        break;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
      TimeZone tz = DateTimeUtils.UTC_ZONE;
      final Comparable v;
      switch (typeName) {
      case DATE:
        final Calendar cal =
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
      return null;
    }
    switch (typeName) {
    case DECIMAL:
      assert value instanceof BigDecimal;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rel/externalize/RelJson.java`
#### Snippet
```java
        }
        map.put("operands", list);
        switch (node.getKind()) {
        case CAST:
          map.put("type", toJson(node.getType()));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexBuilder.java`
#### Snippet
```java

          // Not all types are allowed for literals
          switch (typeName) {
          case INTEGER:
            typeName = SqlTypeName.BIGINT;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexInterpreter.java`
#### Snippet
```java

  private static TimeUnitRange subUnit(TimeUnitRange unit) {
    switch (unit) {
    case QUARTER:
      return TimeUnitRange.MONTH;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexInterpreter.java`
#### Snippet
```java
    case YEAR:
    case MONTH:
      switch (call.getKind()) {
      case FLOOR:
        return DateTimeUtils.unixTimestampFloor(unit, v);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlPivot.java`
#### Snippet
```java
   * "(5, 6) as b" remains "(5, 6) as b". */
  private static SqlNode strip(SqlNode e) {
    switch (e.getKind()) {
    case AS:
      final SqlCall call = (SqlCall) e;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlPivot.java`
#### Snippet
```java
  @Override public void setOperand(int i, @Nullable SqlNode operand) {
    // Only 'query' is mutable. (It is required for validation.)
    switch (i) {
    case 0:
      query = operand;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
  private static void addOr(ImmutableList.Builder<RexNode> builder,
      Set<RexNode> set, RexNode node) {
    switch (node.getKind()) {
    case OR:
      for (RexNode operand : ((RexCall) node).getOperands()) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
  public static boolean isNullabilityCast(RelDataTypeFactory typeFactory,
      RexNode node) {
    switch (node.getKind()) {
    case CAST:
      final RexCall call = (RexCall) node;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
      for (RexNode node : nodes) {
        RexNode dnf = toDnf(node);
        switch (dnf.getKind()) {
        case OR:
          list.addAll(((RexCall) dnf).getOperands());
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
      for (RexNode node : nodes) {
        RexNode pulled = pull(node);
        switch (pulled.getKind()) {
        case AND:
          list.addAll(((RexCall) pulled).getOperands());
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
    // If "e" is of the form "x = literal", remove all "x = otherLiteral"
    // terms from notTerms.
    switch (e.getKind()) {
    case EQUALS:
      final RexCall call = (RexCall) e;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
        notTerms =
            Util.filter(notTerms, e2 -> {
              switch (e2.getKind()) {
              case EQUALS:
                RexCall call2 = (RexCall) e2;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
  private static void addAnd(ImmutableList.Builder<RexNode> builder,
      Set<RexNode> digests, RexNode node) {
    switch (node.getKind()) {
    case AND:
      for (RexNode operand : ((RexCall) node).getOperands()) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
    final C existedValue = map.get(left);
    if (existedValue == null) {
      switch (left.getKind()) {
      case CAST:
        // Convert "CAST(c) = literal" to "c = literal", as long as it is a
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
  public static RexNode removeCast(RexNode e) {
    for (;;) {
      switch (e.getKind()) {
      case CAST:
        e = ((RexCall) e).operands.get(0);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
    @Override public RexNode visitCall(RexCall call) {
      RexUnknownAs unknownAs = this.unknownAs;
      switch (unknownAs)  {
      case FALSE:
        switch (call.getKind()) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
        for (RexNode node : operands) {
          RexNode cnf = toCnf2(node);
          switch (cnf.getKind()) {
          case AND:
            incrementAndCheck();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
                + ", SqlTypeName " + name1);
    if (family == name2.getFamily()) {
      switch (family) {
      case NUMERIC:
        if (SqlTypeUtil.isExactNumeric(type1)
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexUtil.java`
#### Snippet
```java
  /** Negates a logical expression by adding or removing a NOT. */
  public static RexNode not(RexNode e) {
    switch (e.getKind()) {
    case NOT:
      return ((RexCall) e).getOperands().get(0);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlUnpivot.java`
#### Snippet
```java
      BiConsumer<SqlNodeList, @Nullable SqlNodeList> consumer) {
    for (SqlNode node : inList) {
      switch (node.getKind()) {
      case AS:
        final SqlCall call = (SqlCall) node;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlUnpivot.java`
#### Snippet
```java
  @Override public void setOperand(int i, @Nullable SqlNode operand) {
    // Only 'query' is mutable. (It is required for validation.)
    switch (i) {
    case 0:
      query = operand;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlDialect.java`
#### Snippet
```java
      int rightPrec) {
    SqlOperator operator = call.getOperator();
    switch (call.getKind()) {
    case ROW:
      // Remove the ROW keyword if the dialect does not allow that.
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
          final C c1 = literal.getValueAs(clazz);
          assert c1 != null : "value must not be null in " + literal;
          switch (predicate.getKind()) {
          case NOT_EQUALS:
            // We want to intersect result with the range set of everything but
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      return RexUtil.composeConjunction(rexBuilder, operands);
    case CUSTOM:
      switch (a.getKind()) {
      case LITERAL:
        return rexBuilder.makeLiteral(!((RexLiteral) a).isNull());
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      case FIELD_ACCESS:
      case CAST:
        switch (right.getKind()) {
        case LITERAL:
          return accept2b(left, kind, (RexLiteral) right, newTerms);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    }
    final boolean safe = e.getKind() == SqlKind.SAFE_CAST;
    switch (operand.getKind()) {
    case LITERAL:
      final RexLiteral literal = (RexLiteral) operand;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      // Next, try to convert the value to a different type,
      // e.g. CAST('123' as integer)
      switch (literal.getTypeName()) {
      case TIME:
        switch (e.getType().getSqlTypeName()) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      switch (literal.getTypeName()) {
      case TIME:
        switch (e.getType().getSqlTypeName()) {
        case TIMESTAMP:
          return e;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
        final RexNode left = call.getOperands().get(0);
        final RexNode right = call.getOperands().get(1);
        switch (right.getKind()) {
        case LITERAL:
          if (nodePredicate.test(left)) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
          break;
        }
        switch (left.getKind()) {
        case LITERAL:
          if (nodePredicate.test(right)) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
      break;
    }
    switch (a.getKind()) {
    case NOT:
      // (NOT x) IS TRUE ==> x IS FALSE
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/rex/RexSimplify.java`
#### Snippet
```java
    }

    switch (unknownAs) {
    case FALSE:
      return simplifyAnd2ForUnknownAsFalse(terms, notTerms, Comparable.class);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
    opTab.lookupOperatorOverloads(funcName, category, syntax, sqlOperators,
        nameMatcher);
    switch (syntax) {
    case FUNCTION:
      return Iterators.filter(sqlOperators.iterator(),
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlUtil.java`
#### Snippet
```java
    }
    if (call.operandCount() == 0) {
      switch (call.getOperator().getSyntax()) {
      case FUNCTION_STAR:
        writer.sep("*");
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlLiteral.java`
#### Snippet
```java
      return null;
    }
    switch (typeName) {
    case CHAR:

```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/SqlDescribeSchema.java`
#### Snippet
```java
  @SuppressWarnings("assignment.type.incompatible")
  @Override public void setOperand(int i, @Nullable SqlNode operand) {
    switch (i) {
    case 0:
      schema = (SqlIdentifier) operand;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/fun/SqlOverlapsOperator.java`
#### Snippet
```java
    }
    final SqlSingleOperandTypeChecker rightChecker;
    switch (kind) {
    case CONTAINS:
      rightChecker = OperandTypes.PERIOD_OR_DATETIME;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/fun/SqlIntervalOperator.java`
#### Snippet
```java

  @Override public String getSignatureTemplate(int operandsCount) {
    switch (operandsCount) {
    case 2:
      return "{0} {1} {2}"; // e.g. "INTERVAL <INTEGER> <INTERVAL HOUR>"
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/fun/SqlExtractFunction.java`
#### Snippet
```java
  @Override public SqlMonotonicity getMonotonicity(SqlOperatorBinding call) {
    TimeUnitRange value = getOperandLiteralValueOrThrow(call, 0, TimeUnitRange.class);
    switch (value) {
    case YEAR:
      return call.getOperandMonotonicity(1).unstrict();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/fun/SqlTranslateFunction.java`
#### Snippet
```java

  @Override public String getSignatureTemplate(final int operandsCount) {
    switch (operandsCount) {
    case 2:
      return "{0}({1} USING {2})";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/fun/SqlTrimFunction.java`
#### Snippet
```java

  @Override public String getSignatureTemplate(final int operandsCount) {
    switch (operandsCount) {
    case 3:
      return "{0}([BOTH|LEADING|TRAILING] {1} FROM {2})";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/fun/SqlTrimFunction.java`
#### Snippet
```java
      return false;
    }
    switch (kind) {
    case TRIM:
      return SqlTypeUtil.isCharTypeComparable(callBinding,
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/fun/SqlDotOperator.java`
#### Snippet
```java

  private static SqlSingleOperandTypeChecker getChecker(RelDataType operandType) {
    switch (operandType.getSqlTypeName()) {
    case ROW:
      return OperandTypes.family(SqlTypeFamily.STRING);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/fun/SqlDotOperator.java`
#### Snippet
```java
    final RelDataTypeFactory typeFactory = opBinding.getTypeFactory();
    final RelDataType recordType = opBinding.getOperandType(0);
    switch (recordType.getSqlTypeName()) {
    case ROW:
      final String fieldName =
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeName.java`
#### Snippet
```java
      // Decimal values must fit into 64 bits. So, the maximum value of
      // a DECIMAL(19, 0) is 2^63 - 1, not 10^19 - 1.
      switch (limit) {
      case OVERFLOW:
        final BigDecimal other =
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
    }

    switch (typeName) {
    case BOOLEAN:
      return "Boolean";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeUtil.java`
#### Snippet
```java
      return true;
    }
    switch (toType.getSqlTypeName()) {
    case DECIMAL:
      final int intDigits = value.precision() - value.scale();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/VerticaSqlDialect.java`
#### Snippet
```java
  @Override public boolean supportsFunction(SqlOperator operator,
      RelDataType type, final List<RelDataType> paramTypes) {
    switch (operator.kind) {
    case LIKE:
      // introduces support for ILIKE as well
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/HsqldbSqlDialect.java`
#### Snippet
```java
  @Override public void unparseCall(SqlWriter writer, SqlCall call,
      int leftPrec, int rightPrec) {
    switch (call.getKind()) {
    case FLOOR:
      if (call.operandCount() != 2) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/HiveSqlDialect.java`
#### Snippet
```java
  @Override public @Nullable SqlNode getCastSpec(final RelDataType type) {
    if (type instanceof BasicSqlType) {
      switch (type.getSqlTypeName()) {
      case INTEGER:
        SqlAlienSystemTypeNameSpec typeNameSpec =
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/PostgresqlSqlDialect.java`
#### Snippet
```java
  @Override public boolean supportsFunction(SqlOperator operator,
      RelDataType type, final List<RelDataType> paramTypes) {
    switch (operator.kind) {
    case LIKE:
      // introduces support for ILIKE as well
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/PostgresqlSqlDialect.java`
#### Snippet
```java
  @Override public void unparseCall(SqlWriter writer, SqlCall call,
      int leftPrec, int rightPrec) {
    switch (call.getKind()) {
    case FLOOR:
      if (call.operandCount() != 2) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/PostgresqlSqlDialect.java`
#### Snippet
```java
      new RelDataTypeSystemImpl() {
        @Override public int getMaxPrecision(SqlTypeName typeName) {
          switch (typeName) {
          case VARCHAR:
            // From htup_details.h in postgresql:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/FireboltSqlDialect.java`
#### Snippet
```java
          .unparse(writer, call, 0, 0);
    } else {
      switch (call.getKind()) {
      case FLOOR:
        if (call.operandCount() != 2) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/OracleSqlDialect.java`
#### Snippet
```java

  @Override public boolean supportsDataType(RelDataType type) {
    switch (type.getSqlTypeName()) {
    case BOOLEAN:
      return false;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/OracleSqlDialect.java`
#### Snippet
```java
      new RelDataTypeSystemImpl() {
        @Override public int getMaxPrecision(SqlTypeName typeName) {
          switch (typeName) {
          case VARCHAR:
            // Maximum size of 4000 bytes for varchar2.
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/parser/SqlParserUtil.java`
#### Snippet
```java
  public static SqlNodeList stripRow(SqlNode n) {
    final List<SqlNode> list;
    switch (n.getKind()) {
    case ROW:
      list = ((SqlCall) n).getOperandList();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/MssqlSqlDialect.java`
#### Snippet
```java
      SqlUtil.unparseFunctionSyntax(MSSQL_SUBSTRING, writer, call, false);
    } else {
      switch (call.getKind()) {
      case FLOOR:
        if (call.operandCount() != 2) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/advise/SqlSimpleParser.java`
#### Snippet
```java
      // Simplify sub-queries.
      for (Token token : tokenList) {
        switch (token.type) {
        case QUERY: {
          Query query = (Query) token;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlConformanceEnum.java`
#### Snippet
```java

  @Override public boolean allowHyphenInUnquotedTableName() {
    switch (this) {
    case BIG_QUERY:
      return true;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlConformanceEnum.java`
#### Snippet
```java

  @Override public boolean splitQuotedTableName() {
    switch (this) {
    case BIG_QUERY:
      return true;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlConformanceEnum.java`
#### Snippet
```java

  @Override public boolean isLiberal() {
    switch (this) {
    case BABEL:
      return true;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlConformanceEnum.java`
#### Snippet
```java

  @Override public boolean allowAliasUnnestItems() {
    switch (this) {
    case PRESTO:
      return true;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/dialect/ExasolSqlDialect.java`
#### Snippet
```java

  @Override public @Nullable SqlNode getCastSpec(RelDataType type) {
    switch (type.getSqlTypeName()) {
    case TIMESTAMP:
      // Exasol does not support TIMESTAMP with precision.
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/ListScope.java`
#### Snippet
```java
      }
    }
    switch (map.size()) {
    case 0:
      return parent.findQualifyingTableNames(columnName, ctx, nameMatcher);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
        ((CustomColumnResolvingTable) t).resolveColumn(
            rowType, typeFactory, id.names);
    switch (entries.size()) {
    case 1:
      if (!entries.get(0).getValue().isEmpty()) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
          analyzeGroupTuple(scope, groupAnalyzer,
              ((SqlCall) groupExpr).getOperandList());
      switch (groupExpr.getKind()) {
      case ROLLUP:
        topBuilder.add(rollup(bitSets));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorUtil.java`
#### Snippet
```java
          analyzeGroupTuple(scope, groupAnalyzer,
              ((SqlCall) groupExpr).getOperandList());
      switch (groupExpr.getKind()) {
      case ROLLUP:
        builder.addAll(rollup(operandBitSet));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/MatchRecognizeScope.java`
#### Snippet
```java
      }
    }
    switch (map.size()) {
    case 0:
      return parent.findQualifyingTableNames(columnName, ctx, nameMatcher);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingScope.java`
#### Snippet
```java
    final String tableName;
    final SqlValidatorNamespace namespace;
    switch (identifier.names.size()) {
    case 1: {
      columnName = identifier.names.get(0);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/DelegatingScope.java`
#### Snippet
```java
        final Map<String, ScopeChild> map =
            findQualifyingTableNames(columnName, identifier, nameMatcher);
        switch (map.size()) {
        default:
          final SqlIdentifier prefix1 = identifier.skipLast(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/RexImplicationChecker.java`
#### Snippet
```java

    // Several things imply "IS NOT NULL"
    switch (second.getKind()) {
    case IS_NOT_NULL:
      // Suppose we know that first is strong in second; that is,
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/RexImplicationChecker.java`
#### Snippet
```java
      return true;
    }
    switch (first.getKind()) {
    case AND:
      for (RexNode f : RelOptUtil.conjunctions(first)) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/volcano/VolcanoRuleCall.java`
#### Snippet
```java

        // Assign "childRels" if the operand is UNORDERED.
        switch (parentOperand.childPolicy) {
        case UNORDERED:
          // Note: below is ill-defined. Suppose there's a union with 3 inputs,
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
    // always false.
    for (RexNode disjunction : disjunctions) {
      switch (disjunction.getKind()) {
      case LITERAL:
        if (!RexLiteral.booleanValue(disjunction)) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/SubstitutionVisitor.java`
#### Snippet
```java
    }
    for (RexNode disjunction : notDisjunctions) {
      switch (disjunction.getKind()) {
      case LITERAL:
        if (RexLiteral.booleanValue(disjunction)) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      boolean notIn,
      RelBuilder relBuilder) {
    switch (subQueryType) {
    case SCALAR:
      return new Exists(seekRel, false, true);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
        return;
      }
      switch (e.getKind()) {
      case OR:
        final List<RexNode> ors = new ArrayList<>();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
        decomposeDisjunction(e, ors);
        for (RexNode or : ors) {
          switch (or.getKind()) {
          case NOT:
            rexList.add(((RexCall) or).operands.get(0));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      List<RexNode> filters, RexNode constraint) {
    for (RexNode node : conjunctions(constraint)) {
      switch (node.getKind()) {
      case EQUALS:
        final List<RexNode> operands = ((RexCall) node).getOperands();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

  private static SqlNode stripOver(SqlNode node) {
    switch (node.getKind()) {
    case OVER:
      return ((SqlCall) node).getOperandList().get(0);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

    validateNamespace(ns, targetRowType);
    switch (node.getKind()) {
    case EXTEND:
      // Until we have a dedicated namespace for EXTEND
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    final SelectScope scope = getRawSelectScopeNonNull(select);

    switch (modality) {
    case STREAM:
      if (scope.children.size() == 1) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    final SqlNode aggregateNode = getAggregate(select);
    if (aggregateNode != null) {
      switch (modality) {
      case STREAM:
        SqlNodeList groupList = select.getGroup();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    final SqlNodeList orderList  = select.getOrderList();
    if (orderList != null && orderList.size() > 0) {
      switch (modality) {
      case STREAM:
        if (!hasSortedPrefix(scope, orderList)) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
   */
  private void validateOrderItem(SqlSelect select, SqlNode orderItem) {
    switch (orderItem.getKind()) {
    case DESCENDING:
      validateFeature(RESOURCE.sQLConformance_OrderByDesc(),
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

  private void validateNodeFeature(SqlNode node) {
    switch (node.getKind()) {
    case MULTISET_VALUE_CONSTRUCTOR:
      validateFeature(RESOURCE.sQLFeature_S271(), node.getParserPosition());
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
  private static boolean isSortCompatible(SelectScope scope, SqlNode node,
      boolean descending) {
    switch (node.getKind()) {
    case DESCENDING:
      return isSortCompatible(scope, ((SqlCall) node).getOperandList().get(0),
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
      }
    }
    switch (node.getKind()) {
    case JOIN:
      lookupJoinHints((SqlJoin) node, scope, pos, hintList);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

      if (isSingle) {
        switch (kind) {
        case COUNT:
          if (vars.size() > 1) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

  @Override public SqlLiteral resolveLiteral(SqlLiteral literal) {
    switch (literal.getTypeName()) {
    case UNKNOWN:
      final SqlUnknownLiteral unknownLiteral = (SqlUnknownLiteral) literal;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    }
    final SqlParserPos startPosition = identifier.getParserPosition();
    switch (identifier.names.size()) {
    case 1:
      boolean hasDynamicStruct = false;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    Permute(SqlNode from, int offset) {
      this.offset = offset;
      switch (from.getKind()) {
      case JOIN:
        final SqlJoin join = (SqlJoin) from;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
      validateModality(select, modality, true);
    } else if (query.getKind() == SqlKind.VALUES) {
      switch (modality) {
      case STREAM:
        throw newValidationError(query, Static.RESOURCE.cannotStreamValues());
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java

  private static boolean isRowWithDefault(SqlNode operand, int column) {
    switch (operand.getKind()) {
    case ROW:
      final SqlCall row = (SqlCall) operand;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
   * column. */
  private static boolean isValuesWithDefault(SqlNode source, int column) {
    switch (source.getKind()) {
    case VALUES:
      for (SqlNode operand : ((SqlCall) source).getOperandList()) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
      }

      switch (kind) {
      case PREV:
        final List<SqlNode> operands = call.getOperandList();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorImpl.java`
#### Snippet
```java
    }
    final JoinConditionType conditionType = join.getConditionType();
    switch (conditionType) {
    case ON:
      requireNonNull(condition, () -> "join.getCondition() for " + join)
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcToEnumerableConverter.java`
#### Snippet
```java
        jdbcConvention.dialect.getCalendarPolicy();
    final Expression calendar_;
    switch (calendarPolicy) {
    case LOCAL:
      calendar_ =
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/jdbc/JdbcSchema.java`
#### Snippet
```java
    final SqlTypeName sqlTypeName =
        Util.first(SqlTypeName.getNameForJdbcType(dataType), SqlTypeName.ANY);
    switch (sqlTypeName) {
    case ARRAY:
      RelDataType component = null;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableRel.java`
#### Snippet
```java

    public Prefer of(JavaRowFormat format) {
      switch (format) {
      case ARRAY:
        return ARRAY;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/JavaRowFormat.java`
#### Snippet
```java
    @Override public Expression record(
        Type javaRowClass, List<Expression> expressions) {
      switch (expressions.size()) {
      case 0:
        assert javaRowClass == Unit.class;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumerableRelImplementor.java`
#### Snippet
```java
      throw ex;
    }
    switch (prefer) {
    case ARRAY:
      if (result.physType.getFormat() == JavaRowFormat.ARRAY
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/EnumUtils.java`
#### Snippet
```java
      }
      if (toBox != null) {
        switch (toBox) {
        case CHAR:
          // Generate "SqlFunctions.toCharBoxed(x)".
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/PhysTypeImpl.java`
#### Snippet
```java
    final PhysType targetPhysType =
        project(fields, targetFormat);
    switch (format) {
    case SCALAR:
      return Expressions.call(BuiltInMethod.IDENTITY_SELECTOR.method);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/PhysTypeImpl.java`
#### Snippet
```java
    final PhysType targetPhysType =
        project(fields, targetFormat);
    switch (format) {
    case SCALAR:
      return Pair.of(parameter.getType(), ImmutableList.of(parameter));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/PhysTypeImpl.java`
#### Snippet
```java
      Expression arg0 = fieldReference(parameterV0, index);
      Expression arg1 = fieldReference(parameterV1, index);
      switch (Primitive.flavor(fieldClass(index))) {
      case OBJECT:
        arg0 = EnumUtils.convert(arg0, Comparable.class);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/PhysTypeImpl.java`
#### Snippet
```java
      Expression arg0 = fieldReference(parameterV0, index);
      Expression arg1 = fieldReference(parameterV1, index);
      switch (Primitive.flavor(fieldClass(index))) {
      case OBJECT:
        arg0 = EnumUtils.convert(arg0, Comparable.class);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
    int fieldIndex = fieldAccess.getField().getIndex();
    String fieldName = fieldAccess.getField().getName();
    switch (target.getKind()) {
    case CORREL_VARIABLE:
      if (correlates == null) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
              () -> "null SqlTypeFamily for " + sourceType + ", SqlTypeName "
                  + sourceType.getSqlTypeName());
      switch (family) {
      case NUMERIC:
        final BigDecimal multiplier =
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexToLixTranslator.java`
#### Snippet
```java
      RelDataType targetType,
      Expression operand) {
    switch (requireNonNull(targetType.getSqlTypeName().getFamily(),
        () -> "SqlTypeFamily for " + targetType)) {
    case NUMERIC:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
  public RexNode alias(RexNode expr, String alias) {
    final RexNode aliasLiteral = literal(alias);
    switch (expr.getKind()) {
    case AS:
      final RexCall call = (RexCall) expr;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
      RexNode node = extraNodes.get(groupField);
      final SqlKind kind = node.getKind();
      switch (kind) {
      case INPUT_REF:
        fields.add(inFields.get(((RexInputRef) node).getIndex()));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
      for (Pair<RexNode, Field> pair
          : Pair.zip(project.getProjects(), frame1.fields)) {
        switch (pair.left.getKind()) {
        case INPUT_REF:
          final int i = ((RexInputRef) pair.left).getIndex();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
      RelDataTypeField fieldType =
          new RelDataTypeFieldImpl(name, i, node.getType());
      switch (node.getKind()) {
      case INPUT_REF:
        // preserve rel aliases for INPUT_REF fields
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/tools/RelBuilder.java`
#### Snippet
```java
    for (RelFieldCollation fieldCollation : collation.getFieldCollations()) {
      RexNode node = field(fieldCollation.getFieldIndex());
      switch (fieldCollation.direction) {
      case DESCENDING:
        node = desc(node);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/prepare/Prepare.java`
#### Snippet
```java
    // Display physical plan after decorrelation.
    if (sqlExplain != null) {
      switch (sqlExplain.getDepth()) {
      case PHYSICAL:
      default:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java

  @Override public boolean supportsModality(SqlModality modality) {
    switch (modality) {
    case STREAM:
      return table instanceof StreamableTable;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/prepare/RelOptTableImpl.java`
#### Snippet
```java
    int n = 0;
    for (int j = 0; j < i; j++) {
      switch (strategies.get(j)) {
      case VIRTUAL:
        ++n;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/prepare/PlannerImpl.java`
#### Snippet
```java

  private void ready() {
    switch (state) {
    case STATE_0_CLOSED:
      reset();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/runtime/FlatLists.java`
#### Snippet
```java

    @Override public T get(int index) {
      switch (index) {
      case 0:
        return t0;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/interpreter/SortNode.java`
#### Snippet
```java
    final int nullComparison = fieldCollation.nullDirection.nullComparison;
    final int x = fieldCollation.getFieldIndex();
    switch (fieldCollation.direction) {
    case ASCENDING:
      return (o1, o2) -> {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
    /** Normalizes a TIME value into 00:00:00..23:59:39. */
    private static Expression normalize(SqlTypeName typeName, Expression e) {
      switch (typeName) {
      case TIME:
        return Expressions.call(BuiltInMethod.FLOOR_MOD.method, e,
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
      switch (operand0.getType().getSqlTypeName()) {
      case DATE:
        switch (typeName) {
        case TIMESTAMP:
          trop0 =
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
      case INTERVAL_YEAR_MONTH:
      case INTERVAL_MONTH:
        switch (call.getKind()) {
        case MINUS:
          trop1 = Expressions.negate(trop1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
          break;
        }
        switch (typeName) {
        case TIME:
          return Expressions.convert_(trop0, long.class);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
      case INTERVAL_MINUTE_SECOND:
      case INTERVAL_SECOND:
        switch (call.getKind()) {
        case MINUS:
          return normalize(typeName, Expressions.subtract(trop0, trop1));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java

      default:
        switch (call.getKind()) {
        case MINUS:
          switch (typeName) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
      case MINUTE:
      case SECOND:
        switch (sqlTypeName) {
        case DATE:
          return Expressions.multiply(operand, Expressions.constant(0L));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
        AggResultContext result) {
      final List<Integer> keys;
      switch (info.aggregation().kind) {
      case GROUPING: // "GROUPING(e, ...)", also "GROUPING_ID(e, ...)"
        keys = result.call().getArgList();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/adapter/enumerable/RexImpTable.java`
#### Snippet
```java
        }
      case BOX:
        switch (this) {
        case NOT_POSSIBLE:
          return EnumUtils.convert(x,
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
    final SqlNode a0;
    final SqlNode a1;
    switch (operand.getKind()) {
    case ROW:
      a0 = ((SqlCall) operand).operand(0);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
      final RexNode length = exprs.get(2);
      final RexNode newLength;
      switch (library) {
      case POSTGRESQL:
        newLength = length;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
          rexBuilder.makeCall(SqlStdOperatorTable.SUBSTRING, value, newStart,
              newLength);
      switch (library) {
      case BIG_QUERY:
        return rexBuilder.makeCall(SqlStdOperatorTable.CASE,
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/StandardConvertletTable.java`
#### Snippet
```java
      final RexNode op1;
      final RexNode op2;
      switch (call.operandCount()) {
      case 2:
        // BigQuery-style 'TIMESTAMP_ADD(timestamp, interval)'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/materialize/LatticeSuggester.java`
#### Snippet
```java
          }
          final List<TableRef> tableRefList = tableRefs.build();
          switch (tableRefList.size()) {
          case 1:
            return new SingleTableDerivedColRef(tableRefList.get(0), e, alias);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/materialize/Lattice.java`
#### Snippet
```java
  /** Converts an "t1.c1 = t2.c2" expression into two (input, field) pairs. */
  private static int[][] grab(List<TableScan> leaves, RexNode rex) {
    switch (rex.getKind()) {
    case EQUALS:
      break;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `plus/src/main/java/org/apache/calcite/adapter/os/VmstatTableFunction.java`
#### Snippet
```java
        final String osName = System.getProperty("os.name");
        final RelDataTypeFactory.Builder builder = typeFactory.builder();
        switch (osName) {
        case "Mac OS X":
          return builder
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `plus/src/main/java/org/apache/calcite/adapter/os/VmstatTableFunction.java`
#### Snippet
```java
        // Fork out to a shell so that we can get normal text-munging support.
        // Could do this here too..
        switch (osName) {
        case "Mac OS X": // tested on version 10.11.6
          args = new String[] {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `plus/src/main/java/org/apache/calcite/adapter/os/PsTableFunction.java`
#### Snippet
```java
        final String osVersion = System.getProperty("os.version");
        Util.discard(osVersion);
        switch (osName) {
        case "Mac OS X": // tested on version 10.12.5
          args = new String[] {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `plus/src/main/java/org/apache/calcite/adapter/os/FilesTableFunction.java`
#### Snippet
```java
        Util.discard(osVersion);
        final Enumerable<String> enumerable;
        switch (osName) {
        case "Mac OS X": // tested on version 10.12.5
          enumerable = sourceMacOs();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `plus/src/main/java/org/apache/calcite/adapter/os/FilesTableFunction.java`
#### Snippet
```java
                  }
                }
                switch (osName) {
                case "Mac OS X":
                  // Strip leading "./"
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `druid/src/main/java/org/apache/calcite/adapter/druid/DruidJsonFilter.java`
#### Snippet
```java
      generator.writeStartObject();
      generator.writeStringField("type", type.lowercase());
      switch (type) {
      case NOT:
        DruidQuery.writeField(generator, "field", fields.get(0));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
     */
    private String translateBinary2(String op, RexNode left, RexNode right) {
      switch (right.getKind()) {
      case LITERAL:
        break;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    SqlNode converted = validator().expandOrderExpr(select, orderItem);

    switch (nullDirection) {
    case UNSPECIFIED:
      nullDirection = validator().config().defaultNullCollation().last(desc(direction))
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
    }

    switch (nullDirection) {
    case UNSPECIFIED:
      nullDirection = validator().config().defaultNullCollation().last(desc(direction))
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
            final SqlNode operand = call.operand(0);
            List<SqlNode> nodes;
            switch (operand.getKind()) {
            case ROW:
              nodes = ((SqlCall) operand).getOperandList();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java

      final List<RexNode> leftKeys;
      switch (leftKeyNode.getKind()) {
      case ROW:
        leftKeys = new ArrayList<>();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
        final ColumnStrategy strategy =
            ief.generationStrategy(table, f.getIndex());
        switch (strategy) {
        case VIRTUAL:
          list.add(ief.newColumnDefaultValue(table, f.getIndex(), bb));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/UnaryExpression.java`
#### Snippet
```java

  @Override void accept(ExpressionWriter writer, int lprec, int rprec) {
    switch (nodeType) {
    case Convert:
      if (!writer.requireParentheses(this, lprec, rprec)) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/GotoStatement.java`
#### Snippet
```java
        writer.append(' ');
      }
      switch (kind) {
      case Sequence:
        // don't indent for sequence
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/OptimizeShuttle.java`
#### Snippet
```java
        new TernaryExpression(ternary.getNodeType(), ternary.getType(),
            expression0, expression1, expression2);
    switch (ternary.getNodeType()) {
    case Conditional:
      Boolean always = always(expression0);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
      Expression e0, Expression e1, Expression e2) {
    final Type type;
    switch (ternaryType) {
    case Conditional:
      if (e1 instanceof ConstantUntypedNull) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/Expressions.java`
#### Snippet
```java
      Expression expression) {
    Type type = expression.getType();
    switch (expressionType) {
    case Negate:
      if (type == byte.class || type == short.class) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
      System.out.println(input.getRowType());
      for (RelDataTypeField field : input.getRowType().getFieldList()) {
        switch (field.getType().getSqlTypeName()) {
        case ARRAY:
          System.out.println(field);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
    if (pair.left.isStar()) {
      for (RexNode node : builder.fields()) {
        switch (pair.right) {
        case DESC:
          node = builder.desc(node);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `piglet/src/main/java/org/apache/calcite/piglet/Handler.java`
#### Snippet
```java
    } else {
      RexNode node = toRex(pair.left);
      switch (pair.right) {
      case DESC:
        node = builder.desc(node);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `server/src/main/java/org/apache/calcite/server/ServerDdlExecutor.java`
#### Snippet
```java
              typeName.toUpperCase(Locale.ROOT));
      if (type != null) {
        switch (type) {
        case JDBC:
          libraryName = JdbcSchema.Factory.class.getName();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoAggregate.java`
#### Snippet
```java
      }
    }
    switch (getGroupType()) {
    case SIMPLE:
      break;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
        list.add(translateAnd(node));
      }
      switch (list.size()) {
      case 1:
        return list.get(0);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `mongodb/src/main/java/org/apache/calcite/adapter/mongodb/MongoFilter.java`
#### Snippet
```java
    /** Translates a call to a binary operator. Returns whether successful. */
    private boolean translateBinary2(String op, RexNode left, RexNode right) {
      switch (right.getKind()) {
      case LITERAL:
        break;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `testkit/src/main/java/org/apache/calcite/test/MockRelOptPlanner.java`
#### Snippet
```java
    }
    bindings.add(rel);
    switch (operand.childPolicy) {
    case ANY:
      return true;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraSort.java`
#### Snippet
```java
            fields.get(fieldCollation.getFieldIndex()).getName();
        final String direction;
        switch (fieldCollation.getDirection()) {
        case DESCENDING:
          direction = "DESC";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `cassandra/src/main/java/org/apache/calcite/adapter/cassandra/CassandraFilter.java`
#### Snippet
```java
    /** Translates a call to a binary operator. Returns null on failure. */
    private @Nullable String translateBinary2(String op, RexNode left, RexNode right) {
      switch (right.getKind()) {
      case LITERAL:
        break;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
        }
      case PREFIX: // NOT()
        switch (call.getKind()) {
        case NOT:
          return true;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/PredicateAnalyzer.java`
#### Snippet
```java
        }
      case INTERNAL:
        switch (call.getKind()) {
        case SEARCH:
          return canBeTranslatedToTermsQuery(call);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
      assertReturns("abc", 8, 2, "");

      switch (library) {
      case POSTGRESQL:
        assertReturns("abc", 1, -1, null);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `testkit/src/main/java/org/apache/calcite/test/SqlOperatorTest.java`
#### Snippet
```java
        "aabb", "VARBINARY(3) NOT NULL");

    switch (f.conformance().semantics()) {
    case BIG_QUERY:
      f.checkString("substring('abc' from 1 for -1)", "",
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
        }
      }
    } catch (IOException ignore) {
      StringWriter sw = new StringWriter();
      ignore.printStackTrace(new PrintWriter(sw));
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
          }
        }
      } catch (IOException ignore) {
        StringWriter sw = new StringWriter();
        ignore.printStackTrace(new PrintWriter(sw));
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
          }
        }
      } catch (IOException ignore) {
        StringWriter sw = new StringWriter();
        ignore.printStackTrace(new PrintWriter(sw));
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `core/src/main/java/org/apache/calcite/rel/RelNode.java`
#### Snippet
```java
   * inputs, returns an empty list, not {@code null}.
   *
   * @return Array of this relational expression's inputs
   */
  @Override List<RelNode> getInputs();
```

### MismatchedJavadocCode
Method is specified to return 'null' but it's annotated as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMetadataQuery.java`
#### Snippet
```java
   * @param rel         the relational expression
   * @return List of sorted column combinations, or
   * null if not enough information is available to make that determination
   */
  public RelDistribution distribution(RelNode rel) {
```

### MismatchedJavadocCode
Method is specified to return 'null' but it's annotated as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/rel/metadata/BuiltInMetadata.java`
#### Snippet
```java
     *                    uniqueness
     * @return whether the columns are unique, or
     * null if not enough information is available to make that determination
     */
    Boolean areColumnsUnique(ImmutableBitSet columns, boolean ignoreNulls);
```

### MismatchedJavadocCode
Method is specified to return 'true' but there's no such enum constant in RexDigestIncludeType
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java

  /**
   * Returns true if {@link RexDigestIncludeType#OPTIONAL} digest would include data type.
   *
   * @see RexCall#computeDigest(boolean)
```

### MismatchedJavadocCode
Method is specified to return 'true' but there's no such enum constant in RexDigestIncludeType
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
   *
   * @see RexCall#computeDigest(boolean)
   * @return true if {@link RexDigestIncludeType#OPTIONAL} digest would include data type
   */
  @RequiresNonNull("type")
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
   * @param update If not null, sets this to true if any of the expressions
   *               was modified
   * @return Array of visited expressions
   */
  protected List<RexNode> visitList(
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `core/src/main/java/org/apache/calcite/rex/RexShuttle.java`
#### Snippet
```java
   * @param update     If not null, sets this to true if any of the expressions
   *                   was modified
   * @return Array of visited field collations
   */
  protected List<RexFieldCollation> visitFieldCollations(
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `core/src/main/java/org/apache/calcite/sql/advise/SqlAdvisor.java`
#### Snippet
```java
   *            from sales.dept table setting pos to 'Line 1, Column 31' returns
   *            all the possible table names in 'sales' schema
   * @return an array of hints ({@link SqlMoniker}) that can fill in at the
   * indicated position
   */
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `core/src/main/java/org/apache/calcite/sql/validate/SqlValidatorWithHints.java`
#### Snippet
```java
   *                sales.dept table setting pos to 'Line 1, Column 31' returns
   *                all the possible table names in 'sales' schema
   * @return an array of {@link SqlMoniker} (sql identifiers) that can fill in
   * at the indicated position
   */
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `core/src/main/java/org/apache/calcite/plan/RelOptNode.java`
#### Snippet
```java
   * inputs, returns an empty list, not {@code null}.
   *
   * @return Array of this relational expression's inputs
   */
  List<? extends RelOptNode> getInputs();
```

### MismatchedJavadocCode
Method is specified to return 'null' but it's annotated as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/util/NumberUtil.java`
#### Snippet
```java

  /** Converts a number to a BigDecimal with the same value;
   * returns null if and only if the number is null. */
  public static @PolyNull BigDecimal toBigDecimal(@PolyNull Number number) {
    if (number == null) {
```

### MismatchedJavadocCode
Method is specified to return 'null' but it's annotated as @DefaultQualifier
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
  /**
   * Converts a relational expression to a string;
   * returns null if and only if {@code rel} is null.
   */
  public static @PolyNull String toString(
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
   *                  create the projection to reflect the order of the swapped
   *                  projection
   * @return array of expression representing the swapped join inputs
   */
  public static List<RexNode> createSwappedJoinExprs(
```

### MismatchedJavadocCode
Method is specified to return list but the return type is set
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchSchema.java`
#### Snippet
```java
   * Queries {@code _alias} definition to automatically detect all indices.
   *
   * @return list of indices
   * @throws IOException for any IO related issues
   * @throws IllegalStateException if reply is not understood
```

## RuleId[id=IOStreamConstructor]
### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `core/src/main/java/org/apache/calcite/util/Sources.java`
#### Snippet
```java
    @Override public InputStream openStream() throws IOException {
      if (file != null) {
        return new FileInputStream(file);
      } else {
        return url.openStream();
```

### IOStreamConstructor
'OutputStream' can be constructed using 'Files.newOutputStream()'
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
  public static PrintWriter printWriter(File file)
      throws FileNotFoundException {
    return printWriter(new FileOutputStream(file));
  }

```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
   * <p>Does not use the default character set. */
  public static BufferedReader reader(File file) throws FileNotFoundException {
    return reader(new FileInputStream(file));
  }

```

### IOStreamConstructor
'OutputStream' can be constructed using 'Files.newOutputStream()'
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
    refFile = new File(testFileSansExt.toString() + ".ref");

    logOutputStream = new FileOutputStream(logFile);
    return logOutputStream;
  }
```

## RuleId[id=CollectionAddAllCanBeReplacedWithConstructor]
### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
      List<AggregateCall> aggCallList) {
    final Set<Integer> allFields = new TreeSet<>();
    allFields.addAll(groupSet.asList());
    for (AggregateCall aggregateCall : aggCallList) {
      allFields.addAll(aggregateCall.getArgList());
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `core/src/main/java/org/apache/calcite/adapter/enumerable/StrictAggImplementor.java`
#### Snippet
```java
    final RexToLixTranslator translator = add.rowTranslator();
    final List<Expression> conditions = new ArrayList<>();
    conditions.addAll(
        translator.translateList(args, RexImpTable.NullAs.IS_NOT_NULL));
    RexNode filterArgument = add.rexFilterArgument();
```

### CollectionAddAllCanBeReplacedWithConstructor
'putAll()' call can be replaced with parametrized constructor call
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java

    // Left input positions are not changed.
    mapOldToNewOutputs.putAll(leftFrame.oldToNewOutputs);

    // Right input positions are shifted by newLeftFieldCount.
```

### CollectionAddAllCanBeReplacedWithConstructor
'putAll()' call can be replaced with parametrized constructor call
in `core/src/main/java/org/apache/calcite/sql2rel/RelDecorrelator.java`
#### Snippet
```java

    // Left input positions are not changed.
    mapOldToNewOutputs.putAll(leftFrame.oldToNewOutputs);

    // Right input positions are shifted by newLeftFieldCount.
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rel/hint/RelHint.java`
#### Snippet
```java
      builder.append(" options:");
      if (this.listOptions.size() > 0) {
        builder.append(this.listOptions.toString());
      } else {
        builder.append(this.kvOptions.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rel/hint/RelHint.java`
#### Snippet
```java
        builder.append(this.listOptions.toString());
      } else {
        builder.append(this.kvOptions.toString());
      }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rel/type/RelRecordType.java`
#### Snippet
```java
        sb.append(field.getType().getFullTypeString());
      } else {
        sb.append(field.getType().toString());
      }
      sb.append(" ");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rel/rules/ReduceDecimalsRule.java`
#### Snippet
```java
          || !SqlTypeUtil.isExactNumeric(toType)) {
        throw Util.needToImplement(
            "Cast from '" + fromType.toString()
                + "' to '" + toType.toString() + "'");
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rel/rules/ReduceDecimalsRule.java`
#### Snippet
```java
        throw Util.needToImplement(
            "Cast from '" + fromType.toString()
                + "' to '" + toType.toString() + "'");
      }
      int fromScale = fromType.getScale();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rel/rules/CalcRelSplitter.java`
#### Snippet
```java
    PrintWriter traceWriter = new PrintWriter(traceMsg);
    traceWriter.println("FarragoAutoCalcRule result expressions for: ");
    traceWriter.println(program.toString());

    for (int level = 0; level < levelCount; level++) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case BOOLEAN:
      assert value instanceof Boolean;
      sb.append(value.toString());
      break;
    case DECIMAL:
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case DECIMAL:
      assert value instanceof BigDecimal;
      sb.append(value.toString());
      break;
    case DOUBLE:
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
      assert value instanceof Enum;
      sb.append("FLAG(");
      sb.append(value.toString());
      sb.append(")");
      break;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case DATE:
      assert value instanceof DateString;
      sb.append(value.toString());
      break;
    case TIME:
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case TIME_WITH_LOCAL_TIME_ZONE:
      assert value instanceof TimeString;
      sb.append(value.toString());
      break;
    case TIMESTAMP:
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case TIMESTAMP_WITH_LOCAL_TIME_ZONE:
      assert value instanceof TimestampString;
      sb.append(value.toString());
      break;
    case INTERVAL_YEAR:
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexLiteral.java`
#### Snippet
```java
    case INTERVAL_SECOND:
      assert value instanceof BigDecimal;
      sb.append(value.toString());
      break;
    case MULTISET:
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexTableInputRef.java`
#### Snippet
```java
    super(index, type);
    this.tableRef = tableRef;
    this.digest = tableRef.toString() + ".$" + index;
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
    if (projects.size() < fieldCount) {
      assert !fail
          : "program '" + toString()
          + "' does not project identity for input row type '"
          + inputRowType + "'";
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/rex/RexProgram.java`
#### Snippet
```java
      if (project.index != i) {
        assert !fail
            : "program " + toString()
            + "' does not project identity for input row type '"
            + inputRowType + "', field #" + i;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/sql/SqlIntervalQualifier.java`
#### Snippet
```java
    return SqlUtil.newContextException(pos,
        RESOURCE.unsupportedIntervalLiteral(
            "'" + value + "'", "INTERVAL " + toString()));
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/sql/SqlLiteral.java`
#### Snippet
```java

    default:
      throw Util.needToImplement(toString() + ", operand=" + value);
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/sql/type/MultisetSqlType.java`
#### Snippet
```java
      sb.append(elementType.getFullTypeString());
    } else {
      sb.append(elementType.toString());
    }
    sb.append(" MULTISET");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/sql/type/ArraySqlType.java`
#### Snippet
```java
      sb.append(elementType.getFullTypeString());
    } else {
      sb.append(elementType.toString());
    }
    sb.append(" ARRAY");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/sql/parser/SqlParserUtil.java`
#### Snippet
```java
      throw SqlUtil.newContextException(pos,
          RESOURCE.illegalIntervalLiteral(s + " "
              + intervalQualifier.toString(), pos.toString()));
    }
    return SqlLiteral.createInterval(sign, s, intervalQualifier, pos);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/sql/validate/SqlQualified.java`
#### Snippet
```java

  @Override public String toString() {
    return "{id: " + identifier.toString() + ", prefix: " + prefixLength + "}";
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/plan/volcano/Dumpers.java`
#### Snippet
```java
          continue;
        }
        String traits = "." + relSubset.getTraitSet().toString();
        String title = rel.toString().replace(traits, "");
        if (title.endsWith(")")) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/plan/RelOptUtil.java`
#### Snippet
```java
        + "set type is " + expectedRowType.getFullTypeString()
        + "\nexpression type is " + actualRowType.getFullTypeString()
        + "\nset is " + equivalenceClass.toString()
        + "\nexpression is " + RelOptUtil.toString(newRel)
        + getFullTypeDifferenceString("rowtype of original rel", expectedRowType,
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/BarfingInvocationHandler.java`
#### Snippet
```java
        method.getReturnType().getName() + " "
            + method.getDeclaringClass().getName() + "." + method.getName()
            + "(" + buf.toString() + ")";
    return new UnsupportedOperationException(signature);
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/TimeWithTimeZoneString.java`
#### Snippet
```java
   * in the given time-zone. */
  public TimeWithTimeZoneString(int h, int m, int s, String timeZone) {
    this(DateTimeStringUtils.hms(new StringBuilder(), h, m, s).toString() + " " + timeZone);
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/TimeWithTimeZoneString.java`
#### Snippet
```java
    this.localTime = localTime;
    this.timeZone = timeZone;
    this.v = localTime.toString() + " " + timeZone.getID();
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/TimestampWithTimeZoneString.java`
#### Snippet
```java
    this.localDateTime = localDateTime;
    this.timeZone = timeZone;
    this.v = localDateTime.toString() + " " + timeZone.getID();
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/TimestampWithTimeZoneString.java`
#### Snippet
```java
  public TimestampWithTimeZoneString(int year, int month, int day, int h, int m, int s,
      String timeZone) {
    this(DateTimeStringUtils.ymdhms(new StringBuilder(), year, month, day, h, m, s).toString()
        + " " + timeZone);
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
    String description = null;
    if (o != null) {
      description = o.getClass().toString() + ": " + o.toString();
    }
    throw new UnsupportedOperationException(description);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
            || (clazz == Double.class)
            || (clazz == Void.class)) {
      pw.print(o.toString());
    } else if (clazz.isArray()) {
      // o is an array, but we can't cast to Object[] because it may be
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
      if (source == -1) {
        throw new NoElementException(
            "target #" + target + " has no source in mapping " + toString());
      }
      return source;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/util/mapping/Mappings.java`
#### Snippet
```java
      if (target == -1) {
        throw new NoElementException(
            "source #" + source + " has no target in mapping " + toString());
      }
      return target;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
    @Override public String toString() {
      return "min: " + priorityQueue.peek()
          + ", contents: " + deque.toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/profile/ProfilerImpl.java`
#### Snippet
```java
    boolean isValid() {
      if (CalciteSystemProperty.DEBUG.value()) {
        System.out.println(toString());
      }
      assert deque.size() == priorityQueue.size();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    final TimeWithTimeZoneString tTZ = TimeWithTimeZoneString.fromMillisOfDay(v)
        .withTimeZone(DateTimeUtils.UTC_ZONE);
    return new TimestampWithTimeZoneString(date + " " + tTZ.toString())
        .getLocalTimestampString()
        .getMillisSinceEpoch();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/runtime/SqlFunctions.java`
#### Snippet
```java
    final TimeWithTimeZoneString tTZ = TimeWithTimeZoneString.fromMillisOfDay(v)
        .withTimeZone(DateTimeUtils.UTC_ZONE);
    return new TimestampWithTimeZoneString(date + " " + tTZ.toString())
        .withTimeZone(timeZone)
        .getLocalTimestampString()
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
      case TIMESTAMP_WITH_LOCAL_TIME_ZONE:
        assert valueComparable instanceof TimestampString;
        return "TIMESTAMP '" + valueComparable.toString() + "'";
      case DATE:
        assert valueComparable instanceof DateString;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
      case DATE:
        assert valueComparable instanceof DateString;
        return "DATE '" + valueComparable.toString() + "'";
      case TIME:
      case TIME_WITH_LOCAL_TIME_ZONE:
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `geode/src/main/java/org/apache/calcite/adapter/geode/rel/GeodeFilter.java`
#### Snippet
```java
      case TIME_WITH_LOCAL_TIME_ZONE:
        assert valueComparable instanceof TimeString;
        return "TIME '" + valueComparable.toString() + "'";
      default:
        return String.valueOf(literal.getValue3());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
            SqlValidatorUtil.getTargetField(
                tableRowType, typeFactory, id, catalogReader, targetTable);
        assert field != null : "column " + id.toString() + " not found";
        targetColumnNames.add(field.getName());
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/calcite/sql2rel/SqlToRelConverter.java`
#### Snippet
```java
            SqlValidatorUtil.getTargetField(
                targetRowType, typeFactory, id, catalogReader, targetTable);
        assert field != null : "column " + id.toString() + " not found";
        targetColumnNameList.add(field.getName());
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `example/function/src/main/java/org/apache/calcite/example/maze/Maze.java`
#### Snippet
```java
    for (int y = 0; y < height; y++) {
      row(cellContent, b, b2, y);
      pw.println(b.toString());
      pw.println(b2.toString());
      b.setLength(0);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `example/function/src/main/java/org/apache/calcite/example/maze/Maze.java`
#### Snippet
```java
      row(cellContent, b, b2, y);
      pw.println(b.toString());
      pw.println(b2.toString());
      b.setLength(0);
      b2.setLength(0);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `testkit/src/main/java/org/apache/calcite/util/Smalls.java`
#### Snippet
```java
        return "<null>";
      }
      return "<" + o.toString() + ">";
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchFilter.java`
#### Snippet
```java
      generator.flush();
      generator.close();
      return "{\"query\" : " + writer.toString() + "}";
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `elasticsearch/src/main/java/org/apache/calcite/adapter/elasticsearch/ElasticsearchProject.java`
#### Snippet
```java

    implementor.list.removeIf(l -> l.startsWith("\"_source\""));
    implementor.add("{" + query.toString() + "}");
  }
}
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `core/src/main/java/org/apache/calcite/plan/hep/HepPlanner.java`
#### Snippet
```java
    // Check if a transformation already produced a reference
    // to an existing vertex.
    if (graph.vertexSet().contains(rel)) {
      return (HepRelVertex) rel;
    }
```

### SuspiciousMethodCalls
Suspicious call to 'ImmutableCollection.contains()'
in `core/src/main/java/org/apache/calcite/statistic/MapSqlStatisticProvider.java`
#### Snippet
```java
        // ... and the column names match the name of the primary key
        && keyMap.get(table.getQualifiedName().toString())
            .contains(columnNames(table, columns));
  }

```

### SuspiciousMethodCalls
Suspicious call to 'LinkedHashMap.get()'
in `file/src/main/java/org/apache/calcite/adapter/file/JsonEnumerator.java`
#### Snippet
```java

    for (Object key : jsonFieldMap.keySet()) {
      final RelDataType type = typeFactory.createJavaType(jsonFieldMap.get(key).getClass());
      names.add(key.toString());
      types.add(type);
```

### SuspiciousMethodCalls
Suspicious call to 'ImmutableMap.get()'
in `redis/src/main/java/org/apache/calcite/adapter/redis/RedisTable.java`
#### Snippet
```java

    for (Object key : allFields.keySet()) {
      final RelDataType type = typeFactory.createJavaType(allFields.get(key).getClass());
      names.add(key.toString());
      types.add(type);
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/FunctionExpression.java`
#### Snippet
```java
    }
    requireNonNull(body, "body");
    Type bridgeResultType = Functions.FUNCTION_RESULT_TYPES.get(this.type);
    if (bridgeResultType == null) {
      bridgeResultType = body.getType();
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `linq4j/src/main/java/org/apache/calcite/linq4j/function/Functions.java`
#### Snippet
```java
   */
  public static Class<? extends Function> functionClass(Type aClass) {
    Class<? extends Function> c = FUNCTION1_CLASSES.get(aClass);
    if (c != null) {
      return c;
```

## RuleId[id=RegExpDuplicateAlternationBranch]
### RegExpDuplicateAlternationBranch
Duplicate branch in alternation
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
      ignorePatterns = javaPattern;
    } else {
      ignorePatterns = ignorePatterns + "|" + javaPattern;
    }
    compiledIgnorePattern = Pattern.compile(ignorePatterns);
```

### RegExpDuplicateAlternationBranch
Duplicate branch in alternation
in `testkit/src/main/java/org/apache/calcite/test/DiffTestCase.java`
#### Snippet
```java
      diffMasks = mask;
    } else {
      diffMasks = diffMasks + "|" + mask;
    }
    compiledDiffPattern = Pattern.compile(diffMasks);
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'this == o' covered by subsequent condition 'o instanceof RexCurrentRowWindowBound'
in `core/src/main/java/org/apache/calcite/rex/RexWindowBounds.java`
#### Snippet
```java

    @Override public boolean equals(@Nullable Object o) {
      return this == o
          || o instanceof RexCurrentRowWindowBound;
    }
```

### ConditionCoveredByFurtherCondition
Condition 'this == obj' covered by subsequent condition 'obj instanceof ImmutableIntList'
in `core/src/main/java/org/apache/calcite/util/ImmutableIntList.java`
#### Snippet
```java
  @SuppressWarnings("contracts.conditional.postcondition.not.satisfied")
  @Override public boolean equals(@Nullable Object obj) {
    return ((this == obj)
        || (obj instanceof ImmutableIntList))
        ? Arrays.equals(ints, ((ImmutableIntList) obj).ints)
```

### ConditionCoveredByFurtherCondition
Condition 'n \<= 0' covered by subsequent conditions
in `core/src/main/java/org/apache/calcite/util/Util.java`
#### Snippet
```java
  public static <E> List<E> quotientList(
      final List<E> list, final int n, final int k) {
    if (n <= 0 || k < 0 || k >= n) {
      throw new IllegalArgumentException(
          "n must be positive; k must be between 0 and n - 1");
```

### ConditionCoveredByFurtherCondition
Condition 'fieldNames == null' covered by subsequent condition 'fieldNames.length == 0'
in `splunk/src/main/java/org/apache/calcite/adapter/splunk/search/SplunkConnectionImpl.java`
#### Snippet
```java
      try {
        fieldNames = csvReader.readNext();
        if (fieldNames == null
            || fieldNames.length == 0
            || fieldNames.length == 1 && fieldNames[0].isEmpty()) {
```

## RuleId[id=RemoveEmptyParenthesesFromLambdaCall]
### RemoveEmptyParenthesesFromLambdaCall
Unnecessary parentheses in function call with lambda
in `core/build.gradle.kts`
#### Snippet
```java
}

val integTestAll by tasks.registering() {
    group = LifecycleBasePlugin.VERIFICATION_GROUP
    description = "Executes integration JDBC tests for all DBs"
```

### RemoveEmptyParenthesesFromLambdaCall
Unnecessary parentheses in function call with lambda
in `build.gradle.kts`
#### Snippet
```java
                group = LifecycleBasePlugin.VERIFICATION_GROUP
                description = "Runs the slow unit tests."
                useJUnitPlatform() {
                    includeTags("slow")
                }
```

## RuleId[id=RegExpSingleCharAlternation]
### RegExpSingleCharAlternation
Single character alternation in RegExp
in `core/src/main/java/org/apache/calcite/rel/metadata/janino/RelMetadataHandlerGeneratorUtil.java`
#### Snippet
```java
    //So we need to add the parents class to get a unique name
    if (simpleName.equals("Handler")) {
      String[] parts = clazz.getName().split("\\.|\\$");
      return parts[parts.length - 2] + parts[parts.length - 1];
    } else {
```

## RuleId[id=ReplaceWithOperatorAssignment]
### ReplaceWithOperatorAssignment
Replaceable with operator-assignment
in `core/build.gradle.kts`
#### Snippet
```java
        // Include the jars from the custom configuration to the classpath
        // otherwise the JDBC drivers for each DBMS will be missing
        classpath = classpath + configurations.getAt("test" + db.capitalize())
    }
    integTestAll {
```

## RuleId[id=CloneDeclaresCloneNotSupported]
### CloneDeclaresCloneNotSupported
`clone()` does not declare 'CloneNotSupportedException'
in `core/src/main/java/org/apache/calcite/util/XmlOutput.java`
#### Snippet
```java
    }

    @Override protected StringEscaper clone() {
      StringEscaper clone = new StringEscaper();
      if (translationVector != null) {
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `core/src/main/java/org/apache/calcite/rel/metadata/RelMdColumnUniqueness.java`
#### Snippet
```java
        // If the expression will not influence uniqueness of the
        // projection, then skip it.
        continue;
      }
    }
```

## RuleId[id=ThrowableNotThrown]
### ThrowableNotThrown
Result of `rethrow()` not thrown
in `testkit/src/main/java/org/apache/calcite/test/CalciteAssert.java`
#### Snippet
```java
        assertThat(resultSetFormatter.string(), isLinux(expected));
      } catch (SQLException e) {
        TestUtil.rethrow(e);
      }
    };
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `linq4j/src/main/java/org/apache/calcite/linq4j/tree/ConstantUntypedNull.java`
#### Snippet
```java
  }

  @Override public boolean equals(@Nullable Object o) {
    return o == INSTANCE;
  }
```

## RuleId[id=BigDecimalMethodWithoutRoundingCalled]
### BigDecimalMethodWithoutRoundingCalled
'BigDecimal.divide()' called without a rounding mode argument
in `core/src/main/java/org/apache/calcite/rex/RexInterpreter.java`
#### Snippet
```java
    case DIVIDE:
      return containsNull(values) ? N
          : number(values.get(0)).divide(number(values.get(1)));
    case CAST:
      return cast(values);
```

### BigDecimalMethodWithoutRoundingCalled
'BigDecimal.divide()' called without a rounding mode argument
in `core/src/main/java/org/apache/calcite/sql/type/SqlTypeName.java`
#### Snippet
```java
        // do nothing
      } else if (scale > 0) {
        decimal = decimal.divide(BigDecimal.TEN.pow(scale));
      } else {
        decimal = decimal.multiply(BigDecimal.TEN.pow(-scale));
```

### BigDecimalMethodWithoutRoundingCalled
'BigDecimal.divide()' called without a rounding mode argument
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> BigDecimal average(Enumerable<TSource> source,
      BigDecimalFunction1<TSource> selector) {
    return sum(source, selector).divide(BigDecimal.valueOf(longCount(source)));
  }

```

### BigDecimalMethodWithoutRoundingCalled
'BigDecimal.divide()' called without a rounding mode argument
in `linq4j/src/main/java/org/apache/calcite/linq4j/EnumerableDefaults.java`
#### Snippet
```java
  public static <TSource> BigDecimal average(Enumerable<TSource> source,
      NullableBigDecimalFunction1<TSource> selector) {
    return sum(source, selector).divide(BigDecimal.valueOf(longCount(source)));
  }

```

