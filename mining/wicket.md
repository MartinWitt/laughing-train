# wicket 
 
# Bad smells
I found 6409 bad smells with 941 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| UnnecessaryFullyQualifiedName | 1416 | false |
| MissingSerialAnnotation | 1056 | false |
| ReturnNull | 750 | false |
| PointlessBooleanExpression | 326 | true |
| NonProtectedConstructorInAbstractClass | 276 | true |
| RedundantFieldInitialization | 201 | false |
| AssignmentToMethodParameter | 195 | false |
| BoundedWildcard | 183 | false |
| PatternVariableCanBeUsed | 161 | false |
| RegExpRedundantEscape | 141 | false |
| SizeReplaceableByIsEmpty | 119 | true |
| Convert2Lambda | 86 | false |
| RegExpDuplicateCharacterInClass | 82 | false |
| EmptyMethod | 70 | false |
| NestedAssignment | 68 | false |
| MissortedModifiers | 66 | false |
| UnnecessaryToStringCall | 61 | true |
| ConstantValue | 59 | false |
| UNUSED_IMPORT | 58 | false |
| DataFlowIssue | 54 | false |
| CStyleArrayDeclaration | 41 | false |
| ClassCanBeRecord | 38 | false |
| UnusedAssignment | 35 | false |
| TextBlockMigration | 35 | false |
| UtilityClassWithoutPrivateConstructor | 33 | true |
| FieldAccessedSynchronizedAndUnsynchronized | 32 | false |
| UnnecessaryLocalVariable | 31 | true |
| RedundantMethodOverride | 30 | false |
| DynamicRegexReplaceableByCompiledPattern | 30 | false |
| UnnecessaryModifier | 28 | true |
| PublicFieldAccessedInSynchronizedContext | 25 | false |
| ZeroLengthArrayInitialization | 24 | false |
| StringBufferReplaceableByString | 21 | false |
| RedundantImplements | 20 | false |
| EnhancedSwitchMigration | 20 | false |
| TrivialStringConcatenation | 19 | false |
| UnnecessarySemicolon | 18 | false |
| IOResource | 18 | false |
| InnerClassMayBeStatic | 16 | true |
| RegExpUnnecessaryNonCapturingGroup | 16 | false |
| ReplaceAssignmentWithOperatorAssignment | 15 | false |
| ProtectedMemberInFinalClass | 15 | true |
| UnnecessaryQualifierForThis | 14 | false |
| NonFinalFieldOfException | 14 | false |
| OptionalUsedAsFieldOrParameterType | 12 | false |
| NonSerializableFieldInSerializableClass | 12 | false |
| StringOperationCanBeSimplified | 11 | false |
| AssignmentToForLoopParameter | 11 | false |
| MarkedForRemoval | 10 | false |
| SynchronizeOnThis | 10 | false |
| RedundantSuppression | 10 | false |
| IgnoreResultOfCall | 9 | false |
| RedundantArrayCreation | 9 | true |
| MethodOverridesStaticMethod | 9 | false |
| ToArrayCallWithZeroLengthArrayArgument | 8 | true |
| UnnecessaryCallToStringValueOf | 8 | false |
| Convert2MethodRef | 8 | false |
| Java8MapApi | 8 | false |
| FinalPrivateMethod | 7 | false |
| UseBulkOperation | 7 | false |
| AbstractClassNeverImplemented | 7 | false |
| UnnecessarySuperQualifier | 7 | false |
| FinalStaticMethod | 6 | false |
| ClassNameSameAsAncestorName | 6 | false |
| DuplicateThrows | 6 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 6 | false |
| UseCompareMethod | 6 | false |
| UnnecessaryStringEscape | 6 | true |
| ManualMinMaxCalculation | 6 | false |
| CodeBlock2Expr | 6 | true |
| UnnecessaryBoxing | 6 | false |
| ConditionCoveredByFurtherCondition | 6 | false |
| ReplaceNullCheck | 6 | false |
| WhileCanBeForeach | 5 | false |
| DuplicateBranchesInSwitch | 5 | false |
| UnnecessaryReturn | 5 | true |
| AssignmentToStaticFieldFromInstanceMethod | 4 | false |
| UnnecessaryUnboxing | 4 | false |
| CollectionContainsUrl | 4 | false |
| UtilityClassWithPublicConstructor | 4 | false |
| CharsetObjectCanBeUsed | 4 | false |
| ObsoleteCollection | 4 | false |
| RegExpSimplifiable | 4 | false |
| IfStatementWithIdenticalBranches | 4 | false |
| InstanceofCatchParameter | 4 | false |
| FieldMayBeStatic | 4 | false |
| ComparatorCombinators | 4 | false |
| AbstractMethodCallInConstructor | 4 | false |
| CatchMayIgnoreException | 4 | false |
| UnnecessaryContinue | 4 | false |
| CopyConstructorMissesField | 4 | false |
| CommentedOutCode | 3 | false |
| MethodOverloadsParentMethod | 3 | false |
| NonSynchronizedMethodOverridesSynchronizedMethod | 3 | false |
| SystemOutErr | 3 | false |
| InstanceofIncompatibleInterface | 3 | false |
| ThrowFromFinallyBlock | 3 | false |
| StringBufferReplaceableByStringBuilder | 3 | false |
| MismatchedJavadocCode | 3 | false |
| ThrowablePrintStackTrace | 3 | false |
| KeySetIterationMayUseEntrySet | 2 | false |
| SuspiciousInvocationHandlerImplementation | 2 | false |
| IntegerMultiplicationImplicitCastToLong | 2 | false |
| ListRemoveInLoop | 2 | false |
| ManualArrayToCollectionCopy | 2 | false |
| ObviousNullCheck | 2 | false |
| NonStrictComparisonCanBeEquality | 2 | true |
| EnumSwitchStatementWhichMissesCases | 2 | false |
| SuspiciousSystemArraycopy | 2 | false |
| StaticCallOnSubclass | 2 | false |
| NonShortCircuitBoolean | 2 | false |
| RedundantOperationOnEmptyContainer | 2 | false |
| RedundantCollectionOperation | 2 | false |
| Java9CollectionFactory | 2 | false |
| EqualsWhichDoesntCheckParameterClass | 2 | false |
| WrapperTypeMayBePrimitive | 1 | false |
| ExplicitArrayFilling | 1 | false |
| PointlessBitwiseExpression | 1 | false |
| HtmlWrongAttributeValue | 1 | false |
| CastConflictsWithInstanceof | 1 | false |
| IndexOfReplaceableByContains | 1 | false |
| StringEqualsCharSequence | 1 | false |
| SimplifyStreamApiCallChains | 1 | false |
| RegExpUnexpectedAnchor | 1 | false |
| Java9ReflectionClassVisibility | 1 | false |
| SuspiciousToArrayCall | 1 | false |
| TestCaseWithConstructor | 1 | false |
| BusyWait | 1 | false |
| WaitNotInLoop | 1 | false |
| StringEquality | 1 | false |
| ConditionalBreakInInfiniteLoop | 1 | false |
| RedundantClassCall | 1 | false |
| LongLiteralsEndingWithLowercaseL | 1 | false |
| MismatchedStringBuilderQueryUpdate | 1 | false |
| AnonymousHasLambdaAlternative | 1 | false |
| IfStatementMissingBreakInLoop | 1 | false |
| FunctionalExpressionCanBeFolded | 1 | false |
| WhileLoopSpinsOnField | 1 | false |
| ExceptionNameDoesntEndWithException | 1 | false |
| NonExceptionNameEndsWithException | 1 | false |
| TypeParameterExtendsObject | 1 | false |
| RedundantLengthCheck | 1 | false |
| DeprecatedIsStillUsed | 1 | false |
| Java9UndeclaredServiceUsage | 1 | false |
| StringEqualsEmptyString | 1 | false |
| Anonymous2MethodRef | 1 | false |
| ThreadStartInConstruction | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[l.size()\]'
in `wicket-util/src/main/java/org/apache/wicket/util/diff/ToString.java`
#### Snippet
```java
		{
		}
		return l.toArray(new String[l.size()]);
	}

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new File\[files.size()\]'
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
			files.addAll(Arrays.asList(folder.getNestedFiles(filter)));
		}
		return files.toArray(new File[files.size()]);
	}

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Field\[matched.size()\]'
in `wicket-ioc/src/main/java/org/apache/wicket/injection/Injector.java`
#### Snippet
```java
		}

		return matched.toArray(new Field[matched.size()]);
	}

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FormComponent\[components.size()\]'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
					}
				}
				return components.toArray(new FormComponent[components.size()]);
			}
			return null;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[clientFileNames.size()\]'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUploadField.java`
#### Snippet
```java
				clientFileNames.add(fu.getClientFileName());
			}
			return clientFileNames.toArray(new String[clientFileNames.size()]);
		}
		return null;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[names.size()\]'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/MultiFileUploadField.java`
#### Snippet
```java
			if (names != null)
			{
				inputArrayCache = names.toArray(new String[names.size()]);
			}
		}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Pattern\[patternsList.size()\]'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/AbstractRequestWrapperFactory.java`
#### Snippet
```java
			}
		}
		return patternsList.toArray(new Pattern[patternsList.size()]);
	}

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Cookie\[cookieValues.size()\]'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		List<Cookie> cookieValues = new ArrayList<Cookie>();
		cookieValues.addAll(cookies.values());
		return cookieValues.toArray(new Cookie[cookieValues.size()]);
	}

```

## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	{
		Boolean old = stateless;
		Boolean res = isPageStateless();
		stateless = old;
		return res;
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-util/src/main/java/org/apache/wicket/util/diff/RevisionVisitor.java`
#### Snippet
```java
	 * @param delta
	 */
	public void visit(AddDelta delta);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-util/src/main/java/org/apache/wicket/util/diff/RevisionVisitor.java`
#### Snippet
```java
	 * @param revision
	 */
	public void visit(Revision revision);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-util/src/main/java/org/apache/wicket/util/diff/RevisionVisitor.java`
#### Snippet
```java
	 * @param delta
	 */
	public void visit(DeleteDelta delta);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-util/src/main/java/org/apache/wicket/util/diff/RevisionVisitor.java`
#### Snippet
```java
	 * @param delta
	 */
	public void visit(ChangeDelta delta);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-util/src/main/java/org/apache/wicket/util/diff/DiffAlgorithm.java`
#### Snippet
```java
	 *             if the diff could not be computed.
	 */
	public abstract Revision diff(Object[] orig, Object[] rev)
		throws DifferentiationFailedException;
}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `wicket-util/src/main/java/org/apache/wicket/util/diff/DiffAlgorithm.java`
#### Snippet
```java
	 *             if the diff could not be computed.
	 */
	public abstract Revision diff(Object[] orig, Object[] rev)
		throws DifferentiationFailedException;
}
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `wicket-util/src/main/java/org/apache/wicket/util/xml/CustomEntityResolver.java`
#### Snippet
```java
	 * manages locating an entity
	 */
	public static interface EntityLocator
	{
		/**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Exceptions.java`
#### Snippet
```java
	 * @param <T>
	 */
	public static interface IThrowableVisitor<T>
	{
		/**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
	 * @author Jonathan Locke
	 */
	public static interface FolderFilter
	{
		/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
		 * File filter that matches all files
		 */
		public static FileFilter ALL_FILES = new FileFilter()
		{
			@Override
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
		 * File filter that matches all files
		 */
		public static FileFilter ALL_FILES = new FileFilter()
		{
			@Override
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
		 * @return True if the file should be accepted
		 */
		public boolean accept(File file);
	}

```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
	 * @author Jonathan Locke
	 */
	public static interface FileFilter
	{
		/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
		 * @return True if the file should be accepted
		 */
		public boolean accept(Folder folder);
	}

```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `wicket-util/src/main/java/org/apache/wicket/util/visit/Visit.java`
#### Snippet
```java
public class Visit<R> implements IVisit<R>
{
	private static enum Action {
		CONTINUE, CONTINUE_BUT_DONT_GO_DEEPER, STOP
	}
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
	}

	static interface EntityMap
	{
		/**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `wicket-util/src/main/java/org/apache/wicket/util/listener/ListenerCollection.java`
#### Snippet
```java
	 * @param <T>
	 */
	protected static interface INotifier<T>
	{
		void notify(T listener);
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `wicket-request/src/main/java/org/apache/wicket/request/http/WebResponse.java`
#### Snippet
```java
	 * href="http://www.w3.org/Protocols/rfc2616/rfc2616-sec14.html">RFC-2616</a>.
	 */
	public static enum CacheScope
	{
		/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardModel.java`
#### Snippet
```java
		 * @return True if the step this condition is coupled to is available, false otherwise
		 */
		public boolean evaluate();
	}

```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxChannel.java`
#### Snippet
```java
	 * The type of an {@link AjaxChannel}
	 */
	public static enum Type {

		/**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/WicketObjects.java`
#### Snippet
```java
	 * Interface that enables users to plugin the way object sizes are calculated with Wicket.
	 */
	public static interface IObjectSizeOfStrategy
	{
		/**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `wicket-core/src/main/java/org/apache/wicket/markup/head/PriorityFirstComparator.java`
#### Snippet
```java
public class PriorityFirstComparator implements Comparator<RecordedHeaderItem>, Serializable
{
	protected static enum HeaderItemType
	{
		PRIORITY, PAGE, COMPONENT;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	 * @author igor
	 */
	public static enum MethodMismatchResponse {
		/**
		 * Continue processing.
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
		private final String realName;

		private Cors(String realName)
		{
			this.realName = realName;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlTag.java`
#### Snippet
```java
	 * Enumerated type for different kinds of component tags.
	 */
	public static enum TagType {
		/** A close tag, like &lt;/TAG&gt;. */
		CLOSE,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IResourceIsolationPolicy.java`
#### Snippet
```java
	 * @see IResourceIsolationPolicy#isRequestAllowed(jakarta.servlet.http.HttpServletRequest, org.apache.wicket.request.component.IRequestablePage)
	 */
	public enum ResourceIsolationOutcome
	{
		ALLOWED, DISALLOWED, UNKNOWN
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/AbstractRangeValidator.java`
#### Snippet
```java
	 * @author igor
	 */
	public static enum Mode {
		MINIMUM, MAXIMUM, RANGE, EXACT;

```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java

	/** */
	public static enum CreditCard {
		/** */
		INVALID(null),
```

## RuleId[id=MarkedForRemoval]
### MarkedForRemoval
'setComment(java.lang.String)' is deprecated and marked for removal
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java
		if (comment != null)
		{
			cookie.setComment(comment);
		}

```

### MarkedForRemoval
'setVersion(int)' is deprecated and marked for removal
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java

		cookie.setPath(path);
		cookie.setVersion(settings.getVersion());
		cookie.setSecure(settings.getSecure());
		cookie.setMaxAge(settings.getMaxAge());
```

### MarkedForRemoval
'getVersion()' is deprecated and marked for removal
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java

		cookie.setPath(path);
		cookie.setVersion(settings.getVersion());
		cookie.setSecure(settings.getSecure());
		cookie.setMaxAge(settings.getMaxAge());
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.servlet.SessionCookieConfig'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java

		@Override
		public String getComment()
		{
			return comment;
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.servlet.SessionCookieConfig'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java

		@Override
		public void setComment(String comment)
		{
			this.comment = comment;
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
				() -> StackWalker.getInstance(StackWalker.Option.RETAIN_CLASS_REFERENCE);
		PrivilegedAction<ClassLoader> pa2 = ClassLoader::getPlatformClassLoader;
		STACKWALKER = AccessController.doPrivileged(pa1);
		PLATFORM_CLASS_LOADER = AccessController.doPrivileged(pa2);
	}
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
				() -> StackWalker.getInstance(StackWalker.Option.RETAIN_CLASS_REFERENCE);
		PrivilegedAction<ClassLoader> pa2 = ClassLoader::getPlatformClassLoader;
		STACKWALKER = AccessController.doPrivileged(pa1);
		PLATFORM_CLASS_LOADER = AccessController.doPrivileged(pa2);
	}
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
		PrivilegedAction<ClassLoader> pa2 = ClassLoader::getPlatformClassLoader;
		STACKWALKER = AccessController.doPrivileged(pa1);
		PLATFORM_CLASS_LOADER = AccessController.doPrivileged(pa2);
	}

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
		PrivilegedAction<ClassLoader> pa2 = ClassLoader::getPlatformClassLoader;
		STACKWALKER = AccessController.doPrivileged(pa1);
		PLATFORM_CLASS_LOADER = AccessController.doPrivileged(pa2);
	}

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
import java.lang.reflect.Modifier;
import java.lang.reflect.Proxy;
import java.security.AccessController;
import java.security.PrivilegedAction;
import java.util.Objects;
```

## RuleId[id=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/StatelessForm.java`
#### Snippet
```java
			List<INamedParameters.NamedPair> namedParameters = parameters.getAllNamed();
			Iterator<INamedParameters.NamedPair> iterator = namedParameters.iterator();
			while (iterator.hasNext())
			{
				INamedParameters.NamedPair namedParameter = iterator.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java
		 */
		Iterator<MarkupElement> markupIterator = markup.iterator();
		while (markupIterator.hasNext())
		{
			MarkupElement next = markupIterator.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
			Set<String> usedComponentIds = new HashSet<>();
			Iterator<? extends Component> i = iterator();
			while (i.hasNext())
			{
				Component c = i.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AjaxEnclosureListener.java`
#### Snippet
```java
			{
				Iterator<Map.Entry<String, Component>> entriesItor = map.entrySet().iterator();
				while (entriesItor.hasNext())
				{
					Map.Entry<String, Component> entry = entriesItor.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemResourceReference.java`
#### Snippet
```java
		Iterator<FileSystemPathService> pathServiceIterator = ServiceLoader
			.load(FileSystemPathService.class).iterator();
		while (pathServiceIterator.hasNext())
		{
			FileSystemPathService pathService = pathServiceIterator.next();
```

## RuleId[id=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `META_INF_RESOURCES_SUPPORTED` from instance context
in `wicket-core/src/main/java/org/apache/wicket/request/resource/MetaInfStaticResourceReference.java`
#### Snippet
```java
		{
			int majorVersion = WebApplication.get().getServletContext().getMajorVersion();
			META_INF_RESOURCES_SUPPORTED = majorVersion >= 3;
		}

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `nextId` from instance context
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/ContactGenerator.java`
#### Snippet
```java
	public synchronized long generateId()
	{
		return nextId++;
	}

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `nextId` from instance context
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/Book.java`
#### Snippet
```java
	public Book(final String title, final String author, final boolean isFiction)
	{
		id = nextId++;
		this.title = title;
		this.author = author;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `nextId` from instance context
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactGenerator.java`
#### Snippet
```java
	public synchronized long generateId()
	{
		return nextId++;
	}

```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (2 lines)
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
		// -------------------------------
		// Configure defaults
		// -------------------------------

		// addHeaders = true;
```

### CommentedOutCode
Commented out code (3 lines)
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
				// Try to find the specified factory class
				// see http://bugs.sun.com/bugdatabase/view_bug.do?bug_id=6500212
				// final Class<?> factoryClass = Thread.currentThread()
				// .getContextClassLoader()
				// .loadClass(appFactoryClassName);
```

### CommentedOutCode
Commented out code (2 lines)
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
				{
					// Ignore : fall back to DEVELOPMENT mode
					// log.warn("Unknown runtime configuration type '" + result +
					// "', falling back to DEVELOPMENT mode.");
					throw new IllegalArgumentException("Invalid configuration type: '" + result +
```

## RuleId[id=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of parameter `charArray`
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java
	 */
	@Override
	public void write(final char charArray[], final int offset, final int length)
	{
		buffer.append(charArray, offset, length);
```

### CStyleArrayDeclaration
C-style array declaration of parameter `charArray`
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java
	 */
	@Override
	public void write(final char charArray[])
	{
		buffer.append(charArray);
```

### CStyleArrayDeclaration
C-style array declaration of parameter `b`
in `wicket-util/src/main/java/org/apache/wicket/util/io/ThresholdingOutputStream.java`
#### Snippet
```java
	 */
	@Override
	public void write(final byte b[], final int off, final int len) throws IOException
	{
		checkThreshold(len);
```

### CStyleArrayDeclaration
C-style array declaration of parameter `b`
in `wicket-util/src/main/java/org/apache/wicket/util/io/ThresholdingOutputStream.java`
#### Snippet
```java
	 */
	@Override
	public void write(final byte b[]) throws IOException
	{
		checkThreshold(b.length);
```

### CStyleArrayDeclaration
C-style array declaration of parameter `b`
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteCountingOutputStream.java`
#### Snippet
```java
	 */
	@Override
	public void write(final byte b[], final int off, final int len) throws IOException
	{
		size += len;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `newbuf`
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java
		int remaining = count;
		int pos = 0;
		byte newbuf[] = new byte[count];
		for (int i = 0; i < buffers.size(); i++)
		{
```

### CStyleArrayDeclaration
C-style array declaration of local variable `moduleNames`
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceWebApplicationFactory.java`
#### Snippet
```java
		{
			String paramValue = filter.getFilterConfig().getInitParameter("module");
			String moduleNames[] = Strings.split(paramValue, ',');
			Module modules[] = new Module[moduleNames.length];
			ClassLoader classLoader = Thread.currentThread().getContextClassLoader();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `modules`
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceWebApplicationFactory.java`
#### Snippet
```java
			String paramValue = filter.getFilterConfig().getInitParameter("module");
			String moduleNames[] = Strings.split(paramValue, ',');
			Module modules[] = new Module[moduleNames.length];
			ClassLoader classLoader = Thread.currentThread().getContextClassLoader();
			for (int i = 0; i < moduleNames.length; i++)
```

### CStyleArrayDeclaration
C-style array declaration of local variable `diagonal`
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/MyersDiff.java`
#### Snippet
```java
		final int size = 1 + 2 * MAX;
		final int middle = (size + 1) / 2;
		final PathNode diagonal[] = new PathNode[size];

		diagonal[middle + 1] = new Snake(0, -1, null);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `newValue`
in `wicket-util/src/main/java/org/apache/wicket/util/string/PrependingStringBuffer.java`
#### Snippet
```java
		}

		char newValue[] = new char[newCapacity];
		System.arraycopy(buffer, position, newValue, newCapacity - size, size);
		buffer = newValue;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `destArray`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		{
			int length = Array.getLength(o);
			String destArray[] = new String[length + 1];
			for (int i = 0; i < length; i++)
			{
```

### CStyleArrayDeclaration
C-style array declaration of parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @since 1.2
	 */
	public AppendingStringBuffer insert(final int index, final char str[], final int offset,
		final int len)
	{
```

### CStyleArrayDeclaration
C-style array declaration of parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 */
	public AppendingStringBuffer append(final char str[])
	{
		int len = str.length;
```

### CStyleArrayDeclaration
C-style array declaration of field `value`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @serial
	 */
	private char value[];

	/**
```

### CStyleArrayDeclaration
C-style array declaration of parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 */
	public AppendingStringBuffer append(final char str[], final int offset, final int len)
	{
		int newcount = count + len;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `val`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		{
			int off = 0;
			char val[] = value;
			int len = count;

```

### CStyleArrayDeclaration
C-style array declaration of parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 */
	public AppendingStringBuffer insert(final int offset, final char str[])
	{
		if ((offset < 0) || (offset > count))
```

### CStyleArrayDeclaration
C-style array declaration of local variable `newValue`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		}

		char newValue[] = new char[newCapacity];
		System.arraycopy(value, 0, newValue, 0, count);
		value = newValue;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `v1`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
			if (n == anotherString.count)
			{
				char v1[] = value;
				char v2[] = anotherString.value;
				int i = 0;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `v2`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
			{
				char v1[] = value;
				char v2[] = anotherString.value;
				int i = 0;
				while (n-- != 0)
```

### CStyleArrayDeclaration
C-style array declaration of local variable `v1`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
			if (sequence.length() == count)
			{
				char v1[] = value;
				int i = 0;
				while (n-- != 0)
```

### CStyleArrayDeclaration
C-style array declaration of parameter `dst`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                </ul>
	 */
	public void getChars(final int srcBegin, final int srcEnd, final char dst[], final int dstBegin)
	{
		if (srcBegin < 0)
```

### CStyleArrayDeclaration
C-style array declaration of local variable `ta`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	public boolean startsWith(final CharSequence prefix, final int toffset)
	{
		char ta[] = value;
		int to = toffset;
		int po = 0;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `p`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		}

		int p[] = new int[n + 1]; // 'previous' cost array, horizontally
		int d[] = new int[n + 1]; // cost array, horizontally
		int _d[]; // placeholder to assist in swapping p and d
```

### CStyleArrayDeclaration
C-style array declaration of local variable `d`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java

		int p[] = new int[n + 1]; // 'previous' cost array, horizontally
		int d[] = new int[n + 1]; // cost array, horizontally
		int _d[]; // placeholder to assist in swapping p and d

```

### CStyleArrayDeclaration
C-style array declaration of local variable `_d`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		int p[] = new int[n + 1]; // 'previous' cost array, horizontally
		int d[] = new int[n + 1]; // cost array, horizontally
		int _d[]; // placeholder to assist in swapping p and d

		// indexes into strings s and t
```

### CStyleArrayDeclaration
C-style array declaration of local variable `data`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java
		int BUFFER = 2048;
		BufferedInputStream origin;
		byte data[] = new byte[BUFFER];

		if (files != null)
```

### CStyleArrayDeclaration
C-style array declaration of local variable `tab`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	private boolean containsNullValue()
	{
		Entry<V> tab[] = table;
		for (Entry<V> tabEntry : tab)
		{
```

### CStyleArrayDeclaration
C-style array declaration of local variable `tab`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		modCount.incrementAndGet();
		Entry<V> tab[] = table;
		for (int i = 0; i < tab.length; i++)
		{
```

### CStyleArrayDeclaration
C-style array declaration of local variable `listenerParts`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
			Integer renderCount = null;

			String listenerParts[] = Strings.split(listener, BEHAVIOR_INDEX_SEPARATOR);
			if (listenerParts.length == 0)
			{
```

### CStyleArrayDeclaration
C-style array declaration of field `parameters`
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/CombinedRequestParametersAdapter.java`
#### Snippet
```java
public class CombinedRequestParametersAdapter implements IRequestParameters
{
	private final IRequestParameters parameters[];

	/**
```

### CStyleArrayDeclaration
C-style array declaration of local variable `segmentArray`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			}

			String segmentArray[] = Strings.split(relativeUrl, '/');

			if (removeLast)
```

### CStyleArrayDeclaration
C-style array declaration of local variable `queryArray`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		if (queryString.length() > 0)
		{
			String queryArray[] = Strings.split(queryString, '&');
			for (String s : queryArray)
			{
```

### CStyleArrayDeclaration
C-style array declaration of local variable `b`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
	{
		int num = randomInt(rng, min, max);
		byte b[] = new byte[num];
		for (int i = 0; i < num; i++)
		{
```

### CStyleArrayDeclaration
C-style array declaration of local variable `rtn`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/MarkupUtil.java`
#### Snippet
```java
		Page page = container.getPage();

		final boolean rtn[] = new boolean[] { true };
		page.visitChildren(MarkupContainer.class, new IVisitor<MarkupContainer, Void>()
		{
```

### CStyleArrayDeclaration
C-style array declaration of field `attributeNames`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java

	/** List of attribute names considered */
	private static final String attributeNames[] = new String[] { "href", "src", "background",
			"action" };

```

### CStyleArrayDeclaration
C-style array declaration of local variable `copied`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
	private static void writeStream(final Response response, ByteArrayOutputStream stream)
	{
		final boolean copied[] = { false };
		try
		{
```

### CStyleArrayDeclaration
C-style array declaration of local variable `split`
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
		if (Strings.isEmpty(encodedAttributes) == false)
		{
			String split[] = Strings.split(encodedAttributes, '-');
			locale = parseLocale(split[0]);
			if (split.length == 2)
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
		else
		{
			String parts[] = locale.toLowerCase(Locale.ROOT).split("_", 3);
			if (parts.length == 1)
			{
```

### CStyleArrayDeclaration
C-style array declaration of parameter `data`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
		 * @param data
		 */
		public synchronized void savePage(int pageId, String pageType, byte data[])
		{
			if (sessionIdentifier == null)
```

### CStyleArrayDeclaration
C-style array declaration of local variable `b`
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/Captcha.java`
#### Snippet
```java
	{
		int num = randomInt(min, max);
		byte b[] = new byte[num];
		for (int i = 0; i < num; i++)
			b[i] = (byte)randomInt('a', 'z');
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Bytes.java`
#### Snippet
```java
	/** Pattern for string parsing. */
	private static final Pattern valuePattern = Pattern.compile(
		"([0-9]+([\\.,][0-9]+)?)\\s*(|K|M|G|T)B?", Pattern.CASE_INSENSITIVE);

	/** Maximum bytes value */
```

### RegExpRedundantEscape
Redundant character escape `\\<` in RegExp
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlLicenseHeaderHandler.java`
#### Snippet
```java
{
	private final Pattern xmlHeader = Pattern.compile(
		"^(\\<\\?xml[^" + LINE_ENDING + "]+?)" + LINE_ENDING + "(.*)$",
		Pattern.DOTALL | Pattern.MULTILINE);

```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceUtils.java`
#### Snippet
```java
	/** Regular expression patter to extract the locale from the filename (ex: de_DE) **/
	private static final Pattern LOCALE_MIN_PATTERN = Pattern
		.compile("_([a-z]{2})(_([A-Z]{2})(_([^_\\.]+))?)?(\\.min)?$");
	/** Stores standard ISO country codes from {@code java.util.Locale} **/
	private final static Set<String> isoCountries = new HashSet<>(
```

### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
	{
		// the regex means "doesn't start with 'scheme://'"
		if ((url != null) && (url.startsWith("/") == false) && (!url.matches("^\\w+\\:\\/\\/.*")) &&
			!(url.startsWith("#")))
		{
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
	{
		// the regex means "doesn't start with 'scheme://'"
		if ((url != null) && (url.startsWith("/") == false) && (!url.matches("^\\w+\\:\\/\\/.*")) &&
			!(url.startsWith("#")))
		{
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
	{
		// the regex means "doesn't start with 'scheme://'"
		if ((url != null) && (url.startsWith("/") == false) && (!url.matches("^\\w+\\:\\/\\/.*")) &&
			!(url.startsWith("#")))
		{
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private static final String EMAIL_PATTERN = "(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java

	private static final String EMAIL_PATTERN = "(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java

	private static final String EMAIL_PATTERN = "(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
	private static final String EMAIL_PATTERN = "(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
```

### RegExpRedundantEscape
Redundant character escape `\\<` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
```

### RegExpRedundantEscape
Redundant character escape `\\>` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
{
	private static final Pattern AJAX_COMPONENT_CDATA_OPEN = Pattern.compile("<component.*?><!\\[CDATA\\[");
	private static final Pattern AJAX_COMPONENT_CDATA_CLOSE = Pattern.compile("\\]\\]></component>");

	private final XmlTag openTag;
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
{
	private static final Pattern AJAX_COMPONENT_CDATA_OPEN = Pattern.compile("<component.*?><!\\[CDATA\\[");
	private static final Pattern AJAX_COMPONENT_CDATA_CLOSE = Pattern.compile("\\]\\]></component>");

	private final XmlTag openTag;
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
```

### RegExpRedundantEscape
Redundant character escape `\\<` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
```

### RegExpRedundantEscape
Redundant character escape `\\>` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
```

### RegExpRedundantEscape
Redundant character escape `\\<` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
		+ "?:\\r\\n)?[ \\t])*))*)?;\\s*)";
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
		+ "?:\\r\\n)?[ \\t])*))*)?;\\s*)";

```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
		+ "?:\\r\\n)?[ \\t])*))*)?;\\s*)";

```

### RegExpRedundantEscape
Redundant character escape `\\>` in RegExp
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
		+ "?:\\r\\n)?[ \\t])*))*)?;\\s*)";

```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java

	/** Opening a conditional comment section, which is NOT treated as a comment section */
	public static final Pattern CONDITIONAL_COMMENT_OPENING = Pattern.compile("(s?)^[^>]*?<!--\\[if.*?\\]>(-->)?(<!.*?-->)?");

	private static final Pattern PRE_BLOCK = Pattern.compile("<pre>.*?</pre>", Pattern.DOTALL | Pattern.MULTILINE);
```

### RegExpRedundantEscape
Redundant character escape `\\\"` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
			{
				parser.next();
				return parser.getString().toString().split("\\\"")[1];
			}
		}
```

### RegExpRedundantEscape
Redundant character escape `\\*` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
			regex = regex.replaceAll("^\\*" + PATH_SEPARATOR, "[^" + PATH_SEPARATOR + "]+" +
				PATH_SEPARATOR);
			regex = regex.replaceAll("^[\\*]{2,}" + PATH_SEPARATOR, "([^" + PATH_SEPARATOR +
				"].#star#" + PATH_SEPARATOR + ")?");

```

### RegExpRedundantEscape
Redundant character escape `\\*` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
			regex = regex.replaceAll(PATH_SEPARATOR + "\\*" + PATH_SEPARATOR, PATH_SEPARATOR +
				"[^" + PATH_SEPARATOR + "]+" + PATH_SEPARATOR);
			regex = regex.replaceAll(PATH_SEPARATOR + "[\\*]{2,}" + PATH_SEPARATOR, "(" +
				PATH_SEPARATOR + "|" + PATH_SEPARATOR + ".+" + PATH_SEPARATOR + ")");

```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/StyleAndScriptIdentifier.java`
#### Snippet
```java

	// OES/*OES<![CDATA[OES*/OES
	private static final Pattern JS_CDATA_START_COMMENT = Pattern.compile("^\\s*\\/\\*\\s*<!\\[CDATA\\[\\s*\\*\\/\\s*.*", Pattern.DOTALL);

	boolean shouldWrapInCdata(final String elementBody)
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/StyleAndScriptIdentifier.java`
#### Snippet
```java

	// OES/*OES<![CDATA[OES*/OES
	private static final Pattern JS_CDATA_START_COMMENT = Pattern.compile("^\\s*\\/\\*\\s*<!\\[CDATA\\[\\s*\\*\\/\\s*.*", Pattern.DOTALL);

	boolean shouldWrapInCdata(final String elementBody)
```

### RegExpRedundantEscape
Redundant character escape `\.` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java

	private static final String AUTHORITY_PATTERN = "^(.+(:.*)?@)?([" + AUTHORITY_CHARS +
		"]*)(:\\d*)?(.*)?";

	private static final int PARSE_AUTHORITY_HOST_IP = 3;
```

### RegExpRedundantEscape
Redundant character escape `\\(` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
	private static final int PARSE_AUTHORITY_EXTRA = 5; // Should always be empty.

	private static final String PATH_PATTERN = "^(/[-\\w:@&?=+,.!/~*'%$_;\\(\\)]*)?$";

	private static final String QUERY_PATTERN = "^(.*)$";
```

### RegExpRedundantEscape
Redundant character escape `\\)` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
	private static final int PARSE_AUTHORITY_EXTRA = 5; // Should always be empty.

	private static final String PATH_PATTERN = "^(/[-\\w:@&?=+,.!/~*'%$_;\\(\\)]*)?$";

	private static final String QUERY_PATTERN = "^(.*)$";
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java

	// Drop numeric, and "+-." for now
	private static final String AUTHORITY_CHARS = ALPHA_NUMERIC_CHARS + "\\-\\.";

	private static final String ATOM = VALID_CHARS + '+';
```

## RuleId[id=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `data.postParameters.keySet()` may be replaced with 'entrySet()' iteration
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
					IWritableRequestParameters parameters = (IWritableRequestParameters)request.getPostParameters();
					parameters.reset();
					for (String s : data.postParameters.keySet())
					{
						parameters.setParameterValues(s, data.postParameters.get(s));
```

### KeySetIterationMayUseEntrySet
Iteration over `headers.keySet()` may be replaced with 'entrySet()' iteration
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
			webResponse.addCookie(cookie);
		}
		for (String name : headers.keySet())
		{
			webResponse.setHeader(name, headers.get(name).toString());
```

## RuleId[id=UnnecessaryQualifierForThis]
### UnnecessaryQualifierForThis
Qualifier `AjaxLazyLoadPanel` on 'this' is unnecessary in this context
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
				// replace the loading component with the new component
				// note: use addOrReplace(), since onConfigure() might not have been called yet 
				AjaxLazyLoadPanel.this.addOrReplace(content);

				Optional<AjaxRequestTarget> target = getRequestCycle().find(AjaxRequestTarget.class);
```

### UnnecessaryQualifierForThis
Qualifier `AjaxLazyLoadPanel` on 'this' is unnecessary in this context
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
				// repaint our selves if there's an AJAX request in play, otherwise let the page
				// redraw itself
				target.ifPresent(t -> t.add(AjaxLazyLoadPanel.this));
			}
		}
```

### UnnecessaryQualifierForThis
Qualifier `AjaxEditableLabel` on 'this' is unnecessary in this context
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
		label.setVisible(true);
		editor.setVisible(false);
		target.add(AjaxEditableLabel.this);

		target.appendJavaScript("window.status='';");
```

### UnnecessaryQualifierForThis
Qualifier `AjaxEditableLabel` on 'this' is unnecessary in this context
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
		label.setVisible(false);
		editor.setVisible(true);
		target.add(AjaxEditableLabel.this);
		String selectScript = String.format(
			"(function(){var el = Wicket.$('%s'); if (el.select) el.select();})()",
```

### UnnecessaryQualifierForThis
Qualifier `AjaxEditableLabel` on 'this' is unnecessary in this context
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
		editor.setVisible(false);
		editor.clearInput();
		target.add(AjaxEditableLabel.this);
	}

```

### UnnecessaryQualifierForThis
Qualifier `ComponentRenderer` on 'this' is unnecessary in this context
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		}
		
		application.setName("ComponentRenderer[" + System.identityHashCode(ComponentRenderer.this) + "]");
		application.initApplication();
	}
```

### UnnecessaryQualifierForThis
Qualifier `FormComponentPanel` on 'this' is unnecessary in this context
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentPanel.java`
#### Snippet
```java
		// Visit all the (visible) form components and clear the input on each.
		visitFormComponentsPostOrder(this, (IVisitor<FormComponent<?>, Void>) (formComponent, visit) -> {
			if (formComponent != FormComponentPanel.this && formComponent.isVisibleInHierarchy())
			{
				formComponent.clearInput();
```

### UnnecessaryQualifierForThis
Qualifier `FeedbackPanel` on 'this' is unnecessary in this context
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
		Serializable rawMessage = message.getMessage();
		Label label = new Label(id, rawMessage);
		label.setEscapeModelStrings(FeedbackPanel.this.getEscapeModelStrings());
		return label;
	}
```

### UnnecessaryQualifierForThis
Qualifier `AbstractCaptchaForm` on 'this' is unnecessary in this context
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/AbstractCaptchaForm.java`
#### Snippet
```java

		form.add(new RequiredTextField<String>("text",
				new PropertyModel<>(AbstractCaptchaForm.this, "captchaText"), String.class)
		{
			@Override
```

### UnnecessaryQualifierForThis
Qualifier `AbstractCaptchaForm` on 'this' is unnecessary in this context
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/AbstractCaptchaForm.java`
#### Snippet
```java

		final FeedbackPanel feedback = new FeedbackPanel("feedback",
				new ContainerFeedbackMessageFilter(AbstractCaptchaForm.this));
		form.add(feedback);

```

### UnnecessaryQualifierForThis
Qualifier `MailTemplate` on 'this' is unnecessary in this context
in `wicket-examples/src/main/java/org/apache/wicket/examples/asemail/MailTemplate.java`
#### Snippet
```java

		TextField<String> nameTextField = new TextField<>("name", new PropertyModel<String>(
			MailTemplate.this, "name"));
		nameTextField.setOutputMarkupId(true);
		form.add(nameTextField);
```

### UnnecessaryQualifierForThis
Qualifier `TabbedPanelPage` on 'this' is unnecessary in this context
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
		// between different css variations
		final TabbedPanel<ITab> tabbedPanel = new TabbedPanel<>("tabs", tabs);
		tabbedPanel.add(AttributeModifier.replace("class", TabbedPanelPage.this.getDefaultModel()));
		add(tabbedPanel);
	}
```

### UnnecessaryQualifierForThis
Qualifier `ProgressUpdateTask` on 'this' is unnecessary in this context
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressUpdater.java`
#### Snippet
```java
						running = false;
						broadcaster.broadcastAllMatchingFilter(application, (sessionId, key) ->
								ProgressUpdateTask.this.sessionId.equals(sessionId) && key instanceof PageIdKey
										&& ITaskProgressListener.class.isAssignableFrom(getPageClass(application, key)),
								new TaskCanceled());
```

### UnnecessaryQualifierForThis
Qualifier `ProgressUpdateTask` on 'this' is unnecessary in this context
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressUpdater.java`
#### Snippet
```java
					}
					broadcaster.broadcastAllMatchingFilter(application, (sessionId, key) ->
							ProgressUpdateTask.this.sessionId.equals(sessionId) && key instanceof PageIdKey &&
									ITaskProgressListener.class.isAssignableFrom(getPageClass(application, key)),
							new ProgressUpdate(progress));
```

## RuleId[id=FinalPrivateMethod]
### FinalPrivateMethod
'private' method declared `final`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	 * @return the found charset or an empty string.
	 */
	private final String searchCharSet(final String key)
	{
		if ((key != null) && (key.length() > 0))
```

### FinalPrivateMethod
'private' method declared `final`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	 * @return the found charset or an empty string.
	 */
	private final String searchCharSet(final String[] items, final StringBuilder base, int count)
	{
		if ((--count >= 0) && (items[count] != null) && (items[count].length() > 0))
```

### FinalPrivateMethod
'private' method declared `final`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	 * @return the found charset or an empty string.
	 */
	private final String searchCharSet(final String[] items)
	{
		String charset;
```

### FinalPrivateMethod
'private' method declared `final`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/ListenerRequestHandler.java`
#### Snippet
```java
	 *             when listener invocation attempted on a component that does not allow it
	 */
	private final void invoke(final IRequestCycle requestCycle, RedirectPolicy policy, boolean ajax, final IRequestableComponent rcomponent, final Behavior behavior)
	{
		// we are in Wicket core land
```

### FinalPrivateMethod
'private' method declared `final`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/ListenerRequestHandler.java`
#### Snippet
```java
	 *             when listener invocation attempted on a component that does not allow it
	 */
	private final void invoke(final IRequestCycle requestCycle, RedirectPolicy policy, boolean ajax, final IRequestableComponent rcomponent)
	{
		// we are in Wicket core land
```

### FinalPrivateMethod
'private' method declared `final`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * Verify super calls of an overridden hook method.
	 */
	private final void verifySuperCall(String method, short flag)
	{
		if (!getRequestFlag(flag))
```

### FinalPrivateMethod
'private' method declared `final`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	 * @return the id of the hidden div
	 */
	private final String getHiddenFieldsId(int idx)
	{
		return getInputNamePrefix() + getMarkupId() + "_hf_" + idx;
```

## RuleId[id=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`charsetName.length() == 0` can be replaced with 'charsetName.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteOrderMark.java`
#### Snippet
```java
     */
    public ByteOrderMark(final String charsetName, final int... bytes) {
        if (charsetName == null || charsetName.length() == 0) {
            throw new IllegalArgumentException("No charsetName specified");
        }
```

### SizeReplaceableByIsEmpty
`encoding.length() == 0` can be replaced with 'encoding.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/io/XmlReader.java`
#### Snippet
```java
		// Extract the encoding
		String encoding = matcher.group(2);
		if ((encoding == null) || (encoding.length() == 0))
		{
			encoding = matcher.group(3);
```

### SizeReplaceableByIsEmpty
`result.size() > 0` can be replaced with '!result.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Diff.java`
#### Snippet
```java
			int pos = r.nextInt(result.size() + 1);
			int len = Math.min(result.size() - pos, 1 + r.nextInt(4));
			if (del && (result.size() > 0))
			{ // delete
				result.subList(pos, pos + len).clear();
```

### SizeReplaceableByIsEmpty
`urlPatterns.size() == 0` can be replaced with 'urlPatterns.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/file/WebXmlFile.java`
#### Snippet
```java
			document.getChildNodes());

		if (urlPatterns.size() == 0)
		{
			log.warn("web.xml: No url-pattern found for '{}' with name '{}'", tag, filterName);
```

### SizeReplaceableByIsEmpty
`s.length() == 0` can be replaced with 's.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Objects.java`
#### Snippet
```java
		String s = stringValue(value, true);

		return (s.length() == 0) ? 0.0 : Double.parseDouble(s);
	}

```

### SizeReplaceableByIsEmpty
`sb.length() > 0` can be replaced with '!sb.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlLicenseHeaderHandler.java`
#### Snippet
```java
				for (int i = 0; (i < 16) && (i < headers.length); i++)
				{
					if (sb.length() > 0)
					{
						sb.append(LINE_ENDING);
```

### SizeReplaceableByIsEmpty
`string.length() == 0` can be replaced with 'string.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
	public static boolean isEmpty(final CharSequence string)
	{
		return string == null || string.length() == 0 ||
			(string.charAt(0) <= ' ' && string.toString().trim().isEmpty());
	}
```

### SizeReplaceableByIsEmpty
`searchFor.length() == 0` can be replaced with 'searchFor.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		// If searchFor is null or the empty string, then there is nothing to
		// replace, so returning s is the only option here.
		if ((searchFor == null) || searchFor.length() == 0)
		{
			return s;
```

### SizeReplaceableByIsEmpty
`files.size() > 0` can be replaced with '!files.isEmpty()'
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
	private void failIncorrectLicenceHeaders(final Map<ILicenseHeaderHandler, List<File>> files)
	{
		if (files.size() > 0)
		{
			StringBuilder failString = new StringBuilder();
```

### SizeReplaceableByIsEmpty
`beansToInjectMap.size() > 0` can be replaced with '!beansToInjectMap.isEmpty()'
in `wicket-spring/src/main/java/org/apache/wicket/spring/FieldBeansCollector.java`
#### Snippet
```java
	public Object getBeansToInject()
	{
		if (beansToInjectMap != null && beansToInjectMap.size() > 0)
		{
			return beansToInjectMap;
```

### SizeReplaceableByIsEmpty
`beansToInjectColl.size() > 0` can be replaced with '!beansToInjectColl.isEmpty()'
in `wicket-spring/src/main/java/org/apache/wicket/spring/FieldBeansCollector.java`
#### Snippet
```java
		}
		
		if (beansToInjectColl != null && beansToInjectColl.size() > 0)
		{
			return beansToInjectColl;
```

### SizeReplaceableByIsEmpty
`beans.size() == 0` can be replaced with 'beans.isEmpty()'
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringWebApplicationFactory.java`
#### Snippet
```java
			Map<?, ?> beans = BeanFactoryUtils.beansOfTypeIncludingAncestors(applicationContext,
				WebApplication.class, false, false);
			if (beans.size() == 0)
			{
				throw new IllegalStateException("bean of type [" + WebApplication.class.getName() +
```

### SizeReplaceableByIsEmpty
`groups.size() == 0` can be replaced with 'groups.isEmpty()'
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/PropertyValidator.java`
#### Snippet
```java
			if (config.isRequiredConstraint(constraint))
			{
				if (canApplyToDefaultGroup(constraint) && groups.size() == 0)
				{
					return true;
```

### SizeReplaceableByIsEmpty
`groups.size() == 0` can be replaced with 'groups.isEmpty()'
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/PropertyValidator.java`
#### Snippet
```java
		//the constraint can be applied to default group either if its group array is empty
		//or if it contains jakarta.validation.groups.Default
		return groups.size() == 0 || groups.contains(Default.class);
	}

```

### SizeReplaceableByIsEmpty
`compatibleMappers.size() == 0` can be replaced with 'compatibleMappers.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/CompoundRequestMapper.java`
#### Snippet
```java
			}
		}
		if (compatibleMappers.size() == 0)
		{
			LOG.debug("No compatible mapper found for URL '{}'", url);
```

### SizeReplaceableByIsEmpty
`behaviors.size() == 0` can be replaced with 'behaviors.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanelTester.java`
#### Snippet
```java
		// getting the contents of the lazy panel
		List<AjaxLazyLoadTimer> behaviors = page.getBehaviors(AjaxLazyLoadTimer.class);
		if (behaviors.size() == 0)
		{
			logger.warn("No timer behavior for AjaxLazyLoadPanel found. A curious situation...");
```

### SizeReplaceableByIsEmpty
`str.length() > 0` can be replaced with '!str.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/parameter/PageParameters.java`
#### Snippet
```java
		}

		if (str.length() > 0)
		{
			str.append(", ");
```

### SizeReplaceableByIsEmpty
`key.length() > 0` can be replaced with '!key.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	private final String searchCharSet(final String key)
	{
		if ((key != null) && (key.length() > 0))
		{
			// Go through mappers.
```

### SizeReplaceableByIsEmpty
`variant.length() > 0` can be replaced with '!variant.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	{
		// Check the cache first.
		if ((variant != null) && (variant.length() > 0))
		{
			String key = locale.toString();
```

### SizeReplaceableByIsEmpty
`key.length() == 0` can be replaced with 'key.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
		{
			String key = locale.toString();
			if (key.length() == 0)
			{
				key = "__" + locale.getVariant();
```

### SizeReplaceableByIsEmpty
`locale.getCountry().length() == 0` can be replaced with 'locale.getCountry().isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
				}
			}
			else if (locale.getCountry().length() == 0)
			{
				key += "__" + variant;
```

### SizeReplaceableByIsEmpty
`charset.length() == 0` can be replaced with 'charset.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java

			String charset = searchCharSet(key);
			if (charset.length() == 0)
			{
				// Not found, perform a full search and update the cache.
```

### SizeReplaceableByIsEmpty
`charset.length() == 0` can be replaced with 'charset.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java

				charset = searchCharSet(items);
				if (charset.length() == 0)
				{
					charset = DEFAULT_CHARSET;
```

### SizeReplaceableByIsEmpty
`charset.length() > 0` can be replaced with '!charset.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	{
		final String charset = searchCharSet(key);
		return charset.length() > 0 ? charset : DEFAULT_CHARSET;
	}

```

### SizeReplaceableByIsEmpty
`items[count].length() > 0` can be replaced with '!items\[count\].isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	private final String searchCharSet(final String[] items, final StringBuilder base, int count)
	{
		if ((--count >= 0) && (items[count] != null) && (items[count].length() > 0))
		{
			String charset;
```

### SizeReplaceableByIsEmpty
`charset.length() > 0` can be replaced with '!charset.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java

				charset = searchCharSet(items, base, i);
				if (charset.length() > 0)
				{
					return charset;
```

### SizeReplaceableByIsEmpty
`charset.length() > 0` can be replaced with '!charset.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	{
		String charset = searchCharSet(key);
		return charset.length() > 0 ? charset : def;
	}

```

### SizeReplaceableByIsEmpty
`charset.length() > 0` can be replaced with '!charset.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
		{
			charset = searchCharSet(items, sb, i);
			if (charset.length() > 0)
			{
				return charset;
```

### SizeReplaceableByIsEmpty
`key.length() == 0` can be replaced with 'key.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
		// Check the cache first.
		String key = locale.toString();
		if (key.length() == 0)
		{
			key = "__" + locale.getVariant();
```

### SizeReplaceableByIsEmpty
`charset.length() == 0` can be replaced with 'charset.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java

		String charset = searchCharSet(key);
		if (charset.length() == 0)
		{
			// Not found, perform a full search and update the cache.
```

### SizeReplaceableByIsEmpty
`charset.length() == 0` can be replaced with 'charset.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java

			charset = searchCharSet(items);
			if (charset.length() == 0)
			{
				charset = DEFAULT_CHARSET;
```

### SizeReplaceableByIsEmpty
`builder.length() > 0` can be replaced with '!builder.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/Select.java`
#### Snippet
```java
				if (isSelected(option.getDefaultModel()))
				{
					if (builder.length() > 0)
					{
						builder.append(VALUE_SEPARATOR);
```

### SizeReplaceableByIsEmpty
`last.length() == 0` can be replaced with 'last.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		}
		String last = segments.get(segments.size() - 1);
		return last.length() == 0;
	}

```

### SizeReplaceableByIsEmpty
`s.length() > 0` can be replaced with '!s.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		for (String s : segments)
		{
			if ((s.length() > 0) && !".".equals(s) && !"..".equals(s))
			{
				return true;
```

### SizeReplaceableByIsEmpty
`last.length() > 0` can be replaced with '!last.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		}
		String last = segments.get(segments.size() - 1);
		return (last.length() > 0) && !".".equals(last) && !"..".equals(last);
	}

```

### SizeReplaceableByIsEmpty
`getSegments().size() > 0` can be replaced with '!getSegments().isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
	public void resolveRelative(final Url relative)
	{
		if (getSegments().size() > 0)
		{
			// strip the first non-folder segment (if it is not empty)
```

### SizeReplaceableByIsEmpty
`this.segments.get(0).length() == 0` can be replaced with 'this.segments.get(0).isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		}

		if ((this.segments.size() == 1) && (this.segments.get(0).length() == 0))
		{
			this.segments.clear();
```

### SizeReplaceableByIsEmpty
`query.length() != 0` can be replaced with '!query.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		for (QueryParameter parameter : queryParameters)
		{
			if (query.length() != 0) {
				query.append('&');
			}
```

### SizeReplaceableByIsEmpty
`relativeUrl.length() > 0` can be replaced with '!relativeUrl.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		}

		if (relativeUrl.length() > 0)
		{
			boolean removeLast = false;
```

### SizeReplaceableByIsEmpty
`queryString.length() > 0` can be replaced with '!queryString.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		}

		if (queryString.length() > 0)
		{
			String queryArray[] = Strings.split(queryString, '&');
```

### SizeReplaceableByIsEmpty
`tabs.size() == 0` can be replaced with 'tabs.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
		final Component component;

		if (currentTab == -1 || (tabs.size() == 0) || !getVisiblityCache().isVisible(currentTab))
		{
			// no tabs or the current tab is not visible
```

### SizeReplaceableByIsEmpty
`models.size() == 0` can be replaced with 'models.isEmpty()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/ProviderSubset.java`
#### Snippet
```java
	public boolean isEmpty()
	{
		return models.size() == 0;
	}

```

### SizeReplaceableByIsEmpty
`roles.size() == 0` can be replaced with 'roles.isEmpty()'
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/ActionPermissions.java`
#### Snippet
```java
		// so that removing authorization can't suddenly open something up to
		// everyone.
		if (roles.size() == 0)
		{
			roles.add(MetaDataRoleAuthorizationStrategy.NO_ROLE);
```

### SizeReplaceableByIsEmpty
`roles.size() == 0` can be replaced with 'roles.isEmpty()'
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/InstantiationPermissions.java`
#### Snippet
```java
		// so that removing authorization can't suddenly open something up to
		// everyone.
		if (roles.size() == 0)
		{
			roles.add(MetaDataRoleAuthorizationStrategy.NO_ROLE);
```

### SizeReplaceableByIsEmpty
`result.length() > 0` can be replaced with '!result.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/ClassAttributeModifier.java`
#### Snippet
```java
		for (String cls : newClasses)
		{
			if (result.length() > 0)
			{
				result.append(separator);
```

### SizeReplaceableByIsEmpty
`result.length() > 0` can be replaced with '!result.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/ClassAttributeModifier.java`
#### Snippet
```java
			result.append(cls);
		}
		return result.length() > 0 ? result.toString() : VALUELESS_ATTRIBUTE_REMOVE;
	}

```

### SizeReplaceableByIsEmpty
`tags.size() > 0` can be replaced with '!tags.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/DequeueContext.java`
#### Snippet
```java
			tags.push(taken);
		}
		else if (tags.size() > 0 && taken.closes(tags.peek()))
		{
			tags.pop();
```

### SizeReplaceableByIsEmpty
`result.length() > 0` can be replaced with '!result.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/StyleAttributeModifier.java`
#### Snippet
```java
				.append(separator);
		}
		return result.length() > 0 ? result.toString() : VALUELESS_ATTRIBUTE_REMOVE;
	}

```

### SizeReplaceableByIsEmpty
`unrenderedComponents.size() > 0` can be replaced with '!unrenderedComponents.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java

			// Throw exception if any errors were found
			if (unrenderedComponents.size() > 0)
			{
				renderedComponents = null;
```

### SizeReplaceableByIsEmpty
`unrenderedComponents.size() > 0` can be replaced with '!unrenderedComponents.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java

				// if still > 0
				if (unrenderedComponents.size() > 0)
				{
					// Throw exception
```

### SizeReplaceableByIsEmpty
`exp.length() == 0` can be replaced with 'exp.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
		{
			exp = expressionBracketsSeperated.substring(lastIndex, index);
			if (exp.length() == 0)
			{
				exp = expressionBracketsSeperated.substring(index + 1);
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java
					Checks.notEmpty(segment, "Caching strategy returned empty name for '%s'", resourceUrl);
				}
				if (name.length() > 0)
				{
					name.append('/');
```

### SizeReplaceableByIsEmpty
`url.getSegments().size() > 0` can be replaced with '!url.getSegments().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/HomePageMapper.java`
#### Snippet
```java
		final Url url = request.getUrl().canonical();

		if (url.getSegments().size() > 0)
		{
			// home page cannot have segments/indexed parameters
```

### SizeReplaceableByIsEmpty
`encryptedUrl.getSegments().size() > 0` can be replaced with '!encryptedUrl.getSegments().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
		if (url == null)
		{
			if (encryptedUrl.getSegments().size() > 0
				&& encryptedUrl.getSegments().get(0).equals(getContext().getNamespace()))
			{
```

### SizeReplaceableByIsEmpty
`request.getOriginalUrl().getSegments().size() > 0` can be replaced with '!request.getOriginalUrl().getSegments().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
				 * CryptoMapper.
				 */
				if (request.getOriginalUrl().getSegments().size() > 0
					&& request.getOriginalUrl().getSegments().get(0).equals(getContext().getNamespace()))
				{
```

### SizeReplaceableByIsEmpty
`url.getSegments().size() > 0` can be replaced with '!url.getSegments().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
	protected Url encryptUrl(final Url url)
	{
		if (url.getSegments().size() > 0
			&& url.getSegments().get(0).equals(getContext().getNamespace()))
		{
```

### SizeReplaceableByIsEmpty
`messages.size() > 0` can be replaced with '!messages.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			return false;
		}
		return messages.size() > 0;
	}

```

### SizeReplaceableByIsEmpty
`path.length() == 0` can be replaced with 'path.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	{
		// Path to this component is an empty path
		if (path.length() == 0)
		{
			return this;
```

### SizeReplaceableByIsEmpty
`tester.size() == 0` can be replaced with 'tester.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
	{
		List<TagTester> tester = createTags(markup, xmlTag -> xmlTag.getName().equalsIgnoreCase(tagName), true);
		if ((tester == null) || (tester.size() == 0))
		{
			return null;
```

### SizeReplaceableByIsEmpty
`stack.size() > 0` can be replaced with '!stack.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
	private static XmlTag findOpenTag(XmlTag closeTag, Stack<XmlTag> stack)
	{
		while (stack.size() > 0)
		{
			XmlTag popped = stack.pop();
```

### SizeReplaceableByIsEmpty
`tester.size() == 0` can be replaced with 'tester.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
	{
		List<TagTester> tester = createTagsByAttribute(markup, attribute, value, true);
		if ((tester == null) || (tester.size() == 0))
		{
			return null;
```

### SizeReplaceableByIsEmpty
`parameter.trim().length() > 0` can be replaced with '!parameter.trim().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
			.toString();

		return parameter != null && parameter.trim().length() > 0;
	}

```

### SizeReplaceableByIsEmpty
`buffer.length() == 0` can be replaced with 'buffer.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/util/template/PackageTextTemplate.java`
#### Snippet
```java
	 */
	private void load() {
		if (buffer.length() == 0)
		{
			String path = Packages.absolutePath(scope, fileName);
```

### SizeReplaceableByIsEmpty
`getAttributes().size() > 0` can be replaced with '!getAttributes().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
		}

		if (getAttributes().size() > 0)
		{
			for (String key : getAttributes().keySet())
```

### SizeReplaceableByIsEmpty
`text.length() > 0` can be replaced with '!text.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
						// Add text from last position to the current tag position
						CharSequence text = xmlParser.getInputFromPositionMarker(tag.getPos());
						if (text.length() > 0)
						{
							text = handleRawText(text.toString());
```

### SizeReplaceableByIsEmpty
`text.length() > 0` can be replaced with '!text.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
			// Add remaining input string
			final CharSequence text = xmlParser.getInputFromPositionMarker(-1);
			if (text.length() > 0)
			{
				markup.addMarkupElement(new RawMarkup(text));
```

### SizeReplaceableByIsEmpty
`text.length() > 0` can be replaced with '!text.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
		// Add tail?
		CharSequence text = xmlParser.getInputFromPositionMarker(-1);
		if (text.length() > 0)
		{
			text = handleRawText(text.toString());
```

### SizeReplaceableByIsEmpty
`domReadScript.length() > 0` can be replaced with '!domReadScript.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/ResourceAggregator.java`
#### Snippet
```java
			}
		}
		if (domReadScript.length() > 0)
		{
			domReadScript.append("\nWicket.Event.publish(Wicket.Event.Topic.AJAX_HANDLERS_BOUND);\n");
```

### SizeReplaceableByIsEmpty
`onLoadScript.length() > 0` can be replaced with '!onLoadScript.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/ResourceAggregator.java`
#### Snippet
```java
			}
		}
		if (onLoadScript.length() > 0)
		{
			getRealResponse().render(
```

### SizeReplaceableByIsEmpty
`url.getSegments().size() > 0` can be replaced with '!url.getSegments().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	private Url transform(final Url url)
	{
		while (url.getSegments().size() > 0 &&
			(url.getSegments().get(0).equals("..") || url.getSegments().get(0).equals(".")))
		{
```

### SizeReplaceableByIsEmpty
`collectedHeaderOutput.length() > 0` can be replaced with '!collectedHeaderOutput.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebPage.java`
#### Snippet
```java
				headerResponse.close();
				CharSequence collectedHeaderOutput = tempResponse.getBuffer();
				if (collectedHeaderOutput.length() > 0)
				{
					reportMissingHead(collectedHeaderOutput);
```

### SizeReplaceableByIsEmpty
`buffer.length() > 0` can be replaced with '!buffer.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java
			for (T object : selectedValues)
			{
				if (buffer.length() > 0)
				{
					buffer.append(VALUE_SEPARATOR);
```

### SizeReplaceableByIsEmpty
`builder.length() > 0` can be replaced with '!builder.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
				if (ts.contains(check.getModelObject()))
				{
					if (builder.length() > 0)
					{
						builder.append(VALUE_SEPARATOR);
```

### SizeReplaceableByIsEmpty
`new FeedbackCollector(getPage()).collect(getFeedbackMessageFilter()).size() > 0` can be replaced with '!new FeedbackCollector(getPage()).collect(getFeedbackMessageFilter()).isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/FormComponentFeedbackIndicator.java`
#### Snippet
```java
		super.onConfigure();
		// Get the messages for the current page
		setVisible(new FeedbackCollector(getPage()).collect(getFeedbackMessageFilter()).size() > 0);
	}

```

### SizeReplaceableByIsEmpty
`new FeedbackCollector(getPage()).collect(getMessagesFilter()).size() > 0` can be replaced with '!new FeedbackCollector(getPage()).collect(getMessagesFilter()).isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/FormComponentFeedbackBorder.java`
#### Snippet
```java
		super.onBeforeRender();
		// Get the messages for the current page
		visible = new FeedbackCollector(getPage()).collect(getMessagesFilter()).size() > 0;
	}

```

### SizeReplaceableByIsEmpty
`sb.length() != 0` can be replaced with '!sb.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ClientSideImageMap.java`
#### Snippet
```java
			{
				case PathIterator.SEG_MOVETO :
					if (sb.length() != 0)
					{
						sb.append(',');
```

### SizeReplaceableByIsEmpty
`sb.length() != 0` can be replaced with '!sb.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ClientSideImageMap.java`
#### Snippet
```java
					break;
				case PathIterator.SEG_LINETO :
					if (sb.length() != 0)
					{
						sb.append(',');
```

### SizeReplaceableByIsEmpty
`sb.length() != 0` can be replaced with '!sb.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ClientSideImageMap.java`
#### Snippet
```java
					break;
				case PathIterator.SEG_CLOSE :
					if (sb.length() != 0)
					{
						sb.append(',');
```

### SizeReplaceableByIsEmpty
`url.length() > 0` can be replaced with '!url.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
		if (contextRelative)
		{
			if (url.length() > 0 && url.charAt(0) == '/')
			{
				url = url.substring(1);
```

### SizeReplaceableByIsEmpty
`output.length() > 0` can be replaced with '!output.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java
	{
		CharSequence output = response.getBuffer();
		if (output.length() > 0)
		{
			if (output.charAt(0) == '\r')
```

### SizeReplaceableByIsEmpty
`output.length() > 0` can be replaced with '!output.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java

			// Automatically add <head> if necessary
			if (output.length() > 0)
			{
				if (renderOpenAndCloseTags())
```

### SizeReplaceableByIsEmpty
`bodyOutput.length() > 0` can be replaced with '!bodyOutput.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java
				headerStreamState.getOpenTag());
			CharSequence bodyOutput = getCleanResponse(bodyResponse);
			if (bodyOutput.length() > 0)
			{
				getHeaderResponse().render(new PageHeaderItem(bodyOutput));
```

### SizeReplaceableByIsEmpty
`stack.size() > 0` can be replaced with '!stack.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/TagStack.java`
#### Snippet
```java
		// No more tags from the markup.
		// If there's still a non-simple tag left, it's an error
		if (stack.size() > 0)
		{
			Iterator<ComponentTag> it = stack.descendingIterator();
```

### SizeReplaceableByIsEmpty
`stack.size() > 0` can be replaced with '!stack.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/TagStack.java`
#### Snippet
```java
	{
		// Check that there is something on the stack
		if (stack.size() > 0)
		{
			// Pop the top tag off the stack
```

### SizeReplaceableByIsEmpty
`attributes.size() > 0` can be replaced with '!attributes.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlTag.java`
#### Snippet
```java
	public boolean hasAttributes()
	{
		return attributes != null && attributes.size() > 0;
	}

```

### SizeReplaceableByIsEmpty
`wicketIdValue.trim().length() == 0` can be replaced with 'wicketIdValue.trim().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketTagIdentifier.java`
#### Snippet
```java
		if (wicketIdValue != null)
		{
			if (wicketIdValue.trim().length() == 0)
			{
				throw new WicketParseException(
```

### SizeReplaceableByIsEmpty
`stack.size() == 0` can be replaced with 'stack.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/EnclosureHandler.java`
#### Snippet
```java
				}

				if (stack.size() == 0)
				{
					stack = null;
```

### SizeReplaceableByIsEmpty
`src.trim().length() == 0` can be replaced with 'src.trim().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlProblemFinder.java`
#### Snippet
```java
		{
			String src = tag.getAttributes().getString("src");
			if ((src != null) && (src.trim().length() == 0))
			{
				escalateWarning("Attribute 'src' should not be empty. Location: ", tag);
```

### SizeReplaceableByIsEmpty
`enclosures.size() > 0` can be replaced with '!enclosures.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
		}
		// Are we within an enclosure?
		else if ((enclosures != null) && (enclosures.size() > 0))
		{
			// In case the enclosure tag did not provide a child component id, then assign the
```

### SizeReplaceableByIsEmpty
`xmlnsUrl.trim().length() == 0` can be replaced with 'xmlnsUrl.trim().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketNamespaceHandler.java`
#### Snippet
```java

				// If Wicket relevant ...
				if ((xmlnsUrl == null) || (xmlnsUrl.trim().length() == 0) ||
					xmlnsUrl.startsWith(WICKET_URI))
				{
```

### SizeReplaceableByIsEmpty
`stack.size() > 0` can be replaced with '!stack.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHandler.java`
#### Snippet
```java
	{
		// If there's still a non-simple tag left, it's an error
		while (stack.size() > 0)
		{
			final ComponentTag top = stack.peek();
```

### SizeReplaceableByIsEmpty
`stack.size() > 0` can be replaced with '!stack.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHandler.java`
#### Snippet
```java
		{
			// Check that there is something on the stack
			if (stack.size() > 0)
			{
				// Pop the top tag off the stack
```

### SizeReplaceableByIsEmpty
`tagText.length() == 0` can be replaced with 'tagText.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java
		// Get the tagtext between open and close brackets
		String tagText = lastText.subSequence(1, lastText.length() - 1).toString();
		if (tagText.length() == 0)
		{
			throw new ParseException("Found empty tag: '<>' at" + getLineAndColumnText(),
```

### SizeReplaceableByIsEmpty
`pathInfo.path.length() > 0` can be replaced with '!pathInfo.path.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
			final MarkupContainer container = pathInfo.getContainer();

			if (!pathInfo.absolute && (pathInfo.path != null) && (pathInfo.path.length() > 0))
			{
				// Href is relative. Create a resource reference pointing at this file
```

### SizeReplaceableByIsEmpty
`feedbackMessages.size() > 0` can be replaced with '!feedbackMessages.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/request/handler/render/WebPageRenderer.java`
#### Snippet
```java
		FeedbackCollector collector = new FeedbackCollector();
		List<FeedbackMessage> feedbackMessages = collector.collect();
		if (feedbackMessages.size() > 0)
		{
			Session.get().bind();
```

### SizeReplaceableByIsEmpty
`messages.size() > 0` can be replaced with '!messages.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessages.java`
#### Snippet
```java
	public final FeedbackMessage first()
	{
		return messages.size() > 0 ? messages.get(0) : null;
	}

```

### SizeReplaceableByIsEmpty
`url.length() > 0` can be replaced with '!url.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java

		// If url is non-empty it will start with '/', which we should lose
		if (url.length() > 0 && url.charAt(0) == '/')
		{
			// Remove leading '/'
```

### SizeReplaceableByIsEmpty
`url.length() > 0` can be replaced with '!url.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java
		// NOTE: we NEED to put the '/' in front as otherwise some versions of application servers
		// (e.g. Jetty 5.1.x) will fail for requests like '/mysubdir/myfile.css'
		if ((url.length() > 0 && url.charAt(0) != '/') || url.length() == 0)
		{
			url = '/' + url;
```

### SizeReplaceableByIsEmpty
`url.length() == 0` can be replaced with 'url.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java
		// NOTE: we NEED to put the '/' in front as otherwise some versions of application servers
		// (e.g. Jetty 5.1.x) will fail for requests like '/mysubdir/myfile.css'
		if ((url.length() > 0 && url.charAt(0) != '/') || url.length() == 0)
		{
			url = '/' + url;
```

### SizeReplaceableByIsEmpty
`cookies.size() > 0` can be replaced with '!cookies.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ClientProperties.java`
#### Snippet
```java
		{
			Collection<Cookie> cookies = ((WebRequest)RequestCycle.get().getRequest()).getCookies();
			navigatorCookieEnabled = cookies != null && cookies.size() > 0;
		}
		return navigatorCookieEnabled;
```

### SizeReplaceableByIsEmpty
`(newcomponents.get(i)).length() > 0` can be replaced with '!(newcomponents.get(i)).isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestUtils.java`
#### Snippet
```java
			{
				// Verify for a ".." component at next iteration
				if ((newcomponents.get(i)).length() > 0 && newcomponents.get(i + 1).equals(".."))
				{
					newcomponents.remove(i);
```

### SizeReplaceableByIsEmpty
`path.length() > 0` can be replaced with '!path.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
		}

		if (path.length() > 0)
		{
			path = path.substring(1);
```

### SizeReplaceableByIsEmpty
`ignorePaths.size() > 0` can be replaced with '!ignorePaths.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
	{
		boolean ignore = false;
		if (ignorePaths.size() > 0)
		{
			String relativePath = getRelativePath(request);
```

### SizeReplaceableByIsEmpty
`filterPrefix.length() > 0` can be replaced with '!filterPrefix.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
	private Url getContextRelativeUrl(String uri, String filterPrefix)
	{
		if (filterPrefix.length() > 0 && !filterPrefix.endsWith("/"))
		{
			filterPrefix += "/";
```

### SizeReplaceableByIsEmpty
`name.trim().length() == 0` can be replaced with 'name.trim().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
		}
		String[] elements = null;
		if (name.trim().length() == 0)
		{
			elements = new String[0];
```

### SizeReplaceableByIsEmpty
`url.getQueryParameters().size() > 0` can be replaced with '!url.getQueryParameters().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
		Args.notNull(url, "url");
	
		if (url.getQueryParameters().size() > 0)
		{
			Url.QueryParameter param = url.getQueryParameters().get(0);
```

### SizeReplaceableByIsEmpty
`proxiesHeaderValue.size() == 0` can be replaced with 'proxiesHeaderValue.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapperFactory.java`
#### Snippet
```java
			xRequest.setRemoteHost(remoteIp);

			if (proxiesHeaderValue.size() == 0)
			{
				xRequest.removeHeader(config.proxiesHeader);
```

### SizeReplaceableByIsEmpty
`newRemoteIpHeaderValue.size() == 0` can be replaced with 'newRemoteIpHeaderValue.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapperFactory.java`
#### Snippet
```java
				xRequest.setHeader(config.proxiesHeader, commaDelimitedListOfProxies);
			}
			if (newRemoteIpHeaderValue.size() == 0)
			{
				xRequest.removeHeader(config.remoteIPHeader);
```

### SizeReplaceableByIsEmpty
`packageName.length() > 0` can be replaced with '!packageName.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/PackageStringResourceLoader.java`
#### Snippet
```java
				// Create the base path
				String path = filename;
				if (packageName.length() > 0)
				{
					path = packageName + "/" + path;
```

### SizeReplaceableByIsEmpty
`packageName.length() > 0` can be replaced with '!packageName.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/PackageStringResourceLoader.java`
#### Snippet
```java
				packageName = Strings.beforeLast(packageName, '/');
			}
			while (packageName.length() > 0);

			clazz = clazz.getSuperclass();
```

### SizeReplaceableByIsEmpty
`buffer.length() > 0` can be replaced with '!buffer.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
			if (skip == false)
			{
				if (buffer.length() > 0)
				{
					buffer.insert(0, '.');
```

### SizeReplaceableByIsEmpty
`list.size() == 0` can be replaced with 'list.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		}

		if (list.size() == 0)
		{
			list.add(Locale.getDefault());
```

### SizeReplaceableByIsEmpty
`parameters.size() == 0` can be replaced with 'parameters.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	public String getQueryString()
	{
		if (parameters.size() == 0)
		{
			return null;
```

### SizeReplaceableByIsEmpty
`post.getParameterNames().size() == 0` can be replaced with 'post.getParameterNames().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (uploadedFiles == null && useMultiPartContentType == false)
		{
			if (post.getParameterNames().size() == 0)
			{
				return "".getBytes();
```

### SizeReplaceableByIsEmpty
`value.trim().length() == 0` can be replaced with 'value.trim().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
	public static boolean isBlankOrNull(String value)
	{
		return ((value == null) || (value.trim().length() == 0));
	}

```

### SizeReplaceableByIsEmpty
`patterns.size() == 0` can be replaced with 'patterns.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java

		// If no explicit includes, try here
		if (patterns == null || patterns.size() == 0)
		{
			tryHere = true;
```

### SizeReplaceableByIsEmpty
`book.getWritingStyles().size() > 0` can be replaced with '!book.getWritingStyles().isEmpty()'
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/BookDetails.java`
#### Snippet
```java
		String writingStyles;
		final boolean hasStyles = (book.getWritingStyles() != null) &&
			(book.getWritingStyles().size() > 0);

		if (hasStyles)
```

### SizeReplaceableByIsEmpty
`sb.length() > 0` can be replaced with '!sb.isEmpty()'
in `wicket-examples/src/main/java/org/apache/wicket/examples/niceurl/mounted/Page5.java`
#### Snippet
```java
		for (StringValue sv : values)
		{
			if (sb.length() > 0)
			{
				sb.append(", ");
```

### SizeReplaceableByIsEmpty
`version.length() == 0` can be replaced with 'version.isEmpty()'
in `wicket-examples/src/main/java/org/apache/wicket/examples/homepage/HomePageHeader.java`
#### Snippet
```java
			String version = p.getSpecificationVersion();

			if (version == null || version.length() == 0)
			{
				return "Missing Version";
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/AbstractFileUpload.java`
#### Snippet
```java
     *         {@code false} otherwise.
     */
    public static final boolean isMultipartContent(final RequestContext ctx) {
        final String contentType = ctx.getContentType();
        if (contentType == null) {
```

### FinalStaticMethod
'static' method declared `final`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/jaksrvlt/JakSrvltFileUpload.java`
#### Snippet
```java
     *         {@code false} otherwise.
     */
    public static final boolean isMultipartContent(final HttpServletRequest request) {
        return POST_METHOD.equalsIgnoreCase(request.getMethod()) && AbstractFileUpload.isMultipartContent(new JakSrvltRequestContext(request));
    }
```

### FinalStaticMethod
'static' method declared `final`
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
	 * @return the {@link IMarkupFragment} of the component tag if found, {@code null} is not found.
	 */
	public static final IMarkupFragment findTagMarkup(IMarkupFragment fragment, String id, String tagName, int streamOffset)
	{
		/*
```

### FinalStaticMethod
'static' method declared `final`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxSelector.java`
#### Snippet
```java
	 * @return all contained checkBoxes
	 */
	public static final Iterable<CheckBox> collectCheckBoxes(WebMarkupContainer container)
	{
		List<CheckBox> checkBoxes = new ArrayList<>();
```

### FinalStaticMethod
'static' method declared `final`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	 * @return the new resource reference
	 */
	public static final ResourceReference of(String name, SerializableSupplier<IResource> resourceSupplier)
	{
		return new LambdaResourceReference(name, resourceSupplier);
```

### FinalStaticMethod
'static' method declared `final`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	 * @return  the new resource reference
	 */
	public static final ResourceReference of(Key key, SerializableSupplier<IResource> resourceSupplier)
	{
		return new LambdaResourceReference(key, resourceSupplier);
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/ApplicationView.java`
#### Snippet
```java
		// Basic attributes
		add(new Label("name", application.getName()));
		add(new Label("componentUseCheck", "" +
			application.getDebugSettings().getComponentUseCheck()));
		add(new Label("compressWhitespace", "" +
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/ApplicationView.java`
#### Snippet
```java
		add(new Label("componentUseCheck", "" +
			application.getDebugSettings().getComponentUseCheck()));
		add(new Label("compressWhitespace", "" +
			application.getMarkupSettings().getCompressWhitespace()));
		add(new Label("stripComments", "" + application.getMarkupSettings().getStripComments()));
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/ApplicationView.java`
#### Snippet
```java
		add(new Label("compressWhitespace", "" +
			application.getMarkupSettings().getCompressWhitespace()));
		add(new Label("stripComments", "" + application.getMarkupSettings().getStripComments()));
		add(new Label("stripWicketTags", "" + application.getMarkupSettings().getStripWicketTags()));
		add(new Label("bufferResponse", "" +
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/ApplicationView.java`
#### Snippet
```java
			application.getMarkupSettings().getCompressWhitespace()));
		add(new Label("stripComments", "" + application.getMarkupSettings().getStripComments()));
		add(new Label("stripWicketTags", "" + application.getMarkupSettings().getStripWicketTags()));
		add(new Label("bufferResponse", "" +
			application.getRequestCycleSettings().getBufferResponse()));
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/ApplicationView.java`
#### Snippet
```java
		add(new Label("stripComments", "" + application.getMarkupSettings().getStripComments()));
		add(new Label("stripWicketTags", "" + application.getMarkupSettings().getStripWicketTags()));
		add(new Label("bufferResponse", "" +
			application.getRequestCycleSettings().getBufferResponse()));
		add(new Label("resourcePollFrequency", "" +
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/ApplicationView.java`
#### Snippet
```java
		add(new Label("bufferResponse", "" +
			application.getRequestCycleSettings().getBufferResponse()));
		add(new Label("resourcePollFrequency", "" +
			application.getResourceSettings().getResourcePollFrequency()));
		add(new Label("versionPages", "" + application.getPageSettings().getVersionPagesByDefault()));
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/ApplicationView.java`
#### Snippet
```java
		add(new Label("resourcePollFrequency", "" +
			application.getResourceSettings().getResourcePollFrequency()));
		add(new Label("versionPages", "" + application.getPageSettings().getVersionPagesByDefault()));
	}
}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPanel.java`
#### Snippet
```java
					public void onComponentTagBody(MarkupStream markupStream, ComponentTag openTag)
					{
						replaceComponentTagBody(markupStream, openTag, "" + getModelObject().getPageId());
					}
					
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
			CharAttributes cf = new CharAttributes(ch, fontName, rotation, rise, shearX, shearY);
			charAttsList.add(cf);
			text = new TextLayout(ch + "", getFont(fontName), new FontRenderContext(null, false,
				false));
			textAt = new AffineTransform();
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
		for (CharAttributes cf : charAttsList)
		{
			text = new TextLayout(cf.getChar() + "", getFont(cf.getName()),
				gfx.getFontRenderContext());
			textAt = new AffineTransform();
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/CSVDataExporter.java`
#### Snippet
```java
	protected String quoteValue(String value)
	{
		return quoteCharacter + value.replace("" + quoteCharacter, "" + quoteCharacter + quoteCharacter) + quoteCharacter;
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/CSVDataExporter.java`
#### Snippet
```java
	protected String quoteValue(String value)
	{
		return quoteCharacter + value.replace("" + quoteCharacter, "" + quoteCharacter + quoteCharacter) + quoteCharacter;
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
			char c = characters[Math.abs(hash % characters.length)];

			String segment = "" + a + b + c;
			hash = hashString(segment);

```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-core/src/main/java/org/apache/wicket/markup/HtmlSpecialTag.java`
#### Snippet
```java
	public final String toString()
	{
		return "" + httpTagType + ": '" + xmlTag.toString() + "'";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
		{
			throw new IllegalStateException(
				"Configuration type is write-once. You can not change it. " + "" +
					"Current value='" + configurationType + "'");
		}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
	public void addIntHeader(final String name, final int i)
	{
		addHeader(name, "" + i);
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
	public void setIntHeader(final String name, final int i)
	{
		setHeader(name, "" + i);
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/EditableLabelPage.java`
#### Snippet
```java
		

		form.add(new Label("refresh-counter", () -> "" + refreshCounter));

		form.add(new Link<Void>("refresh-link")
```

### TrivialStringConcatenation
Empty string used in concatenation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/TableTreePage.java`
#### Snippet
```java
				NodeModel<Foo> nodeModel = (NodeModel<Foo>)rowModel;

				cellItem.add(new Label(componentId, "" + nodeModel.getDepth()));
			}

```

## RuleId[id=SuspiciousInvocationHandlerImplementation]
### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
			}

			return null;
		}
	}
```

### SuspiciousInvocationHandlerImplementation
Method is never used in 'invoke': it's unlikely that 'hashCode', 'equals' and 'toString' are implemented correctly
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
				try
				{
					final InvocationHandler invocationHandler = (proxy, method, args) -> null;
					final Object proxyInstance = Proxy.newProxyInstance(
							hasNonPublicInterface ? nonPublicLoader : latestLoader, classObjs, invocationHandler);
```

## RuleId[id=ExplicitArrayFilling]
### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
		modCount.incrementAndGet();
		Entry<V> tab[] = table;
		for (int i = 0; i < tab.length; i++)
		{
			tab[i] = null;
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `String.getBytes()` is ignored
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java
	{
		// test encoding is available
		"".getBytes(responseRequestEncoding);

		application.getRequestCycleSettings().setResponseRequestEncoding(responseRequestEncoding);
```

### IgnoreResultOfCall
Result of `String.getBytes()` is ignored
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java
	{
		// test encoding is available
		"".getBytes(encoding);

		application.getMarkupSettings().setDefaultMarkupEncoding(encoding);
```

### IgnoreResultOfCall
Result of `ItemInputStream.skip()` is ignored
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/MultipartStream.java`
#### Snippet
```java
                        }
                    }
                    skip(av);
                }
            }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/FolderUploadsFileManager.java`
#### Snippet
```java
    {
        File uploadFieldFolder = new File(getFolder(), uploadFieldId);
        uploadFieldFolder.mkdirs();
        try
        {
```

### IgnoreResultOfCall
Result of `InputStream.skip()` is ignored
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PartWriterCallback.java`
#### Snippet
```java
				if (startbyte != null)
				{
					inputStream.skip(startbyte);
				}
				else
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
		try
		{
			file.mkdirs();
		}
		catch (SecurityException sx)
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadApplication.java`
#### Snippet
```java
		uploadFolder = new Folder(System.getProperty("java.io.tmpdir"), "wicket-uploads");
		// Ensure folder exists
		uploadFolder.mkdirs();

		uploadsFileManager = new FolderUploadsFileManager(uploadFolder);
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/MultiUploadPage.java`
#### Snippet
```java
				{
					// Save to new file
					newFile.createNewFile();
					upload.writeTo(newFile);

```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadPage.java`
#### Snippet
```java
					{
						// Save to new file
						newFile.createNewFile();
						upload.writeTo(newFile);

```

## RuleId[id=UnnecessaryUnboxing]
### UnnecessaryUnboxing
Unnecessary unboxing
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Objects.java`
#### Snippet
```java
		if (c == Character.class)
		{
			return BigDecimal.valueOf(((Character)value).charValue());
		}
		return new BigDecimal(stringValue(value, true));
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
				return -1;
			}
			return value.intValue();
		}
	}
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
				return -1;
			}
			return value.intValue();
		}
	}
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
			stateless = statefulComponent == null;

			if (log.isDebugEnabled() && !stateless.booleanValue() && getStatelessHint())
			{
				log.debug("Page '{}' is not stateless because of component with path '{}'.", this,
```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `getContentType()` is identical to its super method
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
	 */
	@Override
	public String getContentType()
	{
		return null;
```

### RedundantMethodOverride
Method `lastModifiedTime()` is identical to its super method
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
	 */
	@Override
	public Instant lastModifiedTime()
	{
		return null;
```

### RedundantMethodOverride
Method `lastModifiedTime()` is identical to its super method
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java
	 */
	@Override
	public Instant lastModifiedTime()
	{
		return null;
```

### RedundantMethodOverride
Method `getContentType()` is identical to its super method
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java
	 */
	@Override
	public String getContentType()
	{
		return null;
```

### RedundantMethodOverride
Method `onModelChanging()` only delegates to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
	 */
	@Override
	protected void onModelChanging()
	{
		super.onModelChanging();
```

### RedundantMethodOverride
Method `defaultNullLabel()` is identical to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
	 */
	@Override
	protected String defaultNullLabel()
	{
		return "...";
```

### RedundantMethodOverride
Method `onModelChanged()` only delegates to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
	 */
	@Override
	protected void onModelChanged()
	{
		super.onModelChanged();
```

### RedundantMethodOverride
Method `getLabelAjaxEvent()` is identical to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
	 */
	@Override
	protected String getLabelAjaxEvent()
	{
		return "click";
```

### RedundantMethodOverride
Method `onSortChanged()` is identical to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/sort/AjaxFallbackOrderByBorder.java`
#### Snippet
```java
	 */
	@Override
	protected void onSortChanged()
	{
		// noop
```

### RedundantMethodOverride
Method `onModelChanging()` only delegates to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
	 */
	@Override
	protected void onModelChanging()
	{
		super.onModelChanging();
```

### RedundantMethodOverride
Method `onModelChanged()` only delegates to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
	 */
	@Override
	protected void onModelChanged()
	{
		super.onModelChanged();
```

### RedundantMethodOverride
Method `onCancel()` is identical to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/Wizard.java`
#### Snippet
```java
	 */
	@Override
	public void onCancel()
	{
	}
```

### RedundantMethodOverride
Method `onFinish()` is identical to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/Wizard.java`
#### Snippet
```java
	 */
	@Override
	public void onFinish()
	{
	}
```

### RedundantMethodOverride
Method `breadCrumbRemoved()` is identical to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java

		@Override
		public void breadCrumbRemoved(final IBreadCrumbParticipant breadCrumbParticipant)
		{
		}
```

### RedundantMethodOverride
Method `breadCrumbAdded()` is identical to its super method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java

		@Override
		public void breadCrumbAdded(final IBreadCrumbParticipant breadCrumbParticipant)
		{
		}
```

### RedundantMethodOverride
Method `close()` only delegates to its super method
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/LimitedInputStream.java`
#### Snippet
```java
     */
    @Override
    public void close() throws IOException {
        super.close();
    }
```

### RedundantMethodOverride
Method `detachModels()` only delegates to its super method
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	 */
	@Override
	public void detachModels()
	{
		super.detachModels();
```

### RedundantMethodOverride
Method `length()` is identical to its super method
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/WebExternalResourceStream.java`
#### Snippet
```java

	@Override
	public Bytes length()
	{
		return null;
```

### RedundantMethodOverride
Method `remove()` is identical to its super method
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/EmptyResourceNameIterator.java`
#### Snippet
```java

	@Override
	public void remove()
	{
		// noop
```

### RedundantMethodOverride
Method `assertResultPage()` is identical to its super method
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTester.java`
#### Snippet
```java
	 */
	@Override
	public void assertResultPage(final Class<?> clazz, final String filename) throws Exception
	{
		String document = getLastResponseAsString();
```

### RedundantMethodOverride
Method `onComponentTagBody()` is identical to its super method
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java
	 */
	@Override
	public void onComponentTagBody(final MarkupStream markupStream, final ComponentTag openTag)
	{
	}
```

### RedundantMethodOverride
Method `onComponentTag()` only delegates to its super method
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AssociatedMarkupSourcingStrategy.java`
#### Snippet
```java

	@Override
	public void onComponentTag(final Component component, final ComponentTag tag)
	{
		super.onComponentTag(component, tag);
```

### RedundantMethodOverride
Method `addDequeuedComponent()` is identical to its super method
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java

	@Override
	protected void addDequeuedComponent(Component component, ComponentTag tag)
	{
		// components queued in border get dequeued into the border not into the body container
```

### RedundantMethodOverride
Method `onError()` is identical to its super method
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AutoCompletePage.java`
#### Snippet
```java

			@Override
			protected void onError(AjaxRequestTarget target)
			{
			}
```

### RedundantMethodOverride
Method `onError()` is identical to its super method
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/GuestBook.java`
#### Snippet
```java

			@Override
			protected void onError(AjaxRequestTarget target)
			{
			}
```

### RedundantMethodOverride
Method `onError()` is identical to its super method
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java

					@Override
					protected void onError(AjaxRequestTarget target)
					{
					}
```

### RedundantMethodOverride
Method `onError()` is identical to its super method
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java

					@Override
					protected void onError(AjaxRequestTarget target)
					{
					}
```

### RedundantMethodOverride
Method `newContentComponent()` only delegates to its super method
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/BeginnersTreePage.java`
#### Snippet
```java
			 */
			@Override
			protected Component newContentComponent(String id, IModel<Foo> node)
			{
				return super.newContentComponent(id, node);
```

### RedundantMethodOverride
Method `onSubmit()` is identical to its super method
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/AdvancedTreePage.java`
#### Snippet
```java
		{
			@Override
			public void onSubmit()
			{
			}
```

### RedundantMethodOverride
Method `onSubmit()` is identical to its super method
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/AjaxStatelessExample.java`
#### Snippet
```java
		{
			@Override
			protected void onSubmit()
			{
			}
```

## RuleId[id=ClassNameSameAsAncestorName]
### ClassNameSameAsAncestorName
Class name `File` is the same as one of its superclass' names
in `wicket-util/src/main/java/org/apache/wicket/util/file/File.java`
#### Snippet
```java
 * @author Jonathan Locke
 */
public class File extends java.io.File implements IModifiable
{
	private static final long serialVersionUID = 1L;
```

### ClassNameSameAsAncestorName
Class name `HttpSession` is the same as one of its superclass' names
in `wicket-util/src/main/java/javax/servlet/http/HttpSession.java`
#### Snippet
```java
 * A temporary class used until all dependencies provide releases based on jakarta.** APIs
 */
public interface HttpSession extends jakarta.servlet.http.HttpSession {
    
    class Impl implements javax.servlet.http.HttpSession {
```

### ClassNameSameAsAncestorName
Class name `CloseCode` is the same as one of its superclass' names
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxWebSocketConnection.java`
#### Snippet
```java
	}

	private static class CloseCode implements CloseReason.CloseCode
	{
		private final int code;
```

### ClassNameSameAsAncestorName
Class name `HttpServletRequest` is the same as one of its superclass' names
in `wicket-util/src/main/java/javax/servlet/http/HttpServletRequest.java`
#### Snippet
```java
 * A temporary class used until all dependencies provide releases based on jakarta.** APIs
 */
public interface HttpServletRequest extends jakarta.servlet.http.HttpServletRequest {

    class Impl implements HttpServletRequest {
```

### ClassNameSameAsAncestorName
Class name `ServletContext` is the same as one of its superclass' names
in `wicket-util/src/main/java/javax/servlet/ServletContext.java`
#### Snippet
```java
 * A temporary class used until all dependencies provide releases based on jakarta.** APIs
 */
public interface ServletContext extends jakarta.servlet.ServletContext {

    class Impl implements ServletContext {
```

### ClassNameSameAsAncestorName
Class name `ThreadFactory` is the same as one of its superclass' names
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
	}

	public static class ThreadFactory implements java.util.concurrent.ThreadFactory
	{
		private final AtomicInteger counter = new AtomicInteger();
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
seconds \* 1000: integer multiplication implicitly cast to long
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LazyLoadingPage.java`
#### Snippet
```java
					try
					{
						Thread.sleep(seconds * 1000);
					}
					catch (InterruptedException e)
```

### IntegerMultiplicationImplicitCastToLong
seconds \* 1000: integer multiplication implicitly cast to long
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LazyLoadingPage.java`
#### Snippet
```java
				protected Duration getUpdateInterval()
				{
					return Duration.ofMillis(seconds * 1000 / 10);
				}

```

## RuleId[id=RegExpDuplicateCharacterInClass]
### RegExpDuplicateCharacterInClass
Duplicate character 's' inside character class
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlLicenseHeaderHandler.java`
#### Snippet
```java
{
	private final Pattern xmlHeader = Pattern.compile(
		"^(\\<\\?xml[^" + LINE_ENDING + "]+?)" + LINE_ENDING + "(.*)$",
		Pattern.DOTALL | Pattern.MULTILINE);

```

### RegExpDuplicateCharacterInClass
Duplicate character 'i' inside character class
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlLicenseHeaderHandler.java`
#### Snippet
```java
{
	private final Pattern xmlHeader = Pattern.compile(
		"^(\\<\\?xml[^" + LINE_ENDING + "]+?)" + LINE_ENDING + "(.*)$",
		Pattern.DOTALL | Pattern.MULTILINE);

```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java

	private static final String EMAIL_PATTERN = "(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java

	private static final String EMAIL_PATTERN = "(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
		+ "](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+"
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
		+ " \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)"
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t]"
		+ ")*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?["
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ " \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*"
		+ ")(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
		+ "?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?"
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\"
		+ "]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\"
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\"
		+ "r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]"
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
		+ "^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]"
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|"
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>"
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*(?:,@(?:(?:\\r\\n)?["
		+ " \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,"
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ";:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\"' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
		+ "?:\\r\\n)?[ \\t])*))*)?;\\s*)";

```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\[' inside character class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
		+ ".(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:("
		+ "?:\\r\\n)?[ \\t])*))*)?;\\s*)";

```

## RuleId[id=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import org.apache.wicket.markup.head.HeaderItem;`
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
import org.apache.wicket.javascript.DefaultJavaScriptCompressor;
import org.apache.wicket.markup.MarkupFactory;
import org.apache.wicket.markup.head.HeaderItem;
import org.apache.wicket.markup.head.IHeaderResponse;
import org.apache.wicket.markup.head.ResourceAggregator;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.markup.head.ResourceAggregator;`
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
import org.apache.wicket.markup.head.HeaderItem;
import org.apache.wicket.markup.head.IHeaderResponse;
import org.apache.wicket.markup.head.ResourceAggregator;
import org.apache.wicket.markup.html.HeaderResponseDecoratorCollection;
import org.apache.wicket.markup.html.IHeaderContributor;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.Page;`
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigator.java`
#### Snippet
```java
import org.apache.wicket.Component;
import org.apache.wicket.MarkupContainer;
import org.apache.wicket.Page;
import org.apache.wicket.ajax.AjaxRequestTarget;
import org.apache.wicket.markup.html.link.AbstractLink;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.Page;`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/ComponentRenderingRequestHandler.java`
#### Snippet
```java

import org.apache.wicket.Component;
import org.apache.wicket.Page;
import org.apache.wicket.request.IRequestCycle;
import org.apache.wicket.request.component.IRequestableComponent;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.util.crypt.ICryptFactory;`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
import org.apache.wicket.request.mapper.info.PageComponentInfo;
import org.apache.wicket.util.crypt.ICrypt;
import org.apache.wicket.util.crypt.ICryptFactory;
import org.apache.wicket.util.lang.Args;
import org.apache.wicket.util.string.Strings;
```

### UNUSED_IMPORT
Unused import `import java.security.GeneralSecurityException;`
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/AbstractJceCrypt.java`
#### Snippet
```java

import java.nio.charset.StandardCharsets;
import java.security.GeneralSecurityException;
import java.util.Base64;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.util.string.Strings;`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/CssUtils.java`
#### Snippet
```java
import org.apache.wicket.request.Response;
import org.apache.wicket.util.lang.Classes;
import org.apache.wicket.util.string.Strings;
import org.apache.wicket.util.value.AttributeMap;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.WicketRuntimeException;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/MarkupUtil.java`
#### Snippet
```java
import org.apache.wicket.MarkupContainer;
import org.apache.wicket.Page;
import org.apache.wicket.WicketRuntimeException;
import org.apache.wicket.markup.ComponentTag;
import org.apache.wicket.markup.IMarkupFragment;
```

### UNUSED_IMPORT
Unused import `import java.util.List;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ChoiceRenderer.java`
#### Snippet
```java
package org.apache.wicket.markup.html.form;

import java.util.List;

import org.apache.wicket.core.util.lang.PropertyResolver;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.model.IModel;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ChoiceRenderer.java`
#### Snippet
```java

import org.apache.wicket.core.util.lang.PropertyResolver;
import org.apache.wicket.model.IModel;
import org.apache.wicket.util.io.IClusterable;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.ajax.AjaxRequestTarget;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentUpdatingBehavior.java`
#### Snippet
```java
import org.apache.wicket.IRequestListener;
import org.apache.wicket.WicketRuntimeException;
import org.apache.wicket.ajax.AjaxRequestTarget;
import org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior;
import org.apache.wicket.behavior.Behavior;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.markup.html.form.Form;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/FormComponentFeedbackBorder.java`
#### Snippet
```java
import org.apache.wicket.markup.html.WebMarkupContainer;
import org.apache.wicket.markup.html.border.Border;
import org.apache.wicket.markup.html.form.Form;

/**
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.util.tester.BaseWicketTester;`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java
import org.apache.wicket.markup.parser.XmlTag.TagType;
import org.apache.wicket.markup.resolver.HtmlHeaderResolver;
import org.apache.wicket.util.tester.BaseWicketTester;


```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.Component;`
in `wicket-core/src/main/java/org/apache/wicket/page/XmlPartialPageUpdate.java`
#### Snippet
```java
package org.apache.wicket.page;

import org.apache.wicket.Component;
import org.apache.wicket.Page;
import org.apache.wicket.request.Response;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.pageStore.crypt.DefaultCrypter;`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/CryptingPageStore.java`
#### Snippet
```java
import org.apache.wicket.WicketRuntimeException;
import org.apache.wicket.page.IManageablePage;
import org.apache.wicket.pageStore.crypt.DefaultCrypter;
import org.apache.wicket.pageStore.crypt.ICrypter;
import org.apache.wicket.util.lang.Args;
```

### UNUSED_IMPORT
Unused import `import java.io.Serializable;`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/crypt/ICrypter.java`
#### Snippet
```java
package org.apache.wicket.pageStore.crypt;

import java.io.Serializable;
import java.security.SecureRandom;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.Session;`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
import jakarta.servlet.http.HttpServletResponse;

import org.apache.wicket.Session;
import org.apache.wicket.ThreadContext;
import org.apache.wicket.WicketRuntimeException;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.request.IRequestCycle;`
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/WebExternalResourceRequestHandler.java`
#### Snippet
```java

import org.apache.wicket.core.util.resource.WebExternalResourceStream;
import org.apache.wicket.request.IRequestCycle;
import org.apache.wicket.request.resource.ContentDisposition;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.util.string.Strings;`
in `wicket-core/src/main/java/org/apache/wicket/response/filter/XmlCleaningResponseFilter.java`
#### Snippet
```java
import org.apache.wicket.page.XmlPartialPageUpdate;
import org.apache.wicket.util.string.AppendingStringBuffer;
import org.apache.wicket.util.string.Strings;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.core.util.lang.WicketObjects;`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/SessionSizeModel.java`
#### Snippet
```java

import org.apache.wicket.Session;
import org.apache.wicket.core.util.lang.WicketObjects;
import org.apache.wicket.model.IModel;
import org.apache.wicket.util.lang.Bytes;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.Page;`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessCheckFailureException.java`
#### Snippet
```java

import org.apache.wicket.Component;
import org.apache.wicket.Page;
import org.apache.wicket.WicketRuntimeException;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.request.resource.AbstractResource;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
import org.apache.wicket.request.http.flow.AbortWithHttpErrorCodeException;
import org.apache.wicket.request.mapper.parameter.PageParameters;
import org.apache.wicket.request.resource.AbstractResource;
import org.apache.wicket.request.resource.ContentDisposition;
import org.apache.wicket.request.resource.DynamicImageResource;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.request.resource.DynamicImageResource;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
import org.apache.wicket.request.resource.AbstractResource;
import org.apache.wicket.request.resource.ContentDisposition;
import org.apache.wicket.request.resource.DynamicImageResource;
import org.apache.wicket.request.resource.IResource;
import org.apache.wicket.request.resource.ResourceReference;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.examples.homepage.HomePage;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/BasePage.java`
#### Snippet
```java

import org.apache.wicket.examples.WicketExamplePage;
import org.apache.wicket.examples.homepage.HomePage;
import org.apache.wicket.markup.html.link.BookmarkablePageLink;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.protocol.http.WebApplication;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/breadcrumb/BreadCrumbApplication.java`
#### Snippet
```java
import org.apache.wicket.Page;
import org.apache.wicket.examples.WicketExampleApplication;
import org.apache.wicket.protocol.http.WebApplication;

/**
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.model.IModel;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/LabelPage.java`
#### Snippet
```java
import org.apache.wicket.examples.WicketExamplePage;
import org.apache.wicket.markup.html.basic.Label;
import org.apache.wicket.model.IModel;
import org.apache.wicket.model.Model;
import org.apache.wicket.model.StringResourceModel;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.model.Model;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/LabelPage.java`
#### Snippet
```java
import org.apache.wicket.markup.html.basic.Label;
import org.apache.wicket.model.IModel;
import org.apache.wicket.model.Model;
import org.apache.wicket.model.StringResourceModel;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.csp.CSPDirective;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/customresourceloading/CustomResourceLoadingApplication.java`
#### Snippet
```java
import org.apache.wicket.core.util.resource.locator.IResourceStreamLocator;
import org.apache.wicket.core.util.resource.locator.ResourceStreamLocator;
import org.apache.wicket.csp.CSPDirective;
import org.apache.wicket.examples.WicketExampleApplication;
import org.apache.wicket.util.resource.IResourceStream;
```

### UNUSED_IMPORT
Unused import `import java.util.Comparator;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactsDatabase.java`
#### Snippet
```java
import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;
import java.util.HashMap;
import java.util.List;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.model.IModel;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/RepeatingPage.java`
#### Snippet
```java
import org.apache.wicket.markup.html.list.AbstractItem;
import org.apache.wicket.markup.repeater.RepeatingView;
import org.apache.wicket.model.IModel;


```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.model.IModel;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SimplePage.java`
#### Snippet
```java
import org.apache.wicket.markup.repeater.Item;
import org.apache.wicket.markup.repeater.data.DataView;
import org.apache.wicket.model.IModel;

/**
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.model.IModel;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SortingPage.java`
#### Snippet
```java
import org.apache.wicket.markup.repeater.Item;
import org.apache.wicket.markup.repeater.data.DataView;
import org.apache.wicket.model.IModel;

/**
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.examples.WicketExamplePage;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/source/SourcesPage.java`
#### Snippet
```java
import org.apache.wicket.authorization.UnauthorizedInstantiationException;
import org.apache.wicket.core.util.lang.WicketObjects;
import org.apache.wicket.examples.WicketExamplePage;
import org.apache.wicket.markup.head.CssHeaderItem;
import org.apache.wicket.markup.head.IHeaderResponse;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.markup.head.ResourceAggregator;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/sri/SriApplication.java`
#### Snippet
```java
import org.apache.wicket.Page;
import org.apache.wicket.examples.WicketExampleApplication;
import org.apache.wicket.markup.head.ResourceAggregator;

public class SriApplication extends WicketExampleApplication
```

### UNUSED_IMPORT
Unused import `import java.util.Optional;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/EditableFolderContent.java`
#### Snippet
```java
package org.apache.wicket.examples.tree.content;

import java.util.Optional;

import org.apache.wicket.Component;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.util.lang.Bytes;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadApplication.java`
#### Snippet
```java
import org.apache.wicket.markup.html.form.upload.resource.IUploadsFileManager;
import org.apache.wicket.util.file.Folder;
import org.apache.wicket.util.lang.Bytes;


```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.Application;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadPage.java`
#### Snippet
```java
import java.util.List;

import org.apache.wicket.Application;
import org.apache.wicket.examples.WicketExamplePage;
import org.apache.wicket.extensions.ajax.markup.html.form.upload.UploadProgressBar;
```

### UNUSED_IMPORT
Unused import `import java.util.concurrent.ScheduledExecutorService;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressBarTogglePanel.java`
#### Snippet
```java
package org.apache.wicket.examples.websocket.progress;

import java.util.concurrent.ScheduledExecutorService;

import org.apache.wicket.ajax.AjaxRequestTarget;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.examples.websocket.JSR356Application;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressBarTogglePanel.java`
#### Snippet
```java
import org.apache.wicket.ajax.markup.html.AjaxLink;
import org.apache.wicket.event.IEvent;
import org.apache.wicket.examples.websocket.JSR356Application;
import org.apache.wicket.examples.websocket.JSR356Session;
import org.apache.wicket.markup.html.basic.Label;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.protocol.ws.api.message.ConnectedMessage;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressBarTogglePanel.java`
#### Snippet
```java
import org.apache.wicket.protocol.ws.api.WebSocketBehavior;
import org.apache.wicket.protocol.ws.api.event.WebSocketPushPayload;
import org.apache.wicket.protocol.ws.api.message.ConnectedMessage;

public class ProgressBarTogglePanel extends Panel
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.markup.html.WebComponent;`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
import org.apache.wicket.markup.ComponentTag;
import org.apache.wicket.markup.MarkupStream;
import org.apache.wicket.markup.html.WebComponent;
import org.apache.wicket.markup.html.basic.Label;
import org.apache.wicket.markup.html.form.DropDownChoice;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.markup.html.WebComponent;`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NavigationToolbar.java`
#### Snippet
```java
import org.apache.wicket.AttributeModifier;
import org.apache.wicket.Component;
import org.apache.wicket.markup.html.WebComponent;
import org.apache.wicket.markup.html.WebMarkupContainer;
import org.apache.wicket.markup.html.navigation.paging.IPageableItems;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.markup.html.navigation.paging.IPageableItems;`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NavigationToolbar.java`
#### Snippet
```java
import org.apache.wicket.markup.html.WebComponent;
import org.apache.wicket.markup.html.WebMarkupContainer;
import org.apache.wicket.markup.html.navigation.paging.IPageableItems;
import org.apache.wicket.markup.html.navigation.paging.PagingNavigator;
import org.apache.wicket.model.IModel;
```

### UNUSED_IMPORT
Unused import `import java.util.Optional;`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/TableTree.java`
#### Snippet
```java

import java.util.List;
import java.util.Optional;
import java.util.Set;

```

### UNUSED_IMPORT
Unused import `import java.util.concurrent.Future;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/AbstractWebSocketConnection.java`
#### Snippet
```java
import org.apache.wicket.util.lang.Args;

import java.util.concurrent.Future;

/**
```

### UNUSED_IMPORT
Unused import `import java.util.Collections;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/HttpSessionCopy.java`
#### Snippet
```java
package org.apache.wicket.protocol.ws.api;

import java.util.Collections;
import java.util.Enumeration;
import java.util.concurrent.ConcurrentHashMap;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.Page;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketRequestHandler.java`
#### Snippet
```java

import org.apache.wicket.Component;
import org.apache.wicket.Page;
import org.apache.wicket.core.request.handler.AbstractPartialPageRequestHandler;
import org.apache.wicket.core.request.handler.logger.PageLogData;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.markup.head.IHeaderResponse;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketRequestHandler.java`
#### Snippet
```java
import org.apache.wicket.core.request.handler.AbstractPartialPageRequestHandler;
import org.apache.wicket.core.request.handler.logger.PageLogData;
import org.apache.wicket.markup.head.IHeaderResponse;
import org.apache.wicket.page.PartialPageUpdate;
import org.apache.wicket.page.XmlPartialPageUpdate;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.request.component.IRequestablePage;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketRequestHandler.java`
#### Snippet
```java
import org.apache.wicket.request.ILogData;
import org.apache.wicket.request.IRequestCycle;
import org.apache.wicket.request.component.IRequestablePage;
import org.apache.wicket.request.mapper.parameter.PageParameters;
import org.apache.wicket.util.lang.Args;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.request.mapper.parameter.PageParameters;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketRequestHandler.java`
#### Snippet
```java
import org.apache.wicket.request.IRequestCycle;
import org.apache.wicket.request.component.IRequestablePage;
import org.apache.wicket.request.mapper.parameter.PageParameters;
import org.apache.wicket.util.lang.Args;
import org.slf4j.Logger;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.Session;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/registry/PageIdKey.java`
#### Snippet
```java
package org.apache.wicket.protocol.ws.api.registry;

import org.apache.wicket.Session;
import org.apache.wicket.WicketRuntimeException;
import org.apache.wicket.page.IManageablePage;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.WicketRuntimeException;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/registry/PageIdKey.java`
#### Snippet
```java

import org.apache.wicket.Session;
import org.apache.wicket.WicketRuntimeException;
import org.apache.wicket.page.IManageablePage;
import org.apache.wicket.util.lang.Args;
```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.page.IManageablePage;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/registry/PageIdKey.java`
#### Snippet
```java
import org.apache.wicket.Session;
import org.apache.wicket.WicketRuntimeException;
import org.apache.wicket.page.IManageablePage;
import org.apache.wicket.util.lang.Args;

```

### UNUSED_IMPORT
Unused import `import org.apache.wicket.protocol.ws.api.message.IWebSocketPushMessage;`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketConnection.java`
#### Snippet
```java
import org.apache.wicket.protocol.http.WebApplication;
import org.apache.wicket.protocol.ws.api.IWebSocketConnection;
import org.apache.wicket.protocol.ws.api.message.IWebSocketPushMessage;
import org.apache.wicket.protocol.ws.api.registry.IKey;

```

### UNUSED_IMPORT
Unused import `import java.io.IOException;`
in `wicket-request/src/main/java/org/apache/wicket/request/http/WebResponse.java`
#### Snippet
```java
package org.apache.wicket.request.http;

import java.io.IOException;
import java.nio.charset.StandardCharsets;
import java.time.Duration;
```

### UNUSED_IMPORT
Unused import `import jakarta.servlet.ServletContext;`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/SpringComponentInjector.java`
#### Snippet
```java
package org.apache.wicket.spring.injection.annot;

import jakarta.servlet.ServletContext;

import org.apache.wicket.Application;
```

### UNUSED_IMPORT
Unused import `import java.sql.Time;`
in `wicket-util/src/main/java/org/apache/wicket/util/value/CopyOnWriteValueMap.java`
#### Snippet
```java

import java.io.Serializable;
import java.sql.Time;
import java.time.Duration;
import java.time.Instant;
```

### UNUSED_IMPORT
Unused import `import java.sql.Time;`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.sql.Time;
import java.time.Duration;
import java.time.Instant;
```

## RuleId[id=ListRemoveInLoop]
### ListRemoveInLoop
Can be replaced with 'List.subList().clear()'
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Chunk.java`
#### Snippet
```java
	public void applyDelete(final List<Object> target)
	{
		for (int i = last(); i >= first(); i--)
		{
			target.remove(i);
```

### ListRemoveInLoop
Can be replaced with 'List.subList().clear()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
	{
		Args.withinRange(0, segments.size(), count, "count");
		for (int i = 0; i < count; i++)
		{
			segments.remove(0);
```

## RuleId[id=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/io/Streams.java`
#### Snippet
```java
		int value;

		while ((value = in.read()) != -1)
		{
			buffer.append((char)value);
```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/io/XmlReader.java`
#### Snippet
```java
		// The current char from the markup file
		int value;
		while ((value = in.read()) != -1)
		{
			pushBack.append((char)value);
```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/diff/ToString.java`
#### Snippet
```java
		try
		{
			while ((s = reader.readLine()) != null)
			{
				l.add(s);
```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		int count = 0;
		int n = 0;
		while (-1 != (n = input.read(buffer)))
		{
			output.write(buffer, 0, n);
```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		int count = 0;
		int n = 0;
		while (-1 != (n = input.read(buffer)))
		{
			output.write(buffer, 0, n);
```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/string/interpolator/VariableInterpolator.java`
#### Snippet
```java
		int pos = 0;

		while ((start = lowerPositive(string.indexOf("$$", pos), string.indexOf("${", pos))) != -1)
		{
			// Append text before possible variable
```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java
					{
						int count;
						while ((count = origin.read(data, 0, BUFFER)) != -1)
						{
							out.write(data, 0, count);
```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
			index = i;
			next = n;
			return current = e;
		}

```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
			if (size != 0)
			{ // advance to first entry
				while ((i > 0) && ((n = t[--i]) == null))
				{
					/* NoOp */
```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		Set<Entry<V>> es = entrySet;
		return (es != null ? es : (entrySet = new EntrySet()));
	}

```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		Collection<V> vs = values;
		return (vs != null ? vs : (values = new Values()));
	}

```

### NestedAssignment
Result of assignment expression used
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		Set<Integer> ks = keySet;
		return (ks != null ? ks : (keySet = new KeySet()));
	}

```

### NestedAssignment
Result of assignment expression used
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
		{
			beanTypeCache = new WeakReference<>(
				clazz = WicketObjects.resolveClass(beanTypeName));
			if (clazz == null)
			{
```

### NestedAssignment
Result of assignment expression used
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
			TreeNode nextChild = this;
			TreeNode parent;
			while ((parent = nextChild.parent) != null)
			{
				int indexOf = parent.children.indexOf(nextChild);
```

### NestedAssignment
Result of assignment expression used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
		super.onInitialize();
		
		add(localDateField = newDateField("date", new DateModel()));
		add(timeField = newTimeField("time", new TimeModel()));
	}
```

### NestedAssignment
Result of assignment expression used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
		
		add(localDateField = newDateField("date", new DateModel()));
		add(timeField = newTimeField("time", new TimeModel()));
	}

```

### NestedAssignment
Result of assignment expression used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java

		// Create and add the "hours" TextField
		add(hoursField = newHoursTextField("hours", new HoursModel(), Integer.class));

		// Create and add the "minutes" TextField
```

### NestedAssignment
Result of assignment expression used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java

		// Create and add the "minutes" TextField
		add(minutesField = newMinutesTextField("minutes", new MinutesModel(), Integer.class));

		// Create and add the "AM/PM" choice
```

### NestedAssignment
Result of assignment expression used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java

		// Create and add the "AM/PM" choice
		add(amOrPmChoice = new DropDownChoice<AM_PM>("amOrPmChoice", new AmPmModel(),
			Arrays.asList(AM_PM.values())) {
				private static final long serialVersionUID = 1L;

			@Override
			protected boolean localizeDisplayValues()
			{
				return true;
			}
		});
	}

```

### NestedAssignment
Result of assignment expression used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java

		// add the text label for the message 'Rating 4.5 out of 25 votes'
		rater.add(ratingLabel = newRatingLabel("rating", wrap(rating), this.nrOfVotes));

		// set auto generation of the markup id on, such that ajax calls work.
```

### NestedAssignment
Result of assignment expression used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java

		// Image referencing captcha image resource
		image = new NonCachingImage("image", imageResource = new CaptchaImageResource(animals));
		image.add(new AjaxEventBehavior("click")
		{
```

### NestedAssignment
Result of assignment expression used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
		if (behavior == null)
		{
			super.add(behavior = newAutoCompleteBehavior(renderer, settings));
		}
	}
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/csp/ReportCSPViolationMapper.java`
#### Snippet
```java
						char[] buffer = new char[MAX_LOG_SIZE];
						int n;
						if (-1 != (n = httpRequest.getReader().read(buffer)))
						{
							sw.write(buffer, 0, n);
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
		if (result == null)
		{
			IPropertyLocator tmpResult = applicationToLocators.putIfAbsent(key, result = new CachingPropertyLocator(new DefaultPropertyLocator()));
			if (tmpResult != null)
			{
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
				Class<?> repCl;
				if (!(Boolean)HAS_WRITE_REPLACE_METHOD_METHOD.invoke(desc, (Object[])null) ||
						(obj = INVOKE_WRITE_REPLACE_METHOD.invoke(desc, obj)) == null ||
						(repCl = obj.getClass()) == cls)
				{
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
				if (!(Boolean)HAS_WRITE_REPLACE_METHOD_METHOD.invoke(desc, (Object[])null) ||
						(obj = INVOKE_WRITE_REPLACE_METHOD.invoke(desc, obj)) == null ||
						(repCl = obj.getClass()) == cls)
				{
					break;
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			{
				refreshInternalIteratorIfNeeded();
				return currentComponent = internalIterator.next();
			}

```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
				return;
			}
		} while ((close = dequeue.takeTag()) != null);

		throw new IllegalStateException(String.format("Could not find the closing tag for '%s'", tag));
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java

					XmlTag tag;
					while ((tag = p.nextTag()) != null)
					{
						if (tagName.equalsIgnoreCase(tag.getName()))
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
			// Loop through tags
			MarkupElement elem;
			while (null != (elem = getNextTag()))
			{
				if (elem instanceof HtmlSpecialTag)
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
	public final String getTagAttribute(final String name, final boolean withWicketNamespace)
	{
		String attr = (withWicketNamespace ? attr = getWicketNamespace() + ":" + name : name);
		return getTag().getAttributes().getString(attr);
	}
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
				if (tag.isOpen() || tag.isOpenClose())
				{
					return current = get(currentIndex);
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
		if (++currentIndex < markup.size())
		{
			return current = get(currentIndex);
		}

```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		parser.parse(getLastResponseAsString());
		XmlTag tag;
		while ((tag = parser.nextTag()) != null)
		{
			if (tag.isOpen() && tag.getName().equals("script") &&
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
							componentInPage = null;
						}
						lastRenderedPage = lastPage = renderedPage;
					}
					else
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUpload.java`
#### Snippet
```java
				byte[] buf = new byte[Math.min((int)item.getSize(), 4096 * 10)];
				int len;
				while (-1 != (len = in.read(buf)))
				{
					digest.update(buf, 0, len);
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RootMarkupFilter.java`
#### Snippet
```java
	{
		HttpTagType type;
		while ((type = parser.next()) != HttpTagType.NOT_INITIALIZED)
		{
			if (type == HttpTagType.BODY)
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketRemoveTagHandler.java`
#### Snippet
```java
		// fetch markup elements
		MarkupElement markupElement;
		while ((markupElement = getNextFilter().nextElement()) != null)
		{
			// skip non-component tags
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PartWriterCallback.java`
#### Snippet
```java
					int readBytes;

					while ((readBytes = boundedInputStream.read(buffer)) != -1)
					{
						outputStream.write(buffer, 0, readBytes);
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/RequestCycleCachedResourceVersion.java`
#### Snippet
```java
			{
				// no, so create it
				requestCycle.setMetaData(CACHE_KEY, cache = Generics.newHashMap());
			}
			else if (cache.containsKey(key))
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/MessageDigestResourceVersion.java`
#### Snippet
```java

		// read stream and update message digest
		while ((len = inputStream.read(buf)) != -1)
		{
			digest.update(buf, 0, len);
```

### NestedAssignment
Result of assignment expression used
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
		}
		int o;
		int i = o = beginIndex;
		while (i < endIndex)
		{
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/GuestBook.java`
#### Snippet
```java

		// Add commentListView of existing comments
		comments.add(commentListView = new ListView<Comment>("comments",
			new PropertyModel<>(this, "commentList"))
		{
			@Override
			public void populateItem(final ListItem<Comment> listItem)
			{
				final Comment comment = listItem.getModelObject();
				listItem.add(new Label("date", comment::getDate));
				listItem.add(new MultiLineLabel("text", comment.getText()));
			}
		});

		// The AjaxFormSubmitBehavior already calls the onSubmit of the form,
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/prototype/Index.java`
#### Snippet
```java

		// Add the label
		add(counter = new Label("counter", new PropertyModel<>(this, "count")));
	}

```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java

			// create a textfield to demo non-file content
			form.add(text = new TextField<>("text", Model.of()));
			text.add(StringValidator.minimumLength(2));

```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java

			// create the file upload field
			form.add(file = new FileUploadField("file"));

			form.add(new Label("max", form::getMaxSize));
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java

			// create a submit button
			form.add(submit = new Button("submit"));
			submit.setOutputMarkupId(true);

```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java

			// create the ajax button used to submit the form
			form.add(ajaxSubmit = new AjaxButton("ajaxSubmit")
			{
				private static final long serialVersionUID = 1L;

				/**
				 * Need to trigger submit to initiate progressbar.
				 */
				@Override
				protected boolean shouldTriggerJavaScriptSubmitEvent()
				{
					return true;
				}

				@Override
				protected void onSubmit(AjaxRequestTarget target)
				{
					info("This request was processed using AJAX");

					// ajax-update the feedback panel
					target.add(feedback);
				}

				@Override
				protected void onError(AjaxRequestTarget target)
				{
					// update feedback to display errors
					target.add(feedback);
				}

```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
			columns.add(new PropertyColumn<>(Model.of("Last Modified"), "lastModified"));
			columns.add(new PropertyColumn<>(Model.of("MIME Type"), "mimeType"));
			selectedFileInfo = new AjaxFallbackDefaultDataTable<>("selectedFileInfo", columns, dataProvider = new DataProvider(), 100) {
				@Override
				protected void onConfigure() {
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java

			// create a textfield to demo non-file content
			form.add(text = new TextField<>("text", Model.of()));
			text.add(StringValidator.minimumLength(2));

```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java

			// create the file upload field
			form.add(file = new FileUploadField("file"));

			add(selectedFileInfo = new Label("selectedFileInfo", (IModel<String>) () -> fileInfo) {
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
			form.add(file = new FileUploadField("file"));

			add(selectedFileInfo = new Label("selectedFileInfo", (IModel<String>) () -> fileInfo) {
				@Override
				protected void onAfterRender() {
					super.onAfterRender();
					fileInfo = null;
				}
			});
			selectedFileInfo.setOutputMarkupId(true);
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java

			// create a submit button
			form.add(submit = new Button("submit"));
			submit.setOutputMarkupId(true);

```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java

			// create the ajax button used to submit the form
			form.add(ajaxSubmit = new AjaxButton("ajaxSubmit")
			{
				private static final long serialVersionUID = 1L;

				/**
				 * Need to trigger submit to initiate progressbar.
				 */
				@Override
				protected boolean shouldTriggerJavaScriptSubmitEvent()
				{
					return true;
				}

				@Override
				protected void onSubmit(AjaxRequestTarget target)
				{
					info("This request was processed using AJAX");

					// ajax-update the feedback panel
					target.add(feedback);
				}

				@Override
				protected void onError(AjaxRequestTarget target)
				{
					// update feedback to display errors
					target.add(feedback);
				}

```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadPage.java`
#### Snippet
```java

			// Add one file input field
			add(fileUploadField = new FileUploadField("fileInput"));

			// Set maximum size to 100K for demo purposes
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/Home.java`
#### Snippet
```java
		// Add table of books
		final PageableListView<Book> listView;
		add(listView = new PageableListView<Book>("books", new PropertyModel<>(this,
			"books"), 4)
		{
			@Override
			public void populateItem(final ListItem<Book> listItem)
			{
				final Book book = listItem.getModelObject();
				listItem.add(BookDetails.link("details", book, getLocalizer().getString(
					"noBookTitle", this)));
				listItem.add(new Label("author", new Model<>(book)));
				listItem.add(moveUpLink("moveUp", listItem));
				listItem.add(moveDownLink("moveDown", listItem));
				listItem.add(removeLink("remove", listItem));
				listItem.add(EditBook.link("edit", book.getId()));
			}
		});
		add(new PagingNavigator("navigator", listView));
	}
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/border/TemplateBorder.java`
#### Snippet
```java
	{
		super(id);
		addToBorder(currentBanner = new Banner1("ad"));
		addToBorder(new Link<Void>("changeAdLink")
		{
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/border/TemplateBorder.java`
#### Snippet
```java
				if (currentBanner.getClass() == Banner1.class)
				{
					TemplateBorder.this.replaceInBorder(currentBanner = new Banner2("ad"));
				}
				else
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/border/TemplateBorder.java`
#### Snippet
```java
				else
				{
					TemplateBorder.this.replaceInBorder(currentBanner = new Banner1("ad"));
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/pageinheritance/TemplatePage.java`
#### Snippet
```java
				if (currentBanner.getClass() == Banner1.class)
				{
					TemplatePage.this.replace(currentBanner = new Banner2("ad"));
				}
				else
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/pageinheritance/TemplatePage.java`
#### Snippet
```java
				else
				{
					TemplatePage.this.replace(currentBanner = new Banner1("ad"));
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/pageinheritance/TemplatePage.java`
#### Snippet
```java
	{
		add(new Label("title", new PropertyModel<>(this, "pageTitle")));
		add(currentBanner = new Banner1("ad"));
		add(new Link<Void>("changeAdLink")
		{
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/velocity/DynamicPage.java`
#### Snippet
```java

		VelocityPanel panel;
		add(panel = new VelocityPanel("templatePanel", new MapModel<>(map))
		{
			@Override
			protected IResourceStream getTemplateResource()
			{
				return template;
			}

			@Override
			protected boolean parseGeneratedMarkup()
			{
				return true;
			}
		});
		for (Field field : fields)
		{
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/velocity/TemplatePage.java`
#### Snippet
```java
		{
			super(name);
			add(templateTextArea = new TextArea<>("templateInput",
				new PropertyModel<>(
					new Model<>(TemplatePage.this), "template")));
		}
	}
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/Multiply.java`
#### Snippet
```java
	private void init()
	{
		add(left = new TextField<>("left", new PropertyModel<>(this, "lhs")));
		add(right = new TextField<>("right", new PropertyModel<>(this, "rhs")));
		left.setRequired(true);
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/Multiply.java`
#### Snippet
```java
	{
		add(left = new TextField<>("left", new PropertyModel<>(this, "lhs")));
		add(right = new TextField<>("right", new PropertyModel<>(this, "rhs")));
		left.setRequired(true);
		right.setRequired(true);
```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/authorization/pages/AnnotationsPanelsPage.java`
#### Snippet
```java
		add(new ForAdminsAndUsers("forAdminsAndUsersPanel"));
		add(new ForAdmins("forAdminsPanel"));
		add(outer = new WebMarkupContainer("outer"));
		outer.setOutputMarkupId(true);

```

### NestedAssignment
Result of assignment expression used
in `wicket-examples/src/main/java/org/apache/wicket/examples/kittenCaptcha/HomePage.java`
#### Snippet
```java
	public HomePage(final PageParameters parameters)
	{
		add(captcha = new KittenCaptchaPanel("captcha", new Dimension(400, 200)));

		// In a real application, you'd check the kittens in a form
```

## RuleId[id=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`value = value / len` could be simplified to 'value /= len'
in `wicket-util/src/main/java/org/apache/wicket/util/LongEncoder.java`
#### Snippet
```java
			int mod = (int)(value % len);
			buff.prepend(alphabet.charAt(mod));
			value = value / len;
		}
		while (value > 0);
```

### ReplaceAssignmentWithOperatorAssignment
`totalLength = totalLength - string.length()` could be simplified to 'totalLength -= string.length()'
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringList.java`
#### Snippet
```java
	{
		String string = strings.remove(index);
		totalLength = totalLength - string.length();
	}

```

### ReplaceAssignmentWithOperatorAssignment
`hashcode = hashcode + (127 * beanName.hashCode())` could be simplified to 'hashcode += (127 \* beanName.hashCode())'
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
		if (getBeanName() != null)
		{
			hashcode = hashcode + (127 * beanName.hashCode());
		}
		return hashcode;
```

### ReplaceAssignmentWithOperatorAssignment
`c = c * -1` could be simplified to 'c \*= -1'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPagesProvider.java`
#### Snippet
```java
			if (getSort().isAscending() == false)
			{
				c = c * -1;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`renderedUrl = renderedUrl + '/'` could be simplified to 'renderedUrl += '/''
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
		{
			// WICKET-4401
			renderedUrl = renderedUrl + '/';
		}

```

### ReplaceAssignmentWithOperatorAssignment
`prefix = prefix + "/"` could be simplified to 'prefix += "/"'
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/WicketMetricsSettings.java`
#### Snippet
```java
		if (!prefix.endsWith("/"))
		{
			prefix = prefix + "/";
		}
		this.prefix = prefix;
```

### ReplaceAssignmentWithOperatorAssignment
`basedir = basedir + "/"` could be simplified to 'basedir += "/"'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTester.java`
#### Snippet
```java
		if (basedir != null)
		{
			basedir = basedir + "/";
		}
		else
```

### ReplaceAssignmentWithOperatorAssignment
`pos1 = pos1 + matcher.end()` could be simplified to 'pos1 += matcher.end()'
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
			if (matcher.find())
			{
				pos1 = pos1 + matcher.end();
			}
			else
```

### ReplaceAssignmentWithOperatorAssignment
`url = url + href` could be simplified to 'url += href'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
						if (href != null && href.length() > 1 && href.charAt(0) == '#')
						{
							url = url + href;
						}
					}
```

### ReplaceAssignmentWithOperatorAssignment
`url = url + (url.contains("?") ? "&" : "?")` could be simplified to 'url += (url.contains("?") ? "\&" : "?")'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java
	{
		String url = tag.getAttributes().getString("src");
		url = url + (url.contains("?") ? "&" : "?");
		url = url + "antiCache=" + System.currentTimeMillis();

```

### ReplaceAssignmentWithOperatorAssignment
`url = url + anchor` could be simplified to 'url += anchor'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
			if (anchor != null)
			{
				url = url + anchor;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`i = i - 2` could be simplified to 'i -= 2'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestUtils.java`
#### Snippet
```java
					newcomponents.remove(i);
					newcomponents.remove(i);
					i = i - 2;
					if (i < -1)
					{
```

### ReplaceAssignmentWithOperatorAssignment
`epoch = epoch / 60` could be simplified to 'epoch /= 60'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
		int epoch = (int)((calendar.getTimeInMillis() / 1000) % (60 * 60 * 24));
		int seconds = epoch % 60;
		epoch = epoch / 60;
		int minutes = epoch % 60;
		int hours = epoch / 60;
```

### ReplaceAssignmentWithOperatorAssignment
`s = s + "/"` could be simplified to 's += "/"'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
				if (file.isDirectory())
				{
					s = s + "/";
				}
				result.add(s);
```

### ReplaceAssignmentWithOperatorAssignment
`size = size + diskData.size()` could be simplified to 'size += diskData.size()'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
			for (DiskData diskData : diskDatas.values())
			{
				size = size + diskData.size();
			}
		}
```

## RuleId[id=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `fbIndex` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
    private int[] firstBytes;
    private int fbLength;
    private int fbIndex;
    private int markFbIndex;
    private boolean markedAtStart;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `firstBytes` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
    private final List<ByteOrderMark> boms;
    private ByteOrderMark byteOrderMark;
    private int[] firstBytes;
    private int fbLength;
    private int fbIndex;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `count` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java
{
	private final List<byte[]> buffers = new java.util.ArrayList<>();
	private int count;
	private byte[] currentBuffer;
	private int currentBufferIndex;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentBufferIndex` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java
	private int count;
	private byte[] currentBuffer;
	private int currentBufferIndex;
	private int filledBufferSum;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentBuffer` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java
	private final List<byte[]> buffers = new java.util.ArrayList<>();
	private int count;
	private byte[] currentBuffer;
	private int currentBufferIndex;
	private int filledBufferSum;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `filledBufferSum` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java
	private byte[] currentBuffer;
	private int currentBufferIndex;
	private int filledBufferSum;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deltas_` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Revision.java`
#### Snippet
```java
{

	List<Delta> deltas_ = new LinkedList<>();

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `compiledPattern` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/MetaPattern.java`
#### Snippet
```java

	/** The compiled MetaPattern */
	private Pattern compiledPattern;

	// Regexps that are used multiple times in defining meta patterns
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `patterns` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/MetaPattern.java`
#### Snippet
```java

	/** List of patterns, or null if pattern variable is valid instead */
	private List<MetaPattern> patterns;

	/** The compiled MetaPattern */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `log` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java

	/** the <code>log</code> to give to the user's code */
	private Logger log = null;

	/** the name of this <code>Task</code> */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isStarted` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java

	/** <code>true</code> if the task's thread has already started executing */
	private boolean isStarted = false;

	/** the <code>log</code> to give to the user's code */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isDaemon` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java
{
	/** <code>true</code> if the task's thread should be a daemon */
	private boolean isDaemon = true;

	/** <code>true</code> if the task's thread has already started executing */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `startTime` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java

	/** the <code>Instant</code> at which the task should start */
	private Instant startTime = Instant.now();

	/** When set the task will stop as soon as possible */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `thread` is accessed in both synchronized and unsynchronized contexts
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java

	/** each <code>Task</code> has an associated <code>Thread</code> */
	private Thread thread;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `charSetMap` is accessed in both synchronized and unsynchronized contexts
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetUtil.java`
#### Snippet
```java

	/** Locale to character encoding mapping */
	private static CharSetMap charSetMap;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pageManagerProvider` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java

	private volatile IPageManager pageManager;
	private IPageManagerProvider pageManagerProvider;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sessionStoreProvider` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java

	/** session store provider */
	private Supplier<ISessionStore> sessionStoreProvider;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metaData` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java

	/** Session level meta data. */
	private MetaDataEntry<?>[] metaData;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `arcWidth` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResource.java`
#### Snippet
```java

	/** The width of the arc in the corner */
	private int arcWidth = 10;

	/** The background color behind the button */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `arcHeight` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResource.java`
#### Snippet
```java

	/** The height of the arc in the corner */
	private int arcHeight = 10;

	/** The width of the arc in the corner */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `imageData` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/BufferedDynamicImageResource.java`
#### Snippet
```java

	/** The byte array holding the contents of the dynamic image */
	private byte[] imageData;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `imageData` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/RenderedDynamicImageResource.java`
#### Snippet
```java

	/** Transient image data so that image only needs to be generated once per VM */
	private transient SoftReference<byte[]> imageData;

	/** Type of image (one of BufferedImage.TYPE_*) */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `format` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/request/resource/DynamicImageResource.java`
#### Snippet
```java

	/** The image type */
	private String format = "png";

	/** The last modified time of this resource */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastModifiedTime` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/request/resource/DynamicImageResource.java`
#### Snippet
```java

	/** The last modified time of this resource */
	private Instant lastModifiedTime;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `requestWindow` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	 * Rounded request buffer that keeps the request data for the last N requests in the buffer.
	 */
	private RequestData[] requestWindow;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cryptFactory` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/settings/SecuritySettings.java`
#### Snippet
```java

	/** factory for creating crypt objects */
	private ICryptFactory cryptFactory;

	/** supplier of random data and SecureRandom */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `resourceWatcher` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java

	/** ModificationWatcher to watch for changes in markup files */
	private IModificationWatcher resourceWatcher;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pageStore` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
		private static final long serialVersionUID = 1L;

		private transient DiskPageStore pageStore;

		private transient String fileName;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sessionIdentifier` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
		private transient String fileName;

		private String sessionIdentifier;

		private PageWindowManager manager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pages` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
		 * request.
		 */
		List<IManageablePage> pages = new LinkedList<>();
		
		@Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serializer` is accessed in both synchronized and unsynchronized contexts
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
	{

		transient ISerializer serializer;

		/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `progressUpdateTask` is accessed in both synchronized and unsynchronized contexts
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/JSR356Session.java`
#### Snippet
```java
public class JSR356Session extends WebSession
{
    private ProgressUpdater.ProgressUpdateTask progressUpdateTask;

    public JSR356Session(Request request)
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`1 << 0` can be replaced with '1'
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
	 * schemes.
	 */
	public static final int ALLOW_ALL_SCHEMES = 1 << 0;

	/**
```

## RuleId[id=HtmlWrongAttributeValue]
### HtmlWrongAttributeValue
Wrong attribute value
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/EditBook.html`
#### Snippet
```java
	    <td align="right">Author:</td>
	    <td wicket:id = "authorFeedback">
	      <input wicket:id = "author" type="text" value="Mark Twain" hidden="1" size="30"/>
	    </td>
	  </tr>
```

## RuleId[id=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileItemIterator.java`
#### Snippet
```java
public interface FileItemIterator {

    List<FileItem> getFileItems() throws FileUploadException, IOException;

    /**
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/impl/FileItemIteratorImpl.java`
#### Snippet
```java
     * @throws IOException An I/O error occurred.
     */
    private boolean findNextItem() throws FileUploadException, IOException {
        if (eof) {
            return false;
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/impl/FileItemIteratorImpl.java`
#### Snippet
```java
    }

    public MultipartStream getMultiPartStream() throws FileUploadException, IOException {
        if (multiPartStream == null) {
            init(fileUploadBase, ctx);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/impl/FileItemIteratorImpl.java`
#### Snippet
```java
    }

    protected void init(final AbstractFileUpload fileUploadBase, final RequestContext requestContext) throws FileUploadException, IOException {
        final String contentType = ctx.getContentType();
        if ((null == contentType) || (!contentType.toLowerCase(Locale.ENGLISH).startsWith(AbstractFileUpload.MULTIPART))) {
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/impl/FileItemIteratorImpl.java`
#### Snippet
```java

    @Override
    public List<FileItem> getFileItems() throws FileUploadException, IOException {
        final List<FileItem> items = new ArrayList<>();
        while (hasNext()) {
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
    @Override
    public String getString(final String charset)
        throws UnsupportedEncodingException, IOException {
        return new String(get(), charset);
    }
```

## RuleId[id=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-util/src/main/java/org/apache/wicket/util/LazyInitializer.java`
#### Snippet
```java
		if (instance == null)
		{
			synchronized (this)
			{
				if (instance == null)
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-util/src/main/java/org/apache/wicket/util/collections/ClassMetaCache.java`
#### Snippet
```java

			// only lock in rare event of unknown ClassLoader
			synchronized (this)
			{
				// check again inside lock
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
		if (sessionStore == null)
		{
			synchronized (this)
			{
				if (sessionStore == null)
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
		if (pageManager == null)
		{
			synchronized (this)
			{
				if (pageManager == null)
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-core/src/main/java/org/apache/wicket/page/PageAccessSynchronizer.java`
#### Snippet
```java
			try
			{
				wait(remaining);
			}
			catch (InterruptedException e)
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-core/src/main/java/org/apache/wicket/page/PageAccessSynchronizer.java`
#### Snippet
```java
			}
			released = true;
			notifyAll();
		}
	}
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/StoredResponsesMap.java`
#### Snippet
```java
		BufferedWebResponse result = null;
		Value value;
		synchronized (this)
		{
			value = (Value)super.get(key);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/StoredResponsesMap.java`
#### Snippet
```java
	{
		Value removedValue;
		synchronized (this)
		{
			removedValue = (Value)super.remove(key);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/StoredResponsesMap.java`
#### Snippet
```java

		Value oldValue;
		synchronized (this)
		{
			oldValue = (Value)super.put(key, value);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
		if (resourceWatcher == null && start)
		{
			synchronized (this)
			{
				if (resourceWatcher == null)
```

## RuleId[id=CastConflictsWithInstanceof]
### CastConflictsWithInstanceof
Cast to 'Component' type conflicts with preceding 'instanceof IFormSubmittingComponent' check
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
				{
					IFormSubmittingComponent submittingComponent = (IFormSubmittingComponent)submitter;
					Component component = (Component)submitter;

					if (!component.isVisibleInHierarchy())
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `n` initializer `0` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		char[] buffer = new char[DEFAULT_BUFFER_SIZE];
		int count = 0;
		int n = 0;
		while (-1 != (n = input.read(buffer)))
		{
```

### UnusedAssignment
Variable `n` initializer `0` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		byte[] buffer = new byte[DEFAULT_BUFFER_SIZE];
		int count = 0;
		int n = 0;
		while (-1 != (n = input.read(buffer)))
		{
```

### UnusedAssignment
Variable `prev` initializer `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/MyersDiff.java`
#### Snippet
```java
				final int kplus = kmiddle + 1;
				final int kminus = kmiddle - 1;
				PathNode prev = null;

				int i;
```

### UnusedAssignment
Variable `valueOf` initializer `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		}

		Method valueOf = null;
		try
		{
```

### UnusedAssignment
Variable `revision` initializer `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/license/VelocityLicenseHeaderHandler.java`
#### Snippet
```java
	public boolean checkLicenseHeader(final File file)
	{
		Revision revision = null;

		try
```

### UnusedAssignment
Variable `added` initializer `false` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlLicenseHeaderHandler.java`
#### Snippet
```java
	public boolean addLicenseHeader(final File file)
	{
		boolean added = false;

		try
```

### UnusedAssignment
Variable `revision` initializer `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlLicenseHeaderHandler.java`
#### Snippet
```java
	public boolean checkLicenseHeader(final File file)
	{
		Revision revision = null;

		try
```

### UnusedAssignment
Variable `revision` initializer `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/license/CssLicenseHeaderHandler.java`
#### Snippet
```java
	public boolean checkLicenseHeader(final File file)
	{
		Revision revision = null;

		try
```

### UnusedAssignment
Variable `revision` initializer `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/license/PropertiesLicenseHeaderHandler.java`
#### Snippet
```java
	public boolean checkLicenseHeader(final File file)
	{
		Revision revision = null;

		try
```

### UnusedAssignment
Variable `lastModified` initializer `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java

	/** The last time this stylesheet was modified */
	private Instant lastModified = null;

	/**
```

### UnusedAssignment
Variable `fields` initializer `null` is redundant
in `wicket-ioc/src/main/java/org/apache/wicket/injection/Injector.java`
#### Snippet
```java
		final Class<?> clazz = object.getClass();

		Field[] fields = null;

		// try cache
```

### UnusedAssignment
Variable `datePattern` initializer `null` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
	 * The date pattern of the text field
	 */
	private String datePattern = null;

	/**
```

### UnusedAssignment
Variable `originalImage` initializer `null` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/image/resource/ThumbnailImageResource.java`
#### Snippet
```java
	{
		InputStream is = null;
		BufferedImage originalImage = null;
		try
		{
```

### UnusedAssignment
Variable `nrOfStars` initializer `new Model<>(5)` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	 * The number of stars that need to be shown, should result in an Integer object.
	 */
	private IModel<Integer> nrOfStars = new Model<>(5);

	/**
```

### UnusedAssignment
Variable `filterName` initializer `null` is redundant
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/aspects/WicketFilterInitAspect.java`
#### Snippet
```java
	{
		Object[] args = joinPoint.getArgs();
		String filterName = null;
		if (args.length == 2)
		{
```

### UnusedAssignment
Variable `sizeThreshold` initializer `DEFAULT_SIZE_THRESHOLD` is redundant
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItemFactory.java`
#### Snippet
```java
     * The threshold above which uploads will be stored on disk.
     */
    private int sizeThreshold = DEFAULT_SIZE_THRESHOLD;

    /**
```

### UnusedAssignment
Variable `includeRememberMe` initializer `true` is redundant
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/panel/SignInPanel.java`
#### Snippet
```java

	/** True if the panel should display a remember-me checkbox */
	private boolean includeRememberMe = true;

	/** True if the user should be remembered via form persistence (cookies) */
```

### UnusedAssignment
Variable `rtlLocale` initializer `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java

	/** True if locale's language is RTL (right-to-left) */
	private boolean rtlLocale = false;

	/** Session level meta data. */
```

### UnusedAssignment
The value `null` assigned to `in` is never used
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java
			{
				in.close();
				in = null;

				replaceExpectedResultFile(document, clazz, file);
```

### UnusedAssignment
The value `getWicketNamespace() + ":" + name` assigned to `attr` is never used
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
	public final String getTagAttribute(final String name, final boolean withWicketNamespace)
	{
		String attr = (withWicketNamespace ? attr = getWicketNamespace() + ":" + name : name);
		return getTag().getAttributes().getString(attr);
	}
```

### UnusedAssignment
Variable `handler` initializer `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
			component);

		IRequestHandler handler = null;
		if (page.isPageStateless() || (page.isBookmarkable() && page.wasCreatedBookmarkable()))
		{
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/debug/PageView.java`
#### Snippet
```java

		// Create an empty list. It'll be filled later
		List<ComponentData> data = null;

		String pageRenderDuration = "n/a";
```

### UnusedAssignment
Variable `width` initializer `100` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/RenderedDynamicImageResource.java`
#### Snippet
```java

	/** Width of image */
	private int width = 100;

	/**
```

### UnusedAssignment
Variable `height` initializer `100` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/RenderedDynamicImageResource.java`
#### Snippet
```java

	/** Height of image */
	private int height = 100;

	/** Transient image data so that image only needs to be generated once per VM */
```

### UnusedAssignment
Variable `label` initializer `""` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigation.java`
#### Snippet
```java

		// Add a page number label to the list which is enclosed by the link
		String label = "";
		if (labelProvider != null)
		{
```

### UnusedAssignment
Variable `includeSession` initializer `true` is redundant
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackCollector.java`
#### Snippet
```java
{
	private final Component component;
	private boolean includeSession = true;
	private boolean recursive = true;

```

### UnusedAssignment
Variable `elements` initializer `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
			name = name.substring(0, name.length() - 1);
		}
		String[] elements = null;
		if (name.trim().length() == 0)
		{
```

### UnusedAssignment
Variable `fileSystem` initializer `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemJarPathService.java`
#### Snippet
```java
			int indexOfExclamationMark = uriString.indexOf('!');
			String jarFile = uriString.substring(0, indexOfExclamationMark);
			FileSystem fileSystem = null;

			synchronized (FILE_SYSTEM_META_DATA_KEY)
```

### UnusedAssignment
Variable `ipV4Address` initializer `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
		}

		boolean ipV4Address = false;
		boolean hostname = false;
		// check if authority is IP address or hostname
```

### UnusedAssignment
Variable `segmentLength` initializer `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
			boolean match = true;
			int segmentCount = 0;
			int segmentLength = 0;

			while (match)
```

### UnusedAssignment
Variable `options` initializer `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
	 * Holds the set of current validation options.
	 */
	private long options = 0;

	/**
```

### UnusedAssignment
Variable `html` initializer `"`\n" + " site 1\n" + " site 2... is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<select wicket:id=\"sites\">\n" + "    <option>site 1</option>\n"
			+ "    <option>site 2</option>\n" + "</select>\n" + "<select wicket:id=\"choices\">\n"
			+ "    <option>choice 1</option>\n" + "    <option>choice 2</option>\n" + "</select>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;// Add a multiple list choice component that uses the model object's 'site'\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// property to designate the current selection, and that uses the SITES\n"
```

### UnusedAssignment
Variable `code` initializer `" // Add a multiple list choice component that uses the model object's 'site'...` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java
			+ "    <option>site 2</option>\n" + "</select>\n" + "<select wicket:id=\"choices\">\n"
			+ "    <option>choice 1</option>\n" + "    <option>choice 2</option>\n" + "</select>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;// Add a multiple list choice component that uses the model object's 'site'\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// property to designate the current selection, and that uses the SITES\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// list for the available options.\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// Note that our model here holds a Collection, as we need to store\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// multiple values too\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;ListMultipleChoice siteChoice = new ListMultipleChoice(\"sites\", SITES);\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;form.add(siteChoice);\n"
			+ "\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;ListMultipleChoice manyChoice = new ListMultipleChoice(\"choices\", MANY_CHOICES).setMaxRows(5);\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;form.add(manyChoice);";

		html = "SEE INSIDE FOR NOW";
```

### UnusedAssignment
Variable `properties` initializer `null` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajaxhellobrowser/AjaxHelloBrowser.java`
#### Snippet
```java
		RequestCycleSettings requestCycleSettings = getApplication().getRequestCycleSettings();
		boolean gatherExtendedBrowserInfo = requestCycleSettings.getGatherExtendedBrowserInfo();
		ClientProperties properties = null;
		try
		{
```

### UnusedAssignment
Variable `properties` initializer `null` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajaxhellowbrowserondomready/AjaxHelloBrowserOnDomReady.java`
#### Snippet
```java
		RequestCycleSettings requestCycleSettings = getApplication().getRequestCycleSettings();
		boolean gatherExtendedBrowserInfo = requestCycleSettings.getGatherExtendedBrowserInfo();
		ClientProperties properties = null;
		try
		{
```

## RuleId[id=IndexOfReplaceableByContains]
### IndexOfReplaceableByContains
`accept.indexOf(MarkupType.XML_MIME) != -1` can be replaced with 'accept.contains(MarkupType.XML_MIME)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

				String accept = request.getHeader("Accept");
				insert = ((accept == null) || (accept.indexOf(MarkupType.XML_MIME) != -1));
			}

```

## RuleId[id=StringEqualsCharSequence]
### StringEqualsCharSequence
`String.equals()` called with 'CharSequence' argument
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		{
			if (tag.isOpen() && tag.getName().equals("script") &&
				"wicket-ajax-base-url".equals(tag.getAttribute("id")))
			{
				parser.next();
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `wicket-core/src/main/java/org/apache/wicket/request/resource/MetaInfStaticResourceReference.java`
#### Snippet
```java
		String packageName = Packages.extractPackageName(getScope());
		String[] parts = Strings.split(packageName, '.');
		for (String p : parts)
		{
			segments.add(p);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `wicket-core/src/main/java/org/apache/wicket/request/resource/MetaInfStaticResourceReference.java`
#### Snippet
```java

		parts = Strings.split(getName(), '/');
		for (String p : parts)
		{
			segments.add(p);
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-request/src/main/java/org/apache/wicket/request/RequestHandlerExecutor.java`
#### Snippet
```java
		}

		RuntimeException rethrow = null;;
		for (IRequestHandler handler : inactiveRequestHandlers)
		{
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-request/src/main/java/org/apache/wicket/request/http/handler/RedirectRequestHandler.java`
#### Snippet
```java
		 * Decide dynamically depending on the value of {@code status} field
		 */
		AUTO;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		 * {@literal ..} an {@link IllegalStateException} is thrown.
		 */
		FULL;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
	public enum AM_PM
	{
		AM, PM;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/DequeueTagAction.java`
#### Snippet
```java
	SKIP,
	/** ignore this tag, skip it but do not skip its children */
	IGNORE;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
				return current;
			}
		};
		return new MarkupChildIterator();
	}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/model/StringResourceModel.java`
#### Snippet
```java
					// WICKET-5820 interpolate null with 'null'
					return result == null ? "null" : result;
				};
			}.toString();
		}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/PriorityFirstComparator.java`
#### Snippet
```java
	protected static enum HeaderItemType
	{
		PRIORITY, PAGE, COMPONENT;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
		setFlag(FLAG_CONVERT_EMPTY_INPUT_STRING_TO_NULL, flag);
		return this;
	};
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/BrowserInfoPage.java`
#### Snippet
```java
			setResponsePage(getApplication().getHomePage());
		}
	};
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java
				// Header response will be auto-closed before rendering the header container itself
				// See https://issues.apache.org/jira/browse/WICKET-3728
			};

			// Cleanup extraneous CR and LF from the response
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlTag.java`
#### Snippet
```java

		/** An open/close tag, like &lt;TAG componentId = "xyz"/&gt;. */
		OPEN_CLOSE;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
		
		super.onDetach();
	};
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java
			catch (Exception ignored)
			{
				; // noop
			}
		}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-core/src/main/java/org/apache/wicket/application/WildcardMatcherHelper.java`
#### Snippet
```java
				while (++ipat < lpat && apat[ipat] == STAR)
				{
					; // noop
				}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
					// clear raw input of all inputs so that values are reformatted
					getForm().clearInput();
				};
			});
		}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-examples/src/main/java/org/apache/wicket/examples/linkomatic/Home.java`
#### Snippet
```java
			public void onClick()
			{
			};
		};
		add(anotherlinkToAnchor);
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationPropagation.java`
#### Snippet
```java
			return true;
		}
	};
}

```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushBuilder.java`
#### Snippet
```java
			{
				pushBuilder.path(pushItem.getUrl());
				pushItem.getHeaders().entrySet().stream().forEach(pushHeader -> {
					String key = pushHeader.getKey();
					PushItemHeaderValue value = pushHeader.getValue();
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/string/interpolator/VariableInterpolator.java`
#### Snippet
```java
		{
			// Append text before possible variable
			buffer.append(string.substring(pos, start));

			if (string.charAt(start + 1) == '$')
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `wicket-util/src/main/java/org/apache/wicket/util/encoding/UrlDecoder.java`
#### Snippet
```java
			}
		}
		final String result = changed ? new String(bos.toByteArray(), charset) : source;
		// no trying to filter out bad escapes beforehand, just kill all null bytes here at the end,
		// that way none will come through
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/mime/MimeUtility.java`
#### Snippet
```java
            // get the decoded byte data and convert into a string.
            final byte[] decodedData = out.toByteArray();
            return new String(decodedData, javaCharset(charset));
        } catch (final IOException e) {
            throw new UnsupportedEncodingException("Invalid RFC 2047 encoding");
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
	public void validate(IValidatable<String> validatable)
	{
		String email = validatable.getValue().toString();
		if (email.length() != email.trim().length())
		{
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
			{
				int pos2 = rawMarkup.indexOf("-->", pos1 + 4);
				buf.append(rawMarkup.substring(0, pos1));
				if (rawMarkup.length() >= pos2 + 3)
				{
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/Enclosure.java`
#### Snippet
```java
							{
								ComponentTag fullComponentTag = new ComponentTag(tag);
								fullComponentTag.setId(childId.toString());

								controller = ComponentResolvers.resolve(enclosureParent,
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/Enclosure.java`
#### Snippet
```java
	public final String getChildId()
	{
		return childId.toString();
	}

```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/FilenameWithVersionResourceCachingStrategy.java`
#### Snippet
```java
		else
		{
			versionedFilename.append(filename.substring(0, extensionAt));
		}

```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java

			String range = rangeHeader.substring(rangeHeader.indexOf('=') + 1,
					rangeHeader.length());
			
			// support only the first range (WICKET-5995)
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
		if (mode == MODE_BINARY)
		{
			return new String(byteStream.toByteArray());
		}
		else
```

### StringOperationCanBeSimplified
`substring()` call can be replaced with 'charAt()'
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java
				}
				// FIXME it seems that only "includes" are handled. "Excludes" are ignored
				boolean isInclude = rawpattern.substring(0, 1).equals("+");
				String pattern = rawpattern.substring(1);
				if (WildcardMatcherHelper.match(pattern, name) != null)
```

## RuleId[id=MethodOverloadsParentMethod]
### MethodOverloadsParentMethod
Method `write()` overloads a compatible method of a superclass, when overriding might have been intended
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java
	 * @param ch
	 */
	public void write(final char ch)
	{
		buffer.append(ch);
```

### MethodOverloadsParentMethod
Method `greaterThan()` overloads a compatible method of a superclass, when overriding might have been intended
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Bytes.java`
#### Snippet
```java
	 *         <code>Bytes</code> instance
	 */
	public boolean greaterThan(final Bytes other)
	{
		if ((this == other) || (other == null))
```

### MethodOverloadsParentMethod
Method `error()` overloads a compatible method of a superclass, when overriding might have been intended
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 *            validation error
	 */
	public void error(IValidationError error)
	{
		Args.notNull(error, "error");
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/PathNode.java`
#### Snippet
```java
		{
			buf.append("(");
			buf.append(Integer.toString(node.i));
			buf.append(",");
			buf.append(Integer.toString(node.j));
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/PathNode.java`
#### Snippet
```java
			buf.append(Integer.toString(node.i));
			buf.append(",");
			buf.append(Integer.toString(node.j));
			buf.append(")");
			node = node.prev;
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Chunk.java`
#### Snippet
```java
		if (size() <= 1)
		{
			s.append(Integer.toString(rcsfrom()));
		}
		else
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Chunk.java`
#### Snippet
```java
		else
		{
			s.append(Integer.toString(rcsfrom()));
			s.append(separ);
			s.append(Integer.toString(rcsto()));
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Chunk.java`
#### Snippet
```java
			s.append(Integer.toString(rcsfrom()));
			s.append(separ);
			s.append(Integer.toString(rcsto()));
		}
	}
```

### UnnecessaryCallToStringValueOf
Unnecessary `Long.toString()` call
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Bytes.java`
#### Snippet
```java
		}

		return Long.toString(value) + " bytes";
	}

```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java

							// Add to explanatory string to buffer
							buffer.append(Integer.toString(unrenderedComponents.size()))
								.append(". ")
								.append(component.toString(true))
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
	public String toString()
	{
		return "[markup = " + String.valueOf(markup) + ", index = " + currentIndex +
			", current = " + ((current == null) ? "null" : current.toUserDebugString()) + "]";
	}
```

## RuleId[id=ObviousNullCheck]
### ObviousNullCheck
Redundant null-check: literal is never null
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	{
		String contentLength = getLastResponse().getHeader("Content-Length");
		assertNotNull("No Content-Length header found", contentLength);
		return Integer.parseInt(contentLength);
	}
```

### ObviousNullCheck
Redundant null-check: literal is never null
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	{
		String contentType = getLastResponse().getContentType();
		assertNotNull("No Content-Type header found", contentType);
		return contentType;
	}
```

## RuleId[id=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `fillInStackTrace()` overrides synchronized method
in `wicket-request/src/main/java/org/apache/wicket/request/RequestHandlerExecutor.java`
#### Snippet
```java
		 */
		@Override
		public Throwable fillInStackTrace()
		{
			// don't do anything here
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `fillInStackTrace()` overrides synchronized method
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/StalePageException.java`
#### Snippet
```java
	 */
	@Override
	public Throwable fillInStackTrace()
	{
		// don't do anything here
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `fillInStackTrace()` overrides synchronized method
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/PageExpiredException.java`
#### Snippet
```java
	 */
	@Override
	public Throwable fillInStackTrace()
	{
		// don't do anything here
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Set `urls` may contain URL objects
in `wicket-core/src/main/java/org/apache/wicket/application/CompoundClassResolver.java`
#### Snippet
```java
		Args.notNull(name, "name");

		Set<URL> urls = new TreeSet<URL>(new UrlExternalFormComparator());

		for (IClassResolver resolver : resolvers)
```

### CollectionContainsUrl
Set `loadedResources` may contain URL objects
in `wicket-core/src/main/java/org/apache/wicket/application/AbstractClassResolver.java`
#### Snippet
```java
	 * @param loadedResources
	 */
	private void loadResources(Enumeration<URL> resources, Set<URL> loadedResources)
	{
		if (resources != null)
```

### CollectionContainsUrl
Set `resultSet` may contain URL objects
in `wicket-core/src/main/java/org/apache/wicket/application/AbstractClassResolver.java`
#### Snippet
```java
	public Iterator<URL> getResources(final String name)
	{
		Set<URL> resultSet = new TreeSet<>(new UrlExternalFormComparator());

		try
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(ReloadingClassLoader.class);

	private static final Set<URL> urls = new TreeSet<>(new UrlExternalFormComparator());

	private static final List<String> patterns = new ArrayList<>();
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java
	 * @return the log
	 */
	protected synchronized Logger getLog()
	{
		if (log == null)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	 * @return the mappings.
	 */
	protected static Map<String, String> loadResource(final String name)
	{
		final InputStream input = CharSetMap.class.getResourceAsStream(name);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	 *             for an incorrect file.
	 */
	protected static Map<String, String> loadFile(final File file) throws IOException
	{
		return loadStream(new FileInputStream(file));
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	 *             for an incorrect file.
	 */
	protected static Map<String, String> loadPath(final String path) throws IOException
	{
		return loadFile(new File(path));
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	 */
	@SuppressWarnings({ "unchecked" })
	protected final synchronized void setCommonCharSet(final String key, final String charset)
	{
		HashMap<String, String> map = (HashMap<String, String>)mappers.get(MAP_COM);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	 *             for an incorrect stream.
	 */
	protected static Map<String, String> loadStream(final InputStream input)
		throws IOException
	{
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
			 *            a <code>FormComponent</code>
			 */
			protected SingleChoiceSelector(FormComponent<?> formComponent)
			{
				super(formComponent);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
			 *            a <code>FormComponent</code>
			 */
			protected MultipleChoiceSelector(FormComponent<?> formComponent)
			{
				super(formComponent);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java
	 * @throws ParseException
	 */
	protected void specialTagHandling(String tagText, final int openBracketIndex,
		int closeBracketIndex) throws ParseException
	{
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
		}

		protected final void invoke(WebResponse response)
		{
			action.accept(response);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
		}

		protected final ActionType getType()
		{
			return type;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
		DATA;

		protected final Action action(Consumer<WebResponse> action) {
			return new Action(this, action);
		}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/LibrarySession.java`
#### Snippet
```java
	 *            The current request object
	 */
	protected LibrarySession(Request request)
	{
		super(request);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-examples/src/main/java/org/apache/wicket/examples/authentication1/SignInSession.java`
#### Snippet
```java
	 * @param request
	 */
	protected SignInSession(Request request)
	{
		super(request);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `wicket-examples/src/main/java/org/apache/wicket/examples/authentication2/SignIn2Session.java`
#### Snippet
```java
	 *            The current request object
	 */
	protected SignIn2Session(Request request)
	{
		super(request);
```

## RuleId[id=RegExpUnexpectedAnchor]
### RegExpUnexpectedAnchor
Anchor `^` in unexpected position
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java

	/** Opening a conditional comment section, which is NOT treated as a comment section */
	public static final Pattern CONDITIONAL_COMMENT_OPENING = Pattern.compile("(s?)^[^>]*?<!--\\[if.*?\\]>(-->)?(<!.*?-->)?");

	private static final Pattern PRE_BLOCK = Pattern.compile("<pre>.*?</pre>", Pattern.DOTALL | Pattern.MULTILINE);
```

## RuleId[id=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `in` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
        markFbIndex = fbIndex;
        markedAtStart = firstBytes == null;
        in.mark(readlimit);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `in` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
        }

        in.reset();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `in` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/io/ProxyInputStream.java`
#### Snippet
```java
    @Override
    public synchronized void mark(final int readlimit) {
        in.mark(readlimit);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `in` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/io/ProxyInputStream.java`
#### Snippet
```java
    public synchronized void reset() throws IOException {
        try {
            in.reset();
        } catch (final IOException e) {
            handleIOException(e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `deltas_` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Revision.java`
#### Snippet
```java
			throw new IllegalArgumentException("new delta is null");
		}
		deltas_.add(delta);
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `deltas_` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Revision.java`
#### Snippet
```java
	public synchronized void applyTo(final List<Object> target) throws PatchFailedException
	{
		ListIterator<Delta> i = deltas_.listIterator(deltas_.size());
		while (i.hasPrevious())
		{
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `deltas_` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Revision.java`
#### Snippet
```java
	public synchronized void applyTo(final List<Object> target) throws PatchFailedException
	{
		ListIterator<Delta> i = deltas_.listIterator(deltas_.size());
		while (i.hasPrevious())
		{
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `deltas_` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Revision.java`
#### Snippet
```java
			throw new IllegalArgumentException("new delta is null");
		}
		deltas_.add(0, delta);
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `deltas_` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Revision.java`
#### Snippet
```java
	public synchronized void toRCSString(final StringBuilder s, final String EOL)
	{
		for (Delta deltas : deltas_)
		{
			deltas.toRCSString(s, EOL);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `deltas_` accessed in synchronized context
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Revision.java`
#### Snippet
```java
	public synchronized void toString(final StringBuilder s)
	{
		for (Delta delta : deltas_)
		{
			delta.toString(s);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `removedValue` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/StoredResponsesMap.java`
#### Snippet
```java
				if (lifetime.compareTo(elapsedTime) <= 0)
				{
					removedValue = value.response;
					removed = true;
				}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
		public synchronized IManageablePage remove(int pageId)
		{
			Iterator<IManageablePage> iterator = pages.iterator();
			while (iterator.hasNext())
			{
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
			remove(page.getPageId());

			pages.add(page);
		}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
		public synchronized IManageablePage get(int pageId)
		{
			for (final IManageablePage page : pages)
			{
				if (page.getPageId() == pageId)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
			super.add(page);

			while (pages.size() > maxPages)
			{
				removeOldest();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
		public synchronized IManageablePage get(int id)
		{
			for (int p = 0; p < pages.size(); p++)
			{
				IManageablePage candidate = pages.get(p);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
			for (int p = 0; p < pages.size(); p++)
			{
				IManageablePage candidate = pages.get(p);

				if (candidate.getPageId() == id)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serializer` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
				if (candidate.getPageId() == id)
				{
					if (candidate instanceof SerializedPage && serializer != null)
					{
						candidate = (IManageablePage)serializer
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serializer` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
					if (candidate instanceof SerializedPage && serializer != null)
					{
						candidate = (IManageablePage)serializer
							.deserialize(((SerializedPage)candidate).getData());

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
							.deserialize(((SerializedPage)candidate).getData());

						pages.set(p, candidate);
					}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
		public synchronized IManageablePage remove(int pageId)
		{
			Iterator<IManageablePage> iterator = pages.iterator();
			while (iterator.hasNext())
			{
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
			super.add(page);

			while (pages.size() > maxPages)
			{
				removeOldest();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
			remove(page.getPageId());

			pages.add(page);
		}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
		protected synchronized void removeOldest()
		{
			IManageablePage page = pages.get(0);

			remove(page.getPageId());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pages` accessed in synchronized context
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
		public synchronized void removeAll()
		{
			pages.clear();
		}

```

## RuleId[id=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/FilteringHeaderResponse.java`
#### Snippet
```java
	 */
	@SuppressWarnings("resource")
	public final CharSequence getContent(String filterName)
	{
		if (filterName == null || !responseFilterMap.containsKey(filterName))
```

### RedundantSuppression
Redundant suppression
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java
			@Override
			@SuppressWarnings("unchecked")
			public Enumeration<String> getInitParameterNames()
			{
				return WicketServlet.this.getInitParameterNames();
```

### RedundantSuppression
Redundant suppression
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
	@SuppressWarnings("unchecked")
	@Override
	public List<String> getHeaders(String name)
	{
		List<String> result = new ArrayList<>();
```

### RedundantSuppression
Redundant suppression
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
		IRequestParameters queryParams = getQueryParameters();

		@SuppressWarnings("unchecked")
		Map<String, String[]> params = getContainerRequest().getParameterMap();
		for (Map.Entry<String, String[]> param : params.entrySet())
		{
```

### RedundantSuppression
Redundant suppression
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	public XForwardedRequestWrapper(final HttpServletRequest request)
	{
		super(request);
```

### RedundantSuppression
Redundant suppression
in `wicket-core/src/main/java/org/apache/wicket/session/HttpSessionStore.java`
#### Snippet
```java
		if (httpSession != null)
		{
			@SuppressWarnings("unchecked")
			final Enumeration<String> names = httpSession.getAttributeNames();
			final String prefix = getSessionAttributePrefix(request);
			while (names.hasMoreElements())
```

### RedundantSuppression
Redundant suppression
in `wicket-core/src/main/java/org/apache/wicket/settings/SecuritySettings.java`
#### Snippet
```java
	 */
	@SuppressWarnings("deprecation")
	public IAuthenticationStrategy getAuthenticationStrategy()
	{
		if (authenticationStrategy == null)
```

### RedundantSuppression
Redundant suppression
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LazyLoadingPage.java`
#### Snippet
```java

@SuppressWarnings({ "javadoc", "serial" })
public class LazyLoadingPage extends BasePage
{
	private Random r = new Random();
```

### RedundantSuppression
Redundant suppression
in `wicket-spring/src/main/java/org/apache/wicket/spring/test/ApplicationContextMock.java`
#### Snippet
```java
	@Override
	@SuppressWarnings({ "unchecked" })
	public <T> T getBean(String name, Class<T> requiredType) throws BeansException
	{
		return beanFactory.getBean(name, requiredType);
```

### RedundantSuppression
Redundant suppression
in `wicket-spring/src/main/java/org/apache/wicket/spring/test/ApplicationContextMock.java`
#### Snippet
```java
	@Override
	@SuppressWarnings({ "unchecked" })
	public <T> Map<String, T> getBeansOfType(Class<T> type) throws BeansException
	{
		return beanFactory.getBeansOfType(type);
```

## RuleId[id=UtilityClassWithPublicConstructor]
### UtilityClassWithPublicConstructor
Class `IOUtils` has only 'static' members, and a 'public' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
 * @author Gareth Davis
 */
public final class IOUtils
{
	private static final Logger log = LoggerFactory.getLogger(IOUtils.class);
```

### UtilityClassWithPublicConstructor
Class `StringEscapeUtils` has only 'static' members, and a 'public' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringEscapeUtils.java`
#### Snippet
```java
 */
// Copy from commons-lang ver. 2.6. Non-html/xml methods were removed
class StringEscapeUtils
{

```

### UtilityClassWithPublicConstructor
Class `CharSetUtil` has only 'static' members, and a 'public' constructor
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetUtil.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class CharSetUtil
{
	/** Logging */
```

### UtilityClassWithPublicConstructor
Class `TagUtils` has only 'static' members, and a 'public' constructor
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class TagUtils
{
	private static final String DEFAULT_ATTRIBUTE_SEPARATOR = "; ";
```

## RuleId[id=Java9ReflectionClassVisibility]
### Java9ReflectionClassVisibility
The module 'java.base' does not export the package 'com.sun.crypto.provider' to the module 'org.apache.wicket.core'
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/KeyInSessionSunJceCryptFactory.java`
#### Snippet
```java
			{
				// Initialize and add a security provider required for encryption
				final Class<?> clazz = Class.forName("com.sun.crypto.provider.SunJCE");

				final Provider provider = (Provider) clazz.getDeclaredConstructor().newInstance();
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
			for (int i = count; i > 0; i--)
			{
				if ((i == count) || (i <= 1))
				{
					base.insert(0, '_');
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
			{
				String ipSegment = matchIPV4Pat.group(i);
				if (ipSegment == null || ipSegment.length() <= 0)
				{
					return false;
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'target'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
	 *            optional Ajax request handler
	 */
	protected void onContentLoaded(T content, Optional<AjaxRequestTarget> target)
	{
	}
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'target'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/tabs/AjaxTabbedPanel.java`
#### Snippet
```java
	 *            ajax target used to update this component
	 */
	protected void onAjaxUpdate(final Optional<AjaxRequestTarget> target)
	{
	}
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'target'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/StyledLinkLabel.java`
#### Snippet
```java
	 * @see #isClickable()
	 */
	protected void onClick(Optional<AjaxRequestTarget> target)
	{
	}
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'target'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	 * @param target
	 */
	protected abstract void onRated(int rating, Optional<AjaxRequestTarget> target);
}

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'target'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxFallbackLink.java`
#### Snippet
```java
	 *            ajax target if this linked was invoked using ajax, null otherwise
	 */
	public abstract void onClick(final Optional<AjaxRequestTarget> target);

	/**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'target'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	 *            ajax target if this linked was invoked using ajax, null otherwise
	 */
	protected void onAfterSubmit(final Optional<AjaxRequestTarget> target)
	{
	}
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'target'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	 *            otherwise
	 */
	protected void onSubmit(final Optional<AjaxRequestTarget> target)
	{
	}
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'target'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	 * @param target
	 */
	protected void onError(Optional<AjaxRequestTarget> target)
	{
	}
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'targetOptional'
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java
		 *            the request target.
		 */
		void onShowForm(Optional<AjaxRequestTarget> targetOptional)
		{
			// toggle the visibility
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'targetOptional'
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java
		}

		void onRemoveCompletedTodos(Optional<AjaxRequestTarget> targetOptional)
		{
			List<TodoItem> ready = new ArrayList<>();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'targetOptional'
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/SelectableFolderContent.java`
#### Snippet
```java
	}

	protected void select(Foo foo, AbstractTree<Foo> tree, final Optional<AjaxRequestTarget> targetOptional)
	{
		if (selected != null)
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'targetOptional'
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MultiSelectableFolderContent.java`
#### Snippet
```java
	}

	protected void toggle(Foo foo, AbstractTree<Foo> tree, final Optional<AjaxRequestTarget> targetOptional)
	{
		if (isSelected(foo))
```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
getCharset() can be used instead
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java
			try
			{
				bytes = getString().getBytes(getCharset().name());
			}
			catch (UnsupportedEncodingException e)
```

### CharsetObjectCanBeUsed
charset can be used instead
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
			try
			{
				return string.getBytes(charset.name()).length;
			}
			catch (UnsupportedEncodingException e)
```

### CharsetObjectCanBeUsed
charset can be used instead
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
                                }

				return new String(bytes, charset.name());
			}
			finally
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `wicket-examples/src/main/java/org/apache/wicket/examples/source/SourcesPage.java`
#### Snippet
```java
						ByteArrayOutputStream output = new ByteArrayOutputStream();
						renderer.highlight(source, resourceAsStream, output, "UTF-8", true);
						return output.toString("UTF-8");
					}
				}
```

## RuleId[id=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `wicket-util/src/main/java/org/apache/wicket/util/license/AbstractLicenseHeaderHandler.java`
#### Snippet
```java
	public boolean addLicenseHeader(final File file)
	{
		System.out.println("Not supported yet.");
		return false;
	}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
			}

			System.out.println(failString);
			throw new AssertionError(failString.toString());
		}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
	protected void outputDevelopmentModeWarning()
	{
		System.err.print("********************************************************************\n"
			+ "*** WARNING: Wicket is running in DEVELOPMENT mode.              ***\n"
			+ "***                               ^^^^^^^^^^^                    ***\n"
```

## RuleId[id=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractNumberConverter.java`
#### Snippet
```java
			// Convert spaces to no-break space (groupingSeparator) as required by Java formats:
			// http://bugs.sun.com/view_bug.do?bug_id=4510618
			value = ((String)value).replaceAll("(\\d+)\\s(?=\\d)", "$1" + groupingSeparator);
		}

```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
	{
		// the regex means "doesn't start with 'scheme://'"
		if ((url != null) && (url.startsWith("/") == false) && (!url.matches("^\\w+\\:\\/\\/.*")) &&
			!(url.startsWith("#")))
		{
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/aspects/request/WicketFilterRequestCycleUrlAspect.java`
#### Snippet
```java
				String replacedUrl = requestUrl.replace('/', '_');
				replacedUrl = replacedUrl.replace('.', '_');
				replacedUrl = replacedUrl.replaceAll(";jsessionid=.*?(?=\\?|$)", "");
				return measureTime("core/application/request/" + replacedUrl, joinPoint, false);
			}
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/Roles.java`
#### Snippet
```java
	{
		Args.notNull(roles, "roles");
		for (final String role : roles.split("\\s*,\\s*"))
		{
			add(role);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/image/ImageUtil.java`
#### Snippet
```java
			String base64EncodedImage = Base64.getEncoder().encodeToString(bytes);
			return "data:" + resourceStream.getContentType() + ";base64," +
				(removeWhitespaces ? base64EncodedImage.replaceAll("\\s", "") : base64EncodedImage);
		}
		finally
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java
	 */
	protected final void compareMarkupWithString(IMarkupFragment markup, String testMarkup) {
		testMarkup = testMarkup.replaceAll("\r", "");
		testMarkup = testMarkup.replaceAll("\n", "");
		testMarkup = testMarkup.replaceAll("\t", "");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java
	protected final void compareMarkupWithString(IMarkupFragment markup, String testMarkup) {
		testMarkup = testMarkup.replaceAll("\r", "");
		testMarkup = testMarkup.replaceAll("\n", "");
		testMarkup = testMarkup.replaceAll("\t", "");

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java
		testMarkup = testMarkup.replaceAll("\r", "");
		testMarkup = testMarkup.replaceAll("\n", "");
		testMarkup = testMarkup.replaceAll("\t", "");

		String doc = markup.toString(true);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java

		String doc = markup.toString(true);
		doc = doc.replaceAll("\n", "");
		doc = doc.replaceAll("\r", "");
		doc = doc.replaceAll("\t", "");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java
		String doc = markup.toString(true);
		doc = doc.replaceAll("\n", "");
		doc = doc.replaceAll("\r", "");
		doc = doc.replaceAll("\t", "");
		assertEquals(doc, testMarkup);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java
		doc = doc.replaceAll("\n", "");
		doc = doc.replaceAll("\r", "");
		doc = doc.replaceAll("\t", "");
		assertEquals(doc, testMarkup);
	}
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java

		// replace all line endings with unix style line ending
		reference = reference.replaceAll("\n\r", "\n");
		reference = reference.replaceAll("\r\n", "\n");

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java
		// replace all line endings with unix style line ending
		reference = reference.replaceAll("\n\r", "\n");
		reference = reference.replaceAll("\r\n", "\n");

		// replace all line endings with unix style line ending
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java

		// replace all line endings with unix style line ending
		document = document.replaceAll("\n\r", "\n");
		document = document.replaceAll("\r\n", "\n");

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java
		// replace all line endings with unix style line ending
		document = document.replaceAll("\n\r", "\n");
		document = document.replaceAll("\r\n", "\n");

		boolean equals = compareMarkup(document, reference);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java
		}

		filename = filename.replaceAll("([/\\\\])target\\1test-classes\\1", "$1src$1test$1java$1");
		PrintWriter out = new PrintWriter(new FileOutputStream(filename));
		out.print(document);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupResourceStream.java`
#### Snippet
```java
		if (Strings.isEmpty(doctype) == false)
		{
			String doc = doctype.toString().replaceAll("[\n\r]+", "");
			doc = doc.replaceAll("\\s+", " ");
			Matcher matcher = DOCTYPE_REGEX.matcher(doc);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupResourceStream.java`
#### Snippet
```java
		{
			String doc = doctype.toString().replaceAll("[\n\r]+", "");
			doc = doc.replaceAll("\\s+", " ");
			Matcher matcher = DOCTYPE_REGEX.matcher(doc);
			if (matcher.matches() == false)
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java

			// If path starts with "*/" or "**/"
			regex = regex.replaceAll("^\\*" + PATH_SEPARATOR, "[^" + PATH_SEPARATOR + "]+" +
				PATH_SEPARATOR);
			regex = regex.replaceAll("^[\\*]{2,}" + PATH_SEPARATOR, "([^" + PATH_SEPARATOR +
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
			regex = regex.replaceAll("^\\*" + PATH_SEPARATOR, "[^" + PATH_SEPARATOR + "]+" +
				PATH_SEPARATOR);
			regex = regex.replaceAll("^[\\*]{2,}" + PATH_SEPARATOR, "([^" + PATH_SEPARATOR +
				"].#star#" + PATH_SEPARATOR + ")?");

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java

			// Handle "/*/" and "/**/"
			regex = regex.replaceAll(PATH_SEPARATOR + "\\*" + PATH_SEPARATOR, PATH_SEPARATOR +
				"[^" + PATH_SEPARATOR + "]+" + PATH_SEPARATOR);
			regex = regex.replaceAll(PATH_SEPARATOR + "[\\*]{2,}" + PATH_SEPARATOR, "(" +
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
			regex = regex.replaceAll(PATH_SEPARATOR + "\\*" + PATH_SEPARATOR, PATH_SEPARATOR +
				"[^" + PATH_SEPARATOR + "]+" + PATH_SEPARATOR);
			regex = regex.replaceAll(PATH_SEPARATOR + "[\\*]{2,}" + PATH_SEPARATOR, "(" +
				PATH_SEPARATOR + "|" + PATH_SEPARATOR + ".+" + PATH_SEPARATOR + ")");

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java

			// Handle "*" within dir or file names
			regex = regex.replaceAll("\\*+", "[^" + PATH_SEPARATOR + "]*");

			// replace placeholder
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/PopupSettings.java`
#### Snippet
```java
		{
			// Fix for IE bug.
			windowTitle = windowTitle.replaceAll("\\W", "_");
		}

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		{
			// fixing white spaces
			rangeHeader = rangeHeader.replaceAll(" ", "");

			String range = rangeHeader.substring(rangeHeader.indexOf('=') + 1,
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java

		// strip spaces and dashes
		creditCardNumber = creditCardNumber.replaceAll("[ -]", "");

		// the length of the credit card number has to be between 12 and 19.
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java

		// strip spaces and dashes
		creditCardNumber = creditCardNumber.replaceAll("[ -]", "");

		// the length of the credit card number has to be between 12 and 19.
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/DefaultAuthenticationStrategy.java`
#### Snippet
```java
			String password = null;

			String[] values = value.split(VALUE_SEPARATOR);
			if ((values.length > 0) && (Strings.isEmpty(values[0]) == false))
			{
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java
			// as well
			URL location = locationsIterator.next();
			String clzLocation = location.getFile() + clz.getName().replaceAll("\\.", "/") +
				".class";
			log.debug("clzLocation=" + clzLocation);
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `wicket-examples/src/main/java/org/apache/wicket/examples/hangman/WordGenerator.java`
#### Snippet
```java
				".txt", false);
			final String wordlist = Streams.readString(resource.getInputStream());
			words = Arrays.asList(wordlist.split("\\s+"));
			shuffle();
		}
```

## RuleId[id=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'org.apache.wicket.Component\[\]' expected, 'org.apache.wicket.markup.html.form.FormComponent\[\]' found
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
					}
				}
				return components.toArray(new FormComponent[components.size()]);
			}
			return null;
```

## RuleId[id=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `ProxyInputStream()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/io/ProxyInputStream.java`
#### Snippet
```java
     * @param proxy  the InputStream to delegate to
     */
    public ProxyInputStream(final InputStream proxy) {
        super(proxy);
        // the proxy is stored in a protected superclass variable named 'in'
```

### NonProtectedConstructorInAbstractClass
Constructor `ThresholdingOutputStream()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/io/ThresholdingOutputStream.java`
#### Snippet
```java
	 *            The number of bytes at which to trigger an event.
	 */
	public ThresholdingOutputStream(final int threshold)
	{
		this.threshold = threshold;
```

### NonProtectedConstructorInAbstractClass
Constructor `PathNode()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/PathNode.java`
#### Snippet
```java
	 *            The previous node in the path.
	 */
	public PathNode(final int i, final int j, final PathNode prev)
	{
		this.i = i;
```

### NonProtectedConstructorInAbstractClass
Constructor `EnumeratedType()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/EnumeratedType.java`
#### Snippet
```java
	 *            Name of this enumerated type value
	 */
	public EnumeratedType(final String name)
	{
		super(name);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCrypt()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/AbstractCrypt.java`
#### Snippet
```java
	 * Constructor
	 */
	public AbstractCrypt()
	{
		this.encryptionKey = UUID.randomUUID().toString();
```

### NonProtectedConstructorInAbstractClass
Constructor `MetaPatternParser()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/parsers/MetaPatternParser.java`
#### Snippet
```java
	 *            to parse
	 */
	public MetaPatternParser(final CharSequence input)
	{
		this.input = input;
```

### NonProtectedConstructorInAbstractClass
Constructor `MetaPatternParser()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/parsers/MetaPatternParser.java`
#### Snippet
```java
	 *            Input to parse
	 */
	public MetaPatternParser(final MetaPattern pattern, final CharSequence input)
	{
		this(input);
```

### NonProtectedConstructorInAbstractClass
Constructor `VariableInterpolator()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/string/interpolator/VariableInterpolator.java`
#### Snippet
```java
	 *            a <code>String</code> to interpolate with variable values
	 */
	public VariableInterpolator(final String string)
	{
		this(string, false);
```

### NonProtectedConstructorInAbstractClass
Constructor `VariableInterpolator()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/string/interpolator/VariableInterpolator.java`
#### Snippet
```java
	 *            multiple interpolators can be chained
	 */
	public VariableInterpolator(final String string, final boolean exceptionOnNullVarValue)
	{
		this.string = string;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractLicenseHeaderHandler()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/license/AbstractLicenseHeaderHandler.java`
#### Snippet
```java
	 * @param ignoreFiles
	 */
	public AbstractLicenseHeaderHandler(final List<String> ignoreFiles)
	{
		this.ignoreFiles = ignoreFiles;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractStringResourceStream()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java
	 *            The mime type of this resource, such as "image/jpeg" or "text/html"
	 */
	public AbstractStringResourceStream(final String contentType)
	{
		// TODO null for contentType is allowed? or should the default be applied instead?
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractStringResourceStream()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java
	 * Constructor.
	 */
	public AbstractStringResourceStream()
	{
		this(DEFAULT_CONTENT_TYPE);
```

### NonProtectedConstructorInAbstractClass
Constructor `ApacheLicenseHeaderTestCase()` of an abstract class should not be declared 'public'
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
	 * Construct.
	 */
	public ApacheLicenseHeaderTestCase()
	{

```

### NonProtectedConstructorInAbstractClass
Constructor `StandardDebugPanel()` of an abstract class should not be declared 'public'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/StandardDebugPanel.java`
#### Snippet
```java
	 * @param id
	 */
	public StandardDebugPanel(final String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMapper()` of an abstract class should not be declared 'public'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/AbstractMapper.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractMapper()
	{
		super();
```

### NonProtectedConstructorInAbstractClass
Constructor `IndicatingAjaxLink()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxLink.java`
#### Snippet
```java
	 * @param model
	 */
	public IndicatingAjaxLink(final String id, final IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `IndicatingAjaxLink()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxLink.java`
#### Snippet
```java
	 * @param id
	 */
	public IndicatingAjaxLink(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxLazyLoadPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
	 * @param id
	 */
	public AjaxLazyLoadPanel(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxLazyLoadPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
	 * @param model
	 */
	public AjaxLazyLoadPanel(final String id, final IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `IndicatingAjaxFallbackLink()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxFallbackLink.java`
#### Snippet
```java
	 * @param id
	 */
	public IndicatingAjaxFallbackLink(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `IndicatingAjaxFallbackLink()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxFallbackLink.java`
#### Snippet
```java
	 * @param model
	 */
	public IndicatingAjaxFallbackLink(final String id, final IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxOrderByLink()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/sort/AjaxOrderByLink.java`
#### Snippet
```java
	 * @param stateLocator
	 */
	public AjaxOrderByLink(final String id, final S sortProperty,
	                       final ISortStateLocator<S> stateLocator)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `IndicatingAjaxButton()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxButton.java`
#### Snippet
```java
	 *            model used to set <code>value</code> markup attribute
	 */
	public IndicatingAjaxButton(final String id, final IModel<String> model)
	{
		this(id, model, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `IndicatingAjaxButton()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxButton.java`
#### Snippet
```java
	 * @param form
	 */
	public IndicatingAjaxButton(final String id, final Form<?> form)
	{
		this(id, null, form);
```

### NonProtectedConstructorInAbstractClass
Constructor `IndicatingAjaxButton()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxButton.java`
#### Snippet
```java
	 * @param id
	 */
	public IndicatingAjaxButton(final String id)
	{
		this(id, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `IndicatingAjaxButton()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxButton.java`
#### Snippet
```java
	 * @param form
	 */
	public IndicatingAjaxButton(final String id, final IModel<String> model, final Form<?> form)
	{
		super(id, model, form);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAutoCompleteBehavior()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AbstractAutoCompleteBehavior.java`
#### Snippet
```java
	 *            settings for the autocomplete list
	 */
	public AbstractAutoCompleteBehavior(AutoCompleteSettings settings)
	{
		if (settings == null)
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAutoCompleteBehavior()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AbstractAutoCompleteBehavior.java`
#### Snippet
```java
	 * Constructor that creates an default {@link AutoCompleteSettings}
	 */
	public AbstractAutoCompleteBehavior()
	{
		this(new AutoCompleteSettings());
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFallbackOrderByBorder()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/sort/AjaxFallbackOrderByBorder.java`
#### Snippet
```java
	 * @param stateLocator
	 */
	public AjaxFallbackOrderByBorder(final String id, final S sortProperty,
		final ISortStateLocator<S> stateLocator)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `DefaultCssAutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/DefaultCssAutoCompleteTextField.java`
#### Snippet
```java
	 *      the component id
	 */
	public DefaultCssAutoCompleteTextField(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `DefaultCssAutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/DefaultCssAutoCompleteTextField.java`
#### Snippet
```java
	 *      the component model
	 */
	public DefaultCssAutoCompleteTextField(final String id, final IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteBehavior()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteBehavior.java`
#### Snippet
```java
	 *            settings for the autocomplete list
	 */
	public AutoCompleteBehavior(final IAutoCompleteRenderer<T> renderer,
		final AutoCompleteSettings settings)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteBehavior()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteBehavior.java`
#### Snippet
```java
	 *            highlight/preselect the first item in the autocomplete list automatically
	 */
	public AutoCompleteBehavior(final IAutoCompleteRenderer<T> renderer, final boolean preselect)
	{
		this(renderer, new AutoCompleteSettings().setPreselect(preselect));
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteBehavior()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteBehavior.java`
#### Snippet
```java
	 *            renderer that will be used to generate output
	 */
	public AutoCompleteBehavior(final IAutoCompleteRenderer<T> renderer)
	{
		this(renderer, false);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractOptions()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/component/AbstractOptions.java`
#### Snippet
```java
	 *            parent palette
	 */
	public AbstractOptions(final String id, final Palette<T> palette)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTab()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/AbstractTab.java`
#### Snippet
```java
	 *            IModel used to represent the title of the tab. Must contain a string
	 */
	public AbstractTab(final IModel<String> title)
	{
		this.title = title;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDateTimeField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
	 * @param id
	 */
	public AbstractDateTimeField(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDateTimeField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
	 * @param model
	 */
	public AbstractDateTimeField(final String id, final IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDataGridView()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/AbstractDataGridView.java`
#### Snippet
```java
	 *            data provider
	 */
	public AbstractDataGridView(final String id,
		final List<? extends ICellPopulator<T>> populators, final IDataProvider<T> dataProvider)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/AbstractColumn.java`
#### Snippet
```java
	 *            model used to generate header text
	 */
	public AbstractColumn(final IModel<String> displayModel)
	{
		this(displayModel, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/AbstractColumn.java`
#### Snippet
```java
	 *            sort property this column represents
	 */
	public AbstractColumn(final IModel<String> displayModel, final S sortProperty)
	{
		this.displayModel = displayModel;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractToolbar()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/AbstractToolbar.java`
#### Snippet
```java
	 *            data table this toolbar will be attached to
	 */
	public AbstractToolbar(final DataTable<?, ?> table)
	{
		this(null, table);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractToolbar()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/AbstractToolbar.java`
#### Snippet
```java
	 *            data table this toolbar will be attached to
	 */
	public AbstractToolbar(final IModel<?> model, final DataTable<?, ?> table)
	{
		super(table.newToolbarId(), model);
```

### NonProtectedConstructorInAbstractClass
Constructor `HeaderlessColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/HeaderlessColumn.java`
#### Snippet
```java
	 * Construct.
	 */
	public HeaderlessColumn()
	{
		super(new Model<>("&nbsp;"));
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDataExporter()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/AbstractDataExporter.java`
#### Snippet
```java
	 *      The file name extensions to use in the file name for the exported data.
	 */
	public AbstractDataExporter(IModel<String> dataFormatNameModel, String contentType, String fileNameExtension)
	{
		this.dataFormatNameModel = dataFormatNameModel;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractExportableColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/AbstractExportableColumn.java`
#### Snippet
```java
	 * @param sortProperty The sort property used by this column.
	 */
	public AbstractExportableColumn(IModel<String> displayModel, S sortProperty)
	{
		super(displayModel, sortProperty);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractExportableColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/AbstractExportableColumn.java`
#### Snippet
```java
	 * @param displayModel The {@link IModel} of the text to be used in the column header.
	 */
	public AbstractExportableColumn(IModel<String> displayModel)
	{
		super(displayModel);
```

### NonProtectedConstructorInAbstractClass
Constructor `FilteredAbstractColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilteredAbstractColumn.java`
#### Snippet
```java
	 *            model used to display the header text of this column
	 */
	public FilteredAbstractColumn(final IModel<String> displayModel)
	{
		super(displayModel);
```

### NonProtectedConstructorInAbstractClass
Constructor `FilteredAbstractColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilteredAbstractColumn.java`
#### Snippet
```java
	 *            sort property this column represents
	 */
	public FilteredAbstractColumn(final IModel<String> displayModel, final S sortProperty)
	{
		super(displayModel, sortProperty);
```

### NonProtectedConstructorInAbstractClass
Constructor `FilteredPropertyColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilteredPropertyColumn.java`
#### Snippet
```java
	 *            wicket property expression for the column, see {@link PropertyModel} for details
	 */
	public FilteredPropertyColumn(final IModel<String> displayModel, final S sortProperty,
		final String propertyExpression)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `FilteredPropertyColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilteredPropertyColumn.java`
#### Snippet
```java
	 *            wicket property expression for the column, see {@link PropertyModel} for details
	 */
	public FilteredPropertyColumn(final IModel<String> displayModel, final String propertyExpression)
	{
		super(displayModel, propertyExpression);
```

### NonProtectedConstructorInAbstractClass
Constructor `Node()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/Node.java`
#### Snippet
```java
	 *            the model for this node
	 */
	public Node(String id, AbstractTree<T> tree, IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `NestedTree()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/NestedTree.java`
#### Snippet
```java
	 *            the provider of the tree
	 */
	public NestedTree(String id, ITreeProvider<T> provider)
	{
		this(id, provider, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `NestedTree()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/NestedTree.java`
#### Snippet
```java
	 * @see org.apache.wicket.extensions.markup.html.repeater.tree.AbstractTree.State
	 */
	public NestedTree(String id, ITreeProvider<T> provider, IModel<? extends Set<T>> state)
	{
		super(id, provider, state);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTreeColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/table/AbstractTreeColumn.java`
#### Snippet
```java
	 *            sort property this column represents
	 */
	public AbstractTreeColumn(IModel<String> displayModel, S sortProperty)
	{
		super(displayModel, sortProperty);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTreeColumn()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/table/AbstractTreeColumn.java`
#### Snippet
```java
	 *            model used to generate header text
	 */
	public AbstractTreeColumn(IModel<String> displayModel)
	{
		super(displayModel);
```

### NonProtectedConstructorInAbstractClass
Constructor `TableTree()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/TableTree.java`
#### Snippet
```java
	 *            number of rows per page
	 */
	public TableTree(final String id, final List<? extends IColumn<T, S>> columns,
		final ITreeProvider<T> dataProvider, final long rowsPerPage)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `TableTree()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/TableTree.java`
#### Snippet
```java
	 *            the expansion state
	 */
	public TableTree(final String id, final List<? extends IColumn<T, S>> columns,
		final ITreeProvider<T> provider, final long rowsPerPage, IModel<? extends Set<T>> state)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `TreeModelProvider()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/TreeModelProvider.java`
#### Snippet
```java
	 *            model to wrap
	 */
	public TreeModelProvider(TreeModel treeModel)
	{
		this(treeModel, true);
```

### NonProtectedConstructorInAbstractClass
Constructor `TreeModelProvider()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/TreeModelProvider.java`
#### Snippet
```java
	 *            should the root be visible
	 */
	public TreeModelProvider(TreeModel treeModel, boolean rootVisible)
	{
		this.treeModel = treeModel;
```

### NonProtectedConstructorInAbstractClass
Constructor `StyledLinkLabel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/StyledLinkLabel.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	public StyledLinkLabel(String id, IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWizardModel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/AbstractWizardModel.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractWizardModel()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `WizardButton()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardButton.java`
#### Snippet
```java
	 *            The button's label
	 */
	public WizardButton(final String id, final IWizard wizard, final IModel<String> label)
	{
		super(id, label);
```

### NonProtectedConstructorInAbstractClass
Constructor `BreadCrumbLink()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbLink.java`
#### Snippet
```java
	 *            The bread crumb model
	 */
	public BreadCrumbLink(final String id, final IBreadCrumbModel breadCrumbModel)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `RatingPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	 *            should this component render its own default CSS style?
	 */
	public RatingPanel(final String id, final IModel<? extends Number> rating, final int nrOfStars,
		final boolean addDefaultCssStyle)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `RatingPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	 *            the model to get the rating
	 */
	public RatingPanel(final String id, final IModel<? extends Number> rating)
	{
		this(id, rating, new Model<Integer>(5), null, new Model<>(Boolean.FALSE), true);
```

### NonProtectedConstructorInAbstractClass
Constructor `RatingPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	 *            the component id.
	 */
	public RatingPanel(final String id)
	{
		this(id, null, 5, true);
```

### NonProtectedConstructorInAbstractClass
Constructor `RatingPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	 *            should this component render its own default CSS style?
	 */
	public RatingPanel(final String id, final IModel<? extends Number> rating,
		final IModel<Integer> nrOfStars, final IModel<Integer> nrOfVotes,
		final IModel<Boolean> hasVoted, final boolean addDefaultCssStyle)
```

### NonProtectedConstructorInAbstractClass
Constructor `RatingPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	 *            the number of stars to display
	 */
	public RatingPanel(final String id, final int nrOfStars)
	{
		this(id, null, nrOfStars, true);
```

### NonProtectedConstructorInAbstractClass
Constructor `BreadCrumbPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanel.java`
#### Snippet
```java
	 *            The model
	 */
	public BreadCrumbPanel(final String id, final IBreadCrumbModel breadCrumbModel,
		final IModel<?> model)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `BreadCrumbPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanel.java`
#### Snippet
```java
	 *            The bread crumb model
	 */
	public BreadCrumbPanel(final String id, final IBreadCrumbModel breadCrumbModel)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `RatingPanel()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	 *            should this component render its own default CSS style?
	 */
	public RatingPanel(final String id, final IModel<? extends Number> rating, final int nrOfStars,
		final IModel<Integer> nrOfVotes, final boolean addDefaultCssStyle)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `BreadCrumbParticipantDelegate()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java
	 * @param component
	 */
	public BreadCrumbParticipantDelegate(final Component component)
	{
		if (component == null)
```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicWizardStep()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java
	 *            Any model which is to be used for this step
	 */
	public DynamicWizardStep(final IDynamicWizardStep previousStep, final String title,
		final String summary, final IModel<?> model)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicWizardStep()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java
	 *            a brief summary of this step or some usage guidelines.
	 */
	public DynamicWizardStep(final IDynamicWizardStep previousStep, final String title,
		final String summary)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicWizardStep()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java
	 *            Any model which is to be used for this step
	 */
	public DynamicWizardStep(final IDynamicWizardStep previousStep, final IModel<String> title,
		final IModel<String> summary, final IModel<?> model)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicWizardStep()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java
	 *            The previous step. May be null if this is the first step in the wizard
	 */
	public DynamicWizardStep(final IDynamicWizardStep previousStep)
	{
		super();
```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicWizardStep()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java
	 *            a brief summary of this step or some usage guidelines.
	 */
	public DynamicWizardStep(final IDynamicWizardStep previousStep, final IModel<String> title,
		final IModel<String> summary)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            renderer for autocomplete
	 */
	public AutoCompleteTextField(final String id, final Class<T> type,
		final IAutoCompleteRenderer<T> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            renderer for autocomplete
	 */
	public AutoCompleteTextField(final String id, final IAutoCompleteRenderer<T> renderer)
	{
		this(id, (IModel<T>)null, renderer);
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            model
	 */
	public AutoCompleteTextField(final String id, final IModel<T> model)
	{
		this(id, model, null, new AutoCompleteSettings());
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            settings for autocomplete
	 */
	public AutoCompleteTextField(final String id, final IModel<T> model,
		final AutoCompleteSettings settings)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            settings for autocomplete
	 */
	public AutoCompleteTextField(final String id, final AutoCompleteSettings settings)
	{
		this(id, null, settings);
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            settings for autocomplete
	 */
	public AutoCompleteTextField(final String id, final IModel<T> model, final Class<T> type,
		final IAutoCompleteRenderer<T> renderer, final AutoCompleteSettings settings)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            component id
	 */
	public AutoCompleteTextField(final String id)
	{
		this(id, null, new AutoCompleteSettings());
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            renderer for autocomplete
	 */
	public AutoCompleteTextField(final String id, final IModel<T> model,
		final IAutoCompleteRenderer<T> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	public AutoCompleteTextField(final String id, final IModel<T> model, final Class<T> type,
		final AutoCompleteSettings settings)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AutoCompleteTextField()` of an abstract class should not be declared 'public'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	 *            model objec type
	 */
	public AutoCompleteTextField(final String id, final Class<T> type)
	{
		this(id, null, type, new AutoCompleteSettings());
```

### NonProtectedConstructorInAbstractClass
Constructor `LimitedInputStream()` of an abstract class should not be declared 'public'
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/LimitedInputStream.java`
#### Snippet
```java
     *   shall be returned by the source stream.
     */
    public LimitedInputStream(final InputStream inputStream, final long sizeMax) {
        super(inputStream);
        this.sizeMax = sizeMax;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRoleAuthorizationStrategy()` of an abstract class should not be declared 'public'
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/AbstractRoleAuthorizationStrategy.java`
#### Snippet
```java
	 *            the authorizer delegate
	 */
	public AbstractRoleAuthorizationStrategy(IRoleCheckingStrategy roleCheckingStrategy)
	{
		Args.notNull(roleCheckingStrategy, "roleCheckingStrategy");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAuthenticatedWebSession()` of an abstract class should not be declared 'public'
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/AbstractAuthenticatedWebSession.java`
#### Snippet
```java
	 *            The current request object
	 */
	public AbstractAuthenticatedWebSession(final Request request)
	{
		super(request);
```

### NonProtectedConstructorInAbstractClass
Constructor `AuthenticatedWebSession()` of an abstract class should not be declared 'public'
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/AuthenticatedWebSession.java`
#### Snippet
```java
	 *            The current request object
	 */
	public AuthenticatedWebSession(Request request)
	{
		super(request);
```

### NonProtectedConstructorInAbstractClass
Constructor `MetaDataKey()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java
	 * Constructor.
	 */
	public MetaDataKey()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `ClassAttributeModifier()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ClassAttributeModifier.java`
#### Snippet
```java
	 * Constructor.
	 */
	public ClassAttributeModifier()
	{
		super("class", null, " ");
```

### NonProtectedConstructorInAbstractClass
Constructor `Application()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
	 * overriding the constructor.</strong>
	 */
	public Application()
	{
		// Install default component instantiation listener that uses
```

### NonProtectedConstructorInAbstractClass
Constructor `StyleAttributeModifier()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/StyleAttributeModifier.java`
#### Snippet
```java
	 * Constructor.
	 */
	public StyleAttributeModifier()
	{
		super("style", null, ";");
```

### NonProtectedConstructorInAbstractClass
Constructor `Session()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	 *            The current request
	 */
	public Session(Request request)
	{
		Locale locale = request.getLocale();
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxEventBehavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxEventBehavior.java`
#### Snippet
```java
	 *      the event this behavior will be attached to
	 */
	public AjaxEventBehavior(String event)
	{
		Args.notEmpty(event, "event");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAjaxTimerBehavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractAjaxTimerBehavior.java`
#### Snippet
```java
	 *            {@link Duration} between AJAX callbacks
	 */
	public AbstractAjaxTimerBehavior(final Duration updateInterval)
	{
		setUpdateInterval(updateInterval);
```

### NonProtectedConstructorInAbstractClass
Constructor `OnChangeAjaxBehavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/OnChangeAjaxBehavior.java`
#### Snippet
```java
	 * Constructor.
	 */
	public OnChangeAjaxBehavior()
	{
		super(EVENT_NAME);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFormComponentUpdatingBehavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormComponentUpdatingBehavior.java`
#### Snippet
```java
	 *            event to trigger this behavior
	 */
	public AjaxFormComponentUpdatingBehavior(final String event)
	{
		super(event);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFormChoiceComponentUpdatingBehavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
	 * Construct.
	 */
	public AjaxFormChoiceComponentUpdatingBehavior()
	{
		super("change");
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFormSubmitBehavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java
	 *            javascript event this behavior is attached to, like onclick
	 */
	public AjaxFormSubmitBehavior(Form<?> form, String event)
	{
		super(event);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFormSubmitBehavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java
	 *            javascript event this behavior is attached to, like onclick
	 */
	public AjaxFormSubmitBehavior(String event)
	{
		this(null, event);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxLink.java`
#### Snippet
```java
	 * @param id
	 */
	public AjaxLink(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxLink.java`
#### Snippet
```java
	 * @param model
	 */
	public AjaxLink(final String id, final IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxSubmitLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxSubmitLink.java`
#### Snippet
```java
	 * @param id
	 */
	public AjaxSubmitLink(String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxSubmitLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxSubmitLink.java`
#### Snippet
```java
	 * @param form
	 */
	public AjaxSubmitLink(String id, final Form<?> form)
	{
		super(id, form);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFallbackLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxFallbackLink.java`
#### Snippet
```java
	 * @param id
	 */
	public AjaxFallbackLink(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFallbackLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxFallbackLink.java`
#### Snippet
```java
	 * @param model
	 */
	public AjaxFallbackLink(final String id, final IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxCheckBox()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxCheckBox.java`
#### Snippet
```java
	 * @param id
	 */
	public AjaxCheckBox(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxCheckBox()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxCheckBox.java`
#### Snippet
```java
	 * @param model
	 */
	public AjaxCheckBox(final String id, final IModel<Boolean> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFallbackButton()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	 * @param form
	 */
	public AjaxFallbackButton(String id, Form<?> form)
	{
		this(id, null, form);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxFallbackButton()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	 * @param form
	 */
	public AjaxFallbackButton(String id, IModel<String> model, Form<?> form)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxButton()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxButton.java`
#### Snippet
```java
	 *            model used to set <code>value</code> markup attribute
	 */
	public AjaxButton(String id, IModel<String> model)
	{
		this(id, model, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxButton()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxButton.java`
#### Snippet
```java
	 * @param form
	 */
	public AjaxButton(String id, IModel<String> model, final Form<?> form)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxButton()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxButton.java`
#### Snippet
```java
	 * @param form
	 */
	public AjaxButton(String id, Form<?> form)
	{
		this(id, null, form);
```

### NonProtectedConstructorInAbstractClass
Constructor `AjaxButton()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxButton.java`
#### Snippet
```java
	 * @param id
	 */
	public AjaxButton(String id)
	{
		this(id, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `ClientInfo()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/core/request/ClientInfo.java`
#### Snippet
```java
	 * Construct.
	 */
	public ClientInfo()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractComponentMapper()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractComponentMapper.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractComponentMapper()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `MarkupContainer()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	private Object children;

	public MarkupContainer(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `MarkupContainer()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	}

	public MarkupContainer(final String id, IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBookmarkableMapper()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
	}

	public AbstractBookmarkableMapper(String mountPath, IPageParametersEncoder pageParametersEncoder)
	{
		Args.notEmpty(mountPath, "mountPath");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBookmarkableMapper()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractBookmarkableMapper()
	{
		this("notUsed", new PageParametersEncoder());
```

### NonProtectedConstructorInAbstractClass
Constructor `Component()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 *             Thrown if the component has been given a null id.
	 */
	public Component(final String id, final IModel<?> model)
	{
		checkId(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `Component()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 *             Thrown if the component has been given a null id.
	 */
	public Component(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `TextTemplate()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplate.java`
#### Snippet
```java
	 * Constructor.
	 */
	public TextTemplate()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `TextTemplate()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplate.java`
#### Snippet
```java
	 *            <code>text/html</code>"
	 */
	public TextTemplate(String contentType)
	{
		super(contentType);
```

### NonProtectedConstructorInAbstractClass
Constructor `TextTemplateDecorator()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplateDecorator.java`
#### Snippet
```java
	 *            a <code>TextTemplate</code> to decorate
	 */
	public TextTemplateDecorator(TextTemplate textTemplate)
	{
		Args.notNull(textTemplate, "textTemplate");
```

### NonProtectedConstructorInAbstractClass
Constructor `PartialPageUpdate()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
	 *      the page which components are being updated.
	 */
	public PartialPageUpdate(final Page page)
	{
		this.page = page;
```

### NonProtectedConstructorInAbstractClass
Constructor `LoadableDetachableModel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/model/LoadableDetachableModel.java`
#### Snippet
```java
	 *            retrieved instance of the detachable object
	 */
	public LoadableDetachableModel(T object)
	{
		this.transientModelObject = object;
```

### NonProtectedConstructorInAbstractClass
Constructor `LoadableDetachableModel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/model/LoadableDetachableModel.java`
#### Snippet
```java
	 * model.
	 */
	public LoadableDetachableModel()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPropertyModel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
	 *            The nested model object
	 */
	public AbstractPropertyModel(final Object modelObject)
	{
		super(modelObject);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMarkupParser()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
	 *            The markup resource (file)
	 */
	public AbstractMarkupParser(final MarkupResourceStream resource)
	{
		this(new XmlPullParser(), resource);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMarkupParser()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
	 *            The markup resource (file)
	 */
	public AbstractMarkupParser(final IXmlPullParser xmlParser, final MarkupResourceStream resource)
	{
		this.xmlParser = xmlParser;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMarkupParser()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
	 *            The markup resource.
	 */
	public AbstractMarkupParser(final String markup)
	{
		this(new XmlPullParser(), new MarkupResourceStream(new StringResourceStream(markup)));
```

### NonProtectedConstructorInAbstractClass
Constructor `MarkupElement()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupElement.java`
#### Snippet
```java
	 * Constructor.
	 */
	public MarkupElement()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCssReferenceHeaderItem()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/AbstractCssReferenceHeaderItem.java`
#### Snippet
```java
	private String integrity;

	public AbstractCssReferenceHeaderItem(String media, String rel)
	{
		this.media = media;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHeaderResponseFilter()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/AbstractHeaderResponseFilter.java`
#### Snippet
```java
	 * @param name
	 */
	public AbstractHeaderResponseFilter(String name)
	{
		this.name = name;
```

### NonProtectedConstructorInAbstractClass
Constructor `SubresourceHeaderResponse()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/SubresourceHeaderResponse.java`
#### Snippet
```java
{

	public SubresourceHeaderResponse(IHeaderResponse real)
	{
		super(real);
```

### NonProtectedConstructorInAbstractClass
Constructor `DecoratingHeaderResponse()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/DecoratingHeaderResponse.java`
#### Snippet
```java
	 *            the actual response that this class delegates to by default
	 */
	public DecoratingHeaderResponse(IHeaderResponse real)
	{
		realResponse = real;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCheckSelector()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractCheckSelector.java`
#### Snippet
```java
	 *            the component id
	 */
	public AbstractCheckSelector(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `FormComponentPanel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentPanel.java`
#### Snippet
```java
	 *          The component id
	 */
	public FormComponentPanel(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `FormComponentPanel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentPanel.java`
#### Snippet
```java
	 *          The component model
	 */
	public FormComponentPanel(String id, IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSubmitLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSubmitLink.java`
#### Snippet
```java
	 * @param form
	 */
	public AbstractSubmitLink(String id, IModel<?> model, Form<?> form)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSubmitLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSubmitLink.java`
#### Snippet
```java
	 * @param form
	 */
	public AbstractSubmitLink(String id, Form<?> form)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSubmitLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSubmitLink.java`
#### Snippet
```java
	 * @param id
	 */
	public AbstractSubmitLink(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSubmitLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSubmitLink.java`
#### Snippet
```java
	 * @param model
	 */
	public AbstractSubmitLink(String id, IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `LabeledWebMarkupContainer()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/LabeledWebMarkupContainer.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public LabeledWebMarkupContainer(final String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `LabeledWebMarkupContainer()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/LabeledWebMarkupContainer.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public LabeledWebMarkupContainer(final String id, final IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTextComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public AbstractTextComponent(String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTextComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public AbstractTextComponent(final String id, final IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 */
	public AbstractSingleSelectChoice(String id, IModel<? extends List<? extends T>> choices,
		IChoiceRenderer<? super T> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 */
	public AbstractSingleSelectChoice(final String id, IModel<T> model,
		final List<? extends T> choices, final IChoiceRenderer<? super T> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractSingleSelectChoice(final String id, IModel<T> model,
		final List<? extends T> choices)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractSingleSelectChoice(final String id, final List<? extends T> choices)
	{
		super(id, choices);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 */
	public AbstractSingleSelectChoice(String id, IModel<T> model,
		IModel<? extends List<? extends T>> choices, IChoiceRenderer<? super T> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            The drop down choices
	 */
	public AbstractSingleSelectChoice(String id, IModel<T> model,
		IModel<? extends List<? extends T>> choices)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractSingleSelectChoice(String id, IModel<? extends List<? extends T>> choices)
	{
		super(id, choices);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            See Component
	 */
	public AbstractSingleSelectChoice(final String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSingleSelectChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractSingleSelectChoice(final String id, final List<? extends T> choices,
		final IChoiceRenderer<? super T> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `FormComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public FormComponent(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `FormComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public FormComponent(final String id, IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `FileUploadToResourceField()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/FileUploadToResourceField.java`
#### Snippet
```java
    private Bytes maxSize = null;

    public FileUploadToResourceField(String id)
    {
        super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractFileUploadResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/AbstractFileUploadResource.java`
#### Snippet
```java
	private final IUploadsFileManager fileManager;

	public AbstractFileUploadResource(IUploadsFileManager fileManager)
	{
		this.fileManager = fileManager;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/AbstractLink.java`
#### Snippet
```java
	 *            the link's model
	 */
	public AbstractLink(String id, IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/AbstractLink.java`
#### Snippet
```java
	 * @param id
	 */
	public AbstractLink(String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `StatelessLink()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/StatelessLink.java`
#### Snippet
```java
	 * @param id
	 */
	public StatelessLink(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `Link()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public Link(final String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `Link()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Link(final String id, IModel<T> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `Loop()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Loop(final String id, final IModel<Integer> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `Loop()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Loop(final String id, final int iterations)
	{
		super(id, new Model<Integer>(iterations));
```

### NonProtectedConstructorInAbstractClass
Constructor `PropertyListView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PropertyListView.java`
#### Snippet
```java
	 *            Wicket id
	 */
	public PropertyListView(final String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `PropertyListView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PropertyListView.java`
#### Snippet
```java
	 *            wrapping a List
	 */
	public PropertyListView(final String id, final IModel<? extends List<T>> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `PropertyListView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PropertyListView.java`
#### Snippet
```java
	 *            unmodeled List
	 */
	public PropertyListView(final String id, final List<T> list)
	{
		super(id, list);
```

### NonProtectedConstructorInAbstractClass
Constructor `PageableListView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java
	 * @see ListView#ListView(String, List)
	 */
	public PageableListView(final String id, final List<T> list, final long itemsPerPage)
	{
		super(id, list);
```

### NonProtectedConstructorInAbstractClass
Constructor `PageableListView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java
	 *            Number of rows to show on a page
	 */
	public PageableListView(final String id, final IModel<? extends List<T>> model,
		long itemsPerPage)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `BlobImageResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/BlobImageResource.java`
#### Snippet
```java
	 * Construct.
	 */
	public BlobImageResource()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `BlobImageResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/BlobImageResource.java`
#### Snippet
```java
	 * @param format
	 */
	public BlobImageResource(String format)
	{
		super(format);
```

### NonProtectedConstructorInAbstractClass
Constructor `RenderedDynamicImageResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/RenderedDynamicImageResource.java`
#### Snippet
```java
	 *            Height of image
	 */
	public RenderedDynamicImageResource(final int width, final int height)
	{
		this.width = width;
```

### NonProtectedConstructorInAbstractClass
Constructor `RenderedDynamicImageResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/RenderedDynamicImageResource.java`
#### Snippet
```java
	 *            The format of the image (jpg, png or gif)
	 */
	public RenderedDynamicImageResource(final int width, final int height, String format)
	{
		super(format);
```

### NonProtectedConstructorInAbstractClass
Constructor `ListView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public ListView(final String id, final IModel<? extends List<T>> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `ListView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public ListView(final String id, final List<T> list)
	{
		this(id, Model.ofList(list));
```

### NonProtectedConstructorInAbstractClass
Constructor `ListView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public ListView(final String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            an external URL to be used for the media component
	 */
	public MediaComponent(String id, String url)
	{
		this(id, null, url, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            the resource reference of the media file
	 */
	public MediaComponent(String id, ResourceReference resourceReference)
	{
		this(id, null, null, null, resourceReference);
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            the page parameters to be used to be prepended to the media URL
	 */
	public MediaComponent(String id, ResourceReference resourceReference,
		PageParameters pageParameters)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            the page parameters to be used to be prepended to the media URL
	 */
	public MediaComponent(String id, IModel<?> model, String url, PageParameters pageParameters)
	{
		this(id, model, url, pageParameters, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            the resource reference of the media file
	 */
	public MediaComponent(String id, IModel<?> model, ResourceReference resourceReference)
	{
		this(id, model, null, null, resourceReference);
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            The component id
	 */
	public MediaComponent(String id)
	{
		this(id, null, null, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            an external URL to be used for the media component
	 */
	public MediaComponent(String id, IModel<?> model, String url)
	{
		this(id, model, url, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            the page parameters to be used to be prepended to the media URL
	 */
	public MediaComponent(String id, IModel<?> model, ResourceReference resourceReference,
		PageParameters pageParameters)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `MediaComponent()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
	 *            The component model
	 */
	public MediaComponent(String id, IModel<?> model)
	{
		this(id, model, null, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMarkupSourcingStrategy()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AbstractMarkupSourcingStrategy.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractMarkupSourcingStrategy()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `Panel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/Panel.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public Panel(final String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `Panel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/Panel.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Panel(final String id, final IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `BorderPanel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderPanel.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public BorderPanel(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `BorderPanel()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderPanel.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public BorderPanel(final String id, final IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AssociatedMarkupSourcingStrategy()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AssociatedMarkupSourcingStrategy.java`
#### Snippet
```java
	 *            Either "panel" or "border"
	 */
	public AssociatedMarkupSourcingStrategy(final String tagName)
	{
		this.tagName = Args.notNull(tagName, "tagName");
```

### NonProtectedConstructorInAbstractClass
Constructor `ResponseBufferZone()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/ResponseBufferZone.java`
#### Snippet
```java
	private final MarkupStream stream;

	public ResponseBufferZone(RequestCycle cycle, MarkupStream stream)
	{
		this.cycle = cycle;
```

### NonProtectedConstructorInAbstractClass
Constructor `Border()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Border(final String id, final IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `Border()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public Border(final String id)
	{
		this(id, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMarkupFilter()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/AbstractMarkupFilter.java`
#### Snippet
```java
	}

	public AbstractMarkupFilter(final MarkupResourceStream markupResourceStream)
	{
		this.markupResourceStream = markupResourceStream;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMarkupFilter()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/AbstractMarkupFilter.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractMarkupFilter()
	{
		this(null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractChoice(final String id, final IModel<? extends List<? extends E>> choices)
	{
		this(id, choices, new ChoiceRenderer<E>());
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 */
	public AbstractChoice(final String id, IModel<T> model, final List<? extends E> choices,
		final IChoiceRenderer<? super E> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractChoice(final String id, IModel<T> model, final List<? extends E> choices)
	{
		this(id, model, new ListModel<>(choices), new ChoiceRenderer<>());
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractChoice(final String id, IModel<T> model,
		final IModel<? extends List<? extends E>> choices)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractChoice(final String id, final IModel<? extends List<? extends E>> choices,
		final IChoiceRenderer<? super E> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            The drop down choices
	 */
	public AbstractChoice(final String id, IModel<T> model,
		final IModel<? extends List<? extends E>> choices, final IChoiceRenderer<? super E> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            See Component
	 */
	public AbstractChoice(final String id)
	{
		this(id, new ListModel<>(new ArrayList<E>()), new ChoiceRenderer<E>());
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractChoice(final String id, final List<? extends E> choices)
	{
		this(id, new ListModel<>(choices), new ChoiceRenderer<E>());
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChoice()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
	 *            The collection of choices in the dropdown
	 */
	public AbstractChoice(final String id, final List<? extends E> choices,
		final IChoiceRenderer<? super E> renderer)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPageableView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java

	/** @see org.apache.wicket.Component#Component(String) */
	public AbstractPageableView(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPageableView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public AbstractPageableView(String id, IModel<? extends Collection<? extends T>> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `ArrayIteratorAdapter()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ArrayIteratorAdapter.java`
#### Snippet
```java
	 * @param array
	 */
	public ArrayIteratorAdapter(T[] array)
	{
		this.array = array;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRepeater()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
	 * @param model
	 */
	public AbstractRepeater(String id, IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRepeater()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
	 * @param id
	 */
	public AbstractRepeater(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `DataViewBase()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/DataViewBase.java`
#### Snippet
```java
	 *            data provider
	 */
	public DataViewBase(String id, IDataProvider<T> dataProvider)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `RefreshingView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/RefreshingView.java`
#### Snippet
```java
	 *            component id
	 */
	public RefreshingView(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `RefreshingView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/RefreshingView.java`
#### Snippet
```java
	 *            model
	 */
	public RefreshingView(String id, IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `ModelIteratorAdapter()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ModelIteratorAdapter.java`
#### Snippet
```java
	 *            iterable whose iterator will be wrapped
	 */
	public ModelIteratorAdapter(Iterable<T> iterable)
	{
		Args.notNull(iterable, "iterable");
```

### NonProtectedConstructorInAbstractClass
Constructor `ModelIteratorAdapter()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ModelIteratorAdapter.java`
#### Snippet
```java
	 *            iterator that will be wrapped
	 */
	public ModelIteratorAdapter(Iterator<T> delegate)
	{
		this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `GridView()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/GridView.java`
#### Snippet
```java
	 *            data provider
	 */
	public GridView(String id, IDataProvider<T> dataProvider)
	{
		super(id, dataProvider);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractOutputTransformerContainer()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public AbstractOutputTransformerContainer(final String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractOutputTransformerContainer()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public AbstractOutputTransformerContainer(final String id, final IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `DeepChildFirstVisitor()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/renderStrategy/DeepChildFirstVisitor.java`
#### Snippet
```java
	 * Construct.
	 */
	public DeepChildFirstVisitor()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `PageRenderer()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/handler/render/PageRenderer.java`
#### Snippet
```java
	 * @param renderPageRequestHandler
	 */
	public PageRenderer(RenderPageRequestHandler renderPageRequestHandler)
	{
		this.renderPageRequestHandler = renderPageRequestHandler;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHeaderRenderStrategy()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/markup/renderStrategy/AbstractHeaderRenderStrategy.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractHeaderRenderStrategy()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceLogData()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceLogData.java`
#### Snippet
```java
	 * @param variation
	 */
	public ResourceLogData(String name, Locale locale, String style, String variation)
	{
		this.name = name;
```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicImageResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/DynamicImageResource.java`
#### Snippet
```java
	 *            The image format ("png", "jpeg", etc)
	 */
	public DynamicImageResource(String format)
	{
		setFormat(format);
```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicImageResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/DynamicImageResource.java`
#### Snippet
```java
	 * Construct.
	 */
	public DynamicImageResource()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `BaseDataResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/BaseDataResource.java`
#### Snippet
```java
	 *            The data
	 */
	public BaseDataResource(final String contentType, final T data)
	{
		this(contentType, data, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `BaseDataResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/BaseDataResource.java`
#### Snippet
```java
	 *            The Content type of the array.
	 */
	public BaseDataResource(final String contentType)
	{
		this(contentType, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `BaseDataResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/BaseDataResource.java`
#### Snippet
```java
	 *            The filename that will be set as the Content-Disposition header.
	 */
	public BaseDataResource(final String contentType, final T data, final String filename)
	{
		this.contentType = contentType;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractResource()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractResource()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAjaxBehavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/behavior/AbstractAjaxBehavior.java`
#### Snippet
```java
	 * Constructor.
	 */
	public AbstractAjaxBehavior()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceReference()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	 *            resource name
	 */
	public ResourceReference(String name)
	{
		this(Application.class, name, null, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceReference()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	 *            The data making up the resource reference
	 */
	public ResourceReference(final Key key)
	{
		Args.notNull(key, "key");
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceReference()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	 *            mandatory parameter
	 */
	public ResourceReference(Class<?> scope, String name)
	{
		this(scope, name, null, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceReference()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	 *            resource variation
	 */
	public ResourceReference(Class<?> scope, String name, Locale locale, String style,
		String variation)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `Behavior()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 * Constructor
	 */
	public Behavior()
	{
		if (Application.exists())
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRequestLogger()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractRequestLogger()
	{
		int requestsWindowSize = getRequestsWindowSize();
```

### NonProtectedConstructorInAbstractClass
Constructor `MultipartServletWebRequest()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/MultipartServletWebRequest.java`
#### Snippet
```java
	 * @param url
	 */
	public MultipartServletWebRequest(HttpServletRequest httpServletRequest, String filterPrefix,
		Url url)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `MultipartServletWebRequest()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/MultipartServletWebRequest.java`
#### Snippet
```java
	 * @param filterPrefix
	 */
	public MultipartServletWebRequest(HttpServletRequest httpServletRequest, String filterPrefix)
	{
		super(httpServletRequest, filterPrefix);
```

### NonProtectedConstructorInAbstractClass
Constructor `WebApplication()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
	 * overriding the constructor.</strong>
	 */
	public WebApplication()
	{
		ajaxRequestTargetListeners = new AjaxRequestTargetListenerCollection();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRequestWrapperFactory()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/AbstractRequestWrapperFactory.java`
#### Snippet
```java
	 * Construct.
	 */
	public AbstractRequestWrapperFactory()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `GroupingPageStore()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java
	 *            maximum groups to keep
	 */
	public GroupingPageStore(IPageStore delegate, int maxGroups)
	{
		super(delegate);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRangeValidator()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/AbstractRangeValidator.java`
#### Snippet
```java
	 *            the maximum value
	 */
	public AbstractRangeValidator(R minimum, R maximum)
	{
		setRange(minimum, maximum);
```

### NonProtectedConstructorInAbstractClass
Constructor `AuthorizationException()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/authorization/AuthorizationException.java`
#### Snippet
```java
	 * @param cause
	 */
	public AuthorizationException(String message, Throwable cause)
	{
		super(message, cause);
```

### NonProtectedConstructorInAbstractClass
Constructor `AuthorizationException()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/authorization/AuthorizationException.java`
#### Snippet
```java
	 * Construct.
	 */
	public AuthorizationException()
	{
		super();
```

### NonProtectedConstructorInAbstractClass
Constructor `AuthorizationException()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/authorization/AuthorizationException.java`
#### Snippet
```java
	 * @param cause
	 */
	public AuthorizationException(Throwable cause)
	{
		super(cause);
```

### NonProtectedConstructorInAbstractClass
Constructor `AuthorizationException()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/authorization/AuthorizationException.java`
#### Snippet
```java
	 * @param message
	 */
	public AuthorizationException(String message)
	{
		super(message);
```

### NonProtectedConstructorInAbstractClass
Constructor `SimplePageAuthorizationStrategy()` of an abstract class should not be declared 'public'
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/page/SimplePageAuthorizationStrategy.java`
#### Snippet
```java
	 *            The sign in page class
	 */
	public <S extends Page> SimplePageAuthorizationStrategy(final Class<?> securePageSuperType,
		final Class<S> signInPageClass)
	{
```

### NonProtectedConstructorInAbstractClass
Constructor `AdvancedTreePage()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/AdvancedTreePage.java`
#### Snippet
```java
	 * Construct.
	 */
	public AdvancedTreePage()
	{
		content = new CheckedFolderContent(provider);
```

### NonProtectedConstructorInAbstractClass
Constructor `BasePage()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/events/BasePage.java`
#### Snippet
```java
	 * Constructor
	 */
	public BasePage()
	{
		add(new BookmarkablePageLink<>("back", IndexPage.class).setAutoEnable(true));
```

### NonProtectedConstructorInAbstractClass
Constructor `ContactDetachableModel()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/web/ContactDetachableModel.java`
#### Snippet
```java
	 * @param contact
	 */
	public ContactDetachableModel(Contact contact)
	{
		super(contact);
```

### NonProtectedConstructorInAbstractClass
Constructor `ContactDataProvider()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/web/ContactDataProvider.java`
#### Snippet
```java
{

	public ContactDataProvider()
	{
		setSort("firstName", SortOrder.ASCENDING);
```

### NonProtectedConstructorInAbstractClass
Constructor `ContactsDisplayPage()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/web/ContactsDisplayPage.java`
#### Snippet
```java
	 * Construct.
	 */
	public ContactsDisplayPage()
	{
		List<IColumn<Contact, String>> cols = new ArrayList<>(4);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCaptchaForm()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/AbstractCaptchaForm.java`
#### Snippet
```java
	 *          The component id
	 */
	public AbstractCaptchaForm(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `Banner()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/Banner.java`
#### Snippet
```java
	 * @param id
	 */
	public Banner(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `Banner()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/Banner.java`
#### Snippet
```java
	 * @param model
	 */
	public Banner(String id, IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `TemplatePanel()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/pageinheritance/TemplatePanel.java`
#### Snippet
```java
	 *            the model
	 */
	public TemplatePanel(String id, IModel<?> model)
	{
		super(id, model);
```

### NonProtectedConstructorInAbstractClass
Constructor `TemplatePanel()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/pageinheritance/TemplatePanel.java`
#### Snippet
```java
	 *            component id
	 */
	public TemplatePanel(String id)
	{
		super(id);
```

### NonProtectedConstructorInAbstractClass
Constructor `TemplatePage()` of an abstract class should not be declared 'public'
in `wicket-examples/src/main/java/org/apache/wicket/examples/template/pageinheritance/TemplatePage.java`
#### Snippet
```java
	 * Constructor
	 */
	public TemplatePage()
	{
		add(new Label("title", new PropertyModel<>(this, "pageTitle")));
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWebSocketConnection()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/AbstractWebSocketConnection.java`
#### Snippet
```java
	 *      the web socket processor to delegate to
	 */
	public AbstractWebSocketConnection(AbstractWebSocketProcessor webSocketProcessor)
	{
		this.applicationName = webSocketProcessor.getApplication().getName();
```

### NonProtectedConstructorInAbstractClass
Constructor `WebSocketBehavior()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
public abstract class WebSocketBehavior extends BaseWebSocketBehavior
{
	public WebSocketBehavior()
	{
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `WebSocketPayload()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/event/WebSocketPayload.java`
#### Snippet
```java
	private final WebSocketRequestHandler handler;

	public WebSocketPayload(final WebSocketRequestHandler handler)
	{
		this.handler = Args.notNull(handler, "handler");
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractClientMessage()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/message/AbstractClientMessage.java`
#### Snippet
```java
	private final IKey key;

	public AbstractClientMessage(Application application, String sessionId, IKey key)
	{
		this.applicationName = Args.notNull(application, "application").getName();
```

### NonProtectedConstructorInAbstractClass
Constructor `TestWebSocketConnection()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketConnection.java`
#### Snippet
```java
	private boolean isOpen = true;

	public TestWebSocketConnection(WebApplication application, String sessionId, IKey registryKey)
	{
		this.application = application;
```

### NonProtectedConstructorInAbstractClass
Constructor `TestWebSocketProcessor()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketProcessor.java`
#### Snippet
```java
	 *      the name of the shared resource that will handle the web socket messages
	 */
	public TestWebSocketProcessor(final WicketTester wicketTester, final String resourceName, Page page)
	{
		super(createRequest(wicketTester, resourceName, page),  wicketTester.getApplication());
```

### NonProtectedConstructorInAbstractClass
Constructor `TestWebSocketProcessor()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketProcessor.java`
#### Snippet
```java
	 *      the name of the shared resource that will handle the web socket messages
	 */
	public TestWebSocketProcessor(final WicketTester wicketTester, final String resourceName)
	{
		super(createRequest(wicketTester, resourceName, null),  wicketTester.getApplication());
```

### NonProtectedConstructorInAbstractClass
Constructor `TestWebSocketProcessor()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketProcessor.java`
#### Snippet
```java
	 *            the current Wicket Application
	 */
	public TestWebSocketProcessor(final HttpServletRequest request, final WebApplication application)
	{
		super(request, application);
```

### NonProtectedConstructorInAbstractClass
Constructor `TestWebSocketProcessor()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketProcessor.java`
#### Snippet
```java
	 *      the page that may have registered {@link org.apache.wicket.protocol.ws.api.WebSocketBehavior}
	 */
	public TestWebSocketProcessor(final WicketTester wicketTester, final Page page)
	{
		super(createRequest(wicketTester, page), (WebApplication) page.getApplication());
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWebSocketProcessor()` of an abstract class should not be declared 'public'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/AbstractWebSocketProcessor.java`
#### Snippet
```java
	 *      the current Wicket Application
	 */
	public AbstractWebSocketProcessor(final HttpServletRequest request, final WebApplication application)
	{
		final HttpSession httpSession = request.getSession(true);
```

### NonProtectedConstructorInAbstractClass
Constructor `VelocityPanel()` of an abstract class should not be declared 'public'
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/markup/html/VelocityPanel.java`
#### Snippet
```java
	 */
	@SuppressWarnings("rawtypes")
	public VelocityPanel(final String id, final IModel<? extends Map> model)
	{
		super(id, model);
```

## RuleId[id=TestCaseWithConstructor]
### TestCaseWithConstructor
Initialization logic in constructor `ApacheLicenseHeaderTestCase()` instead of 'setup()' life cycle method
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
	 * Construct.
	 */
	public ApacheLicenseHeaderTestCase()
	{

```

## RuleId[id=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-util/src/main/java/org/apache/wicket/util/LongEncoder.java`
#### Snippet
```java
		if (negative)
		{
			value = -value;
		}
		do
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-util/src/main/java/org/apache/wicket/util/LongEncoder.java`
#### Snippet
```java
			int mod = (int)(value % len);
			buff.prepend(alphabet.charAt(mod));
			value = value / len;
		}
		while (value > 0);
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
            b = readFirstBytes();
            if (b >= 0) {
                buf[off++] = (byte) (b & 0xFF);
                len--;
                firstCount++;
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
            if (b >= 0) {
                buf[off++] = (byte) (b & 0xFF);
                len--;
                firstCount++;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
    public long skip(long n) throws IOException {
        while (n > 0 && readFirstBytes() >= 0) {
            n--;
        }
        return in.skip(n);
```

### AssignmentToMethodParameter
Assignment to method parameter `quotationChar`
in `wicket-util/src/main/java/org/apache/wicket/util/io/FullyBufferedReader.java`
#### Snippet
```java
				if (quotationChar == 0 && (currentChar == '\'' || currentChar == '\"'))
				{// I'm entering inside a quoted string. Set quotationChar
					quotationChar = currentChar;
					countLinesTo(startPos + i);
				}
```

### AssignmentToMethodParameter
Assignment to method parameter `quotationChar`
in `wicket-util/src/main/java/org/apache/wicket/util/io/FullyBufferedReader.java`
#### Snippet
```java
				else if (currentChar == quotationChar && previousTag != '\\')
				{ // I'm out of quotes, reset quotationChar
					quotationChar = 0;
				}
				// I've found character but I'm inside quotes
```

### AssignmentToMethodParameter
Assignment to method parameter `toPos`
in `wicket-util/src/main/java/org/apache/wicket/util/io/FullyBufferedReader.java`
#### Snippet
```java
		if (toPos < 0)
		{
			toPos = input.length();
		}
		else if (toPos < positionMarker)
```

### AssignmentToMethodParameter
Assignment to method parameter `input1`
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		if (!(input1 instanceof BufferedInputStream))
		{
			input1 = new BufferedInputStream(input1);
		}
		if (!(input2 instanceof BufferedInputStream))
```

### AssignmentToMethodParameter
Assignment to method parameter `input2`
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		if (!(input2 instanceof BufferedInputStream))
		{
			input2 = new BufferedInputStream(input2);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `input1`
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		if (!(input1 instanceof BufferedReader))
		{
			input1 = new BufferedReader(input1);
		}
		if (!(input2 instanceof BufferedReader))
```

### AssignmentToMethodParameter
Assignment to method parameter `input2`
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		if (!(input2 instanceof BufferedReader))
		{
			input2 = new BufferedReader(input2);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/MyersDiff.java`
#### Snippet
```java
		if (path.isSnake())
		{
			path = path.prev;
		}
		while ((path != null) && (path.prev != null) && (path.prev.j >= 0))
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/MyersDiff.java`
#### Snippet
```java
			int j = path.j;

			path = path.prev;
			int ianchor = path.i;
			int janchor = path.j;
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/MyersDiff.java`
#### Snippet
```java
			if (path.isSnake())
			{
				path = path.prev;
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `start`
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Chunk.java`
#### Snippet
```java
		for (Object aChunk : chunk)
		{
			target.add(start++, aChunk);
		}
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Classes.java`
#### Snippet
```java
			while (c.isAnonymousClass())
			{
				c = c.getSuperclass();
			}
			simpleName = c.getSimpleName();
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Classes.java`
#### Snippet
```java
			while (c.isAnonymousClass())
			{
				c = c.getSuperclass();
			}
			name = c.getName();
```

### AssignmentToMethodParameter
Assignment to method parameter `msg`
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Args.java`
#### Snippet
```java
	static String format(String msg, final Object... params)
	{
		msg = msg.replace("{}", "%s");
		return String.format(msg, params);
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `group`
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/MetaPattern.java`
#### Snippet
```java
		if (this instanceof Group)
		{
			((Group)this).bind(group++);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `group`
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/MetaPattern.java`
#### Snippet
```java
			for (MetaPattern metaPattern : patterns)
			{
				group = metaPattern.bind(group);
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `t1`
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Objects.java`
#### Snippet
```java
		if (t1 == NONNUMERIC)
		{
			t1 = DOUBLE; // Try to interpret strings as doubles...
		}
		if (t2 == NONNUMERIC)
```

### AssignmentToMethodParameter
Assignment to method parameter `t2`
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Objects.java`
#### Snippet
```java
		if (t2 == NONNUMERIC)
		{
			t2 = DOUBLE; // Try to interpret strings as doubles...
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `prio`
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java
		if (prio < Thread.MIN_PRIORITY)
		{
			prio = Thread.MIN_PRIORITY;
		}
		else if (prio > Thread.MAX_PRIORITY)
```

### AssignmentToMethodParameter
Assignment to method parameter `prio`
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java
		else if (prio > Thread.MAX_PRIORITY)
		{
			prio = Thread.MAX_PRIORITY;
		}
		thread.setPriority(prio);
```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/SqlTimestampConverter.java`
#### Snippet
```java
		if (locale == null)
		{
			locale = Locale.getDefault(Locale.Category.FORMAT);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractNumberConverter.java`
#### Snippet
```java
		if (locale == null)
		{
			locale = Locale.getDefault(Locale.Category.FORMAT);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractNumberConverter.java`
#### Snippet
```java
			// Convert spaces to no-break space (groupingSeparator) as required by Java formats:
			// http://bugs.sun.com/view_bug.do?bug_id=4510618
			value = ((String)value).replaceAll("(\\d+)\\s(?=\\d)", "$1" + groupingSeparator);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractJavaTimeConverter.java`
#### Snippet
```java
		if (locale == null)
		{
			locale = Locale.getDefault();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/SqlTimeConverter.java`
#### Snippet
```java
		if (locale == null)
		{
			locale = Locale.getDefault(Locale.Category.FORMAT);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractDateConverter.java`
#### Snippet
```java
		if (locale == null)
		{
			locale = Locale.getDefault(Locale.Category.FORMAT);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `end`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (end > count)
		{
			end = count;
		}
		if (start > end)
```

### AssignmentToMethodParameter
Assignment to method parameter `end`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (end > count)
		{
			end = count;
		}
		if (start > end)
```

### AssignmentToMethodParameter
Assignment to method parameter `sb`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (sb == null)
		{
			sb = NULL;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (str == null)
		{
			str = SBF_NULL;
		}
		int len = str.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `fromIndex`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (fromIndex < 0)
		{
			fromIndex = 0;
		}
		if (targetCount == 0)
```

### AssignmentToMethodParameter
Assignment to method parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (str == null)
		{
			str = String.valueOf(str);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `fromIndex`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (fromIndex > rightIndex)
		{
			fromIndex = rightIndex;
		}
		/* Empty string always matches. */
```

### AssignmentToMethodParameter
Assignment to method parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (str == null)
		{
			str = SB_NULL;
		}
		int len = str.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `sb`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (sb == null)
		{
			sb = SB_NULL;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (str == null)
		{
			str = String.valueOf(str);
		}
		int len = str.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceUtils.java`
#### Snippet
```java
		{
			extension = path.substring(pos);
			path = path.substring(0, pos);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `replaceWith`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (replaceWith == null)
		{
			replaceWith = "";
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `str`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
			int number = Integer.parseInt(str.substring(pos + 2, end - 1));
			char ch = (char)number;
			str = str.substring(0, pos) + ch + str.substring(end);
			matcher = HTML_NUMBER_REGEX.matcher(str);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
			// swap the input strings to consume less memory
			CharSequence tmp = s;
			s = t;
			t = tmp;
			n = m;
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
			CharSequence tmp = s;
			s = t;
			t = tmp;
			n = m;
			m = t.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `initialCapacity`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
		if (initialCapacity > MAXIMUM_CAPACITY)
		{
			initialCapacity = MAXIMUM_CAPACITY;
		}
		if ((loadFactor <= 0) || Float.isNaN(loadFactor))
```

### AssignmentToMethodParameter
Assignment to method parameter `clazz`
in `wicket-ioc/src/main/java/org/apache/wicket/injection/Injector.java`
#### Snippet
```java
				}
			}
			clazz = clazz.getSuperclass();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
			
			SimpleEntry<Class<?>, Class<?>> key = new SimpleEntry<Class<?>, Class<?>>(fieldType, generic);
			name = beanNameCache.get(key);
			if (name == null)
			{
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
			if (name == null)
			{
				name = getBeanNameOfClass(contextLocator.getSpringContext(), fieldType, generic, field.getName());
				if (name != null)
				{
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
					if (tmpName != null)
					{
						name = tmpName;
					}
				}
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
			return "";
		}
		path = path.trim();
		if (!path.startsWith("/"))
		{
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
		if (!path.startsWith("/"))
		{
			path = "/" + path;
		}
		if (path.endsWith("/"))
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
		if (path.endsWith("/"))
		{
			path = path.substring(0, path.length() - 1);
		}
		return path;
```

### AssignmentToMethodParameter
Assignment to method parameter `duration`
in `wicket-request/src/main/java/org/apache/wicket/request/http/WebResponse.java`
#### Snippet
```java
		if (duration.compareTo(MAX_CACHE_DURATION) > 0)
		{
			duration = MAX_CACHE_DURATION;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-request/src/main/java/org/apache/wicket/request/HttpHeaderCollection.java`
#### Snippet
```java
	{
		// be lenient and strip leading / trailing blanks
		value = Args.notNull(value, "value").trim();

		internalAdd(name, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
		if (Strings.isEmpty(value))
		{
			value = fallback1;
			if (Strings.isEmpty(value))
			{
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
			if (Strings.isEmpty(value))
			{
				value = fallback2;
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
		if (url.startsWith("/"))
		{
			url = url.substring(1);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
		if (value == null)
		{
			value = fallback1;
			if (value == null)
			{
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
			if (value == null)
			{
				value = fallback2;
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `segmentsToSkip`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/AbstractMapper.java`
#### Snippet
```java
		{
			urlCopy.getSegments().remove(0);
			--segmentsToSkip;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `mountPath`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/AbstractMapper.java`
#### Snippet
```java
		if (mountPath.charAt(0) == '/')
		{
			mountPath = mountPath.substring(1);
		}
		Url url = Url.parse(mountPath);
```

### AssignmentToMethodParameter
Assignment to method parameter `pageParameters`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/AbstractMapper.java`
#### Snippet
```java
		if (pageParameters == null)
		{
			pageParameters = new PageParameters();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/AbstractMapper.java`
#### Snippet
```java
		{
			// copy the url
			url = new Url(url);

			for (String s : parametersUrl.getSegments())
```

### AssignmentToMethodParameter
Assignment to method parameter `settings`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AbstractAutoCompleteBehavior.java`
#### Snippet
```java
		if (settings == null)
		{
			settings = new AutoCompleteSettings();
		}
		this.settings = settings;
```

### AssignmentToMethodParameter
Assignment to method parameter `count`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
	private final String searchCharSet(final String[] items, final StringBuilder base, int count)
	{
		if ((--count >= 0) && (items[count] != null) && (items[count].length() > 0))
		{
			String charset;
```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
				if (locale == null)
				{
					locale = Locale.getDefault(Locale.Category.FORMAT);
				}
				return new SimpleDateFormat(DateTextField.this.datePattern, locale);
```

### AssignmentToMethodParameter
Assignment to method parameter `segments`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		if (!isAtLeastOneSegmentReal(segments) && !isLastSegmentEmpty(segments))
		{
			segments = new ArrayList<>(segments);
			segments.add("");
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `charset`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java

		// the url object resolved the charset, use that
		charset = result.getCharset();

		String url = _url.toString();
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/WicketMetricsSettings.java`
#### Snippet
```java
		if (!prefix.endsWith("/"))
		{
			prefix = prefix + "/";
		}
		this.prefix = prefix;
```

### AssignmentToMethodParameter
Assignment to method parameter `metaData`
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java
							System.arraycopy(metaData, 0, newMetaData, 0, i);
							System.arraycopy(metaData, i + 1, newMetaData, i, l - i);
							metaData = newMetaData;
						}
						else
```

### AssignmentToMethodParameter
Assignment to method parameter `metaData`
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java
						else
						{
							metaData = null;
						}
					}
```

### AssignmentToMethodParameter
Assignment to method parameter `metaData`
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java
			if (metaData == null)
			{
				metaData = new MetaDataEntry[1];
				metaData[0] = m;
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `metaData`
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java
				System.arraycopy(metaData, 0, newMetaData, 0, metaData.length);
				newMetaData[metaData.length] = m;
				metaData = newMetaData;
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `pageStore`
in `wicket-core/src/main/java/org/apache/wicket/DefaultPageManagerProvider.java`
#### Snippet
```java
		if (storeSettings.isEncrypted())
		{
			pageStore = new CryptingPageStore(pageStore, application);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `pageStore`
in `wicket-core/src/main/java/org/apache/wicket/DefaultPageManagerProvider.java`
#### Snippet
```java
		{
			int capacity = storeSettings.getAsynchronousQueueCapacity();
			pageStore = new AsynchronousPageStore(pageStore, capacity);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
		if ((locale == null) && (component != null))
		{
			locale = component.getLocale();
		}
		if (locale == null)
```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
		if (locale == null)
		{
			locale = Session.exists() ? Session.get().getLocale() : Locale.getDefault();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `style`
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
		if ((style == null) && (component != null))
		{
			style = component.getStyle();
		}
		if (style == null)
```

### AssignmentToMethodParameter
Assignment to method parameter `style`
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
		if (style == null)
		{
			style = Session.exists() ? Session.get().getStyle() : null;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-core/src/main/java/org/apache/wicket/core/util/file/WebApplicationPath.java`
#### Snippet
```java
		if (!path.startsWith("/"))
		{
			path = "/" + path;
		}
		if (!path.endsWith("/"))
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-core/src/main/java/org/apache/wicket/core/util/file/WebApplicationPath.java`
#### Snippet
```java
		if (!path.endsWith("/"))
		{
			path += "/";
		}
		this.path = path;
```

### AssignmentToMethodParameter
Assignment to method parameter `fromIndex`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java
	{
		int count = 0;
		--fromIndex;
		while (fromIndex >= 0)
		{
```

### AssignmentToMethodParameter
Assignment to method parameter `fromIndex`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java
		while (fromIndex >= 0)
		{
			if (s.charAt(fromIndex--) == c)
			{
				++count;
```

### AssignmentToMethodParameter
Assignment to method parameter `exp`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
				// if expression begins with [ skip method finding and use it as
				// a key/index lookup on a map.
				exp = exp.substring(1, exp.length() - 1);
			}
			else if (exp.endsWith("()"))
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
		public void setValue(Object object, Object value, PropertyResolverConverter converter)
		{
			value = converter.convert(value, clzComponentType);
			Array.set(object, index, value);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `clz`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
				else if (tryToCreateNull == RESOLVE_CLASS)
				{
					clz = getAndSet.getTargetClass();
				}
				else
```

### AssignmentToMethodParameter
Assignment to method parameter `clz`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			{
				// value can be null if we are in the RESOLVE_CLASS
				clz = value.getClass();
			}

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			final PropertyResolverConverter converter)
		{
			value = converter.convert(value, field.getType());
			try
			{
```

### AssignmentToMethodParameter
Assignment to method parameter `expression`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			if (expression.endsWith("()"))
			{
				expression = expression.substring(0, expression.length() - 2);
			}
			Method method = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
				Class<?> repCl;
				if (!(Boolean)HAS_WRITE_REPLACE_METHOD_METHOD.invoke(desc, (Object[])null) ||
						(obj = INVOKE_WRITE_REPLACE_METHOD.invoke(desc, obj)) == null ||
						(repCl = obj.getClass()) == cls)
				{
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
		if ((data != null) && (data.locale != null))
		{
			path = data.path;
			locale = data.locale;
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
		{
			path = data.path;
			locale = data.locale;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `className`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractComponentMapper.java`
#### Snippet
```java
		{
			// remove any path parameters set manually by the user. WICKET-5500
			className = Strings.beforeFirst(className, ';');
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `request`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java
	public int getCompatibilityScore(Request request)
	{
		request = getRequest(request);

		if (hasBufferedResponse(request.getUrl()))
```

### AssignmentToMethodParameter
Assignment to method parameter `request`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java
	public IRequestHandler mapRequest(Request request)
	{
		request = getRequest(request);

		BufferedWebResponse response = getAndRemoveBufferedResponse(request.getUrl());
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
				return null;
			}
			path = path.length() == id.length() ? "" : path.substring(id.length() + 1);
			id = Strings.firstPathComponent(path, Component.PATH_SEPARATOR);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `current`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
									removal.removedChild == null)
								{
									current = removal.previousSibling;
									
									// current was removed, use its sibling instead
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/IPageRequestHandler.java`
#### Snippet
```java
		while (handler instanceof IRequestHandlerDelegate)
		{
			handler = ((IRequestHandlerDelegate) handler).getDelegateHandler();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `requestHandler`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
		while (requestHandler instanceof IRequestHandlerDelegate)
		{
			requestHandler = ((IRequestHandlerDelegate)requestHandler).getDelegateHandler();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `testMarkup`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java
	 */
	protected final void compareMarkupWithString(IMarkupFragment markup, String testMarkup) {
		testMarkup = testMarkup.replaceAll("\r", "");
		testMarkup = testMarkup.replaceAll("\n", "");
		testMarkup = testMarkup.replaceAll("\t", "");
```

### AssignmentToMethodParameter
Assignment to method parameter `testMarkup`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java
	protected final void compareMarkupWithString(IMarkupFragment markup, String testMarkup) {
		testMarkup = testMarkup.replaceAll("\r", "");
		testMarkup = testMarkup.replaceAll("\n", "");
		testMarkup = testMarkup.replaceAll("\t", "");

```

### AssignmentToMethodParameter
Assignment to method parameter `testMarkup`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTestCase.java`
#### Snippet
```java
		testMarkup = testMarkup.replaceAll("\r", "");
		testMarkup = testMarkup.replaceAll("\n", "");
		testMarkup = testMarkup.replaceAll("\t", "");

		String doc = markup.toString(true);
```

### AssignmentToMethodParameter
Assignment to method parameter `document`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java

		// replace all line endings with unix style line ending
		document = document.replaceAll("\n\r", "\n");
		document = document.replaceAll("\r\n", "\n");

```

### AssignmentToMethodParameter
Assignment to method parameter `document`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java
		// replace all line endings with unix style line ending
		document = document.replaceAll("\n\r", "\n");
		document = document.replaceAll("\r\n", "\n");

		boolean equals = compareMarkup(document, reference);
```

### AssignmentToMethodParameter
Assignment to method parameter `tag`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			if (tag.hasBehaviors())
			{
				tag = tag.mutable();
				Iterator<? extends Behavior> tagBehaviors = tag.getBehaviors();
				while (tagBehaviors.hasNext())
```

### AssignmentToMethodParameter
Assignment to method parameter `tag`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
				(isIgnoreAttributeModifier() == false))
			{
				tag = tag.mutable();
				for (Behavior behavior : behaviors)
				{
```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		if (position == -1)
		{
			position = currentLength;
		}
		if (position > currentLength)
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java
		// cookie names cannot contain ':',
		// we replace ':' with '.' but first we have to encode '.' as '..'
		key = Strings.replaceAll(key, ".", "..").toString();
		key = key.replace(':', '.');
		return key;
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java
		// we replace ':' with '.' but first we have to encode '.' as '..'
		key = Strings.replaceAll(key, ".", "..").toString();
		key = key.replace(':', '.');
		return key;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `markup`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
				// remove the CDATA and
				// the id attribute of the component because it is often the same as the element's id
				markup = AJAX_COMPONENT_CDATA_OPEN.matcher(markup).replaceAll("<component>");
				markup = AJAX_COMPONENT_CDATA_CLOSE.matcher(markup).replaceAll("</component>");

```

### AssignmentToMethodParameter
Assignment to method parameter `markup`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
				// the id attribute of the component because it is often the same as the element's id
				markup = AJAX_COMPONENT_CDATA_OPEN.matcher(markup).replaceAll("<component>");
				markup = AJAX_COMPONENT_CDATA_CLOSE.matcher(markup).replaceAll("</component>");

				XmlPullParser parser = new XmlPullParser();
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
		else
		{
			path = this.path + ":" + path;
			tester.clickLink(path, false);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `item`
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
			while (item instanceof IWrappedHeaderItem)
			{
				item = ((IWrappedHeaderItem) item).getWrapped();
			}

```

### AssignmentToMethodParameter
Assignment to method parameter `object`
in `wicket-core/src/main/java/org/apache/wicket/model/util/GenericBaseModel.java`
#### Snippet
```java
		if (!(object instanceof Serializable))
		{
			object = createSerializableVersionOf(object);
		}
		this.object = object;
```

### AssignmentToMethodParameter
Assignment to method parameter `filter`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupParser.java`
#### Snippet
```java
		public boolean add(IMarkupFilter filter, final Class<? extends IMarkupFilter> beforeFilter)
		{
			filter = onAdd(filter);
			if (filter == null)
			{
```

### AssignmentToMethodParameter
Assignment to method parameter `containerClass`
in `wicket-core/src/main/java/org/apache/wicket/markup/DefaultMarkupResourceStreamProvider.java`
#### Snippet
```java
			// Walk up the class hierarchy one level, if markup has not
			// yet been found
			containerClass = containerClass.getSuperclass();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `extendIndex`
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
		// Now append all elements from the derived markup starting with
		// <wicket:extend> until </wicket:extend> to the list
		for (; extendIndex < markup.size(); extendIndex++)
		{
			MarkupElement element = markup.get(extendIndex);
```

### AssignmentToMethodParameter
Assignment to method parameter `rawMarkup`
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
					buf.append(rawMarkup.substring(pos2 + 3));
				}
				rawMarkup = buf.toString();
			}
			pos1 = rawMarkup.indexOf("<!--", pos1);
```

### AssignmentToMethodParameter
Assignment to method parameter `rawMarkup`
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
		if (stripComments)
		{
			rawMarkup = removeComment(rawMarkup);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `rawMarkup`
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
		if (compressWhitespace)
		{
			rawMarkup = compressWhitespace(rawMarkup);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `markup`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
				if (markup == null)
				{
					markup = Markup.NO_MARKUP;
				}

```

### AssignmentToMethodParameter
Assignment to method parameter `markup`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
				// issue. For consistency reasons however, we should always use
				// the markup loaded first which is why it gets returned.
				markup = markupCache.get(locationString);
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `event`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnEventHeaderItem.java`
#### Snippet
```java

		Args.notEmpty(event, "event");
		event = event.toLowerCase(Locale.ROOT);
		this.event = event;
		this.javaScript = javaScript;
```

### AssignmentToMethodParameter
Assignment to method parameter `item`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/AbstractHeaderResponseFilter.java`
#### Snippet
```java
		while (item instanceof IWrappedHeaderItem)
		{
			item = ((IWrappedHeaderItem)item).getWrapped();
		}
		return acceptsWrapped(item);
```

### AssignmentToMethodParameter
Assignment to method parameter `item`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/SubresourceHeaderResponse.java`
#### Snippet
```java
		while (item instanceof IWrappedHeaderItem)
		{
			item = ((IWrappedHeaderItem)item).getWrapped();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `item`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/JavaScriptDeferHeaderResponse.java`
#### Snippet
```java
		if (RequestCycle.get().find(AjaxRequestTarget.class).isEmpty()) {
			while (item instanceof IWrappedHeaderItem) {
				item = ((IWrappedHeaderItem)item).getWrapped();
			}

```

### AssignmentToMethodParameter
Assignment to method parameter `item`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/JavaScriptDeferHeaderResponse.java`
#### Snippet
```java
				((AbstractJavaScriptReferenceHeaderItem)item).setDefer(true);
			} else if (item instanceof JavaScriptContentHeaderItem) {
				item = new NativeOnDomContentLoadedHeaderItem(((JavaScriptContentHeaderItem)item).getJavaScript());
			} else if (item instanceof OnDomReadyHeaderItem) {
				item = new NativeOnDomContentLoadedHeaderItem(((OnDomReadyHeaderItem)item).getJavaScript());
```

### AssignmentToMethodParameter
Assignment to method parameter `item`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/JavaScriptDeferHeaderResponse.java`
#### Snippet
```java
				item = new NativeOnDomContentLoadedHeaderItem(((JavaScriptContentHeaderItem)item).getJavaScript());
			} else if (item instanceof OnDomReadyHeaderItem) {
				item = new NativeOnDomContentLoadedHeaderItem(((OnDomReadyHeaderItem)item).getJavaScript());
			} else if (item instanceof OnLoadHeaderItem) {
				item = new NativeOnLoadHeaderItem(((OnLoadHeaderItem)item).getJavaScript());
```

### AssignmentToMethodParameter
Assignment to method parameter `item`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/JavaScriptDeferHeaderResponse.java`
#### Snippet
```java
				item = new NativeOnDomContentLoadedHeaderItem(((OnDomReadyHeaderItem)item).getJavaScript());
			} else if (item instanceof OnLoadHeaderItem) {
				item = new NativeOnLoadHeaderItem(((OnLoadHeaderItem)item).getJavaScript());
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `item`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/ResourceAggregator.java`
#### Snippet
```java
	public void render(HeaderItem item)
	{
		item = getItemToBeRendered(item);
		if (item instanceof OnDomReadyHeaderItem || item instanceof OnEventHeaderItem)
		{
```

### AssignmentToMethodParameter
Assignment to method parameter `file`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/PackageResourceGuard.java`
#### Snippet
```java
		if (file != null)
		{
			file = file.trim();
		}
		return (!blockedFiles.contains(file));
```

### AssignmentToMethodParameter
Assignment to method parameter `pageMarkup`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
					.getMarkupFactory()
					.newMarkupParser(markupResourceStream);
				pageMarkup = markupParser.parse();
			}
			catch (Exception e)
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
			if (path.startsWith(componentIdPageId) == false)
			{
				path = componentIdPageId + path;
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
			if (fileOnly)
			{
				path = Strings.lastPathComponent(path, PATH_SEPARATOR);
			}
			return regex.matcher(path).matches();
```

### AssignmentToMethodParameter
Assignment to method parameter `container`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
			// remember the container so we dont search it again, and search the parent
			searched[0] = container;
			container = container.getParent();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `submitter`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
			if (submitter == null)
			{
				submitter = findSubmitter();

				if (submitter instanceof IFormSubmittingComponent)
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
					if (id != null)
					{
						url = url + "#" + anchor.getMarkupId();
					}
					else
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
						if (href != null && href.length() > 1 && href.charAt(0) == '#')
						{
							url = url + href;
						}
					}
```

### AssignmentToMethodParameter
Assignment to method parameter `itemsPerPage`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java
		if (itemsPerPage < 0)
		{
			itemsPerPage = 0;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `currentPage`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java
		if (currentPage < 0)
		{
			currentPage = 0;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `currentPage`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java
		if ((currentPage > 0) && (currentPage >= pageCount))
		{
			currentPage = pageCount - 1;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/include/Include.java`
#### Snippet
```java
		if (url.charAt(0) != '/')
		{
			url = '/' + url;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `tag`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketTagIdentifier.java`
#### Snippet
```java
		{
			// It is <wicket:...>
			tag = new WicketTag(tag.getXmlTag());

			if (Strings.isEmpty(wicketIdValue))
```

### AssignmentToMethodParameter
Assignment to method parameter `renderer`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
		if (renderer == null)
		{
			renderer = new ChoiceRenderer<>();
		}
		this.renderer = renderer;
```

### AssignmentToMethodParameter
Assignment to method parameter `startIndex`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java
		char quote = 0;

		for (; startIndex < input.size(); startIndex++)
		{
			final char charAt = input.charAt(startIndex);
```

### AssignmentToMethodParameter
Assignment to method parameter `closeBracketIndex`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java
				{
					// Get index of closing tag and advance past the tag
					closeBracketIndex = findChar('>', pos1);

					if (closeBracketIndex == -1)
```

### AssignmentToMethodParameter
Assignment to method parameter `tagText`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java

					// Get the tagtext between open and close brackets
					tagText = input.getSubstring(openBracketIndex + 1, closeBracketIndex)
						.toString();

```

### AssignmentToMethodParameter
Assignment to method parameter `pathRelativeToContextRoot`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ContextRelativeResource.java`
#### Snippet
```java
		if (!pathRelativeToContextRoot.startsWith("/"))
		{
			pathRelativeToContextRoot = "/" + pathRelativeToContextRoot;
		}
		this.path = pathRelativeToContextRoot;
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
				listeners.onRequestHandlerExecuted(this, handler);
				
				handler = next;
			}
			catch (RuntimeException e)
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
				}

				handler = replacer.getReplacementRequestHandler();
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
	private void checkHeaderAccess(String name)
	{
		name = Args.notEmpty(name.trim().toLowerCase(Locale.ROOT), "name");

		if (INTERNAL_HEADERS.contains(name))
```

### AssignmentToMethodParameter
Assignment to method parameter `queryString`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestUtils.java`
#### Snippet
```java
        if (Strings.indexOf(queryString, '?') == 0)
        {
            queryString = queryString.substring(1);
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `relativePagePath`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestUtils.java`
#### Snippet
```java
		if (relativePagePath.startsWith("./"))
		{
			relativePagePath = relativePagePath.substring(2);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ResourceIsolationRequestCycleListener.java`
#### Snippet
```java
		}

		handler = unwrap(handler);
		if (isChecked(handler))
		{
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ResourceIsolationRequestCycleListener.java`
#### Snippet
```java
		while (handler instanceof IRequestHandlerDelegate)
		{
			handler = ((IRequestHandlerDelegate)handler).getDelegateHandler();
		}
		return handler;
```

### AssignmentToMethodParameter
Assignment to method parameter `filterPrefix`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/DispatchedRequestUtils.java`
#### Snippet
```java
		if (filterPrefix == null)
		{
			filterPrefix = "";
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `filterPrefix`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/DispatchedRequestUtils.java`
#### Snippet
```java
		if (Strings.isEmpty(filterPrefix) == false && filterPrefix.startsWith("/") == false)
		{
			filterPrefix = '/' + filterPrefix;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `filterPath`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
		{
			o++; // include exactly one trailing slash
			filterPath = filterPath.substring(beginIndex, o);
		}
		else
```

### AssignmentToMethodParameter
Assignment to method parameter `filterPath`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
		{
			// ensure to append trailing slash
			filterPath = filterPath.substring(beginIndex) + '/';
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `filterPath`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
		if (filterPath != null)
		{
			filterPath = canonicaliseFilterPath(filterPath);

			// We only need to determine it once. It'll not change.
```

### AssignmentToMethodParameter
Assignment to method parameter `insert`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

				String accept = request.getHeader("Accept");
				insert = ((accept == null) || (accept.indexOf(MarkupType.XML_MIME) != -1));
			}

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
		if (path.charAt(0) == '/')
		{
			path = path.substring(1);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `filterName`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
			{
				// According to the ServletSpec, the filter might not yet been initialized
				filterName = getWicketFilter().getFilterConfig().getFilterName();
			}
			String namespace = getMapperContext().getNamespace();
```

### AssignmentToMethodParameter
Assignment to method parameter `servletRequest`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
			for (AbstractRequestWrapperFactory factory : getFilterFactoryManager())
			{
				servletRequest = factory.getWrapper(servletRequest);
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `config`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/FilterFactoryManager.java`
#### Snippet
```java
		if (config == null)
		{
			config = WebApplication.get().getWicketFilter().getFilterConfig();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `config`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/FilterFactoryManager.java`
#### Snippet
```java
		if (config == null)
		{
			config = WebApplication.get().getWicketFilter().getFilterConfig();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `filterPrefix`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
		if (filterPrefix.length() > 0 && !filterPrefix.endsWith("/"))
		{
			filterPrefix += "/";
		}
		StringBuilder url = new StringBuilder();
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
		}
		StringBuilder url = new StringBuilder();
		uri = Strings.stripJSessionId(uri);
		String contextPath = httpServletRequest.getContextPath();

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
		if (name.startsWith("/"))
		{
			name = name.substring(1);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
		if (name.startsWith("/"))
		{
			name = name.substring(1);
		}
		if (name.endsWith("/"))
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
		if (name.endsWith("/"))
		{
			name = name.substring(0, name.length() - 1);
		}
		String[] elements = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebResponse.java`
#### Snippet
```java
		{
			redirect = true;
			url = encodeRedirectURL(url);

			// wicket redirects should never be cached
```

### AssignmentToMethodParameter
Assignment to method parameter `charset`
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
				if (charset == null)
                                {
					charset = Charset.defaultCharset();
                                }

```

### AssignmentToMethodParameter
Assignment to method parameter `env`
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemJarPathService.java`
#### Snippet
```java
					if (env == null)
					{
						env = new HashMap<>();
						env.put("create", "true");
						env.put("encoding", "UTF-8");
```

### AssignmentToMethodParameter
Assignment to method parameter `clazz`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/PackageStringResourceLoader.java`
#### Snippet
```java
			while (packageName.length() > 0);

			clazz = clazz.getSuperclass();
			if (clazz == null)
			{
```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/BundleStringResourceLoader.java`
#### Snippet
```java
		if (locale == null)
		{
			locale = Session.exists() ? Session.get().getLocale() : Locale.getDefault();
		}
		try
```

### AssignmentToMethodParameter
Assignment to method parameter `component`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
				break;
			}
			component = component.getParent();
		}
		return path;
```

### AssignmentToMethodParameter
Assignment to method parameter `clazz`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java

			// Move to the next superclass
			clazz = clazz.getSuperclass();

			if (clazz == null)
```

### AssignmentToMethodParameter
Assignment to method parameter `page`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/SerializingPageStore.java`
#### Snippet
```java
		if (page instanceof SerializedPage == false)
		{
			page = new SerializedPage(page.getPageId(), Classes.name(page.getClass()), serializer.serialize(page));
		}
		getDelegate().addPage(context, page);
```

### AssignmentToMethodParameter
Assignment to method parameter `page`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/CryptingPageStore.java`
#### Snippet
```java
			application.getSecuritySettings().getRandomSupplier().getRandom());

		page = new SerializedPage(page.getPageId(), serializedPage.getPageType(), encrypted);

		getDelegate().addPage(context, page);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/NestedFolders.java`
#### Snippet
```java
	public File get(String name, final boolean create)
	{
		name = name.replace('*', '_');
		name = name.replace('/', '_');
		name = name.replace(':', '_');
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/NestedFolders.java`
#### Snippet
```java
	{
		name = name.replace('*', '_');
		name = name.replace('/', '_');
		name = name.replace(':', '_');

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/NestedFolders.java`
#### Snippet
```java
		name = name.replace('*', '_');
		name = name.replace('/', '_');
		name = name.replace(':', '_');

		String path = createPathFrom(name);
```

### AssignmentToMethodParameter
Assignment to method parameter `error`
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/StringValidator.java`
#### Snippet
```java
	protected IValidationError decorate(IValidationError error, IValidatable<String> validatable)
	{
		error = super.decorate(error, validatable);
		if (error instanceof ValidationError)
		{
```

### AssignmentToMethodParameter
Assignment to method parameter `error`
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/DateValidator.java`
#### Snippet
```java
	protected IValidationError decorate(IValidationError error, IValidatable<Date> validatable)
	{
		error = super.decorate(error, validatable);

		if (error instanceof ValidationError)
```

### AssignmentToMethodParameter
Assignment to method parameter `schemes`
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
		if (schemes == null)
		{
			schemes = defaultSchemes;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `creditCardNumber`
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java

		// strip spaces and dashes
		creditCardNumber = creditCardNumber.replaceAll("[ -]", "");

		// the length of the credit card number has to be between 12 and 19.
```

### AssignmentToMethodParameter
Assignment to method parameter `creditCardNumber`
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java

		// strip spaces and dashes
		creditCardNumber = creditCardNumber.replaceAll("[ -]", "");

		// the length of the credit card number has to be between 12 and 19.
```

### AssignmentToMethodParameter
Assignment to method parameter `department`
in `wicket-examples/src/main/java/org/apache/wicket/examples/wizard/User.java`
#### Snippet
```java
		if (department == null)
		{
			department = "";
		}
		this.department = department;
```

### AssignmentToMethodParameter
Assignment to method parameter `result`
in `wicket-examples/src/main/java/org/apache/wicket/examples/breadcrumb/ResultPanel.java`
#### Snippet
```java
		if (Strings.isEmpty(result))
		{
			result = "(hey, you didn't even provide any input!)";
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `request`
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/LocaleFirstMapper.java`
#### Snippet
```java
		if (getLocaleFromUrl(request) != null)
		{
			request = stripLocaleSegment(request);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `request`
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/LocaleFirstMapper.java`
#### Snippet
```java

			// now that we have proccessed the first segment we need to strip from the url
			request = stripLocaleSegment(request);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResponse.java`
#### Snippet
```java
	{
		isRedirect = true;
		url = encodeRedirectURL(url);

		String ajaxRedirect = "<ajax-response><redirect><![CDATA[" + url + "]]></redirect></ajax-response>";
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `data`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
		}

		synchronized (data)
		{
			return StreamSupport.stream(data.spliterator(), false).map(page -> {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `data`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
		for (IMemoryData data : datas.values())
		{
			synchronized (data)
			{
				for (IManageablePage page : data)
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `data`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
		if (data != null)
		{
			synchronized (data)
			{
				data.remove(page.getPageId());
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `session`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DefaultPageContext.java`
#### Snippet
```java
		Session session = Session.get();

		synchronized (session)
		{
			T value = (T)session.getAttribute(key);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `session`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DefaultPageContext.java`
#### Snippet
```java
		Session session = Session.get();

		synchronized (session)
		{
			T value = session.getMetaData(key);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `application`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
			if (settings == null)
			{
				synchronized (application)
				{
					settings = application.getMetaData(KEY);
```

## RuleId[id=ReturnNull]
### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/NullProvider.java`
#### Snippet
```java
	public T get()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/io/DeferredFileOutputStream.java`
#### Snippet
```java
			return memoryOutputStream.toByteArray();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
    public String getBOMCharsetName() throws IOException {
        getBOM();
        return byteOrderMark == null ? null : byteOrderMark.getCharsetName();
    }

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/io/Connections.java`
#### Snippet
```java
			if (milliseconds == 0)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceProxyTargetLocator.java`
#### Snippet
```java
				if (injector.getBinding(key) == null)
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceProxyTargetLocator.java`
#### Snippet
```java
			catch (RuntimeException e)
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceFieldValueFactory.java`
#### Snippet
```java
					if (cachedValue != null)
					{
						return cachedValue == NULL_SENTINEL ? null : cachedValue;
					}

```

### ReturnNull
Return of `null`
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceFieldValueFactory.java`
#### Snippet
```java
		}

		return target == NULL_SENTINEL ? null : target;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/io/XmlReader.java`
#### Snippet
```java
		{
			// No
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/io/XmlReader.java`
#### Snippet
```java
		{
			// No
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/xml/CustomEntityResolver.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/xml/CustomEntityResolver.java`
#### Snippet
```java
			if (stream == null)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Delta.java`
#### Snippet
```java
		catch (Exception e)
		{
			return null;
		}
		result.init(orig, rev);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/diff/myers/PathNode.java`
#### Snippet
```java
		if (isBootstrap())
		{
			return null;
		}
		if (!isSnake() && (prev != null))
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/file/Path.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Exceptions.java`
#### Snippet
```java
			cursor = cursor.getCause();
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/file/File.java`
#### Snippet
```java
			return getName().substring(lastDot + 1);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/file/File.java`
#### Snippet
```java
		if(time == 0)
		{
			return null;
		}
		return Instant.ofEpochMilli(time);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/file/Files.java`
#### Snippet
```java
		if (millis == 0)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/file/Files.java`
#### Snippet
```java
			if (resourceAt == -1)
			{
				return null;
			}
			return new File(path.substring(0, resourceAt));
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/file/Files.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/file/Files.java`
#### Snippet
```java
			return Strings.lastPathComponent(path, '.');
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/file/WebXmlFile.java`
#### Snippet
```java
			return paths.iterator().next();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Threads.java`
#### Snippet
```java
		{
			// don't waste time to load the stack of the current thread
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/AbstractCrypt.java`
#### Snippet
```java
		{
			log.error("Unable to encrypt text '" + plainText + "'", e);
			return null;
		}
		catch (UnsupportedEncodingException e)
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/AbstractCrypt.java`
#### Snippet
```java
		{
			log.error("Unable to encrypt text '" + plainText + "'", e);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/AbstractCrypt.java`
#### Snippet
```java
		{
			log.debug("Error decoding text: " + text, ex);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/StringUtils.java`
#### Snippet
```java
		if (string == null)
		{
			return null;
		}
		try
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/StringUtils.java`
#### Snippet
```java
		if (bytes == null)
		{
			return null;
		}
		try
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/markup/xhtml/WellFormedXmlTestCase.java`
#### Snippet
```java
			}

			return null;
		}
	};
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/watch/ModificationWatcher.java`
#### Snippet
```java
			return entry.modifiable;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringEscapeUtils.java`
#### Snippet
```java
		if (str == null)
		{
			return null;
		}
		return Entities.XML.unescape(str);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringEscapeUtils.java`
#### Snippet
```java
		if (str == null)
		{
			return null;
		}
		try
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringEscapeUtils.java`
#### Snippet
```java
		if (str == null)
		{
			return null;
		}
		try
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringEscapeUtils.java`
#### Snippet
```java
		if (str == null)
		{
			return null;
		}
		return Entities.XML.escape(str);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ByteConverter.java`
#### Snippet
```java
		if (number == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractConverter.java`
#### Snippet
```java
		if (value == null)
		{
			return null;
		}
		return value.toString();
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
	public final Duration toOptionalDuration() throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : toDuration();
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
	public final Instant toOptionalInstant() throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : toInstant();
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
	public final Double toOptionalDouble() throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : toDoubleObject();
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
	public final <T> T toOptional(final Class<T> type) throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : to(type);
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
	public final Long toOptionalLong() throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : toLongObject();
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
		if (type == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
	public final Character toOptionalCharacter() throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : toCharacter();
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
	public final Integer toOptionalInteger() throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : toInteger();
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
		throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : toEnum(eClass);
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
	public final Boolean toOptionalBoolean() throws StringValueConversionException
	{
		return Strings.isEmpty(text) ? null : toBooleanObject();
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/DoubleConverter.java`
#### Snippet
```java
		if (number == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ShortConverter.java`
#### Snippet
```java
		if (number == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		if (o == null)
		{
			return null;
		}
		else if (o.getClass().isArray() && (Array.getLength(o) > 0))
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
			if (arrayValue == null)
			{
				return null;
			}
			else
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		if (!containsKey(key))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		catch (StringValueConversionException ignored)
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		if (!containsKey(key))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		catch (StringValueConversionException ignored)
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		if (o == null)
		{
			return null;
		}
		else if (o instanceof String[])
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		if (!containsKey(key))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		catch (StringValueConversionException ignored)
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		if (o == null)
		{
			return null;
		}
		else if (o.getClass().isArray() && (Array.getLength(o) > 0))
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
			if (arrayValue == null)
			{
				return null;
			}
			else
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		if (!containsKey(key))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		catch (StringValueConversionException ignored)
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/IntegerConverter.java`
#### Snippet
```java
		if (number == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/CalendarConverter.java`
#### Snippet
```java
		if (date == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/CalendarConverter.java`
#### Snippet
```java
		if (value == null)
		{
			return null;
		}
		return dateConverter.convertToString(value.getTime(), locale);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractNumberConverter.java`
#### Snippet
```java
		if (value == null)
		{
			return null;
		}
		else if (value instanceof String)
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractNumberConverter.java`
#### Snippet
```java
		if (number == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/FloatConverter.java`
#### Snippet
```java
		if (number == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractJavaTimeConverter.java`
#### Snippet
```java
		if (Strings.isEmpty(value))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractJavaTimeConverter.java`
#### Snippet
```java
		if (value == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/CharacterConverter.java`
#### Snippet
```java
		if (length == 0)
		{
			return null;
		}
		else if (length == 1)
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/BigIntegerConverter.java`
#### Snippet
```java
		if (number == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LongConverter.java`
#### Snippet
```java
		if (number == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractDateConverter.java`
#### Snippet
```java
		if (Strings.isEmpty(value))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractDateConverter.java`
#### Snippet
```java
		if (value == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/license/AbstractLicenseHeaderHandler.java`
#### Snippet
```java
	public String getLicenseType(final File file)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
				}
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
			if (index < 0)
			{
				return null;
			}
			return names[index];
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
	public String getContentType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
	protected Map<Object, Object> getParameters()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
	public Instant lastModifiedTime()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStreamWriter.java`
#### Snippet
```java
	public String getContentType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStreamWriter.java`
#### Snippet
```java
	public Bytes length()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java
	{
		// java.nio.Charset is not serializable so we can only store the name
		return (charsetName != null) ? Charset.forName(charsetName) : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStream.java`
#### Snippet
```java
	public Instant lastModifiedTime()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStream.java`
#### Snippet
```java
	public Bytes length()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStream.java`
#### Snippet
```java
	public String getContentType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (path == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}
		final char[] chars = s.toCharArray();
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (str == null)
		{
			return null;
		}
		Matcher matcher = HTML_NUMBER_REGEX.matcher(str);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (path == null)
		{
			return null;
		}
		final int index = path.indexOf(separator);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}
		final int index = s.indexOf(c);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}
		
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}
		final int index = s.indexOf(c);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (object == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}
		final int index = s.lastIndexOf(c);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s == null)
		{
			return null;
		}
		final int index = s.lastIndexOf(c);
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/FileResourceStream.java`
#### Snippet
```java
			return file.lastModifiedTime();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/FileResourceStream.java`
#### Snippet
```java
			return file.getAbsolutePath();
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/FileResourceStream.java`
#### Snippet
```java
			return Bytes.bytes(file.length());
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MultiMap.java`
#### Snippet
```java
			return list.get(0);
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/ClassMetaCache.java`
#### Snippet
```java
		if (container == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java
	public Instant lastModifiedTime()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java
	public String getContentType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/Stringz.java`
#### Snippet
```java
	static String className(final Object o)
	{
		return (o != null) ? o.getClass().getName() : null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public AsyncContext getAsyncContext()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	@Override
	public ServletConnection getServletConnection() {
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getRemoteHost()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	{
		String[] values = parametersMap.get(name);
		return values != null ? values[0] : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public <T extends HttpUpgradeHandler> T upgrade(Class<T> tClass) throws IOException, ServletException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getRequestedSessionId()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getCharacterEncoding()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getProtocol()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getScheme()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public RequestDispatcher getRequestDispatcher(String path)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getLocalName()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public ServletContext getServletContext()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getMethod()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
			public String nextElement()
			{
				return null;
			}
		};
```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public AsyncContext startAsync(ServletRequest servletRequest, ServletResponse servletResponse) throws IllegalStateException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public Object getAttribute(String name)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public Locale getLocale()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public BufferedReader getReader() throws IOException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getRemoteAddr()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getServletPath()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String changeSessionId()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getContentType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getLocalAddr()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getPathTranslated()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getAuthType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	@Override
	public String getProtocolRequestId() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public ServletInputStream getInputStream() throws IOException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public AsyncContext startAsync() throws IllegalStateException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getServerName()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getPathInfo()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public Part getPart(String name) throws IOException, ServletException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public String getRemoteUser()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public Enumeration<Locale> getLocales()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public Collection<Part> getParts() throws IOException, ServletException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public DispatcherType getDispatcherType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
			return logger.getLiveSessions().length;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
			return Integer.valueOf(logger.getPeakActiveRequestCount());
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
			return Integer.valueOf(logger.getCurrentActiveRequestCount());
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
			return logger.getTotalCreatedSessions();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
			return logger.getPeakSessions();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
			return application.getRequestLogger();
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/ResourceSettings.java`
#### Snippet
```java
	{
		Duration duration = application.getResourceSettings().getResourcePollFrequency();
		return (duration != null) ? duration.toString() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/ResourceSettings.java`
#### Snippet
```java
			return list.toArray(new String[0]);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/Initializer.java`
#### Snippet
```java
				withApplication(() -> {
					super.setAttribute(attribute);
					return null;
				});
			}
```

### ReturnNull
Return of `null`
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/SessionLocaleInterpolator.java`
#### Snippet
```java
	private Locale getLocale()
	{
		return Session.exists() ? Session.get().getLocale() : null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/DefaultPropertyResolver.java`
#### Snippet
```java
		if (delegate == null)
		{
			return null;
		}
		
```

### ReturnNull
Return of `null`
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/DefaultPropertyResolver.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

		// Failed to find key
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/ValidationModelResolver.java`
#### Snippet
```java
            break; // not model found
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/DefaultViolationTranslator.java`
#### Snippet
```java
		}
	
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
			if (e == null)
			{
				return null;
			}
			if (key == e.key)
```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
		modCount.incrementAndGet();
		addEntry(key, value, i);
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		Entry<V> e = removeEntryForKey(key);
		return (e == null ? null : e.value);
	}

```

### ReturnNull
Return of `null`
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
		if (!(o instanceof Entry))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/bytebuddy/ByteBuddyInterceptor.java`
#### Snippet
```java
		{
			// swallow finalize call
			return null;
		}
		else if (LazyInitProxyFactory.isEqualsMethod(method))
```

### ReturnNull
Return of `null`
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/BeanValidationConfiguration.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-ioc/src/main/java/org/apache/wicket/injection/NoopFieldValueFactory.java`
#### Snippet
```java
	public Object getFieldValue(final Field field, final Object fieldOwner)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java
			{
				// swallow finalize call
				return null;
			}
			else if (LazyInitProxyFactory.isEqualsMethod(method))
```

### ReturnNull
Return of `null`
in `wicket-spring/src/main/java/org/apache/wicket/spring/FieldBeansCollector.java`
#### Snippet
```java
		}
		
		return null;
	}
	
```

### ReturnNull
Return of `null`
in `wicket-ioc/src/main/java/org/apache/wicket/injection/CompoundFieldValueFactory.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
		}

		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
		}
		
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
			else
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
				else
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
			return target;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
			if (generic != null)
			{
				return null;
			}
			
```

### ReturnNull
Return of `null`
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
		}
		
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/PageStorePage.java`
#### Snippet
```java
		}
		
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/RequestHandlerExecutor.java`
#### Snippet
```java
		{
			// don't do anything here
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/HttpHeaderCollection.java`
#### Snippet
```java
		if (objects.isEmpty())
		{
			return null;
		}
		Object object = objects.get(0);
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/HttpHeaderCollection.java`
#### Snippet
```java
		if (objects == null || objects.isEmpty())
		{
			return null;
		}
		return valueToString(objects.get(0));
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/http/WebRequest.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/CompoundRequestMapper.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/CompoundRequestMapper.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/PageInfo.java`
#### Snippet
```java
			catch (NumberFormatException e)
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/PageComponentInfo.java`
#### Snippet
```java
		if (Strings.isEmpty(s))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/PageComponentInfo.java`
#### Snippet
```java
		if (pageInfo == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/parameter/PageParametersEncoder.java`
#### Snippet
```java
		}

		return parameters.isEmpty() ? null : parameters;
	}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
		if (Strings.isEmpty(string))
		{
			return null;
		}
		int i = string.indexOf(SEPARATOR);
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
		if (i == -1)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
			else
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/AbstractMapper.java`
#### Snippet
```java
		if (s == null || s.length() < 4)
		{
			return null;
		}
		else if (s.charAt(0) != startChar || s.charAt(1) != '{' || s.charAt(s.length() - 1) != '}')
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/AbstractMapper.java`
#### Snippet
```java
		else if (s.charAt(0) != startChar || s.charAt(1) != '{' || s.charAt(s.length() - 1) != '}')
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/parameter/UrlPathPageParametersEncoder.java`
#### Snippet
```java
		}

		return params.isEmpty() ? null : params;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/EmptyRequestParameters.java`
#### Snippet
```java
	public List<StringValue> getParameterValues(final String name)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPanel.java`
#### Snippet
```java
		IPersistentPageStore store = getModelObject();
		if (store == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/UrlRequestParametersAdapter.java`
#### Snippet
```java
			}
		}
		return values != null ? Collections.unmodifiableList(values) : null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/CombinedRequestParametersAdapter.java`
#### Snippet
```java
		if (result.isEmpty())
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanelTester.java`
#### Snippet
```java
		if (panel.isLoaded())
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
				Page page = getModelObject();
				if (page == null)
					return null;
				return buildTree(page, null);
			}
```

### ReturnNull
Return of `null`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
				return model;
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/form/upload/UploadProgressBar.java`
#### Snippet
```java
		if (form == null)
		{
			return null;
		}
		return form.getRootForm();
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AbstractAutoCompleteRenderer.java`
#### Snippet
```java
	protected CharSequence getOnSelectJavaScriptExpression(final T item)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
			else
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
			catch (IOException ex)
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
			return converter;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		List<QueryParameter> queryParameters = getQueryParameters();
		if (queryParameters.isEmpty())
			return null;
		if (queryParameters.size() == 1)
			return queryParameters.get(0).toString(charset);
```

### ReturnNull
Return of `null`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/component/AbstractOptions.java`
#### Snippet
```java
	protected String getModelValue()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/component/AbstractOptions.java`
#### Snippet
```java
	protected Map<String, String> getAdditionalAttributes(final T choice)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/ZonedToLocalDateTimeModel.java`
#### Snippet
```java
		if (zonedDateTime == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/ZonedToLocalDateTimeModel.java`
#### Snippet
```java
		{
			TimeZone timeZone = ((WebClientInfo)info).getProperties().getTimeZone();
			return timeZone != null ? timeZone.toZoneId() : null;
		}
		return ZoneId.systemDefault();
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
	protected Map<String, String> getAdditionalAttributesForChoices(final Object choice)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
	protected Map<String, String> getAdditionalAttributesForSelection(final Object choice)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeTextField.java`
#### Snippet
```java
			return converter;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalTimeTextField.java`
#### Snippet
```java
			return converter;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
			T temporal = getModelObject();
			if (temporal == null) {
				return null;
			}
			
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
			T temporal = getModelObject();
			if (temporal == null) {
				return null;
			}
			
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
	protected LocalTime getDefaultTime()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java
				if (Strings.isEmpty(value))
				{
					return null;
				}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java
			return converter;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/AbstractColumn.java`
#### Snippet
```java
	public String getCssClass()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/LambdaColumn.java`
#### Snippet
```java
				
				if (before == null) {
					return null;
				} else {
					return function.apply(before);
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
	protected IModel<String> getCaptionModel()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
		{
			// don't try to find the model in the parent
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
		{
			LocalTime t = TimeField.this.getModelObject();
			return t == null ? null : t.getMinute();
		}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
					return MINUTES_CONVERTER;
				}
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
			if (t == null)
			{
				return null;
			}
			return use12HourFormat() ? t.get(ChronoField.CLOCK_HOUR_OF_AMPM) : t.getHour();
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/ChoiceFilteredPropertyColumn.java`
#### Snippet
```java
	protected IChoiceRenderer<Y> getChoiceRenderer()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/NestedTree.java`
#### Snippet
```java
		public T getObject()
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardButtonBar.java`
#### Snippet
```java
			return (WizardButton)get("finish");
		}
		return null;
	}
	
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardModel.java`
#### Snippet
```java
	public Iterator<IWizardStep> stepIterator()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java

			// No animal found
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
			{
				LOG.error("Error loading image", e);
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
		{
			LOG.error("Error loading image", e);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java

		// Could not place animal
		return null;
	}
	
```

### ReturnNull
Return of `null`
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/WicketMetrics.java`
#### Snippet
```java
	private Object proceedSilent(ProceedingJoinPoint joinPoint) throws Throwable
	{
		return joinPoint != null ? joinPoint.proceed() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
				return components.toArray(new FormComponent[components.size()]);
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	public String getTitle()
	{
		return (title != null) ? title.getObject() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	public String getSummary()
	{
		return (summary != null) ? summary.getObject() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/StaticContentStep.java`
#### Snippet
```java
	public final String getContent()
	{
		return (content != null) ? (String)content.getObject() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/FileItemHeadersImpl.java`
#### Snippet
```java
        final List<String> headerValueList = getList(name);
        if (null == headerValueList) {
            return null;
        }
        return headerValueList.get(0);
```

### ReturnNull
Return of `null`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/AbstractFileUpload.java`
#### Snippet
```java

        if (boundaryStr == null) {
            return null;
        }
        final byte[] boundary;
```

### ReturnNull
Return of `null`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
    public File getStoreLocation() {
        if (dfos == null) {
            return null;
        }
        if (isInMemory()) {
```

### ReturnNull
Return of `null`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
        }
        if (isInMemory()) {
            return null;
        }
        return dfos.getFile();
```

### ReturnNull
Return of `null`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/mime/MimeUtility.java`
#### Snippet
```java
        // nothing in, nothing out.
        if (charset == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/MetaDataRoleAuthorizationStrategy.java`
#### Snippet
```java
			return permissions.authorizedRoles(componentClass);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/MetaDataRoleAuthorizationStrategy.java`
#### Snippet
```java
			return permissions.rolesFor(action);
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ThreadContext.java`
#### Snippet
```java
	{
		ThreadContext context = get(false);
		return context != null ? context.requestCycle : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ThreadContext.java`
#### Snippet
```java
	{
		ThreadContext context = get(false);
		return context != null ? context.session : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ThreadContext.java`
#### Snippet
```java
	{
		ThreadContext context = get(false);
		return context != null ? context.application : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ComponentQueue.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ComponentQueue.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/AttributeModifier.java`
#### Snippet
```java
	private String toStringOrNull(final Object replacementValue)
	{
		return (replacementValue != null) ? replacementValue.toString() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/AttributeModifier.java`
#### Snippet
```java
			model = ((IComponentAssignedModel<?>)model).wrapOnAssignment(component);
		}
		return (model != null) ? model.getObject() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/DefaultExceptionMapper.java`
#### Snippet
```java
			return (Page)pageRequestHandler.getPage();
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/DefaultExceptionMapper.java`
#### Snippet
```java
		}
		
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
			return list;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java
			if (value == null)
			{
				return null;
			}
			Class<C> theType = type.get();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java
					return theType.cast("");
				}
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
				return Session.get().getMetaData(key);
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
		public Url mapHandler(IRequestHandler requestHandler)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
				return data;
			}
			return null;
		}
	};
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
				InterceptData.clear();
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/UrlResourceReferenceMapper.java`
#### Snippet
```java
	public IRequestHandler mapRequest(Request request)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/DequeueContext.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/DequeueContext.java`
#### Snippet
```java
		if (taken == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/DequeueContext.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/DefaultMarkupIdGenerator.java`
#### Snippet
```java
		if (storedMarkupId == null && createIfDoesNotExist == false)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/csp/CSPDirective.java`
#### Snippet
```java
		if (Strings.isEmpty(value))
		{
			return null;
		}
		for (int i = 0; i < values().length; i++)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/csp/CSPDirective.java`
#### Snippet
```java
			}
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/csp/ReportCSPViolationMapper.java`
#### Snippet
```java
	public Url mapHandler(IRequestHandler requestHandler)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/csp/ReportCSPViolationMapper.java`
#### Snippet
```java
			};
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
		if (cache == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
		if (NULL_VALUE == value)
		{
			return null;
		}
		return value;
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java
		
		// WICKET-6568 might contain non-ASCII
		return Strings.isEmpty(id) ? null : UrlDecoder.QUERY_INSTANCE.decode(id, request.getCharset());
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
			parent = parent.getParent();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getPrecondition(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getInitHandler(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getFailureHandler(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getCompleteHandler(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getBeforeSendHandler(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getSuccessHandler(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getDoneHandler(Component component)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getBeforeHandler(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	default CharSequence getAfterHandler(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolverConverter.java`
#### Snippet
```java
		if (object == null)
		{
			return null;
		}
		if (clz.isAssignableFrom(object.getClass()))
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/AbstractJceCrypt.java`
#### Snippet
```java
		{
			log.debug("Error decoding text: {}", text, ex);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/WicketObjects.java`
#### Snippet
```java
		if (object == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/WicketObjects.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/interpolator/PropertyVariableInterpolator.java`
#### Snippet
```java
			return toString(value);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/ClassPathResourceFinder.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/ClassPathResourceFinder.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/WebExternalResourceStream.java`
#### Snippet
```java
	public Bytes length()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/WebExternalResourceStream.java`
#### Snippet
```java
		{
			log.warn("failed to retrieve last modified timestamp", e);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/UrlResourceStream.java`
#### Snippet
```java

			// allow modification watcher to detect the problem
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/UrlResourceStream.java`
#### Snippet
```java
		if (length == -1)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		public String getSessionId(Request request, boolean create)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		public Serializable getAttribute(Request request, String name)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		public Session lookup(Request request)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		public List<String> getAttributeNames(Request request)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		{
			// no caching for this page
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		public Set<BindListener> getBindListeners()
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		public Set<UnboundListener> getUnboundListener()
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		public Object deserialize(byte[] data)
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		public byte[] serialize(Object object)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
		inThreadContext(() -> {
			runnable.run();
			return null;
		});
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/LocaleResourceNameIterator.java`
#### Snippet
```java
			return new Locale(locale.getLanguage());
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java
	public final String getVariation()
	{
		return ((state == 1) || (state == 3)) ? variation : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java
	public final String getStyle()
	{
		return ((state == 1) || (state == 2)) ? style : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java
			if ((style != null) && (variation != null))
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java
			if (style != null)
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java
			if (variation != null)
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java

		state = 4;
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/caching/NullResourceStreamReference.java`
#### Snippet
```java
	public IResourceStream getReference()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			// Map can't make a newValue or should it look what is more in the
			// map and try to make one of the class if finds?
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
		public Class<?> getTargetClass()
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			// List can't make a newValue or should it look what is more in the
			// list and try to make one of the class if finds?
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
				return Array.get(object, index);
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
		public Method getSetter()
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
				log.debug("Can't find setter method corresponding to {}", getMethod);
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
		public Method getGetter()
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			if (((List<?>)object).size() <= index)
			{
				return null;
			}
			return ((List<?>)object).get(index);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			@Override
			public Object newValue(Object object) {
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			{
				log.warn("Null setMethod");
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
		if (objectWithGetAndSet == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
					if (nextValue == null)
					{
						return null;
					}
				}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
				else
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
		public Field getField()
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			@Override
			public Object getValue(Object object) {
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			{
				log.warn("Null setMethod");
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
				log.debug("Cannot find setter corresponding to " + getMethod, e);
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
						if (checked.containsKey(streamObj))
						{
							return null;
						}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MapperUtils.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MapperUtils.java`
#### Snippet
```java
		}

		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BookmarkableMapper.java`
#### Snippet
```java
			if (Application.get().getSecuritySettings().getEnforceMounts())
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BookmarkableMapper.java`
#### Snippet
```java
			if (Strings.isEmpty(className))
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BookmarkableMapper.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
		if ((requestHandler instanceof ResourceReferenceRequestHandler) == false)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
		if (resourceReference.equals(handler.getResourceReference()) == false)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
		if (urlStartsWith(url, mountSegments) == false)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java
				if (segment.indexOf('/') > -1)
				{
					return null;
				}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java
			return url;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
			{
				// mandatory parameter is not provided => cannot build Url
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
			if (segments.size() <= mountSegments.length)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
			if (Strings.isEmpty(name))
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
			if (isValidClassName(className) == false)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java
	public Url mapHandler(IRequestHandler requestHandler)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/StalePageException.java`
#### Snippet
```java
	{
		// don't do anything here
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		if (markup == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			if (container == null)
			{
				return null;
			}
			path = path.length() == id.length() ? "" : path.substring(id.length() + 1);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	private LinkedList<RemovedChild> removals_get()
	{
		return getRequestFlag(RFLAG_CONTAINER_HAS_REMOVALS) ? getMetaData(REMOVALS_KEY) : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		if (children == null)
		{
			return null;
		}
		if (children instanceof Component)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		{
			Component child = children();
			return child.getId().equals(childId) ? child : null;
		}
		if (children instanceof List)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
				}
			}
			return null;
		}
		Map<String, Component> kids = children();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			modCounter++;

			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
				// it is an addtion, so we need to notify the iterators of this change.
				modCounter++;
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
				children = newChildren;
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			else
			{
				return null; // don't know
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			}

			return null;
		}
		catch (MarkupException ex)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			return parent.getMarkupType();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	public Component findComponentToDequeue(ComponentTag tag)
	{
		return queue == null ? null : queue.remove(tag.getId());
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/HomePageMapper.java`
#### Snippet
```java
		{
			// home page cannot have segments/indexed parameters
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
		if (url == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
		if (encryptedSegments.isEmpty())
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
		if (Strings.isEmpty(encryptedUrlString))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
			else
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
		{
			log.error("Error decrypting URL", e);
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
			else
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
					&& request.getOriginalUrl().getSegments().get(0).equals(getContext().getNamespace()))
				{
					return null;
				}
				else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
				else
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
		if (url == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/IPageRequestHandler.java`
#### Snippet
```java
			}
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MountedMapper.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MountedMapper.java`
#### Snippet
```java
		{
			// mandatory parameter is not provided => cannot build Url
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/PageProvider.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/logger/PageLogData.java`
#### Snippet
```java
			return function.get();
		} catch (Exception ex) {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockPageStore.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/logger/ListenerLogData.java`
#### Snippet
```java
			{
				final IFormSubmitter submitter = ((Form<?>)component).findSubmitter();
				return submitter instanceof Component ? (Component)submitter : null;
			}
			return null;
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/logger/ListenerLogData.java`
#### Snippet
```java
				return submitter instanceof Component ? (Component)submitter : null;
			}
			return null;
		});
		if (formSubmitter != null)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockApplication.java`
#### Snippet
```java
	public final String getInitParameter(String key)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
			}
			if (!foundFixedPart)
				return null;
			pathSegmentIndex++;
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
		segmentIndex += lastSegment.getMinParameters();
		if (segmentIndex > url.getSegments().size())
			return null;
		ret[pathSegmentIndex] = Math.min(lastSegment.getMaxParameters(), url.getSegments().size() -
				segmentIndex + lastSegment.getMinParameters());
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
			if (!checkPageClass(handler.getPageClass()))
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
			if (!checkPageClass(handler.getPageClass()))
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
			else
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
			if (!checkPageClass(pageClass))
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java

		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockRequestParameters.java`
#### Snippet
```java
	{
		List<StringValue> values = parameters.get(name);
		return values != null ? Collections.unmodifiableList(values) : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebResponse.java`
#### Snippet
```java
		if (binaryResponse == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTesterHelper.java`
#### Snippet
```java
			return behavior;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebRequest.java`
#### Snippet
```java
	{
		List<Object> h = headers.get(name);
		return (h == null || h.isEmpty()) ? null : h.get(0).toString();
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebRequest.java`
#### Snippet
```java
		if (dates == null || dates.isEmpty())
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 */
	protected IConverter<?> createConverter(Class<?> type) {
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java

		// Failed to find component
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			return parent.getVariation();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		if (data == null)
		{
			return null;
		}
		else if (data instanceof Object[] && !(data instanceof MetaDataEntry<?>[]))
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		{
			Object[] array = (Object[])data;
			return index < array.length ? array[index] : null;
		}
		else if (index == 0)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			current = current.getParent();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			return (IModel<?>)data_get(0);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	protected IMarkupSourcingStrategy newMarkupSourcingStrategy()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java
		if (cookie == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java
			return cookie.getValue();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
		if ((tester == null) || (tester.size() == 0))
		{
			return null;
		}
		return tester.get(0);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
		if (openTag == closeTag)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
		if ((tester == null) || (tester.size() == 0))
		{
			return null;
		}
		return tester.get(0);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
			{
				fail("Trying to select on null component.");
				return null;
			}
			else if (formComponent instanceof RadioGroup ||
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
				fail("Selecting on the component:'" + formComponent.getPath() +
					"' is not supported.");
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
				// component without getChoices() or getChoiceRenderer() is not
				// selectable
				return null;
			}
			catch (IllegalAccessException e)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
			return ((AbstractTextComponent<?>)c).getValue();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
				{
					fail("RadioGroup " + formComponent.getPath() + " does not have index:" + index);
					return null;
				}
				return foundRadio.getValue();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
				{
					fail("CheckGroup " + formComponent.getPath() + " does not have index:" + index);
					return null;
				}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
				{
					fail(formComponent.getPath() + " is not a selectable Component.");
					return null;
				}
				else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/IWrapModel.java`
#### Snippet
```java
	default T getObject()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentModel.java`
#### Snippet
```java
	protected T getObject(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/ChainingModel.java`
#### Snippet
```java
			return (IModel<?>)target;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentDetachableModel.java`
#### Snippet
```java
	protected T getObject(Component component)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/Model.java`
#### Snippet
```java
	public Class<T> getObjectClass()
	{
		return object != null ? (Class<T>) object.getClass() : null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/util/GenericBaseModel.java`
#### Snippet
```java
	public Class<T> getObjectClass()
	{
		return object != null ? (Class<T>) object.getClass() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
		{
			// Return a meaningful value for an empty property expression
			return (Class<T>)(target != null ? target.getClass() : null);
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java

		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
			return (T)PropertyResolver.getValue(expression, target);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/util/ListModel.java`
#### Snippet
```java
		if (object == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
				} else
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
				if (object == null)
				{
					return null;
				} else
				{
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
				else
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
					else 
					{
						return null;
					}
				}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
				else
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
				if (x == null)
				{
					return null;
				}
				return getter.apply(x);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/ContainerInfo.java`
#### Snippet
```java
	public String getFileExtension()
	{
		return markupType != null ? markupType.getExtension() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/DefaultMarkupResourceStreamProvider.java`
#### Snippet
```java
		}

		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFragment.java`
#### Snippet
```java
		if (size < 2)
		{
			return null;
		}
		return find(id, 1);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
	public Class<? extends Component> getMarkupClass()
	{
		return (markupClassRef == null ? null : markupClassRef.get());
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
		if (userData == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
		if ((l1 == null) && (l2 == null))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
			stream.next();
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
			if (key == null)
			{
				return null;
			}
			return cache.get(key);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
		if (markup == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java
			// TODO improve: Result { boolean success, enum FailureReason {not found, not yet
			// available}, Markup markup }
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java
			// TODO improve: Result { boolean success, enum FailureReason {not found, not yet
			// available}, Markup markup }
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java
			// TODO improve: Result { boolean success, enum FailureReason {not found, not yet
			// available}, Markup markup }
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java
			// TODO improve: Result { boolean success, enum FailureReason {not found, not yet
			// available}, Markup markup }
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java

		// Markup not found. Errors should throw a Wicket exception
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupResourceStream.java`
#### Snippet
```java
			return ((IFixedLocationResourceStream)resourceStream).locationAsString();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupResourceStream.java`
#### Snippet
```java
		if (baseMarkup == null)
		{
			return null;
		}
		return baseMarkup.getMarkupResourceStream();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
		throwMarkupException("Tag expected");

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/CssReferenceHeaderItem.java`
#### Snippet
```java
	public CrossOrigin getCrossOrigin()
	{
		return null;
	}
	
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/CssReferenceHeaderItem.java`
#### Snippet
```java
	public String getIntegrity()
	{
		return null;
	}
	
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
			// NOOP
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/MarkupUtil.java`
#### Snippet
```java
		}

		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java

		fail("Last response has no AJAX base URL set by AbstractDefaultAjaxBehavior.");
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		{
			fail("path: '" + path + "' is not a form component");
			return null;
		}
		return (FormComponent<?>)component;
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
						Classes.simpleName(getLastRenderedPage().getClass()));
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java

		// Not found or not visible
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
			fail("path: '" + path + "' does not exist for page: " +
				Classes.simpleName(getLastRenderedPage().getClass()));
			return null;
		}
		return component;
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	protected Duration getResourcePollFrequency()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java
		{
			// even having a wicket:id it isn't a component's markup
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java
			 * their grandchildren.
			 */
			return null;
		}
		return resolvedComponent;
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelTextResolver.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelTextResolver.java`
#### Snippet
```java
			}

			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	protected IValueMap getAdditionalAttributesForLabel(int index, T choice)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	protected IValueMap getAdditionalAttributes(final int index, final T choice)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/TextField.java`
#### Snippet
```java
	protected String[] getInputTypes()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
		if (!tag.getId().startsWith(LABEL_ATTR))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Button.java`
#### Snippet
```java
	protected String getOnClickScript()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Button.java`
#### Snippet
```java
	protected IModel<String> initModel()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/SubmitLink.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/IChoiceRenderer.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java
			return choice.getModelObject();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java
			return CheckBoxConverter.INSTANCE;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	public final String getRawInput()
	{
		return NO_RAW_INPUT.equals(rawInput) ? null : rawInput;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUploadField.java`
#### Snippet
```java
		List<FileUpload> fileUploads = getFileUploads();

		return fileUploads.isEmpty() ? null : fileUploads.get(0) ;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	public final Class<T> getType()
	{
		return typeName == null ? null : (Class<T>)WicketObjects.resolveClass(typeName);
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUploadField.java`
#### Snippet
```java
			return clientFileNames.toArray(new String[clientFileNames.size()]);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
			return getForm().getValidatorKeyPrefix();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUploadField.java`
#### Snippet
```java
		if (filenames == null)
		{
			return null;
		}
		return getFileUploads();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
		if (input == null || input.length == 0)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	protected T convertValue(String[] value) throws ConversionException
	{
		return (T)(value != null && value.length > 0 && value[0] != null ? trim(value[0]) : null);
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
			return ints;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	public String getValidatorKeyPrefix()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/FileUploadToResourceField.java`
#### Snippet
```java
            if (file == null)
            {
               return null;
            }

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/AbstractFileUploadResource.java`
#### Snippet
```java
	{
		long fileMaxSize = webRequest.getRequestParameters().getParameterValue("fileMaxSize").toLong(-1);
		return fileMaxSize > 0 ? Bytes.bytes(fileMaxSize) : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
	protected CharSequence getOnClickScript(final CharSequence url)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
		if (hrefValue == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
	public final CrossOrigin getCrossOrigin()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	protected IValueMap getAdditionalAttributesForLabel(int index, T choice)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	protected IValueMap getAdditionalAttributes(final int index, final T choice)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java
		// model, explicitly set, or they use their tag's src or value
		// attribute to determine the image.
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalSource.java`
#### Snippet
```java
	public final CrossOrigin getCrossOrigin()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/DefaultMarkupSourcingStrategy.java`
#### Snippet
```java
		if (containerMarkup == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/BorderMarkupSourcingStrategy.java`
#### Snippet
```java
	public IMarkupFragment getMarkup(final MarkupContainer container, final Component child)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/Fragment.java`
#### Snippet
```java
		if (markup == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/include/Include.java`
#### Snippet
```java
	public Charset getCharset()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
					if (index >= listViewModel.getObject().size())
					{
						return null;
					}
					else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
		if (markupType == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
			if (fragment == null)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
			if (markup == null)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
			}

			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
				cursor = cursor.getParent();
			}
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/TagStack.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RootMarkupFilter.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RootMarkupFilter.java`
#### Snippet
```java
	public final IMarkupFilter getNextFilter()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/EnclosureHandler.java`
#### Snippet
```java

		// We were not able to handle the tag
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java
			return new TransparentWebMarkupContainer(tag.getId());
		}
		return null;
	}
	
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java

		// We were not able to handle the tag
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketNamespaceHandler.java`
#### Snippet
```java
		}

		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketMessageTagHandler.java`
#### Snippet
```java
			return wc;
		}
		return null;
	}
	
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketLinkTagHandler.java`
#### Snippet
```java

		// We were not able to handle the tag
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/EmptyDataProvider.java`
#### Snippet
```java
	public IModel<T> model(Object object)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketContainerResolver.java`
#### Snippet
```java
			return container.get(tag.getId());
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/ComponentResolvers.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/ComponentResolvers.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java

		// We were not able to handle the tag
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

			// fall back on default processing
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		// We were not able to resolve the id
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

			// fallthrough
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/PageRequestHandlerTracker.java`
#### Snippet
```java
	    	return findPageRequestHandler(((IRequestHandlerDelegate)handler).getDelegateHandler());
	    }
	    return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/IRequestCycleListener.java`
#### Snippet
```java
	default IRequestHandler onException(RequestCycle cycle, Exception ex)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/handler/render/PageRenderer.java`
#### Snippet
```java
	protected String getSessionId()
	{
		return Session.exists() ? Session.get().getId() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycleListenerCollection.java`
#### Snippet
```java
		if (handlers.isEmpty())
		{
			return null;
		}
		if (handlers.size() > 1)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/UrlResourceReference.java`
#### Snippet
```java
	public final IResource getResource()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java

		// We were not able to handle the tag
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/handler/render/WebPageRenderer.java`
#### Snippet
```java
		{
			// no need to render
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/handler/render/WebPageRenderer.java`
#### Snippet
```java
				originalResponse.reset();
				response.writeMetaData(originalResponse);
				return null;
			}
			else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/CharSequenceResource.java`
#### Snippet
```java
	protected Charset getCharset()
	{
		return (charsetName != null) ? Charset.forName(charsetName) : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReferenceRegistry.java`
#### Snippet
```java

		log.warn("{} cannot be added to the registry.", reference.getClass().getName());
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/MetaInfStaticResourceReference.java`
#### Snippet
```java
		if (!isMetaInfResourcesSupported())
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResourceReference.java`
#### Snippet
```java
				: attributes != null
					? attributes.getVariation()
					: null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResourceReference.java`
#### Snippet
```java
				: attributes != null
					? attributes.getStyle()
					: null;
	}
	
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResourceReference.java`
#### Snippet
```java
				: attributes != null
					? attributes.getLocale()
					: null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/LastModifiedResourceVersion.java`
#### Snippet
```java
		if (stream == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/LastModifiedResourceVersion.java`
#### Snippet
```java
		if (lastModified == null)
		{
			return null;
		}
		// version string = last modified timestamp converted to milliseconds
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/CachingResourceVersion.java`
#### Snippet
```java
		if(key == null)
		{
			return null;
		}
		
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/CachingResourceVersion.java`
#### Snippet
```java
		{
			// replace holder with null value
			return null;
		}
		
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/MessageDigestResourceVersion.java`
#### Snippet
```java
		if (stream == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/MessageDigestResourceVersion.java`
#### Snippet
```java
		{
			log.warn("unable to compute hash for " + resource, e);
			return null;
		}
		catch (ResourceStreamNotFoundException e)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/MessageDigestResourceVersion.java`
#### Snippet
```java
		{
			log.warn("unable to locate resource for " + resource, e);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	protected ITextResourceCompressor getCompressor()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/session/HttpSessionStore.java`
#### Snippet
```java
			return (Serializable)httpSession.getAttribute(getSessionAttributePrefix(request) + name);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/session/HttpSessionStore.java`
#### Snippet
```java
			return getWicketSession(request);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessages.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessages.java`
#### Snippet
```java
	public final FeedbackMessage first()
	{
		return messages.size() > 0 ? messages.get(0) : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/session/DefaultPageFactory.java`
#### Snippet
```java
					pageClass, argumentType);

				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
		if (extPos == -1)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/PageExpiredException.java`
#### Snippet
```java
	{
		// don't do anything here
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/OriginResourceIsolationPolicy.java`
#### Snippet
```java

		if (Strings.isEmpty(uri) || "null".equals(uri))
			return null;

		StringBuilder target = new StringBuilder();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/OriginResourceIsolationPolicy.java`
#### Snippet
```java
			if (scheme == null)
			{
				return null;
			}
			else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/OriginResourceIsolationPolicy.java`
#### Snippet
```java
			if (host == null)
			{
				return null;
			}
			target.append(host);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/OriginResourceIsolationPolicy.java`
#### Snippet
```java
		{
			log.debug("Invalid URI provided: {}, marked conflicting", uri);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/OriginResourceIsolationPolicy.java`
#### Snippet
```java
		if (scheme == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/OriginResourceIsolationPolicy.java`
#### Snippet
```java
		if (host == null)
		{
			return null;
		}
		target.append(host);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
		public Class<? extends IRequestHandler> getEventTargetClass()
		{
			return eventTarget == null ? null : eventTarget.getClass();
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
		public Class<? extends IRequestHandler> getResponseTargetClass()
		{
			return responseTarget == null ? null : responseTarget.getClass();
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java
		if (value == null)
		{
			return null;
		}
		return value.toString();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/StoredResponsesMap.java`
#### Snippet
```java
		}

		return removedValue != null ? removedValue.response : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
		else
		{
			return url != null ? url.toString() : null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
		else
		{
			return url != null ? url.toString() : null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
		else
		{
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/Cookies.java`
#### Snippet
```java
	public static Cookie copyOf(Cookie cookie)
	{
		return cookie != null ? (Cookie) cookie.clone() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletPartFileItem.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
			return pattern;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
			// requestURI and homePath are different (in length)
			// => continue with standard request processing. No redirect.
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java

		// no match => standard request processing; no redirect
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
				public Locale getLocale()
				{
					return null;
				}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
				public Charset getCharset()
				{
					return null;
				}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
				public Object getContainerRequest()
				{
					return null;
				}
			};
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
		if (l == null || l.size() < 1)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ForwardAttributes.java`
#### Snippet
```java
			return new ForwardAttributes(requestUri, servletPath, contextPath, queryString, pathInfo);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java
		if (header == null || header.getValue() == null || header.getValue().isEmpty())
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/WicketSessionFilter.java`
#### Snippet
```java
			return (Session)session.getAttribute(sessionKey);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ErrorAttributes.java`
#### Snippet
```java
			return new ErrorAttributes(code, message, uri, servlet, type, ex);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
			if (value == -1)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
		{
			// per spec thrown if the header contains a value that cannot be converted to a date
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public FilterRegistration.Dynamic addFilter(String filterName, Filter filter)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
			log.error(e.getMessage(), e);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
			log.error(e.getMessage(), e);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public JspConfigDescriptor getJspConfigDescriptor()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public <T extends Filter> T createFilter(Class<T> clazz) throws ServletException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public FilterRegistration.Dynamic addFilter(String filterName, String className)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public ClassLoader getClassLoader()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public FilterRegistration getFilterRegistration(String filterName)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
		if (index == -1 || index == (name.length() - 1))
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public Dynamic addJspFile(String s, String s1)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public Map<String, ? extends FilterRegistration> getFilterRegistrations()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public FilterRegistration.Dynamic addFilter(String filterName, Class<? extends Filter> filterClass)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
			}

			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	public <T extends EventListener> T createListener(Class<T> clazz) throws ServletException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
			if (!match)
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
			(attributes.getLocale() == null && attributes.getStyle() == null && attributes.getVariation() == null))
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
		if (Strings.isEmpty(locale))
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
			else
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/XmlFilePropertiesLoader.java`
#### Snippet
```java
	public ValueMap loadWicketProperties(InputStream inputStream)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/IsoPropertiesFilePropertiesLoader.java`
#### Snippet
```java
	public ValueMap loadWicketProperties(InputStream inputStream)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/UtfPropertiesFilePropertiesLoader.java`
#### Snippet
```java
	public ValueMap loadWicketProperties(InputStream inputStream)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemResource.java`
#### Snippet
```java
		protected Path load()
		{
			return pathAsString == null ? null : Paths.get(pathAsString);
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/PropertiesFactory.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/PackageStringResourceLoader.java`
#### Snippet
```java
		if (clazz == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/PackageStringResourceLoader.java`
#### Snippet
```java
		}
		// not found
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/BundleStringResourceLoader.java`
#### Snippet
```java
			catch (MissingResourceException mrx2)
			{
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/InitializerStringResourceLoader.java`
#### Snippet
```java

		// not found
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ValidatorStringResourceLoader.java`
#### Snippet
```java
				!(IValidator.class.isAssignableFrom(clazz) || IFormValidator.class.isAssignableFrom(clazz)))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ValidatorStringResourceLoader.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ValidatorStringResourceLoader.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/NestedStringResourceLoader.java`
#### Snippet
```java
		if (value == null)
		{
			return null;
		}
		
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/NestedStringResourceLoader.java`
#### Snippet
```java
			if (replacedPlaceHolder == null)
			{
				return null;
			}
			matcher.appendReplacement(output, replacedPlaceHolder);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/response/StringResponse.java`
#### Snippet
```java
	public Object getContainerResponse()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/response/StringResponse.java`
#### Snippet
```java
	public String encodeURL(CharSequence url)
	{
		return url != null ? url.toString() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
		if (clazz == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java

		// not found
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
		if (component == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		throws IllegalStateException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (bits.length < 1)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	@Override
	public ServletConnection getServletConnection() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (cookies.isEmpty())
		{
			return null;
		}
		List<Cookie> cookieValues = new ArrayList<Cookie>();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	public AsyncContext getAsyncContext()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (user == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (l == null || l.size() < 1)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (param == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	public AsyncContext startAsync() throws IllegalStateException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	@Override
	public String getProtocolRequestId() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/response/NullResponse.java`
#### Snippet
```java
	public Object getContainerResponse()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	public DispatcherType getDispatcherType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/response/NullResponse.java`
#### Snippet
```java
	public String encodeURL(CharSequence url)
	{
		return url != null ? url.toString() : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (parameters.size() == 0)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	public String getPathTranslated()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (cookies == null)
		{
			return null;
		}
		for (Cookie cookie : cookies)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	@Override
	public String getRequestId() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	public <T extends HttpUpgradeHandler> T upgrade(Class<T> aClass) throws IOException, ServletException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (session instanceof MockHttpSession && ((MockHttpSession)session).isTemporary())
		{
			return null;
		}
		return session.getId();
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/response/ByteArrayResponse.java`
#### Snippet
```java
		else
		{
			return url != null ? url.toString() : null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/settings/RequestCycleSettings.java`
#### Snippet
```java
		if (responseFilters == null)
		{
			return null;
		}
		else
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
					catch (IOException e)
					{
						return null;
					}
					catch (ResourceStreamNotFoundException e)
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
					catch (ResourceStreamNotFoundException e)
					{
						return null;
					}
				}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
		if (streams == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
			{
				reportError(curItem.getReference(), "Cannot get resource stream for ");
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
			if (curStream.getContentType() != null)
				return curStream.getContentType();
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
			{
				reportError(curItem.getReference(), "Unable to get cache key for ");
				return null;
			}
			key.add(curKey);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/NoopPageStore.java`
#### Snippet
```java
	public IManageablePage getPage(IPageContext context, int id)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/RequestPageStore.java`
#### Snippet
```java
				}
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java
				}
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java
		SessionData sessionData = getSessionData(context, false);
		if (sessionData == null) {
			return null;
		}
		
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
			if (sessionIdentifier == null)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
			if (window == null)
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
		}
		
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
				}
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
			}

			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
			}

			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
				}
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AbstractPersistentPageStore.java`
#### Snippet
```java
		if (sessionIdentifier == null)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AbstractPersistentPageStore.java`
#### Snippet
```java
			if (attribute == null)
			{
				return null;
			}
			return attribute.sessionIdentifier;
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/PageWindowManager.java`
#### Snippet
```java
			return windows.get(index);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/FilePageStore.java`
#### Snippet
```java
		if (file.exists() == false)
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/FilePageStore.java`
#### Snippet
```java
		{
			log.warn("cannot read page data for session {} page {}", sessionIdentifier, id, ex);
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AsynchronousPageStore.java`
#### Snippet
```java
		String sessionId = context.getSessionId(false);
		if (sessionId == null) {
			return null;
		}
		
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
				try
				{
					final InvocationHandler invocationHandler = (proxy, method, args) -> null;
					final Object proxyInstance = Proxy.newProxyInstance(
							hasNonPublicInterface ? nonPublicLoader : latestLoader, classObjs, invocationHandler);
```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
			          object.getClass(), object, e);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/NoOpAuthenticationStrategy.java`
#### Snippet
```java
	public String[] load()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/application/WildcardMatcherHelper.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/DefaultAuthenticationStrategy.java`
#### Snippet
```java
			return new String[] { username, password };
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/DefaultAuthenticationStrategy.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/sri/DynamicSubresourceIntegrity.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/TreeApplication.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/wizard/NewUserWizard.java`
#### Snippet
```java
					// name and roles don't have anything to validate,
					// so might as well just skip them here
					return null;
				}

```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/LibrarySession.java`
#### Snippet
```java
	public Roles getRoles()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/DataTablePage.java`
#### Snippet
```java
			};
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/OIRPage.java`
#### Snippet
```java
		{
			super(id, index, model);
			add(AttributeModifier.append("class", () -> highlite ? "highlight" : null));
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/AjaxStatelessExample.java`
#### Snippet
```java
		if (value.isNull() || value.isEmpty())
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/FormInput.java`
#### Snippet
```java
		public String convertToString(URL value, Locale locale)
		{
			return value != null ? value.toString() : null;
		}
	}
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/FormInput.java`
#### Snippet
```java
						return URLConverter.INSTANCE;
					}
					return null;
				}
			});
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/FormInput.java`
#### Snippet
```java
						return new MaskConverter<>("(###) ###-####", UsPhoneNumber.class);
					}
					return null;
				}
			});
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/LocaleFirstMapper.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/authentication1/SignInSession.java`
#### Snippet
```java
	public Roles getRoles()
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/authentication2/SignIn2Session.java`
#### Snippet
```java
			return new Roles(Roles.ADMIN);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/authentication3/MyAuthenticatedWebSession.java`
#### Snippet
```java
			return new Roles(Roles.ADMIN);
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/customresourceloading/PageWithCustomLoading.java`
#### Snippet
```java
	public String getCacheKey(MarkupContainer container, Class<?> containerClass)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-examples/src/main/java/org/apache/wicket/examples/customresourceloading/PageWithCustomLoading.java`
#### Snippet
```java

		// no resource was not found
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketConnectionFilterCollection.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/AbstractUpgradeFilter.java`
#### Snippet
```java
	protected String selectSubProtocol(List<String> subProtocols)
	{
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public AsyncContext startAsync() throws IllegalStateException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public Part getPart(String name) throws IOException, ServletException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	@Override
	public ServletConnection getServletConnection() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public String getQueryString()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public ServletContext getServletContext()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public BufferedReader getReader() throws IOException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public String changeSessionId()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public String getAuthType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	@Override
	public String getProtocolRequestId() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public AsyncContext startAsync(ServletRequest servletRequest, ServletResponse servletResponse) throws IllegalStateException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
			return values.nextElement();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public RequestDispatcher getRequestDispatcher(String path)
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public AsyncContext getAsyncContext()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public <T extends HttpUpgradeHandler> T upgrade(Class<T> handlerClass) throws IOException, ServletException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public DispatcherType getDispatcherType()
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public ServletInputStream getInputStream() throws IOException
	{
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	@Override
	public String getParameter(String name) {
		return parameters.get(name) != null ? parameters.get(name)[0] : null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketConnectionOriginFilter.java`
#### Snippet
```java
		if (origins.size() != 1)
		{
			return null;
		}
		return origins.get(0);
```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketConnectionOriginFilter.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketConnection.java`
#### Snippet
```java
        checkOpenness();
        onOutMessage(message);
        return null;
    }

```

### ReturnNull
Return of `null`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketConnection.java`
#### Snippet
```java
        checkOpenness();
        onOutMessage(message, offset, length);
        return null;
    }

```

### ReturnNull
Return of `null`
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationPropagator.java`
#### Snippet
```java
			return pageHandler.getPageParameters();
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/BeanManagerLookup.java`
#### Snippet
```java
				catch (NamingException e)
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/BeanManagerLookup.java`
#### Snippet
```java
				{
					log.debug(e.getMessage(), e);
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/BeanManagerLookup.java`
#### Snippet
```java
				catch (NamingException e)
				{
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/VelocityContributor.java`
#### Snippet
```java
		if (!Velocity.resourceExists(templateName))
		{
			return null;
		}

```

### ReturnNull
Return of `null`
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/VelocityContributor.java`
#### Snippet
```java
			if (!Velocity.mergeTemplate(templateName, encoding, ctx, writer))
			{
				return null;
			}

```

### ReturnNull
Return of `null`
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/markup/html/VelocityPanel.java`
#### Snippet
```java
	{
		// don't cache the evaluated template
		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/markup/html/VelocityPanel.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/markup/html/VelocityPanel.java`
#### Snippet
```java
				onException(e);
			}
			return null;
		}
		return evaluatedTemplate;
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `inputStream` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/io/Streams.java`
#### Snippet
```java
		URLConnection urlConnection = url.openConnection();
		urlConnection.setUseCaches(false);
		InputStream inputStream = urlConnection.getInputStream();
		return inputStream;
	}
```

### UnnecessaryLocalVariable
Local variable `key` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/CipherUtils.java`
#### Snippet
```java
			KeyGenerator keyGenerator = KeyGenerator.getInstance(algorithm);
			keyGenerator.init(keyLength, secureRandom);
			SecretKey key = keyGenerator.generateKey();
			return key;
		}
```

### UnnecessaryLocalVariable
Local variable `secret` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/CipherUtils.java`
#### Snippet
```java
			SecretKeyFactory factory = SecretKeyFactory.getInstance(pbeAlgorithm);
			KeySpec spec = new PBEKeySpec(password.toCharArray(), salt, iterationCount, keyLength);
			SecretKey secret = new SecretKeySpec(factory.generateSecret(spec).getEncoded(),
				keyAlgorithm);
			return secret;
```

### UnnecessaryLocalVariable
Local variable `unescapedMarkup` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
	public static CharSequence unescapeMarkup(final String markup)
	{
		String unescapedMarkup = StringEscapeUtils.unescapeHtml(markup);
		return unescapedMarkup;
	}
```

### UnnecessaryLocalVariable
Local variable `sessionsSelector` is redundant
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPanel.java`
#### Snippet
```java
	private DropDownChoice<String> createSessionsSelector(String id)
	{
		DropDownChoice<String> sessionsSelector = new DropDownChoice<String>("sessions",
			Model.of(getCurrentSessionIdentifier()), new SessionIdentifiersModel(getModel()));

```

### UnnecessaryLocalVariable
Local variable `tree` is redundant
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
			protected TreeNode load()
			{
				TreeNode tree = componentTreeModel.getObject();
				TreeNode currentItem = tree;
				for (Integer index : path)
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
			if (node instanceof Component)
			{
				long size = ((Component)node).getSizeInBytes();
				return size;
			}
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
			else
			{
				long size = WicketObjects.sizeof(node);
				return size;
			}
```

### UnnecessaryLocalVariable
Local variable `propertyModel` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/PropertyColumn.java`
#### Snippet
```java
	public IModel<?> getDataModel(IModel<T> rowModel)
	{
		PropertyModel<?> propertyModel = new PropertyModel<>(rowModel, propertyExpression);
		return propertyModel;
	}
```

### UnnecessaryLocalVariable
Local variable `dataModel` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/LambdaColumn.java`
#### Snippet
```java
	public IModel<?> getDataModel(IModel<T> rowModel)
	{
		IModel<Object> dataModel = new IModel<Object>()
		{
			private static final long serialVersionUID = 1L;
```

### UnnecessaryLocalVariable
Local variable `item` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/TableTree.java`
#### Snippet
```java
	protected Item<T> newRowItem(String id, int index, IModel<T> model)
	{
		Item<T> item = new Item<>(id, index, model);

		return item;
```

### UnnecessaryLocalVariable
Local variable `attributesAsJson` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
		}

		String attributesAsJson = attributesJson.toString();

		return attributesAsJson;
```

### UnnecessaryLocalVariable
Local variable `crypt` is redundant
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/AbstractKeyInSessionCryptFactory.java`
#### Snippet
```java

		// build the crypt based on session key
		ICrypt crypt = createCrypt(key);
		return crypt;
	}
```

### UnnecessaryLocalVariable
Local variable `i` is redundant
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
			for (char c : str.toCharArray())
			{
				int i = c;
				hash = 47 * hash + i;
			}
```

### UnnecessaryLocalVariable
Local variable `rex` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			{
				// wrap the exception so that it brings info about the component
				WicketRuntimeException rex = new WicketRuntimeException(
					"An error occurred while getting the model object for Component: " +
						this.toString(true), ex);
```

### UnnecessaryLocalVariable
Local variable `markupId` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	{
		IMarkupIdGenerator markupIdGenerator = getApplication().getMarkupSettings().getMarkupIdGenerator();
		String markupId = markupIdGenerator.generateMarkupId(this, createIfDoesNotExist);
		return markupId;
	}
```

### UnnecessaryLocalVariable
Local variable `component` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelTextResolver.java`
#### Snippet
```java
			if (forAttributeValue != null)
			{
				Component component = AutoLabelResolver.findRelatedComponent(container, forAttributeValue);
				related = component;
			}
```

### UnnecessaryLocalVariable
Local variable `objectClass` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
		if (model instanceof IObjectClassAwareModel)
		{
			Class<?> objectClass = ((IObjectClassAwareModel<?>)model).getObjectClass();
			return objectClass;
		}
```

### UnnecessaryLocalVariable
Local variable `radioValue` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java
	protected String getModelValue()
	{
		String radioValue = visitChildren(Radio.class, new IVisitor<Radio<T>, String>()
		{
			@Override
```

### UnnecessaryLocalVariable
Local variable `object` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
		final IModel<? extends List<? extends T>> choices = getChoicesModel();
		final IChoiceRenderer<? super T> renderer = getChoiceRenderer();
		T object = (T) renderer.getObject(id, choices);
		return object;
	}
```

### UnnecessaryLocalVariable
Local variable `submittingComponent` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
		final IRequestParameters parameters = getRequestParameters(this);

		IFormSubmittingComponent submittingComponent = getPage().visitChildren(
			IFormSubmittingComponent.class, new IVisitor<Component, IFormSubmittingComponent>()
			{
```

### UnnecessaryLocalVariable
Local variable `form` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/BrowserInfoForm.java`
#### Snippet
```java
	protected Form<ClientProperties> createForm(String componentId, IModel<ClientProperties> properties)
	{
		Form<ClientProperties> form = new Form<ClientProperties>(componentId, properties)
		{
			private static final long serialVersionUID = 1L;
```

### UnnecessaryLocalVariable
Local variable `isBodyTag` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
	private boolean canDequeueBody(ComponentTag tag)
	{
		boolean isBodyTag = (tag instanceof WicketTag) && ((WicketTag)tag).isBodyTag();
		
		return isBodyTag;
```

### UnnecessaryLocalVariable
Local variable `that` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlTag.java`
#### Snippet
```java
	public final boolean equalTo(final XmlTag element)
	{
		final XmlTag that = element;
		if (!Objects.equal(getNamespace(), that.getNamespace()))
		{
```

### UnnecessaryLocalVariable
Local variable `header` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
				// relevant markup. Thus we simply create a WebMarkupContainer to handle
				// the tag.
				WebMarkupContainer header = new WicketHeadContainer();

				return header;
```

### UnnecessaryLocalVariable
Local variable `url` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
		{
			Url mappedUrl = mapUrlFor(handler);
			CharSequence url = renderUrl(mappedUrl, handler);
			return url;
		}
```

### UnnecessaryLocalVariable
Local variable `fslex` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/MultipartServletWebRequestImpl.java`
#### Snippet
```java
				{
					String fieldName = entry.getKey();
					FileUploadException fslex = new FileUploadByteCountLimitException("The field '" +
							fieldName + "' exceeds its maximum permitted size of '" +
							maxSize + "' characters.", fileItem.getSize(), fileMaxSize.bytes(), fileItem.getName(), fieldName);
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
		final String contentType = findContentType(streams);
		final Instant lastModified = findLastModified(streams);
		AbstractResourceStream ret = new AbstractResourceStream()
		{
			private static final long serialVersionUID = 1L;
```

### UnnecessaryLocalVariable
Local variable `decrypted` is redundant
in `wicket-core/src/main/java/org/apache/wicket/pageStore/crypt/DefaultCrypter.java`
#### Snippet
```java
			Cipher cipher = getCipher();
			cipher.init(Cipher.DECRYPT_MODE, key, new IvParameterSpec(iv));
			byte[] decrypted = cipher.doFinal(ciphertext);

			return decrypted;
```

### UnnecessaryLocalVariable
Local variable `decrypted` is redundant
in `wicket-core/src/main/java/org/apache/wicket/pageStore/crypt/GCMSIVCrypter.java`
#### Snippet
```java
			Cipher cipher = getCipher();
			cipher.init(Cipher.DECRYPT_MODE, key, new AEADParameterSpec(nonce, 128));
			byte[] decrypted = cipher.doFinal(ciphertext);

			return decrypted;
```

### UnnecessaryLocalVariable
Local variable `lang` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/LocaleHelper.java`
#### Snippet
```java
		else
		{
			String lang = localeAsString;

			result = new Locale(lang);
```

## RuleId[id=UseCompareMethod]
### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
            final int len1 = bom1.length();
            final int len2 = bom2.length();
            if (len1 > len2) {
                return -1;
            }
```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `wicket-util/src/main/java/org/apache/wicket/util/value/IntValue.java`
#### Snippet
```java
	public final int compareTo(final IntValue that)
	{
		if (value < that.value)
		{
			return -1;
```

### UseCompareMethod
Expression can be replaced with 'Long.compare'
in `wicket-util/src/main/java/org/apache/wicket/util/value/LongValue.java`
#### Snippet
```java
	public final int compareTo(final LongValue that)
	{
		if (value < that.value)
		{
			return -1;
```

### UseCompareMethod
Expression can be replaced with 'Double.compare'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Objects.java`
#### Snippet
```java
					dv2 = doubleValue(v2);

					return (dv1 == dv2) ? 0 : ((dv1 < dv2) ? -1 : 1);

				default :
```

### UseCompareMethod
Expression can be replaced with 'Long.compare'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Objects.java`
#### Snippet
```java
					lv2 = longValue(v2);

					return (lv1 == lv2) ? 0 : ((lv1 < lv2) ? -1 : 1);
			}
		}
```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/CompoundRequestMapper.java`
#### Snippet
```java
		public int compareTo(final MapperWithScore o)
		{
			return (compatibilityScore < o.compatibilityScore ? 1
				: (compatibilityScore > o.compatibilityScore ? -1 : 0));
		}

```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java
							if (!timeToNextExecution.isNegative())
							{
								Thread.sleep(timeToNextExecution.toMillis());
							}
							
```

## RuleId[id=WaitNotInLoop]
### WaitNotInLoop
Call to `wait()` is not in loop
in `wicket-core/src/main/java/org/apache/wicket/page/PageAccessSynchronizer.java`
#### Snippet
```java
			try
			{
				wait(remaining);
			}
			catch (InterruptedException e)
```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`".".equals(folder) == false` can be simplified to '!".".equals(folder)'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Packages.java`
#### Snippet
```java
					}
				}
				else if (".".equals(folder) == false)
				{
					// Add to stack
```

### PointlessBooleanExpression
`argument == false` can be simplified to '!argument'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Args.java`
#### Snippet
```java
	public static boolean isTrue(final boolean argument, final String msg, final Object... params)
	{
		if (argument == false)
		{
			throw new IllegalArgumentException(format(msg, params));
```

### PointlessBooleanExpression
`argument == true` can be simplified to 'argument'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Args.java`
#### Snippet
```java
	public static boolean isFalse(final boolean argument, final String msg, final Object... params)
	{
		if (argument == true)
		{
			throw new IllegalArgumentException(format(msg, params));
```

### PointlessBooleanExpression
`first == false` can be simplified to '!first'
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
		for (Map.Entry<String, Object> entry : entrySet())
		{
			if (first == false)
			{
				buffer.append(' ');
```

### PointlessBooleanExpression
`Strings.isEmpty(xml) == false` can be simplified to '!Strings.isEmpty(xml)'
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlLicenseHeaderHandler.java`
#### Snippet
```java
			}

			if (Strings.isEmpty(xml) == false)
			{
				newContent.append(xml).append(LINE_ENDING);
```

### PointlessBooleanExpression
`header.equals(getLicenseHeader()) == false` can be simplified to '!header.equals(getLicenseHeader())'
in `wicket-util/src/main/java/org/apache/wicket/util/license/JavaLicenseHeaderHandler.java`
#### Snippet
```java
			{
				String header = mat.group(1);
				if (header.equals(getLicenseHeader()) == false)
				{
					String newContent = Strings.replaceAll(fileContent, header, "").toString();
```

### PointlessBooleanExpression
`isoLanguages.contains(language) == false` can be simplified to '!isoLanguages.contains(language)'
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceUtils.java`
#### Snippet
```java
			if (language != null)
			{
				if (isoLanguages.contains(language) == false)
				{
					language = null;
```

### PointlessBooleanExpression
`isoCountries.contains(country) == false` can be simplified to '!isoCountries.contains(country)'
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceUtils.java`
#### Snippet
```java
			if ((language != null) && (country != null))
			{
				if (isoCountries.contains(country) == false)
				{
					country = null;
```

### PointlessBooleanExpression
`"target".equals(relativePathname) == false` can be simplified to '!"target".equals(relativePathname)'
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
						baseDirectory.getAbsolutePath() + System.getProperty("file.separator"), "")
					.toString();
				if ("target".equals(relativePathname) == false)
				{
					boolean found = false;
```

### PointlessBooleanExpression
`found == false` can be simplified to '!found'
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
						}
					}
					if (found == false)
					{
						accept = true;
```

### PointlessBooleanExpression
`ignoreFile(pathname) == false` can be simplified to '!ignoreFile(pathname)'
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
			if (pathname.isFile())
			{
				if (ignoreFile(pathname) == false)
				{
					for (String suffix : suffixes)
```

### PointlessBooleanExpression
`licenseHeaderHandler.checkLicenseHeader(file) == false` can be simplified to '!licenseHeaderHandler.checkLicenseHeader(file)'
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
					public void visitFile(final File file)
					{
						if (licenseHeaderHandler.checkLicenseHeader(file) == false)
						{
							if ((addHeaders == false) ||
```

### PointlessBooleanExpression
`addHeaders == false` can be simplified to '!addHeaders'
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
						if (licenseHeaderHandler.checkLicenseHeader(file) == false)
						{
							if ((addHeaders == false) ||
								(licenseHeaderHandler.addLicenseHeader(file) == false))
							{
```

### PointlessBooleanExpression
`licenseHeaderHandler.addLicenseHeader(file) == false` can be simplified to '!licenseHeaderHandler.addLicenseHeader(file)'
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
						{
							if ((addHeaders == false) ||
								(licenseHeaderHandler.addLicenseHeader(file) == false))
							{
								List<File> files = badFiles.get(licenseHeaderHandler);
```

### PointlessBooleanExpression
`existed == false` can be simplified to '!existed'
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/Initializer.java`
#### Snippet
```java
				boolean existed = ThreadContext.exists();

				if (existed == false)
				{
					ThreadContext.setApplication(application);
```

### PointlessBooleanExpression
`existed == false` can be simplified to '!existed'
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/Initializer.java`
#### Snippet
```java
				finally
				{
					if (existed == false)
					{
						ThreadContext.detach();
```

### PointlessBooleanExpression
`Application.get().getDebugSettings().isDevelopmentUtilitiesEnabled() == false` can be simplified to '!Application.get().getDebugSettings().isDevelopmentUtilitiesEnabled()'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/DevelopmentUtilitiesNotEnabledException.java`
#### Snippet
```java
	public static void check()
	{
		if (Application.get().getDebugSettings().isDevelopmentUtilitiesEnabled() == false)
		{
			throw new DevelopmentUtilitiesNotEnabledException();
```

### PointlessBooleanExpression
`requiredFlagSet == false` can be simplified to '!requiredFlagSet'
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/PropertyValidator.java`
#### Snippet
```java
	{
		super.onConfigure(component);
		if (requiredFlagSet == false)
		{
			// "Required" flag is calculated upon component's model property, so
```

### PointlessBooleanExpression
`component.isAuto() == false` can be simplified to '!component.isAuto()'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/RenderPerformanceListener.java`
#### Snippet
```java
			Long renderEnd = System.currentTimeMillis();
			Long renderStart = component.getMetaData(PageView.RENDER_KEY);
			if (renderStart != null && component.isAuto() == false)
			{
				Long duration = renderEnd - renderStart;
```

### PointlessBooleanExpression
`component.isAuto() == false` can be simplified to '!component.isAuto()'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/RenderPerformanceListener.java`
#### Snippet
```java
		{
			super.beforeRender(component);
			if (component.isAuto() == false)
			{
				Long now = System.currentTimeMillis();
```

### PointlessBooleanExpression
`component.isAuto() == false` can be simplified to '!component.isAuto()'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/RenderPerformanceListener.java`
#### Snippet
```java
	protected boolean accepts(final Component component)
	{
		return component.isAuto() == false;
	}

```

### PointlessBooleanExpression
`identifiers.contains(current) == false` can be simplified to '!identifiers.contains(current)'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/SessionIdentifiersModel.java`
#### Snippet
```java
		IPageContext context = new DefaultPageContext();
		String current = store.getSessionIdentifier(context);
		if (identifiers.contains(current) == false)
		{
			// identifiers of the store seem no to match their sessions ids,
```

### PointlessBooleanExpression
`component.isStateless() == false` can be simplified to '!component.isStateless()'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java
			};

			if (component.isStateless() == false)
			{
				StringList statefulBehaviors = new StringList();
```

### PointlessBooleanExpression
`b.getStatelessHint(component) == false` can be simplified to '!b.getStatelessHint(component)'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java
				for (Behavior b : component.getBehaviors())
				{
					if (b.getStatelessHint(component) == false)
					{
						statefulBehaviors.add(Classes.name(b.getClass()));
```

### PointlessBooleanExpression
`getSort().isAscending() == false` can be simplified to '!getSort().isAscending()'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPagesProvider.java`
#### Snippet
```java
			int c = Objects.compareWithConversion(value0, value1);

			if (getSort().isAscending() == false)
			{
				c = c * -1;
```

### PointlessBooleanExpression
`url.startsWith("/") == false` can be simplified to '!url.startsWith("/")'
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
	{
		// the regex means "doesn't start with 'scheme://'"
		if ((url != null) && (url.startsWith("/") == false) && (!url.matches("^\\w+\\:\\/\\/.*")) &&
			!(url.startsWith("#")))
		{
```

### PointlessBooleanExpression
`(object instanceof Instant) == false` can be simplified to '!(object instanceof Instant)'
in `wicket-request/src/main/java/org/apache/wicket/request/HttpHeaderCollection.java`
#### Snippet
```java
		Object object = objects.get(0);

		if ((object instanceof Instant) == false)
		{
			throw new IllegalStateException("header value is not of type date");
```

### PointlessBooleanExpression
`Strings.isEmpty(protocol) == false` can be simplified to '!Strings.isEmpty(protocol)'
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java

		StringBuilder render = new StringBuilder();
		if (Strings.isEmpty(protocol) == false)
		{
			render.append(protocol);
```

### PointlessBooleanExpression
`Strings.isEmpty(host) == false` can be simplified to '!Strings.isEmpty(host)'
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
		}

		if (Strings.isEmpty(host) == false)
		{
			render.append("//");
```

### PointlessBooleanExpression
`renderedUrl.endsWith("...") == false` can be simplified to '!renderedUrl.endsWith("...")'
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java

		// add trailing slash if the url has no query string and ends with ..
		if (renderedUrl.indexOf('?') == -1 && (renderedUrl.endsWith("..") && renderedUrl.endsWith("...") == false))
		{
			// WICKET-4401
```

### PointlessBooleanExpression
`(segments.isEmpty() || segments.get(0).isEmpty()) == false` can be simplified to '!(segments.isEmpty() \|\| segments.get(0).isEmpty())'
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
		// try to remove context/filter path only if the Url starts with '/',
		// i.e. has an empty segment in the beginning
		if ((segments.isEmpty() || segments.get(0).isEmpty()) == false)
		{
			return;
```

### PointlessBooleanExpression
`commonPrefixSegment.equals(segmentToClean) == false` can be simplified to '!commonPrefixSegment.equals(segmentToClean)'
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
			String commonPrefixSegment = Strings.stripJSessionId(commonPrefix.getSegments().get(i));
			String segmentToClean = Strings.stripJSessionId(segments.get(i));
			if (commonPrefixSegment.equals(segmentToClean) == false)
			{
				LOG.debug("Segments '{}' do not start with common prefix '{}'", segments,
```

### PointlessBooleanExpression
`Strings.isEmpty(parameterName) == false` can be simplified to '!Strings.isEmpty(parameterName)'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/parameter/PageParametersEncoder.java`
#### Snippet
```java
		{
			String parameterName = p.getName();
			if (Strings.isEmpty(parameterName) == false)
			{
				parameters.add(parameterName, p.getValue(), INamedParameters.Type.QUERY_STRING);
```

### PointlessBooleanExpression
`urlCopy.getSegments().isEmpty() == false` can be simplified to '!urlCopy.getSegments().isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/AbstractMapper.java`
#### Snippet
```java
		// strip the segments and first query parameter from URL
		Url urlCopy = new Url(request.getUrl());
		while ((segmentsToSkip > 0) && (urlCopy.getSegments().isEmpty() == false))
		{
			urlCopy.getSegments().remove(0);
```

### PointlessBooleanExpression
`PersistedPanel.this.getSession().isTemporary() == false` can be simplified to '!PersistedPanel.this.getSession().isTemporary()'
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPanel.java`
#### Snippet
```java
			public boolean isVisible()
			{
				return PersistedPanel.this.getSession().isTemporary() == false;
			}
		};
```

### PointlessBooleanExpression
`loaded == false` can be simplified to '!loaded'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
	 */
	protected final boolean isLoaded() {
		if (loaded == false)
		{
			if (isContentReady())
```

### PointlessBooleanExpression
`panel.isLoaded() == false` can be simplified to '!panel.isLoaded()'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
				public void component(AjaxLazyLoadPanel<?> panel, IVisit<Void> visit)
				{
					if (panel.isVisibleInHierarchy() && panel.isLoaded() == false) {
						Duration updateInterval = panel.getUpdateInterval();
						if (getUpdateInterval() == null) {
```

### PointlessBooleanExpression
`loaded == false` can be simplified to '!loaded'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
		super.onBeforeRender();

		if (loaded == false) {
			initTimer();
		}
```

### PointlessBooleanExpression
`(obj instanceof Url) == false` can be simplified to '!(obj instanceof Url)'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			return true;
		}
		if ((obj instanceof Url) == false)
		{
			return false;
```

### PointlessBooleanExpression
`url.segments.isEmpty() == false` can be simplified to '!url.segments.isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
				// skip
			}
			else if ("..".equals(segment) && url.segments.isEmpty() == false)
			{
				url.segments.remove(url.segments.size() - 1);
```

### PointlessBooleanExpression
`getSegments().isEmpty() == false` can be simplified to '!getSegments().isEmpty()'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			{
				relativeSegments.remove(0);
				if (getSegments().isEmpty() == false)
				{
					getSegments().remove(getSegments().size() - 1);
```

### PointlessBooleanExpression
`(obj instanceof QueryParameter) == false` can be simplified to '!(obj instanceof QueryParameter)'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
				return true;
			}
			if ((obj instanceof QueryParameter) == false)
			{
				return false;
```

### PointlessBooleanExpression
`protocolLess == false` can be simplified to '!protocolLess'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			result.shouldRenderAsFull = true;

			if (protocolLess == false)
			{
				result.protocol = absoluteUrl.substring(0, protocolAt).toLowerCase(Locale.US);
```

### PointlessBooleanExpression
`Strings.isEmpty(s) == false` can be simplified to '!Strings.isEmpty(s)'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			for (String s : queryArray)
			{
				if (Strings.isEmpty(s) == false)
				{
					result.parameters.add(parseQueryParameter(s, charset));
```

### PointlessBooleanExpression
`Strings.isEmpty(protocol) == false` can be simplified to '!Strings.isEmpty(protocol)'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			}

			if (Strings.isEmpty(protocol) == false)
			{
				result.append(protocol);
```

### PointlessBooleanExpression
`Strings.isEmpty(host) == false` can be simplified to '!Strings.isEmpty(host)'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
				result.append("://");
			}
			else if (Strings.isEmpty(protocol) && Strings.isEmpty(host) == false)
			{
				result.append("//");
```

### PointlessBooleanExpression
`port.equals(getDefaultPortForProtocol(protocol)) == false` can be simplified to '!port.equals(getDefaultPortForProtocol(protocol))'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			result.append(host);

			if (port != null && port.equals(getDefaultPortForProtocol(protocol)) == false)
			{
				result.append(':');
```

### PointlessBooleanExpression
`Strings.isEmpty(_fragment) == false` can be simplified to '!Strings.isEmpty(_fragment)'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
		}

		if (Strings.isEmpty(_fragment) == false)
		{
			result.append('#').append(_fragment);
```

### PointlessBooleanExpression
`ids.contains(choiceId) == false` can be simplified to '!ids.contains(choiceId)'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/component/Recorder.java`
#### Snippet
```java
			final String choiceId = renderer.getIdValue(choice, 0);

			if (ids.contains(choiceId) == false)
			{
				unselected.add(choice);
```

### PointlessBooleanExpression
`getVisiblityCache().isVisible(index) == false` can be simplified to '!getVisiblityCache().isVisible(index)'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
		int index = getSelectedTab();

		if ((index == -1) || (getVisiblityCache().isVisible(index) == false))
		{
			// find first visible tab
```

### PointlessBooleanExpression
`param.getProperty().equals(property) == false` can be simplified to '!param.getProperty().equals(property)'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SingleSortState.java`
#### Snippet
```java
		Args.notNull(property, "property");

		if ((param == null) || (param.getProperty().equals(property) == false))
		{
			return SortOrder.NONE;
```

### PointlessBooleanExpression
`isEmpty(deniedRoles) == false` can be simplified to '!isEmpty(deniedRoles)'
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/annotations/AnnotationsRoleAuthorizationStrategy.java`
#### Snippet
```java
			{
				Roles deniedRoles = new Roles(authorizeActionAnnotation.deny());
				if (isEmpty(deniedRoles) == false && hasAny(deniedRoles))
				{
					return false;
```

### PointlessBooleanExpression
`isSignedIn() == false` can be simplified to '!isSignedIn()'
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/panel/SignInPanel.java`
#### Snippet
```java
	{
		// logged in already?
		if (isSignedIn() == false)
		{
			IAuthenticationStrategy authenticationStrategy = getApplication().getSecuritySettings()
```

### PointlessBooleanExpression
`rememberMe == true` can be simplified to 'rememberMe'
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/panel/SignInPanel.java`
#### Snippet
```java
			if (signIn(getUsername(), getPassword()))
			{
				if (rememberMe == true)
				{
					strategy.save(username, password);
```

### PointlessBooleanExpression
`behavior.getStatelessHint(component) == false` can be simplified to '!behavior.getStatelessHint(component)'
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
	{
		component.data_add(behavior);
		if (behavior.getStatelessHint(component) == false)
		{
			getBehaviorId(component, behavior);
```

### PointlessBooleanExpression
`data.postParameters.isEmpty() == false` can be simplified to '!data.postParameters.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
			if (data != null)
			{
				if (data.postParameters.isEmpty() == false &&
					request.getPostParameters() instanceof IWritableRequestParameters)
				{
```

### PointlessBooleanExpression
`createIfDoesNotExist == false` can be simplified to '!createIfDoesNotExist'
in `wicket-core/src/main/java/org/apache/wicket/DefaultMarkupIdGenerator.java`
#### Snippet
```java
		}

		if (storedMarkupId == null && createIfDoesNotExist == false)
		{
			return null;
```

### PointlessBooleanExpression
`isBookmarkable() == false` can be simplified to '!isBookmarkable()'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	public final boolean isPageStateless()
	{
		if (isBookmarkable() == false)
		{
			stateless = Boolean.FALSE;
```

### PointlessBooleanExpression
`getStatelessHint() == false` can be simplified to '!getStatelessHint()'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
		}

		if (getStatelessHint() == false)
		{
			return false;
```

### PointlessBooleanExpression
`isStateless() == false` can be simplified to '!isStateless()'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
			internalInitialize();

			if (isStateless() == false)
			{
				stateless = Boolean.FALSE;
```

### PointlessBooleanExpression
`rtn == true` can be simplified to 'rtn'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
				{
					boolean rtn = hasInvisibleTransparentChild((MarkupContainer)sibling, self);
					if (rtn == true)
					{
						return true;
```

### PointlessBooleanExpression
`isBookmarkable() == false` can be simplified to '!isBookmarkable()'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	void init()
	{
		if (isBookmarkable() == false)
		{
			setStatelessHint(false);
```

### PointlessBooleanExpression
`isInitialization == false` can be simplified to '!isInitialization'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
			setNextAvailableId();

			if (isInitialization == false)
			{
				pageManager.touchPage(this);
```

### PointlessBooleanExpression
`renderedComponents.add(component) == false` can be simplified to '!renderedComponents.add(component)'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
				renderedComponents = new HashSet<Component>();
			}
			if (renderedComponents.add(component) == false)
			{
				throw new MarkupException(
```

### PointlessBooleanExpression
`isSessionInvalidated() == false` can be simplified to '!isSessionInvalidated()'
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	public void invalidateNow()
	{
		if (isSessionInvalidated() == false) 
		{
			invalidate();
```

### PointlessBooleanExpression
`isTemporary() == false` can be simplified to '!isTemporary()'
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	public final void clear()
	{
		if (isTemporary() == false)
		{
			getPageManager().clear();
```

### PointlessBooleanExpression
`skip == false` can be simplified to '!skip'
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
				final boolean skip = parent instanceof AbstractRepeater;

				if (skip == false)
				{
					String cursorKey = cursor.isAuto() ? "wicket-auto" : cursor.getId();
```

### PointlessBooleanExpression
`value == false` can be simplified to '!value'
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
	public final void setEnableCache(boolean value)
	{
		if (value == false)
		{
			cache = null;
```

### PointlessBooleanExpression
`component instanceof TextField`` == false` can be simplified to '!(component instanceof TextField)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxPreventSubmitBehavior.java`
#### Snippet
```java

		Component component = getComponent();
		if (component instanceof TextField<?> == false)
		{
			attributes.setChildSelector("input");
```

### PointlessBooleanExpression
`boundName.equals(windowName) == false` can be simplified to '!boundName.equals(windowName)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxNewWindowNotifyingBehavior.java`
#### Snippet
```java
			boundName = windowName;
		}
		else if (boundName.equals(windowName) == false)
		{
			onNewWindow(target);
```

### PointlessBooleanExpression
`((WebRequest)requestCycle.getRequest()).isAjax() == false` can be simplified to '!((WebRequest)requestCycle.getRequest()).isAjax()'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java
		}

		if (((WebRequest)requestCycle.getRequest()).isAjax() == false)
		{
			// the request was not sent by wicket-ajax.js - this can happen when an Ajax request was
```

### PointlessBooleanExpression
`stopped == false` can be simplified to '!stopped'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractAjaxTimerBehavior.java`
#### Snippet
```java
	public final void stop(final IPartialPageRequestHandler target)
	{
		if (stopped == false)
		{
			stopped = true;
```

### PointlessBooleanExpression
`isStopped() == false` can be simplified to '!isStopped()'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractAjaxTimerBehavior.java`
#### Snippet
```java
	protected boolean shouldTrigger()
	{
		return isStopped() == false &&
				isEnabled(getComponent()) &&
				(getComponent() instanceof Page || getComponent().findParent(Page.class) != null);
```

### PointlessBooleanExpression
`isStopped() == false` can be simplified to '!isStopped()'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractAjaxTimerBehavior.java`
#### Snippet
```java
		super.renderHead(component, response);

		if (isStopped() == false)
		{
			setTimeout(response);
```

### PointlessBooleanExpression
`hasBeenRendered == false` can be simplified to '!hasBeenRendered'
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
		super.onConfigure(component);

		if (hasBeenRendered == false)
		{
			hasBeenRendered = true;
```

### PointlessBooleanExpression
`Strings.isEmpty(handler) == false` can be simplified to '!Strings.isEmpty(handler)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
		final String propertyName, final String signature) throws JSONException
	{
		if (Strings.isEmpty(handler) == false)
		{
			final JSONFunction function;
```

### PointlessBooleanExpression
`component instanceof Page == false` can be simplified to '!(component instanceof Page)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
			}

			if (component instanceof Page == false)
			{
				String componentId = component.getMarkupId();
```

### PointlessBooleanExpression
`Strings.isEmpty(formId) == false` can be simplified to '!Strings.isEmpty(formId)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java

			String formId = attributes.getFormId();
			if (Strings.isEmpty(formId) == false)
			{
				attributesJson.put(AjaxAttributeName.FORM_ID.jsonName(), formId);
```

### PointlessBooleanExpression
`Strings.isEmpty(submittingComponentId) == false` can be simplified to '!Strings.isEmpty(submittingComponentId)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java

			String submittingComponentId = attributes.getSubmittingComponentName();
			if (Strings.isEmpty(submittingComponentId) == false)
			{
				attributesJson.put(AjaxAttributeName.SUBMITTING_COMPONENT_NAME.jsonName(),
```

### PointlessBooleanExpression
`Strings.isEmpty(childSelector) == false` can be simplified to '!Strings.isEmpty(childSelector)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java

			CharSequence childSelector = attributes.getChildSelector();
			if (Strings.isEmpty(childSelector) == false)
			{
				attributesJson.put(AjaxAttributeName.CHILD_SELECTOR.jsonName(),
```

### PointlessBooleanExpression
`Strings.isEmpty(indicatorId) == false` can be simplified to '!Strings.isEmpty(indicatorId)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java

			String indicatorId = findIndicatorId();
			if (Strings.isEmpty(indicatorId) == false)
			{
				attributesJson.put(AjaxAttributeName.INDICATOR_ID.jsonName(), indicatorId);
```

### PointlessBooleanExpression
`attributes.isAsynchronous() == false` can be simplified to '!attributes.isAsynchronous()'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
			}

			if (attributes.isAsynchronous() == false)
			{
				attributesJson.put(AjaxAttributeName.IS_ASYNC.jsonName(), false);
```

### PointlessBooleanExpression
`channel.equals(AjaxChannel.DEFAULT) == false` can be simplified to '!channel.equals(AjaxChannel.DEFAULT)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java

			AjaxChannel channel = attributes.getChannel();
			if (channel != null && channel.equals(AjaxChannel.DEFAULT) == false)
			{
				attributesJson.put(AjaxAttributeName.CHANNEL.jsonName(), channel);
```

### PointlessBooleanExpression
`wicketAjaxResponse == false` can be simplified to '!wicketAjaxResponse'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java

			boolean wicketAjaxResponse = attributes.isWicketAjaxResponse();
			if (wicketAjaxResponse == false)
			{
				attributesJson.put(AjaxAttributeName.IS_WICKET_AJAX_RESPONSE.jsonName(), false);
```

### PointlessBooleanExpression
`AjaxRequestAttributes.XML_DATA_TYPE.equals(dataType) == false` can be simplified to '!AjaxRequestAttributes.XML_DATA_TYPE.equals(dataType)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java

			String dataType = attributes.getDataType();
			if (AjaxRequestAttributes.XML_DATA_TYPE.equals(dataType) == false)
			{
				attributesJson.put(AjaxAttributeName.DATATYPE.jsonName(), dataType);
```

### PointlessBooleanExpression
`container.getOutputMarkupId() == false` can be simplified to '!container.getOutputMarkupId()'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigator.java`
#### Snippet
```java
		// Update a parental container of the pageable, this assumes that the pageable is a component.
		Component container = ((Component)pageable);
		while (container instanceof AbstractRepeater || container.getOutputMarkupId() == false)
		{
			Component parent = container.getParent();
```

### PointlessBooleanExpression
`((MarkupContainer)container).contains(this, true) == false` can be simplified to '!((MarkupContainer)container).contains(this, true)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigator.java`
#### Snippet
```java
		// in case the navigator is not contained by the container, we have
		// to add it to the response
		if (((MarkupContainer)container).contains(this, true) == false)
		{
			target.add(this);
```

### PointlessBooleanExpression
`getRequestCycle().find(AjaxRequestTarget.class).isPresent() == false` can be simplified to '!getRequestCycle().find(AjaxRequestTarget.class).isPresent()'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	public final void onAfterSubmit()
	{
		if (getRequestCycle().find(AjaxRequestTarget.class).isPresent() == false)
		{
			onAfterSubmit(Optional.empty());
```

### PointlessBooleanExpression
`getRequestCycle().find(AjaxRequestTarget.class).isPresent() == false` can be simplified to '!getRequestCycle().find(AjaxRequestTarget.class).isPresent()'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	public final void onSubmit()
	{
		if (getRequestCycle().find(AjaxRequestTarget.class).isPresent() == false)
		{
			onSubmit(Optional.empty());
```

### PointlessBooleanExpression
`getRequestCycle().find(AjaxRequestTarget.class).isPresent() == false` can be simplified to '!getRequestCycle().find(AjaxRequestTarget.class).isPresent()'
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	public final void onError()
	{
		if (getRequestCycle().find(AjaxRequestTarget.class).isPresent() == false)
		{
			onError(Optional.empty());
```

### PointlessBooleanExpression
`Strings.isEmpty(before) == false` can be simplified to '!Strings.isEmpty(before)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onBefore(final CharSequence before)
	{
		if (Strings.isEmpty(before) == false)
		{
			if (this.before == null)
```

### PointlessBooleanExpression
`Strings.isEmpty(failure) == false` can be simplified to '!Strings.isEmpty(failure)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onFailure(final CharSequence failure)
	{
		if (Strings.isEmpty(failure) == false)
		{
			if (this.failure == null)
```

### PointlessBooleanExpression
`Strings.isEmpty(success) == false` can be simplified to '!Strings.isEmpty(success)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onSuccess(final CharSequence success)
	{
		if (Strings.isEmpty(success) == false)
		{
			if (this.success == null)
```

### PointlessBooleanExpression
`Strings.isEmpty(init) == false` can be simplified to '!Strings.isEmpty(init)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onInit(final CharSequence init)
	{
		if (Strings.isEmpty(init) == false)
		{
			if (this.init == null)
```

### PointlessBooleanExpression
`Strings.isEmpty(beforeSend) == false` can be simplified to '!Strings.isEmpty(beforeSend)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onBeforeSend(final CharSequence beforeSend)
	{
		if (Strings.isEmpty(beforeSend) == false)
		{
			if (this.beforeSend == null)
```

### PointlessBooleanExpression
`Strings.isEmpty(complete) == false` can be simplified to '!Strings.isEmpty(complete)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onComplete(final CharSequence complete)
	{
		if (Strings.isEmpty(complete) == false)
		{
			if (this.complete == null)
```

### PointlessBooleanExpression
`Strings.isEmpty(precondition) == false` can be simplified to '!Strings.isEmpty(precondition)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onPrecondition(final CharSequence precondition)
	{
		if (Strings.isEmpty(precondition) == false)
		{
			if (this.precondition == null)
```

### PointlessBooleanExpression
`Strings.isEmpty(after) == false` can be simplified to '!Strings.isEmpty(after)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onAfter(final CharSequence after)
	{
		if (Strings.isEmpty(after) == false)
		{
			if (this.after == null)
```

### PointlessBooleanExpression
`Strings.isEmpty(done) == false` can be simplified to '!Strings.isEmpty(done)'
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxCallListener.java`
#### Snippet
```java
	public AjaxCallListener onDone(final CharSequence done)
	{
		if (Strings.isEmpty(done) == false)
		{
			if (this.done == null)
```

### PointlessBooleanExpression
`pathname.startsWith(WEB_INF) == false` can be simplified to '!pathname.startsWith(WEB_INF)'
in `wicket-core/src/main/java/org/apache/wicket/core/util/file/WebApplicationPath.java`
#### Snippet
```java
	{
		IResourceStream resourceStream = null;
		if (pathname.startsWith(WEB_INF) == false)
		{
			try
```

### PointlessBooleanExpression
`classPathEntry.endsWith(".jar") == false` can be simplified to '!classPathEntry.endsWith(".jar")'
in `wicket-core/src/main/java/org/apache/wicket/core/util/watch/Nio2ModificationWatcher.java`
#### Snippet
```java
		for (String classPathEntry : classPathEntries)
		{
			if (classPathEntry.endsWith(".jar") == false)
			{
				Path folder = Paths.get(classPathEntry);
```

### PointlessBooleanExpression
`component instanceof Page == false` can be simplified to '!(component instanceof Page)'
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/OrphanComponentChecker.java`
#### Snippet
```java
		{
			Component component = (Component) object;
			if (component instanceof Page == false && component.getParent() == null)
			{
				result = new Result(Result.Status.FAILURE, "A component without a parent is detected.");
```

### PointlessBooleanExpression
`getExclusions().isEmpty() == false` can be simplified to '!getExclusions().isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/AbstractObjectChecker.java`
#### Snippet
```java
		Result result = Result.SUCCESS;

		if (object != null && getExclusions().isEmpty() == false)
		{
			Class<?> objectType = object.getClass();
```

### PointlessBooleanExpression
`Character.isWhitespace(next) == false` can be simplified to '!Character.isWhitespace(next)'
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java
					wasNewLineInWhitespace = true;
				}
				if (Character.isWhitespace(next) == false)
				{
					state = REGULAR_TEXT;
```

### PointlessBooleanExpression
`Objects.equal(time, lastModified) == false` can be simplified to '!Objects.equal(time, lastModified)'
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/UrlResourceStream.java`
#### Snippet
```java

			// if timestamp changed: update content length and last modified date
			if (Objects.equal(time, lastModified) == false)
			{
				lastModified = time;
```

### PointlessBooleanExpression
`extensionsIterator.hasNext() == true` can be simplified to 'extensionsIterator.hasNext()'
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceNameIterator.java`
#### Snippet
```java
		if (extensionsIterator != null)
		{
			if (extensionsIterator.hasNext() == true)
			{
				return true;
```

### PointlessBooleanExpression
`extensionsIterator.hasNext() == true` can be simplified to 'extensionsIterator.hasNext()'
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceNameIterator.java`
#### Snippet
```java

				extensionsIterator = newExtensionResourceNameIterator(extensions);
				if (extensionsIterator.hasNext() == true)
				{
					return true;
```

### PointlessBooleanExpression
`extensionsIterator.hasNext() == true` can be simplified to 'extensionsIterator.hasNext()'
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceNameIterator.java`
#### Snippet
```java

				extensionsIterator = newExtensionResourceNameIterator(extensions);
				if (extensionsIterator.hasNext() == true)
				{
					return true;
```

### PointlessBooleanExpression
`writeObjectMethodMissing.contains(cls) == false` can be simplified to '!writeObjectMethodMissing.contains(cls)'
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
		{
			Method writeObjectMethod = null;
			if (writeObjectMethodMissing.contains(cls) == false)
			{
				try
```

### PointlessBooleanExpression
`Strings.isEmpty(parameter.getName()) == false` can be simplified to '!Strings.isEmpty(parameter.getName())'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MapperUtils.java`
#### Snippet
```java
		Args.notNull(parameter, "parameter");

		if (Strings.isEmpty(parameter.getName()) == false && Strings.isEmpty(parameter.getValue()) == true)
		{
			return PageComponentInfo.parse(parameter.getName());
```

### PointlessBooleanExpression
`Strings.isEmpty(parameter.getValue()) == true` can be simplified to 'Strings.isEmpty(parameter.getValue())'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MapperUtils.java`
#### Snippet
```java
		Args.notNull(parameter, "parameter");

		if (Strings.isEmpty(parameter.getName()) == false && Strings.isEmpty(parameter.getValue()) == true)
		{
			return PageComponentInfo.parse(parameter.getName());
```

### PointlessBooleanExpression
`Strings.isEmpty(filename) == false` can be simplified to '!Strings.isEmpty(filename)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
		final String filename = segments.get(lastSegmentAt);

		if (Strings.isEmpty(filename) == false)
		{
			final IResource resource = resourceReference.getResource();
```

### PointlessBooleanExpression
`(requestHandler instanceof ResourceReferenceRequestHandler) == false` can be simplified to '!(requestHandler instanceof ResourceReferenceRequestHandler)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
	public Url mapHandler(IRequestHandler requestHandler)
	{
		if ((requestHandler instanceof ResourceReferenceRequestHandler) == false)
		{
			return null;
```

### PointlessBooleanExpression
`resourceReference.equals(handler.getResourceReference()) == false` can be simplified to '!resourceReference.equals(handler.getResourceReference())'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java

		// see if request handler addresses the resource reference we serve
		if (resourceReference.equals(handler.getResourceReference()) == false)
		{
			return null;
```

### PointlessBooleanExpression
`urlStartsWith(url, mountSegments) == false` can be simplified to '!urlStartsWith(url, mountSegments)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java

		// check if url matches mount path
		if (urlStartsWith(url, mountSegments) == false)
		{
			return null;
```

### PointlessBooleanExpression
`segments.isEmpty() == false` can be simplified to '!segments.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
		final List<String> segments = url.getSegments();

		if (segments.isEmpty() == false)
		{
			// get filename (the last segment)
```

### PointlessBooleanExpression
`Strings.isEmpty(segment) == false` can be simplified to '!Strings.isEmpty(segment)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java

				// remove caching information
				if (i + 1 == segmentsSize && Strings.isEmpty(segment) == false)
				{
					// The filename + parameters eventually contain caching
```

### PointlessBooleanExpression
`Strings.isEmpty(segments.get(3)) == false` can be simplified to '!Strings.isEmpty(segments.get(3))'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java
		return (segments.size() >= 4 &&
				urlStartsWith(url, getContext().getNamespace(), getContext().getResourceIdentifier()) &&
				Strings.isEmpty(segments.get(3)) == false
		);

```

### PointlessBooleanExpression
`tokens.hasMoreTokens() == false` can be simplified to '!tokens.hasMoreTokens()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java

				// on the last component of the resource path
				if (tokens.hasMoreTokens() == false && Strings.isEmpty(token) == false)
				{
					final IResource resource = reference.getResource();
```

### PointlessBooleanExpression
`Strings.isEmpty(token) == false` can be simplified to '!Strings.isEmpty(token)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java

				// on the last component of the resource path
				if (tokens.hasMoreTokens() == false && Strings.isEmpty(token) == false)
				{
					final IResource resource = reference.getResource();
```

### PointlessBooleanExpression
`parameters.isEmpty() == false` can be simplified to '!parameters.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java
			}

			if (parameters.isEmpty() == false)
			{
				url = encodePageParameters(url, parameters, pageParametersEncoder);
```

### PointlessBooleanExpression
`setPlaceholders(copy, url) == false` can be simplified to '!setPlaceholders(copy, url)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
			PageParameters copy = newPageParameters();
			copy.mergeWith(info.getPageParameters());
			if (setPlaceholders(copy, url) == false)
			{
				// mandatory parameter is not provided => cannot build Url
```

### PointlessBooleanExpression
`isValidClassName(className) == false` can be simplified to '!isValidClassName(className)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
			String className = cleanClassName(name);

			if (isValidClassName(className) == false)
			{
				return null;
```

### PointlessBooleanExpression
`Modifier.isAbstract(pageClass.getModifiers()) == false` can be simplified to '!Modifier.isAbstract(pageClass.getModifiers())'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PackageMapper.java`
#### Snippet
```java
			Class<? extends IRequestablePage> pageClass = getPageClass(fullyQualifiedClassName);

			if (pageClass != null && Modifier.isAbstract(pageClass.getModifiers()) == false &&
				IRequestablePage.class.isAssignableFrom(pageClass))
			{
```

### PointlessBooleanExpression
`Strings.isEmpty(sessionId) == false` can be simplified to '!Strings.isEmpty(sessionId)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java
		String sessionId = getSessionId();
		boolean hasResponse = false;
		if (Strings.isEmpty(sessionId) == false)
		{
			hasResponse = WebApplication.get().hasBufferedResponse(sessionId, url);
```

### PointlessBooleanExpression
`Strings.isEmpty(sessionId) == false` can be simplified to '!Strings.isEmpty(sessionId)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java
		String sessionId = getSessionId();
		BufferedWebResponse response = null;
		if (Strings.isEmpty(sessionId) == false)
		{
			response = WebApplication.get().getAndRemoveBufferedResponse(sessionId, url);
```

### PointlessBooleanExpression
`render == false` can be simplified to '!render'
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		// If the open tag requires a close tag
		boolean render = openTag.requiresCloseTag();
		if (render == false)
		{
			// Tags like <p> do not require a close tag, but they may have.
```

### PointlessBooleanExpression
`throwException == true` can be simplified to 'throwException'
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		}

		if (throwException == true)
		{
			// throw exception since there is no associated markup
```

### PointlessBooleanExpression
`(elem instanceof ComponentTag) == false` can be simplified to '!(elem instanceof ComponentTag)'
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		// Get open tag in associated markup of border component
		MarkupElement elem = associatedMarkupStream.get();
		if ((elem instanceof ComponentTag) == false)
		{
			associatedMarkupStream.throwMarkupException("Expected the open tag. Markup for a "
```

### PointlessBooleanExpression
`setPlaceholders(copy, url) == false` can be simplified to '!setPlaceholders(copy, url)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MountedMapper.java`
#### Snippet
```java
		PageParameters copy = newPageParameters();
		copy.mergeWith(info.getPageParameters());
		if (setPlaceholders(copy, url) == false)
		{
			// mandatory parameter is not provided => cannot build Url
```

### PointlessBooleanExpression
`component.getOutputMarkupId() == false` can be simplified to '!component.getOutputMarkupId()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/AbstractPartialPageRequestHandler.java`
#### Snippet
```java
            Args.notNull(component, "component");

            if (component.getOutputMarkupId() == false && !(component instanceof Page))
            {
                throw new IllegalArgumentException(
```

### PointlessBooleanExpression
`component.getOutputMarkupId() == false` can be simplified to '!component.getOutputMarkupId()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/AbstractPartialPageRequestHandler.java`
#### Snippet
```java
    public final void focusComponent(Component component)
    {
        if (component != null && component.getOutputMarkupId() == false)
        {
            throw new IllegalArgumentException(
```

### PointlessBooleanExpression
`PageParameters.equals(constructionPageParameters, pageParameters) == false` can be simplified to '!PageParameters.equals(constructionPageParameters, pageParameters)'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
			provider.getPageInstance().getPageParameters() : new PageParameters();

		if (PageParameters.equals(constructionPageParameters, pageParameters) == false)
		{
			// create a fresh page instance because the request page parameters are different than the ones
```

### PointlessBooleanExpression
`page.isPageStateless() == false` can be simplified to '!page.isPageStateless()'
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
			IRequestablePage page = handler.getPage();

			if (page.isPageStateless() == false)
			{
				pageId = page.getPageId();
```

### PointlessBooleanExpression
`headers.containsHeader(name) == false` can be simplified to '!headers.containsHeader(name)'
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebResponse.java`
#### Snippet
```java
		if ("Content-Type".equalsIgnoreCase(name))
		{
			if (headers.containsHeader(name) == false)
			{
				setContentType(value);
```

### PointlessBooleanExpression
`Strings.isEmpty(contents) == false` can be simplified to '!Strings.isEmpty(contents)'
in `wicket-core/src/main/java/org/apache/wicket/page/XmlPartialPageUpdate.java`
#### Snippet
```java
	private void writeHeaderContribution(Response response, String elementName, CharSequence contents)
	{
		if (Strings.isEmpty(contents) == false)
		{
			response.write("<" + elementName + ">");
```

### PointlessBooleanExpression
`behaviors.isEmpty() == false` can be simplified to '!behaviors.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTesterHelper.java`
#### Snippet
```java
		List<AjaxEventBehavior> behaviors = findAjaxEventBehaviors(component, event);
		AjaxEventBehavior behavior = null;
		if (behaviors != null && behaviors.isEmpty() == false)
		{
			behavior = behaviors.get(0);
```

### PointlessBooleanExpression
`date instanceof Instant == false` can be simplified to '!(date instanceof Instant)'
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebRequest.java`
#### Snippet
```java
		Object date = dates.get(0);

		if (date instanceof Instant == false)
		{
			throw new WicketRuntimeException("Date header with name '" + name +
```

### PointlessBooleanExpression
`equals == false` can be simplified to '!equals'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java

		boolean equals = compareMarkup(document, reference);
		if (equals == false)
		{
			// Change the condition to true, if you want to make the new output
```

### PointlessBooleanExpression
`isIgnoreAttributeModifier() == false` can be simplified to '!isIgnoreAttributeModifier()'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			List<? extends Behavior> behaviors = getBehaviors();
			if ((behaviors != null) && !behaviors.isEmpty() && !tag.isClose() &&
				(isIgnoreAttributeModifier() == false))
			{
				tag = tag.mutable();
```

### PointlessBooleanExpression
`wasRendered == false` can be simplified to '!wasRendered'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			// Allow component to contribute
			boolean wasRendered = response.wasRendered(this);
			if (wasRendered == false)
			{
				StringResponse markupHeaderResponse = new StringResponse();
```

### PointlessBooleanExpression
`Strings.isEmpty(headerContribution) == false` can be simplified to '!Strings.isEmpty(headerContribution)'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
					getMarkupSourcingStrategy().renderHead(this, container);
					CharSequence headerContribution = markupHeaderResponse.getBuffer();
					if (Strings.isEmpty(headerContribution) == false)
					{
						response.render(StringHeaderItem.forString(headerContribution));
```

### PointlessBooleanExpression
`wasRendered == false` can be simplified to '!wasRendered'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			}
			
			if (wasRendered == false)
			{
				response.markRendered(this);
```

### PointlessBooleanExpression
`renderBodyOnly == false` can be simplified to '!renderBodyOnly'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			{
				// Render the close tag
				if ((renderBodyOnly == false) && needToRenderTag(openTag))
				{
					openTag.writeSyntheticCloseTag(getResponse());
```

### PointlessBooleanExpression
`Strings.isEmpty(componentPathAttributeName) == false` can be simplified to '!Strings.isEmpty(componentPathAttributeName)'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		DebugSettings debugSettings = getApplication().getDebugSettings();
		String componentPathAttributeName = debugSettings.getComponentPathAttributeName();
		if (Strings.isEmpty(componentPathAttributeName) == false)
		{
			String path = getPageRelativePath();
```

### PointlessBooleanExpression
`found == false` can be simplified to '!found'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			}

			if (found == false)
			{
				String msg = String.format("Component [%s] (path = [%s]) must be applied to a tag "
```

### PointlessBooleanExpression
`renderBodyOnly == false` can be simplified to '!renderBodyOnly'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
					renderClosingComponentTag(markupStream, tag, renderBodyOnly);
				}
				else if (renderBodyOnly == false)
				{
					if (needToRenderTag(openTag))
```

### PointlessBooleanExpression
`renderTag == false` can be simplified to '!renderTag'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		// synthetic close tag must be rendered.
		boolean renderTag = (openTag != null && !(openTag instanceof WicketTag));
		if (renderTag == false)
		{
			renderTag = !getApplication().getMarkupSettings().getStripWicketTags();
```

### PointlessBooleanExpression
`getFlag(FLAG_IGNORE_ATTRIBUTE_MODIFIER) != false` can be simplified to 'getFlag(FLAG_IGNORE_ATTRIBUTE_MODIFIER)'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		// Ignore AttributeModifiers when FLAG_IGNORE_ATTRIBUTE_MODIFIER is set
		if ((behavior instanceof AttributeModifier) &&
			(getFlag(FLAG_IGNORE_ATTRIBUTE_MODIFIER) != false))
		{
			return false;
```

### PointlessBooleanExpression
`Strings.isEmpty(id) == false` can be simplified to '!Strings.isEmpty(id)'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		{
			String id = tag.getAttribute("id");
			if (Strings.isEmpty(id) == false)
			{
				return id.trim();
```

### PointlessBooleanExpression
`(isVisibleInHierarchy() && isEnabledInHierarchy()) == false` can be simplified to '!(isVisibleInHierarchy() \&\& isEnabledInHierarchy())'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	public final boolean isStateless()
	{
		if ((isVisibleInHierarchy() && isEnabledInHierarchy()) == false && canCallListener() == false)
		{
			// the component is either invisible or disabled and it can't call listeners
```

### PointlessBooleanExpression
`canCallListener() == false` can be simplified to '!canCallListener()'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	public final boolean isStateless()
	{
		if ((isVisibleInHierarchy() && isEnabledInHierarchy()) == false && canCallListener() == false)
		{
			// the component is either invisible or disabled and it can't call listeners
```

### PointlessBooleanExpression
`Strings.isEmpty(markup) == false` can be simplified to '!Strings.isEmpty(markup)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
				String markup = parser.getInput(startPos, endPos).toString();

				if (Strings.isEmpty(markup) == false)
				{
					XmlPullParser p = new XmlPullParser();
```

### PointlessBooleanExpression
`Strings.isEmpty(markup) == false` can be simplified to '!Strings.isEmpty(markup)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
			String markup = getMarkup();

			if (Strings.isEmpty(markup) == false)
			{
				childTag = TagTester.createTagByAttribute(markup, attribute, value);
```

### PointlessBooleanExpression
`Strings.isEmpty(markup) == false` can be simplified to '!Strings.isEmpty(markup)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
		List<TagTester> testers = new ArrayList<>();

		if ((Strings.isEmpty(markup) == false))
		{
			try
```

### PointlessBooleanExpression
`requiresCloseTag(openTag.getName()) == false` can be simplified to '!requiresCloseTag(openTag.getName())'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
									closeTag.setOpenTag(openTag);
								}
								else if (requiresCloseTag(openTag.getName()) == false)
								{
									// no closeTag for current openTag (allowed)
```

### PointlessBooleanExpression
`Objects.equals(locale, getLocale()) == false` can be simplified to '!Objects.equals(locale, getLocale())'
in `wicket-core/src/main/java/org/apache/wicket/util/template/PackageTextTemplate.java`
#### Snippet
```java
	public void setLocale(Locale locale)
	{
		if (Objects.equals(locale, getLocale()) == false)
		{
			buffer.setLength(0);
```

### PointlessBooleanExpression
`Objects.equals(variation, getVariation()) == false` can be simplified to '!Objects.equals(variation, getVariation())'
in `wicket-core/src/main/java/org/apache/wicket/util/template/PackageTextTemplate.java`
#### Snippet
```java
	public void setVariation(String variation)
	{
		if (Objects.equals(variation, getVariation()) == false)
		{
			buffer.setLength(0);
```

### PointlessBooleanExpression
`Objects.equals(encoding, this.encoding) == false` can be simplified to '!Objects.equals(encoding, this.encoding)'
in `wicket-core/src/main/java/org/apache/wicket/util/template/PackageTextTemplate.java`
#### Snippet
```java
	public void setEncoding(String encoding)
	{
		if (Objects.equals(encoding, this.encoding) == false)
		{
			buffer.setLength(0);
```

### PointlessBooleanExpression
`Objects.equals(style, getStyle()) == false` can be simplified to '!Objects.equals(style, getStyle())'
in `wicket-core/src/main/java/org/apache/wicket/util/template/PackageTextTemplate.java`
#### Snippet
```java
	public void setStyle(String style)
	{
		if (Objects.equals(style, getStyle()) == false)
		{
			buffer.setLength(0);
```

### PointlessBooleanExpression
`modelObject instanceof Serializable == false` can be simplified to '!(modelObject instanceof Serializable)'
in `wicket-core/src/main/java/org/apache/wicket/model/ChainingModel.java`
#### Snippet
```java
					+ "If you need to access a property of the session via the model use the "
					+ "page instance as the model object and 'session.attribute' as the path.");
		} else if (modelObject != null && (modelObject instanceof Serializable == false))
		{
			LOG.warn("It is not a good idea to reference non-serializable {} "
```

### PointlessBooleanExpression
`Objects.equal(expectedLocale, actualLocale) == false` can be simplified to '!Objects.equal(expectedLocale, actualLocale)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTester.java`
#### Snippet
```java

		Locale actualLocale = markup.getMarkupResourceStream().getLocale();
		if (Objects.equal(expectedLocale, actualLocale) == false)
		{
			result = Result
```

### PointlessBooleanExpression
`Objects.equal(expectedStyle, actualStyle) == false` can be simplified to '!Objects.equal(expectedStyle, actualStyle)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTester.java`
#### Snippet
```java

		String actualStyle = markup.getMarkupResourceStream().getStyle();
		if (Objects.equal(expectedStyle, actualStyle) == false)
		{
			result = Result
```

### PointlessBooleanExpression
`Objects.equal(expectedVariation, actualVariation) == false` can be simplified to '!Objects.equal(expectedVariation, actualVariation)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTester.java`
#### Snippet
```java

		String actualVariation = markup.getMarkupResourceStream().getVariation();
		if (Objects.equal(expectedVariation, actualVariation) == false)
		{
			result = Result.fail(
```

### PointlessBooleanExpression
`Strings.isEmpty(expression) == false` can be simplified to '!Strings.isEmpty(expression)'
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
	{
		String expression = propertyExpression();
		if (Strings.isEmpty(expression) == false)
		{
			Object target = getInnermostModelOrObject();
```

### PointlessBooleanExpression
`Strings.isEmpty(expression) == false` can be simplified to '!Strings.isEmpty(expression)'
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
	{
		String expression = propertyExpression();
		if (Strings.isEmpty(expression) == false)
		{
			Object target = getInnermostModelOrObject();
```

### PointlessBooleanExpression
`Strings.isEmpty(expression) == false` can be simplified to '!Strings.isEmpty(expression)'
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
	{
		String expression = propertyExpression();
		if (Strings.isEmpty(expression) == false)
		{
			Object target = getInnermostModelOrObject();
```

### PointlessBooleanExpression
`markupOnly == false` can be simplified to '!markupOnly'
in `wicket-core/src/main/java/org/apache/wicket/markup/Markup.java`
#### Snippet
```java
		final AppendingStringBuffer buf = new AppendingStringBuffer(400);

		if (markupOnly == false)
		{
			if (markupResourceStream != null)
```

### PointlessBooleanExpression
`markupOnly == false` can be simplified to '!markupOnly'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFragment.java`
#### Snippet
```java
	{
		final AppendingStringBuffer buf = new AppendingStringBuffer(400);
		if (markupOnly == false)
		{
			buf.append(getRootMarkup().getMarkupResourceStream().toString());
```

### PointlessBooleanExpression
`(startElem instanceof ComponentTag) == false` can be simplified to '!(startElem instanceof ComponentTag)'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFragment.java`
#### Snippet
```java
		// Make sure we are at an open tag
		MarkupElement startElem = markup.get(startIndex);
		if ((startElem instanceof ComponentTag) == false)
		{
			throw new IllegalArgumentException(
```

### PointlessBooleanExpression
`HtmlHandler.requiresCloseTag(startTag.getName()) == false` can be simplified to '!HtmlHandler.requiresCloseTag(startTag.getName())'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFragment.java`
#### Snippet
```java
		else if (startTag.hasNoCloseTag())
		{
			if (HtmlHandler.requiresCloseTag(startTag.getName()) == false)
			{
				// set endIndex to a "good" value
```

### PointlessBooleanExpression
`stripWicketAttributes == true` can be simplified to 'stripWicketAttributes'
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

		String namespacePrefix = null;
		if (stripWicketAttributes == true)
		{
			namespacePrefix = namespace + ":";
```

### PointlessBooleanExpression
`key.startsWith(namespacePrefix) == false` can be simplified to '!key.startsWith(namespacePrefix)'
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
				}

				if ((namespacePrefix == null) || (key.startsWith(namespacePrefix) == false))
				{
					response.write(" ");
```

### PointlessBooleanExpression
`wicketHeadProcessed == false` can be simplified to '!wicketHeadProcessed'
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java

				// Process the head of the extended markup only once
				if (wicketHeadProcessed == false)
				{
					// if </wicket:head> in base markup and no <head>
```

### PointlessBooleanExpression
`foundHeadTag == false` can be simplified to '!foundHeadTag'
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
				{
					// if </wicket:head> in base markup and no <head>
					if (wtag.isClose() && wtag.isHeadTag() && (foundHeadTag == false))
					{
						wicketHeadProcessed = true;
```

### PointlessBooleanExpression
`wicketHeadProcessed == false` can be simplified to '!wicketHeadProcessed'
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java

			// Process the head of the extended markup only once
			if (wicketHeadProcessed == false)
			{
				// Remember that we found <head> in the base markup
```

### PointlessBooleanExpression
`key.startsWith(namespace) == false` can be simplified to '!key.startsWith(namespace)'
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
			{
				// exclude "wicket:XX" attributes
				if (key.startsWith(namespace) == false)
				{
					String separator = ATTRIBUTES_SEPARATORS.getString(key, DEFAULT_ATTRIBUTE_SEPARATOR);
```

### PointlessBooleanExpression
`tag.isIgnore() == false` can be simplified to '!tag.isIgnore()'
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
							// Add to the markup unless the tag has been flagged as
							// to be removed from the markup. (e.g. <wicket:remove>
							if (tag.isIgnore() == false)
							{
								markup.addMarkupElement(tag);
```

### PointlessBooleanExpression
`enforceReload == false` can be simplified to '!enforceReload'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
		if (cacheKey != null)
		{
			if (enforceReload == false)
			{
				// get the location String
```

### PointlessBooleanExpression
`enforceReload == false` can be simplified to '!enforceReload'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
		// Is the markup already in the cache?
		Markup markup = null;
		if ((enforceReload == false) && (cacheKey != null))
		{
			markup = getMarkupFromCache(cacheKey, container);
```

### PointlessBooleanExpression
`markupCache.containsKey(locationString) == false` can be simplified to '!markupCache.containsKey(locationString)'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
		if (locationString != null)
		{
			if (markupCache.containsKey(locationString) == false)
			{
				// The default cache implementation is a ConcurrentHashMap. Thus neither the key nor
```

### PointlessBooleanExpression
`checkMarkupType(container) == false` can be simplified to '!checkMarkupType(container)'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java
		Args.notNull(container, "container");

		if (checkMarkupType(container) == false)
		{
			// TODO improve: Result { boolean success, enum FailureReason {not found, not yet
```

### PointlessBooleanExpression
`checkMarkupType(container) == false` can be simplified to '!checkMarkupType(container)'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java
		Args.notNull(container, "container");

		if (checkMarkupType(container) == false)
		{
			// TODO improve: Result { boolean success, enum FailureReason {not found, not yet
```

### PointlessBooleanExpression
`checkMarkupType(container) == false` can be simplified to '!checkMarkupType(container)'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java
		Args.notNull(markupResourceStream, "markupResourceStream");

		if (checkMarkupType(container) == false)
		{
			// TODO improve: Result { boolean success, enum FailureReason {not found, not yet
```

### PointlessBooleanExpression
`Strings.isEmpty(doctype) == false` can be simplified to '!Strings.isEmpty(doctype)'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupResourceStream.java`
#### Snippet
```java
	public final void setDoctype(final CharSequence doctype)
	{
		if (Strings.isEmpty(doctype) == false)
		{
			String doc = doctype.toString().replaceAll("[\n\r]+", "");
```

### PointlessBooleanExpression
`matcher.matches() == false` can be simplified to '!matcher.matches()'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupResourceStream.java`
#### Snippet
```java
			doc = doc.replaceAll("\\s+", " ");
			Matcher matcher = DOCTYPE_REGEX.matcher(doc);
			if (matcher.matches() == false)
			{
				throw new MarkupException("Invalid DOCTYPE: '" + doctype + "'");
```

### PointlessBooleanExpression
`startTag.hasNoCloseTag() == false` can be simplified to '!startTag.hasNoCloseTag()'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
			// With HTML not all tags require a close tag which
			// must have been detected by the HtmlHandler earlier on.
			if (startTag.hasNoCloseTag() == false)
			{
				// Skip <tag>
```

### PointlessBooleanExpression
`(elem instanceof ComponentTag) == false` can be simplified to '!(elem instanceof ComponentTag)'
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
	{
		MarkupElement elem = get(currentIndex - 1);
		if ((elem instanceof ComponentTag) == false)
		{
			throwMarkupException("Tag expected");
```

### PointlessBooleanExpression
`Strings.isEmpty(js) == false` can be simplified to '!Strings.isEmpty(js)'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnDomReadyHeaderItem.java`
#### Snippet
```java
	{
		CharSequence js = getJavaScript();
		if (Strings.isEmpty(js) == false)
		{
			AttributeMap attributes = new AttributeMap();
```

### PointlessBooleanExpression
`Strings.isEmpty(getJavaScript()) == false` can be simplified to '!Strings.isEmpty(getJavaScript())'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnEventHeaderItem.java`
#### Snippet
```java
	public void render(Response response)
	{
		if (Strings.isEmpty(getJavaScript()) == false)
		{
			AttributeMap attributes = new AttributeMap();
```

### PointlessBooleanExpression
`Strings.isEmpty(js) == false` can be simplified to '!Strings.isEmpty(js)'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnLoadHeaderItem.java`
#### Snippet
```java
	{
		CharSequence js = getJavaScript();
		if (Strings.isEmpty(js) == false)
		{
			AttributeMap attributes = new AttributeMap();
```

### PointlessBooleanExpression
`Strings.isEmpty(js) == false` can be simplified to '!Strings.isEmpty(js)'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/JavaScriptDeferHeaderResponse.java`
#### Snippet
```java
		{
			CharSequence js = getJavaScript();
			if (Strings.isEmpty(js) == false)
			{
				JavaScriptUtils.writeJavaScript(response, "window.addEventListener('load', function() { " + js + "; });");
```

### PointlessBooleanExpression
`Strings.isEmpty(js) == false` can be simplified to '!Strings.isEmpty(js)'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/JavaScriptDeferHeaderResponse.java`
#### Snippet
```java
		{
			CharSequence js = getJavaScript();
			if (Strings.isEmpty(js) == false)
			{
				JavaScriptUtils.writeJavaScript(response, "document.addEventListener('DOMContentLoaded', function() { " + js + "; });");
```

### PointlessBooleanExpression
`responseFilterMap.containsKey(filterName) == false` can be simplified to '!responseFilterMap.containsKey(filterName)'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/FilteringHeaderResponse.java`
#### Snippet
```java
			String filterName = ((FilteredHeaderItem)item).getFilterName();

			if (responseFilterMap.containsKey(filterName) == false)
			{
				responseFilterMap.put(filterName, new ArrayList<HeaderItem>());
```

### PointlessBooleanExpression
`responseFilterMap.containsKey(headerFilterName) == false` can be simplified to '!responseFilterMap.containsKey(headerFilterName)'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/FilteringHeaderResponse.java`
#### Snippet
```java

			// none of the configured filters accepted it so put it in the header
			if (responseFilterMap.containsKey(headerFilterName) == false)
			{
				responseFilterMap.put(headerFilterName, new ArrayList<HeaderItem>());
```

### PointlessBooleanExpression
`responseFilterMap.containsKey(filterName) == false` can be simplified to '!responseFilterMap.containsKey(filterName)'
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/FilteringHeaderResponse.java`
#### Snippet
```java
	private void render(HeaderItem item, String filterName)
	{
		if (responseFilterMap.containsKey(filterName) == false)
		{
			throw new IllegalArgumentException("No filter named '" + filterName +
```

### PointlessBooleanExpression
`rs.isHtml5() == false` can be simplified to '!rs.isHtml5()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/MarkupUtil.java`
#### Snippet
```java
				{
					MarkupResourceStream rs = associatedMarkup.getMarkupResourceStream();
					if (rs.isHtml5() == false)
					{
						rtn[0] = false;
```

### PointlessBooleanExpression
`isAjax == false` can be simplified to '!isAjax'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		{
			// If it's not ajax we fail
			if (isAjax == false)
			{
				fail("Link " + path + "is an AjaxLink and will " +
```

### PointlessBooleanExpression
`isAjax == false` can be simplified to '!isAjax'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		{
			// If it's not ajax we fail
			if (isAjax == false)
			{
				fail("Link " + path + " is an AjaxSubmitLink and " +
```

### PointlessBooleanExpression
`isAjax == false` can be simplified to '!isAjax'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		}
		// The link requires AJAX
		else if (linkComponent instanceof IAjaxLink && isAjax == false)
		{
			fail("Link " + path + "is an IAjaxLink and will " +
```

### PointlessBooleanExpression
`component instanceof FormComponent`` == false` can be simplified to '!(component instanceof FormComponent)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		Component component = assertExists(path);

		if (component instanceof FormComponent<?> == false)
		{
			fail("path: '" + path + "' is not a form component");
```

### PointlessBooleanExpression
`path.startsWith(componentIdPageId) == false` can be simplified to '!path.startsWith(componentIdPageId)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		{
			String componentIdPageId = componentInPage.component.getId() + ':';
			if (path.startsWith(componentIdPageId) == false)
			{
				path = componentIdPageId + path;
```

### PointlessBooleanExpression
`component.isVisibleInHierarchy() == false` can be simplified to '!component.isVisibleInHierarchy()'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		Result res = Result.pass();

		if (component.isVisibleInHierarchy() == false)
		{
			res = Result.fail(
```

### PointlessBooleanExpression
`component.isEnabledInHierarchy() == false` can be simplified to '!component.isEnabledInHierarchy()'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		}

		if (component.isEnabledInHierarchy() == false)
		{
			res = Result.fail(
```

### PointlessBooleanExpression
`originalPort.equals(newPort) == false` can be simplified to '!originalPort.equals(newPort)'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
			return false; // no new host
		}
		else if (originalPort.equals(newPort) == false)
		{
			return true;
```

### PointlessBooleanExpression
`Strings.isEmpty(entry.getKey()) == false` can be simplified to '!Strings.isEmpty(entry.getKey())'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
			for (Map.Entry<String, String> entry : preHeader.entrySet())
			{
				if (Strings.isEmpty(entry.getKey()) == false)
				{
					request.setHeader(entry.getKey(), entry.getValue());
```

### PointlessBooleanExpression
`acceptExtension(ext) == false` can be simplified to '!acceptExtension(ext)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/PackageResourceGuard.java`
#### Snippet
```java
		}

		if (acceptExtension(ext) == false)
		{
			log.warn("Access denied to shared (static) resource because of the file extension: " +
```

### PointlessBooleanExpression
`acceptFile(filename) == false` can be simplified to '!acceptFile(filename)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/PackageResourceGuard.java`
#### Snippet
```java

		String filename = Strings.lastPathComponent(path, File.separatorChar);
		if (acceptFile(filename) == false)
		{
			log.warn("Access denied to shared (static) resource because of the file name: " + path);
```

### PointlessBooleanExpression
`super.accept(path) == false` can be simplified to '!super.accept(path)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java

		// Check typical files such as log4j.xml etc.
		if (super.accept(path) == false)
		{
			return false;
```

### PointlessBooleanExpression
`hit == false` can be simplified to '!hit'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
		}

		if (hit == false)
		{
			log.warn("Access denied to shared (static) resource: " + path);
```

### PointlessBooleanExpression
`Strings.isEmpty(encoding) == false` can be simplified to '!Strings.isEmpty(encoding)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebPage.java`
#### Snippet
```java
		final String encoding = getApplication().getRequestCycleSettings()
			.getResponseRequestEncoding();
		final boolean validEncoding = (Strings.isEmpty(encoding) == false);
		final String contentType;
		if (validEncoding)
```

### PointlessBooleanExpression
`component instanceof TransparentWebMarkupContainer == false` can be simplified to '!(component instanceof TransparentWebMarkupContainer)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebPage.java`
#### Snippet
```java
					visit.stop((HtmlHeaderContainer)component);
				}
				else if (component instanceof TransparentWebMarkupContainer == false)
				{
					visit.dontGoDeeper();
```

### PointlessBooleanExpression
`isInitialization == false` can be simplified to '!isInitialization'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebPage.java`
#### Snippet
```java
	{
		Request request = getRequest();
		if (isInitialization == false && request instanceof WebRequest &&
			((WebRequest)request).isAjax())
		{
```

### PointlessBooleanExpression
`Strings.isEmpty(componentPathAttributeName) == false` can be simplified to '!Strings.isEmpty(componentPathAttributeName)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
		DebugSettings debugSettings = getApplication().getDebugSettings();
		String componentPathAttributeName = debugSettings.getComponentPathAttributeName();
		if (Strings.isEmpty(componentPathAttributeName) == false)
		{
			CharSequence path = getPageRelativePath();
```

### PointlessBooleanExpression
`fc.isValid() == false` can be simplified to '!fc.isValid()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentLabel.java`
#### Snippet
```java
				tag.append("class", getString(REQUIRED_CSS_CLASS_KEY), " ");
			}
			if (fc.isValid() == false)
			{
				tag.append("class", getString(INVALID_CSS_CLASS_KEY), " ");
```

### PointlessBooleanExpression
`formComponent.isEnabledInHierarchy() == false` can be simplified to '!formComponent.isEnabledInHierarchy()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentLabel.java`
#### Snippet
```java
		}

		if (formComponent.isEnabledInHierarchy() == false)
		{
			tag.append("class", getString(DISABLED_CSS_CLASS_KEY), " ");
```

### PointlessBooleanExpression
`Strings.isEmpty(value) == false` can be simplified to '!Strings.isEmpty(value)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Button.java`
#### Snippet
```java
		{
			String value = getDefaultModelObjectAsString();
			if (Strings.isEmpty(value) == false)
			{
				tag.put("value", value);
```

### PointlessBooleanExpression
`Strings.isEmpty(modelObjectAsString) == false` can be simplified to '!Strings.isEmpty(modelObjectAsString)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Button.java`
#### Snippet
```java
		{
			String modelObjectAsString = getDefaultModelObjectAsString();
			if (Strings.isEmpty(modelObjectAsString) == false) {
				replaceComponentTagBody(markupStream, openTag, modelObjectAsString);
				return;
```

### PointlessBooleanExpression
`isBehaviorAccepted(behavior) == false` can be simplified to '!isBehaviorAccepted(behavior)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
		for (Behavior behavior : getBehaviors())
		{
			if (isBehaviorAccepted(behavior) == false)
			{
				continue;
```

### PointlessBooleanExpression
`isNull == false` can be simplified to '!isNull'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
			if (validator != null)
			{
				if (isNull == false || validator instanceof INullAcceptingValidator<?>)
				{
					try
```

### PointlessBooleanExpression
`fileUploads.isEmpty() == false` can be simplified to '!fileUploads.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUploadField.java`
#### Snippet
```java
	{
		List<FileUpload> fileUploads = getFileUploads();
		if (fileUploads.isEmpty() == false)
		{
			List<String> clientFileNames = new ArrayList<>();
```

### PointlessBooleanExpression
`Strings.isEmpty(item.getName()) == false` can be simplified to '!Strings.isEmpty(item.getName())'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUploadField.java`
#### Snippet
```java
				{
					// WICKET-6270 detect empty field by missing file name
					if (Strings.isEmpty(item.getName()) == false) {
						fileUploads.add(new FileUpload(item));
					}
```

### PointlessBooleanExpression
`formComponent.processChildren() == false` can be simplified to '!formComponent.processChildren()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
				validate(formComponent);
			}
			if (formComponent.processChildren() == false)
			{
				visit.dontGoDeeper();
```

### PointlessBooleanExpression
`isEnabledInHierarchy() == false` can be simplified to '!isEnabledInHierarchy()'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
		super.onComponentTag(tag);

		if (isEnabledInHierarchy() == false)
		{
			disableLink(tag);
```

### PointlessBooleanExpression
`Strings.isEmpty(componentPathAttributeName) == false` can be simplified to '!Strings.isEmpty(componentPathAttributeName)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
		DebugSettings debugSettings = getApplication().getDebugSettings();
		String componentPathAttributeName = debugSettings.getComponentPathAttributeName();
		if (Strings.isEmpty(componentPathAttributeName) == false)
		{
			CharSequence path = getPageRelativePath();
```

### PointlessBooleanExpression
`markupStream.get().closes(openTag) == false` can be simplified to '!markupStream.get().closes(openTag)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/PanelMarkupSourcingStrategy.java`
#### Snippet
```java
			{
				markupStream.skipRawMarkup();
				if (markupStream.get().closes(openTag) == false)
				{
					StringBuilder msg = new StringBuilder();
```

### PointlessBooleanExpression
`msg.equals(cause.getMessage()) == false` can be simplified to '!msg.equals(cause.getMessage())'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/ExceptionErrorPage.java`
#### Snippet
```java
			{
				String msg = throwable.getMessage();
				if ((msg != null) && (msg.equals(cause.getMessage()) == false))
				{
					if (throwable instanceof MarkupException)
```

### PointlessBooleanExpression
`(fragmentTag instanceof WicketTag && ((WicketTag)fragmentTag).isFragmentTag()) == false` can be simplified to '!(fragmentTag instanceof WicketTag \&\& ((WicketTag)fragmentTag).isFragmentTag())'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FragmentMarkupSourcingStrategy.java`
#### Snippet
```java
		{
			MarkupElement fragmentTag = childMarkup.get(0);
			if ((fragmentTag instanceof WicketTag && ((WicketTag)fragmentTag).isFragmentTag()) == false)
			{
				throw new MarkupNotFoundException("Markup found for Fragment '" + markupId
```

### PointlessBooleanExpression
`markupStream.get().closes(openTag) == false` can be simplified to '!markupStream.get().closes(openTag)'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AbstractMarkupSourcingStrategy.java`
#### Snippet
```java
		{
			markupStream.skipRawMarkup();
			if (markupStream.get().closes(openTag) == false)
			{
				throw new MarkupException(
```

### PointlessBooleanExpression
`body.rendering == false` can be simplified to '!body.rendering'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
	{
		// make sure nested borders are resolved properly
		if (body.rendering == false)
		{
			// We are only interested in border body tags. The tag ID actually is irrelevant since
```

### PointlessBooleanExpression
`foundHead == false` can be simplified to '!foundHead'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java

		// We found <body>
		if (foundHead == false)
		{
			insertHeadTag();
```

### PointlessBooleanExpression
`ignoreTheRest == true` can be simplified to 'ignoreTheRest'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java
	{
		// Whatever there is left in the markup, ignore it
		if (ignoreTheRest == true)
		{
			return tag;
```

### PointlessBooleanExpression
`tag.isClose() == false` can be simplified to '!tag.isClose()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketTagIdentifier.java`
#### Snippet
```java
					tag.setFlag(ComponentTag.RENDER_RAW, true);
				}
				else if (tag.isClose() == false)
				{
					tag.setAutoComponentTag(true);
```

### PointlessBooleanExpression
`tag.isAutolinkEnabled() == true` can be simplified to 'tag.isAutolinkEnabled()'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java

		// Don't touch any wicket:id component and any auto-components
		if ((tag instanceof WicketTag) || (tag.isAutolinkEnabled() == true)
			|| (tag.getAttributes().get(wicketIdAttr) != null))
		{
```

### PointlessBooleanExpression
`attrValue.startsWith("/") == false` can be simplified to '!attrValue.startsWith("/")'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java
		{
			String attrValue = tag.getAttributes().getString(attrName);
			if ((attrValue != null) && (attrValue.startsWith("/") == false)
				&& (!attrValue.contains(":")) && !(attrValue.startsWith("#")))
			{
```

### PointlessBooleanExpression
`attrValue.startsWith("/") == false` can be simplified to '!attrValue.startsWith("/")'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java
				String attrValue = tag.getAttributes().getString(attrName);

				if ((attrValue != null) && (attrValue.startsWith("/") == false)
					&& (!attrValue.contains(":")) && !(attrValue.startsWith("#")))
				{
```

### PointlessBooleanExpression
`Character.isLetterOrDigit(buffer.getValue()[i]) == false` can be simplified to '!Character.isLetterOrDigit(buffer.getValue()\[i\])'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HeadForceTagIdHandler.java`
#### Snippet
```java
		for (int i = 0; i < buffer.getValue().length; ++i)
		{
			if (Character.isLetterOrDigit(buffer.getValue()[i]) == false)
			{
				buffer.getValue()[i] = '-';
```

### PointlessBooleanExpression
`inHead == true` can be simplified to 'inHead'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HeadForceTagIdHandler.java`
#### Snippet
```java
		}
		// no, it's not. Are we in <wicket:head> ?
		else if (inHead == true)
		{
			// is the tag open and has empty wicket:id?
```

### PointlessBooleanExpression
`tag instanceof WicketTag == false` can be simplified to '!(tag instanceof WicketTag)'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HeadForceTagIdHandler.java`
#### Snippet
```java
		{
			// is the tag open and has empty wicket:id?
			if ((tag instanceof WicketTag == false) && (tag.getId() == null) &&
				(tag.isOpen() || tag.isOpenClose()) && needId(tag))
			{
```

### PointlessBooleanExpression
`tag.getAttributes().containsKey("src") == false` can be simplified to '!tag.getAttributes().containsKey("src")'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HeadForceTagIdHandler.java`
#### Snippet
```java
	{
		final String name = tag.getName().toLowerCase(Locale.ROOT);
		if (name.equals("script") && tag.getAttributes().containsKey("src") == false)
		{
			return true;
```

### PointlessBooleanExpression
`tag.getAttributes().containsKey("href") == false` can be simplified to '!tag.getAttributes().containsKey("href")'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HeadForceTagIdHandler.java`
#### Snippet
```java
			return true;
		}
		else if (name.equals("style") && tag.getAttributes().containsKey("href") == false)
		{
			return true;
```

### PointlessBooleanExpression
`Strings.isEmpty(attr) == true` can be simplified to 'Strings.isEmpty(attr)'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
					ComponentTag lastEnclosure = componentTagIterator.next();
					String attr = getAttribute(lastEnclosure, null);
					if (Strings.isEmpty(attr) == true)
					{
						lastEnclosure.getAttributes().put(getInlineEnclosureAttributeName(null),
```

### PointlessBooleanExpression
`Strings.isEmpty(attr) == true` can be simplified to 'Strings.isEmpty(attr)'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
				ComponentTag lastEnclosure = enclosures.pop();
				String attr = getAttribute(lastEnclosure, null);
				if (Strings.isEmpty(attr) == true)
				{
					throw new ParseException("Did not find any child for InlineEnclosure. Tag:" +
```

### PointlessBooleanExpression
`Strings.isEmpty(inlineEnclosureChildId) == false` can be simplified to '!Strings.isEmpty(inlineEnclosureChildId)'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
	{
		String inlineEnclosureChildId = getAttribute(tag, markupStream);
		if (Strings.isEmpty(inlineEnclosureChildId) == false)
		{
			String id = tag.getId();
```

### PointlessBooleanExpression
`Strings.isEmpty(wicketMessageAttribute) == false` can be simplified to '!Strings.isEmpty(wicketMessageAttribute)'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketMessageTagHandler.java`
#### Snippet
```java
			getWicketMessageAttrName());

		if (Strings.isEmpty(wicketMessageAttribute) == false)
		{
			// check if this tag is raw markup
```

### PointlessBooleanExpression
`(markupElement instanceof ComponentTag) == false` can be simplified to '!(markupElement instanceof ComponentTag)'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketRemoveTagHandler.java`
#### Snippet
```java
		{
			// skip non-component tags
			if((markupElement instanceof ComponentTag) == false)
			{
				continue;
```

### PointlessBooleanExpression
`autolinking == true` can be simplified to 'autolinking'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketLinkTagHandler.java`
#### Snippet
```java
		// implementation and register the new tag handler with the markup
		// parser through Application.newMarkupParser().
		if ((autolinking == true) && (analyzeAutolinkCondition(tag) == true))
		{
			// Mark it as autolink enabled
```

### PointlessBooleanExpression
`analyzeAutolinkCondition(tag) == true` can be simplified to 'analyzeAutolinkCondition(tag)'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketLinkTagHandler.java`
#### Snippet
```java
		// implementation and register the new tag handler with the markup
		// parser through Application.newMarkupParser().
		if ((autolinking == true) && (analyzeAutolinkCondition(tag) == true))
		{
			// Mark it as autolink enabled
```

### PointlessBooleanExpression
`tagText.endsWith("]]") == false` can be simplified to '!tagText.endsWith("\]\]")'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java
					pos1 = closeBracketIndex + 1;
				}
				while (tagText.endsWith("]]") == false);

				// Move to position after the tag
```

### PointlessBooleanExpression
`usedComponentIds.add(componentId) == false` can be simplified to '!usedComponentIds.add(componentId)'
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
				Component c = i.next();
				String componentId = c.getId();
				if (usedComponentIds.add(componentId) == false)
				{
					log.warn("Repeater '{}' has multiple children with the same component id: '{}'",
```

### PointlessBooleanExpression
`filter.ignoreResolver(resolver) == false` can be simplified to '!filter.ignoreResolver(resolver)'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/ComponentResolvers.java`
#### Snippet
```java
			.getComponentResolvers())
		{
			if ((filter == null) || (filter.ignoreResolver(resolver) == false))
			{
				Component component = resolver.resolve(container, markupStream, tag);
```

### PointlessBooleanExpression
`component instanceof TransparentWebMarkupContainer == false` can be simplified to '!(component instanceof TransparentWebMarkupContainer)'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
						{
							visit.stop((HtmlHeaderContainer) component);
						} else if (component instanceof TransparentWebMarkupContainer == false)
						{
							visit.dontGoDeeper();
```

### PointlessBooleanExpression
`transformBodyOnly == false` can be simplified to '!transformBodyOnly'
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
	protected final void onRender()
	{
		if (transformBodyOnly == false)
		{
			execute(new Runnable()
```

### PointlessBooleanExpression
`transformBodyOnly == true` can be simplified to 'transformBodyOnly'
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
	public final void onComponentTagBody(final MarkupStream markupStream, final ComponentTag openTag)
	{
		if (transformBodyOnly == true)
		{
			execute(new Runnable()
```

### PointlessBooleanExpression
`preCheck(rootComponent) == false` can be simplified to '!preCheck(rootComponent)'
in `wicket-core/src/main/java/org/apache/wicket/markup/renderStrategy/DeepChildFirstVisitor.java`
#### Snippet
```java
		// while walking down, towards the deep child, we validate if the component is visible. If
		// not, there is no need to go any deeper
		if (preCheck(rootComponent) == false)
		{
			return visit;
```

### PointlessBooleanExpression
`authorizationStrategy.isResourceAuthorized(resource, parameters) == false` can be simplified to '!authorizationStrategy.isResourceAuthorized(resource, parameters)'
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceRequestHandler.java`
#### Snippet
```java
		}

		if (authorizationStrategy != null && authorizationStrategy.isResourceAuthorized(resource, parameters) == false)
		{
			if (Application.exists())
```

### PointlessBooleanExpression
`isThrowExceptionIfPropertyNotFound() == true` can be simplified to 'isThrowExceptionIfPropertyNotFound()'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java
							"element with a wicket:id=\"" + variableName + "\".";

						if (isThrowExceptionIfPropertyNotFound() == true)
						{
							markupStream.throwMarkupException(msg);
```

### PointlessBooleanExpression
`variablesReplaced.containsKey(id) == false` can be simplified to '!variablesReplaced.containsKey(id)'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java
			for (String id : childTags.keySet())
			{
				if (variablesReplaced.containsKey(id) == false)
				{
					String msg = "The <wicket:message key=\"" + key +
```

### PointlessBooleanExpression
`isThrowExceptionIfPropertyNotFound() == true` can be simplified to 'isThrowExceptionIfPropertyNotFound()'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java
						"} to the localized text for the wicket:message.";

					if (isThrowExceptionIfPropertyNotFound() == true)
					{
						markupStream.throwMarkupException(msg);
```

### PointlessBooleanExpression
`isThrowExceptionIfPropertyNotFound() == true` can be simplified to 'isThrowExceptionIfPropertyNotFound()'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java
			else
			{
				if (isThrowExceptionIfPropertyNotFound() == true)
				{
					throw new WicketRuntimeException("Property '" + key +
```

### PointlessBooleanExpression
`markupStream.isCurrentIndexInsideTheStream() == false` can be simplified to '!markupStream.isCurrentIndexInsideTheStream()'
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java

				// If open tag was open-close
				if (markupStream.isCurrentIndexInsideTheStream() == false)
				{
					String formatedNotFound = String.format(NOT_FOUND, key);
```

### PointlessBooleanExpression
`isRedirectToBuffer() == false` can be simplified to '!isRedirectToBuffer()'
in `wicket-core/src/main/java/org/apache/wicket/request/handler/render/WebPageRenderer.java`
#### Snippet
```java
		else
		{
			if (isRedirectToBuffer() == false && logger.isDebugEnabled())
			{
				String details = String
```

### PointlessBooleanExpression
`beforeRenderUrl.getSegments().equals(afterRenderUrl.getSegments()) == false` can be simplified to '!beforeRenderUrl.getSegments().equals(afterRenderUrl.getSegments())'
in `wicket-core/src/main/java/org/apache/wicket/request/handler/render/WebPageRenderer.java`
#### Snippet
```java
				.mapUrlFor(getRenderPageRequestHandler());

			if (beforeRenderUrl.getSegments().equals(afterRenderUrl.getSegments()) == false)
			{
				// the amount of segments is different - generated relative URLs
```

### PointlessBooleanExpression
`resourceStream instanceof IResourceStreamWriter == false` can be simplified to '!(resourceStream instanceof IResourceStreamWriter)'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceStreamResource.java`
#### Snippet
```java
		{
			InputStream inputStream = null;
			if (resourceStream instanceof IResourceStreamWriter == false)
			{
				try
```

### PointlessBooleanExpression
`scannedClasses.contains(className) == false` can be simplified to '!scannedClasses.contains(className)'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ClassScanner.java`
#### Snippet
```java
		int count = 0;
		String className = klass.getName();
		if (scannedClasses.contains(className) == false)
		{
			scannedClasses.add(className);
```

### PointlessBooleanExpression
`foundResourceReference((ResourceReference)value) == true` can be simplified to 'foundResourceReference((ResourceReference)value)'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ClassScanner.java`
#### Snippet
```java
						if (value instanceof ResourceReference)
						{
							if (foundResourceReference((ResourceReference)value) == true)
							{
								count += 1;
```

### PointlessBooleanExpression
`versionValue.isEmpty() == false` can be simplified to '!versionValue.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/QueryStringWithVersionResourceCachingStrategy.java`
#### Snippet
```java
			StringValue versionValue = parameters.get(versionParameter);
			RequestCycle requestCycle = RequestCycle.get();
			if (versionValue.isEmpty() == false && requestCycle != null)
			{
				requestCycle.setMetaData(URL_VERSION, versionValue.toString());
```

### PointlessBooleanExpression
`needsBody(data.getStatusCode()) == false` can be simplified to '!needsBody(data.getStatusCode())'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java

		if (!data.dataNeedsToBeWritten(attributes) || data.getErrorCode() != null ||
			needsBody(data.getStatusCode()) == false)
		{
			return;
```

### PointlessBooleanExpression
`Strings.isEmpty(parentEscape) == false` can be simplified to '!Strings.isEmpty(parentEscape)'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
		final String parentEscape = getParentFolderPlaceholder();

		if (Strings.isEmpty(parentEscape) == false)
		{
			path = Strings.replaceAll(name, "../", parentEscape + "/").toString();
```

### PointlessBooleanExpression
`accept(realPath) == false` can be simplified to '!accept(realPath)'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
		}

		if (accept(realPath) == false)
		{
			throw new PackageResourceBlockedException(
```

### PointlessBooleanExpression
`(containerRequest instanceof HttpServletRequest) == false` can be simplified to '!(containerRequest instanceof HttpServletRequest)'
in `wicket-core/src/main/java/org/apache/wicket/session/HttpSessionStore.java`
#### Snippet
```java
	{
		Object containerRequest = request.getContainerRequest();
		if (containerRequest == null || (containerRequest instanceof HttpServletRequest) == false)
		{
			throw new IllegalArgumentException("Request must be ServletWebRequest");
```

### PointlessBooleanExpression
`obj instanceof Key == false` can be simplified to '!(obj instanceof Key)'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
				return true;
			}
			if (obj instanceof Key == false)
			{
				return false;
```

### PointlessBooleanExpression
`obj instanceof UrlAttributes == false` can be simplified to '!(obj instanceof UrlAttributes)'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
				return true;
			}
			if (obj instanceof UrlAttributes == false)
			{
				return false;
```

### PointlessBooleanExpression
`obj instanceof ResourceReference == false` can be simplified to '!(obj instanceof ResourceReference)'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
			return true;
		}
		if (obj instanceof ResourceReference == false)
		{
			return false;
```

### PointlessBooleanExpression
`wicketFilter.processRequest(servletRequest, servletResponse, null) == false` can be simplified to '!wicketFilter.processRequest(servletRequest, servletResponse, null)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java
		final HttpServletResponse servletResponse) throws ServletException, IOException
	{
		if (wicketFilter.processRequest(servletRequest, servletResponse, null) == false)
		{
			fallback(servletRequest, servletResponse);
```

### PointlessBooleanExpression
`wicketFilter.processRequest(servletRequest, servletResponse, null) == false` can be simplified to '!wicketFilter.processRequest(servletRequest, servletResponse, null)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java
		final HttpServletResponse servletResponse) throws ServletException, IOException
	{
		if (wicketFilter.processRequest(servletRequest, servletResponse, null) == false)
		{
			fallback(servletRequest, servletResponse);
```

### PointlessBooleanExpression
`isSessionInvalidated() == false` can be simplified to '!isSessionInvalidated()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebSession.java`
#### Snippet
```java
	public void invalidate()
	{
		if (isSessionInvalidated() == false)
		{
			getApplication().getSecuritySettings().getAuthenticationStrategy().remove();
```

### PointlessBooleanExpression
`removed == false` can be simplified to '!removed'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/StoredResponsesMap.java`
#### Snippet
```java
	{
		boolean removed = super.removeEldestEntry(eldest);
		if (removed == false)
		{
			Value value = (Value)eldest.getValue();
```

### PointlessBooleanExpression
`copied[0] == false` can be simplified to '!copied\[0\]'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
			throw new WicketRuntimeException(e1);
		}
		if (copied[0] == false)
		{
			response.write(stream.toByteArray());
```

### PointlessBooleanExpression
`Strings.isEmpty(filterPrefix) == false` can be simplified to '!Strings.isEmpty(filterPrefix)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/DispatchedRequestUtils.java`
#### Snippet
```java
		}

		if (Strings.isEmpty(filterPrefix) == false && filterPrefix.startsWith("/") == false)
		{
			filterPrefix = '/' + filterPrefix;
```

### PointlessBooleanExpression
`filterPrefix.startsWith("/") == false` can be simplified to '!filterPrefix.startsWith("/")'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/DispatchedRequestUtils.java`
#### Snippet
```java
		}

		if (Strings.isEmpty(filterPrefix) == false && filterPrefix.startsWith("/") == false)
		{
			filterPrefix = '/' + filterPrefix;
```

### PointlessBooleanExpression
`"/".equals(filterPrefix) == false` can be simplified to '!"/".equals(filterPrefix)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/DispatchedRequestUtils.java`
#### Snippet
```java

		String uri = (String)request.getAttribute(attributeName);
		if (uri != null && uri.startsWith(filterPrefix) && "/".equals(filterPrefix) == false) {
			uri = uri.substring(filterPrefix.length());
		}
```

### PointlessBooleanExpression
`Strings.isEmpty(httpServletRequest.getQueryString()) == false` can be simplified to '!Strings.isEmpty(httpServletRequest.getQueryString())'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
			else
			{
				if (Strings.isEmpty(httpServletRequest.getQueryString()) == false)
				{
					redirectURL += "?" + httpServletRequest.getQueryString();
```

### PointlessBooleanExpression
`Strings.isEmpty(paths) == false` can be simplified to '!Strings.isEmpty(paths)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
	{
		String paths = filterConfig.getInitParameter(IGNORE_PATHS_PARAM);
		if (Strings.isEmpty(paths) == false)
		{
			String[] parts = Strings.split(paths, ',');
```

### PointlessBooleanExpression
`Strings.isEmpty(relativePath) == false` can be simplified to '!Strings.isEmpty(relativePath)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
		{
			String relativePath = getRelativePath(request);
			if (Strings.isEmpty(relativePath) == false)
			{
				for (String path : ignorePaths)
```

### PointlessBooleanExpression
`insert == false` can be simplified to '!insert'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
			final RequestCycle cycle = RequestCycle.get();

			if (insert == false)
			{
				WebRequest request = (WebRequest)cycle.getRequest();
```

### PointlessBooleanExpression
`Strings.isEmpty(encoding) == false` can be simplified to '!Strings.isEmpty(encoding)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
				response.write("<?xml version='1.0'");
				String encoding = getRequestCycleSettings().getResponseRequestEncoding();
				if (Strings.isEmpty(encoding) == false)
				{
					response.write(" encoding='");
```

### PointlessBooleanExpression
`application instanceof WebApplication == false` can be simplified to '!(application instanceof WebApplication)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
		Application application = Application.get();

		if (application instanceof WebApplication == false)
		{
			throw new WicketRuntimeException(
```

### PointlessBooleanExpression
`matchesOne(request.getRemoteAddr(), config.securedRemoteAddresses) == false` can be simplified to '!matchesOne(request.getRemoteAddr(), config.securedRemoteAddresses)'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/SecuredRemoteAddressRequestWrapperFactory.java`
#### Snippet
```java
	{
		return !request.isSecure() &&
			matchesOne(request.getRemoteAddr(), config.securedRemoteAddresses) == false;
	}

```

### PointlessBooleanExpression
`httpServletRequest.isAsyncStarted() == false` can be simplified to '!httpServletRequest.isAsyncStarted()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebResponse.java`
#### Snippet
```java
		{
			HttpServletRequest httpServletRequest = webRequest.getContainerRequest();
			if (httpServletRequest.isAsyncStarted() == false)
			{
				httpServletResponse.flushBuffer();
```

### PointlessBooleanExpression
`Strings.isEmpty(encodedAttributes) == false` can be simplified to '!Strings.isEmpty(encodedAttributes)'
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
		String variation = null;

		if (Strings.isEmpty(encodedAttributes) == false)
		{
			String split[] = Strings.split(encodedAttributes, '-');
```

### PointlessBooleanExpression
`rtn == false` can be simplified to '!rtn'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapperFactory.java`
#### Snippet
```java
	{
		boolean rtn = matchesOne(request.getRemoteAddr(), config.allowedInternalProxies);
		if (rtn == false)
		{
			if (log.isDebugEnabled())
```

### PointlessBooleanExpression
`afterReloadListeners.contains(listener) == false` can be simplified to '!afterReloadListeners.contains(listener)'
in `wicket-core/src/main/java/org/apache/wicket/resource/PropertiesFactory.java`
#### Snippet
```java
	{
		// Make sure listeners are added only once
		if (afterReloadListeners.contains(listener) == false)
		{
			afterReloadListeners.add(listener);
```

### PointlessBooleanExpression
`Strings.isEmpty(prefix) == false` can be simplified to '!Strings.isEmpty(prefix)'
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
			// first, try the fully qualified resource name relative to the
			// component on the path from page down.
			if (Strings.isEmpty(prefix) == false)
			{
				// lookup fully qualified path
```

### PointlessBooleanExpression
`(current instanceof AbstractRepeater) == false` can be simplified to '!(current instanceof AbstractRepeater)'
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java

				// shorten resource key prefix when going downwards (skip for repeaters)
				if ((current instanceof AbstractRepeater) == false)
				{
					prefix = Strings.afterFirst(prefix, '.');
```

### PointlessBooleanExpression
`skip == false` can be simplified to '!skip'
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
			final boolean skip = current.getParent() instanceof AbstractRepeater;

			if (skip == false)
			{
				if (buffer.length() > 0)
```

### PointlessBooleanExpression
`file.exists() == false` can be simplified to '!file.exists()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	{
		if (file != null) {
			if (file.exists() == false)
			{
				throw new IllegalArgumentException(
```

### PointlessBooleanExpression
`file.isFile() == false` can be simplified to '!file.isFile()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
			}

			if (file.isFile() == false)
			{
				throw new IllegalArgumentException(
```

### PointlessBooleanExpression
`path.startsWith("/") == false` can be simplified to '!path.startsWith("/")'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		String path = url.getPath(getCharset());

		if (path.startsWith("/") == false)
		{
			path = getContextPath() + getServletPath() + '/' + path;
```

### PointlessBooleanExpression
`useMultiPartContentType == false` can be simplified to '!useMultiPartContentType'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	private byte[] buildRequest()
	{
		if (uploadedFiles == null && useMultiPartContentType == false)
		{
			if (post.getParameterNames().size() == 0)
```

### PointlessBooleanExpression
`mockHttpSession.isTemporary() == false` can be simplified to '!mockHttpSession.isTemporary()'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
			}

			if (mockHttpSession.isTemporary() == false)
			{
				sess = session;
```

### PointlessBooleanExpression
`page instanceof SerializedPage == false` can be simplified to '!(page instanceof SerializedPage)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/SerializingPageStore.java`
#### Snippet
```java
	public void addPage(IPageContext context, IManageablePage page)
	{
		if (page instanceof SerializedPage == false)
		{
			page = new SerializedPage(page.getPageId(), Classes.name(page.getClass()), serializer.serialize(page));
```

### PointlessBooleanExpression
`isPageStateless(page) == false` can be simplified to '!isPageStateless(page)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/RequestPageStore.java`
#### Snippet
```java
		for (IManageablePage page : requestData.pages())
		{
			if (isPageStateless(page) == false)
			{
				getDelegate().addPage(context, page);
```

### PointlessBooleanExpression
`isPageStateless(page) == false` can be simplified to '!isPageStateless(page)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/RequestPageStore.java`
#### Snippet
```java
		for (IManageablePage page : requestData.pages())
		{
			if (isPageStateless(page) == false)
			{
				// last opportunity to create a session
```

### PointlessBooleanExpression
`page instanceof SerializedPage == false` can be simplified to '!(page instanceof SerializedPage)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/CryptingPageStore.java`
#### Snippet
```java
		if (page != null)
		{
			if (page instanceof SerializedPage == false)
			{
				throw new WicketRuntimeException("CryptingPageStore expects serialized pages");
```

### PointlessBooleanExpression
`page instanceof SerializedPage == false` can be simplified to '!(page instanceof SerializedPage)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/CryptingPageStore.java`
#### Snippet
```java
	public void addPage(IPageContext context, IManageablePage page)
	{
		if (page instanceof SerializedPage == false)
		{
			throw new WicketRuntimeException("CryptingPageStore works with serialized pages only");
```

### PointlessBooleanExpression
`stableGroups == false` can be simplified to '!stableGroups'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java
		public synchronized void addPage(IPageContext context, IManageablePage page, String group, int maxGroups, boolean stableGroups, IPageStore delegate)
		{
			if (stableGroups == false)
			{
				// group might have changed, so remove page first from all groups
```

### PointlessBooleanExpression
`page instanceof SerializedPage == false` can be simplified to '!(page instanceof SerializedPage)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
	protected void addPersistedPage(String sessionIdentifier, IManageablePage page)
	{
		if (page instanceof SerializedPage == false)
		{
			throw new WicketRuntimeException("DiskPageStore works with serialized pages only");
```

### PointlessBooleanExpression
`page instanceof SerializedPage == false` can be simplified to '!(page instanceof SerializedPage)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
		public synchronized void add(IManageablePage page)
		{
			if (page instanceof SerializedPage == false)
			{
				throw new WicketRuntimeException(
```

### PointlessBooleanExpression
`page instanceof SerializedPage == false` can be simplified to '!(page instanceof SerializedPage)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
		public synchronized void add(IManageablePage page)
		{
			if (page instanceof SerializedPage == false)
			{
				throw new WicketRuntimeException(
```

### PointlessBooleanExpression
`(page instanceof SerializedPage) == false` can be simplified to '!(page instanceof SerializedPage)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
				IManageablePage page = pages.get(p);

				if ((page instanceof SerializedPage) == false)
				{
					// remove if not already serialized
```

### PointlessBooleanExpression
`folder.exists() == false` can be simplified to '!folder.exists()'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/NestedFolders.java`
#### Snippet
```java

		File folder = new File(base, path);
		if (create && folder.exists() == false)
		{
			Files.mkdirs(folder);
```

### PointlessBooleanExpression
`page instanceof SerializedPage == false` can be simplified to '!(page instanceof SerializedPage)'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/FilePageStore.java`
#### Snippet
```java
	protected void addPersistedPage(String sessionIdentifier, IManageablePage page)
	{
		if (page instanceof SerializedPage == false)
		{
			throw new WicketRuntimeException("FilePageStore works with serialized pages only");
```

### PointlessBooleanExpression
`file.exists() == false` can be simplified to '!file.exists()'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/FilePageStore.java`
#### Snippet
```java
	{
		File file = getPageFile(sessionIdentifier, id, false);
		if (file.exists() == false)
		{
			return null;
```

### PointlessBooleanExpression
`((Behavior)validator).getStatelessHint(component) == false` can be simplified to '!((Behavior)validator).getStatelessHint(component)'
in `wicket-core/src/main/java/org/apache/wicket/validation/CompoundValidator.java`
#### Snippet
```java
	{
		for (IValidator<T> validator : validators) {
			if (validator instanceof Behavior && ((Behavior)validator).getStatelessHint(component) == false) {
				return false;
			}
```

### PointlessBooleanExpression
`resolvers.isEmpty() == false` can be simplified to '!resolvers.isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/application/CompoundClassResolver.java`
#### Snippet
```java
	{
		final ClassLoader classLoader;
		if (resolvers.isEmpty() == false)
		{
			classLoader = resolvers.iterator().next().getClassLoader();
```

### PointlessBooleanExpression
`failOnUnknown == false` can be simplified to '!failOnUnknown'
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java
			isChecksumCorrect(creditCardNumber))
		{
			if ((failOnUnknown == false) ||
				(determineCardId(creditCardNumber) != CreditCard.INVALID))
			{
```

### PointlessBooleanExpression
`Strings.isEmpty(value) == false` can be simplified to '!Strings.isEmpty(value)'
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/DefaultAuthenticationStrategy.java`
#### Snippet
```java
	 */
	protected String[] decode(String value) {
		if (Strings.isEmpty(value) == false)
		{
			String username = null;
```

### PointlessBooleanExpression
`Strings.isEmpty(values[0]) == false` can be simplified to '!Strings.isEmpty(values\[0\])'
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/DefaultAuthenticationStrategy.java`
#### Snippet
```java

			String[] values = value.split(VALUE_SEPARATOR);
			if ((values.length > 0) && (Strings.isEmpty(values[0]) == false))
			{
				username = values[0];
```

### PointlessBooleanExpression
`Strings.isEmpty(values[1]) == false` can be simplified to '!Strings.isEmpty(values\[1\])'
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/DefaultAuthenticationStrategy.java`
#### Snippet
```java
				username = values[0];
			}
			if ((values.length > 1) && (Strings.isEmpty(values[1]) == false))
			{
				password = values[1];
```

### PointlessBooleanExpression
`Strings.isEmpty(value) == false` can be simplified to '!Strings.isEmpty(value)'
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/DefaultAuthenticationStrategy.java`
#### Snippet
```java
	{
		String value = getCookieUtils().load(cookieKey);
		if (Strings.isEmpty(value) == false)
		{
			try
```

### PointlessBooleanExpression
`hit == false` can be simplified to '!hit'
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/Book.java`
#### Snippet
```java
		}

		if (hit == false)
		{
			idToBook.put(book.id, book);
```

### PointlessBooleanExpression
`httpServletRequest.isRequestedSessionIdValid() == false` can be simplified to '!httpServletRequest.isRequestedSessionIdValid()'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
			final Cookie jsessionid = cookieUtils.getCookie(jsessionCookieName);
			HttpServletRequest httpServletRequest = (HttpServletRequest) containerRequest;
			if (jsessionid == null || httpServletRequest.isRequestedSessionIdValid() == false)
			{
				sessionId = component.getSession().getId();
```

### PointlessBooleanExpression
`isOpen() == false` can be simplified to '!isOpen()'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/util/tester/TestWebSocketConnection.java`
#### Snippet
```java
	private void checkOpenness()
	{
		if (isOpen() == false)
		{
			throw new IllegalStateException("The connection is closed!");
```

### PointlessBooleanExpression
`autoConversation.isAutomatic() == false` can be simplified to '!autoConversation.isAutomatic()'
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/AutoConversationManager.java`
#### Snippet
```java
		if (conversation == null || conversation.isTransient() || page == null
				|| hasConversationalComponent(page) || !propagation.propagatesVia(handler, page)
				|| autoConversation.isAutomatic() == false)
		{
			return;
```

### PointlessBooleanExpression
`Strings.isEmpty(pageId) == false` can be simplified to '!Strings.isEmpty(pageId)'
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/AbstractWebSocketProcessor.java`
#### Snippet
```java
			throw new IllegalArgumentException("The request should have either 'pageId' or 'resourceName' parameter!");
		}
		if (Strings.isEmpty(pageId) == false)
		{
			this.pageId = Integer.parseInt(pageId, 10);
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/CombinedRequestParametersAdapter.java`
#### Snippet
```java
				for (StringValue v : values)
				{
					result.add(v);
				}
			}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/Roles.java`
#### Snippet
```java
		for (final String role : roles.split("\\s*,\\s*"))
		{
			add(role);
		}
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/Roles.java`
#### Snippet
```java
		for (final String role : roles)
		{
			add(role);
		}
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `wicket-core/src/main/java/org/apache/wicket/request/resource/MetaInfStaticResourceReference.java`
#### Snippet
```java
		for (String p : parts)
		{
			segments.add(p);
		}

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `wicket-core/src/main/java/org/apache/wicket/request/resource/MetaInfStaticResourceReference.java`
#### Snippet
```java
		for (String p : parts)
		{
			segments.add(p);
		}

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ResourceBundleReference.java`
#### Snippet
```java
		{
			for (HeaderItem curDependency : curProvided.getDependencies())
				ret.add(curDependency);
		}
		for (HeaderItem curProvided : providedResources)
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatResourceBundleReference.java`
#### Snippet
```java
		{
			for (HeaderItem curDependency : curProvided.getDependencies())
				ret.add(curDependency);
		}
		for (HeaderItem curProvided : providedResources)
```

## RuleId[id=EnumSwitchStatementWhichMissesCases]
### EnumSwitchStatementWhichMissesCases
`switch (application.getExceptionSettings().getAjaxErrorHandlingStrategy()) { case INVOKE_FAIL...` statement on enum type 'org.apache.wicket.settings.ExceptionSettings.AjaxErrorStrategy' misses case 'REDIRECT_TO_ERROR_PAGE'
in `wicket-core/src/main/java/org/apache/wicket/DefaultExceptionMapper.java`
#### Snippet
```java
		if (isProcessingAjaxRequest())
		{
			switch (application.getExceptionSettings().getAjaxErrorHandlingStrategy())
			{
				case INVOKE_FAILURE_HANDLER :
					return new ErrorCodeRequestHandler(500);
			}
		}

```

### EnumSwitchStatementWhichMissesCases
`switch (lastType) { case TAG : return lastTag; case BODY : break; case CO...` statement on enum type 'org.apache.wicket.markup.parser.IXmlPullParser.HttpTagType' misses cases: 'NOT_INITIALIZED', ...
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java
		while (next() != HttpTagType.NOT_INITIALIZED)
		{
			switch (lastType)
			{
				case TAG :
					return lastTag;

				case BODY :
					break;

				case COMMENT :
					break;

				case CONDITIONAL_COMMENT :
					break;

				case CDATA :
					break;

				case PROCESSING_INSTRUCTION :
					break;

				case SPECIAL_TAG :
					break;
			}
		}

```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java

		// ConcurrentHashMap does not allow null values
		if (NULL_VALUE == value)
		{
			return null;
```

## RuleId[id=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/PackageStringResourceLoader.java`
#### Snippet
```java
		IPropertiesFactory propertiesFactory = getPropertiesFactory();

		while (true)
		{
			Package pkg = clazz.getPackage();
```

## RuleId[id=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
`data` is not of an array type
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			if (position > 0)
			{
				System.arraycopy(current, 0, data, 0, position);
			}
			if (position != currentLength - 1)
```

### SuspiciousSystemArraycopy
`data` is not of an array type
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			{
				final int left = currentLength - position - 1;
				System.arraycopy(current, position + 1, data, position, left);
			}
		}
```

## RuleId[id=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `get()` declared in class 'org.apache.wicket.Application' but referenced via subclass 'org.apache.wicket.protocol.http.WebApplication'
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/WicketEndpoint.java`
#### Snippet
```java
		applicationName = getApplicationName(session);

		WebApplication app = (WebApplication)WebApplication.get(applicationName);
		if (RUNNING_APPLICATIONS.add(applicationName))
		{
```

### StaticCallOnSubclass
Static method `exists()` declared in class 'org.apache.wicket.Application' but referenced via subclass 'org.apache.wicket.protocol.http.WebApplication'
in `wicket-core/src/main/java/org/apache/wicket/settings/StoreSettings.java`
#### Snippet
```java
		if (fileStoreFolder == null)
		{
			if (WebApplication.exists())
			{
				fileStoreFolder = (File) WebApplication.get().getServletContext()
```

## RuleId[id=RedundantClassCall]
### RedundantClassCall
Redundant call to `isInstance()`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	private boolean isQueueRegion() 
	{
		return IQueueRegion.class.isInstance(this);
	}

```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `wicket-util/src/main/java/org/apache/wicket/util/io/XmlReader.java`
#### Snippet
```java

	/** Regex to find <?xml encoding ... ?> */
	private static final Pattern encodingPattern = Pattern.compile("\\s+encoding\\s*=\\s*([\"\'](.*?)[\"\']|(\\S*)).*\\?>");

	/** Null, if JVM default. Else from <?xml encoding=""> */
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `wicket-util/src/main/java/org/apache/wicket/util/io/XmlReader.java`
#### Snippet
```java

	/** Regex to find <?xml encoding ... ?> */
	private static final Pattern encodingPattern = Pattern.compile("\\s+encoding\\s*=\\s*([\"\'](.*?)[\"\']|(\\S*)).*\\?>");

	/** Null, if JVM default. Else from <?xml encoding=""> */
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/MetaPattern.java`
#### Snippet
```java
	// Regexps that are used multiple times in defining meta patterns
	private static final String _DOUBLE_QUOTED_STRING = "\"[^\"]*?\"";
	private static final String _SINGLE_QUOTED_STRING = "'[^']*?\'";
	private static final String _STRING = "(?:[\\w\\-\\.]+|" + _DOUBLE_QUOTED_STRING + "|" +
		_SINGLE_QUOTED_STRING + ")";
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnEventHeaderItem.java`
#### Snippet
```java
		result.append("Wicket.Event.add(")
				.append(getTarget())
				.append(", \'")
				.append(getEvent())
				.append("\', function(event) { ")
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnEventHeaderItem.java`
#### Snippet
```java
				.append(", \'")
				.append(getEvent())
				.append("\', function(event) { ")
				.append(getJavaScript())
				.append(";});");
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java

				// Chop off double quotes or single quotes
				if (value.startsWith("\"") || value.startsWith("\'"))
				{
					value = value.substring(1, value.length() - 1);
```

## RuleId[id=ObsoleteCollection]
### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetMap.java`
#### Snippet
```java

		// Set the cache mapper to have the highest priority.
		mappers.add(MAP_CACHE, new Hashtable<String, String>());
	}

```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java

				// temporary Tag-Hierarchy after openTag
				Stack<XmlTag> stack = new Stack<>();

				while (true)
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java

				// temporary Tag-Hierarchy after openTag
				Stack<XmlTag> stack = new Stack<>();

				while (true)
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
	 * @return corresponding openTag or {@code null}
	 */
	private static XmlTag findOpenTag(XmlTag closeTag, Stack<XmlTag> stack)
	{
		while (stack.size() > 0)
```

## RuleId[id=LongLiteralsEndingWithLowercaseL]
### LongLiteralsEndingWithLowercaseL
'long' literal `0l` ends with lowercase 'l'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	 * calculate the average request time across the window duration.
	 */
	private long totalRequestTime = 0l;

	/**
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			try
			{
				return clz.getMethod(name, new Class[] { int.class, getMethod.getReturnType() });
			}
			catch (Exception e)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
		try
		{
			LOOKUP_METHOD = ObjectStreamClass.class.getDeclaredMethod("lookup", new Class[] {
					Class.class, Boolean.TYPE });
			LOOKUP_METHOD.setAccessible(true);

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java

			GET_OBJ_FIELD_VALUES_METHOD = ObjectStreamClass.class.getDeclaredMethod(
					"getObjFieldValues", new Class[] { Object.class, Object[].class });
			GET_OBJ_FIELD_VALUES_METHOD.setAccessible(true);

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java

			INVOKE_WRITE_REPLACE_METHOD = ObjectStreamClass.class.getDeclaredMethod(
					"invokeWriteReplace", new Class[] { Object.class });
			INVOKE_WRITE_REPLACE_METHOD.setAccessible(true);
		}
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/session/DefaultPageFactory.java`
#### Snippet
```java
			try
			{
				if (pageClass.getDeclaredConstructor(new Class[] { }) != null)
				{
					bookmarkable = Boolean.TRUE;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/session/DefaultPageFactory.java`
#### Snippet
```java
				try
				{
					if (pageClass.getDeclaredConstructor(new Class[] { PageParameters.class }) != null)
					{
						bookmarkable = Boolean.TRUE;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/session/DefaultPageFactory.java`
#### Snippet
```java
			{
				// Try to find the constructor
				constructor = pageClass.getDeclaredConstructor(new Class[] { argumentType });

				// Store it in the cache
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ResourceIsolationRequestCycleListener.java`
#### Snippet
```java
				{
					log.debug("Allowing request to {} because it matches an exempted path",
						new Object[] { pathInfo });
				}
				return;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
		{
			LOG.debug("Calculating context relative path from: context path '{}', filterPrefix '{}', uri '{}'",
					new Object[] {contextPath, filterPrefix, uri});
		}

```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java

				case COMMENT :
					break;

				case CONDITIONAL_COMMENT :
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java

				case CONDITIONAL_COMMENT :
					break;

				case CDATA :
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java

				case CDATA :
					break;

				case PROCESSING_INSTRUCTION :
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java

				case PROCESSING_INSTRUCTION :
					break;

				case SPECIAL_TAG :
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlPullParser.java`
#### Snippet
```java

				case SPECIAL_TAG :
					break;
			}
		}
```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`[\\*]` can be simplified to '\\\*'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
			regex = regex.replaceAll("^\\*" + PATH_SEPARATOR, "[^" + PATH_SEPARATOR + "]+" +
				PATH_SEPARATOR);
			regex = regex.replaceAll("^[\\*]{2,}" + PATH_SEPARATOR, "([^" + PATH_SEPARATOR +
				"].#star#" + PATH_SEPARATOR + ")?");

```

### RegExpSimplifiable
`[\\*]` can be simplified to '\\\*'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
			regex = regex.replaceAll(PATH_SEPARATOR + "\\*" + PATH_SEPARATOR, PATH_SEPARATOR +
				"[^" + PATH_SEPARATOR + "]+" + PATH_SEPARATOR);
			regex = regex.replaceAll(PATH_SEPARATOR + "[\\*]{2,}" + PATH_SEPARATOR, "(" +
				PATH_SEPARATOR + "|" + PATH_SEPARATOR + ".+" + PATH_SEPARATOR + ")");

```

### RegExpSimplifiable
`[ ]` can be simplified to ' '
in `wicket-core/src/main/java/org/apache/wicket/resource/CssUrlReplacer.java`
#### Snippet
```java
	// The pattern to find URLs in CSS resources
	private static final Pattern URL_PATTERN = Pattern
		.compile("url\\([ ]*['|\"]?([^ ]*?)['|\"]?[ ]*\\)");

	/**
```

### RegExpSimplifiable
`[ ]` can be simplified to ' '
in `wicket-core/src/main/java/org/apache/wicket/resource/CssUrlReplacer.java`
#### Snippet
```java
	// The pattern to find URLs in CSS resources
	private static final Pattern URL_PATTERN = Pattern
		.compile("url\\([ ]*['|\"]?([^ ]*?)['|\"]?[ ]*\\)");

	/**
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder dump` can be replaced with 'String'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Threads.java`
#### Snippet
```java
		RuntimeMXBean runtimeMXBean = ManagementFactory.getRuntimeMXBean();

		StringBuilder dump = new StringBuilder();

		dump.append("Full thread dump ")
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
			}
			
			StringBuilder msg = new StringBuilder();
			msg.append("More than one bean of type [");
			msg.append(clazz.getName());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
	protected String buildJSCall(final String funcName)
	{
		return new StringBuilder(funcName).append("('").append(getChoicesComponent().getMarkupId())
			.append("','").append(getSelectionComponent().getMarkupId()).append("','")
			.append(getRecorderComponent().getMarkupId()).append("');").toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SortParam.java`
#### Snippet
```java
	public String toString()
	{
		return new StringBuilder().append("[SortParam property=")
			.append(getProperty())
			.append(" ascending=")
```

### StringBufferReplaceableByString
`StringBuilder b` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplateDecorator.java`
#### Snippet
```java
	public String asString(Map<String, ?> variables)
	{
		StringBuilder b = new StringBuilder();
		b.append(getBeforeTemplateContents());
		b.append(decorated.asString(variables));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/model/ChainingModel.java`
#### Snippet
```java
	public String toString()
	{
		StringBuilder sb = new StringBuilder("Model:classname=[");
		sb.append(getClass().getName()).append(']');
		sb.append(":nestedModel=[").append(target).append(']');
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/model/LoadableDetachableModel.java`
#### Snippet
```java
	public String toString()
	{
		StringBuilder sb = new StringBuilder(super.toString());
		sb.append(":attached=")
			.append(isAttached())
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/model/Model.java`
#### Snippet
```java
	public String toString()
	{
		StringBuilder sb = new StringBuilder("Model:classname=[");
		sb.append(getClass().getName()).append(']');
		sb.append(":object=[").append(object).append(']');
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/model/util/GenericBaseModel.java`
#### Snippet
```java
	public String toString()
	{
	 StringBuilder sb = new StringBuilder("Model:classname=[");
		sb.append(getClass().getName()).append(']');
		sb.append(":object=[").append(object).append(']');
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/PanelMarkupSourcingStrategy.java`
#### Snippet
```java
				if (markupStream.get().closes(openTag) == false)
				{
					StringBuilder msg = new StringBuilder();

					msg.append("Close tag not found for tag: ")
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/behavior/AttributeAppender.java`
#### Snippet
```java
			return currentValue;
		}
		StringBuilder sb = new StringBuilder(currentValue);
		sb.append((getSeparator() == null ? "" : getSeparator()));
		sb.append(appendValue);
```

### StringBufferReplaceableByString
`StringBuilder result` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	public String toString()
	{
		final StringBuilder result = new StringBuilder();
		result.append('[')
			.append(Classes.simpleName(getClass()))
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
		public String toString()
		{
			final StringBuilder sb = new StringBuilder();
			sb.append("CacheKey");
			sb.append("{scopeName='").append(scopeName).append('\'');
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/settings/DefaultUnauthorizedResourceRequestListener.java`
#### Snippet
```java
	protected String createErrorMessage(IResource resource, PageParameters parameters)
	{
		return new StringBuilder()
			.append("The request to resource '")
			.append(resource)
```

### StringBufferReplaceableByString
`StringBuilder bs` can be replaced with 'String'
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/NestedFolders.java`
#### Snippet
```java
		String low = String.valueOf(hash % 9973);
		String high = String.valueOf((hash / 9973) % 9973);
		StringBuilder bs = new StringBuilder(name.length() + 10);
		bs.append(low);
		bs.append(File.separator);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/ContactGenerator.java`
#### Snippet
```java
	private String generatePhoneNumber()
	{
		return new StringBuilder().append(rint(2, 9))
			.append(rint(0, 9))
			.append(rint(0, 9))
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactGenerator.java`
#### Snippet
```java
	private String generatePhoneNumber()
	{
		return new StringBuilder().append(rint(2, 9))
			.append(rint(0, 9))
			.append(rint(0, 9))
```

### StringBufferReplaceableByString
`StringBuilder b` can be replaced with 'String'
in `wicket-examples/src/main/java/org/apache/wicket/examples/hellobrowser/HelloBrowser.java`
#### Snippet
```java
					dateFormat.setTimeZone(timeZone);
					String calAsString = dateFormat.format(cal.getTime());
					StringBuilder b = new StringBuilder("Based on your settings, your time is: ");
					b.append(calAsString);
					b.append(" (and your time zone is ");
```

### StringBufferReplaceableByString
`StringBuilder b` can be replaced with 'String'
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajaxhellobrowser/AjaxHelloBrowser.java`
#### Snippet
```java
				dateFormat.setTimeZone(timeZone);
				String calAsString = dateFormat.format(cal.getTime());
				StringBuilder b = new StringBuilder("Based on your settings, your time is: ");
				b.append(calAsString);
				b.append(" (and your time zone is ");
```

### StringBufferReplaceableByString
`StringBuilder b` can be replaced with 'String'
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajaxhellowbrowserondomready/AjaxHelloBrowserOnDomReady.java`
#### Snippet
```java
				dateFormat.setTimeZone(timeZone);
				String calAsString = dateFormat.format(cal.getTime());
				StringBuilder b = new StringBuilder("Based on your settings, your time is: ");
				b.append(calAsString);
				b.append(" (and your time zone is ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/markup/html/VelocityPanel.java`
#### Snippet
```java

		// evaluate the template and return a new StringResourceStream
		StringBuilder sb = new StringBuilder();
		sb.append("<wicket:panel>");
		sb.append(evaluateVelocityTemplate(reader));
```

## RuleId[id=NonShortCircuitBoolean]
### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= remove(c)`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/ProviderSubset.java`
#### Snippet
```java
		for (Object c : cs)
		{
			changed |= remove(c);
		}

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= add(t)`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/ProviderSubset.java`
#### Snippet
```java
		for (T t : ts)
		{
			changed |= add(t);
		}

```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
		{
			writer.write(str);
			return;
		}
		else
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java
				{
					fail(new StatelessCheckFailureException(p, " for unknown reason"));
					return;
				}
			}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
						{
							visit.stop(child);
							return;
						}
					}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
						+ "if (win == window) { window.location.href='" + url
						+ "'; } ;return false"));
				return;
			}
		}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
						+ "if (win == window) { window.location.href='" + url
						+ "'; } ;return false"));
				return;
			}
		}
```

## RuleId[id=TextBlockMigration]
### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
	protected void outputDevelopmentModeWarning()
	{
		System.err.print("********************************************************************\n"
			+ "*** WARNING: Wicket is running in DEVELOPMENT mode.              ***\n"
			+ "***                               ^^^^^^^^^^^                    ***\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
				@Override
				protected String getContent(Attributes attributes) {
					String licence = "/*\n" +
							" * Licensed to the Apache Software Foundation (ASF) under one or more\n" +
							" * contributor license agreements.  See the NOTICE file distributed with\n" +
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ButtonPage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = " <form wicket:id=\"form\">\n"
			+ "  <input type=\"submit\" value=\"non wicket submit button\" />\n"
			+ "  <input wicket:id=\"button1\" type=\"submit\" value=\"default wicket button\" />\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ButtonPage.java`
#### Snippet
```java
			+ "  <input wicket:id=\"button2\" type=\"submit\" value=\"wicket button with setDefaultFormProcessing(false)\" />\n"
			+ " </form>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;// Add a form with an onSubmit implementation that sets a message\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;Form form = new Form(\"form\") {\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;protected void onSubmit() {\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/FragmentPage.java`
#### Snippet
```java
	{
		String html = "<wicket:fragment wicket:id=\"fragmentid\">...</wicket:fragment>";
		String code = "private class MyFragment extends Fragment {\n ...\n"
			+ "add(new MyFragment(\"fragment\", \"fragmentid\"));";
		add(new ExplainPanel(html, code));
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/RadioGroupPage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<form wicket:id=\"form\">\n" + "<span wicket:id=\"group\">\n"
			+ "<tr wicket:id=\"persons\">\n"
			+ "<td><input type=\"radio\" wicket:id=\"radio\"/></td>\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/IncludePage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<span wicket:id=\"include\">\n"
			+ "Inclusion contents to be inserted here.\n" + "</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;public IncludePage()\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/IncludePage.java`
#### Snippet
```java
		String html = "<span wicket:id=\"include\">\n"
			+ "Inclusion contents to be inserted here.\n" + "</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;public IncludePage()\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;{\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;add(new Include(\"include\", \"to_be_included.html\"));\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/LinkPage.java`
#### Snippet
```java
	{
		String html = "<a href=\"#\" wicket:id=\"link1\">this link is clicked <span wicket:id=\"label1\">n</span> times</a>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;final Count count1 = new Count(); // simple counter object\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Link link1 = new Link(\"link1\") {\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;public void onClick() {\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/CheckGroupPage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<form wicket:id=\"form\">\n" + "<span wicket:id=\"group\">\n"
			+ "<input type=\"checkbox\" wicket:id=\"groupselector\">check/uncheck all</input>\n"
			+ "<tr wicket:id=\"persons\">\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/LabelPage.java`
#### Snippet
```java
	{
		String html = "<span wicket:id=\"markupLabel\" class=\"mark\">this text will be replaced</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Label markupLabel = new Label(\"markupLabel\", \"now &lt;i&gt;that&lt;/i&gt; is a pretty &lt;b&gt;bold&lt;/b&gt; statement!\");\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;markupLabel.setEscapeModelStrings(false);\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;add(markupLabel);";
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ListChoicePage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<select wicket:id=\"site\">\n" + "    <option>site 1</option>\n"
			+ "    <option>site 2</option>\n" + "</select>";
		String code = "private static final List SITES = Arrays.asList(new String[] { \"The Server Side\", \"Java Lobby\", \"Java.Net\" });\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ListChoicePage.java`
#### Snippet
```java
		String html = "<select wicket:id=\"site\">\n" + "    <option>site 1</option>\n"
			+ "    <option>site 2</option>\n" + "</select>";
		String code = "private static final List SITES = Arrays.asList(new String[] { \"The Server Side\", \"Java Lobby\", \"Java.Net\" });\n"
			+ "...\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// Add a list choice component that uses the model object's 'site' property to designate the\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/MultiLineLabelPage.java`
#### Snippet
```java
	public MultiLineLabelPage()
	{
		String text = "\nThis is a line.\n" + "And this is another line.\n" + "End of lines.\n";

		add(new MultiLineLabel("multiLineLabel", text));
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/MultiLineLabelPage.java`
#### Snippet
```java
	{
		String html = "<span wicket:id=\"multiLineLabel\" class=\"mark\">this text will be replaced</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;public MultiLineLabelPage() {\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;String text =\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"\\nThis is a line.\\n\" +\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TextFieldPage.java`
#### Snippet
```java
		String html = "<input type=\"text\" wicket:id=\"text\" />\n"
			+ "<input type=\"text\" wicket:id=\"integer\" />";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;// add a simple text field that uses Input's 'text' property. Nothing can go wrong here\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;form.add(new TextField(\"text\"));\n"
			+ "\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SubmitLinkPage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<form wicket:id=\"form\">\n"
			+ "<a wicket:id=\"internal\">Internal SubmitLink</a>\n" + "</form>\n"
			+ "<a wicket:id=\"external\">External SubmitLink</a>\n";
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SubmitLinkPage.java`
#### Snippet
```java
			+ "<a wicket:id=\"internal\">Internal SubmitLink</a>\n" + "</form>\n"
			+ "<a wicket:id=\"external\">External SubmitLink</a>\n";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;public SubmitLinkPage() {\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;// Add a FeedbackPanel for displaying our messages\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;FeedbackPanel feedbackPanel = new FeedbackPanel(\"feedback\");\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/BookmarkablePageLinkPage.java`
#### Snippet
```java
	{
		String html = "<a wicket:id=\"pageLinkWithArgs\">go to our bookmarkable page passing a message argument</a>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;// Note that any arguments are passed as request parameters, and should thus be strings\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;PageParameters parameters = new PageParameters();\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;parameters.put(\"message\", \"This message was passed as a page parameter argument\");\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/CheckBoxMultipleChoicePage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<span wicket:id=\"sites\">\n" + "</span>\n"
			+ "<span wicket:id=\"choices\">\n" + "</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;CheckBoxMultipleChoice siteChoice = new CheckBoxMultipleChoice(\"sites\", SITES);\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/CheckBoxMultipleChoicePage.java`
#### Snippet
```java
		String html = "<span wicket:id=\"sites\">\n" + "</span>\n"
			+ "<span wicket:id=\"choices\">\n" + "</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;CheckBoxMultipleChoice siteChoice = new CheckBoxMultipleChoice(\"sites\", SITES);\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;form.add(siteChoice);\n"
			+ "\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/BorderPage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<span wicket:id=\"border\" class=\"mark\">\n"
			+ "<span wicket:id=\"label\" class=\"mark\">label contents here</span>\n" + "</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;public BorderPage()\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/BorderPage.java`
#### Snippet
```java
		String html = "<span wicket:id=\"border\" class=\"mark\">\n"
			+ "<span wicket:id=\"label\" class=\"mark\">label contents here</span>\n" + "</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;public BorderPage()\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;{\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Label label = new Label(\"label\", \"I am the label\");\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<select wicket:id=\"sites\">\n" + "    <option>site 1</option>\n"
			+ "    <option>site 2</option>\n" + "</select>\n" + "<select wicket:id=\"choices\">\n"
			+ "    <option>choice 1</option>\n" + "    <option>choice 2</option>\n" + "</select>";
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java
			+ "    <option>site 2</option>\n" + "</select>\n" + "<select wicket:id=\"choices\">\n"
			+ "    <option>choice 1</option>\n" + "    <option>choice 2</option>\n" + "</select>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;// Add a multiple list choice component that uses the model object's 'site'\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// property to designate the current selection, and that uses the SITES\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// list for the available options.\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/PanelPage.java`
#### Snippet
```java
	{
		String html = "<span wicket:id=\"panel\">panel contents come here</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;public PanelPage()\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;{\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;add(new MyPanel(\"panel\"));\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ListMultipleChoicePage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<select wicket:id=\"sites\">\n" + "    <option>site 1</option>\n"
			+ "    <option>site 2</option>\n" + "</select>\n" + "<select wicket:id=\"choices\">\n"
			+ "    <option>choice 1</option>\n" + "    <option>choice 2</option>\n" + "</select>";
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ListMultipleChoicePage.java`
#### Snippet
```java
			+ "    <option>site 2</option>\n" + "</select>\n" + "<select wicket:id=\"choices\">\n"
			+ "    <option>choice 1</option>\n" + "    <option>choice 2</option>\n" + "</select>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;// Add a multiple list choice component that uses the model object's 'site'\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// property to designate the current selection, and that uses the SITES\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// list for the available options.\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/FormPage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<form wicket:id=\"form\">\n"
			+ "<input type=\"submit\" value=\"click me to submit the form and display a message\" />\n"
			+ "</form>\n" + "<span wicket:id=\"feedback\">feedbackmessages will be put here</span>";
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/FormPage.java`
#### Snippet
```java
			+ "<input type=\"submit\" value=\"click me to submit the form and display a message\" />\n"
			+ "</form>\n" + "<span wicket:id=\"feedback\">feedbackmessages will be put here</span>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;public FormPage() {\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;// Add a FeedbackPanel for displaying our messages\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;FeedbackPanel feedbackPanel = new FeedbackPanel(\"feedback\");\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/DropDownChoicePage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<select wicket:id=\"site\">\n" + "    <option>site 1</option>\n"
			+ "    <option>site 2</option>\n" + "</select>\n" + "<select wicket:id=\"integer\">\n"
			+ "    <option>Fifty</option>\n" + "    <option>Sixty</option>\n" + "</select>";
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/DropDownChoicePage.java`
#### Snippet
```java
			+ "    <option>site 2</option>\n" + "</select>\n" + "<select wicket:id=\"integer\">\n"
			+ "    <option>Fifty</option>\n" + "    <option>Sixty</option>\n" + "</select>";
		String code = "/** available sites for selection. */\n"
			+ "private static final List SITES = Arrays.asList(new String[] {\"The Server Side\", \"Java Lobby\", \"Java.Net\" });\n"
			+ "/** available numbers for selection. */\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/PalettePage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<form wicket:id=\"form\">\n" + "<span wicket:id=\"palette\">\n"
			+ "</span>\n</form>";
		String code = "&nbsp;&nbsp;&nbsp;&nbsp;Form f=new Form(\"form\");<br/>"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/RadioChoicePage.java`
#### Snippet
```java
	protected void explain()
	{
		String html = "<span valign=\"top\" wicket:id=\"site\">\n"
			+ "  <input type=\"radio\">site 1</input>\n"
			+ "  <input type=\"radio\">site 2</input>\n" + "</span>";
```

### TextBlockMigration
Concatenation can be replaced with text block
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/RadioChoicePage.java`
#### Snippet
```java
			+ "  <input type=\"radio\">site 1</input>\n"
			+ "  <input type=\"radio\">site 2</input>\n" + "</span>";
		String code = "private static final List SITES = Arrays.asList(new String[] { \"The Server Side\", \"Java Lobby\", \"Java.Net\" });\n"
			+ "...\n"
			+ "&nbsp;&nbsp;&nbsp;&nbsp;// Add a radio choice component that uses the model object's 'site' property to designate the\n"
```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Chunk.java`
#### Snippet
```java
	{
		anchor = pos;
		this.count = (count >= 0 ? count : 0);
	}

```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `wicket-util/src/main/java/org/apache/wicket/util/string/interpolator/VariableInterpolator.java`
#### Snippet
```java
		else
		{
			return i1 < i2 ? i1 : i2;
		}
	}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/MultipartStream.java`
#### Snippet
```java
            pos = MultipartStream.this.findSeparator();
            if (pos == -1) {
                if (tail - head > keepRegion) {
                    pad = keepRegion;
                } else {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ClientProperties.java`
#### Snippet
```java
					{
						int dstSaving = Math.abs(dstTimeZone.getRawOffset() - timeZone.getRawOffset());
						String[] availableIDs = TimeZone.getAvailableIDs(dstTimeZone.getRawOffset() < timeZone.getRawOffset() ? dstTimeZone.getRawOffset() : timeZone.getRawOffset());
						for (String availableID : availableIDs)
						{
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/MultipartServletWebRequestImpl.java`
#### Snippet
```java
		{
			int read = in.read(b, off, len);
			bytesUploaded += (read < 0) ? 0 : read;
			onUploadUpdate(bytesUploaded, totalBytes);
			return read;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/MultipartServletWebRequestImpl.java`
#### Snippet
```java
		{
			int read = in.read(b);
			bytesUploaded += (read < 0) ? 0 : read;
			onUploadUpdate(bytesUploaded, totalBytes);
			return read;
```

## RuleId[id=MismatchedStringBuilderQueryUpdate]
### MismatchedStringBuilderQueryUpdate
Contents of `StringBuilder values` are updated, but never queried
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AutoCompletePage.java`
#### Snippet
```java
public class AutoCompletePage extends BasePage
{
	private StringBuilder values = new StringBuilder();

	/**
```

## RuleId[id=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `DefaultCssAutoCompleteTextField` has no concrete subclass
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/DefaultCssAutoCompleteTextField.java`
#### Snippet
```java
 *            The model type
 */
public abstract class DefaultCssAutoCompleteTextField<T> extends AutoCompleteTextField<T>
{
	private static final long serialVersionUID = 1L;
```

### AbstractClassNeverImplemented
Abstract class `AbstractCheckBoxModel` has no concrete subclass
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/model/AbstractCheckBoxModel.java`
#### Snippet
```java
 * 
 */
public abstract class AbstractCheckBoxModel implements IModel<Boolean>
{
	private static final long serialVersionUID = 1L;
```

### AbstractClassNeverImplemented
Abstract class `HeaderlessColumn` has no concrete subclass
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/HeaderlessColumn.java`
#### Snippet
```java
 *            the type of the sort property
 */
public abstract class HeaderlessColumn<T, S> extends AbstractColumn<T, S>
{
	private static final long serialVersionUID = 1L;
```

### AbstractClassNeverImplemented
Abstract class `AbstractExportableColumn` has no concrete subclass
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/AbstractExportableColumn.java`
#### Snippet
```java
 * @param <S> The type of the sort property of the table.
 */
public abstract class AbstractExportableColumn<T, S>
        extends AbstractColumn<T, S>
        implements IExportableColumn<T, S>
```

### AbstractClassNeverImplemented
Abstract class `FilteredAbstractColumn` has no concrete subclass
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilteredAbstractColumn.java`
#### Snippet
```java
 * 
 */
public abstract class FilteredAbstractColumn<T, S> extends AbstractColumn<T, S>
	implements
		IFilteredColumn<T, S>
```

### AbstractClassNeverImplemented
Abstract class `BlobImageResource` has no concrete subclass
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/BlobImageResource.java`
#### Snippet
```java
 * @author Eelco Hillenius
 */
public abstract class BlobImageResource extends DynamicImageResource
{
	/**
```

### AbstractClassNeverImplemented
Abstract class `ArrayIteratorAdapter` has no concrete subclass
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ArrayIteratorAdapter.java`
#### Snippet
```java
 * 
 */
public abstract class ArrayIteratorAdapter<T> implements Iterator<IModel<T>>
{
	private final T[] array;
```

## RuleId[id=BoundedWildcard]
### BoundedWildcard
Can generalize to `? super T`
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Exceptions.java`
#### Snippet
```java
			@Override
			@SuppressWarnings("unchecked")
			public void visit(final Throwable throwable, final Visit<T> visit)
			{
				if (causeType.isAssignableFrom(throwable.getClass()))
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Chunk.java`
#### Snippet
```java
	 * @return a {@link List List} containing the specified items.
	 */
	public static <T> List<T> slice(final List<T> seq, final int pos, final int count)
	{
		if (count <= 0)
```

### BoundedWildcard
Can generalize to `? super R`
in `wicket-util/src/main/java/org/apache/wicket/util/visit/Visits.java`
#### Snippet
```java
	@SuppressWarnings("unchecked")
	private static <S, R> void visitChildren(final Iterable<? super S> container,
		final IVisitor<S, R> visitor, final IVisitFilter filter, final Visit<R> visit)
	{
		Args.notNull(visitor, "visitor");
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-util/src/main/java/org/apache/wicket/util/listener/ListenerCollection.java`
#### Snippet
```java
	 *            notifier used to notify each listener
	 */
	protected void notifyIgnoringExceptions(final INotifier<T> notifier)
	{
		for (T listener : listeners)
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-util/src/main/java/org/apache/wicket/util/listener/ListenerCollection.java`
#### Snippet
```java
	 *            notifier used to notify each listener
	 */
	protected void reversedNotifyIgnoringExceptions(final INotifier<T> notifier)
	{
		reversedNotify(new INotifier<T>()
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-util/src/main/java/org/apache/wicket/util/listener/ListenerCollection.java`
#### Snippet
```java
	 *            notifier used to notify each listener
	 */
	protected void reversedNotify(final INotifier<T> notifier)
	{
		Iterator<T> it = new ReverseListIterator<>(listeners);
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-util/src/main/java/org/apache/wicket/util/listener/ListenerCollection.java`
#### Snippet
```java
	 *            notifier used to notify each listener
	 */
	protected void notify(final INotifier<T> notifier)
	{
		for (T listener : listeners)
```

### BoundedWildcard
Can generalize to `? extends Validator`
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/BeanValidationConfiguration.java`
#### Snippet
```java
	 * @param validatorProvider
	 */
	public void setValidatorProvider(Supplier<Validator> validatorProvider)
	{
		Args.notNull(validatorProvider, "validatorProvider");
```

### BoundedWildcard
Can generalize to `? extends IFieldValueFactory`
in `wicket-ioc/src/main/java/org/apache/wicket/injection/CompoundFieldValueFactory.java`
#### Snippet
```java
	 * @param factories
	 */
	public CompoundFieldValueFactory(final List<IFieldValueFactory> factories)
	{
		Args.notNull(factories, "factories");
```

### BoundedWildcard
Can generalize to `? extends IDebugBarContributor`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/DebugBar.java`
#### Snippet
```java

			@Override
			protected void populateItem(final ListItem<IDebugBarContributor> item)
			{
				IDebugBarContributor contrib = item.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends IPersistentPageStore`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/SessionIdentifiersModel.java`
#### Snippet
```java
	private final IModel<IPersistentPageStore> store;
	
	public SessionIdentifiersModel(IModel<IPersistentPageStore> store)
	{
		this.store = store;
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java
			{
				@Override
				public void component(final Component comp, final IVisit<Component> visit)
				{
					if ((component instanceof Page) && mustCheck(comp))
```

### BoundedWildcard
Can generalize to `? extends IPersistentPageStore`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPagesProvider.java`
#### Snippet
```java
	private List<IPersistedPage> pages;

	PersistedPagesProvider(final IModel<String> sessionId, IModel<IPersistentPageStore> store)
	{
		this.sessionId = sessionId;
```

### BoundedWildcard
Can generalize to `? extends RequestData`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/RequestsPage.java`
#### Snippet
```java

			@Override
			protected void populateItem(final ListItem<RequestData> item)
			{
				RequestData rd = item.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends SessionData`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java

			@Override
			protected void populateItem(final ListItem<SessionData> item)
			{
				final SessionData sd = item.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends MapperWithScore`
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/CompoundRequestMapper.java`
#### Snippet
```java
	 *      the url to match by these mappers
	 */
	private void logMappers(final List<MapperWithScore> mappersWithScores, final String url)
	{
		final List<MapperWithScore> compatibleMappers = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPanel.java`
#### Snippet
```java
		{
			@Override
			public void onClick(Optional<AjaxRequestTarget> targetOptional)
			{
				sessionsSelector.setModelObject(getCurrentSessionIdentifier());
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/pagestore/browser/PersistedPanel.java`
#### Snippet
```java
		{
			@Override
			public void onClick(Optional<AjaxRequestTarget> targetOptional)
			{
				targetOptional.ifPresent(target -> target.add(table));
```

### BoundedWildcard
Can generalize to `? super AjaxLink`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxLink.java`
#### Snippet
```java

	public static <T> IndicatingAjaxLink<T> onClick(String id,
		SerializableBiConsumer<AjaxLink<T>, AjaxRequestTarget> onClick)
	{
		Args.notNull(onClick, "onClick");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxLink.java`
#### Snippet
```java

	public static <T> IndicatingAjaxLink<T> onClick(String id,
		SerializableBiConsumer<AjaxLink<T>, AjaxRequestTarget> onClick)
	{
		Args.notNull(onClick, "onClick");
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java

			@Override
			public void onClick(Optional<AjaxRequestTarget> targetOptional)
			{
				expandState.collapseAll();
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java

			@Override
			protected void onAfterSubmit(Optional<AjaxRequestTarget> target)
			{
				target.ifPresent(t -> t.add(componentTree));
```

### BoundedWildcard
Can generalize to `? extends TreeNode`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
			@Override
			public void populateItem(Item<ICellPopulator<TreeNode>> item, String componentId,
				IModel<TreeNode> rowModel)
			{
				item.add(new Label(componentId, Bytes.bytes(rowModel.getObject().getSize())
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java

			@Override
			public void onClick(Optional<AjaxRequestTarget> targetOptional)
			{
				expandState.expandAll();
```

### BoundedWildcard
Can generalize to `? super AjaxButton`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxButton.java`
#### Snippet
```java

	public static IndicatingAjaxButton onSubmit(String id,
		SerializableBiConsumer<AjaxButton, AjaxRequestTarget> onSubmit)
	{
		Args.notNull(onSubmit, "onSubmit");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxButton.java`
#### Snippet
```java

	public static IndicatingAjaxButton onSubmit(String id,
		SerializableBiConsumer<AjaxButton, AjaxRequestTarget> onSubmit)
	{
		Args.notNull(onSubmit, "onSubmit");
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
	 * @return This
	 */
	public final AjaxEditableLabel<T> add(final IValidator<T> validator)
	{
		getEditor().add(validator);
```

### BoundedWildcard
Can generalize to `? super SelectOption`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/Select.java`
#### Snippet
```java
					{
						@Override
						public void component(SelectOption<T> option, IVisit<SelectOption<T>> visit)
						{
							if (String.valueOf(option.getValue()).equals(value))
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/LambdaColumn.java`
#### Snippet
```java
	 */
	@Override
	public IModel<?> getDataModel(IModel<T> rowModel)
	{
		IModel<Object> dataModel = new IModel<Object>()
```

### BoundedWildcard
Can generalize to `? extends IColumn`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/HeadersToolbar.java`
#### Snippet
```java

			@Override
			protected void populateItem(Item<IColumn<T, S>> item)
			{
				final IColumn<T, S> column = item.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends IExportableColumn`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/CSVDataExporter.java`
#### Snippet
```java

	@SuppressWarnings({ "rawtypes", "unchecked" })
	private <T> void writeData(IDataProvider<T> dataProvider, List<IExportableColumn<T, ?>> columns, Grid grid) throws IOException
	{
		long numberOfRows = dataProvider.size();
```

### BoundedWildcard
Can generalize to `? extends IExportableColumn`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/CSVDataExporter.java`
#### Snippet
```java
	}
	
	private <T> void writeHeaders(List<IExportableColumn<T, ?>> columns, Grid grid) throws IOException
	{
		if (isExportHeadersEnabled())
```

### BoundedWildcard
Can generalize to `? super Boolean`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
			{
				@Override
				public void component(Component object, IVisit<Boolean> visit)
				{
					object.configure();
```

### BoundedWildcard
Can generalize to `? extends IColumn`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterToolbar.java`
#### Snippet
```java

			@Override
			protected void populateItem(ListItem<IColumn<T, S>> item)
			{
				final IColumn<T, S> col = item.getModelObject();
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/Folder.java`
#### Snippet
```java
	private AbstractTree<T> tree;

	public Folder(String id, AbstractTree<T> tree, IModel<T> model)
	{
		super(id, model);
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/TreeModelProvider.java`
#### Snippet
```java
	 *            the {@link AjaxRequestTarget} which initiated the changes
	 */
	public void update(AbstractTree<T> tree, AjaxRequestTarget target)
	{
		if (completeUpdate)
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java
						@Override
						public void component(final Component component,
							final IVisit<Component> visit)
						{
							if (component.getId().equals(thisId))
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java
								@Override
								public void component(final MarkupContainer component,
									final IVisit<Component> visit)
								{
									if (component.getId().equals(thisId))
```

### BoundedWildcard
Can generalize to `? extends PlacedAnimal`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
	 * @return The placed animal
	 */
	private PlacedAnimal placeAnimal(final List<PlacedAnimal> animals, final Animal newAnimal)
	{
		// Try 100 times
```

### BoundedWildcard
Can generalize to `? super String`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/AbstractRequestContext.java`
#### Snippet
```java
     * @param contentLengthDefault How to get the content length default.
     */
    protected AbstractRequestContext(final Function<String, String> contentLengthString, final LongSupplier contentLengthDefault) {
        super();
        this.contentLengthString = contentLengthString;
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
				{
					@Override
					public void component(final Component component, final IVisit<Component> visit)
					{
						if (!component.isStateless())
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxEventBehavior.java`
#### Snippet
```java
	 * @return the {@link AjaxEventBehavior}
	 */
	public static AjaxEventBehavior onEvent(String eventName, SerializableConsumer<AjaxRequestTarget> onEvent)
	{
		Args.notNull(onEvent, "onEvent");
```

### BoundedWildcard
Can generalize to `? super IRequestHandler`
in `wicket-core/src/main/java/org/apache/wicket/csp/ContentSecurityPolicySettings.java`
#### Snippet
```java
	 */
	public ContentSecurityPolicySettings setProtectedFilter(
		Predicate<IRequestHandler> protectedFilter)
	{
		Args.notNull(protectedFilter, "protectedFilter");
```

### BoundedWildcard
Can generalize to `? super IPartialPageRequestHandler`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxUtils.java`
#### Snippet
```java
     *            The action to run a {@link SerializableConsumer}
     */
    public static void executeIfAjaxOrWebSockets(SerializableConsumer<IPartialPageRequestHandler> ajaxAction)
    {
        Optional<IPartialPageRequestHandler> target = RequestCycle.get().find(IPartialPageRequestHandler.class);
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxUtils.java`
#### Snippet
```java
     *            The action to run a {@link SerializableConsumer}
     */
    public static void executeIfAjax(SerializableConsumer<AjaxRequestTarget> ajaxAction)
    {
        Optional<AjaxRequestTarget> target = RequestCycle.get().find(AjaxRequestTarget.class);
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxNewWindowNotifyingBehavior.java`
#### Snippet
```java
	 * @return the {@link AjaxNewWindowNotifyingBehavior}
	 */
	public static AjaxNewWindowNotifyingBehavior onNewWindow(SerializableConsumer<AjaxRequestTarget> onNewWindow)
	{
		Args.notNull(onNewWindow, "onNewWindow");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxClientInfoBehavior.java`
#### Snippet
```java
	 * @return the {@link AjaxClientInfoBehavior}
	 */
	public static AjaxClientInfoBehavior onClientInfo(SerializableBiConsumer<AjaxRequestTarget, WebClientInfo> onClientInfo)
	{
		Args.notNull(onClientInfo, "onClientInfo");
```

### BoundedWildcard
Can generalize to `? super WebClientInfo`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxClientInfoBehavior.java`
#### Snippet
```java
	 * @return the {@link AjaxClientInfoBehavior}
	 */
	public static AjaxClientInfoBehavior onClientInfo(SerializableBiConsumer<AjaxRequestTarget, WebClientInfo> onClientInfo)
	{
		Args.notNull(onClientInfo, "onClientInfo");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxOnDomReadyClientInfoBehavior.java`
#### Snippet
```java
	 * @return the {@link AjaxOnDomReadyClientInfoBehavior}
	 */
	public static AjaxOnDomReadyClientInfoBehavior onClientInfo(SerializableBiConsumer<AjaxRequestTarget, WebClientInfo> onClientInfo)
	{
		Args.notNull(onClientInfo, "onClientInfo");
```

### BoundedWildcard
Can generalize to `? super WebClientInfo`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxOnDomReadyClientInfoBehavior.java`
#### Snippet
```java
	 * @return the {@link AjaxOnDomReadyClientInfoBehavior}
	 */
	public static AjaxOnDomReadyClientInfoBehavior onClientInfo(SerializableBiConsumer<AjaxRequestTarget, WebClientInfo> onClientInfo)
	{
		Args.notNull(onClientInfo, "onClientInfo");
```

### BoundedWildcard
Can generalize to `? extends IResponseFilter`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java
	 */
	private CharSequence invokeResponseFilters(final StringResponse contentResponse,
		final List<IResponseFilter> responseFilters)
	{
		AppendingStringBuffer responseBuffer = new AppendingStringBuffer(
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxSelfUpdatingTimerBehavior.java`
#### Snippet
```java
	 * @return the {@link AbstractAjaxTimerBehavior}
	 */
	public static AjaxSelfUpdatingTimerBehavior onSelfUpdate(Duration interval, SerializableConsumer<AjaxRequestTarget> onTimer)
	{
		Args.notNull(onTimer, "onTimer");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractAjaxTimerBehavior.java`
#### Snippet
```java
	 * @return the {@link AbstractAjaxTimerBehavior}
	 */
	public static AbstractAjaxTimerBehavior onTimer(Duration interval, SerializableConsumer<AjaxRequestTarget> onTimer)
	{
		Args.notNull(onTimer, "onTimer");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/OnChangeAjaxBehavior.java`
#### Snippet
```java
	 * @return the {@link OnChangeAjaxBehavior}
	 */
	public static OnChangeAjaxBehavior onChange(SerializableConsumer<AjaxRequestTarget> onChange)
	{
		Args.notNull(onChange, "onChange");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormComponentUpdatingBehavior.java`
#### Snippet
```java
	 */
	public static AjaxFormComponentUpdatingBehavior onUpdate(String eventName,
		SerializableConsumer<AjaxRequestTarget> onUpdate)
	{
		Args.notNull(onUpdate, "onUpdate");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
	 */
	public static AjaxFormChoiceComponentUpdatingBehavior onUpdateChoice(
		SerializableConsumer<AjaxRequestTarget> onUpdateChoice)
	{
		Args.notNull(onUpdateChoice, "onUpdateChoice");
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java
	 */
	public static AjaxFormSubmitBehavior onSubmit(String eventName,
		SerializableConsumer<AjaxRequestTarget> onSubmit)
	{
		Args.notNull(onSubmit, "onSubmit");
```

### BoundedWildcard
Can generalize to `? extends Component`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
	 * @return html rendered by the panel
	 */
	public CharSequence renderComponent(final Supplier<Component> component)
	{
		return renderPage(() -> new RenderPage(component.get()));
```

### BoundedWildcard
Can generalize to `? extends IResourceFinder`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 *            resource finders. These will be tried in the given order.
	 */
	public ResourceStreamLocator(final List<IResourceFinder> finders)
	{
		this.finders = finders;
```

### BoundedWildcard
Can generalize to `? extends IResourceCachingStrategy`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceReferenceMapper.java`
#### Snippet
```java
	public ResourceReferenceMapper(IPageParametersEncoder pageParametersEncoder,
		Supplier<String> parentPathPartEscapeSequence,
		Supplier<IResourceCachingStrategy> cachingStrategy)
	{
		super(new BasicResourceReferenceMapper(pageParametersEncoder, cachingStrategy),
```

### BoundedWildcard
Can generalize to `? extends Component`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	 *         traversal occurred
	 */
	public final <R> R visitChildren(final IVisitor<Component, R> visitor)
	{
		return Visits.visitChildren(this, visitor);
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	 * @return Iterator that iterates over children in the order specified by comparator
	 */
	public final Iterator<Component> iterator(Comparator<Component> comparator)
	{
		final List<Component> sorted = copyChildren();
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockPageContext.java`
#### Snippet
```java

	@Override
	public <T> T getRequestData(MetaDataKey<T> key, Supplier<T> defaultValue)
	{
		T value = key.get(requestData);
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockPageContext.java`
#### Snippet
```java

	@Override
	public <T> T getRequestData(MetaDataKey<T> key, Supplier<T> defaultValue)
	{
		T value = key.get(requestData);
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockPageContext.java`
#### Snippet
```java
	
	@Override
	public <T extends Serializable> T getSessionData(MetaDataKey<T> key, Supplier<T> defaultValue)
	{
		T value = key.get(sessionData);
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockPageContext.java`
#### Snippet
```java
	
	@Override
	public <T extends Serializable> T getSessionData(MetaDataKey<T> key, Supplier<T> defaultValue)
	{
		T value = key.get(sessionData);
```

### BoundedWildcard
Can generalize to `? extends Cookie`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebRequest.java`
#### Snippet
```java
	}

	MockWebRequest(Url url, List<Cookie> cookies, Map<String, List<Object>> headers,
		MockRequestParameters postRequestParameters, Locale locale)
	{
```

### BoundedWildcard
Can generalize to `? extends Cookie`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebRequest.java`
#### Snippet
```java
	 * @param cookies
	 */
	public void setCookies(List<Cookie> cookies)
	{
		this.cookies.clear();
```

### BoundedWildcard
Can generalize to `? super C`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	@SuppressWarnings("unchecked")
	public final <R, C extends MarkupContainer> R visitParents(final Class<C> parentClass,
		final IVisitor<C, R> visitor, IVisitFilter filter)
	{
		Args.notNull(filter, "filter");
```

### BoundedWildcard
Can generalize to `? super XmlTag`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
	}
	
	public static List<TagTester> createTags(String markup, Function<XmlTag, Boolean> accept, boolean stopAfterFirst)
	{
		List<TagTester> testers = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends XmlTag`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
	 * @return corresponding openTag or {@code null}
	 */
	private static XmlTag findOpenTag(XmlTag closeTag, Stack<XmlTag> stack)
	{
		while (stack.size() > 0)
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
			 */
			@Override
			public void component(final Component component, final IVisit<Component> visit)
			{
				if (count == index)
```

### BoundedWildcard
Can generalize to `? extends CharSequence`
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
	}

	private CharSequence renderScripts(Collection<CharSequence> scripts) {
		StringBuilder combinedScript = new StringBuilder(1024);
		for (CharSequence script : scripts)
```

### BoundedWildcard
Can generalize to `? extends FeedbackMessage`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTester.java`
#### Snippet
```java
	 * @return the FeedbackMessages' messages
	 */
	private List<Serializable> getActualFeedbackMessages(List<FeedbackMessage> feedbackMessages)
	{
		List<Serializable> actualMessages = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/model/LoadableDetachableModel.java`
#### Snippet
```java
	 * @return the model
	 */
	public static <T> LoadableDetachableModel<T> of(SerializableSupplier<T> getter)
	{
		return new LoadableDetachableModel<T>()
```

### BoundedWildcard
Can generalize to `? extends R`
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
	 */
	default <R, U> IModel<R> combineWith(IModel<U> other,
		SerializableBiFunction<? super T, ? super U, R> combiner)
	{
		Args.notNull(combiner, "combiner");
```

### BoundedWildcard
Can generalize to `? extends IModel`
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
	 * @see LambdaModel#of(IModel, SerializableFunction, SerializableBiConsumer)
	 */
	default <R> IModel<R> flatMap(SerializableFunction<? super T, IModel<R>> mapper)
	{
		Args.notNull(mapper, "mapper");
```

### BoundedWildcard
Can generalize to `? extends R`
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
	 * @return a new IModel
	 */
	default <R> IModel<R> map(SerializableFunction<? super T, R> mapper)
	{
		Args.notNull(mapper, "mapper");
```

### BoundedWildcard
Can generalize to `? extends R`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
	 *            model object type
	 */
	public static <R> IModel<R> of(SerializableSupplier<R> getter)
	{
		return getter::get;
```

### BoundedWildcard
Can generalize to `? extends X`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
	 * @see IModel#flatMap(SerializableFunction)
	 */
	public static <X, R> IModel<R> of(IModel<X> target, SerializableFunction<X, R> getter,
		SerializableBiConsumer<X, R> setter)
	{
```

### BoundedWildcard
Can generalize to `? super X`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
	 * @see IModel#flatMap(SerializableFunction)
	 */
	public static <X, R> IModel<R> of(IModel<X> target, SerializableFunction<X, R> getter,
		SerializableBiConsumer<X, R> setter)
	{
```

### BoundedWildcard
Can generalize to `? extends R`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
	 * @see IModel#flatMap(SerializableFunction)
	 */
	public static <X, R> IModel<R> of(IModel<X> target, SerializableFunction<X, R> getter,
		SerializableBiConsumer<X, R> setter)
	{
```

### BoundedWildcard
Can generalize to `? super X`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
	 */
	public static <X, R> IModel<R> of(IModel<X> target, SerializableFunction<X, R> getter,
		SerializableBiConsumer<X, R> setter)
	{
		Args.notNull(target, "target");
```

### BoundedWildcard
Can generalize to `? super R`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
	 */
	public static <X, R> IModel<R> of(IModel<X> target, SerializableFunction<X, R> getter,
		SerializableBiConsumer<X, R> setter)
	{
		Args.notNull(target, "target");
```

### BoundedWildcard
Can generalize to `? extends R`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
	 *            model object type
	 */
	public static <R> IModel<R> of(SerializableSupplier<R> getter, SerializableConsumer<R> setter)
	{
		Args.notNull(getter, "getter");
```

### BoundedWildcard
Can generalize to `? super R`
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
	 *            model object type
	 */
	public static <R> IModel<R> of(SerializableSupplier<R> getter, SerializableConsumer<R> setter)
	{
		Args.notNull(getter, "getter");
```

### BoundedWildcard
Can generalize to `? super HeaderItem`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/FilteringHeaderResponse.java`
#### Snippet
```java
	}

	protected void render(HeaderItem item, List<HeaderItem> filteredItems)
	{
		if (RequestCycle.get().find(IPartialPageRequestHandler.class).isPresent())
```

### BoundedWildcard
Can generalize to `? extends PushItem`
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	public PushHeaderItem push(List<PushItem> pushItems)
	{
		RequestCycle requestCycle = RequestCycle.get();
```

### BoundedWildcard
Can generalize to `? super String`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
	 *            Use {@code null} to disable caching.
	 */
	public SecurePackageResourceGuard(final ConcurrentMap<String, Boolean> cache)
	{
		this.cache = cache;
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/LambdaChoiceRenderer.java`
#### Snippet
```java
     * @param idExpression      A function to get the id value
     */
    public LambdaChoiceRenderer(SerializableFunction<T, ?> displayExpression, SerializableFunction<T, ?> idExpression) {
        this.displayExpression = displayExpression;
        this.idExpression = idExpression;
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/LambdaChoiceRenderer.java`
#### Snippet
```java
     * @param idExpression      A function to get the id value
     */
    public LambdaChoiceRenderer(SerializableFunction<T, ?> displayExpression, SerializableFunction<T, ?> idExpression) {
        this.displayExpression = displayExpression;
        this.idExpression = idExpression;
```

### BoundedWildcard
Can generalize to `? super HtmlHeaderContainer`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebPage.java`
#### Snippet
```java
		{
			@Override
			public void component(final Component component, final IVisit<HtmlHeaderContainer> visit)
			{
				if (component instanceof HtmlHeaderContainer)
```

### BoundedWildcard
Can generalize to `? super T`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java
	}

	private void addRetainedDisabled(ArrayList<T> selectedValues)
	{
		if (isRetainDisabledSelected())
```

### BoundedWildcard
Can generalize to `? super Check`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
						{
							@Override
							public void component(final Check<T> check, final IVisit<Check<T>> visit)
							{
								if (String.valueOf(check.getValue()).equals(value))
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
				{
					@Override
					public void component(Component child, IVisit<Component> visit)
					{
						if (child == searched[0])
```

### BoundedWildcard
Can generalize to `? super String`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/EmailTextField.java`
#### Snippet
```java
	 *            the validator that will check the correctness of the input value
	 */
	public EmailTextField(String id, IModel<String> model, IValidator<String> emailValidator)
	{
		super(id, model, String.class);
```

### BoundedWildcard
Can generalize to `? super String`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java
		{
			@Override
			public void component(Radio<T> radio, IVisit<String> visit)
			{
				if (getModelComparator().compare(RadioGroup.this, radio.getDefaultModelObject()))
```

### BoundedWildcard
Can generalize to `? super Radio`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java

					@Override
					public void component(final Radio<T> radio, final IVisit<Radio<T>> visit)
					{
						if (radio.getValue().equals(value))
```

### BoundedWildcard
Can generalize to `? super Boolean`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
		{
			@Override
			public void component(final FormComponent<?> formComponent, final IVisit<Boolean> visit)
			{
				if (formComponent.hasErrorMessage())
```

### BoundedWildcard
Can generalize to `? extends S`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 * @throws IllegalArgumentException if type is not supported
	 */
	private static <S> Collection<S> newCollection(Class<?> hint, Collection<S> elements)
	{
		if (Set.class.isAssignableFrom(hint)) {
```

### BoundedWildcard
Can generalize to `? super AjaxRequestTarget`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FilesSelectedBehavior.java`
#### Snippet
```java
     */
    public static FilesSelectedBehavior onSelected(
            SerializableBiConsumer<AjaxRequestTarget, List<FileDescription>> select)
    {
        Args.notNull(select, "select");
```

### BoundedWildcard
Can generalize to `? super List`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FilesSelectedBehavior.java`
#### Snippet
```java
     */
    public static FilesSelectedBehavior onSelected(
            SerializableBiConsumer<AjaxRequestTarget, List<FileDescription>> select)
    {
        Args.notNull(select, "select");
```

### BoundedWildcard
Can generalize to `? extends FileUpload`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/FileUploadResourceReference.java`
#### Snippet
```java

			@Override
			protected String generateJsonResponse(ResourceResponse resourceResponse, ServletWebRequest webRequest, List<FileUpload> files)
			{
				JSONArray json = new JSONArray();
```

### BoundedWildcard
Can generalize to `? extends FormComponent`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	 * @return user provided in callback
	 */
	public final <R> R visitFormComponents(final IVisitor<FormComponent<?>, R> visitor)
	{
		return visitChildren(FormComponent.class, visitor);
```

### BoundedWildcard
Can generalize to `? super IFormSubmittingComponent`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
				@Override
				public void component(final Component component,
					final IVisit<IFormSubmittingComponent> visit)
				{
					// Get submitting component
```

### BoundedWildcard
Can generalize to `? super Boolean`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
					{
						@Override
						public void component(final Component component, final IVisit<Boolean> visit)
						{
							boolean isMultiPart = false;
```

### BoundedWildcard
Can generalize to `? super Boolean`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
		{
			@Override
			public void component(final Component component, final IVisit<Boolean> visit)
			{
				if (component.hasErrorMessage() && component.isVisibleInHierarchy() && component.isEnabledInHierarchy())
```

### BoundedWildcard
Can generalize to `? extends FileUpload`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/AbstractFileUploadResource.java`
#### Snippet
```java
	 *            List<FileUpload>
	 */
	protected void saveFiles(List<FileUpload> fileItems, String uploadId)
	{
		for (FileUpload fileItem : fileItems)
```

### BoundedWildcard
Can generalize to `? extends ComponentData`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/debug/PageView.java`
#### Snippet
```java
			 */
			@Override
			protected void populateItem(final ListItem<ComponentData> listItem)
			{
				final ComponentData componentData = listItem.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends MarkupContainer`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AbstractMarkupSourcingStrategy.java`
#### Snippet
```java
	 */
	protected IMarkupFragment searchInNestedTransparentResolvers(IMarkupFragment containerMarkup, Component child, 
		List<MarkupContainer> componentResolvers)
	{
		IMarkupFragment childMarkupFound = null;
```

### BoundedWildcard
Can generalize to `? extends FeedbackMessage`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java

		@Override
		protected void populateItem(final ListItem<FeedbackMessage> listItem)
		{
			final FeedbackMessage message = listItem.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends List`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalImage.java`
#### Snippet
```java
	 */
	protected void buildSrcSetAttribute(final ComponentTag tag,
		IModel<List<Serializable>> srcSetModel)
	{
		int srcSetPosition = 0;
```

### BoundedWildcard
Can generalize to `? extends IModel`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java
		 *            maximum number of items that can be accessed.
		 */
		public CappedIteratorAdapter(Iterator<IModel<T>> delegate, long max)
		{
			this.delegate = delegate;
```

### BoundedWildcard
Can generalize to `? extends IModel`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/DefaultItemReuseStrategy.java`
#### Snippet
```java
	@Override
	public <T> Iterator<Item<T>> getItems(final IItemFactory<T> factory,
		final Iterator<IModel<T>> newModels, Iterator<Item<T>> existingItems)
	{
		return new Iterator<Item<T>>()
```

### BoundedWildcard
Can generalize to `? extends IModel`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/ReuseIfModelsEqualStrategy.java`
#### Snippet
```java
	@Override
	public <T> Iterator<Item<T>> getItems(final IItemFactory<T> factory,
		final Iterator<IModel<T>> newModels, Iterator<Item<T>> existingItems)
	{
		final Map<IModel<T>, Item<T>> modelToItem = Generics.newHashMap();
```

### BoundedWildcard
Can generalize to `? extends Item`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/ReuseIfModelsEqualStrategy.java`
#### Snippet
```java
	@Override
	public <T> Iterator<Item<T>> getItems(final IItemFactory<T> factory,
		final Iterator<IModel<T>> newModels, Iterator<Item<T>> existingItems)
	{
		final Map<IModel<T>, Item<T>> modelToItem = Generics.newHashMap();
```

### BoundedWildcard
Can generalize to `? extends Item`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/RefreshingView.java`
#### Snippet
```java
	 *            item instances to be added to this view
	 */
	protected void addItems(Iterator<Item<T>> items)
	{
		int index = 0;
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ModelIteratorAdapter.java`
#### Snippet
```java
	 *            iterator that will be wrapped
	 */
	public ModelIteratorAdapter(Iterator<T> delegate)
	{
		this.delegate = delegate;
```

### BoundedWildcard
Can generalize to `? extends Item`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/GridView.java`
#### Snippet
```java

	@Override
	protected void addItems(Iterator<Item<T>> items)
	{
		if (items.hasNext())
```

### BoundedWildcard
Can generalize to `? super WicketHeadContainer`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
	{
		@Override
		public void component(Component component, IVisit<WicketHeadContainer> visit)
		{
			if (component instanceof WicketHeadContainer)
```

### BoundedWildcard
Can generalize to `? super HtmlHeaderContainer`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
				{
					@Override
					public void component(final Component component, final IVisit<HtmlHeaderContainer> visit)
					{
						if (component instanceof HtmlHeaderContainer)
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-core/src/main/java/org/apache/wicket/markup/renderStrategy/AbstractHeaderRenderStrategy.java`
#### Snippet
```java
		Component enclosureController = enclosure.getParent().visitChildren(new IVisitor<Component, Component>() {
			@Override
			public void component(Component object, IVisit<Component> visit) {
				if (object.getId().equals(childId)){
					visit.stop(object);
```

### BoundedWildcard
Can generalize to `? super Component`
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 * @return The created behavior
	 */
	public static Behavior onTag(SerializableBiConsumer<Component, ComponentTag> onTagConsumer)
	{
		Args.notNull(onTagConsumer, "onTagConsumer");
```

### BoundedWildcard
Can generalize to `? super ComponentTag`
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 * @return The created behavior
	 */
	public static Behavior onTag(SerializableBiConsumer<Component, ComponentTag> onTagConsumer)
	{
		Args.notNull(onTagConsumer, "onTagConsumer");
```

### BoundedWildcard
Can generalize to `? super String`
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 */
	public static Behavior onAttribute(String name,
		SerializableFunction<String, CharSequence> onAttribute)
	{
		Args.notEmpty(name, "name");
```

### BoundedWildcard
Can generalize to `? extends CharSequence`
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 */
	public static Behavior onAttribute(String name,
		SerializableFunction<String, CharSequence> onAttribute)
	{
		Args.notEmpty(name, "name");
```

### BoundedWildcard
Can generalize to `? super WebResponse`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
		private final Consumer<WebResponse> action;

		private Action(ActionType type, Consumer<WebResponse> action)
		{
			this.type = type;
```

### BoundedWildcard
Can generalize to `? extends Cookie`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/CookieCollection.java`
#### Snippet
```java
	 * @param cookies list of cookies
	 */
	public void addAll(List<Cookie> cookies)
	{
		for (Cookie cookie : cookies)
```

### BoundedWildcard
Can generalize to `? super String`
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemJarPathService.java`
#### Snippet
```java

	@Override
	public Path getPath(URI uri, Map<String, String> env)
	{
		try
```

### BoundedWildcard
Can generalize to `? extends Cookie`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	 * @param cookies
	 */
	public void addCookies(Iterable<Cookie> cookies)
	{
		for (Cookie cookie : cookies)
```

### BoundedWildcard
Can generalize to `? extends IResourceStream`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
	}

	protected Instant findLastModified(List<IResourceStream> resources)
	{
		Instant ret = null;
```

### BoundedWildcard
Can generalize to `? extends IResourceStream`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
	}

	protected String findContentType(List<IResourceStream> resources)
	{
		for (IResourceStream curStream : resources)
```

### BoundedWildcard
Can generalize to `? extends IResourceStream`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
	}

	protected byte[] readAllResources(List<IResourceStream> resources) throws IOException,
		ResourceStreamNotFoundException
	{
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java

		@Override
		public <T extends Serializable> T getSessionData(MetaDataKey<T> key, Supplier<T> defaultValue)
		{
			synchronized (sessionData)
```

### BoundedWildcard
Can generalize to `? extends IMemoryData`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InMemoryPageStore.java`
#### Snippet
```java
	 *            storage for datas
	 */
	protected InMemoryPageStore(String applicationName, Supplier<IMemoryData> dataCreator,
		Map<String, IMemoryData> datas)
	{
```

### BoundedWildcard
Can generalize to `? extends SessionData`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
	}

	private InSessionPageStore(ISerializer serializer, Supplier<SessionData> dataCreator)
	{
		this.serializer = serializer;
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DefaultPageContext.java`
#### Snippet
```java

	@Override
	public <T> T getRequestData(MetaDataKey<T> key, Supplier<T> defaultValue)
	{
		RequestCycle requestCycle = RequestCycle.get();
```

### BoundedWildcard
Can generalize to `? extends T`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DefaultPageContext.java`
#### Snippet
```java
	
	@Override
	public <T extends Serializable> T getSessionData(MetaDataKey<T> key, Supplier<T> defaultValue)
	{
		Session session = Session.get();
```

### BoundedWildcard
Can generalize to `? extends PendingAdd`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AsynchronousPageStore.java`
#### Snippet
```java
		private final IPageStore delegate;

		private PageAddingRunnable(IPageStore delegate, BlockingQueue<PendingAdd> queue,
		                           ConcurrentMap<String, PendingAdd> map)
		{
```

### BoundedWildcard
Can generalize to `? extends Z`
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/RangeValidator.java`
#### Snippet
```java

	@Override
	protected Z getValue(IValidatable<Z> validatable)
	{
		return validatable.getValue();
```

### BoundedWildcard
Can generalize to `? super URL`
in `wicket-core/src/main/java/org/apache/wicket/application/AbstractClassResolver.java`
#### Snippet
```java
	 * @param loadedResources
	 */
	private void loadResources(Enumeration<URL> resources, Set<URL> loadedResources)
	{
		if (resources != null)
```

### BoundedWildcard
Can generalize to `? super Class`
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java
	 *            the listener to notify upon class change
	 */
	public void setListener(IChangeListener<Class<?>> listener)
	{
		this.listener = listener;
```

### BoundedWildcard
Can generalize to `? extends Comment`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/GuestBook.java`
#### Snippet
```java
		{
			@Override
			public void populateItem(final ListItem<Comment> listItem)
			{
				final Comment comment = listItem.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/EffectsPage.java`
#### Snippet
```java
		{
			@Override
			public void onClick(Optional<AjaxRequestTarget> targetOptional)
			{
				counter2++;
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/EffectsPage.java`
#### Snippet
```java
		{
			@Override
			public void onClick(Optional<AjaxRequestTarget> targetOptional)
			{
				counter3++;
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LinksPage.java`
#### Snippet
```java
		{
			@Override
			public void onClick(Optional<AjaxRequestTarget> targetOptional)
			{
				counter2++;
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java
		 *            the request target.
		 */
		void onShowForm(Optional<AjaxRequestTarget> targetOptional)
		{
			// toggle the visibility
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java
		}

		void onRemoveCompletedTodos(Optional<AjaxRequestTarget> targetOptional)
		{
			List<TodoItem> ready = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends Foo`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/TreeApplication.java`
#### Snippet
```java
	}

	private static Foo findFoo(List<Foo> foos, String id)
	{
		for (Foo foo : foos)
```

### BoundedWildcard
Can generalize to `? extends FileUpload`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
			WebMarkupContainer drop = new WebMarkupContainer("drop");
			drop.add(new AjaxFileDropBehavior() {
				protected void onFileUpload(AjaxRequestTarget target, List<FileUpload> files) {

					// display uploaded info
```

### BoundedWildcard
Can generalize to `? extends FileDescription`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
			private List<FileDescription> fileDescriptions;

			public void setFileDescriptions(List<FileDescription> fileDescriptions) {
				this.fileDescriptions = new ArrayList<>(fileDescriptions);
			}
```

### BoundedWildcard
Can generalize to `? extends Foo`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MultiLineLabelContent.java`
#### Snippet
```java
	@Override
	public Component newContentComponent(String id, final AbstractTree<Foo> tree,
		final IModel<Foo> model)
	{
		return new MultiLineLabel(id, new IModel<String>()
```

### BoundedWildcard
Can generalize to `? extends Content`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MixedContent.java`
#### Snippet
```java
	private List<Content> contents;

	public MixedContent(List<Content> contents)
	{
		this.contents = new ArrayList<>(contents);
```

### BoundedWildcard
Can generalize to `? extends Set`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/NestedTreePage.java`
#### Snippet
```java

	@Override
	protected AbstractTree<Foo> createTree(FooProvider provider, IModel<Set<Foo>> state)
	{
		tree = new NestedTree<Foo>("tree", provider, state)
```

### BoundedWildcard
Can generalize to `? extends Set`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/TableTreePage.java`
#### Snippet
```java

	@Override
	protected AbstractTree<Foo> createTree(FooProvider provider, IModel<Set<Foo>> state)
	{
		List<IColumn<Foo, String>> columns = createColumns();
```

### BoundedWildcard
Can generalize to `? super Foo`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/BookmarkableFolderContent.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	public BookmarkableFolderContent(final AbstractTree<Foo> tree)
	{
		String id = tree.getRequest().getRequestParameters().getParameterValue("foo").toString();
```

### BoundedWildcard
Can generalize to `? extends Foo`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/BookmarkableFolderContent.java`
#### Snippet
```java

			@Override
			protected MarkupContainer newLinkComponent(String id, IModel<Foo> model)
			{
				Foo foo = model.getObject();
```

### BoundedWildcard
Can generalize to `? super Foo`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/SelectableFolderContent.java`
#### Snippet
```java
	}

	protected void select(Foo foo, AbstractTree<Foo> tree, final Optional<AjaxRequestTarget> targetOptional)
	{
		if (selected != null)
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/SelectableFolderContent.java`
#### Snippet
```java
	}

	protected void select(Foo foo, AbstractTree<Foo> tree, final Optional<AjaxRequestTarget> targetOptional)
	{
		if (selected != null)
```

### BoundedWildcard
Can generalize to `? extends Foo`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/CheckedFolderContent.java`
#### Snippet
```java

			@Override
			protected IModel<Boolean> newCheckBoxModel(final IModel<Foo> model)
			{
				return new IModel<Boolean>()
```

### BoundedWildcard
Can generalize to `? super Foo`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MultiSelectableFolderContent.java`
#### Snippet
```java
	}

	protected void toggle(Foo foo, AbstractTree<Foo> tree, final Optional<AjaxRequestTarget> targetOptional)
	{
		if (isSelected(foo))
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MultiSelectableFolderContent.java`
#### Snippet
```java
	}

	protected void toggle(Foo foo, AbstractTree<Foo> tree, final Optional<AjaxRequestTarget> targetOptional)
	{
		if (isSelected(foo))
```

### BoundedWildcard
Can generalize to `? super Contact`
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/ContactGenerator.java`
#### Snippet
```java
	 * @param count
	 */
	public void generate(Collection<Contact> collection, int count)
	{
		for (int i = 0; i < count; i++)
```

### BoundedWildcard
Can generalize to `? extends Foo`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/EditableFolderContent.java`
#### Snippet
```java

			@Override
			protected Component newLabelComponent(String id, final IModel<Foo> model)
			{
				return new AjaxEditableLabel<String>(id, new PropertyModel<>(model, "bar"))
```

### BoundedWildcard
Can generalize to `? extends List`
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/MultiUploadPage.java`
#### Snippet
```java
		 *            The file list model
		 */
		public FileListView(String name, final IModel<List<File>> files)
		{
			super(name, files);
```

### BoundedWildcard
Can generalize to `? extends File`
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/MultiUploadPage.java`
#### Snippet
```java

		@Override
		protected void populateItem(ListItem<File> listItem)
		{
			final File file = listItem.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends List`
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadPage.java`
#### Snippet
```java
		 *            The file list model
		 */
		public FileListView(String name, final IModel<List<File>> files)
		{
			super(name, files);
```

### BoundedWildcard
Can generalize to `? extends File`
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadPage.java`
#### Snippet
```java
		 */
		@Override
		protected void populateItem(ListItem<File> listItem)
		{
			final File file = listItem.getModelObject();
```

### BoundedWildcard
Can generalize to `? super String`
in `wicket-examples/src/main/java/org/apache/wicket/examples/source/SourcesPage.java`
#### Snippet
```java
		}

		private void scanJarFile(String packageRef, JarFile jf, List<String> resources)
		{
			Enumeration<JarEntry> enumeration = jf.entries();
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/source/SourcesPage.java`
#### Snippet
```java
					{
						@Override
						public void onClick(Optional<AjaxRequestTarget> targetOptional)
						{
							setName(getDefaultModelObjectAsString());
```

### BoundedWildcard
Can generalize to `? super String`
in `wicket-examples/src/main/java/org/apache/wicket/examples/source/SourcesPage.java`
#### Snippet
```java

		private void addResources(final AppendingStringBuffer relativePath, final File dir,
			List<String> resources)
		{
			File[] files = dir.listFiles();
```

### BoundedWildcard
Can generalize to `? extends Person`
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/XmlPage.java`
#### Snippet
```java

		@Override
		protected void populateItem(ListItem<Person> item)
		{
			Person person = item.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends Letter`
in `wicket-examples/src/main/java/org/apache/wicket/examples/hangman/Guess.java`
#### Snippet
```java
		{
			@Override
			protected void populateItem(final ListItem<Letter> listItem)
			{
				final Letter letter = listItem.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends StringValue`
in `wicket-examples/src/main/java/org/apache/wicket/examples/niceurl/mounted/Page5.java`
#### Snippet
```java
	 * @return
	 */
	private String join(List<StringValue> values)
	{
	    StringBuilder sb = new StringBuilder();
```

### BoundedWildcard
Can generalize to `? extends Contact`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SortableContactDataProvider.java`
#### Snippet
```java
	}

	private List<Contact> filterContacts(List<Contact> contactsFound)
	{
	    ArrayList<Contact> result = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? super Contact`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactGenerator.java`
#### Snippet
```java
	 * @param count
	 */
	public void generate(Collection<Contact> collection, int count)
	{
		for (int i = 0; i < count; i++)
```

### BoundedWildcard
Can generalize to `? extends Contact`
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/GridViewPage.java`
#### Snippet
```java
		{
			@Override
			protected void populateItem(Item<Contact> item)
			{
				final Contact contact = item.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends AjaxRequestTarget`
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/AjaxStatelessExample.java`
#### Snippet
```java
		{
			@Override
			public void onClick(final Optional<AjaxRequestTarget> target)
			{
				Integer counter = (Integer)incrementLabel.getDefaultModelObject();
```

### BoundedWildcard
Can generalize to `? extends User`
in `wicket-examples/src/main/java/org/apache/wicket/examples/authorization/Index.java`
#### Snippet
```java
		{
			@Override
			protected void populateItem(ListItem<User> item)
			{
				final User user = item.getModelObject();
```

### BoundedWildcard
Can generalize to `? extends IWebSocketConnection`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketPushBroadcaster.java`
#### Snippet
```java
	}

	private void process(final Application application, final Collection<IWebSocketConnection> wsConnections,
	                     final IWebSocketPushMessage message)
	{
```

### BoundedWildcard
Can generalize to `? super Integer`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
	}

	public void setNotifyOnCloseEvent(Function<Integer, Boolean> notifyOnCloseEvent)
	{
		this.notifyOnCloseEvent = notifyOnCloseEvent;
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
	}

	public void setNotifyOnErrorEvent(Function<Throwable, Boolean> notifyOnErrorEvent)
	{
		this.notifyOnErrorEvent = notifyOnErrorEvent;
```

### BoundedWildcard
Can generalize to `? super Boolean`
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/AutoConversationManager.java`
#### Snippet
```java
		{
			@Override
			public void component(Component object, IVisit<Boolean> visit)
			{
				if (object instanceof ConversationalComponent)
```

## RuleId[id=MissortedModifiers]
### MissortedModifiers
Missorted modifiers `synchronized final`
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java
	 *             thrown if task is already running
	 */
	public synchronized final void run(final Duration frequency, final ICode code)
	{
		if (!isStarted)
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
	}

	static abstract class MapIntMap implements Entities.EntityMap
	{
		protected final Map<String, Integer> mapNameToValue;
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceUtils.java`
#### Snippet
```java
		.compile("_([a-z]{2})(_([A-Z]{2})(_([^_\\.]+))?)?(\\.min)?$");
	/** Stores standard ISO country codes from {@code java.util.Locale} **/
	private final static Set<String> isoCountries = new HashSet<>(
		Arrays.asList(Locale.getISOCountries()));
	/** Stores standard ISO language codes from {@code java.util.Locale} **/
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceUtils.java`
#### Snippet
```java
		Arrays.asList(Locale.getISOCountries()));
	/** Stores standard ISO language codes from {@code java.util.Locale} **/
	private final static Set<String> isoLanguages = new HashSet<>(
		Arrays.asList(Locale.getISOLanguages()));
	
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/form/upload/UploadProgressBar.java`
#### Snippet
```java
	 * Initializer for this component; binds static resources.
	 */
	public final static class ComponentInitializer implements IInitializer
	{
		@Override
```

### MissortedModifiers
Missorted modifiers `synchronized static`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetUtil.java`
#### Snippet
```java
	 *            Wicket application object
	 */
	private synchronized static void initialize(final WebApplication application)
	{
		if (charSetMap == null)
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
	 * @author Matej Knopp
	 */
	public final static class QueryParameter implements Serializable
	{
		private static final long serialVersionUID = 1L;
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
public class DataTable<T, S> extends Panel implements IPageableItems
{
	static abstract class CssAttributeBehavior extends Behavior
	{
		private static final long serialVersionUID = 1L;
```

### MissortedModifiers
Missorted modifiers `synchronized final`
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	 */
	@Override
	public synchronized final <M extends Serializable> M getMetaData(final MetaDataKey<M> key)
	{
		return key.get(metaData);
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/file/WebApplicationPath.java`
#### Snippet
```java
public final class WebApplicationPath implements IResourceFinder
{
	private final static Logger log = LoggerFactory.getLogger(WebApplicationPath.class);

	private static final String WEB_INF = "WEB-INF/";
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/AbstractJceCrypt.java`
#### Snippet
```java
	 * @return the string encrypted
	 */
	abstract protected byte[] encrypt(final byte[] plainBytes);
}
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/AbstractJceCrypt.java`
#### Snippet
```java
	 * @return the decrypted text
	 */
	abstract protected byte[] decrypt(final byte[] encrypted);


```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptUtils.java`
#### Snippet
```java

	/** Script close tag including content suffix */
	public final static String SCRIPT_CLOSE_TAG = SCRIPT_CONTENT_SUFFIX + "</script>\n";

	public static final String ATTR_ID = "id";
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptUtils.java`
#### Snippet
```java

	/** Script open tag including content prefix */
	public final static String SCRIPT_OPEN_TAG = "<script type=\"text/javascript\">" +
		SCRIPT_CONTENT_PREFIX;

```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptUtils.java`
#### Snippet
```java
	 * Wicket.Head.Contributor.processScript() function from wicket-ajax-jquery.js
	 */
	public final static String SCRIPT_CONTENT_SUFFIX = "\n/*]]>*/\n";

	/** Script open tag including content prefix */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptUtils.java`
#### Snippet
```java
	 * Wicket.Head.Contributor.processScript() function from wicket-ajax-jquery.js
	 */
	public final static String SCRIPT_CONTENT_PREFIX = "\n/*<![CDATA[*/\n";

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/CssUtils.java`
#### Snippet
```java

	/** CSS inline open tag */
	public final static String INLINE_OPEN_TAG = INLINE_OPEN_TAG_START + ">\n";

	/** CSS inline close tag */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/CssUtils.java`
#### Snippet
```java
	// FIXME type text/css can be omitted for the style tag in supported browsers
	/** start of CSS inline open tag */
	public final static String INLINE_OPEN_TAG_START = "<style type=\"text/css\"";

	/** CSS inline open tag */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/CssUtils.java`
#### Snippet
```java

	/** CSS inline close tag */
	public final static String INLINE_CLOSE_TAG = "</style>\n";

	public static final String ATTR_ID = "id";
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java
	 */
	/** Inside regular text */
	private final static int REGULAR_TEXT = 1;

	/** String started with single quote (') */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java

	/** String started with double quotes (") */
	private final static int STRING_DOUBLE_QUOTES = 3;

	/** Inside two or more whitespace characters */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java

	/** Inside two or more whitespace characters */
	private final static int WHITE_SPACE = 4;

	/** Inside a line comment (// ) */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java

	/** String started with single quote (') */
	private final static int STRING_SINGLE_QUOTE = 2;

	/** String started with double quotes (") */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java

	/** Inside a line comment (// ) */
	private final static int LINE_COMMENT = 5;

	/** Inside a multi line comment */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java

	/** Function "return" keyword, useful to identify if we are inside a regexp */
	private final static String RETURN_KEYWORD = "return";

	private int getPrevCount(String s, int fromIndex, char c)
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java

	/** Inside a template literal */
	private final static int TEMPLATE_LITERAL = 8;

	/** Function "return" keyword, useful to identify if we are inside a regexp */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java

	/** Inside a regular expression */
	private final static int REG_EXP = 7;

	/** Inside a template literal */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java

	/** Inside a multi line comment */
	private final static int MULTILINE_COMMENT = 6;

	/** Inside a regular expression */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/caching/NullResourceStreamReference.java`
#### Snippet
```java
class NullResourceStreamReference implements IResourceStreamReference
{
	final static NullResourceStreamReference INSTANCE = new NullResourceStreamReference();

	@Override
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
	}

	private static abstract class ObjectOutputAdaptor implements ObjectOutput
	{

```

### MissortedModifiers
Missorted modifiers `transient final`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		private static final long serialVersionUID = 1L;

		private transient final Component removedChild;
		private transient final Component previousSibling;

```

### MissortedModifiers
Missorted modifiers `transient final`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java

		private transient final Component removedChild;
		private transient final Component previousSibling;

		private RemovedChild(Component removedChild, Component previousSibling)
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieUtils.java`
#### Snippet
```java
public class CookieUtils
{
	private final static Logger log = LoggerFactory.getLogger(CookieUtils.class);

	public static final String DEFAULT_SESSIONID_COOKIE_NAME = "JSESSIONID";
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupType.java`
#### Snippet
```java

	/** A HTML markup type for web pages */
	public final static MarkupType HTML_MARKUP_TYPE = new MarkupType("html", HTML_MIME);

	private final String extension;
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupParser.java`
#### Snippet
```java
{
	/** "wicket" */
	public final static String WICKET = "wicket";

	/**
```

### MissortedModifiers
Missorted modifiers `final public`
in `wicket-core/src/main/java/org/apache/wicket/markup/Markup.java`
#### Snippet
```java
	 * Make all tags immutable and the list of elements unmodifiable.
	 */
	final public void makeImmutable()
	{
		for (MarkupElement markupElement : markupElements)
```

### MissortedModifiers
Missorted modifiers `final public`
in `wicket-core/src/main/java/org/apache/wicket/markup/Markup.java`
#### Snippet
```java
	 * @param markupElement
	 */
	final public void addMarkupElement(final int pos, final MarkupElement markupElement)
	{
		markupElements.add(pos, markupElement);
```

### MissortedModifiers
Missorted modifiers `final public`
in `wicket-core/src/main/java/org/apache/wicket/markup/Markup.java`
#### Snippet
```java
	 * @param markupElement
	 */
	final public void addMarkupElement(final MarkupElement markupElement)
	{
		markupElements.add(markupElement);
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
	
	/** If true, the current tag contains a child or a descendant with the "wicket:id" attribute */
	public final static int CONTAINS_WICKET_ID = 0x0040;

	/** If close tag, than reference to the corresponding open tag */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/** Some HTML tags are allow to have no close tag, e.g. 'br' */
	private final static int NO_CLOSE_TAG = 0x0010;

	/** Render the tag as RawMarkup even if no Component can be found */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/** True, if attributes have been modified or added */
	private final static int MODIFIED = 0x0002;

	/** If true, then the MarkupParser will ignore (remove) it. Temporary working variable */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/** If true, then the tag contain an automatically created wicket id */
	private final static int AUTO_COMPONENT = 0x0008;

	/** Some HTML tags are allow to have no close tag, e.g. 'br' */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/** Render the tag as RawMarkup even if no Component can be found */
	public final static int RENDER_RAW = 0x0020;
	
	/** If true, the current tag contains a child or a descendant with the "wicket:id" attribute */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/** If true, then the MarkupParser will ignore (remove) it. Temporary working variable */
	private final static int IGNORE = 0x0004;

	/** If true, then the tag contain an automatically created wicket id */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/** True if a href attribute is available and autolinking is on */
	private final static int AUTOLINK = 0x0001;

	/** True, if attributes have been modified or added */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
public class MergedMarkup extends Markup
{
	private final static Logger log = LoggerFactory.getLogger(MergedMarkup.class);

	/**
```

### MissortedModifiers
Missorted modifiers `final public`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupResourceStream.java`
#### Snippet
```java
	 * @return usually it is "wicket:id"
	 */
	final public String getWicketId()
	{
		return wicketId;
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/FileUploadToResourceField.java`
#### Snippet
```java


    private static abstract class FileModel implements IModel<List<UploadInfo>>
    {

```

### MissortedModifiers
Missorted modifiers `final private`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/TagStack.java`
#### Snippet
```java

	/** Tag stack to find balancing tags */
	final private ArrayDeque<ComponentTag> stack = new ArrayDeque<ComponentTag>();
	private boolean debug;

```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/AbstractMarkupFilter.java`
#### Snippet
```java
	 *  The counters are used by {@link #getRequestUniqueId()} to get unique ids for markup tags.
	 * **/
	protected final static MetaDataKey<Map<String, AtomicInteger>> REQUEST_COUNTER_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java

	/** The Component id prefix. */
	public final static String INLINE_ENCLOSURE_ID_PREFIX = "InlineEnclosure-";

	/** Attribute to identify inline enclosures */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java

	/** Attribute to identify inline enclosures */
	public final static String INLINE_ENCLOSURE_ATTRIBUTE_NAME = "enclosure";

	/** enclosures inside enclosures */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketMessageTagHandler.java`
#### Snippet
```java
	 * The id automatically assigned to tags with wicket:message attribute but without id
	 */
	public final static String WICKET_MESSAGE_CONTAINER_ID = "_message_attr_";

	/**
```

### MissortedModifiers
Missorted modifiers `final private`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHandler.java`
#### Snippet
```java

	/** Tag stack to find balancing tags */
	final private ArrayDeque<ComponentTag> stack = new ArrayDeque<ComponentTag>();

	/** Map of simple tags. */
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ArrayIteratorAdapter.java`
#### Snippet
```java
	 * @return IModel wrapper for the object
	 */
	abstract protected IModel<T> model(T object);


```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ModelIteratorAdapter.java`
#### Snippet
```java
	 * @return IModel wrapper for the object
	 */
	abstract protected IModel<T> model(T object);
}

```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/GridView.java`
#### Snippet
```java
	 *            Item object
	 */
	abstract protected void populateEmptyItem(Item<T> item);

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltTransformer.java`
#### Snippet
```java
public class XsltTransformer implements ITransformer
{
	private final static String extension = "xsl";

	/** an optional XSL file */
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
	 * and users wouldn't know where to add the component to.
	 */
	private final static class ResourceReferenceAutolink extends WebMarkupContainer
		implements
			IComponentResolver
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
	 *            type of model object
	 */
	public final static class AutolinkBookmarkablePageLink<T> extends BookmarkablePageLink<T>
		implements
			IComponentResolver
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
	 * Abstract implementation that has a helper method for creating a resource reference.
	 */
	public static abstract class AbstractAutolinkResolverDelegate
		implements
			IAutolinkResolverDelegate
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `wicket-core/src/main/java/org/apache/wicket/markup/renderStrategy/AbstractHeaderRenderStrategy.java`
#### Snippet
```java
	 * @param rootComponent
	 */
	abstract protected void renderChildHeaders(final HtmlHeaderContainer headerContainer,
		final Component rootComponent);

```

### MissortedModifiers
Missorted modifiers `synchronized final`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/DynamicImageResource.java`
#### Snippet
```java
	 *            The format (jpg, png or gif..)
	 */
	public synchronized final void setFormat(String format)
	{
		Args.notNull(format, "format");
```

### MissortedModifiers
Missorted modifiers `synchronized final`
in `wicket-core/src/main/java/org/apache/wicket/request/resource/DynamicImageResource.java`
#### Snippet
```java
	 * @return Returns the image format.
	 */
	public synchronized final String getFormat()
	{
		return format;
```

### MissortedModifiers
Missorted modifiers `final static`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/SecuredRemoteAddressRequestWrapperFactory.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(SecuredRemoteAddressRequestWrapperFactory.class);

	private final static String SECURED_REMOTE_ADDRESSES_PARAMETER = "securedRemoteAddresses";

	public static class Config
```

### MissortedModifiers
Missorted modifiers `static public`
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/EditBook.java`
#### Snippet
```java
	 * @author Jonathan Locke
	 */
	static public final class EditBookForm extends Form<Book>
	{
		/**
```

## RuleId[id=AnonymousHasLambdaAlternative]
### AnonymousHasLambdaAlternative
Anonymous new ThreadLocal\<\>() can be replaced with ThreadLocal.withInitial()
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AbstractPersistentPageStore.java`
#### Snippet
```java
	private static final ConcurrentMap<String, AbstractPersistentPageStore> STORES = new ConcurrentHashMap<>();

	private static final ThreadLocal<Boolean> gettingSessionAttribute = new ThreadLocal<>()
	{
		protected Boolean initialValue()
		{
```

## RuleId[id=IfStatementMissingBreakInLoop]
### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java
		for (LocalizedImageResource localizedImageResource : localizedImageResources)
		{
			if (localizedImageResource.isStateless())
			{
				statelessList = true;
```

## RuleId[id=InstanceofIncompatibleInterface]
### InstanceofIncompatibleInterface
'instanceof' incompatible interface `IAjaxIndicatorAware`
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
		}

		if (this instanceof IAjaxIndicatorAware)
		{
			return ((IAjaxIndicatorAware)this).getAjaxIndicatorMarkupId();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `IAjaxRegionMarkupIdProvider`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		if (markupId == null)
		{
			if (this instanceof IAjaxRegionMarkupIdProvider)
			{
				markupId = ((IAjaxRegionMarkupIdProvider)this).getAjaxRegionMarkupId(this);
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `ISessionLogInfo`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	private Object getSessionInfo(Session session)
	{
		if (session instanceof ISessionLogInfo)
		{
			return ((ISessionLogInfo)session).getSessionInfo();
```

## RuleId[id=FunctionalExpressionCanBeFolded]
### FunctionalExpressionCanBeFolded
Lambda can be replaced with call qualifier
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
	{
		SessionData data = context.getSessionData(getKey(), create ? () -> {
			return dataCreator.get();
		} : null);

```

## RuleId[id=WhileLoopSpinsOnField]
### WhileLoopSpinsOnField
`while` loop spins on field
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java
					try
					{
						while (!stop)
						{
							// Get the start of the current period
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `wicket-util/src/main/java/org/apache/wicket/util/license/AbstractLicenseHeaderHandler.java`
#### Snippet
```java
			catch (IOException e)
			{
				throw new AssertionError(e.getMessage());
			}
		}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
			catch (IOException e)
			{
				throw new WicketRuntimeException("Cannot close markup resource stream: " +
					markupResourceStream, e);
			}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
				catch (ResponseIOException e)
				{
					throw e.getCause();
				}
			}
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	public AppendingStringBuffer append(final boolean b)
	{
		if (b)
		{
			int newcount = count + 4;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `wicket-util/src/main/java/org/apache/wicket/util/encoding/UrlDecoder.java`
#### Snippet
```java
			if (ch == '%')
			{
				if (i + 2 < length)
				{
					final char hex1 = source.charAt(i + 1);
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/aspects/WicketFilterInitAspect.java`
#### Snippet
```java
		Object[] args = joinPoint.getArgs();
		String filterName = null;
		if (args.length == 2)
		{
			FilterConfig filterConfig = (FilterConfig)args[1];
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java
		{
			int currentDigit = creditCardNumber.charAt(creditCardNumber.length() - i) - nulOffset;
			if ((i % 2) == 0)
			{
				currentDigit *= 2;
```

## RuleId[id=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
	public Bytes freeDiskSpace()
	{
		return Bytes.bytes(super.getFreeSpace());
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
		if (behavior == null)
		{
			super.add(behavior = newAutoCompleteBehavior(renderer, settings));
		}
	}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/jaksrvlt/JakSrvltFileUpload.java`
#### Snippet
```java
    public FileItemIterator getItemIterator(final HttpServletRequest request)
    throws FileUploadException, IOException {
        return super.getItemIterator(new JakSrvltRequestContext(request));
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	public final void internalInitialize()
	{
		super.fireInitialize();
		visitChildren(new IVisitor<Component, Void>()
		{
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupParser.java`
#### Snippet
```java
			}

			super.add(index, filter);
			return true;
		}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
		{
			// add the key to the hash map. Do not replace existing once
			Boolean rtn = super.putIfAbsent(key, value);

			// If found, than remove it from the fifo list and ...
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResourceReference.java`
#### Snippet
```java
	protected String getMinifiedName()
	{
		String name = super.getName();
		return ResourceUtils.getMinifiedName(name, ResourceUtils.MIN_POSTFIX_DEFAULT);
	}
```

## RuleId[id=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/InspectorPage.java`
#### Snippet
```java
		
		IModel<Page> page = () -> {
			return reference.getPage();
		};
		
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxSelector.java`
#### Snippet
```java
		List<CheckBox> checkBoxes = new ArrayList<>();

		container.<CheckBox, Void> visitChildren(CheckBox.class, (child, visit) -> {
			checkBoxes.add(child);
		});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

		// update auto labels if we are inside an ajax request
		getRequestCycle().find(AjaxRequestTarget.class).ifPresent(target -> {
			visitChildren(FormComponent.class, new IVisitor<FormComponent<?>, Void>()
			{
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java
	{
		return context.getSessionData(KEY, create ? () -> {
			return new SessionData();
		} : null);
	}
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
	{
		SessionData data = context.getSessionData(getKey(), create ? () -> {
			return dataCreator.get();
		} : null);

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AbstractPersistentPageStore.java`
#### Snippet
```java
			
			SessionAttribute attribute = context.getSessionAttribute(key, create ? () -> {
				return new SessionAttribute(storeKey, createSessionIdentifier(context));
			} : null);
			
```

## RuleId[id=EmptyMethod]
### EmptyMethod
Method only calls its super
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/LimitedInputStream.java`
#### Snippet
```java
     */
    @Override
    public void close() throws IOException {
        super.close();
    }
```

### EmptyMethod
Method only calls its super
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	 */
	@Override
	public void detachModels()
	{
		super.detachModels();
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	 * {@link Application#get(String)}. There is no way to get a reference to a RequestCycle</p>
	 */
	public void onInvalidate()
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
	 *             thrown if an error occurs while modifying {@literal attributesJson} argument
	 */
	protected void postprocessConfiguration(JSONObject attributesJson, Component component)
		throws JSONException
	{
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
	}

	protected void onAfterSubmit(final AjaxRequestTarget target)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
	}

	protected void updateAjaxAttributes(final AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxButton.java`
#### Snippet
```java
	}

	protected void updateAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxCheckBox.java`
#### Snippet
```java
	 * @see org.apache.wicket.ajax.AbstractDefaultAjaxBehavior#updateAjaxAttributes(org.apache.wicket.ajax.attributes.AjaxRequestAttributes)
	 */
	protected void updateAjaxAttributes(final AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
	}

	protected void updateAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxSubmitLink.java`
#### Snippet
```java
	 * @param target the {@link AjaxRequestTarget}
	 */
	protected void onAfterSubmit(AjaxRequestTarget target)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxSubmitLink.java`
#### Snippet
```java
	}

	protected void updateAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationIncrementLink.java`
#### Snippet
```java
	}

	protected void updateAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationLink.java`
#### Snippet
```java
	}

	protected void updateAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/behavior/AbstractAjaxBehavior.java`
#### Snippet
```java
	 * this method to do any cleaning up of temporary state
	 */
	protected void onComponentRendered()
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/core/util/watch/Nio2ModificationWatcher.java`
#### Snippet
```java
	 *              a logger that can be used to log the events
	 */
	protected void entryModified(Path path, Logger log)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/core/util/watch/Nio2ModificationWatcher.java`
#### Snippet
```java
	 *              a logger that can be used to log the events
	 */
	protected void entryDeleted(Path path, Logger log)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
	 *            The current markup stream
	 */
	public void onBeforeRender(final Component component, final MarkupStream markupStream)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/markup/HtmlSpecialTag.java`
#### Snippet
```java
	 *            The current amrkup stream
	 */
	public void onBeforeRender(final Component component, final MarkupStream markupStream)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	 * actual updating of form component models is done.
	 */
	protected void beforeUpdateFormComponentModels()
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 * Handle invalidation
	 */
	protected void onInvalid()
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 * Handle validation
	 */
	protected void onValid()
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentUpdatingBehavior.java`
#### Snippet
```java
	 * component by calling {@link #getFormComponent()}.
	 */
	protected void onBind()
	{
	}
```

### EmptyMethod
Empty method overrides empty method
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java
	 */
	@Override
	public void onComponentTagBody(final MarkupStream markupStream, final ComponentTag openTag)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
	 * @param item
	 */
	protected void onBeginPopulateItem(final ListItem<T> item)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentDetachableModel.java`
#### Snippet
```java
	 * the model object.
	 */
	protected void attach()
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentDetachableModel.java`
#### Snippet
```java
	 *            The new model object
	 */
	protected void setObject(Component component, T object)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentModel.java`
#### Snippet
```java
	 *            The object that will be set in the model.
	 */
	protected void setObject(Component component, T object)
	{
	}
```

### EmptyMethod
All implementations of this method are empty
in `wicket-core/src/main/java/org/apache/wicket/page/IManageablePage.java`
#### Snippet
```java
	 * re-attached later.
	 */
	void detach();

	/**
```

### EmptyMethod
All implementations of this method are empty
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
	 *            the object being removed
	 */
	void objectRemoved(Object value);

	/**
```

### EmptyMethod
All implementations of this method are empty
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
	 *            the object being updated
	 */
	void objectUpdated(Object value);

	/**
```

### EmptyMethod
All implementations of this method are empty
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
	 *            the object being created/added
	 */
	void objectCreated(Object value);

	/**
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
	 * Called when the request cycle object is beginning its response
	 */
	protected void onBeginRequest()
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/request/resource/BaseDataResource.java`
#### Snippet
```java
	 *            The request attributes (web request, web response, parameters)
	 */
	protected void configureResponse(final ResourceResponse response, final Attributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/session/HttpSessionStore.java`
#### Snippet
```java
	 *            The new session
	 */
	protected void onBind(final Request request, final Session newSession)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-core/src/main/java/org/apache/wicket/session/HttpSessionStore.java`
#### Snippet
```java
	 *            The session id of the session that was invalidated.
	 */
	protected void onUnbind(final String sessionId)
	{
	}
```

### EmptyMethod
Empty method overrides empty method
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AutoCompletePage.java`
#### Snippet
```java

			@Override
			protected void onError(AjaxRequestTarget target)
			{
			}
```

### EmptyMethod
Empty method overrides empty method
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/GuestBook.java`
#### Snippet
```java

			@Override
			protected void onError(AjaxRequestTarget target)
			{
			}
```

### EmptyMethod
Empty method overrides empty method
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java

					@Override
					protected void onError(AjaxRequestTarget target)
					{
					}
```

### EmptyMethod
Empty method overrides empty method
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java

					@Override
					protected void onError(AjaxRequestTarget target)
					{
					}
```

### EmptyMethod
Empty method overrides empty method
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/AjaxStatelessExample.java`
#### Snippet
```java
		{
			@Override
			protected void onSubmit()
			{
			}
```

### EmptyMethod
Empty method overrides empty method
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/AdvancedTreePage.java`
#### Snippet
```java
		{
			@Override
			public void onSubmit()
			{
			}
```

### EmptyMethod
The method is empty
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/aspects/session/SessionCountListener.java`
#### Snippet
```java
	 * @param event the http session event
	 */
	public void dec(HttpSessionEvent event)
	{
		// NOOP for aspect usage
```

### EmptyMethod
The method is empty
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/aspects/session/SessionCountListener.java`
#### Snippet
```java
	 * @param event the http session event
	 */
	public void inc(HttpSessionEvent event)
	{
		// NOOP for aspect usage
```

### EmptyMethod
Method only calls its super
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
	 */
	@Override
	protected void onModelChanged()
	{
		super.onModelChanged();
```

### EmptyMethod
Method only calls its super
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
	 */
	@Override
	protected void onModelChanging()
	{
		super.onModelChanging();
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
	 *            The Ajax attributes to modify
	 */
	protected void updateLabelAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
	 *            The Ajax attributes to modify
	 */
	protected void updateEditorAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
Method only calls its super
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
	 */
	@Override
	protected void onModelChanged()
	{
		super.onModelChanged();
```

### EmptyMethod
Method only calls its super
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
	 */
	@Override
	protected void onModelChanging()
	{
		super.onModelChanging();
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
	 *            optional Ajax request handler
	 */
	protected void onContentLoaded(T content, Optional<AjaxRequestTarget> target)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
	}

	protected void updateAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
Empty method overrides empty method
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/sort/AjaxFallbackOrderByBorder.java`
#### Snippet
```java
	 */
	@Override
	protected void onSortChanged()
	{
		// noop
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/table/AjaxFallbackHeadersToolbar.java`
#### Snippet
```java
	}

	protected void updateAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/tabs/AjaxTabbedPanel.java`
#### Snippet
```java
	 *            ajax target used to update this component
	 */
	protected void onAjaxUpdate(final Optional<AjaxRequestTarget> target)
	{
	}
```

### EmptyMethod
All implementations of this method are empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/IBreadCrumbModelListener.java`
#### Snippet
```java
	 *            The new bread crumb
	 */
	default void breadCrumbAdded(IBreadCrumbParticipant breadCrumbParticipant)
	{}

```

### EmptyMethod
All implementations of this method are empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/IBreadCrumbModelListener.java`
#### Snippet
```java
	 *            The bread crumb that was removed
	 */
	default void breadCrumbRemoved(IBreadCrumbParticipant breadCrumbParticipant)
	{}
}
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
	 * Event listener for page-changed event
	 */
	protected void onPageChanged()
	{
		// noop
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/GoFilter.java`
#### Snippet
```java
	 * 
	 */
	protected void onGoSubmit(final Button button)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/AjaxWizardButtonBar.java`
#### Snippet
```java
	 *            Ajax attributes
	 */
	protected void updateAjaxAttributes(AjaxRequestAttributes attributes)
	{
	}
```

### EmptyMethod
All implementations of this method are empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/IWizardStep.java`
#### Snippet
```java
	 * @see IWizardModel#finish() 
	 */
	void applyState();

	/**
```

### EmptyMethod
The method is empty
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	 * @see #init(IWizardModel)
	 */
	protected void onInit(final IWizardModel wizardModel)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
	 *          the close message with the info about the client
	 */
	protected void onClose(ClosedMessage message)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
	 *          The error message that that brings information about the communication error
	 */
	protected void onError(WebSocketRequestHandler handler, ErrorMessage message)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
	 *          the aborted message with the info about the client
	 */
	protected void onAbort(AbortedMessage message) {
	}

```

### EmptyMethod
The method is empty
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
	 *          The message pushed/broadcasted by the server
	 */
	protected void onPush(WebSocketRequestHandler handler, IWebSocketPushMessage message)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
	 *          The error message that that brings information about the communication error
	 */
	protected void onError(WebSocketRequestHandler handler, ErrorMessage message)
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringWebApplicationFactory.java`
#### Snippet
```java
	 *            wicket filter
	 */
	protected void postProcessWebApplicationContext(final ConfigurableWebApplicationContext wac,
		final WicketFilter filter)
	{
```

### EmptyMethod
The method is empty
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	 * knowledge of subclasses.)
	 */
	void init()
	{
	}
```

### EmptyMethod
The method is empty
in `wicket-util/src/main/java/org/apache/wicket/util/io/ProxyInputStream.java`
#### Snippet
```java
     * @throws IOException if the pre-processing fails
     */
    protected void beforeRead(final int n) throws IOException {
    }

```

### EmptyMethod
The method is empty
in `wicket-util/src/main/java/org/apache/wicket/util/io/ProxyInputStream.java`
#### Snippet
```java
     * @throws IOException if the post-processing fails
     */
    protected void afterRead(final int n) throws IOException {
    }

```

## RuleId[id=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/LazyInitializer.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private transient volatile T instance = null;

	@Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceProxyTargetLocator.java`
#### Snippet
```java
	private final String fieldName;

	private Boolean isSingletonCache = null;

	public GuiceProxyTargetLocator(final Field field, final Annotation bindingAnnotation,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java

	/** the <code>log</code> to give to the user's code */
	private Logger log = null;

	/** the name of this <code>Task</code> */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java

	/** <code>true</code> if the task's thread has already started executing */
	private boolean isStarted = false;

	/** the <code>log</code> to give to the user's code */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
	 * <code>true</code> if this <code>ValueMap</code> has been made immutable.
	 */
	private boolean immutable = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
		protected final int growBy;

		protected int size = 0;

		protected String[] names;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java

	/** The last time this stylesheet was modified */
	private Instant lastModified = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
	protected List<String> velocityIgnore = Generics.newArrayList();
	protected List<String> javaScriptIgnore = Generics.newArrayList();
	protected boolean addHeaders = false;
	private ILicenseHeaderHandler[] licenseHeaderHandlers;
	private File baseDirectory = new File("").getAbsoluteFile();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/Initializer.java`
#### Snippet
```java
	// It's best to store a reference to the MBeanServer rather than getting it
	// over and over
	private MBeanServer mbeanServer = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-objectsizeof-agent/src/main/java/org/apache/wicket/util/instrument/InstrumentationObjectSizeOfStrategy.java`
#### Snippet
```java
	{

		private long totalSize = 0;

		/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java
					int keyIndex = -1;

					int index = 0;
				};
			}
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
public class IntHashMap<V> implements Cloneable, Serializable
{
	transient volatile Set<Integer> keySet = null;

	transient volatile Collection<V> values = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	// Views

	private transient Set<Entry<V>> entrySet = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	transient volatile Set<Integer> keySet = null;

	transient volatile Collection<V> values = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
	private ISpringContextLocator springContextLocator;

	private Boolean singletonCache = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-request/src/main/java/org/apache/wicket/request/RequestHandlerExecutor.java`
#### Snippet
```java
	private final List<IRequestHandler> inactiveRequestHandlers = new ArrayList<>();

	private IRequestHandler scheduledAfterCurrent = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java

		private HashSet<List<Integer>> set = new HashSet<>();
		private boolean reversed = false;

		public void expandAll()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteSettings.java`
#### Snippet
```java
	private boolean showListOnFocusGain = false;

	private boolean showCompleteListOnFocusGain = false;

	private int throttleDelay = 300;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteSettings.java`
#### Snippet
```java
	private int maxHeightInPx = -1;

	private boolean showListOnEmptyInput = false;

	private boolean useSmartPositioning = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteSettings.java`
#### Snippet
```java
	private boolean showListOnEmptyInput = false;

	private boolean useSmartPositioning = false;

	private boolean ignoreBordersWhenPositioning = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteSettings.java`
#### Snippet
```java
	private boolean adjustInputWidth = true;

	private boolean showListOnFocusGain = false;

	private boolean showCompleteListOnFocusGain = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteSettings.java`
#### Snippet
```java
	private boolean ignoreBordersWhenPositioning = true;

	private String cssClassName = null;

	private boolean adjustInputWidth = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteSettings.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private boolean preselect = false;

	private int maxHeightInPx = -1;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
	 * The date pattern of the text field
	 */
	private String datePattern = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/SelectOptions.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(SelectOptions.class);

	private boolean recreateChoices = false;

	private final IOptionRenderer<T> renderer;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/AbstractWizardModel.java`
#### Snippet
```java

	/** Whether the last button should be shown at all; false by default. */
	private boolean lastVisible = false;

	/** Listeners for {@link IWizardModelListener model events}. */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/** The currently active participant, if any (possibly null). */
	private IBreadCrumbParticipant activeParticipant = null;

	/** Holds the current list of crumbs. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java
		private transient boolean dirty = false;

		private transient int size = 0;

		/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java
		private static final long serialVersionUID = 1L;

		private transient boolean dirty = false;

		private transient int size = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
		 * Image data array
		 */
		private transient SoftReference<byte[]> data = null;

		@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/ParameterParser.java`
#### Snippet
```java
     * Current position in the string.
     */
    private int pos = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/ParameterParser.java`
#### Snippet
```java
     * String to be parsed.
     */
    private char[] chars = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/ParameterParser.java`
#### Snippet
```java
     * Start of a token.
     */
    private int i1 = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/ParameterParser.java`
#### Snippet
```java
     * End of a token.
     */
    private int i2 = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/ParameterParser.java`
#### Snippet
```java
     * Maximum position in the string.
     */
    private int len = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/ParameterParser.java`
#### Snippet
```java
     * Whether names stored in the map should be converted to lower case.
     */
    private boolean lowerCaseNames = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ComponentQueue.java`
#### Snippet
```java

	private Component[] queue;
	private int queueSize = 0;
	private boolean dirty = false;
	private String[] seen;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ComponentQueue.java`
#### Snippet
```java
	private Component[] queue;
	private int queueSize = 0;
	private boolean dirty = false;
	private String[] seen;
	private int seenSize = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ComponentQueue.java`
#### Snippet
```java
	private boolean dirty = false;
	private String[] seen;
	private int seenSize = 0;

	void add(Component... components)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/csp/CSPHeaderConfiguration.java`
#### Snippet
```java
	private boolean nonceEnabled = false;

	private String reportUriMountPath = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/csp/CSPHeaderConfiguration.java`
#### Snippet
```java
	private final Map<CSPDirective, List<CSPRenderable>> directives = new EnumMap<>(CSPDirective.class);

	private boolean addLegacyHeaders = false;

	private boolean nonceEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/csp/CSPHeaderConfiguration.java`
#### Snippet
```java
	private boolean addLegacyHeaders = false;

	private boolean nonceEnabled = false;

	private String reportUriMountPath = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	 * urlFor
	 */
	private transient Boolean stateless = null;

	/** Page parameters used to construct this page */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	 * @see IRequestablePage#getRenderCount()
	 */
	private int renderCount = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java

	/** cached id because you can't access the id after session unbound */
	private volatile String id = null;

	/** The locale to use when loading resources for this session. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java

	/** True if locale's language is RTL (right-to-left) */
	private boolean rtlLocale = false;

	/** Session level meta data. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java

	/** True if session state has been changed */
	private transient volatile boolean dirty = false;

	/** feedback messages */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java

	/** a set of listeners */
	private Set<AjaxRequestTarget.IListener> listeners = null;

	/** */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractAjaxTimerBehavior.java`
#### Snippet
```java
	private Duration updateInterval;

	private boolean stopped = false;
	
	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
	 * A flag indicating whether this behavior has been rendered at least once
	 */
	private boolean hasBeenRendered = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxRequestAttributes.java`
#### Snippet
```java
	private Duration requestTimeout;

	private boolean preventDefault = false;

	private EventPropagation eventPropagation = EventPropagation.BUBBLE;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxRequestAttributes.java`
#### Snippet
```java
	public static final String XML_DATA_TYPE = "xml";

	private boolean multipart = false;

	private Method method = Method.GET;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/LocaleResourceNameIterator.java`
#### Snippet
```java

	/** Internal state */
	private int state = 0;

	private final boolean strict;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java

	/** Internal state */
	private int state = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
				extObj.writeExternal(new ObjectOutputAdaptor()
				{
					private int count = 0;

					@Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			private int expectedModCounter = -1;
			private Component currentComponent = null;
			private Iterator<Component> internalIterator = null;

			@Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			private int indexInRemovalsSinceLastUpdate;
			private int expectedModCounter = -1;
			private Component currentComponent = null;
			private Iterator<Component> internalIterator = null;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	 * container can keep track when they need to change their iteration strategy.
	 */
	private transient int modCounter = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
	 * Whether or not to mark encrypted URLs as encrypted.
	 */
	private boolean markEncryptedUrls = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
		private char[] characters;

		private int hash = 0;

		public HashedSegmentGenerator(String string)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/page/PageAccessSynchronizer.java`
#### Snippet
```java
		private final Thread thread;

		private volatile boolean released = false;

		/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/util/tester/ComponentInPage.java`
#### Snippet
```java
	 * @see {@link BaseWicketTester#startComponentInPage(Class, IMarkupFragment)}.
	 */
	boolean isInstantiated = false;
}

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * end of the request the value of this variable is reset to 0
	 */
	private transient short requestFlags = 0;

	/** Component id. */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * <p>
	 */
	Object data = null;

	final int data_start()
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
	 * An auto incrementing index used as a suffix for MultiFileUploadField's inputName
	 */
	private int multiFileUploadIndex = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
		private final class SearchOptionByIndexVisitor implements IVisitor<Component, Component>
		{
			int count = 0;

			private final int index;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
	 * test.
	 */
	private boolean closed = false;

	/** path to <code>FormComponent</code> */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java

	// whether a header contribution is being rendered
	private boolean headerRendering = false;

	private IHeaderResponse headerResponse;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
	protected final ResponseBuffer headerBuffer;

	protected HtmlHeaderContainer header = null;
	
	private Component originalHeaderContainer;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentDetachableModel.java`
#### Snippet
```java
	 * we allow 'null' model values.
	 */
	private transient boolean attached = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/HtmlSpecialTag.java`
#### Snippet
```java

	/** Boolean flags. See above */
	private int flags = 0;

	private final HttpTagType httpTagType;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFragment.java`
#### Snippet
```java
	{
		return new Iterator<MarkupElement>() {
			int index = 0;

			@Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
	 * information however is required.
	 */
	private WeakReference<Class<? extends Component>> markupClassRef = null;

	/** added behaviors */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/** Boolean flags. See above */
	private int flags = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java

	/** The markup resource stream provider used by MarkupCache */
	private IMarkupResourceStreamProvider markupResourceStreamProvider = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFactory.java`
#### Snippet
```java

	/** A markup cache */
	private IMarkupCache markupCache = null;

	/** The markup resource stream provider used by MarkupCache */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java

	/** Current index in markup stream */
	private int currentIndex = 0;

	/** The markup element list */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/PackageResourceGuard.java`
#### Snippet
```java
	private Set<String> blockedFiles = new HashSet<>(4);

	private boolean allowAccessToRootResources = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	public static class StartComponentInPage extends WebPage
	{
		private transient IMarkupFragment pageMarkup = null;

		/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/LabeledWebMarkupContainer.java`
#### Snippet
```java
	 * have any specific meaning to FormComponent itself.
	 */
	private IModel<String> labelModel = null;

	@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java

	/** whether or not null will be offered as a choice once a nonnull value is saved */
	private boolean nullValid = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

	/** True if the form has enctype of multipart/form-data */
	private short multiPart = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	 * {@link org.apache.wicket.settings.ApplicationSettings#getDefaultMaximumUploadSize()} is used.
	 */
	private Bytes maxSize = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/FileUploadToResourceField.java`
#### Snippet
```java
     * {@link org.apache.wicket.settings.ApplicationSettings#getDefaultMaximumUploadSize()} is used.
     */
    private Bytes maxSize = null;

    public FileUploadToResourceField(String id)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/MultiFileUploadField.java`
#### Snippet
```java
	private final boolean useMultipleAttr;

	private transient String[] inputArrayCache = null;


```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/PopupSettings.java`
#### Snippet
```java
	 * rather than opening a second window.
	 */
	private String windowName = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
	 * that opens a new window using the popup properties.
	 */
	private PopupSettings popupSettings = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
	 * True if link should automatically enable/disable based on current page; false by default.
	 */
	private boolean autoEnable = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java
		return new ReadOnlyIterator<Component>()
		{
			private int index = 0;

			@Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
	 * that opens a new window using the popup properties.
	 */
	private PopupSettings popupSettings = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private boolean contextRelative = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private String media = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java
	 * Cross origin settings
	 */
	private CrossOrigin crossOrigin = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/** The sizes of the responsive images */
	private List<String> sizes = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/** The x values to be used within the srcset */
	private List<String> xValues = null;

	/** The sizes of the responsive images */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalSource.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private String media = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

	/** Index of the first item to show */
	private int firstIndex = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
	 * work properly.
	 */
	private boolean reuseItems = false;

	/** Max number (not index) of items to show */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
		return new ReadOnlyIterator<Component>()
		{
			private int index = 0;

			@Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java
	 * Header response that is responsible for filtering duplicate contributions.
	 */
	private transient IHeaderResponse headerResponse = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigation.java`
#### Snippet
```java

	/** Default separator between page numbers. Null: no separator. */
	private String separator = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java

	/** True if all the rest of the markup file can be ignored */
	private boolean ignoreTheRest = false;

	/** The Markup available so far for the resource */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java

	/** True if &lt;head&gt; has been found already */
	private boolean foundHead = false;

	/** True if &lt;/head&gt; has been found already */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java

	/** True if &lt;/wicket:header-items&gt; has been found already */
	private boolean foundHeaderItemsTag = false;

	/** True if all the rest of the markup file can be ignored */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java

	/** True if &lt;/head&gt; has been found already */
	private boolean foundClosingHead = false;

	/** True if &lt;/wicket:header-items&gt; has been found already */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalImage.java`
#### Snippet
```java

	/** The x values to be used within the srcset */
	private List<String> xValues = null;

	/** The sizes of the responsive images */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalImage.java`
#### Snippet
```java

	/** The sizes of the responsive images */
	private List<String> sizes = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalImage.java`
#### Snippet
```java
	 * Cross origin settings
	 */
	private CrossOrigin crossOrigin = null;

	private IModel<List<Serializable>> srcSetModel;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HeadForceTagIdHandler.java`
#### Snippet
```java

	/** Unique value per markup file */
	private int counter = 0;

	/** we are in wicket:head */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HeadForceTagIdHandler.java`
#### Snippet
```java

	/** we are in wicket:head */
	private boolean inHead = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/OpenCloseTagExpander.java`
#### Snippet
```java

	// temporary storage. Introduce into flow on next request
	private ComponentTag next = null;

	@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/RepeatingView.java`
#### Snippet
```java

	/** Counter used for generating unique child component ids. */
	private long childIdCounter = 0;

	/** @see Component#Component(String) */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ArrayIteratorAdapter.java`
#### Snippet
```java
{
	private final T[] array;
	private int pos = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/DefaultItemReuseStrategy.java`
#### Snippet
```java
		return new Iterator<Item<T>>()
		{
			private int index = 0;

			@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/ReuseIfModelsEqualStrategy.java`
#### Snippet
```java
		return new Iterator<Item<T>>()
		{
			private int index = 0;

			@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/UrlResourceReference.java`
#### Snippet
```java
	private final Url url;

	private boolean contextRelative = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/MetaInfStaticResourceReference.java`
#### Snippet
```java
	private static final long serialVersionUID = -1858339228780709471L;

	private static Boolean META_INF_RESOURCES_SUPPORTED = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PartWriterCallback.java`
#### Snippet
```java
	 * If the given input stream is going to be closed
	 */
	private boolean close = false;


```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		private ContentDisposition contentDisposition = ContentDisposition.INLINE;
		private String contentType = null;
		private String contentRange = null;
		private ContentRangeType contentRangeType = null;
		private String textEncoding;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		private String contentType = null;
		private String contentRange = null;
		private ContentRangeType contentRangeType = null;
		private String textEncoding;
		private long contentLength = -1;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		private String textEncoding;
		private long contentLength = -1;
		private Instant lastModified = null;
		private WriteCallback writeCallback;
		private Duration cacheDuration;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		private String fileName = null;
		private ContentDisposition contentDisposition = ContentDisposition.INLINE;
		private String contentType = null;
		private String contentRange = null;
		private ContentRangeType contentRangeType = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		private Integer statusCode;
		private String errorMessage;
		private String fileName = null;
		private ContentDisposition contentDisposition = ContentDisposition.INLINE;
		private String contentType = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	 * image). Specializations of this class should change this flag appropriately.
	 */
	private boolean compress = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	 * text encoding (may be null) - only makes sense for character-based resources
	 */
	private String textEncoding = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessage.java`
#### Snippet
```java

	/** Whether or not this message has been rendered */
	private boolean rendered = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpSession.java`
#### Snippet
```java
	private String id = generateSessionId();

	private long lastAccessedTime = 0;

	private boolean temporary = true;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	 * slot or if the buffer has been filled completely to the oldest request in the buffer.
	 */
	private int indexInWindow = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0l` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	 * calculate the average request time across the window duration.
	 */
	private long totalRequestTime = 0l;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
	 * A flag indicating whether WicketFilter is used directly or through WicketServlet
	 */
	private boolean isServlet = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
	private final ValueMap headers = new ValueMap();

	private Locale locale = null;

	private int mode = MODE_NONE;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
	private PrintWriter printWriter;

	private String redirectLocation = null;

	private ServletOutputStream servletStream;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
	private final List<Cookie> cookies = new ArrayList<Cookie>();

	private String errorMessage = null;

	private final ValueMap headers = new ValueMap();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebRequest.java`
#### Snippet
```java
	}

	private Map<String, List<StringValue>> postParameters = null;

	protected Map<String, List<StringValue>> generatePostParameters()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ServletWebResponse.java`
#### Snippet
```java
	private final ServletWebRequest webRequest;

	private boolean redirect = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapperFactory.java`
#### Snippet
```java

		/** @see #setProtocolHeader(String) */
		private String protocolHeader = null;

		/** @see #setProtocolHeaderSslValue(String) */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	private boolean useMultiPartContentType;

	private boolean secure = false;

	private String remoteAddr = "127.0.0.1";
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		return new ServletInputStream()
		{
			private boolean isFinished = false;
			private boolean isReady = true;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/RequestLoggerSettings.java`
#### Snippet
```java
	private boolean recordSessionSize = false;

	private int requestsWindowSize = 0;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/RequestLoggerSettings.java`
#### Snippet
```java
	private boolean requestLoggerEnabled = false;

	private boolean recordSessionSize = false;

	private int requestsWindowSize = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/RequestLoggerSettings.java`
#### Snippet
```java
public class RequestLoggerSettings
{
	private boolean requestLoggerEnabled = false;

	private boolean recordSessionSize = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/SecuritySettings.java`
#### Snippet
```java
	 * This setting basically disables {@link org.apache.wicket.core.request.mapper.BookmarkableMapper}
	 */
	private boolean enforceMounts = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/FrameworkSettings.java`
#### Snippet
```java
	private IDetachListener detachListener;

	private List<IEventDispatcher> eventDispatchers = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/RequestCycleSettings.java`
#### Snippet
```java
	 * capabilities}. False by default.
	 */
	private boolean gatherExtendedBrowserInfo = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/PageSettings.java`
#### Snippet
```java
	 * when its owning page is freshly created after expiration
	 */
	private boolean callListenerAfterExpiry = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/MarkupSettings.java`
#### Snippet
```java

	/** True if multiple tabs/spaces should be compressed to a single space */
	private boolean compressWhitespace = false;

	/** Default markup encoding. If null, the OS default will be used */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/MarkupSettings.java`
#### Snippet
```java
{
	/** Application default for automatically resolving hrefs */
	private boolean automaticLinking = false;

	/** True if multiple tabs/spaces should be compressed to a single space */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/MarkupSettings.java`
#### Snippet
```java
	 * If true, wicket tags ( <wicket: ..>) and wicket:id attributes we be removed from output
	 */
	private boolean stripWicketTags = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/MarkupSettings.java`
#### Snippet
```java

	/** Should HTML comments be stripped during rendering? */
	private boolean stripComments = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/MarkupSettings.java`
#### Snippet
```java

	/** if true than throw an exception if the xml declaration is missing from the markup file */
	private boolean throwExceptionOnMissingXmlDeclaration = false;

	/** Should HTML comments be stripped during rendering? */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/DebugSettings.java`
#### Snippet
```java
	private boolean outputMarkupContainerClassName = false;

	private String componentPathAttributeName = null;

	private boolean developmentUtilitiesEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/DebugSettings.java`
#### Snippet
```java
	 * whether wicket should track line precise instantiations of components for error reporting.
	 */
	private boolean linePreciseReportingOnNewComponentEnabled = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/DebugSettings.java`
#### Snippet
```java
	 * whether wicket should track line precise additions of components for error reporting.
	 */
	private boolean linePreciseReportingOnAddComponentEnabled = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/DebugSettings.java`
#### Snippet
```java
{
	/** ajax debug mode status */
	private boolean ajaxDebugModeEnabled = false;

	/** True to check that each component on a page is used */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/DebugSettings.java`
#### Snippet
```java
	private String componentPathAttributeName = null;

	private boolean developmentUtilitiesEnabled = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/DebugSettings.java`
#### Snippet
```java
	 * Whether the container's class name should be printed to response (in a html comment).
	 */
	private boolean outputMarkupContainerClassName = false;

	private String componentPathAttributeName = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
		false);

	private boolean encodeJSessionId = false;
	
	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java

	/** Frequency at which files should be polled */
	private Duration resourcePollFrequency = null;

	/** resource locator for this application */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/StoreSettings.java`
#### Snippet
```java
	private boolean asynchronous = true;
	
	private boolean encrypted = false;
	
	private Supplier<ICrypter> crypter = DefaultCrypter::new;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/StoreSettings.java`
#### Snippet
```java
	private Bytes maxSizePerSession = DEFAULT_MAX_SIZE_PER_SESSION;

	private File fileStoreFolder = null;

	private int asynchronousQueueCapacity = DEFAULT_ASYNCHRONOUS_QUEUE_CAPACITY;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/settings/ApplicationSettings.java`
#### Snippet
```java
	private Bytes defaultMaximumUploadSize = Bytes.MAX;

	private boolean uploadProgressUpdatesEnabled = false;

	private IFeedbackMessageFilter feedbackMessageCleanupFilter = new DefaultCleanupFeedbackMessageFilter();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java
	 * Is a group of a page stable.
	 */
	private boolean stableGroups = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/PageWindowManager.java`
#### Snippet
```java
	 * Inversed index of #idToWindowIndex
	 */
	private IntHashMap<Integer> windowIndexToPageId = null;

	/** index of last added page */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/PageWindowManager.java`
#### Snippet
```java
	private int indexPointer = -1;

	private int totalSize = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/PageWindowManager.java`
#### Snippet
```java
	 * indexes (e.g. items are shifted on page window removal)
	 */
	private IntHashMap<Integer> idToWindowIndex = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AsynchronousPageStore.java`
#### Snippet
```java
		 * Is this context passed to an asynchronously called {@link IPageStore#addPage(IPageContext, IManageablePage)}.
		 */
		private boolean asynchronous = false;

		/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/PatternValidator.java`
#### Snippet
```java

	/** whether to exclude matching input **/
	private boolean reverse = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
	 * Holds the set of current validation options.
	 */
	private long options = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/application/WildcardMatcherHelper.java`
#### Snippet
```java

		/** map index */
		private int idx = 0;

		/** index into pattern */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/application/WildcardMatcherHelper.java`
#### Snippet
```java

		/** index into string */
		private int istr = 0;

		// ~ Constructors
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-core/src/main/java/org/apache/wicket/application/WildcardMatcherHelper.java`
#### Snippet
```java

		/** index into pattern */
		private int ipat = 0;

		/** index into string */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AutoCompletePage.java`
#### Snippet
```java
		final IModel<String> model = new IModel<String>()
		{
			private String value = null;

			@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/RatingsPage.java`
#### Snippet
```java
		private int nrOfVotes = 0;
		private int sumOfRatings = 0;
		private double rating = 0;

		/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/RatingsPage.java`
#### Snippet
```java
	{
		private int nrOfVotes = 0;
		private int sumOfRatings = 0;
		private double rating = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/RatingsPage.java`
#### Snippet
```java
	private static class RatingModel implements IClusterable
	{
		private int nrOfVotes = 0;
		private int sumOfRatings = 0;
		private double rating = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/EditableLabelPage.java`
#### Snippet
```java
	private int number = 42;
	
	private int refreshCounter = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/EffectsPage.java`
#### Snippet
```java
{
	private int counter1 = 0;
	private int counter2 = 0;
	private int counter3 = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/EffectsPage.java`
#### Snippet
```java
public class EffectsPage extends BasePage
{
	private int counter1 = 0;
	private int counter2 = 0;
	private int counter3 = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/EffectsPage.java`
#### Snippet
```java
	private int counter1 = 0;
	private int counter2 = 0;
	private int counter3 = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		private String content;

		private int count = 0;

		public ExampleResource()
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LinksPage.java`
#### Snippet
```java
	private int counter1 = 0;
	private int counter2 = 0;
	private int counter3 = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LinksPage.java`
#### Snippet
```java
public class LinksPage extends BasePage
{
	private int counter1 = 0;
	private int counter2 = 0;
	private int counter3 = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LinksPage.java`
#### Snippet
```java
{
	private int counter1 = 0;
	private int counter2 = 0;
	private int counter3 = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/prototype/Index.java`
#### Snippet
```java

	/** Click count. */
	private int count = 0;

	/** Label showing count */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/modal/ModalDialogPage.java`
#### Snippet
```java
	 * Should dialogs be stacked rather than nested
	 */
	private boolean stacked = false;

	public ModalDialogPage()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadApplication.java`
#### Snippet
```java
public class UploadApplication extends WicketExampleApplication
{
	private Folder uploadFolder = null;

	private IUploadsFileManager uploadsFileManager;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/wizard/NewUserWizard.java`
#### Snippet
```java

	/** Whether the assign roles step should be executed. */
	private boolean assignRoles = false;

	/** The user we are editing. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/Count.java`
#### Snippet
```java

	/** the count */
	private int count = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/Book.java`
#### Snippet
```java
	public static final WritingStyle BAD = new WritingStyle("bad");

	private static long nextId = 0;
	private static final Map<Long, Book> idToBook = new HashMap<>();

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/OIRPage.java`
#### Snippet
```java
		private static final long serialVersionUID = 1L;

		private boolean highlite = false;

		/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/Multiply.java`
#### Snippet
```java
	private int lhs = 0;

	private int rhs = 0;

	private TextField<Integer> right;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/Multiply.java`
#### Snippet
```java
	private TextField<Integer> left;

	private int lhs = 0;

	private int rhs = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatefulPage.java`
#### Snippet
```java

	/** click count for Link. */
	private int linkClickCount = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressUpdater.java`
#### Snippet
```java
		private final String sessionId;

		private volatile boolean canceled = false;
		private volatile boolean running = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressUpdater.java`
#### Snippet
```java

		private volatile boolean canceled = false;
		private volatile boolean running = false;

		private ProgressUpdateTask(Application application, String sessionId)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressBarTogglePanel.java`
#### Snippet
```java
{

    private int progress = 0;
    private boolean showProgress = true;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/linkomatic/Home.java`
#### Snippet
```java

	/** click count for OnClickLink. */
	private int onClickLinkClickCount = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `wicket-examples/src/main/java/org/apache/wicket/examples/linkomatic/Home.java`
#### Snippet
```java
{
	/** click count for Link. */
	private int linkClickCount = 0;

	/** click count for OnClickLink. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResponse.java`
#### Snippet
```java
	private ByteArrayOutputStream binary;

	private boolean isRedirect = false;

	private final boolean asynchronous;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
	 * are never passed to the Servlet Filters.
	 */
	private static boolean USING_JAVAX_WEB_SOCKET = false;

	static
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
	 * Flag that allows to use asynchronous push. By default, it is set to <code>false</code>.
	 */
	private boolean asynchronousPush = false;

	/**
```

## RuleId[id=RedundantImplements]
### RedundantImplements
Redundant interface declaration `Serializable`
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/bytebuddy/ByteBuddyInterceptor.java`
#### Snippet
```java
	implements
		ILazyInitProxy,
		Serializable,
		IWriteReplace
{
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java
			InvocationHandler,
			ILazyInitProxy,
			Serializable,
			IWriteReplace
	{
```

### RedundantImplements
Redundant interface declaration `IRequestHandler`
in `wicket-request/src/main/java/org/apache/wicket/request/flow/ResetResponseException.java`
#### Snippet
```java
	private static class ResponseResettingDecorator
		implements
			IRequestHandler,
			IRequestHandlerDelegate,
			ILoggableRequestHandler
```

### RedundantImplements
Redundant interface declaration `IModel`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
	 * parents model before the component is added to the parent.
	 */
	private class WrapperModel implements IModel<T>, IObjectClassAwareModel<T>
	{
		@Override
```

### RedundantImplements
Redundant interface declaration `IClusterable`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/ICellPopulator.java`
#### Snippet
```java
 *            Model object type
 */
public interface ICellPopulator<T> extends IClusterable, IDetachable
{
	/**
```

### RedundantImplements
Redundant interface declaration `IClusterable`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SingleSortState.java`
#### Snippet
```java
 * 
 */
public class SingleSortState<T> implements ISortState<T>, IClusterable
{
	private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `IClusterable`
in `wicket-core/src/main/java/org/apache/wicket/AttributeModifier.java`
#### Snippet
```java
 * @author Ralf Ebert
 */
public class AttributeModifier extends Behavior implements IClusterable
{
	/**
```

### RedundantImplements
Redundant interface declaration `IRequestMapper`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractComponentMapper.java`
#### Snippet
```java
 * @author Matej Knopp
 */
public abstract class AbstractComponentMapper extends AbstractMapper implements IRequestMapper
{
	/**
```

### RedundantImplements
Redundant interface declaration `IPageClassRequestHandler`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/RenderPageRequestHandler.java`
#### Snippet
```java
	implements
		IPageRequestHandler,
		IPageClassRequestHandler,
		ILoggableRequestHandler
{
```

### RedundantImplements
Redundant interface declaration `IClusterable`
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
public abstract class Component
	implements
		IClusterable,
		IConverterLocator,
		IRequestableComponent,
```

### RedundantImplements
Redundant interface declaration `IModel`
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentModel.java`
#### Snippet
```java
 *            type of model object
 */
public class ComponentModel<T> implements IModel<T>, IComponentAssignedModel<T>
{
	private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `IHeaderContributor`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractCheckSelector.java`
#### Snippet
```java
public abstract class AbstractCheckSelector extends LabeledWebMarkupContainer
	implements
		IHeaderContributor
{
	private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `IClusterable`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ChoiceRenderer.java`
#### Snippet
```java
 *            The model object type
 */
public class ChoiceRenderer<T> implements IChoiceRenderer<T>, IClusterable
{
	private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `IRequestListener`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ResourceLink.java`
#### Snippet
```java
 *            type of model object
 */
public class ResourceLink<T> extends Link<T> implements IRequestListener
{
	private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `IRequestHandler`
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceReferenceRequestHandler.java`
#### Snippet
```java
 * @author Matej Knopp
 */
public class ResourceReferenceRequestHandler implements IRequestHandler, ILoggableRequestHandler
{
	private final ResourceReference resourceReference;
```

### RedundantImplements
Redundant interface declaration `IRequestHandler`
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceStreamRequestHandler.java`
#### Snippet
```java
 * @author Eelco Hillenius
 */
public class ResourceStreamRequestHandler implements IRequestHandler, ILoggableRequestHandler
{
	/** Logger */
```

### RedundantImplements
Redundant interface declaration `IClusterable`
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
public abstract class Behavior
	implements
		IClusterable,
		IComponentAwareEventSink,
		IComponentAwareHeaderContributor
```

### RedundantImplements
Redundant interface declaration `IClusterable`
in `wicket-core/src/main/java/org/apache/wicket/resource/TextTemplateResourceReference.java`
#### Snippet
```java
 * @author James Carman
 */
public class TextTemplateResourceReference extends ResourceReference implements IClusterable
{

```

### RedundantImplements
Redundant interface declaration `Serializable`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/PageWindowManager.java`
#### Snippet
```java
	 * @author Matej Knopp
	 */
	public static class FileWindow implements IPersistedPage, Serializable
	{
		private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `IDetachable`
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/PanelContent.java`
#### Snippet
```java
 * @author Sven Meier
 */
public class PanelContent extends Content implements IDetachable
{
	private static final long serialVersionUID = 1L;
```

## RuleId[id=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `exception`
in `wicket-request/src/main/java/org/apache/wicket/request/RequestHandlerExecutor.java`
#### Snippet
```java
			catch (Exception exception)
			{
				if (rethrow == null && exception instanceof RuntimeException) {
					rethrow = (RuntimeException) exception;
				} else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
			catch (Exception e)
			{
				if (e instanceof ObjectCheckException)
				{
					throw (ObjectCheckException)e;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
				catch (Exception e)
				{
					if (e instanceof ObjectCheckException)
					{
						throw (ObjectCheckException)e;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		catch (RuntimeException ex)
		{
			if (ex instanceof WicketRuntimeException)
			{
				throw ex;
```

## RuleId[id=ExceptionNameDoesntEndWithException]
### ExceptionNameDoesntEndWithException
Exception class name `ThreadDump` does not end with 'Exception'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Threads.java`
#### Snippet
```java
	 * An exception used to hold the stacktrace of a thread.
	 */
	private static class ThreadDump extends RuntimeException
	{
		private static final long serialVersionUID = 1L;
```

## RuleId[id=StringBufferReplaceableByStringBuilder]
### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/basic/LinkParser.java`
#### Snippet
```java

				Matcher matcher = pattern.matcher(work);
				StringBuffer buffer = new StringBuffer();
				while (matcher.find())
				{
```

### StringBufferReplaceableByStringBuilder
`StringBuffer output` may be declared as 'StringBuilder'
in `wicket-core/src/main/java/org/apache/wicket/resource/CssUrlReplacer.java`
#### Snippet
```java
		Url cssUrl = Url.parse(name);
		Matcher matcher = URL_PATTERN.matcher(input);
		StringBuffer output = new StringBuffer();

		while (matcher.find())
```

### StringBufferReplaceableByStringBuilder
`StringBuffer output` may be declared as 'StringBuilder'
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/NestedStringResourceLoader.java`
#### Snippet
```java
		}
		
		StringBuffer output = new StringBuffer();
		Matcher matcher = pattern.matcher(value);
		// Search for other nested keys to replace
```

## RuleId[id=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceComponentInjector.java`
#### Snippet
```java
	public GuiceComponentInjector(final Application app)
	{
		this(app, new Module[0]);
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-util/src/main/java/org/apache/wicket/util/file/Files.java`
#### Snippet
```java
		File[] files = file.listFiles();
		if (files == null) {
			files = new File[0];
		}
		return files;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
			return wicketFolders;
		}
		return new Folder[0];
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
			return wicketFiles;
		}
		return new File[0];
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringList.java`
#### Snippet
```java
	public String[] toArray()
	{
		return strings.toArray(new String[0]);
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
			return "";
		}
		return join(separator, fragments.toArray(new String[0]));
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
	public Cookie[] getCookies()
	{
		return new Cookie[0];
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxUpgradeHttpRequest.java`
#### Snippet
```java
				String name = entry.getKey();
				List<String> value = entry.getValue();
				parametersMap.put(name, value.toArray(new String[0]));
			}
		}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/ResourceSettings.java`
#### Snippet
```java
				list.add(loader.toString());
			}
			return list.toArray(new String[0]);
		}
		return null;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
                cachedContent = dfos.getData();
            }
            return cachedContent != null ? cachedContent.clone() : new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/ClassAttributeModifier.java`
#### Snippet
```java
		if (Strings.isEmpty(currentValue))
		{
			classes = new String[0];
		}
		else
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/StyleAttributeModifier.java`
#### Snippet
```java
		if (Strings.isEmpty(currentValue))
		{
			styles = new String[0];
		}
		else
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxRequestAttributes.java`
#### Snippet
```java
	 * The names of the events which will trigger the Ajax call
	 */
	private String[] eventNames = new String[0];

	/**
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
			}
		}
		return new String[] { };
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
	protected void push(HttpServletRequest request)
	{
		pushBuilder.push(request, pushItems.toArray(new PushItem[0]));
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/BlobImageResource.java`
#### Snippet
```java
				return out.toByteArray();
			}
			return new byte[0];
		}
		catch (SQLException e)
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/session/DefaultPageFactory.java`
#### Snippet
```java
			try
			{
				if (pageClass.getDeclaredConstructor(new Class[] { }) != null)
				{
					bookmarkable = Boolean.TRUE;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	public SessionData[] getLiveSessions()
	{
		final SessionData[] sessions = liveSessions.values().toArray(new SessionData[0]);
		Arrays.sort(sessions);
		return sessions;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/AbstractRequestWrapperFactory.java`
#### Snippet
```java
		if (Strings.isEmpty(commaDelimitedStrings))
		{
			return new String[0];
		}
		else
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
		if (name.trim().length() == 0)
		{
			elements = new String[0];
		}
		else
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapperFactory.java`
#### Snippet
```java

		/** @see #setTrustedProxies(String) */
		private Pattern[] trustedProxies = new Pattern[0];

		/** @see #setTrustedProxies(String) */
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (value == null)
		{
			return new String[0];
		}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java
	public ReloadingClassLoader(ClassLoader parent)
	{
		super(new URL[] { }, parent);
		// probably doubles from this class, but just in case
		addClassLoaderUrls(parent);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ServletRequestCopy.java`
#### Snippet
```java
	public Cookie[] getCookies()
	{
		return new Cookie[0];
	}

```

## RuleId[id=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `vars` of exception class
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java

	/** Variable map to use in variable substitution */
	private Map<String, Object> vars;

	/**
```

### NonFinalFieldOfException
Non-final field `targetType` of exception class
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java

	/** Target type for the failed conversion. */
	private Class<?> targetType;

	/** Resource key for the message that should be displayed */
```

### NonFinalFieldOfException
Non-final field `format` of exception class
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java

	/** Pattern that was used for conversion. */
	private Format format;

	/** Locale that was used for conversion. */
```

### NonFinalFieldOfException
Non-final field `resourceKey` of exception class
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java

	/** Resource key for the message that should be displayed */
	private String resourceKey;

	/** Variable map to use in variable substitution */
```

### NonFinalFieldOfException
Non-final field `sourceValue` of exception class
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java

	/** The value that was tried to convert. */
	private Object sourceValue;

	/** Target type for the failed conversion. */
```

### NonFinalFieldOfException
Non-final field `converter` of exception class
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java

	/** The converter that was used. */
	private IConverter<?> converter;

	/** Pattern that was used for conversion. */
```

### NonFinalFieldOfException
Non-final field `locale` of exception class
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java

	/** Locale that was used for conversion. */
	private Locale locale;

	/** The value that was tried to convert. */
```

### NonFinalFieldOfException
Non-final field `component` of exception class
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessCheckFailureException.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private Component component;

	/**
```

### NonFinalFieldOfException
Non-final field `contentType` of exception class
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/pub/FileUploadContentTypeException.java`
#### Snippet
```java
     * The guilty content type.
     */
    private String contentType;

    /**
```

### NonFinalFieldOfException
Non-final field `MAPPER` of exception class
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
	}

	static IRequestMapper MAPPER = new IRequestMapper()
	{
		@Override
```

### NonFinalFieldOfException
Non-final field `markupStream` of exception class
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupException.java`
#### Snippet
```java

	/** The markup stream that was being parsed when the exception was thrown */
	private transient MarkupStream markupStream;

	/**
```

### NonFinalFieldOfException
Non-final field `handler` of exception class
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationExpiredException.java`
#### Snippet
```java
	private String cid;
	private Page page;
	private IRequestHandler handler;

	public ConversationExpiredException(Throwable cause, String cid, Page page,
```

### NonFinalFieldOfException
Non-final field `cid` of exception class
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationExpiredException.java`
#### Snippet
```java
{
	private static final long serialVersionUID = 1L;
	private String cid;
	private Page page;
	private IRequestHandler handler;
```

### NonFinalFieldOfException
Non-final field `page` of exception class
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationExpiredException.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;
	private String cid;
	private Page page;
	private IRequestHandler handler;

```

## RuleId[id=NonExceptionNameEndsWithException]
### NonExceptionNameEndsWithException
Non-exception class name `IWicketInternalException` ends with 'Exception'
in `wicket-core/src/main/java/org/apache/wicket/IWicketInternalException.java`
#### Snippet
```java
 * by the framework
 */
public interface IWicketInternalException
{
}
```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `wicket-spring/src/main/java/org/apache/wicket/spring/test/ApplicationContextMock.java`
#### Snippet
```java
	 * @param bean
	 */
	public <T extends Object> void putBean(final String name, final T bean)
	{
		beanFactory.registerBeanDefinition(name, new RootBeanDefinition((Class<T>)bean.getClass(), () -> bean));
```

## RuleId[id=ConstantValue]
### ConstantValue
Value `argument` is always 'true'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Args.java`
#### Snippet
```java
			throw new IllegalArgumentException(format(msg, params));
		}
		return argument;
	}

```

### ConstantValue
Value `argument` is always 'false'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Args.java`
#### Snippet
```java
			throw new IllegalArgumentException(format(msg, params));
		}
		return argument;
	}

```

### ConstantValue
Value `namespace` is always 'null'
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/parsers/TagNameParser.java`
#### Snippet
```java
			return namespace.toLowerCase(Locale.ROOT);
		}
		return namespace;
	}

```

### ConstantValue
Condition `h == 0` is always `true`
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	{
		int h = 0;
		if (h == 0)
		{
			int off = 0;
```

### ConstantValue
Value `str` is always 'null'
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (str == null)
		{
			str = String.valueOf(str);
		}

```

### ConstantValue
Value `str` is always 'null'
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (str == null)
		{
			str = String.valueOf(str);
		}
		int len = str.length();
```

### ConstantValue
Value `added` is always 'true'
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlLicenseHeaderHandler.java`
#### Snippet
```java
		}

		return added;
	}
}
```

### ConstantValue
Value `container` is always 'null'
in `wicket-util/src/main/java/org/apache/wicket/util/collections/ClassMetaCache.java`
#### Snippet
```java
			if (!create)
			{
				return container;
			}

```

### ConstantValue
Value `recursive` is always 'true'
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java
					if (recursive)
					{
						zipDir(f, out, path + f.getName() + "/", recursive);
					}
				} else
```

### ConstantValue
Value `e` is always 'null'
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
		}

		return e;
	}

```

### ConstantValue
Value `e` is always 'null'
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
		}

		return e;
	}

```

### ConstantValue
Condition `application == null` is always `false`
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringWebApplicationFactory.java`
#### Snippet
```java
		{
			application = (WebApplication)applicationContext.getBean(beanName);
			if (application == null)
			{
				throw new IllegalArgumentException(
```

### ConstantValue
Condition `exception instanceof RuntimeException` is always `true` when reached
in `wicket-request/src/main/java/org/apache/wicket/request/RequestHandlerExecutor.java`
#### Snippet
```java
			catch (Exception exception)
			{
				if (rethrow == null && exception instanceof RuntimeException) {
					rethrow = (RuntimeException) exception;
				} else {
```

### ConstantValue
Value `renderCount` is always 'null'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
			if (listenerParts.length == 0)
			{
				return new ComponentInfo(renderCount, componentPath, behaviorIndex);
			}
			else if (listenerParts.length == 2)
```

### ConstantValue
Value `behaviorIndex` is always 'null'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
			if (listenerParts.length == 0)
			{
				return new ComponentInfo(renderCount, componentPath, behaviorIndex);
			}
			else if (listenerParts.length == 2)
```

### ConstantValue
Condition `RequestContext.class.isAssignableFrom(ctx.getClass())` is always `true`
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/impl/FileItemIteratorImpl.java`
#### Snippet
```java
        final long contentLengthInt = ctx.getContentLength();
        // @formatter:off
        final long requestSize = RequestContext.class.isAssignableFrom(ctx.getClass())
                                 // Inline conditional is OK here CHECKSTYLE:OFF
                                 ? ctx.getContentLength()
```

### ConstantValue
Condition `!targetsComponent` is always `true` when reached
in `wicket-core/src/main/java/org/apache/wicket/ComponentEventSender.java`
#### Snippet
```java
		boolean targetsApplication = targetsSession || sink instanceof Application;

		if (!targetsApplication && !targetsComponent)
		{
			dispatcher.dispatchEvent(sink, event, null);
```

### ConstantValue
Value `targetsComponent` is always 'false'
in `wicket-core/src/main/java/org/apache/wicket/ComponentEventSender.java`
#### Snippet
```java
		boolean targetsApplication = targetsSession || sink instanceof Application;

		if (!targetsApplication && !targetsComponent)
		{
			dispatcher.dispatchEvent(sink, event, null);
```

### ConstantValue
Condition `targetsApplication` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/ComponentEventSender.java`
#### Snippet
```java
			return;
		}
		if (targetsApplication)
		{
			dispatcher.dispatchEvent(source.getApplication(), event, null);
```

### ConstantValue
Condition `method != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
			{
				Method method = clz.getMethod(name, getMethod.getReturnType());
				if (method != null)
				{
					method.setAccessible(true);
```

### ConstantValue
Condition `obj instanceof ThreadLocalRandom` is always `false`
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
				obj instanceof Locale ||
				obj instanceof Random ||
				obj instanceof ThreadLocalRandom ||
				obj instanceof StringBuffer ||
				obj instanceof Subject;
```

### ConstantValue
Condition `data != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
		// If path contains a locale, then it'll replace the locale provided to this method
		PathLocale data = ResourceUtils.getLocaleFromFilename(path);
		if ((data != null) && (data.locale != null))
		{
			path = data.path;
```

### ConstantValue
Condition `freshPage` is always `true` when reached
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/ListenerRequestHandler.java`
#### Snippet
```java
		boolean blockIfExpired = component != null && !component.canCallListenerAfterExpiry();

		boolean lateComponent = component == null && freshPage;

		if ((pageComponentProvider.wasExpired() && blockIfExpired) || lateComponent)
```

### ConstantValue
Condition `cookies != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebResponse.java`
#### Snippet
```java
		contentLength = null;
		contentType = null;
		if (cookies != null)
		{
			cookies.clear();
```

### ConstantValue
Condition `headers != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebResponse.java`
#### Snippet
```java
		}
		errorMessage = null;
		if (headers != null)
		{
			headers.clear();
```

### ConstantValue
Condition `behaviors != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTesterHelper.java`
#### Snippet
```java
		List<AjaxEventBehavior> behaviors = findAjaxEventBehaviors(component, event);
		AjaxEventBehavior behavior = null;
		if (behaviors != null && behaviors.isEmpty() == false)
		{
			behavior = behaviors.get(0);
```

### ConstantValue
Condition `metaData == null` is always `true` when reached
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			data_set(index, metaData);
		}
		else if (old != null && metaData == null)
		{
			data_remove(index);
```

### ConstantValue
Condition `tester == null` is always `false`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
	{
		List<TagTester> tester = createTags(markup, xmlTag -> xmlTag.getName().equalsIgnoreCase(tagName), true);
		if ((tester == null) || (tester.size() == 0))
		{
			return null;
```

### ConstantValue
Condition `stopAfterFirst && (closeTag != null)` is always `false`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
					}

					if (stopAfterFirst && (closeTag != null))
					{
						break;
```

### ConstantValue
Condition `closeTag != null` is always `false` when reached
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
					}

					if (stopAfterFirst && (closeTag != null))
					{
						break;
```

### ConstantValue
Condition `ixExtension == len` is always `false`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/PackageResourceGuard.java`
#### Snippet
```java
		int len = path.length();
		final String ext;
		if (ixExtension <= 0 || ixExtension == len ||
			(path.lastIndexOf('/') + 1) == ixExtension ||
			(path.lastIndexOf('\\') + 1) == ixExtension)
```

### ConstantValue
Condition `isAjax == false` is always `true` when reached
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		}
		// The link requires AJAX
		else if (linkComponent instanceof IAjaxLink && isAjax == false)
		{
			fail("Link " + path + "is an IAjaxLink and will " +
```

### ConstantValue
Value `isAjax` is always 'false'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		}
		// The link requires AJAX
		else if (linkComponent instanceof IAjaxLink && isAjax == false)
		{
			fail("Link " + path + "is an IAjaxLink and will " +
```

### ConstantValue
Condition `!(redirectHost.equals(originalHost))` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		else
		{
			return !(redirectHost.equals(originalHost));
		}
	}
```

### ConstantValue
Result of `redirectHost.equals(originalHost)` is always 'false'
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		else
		{
			return !(redirectHost.equals(originalHost));
		}
	}
```

### ConstantValue
Condition `markup != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FragmentMarkupSourcingStrategy.java`
#### Snippet
```java
			{
				markup = associatedMarkup;
				if (markup != null)
				{
					childMarkup = markup.find(markupId);
```

### ConstantValue
Value `markup` is always 'null'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/Panel.java`
#### Snippet
```java
		if (markup == null)
		{
			return markup;
		}
		
```

### ConstantValue
Condition `headerPart != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AssociatedMarkupSourcingStrategy.java`
#### Snippet
```java
			HeaderPartContainer headerPart = getHeaderPart(container, headerId,
				markupStream.getMarkupFragment());
			if (headerPart != null)
			{
				// A component's header section must only be added once,
```

### ConstantValue
Value `markup` is always 'null'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
		if (markup == null)
		{
			return markup;
		}
		
```

### ConstantValue
Condition `!(tag instanceof WicketTag)` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
			// In case the enclosure tag did not provide a child component id, then assign the
			// first ComponentTag's id found as the controlling child to the enclosure.
			if (tag.isOpen() && (tag.getId() != null) && !(tag instanceof WicketTag) &&
				!tag.isAutoComponentTag())
			{
```

### ConstantValue
Condition `tag instanceof WicketTag` is always `false`
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
			// In case the enclosure tag did not provide a child component id, then assign the
			// first ComponentTag's id found as the controlling child to the enclosure.
			if (tag.isOpen() && (tag.getId() != null) && !(tag instanceof WicketTag) &&
				!tag.isAutoComponentTag())
			{
```

### ConstantValue
Value `hasWicketId` is always 'true'
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHandler.java`
#### Snippet
```java
			for (ComponentTag componentTag : stack)
			{
				componentTag.setContainsWicketId(hasWicketId);
			}
		}
```

### ConstantValue
Condition `page != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
			}

			final String pageClassName = (page != null) ? page.getClass().getName() : "unknown";
			final IResourceStream stream = markupStream.getResource();
			final String streamName = (stream != null) ? stream.toString() : "unknown";
```

### ConstantValue
Condition `pathInfo.path != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
			final MarkupContainer container = pathInfo.getContainer();

			if (!pathInfo.absolute && (pathInfo.path != null) && (pathInfo.path.length() > 0))
			{
				// Href is relative. Create a resource reference pointing at this file
```

### ConstantValue
Value `locale` is always 'null'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResourceReference.java`
#### Snippet
```java
		}

		return locale;
	}

```

### ConstantValue
Value `style` is always 'null'
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResourceReference.java`
#### Snippet
```java
		}

		return style;
	}
	
```

### ConstantValue
Condition `pageClass.getDeclaredConstructor(new Class[] { }) != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/session/DefaultPageFactory.java`
#### Snippet
```java
			try
			{
				if (pageClass.getDeclaredConstructor(new Class[] { }) != null)
				{
					bookmarkable = Boolean.TRUE;
```

### ConstantValue
Condition `pageClass.getDeclaredConstructor(new Class[] { PageParameters.class }) != null` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/session/DefaultPageFactory.java`
#### Snippet
```java
				try
				{
					if (pageClass.getDeclaredConstructor(new Class[] { PageParameters.class }) != null)
					{
						bookmarkable = Boolean.TRUE;
```

### ConstantValue
Value `string` is always 'null'
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
		}

		return string;
	}

```

### ConstantValue
Condition `value instanceof String[]` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		}

		if (value instanceof String[])
		{
			return (String[])value;
```

### ConstantValue
Condition `max == null` is always `true` when reached
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/AbstractRangeValidator.java`
#### Snippet
```java
			return Mode.MINIMUM;
		}
		else if ((min == null && max == null) || max.equals(min))
		{
			return Mode.EXACT;
```

### ConstantValue
Condition `clazz == null` is always `false`
in `wicket-core/src/main/java/org/apache/wicket/application/AbstractClassResolver.java`
#### Snippet
```java
					{
						clazz = Class.forName(className, false, getClassLoader());
						if (clazz == null)
						{
							throw new ClassNotFoundException(className);
```

### ConstantValue
Condition `cardId == CreditCard.INVALID` is always `true`
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java
		{
			cardId = CreditCard.INVALID;
			if (cardId == CreditCard.INVALID)
			{
				cardId = isAmericanExpress(creditCardNumber);
```

### ConstantValue
Condition `istr < lstr` is always `true` when reached
in `wicket-core/src/main/java/org/apache/wicket/application/WildcardMatcherHelper.java`
#### Snippet
```java
			// if hole pattern has matched the string so far but the string has more characters left
			// we don't match
			if (ipat >= lpat && istr < lstr)
			{
				return false;
```

### ConstantValue
Condition `patterns == null` is always `false`
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java

		// If no explicit includes, try here
		if (patterns == null || patterns.size() == 0)
		{
			tryHere = true;
```

### ConstantValue
Condition `locale != null` is always `true`
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/LocaleHelper.java`
#### Snippet
```java

		Locale locale = parseLocale(localeCandidate);
		if (locale != null)
		{
			for (final Locale l : Locale.getAvailableLocales())
```

### ConstantValue
Condition `locale != null` is always `true`
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/CustomHomeMapper.java`
#### Snippet
```java
			String localeAsString = segments.get(0);
			Locale locale = LocaleHelper.parseLocale(localeAsString);
			if (locale != null)
			{
				Session.get().setLocale(locale);
```

### ConstantValue
Condition `!Velocity.mergeTemplate(templateName, encoding, ctx, writer)` is always `false`
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/VelocityContributor.java`
#### Snippet
```java
		{
			// execute the velocity script and capture the output in writer
			if (!Velocity.mergeTemplate(templateName, encoding, ctx, writer))
			{
				return null;
```

### ConstantValue
Condition `template != null` is always `true`
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/markup/html/VelocityPanel.java`
#### Snippet
```java

		final String template = ResourceUtil.readString(resource);
		if (template != null)
		{
			return new StringReader(template);
```

## RuleId[id=MethodOverridesStaticMethod]
### MethodOverridesStaticMethod
Method `arrayToString()` tries to override a static method of a superclass
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Diff.java`
#### Snippet
```java
	 * @return String
	 */
	public static String arrayToString(final Object[] o)
	{
		return arrayToString(o, Diff.NL);
```

### MethodOverridesStaticMethod
Method `get()` tries to override a static method of a superclass
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/AbstractAuthenticatedWebSession.java`
#### Snippet
```java
	 * @return Current authenticated web session
	 */
	public static AbstractAuthenticatedWebSession get()
	{
		return (AbstractAuthenticatedWebSession)Session.get();
```

### MethodOverridesStaticMethod
Method `get()` tries to override a static method of a superclass
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/AuthenticatedWebSession.java`
#### Snippet
```java
	 * @return Current authenticated web session
	 */
	public static AuthenticatedWebSession get()
	{
		return (AuthenticatedWebSession)Session.get();
```

### MethodOverridesStaticMethod
Method `get()` tries to override a static method of a superclass
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebSession.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	public static WebSession get()
	{
		return (WebSession)Session.get();
```

### MethodOverridesStaticMethod
Method `get()` tries to override a static method of a superclass
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
	 * it.
	 */
	public static WebApplication get()
	{
		Application application = Application.get();
```

### MethodOverridesStaticMethod
Method `get()` tries to override a static method of a superclass
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/TreeApplication.java`
#### Snippet
```java
	}

	public static TreeApplication get()
	{
		return (TreeApplication)WebApplication.get();
```

### MethodOverridesStaticMethod
Method `get()` tries to override a static method of a superclass
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/JSR356Application.java`
#### Snippet
```java
    }

    public static JSR356Application get()
    {
        return (JSR356Application) WebApplication.get();
```

### MethodOverridesStaticMethod
Method `get()` tries to override a static method of a superclass
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/JSR356Session.java`
#### Snippet
```java
    }

    public static JSR356Session get() {
        return (JSR356Session)WebSession.get();
    }
```

### MethodOverridesStaticMethod
Method `get()` tries to override a static method of a superclass
in `wicket-examples/src/main/java/org/apache/wicket/examples/resourcedecoration/ResourceDecorationApplication.java`
#### Snippet
```java
	}

	public static ResourceDecorationApplication get()
	{
		return (ResourceDecorationApplication)WebApplication.get();
```

## RuleId[id=IOResource]
### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-util/src/main/java/org/apache/wicket/util/diff/ToString.java`
#### Snippet
```java
	public static String[] stringToArray(final String value)
	{
		BufferedReader reader = new BufferedReader(new StringReader(value));
		List<String> l = new LinkedList<>();
		String s;
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		if (!(input1 instanceof BufferedInputStream))
		{
			input1 = new BufferedInputStream(input1);
		}
		if (!(input2 instanceof BufferedInputStream))
```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		if (!(input2 instanceof BufferedInputStream))
		{
			input2 = new BufferedInputStream(input2);
		}

```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		if (!(input1 instanceof BufferedReader))
		{
			input1 = new BufferedReader(input1);
		}
		if (!(input2 instanceof BufferedReader))
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
		if (!(input2 instanceof BufferedReader))
		{
			input2 = new BufferedReader(input2);
		}

```

### IOResource
'ObjectInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-util/src/main/java/org/apache/wicket/util/file/File.java`
#### Snippet
```java
	public Object readObject() throws IOException, ClassNotFoundException
	{
		return new ObjectInputStream(inputStream()).readObject();
	}

```

### IOResource
'ObjectOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-util/src/main/java/org/apache/wicket/util/file/File.java`
#### Snippet
```java
	public void writeObject(final Serializable object) throws IOException
	{
		new ObjectOutputStream(outputStream()).writeObject(object);
	}

```

### IOResource
'SizeRecodingOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-objectsizeof-agent/src/main/java/org/apache/wicket/util/instrument/InstrumentationObjectSizeOfStrategy.java`
#### Snippet
```java
		try
		{
			SizeRecodingOutputStream recorder = new SizeRecodingOutputStream();
			recorder.writeObject(obj);
			return recorder.getTotalSize();
```

### IOResource
'Formatter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/form/upload/UploadProgressBar.java`
#### Snippet
```java

		StringBuilder builder = new StringBuilder(128);
		Formatter formatter = new Formatter(builder);

		Form<?> form = getCallbackForm();
```

### IOResource
'InterceptingObjectOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
				try
				{
					InterceptingObjectOutputStream ioos = new InterceptingObjectOutputStream();
					ioos.writeObject(obj);
				}
```

### IOResource
'WebExternalResourceStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ContextRelativeResource.java`
#### Snippet
```java

		final WebExternalResourceStream webExternalResourceStream =
			new WebExternalResourceStream(path);
		resourceResponse.setContentType(webExternalResourceStream.getContentType());
		resourceResponse.setLastModified(webExternalResourceStream.lastModifiedTime());
```

### IOResource
'ObjectOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
			try (OutputStream stream = new FileOutputStream(index))
			{
				ObjectOutputStream oos = new ObjectOutputStream(stream);
				
				ArrayList<DiskData> list = new ArrayList<>(diskDatas.size());
```

### IOResource
'ObjectInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
			try (InputStream stream = new FileInputStream(index))
			{
				ObjectInputStream ois = new ObjectInputStream(stream);

				diskDatas.clear();
```

### IOResource
'RandomAccessFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
				try
				{
					RandomAccessFile randomAccessFile = new RandomAccessFile(file, mode);
					channel = randomAccessFile.getChannel();
				}
```

### IOResource
'CheckingObjectOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-core/src/main/java/org/apache/wicket/serialize/java/JavaSerializer.java`
#### Snippet
```java
						// trigger serialization again, but this time gather some more info
						CheckingObjectOutputStream checkingObjectOutputStream =
							new CheckingObjectOutputStream(outputStream, new ObjectSerializationChecker(nsx));
						checkingObjectOutputStream.writeObject(obj);
					}
```

### IOResource
'PackageTextTemplate' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-examples/src/main/java/org/apache/wicket/examples/asemail/MailTemplate.java`
#### Snippet
```java
				variables.put("downloadLink", href);

				PackageTextTemplate template = new PackageTextTemplate(MailTemplate.class, "mail-template.tmpl");
				CharSequence templateHtml = template.asString(variables);
				updateResult(result, templateHtml, target);
```

### IOResource
'PackageResourceStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-examples/src/main/java/org/apache/wicket/examples/velocity/DynamicPage.java`
#### Snippet
```java
	public DynamicPage(final PageParameters parameters)
	{
		final IResourceStream template = new PackageResourceStream(DynamicPage.class,
			"fields.vm");

```

### IOResource
'PackageTextTemplate' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
	protected String getWebSocketSetupScript(Map<String, Object> parameters) {
		PackageTextTemplate webSocketSetupTemplate =
				new PackageTextTemplate(WicketWebSocketJQueryResourceReference.class,
						"res/js/wicket-websocket-setup.js.tmpl");

```

## RuleId[id=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Collection `lst` is always empty
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
		{
			List<Behavior> lst = Collections.emptyList();
			return lst.iterator();
		}

```

### RedundantOperationOnEmptyContainer
Collection `list` is always empty
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/EmptyDataProvider.java`
#### Snippet
```java
	{
		List<T> list = Collections.emptyList();
		return list.iterator();
	}

```

## RuleId[id=FieldMayBeStatic]
### FieldMayBeStatic
Field `VALUE_SEPARATOR` may be 'static'
in `wicket-core/src/main/java/org/apache/wicket/authentication/strategy/DefaultAuthenticationStrategy.java`
#### Snippet
```java

	/** The separator used to concatenate the username and password */
	protected final String VALUE_SEPARATOR = "-sep-";

	/** Cookie utils with default settings */
```

### FieldMayBeStatic
Field `number` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatelessPage1.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private final Integer number = 10;

	/**
```

### FieldMayBeStatic
Field `number` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatelessPage.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private final Integer number = 10;

	/**
```

### FieldMayBeStatic
Field `number` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatelessPage2.java`
#### Snippet
```java
	private static final long serialVersionUID = 1L;

	private final Integer number = 10;

	/**
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
		 */

		if (values != null && values.length > 0)
		{
			for (final String value : values)
```

## RuleId[id=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `IOUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/io/IOUtils.java`
#### Snippet
```java
 * @author Gareth Davis
 */
public final class IOUtils
{
	private static final Logger log = LoggerFactory.getLogger(IOUtils.class);
```

### UtilityClassWithoutPrivateConstructor
Class `Primitives` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Primitives.java`
#### Snippet
```java
 * @author Jonathan Locke
 */
public final class Primitives
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Comparators` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Comparators.java`
#### Snippet
```java
package org.apache.wicket.util.lang;

public class Comparators
{

```

### UtilityClassWithoutPrivateConstructor
Class `Checks` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Checks.java`
#### Snippet
```java
 * 
 */
public class Checks
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Durations` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/time/Durations.java`
#### Snippet
```java
 *
 */
public class Durations
{

```

### UtilityClassWithoutPrivateConstructor
Class `Instants` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/time/Instants.java`
#### Snippet
```java
 * Utility class for {@link Instant}
 */
public class Instants
{

```

### UtilityClassWithoutPrivateConstructor
Class `CharEncoding` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/CharEncoding.java`
#### Snippet
```java
 * @version $Id$
 */
public class CharEncoding
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Args` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Args.java`
#### Snippet
```java
 * Class with methods for asserting conditions on arguments. 
 */
public class Args
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `StringUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/StringUtils.java`
#### Snippet
```java
 * @since 1.4
 */
public class StringUtils
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `CipherUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/CipherUtils.java`
#### Snippet
```java
 * Utility class meant to help building {@link Cipher}.
 */
public class CipherUtils
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `StringEscapeUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringEscapeUtils.java`
#### Snippet
```java
 */
// Copy from commons-lang ver. 2.6. Non-html/xml methods were removed
class StringEscapeUtils
{

```

### UtilityClassWithoutPrivateConstructor
Class `ResourceUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceUtils.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class ResourceUtils
{
	/** The default postfix for minified names (ex: /css/mystyle.min.css) **/
```

### UtilityClassWithoutPrivateConstructor
Class `Stringz` has only 'static' members, and lacks a 'private' constructor
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/Stringz.java`
#### Snippet
```java
package org.apache.wicket.jmx.wrapper;

class Stringz
{
	static String className(final Object o)
```

### UtilityClassWithoutPrivateConstructor
Class `ObjectSizeOfAgent` has only 'static' members, and lacks a 'private' constructor
in `wicket-objectsizeof-agent/src/main/java/org/apache/wicket/util/instrument/ObjectSizeOfAgent.java`
#### Snippet
```java
 * @author eelcohillenius
 */
public class ObjectSizeOfAgent
{

```

### UtilityClassWithoutPrivateConstructor
Class `LazyInitProxyFactory` has only 'static' members, and lacks a 'private' constructor
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/LazyInitProxyFactory.java`
#### Snippet
```java
 * 
 */
public class LazyInitProxyFactory
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `UrlUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-request/src/main/java/org/apache/wicket/request/UrlUtils.java`
#### Snippet
```java
 * @author igor.vaynberg
 */
public class UrlUtils
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `AjaxLazyLoadPanelTester` has only 'static' members, and lacks a 'private' constructor
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanelTester.java`
#### Snippet
```java
 * @author Antony Stubbs
 */
public class AjaxLazyLoadPanelTester
{
	private static final Logger logger = LoggerFactory.getLogger(AjaxLazyLoadPanelTester.class);
```

### UtilityClassWithoutPrivateConstructor
Class `CharSetUtil` has only 'static' members, and lacks a 'private' constructor
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/util/encoding/CharSetUtil.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class CharSetUtil
{
	/** Logging */
```

### UtilityClassWithoutPrivateConstructor
Class `AjaxUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxUtils.java`
#### Snippet
```java
 * Some AJAX related utility functions.
 */
public class AjaxUtils
{

```

### UtilityClassWithoutPrivateConstructor
Class `JavaScriptUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptUtils.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class JavaScriptUtils
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ImageUtil` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/util/image/ImageUtil.java`
#### Snippet
```java
 *
 */
public class ImageUtil
{

```

### UtilityClassWithoutPrivateConstructor
Class `WicketTesterHelper` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTesterHelper.java`
#### Snippet
```java
 * @since 1.2.6
 */
public class WicketTesterHelper
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `DiffUtil` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public final class DiffUtil
{
	private static final Logger log = LoggerFactory.getLogger(DiffUtil.class);
```

### UtilityClassWithoutPrivateConstructor
Class `TagUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class TagUtils
{
	private static final String DEFAULT_ATTRIBUTE_SEPARATOR = "; ";
```

### UtilityClassWithoutPrivateConstructor
Class `MarkupUtil` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/markup/html/MarkupUtil.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class MarkupUtil
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `DispatchedRequestUtils` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/DispatchedRequestUtils.java`
#### Snippet
```java
 * @since 1.5.8
 */
class DispatchedRequestUtils
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `CoreLibrariesContributor` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/resource/CoreLibrariesContributor.java`
#### Snippet
```java
 * @since 6.0
 */
public class CoreLibrariesContributor
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `WildcardMatcherHelper` has only 'static' members, and lacks a 'private' constructor
in `wicket-core/src/main/java/org/apache/wicket/application/WildcardMatcherHelper.java`
#### Snippet
```java
 * @version $Id: WildcardMatcherHelper.java 420832 2006-07-11 13:11:02Z cziegeler $
 */
public class WildcardMatcherHelper
{
	// ~ Static fields/initializers
```

### UtilityClassWithoutPrivateConstructor
Class `DatabaseLocator` has only 'static' members, and lacks a 'private' constructor
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/DatabaseLocator.java`
#### Snippet
```java
 * 
 */
public class DatabaseLocator
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ChartUpdater` has only 'static' members, and lacks a 'private' constructor
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/charts/ChartUpdater.java`
#### Snippet
```java
 * the thread.
 */
public class ChartUpdater
{
	public static void start(ConnectedMessage message, ScheduledExecutorService scheduledExecutorService)
```

### UtilityClassWithoutPrivateConstructor
Class `ProgressUpdater` has only 'static' members, and lacks a 'private' constructor
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressUpdater.java`
#### Snippet
```java
 * A helper class that uses the web connection to push components updates to the client.
 */
public class ProgressUpdater
{
	/**
```

### UtilityClassWithoutPrivateConstructor
Class `LocaleHelper` has only 'static' members, and lacks a 'private' constructor
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/LocaleHelper.java`
#### Snippet
```java
 * A helper class to deal with {@link Locale} as string
 */
public class LocaleHelper
{
	static Locale parseLocale(final String localeAsString)
```

### UtilityClassWithoutPrivateConstructor
Class `Holder` has only 'static' members, and lacks a 'private' constructor
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
	 * This way wicket-core module doesn't have reference to wicket-native-websocket.
	 */
	public static final class Holder
	{
		public static WebSocketSettings get(Application application)
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Argument `bts` might be null
in `wicket-util/src/main/java/org/apache/wicket/util/io/ProxyInputStream.java`
#### Snippet
```java
        try {
            beforeRead(bts != null ? bts.length : 0);
            final int n = in.read(bts);
            afterRead(n);
            return n;
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `wicket-util/src/main/java/org/apache/wicket/util/markup/xhtml/WellFormedXmlTestCase.java`
#### Snippet
```java
			if (uri != null)
			{
				return new InputSource(WellFormedXmlTestCase.class.getResource(uri).toString());
			}

```

### DataFlowIssue
Dereference of `dir.listFiles(fileFilter)` may produce `NullPointerException`
in `wicket-util/src/main/java/org/apache/wicket/util/markup/xhtml/WellFormedXmlTestCase.java`
#### Snippet
```java
	private void processDirectory(File dir)
	{
		for (File f : dir.listFiles(fileFilter))
		{
			if (f.isDirectory())
```

### DataFlowIssue
Argument `inputStream` might be null
in `wicket-util/src/main/java/org/apache/wicket/util/license/AbstractLicenseHeaderHandler.java`
#### Snippet
```java
				inputStream = ApacheLicenseHeaderTestCase.class
					.getResourceAsStream(getLicenseHeaderFilename());
				inputStreamReader = new InputStreamReader(inputStream);
				lineNumberReader = new LineNumberReader(inputStreamReader);

```

### DataFlowIssue
Variable is already assigned to this value
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
							catch (NumberFormatException e)
							{
								entityValue = -1;
							}
						}
```

### DataFlowIssue
Method invocation `endsWith` may produce `NullPointerException`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
				if (previousFragment != null || fragment != null)
				{
					boolean lhsClosed = previousFragment.endsWith(separator);
					boolean rhsClosed = fragment.startsWith(separator);
					if (separatorNotEmpty && lhsClosed && rhsClosed)
```

### DataFlowIssue
Argument `clazz` might be null
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
		Class<?> clazz = fieldResolvableType.resolve();
		
		if (Set.class.isAssignableFrom(clazz) || List.class.isAssignableFrom(clazz))
		{
			return fieldResolvableType.getGeneric();
```

### DataFlowIssue
Method invocation `getMarkupId` may produce `NullPointerException`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/form/upload/UploadProgressBar.java`
#### Snippet
```java

		CharSequence url = form != null ? urlFor(ref, UploadStatusResource.newParameter(getPage().getId())) :
				urlFor(ref, UploadStatusResource.newParameter(uploadField.getMarkupId()));

		StringBuilder builder = new StringBuilder(128);
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
			Serializable errorMessage = editor.getFeedbackMessages().first(FeedbackMessage.ERROR);
			target.appendJavaScript("window.status='" +
				JavaScriptUtils.escapeQuotes(errorMessage.toString()) + "';");
		}
		String selectAndFocusScript = String.format(
```

### DataFlowIssue
Unboxing of `getDefaultModelObject()` may produce `NullPointerException`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
	public final int getSelectedTab()
	{
		return (Integer)getDefaultModelObject();
	}

```

### DataFlowIssue
Argument `image` might be null
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
			image = load("images/" + filename);
			highlightedImage = load("images/" + filename + "_highlight");
			visibleRegion = new OpaqueRegion(image);
		}

```

### DataFlowIssue
Argument `KittenCaptchaPanel.class.getResourceAsStream(filename + ".png")` might be null
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
			{
				final BufferedImage loadedImage = ImageIO.read(new MemoryCacheImageInputStream(
					KittenCaptchaPanel.class.getResourceAsStream(filename + ".png")));
				final BufferedImage image = new BufferedImage(loadedImage.getWidth(),
					loadedImage.getHeight(), BufferedImage.TYPE_INT_ARGB);
```

### DataFlowIssue
Argument `KittenCaptchaPanel.class.getResourceAsStream(filename)` might be null
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
		{
			return ImageIO.read(new MemoryCacheImageInputStream(
				KittenCaptchaPanel.class.getResourceAsStream(filename)));
		}
		catch (IOException e)
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

		Form<?> form = findParent(Form.class);
		form.add(formValidatorWrapper);
	}

```

### DataFlowIssue
Method invocation `indexOf` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
		ArrayList<Behavior> ids = getBehaviorsIdList(component, true);

		int id = ids.indexOf(behavior);

		if (id < 0)
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java

			throw new ConversionException("Could not convert value: " + value + " to type: " +
				theType.getName() + ". Could not find compatible converter.").setSourceValue(value);
		}

```

### DataFlowIssue
Method invocation `closes` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/DequeueContext.java`
#### Snippet
```java
	public void skipToCloseTag()
	{
		while (!next.closes(tags.peek()))
		{
			next = nextTag();
```

### DataFlowIssue
Method invocation `getPageRelativePath` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
			{
				log.debug("Page '{}' is not stateless because of component with path '{}'.", this,
					statefulComponent.getPageRelativePath());
			}

```

### DataFlowIssue
Argument `component.getParent()` might be null
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
					}

					if (hasInvisibleTransparentChild(component.getParent(), component))
					{
						// If we found a transparent container that isn't visible then ignore this
```

### DataFlowIssue
Method invocation `getPageRelativePath` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
				}
				buffer.append(StringValue.repeat(levels, "	"))
					.append(component.getPageRelativePath())
					.append(':')
					.append(Classes.simpleName(component.getClass()));
```

### DataFlowIssue
Argument `markupStream` might be null
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		if (render)
		{
			renderAll(markupStream, openTag);
		}
	}
```

### DataFlowIssue
Casting `elem` to `ComponentTag` may produce `ClassCastException`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java

		// Check for required open tag name
		ComponentTag associatedMarkupOpenTag = (ComponentTag)elem;
		if (!(associatedMarkupOpenTag.isOpen() && (associatedMarkupOpenTag instanceof WicketTag)))
		{
```

### DataFlowIssue
Method invocation `isOpen` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		// Check for required open tag name
		ComponentTag associatedMarkupOpenTag = (ComponentTag)elem;
		if (!(associatedMarkupOpenTag.isOpen() && (associatedMarkupOpenTag instanceof WicketTag)))
		{
			associatedMarkupStream.throwMarkupException("Markup for a " + openTagName
```

### DataFlowIssue
Argument `tag` might be null
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	
			// see if child is already added to parent
			Component child = findChildComponent(tag);

			if (child == null)
```

### DataFlowIssue
Method invocation `isPageStateless` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/PageAndComponentProvider.java`
#### Snippet
```java

				// make sure this page instance was just created so the page can be stateless
				if (page.isPageStateless())
				{
					Page p = (Page)page;
```

### DataFlowIssue
Method invocation `getComponentPath` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java

		PageAndComponentProvider provider = new PageAndComponentProvider(pageInfo.getPageId(),
			pageClass, pageParameters, renderCount, componentInfo.getComponentPath());

		provider.setPageSource(getContext());
```

### DataFlowIssue
Method invocation `toURI` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DiffUtil.java`
#### Snippet
```java
		{
			final URL url = clazz.getClassLoader().getResource(filename);
			filename = new File(url.toURI()).getAbsolutePath();
		}
		catch (URISyntaxException ex)
```

### DataFlowIssue
Variable is already assigned to this value
in `wicket-core/src/main/java/org/apache/wicket/util/tester/TagTester.java`
#### Snippet
```java
									// thus reset state
									openTag = null;
									closeTag = null;
								}
							}
```

### DataFlowIssue
Method invocation `doSelect` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java

		ChoiceSelector choiceSelector = choiceSelectorFactory.create(component);
		choiceSelector.doSelect(index);

		for (FormComponentUpdatingBehavior updater : component.getBehaviors(FormComponentUpdatingBehavior.class)) {
```

### DataFlowIssue
Method invocation `getBehaviors` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
		choiceSelector.doSelect(index);

		for (FormComponentUpdatingBehavior updater : component.getBehaviors(FormComponentUpdatingBehavior.class)) {
			tester.invokeListener(component, updater);
		}
```

### DataFlowIssue
Method invocation `mutable` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
			// And now all remaining elements from the derived markup.
			// But first add </wicket:child>
			WicketTag childCloseTag = (WicketTag)childTag.mutable();
			childCloseTag.getXmlTag().setType(TagType.CLOSE);
			childCloseTag.setMarkupClass(markupClass);
```

### DataFlowIssue
Casting `elem` to `ComponentTag` may produce `ClassCastException`
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
		}

		return (ComponentTag)elem;
	}

```

### DataFlowIssue
Casting `object` to `Class` will produce `ClassCastException` for any non-null value
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
				else if (Page.class.isAssignableFrom(object.getClass()))
				{
					url = requestCycle.urlFor((Class<? extends Page>)object, parameters);
				}
				else if (object instanceof IRequestHandler)
```

### DataFlowIssue
Argument `formComponent` might be null
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		FormComponent<?> formComponent = assertFormComponent(path);

		return isRequired(formComponent);
	}

```

### DataFlowIssue
Argument `formComponent` might be null
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		FormComponent<?> formComponent = assertFormComponent(path);

		return isNotRequired(formComponent);
	}

```

### DataFlowIssue
Argument `fifo.poll()` might be null
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
			while (fifo.size() > maxSize)
			{
				remove(fifo.poll());
			}
			return rtn;
```

### DataFlowIssue
Condition `submitter instanceof IFormSubmittingComponent` is redundant and can be replaced with a null check
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
				submitter = findSubmitter();

				if (submitter instanceof IFormSubmittingComponent)
				{
					IFormSubmittingComponent submittingComponent = (IFormSubmittingComponent)submitter;
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java

			// finally, when the tag is not a normal link
			if (!(tag.getName().equalsIgnoreCase("a") || tag.getName().equalsIgnoreCase("link")
				|| tag.getName().equalsIgnoreCase("area")
				|| tag.getName().equalsIgnoreCase("script")
```

### DataFlowIssue
Unboxing of `getDefaultModelObject()` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java
	public final int getIterations()
	{
		return (Integer)getDefaultModelObject();
	}

```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
			ComponentTag tag = getMarkupTag();
			// finally, when the tag is not a normal link
			if (!(tag.getName().equalsIgnoreCase("a") || tag.getName().equalsIgnoreCase("link")
				|| tag.getName().equalsIgnoreCase("area")
				|| tag.getName().equalsIgnoreCase("script")
```

### DataFlowIssue
Method invocation `getMarkup` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
		{
			Border border = findParent(Border.class);
			IMarkupFragment fragment = border.getMarkup();

			if (fragment == null)
```

### DataFlowIssue
Dereference of `getBytes()` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
			public Bytes length()
			{
				return Bytes.bytes(getBytes().length);
			}

```

### DataFlowIssue
Argument `getBytes()` might be null
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
			{
				if (inputStream == null) {
					inputStream = new ByteArrayInputStream(getBytes());				
				}
				
```

### DataFlowIssue
Argument `files` might be null
in `wicket-core/src/main/java/org/apache/wicket/pageStore/FilePageStore.java`
#### Snippet
```java
	{
		File[] files = folders.get(sessionIdentifier, true).listFiles();
		Arrays.sort(files, new LastModifiedComparator());

		long total = 0;
```

### DataFlowIssue
Dereference of `folder.listFiles()` may produce `NullPointerException`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/FilePageStore.java`
#### Snippet
```java
		for (File folder : folders.getAll())
		{
			for (File file : folder.listFiles())
			{
				String name = file.getName();
```

### DataFlowIssue
Argument `files` might be null
in `wicket-core/src/main/java/org/apache/wicket/pageStore/FilePageStore.java`
#### Snippet
```java
		{
			File[] files = folder.listFiles();
			Arrays.sort(files, new LastModifiedComparator());
			for (File file : files)
			{
```

### DataFlowIssue
Method invocation `begin` will produce `NullPointerException`
in `wicket-examples/src/main/java/org/apache/wicket/examples/cdi/ConversationPage1.java`
#### Snippet
```java
	{
		// make the conversation long running
		conversation.begin();

		add(new Label("count", new PropertyModel<Integer>(this, "counter.count")));
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/modal/ModalDialogPage.java`
#### Snippet
```java
				public void onClick()
				{
					findParent(ModalDialog.class).close(null);
				}
			});
```

### DataFlowIssue
Argument `getUploadFolder().listFiles()` might be null
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/MultiUploadPage.java`
#### Snippet
```java
			protected List<File> load()
			{
				return Arrays.asList(getUploadFolder().listFiles());
			}
		});
```

### DataFlowIssue
Argument `getUploadFolder().listFiles()` might be null
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadPage.java`
#### Snippet
```java
			protected List<File> load()
			{
				return Arrays.asList(getUploadFolder().listFiles());
			}
		});
```

### DataFlowIssue
Method invocation `toExternalForm` may produce `NullPointerException`
in `wicket-examples/src/main/java/org/apache/wicket/examples/source/SourcesPage.java`
#### Snippet
```java
					else
					{
						String absolutePath = scope.getResource("").toExternalForm();
						File basedir;
						URI uri;
```

### DataFlowIssue
Dereference of `files` may produce `NullPointerException`
in `wicket-examples/src/main/java/org/apache/wicket/examples/source/SourcesPage.java`
#### Snippet
```java
		{
			File[] files = dir.listFiles();
			for (File file : files)
			{
				if (file.isDirectory())
```

### DataFlowIssue
Argument `contact` might be null
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/FormPage.java`
#### Snippet
```java
					Contact contact = (Contact)ActionPanel.this.getDefaultModelObject();
					info("Removed contact " + contact);
					DatabaseLocator.getDatabase().delete(contact);
				}
			};
```

### DataFlowIssue
Unboxing of `counter` may produce `NullPointerException`
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/AjaxStatelessExample.java`
#### Snippet
```java
			{
				Integer counter = (Integer)incrementLabel.getDefaultModelObject();
				updateParams(getPageParameters(), counter);

				target.ifPresent(ajaxTarget -> ajaxTarget.add(incrementLabel, this));
```

## RuleId[id=MissingSerialAnnotation]
### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/LazyInitializer.java`
#### Snippet
```java
public abstract class LazyInitializer<T> implements Supplier<T>, IClusterable
{
	private static final long serialVersionUID = 1L;

	private transient volatile T instance = null;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceInjectorHolder.java`
#### Snippet
```java
public class GuiceInjectorHolder implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceInjectorHolder.java`
#### Snippet
```java
	public static final MetaDataKey<GuiceInjectorHolder> INJECTOR_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceProxyTargetLocator.java`
#### Snippet
```java
class GuiceProxyTargetLocator implements IProxyTargetLocator
{
	private static final long serialVersionUID = 1L;

	private final Annotation bindingAnnotation;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceFieldValueFactory.java`
#### Snippet
```java
	public static class MoreThanOneBindingException extends Exception
	{
		private static final long serialVersionUID = 1L;
	}
}
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteOrderMark.java`
#### Snippet
```java
public class ByteOrderMark implements Serializable {

    private static final long serialVersionUID = 1L;

    /** UTF-8 BOM */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/diff/PatchFailedException.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/diff/DifferentiationFailedException.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/diff/DiffException.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/lang/PackageName.java`
#### Snippet
```java
public class PackageName implements IClusterable
{
	private static final long serialVersionUID = 1L;

	// The name of this package
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/file/File.java`
#### Snippet
```java
public class File extends java.io.File implements IModifiable
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/file/Folder.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/lang/EnumeratedType.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;
	/** Map of type values by class */
	private static final Map<String, List<EnumeratedType>> valueListByClass = Generics.newConcurrentHashMap();
```

### MissingSerialAnnotation
`readResolve()` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/lang/EnumeratedType.java`
#### Snippet
```java
	 * @throws java.io.ObjectStreamException
	 */
	public Object readResolve() throws java.io.ObjectStreamException
	{
		EnumeratedType result = this;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Threads.java`
#### Snippet
```java
	private static class ThreadDump extends RuntimeException
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Bytes.java`
#### Snippet
```java
public final class Bytes extends LongValue
{
	private static final long serialVersionUID = 1L;

	/** Pattern for string parsing. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/GroupNotBoundException.java`
#### Snippet
```java
public final class GroupNotBoundException extends RuntimeException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/IntegerGroup.java`
#### Snippet
```java
public final class IntegerGroup extends Group
{
	private static final long serialVersionUID = 1L;

	/** The radix to use when converting Strings captured by this group. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/FloatingPointGroup.java`
#### Snippet
```java
public final class FloatingPointGroup extends Group
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/GroupAlreadyBoundException.java`
#### Snippet
```java
public final class GroupAlreadyBoundException extends RuntimeException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/Group.java`
#### Snippet
```java
public class Group extends MetaPattern
{
	private static final long serialVersionUID = 1L;

	/** The capturing group that this Group is bound to. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/BooleanGroup.java`
#### Snippet
```java
public final class BooleanGroup extends Group
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/OptionalMetaPattern.java`
#### Snippet
```java
public final class OptionalMetaPattern extends MetaPattern
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/MetaPattern.java`
#### Snippet
```java
public class MetaPattern implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/value/IntValue.java`
#### Snippet
```java
public class IntValue implements Comparable<IntValue>, Serializable
{
	private static final long serialVersionUID = 1L;

	/** the <code>int</code> value */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/value/AttributeMap.java`
#### Snippet
```java
public final class AttributeMap extends ValueMap
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/value/LongValue.java`
#### Snippet
```java
public class LongValue implements Comparable<LongValue>, Serializable
{
	private static final long serialVersionUID = 1L;

	/** the <code>long</code> value */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValueConversionException.java`
#### Snippet
```java
public final class StringValueConversionException extends ConversionException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/value/CopyOnWriteValueMap.java`
#### Snippet
```java
public class CopyOnWriteValueMap implements IValueMap, Serializable
{
	private static final long serialVersionUID = 1L;

	/** the wrapped <code>IValueMap</code> */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/string/AbstractStringList.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringList.java`
#### Snippet
```java
public final class StringList extends AbstractStringList
{
	private static final long serialVersionUID = 1L;

	// The underlying list of strings
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java
public class ConversionException extends RuntimeException
{
	private static final long serialVersionUID = 1L;

	/** The converter that was used. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ZonedDateTimeConverter.java`
#### Snippet
```java
public class ZonedDateTimeConverter extends AbstractJavaTimeConverter<ZonedDateTime>
{
	private static final long serialVersionUID = 1L;

	private static final DateTimeFormatter DATE_TIME_FORMATTER = DateTimeFormatter.ofLocalizedDateTime(FormatStyle.MEDIUM, FormatStyle.FULL);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/MaskConverter.java`
#### Snippet
```java
public class MaskConverter<C> implements IConverter<C>
{
	private static final long serialVersionUID = 1L;

	/** Object that knows all about masks. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ByteConverter.java`
#### Snippet
```java
public class ByteConverter extends AbstractIntegerConverter<Byte>
{
	private static final long serialVersionUID = 1L;

	private static final BigDecimal MIN_VALUE = new BigDecimal(Byte.MIN_VALUE);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/SqlDateConverter.java`
#### Snippet
```java
public class SqlDateConverter extends AbstractDateConverter<Date>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/SqlTimestampConverter.java`
#### Snippet
```java
public class SqlTimestampConverter extends AbstractDateConverter<Timestamp>
{
	private static final long serialVersionUID = 1L;

	private final int dateFormat;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractConverter.java`
#### Snippet
```java
public abstract class AbstractConverter<C> implements IConverter<C>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
public class StringValue implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private static final Logger LOG = LoggerFactory.getLogger(StringValue.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/DoubleConverter.java`
#### Snippet
```java
public class DoubleConverter extends AbstractDecimalConverter<Double>
{
	private static final long serialVersionUID = 1L;

	// Double.MIN is the smallest nonzero positive number, not the largest
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ShortConverter.java`
#### Snippet
```java
public class ShortConverter extends AbstractIntegerConverter<Short>
{
	private static final long serialVersionUID = 1L;

	private static final BigDecimal MIN_VALUE = new BigDecimal(Short.MIN_VALUE);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/value/ValueMap.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/IntegerConverter.java`
#### Snippet
```java
public class IntegerConverter extends AbstractIntegerConverter<Integer>
{
	private static final long serialVersionUID = 1L;

	private static final BigDecimal MIN_VALUE = new BigDecimal(Integer.MIN_VALUE);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/CalendarConverter.java`
#### Snippet
```java
public class CalendarConverter implements IConverter<Calendar>
{
	private static final long serialVersionUID = 1L;

	private final IConverter<Date> dateConverter;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/BooleanConverter.java`
#### Snippet
```java
public class BooleanConverter extends AbstractConverter<Boolean>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractNumberConverter.java`
#### Snippet
```java
public abstract class AbstractNumberConverter<N extends Number> extends AbstractConverter<N>
{
	private static final long serialVersionUID = 1L;

	/** The date format to use */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractDecimalConverter.java`
#### Snippet
```java
public abstract class AbstractDecimalConverter<N extends Number> extends AbstractNumberConverter<N>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/FloatConverter.java`
#### Snippet
```java
public class FloatConverter extends AbstractDecimalConverter<Float>
{
	private static final long serialVersionUID = 1L;

	// Float.MIN is the smallest nonzero positive number, not the largest
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/DateConverter.java`
#### Snippet
```java
public class DateConverter extends AbstractDateConverter<Date>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractJavaTimeConverter.java`
#### Snippet
```java
public abstract class AbstractJavaTimeConverter<T extends Temporal> extends AbstractConverter<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractIntegerConverter.java`
#### Snippet
```java
public abstract class AbstractIntegerConverter<I extends Number> extends AbstractNumberConverter<I>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/SqlTimeConverter.java`
#### Snippet
```java
public class SqlTimeConverter extends AbstractDateConverter<Time>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LocalDateConverter.java`
#### Snippet
```java
public class LocalDateConverter extends AbstractJavaTimeConverter<LocalDate>
{
	private static final long serialVersionUID = 1L;

	private static final DateTimeFormatter SHORT_DATE_TIME_FORMATTER = DateTimeFormatter.ofLocalizedDate(FormatStyle.SHORT);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/CharacterConverter.java`
#### Snippet
```java
public class CharacterConverter extends AbstractConverter<Character>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/BigIntegerConverter.java`
#### Snippet
```java
public class BigIntegerConverter extends AbstractIntegerConverter<BigInteger>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/BigDecimalConverter.java`
#### Snippet
```java
public class BigDecimalConverter extends AbstractDecimalConverter<BigDecimal>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LocalDateTimeConverter.java`
#### Snippet
```java
public class LocalDateTimeConverter extends AbstractJavaTimeConverter<LocalDateTime>
{
	private static final long serialVersionUID = 1L;

	private static final DateTimeFormatter DATE_TIME_FORMATTER = DateTimeFormatter.ofLocalizedDateTime(FormatStyle.MEDIUM);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LongConverter.java`
#### Snippet
```java
public class LongConverter extends AbstractIntegerConverter<Long>
{
	private static final long serialVersionUID = 1L;

	private static final BigDecimal MIN_VALUE = new BigDecimal(Long.MIN_VALUE);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractDateConverter.java`
#### Snippet
```java
public abstract class AbstractDateConverter<D extends Date> extends AbstractConverter<D>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LocalTimeConverter.java`
#### Snippet
```java
public class LocalTimeConverter extends AbstractJavaTimeConverter<LocalTime>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
{
	/** use serialVersionUID from JDK 1.0.2 for interoperability */
	private static final long serialVersionUID = 1L;

	private static final AppendingStringBuffer NULL = new AppendingStringBuffer("null");
```

### MissingSerialAnnotation
`readObject()` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @throws IOException
	 */
	private void readObject(final java.io.ObjectInputStream s) throws IOException,
		ClassNotFoundException
	{
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/listener/ChangeListenerSet.java`
#### Snippet
```java
public final class ChangeListenerSet<T> extends ListenerCollection<IChangeListener<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/StringResourceStream.java`
#### Snippet
```java
public final class StringResourceStream extends AbstractStringResourceStream
{
	private static final long serialVersionUID = 1L;

	/** The string resource */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/listener/ListenerCollection.java`
#### Snippet
```java
public abstract class ListenerCollection<T> implements Serializable, Iterable<T>
{
	private static final long serialVersionUID = 1L;

	private static final Logger logger = LoggerFactory.getLogger(ListenerCollection.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;
	private final transient ByteArrayOutputStream out;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/StringBufferResourceStream.java`
#### Snippet
```java
public class StringBufferResourceStream extends AbstractStringResourceStream
{
	private static final long serialVersionUID = 1L;

	/** Stylesheet information */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceStreamNotFoundException.java`
#### Snippet
```java
public final class ResourceStreamNotFoundException extends Exception
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStreamWriter.java`
#### Snippet
```java
public abstract class AbstractResourceStreamWriter implements IResourceStreamWriter
{
	private static final long serialVersionUID = 1L;

	private Locale locale;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java
		IStringResourceStream
{
	private static final long serialVersionUID = 1L;

	/** The content-type applied in case the resource stream's default constructor is used */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceStreamWrapper.java`
#### Snippet
```java
public class ResourceStreamWrapper implements IResourceStream
{
	private static final long serialVersionUID = 1L;

	private final IResourceStream delegate;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStream.java`
#### Snippet
```java
public abstract class AbstractResourceStream implements IResourceStream
{
	private static final long serialVersionUID = 1L;

	private Locale locale;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/FileSystemResourceStream.java`
#### Snippet
```java
		IFixedLocationResourceStream
{
	private static final long serialVersionUID = 1L;

	/** Any associated path */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/FileResourceStream.java`
#### Snippet
```java
		IFixedLocationResourceStream
{
	private static final long serialVersionUID = 1L;

	/** Any associated file */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MultiMap.java`
#### Snippet
```java
public class MultiMap<K, V> extends HashMap<K, List<V>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MostRecentlyUsedMap.java`
#### Snippet
```java
public class MostRecentlyUsedMap<K, V> extends LinkedHashMap<K, V>
{
	private static final long serialVersionUID = 1L;

	/** Value most recently removed from map */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/collections/UrlExternalFormComparator.java`
#### Snippet
```java
public class UrlExternalFormComparator implements Comparator<URL>, Serializable
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java
public class ZipResourceStream extends AbstractResourceStream
{
	private static final long serialVersionUID = 1L;

	private static final Logger log = LoggerFactory.getLogger(ZipResourceStream.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java
public class MiniMap<K, V> implements Map<K, V>, Serializable
{
	private static final long serialVersionUID = 1L;

	/** The array of keys. Keys that are null are not used. */
```

### MissingSerialAnnotation
`writeObject()` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	 * 
	 */
	private void writeObject(final java.io.ObjectOutputStream s) throws IOException
	{
		// Write out the threshold, loadfactor, and any hidden stuff
```

### MissingSerialAnnotation
`readObject()` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	private void readObject(final java.io.ObjectInputStream s) throws IOException,
		ClassNotFoundException
	{
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 362498820763181265L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/bytebuddy/ByteBuddyInterceptor.java`
#### Snippet
```java
		IWriteReplace
{
	private static final long serialVersionUID = 1L;

	protected final IProxyTargetLocator locator;
```

### MissingSerialAnnotation
`writeReplace()` can be annotated with '@Serial' annotation
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/bytebuddy/ByteBuddyInterceptor.java`
#### Snippet
```java

	@Override
	public Object writeReplace() throws ObjectStreamException
	{
		return new ProxyReplacement(typeName, locator);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/LazyInitProxyFactory.java`
#### Snippet
```java
	public static final class ProxyReplacement implements IClusterable
	{
		private static final long serialVersionUID = 1L;

		private final IProxyTargetLocator locator;
```

### MissingSerialAnnotation
`readResolve()` can be annotated with '@Serial' annotation
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/LazyInitProxyFactory.java`
#### Snippet
```java
		}

		private Object readResolve() throws ObjectStreamException
		{
			Class<?> clazz = WicketObjects.resolveClass(type);
```

### MissingSerialAnnotation
`writeReplace()` can be annotated with '@Serial' annotation
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java
		 */
		@Override
		public Object writeReplace() throws ObjectStreamException
		{
			return new ProxyReplacement(typeName, locator);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java
			IWriteReplace
	{
		private static final long serialVersionUID = 1L;

		private final IProxyTargetLocator locator;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-spring/src/main/java/org/apache/wicket/spring/test/SpringContextLocatorMock.java`
#### Snippet
```java
public class SpringContextLocatorMock implements ISpringContextLocator
{
	private static final long serialVersionUID = 1L;

	private final ApplicationContext context;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-ioc/src/main/java/org/apache/wicket/injection/Injector.java`
#### Snippet
```java
	private static final MetaDataKey<Injector> KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/DevelopmentUtilitiesNotEnabledException.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/SpringComponentInjector.java`
#### Snippet
```java
		private transient ApplicationContext context;

		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/SpringComponentInjector.java`
#### Snippet
```java
	private static MetaDataKey<ApplicationContext> CONTEXT_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;

	};
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/SessionSizeDebugPanel.java`
#### Snippet
```java
	public static final IDebugBarContributor DEBUG_BAR_CONTRIB = new IDebugBarContributor()
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/SessionSizeDebugPanel.java`
#### Snippet
```java
		return new IModel<String>()
		{
			private static final long serialVersionUID = 1L;

			private final IModel<Bytes> size = new SessionSizeModel();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/SessionSizeDebugPanel.java`
#### Snippet
```java
public class SessionSizeDebugPanel extends StandardDebugPanel
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/StandardDebugPanel.java`
#### Snippet
```java
public abstract class StandardDebugPanel extends DevUtilsPanel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/DevUtilsPage.java`
#### Snippet
```java
public class DevUtilsPage extends WebPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/DevUtilsPanel.java`
#### Snippet
```java
public class DevUtilsPanel extends Panel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
public class SpringBeanLocator implements IProxyTargetLocator
{
	private static final long serialVersionUID = 1L;

	// Weak reference so we don't hold up WebApp classloader garbage collection.
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/VersionDebugContributor.java`
#### Snippet
```java
		Label label = new Label(id, new IModel<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/VersionDebugContributor.java`
#### Snippet
```java
public class VersionDebugContributor implements IDebugBarContributor
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/InspectorDebugPanel.java`
#### Snippet
```java
public class InspectorDebugPanel extends StandardDebugPanel
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/InspectorDebugPanel.java`
#### Snippet
```java
	public static final IDebugBarContributor DEBUG_BAR_CONTRIB = new IDebugBarContributor()
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/SessionSizeModel.java`
#### Snippet
```java
public class SessionSizeModel implements IModel<Bytes>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/PageStoreDebugPanel.java`
#### Snippet
```java
	public static final IDebugBarContributor DEBUG_BAR_CONTRIB = new IDebugBarContributor()
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/PageStoreDebugPanel.java`
#### Snippet
```java
		return new IModel<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/PageStoreDebugPanel.java`
#### Snippet
```java
public class PageStoreDebugPanel extends StandardDebugPanel
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/SessionView.java`
#### Snippet
```java
public final class SessionView extends DevUtilsPanel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-spring/src/main/java/org/apache/wicket/spring/test/ApplicationContextMock.java`
#### Snippet
```java
public class ApplicationContextMock extends AbstractApplicationContext implements Serializable
{
	private static final long serialVersionUID = 1L;

	private final DefaultListableBeanFactory beanFactory;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/RenderPerformanceListener.java`
#### Snippet
```java
	private static class RenderMeasuringBehavior extends Behavior
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/InspectorPage.java`
#### Snippet
```java
public final class InspectorPage extends DevUtilsPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/ApplicationView.java`
#### Snippet
```java
public final class ApplicationView extends DevUtilsPanel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/DebugBar.java`
#### Snippet
```java
		add(AttributeModifier.replace("class", new IModel<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/DebugBar.java`
#### Snippet
```java
public class DebugBar extends DevUtilsPanel
{
	private static final long serialVersionUID = 1L;

	private static final MetaDataKey<List<IDebugBarContributor>> CONTRIBS_META_KEY = new MetaDataKey<>()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/DebugBar.java`
#### Snippet
```java
	private static final MetaDataKey<List<IDebugBarContributor>> CONTRIBS_META_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/debugbar/DebugBar.java`
#### Snippet
```java
		section.add(new ListView<IDebugBarContributor>("contributors", contributors)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessCheckFailureException.java`
#### Snippet
```java
public class StatelessCheckFailureException  extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;

	private Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/RequestHandlerExecutor.java`
#### Snippet
```java
	public static class ReplaceHandlerException extends RuntimeException
	{
		private static final long serialVersionUID = 1L;

		private final boolean removeScheduled;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/RequestsPage.java`
#### Snippet
```java
			requestsModel, 50)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/RequestsPage.java`
#### Snippet
```java
public class RequestsPage extends DevUtilsPage
{
	private static final long serialVersionUID = 1L;
	private final SimpleDateFormat sdf = new SimpleDateFormat("dd MMM hh:mm:ss.SSS");

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/RequestsPage.java`
#### Snippet
```java
		IModel<List<RequestData>> requestsModel = new IModel<List<RequestData>>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
		link.add(new Label("toggletext", new Model<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
public class LiveSessionsPage extends DevUtilsPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
		add(new Label("peakSessions", new Model<Integer>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
		add(new Label("totalSessions", new Model<Integer>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
		IModel<List<SessionData>> sessionModel = new IModel<List<SessionData>>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
		add(new Label("liveSessions", new Model<Integer>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
		Link<Void> link = new Link<Void>("togglelink")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
				Link<Void> link = new Link<Void>("id")
				{
					private static final long serialVersionUID = 1L;

					/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java
			sessionModel, 50)
		{
			private static final long serialVersionUID = 1L;

			private final SimpleDateFormat sdf = new SimpleDateFormat("dd MMM hh:mm:ss.SSS");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/flow/RedirectToUrlException.java`
#### Snippet
```java
public class RedirectToUrlException extends ReplaceHandlerException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/http/flow/AbortWithHttpErrorCodeException.java`
#### Snippet
```java
public final class AbortWithHttpErrorCodeException extends ResetResponseException
{
	private static final long serialVersionUID = 1L;

	private final int errorCode;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/flow/ResetResponseException.java`
#### Snippet
```java
public abstract class ResetResponseException extends ReplaceHandlerException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/handler/logger/NoLogData.java`
#### Snippet
```java
public final class NoLogData implements ILogData
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/handler/logger/DelegateLogData.java`
#### Snippet
```java
public class DelegateLogData implements ILogData
{
	private static final long serialVersionUID = 1L;

	private final ILogData delegateData;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/AjaxDisableComponentListener.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = -3772784701483881109L;
	private static final String DISABLE_ENABLE_JS = ";document.getElementById('%s').disabled = %s;";

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxIndicatorAppender.java`
#### Snippet
```java
public class AjaxIndicatorAppender extends Behavior
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxLink.java`
#### Snippet
```java
public abstract class IndicatingAjaxLink<T> extends AjaxLink<T> implements IAjaxIndicatorAware
{
	private static final long serialVersionUID = 1L;
	private final AjaxIndicatorAppender indicatorAppender = new AjaxIndicatorAppender();

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/parameter/PageParameters.java`
#### Snippet
```java
public class PageParameters implements IClusterable, IIndexedParameters, INamedParameters
{
	private static final long serialVersionUID = 1L;

	private List<String> indexedParameters;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/AjaxDownloadBehavior.java`
#### Snippet
```java
	private class ResourceBehavior extends Behavior implements IRequestListener
	{
		private static final long serialVersionUID = 1L;
		private final IResource resource;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/AjaxDownloadBehavior.java`
#### Snippet
```java
public class AjaxDownloadBehavior extends AbstractDefaultAjaxBehavior
{
	private static final long serialVersionUID = 1L;

	public enum Location {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
public abstract class AjaxLazyLoadPanel<T extends Component> extends Panel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
	static class AjaxLazyLoadTimer extends AbstractAjaxTimerBehavior
	{
		private static final long serialVersionUID = 1L;

		public AjaxLazyLoadTimer()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		add(new AjaxFallbackLink<Void>("collapseAll")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
public final class EnhancedPageView extends GenericPanel<Page>
{
	private static final long serialVersionUID = 1L;

	private ExpandState expandState;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		form.add(new AjaxFallbackButton("submit", form)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		columns.add(new PropertyColumn<TreeNode, Void>(Model.of("Model Object"), "model")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		columns.add(new PropertyColumn<TreeNode, Void>(Model.of("Render time (ms)"), "renderTime")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		columns.add(new TreeColumn<TreeNode, Void>(Model.of("Tree"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		add(new Label("info", new Model<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		columns.add(new AbstractColumn<TreeNode, Void>(Model.of("Size"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		columns.add(new PropertyColumn<TreeNode, Void>(Model.of("Stateless"), "stateless")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		columns.add(new PropertyColumn<TreeNode, Void>(Model.of("Path"), "path")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		IModel<Set<TreeNode>> expandStateModel = new LoadableDetachableModel<Set<TreeNode>>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		Model<String> pageRenderDuration = new Model<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
			provider, Integer.MAX_VALUE, expandStateModel)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
	private class TreeProvider extends SortableTreeProvider<TreeNode, Void>
	{
		private static final long serialVersionUID = 1L;

		private TreeModel componentTreeModel = new TreeModel();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		add(new AjaxFallbackLink<Void>("expandAll")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
	private static class ExpandState implements Set<TreeNode>, IClusterable
	{
		private static final long serialVersionUID = 1L;

		private HashSet<List<Integer>> set = new HashSet<>();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		columns.add(new PropertyColumn<TreeNode, Void>(Model.of("Type"), "type")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		private class TreeNodeModel extends LoadableDetachableModel<TreeNode>
		{
			private static final long serialVersionUID = 1L;

			private List<Integer> path;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
		private class TreeModel extends LoadableDetachableModel<TreeNode>
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxFallbackLink.java`
#### Snippet
```java
		IAjaxIndicatorAware
{
	private static final long serialVersionUID = 1L;

	private final AjaxIndicatorAppender indicatorAppender = new AjaxIndicatorAppender();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/form/upload/UploadStatusResource.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private static final String UPLOAD_PARAMETER = "upload";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/modal/TrapFocusBehavior.java`
#### Snippet
```java
	public static final String CSS_CURRENT_KEY = CssUtils.key(TrapFocusBehavior.class, "current");

	private static final long serialVersionUID = 1L;
	
	private static final ResourceReference JS = new JavaScriptResourceReference(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/modal/theme/DefaultTheme.java`
#### Snippet
```java
public class DefaultTheme extends Behavior
{
	private static final long serialVersionUID = 1L;

	private static final ResourceReference CSS = new CssResourceReference(DefaultTheme.class, "theme.css");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/modal/ModalDialog.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private static final String OVERLAY_ID = "overlay";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/form/upload/UploadProgressBar.java`
#### Snippet
```java
	private static final String RESOURCE_NAME = UploadProgressBar.class.getName();

	private static final long serialVersionUID = 1L;

	private Form<?> form;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/AjaxListPanel.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;
	
	private WebMarkupContainer container;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/sort/AjaxOrderByLink.java`
#### Snippet
```java
		return new AjaxEventBehavior(event)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/sort/AjaxOrderByLink.java`
#### Snippet
```java
	 *
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/table/AjaxNavigationToolbar.java`
#### Snippet
```java
		return new AjaxPagingNavigator(navigatorId, table)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/table/AjaxNavigationToolbar.java`
#### Snippet
```java
public class AjaxNavigationToolbar extends NavigationToolbar
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/IndicatingAjaxButton.java`
#### Snippet
```java
public abstract class IndicatingAjaxButton extends AjaxButton implements IAjaxIndicatorAware
{
	private static final long serialVersionUID = 1L;

	private final AjaxIndicatorAppender indicatorAppender = new AjaxIndicatorAppender();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/tabs/AjaxTabbedPanel.java`
#### Snippet
```java
public class AjaxTabbedPanel<T extends ITab> extends TabbedPanel<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/tabs/AjaxTabbedPanel.java`
#### Snippet
```java
		return new AjaxFallbackLink<Void>(linkId)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
		TextArea<T> editor = new TextArea<T>(componentId, model)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
public class AjaxEditableMultiLineLabel<T> extends AjaxEditableLabel<T>
{
	private static final long serialVersionUID = 1L;

	/** text area's number of rows. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
		editor.add(new EditorAjaxBehavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableMultiLineLabel.java`
#### Snippet
```java
		MultiLineLabel label = new MultiLineLabel(componentId, model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/StringAutoCompleteRenderer.java`
#### Snippet
```java
public final class StringAutoCompleteRenderer extends AbstractAutoCompleteTextRenderer<Object>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AbstractAutoCompleteBehavior.java`
#### Snippet
```java
		AutoCompleteBehavior.class, "wicket-autocomplete.js");

	private static final long serialVersionUID = 1L;

	protected AutoCompleteSettings settings;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/table/AjaxFallbackDefaultDataTable.java`
#### Snippet
```java
public class AjaxFallbackDefaultDataTable<T, S> extends DataTable<T, S>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AbstractAutoCompleteTextRenderer.java`
#### Snippet
```java
public abstract class AbstractAutoCompleteTextRenderer<T> extends AbstractAutoCompleteRenderer<T>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/sort/AjaxFallbackOrderByBorder.java`
#### Snippet
```java
		return new AjaxOrderByLink<S>("orderByLink", property, stateLocator)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/sort/AjaxFallbackOrderByBorder.java`
#### Snippet
```java
public abstract class AjaxFallbackOrderByBorder<S> extends OrderByBorder<S>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AbstractAutoCompleteRenderer.java`
#### Snippet
```java
public abstract class AbstractAutoCompleteRenderer<T> implements IAutoCompleteRenderer<T>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/DefaultCssAutoCompleteTextField.java`
#### Snippet
```java
public abstract class DefaultCssAutoCompleteTextField<T> extends AutoCompleteTextField<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/table/AjaxFallbackHeadersToolbar.java`
#### Snippet
```java
public class AjaxFallbackHeadersToolbar<S> extends HeadersToolbar<S>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/table/AjaxFallbackHeadersToolbar.java`
#### Snippet
```java
		return new AjaxFallbackOrderByBorder<S>(borderId, property, locator)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteSettings.java`
#### Snippet
```java
public final class AutoCompleteSettings implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private boolean preselect = false;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/model/AbstractCheckBoxModel.java`
#### Snippet
```java
public abstract class AbstractCheckBoxModel implements IModel<Boolean>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
	protected class EditorAjaxBehavior extends AbstractDefaultAjaxBehavior
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
		Label label = new Label(componentId, model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
		TextField<T> editor = new TextField<T>(componentId, model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
public class AjaxEditableLabel<T> extends Panel implements IGenericComponent<T, AjaxEditableLabel<T>>
{
	private static final long serialVersionUID = 1L;

	/** editor component. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
	protected class LabelAjaxBehavior extends AjaxEventBehavior
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/SelectOption.java`
#### Snippet
```java
public class SelectOption<T> extends WebMarkupContainer implements IGenericComponent<T, SelectOption<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/theme/DefaultTheme.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/** reference to the palette's css resource */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteBehavior.java`
#### Snippet
```java
public abstract class AutoCompleteBehavior<T> extends AbstractAutoCompleteBehavior
{
	private static final long serialVersionUID = 1L;

	private final IAutoCompleteRenderer<T> renderer;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/Select.java`
#### Snippet
```java
public class Select<T> extends FormComponent<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
		converter = new DateConverter()
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private static final String DEFAULT_PATTERN = "MM/dd/yyyy";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/component/Selection.java`
#### Snippet
```java
public class Selection<T> extends AbstractOptions<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
public class Url implements Serializable
{
	private static final long serialVersionUID = 1L;

	private final List<String> segments;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
	public final static class QueryParameter implements Serializable
	{
		private static final long serialVersionUID = 1L;

		private final String name;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/component/Choices.java`
#### Snippet
```java
public class Choices<T> extends AbstractOptions<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/component/AbstractOptions.java`
#### Snippet
```java
public abstract class AbstractOptions<T> extends FormComponent<T>
{
	private static final long serialVersionUID = 1L;

	private final Palette<T> palette;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/component/Recorder.java`
#### Snippet
```java
public class Recorder<T> extends HiddenField<String>
{
	private static final long serialVersionUID = 1L;

	/** parent palette object */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/ZonedToLocalDateTimeModel.java`
#### Snippet
```java
public class ZonedToLocalDateTimeModel implements IModel<LocalDateTime>
{
	private static final long serialVersionUID = 1L;
	private IModel<ZonedDateTime> model;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
		return new PaletteButton("moveDownButton")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
		return new Choices<T>("choices", this)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
		return new PaletteButton("removeButton")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
	private static final String AVAILABLE_HEADER_ID = "availableHeader";

	private static final long serialVersionUID = 1L;

	/** collection containing all available choices */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
		return new PaletteButton("addAllButton")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
	{

		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
		return new PaletteButton("removeAllButton")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
		return new PaletteButton("moveUpButton")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
		return new Selection<T>("selection", this)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
		return new PaletteButton("addButton")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeField.java`
#### Snippet
```java
public class LocalDateTimeField extends AbstractDateTimeField<LocalDateTime>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/ZonedDateTimeField.java`
#### Snippet
```java
public class ZonedDateTimeField extends AbstractDateTimeField<ZonedDateTime>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/SelectOptions.java`
#### Snippet
```java
public class SelectOptions<T> extends RepeatingView
{
	private static final long serialVersionUID = 1L;

	private static final Logger log = LoggerFactory.getLogger(SelectOptions.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/SelectOptions.java`
#### Snippet
```java
	private static class SimpleSelectOption<V> extends SelectOption<V>
	{
		private static final long serialVersionUID = 1L;

		private final String text;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeTextField.java`
#### Snippet
```java
public class LocalDateTimeTextField extends TextField<LocalDateTime> implements ITextFormatProvider
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeTextField.java`
#### Snippet
```java

		this.converter = new TextFormatConverter() {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeTextField.java`
#### Snippet
```java

		this.converter = new TextFormatConverter() {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeTextField.java`
#### Snippet
```java

	private abstract class TextFormatConverter extends LocalDateTimeConverter {
		private static final long serialVersionUID = 1L;

		public abstract String getTextFormat(Locale locale);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalTimeTextField.java`
#### Snippet
```java

		this.converter = new TextFormatConverter() {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalTimeTextField.java`
#### Snippet
```java

	private abstract class TextFormatConverter extends LocalTimeConverter {
		private static final long serialVersionUID = 1L;

		public abstract String getTextFormat(Locale locale);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalTimeTextField.java`
#### Snippet
```java

		this.converter = new TextFormatConverter() {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalTimeTextField.java`
#### Snippet
```java
public class LocalTimeTextField extends TextField<LocalTime> implements ITextFormatProvider
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/PanelCachingTab.java`
#### Snippet
```java
public class PanelCachingTab implements ITab
{
	private static final long serialVersionUID = 1L;

	// Might Panel or Fragment or ...
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/AbstractTab.java`
#### Snippet
```java
public abstract class AbstractTab implements ITab
{
	private static final long serialVersionUID = 1L;

	IModel<String> title;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
	private class TimeModel implements IModel<LocalTime>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
	{
		return new TimeField(id, timeFieldModel) {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
		add(new Label("timeSeparator", new ResourceModel("AbstractDateTimeField.timeSeparator"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
abstract class AbstractDateTimeField<T extends Temporal> extends FormComponentPanel<T>
{
	private static final long serialVersionUID = 1L;

	public static final String DATE_CSS_CLASS_KEY = CssUtils.key(AbstractDateTimeField.class, "date");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
	private class DateModel implements IModel<LocalDate>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java
	{
		return new LocalDateTextField(id, dateFieldModel, FormatStyle.SHORT) {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/basic/SmartLinkMultiLineLabel.java`
#### Snippet
```java
public class SmartLinkMultiLineLabel extends MultiLineLabel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/basic/SmartLinkLabel.java`
#### Snippet
```java
public class SmartLinkLabel extends Label
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/PropertyPopulator.java`
#### Snippet
```java
public class PropertyPopulator<T> implements ICellPopulator<T>
{
	private static final long serialVersionUID = 1L;
	private final String property;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/image/resource/ThumbnailImageResource.java`
#### Snippet
```java
public class ThumbnailImageResource extends DynamicImageResource
{
	private static final long serialVersionUID = 1L;

	/** the unscaled, original image resource. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
		return new Link<Void>(linkId)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/DataGridView.java`
#### Snippet
```java
public class DataGridView<T> extends AbstractDataGridView<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
		return new LoopItem(tabIndex)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
		final IModel<Integer> tabCount = new IModel<Integer>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
		return new WebMarkupContainer(id)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
		tabsContainer.add(new Loop("tabs", tabCount)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
public class TabbedPanel<T extends ITab> extends Panel
{
	private static final long serialVersionUID = 1L;

	/** id used for child panels */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NavigationToolbar.java`
#### Snippet
```java
public class NavigationToolbar extends AbstractToolbar
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByLink.java`
#### Snippet
```java
public class OrderByLink<S> extends Link<Void>
{
	private static final long serialVersionUID = 1L;

	/** sortable property */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java

		this.converter = new TextFormatConverter() {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java
public class LocalDateTextField extends TextField<LocalDate> implements ITextFormatProvider
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java

		this.converter = new TextFormatConverter() {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java

	private abstract class TextFormatConverter extends LocalDateConverter {
		private static final long serialVersionUID = 1L;

		public abstract String getTextFormat(Locale locale);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/AbstractDataGridView.java`
#### Snippet
```java
public abstract class AbstractDataGridView<T> extends DataViewBase<T>
{
	private static final long serialVersionUID = 1L;

	private static final String CELL_REPEATER_ID = "cells";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/AbstractColumn.java`
#### Snippet
```java
public abstract class AbstractColumn<T, S> implements IStyledColumn<T, S>
{
	private static final long serialVersionUID = 1L;

	private final IModel<String> displayModel;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
		return new OrderByLink<S>(id, property, stateLocator)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
public class OrderByBorder<S> extends Border
{
	private static final long serialVersionUID = 1L;

	public static final String SORT_ASCENDING_CSS_CLASS_KEY = CssUtils.key(OrderByLink.class, "ascending");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/ColGroup.java`
#### Snippet
```java
	public class Col extends WebMarkupContainer
	{
		private static final long serialVersionUID = 1L;

		public Col(Behavior... behaviors)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/ColGroup.java`
#### Snippet
```java
public class ColGroup extends Panel
{
	private static final long serialVersionUID = 1L;

	private final RepeatingView colgroupCols;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/PropertyColumn.java`
#### Snippet
```java
public class PropertyColumn<T, S> extends AbstractColumn<T, S> implements IExportableColumn<T, S>
{
	private static final long serialVersionUID = 1L;

	private final String propertyExpression;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
	private static final class CharAttributes implements IClusterable
	{
		private static final long serialVersionUID = 1L;
		private final char c;
		private final String name;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 1L;

	private static int randomInt(final Random rng, final int min, final int max)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DefaultDataTable.java`
#### Snippet
```java
public class DefaultDataTable<T, S> extends DataTable<T, S>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NavigatorLabel.java`
#### Snippet
```java
public class NavigatorLabel extends Label
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NavigatorLabel.java`
#### Snippet
```java
	public static class LabelModelObject implements IClusterable
	{
		private static final long serialVersionUID = 1L;
		private final IPageableItems pageable;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/AbstractToolbar.java`
#### Snippet
```java
public abstract class AbstractToolbar extends Panel
{
	private static final long serialVersionUID = 1L;

	private final DataTable<?, ?> table;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/HeaderlessColumn.java`
#### Snippet
```java
public abstract class HeaderlessColumn<T, S> extends AbstractColumn<T, S>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/LambdaColumn.java`
#### Snippet
```java
public class LambdaColumn<T, S> extends AbstractColumn<T, S> implements IExportableColumn<T, S>
{
	private static final long serialVersionUID = 1L;
	
	private final SerializableFunction<T, ?> function;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/LambdaColumn.java`
#### Snippet
```java
		IModel<Object> dataModel = new IModel<Object>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NoRecordsToolbar.java`
#### Snippet
```java
		td.add(AttributeModifier.replace("colspan", new IModel<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NoRecordsToolbar.java`
#### Snippet
```java
public class NoRecordsToolbar extends AbstractToolbar
{
	private static final long serialVersionUID = 1L;

	private static final IModel<String> DEFAULT_MESSAGE_MODEL = new ResourceModel(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/AbstractFilter.java`
#### Snippet
```java
public class AbstractFilter extends Panel
{
	private static final long serialVersionUID = 1L;

	private final FilterForm<?> form;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/HeadersToolbar.java`
#### Snippet
```java
		return new OrderByBorder<S>(headerId, property, locator)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/HeadersToolbar.java`
#### Snippet
```java
		RefreshingView<IColumn<T, S>> headers = new RefreshingView<IColumn<T, S>>("headers")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/HeadersToolbar.java`
#### Snippet
```java
public class HeadersToolbar<S> extends AbstractToolbar
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/HeadersToolbar.java`
#### Snippet
```java
					CssAttributeBehavior cssAttributeBehavior = new DataTable.CssAttributeBehavior()
					{
						private static final long serialVersionUID = 1L;

						@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
				item.add(new CssAttributeBehavior()
				{
					private static final long serialVersionUID = 1L;

					@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
	private static class ToolbarsContainer extends WebMarkupContainer
	{
		private static final long serialVersionUID = 1L;

		private final RepeatingView toolbars;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 1L;

	private final DataGridView<T> datagrid;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
	static abstract class CssAttributeBehavior extends Behavior
	{
		private static final long serialVersionUID = 1L;

		protected abstract String getCssClass();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
		/**
		 */
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/NoFilter.java`
#### Snippet
```java
public class NoFilter extends Panel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/TextFilteredPropertyColumn.java`
#### Snippet
```java
public class TextFilteredPropertyColumn<T, F, S> extends FilteredPropertyColumn<T, S>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilteredAbstractColumn.java`
#### Snippet
```java
		IFilteredColumn<T, S>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
	protected class HoursModel implements IModel<Integer>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
	private static final IConverter<Integer> MINUTES_CONVERTER = new IntegerConverter()
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
		add(amOrPmChoice = new DropDownChoice<AM_PM>("amOrPmChoice", new AmPmModel(),
			Arrays.asList(AM_PM.values())) {
				private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
	protected class AmPmModel implements IModel<AM_PM>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
	protected class MinutesModel implements IModel<Integer>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
		TextField<Integer> hoursTextField = new TextField<Integer>(id, model, type)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
public class TimeField extends FormComponentPanel<LocalTime>
{
	private static final long serialVersionUID = 1L;

	public static final String HOURS_CSS_CLASS_KEY = CssUtils.key(TimeField.class, "hours");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
		TextField<Integer> minutesField = new TextField<Integer>(id, model, type)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/TimeField.java`
#### Snippet
```java
		add(new Label("hoursSeparator", new ResourceModel("TimeField.hoursSeparator"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterForm.java`
#### Snippet
```java
		fc.add(new Behavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterForm.java`
#### Snippet
```java
public class FilterForm<T> extends Form<T>
{
	private static final long serialVersionUID = 1L;

	private static final ResourceReference JS = new JavaScriptResourceReference(FilterForm.class,
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/ChoiceFilteredPropertyColumn.java`
#### Snippet
```java
public class ChoiceFilteredPropertyColumn<T, Y, S> extends FilteredPropertyColumn<T, S>
{
	private static final long serialVersionUID = 1L;
	private final IModel<? extends List<? extends Y>> filterChoices;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilteredPropertyColumn.java`
#### Snippet
```java
		IFilteredColumn<T, S>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterStateModel.java`
#### Snippet
```java
class FilterStateModel<T> implements IModel<T>
{
	private static final long serialVersionUID = 1L;

	private final IFilterStateLocator<T> locator;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/TextFilter.java`
#### Snippet
```java
public class TextFilter<T> extends AbstractFilter
{
	private static final long serialVersionUID = 1L;

	private final TextField<T> filter;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/GoFilter.java`
#### Snippet
```java
		go = new Button("go", goModel)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/GoFilter.java`
#### Snippet
```java
public class GoFilter extends Panel
{
	private static final long serialVersionUID = 1L;

	protected static final IModel<String> DEFAULT_GO_MODEL = new ResourceModel("datatable.go",
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/DefaultNestedTree.java`
#### Snippet
```java
public class DefaultNestedTree<T> extends NestedTree<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/ExportToolbar.java`
#### Snippet
```java
		td.add(AttributeModifier.replace("colspan", new IModel<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/ExportToolbar.java`
#### Snippet
```java
public class ExportToolbar extends AbstractToolbar
{
	private static final long serialVersionUID = 1L;

	private static final IModel<String> DEFAULT_MESSAGE_MODEL = new ResourceModel(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterToolbar.java`
#### Snippet
```java
		ListView<IColumn<T, S>> filters = new ListView<>("filters", model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterToolbar.java`
#### Snippet
```java
					filter.add(new Behavior()
					{
						private static final long serialVersionUID = 1L;

						/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterToolbar.java`
#### Snippet
```java
{
	private static final String FILTER_ID = "filter";
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterToolbar.java`
#### Snippet
```java
		
		IModel<List<IColumn<T, S>>> model = new IModel<>() {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/Node.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	public static final String OTHER_CLASS_KEY = CssUtils.key(Node.class, "other");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/Node.java`
#### Snippet
```java
		return new AjaxFallbackLink<Void>(id)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/Node.java`
#### Snippet
```java
	private static class StyleBehavior extends Behavior
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/table/TreeDataProvider.java`
#### Snippet
```java
public abstract class TreeDataProvider<T> implements ITreeDataProvider<T>
{
	private static final long serialVersionUID = 1L;

	private final ITreeProvider<T> provider;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/table/TreeColumn.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/NestedTree.java`
#### Snippet
```java
public abstract class NestedTree<T> extends AbstractTree<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/NestedTree.java`
#### Snippet
```java
	private class RootsModel implements IModel<T>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
public class AjaxEditableChoiceLabel<T> extends AjaxEditableLabel<T>
{
	private static final long serialVersionUID = 1L;

	/** The list of objects. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
		editor.add(new EditorAjaxBehavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
		IModel<List<? extends T>> choiceModel = new IModel<List<? extends T>>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
		DropDownChoice<T> editor = new DropDownChoice<T>(componentId, model, choiceModel, renderer)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableChoiceLabel.java`
#### Snippet
```java
		Label label = new Label(componentId, model)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/AbstractTree.java`
#### Snippet
```java
public abstract class AbstractTree<T> extends Panel implements IGenericComponent<Set<T>, AbstractTree<T>>
{
	private static final long serialVersionUID = 1L;

	private ITreeProvider<T> provider;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/AbstractTree.java`
#### Snippet
```java
		return new Node<T>(id, this, model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/table/NodeBorder.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private boolean[] branches;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/table/NodeModel.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private IModel<T> model;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/theme/WindowsTheme.java`
#### Snippet
```java
public class WindowsTheme extends Behavior
{
	private static final long serialVersionUID = 1L;

	private static final ResourceReference CSS = new CssResourceReference(WindowsTheme.class,
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/theme/HumanTheme.java`
#### Snippet
```java
public class HumanTheme extends Behavior
{
	private static final long serialVersionUID = 1L;

	private static final ResourceReference CSS = new CssResourceReference(WindowsTheme.class,
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/nested/BranchItem.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	public BranchItem(String id, int index, IModel<T> model)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/table/AbstractTreeColumn.java`
#### Snippet
```java
		ITreeColumn<T, S>
{
	private static final long serialVersionUID = 1L;

	private TableTree<T, S> tree;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/TableTree.java`
#### Snippet
```java
public abstract class TableTree<T, S> extends AbstractTree<T>
{
	private static final long serialVersionUID = 1L;

	private final DataTable<T, S> table;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/TableTree.java`
#### Snippet
```java
		return new DataTable<T, S>(id, columns, dataProvider, rowsPerPage)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/TableTree.java`
#### Snippet
```java
		return new TreeDataProvider<T>(provider)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/CheckFolder.java`
#### Snippet
```java
public class CheckFolder<T> extends Folder<T>
{
	private static final long serialVersionUID = 1L;

	public CheckFolder(String id, AbstractTree<T> tree, IModel<T> model)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/DefaultTableTree.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SortParam.java`
#### Snippet
```java
public class SortParam<T> implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private final T property;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/Folder.java`
#### Snippet
```java
public class Folder<T> extends StyledLinkLabel<T>
{
	private static final long serialVersionUID = 1L;

	public static final String OTHER_CSS_CLASS_KEY = CssUtils.key(Folder.class, "other");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/TreeModelProvider.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private final TreeModel treeModel;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/TreeModelProvider.java`
#### Snippet
```java
	private class Listener implements TreeModelListener, Serializable
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/CheckedFolder.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	public CheckedFolder(String id, AbstractTree<T> tree, IModel<T> model)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/CheckedFolder.java`
#### Snippet
```java
		return new AjaxCheckBox(id, newCheckBoxModel(model))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SortableTreeProvider.java`
#### Snippet
```java
public abstract class SortableTreeProvider<T, S> implements ISortableTreeProvider<T, S>
{
	private static final long serialVersionUID = 1L;

	private final SingleSortState<S> state = new SingleSortState<>();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/nested/Subtree.java`
#### Snippet
```java
		branches.setItemReuseStrategy(new IItemReuseStrategy()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/nested/Subtree.java`
#### Snippet
```java
		RefreshingView<T> branches = new RefreshingView<T>("branches")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/nested/Subtree.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private NestedTree<T> tree;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SortableDataProvider.java`
#### Snippet
```java
public abstract class SortableDataProvider<T, S> implements ISortableDataProvider<T, S>
{
	private static final long serialVersionUID = 1L;

	private final SingleSortState<S> state = new SingleSortState<>();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SingleSortState.java`
#### Snippet
```java
public class SingleSortState<T> implements ISortState<T>, IClusterable
{
	private static final long serialVersionUID = 1L;

	SortParam<T> param;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/StyledLinkLabel.java`
#### Snippet
```java
	private static final StyleBehavior STYLE_CLASS = new StyleBehavior();

	private static final long serialVersionUID = 1L;

	public StyledLinkLabel(String id, IModel<T> model)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/StyledLinkLabel.java`
#### Snippet
```java
		return new AjaxFallbackLink<Void>(id)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/content/StyledLinkLabel.java`
#### Snippet
```java
	private static class StyleBehavior extends Behavior
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/ProviderSubset.java`
#### Snippet
```java
		return new IModel<Set<T>>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/ProviderSubset.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private ITreeProvider<T> provider;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/FinishButton.java`
#### Snippet
```java
public class FinishButton extends WizardButton
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/AbstractWizardModel.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/** Whether cancel functionality is available. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/LastButton.java`
#### Snippet
```java
public class LastButton extends WizardButton
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/NextButton.java`
#### Snippet
```java
public class NextButton extends WizardButton
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/CancelButton.java`
#### Snippet
```java
public class CancelButton extends WizardButton
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/Wizard.java`
#### Snippet
```java
	public static final String VIEW_ID = "view";

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/PreviousButton.java`
#### Snippet
```java
public class PreviousButton extends WizardButton
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/AjaxWizardButtonBar.java`
#### Snippet
```java
public class AjaxWizardButtonBar extends WizardButtonBar
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/AjaxWizardButtonBar.java`
#### Snippet
```java
		button.add(new AjaxFormSubmitBehavior("click")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardModel.java`
#### Snippet
```java
	public static final ICondition TRUE = (() -> true);

	private static final long serialVersionUID = 1L;

	/** The currently active step. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardButton.java`
#### Snippet
```java
public abstract class WizardButton extends Button
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardButtonBar.java`
#### Snippet
```java
public class WizardButtonBar extends Panel
{
	private static final long serialVersionUID = 1L;

	private final IWizard wizard;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardModel.java`
#### Snippet
```java
public class DynamicWizardModel extends AbstractWizardModel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/GoAndClearFilter.java`
#### Snippet
```java
public class GoAndClearFilter extends GoFilter
{
	private static final long serialVersionUID = 1L;

	private static final IModel<String> DEFAULT_CLEAR_MODEL = new ResourceModel("datatable.clear",
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/GoAndClearFilter.java`
#### Snippet
```java
		clear = new Button("clear", clearModel)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java
public class DefaultBreadCrumbsModel implements IBreadCrumbModel
{
	private static final long serialVersionUID = 1L;

	/** The currently active participant, if any (possibly null). */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbModelListenerSupport.java`
#### Snippet
```java
public final class BreadCrumbModelListenerSupport implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/** listeners for bread crumb events. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbLink.java`
#### Snippet
```java
public abstract class BreadCrumbLink extends Link<Void>
{
	private static final long serialVersionUID = 1L;

	/** The bread crumb model. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java

	/** For serialization. */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
	{
		/** For serialization. */
		private static final long serialVersionUID = 1L;

		private RatingStarBar(final String id, final IModel<Integer> model)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/rating/RatingPanel.java`
#### Snippet
```java
			AjaxFallbackLink<Void> link = new AjaxFallbackLink<Void>("link")
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanel.java`
#### Snippet
```java
public abstract class BreadCrumbPanel extends Panel implements IBreadCrumbParticipant
{
	private static final long serialVersionUID = 1L;

	/** The bread crumb model. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanel.java`
#### Snippet
```java
	private final IBreadCrumbParticipant decorated = new BreadCrumbParticipantDelegate(this)
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 1L;

	private final IBreadCrumbModel decorated;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java
			IBreadCrumbModelListener
	{
		private static final long serialVersionUID = 1L;

		private transient boolean dirty = false;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java
			BreadCrumbLink link = new BreadCrumbLink("link", breadCrumbModel)
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java
	private static final class BreadCrumbComponent extends Panel
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java
			setDefaultModel(new LoadableDetachableModel<List<IBreadCrumbParticipant>>()
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanelFactory.java`
#### Snippet
```java
public final class BreadCrumbPanelFactory implements IBreadCrumbPanelFactory
{
	private static final long serialVersionUID = 1L;

	/** Class to construct. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java
public abstract class BreadCrumbParticipantDelegate implements IBreadCrumbParticipant
{
	private static final long serialVersionUID = 1L;

	private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanelLink.java`
#### Snippet
```java
public class BreadCrumbPanelLink extends BreadCrumbLink
{
	private static final long serialVersionUID = 1L;

	/** The bread crumb model. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
	private static class CaptchaImageResource extends DynamicImageResource
	{
		private static final long serialVersionUID = -1560784998742404278L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
	private static class PlacedAnimal implements Serializable
	{
		private static final long serialVersionUID = -6703909440564862486L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
		image.add(new AjaxEventBehavior("click")
		{
			private static final long serialVersionUID = 7480352029955897654L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
public class KittenCaptchaPanel extends Panel
{
	private static final long serialVersionUID = 2711167040323855070L;

	private static final Logger LOG = LoggerFactory.getLogger(KittenCaptchaPanel.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/KittenCaptchaPanel.java`
#### Snippet
```java
	private class PlacedAnimalList implements Serializable
	{
		private static final long serialVersionUID = 6335852594326213439L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java
public abstract class DynamicWizardStep extends WizardStep implements IDynamicWizardStep
{
	private static final long serialVersionUID = 1L;

	private final IDynamicWizardStep previousStep;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/WicketMetrics.java`
#### Snippet
```java
	public static final MetaDataKey<MetricRegistry> METRIC_REGISTRY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/WicketMetrics.java`
#### Snippet
```java
	public static final MetaDataKey<WicketMetricsSettings> METRIC_SETTINGS = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	private final class Header extends Panel
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	{

		private static final long serialVersionUID = 1L;

		private final List<IFormValidator> validators = new ArrayList<>();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/requestlogger/JsonRequestLogger.java`
#### Snippet
```java
	private static final class RequestSessionTuple implements Serializable
	{
		private static final long serialVersionUID = 1L;

		private final RequestData request;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileUploadException.java`
#### Snippet
```java
     * Serial version UID, being used, if the exception is serialized.
     */
    private static final long serialVersionUID = 2;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/InvalidFileNameException.java`
#### Snippet
```java
     * is serialized.
     */
    private static final long serialVersionUID = 2;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileItemStream.java`
#### Snippet
```java
         * when serializing an exception instance.
         */
        private static final long serialVersionUID = 2;

    }
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/pub/FileUploadFileCountLimitException.java`
#### Snippet
```java
public class FileUploadFileCountLimitException extends FileUploadSizeException {

    private static final long serialVersionUID = 2;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/pub/FileUploadSizeException.java`
#### Snippet
```java
     * Serial version UID, being used, if serialized.
     */
    private static final long serialVersionUID = 2;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
		return new AutoCompleteBehavior<>(renderer, settings)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteTextField.java`
#### Snippet
```java
public abstract class AutoCompleteTextField<T> extends TextField<T>
{
	private static final long serialVersionUID = 1L;

	/** auto complete behavior attached to this textfield */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/pub/FileUploadByteCountLimitException.java`
#### Snippet
```java
     * The exceptions UID, for serializing an instance.
     */
    private static final long serialVersionUID = 2;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/pub/FileUploadContentTypeException.java`
#### Snippet
```java
     * The exceptions UID, for serializing an instance.
     */
    private static final long serialVersionUID = 2;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/StaticContentStep.java`
#### Snippet
```java
public class StaticContentStep extends WizardStep
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/FileItemHeadersImpl.java`
#### Snippet
```java
     * Serial version UID, being used, if serialized.
     */
    private static final long serialVersionUID = 2;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/MultipartStream.java`
#### Snippet
```java
         * The UID to use when serializing this instance.
         */
        private static final long serialVersionUID = 2;

        /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/MultipartStream.java`
#### Snippet
```java
         * The UID to use when serializing this instance.
         */
        private static final long serialVersionUID = 2;

        /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/mime/ParseException.java`
#### Snippet
```java
     * The UID to use when serializing this instance.
     */
    private static final long serialVersionUID = 2;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/ActionPermissions.java`
#### Snippet
```java
public final class ActionPermissions implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/** Map from an action to a set of role strings */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/AbstractAuthenticatedWebSession.java`
#### Snippet
```java
public abstract class AbstractAuthenticatedWebSession extends WebSession
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/InstantiationPermissions.java`
#### Snippet
```java
public class InstantiationPermissions implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/** Holds roles objects for component classes */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/AuthenticatedWebSession.java`
#### Snippet
```java
public abstract class AuthenticatedWebSession extends AbstractAuthenticatedWebSession
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/pages/SignInPage.java`
#### Snippet
```java
public class SignInPage extends WebPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/Roles.java`
#### Snippet
```java
public class Roles extends HashSet<String> implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/** USER role (for use in annotations) */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/pages/SignOutPage.java`
#### Snippet
```java
public class SignOutPage extends WebPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/MetaDataRoleAuthorizationStrategy.java`
#### Snippet
```java
	public static final MetaDataKey<InstantiationPermissions> INSTANTIATION_PERMISSIONS = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/MetaDataRoleAuthorizationStrategy.java`
#### Snippet
```java
	public static final MetaDataKey<ActionPermissions> ACTION_PERMISSIONS = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/MetaDataEntry.java`
#### Snippet
```java
public final class MetaDataEntry<T> implements IClusterable
{
	private static final long serialVersionUID = 1L;

	final MetaDataKey<T> key;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/NonResettingRestartException.java`
#### Snippet
```java
public class NonResettingRestartException extends RequestHandlerExecutor.ReplaceHandlerException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/panel/SignInPanel.java`
#### Snippet
```java
public class SignInPanel extends Panel
{
	private static final long serialVersionUID = 1L;

	private static final String SIGN_IN_FORM = "signInForm";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authentication/panel/SignInPanel.java`
#### Snippet
```java
	public final class SignInForm extends StatelessForm<SignInPanel>
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java
public abstract class MetaDataKey<T> implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/AttributeModifier.java`
#### Snippet
```java
	public static final MarkerValue VALUELESS_ATTRIBUTE_REMOVE = MarkerValue.VALUELESS_ATTRIBUTE_REMOVE;

	private static final long serialVersionUID = 1L;

	/** Attribute specification. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/AttributeModifier.java`
#### Snippet
```java
		return new AttributeAppender(attributeName, value)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/SharedResources.java`
#### Snippet
```java
	private static final class AutoResourceReference extends ResourceReference
	{
		private static final long serialVersionUID = 1L;

		private final IResource resource;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/DefaultPageManagerProvider.java`
#### Snippet
```java
		private static final MetaDataKey<SessionData> KEY = new MetaDataKey<>()
		{
			private static final long serialVersionUID = 1L;
		};
		
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
	private static class BehaviorIdList extends ArrayList<Behavior>
	{
		private static final long serialVersionUID = 1L;

		public BehaviorIdList()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 1L;

	/** Maps Classes to ITypeConverters. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java
	private static class DefaultConverter<C> implements IConverter<C>
	{
		private static final long serialVersionUID = 1L;

		private final transient WeakReference<Class<C>> type;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/WicketRuntimeException.java`
#### Snippet
```java
public class WicketRuntimeException extends RuntimeException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
	static class InterceptData implements Serializable
	{
		private static final long serialVersionUID = 1L;

		private Url originalUrl;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
		private static final MetaDataKey<InterceptData> key = new MetaDataKey<>()
		{
			private static final long serialVersionUID = 1L;
		};
	}
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
public class RestartResponseAtInterceptPageException extends ResetResponseException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/ChoiceFilter.java`
#### Snippet
```java
public class ChoiceFilter<T> extends AbstractFilter
{
	private static final long serialVersionUID = 1L;

	private final DropDownChoice<T> choice;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/PageReference.java`
#### Snippet
```java
public class PageReference implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private final int pageId;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseException.java`
#### Snippet
```java
public class RestartResponseException extends ResetResponseException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(Page.class);

	private static final long serialVersionUID = 1L;

	/** Used to create page-unique numbers */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestTargetListenerCollection.java`
#### Snippet
```java
	ListenerCollection<AjaxRequestTarget.IListener>
{
	private static final long serialVersionUID = 1L;
}
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	private static final MetaDataKey<Boolean> SESSION_INVALIDATED = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	private static final MetaDataKey<Boolean> PAGES_UNLOCKED = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};
	
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	private final class PageAccessSynchronizerProvider extends LazyInitializer<PageAccessSynchronizer>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
public abstract class Session implements IClusterable, IEventSink, IMetadataContext<Serializable, Session>, IFeedbackContributor
{
	private static final long serialVersionUID = 1L;

	/** Logging object */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/WicketAjaxJQueryResourceReference.java`
#### Snippet
```java
public class WicketAjaxJQueryResourceReference extends JavaScriptResourceReference
{
	private static final long serialVersionUID = -2918665261694523156L;

	private static final WicketAjaxJQueryResourceReference INSTANCE = new WicketAjaxJQueryResourceReference();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/CancelEventIfAjaxListener.java`
#### Snippet
```java
public final class CancelEventIfAjaxListener extends AjaxCallListener
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxEventBehavior.java`
#### Snippet
```java
	private static final Logger LOGGER = LoggerFactory.getLogger(AjaxEventBehavior.class);

	private static final long serialVersionUID = 1L;

	private static final char EVENT_NAME_SEPARATOR = ' ';
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxEventBehavior.java`
#### Snippet
```java
		return new AjaxEventBehavior(eventName)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/csp/ContentSecurityPolicySettings.java`
#### Snippet
```java
	public static final MetaDataKey<String> NONCE_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxNewWindowNotifyingBehavior.java`
#### Snippet
```java
		return new AjaxNewWindowNotifyingBehavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxNewWindowNotifyingBehavior.java`
#### Snippet
```java
public class AjaxNewWindowNotifyingBehavior extends AbstractDefaultAjaxBehavior
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxClientInfoBehavior.java`
#### Snippet
```java
public class AjaxClientInfoBehavior extends AbstractAjaxTimerBehavior
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxClientInfoBehavior.java`
#### Snippet
```java
		{

			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxOnDomReadyClientInfoBehavior.java`
#### Snippet
```java
		{

			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxOnDomReadyClientInfoBehavior.java`
#### Snippet
```java
public class AjaxOnDomReadyClientInfoBehavior extends AbstractDefaultAjaxBehavior
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxSelfUpdatingTimerBehavior.java`
#### Snippet
```java
		return new AjaxSelfUpdatingTimerBehavior(interval)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxSelfUpdatingTimerBehavior.java`
#### Snippet
```java
public class AjaxSelfUpdatingTimerBehavior extends AbstractAjaxTimerBehavior
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxChannel.java`
#### Snippet
```java
public class AjaxChannel implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractAjaxTimerBehavior.java`
#### Snippet
```java
public abstract class AbstractAjaxTimerBehavior extends AbstractDefaultAjaxBehavior
{
	private static final long serialVersionUID = 1L;

	/** The update interval */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractAjaxTimerBehavior.java`
#### Snippet
```java
		return new AbstractAjaxTimerBehavior(interval)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/OnChangeAjaxBehavior.java`
#### Snippet
```java
public abstract class OnChangeAjaxBehavior extends AjaxFormComponentUpdatingBehavior
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/OnChangeAjaxBehavior.java`
#### Snippet
```java
		return new OnChangeAjaxBehavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormComponentUpdatingBehavior.java`
#### Snippet
```java
		.getLogger(AjaxFormComponentUpdatingBehavior.class);

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormComponentUpdatingBehavior.java`
#### Snippet
```java
		return new AjaxFormComponentUpdatingBehavior(eventName)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
		return new AjaxFormChoiceComponentUpdatingBehavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
	AjaxFormComponentUpdatingBehavior
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
		attributes.getAjaxCallListeners().add(new AjaxCallListener()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
	private class FormValidateVisitor implements IVisitor<FormComponent, Void>, IClusterable
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
public class AjaxFormValidatingBehavior extends Behavior
{
	private static final long serialVersionUID = 1L;

	private final String event;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/json/JSONFunction.java`
#### Snippet
```java
public class JSONFunction implements JSONString, CharSequence, IClusterable
{
	private static final long serialVersionUID = 1L;
	private final CharSequence value;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java
public abstract class AjaxFormSubmitBehavior extends AjaxEventBehavior
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java
		return new AjaxFormSubmitBehavior(eventName)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxLink.java`
#### Snippet
```java
public abstract class AjaxLink<T> extends AbstractLink implements IAjaxLink, IGenericComponent<T, AjaxLink<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxLink.java`
#### Snippet
```java
		return new AjaxEventBehavior(event)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxSubmitLink.java`
#### Snippet
```java
public abstract class AjaxSubmitLink extends AbstractSubmitLink
{
	private static final long serialVersionUID = 1L;

	private static final Logger logger = LoggerFactory.getLogger(AjaxSubmitLink.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxSubmitLink.java`
#### Snippet
```java
		return new AjaxFormSubmitBehavior(form, event)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigation.java`
#### Snippet
```java
public class AjaxPagingNavigation extends PagingNavigation
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxFallbackLink.java`
#### Snippet
```java
{
	/** */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/AjaxFallbackLink.java`
#### Snippet
```java
		return new AjaxEventBehavior(event)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationLink.java`
#### Snippet
```java
public class AjaxPagingNavigationLink extends PagingNavigationLink<Void> implements IAjaxLink
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
public abstract class AbstractDefaultAjaxBehavior extends AbstractAjaxBehavior
{
	private static final long serialVersionUID = 1L;

	private static final Logger LOG = LoggerFactory.getLogger(AbstractDefaultAjaxBehavior.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigator.java`
#### Snippet
```java
public class AjaxPagingNavigator extends PagingNavigator
{
	private static final long serialVersionUID = 1L;

	/** The pageable component that needs to be updated. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxCheckBox.java`
#### Snippet
```java
public abstract class AjaxCheckBox extends CheckBox
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxCheckBox.java`
#### Snippet
```java
		add(new AjaxFormComponentUpdatingBehavior("click")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationBehavior.java`
#### Snippet
```java
{
	/** For serialization. */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
		return new AjaxFormSubmitBehavior(form, event)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxFallbackButton.java`
#### Snippet
```java
public abstract class AjaxFallbackButton extends Button
{
	private static final long serialVersionUID = 1L;

	private final Form<?> mForm;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationIncrementLink.java`
#### Snippet
```java
		IAjaxLink
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxButton.java`
#### Snippet
```java
		return new AjaxFormSubmitBehavior(form, event)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxButton.java`
#### Snippet
```java
public abstract class AjaxButton extends Button
{
	private static final long serialVersionUID = 1L;

	private static final Logger logger = LoggerFactory.getLogger(AjaxButton.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/ThrottlingSettings.java`
#### Snippet
```java
public class ThrottlingSettings implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private Duration delay;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolverConverter.java`
#### Snippet
```java
public class PropertyResolverConverter implements IClusterable
{
	private static final long serialVersionUID = 1L;


```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/AbstractKeyInSessionCryptFactory.java`
#### Snippet
```java
	private static final MetaDataKey<Serializable> KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/KeyInSessionSunJceCryptFactory.java`
#### Snippet
```java
    static final class CryptData implements IClusterable
	{
        private static final long serialVersionUID = 1L;

        final String key;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/interpolator/PropertyVariableInterpolator.java`
#### Snippet
```java
public class PropertyVariableInterpolator extends VariableInterpolator
{
	private static final long serialVersionUID = 1L;

	/** The object to introspect on */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/interpolator/ConvertingPropertyVariableInterpolator.java`
#### Snippet
```java
public class ConvertingPropertyVariableInterpolator extends PropertyVariableInterpolator
{
	private static final long serialVersionUID = 1L;

	private IConverterLocator locator;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/PackageResourceStream.java`
#### Snippet
```java
public class PackageResourceStream extends AbstractResourceStream
{
	private static final long serialVersionUID = 1L;

	private final IResourceStream resourceStream;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/WebExternalResourceStream.java`
#### Snippet
```java
{
	private static final Logger log = LoggerFactory.getLogger(WebExternalResourceStream.class);
	private static final long serialVersionUID = 1L;

	transient InputStream in;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/UrlResourceStream.java`
#### Snippet
```java
		IFixedLocationResourceStream
{
	private static final long serialVersionUID = 1L;

	/** Logging. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/ClientInfo.java`
#### Snippet
```java
public abstract class ClientInfo implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/caching/CachingResourceStreamLocator.java`
#### Snippet
```java
	private static class CacheKey extends Key
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/StalePageException.java`
#### Snippet
```java
public class StalePageException extends WicketRuntimeException implements IWicketInternalException
{
	private static final long serialVersionUID = 1L;

	private final transient IRequestablePage page;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
public abstract class MarkupContainer extends Component implements Iterable<Component>
{
	private static final long serialVersionUID = 1L;

	private static final int INITIAL_CHILD_LIST_CAPACITY = 12;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	private static final MetaDataKey<LinkedList<RemovedChild>> REMOVALS_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	private static class RemovedChild implements Serializable
	{
		private static final long serialVersionUID = 1L;

		private transient final Component removedChild;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/ComponentNotFoundException.java`
#### Snippet
```java
public class ComponentNotFoundException extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/PageProvider.java`
#### Snippet
```java
public class PageProvider implements IPageProvider, IClusterable
{
	private static final long serialVersionUID = 1L;

	private final Integer renderCount;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/ListenerInvocationNotAllowedException.java`
#### Snippet
```java
public class ListenerInvocationNotAllowedException extends RuntimeException
{
	private static final long serialVersionUID = 1L;

	private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/logger/ResourceReferenceLogData.java`
#### Snippet
```java
public class ResourceReferenceLogData extends ResourceLogData
{
	private static final long serialVersionUID = 1L;

	private final Class<? extends ResourceReference> resourceReferenceClass;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/mock/MockHomePage.java`
#### Snippet
```java
public class MockHomePage extends WebPage
{
	private static final long serialVersionUID = 1L;

}
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/logger/PageLogData.java`
#### Snippet
```java
public class PageLogData implements ILogData
{
	private static final long serialVersionUID = 1L;

	private final Class<? extends IRequestablePage> pageClass;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/logger/ResourceStreamLogData.java`
#### Snippet
```java
public class ResourceStreamLogData extends ResourceLogData
{
	private static final long serialVersionUID = 1L;

	private final Class<? extends IResourceStream> resourceStreamClass;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/logger/ListenerLogData.java`
#### Snippet
```java
public class ListenerLogData extends PageLogData
{
	private static final long serialVersionUID = 1L;

	private final Class<? extends IRequestableComponent> componentClass;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/page/CouldNotLockPageException.java`
#### Snippet
```java
public class CouldNotLockPageException extends RuntimeException
{
	private static final long serialVersionUID = 1L;

	private final int page;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/page/PageAccessSynchronizer.java`
#### Snippet
```java
public class PageAccessSynchronizer implements Serializable
{
	private static final long serialVersionUID = 1L;

	private static final Logger logger = LoggerFactory.getLogger(PageAccessSynchronizer.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/page/DefaultPageLockManager.java`
#### Snippet
```java
	private final LazyInitializer<ConcurrentMap<Integer, PageAccessSynchronizer.PageLock>> locks = new LazyInitializer<>()
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/page/DefaultPageLockManager.java`
#### Snippet
```java
public class DefaultPageLockManager implements IPageLockManager {

	private static final long serialVersionUID = 1L;

	private static final Logger logger = LoggerFactory.getLogger(DefaultPageLockManager.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTesterHelper.java`
#### Snippet
```java
	public static class ComponentData implements IClusterable
	{
		private static final long serialVersionUID = 1L;

		/** Component path. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DummyHomePage.java`
#### Snippet
```java
public class DummyHomePage extends WebPage
{
	private static final long serialVersionUID = 1L;

	private final Link<?> testPageLink;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DummyHomePage.java`
#### Snippet
```java
	public class TestLink extends Link<Void>
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/tester/DummyPanelPage.java`
#### Snippet
```java
public class DummyPanelPage extends WebPage
{
	private static final long serialVersionUID = 1L;

	/** The dummy <code>Panel</code> <code>Component</code> id */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	private static final IModelComparator defaultModelComparator = new IModelComparator()
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	static final MetaDataKey<String> CONSTRUCTED_AT_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	static final MetaDataKey<String> ADDED_AT_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	private static final MetaDataKey<FeedbackMessages> FEEDBACK_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(Component.class);

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	private static final MetaDataKey<String> MARKUP_ID_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieDefaults.java`
#### Snippet
```java
public class CookieDefaults implements IClusterable
{
	private static final long serialVersionUID = 1L;

	public enum SameSite
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplate.java`
#### Snippet
```java
public abstract class TextTemplate extends AbstractStringResourceStream
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/template/JavaScriptTemplate.java`
#### Snippet
```java
public final class JavaScriptTemplate extends TextTemplateDecorator
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplateDecorator.java`
#### Snippet
```java
public abstract class TextTemplateDecorator extends TextTemplate
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/template/CssTemplate.java`
#### Snippet
```java
public final class CssTemplate extends TextTemplateDecorator
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/template/PackageTextTemplate.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(PackageTextTemplate.class);

	private static final long serialVersionUID = 1L;

	/** The content type used if not provided in the constructor */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/reference/ClassReference.java`
#### Snippet
```java
public class ClassReference<T> implements Serializable, Supplier<Class<T>>
{
	private static final long serialVersionUID = 1L;

	private transient WeakReference<Class<T>> cache;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/PropertyModel.java`
#### Snippet
```java
public class PropertyModel<T> extends AbstractPropertyModel<T>
{
	private static final long serialVersionUID = 1L;

	/** Property expression for property access. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
	private static class PartialHtmlHeaderContainer extends HtmlHeaderContainer
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentModel.java`
#### Snippet
```java
	private class WrapModel implements IWrapModel<T>
	{
		private static final long serialVersionUID = 1L;

		private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentModel.java`
#### Snippet
```java
public class ComponentModel<T> implements IModel<T>, IComponentAssignedModel<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentPropertyModel.java`
#### Snippet
```java
public class ComponentPropertyModel<T> implements IComponentAssignedModel<T>
{
	private static final long serialVersionUID = 1L;

	/** Name of property to read */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentPropertyModel.java`
#### Snippet
```java
	private class AssignmentWrapper<P> implements IWrapModel<P>
	{
		private static final long serialVersionUID = 1L;

		private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentDetachableModel.java`
#### Snippet
```java
public class ComponentDetachableModel<T> implements IComponentAssignedModel<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentDetachableModel.java`
#### Snippet
```java
	private class WrapModel<P> implements IWrapModel<T>
	{
		private static final long serialVersionUID = 1L;

		private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/LoadableDetachableModel.java`
#### Snippet
```java
public abstract class LoadableDetachableModel<T> implements IModel<T>
{
	private static final long serialVersionUID = 1L;

	private static final Logger log = LoggerFactory.getLogger(LoadableDetachableModel.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/LoadableDetachableModel.java`
#### Snippet
```java
		return new LoadableDetachableModel<T>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/CompoundPropertyModel.java`
#### Snippet
```java
			IWrapModel<C>
	{
		private static final long serialVersionUID = 1L;

		private final Component owner;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/CompoundPropertyModel.java`
#### Snippet
```java
public class CompoundPropertyModel<T> extends ChainingModel<T> implements IComponentInheritedModel<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/Model.java`
#### Snippet
```java
public class Model<T extends Serializable> implements IObjectClassAwareModel<T>
{
	private static final long serialVersionUID = 1L;

	/** Backing object. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/util/GenericBaseModel.java`
#### Snippet
```java
public abstract class GenericBaseModel<T> implements IObjectClassAwareModel<T>
{
	private static final long serialVersionUID = 1L;
	/** model object */
	private T object;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
		IPropertyReflectionAwareModel<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/util/ListModel.java`
#### Snippet
```java
public class ListModel<T> extends GenericBaseModel<List<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
		return new IModel<R>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupNotFoundException.java`
#### Snippet
```java
public final class MarkupNotFoundException extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/util/CollectionModel.java`
#### Snippet
```java
public class CollectionModel<T> extends GenericBaseModel<Collection<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
		return new LambdaModel<R>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
		return new LambdaModel<R>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/LambdaModel.java`
#### Snippet
```java
public abstract class LambdaModel<T> implements IModel<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/util/SetModel.java`
#### Snippet
```java
public class SetModel<T> extends GenericBaseModel<Set<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/util/MapModel.java`
#### Snippet
```java
public class MapModel<K, V> extends GenericBaseModel<Map<K, V>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/ResourceModel.java`
#### Snippet
```java
public class ResourceModel implements IComponentAssignedModel<String>
{
	private static final long serialVersionUID = 1L;

	private final String resourceKey;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/ResourceModel.java`
#### Snippet
```java
			IWrapModel<String>
	{
		private static final long serialVersionUID = 1L;

		private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupType.java`
#### Snippet
```java
public class MarkupType implements Serializable
{
	private static final long serialVersionUID = 1L;

	/** Mime string for XHTML */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/WicketParseException.java`
#### Snippet
```java
public class WicketParseException extends ParseException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupParser.java`
#### Snippet
```java
	public class MarkupFilterList extends ArrayList<IMarkupFilter>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
public class RfcCompliantEmailAddressValidator implements IValidator<String>
{
	private static final long serialVersionUID = 1L;

	private static final String EMAIL_PATTERN = "(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupException.java`
#### Snippet
```java
public final class MarkupException extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;

	/** The markup stream that was being parsed when the exception was thrown */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/StringResourceModel.java`
#### Snippet
```java
		IComponentAssignedModel<String>
{
	private static final long serialVersionUID = 1L;

	/** The key of message to get. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/model/StringResourceModel.java`
#### Snippet
```java
			IWrapModel<String>
	{
		private static final long serialVersionUID = 1L;

		private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/CssContentHeaderItem.java`
#### Snippet
```java
public class CssContentHeaderItem extends CssHeaderItem
{
	private static final long serialVersionUID = 1L;

	private final CharSequence css;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupResourceStream.java`
#### Snippet
```java
public class MarkupResourceStream implements IResourceStream, IFixedLocationResourceStream
{
	private static final long serialVersionUID = 1846489965076612828L;

	private static final Logger log = LoggerFactory.getLogger(MarkupResourceStream.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/JavaScriptHeaderItem.java`
#### Snippet
```java
public abstract class JavaScriptHeaderItem extends AbstractCspHeaderItem
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnDomReadyHeaderItem.java`
#### Snippet
```java
public class OnDomReadyHeaderItem extends AbstractCspHeaderItem
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/AbstractCspHeaderItem.java`
#### Snippet
```java
public abstract class AbstractCspHeaderItem extends HeaderItem
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnEventHeaderItem.java`
#### Snippet
```java
public class OnEventHeaderItem extends AbstractCspHeaderItem
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/CssHeaderItem.java`
#### Snippet
```java
public abstract class CssHeaderItem extends AbstractCspHeaderItem
{
	private static final long serialVersionUID = 1L;

	private String markupId;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/MetaDataHeaderItem.java`
#### Snippet
```java
public class MetaDataHeaderItem extends HeaderItem
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/CssUrlReferenceHeaderItem.java`
#### Snippet
```java
public class CssUrlReferenceHeaderItem extends AbstractCssReferenceHeaderItem
{
	private static final long serialVersionUID = 1L;

	private final String url;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/CssReferenceHeaderItem.java`
#### Snippet
```java
public class CssReferenceHeaderItem extends AbstractCssReferenceHeaderItem implements IReferenceHeaderItem
{
	private static final long serialVersionUID = 1L;

	private final ResourceReference reference;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/OnLoadHeaderItem.java`
#### Snippet
```java
public class OnLoadHeaderItem extends AbstractCspHeaderItem
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/FilteredHeaderItem.java`
#### Snippet
```java
public class FilteredHeaderItem extends HeaderItem implements IWrappedHeaderItem
{
	private static final long serialVersionUID = 1L;

	private final HeaderItem wrapped;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/JavaScriptDeferHeaderResponse.java`
#### Snippet
```java
	private static class NativeOnLoadHeaderItem extends OnLoadHeaderItem
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/JavaScriptDeferHeaderResponse.java`
#### Snippet
```java
	private static class NativeOnDomContentLoadedHeaderItem extends OnDomReadyHeaderItem
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/HeaderResponseContainer.java`
#### Snippet
```java
public class HeaderResponseContainer extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	private final String filterName;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebComponent.java`
#### Snippet
```java
public class WebComponent extends Component
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/HeaderPartContainer.java`
#### Snippet
```java
public final class HeaderPartContainer extends WebMarkupContainer implements IComponentResolver
{
	private static final long serialVersionUID = 1L;

	/** The panel or bordered page the header part is associated with */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/GenericWebPage.java`
#### Snippet
```java
public class GenericWebPage<T> extends WebPage implements IGenericComponent<T, GenericWebPage<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/GenericWebMarkupContainer.java`
#### Snippet
```java
public class GenericWebMarkupContainer<T> extends WebMarkupContainer implements IGenericComponent<T, GenericWebMarkupContainer<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushItem.java`
#### Snippet
```java
public class PushItem implements Serializable
{
	private static final long serialVersionUID = 1L;

	private Serializable object;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/filter/FilteringHeaderResponse.java`
#### Snippet
```java
	private static final MetaDataKey<FilteringHeaderResponse> RESPONSE_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebMarkupContainer.java`
#### Snippet
```java
public class WebMarkupContainer extends MarkupContainer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
public class PushHeaderItem extends HeaderItem
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		return startResourceReference(new ResourceReference("testResourceReference")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/HTML5Attributes.java`
#### Snippet
```java
public class HTML5Attributes extends Behavior
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java
public class TransparentWebMarkupContainer extends WebMarkupContainer implements IComponentResolver
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractCheckSelector.java`
#### Snippet
```java
		/**
		 */
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractCheckSelector.java`
#### Snippet
```java
		IHeaderContributor
{
	private static final long serialVersionUID = 1L;

	private static final ResourceReference JS = new JavaScriptResourceReference(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentPanel.java`
#### Snippet
```java
public abstract class FormComponentPanel<T> extends FormComponent<T> implements IQueueRegion
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java
	public static class SimpleCache extends ConcurrentHashMap<String, Boolean>
	{
		private static final long serialVersionUID = 1L;

		private final ConcurrentLinkedQueue<String> fifo = new ConcurrentLinkedQueue<>();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/SimpleFormComponentLabel.java`
#### Snippet
```java
public class SimpleFormComponentLabel extends FormComponentLabel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/StatelessForm.java`
#### Snippet
```java
public class StatelessForm<T> extends Form<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/LambdaChoiceRenderer.java`
#### Snippet
```java
 */
public class LambdaChoiceRenderer<T> implements IChoiceRenderer<T> {
    private static final long serialVersionUID = 1L;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebPage.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(WebPage.class);

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java
public class ListMultipleChoice<T> extends AbstractChoice<Collection<T>, T>
{
	private static final long serialVersionUID = 1L;

	/** Meta key for the retain disabled flag */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java
	static MetaDataKey<Boolean> RETAIN_DISABLED_META_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ImageButton.java`
#### Snippet
```java
public class ImageButton extends Button implements IRequestListener
{
	private static final long serialVersionUID = 1L;

	/** The image resource this image component references */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckboxMultipleChoiceSelector.java`
#### Snippet
```java
public class CheckboxMultipleChoiceSelector extends AbstractCheckSelector
{
	private static final long serialVersionUID = 1L;

	private final CheckBoxMultipleChoice<?> choiceComponent;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/UrlTextField.java`
#### Snippet
```java
public class UrlTextField extends TextField<String>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroupSelector.java`
#### Snippet
```java
{
	/** */
	private static final long serialVersionUID = 1L;

	private CheckGroup<?> group;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ValidationErrorFeedback.java`
#### Snippet
```java
public class ValidationErrorFeedback implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/** error object */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
public class CheckBoxMultipleChoice<T> extends ListMultipleChoice<T>
{
	private static final long serialVersionUID = 1L;

	private String prefix = "";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSubmitLink.java`
#### Snippet
```java
public abstract class AbstractSubmitLink extends AbstractLink implements IFormSubmittingComponent
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/TextField.java`
#### Snippet
```java
public class TextField<T> extends AbstractTextComponent<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ChoiceRenderer.java`
#### Snippet
```java
public class ChoiceRenderer<T> implements IChoiceRenderer<T>, IClusterable
{
	private static final long serialVersionUID = 1L;

	/** expression for getting the display value. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/HiddenField.java`
#### Snippet
```java
public class HiddenField<T> extends TextField<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Radio.java`
#### Snippet
```java
public class Radio<T> extends LabeledWebMarkupContainer implements IGenericComponent<T, Radio<T>>
{
	private static final long serialVersionUID = 1L;

	private static final String ATTR_DISABLED = "disabled";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/LabeledWebMarkupContainer.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;
	/**
	 * The value will be made available to the validator property by means of ${label}. It does not
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
public class CheckGroup<T> extends FormComponent<Collection<T>>
{
	private static final long serialVersionUID = 1L;

	private static final Logger log = LoggerFactory.getLogger(CheckGroup.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/PasswordTextField.java`
#### Snippet
```java
public class PasswordTextField extends TextField<String>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
	protected static class AutoLabel extends TransparentWebMarkupContainer
	{
		private static final long serialVersionUID = 1L;

		private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
public class AutoLabelResolver implements IComponentResolver
{
	private static final long serialVersionUID = 1L;

	private static final Logger logger = LoggerFactory.getLogger(AutoLabelResolver.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentLabel.java`
#### Snippet
```java
public class FormComponentLabel extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	public static final String REQUIRED_CSS_CLASS_KEY = CssUtils.key(FormComponentLabel.class,
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/EmailTextField.java`
#### Snippet
```java
public class EmailTextField extends TextField<String>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/EnumChoiceRenderer.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/NumberTextField.java`
#### Snippet
```java
public class NumberTextField<N extends Number & Comparable<N>> extends TextField<N>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/TextArea.java`
#### Snippet
```java
public class TextArea<T> extends AbstractTextComponent<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RangeTextField.java`
#### Snippet
```java
public class RangeTextField<N extends Number & Comparable<N>> extends NumberTextField<N>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RequiredTextField.java`
#### Snippet
```java
public class RequiredTextField<T> extends TextField<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Button.java`
#### Snippet
```java
public class Button extends FormComponent<String> implements IFormSubmittingComponent
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Check.java`
#### Snippet
```java
public class Check<T> extends LabeledWebMarkupContainer implements IGenericComponent<T, Check<T>>
{
	private static final long serialVersionUID = 1L;

	private static final String ATTR_DISABLED = "disabled";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxSelector.java`
#### Snippet
```java
	private final Behavior cleanup = new Behavior()
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxSelector.java`
#### Snippet
```java
public class CheckBoxSelector extends AbstractCheckSelector
{
	private static final long serialVersionUID = 1L;

	private final Set<CheckBox> connectedCheckBoxes = new HashSet<>();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/SubmitLink.java`
#### Snippet
```java
public class SubmitLink extends AbstractSubmitLink
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java
public class RadioGroup<T> extends FormComponent<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/DropDownChoice.java`
#### Snippet
```java
public class DropDownChoice<T> extends AbstractSingleSelectChoice<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java
	private static class CheckBoxConverter implements IConverter<Boolean>
	{
		private static final long serialVersionUID = 1L;

		private static final IConverter<Boolean> INSTANCE = new CheckBoxConverter();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java
public class CheckBox extends FormComponent<Boolean>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
public abstract class AbstractSingleSelectChoice<T> extends AbstractChoice<T, T>
{
	private static final long serialVersionUID = 1L;

	/** String to display when the selected value is null and nullValid is false. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUploadField.java`
#### Snippet
```java
public class FileUploadField extends FormComponent<List<FileUpload>>
{
	private static final long serialVersionUID = 1L;

	private transient List<FileUpload> fileUploads;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	private static final String NO_RAW_INPUT = "[-NO-RAW-INPUT-]";

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
				.getThrowExceptionOnMissingResource())
			{
				private static final long serialVersionUID = 1L;

				@SuppressWarnings({ "rawtypes", "unchecked" })
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileDescription.java`
#### Snippet
```java
public class FileDescription implements Serializable {

    private static final long serialVersionUID = 1L;

    private final String fileName;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FilesSelectedBehavior.java`
#### Snippet
```java
        return new FilesSelectedBehavior()
        {
            private static final long serialVersionUID = 1L;

            @Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FilesSelectedBehavior.java`
#### Snippet
```java
public abstract class FilesSelectedBehavior extends OnChangeAjaxBehavior {

    private static final long serialVersionUID = 1L;

    private static final ResourceReference JS = new JavaScriptResourceReference(FilesSelectedBehavior.class, "FilesSelectedBehavior.js");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(Form.class);

	private static final long serialVersionUID = 1L;

	public static final String UPLOAD_FAILED_RESOURCE_KEY = "uploadFailed";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/FormComponentFeedbackIndicator.java`
#### Snippet
```java
		.key(FormComponentFeedbackIndicator.class, "error");

	private static final long serialVersionUID = 1L;

	/** The message filter for this indicator component */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/FormComponentFeedbackIndicator.java`
#### Snippet
```java
		add(new WebMarkupContainer("indicator")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/FormValidatorAdapter.java`
#### Snippet
```java
public class FormValidatorAdapter extends Behavior implements IFormValidator
{
	private static final long serialVersionUID = 1L;

	private final IFormValidator validator;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/MultiFileUploadField.java`
#### Snippet
```java
	private class CaptionModel implements IModel<String>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/MultiFileUploadField.java`
#### Snippet
```java
public class MultiFileUploadField extends FormComponentPanel<Collection<FileUpload>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/EqualPasswordInputValidator.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;


```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/EqualInputValidator.java`
#### Snippet
```java
public class EqualInputValidator extends AbstractFormValidator
{
	private static final long serialVersionUID = 1L;

	/** form components to be checked. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/FormComponentFeedbackBorder.java`
#### Snippet
```java
	private final class ErrorIndicator extends WebMarkupContainer
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/FormComponentFeedbackBorder.java`
#### Snippet
```java
public class FormComponentFeedbackBorder extends Border implements IFeedback
{
	private static final long serialVersionUID = 1L;

	private static final String ERROR_CSS_CLASS_KEY = CssUtils
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/AbstractLink.java`
#### Snippet
```java
public abstract class AbstractLink extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	/** this link's label/body. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/PopupSettings.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(PopupSettings.class);

	private static final long serialVersionUID = 1L;

	/** Flag to include location bar */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/DisabledAttributeLinkBehavior.java`
#### Snippet
```java
{

    private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ResourceLink.java`
#### Snippet
```java
public class ResourceLink<T> extends Link<T> implements IRequestListener
{
	private static final long serialVersionUID = 1L;

	/** The Resource reference */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ClientSideImageMap.java`
#### Snippet
```java
	private static class Area extends Behavior
	{
		private static final long serialVersionUID = 1L;

		private final String coordinates;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ClientSideImageMap.java`
#### Snippet
```java
public class ClientSideImageMap extends Panel
{
	private static final long serialVersionUID = 1L;
	private static final String CIRCLE = "circle";
	private static final String POLYGON = "poly";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ClientSideImageMap.java`
#### Snippet
```java
	private class UsemapModel extends Model<String>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/StatelessLink.java`
#### Snippet
```java
public abstract class StatelessLink<T> extends Link<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/PopupCloseLink.java`
#### Snippet
```java
	public static final class ClosePopupPage extends WebPage
	{
		private static final long serialVersionUID = 1L;
	}

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/PopupCloseLink.java`
#### Snippet
```java
public class PopupCloseLink<T> extends Link<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/BookmarkablePageLink.java`
#### Snippet
```java
public class BookmarkablePageLink<T> extends Link<T>
{
	private static final long serialVersionUID = 1L;

	/** The page class that this link links to. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/InlineFrame.java`
#### Snippet
```java
public class InlineFrame extends WebMarkupContainer implements IRequestListener
{
	private static final long serialVersionUID = 1L;

	/** The provider of the page. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
public abstract class Link<T> extends AbstractLink implements IRequestListener, IGenericComponent<T, Link<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/AbstractFormValidator.java`
#### Snippet
```java
public abstract class AbstractFormValidator extends Behavior implements IFormValidator
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java
public abstract class Loop extends AbstractRepeater
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/OddEvenListItem.java`
#### Snippet
```java
public class OddEvenListItem<T> extends ListItem<T>
{
	private static final long serialVersionUID = 1L;

	public static final String ODD_CSS_CLASS_KEY = CssUtils.key(OddEvenListItem.class, "odd");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PropertyListView.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/LoopItem.java`
#### Snippet
```java
public class LoopItem extends AbstractItem
{
	private static final long serialVersionUID = 1L;

	/** The index of the ListItem in the parent ListView */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListItemModel.java`
#### Snippet
```java
public class ListItemModel<T> implements IModel<T>
{
	private static final long serialVersionUID = 1L;

	/** The ListView itself */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
public class ExternalLink extends AbstractLink
{
	private static final long serialVersionUID = 1L;

	private boolean contextRelative = false;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListItem.java`
#### Snippet
```java
public class ListItem<T> extends LoopItem implements IGenericComponent<T, ListItem<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/AbstractItem.java`
#### Snippet
```java
public class AbstractItem extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/basic/MultiLineLabel.java`
#### Snippet
```java
public class MultiLineLabel extends WebComponent
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/basic/EnclosureContainer.java`
#### Snippet
```java
public class EnclosureContainer extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	/** The child component to delegate the isVisible() call to */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java
public abstract class PageableListView<T> extends ListView<T> implements IPageableItems
{
	private static final long serialVersionUID = 1L;

	/** The page to show. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/basic/EnumLabel.java`
#### Snippet
```java
public class EnumLabel<T extends Enum<T>> extends WebComponent implements IGenericComponent<T, EnumLabel<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/basic/Label.java`
#### Snippet
```java
public class Label extends WebComponent
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
public class Source extends Image
{
	private static final long serialVersionUID = 1L;

	private String media = null;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ContextPathGenerator.java`
#### Snippet
```java
public class ContextPathGenerator extends Behavior
{
	private static final long serialVersionUID = 1L;

	private final IModel<String> contextRelativePath;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/InlineImage.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private PackageResourceReference packageResourceReference;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/NonCachingImage.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/debug/PageView.java`
#### Snippet
```java
	}

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/debug/PageView.java`
#### Snippet
```java
		add(new ListView<ComponentData>("components", data)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/debug/PageView.java`
#### Snippet
```java
	private static class ComponentData implements IClusterable
	{
		private static final long serialVersionUID = 1L;

		/** Component path. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ContextImage.java`
#### Snippet
```java
public class ContextImage extends WebComponent
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Picture.java`
#### Snippet
```java
public class Picture extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResource.java`
#### Snippet
```java
public class DefaultButtonImageResource extends RenderedDynamicImageResource
{
	private static final long serialVersionUID = 1L;

	/** The default height for button images */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/BlobImageResource.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
public class RadioChoice<T> extends AbstractSingleSelectChoice<T>
{
	private static final long serialVersionUID = 1L;

	private String prefix = "";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java
public class Image extends WebComponent implements IRequestListener
{
	private static final long serialVersionUID = 1L;

	/** The image resource this image component references (src attribute) */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/BufferedDynamicImageResource.java`
#### Snippet
```java
public class BufferedDynamicImageResource extends DynamicImageResource
{
	private static final long serialVersionUID = 1L;

	/** The byte array holding the contents of the dynamic image */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalSource.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private String media = null;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/RenderedDynamicImageResource.java`
#### Snippet
```java
public abstract class RenderedDynamicImageResource extends DynamicImageResource
{
	private static final long serialVersionUID = 1L;

	/** Height of image */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
		return new Link<Void>(id)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
public abstract class ListView<T> extends AbstractRepeater
{
	private static final long serialVersionUID = 1L;

	/** Index of the first item to show */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
		return new Link<Void>(id)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
		return new Link<Void>(id)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/LocalizedImageResource.java`
#### Snippet
```java
public final class LocalizedImageResource implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/LocalizedImageResource.java`
#### Snippet
```java
		}

		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/Source.java`
#### Snippet
```java
public class Source extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	private boolean displayType;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/audio/Audio.java`
#### Snippet
```java
public class Audio extends MediaComponent
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/video/Video.java`
#### Snippet
```java
public class Video extends MediaComponent
{
	private static final long serialVersionUID = 1L;

	private Integer width;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/InternalErrorPage.java`
#### Snippet
```java
public class InternalErrorPage extends AbstractErrorPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/PageExpiredErrorPage.java`
#### Snippet
```java
public class PageExpiredErrorPage extends AbstractErrorPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/MediaComponent.java`
#### Snippet
```java
public abstract class MediaComponent extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/media/Track.java`
#### Snippet
```java
public class Track extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/AbstractErrorPage.java`
#### Snippet
```java
public abstract class AbstractErrorPage extends WebPage
{
	private static final long serialVersionUID = 1L;

	protected AbstractErrorPage()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/RedirectPage.java`
#### Snippet
```java
public class RedirectPage extends WebPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/DownloadLink.java`
#### Snippet
```java
public class DownloadLink extends Link<File>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/AccessDeniedPage.java`
#### Snippet
```java
public class AccessDeniedPage extends AbstractErrorPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/EmptyPanel.java`
#### Snippet
```java
public final class EmptyPanel extends Panel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/BrowserInfoPage.java`
#### Snippet
```java
		browserInfoForm = new BrowserInfoForm("postback", properties)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/BrowserInfoPage.java`
#### Snippet
```java
public class BrowserInfoPage extends WebPage
{
	private static final long serialVersionUID = 1L;

	private BrowserInfoForm browserInfoForm;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/ExceptionErrorPage.java`
#### Snippet
```java
public class ExceptionErrorPage extends AbstractErrorPage
{
	private static final long serialVersionUID = 1L;

	/** Keep a reference to the root cause. WicketTester will use it */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/ExceptionErrorPage.java`
#### Snippet
```java
		add(new Link<Void>("displayPageViewLink")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/ComponentFeedbackPanel.java`
#### Snippet
```java
{
	/** For serialization. */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/GenericPanel.java`
#### Snippet
```java
public class GenericPanel<T> extends Panel implements IGenericComponent<T, GenericPanel<T>>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/Panel.java`
#### Snippet
```java
public abstract class Panel extends WebMarkupContainer implements IQueueRegion
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/Fragment.java`
#### Snippet
```java
public class Fragment extends WebMarkupContainer implements IQueueRegion
{
	private static final long serialVersionUID = 1L;

	/** The wicket:id of the associated markup fragment */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderPanel.java`
#### Snippet
```java
public abstract class BorderPanel extends Panel
{
	private static final long serialVersionUID = 1L;

	private Body body;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Body.java`
#### Snippet
```java
public class Body extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	private final MarkupContainer markupProvider;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/BrowserInfoForm.java`
#### Snippet
```java
		Form<ClientProperties> form = new Form<ClientProperties>(componentId, properties)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/BrowserInfoForm.java`
#### Snippet
```java
public class BrowserInfoForm extends GenericPanel<ClientProperties>
{
	private static final long serialVersionUID = 1L;
	
	public static final ResourceReference JS = new JavaScriptResourceReference(BrowserInfoForm.class, "wicket-browser-info.js");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/include/Include.java`
#### Snippet
```java
public class Include extends WebComponent implements IGenericComponent<String, Include>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
		WebMarkupContainer messagesContainer = new WebMarkupContainer("feedbackul")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
			return new IModel<FeedbackMessage>()
			{
				private static final long serialVersionUID = 1L;

				/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
	private final class MessageListView extends ListView<FeedbackMessage>
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
    }

	private static final long serialVersionUID = 1L;

	/** Message view */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderItemsContainer.java`
#### Snippet
```java
public class HtmlHeaderItemsContainer extends HtmlHeaderContainer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
public class BorderBehavior extends Behavior
{
	private static final long serialVersionUID = 1L;

	// markup stream associated with this border. bonus of keeping a reference
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/InlineEnclosure.java`
#### Snippet
```java
public class InlineEnclosure extends Enclosure
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigationLink.java`
#### Snippet
```java
public class PagingNavigationLink<T> extends Link<T>
{
	private static final long serialVersionUID = 1L;

	/** The pageable list view. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java
public class HtmlHeaderContainer extends TransparentWebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/Enclosure.java`
#### Snippet
```java
public class Enclosure extends WebMarkupContainer implements IComponentResolver
{
	private static final long serialVersionUID = 1L;

	/** The child component to delegate the isVisible() call to */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
	public class BorderBodyContainer extends WebMarkupContainer implements IQueueRegion
	{
		private static final long serialVersionUID = 1L;

		/** The markup */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
public abstract class Border extends WebMarkupContainer implements IComponentResolver, IQueueRegion
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigationIncrementLink.java`
#### Snippet
```java
public class PagingNavigationIncrementLink<T> extends Link<T>
{
	private static final long serialVersionUID = 1L;

	/** The increment. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigator.java`
#### Snippet
```java
	private final class TitleAppender extends Behavior
	{
		private static final long serialVersionUID = 1L;

		private final String resourceKey;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigator.java`
#### Snippet
```java
public class PagingNavigator extends Panel
{
	private static final long serialVersionUID = 1L;

	/** The navigation bar to be printed, e.g. 1 | 2 | 3 etc. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigation.java`
#### Snippet
```java
public class PagingNavigation extends Loop
{
	private static final long serialVersionUID = 1L;

	/** The PageableListView this navigation is navigating. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigation.java`
#### Snippet
```java
	private final class TitleAppender extends Behavior
	{
		private static final long serialVersionUID = 1L;
		/** resource key for the message */
		private static final String RES = "PagingNavigation.page";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/AbstractMarkupFilter.java`
#### Snippet
```java
	protected final static MetaDataKey<Map<String, AtomicInteger>> REQUEST_COUNTER_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/EnclosureHandler.java`
#### Snippet
```java
public final class EnclosureHandler extends AbstractMarkupFilter implements IComponentResolver
{
	private static final long serialVersionUID = 1L;

	private static final IAutoComponentFactory FACTORY = new IAutoComponentFactory()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalImage.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/** The x values to be used within the srcset */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java
	public static final Behavior RELATIVE_PATH_BEHAVIOR = new Behavior()
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java
		IComponentResolver
{
	private static final long serialVersionUID = 1L;

	/** Logging */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
		IComponentResolver
{
	private static final long serialVersionUID = 1L;

	/** The Component id prefix. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketMessageTagHandler.java`
#### Snippet
```java
	public static class AttributeLocalizer extends Behavior
	{
		private static final long serialVersionUID = 1L;

		private final String wicketMessageAttrName;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketMessageTagHandler.java`
#### Snippet
```java
{
	/** */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketLinkTagHandler.java`
#### Snippet
```java
public class WicketLinkTagHandler extends AbstractMarkupFilter implements IComponentResolver
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractChoice.java`
#### Snippet
```java
public abstract class AbstractChoice<T, E> extends FormComponent<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/OddEvenItem.java`
#### Snippet
```java
public class OddEvenItem<T> extends Item<T>
{
	private static final long serialVersionUID = 1L;

	public static final String ODD_CSS_CLASS_KEY = CssUtils.key(OddEvenItem.class, "odd");
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/RepeatingView.java`
#### Snippet
```java
public class RepeatingView extends AbstractRepeater
{
	private static final long serialVersionUID = 1L;

	/** Counter used for generating unique child component ids. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/Item.java`
#### Snippet
```java
	public static class IndexComparator implements Comparator<Item<?>>, Serializable
	{
		private static final long serialVersionUID = 1L;
		private static final Comparator<Item<?>> instance = new IndexComparator();

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/Item.java`
#### Snippet
```java
public class Item<T> extends ListItem<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/EmptyDataProvider.java`
#### Snippet
```java
public class EmptyDataProvider<T> implements IDataProvider<T>
{
	private static final long serialVersionUID = 1L;

	private static EmptyDataProvider<?> INSTANCE = new EmptyDataProvider<Void>();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java
{
	/** */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/ListDataProvider.java`
#### Snippet
```java
public class ListDataProvider<T extends Serializable> implements IDataProvider<T>
{
	private static final long serialVersionUID = 1L;

	/** reference to the list used as dataprovider for the dataview */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
public abstract class AbstractRepeater extends WebMarkupContainer
{
	private static final long serialVersionUID = 1L;

	private static final Logger log = LoggerFactory.getLogger(AbstractRepeater.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/DefaultItemReuseStrategy.java`
#### Snippet
```java
public class DefaultItemReuseStrategy implements IItemReuseStrategy
{
	private static final long serialVersionUID = 1L;

	private static final IItemReuseStrategy instance = new DefaultItemReuseStrategy();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/ReuseIfModelsEqualStrategy.java`
#### Snippet
```java
public class ReuseIfModelsEqualStrategy implements IItemReuseStrategy
{
	private static final long serialVersionUID = 1L;

	private static IItemReuseStrategy instance = new ReuseIfModelsEqualStrategy();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/DataView.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketContainerResolver.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(WicketContainerResolver.class);

	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/DataViewBase.java`
#### Snippet
```java
public abstract class DataViewBase<T> extends AbstractPageableView<T>
{
	private static final long serialVersionUID = 1L;
	private final IDataProvider<T> dataProvider;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/RefreshingView.java`
#### Snippet
```java
public abstract class RefreshingView<T> extends RepeatingView
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltTransformerBehavior.java`
#### Snippet
```java
public class XsltTransformerBehavior extends AbstractTransformerBehavior
{
	private static final long serialVersionUID = 1L;

	/** An optional xsl file path */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/GridView.java`
#### Snippet
```java
public abstract class GridView<T> extends DataViewBase<T>
{
	private static final long serialVersionUID = 1L;

	private int columns = 1;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/NoopOutputTransformerContainer.java`
#### Snippet
```java
public class NoopOutputTransformerContainer extends AbstractOutputTransformerContainer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractTransformerBehavior.java`
#### Snippet
```java
public abstract class AbstractTransformerBehavior extends Behavior implements ITransformer
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltOutputTransformerContainer.java`
#### Snippet
```java
public class XsltOutputTransformerContainer extends AbstractOutputTransformerContainer
{
	private static final long serialVersionUID = 1L;

	/** An optional xsl file path */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
public class HtmlHeaderResolver implements IComponentResolver
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
	private static class WicketHeadContainer extends WebMarkupContainer
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
		ITransformer
{
	private static final long serialVersionUID = 1L;

	/** Whether the containers tag shall be transformed as well */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
			IComponentResolver
	{
		private static final long serialVersionUID = 1L;

		private final String attribute;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
			IComponentResolver
	{
		private static final long serialVersionUID = 1L;

		private final String anchor;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(AutoLinkResolver.class);

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycleListenerCollection.java`
#### Snippet
```java
{
	private static final Logger logger = LoggerFactory.getLogger(RequestCycleListenerCollection.class);
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/JavaScriptPackageResource.java`
#### Snippet
```java
public class JavaScriptPackageResource extends PackageResource
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceLogData.java`
#### Snippet
```java
public abstract class ResourceLogData implements ILogData
{
	private static final long serialVersionUID = 1L;

	private final String name;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java
	private static class MessageContainer extends MarkupContainer implements IComponentResolver
	{
		private static final long serialVersionUID = 1L;

		private static final String NOT_FOUND = "[Warning: Property for '%s' not found]";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java
public class WicketMessageResolver implements IComponentResolver
{
	private static final long serialVersionUID = 1L;

	private static final Logger log = LoggerFactory.getLogger(WicketMessageResolver.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ByteArrayResource.java`
#### Snippet
```java
public class ByteArrayResource extends BaseDataResource<byte[]>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/SharedResourceReference.java`
#### Snippet
```java
			return new AbstractResource()
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/SharedResourceReference.java`
#### Snippet
```java
public class SharedResourceReference extends ResourceReference
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ContextRelativeResourceReference.java`
#### Snippet
```java
	
	/** The Constant serialVersionUID. */
	private static final long serialVersionUID = 1L;
	
	/** Says if the resource name can be minified or not. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/JavaScriptResourceReference.java`
#### Snippet
```java
public class JavaScriptResourceReference extends PackageResourceReference
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ContextRelativeResource.java`
#### Snippet
```java
{
	private static final String CACHE_PREFIX = "context-relative:/";
	private static final long serialVersionUID = 1L;

	private static final Logger log = LoggerFactory.getLogger(ContextRelativeResource.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/DynamicImageResource.java`
#### Snippet
```java
public abstract class DynamicImageResource extends AbstractResource
{
	private static final long serialVersionUID = 1L;

	/** The image type */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/CharSequenceResource.java`
#### Snippet
```java
public class CharSequenceResource extends BaseDataResource<CharSequence>
{
	private static final long serialVersionUID = 1L;

	/** Charset name for resource */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/MetaInfStaticResourceReference.java`
#### Snippet
```java
public class MetaInfStaticResourceReference extends PackageResourceReference
{
	private static final long serialVersionUID = -1858339228780709471L;

	private static Boolean META_INF_RESOURCES_SUPPORTED = null;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/CssResourceReference.java`
#### Snippet
```java
public class CssResourceReference extends PackageResourceReference
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/CssPackageResource.java`
#### Snippet
```java
public class CssPackageResource extends PackageResource
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceStreamResource.java`
#### Snippet
```java
public class ResourceStreamResource extends AbstractResource
{
	private static final long serialVersionUID = 1L;

	private static final Logger logger = LoggerFactory.getLogger(ResourceStreamResource.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/BaseDataResource.java`
#### Snippet
```java
public abstract class BaseDataResource<T> extends AbstractResource
{
	private static final long serialVersionUID = 1L;

	/** the content type */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResourceReference.java`
#### Snippet
```java
public class PackageResourceReference extends ResourceReference
{
	private static final long serialVersionUID = 1L;

	private static final Logger log = LoggerFactory.getLogger(PackageResourceReference.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
	public static final MetaDataKey<Long> CONTENT_RANGE_STARTBYTE = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
	public static final MetaDataKey<Long> CONTENT_RANGE_ENDBYTE = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
public abstract class AbstractResource implements IResource
{
	private static final long serialVersionUID = 1L;

	/** header values that are managed internally and must not be set directly */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/RequestCycleCachedResourceVersion.java`
#### Snippet
```java
		new MetaDataKey<>()
		{
			private static final long serialVersionUID = 1L;
		};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/behavior/AttributeAppender.java`
#### Snippet
```java
public class AttributeAppender extends AttributeModifier
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(PackageResource.class);

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	private class ProcessingResourceStream extends ResourceStreamWrapper
	{
		private static final long serialVersionUID = 1L;

		private ProcessingResourceStream(IResourceStream delegate)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
			IWicketInternalException
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/behavior/InvalidBehaviorIdException.java`
#### Snippet
```java
public class InvalidBehaviorIdException extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;

	private final Component component;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/ComponentFeedbackMessageFilter.java`
#### Snippet
```java
public class ComponentFeedbackMessageFilter implements IFeedbackMessageFilter
{
	private static final long serialVersionUID = 1L;

	/** The component to accept feedback messages for */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/behavior/AbstractAjaxBehavior.java`
#### Snippet
```java
public abstract class AbstractAjaxBehavior extends Behavior implements IRequestListener
{
	private static final long serialVersionUID = 1L;

	/** the component that this handler is bound to. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/ErrorLevelFeedbackMessageFilter.java`
#### Snippet
```java
public class ErrorLevelFeedbackMessageFilter implements IFeedbackMessageFilter
{
	private static final long serialVersionUID = 1L;

	/** The minimum error level */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackDelay.java`
#### Snippet
```java
	private static final MetaDataKey<FeedbackDelay> KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};
	
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/ContainerFeedbackMessageFilter.java`
#### Snippet
```java
public class ContainerFeedbackMessageFilter implements IFeedbackMessageFilter
{
	private static final long serialVersionUID = 1L;

	private final MarkupContainer container;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/FencedFeedbackPanel.java`
#### Snippet
```java
{
	
	private static final long serialVersionUID = 1L;

	private static final MetaDataKey<Integer> FENCE_KEY = new MetaDataKey<>()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/FencedFeedbackPanel.java`
#### Snippet
```java
		return new FeedbackMessagesModel(this)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/FencedFeedbackPanel.java`
#### Snippet
```java
	private static final MetaDataKey<Integer> FENCE_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/DefaultCleanupFeedbackMessageFilter.java`
#### Snippet
```java
public class DefaultCleanupFeedbackMessageFilter implements IFeedbackMessageFilter
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/session/HttpSessionStore.java`
#### Snippet
```java
			Serializable
	{
		private static final long serialVersionUID = 1L;

		/** The unique key of the application within this web application. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessagesModel.java`
#### Snippet
```java
public class FeedbackMessagesModel implements IModel<List<FeedbackMessage>>
{
	private static final long serialVersionUID = 1L;

	/** Message filter */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessages.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(FeedbackMessages.class);

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessage.java`
#### Snippet
```java
public class FeedbackMessage implements IDetachable
{
	private static final long serialVersionUID = 1L;

	public static final String UNDEFINED_CSS_CLASS_KEY = CssUtils.key(FeedbackMessage.class,
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
public abstract class ResourceReference implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private final Key data;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	public static class Key implements Serializable
	{
		private static final long serialVersionUID = 1L;

		private final String scope;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	public static final class LambdaResourceReference extends ResourceReference
	{
		private static final long serialVersionUID = 1826862147241009289L;
		
		final SerializableSupplier<IResource> resourceBuilder;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
		return new Behavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
		IComponentAwareHeaderContributor
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/PageExpiredException.java`
#### Snippet
```java
public class PageExpiredException extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java
public class WicketServlet extends HttpServlet
{
	private static final long serialVersionUID = 1L;

	/** Log. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
	class RequestData implements IClusterable
	{
		private static final long serialVersionUID = 1L;

		private long startDate;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
	class SessionData implements IClusterable, Comparable<SessionData>
	{
		private static final long serialVersionUID = 1L;

		private final String sessionId;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebSession.java`
#### Snippet
```java
public class WebSession extends Session
{
	private static final long serialVersionUID = 1L;

	public static WebSession get()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/StoredResponsesMap.java`
#### Snippet
```java
class StoredResponsesMap extends MostRecentlyUsedMap<String, Object>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ReloadingWicketServlet.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java
public class ListChoice<T> extends DropDownChoice<T>
{
	private static final long serialVersionUID = 1L;

	/** The default maximum number of rows to display. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ClientProperties.java`
#### Snippet
```java
public class ClientProperties implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private int browserHeight = -1;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplicationFactoryCreationException.java`
#### Snippet
```java
public class WebApplicationFactoryCreationException extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpSession.java`
#### Snippet
```java
public class MockHttpSession implements HttpSession, Serializable
{
	private static final long serialVersionUID = 1L;

	private final ValueMap attributes = new ValueMap();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	private static MetaDataKey<RequestData> REQUEST_DATA = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AbstractRequestLogger.java`
#### Snippet
```java
	private static MetaDataKey<SessionData> SESSION_DATA = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/request/WebClientInfo.java`
#### Snippet
```java
public class WebClientInfo extends ClientInfo
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ResponseIOException.java`
#### Snippet
```java
{
	/** */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/UploadInfo.java`
#### Snippet
```java
public class UploadInfo implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private transient long timeStarted;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/CircularDependencyException.java`
#### Snippet
```java
public final class CircularDependencyException extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;


```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/JQueryResourceReference.java`
#### Snippet
```java
public class JQueryResourceReference extends JavaScriptResourceReference
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/JQueryPluginResourceReference.java`
#### Snippet
```java
public class JQueryPluginResourceReference extends JavaScriptResourceReference
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemJarPathService.java`
#### Snippet
```java
	public static final MetaDataKey<Map<String, FileSystem>> FILE_SYSTEM_META_DATA_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemResource.java`
#### Snippet
```java
	private static class PathModel extends LoadableDetachableModel<Path>
	{
		private static final long serialVersionUID = 1L;
		private final String pathAsString;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemResource.java`
#### Snippet
```java
public class FileSystemResource extends AbstractResource
{
	private static final long serialVersionUID = 1L;

	private LoadableDetachableModel<Path> path;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemResourceReference.java`
#### Snippet
```java
public class FileSystemResourceReference extends ResourceReference
{
	private static final long serialVersionUID = 1L;

	private Path path;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ResourceBundleReference.java`
#### Snippet
```java
public class ResourceBundleReference extends ResourceReference implements IResourceBundle
{
	private static final long serialVersionUID = 1L;

	private final ResourceReference bundleRef;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ReplacementResourceBundleReference.java`
#### Snippet
```java
public class ReplacementResourceBundleReference extends ResourceBundleReference
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/settings/ExceptionSettings.java`
#### Snippet
```java
	public static final class UnexpectedExceptionDisplay extends EnumeratedType
	{
		private static final long serialVersionUID = 1L;

		UnexpectedExceptionDisplay(final String name)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
	private static final Logger log = LoggerFactory.getLogger(ConcatBundleResource.class);

	private static final long serialVersionUID = 1L;

	private final List<? extends IReferenceHeaderItem> providedResources;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
		AbstractResourceStream ret = new AbstractResourceStream()
		{
			private static final long serialVersionUID = 1L;

			private byte[] bytes;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/TextTemplateResourceReference.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private final TextTemplate textTemplate;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/RequestPageStore.java`
#### Snippet
```java
	private static final MetaDataKey<RequestData> KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/CryptingPageStore.java`
#### Snippet
```java
	private static class SessionData implements Serializable
	{
		private static final long serialVersionUID = 1L;

		private final SecretKey key;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/CryptingPageStore.java`
#### Snippet
```java
	private static final MetaDataKey<SessionData> KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java
	private static final MetaDataKey<SessionData> KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DiskPageStore.java`
#### Snippet
```java
	protected static class DiskData implements Serializable
	{
		private static final long serialVersionUID = 1L;

		private transient DiskPageStore pageStore;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
	private static final MetaDataKey<SessionData> KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`writeObject()` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
		 * Serialize pages before writing to output.
		 */
		private void writeObject(final ObjectOutputStream output) throws IOException
		{
			// handle non-serialized pages
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/ValidatorAdapter.java`
#### Snippet
```java
public class ValidatorAdapter<T> extends Behavior implements IValidator<T>
{
	private static final long serialVersionUID = 1L;

	private final IValidator<T> validator;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatResourceBundleReference.java`
#### Snippet
```java
	ResourceReference implements IResourceBundle
{
	private static final long serialVersionUID = 1L;

	private final List<T> providedResources;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/PageWindowManager.java`
#### Snippet
```java
public class PageWindowManager implements Serializable
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/PageWindowManager.java`
#### Snippet
```java
	public static class FileWindow implements IPersistedPage, Serializable
	{
		private static final long serialVersionUID = 1L;

		/** id of data or -1 if the window is empty */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/EmailAddressValidator.java`
#### Snippet
```java
public class EmailAddressValidator extends PatternValidator
{
	private static final long serialVersionUID = 1L;

	/** singleton instance */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/RangeValidator.java`
#### Snippet
```java
	AbstractRangeValidator<Z, Z>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/StringValidator.java`
#### Snippet
```java
public class StringValidator extends AbstractRangeValidator<Integer, String>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/CompoundValidator.java`
#### Snippet
```java
public class CompoundValidator<T> extends Behavior implements IValidator<T>
{
	private static final long serialVersionUID = 1L;

	private final List<IValidator<T>> validators = new ArrayList<IValidator<T>>(2);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/PatternValidator.java`
#### Snippet
```java
public class PatternValidator implements IValidator<String>
{
	private static final long serialVersionUID = 1L;

	/** the pattern to match */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/AbstractRangeValidator.java`
#### Snippet
```java
	extends Behavior implements IValidator<V>
{
	private static final long serialVersionUID = 1L;
	private R minimum;
	private R maximum;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/application/ComponentOnBeforeRenderListenerCollection.java`
#### Snippet
```java
		IComponentOnBeforeRenderListener
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/application/HeaderContributorListenerCollection.java`
#### Snippet
```java
		IHeaderContributor
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/DateValidator.java`
#### Snippet
```java
public class DateValidator extends RangeValidator<Date>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/UrlValidator.java`
#### Snippet
```java
public class UrlValidator implements IValidator<String>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/application/OnComponentTagListenerCollection.java`
#### Snippet
```java
	implements IOnComponentTagListener {

	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/application/ComponentInitializationListenerCollection.java`
#### Snippet
```java
		IComponentInitializationListener
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/application/ComponentOnAfterRenderListenerCollection.java`
#### Snippet
```java
	ListenerCollection<IComponentOnAfterRenderListener> implements IComponentOnAfterRenderListener
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/application/ComponentOnConfigureListenerCollection.java`
#### Snippet
```java
		IComponentOnConfigureListener
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/authorization/Action.java`
#### Snippet
```java
	public static final String ENABLE = "ENABLE";

	private static final long serialVersionUID = -1L;

	/** The name of this action. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/authorization/UnauthorizedInstantiationException.java`
#### Snippet
```java
public class UnauthorizedInstantiationException extends AuthorizationException
{
	private static final long serialVersionUID = 1L;

	/** The component class that could not be instantiated */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/application/ComponentInstantiationListenerCollection.java`
#### Snippet
```java
	ListenerCollection<IComponentInstantiationListener> implements IComponentInstantiationListener
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/authorization/AuthorizationException.java`
#### Snippet
```java
public abstract class AuthorizationException extends WicketRuntimeException
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/authorization/UnauthorizedActionException.java`
#### Snippet
```java
public class UnauthorizedActionException extends AuthorizationException
{
	private static final long serialVersionUID = 1L;

	/** The action */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/CreditCardValidator.java`
#### Snippet
```java
public class CreditCardValidator implements IValidator<String>
{
	private static final long serialVersionUID = 1L;

	/** */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-core/src/main/java/org/apache/wicket/validation/ValidationError.java`
#### Snippet
```java
public final class ValidationError implements IValidationError
{
	private static final long serialVersionUID = 1L;

	/** list of message keys to try against the <code>IErrorMessageSource</code> */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/WicketExamplePage.java`
#### Snippet
```java
public class WicketExamplePage extends WebPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/csp/NonceDemoPage.java`
#### Snippet
```java
		add(new AjaxLink<String>("clickMe")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/csp/NonceDemoPage.java`
#### Snippet
```java
		final WebMarkupContainer delayedVisible = new WebMarkupContainer("delayedVisible")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/RatingsPage.java`
#### Snippet
```java

	/** For serialization. */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/RatingsPage.java`
#### Snippet
```java
	{
		/** For serialization. */
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		add(new AjaxLink<Void>("downloadReference")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
	public static class StaticResource extends ResourceStreamResource
	{
		private static final long serialVersionUID = 1L;

		StaticResource() {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		add(new AjaxLink<Void>("downloadInNewWindow")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
	private static class ExampleResource extends ResourceStreamResource
	{
		private static final long serialVersionUID = 1L;

		private String content;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		final AjaxDownloadBehavior download = new AjaxDownloadBehavior(resource)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		add(new AjaxLink<Void>("downloadInSameWindow")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		final AjaxDownloadBehavior download = new AjaxDownloadBehavior(reference)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
public class AjaxDownloadPage extends BasePage
{
	private static final long serialVersionUID = 1L;
	private final WebMarkupContainer downloadingContainer;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		final AjaxDownloadBehavior download = new AjaxDownloadBehavior(resource)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		final AjaxDownloadBehavior download = new AjaxDownloadBehavior(DynamicTextFileResource.instance)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		add(new AjaxLink<Void>("download")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		ResourceReference reference = new ResourceReference("referenceToResource")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		final AjaxDownloadBehavior download = new AjaxDownloadBehavior(resource)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
		add(new AjaxLink<Void>("downloadIframe")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java

		final AjaxDownloadBehavior download = new AjaxDownloadBehavior(resource) {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LazyLoadingPage.java`
#### Snippet
```java
			nonBlockingRepeater.add(new AjaxLazyLoadPanel<Label>(nonBlockingRepeater.newChildId())
			{
				private static final long serialVersionUID = 1L;

				private long startTime = System.currentTimeMillis();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LazyLoadingPage.java`
#### Snippet
```java
			blockingRepeater.add(new AjaxLazyLoadPanel<Label>(blockingRepeater.newChildId())
			{
				private static final long serialVersionUID = 1L;

				private int seconds = r.nextInt(5);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java
	public static class TodoItem implements IClusterable
	{
		private static final long serialVersionUID = 1L;

		/** Is the item done? */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/prototype/Index.java`
#### Snippet
```java
		add(new Link<Void>("link")
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/prototype/Index.java`
#### Snippet
```java
public class Index extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/** Click count. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/bean/validation/Person.java`
#### Snippet
```java
public class Person implements Serializable
{
	private static final long serialVersionUID = 1L;

	@NotNull
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/bean/validation/BeanValidationPage.java`
#### Snippet
```java

		Form<?> form = new Form<Void>("form") {
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/bean/validation/BeanValidationPage.java`
#### Snippet
```java
public class BeanValidationPage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;
	Person person = new Person();

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/FooExpansion.java`
#### Snippet
```java
public class FooExpansion implements Set<Foo>, Serializable
{
	private static final long serialVersionUID = 1L;

	private static MetaDataKey<FooExpansion> KEY = new MetaDataKey<>()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/FooExpansion.java`
#### Snippet
```java
	private static MetaDataKey<FooExpansion> KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/Foo.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private String id;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/AbstractTreePage.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/FooPanel.java`
#### Snippet
```java
public class FooPanel extends Panel
{
	private static final long serialVersionUID = 1L;

	public FooPanel(String id, IModel<Foo> foo)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/BeginnersTreePage.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/FooProvider.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/FooProvider.java`
#### Snippet
```java
	private static class FooModel extends LoadableDetachableModel<Foo>
	{
		private static final long serialVersionUID = 1L;

		private final String id;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
			form.add(ajaxSubmit = new AjaxButton("ajaxSubmit")
			{
				private static final long serialVersionUID = 1L;

				/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
			final Form<?> form = new Form<Void>("form")
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
			form.add(ajaxSubmit = new AjaxButton("ajaxSubmit")
			{
				private static final long serialVersionUID = 1L;

				/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
public class FileUploadPage extends BasePage
{
	private static final long serialVersionUID = 1L;

	private static class SingleFileUploadSamplePanel extends Panel {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
			final Form<?> form = new Form<Void>("form")
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/AdvancedTreePage.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private Behavior theme;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/AdvancedTreePage.java`
#### Snippet
```java
		tree.add(new Behavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/PanelContent.java`
#### Snippet
```java
public class PanelContent extends Content implements IDetachable
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/LabelContent.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/FolderContent.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MultiLineLabelContent.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private static final String LOREM_IPSUM = " - Lorem ipsum dolor sit amet,\nconsectetur, sadipisci velit...";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MixedContent.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private List<Content> contents;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/NestedTreePage.java`
#### Snippet
```java
		tree = new NestedTree<Foo>("tree", provider, state)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/NestedTreePage.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private NestedTree<Foo> tree;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/TableTreePage.java`
#### Snippet
```java
		final TableTree<Foo, String> tree = new TableTree<Foo, String>("tree", columns, provider, Integer.MAX_VALUE, state)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/TableTreePage.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/TableTreePage.java`
#### Snippet
```java
		columns.add(new AbstractColumn<Foo, String>(Model.of("Depth"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/media/Home.java`
#### Snippet
```java
public final class Home extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/FrameTarget.java`
#### Snippet
```java
public final class FrameTarget implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/** the class of the bookmarkable page. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/Home.java`
#### Snippet
```java
{
	/** */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/LeftFrame.java`
#### Snippet
```java
public class LeftFrame extends WebPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/LeftFrame.java`
#### Snippet
```java
	private static final class ChangeFramePageLink extends Link<Void>
	{
		private static final long serialVersionUID = 1L;

		/** parent frame class. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/ExampleFrame.java`
#### Snippet
```java
{
	/** */
	private static final long serialVersionUID = 1L;

	private CharSequence url;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/BookmarkableFolderContent.java`
#### Snippet
```java
public class BookmarkableFolderContent extends Content
{
	private static final long serialVersionUID = 1L;

	public BookmarkableFolderContent(final AbstractTree<Foo> tree)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/BookmarkableFolderContent.java`
#### Snippet
```java
		return new Folder<Foo>(id, tree, model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/BodyFrame.java`
#### Snippet
```java
		{
			/** */
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/BodyFrame.java`
#### Snippet
```java
{
	/** */
	private static final long serialVersionUID = 1L;

	private final FrameTarget frameTarget = new FrameTarget(Page1.class);
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/SelectableFolderContent.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private ITreeProvider<Foo> provider;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/SelectableFolderContent.java`
#### Snippet
```java
		return new Folder<Foo>(id, tree, model)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/CheckedFolderContent.java`
#### Snippet
```java
				return new IModel<Boolean>()
				{
					private static final long serialVersionUID = 1L;

					@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/CheckedFolderContent.java`
#### Snippet
```java
public class CheckedFolderContent extends Content
{
	private static final long serialVersionUID = 1L;

	private ProviderSubset<Foo> checked;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/CheckedFolderContent.java`
#### Snippet
```java
		return new CheckedFolder<Foo>(id, tree, model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MultiSelectableFolderContent.java`
#### Snippet
```java
		return new Folder<Foo>(id, tree, model)
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MultiSelectableFolderContent.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	private ProviderSubset<Foo> selected;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/EditableFolderContent.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/CheckedSelectableFolderContent.java`
#### Snippet
```java
		return new CheckedFolder<Foo>(id, tree, model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/CheckedSelectableFolderContent.java`
#### Snippet
```java
public class CheckedSelectableFolderContent extends SelectableFolderContent
{
	private static final long serialVersionUID = 1L;

	public CheckedSelectableFolderContent(ITreeProvider<Foo> provider)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/asemail/TemplateBasedOnPage.java`
#### Snippet
```java
public class TemplateBasedOnPage extends WebPage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/Captcha.java`
#### Snippet
```java
public class Captcha extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/asemail/MailTemplatePanel.java`
#### Snippet
```java
public class MailTemplatePanel extends Panel
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/CageForm.java`
#### Snippet
```java
public class CageForm<T> extends AbstractCaptchaForm<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/CaptchaForm.java`
#### Snippet
```java
public class CaptchaForm<T> extends AbstractCaptchaForm<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/MultiUploadPage.java`
#### Snippet
```java
			listItem.add(new Link<Void>("delete")
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/KaptchaForm.java`
#### Snippet
```java
public class KaptchaForm<T> extends AbstractCaptchaForm<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/AbstractCaptchaForm.java`
#### Snippet
```java
public abstract class AbstractCaptchaForm<T> extends GenericPanel<T>
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/asemail/MailTemplate.java`
#### Snippet
```java
		AjaxSubmitLink basedOnTextTemplateLink = new AjaxSubmitLink("textTemplateBasedLink", form)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/asemail/MailTemplate.java`
#### Snippet
```java
		AjaxSubmitLink basedOnPageLink = new AjaxSubmitLink("pageBasedLink", form)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/asemail/MailTemplate.java`
#### Snippet
```java
		AjaxSubmitLink basedOnPanelLink = new AjaxSubmitLink("panelBasedLink", form)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/asemail/MailTemplate.java`
#### Snippet
```java
public class MailTemplate extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/Count.java`
#### Snippet
```java
final class Count implements IClusterable
{
	private static final long serialVersionUID = 1L;

	/** the count */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
	protected class CssSwitchingLink extends Link<Void>
	{
		private static final long serialVersionUID = 1L;

		private final String clazz;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
	private static class TabPanel2 extends Panel
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
	private static class TabPanel1 extends Panel
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
		tabs.add(new AbstractTab(new Model<>("second tab"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
	private static class TabPanel3 extends Panel
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
public class TabbedPanelPage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
		tabs.add(new AbstractTab(new Model<>("first tab"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
		tabs.add(new AbstractTab(new Model<>("third tab"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java
		Form<?> form = new Form<Void>("form")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java
	private static class Input implements IClusterable
	{
		private static final long serialVersionUID = 1L;

		/** the selected sites. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java
		IOptionRenderer<String> renderer = new IOptionRenderer<String>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/PalettePage.java`
#### Snippet
```java
public class PalettePage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/PalettePage.java`
#### Snippet
```java
		Form<Void> form = new Form<Void>("form")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/CheckBoxSelectorPage.java`
#### Snippet
```java
public class CheckBoxSelectorPage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/homepage/HomePage.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SimplePage.java`
#### Snippet
```java
public class SimplePage extends BasePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SimplePage.java`
#### Snippet
```java
		add(new DataView<Contact>("simple", new ContactDataProvider())
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
	private class ZoneDropDownChoice extends DropDownChoice<ZoneId>
	{
		private static final long serialVersionUID = 1L;

		public ZoneDropDownChoice(String id)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
		form.add(new TimeField("time2", new PropertyModel<>(this, "time2"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
	private final class LocaleChoiceRenderer implements IChoiceRenderer<Locale>
	{
		private static final long serialVersionUID = 1L;

		@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
		form.add(new TimeField("time1", new PropertyModel<>(this, "time1"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
public class DateTimePage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	private ZoneId clientZone;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
			super(id, new IModel<List<ZoneId>>()
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
			add(new FormComponentUpdatingBehavior()
			{
				private static final long serialVersionUID = 1L;

				protected void onUpdate()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
			setChoiceRenderer(new IChoiceRenderer<ZoneId>()
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
		form.add(new Link<Void>("defaultLocaleLink")
		{
			private static final long serialVersionUID = 1L;

			public void onClick()
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
			new ZonedToLocalDateTimeModel(zonedDateTime2)
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
			new PropertyModel<>(this, "dateTime0"))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
			add(new FormComponentUpdatingBehavior()
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
	private final class LocaleDropDownChoice extends DropDownChoice<Locale>
	{
		private static final long serialVersionUID = 1L;

		/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
			new ZonedToLocalDateTimeModel(zonedDateTime1)
			{
				private static final long serialVersionUID = 1L;

				@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/RefreshingPage.java`
#### Snippet
```java
public class RefreshingPage extends BasePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/RefreshingPage.java`
#### Snippet
```java
		RefreshingView<Contact> view = new RefreshingView<Contact>("view")
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/RepeatingPage.java`
#### Snippet
```java
public class RepeatingPage extends BasePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/PagingPage.java`
#### Snippet
```java
		DataView<Contact> dataView = new DataView<Contact>("pageable", new ContactDataProvider())
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/PagingPage.java`
#### Snippet
```java
				item.add(AttributeModifier.replace("class", new IModel<String>()
				{
					private static final long serialVersionUID = 1L;

					@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/PagingPage.java`
#### Snippet
```java
public class PagingPage extends BasePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SortingPage.java`
#### Snippet
```java
		add(new OrderByBorder<String>("orderByLastName", "lastName", dp)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SortingPage.java`
#### Snippet
```java
		add(new OrderByBorder<String>("orderByFirstName", "firstName", dp)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SortingPage.java`
#### Snippet
```java
		final DataView<Contact> dataView = new DataView<Contact>("sorting", dp)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/SortingPage.java`
#### Snippet
```java
public class SortingPage extends BasePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/OIRPage.java`
#### Snippet
```java
		final DataView<Contact> dataView = new DataView<Contact>("oir", dp)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/OIRPage.java`
#### Snippet
```java
	private static class HighlitableDataItem<T> extends Item<T>
	{
		private static final long serialVersionUID = 1L;

		private boolean highlite = false;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/OIRPage.java`
#### Snippet
```java
		add(new OrderByBorder<String>("orderByLastName", "lastName", dp)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/OIRPage.java`
#### Snippet
```java
		add(new OrderByBorder<String>("orderByFirstName", "firstName", dp)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/OIRPage.java`
#### Snippet
```java
public class OIRPage extends BasePage
{
	private static final long serialVersionUID = 1L;

	private static class HighlitableDataItem<T> extends Item<T>
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatelessPage1.java`
#### Snippet
```java
public class StatelessPage1 extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	private final Integer number = 10;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatelessPage.java`
#### Snippet
```java
public class StatelessPage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	private final Integer number = 10;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatelessPage.java`
#### Snippet
```java
		StatelessForm<?> statelessForm = new StatelessForm<Void>("statelessform")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/SessionModel.java`
#### Snippet
```java
public class SessionModel implements IModel<String>
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatelessPage2.java`
#### Snippet
```java
public class StatelessPage2 extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	private final Integer number = 10;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/Index.java`
#### Snippet
```java
		add(new StatelessLink<Void>("linkToAjaxExamples")
		{
			private static final long serialVersionUID = 1L;
			
			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/Index.java`
#### Snippet
```java
		add(new StatelessLink<Void>("invalidatesession")
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/Index.java`
#### Snippet
```java
		add(new StatelessLink<Void>("linkToStatefulPage")
		{
			private static final long serialVersionUID = 1L;

			/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/Index.java`
#### Snippet
```java
	 * 
	 */
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatelessPage3.java`
#### Snippet
```java
public class StatelessPage3 extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	private Integer number;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/stateless/StatefulPage.java`
#### Snippet
```java
public class StatefulPage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/** click count for Link. */
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/WebSocketBehaviorDemoPage.java`
#### Snippet
```java
public class WebSocketBehaviorDemoPage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;
	private final WebMarkupContainer downloadingContainer = new WebMarkupContainer("downloading");

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/WebSocketBehaviorDemoPage.java`
#### Snippet
```java
		chartPanel.add(new WebSocketBehavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/WebSocketBehaviorDemoPage.java`
#### Snippet
```java
		ResourceStreamResource resource = new ResourceStreamResource()
		{
			private static final long serialVersionUID = 1L;
			private int i = 42;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/WebSocketBehaviorDemoPage.java`
#### Snippet
```java
		add(new WebSocketBehavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/WebSocketBehaviorDemoPage.java`
#### Snippet
```java
		final AjaxDownloadBehavior download = new AjaxDownloadBehavior(resource)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/linkomatic/Home.java`
#### Snippet
```java
		add(new DownloadLink("downloadLink", new IModel<File>()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/kittenCaptcha/HomePage.java`
#### Snippet
```java
public class HomePage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	private final KittenCaptchaPanel captcha;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/MapperDemoResourceReference.java`
#### Snippet
```java
		return new AbstractResource()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/MapperDemoResourceReference.java`
#### Snippet
```java
{

	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/resourcedecoration/HomePage.java`
#### Snippet
```java
	private static class AjaxProofContainer extends WebMarkupContainer
	{
		private static final long serialVersionUID = 1L;

		private AjaxProofContainer(String id)
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/resourcedecoration/HomePage.java`
#### Snippet
```java
		add(new AbstractAjaxTimerBehavior(Duration.ofSeconds(4))
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/resourcedecoration/HomePage.java`
#### Snippet
```java
		jsPlaceholder.add(new Behavior()
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-examples/src/main/java/org/apache/wicket/examples/resourcedecoration/HomePage.java`
#### Snippet
```java
public class HomePage extends WicketExamplePage
{
	private static final long serialVersionUID = 1L;

	/**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketConnectionFilterCollection.java`
#### Snippet
```java
		implements IWebSocketConnectionFilter
{
	private static final long serialVersionUID = 1L;

	@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WicketWebSocketJQueryResourceReference.java`
#### Snippet
```java
public class WicketWebSocketJQueryResourceReference extends JavaScriptResourceReference
{
	private static final long serialVersionUID = 1;

	private static final WicketWebSocketJQueryResourceReference instance = new WicketWebSocketJQueryResourceReference();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/AutoConversation.java`
#### Snippet
```java
public class AutoConversation implements IClusterable
{
	private static final long serialVersionUID = 1L;

	private boolean automatic;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/DetachEventEmitter.java`
#### Snippet
```java
	private static final MetaDataKey<Boolean> DETACH_SCHEDULED_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationExpiredException.java`
#### Snippet
```java
public class ConversationExpiredException extends RuntimeException
{
	private static final long serialVersionUID = 1L;
	private String cid;
	private Page page;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationPropagator.java`
#### Snippet
```java
	private static final MetaDataKey<String> CONVERSATION_ID_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationPropagator.java`
#### Snippet
```java
	private static final MetaDataKey<Boolean> CONVERSATION_STARTED_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/CdiConfiguration.java`
#### Snippet
```java
	private static final MetaDataKey<CdiConfiguration> CDI_CONFIGURATION_KEY = new MetaDataKey<>()
	{
		private static final long serialVersionUID = 1L;
	};

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/VelocityJavaScriptContributor.java`
#### Snippet
```java
public class VelocityJavaScriptContributor extends VelocityContributor
{
	private static final long serialVersionUID = 1L;

	private final String id;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/VelocityContributor.java`
#### Snippet
```java
public class VelocityContributor extends Behavior
{
	private static final long serialVersionUID = 1L;

	private String encoding = "ISO-8859-1";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/markup/html/VelocityPanel.java`
#### Snippet
```java
		return new VelocityPanel(id, model)
		{
			private static final long serialVersionUID = 1L;

			@Override
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/markup/html/VelocityPanel.java`
#### Snippet
```java
		IMarkupCacheKeyProvider
{
	private static final long serialVersionUID = 1L;

	/**
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'setVersion' is still used
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieDefaults.java`
#### Snippet
```java
		forRemoval = true
	)
	public void setVersion(int version)
	{
		this.version = version;
```

## RuleId[id=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/InspectorPage.java`
#### Snippet
```java
		
		IModel<Page> page = () -> {
			return reference.getPage();
		};
		
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
			getRequestCycle().find(AjaxRequestTarget.class).ifPresent(target -> {
				// the timer will not be rendered, so restart it immediately on the Ajax target
				timer.restart(target);
			});
		}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
				super.init();

				setSessionStoreProvider(() -> new NeverSessionStore());
				getFrameworkSettings().setSerializer(new NeverSerializer());
			}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/logger/ListenerLogData.java`
#### Snippet
```java

		componentClass = optional(() -> pageAndComponentProvider.getComponent().getClass());
		componentPath = optional(() -> pageAndComponentProvider.getComponentPath());

		if (behaviorIndex != null)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `wicket-core/src/main/java/org/apache/wicket/mock/MockApplication.java`
#### Snippet
```java
		// set page and session store providers
		setSessionStoreProvider(MockSessionStore::new);
		setPageManagerProvider(() -> new MockPageManager());

		// for test cases we usually want stable resource names
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `wicket-core/src/main/java/org/apache/wicket/pageStore/GroupingPageStore.java`
#### Snippet
```java
	{
		return context.getSessionData(KEY, create ? () -> {
			return new SessionData();
		} : null);
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
	{
		SessionData data = context.getSessionData(getKey(), create ? () -> {
			return dataCreator.get();
		} : null);

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `wicket-examples/src/main/java/org/apache/wicket/examples/datetime/DateTimePage.java`
#### Snippet
```java
				public List<ZoneId> getObject()
				{
					return ZoneId.getAvailableZoneIds().stream().map(id -> ZoneId.of(id))
						.collect(Collectors.toList());
				}
```

## RuleId[id=ComparatorCombinators]
### ComparatorCombinators
Can be replaced with 'Comparator.comparing'
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/ContactDaoImpl.java`
#### Snippet
```java
	private void updateIndecies()
	{
		Collections.sort(fnameIdx, (arg0, arg1) -> (arg0).getFirstName().compareTo((arg1).getFirstName()));

		Collections.sort(lnameIdx, (arg0, arg1) -> (arg0).getLastName().compareTo((arg1).getLastName()));
```

### ComparatorCombinators
Can be replaced with 'Comparator.comparing'
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/ContactDaoImpl.java`
#### Snippet
```java
		Collections.sort(fnameIdx, (arg0, arg1) -> (arg0).getFirstName().compareTo((arg1).getFirstName()));

		Collections.sort(lnameIdx, (arg0, arg1) -> (arg0).getLastName().compareTo((arg1).getLastName()));

		Collections.sort(fnameDescIdx, (arg0, arg1) -> (arg1).getFirstName().compareTo((arg0).getFirstName()));
```

### ComparatorCombinators
Can be replaced with 'Comparator.comparing'
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactsDatabase.java`
#### Snippet
```java
	private void updateIndecies()
	{
		Collections.sort(fnameIdx, (arg0, arg1) -> (arg0).getFirstName().compareTo((arg1).getFirstName()));

		Collections.sort(lnameIdx, (arg0, arg1) -> (arg0).getLastName().compareTo((arg1).getLastName()));
```

### ComparatorCombinators
Can be replaced with 'Comparator.comparing'
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactsDatabase.java`
#### Snippet
```java
		Collections.sort(fnameIdx, (arg0, arg1) -> (arg0).getFirstName().compareTo((arg1).getFirstName()));

		Collections.sort(lnameIdx, (arg0, arg1) -> (arg0).getLastName().compareTo((arg1).getLastName()));

		Collections.sort(fnameDescIdx, (arg0, arg1) -> (arg1).getFirstName().compareTo((arg0).getFirstName()));
```

## RuleId[id=PatternVariableCanBeUsed]
### PatternVariableCanBeUsed
Variable 'jarUrlConnection' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/io/Connections.java`
#### Snippet
```java
			if (connection instanceof JarURLConnection)
			{
				JarURLConnection jarUrlConnection = (JarURLConnection)connection;
				URL jarFileUrl = jarUrlConnection.getJarFileURL();
				URLConnection jarFileConnection = jarFileUrl.openConnection();
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceProxyTargetLocator.java`
#### Snippet
```java
		if (!(o instanceof GuiceProxyTargetLocator))
			return false;
		GuiceProxyTargetLocator that = (GuiceProxyTargetLocator) o;
		return Objects.equal(optional, that.optional) &&
				Objects.equal(bindingAnnotation, that.bindingAnnotation) &&
```

### PatternVariableCanBeUsed
Variable 'bom' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteOrderMark.java`
#### Snippet
```java
            return false;
        }
        final ByteOrderMark bom = (ByteOrderMark)obj;
        if (bytes.length != bom.length()) {
            return false;
```

### PatternVariableCanBeUsed
Variable 'key' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/xml/CustomEntityResolver.java`
#### Snippet
```java
			}

			EntityKey key = (EntityKey)o;

			if (!id.equals(key.id))
```

### PatternVariableCanBeUsed
Variable 'stringValue' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java
		if (obj instanceof StringValue)
		{
			StringValue stringValue = (StringValue)obj;
			return Objects.isEqual(text, stringValue.text) && locale.equals(stringValue.locale);
		}
```

### PatternVariableCanBeUsed
Variable 'asb' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (obj instanceof AppendingStringBuffer)
		{
			AppendingStringBuffer asb = (AppendingStringBuffer)obj;
			return insert(offset, asb.value, 0, asb.count);
		}
```

### PatternVariableCanBeUsed
Variable 'anotherString' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		if (anObject instanceof AppendingStringBuffer)
		{
			AppendingStringBuffer anotherString = (AppendingStringBuffer)anObject;
			int n = count;
			if (n == anotherString.count)
```

### PatternVariableCanBeUsed
Variable 'sequence' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
		else if (anObject instanceof CharSequence)
		{
			CharSequence sequence = (CharSequence)anObject;
			int n = count;
			if (sequence.length() == count)
```

### PatternVariableCanBeUsed
Variable 'container' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/visit/Visits.java`
#### Snippet
```java
		if (component instanceof Iterable<?>)
		{
			final Iterable<?> container = (Iterable<?>)component;
			if (filter.visitChildren(container))
			{
```

### PatternVariableCanBeUsed
Variable 'asb' can be replaced with pattern variable
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
		if (s instanceof AppendingStringBuffer)
		{
			AppendingStringBuffer asb = (AppendingStringBuffer)s;
			buffer.append(asb.getValue(), from, to - from);
		}
```

### PatternVariableCanBeUsed
Variable 'other' can be replaced with pattern variable
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
		if (obj instanceof SpringBeanLocator)
		{
			SpringBeanLocator other = (SpringBeanLocator)obj;
			return beanTypeName.equals(other.beanTypeName) &&
				Objects.equal(beanName, other.beanName);
```

### PatternVariableCanBeUsed
Variable 'container' can be replaced with pattern variable
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java
			if (component instanceof MarkupContainer)
			{
				MarkupContainer container = ((MarkupContainer)component);
				// Traverse children
				final Object o = container.visitChildren(visitor);
```

### PatternVariableCanBeUsed
Variable 'p' can be replaced with pattern variable
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java
			if (component instanceof Page)
			{
				final Page p = (Page)component;
				if (!p.isBookmarkable())
				{
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-request/src/main/java/org/apache/wicket/request/HttpHeaderCollection.java`
#### Snippet
```java
				return false;

			HeaderKey that = (HeaderKey)o;

			if (!key.equals(that.key))
```

### PatternVariableCanBeUsed
Variable 'renderer' can be replaced with pattern variable
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
		if (url instanceof IUrlRenderer)
		{
			IUrlRenderer renderer = (IUrlRenderer)url;
			return renderer.renderFullUrl(url, getBaseUrl());
		}
```

### PatternVariableCanBeUsed
Variable 'renderer' can be replaced with pattern variable
in `wicket-request/src/main/java/org/apache/wicket/request/UrlRenderer.java`
#### Snippet
```java
		if (url instanceof IUrlRenderer)
		{
			IUrlRenderer renderer = (IUrlRenderer)url;
			return renderer.renderRelativeUrl(url, getBaseUrl());
		}
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/CompoundRequestMapper.java`
#### Snippet
```java
				return false;

			MapperWithScore that = (MapperWithScore)o;

			if (compatibilityScore != that.compatibilityScore)
```

### PatternVariableCanBeUsed
Variable 'container' can be replaced with pattern variable
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java
				if (node instanceof MarkupContainer)
				{
					MarkupContainer container = (MarkupContainer)node;
					for (Component child : container)
					{
```

### PatternVariableCanBeUsed
Variable 'filteredCol' can be replaced with pattern variable
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/FilterToolbar.java`
#### Snippet
```java
				if (col instanceof IFilteredColumn)
				{
					IFilteredColumn<T, S> filteredCol = (IFilteredColumn<T, S>)col;
					filter = filteredCol.getFilter(FILTER_ID, form);
				}
```

### PatternVariableCanBeUsed
Variable 'nodeModel' can be replaced with pattern variable
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/table/NodeModel.java`
#### Snippet
```java
		if (obj instanceof NodeModel<?>)
		{
			NodeModel<?> nodeModel = (NodeModel<?>)obj;

			return Arrays.equals(this.branches, nodeModel.branches) &&
```

### PatternVariableCanBeUsed
Variable 'httpServletRequest' can be replaced with pattern variable
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/aspects/request/WicketFilterRequestCycleUrlAspect.java`
#### Snippet
```java
			if (requestAsObject != null && requestAsObject instanceof HttpServletRequest)
			{
				HttpServletRequest httpServletRequest = (HttpServletRequest)requestAsObject;
				String requestUrl = httpServletRequest.getRequestURL().toString();
				String replacedUrl = requestUrl.replace('/', '_');
```

### PatternVariableCanBeUsed
Variable 'pageRequestHandler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/DefaultExceptionMapper.java`
#### Snippet
```java
		if (handler instanceof IPageRequestHandler)
		{
			IPageRequestHandler pageRequestHandler = (IPageRequestHandler)handler;
			return (Page)pageRequestHandler.getPage();
		}
```

### PatternVariableCanBeUsed
Variable 'behavior' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
			if (obj instanceof Behavior)
			{
				final Behavior behavior = (Behavior)obj;

				behavior.onRemove(component);
```

### PatternVariableCanBeUsed
Variable 'behavior' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
			if (obj instanceof Behavior)
			{
				final Behavior behavior = (Behavior)obj;

				behavior.detach(component);
```

### PatternVariableCanBeUsed
Variable 'parameters' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/RestartResponseAtInterceptPageException.java`
#### Snippet
```java
					request.getPostParameters() instanceof IWritableRequestParameters)
				{
					IWritableRequestParameters parameters = (IWritableRequestParameters)request.getPostParameters();
					parameters.reset();
					for (String s : data.postParameters.keySet())
```

### PatternVariableCanBeUsed
Variable 'resourceReferenceRequestHandler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/UrlResourceReferenceMapper.java`
#### Snippet
```java
		if (requestHandler instanceof ResourceReferenceRequestHandler)
		{
			ResourceReferenceRequestHandler resourceReferenceRequestHandler = (ResourceReferenceRequestHandler) requestHandler;
			ResourceReference resourceReference = resourceReferenceRequestHandler.getResourceReference();

```

### PatternVariableCanBeUsed
Variable 'urlResourceReference' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/UrlResourceReferenceMapper.java`
#### Snippet
```java
			if (resourceReference instanceof UrlResourceReference)
			{
				UrlResourceReference urlResourceReference = (UrlResourceReference) resourceReference;
				url = urlResourceReference.getUrl();
			}
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/DequeueContext.java`
#### Snippet
```java
			if (element instanceof ComponentTag)
			{
				ComponentTag tag = (ComponentTag)element;

				if (tag.isOpen() || tag.isOpenClose())
```

### PatternVariableCanBeUsed
Variable 'webResponse' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/csp/CSPRequestCycleListener.java`
#### Snippet
```java
		}

		WebResponse webResponse = (WebResponse)cycle.getResponse();
		if (!webResponse.isHeaderSupported())
		{
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java
		if (obj instanceof AjaxRequestHandler)
		{
			AjaxRequestHandler that = (AjaxRequestHandler)obj;
			return update.equals(that.update);
		}
```

### PatternVariableCanBeUsed
Variable 'values' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/ajax/json/JsonUtils.java`
#### Snippet
```java
				Object value = entry.getValue();
				if (value instanceof List) {
					List<?> values = (List<?>) value;
					for (Object v : values)
					{
```

### PatternVariableCanBeUsed
Variable 'contributor' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
			if (ajaxCallListener instanceof IComponentAwareHeaderContributor)
			{
				IComponentAwareHeaderContributor contributor = (IComponentAwareHeaderContributor)ajaxCallListener;
				contributor.renderHead(component, response);
			}
```

### PatternVariableCanBeUsed
Variable 'request' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/coep/CrossOriginEmbedderPolicyRequestCycleListener.java`
#### Snippet
```java
		if (containerRequest instanceof HttpServletRequest)
		{
			HttpServletRequest request = (HttpServletRequest) containerRequest;
			String path = request.getContextPath();
			final String coepHeaderName = coepConfig.getCoepHeader();
```

### PatternVariableCanBeUsed
Variable 'webResponse' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/coep/CrossOriginEmbedderPolicyRequestCycleListener.java`
#### Snippet
```java
			if (cycle.getResponse() instanceof WebResponse)
			{
				WebResponse webResponse = (WebResponse) cycle.getResponse();
				if (webResponse.isHeaderSupported())
				{
```

### PatternVariableCanBeUsed
Variable 'request' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/coop/CrossOriginOpenerPolicyRequestCycleListener.java`
#### Snippet
```java
		if (containerRequest instanceof HttpServletRequest)
		{
			HttpServletRequest request = (HttpServletRequest) containerRequest;
			String path = request.getContextPath();

```

### PatternVariableCanBeUsed
Variable 'webResponse' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/coop/CrossOriginOpenerPolicyRequestCycleListener.java`
#### Snippet
```java
			if (cycle.getResponse() instanceof WebResponse)
			{
				WebResponse webResponse = (WebResponse) cycle.getResponse();
				if (webResponse.isHeaderSupported())
				{
```

### PatternVariableCanBeUsed
Variable 'component' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/OrphanComponentChecker.java`
#### Snippet
```java
		if (object instanceof Component)
		{
			Component component = (Component) object;
			if (component instanceof Page == false && component.getParent() == null)
			{
```

### PatternVariableCanBeUsed
Variable 'model' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/NotDetachedModelChecker.java`
#### Snippet
```java
		if (obj instanceof LoadableDetachableModel<?>)
		{
			LoadableDetachableModel<?> model = (LoadableDetachableModel<?>) obj;
			if (model.isAttached())
			{
```

### PatternVariableCanBeUsed
Variable 'webApplication' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/ComponentRenderer.java`
#### Snippet
```java
	{
		if (application instanceof WebApplication) {
			WebApplication webApplication = (WebApplication)application;
			
			// WebApplication requires a servlet context
```

### PatternVariableCanBeUsed
Variable 'fileResourceStream' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/caching/CachingResourceStreamLocator.java`
#### Snippet
```java
		else if (stream instanceof FileResourceStream)
		{
			FileResourceStream fileResourceStream = (FileResourceStream)stream;
			cache.put(key, new FileResourceStreamReference(fileResourceStream));
		}
```

### PatternVariableCanBeUsed
Variable 'urlResourceStream' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/caching/CachingResourceStreamLocator.java`
#### Snippet
```java
		else if (stream instanceof UrlResourceStream)
		{
			UrlResourceStream urlResourceStream = (UrlResourceStream)stream;
			cache.put(key, new UrlResourceStreamReference(urlResourceStream));
		}
```

### PatternVariableCanBeUsed
Variable 'extObj' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
		else if (obj instanceof Externalizable && (!Proxy.isProxyClass(cls)))
		{
			Externalizable extObj = (Externalizable)obj;
			try
			{
```

### PatternVariableCanBeUsed
Variable 'component' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
			if (slot.object instanceof Component)
			{
				Component component = (Component)slot.object;
				result.append(", path=").append(component.getPath());
			}
```

### PatternVariableCanBeUsed
Variable 'cacheable' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
			if (resource instanceof IStaticCacheableResource)
			{
				final IStaticCacheableResource cacheable = (IStaticCacheableResource)resource;
				
				if(cacheable.isCachingEnabled())
```

### PatternVariableCanBeUsed
Variable 'referenceRequestHandler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java
		if (requestHandler instanceof ResourceReferenceRequestHandler)
		{
			ResourceReferenceRequestHandler referenceRequestHandler = (ResourceReferenceRequestHandler)requestHandler;
			ResourceReference reference = referenceRequestHandler.getResourceReference();

```

### PatternVariableCanBeUsed
Variable 'cacheable' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BasicResourceReferenceMapper.java`
#### Snippet
```java
					if (resource instanceof IStaticCacheableResource)
					{
						final IStaticCacheableResource cacheable = (IStaticCacheableResource)resource;
						
						// is caching enabled?
```

### PatternVariableCanBeUsed
Variable 'handler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java
		else if (requestHandler instanceof ListenerRequestHandler)
		{
			ListenerRequestHandler handler = (ListenerRequestHandler)requestHandler;
			IRequestablePage page = handler.getPage();
			String componentPath = handler.getComponentPath();
```

### PatternVariableCanBeUsed
Variable 'wicketTag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		if (tag instanceof WicketTag)
		{
			WicketTag wicketTag = (WicketTag)tag;
			if (wicketTag.isContainerTag())
			{
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		{
			// Get element as tag
			final ComponentTag tag = (ComponentTag)element;

			if (tag instanceof WicketTag && ((WicketTag)tag).isFragmentTag()){
```

### PatternVariableCanBeUsed
Variable 'pageHandler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/IPageRequestHandler.java`
#### Snippet
```java
		if (handler instanceof IPageRequestHandler)
		{
			IPageRequestHandler pageHandler = (IPageRequestHandler) handler;
			if (pageHandler.isPageInstanceCreated())
			{
```

### PatternVariableCanBeUsed
Variable 'handler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MountedMapper.java`
#### Snippet
```java
			getRecreateMountedPagesAfterExpiry())
		{
			ListenerRequestHandler handler = (ListenerRequestHandler)requestHandler;
			IRequestablePage page = handler.getPage();
			if (checkPageInstance(page))
```

### PatternVariableCanBeUsed
Variable 'response' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/ComponentRenderingRequestHandler.java`
#### Snippet
```java
		if (requestCycle.getResponse() instanceof WebResponse)
		{
			WebResponse response = (WebResponse)requestCycle.getResponse();
			response.disableCaching();
		}
```

### PatternVariableCanBeUsed
Variable 'handler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
		{
			// simple bookmarkable URL with no page instance information
			BookmarkablePageRequestHandler handler = (BookmarkablePageRequestHandler)requestHandler;

			if (!checkPageClass(handler.getPageClass()))
```

### PatternVariableCanBeUsed
Variable 'handler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
			// but only allowed if the page was created by bookmarkable URL

			RenderPageRequestHandler handler = (RenderPageRequestHandler)requestHandler;

			if (!checkPageClass(handler.getPageClass()))
```

### PatternVariableCanBeUsed
Variable 'handler' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
		{
			// request listener URL with page class information
			BookmarkableListenerRequestHandler handler = (BookmarkableListenerRequestHandler)requestHandler;
			Class<? extends IRequestablePage> pageClass = handler.getPageClass();

```

### PatternVariableCanBeUsed
Variable 'container' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			if (this instanceof MarkupContainer)
			{
				MarkupContainer container = (MarkupContainer)this;
				Markup associatedMarkup = container.getAssociatedMarkup();
				if (associatedMarkup != null)
```

### PatternVariableCanBeUsed
Variable 'array' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		else if (data instanceof Object[] && !(data instanceof MetaDataEntry<?>[]))
		{
			Object[] array = (Object[])data;
			return index < array.length ? array[index] : null;
		}
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/model/Model.java`
#### Snippet
```java
			return false;
		}
		Model<?> that = (Model<?>)obj;
		return Objects.equal(object, that.object);
	}
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/model/util/GenericBaseModel.java`
#### Snippet
```java
			return false;
		}
		GenericBaseModel<?> that = (GenericBaseModel<?>)obj;
		return Objects.equal(object, that.object);
	}
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/HtmlSpecialTag.java`
#### Snippet
```java
		if (element instanceof HtmlSpecialTag)
		{
			final HtmlSpecialTag that = (HtmlSpecialTag)element;
			return getXmlTag().equalTo(that.getXmlTag());
		}
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFragment.java`
#### Snippet
```java
				if (elem instanceof ComponentTag)
				{
					ComponentTag tag = (ComponentTag)elem;
					if (tag.closes(startTag))
					{
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
		if (element instanceof ComponentTag)
		{
			final ComponentTag that = (ComponentTag)element;
			return getXmlTag().equalTo(that.getXmlTag());
		}
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
			if (element instanceof WicketTag)
			{
				WicketTag wtag = (WicketTag)element;

				// Found wicket:child in the base markup. In case of 3+
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
			if (element instanceof WicketTag)
			{
				WicketTag wtag = (WicketTag)element;
				if (wtag.isExtendTag() && wtag.isClose())
				{
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
				if (element instanceof WicketTag)
				{
					WicketTag tag = (WicketTag)element;
					if (tag.isChildTag() && tag.isClose())
					{
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
			if (element instanceof ComponentTag && resource != null)
			{
				ComponentTag tag = (ComponentTag)element;
				if (tag.getMarkupClass() == null){
					tag.setMarkupClass(markupClass);
```

### PatternVariableCanBeUsed
Variable 'etag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
			if (elem instanceof WicketTag)
			{
				WicketTag etag = (WicketTag)elem;
				if (etag.isHeadTag())
				{
```

### PatternVariableCanBeUsed
Variable 'panelTag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
		if (elem instanceof ComponentTag)
		{
			ComponentTag panelTag = (ComponentTag)elem;
			for (String key : panelTag.getAttributes().keySet())
			{
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
		if (elem instanceof WicketTag)
		{
			WicketTag wtag = (WicketTag)elem;
			if (wtag.isHeadTag())
			{
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
		if (elem instanceof WicketTag)
		{
			WicketTag wtag = (WicketTag)elem;
			return wtag.isExtendTag();
		}
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
						if (tag instanceof WicketTag)
						{
							WicketTag wtag = (WicketTag)tag;

							if (wtag.isExtendTag())
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
		if (elem instanceof WicketTag)
		{
			WicketTag wtag = (WicketTag)elem;
			if (wtag.isHeaderItemsTag())
			{
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
		if (elem instanceof ComponentTag)
		{
			ComponentTag tag = (ComponentTag)elem;
			if ("head".equalsIgnoreCase(tag.getName()) && (tag.getNamespace() == null))
			{
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
		if (elem instanceof WicketTag)
		{
			WicketTag wtag = (WicketTag)elem;
			if (wtag.isBorderTag())
			{
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/TagUtils.java`
#### Snippet
```java
		if (elem instanceof WicketTag)
		{
			WicketTag wtag = (WicketTag)elem;
			if (wtag.isBodyTag())
			{
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java
				if (elem instanceof ComponentTag)
				{
					ComponentTag tag = (ComponentTag)elem;

					boolean add = (tag.getId() != null);
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
			if (elem instanceof ComponentTag)
			{
				ComponentTag tag = (ComponentTag)elem;
				if (tag.isOpen() || tag.isOpenClose())
				{
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
			else if ((current instanceof ComponentTag) && !(current instanceof WicketTag))
			{
				ComponentTag tag = (ComponentTag)current;
				if (tag.isAutoComponentTag())
				{
```

### PatternVariableCanBeUsed
Variable 'originalHeaderItem' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/head/ResourceAggregator.java`
#### Snippet
```java
		if (item instanceof CssReferenceHeaderItem && bundle instanceof CssReferenceHeaderItem)
		{
			CssReferenceHeaderItem originalHeaderItem = (CssReferenceHeaderItem) item;
			resultBundle = preserveCssDetails(originalHeaderItem, (CssReferenceHeaderItem) bundle);
		}
```

### PatternVariableCanBeUsed
Variable 'originalHeaderItem' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/head/ResourceAggregator.java`
#### Snippet
```java
		else if (item instanceof JavaScriptReferenceHeaderItem && bundle instanceof JavaScriptReferenceHeaderItem)
		{
			JavaScriptReferenceHeaderItem originalHeaderItem = (JavaScriptReferenceHeaderItem) item;
			resultBundle = preserveJavaScriptDetails(originalHeaderItem, (JavaScriptReferenceHeaderItem) bundle);
		}
```

### PatternVariableCanBeUsed
Variable 'link' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
			}

			AjaxSubmitLink link = (AjaxSubmitLink)linkComponent;

			String pageRelativePath = link.getInputName();
```

### PatternVariableCanBeUsed
Variable 'submitLink' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		else if (linkComponent instanceof SubmitLink)
		{
			SubmitLink submitLink = (SubmitLink)linkComponent;

			String pageRelativePath = submitLink.getInputName();
```

### PatternVariableCanBeUsed
Variable 'externalLink' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		else if (linkComponent instanceof ExternalLink)
		{
			ExternalLink externalLink = (ExternalLink)linkComponent;
			String href = externalLink.getDefaultModelObjectAsString();
			try
```

### PatternVariableCanBeUsed
Variable 'link' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		else if (linkComponent instanceof AbstractLink)
		{
			AbstractLink link = (AbstractLink)linkComponent;

			/*
```

### PatternVariableCanBeUsed
Variable 'bookmarkablePageLink' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
			if (link instanceof BookmarkablePageLink)
			{
				BookmarkablePageLink<?> bookmarkablePageLink = (BookmarkablePageLink<?>)link;
				try
				{
```

### PatternVariableCanBeUsed
Variable 'formSubmitBehavior' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		if (behavior instanceof AjaxFormSubmitBehavior)
		{
			AjaxFormSubmitBehavior formSubmitBehavior = (AjaxFormSubmitBehavior)behavior;
			Form<?> form = formSubmitBehavior.getForm();
			getRequest().setUseMultiPartContentType(form.isMultiPart());
```

### PatternVariableCanBeUsed
Variable 'webRequest' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java
		if (request instanceof WebRequest)
		{
			WebRequest webRequest = (WebRequest)request;
			return webRequest.isAjax();
		}
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java
			{
				// Get element as tag
				final ComponentTag tag = (ComponentTag)childOpenTag;

				// Get component id
```

### PatternVariableCanBeUsed
Variable 'submitLink' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/StatelessForm.java`
#### Snippet
```java
			if (submittingComponent instanceof AbstractSubmitLink)
			{
				AbstractSubmitLink submitLink = (AbstractSubmitLink)submittingComponent;
				parameters.remove(submitLink.getInputName());
			}
```

### PatternVariableCanBeUsed
Variable 'formComponent' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelTextResolver.java`
#### Snippet
```java
			if (labeled instanceof FormComponent)
			{
				final FormComponent<?> formComponent = (FormComponent<?>)labeled;
				String text = formComponent.getDefaultLabel("wicket:unknown");
				if (!"wicket:unknown".equals(text) && !Strings.isEmpty(text))
```

### PatternVariableCanBeUsed
Variable 'fc' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelTextResolver.java`
#### Snippet
```java
				if (labeled instanceof FormComponent)
				{
					FormComponent<?> fc = (FormComponent<?>)labeled;
					fc.setLabel(labelModel);
				}
```

### PatternVariableCanBeUsed
Variable 'fc' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
			if (component instanceof FormComponent)
			{
				FormComponent<?> fc = (FormComponent<?>)component;
				if (fc.isRequired())
				{
```

### PatternVariableCanBeUsed
Variable 'fc' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentLabel.java`
#### Snippet
```java
		if (formComponent instanceof FormComponent<?>)
		{
			FormComponent<?> fc = (FormComponent<?>) formComponent;

			if (fc.isRequired())
```

### PatternVariableCanBeUsed
Variable 'submittingComponent' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
		if (defaultSubmittingComponent instanceof Component)
		{
			final Component submittingComponent = (Component) defaultSubmittingComponent;
			return submittingComponent.isVisibleInHierarchy()
				&& submittingComponent.isEnabledInHierarchy();
```

### PatternVariableCanBeUsed
Variable 'submittingComponent' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
				if (submitter instanceof IFormSubmittingComponent)
				{
					IFormSubmittingComponent submittingComponent = (IFormSubmittingComponent)submitter;
					Component component = (Component)submitter;

```

### PatternVariableCanBeUsed
Variable 'form' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
							if (component instanceof Form<?>)
							{
								Form<?> form = (Form<?>)component;
								if (form.isVisibleInHierarchy() && form.isEnabledInHierarchy())
								{
```

### PatternVariableCanBeUsed
Variable 'fc' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
							else if (component instanceof FormComponent<?>)
							{
								FormComponent<?> fc = (FormComponent<?>)component;
								if (fc.isVisibleInHierarchy() && fc.isEnabledInHierarchy())
								{
```

### PatternVariableCanBeUsed
Variable 'wTag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AssociatedMarkupSourcingStrategy.java`
#### Snippet
```java
		if (element instanceof WicketTag)
		{
			final WicketTag wTag = (WicketTag)element;
			if ((wTag.isHeadTag()) && (wTag.getNamespace() != null))
			{
```

### PatternVariableCanBeUsed
Variable 'tag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AssociatedMarkupSourcingStrategy.java`
#### Snippet
```java
			if (elem instanceof WicketTag)
			{
				WicketTag tag = (WicketTag)elem;
				if (tag.isOpen() && tag.isHeadTag())
				{
```

### PatternVariableCanBeUsed
Variable 'wTag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
			if (elem instanceof WicketTag)
			{
				WicketTag wTag = (WicketTag)elem;
				if (wTag.isBorderTag() && wTag.isClose())
				{
```

### PatternVariableCanBeUsed
Variable 'wTag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
			if (elem instanceof WicketTag)
			{
				WicketTag wTag = (WicketTag)elem;
				if (!insideBorderMarkup)
				{
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java
				if (tag instanceof WicketTag)
				{
					WicketTag wtag = (WicketTag)tag;
					if (wtag.isHeadTag() || wtag.isHeaderItemsTag())
					{
```

### PatternVariableCanBeUsed
Variable 'componentTag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RelativePathPrefixHandler.java`
#### Snippet
```java
			if (next instanceof ComponentTag)
			{
				ComponentTag componentTag = (ComponentTag)next;

				/**
```

### PatternVariableCanBeUsed
Variable 'open' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/StyleAndScriptIdentifier.java`
#### Snippet
```java
			if (elem instanceof ComponentTag)
			{
				ComponentTag open = (ComponentTag)elem;

				if (shouldProcess(open))
```

### PatternVariableCanBeUsed
Variable 'close' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/StyleAndScriptIdentifier.java`
#### Snippet
```java
						if ((body instanceof RawMarkup) && (tag2 instanceof ComponentTag))
						{
							ComponentTag close = (ComponentTag)tag2;
							if (close.closes(open))
							{
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketLinkTagHandler.java`
#### Snippet
```java
		if (tag instanceof WicketTag)
		{
			final WicketTag wtag = (WicketTag)tag;
			if (wtag.isLinkTag())
			{
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketLinkTagHandler.java`
#### Snippet
```java
		if (tag instanceof WicketTag)
		{
			WicketTag wtag = (WicketTag)tag;
			if (wtag.isLinkTag() && (wtag.getNamespace() != null))
			{
```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/WicketContainerTagHandler.java`
#### Snippet
```java
			if (tag instanceof WicketTag)
			{
				WicketTag wtag = (WicketTag)tag;
				if (tag.isOpen() && wtag.isContainerTag())
				{
```

### PatternVariableCanBeUsed
Variable 'container' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
				{
					dequeue.popContainer(); // pop the repeater
					MarkupContainer container = (MarkupContainer) child;
					dequeue.pushContainer(container);
					container.dequeue(dequeue);
```

### PatternVariableCanBeUsed
Variable 'resolver' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/ComponentResolvers.java`
#### Snippet
```java
			if (cursor instanceof IComponentResolver)
			{
				IComponentResolver resolver = (IComponentResolver)cursor;
				Component component = resolver.resolve(container, markupStream, tag);
				if (component != null)
```

### PatternVariableCanBeUsed
Variable 'result' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
			if (component instanceof WicketHeadContainer)
			{
				WicketHeadContainer result = (WicketHeadContainer) component;
				visit.stop(result);
			}
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/WebExternalResourceRequestHandler.java`
#### Snippet
```java
		if (obj instanceof WebExternalResourceRequestHandler)
		{
			WebExternalResourceRequestHandler that = (WebExternalResourceRequestHandler)obj;
			return uri.equals(that.uri);
		}
```

### PatternVariableCanBeUsed
Variable 'currentTag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java
					if ((element instanceof ComponentTag) && !markupStream.atCloseTag())
					{
						ComponentTag currentTag = (ComponentTag)element;
						String id = currentTag.getId();

```

### PatternVariableCanBeUsed
Variable 'wtag' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/WicketMessageResolver.java`
#### Snippet
```java
		if (tag instanceof WicketTag)
		{
			WicketTag wtag = (WicketTag)tag;
			if (wtag.isMessageTag())
			{
```

### PatternVariableCanBeUsed
Variable 'webRequest' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/handler/render/WebPageRenderer.java`
#### Snippet
```java
		if (request instanceof WebRequest)
		{
			WebRequest webRequest = (WebRequest)request;
			isAjax = webRequest.isAjax();
		}
```

### PatternVariableCanBeUsed
Variable 'rrrh' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
			if (handler instanceof ResourceReferenceRequestHandler)
			{
				ResourceReferenceRequestHandler rrrh = (ResourceReferenceRequestHandler)handler;
				IResource resource = rrrh.getResource();
				if (resource != null && !(resource instanceof IStaticCacheableResource) ||
```

### PatternVariableCanBeUsed
Variable 'rrh' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
			else if (handler instanceof ResourceRequestHandler)
			{
				ResourceRequestHandler rrh = (ResourceRequestHandler)handler;
				IResource resource = rrh.getResource();
				if (resource != null && !(resource instanceof IStaticCacheableResource) ||
```

### PatternVariableCanBeUsed
Variable 'webRequest' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		if (request instanceof WebRequest)
		{
			WebRequest webRequest = (WebRequest)request;

			setRequestRangeMetaData(webRequest);
```

### PatternVariableCanBeUsed
Variable 'cacheable' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		if (this instanceof IStaticCacheableResource)
		{
			final IStaticCacheableResource cacheable = (IStaticCacheableResource)this;

			// is caching enabled?
```

### PatternVariableCanBeUsed
Variable 'webResponse' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		{
			Duration duration = data.getCacheDuration();
			WebResponse webResponse = (WebResponse)response;
			if (duration.compareTo(Duration.ZERO) > 0)
			{
```

### PatternVariableCanBeUsed
Variable 'webResponse' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		if (response instanceof WebResponse)
		{
			WebResponse webResponse = (WebResponse)response;

			// 1. Last Modified
```

### PatternVariableCanBeUsed
Variable 'scopeAwareProcessor' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
				if (compressor instanceof IScopeAwareTextResourceProcessor)
				{
					IScopeAwareTextResourceProcessor scopeAwareProcessor = (IScopeAwareTextResourceProcessor)compressor;
					output = scopeAwareProcessor.process(nonCompressed, getScope(), name);
				}
```

### PatternVariableCanBeUsed
Variable 'cacheKey' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
				return false;

			CacheKey cacheKey = (CacheKey)o;

			return Objects.equals(locale, cacheKey.locale) && Objects.equals(path, cacheKey.path) &&
```

### PatternVariableCanBeUsed
Variable 'component' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackDelay.java`
#### Snippet
```java
		{
			if (feedback instanceof Component) {
				Component component = (Component)feedback;
				
				// render only if it is still in the page hierarchy (WICKET-4895)
```

### PatternVariableCanBeUsed
Variable 'containerRequest' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestLoggerRequestCycleListener.java`
#### Snippet
```java
		if (cycle.getRequest().getContainerRequest() instanceof HttpServletRequest)
		{
			HttpServletRequest containerRequest = (HttpServletRequest)cycle.getRequest()
				.getContainerRequest();

```

### PatternVariableCanBeUsed
Variable 'webResponse' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ResourceIsolationRequestCycleListener.java`
#### Snippet
```java
		if (cycle.getResponse() instanceof WebResponse)
		{
			WebResponse webResponse = (WebResponse)cycle.getResponse();
			if (webResponse.isHeaderSupported())
			{
```

### PatternVariableCanBeUsed
Variable 'processor' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/resource/CompositeJavaScriptCompressor.java`
#### Snippet
```java
			if (compressor instanceof IScopeAwareTextResourceProcessor)
			{
				IScopeAwareTextResourceProcessor processor = (IScopeAwareTextResourceProcessor)compressor;
				compressed = processor.process(compressed, scope, name);
			}
```

### PatternVariableCanBeUsed
Variable 'processor' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/resource/CompositeCssCompressor.java`
#### Snippet
```java
			if (compressor instanceof IScopeAwareTextResourceProcessor)
			{
				IScopeAwareTextResourceProcessor processor = (IScopeAwareTextResourceProcessor)compressor;
				compressed = processor.process(compressed, scope, name);
			}
```

### PatternVariableCanBeUsed
Variable 'mockHttpSession' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
		if (session instanceof MockHttpSession)
		{
			MockHttpSession mockHttpSession = (MockHttpSession)session;
			if (createNew)
			{
```

### PatternVariableCanBeUsed
Variable 've' can be replaced with pattern variable
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/DateValidator.java`
#### Snippet
```java
		if (error instanceof ValidationError)
		{
			ValidationError ve = (ValidationError) error;
			ve.setVariable("inputdate", validatable.getValue());

```

### PatternVariableCanBeUsed
Variable 'cacheableResource' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/sri/DynamicSubresourceIntegrity.java`
#### Snippet
```java
			if (resource instanceof IStaticCacheableResource)
			{
				IStaticCacheableResource cacheableResource = (IStaticCacheableResource)resource;
				
				return getIntegrity(reference, cacheableResource);
```

### PatternVariableCanBeUsed
Variable 'guard' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/media/VideosApplication.java`
#### Snippet
```java
		if (packageResourceGuard instanceof SecurePackageResourceGuard)
		{
			SecurePackageResourceGuard guard = (SecurePackageResourceGuard)packageResourceGuard;
			guard.addPattern("+*.mp4");
		}
```

### PatternVariableCanBeUsed
Variable 'update' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/events/DecoupledAjaxUpdatePage.java`
#### Snippet
```java
			if (event.getPayload() instanceof CounterUpdate)
			{
				CounterUpdate update = (CounterUpdate)event.getPayload();
				update.getTarget().add(this);
			}
```

### PatternVariableCanBeUsed
Variable 'other' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/Contact.java`
#### Snippet
```java
		if (obj instanceof Contact)
		{
			Contact other = (Contact)obj;
			return other.getFirstName().equals(getFirstName()) &&
				other.getLastName().equals(getLastName()) &&
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/hangman/Letter.java`
#### Snippet
```java
		if (object instanceof Letter)
		{
			final Letter that = (Letter)object;
			return that.letter == letter && that.guessed == guessed;
		}
```

### PatternVariableCanBeUsed
Variable 'that' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/hangman/Word.java`
#### Snippet
```java
		if (object instanceof Word)
		{
			final Word that = (Word)object;
			return this.asString().equalsIgnoreCase(that.asString());
		}
```

### PatternVariableCanBeUsed
Variable 'other' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/DetachableContactModel.java`
#### Snippet
```java
		else if (obj instanceof DetachableContactModel)
		{
			DetachableContactModel other = (DetachableContactModel)obj;
			return other.id == id;
		}
```

### PatternVariableCanBeUsed
Variable 'other' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/Contact.java`
#### Snippet
```java
		if (obj instanceof Contact)
		{
			Contact other = (Contact)obj;
			return other.getFirstName().equals(getFirstName()) &&
					other.getLastName().equals(getLastName()) &&
```

### PatternVariableCanBeUsed
Variable 'guard' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/velocity/VelocityTemplateApplication.java`
#### Snippet
```java
		if (packageResourceGuard instanceof SecurePackageResourceGuard)
		{
			SecurePackageResourceGuard guard = (SecurePackageResourceGuard) packageResourceGuard;
			// allow velocity macros resources
			guard.addPattern("+*.vm");
```

### PatternVariableCanBeUsed
Variable 'wsEvent' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressBarTogglePanel.java`
#### Snippet
```java
        if (event.getPayload() instanceof WebSocketPushPayload)
        {
            WebSocketPushPayload wsEvent = (WebSocketPushPayload) event.getPayload();
            if (wsEvent.getMessage() instanceof ProgressUpdater.ProgressUpdate)
            {
```

### PatternVariableCanBeUsed
Variable 'progressUpdate' can be replaced with pattern variable
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressBarTogglePanel.java`
#### Snippet
```java
            if (wsEvent.getMessage() instanceof ProgressUpdater.ProgressUpdate)
            {
                ProgressUpdater.ProgressUpdate progressUpdate = (ProgressUpdater.ProgressUpdate)wsEvent.getMessage();
                progress = progressUpdate.getProgress();
                wsEvent.getHandler().add(this);
```

### PatternVariableCanBeUsed
Variable 'wsResource' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketMessageBroadcastHandler.java`
#### Snippet
```java
					if (resource instanceof WebSocketResource)
					{
						WebSocketResource wsResource = (WebSocketResource) resource;
						wsResource.onPayload(payload);
					}
```

### PatternVariableCanBeUsed
Variable 'textPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
		if (payload instanceof WebSocketTextPayload)
		{
			WebSocketTextPayload textPayload = (WebSocketTextPayload) payload;
			TextMessage data = textPayload.getMessage();
			onMessage(webSocketHandler, data);
```

### PatternVariableCanBeUsed
Variable 'binaryPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
		else if (payload instanceof WebSocketBinaryPayload)
		{
			WebSocketBinaryPayload binaryPayload = (WebSocketBinaryPayload) payload;
			BinaryMessage binaryData = binaryPayload.getMessage();
			onMessage(webSocketHandler, binaryData);
```

### PatternVariableCanBeUsed
Variable 'connectedPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
		else if (payload instanceof WebSocketConnectedPayload)
		{
			WebSocketConnectedPayload connectedPayload = (WebSocketConnectedPayload) payload;
			ConnectedMessage message = connectedPayload.getMessage();
			onConnect(message);
```

### PatternVariableCanBeUsed
Variable 'connectedPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
		else if (payload instanceof WebSocketClosedPayload)
		{
			WebSocketClosedPayload connectedPayload = (WebSocketClosedPayload) payload;
			ClosedMessage message = connectedPayload.getMessage();
			onClose(message);
```

### PatternVariableCanBeUsed
Variable 'errorPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
		else if (payload instanceof WebSocketErrorPayload)
		{
			WebSocketErrorPayload errorPayload = (WebSocketErrorPayload) payload;
			ErrorMessage message = errorPayload.getMessage();
			onError(webSocketHandler, message);
```

### PatternVariableCanBeUsed
Variable 'abortedPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
		else if (payload instanceof WebSocketAbortedPayload)
		{
			WebSocketAbortedPayload abortedPayload = (WebSocketAbortedPayload) payload;
			AbortedMessage message = abortedPayload.getMessage();
			onAbort(message);
```

### PatternVariableCanBeUsed
Variable 'pushPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketResource.java`
#### Snippet
```java
		else if (payload instanceof WebSocketPushPayload)
		{
			WebSocketPushPayload pushPayload = (WebSocketPushPayload) payload;
			IWebSocketPushMessage message = pushPayload.getMessage();
			onPush(webSocketHandler, message);
```

### PatternVariableCanBeUsed
Variable 'wsPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
		if (payload instanceof WebSocketPayload)
		{
			WebSocketPayload<?> wsPayload = (WebSocketPayload<?>) payload;
			WebSocketRequestHandler webSocketHandler = wsPayload.getHandler();

```

### PatternVariableCanBeUsed
Variable 'textPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
			if (payload instanceof WebSocketTextPayload)
			{
				WebSocketTextPayload textPayload = (WebSocketTextPayload) payload;
				TextMessage data = textPayload.getMessage();
				onMessage(webSocketHandler, data);
```

### PatternVariableCanBeUsed
Variable 'binaryPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
			else if (wsPayload instanceof WebSocketBinaryPayload)
			{
				WebSocketBinaryPayload binaryPayload = (WebSocketBinaryPayload) wsPayload;
				BinaryMessage binaryData = binaryPayload.getMessage();
				onMessage(webSocketHandler, binaryData);
```

### PatternVariableCanBeUsed
Variable 'connectedPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
			else if (wsPayload instanceof WebSocketConnectedPayload)
			{
				WebSocketConnectedPayload connectedPayload = (WebSocketConnectedPayload) wsPayload;
				ConnectedMessage message = connectedPayload.getMessage();
				onConnect(message);
```

### PatternVariableCanBeUsed
Variable 'closedPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
			else if (wsPayload instanceof WebSocketClosedPayload)
			{
				WebSocketClosedPayload closedPayload = (WebSocketClosedPayload) wsPayload;
				ClosedMessage message = closedPayload.getMessage();
				onClose(message);
```

### PatternVariableCanBeUsed
Variable 'errorPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
			else if (wsPayload instanceof WebSocketErrorPayload)
			{
				WebSocketErrorPayload errorPayload = (WebSocketErrorPayload) wsPayload;
				ErrorMessage message = errorPayload.getMessage();
				onError(webSocketHandler, message);
```

### PatternVariableCanBeUsed
Variable 'abortedPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
			else if (wsPayload instanceof WebSocketAbortedPayload)
			{
				WebSocketAbortedPayload abortedPayload = (WebSocketAbortedPayload) wsPayload;
				AbortedMessage message = abortedPayload.getMessage();
				onAbort(message);
```

### PatternVariableCanBeUsed
Variable 'pushPayload' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketBehavior.java`
#### Snippet
```java
			else if (wsPayload instanceof WebSocketPushPayload)
			{
				WebSocketPushPayload pushPayload = (WebSocketPushPayload) wsPayload;
				IWebSocketPushMessage message = pushPayload.getMessage();
				onPush(webSocketHandler, message);
```

### PatternVariableCanBeUsed
Variable 'httpServletRequest' can be replaced with pattern variable
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
			final String jsessionCookieName = cookieUtils.getSessionIdCookieName(application);
			final Cookie jsessionid = cookieUtils.getCookie(jsessionCookieName);
			HttpServletRequest httpServletRequest = (HttpServletRequest) containerRequest;
			if (jsessionid == null || httpServletRequest.isRequestedSessionIdValid() == false)
			{
```

### PatternVariableCanBeUsed
Variable 'page' can be replaced with pattern variable
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/AutoConversationManager.java`
#### Snippet
```java
		if (component instanceof Page)
		{
			Page page = (Page)component;
			IRequestHandler activeRequestHandler = page.getRequestCycle().getActiveRequestHandler();
			autoEndIfNecessary(page, activeRequestHandler);
```

### PatternVariableCanBeUsed
Variable 'pageHandler' can be replaced with pattern variable
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/ConversationPropagator.java`
#### Snippet
```java
		if (handler instanceof IPageClassRequestHandler)
		{
			IPageClassRequestHandler pageHandler = (IPageClassRequestHandler)handler;
			return pageHandler.getPageParameters();
		}
```

### PatternVariableCanBeUsed
Variable 'webapp' can be replaced with pattern variable
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/Initializer.java`
#### Snippet
```java
		if (application instanceof WebApplication)
		{
			WebApplication webapp = (WebApplication)application;
			ServletContext servletContext = webapp.getServletContext();
			String propertiesFolder = servletContext.getInitParameter("velocityPropertiesFolder");
```

## RuleId[id=AbstractMethodCallInConstructor]
### AbstractMethodCallInConstructor
Call to 'abstract' method `initializeMarkupFilters()` during object construction
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java

		// Initialize the markup filter chain
		filters = initializeMarkupFilters(markup);
	}

```

### AbstractMethodCallInConstructor
Call to 'abstract' method `createTree()` during object construction
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/AdvancedTreePage.java`
#### Snippet
```java
		add(form);

		tree = createTree(provider, new FooExpansionModel());
		tree.add(new Behavior()
		{
```

### AbstractMethodCallInConstructor
Call to 'abstract' method `getDataProvider()` during object construction
in `wicket-examples/src/main/java/org/apache/wicket/examples/spring/common/web/ContactsDisplayPage.java`
#### Snippet
```java
		cols.add(new PropertyColumn<>(new Model<>("cell phone"), "cellPhone"));

		add(new DefaultDataTable<>("contacts", cols, getDataProvider(), 5));
	}

```

### AbstractMethodCallInConstructor
Call to 'abstract' method `createCaptchaImageResource()` during object construction
in `wicket-examples/src/main/java/org/apache/wicket/examples/captcha/AbstractCaptchaForm.java`
#### Snippet
```java
		form.add(feedback);

		captchaImageResource = createCaptchaImageResource();
		final Image captchaImage = new Image("image", captchaImageResource);
		captchaImage.setOutputMarkupId(true);
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
Unnecessary 'contains()' check
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java
		}

		if (!listeners.contains(listener))
		{
			listeners.add(listener);
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/MultiFileUploadField.java`
#### Snippet
```java
		super.onComponentTag(tag);
		// remove the name attribute added by the FormComponent
		if (tag.getAttributes().containsKey(NAME_ATTR))
		{
			tag.getAttributes().remove(NAME_ATTR);
```

## RuleId[id=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'sourceValue' in a Serializable class
in `wicket-util/src/main/java/org/apache/wicket/util/convert/ConversionException.java`
#### Snippet
```java

	/** The value that was tried to convert. */
	private Object sourceValue;

	/** Target type for the failed conversion. */
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'originalState' in a Serializable class
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/GoAndClearFilter.java`
#### Snippet
```java
	private final Button clear;

	private final Object originalState;

	/**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'object' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/MetaDataEntry.java`
#### Snippet
```java
	final MetaDataKey<T> key;

	Object object;

	/**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'oject' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/interpolator/PropertyVariableInterpolator.java`
#### Snippet
```java

	/** The object to introspect on */
	private final Object oject;

	/**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'children' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	 * when there are more children.
	 */
	private Object children;

	public MarkupContainer(final String id)
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'data' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * <p>
	 */
	Object data = null;

	final int data_start()
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'target' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/model/ChainingModel.java`
#### Snippet
```java

	/** Any model object (which may or may not implement IModel) */
	private Object target;

	public ChainingModel(final Object modelObject)
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'parameters' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/model/StringResourceModel.java`
#### Snippet
```java

	/** Optional parameters. */
	private Object[] parameters;

	/** The default value of the message. */
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'cycle' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackDelay.java`
#### Snippet
```java
	private final List<IFeedback> feedbacks = new ArrayList<>();

	private RequestCycle cycle;
	
	/**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'sessionInfo' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
		private long totalTimeTaken;
		private long sessionSize;
		private Object sessionInfo;

		/**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'sessionInfo' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java
		private String sessionId;
		private long totalSessionSize;
		private Object sessionInfo;
		private int activeRequest;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'inputStream' in a Serializable class
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
			private byte[] bytes;
			
			private ByteArrayInputStream inputStream;

			private byte[] getBytes() {
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestUtils.java`
#### Snippet
```java
					charset = Charset.forName(charsetName);
				}
				catch (UnsupportedCharsetException useDefault)
				{
				}
```

### CatchMayIgnoreException
Empty `catch` block
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
				TimeUnit.MILLISECONDS.sleep(5000);
			}
			catch (InterruptedException e)
			{
			}
```

### CatchMayIgnoreException
Empty `catch` block
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxDownloadPage.java`
#### Snippet
```java
				TimeUnit.MILLISECONDS.sleep(3000);
			}
			catch (InterruptedException e)
			{
			}
```

### CatchMayIgnoreException
Empty `catch` block
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/LazyLoadingPage.java`
#### Snippet
```java
						Thread.sleep(seconds * 1000);
					}
					catch (InterruptedException e)
					{
					}
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is set
in `wicket-request/src/main/java/org/apache/wicket/request/IRequestParameters.java`
#### Snippet
```java
	 * Returns immutable set of all available parameter names.
	 * 
	 * @return list of parameter names
	 */
	Set<String> getParameterNames();
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java

	/**
	 * @return array of column objects this table displays
	 */
	public final List<? extends IColumn<T, S>> getColumns()
```

### MismatchedJavadocCode
Method is specified to return list but the return type is set
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java
	 * Returns the list of all configured locations of directories containing class files
	 * 
	 * @return list of locations as URL
	 */
	public static Set<URL> getLocations()
```

## RuleId[id=EnhancedSwitchMigration]
### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Objects.java`
#### Snippet
```java
	public static Number newInteger(final int type, final long value)
	{
		switch (type)
		{
			case BOOL :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
							try
							{
								switch (isHexChar)
								{
									case 'X' :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
				continue;
			}
			switch (aChar)
			{
				case ' ' :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
					{
						aChar = in[off++];
						switch (aChar)
						{
							case '0' :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-util/src/main/java/org/apache/wicket/util/string/Strings.java`
#### Snippet
```java
				continue;
			}
			switch (c)
			{
				case '\t' :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-spring/src/main/java/org/apache/wicket/spring/FieldBeansCollector.java`
#### Snippet
```java
	public void addBean(String beanName, Object bean)
	{
		switch (fieldType)
		{
			case LIST : 
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
			{
				char c = path.charAt(i);
				switch (c)
				{
					case SEPARATOR_ENCODED :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
			{
				char c = path.charAt(i);
				switch (c)
				{
					case COMPONENT_SEPARATOR :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
            for (int i = 0;  i < fileName.length();  i++) {
                final char c = fileName.charAt(i);
                switch (c) {
                    case 0:
                        sb.append("\\0");
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/DefaultExceptionMapper.java`
#### Snippet
```java
		if (isProcessingAjaxRequest())
		{
			switch (application.getExceptionSettings().getAjaxErrorHandlingStrategy())
			{
				case INVOKE_FAILURE_HANDLER :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/ComponentEventSender.java`
#### Snippet
```java
		ComponentEvent<?> event = new ComponentEvent<T>(sink, source, type, payload);
		Args.notNull(type, "type");
		switch (type)
		{
			case BUBBLE :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
		// As long as this is public api the development and deployment mode
		// should counter act each other for all properties.
		switch (getConfigurationType())
		{
			case DEVELOPMENT : {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/DequeueContext.java`
#### Snippet
```java
				{
					DequeueTagAction action = canDequeueTag(tag);
					switch (action)
					{
						case IGNORE :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/DequeueContext.java`
#### Snippet
```java
					}

					switch (canDequeueTag(open))
					{
						case DEQUEUE :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/page/DefaultPageLockManager.java`
#### Snippet
```java
							.getExceptionSettings()
							.getThreadDumpStrategy();
					switch (strategy)
					{
						case ALL_THREADS :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

		final IRequestParameters parameters;
		switch (method.toLowerCase(Locale.ROOT))
		{
			case Form.METHOD_POST:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ClientSideImageMap.java`
#### Snippet
```java
		while (!pi.isDone())
		{
			switch (pi.currentSegment(coords))
			{
				case PathIterator.SEG_MOVETO :
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
	{
		String cssClass;
		switch (message.getLevel())
		{
			case FeedbackMessage.UNDEFINED:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-core/src/main/java/org/apache/wicket/application/AbstractClassResolver.java`
#### Snippet
```java
		if (clazz == null)
		{
			switch (className)
			{
				case "byte":
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/DropDownChoicePage.java`
#### Snippet
```java
				// your business
				// object or in a utility.
				switch (value)
				{
					case 1 :
```

## RuleId[id=AssignmentToForLoopParameter]
### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
					writer.write(entityValue);
				}
				i = semiColonIdx; // move index up to the semi-colon
			}
			else
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-util/src/main/java/org/apache/wicket/util/encoding/UrlDecoder.java`
#### Snippet
```java
					{
						bos.write((char)((u << 4) + l));
						i += 2;
					}
					changed = true;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			else if ((i + 1) < segments.size() && "..".equals(segments.get(i + 1)))
			{
				i++;
			}
			else
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/captcha/kittens/OpaqueRegion.java`
#### Snippet
```java
							// Whole rectangle is obscured
							workList.remove(i);
							i -= 1;
						}
						else
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
						if (behavior == component.data_get(j))
						{
							i = j;
							len = currentLength;
							break;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
				{
					internalRemove(component, behavior);
					i--;
					len--;
				}
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java
				{
					state = MULTILINE_COMMENT;
					++i;
					continue;
				}
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java
				{
					state = REGULAR_TEXT;
					++i;
					continue;
				}
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestUtils.java`
#### Snippet
```java
					newcomponents.remove(i);
					newcomponents.remove(i);
					i = i - 2;
					if (i < -1)
					{
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestUtils.java`
#### Snippet
```java
					if (i < -1)
					{
						i = -1;
					}
				}
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `p`
in `wicket-core/src/main/java/org/apache/wicket/pageStore/InSessionPageStore.java`
#### Snippet
```java
					{
						// cannot be serialized, thus skip
						p--;
					}
					else
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-util/src/main/java/org/apache/wicket/util/file/File.java`
#### Snippet
```java
	public String toQuotedString()
	{
		return "\"" + toString() + "\"";
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringValue.java`
#### Snippet
```java

		throw new StringValueConversionException(
			"Cannot convert '" + toString() + "'to type " + type);
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-ioc/src/main/java/org/apache/wicket/injection/Injector.java`
#### Snippet
```java
			catch (IllegalArgumentException e)
			{
				throw new RuntimeException("error while injecting object [" + object.toString() +
					"] of type [" + object.getClass().getName() + "]", e);
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-ioc/src/main/java/org/apache/wicket/injection/Injector.java`
#### Snippet
```java
			catch (IllegalAccessException e)
			{
				throw new RuntimeException("error while injecting object [" + object.toString() +
					"] of type [" + object.getClass().getName() + "]", e);
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			{
				throw new IllegalStateException("Cannot render this url in " +
					StringMode.FULL.name() + " mode because it has a `..` segment: " + toString());
			}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
        if (outputFile != null && !isInMemory() && outputFile.exists()) {
            if (!outputFile.delete()) {
                final String desc = "Cannot delete " + outputFile.toString();
                throw new UncheckedIOException(desc, new IOException(desc));
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Behaviors.java`
#### Snippet
```java
			throw new IllegalStateException(
				"Tried to remove a behavior that was not added to the component. Behavior: " +
					behavior.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
					throw new WicketRuntimeException(
						"The component(s) below failed to render. Possible reasons could be that:\n\t1) you have added a component in code but forgot to reference it in the markup (thus the component will never be rendered),\n\t2) if your components were added in a parent container then make sure the markup for the child container includes them in <wicket:extend>.\n\n" +
							buffer.toString());
				}
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/KeyInSessionSunJceCryptFactory.java`
#### Snippet
```java
        
	    // generate new key
        String key = session.getId() + "." + UUID.randomUUID().toString();
        
        return new CryptData(key, salt);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			{
				throw new WicketRuntimeException("Ups. This should never happen. " +
					markupStream.toString());
			}
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
					"' not found." +
					" Enable debug messages for org.apache.wicket.util.resource to get a list of all filenames tried.: " +
					toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			if (((WicketTag)tag).isChildTag())
			{
				markupStream.throwMarkupException("Found " + tag.toString() +
					" but no <wicket:extend>. Container: " + toString());
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			{
				markupStream.throwMarkupException("Found " + tag.toString() +
					" but no <wicket:extend>. Container: " + toString());
			}
			else
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
			{
				markupStream.throwMarkupException("Failed to handle: " +
					tag.toString() +
					". It might be that no resolver has been registered to handle this special tag. " +
					" But it also could be that you declared wicket:id=" + id +
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
					" in your markup, but that you either did not add the " +
					"component to your page at all, or that the hierarchy does not match. " +
					"Container: " + toString());
			}
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		msg.append(id);
		msg.append("' in ");
		msg.append(this.toString());
		msg.append("\n\tExpected: '");
		msg.append(getPageRelativePath());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/ListenerInvocationNotAllowedException.java`
#### Snippet
```java
		if (behavior != null)
		{
			detail.append(" Behavior: ").append(behavior.toString());
		}
		return detail.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/AbstractPartialPageRequestHandler.java`
#### Snippet
```java
                throw new IllegalArgumentException(
                        "cannot update component that does not have setOutputMarkupId property set to true. Component: " +
                                component.toString());
            }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/AbstractPartialPageRequestHandler.java`
#### Snippet
```java
            throw new IllegalArgumentException(
                    "cannot update component that does not have setOutputMarkupId property set to true. Component: " +
                            component.toString());
        }
        final String id = component != null ? ("'" + component.getMarkupId() + "'") : "null";
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		if (markup == null)
		{
			throw new MarkupNotFoundException("Markup not found for Component: " + toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	protected final String exceptionMessage(final String message)
	{
		return message + ":\n" + toString();
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
			throw new MarkupNotFoundException(
				"Can not determine Markup. Component is not yet connected to a parent. " +
					toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		if (markup == null)
		{
			throw new MarkupException("Markup not found. Component: " + toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
				if (markup != null)
				{
					buffer.append(", markup = ").append(new MarkupStream(getMarkup()).toString());
				}
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		{
			log.error("Component is not connected to a Page. Cannot register the component as being rendered. Component: " +
				toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
			throw new IllegalStateException(
					"A partial update is not possible for a component that has renderBodyOnly enabled. Component: " +
							component.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
			{
				// on another page
				throw new IllegalArgumentException("Component " + component.toString() + " cannot be updated because it is on another page.");
			}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
		if (componentsFrozen)
		{
			throw new IllegalStateException("A partial update of the page is being rendered, component " + component.toString() + " can no longer be added");
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTester.java`
#### Snippet
```java
		assertNotNull(behavior);

		log.info("=== " + testClass.getName() + " : " + behavior.toString() + " ===");

		executeBehavior(behavior);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/DefaultMarkupCacheKeyProvider.java`
#### Snippet
```java
		if (locale != null)
		{
			buffer.append('_').append(locale.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupFragment.java`
#### Snippet
```java
		if (endIndex >= markupSize)
		{
			throw new MarkupException("Unable to find close tag for: '" + startTag.toString() +
				"' in " + getRootMarkup().getMarkupResourceStream().toString());
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
					{
						throw new WicketRuntimeException(
							"Did not expect a </wicket:child> tag in " + baseMarkup.toString());
					}
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
		{
			throw new WicketRuntimeException("Expected to find <wicket:child/> in base markup: " +
				baseMarkup.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
		{
			throw new WicketRuntimeException(
				"Missing close tag </wicket:extend> in derived markup: " + markup.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
						throw new WicketRuntimeException(
							"Wicket tags like <wicket:xxx> are not allowed in between <wicket:child> and </wicket:child> tags: " +
								markup.toString());
					}
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
					throw new WicketRuntimeException(
						"Wicket tags identified by wicket:id are not allowed in between <wicket:child> and </wicket:child> tags: " +
							markup.toString());
				}
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
			{
				throw new WicketRuntimeException(
					"Expected to find </wicket:child> in base markup: " + baseMarkup.toString());
			}
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/MergedMarkup.java`
#### Snippet
```java
		if (log.isDebugEnabled())
		{
			log.debug("Merge markup: " + toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
					Url encoded = new PageParametersEncoder().encodePageParameters(parameters);
					String queryString = encoded.getQueryString();
					url = object.toString() + (queryString != null ? "?" + queryString : "");
				}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
			buffer.append(getPageRelativePath());
			buffer.append(" and error: ");
			buffer.append(error.toString());
			buffer.append(". Tried keys: ");
			Iterator<String> keys = source.triedKeys.iterator();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/PanelMarkupSourcingStrategy.java`
#### Snippet
```java
						.append(" Components only raw markup is allow in between the tags but not ")
						.append("other Wicket Component. Component: ")
						.append(component.toString());

					throw new MarkupException(markupStream, msg.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/pages/ExceptionErrorPage.java`
#### Snippet
```java
						if (stream != null)
						{
							String text = "\n" + stream.toString();
							if (msg.endsWith(text))
							{
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AbstractMarkupSourcingStrategy.java`
#### Snippet
```java
							Classes.simpleName(component.getClass()) +
								" Components only raw markup is allow in between the tags but not other Wicket Component." +
								". Component: " + component.toString());
			}
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
			throw new WicketRuntimeException(
				"Could not parse markup from markup resource stream: " +
					markupResourceStream.toString(), e);
		}
		finally
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
					throw new WicketRuntimeException(
						"Unexpected tag encountered in markup of component border " +
							getClass().getName() + ". Tag: " + wTag.toString() +
							", expected tag: </wicket:border>");
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
						throw new WicketRuntimeException(
							"Unexpected tag encountered in markup of component border " +
								getClass().getName() + ". Tag: " + wTag.toString() +
								", expected tag: <wicket:border>");
					}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/BorderBehavior.java`
#### Snippet
```java
						throw new WicketRuntimeException(
							"Unexpected tag encountered in markup of component border " +
								getClass().getName() + ". Tag: " + wTag.toString() +
								", expected tag: <wicket:body> or </wicket:body>");
					}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java
		if (markup == null)
		{
			throw new MarkupException("Unable to get page markup: " + getPage().toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
		{
			throw new MarkupException("Unable to find associated markup file for Border: " +
				this.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
			throw new MarkupException(markup.getMarkupResourceStream(),
				"Unable to find <wicket:border> tag in associated markup file for Border: " +
					this.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlTag.java`
#### Snippet
```java
	public String toUserDebugString()
	{
		return " '" + toString() + "' (line " + getLineNumber() + ", column " + getColumnNumber() +
			")";
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
					throw new ParseException(
						"Make sure that 'id' and 'wicket:id' are the same if both are provided. Tag:" +
							tag.toString(), tag.getPos());
				}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
			{
				throw new ParseException(
					"Open-close tags don't make sense for InlineEnclosure. Tag:" + tag.toString(),
					tag.getPos());
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/InlineEnclosureHandler.java`
#### Snippet
```java
				{
					throw new ParseException("Did not find any child for InlineEnclosure. Tag:" +
						lastEnclosure.toString(), tag.getPos());
				}
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/StyleAndScriptIdentifier.java`
#### Snippet
```java
								if (shouldWrapInCdata(text))
								{
									text = JavaScriptUtils.SCRIPT_CONTENT_PREFIX + body.toString() +
										JavaScriptUtils.SCRIPT_CONTENT_SUFFIX;
									markup.replace(i + 1, new RawMarkup(text));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
			{
				throw new IllegalStateException(
					"The render iterator returned null for a child. Container: " + this.toString() +
						"; Iterator=" + it.toString());
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
				throw new IllegalStateException(
					"The render iterator returned null for a child. Container: " + this.toString() +
						"; Iterator=" + it.toString());
			}
			renderChild(child);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ConcatBundleResource.java`
#### Snippet
```java
		String scope = Classes.name(reference.getScope());
		String name = reference.getName();
		String message = prefix + reference.toString();

		if (getThrowExceptionOnMissingResource())
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
					{
						info("File-Name: " + upload.getClientFileName() + " File-Size: " +
								Bytes.bytes(upload.getSize()).toString());
					}
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
						for (FileUpload file : files) {
							info("File-Name: " + file.getClientFileName() + " File-Size: " +
									Bytes.bytes(file.getSize()).toString());
						}
					}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/FileUploadPage.java`
#### Snippet
```java
					{
						info("File-Name: " + upload.getClientFileName() + " File-Size: " +
								Bytes.bytes(upload.getSize()).toString());
					}
				}
```

## RuleId[id=Java9UndeclaredServiceUsage]
### Java9UndeclaredServiceUsage
Usage of service 'org.apache.wicket.resource.FileSystemPathService' is not declared in module-info
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemResourceReference.java`
#### Snippet
```java
	{
		Iterator<FileSystemPathService> pathServiceIterator = ServiceLoader
			.load(FileSystemPathService.class).iterator();
		while (pathServiceIterator.hasNext())
		{
```

## RuleId[id=Java9CollectionFactory]
### Java9CollectionFactory
Can be replaced with 'List.copyOf()' call
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
			if (temporarySessionAttributes != null)
			{
				return Collections.unmodifiableList(new ArrayList<String>(
					temporarySessionAttributes.keySet()));
			}
```

### Java9CollectionFactory
Can be replaced with 'List.copyOf()' call
in `wicket-core/src/main/java/org/apache/wicket/mock/MockSessionStore.java`
#### Snippet
```java
	public List<String> getAttributeNames(Request request)
	{
		return Collections.unmodifiableList(new ArrayList<>(attributes.keySet()));
	}

```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `TextFormatConverter` may be 'static'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeTextField.java`
#### Snippet
```java
	}

	private abstract class TextFormatConverter extends LocalDateTimeConverter {
		private static final long serialVersionUID = 1L;

```

### InnerClassMayBeStatic
Inner class `TextFormatConverter` may be 'static'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalTimeTextField.java`
#### Snippet
```java
	}

	private abstract class TextFormatConverter extends LocalTimeConverter {
		private static final long serialVersionUID = 1L;

```

### InnerClassMayBeStatic
Inner class `TextFormatConverter` may be 'static'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java
	}

	private abstract class TextFormatConverter extends LocalDateConverter {
		private static final long serialVersionUID = 1L;

```

### InnerClassMayBeStatic
Inner class `FencedFeedbackCollector` may be 'static'
in `wicket-core/src/main/java/org/apache/wicket/feedback/FencedFeedbackPanel.java`
#### Snippet
```java
	}
	
	private final class FencedFeedbackCollector extends FeedbackCollector
	{
		private FencedFeedbackCollector(Component component, boolean includeSession)
```

### InnerClassMayBeStatic
Inner class `MockedServletRegistationHandler` may be 'static'
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	 *
	 */
	class MockedServletRegistationHandler implements InvocationHandler
	{

```

### InnerClassMayBeStatic
Inner class `ResetRatingLink` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/RatingsPage.java`
#### Snippet
```java
	 * Link to reset the ratings.
	 */
	private final class ResetRatingLink extends Link<RatingModel>
	{
		/** For serialization. */
```

### InnerClassMayBeStatic
Inner class `FooExpansionModel` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/AdvancedTreePage.java`
#### Snippet
```java
	}

	private class FooExpansionModel implements IModel<Set<Foo>>
	{
		@Override
```

### InnerClassMayBeStatic
Inner class `CounterUpdate` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/events/DecoupledAjaxUpdatePage.java`
#### Snippet
```java
	 * An event payload that represents a counter update
	 */
	public class CounterUpdate
	{
		private final AjaxRequestTarget target;
```

### InnerClassMayBeStatic
Inner class `FileListView` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/MultiUploadPage.java`
#### Snippet
```java
	 * List view for files in upload folder.
	 */
	private class FileListView extends ListView<File>
	{
		/**
```

### InnerClassMayBeStatic
Inner class `FileListView` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/UploadPage.java`
#### Snippet
```java
	 * List view for files in upload folder.
	 */
	private class FileListView extends ListView<File>
	{
		/**
```

### InnerClassMayBeStatic
Inner class `UserNameStep` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/wizard/NewUserWizard.java`
#### Snippet
```java
	 * The user name step.
	 */
	private final class UserNameStep extends WizardStep
	{
		/**
```

### InnerClassMayBeStatic
Inner class `MyFragment` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/FragmentPage.java`
#### Snippet
```java
	 * A fragment,
	 */
	private class MyFragment extends Fragment
	{
		/**
```

### InnerClassMayBeStatic
Inner class `CommentForm` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/guestbook/GuestBook.java`
#### Snippet
```java
	 * @author Jonathan Locke
	 */
	public final class CommentForm extends Form<ValueMap>
	{
		/**
```

### InnerClassMayBeStatic
Inner class `Line` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/FormInputModel.java`
#### Snippet
```java
	 * Represents a line of text. Hack to get around the fact that strings are immutable.
	 */
	public final class Line implements IClusterable
	{
		private String text;
```

### InnerClassMayBeStatic
Inner class `RedirectForm` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/linkomatic/Home.java`
#### Snippet
```java
	 * Form that handles a redirect.
	 */
	private final class RedirectForm extends Form<RedirectForm>
	{
		/** receives form input. */
```

### InnerClassMayBeStatic
Inner class `SignInForm` may be 'static'
in `wicket-examples/src/main/java/org/apache/wicket/examples/authentication1/SignIn.java`
#### Snippet
```java
	 * Sign in form
	 */
	public final class SignInForm extends Form<Void>
	{
		private static final String USERNAME = "username";
```

## RuleId[id=StringEqualsEmptyString]
### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
	public static Object getValue(final String expression, final Object object)
	{
		if (expression == null || expression.equals("") || object == null)
		{
			return object;
```

## RuleId[id=UnnecessaryBoxing]
### UnnecessaryBoxing
Unnecessary boxing
in `wicket-util/src/main/java/org/apache/wicket/util/string/Entities.java`
#### Snippet
```java
		public void add(String name, int value)
		{
			mapNameToValue.put(name, Integer.valueOf(value));
			mapValueToName.put(value, name);
		}
```

### UnnecessaryBoxing
Unnecessary boxing
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
		if (logger != null)
		{
			return Integer.valueOf(logger.getPeakActiveRequestCount());
		}
		return null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
		if (logger != null)
		{
			return Integer.valueOf(logger.getCurrentActiveRequestCount());
		}
		return null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/NumberTextField.java`
#### Snippet
```java
	 * greater than zero we use zero as delegate for "any" keyword.
	 */
	public static final Double ANY = Double.valueOf(0d);

	private RangeValidator<N> validator;
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletResponse.java`
#### Snippet
```java
		else
		{
			return getDocument().substring(0, Integer.valueOf(ctheader));
		}
	}
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
			return -1;
		}
		return Integer.valueOf(value);
	}

```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'parent != null' covered by subsequent condition 'parent instanceof ConfigurableListableBeanFactory'
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
		{
			BeanFactory parent = beanFactory.getParentBeanFactory();
			if ((parent != null) && (parent instanceof ConfigurableListableBeanFactory))
			{
				return getBeanDefinition((ConfigurableListableBeanFactory)parent, name);
```

### ConditionCoveredByFurtherCondition
Condition 'selected != null' covered by subsequent condition 'selected instanceof Collection'
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/Select.java`
#### Snippet
```java
	private boolean compareModels(final Object selected, final Object value)
	{
		if ((selected != null) && (selected instanceof Collection))
		{
			if (value instanceof Collection)
```

### ConditionCoveredByFurtherCondition
Condition 'requestAsObject != null' covered by subsequent condition 'requestAsObject instanceof HttpServletRequest'
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/aspects/request/WicketFilterRequestCycleUrlAspect.java`
#### Snippet
```java
		{
			Object requestAsObject = args[2];
			if (requestAsObject != null && requestAsObject instanceof HttpServletRequest)
			{
				HttpServletRequest httpServletRequest = (HttpServletRequest)requestAsObject;
```

### ConditionCoveredByFurtherCondition
Condition 'nested != null' covered by subsequent condition 'nested instanceof IWrapModel'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	{
		IModel<?> nested = model;
		while (nested != null && nested instanceof IWrapModel)
		{
			final IModel<?> next = ((IWrapModel<?>)nested).getWrappedModel();
```

### ConditionCoveredByFurtherCondition
Condition 'elem != null' covered by subsequent condition 'elem instanceof ComponentTag'
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
		}
		// elem is null when rendering a page
		else if ((elem != null) && (elem instanceof ComponentTag))
		{
			if (getFlag(FLAG_PLACEHOLDER))
```

### ConditionCoveredByFurtherCondition
Condition 'containerRequest == null' covered by subsequent condition '(containerRequest instanceof HttpServletRequest) == false'
in `wicket-core/src/main/java/org/apache/wicket/session/HttpSessionStore.java`
#### Snippet
```java
	{
		Object containerRequest = request.getContainerRequest();
		if (containerRequest == null || (containerRequest instanceof HttpServletRequest) == false)
		{
			throw new IllegalArgumentException("Request must be ServletWebRequest");
```

## RuleId[id=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java

	/**
	 * @see java.io.Writer#write(char[], int, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java

	/**
	 * @see java.io.Writer#write(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java

	/**
	 * @see java.io.Writer#write(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java

	/**
	 * @see java.io.Writer#write(char[])
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java

	/**
	 * @see java.io.Writer#write(java.lang.String, int, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java

	/**
	 * @see java.io.Writer#write(java.lang.String, int, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java

	/**
	 * @see java.io.Writer#close()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/StringBufferWriter.java`
#### Snippet
```java

	/**
	 * @see java.io.Writer#flush()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteOrderMark.java`
#### Snippet
```java
     *
     * @return the hashcode for this BOM.
     * @see java.lang.Object#hashCode()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteCountingOutputStream.java`
#### Snippet
```java

	/**
	 * @see java.io.OutputStream#write(int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteCountingOutputStream.java`
#### Snippet
```java

	/**
	 * @see java.io.OutputStream#write(byte[], int, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/XmlReader.java`
#### Snippet
```java

	/**
	 * @see java.io.Reader#read(char[], int, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/XmlReader.java`
#### Snippet
```java

	/**
	 * @see java.io.Reader#close()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java
	 * Calls the write(byte[]) method.
	 * 
	 * @see java.io.OutputStream#write(int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java
public class ByteArrayOutputStream extends OutputStream
{
	private final List<byte[]> buffers = new java.util.ArrayList<>();
	private int count;
	private byte[] currentBuffer;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/io/ByteArrayOutputStream.java`
#### Snippet
```java

	/**
	 * @see java.io.OutputStream#write(byte[], int, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/diff/ToString.java`
#### Snippet
```java
			}
		}
		catch (java.io.IOException ignored)
		{
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/diff/ToString.java`
#### Snippet
```java

	/**
	 * Default implementation of the {@link java.lang.Object#toString toString() } method that
	 * delegates work to a {@link java.lang.StringBuffer StringBuffer} base version.
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/diff/ToString.java`
#### Snippet
```java
	/**
	 * Default implementation of the {@link java.lang.Object#toString toString() } method that
	 * delegates work to a {@link java.lang.StringBuffer StringBuffer} base version.
	 * 
	 * @return String
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Diff.java`
#### Snippet
```java
 * <p>
 * Text is represented as <code>Object[]</code> because the diff engine is capable of handling more
 * than plain ascci. In fact, arrays of any type that implements {@link java.lang.Object#hashCode
 * hashCode()} and {@link java.lang.Object#equals equals()} correctly can be subject to differencing
 * using this library.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/diff/Diff.java`
#### Snippet
```java
 * Text is represented as <code>Object[]</code> because the diff engine is capable of handling more
 * than plain ascci. In fact, arrays of any type that implements {@link java.lang.Object#hashCode
 * hashCode()} and {@link java.lang.Object#equals equals()} correctly can be subject to differencing
 * using this library.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/file/Path.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.file` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/file/Path.java`
#### Snippet
```java
	 * @param pathname
	 *      the path to the needed resource. Must be relative to {@code folder}
	 * @see org.apache.wicket.util.file.IResourceFinder#find(Class, String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/lang/PackageName.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/file/WebXmlFile.java`
#### Snippet
```java
	 * @throws SAXException
	 * 
	 * @see #getFilterPath(boolean, String, java.io.InputStream)
	 */
	public final String getUniqueFilterPath(final boolean isServlet, final String filterName,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/lang/EnumeratedType.java`
#### Snippet
```java
	 * @throws java.io.ObjectStreamException
	 */
	public Object readResolve() throws java.io.ObjectStreamException
	{
		EnumeratedType result = this;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/lang/Threads.java`
#### Snippet
```java

		/**
		 * @see java.lang.Throwable#fillInStackTrace()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/AbstractCrypt.java`
#### Snippet
```java
		try
		{
			byte[] decoded = java.util.Base64.getUrlDecoder().decode(text);
			return new String(decryptByteArray(decoded), CHARACTER_ENCODING);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/Group.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/BooleanGroup.java`
#### Snippet
```java
	 *            The matcher
	 * @return The value
	 * @see BooleanGroup#getBoolean(java.util.regex.Matcher, boolean)
	 */
	public boolean getBoolean(final Matcher matcher)
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.crypto` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/SunJceCrypt.java`
#### Snippet
```java

	/**
	 * Creates the {@link javax.crypto.Cipher} that will do the de-/encryption.
	 *
	 * @param key
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.crypto` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/SunJceCrypt.java`
#### Snippet
```java
	 *              the parameters spec to use
	 * @param mode
	 *              the mode ({@link javax.crypto.Cipher#ENCRYPT_MODE} or {@link javax.crypto.Cipher#DECRYPT_MODE})
	 * @return the cipher that will do the de-/encryption
	 * @throws GeneralSecurityException
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.crypto` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/crypt/SunJceCrypt.java`
#### Snippet
```java
	 *              the parameters spec to use
	 * @param mode
	 *              the mode ({@link javax.crypto.Cipher#ENCRYPT_MODE} or {@link javax.crypto.Cipher#DECRYPT_MODE})
	 * @return the cipher that will do the de-/encryption
	 * @throws GeneralSecurityException
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.parse.metapattern.parsers` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/parsers/ListParser.java`
#### Snippet
```java
	 * 
	 * @see #getValues()
	 * @see org.apache.wicket.util.parse.metapattern.parsers.MetaPatternParser#matches()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/parse/metapattern/MetaPattern.java`
#### Snippet
```java
	 * 
	 * @return A String representing this MetaPattern
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/PrependingStringBuffer.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringEscapeUtils.java`
#### Snippet
```java
	 *            the <code>String</code> to escape, may be null
	 * @return a new escaped <code>String</code>, <code>null</code> if null string input
	 * @see #unescapeXml(java.lang.String)
	 */
	public static String escapeXml(String str)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/StringEscapeUtils.java`
#### Snippet
```java
	 * @throws IOException
	 *             if there is a problem writing
	 * @see #unescapeXml(java.lang.String)
	 */
	public static void escapeXml(Writer writer, String str) throws IOException
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.string` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AbstractStringList.java`
#### Snippet
```java
	/**
	 * @return String iterator
	 * @see org.apache.wicket.util.string.IStringSequence#iterator()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.string` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AbstractStringList.java`
#### Snippet
```java
	 *            The index into this string list
	 * @return The string at the given index
	 * @see org.apache.wicket.util.string.IStringSequence#get(int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.string` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AbstractStringList.java`
#### Snippet
```java
	/**
	 * @return Number of strings in this string list
	 * @see org.apache.wicket.util.string.IStringSequence#size()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ZonedDateTimeConverter.java`
#### Snippet
```java

/**
 * Converts to {@link java.time.ZonedDateTime}.
 */
public class ZonedDateTimeConverter extends AbstractJavaTimeConverter<ZonedDateTime>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ByteConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.converter.AbstractConverter#getTargetType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ByteConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ByteConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/SqlDateConverter.java`
#### Snippet
```java

/**
 * Converts to {@link java.sql.Date}.
 */
public class SqlDateConverter extends AbstractDateConverter<Date>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToString(java.lang.Object, Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/AbstractConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToString(java.lang.Object, Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/DoubleConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(String, java.util.Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/DoubleConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(String, java.util.Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/DoubleConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.converter.AbstractConverter#getTargetType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ShortConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ShortConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/ShortConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.converter.AbstractConverter#getTargetType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/IntegerConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.converter.AbstractConverter#getTargetType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/IntegerConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/IntegerConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/BooleanConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/BooleanConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/BooleanConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.converter.AbstractConverter#getTargetType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/FloatConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.converter.AbstractConverter#getTargetType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/FloatConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/FloatConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/CharacterConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.converter.AbstractConverter#getTargetType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/CharacterConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/CharacterConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LocalDateTimeConverter.java`
#### Snippet
```java

/**
 * Converts from Object to {@link java.time.LocalDateTime}.
 */
public class LocalDateTimeConverter extends AbstractJavaTimeConverter<LocalDateTime>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LongConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LongConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/convert/converter/LongConverter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.convert.converter.AbstractConverter#getTargetType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
 * 
 * @author Johan Compagner
 * @see java.lang.StringBuffer
 */
public final class AppendingStringBuffer implements java.io.Serializable, CharSequence
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
 * @see java.lang.StringBuffer
 */
public final class AppendingStringBuffer implements java.io.Serializable, CharSequence
{
	/** use serialVersionUID from JDK 1.0.2 for interoperability */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(boolean)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(boolean)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(boolean)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @see java.lang.String#valueOf(boolean)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, final boolean b)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(java.lang.Object)
	 * @see AppendingStringBuffer#insert(int, java.lang.String)
	 * @see AppendingStringBuffer#length()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(java.lang.Object)
	 * @see AppendingStringBuffer#insert(int, java.lang.String)
	 * @see AppendingStringBuffer#length()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(java.lang.Object)
	 * @see AppendingStringBuffer#insert(int, java.lang.String)
	 * @see AppendingStringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if <code>index</code> is negative or greater than or equal to
	 *                <code>length()</code>.
	 * @see java.lang.StringBuffer#length()
	 */
	public void setCharAt(final int index, final char ch)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *            an <code>int</code>.
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(int)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(int)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final int i)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(int)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final int i)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *         then the index of the first character of the last such substring is returned. If it
	 *         does not occur as a substring, <code>-1</code> is returned.
	 * @exception java.lang.NullPointerException
	 *                if <code>str</code> is <code>null</code>.
	 * @since 1.4
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(float)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(float)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(float)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @see java.lang.String#valueOf(float)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, final float f)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, StringBuffer str)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return the index within this string of the first occurrence of the specified substring,
	 *         starting at the specified index.
	 * @exception java.lang.NullPointerException
	 *                if <code>str</code> is <code>null</code>.
	 * @since 1.4
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *            a <code>double</code>.
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(double)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(double)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final double d)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(double)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final double d)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *            the index to start the search from.
	 * @return the index within this string of the last occurrence of the specified substring.
	 * @exception java.lang.NullPointerException
	 *                if <code>str</code> is <code>null</code>.
	 * @since 1.4
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(int)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(int)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(int)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @see java.lang.String#valueOf(int)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, final int i)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception IndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, final char c)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *            a <code>float</code>.
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(float)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(float)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final float f)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(float)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final float f)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *            a <code>boolean</code>.
	 * @return a reference to this <code>AppendingStringBuffer</code>.
	 * @see java.lang.String#valueOf(boolean)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code>.
	 * @see java.lang.String#valueOf(boolean)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final boolean b)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code>.
	 * @see java.lang.String#valueOf(boolean)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final boolean b)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, StringBuilder str)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, String str)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @throws IOException
	 */
	private void readObject(final java.io.ObjectInputStream s) throws IOException,
		ClassNotFoundException
	{
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(long)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(long)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(long)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @see java.lang.String#valueOf(long)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, final long l)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *            an <code>Object</code>.
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(java.lang.Object)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *            an <code>Object</code>.
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(java.lang.Object)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(java.lang.Object)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final Object obj)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(java.lang.Object)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final Object obj)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @param minimumCapacity
	 * 
	 * @see java.lang.StringBuffer#ensureCapacity(int)
	 */
	private void expandCapacity(final int minimumCapacity)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception IndexOutOfBoundsException
	 *                if the <code>newLength</code> argument is negative.
	 * @see java.lang.StringBuffer#length()
	 */
	public void setLength(final int newLength)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @exception StringIndexOutOfBoundsException
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(double)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(double)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if the offset is invalid.
	 * @see java.lang.String#valueOf(double)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @see java.lang.String#valueOf(double)
	 * @see java.lang.StringBuffer#insert(int, java.lang.String)
	 * @see java.lang.StringBuffer#length()
	 */
	public AppendingStringBuffer insert(final int offset, final double d)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *         the first character of the first such substring is returned; if it does not occur as
	 *         a substring, <code>-1</code> is returned.
	 * @exception java.lang.NullPointerException
	 *                if <code>str</code> is <code>null</code>.
	 * @since 1.4
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *                if <code>index</code> is negative or greater than or equal to
	 *                <code>length()</code>.
	 * @see java.lang.StringBuffer#length()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 *            a <code>long</code>.
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(long)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(long)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final long l)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/string/AppendingStringBuffer.java`
#### Snippet
```java
	 * @return a reference to this <code>AppendingStringBuffer</code> object.
	 * @see java.lang.String#valueOf(long)
	 * @see java.lang.StringBuffer#append(java.lang.String)
	 */
	public AppendingStringBuffer append(final long l)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.license` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlPrologHeaderHandler.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.license.XmlLicenseHeaderHandler#checkLicenseHeader(java.io.File)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/license/XmlPrologHeaderHandler.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.license.XmlLicenseHeaderHandler#checkLicenseHeader(java.io.File)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/visit/Visits.java`
#### Snippet
```java
	@SuppressWarnings("unchecked")
	private static <S, R> void visitPostOrderHelper(final Object component,
		final org.apache.wicket.util.visit.IVisitor<S, R> visitor, final IVisitFilter filter,
		final Visit<R> visit)
	{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/visit/Visits.java`
#### Snippet
```java
	 */
	public static <S, R> R visitPostOrder(final Object root,
		final org.apache.wicket.util.visit.IVisitor<S, R> visitor, final IVisitFilter filter)
	{
		Args.notNull(visitor, "visitor");
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/visit/Visits.java`
#### Snippet
```java
	 */
	public static <S, R> R visitPostOrder(final S root,
		final org.apache.wicket.util.visit.IVisitor<S, R> visitor)
	{
		return visitPostOrder(root, visitor, IVisitFilter.ANY);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/StringResourceStream.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/StringResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.AbstractStringResourceStream#getString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/StringResourceStream.java`
#### Snippet
```java
 * A StringResourceStream is an IResource implementation for strings.
 * 
 * @see org.apache.wicket.util.resource.IResourceStream
 * @see org.apache.wicket.util.watch.IModifiable
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#getInputStream()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#length()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
	 * Returns always null
	 * 
	 * @see org.apache.wicket.util.resource.IResourceStream#getContentType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#close()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
		try
		{
			javax.xml.transform.Source xmlSource = new javax.xml.transform.stream.StreamSource(
				xmlResource.getInputStream());
			javax.xml.transform.Source xsltSource = new javax.xml.transform.stream.StreamSource(
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform.stream` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
		try
		{
			javax.xml.transform.Source xmlSource = new javax.xml.transform.stream.StreamSource(
				xmlResource.getInputStream());
			javax.xml.transform.Source xsltSource = new javax.xml.transform.stream.StreamSource(
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
			javax.xml.transform.Source xmlSource = new javax.xml.transform.stream.StreamSource(
				xmlResource.getInputStream());
			javax.xml.transform.Source xsltSource = new javax.xml.transform.stream.StreamSource(
				xsltResource.getInputStream());
			out = new ByteArrayOutputStream();
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform.stream` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
			javax.xml.transform.Source xmlSource = new javax.xml.transform.stream.StreamSource(
				xmlResource.getInputStream());
			javax.xml.transform.Source xsltSource = new javax.xml.transform.stream.StreamSource(
				xsltResource.getInputStream());
			out = new ByteArrayOutputStream();
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
				xsltResource.getInputStream());
			out = new ByteArrayOutputStream();
			javax.xml.transform.Result result = new javax.xml.transform.stream.StreamResult(out);

			// create an instance of TransformerFactory
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform.stream` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
				xsltResource.getInputStream());
			out = new ByteArrayOutputStream();
			javax.xml.transform.Result result = new javax.xml.transform.stream.StreamResult(out);

			// create an instance of TransformerFactory
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java

			// create an instance of TransformerFactory
			javax.xml.transform.TransformerFactory transFact = javax.xml.transform.TransformerFactory.newInstance();

			javax.xml.transform.Transformer trans = transFact.newTransformer(xsltSource);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java

			// create an instance of TransformerFactory
			javax.xml.transform.TransformerFactory transFact = javax.xml.transform.TransformerFactory.newInstance();

			javax.xml.transform.Transformer trans = transFact.newTransformer(xsltSource);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/resource/XSLTResourceStream.java`
#### Snippet
```java
			javax.xml.transform.TransformerFactory transFact = javax.xml.transform.TransformerFactory.newInstance();

			javax.xml.transform.Transformer trans = transFact.newTransformer(xsltSource);
			Map<Object, Object> parameters = getParameters();
			if (parameters != null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/StringBufferResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.AbstractStringResourceStream#getString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStreamWriter.java`
#### Snippet
```java
/**
 * Base implementation of an IResourceStreamWriter so that you only have to override the
 * {@link org.apache.wicket.util.resource.IResourceStreamWriter#write(java.io.OutputStream)}. Don't forget to overwrite the
 * {@link org.apache.wicket.util.resource.IResourceStream#length()} method if you do know the total
 * length that will be generated.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStreamWriter.java`
#### Snippet
```java
 * Base implementation of an IResourceStreamWriter so that you only have to override the
 * {@link org.apache.wicket.util.resource.IResourceStreamWriter#write(java.io.OutputStream)}. Don't forget to overwrite the
 * {@link org.apache.wicket.util.resource.IResourceStream#length()} method if you do know the total
 * length that will be generated.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStreamWriter.java`
#### Snippet
```java
 * length that will be generated.
 *
 * @see org.apache.wicket.util.resource.IResourceStreamWriter
 */
public abstract class AbstractResourceStreamWriter implements IResourceStreamWriter
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#getInputStream()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#getContentType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractStringResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#close()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/AbstractResourceStream.java`
#### Snippet
```java

/**
 * @see org.apache.wicket.util.resource.IResourceStream
 * 
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/FileSystemResourceStream.java`
#### Snippet
```java
 * A FileSystemResourceStream is an IResourceStream implementation for Java NIO paths.
 * 
 * @see org.apache.wicket.util.resource.IResourceStream
 * @see org.apache.wicket.util.watch.IModifiable
 * @author Tobias Soloschenko
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/FileResourceStream.java`
#### Snippet
```java
 * A FileResourceStream is an IResource implementation for files.
 * 
 * @see org.apache.wicket.util.resource.IResourceStream
 * @see org.apache.wicket.util.watch.IModifiable
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MostRecentlyUsedMap.java`
#### Snippet
```java

	/**
	 * @see java.util.LinkedHashMap#removeEldestEntry(java.util.Map.Entry)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MostRecentlyUsedMap.java`
#### Snippet
```java

	/**
	 * @see java.util.LinkedHashMap#removeEldestEntry(java.util.Map.Entry)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#close()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.AbstractResourceStream#length()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.AbstractResourceStream#lastModifiedTime()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#getContentType()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ZipResourceStream.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.IResourceStream#getInputStream()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.servlet.http` is unnecessary and can be removed
in `wicket-util/src/main/java/javax/servlet/http/HttpSession.java`
#### Snippet
```java
public interface HttpSession extends jakarta.servlet.http.HttpSession {
    
    class Impl implements javax.servlet.http.HttpSession {

        private final jakarta.servlet.http.HttpSession delegate;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.javax` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/WicketServerApplicationConfig.java`
#### Snippet
```java

/**
 * Configures WicketServerEndpointConfig that will setup {@link org.apache.wicket.protocol.ws.javax.WicketEndpoint}
 * and a custom {@link jakarta.websocket.server.ServerEndpointConfig.Configurator} to collect the
 * useful information from the upgrade http request
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.websocket.server` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/WicketServerApplicationConfig.java`
#### Snippet
```java
/**
 * Configures WicketServerEndpointConfig that will setup {@link org.apache.wicket.protocol.ws.javax.WicketEndpoint}
 * and a custom {@link jakarta.websocket.server.ServerEndpointConfig.Configurator} to collect the
 * useful information from the upgrade http request
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.websocket` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxWebSocketSession.java`
#### Snippet
```java

/**
 * An implementation of IWebSocketSession that is just a delegating  wrapper around {@link jakarta.websocket.Session}.
 */
class JavaxWebSocketSession implements IWebSocketSession
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxWebSocketProcessor.java`
#### Snippet
```java
	 *            the WebSocket session
	 * @param application
	 *            the {@link org.apache.wicket.protocol.http.WebApplication}
	 * @param endpointConfig
	 *            the {@link jakarta.websocket.EndpointConfig}
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.websocket` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxWebSocketProcessor.java`
#### Snippet
```java
	 *            the {@link org.apache.wicket.protocol.http.WebApplication}
	 * @param endpointConfig
	 *            the {@link jakarta.websocket.EndpointConfig}
	 */
	public JavaxWebSocketProcessor(final Session session, final WebApplication application, EndpointConfig endpointConfig)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/SessionSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.SessionSettingsMBean#getPageFactory()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/SessionSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.SessionSettingsMBean#getSessionStore()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestLoggerMBean#getNumberOfLiveSessions()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary, and can be replaced with an import
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
	public Integer getNumberOfLiveSessions() throws IOException
	{
		org.apache.wicket.protocol.http.IRequestLogger logger = getRequestLogger();
		if (logger != null)
		{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestLoggerMBean#restart()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestLoggerMBean#getPeakNumberOfActiveRequests()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary, and can be replaced with an import
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
	public Integer getPeakNumberOfActiveRequests() throws IOException
	{
		org.apache.wicket.protocol.http.IRequestLogger logger = getRequestLogger();
		if (logger != null)
		{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestLoggerMBean#stop()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestLoggerMBean#getNumberOfCurrentActiveRequests()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary, and can be replaced with an import
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
	public Integer getNumberOfCurrentActiveRequests() throws IOException
	{
		org.apache.wicket.protocol.http.IRequestLogger logger = getRequestLogger();
		if (logger != null)
		{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestLoggerMBean#getNumberOfCreatedSessions()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary, and can be replaced with an import
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
	public Integer getNumberOfCreatedSessions() throws IOException
	{
		org.apache.wicket.protocol.http.IRequestLogger logger = getRequestLogger();
		if (logger != null)
		{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestLoggerMBean#getPeakNumberOfSessions()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary, and can be replaced with an import
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
	public Integer getPeakNumberOfSessions() throws IOException
	{
		org.apache.wicket.protocol.http.IRequestLogger logger = getRequestLogger();
		if (logger != null)
		{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/SecuritySettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.SecuritySettingsMBean#getCryptFactory()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary, and can be replaced with an import
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestLogger.java`
#### Snippet
```java
	 *         application
	 */
	protected org.apache.wicket.protocol.http.IRequestLogger getRequestLogger()
	{
		if (application instanceof WebApplication)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/SecuritySettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.SecuritySettingsMBean#getUnauthorizedComponentInstantiationListener()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/SecuritySettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.SecuritySettingsMBean#getAuthorizationStrategy()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#setGatherExtendedBrowserInfo(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#setBufferResponse(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#setTimeout(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#setTimeout(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#getTimeout()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#getResponseRequestEncoding()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#getBufferResponse()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java
	 *             if encoding is not supported
	 * 
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#setResponseRequestEncoding(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java
	 *             if encoding is not supported
	 * 
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#setResponseRequestEncoding(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/RequestCycleSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.RequestCycleSettingsMBean#getGatherExtendedBrowserInfo()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/PageSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.PageSettingsMBean#setVersionPagesByDefault(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/PageSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.PageSettingsMBean#getVersionPagesByDefault()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/ApplicationSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.ApplicationSettingsMBean#getUnexpectedExceptionDisplay()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/ApplicationSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.ApplicationSettingsMBean#getPageExpiredErrorPage()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/ApplicationSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.ApplicationSettingsMBean#setDefaultMaximumUploadSize(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/ApplicationSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.ApplicationSettingsMBean#setDefaultMaximumUploadSize(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/DebugSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.DebugSettingsMBean#isAjaxDebugModeEnabled()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/DebugSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.DebugSettingsMBean#getComponentUseCheck()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#getDefaultMarkupEncoding()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#setAutomaticLinking(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#getStripComments()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java
	/**
	 * @throws UnsupportedEncodingException
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#setDefaultMarkupEncoding(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java
	/**
	 * @throws UnsupportedEncodingException
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#setDefaultMarkupEncoding(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#getAutomaticLinking()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#setStripComments(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#getCompressWhitespace()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#setStripWicketTags(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#getStripWicketTags()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.jmx` is unnecessary and can be removed
in `wicket-jmx/src/main/java/org/apache/wicket/jmx/wrapper/MarkupSettings.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.jmx.MarkupSettingsMBean#setCompressWhitespace(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.bean.validation` is unnecessary and can be removed
in `wicket-bean-validation/src/main/java/org/apache/wicket/bean/validation/IViolationTranslator.java`
#### Snippet
```java
 * 
 * @author igor
 * @see BeanValidationConfiguration#setViolationTranslator(org.apache.wicket.bean.validation.IViolationTranslator)
 */
@FunctionalInterface
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-objectsizeof-agent/src/main/java/org/apache/wicket/util/instrument/InstrumentationObjectSizeOfStrategy.java`
#### Snippet
```java
	 * @return object size
	 * 
	 * @see org.apache.wicket.core.util.lang.WicketObjects.IObjectSizeOfStrategy#sizeOf(java.io.Serializable)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#entrySet()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#values()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#keySet()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#clear()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#size()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#containsKey(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#containsKey(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#get(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#get(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#put(java.lang.Object, java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#put(java.lang.Object, java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#put(java.lang.Object, java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#containsValue(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#containsValue(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#isEmpty()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#putAll(java.util.Map)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#putAll(java.util.Map)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#remove(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/MiniMap.java`
#### Snippet
```java

	/**
	 * @see java.util.Map#remove(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#contains(java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#contains(java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		/**
		 * @see java.util.AbstractCollection#iterator()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#hashCode()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#remove()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	 * 
	 */
	private void writeObject(final java.io.ObjectOutputStream s) throws IOException
	{
		// Write out the threshold, loadfactor, and any hidden stuff
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#clear()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#equals(java.lang.Object)
		 */
		@SuppressWarnings("unchecked")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#equals(java.lang.Object)
		 */
		@SuppressWarnings("unchecked")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	private void readObject(final java.io.ObjectInputStream s) throws IOException,
		ClassNotFoundException
	{
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#size()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#contains(java.lang.Object)
		 */
		@SuppressWarnings("unchecked")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#contains(java.lang.Object)
		 */
		@SuppressWarnings("unchecked")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#size()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#remove(java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#remove(java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#hasNext()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#clear()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#clear()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		/**
		 * @see java.util.AbstractCollection#iterator()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		/**
		 * @see java.util.AbstractCollection#iterator()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#contains(java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#contains(java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		/**
		 * @see java.util.Iterator#next()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		/**
		 * @see java.util.Iterator#next()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java
	{
		/**
		 * @see java.util.Iterator#next()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#remove(java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#remove(java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-util/src/main/java/org/apache/wicket/util/collections/IntHashMap.java`
#### Snippet
```java

		/**
		 * @see java.util.AbstractCollection#size()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.injection` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/NoopFieldValueFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#getFieldValue(java.lang.reflect.Field,
	 *      java.lang.Object)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/NoopFieldValueFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#getFieldValue(java.lang.reflect.Field,
	 *      java.lang.Object)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/NoopFieldValueFactory.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#getFieldValue(java.lang.reflect.Field,
	 *      java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.injection` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/NoopFieldValueFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#supportsField(java.lang.reflect.Field)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/NoopFieldValueFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#supportsField(java.lang.reflect.Field)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.proxy` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.proxy.LazyInitProxyFactory.IWriteReplace#writeReplace()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.proxy` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.proxy.ILazyInitProxy#getObjectLocator()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java

		/**
		 * @see java.lang.reflect.InvocationHandler#invoke(java.lang.Object,
		 *      java.lang.reflect.Method, java.lang.Object[])
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java

		/**
		 * @see java.lang.reflect.InvocationHandler#invoke(java.lang.Object,
		 *      java.lang.reflect.Method, java.lang.Object[])
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java
		/**
		 * @see java.lang.reflect.InvocationHandler#invoke(java.lang.Object,
		 *      java.lang.reflect.Method, java.lang.Object[])
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/proxy/jdk/JdkProxyFactory.java`
#### Snippet
```java
		/**
		 * @see java.lang.reflect.InvocationHandler#invoke(java.lang.Object,
		 *      java.lang.reflect.Method, java.lang.Object[])
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.spring` is unnecessary and can be removed
in `wicket-spring/src/main/java/org/apache/wicket/spring/test/SpringContextLocatorMock.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.spring.ISpringContextLocator#getSpringContext()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.injection` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/CompoundFieldValueFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#supportsField(java.lang.reflect.Field)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/CompoundFieldValueFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#supportsField(java.lang.reflect.Field)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.injection` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/CompoundFieldValueFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#getFieldValue(java.lang.reflect.Field,
	 *      java.lang.Object)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/CompoundFieldValueFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#getFieldValue(java.lang.reflect.Field,
	 *      java.lang.Object)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-ioc/src/main/java/org/apache/wicket/injection/CompoundFieldValueFactory.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.injection.IFieldValueFactory#getFieldValue(java.lang.reflect.Field,
	 *      java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/SpringComponentInjector.java`
#### Snippet
```java
	 * Constructor used when spring application context is declared in the spring standard way and
	 * can be located through
	 * {@link WebApplicationContextUtils#getRequiredWebApplicationContext(jakarta.servlet.ServletContext)}.
	 * 
	 * @param webapp
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-spring/src/main/java/org/apache/wicket/spring/SpringBeanLocator.java`
#### Snippet
```java
	 * @param clazz
	 *            bean class
	 * @throws java.lang.IllegalStateException
	 * @return found bean
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.inject` is unnecessary and can be removed
in `wicket-spring/src/main/java/org/apache/wicket/spring/injection/annot/AnnotProxyFieldValueFactory.java`
#### Snippet
```java
 * @see SpringBean
 * @see SpringBeanLocator
 * @see jakarta.inject.Inject
 * 
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.application` is unnecessary and can be removed
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.application.IComponentOnBeforeRenderListener#onBeforeRender(org.apache.wicket.Component)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.application.IComponentOnBeforeRenderListener#onBeforeRender(org.apache.wicket.Component)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/RequestHandlerExecutor.java`
#### Snippet
```java

		/**
		 * @see java.lang.Throwable#fillInStackTrace()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/LiveSessionsPage.java`
#### Snippet
```java

					/**
					 * @see org.apache.wicket.markup.html.link.Link#onClick()
					 */
					@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/http/flow/AbortWithHttpErrorCodeException.java`
#### Snippet
```java
	 * @param errorCode
	 *            the servlet error code; use one of the
	 *            {@link jakarta.servlet.http.HttpServletResponse} constants
	 * @param message
	 *            the optional message to send to the client
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/http/flow/AbortWithHttpErrorCodeException.java`
#### Snippet
```java
	 * @param message
	 *            the optional message to send to the client
	 * @see jakarta.servlet.http.HttpServletResponse
	 */
	public AbortWithHttpErrorCodeException(final int errorCode, final String message)
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/http/flow/AbortWithHttpErrorCodeException.java`
#### Snippet
```java
	 * @param errorCode
	 *            the servlet error code; use one of the
	 *            {@link jakarta.servlet.http.HttpServletResponse} constants
	 * @see jakarta.servlet.http.HttpServletResponse
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/http/flow/AbortWithHttpErrorCodeException.java`
#### Snippet
```java
	 *            the servlet error code; use one of the
	 *            {@link jakarta.servlet.http.HttpServletResponse} constants
	 * @see jakarta.servlet.http.HttpServletResponse
	 */
	public AbortWithHttpErrorCodeException(final int errorCode)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/http/handler/ErrorCodeRequestHandler.java`
#### Snippet
```java
	 * Respond by sending the set errorCode and optionally the message to the browser.
	 * 
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/http/handler/ErrorCodeRequestHandler.java`
#### Snippet
```java
	 * Respond by sending the set errorCode and optionally the message to the browser.
	 * 
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/ParentPathReferenceRewriter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#mapRequest(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/ParentPathReferenceRewriter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#mapRequest(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/PageComponentInfo.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/handler/EmptyRequestHandler.java`
#### Snippet
```java
	 * Does nothing at all.
	 * 
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/handler/EmptyRequestHandler.java`
#### Snippet
```java
	 * Does nothing at all.
	 * 
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/handler/TextRequestHandler.java`
#### Snippet
```java
	 * Responds by sending the string property.
	 * 
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/handler/TextRequestHandler.java`
#### Snippet
```java
	 * Responds by sending the string property.
	 * 
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/EmptyRequestParameters.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValues(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/EmptyRequestParameters.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValues(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/EmptyRequestParameters.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValue(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/EmptyRequestParameters.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValue(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/EmptyRequestParameters.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterNames()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/UrlRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterNames()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/UrlRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValues(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/UrlRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValues(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/UrlRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValue(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/UrlRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValue(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/CombinedRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValues(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/CombinedRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValues(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/CombinedRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterNames()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/CombinedRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValue(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/parameter/CombinedRequestParametersAdapter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestParameters#getParameterValue(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/Initializer.java`
#### Snippet
```java
{
	/**
	 * @see org.apache.wicket.IInitializer#init(org.apache.wicket.Application)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/Initializer.java`
#### Snippet
```java
{
	/**
	 * @see org.apache.wicket.IInitializer#init(org.apache.wicket.Application)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/Initializer.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.mapper.parameter` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/parameter/PageParameters.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.mapper.parameter.IIndexedParameters#set(int, java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/parameter/PageParameters.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.mapper.parameter.IIndexedParameters#set(int, java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.cookies` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/AjaxDownloadBehavior.java`
#### Snippet
```java

	/**
	 * @return The {@link org.apache.wicket.util.cookies.CookieDefaults.SameSite} attribute to be used for the complete download.
	 */
	public CookieDefaults.SameSite getSameSite()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.cookies` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/AjaxDownloadBehavior.java`
#### Snippet
```java

	/**
	 * Setter for the same {@link org.apache.wicket.util.cookies.CookieDefaults.SameSite}
	 *
	 * @param sameSite The non-null sameSite attribute
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/AjaxDownloadBehavior.java`
#### Snippet
```java
	/**
	 * Call this method to initiate the download. You can use the {@link #resourceParameters} to dynamically pass
	 * information to the {@link org.apache.wicket.request.resource.IResource} in order to generate contents.
	 *
	 * @param handler
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/table/AjaxNavigationToolbar.java`
#### Snippet
```java
			 * default implementation doesn't support DataViews.
			 * 
			 * @see AjaxPagingNavigator#onAjaxEvent(org.apache.wicket.ajax.AjaxRequestTarget)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.table` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/repeater/data/table/AjaxFallbackDefaultDataTable.java`
#### Snippet
```java
	/**
	 * Factory method for toolbars
	 * @param dataProvider {@link org.apache.wicket.extensions.markup.html.repeater.data.table.ISortableDataProvider}
	 */
	protected void addToolBars(final ISortableDataProvider<T, S> dataProvider)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/model/AbstractCheckBoxModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.model.IModel#setObject(Object)
	 */
        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/model/AbstractCheckBoxModel.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.model.IModel#getObject()
	 */
        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
 * There are several methods that can be overridden for customization.
 * <ul>
 * <li>{@link #onEdit(org.apache.wicket.ajax.AjaxRequestTarget)} is called when the label is clicked
 * and the editor is to be displayed. The default implementation switches the label for the editor
 * and places the caret at the end of the text.</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
 * and the editor is to be displayed. The default implementation switches the label for the editor
 * and places the caret at the end of the text.</li>
 * <li>{@link #onSubmit(org.apache.wicket.ajax.AjaxRequestTarget)} is called when in edit mode, the
 * user submitted new content, that content validated well, and the model value successfully
 * updated. This implementation also clears any <code>window.status</code> set.</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
 * user submitted new content, that content validated well, and the model value successfully
 * updated. This implementation also clears any <code>window.status</code> set.</li>
 * <li>{@link #onError(org.apache.wicket.ajax.AjaxRequestTarget)} is called when in edit mode, the
 * user submitted new content, but that content did not validate. Get the current input by calling
 * {@link FormComponent#getInput()} on {@link #getEditor()}, and the error message by calling:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxEditableLabel.java`
#### Snippet
```java
 * <code>window.status</code>, redisplays the editor, selects the editor's content and sets the
 * focus on it.
 * <li>{@link #onCancel(org.apache.wicket.ajax.AjaxRequestTarget)} is called when in edit mode, the
 * user choose not to submit the contents (he/she pressed escape). The default implementation
 * displays the label again without any further action.</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
	 *            A <code>SimpleDateFormat</code> pattern
	 * 
	 * @see org.apache.wicket.markup.html.form.TextField
	 */
	public DateTextField(final String id, final String datePattern)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
	 *            The model
	 * 
	 * @see org.apache.wicket.markup.html.form.TextField
	 */
	public DateTextField(final String id, final IModel<Date> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert.converter` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java

			/**
			 * @see org.apache.wicket.util.convert.converter.DateConverter#getDateFormat(java.util.Locale)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java

			/**
			 * @see org.apache.wicket.util.convert.converter.DateConverter#getDateFormat(java.util.Locale)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
	 * @return A pattern-specific converter
	 * 
	 * @see org.apache.wicket.markup.html.form.TextField
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
	 *            The id of the text field
	 * 
	 * @see org.apache.wicket.markup.html.form.TextField
	 */
	public DateTextField(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/DateTextField.java`
#### Snippet
```java
	 *            A <code>SimpleDateFormat</code> pattern
	 * 
	 * @see org.apache.wicket.markup.html.form.TextField
	 */
	public DateTextField(final String id, final IModel<Date> model, final String datePattern)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.form.palette.component` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
	 * @param choice
	 * @return null
	 * @see org.apache.wicket.extensions.markup.html.form.palette.component.Selection#getAdditionalAttributes(Object)
	 */
	protected Map<String, String> getAdditionalAttributesForChoices(final Object choice)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.form.palette.component` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/palette/Palette.java`
#### Snippet
```java
	 * @param choice
	 * @return null
	 * @see org.apache.wicket.extensions.markup.html.form.palette.component.Selection#getAdditionalAttributes(Object)
	 */
	protected Map<String, String> getAdditionalAttributesForSelection(final Object choice)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeField.java`
#### Snippet
```java

/**
 * Works on a {@link java.time.LocalDateTime} object. See {@link AbstractDateTimeField} for
 * further details.
 *  
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/ZonedDateTimeField.java`
#### Snippet
```java

/**
 * Works on a {@link java.time.ZonedDateTime} object. See {@link AbstractDateTimeField} for
 * further details.
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeTextField.java`
#### Snippet
```java
	/**
	 * @return The specialized converter.
	 * @see org.apache.wicket.Component#createConverter(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time.format` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTimeTextField.java`
#### Snippet
```java
 * parse and format values.
 * 
 * @see java.time.format.DateTimeFormatter
 * 
 * @author eelcohillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time.format` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalTimeTextField.java`
#### Snippet
```java
 * parse and format values.
 * 
 * @see java.time.format.DateTimeFormatter
 * 
 * @author eelcohillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalTimeTextField.java`
#### Snippet
```java
	/**
	 * @return The specialized converter.
	 * @see org.apache.wicket.Component#createConverter(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.tabs` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/ITab.java`
#### Snippet
```java
 * Interface used to represent a single tab in a TabbedPanel
 * 
 * @see org.apache.wicket.extensions.markup.html.tabs.TabbedPanel
 * @see org.apache.wicket.extensions.markup.html.tabs.AbstractTab
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.tabs` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/ITab.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.extensions.markup.html.tabs.TabbedPanel
 * @see org.apache.wicket.extensions.markup.html.tabs.AbstractTab
 * 
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.tabs` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/PanelCachingTab.java`
#### Snippet
```java
 * A simple decorator that will cache the panel returned from the first call to getPanel()
 * 
 * @see org.apache.wicket.extensions.markup.html.tabs.ITab
 * @see org.apache.wicket.extensions.markup.html.tabs.TabbedPanel
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.tabs` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/PanelCachingTab.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.extensions.markup.html.tabs.ITab
 * @see org.apache.wicket.extensions.markup.html.tabs.TabbedPanel
 * 
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time.temporal` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/AbstractDateTimeField.java`
#### Snippet
```java

/**
 * Works on a {@link java.time.temporal.Temporal} object, aggregating a {@link LocalDateTextField} and a {@link TimeField}.
 * <p>
 * <strong>Ajaxifying an AbstractDateTimeField</strong>:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.grid` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/PropertyPopulator.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.grid.ICellPopulator#populateItem(org.apache.wicket.markup.repeater.Item,
	 *      java.lang.String, org.apache.wicket.model.IModel)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/PropertyPopulator.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.grid.ICellPopulator#populateItem(org.apache.wicket.markup.repeater.Item,
	 *      java.lang.String, org.apache.wicket.model.IModel)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/PropertyPopulator.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.grid.ICellPopulator#populateItem(org.apache.wicket.markup.repeater.Item,
	 *      java.lang.String, org.apache.wicket.model.IModel)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/PropertyPopulator.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.grid.ICellPopulator#populateItem(org.apache.wicket.markup.repeater.Item,
	 *      java.lang.String, org.apache.wicket.model.IModel)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.tabs` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
 * For a complete example see the component references in wicket-examples project
 * 
 * @see org.apache.wicket.extensions.markup.html.tabs.ITab
 * 
 * @author Igor Vaynberg (ivaynberg at apache dot org)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/tabs/TabbedPanel.java`
#### Snippet
```java
	 * compound model of a parent.
	 * 
	 * @see org.apache.wicket.Component#initModel()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByLink.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.link.Link
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time.format` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java
 * parse and format values.
 * 
 * @see java.time.format.DateTimeFormatter
 * 
 * @author eelcohillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/datetime/LocalDateTextField.java`
#### Snippet
```java
	/**
	 * @return The specialized converter.
	 * @see org.apache.wicket.Component#createConverter(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater.data` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/AbstractDataGridView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.data.DataViewBase#onDetach()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/AbstractDataGridView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.RefreshingView#populateItem(org.apache.wicket.markup.repeater.Item)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/grid/AbstractDataGridView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.RefreshingView#populateItem(org.apache.wicket.markup.repeater.Item)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param id
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 * @param property
	 *            see
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param id
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 * @param property
	 *            see
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.sort` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param id
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 * @param property
	 *            see
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param property
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 * @param stateLocator
	 *            see
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param property
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 * @param stateLocator
	 *            see
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.sort` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param property
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 * @param stateLocator
	 *            see
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param stateLocator
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 */
	public OrderByBorder(final String id, final S property,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param stateLocator
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 */
	public OrderByBorder(final String id, final S property,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.sort` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/sort/OrderByBorder.java`
#### Snippet
```java
	 * @param stateLocator
	 *            see
	 *            {@link OrderByLink#OrderByLink(java.lang.String, java.lang.Object, org.apache.wicket.extensions.markup.html.repeater.data.sort.ISortStateLocator) }
	 */
	public OrderByBorder(final String id, final S property,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java

	/**
	 * The {@code RandomNumberGeneratorFactory} uses {@link java.security.SecureRandom} as RNG and {@code NativePRNG}
	 * on unix and {@code Windows-PRNG} on windows if it exists. Else it will fallback to {@code SHA1PRNG}.
	 * <p/>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
	 * on unix and {@code Windows-PRNG} on windows if it exists. Else it will fallback to {@code SHA1PRNG}.
	 * <p/>
	 * Please keep in mind that {@link java.security.SecureRandom} uses {@code /dev/random} as default on unix systems
	 * which is a blocking call. It is possible to change this by adding {@code -Djava.security.egd=file:/dev/urandom}
	 * to your application server startup script.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
		 * Checks all existing security providers and returns the best fitting service.
		 *
		 * This method is different to {@link java.security.SecureRandom#getPrngAlgorithm()} which uses the first PRNG
		 * algorithm of the first provider that has registered a SecureRandom implementation.
		 * {@code detectBestFittingService()} instead uses a native PRNG if available, then
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
		 * algorithm of the first provider that has registered a SecureRandom implementation.
		 * {@code detectBestFittingService()} instead uses a native PRNG if available, then
		 * {@code SHA1PRNG} else {@code null} which triggers {@link java.security.SecureRandom#getPrngAlgorithm()}
		 * when calling {@code new SecureRandom()}.
		 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NavigatorLabel.java`
#### Snippet
```java
	 * 
	 * @param id The id
	 * @param pageable {@link org.apache.wicket.markup.html.navigation.paging.IPageableItems}
	 */
	public NavigatorLabel(final String id, final IPageableItems pageable)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/NavigatorLabel.java`
#### Snippet
```java
		 * Construct.
		 * 
		 * @param table {@link org.apache.wicket.markup.html.navigation.paging.IPageableItems}
		 */
		public LabelModelObject(final IPageableItems table)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/AbstractExportableColumn.java`
#### Snippet
```java
/**
 * A helper implementation of {@link IExportableColumn}. This implementation requires you to only
 * implement {@link #getDataModel(org.apache.wicket.model.IModel)}.
 * @author Jesse Long
 * @param <T> The type of each row in the table.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/AbstractExportableColumn.java`
#### Snippet
```java
	 * Populated the data for this column in the row into the {@code cellItem}.
	 * <p>
	 * This implementation adds the {@link Component} returned by {@link #createDisplayComponent(java.lang.String, org.apache.wicket.model.IModel) }
	 * to the cell.
	 * @param cellItem The cell to be populated.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/AbstractExportableColumn.java`
#### Snippet
```java
	 * Populated the data for this column in the row into the {@code cellItem}.
	 * <p>
	 * This implementation adds the {@link Component} returned by {@link #createDisplayComponent(java.lang.String, org.apache.wicket.model.IModel) }
	 * to the cell.
	 * @param cellItem The cell to be populated.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.navigation.paging.IPageableItems#getItemCount()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#onDetach()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.table.filter` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/TextFilteredPropertyColumn.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.table.filter.IFilteredColumn#getFilter(java.lang.String,
	 *      org.apache.wicket.extensions.markup.html.repeater.data.table.filter.FilterForm)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/TextFilteredPropertyColumn.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.table.filter.IFilteredColumn#getFilter(java.lang.String,
	 *      org.apache.wicket.extensions.markup.html.repeater.data.table.filter.FilterForm)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.table.filter` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/TextFilteredPropertyColumn.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.table.filter.IFilteredColumn#getFilter(java.lang.String,
	 *      org.apache.wicket.extensions.markup.html.repeater.data.table.filter.FilterForm)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.table.filter` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/ChoiceFilteredPropertyColumn.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.table.filter.IFilteredColumn#getFilter(java.lang.String,
	 *      org.apache.wicket.extensions.markup.html.repeater.data.table.filter.FilterForm)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/ChoiceFilteredPropertyColumn.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.table.filter.IFilteredColumn#getFilter(java.lang.String,
	 *      org.apache.wicket.extensions.markup.html.repeater.data.table.filter.FilterForm)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.table.filter` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/filter/ChoiceFilteredPropertyColumn.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.markup.html.repeater.data.table.filter.IFilteredColumn#getFilter(java.lang.String,
	 *      org.apache.wicket.extensions.markup.html.repeater.data.table.filter.FilterForm)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater.data` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/ExportToolbar.java`
#### Snippet
```java
		/**
		 * Exports the data from the provided data table to the provided output stream.
		 * This methods calls {@link IDataExporter#exportData(org.apache.wicket.markup.repeater.data.IDataProvider, java.util.List, java.io.OutputStream) }
		 * passing it the {@link IDataProvider} of the {@link DataTable}, and a list of the {@link IExportableColumn}s in the table.
		 *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/ExportToolbar.java`
#### Snippet
```java
		/**
		 * Exports the data from the provided data table to the provided output stream.
		 * This methods calls {@link IDataExporter#exportData(org.apache.wicket.markup.repeater.data.IDataProvider, java.util.List, java.io.OutputStream) }
		 * passing it the {@link IDataProvider} of the {@link DataTable}, and a list of the {@link IExportableColumn}s in the table.
		 *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/ExportToolbar.java`
#### Snippet
```java
		/**
		 * Exports the data from the provided data table to the provided output stream.
		 * This methods calls {@link IDataExporter#exportData(org.apache.wicket.markup.repeater.data.IDataProvider, java.util.List, java.io.OutputStream) }
		 * passing it the {@link IDataProvider} of the {@link DataTable}, and a list of the {@link IExportableColumn}s in the table.
		 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.table` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/ExportToolbar.java`
#### Snippet
```java
		/**
		 * Writes the exported data to the output stream. This implementation calls
		 * {@link #exportData(org.apache.wicket.extensions.markup.html.repeater.data.table.DataTable, org.apache.wicket.extensions.markup.html.repeater.data.table.export.IDataExporter, java.io.OutputStream) }.
		 *
		 * @param output
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.table.export` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/ExportToolbar.java`
#### Snippet
```java
		/**
		 * Writes the exported data to the output stream. This implementation calls
		 * {@link #exportData(org.apache.wicket.extensions.markup.html.repeater.data.table.DataTable, org.apache.wicket.extensions.markup.html.repeater.data.table.export.IDataExporter, java.io.OutputStream) }.
		 *
		 * @param output
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/export/ExportToolbar.java`
#### Snippet
```java
		/**
		 * Writes the exported data to the output stream. This implementation calls
		 * {@link #exportData(org.apache.wicket.extensions.markup.html.repeater.data.table.DataTable, org.apache.wicket.extensions.markup.html.repeater.data.table.export.IDataExporter, java.io.OutputStream) }.
		 *
		 * @param output
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.tree` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/NestedTree.java`
#### Snippet
```java
	 *            the expansion state
	 * 
	 * @see org.apache.wicket.extensions.markup.html.repeater.tree.AbstractTree.State
	 */
	public NestedTree(String id, ITreeProvider<T> provider, IModel<? extends Set<T>> state)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SortParam.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/FinishButton.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.WizardButton#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/LastButton.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.WizardButton#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/NextButton.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.WizardButton#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/CancelButton.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.WizardButton#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/Wizard.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizard#getWizardModel()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/Wizard.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModelListener#onActiveStepChanged(org.apache.wicket.extensions.wizard.IWizardStep)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/Wizard.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModelListener#onActiveStepChanged(org.apache.wicket.extensions.wizard.IWizardStep)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/Wizard.java`
#### Snippet
```java
	 * 
	 * @return False
	 * @see org.apache.wicket.Component#isVersioned()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/PreviousButton.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.WizardButton#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModel#last()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModel#reset()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModel#isLastStep(org.apache.wicket.extensions.wizard.IWizardStep)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModel#isLastStep(org.apache.wicket.extensions.wizard.IWizardStep)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModel#next()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModel#previous()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardButton.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.Button#onSubmit()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardModel#isLastStep(org.apache.wicket.extensions.wizard.IWizardStep)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel#allBreadCrumbParticipants()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel#setActive(org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel#setActive(org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel#addListener(org.apache.wicket.extensions.breadcrumb.IBreadCrumbModelListener)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel#addListener(org.apache.wicket.extensions.breadcrumb.IBreadCrumbModelListener)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel#removeListener(org.apache.wicket.extensions.breadcrumb.IBreadCrumbModelListener)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel#removeListener(org.apache.wicket.extensions.breadcrumb.IBreadCrumbModelListener)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/DefaultBreadCrumbsModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel#getActive()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbLink.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.link.Link#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant#onActivate(org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant#onActivate(org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanel.java`
#### Snippet
```java
	 * The participating component == this.
	 * 
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant#getComponent()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.list` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/BreadCrumbBar.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.html.list.ListView#onBeforeRender()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb.panel` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanelFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.panel.IBreadCrumbPanelFactory#create(java.lang.String,
	 *      org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanelFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.panel.IBreadCrumbPanelFactory#create(java.lang.String,
	 *      org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanelFactory.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.breadcrumb.panel.IBreadCrumbPanelFactory#create(java.lang.String,
	 *      org.apache.wicket.extensions.breadcrumb.IBreadCrumbModel)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant#getComponent()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java
	 * component on it's parent with this one.
	 * 
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant#onActivate(org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java
	 * component on it's parent with this one.
	 * 
	 * @see org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant#onActivate(org.apache.wicket.extensions.breadcrumb.IBreadCrumbParticipant)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.breadcrumb` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanelLink.java`
#### Snippet
```java
	 * Uses the set factory for creating a new instance of {@link IBreadCrumbParticipant}.
	 * 
	 * @see org.apache.wicket.extensions.breadcrumb.BreadCrumbLink#getParticipant(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbPanelLink.java`
#### Snippet
```java
	 * Uses the set factory for creating a new instance of {@link IBreadCrumbParticipant}.
	 * 
	 * @see org.apache.wicket.extensions.breadcrumb.BreadCrumbLink#getParticipant(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard.dynamic` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.dynamic.IDynamicWizardStep#isNextAvailable()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard.dynamic` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.dynamic.IDynamicWizardStep#last()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard.dynamic` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.dynamic.IDynamicWizardStep#isLastAvailable()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard.dynamic` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.dynamic.IDynamicWizardStep#previous()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard.dynamic` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/dynamic/DynamicWizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.dynamic.IDynamicWizardStep#isPreviousAvailable()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.metrics` is unnecessary and can be removed
in `wicket-experimental/wicket-metrics/src/main/java/org/apache/wicket/metrics/WicketMetrics.java`
#### Snippet
```java
	 * @throws Throwable
	 *             if there is an exception while execution
	 * @see org.apache.wicket.metrics.WicketMetrics#measureTime(String, ProceedingJoinPoint,
	 *      boolean)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#detachModel()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form.validation` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.html.form.validation.IFormValidator#getDependentFormComponents()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#applyState()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#getHeader(java.lang.String,
	 *      org.apache.wicket.Component, org.apache.wicket.extensions.wizard.IWizard)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#getHeader(java.lang.String,
	 *      org.apache.wicket.Component, org.apache.wicket.extensions.wizard.IWizard)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#getHeader(java.lang.String,
	 *      org.apache.wicket.Component, org.apache.wicket.extensions.wizard.IWizard)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#getHeader(java.lang.String,
	 *      org.apache.wicket.Component, org.apache.wicket.extensions.wizard.IWizard)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form.validation` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.html.form.validation.IFormValidator#validate(org.apache.wicket.markup.html.form.Form)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.html.form.validation.IFormValidator#validate(org.apache.wicket.markup.html.form.Form)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#getView(java.lang.String,
	 *      org.apache.wicket.Component, org.apache.wicket.extensions.wizard.IWizard)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#getView(java.lang.String,
	 *      org.apache.wicket.Component, org.apache.wicket.extensions.wizard.IWizard)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#getView(java.lang.String,
	 *      org.apache.wicket.Component, org.apache.wicket.extensions.wizard.IWizard)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/wizard/WizardStep.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.extensions.wizard.IWizardStep#getView(java.lang.String,
	 *      org.apache.wicket.Component, org.apache.wicket.extensions.wizard.IWizard)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileItem.java`
#### Snippet
```java

    /**
     * Gets an {@link java.io.OutputStream OutputStream} that can be used for storing the contents of the file.
     *
     * @return An {@link java.io.OutputStream OutputStream} that can be used for storing the contents of the file.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileItem.java`
#### Snippet
```java
     * Gets an {@link java.io.OutputStream OutputStream} that can be used for storing the contents of the file.
     *
     * @return An {@link java.io.OutputStream OutputStream} that can be used for storing the contents of the file.
     *
     * @throws IOException if an error occurs.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileItem.java`
#### Snippet
```java

    /**
     * Gets an {@link java.io.InputStream InputStream} that can be used to retrieve the contents of the file.
     *
     * @return An {@link java.io.InputStream InputStream} that can be used to retrieve the contents of the file.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileItem.java`
#### Snippet
```java
     * Gets an {@link java.io.InputStream InputStream} that can be used to retrieve the contents of the file.
     *
     * @return An {@link java.io.InputStream InputStream} that can be used to retrieve the contents of the file.
     *
     * @throws IOException if an error occurs.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileItem.java`
#### Snippet
```java
 * </p>
 * <p>
 * After retrieving an instance of this class from a {@link org.apache.wicket.commons.fileupload2.FileUpload FileUpload} instance (see
 * {@link org.apache.wicket.commons.fileupload2.jaksrvlt.JakSrvltFileUpload
 * #parseRequest(javax.servlet.http.HttpServletRequest)}), you may either request all contents
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileItem.java`
#### Snippet
```java
 * {@link org.apache.wicket.commons.fileupload2.jaksrvlt.JakSrvltFileUpload
 * #parseRequest(javax.servlet.http.HttpServletRequest)}), you may either request all contents
 * of the file at once using {@link #get()} or request an {@link java.io.InputStream InputStream} with {@link #getInputStream()} and process the file without
 * attempting to load it into memory, which may come handy with large files.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/FileUpload.java`
#### Snippet
```java
 * <a href="http://www.ietf.org/rfc/rfc1867.txt">RFC 1867</a>.  Use {@link
 * #parseRequest(RequestContext)} to acquire a list
 * of {@link org.apache.wicket.commons.fileupload2.FileItem FileItems} associated
 * with a given HTML widget.</p>
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2.disk` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItemFactory.java`
#### Snippet
```java

    /**
     * Creates a new {@link org.apache.wicket.commons.fileupload2.disk.DiskFileItem}
     * instance from the supplied parameters and the local factory
     * configuration.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItemFactory.java`
#### Snippet
```java
     *
     * @return The directory in which temporary files will be located.
     * @see #setRepository(java.io.File)
     */
    public File getRepository() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItemFactory.java`
#### Snippet
```java

/**
 * The default {@link org.apache.wicket.commons.fileupload2.FileItemFactory}
 * implementation.
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItemFactory.java`
#### Snippet
```java
 * <p>
 * This implementation creates
 * {@link org.apache.wicket.commons.fileupload2.FileItem} instances which keep their
 * content either in memory, for smaller items, or in a temporary file on disk,
 * for larger items. The size threshold, above which content will be stored on
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItemFactory.java`
#### Snippet
```java
 * then you must consider the following: Temporary files are automatically
 * deleted as soon as they are no longer needed. (More precisely, when the
 * corresponding instance of {@link java.io.File} is garbage collected.)
 * This is done by the so-called reaper thread, which is started and stopped
 * automatically by the {@link FileCleaningTracker} when there are files to be
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/LimitedInputStream.java`
#### Snippet
```java
     *             stream is reached.
     * @throws  IOException  if an I/O error occurs.
     * @see        java.io.FilterInputStream#in
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/LimitedInputStream.java`
#### Snippet
```java
     *
     * @throws  IOException  if an I/O error occurs.
     * @see        java.io.FilterInputStream#in
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/util/LimitedInputStream.java`
#### Snippet
```java
     * {@code b.length - off}
     * @throws  IOException  if an I/O error occurs.
     * @see        java.io.FilterInputStream#in
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/AbstractFileUpload.java`
#### Snippet
```java
 * <a href="http://www.ietf.org/rfc/rfc1867.txt">RFC 1867</a>.  Use {@link
 * #parseRequest(RequestContext)} to acquire a list of {@link
 * org.apache.wicket.commons.fileupload2.FileItem}s associated with a given HTML
 * widget.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/impl/FileItemIteratorImpl.java`
#### Snippet
```java
     * Returns the next available {@link FileItemStream}.
     *
     * @throws java.util.NoSuchElementException No more items are available. Use {@link #hasNext()} to prevent this exception.
     * @throws FileUploadException              Parsing or processing the file item failed.
     * @throws IOException                      Reading the file item failed.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/jaksrvlt/JakSrvltFileUpload.java`
#### Snippet
```java
 * <a href="http://www.ietf.org/rfc/rfc1867.txt">RFC 1867</a>.  Use {@link
 * #parseRequest(HttpServletRequest)} to acquire a list of {@link
 * org.apache.wicket.commons.fileupload2.FileItem}s associated with a given HTML
 * widget.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authroles.authorization.strategies.role.annotations` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/annotations/AuthorizeResource.java`
#### Snippet
```java
/**
 * Annotation for configuring what roles are allowed for requesting the annotated resource. It works analogously
 * to {@link org.apache.wicket.authroles.authorization.strategies.role.annotations.AuthorizeInstantiation}.
 *
 * @author Carl-Eric Menzel
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
/**
 * The default implementation of the
 * {@link org.apache.wicket.commons.fileupload2.FileItem FileItem} interface.
 *
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
 * #parseRequest(javax.servlet.http.HttpServletRequest)}), you may
 * either request all contents of file at once using {@link #get()} or
 * request an {@link java.io.InputStream InputStream} with
 * {@link #getInputStream()} and process the file without attempting to load
 * it into memory, which may come handy with large files.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
 * then you must consider the following: Temporary files are automatically
 * deleted as soon as they are no longer needed. (More precisely, when the
 * corresponding instance of {@link java.io.File} is garbage collected.)
 * This is done by the so-called reaper thread, which is started and stopped
 * automatically by the {@link org.apache.commons.io.FileCleaningTracker} when
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
     *
     * @return The original file name in the client's file system.
     * @throws org.apache.wicket.commons.fileupload2.InvalidFileNameException The file name contains a NUL character,
     *   which might be an indicator of a security attack. If you intend to
     *   use the file name anyways, catch the exception and use
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.commons.fileupload2` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
     *   which might be an indicator of a security attack. If you intend to
     *   use the file name anyways, catch the exception and use
     *   {@link org.apache.wicket.commons.fileupload2.InvalidFileNameException#getName()}.
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java

    /**
     * Gets the {@link java.io.File} object for the {@code FileItem}'s
     * data's temporary location on the disk. Note that for
     * {@code FileItem}s that have their data stored in memory,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
     * {@code FileItem}s that have their data stored in memory,
     * this method will return {@code null}. When handling large
     * files, you can use {@link java.io.File#renameTo(java.io.File)} to
     * move the file to new location without copying the data, if the
     * source and destination locations reside within the same logical
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
     * {@code FileItem}s that have their data stored in memory,
     * this method will return {@code null}. When handling large
     * files, you can use {@link java.io.File#renameTo(java.io.File)} to
     * move the file to new location without copying the data, if the
     * source and destination locations reside within the same logical
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java

    /**
     * Gets an {@link java.io.OutputStream OutputStream} that can
     * be used for storing the contents of the file.
     *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
     * be used for storing the contents of the file.
     *
     * @return An {@link java.io.OutputStream OutputStream} that can be used
     *         for storing the contents of the file.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java

    /**
     * Creates and returns a {@link java.io.File File} representing a uniquely
     * named temporary file in the configured repository path. The lifetime of
     * the file is tied to the lifetime of the {@code FileItem} instance;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
     * same File each time.</b>
     * </p>
     * @return The {@link java.io.File File} to be used for temporary storage.
     */
    protected File getTempFile() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java

    /**
     * Gets an {@link java.io.InputStream InputStream} that can be
     * used to retrieve the contents of the file.
     *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-commons-fileupload/src/main/java/org/apache/wicket/commons/fileupload2/disk/DiskFileItem.java`
#### Snippet
```java
     * used to retrieve the contents of the file.
     *
     * @return An {@link java.io.InputStream InputStream} that can be
     *         used to retrieve the contents of the file.
     * @throws IOException if an error occurs.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/Roles.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/annotations/AnnotationsRoleAuthorizationStrategy.java`
#### Snippet
```java
	
	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/annotations/AnnotationsRoleAuthorizationStrategy.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/annotations/AnnotationsRoleAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isInstantiationAuthorized(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/MetaDataRoleAuthorizationStrategy.java`
#### Snippet
```java
	 * Uses application level meta data to match roles for component instantiation.
	 * 
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isInstantiationAuthorized(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/MetaDataRoleAuthorizationStrategy.java`
#### Snippet
```java
 * for doing role based authorization for component instantation.
 * 
 * @see org.apache.wicket.MetaDataKey
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/MetaDataRoleAuthorizationStrategy.java`
#### Snippet
```java
	 * Uses component level meta data to match roles for component action execution.
	 * 
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-auth-roles/src/main/java/org/apache/wicket/authroles/authorization/strategies/role/metadata/MetaDataRoleAuthorizationStrategy.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/IPageFactory.java`
#### Snippet
```java
 * constructor bubble up.
 * 
 * @see org.apache.wicket.Application#newPageFactory()
 * @see Session#getPageFactory()
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/IResourceFactory.java`
#### Snippet
```java
	 *            The locale for the resource
	 * @param style
	 *            The style of the resource (see {@link org.apache.wicket.Session})
	 * @param variation
	 *            The component's variation
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/MetaDataEntry.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/MetaDataKey.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/SharedResources.java`
#### Snippet
```java
	/**
	 * Resolves a {@link ResourceReference} for a shared resource by using
	 * {@link org.apache.wicket.Application} as a scope and {@code null} for
	 * locale, style and variation.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/SharedResources.java`
#### Snippet
```java
	 *            The locale of the resource
	 * @param style
	 *            The resource style (see {@link org.apache.wicket.Session})
	 * @param variation
	 *            The component specific variation of the style
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/SharedResources.java`
#### Snippet
```java
	 *            The locale of the resource
	 * @param style
	 *            The resource style (see {@link org.apache.wicket.Session})
	 * @param variation
	 *            The component specific variation of the style
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java
		set(Date.class, new DateConverter());
		set(java.sql.Date.class, new SqlDateConverter());
		set(java.sql.Time.class, new SqlTimeConverter());
		set(java.sql.Timestamp.class, new SqlTimestampConverter());
		set(Calendar.class, new CalendarConverter());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java
		set(java.sql.Date.class, new SqlDateConverter());
		set(java.sql.Time.class, new SqlTimeConverter());
		set(java.sql.Timestamp.class, new SqlTimestampConverter());
		set(Calendar.class, new CalendarConverter());
		set(LocalDate.class, new LocalDateConverter());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java
	 * @return The converter for the given type
	 * 
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(String, java.util.Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ConverterLocator.java`
#### Snippet
```java
	 * @return The converter for the given type
	 * 
	 * @see org.apache.wicket.util.convert.IConverter#convertToObject(String, java.util.Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/WicketRuntimeException.java`
#### Snippet
```java

	/**
	 * @see RuntimeException#RuntimeException(java.lang.String, java.lang.Throwable)
	 */
	public WicketRuntimeException(final String message, final Throwable cause)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/WicketRuntimeException.java`
#### Snippet
```java

	/**
	 * @see RuntimeException#RuntimeException(java.lang.String, java.lang.Throwable)
	 */
	public WicketRuntimeException(final String message, final Throwable cause)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/WicketRuntimeException.java`
#### Snippet
```java

	/**
	 * @see RuntimeException#RuntimeException(java.lang.Throwable)
	 */
	public WicketRuntimeException(final Throwable cause)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/WicketRuntimeException.java`
#### Snippet
```java

	/**
	 * @see RuntimeException#RuntimeException(java.lang.String)
	 */
	public WicketRuntimeException(final String message)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.application` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
		{
			/**
			 * @see org.apache.wicket.application.IComponentInstantiationListener#onInstantiation(org.apache.wicket.Component)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
		{
			/**
			 * @see org.apache.wicket.application.IComponentInstantiationListener#onInstantiation(org.apache.wicket.Component)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.session` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.session.ISessionStore.UnboundListener#sessionUnbound(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.session.ISessionStore.UnboundListener#sessionUnbound(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Application.java`
#### Snippet
```java
 * </ul>
 * 
 * @see org.apache.wicket.protocol.http.WebApplication
 * @author Jonathan Locke
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/UrlResourceReferenceMapper.java`
#### Snippet
```java
/**
 * A request mapper that is used to create Url out of UrlResourceReference.
 * UrlResourceReference works with {@link org.apache.wicket.request.resource.UrlResourceReference.CalculatedUrl} and
 * thus this mapper should not use {@link org.apache.wicket.SystemMapper.ParentFolderPlaceholderProvider}
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/UrlResourceReferenceMapper.java`
#### Snippet
```java
 * A request mapper that is used to create Url out of UrlResourceReference.
 * UrlResourceReference works with {@link org.apache.wicket.request.resource.UrlResourceReference.CalculatedUrl} and
 * thus this mapper should not use {@link org.apache.wicket.SystemMapper.ParentFolderPlaceholderProvider}
 */
public class UrlResourceReferenceMapper implements IRequestMapper
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/IQueueRegion.java`
#### Snippet
```java
	 * Starts component dequeueing on this {@link IQueueRegion}. This is the entry point into the
	 * dequeuing process, it creates the {@link DequeueContext} and delegates the operation to the
	 * {@link org.apache.wicket.MarkupContainer#dequeue(DequeueContext)} method which performs the
	 * actual dequeueing. The context's markup is retrieved using the {@link MarkupContainer#getAssociatedMarkup()}.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Initializer.java`
#### Snippet
```java
{
	/**
	 * @see org.apache.wicket.IInitializer#init(org.apache.wicket.Application)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Initializer.java`
#### Snippet
```java
{
	/**
	 * @see org.apache.wicket.IInitializer#init(org.apache.wicket.Application)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Initializer.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Initializer.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.IInitializer#destroy(org.apache.wicket.Application)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Initializer.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.IInitializer#destroy(org.apache.wicket.Application)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/PageReference.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/PageReference.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/PageReference.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#hashCode()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	 * THIS METHOD IS NOT PART OF THE WICKET PUBLIC API. DO NOT CALL OR OVERRIDE.
	 * 
	 * @see org.apache.wicket.Component#internalOnModelChanged()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
 * </ul>
 * 
 * @see org.apache.wicket.markup.html.WebPage
 * @see org.apache.wicket.MarkupContainer
 * @see org.apache.wicket.model.CompoundPropertyModel
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.markup.html.WebPage
 * @see org.apache.wicket.MarkupContainer
 * @see org.apache.wicket.model.CompoundPropertyModel
 * @see org.apache.wicket.Component
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
 * @see org.apache.wicket.MarkupContainer
 * @see org.apache.wicket.model.CompoundPropertyModel
 * @see org.apache.wicket.Component
 * 
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	 * bookmarkable.
	 * 
	 * @see org.apache.wicket.Component#getStatelessHint()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.component` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	 * Checks if this page was created using one of its bookmarkable constructors
	 * 
	 * @see org.apache.wicket.request.component.IRequestablePage#wasCreatedBookmarkable()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestTarget.java`
#### Snippet
```java
	/**
	 * Register the given respond listener. The listener's
	 * {@link org.apache.wicket.ajax.AjaxRequestTarget.ITargetRespondListener#onTargetRespond} method will be invoked when
	 * the {@link AjaxRequestTarget} starts to respond.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java

	/**
	 * Set the style (see {@link org.apache.wicket.Session}).
	 * 
	 * @param style
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	/**
	 * A callback method that is executed when the user session is invalidated
	 * either by explicit call to {@link org.apache.wicket.Session#invalidate()}
	 * or due to HttpSession expiration.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java

	/**
	 * Get the style (see {@link org.apache.wicket.Session}).
	 * 
	 * @return Returns the style (see {@link org.apache.wicket.Session})
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Session.java`
#### Snippet
```java
	 * Get the style (see {@link org.apache.wicket.Session}).
	 * 
	 * @return Returns the style (see {@link org.apache.wicket.Session})
	 */
	public final String getStyle()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.settings` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
 * database. There should be hardly any need to extend Localizer.
 * 
 * @see org.apache.wicket.settings.ResourceSettings#getLocalizer()
 * @see org.apache.wicket.resource.loader.IStringResourceLoader
 * @see org.apache.wicket.settings.ResourceSettings#getStringResourceLoaders()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource.loader` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.settings.ResourceSettings#getLocalizer()
 * @see org.apache.wicket.resource.loader.IStringResourceLoader
 * @see org.apache.wicket.settings.ResourceSettings#getStringResourceLoaders()
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.settings` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java
 * @see org.apache.wicket.settings.ResourceSettings#getLocalizer()
 * @see org.apache.wicket.resource.loader.IStringResourceLoader
 * @see org.apache.wicket.settings.ResourceSettings#getStringResourceLoaders()
 * 
 * @author Chris Turner
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxClientInfoBehavior.java`
#### Snippet
```java
 * WebClientInfo's ClientProperties by using Ajax
 *
 * @see #onClientInfo(AjaxRequestTarget, org.apache.wicket.protocol.http.request.WebClientInfo)
 */
public class AjaxClientInfoBehavior extends AbstractAjaxTimerBehavior
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#hashCode()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.core.request.handler.IPageRequestHandler#detach(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AjaxRequestHandler.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.core.request.handler.IPageRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/OnChangeAjaxBehavior.java`
#### Snippet
```java
 * 
 * @since 1.3
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/OnChangeAjaxBehavior.java`
#### Snippet
```java
 * 
 * @since 1.3
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/OnChangeAjaxBehavior.java`
#### Snippet
```java
 * @since 1.3
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 */
public abstract class OnChangeAjaxBehavior extends AjaxFormComponentUpdatingBehavior
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/OnChangeAjaxBehavior.java`
#### Snippet
```java
 * @since 1.3
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 */
public abstract class OnChangeAjaxBehavior extends AjaxFormComponentUpdatingBehavior
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormComponentUpdatingBehavior.java`
#### Snippet
```java
 * 
 * @author Igor Vaynberg (ivaynberg)
 * @see #onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormComponentUpdatingBehavior.java`
#### Snippet
```java
 * @author Igor Vaynberg (ivaynberg)
 * @see #onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 */
public abstract class AjaxFormComponentUpdatingBehavior extends AjaxEventBehavior
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormComponentUpdatingBehavior.java`
#### Snippet
```java
	/**
	 * Gives the control to the application to decide whether the form component model should
	 * be updated automatically or not. Make sure to call {@link org.apache.wicket.markup.html.form.FormComponent#valid()}
	 * additionally in case the application want to update the model manually.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormComponentUpdatingBehavior.java`
#### Snippet
```java
	/**
	 * Called to handle any error resulting from updating form component. Errors thrown from
	 * {@link #onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)} will not be caught here.
	 *
	 * The RuntimeException will be null if it was just a validation or conversion error of the
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
 * @author svenmeier
 *
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 * @see RadioChoice
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
 * @author svenmeier
 *
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 * @see RadioChoice
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
 *
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 * @see RadioChoice
 * @see CheckBoxMultipleChoice
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormChoiceComponentUpdatingBehavior.java`
#### Snippet
```java
 *
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onUpdate(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see org.apache.wicket.ajax.form.AjaxFormComponentUpdatingBehavior#onError(org.apache.wicket.ajax.AjaxRequestTarget, RuntimeException)
 * @see RadioChoice
 * @see CheckBoxMultipleChoice
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
 * 
 * @author Igor Vaynberg (ivaynberg)
 * @see #onSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onAfterSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onError(org.apache.wicket.ajax.AjaxRequestTarget)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
 * @author Igor Vaynberg (ivaynberg)
 * @see #onSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onAfterSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onError(org.apache.wicket.ajax.AjaxRequestTarget)
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
 * @see #onSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onAfterSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onError(org.apache.wicket.ajax.AjaxRequestTarget)
 */
public class AjaxFormValidatingBehavior extends Behavior
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java

	/**
	 * @return the bound component if it implements {@link org.apache.wicket.markup.html.form.IFormSubmittingComponent},
	 * otherwise - {@code null}
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java
 * 
 * @author Igor Vaynberg (ivaynberg)
 * @see #onSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onAfterSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onError(org.apache.wicket.ajax.AjaxRequestTarget)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java
 * @author Igor Vaynberg (ivaynberg)
 * @see #onSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onAfterSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onError(org.apache.wicket.ajax.AjaxRequestTarget)
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormSubmitBehavior.java`
#### Snippet
```java
 * @see #onSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onAfterSubmit(org.apache.wicket.ajax.AjaxRequestTarget)
 * @see #onError(org.apache.wicket.ajax.AjaxRequestTarget)
 */
public abstract class AjaxFormSubmitBehavior extends AjaxEventBehavior
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigation.java`
#### Snippet
```java
 * Please
 * 
 * @see org.apache.wicket.markup.html.navigation.paging.PagingNavigation
 * 
 * @since 1.2
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.behavior` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.behavior.AbstractAjaxBehavior#renderHead(Component,
	 *      org.apache.wicket.markup.head.IHeaderResponse)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.behavior.AbstractAjaxBehavior#renderHead(Component,
	 *      org.apache.wicket.markup.head.IHeaderResponse)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.behavior` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/AbstractDefaultAjaxBehavior.java`
#### Snippet
```java
	 * Subclasses should call super.onBind()
	 * 
	 * @see org.apache.wicket.behavior.AbstractAjaxBehavior#onBind()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigator.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.navigation.paging.PagingNavigator#newNavigation(java.lang.String,
	 *      org.apache.wicket.markup.html.navigation.paging.IPageable,
	 *      org.apache.wicket.markup.html.navigation.paging.IPagingLabelProvider)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigator.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.navigation.paging.PagingNavigator#newNavigation(java.lang.String,
	 *      org.apache.wicket.markup.html.navigation.paging.IPageable,
	 *      org.apache.wicket.markup.html.navigation.paging.IPagingLabelProvider)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigator.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.markup.html.navigation.paging.PagingNavigator#newNavigation(java.lang.String,
	 *      org.apache.wicket.markup.html.navigation.paging.IPageable,
	 *      org.apache.wicket.markup.html.navigation.paging.IPagingLabelProvider)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigator.java`
#### Snippet
```java
	 * @see org.apache.wicket.markup.html.navigation.paging.PagingNavigator#newNavigation(java.lang.String,
	 *      org.apache.wicket.markup.html.navigation.paging.IPageable,
	 *      org.apache.wicket.markup.html.navigation.paging.IPagingLabelProvider)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax.attributes` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/form/AjaxCheckBox.java`
#### Snippet
```java
	 * @param attributes
	 *      the attributes to use for the Ajax request
	 * @see org.apache.wicket.ajax.AbstractDefaultAjaxBehavior#updateAjaxAttributes(org.apache.wicket.ajax.attributes.AjaxRequestAttributes)
	 */
	protected void updateAjaxAttributes(final AjaxRequestAttributes attributes)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/IAjaxCallListener.java`
#### Snippet
```java
	 * <strong>Note</strong>: if the Ajax call is synchronous (see
	 * {@link AjaxRequestAttributes#setAsynchronous(boolean)}) then this JavaScript will be executed
	 * after the {@linkplain #getCompleteHandler(org.apache.wicket.Component) complete handler},
	 * otherwise it is executed right after the execution of the Ajax request.
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationBehavior.java`
#### Snippet
```java
	 * contents can be replaced with the newly generated pageable.
	 * 
	 * @see org.apache.wicket.ajax.AjaxEventBehavior#onEvent(org.apache.wicket.ajax.AjaxRequestTarget)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationBehavior.java`
#### Snippet
```java
	 * contents can be replaced with the newly generated pageable.
	 * 
	 * @see org.apache.wicket.ajax.AjaxEventBehavior#onEvent(org.apache.wicket.ajax.AjaxRequestTarget)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationBehavior.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.ajax.AjaxEventBehavior#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/markup/html/navigation/paging/AjaxPagingNavigationBehavior.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.ajax.AjaxEventBehavior#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax.attributes` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/ajax/attributes/AjaxAttributeName.java`
#### Snippet
```java
	 * stop propagation
	 *
	 * @see AjaxRequestAttributes#setEventPropagation(org.apache.wicket.ajax.attributes.AjaxRequestAttributes.EventPropagation)
	 */
	EVENT_PROPAGATION("sp"),
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.file` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/file/WebApplicationPath.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.util.file.IResourceFinder#find(Class, String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/file/WebApplicationPath.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/AbstractJceCrypt.java`
#### Snippet
```java
		try
		{
			byte[] decoded = java.util.Base64.getUrlDecoder().decode(text);
			return new String(decrypt(decoded), StandardCharsets.UTF_8);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.crypt` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/AbstractKeyInSessionCryptFactory.java`
#### Snippet
```java

	/**
	 * @return the {@link org.apache.wicket.util.crypt.ICrypt} to use
	 */
	protected abstract ICrypt createCrypt(T key);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.crypt` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/KeyInSessionSunJceCryptFactory.java`
#### Snippet
```java

	/**
	 * Constructor using {@link javax.crypto.Cipher} {@value org.apache.wicket.util.crypt.SunJceCrypt#DEFAULT_CRYPT_METHOD}
	 */
	public KeyInSessionSunJceCryptFactory()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/DifferentPageChecker.java`
#### Snippet
```java
/**
 * An implementation of {@link IObjectChecker} that returns a failure
 * result when the checked object is a second {@link org.apache.wicket.Page} instance
 * in the component tree. The first checked page instance is assumed to be the component
 * tree root. Any other page instance checked by the same checker is a referenced page
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.util.objects.checker` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/SessionChecker.java`
#### Snippet
```java

/**
 * An implementation of {@link org.apache.wicket.core.util.objects.checker.IObjectChecker} that returns a failure
 * result when the checked object is a {@link org.apache.wicket.Session}.
 * Keeping a reference to the session instance in the component tree is an error.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/SessionChecker.java`
#### Snippet
```java
/**
 * An implementation of {@link org.apache.wicket.core.util.objects.checker.IObjectChecker} that returns a failure
 * result when the checked object is a {@link org.apache.wicket.Session}.
 * Keeping a reference to the session instance in the component tree is an error.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/ObjectSerializationChecker.java`
#### Snippet
```java
/**
 * An implementation of IObjectChecker that checks whether the object
 * implements {@link java.io.Serializable} interface
 *
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.util.objects.checker` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/ObjectSerializationChecker.java`
#### Snippet
```java
	 * @param object
	 *      the object to check
	 * @return the {@link org.apache.wicket.core.util.objects.checker.IObjectChecker.Result#SUCCESS} if the object can be serialized.
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.util.resource.locator` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/OsgiResourceStreamLocator.java`
#### Snippet
```java
	/**
	 *
	 * @see org.apache.wicket.core.util.resource.locator.ResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/OsgiResourceStreamLocator.java`
#### Snippet
```java
	/**
	 *
	 * @see org.apache.wicket.core.util.resource.locator.ResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/OsgiResourceStreamLocator.java`
#### Snippet
```java
	 *
	 * @see org.apache.wicket.core.util.resource.locator.ResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/LocaleResourceNameIterator.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#next()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/LocaleResourceNameIterator.java`
#### Snippet
```java
	/**
	 *
	 * @see java.util.Iterator#hasNext()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/LocaleResourceNameIterator.java`
#### Snippet
```java
	/**
	 *
	 * @see java.util.Iterator#remove()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java
	 * The return value will always be null. Use getStyle() and getVariation() instead.
	 *
	 * @see java.util.Iterator#next()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java
	/**
	 *
	 * @see java.util.Iterator#remove()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/StyleAndVariationResourceNameIterator.java`
#### Snippet
```java
	/**
	 *
	 * @see java.util.Iterator#hasNext()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/IResourceStreamLocator.java`
#### Snippet
```java
	 * clients if they are not interested in a lookup that takes the style and locale into account,
	 * or it is called by the implementation of
	 * {@link #locate(Class, String, String, String, java.util.Locale, String, boolean)} where the
	 * latter just takes care of trying out the different combinations for the provided style and
	 * locale and uses this method to actually load the resource stream.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceNameIterator.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
				try
				{
					writeObjectMethod = cls.getDeclaredMethod("writeObject", java.io.ObjectOutputStream.class);
				}
				catch (SecurityException | NoSuchMethodException e)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java

	/**
	 * @see java.io.ObjectOutputStream#writeObjectOverride(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java

	/**
	 * @see java.io.ObjectOutputStream#writeObjectOverride(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java

	/**
	 * @see java.io.ObjectOutputStream#reset()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.util.objects.checker` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java
 * An ObjectOutputStream that uses {@link IObjectChecker IObjectChecker}s to check the
 * state of the object before serializing it. If the checker returns
 * {@link org.apache.wicket.core.util.objects.checker.IObjectChecker.Result.Status#FAILURE}
 * then the serialization process is stopped and the error is logged.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.util.resource.locator` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.util.resource.locator` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#newResourceNameIterator(java.lang.String,
	 *      java.util.Locale, java.lang.String, java.lang.String, java.lang.String, boolean)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#newResourceNameIterator(java.lang.String,
	 *      java.util.Locale, java.lang.String, java.lang.String, java.lang.String, boolean)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#newResourceNameIterator(java.lang.String,
	 *      java.util.Locale, java.lang.String, java.lang.String, java.lang.String, boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#newResourceNameIterator(java.lang.String,
	 *      java.util.Locale, java.lang.String, java.lang.String, java.lang.String, boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#newResourceNameIterator(java.lang.String,
	 *      java.util.Locale, java.lang.String, java.lang.String, java.lang.String, boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#newResourceNameIterator(java.lang.String,
	 *      java.util.Locale, java.lang.String, java.lang.String, java.lang.String, boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.util.resource.locator` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String, java.lang.String, java.lang.String, java.util.Locale,
	 *      java.lang.String, boolean)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String, java.lang.String, java.lang.String, java.util.Locale,
	 *      java.lang.String, boolean)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String, java.lang.String, java.lang.String, java.util.Locale,
	 *      java.lang.String, boolean)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String, java.lang.String, java.lang.String, java.util.Locale,
	 *      java.lang.String, boolean)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String, java.lang.String, java.lang.String, java.util.Locale,
	 *      java.lang.String, boolean)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String, java.lang.String, java.lang.String, java.util.Locale,
	 *      java.lang.String, boolean)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/util/resource/locator/ResourceStreamLocator.java`
#### Snippet
```java
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator#locate(java.lang.Class,
	 *      java.lang.String, java.lang.String, java.lang.String, java.util.Locale,
	 *      java.lang.String, boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
	 *            resource reference that should be linked to the mount path
	 *
	 * @see #ResourceMapper(String, org.apache.wicket.request.resource.ResourceReference,
	 *      org.apache.wicket.request.mapper.parameter.IPageParametersEncoder)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.mapper.parameter` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
	 *
	 * @see #ResourceMapper(String, org.apache.wicket.request.resource.ResourceReference,
	 *      org.apache.wicket.request.mapper.parameter.IPageParametersEncoder)
	 */
	public ResourceMapper(String path, ResourceReference resourceReference)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/ResourceMapper.java`
#### Snippet
```java
 *
 * @see org.apache.wicket.protocol.http.WebApplication#mountResource(String,
 *      org.apache.wicket.request.resource.ResourceReference)
 *
 * @author Peter Ertl
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#mapHandler(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#mapHandler(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#getCompatibilityScore(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#getCompatibilityScore(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#mapRequest(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/BufferedResponseMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#mapRequest(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/StalePageException.java`
#### Snippet
```java
	 * Suppress loading of the stacktrace because it is not needed.
	 *
	 * @see java.lang.Throwable#fillInStackTrace()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#mapRequest(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java
	/**
	 * Matches when the request url starts with
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
	 * or when the base url starts with
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java
	/**
	 * Matches when the request url starts with
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
	 * or when the base url starts with
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
	 * or when the base url starts with
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
	 * and the request url with {@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
	 * or when the base url starts with
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
	 * and the request url with {@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java
	 * or when the base url starts with
	 * {@link org.apache.wicket.core.request.mapper.IMapperContext#getNamespace()}/{@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}
	 * and the request url with {@link org.apache.wicket.core.request.mapper.IMapperContext#getPageIdentifier()}

	 * @param request
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#mapHandler(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/PageInstanceMapper.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestMapper#getCompatibilityScore(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	 * for itself and for all its children.
	 * 
	 * @see org.apache.wicket.Component#fireInitialize()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.Component#internalMarkRendering(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/HomePageMapper.java`
#### Snippet
```java
	 * Use this mapper as a last option. Let all other mappers to try to handle the request
	 *
	 * @see MountedMapper#getCompatibilityScore(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/HomePageMapper.java`
#### Snippet
```java
	 * Matches only when there are no segments/indexed parameters
	 *
	 * @see AbstractBookmarkableMapper#parseRequest(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
 * <p>
 * <strong>Important</strong>: for better security it is recommended to use
 * {@link org.apache.wicket.core.request.mapper.CryptoMapper#CryptoMapper(IRequestMapper, Supplier)}
 * constructor with {@link org.apache.wicket.util.crypt.ICrypt} implementation that generates a
 * separate key for each user. {@link org.apache.wicket.core.util.crypt.KeyInSessionSunJceCryptFactory} provides such an
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.crypt` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
 * <strong>Important</strong>: for better security it is recommended to use
 * {@link org.apache.wicket.core.request.mapper.CryptoMapper#CryptoMapper(IRequestMapper, Supplier)}
 * constructor with {@link org.apache.wicket.util.crypt.ICrypt} implementation that generates a
 * separate key for each user. {@link org.apache.wicket.core.util.crypt.KeyInSessionSunJceCryptFactory} provides such an
 * implementation that stores the key in the HTTP session.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.crypt` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java
 * @author Jesse Long
 * @author svenmeier
 * @see org.apache.wicket.settings.SecuritySettings#setCryptFactory(org.apache.wicket.util.crypt.ICryptFactory)
 * @see org.apache.wicket.core.util.crypt.KeyInSessionSunJceCryptFactory
 * @see org.apache.wicket.util.crypt.SunJceCrypt
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/CryptoMapper.java`
#### Snippet
```java

	/**
	 * Decrypts an entire URL, which was previously encrypted by {@link #encryptEntireUrl(org.apache.wicket.request.Url)}.
	 * This method should return {@code null} if the URL is not decryptable.
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/MountedMapper.java`
#### Snippet
```java

	/**
	 * @see AbstractBookmarkableMapper#checkPageClass(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.handler` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/AbstractPartialPageRequestHandler.java`
#### Snippet
```java

    /**
     * @see org.apache.wicket.core.request.handler.IPageRequestHandler#getPage()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.handler` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/PageAndComponentProvider.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.core.request.handler.IPageAndComponentProvider#getComponentPath()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * This method is called whenever a component is re-added to the page's component tree, if it
	 * had been removed at some earlier time, i.e., if it is already initialized
	 * (see {@link org.apache.wicket.Component#isInitialized()}).
	 *
	 * This is similar to onInitialize, but only comes after the component has been removed and
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * <p>
	 * NOT intended for overriding by framework clients. Rather, use
	 * {@link Component#renderHead(org.apache.wicket.markup.head.IHeaderResponse)}
	 * </p>
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.mapper.parameter` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * @param <C>
	 * 
	 * @see RequestCycle#urlFor(Class, org.apache.wicket.request.mapper.parameter.PageParameters)
	 * 
	 * @param pageClass
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.component` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 *            The response page
	 * 
	 * @see RequestCycle#setResponsePage(org.apache.wicket.request.component.IRequestablePage)
	 */
	public final void setResponsePage(final IRequestablePage page)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * @return The style of this component respectively the style of the Session.
	 * 
	 * @see org.apache.wicket.Session#getStyle()
	 */
	public final String getStyle()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
 * delegates to its parent's locale, finally consulting the {@link Session}'s locale.</li>
 * <li><b>Style </b>- The Session's style ("skin") is available through
 * {@link org.apache.wicket.Component#getStyle()}. Styles are intended to give a particular look to
 * all components or resources in a session that is independent of its Locale. For example, a style
 * might be a set of resources, including images and markup files, which gives the design look of
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
 * <li><b>Feedback Messages </b>- The {@link Component#debug(Serializable)},
 * {@link Component#info(Serializable)}, {@link Component#warn(Serializable)},
 * {@link Component#error(java.io.Serializable)} and {@link Component#fatal(Serializable)} methods
 * associate feedback messages with a Component. It is generally not necessary to use these methods
 * directly since Wicket validators automatically register feedback messages on Components. Feedback
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/Component.java`
#### Snippet
```java
	 * super.onBeforeRender().
	 *
	 * @see org.apache.wicket.MarkupContainer#addOrReplace(Component...) 
	 */
	protected void onBeforeRender()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.cookies` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/cookies/CookieDefaults.java`
#### Snippet
```java

/**
 * This class provides default values that are used by {@link org.apache.wicket.util.cookies.CookieUtils}
 * class when it creates cookies.
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplate.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.resource.AbstractStringResourceStream#asString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.template` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/template/JavaScriptTemplate.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.template.TextTemplateDecorator#getBeforeTemplateContents()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.template` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/template/JavaScriptTemplate.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.template.TextTemplateDecorator#getAfterTemplateContents()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.template` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplateDecorator.java`
#### Snippet
```java
	 * @return the contents decorated with {@link #getBeforeTemplateContents()} and
	 *         {@link #getAfterTemplateContents()}.
	 * @see org.apache.wicket.util.template.TextTemplate#asString(java.util.Map)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplateDecorator.java`
#### Snippet
```java
	 * @return the contents decorated with {@link #getBeforeTemplateContents()} and
	 *         {@link #getAfterTemplateContents()}.
	 * @see org.apache.wicket.util.template.TextTemplate#asString(java.util.Map)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.template` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplateDecorator.java`
#### Snippet
```java
	 * @return the contents decorated with {@link #getBeforeTemplateContents()} and
	 *         {@link #getAfterTemplateContents()}
	 * @see org.apache.wicket.util.template.TextTemplate#asString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java

			/**
			 * @see org.apache.wicket.util.visit.IVisitor#component(Object,
			 *      org.apache.wicket.util.visit.IVisit)
			 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java
			/**
			 * @see org.apache.wicket.util.visit.IVisitor#component(Object,
			 *      org.apache.wicket.util.visit.IVisit)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.template` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/template/CssTemplate.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.template.TextTemplateDecorator#getAfterTemplateContents()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.template` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/template/CssTemplate.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.util.template.TextTemplateDecorator#getBeforeTemplateContents()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/IWrapModel.java`
#### Snippet
```java
	 * Calls getWrappedModel().detach();
	 *
	 * @see org.apache.wicket.model.IDetachable#detach()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.internal` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java
		/**
		 *
		 * @see org.apache.wicket.markup.html.internal.HtmlHeaderContainer#newHeaderResponse()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.request.Response#write(CharSequence)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/page/PartialPageUpdate.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.request.Response#encodeURL(CharSequence)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentModel.java`
#### Snippet
```java
	 * This getObject throws an exception.
	 * 
	 * @see org.apache.wicket.model.IModel#getObject()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/ChainingModel.java`
#### Snippet
```java
	 * Unsets this property model's instance variables and detaches the model.
	 *
	 * @see org.apache.wicket.model.IDetachable#detach()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/ComponentDetachableModel.java`
#### Snippet
```java
	 * This getObject throws an exception.
	 * 
	 * @see org.apache.wicket.model.IModel#getObject()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/CompoundPropertyModel.java`
#### Snippet
```java
 * the object is an instanceof IModel itself.
 * 
 * @see org.apache.wicket.model.IModel
 * @see org.apache.wicket.model.Model
 * @see org.apache.wicket.model.LoadableDetachableModel
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/CompoundPropertyModel.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.model.IModel
 * @see org.apache.wicket.model.Model
 * @see org.apache.wicket.model.LoadableDetachableModel
 * @see ChainingModel
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/CompoundPropertyModel.java`
#### Snippet
```java
 * @see org.apache.wicket.model.IModel
 * @see org.apache.wicket.model.Model
 * @see org.apache.wicket.model.LoadableDetachableModel
 * @see ChainingModel
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/Model.java`
#### Snippet
```java
	 * @param object
	 *            the model object
	 * @see org.apache.wicket.model.IModel#setObject(Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/Model.java`
#### Snippet
```java
 * <code>Model</code> is the basic implementation of an <code>IModel</code>. It just wraps a simple
 * model object. The model object must be serializable, as it is stored in the session. If you have
 * large objects to store, consider using {@link org.apache.wicket.model.LoadableDetachableModel}
 * instead of this class.
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/AbstractPropertyModel.java`
#### Snippet
```java
 * 
 * @see PropertyResolver
 * @see org.apache.wicket.model.IDetachable
 * 
 * @author Chris Turner
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/IModel.java`
#### Snippet
```java
 * Likewise, the {@link IModel#setObject(Object)}method sets a different property depending on which
 * Component is doing the setting. For more information on CompoundPropertyModels and model
 * inheritance, see {@link org.apache.wicket.model.CompoundPropertyModel}and
 * {@link org.apache.wicket.Page}.
 * </ul>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/RawMarkup.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.MarkupElement#equalTo(org.apache.wicket.markup.MarkupElement)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/RawMarkup.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.MarkupElement#equalTo(org.apache.wicket.markup.MarkupElement)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/RawMarkup.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.MarkupElement#toCharSequence()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/HtmlSpecialTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#isOpen()
	 * @return True if this tag is an open tag
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/HtmlSpecialTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#isOpenClose()
	 * @return True if this tag is an open and a close tag
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/HtmlSpecialTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#isClose()
	 * @return True if this tag is a close tag
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/HtmlSpecialTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#getPos()
	 * @return Tag location (index in input string)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ContainerInfo.java`
#### Snippet
```java
	/**
	 * 
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/Markup.java`
#### Snippet
```java

	/**
	 * @see java.lang.Iterable#iterator()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.util.resource.locator` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/DefaultMarkupResourceStreamProvider.java`
#### Snippet
```java
	 * should be extended for ALL components of your application.
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator
	 * @see org.apache.wicket.markup.DefaultMarkupResourceStreamProvider
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/DefaultMarkupResourceStreamProvider.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.core.util.resource.locator.IResourceStreamLocator
	 * @see org.apache.wicket.markup.DefaultMarkupResourceStreamProvider
	 * 
	 * @param container
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#getPos()
	 * @return Tag location (index in input string)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#put(String, StringValue)
	 * @param key
	 *            The key
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
	 *            Required component id
	 * @return True if this tag is an open tag with the given component name
	 * @see org.apache.wicket.markup.parser.XmlTag#isOpen()
	 */
	public final boolean isOpen(String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#putAll(Map)
	 * @param map
	 *            a key/value map
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#isOpenClose()
	 * @return True if this tag is an open and a close tag
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#put(String, boolean)
	 * @param key
	 *            The key
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#remove(String)
	 * @param key
	 *            The key to remove
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
	 * THIS METHOD IS NOT PART OF THE PUBLIC API, DO NOT CALL IT
	 * 
	 * @see org.apache.wicket.markup.parser.XmlTag#put(String, CharSequence)
	 * @param key
	 *            The key
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#put(String, CharSequence)
	 * @param key
	 *            The key
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.MarkupElement#equalTo(org.apache.wicket.markup.MarkupElement)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.MarkupElement#equalTo(org.apache.wicket.markup.MarkupElement)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#setNamespace(String)
	 * @param namespace
	 *            New tag name namespace
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#getAttributes()
	 * @return The tag#s attributes
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#isClose()
	 * @return True if this tag is a close tag
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
	 *            Required component id
	 * @return True if this tag is an openclose tag with the given component id
	 * @see org.apache.wicket.markup.parser.XmlTag#isOpenClose()
	 */
	public final boolean isOpenClose(String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.MarkupElement#toCharSequence()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#put(String, int)
	 * @param key
	 *            The key
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#setName(String)
	 * @param name
	 *            New tag name
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.parser.XmlTag#isOpen()
	 * @return True if this tag is an open tag
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupIterator.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#hasNext()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupIterator.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#remove()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupIterator.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#next()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/AbstractMarkupParser.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupElement.java`
#### Snippet
```java
 * </ul>
 * 
 * @see org.apache.wicket.markup.RawMarkup
 * @see ComponentTag
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource.loader` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/model/StringResourceModel.java`
#### Snippet
```java
 * reusable components/containers that are packaged with their own string resource bundles it should
 * be the actual component/container rather than the page. For more information on this please see
 * {@link org.apache.wicket.resource.loader.ComponentStringResourceLoader}. The relative component
 * may actually be {@code null} if this model is wrapped on assignment (
 * {@link IComponentAssignedModel}) or when all resource loading is to be done from a global
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java

/**
 * A stream of {@link org.apache.wicket.markup.MarkupElement}s, subclasses of which are
 * {@link org.apache.wicket.markup.ComponentTag} and {@link org.apache.wicket.markup.RawMarkup}. A
 * markup stream has a current index in the list of markup elements. The next markup element can be
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
/**
 * A stream of {@link org.apache.wicket.markup.MarkupElement}s, subclasses of which are
 * {@link org.apache.wicket.markup.ComponentTag} and {@link org.apache.wicket.markup.RawMarkup}. A
 * markup stream has a current index in the list of markup elements. The next markup element can be
 * retrieved and the index advanced by calling next(). If the index hits the end, hasMore() will
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupStream.java`
#### Snippet
```java
/**
 * A stream of {@link org.apache.wicket.markup.MarkupElement}s, subclasses of which are
 * {@link org.apache.wicket.markup.ComponentTag} and {@link org.apache.wicket.markup.RawMarkup}. A
 * markup stream has a current index in the list of markup elements. The next markup element can be
 * retrieved and the index advanced by calling next(). If the index hits the end, hasMore() will
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/AbstractJavaScriptReferenceHeaderItem.java`
#### Snippet
```java

/**
 * A {@link org.apache.wicket.markup.head.HeaderItem} that renders a JavaScript reference.
 */
public abstract class AbstractJavaScriptReferenceHeaderItem extends JavaScriptHeaderItem implements ISubresourceHeaderItem
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/HeaderItem.java`
#### Snippet
```java
 * example, be a {@linkplain JavaScriptHeaderItem script} or a {@linkplain CssHeaderItem stylesheet}
 * , but also {@linkplain OnEventHeaderItem event triggers} or {@linkplain StringHeaderItem free
 * form text}. {@code HeaderItem}s are used by {@link org.apache.wicket.markup.head.ResourceAggregator} to be able to collect all
 * header sections in a uniform way. All {@code HeaderItem}s are expected to have decent
 * {@code equals}, {@code hashCode} and {@code toString} (for debugging).
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `wicket-core/src/main/java/org/apache/wicket/markup/head/JavaScriptReferenceHeaderItem.java`
#### Snippet
```java
		if (!super.equals(o)) return false;
		JavaScriptReferenceHeaderItem that = (JavaScriptReferenceHeaderItem) o;
		return java.util.Objects.equals(reference, that.reference) &&
				java.util.Objects.equals(pageParameters, that.pageParameters);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `wicket-core/src/main/java/org/apache/wicket/markup/head/JavaScriptReferenceHeaderItem.java`
#### Snippet
```java
		JavaScriptReferenceHeaderItem that = (JavaScriptReferenceHeaderItem) o;
		return java.util.Objects.equals(reference, that.reference) &&
				java.util.Objects.equals(pageParameters, that.pageParameters);
	}
}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/AbstractCssReferenceHeaderItem.java`
#### Snippet
```java

/**
 * A {@link org.apache.wicket.markup.head.HeaderItem} that renders a CSS reference.
 */
public abstract class AbstractCssReferenceHeaderItem extends CssHeaderItem implements ISubresourceHeaderItem
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/internal/HeaderResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.head.IHeaderResponse#markRendered(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/internal/HeaderResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.head.IHeaderResponse#markRendered(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/internal/HeaderResponse.java`
#### Snippet
```java

/**
 * Default implementation of the {@link org.apache.wicket.markup.head.IHeaderResponse} interface.
 * 
 * @author Matej Knopp
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/internal/HeaderResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.head.IHeaderResponse#wasRendered(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/internal/HeaderResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.head.IHeaderResponse#wasRendered(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/internal/HeaderResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.head.IHeaderResponse#getResponse()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/internal/HeaderResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.head.IHeaderResponse#isClosed()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/internal/HeaderResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.head.IHeaderResponse#close()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebComponent.java`
#### Snippet
```java

	/**
	 * A convenience method to return the current WebRequest. Same as {@link org.apache.wicket.Component#getRequest()}.
	 *
	 * @return the current WebRequest
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebComponent.java`
#### Snippet
```java

	/**
	 * A convenience method to return the WebSession. Same as {@link org.apache.wicket.Component#getSession()} .
	 *
	 * @return the current WebSession
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebComponent.java`
#### Snippet
```java
 * associated markup.
 * 
 * @see org.apache.wicket.markup.html.WebMarkupContainer
 * 
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebComponent.java`
#### Snippet
```java

	/**
	 * A convenience method to return the current WebResponse. Same as {@link org.apache.wicket.Component#getResponse()}.
	 *
	 * @return the current WebResponse
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/ResourceAggregator.java`
#### Snippet
```java
	 *
	 * For example if CSS resource with media <em>screen</em> is replaced with
	 * {@link org.apache.wicket.protocol.http.WebApplication#addResourceReplacement(org.apache.wicket.request.resource.CssResourceReference, org.apache.wicket.request.resource.ResourceReference)} then the replacement will
	 * will inherit the media attribute
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/ResourceAggregator.java`
#### Snippet
```java
	 *
	 * For example if CSS resource with media <em>screen</em> is replaced with
	 * {@link org.apache.wicket.protocol.http.WebApplication#addResourceReplacement(org.apache.wicket.request.resource.CssResourceReference, org.apache.wicket.request.resource.ResourceReference)} then the replacement will
	 * will inherit the media attribute
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/ResourceAggregator.java`
#### Snippet
```java
	 *
	 * For example if CSS resource with media <em>screen</em> is replaced with
	 * {@link org.apache.wicket.protocol.http.WebApplication#addResourceReplacement(org.apache.wicket.request.resource.JavaScriptResourceReference, org.apache.wicket.request.resource.ResourceReference)} then the replacement will
	 * will inherit the media attribute
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebMarkupContainer.java`
#### Snippet
```java

	/**
	 * A convenience method to return the current WebRequest. Same as {@link org.apache.wicket.Component#getRequest()}.
	 *
	 * @return the current WebRequest
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebMarkupContainer.java`
#### Snippet
```java

	/**
	 * A convenience method to return the WebSession. Same as {@link org.apache.wicket.Component#getSession()} .
	 *
	 * @return the current WebSession
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebMarkupContainer.java`
#### Snippet
```java

	/**
	 * A convenience method to return the current WebResponse. Same as {@link org.apache.wicket.Component#getResponse()}.
	 *
	 * @return the current WebResponse
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
	private static final DateTimeFormatter headerDateFormat_RFC1036 = DateTimeFormatter
		.ofPattern("EEEE, dd-MMM-yy HH:mm:ss zzz")
		.withLocale(java.util.Locale.ENGLISH)
		.withZone(ZoneOffset.UTC); // Sunday, 06-Nov-94 08:49:37 GMT ; RFC 850, obs. by RFC 1036

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
	private static final DateTimeFormatter headerDateFormat_RFC1123 = DateTimeFormatter
		.ofPattern("EEE, dd MMM yyyy HH:mm:ss zzz")
		.withLocale(java.util.Locale.ENGLISH)
		.withZone(ZoneOffset.UTC); // Sun, 06 Nov 1994 08:49:37 GMT ; RFC 822, updated by RFC 1123

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/head/http2/PushHeaderItem.java`
#### Snippet
```java
	private static final DateTimeFormatter headerDateFormat_ASCTIME = DateTimeFormatter
		.ofPattern("EEE MMM d HH:mm:ss yyyy")
		.withLocale(java.util.Locale.ENGLISH)
		.withZone(ZoneOffset.UTC); // Sun Nov 6 08:49:37 1994 ; ANSI C's asctime() format

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/CrossOrigin.java`
#### Snippet
```java
 * To be used for the crossOrigin attribute
 *
 * @see ISubresourceHeaderItem#setCrossOrigin(org.apache.wicket.markup.html.CrossOrigin)
 */
public enum CrossOrigin {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/PackageResourceGuard.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.IPackageResourceGuard#accept(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/PackageResourceGuard.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.IPackageResourceGuard#accept(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	/**
	 * Creates a {@link FormTester} for the <code>Form</code> at a given path, and fills all child
	 * {@link org.apache.wicket.markup.html.form.FormComponent}s with blank <code>String</code>s.
	 *
	 * @param path
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	/**
	 * Tests that a <code>Component</code> has been added to a <code>AjaxRequestTarget</code>, using
	 * {@link org.apache.wicket.ajax.AjaxRequestTarget#add(org.apache.wicket.Component...)}. This
	 * method actually tests that a <code>Component</code> is on the Ajax response sent back to the
	 * client.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	 *            if true component needs to be visible in hierarchy else {@code null} is returned
	 * @return The component at the path
	 * @see org.apache.wicket.MarkupContainer#get(String)
	 */
	public Component getComponentFromLastRenderedPage(String path,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	 *            Path to component
	 * @return The component at the path
	 * @see org.apache.wicket.MarkupContainer#get(String)
	 */
	public Component getComponentFromLastRenderedPage(String path)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	 *            the class of the component to be tested
	 * @return The component processed
	 * @see #startComponentInPage(org.apache.wicket.Component)
	 */
	public final <C extends Component> C startComponentInPage(final Class<C> componentClass)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
	 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
	 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
	 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/TransparentWebMarkupContainer.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
	 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java

		/**
		 * @see java.util.concurrent.ConcurrentHashMap#put(java.lang.Object, java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java

		/**
		 * @see java.util.concurrent.ConcurrentHashMap#put(java.lang.Object, java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/SecurePackageResourceGuard.java`
#### Snippet
```java

		/**
		 * @see java.util.concurrent.ConcurrentHashMap#put(java.lang.Object, java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/SimpleFormComponentLabel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#onComponentTagBody(org.apache.wicket.markup.MarkupStream,
	 *      org.apache.wicket.markup.ComponentTag)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/SimpleFormComponentLabel.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.Component#onComponentTagBody(org.apache.wicket.markup.MarkupStream,
	 *      org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/StatelessForm.java`
#### Snippet
```java
 *
 * <strong>Note</strong>: Use {@link org.apache.wicket.markup.repeater.RepeatingView} in the case when
 * {@link org.apache.wicket.markup.html.form.FormComponent}s need to be rendered inside a repeater. See
 * the javadoc of {@link org.apache.wicket.markup.repeater.AbstractRepeater} for more information.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebPage.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.Component#onAfterRender()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,IModel)
	 * 
	 * @param id
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String,
	 *      List,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#convertValue(String[])
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String,
	 *      IModel,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel)
	 */
	public ListMultipleChoice(String id, IModel<? extends List<? extends T>> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, List)
	 */
	public ListMultipleChoice(final String id, final List<? extends T> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#isSelected(Object,int, String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,
	 *      IModel,IChoiceRenderer)
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String)
	 */
	public ListMultipleChoice(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,
	 *      List,IChoiceRenderer)
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel, List)
	 * 
	 * @param id
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListMultipleChoice.java`
#### Snippet
```java
	 * @param maxRows
	 *            the maximum number of visible rows.
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, List)
	 */
	public ListMultipleChoice(final String id, final List<? extends T> choices, final int maxRows)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ImageButton.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#setDefaultModel(org.apache.wicket.model.IModel)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ImageButton.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.Button#getStatelessHint()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroupSelector.java`
#### Snippet
```java
 * CheckGroup it is meant to affect.
 * 
 * @see org.apache.wicket.markup.html.form.CheckGroup
 * @see org.apache.wicket.markup.html.form.Check
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroupSelector.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.markup.html.form.CheckGroup
 * @see org.apache.wicket.markup.html.form.Check
 * 
 * @author Igor Vaynberg
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @param renderer
	 *            The rendering engine
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel,
	 *      java.util.List,org.apache.wicket.markup.html.form.IChoiceRenderer)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel,
	 *      java.util.List,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel,
	 *      java.util.List,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 */
	public CheckBoxMultipleChoice(final String id, IModel<? extends Collection<T>> model,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel,
	 *      java.util.List,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 */
	public CheckBoxMultipleChoice(final String id, IModel<? extends Collection<T>> model,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @param renderer
	 *            The rendering engine
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.markup.html.form.IChoiceRenderer)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 */
	public CheckBoxMultipleChoice(String id, IModel<? extends Collection<T>> model,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 */
	public CheckBoxMultipleChoice(String id, IModel<? extends Collection<T>> model,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The collection of choices in the radio choice
	 * @see AbstractChoice#AbstractChoice(String,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.model.IModel)
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The collection of choices in the radio choice
	 * @see AbstractChoice#AbstractChoice(String,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.model.IModel)
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @see AbstractChoice#AbstractChoice(String,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.model.IModel)
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 */
	public CheckBoxMultipleChoice(String id, IModel<? extends Collection<T>> model,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @param choices
	 *            The collection of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel, java.util.List)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The collection of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel, java.util.List)
	 */
	public CheckBoxMultipleChoice(final String id, IModel<? extends Collection<T>> model,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The collection of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String, org.apache.wicket.model.IModel)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel, java.util.List)
	 */
	public CheckBoxMultipleChoice(final String id, IModel<? extends Collection<T>> model,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 * @see AbstractChoice#AbstractChoice(String,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 * @see org.apache.wicket.Component#Component(String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 * @see AbstractChoice#AbstractChoice(String,
	 *      org.apache.wicket.model.IModel,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 * @see org.apache.wicket.Component#Component(String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 * @see AbstractChoice#AbstractChoice(String,
	 *      java.util.List,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 */
	public CheckBoxMultipleChoice(final String id, final List<? extends T> choices,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 * @see org.apache.wicket.Component#Component(String)
	 * @see AbstractChoice#AbstractChoice(String,
	 *      java.util.List,org.apache.wicket.markup.html.form.IChoiceRenderer)
	 */
	public CheckBoxMultipleChoice(final String id, final List<? extends T> choices,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.ListMultipleChoice#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.ListMultipleChoice#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The collection of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String)
	 * @see AbstractChoice#AbstractChoice(String, org.apache.wicket.model.IModel)
	 */
	public CheckBoxMultipleChoice(String id, IModel<? extends List<? extends T>> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBoxMultipleChoice.java`
#### Snippet
```java
	 *            The collection of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String)
	 * @see AbstractChoice#AbstractChoice(String, java.util.List)
	 */
	public CheckBoxMultipleChoice(final String id, final List<? extends T> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/HiddenField.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.TextField#getInputTypes()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Radio.java`
#### Snippet
```java
 * </p>
 * 
 * @see org.apache.wicket.markup.html.form.RadioGroup
 * 
 * @author Igor Vaynberg
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/LabeledWebMarkupContainer.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.ILabelProvider#getLabel()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
 * </p>
 * 
 * @see org.apache.wicket.markup.html.form.Check
 * @see org.apache.wicket.markup.html.form.CheckGroupSelector <p>
 *      Note: This component does not support cookie persistence
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.markup.html.form.Check
 * @see org.apache.wicket.markup.html.form.CheckGroupSelector <p>
 *      Note: This component does not support cookie persistence
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
				{
					Check<T> checkbox = visitChildren(Check.class,
						new org.apache.wicket.util.visit.IVisitor<Check<T>, Check<T>>()
						{
							@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentLabel.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.Component#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponentUpdatingBehavior.java`
#### Snippet
```java
	/**
	 * Gives the control to the application to decide whether the form component model should
	 * be updated automatically or not. Make sure to call {@link org.apache.wicket.markup.html.form.FormComponent#valid()}
	 * additionally in case the application want to update the model manually.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Button.java`
#### Snippet
```java
	 * (by setting it's markup attribute &quot;value&quot;).
	 * 
	 * @see org.apache.wicket.markup.html.form.FormComponent#updateModel()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
	 * this method should test this flag also.
	 * 
	 * @see org.apache.wicket.markup.html.form.FormComponent#convertInput()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public AbstractTextComponent(String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
	 * to work nicely with validation, this method returns false.
	 * 
	 * @see org.apache.wicket.markup.html.form.FormComponent#isInputNullable()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
	 * If the type is not set try to guess it if the model supports it.
	 * 
	 * @see org.apache.wicket.Component#onBeforeRender()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractTextComponent.java`
#### Snippet
```java
	 * @param id
	 * @param model
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public AbstractTextComponent(final String id, final IModel<T> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Check.java`
#### Snippet
```java
 * </p>
 * 
 * @see org.apache.wicket.markup.html.form.CheckGroup
 * 
 * @author Igor Vaynberg
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#convertValue(String[])
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java
			// retrieve the selected single radio choice component
			Radio<T> choice = visitChildren(Radio.class,
				new org.apache.wicket.util.visit.IVisitor<Radio<T>, Radio<T>>()
				{

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/DropDownChoice.java`
#### Snippet
```java
	 * @param tag
	 *            Tag to modify
	 * @see org.apache.wicket.Component#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.util.convert.IConverter#convertToString(java.lang.Object,
		 *      java.util.Locale)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.util.convert.IConverter#convertToString(java.lang.Object,
		 *      java.util.Locale)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.util.convert.IConverter#convertToString(java.lang.Object,
		 *      java.util.Locale)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.convert` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,
		 *      java.util.Locale)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,
		 *      java.util.Locale)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckBox.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.util.convert.IConverter#convertToObject(java.lang.String,
		 *      java.util.Locale)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java
	 * @see #getNullValidKey()
	 * @see #getNullKey()
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#getDefaultChoice(String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AbstractSingleSelectChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#convertValue(String[])
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 * @param tag
	 *            Tag to modify
	 * @see org.apache.wicket.Component#onComponentTag(ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.validation` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.validation.IValidatable#getValue()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#internalOnModelChanged()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.validation` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	{
		/**
		 * @see org.apache.wicket.validation.IValidatable#error(org.apache.wicket.validation.IValidationError)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.validation` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	{
		/**
		 * @see org.apache.wicket.validation.IValidatable#error(org.apache.wicket.validation.IValidationError)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public FormComponent(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 */
	public static <R> R visitComponentsPostOrder(Component component,
		final org.apache.wicket.util.visit.IVisitor<Component, R> visitor)
	{
		Args.notNull(visitor, "visitor");
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 * Sets the temporary converted input value to null.
	 * 
	 * @see org.apache.wicket.Component#onDetach()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java
	 * @param id
	 * @param model
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public FormComponent(final String id, IModel<T> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.validation` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/FormComponent.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.validation.IValidatable#isValid()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form.upload` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUploadField.java`
#### Snippet
```java
 * Form component that corresponds to a &lt;input type=&quot;file&quot;&gt;. When a FileInput
 * component is nested in a {@link org.apache.wicket.markup.html.form.Form}, that has multipart ==
 * true, its model is updated with the {@link org.apache.wicket.markup.html.form.upload.FileUpload}
 * for this component.
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FileUpload.java`
#### Snippet
```java
		{
			Args.notEmpty(algorithm, "algorithm");
			MessageDigest digest = java.security.MessageDigest.getInstance(algorithm);

			if (item.isInMemory())
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FilesSelectedBehavior.java`
#### Snippet
```java
     * Called when a file, at client side is selected.
     *
     * @param target           The {@link org.apache.wicket.ajax.AjaxRequestTarget}
     * @param fileDescriptions A list of FileDescription
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/FilesSelectedBehavior.java`
#### Snippet
```java

/**
 * {@link org.apache.wicket.ajax.form.OnChangeAjaxBehavior} that streams back to server properties
 * of the selected file(s) (at client side), before uploading it (them).
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#setVersioned(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	 * components.
	 *
	 * @see org.apache.wicket.markup.html.form.FormComponent#updateModel()
	 */
	protected final void updateFormComponentModels()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#renderPlaceholderTag(org.apache.wicket.markup.ComponentTag,
	 *      org.apache.wicket.request.Response)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#renderPlaceholderTag(org.apache.wicket.markup.ComponentTag,
	 *      org.apache.wicket.request.Response)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.Component#renderPlaceholderTag(org.apache.wicket.markup.ComponentTag,
	 *      org.apache.wicket.request.Response)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	 * @param model
	 *            See Component
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Form(final String id, final IModel<T> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	/**
	 *
	 * @see org.apache.wicket.Component#getStatelessHint()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#onDetach()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#onComponentTag(ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/MultiFileUploadField.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponentPanel#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/MultiFileUploadField.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponentPanel#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/MultiFileUploadField.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#getInputAsArray()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/PopupCloseLink.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.link.Link#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/BookmarkablePageLink.java`
#### Snippet
```java
	 * 
	 * @return The URL that this link links to
	 * @see org.apache.wicket.markup.html.link.Link#getURL()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/BookmarkablePageLink.java`
#### Snippet
```java
	 * @param page
	 *            the page
	 * @see org.apache.wicket.markup.html.link.Link#linksTo(org.apache.wicket.Page)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/BookmarkablePageLink.java`
#### Snippet
```java
	 * @param page
	 *            the page
	 * @see org.apache.wicket.markup.html.link.Link#linksTo(org.apache.wicket.Page)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/BookmarkablePageLink.java`
#### Snippet
```java
	 * satisfy the interface only, as bookmarkable links will be dispatched by the handling servlet.
	 * 
	 * @see org.apache.wicket.markup.html.link.Link#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#isEnabled()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public Link(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#getStatelessHint()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
	 * @param id
	 * @param model
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Link(final String id, IModel<T> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/Link.java`
#### Snippet
```java
	 * @param tag
	 *            the component tag
	 * @see org.apache.wicket.Component#onComponentTag(ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form.validation` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/validation/AbstractFormValidator.java`
#### Snippet
```java

/**
 * Base class for {@link org.apache.wicket.markup.html.form.validation.IFormValidator}s.
 * 
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#onBeforeRender()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java
	 * @param model
	 *            Must contain a Integer model object
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Loop(final String id, final IModel<Integer> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractRepeater#renderIterator()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/Loop.java`
#### Snippet
```java
	 * @param iterations
	 *            max index of the loop
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Loop(final String id, final int iterations)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/link/ExternalLink.java`
#### Snippet
```java
	 * @param tag
	 *            Tag to modify
	 * @see org.apache.wicket.Component#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.navigation.paging.IPageableItems#getItemCount()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.list` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java
	 * Prevent users from accidentally using it.
	 * 
	 * @see org.apache.wicket.markup.html.list.ListView#setStartIndex(int)
	 * @throws UnsupportedOperationException
	 *             always
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.list` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.list.ListView#getViewSize()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.list` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/PageableListView.java`
#### Snippet
```java
	 * @throws UnsupportedOperationException
	 *             always
	 * @see org.apache.wicket.markup.html.list.ListView#setStartIndex(int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
	 * @param id
	 *            the component id
	 * @see org.apache.wicket.markup.html.image.Image
	 */
	protected Source(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
	 * @param imageResources
	 *            the image resources applied to the source in the given order
	 * @see org.apache.wicket.markup.html.image.Image
	 */
	public Source(final String id, final IResource... imageResources)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
	 * @param resourceReferences
	 *            the resource references applied to the source in the given order
	 * @see org.apache.wicket.markup.html.image.Image
	 */
	public Source(final String id, PageParameters resourceParameters,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
	 * @param string
	 *            the string used as model
	 * @see org.apache.wicket.markup.html.image.Image
	 */
	public Source(final String id, final String string)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
	 * @param resourceReferences
	 *            the resource references applied to the source in the given order
	 * @see org.apache.wicket.markup.html.image.Image
	 */
	public Source(final String id, final ResourceReference... resourceReferences)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Source.java`
#### Snippet
```java
 * so every ResourceReference and ImageResource is added directly to the srcset attribute.
 *
 * @see org.apache.wicket.markup.html.image.Image
 * @author Tobias Soloschenko
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.awt` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResource.java`
#### Snippet
```java
	 * Renders button image.
	 * 
	 * @see RenderedDynamicImageResource#render(java.awt.Graphics2D, Attributes)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,
	 *      IModel,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 * @param choices
	 *            The list of choices in the radio choice
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,IModel)
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 * @param renderer
	 *            The rendering engine
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String,
	 *      IModel,IChoiceRenderer)
	 * @see org.apache.wicket.Component#Component(String)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 *            The list of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String)
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel)
	 */
	public RadioChoice(String id, IModel<? extends List<? extends T>> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 *            The list of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel, List)
	 */
	public RadioChoice(final String id, IModel<T> model, final List<? extends T> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 *            See Component
	 * @see org.apache.wicket.Component#Component(String)
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String)
	 */
	public RadioChoice(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#onComponentTag(org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 *            The list of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String)
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, List)
	 */
	public RadioChoice(final String id, final List<? extends T> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 *            The rendering engine
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,
	 *      List,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioChoice.java`
#### Snippet
```java
	 *            The list of choices in the radio choice
	 * @see org.apache.wicket.Component#Component(String)
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String,
	 *      List,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#initModel()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java
	 * @param string
	 *            Name of image
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Image(final String id, final String string)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#onComponentTag(ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#getStatelessHint()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#onComponentTagBody(MarkupStream, ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Image(final String id, final IModel<?> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#setDefaultModel(org.apache.wicket.model.IModel)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/Image.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#setDefaultModel(org.apache.wicket.model.IModel)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/BufferedDynamicImageResource.java`
#### Snippet
```java
 * look bad and GIF has patent issues.
 * 
 * @see org.apache.wicket.markup.html.image.resource.RenderedDynamicImageResource
 * @author Jonathan Locke
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalSource.java`
#### Snippet
```java
 * A component which displays external images within a picture tag.
 * 
 * @see org.apache.wicket.markup.html.image.Source
 * 
 * @author Tobias Soloschenko
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResourceFactory.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.IResourceFactory#newResource(java.lang.String, java.util.Locale,
	 *      java.lang.String, java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResourceFactory.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.IResourceFactory#newResource(java.lang.String, java.util.Locale,
	 *      java.lang.String, java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResourceFactory.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.IResourceFactory#newResource(java.lang.String, java.util.Locale,
	 *      java.lang.String, java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResourceFactory.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.IResourceFactory#newResource(java.lang.String, java.util.Locale,
	 *      java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/DefaultButtonImageResourceFactory.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.IResourceFactory#newResource(java.lang.String, java.util.Locale,
	 *      java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/RenderedDynamicImageResource.java`
#### Snippet
```java
 * look bad and GIF has patent issues.
 * 
 * @see org.apache.wicket.markup.html.image.resource.DefaultButtonImageResource
 * @see org.apache.wicket.markup.html.image.resource.DefaultButtonImageResourceFactory
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/RenderedDynamicImageResource.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.markup.html.image.resource.DefaultButtonImageResource
 * @see org.apache.wicket.markup.html.image.resource.DefaultButtonImageResourceFactory
 * @author Jonathan Locke
 * @author Gili Tzabari
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractRepeater#onPopulate()
	 */
	@SuppressWarnings("unchecked")
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
	 * @param id component id
	 * @param model model containing a list of
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public ListView(final String id, final IModel<? extends List<T>> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

			/**
			 * @see org.apache.wicket.markup.html.link.Link#onClick()
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

			/**
			 * @see org.apache.wicket.markup.html.link.Link#onClick()
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java
	 * @param list
	 *            List to cast to Serializable
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public ListView(final String id, final List<T> list)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractRepeater#renderIterator()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

			/**
			 * @see org.apache.wicket.markup.html.link.Link#onClick()
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractRepeater#renderChild(org.apache.wicket.Component)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractRepeater#renderChild(org.apache.wicket.Component)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/list/ListView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public ListView(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/resource/LocalizedImageResource.java`
#### Snippet
```java
 * image factory and its format is dependent on the specific image factory. For details on the
 * default buttonFactory, see
 * {@link org.apache.wicket.markup.html.image.resource.DefaultButtonImageResourceFactory}.
 * <p>
 * Finally, if there is no SRC attribute and no VALUE attribute, the Image component's model is
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/Fragment.java`
#### Snippet
```java
	 * Constructor.
	 * 
	 * @see org.apache.wicket.Component#Component(String)
	 * 
	 * @param id
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/Fragment.java`
#### Snippet
```java
	 * Constructor.
	 * 
	 * @see org.apache.wicket.Component#Component(String)
	 * 
	 * @param id
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public FeedbackPanel(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#isVersioned()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String)
	 * 
	 * @param id
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
				 * WICKET-4258 Feedback messages might be cleared already.
				 * 
				 * @see org.apache.wicket.settings.ApplicationSettings#setFeedbackMessageCleanupFilter(org.apache.wicket.feedback.IFeedbackMessageFilter)
				 */
				@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.Component#Component(String)
		 */
		public MessageListView(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java

/**
 * A panel that displays {@link org.apache.wicket.feedback.FeedbackMessage}s in a list view. The
 * maximum number of messages to show can be set with setMaxMessages().
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/FeedbackPanel.java`
#### Snippet
```java
 * maximum number of messages to show can be set with setMaxMessages().
 * 
 * @see org.apache.wicket.feedback.FeedbackMessage
 * @see org.apache.wicket.feedback.FeedbackMessages
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigationLink.java`
#### Snippet
```java
	 *            The page
	 * @return True if this link links to the given page
	 * @see org.apache.wicket.markup.html.link.Link#linksTo(org.apache.wicket.Page)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigationLink.java`
#### Snippet
```java
	 *            The page
	 * @return True if this link links to the given page
	 * @see org.apache.wicket.markup.html.link.Link#linksTo(org.apache.wicket.Page)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigationLink.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.link.Link#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public Border(final String id, final IModel<?> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
	 * </pre>
	 * 
	 * @see org.apache.wicket.MarkupContainer#add(org.apache.wicket.Component[])
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java
	 * </pre>
	 * 
	 * @see org.apache.wicket.MarkupContainer#add(org.apache.wicket.Component[])
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/border/Border.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public Border(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigationIncrementLink.java`
#### Snippet
```java
	 *            ignored
	 * @return True if this link links to the given page
	 * @see org.apache.wicket.markup.html.link.BookmarkablePageLink#linksTo(org.apache.wicket.Page)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigationIncrementLink.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.link.Link#onClick()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.list` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigation.java`
#### Snippet
```java
	 * sophisticated page links.
	 *
	 * @see org.apache.wicket.markup.html.list.Loop#populateItem(org.apache.wicket.markup.html.list.LoopItem)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.list` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigation.java`
#### Snippet
```java
	 * sophisticated page links.
	 *
	 * @see org.apache.wicket.markup.html.list.Loop#populateItem(org.apache.wicket.markup.html.list.LoopItem)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.list` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/navigation/paging/PagingNavigation.java`
#### Snippet
```java
	 * Renders the page link. Add the separator if not the last page link
	 *
	 * @see Loop#renderItem(org.apache.wicket.markup.html.list.LoopItem)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/XmlTag.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/AbstractMarkupFilter.java`
#### Snippet
```java
	/**
	 *  A key for a request-relative map of counters.
	 *  As map keys we use the class name of the {@link org.apache.wicket.markup.MarkupResourceStream}'s owner 
	 *  container (see {@link org.apache.wicket.markup.MarkupResourceStream#getContainerInfo()}), 
	 *  meaning that each container has its own counter. 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/AbstractMarkupFilter.java`
#### Snippet
```java
	 *  A key for a request-relative map of counters.
	 *  As map keys we use the class name of the {@link org.apache.wicket.markup.MarkupResourceStream}'s owner 
	 *  container (see {@link org.apache.wicket.markup.MarkupResourceStream#getContainerInfo()}), 
	 *  meaning that each container has its own counter. 
	 *  The counters are used by {@link #getRequestUniqueId()} to get unique ids for markup tags.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/AbstractMarkupFilter.java`
#### Snippet
```java
	/**
	 * Returns an id using the request-relative counter associated with the 
	 * underlying {@link org.apache.wicket.markup.MarkupResourceStream}'s owner container 
	 * (see {@link org.apache.wicket.markup.MarkupResourceStream#getContainerInfo()}). 
	 * This can be useful for autocomponent tags that need to get a tag id.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/AbstractMarkupFilter.java`
#### Snippet
```java
	 * Returns an id using the request-relative counter associated with the 
	 * underlying {@link org.apache.wicket.markup.MarkupResourceStream}'s owner container 
	 * (see {@link org.apache.wicket.markup.MarkupResourceStream#getContainerInfo()}). 
	 * This can be useful for autocomponent tags that need to get a tag id.
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.parser.filter` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java
 * This is a markup inline filter.
 * <p>
 * It assumes that {@link org.apache.wicket.markup.parser.filter.WicketTagIdentifier}
 * has been called first and search for a &lt;head&gt; tag (note: not wicket:head). Provided the markup contains a
 * &lt;body&gt; tag it will automatically prepend a &lt;head&gt; tag if missing.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.internal` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java
 * <p>
 * Additionally this filter handles &lt;wicket:header-items/&gt;. If there is such tag then it is marked
 * as the one that should be used as {@link org.apache.wicket.markup.html.internal.HtmlHeaderContainer}, by
 * setting its id to {@value #HEADER_ID}.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/HtmlHeaderSectionHandler.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.markup.MarkupParser
 * @see org.apache.wicket.markup.resolver.HtmlHeaderResolver
 * @author Juergen Donnerstag
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RootMarkupFilter.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalImage.java`
#### Snippet
```java
	 * Gets the cross origin settings
	 * 
	 * @see org.apache.wicket.markup.html.image.Image#setCrossOrigin(CrossOrigin)
	 *
	 * @return the cross origins settings
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalImage.java`
#### Snippet
```java
	 * Sets the cross origin settings
	 * 
	 * @see org.apache.wicket.markup.html.image.Image#setCrossOrigin(CrossOrigin)
	 * @param crossOrigin
	 *            the cross origins settings to set
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.image` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/image/ExternalImage.java`
#### Snippet
```java
 * A component to display external images. The src / srcSet information are hold in models
 * 
 * @see org.apache.wicket.markup.html.image.Image
 * 
 * @author Tobias Soloschenko
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/IItemFactory.java`
#### Snippet
```java
 * Factory interface for creating new child item containers for <b>AbstractPageableView</b>.
 * 
 * @see org.apache.wicket.markup.repeater.AbstractPageableView
 * 
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/RepeatingView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractRepeater#renderIterator()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/RepeatingView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractRepeater#onPopulate()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/Item.java`
#### Snippet
```java

		/**
		 * @see java.util.Comparator#compare(java.lang.Object, java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/Item.java`
#### Snippet
```java

		/**
		 * @see java.util.Comparator#compare(java.lang.Object, java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/Item.java`
#### Snippet
```java

		/**
		 * @see java.util.Comparator#compare(java.lang.Object, java.lang.Object)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractRepeater#onBeforeRender()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#next()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#hasNext()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java
 * </p>
 * 
 * @see org.apache.wicket.markup.repeater.RefreshingView
 * @see org.apache.wicket.markup.html.navigation.paging.IPageable
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractPageableView.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#remove()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ArrayIteratorAdapter.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#remove()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ArrayIteratorAdapter.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#hasNext()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ArrayIteratorAdapter.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#next()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.MarkupContainer#getMarkup(org.apache.wicket.Component)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.MarkupContainer#getMarkup(org.apache.wicket.Component)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Component#onBeforeRender()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/AbstractRepeater.java`
#### Snippet
```java
 * will not be available during the action phase (i.e. at
 * {@link org.apache.wicket.markup.html.form.StatelessForm#onSubmit()}). Use
 * {@link org.apache.wicket.markup.repeater.RepeatingView} in these cases.
 *
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/DefaultItemReuseStrategy.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy#getItems(org.apache.wicket.markup.repeater.IItemFactory,
	 *      java.util.Iterator, java.util.Iterator)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/DefaultItemReuseStrategy.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy#getItems(org.apache.wicket.markup.repeater.IItemFactory,
	 *      java.util.Iterator, java.util.Iterator)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/DefaultItemReuseStrategy.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy#getItems(org.apache.wicket.markup.repeater.IItemFactory,
	 *      java.util.Iterator, java.util.Iterator)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/DefaultItemReuseStrategy.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy#getItems(org.apache.wicket.markup.repeater.IItemFactory,
	 *      java.util.Iterator, java.util.Iterator)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/DefaultItemReuseStrategy.java`
#### Snippet
```java
 * Implementation of <code>IItemReuseStrategy</code> that returns new items every time.
 * 
 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy
 * 
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/ReuseIfModelsEqualStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy#getItems(org.apache.wicket.markup.repeater.IItemFactory,
	 *      java.util.Iterator, java.util.Iterator)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/ReuseIfModelsEqualStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy#getItems(org.apache.wicket.markup.repeater.IItemFactory,
	 *      java.util.Iterator, java.util.Iterator)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/ReuseIfModelsEqualStrategy.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy#getItems(org.apache.wicket.markup.repeater.IItemFactory,
	 *      java.util.Iterator, java.util.Iterator)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/ReuseIfModelsEqualStrategy.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.markup.repeater.IItemReuseStrategy#getItems(org.apache.wicket.markup.repeater.IItemFactory,
	 *      java.util.Iterator, java.util.Iterator)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/DataViewBase.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.repeater.AbstractPageableView#onDetach()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/DataViewBase.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#remove()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/DataViewBase.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#hasNext()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/DataViewBase.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#next()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltTransformerBehavior.java`
#### Snippet
```java
 * container must enclose the &lt;table&gt; tag as well to be HTML compliant.
 * 
 * @see org.apache.wicket.markup.transformer.AbstractOutputTransformerContainer
 * @see org.apache.wicket.markup.transformer.XsltOutputTransformerContainer
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltTransformerBehavior.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.markup.transformer.AbstractOutputTransformerContainer
 * @see org.apache.wicket.markup.transformer.XsltOutputTransformerContainer
 * 
 * @author Juergen Donnerstag
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ModelIteratorAdapter.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#next()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ModelIteratorAdapter.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#remove()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/util/ModelIteratorAdapter.java`
#### Snippet
```java

	/**
	 * @see java.util.Iterator#hasNext()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/GridView.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#next()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/GridView.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#hasNext()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/repeater/data/GridView.java`
#### Snippet
```java

		/**
		 * @see java.util.Iterator#remove()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/NoopOutputTransformerContainer.java`
#### Snippet
```java
	 * Construct
	 * 
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public NoopOutputTransformerContainer(final String id, final IModel<?> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/NoopOutputTransformerContainer.java`
#### Snippet
```java
	 * Construct
	 * 
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public NoopOutputTransformerContainer(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/ITransformer.java`
#### Snippet
```java

/**
 * A common interface to be implemented by {@link org.apache.wicket.markup.transformer.AbstractOutputTransformerContainer}s
 * and {@link org.apache.wicket.markup.transformer.AbstractTransformerBehavior}s which
 * post-process the output markup of a component.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/ITransformer.java`
#### Snippet
```java
/**
 * A common interface to be implemented by {@link org.apache.wicket.markup.transformer.AbstractOutputTransformerContainer}s
 * and {@link org.apache.wicket.markup.transformer.AbstractTransformerBehavior}s which
 * post-process the output markup of a component.
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractTransformerBehavior.java`
#### Snippet
```java
 * markup generated by the component.
 * 
 * @see org.apache.wicket.markup.transformer.AbstractOutputTransformerContainer
 * 
 * @author Juergen Donnerstag
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltOutputTransformerContainer.java`
#### Snippet
```java
	 * the user.
	 * 
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 * 
	 * @param id
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltOutputTransformerContainer.java`
#### Snippet
```java
	 * @param id
	 *            the wicket:id
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public XsltOutputTransformerContainer(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltOutputTransformerContainer.java`
#### Snippet
```java
 * not require an additional Container.
 * 
 * @see org.apache.wicket.markup.transformer.XsltTransformerBehavior
 * 
 * @author Juergen Donnerstag
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltOutputTransformerContainer.java`
#### Snippet
```java
	 * @param model
	 *            the model (unused)
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public XsltOutputTransformerContainer(final String id, final IModel<?> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
	 * Construct
	 * 
	 * @see org.apache.wicket.Component#Component(String)
	 */
	public AbstractOutputTransformerContainer(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
	 * Construct
	 * 
	 * @see org.apache.wicket.Component#Component(String, IModel)
	 */
	public AbstractOutputTransformerContainer(final String id, final IModel<?> model)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
 * components (excluding the containers tag)
 * <p>
 * Please see {@link org.apache.wicket.markup.transformer.AbstractTransformerBehavior} for an alternative
 * based on {@link org.apache.wicket.behavior.Behavior}
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
 * based on {@link org.apache.wicket.behavior.Behavior}
 * 
 * @see org.apache.wicket.markup.transformer.AbstractTransformerBehavior
 * @see org.apache.wicket.markup.transformer.ITransformer
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.markup.transformer.AbstractTransformerBehavior
 * @see org.apache.wicket.markup.transformer.ITransformer
 * 
 * @author Juergen Donnerstag
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltTransformer.java`
#### Snippet
```java
 * A processor to XSLT transform the output generated by a Component.
 * 
 * @see org.apache.wicket.markup.transformer.XsltOutputTransformerContainer
 * @see org.apache.wicket.markup.transformer.XsltTransformerBehavior
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.transformer` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/XsltTransformer.java`
#### Snippet
```java
 * 
 * @see org.apache.wicket.markup.transformer.XsltOutputTransformerContainer
 * @see org.apache.wicket.markup.transformer.XsltTransformerBehavior
 * 
 * @author Juergen Donnerstag
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.visit` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/renderStrategy/ParentFirstHeaderRenderStrategy.java`
#### Snippet
```java
/**
 * This has been Wicket's default header render strategy before WICKET 1.5 which uses
 * {@link MarkupContainer#visitChildren(org.apache.wicket.util.visit.IVisitor)} to traverse the hierarchy to
 * render the children headers.
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
		 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
		 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
		 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
		 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
	 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
	 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
	 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
	 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.resolver.AutoLinkResolver.IAutolinkResolverDelegate#newAutoComponent(java.lang.String,
		 * org.apache.wicket.markup.resolver.AutoLinkResolver.PathInfo)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.resolver.AutoLinkResolver.IAutolinkResolverDelegate#newAutoComponent(java.lang.String,
		 * org.apache.wicket.markup.resolver.AutoLinkResolver.PathInfo)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.markup.resolver.AutoLinkResolver.IAutolinkResolverDelegate#newAutoComponent(java.lang.String,
		 * org.apache.wicket.markup.resolver.AutoLinkResolver.PathInfo)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
		 * @param tag
		 *            the component tag
		 * @see org.apache.wicket.Component#onComponentTag(ComponentTag)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
		 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
		 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
		 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.markup.resolver.IComponentResolver#resolve(org.apache.wicket.MarkupContainer,
		 *      org.apache.wicket.markup.MarkupStream, org.apache.wicket.markup.ComponentTag)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
		/**
		 * 
		 * @see org.apache.wicket.markup.html.link.BookmarkablePageLink#getURL()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.Component#getVariation()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.resolver.AutoLinkResolver.IAutolinkResolverDelegate#newAutoComponent(java.lang.String,
		 * org.apache.wicket.markup.resolver.AutoLinkResolver.PathInfo)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.resolver.AutoLinkResolver.IAutolinkResolverDelegate#newAutoComponent(java.lang.String,
		 * org.apache.wicket.markup.resolver.AutoLinkResolver.PathInfo)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.resolver` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.markup.resolver.AutoLinkResolver.IAutolinkResolverDelegate#newAutoComponent(java.lang.String,
		 * org.apache.wicket.markup.resolver.AutoLinkResolver.PathInfo)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/IRequestCycleListener.java`
#### Snippet
```java
 * </p>
 * <ul>
 * <li>{@link #onRequestHandlerResolved(RequestCycle, org.apache.wicket.request.IRequestHandler)}</li>
 * <li>{@link #onRequestHandlerScheduled(RequestCycle, org.apache.wicket.request.IRequestHandler)}</li>
 * <li>{@link #onException(RequestCycle, Exception)}, followed by
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/IRequestCycleListener.java`
#### Snippet
```java
 * <ul>
 * <li>{@link #onRequestHandlerResolved(RequestCycle, org.apache.wicket.request.IRequestHandler)}</li>
 * <li>{@link #onRequestHandlerScheduled(RequestCycle, org.apache.wicket.request.IRequestHandler)}</li>
 * <li>{@link #onException(RequestCycle, Exception)}, followed by
 * {@link #onExceptionRequestHandlerResolved(RequestCycle, org.apache.wicket.request.IRequestHandler, Exception)} </li>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/IRequestCycleListener.java`
#### Snippet
```java
 * <li>{@link #onRequestHandlerScheduled(RequestCycle, org.apache.wicket.request.IRequestHandler)}</li>
 * <li>{@link #onException(RequestCycle, Exception)}, followed by
 * {@link #onExceptionRequestHandlerResolved(RequestCycle, org.apache.wicket.request.IRequestHandler, Exception)} </li>
 * </ul>
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/IRequestCycleListener.java`
#### Snippet
```java
 * @author Martijn Dashorst
 * 
 * @see org.apache.wicket.Application#getRequestCycleListeners()
 */
public interface IRequestCycleListener
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceRequestHandler.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceRequestHandler.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ByteArrayResource.java`
#### Snippet
```java
	/**
	 * Creates a {@link ByteArrayResource} which will provide its data dynamically with
	 * {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
	 * 
	 * @param contentType
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ByteArrayResource.java`
#### Snippet
```java
 * An {@link IResource} for byte arrays. The byte array can be static - passed to the constructor,
 * or dynamic - by overriding
 * {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
 * 
 * @author Matej Knopp
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceStreamRequestHandler.java`
#### Snippet
```java
	 * Responds by sending the contents of the resource stream.
	 * 
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceStreamRequestHandler.java`
#### Snippet
```java
	 * Responds by sending the contents of the resource stream.
	 * 
	 * @see org.apache.wicket.request.IRequestHandler#respond(org.apache.wicket.request.IRequestCycle)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceStreamRequestHandler.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/CharSequenceResource.java`
#### Snippet
```java

	/**
	 * Creates a {@link org.apache.wicket.request.resource.CharSequenceResource} which will provide its data dynamically with
	 * {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/CharSequenceResource.java`
#### Snippet
```java
	/**
	 * Creates a {@link org.apache.wicket.request.resource.CharSequenceResource} which will provide its data dynamically with
	 * {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
	 *
	 * @param contentType
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/CharSequenceResource.java`
#### Snippet
```java

/**
 * An {@link org.apache.wicket.request.resource.IResource} for CharSequences.
 * The char sequence can be static - passed to the constructor,
 * or dynamic - by overriding {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/CharSequenceResource.java`
#### Snippet
```java
 * An {@link org.apache.wicket.request.resource.IResource} for CharSequences.
 * The char sequence can be static - passed to the constructor,
 * or dynamic - by overriding {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
 */
public class CharSequenceResource extends BaseDataResource<CharSequence>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReferenceRegistry.java`
#### Snippet
```java
	 * auto-generated ResourceReferences).
	 * 
	 * @see #createDefaultResourceReference(org.apache.wicket.request.resource.ResourceReference.Key)
	 * @see ClassScanner
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReferenceRegistry.java`
#### Snippet
```java
	/**
	 * @return the factory that will create the resource reference by using the parsed
	 *          {@link org.apache.wicket.request.resource.ResourceReference.Key}
	 */
	public IResourceReferenceFactory getResourceReferenceFactory()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReferenceRegistry.java`
#### Snippet
```java
	 * auto-generated ResourceReferences).
	 * 
	 * @see #createDefaultResourceReference(org.apache.wicket.request.resource.ResourceReference.Key)
	 * @see ClassScanner
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReferenceRegistry.java`
#### Snippet
```java
	 * @param resourceReferenceFactory
	 *          the factory that will create the resource reference by using the parsed
	 *          {@link org.apache.wicket.request.resource.ResourceReference.Key}
	 */
	public void setResourceReferenceFactory(IResourceReferenceFactory resourceReferenceFactory)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PartWriterCallback.java`
#### Snippet
```java
/**
 * Used to read a part of an input stream and writes it to the output stream of the response taken
 * from attributes in {@link #writeData(org.apache.wicket.request.resource.IResource.Attributes)}
 * method.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/BaseDataResource.java`
#### Snippet
```java
/**
 * An abstract resource that can deliver static data - passed to the constructor, or dynamic -
 * returned by {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
 *
 * @param <T>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/BaseDataResource.java`
#### Snippet
```java

	/**
	 * Creates a {@link org.apache.wicket.request.resource.BaseDataResource} which will provide its
	 * data dynamically with
	 * {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/BaseDataResource.java`
#### Snippet
```java
	 * Creates a {@link org.apache.wicket.request.resource.BaseDataResource} which will provide its
	 * data dynamically with
	 * {@link #getData(org.apache.wicket.request.resource.IResource.Attributes)}
	 *
	 * @param contentType
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResourceReference.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.resource.ResourceReference#getResource()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
	 * <strong>Note</strong>: The produced URL is relative to the filter path. Application code most
	 * probably need URL relative to the currently used page, for this use
	 * {@linkplain #urlFor(org.apache.wicket.request.resource.ResourceReference, org.apache.wicket.request.mapper.parameter.PageParameters)}
	 * </p>
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.mapper.parameter` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
	 * <strong>Note</strong>: The produced URL is relative to the filter path. Application code most
	 * probably need URL relative to the currently used page, for this use
	 * {@linkplain #urlFor(org.apache.wicket.request.resource.ResourceReference, org.apache.wicket.request.mapper.parameter.PageParameters)}
	 * </p>
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
	 * <strong>Note</strong>: The produced URL is relative to the filter path. Application code most
	 * probably need URL relative to the currently used page, for this use
	 * {@linkplain #urlFor(org.apache.wicket.request.IRequestHandler)}
	 * </p>
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.mapper.parameter` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java
	 * <strong>Note</strong>: The produced URL is relative to the filter path. Application code most
	 * probably need URL relative to the currently used page, for this use
	 * {@linkplain #urlFor(Class, org.apache.wicket.request.mapper.parameter.PageParameters)}
	 * </p>
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/cycle/RequestCycle.java`
#### Snippet
```java

	/**
	 * Called to handle a {@link java.lang.RuntimeException} that might be 
	 * thrown during detaching phase. 
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource.caching` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/IResourceVersion.java`
#### Snippet
```java

/**
 * Provides a version string for a cacheable resource specified by {@link org.apache.wicket.request.resource.caching.IStaticCacheableResource} 
 * <p/>
 * the version string will be used as a distinguishing mark when rendering resources
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource.caching.version` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/FilenameWithVersionResourceCachingStrategy.java`
#### Snippet
```java
	 *            resource version object
	 * 
	 * @see #FilenameWithVersionResourceCachingStrategy(org.apache.wicket.request.resource.caching.version.IResourceVersion) 
	 */
	public FilenameWithVersionResourceCachingStrategy(String versionPrefix,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource.caching.version` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/FilenameWithVersionResourceCachingStrategy.java`
#### Snippet
```java
	 *            version provider
	 *            
	 * @see #FilenameWithVersionResourceCachingStrategy(String, org.apache.wicket.request.resource.caching.version.IResourceVersion) 
	 */
	public FilenameWithVersionResourceCachingStrategy(IResourceVersion resourceVersion)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		 * <p>
		 * It is necessary to set the {@link WriteCallback} if
		 * {@link #dataNeedsToBeWritten(org.apache.wicket.request.resource.IResource.Attributes)}
		 * returns <code>true</code> and {@link #setError(Integer)} has not been called.
		 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
	/**
	 * Callback invoked when resource data needs to be written to response. Subclass needs to
	 * implement the {@link #writeData(org.apache.wicket.request.resource.IResource.Attributes)}
	 * method.
	 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		 *            scope for caching
		 * 
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#getCacheDuration()
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#setCacheDuration(Duration)
		 * @see org.apache.wicket.request.http.WebResponse.CacheScope
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		 * 
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#getCacheDuration()
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#setCacheDuration(Duration)
		 * @see org.apache.wicket.request.http.WebResponse.CacheScope
		 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#getCacheDuration()
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#setCacheDuration(Duration)
		 * @see org.apache.wicket.request.http.WebResponse.CacheScope
		 *
		 * @return {@code this}, for chaining.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.request.resource.IResource#respond(org.apache.wicket.request.resource.IResource.Attributes)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
	/**
	 * 
	 * @see org.apache.wicket.request.resource.IResource#respond(org.apache.wicket.request.resource.IResource.Attributes)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		 * @return cache scope
		 * 
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#getCacheDuration()
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#setCacheDuration(Duration)
		 * @see org.apache.wicket.request.http.WebResponse.CacheScope
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		 * 
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#getCacheDuration()
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#setCacheDuration(Duration)
		 * @see org.apache.wicket.request.http.WebResponse.CacheScope
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#getCacheDuration()
		 * @see org.apache.wicket.request.resource.AbstractResource.ResourceResponse#setCacheDuration(Duration)
		 * @see org.apache.wicket.request.http.WebResponse.CacheScope
		 */
		public WebResponse.CacheScope getCacheScope()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/AbstractResource.java`
#### Snippet
```java
/**
 * Convenience resource implementation. The subclass must implement
 * {@link #newResourceResponse(org.apache.wicket.request.resource.IResource.Attributes)} method.
 * 
 * @author Matej Knopp
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource.caching` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/LastModifiedResourceVersion.java`
#### Snippet
```java

/**
 * Uses the last modified timestamp of a {@link org.apache.wicket.request.resource.caching.IStaticCacheableResource} 
 * converted to milliseconds as a version string.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/session/ISessionStore.java`
#### Snippet
```java

/**
 * The actual store that is used by {@link org.apache.wicket.Session} to store its attributes.
 * <p>
 * This class is intended for internal framework use.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/MessageDigestResourceVersion.java`
#### Snippet
```java
	 *
	 * @see #MessageDigestResourceVersion()
	 * @see #MessageDigestResourceVersion(String, org.apache.wicket.util.lang.Bytes)
	 */
	public MessageDigestResourceVersion(String algorithm)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource.caching` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/MessageDigestResourceVersion.java`
#### Snippet
```java

/**
 * computes the message digest of a {@link org.apache.wicket.request.resource.caching.IStaticCacheableResource} 
 * and uses it as a version string
 * <p/>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/caching/version/MessageDigestResourceVersion.java`
#### Snippet
```java
	 * 
	 * @see #MessageDigestResourceVersion(String) 
	 * @see #MessageDigestResourceVersion(String, org.apache.wicket.util.lang.Bytes)
	 */
	public MessageDigestResourceVersion()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	 *            The locale of the resource
	 * @param style
	 *            The style of the resource (see {@link org.apache.wicket.Session})
	 * @param variation
	 *            The component's variation (of the style)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java

	/**
	 * @return whether {@link org.apache.wicket.resource.ITextResourceCompressor} can be used to
	 *         compress the resource.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.javascript` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	 * WARNING - if the stream is not read buffered compressors will not work, because they require
	 * the whole content to be read into memory.<br>
	 * ({@link org.apache.wicket.javascript.IJavaScriptCompressor}, <br>
	 * {@link org.apache.wicket.css.ICssCompressor}, <br>
	 * {@link org.apache.wicket.resource.IScopeAwareTextResourceProcessor})
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	 * ({@link org.apache.wicket.javascript.IJavaScriptCompressor}, <br>
	 * {@link org.apache.wicket.css.ICssCompressor}, <br>
	 * {@link org.apache.wicket.resource.IScopeAwareTextResourceProcessor})
	 * 
	 * @param readBuffered
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/ErrorLevelFeedbackMessageFilter.java`
#### Snippet
```java
	 * @param minimumErrorLevel
	 *            The component to filter on, the constraints can be: <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#SUCCESS} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#UNDEFINED} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#DEBUG} <br />
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/ErrorLevelFeedbackMessageFilter.java`
#### Snippet
```java
	 *            The component to filter on, the constraints can be: <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#SUCCESS} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#UNDEFINED} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#DEBUG} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#INFO} <br />
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/ErrorLevelFeedbackMessageFilter.java`
#### Snippet
```java
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#SUCCESS} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#UNDEFINED} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#DEBUG} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#INFO} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#WARNING} <br />
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/ErrorLevelFeedbackMessageFilter.java`
#### Snippet
```java
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#UNDEFINED} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#DEBUG} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#INFO} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#WARNING} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#ERROR} <br />
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/ErrorLevelFeedbackMessageFilter.java`
#### Snippet
```java
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#DEBUG} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#INFO} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#WARNING} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#ERROR} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#FATAL} <br />
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/ErrorLevelFeedbackMessageFilter.java`
#### Snippet
```java
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#INFO} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#WARNING} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#ERROR} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#FATAL} <br />
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/ErrorLevelFeedbackMessageFilter.java`
#### Snippet
```java
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#WARNING} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#ERROR} <br />
	 *            {@link org.apache.wicket.feedback.FeedbackMessage#FATAL} <br />
	 */
	public ErrorLevelFeedbackMessageFilter(int minimumErrorLevel)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessages.java`
#### Snippet
```java

	/**
	 * Holds a list of {@link org.apache.wicket.feedback.FeedbackMessage}s.
	 */
	private final List<FeedbackMessage> messages;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.feedback` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessages.java`
#### Snippet
```java
	 * Checks if a message of the specified {@code level} or greater was registered.<br />
	 * To check for a precise {@code level} use {@link #hasMessage(IFeedbackMessageFilter)}
	 * and pass it a reference to {@link org.apache.wicket.feedback.ExactLevelFeedbackMessageFilter}.
	 * 
	 * @param level
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackMessages.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#hashCode()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 * Called to notify the behavior about any events sent to the component
	 * 
	 * @see org.apache.wicket.IComponentAwareEventSink#onEvent(org.apache.wicket.Component,
	 *      org.apache.wicket.event.IEvent)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 * Called to notify the behavior about any events sent to the component
	 * 
	 * @see org.apache.wicket.IComponentAwareEventSink#onEvent(org.apache.wicket.Component,
	 *      org.apache.wicket.event.IEvent)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.event` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.IComponentAwareEventSink#onEvent(org.apache.wicket.Component,
	 *      org.apache.wicket.event.IEvent)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	/**
	 * In case an unexpected exception happened anywhere between
	 * {@linkplain #onComponentTag(org.apache.wicket.Component, org.apache.wicket.markup.ComponentTag)} and
	 * {@linkplain #afterRender(org.apache.wicket.Component)},
	 * onException() will be called for any behavior. Typically, if you clean up resources in
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	/**
	 * In case an unexpected exception happened anywhere between
	 * {@linkplain #onComponentTag(org.apache.wicket.Component, org.apache.wicket.markup.ComponentTag)} and
	 * {@linkplain #afterRender(org.apache.wicket.Component)},
	 * onException() will be called for any behavior. Typically, if you clean up resources in
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
	 * In case an unexpected exception happened anywhere between
	 * {@linkplain #onComponentTag(org.apache.wicket.Component, org.apache.wicket.markup.ComponentTag)} and
	 * {@linkplain #afterRender(org.apache.wicket.Component)},
	 * onException() will be called for any behavior. Typically, if you clean up resources in
	 * {@link #afterRender(Component)}, you should do the same in the implementation of this method.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.behavior` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/behavior/Behavior.java`
#### Snippet
```java
 * @see IRequestListener
 * @see org.apache.wicket.markup.html.IHeaderContributor
 * @see org.apache.wicket.behavior.AbstractAjaxBehavior
 * @see org.apache.wicket.AttributeModifier
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/PageExpiredException.java`
#### Snippet
```java

	/**
	 * @see WicketRuntimeException#WicketRuntimeException(java.lang.String)
	 */
	public PageExpiredException(final String message)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/PageExpiredException.java`
#### Snippet
```java
	 * Suppress loading of the stacktrace because it is not needed.
	 *
	 * @see java.lang.Throwable#fillInStackTrace()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IResourceIsolationPolicy.java`
#### Snippet
```java
	 * @author papegaaij
	 * 
	 * @see IResourceIsolationPolicy#isRequestAllowed(jakarta.servlet.http.HttpServletRequest, org.apache.wicket.request.component.IRequestablePage)
	 */
	public enum ResourceIsolationOutcome
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.component` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IResourceIsolationPolicy.java`
#### Snippet
```java
	 * @author papegaaij
	 * 
	 * @see IResourceIsolationPolicy#isRequestAllowed(jakarta.servlet.http.HttpServletRequest, org.apache.wicket.request.component.IRequestablePage)
	 */
	public enum ResourceIsolationOutcome
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java
		{
			/**
			 * @see jakarta.servlet.FilterConfig#getServletContext()
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java

			/**
			 * @see jakarta.servlet.FilterConfig#getInitParameter(java.lang.String)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java

			/**
			 * @see jakarta.servlet.FilterConfig#getInitParameter(java.lang.String)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java
 * </pre>
 * 
 * and it has to satisfy interface {@link org.apache.wicket.protocol.http.IWebApplicationFactory}.
 * 
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java

			/**
			 * @see jakarta.servlet.FilterConfig#getInitParameterNames()
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketServlet.java`
#### Snippet
```java

			/**
			 * @see jakarta.servlet.FilterConfig#getFilterName()
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ReloadingWicketFilter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.protocol.http.WicketFilter#getClassLoader()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ReloadingWicketFilter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.protocol.http.WicketFilter#init(boolean, jakarta.servlet.FilterConfig)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ReloadingWicketFilter.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.protocol.http.WicketFilter#init(boolean, jakarta.servlet.FilterConfig)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/IRequestLogger.java`
#### Snippet
```java

	/**
	 * @return The {@link org.apache.wicket.protocol.http.IRequestLogger.RequestData} for the current request.
	 */
	RequestData getCurrentRequest();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#encodeURL(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#encodeURL(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#addCookie(javax.servlet.http.Cookie)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.ServletResponse#setLocale(java.util.Locale)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#addDateHeader(java.lang.String, long)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#addDateHeader(java.lang.String, long)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#sendRedirect(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#sendRedirect(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setStatus(int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#sendError(int, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#sendError(int, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#encodeRedirectURL(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#encodeRedirectURL(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequest#getHeader(java.lang.String)
	 * @param name
	 * @return the first header with name
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setStatus(int)
	 * @return status
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.ServletResponse#setContentType(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#sendError(int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setHeader(java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setHeader(java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setHeader(java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequest#getHeaders(java.lang.String)
	 * @param name
	 * @return all headers with name
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#containsHeader(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#containsHeader(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setIntHeader(java.lang.String, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setIntHeader(java.lang.String, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setDateHeader(java.lang.String, long)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#setDateHeader(java.lang.String, long)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#addIntHeader(java.lang.String, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#addIntHeader(java.lang.String, int)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#addHeader(java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#addHeader(java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedHttpServletResponse.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletResponse#addHeader(java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebSession.java`
#### Snippet
```java
	 * Cookies)
	 * 
	 * @see org.apache.wicket.Session#invalidate()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/StoredResponsesMap.java`
#### Snippet
```java

	@Override
	protected synchronized boolean removeEldestEntry(java.util.Map.Entry<String, Object> eldest)
	{
		boolean removed = super.removeEldestEntry(eldest);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java
	 * @param renderer
	 * @param maxRows
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,
	 *      IModel,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, List)
	 */
	public ListChoice(final String id, final List<? extends T> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,
	 *      IModel,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel,IModel)
	 */
	public ListChoice(String id, IModel<T> model, IModel<? extends List<? extends T>> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String,
	 *      IModel,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String, IModel)
	 */
	public ListChoice(String id, IModel<? extends List<? extends T>> choices)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java
	 * @param renderer
	 *            See AbstractChoice
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String,
	 *      List,IChoiceRenderer)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ListChoice.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.markup.html.form.AbstractChoice#AbstractChoice(String)
	 */
	public ListChoice(final String id)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/RequestLogger.java`
#### Snippet
```java
/**
 * This is the logger class that can be set in the
 * {@link org.apache.wicket.protocol.http.WebApplication#getRequestLogger()} method. If this class
 * is set all request and live sessions will be recorded and displayed From the total created
 * sessions, to the peak session count and the current live sessions. For the live sessions the
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplicationFactoryCreationException.java`
#### Snippet
```java

/**
 * Thrown when the {@link org.apache.wicket.protocol.http.IWebApplicationFactory} could not be
 * created for some reason.
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/ResourceIsolationRequestCycleListener.java`
#### Snippet
```java
	 * {@link CsrfAction#ABORT}. Default {@code 403 FORBIDDEN}.
	 */
	private int errorCode = jakarta.servlet.http.HttpServletResponse.SC_FORBIDDEN;

	/**
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/request/WebClientInfo.java`
#### Snippet
```java
	/**
	 * The user agent string from the User-Agent header, app. Theoretically, this might differ from
	 * {@link org.apache.wicket.protocol.http.ClientProperties#isNavigatorJavaEnabled()} property, which is
	 * not set until an actual reply from a browser (e.g. using {@link BrowserInfoPage} is set.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
	 * If you do have a need to subclass, you may subclass {@link #init(boolean, FilterConfig)}
	 * 
	 * @see jakarta.servlet.Filter#init(jakarta.servlet.FilterConfig)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
	 * If you do have a need to subclass, you may subclass {@link #init(boolean, FilterConfig)}
	 * 
	 * @see jakarta.servlet.Filter#init(jakarta.servlet.FilterConfig)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.Filter#destroy()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.Filter#doFilter(jakarta.servlet.ServletRequest,
	 *      jakarta.servlet.ServletResponse, jakarta.servlet.FilterChain)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.Filter#doFilter(jakarta.servlet.ServletRequest,
	 *      jakarta.servlet.ServletResponse, jakarta.servlet.FilterChain)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
	/**
	 * @see jakarta.servlet.Filter#doFilter(jakarta.servlet.ServletRequest,
	 *      jakarta.servlet.ServletResponse, jakarta.servlet.FilterChain)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WicketFilter.java`
#### Snippet
```java
	/**
	 * @see jakarta.servlet.Filter#doFilter(jakarta.servlet.ServletRequest,
	 *      jakarta.servlet.ServletResponse, jakarta.servlet.FilterChain)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Application#getApplicationKey()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Application#logEventTarget(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Application#logEventTarget(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

	/**
	 * Returns the provider for {@link org.apache.wicket.ajax.AjaxRequestTarget} objects.
	 * 
	 * @return the provider for {@link org.apache.wicket.ajax.AjaxRequestTarget} objects.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
	 * Returns the provider for {@link org.apache.wicket.ajax.AjaxRequestTarget} objects.
	 * 
	 * @return the provider for {@link org.apache.wicket.ajax.AjaxRequestTarget} objects.
	 */
	public Function<Page, AjaxRequestTarget> getAjaxRequestTargetProvider()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Application#logResponseTarget(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.Application#logResponseTarget(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
 * @see jakarta.servlet.Filter
 * @see jakarta.servlet.FilterConfig
 * @see jakarta.servlet.ServletContext
 * 
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

	/**
	 * Returns the registered {@link org.apache.wicket.ajax.AjaxRequestTarget.IListener} objects.
	 * 
	 * @return the registered {@link org.apache.wicket.ajax.AjaxRequestTarget.IListener} objects.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java
	 * Returns the registered {@link org.apache.wicket.ajax.AjaxRequestTarget.IListener} objects.
	 * 
	 * @return the registered {@link org.apache.wicket.ajax.AjaxRequestTarget.IListener} objects.
	 */
	public AjaxRequestTargetListenerCollection getAjaxRequestTargetListeners()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/WebApplication.java`
#### Snippet
```java

	/**
	 * Sets the provider for {@link org.apache.wicket.ajax.AjaxRequestTarget} objects.
	 * 
	 * @param ajaxRequestTargetProvider
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ForwardAttributes.java`
#### Snippet
```java
 * See documentation for the following request attributes for the values stored in this object:
 * <ul>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_CONTEXT_PATH}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_PATH_INFO}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_QUERY_STRING}</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ForwardAttributes.java`
#### Snippet
```java
 * <ul>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_CONTEXT_PATH}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_PATH_INFO}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_QUERY_STRING}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_REQUEST_URI}</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ForwardAttributes.java`
#### Snippet
```java
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_CONTEXT_PATH}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_PATH_INFO}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_QUERY_STRING}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_REQUEST_URI}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_SERVLET_PATH}</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ForwardAttributes.java`
#### Snippet
```java
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_PATH_INFO}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_QUERY_STRING}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_REQUEST_URI}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_SERVLET_PATH}</li>
 * </ul>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/ForwardAttributes.java`
#### Snippet
```java
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_QUERY_STRING}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_REQUEST_URI}</li>
 * <li>{@link jakarta.servlet.RequestDispatcher#FORWARD_SERVLET_PATH}</li>
 * </ul>
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/FilterFactoryManager.java`
#### Snippet
```java

	/**
	 * @see java.lang.Iterable#iterator()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getHeader(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getHeader(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getHeaderNames()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getDateHeader(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getDateHeader(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getHeaders(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getHeaders(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getIntHeader(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapper.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.http.HttpServletRequestWrapper#getIntHeader(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/WicketSessionFilter.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.Filter#init(jakarta.servlet.FilterConfig)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/WicketSessionFilter.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.Filter#init(jakarta.servlet.FilterConfig)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/WicketSessionFilter.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.Filter#doFilter(jakarta.servlet.ServletRequest,
	 *      jakarta.servlet.ServletResponse, jakarta.servlet.FilterChain)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/WicketSessionFilter.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.Filter#doFilter(jakarta.servlet.ServletRequest,
	 *      jakarta.servlet.ServletResponse, jakarta.servlet.FilterChain)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/WicketSessionFilter.java`
#### Snippet
```java
	/**
	 * @see jakarta.servlet.Filter#doFilter(jakarta.servlet.ServletRequest,
	 *      jakarta.servlet.ServletResponse, jakarta.servlet.FilterChain)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/WicketSessionFilter.java`
#### Snippet
```java
	/**
	 * @see jakarta.servlet.Filter#doFilter(jakarta.servlet.ServletRequest,
	 *      jakarta.servlet.ServletResponse, jakarta.servlet.FilterChain)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/WicketSessionFilter.java`
#### Snippet
```java

	/**
	 * @see jakarta.servlet.Filter#destroy()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/SecuredRemoteAddressRequestWrapperFactory.java`
#### Snippet
```java
 * <td>IP addresses for which {@link ServletRequest#isSecure()} must return <code>true</code></td>
 * <td>Comma delimited list of regular expressions (in the syntax supported by the
 * {@link java.util.regex.Pattern} library)</td>
 * <td>Class A, B and C <a href="http://en.wikipedia.org/wiki/Private_network">private network IP
 * address blocks</a> : 10\.\d{1,3}\.\d{1,3}\.\d{1,3}, 192\.168\.\d{1,3}\.\d{1,3},
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java

	/**
	 * Invocation handler for proxy interface of {@link jakarta.servlet.ServletRegistration.Dynamic}.
	 * This class intercepts invocation for method {@link jakarta.servlet.ServletRegistration.Dynamic#getMappings}
	 * and returns the servlet name.
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockServletContext.java`
#### Snippet
```java
	/**
	 * Invocation handler for proxy interface of {@link jakarta.servlet.ServletRegistration.Dynamic}.
	 * This class intercepts invocation for method {@link jakarta.servlet.ServletRegistration.Dynamic#getMappings}
	 * and returns the servlet name.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemPathService.java`
#### Snippet
```java
/**
 * Gets the actual path for a specific file system. Have a look into
 * {@link org.apache.wicket.resource.FileSystemJarPathService} to find a reference implementation.
 * 
 * @see org.apache.wicket.resource.FileSystemJarPathService
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/FileSystemPathService.java`
#### Snippet
```java
 * {@link org.apache.wicket.resource.FileSystemJarPathService} to find a reference implementation.
 * 
 * @see org.apache.wicket.resource.FileSystemJarPathService
 * @author Tobias Soloschenko
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/IScopeAwareTextResourceProcessor.java`
#### Snippet
```java

/**
 * A {@link org.apache.wicket.resource.ITextResourceCompressor} that receives
 * the scope class and the resource name as a context information that it can
 * use for the processing of the resource
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/IPropertiesFactory.java`
#### Snippet
```java
 * </p>
 * 
 * @see org.apache.wicket.resource.Properties
 * 
 * @author Juergen Donnerstag
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/Properties.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java

	/**
	 * Parses the string representation of a {@link java.util.Locale} (for example 'en_GB').
	 * 
	 * @param locale
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
	 * 
	 * @param locale
	 * 		the string representation of a {@link java.util.Locale}
	 * @return the corresponding {@link java.util.Locale} instance
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
	 * @param locale
	 * 		the string representation of a {@link java.util.Locale}
	 * @return the corresponding {@link java.util.Locale} instance
	 */
	public static Locale parseLocale(String locale)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.util.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
	 * @return string read from resource stream
	 * 
	 * @see #readString(org.apache.wicket.util.resource.IResourceStream, java.nio.charset.Charset)
	 */
	public static String readString(IResourceStream resourceStream)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio.charset` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/ResourceUtil.java`
#### Snippet
```java
	 * @return string read from resource stream
	 * 
	 * @see #readString(org.apache.wicket.util.resource.IResourceStream, java.nio.charset.Charset)
	 */
	public static String readString(IResourceStream resourceStream)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/XmlFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadWicketProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/XmlFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadWicketProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/XmlFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#getFileExtension()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/XmlFilePropertiesLoader.java`
#### Snippet
```java
	/**
	 * @throws IOException
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadJavaProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/XmlFilePropertiesLoader.java`
#### Snippet
```java
	/**
	 * @throws IOException
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadJavaProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/IsoPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#getFileExtension()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/IsoPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadWicketProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/IsoPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadWicketProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/IsoPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadJavaProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/IsoPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadJavaProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/UtfPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadJavaProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/UtfPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadJavaProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/UtfPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#getFileExtension()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/UtfPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadWicketProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/UtfPropertiesFilePropertiesLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesLoader#loadWicketProperties(java.io.InputStream)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapperFactory.java`
#### Snippet
```java
 * <td>RemoteIPInternalProxy</td>
 * <td>Comma delimited list of regular expressions (in the syntax supported by the
 * {@link java.util.regex.Pattern} library)</td>
 * <td>10\.\d{1,3}\.\d{1,3}\.\d{1,3}, 192\.168\.\d{1,3}\.\d{1,3},
 * 172\\.(?:1[6-9]|2\\d|3[0-1]).\\d{1,3}.\\d{1,3}, 169\.254\.\d{1,3}\.\d{1,3},
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/XForwardedRequestWrapperFactory.java`
#### Snippet
```java
 * <td>RemoteIPTrustedProxy</td>
 * <td>Comma delimited list of regular expressions (in the syntax supported by the
 * {@link java.util.regex.Pattern} library)</td>
 * <td>&nbsp;</td>
 * </tr>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/PropertiesFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesFactory#clearCache()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/PropertiesFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesFactory#addListener(org.apache.wicket.resource.IPropertiesChangeListener)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/PropertiesFactory.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.IPropertiesFactory#addListener(org.apache.wicket.resource.IPropertiesChangeListener)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ResourceBundleReference.java`
#### Snippet
```java
 * A resource reference that wraps another resource to make it into a bundle. The resources that are
 * provided by the wrapped reference, have to be added with
 * {@link #addProvidedResources(org.apache.wicket.markup.head.HeaderItem...)}. Normally, you will
 * have to register this bundle in {@link ResourceBundles} under
 * {@link Application#getResourceBundles()}. Dependencies are inherited from the provided resources
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ReplacementResourceBundleReference.java`
#### Snippet
```java
 * for resource replacements
 *
 * @see org.apache.wicket.protocol.http.WebApplication#addResourceReplacement(org.apache.wicket.request.resource.CssResourceReference, org.apache.wicket.request.resource.ResourceReference)
 * @see org.apache.wicket.protocol.http.WebApplication#addResourceReplacement(org.apache.wicket.request.resource.JavaScriptResourceReference, org.apache.wicket.request.resource.ResourceReference)
 * @since 6.13
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.resource` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/bundles/ReplacementResourceBundleReference.java`
#### Snippet
```java
 *
 * @see org.apache.wicket.protocol.http.WebApplication#addResourceReplacement(org.apache.wicket.request.resource.CssResourceReference, org.apache.wicket.request.resource.ResourceReference)
 * @see org.apache.wicket.protocol.http.WebApplication#addResourceReplacement(org.apache.wicket.request.resource.JavaScriptResourceReference, org.apache.wicket.request.resource.ResourceReference)
 * @since 6.13
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource.loader` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.loader.IStringResourceLoader#loadStringResource(org.apache.wicket.Component,
	 *      java.lang.String, java.util.Locale, java.lang.String, java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.resource.loader.IStringResourceLoader#loadStringResource(org.apache.wicket.Component,
	 *      java.lang.String, java.util.Locale, java.lang.String, java.lang.String)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.resource.loader.IStringResourceLoader#loadStringResource(org.apache.wicket.Component,
	 *      java.lang.String, java.util.Locale, java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.resource.loader.IStringResourceLoader#loadStringResource(org.apache.wicket.Component,
	 *      java.lang.String, java.util.Locale, java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.resource.loader.IStringResourceLoader#loadStringResource(org.apache.wicket.Component,
	 *      java.lang.String, java.util.Locale, java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/resource/loader/ComponentStringResourceLoader.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.resource.loader.IStringResourceLoader#loadStringResource(org.apache.wicket.Component,
	 *      java.lang.String, java.util.Locale, java.lang.String, java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/response/NullResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.Response#write(CharSequence)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	 *
	 * @return The url
	 * @see jakarta.servlet.http.HttpServletRequest#getRequestURL()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java
	 *
	 * @return The path value
	 * @see jakarta.servlet.http.HttpServletRequest#getRequestURI()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/response/ByteArrayResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.Response#write(CharSequence)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/response/ByteArrayResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.Response#getOutputStream()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/response/ByteArrayResponse.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.request.Response#reset()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.http` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
	 *
	 * @see Duration#ZERO
	 * @see org.apache.wicket.request.http.WebResponse#MAX_CACHE_DURATION
	 * @return {@code this} object for chaining
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary, and can be replaced with an import
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java

	/** The factory to be used for the property files */
	private org.apache.wicket.resource.IPropertiesFactory propertiesFactory;

	/** Filesystem Path to search for resources */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
 * functionality required to access localized resources. For many localization problems, even this
 * will not be required, as there are convenience methods available to all components:
 * {@link org.apache.wicket.Component#getString(String key)} and
 * {@link org.apache.wicket.Component#getString(String key, org.apache.wicket.model.IModel model)}.
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
 * will not be required, as there are convenience methods available to all components:
 * {@link org.apache.wicket.Component#getString(String key)} and
 * {@link org.apache.wicket.Component#getString(String key, org.apache.wicket.model.IModel model)}.
 * <p>
 * <i>stringResourceLoaders </i>- A chain of <code>IStringResourceLoader</code> instances that are
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
	 * Wicket-1992). In case you really need it, a good value for placeholder would e.g. be "$up$".
	 * Resources additionally are protected by a
	 * {@link org.apache.wicket.markup.html.IPackageResourceGuard IPackageResourceGuard}
	 * implementation such as {@link org.apache.wicket.markup.html.PackageResourceGuard} which you
	 * may use or extend based on your needs.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary, and can be replaced with an import
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
	 * @return PropertiesFactory
	 */
	public org.apache.wicket.resource.IPropertiesFactory getPropertiesFactory()
	{
		if (propertiesFactory == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
	 * Placeholder string for '..' within resource urls (which will be crippled by the browser and
	 * not work anymore). Note that by default the placeholder string is <code>::</code>. Resources
	 * are protected by a {@link org.apache.wicket.markup.html.IPackageResourceGuard
	 * IPackageResourceGuard} implementation such as
	 * {@link org.apache.wicket.markup.html.PackageResourceGuard} which you may use or extend based
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.resource` is unnecessary, and can be replaced with an import
in `wicket-core/src/main/java/org/apache/wicket/settings/ResourceSettings.java`
#### Snippet
```java
	 * @return {@code this} object for chaining
	 */
	public ResourceSettings setPropertiesFactory(org.apache.wicket.resource.IPropertiesFactory factory)
	{
		propertiesFactory = factory;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.pageStore` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/pageStore/DefaultPageContext.java`
#### Snippet
```java
	
	/**
	 * @see org.apache.wicket.pageStore.IPageContext#getSessionId(boolean)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.javascript` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/javascript/DefaultJavaScriptCompressor.java`
#### Snippet
```java
{
	/**
	 * @see org.apache.wicket.javascript.IJavaScriptCompressor#compress(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/javascript/DefaultJavaScriptCompressor.java`
#### Snippet
```java
{
	/**
	 * @see org.apache.wicket.javascript.IJavaScriptCompressor#compress(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/validation/RawValidationError.java`
#### Snippet
```java
 *
 * @since 6.0
 * @see org.apache.wicket.Component#error(java.io.Serializable)
 * @see org.apache.wicket.Session#error(java.io.Serializable)
 * @see org.apache.wicket.markup.html.panel.FeedbackPanel
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/validation/RawValidationError.java`
#### Snippet
```java
 * @since 6.0
 * @see org.apache.wicket.Component#error(java.io.Serializable)
 * @see org.apache.wicket.Session#error(java.io.Serializable)
 * @see org.apache.wicket.markup.html.panel.FeedbackPanel
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/PatternValidator.java`
#### Snippet
```java
 * further messages keys.
 * 
 * @see java.util.regex.Pattern
 * 
 * @author Jonathan Locke
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/validation/validator/PatternValidator.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/application/IComponentOnConfigureListener.java`
#### Snippet
```java
/**
 * Listener interface that receives messages when components are configured (after
 * {@link org.apache.wicket.Component#onConfigure()} and
 * {@link org.apache.wicket.behavior.Behavior#onConfigure(org.apache.wicket.Component)}
 * methods have been executed).
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/application/IComponentOnConfigureListener.java`
#### Snippet
```java
 * Listener interface that receives messages when components are configured (after
 * {@link org.apache.wicket.Component#onConfigure()} and
 * {@link org.apache.wicket.behavior.Behavior#onConfigure(org.apache.wicket.Component)}
 * methods have been executed).
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/application/IComponentOnConfigureListener.java`
#### Snippet
```java
	/**
	 * Called for every component after its {@code Component#onConfigure()} and
	 * {@link org.apache.wicket.behavior.Behavior#onConfigure(org.apache.wicket.Component)}
	 * methods have been executed).
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/IAuthorizationStrategy.java`
#### Snippet
```java
	{
		/**
		 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isInstantiationAuthorized(java.lang.Class)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/IAuthorizationStrategy.java`
#### Snippet
```java
	{
		/**
		 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isInstantiationAuthorized(java.lang.Class)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/IAuthorizationStrategy.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
		 *      org.apache.wicket.authorization.Action)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/IAuthorizationStrategy.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
		 *      org.apache.wicket.authorization.Action)
		 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/IAuthorizationStrategy.java`
#### Snippet
```java
		/**
		 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
		 *      org.apache.wicket.authorization.Action)
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/CompoundAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/CompoundAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/CompoundAuthorizationStrategy.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/CompoundAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isInstantiationAuthorized(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/CompoundAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isInstantiationAuthorized(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/action/ActionAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/action/ActionAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/action/ActionAuthorizationStrategy.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isActionAuthorized(org.apache.wicket.Component,
	 *      org.apache.wicket.authorization.Action)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization.strategies.page` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/page/SimplePageAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.strategies.page.AbstractPageAuthorizationStrategy#isPageAuthorized(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/page/SimplePageAuthorizationStrategy.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.authorization.strategies.page.AbstractPageAuthorizationStrategy#isPageAuthorized(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.authorization` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/page/AbstractPageAuthorizationStrategy.java`
#### Snippet
```java
{
	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isInstantiationAuthorized(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/page/AbstractPageAuthorizationStrategy.java`
#### Snippet
```java
{
	/**
	 * @see org.apache.wicket.authorization.IAuthorizationStrategy#isInstantiationAuthorized(java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-core/src/main/java/org/apache/wicket/validation/ValidationError.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary, and can be replaced with an import
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/WorldClockPage.java`
#### Snippet
```java
		add(new AjaxLink<Void>("stop") {
			@Override
			public void onClick(org.apache.wicket.ajax.AjaxRequestTarget target) {
				timer.stop(target);
			}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary, and can be replaced with an import
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/WorldClockPage.java`
#### Snippet
```java
		add(new AjaxLink<Void>("restart") {
			@Override
			public void onClick(org.apache.wicket.ajax.AjaxRequestTarget target) {
				timer.restart(target);
			}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.examples.frames` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/LeftFrame.java`
#### Snippet
```java
/**
 * The left frame. This page is not bookmarkable, but it's instance is created by
 * {@link org.apache.wicket.examples.frames.BodyFrame} and hold in the same page map as index. It
 * uses the frameTarget object as a shared model; this page updates that model, and as the Index
 * uses that to set the frame tag, any changes to it should be reflected with the next render.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/frames/LeftFrame.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.html.link.Link#onClick()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/upload/MultiUploadPage.java`
#### Snippet
```java

		/**
		 * @see org.apache.wicket.markup.html.form.Form#onSubmit()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ButtonPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.Button}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/wizard/NewUserWizard.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.Wizard#onFinish()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.wizard` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/wizard/NewUserWizard.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.extensions.wizard.Wizard#onCancel()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/CheckBoxPage.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.panel` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/FragmentPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.panel.Fragment}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ExternalLinkPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.link.ExternalLink}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/LinkPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.link.Link}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.mapper.parameter` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/BookmarkablePage.java`
#### Snippet
```java
/**
 * Example page that cannot be bookmarked. A page is bookmarkable when it has a public default
 * constructor and/or a public constructor with a {@link org.apache.wicket.request.mapper.parameter.PageParameters} argument.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/Address.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.examples` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/CheckGroupPage.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.examples.WicketExamplePage#explain()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/Person.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.basic` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/LabelPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.basic.Label}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ListChoicePage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.ListChoice}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.basic` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/MultiLineLabelPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.basic.MultiLineLabel}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TextAreaPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.TextArea}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TextFieldPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.TextField}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SubmitLinkPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.Form}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.link` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/BookmarkablePageLinkPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.link.BookmarkablePageLink}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.tabs` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/TabbedPanelPage.java`
#### Snippet
```java
 * Reference page for TabbedPanel wicket-extensions component
 * 
 * @see org.apache.wicket.extensions.markup.html.tabs.TabbedPanel
 * 
 * @author igor
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/CheckBoxMultipleChoicePage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.CheckBoxMultipleChoice}.
 * 
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/CheckBoxMultipleChoicePage.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.form.select` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.extensions.markup.html.form.select.Select}.
 * 
 * @author Igor Vaynberg (ivaynberg)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/SelectPage.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/ListMultipleChoicePage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.ListMultipleChoice}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/FormPage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.Form}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/DropDownChoicePage.java`
#### Snippet
```java
			 * id.
			 * 
			 * @see org.apache.wicket.markup.html.form.IChoiceRenderer#getIdValue(java.lang.Object,
			 *      int)
			 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/DropDownChoicePage.java`
#### Snippet
```java
			 * id.
			 * 
			 * @see org.apache.wicket.markup.html.form.IChoiceRenderer#getIdValue(java.lang.Object,
			 *      int)
			 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/DropDownChoicePage.java`
#### Snippet
```java
			 * Gets the display value that is visible to the end user.
			 * 
			 * @see org.apache.wicket.markup.html.form.IChoiceRenderer#getDisplayValue(java.lang.Object)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/DropDownChoicePage.java`
#### Snippet
```java
			 * Gets the display value that is visible to the end user.
			 * 
			 * @see org.apache.wicket.markup.html.form.IChoiceRenderer#getDisplayValue(java.lang.Object)
			 */
			@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.form` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/DropDownChoicePage.java`
#### Snippet
```java

/**
 * Page with examples on {@link org.apache.wicket.markup.html.form.DropDownChoice}.
 * 
 * @author Eelco Hillenius
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/compref/RadioChoicePage.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/hangman/Word.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/hangman/Word.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/library/Book.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater.data` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactDataProvider.java`
#### Snippet
```java
	 * returns total number of contacts in the database
	 * 
	 * @see org.apache.wicket.markup.repeater.data.IDataProvider#size()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater.data` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactDataProvider.java`
#### Snippet
```java
	 * wraps retrieved contact pojo with a wicket model
	 * 
	 * @see org.apache.wicket.markup.repeater.data.IDataProvider#model(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactDataProvider.java`
#### Snippet
```java
	 * wraps retrieved contact pojo with a wicket model
	 * 
	 * @see org.apache.wicket.markup.repeater.data.IDataProvider#model(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.repeater.data` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/ContactDataProvider.java`
#### Snippet
```java
	 * <code>first+count</code>
	 * 
	 * @see org.apache.wicket.markup.repeater.data.IDataProvider#iterator(long, long)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.model` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/DetachableContactModel.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.model.LoadableDetachableModel#load()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/DetachableContactModel.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.markup.repeater.ReuseIfModelsEqualStrategy
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/DetachableContactModel.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.markup.repeater.ReuseIfModelsEqualStrategy
	 * @see java.lang.Object#equals(java.lang.Object)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.html.navigation.paging` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/PagingPage.java`
#### Snippet
```java
 * Page that demonstrates a paging dataview
 * 
 * @see org.apache.wicket.markup.html.navigation.paging.PagingNavigator
 * @author igor
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.extensions.markup.html.repeater.data.table` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/repeater/DataTablePage.java`
#### Snippet
```java
 * demo page for the datatable component
 * 
 * @see org.apache.wicket.extensions.markup.html.repeater.data.table.DefaultDataTable
 * @author igor
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/FormInputModel.java`
#### Snippet
```java

		/**
		 * @see java.lang.Object#toString()
		 */
		@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/authorization/User.java`
#### Snippet
```java

	/**
	 * @see java.lang.Object#toString()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/authorization/RolesApplication.java`
#### Snippet
```java

	/**
	 * @see org.apache.wicket.protocol.http.WebApplication#newSession(org.apache.wicket.request.Request,
	 *      org.apache.wicket.request.Response)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/authorization/RolesApplication.java`
#### Snippet
```java
	/**
	 * @see org.apache.wicket.protocol.http.WebApplication#newSession(org.apache.wicket.request.Request,
	 *      org.apache.wicket.request.Response)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.ajax` is unnecessary, and can be replaced with an import
in `wicket-examples/src/main/java/org/apache/wicket/examples/authorization/pages/AnnotationsPanelsPage.java`
#### Snippet
```java
		add(new AjaxLink<Void>("link")
		{
			public void onClick(org.apache.wicket.ajax.AjaxRequestTarget target)
			{
				showDummy = !showDummy;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/CustomHomeMapper.java`
#### Snippet
```java
	 * Removes the leading segment if it a valid Locale
	 *
	 * @see org.apache.wicket.core.request.mapper.HomePageMapper#mapRequest(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/CustomHomeMapper.java`
#### Snippet
```java
	 * Removes the leading segment if it a valid Locale
	 *
	 * @see org.apache.wicket.core.request.mapper.HomePageMapper#mapRequest(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/CustomHomeMapper.java`
#### Snippet
```java
	 * {@link HomePageMapper#getCompatibilityScore(Request)}
	 *
	 * @see org.apache.wicket.core.request.mapper.HomePageMapper#getCompatibilityScore(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/CustomHomeMapper.java`
#### Snippet
```java
	 * {@link HomePageMapper#getCompatibilityScore(Request)}
	 *
	 * @see org.apache.wicket.core.request.mapper.HomePageMapper#getCompatibilityScore(org.apache.wicket.request.Request)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.core.request.mapper` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/CustomHomeMapper.java`
#### Snippet
```java
	 * Sets the current session Locale as first segment in the Url.
	 *
	 * @see org.apache.wicket.core.request.mapper.HomePageMapper#mapHandler(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/requestmapper/CustomHomeMapper.java`
#### Snippet
```java
	 * Sets the current session Locale as first segment in the Url.
	 *
	 * @see org.apache.wicket.core.request.mapper.HomePageMapper#mapHandler(org.apache.wicket.request.IRequestHandler)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/customresourceloading/PageWithCustomLoading.java`
#### Snippet
```java
	 * {@link IMarkupCacheKeyProvider}.
	 * 
	 * @see org.apache.wicket.markup.IMarkupCacheKeyProvider#getCacheKey(org.apache.wicket.MarkupContainer,
	 *      java.lang.Class)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/customresourceloading/PageWithCustomLoading.java`
#### Snippet
```java
	 * {@link IMarkupCacheKeyProvider}.
	 * 
	 * @see org.apache.wicket.markup.IMarkupCacheKeyProvider#getCacheKey(org.apache.wicket.MarkupContainer,
	 *      java.lang.Class)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-examples/src/main/java/org/apache/wicket/examples/customresourceloading/PageWithCustomLoading.java`
#### Snippet
```java
	 * 
	 * @see org.apache.wicket.markup.IMarkupCacheKeyProvider#getCacheKey(org.apache.wicket.MarkupContainer,
	 *      java.lang.Class)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketProcessor.java`
#### Snippet
```java
{
	/**
	 * Called when then {@link org.apache.wicket.protocol.ws.api.IWebSocketSession} is being opened: to allow to configure
	 * the underlying web socket session.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketProcessor.java`
#### Snippet
```java
	 *
	 * @param webSocketSession
	 * 			the {@link org.apache.wicket.protocol.ws.api.IWebSocketSession}
	 * @param application
	 *          the {@link org.apache.wicket.protocol.http.WebApplication}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.http` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketProcessor.java`
#### Snippet
```java
	 * 			the {@link org.apache.wicket.protocol.ws.api.IWebSocketSession}
	 * @param application
	 *          the {@link org.apache.wicket.protocol.http.WebApplication}
	 */
	default void onOpen(IWebSocketSession webSocketSession, final WebApplication application) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketRequestHandler.java`
#### Snippet
```java
	 *      the text message to push to the client if the web socket connection is open
	 * @return
	 *      a {@link java.util.concurrent.Future} representing the send operation. Or null if connection is closed.
	 */
	Future<Void> pushAsync(CharSequence message);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketRequestHandler.java`
#### Snippet
```java
	 *      the timeout for operation
	 * @return
	 * 		a {@link java.util.concurrent.Future} representing the send operation. Or null if connection is closed.
	 */
	Future<Void> pushAsync(CharSequence message, long timeout);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketRequestHandler.java`
#### Snippet
```java
	 *      the timeout for operation
	 * @return
	 * 		a {@link java.util.concurrent.Future} representing the send operation. Or null if connection is closed.
	 */
	Future<Void> pushAsync(byte[] message, int offset, int length, long timeout);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketRequestHandler.java`
#### Snippet
```java
	 *      how many bytes to read from the message
	 * @return
	 * 		a {@link java.util.concurrent.Future} representing the send operation. Or null if connection is closed.
	 */
	Future<Void> pushAsync(byte[] message, int offset, int length);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketSessionConfigurer.java`
#### Snippet
```java

    /**
     * Allows to configure {@link org.apache.wicket.protocol.ws.api.IWebSocketSession}
     *
     * @param webSocketSession
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketSessionConfigurer.java`
#### Snippet
```java
     *
     * @param webSocketSession
     *        The {@link org.apache.wicket.protocol.ws.api.IWebSocketSession}
     */
    void configureSession(IWebSocketSession webSocketSession);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketSessionConfigurer.java`
#### Snippet
```java

/**
 * Allows to configure the {@link org.apache.wicket.protocol.ws.api.IWebSocketSession}
 */
public interface IWebSocketSessionConfigurer {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketRequestMapper.java`
#### Snippet
```java

/**
 * An {@link org.apache.wicket.request.IRequestMapper} that is used to set a custom
 * {@link org.apache.wicket.request.IRequestHandler} that broadcasts the
 * {@link org.apache.wicket.protocol.ws.api.event.WebSocketPayload}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketRequestMapper.java`
#### Snippet
```java
/**
 * An {@link org.apache.wicket.request.IRequestMapper} that is used to set a custom
 * {@link org.apache.wicket.request.IRequestHandler} that broadcasts the
 * {@link org.apache.wicket.protocol.ws.api.event.WebSocketPayload}
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketRequestHandler.java`
#### Snippet
```java
	 * @return if <code>true</code> then EMPTY partial updates will se send. If <code>false</code> then EMPTY
	 *    partial updates will be skipped. A possible use case is: a page receives and a push event but no one is
	 *    listening to it, and nothing is added to {@link org.apache.wicket.protocol.ws.api.WebSocketRequestHandler}
	 *    thus no real push to client is needed. For compatibilities this is set to true. Thus EMPTY updates are sent
	 *    by default.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketMessageBroadcastHandler.java`
#### Snippet
```java
	 *
	 * @param pageId
	 *          The id of the page if {@link org.apache.wicket.protocol.ws.api.WebSocketBehavior}
	 *          or {@value org.apache.wicket.protocol.ws.api.AbstractWebSocketProcessor#NO_PAGE_ID} if using a resource
	 * @param resourceName
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketMessageBroadcastHandler.java`
#### Snippet
```java
	 * @param pageId
	 *          The id of the page if {@link org.apache.wicket.protocol.ws.api.WebSocketBehavior}
	 *          or {@value org.apache.wicket.protocol.ws.api.AbstractWebSocketProcessor#NO_PAGE_ID} if using a resource
	 * @param resourceName
	 *          The name of the shared {@link org.apache.wicket.protocol.ws.api.WebSocketResource}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketMessageBroadcastHandler.java`
#### Snippet
```java
	 *          or {@value org.apache.wicket.protocol.ws.api.AbstractWebSocketProcessor#NO_PAGE_ID} if using a resource
	 * @param resourceName
	 *          The name of the shared {@link org.apache.wicket.protocol.ws.api.WebSocketResource}
	 * @param payload
	 *          The payload to broadcast
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketMessageBroadcastHandler.java`
#### Snippet
```java

/**
 * An {@link org.apache.wicket.request.IRequestHandler} that broadcasts the payload to the
 * page/resource
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketPushBroadcaster.java`
#### Snippet
```java
	 * This method can be invoked from any thread, even a non-wicket thread. By default all processing
	 * is done in the caller thread. Use
	 * {@link WebSocketSettings#setWebSocketPushMessageExecutor(org.apache.wicket.protocol.ws.concurrent.Executor)}
	 * to move processing to background threads.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api.registry` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketPushBroadcaster.java`
#### Snippet
```java
	 *
	 * @param connectionsFilter
	 *         the {@link org.apache.wicket.protocol.ws.api.registry.IWebSocketConnectionRegistry.IConnectionsFilter}
	 * @param message
	 *			The push message event
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketPushBroadcaster.java`
#### Snippet
```java
	 * This method can be invoked from any thread, even a non-wicket thread. By default all processing
	 * is done in the caller thread. Use
	 * {@link WebSocketSettings#setWebSocketPushMessageExecutor(org.apache.wicket.protocol.ws.concurrent.Executor)}
	 * to move processing to background threads.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketPushBroadcaster.java`
#### Snippet
```java
	 * This method can be invoked from any thread, even a non-wicket thread. By default all processing
	 * is done in the caller thread. Use
	 * {@link WebSocketSettings#setWebSocketPushMessageExecutor(org.apache.wicket.protocol.ws.concurrent.Executor)}
	 * to move processing to background threads.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketPushBroadcaster.java`
#### Snippet
```java
	 * This method can be invoked from any thread, even a non-wicket thread. By default all processing
	 * is done in the caller thread. Use
	 * {@link WebSocketSettings#setWebSocketPushMessageExecutor(org.apache.wicket.protocol.ws.concurrent.Executor)}
	 * to move processing to background threads.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
	 * Override to return a context. By default, this is the page class name.
	 *
	 * @param component the {@link org.apache.wicket.Component}
	 * @return the context for this websocket behavior.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
	 *
	 * Contributes WebSocket initialization code that will
	 * work with {@link org.apache.wicket.protocol.ws.api.WebSocketResource}
	 *
	 * To use WebSocketResource the application have to setup the
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
	 *
	 *  @param resourceName
	 *          the name of the shared {@link org.apache.wicket.protocol.ws.api.WebSocketResource}
	 */
	public BaseWebSocketBehavior(String resourceName)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
	 *
	 * Contributes WebSocket initialization code that will
	 * work with {@link org.apache.wicket.protocol.ws.api.WebSocketResource}
	 *
	 * To use WebSocketResource the application have to setup the
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
	 *
	 *  @param resourceName
	 *          the name of the shared {@link org.apache.wicket.protocol.ws.api.WebSocketResource}
	 *  @param connectionToken
	 *  		an optional token to support connections to the same resource from multiple browser tabs
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/BaseWebSocketBehavior.java`
#### Snippet
```java
	 *
	 * Contributes WebSocket initialization code that will
	 * work with {@link org.apache.wicket.protocol.ws.api.WebSocketBehavior}
	 */
	protected BaseWebSocketBehavior()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketConnection.java`
#### Snippet
```java
     * @param length
     *      how much data to read
     * @return a {@link java.util.concurrent.Future} representing the send operation
     */
    Future<Void> sendMessageAsync(byte[] message, int offset, int length);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketConnection.java`
#### Snippet
```java
     * @param message
     *      the text message
     * @return a {@link java.util.concurrent.Future} representing the send operation
     *
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketConnection.java`
#### Snippet
```java
     * @param timeout
     *      the timeout for operation
     * @return a {@link java.util.concurrent.Future} representing the send operation
     */
    Future<Void> sendMessageAsync(String message, long timeout);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/IWebSocketConnection.java`
#### Snippet
```java
     * @param timeout
     *      the timeout for operation
     * @return a {@link java.util.concurrent.Future} representing the send operation
     */
    Future<Void> sendMessageAsync(byte[] message, int offset, int length, long timeout);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketConnectionOriginFilter.java`
#### Snippet
```java
	 * The list of whitelisted domains which are allowed to initiate a websocket connection. This
	 * list will be eventually used by the
	 * {@link org.apache.wicket.protocol.ws.api.IWebSocketConnectionFilter} to abort potentially
	 * unsafe connections
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketConnectionOriginFilter.java`
#### Snippet
```java
	 * The list of whitelisted domains which are allowed to initiate a websocket connection. This
	 * list will be eventually used by the
	 * {@link org.apache.wicket.protocol.ws.api.IWebSocketConnectionFilter} to abort potentially
	 * unsafe connections. Example domain names might be:
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api.registry` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/registry/IWebSocketConnectionRegistry.java`
#### Snippet
```java
	 * 			 the web application to look in
	 * @param connectionsFilter
	 * 			the {@link org.apache.wicket.protocol.ws.api.registry.IWebSocketConnectionRegistry.IConnectionsFilter}
	 *
	 * @return collection of web socket connections that match certain filter
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/concurrent/Executor.java`
#### Snippet
```java
	 * Runs a simple task that doesn't return a result
	 *
	 * @see     java.lang.Thread#run()
	 */
	void run(Runnable command);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.http` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java

	/**
	 * A factory method for the {@link org.apache.wicket.request.http.WebResponse}
	 * that should be used to write the response back to the client/browser
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.http` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java

	/**
	 * A factory method for the {@link org.apache.wicket.request.http.WebRequest}
	 * that should be used in the WebSocket processing request cycle
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
	/**
	 * Sets the IWebSocketSessionConfigurer
	 * @param socketSessionConfigurer A non-null {@link org.apache.wicket.protocol.ws.api.IWebSocketSessionConfigurer}
	 */
	public void setSocketSessionConfigurer(IWebSocketSessionConfigurer socketSessionConfigurer) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.request.http` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java

	/**
	 * A factory method for the {@link org.apache.wicket.request.http.WebResponse}
	 * that should be used to write the response back to the client/browser
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java

	/**
	 * @return returns the {@link org.apache.wicket.protocol.ws.api.IWebSocketSessionConfigurer}
	 */
	public IWebSocketSessionConfigurer getSocketSessionConfigurer() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java

	/**
	 * A factory method for creating instances of {@link org.apache.wicket.protocol.ws.api.WebSocketRequestHandler}
	 * for processing a web socket request
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java

	/**
	 * A {@link org.apache.wicket.protocol.ws.api.IWebSocketSessionConfigurer} that allows to configure
	 * {@link org.apache.wicket.protocol.ws.api.IWebSocketSession}s.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/WebSocketSettings.java`
#### Snippet
```java
	/**
	 * A {@link org.apache.wicket.protocol.ws.api.IWebSocketSessionConfigurer} that allows to configure
	 * {@link org.apache.wicket.protocol.ws.api.IWebSocketSession}s.
	 */
	private IWebSocketSessionConfigurer socketSessionConfigurer = new IWebSocketSessionConfigurer() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.protocol.ws.api` is unnecessary and can be removed
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/AbstractWebSocketProcessor.java`
#### Snippet
```java
	 *      the message to broadcast
     * @param connection
     * 	    the {@link org.apache.wicket.protocol.ws.api.IWebSocketConnection}
     * @param asynchronousPush
     * 	    whether asynchronous pus is used or not
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket.markup.head` is unnecessary and can be removed
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/VelocityJavaScriptContributor.java`
#### Snippet
```java
/**
 * A derivation of VelocityContributor that uses
 * {@link org.apache.wicket.markup.head.IHeaderResponse#render(org.apache.wicket.markup.head.HeaderItem)}
 */
public class VelocityJavaScriptContributor extends VelocityContributor
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wicket` is unnecessary and can be removed
in `wicket-velocity/src/main/java/org/apache/wicket/velocity/Initializer.java`
#### Snippet
```java

/**
 * An implementation of {@link org.apache.wicket.IInitializer} for the Velocity Runtime Singleton.
 * If Application is an instance of WebApplication, Initializer will retrieve
 * "velocityPropertiesFolder" as an initparam to point to the directory the properties file lives
```

## RuleId[id=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `wicket-util/src/main/java/org/apache/wicket/util/license/JavaLicenseHeaderHandler.java`
#### Snippet
```java
		catch (Exception e)
		{
			e.printStackTrace();
			throw new AssertionError(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `wicket-examples/src/main/java/org/apache/wicket/examples/forminput/FormInputModel.java`
#### Snippet
```java
		catch (MalformedURLException e)
		{
			e.printStackTrace();
		}
		lines.add(new Line("line one"));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/charts/ChartUpdater.java`
#### Snippet
```java
				catch (Exception e)
				{
					e.printStackTrace();
					break;
				}
```

## RuleId[id=ReplaceNullCheck]
### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
		super(null, model);

		if (parameters == null)
		{
			pageParameters = new PageParameters();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `wicket-core/src/main/java/org/apache/wicket/Localizer.java`
#### Snippet
```java

		// ConcurrentHashMap does not allow null values
		if (string == null)
		{
			cache.put(cacheKey, NULL_VALUE);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/WicketObjects.java`
#### Snippet
```java
	public static void setObjectSizeOfStrategy(IObjectSizeOfStrategy objectSizeOfStrategy)
	{
		if (objectSizeOfStrategy == null)
		{
			WicketObjects.objectSizeOfStrategy = new SerializingObjectSizeOfStrategy();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
	public static void setLocator(final Application application, final IPropertyLocator locator)
	{
		if (application == null)
		{
			applicationToLocators.put(PropertyResolver.class, locator);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `wicket-core/src/main/java/org/apache/wicket/request/handler/resource/ResourceStreamRequestHandler.java`
#### Snippet
```java
	{
		resource.setFileName(fileName);
		if (contentDisposition != null)
		{
			resource.setContentDisposition(contentDisposition);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `wicket-core/src/main/java/org/apache/wicket/resource/PropertiesFactory.java`
#### Snippet
```java
			if (propertiesCache != null)
			{
				if (properties == null)
				{
					// Could not locate properties, store a placeholder
```

## RuleId[id=Anonymous2MethodRef]
### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
		if (transformBodyOnly == false)
		{
			execute(new Runnable()
			{
				@Override
				public void run()
```

## RuleId[id=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `wicket-util/src/main/java/org/apache/wicket/util/lang/EnumeratedType.java`
#### Snippet
```java

		// If no list exists
		if (valueList == null)
		{
			// create lazily
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `wicket-util/src/main/java/org/apache/wicket/util/license/ApacheLicenseHeaderTestCase.java`
#### Snippet
```java
								List<File> files = badFiles.get(licenseHeaderHandler);

								if (files == null)
								{
									files = new ArrayList<>();
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `wicket-request/src/main/java/org/apache/wicket/request/HttpHeaderCollection.java`
#### Snippet
```java
		List<Object> values = headers.get(key);

		if (values == null)
		{
			values = new ArrayList<>();
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `wicket-core/src/main/java/org/apache/wicket/mock/MockRequestParameters.java`
#### Snippet
```java
	{
		List<StringValue> list = parameters.get(name);
		if (list == null)
		{
			list = new ArrayList<StringValue>(1);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `wicket-core/src/main/java/org/apache/wicket/mock/MockWebRequest.java`
#### Snippet
```java
	{
		List<Object> values = headers.get(name);
		if (values == null)
		{
			values = new ArrayList<Object>();
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `wicket-core/src/main/java/org/apache/wicket/markup/html/internal/HtmlHeaderContainer.java`
#### Snippet
```java

		List<String> componentScope = renderedComponentsPerScope.get(scope);
		if (componentScope == null)
		{
			componentScope = new ArrayList<>();
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/servlet/MultipartServletWebRequestImpl.java`
#### Snippet
```java
			{
				List<FileItem> fileItems = files.get(item.getFieldName());
				if (fileItems == null)
				{
					fileItems = new ArrayList<>();
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/mock/MockHttpServletRequest.java`
#### Snippet
```java

		List<UploadedFile> filesPerField = uploadedFiles.get(fieldName);
		if (filesPerField == null)
		{
			filesPerField = new ArrayList<>();
```

## RuleId[id=Convert2Lambda]
### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `wicket-util/src/main/java/org/apache/wicket/util/io/BOMInputStream.java`
#### Snippet
```java
     * Compares ByteOrderMark objects in descending length order.
     */
    private static final Comparator<ByteOrderMark> ByteOrderMarkLengthComparator = new Comparator<ByteOrderMark>() {

        @Override
```

### Convert2Lambda
Anonymous new FileFilter() can be replaced with lambda
in `wicket-util/src/main/java/org/apache/wicket/util/markup/xhtml/WellFormedXmlTestCase.java`
#### Snippet
```java
	}

	private static final FileFilter fileFilter = new FileFilter()
	{
		@Override
		public boolean accept(File pathname)
```

### Convert2Lambda
Anonymous new ICode() can be replaced with lambda
in `wicket-util/src/main/java/org/apache/wicket/util/watch/ModificationWatcher.java`
#### Snippet
```java
		task = new Task("ModificationWatcher");

		task.run(pollFrequency, new ICode()
		{
			@Override
			public void run(final Logger log)
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `wicket-util/src/main/java/org/apache/wicket/util/thread/Task.java`
#### Snippet
```java
		if (!isStarted)
		{
			final Runnable runnable = new Runnable()
			{
				@Override
				public void run()
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/stateless/StatelessChecker.java`
#### Snippet
```java
		if (mustCheck(component))
		{
			final IVisitor<Component, Component> visitor = new IVisitor<Component, Component>()
			{
				@Override
				public void component(final Component comp, final IVisit<Component> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/AjaxLazyLoadPanel.java`
#### Snippet
```java
			setUpdateInterval(Duration.ofMillis(Long.MAX_VALUE));
			
			getComponent().getPage().visitChildren(AjaxLazyLoadPanel.class, new IVisitor<AjaxLazyLoadPanel<?>, Void>()
			{
				@Override
				public void component(AjaxLazyLoadPanel<?> panel, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `wicket-devutils/src/main/java/org/apache/wicket/devutils/inspector/EnhancedPageView.java`
#### Snippet
```java

				// Sort the children list, putting behaviors first
				Collections.sort(children, new Comparator<TreeNode>()
				{
					@Override
					public int compare(TreeNode o1, TreeNode o2)
```

### Convert2Lambda
Anonymous new IRequestHandler() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/ajax/markup/html/autocomplete/AutoCompleteBehavior.java`
#### Snippet
```java
	protected final void onRequest(final String val, final RequestCycle requestCycle)
	{
		IRequestHandler target = new IRequestHandler()
		{
			@Override
			public void respond(final IRequestCycle requestCycle)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/Select.java`
#### Snippet
```java
		final StringBuilder builder = new StringBuilder();

		visitChildren(SelectOption.class, new IVisitor<SelectOption<T>, Void>()
		{
			@Override
			public void component(SelectOption<T> option, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, SelectOption\>() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/form/select/Select.java`
#### Snippet
```java
			{
				SelectOption<T> option = visitChildren(SelectOption.class,
					new IVisitor<SelectOption<T>, SelectOption<T>>()
					{
						@Override
						public void component(SelectOption<T> option, IVisit<SelectOption<T>> visit)
```

### Convert2Lambda
Anonymous new ILinkRenderStrategy() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/basic/DefaultLinkParser.java`
#### Snippet
```java
	 * Renders &lt;a href="mailto:{EMAIL}"&gt;{EMAIL}&lt;/a&gt;
	 */
	public static final ILinkRenderStrategy EMAIL_RENDER_STRATEGY = new ILinkRenderStrategy()
	{
		@Override
		public String buildLink(final String linkTarget)
```

### Convert2Lambda
Anonymous new ILinkRenderStrategy() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/basic/DefaultLinkParser.java`
#### Snippet
```java
	 * the email address with html entities.
	 */
	public static final ILinkRenderStrategy ENCRYPTED_EMAIL_RENDER_STRATEGY = new ILinkRenderStrategy()
	{
		@Override
		public String buildLink(final String linkTarget)
```

### Convert2Lambda
Anonymous new ILinkRenderStrategy() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/basic/DefaultLinkParser.java`
#### Snippet
```java
	 * Renders &lt;a href="{URL}"&gt;{URL}&lt;/a&gt;
	 */
	public static final ILinkRenderStrategy URL_RENDER_STRATEGY = new ILinkRenderStrategy()
	{
		@Override
		public String buildLink(final String linkTarget)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/data/table/DataTable.java`
#### Snippet
```java
			toolbars.configure();

			Boolean visible = toolbars.visitChildren(new IVisitor<Component, Boolean>()
			{
				@Override
				public void component(Component object, IVisit<Boolean> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/NestedTree.java`
#### Snippet
```java
	{
		final IModel<T> model = getProvider().model(node);
		visitChildren(Node.class, new IVisitor<Node<T>, Void>()
		{
			@Override
			public void component(Node<T> node, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/NestedTree.java`
#### Snippet
```java
	{
		final IModel<T> model = getProvider().model(t);
		visitChildren(BranchItem.class, new IVisitor<BranchItem<T>, Void>()
		{
			@Override
			public void component(BranchItem<T> branch, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/tree/TableTree.java`
#### Snippet
```java
	{
		final IModel<T> model = getProvider().model(t);
		table.getBody().visitChildren(Item.class, new IVisitor<Item<T>, Void>()
		{
			@Override
			public void component(Item<T> item, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java
					// try to search downwards to match the id
					// NOTE unfortunately, we can't rely on the path pre 2.0
					Component c = parent.visitChildren(new IVisitor<Component, Component>()
					{
						@Override
						public void component(final Component component,
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/breadcrumb/panel/BreadCrumbParticipantDelegate.java`
#### Snippet
```java
						// not found... do a reverse search (upwards)
						c = parent.visitParents(MarkupContainer.class,
							new IVisitor<MarkupContainer, Component>()
							{
								@Override
								public void component(final MarkupContainer component,
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
		if (getApplication().getDebugSettings().getComponentUseCheck())
		{
			visitChildren(new IVisitor<Component, Void>()
			{
				@Override
				public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
	protected final void internalOnModelChanged()
	{
		visitChildren(new IVisitor<Component, Void>()
		{
			@Override
			public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
		{
			Component statefulComponent = visitChildren(Component.class,
				new IVisitor<Component, Component>()
				{
					@Override
					public void component(final Component component, final IVisit<Component> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
		final StringBuilder buffer = new StringBuilder();
		buffer.append("Page ").append(getId());
		visitChildren(new IVisitor<Component, Void>()
		{
			@Override
			public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
			final List<Component> unrenderedComponents = new ArrayList<Component>();
			final StringBuilder buffer = new StringBuilder();
			renderedContainer.visitChildren(new IVisitor<Component, Void>()
			{
				@Override
				public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/ajax/form/AjaxFormValidatingBehavior.java`
#### Snippet
```java
	protected final void addFeedbackPanels(final AjaxRequestTarget target)
	{
		form.getPage().visitChildren(IFeedback.class, new IVisitor<Component, Void>()
		{
			@Override
			public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new ICode() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/core/util/watch/Nio2ModificationWatcher.java`
#### Snippet
```java
		task = new Task("Wicket-ModificationWatcher-NIO2");

		task.run(pollFrequency, new ICode() {
			@Override
			public void run(final Logger log)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	{
		super.fireInitialize();
		visitChildren(new IVisitor<Component, Void>()
		{
			@Override
			public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	{
		super.onEnabledStateChanged();
		visitChildren(new IVisitor<Component, Void>()
		{
			@Override
			public void component(Component component, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	{
		super.onVisibleStateChanged();
		visitChildren(new IVisitor<Component, Void>()
		{
			@Override
			public void component(Component component, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		if (previous instanceof IComponentInheritedModel)
		{
			visitChildren(new IVisitor<Component, Void>()
			{
				@Override
				public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
		if (page != null)
		{
			page.visitChildren(new IVisitor<Component, Void>()
			{
				@Override
				public void component(Component component, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/core/request/handler/AbstractPartialPageRequestHandler.java`
#### Snippet
```java
        Args.notNull(childCriteria, "childCriteria");

        parent.visitChildren(childCriteria, new IVisitor<Component, Void>()
        {
            @Override
            public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/util/tester/WicketTesterHelper.java`
#### Snippet
```java
		if (page != null)
		{
			page.visitChildren(new IVisitor<Component, Void>()
			{
				@Override
				public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/util/tester/FormTester.java`
#### Snippet
```java

		// fill blank String for Text Component.
		workingForm.visitFormComponents(new IVisitor<FormComponent<?>, Void>()
		{
			@Override
			public void component(final FormComponent<?> formComponent, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IChangeListener() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupCache.java`
#### Snippet
```java
			if (watcher != null)
			{
				watcher.add(markupResourceStream, new IChangeListener<IModifiable>()
				{
					@Override
					public void onChange(IModifiable modifiable)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/MarkupUtil.java`
#### Snippet
```java

		final boolean rtn[] = new boolean[] { true };
		page.visitChildren(MarkupContainer.class, new IVisitor<MarkupContainer, Void>()
		{
			@Override
			public void component(final MarkupContainer comp, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java

		// and for all its children
		page.visitChildren(Component.class, new IVisitor<Component, Void>()
		{
			@Override
			public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
	{
		
		IVisitor<Component, Void> clearer = new IVisitor<Component, Void>()
		{
			@Override
			public void component(Component component, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/util/tester/BaseWicketTester.java`
#### Snippet
```java
		final Set<String> currentParameterNamesSet = postParameters.getParameterNames();

		form.visitFormComponents(new IVisitor<FormComponent<?>, Void>()
		{
			@Override
			public void component(final FormComponent<?> formComponent, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/StatelessForm.java`
#### Snippet
```java
		if (parameters != null)
		{
			visitFormComponents(new IVisitor<FormComponent<?>, Void>()
			{
				@Override
				public void component(final FormComponent<?> formComponent, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/WebPage.java`
#### Snippet
```java
		// search for HtmlHeaderContainer in the first level of children or deeper
		// if there are transparent resolvers used
		HtmlHeaderContainer header = visitChildren(new IVisitor<Component, HtmlHeaderContainer>()
		{
			@Override
			public void component(final Component component, final IVisit<HtmlHeaderContainer> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
		final Collection<T> ts = getModelObject();

		visitChildren(Check.class, new IVisitor<Check<T>, Void>()
		{
			@Override
			public void component(Check<T> check, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new org.apache.wicket.util.visit.IVisitor, Check\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/CheckGroup.java`
#### Snippet
```java
				{
					Check<T> checkbox = visitChildren(Check.class,
						new org.apache.wicket.util.visit.IVisitor<Check<T>, Check<T>>()
						{
							@Override
							public void component(final Check<T> check, final IVisit<Check<T>> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/AutoLabelResolver.java`
#### Snippet
```java
		{
			component = container.visitChildren(Component.class,
				new IVisitor<Component, Component>()
				{
					@Override
					public void component(Component child, IVisit<Component> visit)
```

### Convert2Lambda
Anonymous new org.apache.wicket.util.visit.IVisitor, Radio\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java
			// retrieve the selected single radio choice component
			Radio<T> choice = visitChildren(Radio.class,
				new org.apache.wicket.util.visit.IVisitor<Radio<T>, Radio<T>>()
				{

					@Override
```

### Convert2Lambda
Anonymous new IVisitor, String\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/RadioGroup.java`
#### Snippet
```java
	protected String getModelValue()
	{
		String radioValue = visitChildren(Radio.class, new IVisitor<Radio<T>, String>()
		{
			@Override
			public void component(Radio<T> radio, IVisit<String> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	private void updateNestedFormComponentModels()
	{
		visitChildren(Form.class, new IVisitor<Form<?>, Void>()
		{
			@Override
			public void component(final Form<?> form, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	{
		// Visit all the (visible) form components and clear the input on each.
		visitFormComponentsPostOrder(new IVisitor<FormComponent<?>, Void>()
		{
			@Override
			public void component(final FormComponent<?> formComponent, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	private void inputChanged()
	{
		visitFormComponentsPostOrder(new IVisitor<FormComponent<?>, Void>()
		{
			@Override
			public void component(final FormComponent<?> formComponent, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
		// collect all forms innermost to outermost before any hierarchy is changed
		final List<Form<?>> forms = Generics.newArrayList(3);
		Visits.visitPostOrder(processingForm, new IVisitor<Form<?>, Void>()
		{
			@Override
			public void component(Form<?> form, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	{
		// Visit all the form components and validate each
		visitFormComponentsPostOrder(new IVisitor<FormComponent<?>, Void>()
		{
			@Override
			public void component(final FormComponent<?> formComponent, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
		// update auto labels if we are inside an ajax request
		getRequestCycle().find(AjaxRequestTarget.class).ifPresent(target -> {
			visitChildren(FormComponent.class, new IVisitor<FormComponent<?>, Void>()
			{
				@Override
				public void component(FormComponent<?> component, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	{
		// call invalidate methods of all nested form components
		visitFormComponentsPostOrder(new IVisitor<FormComponent<?>, Void>()
		{
			@Override
			public void component(final FormComponent<?> formComponent, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

		IFormSubmittingComponent submittingComponent = getPage().visitChildren(
			IFormSubmittingComponent.class, new IVisitor<Component, IFormSubmittingComponent>()
			{
				@Override
				public void component(final Component component,
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	{
		onValidateModelObjects();
		visitChildren(Form.class, new IVisitor<Form<?>, Void>()
		{
			@Override
			public void component(Form<?> form, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Bytes\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
		if (maxSize[0] == null)
		{
			visitChildren(Form.class, new IVisitor<Form<?>, Bytes>()
			{
				@Override
				public void component(Form<?> component, IVisit<Bytes> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
		{
			Boolean anyEmbeddedMultipart = visitChildren(Component.class,
					new IVisitor<Component, Boolean>()
					{
						@Override
						public void component(final Component component, final IVisit<Boolean> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	{
		// Check ALL children for error messages irrespective of FormComponents or not
		Boolean error = visitChildren(Component.class, new IVisitor<Component, Boolean>()
		{
			@Override
			public void component(final Component component, final IVisit<Boolean> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

		// Search for FormComponents like TextField etc.
		visitFormComponents(new IVisitor<FormComponent<?>, Void>()
		{
			@Override
			public void component(final FormComponent<?> formComponent, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	{
		// call valid methods of all nested form components
		visitFormComponentsPostOrder(new IVisitor<FormComponent<?>, Void>()
		{
			@Override
			public void component(final FormComponent<?> formComponent, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java

		// invoke Form#onSubmit(..) going from innermost to outermost
		Visits.visitPostOrder(processingForm, new IVisitor<Form<?>, Void>()
		{
			@Override
			public void component(Form<?> form, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	private void validateNestedForms()
	{
		Visits.visitPostOrder(this, new IVisitor<Form<?>, Void>()
		{
			@Override
			public void component(final Form<?> form, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor, Void\>() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	private void markNestedFormComponentsValid()
	{
		visitChildren(Form.class, new IVisitor<Form<?>, Void>()
		{
			@Override
			public void component(final Form<?> form, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
		Form<?> formToProcess = findFormToProcess(submitter);

		visitChildren(Form.class, new IVisitor<Component, Void>()
		{
			@Override
			public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/debug/PageView.java`
#### Snippet
```java
		final List<ComponentData> data = new ArrayList<ComponentData>();

		page.visitChildren(new IVisitor<Component, Void>()
		{
			@Override
			public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/debug/PageView.java`
#### Snippet
```java
			// Get the components data and fill and sort the list
			data = new ArrayList<ComponentData>(getComponentData(page));
			Collections.sort(data, new Comparator<ComponentData>()
			{
				@Override
				public int compare(ComponentData o1, ComponentData o2)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/html/panel/AbstractMarkupSourcingStrategy.java`
#### Snippet
```java
		
		//collect all "transparent" (i.e. component resolvers) children
		container.visitChildren(IComponentResolver.class, new IVisitor<MarkupContainer, Void>()
		{
			@Override
			public void component(MarkupContainer child, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/HtmlHeaderResolver.java`
#### Snippet
```java
			if (container instanceof WebPage)
			{
				HtmlHeaderContainer header = container.visitChildren(new IVisitor<Component, HtmlHeaderContainer>()
				{
					@Override
					public void component(final Component component, final IVisit<HtmlHeaderContainer> visit)
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
		if (transformBodyOnly == false)
		{
			execute(new Runnable()
			{
				@Override
				public void run()
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/transformer/AbstractOutputTransformerContainer.java`
#### Snippet
```java
		if (transformBodyOnly == true)
		{
			execute(new Runnable()
			{
				@Override
				public void run()
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/renderStrategy/ParentFirstHeaderRenderStrategy.java`
#### Snippet
```java
		{
			// Visit the children with parent first, than children
			((MarkupContainer)rootComponent).visitChildren(new IVisitor<Component, Void>()
			{
				@Override
				public void component(final Component component, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/markup/renderStrategy/AbstractHeaderRenderStrategy.java`
#### Snippet
```java

		// Visit the siblings of the enclosure to attempt and find the controller of the enclosure
		Component enclosureController = enclosure.getParent().visitChildren(new IVisitor<Component, Component>() {
			@Override
			public void component(Component object, IVisit<Component> visit) {
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/feedback/FeedbackCollector.java`
#### Snippet
```java
		if (component != null && recursive && component instanceof MarkupContainer)
		{
			((MarkupContainer)component).visitChildren(new IVisitor<Component, Void>()
			{
				@Override
				public void component(Component object, IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AjaxEnclosureListener.java`
#### Snippet
```java
						target.add(enclosure);
						visit.dontGoDeeper();
						enclosure.visitChildren(new IVisitor<Component, Void>() {
							@Override
							public void component(Component descendant, IVisit<Void> visit) {
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/AjaxEnclosureListener.java`
#### Snippet
```java
		final List<String> keysToRemove = new ArrayList<>();

		target.getPage().visitChildren(InlineEnclosure.class, new IVisitor<InlineEnclosure, Void>()
		{
			@Override
			public void component(final InlineEnclosure enclosure, final IVisit<Void> visit)
```

### Convert2Lambda
Anonymous new IChangeListener() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/resource/PropertiesFactory.java`
#### Snippet
```java
		final IModificationWatcher watcher)
	{
		watcher.add(resourceStream, new IChangeListener<IModifiable>()
		{
			@Override
			public void onChange(IModifiable modifiable)
```

### Convert2Lambda
Anonymous new IUnauthorizedComponentInstantiationListener() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/authorization/strategies/page/SimplePageAuthorizationStrategy.java`
#### Snippet
```java
		// Handle unauthorized access to pages
		Application.get().getSecuritySettings().setUnauthorizedComponentInstantiationListener(
			new IUnauthorizedComponentInstantiationListener()
			{
				@Override
				public void onUnauthorizedInstantiation(final Component component)
```

### Convert2Lambda
Anonymous new IChangeListener() can be replaced with lambda
in `wicket-core/src/main/java/org/apache/wicket/application/ReloadingClassLoader.java`
#### Snippet
```java
			{
				log.info("Watching changes of class " + clzFile);
				watcher.add(clzFile, new IChangeListener<IModifiable>()
				{
					@Override
					public void onChange(IModifiable modifiable)
```

### Convert2Lambda
Anonymous new IComponentInitializationListener() can be replaced with lambda
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/AjaxApplication.java`
#### Snippet
```java
		getResourceSettings().setThrowExceptionOnMissingResource(false);

		getComponentInitializationListeners().add(new IComponentInitializationListener()
		{
			@Override
			public void onInitialize(Component component)
```

### Convert2Lambda
Anonymous new IModel() can be replaced with lambda
in `wicket-examples/src/main/java/org/apache/wicket/examples/tree/content/MultiLineLabelContent.java`
#### Snippet
```java
		final IModel<Foo> model)
	{
		return new MultiLineLabel(id, new IModel<String>()
		{
			@Override
			public String getObject()
```

### Convert2Lambda
Anonymous new IModel() can be replaced with lambda
in `wicket-examples/src/main/java/org/apache/wicket/examples/hellobrowser/HelloBrowser.java`
#### Snippet
```java
		add(new MultiLineLabel("clientinfo", properties.toString()));

		IModel<String> clientTimeModel = new IModel<String>()
		{
			@Override
			public String getObject()
```

### Convert2Lambda
Anonymous new IModel() can be replaced with lambda
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajaxhellobrowser/AjaxHelloBrowser.java`
#### Snippet
```java
	public AjaxHelloBrowser()
	{
		final MultiLineLabel clientInfo = new MultiLineLabel("clientinfo", new IModel<String>()
		{
			@Override
			public String getObject()
```

### Convert2Lambda
Anonymous new IModel() can be replaced with lambda
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajaxhellowbrowserondomready/AjaxHelloBrowserOnDomReady.java`
#### Snippet
```java
	public AjaxHelloBrowserOnDomReady()
	{
		final MultiLineLabel clientInfo = new MultiLineLabel("clientinfo", new IModel<String>()
		{
			@Override
			public String getObject()
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketMessageBroadcastHandler.java`
#### Snippet
```java
		final Application application = Application.get();

		final Runnable action = new Runnable()
		{
			@Override
			public void run()
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketPushBroadcaster.java`
#### Snippet
```java
		for (final IWebSocketConnection wsConnection : wsConnections)
		{
			executor.run(new Runnable()
			{
				@Override
				public void run()
```

### Convert2Lambda
Anonymous new IVisitor() can be replaced with lambda
in `wicket-cdi/src/main/java/org/apache/wicket/cdi/AutoConversationManager.java`
#### Snippet
```java
	protected boolean hasConversationalComponent(Page page)
	{
		Boolean hasConversational = Visits.visit(page, new IVisitor<Component, Boolean>()
		{
			@Override
			public void component(Component object, IVisit<Boolean> visit)
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
			if (".".equals(s))
			{
				continue;
			}
			else if ("..".equals(s) && !this.segments.isEmpty())
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `wicket-core/src/main/java/org/apache/wicket/Page.java`
#### Snippet
```java
						transparentContainerChildren.add(component);
						iterator.remove();
						continue outerWhile;
					}
				}
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `wicket-core/src/main/java/org/apache/wicket/core/util/string/JavaScriptStripper.java`
#### Snippet
```java
				}
				result.append(c);
				continue;
			}
		}
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `wicket-core/src/main/java/org/apache/wicket/markup/parser/filter/RootMarkupFilter.java`
#### Snippet
```java
			if (type == HttpTagType.BODY)
			{
				continue;
			}
			else if (type == HttpTagType.TAG)
```

## RuleId[id=RegExpUnnecessaryNonCapturingGroup]
### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java

	private static final String EMAIL_PATTERN = "(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t]"
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:"
		+ "\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:("
		+ "?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ "
		+ "\\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\0"
		+ "31]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:"
		+ "(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z"
		+ "|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)"
		+ "?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\"
		+ "r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?["
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ")+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)"
		+ "*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+"
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r"
		+ "\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:"
		+ "\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t"
		+ "]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031"
		+ "]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\]("
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?"
		+ ":\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)|(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?"
		+ ":(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?"
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*:(?:(?:\\r\\n)?[ \\t])*(?:(?:(?:[^()<>@,;:\\\".\\[\\] "
		+ "\\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|"
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>"
		+ "@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\""
		+ "(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t]"
		+ ")*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ":[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\["
		+ "\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:[^()<>@,;:\\\".\\[\\] \\000-"
		+ "\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|("
		+ "?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)?[ \\t])*(?:@(?:[^()<>@,;"
		+ ":\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[(["
		+ "^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\""
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?(?:[^()<>@,;:\\\".\\[\\] \\0"
		+ "00-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\"
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ ".|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,"
		+ ";:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\"(?"
		+ ":[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*))*@(?:(?:\\r\\n)?[ \\t])*"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)?[ \\t])*(?:["
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*\\>(?:(?:\\r\\n)?[ \\t])*)(?:,\\s*("
		+ "?:(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\\"
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:("
		+ "?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=["
		+ "\\[\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t"
		+ "])*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t"
		+ "])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*|(?:"
		+ "[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\"
		+ "]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)*\\<(?:(?:\\r\\n)"
		+ "?[ \\t])*(?:@(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\""
		+ "()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:\\r\\n)"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*))*)*:(?:(?:\\r\\n)?[ \\t])*)?"
		+ "(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\[\"()<>@,;:\\\"."
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:\\r\\n)?[ \\t])`
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/validation/validator/RfcCompliantEmailAddressValidator.java`
#### Snippet
```java
		+ "\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])*)(?:\\.(?:(?:"
		+ "\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])+|\\Z|(?=[\\["
		+ "\"()<>@,;:\\\".\\[\\]]))|\"(?:[^\\\"\\r\\\\]|\\\\.|(?:(?:\\r\\n)?[ \\t]))*\"(?:(?:\\r\\n)?[ \\t])"
		+ "*))*@(?:(?:\\r\\n)?[ \\t])*(?:[^()<>@,;:\\\".\\[\\] \\000-\\031]+(?:(?:(?:\\r\\n)?[ \\t])"
		+ "+|\\Z|(?=[\\[\"()<>@,;:\\\".\\[\\]]))|\\[([^\\[\\]\\r\\\\]|\\\\.)*\\](?:(?:\\r\\n)?[ \\t])*)(?:\\"
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy fields 'indexedParameters' and 'namedParameters'
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/parameter/PageParameters.java`
#### Snippet
```java
	 *          The parameters to copy from
	 */
	public PageParameters(final PageParameters copy)
	{
		if (copy != null)
```

### CopyConstructorMissesField
Copy constructor does not copy fields 'fragment' and 'contextRelative'
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
	 *            url being copied
	 */
	public Url(final Url url)
	{
		Args.notNull(url, "url");
```

### CopyConstructorMissesField
Copy constructor does not copy 7 fields
in `wicket-core/src/main/java/org/apache/wicket/markup/ComponentTag.java`
#### Snippet
```java
	 *            The ComponentTag tag which this wicket tag is based upon.
	 */
	public ComponentTag(final ComponentTag tag)
	{
		this(tag.getXmlTag());
```

### CopyConstructorMissesField
Copy constructor does not copy field 'checked'
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/TodoList.java`
#### Snippet
```java
		 *            the item to copy the values from.
		 */
		public TodoItem(TodoItem item)
		{
			text = item.text;
```

## RuleId[id=ClassCanBeRecord]
### ClassCanBeRecord
Class can be a record
in `wicket-guice/src/main/java/org/apache/wicket/guice/GuiceInjectorHolder.java`
#### Snippet
```java
 * are never serialized.
 */
public class GuiceInjectorHolder implements IClusterable
{
	private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-util/src/main/java/org/apache/wicket/util/visit/Visits.java`
#### Snippet
```java
	}

	private static class SingletonIterable<T> implements Iterable<T>
	{
		private final T singleton;
```

### ClassCanBeRecord
Class can be a record
in `wicket-util/src/main/java/org/apache/wicket/util/resource/ResourceUtils.java`
#### Snippet
```java
	 * 
	 */
	public static class PathLocale
	{
		/** */
```

### ClassCanBeRecord
Class can be a record
in `wicket-util/src/main/java/javax/servlet/http/HttpSession.java`
#### Snippet
```java
public interface HttpSession extends jakarta.servlet.http.HttpSession {
    
    class Impl implements javax.servlet.http.HttpSession {

        private final jakarta.servlet.http.HttpSession delegate;
```

### ClassCanBeRecord
Class can be a record
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxWebSocketFilter.java`
#### Snippet
```java
	 * the list of ignored paths
	 */
	private static class JavaxWebSocketFilterConfig implements FilterConfig
	{
		private final FilterConfig delegate;
```

### ClassCanBeRecord
Class can be a record
in `wicket-native-websocket/wicket-native-websocket-javax/src/main/java/org/apache/wicket/protocol/ws/javax/JavaxWebSocketConnection.java`
#### Snippet
```java
	}

	private static class CloseCode implements CloseReason.CloseCode
	{
		private final int code;
```

### ClassCanBeRecord
Class can be a record
in `wicket-util/src/main/java/javax/servlet/http/HttpServletRequest.java`
#### Snippet
```java
public interface HttpServletRequest extends jakarta.servlet.http.HttpServletRequest {

    class Impl implements HttpServletRequest {

        private final jakarta.servlet.http.HttpServletRequest delegate;
```

### ClassCanBeRecord
Class can be a record
in `wicket-util/src/main/java/javax/servlet/ServletContext.java`
#### Snippet
```java
public interface ServletContext extends jakarta.servlet.ServletContext {

    class Impl implements ServletContext {

        private final jakarta.servlet.ServletContext delegate;
```

### ClassCanBeRecord
Class can be a record
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/PageComponentInfo.java`
#### Snippet
```java
 * @author Matej Knopp
 */
public class PageComponentInfo
{
	private static final char SEPARATOR = '-';
```

### ClassCanBeRecord
Class can be a record
in `wicket-request/src/main/java/org/apache/wicket/request/mapper/info/ComponentInfo.java`
#### Snippet
```java
 * @author Matej Knopp
 */
public class ComponentInfo
{
	private static final char BEHAVIOR_INDEX_SEPARATOR = '.';
```

### ClassCanBeRecord
Class can be a record
in `wicket-request/src/main/java/org/apache/wicket/request/handler/logger/DelegateLogData.java`
#### Snippet
```java
 * @author Emond Papegaaij
 */
public class DelegateLogData implements ILogData
{
	private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-request/src/main/java/org/apache/wicket/request/Url.java`
#### Snippet
```java
	 * @author Matej Knopp
	 */
	public final static class QueryParameter implements Serializable
	{
		private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/captcha/CaptchaImageResource.java`
#### Snippet
```java
	 * variables here.
	 */
	private static final class CharAttributes implements IClusterable
	{
		private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/markup/html/repeater/util/SortParam.java`
#### Snippet
```java
 *            the type of the sort property
 */
public class SortParam<T> implements IClusterable
{
	private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-extensions/src/main/java/org/apache/wicket/extensions/requestlogger/JsonRequestLogger.java`
#### Snippet
```java
	 * A simple tuple for request and session.
	 */
	private static final class RequestSessionTuple implements Serializable
	{
		private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/PageReference.java`
#### Snippet
```java
 * @author igor.vaynberg
 */
public class PageReference implements IClusterable
{
	private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/core/util/crypt/KeyInSessionSunJceCryptFactory.java`
#### Snippet
```java
	}

    static final class CryptData implements IClusterable
	{
        private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/core/util/lang/PropertyResolver.java`
#### Snippet
```java
	 *
	 */
	private static final class ObjectWithGetAndSet
	{
		private final IGetAndSet getAndSet;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/core/util/objects/checker/CheckingObjectOutputStream.java`
#### Snippet
```java

	/** Holds information about the field and the resulting object being traced. */
	private static final class TraceSlot
	{
		private final String fieldDescription;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/MarkupContainer.java`
#### Snippet
```java
	 * be serializable but for e.g. determining the size of the component it has to be serializable.
	 */
	private static class RemovedChild implements Serializable
	{
		private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/core/request/mapper/AbstractBookmarkableMapper.java`
#### Snippet
```java
	 * @author Matej Knopp
	 */
	protected static final class UrlInfo
	{
		private final PageComponentInfo pageComponentInfo;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/markup/MarkupType.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class MarkupType implements Serializable
{
	private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/ValidationErrorFeedback.java`
#### Snippet
```java
 * @author Igor Vaynberg (ivaynberg)
 */
public class ValidationErrorFeedback implements IClusterable
{
	private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/upload/resource/FolderUploadsFileManager.java`
#### Snippet
```java
 * is no possibility that uploads get mixed up.
 */
public class FolderUploadsFileManager implements IUploadsFileManager
{

```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/markup/html/form/Form.java`
#### Snippet
```java
	 * @author Igor Vaynberg (ivaynberg)
	 */
	private static class FormModelUpdateVisitor implements IVisitor<Component, Void>
	{
		private final Form<?> formFilter;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/markup/resolver/AutoLinkResolver.java`
#### Snippet
```java
	 * Resolver object that returns the proper attribute value from component tags.
	 */
	private static final class TagReferenceResolver implements ITagReferenceResolver
	{
		/** the attribute to fetch. */
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/request/resource/PackageResource.java`
#### Snippet
```java
	}

	private static class CacheKey implements Serializable
	{
		private final String scopeName;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/request/resource/ResourceReference.java`
#### Snippet
```java
	 * @author Matej Knopp
	 */
	public static class UrlAttributes
	{
		private final Locale locale;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/protocol/http/BufferedWebResponse.java`
#### Snippet
```java
	}

	private static final class Action implements Comparable<Action>
	{
		private final ActionType type;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/resource/XmlFilePropertiesLoader.java`
#### Snippet
```java
 * @author Juergen Donnerstag
 */
public class XmlFilePropertiesLoader implements IPropertiesLoader
{
	private final String fileExtension;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/pageStore/CryptingPageStore.java`
#### Snippet
```java
	}

	private static class SessionData implements Serializable
	{
		private static final long serialVersionUID = 1L;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AbstractPersistentPageStore.java`
#### Snippet
```java
	 * Attribute held in session.
	 */
	private static class SessionAttribute implements Serializable, HttpSessionBindingListener
	{

```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/pageStore/disk/NestedFolders.java`
#### Snippet
```java
 * @author svenmeier
 */
public class NestedFolders
{
	private final File base;
```

### ClassCanBeRecord
Class can be a record
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AsynchronousPageStore.java`
#### Snippet
```java
	 * The consumer of {@link PendingAdd}s.
	 */
	private static class PageAddingRunnable implements Runnable
	{
		private static final Logger log = LoggerFactory.getLogger(PageAddingRunnable.class);
```

### ClassCanBeRecord
Class can be a record
in `wicket-examples/src/main/java/org/apache/wicket/examples/ajax/builtin/Clock.java`
#### Snippet
```java
	 * @author Igor Vaynberg (ivaynberg)
	 */
	private static class ClockModel implements IModel<String>
	{
		private final ZoneId zoneId;
```

### ClassCanBeRecord
Class can be a record
in `wicket-examples/src/main/java/org/apache/wicket/examples/websocket/progress/ProgressUpdater.java`
#### Snippet
```java
	 * A push message used to update progress.
	 */
	public static class ProgressUpdate implements IWebSocketPushMessage
	{

```

### ClassCanBeRecord
Class can be a record
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/ConnectionRejected.java`
#### Snippet
```java
 * A class used by {@link IWebSocketConnectionFilter}s when they need to reject a connection
 */
public class ConnectionRejected
{
	private final int code;
```

### ClassCanBeRecord
Class can be a record
in `wicket-native-websocket/wicket-native-websocket-core/src/main/java/org/apache/wicket/protocol/ws/api/WebSocketConnectionOriginFilter.java`
#### Snippet
```java
 * @author Gergely Nagy
 */
public class WebSocketConnectionOriginFilter implements IWebSocketConnectionFilter
{

```

## RuleId[id=ThreadStartInConstruction]
### ThreadStartInConstruction
Call to `start()` during object construction
in `wicket-core/src/main/java/org/apache/wicket/pageStore/AsynchronousPageStore.java`
#### Snippet
```java
		pageSavingThread = new Thread(savingRunnable, "Wicket-AsyncPageStore-PageSavingThread");
		pageSavingThread.setDaemon(true);
		pageSavingThread.start();
	}

```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `wicket-util/src/main/java/org/apache/wicket/util/value/CopyOnWriteValueMap.java`
#### Snippet
```java

	@Override
	public boolean equals(final Object o)
	{
		return wrapped.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `wicket-core/src/main/java/org/apache/wicket/util/template/TextTemplateDecorator.java`
#### Snippet
```java

	@Override
	public boolean equals(Object obj)
	{
		return decorated.equals(obj);
```

