# incubator-hugegraph-computer 
 
# Bad smells
I found 2637 bad smells with 427 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| StaticCallOnSubclass | 1650 | false |
| CodeBlock2Expr | 375 | true |
| IOResource | 83 | false |
| BoundedWildcard | 75 | false |
| ReturnNull | 44 | false |
| UnnecessarySuperQualifier | 35 | false |
| UtilityClassWithoutPrivateConstructor | 28 | true |
| AssignmentToStaticFieldFromInstanceMethod | 26 | false |
| FieldAccessedSynchronizedAndUnsynchronized | 24 | false |
| ConstantValue | 23 | false |
| IgnoreResultOfCall | 22 | false |
| Convert2MethodRef | 22 | false |
| UnstableApiUsage | 20 | false |
| RedundantMethodOverride | 17 | false |
| NonProtectedConstructorInAbstractClass | 16 | true |
| AssignmentToMethodParameter | 15 | false |
| EmptyMethod | 13 | false |
| DataFlowIssue | 13 | false |
| SynchronizeOnThis | 10 | false |
| ZeroLengthArrayInitialization | 10 | false |
| EqualsWithItself | 9 | false |
| EqualsBetweenInconvertibleTypes | 9 | false |
| NestedAssignment | 6 | false |
| RedundantFieldInitialization | 6 | false |
| PublicFieldAccessedInSynchronizedContext | 6 | false |
| UnnecessaryFullyQualifiedName | 6 | false |
| ObjectNotify | 5 | false |
| SizeReplaceableByIsEmpty | 5 | true |
| ShiftOutOfRange | 4 | false |
| WhileCanBeForeach | 3 | false |
| FunctionalExpressionCanBeFolded | 3 | false |
| InstanceofCatchParameter | 3 | false |
| UnusedAssignment | 3 | false |
| DynamicRegexReplaceableByCompiledPattern | 3 | false |
| KeySetIterationMayUseEntrySet | 2 | false |
| UnnecessaryQualifierForThis | 2 | false |
| FinalPrivateMethod | 2 | false |
| ReplaceAssignmentWithOperatorAssignment | 2 | false |
| MismatchedCollectionQueryUpdate | 2 | false |
| UnstableTypeUsedInSignature | 2 | false |
| UnnecessarySemicolon | 2 | false |
| SwitchStatementWithConfusingDeclaration | 2 | false |
| RedundantSuppression | 2 | false |
| TestCaseWithConstructor | 2 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 2 | false |
| MismatchedArrayReadWrite | 1 | false |
| UnnecessaryModifier | 1 | true |
| ConditionalBreakInInfiniteLoop | 1 | false |
| EmptyStatementBody | 1 | false |
| Java8ListReplaceAll | 1 | false |
| RedundantArrayCreation | 1 | true |
| StringBufferReplaceableByString | 1 | false |
| AccessStaticViaInstance | 1 | false |
| AssertBetweenInconvertibleTypes | 1 | false |
| IntegerMultiplicationImplicitCastToLong | 1 | false |
| PointlessBitwiseExpression | 1 | false |
| CallToStringConcatCanBeReplacedByOperator | 1 | false |
| ExceptionNameDoesntEndWithException | 1 | false |
| ArrayEquality | 1 | false |
| StringBufferReplaceableByStringBuilder | 1 | false |
| DeprecatedIsStillUsed | 1 | false |
| RedundantCollectionOperation | 1 | false |
| AbstractMethodCallInConstructor | 1 | false |
| UnnecessaryToStringCall | 1 | true |
| SystemOutErr | 1 | false |
| ConditionCoveredByFurtherCondition | 1 | false |
| ThrowablePrintStackTrace | 1 | false |
| BusyWait | 1 | false |
## RuleId[id=EqualsWithItself]
### EqualsWithItself
`compareTo()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(utf8Id3.compareTo(utf8Id2) > 0);
        Assert.assertTrue(utf8Id2.compareTo(utf8Id3) < 0);
        Assert.assertEquals(0, utf8Id2.compareTo(utf8Id2));
        Assert.assertTrue(utf8Id2.compareTo(utf8Id4) < 0);
        Assert.assertTrue(utf8Id4.compareTo(utf8Id2) > 0);
```

### EqualsWithItself
`equals()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
    public void testEquals() {
        BooleanValue value1 = new BooleanValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new BooleanValue(false)));
        Assert.assertFalse(value1.equals(new BooleanValue(true)));
```

### EqualsWithItself
`equals()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        NullValue value1 = NullValue.get();

        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(NullValue.get()));

```

### EqualsWithItself
`equals()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
    public void testEquals() {
        FloatValue value1 = new FloatValue(0f);
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new FloatValue(0f)));
        Assert.assertFalse(value1.equals(new FloatValue(1)));
```

### EqualsWithItself
`equals()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
    public void testEquals() {
        IntValue intValue1 = new IntValue();
        Assert.assertTrue(intValue1.equals(intValue1));
        Assert.assertTrue(intValue1.equals(new IntValue(0)));
        Assert.assertFalse(intValue1.equals(new IntValue(1)));
```

### EqualsWithItself
`equals()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
    public void testEquals() {
        LongValue value1 = new LongValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new LongValue(0L)));
        Assert.assertFalse(value1.equals(new LongValue(1L)));
```

### EqualsWithItself
`equals()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
    public void testEquals() {
        DoubleValue value1 = new DoubleValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new DoubleValue(0d)));
        Assert.assertFalse(value1.equals(new DoubleValue(1)));
```

### EqualsWithItself
`equals()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(202f));

        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value2.equals(value2));

```

### EqualsWithItself
`equals()` called on itself
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value2.equals(value2));

        Assert.assertTrue(value1.equals(value1.copy()));
```

## RuleId[id=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `requiredOptions` are read, but never written to
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
    public static synchronized Config updateWithRequiredOptions(
                                      Object... options) {
        Object[] requiredOptions = new Object[] {
        };
        Object[] allOptions = new Object[requiredOptions.length +
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `computer-driver/src/main/java/org/apache/hugegraph/computer/driver/JobObserver.java`
#### Snippet
```java
     * This observer will notified by every superstep.
     */
    public void onJobStateChanged(JobState state);
}

```

## RuleId[id=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/CombineSubKvOuterSortFlusher.java`
#### Snippet
```java
        KvEntry currentKv = sorter.currentKv();

        while (true) {
            currentKv.key().write(this.output);
            long position = this.output.position();
```

## RuleId[id=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/AbstractK8sTest.java`
#### Snippet
```java
        crd.createOrReplace();
        crd.waitUntilReady(2, TimeUnit.SECONDS);
        Assert.assertNotNull(crd.get());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/CsvStructGraphOutputTest.java`
#### Snippet
```java
            @SuppressWarnings("deprecation")
            String text = FileUtils.readFileToString(file);
            Assert.assertEquals("100,[[66],[998,999]],{true,127,-0.01,16383," +
                                "100,0.1,1000000,10000000000}" +
                                System.lineSeparator(), text);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/CsvStructGraphOutputTest.java`
#### Snippet
```java
            @SuppressWarnings("deprecation")
            String text = FileUtils.readFileToString(file);
            Assert.assertEquals("100,999" + System.lineSeparator(), text);
        } finally {
            FileUtils.deleteQuietly(file);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/CsvStructGraphOutputTest.java`
#### Snippet
```java
            @SuppressWarnings("deprecation")
            String text = FileUtils.readFileToString(file);
            Assert.assertEquals("100,[998,999],[{200,\"knows\",\"\"}," +
                                "{300,\"watch\",\"1111\"}]" +
                                System.lineSeparator(), text);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        output.write(input, 0, input.available());
        Assert.assertEquals(uuid, new String(output.toByteArray()));

        // Input class isn't OptimizedBytesInput
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
            output = new OptimizedBytesOutput(SIZE);
            output.write(fileInput, 0, fileInput.available());
            Assert.assertEquals(uuid, new String(output.toByteArray()));
        } finally {
            if (fileInput != null) {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        OptimizedBytesInput apple = inputByString("apple");
        OptimizedBytesInput egg = inputByString("egg");
        Assert.assertTrue(apple.compare(0, 2, egg, 0, 2) < 0);
        Assert.assertTrue(apple.compare(1, 3, egg, 0, 2) > 0);
        Assert.assertEquals(0, apple.compare(4, 1, egg, 0, 1));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        OptimizedBytesInput egg = inputByString("egg");
        Assert.assertTrue(apple.compare(0, 2, egg, 0, 2) < 0);
        Assert.assertTrue(apple.compare(1, 3, egg, 0, 2) > 0);
        Assert.assertEquals(0, apple.compare(4, 1, egg, 0, 1));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertTrue(apple.compare(0, 2, egg, 0, 2) < 0);
        Assert.assertTrue(apple.compare(1, 3, egg, 0, 2) > 0);
        Assert.assertEquals(0, apple.compare(4, 1, egg, 0, 1));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        for (char i = 'a'; i <= 'z'; i++) {
            char value = input.readChar();
            Assert.assertEquals(i, value);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        OptimizedBytesInput dup = raw.duplicate();
        raw.readByte();
        Assert.assertEquals(1, raw.position());
        Assert.assertEquals(0, dup.position());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        raw.readByte();
        Assert.assertEquals(1, raw.position());
        Assert.assertEquals(0, dup.position());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        long position = output.skip(4);
        Assert.assertEquals(0, position);
        output.writeFixedInt(Integer.MAX_VALUE);
        OptimizedBytesInput input = new OptimizedBytesInput(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
                                    output.toByteArray());
        int bytesSkipped = input.skipBytes(4);
        Assert.assertEquals(4, bytesSkipped);
        Assert.assertEquals(Integer.MAX_VALUE, input.readFixedInt());
        Assert.assertEquals(0, input.skipBytes(1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        int bytesSkipped = input.skipBytes(4);
        Assert.assertEquals(4, bytesSkipped);
        Assert.assertEquals(Integer.MAX_VALUE, input.readFixedInt());
        Assert.assertEquals(0, input.skipBytes(1));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(4, bytesSkipped);
        Assert.assertEquals(Integer.MAX_VALUE, input.readFixedInt());
        Assert.assertEquals(0, input.skipBytes(1));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        for (int i = 0; i < chars.length(); i++) {
            char c = input.readChar();
            Assert.assertEquals(chars.charAt(i), c);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
                                    output.toByteArray());
        for (int i = 0; i <= 255; i++) {
            Assert.assertEquals(i, input.readUnsignedShort());
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readUnsignedShort());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(i, input.readUnsignedShort());
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readUnsignedShort());
        Assert.assertEquals(Short.MIN_VALUE & 0xFFFF,
                            input.readUnsignedShort());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readUnsignedShort());
        Assert.assertEquals(Short.MIN_VALUE & 0xFFFF,
                            input.readUnsignedShort());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        for (int i = 0; i <= 255; i++) {
            int value = input.readUnsignedByte();
            Assert.assertEquals(i, value);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    public void testConstructor() {
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        Assert.assertEquals(0, output.position());

        OptimizedBytesOutput output2 = new OptimizedBytesOutput(16);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java

        OptimizedBytesOutput output2 = new OptimizedBytesOutput(16);
        Assert.assertEquals(0, output2.position());

        OptimizedBytesInput input = new OptimizedBytesInput(output.buffer());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java

        OptimizedBytesInput input = new OptimizedBytesInput(output.buffer());
        Assert.assertEquals(0, input.position());

        OptimizedBytesInput input2 = new OptimizedBytesInput(output.buffer(),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        OptimizedBytesInput input2 = new OptimizedBytesInput(output.buffer(),
                                                             4);
        Assert.assertEquals(0, input2.position());
    }

```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.INITIALIZING);
        Mockito.verify(jobObserver, Mockito.timeout(150000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));


```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState2.jobStatus(JobStatus.FAILED);
        Mockito.verify(jobObserver, Mockito.timeout(250000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState2));
        future.cancel(true);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        HttpGet request = new HttpGet(health);
        HttpResponse response = client.execute(request);
        Assert.assertEquals(HttpStatus.SC_OK,
                            response.getStatusLine().getStatusCode());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        HttpGet requestReady = new HttpGet(ready);
        HttpResponse responseReady = client.execute(requestReady);
        Assert.assertEquals(HttpStatus.SC_OK,
                            responseReady.getStatusLine().getStatusCode());
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
            String kubeconfigFilename = getKubeconfigFilename();
            File file = new File(kubeconfigFilename);
            Assert.assertTrue(file.exists());

            this.namespace = "minikube";
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.INITIALIZING);
        Mockito.verify(jobObserver, Mockito.timeout(150000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        DefaultJobState jobState2 = new DefaultJobState();
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState2.jobStatus(JobStatus.SUCCEEDED);
        Mockito.verify(jobObserver, Mockito.timeout(150000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState2));

        future.cancel(true);
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.FAILED);
        Mockito.verify(jobObserver, Mockito.timeout(30000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        String diagnostics = this.driver.diagnostics(jobId, params);
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.INITIALIZING);
        Mockito.verify(jobObserver, Mockito.timeout(15000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        DefaultJobState jobState2 = new DefaultJobState();
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState2.jobStatus(JobStatus.SUCCEEDED);
        Mockito.verify(jobObserver, Mockito.timeout(15000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState2));

        future.cancel(true);
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.RUNNING);
        Mockito.verify(jobObserver, Mockito.timeout(20000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        HugeGraphComputerJob computerJob = this.operation
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.FAILED);
        Mockito.verify(jobObserver, Mockito.timeout(30000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        String diagnostics = this.driver.diagnostics(jobId, params);
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.INITIALIZING);
        Mockito.verify(jobObserver, Mockito.timeout(15000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        DefaultJobState jobState2 = new DefaultJobState();
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState2.jobStatus(JobStatus.FAILED);
        Mockito.verify(jobObserver, Mockito.timeout(150000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState2));

        UnitTestBase.sleep(500L);
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.INITIALIZING);
        Mockito.verify(jobObserver, Mockito.timeout(15000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        List<AbstractController<?>> controllers = Whitebox.getInternalState(
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
                                         this.namespace, Pod.class,
                                         new HashMap<String, String>());
        Assert.assertNotEquals(0, pods.size());

        future.cancel(true);
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.INITIALIZING);
        Mockito.verify(jobObserver, Mockito.timeout(15000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        this.driver.cancelJob(jobId, params);
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState2.jobStatus(JobStatus.CANCELLED);
        Mockito.verify(jobObserver, Mockito.timeout(15000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState2));

        future.cancel(true);
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState.jobStatus(JobStatus.INITIALIZING);
        Mockito.verify(jobObserver, Mockito.timeout(15000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState));

        DefaultJobState jobState2 = new DefaultJobState();
```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        jobState2.jobStatus(JobStatus.SUCCEEDED);
        Mockito.verify(jobObserver, Mockito.timeout(15000L).atLeast(1))
               .onJobStateChanged(Mockito.eq(jobState2));

        future.cancel(true);
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java

        JobState jobState = this.driver.jobState(jobId, params);
        Assert.assertNotNull(jobState);
        Assert.assertEquals(JobStatus.INITIALIZING, jobState.jobStatus());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        JobState jobState = this.driver.jobState(jobId, params);
        Assert.assertNotNull(jobState);
        Assert.assertEquals(JobStatus.INITIALIZING, jobState.jobStatus());
    }

```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
                                               .withName(KubeUtil.crName(jobId))
                                               .get();
        Assert.assertNotNull(computerJob);
        Assert.assertEquals(computerJob.getSpec().getAlgorithmName(),
                            "PageRank");
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        MutableBoolean watchActive = Whitebox.getInternalState(this.driver,
                                                               "watchActive");
        Assert.assertFalse(watchActive.booleanValue());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        File file = new File("/tmp/upload.txt");
        try {
            Assert.assertTrue(file.exists());
        } finally {
            FileUtils.deleteQuietly(file);
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java

        Mockito.verify(jobObserver, Mockito.timeout(5000L).atLeast(1))
               .onJobStateChanged(Mockito.any(DefaultJobState.class));

        future.getNow(null);
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
                                                                   params,
                                                                   jobObserver);
        Assert.assertNull(future2);
    }

```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        HugeGraphComputerJob computerJob = this.operation.withName(crName)
                                                         .get();
        Assert.assertNotNull(computerJob);

        UnitTestBase.sleep(1000L);
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
                                                       .withName(crName)
                                                       .get();
        Assert.assertNull(canceledComputerJob);
        Assert.assertNull(this.driver.jobState(jobId, params));
    }
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
                                                       .get();
        Assert.assertNull(canceledComputerJob);
        Assert.assertNull(this.driver.jobState(jobId, params));
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        MutableBoolean watchActive = Whitebox.getInternalState(
                                     this.driver, "watchActive");
        Assert.assertTrue(watchActive.booleanValue());
        Assert.assertEquals(namespace, "test");
        Assert.assertNotNull(conf);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
                                     this.driver, "watchActive");
        Assert.assertTrue(watchActive.booleanValue());
        Assert.assertEquals(namespace, "test");
        Assert.assertNotNull(conf);
        Assert.assertNotNull(operation);
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        Assert.assertTrue(watchActive.booleanValue());
        Assert.assertEquals(namespace, "test");
        Assert.assertNotNull(conf);
        Assert.assertNotNull(operation);

```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        Assert.assertEquals(namespace, "test");
        Assert.assertNotNull(conf);
        Assert.assertNotNull(operation);

        final int workerInstances = 2;
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        String workerInstancesKey = KubeUtil.covertSpecKey(
                                    KubeSpecOptions.WORKER_INSTANCES.name());
        Assert.assertEquals(defaultSpec.get(workerInstancesKey),
                            workerInstances);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/JsonStructGraphOutputTest.java`
#### Snippet
```java
            @SuppressWarnings("deprecation")
            String json = FileUtils.readFileToString(file);
            Assert.assertEquals("{\"id\":100,\"rank\":999}" +
                                System.lineSeparator(), json);
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/JsonStructGraphOutputTest.java`
#### Snippet
```java
            @SuppressWarnings("deprecation")
            String json = FileUtils.readFileToString(file);
            Assert.assertEquals("{\"id\":100,\"rank\":[[66],[998,999]]," +
                                "\"properties\":{\"boolean\":true," +
                                "\"byte\":127,\"double\":-0.01," +
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/JsonStructGraphOutputTest.java`
#### Snippet
```java
            @SuppressWarnings("deprecation")
            String json = FileUtils.readFileToString(file);
            Assert.assertEquals("{\"id\":100,\"rank\":[998,999]," +
                                "\"adjacent_edges\":[{\"target_id\":200," +
                                "\"label\":\"knows\",\"name\":\"\"}," +
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutputInputTest.java`
#### Snippet
```java
            // Compare label, name and targetId
        }
        Assert.assertEquals(expect, actual);
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutputInputTest.java`
#### Snippet
```java
        expect.value(null);
        actual.value(null);
        Assert.assertEquals(expect, actual);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutputInputTest.java`
#### Snippet
```java
        try (BytesInput bai = IOFactory.createBytesInput(bytes)) {
            StreamGraphInput input = newStreamGraphInput(bai);
            Assert.assertEquals(Pair.of(id, value), input.readMessage());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/BspEventTest.java`
#### Snippet
```java
        }
        // Assert code in unique
        Assert.assertEquals(events.length, codeMap.size());
        // Assert key in unique
        Assert.assertEquals(events.length, keyMap.size());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/BspEventTest.java`
#### Snippet
```java
        Assert.assertEquals(events.length, codeMap.size());
        // Assert key in unique
        Assert.assertEquals(events.length, keyMap.size());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdBspTest.java`
#### Snippet
```java
            List<WorkerStat> workerStats = this.bsp4Master
                                               .waitWorkersStepDone(-1);
            Assert.assertEquals(1, workerStats.size());
            Assert.assertEquals(workerStat, workerStats.get(0));
            countDownLatch.countDown();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdBspTest.java`
#### Snippet
```java
                                               .waitWorkersStepDone(-1);
            Assert.assertEquals(1, workerStats.size());
            Assert.assertEquals(workerStat, workerStats.get(0));
            countDownLatch.countDown();
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdBspTest.java`
#### Snippet
```java
        this.executorService.submit(() -> {
            int firstSuperStep = this.bsp4Worker.waitMasterResumeDone();
            Assert.assertEquals(-1, firstSuperStep);
            this.bsp4Worker.workerInputDone();
            this.bsp4Worker.waitMasterInputDone();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdBspTest.java`
#### Snippet
```java
            this.bsp4Master.masterInitDone(this.masterInfo);
            List<ContainerInfo> workers = this.bsp4Master.waitWorkersInitDone();
            Assert.assertEquals(1, workers.size());
            countDownLatch.countDown();
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdBspTest.java`
#### Snippet
```java
            this.bsp4Worker.workerInitDone();
            ContainerInfo master = this.bsp4Worker.waitMasterInitDone();
            Assert.assertEquals(this.masterInfo, master);
            List<ContainerInfo> workers = this.bsp4Worker
                                              .waitMasterAllInitDone();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdBspTest.java`
#### Snippet
```java
            List<ContainerInfo> workers = this.bsp4Worker
                                              .waitMasterAllInitDone();
            Assert.assertEquals(1, workers.size());
            Assert.assertEquals(this.workerInfo, workers.get(0));
            countDownLatch.countDown();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdBspTest.java`
#### Snippet
```java
                                              .waitMasterAllInitDone();
            Assert.assertEquals(1, workers.size());
            Assert.assertEquals(this.workerInfo, workers.get(0));
            countDownLatch.countDown();
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                srao.writeShort(i);
            }
            Assert.assertEquals(678L, srao.position());
            srao.writeShort(1000);
            Assert.assertEquals(682L, srao.position());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            Assert.assertEquals(678L, srao.position());
            srao.writeShort(1000);
            Assert.assertEquals(682L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                                  Constants.SMALL_BUF_SIZE);
             RandomAccessOutput srao = new StructRandomAccessOutput(output)) {
            Assert.assertEquals(0L, srao.position());
            output.writeInt(1000);
            output.writeInt(2000);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            output.writeInt(1000);
            output.writeInt(2000);
            Assert.assertEquals(8L, srao.position());
            srao.skip(4);
            Assert.assertEquals(12L, srao.position());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            Assert.assertEquals(8L, srao.position());
            srao.skip(4);
            Assert.assertEquals(12L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                srao.write(i);
            }
            Assert.assertEquals(678L, srao.position());
        }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                srao.writeByte(i);
            }
            Assert.assertEquals(678L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
             RandomAccessOutput srao = new StructRandomAccessOutput(output)) {
            srao.write(bytes);
            Assert.assertEquals(22L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                srao.writeUTF(prefix + i);
            }
            Assert.assertEquals(160L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
             RandomAccessOutput srao = new StructRandomAccessOutput(output)) {
            srao.write(bytes, 1, bytes.length - 1);
            Assert.assertEquals(18L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                                  Constants.SMALL_BUF_SIZE);
             RandomAccessOutput srao = new StructRandomAccessOutput(output)) {
            Assert.assertEquals(0L, srao.position());
            srao.writeFixedInt(4, 1000);
            Assert.assertEquals(0L, srao.position());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            Assert.assertEquals(0L, srao.position());
            srao.writeFixedInt(4, 1000);
            Assert.assertEquals(0L, srao.position());
            srao.seek(8);
            srao.writeFixedInt(4, 2000);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            srao.seek(8);
            srao.writeFixedInt(4, 2000);
            Assert.assertEquals(8L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                srao.writeInt(i);
            }
            Assert.assertEquals(678L, srao.position());
            // Overwrite last 8 bytes
            srao.seek(srao.position() - 8);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            srao.writeInt(1000);
            srao.writeInt(2000);
            Assert.assertEquals(678L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                Constants.SMALL_BUF_SIZE);
             RandomAccessOutput srao = new StructRandomAccessOutput(output)) {
            Assert.assertEquals(0L, srao.position());
            srao.writeLong(12345678);
            Assert.assertEquals(8L, srao.position());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            Assert.assertEquals(0L, srao.position());
            srao.writeLong(12345678);
            Assert.assertEquals(8L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                                  Constants.SMALL_BUF_SIZE);
             RandomAccessOutput srao = new StructRandomAccessOutput(output)) {
            Assert.assertEquals(0L, srao.position());
            srao.writeFixedInt(1000);
            Assert.assertEquals(4L, srao.position());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            Assert.assertEquals(0L, srao.position());
            srao.writeFixedInt(1000);
            Assert.assertEquals(4L, srao.position());
            srao.writeFixedInt(2000);
            Assert.assertEquals(8L, srao.position());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            Assert.assertEquals(4L, srao.position());
            srao.writeFixedInt(2000);
            Assert.assertEquals(8L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
             RandomAccessOutput srao = new StructRandomAccessOutput(output)) {
            srao.writeBytes(bytes);
            Assert.assertEquals(15L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
                srao.writeChar(i);
            }
            Assert.assertEquals(75L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
             RandomAccessOutput srao = new StructRandomAccessOutput(output)) {
            srao.writeChars(chars);
            Assert.assertEquals(15L, srao.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
        RandomAccessOutput srao = new StructRandomAccessOutput(output);
        srao.write(input, 0, input.available());
        Assert.assertEquals(50L, srao.position());
        srao.close();

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StructRandomAccessOutputTest.java`
#### Snippet
```java
            fileInput = new BufferedFileInput(tempFile);
            srao.write(fileInput, 0, fileInput.available());
            Assert.assertEquals(100L, srao.position());
            srao.close();
        } finally {
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/EmptyFlusherTest.java`
#### Snippet
```java
    public void test() throws Exception {
        PeekableIterator<Double> it = PeekableIterator.emptyIterator();
        Assert.assertFalse(it.hasNext());

        Assert.assertThrows(NoSuchElementException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        this.client.put(KEY2, VALUE2);
        List<byte[]> valueList1  = this.client.getWithPrefix(KEY_PREFIX);
        Assert.assertEquals(2, valueList1.size());
        List<byte[]> valueList2 = this.client.getWithPrefix(NO_SUCH_KEY);
        Assert.assertEquals(0, valueList2.size());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertEquals(2, valueList1.size());
        List<byte[]> valueList2 = this.client.getWithPrefix(NO_SUCH_KEY);
        Assert.assertEquals(0, valueList2.size());
        this.client.delete(KEY1);
        this.client.delete(KEY2);
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        this.client.put(KEY1, VALUE1);
        byte[] bytes1 = this.client.get(KEY1);
        Assert.assertArrayEquals(VALUE1, bytes1);
        byte[] bytes2 = this.client.get(NO_SUCH_KEY);
        Assert.assertNull(bytes2);
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertArrayEquals(VALUE1, bytes1);
        byte[] bytes2 = this.client.get(NO_SUCH_KEY);
        Assert.assertNull(bytes2);
        this.client.delete(KEY1);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        executorService.awaitTermination(1L, TimeUnit.SECONDS);
        long deleteCount = this.client.delete(KEY1);
        Assert.assertEquals(1L, deleteCount);
    }

```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        this.client.put(KEY1, VALUE1);
        byte[] bytes = this.client.get(KEY1);
        Assert.assertArrayEquals(VALUE1, bytes);
        this.client.delete(KEY1);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        executorService.shutdown();
        executorService.awaitTermination(1L, TimeUnit.SECONDS);
        Assert.assertEquals(2, valueList1.size());
        Assert.assertArrayEquals(VALUE1, valueList1.get(0));
        Assert.assertArrayEquals(VALUE2, valueList1.get(1));
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        executorService.awaitTermination(1L, TimeUnit.SECONDS);
        Assert.assertEquals(2, valueList1.size());
        Assert.assertArrayEquals(VALUE1, valueList1.get(0));
        Assert.assertArrayEquals(VALUE2, valueList1.get(1));

```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertEquals(2, valueList1.size());
        Assert.assertArrayEquals(VALUE1, valueList1.get(0));
        Assert.assertArrayEquals(VALUE2, valueList1.get(1));

        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        executorService.shutdown();
        executorService.awaitTermination(1L, TimeUnit.SECONDS);
        Assert.assertArrayEquals(VALUE1, bytes1);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        this.client.put(KEY1, VALUE1);
        long deleteCount1 = this.client.delete(KEY1);
        Assert.assertEquals(1L, deleteCount1);
        long deleteCount2 = this.client.delete(KEY1);
        Assert.assertEquals(0L, deleteCount2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertEquals(1L, deleteCount1);
        long deleteCount2 = this.client.delete(KEY1);
        Assert.assertEquals(0L, deleteCount2);
        byte[] value = this.client.get(KEY1);
        Assert.assertNull(value);
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, deleteCount2);
        byte[] value = this.client.get(KEY1);
        Assert.assertNull(value);
        long deleteCount3 = this.client.delete(NO_SUCH_KEY);
        Assert.assertEquals(0L, deleteCount3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertNull(value);
        long deleteCount3 = this.client.delete(NO_SUCH_KEY);
        Assert.assertEquals(0L, deleteCount3);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        this.client.put(KEY1, VALUE1);
        List<byte[]> valueList1 = this.client.getWithPrefix(KEY_PREFIX, 2);
        Assert.assertEquals(2, valueList1.size());
        Assert.assertArrayEquals(VALUE1, valueList1.get(0));
        Assert.assertArrayEquals(VALUE2, valueList1.get(1));
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        List<byte[]> valueList1 = this.client.getWithPrefix(KEY_PREFIX, 2);
        Assert.assertEquals(2, valueList1.size());
        Assert.assertArrayEquals(VALUE1, valueList1.get(0));
        Assert.assertArrayEquals(VALUE2, valueList1.get(1));

```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertEquals(2, valueList1.size());
        Assert.assertArrayEquals(VALUE1, valueList1.get(0));
        Assert.assertArrayEquals(VALUE2, valueList1.get(1));

        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        this.client.put(NO_SUCH_KEY, VALUE2);
        long deleteCount1 = this.client.deleteWithPrefix(KEY_PREFIX);
        Assert.assertEquals(2L, deleteCount1);
        long deleteCount2 = this.client.delete(NO_SUCH_KEY);
        Assert.assertEquals(1L, deleteCount2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertEquals(2L, deleteCount1);
        long deleteCount2 = this.client.delete(NO_SUCH_KEY);
        Assert.assertEquals(1L, deleteCount2);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        this.client.put(KEY2, VALUE2);
        long deleteCount1 = this.client.deleteAllKvsInNamespace();
        Assert.assertEquals(2L, deleteCount1);
        long deleteCount2 = this.client.delete(KEY1);
        Assert.assertEquals(0L, deleteCount2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertEquals(2L, deleteCount1);
        long deleteCount2 = this.client.delete(KEY1);
        Assert.assertEquals(0L, deleteCount2);
    }
}
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        executorService.shutdown();
        executorService.awaitTermination(1L, TimeUnit.SECONDS);
        Assert.assertArrayEquals(VALUE1, bytes1);
        Assert.assertThrows(ComputerException.class, () -> {
            this.client.get(NO_SUCH_KEY, 1000L, 500L);
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        this.client.put(KEY1, VALUE1);
        byte[] bytes1 = this.client.get(KEY1);
        Assert.assertArrayEquals(VALUE1, bytes1);
        byte[] bytes2 = this.client.get(NO_SUCH_KEY);
        Assert.assertNull(bytes2);
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertArrayEquals(VALUE1, bytes1);
        byte[] bytes2 = this.client.get(NO_SUCH_KEY);
        Assert.assertNull(bytes2);
        Assert.assertThrows(ComputerException.class, () -> {
            this.client.get(NO_SUCH_KEY, true);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
                                                        StandardCharsets.UTF_8))
                              .get().getDeleted();
        Assert.assertEquals(1, deleteCount1);
        GetOption getOption1 = GetOption.newBuilder().withRevision(revision1)
                                        .build();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
                                        .build();
        GetResponse getResponse1 = kv.get(key1Seq, getOption1).get();
        Assert.assertEquals(value1Seq,
                            getResponse1.getKvs().get(0).getValue());
        GetOption getOption2 = GetOption.newBuilder().withRevision(revision2)
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
                                        .build();
        GetResponse getResponse2 = kv.get(key1Seq, getOption2).get();
        Assert.assertEquals(value2Seq,
                            getResponse2.getKvs().get(0).getValue());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        for (int i = 0; i < chars.length(); i++) {
            char c = input.readChar();
            Assert.assertEquals(chars.charAt(i), c);
        }
    }
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        input.readFully(bytesRead, 1, bytes.length - 1);
        bytesRead[0] = bytes[0];
        Assert.assertArrayEquals(bytes, bytesRead);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer(),
                                                      (int) output.position());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        for (char i = 'a'; i <= 'z'; i++) {
            char value = input.readChar();
            Assert.assertEquals(i, value);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        for (int i = 0; i <= 255; i++) {
            int value = input.readUnsignedByte();
            Assert.assertEquals(i, value);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readFloat(), 0.0D);
        }
        Assert.assertEquals(Float.MAX_VALUE, input.readFloat(), 0.0D);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(i, input.readFloat(), 0.0D);
        }
        Assert.assertEquals(Float.MAX_VALUE, input.readFloat(), 0.0D);
        Assert.assertEquals(Float.MIN_VALUE, input.readFloat(), 0.0D);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(Float.MAX_VALUE, input.readFloat(), 0.0D);
        Assert.assertEquals(Float.MIN_VALUE, input.readFloat(), 0.0D);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        byte[] bytes;
        try (UnsafeBytesOutput bao = new UnsafeBytesOutput(SIZE)) {
            Assert.assertEquals(0L, bao.position());
            bao.writeLong(Long.MAX_VALUE);
            Assert.assertEquals(8L, bao.position());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(0L, bao.position());
            bao.writeLong(Long.MAX_VALUE);
            Assert.assertEquals(8L, bao.position());
            bytes = bao.toByteArray();
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

        try (UnsafeBytesInput bai = new UnsafeBytesInput(bytes)) {
            Assert.assertEquals(0L, bai.position());
            Assert.assertEquals(Long.MAX_VALUE, bai.readLong());
            Assert.assertEquals(8L, bai.position());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        try (UnsafeBytesInput bai = new UnsafeBytesInput(bytes)) {
            Assert.assertEquals(0L, bai.position());
            Assert.assertEquals(Long.MAX_VALUE, bai.readLong());
            Assert.assertEquals(8L, bai.position());
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(0L, bai.position());
            Assert.assertEquals(Long.MAX_VALUE, bai.readLong());
            Assert.assertEquals(8L, bai.position());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        Assert.assertEquals(1, input.readInt());
        Assert.assertEquals(2, input.readInt());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        Assert.assertEquals(1, input.readInt());
        Assert.assertEquals(2, input.readInt());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readShort());
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readShort());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(i, input.readShort());
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readShort());
        Assert.assertEquals(Short.MIN_VALUE, input.readShort());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readShort());
        Assert.assertEquals(Short.MIN_VALUE, input.readShort());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testConstructor() {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        Assert.assertEquals(0, output.position());

        UnsafeBytesOutput output2 = new UnsafeBytesOutput(SIZE);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

        UnsafeBytesOutput output2 = new UnsafeBytesOutput(SIZE);
        Assert.assertEquals(0, output2.position());

        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer());
        Assert.assertEquals(0, input.position());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.write(input, 0, input.available());
        Assert.assertEquals(uuid, new String(output.toByteArray()));

        // Input class isn't UnsafeBytesInput
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            output = new UnsafeBytesOutput(SIZE);
            output.write(fileInput, 0, fileInput.available());
            Assert.assertEquals(uuid, new String(output.toByteArray()));
        } finally {
            if (fileInput != null) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = 0; i < 256; i++) {
            Assert.assertEquals(256 - i, input.available());
            input.readByte();
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = 0; i <= 255; i++) {
            Assert.assertEquals(i, input.readUnsignedShort());
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readUnsignedShort());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(i, input.readUnsignedShort());
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readUnsignedShort());
        Assert.assertEquals(Short.MIN_VALUE & 0xFFFF,
                            input.readUnsignedShort());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(Short.MAX_VALUE, input.readUnsignedShort());
        Assert.assertEquals(Short.MIN_VALUE & 0xFFFF,
                            input.readUnsignedShort());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput dup = raw.duplicate();
        raw.readByte();
        Assert.assertEquals(1, raw.position());
        Assert.assertEquals(0, dup.position());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        raw.readByte();
        Assert.assertEquals(1, raw.position());
        Assert.assertEquals(0, dup.position());

        String uuid = UUID.randomUUID().toString();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

            fileInput.readChar();
            Assert.assertEquals(2, fileInput.position());
            Assert.assertEquals(0, dup.position());
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            fileInput.readChar();
            Assert.assertEquals(2, fileInput.position());
            Assert.assertEquals(0, dup.position());
        } finally {
            if (fileInput != null) {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput apple = inputByString("apple");
        UnsafeBytesInput egg = inputByString("egg");
        Assert.assertTrue(apple.compare(0, 2, egg, 0, 2) < 0);
        Assert.assertTrue(apple.compare(1, 3, egg, 0, 2) > 0);
        Assert.assertEquals(0, apple.compare(4, 1, egg, 0, 1));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput egg = inputByString("egg");
        Assert.assertTrue(apple.compare(0, 2, egg, 0, 2) < 0);
        Assert.assertTrue(apple.compare(1, 3, egg, 0, 2) > 0);
        Assert.assertEquals(0, apple.compare(4, 1, egg, 0, 1));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertTrue(apple.compare(0, 2, egg, 0, 2) < 0);
        Assert.assertTrue(apple.compare(1, 3, egg, 0, 2) > 0);
        Assert.assertEquals(0, apple.compare(4, 1, egg, 0, 1));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer());
        String value = input.readUTF();
        Assert.assertEquals(s1, value);
    }

```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        try (OptimizedBytesOutput bao = new OptimizedBytesOutput(9)) {
            bao.writeLong(value);
            Assert.assertArrayEquals(bytes, bao.toByteArray());
        }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        try (OptimizedBytesInput bai = new OptimizedBytesInput(bytes)) {
            long readValue = bai.readLong();
            Assert.assertEquals(value, readValue);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        input.skipBytes(4);
        Assert.assertEquals(4, input.remaining());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(0, input.remaining());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        input.skipBytes(4);
        Assert.assertEquals(4, input.remaining());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(0, input.remaining());
        Assert.assertEquals(0, input.skipBytes(1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(4, input.remaining());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(0, input.remaining());
        Assert.assertEquals(0, input.skipBytes(1));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(0, input.remaining());
        Assert.assertEquals(0, input.skipBytes(1));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        long position = output.skip(4);
        Assert.assertEquals(0, position);
        output.writeInt(Integer.MAX_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        int bytesSkipped = input.skipBytes(4);
        Assert.assertEquals(4, bytesSkipped);
        Assert.assertEquals(4, input.remaining());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        int bytesSkipped = input.skipBytes(4);
        Assert.assertEquals(4, bytesSkipped);
        Assert.assertEquals(4, input.remaining());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(0, input.remaining());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/SerializeUtilTest.java`
#### Snippet
```java
        List<Id> list2 = SerializeUtil.fromBytes(bytes, () -> new BytesId());

        Assert.assertEquals(list1, list2);
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/SerializeUtilTest.java`
#### Snippet
```java
        SerializeUtil.fromBytes(bytes, id2);

        Assert.assertEquals(id1, id2);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(4, bytesSkipped);
        Assert.assertEquals(4, input.remaining());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(0, input.remaining());
        Assert.assertEquals(0, input.skipBytes(1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(4, input.remaining());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(0, input.remaining());
        Assert.assertEquals(0, input.skipBytes(1));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(0, input.remaining());
        Assert.assertEquals(0, input.skipBytes(1));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readDouble(), 0.0D);
        }
        Assert.assertEquals(Double.MAX_VALUE, input.readDouble(), 0.0D);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(i, input.readDouble(), 0.0D);
        }
        Assert.assertEquals(Double.MAX_VALUE, input.readDouble(), 0.0D);
        Assert.assertEquals(Double.MIN_VALUE, input.readDouble(), 0.0D);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(Double.MAX_VALUE, input.readDouble(), 0.0D);
        Assert.assertEquals(Double.MIN_VALUE, input.readDouble(), 0.0D);
    }

```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        try (OptimizedBytesOutput bao = new OptimizedBytesOutput(SIZE)) {
            bao.writeUTF(value);
            Assert.assertArrayEquals(bytes, bao.toByteArray());
        }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        try (OptimizedBytesInput bai = new OptimizedBytesInput(bytes)) {
            String readValue = bai.readUTF();
            Assert.assertEquals(value, readValue);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 125; i++) {
            Assert.assertEquals(i, input.readInt());
        }
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(i, input.readInt());
        }
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
        input.seek(256 * 4 - 8);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
        input.seek(256 * 4 - 8);
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
        input.seek(256 * 4 - 8);
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        input.seek(256 * 4 - 8);
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        byte[] bytes = input.readBytes(uuid.length());
        Assert.assertEquals(uuid, new String(bytes));
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeBoolean(false);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        Assert.assertTrue(input.readBoolean());
        Assert.assertFalse(input.readBoolean());
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        Assert.assertTrue(input.readBoolean());
        Assert.assertFalse(input.readBoolean());
    }

```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        input.readFully(bytesRead);
        Assert.assertArrayEquals(bytes, bytesRead);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (long i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readLong());
        }
        Assert.assertEquals(Long.MAX_VALUE, input.readLong());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(i, input.readLong());
        }
        Assert.assertEquals(Long.MAX_VALUE, input.readLong());
        Assert.assertEquals(Long.MIN_VALUE, input.readLong());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(Long.MAX_VALUE, input.readLong());
        Assert.assertEquals(Long.MIN_VALUE, input.readLong());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readInt());
        }
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            Assert.assertEquals(i, input.readInt());
        }
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        for (int i = -128; i <= 127; i++) {
            String value = input.readUTF();
            Assert.assertEquals(prefix + i, value);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        for (int i = -128; i <= 127; i++) {
            int value = input.readByte();
            Assert.assertEquals(i, value);
        }
    }
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        try (OptimizedBytesOutput bao = new OptimizedBytesOutput(5)) {
            bao.writeInt(value);
            Assert.assertArrayEquals(bytes, bao.toByteArray());
        }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        try (OptimizedBytesInput bai = new OptimizedBytesInput(bytes)) {
            int readValue = bai.readInt();
            Assert.assertEquals(value, readValue);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        String value = input.readUTF();
        Assert.assertEquals(s, value);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer(),
                                                      (int) output.position());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertThrows(ComputerException.class, () -> {
            input.readInt();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java
        Map<String, String> result = ComputerContextUtil.convertToMap(
                                     "key1", "value1", "key2", "value2");
        Assert.assertEquals(ImmutableMap.of("key1", "value1", "key2", "value2"),
                            result);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java
                               MockComputationParams.class.getName());
        result = ComputerContextUtil.convertToMap(properties);
        Assert.assertEquals(ImmutableMap.of(
                            "key1", "newValue1",
                            "algorithm.params_class",
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java

        ComputerContextUtil.initContext(properties);
        Assert.assertEquals("newValue1",
                            ComputerContext.instance()
                                           .config()
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                for (int i = 0; i < size; i++) {
                    dis.readFully(arrayRead);
                    Assert.assertArrayEquals(array, arrayRead);
                }
            }
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                for (int i = 0; i < size - 1; i++) {
                    input.readFully(arrayRead);
                    Assert.assertArrayEquals(array, arrayRead);
                }
                Assert.assertThrows(IOException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            }
            try (BufferedStreamInput input = createInput(file)) {
                Assert.assertEquals(Long.MAX_VALUE, input.available());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                for (int i = 0; i < size; i++) {
                    input.seek(i * 4);
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertThrows(IOException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                for (int i = 0; i < size - 2; i++) {
                    input.seek(i * 4);
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                Assert.assertThrows(IOException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                Assert.assertThrows(IOException.class, () -> {
                    input.seek(input.position() - BUFFER_SIZE - 2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            try (DataInputStream dis = new DataInputStream(
                                       new FileInputStream(file))) {
                Assert.assertEquals(-128, dis.readByte());
                dis.skip(1);
                for (int i = -126; i <= 127; i++) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                dis.skip(1);
                for (int i = -126; i <= 127; i++) {
                    Assert.assertEquals(i, dis.readByte());
                }
                long count1 = dis.skip(4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                }
                long count1 = dis.skip(4);
                Assert.assertEquals(4, count1);
                Assert.assertEquals(127, dis.readByte());
                long count2 = dis.skip(1280);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                long count1 = dis.skip(4);
                Assert.assertEquals(4, count1);
                Assert.assertEquals(127, dis.readByte());
                long count2 = dis.skip(1280);
                Assert.assertEquals(1280, count2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertEquals(127, dis.readByte());
                long count2 = dis.skip(1280);
                Assert.assertEquals(1280, count2);
                for (int i = 1; i <= 1280; i++) {
                    Assert.assertEquals((byte) i, dis.readByte());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertEquals(1280, count2);
                for (int i = 1; i <= 1280; i++) {
                    Assert.assertEquals((byte) i, dis.readByte());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java

            try (BufferedStreamInput input = createInput(file)) {
                Assert.assertEquals(-128, input.readByte());
                input.skip(1);
                for (int i = -126; i <= 127; i++) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                input.skip(1);
                for (int i = -126; i <= 127; i++) {
                    Assert.assertEquals(i, input.readByte());
                }
                input.skip(4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                }
                input.skip(4);
                Assert.assertEquals(127, input.readByte());
                input.skip(1280);
                for (int i = 1; i <= 1280; i++) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                input.skip(1280);
                for (int i = 1; i <= 1280; i++) {
                    Assert.assertEquals((byte) i, input.readByte());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            try (BufferedStreamOutput output = createOutput(file)) {
                for (int i = 0; i <= size; i++) {
                    Assert.assertEquals(i * 4, output.position());
                    output.writeInt(i);
                }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            try (BufferedStreamInput input = createInput(file)) {
                for (int i = 0; i <= size; i++) {
                    Assert.assertEquals(i * 4, input.position());
                    Assert.assertEquals(i, input.readInt());
                }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                for (int i = 0; i <= size; i++) {
                    Assert.assertEquals(i * 4, input.position());
                    Assert.assertEquals(i, input.readInt());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                    switch (position) {
                        case 128:
                            Assert.assertEquals(1, input.readInt());
                            break;
                        case 200:
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                            break;
                        case 200:
                            Assert.assertEquals(2, input.readInt());
                            break;
                        case 252:
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                            break;
                        case 252:
                            Assert.assertEquals(3, input.readInt());
                            break;
                        default:
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                            break;
                        default:
                            Assert.assertEquals(i, input.readInt());
                    }
                }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                    }
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            OutputStream os = new FileOutputStream(file);
            try (BufferedStreamOutput output = new BufferedStreamOutput(os)) {
                Assert.assertEquals(0, output.position());
            }
            InputStream in = new FileInputStream(file);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            InputStream in = new FileInputStream(file);
            try (BufferedStreamInput input = new BufferedStreamInput(in)) {
                Assert.assertEquals(0, input.position());
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                for (int i = 0; i < loopTimes; i++) {
                    dis.readFully(arrayRead);
                    Assert.assertArrayEquals(array, arrayRead);
                }
            }
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                for (int i = 0; i < loopTimes - 1; i++) {
                    input.readFully(arrayRead);
                    Assert.assertArrayEquals(array, arrayRead);
                }
                Assert.assertThrows(IOException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            try (BufferedStreamInput input = createInput(file)) {
                for (int i = -128; i <= 127; i++) {
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/JsonUtilTest.java`
#### Snippet
```java
        PartitionStat partitionStat1 = JsonUtil.fromJson(json,
                                                         PartitionStat.class);
        Assert.assertEquals(null, partitionStat1);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/JsonUtilTest.java`
#### Snippet
```java
        String json = JsonUtil.toJson(integer);
        Object integer1 = JsonUtil.fromJson(json, BigInteger.class);
        Assert.assertEquals(integer, integer1);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        edge.properties(properties);

        Assert.assertEquals("knows", edge.label());
        Assert.assertEquals("2021-06-01", edge.name());
        Assert.assertEquals(BytesId.of(1L), edge.targetId());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java

        Assert.assertEquals("knows", edge.label());
        Assert.assertEquals("2021-06-01", edge.name());
        Assert.assertEquals(BytesId.of(1L), edge.targetId());
        Assert.assertEquals(properties, edge.properties());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        Assert.assertEquals("knows", edge.label());
        Assert.assertEquals("2021-06-01", edge.name());
        Assert.assertEquals(BytesId.of(1L), edge.targetId());
        Assert.assertEquals(properties, edge.properties());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        Assert.assertEquals("2021-06-01", edge.name());
        Assert.assertEquals(BytesId.of(1L), edge.targetId());
        Assert.assertEquals(properties, edge.properties());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
    public void testConstructor() {
        DefaultEdge edge = new DefaultEdge(graphFactory());
        Assert.assertEquals(Constants.EMPTY_STR, edge.label());
        Assert.assertEquals(Constants.EMPTY_STR, edge.name());
        Assert.assertNull(edge.targetId());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        DefaultEdge edge = new DefaultEdge(graphFactory());
        Assert.assertEquals(Constants.EMPTY_STR, edge.label());
        Assert.assertEquals(Constants.EMPTY_STR, edge.name());
        Assert.assertNull(edge.targetId());
        Assert.assertEquals(new DefaultProperties(graphFactory()),
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        Assert.assertEquals(Constants.EMPTY_STR, edge.label());
        Assert.assertEquals(Constants.EMPTY_STR, edge.name());
        Assert.assertNull(edge.targetId());
        Assert.assertEquals(new DefaultProperties(graphFactory()),
                            edge.properties());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        Assert.assertEquals(Constants.EMPTY_STR, edge.name());
        Assert.assertNull(edge.targetId());
        Assert.assertEquals(new DefaultProperties(graphFactory()),
                            edge.properties());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        edge = new DefaultEdge(graphFactory(), "knows", "2021-06-01",
                               BytesId.of(1L));
        Assert.assertEquals("knows", edge.label());
        Assert.assertEquals("2021-06-01", edge.name());
        Assert.assertEquals(BytesId.of(1L), edge.targetId());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
                               BytesId.of(1L));
        Assert.assertEquals("knows", edge.label());
        Assert.assertEquals("2021-06-01", edge.name());
        Assert.assertEquals(BytesId.of(1L), edge.targetId());
        Assert.assertEquals(new DefaultProperties(graphFactory()),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        Assert.assertEquals("knows", edge.label());
        Assert.assertEquals("2021-06-01", edge.name());
        Assert.assertEquals(BytesId.of(1L), edge.targetId());
        Assert.assertEquals(new DefaultProperties(graphFactory()),
                            edge.properties());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        Assert.assertEquals("2021-06-01", edge.name());
        Assert.assertEquals(BytesId.of(1L), edge.targetId());
        Assert.assertEquals(new DefaultProperties(graphFactory()),
                            edge.properties());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        edge2.properties().put("p2", new DoubleValue(2.0D));

        Assert.assertEquals(edge1, edge2);

        Assert.assertNotEquals(edge1, properties);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        Assert.assertEquals(edge1, edge2);

        Assert.assertNotEquals(edge1, properties);
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
    public void testOverwrite() {
        DefaultProperties properties = new DefaultProperties(graphFactory());
        Assert.assertEquals(0, properties.get().size());
        properties.put("p1", new LongValue(1L));
        properties.put("p2", new DoubleValue(2.0D));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
        properties.put("p2", new DoubleValue(2.0D));

        Assert.assertEquals(new LongValue(1L), properties.get("p1"));
        properties.put("p1", new LongValue(2L));
        Assert.assertEquals(new LongValue(2L), properties.get("p1"));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(1L), properties.get("p1"));
        properties.put("p1", new LongValue(2L));
        Assert.assertEquals(new LongValue(2L), properties.get("p1"));

        Map<String, Value> props = properties.get();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java

        Map<String, Value> props = properties.get();
        Assert.assertEquals(2, props.size());
        Assert.assertEquals(new LongValue(2L), props.get("p1"));
        Assert.assertEquals(new DoubleValue(2.0D), props.get("p2"));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
        Map<String, Value> props = properties.get();
        Assert.assertEquals(2, props.size());
        Assert.assertEquals(new LongValue(2L), props.get("p1"));
        Assert.assertEquals(new DoubleValue(2.0D), props.get("p2"));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
        Assert.assertEquals(2, props.size());
        Assert.assertEquals(new LongValue(2L), props.get("p1"));
        Assert.assertEquals(new DoubleValue(2.0D), props.get("p2"));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
        props.put("p2", new DoubleValue(2.0D));

        Assert.assertEquals(1073748897, props.hashCode());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
                                   props1.get(),
                                   UnitTestBase.graphFactory());
        Assert.assertEquals(props1, props2);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
    public void testConstructor() {
        DefaultProperties properties = new DefaultProperties(graphFactory());
        Assert.assertEquals(0, properties.get().size());
        properties.put("p1", new LongValue(1L));
        properties.put("p2", new DoubleValue(2.0D));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java

        Map<String, Value> props = properties.get();
        Assert.assertEquals(2, props.size());
        Assert.assertEquals(new LongValue(1L), props.get("p1"));
        Assert.assertEquals(new DoubleValue(2.0D), props.get("p2"));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
        Map<String, Value> props = properties.get();
        Assert.assertEquals(2, props.size());
        Assert.assertEquals(new LongValue(1L), props.get("p1"));
        Assert.assertEquals(new DoubleValue(2.0D), props.get("p2"));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
        Assert.assertEquals(2, props.size());
        Assert.assertEquals(new LongValue(1L), props.get("p1"));
        Assert.assertEquals(new DoubleValue(2.0D), props.get("p2"));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultPropertiesTest.java`
#### Snippet
```java
    public void testReadWrite() throws IOException {
        DefaultProperties properties = new DefaultProperties(graphFactory());
        Assert.assertEquals(0, properties.get().size());
        properties.put("p1", new LongValue(1L));
        properties.put("p2", new DoubleValue(2.0D));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        GraphFactory graphFactory = graphFactory();
        Id id = graphFactory.createId(value);
        Assert.assertEquals(IdType.LONG, id.idType());
        Assert.assertEquals(BytesId.of(value), id);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        Id id = graphFactory.createId(value);
        Assert.assertEquals(IdType.LONG, id.idType());
        Assert.assertEquals(BytesId.of(value), id);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        GraphFactory graphFactory = graphFactory();
        Id id = graphFactory.createId(value);
        Assert.assertEquals(IdType.UTF8, id.idType());
        Assert.assertEquals(BytesId.of(value), id);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        Id id = graphFactory.createId(value);
        Assert.assertEquals(IdType.UTF8, id.idType());
        Assert.assertEquals(BytesId.of(value), id);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
    public void testCreateValue() {
        GraphFactory factory = context().graphFactory();
        Assert.assertEquals(ValueType.NULL,
                            factory.createValue(ValueType.NULL.code())
                                   .valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                            factory.createValue(ValueType.NULL.code())
                                   .valueType());
        Assert.assertEquals(ValueType.LONG,
                            factory.createValue(ValueType.LONG.code())
                                   .valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                            factory.createValue(ValueType.LONG.code())
                                   .valueType());
        Assert.assertEquals(ValueType.DOUBLE,
                            factory.createValue(ValueType.DOUBLE.code())
                                   .valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                            factory.createValue(ValueType.DOUBLE.code())
                                   .valueType());
        Assert.assertEquals(ValueType.ID,
                            factory.createValue(ValueType.ID.code())
                                   .valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                            factory.createValue(ValueType.ID.code())
                                   .valueType());
        Assert.assertEquals(ValueType.ID_LIST,
                            factory.createValue(ValueType.ID_LIST.code())
                                   .valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                            factory.createValue(ValueType.ID_LIST.code())
                                   .valueType());
        Assert.assertEquals(ValueType.ID_LIST_LIST,
                            factory.createValue(
                                    ValueType.ID_LIST_LIST.code())
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                                   .valueType());

        Assert.assertEquals(ValueType.NULL,
                            factory.createValue(ValueType.NULL).valueType());
        Assert.assertEquals(ValueType.LONG,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.NULL,
                            factory.createValue(ValueType.NULL).valueType());
        Assert.assertEquals(ValueType.LONG,
                            factory.createValue(ValueType.LONG).valueType());
        Assert.assertEquals(ValueType.DOUBLE,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.LONG,
                            factory.createValue(ValueType.LONG).valueType());
        Assert.assertEquals(ValueType.DOUBLE,
                            factory.createValue(ValueType.DOUBLE).valueType());
        Assert.assertEquals(ValueType.ID,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.DOUBLE,
                            factory.createValue(ValueType.DOUBLE).valueType());
        Assert.assertEquals(ValueType.ID,
                            factory.createValue(ValueType.ID)
                                   .valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                            factory.createValue(ValueType.ID)
                                   .valueType());
        Assert.assertEquals(ValueType.ID_LIST,
                            factory.createValue(ValueType.ID_LIST)
                                   .valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                            factory.createValue(ValueType.ID_LIST)
                                   .valueType());
        Assert.assertEquals(ValueType.ID_LIST_LIST,
                            factory.createValue(ValueType.ID_LIST_LIST)
                                   .valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        GraphFactory graphFactory = graphFactory();
        Id id = graphFactory.createId(uuid);
        Assert.assertEquals(IdType.UUID, id.idType());
        Assert.assertEquals(BytesId.of(uuid), id);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
        Id id = graphFactory.createId(uuid);
        Assert.assertEquals(IdType.UUID, id.idType());
        Assert.assertEquals(BytesId.of(uuid), id);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SorterTest.java`
#### Snippet
```java
        // Assert file properties
        HgkvDir dir = HgkvDirImpl.open(outputFile);
        Assert.assertEquals(5, dir.numEntries());
        Assert.assertEquals(8, dir.numSubEntries());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SorterTest.java`
#### Snippet
```java
        HgkvDir dir = HgkvDirImpl.open(outputFile);
        Assert.assertEquals(5, dir.numEntries());
        Assert.assertEquals(8, dir.numSubEntries());
    }

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SorterTest.java`
#### Snippet
```java
        SorterTestUtil.assertKvEntry(iter.next(), 6, 40);
        SorterTestUtil.assertKvEntry(iter.next(), 8, 4);
        Assert.assertFalse(iter.hasNext());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SorterTest.java`
#### Snippet
```java
            }

            Assert.assertEquals(StoreTestUtil.dataFromPointer(last.key()),
                                resultIter.next());
            Assert.assertEquals(value, resultIter.next());
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SorterTest.java`
#### Snippet
```java
            value = StoreTestUtil.dataFromPointer(last.value());
        }
        Assert.assertFalse(resultIter.hasNext());

        FileUtil.deleteFilesQuietly(inputs);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
                     "messageSendBytes\":6,\"messageRecvCount\":7,\"" +
                     "messageRecvBytes\":8,\"active\":true}";
        Assert.assertEquals(str, stat.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        stat.increase(partitionStat);
        stat.increase(partitionStat);
        Assert.assertTrue(stat.active());
        stat.inactivate();
        Assert.assertFalse(stat.active());
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertTrue(stat.active());
        stat.inactivate();
        Assert.assertFalse(stat.active());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        stat.increase(partitionStat);

        Assert.assertEquals(partitionStat.vertexCount() * 2L,
                            stat.vertexCount());
        Assert.assertEquals(partitionStat.edgeCount() * 2L, stat.edgeCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertEquals(partitionStat.vertexCount() * 2L,
                            stat.vertexCount());
        Assert.assertEquals(partitionStat.edgeCount() * 2L, stat.edgeCount());

        Assert.assertEquals(partitionStat.finishedVertexCount() * 2L,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertEquals(partitionStat.edgeCount() * 2L, stat.edgeCount());

        Assert.assertEquals(partitionStat.finishedVertexCount() * 2L,
                            stat.finishedVertexCount());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
                            stat.finishedVertexCount());

        Assert.assertEquals(partitionStat.messageSendCount() * 2L,
                            stat.messageSendCount());
        Assert.assertEquals(partitionStat.messageSendBytes() * 2L,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertEquals(partitionStat.messageSendCount() * 2L,
                            stat.messageSendCount());
        Assert.assertEquals(partitionStat.messageSendBytes() * 2L,
                            stat.messageSendBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
                            stat.messageSendBytes());

        Assert.assertEquals(partitionStat.messageRecvCount() * 2L,
                            stat.messageRecvCount());
        Assert.assertEquals(partitionStat.messageRecvBytes() * 2L,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertEquals(partitionStat.messageRecvCount() * 2L,
                            stat.messageRecvCount());
        Assert.assertEquals(partitionStat.messageRecvBytes() * 2L,
                            stat.messageRecvBytes());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        stat2.increase(partitionStat);
        SuperstepStat stat3 = new SuperstepStat();
        Assert.assertEquals(stat1.hashCode(), stat2.hashCode());
        Assert.assertNotEquals(stat1.hashCode(), stat3.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        SuperstepStat stat3 = new SuperstepStat();
        Assert.assertEquals(stat1.hashCode(), stat2.hashCode());
        Assert.assertNotEquals(stat1.hashCode(), stat3.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        SuperstepStat stat3 = new SuperstepStat();

        Assert.assertEquals(stat1, stat2);
        Assert.assertNotEquals(stat1, stat3);
        Assert.assertNotEquals(stat1, new Object());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java

        Assert.assertEquals(stat1, stat2);
        Assert.assertNotEquals(stat1, stat3);
        Assert.assertNotEquals(stat1, new Object());

```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertEquals(stat1, stat2);
        Assert.assertNotEquals(stat1, stat3);
        Assert.assertNotEquals(stat1, new Object());

        stat1.inactivate();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java

        stat1.inactivate();
        Assert.assertFalse(stat1.active());
        Assert.assertNotEquals(stat1, stat2);
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        stat1.inactivate();
        Assert.assertFalse(stat1.active());
        Assert.assertNotEquals(stat1, stat2);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        stat.increase(workerStat);

        Assert.assertEquals(18, stat.vertexCount());
        Assert.assertEquals(16, stat.edgeCount());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java

        Assert.assertEquals(18, stat.vertexCount());
        Assert.assertEquals(16, stat.edgeCount());

        Assert.assertEquals(14L, stat.finishedVertexCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertEquals(16, stat.edgeCount());

        Assert.assertEquals(14L, stat.finishedVertexCount());

        Assert.assertEquals(20L, stat.messageSendCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertEquals(14L, stat.finishedVertexCount());

        Assert.assertEquals(20L, stat.messageSendCount());
        Assert.assertEquals(22L, stat.messageSendBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java

        Assert.assertEquals(20L, stat.messageSendCount());
        Assert.assertEquals(22L, stat.messageSendBytes());

        Assert.assertEquals(24L, stat.messageRecvCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java
        Assert.assertEquals(22L, stat.messageSendBytes());

        Assert.assertEquals(24L, stat.messageRecvCount());
        Assert.assertEquals(26L, stat.messageRecvBytes());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStatTest.java`
#### Snippet
```java

        Assert.assertEquals(24L, stat.messageRecvCount());
        Assert.assertEquals(26L, stat.messageRecvBytes());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/SorterTestUtil.java`
#### Snippet
```java
            input.readInt();
            int key = input.readInt();
            Assert.assertEquals(entry.getKey().intValue(), key);
            int valueLength = input.readFixedInt();
            int valueCount = valueLength / Integer.BYTES;
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/SorterTestUtil.java`
#### Snippet
```java
                values.add(input.readInt());
            }
            Assert.assertEquals(entry.getValue(), values);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/SorterTestUtil.java`
#### Snippet
```java
                                     Integer expectValue)
                                     throws IOException {
        Assert.assertEquals(expectKey,
                            StoreTestUtil.dataFromPointer(entry.key()));
        Assert.assertEquals(expectValue,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/SorterTestUtil.java`
#### Snippet
```java
        Assert.assertEquals(expectKey,
                            StoreTestUtil.dataFromPointer(entry.key()));
        Assert.assertEquals(expectValue,
                            StoreTestUtil.dataFromPointer(entry.value()));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/SorterTestUtil.java`
#### Snippet
```java
    public static void assertKvEntry(KvEntry entry, Id expectKey,
                                     Id expectValue) throws IOException {
        Assert.assertEquals(expectKey,
                            StoreTestUtil.idFromPointer(entry.key()));
        Assert.assertEquals(expectValue,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/SorterTestUtil.java`
#### Snippet
```java
        Assert.assertEquals(expectKey,
                            StoreTestUtil.idFromPointer(entry.key()));
        Assert.assertEquals(expectValue,
                            StoreTestUtil.idFromPointer(entry.value()));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java
    @Test
    public void testCreateIdFromType() {
        Assert.assertEquals(IdType.LONG,
                            IdFactory.createId(IdType.LONG).idType());
        Assert.assertEquals(IdType.UTF8,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.LONG,
                            IdFactory.createId(IdType.LONG).idType());
        Assert.assertEquals(IdType.UTF8,
                            IdFactory.createId(IdType.UTF8).idType());
        Assert.assertEquals(IdType.UUID,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.UTF8,
                            IdFactory.createId(IdType.UTF8).idType());
        Assert.assertEquals(IdType.UUID,
                            IdFactory.createId(IdType.UUID).idType());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java
                            IdFactory.createId(IdType.UUID).idType());

        Assert.assertEquals(BytesId.of(0L), IdFactory.createId(IdType.LONG));
        Assert.assertEquals(BytesId.of(Constants.EMPTY_STR),
                            IdFactory.createId(IdType.UTF8));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java

        Assert.assertEquals(BytesId.of(0L), IdFactory.createId(IdType.LONG));
        Assert.assertEquals(BytesId.of(Constants.EMPTY_STR),
                            IdFactory.createId(IdType.UTF8));
        Assert.assertEquals(BytesId.of(new UUID(0L, 0L)),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java
        Assert.assertEquals(BytesId.of(Constants.EMPTY_STR),
                            IdFactory.createId(IdType.UTF8));
        Assert.assertEquals(BytesId.of(new UUID(0L, 0L)),
                            IdFactory.createId(IdType.UUID));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java
    @Test
    public void testCreateIdFromCode() {
        Assert.assertEquals(IdType.LONG,
                            IdFactory.createId(IdType.LONG.code()).idType());
        Assert.assertEquals(IdType.UTF8,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.LONG,
                            IdFactory.createId(IdType.LONG.code()).idType());
        Assert.assertEquals(IdType.UTF8,
                            IdFactory.createId(IdType.UTF8.code()).idType());
        Assert.assertEquals(IdType.UUID,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactoryTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.UTF8,
                            IdFactory.createId(IdType.UTF8.code()).idType());
        Assert.assertEquals(IdType.UUID,
                            IdFactory.createId(IdType.UUID.code()).idType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdTypeTest.java`
#### Snippet
```java
    public void testFromCode() {
        for (IdType type : IdType.values()) {
            Assert.assertEquals(type, SerialEnum.fromCode(IdType.class,
                                                          type.code()));
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdTypeTest.java`
#### Snippet
```java
    @Test
    public void testCode() {
        Assert.assertEquals(1, IdType.LONG.code());
        Assert.assertEquals(2, IdType.UTF8.code());
        Assert.assertEquals(3, IdType.UUID.code());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdTypeTest.java`
#### Snippet
```java
    public void testCode() {
        Assert.assertEquals(1, IdType.LONG.code());
        Assert.assertEquals(2, IdType.UTF8.code());
        Assert.assertEquals(3, IdType.UUID.code());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(1, IdType.LONG.code());
        Assert.assertEquals(2, IdType.UTF8.code());
        Assert.assertEquals(3, IdType.UUID.code());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
    @Test
    public void testCodeAndByteSize() {
        Assert.assertEquals(1, ValueType.NULL.code());
        Assert.assertEquals(2, ValueType.BOOLEAN.code());
        Assert.assertEquals(3, ValueType.INT.code());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
    public void testCodeAndByteSize() {
        Assert.assertEquals(1, ValueType.NULL.code());
        Assert.assertEquals(2, ValueType.BOOLEAN.code());
        Assert.assertEquals(3, ValueType.INT.code());
        Assert.assertEquals(4, ValueType.LONG.code());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(1, ValueType.NULL.code());
        Assert.assertEquals(2, ValueType.BOOLEAN.code());
        Assert.assertEquals(3, ValueType.INT.code());
        Assert.assertEquals(4, ValueType.LONG.code());
        Assert.assertEquals(5, ValueType.FLOAT.code());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(2, ValueType.BOOLEAN.code());
        Assert.assertEquals(3, ValueType.INT.code());
        Assert.assertEquals(4, ValueType.LONG.code());
        Assert.assertEquals(5, ValueType.FLOAT.code());
        Assert.assertEquals(6, ValueType.DOUBLE.code());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(3, ValueType.INT.code());
        Assert.assertEquals(4, ValueType.LONG.code());
        Assert.assertEquals(5, ValueType.FLOAT.code());
        Assert.assertEquals(6, ValueType.DOUBLE.code());
        Assert.assertEquals(20, ValueType.ID.code());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(4, ValueType.LONG.code());
        Assert.assertEquals(5, ValueType.FLOAT.code());
        Assert.assertEquals(6, ValueType.DOUBLE.code());
        Assert.assertEquals(20, ValueType.ID.code());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(5, ValueType.FLOAT.code());
        Assert.assertEquals(6, ValueType.DOUBLE.code());
        Assert.assertEquals(20, ValueType.ID.code());

        Assert.assertEquals(0, ValueType.NULL.byteSize());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(20, ValueType.ID.code());

        Assert.assertEquals(0, ValueType.NULL.byteSize());
        Assert.assertEquals(1, ValueType.BOOLEAN.byteSize());
        Assert.assertEquals(4, ValueType.INT.byteSize());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java

        Assert.assertEquals(0, ValueType.NULL.byteSize());
        Assert.assertEquals(1, ValueType.BOOLEAN.byteSize());
        Assert.assertEquals(4, ValueType.INT.byteSize());
        Assert.assertEquals(8, ValueType.LONG.byteSize());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(0, ValueType.NULL.byteSize());
        Assert.assertEquals(1, ValueType.BOOLEAN.byteSize());
        Assert.assertEquals(4, ValueType.INT.byteSize());
        Assert.assertEquals(8, ValueType.LONG.byteSize());
        Assert.assertEquals(4, ValueType.FLOAT.byteSize());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(1, ValueType.BOOLEAN.byteSize());
        Assert.assertEquals(4, ValueType.INT.byteSize());
        Assert.assertEquals(8, ValueType.LONG.byteSize());
        Assert.assertEquals(4, ValueType.FLOAT.byteSize());
        Assert.assertEquals(8, ValueType.DOUBLE.byteSize());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(4, ValueType.INT.byteSize());
        Assert.assertEquals(8, ValueType.LONG.byteSize());
        Assert.assertEquals(4, ValueType.FLOAT.byteSize());
        Assert.assertEquals(8, ValueType.DOUBLE.byteSize());
        Assert.assertEquals(-1, ValueType.ID.byteSize());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(8, ValueType.LONG.byteSize());
        Assert.assertEquals(4, ValueType.FLOAT.byteSize());
        Assert.assertEquals(8, ValueType.DOUBLE.byteSize());
        Assert.assertEquals(-1, ValueType.ID.byteSize());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
        Assert.assertEquals(4, ValueType.FLOAT.byteSize());
        Assert.assertEquals(8, ValueType.DOUBLE.byteSize());
        Assert.assertEquals(-1, ValueType.ID.byteSize());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
    public void testFromCode() {
        for (ValueType type : ValueType.values()) {
            Assert.assertEquals(type, SerialEnum.fromCode(ValueType.class,
                                                          type.code()));
        }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Id uuidId = BytesId.of(uuid);

        Assert.assertTrue(longId.compareTo(utf8Id) < 0);
        Assert.assertTrue(utf8Id.compareTo(uuidId) < 0);
        Assert.assertTrue(longId.compareTo(uuidId) < 0);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertTrue(longId.compareTo(utf8Id) < 0);
        Assert.assertTrue(utf8Id.compareTo(uuidId) < 0);
        Assert.assertTrue(longId.compareTo(uuidId) < 0);

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(longId.compareTo(utf8Id) < 0);
        Assert.assertTrue(utf8Id.compareTo(uuidId) < 0);
        Assert.assertTrue(longId.compareTo(uuidId) < 0);

        Assert.assertNotEquals(longId, utf8Id);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(longId.compareTo(uuidId) < 0);

        Assert.assertNotEquals(longId, utf8Id);
        Assert.assertNotEquals(utf8Id, uuidId);
        Assert.assertNotEquals(longId, uuidId);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertNotEquals(longId, utf8Id);
        Assert.assertNotEquals(utf8Id, uuidId);
        Assert.assertNotEquals(longId, uuidId);
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertNotEquals(longId, utf8Id);
        Assert.assertNotEquals(utf8Id, uuidId);
        Assert.assertNotEquals(longId, uuidId);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Id utf8Id7 = new BytesId();

        Assert.assertEquals(IdType.UTF8, utf8Id1.idType());
        Assert.assertEquals(ValueType.ID, utf8Id1.valueType());
        Assert.assertEquals(0, utf8Id1.length());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(IdType.UTF8, utf8Id1.idType());
        Assert.assertEquals(ValueType.ID, utf8Id1.valueType());
        Assert.assertEquals(0, utf8Id1.length());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.UTF8, utf8Id1.idType());
        Assert.assertEquals(ValueType.ID, utf8Id1.valueType());
        Assert.assertEquals(0, utf8Id1.length());

        Assert.assertEquals(IdType.UTF8, utf8Id2.idType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(0, utf8Id1.length());

        Assert.assertEquals(IdType.UTF8, utf8Id2.idType());
        Assert.assertEquals(ValueType.ID, utf8Id2.valueType());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(IdType.UTF8, utf8Id2.idType());
        Assert.assertEquals(ValueType.ID, utf8Id2.valueType());

        Assert.assertEquals(IdType.UTF8, utf8Id3.idType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.ID, utf8Id2.valueType());

        Assert.assertEquals(IdType.UTF8, utf8Id3.idType());
        Assert.assertEquals(ValueType.ID, utf8Id3.valueType());
        Assert.assertEquals(4, utf8Id3.length());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(IdType.UTF8, utf8Id3.idType());
        Assert.assertEquals(ValueType.ID, utf8Id3.valueType());
        Assert.assertEquals(4, utf8Id3.length());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.UTF8, utf8Id3.idType());
        Assert.assertEquals(ValueType.ID, utf8Id3.valueType());
        Assert.assertEquals(4, utf8Id3.length());

        Assert.assertEquals(IdType.UTF8, utf8Id7.idType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(4, utf8Id3.length());

        Assert.assertEquals(IdType.UTF8, utf8Id7.idType());
        Assert.assertEquals(ValueType.ID, utf8Id7.valueType());
        Assert.assertEquals(0, utf8Id7.length());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(IdType.UTF8, utf8Id7.idType());
        Assert.assertEquals(ValueType.ID, utf8Id7.valueType());
        Assert.assertEquals(0, utf8Id7.length());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.UTF8, utf8Id7.idType());
        Assert.assertEquals(ValueType.ID, utf8Id7.valueType());
        Assert.assertEquals(0, utf8Id7.length());

        Assert.assertEquals("", utf8Id1.asObject());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(0, utf8Id7.length());

        Assert.assertEquals("", utf8Id1.asObject());
        Assert.assertEquals("abc", utf8Id2.asObject());
        Assert.assertEquals("abcd", utf8Id3.asObject());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals("", utf8Id1.asObject());
        Assert.assertEquals("abc", utf8Id2.asObject());
        Assert.assertEquals("abcd", utf8Id3.asObject());
        Assert.assertEquals("100", utf8Id6.asObject());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals("", utf8Id1.asObject());
        Assert.assertEquals("abc", utf8Id2.asObject());
        Assert.assertEquals("abcd", utf8Id3.asObject());
        Assert.assertEquals("100", utf8Id6.asObject());
        Assert.assertEquals("", utf8Id7.asObject());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals("abc", utf8Id2.asObject());
        Assert.assertEquals("abcd", utf8Id3.asObject());
        Assert.assertEquals("100", utf8Id6.asObject());
        Assert.assertEquals("", utf8Id7.asObject());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals("abcd", utf8Id3.asObject());
        Assert.assertEquals("100", utf8Id6.asObject());
        Assert.assertEquals("", utf8Id7.asObject());

        Assert.assertTrue(utf8Id3.compareTo(utf8Id2) > 0);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals("", utf8Id7.asObject());

        Assert.assertTrue(utf8Id3.compareTo(utf8Id2) > 0);
        Assert.assertTrue(utf8Id2.compareTo(utf8Id3) < 0);
        Assert.assertEquals(0, utf8Id2.compareTo(utf8Id2));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertTrue(utf8Id3.compareTo(utf8Id2) > 0);
        Assert.assertTrue(utf8Id2.compareTo(utf8Id3) < 0);
        Assert.assertEquals(0, utf8Id2.compareTo(utf8Id2));
        Assert.assertTrue(utf8Id2.compareTo(utf8Id4) < 0);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(utf8Id3.compareTo(utf8Id2) > 0);
        Assert.assertTrue(utf8Id2.compareTo(utf8Id3) < 0);
        Assert.assertEquals(0, utf8Id2.compareTo(utf8Id2));
        Assert.assertTrue(utf8Id2.compareTo(utf8Id4) < 0);
        Assert.assertTrue(utf8Id4.compareTo(utf8Id2) > 0);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(utf8Id2.compareTo(utf8Id3) < 0);
        Assert.assertEquals(0, utf8Id2.compareTo(utf8Id2));
        Assert.assertTrue(utf8Id2.compareTo(utf8Id4) < 0);
        Assert.assertTrue(utf8Id4.compareTo(utf8Id2) > 0);

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(0, utf8Id2.compareTo(utf8Id2));
        Assert.assertTrue(utf8Id2.compareTo(utf8Id4) < 0);
        Assert.assertTrue(utf8Id4.compareTo(utf8Id2) > 0);

        Assert.assertEquals(utf8Id2, utf8Id5);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(utf8Id4.compareTo(utf8Id2) > 0);

        Assert.assertEquals(utf8Id2, utf8Id5);
        Assert.assertNotEquals(utf8Id2, utf8Id4);
        Assert.assertEquals(utf8Id1, utf8Id7);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(utf8Id2, utf8Id5);
        Assert.assertNotEquals(utf8Id2, utf8Id4);
        Assert.assertEquals(utf8Id1, utf8Id7);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(utf8Id2, utf8Id5);
        Assert.assertNotEquals(utf8Id2, utf8Id4);
        Assert.assertEquals(utf8Id1, utf8Id7);

        Assert.assertEquals(utf8Id2.hashCode(), utf8Id5.hashCode());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(utf8Id1, utf8Id7);

        Assert.assertEquals(utf8Id2.hashCode(), utf8Id5.hashCode());
        Assert.assertNotEquals(utf8Id2.hashCode(), utf8Id3.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(utf8Id2.hashCode(), utf8Id5.hashCode());
        Assert.assertNotEquals(utf8Id2.hashCode(), utf8Id3.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Id uuidId3 = BytesId.of(uuid1);

        Assert.assertEquals(IdType.UUID, uuidId1.idType());
        Assert.assertEquals(ValueType.ID, uuidId1.valueType());
        Assert.assertEquals(19, uuidId1.length());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(IdType.UUID, uuidId1.idType());
        Assert.assertEquals(ValueType.ID, uuidId1.valueType());
        Assert.assertEquals(19, uuidId1.length());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.UUID, uuidId1.idType());
        Assert.assertEquals(ValueType.ID, uuidId1.valueType());
        Assert.assertEquals(19, uuidId1.length());

        Assert.assertEquals(IdType.UUID, uuidId2.idType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(19, uuidId1.length());

        Assert.assertEquals(IdType.UUID, uuidId2.idType());
        Assert.assertEquals(ValueType.ID, uuidId2.valueType());
        Assert.assertEquals(19, uuidId2.length());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(IdType.UUID, uuidId2.idType());
        Assert.assertEquals(ValueType.ID, uuidId2.valueType());
        Assert.assertEquals(19, uuidId2.length());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.UUID, uuidId2.idType());
        Assert.assertEquals(ValueType.ID, uuidId2.valueType());
        Assert.assertEquals(19, uuidId2.length());

        Assert.assertEquals(uuid1, uuidId1.asObject());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(19, uuidId2.length());

        Assert.assertEquals(uuid1, uuidId1.asObject());
        ByteBuffer buffer = ByteBuffer.allocate(16);
        buffer.putLong(uuid1.getMostSignificantBits());
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        buffer.putLong(uuid1.getLeastSignificantBits());

        Assert.assertTrue(uuidId1.compareTo(uuidId2) < 0);
        Assert.assertTrue(uuidId2.compareTo(uuidId1) > 0);
        Assert.assertEquals(0, uuidId1.compareTo(uuidId3));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertTrue(uuidId1.compareTo(uuidId2) < 0);
        Assert.assertTrue(uuidId2.compareTo(uuidId1) > 0);
        Assert.assertEquals(0, uuidId1.compareTo(uuidId3));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(uuidId1.compareTo(uuidId2) < 0);
        Assert.assertTrue(uuidId2.compareTo(uuidId1) > 0);
        Assert.assertEquals(0, uuidId1.compareTo(uuidId3));

        Assert.assertEquals(uuidId1, uuidId3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(0, uuidId1.compareTo(uuidId3));

        Assert.assertEquals(uuidId1, uuidId3);
        Assert.assertNotEquals(uuidId1, uuidId2);
        Assert.assertEquals(uuidId1.hashCode(), uuidId3.hashCode());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(uuidId1, uuidId3);
        Assert.assertNotEquals(uuidId1, uuidId2);
        Assert.assertEquals(uuidId1.hashCode(), uuidId3.hashCode());
        Assert.assertNotEquals(uuidId1.hashCode(), uuidId2.hashCode());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(uuidId1, uuidId3);
        Assert.assertNotEquals(uuidId1, uuidId2);
        Assert.assertEquals(uuidId1.hashCode(), uuidId3.hashCode());
        Assert.assertNotEquals(uuidId1.hashCode(), uuidId2.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertNotEquals(uuidId1, uuidId2);
        Assert.assertEquals(uuidId1.hashCode(), uuidId3.hashCode());
        Assert.assertNotEquals(uuidId1.hashCode(), uuidId2.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Id longId8 = BytesId.of(0L);

        Assert.assertEquals(IdType.LONG, longId1.idType());
        Assert.assertEquals(ValueType.ID, longId1.valueType());
        Assert.assertEquals(1, longId1.length());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(IdType.LONG, longId1.idType());
        Assert.assertEquals(ValueType.ID, longId1.valueType());
        Assert.assertEquals(1, longId1.length());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.LONG, longId1.idType());
        Assert.assertEquals(ValueType.ID, longId1.valueType());
        Assert.assertEquals(1, longId1.length());

        Assert.assertEquals(IdType.LONG, longId2.idType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(1, longId1.length());

        Assert.assertEquals(IdType.LONG, longId2.idType());
        Assert.assertEquals(ValueType.ID, longId2.valueType());
        Assert.assertEquals(2, longId2.length());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(IdType.LONG, longId2.idType());
        Assert.assertEquals(ValueType.ID, longId2.valueType());
        Assert.assertEquals(2, longId2.length());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(IdType.LONG, longId2.idType());
        Assert.assertEquals(ValueType.ID, longId2.valueType());
        Assert.assertEquals(2, longId2.length());

        Assert.assertEquals(new Long(123L), longId1.asObject());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(2, longId2.length());

        Assert.assertEquals(new Long(123L), longId1.asObject());
        Assert.assertEquals(123L, longId1.asObject());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(123L, longId1.asObject());

        Assert.assertEquals(new Long(-100L), longId5.asObject());
        Assert.assertEquals(-100L, longId5.asObject());

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(-100L, longId5.asObject());

        Assert.assertTrue(longId1.compareTo(longId2) < 0);
        Assert.assertTrue(longId2.compareTo(longId1) > 0);
        Assert.assertEquals(0, longId1.compareTo(longId3));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertTrue(longId1.compareTo(longId2) < 0);
        Assert.assertTrue(longId2.compareTo(longId1) > 0);
        Assert.assertEquals(0, longId1.compareTo(longId3));
        Assert.assertTrue(longId2.compareTo(longId4) < 0);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(longId1.compareTo(longId2) < 0);
        Assert.assertTrue(longId2.compareTo(longId1) > 0);
        Assert.assertEquals(0, longId1.compareTo(longId3));
        Assert.assertTrue(longId2.compareTo(longId4) < 0);

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(longId2.compareTo(longId1) > 0);
        Assert.assertEquals(0, longId1.compareTo(longId3));
        Assert.assertTrue(longId2.compareTo(longId4) < 0);

        Assert.assertTrue(longId5.compareTo(longId6) > 0);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(longId2.compareTo(longId4) < 0);

        Assert.assertTrue(longId5.compareTo(longId6) > 0);
        Assert.assertTrue(longId6.compareTo(longId5) < 0);
        Assert.assertEquals(0, longId5.compareTo(longId7));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertTrue(longId5.compareTo(longId6) > 0);
        Assert.assertTrue(longId6.compareTo(longId5) < 0);
        Assert.assertEquals(0, longId5.compareTo(longId7));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(longId5.compareTo(longId6) > 0);
        Assert.assertTrue(longId6.compareTo(longId5) < 0);
        Assert.assertEquals(0, longId5.compareTo(longId7));

        Assert.assertEquals(0, longId8.compareTo(longId6));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(0, longId5.compareTo(longId7));

        Assert.assertEquals(0, longId8.compareTo(longId6));
        Assert.assertTrue(longId8.compareTo(longId1) < 0);
        Assert.assertTrue(longId1.compareTo(longId8) > 0);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(0, longId8.compareTo(longId6));
        Assert.assertTrue(longId8.compareTo(longId1) < 0);
        Assert.assertTrue(longId1.compareTo(longId8) > 0);

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(0, longId8.compareTo(longId6));
        Assert.assertTrue(longId8.compareTo(longId1) < 0);
        Assert.assertTrue(longId1.compareTo(longId8) > 0);

        Assert.assertEquals(longId1, longId3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertTrue(longId1.compareTo(longId8) > 0);

        Assert.assertEquals(longId1, longId3);
        Assert.assertNotEquals(longId1, longId2);
        Assert.assertEquals(longId1.hashCode(), longId3.hashCode());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java

        Assert.assertEquals(longId1, longId3);
        Assert.assertNotEquals(longId1, longId2);
        Assert.assertEquals(longId1.hashCode(), longId3.hashCode());
        Assert.assertNotEquals(longId1.hashCode(), longId2.hashCode());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(longId1, longId3);
        Assert.assertNotEquals(longId1, longId2);
        Assert.assertEquals(longId1.hashCode(), longId3.hashCode());
        Assert.assertNotEquals(longId1.hashCode(), longId2.hashCode());
        Assert.assertEquals(longId1, BytesId.of((Long) longId1.asObject()));
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertNotEquals(longId1, longId2);
        Assert.assertEquals(longId1.hashCode(), longId3.hashCode());
        Assert.assertNotEquals(longId1.hashCode(), longId2.hashCode());
        Assert.assertEquals(longId1, BytesId.of((Long) longId1.asObject()));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesIdTest.java`
#### Snippet
```java
        Assert.assertEquals(longId1.hashCode(), longId3.hashCode());
        Assert.assertNotEquals(longId1.hashCode(), longId2.hashCode());
        Assert.assertEquals(longId1, BytesId.of((Long) longId1.asObject()));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueListTest.java`
#### Snippet
```java
        listValue2.add(longId1);

        Assert.assertEquals(ValueType.ID_LIST, listValue1.valueType());
        Assert.assertEquals(ValueType.ID, listValue1.elemType());
        Assert.assertTrue(ListUtils.isEqualList(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueListTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.ID_LIST, listValue1.valueType());
        Assert.assertEquals(ValueType.ID, listValue1.elemType());
        Assert.assertTrue(ListUtils.isEqualList(
                          Lists.newArrayList(longId1),
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueListTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.ID_LIST, listValue1.valueType());
        Assert.assertEquals(ValueType.ID, listValue1.elemType());
        Assert.assertTrue(ListUtils.isEqualList(
                          Lists.newArrayList(longId1),
                          listValue1.values()));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueListTest.java`
#### Snippet
```java
                          Lists.newArrayList(longId1),
                          listValue1.values()));
        Assert.assertEquals(listValue1, listValue2);

        listValue2.add(longId2);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueListTest.java`
#### Snippet
```java

        listValue2.add(longId2);
        Assert.assertTrue(ListUtils.isEqualList(
                          Lists.newArrayList(longId1, longId2),
                          listValue2.values()));
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueListTest.java`
#### Snippet
```java
                          Lists.newArrayList(longId1, longId2),
                          listValue2.values()));
        Assert.assertNotEquals(listValue1, listValue2);
        Assert.assertEquals(ListUtils.hashCodeForList(
                            Lists.newArrayList(longId1)),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueListTest.java`
#### Snippet
```java
                          listValue2.values()));
        Assert.assertNotEquals(listValue1, listValue2);
        Assert.assertEquals(ListUtils.hashCodeForList(
                            Lists.newArrayList(longId1)),
                            listValue1.hashCode());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueListTest.java`
#### Snippet
```java
        value3.add(longId1);
        value3.add(longId2);
        Assert.assertEquals(0, value1.compareTo(value2));
        Assert.assertLt(0, value1.compareTo(value3));
        Assert.assertGt(0, value3.compareTo(value1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue value3 = new BooleanValue(true);

        Assert.assertEquals(0, value1.compareTo(value2));
        Assert.assertLt(0, value1.compareTo(value3));
        Assert.assertGt(0, value3.compareTo(value1));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
    public void testEquals() {
        BooleanValue value1 = new BooleanValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new BooleanValue(false)));
        Assert.assertFalse(value1.equals(new BooleanValue(true)));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue value1 = new BooleanValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new BooleanValue(false)));
        Assert.assertFalse(value1.equals(new BooleanValue(true)));

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new BooleanValue(false)));
        Assert.assertFalse(value1.equals(new BooleanValue(true)));

        Assert.assertFalse(value1.equals(new IntValue(1)));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value1.equals(new BooleanValue(true)));

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue value3 = new BooleanValue(false);

        Assert.assertEquals(false, value1.value());
        Assert.assertEquals(true, value2.value());
        Assert.assertEquals(false, value3.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        Assert.assertEquals(false, value1.value());
        Assert.assertEquals(true, value2.value());
        Assert.assertEquals(false, value3.value());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals(false, value1.value());
        Assert.assertEquals(true, value2.value());
        Assert.assertEquals(false, value3.value());

        Assert.assertEquals(false, value1.boolValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        Assert.assertEquals(1234L, value1.value());
        Assert.assertEquals("1234", value2.value());
        Assert.assertEquals("12345", value3.value());
        Assert.assertEquals("", value4.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals(false, value3.value());

        Assert.assertEquals(false, value1.boolValue());
        Assert.assertEquals(true, value2.boolValue());
        Assert.assertEquals(false, value3.boolValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234L, value1.value());
        Assert.assertEquals("1234", value2.value());
        Assert.assertEquals("12345", value3.value());
        Assert.assertEquals("", value4.value());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        Assert.assertEquals(false, value1.boolValue());
        Assert.assertEquals(true, value2.boolValue());
        Assert.assertEquals(false, value3.boolValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertEquals("1234", value2.value());
        Assert.assertEquals("12345", value3.value());
        Assert.assertEquals("", value4.value());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals(false, value1.boolValue());
        Assert.assertEquals(true, value2.boolValue());
        Assert.assertEquals(false, value3.boolValue());

        value2.value(false);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Value value4 = BytesId.of("1");

        Assert.assertEquals(ValueType.ID, value1.valueType());
        Assert.assertEquals(ValueType.ID, value2.valueType());
        Assert.assertEquals(ValueType.ID, value3.valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        value2.value(false);
        Assert.assertEquals(false, value2.value());
        Assert.assertEquals(value3, value2);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.ID, value1.valueType());
        Assert.assertEquals(ValueType.ID, value2.valueType());
        Assert.assertEquals(ValueType.ID, value3.valueType());
        Assert.assertEquals(ValueType.ID, value4.valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        value2.value(false);
        Assert.assertEquals(false, value2.value());
        Assert.assertEquals(value3, value2);

        BooleanValue value5 = new BooleanValue(value2.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.ID, value1.valueType());
        Assert.assertEquals(ValueType.ID, value2.valueType());
        Assert.assertEquals(ValueType.ID, value3.valueType());
        Assert.assertEquals(ValueType.ID, value4.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        BooleanValue value5 = new BooleanValue(value2.value());
        Assert.assertEquals(value2, value5);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.ID, value2.valueType());
        Assert.assertEquals(ValueType.ID, value3.valueType());
        Assert.assertEquals(ValueType.ID, value4.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue value3 = new BooleanValue(false);

        Assert.assertEquals(ValueType.BOOLEAN, value1.valueType());
        Assert.assertEquals(ValueType.BOOLEAN, value2.valueType());
        Assert.assertEquals(ValueType.BOOLEAN, value3.valueType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Value value4 = new BytesId();

        Assert.assertEquals("1234", value1.string());
        Assert.assertEquals("1234", value2.string());
        Assert.assertEquals("12345", value3.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.BOOLEAN, value1.valueType());
        Assert.assertEquals(ValueType.BOOLEAN, value2.valueType());
        Assert.assertEquals(ValueType.BOOLEAN, value3.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        Assert.assertEquals("1234", value1.string());
        Assert.assertEquals("1234", value2.string());
        Assert.assertEquals("12345", value3.string());
        Assert.assertEquals("", value4.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.BOOLEAN, value1.valueType());
        Assert.assertEquals(ValueType.BOOLEAN, value2.valueType());
        Assert.assertEquals(ValueType.BOOLEAN, value3.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertEquals("1234", value1.string());
        Assert.assertEquals("1234", value2.string());
        Assert.assertEquals("12345", value3.string());
        Assert.assertEquals("", value4.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue value3 = new BooleanValue(false);

        Assert.assertEquals("false", value1.toString());
        Assert.assertEquals("true", value2.toString());
        Assert.assertEquals("false", value3.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertEquals("1234", value2.string());
        Assert.assertEquals("12345", value3.string());
        Assert.assertEquals("", value4.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        Assert.assertEquals("false", value1.toString());
        Assert.assertEquals("true", value2.toString());
        Assert.assertEquals("false", value3.toString());
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Value value3 = new BytesId(IdType.LONG, new byte[]{'1', '2', '3'});

        Assert.assertNotEquals(value1.hashCode(), value2.hashCode());
        Assert.assertNotEquals(value1.hashCode(), value3.hashCode());
        Assert.assertNotEquals(value2.hashCode(), value3.hashCode());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        Assert.assertNotEquals(value1.hashCode(), value2.hashCode());
        Assert.assertNotEquals(value1.hashCode(), value3.hashCode());
        Assert.assertNotEquals(value2.hashCode(), value3.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals("false", value1.toString());
        Assert.assertEquals("true", value2.toString());
        Assert.assertEquals("false", value3.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertNotEquals(value1.hashCode(), value2.hashCode());
        Assert.assertNotEquals(value1.hashCode(), value3.hashCode());
        Assert.assertNotEquals(value2.hashCode(), value3.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        BooleanValue copy = value1.copy();
        Assert.assertEquals(false, value1.value());
        Assert.assertEquals(false, copy.value());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Value value4 = BytesId.of(322L);

        Assert.assertEquals(0, value1.compareTo(value2));
        Assert.assertLt(0, value2.compareTo(value3));
        Assert.assertGt(0, value3.compareTo(value1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue copy = value1.copy();
        Assert.assertEquals(false, value1.value());
        Assert.assertEquals(false, copy.value());

        copy.assign(value2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        Value copy = value1.copy();
        Assert.assertEquals(value1, copy);

        copy.assign(value2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        copy.assign(value2);
        Assert.assertEquals(false, value1.value());
        Assert.assertEquals(true, copy.value());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        copy.assign(value2);
        Assert.assertEquals(value2, copy);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        value1.assign(value2);
        Assert.assertEquals(value2, value1);

        value2.assign(value3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        copy.assign(value2);
        Assert.assertEquals(false, value1.value());
        Assert.assertEquals(true, copy.value());

        copy = value2.copy();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        value2.assign(value3);
        Assert.assertEquals(value3, value2);

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        copy = value2.copy();
        Assert.assertEquals(true, value2.value());
        Assert.assertEquals(true, copy.value());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        copy = value2.copy();
        Assert.assertEquals(true, value2.value());
        Assert.assertEquals(true, copy.value());

        copy.assign(value3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        copy.assign(value3);
        Assert.assertEquals(true, value2.value());
        Assert.assertEquals(false, copy.value());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        copy.assign(value3);
        Assert.assertEquals(true, value2.value());
        Assert.assertEquals(false, copy.value());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue value3 = new BooleanValue(false);

        Assert.assertEquals(false, value1.value());
        value1.assign(value2);
        Assert.assertEquals(true, value1.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals(false, value1.value());
        value1.assign(value2);
        Assert.assertEquals(true, value1.value());
        Assert.assertEquals(true, value2.value());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        value1.assign(value2);
        Assert.assertEquals(true, value1.value());
        Assert.assertEquals(true, value2.value());

        value2.assign(value3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        value2.assign(value3);
        Assert.assertEquals(true, value1.value());
        Assert.assertEquals(false, value2.value());
        Assert.assertEquals(false, value3.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        value2.assign(value3);
        Assert.assertEquals(true, value1.value());
        Assert.assertEquals(false, value2.value());
        Assert.assertEquals(false, value3.value());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals(true, value1.value());
        Assert.assertEquals(false, value2.value());
        Assert.assertEquals(false, value3.value());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue value3 = new BooleanValue(false);

        Assert.assertEquals(Boolean.hashCode(false), value1.hashCode());
        Assert.assertEquals(Boolean.hashCode(true), value2.hashCode());
        Assert.assertEquals(Boolean.hashCode(false), value3.hashCode());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
    public void testEquals() {
        Value value1 = new BytesId();
        Assert.assertEquals(value1, value1);
        Assert.assertEquals(value1, new BytesId());
        Assert.assertNotEquals(value1,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Value value1 = new BytesId();
        Assert.assertEquals(value1, value1);
        Assert.assertEquals(value1, new BytesId());
        Assert.assertNotEquals(value1,
                               new BytesId(IdType.LONG, new byte[] {1}));
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertEquals(value1, value1);
        Assert.assertEquals(value1, new BytesId());
        Assert.assertNotEquals(value1,
                               new BytesId(IdType.LONG, new byte[] {1}));

```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
                               new BytesId(IdType.LONG, new byte[] {1}));

        Assert.assertNotEquals(value1, new IntValue(1));
        Assert.assertNotEquals(null, value1);
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        Assert.assertNotEquals(value1, new IntValue(1));
        Assert.assertNotEquals(null, value1);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        try (BytesInput bai = IOFactory.createBytesInput(bytes)) {
            value3.read(bai);
            Assert.assertEquals(value1, value3);
            value3.read(bai);
            Assert.assertEquals(value2, value3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
            Assert.assertEquals(value1, value3);
            value3.read(bai);
            Assert.assertEquals(value2, value3);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Value value3 = BytesId.of("123");

        Assert.assertEquals("", value1.toString());
        Assert.assertEquals("123", value2.toString());
        Assert.assertEquals("123", value3.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java

        Assert.assertEquals("", value1.toString());
        Assert.assertEquals("123", value2.toString());
        Assert.assertEquals("123", value3.toString());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertEquals("", value1.toString());
        Assert.assertEquals("123", value2.toString());
        Assert.assertEquals("123", value3.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        Assert.assertEquals(Boolean.hashCode(false), value1.hashCode());
        Assert.assertEquals(Boolean.hashCode(true), value2.hashCode());
        Assert.assertEquals(Boolean.hashCode(false), value3.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Boolean.hashCode(false), value1.hashCode());
        Assert.assertEquals(Boolean.hashCode(true), value2.hashCode());
        Assert.assertEquals(Boolean.hashCode(false), value3.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        BooleanValue value3 = new BooleanValue(false);

        Assert.assertEquals("false", value1.string());
        Assert.assertEquals("true", value2.string());
        Assert.assertEquals("false", value3.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        Assert.assertEquals("false", value1.string());
        Assert.assertEquals("true", value2.string());
        Assert.assertEquals("false", value3.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertEquals("false", value1.string());
        Assert.assertEquals("true", value2.string());
        Assert.assertEquals("false", value3.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdListListTest.java`
#### Snippet
```java
        listListValue2.add(listValue1);

        Assert.assertEquals(ValueType.ID_LIST_LIST,
                            listListValue1.valueType());
        Assert.assertEquals(ValueType.ID_LIST,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdListListTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.ID_LIST_LIST,
                            listListValue1.valueType());
        Assert.assertEquals(ValueType.ID_LIST,
                            listListValue1.elemType());
        Assert.assertTrue(ListUtils.isEqualList(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdListListTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.ID_LIST,
                            listListValue1.elemType());
        Assert.assertTrue(ListUtils.isEqualList(
                          Lists.newArrayList(listValue1),
                          listListValue1.values()));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdListListTest.java`
#### Snippet
```java
                          Lists.newArrayList(listValue1),
                          listListValue1.values()));
        Assert.assertEquals(listListValue1, listListValue2);

        listValue2.add(longId2);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdListListTest.java`
#### Snippet
```java
        listValue2.add(longId2);
        listListValue2.add(listValue2);
        Assert.assertTrue(ListUtils.isEqualList(
                          Lists.newArrayList(listValue1, listValue2),
                          listListValue2.values()));
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdListListTest.java`
#### Snippet
```java
                          Lists.newArrayList(listValue1, listValue2),
                          listListValue2.values()));
        Assert.assertNotEquals(listListValue1, listListValue2);
        Assert.assertEquals(ListUtils.hashCodeForList(
                            Lists.newArrayList(listValue1)),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdListListTest.java`
#### Snippet
```java
                          listListValue2.values()));
        Assert.assertNotEquals(listListValue1, listListValue2);
        Assert.assertEquals(ListUtils.hashCodeForList(
                            Lists.newArrayList(listValue1)),
                            listListValue1.hashCode());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdListListTest.java`
#### Snippet
```java
        value3.add(listValue);

        Assert.assertEquals(0, value1.compareTo(value2));
        Assert.assertLt(0, value1.compareTo(value3));
        Assert.assertGt(0, value3.compareTo(value1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            try (BufferedFileInput input = createInput(file)) {
                for (int i = 0; i < size; i++) {
                    Assert.assertEquals(4096 - i * 4, input.available());
                    Assert.assertEquals(i, input.readInt());
                }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                for (int i = 0; i < size; i++) {
                    Assert.assertEquals(4096 - i * 4, input.available());
                    Assert.assertEquals(i, input.readInt());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            try (BufferedFileInput input = createInput(file)) {
                input.seek(100L);
                Assert.assertEquals(1, input.readInt());
                input.seek(511L);
                Assert.assertEquals(2, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertEquals(1, input.readInt());
                input.seek(511L);
                Assert.assertEquals(2, input.readInt());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                for (int i = 0; i < loopTimes; i++) {
                    input.readFully(arrayRead);
                    Assert.assertArrayEquals(array, arrayRead);
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            try (BufferedFileInput input = createInput(file)) {
                for (int i = -128; i <= 127; i++) {
                    Assert.assertEquals(i, input.readByte());
                }
                input.skip(4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                }
                input.skip(4);
                Assert.assertEquals(127, input.readByte());
                input.skip(4);
                input.skip(1280);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                input.skip(4);
                input.skip(1280);
                Assert.assertEquals((byte) 1, input.readByte());
                Assert.assertThrows(IllegalArgumentException.class, () -> {
                    input.skip(-1);
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            for (int i = 0; i < loopTimes; i++) {
                dis.readFully(arrayRead);
                Assert.assertArrayEquals(array, arrayRead);
            }
        }
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            for (int i = 0; i < loopTimes; i++) {
                input.readFully(arrayRead);
                Assert.assertArrayEquals(array, arrayRead);
            }
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            try (BufferedFileOutput output = createOutput(file)) {
                for (int i = 0; i < size; i++) {
                    Assert.assertEquals(i * 4, output.position());
                    output.writeInt(i);
                }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            try (BufferedFileInput input = createInput(file)) {
                for (int i = 0; i < size; i++) {
                    Assert.assertEquals(i * 4, input.position());
                    Assert.assertEquals(i, input.readInt());
                }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                for (int i = 0; i < size; i++) {
                    Assert.assertEquals(i * 4, input.position());
                    Assert.assertEquals(i, input.readInt());
                }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                    long position = 4 * random.nextInt(size);
                    input.seek(position);
                    Assert.assertEquals(position / 4, input.readInt());
                    Assert.assertEquals(position + 4, input.position());
                }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                    input.seek(position);
                    Assert.assertEquals(position / 4, input.readInt());
                    Assert.assertEquals(position + 4, input.position());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java

            try (BufferedFileInput input = createInput(file)) {
                Assert.assertEquals(1, input.readInt());
                input.skip(4);
                Assert.assertThrows(EOFException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                        position != 200 && position != 100 &&
                        position != 1000) {
                        Assert.assertEquals(expectValue, readValue);
                    }
                }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                }
                input.seek(0);
                Assert.assertEquals(1, input.readInt());
                input.seek(12);
                Assert.assertEquals(2, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertEquals(1, input.readInt());
                input.seek(12);
                Assert.assertEquals(2, input.readInt());
                input.seek(200);
                Assert.assertEquals(3, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertEquals(2, input.readInt());
                input.seek(200);
                Assert.assertEquals(3, input.readInt());
                input.seek(100);
                Assert.assertEquals(4, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertEquals(3, input.readInt());
                input.seek(100);
                Assert.assertEquals(4, input.readInt());
                input.seek(256 * 4);
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertEquals(4, input.readInt());
                input.seek(256 * 4);
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                Assert.assertEquals(6, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                input.seek(256 * 4);
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                Assert.assertEquals(6, input.readInt());
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                Assert.assertEquals(6, input.readInt());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
        try {
            try (BufferedFileOutput output = new BufferedFileOutput(file)) {
                Assert.assertEquals(0, output.position());
            }
            try (BufferedFileInput input = new BufferedFileInput(file)) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            }
            try (BufferedFileInput input = new BufferedFileInput(file)) {
                Assert.assertEquals(0, input.position());
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            try (BufferedFileInput input = createInput(file)) {
                for (int i = -128; i <= 127; i++) {
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
             */
            result = input1.compare(12, 3, input2, 12, 3);
            Assert.assertEquals(0, result);

            /*
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                    long position = 4 * random.nextInt(size);
                    input.seek(position);
                    Assert.assertEquals(size - position / 4, input.readInt());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            try (BufferedFileInput input = createInput(file)) {
                for (int i = -128; i <= 125; i++) {
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                input.seek(input.position() - 8);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                }
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                input.seek(input.position() - 8);
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                input.seek(input.position() - 8);
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                input.seek(input.position() - 8);
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/TestData.java`
#### Snippet
```java

        while (inputsSorting.hasNext() && sortedResult.hasNext()) {
            Assert.assertEquals(inputsSorting.next(), sortedResult.next());
        }

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/TestData.java`
#### Snippet
```java
        }

        Assert.assertFalse(inputsSorting.hasNext());
        Assert.assertFalse(sortedResult.hasNext());
        Assert.assertThrows(NoSuchElementException.class, inputsSorting::next);
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/TestData.java`
#### Snippet
```java

        Assert.assertFalse(inputsSorting.hasNext());
        Assert.assertFalse(sortedResult.hasNext());
        Assert.assertThrows(NoSuchElementException.class, inputsSorting::next);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java

        StringValue copy = value1.copy();
        Assert.assertEquals("test", value1.value());
        Assert.assertEquals(value1, copy);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        StringValue copy = value1.copy();
        Assert.assertEquals("test", value1.value());
        Assert.assertEquals(value1, copy);

        StringValue value2 = new StringValue("assign test");
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        StringValue value2 = new StringValue("assign test");
        copy.assign(value2);
        Assert.assertEquals("assign test", copy.value());
        Assert.assertEquals(value2, copy);
        Assert.assertNotEquals(value1, copy);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        copy.assign(value2);
        Assert.assertEquals("assign test", copy.value());
        Assert.assertEquals(value2, copy);
        Assert.assertNotEquals(value1, copy);
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        Assert.assertEquals("assign test", copy.value());
        Assert.assertEquals(value2, copy);
        Assert.assertNotEquals(value1, copy);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        StringValue value4 = new StringValue("test");

        Assert.assertEquals("", value1.string());
        Assert.assertEquals("t1", value2.string());
        Assert.assertEquals("t1", value3.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java

        Assert.assertEquals("", value1.string());
        Assert.assertEquals("t1", value2.string());
        Assert.assertEquals("t1", value3.string());
        Assert.assertEquals("test", value4.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        Assert.assertEquals("", value1.string());
        Assert.assertEquals("t1", value2.string());
        Assert.assertEquals("t1", value3.string());
        Assert.assertEquals("test", value4.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        Assert.assertEquals("t1", value2.string());
        Assert.assertEquals("t1", value3.string());
        Assert.assertEquals("test", value4.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        StringValue value2 = new StringValue("test");

        Assert.assertEquals(value1, value2);

        DoubleValue value3 = new DoubleValue(1.1);
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java

        DoubleValue value3 = new DoubleValue(1.1);
        Assert.assertFalse(value1.equals(value3));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        StringValue value2 = new StringValue("t2");

        Assert.assertEquals(ValueType.STRING, value1.valueType());
        Assert.assertEquals(ValueType.STRING, value2.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.STRING, value1.valueType());
        Assert.assertEquals(ValueType.STRING, value2.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        StringValue value2 = new StringValue("test");

        Assert.assertEquals(Constants.EMPTY_STR,
                            value1.toString());
        Assert.assertEquals("test",
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Constants.EMPTY_STR,
                            value1.toString());
        Assert.assertEquals("test",
                            value2.toString());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        StringValue value2 = new StringValue("test");

        Assert.assertEquals(Constants.EMPTY_STR.hashCode(),
                            value1.hashCode());
        Assert.assertEquals("test".hashCode(),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Constants.EMPTY_STR.hashCode(),
                            value1.hashCode());
        Assert.assertEquals("test".hashCode(),
                            value2.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        value1.assign(value2);

        Assert.assertEquals("test", value1.value());
        Assert.assertEquals("test", value2.value());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java

        Assert.assertEquals("test", value1.value());
        Assert.assertEquals("test", value2.value());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        StringValue value2 = new StringValue("t1");

        Assert.assertEquals("t1", value1.value());
        Assert.assertEquals(value1, value2);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java

        Assert.assertEquals("t1", value1.value());
        Assert.assertEquals(value1, value2);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        NullValue value2 = NullValue.get();

        Assert.assertEquals("<null>", value1.toString());
        Assert.assertEquals("<null>", value2.toString());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        Assert.assertEquals("<null>", value1.toString());
        Assert.assertEquals("<null>", value2.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        NullValue value1 = NullValue.get();
        NullValue value2 = NullValue.get();
        Assert.assertEquals(0, value1.compareTo(value2));

        Assert.assertLt(0, value1.compareTo(new BooleanValue()));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        NullValue value2 = NullValue.get();

        Assert.assertEquals(0, value1.hashCode());
        Assert.assertEquals(0, value2.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0, value1.hashCode());
        Assert.assertEquals(0, value2.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        NullValue value1 = NullValue.get();

        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(NullValue.get()));

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(NullValue.get()));

        Assert.assertFalse(value1.equals(new IntValue(1)));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(NullValue.get()));

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        value1.assign(value2);
        Assert.assertEquals(value1, value2);

        value2.assign(value2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        value2.assign(value2);
        Assert.assertEquals(value1, value2);

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        NullValue value2 = NullValue.get();

        Assert.assertEquals(value1, value2);
        Assert.assertEquals(value1.value(), value2.value());
        Assert.assertNull(value1.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        Assert.assertEquals(value1, value2);
        Assert.assertEquals(value1.value(), value2.value());
        Assert.assertNull(value1.value());
        Assert.assertNull(value2.value());
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        Assert.assertEquals(value1, value2);
        Assert.assertEquals(value1.value(), value2.value());
        Assert.assertNull(value1.value());
        Assert.assertNull(value2.value());
    }
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        Assert.assertEquals(value1.value(), value2.value());
        Assert.assertNull(value1.value());
        Assert.assertNull(value2.value());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        NullValue value2 = NullValue.get();

        Assert.assertEquals(ValueType.NULL, value1.valueType());
        Assert.assertEquals(ValueType.NULL, value2.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.NULL, value1.valueType());
        Assert.assertEquals(ValueType.NULL, value2.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        NullValue value2 = NullValue.get();

        Assert.assertEquals("", value1.string());
        Assert.assertEquals("", value2.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        Assert.assertEquals("", value1.string());
        Assert.assertEquals("", value2.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        NullValue copy = value1.copy();
        Assert.assertEquals(value1, copy);

        copy.assign(value2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        copy.assign(value2);
        Assert.assertEquals(value2, copy);
        Assert.assertEquals(value1, copy);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        copy.assign(value2);
        Assert.assertEquals(value2, copy);
        Assert.assertEquals(value1, copy);
    }

```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/MockWorkerInputManager.java`
#### Snippet
```java
            Edge edge = edgeFetcher.next();
            // Write edge to buffer
            Assert.assertNotNull(edge);
            count++;
        }
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/MockWorkerInputManager.java`
#### Snippet
```java
            Vertex vertex = vertexFetcher.next();
            // Write vertex to buffer
            Assert.assertNotNull(vertex);
            count++;
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat2.mergeSendMessageStat(new MessageStat(5L, 6L));
        stat2.mergeRecvMessageStat(new MessageStat(7L, 8L));
        Assert.assertEquals(Integer.hashCode(0), stat1.hashCode());
        Assert.assertEquals(Integer.hashCode(1), stat2.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat2.mergeRecvMessageStat(new MessageStat(7L, 8L));
        Assert.assertEquals(Integer.hashCode(0), stat1.hashCode());
        Assert.assertEquals(Integer.hashCode(1), stat2.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        PartitionStat stat1 = new PartitionStat();

        Assert.assertEquals(0, stat1.partitionId());
        Assert.assertEquals(0L, stat1.vertexCount());
        Assert.assertEquals(0L, stat1.edgeCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java

        Assert.assertEquals(0, stat1.partitionId());
        Assert.assertEquals(0L, stat1.vertexCount());
        Assert.assertEquals(0L, stat1.edgeCount());
        Assert.assertEquals(0L, stat1.finishedVertexCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(0, stat1.partitionId());
        Assert.assertEquals(0L, stat1.vertexCount());
        Assert.assertEquals(0L, stat1.edgeCount());
        Assert.assertEquals(0L, stat1.finishedVertexCount());
        Assert.assertEquals(0L, stat1.messageSendCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, stat1.vertexCount());
        Assert.assertEquals(0L, stat1.edgeCount());
        Assert.assertEquals(0L, stat1.finishedVertexCount());
        Assert.assertEquals(0L, stat1.messageSendCount());
        Assert.assertEquals(0L, stat1.messageSendBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, stat1.edgeCount());
        Assert.assertEquals(0L, stat1.finishedVertexCount());
        Assert.assertEquals(0L, stat1.messageSendCount());
        Assert.assertEquals(0L, stat1.messageSendBytes());
        Assert.assertEquals(0L, stat1.messageRecvCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, stat1.finishedVertexCount());
        Assert.assertEquals(0L, stat1.messageSendCount());
        Assert.assertEquals(0L, stat1.messageSendBytes());
        Assert.assertEquals(0L, stat1.messageRecvCount());
        Assert.assertEquals(0L, stat1.messageRecvBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, stat1.messageSendCount());
        Assert.assertEquals(0L, stat1.messageSendBytes());
        Assert.assertEquals(0L, stat1.messageRecvCount());
        Assert.assertEquals(0L, stat1.messageRecvBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, stat1.messageSendBytes());
        Assert.assertEquals(0L, stat1.messageRecvCount());
        Assert.assertEquals(0L, stat1.messageRecvBytes());

        PartitionStat stat2 = new PartitionStat(1, 4L, 3L, 2L);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat2.mergeRecvMessageStat(new MessageStat(7L, 8L));

        Assert.assertEquals(1, stat2.partitionId());
        Assert.assertEquals(4L, stat2.vertexCount());
        Assert.assertEquals(3L, stat2.edgeCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java

        Assert.assertEquals(1, stat2.partitionId());
        Assert.assertEquals(4L, stat2.vertexCount());
        Assert.assertEquals(3L, stat2.edgeCount());
        Assert.assertEquals(2L, stat2.finishedVertexCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(1, stat2.partitionId());
        Assert.assertEquals(4L, stat2.vertexCount());
        Assert.assertEquals(3L, stat2.edgeCount());
        Assert.assertEquals(2L, stat2.finishedVertexCount());
        Assert.assertEquals(5L, stat2.messageSendCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(4L, stat2.vertexCount());
        Assert.assertEquals(3L, stat2.edgeCount());
        Assert.assertEquals(2L, stat2.finishedVertexCount());
        Assert.assertEquals(5L, stat2.messageSendCount());
        Assert.assertEquals(6L, stat2.messageSendBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(3L, stat2.edgeCount());
        Assert.assertEquals(2L, stat2.finishedVertexCount());
        Assert.assertEquals(5L, stat2.messageSendCount());
        Assert.assertEquals(6L, stat2.messageSendBytes());
        Assert.assertEquals(7L, stat2.messageRecvCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(2L, stat2.finishedVertexCount());
        Assert.assertEquals(5L, stat2.messageSendCount());
        Assert.assertEquals(6L, stat2.messageSendBytes());
        Assert.assertEquals(7L, stat2.messageRecvCount());
        Assert.assertEquals(8L, stat2.messageRecvBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(5L, stat2.messageSendCount());
        Assert.assertEquals(6L, stat2.messageSendBytes());
        Assert.assertEquals(7L, stat2.messageRecvCount());
        Assert.assertEquals(8L, stat2.messageRecvBytes());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(6L, stat2.messageSendBytes());
        Assert.assertEquals(7L, stat2.messageRecvCount());
        Assert.assertEquals(8L, stat2.messageRecvBytes());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat40.mergeRecvMessageStat(new MessageStat(5L, 6L));

        Assert.assertEquals(stat1, stat2);
        Assert.assertNotEquals(stat1, stat3);
        Assert.assertNotEquals(stat1, stat4);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java

        Assert.assertEquals(stat1, stat2);
        Assert.assertNotEquals(stat1, stat3);
        Assert.assertNotEquals(stat1, stat4);
        Assert.assertNotEquals(stat3, stat4);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(stat1, stat2);
        Assert.assertNotEquals(stat1, stat3);
        Assert.assertNotEquals(stat1, stat4);
        Assert.assertNotEquals(stat3, stat4);
        Assert.assertNotEquals(stat1, new Object());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertNotEquals(stat1, stat3);
        Assert.assertNotEquals(stat1, stat4);
        Assert.assertNotEquals(stat3, stat4);
        Assert.assertNotEquals(stat1, new Object());
        Assert.assertEquals(stat3, stat30);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertNotEquals(stat1, stat4);
        Assert.assertNotEquals(stat3, stat4);
        Assert.assertNotEquals(stat1, new Object());
        Assert.assertEquals(stat3, stat30);
        Assert.assertEquals(stat4, stat40);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertNotEquals(stat3, stat4);
        Assert.assertNotEquals(stat1, new Object());
        Assert.assertEquals(stat3, stat30);
        Assert.assertEquals(stat4, stat40);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertNotEquals(stat1, new Object());
        Assert.assertEquals(stat3, stat30);
        Assert.assertEquals(stat4, stat40);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat1.mergeRecvMessageStat(new MessageStat(7L, 8L));

        Assert.assertEquals(1L, stat1.vertexCount());
        Assert.assertEquals(2L, stat1.edgeCount());
        Assert.assertEquals(5L, stat1.messageSendCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java

        Assert.assertEquals(1L, stat1.vertexCount());
        Assert.assertEquals(2L, stat1.edgeCount());
        Assert.assertEquals(5L, stat1.messageSendCount());
        Assert.assertEquals(6L, stat1.messageSendBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(1L, stat1.vertexCount());
        Assert.assertEquals(2L, stat1.edgeCount());
        Assert.assertEquals(5L, stat1.messageSendCount());
        Assert.assertEquals(6L, stat1.messageSendBytes());
        Assert.assertEquals(7L, stat1.messageRecvCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(2L, stat1.edgeCount());
        Assert.assertEquals(5L, stat1.messageSendCount());
        Assert.assertEquals(6L, stat1.messageSendBytes());
        Assert.assertEquals(7L, stat1.messageRecvCount());
        Assert.assertEquals(8L, stat1.messageRecvBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(5L, stat1.messageSendCount());
        Assert.assertEquals(6L, stat1.messageSendBytes());
        Assert.assertEquals(7L, stat1.messageRecvCount());
        Assert.assertEquals(8L, stat1.messageRecvBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(6L, stat1.messageSendBytes());
        Assert.assertEquals(7L, stat1.messageRecvCount());
        Assert.assertEquals(8L, stat1.messageRecvBytes());

        stat1.mergeSendMessageStat(new MessageStat(15L, 16L));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat1.mergeSendMessageStat(new MessageStat(15L, 16L));
        stat1.mergeRecvMessageStat(new MessageStat(17L, 18L));
        Assert.assertEquals(20L, stat1.messageSendCount());
        Assert.assertEquals(22L, stat1.messageSendBytes());
        Assert.assertEquals(24L, stat1.messageRecvCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat1.mergeRecvMessageStat(new MessageStat(17L, 18L));
        Assert.assertEquals(20L, stat1.messageSendCount());
        Assert.assertEquals(22L, stat1.messageSendBytes());
        Assert.assertEquals(24L, stat1.messageRecvCount());
        Assert.assertEquals(26L, stat1.messageRecvBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(20L, stat1.messageSendCount());
        Assert.assertEquals(22L, stat1.messageSendBytes());
        Assert.assertEquals(24L, stat1.messageRecvCount());
        Assert.assertEquals(26L, stat1.messageRecvBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(22L, stat1.messageSendBytes());
        Assert.assertEquals(24L, stat1.messageRecvCount());
        Assert.assertEquals(26L, stat1.messageRecvBytes());

        MessageStat messageStat = new MessageStat(100L, 400L);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat1.mergeSendMessageStat(messageStat);
        stat1.mergeRecvMessageStat(messageStat);
        Assert.assertEquals(1L, stat1.vertexCount());
        Assert.assertEquals(2L, stat1.edgeCount());
        Assert.assertEquals(120L, stat1.messageSendCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        stat1.mergeRecvMessageStat(messageStat);
        Assert.assertEquals(1L, stat1.vertexCount());
        Assert.assertEquals(2L, stat1.edgeCount());
        Assert.assertEquals(120L, stat1.messageSendCount());
        Assert.assertEquals(422L, stat1.messageSendBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(1L, stat1.vertexCount());
        Assert.assertEquals(2L, stat1.edgeCount());
        Assert.assertEquals(120L, stat1.messageSendCount());
        Assert.assertEquals(422L, stat1.messageSendBytes());
        Assert.assertEquals(124L, stat1.messageRecvCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(2L, stat1.edgeCount());
        Assert.assertEquals(120L, stat1.messageSendCount());
        Assert.assertEquals(422L, stat1.messageSendBytes());
        Assert.assertEquals(124L, stat1.messageRecvCount());
        Assert.assertEquals(426L, stat1.messageRecvBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(120L, stat1.messageSendCount());
        Assert.assertEquals(422L, stat1.messageSendBytes());
        Assert.assertEquals(124L, stat1.messageRecvCount());
        Assert.assertEquals(426L, stat1.messageRecvBytes());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
        Assert.assertEquals(422L, stat1.messageSendBytes());
        Assert.assertEquals(124L, stat1.messageRecvCount());
        Assert.assertEquals(426L, stat1.messageRecvBytes());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/PartitionStatTest.java`
#### Snippet
```java
                     "\"messageSendCount\":5,\"messageSendBytes\":6," +
                     "\"messageRecvCount\":7,\"messageRecvBytes\":8}";
        Assert.assertEquals(str, stat.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        FloatValue copy = value1.copy();
        Assert.assertEquals(0f, value1.value(), 0f);
        Assert.assertEquals(0f, copy.value(), 0f);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue copy = value1.copy();
        Assert.assertEquals(0f, value1.value(), 0f);
        Assert.assertEquals(0f, copy.value(), 0f);

        copy.assign(value2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        copy.assign(value2);
        Assert.assertEquals(1234.56f, copy.value(), 0f);
        Assert.assertEquals(0f, value1.value(), 0f);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        copy.assign(value2);
        Assert.assertEquals(1234.56f, copy.value(), 0f);
        Assert.assertEquals(0f, value1.value(), 0f);
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
    public void testEquals() {
        FloatValue value1 = new FloatValue(0f);
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new FloatValue(0f)));
        Assert.assertFalse(value1.equals(new FloatValue(1)));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value1 = new FloatValue(0f);
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new FloatValue(0f)));
        Assert.assertFalse(value1.equals(new FloatValue(1)));
        Assert.assertFalse(value1.equals(new FloatValue(1.0f)));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new FloatValue(0f)));
        Assert.assertFalse(value1.equals(new FloatValue(1)));
        Assert.assertFalse(value1.equals(new FloatValue(1.0f)));

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(new FloatValue(0f)));
        Assert.assertFalse(value1.equals(new FloatValue(1)));
        Assert.assertFalse(value1.equals(new FloatValue(1.0f)));

        Assert.assertFalse(value1.equals(new IntValue(0)));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value1.equals(new FloatValue(1.0f)));

        Assert.assertFalse(value1.equals(new IntValue(0)));
        Assert.assertFalse(value1.equals(null));
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(new IntValue(0)));
        Assert.assertFalse(value1.equals(null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value4 = new FloatValue(Float.MAX_VALUE);

        Assert.assertEquals("0.0", value1.string());
        Assert.assertEquals("1234.56", value2.string());
        Assert.assertEquals("1.4E-45", value3.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertEquals("0.0", value1.string());
        Assert.assertEquals("1234.56", value2.string());
        Assert.assertEquals("1.4E-45", value3.string());
        Assert.assertEquals("3.4028235E38", value4.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals("0.0", value1.string());
        Assert.assertEquals("1234.56", value2.string());
        Assert.assertEquals("1.4E-45", value3.string());
        Assert.assertEquals("3.4028235E38", value4.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals("1234.56", value2.string());
        Assert.assertEquals("1.4E-45", value3.string());
        Assert.assertEquals("3.4028235E38", value4.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value4 = new FloatValue(Float.MAX_VALUE);

        Assert.assertEquals(0f, value1.value(), 0f);
        value1.assign(value2);
        Assert.assertEquals(1234.56f, value1.value(), 0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0f, value1.value(), 0f);
        value1.assign(value2);
        Assert.assertEquals(1234.56f, value1.value(), 0f);
        Assert.assertEquals(1234.56f, value2.value(), 0f);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        value1.assign(value2);
        Assert.assertEquals(1234.56f, value1.value(), 0f);
        Assert.assertEquals(1234.56f, value2.value(), 0f);

        value2.assign(value3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        value2.assign(value3);
        Assert.assertEquals(1234.56f, value1.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value2.value(), 0f);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        value2.assign(value3);
        Assert.assertEquals(1234.56f, value1.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value2.value(), 0f);

        value2.assign(value4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        value2.assign(value4);
        Assert.assertEquals(1234.56f, value1.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value2.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value3.value(), 0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        value2.assign(value4);
        Assert.assertEquals(1234.56f, value1.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value2.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value3.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value4.value(), 0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234.56f, value1.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value2.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value3.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value4.value(), 0f);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Float.MAX_VALUE, value2.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value3.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value4.value(), 0f);

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value4 = new FloatValue(Float.MAX_VALUE);

        Assert.assertEquals(0f, value1.value(), 0f);
        Assert.assertEquals(1234.56f, value2.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value3.value(), 0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0f, value1.value(), 0f);
        Assert.assertEquals(1234.56f, value2.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value3.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value4.value(), 0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0f, value1.value(), 0f);
        Assert.assertEquals(1234.56f, value2.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value3.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value4.value(), 0f);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234.56f, value2.value(), 0f);
        Assert.assertEquals(Float.MIN_VALUE, value3.value(), 0f);
        Assert.assertEquals(Float.MAX_VALUE, value4.value(), 0f);

        value3.value(Float.MAX_VALUE);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        value3.value(Float.MAX_VALUE);
        Assert.assertEquals(Float.MAX_VALUE, value3.value(), 0f);
        Assert.assertEquals(value3, value4);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        value3.value(Float.MAX_VALUE);
        Assert.assertEquals(Float.MAX_VALUE, value3.value(), 0f);
        Assert.assertEquals(value3, value4);

        FloatValue value5 = new FloatValue(value2.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        FloatValue value5 = new FloatValue(value2.value());
        Assert.assertEquals(value2, value5);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value4 = new FloatValue(Float.MAX_VALUE);

        Assert.assertEquals(Float.hashCode(0f),
                            value1.hashCode());
        Assert.assertEquals(Float.hashCode(1234.56f),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Float.hashCode(0f),
                            value1.hashCode());
        Assert.assertEquals(Float.hashCode(1234.56f),
                            value2.hashCode());
        Assert.assertEquals(Float.hashCode(Float.MIN_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Float.hashCode(1234.56f),
                            value2.hashCode());
        Assert.assertEquals(Float.hashCode(Float.MIN_VALUE),
                            value3.hashCode());
        Assert.assertEquals(Float.hashCode(Float.MAX_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Float.hashCode(Float.MIN_VALUE),
                            value3.hashCode());
        Assert.assertEquals(Float.hashCode(Float.MAX_VALUE),
                            value4.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value2 = new FloatValue(Float.MIN_VALUE);

        Assert.assertEquals(ValueType.FLOAT, value1.valueType());
        Assert.assertEquals(ValueType.FLOAT, value2.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.FLOAT, value1.valueType());
        Assert.assertEquals(ValueType.FLOAT, value2.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value4 = new FloatValue(Float.MAX_VALUE);

        Assert.assertEquals("0.0", value1.toString());
        Assert.assertEquals("1234.56", value2.toString());
        Assert.assertEquals("1.4E-45", value3.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertEquals("0.0", value1.toString());
        Assert.assertEquals("1234.56", value2.toString());
        Assert.assertEquals("1.4E-45", value3.toString());
        Assert.assertEquals("3.4028235E38", value4.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals("0.0", value1.toString());
        Assert.assertEquals("1234.56", value2.toString());
        Assert.assertEquals("1.4E-45", value3.toString());
        Assert.assertEquals("3.4028235E38", value4.toString());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals("1234.56", value2.toString());
        Assert.assertEquals("1.4E-45", value3.toString());
        Assert.assertEquals("3.4028235E38", value4.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value2 = new FloatValue(123f);
        FloatValue value3 = new FloatValue(321f);
        Assert.assertEquals(0, value1.compareTo(value2));
        Assert.assertLt(0, value1.compareTo(value3));
        Assert.assertGt(0, value3.compareTo(value1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        FloatValue value4 = new FloatValue(Float.MAX_VALUE);

        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

        Assert.assertEquals(1234, value2.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

        Assert.assertEquals(1234, value2.intValue());
        Assert.assertEquals(1234L, value2.longValue());
        Assert.assertEquals(1234.56f, value2.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertEquals(1234, value2.intValue());
        Assert.assertEquals(1234L, value2.longValue());
        Assert.assertEquals(1234.56f, value2.floatValue(), 0.0f);
        Assert.assertEquals(1234.56005859375d, value2.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234, value2.intValue());
        Assert.assertEquals(1234L, value2.longValue());
        Assert.assertEquals(1234.56f, value2.floatValue(), 0.0f);
        Assert.assertEquals(1234.56005859375d, value2.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234L, value2.longValue());
        Assert.assertEquals(1234.56f, value2.floatValue(), 0.0f);
        Assert.assertEquals(1234.56005859375d, value2.doubleValue(), 0.0d);

        Assert.assertEquals(0, value3.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234.56005859375d, value2.doubleValue(), 0.0d);

        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(0L, value3.longValue());
        Assert.assertEquals(1.4E-45f, value3.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(0L, value3.longValue());
        Assert.assertEquals(1.4E-45f, value3.floatValue(), 0.0f);
        Assert.assertEquals(1.401298464324817E-45d, value3.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(0L, value3.longValue());
        Assert.assertEquals(1.4E-45f, value3.floatValue(), 0.0f);
        Assert.assertEquals(1.401298464324817E-45d, value3.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, value3.longValue());
        Assert.assertEquals(1.4E-45f, value3.floatValue(), 0.0f);
        Assert.assertEquals(1.401298464324817E-45d, value3.doubleValue(), 0.0d);

        Assert.assertEquals(2147483647, value4.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1.401298464324817E-45d, value3.doubleValue(), 0.0d);

        Assert.assertEquals(2147483647, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(3.4028235E38f, value4.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SortLargeDataTest.java`
#### Snippet
```java
            mergeTotal += HgkvDirImpl.open(output).numEntries();
        }
        Assert.assertEquals(total, mergeTotal);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SortLargeDataTest.java`
#### Snippet
```java
        // Assert result
        long result = sumOfEntryValue(sorter, ImmutableList.of(resultFile));
        Assert.assertEquals(totalValue, result);
        assertFileOrder(sorter, ImmutableList.of(resultFile));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SortLargeDataTest.java`
#### Snippet
```java

        long result = sumOfEntryValue(sorter, ImmutableList.of(resultFile));
        Assert.assertEquals(value, result);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SortLargeDataTest.java`
#### Snippet
```java
        // Assert result
        long result = sumOfEntryValue(sorter, ImmutableList.of(resultFile));
        Assert.assertEquals(1000 * 100, result);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class,
                            () -> HugeConverter.convertId(null));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId((byte) 1));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId((short) 1));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId(1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
                            () -> HugeConverter.convertId(null));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId((byte) 1));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId((short) 1));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId(1));
        Assert.assertEquals(BytesId.of(1L), HugeConverter.convertId(1L));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId((byte) 1));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId((short) 1));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId(1));
        Assert.assertEquals(BytesId.of(1L), HugeConverter.convertId(1L));
        Assert.assertEquals(BytesId.of("abc"), HugeConverter.convertId("abc"));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId((short) 1));
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId(1));
        Assert.assertEquals(BytesId.of(1L), HugeConverter.convertId(1L));
        Assert.assertEquals(BytesId.of("abc"), HugeConverter.convertId("abc"));
        UUID uuid = UUID.randomUUID();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertEquals(BytesId.of(1), HugeConverter.convertId(1));
        Assert.assertEquals(BytesId.of(1L), HugeConverter.convertId(1L));
        Assert.assertEquals(BytesId.of("abc"), HugeConverter.convertId("abc"));
        UUID uuid = UUID.randomUUID();
        Assert.assertEquals(BytesId.of(uuid), HugeConverter.convertId(uuid));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertEquals(BytesId.of("abc"), HugeConverter.convertId("abc"));
        UUID uuid = UUID.randomUUID();
        Assert.assertEquals(BytesId.of(uuid), HugeConverter.convertId(uuid));

        Assert.assertThrows(ComputerException.class,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        properties.put("list-value", listValue);

        Assert.assertEquals(properties,
                            HugeConverter.convertProperties(rawProperties));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
    @Test
    public void testConvertValue() {
        Assert.assertEquals(NullValue.get(), HugeConverter.convertValue(null));
        Assert.assertEquals(new BooleanValue(true),
                            HugeConverter.convertValue(true));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
    public void testConvertValue() {
        Assert.assertEquals(NullValue.get(), HugeConverter.convertValue(null));
        Assert.assertEquals(new BooleanValue(true),
                            HugeConverter.convertValue(true));
        Assert.assertEquals(new IntValue(1), HugeConverter.convertValue(1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertEquals(new BooleanValue(true),
                            HugeConverter.convertValue(true));
        Assert.assertEquals(new IntValue(1), HugeConverter.convertValue(1));
        Assert.assertEquals(new LongValue(-1L),
                            HugeConverter.convertValue(-1L));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
                            HugeConverter.convertValue(true));
        Assert.assertEquals(new IntValue(1), HugeConverter.convertValue(1));
        Assert.assertEquals(new LongValue(-1L),
                            HugeConverter.convertValue(-1L));
        Assert.assertEquals(new FloatValue(0.999F),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(-1L),
                            HugeConverter.convertValue(-1L));
        Assert.assertEquals(new FloatValue(0.999F),
                            HugeConverter.convertValue(0.999F));
        Assert.assertEquals(new DoubleValue(-0.001D),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(0.999F),
                            HugeConverter.convertValue(0.999F));
        Assert.assertEquals(new DoubleValue(-0.001D),
                            HugeConverter.convertValue(-0.001D));
        Assert.assertEquals(new StringValue("test"),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        Assert.assertEquals(new DoubleValue(-0.001D),
                            HugeConverter.convertValue(-0.001D));
        Assert.assertEquals(new StringValue("test"),
                            HugeConverter.convertValue("test"));
        ListValue<IntValue> listValue = new ListValue<>(ValueType.INT);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
        listValue.add(new IntValue(2));
        List<Integer> list = ImmutableList.of(1, 2);
        Assert.assertEquals(listValue, HugeConverter.convertValue(list));

        ListValue<ListValue<LongValue>> nestListValue = new ListValue<>(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
                                    ImmutableList.of(1L, 2L),
                                    ImmutableList.of(3L, 4L));
        Assert.assertEquals(nestListValue,
                            HugeConverter.convertValue(nestList));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertEquals(2147483647, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(3.4028235E38f, value4.floatValue(), 0.0f);
        Assert.assertEquals(3.4028234663852886E38d, value4.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(2147483647, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(3.4028235E38f, value4.floatValue(), 0.0f);
        Assert.assertEquals(3.4028234663852886E38d, value4.doubleValue(), 0.0d);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(3.4028235E38f, value4.floatValue(), 0.0f);
        Assert.assertEquals(3.4028234663852886E38d, value4.doubleValue(), 0.0d);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        IntValue intValue4 = new IntValue(Integer.MAX_VALUE);

        Assert.assertEquals(Integer.hashCode(0),
                            intValue1.hashCode());
        Assert.assertEquals(Integer.hashCode(123456),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.hashCode(0),
                            intValue1.hashCode());
        Assert.assertEquals(Integer.hashCode(123456),
                            intValue2.hashCode());
        Assert.assertEquals(Integer.hashCode(Integer.MIN_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.hashCode(123456),
                            intValue2.hashCode());
        Assert.assertEquals(Integer.hashCode(Integer.MIN_VALUE),
                            intValue3.hashCode());
        Assert.assertEquals(Integer.hashCode(Integer.MAX_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.hashCode(Integer.MIN_VALUE),
                            intValue3.hashCode());
        Assert.assertEquals(Integer.hashCode(Integer.MAX_VALUE),
                            intValue4.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        intValue3.value(Integer.MAX_VALUE);
        Assert.assertEquals(Integer.MAX_VALUE, intValue3.value());
        Assert.assertEquals(intValue3, intValue4);

        IntValue intValue5 = new IntValue(intValue2.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java

        IntValue intValue5 = new IntValue(intValue2.value());
        Assert.assertEquals(intValue2, intValue5);
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
    public void testEquals() {
        IntValue intValue1 = new IntValue();
        Assert.assertTrue(intValue1.equals(intValue1));
        Assert.assertTrue(intValue1.equals(new IntValue(0)));
        Assert.assertFalse(intValue1.equals(new IntValue(1)));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        IntValue intValue1 = new IntValue();
        Assert.assertTrue(intValue1.equals(intValue1));
        Assert.assertTrue(intValue1.equals(new IntValue(0)));
        Assert.assertFalse(intValue1.equals(new IntValue(1)));
        Assert.assertFalse(intValue1.equals(new FloatValue(1f)));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertTrue(intValue1.equals(intValue1));
        Assert.assertTrue(intValue1.equals(new IntValue(0)));
        Assert.assertFalse(intValue1.equals(new IntValue(1)));
        Assert.assertFalse(intValue1.equals(new FloatValue(1f)));
        Assert.assertFalse(intValue1.equals(null));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertTrue(intValue1.equals(new IntValue(0)));
        Assert.assertFalse(intValue1.equals(new IntValue(1)));
        Assert.assertFalse(intValue1.equals(new FloatValue(1f)));
        Assert.assertFalse(intValue1.equals(null));
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertFalse(intValue1.equals(new IntValue(1)));
        Assert.assertFalse(intValue1.equals(new FloatValue(1f)));
        Assert.assertFalse(intValue1.equals(null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        IntValue intValue2 = new IntValue(Integer.MIN_VALUE);

        Assert.assertEquals(ValueType.INT, intValue1.valueType());
        Assert.assertEquals(ValueType.INT, intValue2.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.INT, intValue1.valueType());
        Assert.assertEquals(ValueType.INT, intValue2.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        IntValue intValue4 = new IntValue(Integer.MAX_VALUE);

        Assert.assertEquals("0", intValue1.toString());
        Assert.assertEquals("123456", intValue2.toString());
        Assert.assertEquals("-2147483648", intValue3.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java

        Assert.assertEquals("0", intValue1.toString());
        Assert.assertEquals("123456", intValue2.toString());
        Assert.assertEquals("-2147483648", intValue3.toString());
        Assert.assertEquals("2147483647", intValue4.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals("0", intValue1.toString());
        Assert.assertEquals("123456", intValue2.toString());
        Assert.assertEquals("-2147483648", intValue3.toString());
        Assert.assertEquals("2147483647", intValue4.toString());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals("123456", intValue2.toString());
        Assert.assertEquals("-2147483648", intValue3.toString());
        Assert.assertEquals("2147483647", intValue4.toString());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        SubIntValue subIntValue = new SubIntValue(13579);
        Assert.assertEquals(13579, subIntValue.value());
        Assert.assertEquals("SubIntValue:13579", subIntValue.toString());
        intValue1.assign(subIntValue);
        Assert.assertEquals(13579, intValue1.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        intValue1.assign(subIntValue);
        Assert.assertEquals(13579, intValue1.value());
        Assert.assertEquals("13579", intValue1.toString());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        IntValue intValue4 = new IntValue(Integer.MAX_VALUE);

        Assert.assertEquals(0, intValue1.intValue());
        Assert.assertEquals(0L, intValue1.longValue());
        Assert.assertEquals(0.0f, intValue1.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0, intValue1.intValue());
        Assert.assertEquals(0L, intValue1.longValue());
        Assert.assertEquals(0.0f, intValue1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, intValue1.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0, intValue1.intValue());
        Assert.assertEquals(0L, intValue1.longValue());
        Assert.assertEquals(0.0f, intValue1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, intValue1.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, intValue1.longValue());
        Assert.assertEquals(0.0f, intValue1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, intValue1.doubleValue(), 0.0d);

        Assert.assertEquals(123456, intValue2.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0.0d, intValue1.doubleValue(), 0.0d);

        Assert.assertEquals(123456, intValue2.intValue());
        Assert.assertEquals(123456L, intValue2.longValue());
        Assert.assertEquals(123456f, intValue2.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java

        Assert.assertEquals(123456, intValue2.intValue());
        Assert.assertEquals(123456L, intValue2.longValue());
        Assert.assertEquals(123456f, intValue2.floatValue(), 0.0f);
        Assert.assertEquals(123456d, intValue2.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(123456, intValue2.intValue());
        Assert.assertEquals(123456L, intValue2.longValue());
        Assert.assertEquals(123456f, intValue2.floatValue(), 0.0f);
        Assert.assertEquals(123456d, intValue2.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(123456L, intValue2.longValue());
        Assert.assertEquals(123456f, intValue2.floatValue(), 0.0f);
        Assert.assertEquals(123456d, intValue2.doubleValue(), 0.0d);

        Assert.assertEquals(-2147483648, intValue3.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(123456d, intValue2.doubleValue(), 0.0d);

        Assert.assertEquals(-2147483648, intValue3.intValue());
        Assert.assertEquals(-2147483648L, intValue3.longValue());
        Assert.assertEquals(-2147483648f, intValue3.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java

        Assert.assertEquals(-2147483648, intValue3.intValue());
        Assert.assertEquals(-2147483648L, intValue3.longValue());
        Assert.assertEquals(-2147483648f, intValue3.floatValue(), 0.0f);
        Assert.assertEquals(-2147483648d, intValue3.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(-2147483648, intValue3.intValue());
        Assert.assertEquals(-2147483648L, intValue3.longValue());
        Assert.assertEquals(-2147483648f, intValue3.floatValue(), 0.0f);
        Assert.assertEquals(-2147483648d, intValue3.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(-2147483648L, intValue3.longValue());
        Assert.assertEquals(-2147483648f, intValue3.floatValue(), 0.0f);
        Assert.assertEquals(-2147483648d, intValue3.doubleValue(), 0.0d);

        Assert.assertEquals(2147483647, intValue4.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(-2147483648d, intValue3.doubleValue(), 0.0d);

        Assert.assertEquals(2147483647, intValue4.intValue());
        Assert.assertEquals(2147483647L, intValue4.longValue());
        Assert.assertEquals(2147483647f, intValue4.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java

        Assert.assertEquals(2147483647, intValue4.intValue());
        Assert.assertEquals(2147483647L, intValue4.longValue());
        Assert.assertEquals(2147483647f, intValue4.floatValue(), 0.0f);
        Assert.assertEquals(2147483647d, intValue4.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(2147483647, intValue4.intValue());
        Assert.assertEquals(2147483647L, intValue4.longValue());
        Assert.assertEquals(2147483647f, intValue4.floatValue(), 0.0f);
        Assert.assertEquals(2147483647d, intValue4.doubleValue(), 0.0d);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals(2147483647L, intValue4.longValue());
        Assert.assertEquals(2147483647f, intValue4.floatValue(), 0.0f);
        Assert.assertEquals(2147483647d, intValue4.doubleValue(), 0.0d);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        IntValue intValue4 = new IntValue(Integer.MAX_VALUE);

        Assert.assertEquals("0", intValue1.string());
        Assert.assertEquals("123456", intValue2.string());
        Assert.assertEquals("-2147483648", intValue3.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java

        Assert.assertEquals("0", intValue1.string());
        Assert.assertEquals("123456", intValue2.string());
        Assert.assertEquals("-2147483648", intValue3.string());
        Assert.assertEquals("2147483647", intValue4.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals("0", intValue1.string());
        Assert.assertEquals("123456", intValue2.string());
        Assert.assertEquals("-2147483648", intValue3.string());
        Assert.assertEquals("2147483647", intValue4.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals("123456", intValue2.string());
        Assert.assertEquals("-2147483648", intValue3.string());
        Assert.assertEquals("2147483647", intValue4.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        IntValue intValue2 = new IntValue(123);
        IntValue intValue3 = new IntValue(321);
        Assert.assertEquals(0, intValue1.compareTo(intValue2));
        Assert.assertLt(0, intValue1.compareTo(intValue3));
        Assert.assertGt(0, intValue3.compareTo(intValue1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java
        InputSplit split1 = new InputSplit("0", "100");
        InputSplit split2 = new InputSplit("0", "100");
        Assert.assertEquals(split1, split1);
        Assert.assertEquals(split1, split2);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java
        InputSplit split2 = new InputSplit("0", "100");
        Assert.assertEquals(split1, split1);
        Assert.assertEquals(split1, split2);

        Assert.assertNotEquals(split1, null);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java
        Assert.assertEquals(split1, split2);

        Assert.assertNotEquals(split1, null);
        Assert.assertNotEquals(split1, new Object());

```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java

        Assert.assertNotEquals(split1, null);
        Assert.assertNotEquals(split1, new Object());

        Assert.assertEquals(InputSplit.END_SPLIT, InputSplit.END_SPLIT);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java
        Assert.assertNotEquals(split1, new Object());

        Assert.assertEquals(InputSplit.END_SPLIT, InputSplit.END_SPLIT);
        Assert.assertNotEquals(InputSplit.END_SPLIT, split1);
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java

        Assert.assertEquals(InputSplit.END_SPLIT, InputSplit.END_SPLIT);
        Assert.assertNotEquals(InputSplit.END_SPLIT, split1);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java
    public void testHashCode() {
        InputSplit split = new InputSplit("0", "100");
        Assert.assertEquals(51074, split.hashCode());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java
    public void testConstructor() {
        InputSplit split = new InputSplit("0", "100");
        Assert.assertEquals("0", split.start());
        Assert.assertEquals("100", split.end());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitTest.java`
#### Snippet
```java
        InputSplit split = new InputSplit("0", "100");
        Assert.assertEquals("0", split.start());
        Assert.assertEquals("100", split.end());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
            count--;
        }
        Assert.assertEquals(0, count);

        count = masterInputHandler.createEdgeInputSplits();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
            count--;
        }
        Assert.assertEquals(0, count);
    }
}
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
        while (!(split = masterInputHandler.nextVertexInputSplit()).equals(
               InputSplit.END_SPLIT)) {
            Assert.assertNotNull(split.start());
            Assert.assertNotNull(split.end());
            count--;
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
               InputSplit.END_SPLIT)) {
            Assert.assertNotNull(split.start());
            Assert.assertNotNull(split.end());
            count--;
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
            count--;
        }
        Assert.assertEquals(InputSplit.END_SPLIT, split);
        Assert.assertEquals(0, count);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(InputSplit.END_SPLIT, split);
        Assert.assertEquals(0, count);

        count = masterInputHandler.createEdgeInputSplits();
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
        while (!(split = masterInputHandler.nextEdgeInputSplit()).equals(
               InputSplit.END_SPLIT)) {
            Assert.assertNotNull(split.start());
            Assert.assertNotNull(split.end());
            count--;
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
               InputSplit.END_SPLIT)) {
            Assert.assertNotNull(split.start());
            Assert.assertNotNull(split.end());
            count--;
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
            count--;
        }
        Assert.assertEquals(InputSplit.END_SPLIT, split);
        Assert.assertEquals(0, count);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
        }
        Assert.assertEquals(InputSplit.END_SPLIT, split);
        Assert.assertEquals(0, count);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value5 = new DoubleValue(-Double.MAX_VALUE);

        Assert.assertEquals("0.0", value1.toString());
        Assert.assertEquals("1234.56", value2.toString());
        Assert.assertEquals("4.9E-324", value3.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        Assert.assertEquals("0.0", value1.toString());
        Assert.assertEquals("1234.56", value2.toString());
        Assert.assertEquals("4.9E-324", value3.toString());
        Assert.assertEquals("1.7976931348623157E308", value4.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals("0.0", value1.toString());
        Assert.assertEquals("1234.56", value2.toString());
        Assert.assertEquals("4.9E-324", value3.toString());
        Assert.assertEquals("1.7976931348623157E308", value4.toString());
        Assert.assertEquals("-1.7976931348623157E308", value5.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals("1234.56", value2.toString());
        Assert.assertEquals("4.9E-324", value3.toString());
        Assert.assertEquals("1.7976931348623157E308", value4.toString());
        Assert.assertEquals("-1.7976931348623157E308", value5.toString());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals("4.9E-324", value3.toString());
        Assert.assertEquals("1.7976931348623157E308", value4.toString());
        Assert.assertEquals("-1.7976931348623157E308", value5.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
    public void testEquals() {
        DoubleValue value1 = new DoubleValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new DoubleValue(0d)));
        Assert.assertFalse(value1.equals(new DoubleValue(1)));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value1 = new DoubleValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new DoubleValue(0d)));
        Assert.assertFalse(value1.equals(new DoubleValue(1)));
        Assert.assertFalse(value1.equals(new DoubleValue(1.0d)));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new DoubleValue(0d)));
        Assert.assertFalse(value1.equals(new DoubleValue(1)));
        Assert.assertFalse(value1.equals(new DoubleValue(1.0d)));
        Assert.assertFalse(value1.equals(null));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        LongValue value4 = new LongValue(Long.MAX_VALUE);

        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

        Assert.assertEquals(123456, value2.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

        Assert.assertEquals(123456, value2.intValue());
        Assert.assertEquals(123456L, value2.longValue());
        Assert.assertEquals(123456f, value2.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java

        Assert.assertEquals(123456, value2.intValue());
        Assert.assertEquals(123456L, value2.longValue());
        Assert.assertEquals(123456f, value2.floatValue(), 0.0f);
        Assert.assertEquals(123456d, value2.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(123456, value2.intValue());
        Assert.assertEquals(123456L, value2.longValue());
        Assert.assertEquals(123456f, value2.floatValue(), 0.0f);
        Assert.assertEquals(123456d, value2.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(123456L, value2.longValue());
        Assert.assertEquals(123456f, value2.floatValue(), 0.0f);
        Assert.assertEquals(123456d, value2.doubleValue(), 0.0d);

        Assert.assertEquals(0, value3.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(123456d, value2.doubleValue(), 0.0d);

        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(-9223372036854775808L, value3.longValue());
        Assert.assertEquals(-9223372036854775808f, value3.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(-9223372036854775808L, value3.longValue());
        Assert.assertEquals(-9223372036854775808f, value3.floatValue(), 0.0f);
        Assert.assertEquals(-9223372036854775808d, value3.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(-9223372036854775808L, value3.longValue());
        Assert.assertEquals(-9223372036854775808f, value3.floatValue(), 0.0f);
        Assert.assertEquals(-9223372036854775808d, value3.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(-9223372036854775808L, value3.longValue());
        Assert.assertEquals(-9223372036854775808f, value3.floatValue(), 0.0f);
        Assert.assertEquals(-9223372036854775808d, value3.doubleValue(), 0.0d);

        Assert.assertEquals(-1, value4.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(-9223372036854775808d, value3.doubleValue(), 0.0d);

        Assert.assertEquals(-1, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(9223372036854775807f, value4.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java

        Assert.assertEquals(-1, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(9223372036854775807f, value4.floatValue(), 0.0f);
        Assert.assertEquals(9223372036854775807d, value4.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(-1, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(9223372036854775807f, value4.floatValue(), 0.0f);
        Assert.assertEquals(9223372036854775807d, value4.doubleValue(), 0.0d);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(9223372036854775807f, value4.floatValue(), 0.0f);
        Assert.assertEquals(9223372036854775807d, value4.doubleValue(), 0.0d);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        LongValue value4 = new LongValue(Long.MAX_VALUE);

        Assert.assertEquals(Long.hashCode(0L),
                            value1.hashCode());
        Assert.assertEquals(Long.hashCode(123456L),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Long.hashCode(0L),
                            value1.hashCode());
        Assert.assertEquals(Long.hashCode(123456L),
                            value2.hashCode());
        Assert.assertEquals(Long.hashCode(Long.MIN_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Long.hashCode(123456L),
                            value2.hashCode());
        Assert.assertEquals(Long.hashCode(Long.MIN_VALUE),
                            value3.hashCode());
        Assert.assertEquals(Long.hashCode(Long.MAX_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Long.hashCode(Long.MIN_VALUE),
                            value3.hashCode());
        Assert.assertEquals(Long.hashCode(Long.MAX_VALUE),
                            value4.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
    public void testEquals() {
        LongValue value1 = new LongValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new LongValue(0L)));
        Assert.assertFalse(value1.equals(new LongValue(1L)));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        LongValue value1 = new LongValue();
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new LongValue(0L)));
        Assert.assertFalse(value1.equals(new LongValue(1L)));
        Assert.assertFalse(value1.equals(new FloatValue(1f)));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value1.equals(new LongValue(0L)));
        Assert.assertFalse(value1.equals(new LongValue(1L)));
        Assert.assertFalse(value1.equals(new FloatValue(1f)));
        Assert.assertFalse(value1.equals(null));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(new LongValue(0L)));
        Assert.assertFalse(value1.equals(new LongValue(1L)));
        Assert.assertFalse(value1.equals(new FloatValue(1f)));
        Assert.assertFalse(value1.equals(null));
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value1.equals(new LongValue(1L)));
        Assert.assertFalse(value1.equals(new FloatValue(1f)));
        Assert.assertFalse(value1.equals(null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        LongValue value2 = new LongValue(Long.MIN_VALUE);

        Assert.assertEquals(ValueType.LONG, value1.valueType());
        Assert.assertEquals(ValueType.LONG, value2.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.LONG, value1.valueType());
        Assert.assertEquals(ValueType.LONG, value2.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        LongValue value4 = new LongValue(Long.MAX_VALUE);

        Assert.assertEquals("0", value1.string());
        Assert.assertEquals("123456", value2.string());
        Assert.assertEquals("-9223372036854775808", value3.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java

        Assert.assertEquals("0", value1.string());
        Assert.assertEquals("123456", value2.string());
        Assert.assertEquals("-9223372036854775808", value3.string());
        Assert.assertEquals("9223372036854775807", value4.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals("0", value1.string());
        Assert.assertEquals("123456", value2.string());
        Assert.assertEquals("-9223372036854775808", value3.string());
        Assert.assertEquals("9223372036854775807", value4.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals("123456", value2.string());
        Assert.assertEquals("-9223372036854775808", value3.string());
        Assert.assertEquals("9223372036854775807", value4.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        value3.value(Long.MAX_VALUE);
        Assert.assertEquals(Long.MAX_VALUE, value3.value());
        Assert.assertEquals(value3, value4);

        LongValue value5 = new LongValue(value2.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java

        LongValue value5 = new LongValue(value2.value());
        Assert.assertEquals(value2, value5);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        LongValue value4 = new LongValue(Long.MAX_VALUE);

        Assert.assertEquals("0", value1.toString());
        Assert.assertEquals("123456", value2.toString());
        Assert.assertEquals("-9223372036854775808", value3.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java

        Assert.assertEquals("0", value1.toString());
        Assert.assertEquals("123456", value2.toString());
        Assert.assertEquals("-9223372036854775808", value3.toString());
        Assert.assertEquals("9223372036854775807", value4.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals("0", value1.toString());
        Assert.assertEquals("123456", value2.toString());
        Assert.assertEquals("-9223372036854775808", value3.toString());
        Assert.assertEquals("9223372036854775807", value4.toString());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertEquals("123456", value2.toString());
        Assert.assertEquals("-9223372036854775808", value3.toString());
        Assert.assertEquals("9223372036854775807", value4.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        LongValue value2 = new LongValue(123L);
        LongValue value3 = new LongValue(321L);
        Assert.assertEquals(0, value1.compareTo(value2));
        Assert.assertLt(0, value1.compareTo(value3));
        Assert.assertGt(0, value3.compareTo(value1));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(new DoubleValue(0d)));
        Assert.assertFalse(value1.equals(new DoubleValue(1)));
        Assert.assertFalse(value1.equals(new DoubleValue(1.0d)));
        Assert.assertFalse(value1.equals(null));
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value1.equals(new DoubleValue(1)));
        Assert.assertFalse(value1.equals(new DoubleValue(1.0d)));
        Assert.assertFalse(value1.equals(null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value4 = new DoubleValue(Double.MAX_VALUE);

        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0, value1.intValue());
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, value1.longValue());
        Assert.assertEquals(0.0f, value1.floatValue(), 0.0f);
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

        Assert.assertEquals(1234, value2.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0.0d, value1.doubleValue(), 0.0d);

        Assert.assertEquals(1234, value2.intValue());
        Assert.assertEquals(1234L, value2.longValue());
        Assert.assertEquals(1234.56f, value2.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        Assert.assertEquals(1234, value2.intValue());
        Assert.assertEquals(1234L, value2.longValue());
        Assert.assertEquals(1234.56f, value2.floatValue(), 0.0f);
        Assert.assertEquals(1234.56d, value2.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234, value2.intValue());
        Assert.assertEquals(1234L, value2.longValue());
        Assert.assertEquals(1234.56f, value2.floatValue(), 0.0f);
        Assert.assertEquals(1234.56d, value2.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234L, value2.longValue());
        Assert.assertEquals(1234.56f, value2.floatValue(), 0.0f);
        Assert.assertEquals(1234.56d, value2.doubleValue(), 0.0d);

        Assert.assertEquals(0, value3.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234.56d, value2.doubleValue(), 0.0d);

        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(0L, value3.longValue());
        Assert.assertEquals(0.0f, value3.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(0L, value3.longValue());
        Assert.assertEquals(0.0f, value3.floatValue(), 0.0f);
        Assert.assertEquals(4.9E-324d, value3.doubleValue(), 0.0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0, value3.intValue());
        Assert.assertEquals(0L, value3.longValue());
        Assert.assertEquals(0.0f, value3.floatValue(), 0.0f);
        Assert.assertEquals(4.9E-324d, value3.doubleValue(), 0.0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0L, value3.longValue());
        Assert.assertEquals(0.0f, value3.floatValue(), 0.0f);
        Assert.assertEquals(4.9E-324d, value3.doubleValue(), 0.0d);

        Assert.assertEquals(2147483647, value4.intValue());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(4.9E-324d, value3.doubleValue(), 0.0d);

        Assert.assertEquals(2147483647, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(Float.POSITIVE_INFINITY, value4.floatValue(), 0.0f);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        Assert.assertEquals(2147483647, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(Float.POSITIVE_INFINITY, value4.floatValue(), 0.0f);
        Assert.assertEquals(1.7976931348623157E308d, value4.doubleValue(), 0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(2147483647, value4.intValue());
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(Float.POSITIVE_INFINITY, value4.floatValue(), 0.0f);
        Assert.assertEquals(1.7976931348623157E308d, value4.doubleValue(), 0d);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(9223372036854775807L, value4.longValue());
        Assert.assertEquals(Float.POSITIVE_INFINITY, value4.floatValue(), 0.0f);
        Assert.assertEquals(1.7976931348623157E308d, value4.doubleValue(), 0d);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value4 = new DoubleValue(Double.MAX_VALUE);

        Assert.assertEquals(0d, value1.value(), 0d);
        value1.assign(value2);
        Assert.assertEquals(1234.56d, value1.value(), 0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0d, value1.value(), 0d);
        value1.assign(value2);
        Assert.assertEquals(1234.56d, value1.value(), 0d);
        Assert.assertEquals(1234.56d, value2.value(), 0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        value1.assign(value2);
        Assert.assertEquals(1234.56d, value1.value(), 0d);
        Assert.assertEquals(1234.56d, value2.value(), 0d);

        value2.assign(value3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        value2.assign(value3);
        Assert.assertEquals(1234.56d, value1.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value2.value(), 0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        value2.assign(value3);
        Assert.assertEquals(1234.56d, value1.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value2.value(), 0d);

        value2.assign(value4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        value2.assign(value4);
        Assert.assertEquals(1234.56d, value1.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value2.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value3.value(), 0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        value2.assign(value4);
        Assert.assertEquals(1234.56d, value1.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value2.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value3.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value4.value(), 0d);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234.56d, value1.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value2.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value3.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value4.value(), 0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Double.MAX_VALUE, value2.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value3.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value4.value(), 0d);

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int partition5 = partitioner.partitionId(vertexId5);
        int partition6 = partitioner.partitionId(vertexId6);
        Assert.assertTrue(partition1 < 3);
        Assert.assertTrue(partition2 < 3);
        Assert.assertTrue(partition3 < 3);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int partition6 = partitioner.partitionId(vertexId6);
        Assert.assertTrue(partition1 < 3);
        Assert.assertTrue(partition2 < 3);
        Assert.assertTrue(partition3 < 3);
        Assert.assertTrue(partition4 < 3);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertTrue(partition1 < 3);
        Assert.assertTrue(partition2 < 3);
        Assert.assertTrue(partition3 < 3);
        Assert.assertTrue(partition4 < 3);
        Assert.assertTrue(partition5 < 3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value4 = new DoubleValue(Double.MAX_VALUE);

        Assert.assertEquals(0d, value1.value(), 0d);
        Assert.assertEquals(1234.56d, value2.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value3.value(), 0d);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertTrue(partition2 < 3);
        Assert.assertTrue(partition3 < 3);
        Assert.assertTrue(partition4 < 3);
        Assert.assertTrue(partition5 < 3);
        Assert.assertTrue(partition6 < 3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        Assert.assertEquals(0d, value1.value(), 0d);
        Assert.assertEquals(1234.56d, value2.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value3.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value4.value(), 0d);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertTrue(partition3 < 3);
        Assert.assertTrue(partition4 < 3);
        Assert.assertTrue(partition5 < 3);
        Assert.assertTrue(partition6 < 3);

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertTrue(partition4 < 3);
        Assert.assertTrue(partition5 < 3);
        Assert.assertTrue(partition6 < 3);

        int workerId1 = partitioner.workerId(partition1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(0d, value1.value(), 0d);
        Assert.assertEquals(1234.56d, value2.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value3.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value4.value(), 0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(1234.56d, value2.value(), 0d);
        Assert.assertEquals(Double.MIN_VALUE, value3.value(), 0d);
        Assert.assertEquals(Double.MAX_VALUE, value4.value(), 0d);

        value3.value(Double.MAX_VALUE);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        value3.value(Double.MAX_VALUE);
        Assert.assertEquals(Double.MAX_VALUE, value3.value(), 0d);
        Assert.assertEquals(value3, value4);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        value3.value(Double.MAX_VALUE);
        Assert.assertEquals(Double.MAX_VALUE, value3.value(), 0d);
        Assert.assertEquals(value3, value4);

        DoubleValue value5 = new DoubleValue(value2.value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        DoubleValue value5 = new DoubleValue(value2.value());
        Assert.assertEquals(value2, value5);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java
        FileInputSplit split2 = new FileInputSplit(ElemType.VERTEX, inputStruct,
                                                   "/tmp/test");
        Assert.assertEquals(split1.hashCode(), split2.hashCode());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java
        FileInputSplit split2 = new FileInputSplit(ElemType.VERTEX, inputStruct,
                                                   "/tmp/test");
        Assert.assertEquals(split1, split1);
        Assert.assertEquals(split1, split2);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java
                                                   "/tmp/test");
        Assert.assertEquals(split1, split1);
        Assert.assertEquals(split1, split2);

        Assert.assertNotEquals(split1, null);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java
        Assert.assertEquals(split1, split2);

        Assert.assertNotEquals(split1, null);
        Assert.assertNotEquals(split1, new Object());

```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java

        Assert.assertNotEquals(split1, null);
        Assert.assertNotEquals(split1, new Object());

        Assert.assertEquals(InputSplit.END_SPLIT, InputSplit.END_SPLIT);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java
        Assert.assertNotEquals(split1, new Object());

        Assert.assertEquals(InputSplit.END_SPLIT, InputSplit.END_SPLIT);
        Assert.assertNotEquals(InputSplit.END_SPLIT, split1);
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java

        Assert.assertEquals(InputSplit.END_SPLIT, InputSplit.END_SPLIT);
        Assert.assertNotEquals(InputSplit.END_SPLIT, split1);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java
        FileInputSplit split = new FileInputSplit(ElemType.VERTEX, inputStruct,
                                                  "/tmp/test");
        Assert.assertEquals("/tmp/test", split.path());
        Assert.assertEquals(inputStruct, split.struct());
        Assert.assertSame(ElemType.VERTEX, split.type());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java
                                                  "/tmp/test");
        Assert.assertEquals("/tmp/test", split.path());
        Assert.assertEquals(inputStruct, split.struct());
        Assert.assertSame(ElemType.VERTEX, split.type());
    }
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/FileInputSplitTest.java`
#### Snippet
```java
        Assert.assertEquals("/tmp/test", split.path());
        Assert.assertEquals(inputStruct, split.struct());
        Assert.assertSame(ElemType.VERTEX, split.type());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int workerId5 = partitioner.workerId(partition5);
        int workerId6 = partitioner.workerId(partition6);
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int workerId6 = partitioner.workerId(partition6);
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int partition5 = partitioner.partitionId(vertexId5);
        int partition6 = partitioner.partitionId(vertexId6);
        Assert.assertTrue(partition1 < 2);
        Assert.assertTrue(partition2 < 2);
        Assert.assertTrue(partition3 < 2);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int partition6 = partitioner.partitionId(vertexId6);
        Assert.assertTrue(partition1 < 2);
        Assert.assertTrue(partition2 < 2);
        Assert.assertTrue(partition3 < 2);
        Assert.assertTrue(partition4 < 2);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertTrue(partition1 < 2);
        Assert.assertTrue(partition2 < 2);
        Assert.assertTrue(partition3 < 2);
        Assert.assertTrue(partition4 < 2);
        Assert.assertTrue(partition5 < 2);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertTrue(partition2 < 2);
        Assert.assertTrue(partition3 < 2);
        Assert.assertTrue(partition4 < 2);
        Assert.assertTrue(partition5 < 2);
        Assert.assertTrue(partition6 < 2);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertTrue(partition3 < 2);
        Assert.assertTrue(partition4 < 2);
        Assert.assertTrue(partition5 < 2);
        Assert.assertTrue(partition6 < 2);

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertTrue(partition4 < 2);
        Assert.assertTrue(partition5 < 2);
        Assert.assertTrue(partition6 < 2);

        int workerId1 = partitioner.workerId(partition1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int workerId5 = partitioner.workerId(partition5);
        int workerId6 = partitioner.workerId(partition6);
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int workerId6 = partitioner.workerId(partition6);
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int partition5 = partitioner.partitionId(vertexId5);
        int partition6 = partitioner.partitionId(vertexId6);
        Assert.assertEquals(0, partition1);
        Assert.assertEquals(0, partition2);
        Assert.assertEquals(0, partition3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int partition6 = partitioner.partitionId(vertexId6);
        Assert.assertEquals(0, partition1);
        Assert.assertEquals(0, partition2);
        Assert.assertEquals(0, partition3);
        Assert.assertEquals(0, partition4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(0, partition1);
        Assert.assertEquals(0, partition2);
        Assert.assertEquals(0, partition3);
        Assert.assertEquals(0, partition4);
        Assert.assertEquals(0, partition5);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(0, partition2);
        Assert.assertEquals(0, partition3);
        Assert.assertEquals(0, partition4);
        Assert.assertEquals(0, partition5);
        Assert.assertEquals(0, partition6);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(0, partition3);
        Assert.assertEquals(0, partition4);
        Assert.assertEquals(0, partition5);
        Assert.assertEquals(0, partition6);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(0, partition4);
        Assert.assertEquals(0, partition5);
        Assert.assertEquals(0, partition6);

        int workerId1 = partitioner.workerId(partition1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int workerId5 = partitioner.workerId(partition5);
        int workerId6 = partitioner.workerId(partition6);
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        int workerId6 = partitioner.workerId(partition6);
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId1);
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId2);
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId3);
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        Assert.assertEquals(1, workerId4);
        Assert.assertEquals(1, workerId5);
        Assert.assertEquals(1, workerId6);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        LOG.info("Partition distribution: {}", Arrays.toString(partitionStat));
        LOG.info("Worker distribution: {}", Arrays.toString(workerStat));
        Assert.assertEquals(hashTimes, Arrays.stream(partitionStat).sum());
        Assert.assertEquals(hashTimes, Arrays.stream(workerStat).sum());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
        LOG.info("Worker distribution: {}", Arrays.toString(workerStat));
        Assert.assertEquals(hashTimes, Arrays.stream(partitionStat).sum());
        Assert.assertEquals(hashTimes, Arrays.stream(workerStat).sum());
    }
}
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
            );
        }, e -> {
            Assert.assertTrue(e.getMessage().contains(
                              "The partitions count must be >= workers count"));
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value2 = new DoubleValue(123d);
        DoubleValue value3 = new DoubleValue(321d);
        Assert.assertEquals(0, value1.compareTo(value2));
        Assert.assertLt(0, value1.compareTo(value3));
        Assert.assertGt(0, value3.compareTo(value1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        DoubleValue copy = value1.copy();
        Assert.assertEquals(0d, value1.value(), 0d);
        Assert.assertEquals(0d, copy.value(), 0d);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue copy = value1.copy();
        Assert.assertEquals(0d, value1.value(), 0d);
        Assert.assertEquals(0d, copy.value(), 0d);

        copy.assign(value2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        copy.assign(value2);
        Assert.assertEquals(1234.56d, copy.value(), 0d);
        Assert.assertEquals(0d, value1.value(), 0d);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        copy.assign(value2);
        Assert.assertEquals(1234.56d, copy.value(), 0d);
        Assert.assertEquals(0d, value1.value(), 0d);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value2 = new DoubleValue(Double.MIN_VALUE);

        Assert.assertEquals(ValueType.DOUBLE, value1.valueType());
        Assert.assertEquals(ValueType.DOUBLE, value2.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.DOUBLE, value1.valueType());
        Assert.assertEquals(ValueType.DOUBLE, value2.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value4 = new DoubleValue(Double.MAX_VALUE);

        Assert.assertEquals("0.0", value1.string());
        Assert.assertEquals("1234.56", value2.string());
        Assert.assertEquals("4.9E-324", value3.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java

        Assert.assertEquals("0.0", value1.string());
        Assert.assertEquals("1234.56", value2.string());
        Assert.assertEquals("4.9E-324", value3.string());
        Assert.assertEquals("1.7976931348623157E308", value4.string());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals("0.0", value1.string());
        Assert.assertEquals("1234.56", value2.string());
        Assert.assertEquals("4.9E-324", value3.string());
        Assert.assertEquals("1.7976931348623157E308", value4.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals("1234.56", value2.string());
        Assert.assertEquals("4.9E-324", value3.string());
        Assert.assertEquals("1.7976931348623157E308", value4.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        DoubleValue value4 = new DoubleValue(Double.MAX_VALUE);

        Assert.assertEquals(Double.hashCode(0d),
                            value1.hashCode());
        Assert.assertEquals(Double.hashCode(1234.56d),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Double.hashCode(0d),
                            value1.hashCode());
        Assert.assertEquals(Double.hashCode(1234.56d),
                            value2.hashCode());
        Assert.assertEquals(Double.hashCode(Double.MIN_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Double.hashCode(1234.56d),
                            value2.hashCode());
        Assert.assertEquals(Double.hashCode(Double.MIN_VALUE),
                            value3.hashCode());
        Assert.assertEquals(Double.hashCode(Double.MAX_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertEquals(Double.hashCode(Double.MIN_VALUE),
                            value3.hashCode());
        Assert.assertEquals(Double.hashCode(Double.MAX_VALUE),
                            value4.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
            // Open the file and determine the footer is as expected
            HgkvDir dir = HgkvDirImpl.open(path);
            Assert.assertEquals(HgkvFileImpl.MAGIC, dir.magic());
            String version = HgkvDirImpl.MAJOR_VERSION + "." +
                             HgkvDirImpl.MINOR_VERSION;
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
            String version = HgkvDirImpl.MAJOR_VERSION + "." +
                             HgkvDirImpl.MINOR_VERSION;
            Assert.assertEquals(version, dir.version());
            Assert.assertEquals(5, dir.numEntries());
            int maxKey = StoreTestUtil.byteArrayToInt(dir.max());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
                             HgkvDirImpl.MINOR_VERSION;
            Assert.assertEquals(version, dir.version());
            Assert.assertEquals(5, dir.numEntries());
            int maxKey = StoreTestUtil.byteArrayToInt(dir.max());
            Assert.assertEquals(6, maxKey);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
            Assert.assertEquals(5, dir.numEntries());
            int maxKey = StoreTestUtil.byteArrayToInt(dir.max());
            Assert.assertEquals(6, maxKey);
            int minKey = StoreTestUtil.byteArrayToInt(dir.min());
            Assert.assertEquals(2, minKey);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
            Assert.assertEquals(6, maxKey);
            int minKey = StoreTestUtil.byteArrayToInt(dir.min());
            Assert.assertEquals(2, minKey);
        } finally {
            FileUtils.deleteQuietly(new File(path));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
                KvEntry entry = iterator.next();
                int key = StoreTestUtil.byteArrayToInt(entry.key().bytes());
                Assert.assertEquals(data.get(i).intValue(), key);
                i += 2;
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/BitFileTest.java`
#### Snippet
```java
            try (BitsFileReader reader = new BitsFileReaderImpl(CONFIG, dir)) {
                for (int i = 0; i < 1000000; i++) {
                    Assert.assertEquals(i % 2 == 0, reader.readBoolean());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/BitFileTest.java`
#### Snippet
```java
            try (BitsFileReader reader = new BitsFileReaderImpl(CONFIG, dir)) {
                for (int i = 0; i < data.size(); i++) {
                    Assert.assertEquals(data.get(i), reader.readBoolean());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java

        File dir1 = new File(dataFileManager.nextDirectory("vertex"));
        Assert.assertEquals("vertex", dir1.getName());

        File dir2 = new File(dataFileManager.nextDirectory("message", "1"));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java

        File dir2 = new File(dataFileManager.nextDirectory("message", "1"));
        Assert.assertEquals("1", dir2.getName());
        Assert.assertEquals("message", dir2.getParentFile().getName());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        File dir2 = new File(dataFileManager.nextDirectory("message", "1"));
        Assert.assertEquals("1", dir2.getName());
        Assert.assertEquals("message", dir2.getParentFile().getName());

        dataFileManager.close(config);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        );
        FileManager dataFileManager = new FileManager();
        Assert.assertEquals(FileManager.NAME, dataFileManager.name());
        Assert.assertThrows(ComputerException.class, () -> {
            dataFileManager.init(config);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        String dir4 = dataFileManager.nextDirectory();
        String dir5 = dataFileManager.nextDirectory();
        Assert.assertEquals(dir1, dir3);
        Assert.assertEquals(dir3, dir5);
        Assert.assertEquals(dir2, dir4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        String dir5 = dataFileManager.nextDirectory();
        Assert.assertEquals(dir1, dir3);
        Assert.assertEquals(dir3, dir5);
        Assert.assertEquals(dir2, dir4);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        Assert.assertEquals(dir1, dir3);
        Assert.assertEquals(dir3, dir5);
        Assert.assertEquals(dir2, dir4);

        dataFileManager.close(config);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
        HgkvFile hgkvFile = HgkvFileImpl.open(file.getPath());
        // Assert magic
        Assert.assertEquals(HgkvFileImpl.MAGIC, hgkvFile.magic());
        // Assert version
        String version = HgkvFileImpl.MAJOR_VERSION + "." +
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
        String version = HgkvFileImpl.MAJOR_VERSION + "." +
                         HgkvFileImpl.MINOR_VERSION;
        Assert.assertEquals(version, hgkvFile.version());
        // Assert numEntries
        Assert.assertEquals(5, hgkvFile.numEntries());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
        Assert.assertEquals(version, hgkvFile.version());
        // Assert numEntries
        Assert.assertEquals(5, hgkvFile.numEntries());
        // Assert max key
        int maxKey = StoreTestUtil.byteArrayToInt(hgkvFile.max());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
        // Assert max key
        int maxKey = StoreTestUtil.byteArrayToInt(hgkvFile.max());
        Assert.assertEquals(6, maxKey);
        // Assert min key
        int minKey = StoreTestUtil.byteArrayToInt(hgkvFile.min());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
        // Assert min key
        int minKey = StoreTestUtil.byteArrayToInt(hgkvFile.min());
        Assert.assertEquals(2, minKey);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
                KvEntry next = iterator.next();
                int key = StoreTestUtil.byteArrayToInt(next.key().bytes());
                Assert.assertEquals(data.get(index).intValue(), key);
                index += 2;
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
            throw new ComputerException("computer exception");
        }, e -> {
            Assert.assertEquals("computer exception", e.getMessage());
            Assert.assertNull(e.getCause());
        });
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
        }, e -> {
            Assert.assertEquals("computer exception", e.getMessage());
            Assert.assertNull(e.getCause());
        });

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
                                        new IOException());
        }, e -> {
            Assert.assertEquals("computer exception", e.getMessage());
            Assert.assertEquals(IOException.class, e.getCause().getClass());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
        }, e -> {
            Assert.assertEquals("computer exception", e.getMessage());
            Assert.assertEquals(IOException.class, e.getCause().getClass());
        });

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
            throw new ComputerException("computer exception at step %s", 1);
        }, e -> {
            Assert.assertEquals("computer exception at step 1",
                                e.getMessage());
            Assert.assertNull(e.getCause());
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
            Assert.assertEquals("computer exception at step 1",
                                e.getMessage());
            Assert.assertNull(e.getCause());
        });

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
                                        new IOException(), 1);
        }, e -> {
            Assert.assertEquals("computer exception at step 1",
                                e.getMessage());
            Assert.assertEquals(IOException.class, e.getCause().getClass());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
            Assert.assertEquals("computer exception at step 1",
                                e.getMessage());
            Assert.assertEquals(IOException.class, e.getCause().getClass());
        });

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
                                        new IOException(rootCause));
        }, e -> {
            Assert.assertEquals("computer exception", e.getMessage());
            Assert.assertEquals(IOException.class, e.getCause().getClass());
            Assert.assertEquals(rootCause, ComputerException.rootCause(e));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
        }, e -> {
            Assert.assertEquals("computer exception", e.getMessage());
            Assert.assertEquals(IOException.class, e.getCause().getClass());
            Assert.assertEquals(rootCause, ComputerException.rootCause(e));
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ExceptionTest.java`
#### Snippet
```java
            Assert.assertEquals("computer exception", e.getMessage());
            Assert.assertEquals(IOException.class, e.getCause().getClass());
            Assert.assertEquals(rootCause, ComputerException.rootCause(e));
        });
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value2 = new ListValue<>(ValueType.FLOAT);

        Assert.assertEquals(1, value1.hashCode());
        Assert.assertEquals(1, value2.hashCode());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals(1, value1.hashCode());
        Assert.assertEquals(1, value2.hashCode());

        value1.add(new IntValue(101));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(202f));

        Assert.assertEquals(130117, value1.hashCode());
        Assert.assertEquals(1763771329, value2.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals(130117, value1.hashCode());
        Assert.assertEquals(1763771329, value2.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<IntValue> value3 = new ListValue<>(ValueType.INT);
        ListValue<FloatValue> value4 = new ListValue<>(ValueType.FLOAT);
        Assert.assertEquals(ImmutableList.of(), value3.values());
        Assert.assertEquals(ImmutableList.of(), value4.values());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value4 = new ListValue<>(ValueType.FLOAT);
        Assert.assertEquals(ImmutableList.of(), value3.values());
        Assert.assertEquals(ImmutableList.of(), value4.values());

        Assert.assertNotEquals(value1, value3);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(ImmutableList.of(), value4.values());

        Assert.assertNotEquals(value1, value3);
        value3.assign(value1);
        Assert.assertEquals(value1, value3);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
        String str = "ContainerInfo{\"id\":0,\"hostname\":\"localhost\"," +
                     "\"rpcPort\":8001,\"dataPort\":8002}";
        Assert.assertEquals(str, container.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
    public void testHashCode() {
        ContainerInfo container = new ContainerInfo(0, HOST1, 8001, 8002);
        Assert.assertEquals(Integer.hashCode(0), container.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
    public void testConstructor() {
        ContainerInfo worker = new ContainerInfo(0, HOST1, 8001, 8002);
        Assert.assertEquals(0, worker.id());
        Assert.assertEquals(HOST1, worker.hostname());
        Assert.assertEquals(8001, worker.rpcPort());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
        ContainerInfo worker = new ContainerInfo(0, HOST1, 8001, 8002);
        Assert.assertEquals(0, worker.id());
        Assert.assertEquals(HOST1, worker.hostname());
        Assert.assertEquals(8001, worker.rpcPort());
        Assert.assertEquals(8002, worker.dataPort());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
        Assert.assertEquals(0, worker.id());
        Assert.assertEquals(HOST1, worker.hostname());
        Assert.assertEquals(8001, worker.rpcPort());
        Assert.assertEquals(8002, worker.dataPort());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
        Assert.assertEquals(HOST1, worker.hostname());
        Assert.assertEquals(8001, worker.rpcPort());
        Assert.assertEquals(8002, worker.dataPort());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
        ContainerInfo container2 = new ContainerInfo(0, HOST1, 8001, 8002);
        ContainerInfo container3 = new ContainerInfo(0, HOST2, 8001, 8002);
        Assert.assertEquals(container1, container2);
        Assert.assertNotEquals(container1, container3);
        Assert.assertNotEquals(container1, new Object());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
        ContainerInfo container3 = new ContainerInfo(0, HOST2, 8001, 8002);
        Assert.assertEquals(container1, container2);
        Assert.assertNotEquals(container1, container3);
        Assert.assertNotEquals(container1, new Object());
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfoTest.java`
#### Snippet
```java
        Assert.assertEquals(container1, container2);
        Assert.assertNotEquals(container1, container3);
        Assert.assertNotEquals(container1, new Object());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertNotEquals(value1, value3);
        value3.assign(value1);
        Assert.assertEquals(value1, value3);

        /*
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
         * TODO: should keep value1 unmodifiable and let value3 modifiable?
         */
        Assert.assertEquals(3, value3.size());
        value1.add(new IntValue(104));
        Assert.assertEquals(4, value1.size());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(3, value3.size());
        value1.add(new IntValue(104));
        Assert.assertEquals(4, value1.size());
        Assert.assertEquals(4, value3.size());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value1.add(new IntValue(104));
        Assert.assertEquals(4, value1.size());
        Assert.assertEquals(4, value3.size());

        Assert.assertNotEquals(value2, value4);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(4, value3.size());

        Assert.assertNotEquals(value2, value4);
        value4.assign(value2);
        Assert.assertEquals(value2, value4);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/QueuedMessageTest.java`
#### Snippet
```java
        QueuedMessage message = new QueuedMessage(1, MessageType.VERTEX,
                                                  ByteBuffer.allocate(4));
        Assert.assertEquals(1, message.partitionId());
        Assert.assertEquals(MessageType.VERTEX, message.type());
        Assert.assertEquals(ByteBuffer.allocate(4), message.buffer());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertNotEquals(value2, value4);
        value4.assign(value2);
        Assert.assertEquals(value2, value4);

        Assert.assertThrows(UnsupportedOperationException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value1.add(new IntValue(100));
        value1.add(new IntValue(200));
        Assert.assertTrue(value1.contains(new IntValue(100)));
        Assert.assertTrue(value1.contains(new IntValue(200)));
        Assert.assertFalse(value1.contains(new IntValue(300)));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/QueuedMessageTest.java`
#### Snippet
```java
                                                  ByteBuffer.allocate(4));
        Assert.assertEquals(1, message.partitionId());
        Assert.assertEquals(MessageType.VERTEX, message.type());
        Assert.assertEquals(ByteBuffer.allocate(4), message.buffer());
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value1.add(new IntValue(200));
        Assert.assertTrue(value1.contains(new IntValue(100)));
        Assert.assertTrue(value1.contains(new IntValue(200)));
        Assert.assertFalse(value1.contains(new IntValue(300)));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/QueuedMessageTest.java`
#### Snippet
```java
        Assert.assertEquals(1, message.partitionId());
        Assert.assertEquals(MessageType.VERTEX, message.type());
        Assert.assertEquals(ByteBuffer.allocate(4), message.buffer());
    }
}
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.contains(new IntValue(100)));
        Assert.assertTrue(value1.contains(new IntValue(200)));
        Assert.assertFalse(value1.contains(new IntValue(300)));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value2 = new ListValue<>(ValueType.FLOAT);

        Assert.assertEquals(ValueType.INT, value1.elemType());
        Assert.assertEquals(ValueType.FLOAT, value2.elemType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.INT, value1.elemType());
        Assert.assertEquals(ValueType.FLOAT, value2.elemType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value3.add(new IntValue(100));
        value3.add(new IntValue(200));
        Assert.assertEquals(0, value1.compareTo(value2));
        Assert.assertLt(0, value1.compareTo(value3));
        Assert.assertGt(0, value3.compareTo(value1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value2 = new ListValue<>(ValueType.FLOAT);

        Assert.assertEquals(0, value1.size());
        value1.add(new IntValue(101));
        value1.add(new IntValue(102));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value1.add(new IntValue(102));
        value1.add(new IntValue(103));
        Assert.assertEquals(3, value1.size());

        Assert.assertEquals(0, value2.size());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(3, value1.size());

        Assert.assertEquals(0, value2.size());
        value2.add(new FloatValue(201f));
        value2.add(new FloatValue(202f));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(201f));
        value2.add(new FloatValue(202f));
        Assert.assertEquals(2, value2.size());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value2 = new ListValue<>(ValueType.FLOAT);

        Assert.assertEquals(ValueType.LIST_VALUE, value1.valueType());
        Assert.assertEquals(ValueType.LIST_VALUE, value2.valueType());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ValueType.LIST_VALUE, value1.valueType());
        Assert.assertEquals(ValueType.LIST_VALUE, value2.valueType());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(202f));

        Assert.assertEquals("[101, 102, 103]", value1.string());
        Assert.assertEquals("[201.0, 202.0]", value2.string());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals("[101, 102, 103]", value1.string());
        Assert.assertEquals("[201.0, 202.0]", value2.string());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value2 = new ListValue<>(ValueType.FLOAT);

        Assert.assertEquals(0, value1.size());
        value1.add(new IntValue(101));
        value1.add(new IntValue(102));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value1.add(new IntValue(102));
        value1.add(new IntValue(103));
        Assert.assertEquals(3, value1.size());

        Assert.assertEquals(0, value2.size());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(3, value1.size());

        Assert.assertEquals(0, value2.size());
        value2.add(new FloatValue(201f));
        value2.add(new FloatValue(202f));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(201f));
        value2.add(new FloatValue(202f));
        Assert.assertEquals(2, value2.size());

        Assert.assertEquals(101, value1.get(0).value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(103, value1.get(2).value());

        Assert.assertEquals(201f, value2.get(0).value(), 0f);
        Assert.assertEquals(202f, value2.get(1).value(), 0f);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals(201f, value2.get(0).value(), 0f);
        Assert.assertEquals(202f, value2.get(1).value(), 0f);

        Assert.assertThrows(IndexOutOfBoundsException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value2 = new ListValue<>(ValueType.FLOAT);

        Assert.assertEquals(ImmutableList.of(), value1.values());
        Assert.assertEquals(ImmutableList.of(), value2.values());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ImmutableList.of(), value1.values());
        Assert.assertEquals(ImmutableList.of(), value2.values());

        value1.add(new IntValue(101));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(202f));

        Assert.assertEquals(ImmutableList.of(new IntValue(101),
                                             new IntValue(102),
                                             new IntValue(103)),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
                            value1.values());

        Assert.assertEquals(ImmutableList.of(new FloatValue(201f),
                                             new FloatValue(202f)),
                            value2.values());
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(202f));

        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value2.equals(value2));

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertTrue(value1.equals(value1));
        Assert.assertTrue(value2.equals(value2));

        Assert.assertTrue(value1.equals(value1.copy()));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value2.equals(value2));

        Assert.assertTrue(value1.equals(value1.copy()));
        Assert.assertTrue(value2.equals(value2.copy()));

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertTrue(value1.equals(value1.copy()));
        Assert.assertTrue(value2.equals(value2.copy()));

        Assert.assertFalse(value1.equals(value2));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value2.equals(value2.copy()));

        Assert.assertFalse(value1.equals(value2));
        Assert.assertFalse(value2.equals(value1));

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(value2));
        Assert.assertFalse(value2.equals(value1));

        Assert.assertFalse(value1.equals(new IntValue(1)));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value2.equals(value1));

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value2 = new ListValue<>(ValueType.FLOAT);

        Assert.assertEquals(0, value1.size());
        value1.add(new IntValue(101));
        value1.add(new IntValue(102));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value1.add(new IntValue(102));
        value1.add(new IntValue(103));
        Assert.assertEquals(3, value1.size());

        Assert.assertEquals(0, value2.size());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(3, value1.size());

        Assert.assertEquals(0, value2.size());
        value2.add(new FloatValue(201f));
        value2.add(new FloatValue(202f));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(201f));
        value2.add(new FloatValue(202f));
        Assert.assertEquals(2, value2.size());

        ListValue<Value.Tvalue<?>> value3 = new ListValue<>(ValueType.FLOAT);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        ListValue<Value.Tvalue<?>> value4 = new ListValue<>(ValueType.UNKNOWN);
        Assert.assertEquals(ValueType.UNKNOWN, value4.elemType());
        value4.add(new IntValue(303));
        Assert.assertEquals(ValueType.INT, value4.elemType());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.UNKNOWN, value4.elemType());
        value4.add(new IntValue(303));
        Assert.assertEquals(ValueType.INT, value4.elemType());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        ListValue<IntValue> value2 = value1.copy();
        Assert.assertEquals(value1, value2);

        value2.add(new IntValue(300));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new IntValue(300));

        Assert.assertEquals(3, value2.size());
        Assert.assertEquals(100, value2.get(0).value());
        Assert.assertEquals(200, value2.get(1).value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(300, value2.get(2).value());

        Assert.assertEquals(2, value1.size());
        Assert.assertEquals(100, value1.get(0).value());
        Assert.assertEquals(200, value1.get(1).value());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<FloatValue> value2 = new ListValue<>(ValueType.FLOAT);

        Assert.assertEquals("[]", value1.toString());
        Assert.assertEquals("[]", value2.toString());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals("[]", value1.toString());
        Assert.assertEquals("[]", value2.toString());

        value1.add(new IntValue(101));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(202f));

        Assert.assertEquals("[101, 102, 103]", value1.toString());
        Assert.assertEquals("[201.0, 202.0]", value2.toString());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals("[101, 102, 103]", value1.toString());
        Assert.assertEquals("[201.0, 202.0]", value2.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value2.add(new FloatValue(202f));

        Assert.assertEquals(ImmutableList.of(101, 102, 103), value1.value());
        Assert.assertEquals(ImmutableList.of(201.0f, 202.0f), value2.value());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertEquals(ImmutableList.of(101, 102, 103), value1.value());
        Assert.assertEquals(ImmutableList.of(201.0f, 202.0f), value2.value());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java
        // Trigger notifier called
        queue.put(message1);
        Assert.assertEquals(1, notifyCounter.get());

        QueuedMessage message2 = new QueuedMessage(2, MessageType.EDGE,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java
                                                   ByteBuffer.allocate(4));
        queue.put(message2);
        Assert.assertEquals(2, notifyCounter.get());

        BlockingQueue<?> blockQueue = Whitebox.getInternalState(queue, "queue");
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java

        BlockingQueue<?> blockQueue = Whitebox.getInternalState(queue, "queue");
        Assert.assertEquals(2, blockQueue.size());

        Assert.assertEquals(message1.partitionId(), queue.peek().partitionId());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java
        Assert.assertEquals(2, blockQueue.size());

        Assert.assertEquals(message1.partitionId(), queue.peek().partitionId());
        Assert.assertEquals(2, blockQueue.size());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java

        Assert.assertEquals(message1.partitionId(), queue.peek().partitionId());
        Assert.assertEquals(2, blockQueue.size());

        Assert.assertEquals(message1.partitionId(), queue.take().partitionId());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java
        Assert.assertEquals(2, blockQueue.size());

        Assert.assertEquals(message1.partitionId(), queue.take().partitionId());
        Assert.assertEquals(1, blockQueue.size());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java

        Assert.assertEquals(message1.partitionId(), queue.take().partitionId());
        Assert.assertEquals(1, blockQueue.size());

        Assert.assertEquals(message2.partitionId(), queue.take().partitionId());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java
        Assert.assertEquals(1, blockQueue.size());

        Assert.assertEquals(message2.partitionId(), queue.take().partitionId());
        Assert.assertEquals(0, blockQueue.size());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java

        Assert.assertEquals(message2.partitionId(), queue.take().partitionId());
        Assert.assertEquals(0, blockQueue.size());

        // Trigger notifier called
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageQueueTest.java`
#### Snippet
```java
        // Trigger notifier called
        queue.put(message1);
        Assert.assertEquals(3, notifyCounter.get());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
        entry = EntriesUtil.kvEntryWithFirstSubKv(entry);
        // Assert subKvEntry size
        Assert.assertEquals(4, entry.numSubEntries());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
        // Test inlinePointer kvEntry
        KvEntry entry = EntriesUtil.kvEntryFromInput(input, true, true);
        Assert.assertEquals(BytesId.of(100),
                            StoreTestUtil.idFromPointer(entry.key()));
        try (EntryIterator iter = new SubKvEntriesInput(entry, true)) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
                            StoreTestUtil.idFromPointer(entry.key()));
        try (EntryIterator iter = new SubKvEntriesInput(entry, true)) {
            Assert.assertEquals(BytesId.of(10),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(10),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
            Assert.assertEquals(BytesId.of(10),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(10),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(20),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
            Assert.assertEquals(BytesId.of(10),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(20),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(40),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
            Assert.assertEquals(BytesId.of(20),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(40),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(50),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
            Assert.assertEquals(BytesId.of(40),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(50),
                                StoreTestUtil.idFromPointer(iter.next().key()));
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
        // Test cachedPointer kvEntry
        entry = EntriesUtil.kvEntryFromInput(input, false, true);
        Assert.assertEquals(BytesId.of(100),
                            StoreTestUtil.idFromPointer(entry.key()));
        try (EntryIterator iter = new SubKvEntriesInput(entry, false)) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
                            StoreTestUtil.idFromPointer(entry.key()));
        try (EntryIterator iter = new SubKvEntriesInput(entry, false)) {
            Assert.assertEquals(BytesId.of(10),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(10),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
            Assert.assertEquals(BytesId.of(10),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(10),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(20),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
            Assert.assertEquals(BytesId.of(10),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(20),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(40),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
            Assert.assertEquals(BytesId.of(20),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(40),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(50),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/EntriesUtilTest.java`
#### Snippet
```java
            Assert.assertEquals(BytesId.of(40),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertEquals(BytesId.of(50),
                                StoreTestUtil.idFromPointer(iter.next().key()));
            Assert.assertThrows(NoSuchElementException.class, iter::next);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/QueuedMessageSenderTest.java`
#### Snippet
```java

        Thread sendExecutor = Whitebox.getInternalState(sender, "sendExecutor");
        Assert.assertTrue(ImmutableSet.of(Thread.State.NEW,
                                          Thread.State.RUNNABLE,
                                          Thread.State.WAITING)
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/QueuedMessageSenderTest.java`
#### Snippet
```java

        sender.close();
        Assert.assertTrue(ImmutableSet.of(Thread.State.TERMINATED)
                                      .contains(sendExecutor.getState()));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendBuffersTest.java`
#### Snippet
```java
                ComputerContext.instance());
        Map<Integer, MessageSendPartition> innerBuffers = buffers.all();
        Assert.assertEquals(3, innerBuffers.size());
    }

```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendBuffersTest.java`
#### Snippet
```java
        MessageSendBuffers buffers = new MessageSendBuffers(
                                     ComputerContext.instance());
        Assert.assertNotNull(buffers.get(0));
        Assert.assertNotNull(buffers.get(1));
        Assert.assertNotNull(buffers.get(2));
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendBuffersTest.java`
#### Snippet
```java
                                     ComputerContext.instance());
        Assert.assertNotNull(buffers.get(0));
        Assert.assertNotNull(buffers.get(1));
        Assert.assertNotNull(buffers.get(2));
        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendBuffersTest.java`
#### Snippet
```java
        Assert.assertNotNull(buffers.get(0));
        Assert.assertNotNull(buffers.get(1));
        Assert.assertNotNull(buffers.get(2));
        Assert.assertThrows(ComputerException.class, () -> {
            buffers.get(3);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendBuffersTest.java`
#### Snippet
```java
            buffers.get(3);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("Invalid partition id"));
        });
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        options.put(KEY_FALSE, Boolean.toString(Boolean.FALSE));
        Config config = new DefaultConfig(options);
        Assert.assertTrue(config.getBoolean(KEY_TRUE, defaultValue));
        Assert.assertFalse(config.getBoolean(KEY_FALSE, defaultValue));
        Assert.assertFalse(config.getBoolean(KEY_EMPTY, defaultValue));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Config config = new DefaultConfig(options);
        Assert.assertTrue(config.getBoolean(KEY_TRUE, defaultValue));
        Assert.assertFalse(config.getBoolean(KEY_FALSE, defaultValue));
        Assert.assertFalse(config.getBoolean(KEY_EMPTY, defaultValue));
        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertTrue(config.getBoolean(KEY_TRUE, defaultValue));
        Assert.assertFalse(config.getBoolean(KEY_FALSE, defaultValue));
        Assert.assertFalse(config.getBoolean(KEY_EMPTY, defaultValue));
        Assert.assertThrows(ComputerException.class, () -> {
            config.getBoolean(KEY_ABC, Boolean.TRUE);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        options.put(KEY_MIN, Double.toString(Double.MIN_VALUE));
        Config config = new DefaultConfig(options);
        Assert.assertEquals(Double.MAX_VALUE,
                            config.getDouble(KEY_MAX, defaultValue),
                            delta);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
                            config.getDouble(KEY_MAX, defaultValue),
                            delta);
        Assert.assertEquals(Double.MIN_VALUE,
                            config.getDouble(KEY_MIN, defaultValue),
                            delta);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
                            config.getDouble(KEY_MIN, defaultValue),
                            delta);
        Assert.assertEquals(defaultValue,
                            config.getDouble(KEY_EMPTY, defaultValue),
                            delta);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        options.put(KEY_MIN, Integer.toString(Integer.MIN_VALUE));
        Config config = new DefaultConfig(options);
        Assert.assertEquals(Integer.MAX_VALUE,
                            config.getInt(KEY_MAX, defaultValue));
        Assert.assertEquals(Integer.MIN_VALUE,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.MAX_VALUE,
                            config.getInt(KEY_MAX, defaultValue));
        Assert.assertEquals(Integer.MIN_VALUE,
                            config.getInt(KEY_MIN, defaultValue));
        Assert.assertEquals(defaultValue,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.MIN_VALUE,
                            config.getInt(KEY_MIN, defaultValue));
        Assert.assertEquals(defaultValue,
                            config.getInt(KEY_EMPTY, defaultValue));
        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        MasterComputation masterComputation = config.createObject(
                          ComputerOptions.MASTER_COMPUTATION_CLASS);
        Assert.assertEquals(DefaultMasterComputation.class,
                            masterComputation.getClass());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        options.put(KEY, value);
        Config config = new DefaultConfig(options);
        Assert.assertEquals(value, config.getString(KEY, defaultValue));
        Assert.assertEquals(value, config.getString(KEY, null));
        Assert.assertEquals(defaultValue,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Config config = new DefaultConfig(options);
        Assert.assertEquals(value, config.getString(KEY, defaultValue));
        Assert.assertEquals(value, config.getString(KEY, null));
        Assert.assertEquals(defaultValue,
                            config.getString(KEY_EMPTY, defaultValue));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertEquals(value, config.getString(KEY, defaultValue));
        Assert.assertEquals(value, config.getString(KEY, null));
        Assert.assertEquals(defaultValue,
                            config.getString(KEY_EMPTY, defaultValue));
        Assert.assertNull(config.getString(KEY_EMPTY, null));
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertEquals(defaultValue,
                            config.getString(KEY_EMPTY, defaultValue));
        Assert.assertNull(config.getString(KEY_EMPTY, null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        options.put(KEY_MIN, Long.toString(Long.MIN_VALUE));
        Config config = new DefaultConfig(options);
        Assert.assertEquals(Long.MAX_VALUE,
                            config.getLong(KEY_MAX, defaultValue));
        Assert.assertEquals(Long.MIN_VALUE,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertEquals(Long.MAX_VALUE,
                            config.getLong(KEY_MAX, defaultValue));
        Assert.assertEquals(Long.MIN_VALUE,
                            config.getLong(KEY_MIN, defaultValue));
        Assert.assertEquals(defaultValue,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertEquals(Long.MIN_VALUE,
                            config.getLong(KEY_MIN, defaultValue));
        Assert.assertEquals(defaultValue,
                            config.getLong(KEY_EMPTY, defaultValue));
        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Object combiner = config.createObject(
                          ComputerOptions.WORKER_COMBINER_CLASS, false);
        Assert.assertNull(combiner);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
    @Override
    protected void assertStep1Aggregators(MasterComputationContext context) {
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                AGGR_CUSTOM_FLOAT));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_CUSTOM_FLOAT));

        Assert.assertEquals(new FloatValue(6.28f), context.aggregatedValue(
                AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(9), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(6.28f), context.aggregatedValue(
                AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(9), context.aggregatedValue(
                AGGR_INT_UNSTABLE));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_INT_UNSTABLE));

        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
                AGGR_INT_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_INT_MAX));

        Assert.assertEquals(new LongValue(10L), context.aggregatedValue(
                AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(10L), context.aggregatedValue(
                AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
                AGGR_LONG_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_LONG_MAX));

        Assert.assertEquals(new FloatValue(20.8f), context.aggregatedValue(
                AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(20.8f), context.aggregatedValue(
                AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
                AGGR_FLOAT_MIN));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_FLOAT_MIN));

        Assert.assertEquals(new DoubleValue(20.8), context.aggregatedValue(
                AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new DoubleValue(20.8), context.aggregatedValue(
                AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
                AGGR_DOUBLE_MIN));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
    @Override
    protected void assertStep0Aggregators(MasterComputationContext context) {
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                AGGR_CUSTOM_FLOAT));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_CUSTOM_FLOAT));

        Assert.assertEquals(new FloatValue(6.28f), context.aggregatedValue(
                AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(6.28f), context.aggregatedValue(
                AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_INT_UNSTABLE));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_INT_UNSTABLE));

        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
                AGGR_INT_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_INT_MAX));

        Assert.assertEquals(new LongValue(10L), context.aggregatedValue(
                AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(10L), context.aggregatedValue(
                AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
                AGGR_LONG_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_LONG_MAX));

        Assert.assertEquals(new FloatValue(20.8f), context.aggregatedValue(
                AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(20.8f), context.aggregatedValue(
                AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
                AGGR_FLOAT_MIN));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
                AGGR_FLOAT_MIN));

        Assert.assertEquals(new DoubleValue(20.8), context.aggregatedValue(
                AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new DoubleValue(20.8), context.aggregatedValue(
                AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
                AGGR_DOUBLE_MIN));
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
    public void testReachThreshold() throws IOException {
        WriteBuffer buffer = new WriteBuffer(context, 20, 50);
        Assert.assertFalse(buffer.reachThreshold());

        Vertex vertex = context.graphFactory().createVertex(
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
        // After write, the position is 11
        buffer.writeVertex(vertex);
        Assert.assertFalse(buffer.reachThreshold());

        // After write, the position is 22
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
        // After write, the position is 22
        buffer.writeVertex(vertex);
        Assert.assertTrue(buffer.reachThreshold());

        // After write, the position is 33
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
        // After write, the position is 33
        buffer.writeVertex(vertex);
        Assert.assertTrue(buffer.reachThreshold());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
    public void testClear() throws IOException {
        WriteBuffer buffer = new WriteBuffer(context, 10, 20);
        Assert.assertTrue(buffer.isEmpty());

        Vertex vertex = context.graphFactory().createVertex(
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
                        BytesId.of(1L), new DoubleValue(0.5d));
        buffer.writeVertex(vertex);
        Assert.assertFalse(buffer.isEmpty());

        buffer.clear();
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java

        buffer.clear();
        Assert.assertTrue(buffer.isEmpty());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
    public void testIsEmpty() throws IOException {
        WriteBuffer buffer = new WriteBuffer(context, 10, 20);
        Assert.assertTrue(buffer.isEmpty());

        Vertex vertex = context.graphFactory().createVertex(
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
                        BytesId.of(1L), new DoubleValue(0.5d));
        buffer.writeVertex(vertex);
        Assert.assertFalse(buffer.isEmpty());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
    @Override
    protected void assertStep1Aggregators(WorkerContext context) {
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(10), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_MAX));

        Assert.assertEquals(new LongValue(10L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(10L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_LONG_MAX));

        Assert.assertEquals(new FloatValue(20.8f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(20.8f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_MIN));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_FLOAT_MIN));

        Assert.assertEquals(new DoubleValue(20.8), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation2.java`
#### Snippet
```java
        Assert.assertEquals(new DoubleValue(20.8), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_MIN));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
        WorkerStat workerStat3 = new WorkerStat();

        Assert.assertEquals(workerStat1, workerStat2);
        Assert.assertNotEquals(workerStat1, workerStat3);
        Assert.assertNotEquals(workerStat1, new Object());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java

        Assert.assertEquals(workerStat1, workerStat2);
        Assert.assertNotEquals(workerStat1, workerStat3);
        Assert.assertNotEquals(workerStat1, new Object());
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
        Assert.assertEquals(workerStat1, workerStat2);
        Assert.assertNotEquals(workerStat1, workerStat3);
        Assert.assertNotEquals(workerStat1, new Object());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
        workerStat1.add(stat1);
        workerStat1.add(stat2);
        Assert.assertEquals(2, workerStat1.size());
        Assert.assertEquals(stat1, workerStat1.get(0));
        Assert.assertEquals(stat2, workerStat1.get(1));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
        workerStat1.add(stat2);
        Assert.assertEquals(2, workerStat1.size());
        Assert.assertEquals(stat1, workerStat1.get(0));
        Assert.assertEquals(stat2, workerStat1.get(1));
        WorkerStat workerStat2 = new WorkerStat(1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
        Assert.assertEquals(2, workerStat1.size());
        Assert.assertEquals(stat1, workerStat1.get(0));
        Assert.assertEquals(stat2, workerStat1.get(1));
        WorkerStat workerStat2 = new WorkerStat(1);
        Assert.assertEquals(1, workerStat2.workerId());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
        Assert.assertEquals(stat2, workerStat1.get(1));
        WorkerStat workerStat2 = new WorkerStat(1);
        Assert.assertEquals(1, workerStat2.workerId());
        Assert.assertEquals(0, workerStat2.size());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
        WorkerStat workerStat2 = new WorkerStat(1);
        Assert.assertEquals(1, workerStat2.workerId());
        Assert.assertEquals(0, workerStat2.size());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
                     "\"messageSendCount\":5,\"messageSendBytes\":6," +
                     "\"messageRecvCount\":7,\"messageRecvBytes\":8}]}";
        Assert.assertEquals(str, workerStat.toString());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java
        WorkerStat workerStat3 = new WorkerStat(2);

        Assert.assertEquals(workerStat1.hashCode(), workerStat2.hashCode());
        Assert.assertNotEquals(workerStat1.hashCode(), workerStat3.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerStatTest.java`
#### Snippet
```java

        Assert.assertEquals(workerStat1.hashCode(), workerStat2.hashCode());
        Assert.assertNotEquals(workerStat1.hashCode(), workerStat3.hashCode());
    }

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
    public void testReachThreshold() throws IOException {
        WriteBuffers buffers = new WriteBuffers(context(), 20, 50);
        Assert.assertFalse(buffers.reachThreshold());

        Vertex vertex = context().graphFactory().createVertex(
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        // After write, the position is 11
        buffers.writeVertex(vertex);
        Assert.assertFalse(buffers.reachThreshold());

        // After write, the position is 22
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        // After write, the position is 22
        buffers.writeVertex(vertex);
        Assert.assertTrue(buffers.reachThreshold());

        // After write, the position is 33
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        // After write, the position is 33
        buffers.writeVertex(vertex);
        Assert.assertTrue(buffers.reachThreshold());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        buffers.writeEdges(vertex);
        // Reached threshold, the position is 76
        Assert.assertTrue(buffers.reachThreshold());
        /*
         * When reached threshold, switchForSorting will exchange writing buffer
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
         */
        buffers.switchForSorting();
        Assert.assertFalse(buffers.reachThreshold());
        Assert.assertTrue(buffers.isEmpty());
        // Nothing changed
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        buffers.switchForSorting();
        Assert.assertFalse(buffers.reachThreshold());
        Assert.assertTrue(buffers.isEmpty());
        // Nothing changed
        buffers.switchForSorting();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        // Nothing changed
        buffers.switchForSorting();
        Assert.assertFalse(buffers.reachThreshold());
        Assert.assertTrue(buffers.isEmpty());
        // The writing buffer reached threshold again, position is 76
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        buffers.switchForSorting();
        Assert.assertFalse(buffers.reachThreshold());
        Assert.assertTrue(buffers.isEmpty());
        // The writing buffer reached threshold again, position is 76
        buffers.writeEdges(vertex);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
            // Await until finishSorting method called
            buffers.switchForSorting();
            Assert.assertEquals(2, counter.get());
        });
        Thread thread2 = new Thread(() -> {
```

### StaticCallOnSubclass
Static method `fail()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
                    Thread.sleep(100);
                } catch (InterruptedException e) {
                    Assert.fail(e.getMessage());
                }
                counter.incrementAndGet();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
                                                               entryInput);
            vertex.value(null);
            Assert.assertEquals(vertex, graphInput.readVertex());
        }
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        buffers.writeEdges(vertex);
        // Reached threshold, the position is 76
        Assert.assertTrue(buffers.reachThreshold());
        Assert.assertFalse(buffers.isEmpty());
        // Exchange writing buffer and sorting buffer
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        // Reached threshold, the position is 76
        Assert.assertTrue(buffers.reachThreshold());
        Assert.assertFalse(buffers.isEmpty());
        // Exchange writing buffer and sorting buffer
        buffers.prepareSorting();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        // Exchange writing buffer and sorting buffer
        buffers.prepareSorting();
        Assert.assertFalse(buffers.reachThreshold());
        Assert.assertTrue(buffers.isEmpty());

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        buffers.prepareSorting();
        Assert.assertFalse(buffers.reachThreshold());
        Assert.assertTrue(buffers.isEmpty());

        Thread thread1 = new Thread(() -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
                buffers.prepareSorting();
            }, e -> {
                Assert.assertTrue(e.getMessage().contains("Interrupted"));
            });
        });
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
            new WriteBuffers(context(), 0, 20);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains(
                              "The threshold of buffer must be > 0"));
        });
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
            new WriteBuffers(context(), 10, -1);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains(
                              "The capacity of buffer must be > 0"));
        });
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
            new WriteBuffers(context(), 20, 10);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains(
                              "The threshold must be <= capacity"));
        });
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
    public void testIsEmpty() throws IOException {
        WriteBuffers buffers = new WriteBuffers(context(), 10, 20);
        Assert.assertTrue(buffers.isEmpty());

        Vertex vertex = context().graphFactory().createVertex(
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
                        BytesId.of(1L), new DoubleValue(0.5d));
        buffers.writeVertex(vertex);
        Assert.assertFalse(buffers.isEmpty());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                                                           bufferSize)) {
                for (int i = 0; i < 50; i++) {
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertThrows(IOException.class, input::readInt, e -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                                   long position) throws IOException {
        input.seek(position);
        Assert.assertEquals(position, input.position());
        Assert.assertEquals(fileLength - position, input.available());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
        input.seek(position);
        Assert.assertEquals(position, input.position());
        Assert.assertEquals(fileLength - position, input.available());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            try (ValueFileInput input = new ValueFileInput(CONFIG, dir,
                                                           bufferSize)) {
                Assert.assertEquals(100, input.readInt());

                input.skip(8);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java

                input.skip(8);
                Assert.assertEquals(200, input.readInt());

                input.seek(20 * Integer.BYTES);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java

                input.seek(20 * Integer.BYTES);
                Assert.assertEquals(20, input.readInt());

                input.skip(10 * Integer.BYTES);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java

                input.skip(10 * Integer.BYTES);
                Assert.assertEquals(31, input.readInt());

                Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java

                result = input1.compare(1, 3, input2, 1, 3);
                Assert.assertEquals(0, result);

                // The input1 in buffer, input2 not in buffer
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java

                result = input1.compare(3, 1, input2, 23, 1);
                Assert.assertEquals(0, result);

                // The input1 not in buffer, input2 in buffer
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                input2.seek(20);
                result = input1.compare(23, 5, input2, 23, 5);
                Assert.assertEquals(0, result);

                result = input1.compare(23, 12, input2, 23, 5);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                input2.seek(0);
                result = input1.compare(23, 5, input2, 23, 5);
                Assert.assertEquals(0, result);

                result = input1.compare(23, 12, input2, 23, 5);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                        Byte.MAX_VALUE, expectSize);
        for (int i = 0; i < expectSize; i++) {
            Assert.assertEquals(i, bytes[i]);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java

                input.seek(16 * Integer.BYTES);
                Assert.assertEquals(16, input.readInt());

                // Random seek and assert data
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                    int num = random.nextInt(49);
                    input.seek(num * Integer.BYTES);
                    Assert.assertEquals(num, input.readInt());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                for (int i = 0; i < 50; i++) {
                    if (i == 0) {
                        Assert.assertEquals(200, input.readInt());
                        continue;
                    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                    }
                    if (i == 1) {
                        Assert.assertEquals(100, input.readInt());
                        continue;
                    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                        continue;
                    }
                    Assert.assertEquals(i, input.readInt());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            try (ValueFileOutput output = new ValueFileOutput(CONFIG, dir,
                                                              bufferCapacity)) {
                Assert.assertEquals(0, output.position());
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            try (ValueFileInput input = new ValueFileInput(CONFIG, dir,
                                                           bufferCapacity)) {
                Assert.assertEquals(0, input.position());
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            );
            try (ValueFileOutput output = new ValueFileOutput(config, dir)) {
                Assert.assertEquals(0, output.position());
            }
            try (ValueFileInput input = new ValueFileInput(config, dir)) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            }
            try (ValueFileInput input = new ValueFileInput(config, dir)) {
                Assert.assertEquals(0, input.position());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                for (int i = 0; i < 50; i++) {
                    if (i == 1 || i == 10) {
                        Assert.assertEquals(100, input.readInt());
                        continue;
                    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                        continue;
                    }
                    Assert.assertEquals(i, input.readInt());
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                                                           bufferSize)) {
                input.seek(20 * Integer.BYTES);
                Assert.assertEquals(20, input.readInt());

                UnsafeBytesInput other = input.duplicate();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java

                UnsafeBytesInput other = input.duplicate();
                Assert.assertEquals(21, other.readInt());
                Assert.assertEquals(21, input.readInt());
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                UnsafeBytesInput other = input.duplicate();
                Assert.assertEquals(21, other.readInt());
                Assert.assertEquals(21, input.readInt());
            }
        } finally {
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java

        for (Throwable e : exceptions) {
            Assert.assertNull(e);
        }
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                QueuedMessage message1 = queue.take();
                QueuedMessage message2 = queue.take();
                Assert.assertTrue(ImmutableSet.of(1, 2).contains(
                                  message1.partitionId()));
                Assert.assertTrue(ImmutableSet.of(1, 2).contains(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                Assert.assertTrue(ImmutableSet.of(1, 2).contains(
                                  message1.partitionId()));
                Assert.assertTrue(ImmutableSet.of(1, 2).contains(
                                  message2.partitionId()));

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                latches[1].countDown();
                QueuedMessage message = queue.take();
                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
                                  message.partitionId()));

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                }

                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
                                  queue.take().partitionId()));

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java

                latches[2].countDown();
                Assert.assertTrue(ImmutableSet.of(5, 6).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(5, 6).contains(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                Assert.assertTrue(ImmutableSet.of(5, 6).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(5, 6).contains(
                                  queue.take().partitionId()));
            } catch (Throwable e) {
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java

        for (Throwable e : exceptions) {
            Assert.assertNull(e);
        }
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
            try {
                latches[0].countDown();
                Assert.assertTrue(ImmutableSet.of(1, 2).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(1, 2).contains(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                Assert.assertTrue(ImmutableSet.of(1, 2).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(1, 2).contains(
                                  queue.take().partitionId()));

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java

                latches[1].countDown();
                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(3, 4).contains(
                                  queue.take().partitionId()));

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java

                latches[2].countDown();
                Assert.assertTrue(ImmutableSet.of(5, 6).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(5, 6).contains(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java
                Assert.assertTrue(ImmutableSet.of(5, 6).contains(
                                  queue.take().partitionId()));
                Assert.assertTrue(ImmutableSet.of(5, 6).contains(
                                  queue.take().partitionId()));
            } catch (Throwable e) {
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueueTest.java`
#### Snippet
```java

        for (Throwable e : exceptions) {
            Assert.assertNull(e);
        }
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerServiceTest.java`
#### Snippet
```java
        pool.shutdownNow();

        Assert.assertFalse(Arrays.asList(exceptions).toString(),
                           existError(exceptions));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/ResuablePointerTest.java`
#### Snippet
```java
        ReusablePointer p2 = new ReusablePointer();
        p2.read(IOFactory.createBytesInput(output2.buffer()));
        Assert.assertEquals(0, p1.compareTo(p2));
        Assert.assertEquals(p1.length(), p2.length());
        Assert.assertEquals(0L, p1.offset());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/ResuablePointerTest.java`
#### Snippet
```java
        p2.read(IOFactory.createBytesInput(output2.buffer()));
        Assert.assertEquals(0, p1.compareTo(p2));
        Assert.assertEquals(p1.length(), p2.length());
        Assert.assertEquals(0L, p1.offset());
        Assert.assertEquals(0L, p2.offset());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/ResuablePointerTest.java`
#### Snippet
```java
        Assert.assertEquals(0, p1.compareTo(p2));
        Assert.assertEquals(p1.length(), p2.length());
        Assert.assertEquals(0L, p1.offset());
        Assert.assertEquals(0L, p2.offset());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/ResuablePointerTest.java`
#### Snippet
```java
        Assert.assertEquals(p1.length(), p2.length());
        Assert.assertEquals(0L, p1.offset());
        Assert.assertEquals(0L, p2.offset());
    }
}
```

### StaticCallOnSubclass
Static method `fail()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerServiceTest.java`
#### Snippet
```java
                    workerService.close();
                } catch (Throwable e) {
                    Assert.fail(e.getMessage());
                }
                countDownLatch.countDown();
```

### StaticCallOnSubclass
Static method `fail()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerServiceTest.java`
#### Snippet
```java
                    masterService.close();
                } catch (Throwable e) {
                    Assert.fail(e.getMessage());
                }
                countDownLatch.countDown();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerServiceTest.java`
#### Snippet
```java
        pool.shutdownNow();

        Assert.assertFalse(Arrays.asList(exceptions).toString(),
                           existError(exceptions));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java

    protected void assertStat(MasterComputationContext context) {
        Assert.assertEquals(6L, context.totalVertexCount());
        Assert.assertEquals(5L, context.totalEdgeCount());
        Assert.assertEquals(0L, context.finishedVertexCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
    protected void assertStat(MasterComputationContext context) {
        Assert.assertEquals(6L, context.totalVertexCount());
        Assert.assertEquals(5L, context.totalEdgeCount());
        Assert.assertEquals(0L, context.finishedVertexCount());
        Assert.assertEquals(0L, context.messageCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(6L, context.totalVertexCount());
        Assert.assertEquals(5L, context.totalEdgeCount());
        Assert.assertEquals(0L, context.finishedVertexCount());
        Assert.assertEquals(0L, context.messageCount());
        Assert.assertEquals(0L, context.messageBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(5L, context.totalEdgeCount());
        Assert.assertEquals(0L, context.finishedVertexCount());
        Assert.assertEquals(0L, context.messageCount());
        Assert.assertEquals(0L, context.messageBytes());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(0L, context.finishedVertexCount());
        Assert.assertEquals(0L, context.messageCount());
        Assert.assertEquals(0L, context.messageBytes());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        context.aggregatedValue(MockMasterComputation.AGGR_FLOAT_UNSTABLE,
                                new FloatValue(8.8f));
        Assert.assertEquals(new FloatValue(8.8f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        context.aggregatedValue(MockMasterComputation.AGGR_INT_UNSTABLE,
                                new IntValue(888));
        Assert.assertEquals(new IntValue(888), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_UNSTABLE));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java

    protected void assertStep1Aggregators(MasterComputationContext context) {
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(5.2f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(5.2f), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

        Assert.assertEquals(new FloatValue(3.14f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(9), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(3.14f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(9), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_UNSTABLE));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_UNSTABLE));

        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_MAX));

        Assert.assertEquals(new LongValue(5L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(5L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_LONG_MAX));

        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_MIN));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_FLOAT_MIN));

        Assert.assertEquals(new DoubleValue(10.4), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new DoubleValue(10.4), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_MIN));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java

    protected void assertStep0Aggregators(MasterComputationContext context) {
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(5.2f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(5.2f), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

        Assert.assertEquals(new FloatValue(3.14f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(10),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(3.14f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(10),
                            context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_UNSTABLE));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_UNSTABLE));

        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_MAX));

        Assert.assertEquals(new LongValue(5L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(5L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_LONG_MAX));

        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_MIN));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_FLOAT_MIN));

        Assert.assertEquals(new DoubleValue(10.4), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertEquals(new DoubleValue(10.4), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_MIN));
    }
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        Channel channel = null;
        ConnectionId connectionId = TransportUtil.remoteConnectionId(channel);
        Assert.assertNull(connectionId);

        Channel channel2 = new EmbeddedChannel();
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        channel2.close();
        ConnectionId connectionId2 = TransportUtil.remoteConnectionId(channel2);
        Assert.assertNull(connectionId2);
    }

```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        Channel channel = null;
        String address = TransportUtil.remoteAddress(channel);
        Assert.assertNull(address);

        Channel channel2 = new EmbeddedChannel();
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        channel2.close();
        String address2 = TransportUtil.remoteAddress(channel2);
        Assert.assertNull(address2);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
                                    "xxxxx", 80);
        String formatAddress = TransportUtil.formatAddress(address);
        Assert.assertEquals("xxxxx:80", formatAddress);

        InetSocketAddress address2 = TransportUtil.resolvedSocketAddress(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
            buffer.writeBytes(testData);
            String readString = TransportUtil.readString(buffer);
            Assert.assertEquals("test data", readString);
        } finally {
            buffer.release();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
            TransportUtil.writeString(buffer, "");
            String readString = TransportUtil.readString(buffer);
            Assert.assertEquals("", readString);
        } finally {
            buffer.release();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        InetSocketAddress address = TransportUtil.resolvedSocketAddress(
                                    "www.baidu.com", 80);
        Assert.assertFalse(address.isUnresolved());

        InetSocketAddress address2 = TransportUtil.resolvedSocketAddress(
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        InetSocketAddress address2 = TransportUtil.resolvedSocketAddress(
                                     "www.baidu.com", 9797);
        Assert.assertFalse(address2.isUnresolved());

        InetSocketAddress address3 = TransportUtil.resolvedSocketAddress(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        InetSocketAddress address3 = TransportUtil.resolvedSocketAddress(
                                     "xxxxx", 80);
        Assert.assertTrue(address3.isUnresolved());

        InetSocketAddress address4 = TransportUtil.resolvedSocketAddress(
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        InetSocketAddress address4 = TransportUtil.resolvedSocketAddress(
                                     "127.0.0.1", 80);
        Assert.assertFalse(address4.isUnresolved());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
            TransportUtil.writeString(buffer, "test data");
            String readString = TransportUtil.readString(buffer);
            Assert.assertEquals("test data", readString);
        } finally {
            buffer.release();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/DataServerManagerTest.java`
#### Snippet
```java
        serverManager.init(config);

        Assert.assertEquals(DataServerManager.NAME, serverManager.name());
        InetSocketAddress address = serverManager.address();
        Assert.assertNotEquals(0, address.getPort());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/DataServerManagerTest.java`
#### Snippet
```java
        Assert.assertEquals(DataServerManager.NAME, serverManager.name());
        InetSocketAddress address = serverManager.address();
        Assert.assertNotEquals(0, address.getPort());
        ConnectionId connectionId = ConnectionId.parseConnectionId(
                                                 address.getHostName(),
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/ConnectionIdTest.java`
#### Snippet
```java
        ConnectionId connectionId2 = ConnectionId.parseConnectionId(
                                     "127.0.0.1", 8080);
        Assert.assertSame(connectionId1, connectionId2);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/ConnectionIdTest.java`
#### Snippet
```java
        ConnectionId connectionId2 = ConnectionId.parseConnectionId(
                                     hostName, 8080);
        Assert.assertEquals(connectionId, connectionId2);
    }
}
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/ConnectionIdTest.java`
#### Snippet
```java
            new ConnectionId(localSocketAddress);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("The address must be " +
                                                      "resolved"));
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/ConnectionIdTest.java`
#### Snippet
```java
                                    "127.0.0.1", 8080);
        ConnectionId connectionId2 = new ConnectionId(address);
        Assert.assertEquals(connectionId1, connectionId2);

        ConnectionId connectionId3 = ConnectionId.parseConnectionId(
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/ConnectionIdTest.java`
#### Snippet
```java
        ConnectionId connectionId3 = ConnectionId.parseConnectionId(
                                     "127.0.0.1", 8080, 2);
        Assert.assertNotEquals(connectionId1, connectionId3);
    }

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/MessageInputTest.java`
#### Snippet
```java
            Iterator<IdList> mit = input.iterator(idPointer);
            if (messages == null) {
                Assert.assertFalse(mit.hasNext());
            } else {
                for (int j = 0; j < messages.size();j++) {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/MessageInputTest.java`
#### Snippet
```java
            } else {
                for (int j = 0; j < messages.size();j++) {
                    Assert.assertTrue(mit.hasNext());
                    Assert.assertTrue(messages.contains(mit.next()));
                }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/MessageInputTest.java`
#### Snippet
```java
                for (int j = 0; j < messages.size();j++) {
                    Assert.assertTrue(mit.hasNext());
                    Assert.assertTrue(messages.contains(mit.next()));
                }
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/StoreTestUtil.java`
#### Snippet
```java
             * so it's not recommended to assert length value.
             */
            Assert.assertEquals(19, builder.headerLength());
        } catch (Exception e) {
            FileUtils.deleteQuietly(file);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

    protected void assertStep1Aggregators(WorkerContext context) {
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(5.2f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(5.2f), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

        Assert.assertEquals(new FloatValue(8.8f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(888), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(8.8f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(888), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_UNSTABLE));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_UNSTABLE));

        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(5), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(8), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_MAX));

        Assert.assertEquals(new LongValue(5L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        TransportClient client = connectionManager.getOrCreateClient(
                                 connectionId);
        Assert.assertTrue(client.active());
        return client;
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(5L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(8L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_LONG_MAX));

        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(10.4f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(-10.0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_MIN));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_FLOAT_MIN));

        Assert.assertEquals(new DoubleValue(10.4), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new DoubleValue(10.4), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(-10.0), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_MIN));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

    protected void assertStep0Aggregators(WorkerContext context) {
        Assert.assertEquals(new IntValue(0), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(0f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(0), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_INT));
        Assert.assertEquals(new FloatValue(0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_CUSTOM_FLOAT));

        Assert.assertEquals(new FloatValue(0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(Integer.MAX_VALUE),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_UNSTABLE));
        Assert.assertEquals(new IntValue(Integer.MAX_VALUE),
                            context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_UNSTABLE));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_UNSTABLE));

        Assert.assertEquals(new IntValue(0), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(0), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new IntValue(0), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_SUM));
        Assert.assertEquals(new IntValue(0), context.aggregatedValue(
                            MockMasterComputation.AGGR_INT_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_INT_MAX));

        Assert.assertEquals(new LongValue(0L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(0L), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new LongValue(0L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_SUM));
        Assert.assertEquals(new LongValue(0L), context.aggregatedValue(
                            MockMasterComputation.AGGR_LONG_MAX));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_LONG_MAX));

        Assert.assertEquals(new FloatValue(0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(0f), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new FloatValue(0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_SUM));
        Assert.assertEquals(new FloatValue(0f), context.aggregatedValue(
                            MockMasterComputation.AGGR_FLOAT_MIN));

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_FLOAT_MIN));

        Assert.assertEquals(new DoubleValue(0d), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(0d), context.aggregatedValue(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        Assert.assertEquals(new DoubleValue(0d), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_SUM));
        Assert.assertEquals(new DoubleValue(0d), context.aggregatedValue(
                            MockMasterComputation.AGGR_DOUBLE_MIN));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                             MockMasterComputation.AGGR_CUSTOM_INT);

        Assert.assertEquals(new IntValue(0),
                            this.aggrCustomInt.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrCustomInt.aggregateValue(1);
        Assert.assertEquals(new IntValue(1),
                            this.aggrCustomInt.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
                                    "127.0.0.1", 8086);
        this.client = clientFactory.createClient(connectionId, clientHandler);
        Assert.assertTrue(this.client.active());
        this.client.close();
        Assert.assertFalse(this.client.active());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrCustomInt.aggregateValue(new IntValue(1));
        Assert.assertEquals(new IntValue(2),
                            this.aggrCustomInt.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
        Assert.assertTrue(this.client.active());
        this.client.close();
        Assert.assertFalse(this.client.active());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrCustomInt.aggregateValue(3);
        Assert.assertEquals(new IntValue(5),
                            this.aggrCustomInt.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                               MockMasterComputation.AGGR_CUSTOM_FLOAT);

        Assert.assertEquals(new FloatValue(0f),
                            this.aggrCustomFloat.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
        Object bootstrap = Whitebox.getInternalState(clientFactory,
                                                     "bootstrap");
        Assert.assertNotNull(bootstrap);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrCustomFloat.aggregateValue(1f);
        Assert.assertEquals(new FloatValue(1f),
                            this.aggrCustomFloat.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrCustomFloat.aggregateValue(new FloatValue(1f));
        Assert.assertEquals(new FloatValue(2f),
                            this.aggrCustomFloat.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrCustomFloat.aggregateValue(3.2f);
        Assert.assertEquals(new FloatValue(5.2f),
                            this.aggrCustomFloat.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                          MockMasterComputation.AGGR_INT_SUM);

        Assert.assertEquals(new IntValue(0),
                            this.aggrIntSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrIntSum.aggregateValue(1);
        Assert.assertEquals(new IntValue(1),
                            this.aggrIntSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrIntSum.aggregateValue(new IntValue(1));
        Assert.assertEquals(new IntValue(2),
                            this.aggrIntSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrIntSum.aggregateValue(3);
        Assert.assertEquals(new IntValue(5),
                            this.aggrIntSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
                                    "127.0.0.1", 8086);
        this.client = clientFactory.createClient(connectionId, clientHandler);
        Assert.assertTrue(this.client.active());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                          MockMasterComputation.AGGR_INT_MAX);

        Assert.assertEquals(new IntValue(0),
                            this.aggrIntMax.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrIntMax.aggregateValue(1);
        Assert.assertEquals(new IntValue(1),
                            this.aggrIntMax.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrIntMax.aggregateValue(8);
        Assert.assertEquals(new IntValue(8),
                            this.aggrIntMax.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrIntMax.aggregateValue(3);
        Assert.assertEquals(new IntValue(8),
                            this.aggrIntMax.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                           MockMasterComputation.AGGR_LONG_SUM);

        Assert.assertEquals(new LongValue(0L),
                            this.aggrLongSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrLongSum.aggregateValue(1L);
        Assert.assertEquals(new LongValue(1L),
                            this.aggrLongSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrLongSum.aggregateValue(new LongValue(1L));
        Assert.assertEquals(new LongValue(2L),
                            this.aggrLongSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrLongSum.aggregateValue(3L);
        Assert.assertEquals(new LongValue(5L),
                            this.aggrLongSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                           MockMasterComputation.AGGR_LONG_MAX);

        Assert.assertEquals(new LongValue(0L),
                            this.aggrLongMax.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrLongMax.aggregateValue(1L);
        Assert.assertEquals(new LongValue(1L),
                            this.aggrLongMax.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrLongMax.aggregateValue(8L);
        Assert.assertEquals(new LongValue(8L),
                            this.aggrLongMax.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrLongMax.aggregateValue(3L);
        Assert.assertEquals(new LongValue(8L),
                            this.aggrLongMax.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_FLOAT_SUM);

        Assert.assertEquals(new FloatValue(0.0f),
                            this.aggrFloatSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrFloatSum.aggregateValue(1.1f);
        Assert.assertEquals(new FloatValue(1.1f),
                            this.aggrFloatSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrFloatSum.aggregateValue(2.3f);
        Assert.assertEquals(new FloatValue(3.4f),
                            this.aggrFloatSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrFloatSum.aggregateValue(7.0f);
        Assert.assertEquals(new FloatValue(10.4f),
                            this.aggrFloatSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                            MockMasterComputation.AGGR_FLOAT_MIN);

        Assert.assertEquals(new FloatValue(0.0f),
                            this.aggrFloatMin.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrFloatMin.aggregateValue(1.1f);
        Assert.assertEquals(new FloatValue(0.0f),
                            this.aggrFloatMin.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrFloatMin.aggregateValue(-10.0f);
        Assert.assertEquals(new FloatValue(-10.0f),
                            this.aggrFloatMin.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrFloatMin.aggregateValue(-4.0f);
        Assert.assertEquals(new FloatValue(-10.0f),
                            this.aggrFloatMin.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                             MockMasterComputation.AGGR_DOUBLE_SUM);

        Assert.assertEquals(new DoubleValue(0.0),
                            this.aggrDoubleSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrDoubleSum.aggregateValue(1.1);
        Assert.assertEquals(new DoubleValue(1.1),
                            this.aggrDoubleSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrDoubleSum.aggregateValue(2.3);
        Assert.assertEquals(new DoubleValue(3.4),
                            this.aggrDoubleSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrDoubleSum.aggregateValue(7.0);
        Assert.assertEquals(new DoubleValue(10.4),
                            this.aggrDoubleSum.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
                             MockMasterComputation.AGGR_DOUBLE_MIN);

        Assert.assertEquals(new DoubleValue(0.0),
                            this.aggrDoubleMin.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrDoubleMin.aggregateValue(1.1);
        Assert.assertEquals(new DoubleValue(0.0),
                            this.aggrDoubleMin.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrDoubleMin.aggregateValue(-10.0);
        Assert.assertEquals(new DoubleValue(-10.0),
                            this.aggrDoubleMin.aggregatedValue());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

        this.aggrDoubleMin.aggregateValue(-4.0);
        Assert.assertEquals(new DoubleValue(-10.0),
                            this.aggrDoubleMin.aggregatedValue());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
        PropertiesCombiner combiner = new MergeOldPropertiesCombiner();
        combiner.combine(properties1, properties2, properties);
        Assert.assertEquals(expect, properties);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, properties2, properties2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
            combiner.combine(properties1, null, properties2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
            combiner.combine(properties1, properties2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        ByteBuffer byteBuffer = ByteBuffer.allocate(10);
        NetworkBuffer nioNetworkBuffer = new NioBuffer(byteBuffer);
        Assert.assertSame(nioNetworkBuffer.nioByteBuffer().array(),
                          byteBuffer.array());
        nioNetworkBuffer.release();
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        NetworkBuffer nettyNetworkBuffer = new NettyBuffer(byteBuf);
        nettyNetworkBuffer.release();
        Assert.assertSame(cnt - 1, nettyNetworkBuffer.referenceCount());
    }

```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        ByteBuffer byteBuffer = ByteBuffer.allocate(10);
        NetworkBuffer nioNetworkBuffer = new NioBuffer(byteBuffer);
        Assert.assertSame(nioNetworkBuffer.nettyByteBuf().array(),
                          byteBuffer.array());
        nioNetworkBuffer.release();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
                          byteBuffer.array());
        nioNetworkBuffer.release();
        Assert.assertEquals(0, nioNetworkBuffer.referenceCount());

        ByteBuf byteBuf = Unpooled.buffer(10);
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        NetworkBuffer nettyNetworkBuffer = new NettyBuffer(byteBuf);
        ByteBuf buf = nettyNetworkBuffer.nettyByteBuf();
        Assert.assertSame(buf.array(), byteBuf.array());
    }

```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        ByteBuffer byteBuffer = ByteBuffer.allocate(10);
        NetworkBuffer nioNetworkBuffer = new NioBuffer(byteBuffer);
        Assert.assertSame(nioNetworkBuffer.nioByteBuffer().array(),
                          byteBuffer.array());
        nioNetworkBuffer.release();
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        NetworkBuffer nettyNetworkBuffer = new NettyBuffer(byteBuf);
        ByteBuffer buffer = nettyNetworkBuffer.nioByteBuffer();
        Assert.assertSame(buffer.array(), byteBuf.array());
    }

```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        NioBuffer nioManagedBuffer = new NioBuffer(byteBuffer);
        byte[] bytes = nioManagedBuffer.copyToByteArray();
        Assert.assertArrayEquals(bytesSource, bytes);
        Assert.assertNotSame(bytesSource, bytes);

```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        byte[] bytes = nioManagedBuffer.copyToByteArray();
        Assert.assertArrayEquals(bytesSource, bytes);
        Assert.assertNotSame(bytesSource, bytes);

        ByteBuffer byteBuffer2 = ByteBuffer.allocate(bytesSource.length);
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        NioBuffer nioManagedBuffer2 = new NioBuffer(byteBuffer2);
        byte[] bytes2 = nioManagedBuffer2.copyToByteArray();
        Assert.assertArrayEquals(bytesSource, bytes2);
        Assert.assertNotSame(bytesSource, bytes2);
        Assert.assertEquals(position, byteBuffer2.position());
```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        byte[] bytes2 = nioManagedBuffer2.copyToByteArray();
        Assert.assertArrayEquals(bytesSource, bytes2);
        Assert.assertNotSame(bytesSource, bytes2);
        Assert.assertEquals(position, byteBuffer2.position());
        Assert.assertEquals(remaining, byteBuffer2.remaining());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        Assert.assertArrayEquals(bytesSource, bytes2);
        Assert.assertNotSame(bytesSource, bytes2);
        Assert.assertEquals(position, byteBuffer2.position());
        Assert.assertEquals(remaining, byteBuffer2.remaining());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        Assert.assertNotSame(bytesSource, bytes2);
        Assert.assertEquals(position, byteBuffer2.position());
        Assert.assertEquals(remaining, byteBuffer2.remaining());

        ByteBuf buf3 = Unpooled.directBuffer(bytesSource.length);
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
                               new NettyBuffer(buf3);
            byte[] bytes3 = nettyManagedBuffer3.copyToByteArray();
            Assert.assertArrayEquals(bytesSource, bytes3);
            Assert.assertNotSame(bytesSource, bytes3);
            Assert.assertEquals(readerIndex, buf3.readerIndex());
```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
            byte[] bytes3 = nettyManagedBuffer3.copyToByteArray();
            Assert.assertArrayEquals(bytesSource, bytes3);
            Assert.assertNotSame(bytesSource, bytes3);
            Assert.assertEquals(readerIndex, buf3.readerIndex());
            Assert.assertEquals(readableBytes, buf3.readableBytes());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
            Assert.assertArrayEquals(bytesSource, bytes3);
            Assert.assertNotSame(bytesSource, bytes3);
            Assert.assertEquals(readerIndex, buf3.readerIndex());
            Assert.assertEquals(readableBytes, buf3.readableBytes());
        } finally {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
            Assert.assertNotSame(bytesSource, bytes3);
            Assert.assertEquals(readerIndex, buf3.readerIndex());
            Assert.assertEquals(readableBytes, buf3.readableBytes());
        } finally {
            buf3.release();
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
                               new NettyBuffer(buf4);
            byte[] bytes4 = nettyManagedBuffer4.copyToByteArray();
            Assert.assertArrayEquals(bytesSource, bytes4);
            Assert.assertNotSame(bytesSource, bytes4);
        } finally {
```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
            byte[] bytes4 = nettyManagedBuffer4.copyToByteArray();
            Assert.assertArrayEquals(bytesSource, bytes4);
            Assert.assertNotSame(bytesSource, bytes4);
        } finally {
            buf4.release();
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        nioNetworkBuffer.retain();
        nioNetworkBuffer.release();
        Assert.assertSame(nioNetworkBuffer.nioByteBuffer().array(),
                          byteBuffer.array());
        nioNetworkBuffer.release();
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        NetworkBuffer nettyNetworkBuffer = new NettyBuffer(byteBuf);
        nettyNetworkBuffer.retain();
        Assert.assertSame(cnt + 1, byteBuf.refCnt());
        Assert.assertSame(cnt + 1, nettyNetworkBuffer.referenceCount());
        ByteBuf buf = nettyNetworkBuffer.nettyByteBuf();
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        nettyNetworkBuffer.retain();
        Assert.assertSame(cnt + 1, byteBuf.refCnt());
        Assert.assertSame(cnt + 1, nettyNetworkBuffer.referenceCount());
        ByteBuf buf = nettyNetworkBuffer.nettyByteBuf();
        nettyNetworkBuffer.retain();
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        ByteBuf buf = nettyNetworkBuffer.nettyByteBuf();
        nettyNetworkBuffer.retain();
        Assert.assertSame(cnt + 2, buf.refCnt());
        Assert.assertSame(cnt + 2, nettyNetworkBuffer.referenceCount());
        nettyNetworkBuffer.release();
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/buffer/NetworkBufferTest.java`
#### Snippet
```java
        nettyNetworkBuffer.retain();
        Assert.assertSame(cnt + 2, buf.refCnt());
        Assert.assertSame(cnt + 2, nettyNetworkBuffer.referenceCount());
        nettyNetworkBuffer.release();
        nettyNetworkBuffer.release();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
            Edges edges = edgesInput.edges(idPointer);
            Iterator<Edge> edgesIt = edges.iterator();
            Assert.assertEquals(i, edges.size());
            for (int j = 0; j < edges.size(); j++) {
                Assert.assertTrue(edgesIt.hasNext());
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
            Assert.assertEquals(i, edges.size());
            for (int j = 0; j < edges.size(); j++) {
                Assert.assertTrue(edgesIt.hasNext());
                Edge edge = edgesIt.next();
                switch (freq) {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
                switch (freq) {
                    case SINGLE:
                        Assert.assertEquals(BytesId.of(j), edge.targetId());
                        break;
                    case SINGLE_PER_LABEL:
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
                        break;
                    case SINGLE_PER_LABEL:
                        Assert.assertEquals(BytesId.of(j), edge.targetId());
                        Assert.assertEquals(String.valueOf(j), edge.label());
                        break;
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
                    case SINGLE_PER_LABEL:
                        Assert.assertEquals(BytesId.of(j), edge.targetId());
                        Assert.assertEquals(String.valueOf(j), edge.label());
                        break;
                    case MULTIPLE:
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
                        break;
                    case MULTIPLE:
                        Assert.assertEquals(BytesId.of(j), edge.targetId());
                        Assert.assertEquals(String.valueOf(j), edge.label());
                        Assert.assertEquals(String.valueOf(j), edge.name());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
                    case MULTIPLE:
                        Assert.assertEquals(BytesId.of(j), edge.targetId());
                        Assert.assertEquals(String.valueOf(j), edge.label());
                        Assert.assertEquals(String.valueOf(j), edge.name());
                        break;
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
                        Assert.assertEquals(BytesId.of(j), edge.targetId());
                        Assert.assertEquals(String.valueOf(j), edge.label());
                        Assert.assertEquals(String.valueOf(j), edge.name());
                        break;
                    default:
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
                }
            }
            Assert.assertFalse(edgesIt.hasNext());
        }
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
        EdgesInput.EmptyEdges edges = EdgesInput.EmptyEdges.instance();
        Iterator<Edge> it = edges.iterator();
        Assert.assertFalse(it.hasNext());
        Assert.assertEquals(0, edges.size());
        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
        Iterator<Edge> it = edges.iterator();
        Assert.assertFalse(it.hasNext());
        Assert.assertEquals(0, edges.size());
        Assert.assertThrows(ComputerException.class, () -> {
            edges.add(graphFactory().createEdge());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    public void testListenTwice() {
        int port = this.server.listen(config, messageHandler);
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        int port = this.server.listen(config, messageHandler);
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
        Assert.assertEquals(port, this.server.port());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
        Assert.assertEquals(port, this.server.port());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
        Assert.assertEquals(port, this.server.port());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        TransportConf conf = this.server.conf();
        Assert.assertEquals("127.0.0.1",
                            conf.serverAddress().getHostAddress());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
                            conf.serverAddress().getHostAddress());

        Assert.assertEquals(9091, this.server.port());
        Assert.assertEquals(9091, port);
        String ip = this.server.bindAddress().getAddress().getHostAddress();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        Assert.assertEquals(9091, this.server.port());
        Assert.assertEquals(9091, port);
        String ip = this.server.bindAddress().getAddress().getHostAddress();
        Assert.assertEquals("127.0.0.1", ip);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertEquals(9091, port);
        String ip = this.server.bindAddress().getAddress().getHostAddress();
        Assert.assertEquals("127.0.0.1", ip);
        Assert.assertEquals(port, this.server.port());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        String ip = this.server.bindAddress().getAddress().getHostAddress();
        Assert.assertEquals("127.0.0.1", ip);
        Assert.assertEquals(port, this.server.port());
    }

```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    public void testConstructor() throws IOException {
        try (NettyTransportServer nettyServer = new NettyTransportServer()) {
            Assert.assertNotEquals(null, nettyServer);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        TransportConf conf = this.server.conf();
        Assert.assertEquals("0.0.0.0", conf.serverAddress().getHostAddress());

        Assert.assertNotEquals(0, port);
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertEquals("0.0.0.0", conf.serverAddress().getHostAddress());

        Assert.assertNotEquals(0, port);
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertTrue(this.server.bindAddress().getAddress()
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        Assert.assertNotEquals(0, port);
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertTrue(this.server.bindAddress().getAddress()
                                     .isAnyLocalAddress());
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertNotEquals(0, port);
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertTrue(this.server.bindAddress().getAddress()
                                     .isAnyLocalAddress());
        Assert.assertEquals(port, this.server.port());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertTrue(this.server.bindAddress().getAddress()
                                     .isAnyLocalAddress());
        Assert.assertEquals(port, this.server.port());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        TransportConf conf = this.server.conf();
        Assert.assertEquals("localhost", conf.serverAddress().getHostName());

        Assert.assertNotEquals(0, this.server.port());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertEquals("localhost", conf.serverAddress().getHostName());

        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
        Assert.assertEquals(port, this.server.port());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
        Assert.assertEquals(port, this.server.port());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
        Assert.assertEquals(port, this.server.port());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        TransportConf conf = this.server.conf();
        Assert.assertEquals(hostName, conf.serverAddress().getHostName());

        Assert.assertNotEquals(0, this.server.port());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertEquals(hostName, conf.serverAddress().getHostName());

        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals(ip, this.server.ip());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals(ip, this.server.ip());
        Assert.assertEquals(port, this.server.port());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals(ip, this.server.ip());
        Assert.assertEquals(port, this.server.port());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertNotEquals(0, port);
        Assert.assertEquals(ip, this.server.ip());
        Assert.assertEquals(port, this.server.port());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        if (Epoll.isAvailable()) {
            this.server.listen(config, messageHandler);
            Assert.assertEquals(IOMode.EPOLL, this.server.conf().ioMode());
        } else {
            Assert.assertThrows(UnsatisfiedLinkError.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        TransportConf conf = this.server.conf();
        Assert.assertLte(3, conf.serverThreads());
        Assert.assertEquals(IOMode.NIO, conf.ioMode());
        Assert.assertEquals("127.0.0.1",
                            conf.serverAddress().getHostAddress());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertLte(3, conf.serverThreads());
        Assert.assertEquals(IOMode.NIO, conf.ioMode());
        Assert.assertEquals("127.0.0.1",
                            conf.serverAddress().getHostAddress());

```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
                            conf.serverAddress().getHostAddress());

        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java

        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
        String hostName = this.server.bindAddress().getHostName();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertNotEquals(0, this.server.port());
        Assert.assertNotEquals(0, port);
        Assert.assertEquals("127.0.0.1", this.server.ip());
        String hostName = this.server.bindAddress().getHostName();
        Assert.assertEquals("localhost", hostName);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertEquals("127.0.0.1", this.server.ip());
        String hostName = this.server.bindAddress().getHostName();
        Assert.assertEquals("localhost", hostName);
        Assert.assertEquals(port, this.server.port());
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        String hostName = this.server.bindAddress().getHostName();
        Assert.assertEquals("localhost", hostName);
        Assert.assertEquals(port, this.server.port());
    }

```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
            );
            int port = this.server.listen(config, messageHandler);
            Assert.assertNotEquals(0, this.server.port());
            Assert.assertNotEquals(0, port);
            Assert.assertEquals(ip, this.server.ip());
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
                                       mockHeartbeatHandler);
            return null;
        }).when(clientProtocol).initializeClientPipeline(Mockito.any());

        // Skip processPingMessage()
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
                                       spyNettyServerHandler);
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());
        Mockito.doAnswer(invocationOnMock -> null)
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());
        Mockito.doAnswer(invocationOnMock -> null)
               .when(spyNettyServerHandler).processPingMessage(Mockito.any(),
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
                                                         Mockito.any());
        Mockito.doAnswer(invocationOnMock -> null)
               .when(spyNettyServerHandler).processPingMessage(Mockito.any(),
                                                               Mockito.any(),
                                                               Mockito.any());
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
        Mockito.doAnswer(invocationOnMock -> null)
               .when(spyNettyServerHandler).processPingMessage(Mockito.any(),
                                                               Mockito.any(),
                                                               Mockito.any());

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
               .when(spyNettyServerHandler).processPingMessage(Mockito.any(),
                                                               Mockito.any(),
                                                               Mockito.any());

        NettyTransportClient client = (NettyTransportClient) this.oneClient();
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
        Mockito.verify(mockHeartbeatHandler,
                       Mockito.timeout(timout).times(heartbeatTimesClose))
               .userEventTriggered(Mockito.any(),
                                   Mockito.any(IdleStateEvent.class));

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
                       Mockito.timeout(timout).times(heartbeatTimesClose))
               .userEventTriggered(Mockito.any(),
                                   Mockito.any(IdleStateEvent.class));

        Assert.assertFalse(client.active());
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
                                       spyServerIdleHandler);
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());

        HeartbeatHandler beatHandler = new HeartbeatHandler();
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
                                       mockHeartbeatHandler);
            return null;
        }).when(clientProtocol).initializeClientPipeline(Mockito.any());

        Mockito.doAnswer(invocationOnMock -> {
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
            return null;
        }).when(mockHeartbeatHandler)
          .userEventTriggered(Mockito.any(), Mockito.any(IdleStateEvent.class));

        this.oneClient();
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
            return null;
        }).when(mockHeartbeatHandler)
          .userEventTriggered(Mockito.any(), Mockito.any(IdleStateEvent.class));

        this.oneClient();
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
        long timeout = IDLE_TIMEOUT + 1000L;
        Mockito.verify(spyServerIdleHandler, Mockito.timeout(timeout).times(1))
               .userEventTriggered(Mockito.any(),
                                   Mockito.any(IdleStateEvent.class));
    }
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
        Mockito.verify(spyServerIdleHandler, Mockito.timeout(timeout).times(1))
               .userEventTriggered(Mockito.any(),
                                   Mockito.any(IdleStateEvent.class));
    }
}
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java

        embeddedChannel.writeInbound(buf);
        Assert.assertFalse(embeddedChannel.finish());
        Assert.assertNull(embeddedChannel.readInbound());
    }
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
        embeddedChannel.writeInbound(buf);
        Assert.assertFalse(embeddedChannel.finish());
        Assert.assertNull(embeddedChannel.readInbound());
    }

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java

        embeddedChannel.writeInbound(buf);
        Assert.assertFalse(embeddedChannel.finish());
        Assert.assertNull(embeddedChannel.readInbound());
    }
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
        embeddedChannel.writeInbound(buf);
        Assert.assertFalse(embeddedChannel.finish());
        Assert.assertNull(embeddedChannel.readInbound());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
                                                      requestId, partition,
                                                      managedBuffer);
            Assert.assertEquals("DataMessage[messageType=MSG," +
                                "sequenceNumber=99,partition=1,hasBody=true," +
                                "bodyLength=8]", dataMessage.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
                                "sequenceNumber=99,partition=1,hasBody=true," +
                                "bodyLength=8]", dataMessage.toString());
            Assert.assertEquals(1, managedBuffer.referenceCount());
            dataMessage.release();
            Assert.assertEquals(0, managedBuffer.referenceCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
            Assert.assertEquals(1, managedBuffer.referenceCount());
            dataMessage.release();
            Assert.assertEquals(0, managedBuffer.referenceCount());
        } finally {
            if (buf.refCnt() > 0) {
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java

        Mockito.verify(serverHandler, Mockito.timeout(2000L).times(1))
               .onChannelActive(Mockito.any());
        Mockito.verify(serverHandler, Mockito.timeout(2000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
               .onChannelActive(Mockito.any());
        Mockito.verify(serverHandler, Mockito.timeout(2000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
    }

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
               .onChannelActive(Mockito.any());
        Mockito.verify(serverHandler, Mockito.timeout(2000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
        StartMessage.INSTANCE.encode(buf);
        boolean writeInbound = embeddedChannel.writeInbound(buf);
        Assert.assertTrue(writeInbound);
        Assert.assertTrue(embeddedChannel.finish());
        buffer.release();
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
        boolean writeInbound = embeddedChannel.writeInbound(buf);
        Assert.assertTrue(writeInbound);
        Assert.assertTrue(embeddedChannel.finish());
        buffer.release();
    }
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java

        Mockito.verify(clientHandler, Mockito.timeout(3000L).times(1))
               .onChannelActive(Mockito.any());
        Mockito.verify(clientHandler, Mockito.timeout(3000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
               .onChannelActive(Mockito.any());
        Mockito.verify(clientHandler, Mockito.timeout(3000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
        Mockito.verify(spyListener, Mockito.timeout(3000L).times(1))
               .onFailure(Mockito.any(), Mockito.any());
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
               .onChannelActive(Mockito.any());
        Mockito.verify(clientHandler, Mockito.timeout(3000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
        Mockito.verify(spyListener, Mockito.timeout(3000L).times(1))
               .onFailure(Mockito.any(), Mockito.any());
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
               .exceptionCaught(Mockito.any(), Mockito.any());
        Mockito.verify(spyListener, Mockito.timeout(3000L).times(1))
               .onFailure(Mockito.any(), Mockito.any());
    }

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
               .exceptionCaught(Mockito.any(), Mockito.any());
        Mockito.verify(spyListener, Mockito.timeout(3000L).times(1))
               .onFailure(Mockito.any(), Mockito.any());
    }

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
            channel.writeAndFlush(new MockUnDecodeMessage());
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());

        this.oneClient();
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java

        Mockito.verify(clientHandler, Mockito.timeout(15_000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
    }

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java

        Mockito.verify(clientHandler, Mockito.timeout(15_000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
    }

```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
            int port = this.server.listen(config, messageHandler);
            Assert.assertNotEquals(0, this.server.port());
            Assert.assertNotEquals(0, port);
            Assert.assertEquals(ip, this.server.ip());
            Assert.assertEquals(port, this.server.port());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
            Assert.assertNotEquals(0, this.server.port());
            Assert.assertNotEquals(0, port);
            Assert.assertEquals(ip, this.server.ip());
            Assert.assertEquals(port, this.server.port());
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
            Assert.assertNotEquals(0, port);
            Assert.assertEquals(ip, this.server.ip());
            Assert.assertEquals(port, this.server.port());
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
        PropertiesCombiner combiner = new MergeNewPropertiesCombiner();
        combiner.combine(properties1, properties2, properties);
        Assert.assertEquals(expect, properties);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, properties2, properties2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
            combiner.combine(properties1, null, properties2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
            combiner.combine(properties1, properties2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(sum, value, sum);
        }
        Assert.assertEquals(55.0D, sum.value(), 0.0D);
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        TransportClient client = connectionManager.getOrCreateClient(
                                 connectionId);
        Assert.assertTrue(client.active());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
    public void testGetServer() {
        TransportServer server = connectionManager.getServer();
        Assert.assertTrue(server.bound());
        Assert.assertEquals(port, server.port());
        Assert.assertNotEquals(0, server.port());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        TransportServer server = connectionManager.getServer();
        Assert.assertTrue(server.bound());
        Assert.assertEquals(port, server.port());
        Assert.assertNotEquals(0, server.port());
    }
```

### StaticCallOnSubclass
Static method `assertNotEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        Assert.assertTrue(server.bound());
        Assert.assertEquals(port, server.port());
        Assert.assertNotEquals(0, server.port());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        TransportClient client = connectionManager.getOrCreateClient(
                                 connectionId);
        Assert.assertTrue(client.active());
        connectionManager.shutdownClients();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        TransportClient client = connectionManager.getOrCreateClient(
                                 connectionId);
        Assert.assertTrue(client.active());
        connectionManager.closeClient(client.connectionId());
        Assert.assertFalse(client.active());
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        Assert.assertTrue(client.active());
        connectionManager.closeClient(client.connectionId());
        Assert.assertFalse(client.active());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        TransportClient client = connectionManager.getOrCreateClient(
                                 "127.0.0.1", port);
        Assert.assertTrue(client.active());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        ConnectionId connectionId = ConnectionId.parseConnectionId(host, port);
        InetSocketAddress address = client.remoteAddress();
        Assert.assertEquals(connectionId.socketAddress(), address);
    }

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        NettyTransportClient client = (NettyTransportClient) this.oneClient();

        Assert.assertFalse(client.sessionActive());

        CompletableFuture<Void> future = client.startSessionAsync();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        CompletableFuture<Void> future = client.startSessionAsync();
        Assert.assertFalse(client.sessionActive());

        future.get(5, TimeUnit.SECONDS);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        future.get(5, TimeUnit.SECONDS);
        Assert.assertTrue(client.sessionActive());

        CompletableFuture<Void> finishFuture = client.finishSessionAsync();
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        CompletableFuture<Void> finishFuture = client.finishSessionAsync();
        Assert.assertTrue(client.sessionActive());

        finishFuture.get(5, TimeUnit.SECONDS);
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        finishFuture.get(5, TimeUnit.SECONDS);
        Assert.assertFalse(client.sessionActive());

        client.close();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        client.close();
        Assert.assertFalse(client.sessionActive());
    }
}
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        Mockito.doThrow(new RuntimeException("test exception"))
               .when(sendFunc)
               .apply(Mockito.any());

        Assert.assertThrows(RuntimeException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        NettyTransportClient client = (NettyTransportClient) this.oneClient();
        Channel channel = client.channel();
        Assert.assertTrue(channel.isActive());
    }

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Mockito.doThrow(new RuntimeException("test exception")).when(serverHandler)
               .handle(Mockito.any(), Mockito.anyInt(), Mockito.any());

        ByteBuffer buffer = ByteBuffer.wrap(StringEncoding.encode("test data"));
```

### StaticCallOnSubclass
Static method `anyInt()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Mockito.doThrow(new RuntimeException("test exception")).when(serverHandler)
               .handle(Mockito.any(), Mockito.anyInt(), Mockito.any());

        ByteBuffer buffer = ByteBuffer.wrap(StringEncoding.encode("test data"));
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Mockito.doThrow(new RuntimeException("test exception")).when(serverHandler)
               .handle(Mockito.any(), Mockito.anyInt(), Mockito.any());

        ByteBuffer buffer = ByteBuffer.wrap(StringEncoding.encode("test data"));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        ByteBuffer buffer = ByteBuffer.wrap(StringEncoding.encode("test data"));
        boolean send = client.send(MessageType.MSG, 1, buffer);
        Assert.assertTrue(send);

        Whitebox.setInternalState(client, "timeoutFinishSession",
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Mockito.verify(serverHandler, Mockito.timeout(10_000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
    }

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Mockito.verify(serverHandler, Mockito.timeout(10_000L).times(1))
               .exceptionCaught(Mockito.any(), Mockito.any());
    }

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        Mockito.doThrow(new RuntimeException("test exception"))
               .when(sendFunc)
               .apply(Mockito.any());

        Assert.assertThrows(RuntimeException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        ConnectionId connectionId = ConnectionId.parseConnectionId(host, port);
        ConnectionId clientConnectionId = client.connectionId();
        Assert.assertEquals(connectionId, clientConnectionId);
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
            boolean send = client.send(MessageType.MSG, 1, buffer);
            if (i <= conf.maxPendingRequests()) {
                Assert.assertTrue(send);
            } else {
                Assert.assertFalse(send);
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
                Assert.assertTrue(send);
            } else {
                Assert.assertFalse(send);
            }
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        int maxAckId = Whitebox.getInternalState(client.clientSession(),
                                                 "maxAckId");
        Assert.assertEquals(conf.maxPendingRequests(), maxRequestId);
        Assert.assertEquals(0, maxAckId);

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
                                                 "maxAckId");
        Assert.assertEquals(conf.maxPendingRequests(), maxRequestId);
        Assert.assertEquals(0, maxAckId);

        int pendings = conf.maxPendingRequests() - conf.minPendingRequests();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        int pendings = conf.maxPendingRequests() - conf.minPendingRequests();
        for (int i = 1; i <= pendings + 1; i++) {
            Assert.assertFalse(client.checkSendAvailable());
            client.clientSession().onRecvAck(i);
        }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
            client.clientSession().onRecvAck(i);
        }
        Assert.assertTrue(client.checkSendAvailable());

        maxAckId = Whitebox.getInternalState(client.clientSession(),
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        maxAckId = Whitebox.getInternalState(client.clientSession(),
                                             "maxAckId");
        Assert.assertEquals(pendings + 1, maxAckId);

        Whitebox.setInternalState(client.clientSession(), "sendFunction",
```

### StaticCallOnSubclass
Static method `assertArrayEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
            }

            Assert.assertArrayEquals(sourceBytes, bytes);
            Assert.assertNotSame(sourceBytes, bytes);

```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

            Assert.assertArrayEquals(sourceBytes, bytes);
            Assert.assertNotSame(sourceBytes, bytes);

            return null;
```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

            return null;
        }).when(serverHandler).handle(Mockito.any(), Mockito.eq(1),
                                      Mockito.any());

```

### StaticCallOnSubclass
Static method `eq()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

            return null;
        }).when(serverHandler).handle(Mockito.any(), Mockito.eq(1),
                                      Mockito.any());

```

### StaticCallOnSubclass
Static method `any()` declared in class 'org.mockito.ArgumentMatchers' but referenced via subclass 'org.mockito.Mockito'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
            return null;
        }).when(serverHandler).handle(Mockito.any(), Mockito.eq(1),
                                      Mockito.any());

        client.startSession();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
        LongValue value2 = new LongValue(2L);
        combiner.combine(value2, max, max);
        Assert.assertEquals(new LongValue(2L), max);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMinCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMinCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, null, value1);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMinCombinerTest.java`
#### Snippet
```java
        LongValue value2 = new LongValue(2L);
        combiner.combine(value2, min, min);
        Assert.assertEquals(new LongValue(0L), min);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(sum, value, sum);
        }
        Assert.assertEquals(55.0D, sum.value(), 0.0D);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
        OverwriteCombiner<LongValue> combiner = new OverwriteCombiner<>();
        combiner.combine(value1, value2, value2);
        Assert.assertEquals(value2, value2);
        combiner.combine(value1, value2, value1);
        Assert.assertEquals(value2, value1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
        Assert.assertEquals(value2, value2);
        combiner.combine(value1, value2, value1);
        Assert.assertEquals(value2, value1);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
            combiner.combine(null, value2,  value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/PointerCombinerTest.java`
#### Snippet
```java
            combinedValue.read(input);
            Map<String, Value> map = combinedValue.get();
            Assert.assertEquals(2, map.size());
            Assert.assertEquals(new LongValue(1L), map.get("p1"));
            Assert.assertEquals(new LongValue(2L), map.get("p2"));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/PointerCombinerTest.java`
#### Snippet
```java
            Map<String, Value> map = combinedValue.get();
            Assert.assertEquals(2, map.size());
            Assert.assertEquals(new LongValue(1L), map.get("p1"));
            Assert.assertEquals(new LongValue(2L), map.get("p2"));
        }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/PointerCombinerTest.java`
#### Snippet
```java
            Assert.assertEquals(2, map.size());
            Assert.assertEquals(new LongValue(1L), map.get("p1"));
            Assert.assertEquals(new LongValue(2L), map.get("p2"));
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
        Map<Integer, PeekableIterator<KvEntry>> edgePartitions =
                     this.receiveManager.edgePartitions();
        Assert.assertEquals(1, vertexPartitions.size());
        Assert.assertEquals(1, edgePartitions.size());
        VertexMessageRecvPartitionTest.checkPartitionIterator(
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
                     this.receiveManager.edgePartitions();
        Assert.assertEquals(1, vertexPartitions.size());
        Assert.assertEquals(1, edgePartitions.size());
        VertexMessageRecvPartitionTest.checkPartitionIterator(
                                       vertexPartitions.get(0));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
        Map<Integer, PeekableIterator<KvEntry>> messagePartitions =
        this.receiveManager.messagePartitions();
        Assert.assertEquals(1, messagePartitions.size());
        ComputeMessageRecvPartitionTest.checkTenCombineMessages(
                                        messagePartitions.get(0));
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
            });
        }, e -> {
            Assert.assertEquals("Unable handle NetworkBuffer with type 'ACK'",
                                e.getMessage());
        });
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/PointerCombinerTest.java`
#### Snippet
```java
            DoubleValue combinedValue = new DoubleValue();
            combinedValue.read(input);
            Assert.assertEquals(new DoubleValue(3.0D), combinedValue);
        }
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        sortThread.interrupt();
        countDownLatch.await();
        Assert.assertTrue(success.get());
    }

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        }

        Assert.assertFalse(buffers.full());
        Assert.assertEquals(1000L, buffers.totalBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java

        Assert.assertFalse(buffers.full());
        Assert.assertEquals(1000L, buffers.totalBytes());

        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java

        addMockBufferToBuffers(buffers, size);
        Assert.assertTrue(buffers.full());

        List<RandomAccessInput> list = buffers.buffers();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java

        List<RandomAccessInput> list = buffers.buffers();
        Assert.assertEquals(11, list.size());

        buffers.signalSorted();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        buffers.signalSorted();
        List<RandomAccessInput> list2 = buffers.buffers();
        Assert.assertEquals(11, list2.size());
        Assert.assertEquals(1100L, buffers.totalBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        List<RandomAccessInput> list2 = buffers.buffers();
        Assert.assertEquals(11, list2.size());
        Assert.assertEquals(1100L, buffers.totalBytes());

        // It's ok to call waitSorted multi-times
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        buffers.prepareSort();
        List<RandomAccessInput> list3 = buffers.buffers();
        Assert.assertEquals(0, list3.size());
        Assert.assertEquals(0L, buffers.totalBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        List<RandomAccessInput> list3 = buffers.buffers();
        Assert.assertEquals(0, list3.size());
        Assert.assertEquals(0L, buffers.totalBytes());

        buffers.waitSorted();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        }

        Assert.assertEquals(1000L, buffers.totalBytes());
        Assert.assertFalse(buffers.full());

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java

        Assert.assertEquals(1000L, buffers.totalBytes());
        Assert.assertFalse(buffers.full());

        Assert.assertThrows(ComputerException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        addMockBufferToBuffers(buffers, size);

        Assert.assertTrue(buffers.full());

        List<RandomAccessInput> list4 = buffers.buffers();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java

        List<RandomAccessInput> list4 = buffers.buffers();
        Assert.assertEquals(11, list4.size());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

        this.syncStartWithAutoComplete(executorService, clientSession);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        pool.schedule(() -> {
            Assert.assertEquals(TransportState.START_SENT,
                                clientSession.state());
            try {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        clientSession.start(conf.timeoutSyncRequest());

        Assert.assertEquals(TransportState.ESTABLISHED,
                            clientSession.state());
        Assert.assertEquals(AbstractMessage.START_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(TransportState.ESTABLISHED,
                            clientSession.state());
        Assert.assertEquals(AbstractMessage.START_SEQ,
                            clientSession.maxRequestId);
        Assert.assertEquals(AbstractMessage.START_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.START_SEQ,
                            clientSession.maxRequestId);
        Assert.assertEquals(AbstractMessage.START_SEQ,
                            clientSession.maxAckId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.START_SEQ,
                            clientSession.maxAckId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.finishId);

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
                            clientSession.finishId);

        Assert.assertFalse(this.existError(exceptions));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        pool.schedule(() -> {
            Assert.assertEquals(TransportState.FINISH_SENT,
                                clientSession.state());
            try {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        clientSession.finish(conf.timeoutFinishSession());

        Assert.assertEquals(TransportState.READY,
                            clientSession.state());
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(TransportState.READY,
                            clientSession.state());
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.finishId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.finishId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.maxAckId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.maxAckId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.maxRequestId);
        Assert.assertFalse(clientSession.flowBlocking());
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.maxRequestId);
        Assert.assertFalse(clientSession.flowBlocking());

        Assert.assertFalse(this.existError(exceptions));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertFalse(clientSession.flowBlocking());

        Assert.assertFalse(this.existError(exceptions));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
    public void testServerSessionWithException() {
        ServerSession serverSession = new ServerSession(conf);
        Assert.assertEquals(TransportState.READY, serverSession.state());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
    public void testConstruct() {
        ServerSession serverSession = new ServerSession(conf);
        Assert.assertEquals(TransportState.READY,
                            serverSession.state());
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(TransportState.READY,
                            serverSession.state());
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            serverSession.maxRequestId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            serverSession.maxRequestId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            serverSession.maxAckId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            serverSession.maxAckId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            serverSession.finishId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY,
                            clientSession.state());
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(TransportState.READY,
                            clientSession.state());
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.maxRequestId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.maxRequestId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.maxAckId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.maxAckId);
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.finishId);
        Assert.assertFalse(clientSession.flowBlocking());
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(AbstractMessage.UNKNOWN_SEQ,
                            clientSession.finishId);
        Assert.assertFalse(clientSession.flowBlocking());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
    public void testCheckFinishReady() {
        ServerSession serverSession = new ServerSession(conf);
        Assert.assertEquals(TransportState.READY, serverSession.state());

        serverSession.onRecvStateStart();
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
                                              "needAckFinish",
                                              serverSession);
        Assert.assertFalse(finishRead2);

        serverSession.onRecvStateFinish(1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
    public void testServerSession() {
        ServerSession serverSession = new ServerSession(conf);
        Assert.assertEquals(TransportState.READY, serverSession.state());

        serverSession.onRecvStateStart();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        serverSession.onRecvStateStart();
        Assert.assertEquals(TransportState.START_RECV, serverSession.state());

        serverSession.completeStateStart();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        serverSession.completeStateStart();
        Assert.assertEquals(TransportState.ESTABLISHED, serverSession.state());
        Assert.assertEquals(AbstractMessage.START_SEQ, serverSession.maxAckId);
        Assert.assertEquals(AbstractMessage.START_SEQ,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        serverSession.completeStateStart();
        Assert.assertEquals(TransportState.ESTABLISHED, serverSession.state());
        Assert.assertEquals(AbstractMessage.START_SEQ, serverSession.maxAckId);
        Assert.assertEquals(AbstractMessage.START_SEQ,
                            Whitebox.getInternalState(serverSession,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        for (int i = 1; i < 100 ; i++) {
            serverSession.onRecvData(i);
            Assert.assertEquals(i, serverSession.maxRequestId);
            serverSession.onHandledData(i);
            Assert.assertEquals(i, Whitebox.getInternalState(serverSession,
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
                                                             "maxHandledId"));
            serverSession.onDataAckSent(i);
            Assert.assertEquals(i, serverSession.maxAckId);
        }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        serverSession.onRecvStateFinish(100);
        Assert.assertEquals(TransportState.FINISH_RECV,
                            serverSession.state());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        serverSession.completeStateFinish();
        Assert.assertEquals(TransportState.READY, serverSession.state());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

        this.syncStartWithAutoComplete(executorService, clientSession);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

        this.syncStartWithAutoComplete(executorService, clientSession);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

        this.syncStartWithAutoComplete(executorService, clientSession);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
    public void testSendAsyncWithException() {
        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

        ByteBuffer buffer = ByteBuffer.wrap(StringEncoding.encode("test data"));
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        thread1.start();
        thread1.join();
        Assert.assertSame(object, recycler.get());
    }

```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java

        RecyclableObject object2 = recycler.get();
        Assert.assertSame(object1, object2);
        object2.handle.recycle(object2);
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        int threadLocalCapacity = (Integer) threadLocalCapacityMethod.invoke(
                                            recycler);
        Assert.assertTrue(maxCapacity >= threadLocalCapacity);
        int threadLocalSize = (Integer) threadLocalSizeMethod.invoke(recycler);
        Assert.assertEquals(0, threadLocalSize);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        Assert.assertTrue(maxCapacity >= threadLocalCapacity);
        int threadLocalSize = (Integer) threadLocalSizeMethod.invoke(recycler);
        Assert.assertEquals(0, threadLocalSize);

        for (int i = 0; i < objects.length; i++) {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        threadLocalCapacity = (Integer) threadLocalCapacityMethod.invoke(
                                        recycler);
        Assert.assertTrue(maxCapacity >= threadLocalCapacity);
        threadLocalSize = (Integer) threadLocalSizeMethod.invoke(recycler);
        Assert.assertTrue(maxCapacity >= threadLocalSize);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        Assert.assertTrue(maxCapacity >= threadLocalCapacity);
        threadLocalSize = (Integer) threadLocalSizeMethod.invoke(recycler);
        Assert.assertTrue(maxCapacity >= threadLocalSize);
        Assert.assertTrue(threadLocalSize > 0);
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        threadLocalSize = (Integer) threadLocalSizeMethod.invoke(recycler);
        Assert.assertTrue(maxCapacity >= threadLocalSize);
        Assert.assertTrue(threadLocalSize > 0);
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
            object.handle.recycle(object);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("recycled already"));
        });
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
                object.handle.recycle(object);
            }, e -> {
                Assert.assertTrue(e.getMessage().contains("recycled already"));
            });
        });
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java

        RecyclerReference<Edge> reference2 = allocator.newEdge();
        Assert.assertSame(reference1, reference2);
        Assert.assertSame(reference1.get(), reference2.get());

```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        RecyclerReference<Edge> reference2 = allocator.newEdge();
        Assert.assertSame(reference1, reference2);
        Assert.assertSame(reference1.get(), reference2.get());

        RecyclerReference<Edge> reference3 = allocator.newEdge();
```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java

        RecyclerReference<Edge> reference3 = allocator.newEdge();
        Assert.assertNotSame(reference1, reference3);
        Assert.assertNotSame(reference1.get(), reference3.get());

```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        RecyclerReference<Edge> reference3 = allocator.newEdge();
        Assert.assertNotSame(reference1, reference3);
        Assert.assertNotSame(reference1.get(), reference3.get());

        allocator.freeEdge(reference2);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
                allocator.freeVertex(reference1);
            }, e -> {
                Assert.assertTrue(e.getMessage().contains("recycled already"));
            });
        });
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java

        RecyclerReference<Vertex> reference2 = allocator.newVertex();
        Assert.assertSame(reference1, reference2);
        Assert.assertSame(reference1.get(), reference2.get());
        allocator.freeVertex(reference2);
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        RecyclerReference<Vertex> reference2 = allocator.newVertex();
        Assert.assertSame(reference1, reference2);
        Assert.assertSame(reference1.get(), reference2.get());
        allocator.freeVertex(reference2);
    }
```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java

        RecyclerReference<Vertex> reference2 = allocator.newVertex();
        Assert.assertSame(reference1, reference2);
        Assert.assertSame(reference1.get(), reference2.get());

```

### StaticCallOnSubclass
Static method `assertSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        RecyclerReference<Vertex> reference2 = allocator.newVertex();
        Assert.assertSame(reference1, reference2);
        Assert.assertSame(reference1.get(), reference2.get());

        RecyclerReference<Vertex> reference3 = allocator.newVertex();
```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java

        RecyclerReference<Vertex> reference3 = allocator.newVertex();
        Assert.assertNotSame(reference1, reference3);
        Assert.assertNotSame(reference1.get(), reference3.get());

```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        RecyclerReference<Vertex> reference3 = allocator.newVertex();
        Assert.assertNotSame(reference1, reference3);
        Assert.assertNotSame(reference1.get(), reference3.get());

        allocator.freeVertex(reference2);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/dist/HugeGraphComputerTest.java`
#### Snippet
```java
        t.start();
        t.join();
        Assert.assertTrue(isRun.get());
    }
}
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/dist/HugeGraphComputerTest.java`
#### Snippet
```java
        pool.shutdownNow();

        Assert.assertFalse(Arrays.asList(exceptions).toString(),
                           this.existError(exceptions));
    }
```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
             RecyclerReference<Vertex> reference2 = allocator.newVertex()) {
            reference = reference1;
            Assert.assertNotSame(reference1, reference2);
            Assert.assertNotSame(reference1.get(), reference2.get());
        }
```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
            reference = reference1;
            Assert.assertNotSame(reference1, reference2);
            Assert.assertNotSame(reference1.get(), reference2.get());
        }
        Assert.assertThrows(IllegalStateException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
            allocator.freeVertex(reference);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("recycled already"));
        });
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
            allocator.freeVertex(reference1);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("recycled already"));
        });
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
    public static void checkTenCombineMessages(PeekableIterator<KvEntry> it)
                                               throws IOException {
        Assert.assertTrue(it.hasNext());
        KvEntry lastEntry = it.next();
        Id lastId = ReceiverUtil.readId(lastEntry.key());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
                lastSumValue.value(lastSumValue.value() + currentValue.value());
            } else {
                Assert.assertEquals((Long) lastId.asObject() * 2.0D,
                                    lastSumValue.value(), 0.0D);
            }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
            }
        }
        Assert.assertEquals((Long) lastId.asObject() * 2.0D,
                            lastSumValue.value(), 0.0D);
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
                                                context(), fileGenerator,
                                                sortManager);
        Assert.assertEquals("msg", partition.type());

        addTwentyCombineMessageBuffer(partition::addBuffer);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
        for (long i = 0L; i < 10L; i++) {
            for (int j = 0; j < 2; j++) {
                Assert.assertTrue(it.hasNext());
                KvEntry currentEntry = it.next();
                Id currentId = ReceiverUtil.readId(currentEntry.key());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
                Id currentId = ReceiverUtil.readId(currentEntry.key());
                Id expectId = BytesId.of(i);
                Assert.assertEquals(expectId, currentId);
                IdList expectMessage = new IdList();
                expectMessage.add(expectId);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
                IdList currentValue = new IdList();
                ReceiverUtil.readValue(currentEntry.value(), currentValue);
                Assert.assertEquals(expectMessage, currentValue);
            }
        }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
            }
        }
        Assert.assertFalse(it.hasNext());
    }
}
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
                                                context(), fileGenerator,
                                                sortManager);
        Assert.assertEquals("msg", partition.type());

        addTwentyDuplicateIdValueListMessageBuffer(partition::addBuffer);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/ComputerOptionsTest.java`
#### Snippet
```java
            String key = typedOption.name();
            if (options.containsKey(key)) {
                Assert.assertEquals(value instanceof Class ? ((Class<?>) value).getName() :
                                    String.valueOf(value), options.get(key));
            } else {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/ComputerOptionsTest.java`
#### Snippet
```java
                                    String.valueOf(value), options.get(key));
            } else {
                Assert.assertEquals(value, typedOption.defaultValue());
            }
        }
```

### StaticCallOnSubclass
Static method `assertNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/ComputerOptionsTest.java`
#### Snippet
```java
        });

        Assert.assertNull(option.parseConvert(null));
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java

    public static void assertEquals(double v1, double v2) {
        Assert.assertEquals(v1, v2, 1E-6);
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
        try (BytesInput bai = IOFactory.createBytesInput(bytes)) {
            newId.read(bai);
            Assert.assertEquals(oldId, newId);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
        try (BytesInput bai = IOFactory.createBytesInput(bytes)) {
            readObj.read(bai);
            Assert.assertEquals(writeObj, readObj);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
        try (BytesInput bai = IOFactory.createBytesInput(bytes)) {
            newValue.read(bai);
            Assert.assertEquals(oldValue, newValue);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        SuperstepStat superstepStat1 = JsonUtil.fromJson(json,
                                                         SuperstepStat.class);
        Assert.assertEquals(superstepStat, superstepStat1);

        Assert.assertThrows(RuntimeException.class, () -> {
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        jobState.lastSuperstepStat(superstepStat);

        Assert.assertEquals(JobStatus.INITIALIZING, jobState.jobStatus());
        Assert.assertEquals(3, jobState.superstep());
        Assert.assertEquals(99, jobState.maxSuperstep());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java

        Assert.assertEquals(JobStatus.INITIALIZING, jobState.jobStatus());
        Assert.assertEquals(3, jobState.superstep());
        Assert.assertEquals(99, jobState.maxSuperstep());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertEquals(JobStatus.INITIALIZING, jobState.jobStatus());
        Assert.assertEquals(3, jobState.superstep());
        Assert.assertEquals(99, jobState.maxSuperstep());

        Assert.assertEquals(1, superstepStat.vertexCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertEquals(99, jobState.maxSuperstep());

        Assert.assertEquals(1, superstepStat.vertexCount());
        Assert.assertEquals(1, superstepStat.edgeCount());
        Assert.assertEquals(1, superstepStat.finishedVertexCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java

        Assert.assertEquals(1, superstepStat.vertexCount());
        Assert.assertEquals(1, superstepStat.edgeCount());
        Assert.assertEquals(1, superstepStat.finishedVertexCount());
        Assert.assertEquals(1, superstepStat.messageCount());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertEquals(1, superstepStat.vertexCount());
        Assert.assertEquals(1, superstepStat.edgeCount());
        Assert.assertEquals(1, superstepStat.finishedVertexCount());
        Assert.assertEquals(1, superstepStat.messageCount());
        Assert.assertTrue(superstepStat.active());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertEquals(1, superstepStat.edgeCount());
        Assert.assertEquals(1, superstepStat.finishedVertexCount());
        Assert.assertEquals(1, superstepStat.messageCount());
        Assert.assertTrue(superstepStat.active());
        Assert.assertEquals(1, superstepStat.messageBytes());
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertEquals(1, superstepStat.finishedVertexCount());
        Assert.assertEquals(1, superstepStat.messageCount());
        Assert.assertTrue(superstepStat.active());
        Assert.assertEquals(1, superstepStat.messageBytes());

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertEquals(1, superstepStat.messageCount());
        Assert.assertTrue(superstepStat.active());
        Assert.assertEquals(1, superstepStat.messageBytes());

        SuperstepStat superstepStat2 = new SuperstepStat();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        superstepStat2.active(true);
        superstepStat2.messageBytes(1);
        Assert.assertEquals(superstepStat, superstepStat2);
        Assert.assertEquals(superstepStat.hashCode(),
                            superstepStat2.hashCode());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        superstepStat2.messageBytes(1);
        Assert.assertEquals(superstepStat, superstepStat2);
        Assert.assertEquals(superstepStat.hashCode(),
                            superstepStat2.hashCode());
    }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
    @Test
    public void testJobStatus() {
        Assert.assertFalse(JobStatus.finished(JobStatus.INITIALIZING));
        Assert.assertFalse(JobStatus.finished(JobStatus.RUNNING));
        Assert.assertTrue(JobStatus.finished(JobStatus.FAILED));
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
    public void testJobStatus() {
        Assert.assertFalse(JobStatus.finished(JobStatus.INITIALIZING));
        Assert.assertFalse(JobStatus.finished(JobStatus.RUNNING));
        Assert.assertTrue(JobStatus.finished(JobStatus.FAILED));
        Assert.assertTrue(JobStatus.finished(JobStatus.SUCCEEDED));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertFalse(JobStatus.finished(JobStatus.INITIALIZING));
        Assert.assertFalse(JobStatus.finished(JobStatus.RUNNING));
        Assert.assertTrue(JobStatus.finished(JobStatus.FAILED));
        Assert.assertTrue(JobStatus.finished(JobStatus.SUCCEEDED));
        Assert.assertTrue(JobStatus.finished(JobStatus.CANCELLED));
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertFalse(JobStatus.finished(JobStatus.RUNNING));
        Assert.assertTrue(JobStatus.finished(JobStatus.FAILED));
        Assert.assertTrue(JobStatus.finished(JobStatus.SUCCEEDED));
        Assert.assertTrue(JobStatus.finished(JobStatus.CANCELLED));
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertTrue(JobStatus.finished(JobStatus.FAILED));
        Assert.assertTrue(JobStatus.finished(JobStatus.SUCCEEDED));
        Assert.assertTrue(JobStatus.finished(JobStatus.CANCELLED));
    }

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/AlgorithmTestBase.java`
#### Snippet
```java
        pool.shutdownNow();

        Assert.assertFalse(Arrays.asList(exceptions).toString(),
                           existError(exceptions));
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficientTest.java`
#### Snippet
```java

        ClusteringCoefficientValue copy = value.copy();
        Assert.assertEquals(10, copy.count());
        Assert.assertNotSame(value.idSet(), copy.idSet());

```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficientTest.java`
#### Snippet
```java
        ClusteringCoefficientValue copy = value.copy();
        Assert.assertEquals(10, copy.count());
        Assert.assertNotSame(value.idSet(), copy.idSet());

        Assert.assertContains("10", value.toString());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficientTest.java`
#### Snippet
```java

            if (expected != null) {
                Assert.assertEquals(expected, result.property(super.name()));
            }
            return result;
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/trianglecount/TriangleCountTest.java`
#### Snippet
```java
            Integer expected = EXPECTED_RESULTS.get(vertex.id().string());
            if (expected != null) {
                Assert.assertEquals(expected, value);
            }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/trianglecount/TriangleCountTest.java`
#### Snippet
```java

        TriangleCountValue copy = value.copy();
        Assert.assertEquals(10, copy.count());
        Assert.assertNotSame(value.idSet(), copy.idSet());

```

### StaticCallOnSubclass
Static method `assertNotSame()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/trianglecount/TriangleCountTest.java`
#### Snippet
```java
        TriangleCountValue copy = value.copy();
        Assert.assertEquals(10, copy.count());
        Assert.assertNotSame(value.idSet(), copy.idSet());

        Assert.assertContains("10", value.toString());
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
    public void testEmptyIterator() {
        PeekableIterator<KvEntry> it = this.partition.iterator();
        Assert.assertFalse(it.hasNext());
    }

```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
                                              throws IOException {
        for (long i = 0L; i < 10L; i++) {
            Assert.assertTrue(it.hasNext());
            KvEntry entry = it.next();
            Id id = ReceiverUtil.readId(entry.key());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
            KvEntry entry = it.next();
            Id id = ReceiverUtil.readId(entry.key());
            Assert.assertEquals(BytesId.of(i), id);
        }
    }
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
        for (long i = 0L; i < 10L; i++) {
            // Assert key
            Assert.assertTrue(it.hasNext());
            KvEntry entry = it.next();
            Id id = ReceiverUtil.readId(entry.key());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
            KvEntry entry = it.next();
            Id id = ReceiverUtil.readId(entry.key());
            Assert.assertEquals(BytesId.of(i), id);

            // Assert value
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
            input.seek(value.offset());
            String label = StreamGraphInput.readLabel(input);
            Assert.assertEquals("", label);
            Properties properties = graphFactory().createProperties();
            properties.read(input);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
            input.seek(position);

            Assert.assertEquals(2, properties.size());
            LongValue v1 = properties.get("p1");
            Assert.assertEquals(new LongValue(i), v1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
            Assert.assertEquals(2, properties.size());
            LongValue v1 = properties.get("p1");
            Assert.assertEquals(new LongValue(i), v1);
            LongValue v2 = properties.get("p2");
            Assert.assertEquals(new LongValue(2L * i), v2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
            Assert.assertEquals(new LongValue(i), v1);
            LongValue v2 = properties.get("p2");
            Assert.assertEquals(new LongValue(2L * i), v2);
        }
    }
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
    @Test
    public void testVertexMessageRecvPartition() throws IOException {
        Assert.assertEquals("vertex", this.partition.type());
        Assert.assertEquals(0L, this.partition.totalBytes());
        addTenVertexBuffer(this.partition::addBuffer);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
    public void testVertexMessageRecvPartition() throws IOException {
        Assert.assertEquals("vertex", this.partition.type());
        Assert.assertEquals(0L, this.partition.totalBytes());
        addTenVertexBuffer(this.partition::addBuffer);

```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
        PeekableIterator<KvEntry> it = this.partition.iterator();
        checkPartitionIterator(it);
        Assert.assertFalse(it.hasNext());
    }

```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/betweenness/BetweennessCentralityTest.java`
#### Snippet
```java
            Double result = super.value(vertex);
            Double expect = EXPECT_RESULTS.get(vertex.id().string());
            Assert.assertNotNull(expect);
            assertEquals(expect, result);
            return result;
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
    @Test
    public void testEdgeMessageRecvPartition() throws IOException {
        Assert.assertEquals("edge", this.partition.type());

        addTenEdgeBuffer((NetworkBuffer buffer) -> {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
                        throws IOException {
        for (long i = 0L; i < 10L; i++) {
            Assert.assertTrue(it.hasNext());
            KvEntry entry = it.next();
            Id id = ReceiverUtil.readId(entry.key());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            KvEntry entry = it.next();
            Id id = ReceiverUtil.readId(entry.key());
            Assert.assertEquals(BytesId.of(i), id);
            EntryIterator subKvIt = EntriesUtil.subKvIterFromEntry(entry);
            for (long j = i + 1; j < i + 3; j++) {
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            EntryIterator subKvIt = EntriesUtil.subKvIterFromEntry(entry);
            for (long j = i + 1; j < i + 3; j++) {
                Assert.assertTrue(subKvIt.hasNext());
                KvEntry subKv = subKvIt.next();
                Id targetId = ReceiverUtil.readId(subKv.key());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
                KvEntry subKv = subKvIt.next();
                Id targetId = ReceiverUtil.readId(subKv.key());
                Assert.assertEquals(BytesId.of(j), targetId);
                Properties properties = graphFactory().createProperties();

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java

                ReceiverUtil.readValue(subKv.value(), properties);
                Assert.assertEquals(2, properties.size());
                LongValue v1 = properties.get("p1");
                Assert.assertEquals(new LongValue(i), v1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
                Assert.assertEquals(2, properties.size());
                LongValue v1 = properties.get("p1");
                Assert.assertEquals(new LongValue(i), v1);
                LongValue v2 = properties.get("p2");
                Assert.assertEquals(new LongValue(2L * i), v2);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
                Assert.assertEquals(new LongValue(i), v1);
                LongValue v2 = properties.get("p2");
                Assert.assertEquals(new LongValue(2L * i), v2);
            }
        }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            }
        }
        Assert.assertFalse(it.hasNext());
    }

```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
    @Test
    public void testOverwriteCombiner() throws IOException {
        Assert.assertEquals("edge", this.partition.type());

        addTenEdgeBuffer(this.partition::addBuffer);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
        this.partition = new EdgeMessageRecvPartition(context(), fileGenerator,
                                                      this.sortManager);
        Assert.assertEquals("edge", this.partition.type());

        addTenDuplicateEdgeBuffer(this.partition::addBuffer);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
                                     throws IOException {
        for (long i = 0L; i < 10L; i++) {
            Assert.assertTrue(it.hasNext());
            KvEntry entry = it.next();
            Id id = ReceiverUtil.readId(entry.key());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            KvEntry entry = it.next();
            Id id = ReceiverUtil.readId(entry.key());
            Assert.assertEquals(BytesId.of(i), id);

            EntryIterator subKvIt = EntriesUtil.subKvIterFromEntry(entry);
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            EntryIterator subKvIt = EntriesUtil.subKvIterFromEntry(entry);
            for (long j = i + 1; j < i + 3; j++) {
                Assert.assertTrue(subKvIt.hasNext());
                KvEntry subKv = subKvIt.next();
                Id targetId = ReceiverUtil.readId(subKv.key());
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
                KvEntry subKv = subKvIt.next();
                Id targetId = ReceiverUtil.readId(subKv.key());
                Assert.assertEquals(BytesId.of(j), targetId);

                Properties properties = graphFactory().createProperties();
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
                Properties properties = graphFactory().createProperties();
                ReceiverUtil.readValue(subKv.value(), properties);
                Assert.assertEquals(1, properties.size());
                LongValue v1 = properties.get("p1");
                Assert.assertEquals(new LongValue(i), v1);
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
                Assert.assertEquals(1, properties.size());
                LongValue v1 = properties.get("p1");
                Assert.assertEquals(new LongValue(i), v1);
            }
        }
```

### StaticCallOnSubclass
Static method `assertFalse()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            }
        }
        Assert.assertFalse(it.hasNext());
    }

```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/closeness/ClosenessCentralityTest.java`
#### Snippet
```java
            Double result = super.value(vertex);
            Double expect = expectResults.get(vertex.id().string());
            Assert.assertNotNull(expect);
            assertEquals(expect, result);
            return result;
```

### StaticCallOnSubclass
Static method `assertNotNull()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/closeness/ClosenessCentralityTest.java`
#### Snippet
```java
            Double result = super.value(vertex);
            Double expect = expectResults.get(vertex.id().string());
            Assert.assertNotNull(expect);
            assertEquals(expect, result);
            return result;
```

### StaticCallOnSubclass
Static method `assertEquals()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/RingsDetectionTest.java`
#### Snippet
```java
                                                           new HashSet<>());

            Assert.assertEquals(expect.size(), rings.size());
            for (String ring : rings) {
                String error = "Expect: '" + ring + "' in " + expect;
```

### StaticCallOnSubclass
Static method `assertTrue()` declared in class 'org.junit.Assert' but referenced via subclass 'org.apache.hugegraph.testutil.Assert'
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/RingsDetectionTest.java`
#### Snippet
```java
            for (String ring : rings) {
                String error = "Expect: '" + ring + "' in " + expect;
                Assert.assertTrue(error, expect.contains(ring));
            }
        }
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`while` statement has empty body
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/Consumers.java`
#### Snippet
```java
        }
        assert this.ending;
        while (this.consume()) {

        }
```

## RuleId[id=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/ComputationContext.java`
#### Snippet
```java
    default void sendMessageToAllEdges(Vertex vertex, Value value) {
        Iterator<Edge> edges = vertex.edges().iterator();
        while (edges.hasNext()) {
            Edge edge = edges.next();
            this.sendMessage(edge.targetId(), value);
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/ComputationContext.java`
#### Snippet
```java
            M value = values.next();
            Iterator<Edge> edges = vertex.edges().iterator();
            while (edges.hasNext()) {
                Edge edge = edges.next();
                if (filter.apply(value, edge.targetId())) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/ComputationContext.java`
#### Snippet
```java
                                   BiFunction<M, Id, Boolean> filter) {
        Iterator<Edge> edges = vertex.edges().iterator();
        while (edges.hasNext()) {
            Edge edge = edges.next();
            if (filter.apply(value, edge.targetId())) {
```

## RuleId[id=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        }

        config = UnitTestBase.updateWithRequiredOptions(objects);
        conf = TransportConf.wrapConfig(config);
        serverHandler = Mockito.spy(new MockMessageHandler());
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `conf` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java

        config = UnitTestBase.updateWithRequiredOptions(objects);
        conf = TransportConf.wrapConfig(config);
        serverHandler = Mockito.spy(new MockMessageHandler());
        clientHandler = Mockito.spy(new MockClientHandler());
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `serverHandler` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        config = UnitTestBase.updateWithRequiredOptions(objects);
        conf = TransportConf.wrapConfig(config);
        serverHandler = Mockito.spy(new MockMessageHandler());
        clientHandler = Mockito.spy(new MockClientHandler());
        connectionManager = new TransportConnectionManager();
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `clientHandler` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        conf = TransportConf.wrapConfig(config);
        serverHandler = Mockito.spy(new MockMessageHandler());
        clientHandler = Mockito.spy(new MockClientHandler());
        connectionManager = new TransportConnectionManager();
        port = connectionManager.startServer(config, serverHandler);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `connectionManager` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        serverHandler = Mockito.spy(new MockMessageHandler());
        clientHandler = Mockito.spy(new MockClientHandler());
        connectionManager = new TransportConnectionManager();
        port = connectionManager.startServer(config, serverHandler);
        connectionManager.initClientManager(config, clientHandler);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `port` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        clientHandler = Mockito.spy(new MockClientHandler());
        connectionManager = new TransportConnectionManager();
        port = connectionManager.startServer(config, serverHandler);
        connectionManager.initClientManager(config, clientHandler);

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `clientProtocol` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        NettyProtocol protocol2 = Whitebox.getInternalState(clientFactory,
                                                            "protocol");
        clientProtocol = Mockito.spy(protocol2);
        Whitebox.setInternalState(clientFactory, "protocol",
                                  clientProtocol);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `serverProtocol` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        NettyProtocol protocol = Whitebox.getInternalState(channelInitializer,
                                                           "protocol");
        serverProtocol = Mockito.spy(protocol);
        Whitebox.setInternalState(channelInitializer, "protocol",
                                  serverProtocol);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `connectionManager` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNetworkTest.java`
#### Snippet
```java
        if (connectionManager != null) {
            connectionManager.shutdown();
            connectionManager = null;
        }
        Configurator.setAllLevels("org.apache.hugegraph", Level.INFO);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
    @Before
    public void setup() {
        config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.TRANSPORT_SERVER_HOST, "127.0.0.1",
            ComputerOptions.TRANSPORT_SERVER_PORT, "8086",
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `clientHandler` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
        );
        MockMessageHandler serverHandler = new MockMessageHandler();
        clientHandler = new MockClientHandler();
        this.server = new NettyTransportServer();
        this.server.listen(config, serverHandler);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    @Test
    public void testListenWithInvalidPort() {
        config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.TRANSPORT_SERVER_HOST, "127.0.0.1",
            ComputerOptions.TRANSPORT_SERVER_PORT, "67899",
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    @Before
    public void setup() {
        config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.TRANSPORT_SERVER_HOST, "127.0.0.1",
            ComputerOptions.TRANSPORT_SERVER_PORT, "0",
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `messageHandler` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
            ComputerOptions.TRANSPORT_MAX_SYN_BACKLOG, "1024"
        );
        messageHandler = new MockMessageHandler();
        this.server = new NettyTransportServer();
    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    @Test
    public void testListenWithAssignPort() {
        config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.TRANSPORT_SERVER_HOST, "127.0.0.1",
            ComputerOptions.TRANSPORT_SERVER_PORT, "9091"
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    @Test
    public void testListenWithZeroIp() {
        config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.TRANSPORT_SERVER_HOST, "0.0.0.0"
        );
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    @Test
    public void testListenWithLocalHost() {
        config = UnitTestBase.updateWithRequiredOptions(
                ComputerOptions.TRANSPORT_SERVER_HOST, "localhost"
        );
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    @Test
    public void testListenWithInvalidHost() {
        config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.TRANSPORT_SERVER_HOST, "abcdefd",
            ComputerOptions.TRANSPORT_SERVER_PORT, "0",
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        String hostName = localHost.getHostName();
        String ip = InetAddress.getLocalHost().getHostAddress();
        config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.TRANSPORT_SERVER_HOST, hostName
        );
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
    @Test
    public void testEpollMode() {
        config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.TRANSPORT_SERVER_HOST, "127.0.0.1",
            ComputerOptions.TRANSPORT_IO_MODE, "EPOLL"
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        if (!ips.isEmpty()) {
            String ip = ips.get(0);
            config = UnitTestBase.updateWithRequiredOptions(
                ComputerOptions.TRANSPORT_SERVER_HOST, ip
            );
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `connectionManager` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        MessageHandler serverHandler = new MockMessageHandler();
        ClientHandler clientHandler = new MockClientHandler();
        connectionManager = new TransportConnectionManager();
        port = connectionManager.startServer(config, serverHandler);
        connectionManager.initClientManager(config, clientHandler);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `port` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        ClientHandler clientHandler = new MockClientHandler();
        connectionManager = new TransportConnectionManager();
        port = connectionManager.startServer(config, serverHandler);
        connectionManager.initClientManager(config, clientHandler);
    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `config` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
                ComputerOptions.TRANSPORT_MIN_PENDING_REQUESTS, "101"
        );
        config = ComputerContext.instance().config();

        TransportConf conf = TransportConf.wrapConfig(config);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `isRun` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/degree/DegreeCentralityTest.java`
#### Snippet
```java
        public Double value(Vertex vertex) {
            Double value = super.value(vertex);
            isRun = true;
            if (StringUtils.isEmpty(this.weight)) {
                Assert.assertEquals(vertex.numEdges(), value, 0.000001);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `isRun` from instance context
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/degree/DegreeCentralityTest.java`
#### Snippet
```java
            this.weight = config.getString(
                          DegreeCentrality.OPTION_WEIGHT_PROPERTY, "");
            isRun = false;
        }

```

## RuleId[id=Java8ListReplaceAll]
### Java8ListReplaceAll
The loop can be replaced with 'List.replaceAll'
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/AbstractController.java`
#### Snippet
```java
            return rs;
        }
        for (int i = 0; i < rs.size(); i++) {
            rs.set(i, Serialization.clone(rs.get(i)));
        }
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            ComputerContextUtil.convertToMap(new String[0]);
        });

```

## RuleId[id=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `configMapPaths.keySet()` may be replaced with 'entrySet()' iteration
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/controller/ComputerJobDeployer.java`
#### Snippet
```java
        Map<String, String> configMapPaths = spec.getConfigMapPaths();
        if (MapUtils.isNotEmpty(configMapPaths)) {
            for (String key : configMapPaths.keySet()) {
                ConfigMap configMap = client.configMaps().withName(key).get();
                E.checkArgument(configMap != null &&
```

### KeySetIterationMayUseEntrySet
Iteration over `secretPaths.keySet()` may be replaced with 'entrySet()' iteration
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/controller/ComputerJobDeployer.java`
#### Snippet
```java
        Map<String, String> secretPaths = spec.getSecretPaths();
        if (MapUtils.isNotEmpty(secretPaths)) {
            for (String key : secretPaths.keySet()) {
                Secret secret = client.secrets().withName(key).get();
                E.checkArgument(secret != null &&
```

## RuleId[id=UnnecessaryQualifierForThis]
### UnnecessaryQualifierForThis
Qualifier `OperatorEntrypoint` on 'this' is unnecessary in this context
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
                LOG.error("Informer event listener exception occurred",
                          exception);
                OperatorEntrypoint.this.shutdown();
            });

```

### UnnecessaryQualifierForThis
Qualifier `OperatorEntrypoint` on 'this' is unnecessary in this context
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
                } catch (Throwable e) {
                    LOG.error("Failed to set up ready check");
                    OperatorEntrypoint.this.shutdown();
                }
            });
```

## RuleId[id=ObjectNotify]
### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffers.java`
#### Snippet
```java
            throw new ComputerException("Failed to clear sorting buffer");
        }
        this.notify();
    }

```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueue.java`
#### Snippet
```java
        if (deque.size() == 1) {
            // Notify when the queue is empty before put
            this.notify();
        }
    }
```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueue.java`
#### Snippet
```java
        if (deque.size() == 1) {
            // Notify when the queue is empty before put
            this.notify();
        }
    }
```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/WorkQueue.java`
#### Snippet
```java

        this.queue.add(item);
        this.notify();
    }

```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/WorkQueue.java`
#### Snippet
```java
        if (this.incoming.contains(item)) {
            this.queue.add(item);
            this.notify();
        }
    }
```

## RuleId[id=FinalPrivateMethod]
### FinalPrivateMethod
'private' method declared `final`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamOutput.java`
#### Snippet
```java
    }

    private final int bufferAvailable() {
        return this.bufferCapacity - (int) super.position();
    }
```

### FinalPrivateMethod
'private' method declared `final`
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java

    @SafeVarargs
    private final void registerController(
                       AbstractController<?> controller,
                       Class<? extends HasMetadata>... ownsClass) {
```

## RuleId[id=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`this.outputFiles.size() == 0` can be replaced with 'this.outputFiles.isEmpty()'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvPartition.java`
#### Snippet
```java
        }
        this.mergeOutputFilesIfNeeded();
        if (this.outputFiles.size() == 0) {
            return PeekableIterator.emptyIterator();
        }
```

### SizeReplaceableByIsEmpty
`filter.size() == 0` can be replaced with 'filter.isEmpty()'
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/filter/SpreadFilter.java`
#### Snippet
```java
                                      Map<String, Serializable> filter,
                                      String label) {
        if (filter.size() == 0) {
            return PASS;
        }
```

### SizeReplaceableByIsEmpty
`this.queue.size() == 0` can be replaced with 'this.queue.isEmpty()'
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/WorkQueue.java`
#### Snippet
```java

    public synchronized T get() throws InterruptedException {
        while (this.queue.size() == 0) {
            if (this.shutdown) {
                // We must be shutting down
```

### SizeReplaceableByIsEmpty
`configMap.getData().size() > 0` can be replaced with '!configMap.getData().isEmpty()'
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/controller/ComputerJobDeployer.java`
#### Snippet
```java
                ConfigMap configMap = client.configMaps().withName(key).get();
                E.checkArgument(configMap != null &&
                                configMap.getData().size() > 0,
                                "The configMap '%s' don't exist", key);

```

### SizeReplaceableByIsEmpty
`secret.getData().size() > 0` can be replaced with '!secret.getData().isEmpty()'
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/controller/ComputerJobDeployer.java`
#### Snippet
```java
                Secret secret = client.secrets().withName(key).get();
                E.checkArgument(secret != null &&
                                secret.getData().size() > 0,
                                "The secret '%s' don't exist", key);

```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/common/ContainerInfo.java`
#### Snippet
```java

    public String uniqueName() {
        StringBuilder sb = new StringBuilder();
        sb.append(this.hostname)
          .append(":")
```

## RuleId[id=BoundedWildcard]
### BoundedWildcard
Can generalize to `? extends Id`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdSet.java`
#### Snippet
```java
    }

    public void addAll(Collection<Id> other) {
        this.values.addAll(other);
    }
```

### BoundedWildcard
Can generalize to `? extends Value`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/graph/properties/DefaultProperties.java`
#### Snippet
```java

    @Override
    public void putAll(Map<String, Value> kvs) {
        this.keyValues.putAll(kvs);
    }
```

### BoundedWildcard
Can generalize to `? super T`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/combiner/Combiner.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    static <T extends Value> T combineAll(Combiner<T> combiner,
                                          Iterator<T> values) {
        if (!values.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/combiner/Combiner.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    static <T extends Value> T combineAll(Combiner<T> combiner,
                                          Iterator<T> values) {
        if (!values.hasNext()) {
            return null;
```

### BoundedWildcard
Can generalize to `? extends M`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/FilterComputation.java`
#### Snippet
```java
    default Iterator<M> computeMessages(ComputationContext context,
                                        Vertex vertex,
                                        Iterator<M> messages) {
        // Streaming iterate messages
        return new MapperIterator<>(messages, message -> {
```

### BoundedWildcard
Can generalize to `? extends M`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/FilterComputation.java`
#### Snippet
```java
    default void sendMessages(ComputationContext context,
                              Vertex vertex,
                              Iterator<M> results) {
        context.sendMessagesToAllEdges(vertex, results);
    }
```

### BoundedWildcard
Can generalize to `? extends M`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/ComputationContext.java`
#### Snippet
```java
    default <M extends Value> void sendMessagesToAllEdgesIf(
                                   Vertex vertex,
                                   Iterator<M> values,
                                   BiFunction<M, Id, Boolean> filter) {
        while (values.hasNext()) {
```

### BoundedWildcard
Can generalize to `? super M`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/ComputationContext.java`
#### Snippet
```java
                                   Vertex vertex,
                                   Iterator<M> values,
                                   BiFunction<M, Id, Boolean> filter) {
        while (values.hasNext()) {
            M value = values.next();
```

### BoundedWildcard
Can generalize to `? super Id`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/ComputationContext.java`
#### Snippet
```java
                                   Vertex vertex,
                                   Iterator<M> values,
                                   BiFunction<M, Id, Boolean> filter) {
        while (values.hasNext()) {
            M value = values.next();
```

### BoundedWildcard
Can generalize to `? super Id`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/ComputationContext.java`
#### Snippet
```java
                                   Vertex vertex,
                                   M value,
                                   BiFunction<M, Id, Boolean> filter) {
        Iterator<Edge> edges = vertex.edges().iterator();
        while (edges.hasNext()) {
```

### BoundedWildcard
Can generalize to `? super String`
in `computer-api/src/main/java/org/apache/hugegraph/computer/algorithm/AlgorithmParams.java`
#### Snippet
```java
    void setAlgorithmParameters(Map<String, String> params);

    default void setIfAbsent(Map<String, String> params, String key,
                             String value) {
        if (!params.containsKey(key) && !COMPUTER_PROHIBIT_USER_OPTIONS.contains(key)) {
```

### BoundedWildcard
Can generalize to `? super String`
in `computer-api/src/main/java/org/apache/hugegraph/computer/algorithm/AlgorithmParams.java`
#### Snippet
```java
    void setAlgorithmParameters(Map<String, String> params);

    default void setIfAbsent(Map<String, String> params, String key,
                             String value) {
        if (!params.containsKey(key) && !COMPUTER_PROHIBIT_USER_OPTIONS.contains(key)) {
```

### BoundedWildcard
Can generalize to `? extends ContainerInfo`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/Bsp4Worker.java`
#### Snippet
```java

    // Note: The workerInfo in Bsp4Worker is the same object in WorkerService.
    private void assignThisWorkerId(List<ContainerInfo> workersFromMaster) {
        for (ContainerInfo container : workersFromMaster) {
            if (this.workerInfo.uniqueName().equals(container.uniqueName())) {
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/JavaInputSorter.java`
#### Snippet
```java

    @Override
    public Iterator<KvEntry> sort(Iterator<KvEntry> entries)
                             throws IOException {
        List<KvEntry> kvEntries = threadLocalSortList();
```

### BoundedWildcard
Can generalize to `? extends ContainerInfo`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/Bsp4Master.java`
#### Snippet
```java
    }

    private void masterAllInitDone(List<ContainerInfo> workers) {
        String path = this.constructPath(BspEvent.BSP_MASTER_ALL_INIT_DONE);
        this.bspClient().put(path, SerializeUtil.toBytes(workers));
```

### BoundedWildcard
Can generalize to `? extends ContainerInfo`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/Bsp4Master.java`
#### Snippet
```java
    }

    private void assignIdForWorkers(List<ContainerInfo> containers) {
        // Assign worker id from 1.
        for (int i = 0; i < containers.size(); i++) {
```

### BoundedWildcard
Can generalize to `? extends EntryIterator`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/InputsSorterImpl.java`
#### Snippet
```java
        private boolean closed;

        public SortingEntries(List<EntryIterator> sources) {
            this.sources = sources;
            this.inputsSorting = SortingFactory.createSorting(sources);
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SubKvSorter.java`
#### Snippet
```java
        private KvEntry currentEntry;

        public MergePath(PeekableIterator<KvEntry> entries,
                         KvEntry currentEntry) {
            this.entries = entries;
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/KvInnerSortFlusher.java`
#### Snippet
```java

    @Override
    public void flush(Iterator<KvEntry> entries) throws IOException {
        E.checkArgument(entries.hasNext(),
                        "Parameter entries can't be empty");
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/CombinableSorterFlusher.java`
#### Snippet
```java
    }

    public void flush(Iterator<KvEntry> entries) throws IOException {
        E.checkArgument(entries.hasNext(),
                        "Parameter entries can't be empty");
```

### BoundedWildcard
Can generalize to `? extends T`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/PeekableIteratorAdaptor.java`
#### Snippet
```java
    private T next;

    private PeekableIteratorAdaptor(CIter<T> entries) {
        this.entries = entries;
        this.fetchNext();
```

### BoundedWildcard
Can generalize to `? extends KeyValue`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
    private List<byte[]> waitAndPrefixGetFromPutEvent(
                         ByteSequence prefixSeq, int count,
                         List<KeyValue> existedKeyValues,
                         long revision, long timeout, long logInterval)
                         throws InterruptedException {
```

### BoundedWildcard
Can generalize to `? extends RandomAccessInput`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/BufferFileSorter.java`
#### Snippet
```java

    @Override
    public void mergeBuffers(List<RandomAccessInput> inputs,
                             OuterSortFlusher flusher, String output,
                             boolean withSubKv) throws Exception {
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/CombineSubKvInnerSortFlusher.java`
#### Snippet
```java

    @Override
    public void flush(Iterator<KvEntry> entries) throws IOException {
        E.checkArgument(entries.hasNext(), "Parameter entries can't be empty");

```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/CombineSubKvInnerSortFlusher.java`
#### Snippet
```java
    }

    private void writeSubKvs(KvEntry kvEntry, Iterator<KvEntry> subKvIter)
                             throws IOException {
        E.checkArgument(subKvIter.hasNext(),
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/CombineSubKvInnerSortFlusher.java`
#### Snippet
```java
    }

    private Iterator<KvEntry> sortedSubKvFromEntries(List<KvEntry> entries) {
        Function<KvEntry, Iterator<KvEntry>> kvEntryToSubKvs =
                                             EntriesUtil::subKvIterFromEntry;
```

### BoundedWildcard
Can generalize to `? extends RandomAccessInput`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/HgkvFileSorter.java`
#### Snippet
```java

    @Override
    public void mergeBuffers(List<RandomAccessInput> inputs,
                             OuterSortFlusher flusher, String output,
                             boolean withSubKv) throws Exception {
```

### BoundedWildcard
Can generalize to `? extends SelectedFiles`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/DefaultSorter.java`
#### Snippet
```java
    }

    public void mergeFile(List<SelectedFiles> selectedFiles,
                          Function<String, EntryIterator> fileToEntries,
                          Function<String, KvEntryFileWriter> fileToWriter,
```

### BoundedWildcard
Can generalize to `? super String`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/DefaultSorter.java`
#### Snippet
```java

    public PeekableIterator<KvEntry> iterator(
           List<String> inputs, Function<String, EntryIterator> fileToEntries)
           throws IOException {
        List<EntryIterator> entries = inputs.stream()
```

### BoundedWildcard
Can generalize to `? extends EntryIterator`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/DefaultSorter.java`
#### Snippet
```java

    public PeekableIterator<KvEntry> iterator(
           List<String> inputs, Function<String, EntryIterator> fileToEntries)
           throws IOException {
        List<EntryIterator> entries = inputs.stream()
```

### BoundedWildcard
Can generalize to `? extends V`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/SerializeUtil.java`
#### Snippet
```java

    public static <V extends Readable> List<V> fromBytes(byte[] bytes,
                                                         Supplier<V> supplier) {
        try (BytesInput bai = IOFactory.createBytesInput(bytes)) {
            int size = bai.readInt();
```

### BoundedWildcard
Can generalize to `? extends WorkerStat`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/graph/SuperstepStat.java`
#### Snippet
```java
    }

    public static SuperstepStat from(List<WorkerStat> workerStats) {
        SuperstepStat superstepStat = new SuperstepStat();
        for (WorkerStat workerStat : workerStats) {
```

### BoundedWildcard
Can generalize to `? super V`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/Consumers.java`
#### Snippet
```java

    public Consumers(ExecutorService executor,
                     Consumer<V> consumer, Runnable done) {
        this.executor = executor;
        this.consumer = consumer;
```

### BoundedWildcard
Can generalize to `? super String`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/merge/FileMergerImpl.java`
#### Snippet
```java

    private void mergeInputs(List<String> inputs,
                             Function<String, EntryIterator> inputToIter,
                             OuterSortFlusher flusher, String output,
                             Function<String, KvEntryFileWriter> fileToWriter)
```

### BoundedWildcard
Can generalize to `? extends EntryIterator`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/merge/FileMergerImpl.java`
#### Snippet
```java

    private void mergeInputs(List<String> inputs,
                             Function<String, EntryIterator> inputToIter,
                             OuterSortFlusher flusher, String output,
                             Function<String, KvEntryFileWriter> fileToWriter)
```

### BoundedWildcard
Can generalize to `? super String`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/merge/FileMergerImpl.java`
#### Snippet
```java
                             Function<String, EntryIterator> inputToIter,
                             OuterSortFlusher flusher, String output,
                             Function<String, KvEntryFileWriter> fileToWriter)
                             throws Exception {
        /*
```

### BoundedWildcard
Can generalize to `? extends KvEntryFileWriter`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/merge/FileMergerImpl.java`
#### Snippet
```java
                             Function<String, EntryIterator> inputToIter,
                             OuterSortFlusher flusher, String output,
                             Function<String, KvEntryFileWriter> fileToWriter)
                             throws Exception {
        /*
```

### BoundedWildcard
Can generalize to `? super T`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/select/DisperseEvenlySelector.java`
#### Snippet
```java
        private final int size;

        public Heap(List<T> data, Comparator<T> comparator) {
            this.data = data;
            this.size = data.size();
```

### BoundedWildcard
Can generalize to `? extends InputStruct`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/loader/LoaderFileInputSplitFetcher.java`
#### Snippet
```java
    }

    private void splitStructs(List<InputStruct> structs) {
        for (InputStruct struct : structs) {
            InputStruct result = struct.extractVertexStruct();
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/input/MessageInput.java`
#### Snippet
```java

    public MessageInput(ComputerContext context,
                        PeekableIterator<KvEntry> messages) {
        if (messages == null) {
            this.messages = PeekableIterator.emptyIterator();
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java
    }

    protected PartitionStat input(PeekableIterator<KvEntry> vertices,
                                  PeekableIterator<KvEntry> edges) {
        try {
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java
    }

    private void writeEdges(Pointer vid, PeekableIterator<KvEntry> edges,
                            BufferedFileOutput edgeOut) throws IOException {
        byte[] vidBytes = vid.bytes();
```

### BoundedWildcard
Can generalize to `? extends T`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/combiner/AbstractPointerCombiner.java`
#### Snippet
```java
    protected BytesOutput output;

    public AbstractPointerCombiner(Supplier<T> supplier, Combiner<T> combiner) {
        this.v1 = supplier.get();
        this.v2 = supplier.get();
```

### BoundedWildcard
Can generalize to `? super RecyclerReference`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/allocator/RecycleHandler.java`
#### Snippet
```java
    private Recycler.Handle<RecyclerReference<T>> handle;

    public RecycleHandler(Recycler.Handle<RecyclerReference<T>> handle) {
        this.handle = handle;
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocator.java`
#### Snippet
```java
    private <T extends Recyclable> Recycler<RecyclerReference<T>>
                                   newRecycler(int capacityPerThread,
                                               Supplier<T> supplier) {
        // TODO: Add more params for Recycler
        return new Recycler<RecyclerReference<T>>(capacityPerThread) {
```

### BoundedWildcard
Can generalize to `? super Message`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java

    public ClientSession(TransportConf conf,
                         Function<Message, Future<Void>> sendFunction) {
        super(conf);
        this.maxPendingRequests = this.conf.maxPendingRequests();
```

### BoundedWildcard
Can generalize to `? extends IdList`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/RingsDetection.java`
#### Snippet
```java
    @Override
    public void compute(ComputationContext context, Vertex vertex,
                        Iterator<IdList> messages) {
        Id id = vertex.id();
        boolean halt = true;
```

### BoundedWildcard
Can generalize to `? extends RingsDetectionMessage`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/filter/RingsDetectionWithFilter.java`
#### Snippet
```java
    @Override
    public void compute(ComputationContext context, Vertex vertex,
                        Iterator<RingsDetectionMessage> messages) {
        boolean halt = true;
        if (this.filter.filter(vertex)) {
```

### BoundedWildcard
Can generalize to `? extends Id`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/lpa/Lpa.java`
#### Snippet
```java
    }

    private Id voteLabel(Iterator<Id> messages) {
        // Calculate label frequency
        Map<Id, MutableInt> labels = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? extends Id`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/wcc/Wcc.java`
#### Snippet
```java
    @Override
    public void compute(ComputationContext context, Vertex vertex,
                        Iterator<Id> messages) {
        Id message = Combiner.combineAll(context.combiner(), messages);
        Id value = vertex.value();
```

### BoundedWildcard
Can generalize to `? extends Id`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/kcore/Kcore.java`
#### Snippet
```java
    @Override
    public void compute(ComputationContext context, Vertex vertex,
                        Iterator<Id> messages) {
        KcoreValue value = vertex.value();
        if (!value.active()) {
```

### BoundedWildcard
Can generalize to `? extends IdList`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/trianglecount/TriangleCount.java`
#### Snippet
```java

    protected Integer triangleCount(ComputationContext context, Vertex vertex,
                                    Iterator<IdList> messages, IdSet neighbors) {
        if (context.superstep() == 1) {
            // Collect outgoing neighbors
```

### BoundedWildcard
Can generalize to `? extends DoubleValue`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/pagerank/PageRank.java`
#### Snippet
```java
    @Override
    public void compute(ComputationContext context, Vertex vertex,
                        Iterator<DoubleValue> messages) {
        DoubleValue message = Combiner.combineAll(context.combiner(), messages);
        double rankFromNeighbors = 0.0;
```

### BoundedWildcard
Can generalize to `? extends ClosenessMessage`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/closeness/ClosenessCentrality.java`
#### Snippet
```java
    @Override
    public void compute(ComputationContext context, Vertex vertex,
                        Iterator<ClosenessMessage> messages) {
        Id selfId = vertex.id();
        // Save the distance from other vertices to self
```

### BoundedWildcard
Can generalize to `? extends Serializable`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/filter/SpreadFilter.java`
#### Snippet
```java

    private static List<Serializable> expressions(
                                      Map<String, Serializable> filter,
                                      String label) {
        if (filter.size() == 0) {
```

### BoundedWildcard
Can generalize to `? super String`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/filter/SpreadFilter.java`
#### Snippet
```java
    }

    private void init(Map<String, Serializable> filter,
                      List<FilterDescribe.DescribeItem> describes,
                      Predicate<? super String> predicate) {
```

### BoundedWildcard
Can generalize to `? super Serializable`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/filter/SpreadFilter.java`
#### Snippet
```java
    }

    private void init(Map<String, Serializable> filter,
                      List<FilterDescribe.DescribeItem> describes,
                      Predicate<? super String> predicate) {
```

### BoundedWildcard
Can generalize to `? extends FilterDescribe.DescribeItem`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/filter/SpreadFilter.java`
#### Snippet
```java

    private void init(Map<String, Serializable> filter,
                      List<FilterDescribe.DescribeItem> describes,
                      Predicate<? super String> predicate) {
        for (FilterDescribe.DescribeItem describe : describes) {
```

### BoundedWildcard
Can generalize to `? extends BetweennessMessage`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/betweenness/BetweennessCentrality.java`
#### Snippet
```java
    @Override
    public void compute(ComputationContext context, Vertex vertex,
                        Iterator<BetweennessMessage> messages) {
        BetweennessValue value = vertex.value();
        // The betweenness value to be updated
```

### BoundedWildcard
Can generalize to `? extends Pod`
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/controller/ComputerJobController.java`
#### Snippet
```java
    }

    private MatchWithMsg imagePullBackOff(List<Pod> pods) {
        if (CollectionUtils.isEmpty(pods)) {
            return MatchWithMsg.NO_MATCH;
```

### BoundedWildcard
Can generalize to `? extends Pod`
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/controller/ComputerJobController.java`
#### Snippet
```java
    }

    private MatchWithMsg unSchedulable(List<Pod> pods) {
        if (CollectionUtils.isEmpty(pods)) {
            return MatchWithMsg.NO_MATCH;
```

### BoundedWildcard
Can generalize to `? super VolumeMount`
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/controller/ComputerJobDeployer.java`
#### Snippet
```java
    }

    private void mountConfigMapOrSecret(List<VolumeMount> volumeMounts,
                                        String key, String volumePath,
                                        Map<String, String> data) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/SorterTestUtil.java`
#### Snippet
```java
    }

    public static BytesOutput writeSubKvMapToOutput(List<List<Integer>> data)
                                                    throws IOException {
        BytesOutput output = IOFactory.createBytesOutput(
```

### BoundedWildcard
Can generalize to `? extends List`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/TestData.java`
#### Snippet
```java
    }

    private static List<List<Integer>> cloneList(List<List<Integer>> list) {
        List<List<Integer>> result = new ArrayList<>();
        list.forEach(item -> {
```

### BoundedWildcard
Can generalize to `? extends Iterator`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/TestData.java`
#### Snippet
```java
            );

    public static List<Integer> getSortedResult(List<Iterator<Integer>> list) {
        return list.stream()
                   .map(iter -> {
```

### BoundedWildcard
Can generalize to `? extends IdList`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/MockComputation.java`
#### Snippet
```java
    @Override
    public void compute(ComputationContext context, Vertex vertex,
                        Iterator<IdList> messages) {
        IdListList value = vertex.value();
        while (messages.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/MessageInputTest.java`
#### Snippet
```java
    }

    private void checkMessages(Map<Id, List<IdList>> expectedMessages,
                               MessageInput<IdList> input) throws IOException {
        for (long i = 0L; i < 200L; i++) {
```

### BoundedWildcard
Can generalize to `? super NetworkBuffer`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/ReceiverUtil.java`
#### Snippet
```java

    public static void consumeBuffer(byte[] bytes,
                                     Consumer<NetworkBuffer> consumer) {
        ByteBuf buf = Unpooled.directBuffer(bytes.length);
        try {
```

### BoundedWildcard
Can generalize to `? extends Throwable`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
    }

    private boolean existError(List<Throwable> exceptions) {
        boolean error = false;
        for (Throwable e : exceptions) {
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
    }

    public static void checkTenCombineMessages(PeekableIterator<KvEntry> it)
                                               throws IOException {
        Assert.assertTrue(it.hasNext());
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/message/ComputeMessageRecvPartitionTest.java`
#### Snippet
```java
    }

    private static void checkIdValueListMessages(PeekableIterator<KvEntry> it)
                                                 throws IOException {
        for (long i = 0L; i < 10L; i++) {
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
    }

    public static void checkPartitionIterator(PeekableIterator<KvEntry> it)
                                              throws IOException {
        for (long i = 0L; i < 10L; i++) {
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java

    private static void checkTenVertexWithMergedProperties(
                        PeekableIterator<KvEntry> it)
                        throws IOException {
        for (long i = 0L; i < 10L; i++) {
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java

    private static void checkTenEdgesWithCombinedProperties(
                        Iterator<KvEntry> it)
                        throws IOException {
        for (long i = 0L; i < 10L; i++) {
```

### BoundedWildcard
Can generalize to `? extends KvEntry`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java


    public static void checkTenEdges(PeekableIterator<KvEntry> it)
                                     throws IOException {
        for (long i = 0L; i < 10L; i++) {
```

## RuleId[id=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'BooleanValue' and 'IntValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value1.equals(new BooleanValue(true)));

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'StringValue' and 'DoubleValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java

        DoubleValue value3 = new DoubleValue(1.1);
        Assert.assertFalse(value1.equals(value3));
    }

```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'NullValue' and 'IntValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(NullValue.get()));

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'FloatValue' and 'IntValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value1.equals(new FloatValue(1.0f)));

        Assert.assertFalse(value1.equals(new IntValue(0)));
        Assert.assertFalse(value1.equals(null));
    }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'IntValue' and 'FloatValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertTrue(intValue1.equals(new IntValue(0)));
        Assert.assertFalse(intValue1.equals(new IntValue(1)));
        Assert.assertFalse(intValue1.equals(new FloatValue(1f)));
        Assert.assertFalse(intValue1.equals(null));
    }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'LongValue' and 'FloatValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value1.equals(new LongValue(0L)));
        Assert.assertFalse(value1.equals(new LongValue(1L)));
        Assert.assertFalse(value1.equals(new FloatValue(1f)));
        Assert.assertFalse(value1.equals(null));
    }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'ListValue' and 'ListValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertTrue(value2.equals(value2.copy()));

        Assert.assertFalse(value1.equals(value2));
        Assert.assertFalse(value2.equals(value1));

```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'ListValue' and 'ListValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(value2));
        Assert.assertFalse(value2.equals(value1));

        Assert.assertFalse(value1.equals(new IntValue(1)));
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'ListValue' and 'IntValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value2.equals(value1));

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/HgkvFileImpl.java`
#### Snippet
```java
                        "file already exists: '%s'", file.getPath());
        if (!file.getParentFile().exists()) {
            file.getParentFile().mkdirs();
        }
        file.createNewFile();
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/HgkvFileImpl.java`
#### Snippet
```java
            file.getParentFile().mkdirs();
        }
        file.createNewFile();

        return new HgkvFileImpl(path);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/HgkvDirImpl.java`
#### Snippet
```java
                        "Can't create HgkvDir because the " +
                        "directory already exists: '%s'", file.getPath());
        file.mkdirs();
        return new HgkvDirImpl(path);
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java
            this.preStatusInput.close();
            this.preValueInput.close();
            this.preStatusFile.delete();
            this.preValueFile.delete();
        }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java
            this.preValueInput.close();
            this.preStatusFile.delete();
            this.preValueFile.delete();
        }
        this.curStatusOutput.close();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java

    private static void createFile(File file) throws IOException {
        file.getParentFile().mkdirs();
        E.checkArgument(file.createNewFile(), "Already exists file: %s", file);
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java
        assert this.preStatusFile == this.curStatusFile;
        assert this.preValueFile == this.curValueFile;
        this.preStatusFile.delete();
        this.preValueFile.delete();

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java
        assert this.preValueFile == this.curValueFile;
        this.preStatusFile.delete();
        this.preValueFile.delete();

        this.vertexFile.delete();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java
        this.preValueFile.delete();

        this.vertexFile.delete();
        this.edgeFile.delete();
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java

        this.vertexFile.delete();
        this.edgeFile.delete();
    }

```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvPartitions.java`
#### Snippet
```java
        P partition = this.partition(partitionId);
        String path = partition.genOutputPath();
        new File(path).getParentFile().mkdirs();
        return path;
    }
```

### IgnoreResultOfCall
Result of `FilterInputStream.skip()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                                       new FileInputStream(file))) {
                Assert.assertEquals(-128, dis.readByte());
                dis.skip(1);
                for (int i = -126; i <= 127; i++) {
                    Assert.assertEquals(i, dis.readByte());
```

### IgnoreResultOfCall
Result of `String.contains()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                    output.skip(-1);
                }, e -> {
                    e.getMessage().contains("The parameter bytesToSkip must " +
                                            "be >= 0");
                });
```

### IgnoreResultOfCall
Result of `String.contains()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                    input.skip(-1);
                }, e -> {
                    e.getMessage().contains("The parameter bytesToSkip must " +
                                            "be >= 0");
                });
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
        // Path isn't directory
        File file = new File(StoreTestUtil.availablePathById("1"));
        file.getParentFile().mkdirs();
        file.createNewFile();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
        File file = new File(StoreTestUtil.availablePathById("1"));
        file.getParentFile().mkdirs();
        file.createNewFile();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            HgkvDirImpl.open(file.getPath());
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/BitFileTest.java`
#### Snippet
```java
    private static File createTempDir() {
        File dir = new File(UUID.randomUUID().toString());
        dir.mkdirs();
        return dir;
    }
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
    public void testInitWithFile() throws IOException {
        File file = new File("exist");
        file.createNewFile();
        Config config = UnitTestBase.updateWithRequiredOptions(
            ComputerOptions.JOB_ID, "local_001",
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
            Assert.assertContains("Can't create dir ", e.getMessage());
        });
        file.delete();
    }

```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
    private static File createTempDir() {
        File dir = new File(UUID.randomUUID().toString());
        dir.mkdirs();
        return dir;
    }
```

### IgnoreResultOfCall
Result of `CustomizeValue.compareTo()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficientTest.java`
#### Snippet
```java
                            () -> value.assign(null));
        Assert.assertThrows(UnsupportedOperationException.class,
                            () -> value.compareTo(new ClusteringCoefficientValue()));

        ClusteringCoefficientValue copy = value.copy();
```

### IgnoreResultOfCall
Result of `CustomizeValue.compareTo()` is ignored
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/trianglecount/TriangleCountTest.java`
#### Snippet
```java
                            () -> value.assign(null));
        Assert.assertThrows(UnsupportedOperationException.class,
                            () -> value.compareTo(new TriangleCountValue()));

        TriangleCountValue copy = value.copy();
```

## RuleId[id=FunctionalExpressionCanBeFolded]
### FunctionalExpressionCanBeFolded
Lambda can be replaced with call qualifier
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/ReceiverUtil.java`
#### Snippet
```java

        entryOutput.writeEntry(out -> {
            id.write(out);
        }, message);
        return bytesOutput.toByteArray();
```

### FunctionalExpressionCanBeFolded
Lambda can be replaced with call qualifier
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
        Id id = vertex.id();
        KvEntryWriter subKvWriter = entryOutput.writeEntry(out -> {
            id.write(out);
        });
        for (Edge edge : vertex.edges()) {
```

### FunctionalExpressionCanBeFolded
Lambda can be replaced with call qualifier
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            Id targetId = edge.targetId();
            subKvWriter.writeSubKv(out -> {
                targetId.write(out);
            }, out -> {
                edge.properties().write(out);
```

## RuleId[id=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'org.apache.hugegraph.computer.dist.HugeGraphComputerTest.existError(java.lang.Throwable\[\])' accessed via instance reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/dist/HugeGraphComputerTest.java`
#### Snippet
```java

        Assert.assertFalse(Arrays.asList(exceptions).toString(),
                           this.existError(exceptions));
    }

```

## RuleId[id=AssertBetweenInconvertibleTypes]
### AssertBetweenInconvertibleTypes
Possibly redundant assertion: incompatible types are compared 'DefaultEdge' and 'Properties'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/DefaultEdgeTest.java`
#### Snippet
```java
        Assert.assertEquals(edge1, edge2);

        Assert.assertNotEquals(edge1, properties);
    }
}
```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `readFully()` is identical to its super method
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/AbstractBufferedFileInput.java`
#### Snippet
```java

    @Override
    public void readFully(byte[] b) throws IOException {
        this.readFully(b, 0, b.length);
    }
```

### RedundantMethodOverride
Method `close()` only delegates to its super method
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/loader/FileVertxFetcher.java`
#### Snippet
```java

    @Override
    public void close() {
        super.close();
    }
```

### RedundantMethodOverride
Method `init()` is identical to its super method
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/SortManager.java`
#### Snippet
```java

    @Override
    public void init(Config config) {
        // pass
    }
```

### RedundantMethodOverride
Method `close()` only delegates to its super method
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/loader/FileEdgeFetcher.java`
#### Snippet
```java

    @Override
    public void close() {
        super.close();
    }
```

### RedundantMethodOverride
Method `max()` is identical to its super method
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/HgkvDirImpl.java`
#### Snippet
```java

    @Override
    public byte[] max() {
        return this.max;
    }
```

### RedundantMethodOverride
Method `min()` is identical to its super method
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/HgkvDirImpl.java`
#### Snippet
```java

    @Override
    public byte[] min() {
        return this.min;
    }
```

### RedundantMethodOverride
Method `close()` is identical to its super method
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendManager.java`
#### Snippet
```java

    @Override
    public void close(Config config) {
        // pass
    }
```

### RedundantMethodOverride
Method `init()` is identical to its super method
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficient.java`
#### Snippet
```java

    @Override
    public void init(Config config) {
        // Reuse triangle count later
    }
```

### RedundantMethodOverride
Method `category()` is identical to its super method
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficient.java`
#### Snippet
```java

    @Override
    public String category() {
        return "community";
    }
```

### RedundantMethodOverride
Method `beforeSuperstep()` is identical to its super method
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/degree/DegreeCentrality.java`
#### Snippet
```java

    @Override
    public void beforeSuperstep(WorkerContext context) {
        // pass
    }
```

### RedundantMethodOverride
Method `close()` is identical to its super method
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/degree/DegreeCentrality.java`
#### Snippet
```java

    @Override
    public void close(Config config) {
        // pass
    }
```

### RedundantMethodOverride
Method `afterSuperstep()` is identical to its super method
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/degree/DegreeCentrality.java`
#### Snippet
```java

    @Override
    public void afterSuperstep(WorkerContext context) {
        // pass
    }
```

### RedundantMethodOverride
Method `close()` is identical to its super method
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/pagerank/PageRank.java`
#### Snippet
```java

    @Override
    public void close(Config config) {
        // pass
    }
```

### RedundantMethodOverride
Method `close()` is identical to its super method
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/closeness/ClosenessCentrality.java`
#### Snippet
```java

    @Override
    public void close(Config config) {
        // pass
    }
```

### RedundantMethodOverride
Method `close()` is identical to its super method
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/betweenness/BetweennessCentrality.java`
#### Snippet
```java

    @Override
    public void close(Config config) {
        // pass
    }
```

### RedundantMethodOverride
Method `teardown()` only delegates to its super method
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
    @After
    @Override
    public void teardown() throws InterruptedException, ExecutionException {
        super.teardown();
    }
```

### RedundantMethodOverride
Method `init()` only delegates to its super method
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockWorkerService.java`
#### Snippet
```java

    @Override
    public void init(Config config) {
        super.init(config);
    }
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
i \* 4: integer multiplication implicitly cast to long
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                }
                for (int i = size; i >= 0; i--) {
                    output.seek(i * 4);
                    output.writeInt(size - i);
                }
```

## RuleId[id=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileInput.java`
#### Snippet
```java
    @Override
    public void readFully(byte[] b, int off, int len) throws IOException {
        int remaining = super.remaining();
        if (len <= remaining) {
            super.readFully(b, off, len);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileOutput.java`
#### Snippet
```java
    @Override
    protected void flushBuffer() throws IOException {
        int bufferSize = super.bufferSize();
        if (bufferSize == 0) {
            return;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamInput.java`
#### Snippet
```java
            }
            super.seek(0);
            super.limit(0);
            this.fillBuffer();
        } else {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamInput.java`
#### Snippet
```java
    @Override
    public void readFully(byte[] b, int off, int len) throws IOException {
        if (len <= super.remaining()) {
            super.readFully(b, off, len);
        } else if (len <= this.bufferCapacity) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamInput.java`
#### Snippet
```java
            super.readFully(b, off, len);
        } else {
            int remaining = super.remaining();
            super.readFully(b, off, remaining);
            int expectedLen = len - remaining;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamInput.java`
#### Snippet
```java
    @Override
    public long position() {
        return this.inputOffset - super.remaining();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/AbstractBufferedFileOutput.java`
#### Snippet
```java
        long latestPosition = this.position();
        this.seek(position);
        super.writeInt(v);
        this.seek(latestPosition);
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamOutput.java`
#### Snippet
```java
        } else {
            this.outputOffset += bytesToSkip;
            byte[] buffer = super.buffer();
            int writeSize = (int) bytesToSkip;
            while (writeSize > 0) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/AbstractBufferedFileInput.java`
#### Snippet
```java
    @Override
    public long position() {
        return this.fileOffset - super.remaining();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/seqfile/ValueFileOutput.java`
#### Snippet
```java
    public void flushBuffer() throws IOException {
        int segmentRemain = this.currentSegmentRemain();
        int bufferSize = super.bufferSize();

        if (segmentRemain >= bufferSize) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/seqfile/ValueFileOutput.java`
#### Snippet
```java

        if (segmentRemain >= bufferSize) {
            this.currentSegment.write(super.buffer(), 0, bufferSize);
        } else {
            this.currentSegment.write(super.buffer(), 0, segmentRemain);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/seqfile/ValueFileOutput.java`
#### Snippet
```java
            this.currentSegment.write(super.buffer(), 0, bufferSize);
        } else {
            this.currentSegment.write(super.buffer(), 0, segmentRemain);
            this.currentSegment.close();
            this.currentSegment = this.nextSegment();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/seqfile/ValueFileOutput.java`
#### Snippet
```java
            this.currentSegment.close();
            this.currentSegment = this.nextSegment();
            this.currentSegment.write(super.buffer(), segmentRemain,
                                      bufferSize - segmentRemain);
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/seqfile/ValueFileInput.java`
#### Snippet
```java
    @Override
    public void readFully(byte[] b, int off, int len) throws IOException {
        int bufferRemain = super.remaining();
        int segmentRemain = this.currentSegmentRemain();

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/codec/PreciseFrameDecoder.java`
#### Snippet
```java

    public PreciseFrameDecoder() {
        super.setSingleDecode(true);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficient.java`
#### Snippet
```java
    public void compute(ComputationContext context, Vertex vertex, Iterator<IdList> messages) {
        IdSet neighbors = ((ClusteringCoefficientValue) vertex.value()).idSet();
        Integer count = super.triangleCount(context, vertex, messages, neighbors);
        if (count != null) {
            ((ClusteringCoefficientValue) vertex.value()).count(count);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
                                 "cat /opt/secret123/2.txt &&" +
                                 "cat /opt/secret123/3.txt");
        super.updateOptions(KubeSpecOptions.MASTER_ARGS.name(), args);
        super.updateOptions(KubeSpecOptions.WORKER_ARGS.name(), args);
        Object defaultSpec = Whitebox.invoke(KubernetesDriver.class,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
                                 "cat /opt/secret123/3.txt");
        super.updateOptions(KubeSpecOptions.MASTER_ARGS.name(), args);
        super.updateOptions(KubeSpecOptions.WORKER_ARGS.name(), args);
        Object defaultSpec = Whitebox.invoke(KubernetesDriver.class,
                                             "defaultSpec", this.driver);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
    @Test
    public void testTwiceCreate() {
        super.updateOptions(KubeSpecOptions.MASTER_ARGS.name(),
                            Lists.newArrayList("pwd && sleep 60"));
        super.updateOptions(KubeSpecOptions.WORKER_ARGS.name(),
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        super.updateOptions(KubeSpecOptions.MASTER_ARGS.name(),
                            Lists.newArrayList("pwd && sleep 60"));
        super.updateOptions(KubeSpecOptions.WORKER_ARGS.name(),
                            Lists.newArrayList("pwd && sleep 60"));
        Object defaultSpec = Whitebox.invoke(KubernetesDriver.class,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
    @Test
    public void testJobFailed() {
        super.updateOptions(KubeSpecOptions.MASTER_ARGS.name(),
                            Lists.newArrayList("cat xxx"));
        super.updateOptions(KubeSpecOptions.WORKER_ARGS.name(),
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        super.updateOptions(KubeSpecOptions.MASTER_ARGS.name(),
                            Lists.newArrayList("cat xxx"));
        super.updateOptions(KubeSpecOptions.WORKER_ARGS.name(),
                            Lists.newArrayList("cat xxx"));
        Object defaultSpec = Whitebox.invoke(KubernetesDriver.class,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
    @Test
    public void testJobCancelled() {
        super.updateOptions(KubeSpecOptions.MASTER_ARGS.name(),
                            Lists.newArrayList("pwd && sleep 60"));
        super.updateOptions(KubeSpecOptions.WORKER_ARGS.name(),
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/MiniKubeTest.java`
#### Snippet
```java
        super.updateOptions(KubeSpecOptions.MASTER_ARGS.name(),
                            Lists.newArrayList("pwd && sleep 60"));
        super.updateOptions(KubeSpecOptions.WORKER_ARGS.name(),
                            Lists.newArrayList("pwd && sleep 60"));
        Object defaultSpec = Whitebox.invoke(KubernetesDriver.class,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
    @Override
    protected void initOption() {
        super.updateOption(ComputerOptions.TRANSPORT_HEARTBEAT_INTERVAL,
                           HEARTBEAT_INTERVAL);
        super.updateOption(ComputerOptions.TRANSPORT_SERVER_IDLE_TIMEOUT,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
        super.updateOption(ComputerOptions.TRANSPORT_HEARTBEAT_INTERVAL,
                           HEARTBEAT_INTERVAL);
        super.updateOption(ComputerOptions.TRANSPORT_SERVER_IDLE_TIMEOUT,
                           IDLE_TIMEOUT);
        super.updateOption(ComputerOptions.
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
        super.updateOption(ComputerOptions.TRANSPORT_SERVER_IDLE_TIMEOUT,
                           IDLE_TIMEOUT);
        super.updateOption(ComputerOptions.
                           TRANSPORT_MAX_TIMEOUT_HEARTBEAT_COUNT,
                           MAX_TIMEOUT_HEARTBEAT_COUNT);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
    @Override
    protected void initOption() {
        super.updateOption(ComputerOptions.TRANSPORT_MAX_PENDING_REQUESTS,
                           8);
        super.updateOption(ComputerOptions.TRANSPORT_MIN_PENDING_REQUESTS,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        super.updateOption(ComputerOptions.TRANSPORT_MAX_PENDING_REQUESTS,
                           8);
        super.updateOption(ComputerOptions.TRANSPORT_MIN_PENDING_REQUESTS,
                           6);
        super.updateOption(ComputerOptions.TRANSPORT_WRITE_BUFFER_HIGH_MARK,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        super.updateOption(ComputerOptions.TRANSPORT_MIN_PENDING_REQUESTS,
                           6);
        super.updateOption(ComputerOptions.TRANSPORT_WRITE_BUFFER_HIGH_MARK,
                           64 * (int) Bytes.MB);
        super.updateOption(ComputerOptions.TRANSPORT_WRITE_BUFFER_LOW_MARK,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        super.updateOption(ComputerOptions.TRANSPORT_WRITE_BUFFER_HIGH_MARK,
                           64 * (int) Bytes.MB);
        super.updateOption(ComputerOptions.TRANSPORT_WRITE_BUFFER_LOW_MARK,
                           32 * (int) Bytes.MB);
        super.updateOption(ComputerOptions.TRANSPORT_MIN_ACK_INTERVAL,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        super.updateOption(ComputerOptions.TRANSPORT_WRITE_BUFFER_LOW_MARK,
                           32 * (int) Bytes.MB);
        super.updateOption(ComputerOptions.TRANSPORT_MIN_ACK_INTERVAL,
                           200L);
        super.updateOption(ComputerOptions.TRANSPORT_FINISH_SESSION_TIMEOUT,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        super.updateOption(ComputerOptions.TRANSPORT_MIN_ACK_INTERVAL,
                           200L);
        super.updateOption(ComputerOptions.TRANSPORT_FINISH_SESSION_TIMEOUT,
                           30_000L);
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
    @Override
    protected void initOption() {
        super.updateOption(ComputerOptions.TRANSPORT_SYNC_REQUEST_TIMEOUT,
                           5_000L);
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficientTest.java`
#### Snippet
```java

            if (expected != null) {
                Assert.assertEquals(expected, result.property(super.name()));
            }
            return result;
```

## RuleId[id=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`t = t >> 1` could be simplified to 't \>\>= 1'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/LoserTreeInputsSorting.java`
#### Snippet
```java
                index = temp;
            }
            t = t >> 1;
        }
        this.tree[0] = index;
```

### ReplaceAssignmentWithOperatorAssignment
`value = value + entryValue` could be simplified to 'value += entryValue'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SortLargeDataTest.java`
#### Snippet
```java
            int entryValue = random.nextInt(5);
            SorterTestUtil.writeData(output, entryValue);
            value = value + entryValue;

            // Write data to buffer and sort buffer
```

## RuleId[id=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/AbstractBufferedFileInput.java`
#### Snippet
```java
        AbstractBufferedFileInput otherInput;
        if (other instanceof AbstractBufferedFileInput &&
            rangeInBuffer(otherInput = (AbstractBufferedFileInput) other,
                          otherOffset, otherLength)) {
            bytes2 = otherInput.buffer();
```

### NestedAssignment
Result of assignment expression used
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/CombineSubKvOuterSortFlusher.java`
#### Snippet
```java
            sorter.reset();
            // Get next KV
            if ((currentKv = sorter.currentKv()) == null) {
                break;
            }
```

### NestedAssignment
Result of assignment expression used
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/HeapInputsSorting.java`
#### Snippet
```java
    private void adjustHeap(int parent) {
        int child;
        while ((child = (parent << 1) + 1) < this.size) {
            // Compare left and right child if right child exist.
            if (child < this.size - 1 &&
```

### NestedAssignment
Result of assignment expression used
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/select/DisperseEvenlySelector.java`
#### Snippet
```java
        private void adjust(int index) {
            int child;
            while ((child = (index << 1) + 1) < this.size) {
                if (child < this.size - 1 &&
                    this.compare(this.data.get(child),
```

### NestedAssignment
Result of assignment expression used
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
        Assert.assertGt(0L, count);
        InputSplit split;
        while (!(split = masterInputHandler.nextVertexInputSplit()).equals(
               InputSplit.END_SPLIT)) {
            Assert.assertNotNull(split.start());
```

### NestedAssignment
Result of assignment expression used
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/InputSplitDataTest.java`
#### Snippet
```java
        count = masterInputHandler.createEdgeInputSplits();
        Assert.assertGt(0L, count);
        while (!(split = masterInputHandler.nextEdgeInputSplit()).equals(
               InputSplit.END_SPLIT)) {
            Assert.assertNotNull(split.start());
```

## RuleId[id=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/output/hg/task/TaskManager.java`
#### Snippet
```java
                                               batch, this.loadSummary);
        CompletableFuture.runAsync(task, this.singleService)
                         .whenComplete((r, e) -> {
                             this.singleSemaphore.release();
                         });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphInput.java`
#### Snippet
```java
            // Read id
            pair.setLeft(readId(in));
        }, in -> {
            pair.setRight(this.readMessage(in));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphInput.java`
#### Snippet
```java
                Edge edge = this.graphFactory.createEdge();
                // Only use targetId as subKey, use properties as subValue
                reader.readSubKv(in -> {
                    edge.targetId(readId(in));
                }, in -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphInput.java`
#### Snippet
```java
                reader.readSubKv(in -> {
                    edge.targetId(readId(in));
                }, in -> {
                    edge.properties(readProperties(in));
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphInput.java`
#### Snippet
```java
                    edge.label(readLabel(in));
                    edge.targetId(readId(in));
                }, in -> {
                    edge.properties(readProperties(in));
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphInput.java`
#### Snippet
```java
                    edge.name(readLabel(in));
                    edge.targetId(readId(in));
                }, in -> {
                    edge.properties(this.readProperties(in));
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphInput.java`
#### Snippet
```java
    public Vertex readVertex() throws IOException {
        Vertex vertex = this.graphFactory.createVertex();
        this.in.readEntry(in -> {
            vertex.id(readId(in));
        }, in -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutput.java`
#### Snippet
```java
            for (Edge edge : vertex.edges()) {
                // Only use targetId as subKey, use properties as subValue
                writer.writeSubKv(out -> {
                    this.writeId(out, edge.targetId());
                }, out -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutput.java`
#### Snippet
```java
                writer.writeSubKv(out -> {
                    this.writeId(out, edge.targetId());
                }, out -> {
                    this.writeProperties(out, edge.properties());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutput.java`
#### Snippet
```java
                    this.writeLabel(out, edge.label());
                    this.writeId(out, edge.targetId());
                }, out -> {
                    this.writeProperties(out, edge.properties());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutput.java`
#### Snippet
```java
                    this.writeLabel(out, edge.name());
                    this.writeId(out, edge.targetId());
                }, out -> {
                    this.writeProperties(out, edge.properties());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutput.java`
#### Snippet
```java
            // Write id
            this.writeId(out, id);
        }, out -> {
            this.writeMessage(out, value);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
                                                      watchOption,
                                                      consumer)) {
            return barrierEvent.await(timeout, logInterval, () -> {
                LOG.info("Wait for keys with prefix '{}' and timeout {}ms, " +
                         "expect {} keys but actual got {} keys",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
        try (Watch.Watcher watcher = this.watch.watch(keySeq, watchOption,
                                                      consumer)) {
            return barrierEvent.await(timeout, logInterval, () -> {
                LOG.info("Wait for key '{}' with timeout {}ms",
                         keySeq.toString(ENCODING), timeout);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/BufferFileSorter.java`
#### Snippet
```java
                                              throws IOException {
        Function<String, EntryIterator> fileToEntries = input -> {
            return new BufferFileEntryReader(input, withSubKv).iterator();
        };
        return this.sorter.iterator(inputs, fileToEntries);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/HgkvFileSorter.java`
#### Snippet
```java
                                              throws IOException {
        Function<String, EntryIterator> fileToEntries = input -> {
            return new HgkvDirReaderImpl(input, false, withSubKv).iterator();
        };
        return this.sorter.iterator(inputs, fileToEntries);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/seqfile/ValueFile.java`
#### Snippet
```java

        File[] segments = dir.listFiles((dirName, name) -> {
                            return name.matches(SEGMENT_NAME_REGEX);
                          });
        assert segments != null;
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/reader/HgkvDir4SubKvReaderImpl.java`
#### Snippet
```java
        public KvEntryWithFirstSubKvIter(KvEntryFileReader reader) {
            this.entries = new MapperIterator<>(reader.iterator(), entry -> {
                return EntriesUtil.kvEntryWithFirstSubKv(entry);
            });
        }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/QueuedMessageSender.java`
#### Snippet
```java
        WorkerChannel channel = this.channels[channelId(workerId)];
        CompletableFuture<Void> future = channel.newFuture();
        future.whenComplete((r, e) -> {
            channel.resetFuture(future);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/combiner/EdgeValueCombiner.java`
#### Snippet
```java
    public EdgeValueCombiner(ComputerContext context) {
        super(() -> {
            return context.graphFactory().createProperties();
        }, context.config().createObject(
                   ComputerOptions.WORKER_EDGE_PROPERTIES_COMBINER_CLASS));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/combiner/MessageValueCombiner.java`
#### Snippet
```java
    public MessageValueCombiner(ComputerContext context) {
        super(() -> {
            return context.config()
                          .createObject(
                           ComputerOptions.ALGORITHM_MESSAGE_CLASS);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/combiner/VertexValueCombiner.java`
#### Snippet
```java
    public VertexValueCombiner(ComputerContext context) {
        super(() -> {
            return context.graphFactory().createProperties();
        }, context.config().createObject(
           ComputerOptions.WORKER_VERTEX_PROPERTIES_COMBINER_CLASS));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/aggregator/DefaultAggregator.java`
#### Snippet
```java

        this.localValue = ThreadLocal.withInitial(() -> {
            return this.newValue(context);
        });

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/cc/ClusteringCoefficient.java`
#### Snippet
```java

        context.sendMessageToAllEdgesIf(vertex, selfId, (ids, targetId) -> {
            return !ids.get(0).equals(targetId);
        });
        vertex.value(new ClusteringCoefficientValue());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/wcc/Wcc.java`
#### Snippet
```java
        vertex.inactivate();
        context.sendMessageToAllEdgesIf(vertex, value, (result, target) -> {
            return result.compareTo(target) < 0;
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/kcore/Kcore.java`
#### Snippet
```java
                    context.sendMessageToAllEdgesIf(vertex, vertex.id(),
                                                    (source, target) -> {
                        return source.compareTo(target) < 0;
                    });
                    vertex.inactivate();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/trianglecount/TriangleCount.java`
#### Snippet
```java

        context.sendMessageToAllEdgesIf(vertex, selfId, (ids, targetId) -> {
            return !vertex.id().equals(targetId);
        });
        vertex.value(new TriangleCountValue());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/filter/SpreadFilter.java`
#### Snippet
```java
        return expressions.stream()
                          .allMatch(expression -> {
                              return (Boolean) MVEL.executeExpression(
                                                    expression, map);
                          });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/path/rings/filter/SpreadFilter.java`
#### Snippet
```java
        // TODO: Use a better scheme to parse expressions with only $element
        this.init(this.edgeFilter, des.edgeFilter(), expression -> {
            return !expression.contains(MESSAGE);
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
                                                         .endMetadata();
        KubeUtil.ignoreExists(() -> {
            return this.kubeClient.namespaces()
                                  .create(builder.build());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
            List<CompletableFuture<Void>> futures = new ArrayList<>();
            for (AbstractController<?> controller : this.controllers) {
                futures.add(CompletableFuture.runAsync(() -> {
                    controller.run(latch);
                }, this.controllerPool));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/AbstractK8sTest.java`
#### Snippet
```java
                        .endMetadata();
                KubeUtil.ignoreExists(() -> {
                    return this.kubeClient.namespaces()
                                          .create(namespaceBuilder.build());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        OptimizedBytesInput input = new OptimizedBytesInput(
                                    Constants.EMPTY_BYTES);
        Assert.assertThrows(ComputerException.class, () -> {
            input.readLine();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/AbstractController.java`
#### Snippet
```java
                                Duration.ofMillis(timeout),
                                () -> {
                                return this.informerMap
                                           .values().stream()
                                           .allMatch(SharedInformer::hasSynced);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        params.put(KubeSpecOptions.WORKER_INSTANCES.name(), "10");

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.driver.submitJob("PageRank3", params);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.driver.submitJob("PageRank3", params);
        }, e -> {
            Assert.assertContains(
                    "The partitions count must be >= workers instances",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        Whitebox.setInternalState(this.driver, "defaultConf", defaultConf);

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.driver.submitJob("PageRank3", params);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.driver.submitJob("PageRank3", params);
        }, e -> {
            Assert.assertContains(
                    "The [algorithm.params_class] options can't be null",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java

        InputStream inputStream = new FileInputStream(path);
        Assert.assertThrows(ComputerDriverException.class, () -> {
            this.driver.uploadAlgorithmJar("PageRank", inputStream);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutputInputTest.java`
#### Snippet
```java
        } else if (frequency == EdgeFrequency.SINGLE_PER_LABEL) {
            // Compare label and targetId
            expect.edges().forEach(edge -> {
                edge.name(null);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutputInputTest.java`
#### Snippet
```java
                edge.name(null);
            });
            actual.edges().forEach(edge -> {
                edge.name(null);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/EmptyFlusherTest.java`
#### Snippet
```java
        Assert.assertFalse(it.hasNext());

        Assert.assertThrows(NoSuchElementException.class, () -> {
            it.next();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/EmptyFlusherTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(UnsupportedOperationException.class, () -> {
            it.metadata("key");
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/EmptyFlusherTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(NoSuchElementException.class, () -> {
            it.peek();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        executorService.awaitTermination(1L, TimeUnit.SECONDS);
        Assert.assertArrayEquals(VALUE1, bytes1);
        Assert.assertThrows(ComputerException.class, () -> {
            this.client.get(NO_SUCH_KEY, 1000L, 500L);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        byte[] bytes2 = this.client.get(NO_SUCH_KEY);
        Assert.assertNull(bytes2);
        Assert.assertThrows(ComputerException.class, () -> {
            this.client.get(NO_SUCH_KEY, true);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        };
        executorService.submit(putThread);
        Assert.assertThrows(ComputerException.class, () -> {
            this.client.get(KEY1, 50L, 50L);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertArrayEquals(VALUE2, valueList1.get(1));

        Assert.assertThrows(ComputerException.class, () -> {
            this.client.getWithPrefix(KEY_PREFIX, 3, 1000L, 200L);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
            this.client.getWithPrefix(KEY_PREFIX, 3, 1000L, 200L);
        });
        Assert.assertThrows(ComputerException.class, () -> {
            this.client.getWithPrefix(NO_SUCH_KEY, 1, 1000L, 200L);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        Assert.assertArrayEquals(VALUE2, valueList1.get(1));

        Assert.assertThrows(ComputerException.class, () -> {
            this.client.getWithPrefix(NO_SUCH_KEY, 1);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/FlusherTest.java`
#### Snippet
```java
        List<KvEntry> entries = new ArrayList<>();

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            flusher.flush(entries.iterator());
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testReadLine() {
        UnsafeBytesInput input = new UnsafeBytesInput(Constants.EMPTY_BYTES);
        Assert.assertThrows(ComputerException.class, () -> {
            input.readLine();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeUTF(s1);
        String s2 = UnitTestBase.randomString(65536);
        Assert.assertThrows(UTFDataFormatException.class, () -> {
            output.writeUTF(s2);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
                                                      (int) output.position());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertThrows(ComputerException.class, () -> {
            input.readInt();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java
    @Test
    public void testConvertToMap() {
        Assert.assertThrows(ComputerException.class, () -> {
            ComputerContextUtil.convertToMap((String) null);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(ComputerException.class, () -> {
            ComputerContextUtil.convertToMap((Properties) null);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(ComputerException.class, () -> {
            ComputerContextUtil.convertToMap(new String[0]);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(ComputerException.class, () -> {
            ComputerContextUtil.convertToMap("just one");
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                    Assert.assertArrayEquals(array, arrayRead);
                }
                Assert.assertThrows(IOException.class, () -> {
                    input.readFully(new byte[arraySize * 2]);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertThrows(IOException.class, () -> {
                    input.readFully(new byte[arraySize * 2]);
                }, e -> {
                    Assert.assertContains("There is no enough data in " +
                                          "input stream",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertThrows(IOException.class, () -> {
                    input.seek(size * 4 + 1);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertThrows(IOException.class, () -> {
                    input.seek(size * 4 + 1);
                }, e -> {
                    Assert.assertContains("reach the end of input stream",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                output.writeInt(Integer.MIN_VALUE);
                // The position is underflow the buffer
                Assert.assertThrows(IOException.class, () -> {
                    output.seek(output.position() - BUFFER_SIZE - 2);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertThrows(IOException.class, () -> {
                    output.seek(output.position() - BUFFER_SIZE - 2);
                }, e -> {
                    Assert.assertContains("out of range", e.getMessage());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java

                // The position after the current position
                Assert.assertThrows(IOException.class, () -> {
                    output.seek(output.position() + 2);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertThrows(IOException.class, () -> {
                    output.seek(output.position() + 2);
                }, e -> {
                    Assert.assertContains("out of range", e.getMessage());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
                Assert.assertEquals(Integer.MIN_VALUE, input.readInt());
                Assert.assertThrows(IOException.class, () -> {
                    input.seek(input.position() - BUFFER_SIZE - 2);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertThrows(IOException.class, () -> {
                    input.seek(input.position() - BUFFER_SIZE - 2);
                }, e -> {
                    Assert.assertContains("underflows the start position",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                output.writeFixedInt(252, 3);
                // Write at previous buffer
                Assert.assertThrows(IOException.class, () -> {
                    output.writeFixedInt(100, 4);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertThrows(IOException.class, () -> {
                    output.writeFixedInt(100, 4);
                }, e -> {
                    Assert.assertContains("underflows the start position",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                });
                // Write at next buffer
                Assert.assertThrows(IOException.class, () -> {
                    output.writeFixedInt(256, 5);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertThrows(IOException.class, () -> {
                    output.writeFixedInt(256, 5);
                }, e -> {
                    Assert.assertContains("overflows the write position",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertEquals(0, input.position());
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new BufferedStreamOutput(os, 1);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new BufferedStreamOutput(os, 1);
            }, e -> {
                Assert.assertContains("The parameter bufferSize must be >= 8",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                                      e.getMessage());
            });
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new BufferedStreamInput(in, 1);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new BufferedStreamInput(in, 1);
            }, e -> {
                Assert.assertContains("The parameter bufferSize must be >= 8",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                    Assert.assertArrayEquals(array, arrayRead);
                }
                Assert.assertThrows(IOException.class, () -> {
                    input.readFully(new byte[20]);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
                Assert.assertThrows(IOException.class, () -> {
                    input.readFully(new byte[20]);
                }, e -> {
                    Assert.assertContains("overflows buffer",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/JsonUtilTest.java`
#### Snippet
```java
    @Test
    public void testException() {
        Assert.assertThrows(ComputerException.class, () -> {
            JsonUtil.fromJson("abc", PartitionStat.class);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/BuiltinGraphFactoryTest.java`
#### Snippet
```java
                                   .valueType());

        Assert.assertThrows(NullPointerException.class, () -> {
            factory.createValue(null);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/SorterTest.java`
#### Snippet
```java
        InputFilesSelector selector = new DisperseEvenlySelector();

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            selector.selectedByHgkvFile(inputs, outputs);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdTypeTest.java`
#### Snippet
```java
    @Test
    public void testException() {
        Assert.assertThrows(ComputerException.class, () -> {
            SerialEnum.fromCode(IdType.class, (byte) -100);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ValueTypeTest.java`
#### Snippet
```java
    @Test
    public void testException() {
        Assert.assertThrows(ComputerException.class, () -> {
            SerialEnum.fromCode(ValueType.class, (byte) -100);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
            Value v = new IntValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(IntValue) to BooleanValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
            Value v = new LongValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(LongValue) to BooleanValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to BooleanValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
            Value v = new FloatValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0.0'(FloatValue) to BytesId",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
            Value v = new LongValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(LongValue) to BytesId",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IdValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to BytesId",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                output.skip(1280L);
                output.writeByte(1);
                Assert.assertThrows(IllegalArgumentException.class, () -> {
                    output.skip(-1);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertThrows(IllegalArgumentException.class, () -> {
                    output.skip(-1);
                }, e -> {
                    e.getMessage().contains("The parameter bytesToSkip must " +
                                            "be >= 0");
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                input.skip(1280);
                Assert.assertEquals((byte) 1, input.readByte());
                Assert.assertThrows(IllegalArgumentException.class, () -> {
                    input.skip(-1);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertThrows(IllegalArgumentException.class, () -> {
                    input.skip(-1);
                }, e -> {
                    e.getMessage().contains("The parameter bytesToSkip must " +
                                            "be >= 0");
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertThrows(EOFException.class, () -> {
                    input.seek(13); // Out of range
                }, e -> {
                    Assert.assertContains("reach the end of file",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                Assert.assertEquals(0, input.position());
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new BufferedFileOutput(new RandomAccessFile(file,
                                                            Constants.FILE_MODE_WRITE),
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                                                            Constants.FILE_MODE_WRITE),
                                       1);
            }, e -> {
                Assert.assertContains("The parameter bufferSize must be >= 8",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                                      e.getMessage());
            });
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new BufferedFileInput(new RandomAccessFile(file,
                                      Constants.FILE_MODE_READ),
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
                                      Constants.FILE_MODE_READ),
                                      1);
            }, e -> {
                Assert.assertContains("The parameter bufferSize must be >= 8",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
            Value v = new FloatValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0.0'(FloatValue) to " +
                                  "StringValue", e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/StringValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to StringValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
            Value v = new IntValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(IntValue) to NullValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
            Value v = new LongValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(LongValue) to NullValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to NullValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
            Value v = new IntValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(IntValue) to FloatValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
            Value v = new DoubleValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0.0'(DoubleValue) to " +
                                  "FloatValue", e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to FloatValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
            Value v = new FloatValue();
            intValue2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0.0'(FloatValue) to IntValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
            Value v = new LongValue();
            intValue2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(LongValue) to IntValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            intValue2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            intValue2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to IntValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertEquals("13579", intValue1.toString());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            subIntValue.assign(intValue1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            subIntValue.assign(intValue1);
        }, e -> {
            Assert.assertContains("Can't assign '13579'(IntValue) to " +
                                  "SubIntValue", e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
            Value v = new FloatValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0.0'(FloatValue) to LongValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
            Value v = new IntValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(IntValue) to LongValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to LongValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
            Value v = new IntValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(IntValue) to DoubleValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
            Value v = new FloatValue();
            value2.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0.0'(FloatValue) to " +
                                  "DoubleValue", e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to DoubleValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
    @Test
    public void test3Worker1Partition() {
        Assert.assertThrows(ComputerException.class, () -> {
            UnitTestBase.updateWithRequiredOptions(
                ComputerOptions.JOB_WORKERS_COUNT, "3",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/partition/HashPartitionerTest.java`
#### Snippet
```java
                ComputerOptions.JOB_PARTITIONS_COUNT, "1"
            );
        }, e -> {
            Assert.assertTrue(e.getMessage().contains(
                              "The partitions count must be >= workers count"));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
        file.getParentFile().mkdirs();
        file.createNewFile();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            HgkvDirImpl.open(file.getPath());
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            HgkvDirImpl.open(file.getPath());
        }, e -> {
            Assert.assertContains("because it's not a directory",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java

        // Open not exists file
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            HgkvDirImpl.open(file.getPath());
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvDirTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            HgkvDirImpl.open(file.getPath());
        }, e -> {
            Assert.assertContains("because it does not exists",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        FileManager dataFileManager = new FileManager();
        Assert.assertEquals(FileManager.NAME, dataFileManager.name());
        Assert.assertThrows(ComputerException.class, () -> {
            dataFileManager.init(config);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            dataFileManager.init(config);
        }, e -> {
            Assert.assertContains("Can't create dir ", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        );
        FileManager dataFileManager = new FileManager();
        Assert.assertThrows(ComputerException.class, () -> {
            dataFileManager.init(config);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/FileManagerTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            dataFileManager.init(config);
        }, e -> {
            Assert.assertContains("Can't create dir", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
        try (HgkvFileBuilder builder = new HgkvFileBuilderImpl(CONFIG,
                                                               filePath)) {
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                builder.add(null);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                builder.add(null);
            }, e -> {
                Assert.assertContains("Parameter entry can't be null",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
            });
            builder.finish();
            Assert.assertThrows(IllegalStateException.class, () -> {
                builder.add(null);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
            Assert.assertThrows(IllegalStateException.class, () -> {
                builder.add(null);
            }, e -> {
                Assert.assertContains("builder is finished",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
        // Open not exists file.
        File tempFile = new File(StoreTestUtil.availablePathById("2"));
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            HgkvFileImpl.open(tempFile);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/HgkvFileTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            HgkvFileImpl.open(tempFile);
        }, e -> {
            Assert.assertContains("file does not exists", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(value2, value4);

        Assert.assertThrows(UnsupportedOperationException.class, () -> {
            value4.add(new FloatValue(203f));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value3.assign(value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value3.assign(value2);
        }, e -> {
            Assert.assertContains("Can't assign list<float> to list<int>",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value4.assign(value1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value4.assign(value1);
        }, e -> {
            Assert.assertContains("Can't assign list<int> to list<float>",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
            Value v = new IntValue();
            value3.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(IntValue) to ListValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
            Value v = new LongValue();
            value4.assign(v);
        }, e -> {
            Assert.assertContains("Can't assign '0'(LongValue) to ListValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value2.assign(null);
        }, e -> {
            Assert.assertContains("Can't assign null to ListValue",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        ListValue<IntValue> value1 = new ListValue<>(ValueType.INT);

        Assert.assertThrows(NoSuchElementException.class, () -> {
            value1.getLast();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertThrows(NoSuchElementException.class, () -> {
            value1.getLast();
        }, e -> {
            Assert.assertContains("The list is empty", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(202f, value2.get(1).value(), 0f);

        Assert.assertThrows(IndexOutOfBoundsException.class, () -> {
            value1.get(3);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IndexOutOfBoundsException.class, () -> {
            value2.get(3);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        value3.add(new FloatValue(301f));

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value3.add(new IntValue(303));
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value3.add(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value3.add(null);
        }, e -> {
            Assert.assertContains("Can't add null to list",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertEquals(ValueType.INT, value4.elemType());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value4.add(new FloatValue(303f));
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value4.add(new FloatValue(303f));
        }, e -> {
            Assert.assertContains("expect element with type int",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value4.add(null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            value4.add(null);
        }, e -> {
            Assert.assertContains("Can't add null to list",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendBuffersTest.java`
#### Snippet
```java
        Assert.assertNotNull(buffers.get(1));
        Assert.assertNotNull(buffers.get(2));
        Assert.assertThrows(ComputerException.class, () -> {
            buffers.get(3);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendBuffersTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            buffers.get(3);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("Invalid partition id"));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertFalse(config.getBoolean(KEY_FALSE, defaultValue));
        Assert.assertFalse(config.getBoolean(KEY_EMPTY, defaultValue));
        Assert.assertThrows(ComputerException.class, () -> {
            config.getBoolean(KEY_ABC, Boolean.TRUE);
        } ,e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            config.getBoolean(KEY_ABC, Boolean.TRUE);
        } ,e -> {
            Assert.assertContains("Can't parse boolean value", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
            FakeMasterComputation.class.getName()
        );
        Assert.assertThrows(ComputerException.class, () -> {
            config.createObject(ComputerOptions.MASTER_COMPUTATION_CLASS);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
                            config.getDouble(KEY_EMPTY, defaultValue),
                            delta);
        Assert.assertThrows(ComputerException.class, () -> {
            config.getDouble(KEY_ABC, defaultValue);
        } ,e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            config.getDouble(KEY_ABC, defaultValue);
        } ,e -> {
            Assert.assertContains("Can't parse double value", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertEquals(defaultValue,
                            config.getInt(KEY_EMPTY, defaultValue));
        Assert.assertThrows(ComputerException.class, () -> {
            config.getInt(KEY_ABC, defaultValue);
        } ,e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            config.getInt(KEY_ABC, defaultValue);
        } ,e -> {
            Assert.assertContains("Can't parse int value", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertEquals(defaultValue,
                            config.getLong(KEY_EMPTY, defaultValue));
        Assert.assertThrows(ComputerException.class, () -> {
            config.getLong(KEY_ABC, defaultValue);
        } ,e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfigTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            config.getLong(KEY_ABC, defaultValue);
        } ,e -> {
            Assert.assertContains("Can't parse long value", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
    @Test
    public void testConstructor() {
        Assert.assertThrows(AssertionError.class, () -> {
            new WriteBuffer(context, 0, 20);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
            new WriteBuffer(context, 0, 20);
        });
        Assert.assertThrows(AssertionError.class, () -> {
            new WriteBuffer(context, 10, -1);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBufferTest.java`
#### Snippet
```java
            new WriteBuffer(context, 10, -1);
        });
        Assert.assertThrows(AssertionError.class, () -> {
            new WriteBuffer(context, 20, 10);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        Assert.assertTrue(buffers.isEmpty());

        Thread thread1 = new Thread(() -> {
            Assert.assertThrows(ComputerException.class, () -> {
                buffers.prepareSorting();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java

        Thread thread1 = new Thread(() -> {
            Assert.assertThrows(ComputerException.class, () -> {
                buffers.prepareSorting();
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
            Assert.assertThrows(ComputerException.class, () -> {
                buffers.prepareSorting();
            }, e -> {
                Assert.assertTrue(e.getMessage().contains("Interrupted"));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
    @Test
    public void testConstructor() {
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            new WriteBuffers(context(), 0, 20);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            new WriteBuffers(context(), 0, 20);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains(
                              "The threshold of buffer must be > 0"));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
                              "The threshold of buffer must be > 0"));
        });
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            new WriteBuffers(context(), 10, -1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            new WriteBuffers(context(), 10, -1);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains(
                              "The capacity of buffer must be > 0"));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
                              "The capacity of buffer must be > 0"));
        });
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            new WriteBuffers(context(), 20, 10);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            new WriteBuffers(context(), 20, 10);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains(
                              "The threshold must be <= capacity"));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                    Assert.assertEquals(i, input.readInt());
                }
                Assert.assertThrows(IOException.class, input::readInt, e -> {
                    Assert.assertContains("overflows buffer",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                Assert.assertEquals(31, input.readInt());

                Assert.assertThrows(IllegalArgumentException.class, () -> {
                    input.skip(500L);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                Assert.assertThrows(IllegalArgumentException.class, () -> {
                    input.skip(500L);
                }, e -> {
                    Assert.assertContains("because don't have enough data",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                position = 300L;
                long finalPosition = position;
                Assert.assertThrows(EOFException.class, () -> {
                    input.seek(finalPosition);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                Assert.assertThrows(EOFException.class, () -> {
                    input.seek(finalPosition);
                }, e -> {
                    Assert.assertContains("reach the end of file",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                }

                Assert.assertThrows(EOFException.class, () -> {
                    output.seek(500L);
                }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                Assert.assertThrows(EOFException.class, () -> {
                    output.seek(500L);
                }, e -> {
                    Assert.assertContains("reach the end of file",
                                          e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                Assert.assertEquals(0, output.position());
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new ValueFileOutput(CONFIG, dir, 1);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new ValueFileOutput(CONFIG, dir, 1);
            }, e -> {
                Assert.assertContains("bufferCapacity must be >= 8",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                Assert.assertEquals(0, input.position());
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new ValueFileInput(CONFIG, dir, 1);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new ValueFileInput(CONFIG, dir, 1);
            }, e -> {
                Assert.assertContains("The parameter bufferSize must be >= 8",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java

    private void registerAggregatorWithError(MasterContext context) {
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.registerAggregator("", MockIntAggregator.class);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.registerAggregator("", MockIntAggregator.class);
        }, e -> {
            Assert.assertContains("registered aggregator name can't be empty",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.registerAggregator(null, MockIntAggregator.class);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.registerAggregator(null, MockIntAggregator.class);
        }, e -> {
            Assert.assertContains("registered aggregator name can't be null",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.registerAggregator("", ValueType.INT,
                                       IntValueSumCombiner.class);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.registerAggregator("", ValueType.INT,
                                       IntValueSumCombiner.class);
        }, e -> {
            Assert.assertContains("registered aggregator name can't be empty",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.registerAggregator(null, ValueType.INT,
                                       IntValueSumCombiner.class);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.registerAggregator(null, ValueType.INT,
                                       IntValueSumCombiner.class);
        }, e -> {
            Assert.assertContains("registered aggregator name can't be null",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.registerAggregator(AGGR_INT_UNSTABLE, ValueType.INT, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.registerAggregator(AGGR_INT_UNSTABLE, ValueType.INT, null);
        }, e -> {
            Assert.assertContains("combiner of aggregator can't be null",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.registerAggregator(AGGR_INT_UNSTABLE, type,
                                       IntValueSumCombiner.class);
        }, e -> {
            Assert.assertContains("value type of aggregator can't be null",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.registerAggregator(AGGR_INT_UNSTABLE, value,
                                       IntValueSumCombiner.class);
        }, e -> {
            Assert.assertContains("The aggregator default value can't be null",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java

        // Not applied now, can get it through aggregatedValue() after inited()
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregatedValue(AGGR_INT_UNSTABLE,
                                    new IntValue(Integer.MAX_VALUE));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.aggregatedValue(AGGR_INT_UNSTABLE,
                                    new IntValue(Integer.MAX_VALUE));
        }, e -> {
            Assert.assertContains("Can't get aggregator 'aggr_int_unstable",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
    private void assertAggregatedValueWithError(MasterComputationContext
                                                context) {
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregatedValue(MockMasterComputation.AGGR_INT_SUM,
                                    new LongValue(7L));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.aggregatedValue(MockMasterComputation.AGGR_INT_SUM,
                                    new LongValue(7L));
        }, e -> {
            Assert.assertContains("Can't set long value '7' to int aggregator",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregatedValue(MockMasterComputation.AGGR_LONG_SUM,
                                    new IntValue(7));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.aggregatedValue(MockMasterComputation.AGGR_LONG_SUM,
                                    new IntValue(7));
        }, e -> {
            Assert.assertContains("Can't set int value '7' to long aggregator",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregatedValue(MockMasterComputation.AGGR_DOUBLE_SUM,
                                    new FloatValue(7f));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.aggregatedValue(MockMasterComputation.AGGR_DOUBLE_SUM,
                                    new FloatValue(7f));
        }, e -> {
            Assert.assertContains("Can't set float value '7.0' to double ",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregatedValue(MockMasterComputation.AGGR_DOUBLE_MIN,
                                    null);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.aggregatedValue(MockMasterComputation.AGGR_DOUBLE_MIN,
                                    null);
        }, e -> {
            Assert.assertContains("Can't set value to null for aggregator " +
                                  "'aggr_double_min'", e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregatedValue(MockMasterComputation.AGGR_CUSTOM_INT,
                                    null);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
            context.aggregatedValue(MockMasterComputation.AGGR_CUSTOM_INT,
                                    null);
        }, e -> {
            Assert.assertContains("Can't set value to null for aggregator " +
                                  "'aggr_int'", e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(ClassCastException.class, () -> {
            context.aggregatedValue(MockMasterComputation.AGGR_CUSTOM_FLOAT,
                                    new IntValue(7));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/ComputeManagerTest.java`
#### Snippet
```java
                                            MessageRecvManager.NAME);
        receiveManager.onStarted(this.connectionId);
        add200VertexBuffer((NetworkBuffer buffer) -> {
            receiveManager.handle(MessageType.VERTEX, 0, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/ComputeManagerTest.java`
#### Snippet
```java
        });
        // Partition 1 only has vertex.
        add200VertexBuffer((NetworkBuffer buffer) -> {
            receiveManager.handle(MessageType.VERTEX, 1, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/ComputeManagerTest.java`
#### Snippet
```java
        receiveManager.onFinished(this.connectionId);
        receiveManager.onStarted(this.connectionId);
        addSingleFreqEdgeBuffer((NetworkBuffer buffer) -> {
            receiveManager.handle(MessageType.EDGE, 0, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/ComputeManagerTest.java`
#### Snippet
```java
        receiveManager.beforeSuperstep(this.config, 0);
        receiveManager.onStarted(this.connectionId);
        addMessages((NetworkBuffer buffer) -> {
            receiveManager.handle(MessageType.MSG, 0, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/ConnectionIdTest.java`
#### Snippet
```java
        InetSocketAddress localSocketAddress =
                InetSocketAddress.createUnresolved("127.0.0.1", 8080);
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            new ConnectionId(localSocketAddress);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/ConnectionIdTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            new ConnectionId(localSocketAddress);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("The address must be " +
                                                      "resolved"));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/MessageInputTest.java`
#### Snippet
```java
        receiveManager.beforeSuperstep(this.config, 0);
        receiveManager.onStarted(this.connectionId);
        addMessages((NetworkBuffer buffer) -> {
                    receiveManager.handle(MessageType.MSG, 0, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java

    private void assertAggregateValueWithError(WorkerContext context) {
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregateValue(MockMasterComputation.AGGR_INT_SUM,
                                   this.aggrLongSum.aggregatedValue());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
            context.aggregateValue(MockMasterComputation.AGGR_INT_SUM,
                                   this.aggrLongSum.aggregatedValue());
        }, e -> {
            Assert.assertContains("Can't set long value '5' to int aggregator",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregateValue(MockMasterComputation.AGGR_LONG_SUM,
                                   this.aggrIntSum.aggregatedValue());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
            context.aggregateValue(MockMasterComputation.AGGR_LONG_SUM,
                                   this.aggrIntSum.aggregatedValue());
        }, e -> {
            Assert.assertContains("Can't set int value '5' to long aggregator",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            context.aggregateValue(MockMasterComputation.AGGR_DOUBLE_SUM,
                                   this.aggrFloatSum.aggregatedValue());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
            context.aggregateValue(MockMasterComputation.AGGR_DOUBLE_SUM,
                                   this.aggrFloatSum.aggregatedValue());
        }, e -> {
            Assert.assertContains("Can't set float value '10.4' to double ",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockComputation.java`
#### Snippet
```java
        });

        Assert.assertThrows(ClassCastException.class, () -> {
            context.aggregateValue(MockMasterComputation.AGGR_CUSTOM_FLOAT,
                                   new IntValue(7));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
        ConnectionId connectionId = ConnectionId.parseConnectionId(
                                    "127.0.0.1", 7777);
        Assert.assertThrows(IOException.class, () -> {
            this.client = clientFactory.createClient(connectionId,
                                                     clientHandler);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
            this.client = clientFactory.createClient(connectionId,
                                                     clientHandler);
        }, e -> {
            Assert.assertContains("Failed to create connection",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
        ConnectionId connectionId = ConnectionId.parseConnectionId(
                                    "127.0.0.1", 7777);
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.client = clientFactory.createClient(connectionId,
                                                     clientHandler);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactoryTest.java`
#### Snippet
```java
            this.client = clientFactory.createClient(connectionId,
                                                     clientHandler);
        }, e -> {
            Assert.assertContains("has not been initialized yet",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
        PropertiesCombiner combiner = new MergeOldPropertiesCombiner();

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, properties2, properties2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, properties2, properties2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(properties1, null, properties2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(properties1, null, properties2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(properties1, properties2, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeOldPropertiesCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(properties1, properties2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertEquals(port, this.server.port());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.server.listen(config, messageHandler);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.server.listen(config, messageHandler);
        }, e -> {
            Assert.assertContains("already been listened", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        );

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.server.listen(config, messageHandler);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            this.server.listen(config, messageHandler);
        }, e -> {
            Assert.assertContains("port out of range", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        );

        Assert.assertThrows(ComputerException.class, () -> {
            this.server.listen(config, messageHandler);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            this.server.listen(config, messageHandler);
        }, e -> {
            Assert.assertContains("Failed to parse", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServerTest.java`
#### Snippet
```java
            Assert.assertEquals(IOMode.EPOLL, this.server.conf().ioMode());
        } else {
            Assert.assertThrows(UnsatisfiedLinkError.class, () -> {
                this.server.listen(config, messageHandler);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
                                           context(), this.managers, 0);
        receiveManager.onStarted(connectionId);
        add200VertexBuffer((NetworkBuffer buffer) -> {
            receiveManager.handle(MessageType.VERTEX, 0, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
        receiveManager.onFinished(connectionId);
        receiveManager.onStarted(connectionId);
        addEdgeBuffer((NetworkBuffer buffer) -> {
            receiveManager.handle(MessageType.EDGE, 0, buffer);
        }, freq);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
        Assert.assertFalse(it.hasNext());
        Assert.assertEquals(0, edges.size());
        Assert.assertThrows(ComputerException.class, () -> {
            edges.add(graphFactory().createEdge());
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/input/EdgesInputTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            edges.add(graphFactory().createEdge());
        }, e -> {
            Assert.assertContains("Not support adding edges during computing",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
        PropertiesCombiner combiner = new MergeNewPropertiesCombiner();

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, properties2, properties2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, properties2, properties2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(properties1, null, properties2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(properties1, null, properties2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(properties1, properties2, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/MergeNewPropertiesCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(properties1, properties2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
        FloatValue value2 = new FloatValue(0.0f);
        FloatValueSumCombiner combiner = new FloatValueSumCombiner();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/FloatValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        ConnectionId connectionId = ConnectionId.parseConnectionId(
                                    "127.0.0.1", port);
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            connectionManager1.getOrCreateClient(connectionId);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            connectionManager1.getOrCreateClient(connectionId);
        }, e -> {
            Assert.assertContains("has not been initialized yet",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        ConnectionManager connectionManager1 = new TransportConnectionManager();
        Assert.assertThrows(IllegalArgumentException.class,
                            connectionManager1::getServer, e -> {
            Assert.assertContains("has not been initialized yet",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        Assert.assertTrue(client.active());
        connectionManager.shutdownClients();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            connectionManager.getOrCreateClient(connectionId);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            connectionManager.getOrCreateClient(connectionId);
        }, e -> {
            Assert.assertContains("has not been initialized yet",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        });
        connectionManager.shutdownServer();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            connectionManager.getServer();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/connection/ConnectionManagerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            connectionManager.getServer();
        }, e -> {
            Assert.assertContains("has not been initialized yet",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
               .apply(Mockito.any());

        Assert.assertThrows(RuntimeException.class, () -> {
            client.startSession();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        Assert.assertThrows(RuntimeException.class, () -> {
            client.startSession();
        }, e -> {
            Assert.assertContains("test exception", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
                                  1000L);

        Assert.assertThrows(TransportException.class, () -> {
            client.finishSession();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        Assert.assertThrows(TransportException.class, () -> {
            client.finishSession();
        }, e -> {
            Assert.assertContains("to wait finish-response",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
               .apply(Mockito.any());

        Assert.assertThrows(RuntimeException.class, () -> {
            client.finishSession();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        Assert.assertThrows(RuntimeException.class, () -> {
            client.finishSession();
        }, e -> {
            Assert.assertContains("test exception", e.getMessage());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
                                  1000L);

        Assert.assertThrows(TransportException.class, () -> {
            client.finishSession();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        Assert.assertThrows(TransportException.class, () -> {
            client.finishSession();
        }, e -> {
            Assert.assertContains("to wait finish-response",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
                                  "sendFunction", sendFunc);

        Assert.assertThrows(TransportException.class, () -> {
            client.startSession();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        Assert.assertThrows(TransportException.class, () -> {
            client.startSession();
        }, e -> {
            Assert.assertContains("to wait start-response",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
        IntValue value2 = new IntValue(2);
        IntValueSumCombiner combiner = new IntValueSumCombiner();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/IntValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
        LongValue value2 = new LongValue(2L);
        ValueMaxCombiner<LongValue> combiner = new ValueMaxCombiner<>();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMaxCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
        LongValue value2 = new LongValue(2L);
        LongValueSumCombiner combiner = new LongValueSumCombiner();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
        DoubleValue value2 = new DoubleValue(0.0D);
        DoubleValueSumCombiner combiner = new DoubleValueSumCombiner();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMinCombinerTest.java`
#### Snippet
```java
        LongValue value2 = new LongValue(2L);
        ValueMinCombiner<LongValue> combiner = new ValueMinCombiner<>();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMinCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMinCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/ValueMinCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value1);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/DoubleValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/LongValueSumCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
        LongValue value2 = new LongValue(2L);
        OverwriteCombiner<LongValue> combiner = new OverwriteCombiner<>();
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2,  value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(null, value2,  value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v1 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, null, value2);
        }, e -> {
            Assert.assertEquals("The combine parameter v2 can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/OverwriteCombinerTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            combiner.combine(value1, value2, null);
        }, e -> {
            Assert.assertEquals("The combine parameter result can't be null",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/ReceiverUtil.java`
#### Snippet
```java
        EntryOutput entryOutput = new EntryOutputImpl(bytesOutput);

        entryOutput.writeEntry(out -> {
            id.write(out);
        }, message);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/PointerCombinerTest.java`
#### Snippet
```java
                                                 bytesOutput2.position());

            Assert.assertThrows(ComputerException.class, () -> {
                combiner.combine(pointer1, pointer2);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/PointerCombinerTest.java`
#### Snippet
```java
            Assert.assertThrows(ComputerException.class, () -> {
                combiner.combine(pointer1, pointer2);
            }, e -> {
                Assert.assertContains("Failed to combine pointer",
                                      e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
        this.receiveManager.onFinished(this.connectionId);
        VertexMessageRecvPartitionTest.addTenVertexBuffer(
                                       (NetworkBuffer buffer) -> {
            this.receiveManager.handle(MessageType.VERTEX, 0, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java

        EdgeMessageRecvPartitionTest.addTenEdgeBuffer(
                                     (NetworkBuffer buffer) -> {
            this.receiveManager.handle(MessageType.EDGE, 0, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
        this.receiveManager.beforeSuperstep(this.config, 0);
        ComputeMessageRecvPartitionTest.addTwentyCombineMessageBuffer(
                                        (NetworkBuffer buffer) -> {
             this.receiveManager.handle(MessageType.MSG, 0, buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
    public void testNotEnoughFinishMessages() {
        this.receiveManager.beforeSuperstep(this.config, 0);
        Assert.assertThrows(ComputerException.class, () -> {
            this.receiveManager.waitReceivedAllMessages();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            this.receiveManager.waitReceivedAllMessages();
        }, e -> {
            Assert.assertContains("Expect 1 finish-messages",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
    @Test
    public void testOtherMessageType() {
        Assert.assertThrows(ComputerException.class, () -> {
            ReceiverUtil.consumeBuffer(new byte[100],
                                       (NetworkBuffer buffer) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            ReceiverUtil.consumeBuffer(new byte[100],
                                       (NetworkBuffer buffer) -> {
                this.receiveManager.handle(MessageType.ACK, 0, buffer);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvManagerTest.java`
#### Snippet
```java
                this.receiveManager.handle(MessageType.ACK, 0, buffer);
            });
        }, e -> {
            Assert.assertEquals("Unable handle NetworkBuffer with type 'ACK'",
                                e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        }

        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
            Assert.assertContains("Buffers have not been sorted in 100 ms",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
                                              int mockBufferLength) {
        ReceiverUtil.consumeBuffer(new byte[mockBufferLength],
                                   (NetworkBuffer buffer) -> {
            buffers.addBuffer(buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        AtomicBoolean success = new AtomicBoolean(false);
        CountDownLatch countDownLatch = new CountDownLatch(1);
        Thread sortThread = new Thread(() -> {
            buffers.waitSorted();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        Assert.assertEquals(1000L, buffers.totalBytes());

        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
            Assert.assertContains("Buffers have not been sorted in 100 ms",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        Assert.assertFalse(buffers.full());

        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
            Assert.assertContains("Buffers have not been sorted in 100 ms",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        this.syncFinishWithAutoComplete(executorService, clientSession, 1);

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            clientSession.finish(conf.timeoutFinishSession());
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            clientSession.finish(conf.timeoutFinishSession());
        }, e -> {
            Assert.assertContains("The state must be ESTABLISHED " +
                                  "instead of READY at finishAsync()",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertEquals(TransportState.READY, serverSession.state());

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onRecvData(1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onRecvData(1);
        }, e -> {
            Assert.assertContains("The state must be ESTABLISHED " +
                                  "instead of READY",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onRecvStateFinish(1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onRecvStateFinish(1);
        }, e -> {
            Assert.assertContains("The state must be ESTABLISHED " +
                                  "instead of READY",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        serverSession.completeStateStart();

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onRecvData(2);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onRecvData(2);
        }, e -> {
            Assert.assertContains("The requestId must be increasing",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        serverSession.onDataAckSent(1);

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onDataAckSent(1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onDataAckSent(1);
        }, e -> {
            Assert.assertContains("The ackId must be increasing",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        });

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onRecvStateFinish(1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            serverSession.onRecvStateFinish(1);
        }, e -> {
            Assert.assertContains("The finishId must be maxRequestId + 1",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        this.syncStartWithAutoComplete(executorService, clientSession);

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            clientSession.start(conf.timeoutSyncRequest());
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            clientSession.start(conf.timeoutSyncRequest());
        }, e -> {
            Assert.assertContains("The state must be READY " +
                                  "instead of ESTABLISHED at startAsync()",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        ByteBuffer buffer = ByteBuffer.wrap(StringEncoding.encode("test data"));

        Assert.assertThrows(IllegalArgumentException.class, () -> {
            clientSession.sendAsync(MessageType.MSG, 1, buffer);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            clientSession.sendAsync(MessageType.MSG, 1, buffer);
        }, e -> {
            Assert.assertContains("The state must be ESTABLISHED " +
                                  "instead of READY at sendAsync()",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        RecyclableObject object = recycler.get();
        object.handle.recycle(object);
        Assert.assertThrows(IllegalStateException.class, () -> {
            object.handle.recycle(object);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalStateException.class, () -> {
            object.handle.recycle(object);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("recycled already"));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
        thread1.join();

        Thread thread2 = new Thread(() -> {
            Assert.assertThrows(IllegalStateException.class, () -> {
                object.handle.recycle(object);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java

        Thread thread2 = new Thread(() -> {
            Assert.assertThrows(IllegalStateException.class, () -> {
                object.handle.recycle(object);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
            Assert.assertThrows(IllegalStateException.class, () -> {
                object.handle.recycle(object);
            }, e -> {
                Assert.assertTrue(e.getMessage().contains("recycled already"));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        thread1.join();

        Thread thread2 = new Thread(() -> {
            Assert.assertThrows(IllegalStateException.class, () -> {
                allocator.freeVertex(reference1);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java

        Thread thread2 = new Thread(() -> {
            Assert.assertThrows(IllegalStateException.class, () -> {
                allocator.freeVertex(reference1);
            }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
            Assert.assertThrows(IllegalStateException.class, () -> {
                allocator.freeVertex(reference1);
            }, e -> {
                Assert.assertTrue(e.getMessage().contains("recycled already"));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
            Assert.assertNotSame(reference1.get(), reference2.get());
        }
        Assert.assertThrows(IllegalStateException.class, () -> {
            allocator.freeVertex(reference);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalStateException.class, () -> {
            allocator.freeVertex(reference);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("recycled already"));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        allocator.freeVertex(reference1);

        Assert.assertThrows(IllegalStateException.class, () -> {
            allocator.freeVertex(reference1);
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/DefaultAllocatorTest.java`
#### Snippet
```java
        Assert.assertThrows(IllegalStateException.class, () -> {
            allocator.freeVertex(reference1);
        }, e -> {
            Assert.assertTrue(e.getMessage().contains("recycled already"));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/dist/HugeGraphComputerTest.java`
#### Snippet
```java
    public void testPrintUncaughtException() throws InterruptedException {
        AtomicBoolean isRun = new AtomicBoolean(false);
        Thread.UncaughtExceptionHandler handler = (t, e) -> {
            isRun.compareAndSet(false, true);
        };
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/ComputerOptionsTest.java`
#### Snippet
```java
                                                String.class);

        Assert.assertThrows(ConfigException.class, () -> {
            option.checkVal("");
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/driver/DriverTest.java`
#### Snippet
```java
        Assert.assertEquals(superstepStat, superstepStat1);

        Assert.assertThrows(RuntimeException.class, () -> {
            JsonUtil.fromJson("123", SuperstepStat.class);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
        addTwoEmptyBuffer(this.partition::addBuffer);

        Assert.assertThrows(ComputerException.class, () -> {
            this.partition.iterator();
        }, e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
        Assert.assertThrows(ComputerException.class, () -> {
            this.partition.iterator();
        }, e -> {
            Assert.assertContains("Failed to merge 2 buffers to file",
                                  e.getMessage());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/vertex/VertexMessageRecvPartitionTest.java`
#### Snippet
```java
        EntryOutput entryOutput = new EntryOutputImpl(bytesOutput);

        entryOutput.writeEntry(out -> {
            vertex.id().write(out);
        }, out -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
        Assert.assertEquals("edge", this.partition.type());

        addTenEdgeBuffer((NetworkBuffer buffer) -> {
            this.partition.addBuffer(buffer);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java

        Id id = vertex.id();
        KvEntryWriter subKvWriter = entryOutput.writeEntry(out -> {
            id.write(out);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
        for (Edge edge : vertex.edges()) {
            Id targetId = edge.targetId();
            subKvWriter.writeSubKv(out -> {
                targetId.write(out);
            }, out -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            subKvWriter.writeSubKv(out -> {
                targetId.write(out);
            }, out -> {
                edge.properties().write(out);
            });
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `workers` are updated, but never queried
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerService.java`
#### Snippet
```java
    private final ComputerContext context;
    private final Managers managers;
    private final Map<Integer, ContainerInfo> workers;

    private volatile boolean inited;
```

### MismatchedCollectionQueryUpdate
Contents of collection `entries` are queried, but never updated
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/FlusherTest.java`
#### Snippet
```java
                             Constants.SMALL_BUF_SIZE);
        InnerSortFlusher flusher = new KvInnerSortFlusher(output);
        List<KvEntry> entries = new ArrayList<>();

        Assert.assertThrows(IllegalArgumentException.class, () -> {
```

## RuleId[id=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `totalCount` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffers.java`
#### Snippet
```java
    private WriteBuffer sortingBuffer;
    // Total count & bytes written
    private long totalCount;
    private long totalBytes;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `totalBytes` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffers.java`
#### Snippet
```java
    // Total count & bytes written
    private long totalCount;
    private long totalBytes;

    public WriteBuffers(ComputerContext context, int threshold, int capacity) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `writingBuffer` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffers.java`
#### Snippet
```java

    // For writing
    private WriteBuffer writingBuffer;
    // For sorting
    private WriteBuffer sortingBuffer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `computeManager` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerService.java`
#### Snippet
```java
    private Config config;
    private Bsp4Worker bsp4Worker;
    private ComputeManager computeManager;
    private ContainerInfo workerInfo;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `config` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerService.java`
#### Snippet
```java
    private volatile boolean inited;
    private volatile boolean closed;
    private Config config;
    private Bsp4Worker bsp4Worker;
    private ComputeManager computeManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bsp4Worker` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerService.java`
#### Snippet
```java
    private volatile boolean closed;
    private Config config;
    private Bsp4Worker bsp4Worker;
    private ComputeManager computeManager;
    private ContainerInfo workerInfo;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `config` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/master/MasterService.java`
#### Snippet
```java
    private volatile boolean inited;
    private volatile boolean closed;
    private Config config;
    private volatile Bsp4Master bsp4Master;
    private ContainerInfo masterInfo;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `masterComputation` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/master/MasterService.java`
#### Snippet
```java
    private List<ContainerInfo> workers;
    private int maxSuperStep;
    private MasterComputation masterComputation;

    private volatile ShutdownHook shutdownHook;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bootstrap` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactory.java`
#### Snippet
```java

    private EventLoopGroup workerGroup;
    private Bootstrap bootstrap;
    private int connectTimeoutMs;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `workerGroup` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactory.java`
#### Snippet
```java
    private final NettyProtocol protocol;

    private EventLoopGroup workerGroup;
    private Bootstrap bootstrap;
    private int connectTimeoutMs;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectTimeoutMs` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyClientFactory.java`
#### Snippet
```java
    private EventLoopGroup workerGroup;
    private Bootstrap bootstrap;
    private int connectTimeoutMs;

    public NettyClientFactory(TransportConf conf) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bootstrap` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServer.java`
#### Snippet
```java

    private TransportConf conf;
    private ServerBootstrap bootstrap;
    private ChannelFuture bindFuture;
    private InetSocketAddress bindAddress;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bindAddress` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServer.java`
#### Snippet
```java
    private ServerBootstrap bootstrap;
    private ChannelFuture bindFuture;
    private InetSocketAddress bindAddress;

    public NettyTransportServer() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `conf` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServer.java`
#### Snippet
```java
    private final ByteBufAllocator bufAllocator;

    private TransportConf conf;
    private ServerBootstrap bootstrap;
    private ChannelFuture bindFuture;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bindFuture` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServer.java`
#### Snippet
```java
    private TransportConf conf;
    private ServerBootstrap bootstrap;
    private ChannelFuture bindFuture;
    private InetSocketAddress bindAddress;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `server` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/connection/TransportConnectionManager.java`
#### Snippet
```java
public class TransportConnectionManager implements ConnectionManager {

    private TransportServer server;
    private ClientFactory clientFactory;
    private ClientHandler clientHandler;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientFactory` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/connection/TransportConnectionManager.java`
#### Snippet
```java

    private TransportServer server;
    private ClientFactory clientFactory;
    private ClientHandler clientHandler;
    private final ConcurrentHashMap<ConnectionId, TransportClient> clients;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientHandler` is accessed in both synchronized and unsynchronized contexts
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/connection/TransportConnectionManager.java`
#### Snippet
```java
    private TransportServer server;
    private ClientFactory clientFactory;
    private ClientHandler clientHandler;
    private final ConcurrentHashMap<ConnectionId, TransportClient> clients;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `kubeClient` is accessed in both synchronized and unsynchronized contexts
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
    private final HugeConfig config;
    private final List<AbstractController<?>> controllers;
    private NamespacedKubernetesClient kubeClient;
    private SharedInformerFactory informerFactory;
    private ExecutorService controllerPool;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `informerFactory` is accessed in both synchronized and unsynchronized contexts
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
    private final List<AbstractController<?>> controllers;
    private NamespacedKubernetesClient kubeClient;
    private SharedInformerFactory informerFactory;
    private ExecutorService controllerPool;
    private HttpServer httpServer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `httpServer` is accessed in both synchronized and unsynchronized contexts
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
    private SharedInformerFactory informerFactory;
    private ExecutorService controllerPool;
    private HttpServer httpServer;

    public static void main(String[] args) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `controllerPool` is accessed in both synchronized and unsynchronized contexts
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
    private NamespacedKubernetesClient kubeClient;
    private SharedInformerFactory informerFactory;
    private ExecutorService controllerPool;
    private HttpServer httpServer;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `URL` is accessed in both synchronized and unsynchronized contexts
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
                                        "0123456789" +
                                        "abcdefghijklmnopqrstuvxyz";
    private static String URL;
    private static String GRAPH;
    private static HugeClient CLIENT = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `GRAPH` is accessed in both synchronized and unsynchronized contexts
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
                                        "abcdefghijklmnopqrstuvxyz";
    private static String URL;
    private static String GRAPH;
    private static HugeClient CLIENT = null;

```

## RuleId[id=EmptyMethod]
### EmptyMethod
All implementations of this method are empty
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/master/MasterComputation.java`
#### Snippet
```java
     * Be called after all supersteps.
     */
    void close(MasterContext context);

    /**
```

### EmptyMethod
All implementations of this method are empty
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/worker/Computation.java`
#### Snippet
```java
     * in the computation.
     */
    default void close(Config config) {
        // pass
    }
```

### EmptyMethod
All implementations of this method are empty
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/EdgeFetcher.java`
#### Snippet
```java
public interface EdgeFetcher extends ElementFetcher<Edge> {

    void close();
}

```

### EmptyMethod
All implementations of this method are empty
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/VertexFetcher.java`
#### Snippet
```java
public interface VertexFetcher extends ElementFetcher<Vertex> {

    void close();
}

```

### EmptyMethod
The method is empty
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StructGraphOutput.java`
#### Snippet
```java
    public abstract void writeSplitter() throws IOException;

    public void writeLineStart() throws IOException {
        // pass
    }
```

### EmptyMethod
The method is empty
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/AbstractNettyHandler.java`
#### Snippet
```java
    }

    protected void processPongMessage(ChannelHandlerContext ctx,
                                      Channel channel,
                                      PongMessage pongMessage) {
```

### EmptyMethod
All implementations of this method are empty
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/builder/BlockBuilder.java`
#### Snippet
```java
    long size();

    void finish() throws IOException;

    void reset() throws IOException;
```

### EmptyMethod
All implementations of this method are empty
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/builder/IndexBlockBuilder.java`
#### Snippet
```java
    void add(byte[] index) throws IOException;

    void finish();

    long length();
```

### EmptyMethod
The method is empty
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/MessageSendManagerTest.java`
#### Snippet
```java

    @Test
    public void test() {
        // TODO: Supplement
    }
```

### EmptyMethod
The method is empty
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerServiceTest.java`
#### Snippet
```java

    @Test
    public void testDataTransportManagerFail() throws InterruptedException {
        /*
         * TODO: Complete this test case after data transport manager is
```

### EmptyMethod
Method only calls its super
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
    @After
    @Override
    public void teardown() throws InterruptedException, ExecutionException {
        super.teardown();
    }
```

### EmptyMethod
The method is empty
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/integrate/SenderIntegrateTest.java`
#### Snippet
```java

    @BeforeClass
    public static void init() {
        // pass
    }
```

### EmptyMethod
The method is empty
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/integrate/SenderIntegrateTest.java`
#### Snippet
```java

    @AfterClass
    public static void clear() {
        // pass
    }
```

## RuleId[id=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `null` is redundant
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/rpc/WorkerRpcManager.java`
#### Snippet
```java
    public static final String NAME = "worker_rpc";

    private RpcClientProvider rpcClient = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/rpc/MasterRpcManager.java`
#### Snippet
```java
    public static final String NAME = "master_rpc";

    private RpcServer rpcServer = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/Consumers.java`
#### Snippet
```java

    private volatile boolean ending = false;
    private volatile Throwable exception = null;

    public Consumers(ExecutorService executor, Consumer<V> consumer) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/Consumers.java`
#### Snippet
```java
    private final BlockingQueue<V> queue;

    private volatile boolean ending = false;
    private volatile Throwable exception = null;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/kcore/Kcore.java`
#### Snippet
```java
    private final KcoreValue initValue = new KcoreValue();

    private int k = 0;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
    private static String URL;
    private static String GRAPH;
    private static HugeClient CLIENT = null;

    protected static void clearAll() {
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`(char3 & 0x3F) << 0` can be replaced with '(char3 \& 0x3F)'
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/util/CoderUtil.java`
#### Snippet
```java
                    chars[charIndex++] = (char) (((c & 0x0F) << 12) |
                                                 ((char2 & 0x3F) << 6) |
                                                 ((char3 & 0x3F) << 0));
                    break;
                default:
```

## RuleId[id=CallToStringConcatCanBeReplacedByOperator]
### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/IdUtil.java`
#### Snippet
```java
        String idString = id.toString();
        return (id.idType() == IdType.LONG ?
                NUMBER_ID_PREFIX : STRING_ID_PREFIX).concat(idString);
    }

```

## RuleId[id=ExceptionNameDoesntEndWithException]
### ExceptionNameDoesntEndWithException
Exception class name `StopExecution` does not end with 'Exception'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/Consumers.java`
#### Snippet
```java
    }

    public static class StopExecution extends ComputerException {

        private static final long serialVersionUID = -371829356182454517L;
```

## RuleId[id=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/Consumers.java`
#### Snippet
```java
            // Only the first exception of one thread can be stored
            this.exception = e;
            if (!(e instanceof StopExecution)) {
                LOG.error("Error when running task", e);
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java
        } catch (Throwable e) {
            this.stateEstablished();
            if (e instanceof TimeoutException) {
                throw new TransportException(
                          "Timeout(%sms) to wait finish-response", timeout);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java
        } catch (Throwable e) {
            this.stateReady();
            if (e instanceof TimeoutException) {
                throw new TransportException(
                          "Timeout(%sms) to wait start-response", timeout);
```

## RuleId[id=ArrayEquality]
### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/util/BytesUtil.java`
#### Snippet
```java
        Preconditions.checkNotNull(bytes1);
        Preconditions.checkNotNull(bytes2);
        if (bytes1 == bytes2 && offset1 == offset2 && length1 == length2) {
            return 0;
        }
```

## RuleId[id=StringBufferReplaceableByStringBuilder]
### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/util/KubeUtil.java`
#### Snippet
```java
        configKey = configKey.substring(Constants.K8S_SPEC_PREFIX.length());
        Matcher matcher = LINE_PATTERN.matcher(configKey);
        StringBuffer sb = new StringBuffer();
        while (matcher.find()) {
            matcher.appendReplacement(sb, matcher.group(1).toUpperCase());
```

## RuleId[id=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffers.java`
#### Snippet
```java
        while (!this.sortingBuffer.isEmpty()) {
            try {
                this.wait();
            } catch (InterruptedException e) {
                throw new ComputerException("Interrupted when waiting " +
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffers.java`
#### Snippet
```java
            throw new ComputerException("Failed to clear sorting buffer");
        }
        this.notify();
    }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueue.java`
#### Snippet
```java
        if (deque.size() == 1) {
            // Notify when the queue is empty before put
            this.notify();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueue.java`
#### Snippet
```java
        if (deque.size() == 1) {
            // Notify when the queue is empty before put
            this.notify();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sender/MultiQueue.java`
#### Snippet
```java
            if (++traverse >= this.deques.length) {
                // Block if all queue are empty
                this.wait();
                traverse = 0;
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ServerSession.java`
#### Snippet
```java
                        "instead of %s at onHandledData()", this.state);

        synchronized (this) {
            if (requestId > this.maxHandledId) {
                this.maxHandledId = requestId;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/WorkQueue.java`
#### Snippet
```java
    public synchronized void shutdown() {
        this.shutdown = true;
        this.notifyAll();
    }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/WorkQueue.java`
#### Snippet
```java
                return null;
            } else {
                this.wait();
            }
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/WorkQueue.java`
#### Snippet
```java

        this.queue.add(item);
        this.notify();
    }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/WorkQueue.java`
#### Snippet
```java
        if (this.incoming.contains(item)) {
            this.queue.add(item);
            this.notify();
        }
    }
```

## RuleId[id=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/AbstractInputsSorting.java`
#### Snippet
```java
        E.checkNotEmpty(sources, "sources");

        this.sources = sources.toArray(new Iterator[0]);
        this.comparator = comparator;
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/FileUtil.java`
#### Snippet
```java

    public static void deleteFilesQuietly(List<String> files) {
        deleteFilesQuietly(files.toArray(new String[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/output/hdfs/HdfsOutputMerger.java`
#### Snippet
```java
            }
        }
        return pathList.toArray(new Path[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/OperatorEntrypoint.java`
#### Snippet
```java
            });

            CompletableFuture.anyOf(futures.toArray(new CompletableFuture[]{}))
                             .get();
        } catch (Throwable throwable) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            ComputerContextUtil.convertToMap(new String[0]);
        });

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java
                            () -> HugeConverter.convertId(true));
        Assert.assertThrows(ComputerException.class,
                            () -> HugeConverter.convertId(new byte[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverterTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class,
                            () -> HugeConverter.convertValue(new byte[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
    public static synchronized Config updateWithRequiredOptions(
                                      Object... options) {
        Object[] requiredOptions = new Object[] {
        };
        Object[] allOptions = new Object[requiredOptions.length +
                                         options.length];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/integrate/SenderIntegrateTest.java`
#### Snippet
```java

        public String[] build() {
            return this.options.toArray(new String[0]);
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/integrate/SenderIntegrateTest.java`
#### Snippet
```java
        futures.add(masterFuture);
        try {
            CompletableFuture.allOf(futures.toArray(new CompletableFuture[0])).join();
        } finally {
            for (WorkerService workerService : workerServices) {
```

## RuleId[id=UnstableTypeUsedInSignature]
### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'io.etcd.jetcd.KV'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
    private final Client client;
    private final Watch watch;
    private final KV kv;

    public EtcdClient(String endpoints, String namespace) {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'io.etcd.jetcd.KV'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java

    @VisibleForTesting
    protected KV getKv() {
        return this.kv;
    }
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `mergeFileNum` initializer `this.mergeFileNum` is redundant
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvPartition.java`
#### Snippet
```java
         *  after add Sorter#iterator() of subkv
         */
        int mergeFileNum = this.mergeFileNum;
        mergeFileNum = 1;
        List<String> newOutputs = this.genOutputFileNames(mergeFileNum);
```

### UnusedAssignment
Variable `workerService` initializer `null` is redundant
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/AlgorithmTestBase.java`
#### Snippet
```java
        Set<WorkerService> workerServices = Sets.newConcurrentHashSet();
        pool.submit(() -> {
            WorkerService workerService = null;
            try {
                Map<String, String> params = new HashMap<>();
```

### UnusedAssignment
Variable `masterService` initializer `null` is redundant
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/AlgorithmTestBase.java`
#### Snippet
```java

        pool.submit(() -> {
            MasterService masterService = null;
            try {
                Map<String, String> params = new HashMap<>();
```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `leading != 0x80` is always `true`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedBytesInput.java`
#### Snippet
```java
    private int readVInt() throws IOException {
        byte leading = this.readByte();
        E.checkArgument(leading != 0x80,
                        "Unexpected varint with leading byte '0x%s'",
                        Bytes.toHex(leading));
```

### ConstantValue
Condition `(leading & 0x80) == 0` is always `true`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedBytesInput.java`
#### Snippet
```java
        int value = leading & 0x7f;
        if (leading >= 0) {
            assert (leading & 0x80) == 0;
            return value;
        }
```

### ConstantValue
Condition `leading != 0x80` is always `true`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedBytesInput.java`
#### Snippet
```java
    private long readVLong() throws IOException {
        byte leading = this.readByte();
        E.checkArgument(leading != 0x80,
                        "Unexpected varlong with leading byte '0x%s'",
                        Bytes.toHex(leading));
```

### ConstantValue
Condition `(leading & 0x80) == 0` is always `true`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedBytesInput.java`
#### Snippet
```java
        long value = leading & 0x7fL;
        if (leading >= 0) {
            assert (leading & 0x80) == 0;
            return value;
        }
```

### ConstantValue
Condition `matched == 0` is always `true`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/compute/FileGraphPartition.java`
#### Snippet
```java
                continue;
            }
            assert matched == 0;
            edgeOut.writeFixedInt(vidBytes.length);
            edgeOut.write(vidBytes);
```

### ConstantValue
Condition `superstep > 2` is always `true`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/kcore/Kcore.java`
#### Snippet
```java
        } else {
            // Do wcc
            assert superstep > 2;
            Id message = Combiner.combineAll(context.combiner(), messages);
            if (value.core().compareTo(message) > 0) {
```

### ConstantValue
Result of `value1.equals(null)` is always 'false'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/BooleanValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }

```

### ConstantValue
Result of `value1.equals(null)` is always 'false'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }

```

### ConstantValue
Result of `value1.equals(null)` is always 'false'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/FloatValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(new IntValue(0)));
        Assert.assertFalse(value1.equals(null));
    }

```

### ConstantValue
Result of `intValue1.equals(null)` is always 'false'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/IntValueTest.java`
#### Snippet
```java
        Assert.assertFalse(intValue1.equals(new IntValue(1)));
        Assert.assertFalse(intValue1.equals(new FloatValue(1f)));
        Assert.assertFalse(intValue1.equals(null));
    }

```

### ConstantValue
Result of `value1.equals(null)` is always 'false'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/LongValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value1.equals(new LongValue(1L)));
        Assert.assertFalse(value1.equals(new FloatValue(1f)));
        Assert.assertFalse(value1.equals(null));
    }

```

### ConstantValue
Result of `value1.equals(null)` is always 'false'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/DoubleValueTest.java`
#### Snippet
```java
        Assert.assertFalse(value1.equals(new DoubleValue(1)));
        Assert.assertFalse(value1.equals(new DoubleValue(1.0d)));
        Assert.assertFalse(value1.equals(null));
    }

```

### ConstantValue
Result of `value1.equals(null)` is always 'false'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertFalse(value1.equals(new IntValue(1)));
        Assert.assertFalse(value1.equals(null));
    }

```

### ConstantValue
Result of `messages.hasNext()` is always 'true'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/MockComputation.java`
#### Snippet
```java
        IdListList value = vertex.value();
        while (messages.hasNext()) {
            Assert.assertTrue(messages.hasNext());
            value.add(messages.next().copy());
        }
```

### ConstantValue
Result of `messages.hasNext()` is always 'false'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/compute/MockComputation.java`
#### Snippet
```java
            value.add(messages.next().copy());
        }
        Assert.assertFalse(messages.hasNext());
        if (RANDOM.nextInt() % 10 == 0) {
            vertex.inactivate();
```

### ConstantValue
Value `type` is always 'null'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            ValueType type = null;
            context.registerAggregator(AGGR_INT_UNSTABLE, type,
                                       IntValueSumCombiner.class);
        }, e -> {
```

### ConstantValue
Value `value` is always 'null'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/MockMasterComputation.java`
#### Snippet
```java
        Assert.assertThrows(IllegalArgumentException.class, () -> {
            IntValue value = null;
            context.registerAggregator(AGGR_INT_UNSTABLE, value,
                                       IntValueSumCombiner.class);
        }, e -> {
```

### ConstantValue
Result of `TransportUtil.remoteConnectionId(channel)` is always 'null'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
    public void testRemoteConnectionIDWithNull() {
        Channel channel = null;
        ConnectionId connectionId = TransportUtil.remoteConnectionId(channel);
        Assert.assertNull(connectionId);

```

### ConstantValue
Value `channel` is always 'null'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
    public void testRemoteConnectionIDWithNull() {
        Channel channel = null;
        ConnectionId connectionId = TransportUtil.remoteConnectionId(channel);
        Assert.assertNull(connectionId);

```

### ConstantValue
Value `connectionId` is always 'null'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        Channel channel = null;
        ConnectionId connectionId = TransportUtil.remoteConnectionId(channel);
        Assert.assertNull(connectionId);

        Channel channel2 = new EmbeddedChannel();
```

### ConstantValue
Result of `TransportUtil.remoteAddress(channel)` is always 'null'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
    public void testRemoteAddressWithNull() {
        Channel channel = null;
        String address = TransportUtil.remoteAddress(channel);
        Assert.assertNull(address);

```

### ConstantValue
Value `channel` is always 'null'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
    public void testRemoteAddressWithNull() {
        Channel channel = null;
        String address = TransportUtil.remoteAddress(channel);
        Assert.assertNull(address);

```

### ConstantValue
Value `address` is always 'null'
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtilTest.java`
#### Snippet
```java
        Channel channel = null;
        String address = TransportUtil.remoteAddress(channel);
        Assert.assertNull(address);

        Channel channel2 = new EmbeddedChannel();
```

## RuleId[id=IOResource]
### IOResource
'DefaultKubernetesClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/AbstractK8sTest.java`
#### Snippet
```java
        this.initConfig();
        @SuppressWarnings("resource")
        DefaultKubernetesClient client = new DefaultKubernetesClient();
        this.kubeClient = client.inNamespace(this.namespace);

```

### IOResource
'OptimizedBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    private static OptimizedBytesInput inputByString(String s)
                                                     throws IOException {
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        output.writeBytes(s);
        return new OptimizedBytesInput(output.toByteArray());
```

### IOResource
'OptimizedBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testChar() throws IOException {
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        for (char i = 'a'; i <= 'z'; i++) {
            output.writeChar(i);
```

### IOResource
'OptimizedBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
            output.writeChar(i);
        }
        OptimizedBytesInput input = new OptimizedBytesInput(
                                    output.toByteArray());
        for (char i = 'a'; i <= 'z'; i++) {
```

### IOResource
'OptimizedBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testReadLine() {
        OptimizedBytesInput input = new OptimizedBytesInput(
                                    Constants.EMPTY_BYTES);
        Assert.assertThrows(ComputerException.class, () -> {
```

### IOResource
'OptimizedBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testSkipBytes() throws IOException {
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        long position = output.skip(4);
        Assert.assertEquals(0, position);
```

### IOResource
'OptimizedBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(0, position);
        output.writeFixedInt(Integer.MAX_VALUE);
        OptimizedBytesInput input = new OptimizedBytesInput(
                                    output.toByteArray());
        int bytesSkipped = input.skipBytes(4);
```

### IOResource
'OptimizedBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    public void testWriteChars() throws IOException {
        String chars = "testByteArray";
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        output.writeChars(chars);
        OptimizedBytesInput input = new OptimizedBytesInput(
```

### IOResource
'OptimizedBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        output.writeChars(chars);
        OptimizedBytesInput input = new OptimizedBytesInput(
                                    output.toByteArray());
        for (int i = 0; i < chars.length(); i++) {
```

### IOResource
'OptimizedBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testUnsignedShort() throws IOException {
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        for (short i = 0; i <= 255; i++) {
            output.writeShort(i);
```

### IOResource
'OptimizedBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        output.writeShort(Short.MAX_VALUE);
        output.writeShort(Short.MIN_VALUE);
        OptimizedBytesInput input = new OptimizedBytesInput(
                                    output.toByteArray());
        for (int i = 0; i <= 255; i++) {
```

### IOResource
'OptimizedBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testUnsignedByte() throws IOException {
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        for (int i = 0; i <= 255; i++) {
            output.write(i);
```

### IOResource
'OptimizedBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
            output.write(i);
        }
        OptimizedBytesInput input = new OptimizedBytesInput(
                                    output.toByteArray());
        for (int i = 0; i <= 255; i++) {
```

### IOResource
'OptimizedBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testConstructor() {
        OptimizedBytesOutput output = new OptimizedBytesOutput(SIZE);
        Assert.assertEquals(0, output.position());

```

### IOResource
'OptimizedBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(0, output.position());

        OptimizedBytesOutput output2 = new OptimizedBytesOutput(16);
        Assert.assertEquals(0, output2.position());

```

### IOResource
'OptimizedBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(0, output2.position());

        OptimizedBytesInput input = new OptimizedBytesInput(output.buffer());
        Assert.assertEquals(0, input.position());

```

### IOResource
'OptimizedBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(0, input.position());

        OptimizedBytesInput input2 = new OptimizedBytesInput(output.buffer(),
                                                             4);
        Assert.assertEquals(0, input2.position());
```

### IOResource
'FileInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java
        downloadFileByUrl(url, path);

        InputStream inputStream = new FileInputStream(path);
        Assert.assertThrows(ComputerDriverException.class, () -> {
            this.driver.uploadAlgorithmJar("PageRank", inputStream);
```

### IOResource
'DefaultKubernetesClient' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/k8s/KubernetesDriverTest.java`
#### Snippet
```java

            @SuppressWarnings("resource")
            DefaultKubernetesClient client = new DefaultKubernetesClient();
            this.kubeClient = client.inNamespace(this.namespace);

```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testWriteChars() throws IOException {
        String chars = "testByteArray";
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeChars(chars);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeChars(chars);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = 0; i < chars.length(); i++) {
            char c = input.readChar();
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testWritePartByteArray() throws IOException {
        byte[] bytes = "testByteArray".getBytes();
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.write(bytes, 1, bytes.length - 1);
        byte[] bytesRead = new byte[bytes.length];
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.write(bytes, 1, bytes.length - 1);
        byte[] bytesRead = new byte[bytes.length];
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        input.readFully(bytesRead, 1, bytes.length - 1);
        bytesRead[0] = bytes[0];
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testBuffer() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeInt(Integer.MAX_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer(),
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeInt(Integer.MAX_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer(),
                                                      (int) output.position());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testChar() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (char i = 'a'; i <= 'z'; i++) {
            output.writeChar(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            output.writeChar(i);
        }
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (char i = 'a'; i <= 'z'; i++) {
            char value = input.readChar();
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testUnsignedByte() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (int i = 0; i <= 255; i++) {
            output.write(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            output.write(i);
        }
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = 0; i <= 255; i++) {
            int value = input.readUnsignedByte();
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testFloat() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (int i = -128; i <= 127; i++) {
            output.writeFloat(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeFloat(Float.MAX_VALUE);
        output.writeFloat(Float.MIN_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readFloat(), 0.0D);
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testWriteIntWithPosition() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        long position = output.skip(Constants.INT_LEN);
        output.writeInt(2);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeFixedInt(position, 1);

        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        Assert.assertEquals(1, input.readInt());
        Assert.assertEquals(2, input.readInt());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testShort() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (short i = -128; i <= 127; i++) {
            output.writeShort(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeShort(Short.MAX_VALUE);
        output.writeShort(Short.MIN_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readShort());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testConstructor() {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        Assert.assertEquals(0, output.position());

```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(0, output.position());

        UnsafeBytesOutput output2 = new UnsafeBytesOutput(SIZE);
        Assert.assertEquals(0, output2.position());

```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(0, output2.position());

        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer());
        Assert.assertEquals(0, input.position());
    }
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testAvailable() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (int i = -128; i <= 127; i++) {
            output.write(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            output.write(i);
        }
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = 0; i < 256; i++) {
            Assert.assertEquals(256 - i, input.available());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testUnsignedShort() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (short i = 0; i <= 255; i++) {
            output.writeShort(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeShort(Short.MAX_VALUE);
        output.writeShort(Short.MIN_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = 0; i <= 255; i++) {
            Assert.assertEquals(i, input.readUnsignedShort());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

    private static UnsafeBytesInput inputByString(String s) throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeBytes(s);
        return new UnsafeBytesInput(output.toByteArray());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testUTFBoundary() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        String s1 = UnitTestBase.randomString(65535);
        output.writeUTF(s1);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            output.writeUTF(s2);
        });
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer());
        String value = input.readUTF();
        Assert.assertEquals(s1, value);
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testSkip() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.skip(4);
        output.writeInt(Integer.MAX_VALUE);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.skip(4);
        output.writeInt(Integer.MAX_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        input.skipBytes(4);
        Assert.assertEquals(4, input.remaining());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testSkipBytes() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        long position = output.skip(4);
        Assert.assertEquals(0, position);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(0, position);
        output.writeInt(Integer.MAX_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        int bytesSkipped = input.skipBytes(4);
        Assert.assertEquals(4, bytesSkipped);
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testDouble() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (int i = -128; i <= 127; i++) {
            output.writeDouble(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeDouble(Double.MAX_VALUE);
        output.writeDouble(Double.MIN_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readDouble(), 0.0D);
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testOverWriteWithPosition() {
        Assert.assertThrows(ComputerException.class, () -> {
            UnsafeBytesOutput output = new UnsafeBytesOutput(4);
            output.writeFixedInt(1, 100);
        });
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            UnsafeBytesOutput output = new UnsafeBytesOutput(4);
            output.writeFixedInt(2, 100);
        });
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            UnsafeBytesOutput output = new UnsafeBytesOutput(4);
            output.writeFixedInt(3, 100);
        });
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            UnsafeBytesOutput output = new UnsafeBytesOutput(4);
            output.writeFixedInt(4, 100);
        });
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testSeek() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (int i = -128; i <= 127; i++) {
            output.writeInt(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeInt(Integer.MAX_VALUE);
        output.writeInt(Integer.MIN_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 125; i++) {
            Assert.assertEquals(i, input.readInt());
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testReadLine() {
        UnsafeBytesInput input = new UnsafeBytesInput(Constants.EMPTY_BYTES);
        Assert.assertThrows(ComputerException.class, () -> {
            input.readLine();
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testReadBytes() throws IOException {
        String uuid = UUID.randomUUID().toString();
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeBytes(uuid);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeBytes(uuid);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        byte[] bytes = input.readBytes(uuid.length());
        Assert.assertEquals(uuid, new String(bytes));
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testBoolean() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeBoolean(true);
        output.writeBoolean(false);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeBoolean(true);
        output.writeBoolean(false);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        Assert.assertTrue(input.readBoolean());
        Assert.assertFalse(input.readBoolean());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testByteArray() throws IOException {
        byte[] bytes = "testByteArray".getBytes();
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.write(bytes);
        byte[] bytesRead = new byte[bytes.length];
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.write(bytes);
        byte[] bytesRead = new byte[bytes.length];
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        input.readFully(bytesRead);
        Assert.assertArrayEquals(bytes, bytesRead);
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testLong() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (long i = -128; i <= 127; i++) {
            output.writeLong(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeLong(Long.MAX_VALUE);
        output.writeLong(Long.MIN_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (long i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readLong());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testInt() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (int i = -128; i <= 127; i++) {
            output.writeInt(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        output.writeInt(Integer.MAX_VALUE);
        output.writeInt(Integer.MIN_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            Assert.assertEquals(i, input.readInt());
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testUTF() throws IOException {
        String prefix = "random string";
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (int i = -128; i <= 127; i++) {
            output.writeUTF(prefix + i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            output.writeUTF(prefix + i);
        }
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer());
        for (int i = -128; i <= 127; i++) {
            String value = input.readUTF();
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testByte() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        for (int i = -128; i <= 127; i++) {
            output.write(i);
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
            output.write(i);
        }
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        for (int i = -128; i <= 127; i++) {
            int value = input.readByte();
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    public void testUTFWithChineseCharacters() throws IOException {
        String s = "带汉字的字符串";
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeUTF(s);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeUTF(s);
        UnsafeBytesInput input = new UnsafeBytesInput(output.toByteArray());
        String value = input.readUTF();
        Assert.assertEquals(s, value);
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
    @Test
    public void testOverRead() throws IOException {
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeInt(Integer.MAX_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer(),
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesOutput output = new UnsafeBytesOutput(SIZE);
        output.writeInt(Integer.MAX_VALUE);
        UnsafeBytesInput input = new UnsafeBytesInput(output.buffer(),
                                                      (int) output.position());
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
```

### IOResource
'BufferedStreamOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new BufferedStreamOutput(os, 1);
            }, e -> {
                Assert.assertContains("The parameter bufferSize must be >= 8",
```

### IOResource
'BufferedStreamInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamTest.java`
#### Snippet
```java
            });
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new BufferedStreamInput(in, 1);
            }, e -> {
                Assert.assertContains("The parameter bufferSize must be >= 8",
```

### IOResource
'UnsafeBytesOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
        try (BufferedFileInput fileInput = inputByString(file3, "apple")) {
            @SuppressWarnings("resource")
            UnsafeBytesOutput output = new UnsafeBytesOutput(
                                       Constants.SMALL_BUF_SIZE);
            output.writeBytes("banana");
```

### IOResource
'UnsafeBytesInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileTest.java`
#### Snippet
```java
            output.writeBytes("banana");
            @SuppressWarnings("resource")
            RandomAccessInput input = new UnsafeBytesInput(output.buffer());
            int result = input.compare(0, input.available(), fileInput, 0,
                                        fileInput.available());
```

### IOResource
'ValueFileOutput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new ValueFileOutput(CONFIG, dir, 1);
            }, e -> {
                Assert.assertContains("bufferCapacity must be >= 8",
```

### IOResource
'ValueFileInput' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
            }
            Assert.assertThrows(IllegalArgumentException.class, () -> {
                new ValueFileInput(CONFIG, dir, 1);
            }, e -> {
                Assert.assertContains("The parameter bufferSize must be >= 8",
```

### IOResource
'MockWorkerService' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerServiceTest.java`
#### Snippet
```java
            MockComputation.class.getName()
        );
        WorkerService workerService = new MockWorkerService();
        Assert.assertThrows(ComputerException.class, () -> {
            workerService.init(config);
```

## RuleId[id=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `BytesUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/util/BytesUtil.java`
#### Snippet
```java
import com.google.common.base.Preconditions;

public class BytesUtil {

    public static byte[] ensureCapacityWithoutCopy(byte[] bytes, int length) {
```

### UtilityClassWithoutPrivateConstructor
Class `CoderUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/util/CoderUtil.java`
#### Snippet
```java
import org.apache.hugegraph.computer.core.common.exception.ComputerException;

public class CoderUtil {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `IdFactory` has only 'static' members, and lacks a 'private' constructor
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/graph/id/IdFactory.java`
#### Snippet
```java
import org.apache.hugegraph.computer.core.common.exception.ComputerException;

public final class IdFactory {

    private static final Constructor<?> BYTES_ID_CONSTRUCTOR;
```

### UtilityClassWithoutPrivateConstructor
Class `Constants` has only 'static' members, and lacks a 'private' constructor
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/common/Constants.java`
#### Snippet
```java
import org.apache.hugegraph.util.Bytes;

public final class Constants {

    public static final byte[] EMPTY_BYTES = new byte[0];
```

### UtilityClassWithoutPrivateConstructor
Class `Printer` has only 'static' members, and lacks a 'private' constructor
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/output/hg/metrics/Printer.java`
#### Snippet
```java
import org.slf4j.Logger;

public final class Printer {

    private static final Logger LOG = Log.logger(Printer.class);
```

### UtilityClassWithoutPrivateConstructor
Class `JsonUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-driver/src/main/java/org/apache/hugegraph/computer/driver/util/JsonUtil.java`
#### Snippet
```java
import com.fasterxml.jackson.databind.ObjectMapper;

public class JsonUtil {

    private static final ObjectMapper OBJECT_MAPPER;
```

### UtilityClassWithoutPrivateConstructor
Class `IOFactory` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/IOFactory.java`
#### Snippet
```java
import org.apache.hugegraph.computer.core.store.entry.EntryOutputImpl;

public final class IOFactory {

    public static BytesOutput createBytesOutput(int size) {
```

### UtilityClassWithoutPrivateConstructor
Class `HugeGraphComputer` has only 'static' members, and lacks a 'private' constructor
in `computer-dist/src/main/java/org/apache/hugegraph/computer/dist/HugeGraphComputer.java`
#### Snippet
```java
import org.slf4j.Logger;

public class HugeGraphComputer {

    private static final Logger LOG = Log.logger(HugeGraphComputer.class);
```

### UtilityClassWithoutPrivateConstructor
Class `SortingFactory` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/SortingFactory.java`
#### Snippet
```java
import org.apache.hugegraph.computer.core.common.exception.ComputerException;

public class SortingFactory {

    private static final SortingMode MODE = SortingMode.LOSER_TREE;
```

### UtilityClassWithoutPrivateConstructor
Class `FileUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/FileUtil.java`
#### Snippet
```java
import org.apache.commons.io.FileUtils;

public final class FileUtil {

    public static void deleteFilesQuietly(List<String> files) {
```

### UtilityClassWithoutPrivateConstructor
Class `SerializeUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/SerializeUtil.java`
#### Snippet
```java
import org.apache.hugegraph.computer.core.io.Writable;

public final class SerializeUtil {

    public static byte[] toBytes(Writable obj) {
```

### UtilityClassWithoutPrivateConstructor
Class `JsonUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/JsonUtil.java`
#### Snippet
```java
import com.fasterxml.jackson.databind.ObjectMapper;

public class JsonUtil {

    private static final ObjectMapper OBJECT_MAPPER;
```

### UtilityClassWithoutPrivateConstructor
Class `StringEncoding` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/StringEncoding.java`
#### Snippet
```java
 * @author Matthias Broecheler (me@matthiasb.com)
 */
public final class StringEncoding {

    private static final MessageDigest DIGEST;
```

### UtilityClassWithoutPrivateConstructor
Class `ComputerContextUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtil.java`
#### Snippet
```java
import org.apache.hugegraph.computer.core.graph.GraphFactory;

public class ComputerContextUtil {

    public static Config initContext(Map<String, String> params) {
```

### UtilityClassWithoutPrivateConstructor
Class `IdUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/IdUtil.java`
#### Snippet
```java
import com.google.common.collect.ImmutableList;

public class IdUtil {

    public static final String NUMBER_ID_PREFIX = "L";
```

### UtilityClassWithoutPrivateConstructor
Class `HugeConverter` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/HugeConverter.java`
#### Snippet
```java
import org.apache.hugegraph.util.E;

public final class HugeConverter {

    private static final GraphFactory GRAPH_FACTORY =
```

### UtilityClassWithoutPrivateConstructor
Class `InputSourceFactory` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/InputSourceFactory.java`
#### Snippet
```java
import org.apache.hugegraph.computer.core.rpc.InputSplitRpcService;

public class InputSourceFactory {

    public static InputSplitFetcher createInputSplitFetcher(Config config) {
```

### UtilityClassWithoutPrivateConstructor
Class `EntriesUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/entry/EntriesUtil.java`
#### Snippet
```java
import org.apache.hugegraph.computer.core.store.buffer.SubKvEntriesInput;

public final class EntriesUtil {

    public static KvEntry kvEntryFromInput(RandomAccessInput input,
```

### UtilityClassWithoutPrivateConstructor
Class `BufAllocatorFactory` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/BufAllocatorFactory.java`
#### Snippet
```java
import io.netty.buffer.PooledByteBufAllocator;

public class BufAllocatorFactory {

    public static ByteBufAllocator createBufAllocator() {
```

### UtilityClassWithoutPrivateConstructor
Class `TransportUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtil.java`
#### Snippet
```java
import io.netty.channel.DefaultMaxBytesRecvByteBufAllocator;

public class TransportUtil {

    private static final Logger LOG = Log.logger(TransportUtil.class);
```

### UtilityClassWithoutPrivateConstructor
Class `NettyEventLoopUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEventLoopUtil.java`
#### Snippet
```java
import io.netty.util.concurrent.DefaultThreadFactory;

public class NettyEventLoopUtil {

    private static ThreadFactory createNamedThreadFactory(String prefix) {
```

### UtilityClassWithoutPrivateConstructor
Class `Constants` has only 'static' members, and lacks a 'private' constructor
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/Constants.java`
#### Snippet
```java
import io.fabric8.kubernetes.client.utils.URLUtils;

public class Constants {

    public static final String ALL_NAMESPACE = "*";
```

### UtilityClassWithoutPrivateConstructor
Class `KubeUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/util/KubeUtil.java`
#### Snippet
```java
import io.fabric8.kubernetes.client.utils.Utils;

public class KubeUtil {

    private static final Logger LOG = Log.logger(KubeUtil.class);
```

### UtilityClassWithoutPrivateConstructor
Class `SorterTestUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/SorterTestUtil.java`
#### Snippet
```java
import org.apache.hugegraph.testutil.Assert;

public class SorterTestUtil {

    public static void writeData(BytesOutput output, Integer value)
```

### UtilityClassWithoutPrivateConstructor
Class `TestData` has only 'static' members, and lacks a 'private' constructor
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/TestData.java`
#### Snippet
```java
import com.google.common.collect.ImmutableList;

public class TestData {

    private static final List<List<Integer>> MULTIWAY_DATA_LIST =
```

### UtilityClassWithoutPrivateConstructor
Class `StoreTestUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/StoreTestUtil.java`
#### Snippet
```java
import org.apache.hugegraph.testutil.Assert;

public class StoreTestUtil {

    public static final String FILE_DIR = "hgkv";
```

### UtilityClassWithoutPrivateConstructor
Class `ReceiverUtil` has only 'static' members, and lacks a 'private' constructor
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/ReceiverUtil.java`
#### Snippet
```java
import io.netty.buffer.Unpooled;

public class ReceiverUtil {

    public static void consumeBuffer(byte[] bytes,
```

### UtilityClassWithoutPrivateConstructor
Class `IntegrateTestSuite` has only 'static' members, and lacks a 'private' constructor
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/integrate/IntegrateTestSuite.java`
#### Snippet
```java
    SenderIntegrateTest.class
})
public class IntegrateTestSuite {

    private static final Logger LOG = Log.logger(IntegrateTestSuite.class);
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
The call to 'checkArgument' always fails, according to its method contracts
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/graph/value/Value.java`
#### Snippet
```java
    default void checkAssign(Value other) {
        if (other == null) {
            E.checkArgument(false, "Can't assign null to %s",
                            this.getClass().getSimpleName());
        } else if (!this.getClass().isAssignableFrom(other.getClass())) {
```

### DataFlowIssue
The call to 'checkArgument' always fails, according to its method contracts
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/graph/value/Value.java`
#### Snippet
```java
                            this.getClass().getSimpleName());
        } else if (!this.getClass().isAssignableFrom(other.getClass())) {
            E.checkArgument(false, "Can't assign '%s'(%s) to %s",
                            other, other.getClass().getSimpleName(),
                            this.getClass().getSimpleName());
```

### DataFlowIssue
The call to 'checkArgument' always fails, according to its method contracts
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfig.java`
#### Snippet
```java
    private HugeConfig parseOptions(Map<String, String> options) {
        if (!options.keySet().containsAll(ComputerOptions.REQUIRED_OPTIONS)) {
            E.checkArgument(false,
                            "All required options must be setted, " +
                            "but missing these %s",
```

### DataFlowIssue
Method invocation `active` may produce `NullPointerException`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/worker/WorkerService.java`
#### Snippet
```java
         * superstep.
         */
        while (superstepStat.active()) {
            WorkerContext context = new SuperstepContext(superstep,
                                                         superstepStat);
```

### DataFlowIssue
Method invocation `active` may produce `NullPointerException`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/master/MasterService.java`
#### Snippet
```java
        watcher.start();
        // Step 3: Iteration computation of all supersteps.
        for (; superstepStat.active(); superstep++) {
            LOG.info("{} MasterService superstep {} started",
                     this, superstep);
```

### DataFlowIssue
Argument `message` might be null
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/wcc/Wcc.java`
#### Snippet
```java
        Id message = Combiner.combineAll(context.combiner(), messages);
        Id value = vertex.value();
        if (value.compareTo(message) > 0) {
            vertex.value(message);
            context.sendMessageToAllEdges(vertex, message);
```

### DataFlowIssue
Argument `message` might be null
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/kcore/Kcore.java`
#### Snippet
```java
            assert superstep > 2;
            Id message = Combiner.combineAll(context.combiner(), messages);
            if (value.core().compareTo(message) > 0) {
                value.core(message);
                context.sendMessageToAllEdges(vertex, message);
```

### DataFlowIssue
Argument `bashStream` might be null
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/driver/KubernetesDriver.java`
#### Snippet
```java
                bashStream = new FileInputStream(this.bashPath);
            }
            String bashAsStr = IOHelpers.readFully(bashStream);

            StringBuilder builder = new StringBuilder();
```

### DataFlowIssue
The call to 'convertToMap' always fails, according to its method contracts
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/ComputerContextUtilTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            ComputerContextUtil.convertToMap((Properties) null);
        });

```

### DataFlowIssue
Variable is already assigned to this value
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/store/ValueFileTest.java`
#### Snippet
```java
                position = 3L;
                assertSeek(fileLength, input, position);
                position = 3L;
                assertSeek(fileLength, input, position);
                position = 68L;
```

### DataFlowIssue
Method invocation `getPath` may produce `NullPointerException`
in `computer-test/src/main/java/org/apache/hugegraph/computer/dist/HugeGraphComputerTest.java`
#### Snippet
```java
        Throwable[] exceptions = new Throwable[2];
        String masterConfPath = HugeGraphComputerTest.class.getResource(
                                "/computer.properties").getPath();
        String work1ConfPath = HugeGraphComputerTest.class.getResource(
                               "/computer.properties").getPath();
```

### DataFlowIssue
Method invocation `getPath` may produce `NullPointerException`
in `computer-test/src/main/java/org/apache/hugegraph/computer/dist/HugeGraphComputerTest.java`
#### Snippet
```java
                                "/computer.properties").getPath();
        String work1ConfPath = HugeGraphComputerTest.class.getResource(
                               "/computer.properties").getPath();
        pool.submit(() -> {
            try {
```

### DataFlowIssue
Method invocation `getPath` may produce `NullPointerException`
in `computer-test/src/main/java/org/apache/hugegraph/computer/dist/HugeGraphComputerTest.java`
#### Snippet
```java
    public void testServiceWithError() {
        String work1ConfPath = HugeGraphComputerTest.class.getResource(
                               "/computer.properties").getPath();
        Assert.assertThrows(IllegalArgumentException.class,
                            () -> {
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/OutputFormat.java`
#### Snippet
```java
    CSV,

    JSON;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/combiner/PointerCombinerTest.java`
#### Snippet
```java
                                        Constants.SMALL_BUF_SIZE);
             BytesOutput bytesOutput2 = IOFactory.createBytesOutput(
                                        Constants.SMALL_BUF_SIZE);) {
            DoubleValue value1 = new DoubleValue(1.0D);
            DoubleValue value2 = new DoubleValue(2.0D);
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'FailMessage' is still used
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/message/FailMessage.java`
#### Snippet
```java

@Deprecated
public class FailMessage extends AbstractMessage implements ResponseMessage {

    private final int errorCode;
```

## RuleId[id=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/reader/HgkvDir4SubKvReaderImpl.java`
#### Snippet
```java
        public KvEntryWithFirstSubKvIter(KvEntryFileReader reader) {
            this.entries = new MapperIterator<>(reader.iterator(), entry -> {
                return EntriesUtil.kvEntryWithFirstSubKv(entry);
            });
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/OptimizedUnsafeBytesTest.java`
#### Snippet
```java
                                    Constants.EMPTY_BYTES);
        Assert.assertThrows(ComputerException.class, () -> {
            input.readLine();
        });
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/EmptyFlusherTest.java`
#### Snippet
```java

        Assert.assertThrows(NoSuchElementException.class, () -> {
            it.next();
        });

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorter/EmptyFlusherTest.java`
#### Snippet
```java

        Assert.assertThrows(NoSuchElementException.class, () -> {
            it.peek();
        });

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        UnsafeBytesInput input = new UnsafeBytesInput(Constants.EMPTY_BYTES);
        Assert.assertThrows(ComputerException.class, () -> {
            input.readLine();
        });
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesTest.java`
#### Snippet
```java
        Assert.assertEquals(Integer.MAX_VALUE, input.readInt());
        Assert.assertThrows(ComputerException.class, () -> {
            input.readInt();
        });
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/util/SerializeUtilTest.java`
#### Snippet
```java

        byte[] bytes = SerializeUtil.toBytes(list1);
        List<Id> list2 = SerializeUtil.fromBytes(bytes, () -> new BytesId());

        Assert.assertEquals(list1, list2);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/graph/value/ListValueTest.java`
#### Snippet
```java

        Assert.assertThrows(NoSuchElementException.class, () -> {
            value1.getLast();
        }, e -> {
            Assert.assertContains("The list is empty", e.getMessage());
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
        Thread thread1 = new Thread(() -> {
            Assert.assertThrows(ComputerException.class, () -> {
                buffers.prepareSorting();
            }, e -> {
                Assert.assertTrue(e.getMessage().contains("Interrupted"));
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Assert.assertThrows(RuntimeException.class, () -> {
            client.startSession();
        }, e -> {
            Assert.assertContains("test exception", e.getMessage());
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Assert.assertThrows(TransportException.class, () -> {
            client.finishSession();
        }, e -> {
            Assert.assertContains("to wait finish-response",
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Assert.assertThrows(RuntimeException.class, () -> {
            client.finishSession();
        }, e -> {
            Assert.assertContains("test exception", e.getMessage());
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Assert.assertThrows(TransportException.class, () -> {
            client.finishSession();
        }, e -> {
            Assert.assertContains("to wait finish-response",
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java

        Assert.assertThrows(TransportException.class, () -> {
            client.startSession();
        }, e -> {
            Assert.assertContains("to wait start-response",
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/ReceiverUtil.java`
#### Snippet
```java

        entryOutput.writeEntry(out -> {
            id.write(out);
        }, message);
        return bytesOutput.toByteArray();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
            Assert.assertContains("Buffers have not been sorted in 100 ms",
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        ReceiverUtil.consumeBuffer(new byte[mockBufferLength],
                                   (NetworkBuffer buffer) -> {
            buffers.addBuffer(buffer);
        });
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java
        CountDownLatch countDownLatch = new CountDownLatch(1);
        Thread sortThread = new Thread(() -> {
            buffers.waitSorted();
        });
        sortThread.setUncaughtExceptionHandler((t, e) -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
            Assert.assertContains("Buffers have not been sorted in 100 ms",
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvBuffersTest.java`
#### Snippet
```java

        Assert.assertThrows(ComputerException.class, () -> {
            buffers.waitSorted();
        }, e -> {
            Assert.assertContains("Buffers have not been sorted in 100 ms",
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
        Id id = vertex.id();
        KvEntryWriter subKvWriter = entryOutput.writeEntry(out -> {
            id.write(out);
        });
        for (Edge edge : vertex.edges()) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/receiver/edge/EdgeMessageRecvPartitionTest.java`
#### Snippet
```java
            Id targetId = edge.targetId();
            subKvWriter.writeSubKv(out -> {
                targetId.write(out);
            }, out -> {
                edge.properties().write(out);
```

## RuleId[id=ShiftOutOfRange]
### ShiftOutOfRange
Shift operation `>>>` by overly large constant value 56
in `computer-driver/src/main/java/org/apache/hugegraph/computer/driver/SuperstepStat.java`
#### Snippet
```java
    @Override
    public int hashCode() {
        return (Long.hashCode(this.vertexCount) >>> 56) ^
               (Long.hashCode(this.edgeCount) >>> 48) ^
               (Long.hashCode(this.messageCount) >>> 40) ^
```

### ShiftOutOfRange
Shift operation `>>>` by overly large constant value 48
in `computer-driver/src/main/java/org/apache/hugegraph/computer/driver/SuperstepStat.java`
#### Snippet
```java
    public int hashCode() {
        return (Long.hashCode(this.vertexCount) >>> 56) ^
               (Long.hashCode(this.edgeCount) >>> 48) ^
               (Long.hashCode(this.messageCount) >>> 40) ^
               (Long.hashCode(this.edgeCount) >>> 32) ^
```

### ShiftOutOfRange
Shift operation `>>>` by overly large constant value 40
in `computer-driver/src/main/java/org/apache/hugegraph/computer/driver/SuperstepStat.java`
#### Snippet
```java
        return (Long.hashCode(this.vertexCount) >>> 56) ^
               (Long.hashCode(this.edgeCount) >>> 48) ^
               (Long.hashCode(this.messageCount) >>> 40) ^
               (Long.hashCode(this.edgeCount) >>> 32) ^
               (Long.hashCode(this.finishedVertexCount) >>> 24) ^
```

### ShiftOutOfRange
Shift operation `>>>` by overly large constant value 32
in `computer-driver/src/main/java/org/apache/hugegraph/computer/driver/SuperstepStat.java`
#### Snippet
```java
               (Long.hashCode(this.edgeCount) >>> 48) ^
               (Long.hashCode(this.messageCount) >>> 40) ^
               (Long.hashCode(this.edgeCount) >>> 32) ^
               (Long.hashCode(this.finishedVertexCount) >>> 24) ^
               (Long.hashCode(this.messageCount) >>> 16) ^
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/unit/UnitTestBase.java`
#### Snippet
```java
            map.put(((TypedOption<?, ?>) key).name(), (String) value);
        }
        if (!map.keySet().contains(
                          ComputerOptions.ALGORITHM_PARAMS_CLASS.name())) {
            map.put(ComputerOptions.ALGORITHM_PARAMS_CLASS.name(),
```

## RuleId[id=AbstractMethodCallInConstructor]
### AbstractMethodCallInConstructor
Call to 'abstract' method `threadPrefix()` during object construction
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/SortManager.java`
#### Snippet
```java
        if (this.threadNum(config) != 0) {
            this.sortExecutor = ExecutorUtil.newFixedThreadPool(
                                this.threadNum(config), this.threadPrefix());
        } else {
            this.sortExecutor = null;
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/MockMessageHandler.java`
#### Snippet
```java
    @Override
    public String genOutputPath(MessageType messageType, int partition) {
        return "./" + UUID.randomUUID().toString();
    }

```

## RuleId[id=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `srao` declared in one 'switch' branch and used in another
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/IOFactory.java`
#### Snippet
```java
                return new StreamGraphOutput(context, entryOutput);
            case CSV:
                StructRandomAccessOutput srao;
                srao = new StructRandomAccessOutput(out);
                return new CsvStructGraphOutput(context, srao);
```

### SwitchStatementWithConfusingDeclaration
Local variable `input` declared in one 'switch' branch and used in another
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/graph/id/BytesId.java`
#### Snippet
```java
        switch (this.idType) {
            case LONG:
                BytesInput input = IOFactory.createBytesInput(this.bytes, 0,
                                                              this.length);
                try {
```

## RuleId[id=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `this.state` accessed in synchronized context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java

    public synchronized CompletableFuture<Void> finishAsync() {
        E.checkArgument(this.state == TransportState.ESTABLISHED,
                        "The state must be ESTABLISHED instead of %s " +
                        "at finishAsync()", this.state);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.state` accessed in synchronized context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java
        E.checkArgument(this.state == TransportState.ESTABLISHED,
                        "The state must be ESTABLISHED instead of %s " +
                        "at finishAsync()", this.state);

        CompletableFuture<Void> finishedFuture = new CompletableFuture<>();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.state` accessed in synchronized context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java

    public synchronized CompletableFuture<Void> startAsync() {
        E.checkArgument(this.state == TransportState.READY,
                        "The state must be READY instead of %s " +
                        "at startAsync()", this.state);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.state` accessed in synchronized context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java
        E.checkArgument(this.state == TransportState.READY,
                        "The state must be READY instead of %s " +
                        "at startAsync()", this.state);

        CompletableFuture<Void> startedFuture = new CompletableFuture<>();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.state` accessed in synchronized context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java
    public synchronized void sendAsync(MessageType messageType, int partition,
                                       ByteBuffer buffer) {
        E.checkArgument(this.state == TransportState.ESTABLISHED,
                        "The state must be ESTABLISHED instead of %s " +
                        "at sendAsync()", this.state);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.state` accessed in synchronized context
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/session/ClientSession.java`
#### Snippet
```java
        E.checkArgument(this.state == TransportState.ESTABLISHED,
                        "The state must be ESTABLISHED instead of %s " +
                        "at sendAsync()", this.state);
        int requestId = this.nextRequestId();

```

## RuleId[id=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesInput.java`
#### Snippet
```java

@SuppressWarnings("deprecation") // Unsafe.getXx
public class UnsafeBytesInput implements BytesInput {

    private static final sun.misc.Unsafe UNSAFE;
```

### RedundantSuppression
Redundant suppression
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/driver/KubernetesDriver.java`
#### Snippet
```java
                    JobObserver observer = pair.getRight();

                    @SuppressWarnings("resource")
                    KubernetesDriver driver = KubernetesDriver.this;
                    JobState jobState = driver.buildJobState(computerJob);

```

## RuleId[id=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/io/StreamGraphOutputInputTest.java`
#### Snippet
```java
            output.writeMessage(id, value);
            bytes = bao.toByteArray();
            System.out.println(Arrays.toString(bytes));
        }

```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'obj == INSTANCE' covered by subsequent condition 'obj instanceof NullValue'
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/graph/value/NullValue.java`
#### Snippet
```java
    @Override
    public boolean equals(Object obj) {
        return obj == INSTANCE || obj instanceof NullValue;
    }

```

## RuleId[id=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/seqfile/ValueFile.java`
#### Snippet
```java

        File[] segments = dir.listFiles((dirName, name) -> {
                            return name.matches(SEGMENT_NAME_REGEX);
                          });
        assert segments != null;
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/HgkvDirImpl.java`
#### Snippet
```java

    private static File[] scanHgkvFiles(File dir) {
        return dir.listFiles((dirName, name) -> name.matches(FILE_NAME_REGEX));
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/util/KubeUtil.java`
#### Snippet
```java

    public static String crName(String jobId) {
        return jobId.toLowerCase().replaceAll("_", "-");
    }

```

## RuleId[id=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary and can be removed
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesInput.java`
#### Snippet
```java
public class UnsafeBytesInput implements BytesInput {

    private static final sun.misc.Unsafe UNSAFE;

    private final byte[] buffer;
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary and can be removed
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesInput.java`
#### Snippet
```java
    static {
        try {
            Field field = sun.misc.Unsafe.class.getDeclaredField("theUnsafe");
            field.setAccessible(true);
            UNSAFE = (sun.misc.Unsafe) field.get(null);
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary and can be removed
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesInput.java`
#### Snippet
```java
            Field field = sun.misc.Unsafe.class.getDeclaredField("theUnsafe");
            field.setAccessible(true);
            UNSAFE = (sun.misc.Unsafe) field.get(null);
        } catch (Exception e) {
            throw new ComputerException("Failed to get unsafe", e);
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary and can be removed
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesOutput.java`
#### Snippet
```java
public class UnsafeBytesOutput implements BytesOutput {

    private static final sun.misc.Unsafe UNSAFE;

    private byte[] buffer;
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary and can be removed
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesOutput.java`
#### Snippet
```java
    static {
        try {
            Field field = sun.misc.Unsafe.class.getDeclaredField("theUnsafe");
            field.setAccessible(true);
            UNSAFE = (sun.misc.Unsafe) field.get(null);
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary and can be removed
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/UnsafeBytesOutput.java`
#### Snippet
```java
            Field field = sun.misc.Unsafe.class.getDeclaredField("theUnsafe");
            field.setAccessible(true);
            UNSAFE = (sun.misc.Unsafe) field.get(null);
        } catch (Exception e) {
            throw new ComputerException("Failed to get unsafe", e);
```

## RuleId[id=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `computer-test/src/main/java/org/apache/hugegraph/computer/suite/integrate/SenderIntegrateTest.java`
#### Snippet
```java
                Thread.sleep(100);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
            return sendFuncBak.apply(message);
```

## RuleId[id=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `InsertTask()` of an abstract class should not be declared 'public'
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/output/hg/task/InsertTask.java`
#### Snippet
```java
    private LoadSummary summary;

    public InsertTask(Config config, HugeClient client,
                      List<Vertex> batch, LoadSummary loadSummary) {
        this.config = config;
```

### NonProtectedConstructorInAbstractClass
Constructor `StructGraphOutput()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/StructGraphOutput.java`
#### Snippet
```java
    protected final StructRandomAccessOutput out;

    public StructGraphOutput(ComputerContext context,
                             StructRandomAccessOutput out) {
        this.config = context.config();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBufferedFileOutput()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/AbstractBufferedFileOutput.java`
#### Snippet
```java
    protected long fileOffset;

    public AbstractBufferedFileOutput(int bufferCapacity) {
        super(bufferCapacity);

```

### NonProtectedConstructorInAbstractClass
Constructor `BspBase()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/BspBase.java`
#### Snippet
```java
    private final BspClient bspClient;

    public BspBase(Config config) {
        this.config = config;

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBufferedFileInput()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/AbstractBufferedFileInput.java`
#### Snippet
```java
    protected long fileOffset;

    public AbstractBufferedFileInput(int bufferCapacity, long fileLength) {
        super(new byte[bufferCapacity], 0, 0);

```

### NonProtectedConstructorInAbstractClass
Constructor `CombinableSorterFlusher()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/flusher/CombinableSorterFlusher.java`
#### Snippet
```java
    private final PointerCombiner combiner;

    public CombinableSorterFlusher(PointerCombiner combiner) {
        this.combiner = combiner;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInputsSorting()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/AbstractInputsSorting.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    public AbstractInputsSorting(Collection<? extends Iterator<T>> sources,
                                 Comparator<? super T> comparator) {
        E.checkNotEmpty(sources, "sources");
```

### NonProtectedConstructorInAbstractClass
Constructor `HugeElementFetcher()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/hg/HugeElementFetcher.java`
#### Snippet
```java
    private T next;

    public HugeElementFetcher(Config config, HugeClient client) {
        this.config = config;
        this.client = client;
```

### NonProtectedConstructorInAbstractClass
Constructor `SortManager()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/SortManager.java`
#### Snippet
```java
    private final int flushThreshold;

    public SortManager(ComputerContext context) {
        this.context = context;
        Config config = context.config();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHgkvFile()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/hgkvfile/AbstractHgkvFile.java`
#### Snippet
```java
    protected String version;

    public AbstractHgkvFile(String path) {
        this.path = path;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FileElementFetcher()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/input/loader/FileElementFetcher.java`
#### Snippet
```java
    private T next;

    public FileElementFetcher(Config config) {
        String schemaPath = config.get(
                            ComputerOptions.INPUT_LOADER_SCHEMA_PATH);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractKvEntry()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/entry/AbstractKvEntry.java`
#### Snippet
```java
    protected final Pointer value;

    public AbstractKvEntry(Pointer key, Pointer value) {
        this.key = key;
        this.value = value;
```

### NonProtectedConstructorInAbstractClass
Constructor `MessageRecvPartitions()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvPartitions.java`
#### Snippet
```java
    private final Map<Integer, P> partitions;

    public MessageRecvPartitions(ComputerContext context,
                                 SuperstepFileGenerator fileGenerator,
                                 SortManager sortManager) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPointerCombiner()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/combiner/AbstractPointerCombiner.java`
#### Snippet
```java
    protected BytesOutput output;

    public AbstractPointerCombiner(Supplier<T> supplier, Combiner<T> combiner) {
        this.v1 = supplier.get();
        this.v2 = supplier.get();
```

### NonProtectedConstructorInAbstractClass
Constructor `MessageRecvPartition()` of an abstract class should not be declared 'public'
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/receiver/MessageRecvPartition.java`
#### Snippet
```java
    private final AtomicReference<Throwable> exception;

    public MessageRecvPartition(Config config,
                                SuperstepFileGenerator fileGenerator,
                                SortManager sortManager,
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractController()` of an abstract class should not be declared 'public'
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/AbstractController.java`
#### Snippet
```java
            SharedIndexInformer<? extends HasMetadata>> informerMap;

    public AbstractController(HugeConfig config,
                              NamespacedKubernetesClient kubeClient) {
        this.config = config;
```

## RuleId[id=TestCaseWithConstructor]
### TestCaseWithConstructor
Initialization logic in constructor `InputsSortingTest()` instead of 'setup()' life cycle method
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/InputsSortingTest.java`
#### Snippet
```java
    private final SortingMode mode;
    
    protected InputsSortingTest(SortingMode mode) {
        this.mode = mode;
    }
```

### TestCaseWithConstructor
Initialization logic in constructor `RecyclersTest()` instead of 'setup()' life cycle method
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/allocator/RecyclersTest.java`
#### Snippet
```java
    private final Method threadLocalSizeMethod;

    public RecyclersTest() {
        try {
            Method method = Recycler.class
```

## RuleId[id=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `retryCount`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/output/hg/task/BatchInsertTask.java`
#### Snippet
```java
        }

        if (++retryCount > retryTimes) {
            LOG.error("Batch insert has been retried more than {} times",
                      retryTimes);
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedFileInput.java`
#### Snippet
```java
        } else {
            super.readFully(b, off, remaining);
            len -= remaining;
            this.file.readFully(b, off + remaining, len);
            this.fileOffset += len;
```

### AssignmentToMethodParameter
Assignment to method parameter `bytesToSkip`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/io/BufferedStreamInput.java`
#### Snippet
```java
            return positionBeforeSkip;
        } else {
            bytesToSkip -= this.remaining();
            long position = this.inputOffset + bytesToSkip;
            this.seek(position);
```

### AssignmentToMethodParameter
Assignment to method parameter `logInterval`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
            long deadline = timeout + System.currentTimeMillis();
            while (remaining > 0) {
                logInterval = Math.min(remaining, logInterval);
                if (this.barrierEvent.await(logInterval)) {
                    assert this.result != null;
```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/HeapInputsSorting.java`
#### Snippet
```java
            if (this.compare((T) this.data[parent], (T) this.data[child]) > 0) {
                this.swap(parent, child);
                parent = child;
            } else {
                break;
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/LoserTreeInputsSorting.java`
#### Snippet
```java
                int temp = this.tree[t];
                this.tree[t] = index;
                index = temp;
            }
            t = t >> 1;
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/StringEncoding.java`
#### Snippet
```java
        int c;
        do {
            c = 0xFF & array[offset++];
            if (c != 0x80) {
                sb.append((char) (c & 0x7F));
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/StringEncoding.java`
#### Snippet
```java
        int len = value.length();
        if (len == 0) {
            array[offset++] = (byte) 0x80;
            return offset;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/util/StringEncoding.java`
#### Snippet
```java
                b |= 0x80;
            }
            array[offset++] = b;
        } while (i < len);

```

### AssignmentToMethodParameter
Assignment to method parameter `inputs`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/merge/FileMergerImpl.java`
#### Snippet
```java
            }

            inputs = newInputs;
            round++;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/store/file/select/DisperseEvenlySelector.java`
#### Snippet
```java
                                 this.data.get(child)) > 0) {
                    this.swap(index, child);
                    index = child;
                } else {
                    break;
```

### AssignmentToMethodParameter
Assignment to method parameter `edge`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/worker/load/LoadService.java`
#### Snippet
```java

        private Edge convert(org.apache.hugegraph.structure.graph.Edge edge) {
            edge = inputFilter.filter(edge);
            Id targetId = HugeConverter.convertId(edge.targetId());
            Properties properties = HugeConverter.convertProperties(
```

### AssignmentToMethodParameter
Assignment to method parameter `vertex`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/worker/load/LoadService.java`
#### Snippet
```java
        private Vertex convert(org.apache.hugegraph.structure.graph.Vertex
                               vertex) {
            vertex = inputFilter.filter(vertex);
            Id id = HugeConverter.convertId(vertex.id());
            String label = vertex.label();
```

### AssignmentToMethodParameter
Assignment to method parameter `version`
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/util/KubeUtil.java`
#### Snippet
```java
                                  String version) {
        if (StringUtils.isBlank(version)) {
            version = "latest";
        }
        return String.format("%s:%s-%s", repository, algorithmName, version);
```

### AssignmentToMethodParameter
Assignment to method parameter `configKey`
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/util/KubeUtil.java`
#### Snippet
```java
     */
    public static String covertSpecKey(String configKey) {
        configKey = configKey.substring(Constants.K8S_SPEC_PREFIX.length());
        Matcher matcher = LINE_PATTERN.matcher(configKey);
        StringBuffer sb = new StringBuffer();
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `aggr`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/aggregator/WorkerAggrManager.java`
#### Snippet
```java
                                                             this.service());
        // May be executed in parallel by multiple threads in a worker
        synchronized (aggr) {
            aggr.aggregateValue(value);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `aggr`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/aggregator/MasterAggrManager.java`
#### Snippet
```java
                                                          V value) {
            Aggregator<V> aggr = this.getAggregator(name);
            synchronized (aggr) {
                aggr.aggregateValue(value);
            }
```

## RuleId[id=ReturnNull]
### ReturnNull
Return of `null`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/config/DefaultConfig.java`
#### Snippet
```java
                      "Please config required option '%s'", clazzOption.name());
            }
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `computer-api/src/main/java/org/apache/hugegraph/computer/core/combiner/Combiner.java`
#### Snippet
```java
                                          Iterator<T> values) {
        if (!values.hasNext()) {
            return null;
        }
        T result = (T) values.next().copy();
```

### ReturnNull
Return of `null`
in `computer-driver/src/main/java/org/apache/hugegraph/computer/driver/config/DriverConfigOption.java`
#### Snippet
```java
    protected T parse(String value) {
        if (value == null) {
            return null;
        }
        return super.parse(value);
```

### ReturnNull
Return of `null`
in `computer-driver/src/main/java/org/apache/hugegraph/computer/driver/config/DriverConfigOption.java`
#### Snippet
```java
    public T parseConvert(String value) {
        if (value == null) {
            return null;
        }
        return super.parseConvert(value);
```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
                                             key);
            } else {
                return null;
            }
        } catch (InterruptedException e) {
```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/sort/sorting/SortManager.java`
#### Snippet
```java
                                        false);
        if (valueCombiner == null) {
            return null;
        }
        return new MessageValueCombiner(this.context);
```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtil.java`
#### Snippet
```java
    public static String remoteAddress(Channel channel) {
        if (channel == null || channel.remoteAddress() == null) {
            return null;
        }
        if (channel.remoteAddress() instanceof InetSocketAddress) {
```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/TransportUtil.java`
#### Snippet
```java
    public static ConnectionId remoteConnectionId(Channel channel) {
        if (channel == null || channel.remoteAddress() == null) {
            return null;
        }
        InetSocketAddress address = (InetSocketAddress)
```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/codec/PreciseFrameDecoder.java`
#### Snippet
```java
                AbstractMessage.HEADER_LENGTH - in.readableBytes();
            TransportUtil.setMaxBytesPerRead(ctx.channel(), nextMaxBytesRead);
            return null;
        }

```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/codec/PreciseFrameDecoder.java`
#### Snippet
```java
                     magicNumber, TransportUtil.remoteAddress(ctx.channel()));
            buf.release();
            return null;
        }
        int version = buf.readByte();
```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/codec/PreciseFrameDecoder.java`
#### Snippet
```java
                     version, TransportUtil.remoteAddress(ctx.channel()));
            buf.release();
            return null;
        }

```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/codec/FrameDecoder.java`
#### Snippet
```java
        ByteBuf msg = (ByteBuf) super.decode(ctx, in);
        if (msg == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/codec/FrameDecoder.java`
#### Snippet
```java
                     magicNumber, TransportUtil.remoteAddress(ctx.channel()));
            msg.release();
            return null;
        }
        int version = msg.readByte();
```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportServer.java`
#### Snippet
```java
    public String ip() {
        InetAddress address = this.bindAddress().getAddress();
        return address == null ? null : address.getHostAddress();
    }

```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/netty/codec/FrameDecoder.java`
#### Snippet
```java
                     version, TransportUtil.remoteAddress(ctx.channel()));
            msg.release();
            return null;
        }
        // TODO: improve it use shared memory
```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/message/FailMessage.java`
#### Snippet
```java
        buf.writeInt(this.errorCode);
        TransportUtil.writeString(buf, this.message);
        return null;
    }

```

### ReturnNull
Return of `null`
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/network/message/AbstractMessage.java`
#### Snippet
```java
    @Override
    public NetworkBuffer body() {
        return this.hasBody() ? this.body : null;
    }

```

### ReturnNull
Return of `null`
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/util/KubeUtil.java`
#### Snippet
```java
                LOG.warn("K8s resource already exists: {}",
                         exception.getMessage());
                return null;
            } else {
                throw exception;
```

### ReturnNull
Return of `null`
in `computer-algorithm/src/main/java/org/apache/hugegraph/computer/algorithm/community/trianglecount/TriangleCount.java`
#### Snippet
```java
            return count >> 1;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/WorkQueue.java`
#### Snippet
```java
            if (this.shutdown) {
                // We must be shutting down
                return null;
            } else {
                this.wait();
```

### ReturnNull
Return of `null`
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/driver/KubernetesDriver.java`
#### Snippet
```java
                      Map<String, String> params) {
        // TODO: implement
        return null;
    }

```

### ReturnNull
Return of `null`
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/driver/KubernetesDriver.java`
#### Snippet
```java
    public List<SuperstepStat> superstepStats(String jobId, Map<String, String> params) {
        // TODO: implement
        return null;
    }

```

### ReturnNull
Return of `null`
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/driver/KubernetesDriver.java`
#### Snippet
```java
            LOG.warn("Unable to fetch state of job '{}', it may have been " +
                     "deleted", jobId);
            return null;
        } else {
            observer.onJobStateChanged(jobState);
```

### ReturnNull
Return of `null`
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/driver/KubernetesDriver.java`
#### Snippet
```java
                                     .get();
        if (event == null) {
            return null;
        }
        return event.getMessage();
```

### ReturnNull
Return of `null`
in `computer-k8s/src/main/java/org/apache/hugegraph/computer/k8s/driver/KubernetesDriver.java`
#### Snippet
```java
        HugeGraphComputerJob computerJob = this.operation.withName(crName).get();
        if (computerJob == null) {
            return null;
        }
        return this.buildJobState(computerJob);
```

### ReturnNull
Return of `null`
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/AbstractController.java`
#### Snippet
```java
        R resource = informer.getIndexer().getByKey(key);
        if (resource == null) {
            return null;
        }
        return Serialization.clone(resource);
```

### ReturnNull
Return of `null`
in `computer-k8s-operator/src/main/java/org/apache/hugegraph/computer/k8s/operator/common/AbstractController.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
            channel.pipeline().replace("heartbeatHandler", "heartbeatHandler",
                                       mockHeartbeatHandler);
            return null;
        }).when(clientProtocol).initializeClientPipeline(Mockito.any());

```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
                                       NettyProtocol.SERVER_HANDLER_NAME,
                                       spyNettyServerHandler);
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());
        Mockito.doAnswer(invocationOnMock -> null)
               .when(spyNettyServerHandler).processPingMessage(Mockito.any(),
                                                               Mockito.any(),
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
            channel.pipeline().replace("serverIdleHandler", "serverIdleHandler",
                                       spyServerIdleHandler);
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
            channel.pipeline().replace("heartbeatHandler", "heartbeatHandler",
                                       mockHeartbeatHandler);
            return null;
        }).when(clientProtocol).initializeClientPipeline(Mockito.any());

```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/HeartbeatHandlerTest.java`
#### Snippet
```java
            Object event = invocationOnMock.getArgument(1);
            ctx.fireUserEventTriggered(event);
            return null;
        }).when(mockHeartbeatHandler)
          .userEventTriggered(Mockito.any(), Mockito.any(IdleStateEvent.class));
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyEncodeDecodeHandlerTest.java`
#### Snippet
```java
            Channel channel = invocationOnMock.getArgument(0);
            channel.writeAndFlush(new MockUnDecodeMessage());
            return null;
        }).when(serverProtocol).initializeServerPipeline(Mockito.any(),
                                                         Mockito.any());
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        client.startSession();

        Function<Message, ChannelFuture> sendFunc = message -> null;
        Whitebox.setInternalState(client.clientSession(),
                                  "sendFunction", sendFunc);
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        NettyTransportClient client = (NettyTransportClient) this.oneClient();

        Function<Message, ChannelFuture> sendFunc = message -> null;
        Whitebox.setInternalState(client.clientSession(),
                                  "sendFunction", sendFunc);
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
        Object sendFuncBak = Whitebox.getInternalState(client.clientSession(),
                                                       "sendFunction");
        Function<Message, ChannelFuture> sendFunc = message -> null;
        Whitebox.setInternalState(client.clientSession(),
                                  "sendFunction", sendFunc);
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/netty/NettyTransportClientTest.java`
#### Snippet
```java
            Assert.assertNotSame(sourceBytes, bytes);

            return null;
        }).when(serverHandler).handle(Mockito.any(), Mockito.eq(1),
                                      Mockito.any());
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        ExecutorUtil.newScheduledThreadPool(1, TASK_SCHEDULER);

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
                                                      "maxHandledId"));

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY,
                            clientSession.state());
```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        ExecutorUtil.newScheduledThreadPool(1, TASK_SCHEDULER);

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        ExecutorUtil.newScheduledThreadPool(1, TASK_SCHEDULER);

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
        ExecutorUtil.newScheduledThreadPool(1, TASK_SCHEDULER);

        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

```

### ReturnNull
Return of `null`
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/network/session/TransportSessionTest.java`
#### Snippet
```java
    @Test
    public void testSendAsyncWithException() {
        ClientSession clientSession = new ClientSession(conf, message -> null);
        Assert.assertEquals(TransportState.READY, clientSession.state());

```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/sender/WriteBuffersTest.java`
#### Snippet
```java
            while (counter.get() < 2) {
                try {
                    Thread.sleep(100);
                } catch (InterruptedException e) {
                    Assert.fail(e.getMessage());
```

## RuleId[id=UnstableApiUsage]
### UnstableApiUsage
'get(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.options.GetOption)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
                                           .withSortOrder(SortOrder.ASCEND)
                                           .build();
            GetResponse response = this.kv.get(prefixSeq, getOption).get();
            if (response.getCount() == count) {
                return getResponseValues(response);
```

### UnstableApiUsage
'getKVClient()' is unstable because its signature references unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
        ByteSequence keySeq = ByteSequence.from(key, ENCODING);
        try {
            DeleteResponse response = this.client.getKVClient().delete(keySeq)
                                                 .get();
            return response.getDeleted();
```

### UnstableApiUsage
'delete(io.etcd.jetcd.ByteSequence)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
        ByteSequence keySeq = ByteSequence.from(key, ENCODING);
        try {
            DeleteResponse response = this.client.getKVClient().delete(keySeq)
                                                 .get();
            return response.getDeleted();
```

### UnstableApiUsage
'get(io.etcd.jetcd.ByteSequence)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
        ByteSequence keySeq = ByteSequence.from(key, ENCODING);
        try {
            GetResponse response = this.kv.get(keySeq).get();
            if (response.getCount() > 0) {
                List<KeyValue> kvs = response.getKvs();
```

### UnstableApiUsage
'get(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.options.GetOption)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
                                           .withSortOrder(SortOrder.ASCEND)
                                           .build();
            GetResponse response = this.kv.get(prefixSeq, getOption).get();
            if (response.getCount() > 0) {
                return getResponseValues(response);
```

### UnstableApiUsage
'io.etcd.jetcd.KV' is marked unstable with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
    private final Client client;
    private final Watch watch;
    private final KV kv;

    public EtcdClient(String endpoints, String namespace) {
```

### UnstableApiUsage
'getKVClient()' is unstable because its signature references unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
                                                .withPrefix(prefixSeq).build();
        try {
            DeleteResponse response = this.client.getKVClient()
                                                 .delete(prefixSeq,
                                                         deleteOption)
```

### UnstableApiUsage
'delete(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.options.DeleteOption)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
        try {
            DeleteResponse response = this.client.getKVClient()
                                                 .delete(prefixSeq,
                                                         deleteOption)
                                                 .get();
```

### UnstableApiUsage
'io.etcd.jetcd.KV' is marked unstable with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java

    @VisibleForTesting
    protected KV getKv() {
        return this.kv;
    }
```

### UnstableApiUsage
'put(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.ByteSequence)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
                        "The value can't be null.");
        try {
            this.kv.put(ByteSequence.from(key, ENCODING),
                        ByteSequence.from(value))
                   .get();
```

### UnstableApiUsage
'get(io.etcd.jetcd.ByteSequence)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
        try {
            ByteSequence keySeq = ByteSequence.from(key, ENCODING);
            GetResponse response = this.kv.get(keySeq).get();
            if (response.getCount() > 0) {
                List<KeyValue> kvs = response.getKvs();
```

### UnstableApiUsage
'getKVClient()' is unstable because its signature references unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
                            .namespace(namespaceSeq).build();
        this.watch = this.client.getWatchClient();
        this.kv = this.client.getKVClient();
    }

```

### UnstableApiUsage
'get(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.options.GetOption)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-core/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClient.java`
#### Snippet
```java
                                       .build();
        try {
            GetResponse response = this.kv.get(prefixSeq, getOption).get();
            if (response.getCount() == count) {
                return getResponseValues(response);
```

### UnstableApiUsage
'io.etcd.jetcd.KV' is marked unstable with @Beta
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
    public void testGetWithRevision()
                throws ExecutionException, InterruptedException {
        KV kv = this.client.getKv();
        ByteSequence key1Seq = ByteSequence.from(KEY1, StandardCharsets.UTF_8);
        ByteSequence value1Seq = ByteSequence.from(VALUE1);
```

### UnstableApiUsage
'put(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.ByteSequence)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        ByteSequence value1Seq = ByteSequence.from(VALUE1);
        ByteSequence value2Seq = ByteSequence.from(VALUE2);
        PutResponse putResponse1 = kv.put(key1Seq, value1Seq).get();
        long revision1 = putResponse1.getHeader().getRevision();
        PutResponse putResponse2 = kv.put(key1Seq, value2Seq).get();
```

### UnstableApiUsage
'put(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.ByteSequence)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        PutResponse putResponse1 = kv.put(key1Seq, value1Seq).get();
        long revision1 = putResponse1.getHeader().getRevision();
        PutResponse putResponse2 = kv.put(key1Seq, value2Seq).get();
        long revision2 = putResponse2.getHeader().getRevision();
        long deleteCount1 = kv.delete(ByteSequence.from(KEY1,
```

### UnstableApiUsage
'delete(io.etcd.jetcd.ByteSequence)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        PutResponse putResponse2 = kv.put(key1Seq, value2Seq).get();
        long revision2 = putResponse2.getHeader().getRevision();
        long deleteCount1 = kv.delete(ByteSequence.from(KEY1,
                                                        StandardCharsets.UTF_8))
                              .get().getDeleted();
```

### UnstableApiUsage
'get(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.options.GetOption)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        GetOption getOption1 = GetOption.newBuilder().withRevision(revision1)
                                        .build();
        GetResponse getResponse1 = kv.get(key1Seq, getOption1).get();
        Assert.assertEquals(value1Seq,
                            getResponse1.getKvs().get(0).getValue());
```

### UnstableApiUsage
'get(io.etcd.jetcd.ByteSequence, io.etcd.jetcd.options.GetOption)' is declared in unstable interface 'io.etcd.jetcd.KV' marked with @Beta
in `computer-test/src/main/java/org/apache/hugegraph/computer/core/bsp/EtcdClientTest.java`
#### Snippet
```java
        GetOption getOption2 = GetOption.newBuilder().withRevision(revision2)
                                        .build();
        GetResponse getResponse2 = kv.get(key1Seq, getOption2).get();
        Assert.assertEquals(value2Seq,
                            getResponse2.getKvs().get(0).getValue());
```

### UnstableApiUsage
'stream(java.util.Iterator)' is marked unstable with @Beta
in `computer-test/src/main/java/org/apache/hugegraph/computer/algorithm/centrality/degree/DegreeCentralityTest.java`
#### Snippet
```java
            } else {
                Iterator<Edge> edges = vertex.edges().iterator();
                double totalValue = Streams.stream(edges).map(
                        (edge) -> {
                            DoubleValue weightValue = edge.property(this.weight);
```

